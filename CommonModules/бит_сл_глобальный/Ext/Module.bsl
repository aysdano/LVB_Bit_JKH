
//
// ГЛОБАЛЬНЫЙ МОДУЛЬ СИСТЕМЫ ЛИЦЕНЗИРОВАНИЯ 1.6
//

#Область СлужебныеПроцедурыИФункции

// Проверяет значение на Неопределено
//
// Параметры:
//  Значение	- Любой	- Значение, которое необходимо проверить на Неопредлеено.
// Возвращаемое значение:
//  Булево		- Обратный результат сравнения значения с неопределено
Функция бит_сл_ЗначениеОпределено(Знач Значение) Экспорт
	
	Возврат Значение <> Неопределено;
		
КонецФункции

#Область ОБРАБОТЧИКИ_ОЖИДАНИЯ

#Если ТонкийКлиент ИЛИ ВебКлиент ИЛИ ТолстыйКлиентУправляемоеПриложение ИЛИ ТолстыйКлиентОбычноеПриложение Тогда
	
	// Выполняет "Отложенную активацию продукта"
	// "Отложенная активация продукта" настраивается в окне системы лицензирования 1.6
	Процедура бит_сл_Подключаемый_ОтложеннаяАктивация() Экспорт
		
		// Проверка минимальной версии ассистента сервера лицензий
		РезультатПроверки = бит_сл_сервер_ассистент.ПроверитьНеобходимостьОбновленияАссистента();
		
		Если РезультатПроверки.ТребуетсяОбновление Тогда 
			
			бит_сл_общий.ВывестиСообщение(РезультатПроверки.Сообщение);	
			
		КонецЕсли; 
		
		
		// БИТ_ЖКХ 21.11.2016 10:50:44 Начало правки... 			
		//// Получение лицензии для текущего продукта
		//Если Не бит_сл_сервер.НетОшибок( бит_сл_сервер.ПолучитьЛицензиюДляТекущегоПродукта() ) Тогда 			
		//	
		//	// Обработка ошибки при получении лицензии
		//	бит_сл_клиент.ОткрытьФормуКлиентаЛицензирования();
		//КонецЕсли;
		
		// Получение лицензии для текущего продукта
		Если НЕ бит_ЗакрытыйСлужебныйСервер.ПродуктАктивирован() Тогда
			// Обработка ошибки при получении лицензии
			бит_сл_клиент.ОткрытьФормуКлиентаЛицензирования();
		Иначе
			бит_ЗакрытыйСлужебныйСервер.УстановитьПараметрыСеансабит_ПодомовойУчет();
		КонецЕсли;
		// БИТ_ЖКХ 21.11.2016 10:50:47 Конец правки. 
		
	КонецПроцедуры
	
	// Обеспечивает функционирование служебных подсистем
	//  
	Процедура бит_сл_Подключаемый_РасширениеВозможностейПлатформы() Экспорт
		
		бит_сл_сервер_ассистент.ВыполнитьПоддержкуРаботыКлиента();
		
	КонецПроцедуры 	
	
#КонецЕсли

#КонецОбласти

#КонецОбласти