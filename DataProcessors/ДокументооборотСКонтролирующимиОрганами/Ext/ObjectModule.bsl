
#Если Сервер Или ТолстыйКлиентОбычноеПриложение Или ВнешнееСоединение Тогда

Перем ПутьКОбъекту Экспорт;

Перем СоответствиеТипаСообщенияКраткомуПредставлению;

Перем ПолученныеПисьма Экспорт;
Перем ЗапросыПоКоторымПолученыОтветы Экспорт;

Перем КомпонентаЗагружена Экспорт;

#Область СлужебныеПроцедурыИФункции

Функция XMLДата(Значение, XMLТип = "xsd:date")
	
	Попытка
		Если XMLТип = "xsd:date" Тогда
			Возврат Формат(Значение, "ДФ=yyyy-MM-dd");
		Иначе
			Возврат XMLСтрока(Значение)
		КонецЕсли;
	Исключение
		Возврат Неопределено;
	КонецПопытки;
	
КонецФункции
 
Функция ПолучитьВерсиюТекущейПлатформы()
	
	ОбъектСисИнфо = Новый СистемнаяИнформация;
	Возврат СокрЛП(ОбъектСисИнфо.ВерсияПриложения);
	
КонецФункции

Функция ИнформацияОПрограмме() Экспорт
	
	Результат = Новый Структура();
	Результат.Вставить("ВерсияПлатформы",			ПолучитьВерсиюТекущейПлатформы());
	Результат.Вставить("ИмяМетаданныхКонфигурации",	СокрЛП(Метаданные.Имя));
	Результат.Вставить("ИДКонфигурации",			РегламентированнаяОтчетностьПереопределяемый.ИДКонфигурации());
	Результат.Вставить("ВерсияКонфигурации",		СокрЛП(бит_ОбщегоНазначенияПовтИсп.ПолучитьНомерРелиза_1С()));
	Возврат Результат;
	
КонецФункции

Функция ДанныеУЦ(Заявление) Экспорт
	
	ДополнительныеПараметры = Новый Структура();
	ДополнительныеПараметры.Вставить("НаименованиеУЦ", "");
	ДополнительныеПараметры.Вставить("РегламентУЦ", "");
	ДополнительныеПараметры.Вставить("ПолучательЗаявленияВУЦ", "");
	
	Если ЗначениеЗаполнено(Заявление.НаименованиеУЦ) И ЗначениеЗаполнено(Заявление.РегламентУЦ) И ЗначениеЗаполнено(Заявление.ПолучательЗаявленияВУЦ) Тогда
		ЗаполнитьЗначенияСвойств(ДополнительныеПараметры, Заявление);
	Иначе
		
		ПараметрыОператора = ПолучитьПараметрыОператораСвязи(Заявление.СпецоператорСвязи); 
		
		ДополнительныеПараметры.Вставить("НаименованиеУЦ", ПараметрыОператора.НаименованиеУЦ);
		ДополнительныеПараметры.Вставить("РегламентУЦ", ПараметрыОператора.АдресРегламентУЦ);
		ДополнительныеПараметры.Вставить("ПолучательЗаявленияВУЦ", ПараметрыОператора.ЗаявлениеПолучатель)
		
	КонецЕсли;
	
	Возврат ДополнительныеПараметры;
	
КонецФункции

Процедура РазобратьДанныеУЦ(Форма) Экспорт

	Если ЭтоАдресВременногоХранилища(Форма.АдресЗаданияПоПолучениюДанныхССервераКА) Тогда
		
		ДанныеССервераКА = ПолучитьИзВременногоХранилища(Форма.АдресЗаданияПоПолучениюДанныхССервераКА);
		Если ДанныеССервераКА <> Неопределено Тогда
			
			ДанныеУЦ = ДанныеССервераКА.ДанныеУЦ;
			
			Если ДанныеУЦ <> Неопределено Тогда
				
				Форма.НаименованиеУЦ    = ДанныеУЦ.НаименованиеУЦ;
				Форма.РегламентУЦ     = ДанныеУЦ.РегламентУЦ;
				Форма.ПолучательЗаявленияВУЦ = ДанныеУЦ.ПолучательЗаявленияВУЦ;
				
			КонецЕсли;
			
		КонецЕсли;
		
	КонецЕсли;
		
	Если НЕ ЗначениеЗаполнено(Форма.НаименованиеУЦ) И НЕ ЗначениеЗаполнено(Форма.РегламентУЦ) И НЕ ЗначениеЗаполнено(Форма.ПолучательЗаявленияВУЦ) Тогда
		
		// Данные уже нужны, но фоновое задание не успело их вернуть.
		// В этом случае берем из макета
		КонтекстЭДОСервер  = ДокументооборотСКО.ПолучитьОбработкуЭДО();
		ПараметрыОператора = КонтекстЭДОСервер.ПолучитьПараметрыОператораСвязи(Форма.Спецоператор); 
		
		Форма.НаименованиеУЦ 		 = ПараметрыОператора.НаименованиеУЦ;
		Форма.РегламентУЦ 			 = ПараметрыОператора.АдресРегламентУЦ;
		Форма.ПолучательЗаявленияВУЦ = ПараметрыОператора.ЗаявлениеПолучатель;
		
	КонецЕсли;
	
КонецПроцедуры

Функция CNsСУЦПоАдресуЗадания(АдресЗадания) Экспорт

	CNs = Новый Массив;
	Если ЭтоАдресВременногоХранилища(АдресЗадания) Тогда
		
		ДанныеССервераКА = ПолучитьИзВременногоХранилища(АдресЗадания);
		Если ДанныеССервераКА <> Неопределено Тогда
			
			ДоверенныеСторонниеУЦ = ДанныеССервераКА.ДоверенныеСторонниеУЦ;
			
			Если ДоверенныеСторонниеУЦ <> Неопределено Тогда
				
				CNs = CNsДоверенныхСтороннихУЦ(ДоверенныеСторонниеУЦ);

			КонецЕсли;
			
		КонецЕсли;
		
	КонецЕсли;
		
	Возврат Новый ФиксированныйМассив(CNs);
	
КонецФункции

Функция ЭлектронныйДокументооборотИспользуется(УчетнаяЗапись = Неопределено) Экспорт
	
	Запрос = Новый Запрос("ВЫБРАТЬ РАЗРЕШЕННЫЕ
	                      |	КОЛИЧЕСТВО(Организации.Ссылка) КАК Ссылка
	                      |ИЗ
	                      |	Справочник.Организации КАК Организации
	                      |ГДЕ
	                      |	Организации.ВидОбменаСКонтролирующимиОрганами = &ВидОбменаСКонтролирующимиОрганами
	                      |" +
	                      ?(УчетнаяЗапись = Неопределено, 
	                      "	И Организации.УчетнаяЗаписьОбмена <> &ПустаяУчетнаяЗаписьОбмена",
	                      "	И Организации.УчетнаяЗаписьОбмена = &УчетнаяЗаписьОбмена") +
	                      "
	                      |	И НЕ Организации.ПометкаУдаления
	                      |	И НЕ Организации.УчетнаяЗаписьОбмена.ПометкаУдаления");
	Запрос.УстановитьПараметр("ВидОбменаСКонтролирующимиОрганами", Перечисления.ВидыОбменаСКонтролирующимиОрганами.ОбменВУниверсальномФормате);
	Если УчетнаяЗапись = Неопределено Тогда 
		Запрос.УстановитьПараметр("ПустаяУчетнаяЗаписьОбмена", Справочники.УчетныеЗаписиДокументооборота.ПустаяСсылка());
	Иначе
		Запрос.УстановитьПараметр("УчетнаяЗаписьОбмена", УчетнаяЗапись);
	КонецЕсли;
	
	Попытка
		Выборка = Запрос.Выполнить().Выбрать();
	Исключение
		Возврат Ложь;
	КонецПопытки;
	
	Если Выборка.Следующий() Тогда
		Возврат ЗначениеЗаполнено(Выборка.Получить(0));
	Иначе
		Возврат Ложь;
	КонецЕсли;
	
КонецФункции

Функция НовыйКонтекстРасшифровки(Контекст)
	Возврат Новый ФиксированнаяСтруктура("ПриРасшифровке", Контекст);
КонецФункции

Функция НовыйКонтекстОбработки(Контекст)
	Возврат Новый ФиксированнаяСтруктура("ПриОбработке", Контекст);
КонецФункции

Процедура ОбработкаПолученияФормы(ВидОбъекта, ИмяОбъекта, ВидФормы, Параметры, ВыбраннаяФорма, ДополнительнаяИнформация, СтандартнаяОбработка) Экспорт
	
	Если ВидФормы = "ФормаОбъекта" Тогда
		Если ВидОбъекта = "Справочник" Тогда
			ИмяВидаФормы = "ФормаЭлемента";
		ИначеЕсли ВидОбъекта = "Документ" Тогда
			ИмяВидаФормы = "ФормаДокумента";
		КонецЕсли;
	ИначеЕсли ВидФормы = "ФормаВыбора" Тогда
		ИмяВидаФормы = "ФормаСписка";
	Иначе
		ИмяВидаФормы = ВидФормы;
	КонецЕсли;
	
	Если ФормаЗаявленияПодменена(ВидОбъекта, ИмяОбъекта, ВидФормы, Параметры, ВыбраннаяФорма, ДополнительнаяИнформация, СтандартнаяОбработка, ИмяВидаФормы) Тогда
		Возврат;
	КонецЕсли;
	
	ИмяПодменяющейФормы = ВидОбъекта + "_" + ИмяОбъекта + "_" + ИмяВидаФормы;
	ПодменяющаяФорма = Метаданные().Формы.Найти(ИмяПодменяющейФормы);
	Если ПодменяющаяФорма <> Неопределено Тогда
		СтандартнаяОбработка = Ложь;
		ВыбраннаяФорма = ПодменяющаяФорма;
	КонецЕсли;
	
КонецПроцедуры

Функция ФормаЗаявленияПодменена(ВидОбъекта, ИмяОбъекта, ВидФормы, Параметры, ВыбраннаяФорма, ДополнительнаяИнформация, СтандартнаяОбработка, ИмяВидаФормы)
	
	Если ВидОбъекта = "Документ" И ИмяОбъекта = "ЗаявлениеАбонентаСпецоператораСвязи" Тогда
		
		СтандартнаяОбработка = Ложь;
		
		ЭтоРучноеЗаявление = ТипЗнч(Параметры) = Тип("Структура") И Параметры.Свойство("РучнойВвод");
		
		// Если есть ключ, значит это не открытие нового
		Если ТипЗнч(Параметры) = Тип("Структура") И Параметры.Свойство("Ключ") Тогда
			Ключ = Параметры.Ключ;
			ЭтоУпрощенноеЗаявление = Ключ.ЭтоУпрощенноеЗаявление;
			ЭтоНовый = Ложь;
			ЭтоНаИзменение = Ключ.ТипЗаявления = Перечисления.ТипыЗаявленияАбонентаСпецоператораСвязи.Изменение;
			Отправлено = Ключ.Статус <> Перечисления.СтатусыЗаявленияАбонентаСпецоператораСвязи.Подготовлено 
				И Ключ.Статус <> Перечисления.СтатусыЗаявленияАбонентаСпецоператораСвязи.ПустаяСсылка();
		Иначе
			Ключ = Неопределено;
			ЭтоУпрощенноеЗаявление = Ложь;
			ЭтоНовый = Истина;
			ЭтоНаИзменение = Ложь;
			Отправлено = Истина;
		КонецЕсли;
		
		ОпределятьФормуНаКлиенте = НЕ ЭтоРучноеЗаявление И ЭтоНовый ИЛИ ЭтоУпрощенноеЗаявление ИЛИ НЕ ЭтоНовый И ЭтоНаИзменение И НЕ Отправлено;
		
		Если ИмяВидаФормы = "ФормаДокумента" И ОпределятьФормуНаКлиенте Тогда
			// Для определения, какую форму открыть - первичное, вторичное заявление, упрощенное или ручное
			ВыбраннаяФорма = "Обработка.ДокументооборотСКонтролирующимиОрганами.Форма.Мастер_ОпределениеОткрываемойФормы";
		Иначе
			ИмяПодменяющейФормы = ВидОбъекта + "_" + ИмяОбъекта + "_" + ИмяВидаФормы;
			ВыбраннаяФорма = "Обработка.ДокументооборотСКонтролирующимиОрганами.Форма." + ИмяПодменяющейФормы;
		КонецЕсли;
		
		Возврат Истина;
	Иначе
		
		Возврат Ложь;
		
	КонецЕсли;
	
КонецФункции

Функция ПолучитьСообщенияВведенныеНаОсновании(Сообщение, Тип = Неопределено) Экспорт
	
	Результат = Новый Массив;
	
	Запрос = Новый Запрос("ВЫБРАТЬ РАЗРЕШЕННЫЕ
						  |	ТранспортноеСообщение.Ссылка
						  |ИЗ
						  |	Документ.ТранспортноеСообщение КАК ТранспортноеСообщение
						  |ГДЕ
						  |	ТранспортноеСообщение.Основание = &Основание
						  |	И ТранспортноеСообщение.ПометкаУдаления = &ПометкаУдаления");
	Запрос.УстановитьПараметр("Основание", Сообщение);
	Запрос.УстановитьПараметр("ПометкаУдаления", Ложь);
	
	Если Тип <> Неопределено Тогда
		Запрос.Текст = Запрос.Текст + "
						  |	И ТранспортноеСообщение.Тип = &Тип";
		Запрос.УстановитьПараметр("Тип", Тип);
	КонецЕсли;
	
	Выборка = Запрос.Выполнить().Выбрать();
	Пока Выборка.Следующий() Цикл
		Результат.Добавить(Выборка.Ссылка);
	КонецЦикла;
	
	Возврат Результат;
	
КонецФункции

Функция ПредставлениеТранспортногоСообщения(Сообщение) Экспорт
	
	Если ЗначениеЗаполнено(Сообщение.ЦиклОбмена) Тогда
		Возврат "" + Сообщение.Тип + ", связанное с циклом обмена " + Сообщение.ЦиклОбмена;
	Иначе
		Возврат Сообщение;
	КонецЕсли;
	
КонецФункции

Функция ПредставлениеСообщения(Сообщение) Экспорт
	
	Тема = ?(ЗначениеЗаполнено(Сообщение.Тема), СтрШаблон(НСтр("ru = '""%1""'"), Сообщение.Тема), НСтр("ru = '<тема не указана>'"));
	Если ЗначениеЗаполнено(Сообщение.Дата) Тогда
		Возврат СтрШаблон(НСтр("ru = '%1 от %2'"), Тема, Сообщение.Дата);
	Иначе
		Возврат Тема;
	КонецЕсли;
	
КонецФункции

Функция ПолучитьСообщенияЦиклаОбмена(ЦиклОбмена, ТипыСообщений = Неопределено, ПомеченныеНаУдаление = Ложь, ТолькоЗавершенные = Ложь) Экспорт
	
	Запрос = Новый Запрос("ВЫБРАТЬ РАЗРЕШЕННЫЕ
						  |	ТранспортноеСообщение.Ссылка,
						  |	ТранспортноеСообщение.Статус,
						  |	ТранспортноеСообщение.Тип,
						  |	ТранспортноеСообщение.ПометкаУдаления,
						  |	ТранспортноеСообщение.Дата,
						  |	ТранспортноеСообщение.ИдентификаторСообщения,
						  |	ТранспортноеСообщение.ДатаТранспорта,
						  |	ТранспортноеСообщение.Основание,
						  |	ТранспортноеСообщение.ПротоколСОшибкой,
						  |	ТранспортноеСообщение.Тема
						  |ИЗ
						  |	Документ.ТранспортноеСообщение КАК ТранспортноеСообщение
						  |ГДЕ
						  |	ТранспортноеСообщение.ЦиклОбмена В (&ЦиклОбмена)");
	Запрос.УстановитьПараметр("ЦиклОбмена", ЦиклОбмена);
	
	Если НЕ ПомеченныеНаУдаление Тогда
		Запрос.Текст = Запрос.Текст + "
							  |	И ТранспортноеСообщение.ПометкаУдаления = &ПометкаУдаления";
		Запрос.УстановитьПараметр("ПометкаУдаления", Ложь);
	КонецЕсли;
	
	Если ТипыСообщений <> Неопределено Тогда
		Запрос.Текст = Запрос.Текст + "
							  |	И ТранспортноеСообщение.Тип В (&ТипыСообщений)";
		Запрос.УстановитьПараметр("ТипыСообщений", ТипыСообщений);
	КонецЕсли;
	
	Если ТолькоЗавершенные Тогда
		Запрос.Текст = Запрос.Текст + "
							  |	И ТранспортноеСообщение.Статус <> Значение(Перечисление.СтатусыПисем.Исходящее)";
		Запрос.УстановитьПараметр("ТипыСообщений", ТипыСообщений);
	КонецЕсли;	
	
	Запрос.Текст = Запрос.Текст + "
						|УПОРЯДОЧИТЬ ПО
						|	ТранспортноеСообщение.ДатаТранспорта УБЫВ";
	
	Возврат Запрос.Выполнить().Выгрузить();
	
КонецФункции

Процедура ЗаписатьСтатусОтправкиОбъекта(ДокументСсылкаИлиМассив, Статус) Экспорт
	
	Если ТипЗнч(ДокументСсылкаИлиМассив) = Тип("Массив") Тогда
		
		Для Каждого ДокументСсылка Из ДокументСсылкаИлиМассив Цикл
			ЗаписатьСтатусОтправкиЕдиничногоОбъекта(ДокументСсылка, Статус);
			ЭлектронныйДокументооборотСКонтролирующимиОрганамиВызовСервераПереопределяемый.ПриИзмененииСтатусаОтправкиДокумента(ДокументСсылка, Статус);
		КонецЦикла;
				
	Иначе
		
		ЗаписатьСтатусОтправкиЕдиничногоОбъекта(ДокументСсылкаИлиМассив, Статус);				
		ЭлектронныйДокументооборотСКонтролирующимиОрганамиВызовСервераПереопределяемый.ПриИзмененииСтатусаОтправкиДокумента(ДокументСсылкаИлиМассив, Статус);
		
	КонецЕсли;
	
КонецПроцедуры

Функция ПолучитьСтатусОтправкиОбъекта(Док) Экспорт
	
	Запрос = Новый Запрос("ВЫБРАТЬ РАЗРЕШЕННЫЕ
						|	СтатусыОтправки.Статус
						|ИЗ
						|	РегистрСведений.СтатусыОтправки КАК СтатусыОтправки
						|ГДЕ
						|	СтатусыОтправки.Объект = &Объект");
	Запрос.УстановитьПараметр("Объект", Док);
	Выборка = Запрос.Выполнить().Выбрать();
	Возврат ?(Выборка.Следующий(), Выборка.Получить(0), Перечисления.СтатусыОтправки.ПустаяСсылка());
	
КонецФункции

Функция ПолучитьСтатусыОтправкиОбъектов(Объекты) Экспорт
	
	Результат = Новый Соответствие;
	
	Запрос = Новый Запрос("ВЫБРАТЬ РАЗРЕШЕННЫЕ
						|	СтатусыОтправки.Объект,
						|	СтатусыОтправки.Статус
						|ИЗ
						|	РегистрСведений.СтатусыОтправки КАК СтатусыОтправки
						|ГДЕ
						|	СтатусыОтправки.Объект В (&Объект)");
	Запрос.УстановитьПараметр("Объект", Объекты);
	Выборка = Запрос.Выполнить().Выбрать();
	Пока Выборка.Следующий() Цикл
		Результат.Вставить(Выборка.Объект, Выборка.Статус);
	КонецЦикла;
	
	Возврат Результат;
	
КонецФункции

Функция ПолучитьПустуюДатуЗавершенияЦиклаОбмена() Экспорт
	
	Возврат '39991231235959';
	
КонецФункции

Функция СформироватьСтрокуСостоянияТранспортногоСообщения(Сообщение) Экспорт
	
	Если Сообщение = Неопределено ИЛИ Сообщение = Документы.ТранспортноеСообщение.ПустаяСсылка() ИЛИ НЕ ЗначениеЗаполнено(Сообщение.Статус) Тогда
		Возврат "<Подтверждение отсутствует, нажмите для создания>";
	КонецЕсли;
	
	СоответствиеТипаСообщенияПредставлению = СоответствиеТипаСообщенияПредставлению();
	
	СоответствиеТипаСообщенияОкончанию = Новый Соответствие;
	СоответствиеТипаСообщенияОкончанию.Вставить(Перечисления.ТипыТранспортныхСообщений.ДекларацияНП,																		"о");
	СоответствиеТипаСообщенияОкончанию.Вставить(Перечисления.ТипыТранспортныхСообщений.ИзвещениеДекларацияНО,																"о");
	СоответствиеТипаСообщенияОкончанию.Вставить(Перечисления.ТипыТранспортныхСообщений.РезультатПриемаДекларацияНО,															"");
	СоответствиеТипаСообщенияОкончанию.Вставить(Перечисления.ТипыТранспортныхСообщений.ИзвещениеРезультатПриемаНП,															"о");
	СоответствиеТипаСообщенияОкончанию.Вставить(Перечисления.ТипыТранспортныхСообщений.РезультатОбработкиДекларацияНО,														"");
	СоответствиеТипаСообщенияОкончанию.Вставить(Перечисления.ТипыТранспортныхСообщений.ИзвещениеРезультатОбработкиНП,														"о");
	СоответствиеТипаСообщенияОкончанию.Вставить(Перечисления.ТипыТранспортныхСообщений.ПодтверждениеДекларацияНО,															"о");
	СоответствиеТипаСообщенияОкончанию.Вставить(Перечисления.ТипыТранспортныхСообщений.ИзвещениеПодтверждениеНП,															"о");
	
	СоответствиеТипаСообщенияОкончанию.Вставить(Перечисления.ТипыТранспортныхСообщений.Форма2НДФЛНП,																		"о");
	СоответствиеТипаСообщенияОкончанию.Вставить(Перечисления.ТипыТранспортныхСообщений.ПодтверждениеФорма2НДФЛНО,															"о");
	СоответствиеТипаСообщенияОкончанию.Вставить(Перечисления.ТипыТранспортныхСообщений.ИзвещениеФорма2НДФЛНО,																"о");
	СоответствиеТипаСообщенияОкончанию.Вставить(Перечисления.ТипыТранспортныхСообщений.РезультатПриемаФорма2НДФЛНО,															"");
	
	СоответствиеТипаСообщенияОкончанию.Вставить(Перечисления.ТипыТранспортныхСообщений.ОбращениеНП,																			"о");
	СоответствиеТипаСообщенияОкончанию.Вставить(Перечисления.ТипыТранспортныхСообщений.ИзвещениеОбращениеНО,																"о");
	СоответствиеТипаСообщенияОкончанию.Вставить(Перечисления.ТипыТранспортныхСообщений.ПодтверждениеОбращениеНО,															"о");
	СоответствиеТипаСообщенияОкончанию.Вставить(Перечисления.ТипыТранспортныхСообщений.РезультатПриемаОбращениеНО,															"");
	
	СоответствиеТипаСообщенияОкончанию.Вставить(Перечисления.ТипыТранспортныхСообщений.ПредставлениеНП,																		"о");
	СоответствиеТипаСообщенияОкончанию.Вставить(Перечисления.ТипыТранспортныхСообщений.ИзвещениеПредставлениеНО,															"о");
	СоответствиеТипаСообщенияОкончанию.Вставить(Перечисления.ТипыТранспортныхСообщений.ПодтверждениеПредставлениеНО,														"о");
	СоответствиеТипаСообщенияОкончанию.Вставить(Перечисления.ТипыТранспортныхСообщений.РезультатПриемаПредставлениеНО,														"");
	
	СоответствиеТипаСообщенияОкончанию.Вставить(Перечисления.ТипыТранспортныхСообщений.ПисьмоНО,																			"о");
	СоответствиеТипаСообщенияОкончанию.Вставить(Перечисления.ТипыТранспортныхСообщений.ИзвещениеПисьмоНП,																	"о");
	
	СоответствиеТипаСообщенияОкончанию.Вставить(Перечисления.ТипыТранспортныхСообщений.РассылкаНО,																			"а");
	СоответствиеТипаСообщенияОкончанию.Вставить(Перечисления.ТипыТранспортныхСообщений.ИзвещениеРассылкаНП,																	"о");
	
	СоответствиеТипаСообщенияОкончанию.Вставить(Перечисления.ТипыТранспортныхСообщений.ПервичноеСообщениеСодержащееОтчетностьПФР,											"о");
	СоответствиеТипаСообщенияОкончанию.Вставить(Перечисления.ТипыТранспортныхСообщений.ПодтверждениеПолученияОтчетностиПФР,													"а");
	СоответствиеТипаСообщенияОкончанию.Вставить(Перечисления.ТипыТранспортныхСообщений.ПротоколПФР,																			"");
	СоответствиеТипаСообщенияОкончанию.Вставить(Перечисления.ТипыТранспортныхСообщений.ПротоколКвитанцияПФР,																"а");
	
	СоответствиеТипаСообщенияОкончанию.Вставить(Перечисления.ТипыТранспортныхСообщений.ПервичноеСообщениеСодержащееЗапросПФР,												"о");
	СоответствиеТипаСообщенияОкончанию.Вставить(Перечисления.ТипыТранспортныхСообщений.ПодтверждениеПолученияЗапросаПФР,													"а");
	СоответствиеТипаСообщенияОкончанию.Вставить(Перечисления.ТипыТранспортныхСообщений.ОтветНаЗапросПФР,																	"");
	СоответствиеТипаСообщенияОкончанию.Вставить(Перечисления.ТипыТранспортныхСообщений.ОтветНаЗапросКвитанцияПФР,															"а");
	
	СоответствиеТипаСообщенияОкончанию.Вставить(Перечисления.ТипыТранспортныхСообщений.ПисьмоВходящееПФР,																	"о");
	СоответствиеТипаСообщенияОкончанию.Вставить(Перечисления.ТипыТранспортныхСообщений.ПисьмоВходящееКвитанцияПФР,															"а");
	
	СоответствиеТипаСообщенияОкончанию.Вставить(Перечисления.ТипыТранспортныхСообщений.ПисьмоИсходящееПФР,																	"о");
	СоответствиеТипаСообщенияОкончанию.Вставить(Перечисления.ТипыТранспортныхСообщений.ПисьмоИсходящееКвитанцияПФР,															"а");
	
	СоответствиеТипаСообщенияОкончанию.Вставить(Перечисления.ТипыТранспортныхСообщений.ЗапросНП,																			"о");
	СоответствиеТипаСообщенияОкончанию.Вставить(Перечисления.ТипыТранспортныхСообщений.ИзвещениеЗапросНО,																	"о");
	СоответствиеТипаСообщенияОкончанию.Вставить(Перечисления.ТипыТранспортныхСообщений.РезультатПриемаЗапросНО,																"");
	СоответствиеТипаСообщенияОкончанию.Вставить(Перечисления.ТипыТранспортныхСообщений.ИзвещениеРезультатПриемаНП,															"о");
	СоответствиеТипаСообщенияОкончанию.Вставить(Перечисления.ТипыТранспортныхСообщений.РезультатОбработкиЗапросНО,															"");
	СоответствиеТипаСообщенияОкончанию.Вставить(Перечисления.ТипыТранспортныхСообщений.ИзвещениеРезультатОбработкиНП,														"о");
	СоответствиеТипаСообщенияОкончанию.Вставить(Перечисления.ТипыТранспортныхСообщений.ПодтверждениеЗапросНО,																"о");
	СоответствиеТипаСообщенияОкончанию.Вставить(Перечисления.ТипыТранспортныхСообщений.ИзвещениеПодтверждениеНП,															"о");
	
	СоответствиеТипаСообщенияОкончанию.Вставить(Перечисления.ТипыТранспортныхСообщений.ПервичноеСообщениеСодержащееОтчетностьФСГС,											"о");
	СоответствиеТипаСообщенияОкончанию.Вставить(Перечисления.ТипыТранспортныхСообщений.ПодтверждениеДатыОтправкиФСГС,														"о");
	СоответствиеТипаСообщенияОкончанию.Вставить(Перечисления.ТипыТранспортныхСообщений.ИзвещениеПодтверждениеДатыОтправкиФСГС,												"о");
	СоответствиеТипаСообщенияОкончанию.Вставить(Перечисления.ТипыТранспортныхСообщений.ИзвещениеОПолученииОтчетностиФСГС,													"о");
	СоответствиеТипаСообщенияОкончанию.Вставить(Перечисления.ТипыТранспортныхСообщений.ПротоколВходногоКонтроляОтчетностиФСГС,												"о");
	СоответствиеТипаСообщенияОкончанию.Вставить(Перечисления.ТипыТранспортныхСообщений.ИзвещениеПротоколВходногоКонтроляОтчетностиФСГС,										"о");
	
	СоответствиеТипаСообщенияОкончанию.Вставить(Перечисления.ТипыТранспортныхСообщений.ПисьмоИсходящееФСГС,																	"о");
	СоответствиеТипаСообщенияОкончанию.Вставить(Перечисления.ТипыТранспортныхСообщений.ИзвещениеПисьмоИсходящееФСГС,														"о");
	СоответствиеТипаСообщенияОкончанию.Вставить(Перечисления.ТипыТранспортныхСообщений.ПисьмоВходящееФСГС,																	"о");
	СоответствиеТипаСообщенияОкончанию.Вставить(Перечисления.ТипыТранспортныхСообщений.РассылкаФСГС,																		"о");
	СоответствиеТипаСообщенияОкончанию.Вставить(Перечисления.ТипыТранспортныхСообщений.РассылкаШаблоновФСГС,																"о");
	СоответствиеТипаСообщенияОкончанию.Вставить(Перечисления.ТипыТранспортныхСообщений.ИзвещениеПисьмоВходящееФСГС,															"о");
	СоответствиеТипаСообщенияОкончанию.Вставить(Перечисления.ТипыТранспортныхСообщений.УведомлениеОбОшибкеФСГС,																"о");
	
	СоответствиеТипаСообщенияОкончанию.Вставить(Перечисления.ТипыТранспортныхСообщений.КритическаяОшибка,																	"о");
	СоответствиеТипаСообщенияОкончанию.Вставить(Перечисления.ТипыТранспортныхСообщений.УведомлениеОбОшибкеПФР,																"о");
	СоответствиеТипаСообщенияОкончанию.Вставить(Перечисления.ТипыТранспортныхСообщений.УведомлениеОбОшибкеФНС,																"о");
	СоответствиеТипаСообщенияОкончанию.Вставить(Перечисления.ТипыТранспортныхСообщений.Прочее,																				"о");
	СоответствиеТипаСообщенияОкончанию.Вставить(Перечисления.ТипыТранспортныхСообщений.ПустаяСсылка(),																		"о");
	
	СоответствиеТипаСообщенияОкончанию.Вставить(Перечисления.ТипыТранспортныхСообщений.ЗаявлениеНП,																			"о");
	СоответствиеТипаСообщенияОкончанию.Вставить(Перечисления.ТипыТранспортныхСообщений.РезультатПриемаЗаявлениеНО,															"");
	СоответствиеТипаСообщенияОкончанию.Вставить(Перечисления.ТипыТранспортныхСообщений.РезультатОбработкиЗаявлениеРФНО,														"");
	СоответствиеТипаСообщенияОкончанию.Вставить(Перечисления.ТипыТранспортныхСообщений.РезультатОбработкиЗаявлениеТСНО,														"");
	
	СоответствиеТипаСообщенияОкончанию.Вставить(Перечисления.ТипыТранспортныхСообщений.РезультатОбработкиЗаявлениеНО,														"");
	
	СоответствиеТипаСообщенияОкончанию.Вставить(Перечисления.ТипыТранспортныхСообщений.ДокументНО,																			"");
	СоответствиеТипаСообщенияОкончанию.Вставить(Перечисления.ТипыТранспортныхСообщений.ИзвещениеДокументНП,																	"о");
	СоответствиеТипаСообщенияОкончанию.Вставить(Перечисления.ТипыТранспортныхСообщений.РезультатПриемаДокументНП,															"");
	СоответствиеТипаСообщенияОкончанию.Вставить(Перечисления.ТипыТранспортныхСообщений.ИзвещениеРезультатПриемаНО,															"о");
	
	СоответствиеТипаСообщенияОкончанию.Вставить(Перечисления.ТипыТранспортныхСообщений.ЗапросНаВыпискуЕРГЮЛ_ЕГРИП,															"о");
	СоответствиеТипаСообщенияОкончанию.Вставить(Перечисления.ТипыТранспортныхСообщений.РезультатОбработкиЗапросаНаВыпискуЕРГЮЛ_ЕГРИП,										"о");
	
	ПредставлениеТипа = СоответствиеТипаСообщенияПредставлению[Сообщение.Тип];
	Если ПредставлениеТипа = Неопределено Тогда
		ПредставлениеТипа = "Сообщение неизвестного типа";
	КонецЕсли;
	
	ТипСообщения = Сообщение.Тип;
	
	Статус = Сообщение.Статус;
	Окончание = СоответствиеТипаСообщенияОкончанию[ТипСообщения];
	Если Окончание = Неопределено Тогда
		Окончание = "о";
	КонецЕсли;
	
	ЭтоОшибкаИлиПрочееСообщение = (ТипСообщения = Перечисления.ТипыТранспортныхСообщений.КритическаяОшибка
									ИЛИ ТипСообщения = Перечисления.ТипыТранспортныхСообщений.УведомлениеОбОшибкеПФР
									ИЛИ ТипСообщения = Перечисления.ТипыТранспортныхСообщений.УведомлениеОбОшибкеФНС
									ИЛИ ТипСообщения = Перечисления.ТипыТранспортныхСообщений.УведомлениеОбОшибкеФСГС
									ИЛИ ТипСообщения = Перечисления.ТипыТранспортныхСообщений.Прочее);
	
	Если Статус = Перечисления.СтатусыПисем.Сохраненное Тогда
		ТекстДопИнформация = ПредставлениеТипа + ". Создан" + Окончание + ?(НЕ ЭтоОшибкаИлиПрочееСообщение И СообщениеЗашифровано(Сообщение.Ссылка), ", зашифрован" + Окончание, "") + ", но не отправлен" + Окончание + ".";
	ИначеЕсли Статус = Перечисления.СтатусыПисем.Исходящее Тогда
		ТекстДопИнформация = ПредставлениеТипа + ". Создан" + Окончание + ?(НЕ ЭтоОшибкаИлиПрочееСообщение И СообщениеЗашифровано(Сообщение.Ссылка), ", зашифрован" + Окончание, "") + ", но не отправлен" + Окончание + ".";
	ИначеЕсли Статус = Перечисления.СтатусыПисем.Отправленное Тогда
		ТекстДопИнформация = ПредставлениеТипа + ". Отправлен" + Окончание + " " + Формат(Сообщение.ДатаТранспорта, "ДФ='дд.ММ.гггг ЧЧ:мм:сс'") + ".";
	ИначеЕсли Статус = Перечисления.СтатусыПисем.Полученное Тогда
		ТекстДопИнформация = ПредставлениеТипа + ". Получен" + Окончание + " " + Формат(Сообщение.ДатаТранспорта, "ДФ='дд.ММ.гггг ЧЧ:мм:сс'") + "." + ?(ЭтоОшибкаИлиПрочееСообщение ИЛИ СообщениеРасшифровано(Сообщение.Ссылка), "", " Не расшифрован" + Окончание + ".");
	КонецЕсли;
	
	Возврат ТекстДопИнформация;
	
КонецФункции

Функция СписокДопустимыхОрганизацийВОбъектахОбмена() Экспорт
	
	Запрос = Новый Запрос("ВЫБРАТЬ РАЗРЕШЕННЫЕ РАЗЛИЧНЫЕ
	                      |	Организации.Ссылка КАК Ссылка,
	                      |	Организации.Наименование КАК Наименование,
	                      |	Организации.УчетнаяЗаписьОбмена КАК УчетнаяЗаписьОбмена
	                      |ИЗ
	                      |	Справочник.Организации КАК Организации
	                      |ГДЕ
	                      |	Организации.ВидОбменаСКонтролирующимиОрганами = &ВидОбменаСКонтролирующимиОрганами
	                      |
	                      |УПОРЯДОЧИТЬ ПО
	                      |	Организации.Наименование");
	
	
	Запрос.УстановитьПараметр("ВидОбменаСКонтролирующимиОрганами", Перечисления.ВидыОбменаСКонтролирующимиОрганами.ОбменВУниверсальномФормате);
	Запрос.УстановитьПараметр("ПустаяУчетнаяЗапись", Справочники.УчетныеЗаписиДокументооборота.ПустаяСсылка());
	Запрос.УстановитьПараметр("ПометкаУдаления", Ложь);

	Возврат Запрос.Выполнить().Выгрузить().ВыгрузитьКолонку("Ссылка");
	
КонецФункции

Функция ОрганизацииПоУчетнымЗаписямПользователя() Экспорт
	
	Запрос = Новый Запрос("ВЫБРАТЬ РАЗРЕШЕННЫЕ РАЗЛИЧНЫЕ
						  |	Организации.Ссылка КАК Ссылка,
						  |	Организации.Наименование
						  |ИЗ
						  |	РегистрСведений.ПользователиУчетныхЗаписейДокументооборота КАК ПользователиУчетныхЗаписейДокументооборота
						  |		ЛЕВОЕ СОЕДИНЕНИЕ (ВЫБРАТЬ РАЗЛИЧНЫЕ
						  |			Организации.Ссылка КАК Ссылка,
						  |			Организации.Наименование КАК Наименование,
						  |			Организации.УчетнаяЗаписьОбмена КАК УчетнаяЗаписьОбмена
						  |		ИЗ
						  |			Справочник.Организации КАК Организации
						  |		ГДЕ
						  |			Организации.ВидОбменаСКонтролирующимиОрганами = &ВидОбменаСКонтролирующимиОрганами
						  |			И Организации.УчетнаяЗаписьОбмена <> &ПустаяУчетнаяЗапись) КАК Организации
						  |		ПО ПользователиУчетныхЗаписейДокументооборота.УчетнаяЗапись = Организации.УчетнаяЗаписьОбмена
						  |ГДЕ
						  |	ПользователиУчетныхЗаписейДокументооборота.УчетнаяЗапись.ПометкаУдаления = &ПометкаУдаления
						  |	И ПользователиУчетныхЗаписейДокументооборота.Пользователь = &Пользователь
						  |	И (НЕ Организации.Ссылка ЕСТЬ NULL)
						  |
						  |УПОРЯДОЧИТЬ ПО
						  |	Организации.Наименование");
	
	Запрос.УстановитьПараметр("ВидОбменаСКонтролирующимиОрганами", Перечисления.ВидыОбменаСКонтролирующимиОрганами.ОбменВУниверсальномФормате);
	Запрос.УстановитьПараметр("ПустаяУчетнаяЗапись", Справочники.УчетныеЗаписиДокументооборота.ПустаяСсылка());
	Запрос.УстановитьПараметр("ПометкаУдаления", Ложь);
	Запрос.УстановитьПараметр("Пользователь", Пользователи.ТекущийПользователь());

	Возврат Запрос.Выполнить().Выгрузить().ВыгрузитьКолонку("Ссылка");
	
КонецФункции

Функция ПоказатьСообщение(ЦиклОбмена, ТипСообщения) Экспорт
	
	// выбираем все сообщения цикла обмена
	Запрос = Новый Запрос("ВЫБРАТЬ РАЗРЕШЕННЫЕ
						  |	ТранспортноеСообщение.Ссылка,
						  |	ТранспортноеСообщение.ДатаТранспорта,
						  |	ТранспортноеСообщение.Тип,
						  |	ТранспортноеСообщение.УчетнаяЗапись,
						  |	ТранспортноеСообщение.Статус
						  |ИЗ
						  |	Документ.ТранспортноеСообщение КАК ТранспортноеСообщение
						  |ГДЕ
						  |	ТранспортноеСообщение.ЦиклОбмена = &ЦиклОбмена
	                      |	И ТранспортноеСообщение.ПометкаУдаления = ЛОЖЬ
						  |
						  |УПОРЯДОЧИТЬ ПО
						  |	ТранспортноеСообщение.Дата УБЫВ");
	Запрос.УстановитьПараметр("ЦиклОбмена", ЦиклОбмена);
	СообщенияЦиклаОбмена = Запрос.Выполнить().Выгрузить();
	
	// из них выбираем те, которые соответствуют маске по типам
	// если возможных типов несколько то пробуем найти сообщения каждого типа поочередно
	Если ТипЗнч(ТипСообщения) = Тип("Массив") Тогда
		СообщенияИскомогоТипа = Новый Массив;
		Для Каждого ЭлТипСообщения Из ТипСообщения Цикл
			СообщенияИскомогоТипа = СообщенияЦиклаОбмена.НайтиСтроки(Новый Структура("Тип", ЭлТипСообщения));
			Если СообщенияИскомогоТипа.Количество() > 0 Тогда
				Прервать;
			КонецЕсли;
		КонецЦикла;
	Иначе
		СообщенияИскомогоТипа = СообщенияЦиклаОбмена.НайтиСтроки(Новый Структура("Тип", ТипСообщения));
	КонецЕсли;
	
	// если нашли, то показываем форму
	ЧислоНайденныхСообщений = СообщенияИскомогоТипа.Количество();
	Если ЧислоНайденныхСообщений = 1 Тогда
		Если ТипЗнч(СообщенияИскомогоТипа[0].Ссылка) = Тип("ДокументСсылка.ТранспортноеСообщение") Тогда 
			Возврат СообщенияИскомогоТипа[0].Ссылка;
		КонецЕсли;
		Возврат Неопределено;
	ИначеЕсли ЧислоНайденныхСообщений > 1 Тогда
		
		СписокСообщений = Новый СписокЗначений;
		Для Каждого СообщениеИскомогоТипа Из СообщенияИскомогоТипа Цикл
			СписокСообщений.Добавить(СообщениеИскомогоТипа.Ссылка, ПредставлениеСообщения(СообщениеИскомогоТипа.Ссылка));
		КонецЦикла;
		
		Если СписокСообщений.Количество() > 0 Тогда
			Возврат СписокСообщений;
		Иначе
			Возврат Неопределено;
		КонецЕсли;
		
	КонецЕсли;
	
	// если сообщение не найдено, то показываем либо диалог со справочной информацией, либо предупреждение
	
	// ПФР
	Если ТипСообщения = Перечисления.ТипыТранспортныхСообщений.ПервичноеСообщениеСодержащееОтчетностьПФР Тогда
		Возврат СокрЛП("Первичное сообщение, содержащее файл отчетности ПФР, отсутствует.");
	ИначеЕсли ТипСообщения = Перечисления.ТипыТранспортныхСообщений.ПодтверждениеПолученияОтчетностиПФР Тогда
		Возврат СокрЛП("Подтверждение получения отчетности ПФР отсутствует.");
	ИначеЕсли ТипСообщения = Перечисления.ТипыТранспортныхСообщений.ПодтверждениеПолученияОтчетностиПФР Тогда
		Возврат СокрЛП("Протокол ПФР отсутствует.");
	ИначеЕсли ТипСообщения = Перечисления.ТипыТранспортныхСообщений.ПодтверждениеПолученияОтчетностиПФР Тогда
		Возврат СокрЛП("Квитанция на протокол ПФР отсутствует.");
	ИначеЕсли ТипСообщения = Перечисления.ТипыТранспортныхСообщений.ПисьмоИсходящееПФР Тогда
		Возврат СокрЛП("Первичное сообщение, содержащее письмо, отсутствует.");
	ИначеЕсли ТипСообщения = Перечисления.ТипыТранспортныхСообщений.ПисьмоИсходящееКвитанцияПФР Тогда
		Возврат СокрЛП("Квитанция на письмо отсутствует.");
	ИначеЕсли ТипСообщения = Перечисления.ТипыТранспортныхСообщений.ПисьмоВходящееПФР Тогда
		Возврат СокрЛП("Первичное сообщение, содержащее письмо, отсутствует.");
	ИначеЕсли ТипСообщения = Перечисления.ТипыТранспортныхСообщений.ПисьмоВходящееКвитанцияПФР Тогда
		Возврат СокрЛП("Квитанция на письмо отсутствует.");
	ИначеЕсли ТипСообщения = Перечисления.ТипыТранспортныхСообщений.ПервичноеСообщениеСодержащееЗапросПФР Тогда
		Возврат СокрЛП("Первичное сообщение, содержащее запрос в ПФР, отсутствует.");
	ИначеЕсли ТипСообщения = Перечисления.ТипыТранспортныхСообщений.ПодтверждениеПолученияЗапросаПФР Тогда
		Возврат СокрЛП("Подтверждение получения запроса в ПФР отсутствует.");
	ИначеЕсли ТипСообщения = Перечисления.ТипыТранспортныхСообщений.ОтветНаЗапросПФР Тогда
		Возврат СокрЛП("Ответ на запрос в ПФР отсутствует.");
	ИначеЕсли ТипСообщения = Перечисления.ТипыТранспортныхСообщений.ОтветНаЗапросКвитанцияПФР Тогда
		Возврат СокрЛП("Квитанция на ответ на запрос в ПФР отсутствует.");
		
	// ФНС 534
	ИначеЕсли ТипСообщения = Перечисления.ТипыТранспортныхСообщений.ДекларацияНП Тогда
		Возврат СокрЛП("Первичное сообщение, содержащее файл отчетности, отсутствует.");
	ИначеЕсли ТипСообщения = Перечисления.ТипыТранспортныхСообщений.ПодтверждениеДекларацияНО Тогда
		Возврат СокрЛП("Подтверждение даты отправки отсутствует.");
	ИначеЕсли ТипСообщения = Перечисления.ТипыТранспортныхСообщений.ИзвещениеПодтверждениеНП Тогда
		Возврат СокрЛП("Извещение о получении подтверждения даты отправки отсутствует.");
	ИначеЕсли ТипСообщения = Перечисления.ТипыТранспортныхСообщений.ИзвещениеДекларацияНО Тогда
		Возврат СокрЛП("Извещение о получении отчета отсутствует.");
	ИначеЕсли ТипСообщения = Перечисления.ТипыТранспортныхСообщений.РезультатПриемаДекларацияНО Тогда
		Возврат СокрЛП("Сообщение, содержащее результат приема отчета, отсутствует.");
	ИначеЕсли ТипСообщения = Перечисления.ТипыТранспортныхСообщений.ИзвещениеРезультатПриемаНП Тогда
		Возврат СокрЛП("Извещение о получении результата приема отсутствует.");
	ИначеЕсли ТипСообщения = Перечисления.ТипыТранспортныхСообщений.РезультатОбработкиДекларацияНО Тогда
		Возврат СокрЛП("Сообщение, содержащее результат обработки отчета, отсутствует.");
	ИначеЕсли ТипСообщения = Перечисления.ТипыТранспортныхСообщений.ИзвещениеРезультатОбработкиНП Тогда
		Возврат СокрЛП("Извещение о получении результата обработки отсутствует.");
		
	//ФНС 534 Документ	
	ИначеЕсли ТипСообщения = Перечисления.ТипыТранспортныхСообщений.ДокументНО Тогда
		Возврат СокрЛП("Сообщение, содержащее документ налогового органа, отсутствует.");
	ИначеЕсли ТипСообщения = Перечисления.ТипыТранспортныхСообщений.ИзвещениеДокументНП Тогда
		Возврат СокрЛП("Извещение о получении документа налогового органа отсутствует.");
	ИначеЕсли ТипСообщения = Перечисления.ТипыТранспортныхСообщений.РезультатПриемаДокументНП Тогда
		Возврат СокрЛП("Сообщение, содержащее результат приема документа, отсутствует.");
	ИначеЕсли ТипСообщения = Перечисления.ТипыТранспортныхСообщений.ИзвещениеРезультатПриемаНО Тогда
		Возврат СокрЛП("Извещение о получении результата приема отсутствует.");
		
	// Заявление
	ИначеЕсли ТипСообщения = Перечисления.ТипыТранспортныхСообщений.ЗаявлениеНП Тогда
		Возврат СокрЛП("Первичное сообщение, содержащее заявление, отсутствует.");
	ИначеЕсли ТипСообщения = Перечисления.ТипыТранспортныхСообщений.ПодтверждениеЗаявлениеНО Тогда
		Возврат СокрЛП("Подтверждение даты отправки отсутствует.");
	ИначеЕсли ТипСообщения = Перечисления.ТипыТранспортныхСообщений.ИзвещениеЗаявлениеНО Тогда
		Возврат СокрЛП("Извещение о получении заявления отсутствует.");
	ИначеЕсли ТипСообщения = Перечисления.ТипыТранспортныхСообщений.РезультатПриемаЗаявлениеНО Тогда
		Возврат СокрЛП("Результат приема заявления отсутствует.");
		
	ИначеЕсли ТипСообщения = Перечисления.ТипыТранспортныхСообщений.РезультатОбработкиЗаявлениеНО Тогда
		Возврат СокрЛП("Результат обработки заявления отсутствует.");	
			
	ИначеЕсли ТипСообщения = Перечисления.ТипыТранспортныхСообщений.РезультатОбработкиЗаявлениеРФНО Тогда
		Возврат СокрЛП("Результат обработки заявления отсутствует.");
	ИначеЕсли ТипСообщения = Перечисления.ТипыТранспортныхСообщений.ИзвещениеРезультатОбработкиРФНП Тогда
		Возврат СокрЛП("Извещение о получении результата обработки заявления отсутствует.");
	ИначеЕсли ТипСообщения = Перечисления.ТипыТранспортныхСообщений.СообщениеОбОтзывеЗаявлениеРФНО Тогда
		Возврат СокрЛП("Сообщение об отзыве заявления в РФ отсутствует.");
	ИначеЕсли ТипСообщения = Перечисления.ТипыТранспортныхСообщений.ИзвещениеОбОтзывеЗаявлениеРФНП Тогда
		Возврат СокрЛП("Извещение о получении сообщения об отзыве заявления в РФ отсутствует.");
	ИначеЕсли ТипСообщения = Перечисления.ТипыТранспортныхСообщений.РезультатОбработкиЗаявлениеТСНО Тогда
		Возврат СокрЛП("Результат обработки заявления в ТС отсутствует.");
	ИначеЕсли ТипСообщения = Перечисления.ТипыТранспортныхСообщений.ИзвещениеРезультатОбработкиТСНП Тогда
		Возврат СокрЛП("Извещение о получении результата обработки заявления в ТС отсутствует.");
		
	// ФНС 534 2-НДФЛ
	ИначеЕсли ТипСообщения = Перечисления.ТипыТранспортныхСообщений.Форма2НДФЛНП Тогда
		Возврат СокрЛП("Первичное сообщение, содержащее сведения формы 2-НДФЛ, отсутствует.");
	ИначеЕсли ТипСообщения = Перечисления.ТипыТранспортныхСообщений.ПодтверждениеФорма2НДФЛНО Тогда
		Возврат СокрЛП("Подтверждение даты отправки отсутствует.");
	ИначеЕсли ТипСообщения = Перечисления.ТипыТранспортныхСообщений.ИзвещениеФорма2НДФЛНО Тогда
		Возврат СокрЛП("Извещение о получении отчета отсутствует.");
	ИначеЕсли ТипСообщения = Перечисления.ТипыТранспортныхСообщений.РезультатПриемаФорма2НДФЛНО Тогда
		Возврат СокрЛП("Сообщение, содержащее результат приема отчета, отсутствует.");
	ИначеЕсли ТипСообщения = Перечисления.ТипыТранспортныхСообщений.ОбращениеНП Тогда
		Возврат СокрЛП("Сообщение, содержащее обращение налогоплательщика, отсутствует.");
	ИначеЕсли ТипСообщения = Перечисления.ТипыТранспортныхСообщений.ПодтверждениеОбращениеНО Тогда
		Возврат СокрЛП("Подтверждение даты отправки отсутствует.");
	ИначеЕсли ТипСообщения = Перечисления.ТипыТранспортныхСообщений.ИзвещениеПодтверждениеНП Тогда
		Возврат СокрЛП("Извещение о получении подтверждения даты отправки отсутствует.");
	ИначеЕсли ТипСообщения = Перечисления.ТипыТранспортныхСообщений.ИзвещениеОбращениеНО Тогда
		Возврат СокрЛП("Извещение о получении обращения отсутствует.");
	ИначеЕсли ТипСообщения = Перечисления.ТипыТранспортныхСообщений.РезультатПриемаОбращениеНО Тогда
		Возврат СокрЛП("Сообщение, содержащее результат приема обращения, отсутствует.");
	ИначеЕсли ТипСообщения = Перечисления.ТипыТранспортныхСообщений.ИзвещениеРезультатПриемаНП Тогда
		Возврат СокрЛП("Извещение о получении результата приема отсутствует.");
		
	ИначеЕсли ТипСообщения = Перечисления.ТипыТранспортныхСообщений.ПредставлениеНП Тогда
		Возврат СокрЛП("Сообщение, содержащее представление документов, отсутствует.");
	ИначеЕсли ТипСообщения = Перечисления.ТипыТранспортныхСообщений.ПодтверждениеПредставлениеНО Тогда
		Возврат СокрЛП("Подтверждение даты отправки отсутствует.");
	ИначеЕсли ТипСообщения = Перечисления.ТипыТранспортныхСообщений.ИзвещениеПредставлениеНО Тогда
		Возврат СокрЛП("Извещение о получении представленных документов отсутствует.");
	ИначеЕсли ТипСообщения = Перечисления.ТипыТранспортныхСообщений.РезультатПриемаПредставлениеНО Тогда
		Возврат СокрЛП("Сообщение, содержащее результат приема представленных документов, отсутствует.");
		
	ИначеЕсли ТипСообщения = Перечисления.ТипыТранспортныхСообщений.ПисьмоНО Тогда
		Возврат СокрЛП("Сообщение, содержащее письмо налогового органа, отсутствует.");
	ИначеЕсли ТипСообщения = Перечисления.ТипыТранспортныхСообщений.ИзвещениеПисьмоНП Тогда
		Возврат СокрЛП("Извещение о получении письмо налогового органа отсутствует.");
	ИначеЕсли ТипСообщения = Перечисления.ТипыТранспортныхСообщений.РассылкаНО Тогда
		Возврат СокрЛП("Сообщение, содержащее рассылку налогового органа, отсутствует.");
	ИначеЕсли ТипСообщения = Перечисления.ТипыТранспортныхСообщений.ИзвещениеРассылкаНП Тогда
		Возврат СокрЛП("Извещение о получении рассылки налогового органа отсутствует.");
	
	// ФНС 534 ИОН
	ИначеЕсли ТипСообщения = Перечисления.ТипыТранспортныхСообщений.ЗапросНП Тогда
		Возврат СокрЛП("Первичное сообщение, содержащее запрос, отсутствует.");
	ИначеЕсли ТипСообщения = Перечисления.ТипыТранспортныхСообщений.ПодтверждениеЗапросНО Тогда
		Возврат СокрЛП("Подтверждение даты отправки отсутствует.");
	ИначеЕсли ТипСообщения = Перечисления.ТипыТранспортныхСообщений.ИзвещениеПодтверждениеНП Тогда
		Возврат СокрЛП("Извещение о получении подтверждения даты отправки отсутствует.");
	ИначеЕсли ТипСообщения = Перечисления.ТипыТранспортныхСообщений.ИзвещениеЗапросНО Тогда
		Возврат СокрЛП("Извещение о получении запроса отсутствует.");
	ИначеЕсли ТипСообщения = Перечисления.ТипыТранспортныхСообщений.РезультатПриемаЗапросНО Тогда
		Возврат СокрЛП("Сообщение, содержащее результат приема запроса, отсутствует.");
	ИначеЕсли ТипСообщения = Перечисления.ТипыТранспортныхСообщений.ИзвещениеРезультатПриемаНП Тогда
		Возврат СокрЛП("Извещение о получении результата приема отсутствует.");
	ИначеЕсли ТипСообщения = Перечисления.ТипыТранспортныхСообщений.РезультатОбработкиЗапросНО Тогда
		Возврат СокрЛП("Сообщение, содержащее ответ на запрос, отсутствует.");
	ИначеЕсли ТипСообщения = Перечисления.ТипыТранспортныхСообщений.ИзвещениеРезультатОбработкиНП Тогда
		Возврат СокрЛП("Извещение о получении ответа на запрос отсутствует.");
		
	// Росстат
	ИначеЕсли ТипСообщения = Перечисления.ТипыТранспортныхСообщений.ПервичноеСообщениеСодержащееОтчетностьФСГС Тогда
		Возврат СокрЛП("Первичное сообщение, содержащее файл отчетности для Росстата, отсутствует.");
	ИначеЕсли ТипСообщения = Перечисления.ТипыТранспортныхСообщений.ПодтверждениеДатыОтправкиФСГС Тогда
		Возврат СокрЛП("Подтверждение даты отправки отсутствует.");
	ИначеЕсли ТипСообщения = Перечисления.ТипыТранспортныхСообщений.ИзвещениеПодтверждениеДатыОтправкиФСГС Тогда
		Возврат СокрЛП("Извещение о получении подтверждения даты отправки отсутствует.");
	ИначеЕсли ТипСообщения = Перечисления.ТипыТранспортныхСообщений.ИзвещениеОПолученииОтчетностиФСГС Тогда
		Возврат СокрЛП("Извещение о получения отчетности отсутствует.");
	ИначеЕсли ТипСообщения = Перечисления.ТипыТранспортныхСообщений.ПротоколВходногоКонтроляОтчетностиФСГС Тогда
		Возврат СокрЛП("Протокол входного контроля отсутствует.");
	ИначеЕсли ТипСообщения = Перечисления.ТипыТранспортныхСообщений.ИзвещениеПротоколВходногоКонтроляОтчетностиФСГС Тогда
		Возврат СокрЛП("Извещение о получении протокола входного контроля отсутствует.");
	ИначеЕсли ТипСообщения = Перечисления.ТипыТранспортныхСообщений.ПисьмоИсходящееФСГС Тогда
		Возврат СокрЛП("Первичное сообщение, содержащее письмо, отсутствует.");
	ИначеЕсли ТипСообщения = Перечисления.ТипыТранспортныхСообщений.ИзвещениеПисьмоИсходящееФСГС Тогда
		Возврат СокрЛП("Извещение о получении письма отсутствует.");
	ИначеЕсли ТипСообщения = Перечисления.ТипыТранспортныхСообщений.ПисьмоВходящееФСГС Тогда
		Возврат СокрЛП("Первичное сообщение, содержащее письмо, отсутствует.");
	ИначеЕсли ТипСообщения = Перечисления.ТипыТранспортныхСообщений.РассылкаФСГС Тогда
		Возврат СокрЛП("Первичное сообщение, содержащее рассылку, отсутствует.");
	ИначеЕсли ТипСообщения = Перечисления.ТипыТранспортныхСообщений.РассылкаШаблоновФСГС Тогда
		Возврат СокрЛП("Первичное сообщение, содержащее рассылку шаблонов, отсутствует.");
	ИначеЕсли ТипСообщения = Перечисления.ТипыТранспортныхСообщений.ИзвещениеПисьмоВходящееФСГС Тогда
		Возврат СокрЛП("Извещение о получении письма отсутствует.");
	КонецЕсли;
	
КонецФункции

Функция ПолучитьСообщениеЦиклаОбмена(ЦиклОбмена, ТипСообщения, Основание = Неопределено) Экспорт
	
	Запрос = Новый Запрос("ВЫБРАТЬ РАЗРЕШЕННЫЕ
						  |	ТранспортноеСообщение.Ссылка
						  |ИЗ
						  |	Документ.ТранспортноеСообщение КАК ТранспортноеСообщение
						  |ГДЕ
						  |	ТранспортноеСообщение.ЦиклОбмена = &ЦиклОбмена
						  |	И ТранспортноеСообщение.Тип = &Тип");
	Запрос.УстановитьПараметр("ЦиклОбмена", ЦиклОбмена);
	Запрос.УстановитьПараметр("Тип", ТипСообщения);
	
	Если Основание <> Неопределено Тогда
		Запрос.Текст = Запрос.Текст + "
						  |	И ТранспортноеСообщение.Основание = &Основание";
		Запрос.УстановитьПараметр("Основание", Основание);
	КонецЕсли;
	
	Запрос.Текст = Запрос.Текст + "
						  |
						  |УПОРЯДОЧИТЬ ПО
						  |	ТранспортноеСообщение.Дата УБЫВ";
	
	РезультатЗапроса = Запрос.Выполнить();
	Если РезультатЗапроса.Пустой() Тогда
		Возврат Документы.ТранспортноеСообщение.ПустаяСсылка();
	КонецЕсли;
	
	Выборка = РезультатЗапроса.Выбрать();
	Если Выборка.Следующий() Тогда
		Возврат Выборка.Ссылка;
	Иначе
		Возврат Документы.ТранспортноеСообщение.ПустаяСсылка();
	КонецЕсли;
	
КонецФункции

Функция ЗапросНаВводПодтверждения(ЦиклОбмена, Основание = Неопределено) Экспорт

	Если ТипЗнч(Основание) = Тип("ПеречислениеСсылка.ТипыТранспортныхСообщений") Тогда
		СообщениеОснование = ПолучитьСообщениеЦиклаОбмена(ЦиклОбмена, Основание);
		ТипСообщенияОснования = Основание;
	Иначе
		СообщениеОснование = Основание;
		ТипСообщенияОснования = Основание.Тип;
	КонецЕсли;
	
	Если НЕ ЗначениеЗаполнено(СообщениеОснование) Тогда
		ТекстПредупреждения = "Невозможно ввести подтверждение: отсутствует сообщение-основание!";
		Возврат Новый Структура("ТекстПредупреждения", ТекстПредупреждения);
	КонецЕсли;
	
	// ПФР
	Если ТипСообщенияОснования = Перечисления.ТипыТранспортныхСообщений.ПротоколПФР Тогда
		ТекстВопроса = "Создать подтверждение на протокол?" + Символы.ПС + "При этом, сообщение будет записано.";
	ИначеЕсли ТипСообщенияОснования = Перечисления.ТипыТранспортныхСообщений.ПисьмоВходящееПФР Тогда
		ТекстВопроса = "Создать подтверждение на письмо?" + Символы.ПС + "При этом, сообщение будет записано.";
		Если ЭтоРассылкаПФР(СообщениеОснование) Тогда
			ТекстВопроса = ТекстВопроса + Символы.ПС + Символы.ПС
				+ НСтр("ru = 'Внимание! Это письмо является массовой рассылкой, создание подтверждения не требуется.'");
		КонецЕсли;
	ИначеЕсли ТипСообщенияОснования = Перечисления.ТипыТранспортныхСообщений.ОтветНаЗапросПФР Тогда
		ТекстВопроса = "Создать подтверждение на ответ на запрос?" + Символы.ПС + "При этом, сообщение будет записано.";
	
	// ФНС 534
	ИначеЕсли ТипСообщенияОснования = Перечисления.ТипыТранспортныхСообщений.ПодтверждениеДекларацияНО
	ИЛИ ТипСообщенияОснования = Перечисления.ТипыТранспортныхСообщений.ПодтверждениеФорма2НДФЛНО Тогда
		ТекстВопроса = "Создать извещение о получении подтверждения даты отправки?" + Символы.ПС + "При этом, сообщение будет записано.";
	ИначеЕсли ТипСообщенияОснования = Перечисления.ТипыТранспортныхСообщений.РезультатПриемаДекларацияНО
	ИЛИ ТипСообщенияОснования = Перечисления.ТипыТранспортныхСообщений.РезультатПриемаФорма2НДФЛНО Тогда
		ТекстВопроса = "Создать извещение о получении результата приема?" + Символы.ПС + "При этом, сообщение будет записано.";
	ИначеЕсли ТипСообщенияОснования = Перечисления.ТипыТранспортныхСообщений.РезультатОбработкиДекларацияНО Тогда
		ТекстВопроса = "Создать извещение о получении результата обработки?" + Символы.ПС + "При этом, сообщение будет записано.";
	
	// ФНС 534 НФД
	ИначеЕсли ТипСообщенияОснования = Перечисления.ТипыТранспортныхСообщений.ПодтверждениеОбращениеНО Тогда
		ТекстВопроса = "Создать извещение о получении подтверждения даты отправки?" + Символы.ПС + "При этом, сообщение будет записано.";
	ИначеЕсли ТипСообщенияОснования = Перечисления.ТипыТранспортныхСообщений.РезультатПриемаОбращениеНО Тогда
		ТекстВопроса = "Создать извещение о получении результата приема?" + Символы.ПС + "При этом, сообщение будет записано.";
		
	ИначеЕсли ТипСообщенияОснования = Перечисления.ТипыТранспортныхСообщений.ПодтверждениеПредставлениеНО Тогда
		ТекстВопроса = "Создать извещение о получении подтверждения даты отправки?" + Символы.ПС + "При этом, сообщение будет записано.";
	ИначеЕсли ТипСообщенияОснования = Перечисления.ТипыТранспортныхСообщений.РезультатПриемаПредставлениеНО Тогда
		ТекстВопроса = "Создать извещение о получении результата приема?" + Символы.ПС + "При этом, сообщение будет записано.";
		
	ИначеЕсли ТипСообщенияОснования = Перечисления.ТипыТранспортныхСообщений.ПисьмоНО Тогда
		ТекстВопроса = "Создать извещение о получении письма налогового органа?" + Символы.ПС + "При этом, сообщение будет записано.";
	ИначеЕсли ТипСообщенияОснования = Перечисления.ТипыТранспортныхСообщений.РассылкаНО Тогда
		ТекстВопроса = "Создать извещение о получении рассылки налогового органа?" + Символы.ПС + "При этом, сообщение будет записано.";
		
	// ФНС 534 ДРП (Документы реализации полномочий НО)
	ИначеЕсли ТипСообщенияОснования = Перечисления.ТипыТранспортныхСообщений.ДокументНО Тогда
		ТекстВопроса = "Создать извещение о получении документа налогового органа?" + Символы.ПС + "При этом, сообщение будет записано.";
		
	// Заявление
	ИначеЕсли ТипСообщенияОснования = Перечисления.ТипыТранспортныхСообщений.ПодтверждениеЗаявлениеНО Тогда
		ТекстВопроса = "Создать извещение о получении подтверждения даты отправки?" + Символы.ПС + "При этом, сообщение будет записано.";
	ИначеЕсли ТипСообщенияОснования = Перечисления.ТипыТранспортныхСообщений.РезультатПриемаЗаявлениеНО Тогда
		ТекстВопроса = "Создать извещение о получении результата приема?" + Символы.ПС + "При этом, сообщение будет записано.";
		
	ИначеЕсли ТипСообщенияОснования = Перечисления.ТипыТранспортныхСообщений.РезультатОбработкиЗаявлениеНО Тогда
		ТекстВопроса = "Создать извещение о получении результата обработки заявления?" + Символы.ПС + "При этом, сообщение будет записано.";
		
	ИначеЕсли ТипСообщенияОснования = Перечисления.ТипыТранспортныхСообщений.РезультатОбработкиЗаявлениеРФНО Тогда
		ТекстВопроса = "Создать извещение о получении результата обработки заявления в РФ?" + Символы.ПС + "При этом, сообщение будет записано.";
	ИначеЕсли ТипСообщенияОснования = Перечисления.ТипыТранспортныхСообщений.СообщениеОбОтзывеЗаявлениеРФНО Тогда
		ТекстВопроса = "Создать извещение о получении сообщения об отзыве заявления?" + Символы.ПС + "При этом, сообщение будет записано.";
    ИначеЕсли ТипСообщенияОснования = Перечисления.ТипыТранспортныхСообщений.РезультатОбработкиЗаявлениеТСНО Тогда
		ТекстВопроса = "Создать извещение о получении результата обработки заявления в ТС?" + Символы.ПС + "При этом, сообщение будет записано.";
		
	// ФНС 534 ИОН
	ИначеЕсли ТипСообщенияОснования = Перечисления.ТипыТранспортныхСообщений.ПодтверждениеЗапросНО Тогда
		ТекстВопроса = "Создать извещение о получении подтверждения даты отправки?" + Символы.ПС + "При этом, сообщение будет записано.";
	ИначеЕсли ТипСообщенияОснования = Перечисления.ТипыТранспортныхСообщений.РезультатПриемаЗапросНО Тогда
		ТекстВопроса = "Создать извещение о получении результата приема?" + Символы.ПС + "При этом, сообщение будет записано.";
	ИначеЕсли ТипСообщенияОснования = Перечисления.ТипыТранспортныхСообщений.РезультатОбработкиЗапросНО Тогда
		ТекстВопроса = "Создать извещение о получении результата обработки?" + Символы.ПС + "При этом, сообщение будет записано.";
		
	// Росстат
	ИначеЕсли ТипСообщенияОснования = Перечисления.ТипыТранспортныхСообщений.ПротоколВходногоКонтроляОтчетностиФСГС Тогда
		ТекстВопроса = "Создать извещение о получении протокола входного контроля отчетности?" + Символы.ПС + "При этом, сообщение будет записано.";
	ИначеЕсли ТипСообщенияОснования = Перечисления.ТипыТранспортныхСообщений.ПисьмоВходящееФСГС Тогда
		ТекстВопроса = "Создать извещение о получении письма?" + Символы.ПС + "При этом, сообщение будет записано.";
	ИначеЕсли ТипСообщенияОснования = Перечисления.ТипыТранспортныхСообщений.ПодтверждениеДатыОтправкиФСГС Тогда
		ТекстВопроса = "Создать извещение о получении подтверждения даты отправки?" + Символы.ПС + "При этом, сообщение будет записано.";
	Иначе
		Возврат Неопределено;
	КонецЕсли;
	
	Возврат Новый Структура("ТекстВопроса", ТекстВопроса);
	
КонецФункции

Функция ЭтоРассылкаПФР(СсылкаНаТранспортноеСообщениеПисьмоВходящееПФР) Экспорт
	
	ПризнакРассылка = Ложь;
	
	ВложенияОписанияКПисьму = ПолучитьВложенияТранспортногоСообщения(СсылкаНаТранспортноеСообщениеПисьмоВходящееПФР, Истина,
		Перечисления.ТипыСодержимогоТранспортногоКонтейнера.ОписаниеПисьма);
	Если ВложенияОписанияКПисьму.Количество() > 0 Тогда
		ВложенияОписаниеКПисьму = ВложенияОписанияКПисьму[0];
		
		// загружаем описание к письму
		ВремФайлОписаниеКПисьму = ПолучитьИмяВременногоФайла();
		ВложенияОписаниеКПисьму.Данные.Получить().Записать(ВремФайлОписаниеКПисьму);
		ДеревоОписанияКПисьму = ЗагрузитьXMLВДеревоЗначений(ВремФайлОписаниеКПисьму);
		ОперацииСФайламиЭДКО.УдалитьВременныйФайл(ВремФайлОписаниеКПисьму);
		Если ДеревоОписанияКПисьму <> Неопределено Тогда
			// извлекаем категорию из описания письма
			УзелОписаниеПисьма = ДеревоОписанияКПисьму.Строки.Найти("описаниеПисьма", "Имя");
			Если УзелОписаниеПисьма <> Неопределено Тогда
				УзелКатегорияПисьма = УзелОписаниеПисьма.Строки.Найти("категорияПисьма", "Имя");
				Если УзелКатегорияПисьма <> Неопределено Тогда
					СтрКатегорияПисьма = XMLЗначениеВнутр(Тип("Строка"), УзелКатегорияПисьма.Значение);
					ПризнакРассылка = (нрег(СтрКатегорияПисьма) = "рассылка");
				КонецЕсли;
			КонецЕсли;
		КонецЕсли;
	КонецЕсли;
	
	Возврат ПризнакРассылка;
	
КонецФункции

////////////////////////////////////////////////////////////////////////////////
// РАБОТА С ВНЕШНИМИ ЭЛЕКТРОННЫМИ ПРЕДСТАВЛЕНИЯМИ
// Перенесена на форму

Процедура ДобавитьЭлектронноеПредставлениеВХранилище(Владелец, ИмяФайла, Данные, ТипФайлаОтчетности = Неопределено) Экспорт
	МенеджерЗаписи = РегистрыСведений.ХранилищеЭлектронныхПредставленийРегламентированныхОтчетов.СоздатьМенеджерЗаписи();
	МенеджерЗаписи.ЭлектронноеПредставление = Владелец;
	МенеджерЗаписи.ИмяФайла = ИмяФайла;
	Если ТипЗнч(Данные) = Тип("ХранилищеЗначения") Тогда 
		МенеджерЗаписи.Данные = Данные;
	ИначеЕсли ТипЗнч(Данные) = Тип("ДвоичныеДанные") Тогда 
		МенеджерЗаписи.Данные = Новый ХранилищеЗначения(Данные, Новый СжатиеДанных(9));
	КонецЕсли;
	МенеджерЗаписи.Размер = ОпределитьРазмер(Данные);
	МенеджерЗаписи.ТипФайлаОтчетности = ТипФайлаОтчетности;
	МенеджерЗаписи.Записать(Истина);
КонецПроцедуры

Процедура ОчиститьЭлектронныеПредставленияВХранилище(Владелец) Экспорт
	
	НаборЗаписей = РегистрыСведений.ХранилищеЭлектронныхПредставленийРегламентированныхОтчетов.СоздатьНаборЗаписей();
	НаборЗаписей.Отбор.ЭлектронноеПредставление.Установить(Владелец);
	НаборЗаписей.Записать(Истина);
	
КонецПроцедуры

Процедура ИзвлечьЭлектронноеПредставлениеИзХранилища(Владелец, ИмяФайла, Данные, ВАрхиве = Ложь) Экспорт
	
	ИмяФайла = "";
	Данные = Неопределено;
	ВАрхиве = Ложь;
	
	Представления = ИзвлечьЭлектронныеПредставленияИзХранилища(Владелец);
	Если Представления.Количество() = 0 Тогда
		Возврат;
	КонецЕсли;
	
	ИмяФайла = СокрЛП(Представления[0].ИмяФайла);
	Данные = Представления[0].Данные;
	ВАрхиве = Представления[0].ВАрхиве;
	
КонецПроцедуры

Функция ИзвлечьЭлектронныеПредставленияИзХранилища(Владелец, Знач УникальныйИдентификатор = Неопределено) Экспорт
	
	ТаблицаРезультат = Новый ТаблицаЗначений;
	ТаблицаРезультат.Колонки.Добавить("ИмяФайла");
	ТаблицаРезультат.Колонки.Добавить("Данные");
	ТаблицаРезультат.Колонки.Добавить("ТипФайлаОтчетности");
	ТаблицаРезультат.Колонки.Добавить("ВАрхиве");
	
	Запрос = Новый Запрос("ВЫБРАТЬ РАЗРЕШЕННЫЕ
	                      |	ХранилищеЭлектронныхПредставленийРегламентированныхОтчетов.ИмяФайла,
	                      |	ХранилищеЭлектронныхПредставленийРегламентированныхОтчетов.ТипФайлаОтчетности
						  |	, ВЫБОР
						  |		КОГДА СостояниеОбъектов.Архивный ЕСТЬ NULL ТОГДА Ложь
						  |		КОГДА СостояниеОбъектов.Архивный = Ложь ТОГДА Ложь
						  |	ИНАЧЕ
						  |		Истина
						  |	КОНЕЦ ВАрхиве
						  |ИЗ
	                      |	РегистрСведений.ХранилищеЭлектронныхПредставленийРегламентированныхОтчетов КАК ХранилищеЭлектронныхПредставленийРегламентированныхОтчетов
						  |
						  |		ЛЕВОЕ СОЕДИНЕНИЕ РегистрСведений.ПризнакиАрхивированияФайловДОСКонтролирующимиОрганами КАК СостояниеОбъектов
						  |			ПО 		(СостояниеОбъектов.Объект = ХранилищеЭлектронныхПредставленийРегламентированныхОтчетов.ЭлектронноеПредставление)
						  |				И	(СостояниеОбъектов.ИмяФайла = ХранилищеЭлектронныхПредставленийРегламентированныхОтчетов.ИмяФайла)
						  |				И	(СостояниеОбъектов.Владелец = Значение(Перечисление.ВидыАрхивируемыхМетаданныхДО.ХранилищеЭлектронныхПредставленийРегламентированныхОтчетов))
						  |
	                      |ГДЕ
	                      |	ХранилищеЭлектронныхПредставленийРегламентированныхОтчетов.ЭлектронноеПредставление = &ЭлектронноеПредставление");
	
	Запрос.УстановитьПараметр("ЭлектронноеПредставление", Владелец);
	Выборка = Запрос.Выполнить().Выбрать();
	Если Выборка.Количество() = 0 Тогда
		Возврат Неопределено;
	Иначе
		
		Пока Выборка.Следующий() Цикл
			НоваяСтрока = ТаблицаРезультат.Добавить();
			НоваяСтрока.ИмяФайла 			= Выборка.ИмяФайла;
			НоваяСтрока.ТипФайлаОтчетности 	= Выборка.ТипФайлаОтчетности;
			НоваяСтрока.ВАрхиве 			= Выборка.ВАрхиве;
			Если Выборка.ВАрхиве Тогда
				Данные 			= Base64Значение("");
			Иначе
				Данные 			= ДанныеОдногоФайлаЭлектронногоПредставления(Владелец, Выборка.ИмяФайла);
			КонецЕсли;
			Если УникальныйИдентификатор = Неопределено Тогда
				НоваяСтрока.Данные 			= Данные;
			Иначе
				Если Выборка.ВАрхиве Тогда
					НоваяСтрока.Данные 			= Неопределено;
				Иначе
					НоваяСтрока.Данные 			= АдресХранилищаФайлаЭлектронногоПредставления(Владелец, Выборка.ИмяФайла, УникальныйИдентификатор);
				КонецЕсли;
			КонецЕсли;
			
		КонецЦикла;
		
		Возврат ТаблицаРезультат;
		
	КонецЕсли;
	
КонецФункции

//Извлекает данные как адрес
Функция ИзвлечьЭлектронныеПредставленияВХранилище(Владелец, УникальныйИдентификатор) Экспорт
	
	Возврат ИзвлечьЭлектронныеПредставленияИзХранилища(Владелец, УникальныйИдентификатор);
	
КонецФункции

Функция ПолучитьИменаФайловЭлектронныхПредставленийВХранилище(Владелец) Экспорт
	
	Запрос = Новый Запрос("ВЫБРАТЬ РАЗРЕШЕННЫЕ
	                      |	ХранилищеЭлектронныхПредставленийРегламентированныхОтчетов.ИмяФайла
	                      |ИЗ
	                      |	РегистрСведений.ХранилищеЭлектронныхПредставленийРегламентированныхОтчетов КАК ХранилищеЭлектронныхПредставленийРегламентированныхОтчетов
	                      |ГДЕ
	                      |	ХранилищеЭлектронныхПредставленийРегламентированныхОтчетов.ЭлектронноеПредставление = &ЭлектронноеПредставление");
	
	Запрос.УстановитьПараметр("ЭлектронноеПредставление", Владелец);
	Выборка = Запрос.Выполнить().Выгрузить();
	Возврат Выборка.ВыгрузитьКолонку("ИмяФайла");
	
КонецФункции

Функция ДанныеОдногоФайлаЭлектронногоПредставления(Владелец, ИмяФайла) Экспорт
	
	Данные = Неопределено;
	Запрос = Новый Запрос("ВЫБРАТЬ РАЗРЕШЕННЫЕ
	                      |	ХранилищеЭлектронныхПредставленийРегламентированныхОтчетов.Данные КАК ДанныеХранилище
						  |	, ВЫБОР
						  |		КОГДА СостояниеОбъектов.Архивный ЕСТЬ NULL ТОГДА Ложь
						  |		КОГДА СостояниеОбъектов.Архивный = Ложь ТОГДА Ложь
						  |	ИНАЧЕ
						  |		Истина
						  |	КОНЕЦ ВАрхиве
	                      |ИЗ
	                      |	РегистрСведений.ХранилищеЭлектронныхПредставленийРегламентированныхОтчетов КАК ХранилищеЭлектронныхПредставленийРегламентированныхОтчетов
						  |
						  |		ЛЕВОЕ СОЕДИНЕНИЕ РегистрСведений.ПризнакиАрхивированияФайловДОСКонтролирующимиОрганами КАК СостояниеОбъектов
						  |			ПО 		(СостояниеОбъектов.Объект = ХранилищеЭлектронныхПредставленийРегламентированныхОтчетов.ЭлектронноеПредставление)
						  |				И	(СостояниеОбъектов.ИмяФайла = ХранилищеЭлектронныхПредставленийРегламентированныхОтчетов.ИмяФайла)
						  |				И	(СостояниеОбъектов.Владелец = Значение(Перечисление.ВидыАрхивируемыхМетаданныхДО.ХранилищеЭлектронныхПредставленийРегламентированныхОтчетов))
						  |
	                      |ГДЕ
	                      |	ХранилищеЭлектронныхПредставленийРегламентированныхОтчетов.ЭлектронноеПредставление = &ЭлектронноеПредставление
	                      |	И ХранилищеЭлектронныхПредставленийРегламентированныхОтчетов.ИмяФайла = &ИмяФайла");
	
	Запрос.УстановитьПараметр("ЭлектронноеПредставление", Владелец);
	Запрос.УстановитьПараметр("ИмяФайла", ИмяФайла);
	
	Выборка = Запрос.Выполнить().Выбрать();
	Пока Выборка.Следующий() Цикл
		Если Выборка.ВАрхиве Тогда
			Возврат Неопределено;
		Иначе
			Данные = Выборка.ДанныеХранилище.Получить();
		КонецЕсли;
		Прервать;
	КонецЦикла;
		
	Возврат Данные;
	
КонецФункции

Функция АдресХранилищаФайлаЭлектронногоПредставления(Владелец, ИмяФайла, УникальныйИдентификатор) Экспорт
	
	Данные = ДанныеОдногоФайлаЭлектронногоПредставления(Владелец, ИмяФайла);
	
	Если Данные <> Неопределено Тогда
		Возврат ПоместитьВоВременноеХранилище(Данные, УникальныйИдентификатор);
	КонецЕсли;
		
	Возврат Данные;
	
КонецФункции

Функция ИзвлечьИменаФайловИзХранилища(Владелец) Экспорт
	
	Запрос = Новый Запрос("ВЫБРАТЬ РАЗРЕШЕННЫЕ
	                      |	ХранилищеЭлектронныхПредставленийРегламентированныхОтчетов.ИмяФайла,
	                      |	ХранилищеЭлектронныхПредставленийРегламентированныхОтчетов.ТипФайлаОтчетности
	                      |ИЗ
	                      |	РегистрСведений.ХранилищеЭлектронныхПредставленийРегламентированныхОтчетов КАК ХранилищеЭлектронныхПредставленийРегламентированныхОтчетов
	                      |ГДЕ
	                      |	ХранилищеЭлектронныхПредставленийРегламентированныхОтчетов.ЭлектронноеПредставление = &ЭлектронноеПредставление");
	
	Запрос.УстановитьПараметр("ЭлектронноеПредставление", Владелец);
	ТаблицаРезультат = Запрос.Выполнить().Выгрузить();
	ТаблицаРезультат.Колонки.Добавить("Данные");
	Возврат ТаблицаРезультат;
	
КонецФункции

Функция КоличествоФайловВКомплекте(Владелец) Экспорт
	
	Запрос = Новый Запрос("ВЫБРАТЬ РАЗРЕШЕННЫЕ
	                      |	КОЛИЧЕСТВО(ХранилищеЭлектронныхПредставленийРегламентированныхОтчетов.ИмяФайла) КАК КоличествоФайлов
	                      |ИЗ
	                      |	РегистрСведений.ХранилищеЭлектронныхПредставленийРегламентированныхОтчетов КАК ХранилищеЭлектронныхПредставленийРегламентированныхОтчетов
	                      |ГДЕ
	                      |	ХранилищеЭлектронныхПредставленийРегламентированныхОтчетов.ЭлектронноеПредставление = &ЭлектронноеПредставление");
	Запрос.УстановитьПараметр("ЭлектронноеПредставление", Владелец);
	ТаблицаРезультат = Запрос.Выполнить().Выгрузить();
	Возврат ТаблицаРезультат[0].КоличествоФайлов;
	
КонецФункции

Функция РазмерФайловВКомплекте(Владелец) Экспорт
	
	Размер = 0;
	Запрос = Новый Запрос("ВЫБРАТЬ РАЗРЕШЕННЫЕ
	                      |	СУММА(ХранилищеЭлектронныхПредставленийРегламентированныхОтчетов.Размер) КАК Размер
	                      |ИЗ
	                      |	РегистрСведений.ХранилищеЭлектронныхПредставленийРегламентированныхОтчетов КАК ХранилищеЭлектронныхПредставленийРегламентированныхОтчетов
	                      |ГДЕ
	                      |	ХранилищеЭлектронныхПредставленийРегламентированныхОтчетов.ЭлектронноеПредставление = &ЭлектронноеПредставление");
	Запрос.УстановитьПараметр("ЭлектронноеПредставление", Владелец);
	ТаблицаРезультат = Запрос.Выполнить().Выбрать();
	
	Если ТаблицаРезультат.Количество() = 0 Тогда
		Возврат Размер;
	Иначе
		
		Пока ТаблицаРезультат.Следующий() Цикл
			Если ЗначениеЗаполнено(ТаблицаРезультат.Размер) Тогда
				Размер = Размер + ТаблицаРезультат.Размер;
			КонецЕсли;
		КонецЦикла;
		
		Возврат Размер;
		
	КонецЕсли;
	
КонецФункции

Функция ПечатьИзЭлектронногоПредставленияНеПоддерживается(ВидОтчета) Экспорт
	
	Если ПолучитьСписокОтчетовПоддерживающихПечатьИзЭлектронногоПредставления().Найти(ВидОтчета) = Неопределено Тогда
		Возврат Истина;
	Иначе
		Возврат Ложь;
	КонецЕсли;
	
КонецФункции

Функция ПолучитьСписокОтчетовПоддерживающихПечатьИзЭлектронногоПредставления() Экспорт
	
	Запрос = Новый Запрос;
	Запрос.Текст =
	"ВЫБРАТЬ
	|	ВидыОтправляемыхДокументов.Ссылка
	|ИЗ
	|	Справочник.ВидыОтправляемыхДокументов КАК ВидыОтправляемыхДокументов
	|ГДЕ
	|	НЕ ВидыОтправляемыхДокументов.ПометкаУдаления
	|	И ВидыОтправляемыхДокументов.Предопределенный
	|	И ВидыОтправляемыхДокументов.ТипДокумента В (ЗНАЧЕНИЕ(Перечисление.ТипыОтправляемыхДокументов.БухгалтерскаяОтчетность), ЗНАЧЕНИЕ(Перечисление.ТипыОтправляемыхДокументов.ПрочаяОтчетностьФНС))
	|	И НЕ ВидыОтправляемыхДокументов.Родитель В ИЕРАРХИИ (ЗНАЧЕНИЕ(Справочник.ВидыОтправляемыхДокументов.УдалитьОтчетностьПоАлкоголю))
	|
	|УПОРЯДОЧИТЬ ПО
	|	ВидыОтправляемыхДокументов.Наименование";
	
	Возврат Запрос.Выполнить().Выгрузить().ВыгрузитьКолонку("Ссылка");

КонецФункции


//////////////////////////////////////////////////////////////////////////////////
//// РАБОТА С НЕФОРМАЛИЗОВАННЫМИ ДОКУМЕНТАМИ
////

Функция ПолучитьВложениеНеформализованногоДокумента(НеформализованныйДокумент, ИмяФайла, ХранилищеФайла, Тип = Неопределено, Размер = Неопределено, ВАрхиве = Ложь) Экспорт
	
	ТаблицаЗапроса = ПолучитьВложенияНеформализованногоДокумента(НеформализованныйДокумент, Неопределено, Истина);
	
	ИмяФайла = "";
	ХранилищеФайла = Неопределено;
	Размер = Неопределено;
	ВАрхиве = Ложь;
		
	Если ТаблицаЗапроса.Количество() > 0 Тогда
		ИмяФайла = ТаблицаЗапроса[0].ИмяФайла;
		Тип = ТаблицаЗапроса[0].Тип;
		Размер = ТаблицаЗапроса[0].Размер;
		ВАрхиве = ТаблицаЗапроса[0].ВАрхиве;
		
		Возврат Истина;
		
	КонецЕсли;
	
	Возврат Ложь;
	
КонецФункции

Функция ДобавитьВложенияПисьма(Письмо, ИмяФайла, Данные, Тип, Размер = Неопределено) Экспорт
	
	Если НЕ ЗначениеЗаполнено(Письмо) Тогда
		Возврат Ложь;
	КонецЕсли;
	
	Если Размер = Неопределено Тогда
		Размер = ОпределитьРазмер(Данные);
	КонецЕсли;
	
	НачатьТранзакцию();
	
	МенеджерЗаписи = РегистрыСведений.ВложенияНеформализованныхДокументов.СоздатьМенеджерЗаписи();
	МенеджерЗаписи.НеформализованныйДокумент = Письмо;
	МенеджерЗаписи.ИмяФайла = ИмяФайла;
	МенеджерЗаписи.Тип = Тип;
	МенеджерЗаписи.Размер = Размер;
	
	ТипДанных = ТипЗнч(Данные);
	Если ТипДанных = Тип("ХранилищеЗначения") Тогда
		ДвоичныеДанныеФайла = Данные.Получить();
	ИначеЕсли ТипДанных = Тип("Строка") Тогда
		ДвоичныеДанныеФайла = Новый ДвоичныеДанные(Данные);
	Иначе
		ДвоичныеДанныеФайла = Данные;
	КонецЕсли;
	АдресФайла = ПоместитьВоВременноеХранилище(ДвоичныеДанныеФайла, Новый УникальныйИдентификатор);
	
	КомпонентыИмениФайла = ОбщегоНазначенияКлиентСервер.РазложитьПолноеИмяФайла(ИмяФайла);
	УникальноеИмяФайлаБезРасширения = ОбщегоНазначенияЭДКОКлиентСервер.УникальнаяСтрока(КомпонентыИмениФайла.ИмяБезРасширения, 150);
	РасширениеФайлаБезТочки = Сред(КомпонентыИмениФайла.Расширение, 2);
	
	ПараметрыФайла = Новый Структура;
	ПараметрыФайла.Вставить("Автор", 						Неопределено);
	ПараметрыФайла.Вставить("ВладелецФайлов", 				Письмо);
	ПараметрыФайла.Вставить("ИмяБезРасширения", 			УникальноеИмяФайлаБезРасширения);
	ПараметрыФайла.Вставить("РасширениеБезТочки", 			РасширениеФайлаБезТочки);
	ПараметрыФайла.Вставить("ВремяИзмененияУниверсальное", 	Неопределено);
	
	ИнформацияОбОбновляемомФайле = Новый Структура;
	ИнформацияОбОбновляемомФайле.Вставить("АдресФайлаВоВременномХранилище", АдресФайла);
	ИнформацияОбОбновляемомФайле.Вставить("АдресВременногоХранилищаТекста", Неопределено);
	ИнформацияОбОбновляемомФайле.Вставить("ИмяБезРасширения", 				УникальноеИмяФайлаБезРасширения);
	ИнформацияОбОбновляемомФайле.Вставить("Расширение", 					РасширениеФайлаБезТочки);
	
	Попытка
		МенеджерЗаписи.Записать(Истина);
		
		МассивФайлов = ОбщегоНазначенияЭДКО.ПрикрепленныеФайлыКОбъектуИзСправочника(
			Письмо,
			"ПерепискаСКонтролирующимиОрганамиПрисоединенныеФайлы",
			ИмяФайла);
		
		Если МассивФайлов.Количество() > 0 Тогда
			ПрисоединенныйФайл = МассивФайлов[0];
			РаботаСФайлами.ОбновитьФайл(ПрисоединенныйФайл, ИнформацияОбОбновляемомФайле);
			
		Иначе
			ПрисоединенныйФайл = РаботаСФайлами.ДобавитьФайл(ПараметрыФайла, АдресФайла);
		КонецЕсли;
		
		ПрисоединенныйФайлОбъект = ПрисоединенныйФайл.ПолучитьОбъект();
		
		ПрисоединенныйФайлОбъект.Тип = Тип;
		ПрисоединенныйФайлОбъект.ИсходноеИмяФайла = ИмяФайла;
		
		ПрисоединенныйФайлОбъект.Записать();
	Исключение
		ОтменитьТранзакцию();
		УдалитьИзВременногоХранилища(АдресФайла);
		ТекстСообщения = СтроковыеФункцииКлиентСервер.ПодставитьПараметрыВСтроку(НСтр("ru='Не удалось сохранить в информационной базе вложение %1 письма %2!%3'"),
																					Символ(34) + ИмяФайла + Символ(34),
																					Символ(34) + Письмо + Символ(34),
																					Символы.ПС + ОписаниеОшибки());
		ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
		Возврат Ложь;
	КонецПопытки;
	
	ЗафиксироватьТранзакцию();
	УдалитьИзВременногоХранилища(АдресФайла);
	Возврат Истина;
	
КонецФункции

Функция ИзвлечьИменаФайловНФДИзОписания(ФайлОписания) Экспорт
	
	// выгружаем файл на диск, при необходимости
	ТипЗнчФайлОписания = ТипЗнч(ФайлОписания);
	Если ТипЗнчФайлОписания = Тип("ДвоичныеДанные") Тогда
		ВремФайл = ПолучитьИмяВременногоФайла();
		ФайлОписания.Записать(ВремФайл);
		НеобходимоУдалитьФайлОписания = Истина;
	ИначеЕсли ТипЗнчФайлОписания = Тип("ХранилищеЗначения") Тогда
		ВремФайл = ПолучитьИмяВременногоФайла();
		ФайлОписания.Получить().Записать(ВремФайл);
		НеобходимоУдалитьФайлОписания = Истина;
	Иначе
		ВремФайл = ФайлОписания;
		НеобходимоУдалитьФайлОписания = Ложь;
	КонецЕсли;
		
	// читаем содержимое
	Текст = НовыйЧтениеТекстаНаСервере(ВремФайл);
	СодержимоеФайлаОписания = Текст.Прочитать();
	Текст.Закрыть();
	
	// удаляем файл, при необходимости
	Если НеобходимоУдалитьФайлОписания Тогда
		УдалитьФайлы(ВремФайл);
	КонецЕсли;
	
	// анализируем файл
	Результат = Новый Соответствие;
	Для Инд = 1 По СтрЧислоСтрок(СодержимоеФайлаОписания) Цикл
		ТекСтр = СтрПолучитьСтроку(СодержимоеФайлаОписания, Инд);
		ВхождениеДвоеточия = СтрНайти(ТекСтр, ":");
		Если ВхождениеДвоеточия <> 0 Тогда
			Ключ = Лев(ТекСтр, ВхождениеДвоеточия - 1);
			Значение = Сред(ТекСтр, ВхождениеДвоеточия + 1);
			Если НЕ ПустаяСтрока(Ключ) И НЕ ПустаяСтрока(Значение) Тогда
				Результат.Вставить(СокрЛП(Ключ), СокрЛП(Значение));
			КонецЕсли;
		КонецЕсли;
	КонецЦикла;
	
	Возврат Результат;
	
КонецФункции

// продублирована в контейнере
Функция ПолучитьВложенияНеформализованногоДокумента(ДокНФД, ИмяФайла = Неопределено, СДанными = Ложь) Экспорт
	
	Запрос = Новый Запрос("ВЫБРАТЬ
						  |	ВложенияНеформализованныхДокументов.ИмяФайла,
						  |	ВложенияНеформализованныхДокументов.Размер,
						  |	ВложенияНеформализованныхДокументов.Тип
						  |	, ВЫБОР
					  	  |		КОГДА СостояниеОбъектов.Архивный ЕСТЬ NULL ТОГДА Ложь
					   	  |		КОГДА СостояниеОбъектов.Архивный = Ложь ТОГДА Ложь
					   	  |	ИНАЧЕ
					   	  |		Истина
					   	  |	КОНЕЦ ВАрхиве");
	Если СДанными Тогда
		Запрос.Текст = Запрос.Текст + ",
						  |	ВложенияНеформализованныхДокументов.Данные";
	КонецЕсли;
	Запрос.Текст = Запрос.Текст + "
						  |ИЗ
						  |	РегистрСведений.ВложенияНеформализованныхДокументов КАК ВложенияНеформализованныхДокументов
						  |
						  |		ЛЕВОЕ СОЕДИНЕНИЕ РегистрСведений.ПризнакиАрхивированияФайловДОСКонтролирующимиОрганами КАК СостояниеОбъектов
						  |			ПО 		(СостояниеОбъектов.Объект = ВложенияНеформализованныхДокументов.НеформализованныйДокумент)
						  |				И	(СостояниеОбъектов.ИмяФайла = ВложенияНеформализованныхДокументов.ИмяФайла)
						  |				И	(СостояниеОбъектов.Владелец = Значение(Перечисление.ВидыАрхивируемыхМетаданныхДО.ВложенияНеформализованныхДокументов))
						  |
						  |ГДЕ
						  |	ВложенияНеформализованныхДокументов.НеформализованныйДокумент = &НеформализованныйДокумент";
	
	Если ИмяФайла <> Неопределено Тогда
		Запрос.Текст = Запрос.Текст + "
						  |	И ВложенияНеформализованныхДокументов.ИмяФайла " + ?(ТипЗнч(ИмяФайла) = Тип("Строка"), "=", "В") + " (&ИмяФайла)";
		Запрос.УстановитьПараметр("ИмяФайла", ИмяФайла);
	КонецЕсли;
	Запрос.УстановитьПараметр("НеформализованныйДокумент", ДокНФД);
	
	Результат = Запрос.Выполнить().Выгрузить();
	
	Если СДанными Тогда
		ПрочитатьПрисоединенныеФайлы = Ложь;
		Для каждого СтрокаРезультата Из Результат Цикл
			Если СтрокаРезультата.Данные.Получить() = Неопределено Тогда
				ПрочитатьПрисоединенныеФайлы = Истина;
				Прервать;
			КонецЕсли;
		КонецЦикла;
		
		Если ПрочитатьПрисоединенныеФайлы Тогда
			ИсходноеИмяФайла = ?(ТипЗнч(ИмяФайла) = Тип("Строка"), ИмяФайла, Неопределено);
			ТаблицаФайлов = ОбщегоНазначенияЭДКО.ПрикрепленныеФайлыКОбъектуИзСправочника(
				ДокНФД,
				"ПерепискаСКонтролирующимиОрганамиПрисоединенныеФайлы",
				ИсходноеИмяФайла,
				"ИсходноеИмяФайла");
			
			Для каждого СтрокаТаблицы Из ТаблицаФайлов Цикл
				СтрокаТаблицы.ИсходноеИмяФайла = ВРЕГ(СтрокаТаблицы.ИсходноеИмяФайла);
			КонецЦикла;
			
			Для каждого СтрокаРезультата Из Результат Цикл
				Если СтрокаРезультата.Данные.Получить() = Неопределено Тогда
					ИсходноеИмяФайла = ВРЕГ(СтрокаРезультата.ИмяФайла);
					
					СтрокаТаблицы = ТаблицаФайлов.Найти(ИсходноеИмяФайла, "ИсходноеИмяФайла");
					Если СтрокаТаблицы <> Неопределено Тогда
						ДвоичныеДанныеФайла = РаботаСФайлами.ДвоичныеДанныеФайла(СтрокаТаблицы.Ссылка);
						СтрокаРезультата.Данные = Новый ХранилищеЗначения(ДвоичныеДанныеФайла, Новый СжатиеДанных(0));
					КонецЕсли;
				КонецЕсли;
			КонецЦикла;
		КонецЕсли;
	КонецЕсли;
	
	Возврат Результат;
	
КонецФункции

Функция ДобавитьСодержимоеТранспортногоКонтейнера(ТранспортноеСообщение,
													Тип,
													Данные,
													ИмяФайла,
													Размер = Неопределено,
													ЭЦПИмяПодписанногоФайла = Неопределено,
													ЭЦПСертификат = Неопределено,
													ЭЦПСтатусПроверки = Неопределено,
													ЭЦПЭтоПодписьАбонента = Неопределено,
													ТипФайлаОтчетностиПФР = Неопределено,
													Идентификатор = Неопределено,
													ТипСодержимогоФайла = Неопределено) Экспорт
	
	Если Размер = Неопределено Тогда
		Попытка
			Размер = ОпределитьРазмер(Данные);
		Исключение
			ИнформацияОбОшибке = ИнформацияОбОшибке().Описание;
			ТекстСообщения = СтроковыеФункцииКлиентСервер.ПодставитьПараметрыВСтроку(
				НСтр("ru='Повреждено содержимое транспортного контейнера сообщения %1: отсутствует файл.%2'"),
				Символ(34) + ТранспортноеСообщение.Ссылка + Символ(34),
				Символы.ПС + ИнформацияОбОшибке);
			ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения,,,,, НовыйКонтекстРасшифровки(ТранспортноеСообщение));
			Возврат Ложь;
		КонецПопытки;
	КонецЕсли;
	
	НачатьТранзакцию();
	
	МенеджерЗаписи = РегистрыСведений.СодержимоеТранспортныхКонтейнеров.СоздатьМенеджерЗаписи();
	МенеджерЗаписи.ТранспортноеСообщение = ТранспортноеСообщение;
	МенеджерЗаписи.ИмяФайла = ИмяФайла;
	МенеджерЗаписи.Тип = Тип;
	МенеджерЗаписи.Размер = Размер;
	МенеджерЗаписи.Идентификатор = Идентификатор;
	
	Если ЭЦПСертификат <> Неопределено Тогда
		МенеджерЗаписи.ЭЦПСертификат = ЭЦПСертификат;
	КонецЕсли;
	
	Если ЭЦПСтатусПроверки <> Неопределено Тогда
		МенеджерЗаписи.ЭЦПСтатусПроверки = ЭЦПСтатусПроверки;
	КонецЕсли;
	
	Если ЭЦПИмяПодписанногоФайла <> Неопределено Тогда
		МенеджерЗаписи.ЭЦПИмяПодписанногоФайла = ЭЦПИмяПодписанногоФайла;
	КонецЕсли;
	
	Если ЭЦПЭтоПодписьАбонента <> Неопределено Тогда
		МенеджерЗаписи.ЭЦПЭтоПодписьАбонента = ЭЦПЭтоПодписьАбонента;
	КонецЕсли;
	
	Если ТипФайлаОтчетностиПФР <> Неопределено Тогда
		МенеджерЗаписи.ТипФайлаОтчетностиПФР = ТипФайлаОтчетностиПФР;
	КонецЕсли;
	
	ТипСодержимогоФайлаПриведенный = ТипСодержимогоФайла;
	Если ТипСодержимогоФайлаПриведенный <> Неопределено Тогда
		Если ТипЗнч(ТипСодержимогоФайлаПриведенный) = Тип("Строка") Тогда
			ТипСодержимогоФайлаПриведенный = ТипСодержимогоПоСтроке(ТипСодержимогоФайла);
		КонецЕсли;
		МенеджерЗаписи.ТипСодержимогоФайла = ТипСодержимогоФайлаПриведенный;
	КонецЕсли;
	
	ТипДанных = ТипЗнч(Данные);
	Если ТипДанных = Тип("ХранилищеЗначения") Тогда // если хранилище значения
		ДвоичныеДанныеФайла = Данные.Получить();
	ИначеЕсли ТипДанных = Тип("Строка") Тогда // если полное имя файла
		Если ЭтоАдресВременногоХранилища(Данные) Тогда
			ДвоичныеДанныеФайла = ПолучитьИзВременногоХранилища(Данные);
		Иначе
			ДвоичныеДанныеФайла = Новый ДвоичныеДанные(Данные);
		КонецЕсли;
		
		Если (Размер = Неопределено Или Размер = 0) И (ТипЗнч(ДвоичныеДанныеФайла) = Тип("ДвоичныеДанные")) Тогда
			МенеджерЗаписи.Размер = ДвоичныеДанныеФайла.Размер();
		КонецЕсли;
	ИначеЕсли ТипДанных = Тип("Структура") Тогда
		
		ДвоичныеДанныеФайла = ДвоичныеДанныеИзСтроки64НаСервере(Данные.Строка64);
		
		Если (Размер = Неопределено Или Размер = 0) И (ТипЗнч(ДвоичныеДанныеФайла) = Тип("ДвоичныеДанные")) Тогда
			МенеджерЗаписи.Размер = ДвоичныеДанныеФайла.Размер();
		КонецЕсли;
	Иначе // если двоичные данные
		ДвоичныеДанныеФайла = Данные;
	КонецЕсли;
	АдресФайла = ПоместитьВоВременноеХранилище(ДвоичныеДанныеФайла, Новый УникальныйИдентификатор);
	
	КомпонентыИмениФайла = ОбщегоНазначенияКлиентСервер.РазложитьПолноеИмяФайла(ИмяФайла);
	УникальноеИмяФайлаБезРасширения = ОбщегоНазначенияЭДКОКлиентСервер.УникальнаяСтрока(КомпонентыИмениФайла.ИмяБезРасширения, 150);
	РасширениеФайлаБезТочки = Сред(КомпонентыИмениФайла.Расширение, 2);
	
	ПараметрыФайла = Новый Структура;
	ПараметрыФайла.Вставить("Автор", 						Неопределено);
	ПараметрыФайла.Вставить("ВладелецФайлов", 				ТранспортноеСообщение);
	ПараметрыФайла.Вставить("ИмяБезРасширения", 			УникальноеИмяФайлаБезРасширения);
	ПараметрыФайла.Вставить("РасширениеБезТочки", 			РасширениеФайлаБезТочки);
	ПараметрыФайла.Вставить("ВремяИзмененияУниверсальное", 	Неопределено);
	
	ИнформацияОбОбновляемомФайле = Новый Структура;
	ИнформацияОбОбновляемомФайле.Вставить("АдресФайлаВоВременномХранилище", АдресФайла);
	ИнформацияОбОбновляемомФайле.Вставить("АдресВременногоХранилищаТекста", Неопределено);
	ИнформацияОбОбновляемомФайле.Вставить("ИмяБезРасширения", 				УникальноеИмяФайлаБезРасширения);
	ИнформацияОбОбновляемомФайле.Вставить("Расширение", 					РасширениеФайлаБезТочки);
	
	Попытка
		МенеджерЗаписи.Записать(Истина);
		
		МассивФайлов = ОбщегоНазначенияЭДКО.ПрикрепленныеФайлыКОбъектуИзСправочника(
			ТранспортноеСообщение,
			"ТранспортноеСообщениеПрисоединенныеФайлы",
			ИмяФайла);
		
		Если МассивФайлов.Количество() > 0 Тогда
			ПрисоединенныйФайл = МассивФайлов[0];
			РаботаСФайлами.ОбновитьФайл(ПрисоединенныйФайл, ИнформацияОбОбновляемомФайле);
			
		Иначе
			НоваяСсылкаНаФайл = РаботаСФайлами.НоваяСсылкаНаФайл(ТранспортноеСообщение, "ТранспортноеСообщениеПрисоединенныеФайлы");
			ПрисоединенныйФайл = РаботаСФайлами.ДобавитьФайл(ПараметрыФайла, АдресФайла,,, НоваяСсылкаНаФайл);
		КонецЕсли;
		
		ПрисоединенныйФайлОбъект = ПрисоединенныйФайл.ПолучитьОбъект();
		
		ПрисоединенныйФайлОбъект.Тип = Тип;
		ПрисоединенныйФайлОбъект.Идентификатор = Идентификатор;
		
		Если ЭЦПСертификат <> Неопределено Тогда
			ПрисоединенныйФайлОбъект.ЭЦПСертификат = ЭЦПСертификат;
		КонецЕсли;
		
		Если ЭЦПСтатусПроверки <> Неопределено Тогда
			ПрисоединенныйФайлОбъект.ЭЦПСтатусПроверки = ЭЦПСтатусПроверки;
		КонецЕсли;
		
		Если ЭЦПИмяПодписанногоФайла <> Неопределено Тогда
			ПрисоединенныйФайлОбъект.ЭЦПИмяПодписанногоФайла = ЭЦПИмяПодписанногоФайла;
		КонецЕсли;
		
		Если ЭЦПЭтоПодписьАбонента <> Неопределено Тогда
			ПрисоединенныйФайлОбъект.ЭЦПЭтоПодписьАбонента = ЭЦПЭтоПодписьАбонента;
		КонецЕсли;
		
		Если ТипФайлаОтчетностиПФР <> Неопределено Тогда
			ПрисоединенныйФайлОбъект.ТипФайлаОтчетностиПФР = ТипФайлаОтчетностиПФР;
		КонецЕсли;
		
		Если ТипСодержимогоФайлаПриведенный <> Неопределено Тогда
			ПрисоединенныйФайлОбъект.ТипСодержимогоФайла = ТипСодержимогоФайлаПриведенный;
		КонецЕсли;
		
		ПрисоединенныйФайлОбъект.ИсходноеИмяФайла = ИмяФайла;
		
		ПрисоединенныйФайлОбъект.Записать();
	Исключение

		ОтменитьТранзакцию();
		УдалитьИзВременногоХранилища(АдресФайла);
		ИнформацияОбОшибке = ИнформацияОбОшибке().Описание;
		ТекстСообщения = СтроковыеФункцииКлиентСервер.ПодставитьПараметрыВСтроку(
			НСтр("ru='Не удалось сохранить в информационной базе содержимое транспортного контейнера сообщения %1!%2'"),
			Символ(34) + ТранспортноеСообщение.Ссылка + Символ(34),
			Символы.ПС + ИнформацияОбОшибке);
		ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
		Возврат Ложь;
	КонецПопытки;
	
	ЗафиксироватьТранзакцию();
	УдалитьИзВременногоХранилища(АдресФайла);
	Возврат Истина;
	
КонецФункции

Функция ОпределитьРазмер(Данные) Экспорт
	
	Размер = 0;
	
	ТипДанных = ТипЗнч(Данные);
	Если ТипДанных = Тип("Строка") Тогда
		Если ЭтоАдресВременногоХранилища(Данные) Тогда // если это адрес во временном хранилище
			ДвоичныеДанные = ПолучитьИзВременногоХранилища(Данные);
			Размер = ДвоичныеДанные.Размер();
		Иначе // если это файл на сервере
			ОбъектФайл = Новый Файл(Данные);
			Размер = ОбъектФайл.Размер();
		КонецЕсли;
	ИначеЕсли ТипДанных = Тип("ХранилищеЗначения") Тогда
		ДвоичныеДанные = Данные.Получить();
		Если ТипЗнч(ДвоичныеДанные) <> Тип("ДвоичныеДанные") Тогда
			Возврат 0;
		КонецЕсли;
		Размер = ДвоичныеДанные.Размер();
	ИначеЕсли ТипДанных = Тип("ДвоичныеДанные") Тогда
		Размер = Данные.Размер();
	КонецЕсли;
	
	Возврат Размер;
	
КонецФункции

Функция ПолучитьВложенияТранспортногоСообщения(ТранспортноеСообщение, СДанными = Ложь, ФильтрПоТипу = Неопределено, ИмяФайла = Неопределено, САдресом = Ложь) Экспорт
	
	Запрос = Новый Запрос;
	Запрос.Текст = "ВЫБРАТЬ РАЗРЕШЕННЫЕ
					  |	СодержимоеТранспортныхКонтейнеров.ИмяФайла,
					  |	СодержимоеТранспортныхКонтейнеров.Размер,
					  |	СодержимоеТранспортныхКонтейнеров.Тип,
					  |	СодержимоеТранспортныхКонтейнеров.ЭЦПСертификат,
					  |	СодержимоеТранспортныхКонтейнеров.ЭЦПСтатусПроверки,
					  |	СодержимоеТранспортныхКонтейнеров.ЭЦПИмяПодписанногоФайла,
					  |	СодержимоеТранспортныхКонтейнеров.ЭЦПЭтоПодписьАбонента,
					  |	СодержимоеТранспортныхКонтейнеров.ТипФайлаОтчетностиПФР,
					  |	СодержимоеТранспортныхКонтейнеров.Идентификатор,
					  |	СодержимоеТранспортныхКонтейнеров.ТипСодержимогоФайла,
					  |	СодержимоеТранспортныхКонтейнеров.ТранспортноеСообщение
					  |	, ВЫБОР
					  |		КОГДА СостояниеОбъектов.Архивный ЕСТЬ NULL ТОГДА Ложь
					  |		КОГДА СостояниеОбъектов.Архивный = Ложь ТОГДА Ложь
					  |	ИНАЧЕ
					  |		Истина
					  |	КОНЕЦ ВАрхиве";
	Если СДанными Тогда
		Запрос.Текст = Запрос.Текст + ",
					  |	СодержимоеТранспортныхКонтейнеров.Данные";
	КонецЕсли;
	Запрос.Текст = Запрос.Текст + "
					  |ИЗ
					  |	РегистрСведений.СодержимоеТранспортныхКонтейнеров КАК СодержимоеТранспортныхКонтейнеров
					  |
					  |		ЛЕВОЕ СОЕДИНЕНИЕ РегистрСведений.ПризнакиАрхивированияФайловДОСКонтролирующимиОрганами КАК СостояниеОбъектов
					  |			ПО 		(СостояниеОбъектов.Объект = СодержимоеТранспортныхКонтейнеров.ТранспортноеСообщение)
					  |				И	(СостояниеОбъектов.ИмяФайла = СодержимоеТранспортныхКонтейнеров.ИмяФайла)
					  |				И	(СостояниеОбъектов.Владелец = Значение(Перечисление.ВидыАрхивируемыхМетаданныхДО.СодержимоеТранспортныхКонтейнеров))
					  |
					  |ГДЕ
					  |	СодержимоеТранспортныхКонтейнеров.ТранспортноеСообщение В (&ТранспортноеСообщение)";
	
	Если ИмяФайла <> Неопределено Тогда
		Если ТипЗнч(ИмяФайла) = Тип("СписокЗначений") ИЛИ ТипЗнч(ИмяФайла) = Тип("Массив") Тогда
			Запрос.Текст = Запрос.Текст + "
					  	  |	И СодержимоеТранспортныхКонтейнеров.ИмяФайла В (&парамИмяФайла)";
			Запрос.УстановитьПараметр("парамИмяФайла", ИмяФайла);
		ИначеЕсли ТипЗнч(ИмяФайла) = Тип("Строка") И ЗначениеЗаполнено(ИмяФайла) Тогда
			Запрос.Текст = Запрос.Текст + "
					  	  |	И СодержимоеТранспортныхКонтейнеров.ИмяФайла = &парамИмяФайла";
			Запрос.УстановитьПараметр("парамИмяФайла", ИмяФайла);
		КонецЕсли;
	КонецЕсли;
	
	Если ФильтрПоТипу <> Неопределено Тогда
		Если ТипЗнч(ФильтрПоТипу) = Тип("СписокЗначений") ИЛИ ТипЗнч(ФильтрПоТипу) = Тип("Массив") Тогда
			Запрос.Текст = Запрос.Текст + "
					  |	И СодержимоеТранспортныхКонтейнеров.Тип В(&ТипыТранспортныхСообщений)";
			Запрос.УстановитьПараметр("ТипыТранспортныхСообщений", ФильтрПоТипу);
		Иначе
			Запрос.Текст = Запрос.Текст + "
					  |	И СодержимоеТранспортныхКонтейнеров.Тип = &ТипТранспортногоСообщения";
			Запрос.УстановитьПараметр("ТипТранспортногоСообщения", ФильтрПоТипу);
		КонецЕсли;
	КонецЕсли;
	
	ТипСообщения = ТипЗнч(ТранспортноеСообщение);
	Если ТипСообщения = Тип("ДокументСсылка.ТранспортноеСообщение") ИЛИ ТипСообщения = Тип("Массив") Тогда
		ТранспортноеСообщениеСсылкаИлиМассив = ТранспортноеСообщение;
	Иначе
		ТранспортноеСообщениеСсылкаИлиМассив = ТранспортноеСообщение.Ссылка;
	КонецЕсли;
	Запрос.УстановитьПараметр("ТранспортноеСообщение", ТранспортноеСообщениеСсылкаИлиМассив);
	
	ТаблицаВложений = Запрос.Выполнить().Выгрузить();
	
	Если СДанными Тогда
		ТранспортныеСообщения = Новый Массив;
		Для каждого СтрокаТаблицыВложений Из ТаблицаВложений Цикл
			Если ТранспортныеСообщения.Найти(СтрокаТаблицыВложений.ТранспортноеСообщение) = Неопределено
				И СтрокаТаблицыВложений.Данные.Получить() = Неопределено Тогда
				
				ТранспортныеСообщения.Добавить(СтрокаТаблицыВложений.ТранспортноеСообщение);
			КонецЕсли;
		КонецЦикла;
		
		ТаблицаФайлов = Неопределено;
		Если ТранспортныеСообщения.Количество() > 0 Тогда
			ТаблицаФайлов = ОбщегоНазначенияЭДКО.ПрикрепленныеФайлыКОбъектуИзСправочника(
				ТранспортныеСообщения,
				"ТранспортноеСообщениеПрисоединенныеФайлы",
				,
				"ВладелецФайла, ИсходноеИмяФайла");
			
			Для каждого СтрокаТаблицы Из ТаблицаФайлов Цикл
				СтрокаТаблицы.ИсходноеИмяФайла = ВРЕГ(СтрокаТаблицы.ИсходноеИмяФайла);
			КонецЦикла;
		КонецЕсли;
		
		Для каждого СтрокаТаблицыВложений Из ТаблицаВложений Цикл
			Если СтрокаТаблицыВложений.Данные.Получить() = Неопределено Тогда
				ИсходноеИмяФайла = ВРЕГ(СтрокаТаблицыВложений.ИмяФайла);
				
				ПараметрыОтбора = Новый Структура("ВладелецФайла, ИсходноеИмяФайла",
					СтрокаТаблицыВложений.ТранспортноеСообщение, ИсходноеИмяФайла);
				СтрокиТаблицы = ТаблицаФайлов.НайтиСтроки(ПараметрыОтбора);
				Если СтрокиТаблицы.Количество() > 0 Тогда
					Попытка
						ДвоичныеДанныеФайла = РаботаСФайлами.ДвоичныеДанныеФайла(СтрокиТаблицы[0].Ссылка);
						СтрокаТаблицыВложений.Данные = Новый ХранилищеЗначения(ДвоичныеДанныеФайла, Новый СжатиеДанных(0));
					Исключение
						ТекстОшибки = СтрШаблон(
							НСтр("ru = 'Ошибка при получении присоединенного файла содержимого транспортного контейнера: %1'"),
							ПодробноеПредставлениеОшибки(ИнформацияОбОшибке()));
						ЗаписьЖурналаРегистрации(
							НСтр("ru = 'Электронный документооборот с контролирующими органами. Транспортное сообщение'",
								ОбщегоНазначения.КодОсновногоЯзыка()),
							УровеньЖурналаРегистрации.Ошибка,,
							ТранспортноеСообщение,
							ТекстОшибки);
						ВызватьИсключение;
					КонецПопытки;
				КонецЕсли;
			КонецЕсли;
		КонецЦикла;
	КонецЕсли;
	
	Если САдресом Тогда
		ТаблицаВложений.Колонки.Добавить("Адрес", Новый ОписаниеТипов("Строка"));
		Для каждого Вложение Из ТаблицаВложений Цикл
			Вложение.Адрес = ПоместитьВоВременноеХранилище(Вложение.Данные.Получить(), Новый УникальныйИдентификатор);
		КонецЦикла; 
	КонецЕсли;
	
	Возврат ТаблицаВложений;
	
КонецФункции

Функция ПолучитьТранспортныйКонтейнер(
		ТранспортноеСообщение,
		СДанными = Ложь,
		ВозвращатьИсключениеПриЧтенииДанных = Ложь,
		ЗначениеДанныхПриОшибкеЧтения = Неопределено) Экспорт
	
	Запрос = Новый Запрос;
	Запрос.Текст = "ВЫБРАТЬ РАЗРЕШЕННЫЕ
					  |	ТранспортныеКонтейнеры.ИмяФайла
					  |	, ВЫБОР
					  |		КОГДА СостояниеОбъектов.Архивный ЕСТЬ NULL ТОГДА Ложь
					  |		КОГДА СостояниеОбъектов.Архивный = Ложь ТОГДА Ложь
					  |	ИНАЧЕ
					  |		Истина
					  |	КОНЕЦ ВАрхиве";
	
	Если СДанными = Истина Тогда
		Запрос.Текст = Запрос.Текст + ",
					  |	ТранспортныеКонтейнеры.Данные";
	КонецЕсли;
	Запрос.Текст = Запрос.Текст + "
					  |ИЗ
					  |	РегистрСведений.ТранспортныеКонтейнеры КАК ТранспортныеКонтейнеры
					  |
					  |		ЛЕВОЕ СОЕДИНЕНИЕ РегистрСведений.ПризнакиАрхивированияФайловДОСКонтролирующимиОрганами КАК СостояниеОбъектов
					  |			ПО 		(СостояниеОбъектов.Объект = ТранспортныеКонтейнеры.ТранспортноеСообщение)
					  |				И	(СостояниеОбъектов.ИмяФайла = ТранспортныеКонтейнеры.ИмяФайла)
					  |				И	(СостояниеОбъектов.Владелец = Значение(Перечисление.ВидыАрхивируемыхМетаданныхДО.ТранспортныеКонтейнеры))
					  |
					  |ГДЕ
					  |	ТранспортныеКонтейнеры.ТранспортноеСообщение = &ТранспортноеСообщение";
	
	Запрос.УстановитьПараметр("ТранспортноеСообщение", ТранспортноеСообщение);
	Результат = Запрос.Выполнить().Выгрузить();
	
	Если СДанными = Истина Тогда
		ПрочитатьПрисоединенныеФайлы = Ложь;
		Для каждого СтрокаРезультата Из Результат Цикл
			Если СтрокаРезультата.Данные.Получить() = Неопределено Тогда
				ПрочитатьПрисоединенныеФайлы = Истина;
				Прервать;
			КонецЕсли;
		КонецЦикла;
		
		Если ПрочитатьПрисоединенныеФайлы Тогда
			ТаблицаФайлов = ОбщегоНазначенияЭДКО.ПрикрепленныеФайлыКОбъектуИзСправочника(
				ТранспортноеСообщение,
				"ТранспортноеСообщениеКонтейнерыПрисоединенныеФайлы",
				,
				"ИсходноеИмяФайла");
			
			Для каждого СтрокаТаблицы Из ТаблицаФайлов Цикл
				СтрокаТаблицы.ИсходноеИмяФайла = ВРЕГ(СтрокаТаблицы.ИсходноеИмяФайла);
			КонецЦикла;
			
			Для каждого СтрокаРезультата Из Результат Цикл
				Если СтрокаРезультата.Данные.Получить() = Неопределено Тогда
					ИсходноеИмяФайла = ВРЕГ(СтрокаРезультата.ИмяФайла);
					
					СтрокаТаблицы = ТаблицаФайлов.Найти(ИсходноеИмяФайла, "ИсходноеИмяФайла");
					Если СтрокаТаблицы <> Неопределено Тогда
						Попытка
							ДвоичныеДанныеФайла = РаботаСФайлами.ДвоичныеДанныеФайла(СтрокаТаблицы.Ссылка);
							СтрокаРезультата.Данные = Новый ХранилищеЗначения(ДвоичныеДанныеФайла, Новый СжатиеДанных(0));
						Исключение
							Если ВозвращатьИсключениеПриЧтенииДанных Тогда
								ВызватьИсключение;
								
							Иначе
								Если ЗначениеДанныхПриОшибкеЧтения <> Неопределено Тогда
									СтрокаРезультата.Данные = Новый ХранилищеЗначения(ЗначениеДанныхПриОшибкеЧтения, Новый СжатиеДанных(0));
								КонецЕсли;
								ТекстОшибки = СтрШаблон(
									НСтр("ru = 'Ошибка при получении присоединенного файла транспортного контейнера: %1'"),
									ПодробноеПредставлениеОшибки(ИнформацияОбОшибке()));
								ЗаписьЖурналаРегистрации(
									НСтр("ru = 'Электронный документооборот с контролирующими органами. Транспортное сообщение'",
										ОбщегоНазначения.КодОсновногоЯзыка()),
									УровеньЖурналаРегистрации.Ошибка,,
									ТранспортноеСообщение,
									ТекстОшибки);
							КонецЕсли;
						КонецПопытки;
					КонецЕсли;
				КонецЕсли;
			КонецЦикла;
		КонецЕсли;
	КонецЕсли;
	
	Возврат Результат;
	
КонецФункции

Функция ТранспортныйКонтейнерПрисутствует(Сообщение) Экспорт
	
	Запрос = Новый Запрос("ВЫБРАТЬ РАЗРЕШЕННЫЕ ПЕРВЫЕ 1
						  |	ИСТИНА КАК ЕстьКонтейнер
						  |ИЗ
						  |	РегистрСведений.ТранспортныеКонтейнеры КАК ТранспортныеКонтейнеры
						  |ГДЕ
						  |	ТранспортныеКонтейнеры.ТранспортноеСообщение = &ТранспортноеСообщение");
	Запрос.УстановитьПараметр("ТранспортноеСообщение", Сообщение);
	Возврат НЕ Запрос.Выполнить().Пустой();
	
КонецФункции

Процедура УдалитьТранспортныйКонтейнер(Сообщение) Экспорт
	
	Если НЕ ЗначениеЗаполнено(Сообщение) Тогда
		Возврат;
	КонецЕсли;
	
	НаборЗаписей = РегистрыСведений.ТранспортныеКонтейнеры.СоздатьНаборЗаписей();
	НаборЗаписей.Отбор.ТранспортноеСообщение.Установить(Сообщение);
	НаборЗаписей.Записать();
	
	МассивФайлов = ОбщегоНазначенияЭДКО.ПрикрепленныеФайлыКОбъектуИзСправочника(
		Сообщение,
		"ТранспортноеСообщениеКонтейнерыПрисоединенныеФайлы");
	Для каждого ПрисоединенныйФайл Из МассивФайлов Цикл
		ПрисоединенныйФайлОбъект = ПрисоединенныйФайл.ПолучитьОбъект();
		ПрисоединенныйФайлОбъект.УстановитьПометкуУдаления(Истина);
	КонецЦикла;
	
КонецПроцедуры

Функция ТранспортноеСообщениеОбрабатывается(ТранспортноеСообщение) Экспорт
	
	НаборЗаписей = РегистрыСведений.ИсключенныеИзОбработкиТранспортныеСообщения.СоздатьНаборЗаписей();
	НаборЗаписей.Отбор.ТранспортноеСообщение.Установить(ТранспортноеСообщение);
	НаборЗаписей.Прочитать();
	
	Если НаборЗаписей.Количество() > 0 Тогда
		Возврат Ложь; // Есть ссылка в списке запрета обработки - сообщение НЕ обрабатывается
	КонецЕсли;
	
	Если ТранспортноеСообщение.СертификатыПолучателей.Количество() = 0 Тогда
		Возврат Истина; // Сообщение не имеет зашифрованных элементов - сообщение обрабатывается
	КонецЕсли;
	
	СтороннихПолучателей = 0;
	ВсегоПолучателей = ТранспортноеСообщение.СертификатыПолучателей.Количество();
	
	СерийныеНомераТС = Новый Массив;
	Для Каждого СертификатПолучателя Из ТранспортноеСообщение.СертификатыПолучателей Цикл
		Если СертификатПолучателя.Сторонний Тогда 
			СтороннихПолучателей = СтороннихПолучателей + 1;
			Продолжить;
		КонецЕсли;
		
		ОписаниеСертификата = Новый Структура("СерийныйНомер,ПоставщикCN", СертификатПолучателя.СерийныйНомер, СертификатПолучателя.ПоставщикCN);		
		СерийныеНомераТС.Добавить(ОписаниеСертификата);
	КонецЦикла;
	
	НаборЗаписей = РегистрыСведений.ИсключенныеИзОбработкиСертификаты.СоздатьНаборЗаписей();
	
	Для Каждого ОписаниеСертификата Из СерийныеНомераТС Цикл
				
		НаборЗаписей.Отбор.СерийныйНомер.Установить(ОписаниеСертификата.СерийныйНомер);
		НаборЗаписей.Отбор.ПоставщикCN.Установить(ОписаниеСертификата.ПоставщикCN);
		НаборЗаписей.Прочитать();
		
		Если НаборЗаписей.Количество() > 0 Тогда 
			Если (ВсегоПолучателей = 1) Или (ВсегоПолучателей > 1 И ВсегоПолучателей - СтороннихПолучателей = 1) Тогда 
				// Единственный сертификат абонента или
				// Несколько сертификатов из которых только один сертификат абонента
				Возврат Ложь; // сообщение НЕ обрабатывается
			КонецЕсли;
		КонецЕсли;
		
	КонецЦикла;

	Возврат Истина; // Сообщение обрабатывается
	
КонецФункции

//////////////////////////////////////////////////////////////////////////////////
//// ШИФРОВАНИЕ И РАСШИФРОВКА
////

Функция СообщениеЗашифровано(Сообщение) Экспорт
	
	Запрос = Новый Запрос("ВЫБРАТЬ РАЗРЕШЕННЫЕ ПЕРВЫЕ 1
						  |	ИСТИНА КАК Поле1
						  |ИЗ
						  |	РегистрСведений.ТранспортныеКонтейнеры КАК ТранспортныеКонтейнеры
						  |ГДЕ
						  |	ТранспортныеКонтейнеры.ТранспортноеСообщение = &ТранспортноеСообщение");
	Запрос.УстановитьПараметр("ТранспортноеСообщение", Сообщение);
	Возврат НЕ Запрос.Выполнить().Пустой();
	
КонецФункции

Функция СообщениеРасшифровано(Сообщение) Экспорт
	
	Запрос = Новый Запрос("ВЫБРАТЬ РАЗРЕШЕННЫЕ ПЕРВЫЕ 1
						  |	ИСТИНА КАК Поле1
						  |ИЗ
						  |	РегистрСведений.СодержимоеТранспортныхКонтейнеров КАК СодержимоеТранспортныхКонтейнеров
						  |ГДЕ
						  |	СодержимоеТранспортныхКонтейнеров.ТранспортноеСообщение = &ТранспортноеСообщение");
	Запрос.УстановитьПараметр("ТранспортноеСообщение", Сообщение);
	Возврат НЕ Запрос.Выполнить().Пустой();
	
КонецФункции

Функция РасшифроватьТранспортноеСообщение(Объект)

	// Внимание!
	// ---------
	// В Контейнере есть одноименная процедура, которая используется при отправке отчетов
	// в интерактивном режиме пользователем.
	// При изменениях необходимо синхронно менять обе процедуры.
	
	РеквизитыУчетнойЗаписи = ЭлектроннаяПодписьВМоделиСервисаБРОВызовСервера.РеквизитыУчетнойЗаписи(Объект);
	ЭтоЭлектроннаяПодписьВМоделиСервиса = РеквизитыУчетнойЗаписи.ЭлектроннаяПодписьВМоделиСервиса;
	
	Если НЕ ЭтоЭлектроннаяПодписьВМоделиСервиса Тогда
		// Расшифровка на стороне сервера возможна, только если используется электронная подпись в модели сервиса.
		Возврат Ложь;
	КонецЕсли;
	
	// На случай ошибок при проверке подписей в конейнере
	Если Не ТранспортноеСообщениеОбрабатывается(Объект.Ссылка) Тогда 
		Возврат Ложь;
	КонецЕсли;
	
	ТипСообщения = Неопределено;
	ФорматДокументооборота = Неопределено;
	
	СсылкаНаОбъект = ?(ТипЗнч(Объект) = Тип("ДокументСсылка.ТранспортноеСообщение"), Объект, Объект.Ссылка);
	
	// Получаем Контейнер, распаковываем файлы, разбираем XML
	// возвращает "Булево" или "тип сообщения"
	ДокументыПакета = НовыеДокументыПакета();
	ТаблицаРаспакованныхФайлов = НоваяТаблицаРаспакованныхФайлов();
	Результат = ОбработатьСодержимоеТранспортногоКонтейнера(СсылкаНаОбъект, ДокументыПакета, ТаблицаРаспакованныхФайлов);
			
	Если ТипЗнч(Результат) = Тип("Булево") Тогда 
		Возврат Результат;
	ИначеЕсли ТипЗнч(Результат) = Тип("Структура") Тогда 
		Если НЕ Результат.Свойство("ТипСообщения", ТипСообщения) Тогда 
			Возврат Ложь;
		КонецЕсли;
		Результат.Свойство("ФорматДокументооборота", ФорматДокументооборота);
	КонецЕсли;
	
	Если ДокументыПакета.Количество() = 0 Тогда
		Возврат Ложь;
	КонецЕсли;
	
	ФайлОписанияСведений = ТаблицаРаспакованныхФайлов.НайтиСтроки(Новый Структура("Имя","packageDescription.xml"));
	Если ФайлОписанияСведений.Количество() = 0 Тогда
		Если ТипСообщения = "ПФР" Тогда
			Для каждого РаспакованныйФайл Из ТаблицаРаспакованныхФайлов Цикл
				СтруктураИмениФайла = ОбщегоНазначенияКлиентСервер.РазложитьПолноеИмяФайла(РаспакованныйФайл.Имя);
				ИмяФайлаВРег = СокрЛП(СтруктураИмениФайла.ИмяБезРасширения);
				ИмяФайлаВРег = ВРег(ИмяФайлаВРег);
				РасширениеФайла = СокрЛП(СтруктураИмениФайла.Расширение);
				Если Лев(ИмяФайлаВРег, 4) = "ПФР_" И СтрНайти(ИмяФайлаВРег, "_ОСП_") > 0 И нрег(РасширениеФайла) = ".xml" Тогда
					ФайлОписанияСведений.Добавить(РаспакованныйФайл);
					ФорматДокументооборота = Перечисления.ФорматыДокументооборотаСПФР.ЭДОК;
					Прервать;
				КонецЕсли;
			КонецЦикла;
		КонецЕсли;
		
		Если ФайлОписанияСведений.Количество() = 0 Тогда
			ТекстСообщения = ?(ТипСообщения = "ПФР",
				НСтр("ru = 'Ошибка распаковки пакета: не найден файл описания пакета (ПФР_*_ОСП_*.xml или packageDescription.xml).'"),
				НСтр("ru = 'Ошибка распаковки пакета: не найден файл описания пакета (packageDescription.xml).'"));
			ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения,,,,, НовыйКонтекстРасшифровки(СсылкаНаОбъект));
			Возврат Ложь;
		КонецЕсли;
	КонецЕсли;
	
	// расшифровываем содержимое
	// из свойств транспортного сообщения получим учетную запись, оттуда получим отпечаток сертификата получателя, которым зашифрован пакет
	СертификатПолучателя = ПолучитьОтпечатокСертификатаПолучателя(СсылкаНаОбъект);
	
	КонтекстРасшифровки = Новый Структура;
	КонтекстРасшифровки.Вставить("СертификатПолучателя", СертификатПолучателя);
	КонтекстРасшифровки.Вставить("СсылкаНаОбъект", СсылкаНаОбъект);
	КонтекстРасшифровки.Вставить("ТипСообщения", ТипСообщения);
	КонтекстРасшифровки.Вставить("ФорматДокументооборота", ФорматДокументооборота);
	КонтекстРасшифровки.Вставить("ТекущийДокумент", 0);
	КонтекстРасшифровки.Вставить("ДокументыПакета", ДокументыПакета);
		
	Если НЕ ВыполнитьРасшифровкуСообщенийПоследовательно(КонтекстРасшифровки) Тогда
		Возврат Ложь;
	КонецЕсли;
	
	ЗафиксироватьПопыткуРасшифровки(СсылкаНаОбъект, Истина);
   	ПодтвердитьРасшифровку(СсылкаНаОбъект);
	
	Возврат Истина;
	
КонецФункции

Функция ВыполнитьРасшифровкуСообщенийПоследовательно(ВходящийКонтекст)

	// Внимание!
	// ---------
	// В Контейнере есть одноименная процедура, которая используется при отправке отчетов
	// в интерактивном режиме пользователем.
	// При изменениях необходимо синхронно менять обе процедуры.
	
	ДокументыПакета = ВходящийКонтекст.ДокументыПакета;

	Для каждого ЭлементПакета Из ДокументыПакета Цикл

		Если ВходящийКонтекст.ТипСообщения = "ФНС_534" Тогда 
			АдресФайла = ?(ЗначениеЗаполнено(ЭлементПакета.АдресФайла), ЭлементПакета.АдресФайла, ЭлементПакета.АдресФайлаПодписи);
		Иначе
			АдресФайла = ЭлементПакета.АдресФайла;
		КонецЕсли;
		
		Если ЗначениеЗаполнено(АдресФайла) Тогда
			Если ЭлементПакета.ДокументЗашифрован Тогда
				СообщениеОбОшибке = "";
				РасшифроватьPKCS7(АдресФайла, СообщениеОбОшибке);
				Если ЗначениеЗаполнено(СообщениеОбОшибке) Тогда
					Возврат Ложь;
				КонецЕсли;
			КонецЕсли;
		КонецЕсли;
		
		ВыполнитьРасшифровкуСообщенийПоследовательноПослеРасшифровки(Новый Структура("Выполнено", Истина), ВходящийКонтекст);
		
		ВходящийКонтекст.ТекущийДокумент = ВходящийКонтекст.ТекущийДокумент + 1;
	КонецЦикла;
	
	Если ВходящийКонтекст.ТипСообщения = "ПФР" И (НЕ ВходящийКонтекст.Свойство("ФорматДокументооборота")
		ИЛИ ВходящийКонтекст.ФорматДокументооборота <>
		ПредопределенноеЗначение("Перечисление.ФорматыДокументооборотаСПФР.ЭДОК")) Тогда
		// разбираем дополнительные описания с целью восстановить исходные имена файлов
		ВосстановитьИсходныеИменаФайлов(ВходящийКонтекст.СсылкаНаОбъект, ДокументыПакета);
	КонецЕсли;
	
	// помещаем файлы в регистр содержимого, повторно распознаем получателя после расшифровки содержимого
	Если НЕ ПоместитьФайлыВРегистрСодержимого(ВходящийКонтекст.СсылкаНаОбъект, ДокументыПакета) Тогда
		Возврат Ложь;
	КонецЕсли;
	
	// особая обработка в случае неформализованных документов налогового органа, документов реализации своих полномочий
	// уведомлений об уточнении и протоколов приема 2-НДФЛ
	Если НЕ ОбработатьНеформализованныеДокументы(ВходящийКонтекст.СсылкаНаОбъект) Тогда
		Возврат Ложь;
	КонецЕсли;
	
	Возврат СоздатьПодтверждениеПриНеобходимости(ВходящийКонтекст.СсылкаНаОбъект);
	
КонецФункции

Процедура ВыполнитьРасшифровкуСообщенийПоследовательноПослеРасшифровки(Результат, ВходящийКонтекст)

	// Внимание!
	// ---------
	// В Контейнере есть одноименная процедура, которая используется при отправке отчетов
	// в интерактивном режиме пользователем.
	// При изменениях необходимо синхронно менять обе процедуры.
	
	ДокументыПакета = ВходящийКонтекст.ДокументыПакета;

	Если Результат.Выполнено Тогда
		ЭлементПакета = ДокументыПакета[ВходящийКонтекст.ТекущийДокумент];
		Если ВходящийКонтекст.ТипСообщения = "ФНС_534" Тогда 
			АдресФайла = ?(ЗначениеЗаполнено(ЭлементПакета.АдресФайла), ЭлементПакета.АдресФайла, ЭлементПакета.АдресФайлаПодписи);
		Иначе
			АдресФайла = ЭлементПакета.АдресФайла;
		КонецЕсли;
		Если ЭлементПакета.ДокументСжат Тогда		
			Если Не ЗначениеЗаполнено(АдресФайла) Тогда
				Возврат;
			Иначе
				ЭтоФайлПодписи = ВходящийКонтекст.ТипСообщения = "ФНС_534" И НЕ ЗначениеЗаполнено(ЭлементПакета.АдресФайла);
				ТипАрхива = ?(ВходящийКонтекст.ТипСообщения = "ПФР",
					?(ЗначениеЗаполнено(ВходящийКонтекст.ФорматДокументооборота),
					?(ВходящийКонтекст.ФорматДокументооборота = Перечисления.ФорматыДокументооборотаСПФР.ЭДОК, "gzip", "zip"),
					Неопределено), "zip");
				АдресФайла = РазархивироватьФайлИзВременногоХранилища(АдресФайла, НЕ ЭтоФайлПодписи, ТипАрхива);
				Если АдресФайла <> Неопределено ИЛИ НЕ ЭтоФайлПодписи Тогда
					ЭлементПакета.АдресФайла = АдресФайла;
					Если ВходящийКонтекст.ТипСообщения = "ФНС_534" Тогда
						Если ЗначениеЗаполнено(ЭлементПакета.АдресФайла) Тогда
							ЭлементПакета.АдресФайла = АдресФайла;
						Иначе
							ЭлементПакета.АдресФайлаПодписи = АдресФайла;
						КонецЕсли;
					Иначе
						ЭлементПакета.АдресФайла = АдресФайла;
					КонецЕсли;
				КонецЕсли;
			КонецЕсли;
		КонецЕсли;
		
		Если ВходящийКонтекст.ТипСообщения = "ФСГС" Тогда 
			Если ЭлементПакета.ТипДокумента = ПредопределенноеЗначение("Перечисление.ТипыСодержимогоТранспортногоКонтейнера.ШаблонФСГС") Тогда
				Попытка
					ПараметрыШаблона = Новый Структура("АдресФайла, ИмяФайла", ЭлементПакета.АдресФайла, ЭлементПакета.ИмяФайла);
					ЭлектронныйДокументооборотСКонтролирующимиОрганамиКлиентСервер.ПриПолученииНовогоШаблонаФСГС(ПараметрыШаблона);
				Исключение
				КонецПопытки;
			КонецЕсли;
		КонецЕсли;
		
		Если ЗначениеЗаполнено(ЭлементПакета.АдресФайлаПодписи) Тогда
			Если ЗначениеЗаполнено(ЭлементПакета.АдресФайла) Тогда
				Попытка
					Результат = КриптографияЭДКОКлиентСервер.ПроверитьПодписьPKCS7ВМоделиСервиса(
						ЭлементПакета.АдресФайлаПодписи, ЭлементПакета.АдресФайла);
				Исключение
					Результат = КриптографияЭДКОКлиентСервер.ПодготовитьРезультат(
						Ложь,
						"ОписаниеОшибки",
						КриптографияЭДКОКлиентСервер.ОписаниеОшибкиНеУдалосьПроверитьПодпись());
				КонецПопытки;
			Иначе
				Результат = Новый Структура("Выполнено, ПодписьВалидна, Подписанты", Истина, Неопределено, Неопределено);
			КонецЕсли;
			
			ВыполнитьРасшифровкуСообщенийПоследовательноПослеПроверкиПодписи(Результат, ВходящийКонтекст);
			
		КонецЕсли;
	Иначе
		ТекстСообщения = НСтр("ru = 'Ошибка расшифровки файла документа при распаковке пакета.'");
		ЗаписатьСобытиеВЖурнал(УровеньЖурналаРегистрации.Ошибка, ТекстСообщения);
	КонецЕсли;
	
КонецПроцедуры

Процедура ВыполнитьРасшифровкуСообщенийПоследовательноПослеПроверкиПодписи(Результат, ВходящийКонтекст)

	// Внимание!
	// ---------
	// В Контейнере есть одноименная процедура, которая используется при отправке отчетов
	// в интерактивном режиме пользователем.
	// При изменениях необходимо синхронно менять обе процедуры.

	ДокументыПакета = ВходящийКонтекст.ДокументыПакета;
	
	Если НЕ Результат.Выполнено ИЛИ Результат.ПодписьВалидна = Ложь Тогда
		ИмяСобытия = НСтр("ru = 'Электронный документооборот с контролирующими органами. Проверка подписи'");
		ТекстСообщения = НСтр("ru = 'Ошибка проверки подписи файла документа при распаковке пакета ""%1"" (тип сообщения: ""%2"", "
			+ "тип документа: ""%3"", имя файла: ""%4"", идентификатор: ""%5"", в модели сервиса: %6). %7'");
		
		Если Результат.Свойство("ОписаниеОшибки") Тогда
			ТекстОшибки = Результат.ОписаниеОшибки;
		Иначе
			ТекстОшибки = НСтр("ru = 'Подпись невалидна.'");
		КонецЕсли;
		
		Если Результат.Свойство("Подписанты") И Результат.Подписанты <> Неопределено И Результат.Подписанты.Количество() > 0 Тогда
			ТекстИнформацииОСертификате = НСтр("ru = 'Проверка сертификатом поставщика ""%1"" с серийным номером ""%2"".'");
			ТекстОшибки = ТекстОшибки + " " + СтрШаблон(
				ТекстИнформацииОСертификате,
				Результат.Подписанты[0].Поставщик,
				Результат.Подписанты[0].СерийныйНомер);
		КонецЕсли;
		
		ТекстСообщения = СтрШаблон(
			ТекстСообщения,
			Строка(ВходящийКонтекст.СсылкаНаОбъект),
			ВходящийКонтекст.ТипСообщения,
			Строка(ДокументыПакета[ВходящийКонтекст.ТекущийДокумент].ТипДокумента),
			ДокументыПакета[ВходящийКонтекст.ТекущийДокумент].ИмяФайла,
			ДокументыПакета[ВходящийКонтекст.ТекущийДокумент].Идентификатор,
			Строка(ВходящийКонтекст.СертификатПолучателя.ЭтоЭлектроннаяПодписьВМоделиСервиса),
			ТекстОшибки);
		
		ЗаписатьСобытиеВЖурнал(УровеньЖурналаРегистрации.Предупреждение, ТекстСообщения);
	КонецЕсли;
	
	ЭлементПакета = ДокументыПакета[ВходящийКонтекст.ТекущийДокумент];
	ЭлементПакета.СтатусПроверкиПодписи = ?(Результат.Выполнено И Результат.ПодписьВалидна = Неопределено,
		ПредопределенноеЗначение("Перечисление.СтатусыПроверкиЭЦП.ПустаяСсылка"),
		?(Результат.Выполнено И Результат.ПодписьВалидна,
		ПредопределенноеЗначение("Перечисление.СтатусыПроверкиЭЦП.Валидна"),
		ПредопределенноеЗначение("Перечисление.СтатусыПроверкиЭЦП.НеВалидна")));
	Если Результат.Свойство("Подписанты") И Результат.Подписанты <> Неопределено
		И Результат.Подписанты.Количество() > 0 И Результат.Подписанты[0].Свойство("Отпечаток") Тогда
		ЭлементПакета.ОтпечатокСертификатаПодписи = Результат.Подписанты[0].Отпечаток;
	Иначе
		ЭлементПакета.ОтпечатокСертификатаПодписи = "";
	КонецЕсли;
	
КонецПроцедуры

Процедура ПодтвердитьРасшифровку(Сообщение) Экспорт
	
	НаборЗаписей = РегистрыСведений.СвойстваТранспортныхСообщений.СоздатьНаборЗаписей();
	НаборЗаписей.Отбор.УчетнаяЗапись.Установить(Сообщение.УчетнаяЗапись);
	НаборЗаписей.Отбор.ТранспортноеСообщение.Установить(Сообщение);
	
	НаборЗаписей.Прочитать();
	Изменений = 0;
	
	Для Каждого Запись Из НаборЗаписей Цикл 
		
		// Изменение данных полей записи.
		Запись.ТребуетРасшифровки = Ложь;
		Изменений = Изменений + 1;
		
	КонецЦикла;
	
	Если Изменений = 0 Тогда 
		Возврат;
	КонецЕсли;
	
	НаборЗаписей.Записать();
	
КонецПроцедуры

Процедура ВосстановитьИсходныеИменаФайлов(Объект, ДокументыПакета) Экспорт

	ОбъектТип = Объект.Тип;
	
	// разбираем дополнительные описания с целью восстановить исходные имена файлов
	Если ОбъектТип = Перечисления.ТипыТранспортныхСообщений.ПротоколПФР Тогда
		
		// ищем протокол
		СтруктураОтбора = Новый Структура;
		СтруктураОтбора.Вставить("ТипДокумента", Перечисления.ТипыСодержимогоТранспортногоКонтейнера.ПротоколПФР);
		СтрПротоколПФР = ДокументыПакета.НайтиСтроки(СтруктураОтбора);
		Если СтрПротоколПФР <> Неопределено И СтрПротоколПФР.Количество() > 0 Тогда
			
			// загружаем протокол в дерево
			ТекстXML = Неопределено;
			ДеревоПротокола = ЗагрузитьXMLВДеревоЗначений(СтрПротоколПФР[0].АдресФайла, ТекстXML);
			Если ДеревоПротокола <> Неопределено Тогда
				
				// ищем узлы "приложение"
				УзлыПриложение = ДеревоПротокола.Строки.НайтиСтроки(Новый Структура("Имя, Тип", "приложение", "Э"), Истина);
				Для Каждого УзелПриложение Из УзлыПриложение Цикл
					
					// ищем подчиненный элемент "идентификаторДокумента"
					УзелИдентификаторДокумента = УзелПриложение.Строки.Найти("идентификаторДокумента", "Имя", Истина);
					
					// ищем подчиненный элемент "имяФайла"
					УзелИмяФайла = УзелПриложение.Строки.Найти("имяФайла", "Имя", Истина);
					
					Если УзелИдентификаторДокумента <> Неопределено И УзелИмяФайла <> Неопределено
						И ЗначениеЗаполнено(УзелИдентификаторДокумента.Значение) И ЗначениеЗаполнено(УзелИмяФайла.Значение) Тогда
						
						ИдентификаторДокумента = СокрЛП(УзелИдентификаторДокумента.Значение);
						ИмяФайлаПриложения = СокрЛП(УзелИмяФайла.Значение);
						
						СтруктураОтбора = Новый Структура;
						СтруктураОтбора.Вставить("идентификатор", ИдентификаторДокумента);
						СтрПриложение = ДокументыПакета.НайтиСтроки(СтруктураОтбора);
						Если СтрПриложение <> Неопределено И СтрПриложение.Количество() > 0 Тогда
							СтрПриложение[0].ИмяФайла = ИмяФайлаПриложения;
						КонецЕсли;
						
					КонецЕсли;
					
				КонецЦикла;
				
			КонецЕсли;
			
		КонецЕсли;
		
		// разбираем дополнительные описания с целью восстановить исходные имена файлов
	ИначеЕсли ОбъектТип = Перечисления.ТипыТранспортныхСообщений.ПодтверждениеПолученияОтчетностиПФР Тогда
		
		// ищем протокол
		СтруктураОтбора = Новый Структура;
		СтруктураОтбора.Вставить("ТипДокумента", Перечисления.ТипыСодержимогоТранспортногоКонтейнера.ПодтверждениеПолученияОтчетностиПФР);
		СтрПротоколПФР = ДокументыПакета.НайтиСтроки(СтруктураОтбора);
		Если СтрПротоколПФР <> Неопределено И СтрПротоколПФР.Количество() > 0 Тогда
			
			// загружаем протокол в дерево
			ТекстXML = Неопределено;
			ДеревоПротокола = ЗагрузитьXMLВДеревоЗначений(СтрПротоколПФР[0].АдресФайла, ТекстXML);
			Если ДеревоПротокола <> Неопределено Тогда
				
				// ищем узлы "приложение"
				УзлыПриложение = ДеревоПротокола.Строки.НайтиСтроки(Новый Структура("Имя, Тип", "приложение", "Э"), Истина);
				Для Каждого УзелПриложение Из УзлыПриложение Цикл
					
					// ищем подчиненный элемент "идентификаторДокумента"
					УзелИдентификаторДокумента = УзелПриложение.Строки.Найти("идентификаторДокумента", "Имя", Истина);
					
					// ищем подчиненный элемент "имяФайла"
					УзелИмяФайла = УзелПриложение.Строки.Найти("имяФайла", "Имя", Истина);
					
					Если УзелИдентификаторДокумента <> Неопределено И УзелИмяФайла <> Неопределено
						И ЗначениеЗаполнено(УзелИдентификаторДокумента.Значение) И ЗначениеЗаполнено(УзелИмяФайла.Значение) Тогда
						
						ИдентификаторДокумента = СокрЛП(УзелИдентификаторДокумента.Значение);
						ИмяФайлаПриложения = СокрЛП(УзелИмяФайла.Значение);
						
						СтруктураОтбора = Новый Структура;
						СтруктураОтбора.Вставить("идентификатор", ИдентификаторДокумента);
						СтрПриложение = ДокументыПакета.НайтиСтроки(СтруктураОтбора);
						Если СтрПриложение <> Неопределено И СтрПриложение.Количество() > 0 Тогда
							СтрПриложение[0].ИмяФайла = ИмяФайлаПриложения;
						КонецЕсли;
						
					КонецЕсли;
					
				КонецЦикла;
				
			КонецЕсли;
			
		КонецЕсли;
		
	ИначеЕсли ОбъектТип = Перечисления.ТипыТранспортныхСообщений.ОтветНаЗапросПФР Тогда
		
		// ищем протокол
		СтруктураОтбора = Новый Структура;
		СтруктураОтбора.Вставить("ТипДокумента", Перечисления.ТипыСодержимогоТранспортногоКонтейнера.ОтветНаЗапросПФР);
		СтрПротоколПФР = ДокументыПакета.НайтиСтроки(СтруктураОтбора);
		Если СтрПротоколПФР <> Неопределено И СтрПротоколПФР.Количество() > 0 Тогда
			
			// загружаем протокол в дерево
			ТекстXML = Неопределено;
			ДеревоПротокола = ЗагрузитьXMLВДеревоЗначений(СтрПротоколПФР[0].АдресФайла, ТекстXML);
			Если ДеревоПротокола <> Неопределено Тогда
				
				// ищем узлы "приложение"
				УзлыПриложение = ДеревоПротокола.Строки.НайтиСтроки(Новый Структура("Имя, Тип", "приложение", "Э"), Истина);
				Для Каждого УзелПриложение Из УзлыПриложение Цикл
					
					// ищем подчиненный элемент "идентификаторДокумента"
					УзелИдентификаторДокумента = УзелПриложение.Строки.Найти("идентификаторДокумента", "Имя", Истина);
					
					// ищем подчиненный элемент "имяФайла"
					УзелИмяФайла = УзелПриложение.Строки.Найти("имяФайла", "Имя", Истина);
					
					Если УзелИдентификаторДокумента <> Неопределено И УзелИмяФайла <> Неопределено
					И ЗначениеЗаполнено(УзелИдентификаторДокумента.Значение) И ЗначениеЗаполнено(УзелИмяФайла.Значение) Тогда
						
						ИдентификаторДокумента = СокрЛП(УзелИдентификаторДокумента.Значение);
						ИмяФайлаПриложения = СокрЛП(УзелИмяФайла.Значение);
						
						СтруктураОтбора = Новый Структура;
						СтруктураОтбора.Вставить("идентификатор", ИдентификаторДокумента);
						СтрПриложение = ДокументыПакета.НайтиСтроки(СтруктураОтбора);
						Если СтрПриложение <> Неопределено И СтрПриложение.Количество() > 0 Тогда
							СтрПриложение[0].ИмяФайла = ИмяФайлаПриложения;
						КонецЕсли;
						
					КонецЕсли;
					
				КонецЦикла;
				
			КонецЕсли;
			
		КонецЕсли;
		
	ИначеЕсли ОбъектТип = Перечисления.ТипыТранспортныхСообщений.ПисьмоВходящееПФР Тогда
		
		// ищем протокол
		СтруктураОтбора = Новый Структура;
		СтруктураОтбора.Вставить("ТипДокумента", Перечисления.ТипыСодержимогоТранспортногоКонтейнера.ОписаниеПисьма);
		СтрОписаниеПисьма = ДокументыПакета.НайтиСтроки(СтруктураОтбора);
		Если СтрОписаниеПисьма <> Неопределено И СтрОписаниеПисьма.Количество() > 0 Тогда
			
			// загружаем протокол в дерево
			ТекстXML = Неопределено;
			ДеревоОписания = ЗагрузитьXMLВДеревоЗначений(СтрОписаниеПисьма[0].АдресФайла, ТекстXML);
			Если ДеревоОписания <> Неопределено Тогда
				
				// ищем узлы "приложение"
				УзлыПриложение = ДеревоОписания.Строки.НайтиСтроки(Новый Структура("Имя, Тип", "приложение", "Э"), Истина);
				Для Каждого УзелПриложение Из УзлыПриложение Цикл
					
					// ищем подчиненный элемент "идентификаторДокумента"
					УзелИдентификаторДокумента = УзелПриложение.Строки.Найти("идентификаторДокумента", "Имя", Истина);
					
					// ищем подчиненный элемент "имяФайла"
					УзелИмяФайла = УзелПриложение.Строки.Найти("имяФайла", "Имя", Истина);
					
					Если УзелИдентификаторДокумента <> Неопределено И УзелИмяФайла <> Неопределено
						И ЗначениеЗаполнено(УзелИдентификаторДокумента.Значение) И ЗначениеЗаполнено(УзелИмяФайла.Значение) Тогда
						
						ИдентификаторДокумента = СокрЛП(УзелИдентификаторДокумента.Значение);
						ИмяФайлаПриложения = СокрЛП(УзелИмяФайла.Значение);
						
						СтруктураОтбора = Новый Структура;
						СтруктураОтбора.Вставить("идентификатор", ИдентификаторДокумента);
						СтрПриложение = ДокументыПакета.НайтиСтроки(СтруктураОтбора);
						Если СтрПриложение <> Неопределено И СтрПриложение.Количество() > 0 Тогда
							СтрПриложение[0].ИмяФайла = ИмяФайлаПриложения;
						КонецЕсли;
						
					КонецЕсли;
					
				КонецЦикла;
				
			КонецЕсли;
			
		КонецЕсли;
		
	КонецЕсли;

КонецПроцедуры

Функция ПоместитьФайлыВРегистрСодержимого(ОбъектСсылка, ДокументыПакета) Экспорт
	
	 // помещаем файлы в регистр содержимого
	Для Каждого СтрДокументПакета Из ДокументыПакета Цикл
		
		Если ЗначениеЗаполнено(СтрДокументПакета.АдресФайла) Тогда
			ДобавитьСодержимоеТранспортногоКонтейнера(ОбъектСсылка,
														СтрДокументПакета.ТипДокумента,
														СтрДокументПакета.АдресФайла,
														СтрДокументПакета.ИмяФайла,
														,
														,
														,
														,
														,
														,
														СтрДокументПакета.Идентификатор,
														СтрДокументПакета.ТипСодержимого);
		КонецЕсли;
		
		Если ЗначениеЗаполнено(СтрДокументПакета.ИмяФайлаПодписи) Тогда
			ДобавитьСодержимоеТранспортногоКонтейнера(ОбъектСсылка,
														Перечисления.ТипыСодержимогоТранспортногоКонтейнера.ЭлектронноЦифроваяПодпись,
														СтрДокументПакета.АдресФайлаПодписи,
														СтрДокументПакета.ИмяФайлаПодписи,
														,
														СтрДокументПакета.ИмяФайла,
														СтрДокументПакета.ОтпечатокСертификатаПодписи,
														СтрДокументПакета.СтатусПроверкиПодписи,
														Ложь,
														,
														,
														СтрДокументПакета.ТипСодержимого);
		КонецЕсли;
		
	КонецЦикла;
	
	// повторно распознать получателя после расшифровки содержимого
	Возврат ИзменитьПолучателяВходящегоТранспортногоСообщенияПослеРасшифровки(ОбъектСсылка);
	
КонецФункции

Функция ИзменитьПолучателяВходящегоТранспортногоСообщенияПослеРасшифровки(ОбъектСсылка)
	
	Если ОбъектСсылка.Статус = Перечисления.СтатусыПисем.Полученное
		И ОбъектСсылка.Тип = Перечисления.ТипыТранспортныхСообщений.ДокументНО Тогда
		
		ПолучательТранспортногоСообщения = ОпределитьПолучателяВходящегоТранспортногоСообщения(ОбъектСсылка, Истина);
		Если ПолучательТранспортногоСообщения <> ОбъектСсылка.Получатель Тогда
			НачатьТранзакцию();
			
			Если ЗначениеЗаполнено(ОбъектСсылка.ЦиклОбмена) Тогда
				ОбъектЦиклОбмена = ОбъектСсылка.ЦиклОбмена.ПолучитьОбъект();
				ОбъектЦиклОбмена.Организация = ПолучательТранспортногоСообщения;
				Попытка
					ОбъектЦиклОбмена.Записать();
				Исключение
					ОтменитьТранзакцию();
					ТекстСообщения = НСтр("ru = 'Не удалось обновить свойства родительского цикла обмена ""%1""
												|%2'");
					ОписаниеОшибки = ИнформацияОбОшибке().Описание;
					ТекстСообщения = СтрШаблон(ТекстСообщения, ОбъектСсылка.ЦиклОбмена, ОписаниеОшибки);
					ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
					Возврат Ложь;
				КонецПопытки;
			КонецЕсли;
			
			ОбъектСообщение = ОбъектСсылка.ПолучитьОбъект();
			ОбъектСообщение.Получатель = ПолучательТранспортногоСообщения;
			Попытка
				ОбъектСообщение.Записать();
			Исключение
				ОтменитьТранзакцию();
				ТекстСообщения = НСтр("ru = 'Ошибка при записи расшифрованного сообщения %1: %2'");
				ПредставлениеСообщения = ПредставлениеСообщения(ОбъектСсылка.Ссылка);
				ОписаниеОшибки = ИнформацияОбОшибке().Описание;
				ТекстСообщения = СтрШаблон(ТекстСообщения, ПредставлениеСообщения, ОписаниеОшибки);
				ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
				Возврат Ложь;
			КонецПопытки;
			
			ЗафиксироватьТранзакцию();
		КонецЕсли;
	КонецЕсли;
	
	Возврат Истина;
	
КонецФункции

Функция ОбработатьНеформализованныеДокументы(ОбъектСсылка) Экспорт

	ОбъектСообщение = ОбъектСсылка.ПолучитьОбъект();
	
	Если ОбъектСсылка.Тип = Перечисления.ТипыТранспортныхСообщений.ПисьмоВходящееПФР
		ИЛИ ОбъектСсылка.Тип = Перечисления.ТипыТранспортныхСообщений.ПисьмоНО
		ИЛИ ОбъектСсылка.Тип = Перечисления.ТипыТранспортныхСообщений.РассылкаНО 
		ИЛИ ОбъектСсылка.Тип = Перечисления.ТипыТранспортныхСообщений.РассылкаНО 
		ИЛИ ОбъектСсылка.Тип = Перечисления.ТипыТранспортныхСообщений.ДокументНО
		ИЛИ ОбъектСсылка.Тип = Перечисления.ТипыТранспортныхСообщений.ПисьмоВходящееФСГС
		ИЛИ ОбъектСсылка.Тип = Перечисления.ТипыТранспортныхСообщений.РассылкаФСГС
		ИЛИ ОбъектСсылка.Тип = Перечисления.ТипыТранспортныхСообщений.РассылкаШаблоновФСГС Тогда
		
		СоздатьПредметЦиклаОбмена(ОбъектСсылка);
		
	ИначеЕсли ОбъектСсылка.Тип = Перечисления.ТипыТранспортныхСообщений.ПодтверждениеПолученияОтчетностиПФР
		ИЛИ ОбъектСсылка.Тип = Перечисления.ТипыТранспортныхСообщений.ОтветНаЗапросПФР
		ИЛИ ОбъектСсылка.Тип = Перечисления.ТипыТранспортныхСообщений.ПротоколПФР
		ИЛИ ОбъектСсылка.Тип = Перечисления.ТипыТранспортныхСообщений.ИзвещениеОПолученииОтчетностиФСГС
		ИЛИ ОбъектСсылка.Тип = Перечисления.ТипыТранспортныхСообщений.ПротоколВходногоКонтроляОтчетностиФСГС
		ИЛИ ОбъектСсылка.Тип = Перечисления.ТипыТранспортныхСообщений.РезультатПриемаДекларацияНО
		ИЛИ ОбъектСсылка.Тип = Перечисления.ТипыТранспортныхСообщений.РезультатПриемаОбращениеНО
		ИЛИ ОбъектСсылка.Тип = Перечисления.ТипыТранспортныхСообщений.РезультатПриемаПредставлениеНО
		ИЛИ ОбъектСсылка.Тип = Перечисления.ТипыТранспортныхСообщений.РезультатОбработкиДекларацияНО
		ИЛИ ОбъектСсылка.Тип = Перечисления.ТипыТранспортныхСообщений.РезультатПриемаФорма2НДФЛНО 
		ИЛИ ОбъектСсылка.Тип = Перечисления.ТипыТранспортныхСообщений.РезультатПриемаЗаявлениеНО
		ИЛИ ОбъектСсылка.Тип = Перечисления.ТипыТранспортныхСообщений.РезультатОбработкиЗаявлениеРФНО Тогда
		
		ЯвляетсяОтрицательным = ЯвляетсяОтрицательнымПротоколом(ОбъектСсылка);
		Если ЯвляетсяОтрицательным Тогда
			// Чтобы не взводилась модифицированность в случае, если не является отрицательным.
			ОбъектСообщение.ПротоколСОшибкой = Истина;
		КонецЕсли;
		
	КонецЕсли;

	Если ОбъектСообщение.Модифицированность() Тогда
		Попытка
			ОбъектСообщение.Записать();
		Исключение
			
			ТекстСообщения = СтроковыеФункцииКлиентСервер.ПодставитьПараметрыВСтроку(
				НСтр("ru = 'Ошибка при записи расшифрованного сообщения %1: %2'"),
				ПредставлениеСообщения(ОбъектСсылка.Ссылка), 
				Символы.ПС + ИнформацияОбОшибке().Описание);
				
			ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
			
			Возврат Ложь;
			
		КонецПопытки;
	КонецЕсли;

	ОбновитьСтатусыСвязанногоОбъектаПриНеобходимости(ОбъектСсылка.Ссылка);

	// сохраняем ответ на запрос
	Если ОбъектСсылка.Тип = Перечисления.ТипыТранспортныхСообщений.РезультатОбработкиЗапросНО Тогда
		ОбновитьОтветНаЗапросИОНВРегистреОтветовПриНеобходимости(ОбъектСсылка);
	КонецЕсли;
	
	// сохраняем ответ на запрос
	Если ОбъектСсылка.Тип = Перечисления.ТипыТранспортныхСообщений.ОтветНаЗапросПФР Тогда
		ОбновитьОтветНаЗапросИОСВРегистреОтветовПриНеобходимости(ОбъектСсылка);
	КонецЕсли;
	
	// сохраняем ответ на запрос выписки из ЕГРЮЛ/ЕГРИП
	Если ОбъектСсылка.Тип = Перечисления.ТипыТранспортныхСообщений.РезультатОбработкиЗапросаНаВыпискуЕРГЮЛ_ЕГРИП Тогда
		ОбновитьОтветНаЗапросВыпискиЕГРЮЛ_ЕГРИП_ПриНеобходимости(ОбъектСсылка);
	КонецЕсли;

	Возврат Истина;
	
КонецФункции

Функция УчетныеЗаписиПоОрганизациям(Организации) Экспорт
	
	СоответствиеУчетныхЗаписейОрганизациям = Новый Соответствие;
	УчетныеЗаписи = Новый Массив;
	
	Запрос = Новый Запрос("ВЫБРАТЬ РАЗРЕШЕННЫЕ
						  |	Организации.Ссылка КАК Организация,
						  |	ВЫБОР
						  |		КОГДА Организации.ВидОбменаСКонтролирующимиОрганами = &ОбменВУниверсальномВФормате
						  |			ТОГДА Организации.УчетнаяЗаписьОбмена
						  |		ИНАЧЕ НЕОПРЕДЕЛЕНО
						  |	КОНЕЦ КАК УчетнаяЗапись
						  |ИЗ
						  |	Справочник.Организации КАК Организации
						  |ГДЕ
						  |	Организации.Ссылка В(&Ссылка)");
	Запрос.УстановитьПараметр("ОбменВУниверсальномВФормате", Перечисления.ВидыОбменаСКонтролирующимиОрганами.ОбменВУниверсальномФормате);
	Запрос.УстановитьПараметр("Ссылка", Организации);
	Выборка = Запрос.Выполнить().Выбрать();
	Пока Выборка.Следующий() Цикл
		СоответствиеУчетныхЗаписейОрганизациям.Вставить(Выборка.Организация, Выборка.УчетнаяЗапись);
	КонецЦикла;
	
	Для Инд = 1 По Организации.Количество() Цикл
		
		ТекОрганизация = Организации.Получить(Инд - 1);
		ТекУчетнаяЗапись = СоответствиеУчетныхЗаписейОрганизациям[ТекОрганизация];
		
		Если ЗначениеЗаполнено(ТекУчетнаяЗапись) И УчетныеЗаписи.Найти(ТекУчетнаяЗапись) = Неопределено Тогда
			УчетныеЗаписи.Добавить(ТекУчетнаяЗапись);
		КонецЕсли;
		
	КонецЦикла;
	
	Возврат УчетныеЗаписи;
	
КонецФункции

Функция ПолучитьВложения(СсылкаПисьмо, ИменаВложений = Неопределено) Экспорт
	
	Возврат ПолучитьВложенияНеформализованногоДокумента(СсылкаПисьмо, ИменаВложений, Истина);
	
КонецФункции

Функция ВыгрузитьТекстВФайл(Текст, ИмяФайла = Неопределено, КодировкаТекста = "windows-1251")
	
	Если ЗначениеЗаполнено(ИмяФайла) Тогда
		ИмяФайлаСохранения = ИмяФайла;
	Иначе
		ИмяФайлаСохранения = ПолучитьИмяВременногоФайла();
	КонецЕсли;
	
	ОбъектЗапись = Новый ЗаписьТекста(ИмяФайлаСохранения, КодировкаТекста);
	ОбъектЗапись.Записать(Текст);
	Попытка
		ОбъектЗапись.Закрыть();
	Исключение
		Возврат Неопределено;
	КонецПопытки;
	
	Возврат ИмяФайлаСохранения;
	
КонецФункции

Функция СформироватьИОП(ТранспортноеСообщение, ТекстВыгрузки = Неопределено, ИмяФайла = Неопределено) Экспорт
	
	ОрганизацияОтправитель = ТранспортноеСообщение.Отправитель;
	
	// в случае обмена через СОС налоговым органом-получателем считается конечный налоговый орган (указанный в титульном листе отчета),
	// а в случае обмена напрямую, НО-получателем считается налоговый орган, указанный в учетной записи
	НОПолучатель = ?(НЕ ТранспортноеСообщение.УчетнаяЗапись.ОбменНапрямую
					И (НЕ ТранспортноеСообщение.Отправитель.КрупнейшийНалогоплательщик ИЛИ НЕ ЗначениеЗаполнено(ТранспортноеСообщение.Отправитель.КодНалоговогоОрганаПолучателя)),
					ТранспортноеСообщение.ЦиклОбмена.ВнешняяОрганизация,
					ТранспортноеСообщение.УчетнаяЗапись.НалоговыйОрган);
	
	ИНН = СокрЛП(ОрганизацияОтправитель.ИНН);
	Если ПустаяСтрока(ИНН) Тогда
		ТекстСообщения = СтроковыеФункцииКлиентСервер.ПодставитьПараметрыВСтроку(НСтр("ru='Не задан ИНН для отправителя - организации %1!'"),
																					Символ(34) + ОрганизацияОтправитель + Символ(34));
		ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
		Возврат Неопределено;
	КонецЕсли;
	
	Текст = Новый ТекстовыйДокумент;
	Текст.ДобавитьСтроку("ИдОтпр:" + СокрЛП(ИНН));
	
	Текст.ДобавитьСтроку("ВерсПрог:" + РегламентированнаяОтчетность.НазваниеИВерсияПрограммы());
	
	ИмяВремФайла = ПолучитьИмяВременногоФайла();
	Текст.Записать(ИмяВремФайла, "windows-1251");
	
	Возврат ИмяВремФайла;
	
КонецФункции

Функция ИзвлечьПоказатель(ТекстВыгрузки, КодПоказателя) Экспорт
	
	Если Лев(СокрЛ(ТекстВыгрузки), 1) = "<" Тогда // это XML
		ОбъектЧтениеXML = Новый ЧтениеXML;
		ОбъектЧтениеXML.УстановитьСтроку(ТекстВыгрузки);
		ОбъектЧтениеXML.ИгнорироватьПробелы = Ложь;
		Пока ОбъектЧтениеXML.Прочитать() Цикл
			Если ОбъектЧтениеXML.ТипУзла = ТипУзлаXML.НачалоЭлемента Тогда
				Пока ОбъектЧтениеXML.ПрочитатьАтрибут() Цикл
					Если ОбъектЧтениеXML.Имя = КодПоказателя Тогда
						Возврат ОбъектЧтениеXML.Значение;
					КонецЕсли;
				КонецЦикла;
			КонецЕсли;
		КонецЦикла;	
	Иначе
		Для Инд = 1 По СтрЧислоСтрок(ТекстВыгрузки) Цикл
			ТекСтр = СтрПолучитьСтроку(ТекстВыгрузки, Инд);
			Если ВРЕГ(Лев(ТекСтр, СтрДлина(КодПоказателя) + 1)) = ВРЕГ(КодПоказателя + ":") Тогда
				Возврат Сред(ТекСтр, СтрДлина(КодПоказателя) + 2);
			КонецЕсли;
		КонецЦикла;
	КонецЕсли;
	Возврат Неопределено;
	
КонецФункции

Функция ВыборВТабличномПолеЦикловОбмена(ВыбраннаяСтрока, Колонка, СтандартнаяОбработка) Экспорт
	
	СоответствиеИменКолонок = Новый Соответствие();
	// ТаблицаЦиклыОбменаОтчетностьФНС 
	СоответствиеИменКолонок.Вставить("ПервичноеСообщение", "ПервичноеСообщение");
	СоответствиеИменКолонок.Вставить("ПодтверждениеОтправки", "ПодтверждениеОтправки");
	СоответствиеИменКолонок.Вставить("ОтветНаПодтверждениеОтправки", "ОтветНаПодтверждениеОтправки");
	СоответствиеИменКолонок.Вставить("ПодтверждениеДоставки", "ПодтверждениеДоставки");
	СоответствиеИменКолонок.Вставить("ПротоколПроверки", "ПротоколПроверки");
	СоответствиеИменКолонок.Вставить("ПодтверждениеНаПротоколПроверки", "ПодтверждениеНаПротоколПроверки");
	СоответствиеИменКолонок.Вставить("ОтветНаЗапрос", "ОтветНаЗапрос");
	СоответствиеИменКолонок.Вставить("ОтветНаОтветНаЗапрос", "ОтветНаОтветНаЗапрос");
	// ТаблицаЦиклыОбменаИсходящиеФНС 
	СоответствиеИменКолонок.Вставить("ТаблицаЦиклыОбменаИсходящиеФНСКолонка1", "ПервичноеСообщение");
	СоответствиеИменКолонок.Вставить("ТаблицаЦиклыОбменаИсходящиеФНСКолонка2", "ПодтверждениеОтправки");
	СоответствиеИменКолонок.Вставить("ТаблицаЦиклыОбменаИсходящиеФНСКолонка3", "ОтветНаПодтверждениеОтправки");
	СоответствиеИменКолонок.Вставить("ТаблицаЦиклыОбменаИсходящиеФНСКолонка4", "ПодтверждениеДоставки");
	СоответствиеИменКолонок.Вставить("ТаблицаЦиклыОбменаИсходящиеФНСКолонка5", "ПротоколПроверки");
	СоответствиеИменКолонок.Вставить("ТаблицаЦиклыОбменаИсходящиеФНСКолонка6", "ПодтверждениеНаПротоколПроверки");
	
	// ТаблицаЦиклыОбменаПредставления 
	СоответствиеИменКолонок.Вставить("ТаблицаЦиклыОбменаПредставленияКолонка1", "ПервичноеСообщение");
	СоответствиеИменКолонок.Вставить("ТаблицаЦиклыОбменаПредставленияКолонка2", "ПодтверждениеОтправки");
	СоответствиеИменКолонок.Вставить("ТаблицаЦиклыОбменаПредставленияКолонка3", "ОтветНаПодтверждениеОтправки");
	СоответствиеИменКолонок.Вставить("ТаблицаЦиклыОбменаПредставленияКолонка4", "ПодтверждениеДоставки");
	СоответствиеИменКолонок.Вставить("ТаблицаЦиклыОбменаПредставленияКолонка5", "ПротоколПроверки");
	СоответствиеИменКолонок.Вставить("ТаблицаЦиклыОбменаПредставленияКолонка6", "ПодтверждениеНаПротоколПроверки");
	
	// ЦиклыОбменаФНС
	СоответствиеИменКолонок.Вставить("ЦиклыОбменаПредставленияКолонка1", "ПервичноеСообщение");
	СоответствиеИменКолонок.Вставить("ЦиклыОбменаПредставленияКолонка2", "ПодтверждениеОтправки");
	СоответствиеИменКолонок.Вставить("ЦиклыОбменаПредставленияКолонка3", "ОтветНаПодтверждениеОтправки");
	СоответствиеИменКолонок.Вставить("ЦиклыОбменаПредставленияКолонка4", "ПодтверждениеДоставки");
	СоответствиеИменКолонок.Вставить("ЦиклыОбменаПредставленияКолонка5", "ПротоколПроверки");
	СоответствиеИменКолонок.Вставить("ЦиклыОбменаПредставленияКолонка6", "ПодтверждениеНаПротоколПроверки");
	
	// ТаблицаЦиклыОбменаУведомления
	СоответствиеИменКолонок.Вставить("ТаблицаЦиклыОбменаУведомленияКолонка1", "ПервичноеСообщение");
	СоответствиеИменКолонок.Вставить("ТаблицаЦиклыОбменаУведомленияКолонка2", "ПодтверждениеОтправки");
	СоответствиеИменКолонок.Вставить("ТаблицаЦиклыОбменаУведомленияКолонка3", "ОтветНаПодтверждениеОтправки");
	СоответствиеИменКолонок.Вставить("ТаблицаЦиклыОбменаУведомленияКолонка4", "ПодтверждениеДоставки");
	СоответствиеИменКолонок.Вставить("ТаблицаЦиклыОбменаУведомленияКолонка5", "ПротоколПроверки");
	СоответствиеИменКолонок.Вставить("ТаблицаЦиклыОбменаУведомленияКолонка6", "ПодтверждениеНаПротоколПроверки");
	
	//ТаблицаЦиклыОбменаВходящиеФНС
	СоответствиеИменКолонок.Вставить("ТаблицаЦиклыОбменаВходящиеФНСКолонка1", "ПервичноеСообщение");
	СоответствиеИменКолонок.Вставить("ТаблицаЦиклыОбменаВходящиеФНСКолонка4", "ПодтверждениеДоставки");
	// ТаблицаЦиклыОбменаОтчетностьПФР
	СоответствиеИменКолонок.Вставить("ТаблицаЦиклыОбменаОтчетностьПФРКолонка1", "ПервичноеСообщение");
	СоответствиеИменКолонок.Вставить("ТаблицаЦиклыОбменаОтчетностьПФРКолонка4", "ПодтверждениеДоставки");
	СоответствиеИменКолонок.Вставить("ТаблицаЦиклыОбменаОтчетностьПФРКолонка5", "ПротоколПроверки");
	СоответствиеИменКолонок.Вставить("ТаблицаЦиклыОбменаОтчетностьПФРКолонка6", "ПодтверждениеНаПротоколПроверки");
	// ТаблицаЦиклыОбменаЗапросыПФР
	СоответствиеИменКолонок.Вставить("ТаблицаЦиклыОбменаЗапросыПФРКолонка1", "ПервичноеСообщение");
	СоответствиеИменКолонок.Вставить("ТаблицаЦиклыОбменаЗапросыПФРКолонка4", "ПодтверждениеДоставки");
	СоответствиеИменКолонок.Вставить("ТаблицаЦиклыОбменаЗапросыПФРКолонка5", "ПротоколПроверки");
	СоответствиеИменКолонок.Вставить("ТаблицаЦиклыОбменаЗапросыПФРКолонка6", "ПодтверждениеНаПротоколПроверки");
	//ТаблицаЦиклыОбменаИсходящиеПФР
	СоответствиеИменКолонок.Вставить("ТаблицаЦиклыОбменаИсходящиеПФРКолонка1", "ПервичноеСообщение");
	СоответствиеИменКолонок.Вставить("ТаблицаЦиклыОбменаИсходящиеПФРКолонка4", "ПодтверждениеДоставки");
	//ТаблицаЦиклыОбменаВходящиеПФР
	СоответствиеИменКолонок.Вставить("ТаблицаЦиклыОбменаВходящиеПФРКолонка1", "ПервичноеСообщение");
	СоответствиеИменКолонок.Вставить("ТаблицаЦиклыОбменаВходящиеПФРКолонка4", "ПодтверждениеДоставки");
	// ТаблицаЦиклыОбменаЗапросыИОН 
	СоответствиеИменКолонок.Вставить("ТаблицаЦиклыОбменаЗапросыИОНКолонка1", "ПервичноеСообщение");
	СоответствиеИменКолонок.Вставить("ТаблицаЦиклыОбменаЗапросыИОНКолонка2", "ПодтверждениеОтправки");
	СоответствиеИменКолонок.Вставить("ТаблицаЦиклыОбменаЗапросыИОНКолонка3", "ОтветНаПодтверждениеОтправки");
	СоответствиеИменКолонок.Вставить("ТаблицаЦиклыОбменаЗапросыИОНКолонка4", "ПодтверждениеДоставки");
	СоответствиеИменКолонок.Вставить("ТаблицаЦиклыОбменаЗапросыИОНКолонка5", "ПротоколПроверки");
	СоответствиеИменКолонок.Вставить("ТаблицаЦиклыОбменаЗапросыИОНКолонка6", "ПодтверждениеНаПротоколПроверки");
	СоответствиеИменКолонок.Вставить("ТаблицаЦиклыОбменаЗапросыИОНКолонка7", "ОтветНаЗапрос");
	СоответствиеИменКолонок.Вставить("ТаблицаЦиклыОбменаЗапросыИОНКолонка8", "ОтветНаОтветНаЗапрос");
	//ЦиклыОбменаФНС
	СоответствиеИменКолонок.Вставить("ЦиклыОбменаФНСКолонка1", "ПервичноеСообщение");
	СоответствиеИменКолонок.Вставить("ЦиклыОбменаФНСКолонка2", "ПодтверждениеОтправки");
	СоответствиеИменКолонок.Вставить("ЦиклыОбменаФНСКолонка3", "ОтветНаПодтверждениеОтправки");
	СоответствиеИменКолонок.Вставить("ЦиклыОбменаФНСКолонка4", "ПодтверждениеДоставки");
	СоответствиеИменКолонок.Вставить("ЦиклыОбменаФНСКолонка5", "ПротоколПроверки");
	СоответствиеИменКолонок.Вставить("ЦиклыОбменаФНСКолонка6", "ПодтверждениеНаПротоколПроверки");
	СоответствиеИменКолонок.Вставить("ЦиклыОбменаФНСКолонка7", "ОтветНаЗапрос");
	СоответствиеИменКолонок.Вставить("ЦиклыОбменаФНСКолонка8", "ОтветНаОтветНаЗапрос");
	
	//ТаблицаЦиклыОбменаЗаявление
	СоответствиеИменКолонок.Вставить("ТаблицаЦиклыОбменаЗаявлениеКолонка1", "ПервичноеСообщение");
	СоответствиеИменКолонок.Вставить("ТаблицаЦиклыОбменаЗаявлениеКолонка2", "ПодтверждениеОтправки");
	СоответствиеИменКолонок.Вставить("ТаблицаЦиклыОбменаЗаявлениеКолонка3", "ОтветНаПодтверждениеОтправки");
	СоответствиеИменКолонок.Вставить("ТаблицаЦиклыОбменаЗаявлениеКолонка4", "ПодтверждениеДоставки");
	СоответствиеИменКолонок.Вставить("ТаблицаЦиклыОбменаЗаявлениеКолонка5", "ПротоколПроверки");
	СоответствиеИменКолонок.Вставить("ТаблицаЦиклыОбменаЗаявлениеКолонка6", "ПодтверждениеНаПротоколПроверки");
	СоответствиеИменКолонок.Вставить("ТаблицаЦиклыОбменаЗаявлениеКолонка7", "ОтветНаЗапрос");
	СоответствиеИменКолонок.Вставить("ТаблицаЦиклыОбменаЗаявлениеКолонка8", "ОтветНаОтветНаЗапрос");
	СоответствиеИменКолонок.Вставить("ТаблицаЦиклыОбменаЗаявлениеКолонка9", "Возврат");
	СоответствиеИменКолонок.Вставить("ТаблицаЦиклыОбменаЗаявлениеКолонка10", "ПодтверждениеВозврата");
	
	//ЦиклыОбменаЗаявление
	СоответствиеИменКолонок.Вставить("ЦиклыОбменаЗаявлениеКолонка1", "ПервичноеСообщение");
	СоответствиеИменКолонок.Вставить("ЦиклыОбменаЗаявлениеКолонка2", "ПодтверждениеОтправки");
	СоответствиеИменКолонок.Вставить("ЦиклыОбменаЗаявлениеКолонка3", "ОтветНаПодтверждениеОтправки");
	СоответствиеИменКолонок.Вставить("ЦиклыОбменаЗаявлениеКолонка4", "ПодтверждениеДоставки");
	СоответствиеИменКолонок.Вставить("ЦиклыОбменаЗаявлениеКолонка5", "ПротоколПроверки");
	СоответствиеИменКолонок.Вставить("ЦиклыОбменаЗаявлениеКолонка6", "ПодтверждениеНаПротоколПроверки");
	СоответствиеИменКолонок.Вставить("ЦиклыОбменаЗаявлениеКолонка7", "ОтветНаЗапрос");
	СоответствиеИменКолонок.Вставить("ЦиклыОбменаЗаявлениеКолонка8", "ОтветНаОтветНаЗапрос");
	СоответствиеИменКолонок.Вставить("ЦиклыОбменаЗаявлениеКолонка9", "Возврат");
	СоответствиеИменКолонок.Вставить("ЦиклыОбменаЗаявлениеКолонка10", "ПодтверждениеВозврата");

	//ЦиклыОбменаТребования
	СоответствиеИменКолонок.Вставить("ТаблицаЦиклыОбменаТребованияКолонка1", "ПервичноеСообщение");
	СоответствиеИменКолонок.Вставить("ТаблицаЦиклыОбменаТребованияКолонка4", "ПодтверждениеДоставки");
	СоответствиеИменКолонок.Вставить("ТаблицаЦиклыОбменаТребованияКолонка5", "ПротоколПроверки");
	СоответствиеИменКолонок.Вставить("ТаблицаЦиклыОбменаТребованияКолонка6", "ПодтверждениеНаПротоколПроверки");

	//ЦиклыОбменаПФР
	СоответствиеИменКолонок.Вставить("ЦиклыОбменаПФРКолонка1", "ПервичноеСообщение");
	СоответствиеИменКолонок.Вставить("ЦиклыОбменаПФРКолонка4", "ПодтверждениеДоставки");
	СоответствиеИменКолонок.Вставить("ЦиклыОбменаПФРКолонка5", "ПротоколПроверки");
	СоответствиеИменКолонок.Вставить("ЦиклыОбменаПФРКолонка6", "ПодтверждениеНаПротоколПроверки");
	
	СоответствиеИменКолонок.Вставить("ТаблицаЦиклыОбменаЗапросыИОСКолонка1", "ПервичноеСообщение");
	СоответствиеИменКолонок.Вставить("ТаблицаЦиклыОбменаЗапросыИОСКолонка2", "ПодтверждениеДоставки");
	СоответствиеИменКолонок.Вставить("ТаблицаЦиклыОбменаЗапросыИОСКолонка3", "ОтветНаЗапрос");
	СоответствиеИменКолонок.Вставить("ТаблицаЦиклыОбменаЗапросыИОСКолонка4", "ОтветНаОтветНаЗапрос");
	
	//ЦиклыОбменаФСГС
	СоответствиеИменКолонок.Вставить("ЦиклыОбменаФСГСКолонка1", "ПервичноеСообщение");
	СоответствиеИменКолонок.Вставить("ЦиклыОбменаФСГСКолонка2", "ПодтверждениеОтправки");
	СоответствиеИменКолонок.Вставить("ЦиклыОбменаФСГСКолонка3", "ОтветНаПодтверждениеОтправки");
	СоответствиеИменКолонок.Вставить("ЦиклыОбменаФСГСКолонка4", "ПодтверждениеДоставки");
	СоответствиеИменКолонок.Вставить("ЦиклыОбменаФСГСКолонка5", "ПротоколПроверки");
	СоответствиеИменКолонок.Вставить("ЦиклыОбменаФСГСКолонка6", "ПодтверждениеНаПротоколПроверки");
	СоответствиеИменКолонок.Вставить("ЦиклыОбменаФСГСКолонка7", "ОтветНаЗапрос");
	СоответствиеИменКолонок.Вставить("ЦиклыОбменаФСГСКолонка8", "ОтветНаОтветНаЗапрос");
	
	СоответствиеИменКолонок.Вставить("ТаблицаЦиклыОбменаОтчетностьФСГСКолонка1", "ПервичноеСообщение");
	СоответствиеИменКолонок.Вставить("ТаблицаЦиклыОбменаОтчетностьФСГСКолонка2", "ПодтверждениеОтправки");
	СоответствиеИменКолонок.Вставить("ТаблицаЦиклыОбменаОтчетностьФСГСКолонка3", "ОтветНаПодтверждениеОтправки");
	СоответствиеИменКолонок.Вставить("ТаблицаЦиклыОбменаОтчетностьФСГСКолонка4", "ПодтверждениеДоставки");
	СоответствиеИменКолонок.Вставить("ТаблицаЦиклыОбменаОтчетностьФСГСКолонка5", "ПротоколПроверки");
	СоответствиеИменКолонок.Вставить("ТаблицаЦиклыОбменаОтчетностьФСГСКолонка6", "ПодтверждениеНаПротоколПроверки");
	СоответствиеИменКолонок.Вставить("ТаблицаЦиклыОбменаОтчетностьФСГСКолонка7", "ОтветНаЗапрос");
	СоответствиеИменКолонок.Вставить("ТаблицаЦиклыОбменаОтчетностьФСГСКолонка8", "ОтветНаОтветНаЗапрос");
	
	СоответствиеИменКолонок.Вставить("ТаблицаЦиклыОбменаИсходящиеФСГСКолонка1", "ПервичноеСообщение");
	СоответствиеИменКолонок.Вставить("ТаблицаЦиклыОбменаИсходящиеФСГСКолонка4", "ПодтверждениеДоставки");
	
	СоответствиеИменКолонок.Вставить("ТаблицаЦиклыОбменаВходящиеФСГСКолонка1", "ПервичноеСообщение");
	СоответствиеИменКолонок.Вставить("ТаблицаЦиклыОбменаВходящиеФСГСКолонка2", "ПодтверждениеОтправки");
	СоответствиеИменКолонок.Вставить("ТаблицаЦиклыОбменаВходящиеФСГСКолонка3", "ОтветНаПодтверждениеОтправки");
	СоответствиеИменКолонок.Вставить("ТаблицаЦиклыОбменаВходящиеФСГСКолонка4", "ПодтверждениеДоставки");
	
	// Запросы на выписку ЕГРЮЛ/ЕГРИП
	СоответствиеИменКолонок.Вставить("ЦиклыОбменаЗапросыЕГРКолонка1", "ПервичноеСообщение");
	СоответствиеИменКолонок.Вставить("ЦиклыОбменаЗапросыЕГРКолонка2", "ОтветНаЗапрос");
	
	//ЦиклыОбменаУведомление
	СоответствиеИменКолонок.Вставить("ЦиклыОбменаУведомлениеКолонка1", "ПервичноеСообщение");
	СоответствиеИменКолонок.Вставить("ЦиклыОбменаУведомлениеКолонка2", "ПодтверждениеОтправки");
	СоответствиеИменКолонок.Вставить("ЦиклыОбменаУведомлениеКолонка3", "ОтветНаПодтверждениеОтправки");
	СоответствиеИменКолонок.Вставить("ЦиклыОбменаУведомлениеКолонка4", "ПодтверждениеДоставки");
	СоответствиеИменКолонок.Вставить("ЦиклыОбменаУведомлениеКолонка5", "ПротоколПроверки");
	СоответствиеИменКолонок.Вставить("ЦиклыОбменаУведомлениеКолонка6", "ПодтверждениеНаПротоколПроверки");
	
	//ИмяКолонки = Колонка.Имя;
	ИмяКолонки = СоответствиеИменКолонок[Колонка];

	Если ИмяКолонки = "ПервичноеСообщение" ИЛИ ИмяКолонки = "ПодтверждениеОтправки" ИЛИ ИмяКолонки = "ОтветНаПодтверждениеОтправки"
	ИЛИ ИмяКолонки = "ПодтверждениеДоставки" ИЛИ ИмяКолонки = "ПротоколПроверки" ИЛИ ИмяКолонки = "ПодтверждениеНаПротоколПроверки"
	ИЛИ ИмяКолонки = "ОтветНаЗапрос" ИЛИ ИмяКолонки = "ОтветНаОтветНаЗапрос" 
	ИЛИ ИмяКолонки = "Возврат" ИЛИ ИмяКолонки = "ПодтверждениеВозврата" Тогда
	
		СтандартнаяОбработка = Ложь;
		
		ТипСообщенияДляПоказа = Неопределено;
		Если ВыбраннаяСтрока.Ссылка.Тип = Перечисления.ТипыЦикловОбмена.НалоговаяИлиБухгалтерскаяОтчетность Тогда
			Если ВыбраннаяСтрока.Ссылка.ФорматДокументооборота = Перечисления.ФорматыДокументооборотаСФНС.Приказ534 Тогда
				Если ИмяКолонки = "ПервичноеСообщение" Тогда
					ТипСообщенияДляПоказа = Перечисления.ТипыТранспортныхСообщений.ДекларацияНП;
				ИначеЕсли ИмяКолонки = "ПодтверждениеОтправки" Тогда
					ТипСообщенияДляПоказа = Перечисления.ТипыТранспортныхСообщений.ПодтверждениеДекларацияНО;
				ИначеЕсли ИмяКолонки = "ОтветНаПодтверждениеОтправки" Тогда
					ТипСообщенияДляПоказа = Перечисления.ТипыТранспортныхСообщений.ИзвещениеПодтверждениеНП;
				ИначеЕсли ИмяКолонки = "ПодтверждениеДоставки" Тогда
					ТипСообщенияДляПоказа = Перечисления.ТипыТранспортныхСообщений.ИзвещениеДекларацияНО;
				ИначеЕсли ИмяКолонки = "ПротоколПроверки" Тогда
					ТипСообщенияДляПоказа = Перечисления.ТипыТранспортныхСообщений.РезультатПриемаДекларацияНО;
				ИначеЕсли ИмяКолонки = "ПодтверждениеНаПротоколПроверки" Тогда
					ТипСообщенияДляПоказа = Перечисления.ТипыТранспортныхСообщений.ИзвещениеРезультатПриемаНП;
				ИначеЕсли ИмяКолонки = "ОтветНаЗапрос" Тогда
					ТипСообщенияДляПоказа = Перечисления.ТипыТранспортныхСообщений.РезультатОбработкиДекларацияНО;
				ИначеЕсли ИмяКолонки = "ОтветНаОтветНаЗапрос" Тогда
					ТипСообщенияДляПоказа = Перечисления.ТипыТранспортныхСообщений.ИзвещениеРезультатОбработкиНП;
				КонецЕсли;
			КонецЕсли;
		ИначеЕсли ВыбраннаяСтрока.Ссылка.Тип = Перечисления.ТипыЦикловОбмена.Документ Тогда
			Если ИмяКолонки = "ПервичноеСообщение" Тогда
				ТипСообщенияДляПоказа = Перечисления.ТипыТранспортныхСообщений.ДокументНО;
			ИначеЕсли ИмяКолонки = "ПодтверждениеДоставки" Тогда
				ТипСообщенияДляПоказа = Перечисления.ТипыТранспортныхСообщений.ИзвещениеДокументНП;
			ИначеЕсли ИмяКолонки = "ПротоколПроверки" Тогда
				ТипСообщенияДляПоказа = Перечисления.ТипыТранспортныхСообщений.РезультатПриемаДокументНП;
			ИначеЕсли ИмяКолонки = "ПодтверждениеНаПротоколПроверки" Тогда
				ТипСообщенияДляПоказа = Перечисления.ТипыТранспортныхСообщений.ИзвещениеРезультатПриемаНО;
			КонецЕсли;
		ИначеЕсли ВыбраннаяСтрока.Ссылка.Тип = Перечисления.ТипыЦикловОбмена.Заявление Тогда
			Если ИмяКолонки = "ПервичноеСообщение" Тогда
				ТипСообщенияДляПоказа = Перечисления.ТипыТранспортныхСообщений.ЗаявлениеНП;
			ИначеЕсли ИмяКолонки = "ПодтверждениеОтправки" Тогда
				ТипСообщенияДляПоказа = Перечисления.ТипыТранспортныхСообщений.ПодтверждениеЗаявлениеНО;
			ИначеЕсли ИмяКолонки = "ОтветНаПодтверждениеОтправки" Тогда
				ТипСообщенияДляПоказа = Перечисления.ТипыТранспортныхСообщений.ИзвещениеПодтверждениеНП;
			ИначеЕсли ИмяКолонки = "ПодтверждениеДоставки" Тогда
				ТипСообщенияДляПоказа = Перечисления.ТипыТранспортныхСообщений.ИзвещениеЗаявлениеНО;
			ИначеЕсли ИмяКолонки = "ПротоколПроверки" Тогда
				ТипСообщенияДляПоказа = Перечисления.ТипыТранспортныхСообщений.РезультатПриемаЗаявлениеНО;
			ИначеЕсли ИмяКолонки = "ПодтверждениеНаПротоколПроверки" Тогда
				ТипСообщенияДляПоказа = Перечисления.ТипыТранспортныхСообщений.ИзвещениеРезультатПриемаНП;
			ИначеЕсли ИмяКолонки = "ОтветНаЗапрос" Тогда
				ТипСообщенияДляПоказа = Новый Массив;
				ТипСообщенияДляПоказа.Добавить(Перечисления.ТипыТранспортныхСообщений.РезультатОбработкиЗаявлениеНО);
				ТипСообщенияДляПоказа.Добавить(Перечисления.ТипыТранспортныхСообщений.РезультатОбработкиЗаявлениеРФНО);
			ИначеЕсли ИмяКолонки = "ОтветНаОтветНаЗапрос" Тогда
				ТипСообщенияДляПоказа = Новый Массив;
				ТипСообщенияДляПоказа.Добавить(Перечисления.ТипыТранспортныхСообщений.ИзвещениеРезультатОбработкиНП);
				ТипСообщенияДляПоказа.Добавить(Перечисления.ТипыТранспортныхСообщений.ИзвещениеРезультатОбработкиРФНП);
			ИначеЕсли ИмяКолонки = "Возврат" Тогда
				ТипСообщенияДляПоказа = Новый Массив;
				ТипСообщенияДляПоказа.Добавить(Перечисления.ТипыТранспортныхСообщений.СообщениеОбОтзывеЗаявлениеРФНО);
				ТипСообщенияДляПоказа.Добавить(Перечисления.ТипыТранспортныхСообщений.РезультатОбработкиЗаявлениеТСНО);
			ИначеЕсли ИмяКолонки = "ПодтверждениеВозврата" Тогда
				ТипСообщенияДляПоказа = Новый Массив;
				ТипСообщенияДляПоказа.Добавить(Перечисления.ТипыТранспортныхСообщений.ИзвещениеОбОтзывеЗаявлениеРФНП);
				ТипСообщенияДляПоказа.Добавить(Перечисления.ТипыТранспортныхСообщений.ИзвещениеРезультатОбработкиТСНП);
			КонецЕсли;
		ИначеЕсли ВыбраннаяСтрока.Ссылка.Тип = Перечисления.ТипыЦикловОбмена.Форма2НДФЛ Тогда
			Если ИмяКолонки = "ПервичноеСообщение" Тогда
				ТипСообщенияДляПоказа = Перечисления.ТипыТранспортныхСообщений.Форма2НДФЛНП;
			ИначеЕсли ИмяКолонки = "ПодтверждениеОтправки" Тогда
				ТипСообщенияДляПоказа = Перечисления.ТипыТранспортныхСообщений.ПодтверждениеФорма2НДФЛНО;
			ИначеЕсли ИмяКолонки = "ОтветНаПодтверждениеОтправки" Тогда
				ТипСообщенияДляПоказа = Перечисления.ТипыТранспортныхСообщений.ИзвещениеПодтверждениеНП;
			ИначеЕсли ИмяКолонки = "ПодтверждениеДоставки" Тогда
				ТипСообщенияДляПоказа = Перечисления.ТипыТранспортныхСообщений.ИзвещениеФорма2НДФЛНО;
			ИначеЕсли ИмяКолонки = "ПротоколПроверки" Тогда
				ТипСообщенияДляПоказа = Перечисления.ТипыТранспортныхСообщений.РезультатПриемаФорма2НДФЛНО;
			ИначеЕсли ИмяКолонки = "ПодтверждениеНаПротоколПроверки" Тогда
				ТипСообщенияДляПоказа = Перечисления.ТипыТранспортныхСообщений.ИзвещениеРезультатПриемаНП;
			КонецЕсли;
		ИначеЕсли ВыбраннаяСтрока.Ссылка.Тип = Перечисления.ТипыЦикловОбмена.ОтчетностьПФР Тогда
			Если ИмяКолонки = "ПервичноеСообщение" Тогда
				ТипСообщенияДляПоказа = Перечисления.ТипыТранспортныхСообщений.ПервичноеСообщениеСодержащееОтчетностьПФР;
			ИначеЕсли ИмяКолонки = "ПодтверждениеДоставки" Тогда
				ТипСообщенияДляПоказа = Перечисления.ТипыТранспортныхСообщений.ПодтверждениеПолученияОтчетностиПФР;
			ИначеЕсли ИмяКолонки = "ПротоколПроверки" Тогда
				ТипСообщенияДляПоказа = Перечисления.ТипыТранспортныхСообщений.ПротоколПФР;
			ИначеЕсли ИмяКолонки = "ПодтверждениеНаПротоколПроверки" Тогда
				ТипСообщенияДляПоказа = Перечисления.ТипыТранспортныхСообщений.ПротоколКвитанцияПФР;
			КонецЕсли;
		ИначеЕсли ВыбраннаяСтрока.Ссылка.Тип = Перечисления.ТипыЦикловОбмена.НеформализованнаяПерепискаПФРИсходящие Тогда
			Если ИмяКолонки = "ПервичноеСообщение" Тогда
				ТипСообщенияДляПоказа = Перечисления.ТипыТранспортныхСообщений.ПисьмоИсходящееПФР;
			ИначеЕсли ИмяКолонки = "ПодтверждениеДоставки" Тогда
				ТипСообщенияДляПоказа = Перечисления.ТипыТранспортныхСообщений.ПисьмоИсходящееКвитанцияПФР;
			КонецЕсли;
		ИначеЕсли ВыбраннаяСтрока.Ссылка.Тип = Перечисления.ТипыЦикловОбмена.НеформализованнаяПерепискаПФРВходящие
			Или ВыбраннаяСтрока.Ссылка.Тип = Перечисления.ТипыЦикловОбмена.РассылкаПФР Тогда
			Если ИмяКолонки = "ПервичноеСообщение" Тогда
				ТипСообщенияДляПоказа = Перечисления.ТипыТранспортныхСообщений.ПисьмоВходящееПФР;
			ИначеЕсли ИмяКолонки = "ПодтверждениеДоставки" Тогда
				ТипСообщенияДляПоказа = Перечисления.ТипыТранспортныхСообщений.ПисьмоВходящееКвитанцияПФР;
			КонецЕсли;
		ИначеЕсли ВыбраннаяСтрока.Ссылка.Тип = Перечисления.ТипыЦикловОбмена.ИОС Тогда
			Если ИмяКолонки = "ПервичноеСообщение" Тогда
				ТипСообщенияДляПоказа = Перечисления.ТипыТранспортныхСообщений.ПервичноеСообщениеСодержащееЗапросПФР;
			ИначеЕсли ИмяКолонки = "ПодтверждениеДоставки" Тогда
				ТипСообщенияДляПоказа = Перечисления.ТипыТранспортныхСообщений.ПодтверждениеПолученияЗапросаПФР;
			ИначеЕсли ИмяКолонки = "ОтветНаЗапрос" Тогда
				ТипСообщенияДляПоказа = Перечисления.ТипыТранспортныхСообщений.ОтветНаЗапросПФР;
			ИначеЕсли ИмяКолонки = "ОтветНаОтветНаЗапрос" Тогда
				ТипСообщенияДляПоказа = Перечисления.ТипыТранспортныхСообщений.ОтветНаЗапросКвитанцияПФР;
			КонецЕсли;
		ИначеЕсли ВыбраннаяСтрока.Ссылка.Тип = Перечисления.ТипыЦикловОбмена.ПисьмоНО Тогда
			Если ИмяКолонки = "ПервичноеСообщение" Тогда
				ТипСообщенияДляПоказа = Перечисления.ТипыТранспортныхСообщений.ПисьмоНО;
			ИначеЕсли ИмяКолонки = "ПодтверждениеДоставки" Тогда
				ТипСообщенияДляПоказа = Перечисления.ТипыТранспортныхСообщений.ИзвещениеПисьмоНП;
			КонецЕсли;
		ИначеЕсли ВыбраннаяСтрока.Ссылка.Тип = Перечисления.ТипыЦикловОбмена.Рассылка Тогда
			Если ИмяКолонки = "ПервичноеСообщение" Тогда
				ТипСообщенияДляПоказа = Перечисления.ТипыТранспортныхСообщений.РассылкаНО;
			КонецЕсли;
		ИначеЕсли ВыбраннаяСтрока.Ссылка.Тип = Перечисления.ТипыЦикловОбмена.РассылкаГрупповая Тогда
			Если ИмяКолонки = "ПервичноеСообщение" Тогда
				ТипСообщенияДляПоказа = Перечисления.ТипыТранспортныхСообщений.РассылкаНО;
			ИначеЕсли ИмяКолонки = "ПодтверждениеДоставки" Тогда
				ТипСообщенияДляПоказа = Перечисления.ТипыТранспортныхСообщений.ИзвещениеРассылкаНП;
			КонецЕсли;
		ИначеЕсли ВыбраннаяСтрока.Ссылка.Тип = Перечисления.ТипыЦикловОбмена.ОбращениеНП Тогда
			Если ИмяКолонки = "ПервичноеСообщение" Тогда
				ТипСообщенияДляПоказа = Перечисления.ТипыТранспортныхСообщений.ОбращениеНП;
			ИначеЕсли ИмяКолонки = "ПодтверждениеОтправки" Тогда
				ТипСообщенияДляПоказа = Перечисления.ТипыТранспортныхСообщений.ПодтверждениеОбращениеНО;
			ИначеЕсли ИмяКолонки = "ОтветНаПодтверждениеОтправки" Тогда
				ТипСообщенияДляПоказа = Перечисления.ТипыТранспортныхСообщений.ИзвещениеПодтверждениеНП;
			ИначеЕсли ИмяКолонки = "ПодтверждениеДоставки" Тогда
				ТипСообщенияДляПоказа = Перечисления.ТипыТранспортныхСообщений.ИзвещениеОбращениеНО;
			ИначеЕсли ИмяКолонки = "ПротоколПроверки" Тогда
				ТипСообщенияДляПоказа = Перечисления.ТипыТранспортныхСообщений.РезультатПриемаОбращениеНО;
			ИначеЕсли ИмяКолонки = "ПодтверждениеНаПротоколПроверки" Тогда
				ТипСообщенияДляПоказа = Перечисления.ТипыТранспортныхСообщений.ИзвещениеРезультатПриемаНП;
			КонецЕсли;
		ИначеЕсли ВыбраннаяСтрока.Ссылка.Тип = Перечисления.ТипыЦикловОбмена.Представление Тогда
			Если ИмяКолонки = "ПервичноеСообщение" Тогда
				ТипСообщенияДляПоказа = Перечисления.ТипыТранспортныхСообщений.ПредставлениеНП;
			ИначеЕсли ИмяКолонки = "ПодтверждениеОтправки" Тогда
				ТипСообщенияДляПоказа = Перечисления.ТипыТранспортныхСообщений.ПодтверждениеПредставлениеНО;
			ИначеЕсли ИмяКолонки = "ОтветНаПодтверждениеОтправки" Тогда
				ТипСообщенияДляПоказа = Перечисления.ТипыТранспортныхСообщений.ИзвещениеПодтверждениеНП;
			ИначеЕсли ИмяКолонки = "ПодтверждениеДоставки" Тогда
				ТипСообщенияДляПоказа = Перечисления.ТипыТранспортныхСообщений.ИзвещениеПредставлениеНО;
			ИначеЕсли ИмяКолонки = "ПротоколПроверки" Тогда
				ТипСообщенияДляПоказа = Перечисления.ТипыТранспортныхСообщений.РезультатПриемаПредставлениеНО;
			ИначеЕсли ИмяКолонки = "ПодтверждениеНаПротоколПроверки" Тогда
				ТипСообщенияДляПоказа = Перечисления.ТипыТранспортныхСообщений.ИзвещениеРезультатПриемаНП;
			КонецЕсли;
		ИначеЕсли ВыбраннаяСтрока.Ссылка.Тип = Перечисления.ТипыЦикловОбмена.ИОН Тогда
			Если ИмяКолонки = "ПервичноеСообщение" Тогда
				ТипСообщенияДляПоказа = Перечисления.ТипыТранспортныхСообщений.ЗапросНП;
			ИначеЕсли ИмяКолонки = "ПодтверждениеОтправки" Тогда
				ТипСообщенияДляПоказа = Перечисления.ТипыТранспортныхСообщений.ПодтверждениеЗапросНО;
			ИначеЕсли ИмяКолонки = "ОтветНаПодтверждениеОтправки" Тогда
				ТипСообщенияДляПоказа = Перечисления.ТипыТранспортныхСообщений.ИзвещениеПодтверждениеНП;
			ИначеЕсли ИмяКолонки = "ПодтверждениеДоставки" Тогда
				ТипСообщенияДляПоказа = Перечисления.ТипыТранспортныхСообщений.ИзвещениеЗапросНО;
			ИначеЕсли ИмяКолонки = "ПротоколПроверки" Тогда
				ТипСообщенияДляПоказа = Перечисления.ТипыТранспортныхСообщений.РезультатПриемаЗапросНО;
			ИначеЕсли ИмяКолонки = "ПодтверждениеНаПротоколПроверки" Тогда
				ТипСообщенияДляПоказа = Перечисления.ТипыТранспортныхСообщений.ИзвещениеРезультатПриемаНП;
			ИначеЕсли ИмяКолонки = "ОтветНаЗапрос" Тогда
				ТипСообщенияДляПоказа = Перечисления.ТипыТранспортныхСообщений.РезультатОбработкиЗапросНО;
			ИначеЕсли ИмяКолонки = "ОтветНаОтветНаЗапрос" Тогда
				ТипСообщенияДляПоказа = Перечисления.ТипыТранспортныхСообщений.ИзвещениеРезультатОбработкиНП;
			КонецЕсли;
		ИначеЕсли ВыбраннаяСтрока.Ссылка.Тип = Перечисления.ТипыЦикловОбмена.ОтчетностьФСГС Тогда
			Если ИмяКолонки = "ПервичноеСообщение" Тогда
				ТипСообщенияДляПоказа = Перечисления.ТипыТранспортныхСообщений.ПервичноеСообщениеСодержащееОтчетностьФСГС;
			ИначеЕсли ИмяКолонки = "ПодтверждениеОтправки" Тогда
				ТипСообщенияДляПоказа = Перечисления.ТипыТранспортныхСообщений.ПодтверждениеДатыОтправкиФСГС;
			ИначеЕсли ИмяКолонки = "ОтветНаПодтверждениеОтправки" Тогда
				ТипСообщенияДляПоказа = Перечисления.ТипыТранспортныхСообщений.ИзвещениеПодтверждениеДатыОтправкиФСГС;
			ИначеЕсли ИмяКолонки = "ПодтверждениеДоставки" Тогда
				ТипСообщенияДляПоказа = Перечисления.ТипыТранспортныхСообщений.ИзвещениеОПолученииОтчетностиФСГС;
			ИначеЕсли ИмяКолонки = "ПротоколПроверки" Тогда
				ТипСообщенияДляПоказа = Перечисления.ТипыТранспортныхСообщений.ПротоколВходногоКонтроляОтчетностиФСГС;
			ИначеЕсли ИмяКолонки = "ПодтверждениеНаПротоколПроверки" Тогда
				ТипСообщенияДляПоказа = Перечисления.ТипыТранспортныхСообщений.ИзвещениеПротоколВходногоКонтроляОтчетностиФСГС;
			КонецЕсли;
		ИначеЕсли ВыбраннаяСтрока.Ссылка.Тип = Перечисления.ТипыЦикловОбмена.ПисьменноеОбращениеВФСГС Тогда
			Если ИмяКолонки = "ПервичноеСообщение" Тогда
				ТипСообщенияДляПоказа = Перечисления.ТипыТранспортныхСообщений.ПисьмоИсходящееФСГС;
			ИначеЕсли ИмяКолонки = "ПодтверждениеОтправки" Тогда
				ТипСообщенияДляПоказа = Перечисления.ТипыТранспортныхСообщений.ПодтверждениеДатыОтправкиФСГС;
			ИначеЕсли ИмяКолонки = "ОтветНаПодтверждениеОтправки" Тогда
				ТипСообщенияДляПоказа = Перечисления.ТипыТранспортныхСообщений.ИзвещениеПодтверждениеДатыОтправкиФСГС;
			ИначеЕсли ИмяКолонки = "ПодтверждениеДоставки" Тогда
				ТипСообщенияДляПоказа = Перечисления.ТипыТранспортныхСообщений.ИзвещениеПисьмоИсходящееФСГС;
			КонецЕсли;
		ИначеЕсли ВыбраннаяСтрока.Ссылка.Тип = Перечисления.ТипыЦикловОбмена.ИндивидуальноеИнформированиеФСГС Тогда
			Если ИмяКолонки = "ПервичноеСообщение" Тогда
				ТипСообщенияДляПоказа = Перечисления.ТипыТранспортныхСообщений.ПисьмоВходящееФСГС;
			ИначеЕсли ИмяКолонки = "ПодтверждениеДоставки" Тогда
				ТипСообщенияДляПоказа = Перечисления.ТипыТранспортныхСообщений.ИзвещениеПисьмоВходящееФСГС;
			КонецЕсли;
		ИначеЕсли ВыбраннаяСтрока.Ссылка.Тип = Перечисления.ТипыЦикловОбмена.РассылкаФСГС Тогда
			Если ИмяКолонки = "ПервичноеСообщение" Тогда
				ТипСообщенияДляПоказа = Перечисления.ТипыТранспортныхСообщений.РассылкаФСГС;
			КонецЕсли;
		ИначеЕсли ВыбраннаяСтрока.Ссылка.Тип = Перечисления.ТипыЦикловОбмена.РассылкаШаблоновФСГС Тогда
			Если ИмяКолонки = "ПервичноеСообщение" Тогда
				ТипСообщенияДляПоказа = Перечисления.ТипыТранспортныхСообщений.РассылкаШаблоновФСГС;
			КонецЕсли;
		ИначеЕсли ВыбраннаяСтрока.Ссылка.Тип = Перечисления.ТипыЦикловОбмена.ЗапросНаВыпискуИзЕГРЮЛ_ЕГРИП Тогда
			Если ИмяКолонки = "ПервичноеСообщение" Тогда
				ТипСообщенияДляПоказа = Перечисления.ТипыТранспортныхСообщений.ЗапросНаВыпискуЕРГЮЛ_ЕГРИП;
			ИначеЕсли ИмяКолонки = "ОтветНаЗапрос" Тогда
				ТипСообщенияДляПоказа = Перечисления.ТипыТранспортныхСообщений.РезультатОбработкиЗапросаНаВыпискуЕРГЮЛ_ЕГРИП;
			КонецЕсли;
		КонецЕсли;
		
		Если ТипСообщенияДляПоказа <> Неопределено Тогда
			
			Возврат ПоказатьСообщение(ВыбраннаяСтрока.Ссылка, ТипСообщенияДляПоказа);
			
		Иначе 
		КонецЕсли;
	
	КонецЕсли;

КонецФункции

Функция ТекущийПользовательЯвляетсяАдминистраторомУчетныхЗаписейДокументооборота() Экспорт
	
	Возврат Пользователи.ЭтоПолноправныйПользователь();
	
КонецФункции

Функция РасширениеФайла(ИмяФайла) Экспорт
	
	ДлинаИмени = СтрДлина(ИмяФайла);
	Для Инд = 1 По ДлинаИмени Цикл
		ТекИнд = ДлинаИмени - Инд + 1;
		Если Сред(ИмяФайла, ТекИнд, 1) = "." Тогда
			Возврат Сред(ИмяФайла, ТекИнд + 1);
		КонецЕсли;
	КонецЦикла;
	Возврат Неопределено;
	
КонецФункции

Функция ДатаПоСтроке(Знач Стр) ЭКспорт
	
	Попытка
		Если СтрНайти(Стр, "-") <> 0 Тогда
			Если СтрДлина(Стр) > 10 Тогда
				Возврат Дата(Число(СокрЛП(Сред(Стр, 1, 4))),
							Число(СокрЛП(Сред(Стр, 6, 2))),
							Число(СокрЛП(Сред(Стр, 9, 2))),
							Число(СокрЛП(Сред(Стр, 12, 2))),
							Число(СокрЛП(Сред(Стр, 15, 2))),
							Число(СокрЛП(Сред(Стр, 18, 2))));
			Иначе
				Возврат Дата(Число(СокрЛП(Сред(Стр, 1, 4))),
							Число(СокрЛП(Сред(Стр, 6, 2))),
							Число(СокрЛП(Сред(Стр, 9, 2))));
			КонецЕсли;
		Иначе
			Если СтрНайти(Стр, ":") <> 0 Тогда
				Стр = СтрЗаменить(Стр, " ", ",");
				Стр = СтрЗаменить(Стр, ":", ",");
				Стр = СтрЗаменить(Стр, ".", ",");
				Стр = СтрЗаменить(Стр, ",,", ",");
				МассивЭлементов = Новый Массив;
				ВхождениеРазделителя = СтрНайти(Стр, ",");
				Пока ВхождениеРазделителя <> 0 Цикл
					МассивЭлементов.Добавить(СокрЛП(Лев(Стр, ВхождениеРазделителя - 1)));
					Стр = СокрЛП(Сред(Стр, ВхождениеРазделителя + 1));
					ВхождениеРазделителя = СтрНайти(Стр, ",");
				КонецЦикла;
				Возврат Дата(ПолучитьЭлементДаты(МассивЭлементов, 2),
							ПолучитьЭлементДаты(МассивЭлементов, 1),
							ПолучитьЭлементДаты(МассивЭлементов, 0),
							ПолучитьЭлементДаты(МассивЭлементов, 3),
							ПолучитьЭлементДаты(МассивЭлементов, 4),
							ПолучитьЭлементДаты(МассивЭлементов, 5));
			Иначе
				Возврат Дата(Число(СокрЛП(Сред(Стр, 7, 4))),
							Число(СокрЛП(Сред(Стр, 4, 2))),
							Число(СокрЛП(Сред(Стр, 1, 2))));
			КонецЕсли;
		КонецЕсли;
	Исключение
		Возврат '00010101';
	КонецПопытки;
	
КонецФункции

Функция ОпределитьТипСКЗИ(СвойстваКриптопровайдера)
	
	Если СтрНайти(нрег(СвойстваКриптопровайдера.Имя), нрег("Infotecs")) <> 0 Тогда

		Возврат "Домен-К";
	Иначе
		Возврат "Крипто-Про";
	КонецЕсли;
	
КонецФункции

Функция ПолучитьДанныеСтрокой64(ДвДанные) Экспорт
	
	Если ТипЗнч(ДвДанные) = Тип("ХранилищеЗначения") Тогда 
		ДвДанные = ДвДанные.Получить();
	КонецЕсли;
	
	СтрокаBase64 = Base64Строка(ДвДанные);
	СтрокаЗамены = Символ(13) + Символ(10);
	СтрокаBase64 = СтрЗаменить(СтрокаBase64, СтрокаЗамены, "");
	
	Возврат СтрокаBase64;
	
КонецФункции

//////////////////////////////////////////////////////////////////////////////////
//// ФУНКЦИИ РАБОТЫ С XML
////

Функция ЗагрузитьСтрокуXMLВДеревоЗначений(СтрокаXML, ОписаниеОшибки = Неопределено, ЧтениеXML = Неопределено, Знач ТекУзел = Неопределено) Экспорт
	
	ПерваяИтерация = (ТекУзел = Неопределено);
	Если ПерваяИтерация Тогда
		ТекУзел = СоздатьДеревоСтруктурыXML();
		Попытка
			ЧтениеXML = Новый ЧтениеXML;
			ЧтениеXML.УстановитьСтроку(СтрокаXML);
		Исключение
			ОписаниеОшибки = "Ошибка разбора XML: " + ИнформацияОбОшибке().Описание + ".";
			Возврат Неопределено;
		КонецПопытки;
	КонецЕсли;
	
	Попытка
		Пока ЧтениеXML.Прочитать() Цикл
			ТипУзла = ЧтениеXML.ТипУзла;
			Если ТипУзла = ТипУзлаXML.НачалоЭлемента Тогда
				НовУзел = ТекУзел.Строки.Добавить();
				НовУзел.Имя = ЧтениеXML.Имя;
				НовУзел.Тип = "Э";
				НовУзел.Значение = ЧтениеXML.Значение;
				Пока ЧтениеXML.ПрочитатьАтрибут() Цикл
					НовАтрибут = НовУзел.Строки.Добавить();
					НовАтрибут.Имя = ЧтениеXML.Имя;
					НовАтрибут.Тип = "А";
					НовАтрибут.Значение = ЧтениеXML.Значение;
				КонецЦикла;
				ЗагрузитьСтрокуXMLВДеревоЗначений(СтрокаXML, ОписаниеОшибки, ЧтениеXML, НовУзел);
				Если ЗначениеЗаполнено(ОписаниеОшибки) Тогда
					Возврат Неопределено;
				КонецЕсли;
			ИначеЕсли ТипУзла = ТипУзлаXML.КонецЭлемента Тогда
				Прервать;
			ИначеЕсли ТипУзла = ТипУзлаXML.Текст И ТипЗнч(ТекУзел) = Тип("СтрокаДереваЗначений") И ТекУзел.Тип = "Э" Тогда
				ТекУзел.Значение = ЧтениеXML.Значение;
			КонецЕсли;
		КонецЦикла;
	Исключение
		ОписаниеОшибки = "Ошибка разбора XML: " + ИнформацияОбОшибке().Описание + ".";
		Возврат Неопределено;
	КонецПопытки;
	
	Если ПерваяИтерация Тогда
		Возврат ТекУзел;
	КонецЕсли;
	
КонецФункции

Функция СоздатьДеревоСтруктурыXML() Экспорт
	
	ДеревоСтруктуры = Новый ДеревоЗначений;
	ДеревоСтруктуры.Колонки.Добавить("Имя");
	ДеревоСтруктуры.Колонки.Добавить("Тип");
	ДеревоСтруктуры.Колонки.Добавить("Значение");
	Возврат ДеревоСтруктуры;
	
КонецФункции

// есть в Контейнере
Функция СгенерироватьИмяФайлаДокументаВСоставеПакета(РасширениеБезТочки = "bin") Экспорт
	
	Возврат СгенерироватьUUID() + "." + РасширениеБезТочки;
	
КонецФункции

Функция ЗаписатьXMLВФайл(ТекстДляЗаписи, ФайлДляЗаписи, Кодировка = "windows-1251", УдалятьBOM = Ложь) Экспорт
	
	Попытка
		ПотокЗаписи = Новый ЗаписьТекста(ФайлДляЗаписи, Кодировка, , , Символы.ПС);
		ПотокЗаписи.Записать(ТекстДляЗаписи);
		ПотокЗаписи.Закрыть();
		
		Если (нрег(Кодировка) = "utf-8" ИЛИ Кодировка = КодировкаТекста.UTF8) И УдалятьBOM Тогда
			// удалим из файла в кодировке "UTF-8" сигнатуру BOM
			ДанныеФайла = Новый ДвоичныеДанные(ФайлДляЗаписи);
			Данные64 = Base64Строка(ДанныеФайла);
			Данные64БезBOM = Сред(Данные64, 5);
			ДанныеФайлаБезBOM = Base64Значение(Данные64БезBOM);
			ДанныеФайлаБезBOM.Записать(ФайлДляЗаписи);
		КонецЕсли;
	Исключение
		ТекстСообщения = СтроковыеФункцииКлиентСервер.ПодставитьПараметрыВСтроку(
							НСтр("ru = 'Не удалось записать файл XML:%1'"), Символы.ПС + ИнформацияОбОшибке().Описание);
		ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
		Возврат Ложь;
	Конецпопытки;
	Возврат Истина;
	
КонецФункции

////
//// ФУНКЦИИ РАБОТЫ С XML
//////////////////////////////////////////////////////////////////////////////////

Функция ПредставлениеЦиклаОбмена(ЦиклОбмена) Экспорт
	
	Если ЦиклОбмена.Тип = Перечисления.ТипыЦикловОбмена.НалоговаяИлиБухгалтерскаяОтчетность Тогда
		ПредставлениеЦиклаОбмена = "" + ЦиклОбмена.ВидОтчета + " за " + ПредставлениеПериода(ЦиклОбмена.ДатаНачалаПериода, КонецДня(ЦиклОбмена.ДатаОкончанияПериода), "ФП=Истина") + " (" + РегламентированнаяОтчетность.ПредставлениеВидаДокумента(ЦиклОбмена.ВидДокумента) + ") от " + ЦиклОбмена.ДатаСоздания;
	ИначеЕсли ЦиклОбмена.Тип = Перечисления.ТипыЦикловОбмена.Заявление Тогда
		ПредставлениеЦиклаОбмена = "" + ЦиклОбмена.ВидОтчета + " за " + ПредставлениеПериода(ЦиклОбмена.ДатаНачалаПериода, КонецДня(ЦиклОбмена.ДатаОкончанияПериода), "ФП=Истина") + " от " + ЦиклОбмена.ДатаСоздания;
	ИначеЕсли ЦиклОбмена.Тип = Перечисления.ТипыЦикловОбмена.Документ Тогда
		ПредставлениеЦиклаОбмена = "Опись вход. документов от НО " + ЦиклОбмена.ВнешняяОрганизация + " от " + ЦиклОбмена.ДатаСоздания;
	ИначеЕсли ЦиклОбмена.Тип = Перечисления.ТипыЦикловОбмена.Форма2НДФЛ Тогда
		ПредставлениеЦиклаОбмена = "2-НДФЛ за " + ПредставлениеПериода(ЦиклОбмена.ДатаНачалаПериода, КонецДня(ЦиклОбмена.ДатаОкончанияПериода), "ФП=Истина") + " от " + ЦиклОбмена.ДатаСоздания;
	ИначеЕсли ЦиклОбмена.Тип = Перечисления.ТипыЦикловОбмена.НеформализованныеДокументыНалогоплательщика Тогда
		ПредставлениеЦиклаОбмена = "Исх. док. для НО " + ЦиклОбмена.ВнешняяОрганизация + " от " + ЦиклОбмена.ДатаСоздания;
	ИначеЕсли ЦиклОбмена.Тип = Перечисления.ТипыЦикловОбмена.НеформализованныеДокументыНалоговогоОргана Тогда
		ПредставлениеЦиклаОбмена = "Вход. док. от НО " + ЦиклОбмена.ВнешняяОрганизация + " от " + ЦиклОбмена.ДатаСоздания;
	ИначеЕсли ЦиклОбмена.Тип = Перечисления.ТипыЦикловОбмена.ПисьмоНО Тогда
		ПредставлениеЦиклаОбмена = "Вход. письмо от НО " + ЦиклОбмена.ВнешняяОрганизация + " от " + ЦиклОбмена.ДатаСоздания;
	ИначеЕсли ЦиклОбмена.Тип = Перечисления.ТипыЦикловОбмена.Рассылка Тогда
		ПредставлениеЦиклаОбмена = "Вход. рассылка от НО " + ЦиклОбмена.ВнешняяОрганизация + " от " + ЦиклОбмена.ДатаСоздания;
	ИначеЕсли ЦиклОбмена.Тип = Перечисления.ТипыЦикловОбмена.РассылкаГрупповая Тогда
		ПредставлениеЦиклаОбмена = "Вход. групповая рассылка от НО " + ЦиклОбмена.ВнешняяОрганизация + " от " + ЦиклОбмена.ДатаСоздания;
	ИначеЕсли ЦиклОбмена.Тип = Перечисления.ТипыЦикловОбмена.ЗапросыНаИнформационноеОбслуживание Тогда
		ПредставлениеЦиклаОбмена = "Запрос """ + ЦиклОбмена.ВидУслуги + """ (" + ЦиклОбмена.ФорматОтвета + ") от " + ЦиклОбмена.ДатаСоздания;
	ИначеЕсли ЦиклОбмена.Тип = Перечисления.ТипыЦикловОбмена.ОтчетностьПФР Тогда
		
		Если ЗначениеЗаполнено(ЦиклОбмена.ВидОтчета) Тогда
			Если ТипЗнч(ЦиклОбмена.ВидОтчета) = Тип("СправочникСсылка.РегламентированныеОтчеты") Тогда
				Источник = ЦиклОбмена.ВидОтчета.ИсточникОтчета;
			Иначе
				Источник = ЦиклОбмена.ВидОтчета.Источник;
			КонецЕсли;
			
			Если НачалоДня(ЦиклОбмена.ДатаНачалаПериода) = НачалоДня(ЦиклОбмена.ДатаОкончанияПериода) Тогда
				ПредставлениеЦиклаОбмена = "" + ЦиклОбмена.Предмет + ". Отправка от " + ЦиклОбмена.ДатаСоздания;
			ИначеЕсли Источник = "ПередачаСЗВ4вПФР" Тогда
				ПредставлениеЦиклаОбмена = "" + ЦиклОбмена.ВидОтчета + " и РСВ за " + ПредставлениеПериода(ЦиклОбмена.ДатаНачалаПериода, КонецДня(ЦиклОбмена.ДатаОкончанияПериода), "ФП=Истина") + " от " + ЦиклОбмена.ДатаСоздания;
			Иначе
				ПредставлениеЦиклаОбмена = "" + ЦиклОбмена.ВидОтчета + " за " + ПредставлениеПериода(ЦиклОбмена.ДатаНачалаПериода, КонецДня(ЦиклОбмена.ДатаОкончанияПериода), "ФП=Истина") + " от " + ЦиклОбмена.ДатаСоздания;	
			КонецЕсли;
		Иначе
			ПредставлениеЦиклаОбмена = "<не заполнено>" + " за " + ПредставлениеПериода(ЦиклОбмена.ДатаНачалаПериода, КонецДня(ЦиклОбмена.ДатаОкончанияПериода), "ФП=Истина") + " от " + ЦиклОбмена.ДатаСоздания;	
		КонецЕсли;
		
	ИначеЕсли ЦиклОбмена.Тип = Перечисления.ТипыЦикловОбмена.НеформализованнаяПерепискаПФРИсходящие Тогда
		ПредставлениеЦиклаОбмена = "Письмо для органа ПФР " + ЦиклОбмена.ВнешняяОрганизация + " от " + ЦиклОбмена.ДатаСоздания;
	ИначеЕсли ЦиклОбмена.Тип = Перечисления.ТипыЦикловОбмена.РассылкаПФР Тогда
		ПредставлениеЦиклаОбмена = "Рассылка органа ПФР " + ЦиклОбмена.ВнешняяОрганизация + " от " + ЦиклОбмена.ДатаСоздания;
	ИначеЕсли ЦиклОбмена.Тип = Перечисления.ТипыЦикловОбмена.НеформализованнаяПерепискаПФРВходящие Тогда
		ПредставлениеЦиклаОбмена = "Письмо от органа ПФР " + ЦиклОбмена.ВнешняяОрганизация + " от " + ЦиклОбмена.ДатаСоздания;
	ИначеЕсли ЦиклОбмена.Тип = Перечисления.ТипыЦикловОбмена.ИОС Тогда
		ПредставлениеЦиклаОбмена = "Запрос """ + ЦиклОбмена.ВидУслуги + """ от " + ЦиклОбмена.ДатаСоздания;
	ИначеЕсли ЦиклОбмена.Тип = Перечисления.ТипыЦикловОбмена.ИОН Тогда
		ПредставлениеЦиклаОбмена = "Запрос """ + ЦиклОбмена.ВидУслуги + """ (" + ЦиклОбмена.ФорматОтвета + ") от " + ЦиклОбмена.ДатаСоздания;
	ИначеЕсли ЦиклОбмена.Тип = Перечисления.ТипыЦикловОбмена.ОтчетностьФСГС Тогда
		Если НачалоДня(ЦиклОбмена.ДатаНачалаПериода) = НачалоДня(ЦиклОбмена.ДатаОкончанияПериода) Тогда
			ПредставлениеЦиклаОбмена = "" + ЦиклОбмена.Предмет + ". Отправка от " + ЦиклОбмена.ДатаСоздания;
		Иначе
			ПредставлениеЦиклаОбмена = "" + ЦиклОбмена.ВидОтчета + " за " + ПредставлениеПериода(ЦиклОбмена.ДатаНачалаПериода, КонецДня(ЦиклОбмена.ДатаОкончанияПериода), "ФП=Истина") + " от " + ЦиклОбмена.ДатаСоздания;
		КонецЕсли;
	ИначеЕсли ЦиклОбмена.Тип = Перечисления.ТипыЦикловОбмена.ПисьменноеОбращениеВФСГС Тогда
		ПредставлениеЦиклаОбмена = "Письмо для Росстата " + ЦиклОбмена.ВнешняяОрганизация + " от " + ЦиклОбмена.ДатаСоздания;
	ИначеЕсли ЦиклОбмена.Тип = Перечисления.ТипыЦикловОбмена.ИндивидуальноеИнформированиеФСГС Тогда
		ПредставлениеЦиклаОбмена = "Письмо от Росстата " + ЦиклОбмена.ВнешняяОрганизация + " от " + ЦиклОбмена.ДатаСоздания;
	ИначеЕсли ЦиклОбмена.Тип = Перечисления.ТипыЦикловОбмена.РассылкаФСГС Тогда
		ПредставлениеЦиклаОбмена = "Рассылка от Росстата " + ЦиклОбмена.ВнешняяОрганизация + " от " + ЦиклОбмена.ДатаСоздания;
	ИначеЕсли ЦиклОбмена.Тип = Перечисления.ТипыЦикловОбмена.РассылкаШаблоновФСГС Тогда
		ПредставлениеЦиклаОбмена = "Рассылка шаблонов от Росстата " + ЦиклОбмена.ВнешняяОрганизация + " от " + ЦиклОбмена.ДатаСоздания;
	Иначе
		ПредставлениеЦиклаОбмена = ЦиклОбмена.Ссылка;
	КонецЕсли;
	
	Возврат ПредставлениеЦиклаОбмена;
	
КонецФункции

////
//// ФУНКЦИИ ВЫГРУЗКИ АРХИВА ДОКУМЕНТООБОРОТА
//////////////////////////////////////////////////////////////////////////////////

Функция XMLЗначениеВнутр(Тип, Значение)
	
	Попытка
		Возврат XMLЗначение(Тип, Значение)
	Исключение
		Возврат Неопределено;
	КонецПопытки;
	
КонецФункции

//////////////////////////////////////////////////////////////////////////////////

Функция ФИОИндивидуальногоПредпринимателя(Организация, СведенияОбОрганизации = Неопределено) Экспорт
	
	Если СведенияОбОрганизации = Неопределено Тогда
		СведенияОбОрганизации = РегламентированнаяОтчетностьВызовСервера.ПолучитьСведенияОбОрганизации(Организация);
		Если СведенияОбОрганизации = Неопределено Тогда
			Возврат Неопределено;
		КонецЕсли;
	КонецЕсли;
	
	Если СведенияОбОрганизации.Свойство("ФамилияИП") И СведенияОбОрганизации.Свойство("ИмяИП") И СведенияОбОрганизации.Свойство("ОтчествоИП")
		И ЗначениеЗаполнено(СведенияОбОрганизации.ФамилияИП) И ЗначениеЗаполнено(СведенияОбОрганизации.ИмяИП)
		И СведенияОбОрганизации.ИмяИП <> СведенияОбОрганизации.ФИО Тогда
		
		Результат = Новый Структура("Фамилия, Имя, Отчество",
			СведенияОбОрганизации.ФамилияИП, СведенияОбОрганизации.ИмяИП, СведенияОбОрганизации.ОтчествоИП);
	
	Иначе
		Результат = РегламентированнаяОтчетность.РазложитьФИО(СведенияОбОрганизации.ФИО);
	КонецЕсли;
	
	Возврат Результат;
	
КонецФункции

//////////////////////////////////////////////////////////////////////////////////
//// ОБРАБОТЧИКИ СОБЫТИЙ ОБЪЕКТОВ

Процедура ОбработкаЗаполненияОбъекта(Объект, Основание) Экспорт
	
	ИмяМетаданныхОбъекта = Объект.Метаданные().Имя;
	Если ИмяМетаданныхОбъекта = "НеформализованныйДокументНалогоплательщика" Тогда
		//закомментирован вызов ОбработкаЗаполненияНеформализованногоДокументаНалогоплательщика
	ИначеЕсли ИмяМетаданныхОбъекта = "ТранспортноеСообщение" Тогда
		ОбработкаЗаполненияТранспортногоСообщения(Объект, Основание);
	ИначеЕсли ИмяМетаданныхОбъекта = "ПерепискаСКонтролирующимиОрганами" Тогда
		//закомментирован вызов ОбработкаЗаполненияПисьма
	ИначеЕсли ИмяМетаданныхОбъекта = "УчетныеЗаписиДокументооборота" Тогда
		ОбработкаЗаполненияУчетнойЗаписиДокументооборота(Объект, Основание);
	ИначеЕсли ИмяМетаданныхОбъекта = "ОписиИсходящихДокументовВНалоговыеОрганы" Тогда
		ОбработкаЗаполненияОписиИсходящихДокументов(Объект, Основание);
	ИначеЕсли ИмяМетаданныхОбъекта = "СканированныеДокументыДляПередачиВЭлектронномВиде" Тогда
		ОбработкаЗаполненияСканированногоДокумента(Объект, Основание);
	КонецЕсли;
	
КонецПроцедуры

Процедура ОбработкаЗаполненияСканированногоДокумента(Объект, ДанныеЗаполнения)
	
	Если ДанныеЗаполнения = Неопределено Тогда
		Возврат;
	КонецЕсли;
	
	ЗаполнитьЗначенияСвойств(Объект, ДанныеЗаполнения);
	
КонецПроцедуры

Процедура ОбработкаЗаполненияУчетнойЗаписиДокументооборота(Объект, Основание)
	
	Если НЕ ЗначениеЗаполнено(Объект.ПортPOP3) Тогда
		Объект.ПортPOP3 = 110;
	КонецЕсли;
	
	Если НЕ ЗначениеЗаполнено(Объект.ПортSMTP) Тогда
		Объект.ПортSMTP = 25;
	КонецЕсли;
		
	Если НЕ ЗначениеЗаполнено(Объект.РежимАвтонастройки) Тогда
		Объект.РежимАвтонастройки = Перечисления.РежимыАвтонастройкиУчетнойЗаписиНалогоплательщика.Включена;
		Объект.ИспользоватьСервисОнлайнПроверкиОтчетов = Истина;
	КонецЕсли;
	
КонецПроцедуры

Процедура ПередЗаписьюОбъекта(Объект, Отказ, РежимЗаписи = Неопределено, РежимПроведения = Неопределено, Замещение = Неопределено) Экспорт
	
	ИмяМетаданныхОбъекта = Объект.Метаданные().Имя;
	Если ИмяМетаданныхОбъекта = "ЦиклыОбмена" Тогда
		ПередЗаписьюЦиклаОбмена(Объект, Отказ);
	ИначеЕсли ИмяМетаданныхОбъекта = "УчетныеЗаписиДокументооборота" Тогда
		ПередЗаписьюУчетнойЗаписиНалогоплательщика(Объект, Отказ);
	ИначеЕсли ИмяМетаданныхОбъекта = "СерверыДокументооборота" Тогда
		ПередЗаписьюПочтовогоСервера(Объект, Отказ);
	ИначеЕсли ИмяМетаданныхОбъекта = "ТранспортноеСообщение" Тогда
		ПередЗаписьюТранспортногоСообщения(Объект, Отказ, РежимЗаписи, РежимПроведения);
	ИначеЕсли ИмяМетаданныхОбъекта = "ПерепискаСКонтролирующимиОрганами" Тогда
		ПередЗаписьюПерепискиСКонтролирующимиОрганами(Объект, Отказ);
	ИначеЕсли ИмяМетаданныхОбъекта = "СканированныеДокументыДляПередачиВЭлектронномВиде" Тогда
		ПередЗаписьюСканированныеДокументыДляПередачиВЭлектронномВиде(Объект, Отказ);
	ИначеЕсли ИмяМетаданныхОбъекта = "ОписиВходящихДокументовИзНалоговыхОрганов" Тогда
		ПередЗаписьюОписиВходящихДокументов(Объект, Отказ);
	ИначеЕсли ИмяМетаданныхОбъекта = "ОписиИсходящихДокументовВНалоговыеОрганы" Тогда
		ПередЗаписьюОписиИсходящихДокументов(Объект, Отказ);
	ИначеЕсли ИмяМетаданныхОбъекта = "ЗапросНаИнформационноеОбслуживаниеСтрахователя" Тогда
		ПередЗаписьюЗапросаНаИнформационноеОбслуживаниеСтрахователя(Объект, Отказ, РежимЗаписи, РежимПроведения);
	ИначеЕсли ИмяМетаданныхОбъекта = "ОтправкиФСС" Тогда
		ПередЗаписьюОтправкиФСС(Объект, Отказ);
	ИначеЕсли ИмяМетаданныхОбъекта = "ОтправкиФСРАР" Тогда
		ПередЗаписьюОтправкиФСРАР(Объект, Отказ);
	ИначеЕсли ИмяМетаданныхОбъекта = "ОтправкиРПН" Тогда
		ПередЗаписьюОтправкиРПН(Объект, Отказ);
	ИначеЕсли ИмяМетаданныхОбъекта = "ОтправкиФТС" Тогда
		ПередЗаписьюОтправкиФТС(Объект, Отказ);
	ИначеЕсли ИмяМетаданныхОбъекта = "ОтправкиБанкРоссии" Тогда
		ПередЗаписьюОтправкиВБанкРоссии(Объект, Отказ);
	ИначеЕсли ИмяМетаданныхОбъекта = "ОтправкиМинобороны" Тогда
		ПередЗаписьюОтправкиВМинобороны(Объект, Отказ);
	ИначеЕсли ИмяМетаданныхОбъекта = "ЗаявлениеАбонентаСпецоператораСвязи" Тогда
		ПередЗаписьюЗаявлениеАбонентаСпецоператораСвязи(Объект, Отказ);
	КонецЕсли;
	
КонецПроцедуры

Процедура ПриЗаписиЦиклаОбмена(Объект, Отказ)
	
	Если Объект.ИзмененаПометкаУдаления Тогда
		СообщенияЦиклаОбмена = ПолучитьСообщенияЦиклаОбмена(Объект.Ссылка);
		Для Каждого СообщениеЦиклаОбмена Из СообщенияЦиклаОбмена Цикл
			ОбъектСообщениеЦиклаОбмена = СообщениеЦиклаОбмена.Ссылка.ПолучитьОбъект();
			ОбъектСообщениеЦиклаОбмена.УстановитьПометкуУдаления(Объект.ПометкаУдаления);
		КонецЦикла;
		ОбновитьСтатусыСвязанногоОбъектаПриНеобходимости(Объект.Ссылка);
	КонецЕсли;
	
	Если Объект.Тип = Перечисления.ТипыЦикловОбмена.Документ Тогда 
		ОбновитьСтатусыСвязанногоОбъектаПриНеобходимости(Объект.Ссылка);
	КонецЕсли;
	
	Предмет = Объект.Предмет;
	Если ЗначениеЗаполнено(Предмет) Тогда
		ЭлектронныйДокументооборотСКонтролирующимиОрганами.ЗаписьОбъектовРегламентированнойОтчетности(Предмет, Отказ);
	КонецЕсли;	
	
КонецПроцедуры

Функция РассчитатьСтатусЦиклаОбмена(ЦиклОбмена) Экспорт
	
	ТекСтатус = Перечисления.СтатусыОтправки.ПустаяСсылка();
	Если ТипЗнч(ЦиклОбмена) = Тип("СправочникОбъект.ЦиклыОбмена") И 
		(ЦиклОбмена.ЭтоНовый() ИЛИ (ЦиклОбмена.Тип <> Перечисления.ТипыЦикловОбмена.НалоговаяИлиБухгалтерскаяОтчетность И ЦиклОбмена.Тип <> Перечисления.ТипыЦикловОбмена.Заявление)) Тогда
		Возврат ТекСтатус;
	КонецЕсли;
	
	Если ЦиклОбмена.Тип = Перечисления.ТипыЦикловОбмена.НалоговаяИлиБухгалтерскаяОтчетность Тогда
		
		Если ЦиклОбмена.ФорматДокументооборота = Перечисления.ФорматыДокументооборотаСФНС.Приказ534 Тогда
			
			// получаем сообщения цикла обмена
			СообщенияЦиклаОбмена = ПолучитьСообщенияЦиклаОбмена(ЦиклОбмена.Ссылка);
			
			// анализируем, отправлено ли первичное
			ПервичноеСообщениеСодержащееОтчетность = СообщенияЦиклаОбмена.Найти(Перечисления.ТипыТранспортныхСообщений.ДекларацияНП, "Тип");
			Если ПервичноеСообщениеСодержащееОтчетность <> Неопределено Тогда
				Если ПервичноеСообщениеСодержащееОтчетность.Статус = Перечисления.СтатусыПисем.Отправленное Тогда
					ТекСтатус = Перечисления.СтатусыОтправки.Отправлен;
				Иначе
					Возврат ТекСтатус;
				КонецЕсли;
			Иначе
				Возврат ТекСтатус;
			КонецЕсли;
			
			// анализируем, получено ли подтверждение отправки
			ПодтверждениеОтправки = СообщенияЦиклаОбмена.Найти(Перечисления.ТипыТранспортныхСообщений.ПодтверждениеДекларацияНО, "Тип");
			Если ПодтверждениеОтправки = Неопределено Тогда
				Возврат ТекСтатус;
			КонецЕсли;
			
			// анализируем, получено ли извещение о получении декларации
			ПодтверждениеДоставки = СообщенияЦиклаОбмена.Найти(Перечисления.ТипыТранспортныхСообщений.ИзвещениеДекларацияНО, "Тип");
			Если ПодтверждениеДоставки = Неопределено Тогда
				Возврат ТекСтатус;
			Иначе
				ТекСтатус = Перечисления.СтатусыОтправки.Доставлен;
			КонецЕсли;
			
			// анализируем протоколы приема
			// если их несколько, то интересует последний
			ПротоколыПриема = СообщенияЦиклаОбмена.НайтиСтроки(Новый Структура("Тип", Перечисления.ТипыТранспортныхСообщений.РезультатПриемаДекларацияНО));
			ЧислоПротоколов = ПротоколыПриема.Количество();
			Если ЧислоПротоколов = 0 Тогда
				Возврат ТекСтатус;
			ИначеЕсли ЧислоПротоколов = 1 Тогда
				ПротоколПриема = ПротоколыПриема[0];
				Если ПротоколПриема.Ссылка.ПротоколСОшибкой Тогда
					Возврат Перечисления.СтатусыОтправки.НеПринят;
				КонецЕсли;
			Иначе
				
				// если протоколов несколько, то ориентируемся на наиболее свежий из них
				НаиболееПозднийПротокол = Неопределено;
				ДатаНаиболееПозднегоПротокола = '00010101';
				Для Каждого ПротоколПриема Из ПротоколыПриема Цикл
					ДатаПротоколаПриема = ПротоколПриема.Дата;
					Если ДатаПротоколаПриема = Неопределено Тогда
						ДатаПротоколаПриема = '00010101';
					КонецЕсли;
					Если ДатаПротоколаПриема >= ДатаНаиболееПозднегоПротокола Тогда
						ДатаНаиболееПозднегоПротокола = ДатаПротоколаПриема;
						НаиболееПозднийПротокол = ПротоколПриема;
					КонецЕсли;
				КонецЦикла;
				
				Если НаиболееПозднийПротокол.Ссылка.ПротоколСОшибкой Тогда
					Возврат Перечисления.СтатусыОтправки.НеПринят;
				КонецЕсли;
				
			КонецЕсли;
			
			// анализируем протоколы обработки
			// если их несколько, то анализируем последний
			ПротоколыОбработки = СообщенияЦиклаОбмена.НайтиСтроки(Новый Структура("Тип", Перечисления.ТипыТранспортныхСообщений.РезультатОбработкиДекларацияНО));
			ЧислоПротоколов = ПротоколыОбработки.Количество();
			Если ЧислоПротоколов = 0 Тогда
				Возврат ТекСтатус;
			ИначеЕсли ЧислоПротоколов = 1 Тогда
				ПротоколОбработки = ПротоколыОбработки[0];
				Если ПротоколОбработки.Ссылка.ПротоколСОшибкой Тогда
					Возврат Перечисления.СтатусыОтправки.НеПринят;
				КонецЕсли;
			Иначе
				
				// если протоколов несколько, то ориентируемся на наиболее свежий из них
				НаиболееПозднийПротокол = Неопределено;
				ДатаНаиболееПозднегоПротокола = '00010101';
				Для Каждого ПротоколОбработки Из ПротоколыОбработки Цикл
					ДатаПротоколаОбработки = ПротоколОбработки.Дата;
					Если ДатаПротоколаОбработки = Неопределено Тогда
						ДатаПротоколаОбработки = '00010101';
					КонецЕсли;
					Если ДатаПротоколаОбработки >= ДатаНаиболееПозднегоПротокола Тогда
						ДатаНаиболееПозднегоПротокола = ДатаПротоколаОбработки;
						НаиболееПозднийПротокол = ПротоколОбработки;
					КонецЕсли;
				КонецЦикла;
				
				Если НаиболееПозднийПротокол.Ссылка.ПротоколСОшибкой Тогда
					Возврат Перечисления.СтатусыОтправки.НеПринят;
				КонецЕсли;
				
			КонецЕсли;
			
			// если все предусмотренные регламентом сообщения отправлены/приняты
			// и они без ошибок, то отчет считается сданным
			Возврат Перечисления.СтатусыОтправки.Сдан;
			
		КонецЕсли;
		
	ИначеЕсли ЦиклОбмена.Тип = Перечисления.ТипыЦикловОбмена.Заявление Тогда
		
		
		// получаем сообщения цикла обмена
		СообщенияЦиклаОбмена = ПолучитьСообщенияЦиклаОбмена(ЦиклОбмена.Ссылка);
		
		// анализируем, отправлено ли первичное
		ПервичноеСообщениеСодержащееОтчетность = СообщенияЦиклаОбмена.Найти(Перечисления.ТипыТранспортныхСообщений.ЗаявлениеНП, "Тип");
		Если ПервичноеСообщениеСодержащееОтчетность <> Неопределено Тогда
			Если ПервичноеСообщениеСодержащееОтчетность.Статус = Перечисления.СтатусыПисем.Отправленное Тогда
				ТекСтатус = Перечисления.СтатусыОтправки.Отправлен;
			Иначе
				Возврат ТекСтатус;
			КонецЕсли;
		Иначе
			Возврат ТекСтатус;
		КонецЕсли;
		
		// анализируем, получено ли подтверждение отправки
		ПодтверждениеОтправки = СообщенияЦиклаОбмена.Найти(Перечисления.ТипыТранспортныхСообщений.ПодтверждениеЗаявлениеНО, "Тип");
		Если ПодтверждениеОтправки = Неопределено Тогда
			Возврат ТекСтатус;
		КонецЕсли;
		
		// анализируем, получено ли извещение о получении декларации
		ПодтверждениеДоставки = СообщенияЦиклаОбмена.Найти(Перечисления.ТипыТранспортныхСообщений.ИзвещениеЗаявлениеНО, "Тип");
		Если ПодтверждениеДоставки = Неопределено Тогда
			Возврат ТекСтатус;
		Иначе
			ТекСтатус = Перечисления.СтатусыОтправки.Доставлен;
		КонецЕсли;
		
		// анализируем протоколы приема
		// если их несколько, то интересует последний
		ПротоколыПриема = СообщенияЦиклаОбмена.НайтиСтроки(Новый Структура("Тип", Перечисления.ТипыТранспортныхСообщений.РезультатПриемаЗаявлениеНО));
		ЧислоПротоколов = ПротоколыПриема.Количество();
		Если ЧислоПротоколов = 0 Тогда
			Возврат ТекСтатус;
		ИначеЕсли ЧислоПротоколов = 1 Тогда
			ПротоколПриема = ПротоколыПриема[0];
			Если ПротоколПриема.Ссылка.ПротоколСОшибкой Тогда
				Возврат Перечисления.СтатусыОтправки.НеПринят;
			КонецЕсли;
		Иначе
			
			// если протоколов несколько, то ориентируемся на наиболее свежий из них
			НаиболееПозднийПротокол = Неопределено;
			ДатаНаиболееПозднегоПротокола = '00010101';
			Для Каждого ПротоколПриема Из ПротоколыПриема Цикл
				ДатаПротоколаПриема = ПротоколПриема.Дата;
				Если ДатаПротоколаПриема = Неопределено Тогда
					ДатаПротоколаПриема = '00010101';
				КонецЕсли;
				Если ДатаПротоколаПриема >= ДатаНаиболееПозднегоПротокола Тогда
					ДатаНаиболееПозднегоПротокола = ДатаПротоколаПриема;
					НаиболееПозднийПротокол = ПротоколПриема;
				КонецЕсли;
			КонецЦикла;
			
			Если НаиболееПозднийПротокол.Ссылка.ПротоколСОшибкой Тогда
				Возврат Перечисления.СтатусыОтправки.НеПринят;
			КонецЕсли;
			
		КонецЕсли;
		
		// анализируем протоколы обработки образца 2013
		// если их несколько, то анализируем последний
		ПротоколыОбработки = СообщенияЦиклаОбмена.НайтиСтроки(Новый Структура("Тип", Перечисления.ТипыТранспортныхСообщений.РезультатОбработкиЗаявлениеНО));
		ЧислоПротоколов = ПротоколыОбработки.Количество();
		Если ЧислоПротоколов = 0 Тогда
			//пропускаем анализ протокола обработки образца 2013, статус не возвращаем, так как возможно наличие протокола обработки старого образца
		ИначеЕсли ЧислоПротоколов = 1 Тогда
			ПротоколОбработки = ПротоколыОбработки[0];
			Если ПротоколОбработки.Ссылка.ПротоколСОшибкой Тогда
				Возврат Перечисления.СтатусыОтправки.НеПринят;
			Иначе
				Возврат Перечисления.СтатусыОтправки.Сдан;
			КонецЕсли;
		Иначе
			
			// если протоколов несколько, то ориентируемся на наиболее свежий из них
			НаиболееПозднийПротокол = Неопределено;
			ДатаНаиболееПозднегоПротокола = '00010101';
			Для Каждого ПротоколОбработки Из ПротоколыОбработки Цикл
				ДатаПротоколаОбработки = ПротоколОбработки.Дата;
				Если ДатаПротоколаОбработки = Неопределено Тогда
					ДатаПротоколаОбработки = '00010101';
				КонецЕсли;
				Если ДатаПротоколаОбработки >= ДатаНаиболееПозднегоПротокола Тогда
					ДатаНаиболееПозднегоПротокола = ДатаПротоколаОбработки;
					НаиболееПозднийПротокол = ПротоколОбработки;
				КонецЕсли;
			КонецЦикла;
			
			Если НаиболееПозднийПротокол.Ссылка.ПротоколСОшибкой Тогда
				Возврат Перечисления.СтатусыОтправки.НеПринят;
			Иначе
				Возврат Перечисления.СтатусыОтправки.Сдан;
			КонецЕсли;
			
		КонецЕсли;
		
		// анализируем протоколы обработки
		// если их несколько, то анализируем последний
		ПротоколыОбработки = СообщенияЦиклаОбмена.НайтиСтроки(Новый Структура("Тип", Перечисления.ТипыТранспортныхСообщений.РезультатОбработкиЗаявлениеРФНО));
		ЧислоПротоколов = ПротоколыОбработки.Количество();
		Если ЧислоПротоколов = 0 Тогда
			Возврат ТекСтатус;
		ИначеЕсли ЧислоПротоколов = 1 Тогда
			ПротоколОбработки = ПротоколыОбработки[0];
			Если ПротоколОбработки.Ссылка.ПротоколСОшибкой Тогда
				Возврат Перечисления.СтатусыОтправки.НеПринят;
			Иначе
				ТекСтатус = Перечисления.СтатусыОтправки.Сдан;
			КонецЕсли;
		Иначе
			
			// если протоколов несколько, то ориентируемся на наиболее свежий из них
			НаиболееПозднийПротокол = Неопределено;
			ДатаНаиболееПозднегоПротокола = '00010101';
			Для Каждого ПротоколОбработки Из ПротоколыОбработки Цикл
				ДатаПротоколаОбработки = ПротоколОбработки.Дата;
				Если ДатаПротоколаОбработки = Неопределено Тогда
					ДатаПротоколаОбработки = '00010101';
				КонецЕсли;
				Если ДатаПротоколаОбработки >= ДатаНаиболееПозднегоПротокола Тогда
					ДатаНаиболееПозднегоПротокола = ДатаПротоколаОбработки;
					НаиболееПозднийПротокол = ПротоколОбработки;
				КонецЕсли;
			КонецЦикла;
			
			Если НаиболееПозднийПротокол.Ссылка.ПротоколСОшибкой Тогда
				Возврат Перечисления.СтатусыОтправки.НеПринят;
			Иначе
				ТекСтатус = Перечисления.СтатусыОтправки.Сдан;
			КонецЕсли;
			
		КонецЕсли;
		
		// анализируем наличие сообщений об отзыве и несоответствиях
		СообщениеОбОтзыве = СообщенияЦиклаОбмена.Найти(Перечисления.ТипыТранспортныхСообщений.СообщениеОбОтзывеЗаявлениеРФНО, "Тип");
		РезультатОбработкиЗаявлениеТСНО = СообщенияЦиклаОбмена.Найти(Перечисления.ТипыТранспортныхСообщений.РезультатОбработкиЗаявлениеТСНО, "Тип");
		
		Если СообщениеОбОтзыве = Неопределено И РезультатОбработкиЗаявлениеТСНО = Неопределено Тогда
			Возврат ТекСтатус;
		Иначе
			Возврат Перечисления.СтатусыОтправки.НеПринят;
		КонецЕсли;
		
		// если все предусмотренные регламентом сообщения отправлены/приняты
		// и они без ошибок, то отчет считается сданным
		Возврат Перечисления.СтатусыОтправки.Сдан;
		
	ИначеЕсли ЦиклОбмена.Тип = Перечисления.ТипыЦикловОбмена.Документ Тогда
		
		ТекСтатус = Перечисления.СтатусыОтправки.Доставлен;
		
		// получаем сообщения цикла обмена
		СообщенияЦиклаОбмена = ПолучитьСообщенияЦиклаОбмена(ЦиклОбмена.Ссылка);
		
		// анализируем результаты приема
		// если их несколько, то интересует последний
		РезультатыПриема = СообщенияЦиклаОбмена.НайтиСтроки(Новый Структура("Тип", Перечисления.ТипыТранспортныхСообщений.РезультатПриемаДокументНП));
		ЧислоСообщений = РезультатыПриема.Количество();
		Если ЧислоСообщений = 0 Тогда
			Возврат ТекСтатус;
		ИначеЕсли ЧислоСообщений = 1 Тогда
			РезультатПриема = РезультатыПриема[0];
		Иначе
			
			// если протоколов несколько, то ориентируемся на наиболее свежий из них
			НаиболееПозднийРезультат = Неопределено;
			ДатаНаиболееПозднегоРезультата = '00010101';
			Для Каждого РезультатПриема Из РезультатыПриема Цикл
				ДатаРезультатаПриема = РезультатПриема.Дата;
				Если ДатаРезультатаПриема = Неопределено Тогда
					ДатаРезультатаПриема = '00010101';
				КонецЕсли;
				Если ДатаРезультатаПриема >= ДатаНаиболееПозднегоРезультата Тогда
					ДатаНаиболееПозднегоРезультата = ДатаРезультатаПриема;
					НаиболееПозднийРезультат = РезультатПриема;
				КонецЕсли;
			КонецЦикла;
			
			РезультатПриема = НаиболееПозднийРезультат;
			
		КонецЕсли;
		
		Если РезультатПриема.Ссылка.Статус = Перечисления.СтатусыПисем.Отправленное Тогда
			Если РезультатПриема.Ссылка.ПротоколСОшибкой Тогда
				Возврат Перечисления.СтатусыОтправки.НеПринят;
			Иначе 
				Возврат Перечисления.СтатусыОтправки.Сдан;
			КонецЕсли;
		КонецЕсли;
		
	ИначеЕсли ЦиклОбмена.Тип = Перечисления.ТипыЦикловОбмена.Форма2НДФЛ Тогда
		
		// получаем сообщения цикла обмена
		СообщенияЦиклаОбмена = ПолучитьСообщенияЦиклаОбмена(ЦиклОбмена.Ссылка);
		
		// анализируем, отправлено ли первичное
		ПервичноеСообщениеСодержащееОтчетность = СообщенияЦиклаОбмена.Найти(Перечисления.ТипыТранспортныхСообщений.Форма2НДФЛНП, "Тип");
		Если ПервичноеСообщениеСодержащееОтчетность <> Неопределено Тогда
			Если ПервичноеСообщениеСодержащееОтчетность.Статус = Перечисления.СтатусыПисем.Отправленное Тогда
				ТекСтатус = Перечисления.СтатусыОтправки.Отправлен;
			Иначе
				Возврат ТекСтатус;
			КонецЕсли;
		Иначе
			Возврат ТекСтатус;
		КонецЕсли;
		
		// анализируем, получено ли подтверждение отправки
		ПодтверждениеОтправки = СообщенияЦиклаОбмена.Найти(Перечисления.ТипыТранспортныхСообщений.ПодтверждениеФорма2НДФЛНО, "Тип");
		Если ПодтверждениеОтправки = Неопределено Тогда
			Возврат ТекСтатус;
		КонецЕсли;
		
		// анализируем, получено ли извещение о получении декларации
		ПодтверждениеДоставки = СообщенияЦиклаОбмена.Найти(Перечисления.ТипыТранспортныхСообщений.ИзвещениеФорма2НДФЛНО, "Тип");
		Если ПодтверждениеДоставки = Неопределено Тогда
			Возврат ТекСтатус;
		Иначе
			ТекСтатус = Перечисления.СтатусыОтправки.Доставлен;
		КонецЕсли;
		
		// анализируем протоколы приема
		// если их несколько, то интересует последний
		ПротоколыПриема = СообщенияЦиклаОбмена.НайтиСтроки(Новый Структура("Тип", Перечисления.ТипыТранспортныхСообщений.РезультатПриемаФорма2НДФЛНО));
		ЧислоПротоколов = ПротоколыПриема.Количество();
		Если ЧислоПротоколов = 0 Тогда
			Возврат ТекСтатус;
		ИначеЕсли ЧислоПротоколов = 1 Тогда
			
			ПротоколПриема = ПротоколыПриема[0];
			
			ОтчетСданЧастично 		= Отчет2НДФЛСданЧастично(ПротоколПриема.Ссылка);
			ПротоколСОшибкой 		= ПротоколПриема.ПротоколСОшибкой;

			Если ОтчетСданЧастично Тогда
				Возврат Перечисления.СтатусыОтправки.ПринятЕстьОшибки;
			ИначеЕсли ПротоколСОшибкой Тогда
				Возврат Перечисления.СтатусыОтправки.НеПринят;
			КонецЕсли;
		Иначе
			
			// если протоколов несколько, то ориентируемся на наиболее свежий из них
			НаиболееПозднийПротокол = Неопределено;
			ДатаНаиболееПозднегоПротокола = '00010101';
			Для Каждого ПротоколПриема Из ПротоколыПриема Цикл
				ДатаПротоколаПриема = ПротоколПриема.Дата;
				Если ДатаПротоколаПриема = Неопределено Тогда
					ДатаПротоколаПриема = '00010101';
				КонецЕсли;
				Если ДатаПротоколаПриема >= ДатаНаиболееПозднегоПротокола Тогда
					ДатаНаиболееПозднегоПротокола = ДатаПротоколаПриема;
					НаиболееПозднийПротокол = ПротоколПриема;
				КонецЕсли;
			КонецЦикла;
			
			ОтчетСданЧастично 		= Отчет2НДФЛСданЧастично(НаиболееПозднийПротокол.Ссылка);
			ПротоколСОшибкой 		= НаиболееПозднийПротокол.ПротоколСОшибкой;
			
 			Если ОтчетСданЧастично Тогда
				Возврат Перечисления.СтатусыОтправки.ПринятЕстьОшибки;
			ИначеЕсли ПротоколСОшибкой Тогда
				Возврат Перечисления.СтатусыОтправки.НеПринят;
			КонецЕсли;
			
		КонецЕсли;
		
		// если все предусмотренные регламентом сообщения отправлены/приняты
		// и они без ошибок, то отчет считается сданным
		Возврат Перечисления.СтатусыОтправки.Сдан;
			
	ИначеЕсли ЦиклОбмена.Тип = Перечисления.ТипыЦикловОбмена.ОтчетностьПФР Тогда
		
		СообщенияЦиклаОбмена = ПолучитьСообщенияЦиклаОбмена(ЦиклОбмена.Ссылка);
		
		ПервичноеСообщениеСодержащееОтчетность = СообщенияЦиклаОбмена.Найти(Перечисления.ТипыТранспортныхСообщений.ПервичноеСообщениеСодержащееОтчетностьПФР, "Тип");
		Если ПервичноеСообщениеСодержащееОтчетность <> Неопределено Тогда
			Если ПервичноеСообщениеСодержащееОтчетность.Статус = Перечисления.СтатусыПисем.Отправленное Тогда
				ТекСтатус = Перечисления.СтатусыОтправки.Отправлен;
			Иначе
				Возврат ТекСтатус;
			КонецЕсли;
		Иначе
			Возврат ТекСтатус;
		КонецЕсли;
		
		ПодтвержденияДоставки = СообщенияЦиклаОбмена.НайтиСтроки(Новый Структура("Тип", Перечисления.ТипыТранспортныхСообщений.ПодтверждениеПолученияОтчетностиПФР));
		ЧислоПодтвержденийДоставки = ПодтвержденияДоставки.Количество();
		Если ЧислоПодтвержденийДоставки = 0 Тогда
			Возврат ТекСтатус;
		ИначеЕсли ЧислоПодтвержденийДоставки = 1 Тогда
			ПодтверждениеДоставки = ПодтвержденияДоставки[0];
			Если ПодтверждениеДоставки.Ссылка.ПротоколСОшибкой Тогда
				Возврат Перечисления.СтатусыОтправки.НеПринят;
			Иначе
				ТекСтатус = Перечисления.СтатусыОтправки.Доставлен;
			КонецЕсли;
		Иначе
			
			// если протоколов несколько, то ориентируемся на наиболее свежий из них
			НаиболееПоздниееПодтверждение = Неопределено;
			ДатаНаиболееПозднегоПодтверждения = '00010101';
			Для Каждого ПодтверждениеДоставки Из ПодтвержденияДоставки Цикл
				ДатаПодтверждения = ПодтверждениеДоставки.Дата;
				Если ДатаПодтверждения = Неопределено Тогда
					ДатаПодтверждения = '00010101';
				КонецЕсли;
				Если ДатаПодтверждения >= ДатаНаиболееПозднегоПодтверждения Тогда
					ДатаНаиболееПозднегоПодтверждения = ДатаПодтверждения;
					НаиболееПоздниееПодтверждение = ПодтверждениеДоставки;
				КонецЕсли;
			КонецЦикла;
			
			Если НаиболееПоздниееПодтверждение.Ссылка.ПротоколСОшибкой Тогда
				Возврат Перечисления.СтатусыОтправки.НеПринят;
			Иначе
				ТекСтатус = Перечисления.СтатусыОтправки.Доставлен;
			КонецЕсли;
			
		КонецЕсли;
		
		ПротоколыПроверки = СообщенияЦиклаОбмена.НайтиСтроки(Новый Структура("Тип", Перечисления.ТипыТранспортныхСообщений.ПротоколПФР));
		ЧислоПротоколов = ПротоколыПроверки.Количество();
		Если ЧислоПротоколов = 0 Тогда
			Возврат ТекСтатус;
			
		ИначеЕсли ЧислоПротоколов = 1 Тогда
			ПротоколПроверки = ПротоколыПроверки[0];
			
			АнализПриложенияПротокола = ?(ЦиклОбмена.ВидОтчета = Справочники.ВидыОтправляемыхДокументов.СведенияОЗастрахованныхЛицахСЗВ_М
				ИЛИ ЦиклОбмена.ВидОтчета = Справочники.ВидыОтправляемыхДокументов.СведенияОСтраховомСтажеЗастрахованныхЛицСЗВ_СТАЖ
				ИЛИ ЦиклОбмена.ВидОтчета = Справочники.ВидыОтправляемыхДокументов.ДанныеОКорректировкеСведенийЗастрахованныхЛицСЗВ_КОРР
				ИЛИ ЦиклОбмена.ВидОтчета = Справочники.ВидыОтправляемыхДокументов.СведенияОЗаработкеСтажеЗастрахованныхЛицСЗВ_ИСХ
				ИЛИ ЦиклОбмена.ВидОтчета = Справочники.ВидыОтправляемыхДокументов.ОписьОДВ_1
				ИЛИ ЦиклОбмена.ФорматДокументооборота = Перечисления.ФорматыДокументооборотаСПФР.ЭДОК,
				ОтчетПФРСданЧастично(ПротоколПроверки.Ссылка,, ЦиклОбмена.ФорматДокументооборота), Неопределено);
			ПротоколСОшибкой = ПротоколПроверки.ПротоколСОшибкой; // ошибка означает не сдан или сдан частично (при поддержке)
			
			Если АнализПриложенияПротокола <> Неопределено И НЕ АнализПриложенияПротокола.ЕстьПриложение
				И (НЕ АнализПриложенияПротокола.ЕстьВложения
				ИЛИ ЦиклОбмена.ФорматДокументооборота = Перечисления.ФорматыДокументооборотаСПФР.ЭДОК) Тогда
				Возврат ТекСтатус; // не расшифрован, но поддерживает частичную сдачу, определяемую по приложению / протоколу ЭДОК
			ИначеЕсли АнализПриложенияПротокола <> Неопределено И АнализПриложенияПротокола.СданЧастично Тогда
				Возврат Перечисления.СтатусыОтправки.ПринятЕстьОшибки;
			ИначеЕсли ПротоколСОшибкой ИЛИ АнализПриложенияПротокола <> Неопределено И АнализПриложенияПротокола.НеСдан Тогда
				Возврат Перечисления.СтатусыОтправки.НеПринят;
			КонецЕсли;
			
		Иначе
			// если протоколов несколько, то ориентируемся на наиболее свежий из них
			НаиболееПозднийПротокол = Неопределено;
			ДатаНаиболееПозднегоПротокола = '00010101';
			Для Каждого ПротоколПроверки Из ПротоколыПроверки Цикл
				ДатаПротоколаПроверки = ПротоколПроверки.Дата;
				Если ДатаПротоколаПроверки = Неопределено Тогда
					ДатаПротоколаПроверки = '00010101';
				КонецЕсли;
				Если ДатаПротоколаПроверки >= ДатаНаиболееПозднегоПротокола Тогда
					ДатаНаиболееПозднегоПротокола = ДатаПротоколаПроверки;
					НаиболееПозднийПротокол = ПротоколПроверки;
				КонецЕсли;
			КонецЦикла;
			
			АнализПриложенияПротокола = ?(ЦиклОбмена.ВидОтчета = Справочники.ВидыОтправляемыхДокументов.СведенияОЗастрахованныхЛицахСЗВ_М
				ИЛИ ЦиклОбмена.ВидОтчета = Справочники.ВидыОтправляемыхДокументов.СведенияОСтраховомСтажеЗастрахованныхЛицСЗВ_СТАЖ
				ИЛИ ЦиклОбмена.ВидОтчета = Справочники.ВидыОтправляемыхДокументов.ДанныеОКорректировкеСведенийЗастрахованныхЛицСЗВ_КОРР
				ИЛИ ЦиклОбмена.ВидОтчета = Справочники.ВидыОтправляемыхДокументов.СведенияОЗаработкеСтажеЗастрахованныхЛицСЗВ_ИСХ
				ИЛИ ЦиклОбмена.ВидОтчета = Справочники.ВидыОтправляемыхДокументов.ОписьОДВ_1
				ИЛИ ЦиклОбмена.ФорматДокументооборота = Перечисления.ФорматыДокументооборотаСПФР.ЭДОК,
				ОтчетПФРСданЧастично(НаиболееПозднийПротокол.Ссылка,, ЦиклОбмена.ФорматДокументооборота), Неопределено);
			ПротоколСОшибкой = НаиболееПозднийПротокол.ПротоколСОшибкой; // ошибка означает не сдан или сдан частично (при поддержке)
			
			Если АнализПриложенияПротокола <> Неопределено И НЕ АнализПриложенияПротокола.ЕстьПриложение
				И (НЕ АнализПриложенияПротокола.ЕстьВложения
				ИЛИ ЦиклОбмена.ФорматДокументооборота = Перечисления.ФорматыДокументооборотаСПФР.ЭДОК) Тогда
				Возврат ТекСтатус; // не расшифрован, но поддерживает частичную сдачу, определяемую по приложению / протоколу ЭДОК
			ИначеЕсли АнализПриложенияПротокола <> Неопределено И АнализПриложенияПротокола.СданЧастично Тогда
				Возврат Перечисления.СтатусыОтправки.ПринятЕстьОшибки;
			ИначеЕсли ПротоколСОшибкой ИЛИ АнализПриложенияПротокола <> Неопределено И АнализПриложенияПротокола.НеСдан Тогда
				Возврат Перечисления.СтатусыОтправки.НеПринят;
			КонецЕсли;
		КонецЕсли;
		
		Возврат Перечисления.СтатусыОтправки.Сдан;
		
	ИначеЕсли ЦиклОбмена.Тип = Перечисления.ТипыЦикловОбмена.ИОС Тогда
		
		СообщенияЦиклаОбмена = ПолучитьСообщенияЦиклаОбмена(ЦиклОбмена.Ссылка);
		
		ПервичноеСообщениеСодержащееЗапрос = СообщенияЦиклаОбмена.Найти(Перечисления.ТипыТранспортныхСообщений.ПервичноеСообщениеСодержащееЗапросПФР, "Тип");
		Если ПервичноеСообщениеСодержащееЗапрос <> Неопределено Тогда
			Если ПервичноеСообщениеСодержащееЗапрос.Статус = Перечисления.СтатусыПисем.Отправленное Тогда
				ТекСтатус = Перечисления.СтатусыОтправки.Отправлен;
			Иначе
				Возврат ТекСтатус;
			КонецЕсли;
		Иначе
			Возврат ТекСтатус;
		КонецЕсли;
		
		ПодтвержденияДоставки = СообщенияЦиклаОбмена.НайтиСтроки(Новый Структура("Тип", Перечисления.ТипыТранспортныхСообщений.ПодтверждениеПолученияЗапросаПФР));
		ЧислоПодтвержденийДоставки = ПодтвержденияДоставки.Количество();
		Если ЧислоПодтвержденийДоставки = 0 Тогда
			Возврат ТекСтатус;
		ИначеЕсли ЧислоПодтвержденийДоставки = 1 Тогда
			ПодтверждениеДоставки = ПодтвержденияДоставки[0];
			Если ПодтверждениеДоставки.Ссылка.ПротоколСОшибкой Тогда
				Возврат Перечисления.СтатусыОтправки.НеПринят;
			Иначе
				ТекСтатус = Перечисления.СтатусыОтправки.Доставлен;
			КонецЕсли;
		Иначе
			
			// если протоколов несколько, то ориентируемся на наиболее свежий из них
			НаиболееПоздниееПодтверждение = Неопределено;
			ДатаНаиболееПозднегоПодтверждения = '00010101';
			Для Каждого ПодтверждениеДоставки Из ПодтвержденияДоставки Цикл
				ДатаПодтверждения = ПодтверждениеДоставки.Дата;
				Если ДатаПодтверждения = Неопределено Тогда
					ДатаПодтверждения = '00010101';
				КонецЕсли;
				Если ДатаПодтверждения >= ДатаНаиболееПозднегоПодтверждения Тогда
					ДатаНаиболееПозднегоПодтверждения = ДатаПодтверждения;
					НаиболееПоздниееПодтверждение = ПодтверждениеДоставки;
				КонецЕсли;
			КонецЦикла;
			
			Если НаиболееПоздниееПодтверждение.Ссылка.ПротоколСОшибкой Тогда
				Возврат Перечисления.СтатусыОтправки.НеПринят;
			Иначе
				ТекСтатус = Перечисления.СтатусыОтправки.Доставлен;
			КонецЕсли;
			
		КонецЕсли;
		
		ОтветыНаЗапрос = СообщенияЦиклаОбмена.НайтиСтроки(Новый Структура("Тип", Перечисления.ТипыТранспортныхСообщений.ОтветНаЗапросПФР));
		ЧислоОтветов = ОтветыНаЗапрос.Количество();
		Если ЧислоОтветов = 0 Тогда
			Возврат ТекСтатус;
		ИначеЕсли ЧислоОтветов = 1 Тогда
			ОтветНаЗапрос = ОтветыНаЗапрос[0];
			Если ОтветНаЗапрос.Ссылка.ПротоколСОшибкой Тогда
				Возврат Перечисления.СтатусыОтправки.НеПринят;
			КонецЕсли;
		Иначе
			
			// если ответов несколько, то ориентируемся на наиболее свежий из них
			НаиболееПозднийОтвет = Неопределено;
			ДатаНаиболееПозднегоПротокола = '00010101';
			Для Каждого ОтветНаЗапрос Из ОтветыНаЗапрос Цикл
				ДатаПротоколаПроверки = ОтветНаЗапрос.Дата;
				Если ДатаПротоколаПроверки = Неопределено Тогда
					ДатаПротоколаПроверки = '00010101';
				КонецЕсли;
				Если ДатаПротоколаПроверки >= ДатаНаиболееПозднегоПротокола Тогда
					ДатаНаиболееПозднегоПротокола = ДатаПротоколаПроверки;
					НаиболееПозднийОтвет = ОтветНаЗапрос;
				КонецЕсли;
			КонецЦикла;
			
			Если НаиболееПозднийОтвет.Ссылка.ПротоколСОшибкой Тогда
				Возврат Перечисления.СтатусыОтправки.НеПринят;
			КонецЕсли;
			
		КонецЕсли;
		
		Возврат Перечисления.СтатусыОтправки.Сдан;
		
	ИначеЕсли ЦиклОбмена.Тип = Перечисления.ТипыЦикловОбмена.НеформализованнаяПерепискаПФРИсходящие Тогда
		
		СообщенияЦиклаОбмена = ПолучитьСообщенияЦиклаОбмена(ЦиклОбмена.Ссылка);
		
		ПервичноеСообщениеСодержащееПисьмо = СообщенияЦиклаОбмена.Найти(Перечисления.ТипыТранспортныхСообщений.ПисьмоИсходящееПФР, "Тип");
		Если ПервичноеСообщениеСодержащееПисьмо <> Неопределено Тогда
			Если ПервичноеСообщениеСодержащееПисьмо.Статус = Перечисления.СтатусыПисем.Отправленное Тогда
				ТекСтатус = Перечисления.СтатусыОтправки.Отправлен;
			Иначе
				Возврат Перечисления.СтатусыОтправки.ВКонверте;
			КонецЕсли;
		Иначе
			Возврат ТекСтатус;
		КонецЕсли;
		
		ПодтверждениеДоставки = СообщенияЦиклаОбмена.Найти(Перечисления.ТипыТранспортныхСообщений.ПисьмоИсходящееКвитанцияПФР, "Тип");
		Если ПодтверждениеДоставки = Неопределено Тогда
			Возврат ТекСтатус;
		Иначе
			ТекСтатус = Перечисления.СтатусыОтправки.Доставлен;
		КонецЕсли;
		
	ИначеЕсли ЦиклОбмена.Тип = Перечисления.ТипыЦикловОбмена.ОбращениеНП Тогда
		
		// получаем сообщения цикла обмена
		СообщенияЦиклаОбмена = ПолучитьСообщенияЦиклаОбмена(ЦиклОбмена.Ссылка);
		
		// анализируем, отправлено ли первичное
		ОбращениеНП = СообщенияЦиклаОбмена.Найти(Перечисления.ТипыТранспортныхСообщений.ОбращениеНП, "Тип");
		Если ОбращениеНП <> Неопределено Тогда
			Если ОбращениеНП.Статус = Перечисления.СтатусыПисем.Отправленное Тогда
				ТекСтатус = Перечисления.СтатусыОтправки.Отправлен;
			Иначе
				Возврат ТекСтатус;
			КонецЕсли;
		Иначе
			Возврат ТекСтатус;
		КонецЕсли;
		
		// анализируем, получено ли подтверждение отправки
		ПодтверждениеОбращениеНО = СообщенияЦиклаОбмена.Найти(Перечисления.ТипыТранспортныхСообщений.ПодтверждениеОбращениеНО, "Тип");
		Если ПодтверждениеОбращениеНО = Неопределено Тогда
			Возврат ТекСтатус;
		КонецЕсли;
		
		// анализируем, получено ли извещение о получении декларации
		ИзвещениеОбращениеНО = СообщенияЦиклаОбмена.Найти(Перечисления.ТипыТранспортныхСообщений.ИзвещениеОбращениеНО, "Тип");
		Если ИзвещениеОбращениеНО = Неопределено Тогда
			Возврат ТекСтатус;
		Иначе
			ТекСтатус = Перечисления.СтатусыОтправки.Доставлен;
		КонецЕсли;
		
		// анализируем результаты приема
		// если их несколько, то интересует последний
		РезультатыПриема = СообщенияЦиклаОбмена.НайтиСтроки(Новый Структура("Тип", Перечисления.ТипыТранспортныхСообщений.РезультатПриемаОбращениеНО));
		ЧислоСообщений = РезультатыПриема.Количество();
		Если ЧислоСообщений = 0 Тогда
			Возврат ТекСтатус;
		ИначеЕсли ЧислоСообщений = 1 Тогда
			РезультатПриема = РезультатыПриема[0];
			Если РезультатПриема.Ссылка.ПротоколСОшибкой Тогда
				Возврат Перечисления.СтатусыОтправки.НеПринят;
			КонецЕсли;
		Иначе
			
			// если протоколов несколько, то ориентируемся на наиболее свежий из них
			НаиболееПозднийРезультат = Неопределено;
			ДатаНаиболееПозднегоРезультата = '00010101';
			Для Каждого РезультатПриема Из РезультатыПриема Цикл
				ДатаРезультатаПриема = РезультатПриема.Дата;
				Если ДатаРезультатаПриема = Неопределено Тогда
					ДатаРезультатаПриема = '00010101';
				КонецЕсли;
				Если ДатаРезультатаПриема >= ДатаНаиболееПозднегоРезультата Тогда
					ДатаНаиболееПозднегоРезультата = ДатаРезультатаПриема;
					НаиболееПозднийРезультат = РезультатПриема;
				КонецЕсли;
			КонецЦикла;
			
			Если НаиболееПозднийРезультат.Ссылка.ПротоколСОшибкой Тогда
				Возврат Перечисления.СтатусыОтправки.НеПринят;
			КонецЕсли;
			
		КонецЕсли;
		
		// если все предусмотренные регламентом сообщения отправлены/приняты
		// и они без ошибок, то обращение считается успешным
		Возврат Перечисления.СтатусыОтправки.Доставлен;
		
	ИначеЕсли ЦиклОбмена.Тип = Перечисления.ТипыЦикловОбмена.Представление Тогда
		
		// получаем сообщения цикла обмена
		СообщенияЦиклаОбмена = ПолучитьСообщенияЦиклаОбмена(ЦиклОбмена.Ссылка);
		
		// анализируем, отправлено ли первичное
		ПредставлениеНП = СообщенияЦиклаОбмена.Найти(Перечисления.ТипыТранспортныхСообщений.ПредставлениеНП, "Тип");
		Если ПредставлениеНП <> Неопределено Тогда
			Если ПредставлениеНП.Статус = Перечисления.СтатусыПисем.Отправленное Тогда
				ТекСтатус = Перечисления.СтатусыОтправки.Отправлен;
			Иначе
				Возврат ТекСтатус;
			КонецЕсли;
		Иначе
			Возврат ТекСтатус;
		КонецЕсли;
		
		// анализируем, получено ли подтверждение отправки
		ПодтверждениеПредставлениеНО = СообщенияЦиклаОбмена.Найти(Перечисления.ТипыТранспортныхСообщений.ПодтверждениеПредставлениеНО, "Тип");
		Если ПодтверждениеПредставлениеНО = Неопределено Тогда
			Возврат ТекСтатус;
		КонецЕсли;
		
		// анализируем, получено ли извещение о получении 
		ИзвещениеПредставлениеНО = СообщенияЦиклаОбмена.Найти(Перечисления.ТипыТранспортныхСообщений.ИзвещениеПредставлениеНО, "Тип");
		Если ИзвещениеПредставлениеНО = Неопределено Тогда
			Возврат ТекСтатус;
		Иначе
			ТекСтатус = Перечисления.СтатусыОтправки.Доставлен;
		КонецЕсли;
		
		// анализируем результаты приема
		// если их несколько, то интересует последний
		РезультатыПриема = СообщенияЦиклаОбмена.НайтиСтроки(Новый Структура("Тип", Перечисления.ТипыТранспортныхСообщений.РезультатПриемаПредставлениеНО));
		ЧислоСообщений = РезультатыПриема.Количество();
		Если ЧислоСообщений = 0 Тогда
			Возврат ТекСтатус;
		ИначеЕсли ЧислоСообщений = 1 Тогда
			РезультатПриема = РезультатыПриема[0];
			Если РезультатПриема.Ссылка.ПротоколСОшибкой Тогда
				Возврат Перечисления.СтатусыОтправки.НеПринят;
			КонецЕсли;
		Иначе
			
			// если протоколов несколько, то ориентируемся на наиболее свежий из них
			НаиболееПозднийРезультат = Неопределено;
			ДатаНаиболееПозднегоРезультата = '00010101';
			Для Каждого РезультатПриема Из РезультатыПриема Цикл
				ДатаРезультатаПриема = РезультатПриема.Дата;
				Если ДатаРезультатаПриема = Неопределено Тогда
					ДатаРезультатаПриема = '00010101';
				КонецЕсли;
				Если ДатаРезультатаПриема >= ДатаНаиболееПозднегоРезультата Тогда
					ДатаНаиболееПозднегоРезультата = ДатаРезультатаПриема;
					НаиболееПозднийРезультат = РезультатПриема;
				КонецЕсли;
			КонецЦикла;
			
			Если НаиболееПозднийРезультат.Ссылка.ПротоколСОшибкой Тогда
				Возврат Перечисления.СтатусыОтправки.НеПринят;
			КонецЕсли;
			
		КонецЕсли;
		
		// если все предусмотренные регламентом сообщения отправлены/приняты
		// и они без ошибок, то представление считается успешным
		Возврат Перечисления.СтатусыОтправки.Сдан;

	ИначеЕсли ЦиклОбмена.Тип = Перечисления.ТипыЦикловОбмена.ИОН Тогда
		
		// получаем сообщения цикла обмена
		СообщенияЦиклаОбмена = ПолучитьСообщенияЦиклаОбмена(ЦиклОбмена.Ссылка);
		
		// анализируем, отправлено ли первичное
		ПервичноеСообщениеСодержащееЗапрос = СообщенияЦиклаОбмена.Найти(Перечисления.ТипыТранспортныхСообщений.ЗапросНП, "Тип");
		Если ПервичноеСообщениеСодержащееЗапрос <> Неопределено Тогда
			Если ПервичноеСообщениеСодержащееЗапрос.Статус = Перечисления.СтатусыПисем.Отправленное Тогда
				ТекСтатус = Перечисления.СтатусыОтправки.Отправлен; // если первичное отправлено, то статус - отправлено
			Иначе
				Возврат ТекСтатус; // если первичное не отпарвлено, то дальше не анализируем
			КонецЕсли;
		Иначе // если первичное отсутствует, то дальше не анализируем
			Возврат ТекСтатус;
		КонецЕсли;
		
		// анализируем, получено ли подтверждение отправки
		ПодтверждениеОтправки = СообщенияЦиклаОбмена.Найти(Перечисления.ТипыТранспортныхСообщений.ПодтверждениеЗапросНО, "Тип");
		Если ПодтверждениеОтправки = Неопределено Тогда
			Возврат ТекСтатус;
		КонецЕсли;
		
		// анализируем, получено ли извещение о получении запроса
		ПодтверждениеДоставки = СообщенияЦиклаОбмена.Найти(Перечисления.ТипыТранспортныхСообщений.ИзвещениеЗапросНО, "Тип");
		Если ПодтверждениеДоставки <> Неопределено Тогда
			ТекСтатус = Перечисления.СтатусыОтправки.Доставлен;
		КонецЕсли;
		
		// анализируем протоколы приема
		// если их несколько, то интересует последний
		ПротоколыПриема = СообщенияЦиклаОбмена.НайтиСтроки(Новый Структура("Тип", Перечисления.ТипыТранспортныхСообщений.РезультатПриемаЗапросНО));
		ЧислоПротоколов = ПротоколыПриема.Количество();
		Если ЧислоПротоколов = 0 Тогда
			Возврат ТекСтатус;
		ИначеЕсли ЧислоПротоколов = 1 Тогда
			ПротоколПриема = ПротоколыПриема[0];
			Если ПротоколПриема.Ссылка.ПротоколСОшибкой Тогда
				Возврат Перечисления.СтатусыОтправки.НеПринят;
			КонецЕсли;
		Иначе
			
			// если протоколов несколько, то ориентируемся на наиболее свежий из них
			НаиболееПозднийПротокол = Неопределено;
			ДатаНаиболееПозднегоПротокола = '00010101';
			Для Каждого ПротоколПриема Из ПротоколыПриема Цикл
				ДатаПротоколаПриема = ПротоколПриема.Дата;
				Если ДатаПротоколаПриема = Неопределено Тогда
					ДатаПротоколаПриема = '00010101';
				КонецЕсли;
				Если ДатаПротоколаПриема >= ДатаНаиболееПозднегоПротокола Тогда
					ДатаНаиболееПозднегоПротокола = ДатаПротоколаПриема;
					НаиболееПозднийПротокол = ПротоколПриема;
				КонецЕсли;
			КонецЦикла;
			
			Если НаиболееПозднийПротокол.Ссылка.ПротоколСОшибкой Тогда
				Возврат Перечисления.СтатусыОтправки.НеПринят;
			КонецЕсли;
			
		КонецЕсли;
		
		// анализируем протоколы обработки
		// если их несколько, то анализируем последний
		ПротоколыОбработки = СообщенияЦиклаОбмена.НайтиСтроки(Новый Структура("Тип", Перечисления.ТипыТранспортныхСообщений.РезультатОбработкиЗапросНО));
		ЧислоПротоколов = ПротоколыОбработки.Количество();
		Если ЧислоПротоколов = 0 Тогда
			Возврат ТекСтатус;
		Иначе
			Возврат Перечисления.СтатусыОтправки.Сдан;
		КонецЕсли;
		
	ИначеЕсли ЦиклОбмена.Тип = Перечисления.ТипыЦикловОбмена.ОтчетностьФСГС Тогда
		
		СообщенияЦиклаОбмена = ПолучитьСообщенияЦиклаОбмена(ЦиклОбмена.Ссылка);
		
		ПервичноеСообщениеСодержащееОтчетность = СообщенияЦиклаОбмена.Найти(Перечисления.ТипыТранспортныхСообщений.ПервичноеСообщениеСодержащееОтчетностьФСГС, "Тип");
		Если ПервичноеСообщениеСодержащееОтчетность <> Неопределено Тогда
			Если ПервичноеСообщениеСодержащееОтчетность.Статус = Перечисления.СтатусыПисем.Отправленное Тогда
				ТекСтатус = Перечисления.СтатусыОтправки.Отправлен;
			Иначе
				Возврат ТекСтатус;
			КонецЕсли;
		Иначе
			Возврат ТекСтатус;
		КонецЕсли;
		
		ПодтвержденияДоставки = СообщенияЦиклаОбмена.НайтиСтроки(Новый Структура("Тип", Перечисления.ТипыТранспортныхСообщений.ИзвещениеОПолученииОтчетностиФСГС));
		ЧислоПодтвержденийДоставки = ПодтвержденияДоставки.Количество();
		Если ЧислоПодтвержденийДоставки = 0 Тогда
			Возврат ТекСтатус;
		ИначеЕсли ЧислоПодтвержденийДоставки = 1 Тогда
			ПодтверждениеДоставки = ПодтвержденияДоставки[0];
			Если ПодтверждениеДоставки.Ссылка.ПротоколСОшибкой Тогда
				ТекСтатус = Перечисления.СтатусыОтправки.НеПринят;
			Иначе
				ТекСтатус = Перечисления.СтатусыОтправки.Доставлен;
			КонецЕсли;
		Иначе
			// если извещений несколько, то ориентируемся на наиболее свежее из них
			НаиболееПоздниееПодтверждение = Неопределено;
			ДатаНаиболееПозднегоПодтверждения = '00010101';
			Для Каждого ПодтверждениеДоставки Из ПодтвержденияДоставки Цикл
				ДатаПодтверждения = ПодтверждениеДоставки.Дата;
				Если ДатаПодтверждения = Неопределено Тогда
					ДатаПодтверждения = '00010101';
				КонецЕсли;
				Если ДатаПодтверждения >= ДатаНаиболееПозднегоПодтверждения Тогда
					ДатаНаиболееПозднегоПодтверждения = ДатаПодтверждения;
					НаиболееПоздниееПодтверждение = ПодтверждениеДоставки;
				КонецЕсли;
			КонецЦикла;
			
			Если НаиболееПоздниееПодтверждение.Ссылка.ПротоколСОшибкой Тогда
				ТекСтатус = Перечисления.СтатусыОтправки.НеПринят;
			Иначе
				ТекСтатус = Перечисления.СтатусыОтправки.Доставлен;
			КонецЕсли;
			
		КонецЕсли;
		
		ПротоколыПроверки = СообщенияЦиклаОбмена.НайтиСтроки(Новый Структура("Тип", Перечисления.ТипыТранспортныхСообщений.ПротоколВходногоКонтроляОтчетностиФСГС));
		ЧислоПротоколов = ПротоколыПроверки.Количество();
		Если ЧислоПротоколов = 0 Тогда
			Возврат ТекСтатус;
		ИначеЕсли ЧислоПротоколов = 1 Тогда
			ПротоколПроверки = ПротоколыПроверки[0];
			Если Не СообщениеРасшифровано(ПротоколПроверки.Ссылка) Тогда
				Возврат ТекСтатус;
			КонецЕсли;
			Если ПротоколПроверки.Ссылка.ПротоколСОшибкой Тогда
				Возврат Перечисления.СтатусыОтправки.НеПринят;
			Иначе
				Возврат Перечисления.СтатусыОтправки.Сдан;
			КонецЕсли;
		Иначе
			// если протоколов несколько, то ориентируемся на наиболее свежий из них
			НаиболееПозднийПротокол = Неопределено;
			ДатаНаиболееПозднегоПротокола = '00010101';
			Для Каждого ПротоколПроверки Из ПротоколыПроверки Цикл
				ДатаПротоколаПроверки = ПротоколПроверки.Дата;
				Если ДатаПротоколаПроверки = Неопределено Тогда
					ДатаПротоколаПроверки = '00010101';
				КонецЕсли;
				Если ДатаПротоколаПроверки >= ДатаНаиболееПозднегоПротокола Тогда
					ДатаНаиболееПозднегоПротокола = ДатаПротоколаПроверки;
					НаиболееПозднийПротокол = ПротоколПроверки;
				КонецЕсли;
			КонецЦикла;
			
			Если Не СообщениеРасшифровано(НаиболееПозднийПротокол.Ссылка) Тогда
				Возврат ТекСтатус;
			КонецЕсли;
			Если НаиболееПозднийПротокол.Ссылка.ПротоколСОшибкой Тогда
				Возврат Перечисления.СтатусыОтправки.НеПринят;
			Иначе
				Возврат Перечисления.СтатусыОтправки.Сдан;
			КонецЕсли;
			
		КонецЕсли;
		
		Возврат ТекСтатус;
		
	ИначеЕсли ЦиклОбмена.Тип = Перечисления.ТипыЦикловОбмена.ПисьменноеОбращениеВФСГС Тогда
		
		СообщенияЦиклаОбмена = ПолучитьСообщенияЦиклаОбмена(ЦиклОбмена.Ссылка);
		
		ПервичноеСообщениеСодержащееПисьмо = СообщенияЦиклаОбмена.Найти(Перечисления.ТипыТранспортныхСообщений.ПисьмоИсходящееФСГС, "Тип");
		Если ПервичноеСообщениеСодержащееПисьмо <> Неопределено Тогда
			Если ПервичноеСообщениеСодержащееПисьмо.Статус = Перечисления.СтатусыПисем.Отправленное Тогда
				ТекСтатус = Перечисления.СтатусыОтправки.Отправлен;
			Иначе
				Возврат Перечисления.СтатусыОтправки.ВКонверте;
			КонецЕсли;
		Иначе
			Возврат ТекСтатус;
		КонецЕсли;
		
		ПодтверждениеДоставки = СообщенияЦиклаОбмена.Найти(Перечисления.ТипыТранспортныхСообщений.ИзвещениеПисьмоИсходящееФСГС, "Тип");
		Если ПодтверждениеДоставки = Неопределено Тогда
			Возврат ТекСтатус;
		Иначе
			ТекСтатус = Перечисления.СтатусыОтправки.Доставлен;
		КонецЕсли;
		
	КонецЕсли;
	
	Возврат ТекСтатус;
	
КонецФункции

Функция РассчитатьСтатусОтправкиОбъекта(Объект, Знач ЦиклОбмена = Неопределено) Экспорт
	
	Если ЦиклОбмена = Неопределено Тогда
		ЦиклОбмена = ДокументооборотСКОВызовСервера.ПолучитьПоследнийЦиклОбмена(Объект);
	КонецЕсли;
	
	Если ЗначениеЗаполнено(ЦиклОбмена) Тогда
		Возврат РассчитатьСтатусЦиклаОбмена(ЦиклОбмена);
	Иначе
		Возврат Перечисления.СтатусыОтправки.ПустаяСсылка();
	КонецЕсли;
	
КонецФункции

Процедура ОбновитьСтатусыСвязанногоОбъектаПриНеобходимости(Объект, СтрогоПоДанномуЦиклуОбмена = Ложь) Экспорт
	
	// определяем цикл обмена для расчета статуса отчета
	ТипОбъекта = ТипЗнч(Объект);
	Если ТипОбъекта = Тип("ДокументСсылка.ТранспортноеСообщение") ИЛИ ТипОбъекта = Тип("ДокументОбъект.ТранспортноеСообщение") Тогда
		ЦиклОбменаОснование = Объект.ЦиклОбмена;
	ИначеЕсли ТипОбъекта = Тип("СправочникСсылка.ЦиклыОбмена") Тогда
		ЦиклОбменаОснование = Объект;
	Иначе
		Возврат;
	КонецЕсли;
	
	ПредметыПереписки = ПолучитьПредметыЦиклаОбмена(ЦиклОбменаОснование);
	Для Каждого Предмет Из ПредметыПереписки Цикл
		Если ЗначениеЗаполнено(Предмет) И Метаданные.РегистрыСведений.СтатусыОтправки.Измерения.Объект.Тип.СодержитТип(ТипЗнч(Предмет)) Тогда
			СтатусОтчета = РассчитатьСтатусОтправкиОбъекта(Предмет, ?(СтрогоПоДанномуЦиклуОбмена, ЦиклОбменаОснование, Неопределено));
			Если СтатусОтчета <> Перечисления.СтатусыОтправки.ПустаяСсылка() Тогда
				ЗаписатьСтатусОтправкиОбъекта(Предмет, СтатусОтчета);
			КонецЕсли;
		КонецЕсли;
	КонецЦикла;
	
КонецПроцедуры

Процедура ПриЗаписиОбъекта(Объект, Отказ, Замещение = Неопределено) Экспорт
	
	ТипОбъекта = ТипЗнч(Объект);
	Если ТипОбъекта = Тип("СправочникОбъект.ЭлектронныеПредставленияРегламентированныхОтчетов") Тогда
		ПриЗаписиЭлектронногоПредставленияРегламентированногоОтчета(Объект, Отказ);
	ИначеЕсли ТипОбъекта = Тип("СправочникОбъект.УчетныеЗаписиДокументооборота") Тогда
		ПриЗаписиУчетнойЗаписиНалогоплательщика(Объект, Отказ);
	ИначеЕсли ТипОбъекта = Тип("СправочникОбъект.ЦиклыОбмена") Тогда
		ПриЗаписиЦиклаОбмена(Объект, Отказ);
	ИначеЕсли ТипОбъекта = Тип("ДокументОбъект.ТранспортноеСообщение") Тогда
		ПриЗаписиТранспортногоСообщения(Объект, Отказ);
	ИначеЕсли ТипОбъекта = Тип("СправочникОбъект.ОтправкиФСС") Тогда
		ПриЗаписиОтправкиФСС(Объект, Отказ);
	ИначеЕсли ТипОбъекта = Тип("СправочникОбъект.ОтправкиФСРАР") Тогда
		ПриЗаписиОтправкиФСРАР(Объект, Отказ);
	ИначеЕсли ТипОбъекта = Тип("СправочникОбъект.ОтправкиРПН") Тогда
		ПриЗаписиОтправкиРПН(Объект, Отказ);
	ИначеЕсли ТипОбъекта = Тип("СправочникОбъект.ОтправкиФТС") Тогда
		ПриЗаписиОтправкиФТС(Объект, Отказ);
	ИначеЕсли ТипОбъекта = Тип("СправочникОбъект.ОписиИсходящихДокументовВНалоговыеОрганы") 
		ИЛИ ТипОбъекта = Тип("ДокументОбъект.ПоясненияКДекларацииПоНДС")
		ИЛИ ТипОбъекта = Тип("СправочникОбъект.ПерепискаСКонтролирующимиОрганами") Тогда
		ПриЗаписиОтветаНаТребованиеФНС(Объект, Отказ);
	ИначеЕсли ТипОбъекта = Тип("СправочникОбъект.СканированныеДокументыДляПередачиВЭлектронномВиде") Тогда
		ЭлектронныйДокументооборотСКонтролирующимиОрганами.ЗаписатьСканированныйДокументВРегистрДокументыПоТребованиюФНС(Объект);
	ИначеЕсли Объект.Метаданные().Имя = "ОтправкиБанкРоссии" Тогда
		ПриЗаписиОтправкиБанкРоссии(Объект, Отказ);
	ИначеЕсли Объект.Метаданные().Имя = "ОтправкиМинобороны" Тогда
		ПриЗаписиОтправкиМинобороны(Объект, Отказ);
	ИначеЕсли ТипОбъекта = Тип("ДокументОбъект.ЗаявленияПоЭлДокументооборотуСПФР") Тогда
		ПриЗаписиЗаявленияПоЭлДокументооборотуСПФР(Объект, Отказ);
	ИначеЕсли ТипОбъекта = Тип("РегистрСведенийНаборЗаписей.НастройкиОбменаФСС") Тогда
		ПриЗаписиНастройкиОбменаФСС(Объект, Отказ);
	КонецЕсли;
	
КонецПроцедуры

Функция ПолучитьЭлементДаты(МассивЭлементов, ИндексЭлемента)
	
	Если МассивЭлементов.Количество() <= ИндексЭлемента Тогда
		Возврат 0;
	Иначе
		ЭлементДаты = МассивЭлементов[ИндексЭлемента];
		Если ПустаяСтрока(ЭлементДаты) ИЛИ НЕ СтроковыеФункцииКлиентСервер.ТолькоЦифрыВСтроке(ЭлементДаты) Тогда
			Возврат 0;
		Иначе
			Возврат Число(ЭлементДаты);
		КонецЕсли;
	КонецЕсли;
	
КонецФункции

Функция ПолучитьАтрибутыУзла(Узел) Экспорт
	
	УзлыАтрибуты = Узел.Строки.НайтиСтроки(Новый Структура("Тип", "А"));
	
	АтрибутыУзла = Новый Структура;
	Для Каждого УзелАтрибут Из УзлыАтрибуты Цикл
		АтрибутыУзла.Вставить(УзелАтрибут.Имя, УзелАтрибут.Значение);
	КонецЦикла;
	
	Возврат АтрибутыУзла;
	
КонецФункции

Функция СформироватьНаименованиеЭлектронногоПредставленияРегламентированногоОтчета(ЭлПредставление) Экспорт
	
	Если ЭлПредставление.мИмяФайла = "" ИЛИ ЭлПредставление.мИмяФайла = Неопределено Тогда
		Возврат "Для НО " + ЭлПредставление.НалоговыйОрган + " от " + ЭлПредставление.Организация;
	Иначе
		Возврат СокрЛП(ЭлПредставление.мИмяФайла) + " для НО " + ЭлПредставление.НалоговыйОрган + " от " + ЭлПредставление.Организация;
	КонецЕсли;
	
КонецФункции

//////////////////////////////////////////////////////////////////////////////////
//// ОБРАБОТЧИКИ СОБЫТИЙ ЦИКЛА ОБМЕНА

Процедура ПередЗаписьюЦиклаОбмена(Объект, Отказ)
	
	Объект.ИзмененаПометкаУдаления = (Объект.Ссылка.ПометкаУдаления <> Объект.ПометкаУдаления);
	Если Объект.ИзмененаПометкаУдаления Тогда
		Возврат;
	КонецЕсли;
	
	ШапкаОшибки = "Не удалось записать цикл обмена " + ПредставлениеЦиклаОбмена(Объект) + ":";
	
	Если НЕ ЗначениеЗаполнено(Объект.Организация) Тогда
		РегламентированнаяОтчетностьКлиентСервер.СообщитьОбОшибке("Не указана организация!", Отказ, ШапкаОшибки);
	КонецЕсли;
	
	Если НЕ ЗначениеЗаполнено(Объект.ВнешняяОрганизация) Тогда
		Если Объект.Вид = Перечисления.ВидыЦикловОбмена.ЦиклОбменаСФНС Тогда
			РегламентированнаяОтчетностьКлиентСервер.СообщитьОбОшибке("Не указан налоговый орган!", Отказ, ШапкаОшибки);
		ИначеЕсли Объект.Вид = Перечисления.ВидыЦикловОбмена.ЦиклОбменаСПФР Тогда
			РегламентированнаяОтчетностьКлиентСервер.СообщитьОбОшибке("Не указан орган ПФР!", Отказ, ШапкаОшибки);
		ИначеЕсли Объект.Вид = Перечисления.ВидыЦикловОбмена.ЦиклОбменаСФСГС Тогда
			РегламентированнаяОтчетностьКлиентСервер.СообщитьОбОшибке("Не указан орган ФСГС!", Отказ, ШапкаОшибки);
		КонецЕсли;
	КонецЕсли;
	
	Если НЕ Отказ Тогда
		
		Если НЕ ЗначениеЗаполнено(Объект.ДатаСоздания) Тогда
			Объект.ДатаСоздания = ТекущаяДатаСеанса();
		КонецЕсли;
		
		Если ПустаяСтрока(Объект.Наименование) Тогда
			СгенерироватьНаименованиеЦиклаОбмена(Объект);
		КонецЕсли;
		
		Если НЕ ЗначениеЗаполнено(Объект.ДатаЗакрытия) Тогда
			Объект.ДатаЗакрытия = ПолучитьПустуюДатуЗавершенияЦиклаОбмена();
		КонецЕсли;
		
		Если Объект.Тип = Перечисления.ТипыЦикловОбмена.НалоговаяИлиБухгалтерскаяОтчетность
		ИЛИ Объект.Тип = Перечисления.ТипыЦикловОбмена.Заявление
		ИЛИ Объект.Тип = Перечисления.ТипыЦикловОбмена.Форма2НДФЛ Тогда
			Объект.Период = СокрЛП(Формат(Объект.ДатаОкончанияПериода, "ДФ=yyyyMMdd") + Формат('39991231' - Объект.ДатаНачалаПериода, "ДФ=yyyyMMdd"));
			Объект.ПредставлениеПериода = ПредставлениеПериода(Объект.ДатаНачалаПериода, КонецДня(Объект.ДатаОкончанияПериода), "ФП=Истина");
		ИначеЕсли Объект.Тип = Перечисления.ТипыЦикловОбмена.ЗапросыНаИнформационноеОбслуживание
		ИЛИ Объект.Тип = Перечисления.ТипыЦикловОбмена.ИОН
		ИЛИ Объект.Тип = Перечисления.ТипыЦикловОбмена.ИОС Тогда
			Объект.Период = СокрЛП(Формат(Объект.ДатаНачалаПериода, "ДФ=yyyyMMdd") + Формат(Объект.ДатаОкончанияПериода, "ДФ=yyyyMMdd"));
		ИначеЕсли Объект.Тип = Перечисления.ТипыЦикловОбмена.ОтчетностьПФР Тогда 
			Объект.Период = СокрЛП(Формат(Объект.ДатаОкончанияПериода, "ДФ=yyyyMMdd") + Формат('39991231' - Объект.ДатаНачалаПериода, "ДФ=yyyyMMdd"));
			Объект.ПредставлениеПериода = ПредставлениеПериода(Объект.ДатаНачалаПериода, КонецДня(Объект.ДатаОкончанияПериода), "ФП=Истина");
		ИначеЕсли Объект.Тип = Перечисления.ТипыЦикловОбмена.ОтчетностьФСГС Тогда 
			Объект.Период = СокрЛП(Формат(Объект.ДатаОкончанияПериода, "ДФ=yyyyMMdd") + Формат('39991231' - Объект.ДатаНачалаПериода, "ДФ=yyyyMMdd"));
			Объект.ПредставлениеПериода = ПредставлениеПериода(Объект.ДатаНачалаПериода, КонецДня(Объект.ДатаОкончанияПериода), "ФП=Истина");
		КонецЕсли;
		
		Если НЕ ЗначениеЗаполнено(Объект.ПредставлениеПериода) И (ЗначениеЗаполнено(Объект.ДатаНачалаПериода) И ЗначениеЗаполнено(Объект.ДатаОкончанияПериода)) Тогда
			Объект.ПредставлениеПериода = ПредставлениеПериода(Объект.ДатаНачалаПериода, КонецДня(Объект.ДатаОкончанияПериода), "ФП=Истина");
		КонецЕсли;
		
		// Для закладки ФНС - Отчетность формы Управление обменом
		Если Объект.Вид = Перечисления.ВидыЦикловОбмена.ЦиклОбменаСФНС
			И Объект.ФорматДокументооборота = Перечисления.ФорматыДокументооборотаСФНС.Приказ534
			И (Объект.Тип = Перечисления.ТипыЦикловОбмена.НалоговаяИлиБухгалтерскаяОтчетность
			ИЛИ Объект.Тип = Перечисления.ТипыЦикловОбмена.Форма2НДФЛ) Тогда 
				Объект.ПредставлениеВидаДокумента = РегламентированнаяОтчетность.ПредставлениеВидаДокумента(Объект.ВидДокумента);
		КонецЕсли;
		
	КонецЕсли;
	
КонецПроцедуры

Процедура СгенерироватьНаименованиеЦиклаОбмена(ЦиклОбмена)
	
	Если ЦиклОбмена.Тип = Перечисления.ТипыЦикловОбмена.НалоговаяИлиБухгалтерскаяОтчетность
	ИЛИ ЦиклОбмена.Тип = Перечисления.ТипыЦикловОбмена.Заявление
	ИЛИ ЦиклОбмена.Тип = Перечисления.ТипыЦикловОбмена.Форма2НДФЛ Тогда
		ЦиклОбмена.Наименование = ПолучитьИмяФайлаДИВЦиклаОбмена(ЦиклОбмена);
		Если ЗначениеЗаполнено(ЦиклОбмена.ВнешняяОрганизация) Тогда
			ЦиклОбмена.Наименование = ЦиклОбмена.Наименование + " для НО " + ЦиклОбмена.ВнешняяОрганизация;
		КонецЕсли;
		Если ЗначениеЗаполнено(ЦиклОбмена.Организация) Тогда
			ЦиклОбмена.Наименование = ЦиклОбмена.Наименование + " от " + ЦиклОбмена.Организация;
		КонецЕсли;
	ИначеЕсли ЦиклОбмена.Тип = Перечисления.ТипыЦикловОбмена.НеформализованныеДокументыНалогоплательщика Тогда
		ЦиклОбмена.Наименование = ПолучитьИмяФайлаДИВЦиклаОбмена(ЦиклОбмена);
		Если ЗначениеЗаполнено(ЦиклОбмена.ВнешняяОрганизация) Тогда
			ЦиклОбмена.Наименование = ЦиклОбмена.Наименование + " для НО " + ЦиклОбмена.ВнешняяОрганизация;
		КонецЕсли;
		Если ЗначениеЗаполнено(ЦиклОбмена.Организация) Тогда
			ЦиклОбмена.Наименование = ЦиклОбмена.Наименование + " от " + ЦиклОбмена.Организация;
		КонецЕсли;
	ИначеЕсли ЦиклОбмена.Тип = Перечисления.ТипыЦикловОбмена.ЗапросыНаИнформационноеОбслуживание Тогда
		ЦиклОбмена.Наименование = ПолучитьИмяФайлаДИВЦиклаОбмена(ЦиклОбмена);
		Если ЗначениеЗаполнено(ЦиклОбмена.ВнешняяОрганизация) Тогда
			ЦиклОбмена.Наименование = ЦиклОбмена.Наименование + " для НО " + ЦиклОбмена.ВнешняяОрганизация;
		КонецЕсли;
		Если ЗначениеЗаполнено(ЦиклОбмена.Организация) Тогда
			ЦиклОбмена.Наименование = ЦиклОбмена.Наименование + " от " + ЦиклОбмена.Организация;
		КонецЕсли;
	ИначеЕсли ЦиклОбмена.Тип = Перечисления.ТипыЦикловОбмена.НеформализованныеДокументыНалоговогоОргана Тогда
		ЦиклОбмена.Наименование = "От НО " + ЦиклОбмена.ВнешняяОрганизация;
	ИначеЕсли ЦиклОбмена.Тип = Перечисления.ТипыЦикловОбмена.ПисьмоНО Тогда
		ЦиклОбмена.Наименование = ТемаПисьмаПоЦиклуОбмена(ЦиклОбмена);
	ИначеЕсли ЦиклОбмена.Тип = Перечисления.ТипыЦикловОбмена.Рассылка Тогда
		ЦиклОбмена.Наименование = ТемаПисьмаПоЦиклуОбмена(ЦиклОбмена);
	ИначеЕсли ЦиклОбмена.Тип = Перечисления.ТипыЦикловОбмена.Документ Тогда
		ЦиклОбмена.Наименование = "От НО " + ЦиклОбмена.ВнешняяОрганизация;
	ИначеЕсли ЦиклОбмена.Тип = Перечисления.ТипыЦикловОбмена.РассылкаГрупповая Тогда
		ЦиклОбмена.Наименование = ТемаПисьмаПоЦиклуОбмена(ЦиклОбмена);
	ИначеЕсли ЦиклОбмена.Тип = Перечисления.ТипыЦикловОбмена.ОтчетностьПФР Тогда
		
		Если ТипЗнч(ЦиклОбмена.ВидОтчета) = Тип("СправочникСсылка.РегламентированныеОтчеты") Тогда
			Источник = ЦиклОбмена.ВидОтчета.ИсточникОтчета;
		Иначе
			Источник = ЦиклОбмена.ВидОтчета.Источник;
		КонецЕсли;
		
		Если НачалоДня(ЦиклОбмена.ДатаНачалаПериода) = НачалоДня(ЦиклОбмена.ДатаОкончанияПериода) Тогда
			ЦиклОбмена.Наименование = "" + ЦиклОбмена.Предмет + ". Отправка от " + ЦиклОбмена.ДатаСоздания;
		ИначеЕсли Источник = "ПередачаСЗВ4вПФР" Тогда
			ЦиклОбмена.Наименование = "" + ЦиклОбмена.ВидОтчета + " и РСВ за " + ПредставлениеПериода(ЦиклОбмена.ДатаНачалаПериода, КонецДня(ЦиклОбмена.ДатаОкончанияПериода), "ФП=Истина") + " от " + ЦиклОбмена.ДатаСоздания;
		Иначе
			ЦиклОбмена.Наименование = "" + ЦиклОбмена.ВидОтчета + " за " + ПредставлениеПериода(ЦиклОбмена.ДатаНачалаПериода, КонецДня(ЦиклОбмена.ДатаОкончанияПериода), "ФП=Истина") + " от " + ЦиклОбмена.ДатаСоздания;
		КонецЕсли;
		
	ИначеЕсли ЦиклОбмена.Тип = Перечисления.ТипыЦикловОбмена.ИОС Тогда
		ЦиклОбмена.Наименование = СокрЛП(ЦиклОбмена.Предмет) + " для " + СокрЛП(ЦиклОбмена.ВнешняяОрганизация) + " от " + СокрЛП(ЦиклОбмена.Организация);
	ИначеЕсли ЦиклОбмена.Тип = Перечисления.ТипыЦикловОбмена.НеформализованнаяПерепискаПФРВходящие
		Или ЦиклОбмена.Тип = Перечисления.ТипыЦикловОбмена.РассылкаПФР Тогда
		ЦиклОбмена.Наименование = ТемаПисьмаПоЦиклуОбмена(ЦиклОбмена);
	ИначеЕсли ЦиклОбмена.Тип = Перечисления.ТипыЦикловОбмена.НеформализованнаяПерепискаПФРИсходящие Тогда
		ЦиклОбмена.Наименование = ТемаПисьмаПоЦиклуОбмена(ЦиклОбмена);
	ИначеЕсли ЦиклОбмена.Тип = Перечисления.ТипыЦикловОбмена.ОбращениеНП Тогда
		ЦиклОбмена.Наименование = ТемаПисьмаПоЦиклуОбмена(ЦиклОбмена);
	ИначеЕсли ЦиклОбмена.Тип = Перечисления.ТипыЦикловОбмена.Представление Тогда
		ЦиклОбмена.Наименование = "Представление документов";
		Если ЗначениеЗаполнено(ЦиклОбмена.ВнешняяОрганизация) Тогда
			ЦиклОбмена.Наименование = ЦиклОбмена.Наименование + " для НО " + ЦиклОбмена.ВнешняяОрганизация;
		КонецЕсли;
		Если ЗначениеЗаполнено(ЦиклОбмена.Организация) Тогда
			ЦиклОбмена.Наименование = ЦиклОбмена.Наименование + " от " + ЦиклОбмена.Организация;
		КонецЕсли;	
	ИначеЕсли ЦиклОбмена.Тип = Перечисления.ТипыЦикловОбмена.ИОН Тогда
		ЦиклОбмена.Наименование = ПолучитьИмяФайлаДИВЦиклаОбмена(ЦиклОбмена);
		Если ЗначениеЗаполнено(ЦиклОбмена.ВнешняяОрганизация) Тогда
			ЦиклОбмена.Наименование = ЦиклОбмена.Наименование + " для НО " + ЦиклОбмена.ВнешняяОрганизация;
		КонецЕсли;
		Если ЗначениеЗаполнено(ЦиклОбмена.Организация) Тогда
			ЦиклОбмена.Наименование = ЦиклОбмена.Наименование + " от " + ЦиклОбмена.Организация;
		КонецЕсли;
	ИначеЕсли ЦиклОбмена.Тип = Перечисления.ТипыЦикловОбмена.ОтчетностьФСГС Тогда
		Если ТипЗнч(ЦиклОбмена.Предмет) = Тип("ДокументСсылка.РегламентированныйОтчет") Тогда
			ЦиклОбмена.Наименование = РегламентированнаяОтчетностьКлиентСервер.ПредставлениеДокументаРеглОтч(ЦиклОбмена.Предмет) + " для " + СокрЛП(ЦиклОбмена.ВнешняяОрганизация);
		Иначе
			ЦиклОбмена.Наименование = СокрЛП(ЦиклОбмена.Предмет) + " для " + СокрЛП(ЦиклОбмена.ВнешняяОрганизация) + " от " + СокрЛП(ЦиклОбмена.Организация);
		КонецЕсли;
	ИначеЕсли ЦиклОбмена.Тип = Перечисления.ТипыЦикловОбмена.ИндивидуальноеИнформированиеФСГС Тогда
		ЦиклОбмена.Наименование = ТемаПисьмаПоЦиклуОбмена(ЦиклОбмена);
	ИначеЕсли ЦиклОбмена.Тип = Перечисления.ТипыЦикловОбмена.ПисьменноеОбращениеВФСГС Тогда
		ЦиклОбмена.Наименование = ТемаПисьмаПоЦиклуОбмена(ЦиклОбмена);
	ИначеЕсли ЦиклОбмена.Тип = Перечисления.ТипыЦикловОбмена.РассылкаФСГС Тогда
		ЦиклОбмена.Наименование = ТемаПисьмаПоЦиклуОбмена(ЦиклОбмена);
	ИначеЕсли ЦиклОбмена.Тип = Перечисления.ТипыЦикловОбмена.РассылкаШаблоновФСГС Тогда
		ЦиклОбмена.Наименование = ТемаПисьмаПоЦиклуОбмена(ЦиклОбмена);
	КонецЕсли;
	
КонецПроцедуры

Функция ТемаПисьмаПоЦиклуОбмена(ЦиклОбмена)
	
	Если ЦиклОбмена.Тип = Перечисления.ТипыЦикловОбмена.НеформализованнаяПерепискаПФРИсходящие
	ИЛИ ЦиклОбмена.Тип = Перечисления.ТипыЦикловОбмена.ОбращениеНП
	ИЛИ ЦиклОбмена.Тип = Перечисления.ТипыЦикловОбмена.ПисьменноеОбращениеВФСГС Тогда
		Возврат """" + СокрЛП(ЦиклОбмена.Предмет.Наименование) + """ для " + ЦиклОбмена.ВнешняяОрганизация;
	КонецЕсли;
	
	// получаем исходное сообщение
	Если ТипЗнч(ЦиклОбмена) = Тип("СправочникСсылка.ЦиклыОбмена") Тогда
		СсылкаНаЦиклОбмена = ЦиклОбмена;
	Иначе
		СсылкаНаЦиклОбмена = ?(ЦиклОбмена.ЭтоНовый(), ЦиклОбмена.ПолучитьСсылкуНового(), ЦиклОбмена.Ссылка);
	КонецЕсли;
	ТипПервичногоСообщения = ЭлектронныйДокументооборотСКонтролирующимиОрганами.ПолучитьТипПервичногоСообщенияПоЦиклуОбмена(ЦиклОбмена);
	
	ПисьмоВходящее = Неопределено;
	Если ЗначениеЗаполнено(СсылкаНаЦиклОбмена) Тогда
		ПисьмоВходящее = ПолучитьСообщениеЦиклаОбмена(СсылкаНаЦиклОбмена, ТипПервичногоСообщения);
	КонецЕсли;
	Если НЕ ЗначениеЗаполнено(ПисьмоВходящее) Тогда
		Возврат "";
	КонецЕсли;
	
	Тег = "тема";
	// извлекаем вложения расшифрованного входящего сообщения
	Если ТипПервичногоСообщения = Перечисления.ТипыТранспортныхСообщений.ПисьмоВходящееПФР Тогда
		ТипСодержимогоОписаниеПисьма = Перечисления.ТипыСодержимогоТранспортногоКонтейнера.ОписаниеПисьма;
	ИначеЕсли ТипПервичногоСообщения = Перечисления.ТипыТранспортныхСообщений.ПисьмоВходящееФСГС
		  ИЛИ ТипПервичногоСообщения = Перечисления.ТипыТранспортныхСообщений.РассылкаФСГС Тогда
		ТипСодержимогоОписаниеПисьма = Перечисления.ТипыСодержимогоТранспортногоКонтейнера.ОписаниеПисьмаФСГС;
	ИначеЕсли ТипПервичногоСообщения = Перечисления.ТипыТранспортныхСообщений.РассылкаШаблоновФСГС Тогда
		ТипСодержимогоОписаниеПисьма = Перечисления.ТипыСодержимогоТранспортногоКонтейнера.ОписаниеРассылкиШаблоновФСГС;
		Тег = "имя"; // рассылкаШаблонов > шаблон > @имя
	Иначе
		ТипСодержимогоОписаниеПисьма = Перечисления.ТипыСодержимогоТранспортногоКонтейнера.ОписаниеСведенийОбращениеПисьмоРассылка;
	КонецЕсли;
	
	ВложенияОписанияКПисьму = ПолучитьВложенияТранспортногоСообщения(ПисьмоВходящее, Истина, ТипСодержимогоОписаниеПисьма);
	Если ВложенияОписанияКПисьму.Количество() = 0 Тогда
		Возврат "";
	КонецЕсли;
	ВложенияОписаниеКПисьму = ВложенияОписанияКПисьму[0];
	
	// загружаем описание к письму
	ВремФайлОписаниеКПисьму = ПолучитьИмяВременногоФайла();
	ВложенияОписаниеКПисьму.Данные.Получить().Записать(ВремФайлОписаниеКПисьму);
	ДеревоОписанияКПисьму = ЗагрузитьXMLВДеревоЗначений(ВремФайлОписаниеКПисьму);
	ОперацииСФайламиЭДКО.УдалитьВременныйФайл(ВремФайлОписаниеКПисьму);
	Если ДеревоОписанияКПисьму = Неопределено Тогда
		Возврат "";
	КонецЕсли;
	
	УзелТема = ДеревоОписанияКПисьму.Строки.Найти(Тег, "Имя", Истина);
	Если УзелТема = Неопределено Тогда
		Возврат "";
	КонецЕсли;
	Тема = XMLЗначениеВнутр(Тип("Строка"), УзелТема.Значение);
	
	Возврат """" + Тема + """ от " + ЦиклОбмена.ВнешняяОрганизация;
	
КонецФункции

Процедура СоздатьПредметЦиклаОбмена(Объект)
	
	Если Объект.Тип = Перечисления.ТипыТранспортныхСообщений.ПисьмоВходящееПФР Тогда
		
		НачатьТранзакцию();
		
		// извлекаем вложения расшифрованного входящего сообщения
		Вложения = ПолучитьВложенияТранспортногоСообщения(Объект.Ссылка, Истина);
		
		ВложенияПисьма = Вложения.НайтиСтроки(Новый Структура("Тип", Перечисления.ТипыСодержимогоТранспортногоКонтейнера.Письмо));
		ВложенияПриложенияКПисьму = Вложения.НайтиСтроки(Новый Структура("Тип", Перечисления.ТипыСодержимогоТранспортногоКонтейнера.ПисьмоПриложение));
		ВложенияОписанияКПисьму = Вложения.НайтиСтроки(Новый Структура("Тип", Перечисления.ТипыСодержимогоТранспортногоКонтейнера.ОписаниеПисьма));
		
		Если ВложенияПисьма.Количество() = 0 Тогда
			ТекстСообщения = НСтр("ru = 'Не удалось создать входящее письмо: некорректная структура входящего транспортного пакета.'");
			ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
			ОтменитьТранзакцию();
			Возврат;
		КонецЕсли;
		
		Если ВложенияОписанияКПисьму.Количество() = 0 Тогда
			ТекстСообщения = НСтр("ru = 'Не удалось создать входящее письмо: некорректная структура входящего транспортного пакета.'");
			ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
			ОтменитьТранзакцию();
			Возврат;
		КонецЕсли;
		
		ВложениеПисьмо = ВложенияПисьма[0];
		ВложенияОписаниеКПисьму = ВложенияОписанияКПисьму[0];
		
		// загружаем описание к письму
		ВремФайлОписаниеКПисьму = ПолучитьИмяВременногоФайла();
		ВложенияОписаниеКПисьму.Данные.Получить().Записать(ВремФайлОписаниеКПисьму);
		ДеревоОписанияКПисьму = ЗагрузитьXMLВДеревоЗначений(ВремФайлОписаниеКПисьму);
		ОперацииСФайламиЭДКО.УдалитьВременныйФайл(ВремФайлОписаниеКПисьму);
		Если ДеревоОписанияКПисьму = Неопределено Тогда
			ТекстСообщения = НСтр("ru = 'Не удалось создать входящее письмо: некорректная структура описания к письму.'");
			ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
			ОтменитьТранзакцию();
			Возврат;
		КонецЕсли;
		
		// извлекаем тему, идентификатор и дату-время отправки из описания письма
		УзелОписаниеПисьма = ДеревоОписанияКПисьму.Строки.Найти("описаниеПисьма", "Имя");
		Если УзелОписаниеПисьма = Неопределено Тогда
			ТекстСообщения = НСтр("ru = 'Не удалось создать входящее письмо: некорректная структура описания к письму.'");
			ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
			ОтменитьТранзакцию();
			Возврат;
		КонецЕсли;
		
		УзелТема = УзелОписаниеПисьма.Строки.Найти("тема", "Имя");
		УзелДатаВремяОтправки = УзелОписаниеПисьма.Строки.Найти("датаВремяОтправки", "Имя");
		УзелИдентификаторПисьма = УзелОписаниеПисьма.Строки.Найти("идентификаторПисьма", "Имя");
		Если УзелТема = Неопределено ИЛИ УзелДатаВремяОтправки = Неопределено ИЛИ УзелИдентификаторПисьма = Неопределено Тогда
			ТекстСообщения = НСтр("ru = 'Не удалось создать входящее письмо: некорректная структура описания к письму.'");
			ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
			ОтменитьТранзакцию();
			Возврат;
		КонецЕсли;
		Тема = XMLЗначениеВнутр(Тип("Строка"), УзелТема.Значение);
		ДатаВремяОтправки = XMLЗначениеВнутр(Тип("Дата"), УзелДатаВремяОтправки.Значение);
		ИдентификаторПисьма = XMLЗначениеВнутр(Тип("Строка"), УзелИдентификаторПисьма.Значение);
		
		УзелОтветНаПисьмо = УзелОписаниеПисьма.Строки.Найти("ответНаПисьмо", "Имя");
		Если УзелОтветНаПисьмо <> Неопределено Тогда
			ИдентификаторОснования = XMLЗначениеВнутр(Тип("Строка"), УзелОтветНаПисьмо.Значение);
		Иначе
			ИдентификаторОснования = "";
		КонецЕсли;
		
		УзелКатегорияПисьма = УзелОписаниеПисьма.Строки.Найти("категорияПисьма", "Имя");
		Если ЗначениеЗаполнено(УзелКатегорияПисьма) Тогда
			СтрКатегорияПисьма = XMLЗначениеВнутр(Тип("Строка"), УзелКатегорияПисьма.Значение);
			ПризнакРетроконверсии = (нрег(СтрКатегорияПисьма) = "ретроконверсия");
			ПризнакРассылки = (нрег(СтрКатегорияПисьма) = "рассылка");
		Иначе
			ПризнакРетроконверсии = Ложь;
			ПризнакРассылки = Ложь;
		КонецЕсли;
		
		Если (ПризнакРассылки) И ЗначениеЗаполнено(Объект.ЦиклОбмена) Тогда
			ЦиклыОбмена = Объект.ЦиклОбмена.ПолучитьОбъект();
			ЦиклыОбмена.Тип = Перечисления.ТипыЦикловОбмена.РассылкаПФР;
			Попытка
				ЦиклыОбмена.Записать();
			Исключение
				ТекстСообщения = СтроковыеФункцииКлиентСервер.ПодставитьПараметрыВСтроку(
					НСтр("ru = 'Ошибка записи ЦО, связанного с входящим письмом от ПФР:
					|%1'"), ИнформацияОбОшибке().Описание);					
				ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
				ОтменитьТранзакцию();
				Возврат;
			КонецПопытки;		
		КонецЕсли;		
		
		// считываем содержание
		ВремФайлПисьмо = ПолучитьИмяВременногоФайла();
		ВложениеПисьмо.Данные.Получить().Записать(ВремФайлПисьмо);
		Содержание = ПрочитатьТекстИзФайла(ВремФайлПисьмо);
		ОперацииСФайламиЭДКО.УдалитьВременныйФайл(ВремФайлПисьмо);
		
		// пытаемся найти готовые письма по идентификатору начиная с последнего
		Отбор = Новый Структура("Идентификатор", ИдентификаторПисьма);
		Выборка = Справочники.ПерепискаСКонтролирующимиОрганами.Выбрать(,, Отбор, "ДатаСообщения Убыв");		
		Письмо = Неопределено;
		Пока Выборка.Следующий()  Цикл
			Если Выборка.ПометкаУдаления Тогда
				Продолжить;
			КонецЕсли;
			Если СокрЛП(Выборка.Наименование) = СокрЛП(Тема) И (Выборка.ДатаОтправки = ДатаВремяОтправки) Тогда 
    			Письмо = Выборка.ПолучитьОбъект();
				Прервать;
			КонецЕсли;
		КонецЦикла;
	    		
		Если Письмо = Неопределено Тогда // создаем письмо если не найдено
			Письмо = Справочники.ПерепискаСКонтролирующимиОрганами.СоздатьЭлемент();
		КонецЕсли;
		Письмо.Наименование = Тема;
		Письмо.Тип = Перечисления.ТипыПерепискиСКонтролирующимиОрганами.ПерепискаСПФР;
		Письмо.Статус = Перечисления.СтатусыПисем.Полученное;
		Письмо.Отправитель = Объект.ЦиклОбмена.ВнешняяОрганизация;
		Письмо.Получатель = Объект.ЦиклОбмена.Организация;
		Письмо.Содержание = ПроверитьЗаменитьНедопустимыеСимволы(Содержание);
		Письмо.ДатаСообщения = ТекущаяДатаСеанса();
		Письмо.ДатаОтправки = ДатаВремяОтправки;
		Письмо.Идентификатор = ИдентификаторПисьма;
		Письмо.ИдентификаторОснования = ИдентификаторОснования;
		Письмо.Ретроконверсия = ПризнакРетроконверсии;
		Попытка
			Письмо.Записать();
		Исключение
			ТекстСообщения = СтроковыеФункцииКлиентСервер.ПодставитьПараметрыВСтроку(НСтр("ru = 'Ошибка записи входящего письма от ПФР:%1'"), 
																						Символы.ПС + ИнформацияОбОшибке().Описание);
			ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
			ОтменитьТранзакцию();
			Возврат;
		КонецПопытки;
		
		// добавляем вложения к письму
		Для Каждого ПриложениеКПисьму Из ВложенияПриложенияКПисьму Цикл
			ДобавитьВложенияПисьма(Письмо.Ссылка, ПриложениеКПисьму.ИмяФайла, ПриложениеКПисьму.Данные, ТипСодержимогоПоИмениФайла(ПриложениеКПисьму.ИмяФайла, Истина), ПриложениеКПисьму.Размер);
		КонецЦикла;
		
		// обновляем свойства родительского цикла обмена
		Если НЕ ЭлектронныйДокументооборотСКонтролирующимиОрганами.ОбновитьСвойстваЦиклаОбмена(Объект.ЦиклОбмена, Письмо.Ссылка) Тогда
			ОтменитьТранзакцию();
			Возврат;
		КонецЕсли;
		
		ЗафиксироватьТранзакцию();
		
		// добавляем письмо в массив полученных писем
		ДополнитьСписокПолученныхПисем(Письмо.Ссылка);
		
	ИначеЕсли Объект.Тип = Перечисления.ТипыТранспортныхСообщений.ПисьмоНО Тогда
		
		НачатьТранзакцию();
		
		// извлекаем вложения расшифрованного входящего сообщения
		Вложения = ПолучитьВложенияТранспортногоСообщения(Объект.Ссылка, Истина);
		
		ВложенияПисьма = Вложения.НайтиСтроки(Новый Структура("Тип", Перечисления.ТипыСодержимогоТранспортногоКонтейнера.Письмо));
		ВложенияПриложенияКПисьму = Вложения.НайтиСтроки(Новый Структура("Тип", Перечисления.ТипыСодержимогоТранспортногоКонтейнера.Приложение));
		ВложенияОписанияКПисьму = Вложения.НайтиСтроки(Новый Структура("Тип", Перечисления.ТипыСодержимогоТранспортногоКонтейнера.ОписаниеСведенийОбращениеПисьмоРассылка));
		
		Если ВложенияПисьма.Количество() = 0 Тогда
			ТекстСообщения = НСтр("ru = 'Не удалось создать входящее письмо: некорректная структура входящего транспортного пакета.'");
			ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
			ОтменитьТранзакцию();
			Возврат;
		КонецЕсли;
		
		Если ВложенияОписанияКПисьму.Количество() = 0 Тогда
			ТекстСообщения = НСтр("ru = 'Не удалось создать входящее письмо: некорректная структура входящего транспортного пакета.'");
			ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
			ОтменитьТранзакцию();
			Возврат;
		КонецЕсли;
		
		ВложениеПисьмо = ВложенияПисьма[0];
		ВложенияОписаниеКПисьму = ВложенияОписанияКПисьму[0];
		
		// загружаем описание к письму
		ВремФайлОписаниеКПисьму = ПолучитьИмяВременногоФайла();
		ВложенияОписаниеКПисьму.Данные.Получить().Записать(ВремФайлОписаниеКПисьму);
		ДеревоОписанияКПисьму = ЗагрузитьXMLВДеревоЗначений(ВремФайлОписаниеКПисьму);
		ОперацииСФайламиЭДКО.УдалитьВременныйФайл(ВремФайлОписаниеКПисьму);
		Если ДеревоОписанияКПисьму = Неопределено Тогда
			ТекстСообщения = НСтр("ru = 'Не удалось создать входящее письмо: некорректная структура описания к письму.'");
			ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
			ОтменитьТранзакцию();
			Возврат;
		КонецЕсли;
		
		// извлекаем тему и идентификатор исходного письма, ответом на которое является данное
		УзелОписание = ДеревоОписанияКПисьму.Строки.Найти("описание", "Имя");
		Если УзелОписание = Неопределено Тогда
			ТекстСообщения = НСтр("ru = 'Не удалось создать входящее письмо: некорректная структура описания к письму.'");
			ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
			ОтменитьТранзакцию();
			Возврат;
		КонецЕсли;
		
		// тема
		УзелТема = УзелОписание.Строки.Найти("тема", "Имя");
		Если УзелТема = Неопределено Тогда
			ТекстСообщения = НСтр("ru = 'Не удалось создать входящее письмо: некорректная структура описания к письму.'");
			ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
			ОтменитьТранзакцию();
			Возврат;
		КонецЕсли;
		Тема = XMLЗначениеВнутр(Тип("Строка"), УзелТема.Значение);
		
		// идентификатор исходного письма
		УзелОтветНаПисьмо = УзелОписание.Строки.Найти("ответНаПисьмо", "Имя");
		Если УзелОтветНаПисьмо <> Неопределено Тогда
			ИдентификаторОснования = XMLЗначениеВнутр(Тип("Строка"), УзелОтветНаПисьмо.Значение);
		Иначе
			ИдентификаторОснования = "";
		КонецЕсли;
		
		// считываем само письмо
		ВремФайлПисьмо = ПолучитьИмяВременногоФайла();
		ВложениеПисьмо.Данные.Получить().Записать(ВремФайлПисьмо);
		СвойстваПисьма = РазобратьВходящееПисьмоФНС(ВремФайлПисьмо);
		ОперацииСФайламиЭДКО.УдалитьВременныйФайл(ВремФайлПисьмо);
		Если СвойстваПисьма = Неопределено Тогда
			ТекстСообщения = НСтр("ru = 'Не удалось создать входящее письмо: не удалось разобрать файл письма из состава входящего пакета.'");
			ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
			ОтменитьТранзакцию();
			Возврат;
		КонецЕсли;
		
		// создаем письмо в ИБ
		Письмо = Справочники.ПерепискаСКонтролирующимиОрганами.СоздатьЭлемент();
		Письмо.Наименование = Тема;
		Письмо.Тип = Перечисления.ТипыПерепискиСКонтролирующимиОрганами.ПерепискаСФНС;
		Письмо.Статус = Перечисления.СтатусыПисем.Полученное;
		Письмо.Отправитель = Объект.ЦиклОбмена.ВнешняяОрганизация;
		Письмо.Получатель = Объект.ЦиклОбмена.Организация;
		Письмо.Содержание = ПроверитьЗаменитьНедопустимыеСимволы(СвойстваПисьма.Текст);
		Письмо.ДатаСообщения = ТекущаяДатаСеанса();
		Письмо.ДатаОтправки = ?(ЗначениеЗаполнено(СвойстваПисьма.Дата), СвойстваПисьма.Дата, Объект.ДатаТранспорта);
		Письмо.Идентификатор = Объект.ЦиклОбмена.Идентификатор;
		Письмо.ИдентификаторОснования = ИдентификаторОснования;
		Попытка
			Письмо.Записать();
		Исключение
			ТекстСообщения = СтроковыеФункцииКлиентСервер.ПодставитьПараметрыВСтроку(
				НСтр("ru = 'Ошибка записи входящего письма от ФНС:%1'"), Символы.ПС + ИнформацияОбОшибке().Описание);
			ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
			ОтменитьТранзакцию();
			Возврат;
		КонецПопытки;
		
		// добавляем вложения к письму
		Для Каждого ПриложениеКПисьму Из ВложенияПриложенияКПисьму Цикл
			ДобавитьВложенияПисьма(Письмо.Ссылка, ПриложениеКПисьму.ИмяФайла, ПриложениеКПисьму.Данные, ТипСодержимогоПоИмениФайла(ПриложениеКПисьму.ИмяФайла), ПриложениеКПисьму.Размер);
		КонецЦикла;
		
		// обновляем свойства родительского цикла обмена
		Если НЕ ЭлектронныйДокументооборотСКонтролирующимиОрганами.ОбновитьСвойстваЦиклаОбмена(Объект.ЦиклОбмена, Письмо.Ссылка) Тогда
			ОтменитьТранзакцию();
			Возврат;
		КонецЕсли;
		
		ЗафиксироватьТранзакцию();
		
		// добавляем письмо в массив полученных писем
		ДополнитьСписокПолученныхПисем(Письмо.Ссылка);
		
	ИначеЕсли Объект.Тип = Перечисления.ТипыТранспортныхСообщений.РассылкаНО Тогда
		
		НачатьТранзакцию();
		
		// извлекаем вложения расшифрованного входящего сообщения
		Вложения = ПолучитьВложенияТранспортногоСообщения(Объект.Ссылка, Истина);
		
		ВложенияПисьма = Вложения.НайтиСтроки(Новый Структура("Тип", Перечисления.ТипыСодержимогоТранспортногоКонтейнера.Рассылка));
		ВложенияПриложенияКПисьму = Вложения.НайтиСтроки(Новый Структура("Тип", Перечисления.ТипыСодержимогоТранспортногоКонтейнера.Приложение));
		ВложенияОписанияКПисьму = Вложения.НайтиСтроки(Новый Структура("Тип", Перечисления.ТипыСодержимогоТранспортногоКонтейнера.ОписаниеСведенийОбращениеПисьмоРассылка));
		
		Если ВложенияПисьма.Количество() = 0 Тогда
			ТекстСообщения = НСтр("ru = 'Не удалось создать входящее письмо: некорректная структура входящего транспортного пакета.'");
			ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
			ОтменитьТранзакцию();
			Возврат;
		КонецЕсли;
		
		Если ВложенияОписанияКПисьму.Количество() = 0 Тогда
			ТекстСообщения = НСтр("ru = 'Не удалось создать входящее письмо: некорректная структура входящего транспортного пакета.'");
			ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
			ОтменитьТранзакцию();
			Возврат;
		КонецЕсли;
		
		ВложениеПисьмо = ВложенияПисьма[0];
		ВложенияОписаниеКПисьму = ВложенияОписанияКПисьму[0];
		
		// загружаем описание к письму
		ВремФайлОписаниеКПисьму = ПолучитьИмяВременногоФайла();
		ВложенияОписаниеКПисьму.Данные.Получить().Записать(ВремФайлОписаниеКПисьму);
		ДеревоОписанияКПисьму = ЗагрузитьXMLВДеревоЗначений(ВремФайлОписаниеКПисьму);
		ОперацииСФайламиЭДКО.УдалитьВременныйФайл(ВремФайлОписаниеКПисьму);
		Если ДеревоОписанияКПисьму = Неопределено Тогда
			ТекстСообщения = НСтр("ru = 'Не удалось создать входящее письмо: некорректная структура описания к письму.'");
			ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
			ОтменитьТранзакцию();
			Возврат;
		КонецЕсли;
		
		// извлекаем тему и идентификатор исходного письма, ответом на которое является данное
		УзелОписание = ДеревоОписанияКПисьму.Строки.Найти("описание", "Имя");
		Если УзелОписание = Неопределено Тогда
			ТекстСообщения = НСтр("ru = 'Не удалось создать входящее письмо: некорректная структура описания к письму.'");
			ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
			ОтменитьТранзакцию();
			Возврат;
		КонецЕсли;
		
		// тема
		УзелТема = УзелОписание.Строки.Найти("тема", "Имя");
		Если УзелТема = Неопределено Тогда
			ТекстСообщения = НСтр("ru = 'Не удалось создать входящее письмо: некорректная структура описания к письму.'");
			ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
			ОтменитьТранзакцию();
			Возврат;
		КонецЕсли;
		Тема = XMLЗначениеВнутр(Тип("Строка"), УзелТема.Значение);
		
		// идентификатор исходного письма
		УзелОтветНаПисьмо = УзелОписание.Строки.Найти("ответНаПисьмо", "Имя");
		Если УзелОтветНаПисьмо <> Неопределено Тогда
			ИдентификаторОснования = XMLЗначениеВнутр(Тип("Строка"), УзелОтветНаПисьмо.Значение);
		Иначе
			ИдентификаторОснования = "";
		КонецЕсли;
		
		// считываем само письмо
		ВремФайлПисьмо = ПолучитьИмяВременногоФайла();
		ВложениеПисьмо.Данные.Получить().Записать(ВремФайлПисьмо);
		ТекстПисьма = ПрочитатьТекстИзФайла(ВремФайлПисьмо, "windows-1251");
		ОперацииСФайламиЭДКО.УдалитьВременныйФайл(ВремФайлПисьмо);
		
		// создаем письмо в ИБ
		Письмо = Справочники.ПерепискаСКонтролирующимиОрганами.СоздатьЭлемент();
		Письмо.Наименование = Тема;
		Письмо.Тип = Перечисления.ТипыПерепискиСКонтролирующимиОрганами.ПерепискаСФНС;
		Письмо.Статус = Перечисления.СтатусыПисем.Полученное;
		Письмо.Отправитель = Объект.ЦиклОбмена.ВнешняяОрганизация;
		Письмо.Получатель = Объект.ЦиклОбмена.Организация;
		Письмо.Содержание = ПроверитьЗаменитьНедопустимыеСимволы(ТекстПисьма);
		Письмо.ДатаСообщения = ТекущаяДатаСеанса();
		Письмо.ДатаОтправки = Объект.ДатаТранспорта;
		Письмо.Идентификатор = Объект.ЦиклОбмена.Идентификатор;
		Письмо.ИдентификаторОснования = ИдентификаторОснования;
		Попытка
			Письмо.Записать();
		Исключение
			ТекстСообщения = СтроковыеФункцииКлиентСервер.ПодставитьПараметрыВСтроку(
				НСтр("ru = 'Ошибка записи входящего письма от ФНС:%1'"), Символы.ПС + ИнформацияОбОшибке().Описание);
			ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
			ОтменитьТранзакцию();
			Возврат;
		КонецПопытки;
		
		// добавляем вложения к письму
		Для Каждого ПриложениеКПисьму Из ВложенияПриложенияКПисьму Цикл
			ДобавитьВложенияПисьма(Письмо.Ссылка, ПриложениеКПисьму.ИмяФайла, ПриложениеКПисьму.Данные, ТипСодержимогоПоИмениФайла(ПриложениеКПисьму.ИмяФайла), ПриложениеКПисьму.Размер);
		КонецЦикла;
		
		// обновляем свойства родительского цикла обмена
		Если НЕ ЭлектронныйДокументооборотСКонтролирующимиОрганами.ОбновитьСвойстваЦиклаОбмена(Объект.ЦиклОбмена, Письмо.Ссылка) Тогда
			ОтменитьТранзакцию();
			Возврат;
		КонецЕсли;
		
		ЗафиксироватьТранзакцию();
		
		// добавляем письмо в массив полученных писем
		ДополнитьСписокПолученныхПисем(Письмо.Ссылка);
		
	ИначеЕсли Объект.Тип = Перечисления.ТипыТранспортныхСообщений.ДокументНО Тогда

		// извлекаем вложения расшифрованного входящего сообщения
		Вложения = ПолучитьВложенияТранспортногоСообщения(Объект.Ссылка, Истина);
		
		ВложенияДокумент	= Вложения.НайтиСтроки(Новый Структура("Тип", Перечисления.ТипыСодержимогоТранспортногоКонтейнера.Документ));
		ВложенияПриложение	= Вложения.НайтиСтроки(Новый Структура("Тип", Перечисления.ТипыСодержимогоТранспортногоКонтейнера.Приложение));
		
		Если ВложенияДокумент.Количество() = 0 
		 ИЛИ ВложенияПриложение.Количество() = 0 Тогда
			ТекстСообщения = НСтр("ru = 'Не удалось создать документ налогового органа: некорректная структура входящего транспортного пакета.'");
			ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);

			Возврат;
		КонецЕсли;
		
		ВложениеДокумент = ВложенияДокумент[0];
		
		// считываем документ
		ВремФайлДокумент = ПолучитьИмяВременногоФайла();
		ВложениеДокумент.Данные.Получить().Записать(ВремФайлДокумент);
		
		СвойстваОписиВходящихДокументов = ПолучитьСвойстваОписиВходящихДокументов(ВремФайлДокумент);
		
		ОперацииСФайламиЭДКО.УдалитьВременныйФайл(ВремФайлДокумент);
		
		ТаблицаДокументов = СвойстваОписиВходящихДокументов.ТаблицаДокументов;
		
		Если ТаблицаДокументов.Количество() = 0 Тогда
			ТекстСообщения = НСтр("ru = 'Не удалось создать документ налогового органа: у входящей описи документов нет ни одного вложенного файла.'");
			ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
			Возврат;
		КонецЕсли;
			
		НайденнаяОпись = Справочники.ОписиВходящихДокументовИзНалоговыхОрганов.НайтиПоРеквизиту(
			"Идентификатор", СвойстваОписиВходящихДокументов.Идентификатор);
		
		Если ЗначениеЗаполнено(НайденнаяОпись) 
			И НайденнаяОпись.ИдентификаторОснования = СвойстваОписиВходящихДокументов.ИдентификаторОснования Тогда
			
			// Если опись уже есть, а цикл обмена новый, то в цикл обмена предмет не записывался.
			Если НЕ ЭлектронныйДокументооборотСКонтролирующимиОрганами.ОбновитьСвойстваЦиклаОбмена(Объект.ЦиклОбмена, НайденнаяОпись) Тогда
				Если ТранзакцияАктивна() Тогда 
					ТекстСообщения = НСтр("ru = 'Не удалось создать документ налогового органа.'");
					ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
					ОтменитьТранзакцию();
				КонецЕсли;
			КонецЕсли;
			
			Возврат;
		КонецЕсли;
		
		НачатьТранзакцию();
		НоваяОпись = НоваяОписьВходящихДокументовИзНалоговыхОрганов(
			СвойстваОписиВходящихДокументов,
			ВложенияПриложение,
			Объект);

		Если НоваяОпись = Неопределено Тогда
			
			Если ТранзакцияАктивна() Тогда 
				ТекстСообщения = НСтр("ru = 'Не удалось создать документ налогового органа.'");
				ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
				ОтменитьТранзакцию();
			КонецЕсли;
			
			Возврат;
		КонецЕсли;
		
		// добавляем письмо в массив полученных писем
		ДополнитьСписокПолученныхПисем(НоваяОпись.Ссылка);
		
		ЗафиксироватьТранзакцию();
		
	ИначеЕсли Объект.Тип = Перечисления.ТипыТранспортныхСообщений.ПисьмоВходящееФСГС Тогда
		
		НачатьТранзакцию();
		
		// извлекаем вложения расшифрованного входящего сообщения
		Вложения = ПолучитьВложенияТранспортногоСообщения(Объект.Ссылка, Истина);
		
		ВложенияПисьма = Вложения.НайтиСтроки(Новый Структура("Тип", Перечисления.ТипыСодержимогоТранспортногоКонтейнера.ПисьмоФСГС));
		ВложенияПриложенияКПисьму = Вложения.НайтиСтроки(Новый Структура("Тип", Перечисления.ТипыСодержимогоТранспортногоКонтейнера.ПриложениеПисьмаФСГС));
		ВложенияОписанияКПисьму = Вложения.НайтиСтроки(Новый Структура("Тип", Перечисления.ТипыСодержимогоТранспортногоКонтейнера.ОписаниеПисьмаФСГС));
		
		Если ВложенияПисьма.Количество() = 0 Тогда
			ТекстСообщения = НСтр("ru = 'Не удалось создать входящее письмо: некорректная структура входящего транспортного пакета.'");
			ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
			ОтменитьТранзакцию();
			Возврат;
		КонецЕсли;
		
		Если ВложенияОписанияКПисьму.Количество() = 0 Тогда
			ТекстСообщения = НСтр("ru = 'Не удалось создать входящее письмо: некорректная структура входящего транспортного пакета.'");
			ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
			ОтменитьТранзакцию();
			Возврат;
		КонецЕсли;
		
		ВложениеПисьмо = ВложенияПисьма[0];
		ВложенияОписаниеКПисьму = ВложенияОписанияКПисьму[0];
		
		// загружаем описание к письму
		ВремФайлОписаниеКПисьму = ПолучитьИмяВременногоФайла();
		ВложенияОписаниеКПисьму.Данные.Получить().Записать(ВремФайлОписаниеКПисьму);
		ДеревоОписанияКПисьму = ЗагрузитьXMLВДеревоЗначений(ВремФайлОписаниеКПисьму);
		ОперацииСФайламиЭДКО.УдалитьВременныйФайл(ВремФайлОписаниеКПисьму);
		Если ДеревоОписанияКПисьму = Неопределено Тогда
			ТекстСообщения = НСтр("ru = 'Не удалось создать входящее письмо: некорректная структура описания к письму.'");
			ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
			ОтменитьТранзакцию();
			Возврат;
		КонецЕсли;
		
		// извлекаем тему, идентификатор из описания письма
		УзелОписаниеПисьма = ДеревоОписанияКПисьму.Строки.Найти("описание", "Имя");
		Если УзелОписаниеПисьма = Неопределено Тогда
			ТекстСообщения = НСтр("ru = 'Не удалось создать входящее письмо: некорректная структура описания к письму.'");
			ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
			ОтменитьТранзакцию();
			Возврат;
		КонецЕсли;
		
		УзелТема = УзелОписаниеПисьма.Строки.Найти("тема", "Имя");
		Если УзелТема = Неопределено Тогда
			ТекстСообщения = НСтр("ru = 'Не удалось создать входящее письмо: некорректная структура описания к письму.'");
			ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
			ОтменитьТранзакцию();
			Возврат;
		КонецЕсли;
		Тема = XMLЗначениеВнутр(Тип("Строка"), УзелТема.Значение);
		
		УзелОтветНа = УзелОписаниеПисьма.Строки.Найти("ответНа", "Имя");
		ИдентификаторОснования = ?(УзелОтветНа = Неопределено, "", XMLЗначениеВнутр(Тип("Строка"), УзелОтветНа.Значение));
		
		УзелКатегория = УзелОписаниеПисьма.Строки.Найти("категория", "Имя");
		Категория = ?(УзелКатегория = Неопределено, "", XMLЗначениеВнутр(Тип("Строка"), УзелКатегория.Значение));
		
		ПризнакРетроконверсии = Ложь;
		Если ЗначениеЗаполнено(Категория) Тогда
			ПризнакРетроконверсии = (нрег(Категория) = "ретроконверсия");
		КонецЕсли;
		
		// считываем содержание
		ВремФайлПисьмо = ПолучитьИмяВременногоФайла();
		ВложениеПисьмо.Данные.Получить().Записать(ВремФайлПисьмо);
		Содержание = ПрочитатьТекстИзФайла(ВремФайлПисьмо);
		ОперацииСФайламиЭДКО.УдалитьВременныйФайл(ВремФайлПисьмо);
		
		// создаем письмо
		Письмо = Справочники.ПерепискаСКонтролирующимиОрганами.СоздатьЭлемент();
		Письмо.Наименование = Тема;
		Письмо.Тип = Перечисления.ТипыПерепискиСКонтролирующимиОрганами.ПерепискаСФСГС;
		Письмо.Статус = Перечисления.СтатусыПисем.Полученное;
		Письмо.Отправитель = Объект.ЦиклОбмена.ВнешняяОрганизация;
		Письмо.Получатель = Объект.ЦиклОбмена.Организация;
		Письмо.Содержание = ПроверитьЗаменитьНедопустимыеСимволы(Содержание);
		Письмо.ДатаСообщения = ТекущаяДатаСеанса();
		Письмо.Идентификатор = Объект.ИдентификаторСообщения;
		Письмо.ИдентификаторОснования = ИдентификаторОснования;
		Письмо.Ретроконверсия = ПризнакРетроконверсии;
		Попытка
			Письмо.Записать();
		Исключение
			ТекстСообщения = СтроковыеФункцииКлиентСервер.ПодставитьПараметрыВСтроку(НСтр("ru = 'Ошибка записи входящего письма от Росстата:%1'"), 
																						Символы.ПС + ИнформацияОбОшибке().Описание);
			ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
			ОтменитьТранзакцию();
			Возврат;
		КонецПопытки;
		
		// добавляем вложения к письму
		Для Каждого ПриложениеКПисьму Из ВложенияПриложенияКПисьму Цикл
			ДобавитьВложенияПисьма(Письмо.Ссылка, ПриложениеКПисьму.ИмяФайла, ПриложениеКПисьму.Данные, ТипСодержимогоПоИмениФайла(ПриложениеКПисьму.ИмяФайла, Истина), ПриложениеКПисьму.Размер);
		КонецЦикла;
		
		// обновляем свойства родительского цикла обмена
		Если НЕ ЭлектронныйДокументооборотСКонтролирующимиОрганами.ОбновитьСвойстваЦиклаОбмена(Объект.ЦиклОбмена, Письмо.Ссылка) Тогда
			ОтменитьТранзакцию();
			Возврат;
		КонецЕсли;
		
		ЗафиксироватьТранзакцию();
		
		// добавляем письмо в массив полученных писем
		ДополнитьСписокПолученныхПисем(Письмо.Ссылка);
		
	ИначеЕсли Объект.Тип = Перечисления.ТипыТранспортныхСообщений.РассылкаФСГС
		ИЛИ Объект.Тип = Перечисления.ТипыТранспортныхСообщений.РассылкаШаблоновФСГС Тогда
		
		НачатьТранзакцию();
		
		// извлекаем вложения расшифрованного входящего сообщения
		Вложения = ПолучитьВложенияТранспортногоСообщения(Объект.Ссылка, Истина);
					
		ТегРодитель = "описание";
		ТегВложенный = "тема";
		
		АвтоопределениеКодировкиXML = Ложь;
		
		Если Объект.Тип = Перечисления.ТипыТранспортныхСообщений.РассылкаФСГС Тогда
			ВложенияПисьма = Вложения.НайтиСтроки(Новый Структура("Тип", Перечисления.ТипыСодержимогоТранспортногоКонтейнера.РассылкаФСГС));
			ВложенияОписанияКПисьму = Вложения.НайтиСтроки(Новый Структура("Тип", Перечисления.ТипыСодержимогоТранспортногоКонтейнера.ОписаниеПисьмаФСГС));
		ИначеЕсли Объект.Тип = Перечисления.ТипыТранспортныхСообщений.РассылкаШаблоновФСГС Тогда
			ВложенияПисьма = Вложения.НайтиСтроки(Новый Структура("Тип", Перечисления.ТипыСодержимогоТранспортногоКонтейнера.ШаблонФСГС));
			ВложенияОписанияКПисьму = Вложения.НайтиСтроки(Новый Структура("Тип", Перечисления.ТипыСодержимогоТранспортногоКонтейнера.ОписаниеРассылкиШаблоновФСГС));
			ТегРодитель = "рассылкаШаблонов";
			ТегВложенный = "шаблон";
			
			АвтоопределениеКодировкиXML = Истина;
		КонецЕсли;		
		ВложенияПриложенияКПисьму = Вложения.НайтиСтроки(Новый Структура("Тип", Перечисления.ТипыСодержимогоТранспортногоКонтейнера.ПриложениеПисьмаФСГС));
				
		Если ВложенияПисьма.Количество() = 0 Тогда
			ТекстСообщения = НСтр("ru = 'Не удалось создать входящее письмо: некорректная структура входящего транспортного пакета.'");
			ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
			ОтменитьТранзакцию();
			Возврат;
		КонецЕсли;
		
		Если ВложенияОписанияКПисьму.Количество() = 0 Тогда
			ТекстСообщения = НСтр("ru = 'Не удалось создать входящее письмо: некорректная структура входящего транспортного пакета.'");
			ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
			ОтменитьТранзакцию();
			Возврат;
		КонецЕсли;
		
		ВложениеПисьмо = ВложенияПисьма[0];
		ВложенияОписаниеКПисьму = ВложенияОписанияКПисьму[0];
		
		// загружаем описание к письму
		ВремФайлОписаниеКПисьму = ПолучитьИмяВременногоФайла();
		ВложенияОписаниеКПисьму.Данные.Получить().Записать(ВремФайлОписаниеКПисьму);
		ДеревоОписанияКПисьму = ЗагрузитьXMLВДеревоЗначений(ВремФайлОписаниеКПисьму);
		ОперацииСФайламиЭДКО.УдалитьВременныйФайл(ВремФайлОписаниеКПисьму);
		Если ДеревоОписанияКПисьму = Неопределено Тогда
			ТекстСообщения = НСтр("ru = 'Не удалось создать входящее письмо: некорректная структура описания к письму.'");
			ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
			ОтменитьТранзакцию();
			Возврат;
		КонецЕсли;
		
		// извлекаем тему, идентификатор из описания письма
		УзелОписаниеПисьма = ДеревоОписанияКПисьму.Строки.Найти(ТегРодитель, "Имя");
		Если УзелОписаниеПисьма = Неопределено Тогда
			ТекстСообщения = НСтр("ru = 'Не удалось создать входящее письмо: некорректная структура описания к письму.'");
			ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
			ОтменитьТранзакцию();
			Возврат;
		КонецЕсли;
		
		УзелТема = УзелОписаниеПисьма.Строки.Найти(ТегВложенный, "Имя");
		Если УзелТема = Неопределено Тогда
			ТекстСообщения = НСтр("ru = 'Не удалось создать входящее письмо: некорректная структура описания к письму.'");
			ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
			ОтменитьТранзакцию();
			Возврат;
		КонецЕсли;
		
		Если ТегВложенный = "шаблон" Тогда //	рассылкаШаблонов > шаблон > @имя
			УзелТема = УзелТема.Строки.Найти("имя", "Имя");
			Если УзелТема <> Неопределено Тогда
				Тема = СокрЛП(XMLЗначениеВнутр(Тип("Строка"), УзелТема.Значение));
			КонецЕсли;
		Иначе
			Тема = СокрЛП(XMLЗначениеВнутр(Тип("Строка"), УзелТема.Значение));
		КонецЕсли;
		
		УзелОтветНа = УзелОписаниеПисьма.Строки.Найти("ответНа", "Имя");
		ИдентификаторОснования = ?(УзелОтветНа = Неопределено, "", XMLЗначениеВнутр(Тип("Строка"), УзелОтветНа.Значение));
		
		УзелКатегория = УзелОписаниеПисьма.Строки.Найти("категория", "Имя");
		Категория = ?(УзелКатегория = Неопределено, "", XMLЗначениеВнутр(Тип("Строка"), УзелКатегория.Значение));
		
		ПризнакРетроконверсии = Ложь;
		Если ЗначениеЗаполнено(Категория) Тогда
			ПризнакРетроконверсии = (нрег(Категория) = "ретроконверсия");
		КонецЕсли;
		
		// считываем содержание
		ВремФайлПисьмо = ПолучитьИмяВременногоФайла();
		ВложениеПисьмо.Данные.Получить().Записать(ВремФайлПисьмо);
		Содержание = ПрочитатьТекстИзФайла(ВремФайлПисьмо,, АвтоопределениеКодировкиXML);
		ОперацииСФайламиЭДКО.УдалитьВременныйФайл(ВремФайлПисьмо);
		
		// создаем письмо
		Письмо = Справочники.ПерепискаСКонтролирующимиОрганами.СоздатьЭлемент();
		Письмо.Наименование = Тема;
		Письмо.Тип = Перечисления.ТипыПерепискиСКонтролирующимиОрганами.ПерепискаСФСГС;
		Письмо.Статус = Перечисления.СтатусыПисем.Полученное;
		Письмо.Отправитель = Объект.ЦиклОбмена.ВнешняяОрганизация;
		Письмо.Получатель = Объект.ЦиклОбмена.Организация;
		Письмо.Содержание = ПроверитьЗаменитьНедопустимыеСимволы(Содержание);
		Письмо.ДатаСообщения = ТекущаяДатаСеанса();
		Письмо.Идентификатор = Объект.ИдентификаторСообщения;
		Письмо.ИдентификаторОснования = ИдентификаторОснования;
		Письмо.Ретроконверсия = ПризнакРетроконверсии;
		Попытка
			Письмо.Записать();
		Исключение
			ТекстСообщения = СтроковыеФункцииКлиентСервер.ПодставитьПараметрыВСтроку(НСтр("ru = 'Ошибка записи входящего письма от Росстата:%1'"), 
																						Символы.ПС + ИнформацияОбОшибке().Описание);
			ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
			ОтменитьТранзакцию();
			Возврат;
		КонецПопытки;
		
		// добавляем вложения к письму
		Для Каждого ПриложениеКПисьму Из ВложенияПриложенияКПисьму Цикл
			ДобавитьВложенияПисьма(Письмо.Ссылка, ПриложениеКПисьму.ИмяФайла, ПриложениеКПисьму.Данные, ТипСодержимогоПоИмениФайла(ПриложениеКПисьму.ИмяФайла, Истина), ПриложениеКПисьму.Размер);
		КонецЦикла;
		
		// обновляем свойства родительского цикла обмена
		Если НЕ ЭлектронныйДокументооборотСКонтролирующимиОрганами.ОбновитьСвойстваЦиклаОбмена(Объект.ЦиклОбмена, Письмо.Ссылка) Тогда
			ОтменитьТранзакцию();
			Возврат;
		КонецЕсли;
		
		ЗафиксироватьТранзакцию();
		
		// добавляем письмо в массив полученных писем
		ДополнитьСписокПолученныхПисем(Письмо.Ссылка);
		
	КонецЕсли;
	
КонецПроцедуры

Функция РазобратьВходящееПисьмоФНС(ФайлПисьма)
	
	Дерево = ЗагрузитьXMLВДеревоЗначений(ФайлПисьма);
	Если НЕ ЗначениеЗаполнено(Дерево) Тогда // значит письмо в формате txt - такое возможно, несмотря на формат
		ТекстПисьма = ПрочитатьТекстИзФайла(ФайлПисьма);
		Возврат Новый Структура("Дата, Текст", Неопределено, ТекстПисьма);
	Иначе
		
		УзелПисьмоОтв = Дерево.Строки.Найти("ПисьмоОтв", "Имя", Истина);
		Если УзелПисьмоОтв = Неопределено Тогда
			Возврат Неопределено;
		КонецЕсли;
		
		Результат = Новый Структура("Дата, Текст");
		
		УзелДатаПисьмо = УзелПисьмоОтв.Строки.Найти("ДатаПисьмо", "Имя");
		Если УзелДатаПисьмо <> Неопределено Тогда
			Результат.Дата = ДатаПоСтроке(УзелДатаПисьмо.Значение);
		КонецЕсли;
		
		УзелПисьмоТекст = УзелПисьмоОтв.Строки.Найти("ПисьмоТекст", "Имя");
		Если УзелДатаПисьмо <> Неопределено Тогда
			Результат.Текст = XMLЗначениеВнутр(Тип("Строка"), УзелПисьмоТекст.Значение);
		КонецЕсли;
		
		Возврат Результат;
		
	КонецЕсли;
	
КонецФункции

Функция ПолучитьСвойстваОписиВходящихДокументов(ФайлДокумент) Экспорт
	
	СтруктураСвойств = Новый Структура;
	
	ТаблицаДокументов = Новый Массив;
		
	ДеревоXML = ЗагрузитьXMLВДеревоЗначений(ФайлДокумент);
	КодНО = ИзвлечьДанное(ДеревоXML, "КодНО");
	
	КНДТребования = ИзвлечьДанное(ДеревоXML, "КНД");
	ЭтоНовыйФорматОписи = КНДТребования = "1184002";
	
	СтруктураСвойств.Вставить("НалоговыйОрган", 		Справочники.НалоговыеОрганы.НайтиПоКоду(КодНО));
	СтруктураСвойств.Вставить("Идентификатор",			ИзвлечьДанное(ДеревоXML, "ИдФайл"));
	СтруктураСвойств.Вставить("ИдентификаторОснования",	ИзвлечьДанное(ДеревоXML, "ИдФайлЗаяв"));
	СтруктураСвойств.Вставить("ДатаФормирования",		ДатаИзСтроки(ИзвлечьДанное(ДеревоXML, "ДатаДок")));
	
	Если ЭтоНовыйФорматОписи Тогда
		Подписант = ПолучитьСвойстваПодписантаОписиВходящихДокументов(ДеревоXML);
		СтруктураСвойств.Вставить("Подписант", Подписант);
	КонецЕсли;
		
	УзлыИнфСообДок = ДеревоXML.Строки.НайтиСтроки(Новый Структура("Имя", "ИнфСообДок"), Истина);
	
	Для каждого УзелИнфСообДок Из УзлыИнфСообДок Цикл
		
		РеквизитыДокумента = Новый Структура;
		СписокИменФайловВложений = Новый СписокЗначений;
		
		Если ЭтоНовыйФорматОписи Тогда
			КНДДокумента = ИзвлечьДанное(УзелИнфСообДок, "КНД_Док");
		Иначе
			КНДДокумента = ИзвлечьДанное(УзелИнфСообДок, "ВидФайлДок");
		КонецЕсли;
		
		РеквизитыДокумента.Вставить("ВидДокумента", 	КНДДокумента);
		РеквизитыДокумента.Вставить("НомерДокумента", 	ИзвлечьДанное(УзелИнфСообДок, "НомФайлДок"));
		РеквизитыДокумента.Вставить("ДатаДокумента", 	ДатаИзСтроки(ИзвлечьДанное(УзелИнфСообДок, "ДатаФайлДок")));
		
		УзлыИмяФайл = УзелИнфСообДок.Строки.НайтиСтроки(Новый Структура("Имя", "ИмяФайл"), Истина);
		Для каждого УзелИмяФайл Из УзлыИмяФайл Цикл
			СписокИменФайловВложений.Добавить(УзелИмяФайл.Значение);
		КонецЦикла;	
		
		НовыйДокумент = Новый Структура();
		НовыйДокумент.Вставить("РеквизитыДокумента", 		РеквизитыДокумента);
		НовыйДокумент.Вставить("СписокИменФайловВложений", 	СписокИменФайловВложений);
		
		ТаблицаДокументов.Добавить(НовыйДокумент);
		
	КонецЦикла;
	
	СтруктураСвойств.Вставить("ТаблицаДокументов", ТаблицаДокументов);
	
	Возврат СтруктураСвойств;
	
КонецФункции

Функция ПолучитьСвойстваПодписантаОписиВходящихДокументов(ДеревоXML)
	
	Подписант = Неопределено;
	
	УзлыПодписант = ДеревоXML.Строки.НайтиСтроки(Новый Структура("Имя", "Подписант"), Истина);
	Для каждого УзелПодписант Из УзлыПодписант Цикл
		
		Подписант = Новый Структура;
		Подписант.Вставить("ПодписантДолжность", ИзвлечьДанное(УзелПодписант, "Должн"));
		Подписант.Вставить("ПодписантТелефон", 	 ИзвлечьДанное(УзелПодписант, "Тлф"));
		Подписант.Вставить("ПодписантПочта", 	 ИзвлечьДанное(УзелПодписант, "E-mail"));
		
		УзелыФИО = УзелПодписант.Строки.НайтиСтроки(Новый Структура("Имя", "ФИО"), Истина);
		Если УзелыФИО.Количество() > 0 Тогда
			
			УзелФИО = УзелыФИО[0];
		
			ПодписантФИО = Новый Массив;
			ПодписантФИО.Добавить(СокрЛП(ИзвлечьДанное(УзелФИО, "Фамилия")));
			ПодписантФИО.Добавить(СокрЛП(ИзвлечьДанное(УзелФИО, "Имя")));
			ПодписантФИО.Добавить(СокрЛП(ИзвлечьДанное(УзелФИО, "Отчество")));
			
			ПодписантФИО = СтрСоединить(ПодписантФИО, " ");
			Подписант.Вставить("ПодписантФИО", 	ПодписантФИО);
			
		Иначе
			Подписант.Вставить("ПодписантФИО", 	"");
		КонецЕсли;
		
	КонецЦикла;
	
	Возврат Подписант;
	
КонецФункции

// есть в Контейнере
//
Функция ИзвлечьДанное(СтрокаДерева, Имя) 
	СтрокаДанных = СтрокаДерева.Строки.Найти(Имя, "Имя", Истина);
	Если СтрокаДанных = Неопределено Тогда
		Возврат "";
	Иначе
		Возврат СтрокаДанных.Значение;
	КонецЕсли;
КонецФункции

//////////////////////////////////////////////////////////////////////////////////
//// ОБРАБОТЧИКИ СОБЫТИЙ ТРАНСПОРТНОГО СООБЩЕНИЯ

Процедура ПриЗаписиТранспортногоСообщения(Объект, Отказ)
	
	Если НЕ ЭлектронныйДокументооборотСКонтролирующимиОрганами.ОбновитьСвойстваЦиклаОбмена(Объект.ЦиклОбмена) Тогда
		Отказ = Истина;
	КонецЕсли;
	
	// Обновляем статус в регистре ЖурналОтправокВКонтролирующиеОрганы
	ЦиклОбмена = Объект.ЦиклОбмена;
	Если НЕ ЗначениеЗаполнено(ЦиклОбмена) Тогда
		Возврат;
	КонецЕсли;

	Предмет = ЦиклОбмена.Предмет;
	Если НЕ ЗначениеЗаполнено(Предмет) Тогда 
		Возврат;
	КонецЕсли;
	
	ЭлектронныйДокументооборотСКонтролирующимиОрганами.ЗаписьОбъектовРегламентированнойОтчетности(Предмет, Отказ);
	
КонецПроцедуры

Процедура ПередЗаписьюТранспортногоСообщения(Объект, Отказ, РежимЗаписи, РежимПроведения)
	
	Если НЕ ЗначениеЗаполнено(Объект.Дата) Тогда
		Объект.Дата = ТекущаяДатаСеанса();
	КонецЕсли;
	
	Если Объект.Статус = Перечисления.СтатусыПисем.ПустаяСсылка() Тогда
		Объект.Статус = Перечисления.СтатусыПисем.Сохраненное;
	КонецЕсли;
	
КонецПроцедуры

Функция ТипыСообщенийПоТипамОснований() Экспорт
	
	СоответствиеПодтверждений = Новый Соответствие;
	ТипыТранспортныхСообщений = Перечисления.ТипыТранспортныхСообщений;
	СоответствиеПодтверждений.Вставить(ТипыТранспортныхСообщений.РезультатОбработкиДекларацияНО ,ТипыТранспортныхСообщений.ИзвещениеРезультатОбработкиНП);
	СоответствиеПодтверждений.Вставить(ТипыТранспортныхСообщений.РезультатПриемаЗапросНО ,ТипыТранспортныхСообщений.ИзвещениеРезультатПриемаНП);
	СоответствиеПодтверждений.Вставить(ТипыТранспортныхСообщений.РезультатОбработкиЗапросНО ,ТипыТранспортныхСообщений.ИзвещениеРезультатОбработкиНП);
	СоответствиеПодтверждений.Вставить(ТипыТранспортныхСообщений.ПодтверждениеДекларацияНО ,ТипыТранспортныхСообщений.ИзвещениеПодтверждениеНП);
	СоответствиеПодтверждений.Вставить(ТипыТранспортныхСообщений.РассылкаНО ,ТипыТранспортныхСообщений.ИзвещениеРассылкаНП);
	СоответствиеПодтверждений.Вставить(ТипыТранспортныхСообщений.ПисьмоНО ,ТипыТранспортныхСообщений.ИзвещениеПисьмоНП);
	СоответствиеПодтверждений.Вставить(ТипыТранспортныхСообщений.РезультатПриемаОбращениеНО ,ТипыТранспортныхСообщений.ИзвещениеРезультатПриемаНП);
	СоответствиеПодтверждений.Вставить(ТипыТранспортныхСообщений.ПодтверждениеОбращениеНО ,ТипыТранспортныхСообщений.ИзвещениеПодтверждениеНП);
	СоответствиеПодтверждений.Вставить(ТипыТранспортныхСообщений.РезультатПриемаПредставлениеНО ,ТипыТранспортныхСообщений.ИзвещениеРезультатПриемаНП);
	СоответствиеПодтверждений.Вставить(ТипыТранспортныхСообщений.ПодтверждениеПредставлениеНО ,ТипыТранспортныхСообщений.ИзвещениеПодтверждениеНП);
	СоответствиеПодтверждений.Вставить(ТипыТранспортныхСообщений.РезультатПриемаДекларацияНО ,ТипыТранспортныхСообщений.ИзвещениеРезультатПриемаНП);
	СоответствиеПодтверждений.Вставить(ТипыТранспортныхСообщений.ПисьмоВходящееПФР ,ТипыТранспортныхСообщений.ПисьмоВходящееКвитанцияПФР);
	СоответствиеПодтверждений.Вставить(ТипыТранспортныхСообщений.ПротоколПФР ,ТипыТранспортныхСообщений.ПротоколКвитанцияПФР);
	СоответствиеПодтверждений.Вставить(ТипыТранспортныхСообщений.ОтветНаЗапросПФР ,ТипыТранспортныхСообщений.ОтветНаЗапросКвитанцияПФР);
	СоответствиеПодтверждений.Вставить(ТипыТранспортныхСообщений.ПодтверждениеЗапросНО ,ТипыТранспортныхСообщений.ИзвещениеПодтверждениеНП);
	СоответствиеПодтверждений.Вставить(ТипыТранспортныхСообщений.ПодтверждениеЗаявлениеНО, ТипыТранспортныхСообщений.ИзвещениеПодтверждениеНП);
	СоответствиеПодтверждений.Вставить(ТипыТранспортныхСообщений.РезультатПриемаЗаявлениеНО , ТипыТранспортныхСообщений.ИзвещениеРезультатПриемаНП);
	
	СоответствиеПодтверждений.Вставить(ТипыТранспортныхСообщений.РезультатОбработкиЗаявлениеНО , ТипыТранспортныхСообщений.ИзвещениеРезультатОбработкиНП);
	
	СоответствиеПодтверждений.Вставить(ТипыТранспортныхСообщений.РезультатОбработкиЗаявлениеРФНО , ТипыТранспортныхСообщений.ИзвещениеРезультатОбработкиРФНП);
	СоответствиеПодтверждений.Вставить(ТипыТранспортныхСообщений.СообщениеОбОтзывеЗаявлениеРФНО , ТипыТранспортныхСообщений.ИзвещениеОбОтзывеЗаявлениеРФНП);
	СоответствиеПодтверждений.Вставить(ТипыТранспортныхСообщений.РезультатОбработкиЗаявлениеТСНО , ТипыТранспортныхСообщений.ИзвещениеРезультатОбработкиТСНП);
	СоответствиеПодтверждений.Вставить(ТипыТранспортныхСообщений.ДокументНО , ТипыТранспортныхСообщений.ИзвещениеДокументНП);
	СоответствиеПодтверждений.Вставить(ТипыТранспортныхСообщений.ПисьмоВходящееФСГС ,ТипыТранспортныхСообщений.ИзвещениеПисьмоВходящееФСГС);
	СоответствиеПодтверждений.Вставить(ТипыТранспортныхСообщений.ПротоколВходногоКонтроляОтчетностиФСГС ,ТипыТранспортныхСообщений.ИзвещениеПротоколВходногоКонтроляОтчетностиФСГС);
	СоответствиеПодтверждений.Вставить(ТипыТранспортныхСообщений.ПодтверждениеДатыОтправкиФСГС ,ТипыТранспортныхСообщений.ИзвещениеПодтверждениеДатыОтправкиФСГС);
	СоответствиеПодтверждений.Вставить(ТипыТранспортныхСообщений.ПодтверждениеФорма2НДФЛНО ,ТипыТранспортныхСообщений.ИзвещениеПодтверждениеНП);
	
	Возврат СоответствиеПодтверждений;
	
КонецФункции

Процедура ОбработкаЗаполненияТранспортногоСообщения(Объект, СообщениеОснование) Экспорт
	Перем ИмяФайлаЭЦП;
	Перем ДанныеЭЦП;
	Перем ИмяФайлаСертификата;
	Перем ДанныеСертификата;
	Перем РазмерФайлаСертификата;
	Перем РазмерФайлаЭЦП;
	Перем ЭЦПИмяПодписанногоФайла;
	Перем ЭЦПСертификат;
	Перем ЭЦПСтатусПроверки;
	
	Если ТипЗнч(СообщениеОснование) <> Тип("ДокументСсылка.ТранспортноеСообщение") Тогда
		Объект.ОшибкаЗаполнения = Истина;
		Возврат;
	КонецЕсли;
		
	Если НЕ СообщениеРасшифровано(СообщениеОснование) Тогда
		Объект.ОшибкаЗаполнения = Истина;
		Возврат;
	КонецЕсли;
	
	Если НЕ ЗначениеЗаполнено(СообщениеОснование.ЦиклОбмена) Тогда
		Объект.ОшибкаЗаполнения = Истина;
		Возврат;
	КонецЕсли;
	
	Если СообщениеОснование.Тип = Перечисления.ТипыТранспортныхСообщений.ПротоколПФР Тогда
		//формируем подтверждение на протокол ПФР
		
		ОтветноеСообщение = ОтветноеСообщениеПоСообщениюОснованию(СообщениеОснование, Перечисления.ТипыТранспортныхСообщений.ПротоколКвитанцияПФР);
		Если ОтветноеСообщение <> Неопределено Тогда
			
			Объект.УжеСуществует = Истина;
			Объект.СуществующееСообщение = ОтветноеСообщение;
			
			Возврат;
		КонецЕсли;
		
		Объект.Тип = Перечисления.ТипыТранспортныхСообщений.ПротоколКвитанцияПФР;
		Объект.ЦиклОбмена = СообщениеОснование.ЦиклОбмена;
		Объект.Отправитель = СообщениеОснование.ЦиклОбмена.Организация;
		Объект.Получатель = СообщениеОснование.ЦиклОбмена.ВнешняяОрганизация;
		Объект.УчетнаяЗапись = СообщениеОснование.УчетнаяЗапись;
		Объект.Статус = Перечисления.СтатусыПисем.Исходящее;
		Объект.Основание = СообщениеОснование;
		Объект.Тема = "ReSign: Pension-Protocol";
		
		НачатьТранзакцию();
		
		// пишем текущее сообщение, чтобы было к чему добавлять вложения
		Попытка
			Объект.Записать();
		Исключение
			ОтменитьТранзакцию();
			ТекстСообщения = СтроковыеФункцииКлиентСервер.ПодставитьПараметрыВСтроку(НСтр("ru='Ошибка записи сообщения %1 цикла обмена %2!'"),
																						Символ(34) + Объект.Тип + Символ(34),
																						Символ(34) + Объект.ЦиклОбмена + Символ(34));
			ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
			Объект.ОшибкаЗаполнения = Истина;
			Возврат;
		КонецПопытки;
		
		Объект.ОшибкаЗаполнения = Ложь;
		ЗафиксироватьТранзакцию();
		
	ИначеЕсли СообщениеОснование.Тип = Перечисления.ТипыТранспортныхСообщений.ОтветНаЗапросПФР Тогда
		//формируем подтверждение на протокол ПФР
		
		ОтветноеСообщение = ОтветноеСообщениеПоСообщениюОснованию(СообщениеОснование, Перечисления.ТипыТранспортныхСообщений.ОтветНаЗапросКвитанцияПФР);
		Если ОтветноеСообщение <> Неопределено Тогда
			
			Объект.УжеСуществует = Истина;
			Объект.СуществующееСообщение = ОтветноеСообщение;
			
			Возврат;
		КонецЕсли;
		
		Объект.Тип = Перечисления.ТипыТранспортныхСообщений.ОтветНаЗапросКвитанцияПФР;
		Объект.ЦиклОбмена = СообщениеОснование.ЦиклОбмена;
		Объект.Отправитель = СообщениеОснование.ЦиклОбмена.Организация;
		Объект.Получатель = СообщениеОснование.ЦиклОбмена.ВнешняяОрганизация;
		Объект.УчетнаяЗапись = СообщениеОснование.УчетнаяЗапись;
		Объект.Статус = Перечисления.СтатусыПисем.Исходящее;
		Объект.Основание = СообщениеОснование;
		Объект.Тема = "ReSign: Pension-Reply";
		
		НачатьТранзакцию();
		
		// пишем текущее сообщение, чтобы было к чему добавлять вложения
		Попытка
			Объект.Записать();
		Исключение
			ОтменитьТранзакцию();
			ТекстСообщения = СтроковыеФункцииКлиентСервер.ПодставитьПараметрыВСтроку(НСтр("ru='Ошибка записи сообщения %1 цикла обмена %2!'"),
																						Символ(34) + Объект.Тип + Символ(34),
																						Символ(34) + Объект.ЦиклОбмена + Символ(34));
			ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
			Объект.ОшибкаЗаполнения = Истина;
			Возврат;
		КонецПопытки;
		
		Объект.ОшибкаЗаполнения = Ложь;
		ЗафиксироватьТранзакцию();
		
	ИначеЕсли СообщениеОснование.Тип = Перечисления.ТипыТранспортныхСообщений.ПисьмоВходящееПФР Тогда
		// формируем подтверждение на входящее неформализованное от ПФР
		
		ОтветноеСообщение = ОтветноеСообщениеПоСообщениюОснованию(СообщениеОснование, Перечисления.ТипыТранспортныхСообщений.ПисьмоВходящееКвитанцияПФР);
		Если ОтветноеСообщение <> Неопределено Тогда
			
			Объект.УжеСуществует = Истина;
			Объект.СуществующееСообщение = ОтветноеСообщение;
			
			Возврат;
		КонецЕсли;
		
		Объект.Тип = Перечисления.ТипыТранспортныхСообщений.ПисьмоВходящееКвитанцияПФР;
		Объект.ЦиклОбмена = СообщениеОснование.ЦиклОбмена;
		Объект.Отправитель = СообщениеОснование.ЦиклОбмена.Организация;
		Объект.Получатель = СообщениеОснование.ЦиклОбмена.ВнешняяОрганизация;
		Объект.УчетнаяЗапись = СообщениеОснование.УчетнаяЗапись;
		Объект.Статус = Перечисления.СтатусыПисем.Исходящее;
		Объект.Основание = СообщениеОснование;
		Объект.Тема = "ReSign: Pension-Unformal";
		
		НачатьТранзакцию();
		
		// пишем текущее сообщение, чтобы было к чему добавлять вложения
		Попытка
			Объект.Записать();
		Исключение
			ОтменитьТранзакцию();
			ТекстСообщения = СтроковыеФункцииКлиентСервер.ПодставитьПараметрыВСтроку(НСтр("ru='Ошибка записи сообщения %1 цикла обмена %2!'"),
																						Символ(34) + Объект.Тип + Символ(34),
																						Символ(34) + Объект.ЦиклОбмена + Символ(34));
			ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
			Объект.ОшибкаЗаполнения = Истина;
			Возврат;
		КонецПопытки;
		
		Объект.ОшибкаЗаполнения = Ложь;
		ЗафиксироватьТранзакцию();
		
	ИначеЕсли СообщениеОснование.Тип = Перечисления.ТипыТранспортныхСообщений.ПодтверждениеДекларацияНО 
	ИЛИ СообщениеОснование.Тип = Перечисления.ТипыТранспортныхСообщений.ПодтверждениеЗаявлениеНО Тогда
		// формируем подтверждение на подтверждение даты отправки
		
		ОтветноеСообщение = ОтветноеСообщениеПоСообщениюОснованию(СообщениеОснование, Перечисления.ТипыТранспортныхСообщений.ИзвещениеПодтверждениеНП);
		Если ОтветноеСообщение <> Неопределено Тогда
			
			Объект.УжеСуществует = Истина;
			Объект.СуществующееСообщение = ОтветноеСообщение;
			
			Возврат;
		КонецЕсли;
		
		Объект.Тип = Перечисления.ТипыТранспортныхСообщений.ИзвещениеПодтверждениеНП;
		Объект.ЦиклОбмена = СообщениеОснование.ЦиклОбмена;
		Объект.Отправитель = СообщениеОснование.ЦиклОбмена.Организация;
		Объект.Получатель = СообщениеОснование.УчетнаяЗапись.СерверДокументооборота;
		Объект.УчетнаяЗапись = СообщениеОснование.УчетнаяЗапись;
		Объект.Статус = Перечисления.СтатусыПисем.Исходящее;
		Объект.Основание = СообщениеОснование;
		Объект.Тема = "ФНС:1.0-извещениеОПолучении-ИзвещениеПодтверждениеНП";
		
		НачатьТранзакцию();
		
		// пишем текущее сообщение, чтобы было к чему добавлять вложения
		Попытка
			Объект.Записать();
		Исключение
			ОтменитьТранзакцию();
			ТекстСообщения = СтроковыеФункцииКлиентСервер.ПодставитьПараметрыВСтроку(НСтр("ru='Ошибка записи сообщения %1 цикла обмена %2!'"),
																							Символ(34) + Объект.Тип + Символ(34),
																							Символ(34) + Объект.ЦиклОбмена + Символ(34));
			ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
			Объект.ОшибкаЗаполнения = Истина;
			Возврат;
		КонецПопытки;
		
		Объект.ОшибкаЗаполнения = Ложь;
		ЗафиксироватьТранзакцию();
		
	ИначеЕсли СообщениеОснование.Тип = Перечисления.ТипыТранспортныхСообщений.ПодтверждениеФорма2НДФЛНО Тогда
		// формируем подтверждение на подтверждение даты отправки
		
		ОтветноеСообщение = ОтветноеСообщениеПоСообщениюОснованию(СообщениеОснование, Перечисления.ТипыТранспортныхСообщений.ИзвещениеПодтверждениеНП);
		Если ОтветноеСообщение <> Неопределено Тогда
			
			Объект.УжеСуществует = Истина;
			Объект.СуществующееСообщение = ОтветноеСообщение;
			
			Возврат;
		КонецЕсли;
		
		Объект.Тип = Перечисления.ТипыТранспортныхСообщений.ИзвещениеПодтверждениеНП;
		Объект.ЦиклОбмена = СообщениеОснование.ЦиклОбмена;
		Объект.Отправитель = СообщениеОснование.ЦиклОбмена.Организация;
		Объект.Получатель = СообщениеОснование.УчетнаяЗапись.СерверДокументооборота;
		Объект.УчетнаяЗапись = СообщениеОснование.УчетнаяЗапись;
		Объект.Статус = Перечисления.СтатусыПисем.Исходящее;
		Объект.Основание = СообщениеОснование;
		Объект.Тема = "ФНС:1.0-извещениеОПолучении-ИзвещениеПодтверждениеНП";
		
		НачатьТранзакцию();
		
		// пишем текущее сообщение, чтобы было к чему добавлять вложения
		Попытка
			Объект.Записать();
		Исключение
			ОтменитьТранзакцию();
			ТекстСообщения = СтроковыеФункцииКлиентСервер.ПодставитьПараметрыВСтроку(НСтр("ru='Ошибка записи сообщения %1 цикла обмена %2!'"),
																						Символ(34) + Объект.Тип + Символ(34),
																						Символ(34) + Объект.ЦиклОбмена + Символ(34));
			ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
			Объект.ОшибкаЗаполнения = Истина;
			Возврат;
		КонецПопытки;
		
		Объект.ОшибкаЗаполнения = Ложь;
		ЗафиксироватьТранзакцию();
		
	ИначеЕсли СообщениеОснование.Тип = Перечисления.ТипыТранспортныхСообщений.ПодтверждениеОбращениеНО Тогда
		// формируем подтверждение на подтверждение даты отправки
		
		ОтветноеСообщение = ОтветноеСообщениеПоСообщениюОснованию(СообщениеОснование, Перечисления.ТипыТранспортныхСообщений.ИзвещениеПодтверждениеНП);
		Если ОтветноеСообщение <> Неопределено Тогда
			
			Объект.УжеСуществует = Истина;
			Объект.СуществующееСообщение = ОтветноеСообщение;
			
			Возврат;
		КонецЕсли;
		
		Объект.Тип = Перечисления.ТипыТранспортныхСообщений.ИзвещениеПодтверждениеНП;
		Объект.ЦиклОбмена = СообщениеОснование.ЦиклОбмена;
		Объект.Отправитель = СообщениеОснование.ЦиклОбмена.Организация;
		Объект.Получатель = СообщениеОснование.УчетнаяЗапись.СерверДокументооборота;
		Объект.УчетнаяЗапись = СообщениеОснование.УчетнаяЗапись;
		Объект.Статус = Перечисления.СтатусыПисем.Исходящее;
		Объект.Основание = СообщениеОснование;
		Объект.Тема = "ФНС:1.0-извещениеОПолучении-ИзвещениеПодтверждениеНП";
		
		НачатьТранзакцию();
		
		// пишем текущее сообщение, чтобы было к чему добавлять вложения
		Попытка
			Объект.Записать();
		Исключение
			ОтменитьТранзакцию();
			ТекстСообщения = СтроковыеФункцииКлиентСервер.ПодставитьПараметрыВСтроку(НСтр("ru='Ошибка записи сообщения %1 цикла обмена %2!'"),
																						Символ(34) + Объект.Тип + Символ(34),
																						Символ(34) + Объект.ЦиклОбмена + Символ(34));
			ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
			Объект.ОшибкаЗаполнения = Истина;
			Возврат;
		КонецПопытки;
		
		Объект.ОшибкаЗаполнения = Ложь;
		ЗафиксироватьТранзакцию();
		
	ИначеЕсли СообщениеОснование.Тип = Перечисления.ТипыТранспортныхСообщений.ПодтверждениеПредставлениеНО Тогда
		// формируем подтверждение на подтверждение даты отправки
		
		ОтветноеСообщение = ОтветноеСообщениеПоСообщениюОснованию(СообщениеОснование, Перечисления.ТипыТранспортныхСообщений.ИзвещениеПодтверждениеНП);
		Если ОтветноеСообщение <> Неопределено Тогда
			
			Объект.УжеСуществует = Истина;
			Объект.СуществующееСообщение = ОтветноеСообщение;
			
			Возврат;
		КонецЕсли;
		
		Объект.Тип = Перечисления.ТипыТранспортныхСообщений.ИзвещениеПодтверждениеНП;
		Объект.ЦиклОбмена = СообщениеОснование.ЦиклОбмена;
		Объект.Отправитель = СообщениеОснование.ЦиклОбмена.Организация;
		Объект.Получатель = СообщениеОснование.УчетнаяЗапись.СерверДокументооборота;
		Объект.УчетнаяЗапись = СообщениеОснование.УчетнаяЗапись;
		Объект.Статус = Перечисления.СтатусыПисем.Исходящее;
		Объект.Основание = СообщениеОснование;
		Объект.Тема = "ФНС:1.0-извещениеОПолучении-ИзвещениеПодтверждениеНП";
		
		НачатьТранзакцию();
		
		// пишем текущее сообщение, чтобы было к чему добавлять вложения
		Попытка
			Объект.Записать();
		Исключение
			ОтменитьТранзакцию();
			ТекстСообщения = СтроковыеФункцииКлиентСервер.ПодставитьПараметрыВСтроку(НСтр("ru='Ошибка записи сообщения %1 цикла обмена %2!'"),
																						Символ(34) + Объект.Тип + Символ(34),
																						Символ(34) + Объект.ЦиклОбмена + Символ(34));
			ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
			Объект.ОшибкаЗаполнения = Истина;
			Возврат;
		КонецПопытки;
		
		Объект.ОшибкаЗаполнения = Ложь;
		ЗафиксироватьТранзакцию();
		
	ИначеЕсли СообщениеОснование.Тип = Перечисления.ТипыТранспортныхСообщений.ПодтверждениеЗапросНО Тогда
		// формируем подтверждение на подтверждение даты отправки
		
		ОтветноеСообщение = ОтветноеСообщениеПоСообщениюОснованию(СообщениеОснование, Перечисления.ТипыТранспортныхСообщений.ИзвещениеПодтверждениеНП);
		Если ОтветноеСообщение <> Неопределено Тогда
			
			Объект.УжеСуществует = Истина;
			Объект.СуществующееСообщение = ОтветноеСообщение;
			
			Возврат;
		КонецЕсли;
		
		Объект.Тип = Перечисления.ТипыТранспортныхСообщений.ИзвещениеПодтверждениеНП;
		Объект.ЦиклОбмена = СообщениеОснование.ЦиклОбмена;
		Объект.Отправитель = СообщениеОснование.ЦиклОбмена.Организация;
		Объект.Получатель = СообщениеОснование.УчетнаяЗапись.СерверДокументооборота;
		Объект.УчетнаяЗапись = СообщениеОснование.УчетнаяЗапись;
		Объект.Статус = Перечисления.СтатусыПисем.Исходящее;
		Объект.Основание = СообщениеОснование;
		Объект.Тема = "ФНС:1.0-извещениеОПолучении-ИзвещениеПодтверждениеНП";
		
		НачатьТранзакцию();
		
		// пишем текущее сообщение, чтобы было к чему добавлять вложения
		Попытка
			Объект.Записать();
		Исключение
			ОтменитьТранзакцию();
			ТекстСообщения = СтроковыеФункцииКлиентСервер.ПодставитьПараметрыВСтроку(НСтр("ru='Ошибка записи сообщения %1 цикла обмена %2!'"),
																						Символ(34) + Объект.Тип + Символ(34),
																						Символ(34) + Объект.ЦиклОбмена + Символ(34));
			ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
			Объект.ОшибкаЗаполнения = Истина;
			Возврат;
		КонецПопытки;
		
		Объект.ОшибкаЗаполнения = Ложь;
		ЗафиксироватьТранзакцию();
		
	ИначеЕсли СообщениеОснование.Тип = Перечисления.ТипыТранспортныхСообщений.РезультатПриемаДекларацияНО 
	ИЛИ СообщениеОснование.Тип = Перечисления.ТипыТранспортныхСообщений.РезультатПриемаЗаявлениеНО Тогда
		
		ОтветноеСообщение = ОтветноеСообщениеПоСообщениюОснованию(СообщениеОснование, Перечисления.ТипыТранспортныхСообщений.ИзвещениеРезультатПриемаНП);
		Если ОтветноеСообщение <> Неопределено Тогда
			
			Объект.УжеСуществует = Истина;
			Объект.СуществующееСообщение = ОтветноеСообщение;
			
			Возврат;
		КонецЕсли;
		
		Объект.Тип = Перечисления.ТипыТранспортныхСообщений.ИзвещениеРезультатПриемаНП;
		Объект.ЦиклОбмена = СообщениеОснование.ЦиклОбмена;
		Объект.Отправитель = СообщениеОснование.ЦиклОбмена.Организация;
		Объект.Получатель = ?(НЕ СообщениеОснование.УчетнаяЗапись.ОбменНапрямую, СообщениеОснование.УчетнаяЗапись.СерверДокументооборота, СообщениеОснование.УчетнаяЗапись.НалоговыйОрган);
		Объект.УчетнаяЗапись = СообщениеОснование.УчетнаяЗапись;
		Объект.Статус = Перечисления.СтатусыПисем.Исходящее;
		Объект.Основание = СообщениеОснование;
		Объект.Тема = "ФНС:1.0-извещениеОПолучении-ИзвещениеРезультатПриемаНП";
		
		НачатьТранзакцию();
		
		// пишем текущее сообщение, чтобы было к чему добавлять вложения
		Попытка
			Объект.Записать();
		Исключение
			ОтменитьТранзакцию();
			ТекстСообщения = СтроковыеФункцииКлиентСервер.ПодставитьПараметрыВСтроку(НСтр("ru='Ошибка записи сообщения %1 цикла обмена %2!'"),
																						Символ(34) + Объект.Тип + Символ(34),
																						Символ(34) + Объект.ЦиклОбмена + Символ(34));
			ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
			Объект.ОшибкаЗаполнения = Истина;
			Возврат;
		КонецПопытки;
		
		Объект.ОшибкаЗаполнения = Ложь;
		ЗафиксироватьТранзакцию();
		
	ИначеЕсли СообщениеОснование.Тип = Перечисления.ТипыТранспортныхСообщений.РезультатПриемаФорма2НДФЛНО Тогда
		
		ОтветноеСообщение = ОтветноеСообщениеПоСообщениюОснованию(СообщениеОснование, Перечисления.ТипыТранспортныхСообщений.ИзвещениеРезультатПриемаНП);
		Если ОтветноеСообщение <> Неопределено Тогда
			
			Объект.УжеСуществует = Истина;
			Объект.СуществующееСообщение = ОтветноеСообщение;
			
			Возврат;
		КонецЕсли;
		
		Объект.Тип = Перечисления.ТипыТранспортныхСообщений.ИзвещениеРезультатПриемаНП;
		Объект.ЦиклОбмена = СообщениеОснование.ЦиклОбмена;
		Объект.Отправитель = СообщениеОснование.ЦиклОбмена.Организация;
		Объект.Получатель = ?(НЕ СообщениеОснование.УчетнаяЗапись.ОбменНапрямую, СообщениеОснование.УчетнаяЗапись.СерверДокументооборота, СообщениеОснование.УчетнаяЗапись.НалоговыйОрган);
		Объект.УчетнаяЗапись = СообщениеОснование.УчетнаяЗапись;
		Объект.Статус = Перечисления.СтатусыПисем.Исходящее;
		Объект.Основание = СообщениеОснование;
		Объект.Тема = "ФНС:1.0-извещениеОПолучении-ИзвещениеРезультатПриемаНП";
		
		НачатьТранзакцию();
		
		// пишем текущее сообщение, чтобы было к чему добавлять вложения
		Попытка
			Объект.Записать();
		Исключение
			ОтменитьТранзакцию();
			ТекстСообщения = СтроковыеФункцииКлиентСервер.ПодставитьПараметрыВСтроку(НСтр("ru='Ошибка записи сообщения %1 цикла обмена %2!'"),
																						Символ(34) + Объект.Тип + Символ(34),
																						Символ(34) + Объект.ЦиклОбмена + Символ(34));
			ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
			Объект.ОшибкаЗаполнения = Истина;
			Возврат;
		КонецПопытки;
		
		Объект.ОшибкаЗаполнения = Ложь;
		ЗафиксироватьТранзакцию();
		
	ИначеЕсли СообщениеОснование.Тип = Перечисления.ТипыТранспортныхСообщений.РезультатПриемаОбращениеНО Тогда
		
		ОтветноеСообщение = ОтветноеСообщениеПоСообщениюОснованию(СообщениеОснование, Перечисления.ТипыТранспортныхСообщений.ИзвещениеРезультатПриемаНП);
		Если ОтветноеСообщение <> Неопределено Тогда
			
			Объект.УжеСуществует = Истина;
			Объект.СуществующееСообщение = ОтветноеСообщение;
			
			Возврат;
		КонецЕсли;
		
		Объект.Тип = Перечисления.ТипыТранспортныхСообщений.ИзвещениеРезультатПриемаНП;
		Объект.ЦиклОбмена = СообщениеОснование.ЦиклОбмена;
		Объект.Отправитель = СообщениеОснование.ЦиклОбмена.Организация;
		Объект.Получатель = ?(НЕ СообщениеОснование.УчетнаяЗапись.ОбменНапрямую, СообщениеОснование.УчетнаяЗапись.СерверДокументооборота, СообщениеОснование.УчетнаяЗапись.НалоговыйОрган);
		Объект.УчетнаяЗапись = СообщениеОснование.УчетнаяЗапись;
		Объект.Статус = Перечисления.СтатусыПисем.Исходящее;
		Объект.Основание = СообщениеОснование;
		Объект.Тема = "ФНС:1.0-извещениеОПолучении-ИзвещениеРезультатПриемаНП";
		
		НачатьТранзакцию();
		
		// пишем текущее сообщение, чтобы было к чему добавлять вложения
		Попытка
			Объект.Записать();
		Исключение
			ОтменитьТранзакцию();
			ТекстСообщения = СтроковыеФункцииКлиентСервер.ПодставитьПараметрыВСтроку(НСтр("ru='Ошибка записи сообщения %1 цикла обмена %2!'"),
																							Символ(34) + Объект.Тип + Символ(34),
																							Символ(34) + Объект.ЦиклОбмена + Символ(34));
			ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
			Объект.ОшибкаЗаполнения = Истина;
			Возврат;
		КонецПопытки;
		
		Объект.ОшибкаЗаполнения = Ложь;
		ЗафиксироватьТранзакцию();
		
	ИначеЕсли СообщениеОснование.Тип = Перечисления.ТипыТранспортныхСообщений.РезультатПриемаПредставлениеНО Тогда
		
		ОтветноеСообщение = ОтветноеСообщениеПоСообщениюОснованию(СообщениеОснование, Перечисления.ТипыТранспортныхСообщений.ИзвещениеРезультатПриемаНП);
		Если ОтветноеСообщение <> Неопределено Тогда
			
			Объект.УжеСуществует = Истина;
			Объект.СуществующееСообщение = ОтветноеСообщение;
			
			Возврат;
		КонецЕсли;
		
		Объект.Тип = Перечисления.ТипыТранспортныхСообщений.ИзвещениеРезультатПриемаНП;
		Объект.ЦиклОбмена = СообщениеОснование.ЦиклОбмена;
		Объект.Отправитель = СообщениеОснование.ЦиклОбмена.Организация;
		Объект.Получатель = ?(НЕ СообщениеОснование.УчетнаяЗапись.ОбменНапрямую, СообщениеОснование.УчетнаяЗапись.СерверДокументооборота, СообщениеОснование.УчетнаяЗапись.НалоговыйОрган);
		Объект.УчетнаяЗапись = СообщениеОснование.УчетнаяЗапись;
		Объект.Статус = Перечисления.СтатусыПисем.Исходящее;
		Объект.Основание = СообщениеОснование;
		Объект.Тема = "ФНС:1.0-извещениеОПолучении-ИзвещениеРезультатПриемаНП";
		
		НачатьТранзакцию();
		
		// пишем текущее сообщение, чтобы было к чему добавлять вложения
		Попытка
			Объект.Записать();
		Исключение
			ОтменитьТранзакцию();
			ТекстСообщения = СтроковыеФункцииКлиентСервер.ПодставитьПараметрыВСтроку(НСтр("ru='Ошибка записи сообщения %1 цикла обмена %2!'"),
																							Символ(34) + Объект.Тип + Символ(34),
																							Символ(34) + Объект.ЦиклОбмена + Символ(34));
			ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
			Объект.ОшибкаЗаполнения = Истина;
			Возврат;
		КонецПопытки;
		
		Объект.ОшибкаЗаполнения = Ложь;
		ЗафиксироватьТранзакцию();

	ИначеЕсли СообщениеОснование.Тип = Перечисления.ТипыТранспортныхСообщений.РезультатПриемаЗапросНО Тогда
		
		ОтветноеСообщение = ОтветноеСообщениеПоСообщениюОснованию(СообщениеОснование, Перечисления.ТипыТранспортныхСообщений.ИзвещениеРезультатПриемаНП);
		Если ОтветноеСообщение <> Неопределено Тогда
			
			Объект.УжеСуществует = Истина;
			Объект.СуществующееСообщение = ОтветноеСообщение;
			
			Возврат;
		КонецЕсли;
		
		Объект.Тип = Перечисления.ТипыТранспортныхСообщений.ИзвещениеРезультатПриемаНП;
		Объект.ЦиклОбмена = СообщениеОснование.ЦиклОбмена;
		Объект.Отправитель = СообщениеОснование.ЦиклОбмена.Организация;
		Объект.Получатель = ?(НЕ СообщениеОснование.УчетнаяЗапись.ОбменНапрямую, СообщениеОснование.УчетнаяЗапись.СерверДокументооборота, СообщениеОснование.УчетнаяЗапись.НалоговыйОрган);
		Объект.УчетнаяЗапись = СообщениеОснование.УчетнаяЗапись;
		Объект.Статус = Перечисления.СтатусыПисем.Исходящее;
		Объект.Основание = СообщениеОснование;
		Объект.Тема = "ФНС:1.0-извещениеОПолучении-ИзвещениеРезультатПриемаНП";
		
		НачатьТранзакцию();
		
		// пишем текущее сообщение, чтобы было к чему добавлять вложения
		Попытка
			Объект.Записать();
		Исключение
			ОтменитьТранзакцию();
			ТекстСообщения = СтроковыеФункцииКлиентСервер.ПодставитьПараметрыВСтроку(НСтр("ru='Ошибка записи сообщения %1 цикла обмена %2!'"),
			Символ(34) + Объект.Тип + Символ(34),
			Символ(34) + Объект.ЦиклОбмена + Символ(34));
			ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
			Объект.ОшибкаЗаполнения = Истина;
			Возврат;
		КонецПопытки;
		
		Объект.ОшибкаЗаполнения = Ложь;
		ЗафиксироватьТранзакцию();
		
	ИначеЕсли СообщениеОснование.Тип = Перечисления.ТипыТранспортныхСообщений.РезультатОбработкиДекларацияНО Тогда
		
		ОтветноеСообщение = ОтветноеСообщениеПоСообщениюОснованию(СообщениеОснование, Перечисления.ТипыТранспортныхСообщений.ИзвещениеРезультатОбработкиНП);
		Если ОтветноеСообщение <> Неопределено Тогда
			
			Объект.УжеСуществует = Истина;
			Объект.СуществующееСообщение = ОтветноеСообщение;
			
			Возврат;
		КонецЕсли;
		
		Объект.Тип = Перечисления.ТипыТранспортныхСообщений.ИзвещениеРезультатОбработкиНП;
		Объект.ЦиклОбмена = СообщениеОснование.ЦиклОбмена;
		Объект.Отправитель = СообщениеОснование.ЦиклОбмена.Организация;
		Объект.Получатель = ?(НЕ СообщениеОснование.УчетнаяЗапись.ОбменНапрямую, СообщениеОснование.УчетнаяЗапись.СерверДокументооборота, СообщениеОснование.УчетнаяЗапись.НалоговыйОрган);
		Объект.УчетнаяЗапись = СообщениеОснование.УчетнаяЗапись;
		Объект.Статус = Перечисления.СтатусыПисем.Исходящее;
		Объект.Основание = СообщениеОснование;
		Объект.Тема = "ФНС:1.0-извещениеОПолучении-ИзвещениеРезультатОбработкиНП";
		
		НачатьТранзакцию();
		
		// пишем текущее сообщение, чтобы было к чему добавлять вложения
		Попытка
			Объект.Записать();
		Исключение
			ОтменитьТранзакцию();
			ТекстСообщения = СтроковыеФункцииКлиентСервер.ПодставитьПараметрыВСтроку(НСтр("ru='Ошибка записи сообщения %1 цикла обмена %2!'"),
			Символ(34) + Объект.Тип + Символ(34),
			Символ(34) + Объект.ЦиклОбмена + Символ(34));
			ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
			Объект.ОшибкаЗаполнения = Истина;
			Возврат;
		КонецПопытки;
		
		Объект.ОшибкаЗаполнения = Ложь;
		ЗафиксироватьТранзакцию();
		
	ИначеЕсли СообщениеОснование.Тип = Перечисления.ТипыТранспортныхСообщений.РезультатОбработкиЗаявлениеНО Тогда
		
		ОтветноеСообщение = ОтветноеСообщениеПоСообщениюОснованию(СообщениеОснование, Перечисления.ТипыТранспортныхСообщений.ИзвещениеРезультатОбработкиНП);
		Если ОтветноеСообщение <> Неопределено Тогда
			
			Объект.УжеСуществует = Истина;
			Объект.СуществующееСообщение = ОтветноеСообщение;
			
			Возврат;
		КонецЕсли;
		
		Объект.Тип = Перечисления.ТипыТранспортныхСообщений.ИзвещениеРезультатОбработкиНП;
		Объект.ЦиклОбмена = СообщениеОснование.ЦиклОбмена;
		Объект.Отправитель = СообщениеОснование.ЦиклОбмена.Организация;
		Объект.Получатель = ?(НЕ СообщениеОснование.УчетнаяЗапись.ОбменНапрямую, СообщениеОснование.УчетнаяЗапись.СерверДокументооборота, СообщениеОснование.УчетнаяЗапись.НалоговыйОрган);
		Объект.УчетнаяЗапись = СообщениеОснование.УчетнаяЗапись;
		Объект.Статус = Перечисления.СтатусыПисем.Исходящее;
		Объект.Основание = СообщениеОснование;
		Объект.Тема = "ФНС:1.0-извещениеОПолучении-ИзвещениеРезультатОбработкиНП";
		
		НачатьТранзакцию();
		
		// пишем текущее сообщение, чтобы было к чему добавлять вложения
		Попытка
			Объект.Записать();
		Исключение
			ОтменитьТранзакцию();
			ТекстСообщения = СтроковыеФункцииКлиентСервер.ПодставитьПараметрыВСтроку(НСтр("ru='Ошибка записи сообщения %1 цикла обмена %2!'"),
			Символ(34) + Объект.Тип + Символ(34),
			Символ(34) + Объект.ЦиклОбмена + Символ(34));
			ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
			Объект.ОшибкаЗаполнения = Истина;
			Возврат;
		КонецПопытки;
		
		Объект.ОшибкаЗаполнения = Ложь;
		ЗафиксироватьТранзакцию();
		
	ИначеЕсли СообщениеОснование.Тип = Перечисления.ТипыТранспортныхСообщений.РезультатОбработкиЗаявлениеРФНО Тогда
		
		ОтветноеСообщение = ОтветноеСообщениеПоСообщениюОснованию(СообщениеОснование, Перечисления.ТипыТранспортныхСообщений.ИзвещениеРезультатОбработкиРФНП);
		Если ОтветноеСообщение <> Неопределено Тогда
			
			Объект.УжеСуществует = Истина;
			Объект.СуществующееСообщение = ОтветноеСообщение;
			
			Возврат;
		КонецЕсли;
		
		Объект.Тип = Перечисления.ТипыТранспортныхСообщений.ИзвещениеРезультатОбработкиРФНП;
		Объект.ЦиклОбмена = СообщениеОснование.ЦиклОбмена;
		Объект.Отправитель = СообщениеОснование.ЦиклОбмена.Организация;
		Объект.Получатель = ?(НЕ СообщениеОснование.УчетнаяЗапись.ОбменНапрямую, СообщениеОснование.УчетнаяЗапись.СерверДокументооборота, СообщениеОснование.УчетнаяЗапись.НалоговыйОрган);
		Объект.УчетнаяЗапись = СообщениеОснование.УчетнаяЗапись;
		Объект.Статус = Перечисления.СтатусыПисем.Исходящее;
		Объект.Основание = СообщениеОснование;
		Объект.Тема = "ФНС:1.0-извещениеОПолучении-ИзвещениеРезультатОбработкиРФНП";
		
		НачатьТранзакцию();
		
		// пишем текущее сообщение, чтобы было к чему добавлять вложения
		Попытка
			Объект.Записать();
		Исключение
			ОтменитьТранзакцию();
			ТекстСообщения = СтроковыеФункцииКлиентСервер.ПодставитьПараметрыВСтроку(НСтр("ru='Ошибка записи сообщения %1 цикла обмена %2!'"),
			Символ(34) + Объект.Тип + Символ(34),
			Символ(34) + Объект.ЦиклОбмена + Символ(34));
			ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
			Объект.ОшибкаЗаполнения = Истина;
			Возврат;
		КонецПопытки;
		
		Объект.ОшибкаЗаполнения = Ложь;
		ЗафиксироватьТранзакцию();
		
	ИначеЕсли СообщениеОснование.Тип = Перечисления.ТипыТранспортныхСообщений.СообщениеОбОтзывеЗаявлениеРФНО Тогда
		
		ОтветноеСообщение = ОтветноеСообщениеПоСообщениюОснованию(СообщениеОснование, Перечисления.ТипыТранспортныхСообщений.ИзвещениеОбОтзывеЗаявлениеРФНП);
		Если ОтветноеСообщение <> Неопределено Тогда
			
			Объект.УжеСуществует = Истина;
			Объект.СуществующееСообщение = ОтветноеСообщение;
			
			Возврат;
		КонецЕсли;
		
		Объект.Тип = Перечисления.ТипыТранспортныхСообщений.ИзвещениеОбОтзывеЗаявлениеРФНП;
		Объект.ЦиклОбмена = СообщениеОснование.ЦиклОбмена;
		Объект.Отправитель = СообщениеОснование.ЦиклОбмена.Организация;
		Объект.Получатель = ?(НЕ СообщениеОснование.УчетнаяЗапись.ОбменНапрямую, СообщениеОснование.УчетнаяЗапись.СерверДокументооборота, СообщениеОснование.УчетнаяЗапись.НалоговыйОрган);
		Объект.УчетнаяЗапись = СообщениеОснование.УчетнаяЗапись;
		Объект.Статус = Перечисления.СтатусыПисем.Исходящее;
		Объект.Основание = СообщениеОснование;
		Объект.Тема = "ФНС:1.0-извещениеОПолучении-ИзвещениеОбОтзывеЗаявлениеРФНП";
		
		НачатьТранзакцию();
		
		// пишем текущее сообщение, чтобы было к чему добавлять вложения
		Попытка
			Объект.Записать();
		Исключение
			ОтменитьТранзакцию();
			ТекстСообщения = СтроковыеФункцииКлиентСервер.ПодставитьПараметрыВСтроку(НСтр("ru='Ошибка записи сообщения %1 цикла обмена %2!'"),
			Символ(34) + Объект.Тип + Символ(34),
			Символ(34) + Объект.ЦиклОбмена + Символ(34));
			ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
			Объект.ОшибкаЗаполнения = Истина;
			Возврат;
		КонецПопытки;
		
		Объект.ОшибкаЗаполнения = Ложь;
		ЗафиксироватьТранзакцию();
		
	ИначеЕсли СообщениеОснование.Тип = Перечисления.ТипыТранспортныхСообщений.РезультатОбработкиЗаявлениеТСНО Тогда
		
		ОтветноеСообщение = ОтветноеСообщениеПоСообщениюОснованию(СообщениеОснование, Перечисления.ТипыТранспортныхСообщений.ИзвещениеРезультатОбработкиТСНП);
		Если ОтветноеСообщение <> Неопределено Тогда
			
			Объект.УжеСуществует = Истина;
			Объект.СуществующееСообщение = ОтветноеСообщение;
			
			Возврат;
		КонецЕсли;
		
		Объект.Тип = Перечисления.ТипыТранспортныхСообщений.ИзвещениеРезультатОбработкиТСНП;
		Объект.ЦиклОбмена = СообщениеОснование.ЦиклОбмена;
		Объект.Отправитель = СообщениеОснование.ЦиклОбмена.Организация;
		Объект.Получатель = ?(НЕ СообщениеОснование.УчетнаяЗапись.ОбменНапрямую, СообщениеОснование.УчетнаяЗапись.СерверДокументооборота, СообщениеОснование.УчетнаяЗапись.НалоговыйОрган);
		Объект.УчетнаяЗапись = СообщениеОснование.УчетнаяЗапись;
		Объект.Статус = Перечисления.СтатусыПисем.Исходящее;
		Объект.Основание = СообщениеОснование;
		Объект.Тема = "ФНС:1.0-извещениеОПолучении-ИзвещениеРезультатОбработкиТСНП";
		
		НачатьТранзакцию();
		
		// пишем текущее сообщение, чтобы было к чему добавлять вложения
		Попытка
			Объект.Записать();
		Исключение
			ОтменитьТранзакцию();
			ТекстСообщения = СтроковыеФункцииКлиентСервер.ПодставитьПараметрыВСтроку(НСтр("ru='Ошибка записи сообщения %1 цикла обмена %2!'"),
			Символ(34) + Объект.Тип + Символ(34),
			Символ(34) + Объект.ЦиклОбмена + Символ(34));
			ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
			Объект.ОшибкаЗаполнения = Истина;
			Возврат;
		КонецПопытки;
		
		Объект.ОшибкаЗаполнения = Ложь;
		ЗафиксироватьТранзакцию();
		
	ИначеЕсли СообщениеОснование.Тип = Перечисления.ТипыТранспортныхСообщений.РезультатОбработкиЗапросНО Тогда
		// формируем подтверждение на подтверждение даты отправки
		
		ОтветноеСообщение = ОтветноеСообщениеПоСообщениюОснованию(СообщениеОснование, Перечисления.ТипыТранспортныхСообщений.ИзвещениеРезультатОбработкиНП);
		Если ОтветноеСообщение <> Неопределено Тогда
			
			Объект.УжеСуществует = Истина;
			Объект.СуществующееСообщение = ОтветноеСообщение;
			
			Возврат;
		КонецЕсли;
		
		Объект.Тип = Перечисления.ТипыТранспортныхСообщений.ИзвещениеРезультатОбработкиНП;
		Объект.ЦиклОбмена = СообщениеОснование.ЦиклОбмена;
		Объект.Отправитель = СообщениеОснование.ЦиклОбмена.Организация;
		Объект.Получатель = ?(НЕ СообщениеОснование.УчетнаяЗапись.ОбменНапрямую, СообщениеОснование.УчетнаяЗапись.СерверДокументооборота, СообщениеОснование.УчетнаяЗапись.НалоговыйОрган);
		Объект.УчетнаяЗапись = СообщениеОснование.УчетнаяЗапись;
		Объект.Статус = Перечисления.СтатусыПисем.Исходящее;
		Объект.Основание = СообщениеОснование;
		Объект.Тема = "ФНС:1.0-извещениеОПолучении-ИзвещениеРезультатОбработкиНП";
		
		НачатьТранзакцию();
		
		// пишем текущее сообщение, чтобы было к чему добавлять вложения
		Попытка
			Объект.Записать();
		Исключение
			ОтменитьТранзакцию();
			ТекстСообщения = СтроковыеФункцииКлиентСервер.ПодставитьПараметрыВСтроку(НСтр("ru='Ошибка записи сообщения %1 цикла обмена %2!'"),
			Символ(34) + Объект.Тип + Символ(34),
			Символ(34) + Объект.ЦиклОбмена + Символ(34));
			ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
			Объект.ОшибкаЗаполнения = Истина;
			Возврат;
		КонецПопытки;
		
		Объект.ОшибкаЗаполнения = Ложь;
		ЗафиксироватьТранзакцию();
		
	ИначеЕсли СообщениеОснование.Тип = Перечисления.ТипыТранспортныхСообщений.ПисьмоНО Тогда
		
		ОтветноеСообщение = ОтветноеСообщениеПоСообщениюОснованию(СообщениеОснование, Перечисления.ТипыТранспортныхСообщений.ИзвещениеПисьмоНП);
		Если ОтветноеСообщение <> Неопределено Тогда
			
			Объект.УжеСуществует = Истина;
			Объект.СуществующееСообщение = ОтветноеСообщение;
			
			Возврат;
		КонецЕсли;
		
		Объект.Тип = Перечисления.ТипыТранспортныхСообщений.ИзвещениеПисьмоНП;
		Объект.ЦиклОбмена = СообщениеОснование.ЦиклОбмена;
		Объект.Отправитель = СообщениеОснование.ЦиклОбмена.Организация;
		Объект.Получатель = ?(НЕ СообщениеОснование.УчетнаяЗапись.ОбменНапрямую, СообщениеОснование.УчетнаяЗапись.СерверДокументооборота, СообщениеОснование.УчетнаяЗапись.НалоговыйОрган);
		Объект.УчетнаяЗапись = СообщениеОснование.УчетнаяЗапись;
		Объект.Статус = Перечисления.СтатусыПисем.Исходящее;
		Объект.Основание = СообщениеОснование;
		Объект.Тема = "ФНС:1.0-извещениеОПолучении-ИзвещениеПисьмоНП";
		
		НачатьТранзакцию();
		
		// пишем текущее сообщение, чтобы было к чему добавлять вложения
		Попытка
			Объект.Записать();
		Исключение
			ОтменитьТранзакцию();
			ТекстСообщения = СтроковыеФункцииКлиентСервер.ПодставитьПараметрыВСтроку(НСтр("ru='Ошибка записи сообщения %1 цикла обмена %2!'"),
			Символ(34) + Объект.Тип + Символ(34),
			Символ(34) + Объект.ЦиклОбмена + Символ(34));
			ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
			Объект.ОшибкаЗаполнения = Истина;
			Возврат;
		КонецПопытки;
		
		Объект.ОшибкаЗаполнения = Ложь;
		ЗафиксироватьТранзакцию();
		
	ИначеЕсли СообщениеОснование.Тип = Перечисления.ТипыТранспортныхСообщений.РассылкаНО Тогда
		
		Если СообщениеОснование.ЦиклОбмена.Тип = Перечисления.ТипыЦикловОбмена.Рассылка Тогда // формируем ответ только на групповую рассылку
			Возврат;
		КонецЕсли;
		
		ОтветноеСообщение = ОтветноеСообщениеПоСообщениюОснованию(СообщениеОснование, Перечисления.ТипыТранспортныхСообщений.ИзвещениеРассылкаНП);
		Если ОтветноеСообщение <> Неопределено Тогда
			
			Объект.УжеСуществует = Истина;
			Объект.СуществующееСообщение = ОтветноеСообщение;
			
			Возврат;
		КонецЕсли;
		
		Объект.Тип = Перечисления.ТипыТранспортныхСообщений.ИзвещениеРассылкаНП;
		Объект.ЦиклОбмена = СообщениеОснование.ЦиклОбмена;
		Объект.Отправитель = СообщениеОснование.ЦиклОбмена.Организация;
		Объект.Получатель = ?(НЕ СообщениеОснование.УчетнаяЗапись.ОбменНапрямую, СообщениеОснование.УчетнаяЗапись.СерверДокументооборота, СообщениеОснование.УчетнаяЗапись.НалоговыйОрган);
		Объект.УчетнаяЗапись = СообщениеОснование.УчетнаяЗапись;
		Объект.Статус = Перечисления.СтатусыПисем.Исходящее;
		Объект.Основание = СообщениеОснование;
		Объект.Тема = "ФНС:1.0-извещениеОПолучении-ИзвещениеРассылкаНП";
		
		НачатьТранзакцию();
		
		// пишем текущее сообщение, чтобы было к чему добавлять вложения
		Попытка
			Объект.Записать();
		Исключение
			ОтменитьТранзакцию();
			ТекстСообщения = СтроковыеФункцииКлиентСервер.ПодставитьПараметрыВСтроку(НСтр("ru='Ошибка записи сообщения %1 цикла обмена %2!'"),
			Символ(34) + Объект.Тип + Символ(34),
			Символ(34) + Объект.ЦиклОбмена + Символ(34));
			ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
			Объект.ОшибкаЗаполнения = Истина;
			Возврат;
		КонецПопытки;
		
		Объект.ОшибкаЗаполнения = Ложь;
		ЗафиксироватьТранзакцию();
		
	ИначеЕсли СообщениеОснование.Тип = Перечисления.ТипыТранспортныхСообщений.ДокументНО Тогда
		
		Если Объект.Тема = "ФНС:1.0-квитанцияОПриеме-РезультатПриемаДокументНП" 
		 ИЛИ Объект.Тема = "ФНС:1.0-уведомлениеОбОтказе-РезультатПриемаДокументНП" Тогда
		    ПоложительныйРезультатПриема = (Объект.Тема = "ФНС:1.0-квитанцияОПриеме-РезультатПриемаДокументНП");
			
			ОтветноеСообщение = ОтветноеСообщениеПоСообщениюОснованию(СообщениеОснование, Перечисления.ТипыТранспортныхСообщений.РезультатПриемаДокументНП);
			Если ОтветноеСообщение <> Неопределено Тогда
				
				Объект.УжеСуществует = Истина;
				Объект.СуществующееСообщение = ОтветноеСообщение;
				
				Возврат;
			КонецЕсли;
			
			Объект.Тип = Перечисления.ТипыТранспортныхСообщений.РезультатПриемаДокументНП;
			Объект.ЦиклОбмена = СообщениеОснование.ЦиклОбмена;
			Объект.Отправитель = СообщениеОснование.ЦиклОбмена.Организация;
			Объект.Получатель = ?(НЕ СообщениеОснование.УчетнаяЗапись.ОбменНапрямую, СообщениеОснование.УчетнаяЗапись.СерверДокументооборота, СообщениеОснование.УчетнаяЗапись.НалоговыйОрган);
			Объект.УчетнаяЗапись = СообщениеОснование.УчетнаяЗапись;
			Объект.Статус = Перечисления.СтатусыПисем.Исходящее;
			Объект.Основание = СообщениеОснование;
			Если НЕ ПоложительныйРезультатПриема Тогда
				Объект.ПротоколСОшибкой = Истина;
			КонецЕсли;
			
			НачатьТранзакцию();
			
			// пишем текущее сообщение, чтобы было к чему добавлять вложения
			Попытка
				Объект.Записать();
			Исключение
				ОтменитьТранзакцию();
				ТекстСообщения = СтроковыеФункцииКлиентСервер.ПодставитьПараметрыВСтроку(НСтр("ru='Ошибка записи сообщения %1 цикла обмена %2!'"),
				Символ(34) + Объект.Тип + Символ(34),
				Символ(34) + Объект.ЦиклОбмена + Символ(34));
				ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
				Объект.ОшибкаЗаполнения = Истина;
				Возврат;
			КонецПопытки;
			
			Объект.ОшибкаЗаполнения = Ложь;
			ЗафиксироватьТранзакцию();
			
		Иначе
			
			ОтветноеСообщение = ОтветноеСообщениеПоСообщениюОснованию(СообщениеОснование, Перечисления.ТипыТранспортныхСообщений.ИзвещениеДокументНП);
			Если ОтветноеСообщение <> Неопределено Тогда
				
				Объект.УжеСуществует = Истина;
				Объект.СуществующееСообщение = ОтветноеСообщение;
				
				Возврат;
			КонецЕсли;
			
			Объект.Тип = Перечисления.ТипыТранспортныхСообщений.ИзвещениеДокументНП;
			Объект.ЦиклОбмена = СообщениеОснование.ЦиклОбмена;
			Объект.Отправитель = СообщениеОснование.ЦиклОбмена.Организация;
			Объект.Получатель = ?(НЕ СообщениеОснование.УчетнаяЗапись.ОбменНапрямую, СообщениеОснование.УчетнаяЗапись.СерверДокументооборота, СообщениеОснование.УчетнаяЗапись.НалоговыйОрган);
			Объект.УчетнаяЗапись = СообщениеОснование.УчетнаяЗапись;
			Объект.Статус = Перечисления.СтатусыПисем.Исходящее;
			Объект.Основание = СообщениеОснование;
			Объект.Тема = "ФНС:1.0-извещениеОПолучении-ИзвещениеДокументНП";
			
			НачатьТранзакцию();
			
			// пишем текущее сообщение, чтобы было к чему добавлять вложения
			Попытка
				Объект.Записать();
			Исключение
				ОтменитьТранзакцию();
				ТекстСообщения = СтроковыеФункцииКлиентСервер.ПодставитьПараметрыВСтроку(НСтр("ru='Ошибка записи сообщения %1 цикла обмена %2!'"),
				Символ(34) + Объект.Тип + Символ(34),
				Символ(34) + Объект.ЦиклОбмена + Символ(34));
				ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
				Объект.ОшибкаЗаполнения = Истина;
				Возврат;
			КонецПопытки;
			
			Объект.ОшибкаЗаполнения = Ложь;
			ЗафиксироватьТранзакцию();
			
		КонецЕсли;
		
	// Росстат
	ИначеЕсли СообщениеОснование.Тип = Перечисления.ТипыТранспортныхСообщений.ПодтверждениеДатыОтправкиФСГС Тогда
		//формируем квитанцию на подтверждение даты отправки ФСГС
		
		ОтветноеСообщение = ОтветноеСообщениеПоСообщениюОснованию(СообщениеОснование, Перечисления.ТипыТранспортныхСообщений.ИзвещениеПодтверждениеДатыОтправкиФСГС);
		Если ОтветноеСообщение <> Неопределено Тогда
			
			Объект.УжеСуществует = Истина;
			Объект.СуществующееСообщение = ОтветноеСообщение;
			
			Возврат;
		КонецЕсли;
		
		Объект.Тип = Перечисления.ТипыТранспортныхСообщений.ИзвещениеПодтверждениеДатыОтправкиФСГС;
		Объект.ЦиклОбмена = СообщениеОснование.ЦиклОбмена;
		Объект.Отправитель = СообщениеОснование.ЦиклОбмена.Организация;
		Объект.Получатель = СообщениеОснование.ЦиклОбмена.ВнешняяОрганизация;
		Объект.УчетнаяЗапись = СообщениеОснование.УчетнаяЗапись;
		Объект.Статус = Перечисления.СтатусыПисем.Исходящее;
		Объект.Основание = СообщениеОснование;
		Объект.Тема = "ReSign: Stat-Confirmation";
		
		НачатьТранзакцию();
		
		// пишем текущее сообщение, чтобы было к чему добавлять вложения
		Попытка
			Объект.Записать();
		Исключение
			ОтменитьТранзакцию();
			ТекстСообщения = СтроковыеФункцииКлиентСервер.ПодставитьПараметрыВСтроку(НСтр("ru='Ошибка записи сообщения %1 цикла обмена %2!'"),
																						Символ(34) + Объект.Тип + Символ(34),
																						Символ(34) + Объект.ЦиклОбмена + Символ(34));
			ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
			Объект.ОшибкаЗаполнения = Истина;
			Возврат;
		КонецПопытки;
		
		Объект.ОшибкаЗаполнения = Ложь;
		ЗафиксироватьТранзакцию();

	ИначеЕсли СообщениеОснование.Тип = Перечисления.ТипыТранспортныхСообщений.ПротоколВходногоКонтроляОтчетностиФСГС Тогда
		//формируем подтверждение на протокол ФСГС
		
		ОтветноеСообщение = ОтветноеСообщениеПоСообщениюОснованию(СообщениеОснование, Перечисления.ТипыТранспортныхСообщений.ИзвещениеПротоколВходногоКонтроляОтчетностиФСГС);
		Если ОтветноеСообщение <> Неопределено Тогда
			
			Объект.УжеСуществует = Истина;
			Объект.СуществующееСообщение = ОтветноеСообщение;
			

			Возврат;
		КонецЕсли;
		
		Объект.Тип = Перечисления.ТипыТранспортныхСообщений.ИзвещениеПротоколВходногоКонтроляОтчетностиФСГС;
		Объект.ЦиклОбмена = СообщениеОснование.ЦиклОбмена;
		Объект.Отправитель = СообщениеОснование.ЦиклОбмена.Организация;
		Объект.Получатель = СообщениеОснование.ЦиклОбмена.ВнешняяОрганизация;
		Объект.УчетнаяЗапись = СообщениеОснование.УчетнаяЗапись;
		Объект.Статус = Перечисления.СтатусыПисем.Исходящее;
		Объект.Основание = СообщениеОснование;
		Объект.Тема = "ReSign: Stat-Protocol";
		
		НачатьТранзакцию();
		
		// пишем текущее сообщение, чтобы было к чему добавлять вложения
		Попытка
			Объект.Записать();
		Исключение
			ОтменитьТранзакцию();
			ТекстСообщения = СтроковыеФункцииКлиентСервер.ПодставитьПараметрыВСтроку(НСтр("ru='Ошибка записи сообщения %1 цикла обмена %2!'"),
																						Символ(34) + Объект.Тип + Символ(34),
																						Символ(34) + Объект.ЦиклОбмена + Символ(34));
			ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
			Объект.ОшибкаЗаполнения = Истина;
			Возврат;
		КонецПопытки;
		
		Объект.ОшибкаЗаполнения = Ложь;
		ЗафиксироватьТранзакцию();
		
	ИначеЕсли СообщениеОснование.Тип = Перечисления.ТипыТранспортныхСообщений.ПисьмоВходящееФСГС Тогда
		// формируем подтверждение на входящее неформализованное от ФСГС
		
		ОтветноеСообщение = ОтветноеСообщениеПоСообщениюОснованию(СообщениеОснование, Перечисления.ТипыТранспортныхСообщений.ИзвещениеПисьмоВходящееФСГС);
		Если ОтветноеСообщение <> Неопределено Тогда
			
			Объект.УжеСуществует = Истина;
			Объект.СуществующееСообщение = ОтветноеСообщение;
			
			Возврат;
		КонецЕсли;
		
		Объект.Тип = Перечисления.ТипыТранспортныхСообщений.ИзвещениеПисьмоВходящееФСГС;
		Объект.ЦиклОбмена = СообщениеОснование.ЦиклОбмена;
		Объект.Отправитель = СообщениеОснование.ЦиклОбмена.Организация;
		Объект.Получатель = СообщениеОснование.ЦиклОбмена.ВнешняяОрганизация;
		Объект.УчетнаяЗапись = СообщениеОснование.УчетнаяЗапись;
		Объект.Статус = Перечисления.СтатусыПисем.Исходящее;
		Объект.Основание = СообщениеОснование;
		Объект.Тема = "ReSign: Stat-Unformal";
		
		НачатьТранзакцию();
		
		// пишем текущее сообщение, чтобы было к чему добавлять вложения
		Попытка
			Объект.Записать();
		Исключение
			ОтменитьТранзакцию();
			ТекстСообщения = СтроковыеФункцииКлиентСервер.ПодставитьПараметрыВСтроку(НСтр("ru='Ошибка записи сообщения %1 цикла обмена %2!'"),
																						Символ(34) + Объект.Тип + Символ(34),
																						Символ(34) + Объект.ЦиклОбмена + Символ(34));
			ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
			Объект.ОшибкаЗаполнения = Истина;
			Возврат;
		КонецПопытки;
		
		Объект.ОшибкаЗаполнения = Ложь;
		ЗафиксироватьТранзакцию();
		
	КонецЕсли;
	
КонецПроцедуры

Функция СоздатьИЗаполнитьОтветноеСообщение(Основание) Экспорт
	
	ОтветноеСообщение = Документы.ТранспортноеСообщение.СоздатьДокумент();

	ОбработкаЗаполненияТранспортногоСообщения(ОтветноеСообщение, Основание);
	
	Если ОтветноеСообщение.ОшибкаЗаполнения = Истина Тогда
		Возврат Неопределено;
	КонецЕсли;
	
	УжеСуществует = ОтветноеСообщение.УжеСуществует = Истина;
	
	Если УжеСуществует И ОтветноеСообщение.СуществующееСообщение <> Неопределено Тогда
		Ссылка = ОтветноеСообщение.СуществующееСообщение;
	Иначе
		Ссылка = ОтветноеСообщение.Ссылка;
	КонецЕсли;
	
	Результат = Новый Структура();
	Результат.Вставить("Ссылка",                Ссылка);
	Результат.Вставить("Тип",                   Ссылка.Тип);
	Результат.Вставить("УчетнаяЗапись",         Ссылка.УчетнаяЗапись);
	Результат.Вставить("УжеСуществует",         УжеСуществует);
	Результат.Вставить("СуществующееСообщение", ОтветноеСообщение.СуществующееСообщение);
	
	Возврат Результат;
	
КонецФункции

Функция НужноСоздатьИЗаполнитьОтветноеСообщение(Основание) Экспорт
	
	НужноСоздатьСообщение = Ложь;
	
	Если ТипЗнч(Основание) <> Тип("ДокументСсылка.ТранспортноеСообщение")
		ИЛИ НЕ СообщениеРасшифровано(Основание)
		ИЛИ НЕ ЗначениеЗаполнено(Основание.ЦиклОбмена) Тогда
		
		НужноСоздатьСообщение = Истина;
		
	Иначе
		
		ОтветноеСообщение = ОтветноеСообщениеПоСообщениюОснованию(Основание);
		НужноСоздатьСообщение = ОтветноеСообщение = Неопределено;
				
	КонецЕсли;
	
	Возврат НужноСоздатьСообщение;
	
КонецФункции

Функция ОтветноеСообщениеПоСообщениюОснованию(Основание, ТипСообщения = Неопределено) Экспорт
	
	Если ТипСообщения = Неопределено Тогда
		ТипыСообщений = ТипыСообщенийПоТипамОснований();
		ТипСообщения  = ТипыСообщений.Получить(Основание.Тип);
	КонецЕсли;
	
	Если ТипСообщения <> Неопределено Тогда
		// Если сообщение уже создано, то новое создавать не нужно, получем существующее.
		НовыеСообщения = ПолучитьСообщенияВведенныеНаОсновании(Основание, ТипСообщения);
		Если НовыеСообщения.Количество() > 0 Тогда
			ОтветноеСообщение = НовыеСообщения[0];
			Возврат ОтветноеСообщение;
		КонецЕсли;
	КонецЕсли;
	
	Возврат Неопределено;
	
КонецФункции

// перенесена в контейнер
Функция ОбработатьСодержаниеОснования(Содержание)
	
	Результат = "";
	Для Инд = 1 По СтрЧислоСтрок(Содержание) Цикл
		Результат = Результат + "> " + СтрПолучитьСтроку(Содержание, Инд);
	КонецЦикла;
	Возврат Результат;
	
КонецФункции

//////////////////////////////////////////////////////////////////////////////////
//// ОБРАБОТЧИКИ СОБЫТИЙ СПРАВОЧНИКА ПЕРЕПИСКАСКОНТРОЛИРУЮЩИМИОРГАНАМИ


Процедура ПередЗаписьюПерепискиСКонтролирующимиОрганами(Объект, Отказ)

	Если Объект.ЭтоНовый() Тогда // именно без проверки заполнения Идентификатора, т.к. возможна запись скопированного
		Если Объект.Статус <> Перечисления.СтатусыПисем.Полученное ИЛИ НЕ ЗначениеЗаполнено(Объект.Идентификатор) Тогда
 			Объект.Идентификатор = СгенерироватьUUID();
		КонецЕсли;
	КонецЕсли;
	
	ТипОрганизации = Тип("СправочникСсылка.Организации");
	Если ТипЗнч(Объект.Отправитель) = ТипОрганизации Тогда
		Объект.Организация = Объект.Отправитель;
	ИначеЕсли ТипЗнч(Объект.Получатель) = ТипОрганизации Тогда
		Объект.Организация = Объект.Получатель;
	Иначе
		Объект.Организация = Неопределено;
	КонецЕсли;
	
	НедопустимыеСимволы = ПроверитьНеДопустимыеСимволы(Объект.Наименование);
	Если НедопустимыеСимволы.Количество() > 0 Тогда
		Объект.Наименование = ЗаменитьНеДопустимыеСимволы(Объект.Наименование, НедопустимыеСимволы, " ");
	КонецЕсли;
	
	НедопустимыеСимволы = ПроверитьНеДопустимыеСимволы(Объект.Содержание);
	Если НедопустимыеСимволы.Количество() > 0 Тогда
		Объект.Содержание = ЗаменитьНеДопустимыеСимволы(Объект.Содержание, НедопустимыеСимволы, " ");
	КонецЕсли;
	
КонецПроцедуры

Функция  ОбработкаЗаполненияПисьма(Объект, Основание, НастройкиОтвета)
	
	ЭтоРетроконверсия = (НастройкиОтвета <> Неопределено);
	// если это ретроконверсия, то обработка особая
	Если ЭтоРетроконверсия Тогда
		
		ВариантПодтверждения = НастройкиОтвета.ВариантПодтверждения;
		ПодтвержденныеФайлыСведений = НастройкиОтвета.ФайлыСведений;
		
		// заполняем свойства (кроме вложений)
		Объект.Наименование = "Re:" + СокрЛП(Основание.Наименование); // требование приемного ПО Калуга-Астрал
		Объект.Тип = Основание.Тип;
		Объект.Статус = Перечисления.СтатусыПисем.Исходящее;
		Объект.Отправитель = Основание.Получатель;
		Объект.Получатель = Основание.Отправитель;
		Объект.ДатаСообщения = ТекущаяДатаСеанса();
		Объект.ИдентификаторОснования = Основание.Идентификатор;
		Объект.Ретроконверсия = Основание.Ретроконверсия;
		
		ОбработанноеСодержимоеОснования = ОбработатьСодержаниеОснования(Основание.Содержание);
		Если ВариантПодтверждения = 2 Тогда // отклонение сведений
			Объект.Содержание = "Сведения содержат ошибки." + Символы.ПС + Символы.ПС + ОбработанноеСодержимоеОснования;
		Иначе
			
			Если ВариантПодтверждения = 3 Тогда // частичное подтверждение
				Объект.Содержание = "Сведения подтверждены лишь частично." + Символы.ПС + Символы.ПС + ОбработанноеСодержимоеОснования;
			Иначе
				Объект.Содержание = "Сведения верны." + Символы.ПС + Символы.ПС + ОбработанноеСодержимоеОснования;
			КонецЕсли;
			
			Вложения = ПолучитьВложенияНеформализованногоДокумента(Основание, , Истина);
			
			НачатьТранзакцию();
			
			Попытка
				Объект.Записать();
			Исключение
				ОтменитьТранзакцию();
				ТекстСообщения = СтроковыеФункцииКлиентСервер.ПодставитьПараметрыВСтроку(НСтр("ru='Не удалось создать ответ:%1'"),
																							Символы.ПС + ИнформацияОбОшибке().Описание);
				ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
				Возврат Неопределено;
			КонецПопытки;
			
			// копируем вложения
			Для Каждого Вложение Из Вложения Цикл
				Если ПодтвержденныеФайлыСведений.Найти(Вложение.ИмяФайла) <> Неопределено Тогда
					Если НЕ ДобавитьВложенияПисьма(Объект.Ссылка, Вложение.ИмяФайла, Вложение.Данные, Вложение.Тип, Вложение.Размер) Тогда
						ОтменитьТранзакцию();
						Возврат Неопределено;
					КонецЕсли;
				КонецЕсли;
			КонецЦикла;
			
			ЗафиксироватьТранзакцию();
			
		КонецЕсли;
		
		Возврат Объект;
		
	Иначе
	
		// получаем вложения и, при необходимости, задаем уточняющий вопрос
		Вложения = ПолучитьВложенияНеформализованногоДокумента(Основание.Ссылка, , Истина);
		
		// заполняем свойства (кроме вложений)
		Объект.Наименование = "Ответ на: " + СокрЛП(Основание.Наименование);
		Объект.Тип = Основание.Тип;
		Объект.Статус = Перечисления.СтатусыПисем.Исходящее;
		Объект.Отправитель = Основание.Получатель;
		Объект.Получатель = Основание.Отправитель;
		Объект.Содержание = Символы.ПС + Символы.ПС + ОбработатьСодержаниеОснования(Основание.Содержание);
		Объект.ДатаСообщения = ТекущаяДатаСеанса();
		Объект.ИдентификаторОснования = Основание.Идентификатор;
		Объект.Ретроконверсия = Основание.Ретроконверсия;
		
		// записываем, если необходимо скопировать вложения
		Если Вложения.Количество() > 0 Тогда
			
			// открываем транзакцию
			НачатьТранзакцию();
			
			Попытка
				Объект.Записать();
			Исключение
				ОтменитьТранзакцию();
				ТекстСообщения = СтроковыеФункцииКлиентСервер.ПодставитьПараметрыВСтроку(НСтр("ru='Не удалось создать ответ:%1'"),
																							Символы.ПС + ИнформацияОбОшибке().Описание);
				ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
				Возврат Неопределено;
			КонецПопытки;
			
			// копируем вложения
			Для Каждого Вложение Из Вложения Цикл
				Если НЕ ДобавитьВложенияПисьма(Объект.Ссылка, Вложение.ИмяФайла, Вложение.Данные, Вложение.Тип, Вложение.Размер) Тогда
					ОтменитьТранзакцию();
					Возврат Неопределено;
				КонецЕсли;
			КонецЦикла;
			
			ЗафиксироватьТранзакцию();
			
		КонецЕсли;
		
		Возврат Объект;
		
	КонецЕсли;
	
КонецФункции

Процедура ДополнитьСписокПолученныхПисем(Письмо)
	
КонецПроцедуры

//////////////////////////////////////////////////////////////////////////////////

Функция ПолучитьСвойстваМодуляДокументооборота() Экспорт
	
	Запрос = Новый Запрос("ВЫБРАТЬ
	                      |	Константы.ДокументооборотСКонтролирующимиОрганами_ИспользоватьВнешнийМодуль,
	                      |	Константы.ДокументооборотСКонтролирующимиОрганами_ВнешнийМодуль
	                      |ИЗ
	                      |	Константы КАК Константы");
	
	Попытка
		Выборка = Запрос.Выполнить().Выбрать();
		Выборка.Следующий();
		Возврат Новый Структура("ИспользоватьВнешнийМодуль, ВнешнийМодуль", Выборка.ДокументооборотСКонтролирующимиОрганами_ИспользоватьВнешнийМодуль, Выборка.ДокументооборотСКонтролирующимиОрганами_ВнешнийМодуль);
	Исключение
		Возврат Неопределено;
	КонецПопытки;
	
КонецФункции

// Проверяем, разрешено ли изменение констант внешнего модуля; изменение проводить в привилегированном режиме, поскольку константы неразделенные
Функция ИзменениеСвойствМодуляДокументооборотаВозможно() Экспорт
	
	Возврат		(ПравоДоступа("Изменение", Метаданные.Константы.ДокументооборотСКонтролирующимиОрганами_ИспользоватьВнешнийМодуль)
			И	ПравоДоступа("Изменение", Метаданные.Константы.ДокументооборотСКонтролирующимиОрганами_ВнешнийМодуль))
			ИЛИ	ПравоДоступа("Изменение", Метаданные.Справочники.УчетныеЗаписиДокументооборота);
	
КонецФункции

Функция ПрименитьСвойстваМодуляДокументооборота(ИспользоватьВнешнийМодуль = Неопределено, ВнешнийМодуль = Неопределено, ОписаниеОшибки = Неопределено, ВерсияВнешнегоМодуля = "") Экспорт
	
	// определяем, какие константы будут меняться
	МодифицируемыеКонстанты = Новый Массив;
	
	// подготавливаем двоичные данные внешнего модуля
	Если ВнешнийМодуль <> Неопределено Тогда
		
		МодифицируемыеКонстанты.Добавить("ДокументооборотСКонтролирующимиОрганами_ВерсияВнешнегоМодуля");
		МодифицируемыеКонстанты.Добавить("ДокументооборотСКонтролирующимиОрганами_ВнешнийМодуль");
		
		ТипЗнчВнешнийМодуль = ТипЗнч(ВнешнийМодуль);
		Если ТипЗнчВнешнийМодуль = Тип("Строка") Тогда
			ОбъектВнешнийМодуль = Новый ХранилищеЗначения(Новый ДвоичныеДанные(ВнешнийМодуль));
			ФайлВнешнегоМодуля = ВнешнийМодуль;
		Иначе
			ФайлВнешнегоМодуля = ПолучитьИмяВременногоФайла();
			Если ТипЗнчВнешнийМодуль = Тип("ДвоичныеДанные") Тогда
				ОбъектВнешнийМодуль = Новый ХранилищеЗначения(ВнешнийМодуль);
			ИначеЕсли ТипЗнчВнешнийМодуль = Тип("ХранилищеЗначения") Тогда
				ОбъектВнешнийМодуль = ВнешнийМодуль;
			КонецЕсли;
			ОбъектВнешнийМодуль.Получить().Записать(ФайлВнешнегоМодуля);
		КонецЕсли;
		
		Если НЕ ЗначениеЗаполнено(ВерсияВнешнегоМодуля) Тогда
			ОписаниеОшибки = "Некорректный формат внешнего модуля: не удалось извлечь версию.";
			Возврат Ложь;
		КонецЕсли;
		
	КонецЕсли;
	
	Если ИспользоватьВнешнийМодуль <> Неопределено Тогда
		МодифицируемыеКонстанты.Добавить("ДокументооборотСКонтролирующимиОрганами_ИспользоватьВнешнийМодуль");
	КонецЕсли;
	
	КоличествоМодифицируемыхКонстант = МодифицируемыеКонстанты.Количество();
	Если КоличествоМодифицируемыхКонстант = 0 Тогда
		Возврат Истина;
	ИначеЕсли КоличествоМодифицируемыхКонстант = 1 Тогда
		СтрМодифицируемыеКонстанты = МодифицируемыеКонстанты[0];
	ИначеЕсли КоличествоМодифицируемыхКонстант > 1 Тогда
		СтрМодифицируемыеКонстанты = МодифицируемыеКонстанты[0] + ", " + МодифицируемыеКонстанты[1] + ", " + МодифицируемыеКонстанты[2];
	КонецЕсли;
	
	ИзменениеВнешнегоМодуляРазрешено = ИзменениеСвойствМодуляДокументооборотаВозможно();
	
	// инициализируем значения констант
	Попытка
		Если ИзменениеВнешнегоМодуляРазрешено Тогда
			УстановитьПривилегированныйРежим(Истина);
		КонецЕсли;
		КонстантыНабор = Константы.СоздатьНабор(СтрМодифицируемыеКонстанты);
		Если ИспользоватьВнешнийМодуль <> Неопределено Тогда
			КонстантыНабор.ДокументооборотСКонтролирующимиОрганами_ИспользоватьВнешнийМодуль = ИспользоватьВнешнийМодуль;
		КонецЕсли;
		Если ВнешнийМодуль <> Неопределено Тогда
			КонстантыНабор.ДокументооборотСКонтролирующимиОрганами_ВнешнийМодуль = ОбъектВнешнийМодуль;
			КонстантыНабор.ДокументооборотСКонтролирующимиОрганами_ВерсияВнешнегоМодуля = ВерсияВнешнегоМодуля;
		КонецЕсли;
		КонстантыНабор.Записать();
		Если ИзменениеВнешнегоМодуляРазрешено Тогда
			УстановитьПривилегированныйРежим(Ложь);
		КонецЕсли;
		Возврат Истина;
	Исключение
		ОписаниеОшибки = ИнформацияОбОшибке().Описание;
		Возврат Ложь;
	КонецПопытки;
	
КонецФункции

Функция ЗагрузитьXMLВДеревоЗначений(ИмяФайлаXML = Неопределено, ТекстXML = Неопределено, ОписаниеОшибки = Неопределено) Экспорт
	
	// считываем текст из файла
	Если НЕ ЗначениеЗаполнено(ТекстXML) Тогда
		Если НЕ ЭтоАдресВременногоХранилища(ИмяФайлаXML) Тогда
			ОбъектФайлXML = Новый Файл(ИмяФайлаXML);
			Если НЕ ОбъектФайлXML.Существует() Тогда
				Возврат Неопределено;
			КонецЕсли;
		КонецЕсли;
		ТекстXML = ПрочитатьТекстИзФайла(ИмяФайлаXML, , Истина);
		Если НЕ ЗначениеЗаполнено(ТекстXML) Тогда
			Возврат Неопределено;
		КонецЕсли;
	КонецЕсли;
	
	// восстанавливаем дерево
	Возврат ЗагрузитьСтрокуXMLВДеревоЗначений(ТекстXML, ОписаниеОшибки);
	
КонецФункции

Функция ПрочитатьBase64ИзФайла(ИмяФайла)
	
	МассивФайловПослеРазбиения = РазделитьФайл(ИмяФайла, 1024 * 1024 * 3);
	КоличествоФайловВМассиве = МассивФайловПослеРазбиения.Количество();
	СтрокаЗамены = Символ(13) + Символ(10);
	СтрокаРезультат = "";
	
	Для НомерФайла = 0 По КоличествоФайловВМассиве - 1 Цикл
		
		ВремФайл = МассивФайловПослеРазбиения[НомерФайла];
		
		ВременнаяСтрокаРезультата = Base64Строка(Новый ДвоичныеДанные(ВремФайл));
		ВременнаяСтрокаРезультата = СтрЗаменить(ВременнаяСтрокаРезультата, СтрокаЗамены, "");
		СтрокаРезультат = СтрокаРезультат + ВременнаяСтрокаРезультата;
		ОперацииСФайламиЭДКО.УдалитьВременныйФайл(ВремФайл);
		
	КонецЦикла;
	
	Возврат СтрокаРезультат;
	
КонецФункции

Функция СгенерироватьUUID(ФорматДокументооборота = Неопределено) Экспорт
	
	Если ФорматДокументооборота = Перечисления.ФорматыДокументооборотаСПФР.ЭДОК Тогда
		Возврат нрег(Строка(Новый УникальныйИдентификатор));
	Иначе
		Возврат нрег(СтрЗаменить(Строка(Новый УникальныйИдентификатор), "-", ""));
	КонецЕсли;
	
КонецФункции

Функция СоздатьВременныйКаталог() Экспорт
	
	ИмяКаталога = КаталогВременныхФайлов();
	РазделительПутиОС = ПолучитьРазделительПути();
	Если Прав(ИмяКаталога, 1) <> РазделительПутиОС Тогда
		ИмяКаталога = ИмяКаталога + РазделительПутиОС;
	КонецЕсли;
	
	ИмяКаталога = ИмяКаталога + Строка(Новый УникальныйИдентификатор) + РазделительПутиОС;
	СоздатьКаталог(ИмяКаталога);
	
	Возврат ИмяКаталога;
	
КонецФункции

Функция СоздатьДеревоXML() Экспорт
	
	ДеревоXML = Новый ДеревоЗначений;
	ДеревоXML.Колонки.Добавить("Код");
	ДеревоXML.Колонки.Добавить("Тип");
	ДеревоXML.Колонки.Добавить("Значение");
	ДеревоXML.Колонки.Добавить("ЭтоCDATA");
	Возврат ДеревоXML;
	
КонецФункции

Функция ДобавитьУзелXML(Родитель, Код, ЭтоАтрибут = Ложь, Значение = Неопределено, Необязательный = Ложь, ЭтоCDATA = Ложь) Экспорт
	
	Если Необязательный И НЕ ЗначениеЗаполнено(Значение) Тогда
		Возврат Неопределено;
	КонецЕсли;
	
	НовСтр = Родитель.Строки.Добавить();
	
	НовСтр.Код = Код;
	НовСтр.Значение = Значение;
	Если ТипЗнч(ЭтоАтрибут) = Тип("Булево") Тогда
		НовСтр.Тип = ?(ЭтоАтрибут, "А", "Э");
	Иначе
		НовСтр.Тип = ЭтоАтрибут;
	КонецЕсли;
	
	ТипЗнчРодитель = ТипЗнч(Родитель);
	Если (ТипЗнчРодитель = Тип("ДеревоЗначений") И Родитель.Колонки.Найти("ЭтоCDATA") <> Неопределено)
	ИЛИ (ТипЗнчРодитель = Тип("СтрокаДереваЗначений") И Родитель.Владелец().Колонки.Найти("ЭтоCDATA") <> Неопределено) Тогда
		НовСтр.ЭтоCDATA = ЭтоCDATA;
	КонецЕсли;
	
	Возврат НовСтр;
	
КонецФункции

Функция ВыгрузитьДеревоВXML_ПФР(
		ДеревоВыгрузки,
		ИмяКорневогоЭлемента = Неопределено,
		ФорматДокументооборота = Неопределено)
	
	ПотокXML = СоздатьНовыйПотокXML_ПФР();
	ЗаписатьУзелДереваВXML_ПФР(ДеревоВыгрузки, ПотокXML, ИмяКорневогоЭлемента);
	ТекстДляЗаписи = ПотокXML.Закрыть();
	ТекстДляЗаписи = ?(ФорматДокументооборота = Перечисления.ФорматыДокументооборотаСПФР.ЭДОК,
		"<?xml version=""1.0"" encoding=""UTF-8""?>", "<?xml version=""1.0"" encoding=""windows-1251""?>")
		+ Сред(ТекстДляЗаписи, СтрНайти(ТекстДляЗаписи, Символы.ПС));
	Возврат ТекстДляЗаписи;
	
КонецФункции

Функция СоздатьНовыйПотокXML_ПФР()
	
	ПотокXML = Новый ЗаписьXML();
	ПотокXML.УстановитьСтроку("UTF-8");
	ПотокXML.ЗаписатьОбъявлениеXML();
	ПотокXML.Отступ = Истина;
	Возврат ПотокXML;
	
КонецФункции

Функция ЗаписатьУзелДереваВXML_ПФР(СтрокаДерева, ПотокXML, ИмяКорневогоЭлемента)
	
	Если ТипЗнч(СтрокаДерева) = Тип("ДеревоЗначений") Тогда
		Если ЗначениеЗаполнено(ИмяКорневогоЭлемента) Тогда
			ПотокXML.ЗаписатьНачалоЭлемента(ИмяКорневогоЭлемента);
		КонецЕсли;
		Для каждого Стр Из СтрокаДерева.Строки Цикл
			ЗаписатьУзелДереваВXML_ПФР(Стр, ПотокXML, ИмяКорневогоЭлемента);
		КонецЦикла;
		Если ЗначениеЗаполнено(ИмяКорневогоЭлемента) Тогда
			ПотокXML.ЗаписатьКонецЭлемента();
		КонецЕсли;
	Иначе
		Если СтрокаДерева.Тип = "А" ИЛИ СтрокаДерева.Тип = "A" Тогда 
			ПотокXML.ЗаписатьАтрибут(СтрокаДерева.Код, Строка(СтрокаДерева.Значение));
		Иначе
			ПотокXML.ЗаписатьНачалоЭлемента(СтрокаДерева.Код);
			Для каждого Лист из СтрокаДерева.Строки Цикл
				ЗаписатьУзелДереваВXML_ПФР(Лист, ПотокXML, ИмяКорневогоЭлемента);
			КонецЦикла;
			ПотокXML.ЗаписатьТекст(Строка(СтрокаДерева.Значение));
			ПотокXML.ЗаписатьКонецЭлемента();
		КонецЕсли;
	КонецЕсли;

КонецФункции

Функция СоздатьОтветНаПисьмо(ОснованиеСсылка, НастройкиОтвета = Неопределено) Экспорт
	
	НовоеПисьмо = Справочники.ПерепискаСКонтролирующимиОрганами.СоздатьЭлемент();
	Попытка
		НовоеПисьмо = ОбработкаЗаполненияПисьма(НовоеПисьмо, ОснованиеСсылка, НастройкиОтвета);
	Исключение
		ТекстСообщения = СтроковыеФункцииКлиентСервер.ПодставитьПараметрыВСтроку(НСтр("ru='Не удалось создать ответ:%1'"),
																					Символы.ПС + ИнформацияОбОшибке().Описание);
		ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
		Возврат Неопределено;
	КонецПопытки;
	Если НовоеПисьмо <> Неопределено Тогда
		Возврат НовоеПисьмо;
	Иначе
		Возврат Неопределено;
	КонецЕсли;
	
КонецФункции

Функция ПолучитьИмяФайлаДИВЦиклаОбмена(Объект)
	
	Запрос = Новый Запрос("ВЫБРАТЬ РАЗРЕШЕННЫЕ ПЕРВЫЕ 1
						  |	СодержимоеТранспортныхКонтейнеров.ИмяФайла
						  |ИЗ
						  |	РегистрСведений.СодержимоеТранспортныхКонтейнеров КАК СодержимоеТранспортныхКонтейнеров
						  |ГДЕ
						  |	СодержимоеТранспортныхКонтейнеров.ТранспортноеСообщение.ЦиклОбмена = &ЦиклОбмена
						  |	И СодержимоеТранспортныхКонтейнеров.Тип В(&Тип)
						  |
						  |ОБЪЕДИНИТЬ ВСЕ
						  |
						  |ВЫБРАТЬ ПЕРВЫЕ 1
						  |	ТранспортныеКонтейнеры.ИмяФайла
						  |ИЗ
						  |	РегистрСведений.ТранспортныеКонтейнеры КАК ТранспортныеКонтейнеры
						  |ГДЕ
						  |	ТранспортныеКонтейнеры.ТранспортноеСообщение.ЦиклОбмена = &ЦиклОбмена");
	Запрос.УстановитьПараметр("ЦиклОбмена", ?(Объект.ЭтоНовый(), Объект.ПолучитьСсылкуНового(), Объект.Ссылка));
	ТипыВложений = Новый Массив;
	ТипыВложений.Добавить(Перечисления.ТипыСодержимогоТранспортногоКонтейнера.ФайлОтчетности);
	ТипыВложений.Добавить(Перечисления.ТипыСодержимогоТранспортногоКонтейнера.Заявление);
	ТипыВложений.Добавить(Перечисления.ТипыСодержимогоТранспортногоКонтейнера.Форма2НДФЛ);
	ТипыВложений.Добавить(Перечисления.ТипыСодержимогоТранспортногоКонтейнера.НеформализованныйДокумент);
	ТипыВложений.Добавить(Перечисления.ТипыСодержимогоТранспортногоКонтейнера.ЗапросИОН);
	ТипыВложений.Добавить(Перечисления.ТипыСодержимогоТранспортногоКонтейнера.ЗапросИОС);
	ТипыВложений.Добавить(Перечисления.ТипыСодержимогоТранспортногоКонтейнера.Обращение);
	ТипыВложений.Добавить(Перечисления.ТипыСодержимогоТранспортногоКонтейнера.Представление);
	ТипыВложений.Добавить(Перечисления.ТипыСодержимогоТранспортногоКонтейнера.Запрос);
	ТипыВложений.Добавить(Перечисления.ТипыСодержимогоТранспортногоКонтейнера.Рассылка);
	ТипыВложений.Добавить(Перечисления.ТипыСодержимогоТранспортногоКонтейнера.Письмо);
	Запрос.УстановитьПараметр("Тип", ТипыВложений);
	РезультатЗапроса = Запрос.Выполнить();
	Если НЕ РезультатЗапроса.Пустой() Тогда
		Выборка = РезультатЗапроса.Выбрать();
		Если Выборка.Следующий() Тогда
			Возврат СокрЛП(Выборка.ИмяФайла);
		КонецЕсли;
	КонецЕсли;
	Возврат "";
	
КонецФункции

Функция СформироватьИзвещениеОПолучении(ТранспортноеСообщение) Экспорт
	
	// проверяем, не сформировано ли уже извещение
	ИмеющиесяИзвещения = ПолучитьВложенияТранспортногоСообщения(ТранспортноеСообщение, Ложь, Перечисления.ТипыСодержимогоТранспортногоКонтейнера.ИзвещениеОПолучении);
	Если ИмеющиесяИзвещения.Количество() > 0 Тогда
		Возврат Истина;
	КонецЕсли;
	
	// определяем возможные типы документа, извещение о получении которого формируется
	Если ТранспортноеСообщение.Тип = Перечисления.ТипыТранспортныхСообщений.ИзвещениеПодтверждениеНП Тогда
		ТипыДИВ = Новый Массив;
		ТипыДИВ.Добавить(Перечисления.ТипыСодержимогоТранспортногоКонтейнера.ПодтверждениеДатыОтправки);
		ТипыДИВ.Добавить(Перечисления.ТипыСодержимогоТранспортногоКонтейнера.ПодтверждениеДатыОтправкиПредставление);
		ТипыДИВ.Добавить(Перечисления.ТипыСодержимогоТранспортногоКонтейнера.ПодтверждениеДатыПолучения);
	ИначеЕсли ТранспортноеСообщение.Тип = Перечисления.ТипыТранспортныхСообщений.ИзвещениеРезультатПриемаНП Тогда
		ТипыДИВ = Новый Массив;
		ТипыДИВ.Добавить(Перечисления.ТипыСодержимогоТранспортногоКонтейнера.КвитанцияОПриеме);
		ТипыДИВ.Добавить(Перечисления.ТипыСодержимогоТранспортногоКонтейнера.КвитанцияОПриемеДокумент);
		ТипыДИВ.Добавить(Перечисления.ТипыСодержимогоТранспортногоКонтейнера.КвитанцияОПриемеЗаявления);
		ТипыДИВ.Добавить(Перечисления.ТипыСодержимогоТранспортногоКонтейнера.УведомлениеОбОтказе);
		ТипыДИВ.Добавить(Перечисления.ТипыСодержимогоТранспортногоКонтейнера.УведомлениеОбОтказеДокумент);
		ТипыДИВ.Добавить(Перечисления.ТипыСодержимогоТранспортногоКонтейнера.Протокол2НДФЛ);
		ТипыДИВ.Добавить(Перечисления.ТипыСодержимогоТранспортногоКонтейнера.Реестр2НДФЛ);
	ИначеЕсли ТранспортноеСообщение.Тип = Перечисления.ТипыТранспортныхСообщений.ИзвещениеРезультатОбработкиНП Тогда
		ТипыДИВ = Новый Массив;
		ТипыДИВ.Добавить(Перечисления.ТипыСодержимогоТранспортногоКонтейнера.ИзвещениеОВводе);
		ТипыДИВ.Добавить(Перечисления.ТипыСодержимогоТранспортногоКонтейнера.УведомлениеОбУточнении);
		ТипыДИВ.Добавить(Перечисления.ТипыСодержимогоТранспортногоКонтейнера.Ответ);
		
		//Заявление
		ТипыДИВ.Добавить(Перечисления.ТипыСодержимогоТранспортногоКонтейнера.СообщениеОПростОтметки);
		ТипыДИВ.Добавить(Перечисления.ТипыСодержимогоТранспортногоКонтейнера.УведомлениеОбОтказеОтметки);
	ИначеЕсли ТранспортноеСообщение.Тип = Перечисления.ТипыТранспортныхСообщений.ИзвещениеРезультатОбработкиРФНП Тогда
		ТипыДИВ = Перечисления.ТипыСодержимогоТранспортногоКонтейнера.СообщениеОПроверке;
	ИначеЕсли ТранспортноеСообщение.Тип = Перечисления.ТипыТранспортныхСообщений.ИзвещениеОбОтзывеЗаявлениеРФНП Тогда
		ТипыДИВ = Перечисления.ТипыСодержимогоТранспортногоКонтейнера.СообщениеОбОтзывеЗаявления;
	ИначеЕсли ТранспортноеСообщение.Тип = Перечисления.ТипыТранспортныхСообщений.ИзвещениеРезультатОбработкиТСНП Тогда
		ТипыДИВ = Перечисления.ТипыСодержимогоТранспортногоКонтейнера.СообщениеОНесоответствиях;
	ИначеЕсли ТранспортноеСообщение.Тип = Перечисления.ТипыТранспортныхСообщений.ИзвещениеДокументНП Тогда
		ТипыДИВ = Новый Массив;
		ТипыДИВ.Добавить(Перечисления.ТипыСодержимогоТранспортногоКонтейнера.Документ);
		ТипыДИВ.Добавить(Перечисления.ТипыСодержимогоТранспортногоКонтейнера.Приложение);
		ТипыДИВ.Добавить(Перечисления.ТипыСодержимогоТранспортногоКонтейнера.ПодтверждениеДатыОтправки);
	ИначеЕсли ТранспортноеСообщение.Тип = Перечисления.ТипыТранспортныхСообщений.ИзвещениеПисьмоНП Тогда
		ТипыДИВ = Новый Массив;
		ТипыДИВ.Добавить(Перечисления.ТипыСодержимогоТранспортногоКонтейнера.Письмо);
		ТипыДИВ.Добавить(Перечисления.ТипыСодержимогоТранспортногоКонтейнера.Приложение);
	ИначеЕсли ТранспортноеСообщение.Тип = Перечисления.ТипыТранспортныхСообщений.ИзвещениеРассылкаНП Тогда
		ТипыДИВ = Перечисления.ТипыСодержимогоТранспортногоКонтейнера.Рассылка;
	КонецЕсли;
	
	// ищем ДИВ исходного сообщения
	ДИВы = ПолучитьВложенияТранспортногоСообщения(ТранспортноеСообщение.Основание, Ложь, ТипыДИВ);
	Если ДИВы.Количество() = 0 Тогда
		ТекстСообщения = НСтр("ru = 'Не удалось создать извещение о получении документа: отсутствует исходный документ в сообщении-основании.'");
		ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
		Возврат Ложь;
	КонецЕсли;
	ДИВы.Колонки.Добавить("ПодписьBase64");
	
	Для Каждого СтрДИВ Из ДИВы Цикл
		
		ИмяФайлаДИВ = СтрДИВ.ИмяФайла;
		
		// ищем подпись под ДИВ
		ПодписиСообщения = ПолучитьВложенияТранспортногоСообщения(ТранспортноеСообщение.Основание, Истина, Перечисления.ТипыСодержимогоТранспортногоКонтейнера.ЭлектронноЦифроваяПодпись);
		ПодписьПодДИВ = ПодписиСообщения.Найти(ИмяФайлаДИВ, "ЭЦПИмяПодписанногоФайла");
		Если ПодписьПодДИВ = Неопределено Тогда
			ТекстСообщения = НСтр("ru = 'Не удалось создать извещение о получении документа: отсутствует подпись под исходным документом.'");
			ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
			Возврат Ложь;
		КонецЕсли;
		
		// пишем подпись во временный файл
		ВремФайл = ПолучитьИмяВременногоФайла();
		Попытка
			ПодписьПодДИВ.Данные.Получить().Записать(ВремФайл);
		Исключение
		КонецПопытки;
		
		// формируем Base64-представление подписи
		СтрДИВ.ПодписьBase64 = ПрочитатьBase64ИзФайла(ВремФайл);
		Если НЕ ЗначениеЗаполнено(СтрДИВ.ПодписьBase64) Тогда
			ТекстСообщения = НСтр("ru = 'Не удалось создать извещение о получении документа: не удалось прочитать данные из файла подписи абонента под исходным документом.'");
			ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
			Возврат Ложь;
		КонецЕсли;
		ОперацииСФайламиЭДКО.УдалитьВременныйФайл(ВремФайл);
		
	КонецЦикла;
	
	// формируем имя файла извещения
	UUIDИзвещения = СгенерироватьUUID();
	ИмяФайлаИзвещения = СформироватьИмяФайлаИзвещения(ТранспортноеСообщение, ДИВы, UUIDИзвещения);
	Если НЕ ЗначениеЗаполнено(ИмяФайлаИзвещения) Тогда
		ТекстСообщения = НСтр("ru = 'Не удалось создать извещение о получении документа: не удалось сформировать имя файла извещения.'");
		ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
		Возврат Ложь;
	КонецЕсли;
	
	// формируем XML извещения о получении электронного документа
	ТекстИзвещения = СформироватьXMLИзвещенияОПолученииЭлектронногоДокумента(ТранспортноеСообщение, ДИВы, ИмяФайлаИзвещения);
	Если НЕ ЗначениеЗаполнено(ТекстИзвещения) Тогда
		Возврат Ложь;
	КонецЕсли;
	
	// пишем XML во временный файл в кодировке windows-1251
	ФайлИзвещения = ВыгрузитьТекстВФайл(ТекстИзвещения);
	Если НЕ ЗначениеЗаполнено(ФайлИзвещения) Тогда
		Возврат Ложь;
	КонецЕсли;
	
	// загружаем файл извещения в ИБ
	ТипСодержимогоТранспортногоКонтейнера = Перечисления.ТипыСодержимогоТранспортногоКонтейнера.ИзвещениеОПолучении;
	ДобавитьСодержимоеТранспортногоКонтейнера(ТранспортноеСообщение, ТипСодержимогоТранспортногоКонтейнера, ФайлИзвещения, ИмяФайлаИзвещения, , , , , , , UUIDИзвещения, Перечисления.ТипыСодержимогоФайлов.xml);
	
	// удаляем временный файл с извещением
	ОперацииСФайламиЭДКО.УдалитьВременныйФайл(ФайлИзвещения);
	
	Возврат Истина;
	
КонецФункции

Функция СформироватьИмяФайлаИзвещения(ТранспортноеСообщение, ДИВы, UUID)
	
	Префикс = "IZ";
	
	// выделяем префикс из имени основного ДИВ основания
	Если ДИВы.Количество() = 1 Тогда
		ОсновнойДИВ = ДИВы[0];
	Иначе
		ОсновнойДИВ = ДИВы.Найти(Перечисления.ТипыСодержимогоТранспортногоКонтейнера.Протокол2НДФЛ, "Тип");
		Если ОсновнойДИВ = Неопределено Тогда
			ОсновнойДИВ = ДИВы[0];
		КонецЕсли;
	КонецЕсли;
	ИмяФайлаДИВ = ОсновнойДИВ.ИмяФайла;
	
	ПрефиксОснования = ВыделитьПрефиксИзИмениФайлаОснования(ИмяФайлаДИВ);
	Если НЕ ЗначениеЗаполнено(ПрефиксОснования) Тогда
		// если префикс выделить не удалось, то пишем хоть какой-нибудь (это лучше, чем не отправлять извещение вообще)
		ПрефиксОснования = "NO_PREF";
	КонецЕсли;
	
	// формируем идентификатор получателя
	ИдентификаторПолучателя = СформироватьИдентификаторПолучателя(ТранспортноеСообщение);
	
	// формируем идентификатор отправителя
	ИдентификаторОтправителя = СформироватьИдентификаторОтправителя(ТранспортноеСообщение);
	
	// формируем строку с текущей датой
	GGGGMMDD = Формат(ТекущаяДатаСеанса(), "ДФ=yyyyMMdd");
	
	// 
	ИдентификаторФайла = UUID;
	
	//
	РасширениеФайла = "xml";
	
	// собираем имя файла извещения из составляющих
	ИмяФайлаИзвещения = Префикс + "_" + ПрефиксОснования + "_" + ИдентификаторПолучателя + "_" + ИдентификаторПолучателя + "_" + ИдентификаторОтправителя + "_" + GGGGMMDD + "_" + ИдентификаторФайла + "." + РасширениеФайла;
	Возврат ИмяФайлаИзвещения;
	
КонецФункции

Функция СформироватьИдентификаторПолучателя(ТранспортноеСообщение) Экспорт
	
	// если тип сообщения - ИзвещениеПодтверждениеНП или извещение о получении, то идентификатор получателя
	// берется из имени контейнера сообщения-основания
	// ранее для извещений о получении указывался код ТранспортноеСообщение.ЦиклОбмена.ВнешняяОрганизация.Код
	Если ТранспортноеСообщение.Тип = Перечисления.ТипыТранспортныхСообщений.ИзвещениеПодтверждениеНП 
	ИЛИ ТранспортноеСообщение.Тип = Перечисления.ТипыТранспортныхСообщений.ИзвещениеРезультатПриемаНП
	ИЛИ ТранспортноеСообщение.Тип = Перечисления.ТипыТранспортныхСообщений.ИзвещениеРезультатОбработкиНП 
	ИЛИ ТранспортноеСообщение.Тип = Перечисления.ТипыТранспортныхСообщений.ИзвещениеДокументНП
	ИЛИ ТранспортноеСообщение.Тип = Перечисления.ТипыТранспортныхСообщений.РезультатПриемаДокументНП
	ИЛИ ТранспортноеСообщение.Тип = Перечисления.ТипыТранспортныхСообщений.ИзвещениеПисьмоНП
	ИЛИ ТранспортноеСообщение.Тип = Перечисления.ТипыТранспортныхСообщений.ИзвещениеРассылкаНП
	ИЛИ ТранспортноеСообщение.Тип = Перечисления.ТипыТранспортныхСообщений.ИзвещениеРезультатОбработкиРФНП
	ИЛИ ТранспортноеСообщение.Тип = Перечисления.ТипыТранспортныхСообщений.ИзвещениеОбОтзывеЗаявлениеРФНП
	ИЛИ ТранспортноеСообщение.Тип = Перечисления.ТипыТранспортныхСообщений.ИзвещениеРезультатОбработкиТСНП Тогда
		Возврат ПолучитьИдентификаторОтправителяПодтвержденияДатыОтправки(ТранспортноеСообщение.Основание);
	Иначе
		Получатель = ТранспортноеСообщение.Получатель;
		ТипПолучателя = ТипЗнч(Получатель);
		Если ТипПолучателя = Тип("СправочникСсылка.СерверыДокументооборота") Тогда
			Возврат СокрЛП(ТранспортноеСообщение.УчетнаяЗапись.ИдентификаторСпецоператора);
		ИначеЕсли ТипПолучателя = Тип("СправочникСсылка.НалоговыеОрганы") Тогда
			Возврат СокрЛП(Получатель.Код);
		КонецЕсли;
	КонецЕсли;
	
КонецФункции

Функция СформироватьИдентификаторОтправителя(ТранспортноеСообщение) Экспорт
	
	Если ТранспортноеСообщение.УчетнаяЗапись.ЯвляетсяУчетнойЗаписьюУполномоченногоПредставителя Тогда
		
		Возврат СокрЛП(ТранспортноеСообщение.УчетнаяЗапись.ИННУполномоченногоПредставителя) + СокрЛП(ТранспортноеСообщение.УчетнаяЗапись.КППУполномоченногоПредставителя);
		
	Иначе
		
		ИдентификаторОтправителя = "";
		
		Отправитель = ТранспортноеСообщение.Отправитель;
		ИНН = СокрЛП(Отправитель.ИНН);
		Если НЕ РегламентированнаяОтчетностьВызовСервера.ЭтоЮридическоеЛицо(Отправитель) Тогда
			ИдентификаторОтправителя = ИНН;
		Иначе
			СведенияОбОрганизации = РегламентированнаяОтчетностьВызовСервера.ПолучитьСведенияОбОрганизации(Отправитель, , "КППЮЛ");
			ИдентификаторОтправителя = ИНН + СокрЛП(СведенияОбОрганизации.КППЮЛ);
		КонецЕсли;
		
		Возврат ИдентификаторОтправителя;
		
	КонецЕсли;
	
КонецФункции

Функция ВыделитьПрефиксИзИмениФайлаОснования(ИмяФайлаДИВ)
	
	РасширениеФайлаДИВ = нрег(РасширениеФайла(ИмяФайлаДИВ));
	Если РасширениеФайлаДИВ = "txt" Тогда // если это текстовый файл, то префикс - это то, что до первой цифры, кроме случая протокола 2-НДФЛ
		
		ПрефиксПротокол2НДФЛ1 = "ПРОТ_ДОХОД_2НДФЛ"; // префикс протокола 2-НДФЛ
		ПрефиксПротокол2НДФЛ2 = "PROT_NO_NDFL2"; // префикс протокола 2-НДФЛ
		ПрефиксРассылки = "IU_RASS"; // префикс файла рассылки
		Если нрег(Лев(ИмяФайлаДИВ, СтрДлина(ПрефиксПротокол2НДФЛ1))) = нрег(ПрефиксПротокол2НДФЛ1) Тогда
			Возврат СтрЗаменить(Лев(ИмяФайлаДИВ, СтрДлина(ПрефиксПротокол2НДФЛ1)), "_", "");
		ИначеЕсли нрег(Лев(ИмяФайлаДИВ, СтрДлина(ПрефиксПротокол2НДФЛ2))) = нрег(ПрефиксПротокол2НДФЛ2) Тогда
			Возврат СтрЗаменить(Лев(ИмяФайлаДИВ, СтрДлина(ПрефиксПротокол2НДФЛ2)), "_", "");
		ИначеЕсли нрег(Лев(ИмяФайлаДИВ, СтрДлина(ПрефиксРассылки))) = нрег(ПрефиксРассылки) Тогда
			Возврат СтрЗаменить(Лев(ИмяФайлаДИВ, СтрДлина(ПрефиксРассылки)), "_", "");
		Иначе
			СтрЦифры = "0123456789";
			Для Инд = 1 По СтрДлина(ИмяФайлаДИВ) Цикл
				ТекСимв = Сред(ИмяФайлаДИВ, Инд, 1);
				Если СтрНайти(СтрЦифры, ТекСимв) <> 0 Тогда
					Возврат СтрЗаменить(Лев(ИмяФайлаДИВ, Инд - 1), "_", "");
				КонецЕсли;
			КонецЦикла;
		КонецЕсли;
		
	Иначе
		
		Разделитель = "_";
		ИндексВхожденияРазделителя = 0;
		Для Инд = 1 По СтрДлина(ИмяФайлаДИВ) Цикл
			ТекСимв = Сред(ИмяФайлаДИВ, Инд, 1);
			Если ТекСимв = Разделитель Тогда
				ИндексВхожденияРазделителя = ИндексВхожденияРазделителя + 1;
				Если ИндексВхожденияРазделителя = 2 Тогда
					Возврат СтрЗаменить(Лев(ИмяФайлаДИВ, Инд - 1), "_", "");
				КонецЕсли;
			КонецЕсли;
		КонецЦикла;
		
	КонецЕсли;
	
	Возврат Неопределено;
	
КонецФункции

Функция СформироватьСведенияОПолучателеИзвещения(ТранспортноеСообщение)
	
	Если ТранспортноеСообщение.Тип = Перечисления.ТипыТранспортныхСообщений.ИзвещениеПодтверждениеНП Тогда
		УчастникДокументооборота = ТранспортноеСообщение.Получатель; // сервер документооборота СОС или ФНС
	Иначе
		УчастникДокументооборота = ТранспортноеСообщение.ЦиклОбмена.ВнешняяОрганизация;
	КонецЕсли;
	
	Возврат СформироватьСведенияОбУчастникеДокументооборота(УчастникДокументооборота, ТранспортноеСообщение);
	
КонецФункции

Функция СформироватьСведенияОбОтправителеИзвещения(ТранспортноеСообщение)
	
	Возврат СформироватьСведенияОбУчастникеДокументооборота(ТранспортноеСообщение.Отправитель, ТранспортноеСообщение);
	
КонецФункции

Функция СформироватьСведенияОбУчастникеДокументооборота(Участник, ТранспортноеСообщение) Экспорт
	
	Сведения = Новый Структура;
	
	УчетнаяЗапись = ТранспортноеСообщение.УчетнаяЗапись;
	
	ТипУчастника = ТипЗнч(Участник);
	Если ТипУчастника = Тип("СправочникСсылка.Организации") Тогда
		
		Сведения.Вставить("ТипУчастника", "Организация");
		Сведения.Вставить("Email", ?(ПустаяСтрока(УчетнаяЗапись.ИдентификаторАбонента), СокрЛП(УчетнаяЗапись.АдресЭлектроннойПочты), СокрЛП(УчетнаяЗапись.ИдентификаторАбонента)));
		
		Если УчетнаяЗапись.ЯвляетсяУчетнойЗаписьюУполномоченногоПредставителя Тогда
			
			ЭтоФизЛицо = (СтрДлина(СокрЛП(УчетнаяЗапись.ИННУполномоченногоПредставителя)) = 12);
			Сведения.Вставить("ЭтоФизЛицо", ЭтоФизЛицо);
			Если ЭтоФизЛицо Тогда
				
				Сведения.Вставить("ИНН", СокрЛП(УчетнаяЗапись.ИННУполномоченногоПредставителя));
				
				ФИО = РегламентированнаяОтчетность.РазложитьФИО(СокрЛП(УчетнаяЗапись.ПолноеНаименованиеУполномоченногоПредставителя));
				Сведения.Вставить("Фамилия", СокрЛП(ФИО.Фамилия));
				Сведения.Вставить("Имя", СокрЛП(ФИО.Имя));
				Сведения.Вставить("Отчество", СокрЛП(ФИО.Отчество));
				
			Иначе
				СведенияОбОрганизации = РегламентированнаяОтчетностьВызовСервера.ПолучитьСведенияОбОрганизации(Участник, , "НаимЮЛПол, ИННЮЛ, КППЮЛ");
				Сведения.Вставить("Наименование", СокрЛП(УчетнаяЗапись.ПолноеНаименованиеУполномоченногоПредставителя));
				Сведения.Вставить("ИНН", СокрЛП(УчетнаяЗапись.ИННУполномоченногоПредставителя));
				Сведения.Вставить("КПП", СокрЛП(УчетнаяЗапись.КППУполномоченногоПредставителя));
			КонецЕсли;
			
		Иначе
			
			ЭтоФизЛицо = НЕ РегламентированнаяОтчетностьВызовСервера.ЭтоЮридическоеЛицо(Участник);
			Сведения.Вставить("ЭтоФизЛицо", ЭтоФизЛицо);
			Если ЭтоФизЛицо Тогда
				
				Сведения.Вставить("ИНН", СокрЛП(Участник.ИНН));
				
				ФИО = ФИОИндивидуальногоПредпринимателя(Участник);
				Сведения.Вставить("Фамилия", СокрЛП(ФИО.Фамилия));
				Сведения.Вставить("Имя", СокрЛП(ФИО.Имя));
				Сведения.Вставить("Отчество", СокрЛП(ФИО.Отчество));
				
			Иначе
				СведенияОбОрганизации = РегламентированнаяОтчетностьВызовСервера.ПолучитьСведенияОбОрганизации(Участник, , "НаимЮЛПол, ИННЮЛ, КППЮЛ");
				Сведения.Вставить("Наименование", СокрЛП(СведенияОбОрганизации.НаимЮЛПол));
				Сведения.Вставить("ИНН", СокрЛП(СведенияОбОрганизации.ИННЮЛ));
				Сведения.Вставить("КПП", СокрЛП(СведенияОбОрганизации.КППЮЛ));
			КонецЕсли;
			
		КонецЕсли;
		
	ИначеЕсли ТипУчастника = Тип("СправочникСсылка.НалоговыеОрганы") Тогда
		
		Сведения.Вставить("ТипУчастника", "НалоговыйОрган");
		Если УчетнаяЗапись.ОбменНапрямую Тогда
			Сведения.Вставить("Email", Участник.АдресЭлектроннойПочтыДляЦелейДокументооборотаСНалогоплательщиками);
		Иначе
			Сведения.Вставить("Email", ПолучитьEmailОтправителяИзПодтвержденияОПолучении(ТранспортноеСообщение.ЦиклОбмена));
		КонецЕсли;
		
		Сведения.Вставить("Код", СокрЛП(Участник.Код));
		Сведения.Вставить("Наименование", СокрЛП(Участник.Наименование));
		
	ИначеЕсли ТипУчастника = Тип("СправочникСсылка.СерверыДокументооборота") Тогда
		
		// особая обработка, если описывается получатель ИзвещениеПодтверждениеНП при обмене напрямую
		Если УчетнаяЗапись.ОбменНапрямую И ТранспортноеСообщение.Тип = Перечисления.ТипыТранспортныхСообщений.ИзвещениеПодтверждениеНП Тогда
			
			Сведения.Вставить("ТипУчастника", "НалоговыйОрган");
			Сведения.Вставить("Код", ПолучитьИдентификаторОтправителяПодтвержденияДатыОтправки(ТранспортноеСообщение.Основание));
			Сведения.Вставить("Email", Участник.АдресЭлектроннойПочтыФНС);
			
		Иначе
			
			Сведения.Вставить("ТипУчастника", "СерверДокументооборота");
			Сведения.Вставить("Email", Участник.АдресЭлектроннойПочтыФНС);
			
			Сведения.Вставить("Идентификатор", СокрЛП(УчетнаяЗапись.ИдентификаторСпецоператора));
			Сведения.Вставить("Наименование", СокрЛП(УчетнаяЗапись.ИдентификаторСпецоператора));
			
		КонецЕсли;
		
	КонецЕсли;
	
	Возврат Сведения;
	
КонецФункции

Функция ПолучитьEmailОтправителяИзПодтвержденияОПолучении(ЦиклОбмена)
	
	// получаем извещение о получении декларации или 2-НДФЛ
	ИзвещениеДекларацияНО = ПолучитьСообщениеЦиклаОбмена(ЦиклОбмена, Перечисления.ТипыТранспортныхСообщений.ИзвещениеДекларацияНО);
	Если НЕ ЗначениеЗаполнено(ИзвещениеДекларацияНО) Тогда
		ИзвещениеДекларацияНО = ПолучитьСообщениеЦиклаОбмена(ЦиклОбмена, Перечисления.ТипыТранспортныхСообщений.ИзвещениеФорма2НДФЛНО);
		Если НЕ ЗначениеЗаполнено(ИзвещениеДекларацияНО) Тогда
			ИзвещениеДекларацияНО = ПолучитьСообщениеЦиклаОбмена(ЦиклОбмена, Перечисления.ТипыТранспортныхСообщений.ИзвещениеОбращениеНО);
			Если НЕ ЗначениеЗаполнено(ИзвещениеДекларацияНО) Тогда
				ИзвещениеДекларацияНО = ПолучитьСообщениеЦиклаОбмена(ЦиклОбмена, Перечисления.ТипыТранспортныхСообщений.ИзвещениеПредставлениеНО);
				Если НЕ ЗначениеЗаполнено(ИзвещениеДекларацияНО) Тогда
					Возврат Неопределено;
				КонецЕсли;
			КонецЕсли;
		КонецЕсли;
	КонецЕсли;
	
	// получаем данные извещения
	ИзвещенияОПолучении = ПолучитьВложенияТранспортногоСообщения(ИзвещениеДекларацияНО, Истина, Перечисления.ТипыСодержимогоТранспортногоКонтейнера.ИзвещениеОПолучении);
	Если ИзвещенияОПолучении.Количество() = 0 Тогда
		Возврат Неопределено;
	КонецЕсли;
	ДанныеИзвещенияОПолучении = ИзвещенияОПолучении[0].Данные;
	
	// выгружаем данные извещения во временный файл
	Попытка
		ФайлИзвещения = ПолучитьИмяВременногоФайла();
		ДанныеИзвещенияОПолучении.Получить().Записать(ФайлИзвещения);
	Исключение
		Возврат Неопределено;
	КонецПопытки;
	
	// создаем дерево из файла
	ДеревоXML = ЗагрузитьXMLВДеревоЗначений(ФайлИзвещения);
	ОперацииСФайламиЭДКО.УдалитьВременныйФайл(ФайлИзвещения);
	Если НЕ ЗначениеЗаполнено(ДеревоXML) Тогда
		Возврат Неопределено;
	КонецЕсли;
	
	// находим узел ПодтвДок
	УзелПодтвДок = ДеревоXML.Строки.Найти("ПодтвДок", "Имя", Истина);
	Если УзелПодтвДок = Неопределено Тогда
		Возврат Неопределено;
	КонецЕсли;
	
	// находим атрибут E-mail
	УзелEmail = УзелПодтвДок.Строки.Найти("E-mail", "Имя");
	Если УзелEmail = Неопределено ИЛИ НЕ ЗначениеЗаполнено(УзелEmail.Значение) Тогда
		Возврат Неопределено;
	Иначе
		Возврат СокрЛП(УзелEmail.Значение);
	КонецЕсли;
	
КонецФункции

Функция ПолучитьИдентификаторОтправителяПодтвержденияДатыОтправки(ПодтверждениеДатыОтправки)
	
	КонтейнерыОснования = ПолучитьТранспортныйКонтейнер(ПодтверждениеДатыОтправки);
	Если КонтейнерыОснования.Количество() = 0 Тогда
		Возврат Неопределено;
	КонецЕсли;
	ИмяФайлаКонтейнера = КонтейнерыОснования[0].ИмяФайла;
	
	КомпонентыИмени = РазложитьСтрокуВМассивПодстрок(ИмяФайлаКонтейнера, "_");
	Если КомпонентыИмени.Количество() < 2 Тогда
		Возврат Неопределено;
	КонецЕсли;
	
	Возврат СокрЛП(КомпонентыИмени[1]);
	
КонецФункции

Функция ИмяФайлаБезРасширения(ИмяФайлаСРасширением) Экспорт
	
	ДлинаИмениФайла = СтрДлина(ИмяФайлаСРасширением);
	Для Инд = 1 По ДлинаИмениФайла Цикл
		Если Сред(ИмяФайлаСРасширением, ДлинаИмениФайла - Инд + 1, 1) = "." Тогда
			Возврат Лев(ИмяФайлаСРасширением, ДлинаИмениФайла - Инд);
		КонецЕсли;
	КонецЦикла;
	Возврат ИмяФайлаСРасширением;
	
КонецФункции

// есть в Контейнере
Функция СформироватьXMLИзвещенияОПолученииЭлектронногоДокумента(ТранспортноеСообщение, ДИВы, ИмяФайлаИзвещения)
	
	// выделяем имя файла без расширения из короткого имени файла извещения
	РасширениеФайлаИзвещения = РасширениеФайла(ИмяФайлаИзвещения);
	ДлинаРасширенияИмениФайлаИзвещения = СтрДлина(РасширениеФайлаИзвещения);
	ИмяФайлаИзвещенияБезРасширения = Лев(ИмяФайлаИзвещения, СтрДлина(ИмяФайлаИзвещения) - ДлинаРасширенияИмениФайлаИзвещения - 1);
	
	// определяем дату получения основания
	ДатаПолученияОснования = ТранспортноеСообщение.Основание.ДатаТранспорта;
	
	// получаем сведения о получателе
	СведенияОПолучателе = СформироватьСведенияОПолучателеИзвещения(ТранспортноеСообщение);
	Если НЕ ЗначениеЗаполнено(СведенияОПолучателе) Тогда
		Возврат Неопределено;
	КонецЕсли;
	
	// получаем сведения об отправителе
	СведенияОбОтправителе = СформироватьСведенияОбОтправителеИзвещения(ТранспортноеСообщение);
	Если НЕ ЗначениеЗаполнено(СведенияОбОтправителе) Тогда
		Возврат Неопределено;
	КонецЕсли;
	
	// создаем XML-дерево извещения
	ДеревоXML = СоздатьДеревоXML();
	
	// иниицализируем атрибуты корневого элемента
	ДобавитьУзелXML(ДеревоXML, "ИдФайл", Истина, ИмяФайлаИзвещенияБезРасширения);
	ДобавитьУзелXML(ДеревоXML, "ВерсПрог", Истина, Лев(РегламентированнаяОтчетность.НазваниеИВерсияПрограммы(), 40));
	ДобавитьУзелXML(ДеревоXML, "ВерсФорм", Истина, "5.02");
	
	// создаем узел Документ
	УзелДокумент = ДобавитьУзелXML(ДеревоXML, "Документ");
		ДобавитьУзелXML(УзелДокумент, "КНД", Истина, "1167004");
	
	// добавляем сведения об организации
	УзелПодтвДок = ДобавитьУзелXML(УзелДокумент, "ПодтвДок");
	ЗаполнитьУзелСведениямиОбУчастникеЭДО(УзелПодтвДок, СведенияОбОтправителе);
	
	// создаем узел СвИзвещП
	УзелСвИзвещП = ДобавитьУзелXML(УзелДокумент, "СвИзвещП");
	ДобавитьУзелXML(УзелСвИзвещП, "ДатаПол", Истина, Формат(ДатаПолученияОснования, "ДФ=dd.MM.yyyy"));
	ДобавитьУзелXML(УзелСвИзвещП, "ВремяПол", Истина, Формат(ДатаПолученияОснования, "ДФ=HH.mm.ss"));
	
	Для Каждого СтрДИВ Из ДИВы Цикл
		
		// создаем узел СведПолФайл
		УзелСведПолФайл = ДобавитьУзелXML(УзелСвИзвещП, "СведПолФайл");
		ДобавитьУзелXML(УзелСведПолФайл, "ИмяПостФайла", Истина, ИмяФайлаБезРасширения(СтрДИВ.ИмяФайла));
		
		// добавляем подпись под ДИВ
		ДобавитьУзелXML(УзелСведПолФайл, "ЭЦППолФайл", , СтрДИВ.ПодписьBase64);
		
	КонецЦикла;
	
	// добавляем сведения об отправителе исходного документа
	УзелОтпрДок = ДобавитьУзелXML(УзелДокумент, "ОтпрДок");
	ЗаполнитьУзелСведениямиОбУчастникеЭДО(УзелОтпрДок, СведенияОПолучателе);
	
	// добавляем сведения о получателе исходного документа
	УзелПолДок = ДобавитьУзелXML(УзелДокумент, "ПолДок");
	ЗаполнитьУзелСведениямиОбУчастникеЭДО(УзелПолДок, СведенияОбОтправителе);
	
	// формируем XML-строку на основе дерева
	ТекстXML = ВыгрузитьДеревоВXML_ПФР(ДеревоXML, "Файл");
	
	Возврат ТекстXML;
	
КонецФункции

Процедура ЗаполнитьУзелСведениямиОбУчастникеЭДО(Узел, Сведения)
	
	ТипУчастникаЭДО = Сведения.ТипУчастника;
	Если ТипУчастникаЭДО = "Организация" Тогда
		
		Если ЗначениеЗаполнено(Сведения.Email) Тогда
			ДобавитьУзелXML(Узел, "E-mail", Истина, Сведения.Email);
		КонецЕсли;
		
		Если Сведения.ЭтоФизЛицо Тогда
			
			УзелФЛ = ДобавитьУзелXML(Узел, "ФЛ");
			ДобавитьУзелXML(УзелФЛ, "ИННФЛ", Истина, Сведения.ИНН);
			
			УзелФИО = ДобавитьУзелXML(УзелФЛ, "ФИО");
			ДобавитьУзелXML(УзелФИО, "Фамилия", Истина, Сведения.Фамилия);
			ДобавитьУзелXML(УзелФИО, "Имя", Истина, Сведения.Имя);
			Если ЗначениеЗаполнено(Сведения.Отчество) Тогда
				ДобавитьУзелXML(УзелФИО, "Отчество", Истина, Сведения.Отчество);
			КонецЕсли;
			
		Иначе
			
			УзелЮЛ = ДобавитьУзелXML(Узел, "ЮЛ");
			ДобавитьУзелXML(УзелЮЛ, "НаимОрг", Истина, Сведения.Наименование);
			ДобавитьУзелXML(УзелЮЛ, "ИННЮЛ", Истина, Сведения.ИНН);
			ДобавитьУзелXML(УзелЮЛ, "КПП", Истина, Сведения.КПП);
			
		КонецЕсли;
		
	ИначеЕсли ТипУчастникаЭДО = "НалоговыйОрган" Тогда
		
		Если ЗначениеЗаполнено(Сведения.Email) Тогда
			ДобавитьУзелXML(Узел, "E-mail", Истина, Сведения.Email);
		Иначе
			ДобавитьУзелXML(Узел, "E-mail", Истина, "-");
		КонецЕсли;
		ДобавитьУзелXML(Узел, "КодНО", , Сведения.Код);
		
	ИначеЕсли ТипУчастникаЭДО = "СерверДокументооборота" Тогда
		
		Если ЗначениеЗаполнено(Сведения.Email) Тогда
			ДобавитьУзелXML(Узел, "E-mail", Истина, Сведения.Email);
		КонецЕсли;
		
		УзелСпецоперат = ДобавитьУзелXML(Узел, "СпецОперат");
		ДобавитьУзелXML(УзелСпецоперат, "НаимОрг", Истина, Сведения.Наименование);
		ДобавитьУзелXML(УзелСпецоперат, "ИденСОС", Истина, Сведения.Идентификатор);
		
	КонецЕсли;
	
КонецПроцедуры

// Функция "расщепляет" строку на подстроки, используя заданный 
//		разделитель. Разделитель может иметь любую длину. 
//		Если в качестве разделителя задан пробел, рядом стоящие пробелы 
//		считаются одним разделителем, а ведущие и хвостовые пробелы параметра Стр
//		игнорируются.
//		Например, 
//		РазложитьСтрокуВМассивПодстрок(",ку,,,му", ",") возвратит массив значений из пяти элементов, 
//		три из которых - пустые строки, а 
//		РазложитьСтрокуВМассивПодстрок(" ку   му", " ") возвратит массив значений из двух элементов
//
//	Параметры: 
//		Стр - 			строка, которую необходимо разложить на подстроки. 
//						Параметр передается по значению.
//		Разделитель - 	строка-разделитель, по умолчанию - запятая.
//
//
//	Возвращаемое значение:
//		массив значений, элементы которого - подстроки
//
Функция РазложитьСтрокуВМассивПодстрок(Знач Стр, Разделитель = ",")
	
	МассивСтрок = Новый Массив();
	Если Разделитель = " " Тогда
		Стр = СокрЛП(Стр);
		Пока 1=1 Цикл
			Поз = СтрНайти(Стр,Разделитель);
			Если Поз=0 Тогда
				МассивСтрок.Добавить(Стр);
				Возврат МассивСтрок;
			КонецЕсли;
			МассивСтрок.Добавить(Лев(Стр,Поз-1));
			Стр = СокрЛ(Сред(Стр,Поз));
		КонецЦикла;
	Иначе
		ДлинаРазделителя = СтрДлина(Разделитель);
		Пока 1=1 Цикл
			Поз = СтрНайти(Стр,Разделитель);
			Если Поз=0 Тогда
				МассивСтрок.Добавить(Стр);
				Возврат МассивСтрок;
			КонецЕсли;
			МассивСтрок.Добавить(Лев(Стр,Поз-1));
			Стр = Сред(Стр,Поз+ДлинаРазделителя);
		КонецЦикла;
	КонецЕсли;
	
КонецФункции // глРазложить

Функция ПолучитьПредметыЦиклаОбмена(ЦиклОбмена) Экспорт
	
	Предметы = ЦиклОбмена.ДополнительныеПредметы.ВыгрузитьКолонку("Предмет");
	Предметы.Вставить(0, ЦиклОбмена.Предмет);
	Возврат Предметы;
	
КонецФункции

Функция ПолучитьОтветыНаЗапросИОН(парамЗапрос, СДанными = Ложь) Экспорт
	
	Запрос = Новый Запрос;
	Запрос.Текст = Запрос.Текст + "ВЫБРАТЬ РАЗРЕШЕННЫЕ
						  |	ОтветыНаЗапросыИОН.ИмяФайла,
						  |	ОтветыНаЗапросыИОН.Размер,
						  |	ОтветыНаЗапросыИОН.ТипСодержимого";
	Если СДанными Тогда
		Запрос.Текст = Запрос.Текст + ",
						  |	ОтветыНаЗапросыИОН.Данные";
	КонецЕсли;
	Запрос.Текст = Запрос.Текст + "
						  | , ВЫБОР
						  |		КОГДА СостояниеОбъектов.Архивный ЕСТЬ NULL ТОГДА Ложь
						  |		КОГДА СостояниеОбъектов.Архивный = Ложь ТОГДА Ложь
						  |	ИНАЧЕ
						  |		Истина
						  |	КОНЕЦ ВАрхиве
						  |ИЗ
						  |	РегистрСведений.ОтветыНаЗапросыИОН КАК ОтветыНаЗапросыИОН
						  |
						  |		ЛЕВОЕ СОЕДИНЕНИЕ РегистрСведений.ПризнакиАрхивированияФайловДОСКонтролирующимиОрганами КАК СостояниеОбъектов
						  |			ПО 		(СостояниеОбъектов.Объект = ОтветыНаЗапросыИОН.Запрос)
						  |				И	(СостояниеОбъектов.ИмяФайла = ОтветыНаЗапросыИОН.ИмяФайла)
						  |				И	(СостояниеОбъектов.Владелец = Значение(Перечисление.ВидыАрхивируемыхМетаданныхДО.ОтветыНаЗапросыИОН))
						  |
						  |ГДЕ
						  |	ОтветыНаЗапросыИОН.Запрос = &Запрос";
	Запрос.УстановитьПараметр("Запрос", парамЗапрос);
	Возврат Запрос.Выполнить().Выгрузить();
	
КонецФункции

Функция ДобавитьОтветНаЗапросИОН(Запрос, ИмяФайла, Данные, Размер = Неопределено, ТипСодержимогоФайла = Неопределено) Экспорт
	
	Если Размер = Неопределено Тогда
		Размер = ОпределитьРазмер(Данные);
	КонецЕсли;
	
	МенеджерЗаписи = РегистрыСведений.ОтветыНаЗапросыИОН.СоздатьМенеджерЗаписи();
	МенеджерЗаписи.Запрос = Запрос;
	МенеджерЗаписи.ИмяФайла = ИмяФайла;
	МенеджерЗаписи.Размер = Размер;
	
	Если ТипСодержимогоФайла <> Неопределено Тогда
		МенеджерЗаписи.ТипСодержимого = ТипСодержимогоФайла;
	КонецЕсли;
	
	ТипДанных = ТипЗнч(Данные);
	Если ТипДанных = Тип("ХранилищеЗначения") Тогда // если хранилище значения
		МенеджерЗаписи.Данные = Данные;
	ИначеЕсли ТипДанных = Тип("Строка") Тогда // если полное имя файла
		МенеджерЗаписи.Данные = Новый ХранилищеЗначения(Новый ДвоичныеДанные(Данные), Новый СжатиеДанных(9));
	Иначе // если двоичные данные
		МенеджерЗаписи.Данные = Новый ХранилищеЗначения(Данные, Новый СжатиеДанных(9));
	КонецЕсли;
	
	Попытка
		МенеджерЗаписи.Записать(Истина);
		Отказ = Ложь;
		ЭлектронныйДокументооборотСКонтролирующимиОрганами.ЗаписьОбъектовРегламентированнойОтчетности(Запрос, Отказ);
		
		Попытка
			МодульСверкаСФНС = ОбщегоНазначения.ОбщийМодуль("СверкаСФНС");
		Исключение
		    МодульСверкаСФНС = Неопределено;
		КонецПопытки;
		
		Если МодульСверкаСФНС <> Неопределено Тогда
			МодульСверкаСФНС.ПослеДобавленияОтветаНаЗапросИОН(Запрос, МенеджерЗаписи.Данные);
		КонецЕсли;
		
		Возврат Истина;
	Исключение
		ТекстСообщения = СтроковыеФункцииКлиентСервер.ПодставитьПараметрыВСтроку(НСтр("ru='Не удалось сохранить в информационной базе файл ответа на запрос ИОН %1!%2'"),
																					Запрос,
																					Символы.ПС + ИнформацияОбОшибке().Описание);
		ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
		Возврат Ложь;
	КонецПопытки;
	
КонецФункции

Процедура ОбновитьОтветНаЗапросИОНВРегистреОтветовПриНеобходимости(Объект)
	
	ЗапросИОН = Объект.ЦиклОбмена.Предмет;
	ОтветыНаЗапрос = ПолучитьОтветыНаЗапросИОН(ЗапросИОН);
	Если ОтветыНаЗапрос.Количество() = 0 Тогда
		ВложенияОтветы = ПолучитьВложенияТранспортногоСообщения(Объект.Ссылка, Истина, Перечисления.ТипыСодержимогоТранспортногоКонтейнера.Ответ);
		Для Каждого ВложениеОтвет Из ВложенияОтветы Цикл
			ДобавитьОтветНаЗапросИОН(ЗапросИОН, ВложениеОтвет.ИмяФайла, ВложениеОтвет.Данные, ВложениеОтвет.Размер, ВложениеОтвет.ТипсодержимогоФайла);
		КонецЦикла;
	КонецЕсли;
	
КонецПроцедуры

Функция ЗаписатьУзелДереваВXML(СтрокаДерева, ПотокXML)
	
	Если ТипЗнч(СтрокаДерева) = Тип("ДеревоЗначений") Тогда
		ПотокXML.ЗаписатьНачалоЭлемента("К_2_180_06_001_001");
		ПотокXML.ЗаписатьАтрибут("xmlns:xsi", "http://www.w3.org/2001/XMLSchema-instance");
		ПотокXML.ЗаписатьАтрибут("xsi:noNamespaceSchemaLocation", "NP_NO_2_180_06_001_001.xsd");
		Для каждого Стр Из СтрокаДерева.Строки Цикл
			ЗаписатьУзелДереваВXML(Стр, ПотокXML);
		КонецЦикла;
		ПотокXML.ЗаписатьКонецЭлемента();
	Иначе
		Если СтрокаДерева.Тип = "А" ИЛИ СтрокаДерева.Тип = "A" Тогда 
			ПотокXML.ЗаписатьАтрибут(СтрокаДерева.Код, Строка(СтрокаДерева.Значение));
		Иначе
			ПотокXML.ЗаписатьНачалоЭлемента(СтрокаДерева.Код);
			Если СтрокаДерева.Владелец().Колонки.Найти("ЭтоCDATA") <> Неопределено И СтрокаДерева.ЭтоCDATA Тогда
				ПотокXML.ЗаписатьСекциюCDATA(СтрокаДерева.Значение);
			Иначе
				Для каждого Лист из СтрокаДерева.Строки Цикл
					ЗаписатьУзелДереваВXML(Лист, ПотокXML);
				КонецЦикла;
				ПотокXML.ЗаписатьТекст(Строка(СтрокаДерева.Значение));
			КонецЕсли;
			ПотокXML.ЗаписатьКонецЭлемента();
		КонецЕсли;
	КонецЕсли;

КонецФункции

Функция СтрокаОС() Экспорт
	
	СисИнфо = Новый СистемнаяИнформация;
	Если СисИнфо.ТипПлатформы = ТипПлатформы.Linux_x86 Тогда
		Возврат "Linux32";
	ИначеЕсли СисИнфо.ТипПлатформы = ТипПлатформы.Linux_x86_64 Тогда
		Возврат "Linux64";
	ИначеЕсли СисИнфо.ТипПлатформы = ТипПлатформы.Windows_x86 Тогда
		Возврат "Windows32";
	Иначе
		Возврат "Windows64";
	КонецЕсли;
	
КонецФункции

Функция ТекстСообщенийПользователю()

	// Получаем сохраненные ошибки.
	Ошибки = ДлительнаяОтправкаКлиентСервер.ЗначениеПараметраДлительнойОтправки("Ошибки");

	ТекстыСообщений = Новый Массив;

	Для каждого Ошибка Из Ошибки Цикл
		ТекстыСообщений.Добавить(Ошибка.ОписаниеОшибки);
	КонецЦикла;

	// Дополнительно проверим список сообщений пользователю.
	СообщенияПользователю = ДлительныеОперации.СообщенияПользователю(Ложь);
	
	Для каждого СообщениеПользователю Из СообщенияПользователю Цикл
		Если ЗначениеЗаполнено(СообщениеПользователю.Текст) Тогда
			ТекстыСообщений.Добавить(СообщениеПользователю.Текст);
		КонецЕсли;
	КонецЦикла;
	
	Если ТекстыСообщений.Количество() = 0 Тогда
		Возврат "";
	Иначе
		Возврат СтрСоединить(ТекстыСообщений, Символы.ПС);
	КонецЕсли;

КонецФункции

Функция СтрокаСоответствуетШаблону(Стр, СтрШаблон) Экспорт
	
	ДлинаСтроки = СтрДлина(Стр);
	ДлинаШаблона = СтрДлина(СтрШаблон);
	Если ДлинаСтроки <> ДлинаШаблона Тогда
		Возврат Ложь;
	КонецЕсли;
	
	Для Инд =1 По ДлинаШаблона Цикл
		
		ТекСимволШаблона = Сред(СтрШаблон, Инд ,1);
		ТекСимволСтроки = Сред(Стр, Инд, 1);
		
		Если ТекСимволШаблона = "№" Тогда
			Если СтрНайти("1234567890", ТекСимволСтроки) = 0 Тогда
				Возврат Ложь;
			КонецЕсли;
		ИначеЕсли ТекСимволШаблона <> ТекСимволСтроки Тогда
			Возврат Ложь;
		КонецЕсли;
		
	КонецЦикла;
	
	Возврат Истина;
	
КонецФункции

//////////////////////////////////////////////////////////////////////////////////
//// ОБРАБОТЧИКИ СОБЫТИЙ УЧЕТНОЙ ЗАПИСИ НАЛОГОПЛАТЕЛЬЩИКА

Процедура ПередЗаписьюУчетнойЗаписиНалогоплательщика(Объект, Отказ)

	ШапкаОшибки = СтроковыеФункцииКлиентСервер.ПодставитьПараметрыВСтроку(НСтр("ru='Не удалось записать учетную запись налогоплательщика %1:'"),
																			?(ПустаяСтрока(Объект.Наименование), "<наименование не задано>", СокрЛП(Объект.Наименование)));
	
	Если НЕ ЗначениеЗаполнено(Объект.АдресЭлектроннойПочты) Тогда
		Отказ = Истина;
		ТекстСообщения = СтроковыеФункцииКлиентСервер.ПодставитьПараметрыВСтроку(НСтр("ru='%1Не указан адрес электронной почты!'"), ШапкаОшибки + Символы.ПС);
		ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
	КонецЕсли;
	
	Если НЕ ЗначениеЗаполнено(Объект.СерверДокументооборота) Тогда
		Отказ = Истина;
		ТекстСообщения = СтроковыеФункцииКлиентСервер.ПодставитьПараметрыВСтроку(НСтр("ru='%1Не выбран сервер документооборота!'"), ШапкаОшибки + Символы.ПС);
		ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
	КонецЕсли;
	
	// проверка параметров SMTP
	Если НЕ ЗначениеЗаполнено(Объект.СерверSMTP) Тогда
		Отказ = Истина;
		ТекстСообщения = СтроковыеФункцииКлиентСервер.ПодставитьПараметрыВСтроку(НСтр("ru='%1Не задан адрес SMTP-сервера налоговых органов!'"), ШапкаОшибки + Символы.ПС);
		ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
	КонецЕсли;
	
	Если НЕ ЗначениеЗаполнено(Объект.ПортSMTP) Тогда
		Отказ = Истина;
		ТекстСообщения = СтроковыеФункцииКлиентСервер.ПодставитьПараметрыВСтроку(НСтр("ru='%1Не указан порт SMTP для доступа к серверу налоговых органов!'"), ШапкаОшибки + Символы.ПС);
		ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
	КонецЕсли;
	
	Если Объект.ТребуетсяSMTPАутентификация Тогда
		Если НЕ ЗначениеЗаполнено(Объект.ИмяПользователяSMTP) Тогда
			Отказ = Истина;
			ТекстСообщения = СтроковыеФункцииКлиентСервер.ПодставитьПараметрыВСтроку(НСтр("ru='%1Не задано имя пользователя SMTP для доступа к серверу налоговых органов!'"), ШапкаОшибки + Символы.ПС);
			ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
		КонецЕсли;
		Если НЕ ЗначениеЗаполнено(Объект.ПарольSMTP) Тогда
			Отказ = Истина;
			ТекстСообщения = СтроковыеФункцииКлиентСервер.ПодставитьПараметрыВСтроку(НСтр("ru='%1Не задан пароль SMTP для доступа к серверу налоговых органов!'"), ШапкаОшибки + Символы.ПС);
			ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
		КонецЕсли;
	КонецЕсли;
	
	// проверка параметров POP3
	Если НЕ ЗначениеЗаполнено(Объект.СерверPOP3) Тогда
		Отказ = Истина;
		ТекстСообщения = СтроковыеФункцииКлиентСервер.ПодставитьПараметрыВСтроку(НСтр("ru='%1Не указан адрес POP3-сервера налоговых органов!'"), ШапкаОшибки + Символы.ПС);
		ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
	КонецЕсли;
	
	Если НЕ ЗначениеЗаполнено(Объект.ПортPOP3) Тогда
		Отказ = Истина;
		ТекстСообщения = СтроковыеФункцииКлиентСервер.ПодставитьПараметрыВСтроку(НСтр("ru='%1Не указан порт POP3 для доступа к серверу налоговых органов!'"), ШапкаОшибки + Символы.ПС);
		ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
	КонецЕсли;
	
	Если НЕ ЗначениеЗаполнено(Объект.ИмяПользователяPOP3) Тогда
		Отказ = Истина;
		ТекстСообщения = СтроковыеФункцииКлиентСервер.ПодставитьПараметрыВСтроку(НСтр("ru='%1Не задано имя пользователя POP3 для доступа к серверу налоговых органов!'"), ШапкаОшибки + Символы.ПС);
		ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
	КонецЕсли;
	
	Если НЕ ЗначениеЗаполнено(Объект.ПарольPOP3) Тогда
		Отказ = Истина;
		ТекстСообщения = СтроковыеФункцииКлиентСервер.ПодставитьПараметрыВСтроку(НСтр("ru='%1Не задан пароль POP3 для доступа к серверу налоговых органов!'"), ШапкаОшибки + Символы.ПС);
		ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
	КонецЕсли;
	
	Если Объект.ПредназначенаДляДокументооборотаСФНС Тогда
		Если НЕ ЗначениеЗаполнено(Объект.НалоговыйОрган) Тогда
			Отказ = Истина;
			ТекстСообщения = СтроковыеФункцииКлиентСервер.ПодставитьПараметрыВСтроку(НСтр("ru='%1Не указан налоговый орган!'"), ШапкаОшибки + Символы.ПС);
			ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
		КонецЕсли;
	КонецЕсли;
	
	Если НЕ ЗначениеЗаполнено(Объект.СертификатРуководителя) Тогда
		Отказ = Истина;
		ТекстСообщения = СтроковыеФункцииКлиентСервер.ПодставитьПараметрыВСтроку(НСтр("ru='%1Не выбран сертификат руководителя!'"), ШапкаОшибки + Символы.ПС);
		ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
	КонецЕсли;
	
	Если НЕ ЗначениеЗаполнено(Объект.СертификатДляШифрования) Тогда
		Отказ = Истина;
		ТекстСообщения = СтроковыеФункцииКлиентСервер.ПодставитьПараметрыВСтроку(НСтр("ru='%1Не выбран сертификат для целей шифрования!'"), ШапкаОшибки + Символы.ПС);
		ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
	КонецЕсли;
	
	Если НЕ Отказ Тогда
		
		Объект.Наименование = Объект.АдресЭлектроннойПочты;
		
		Если Объект.ОбменНапрямую Тогда
			Объект.РежимАвтонастройки = Перечисления.РежимыАвтонастройкиУчетнойЗаписиНалогоплательщика.Отключена;
			Объект.ИспользоватьСервисОнлайнПроверкиОтчетов = Ложь;
		КонецЕсли;
		
		Если НЕ ЗначениеЗаполнено(Объект.ИдентификаторСпецоператора) И СтрДлина(СокрЛП(Объект.ИдентификаторАбонента)) > 3 Тогда
			Объект.ИдентификаторСпецоператора = Лев(СокрЛП(Объект.ИдентификаторАбонента), 3);
		КонецЕсли;
		
	КонецЕсли;
	
	Объект.ЭтоЗаписьНового = Объект.ЭтоНовый();
	
	Объект.ДополнительныеСвойства.Вставить("ОтключитьАвтообмен", Объект.Ссылка.ОтключитьАвтообмен);
	
КонецПроцедуры

Процедура ПриЗаписиУчетнойЗаписиНалогоплательщика(Объект, Отказ)
	
	// если пишется новый элемент и ему не сопоставлены пользователи,
	// то сопоставим ему текущего пользователя - автора
	Если (Объект.ЭтоЗаписьНового = Неопределено ИЛИ Объект.ЭтоЗаписьНового) И Объект.ЗаписатьПользователяПоУмолчанию Тогда
		ТекущийПользователь = Пользователи.ТекущийПользователь();
		Если ЗначениеЗаполнено(ТекущийПользователь) Тогда
			Запрос = Новый Запрос("ВЫБРАТЬ
			                      |	КОЛИЧЕСТВО(ПользователиУчетныхЗаписейДокументооборота.Пользователь) КАК Пользователь
			                      |ИЗ
			                      |	РегистрСведений.ПользователиУчетныхЗаписейДокументооборота КАК ПользователиУчетныхЗаписейДокументооборота
			                      |ГДЕ
			                      |	ПользователиУчетныхЗаписейДокументооборота.УчетнаяЗапись = &УчетнаяЗапись");
			Запрос.УстановитьПараметр("УчетнаяЗапись", Объект.Ссылка);
			Выборка = Запрос.Выполнить().Выбрать();
			Если НЕ Выборка.Следующий() ИЛИ НЕ ЗначениеЗаполнено(Выборка.Пользователь) Тогда
				МенеджерЗаписи = РегистрыСведений.ПользователиУчетныхЗаписейДокументооборота.СоздатьМенеджерЗаписи();
				МенеджерЗаписи.УчетнаяЗапись = Объект.Ссылка;
				МенеджерЗаписи.Пользователь = ТекущийПользователь;
				МенеджерЗаписи.Записать(Истина);
			КонецЕсли;
		КонецЕсли;
	КонецЕсли;
	
	Если Объект.ДополнительныеСвойства.Свойство("ОтключитьАвтообмен")
		И Объект.ДополнительныеСвойства.ОтключитьАвтообмен <> Объект.ОтключитьАвтообмен Тогда
		ДокументооборотСКО.НастроитьОбменПоУчетнойЗаписи(Объект.Ссылка);
	КонецЕсли;
	
КонецПроцедуры


////////////////////////////////////////////////////////////////////////////////
// ОБРАБОТЧИКИ СОБЫТИЙ ПОЧТОВОГО СЕРВЕРА

Процедура ПередЗаписьюПочтовогоСервера(Объект, Отказ)
	
	Если ПустаяСтрока(Объект.АдресЭлектроннойПочтыФНС) И ПустаяСтрока(Объект.АдресЭлектроннойПочтыПФР) И ПустаяСтрока(Объект.АдресЭлектроннойПочтыФСГС)Тогда
		ТекстСообщения = СтроковыеФункцииКлиентСервер.ПодставитьПараметрыВСтроку(
			НСтр("ru='Не удалось записать элемент справочника ""Серверы документооборота"" %1:%2'"),
			?(ПустаяСтрока(Объект.Наименование), "<наименование не задано>", СокрЛП(Объект.Наименование)),
			Символы.ПС + НСтр("ru = 'Не задан ни один из адресов электронной почты сервера документооборота.'"));
		ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
		Отказ = Истина;
	КонецЕсли;
	
	Если НЕ Отказ Тогда
		Объект.АдресЭлектроннойПочтыФНС = нрег(Объект.АдресЭлектроннойПочтыФНС);
		Объект.АдресЭлектроннойПочтыПФР = нрег(Объект.АдресЭлектроннойПочтыПФР);
		Объект.АдресЭлектроннойПочтыФСГС = нрег(Объект.АдресЭлектроннойПочтыФСГС);
		Если НЕ ЗначениеЗаполнено(Объект.ДлительностьОжиданияСервера) Тогда
			Объект.ДлительностьОжиданияСервера = 60;
		КонецЕсли;
		СгенерироватьНаименованиеПочтовогоСервера(Объект);
	КонецЕсли;
	
КонецПроцедуры

Процедура СгенерироватьНаименованиеПочтовогоСервера(Объект, Обязательно = Ложь) Экспорт
	
	Если ПустаяСтрока(Объект.Наименование) Тогда
		
		СтрАдрес = Объект.АдресЭлектроннойПочтыФНС;
		ВхождениеА = СтрНайти(СтрАдрес, "@");
		Если ВхождениеА = 0 Тогда
			СтрАдрес = Объект.АдресЭлектроннойПочтыПФР;
			ВхождениеА = СтрНайти(СтрАдрес, "@");
		КонецЕсли;
		
		Если ВхождениеА <> 0 Тогда
			ДоменСервера = СокрЛП(Сред(СтрАдрес, ВхождениеА + 1));
			Если НЕ ПустаяСтрока(ДоменСервера) Тогда
				Объект.Наименование = "Сервер документооборота " + ДоменСервера;
				Возврат;
			КонецЕсли;
		КонецЕсли;
		
	КонецЕсли;
	
	Если Обязательно Тогда
		Объект.Наименование = ?(ПустаяСтрока(Объект.АдресЭлектроннойПочтыФНС), СокрЛП(Объект.АдресЭлектроннойПочтыПФР), СокрЛП(Объект.АдресЭлектроннойПочтыФНС));
	КонецЕсли;
	
КонецПроцедуры

Функция ПрочитатьТекстИзФайла(ИмяФайла, ЗаданнаяКодировкаТекста = Неопределено, АвтоопределениеКодировкиXML = Ложь) Экспорт
	
	Если ЭтоАдресВременногоХранилища(ИмяФайла) Тогда
		ИмяФайлаНаДиске = ПолучитьИмяВременногоФайла();
		ПолучитьИзВременногоХранилища(ИмяФайла).Записать(ИмяФайлаНаДиске);
	Иначе
		ИмяФайлаНаДиске = ИмяФайла;
	КонецЕсли;
	
	ИспользоватьКодировку = ЗаданнаяКодировкаТекста;
	
	Если АвтоопределениеКодировкиXML Тогда
		Попытка
			ФайлДляРаспознавания = Новый ДвоичныеДанные(ИмяФайлаНаДиске);
			ИспользоватьКодировку = ОпределитьКодировку(ФайлДляРаспознавания);
		Исключение
			ИнформацияОбОшибке = ПодробноеПредставлениеОшибки(ИнформацияОбОшибке());
			ЗаписьЖурналаРегистрации(
				НСтр("ru = 'Электронный документооборот с контролирующими органами.ПрочитатьТекстИзФайла()'"), 
				УровеньЖурналаРегистрации.Ошибка,,,
				СтрШаблон("%1: %2", "Определение кодировки не удалось", ИнформацияОбОшибке));
		КонецПопытки;
	КонецЕсли;
	
	ОбъектЧтение = НовыйЧтениеТекстаНаСервере(ИмяФайлаНаДиске, ИспользоватьКодировку);
	ТекстИзФайла = ОбъектЧтение.Прочитать();
	ОбъектЧтение.Закрыть();
			
	Возврат ТекстИзФайла;
	
КонецФункции

Функция ИнициализироватьСвойстваПочты(Знач УчетнаяЗапись, ДляПолучения = Истина, ДляОтправки = Истина) Экспорт
	
	ПочтовыйПрофиль = Новый ИнтернетПочтовыйПрофиль;
	ПочтовыйПрофиль.POP3ПередSMTP = Истина;
	
	Если ТипЗнч(УчетнаяЗапись) = Тип("СправочникСсылка.Организации") Тогда
		УчетнаяЗапись = УчетнаяЗаписьОрганизации(УчетнаяЗапись);
	КонецЕсли;
	
	Если ДляПолучения Тогда
		ПочтовыйПрофиль.АутентификацияPOP3 = СпособPOP3Аутентификации.Обычная;
		ПочтовыйПрофиль.АдресСервераPOP3 = УчетнаяЗапись.СерверPOP3;
		ПочтовыйПрофиль.Пароль = УчетнаяЗапись.ПарольPOP3;
		ПочтовыйПрофиль.Пользователь = УчетнаяЗапись.ИмяПользователяPOP3;
		Если УчетнаяЗапись.ПортPOP3 <> 0 Тогда // по умолчанию - 110
			ПочтовыйПрофиль.ПортPOP3 = УчетнаяЗапись.ПортPOP3;
		КонецЕсли;
	КонецЕсли;
	
	Если ДляОтправки Тогда
		ПочтовыйПрофиль.АдресСервераSMTP = УчетнаяЗапись.СерверSMTP;
		Если УчетнаяЗапись.ТребуетсяSMTPАутентификация Тогда
			ПочтовыйПрофиль.АутентификацияSMTP = СпособSMTPАутентификации.Login;
			ПочтовыйПрофиль.ПарольSMTP = УчетнаяЗапись.ПарольSMTP;
			ПочтовыйПрофиль.ПользовательSMTP = УчетнаяЗапись.ИмяПользователяSMTP;
		Иначе
			ПочтовыйПрофиль.АутентификацияSMTP = СпособSMTPАутентификации.БезАутентификации;
		КонецЕсли;
		Если УчетнаяЗапись.ПортSMTP <> 0 Тогда // по умолчанию - 25
			ПочтовыйПрофиль.ПортSMTP = УчетнаяЗапись.ПортSMTP;
		КонецЕсли;
	КонецЕсли;
	
	СерверДокументооборота = УчетнаяЗапись.СерверДокументооборота;
	Если СерверДокументооборота.ДлительностьОжиданияСервера <> 0 Тогда
		ПочтовыйПрофиль.ВремяОжидания = СерверДокументооборота.ДлительностьОжиданияСервера;
	Иначе
		ПочтовыйПрофиль.ВремяОжидания = 60;
	КонецЕсли;
	
	Возврат ПочтовыйПрофиль;
	
КонецФункции

Функция ОтправитьТранспортноеСообщение(Сообщение, ПочтовыйПрофильИлиПочта = Неопределено, ТихийРежим = Ложь, ПараметрыГрупповойОбработки = Неопределено) Экспорт
	
	Попытка
		ЗаблокироватьДанныеДляРедактирования(Сообщение);
	Исключение
		// Транспортное сообщение уже находится в процессе отправки.
		Если Сообщение.Статус = Перечисления.СтатусыПисем.Отправленное Тогда
			Возврат Истина;
		Иначе
			Возврат Ложь;
		КонецЕсли;
		
	КонецПопытки;
	
	// инициализируем флаг ошибки
	ПризнакОшибки = Ложь;
	
	// инициализируем объект-сообщение и ссылку на сообщение, используемые в ниже по тексту
	Если ТипЗнч(Сообщение) = Тип("ДокументСсылка.ТранспортноеСообщение") Тогда
		ОбъектСообщение = Сообщение.ПолучитьОбъект();
	Иначе
		ОбъектСообщение = Сообщение;
	КонецЕсли;
	СообщениеСсылка = ОбъектСообщение.Ссылка;
	
	// Инициализация переменных
	///////////////////////////////////////////////////////////////////////////////
	
	
	// полученные сообщения не отправляются
	Если ОбъектСообщение.Статус = Перечисления.СтатусыПисем.Полученное Тогда
		
		РазблокироватьДанныеДляРедактирования(Сообщение);
		
		ТекстСообщения = НСтр("ru = 'Отправка полученных сообщений запрещена!'");
		ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
		ЗаписатьСобытиеВЖурнал(УровеньЖурналаРегистрации.Предупреждение, ТекстСообщения, СообщениеСсылка);
		Возврат Ложь;
	КонецЕсли;
	
	// если компонента для работы с почтой создается в процедуре, то запускаем автонастройку
	КомпонентаПочтыСоздаетсяВПроцедуре = (ПочтовыйПрофильИлиПочта = Неопределено);
	
	///////////////////////////////////////////////////////////////////////////////
	// Подготавливаем пакет в отправке, при необходимости
	
	// если пакет не сформирован, то пытаемся сформировать
	СообщениеЗашифровано = СообщениеЗашифровано(СообщениеСсылка);
	Если НЕ СообщениеЗашифровано Тогда
		
		РазблокироватьДанныеДляРедактирования(Сообщение);
		
		ТекстСообщения = НСтр("ru = 'Не обнаружен зашифрованный пакет для отправки.'");
		ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
		ЗаписатьСобытиеВЖурнал(УровеньЖурналаРегистрации.Предупреждение, ТекстСообщения, СообщениеСсылка);
		Возврат Ложь;
	КонецЕсли;

	// Подготавливаем пакет в отправке, при необходимости
	///////////////////////////////////////////////////////////////////////////////
	
	// определяем получателя сообщения (to)
	СсылкаПолучатель = ОпределитьПолучателяТранспортногоСообщения(ОбъектСообщение);
	
	///////////////////////////////////////////////////////////////////////////////
	// Разнообразные проверки перед отправкой
	
	// различные проверки
	Если НЕ ЗначениеЗаполнено(ОбъектСообщение.Тип) Тогда
		ТекстСообщения = НСтр("ru = 'Ошибка отправки транспортного сообщения: тип транспортного сообщения не определен!'");
		ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
		ЗаписатьСобытиеВЖурнал(УровеньЖурналаРегистрации.Ошибка, ТекстСообщения, СообщениеСсылка);
		ПризнакОшибки = Истина;
	КонецЕсли;
	
	Если НЕ ЗначениеЗаполнено(ОбъектСообщение.Отправитель) Тогда
		ТекстСообщения = НСтр("ru = 'Ошибка отправки транспортного сообщения: не задан отправитель!'");
		ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
		ЗаписатьСобытиеВЖурнал(УровеньЖурналаРегистрации.Ошибка, ТекстСообщения, СообщениеСсылка);
		ПризнакОшибки = Истина;
	КонецЕсли;
	
	Если НЕ ЗначениеЗаполнено(СсылкаПолучатель) Тогда
		ТекстСообщения = НСтр("ru = 'Ошибка отправки транспортного сообщения: не задан получатель!'");
		ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
		ЗаписатьСобытиеВЖурнал(УровеньЖурналаРегистрации.Ошибка, ТекстСообщения, СообщениеСсылка);
		ПризнакОшибки = Истина;
	КонецЕсли;
	
	Если НЕ ЗначениеЗаполнено(ОбъектСообщение.УчетнаяЗапись) Тогда
		ТекстСообщения = НСтр("ru = 'Ошибка отправки транспортного сообщения: не задана учетная запись!'");
		ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
		ЗаписатьСобытиеВЖурнал(УровеньЖурналаРегистрации.Ошибка, ТекстСообщения, СообщениеСсылка);
		ПризнакОшибки = Истина;
	КонецЕсли;
	
	Если НЕ ЗначениеЗаполнено(ОбъектСообщение.ЦиклОбмена) Тогда
		ТекстСообщения = НСтр("ru = 'Ошибка отправки транспортного сообщения: цикл обмена не задан!'");
		ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
		ЗаписатьСобытиеВЖурнал(УровеньЖурналаРегистрации.Ошибка, ТекстСообщения, СообщениеСсылка);
		ПризнакОшибки = Истина;
	КонецЕсли;
	
	// если ошибка, то прервемся
	Если ПризнакОшибки Тогда
		
		РазблокироватьДанныеДляРедактирования(Сообщение);
		
		Возврат Ложь;
	КонецЕсли;
	
	// Разнообразные проверки перед отправкой
	///////////////////////////////////////////////////////////////////////////////
	
	
	///////////////////////////////////////////////////////////////////////////////
	// Инициализация свойств транспортного сообщения,
	// относящихся к заголовку почтового сообщения
	
	// формируем поля от кого
	Если ПустаяСтрока(ОбъектСообщение.ОтКогоАдрес) Тогда
		ОбъектСообщение.ОтКогоАдрес = ОбъектСообщение.УчетнаяЗапись.АдресЭлектроннойПочты;
	КонецЕсли;
	
	Если ПустаяСтрока(ОбъектСообщение.ОтКогоПредставление) Тогда
		// в представление отправителя записываем полное наименование организации-отправителя
		ОбъектСообщение.ОтКогоПредставление = Строка(ОбъектСообщение.Отправитель.НаименованиеПолное);
	КонецЕсли;
	
	// формируем кодировку
	ОбъектСообщение.Кодировка = "Windows-1251";
	
	// формируем поля кому
	Если ОбъектСообщение.Кому.Количество() = 0 Тогда
		
		АдресИПредставлениеПолучателя = ОпределитьАдресИПредставлениеПолучателяТранспортногоСообщения(ОбъектСообщение, СсылкаПолучатель);
		Если ПустаяСтрока(АдресИПредставлениеПолучателя.Адрес) Тогда
			
			РазблокироватьДанныеДляРедактирования(Сообщение);
			
			ТекстСообщения = СтроковыеФункцииКлиентСервер.ПодставитьПараметрыВСтроку(
				НСтр("ru = 'Не удалось определить адрес электронной почты получателя %1!'"), СсылкаПолучатель);
			ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
			ЗаписатьСобытиеВЖурнал(УровеньЖурналаРегистрации.Ошибка, ТекстСообщения, СообщениеСсылка);
			Возврат Ложь;
		КонецЕсли;
		
		НовыйАдресат = ОбъектСообщение.Кому.Добавить();
		НовыйАдресат.АдресЭлектроннойПочты = АдресИПредставлениеПолучателя.Адрес;
		НовыйАдресат.Представление = АдресИПредставлениеПолучателя.Представление;
		
	КонецЕсли;
	
	
	// проверка наличия вложений
	Вложения = ПолучитьВложенияТранспортногоСообщения(СообщениеСсылка, Истина);

	НеобходимоИнициализироватьXMessageIDИзMessageID = Ложь;
	
	// формируем дополнительные реквизиты заголовка и тему
	Если ОбъектСообщение.Тип = Перечисления.ТипыТранспортныхСообщений.ДекларацияНП Тогда
		
		ОбъектСообщение.Тема = "ФНС:1.0-декларация-ДекларацияНП";
		
		ОбъектСообщение.ДополнительныеРеквизитыЗаголовка.Очистить();
		
		НовыйДопРеквизит = ОбъектСообщение.ДополнительныеРеквизитыЗаголовка.Добавить();
		НовыйДопРеквизит.Тип = "X-Message-ID";
		НовыйДопРеквизит.Значение = СокрЛП(ОбъектСообщение.ЦиклОбмена.Идентификатор);
		
		НовыйДопРеквизит = ОбъектСообщение.ДополнительныеРеквизитыЗаголовка.Добавить();
		НовыйДопРеквизит.Тип = "X-Tax-Code";
		НовыйДопРеквизит.Значение = "01";
		
		НовыйДопРеквизит = ОбъектСообщение.ДополнительныеРеквизитыЗаголовка.Добавить();
		НовыйДопРеквизит.Тип = "X-Tax-Version";
		НовыйДопРеквизит.Значение = "ФНС:1.0";
		
	ИначеЕсли ОбъектСообщение.Тип = Перечисления.ТипыТранспортныхСообщений.ИзвещениеДокументНП Тогда
		
		ОбъектСообщение.Тема = "ФНС:1.0-извещениеОПолучении-ИзвещениеДокументНП";
		
		ОбъектСообщение.ДополнительныеРеквизитыЗаголовка.Очистить();
		
		НовыйДопРеквизит = ОбъектСообщение.ДополнительныеРеквизитыЗаголовка.Добавить();
		НовыйДопРеквизит.Тип = "X-Message-ID";
		НовыйДопРеквизит.Значение = СокрЛП(ОбъектСообщение.ЦиклОбмена.Идентификатор);
		
		НовыйДопРеквизит = ОбъектСообщение.ДополнительныеРеквизитыЗаголовка.Добавить();
		НовыйДопРеквизит.Тип = "X-Tax-Code";
		НовыйДопРеквизит.Значение = "04";
		
		НовыйДопРеквизит = ОбъектСообщение.ДополнительныеРеквизитыЗаголовка.Добавить();
		НовыйДопРеквизит.Тип = "X-Tax-Version";
		НовыйДопРеквизит.Значение = "ФНС:1.0";
		
	ИначеЕсли ОбъектСообщение.Тип = Перечисления.ТипыТранспортныхСообщений.РезультатПриемаДокументНП Тогда
		
		ОбъектСообщение.ДополнительныеРеквизитыЗаголовка.Очистить();
		
		НовыйДопРеквизит = ОбъектСообщение.ДополнительныеРеквизитыЗаголовка.Добавить();
		НовыйДопРеквизит.Тип = "X-Message-ID";
		НовыйДопРеквизит.Значение = СокрЛП(ОбъектСообщение.ЦиклОбмена.Идентификатор);
		
		НовыйДопРеквизит = ОбъектСообщение.ДополнительныеРеквизитыЗаголовка.Добавить();
		НовыйДопРеквизит.Тип = "X-Tax-Code";
		НовыйДопРеквизит.Значение = "05";
		
		НовыйДопРеквизит = ОбъектСообщение.ДополнительныеРеквизитыЗаголовка.Добавить();
		НовыйДопРеквизит.Тип = "X-Tax-Version";
		НовыйДопРеквизит.Значение = "ФНС:1.0";
		
	ИначеЕсли ОбъектСообщение.Тип = Перечисления.ТипыТранспортныхСообщений.ЗаявлениеНП Тогда
		
		ОбъектСообщение.Тема = "ФНС:1.0-заявление-ЗаявлениеНП";
		
		ОбъектСообщение.ДополнительныеРеквизитыЗаголовка.Очистить();
		
		НовыйДопРеквизит = ОбъектСообщение.ДополнительныеРеквизитыЗаголовка.Добавить();
		НовыйДопРеквизит.Тип = "X-Message-ID";
		НовыйДопРеквизит.Значение = СокрЛП(ОбъектСообщение.ЦиклОбмена.Идентификатор);
		
		НовыйДопРеквизит = ОбъектСообщение.ДополнительныеРеквизитыЗаголовка.Добавить();
		НовыйДопРеквизит.Тип = "X-Tax-Code";
		НовыйДопРеквизит.Значение = "01";
		
		НовыйДопРеквизит = ОбъектСообщение.ДополнительныеРеквизитыЗаголовка.Добавить();
		НовыйДопРеквизит.Тип = "X-Tax-Version";
		НовыйДопРеквизит.Значение = "ФНС:1.0";
		
	ИначеЕсли ОбъектСообщение.Тип = Перечисления.ТипыТранспортныхСообщений.ИзвещениеПодтверждениеНП Тогда
		
		ОбъектСообщение.Тема = "ФНС:1.0-извещениеОПолучении-ИзвещениеПодтверждениеНП";
		
		ОбъектСообщение.ДополнительныеРеквизитыЗаголовка.Очистить();
		
		НовыйДопРеквизит = ОбъектСообщение.ДополнительныеРеквизитыЗаголовка.Добавить();
		НовыйДопРеквизит.Тип = "X-Message-ID";
		НовыйДопРеквизит.Значение = СокрЛП(ОбъектСообщение.ЦиклОбмена.Идентификатор);
		
		НовыйДопРеквизит = ОбъектСообщение.ДополнительныеРеквизитыЗаголовка.Добавить();
		НовыйДопРеквизит.Тип = "X-Tax-Code";
		Если ОбъектСообщение.ЦиклОбмена.Тип = Перечисления.ТипыЦикловОбмена.Заявление Тогда
			НовыйДопРеквизит.Значение = "17";
		ИначеЕсли ОбъектСообщение.ЦиклОбмена.Тип = Перечисления.ТипыЦикловОбмена.Представление Тогда
			НовыйДопРеквизит.Значение = "08";		
		Иначе
			НовыйДопРеквизит.Значение = "11";
		КонецЕсли;
		
		НовыйДопРеквизит = ОбъектСообщение.ДополнительныеРеквизитыЗаголовка.Добавить();
		НовыйДопРеквизит.Тип = "X-Tax-Version";
		НовыйДопРеквизит.Значение = "ФНС:1.0";
		
	ИначеЕсли ОбъектСообщение.Тип = Перечисления.ТипыТранспортныхСообщений.ИзвещениеРезультатПриемаНП Тогда
		
		ОбъектСообщение.Тема = "ФНС:1.0-извещениеОПолучении-ИзвещениеРезультатПриемаНП";
		
		ОбъектСообщение.ДополнительныеРеквизитыЗаголовка.Очистить();
		
		НовыйДопРеквизит = ОбъектСообщение.ДополнительныеРеквизитыЗаголовка.Добавить();
		НовыйДопРеквизит.Тип = "X-Message-ID";
		НовыйДопРеквизит.Значение = СокрЛП(ОбъектСообщение.ЦиклОбмена.Идентификатор);
		
		НовыйДопРеквизит = ОбъектСообщение.ДополнительныеРеквизитыЗаголовка.Добавить();
		НовыйДопРеквизит.Тип = "X-Tax-Code";
		НовыйДопРеквизит.Значение = "06";
		
		НовыйДопРеквизит = ОбъектСообщение.ДополнительныеРеквизитыЗаголовка.Добавить();
		НовыйДопРеквизит.Тип = "X-Tax-Version";
		НовыйДопРеквизит.Значение = "ФНС:1.0";
		
	ИначеЕсли ОбъектСообщение.Тип = Перечисления.ТипыТранспортныхСообщений.ИзвещениеРезультатОбработкиНП Тогда

		ОбъектСообщение.Тема = "ФНС:1.0-извещениеОПолучении-ИзвещениеРезультатОбработкиНП";
		
		ОбъектСообщение.ДополнительныеРеквизитыЗаголовка.Очистить();
		
		НовыйДопРеквизит = ОбъектСообщение.ДополнительныеРеквизитыЗаголовка.Добавить();
		НовыйДопРеквизит.Тип = "X-Message-ID";
		НовыйДопРеквизит.Значение = СокрЛП(ОбъектСообщение.ЦиклОбмена.Идентификатор);
		
		НовыйДопРеквизит = ОбъектСообщение.ДополнительныеРеквизитыЗаголовка.Добавить();
		НовыйДопРеквизит.Тип = "X-Tax-Code";
		НовыйДопРеквизит.Значение = "09";
		
		НовыйДопРеквизит = ОбъектСообщение.ДополнительныеРеквизитыЗаголовка.Добавить();
		НовыйДопРеквизит.Тип = "X-Tax-Version";
		НовыйДопРеквизит.Значение = "ФНС:1.0";

	ИначеЕсли ОбъектСообщение.Тип = Перечисления.ТипыТранспортныхСообщений.ИзвещениеРезультатОбработкиРФНП Тогда
		
		ОбъектСообщение.Тема = "ФНС:1.0-извещениеОПолучении-ИзвещениеРезультатОбработкиРФНП";
		
		ОбъектСообщение.ДополнительныеРеквизитыЗаголовка.Очистить();
		
		НовыйДопРеквизит = ОбъектСообщение.ДополнительныеРеквизитыЗаголовка.Добавить();
		НовыйДопРеквизит.Тип = "X-Message-ID";
		НовыйДопРеквизит.Значение = СокрЛП(ОбъектСообщение.ЦиклОбмена.Идентификатор);
		
		НовыйДопРеквизит = ОбъектСообщение.ДополнительныеРеквизитыЗаголовка.Добавить();
		НовыйДопРеквизит.Тип = "X-Tax-Code";
		НовыйДопРеквизит.Значение = "09";
		
		НовыйДопРеквизит = ОбъектСообщение.ДополнительныеРеквизитыЗаголовка.Добавить();
		НовыйДопРеквизит.Тип = "X-Tax-Version";
		НовыйДопРеквизит.Значение = "ФНС:1.0";
		
	ИначеЕсли ОбъектСообщение.Тип = Перечисления.ТипыТранспортныхСообщений.ИзвещениеОбОтзывеЗаявлениеРФНП Тогда
		
		ОбъектСообщение.Тема = "ФНС:1.0-извещениеОПолучении-ИзвещениеОбОтзывеЗаявлениеРФНП";
		
		ОбъектСообщение.ДополнительныеРеквизитыЗаголовка.Очистить();
		
		НовыйДопРеквизит = ОбъектСообщение.ДополнительныеРеквизитыЗаголовка.Добавить();
		НовыйДопРеквизит.Тип = "X-Message-ID";
		НовыйДопРеквизит.Значение = СокрЛП(ОбъектСообщение.ЦиклОбмена.Идентификатор);
		
		НовыйДопРеквизит = ОбъектСообщение.ДополнительныеРеквизитыЗаголовка.Добавить();
		НовыйДопРеквизит.Тип = "X-Tax-Code";
		НовыйДопРеквизит.Значение = "12";
		
		НовыйДопРеквизит = ОбъектСообщение.ДополнительныеРеквизитыЗаголовка.Добавить();
		НовыйДопРеквизит.Тип = "X-Tax-Version";
		НовыйДопРеквизит.Значение = "ФНС:1.0";

	ИначеЕсли ОбъектСообщение.Тип = Перечисления.ТипыТранспортныхСообщений.ИзвещениеРезультатОбработкиТСНП Тогда
		
		ОбъектСообщение.Тема = "ФНС:1.0-извещениеОПолучении-ИзвещениеРезультатОбработкиТСНП";
		
		ОбъектСообщение.ДополнительныеРеквизитыЗаголовка.Очистить();
		
		НовыйДопРеквизит = ОбъектСообщение.ДополнительныеРеквизитыЗаголовка.Добавить();
		НовыйДопРеквизит.Тип = "X-Message-ID";
		НовыйДопРеквизит.Значение = СокрЛП(ОбъектСообщение.ЦиклОбмена.Идентификатор);
		
		НовыйДопРеквизит = ОбъектСообщение.ДополнительныеРеквизитыЗаголовка.Добавить();
		НовыйДопРеквизит.Тип = "X-Tax-Code";
		НовыйДопРеквизит.Значение = "15";
		
		НовыйДопРеквизит = ОбъектСообщение.ДополнительныеРеквизитыЗаголовка.Добавить();
		НовыйДопРеквизит.Тип = "X-Tax-Version";
		НовыйДопРеквизит.Значение = "ФНС:1.0";
		
	ИначеЕсли ОбъектСообщение.Тип = Перечисления.ТипыТранспортныхСообщений.Форма2НДФЛНП Тогда
		
		ОбъектСообщение.Тема = "ФНС:1.0-форма2НДФЛ-Форма2НДФЛНП";
		
		ОбъектСообщение.ДополнительныеРеквизитыЗаголовка.Очистить();
		
		НовыйДопРеквизит = ОбъектСообщение.ДополнительныеРеквизитыЗаголовка.Добавить();
		НовыйДопРеквизит.Тип = "X-Message-ID";
		НовыйДопРеквизит.Значение = СокрЛП(ОбъектСообщение.ЦиклОбмена.Идентификатор);
		
		НовыйДопРеквизит = ОбъектСообщение.ДополнительныеРеквизитыЗаголовка.Добавить();
		НовыйДопРеквизит.Тип = "X-Tax-Code";
		НовыйДопРеквизит.Значение = "01";
		
		НовыйДопРеквизит = ОбъектСообщение.ДополнительныеРеквизитыЗаголовка.Добавить();
		НовыйДопРеквизит.Тип = "X-Tax-Version";
		НовыйДопРеквизит.Значение = "ФНС:1.0";
		
	ИначеЕсли ОбъектСообщение.Тип = Перечисления.ТипыТранспортныхСообщений.ПервичноеСообщениеСодержащееОтчетностьПФР Тогда
		
		// формируем тему
		Если ПустаяСтрока(ОбъектСообщение.Тема) Тогда
			ОбъектСообщение.Тема = "Pension";
		КонецЕсли;
		
		Если ОбъектСообщение.ЦиклОбмена.ФорматДокументооборота = Перечисления.ФорматыДокументооборотаСПФР.ЭДОК Тогда
			ОбъектСообщение.ДополнительныеРеквизитыЗаголовка.Очистить();
			
			НовыйДопРеквизит = ОбъектСообщение.ДополнительныеРеквизитыЗаголовка.Добавить();
			НовыйДопРеквизит.Тип = "X-PFR-Message-ID";
			НовыйДопРеквизит.Значение = СокрЛП(ОбъектСообщение.ЦиклОбмена.Идентификатор);
			
			НовыйДопРеквизит = ОбъектСообщение.ДополнительныеРеквизитыЗаголовка.Добавить();
			НовыйДопРеквизит.Тип = "X-Msg-Type";
			НовыйДопРеквизит.Значение = "EDOK";
		КонецЕсли;
		
	ИначеЕсли ОбъектСообщение.Тип = Перечисления.ТипыТранспортныхСообщений.ПисьмоИсходящееПФР Тогда
		
		// формируем тему
		Если ПустаяСтрока(ОбъектСообщение.Тема) Тогда
			ОбъектСообщение.Тема = "Pension-Unformal";
		КонецЕсли;
		
	ИначеЕсли ОбъектСообщение.Тип = Перечисления.ТипыТранспортныхСообщений.ПервичноеСообщениеСодержащееЗапросПФР Тогда
		
		// формируем тему
		Если ПустаяСтрока(ОбъектСообщение.Тема) Тогда
			ОбъектСообщение.Тема = "Pension-Request";
		КонецЕсли;
		
	ИначеЕсли ОбъектСообщение.Тип = Перечисления.ТипыТранспортныхСообщений.ОтветНаЗапросКвитанцияПФР Тогда
		
		// формируем тему
		Если ПустаяСтрока(ОбъектСообщение.Тема) Тогда
			ОбъектСообщение.Тема = "ReSign: Pension-Reply";
		КонецЕсли;
		
	ИначеЕсли ОбъектСообщение.Тип = Перечисления.ТипыТранспортныхСообщений.ОбращениеНП Тогда
		
		ОбъектСообщение.Тема = "ФНС:1.0-обращение-ОбращениеНП";
		
		ОбъектСообщение.ДополнительныеРеквизитыЗаголовка.Очистить();
		
		НовыйДопРеквизит = ОбъектСообщение.ДополнительныеРеквизитыЗаголовка.Добавить();
		НовыйДопРеквизит.Тип = "X-Message-ID";
		НовыйДопРеквизит.Значение = СокрЛП(ОбъектСообщение.ЦиклОбмена.Идентификатор);
		
		НовыйДопРеквизит = ОбъектСообщение.ДополнительныеРеквизитыЗаголовка.Добавить();
		НовыйДопРеквизит.Тип = "X-Tax-Code";
		НовыйДопРеквизит.Значение = "01";
		
		НовыйДопРеквизит = ОбъектСообщение.ДополнительныеРеквизитыЗаголовка.Добавить();
		НовыйДопРеквизит.Тип = "X-Tax-Version";
		НовыйДопРеквизит.Значение = "ФНС:1.0";
		
	ИначеЕсли ОбъектСообщение.Тип = Перечисления.ТипыТранспортныхСообщений.ПредставлениеНП Тогда
		
		ОбъектСообщение.Тема = "ФНС:1.0-представление-ПредставлениеНП";
		
		ОбъектСообщение.ДополнительныеРеквизитыЗаголовка.Очистить();
		
		НовыйДопРеквизит = ОбъектСообщение.ДополнительныеРеквизитыЗаголовка.Добавить();
		НовыйДопРеквизит.Тип = "X-Message-ID";
		НовыйДопРеквизит.Значение = СокрЛП(ОбъектСообщение.ЦиклОбмена.Идентификатор);
		
		НовыйДопРеквизит = ОбъектСообщение.ДополнительныеРеквизитыЗаголовка.Добавить();
		НовыйДопРеквизит.Тип = "X-Tax-Code";
		НовыйДопРеквизит.Значение = "01";
		
		НовыйДопРеквизит = ОбъектСообщение.ДополнительныеРеквизитыЗаголовка.Добавить();
		НовыйДопРеквизит.Тип = "X-Tax-Version";
		НовыйДопРеквизит.Значение = "ФНС:1.0";
		
	ИначеЕсли ОбъектСообщение.Тип = Перечисления.ТипыТранспортныхСообщений.ИзвещениеПисьмоНП Тогда
		
		ОбъектСообщение.Тема = "ФНС:1.0-извещениеОПолучении-ИзвещениеПисьмоНП";
		
		ОбъектСообщение.ДополнительныеРеквизитыЗаголовка.Очистить();
		
		НовыйДопРеквизит = ОбъектСообщение.ДополнительныеРеквизитыЗаголовка.Добавить();
		НовыйДопРеквизит.Тип = "X-Message-ID";
		НовыйДопРеквизит.Значение = СокрЛП(ОбъектСообщение.ЦиклОбмена.Идентификатор);
		
		НовыйДопРеквизит = ОбъектСообщение.ДополнительныеРеквизитыЗаголовка.Добавить();
		НовыйДопРеквизит.Тип = "X-Tax-Code";
		НовыйДопРеквизит.Значение = "03";
		
		НовыйДопРеквизит = ОбъектСообщение.ДополнительныеРеквизитыЗаголовка.Добавить();
		НовыйДопРеквизит.Тип = "X-Tax-Version";
		НовыйДопРеквизит.Значение = "ФНС:1.0";
		
	ИначеЕсли ОбъектСообщение.Тип = Перечисления.ТипыТранспортныхСообщений.ИзвещениеРассылкаНП Тогда
		
		ОбъектСообщение.Тема = "ФНС:1.0-извещениеОПолучении-ИзвещениеРассылкаНП";
		
		ОбъектСообщение.ДополнительныеРеквизитыЗаголовка.Очистить();
		
		НовыйДопРеквизит = ОбъектСообщение.ДополнительныеРеквизитыЗаголовка.Добавить();
		НовыйДопРеквизит.Тип = "X-Message-ID";
		НовыйДопРеквизит.Значение = СокрЛП(ОбъектСообщение.ЦиклОбмена.Идентификатор);
		
		НовыйДопРеквизит = ОбъектСообщение.ДополнительныеРеквизитыЗаголовка.Добавить();
		НовыйДопРеквизит.Тип = "X-Tax-Code";
		НовыйДопРеквизит.Значение = "03";
		
		НовыйДопРеквизит = ОбъектСообщение.ДополнительныеРеквизитыЗаголовка.Добавить();
		НовыйДопРеквизит.Тип = "X-Tax-Version";
		НовыйДопРеквизит.Значение = "ФНС:1.0";
		
	ИначеЕсли ОбъектСообщение.Тип = Перечисления.ТипыТранспортныхСообщений.ЗапросНП Тогда
		
		ОбъектСообщение.Тема = "ФНС:1.0-запрос-ЗапросНП";
		
		ОбъектСообщение.ДополнительныеРеквизитыЗаголовка.Очистить();
		
		НовыйДопРеквизит = ОбъектСообщение.ДополнительныеРеквизитыЗаголовка.Добавить();
		НовыйДопРеквизит.Тип = "X-Message-ID";
		НовыйДопРеквизит.Значение = СокрЛП(ОбъектСообщение.ЦиклОбмена.Идентификатор);
		
		НовыйДопРеквизит = ОбъектСообщение.ДополнительныеРеквизитыЗаголовка.Добавить();
		НовыйДопРеквизит.Тип = "X-Tax-Code";
		НовыйДопРеквизит.Значение = "01";
		
		НовыйДопРеквизит = ОбъектСообщение.ДополнительныеРеквизитыЗаголовка.Добавить();
		НовыйДопРеквизит.Тип = "X-Tax-Version";
		НовыйДопРеквизит.Значение = "ФНС:1.0";
		
	ИначеЕсли ОбъектСообщение.Тип = Перечисления.ТипыТранспортныхСообщений.ПервичноеСообщениеСодержащееОтчетностьФСГС Тогда
		
		// формируем тему
		Если ПустаяСтрока(ОбъектСообщение.Тема) Тогда
			ОбъектСообщение.Тема = "Stat";
		КонецЕсли;
		ОбъектСообщение.ДополнительныеРеквизитыЗаголовка.Очистить();
		НовыйДопРеквизит = ОбъектСообщение.ДополнительныеРеквизитыЗаголовка.Добавить();
		НовыйДопРеквизит.Тип = "X-Stat-Message-ID";
		НовыйДопРеквизит.Значение = СокрЛП(ОбъектСообщение.ЦиклОбмена.Идентификатор) + "@STAT";
		НовыйДопРеквизит = ОбъектСообщение.ДополнительныеРеквизитыЗаголовка.Добавить();
		НовыйДопРеквизит.Тип = "X-Msg-Type";
		НовыйДопРеквизит.Значение = "STAT";
		НовыйДопРеквизит = ОбъектСообщение.ДополнительныеРеквизитыЗаголовка.Добавить();
		НовыйДопРеквизит.Тип = "X-File-Name";
		НовыйДопРеквизит.Значение = ИзвлечьИмяФайлаОтчетаИзПакета(ОбъектСообщение);
		НовыйДопРеквизит = ОбъектСообщение.ДополнительныеРеквизитыЗаголовка.Добавить();
		НовыйДопРеквизит.Тип = "X-File-Type";
		НовыйДопРеквизит.Значение = "Форма";
		
	ИначеЕсли ОбъектСообщение.Тип = Перечисления.ТипыТранспортныхСообщений.ИзвещениеПодтверждениеДатыОтправкиФСГС Тогда
		
		// формируем тему
		Если ПустаяСтрока(ОбъектСообщение.Тема) Тогда
			ОбъектСообщение.Тема = "ReSign: Stat-Confirmation";
		КонецЕсли;
		ОбъектСообщение.ДополнительныеРеквизитыЗаголовка.Очистить();
		НовыйДопРеквизит = ОбъектСообщение.ДополнительныеРеквизитыЗаголовка.Добавить();
		НовыйДопРеквизит.Тип = "X-Stat-Message-ID";
		НовыйДопРеквизит.Значение = СокрЛП(ОбъектСообщение.ЦиклОбмена.Идентификатор) + "@STAT";
		
	ИначеЕсли ОбъектСообщение.Тип = Перечисления.ТипыТранспортныхСообщений.ИзвещениеПротоколВходногоКонтроляОтчетностиФСГС Тогда
		
		// формируем тему
		Если ПустаяСтрока(ОбъектСообщение.Тема) Тогда
			ОбъектСообщение.Тема = "ReSign: Stat-Protocol";
		КонецЕсли;
		ОбъектСообщение.ДополнительныеРеквизитыЗаголовка.Очистить();
		НовыйДопРеквизит = ОбъектСообщение.ДополнительныеРеквизитыЗаголовка.Добавить();
		НовыйДопРеквизит.Тип = "X-Stat-Message-ID";
		НовыйДопРеквизит.Значение = СокрЛП(ОбъектСообщение.ЦиклОбмена.Идентификатор) + "@STAT";
	ИначеЕсли ОбъектСообщение.Тип = Перечисления.ТипыТранспортныхСообщений.ПисьмоИсходящееФСГС Тогда
		
		// формируем тему
		Если ПустаяСтрока(ОбъектСообщение.Тема) Тогда
			ОбъектСообщение.Тема = "Stat-Unformal";
		КонецЕсли;
		ОбъектСообщение.ДополнительныеРеквизитыЗаголовка.Очистить();
		НовыйДопРеквизит = ОбъектСообщение.ДополнительныеРеквизитыЗаголовка.Добавить();
		НовыйДопРеквизит.Тип = "X-Stat-Message-ID";
		НовыйДопРеквизит.Значение = СокрЛП(ОбъектСообщение.ЦиклОбмена.Идентификатор) + "@STAT";
		
	ИначеЕсли ОбъектСообщение.Тип = Перечисления.ТипыТранспортныхСообщений.ИзвещениеПисьмоВходящееФСГС Тогда
		
		// формируем тему
		Если ПустаяСтрока(ОбъектСообщение.Тема) Тогда
			ОбъектСообщение.Тема = "ReSign: Stat-Unformal";
		КонецЕсли;
		ОбъектСообщение.ДополнительныеРеквизитыЗаголовка.Очистить();
		НовыйДопРеквизит = ОбъектСообщение.ДополнительныеРеквизитыЗаголовка.Добавить();
		НовыйДопРеквизит.Тип = "X-Stat-Message-ID";
		НовыйДопРеквизит.Значение = СокрЛП(ОбъектСообщение.ЦиклОбмена.Идентификатор) + "@STAT";
	ИначеЕсли ОбъектСообщение.ЦиклОбмена.Тип = Перечисления.ТипыЦикловОбмена.ЗапросНаВыпискуИзЕГРЮЛ_ЕГРИП Тогда
		
		ОбъектСообщение.Тема = "СОС:1.0-запрос-ЗапросНП";
		
		ОбъектСообщение.ДополнительныеРеквизитыЗаголовка.Очистить();
		
		НовыйДопРеквизит = ОбъектСообщение.ДополнительныеРеквизитыЗаголовка.Добавить();
		НовыйДопРеквизит.Тип = "X-Message-ID";
		НовыйДопРеквизит.Значение = СокрЛП(ОбъектСообщение.ЦиклОбмена.Идентификатор);
		
		НовыйДопРеквизит = ОбъектСообщение.ДополнительныеРеквизитыЗаголовка.Добавить();
		НовыйДопРеквизит.Тип = "X-Tax-Code";
		НовыйДопРеквизит.Значение = "01";
		
		НовыйДопРеквизит = ОбъектСообщение.ДополнительныеРеквизитыЗаголовка.Добавить();
		НовыйДопРеквизит.Тип = "X-Tax-Version";
		НовыйДопРеквизит.Значение = "СОС:1.0";
	КонецЕсли;
	
	// Инициализация свойств транспортного сообщения,
	// относящихся к заголовку почтового сообщения
	///////////////////////////////////////////////////////////////////////////////
	
	
	///////////////////////////////////////////////////////////////////////////////
	// Формирование почтового сообщения на основе транспортного сообщения
	// Работа с объектом Почта
	
	// получаем зашифрованный транспортный контейнер
	ОписаниеТранспортныхКонтейнеров = ПолучитьТранспортныйКонтейнер(СообщениеСсылка, Истина);
	Если ОписаниеТранспортныхКонтейнеров.Количество() = 0 Тогда
		
		РазблокироватьДанныеДляРедактирования(Сообщение);
		
		ТекстСообщения = НСтр("ru = 'Не удалось отправить транспортное сообщение по причине отсутствия сформированного пакета.'");
		ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
		ЗаписатьСобытиеВЖурнал(УровеньЖурналаРегистрации.Ошибка, ТекстСообщения, СообщениеСсылка);
		Возврат Ложь;
	КонецЕсли;
	ОписаниеТранспортногоКонтейнера = ОписаниеТранспортныхКонтейнеров[0];
	ДанныеТранспортногоКонтейнера = ОписаниеТранспортногоКонтейнера.Данные.Получить();
	Если ДанныеТранспортногоКонтейнера = Неопределено Тогда
		РазблокироватьДанныеДляРедактирования(Сообщение);
		
		ТекстСообщения = НСтр("ru = 'Не удалось отправить транспортное сообщение по причине отсутствия данных сформированного пакета.'");
		ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
		ЗаписатьСобытиеВЖурнал(УровеньЖурналаРегистрации.Ошибка, ТекстСообщения, СообщениеСсылка);
		Возврат Ложь;
	КонецЕсли;

	// работа с почтой
	Если КомпонентаПочтыСоздаетсяВПроцедуре Тогда
		локПочтовыйПрофильИлиПочта = ИнициализироватьСвойстваПочты(ОбъектСообщение.УчетнаяЗапись, , Истина);
	Иначе
		локПочтовыйПрофильИлиПочта = ПочтовыйПрофильИлиПочта;
	КонецЕсли;
	ПочтовоеСообщение = Новый ИнтернетПочтовоеСообщение;

	// инициализация свойств почтового сообщения
	Если ОбъектСообщение.ЦиклОбмена.Вид = Перечисления.ВидыЦикловОбмена.ЦиклОбменаСФНС
		ИЛИ ОбъектСообщение.ЦиклОбмена.Вид = Перечисления.ВидыЦикловОбмена.ЦиклОбменаССОС Тогда
		
		ПочтовоеСообщение.Кодировка = ОбъектСообщение.Кодировка;
		ПочтовоеСообщение.Тема = ОбъектСообщение.Тема;
		
		// инициализируем адреса to
		СтрКому = ОбъектСообщение.Кому.Получить(0);
		АдресTo = ПочтовоеСообщение.Получатели.Добавить(СокрЛП(СтрКому.АдресЭлектроннойПочты));
		АдресTo.ОтображаемоеИмя = СокрЛП(СтрКому.Представление);
		
		// инициализируем адреса reply-to
		АдресReplyTo = ПочтовоеСообщение.ОбратныйАдрес.Добавить(СокрЛП(ОбъектСообщение.ОтКогоАдрес));
		АдресReplyTo.ОтображаемоеИмя = СокрЛП(ОбъектСообщение.ОтКогоПредставление);
		
		// инициализируем адреса from
		ПочтовоеСообщение.Отправитель.Адрес = СокрЛП(ОбъектСообщение.ОтКогоАдрес);
		ПочтовоеСообщение.Отправитель.ОтображаемоеИмя = СокрЛП(ОбъектСообщение.ОтКогоПредставление);
		
		// сохраняем идентификатор сообщения
		ОбъектСообщение.ИдентификаторСообщения = ПочтовоеСообщение.ИдентификаторСообщения;
		
		// дополнительные реквизиты
		Для Каждого ДополнительныйРеквизит Из ОбъектСообщение.ДополнительныеРеквизитыЗаголовка Цикл
			ПочтовоеСообщение.УстановитьПолеЗаголовка(ДополнительныйРеквизит.Тип,
													ДополнительныйРеквизит.Значение,
													?(СтрокаСодержитРусскиеСимволы(ДополнительныйРеквизит.Значение), СпособКодированияНеASCIIСимволовИнтернетПочтовогоСообщения.MIME, СпособКодированияНеASCIIСимволовИнтернетПочтовогоСообщения.БезКодирования));
		КонецЦикла;
		
		// добавляем вложение
		ПочтовоеСообщение.Вложения.Добавить(ДанныеТранспортногоКонтейнера, ОписаниеТранспортногоКонтейнера.ИмяФайла);
		
		// считываем
		ВремФайлВложение = ПолучитьИмяВременногоФайла();
		ДанныеТранспортногоКонтейнера.Записать(ВремФайлВложение);
		// инициализируем длину транспортного контейнера
		ОбъектВремФайл = Новый Файл(ВремФайлВложение);
		РазмерЗашифрованногоВложения = ОбъектВремФайл.Размер();
		ОперацииСФайламиЭДКО.УдалитьВременныйФайл(ВремФайлВложение);

		// проверяем, не превышает ли размер транспортного контейнера 72 МБ
		РазмерКонтейнера = ДанныеТранспортногоКонтейнера.Размер();
		Если РазмерКонтейнера > 72 * 1024 * 1024 Тогда
			
			РазблокироватьДанныеДляРедактирования(Сообщение);
			
			ТекстСообщения = НСтр("ru = 'Невозможна отправка транспортного сообщения, размер которого превышает 72 МБ!'");
			ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
			ЗаписатьСобытиеВЖурнал(УровеньЖурналаРегистрации.Ошибка, ТекстСообщения, СообщениеСсылка);
			Возврат Ложь;
		КонецЕсли;		
		
	ИначеЕсли ОбъектСообщение.ЦиклОбмена.Вид = Перечисления.ВидыЦикловОбмена.ЦиклОбменаСПФР Тогда
		
		ПочтовоеСообщение.Кодировка = ОбъектСообщение.Кодировка;
		ПочтовоеСообщение.Тема = ОбъектСообщение.Тема;
		
		// инициализируем адреса to
		СтрКому = ОбъектСообщение.Кому.Получить(0);
		АдресTo = ПочтовоеСообщение.Получатели.Добавить(СокрЛП(СтрКому.АдресЭлектроннойПочты));
		АдресTo.ОтображаемоеИмя = СокрЛП(СтрКому.Представление);
		
		// инициализируем адреса reply-to
		АдресReplyTo = ПочтовоеСообщение.ОбратныйАдрес.Добавить(СокрЛП(ОбъектСообщение.ОтКогоАдрес));
		АдресReplyTo.ОтображаемоеИмя = СокрЛП(ОбъектСообщение.ОтКогоПредставление);
		
		// инициализируем адреса from
		ПочтовоеСообщение.Отправитель.Адрес = СокрЛП(ОбъектСообщение.ОтКогоАдрес);
		ПочтовоеСообщение.Отправитель.ОтображаемоеИмя = СокрЛП(ОбъектСообщение.ОтКогоПредставление);
		
		// сохраняем идентификатор сообщения
		ОбъектСообщение.ИдентификаторСообщения = ПочтовоеСообщение.ИдентификаторСообщения;
		
		// дополнительные реквизиты
		Если ОбъектСообщение.ЦиклОбмена.ФорматДокументооборота = Перечисления.ФорматыДокументооборотаСПФР.ЭДОК Тогда
			Для каждого ДополнительныйРеквизит Из ОбъектСообщение.ДополнительныеРеквизитыЗаголовка Цикл
				ПочтовоеСообщение.УстановитьПолеЗаголовка(
					ДополнительныйРеквизит.Тип,
					ДополнительныйРеквизит.Значение,
					?(СтрокаСодержитРусскиеСимволы(ДополнительныйРеквизит.Значение),
					СпособКодированияНеASCIIСимволовИнтернетПочтовогоСообщения.MIME,
					СпособКодированияНеASCIIСимволовИнтернетПочтовогоСообщения.БезКодирования));
			КонецЦикла;
		КонецЕсли;
		
		ПочтовоеСообщение.Вложения.Добавить(ДанныеТранспортногоКонтейнера, ОписаниеТранспортногоКонтейнера.ИмяФайла);
		
	ИначеЕсли ОбъектСообщение.ЦиклОбмена.Вид = Перечисления.ВидыЦикловОбмена.ЦиклОбменаСФСГС Тогда
		
		ПочтовоеСообщение.Кодировка = ОбъектСообщение.Кодировка;
		ПочтовоеСообщение.Тема = ОбъектСообщение.Тема;
		
		// инициализируем адреса to
		СтрКому = ОбъектСообщение.Кому.Получить(0);
		АдресTo = ПочтовоеСообщение.Получатели.Добавить(СокрЛП(СтрКому.АдресЭлектроннойПочты));
		АдресTo.ОтображаемоеИмя = СокрЛП(СтрКому.Представление);
		
		// инициализируем адреса reply-to
		АдресReplyTo = ПочтовоеСообщение.ОбратныйАдрес.Добавить(СокрЛП(ОбъектСообщение.ОтКогоАдрес));
		АдресReplyTo.ОтображаемоеИмя = СокрЛП(ОбъектСообщение.ОтКогоПредставление);
		
		// инициализируем адреса from
		ПочтовоеСообщение.Отправитель.Адрес = СокрЛП(ОбъектСообщение.ОтКогоАдрес);
		ПочтовоеСообщение.Отправитель.ОтображаемоеИмя = СокрЛП(ОбъектСообщение.ОтКогоПредставление);
		
		// дополнительные реквизиты
		Для Каждого ДополнительныйРеквизит Из ОбъектСообщение.ДополнительныеРеквизитыЗаголовка Цикл
			Если ДополнительныйРеквизит.Тип = "X-Stat-Message-ID" 
			 ИЛИ ДополнительныйРеквизит.Тип = "X-Msg-Type"
			 ИЛИ ДополнительныйРеквизит.Тип = "X-File-Name"
			 ИЛИ ДополнительныйРеквизит.Тип = "X-File-Type" Тогда
				ПочтовоеСообщение.УстановитьПолеЗаголовка(ДополнительныйРеквизит.Тип,
														ДополнительныйРеквизит.Значение,
														?(СтрокаСодержитРусскиеСимволы(ДополнительныйРеквизит.Значение), СпособКодированияНеASCIIСимволовИнтернетПочтовогоСообщения.MIME, СпособКодированияНеASCIIСимволовИнтернетПочтовогоСообщения.БезКодирования));
				
			КонецЕсли;
		КонецЦикла;

		// сохраняем идентификатор сообщения
		ОбъектСообщение.ИдентификаторСообщения = ПочтовоеСообщение.ИдентификаторСообщения;
		
		ПочтовоеСообщение.Вложения.Добавить(ДанныеТранспортногоКонтейнера, ОписаниеТранспортногоКонтейнера.ИмяФайла);
		
		// считываем
		ВремФайлВложение = ПолучитьИмяВременногоФайла();
		ДанныеТранспортногоКонтейнера.Записать(ВремФайлВложение);
		// инициализируем длину транспортного контейнера
		ОбъектВремФайл = Новый Файл(ВремФайлВложение);
		РазмерЗашифрованногоВложения = ОбъектВремФайл.Размер();
		ОперацииСФайламиЭДКО.УдалитьВременныйФайл(ВремФайлВложение);

		// проверяем, не превышает ли размер транспортного сообщения 512 МБ
		// вычисления весьма приблизительные и только для вложения
		Если Окр(РазмерЗашифрованногоВложения * 4 / 3) > 512 * 1024 * 1024 Тогда
			
			РазблокироватьДанныеДляРедактирования(Сообщение);
			
			ТекстСообщения = НСтр("ru = 'Невозможна отправка транспортного сообщения, размер которого превышает 512 МБ!'");
			ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
			ЗаписатьСобытиеВЖурнал(УровеньЖурналаРегистрации.Ошибка, ТекстСообщения, СообщениеСсылка);
			Возврат Ложь;
		КонецЕсли;
		
	КонецЕсли;
	
	// Формирование почтового сообщения на основе транспортного сообщения
	// Работа с объектом Почта
	///////////////////////////////////////////////////////////////////////////////
	
	///////////////////////////////////////////////////////////////////////////////
	// Отправка сообщения

	Если Сообщение.Статус <> Перечисления.СтатусыПисем.Отправленное Тогда
		
		Если ТипЗнч(локПочтовыйПрофильИлиПочта) = Тип("ИнтернетПочта") Тогда
			РезультатОтправки = Ложь;
			Попытка
				локПочтовыйПрофильИлиПочта.Послать(ПочтовоеСообщение);
				РезультатОтправки = Истина;
			Исключение
				ЗаписатьСобытиеВЖурнал(УровеньЖурналаРегистрации.Ошибка, ПодробноеПредставлениеОшибки(ИнформацияОбОшибке()), СообщениеСсылка);
			КонецПопытки;
		Иначе
			РезультатОтправки = ОтправитьСообщение(локПочтовыйПрофильИлиПочта, ПочтовоеСообщение);
		КонецЕсли;

		Если НЕ РезультатОтправки Тогда
			
			РазблокироватьДанныеДляРедактирования(Сообщение);
			
			Возврат Ложь;
		КонецЕсли;
		
	КонецЕсли;
	
	// Отправка сообщения
	///////////////////////////////////////////////////////////////////////////////
	
	///////////////////////////////////////////////////////////////////////////////
	// Отражение отправки в ИБ
	
	ОбъектСообщение.Статус = Перечисления.СтатусыПисем.Отправленное;
	
	// определяем дату транспорта
	ОбъектСообщение.ДатаТранспорта = ТекущаяДатаСеанса();
	
	// сохраняем сообщение
	Попытка
	
		Если Сообщение.Статус <> Перечисления.СтатусыПисем.Отправленное Тогда
			ОбъектСообщение.Записать();
		КонецЕсли;
		
	Исключение
		
		ИнформацияОбОшибке = ИнформацияОбОшибке();
		
		ТекстСообщения = СтроковыеФункцииКлиентСервер.ПодставитьПараметрыВСтроку(
			НСтр("ru = 'Не удалось записать сообщение 
                  |%1!
                  |%2'"),
			ПредставлениеСообщения(СообщениеСсылка),
			ОбщегоНазначенияЭДКОКлиентСервер.ПолучитьИнформациюОбОшибке(ИнформацияОбОшибке).Описание);
			
		ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
		
		ЗаписьЖурналаРегистрации(
			НСтр("ru = 'Электронный документооборот с контролирующими органами.Транспортное сообщение'"), 
			УровеньЖурналаРегистрации.Ошибка,
			,
			СообщениеСсылка,
			ПодробноеПредставлениеОшибки(ИнформацияОбОшибке));
			
		РазблокироватьДанныеДляРедактирования(Сообщение);
		Возврат Ложь;
		
	КонецПопытки;
	
	// Отражение отправки в ИБ
	///////////////////////////////////////////////////////////////////////////////
	
	РазблокироватьДанныеДляРедактирования(Сообщение);
	Возврат Истина;
	
КонецФункции

Функция ОтправитьСообщение(ПочтовыйПрофиль, ПочтовоеСообщение) Экспорт

	// подключаемся к серверу
	ИнтернетПочта = ПодключитьсяКПочтовомуСерверу(ПочтовыйПрофиль);
	Если ИнтернетПочта = Неопределено Тогда
		Возврат Ложь;
	КонецЕсли;
	
	// отправляем сообщение
	Попытка
		ИнтернетПочта.Послать(ПочтовоеСообщение);
	Исключение
		ИнформацияОбОшибке = ИнформацияОбОшибке();
		ТекстСообщения = СтрШаблон(НСтр("ru = 'Не удалось отправить сообщение:
                                         |%1'"), КраткоеПредставлениеОшибки(ИнформацияОбОшибке));
		ЗаписьЖурналаРегистрации(
			НСтр("ru = 'Электронный документооборот с контролирующими органами.Отправка почты'"),
			УровеньЖурналаРегистрации.Ошибка,
			,, 
			ПодробноеПредставлениеОшибки(ИнформацияОбОшибке));
		ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
		Возврат Ложь;
	КонецПопытки;
	
	// отключаемся от сервера
	СтатусОтключения = ОтключитьсяОтПочтовогоСервера(ИнтернетПочта);
	Если СтатусОтключения = Ложь Тогда
		Возврат Ложь;
	КонецЕсли;
	
	//очищаем почтовые переменные
	ПочтовоеСообщение = Неопределено;	
	ИнтернетПочта = Неопределено;
	
	Возврат Истина;
	
КонецФункции

// Нет поддержки ПФР ЭДОК
Функция ИзвлечьИмяФайлаОтчетаИзПакета(ОбъектСообщение, ПрефиксИмениФайла = "")
	
	// выгружаем контейнер во временный файл
	ИмяФайлаКонтейнера = ПолучитьИмяВременногоФайла();
	Контейнер = ПолучитьТранспортныйКонтейнер(ОбъектСообщение.Ссылка, Истина);
	Если НЕ ЗначениеЗаполнено(Контейнер) ИЛИ Контейнер.Количество() = 0 Тогда
		Возврат Неопределено;
	КонецЕсли;
	ДанныеКонтейнера = Контейнер[0].Данные.Получить();
	Если ДанныеКонтейнера = Неопределено Тогда
		Возврат Неопределено;
	КонецЕсли;
	ДанныеКонтейнера.Записать(ИмяФайлаКонтейнера);
	
	// распаковываем файл описания сведений архива
	ИмяФайлаОписанияСведений = "packageDescription.xml";
	ЧтениеЗИП = Новый ЧтениеZipФайла(ИмяФайлаКонтейнера);
	ЭлементОписаниеСведений = ЧтениеЗИП.Элементы.Найти(ИмяФайлаОписанияСведений);
	Если ЭлементОписаниеСведений = Неопределено Тогда
		Возврат Неопределено;
	КонецЕсли;
	
	КаталогРаспаковки = СоздатьВременныйКаталог();
	ЧтениеЗИП.Извлечь(ЭлементОписаниеСведений, КаталогРаспаковки, РежимВосстановленияПутейФайловZIP.НеВосстанавливать);
	
	// читаем XML
	ТекстXML = ПрочитатьТекстИзФайла(КаталогРаспаковки + ИмяФайлаОписанияСведений, , Истина);
	Если НЕ ЗначениеЗаполнено(ТекстXML) Тогда
		ЧтениеЗИП.Закрыть();
		ОперацииСФайламиЭДКО.УдалитьВременныйФайл(ИмяФайлаКонтейнера);
		ОперацииСФайламиЭДКО.УдалитьВременныйФайл(КаталогРаспаковки);
		Возврат Неопределено;
	КонецЕсли;
	
	// загружаем XML в дерево
	ДеревоXML = ЗагрузитьСтрокуXMLВДеревоЗначений(ТекстXML);
	Если НЕ ЗначениеЗаполнено(ДеревоXML) Тогда
		ЧтениеЗИП.Закрыть();
		ОперацииСФайламиЭДКО.УдалитьВременныйФайл(ИмяФайлаКонтейнера);
		ОперацииСФайламиЭДКО.УдалитьВременныйФайл(КаталогРаспаковки);
		Возврат Неопределено;
	КонецЕсли;
	
	// в случае уведомления об ошибке, источником для получения имени файла отчета должен служить
	// документ описаниеОшибочногоПакета, на который ссылается описание пакета
	Если ОбъектСообщение.Тип = Перечисления.ТипыТранспортныхСообщений.УведомлениеОбОшибкеПФР
	ИЛИ ОбъектСообщение.Тип = Перечисления.ТипыТранспортныхСообщений.УведомлениеОбОшибкеФНС
	ИЛИ ОбъектСообщение.Тип = Перечисления.ТипыТранспортныхСообщений.УведомлениеОбОшибкеФСГС
	ИЛИ ОбъектСообщение.Тип = Перечисления.ТипыТранспортныхСообщений.КритическаяОшибка Тогда
		
		// находим элемент описаниеОшибочногоПакета
		Узлы_ТипДокументаОписаниеОшибочногоПакета = ДеревоXML.Строки.НайтиСтроки(Новый Структура("Имя, Тип, Значение", "типДокумента", "А", "описаниеОшибочногоПакета"), Истина);
		Если Узлы_ТипДокументаОписаниеОшибочногоПакета.Количество() > 0 Тогда
			
			Узел_ОписаниеОшибочногоПакета = Узлы_ТипДокументаОписаниеОшибочногоПакета[0].Родитель;
			
			// ищем в подчинении элемент Содержимое
			УзлыСодержимое = Узел_ОписаниеОшибочногоПакета.Строки.НайтиСтроки(Новый Структура("Имя, Тип", "содержимое", "Э"));
			Если УзлыСодержимое.Количество() > 0 Тогда
				
				// ищем в подчинении атрибут имяФайла
				УзелСодержимое = УзлыСодержимое[0];
				УзлыИмяФайла = УзелСодержимое.Строки.НайтиСтроки(Новый Структура("Имя, Тип", "имяФайла", "А"));
				Если УзлыИмяФайла.Количество() > 0 Тогда
					
					УзелИмяФайла = УзлыИмяФайла[0];
					ИмяФайлаОписанияОшибочногоПакета = СокрЛП(УзелИмяФайла.Значение);
					
					ЭлементОписаниеСведений = ЧтениеЗИП.Элементы.Найти(ИмяФайлаОписанияОшибочногоПакета);
					Если ЭлементОписаниеСведений <> Неопределено Тогда
						
						ЧтениеЗИП.Извлечь(ЭлементОписаниеСведений, КаталогРаспаковки, РежимВосстановленияПутейФайловZIP.НеВосстанавливать);
						
						// распаковываем
						ПолноеИмяФайлаДокумента = РазархивироватьФайл(КаталогРаспаковки + ИмяФайлаОписанияОшибочногоПакета);
						Если ПолноеИмяФайлаДокумента <> Неопределено Тогда
						
							// читаем XML
							ТекстXML = ПрочитатьТекстИзФайла(ПолноеИмяФайлаДокумента, , Истина);
							Если ЗначениеЗаполнено(ТекстXML) Тогда
								
								// загружаем XML в дерево
								ДеревоЗначенийИзСтрокиXML = ЗагрузитьСтрокуXMLВДеревоЗначений(ТекстXML);
								
								// подменяем дерево описания сведений
								Если ДеревоЗначенийИзСтрокиXML <> Неопределено Тогда
									ДеревоXML = ДеревоЗначенийИзСтрокиXML;
								КонецЕсли;
								
							КонецЕсли;
							
							ОперацииСФайламиЭДКО.УдалитьВременныйФайл(ПолноеИмяФайлаДокумента);
							
						КонецЕсли;
						
					КонецЕсли;
					
				КонецЕсли;
				
			КонецЕсли;
			
		КонецЕсли;
		
	КонецЕсли;
	
	// закрываем архив и удаляем временный файл контейнера
	ЧтениеЗИП.Закрыть();
	ОперацииСФайламиЭДКО.УдалитьВременныйФайл(ИмяФайлаКонтейнера);
	ОперацииСФайламиЭДКО.УдалитьВременныйФайл(КаталогРаспаковки);
	
	// ищем атрибут "исходноеИмяФайла"
	Узлы_исходноеИмяФайла = ДеревоXML.Строки.НайтиСтроки(Новый Структура("Имя, Тип", "исходноеИмяФайла", "А"), Истина);
	Если НЕ ЗначениеЗаполнено(ПрефиксИмениФайла) Тогда
		Если Узлы_исходноеИмяФайла.Количество() = 0 Тогда
			Возврат Ложь;
		КонецЕсли;
		
		// возвращаем первое найденное значение атрибута
		Возврат СокрЛП(Узлы_исходноеИмяФайла[0].Значение);
		
	Иначе
		Для каждого Узел_исходноеИмяФайла Из Узлы_исходноеИмяФайла Цикл
			ДлинаПрефиксаИмениФайла = СтрДлина(ПрефиксИмениФайла);
			ПрефиксИмениФайлаИзУзла = Лев(Узел_исходноеИмяФайла.Значение, ДлинаПрефиксаИмениФайла);
			
			Если ВРЕГ(ПрефиксИмениФайлаИзУзла) = ВРЕГ(ПрефиксИмениФайла) Тогда
				Возврат Узел_исходноеИмяФайла.Значение;
			КонецЕсли;
		КонецЦикла;
		
		Возврат Неопределено;
	КонецЕсли;
	
КонецФункции

Функция ПолучитьСообщенияДляОтправки(УчетнаяЗапись)
	
	Запрос = Новый Запрос;
	Запрос.Текст =
	"ВЫБРАТЬ РАЗЛИЧНЫЕ
	|	ДокументТранспортноеСообщение.Ссылка
	|ИЗ
	|	Документ.ТранспортноеСообщение КАК ДокументТранспортноеСообщение
	|		ВНУТРЕННЕЕ СОЕДИНЕНИЕ РегистрСведений.ТранспортныеКонтейнеры КАК ТранспортныеКонтейнеры
	|		ПО (ТранспортныеКонтейнеры.ТранспортноеСообщение = ДокументТранспортноеСообщение.Ссылка)
	|ГДЕ
	|	ДокументТранспортноеСообщение.Статус = ЗНАЧЕНИЕ(Перечисление.СтатусыПисем.Исходящее)
	|	И НЕ ДокументТранспортноеСообщение.ПометкаУдаления
	|	И ДокументТранспортноеСообщение.УчетнаяЗапись = &УчетнаяЗапись
	|	И ДокументТранспортноеСообщение.Тип В(&ТипыАвтоотправляемыхСообщений)
	|	И ДокументТранспортноеСообщение.ЦиклОбмена.ДатаЗакрытия = &ДатаЗакрытия
	|	И НЕ ДокументТранспортноеСообщение.ЦиклОбмена.ПометкаУдаления
	|
	|УПОРЯДОЧИТЬ ПО
	|	ДокументТранспортноеСообщение.ЦиклОбмена.ДатаСоздания,
	|	ДокументТранспортноеСообщение.Тип.Порядок";
	
	Запрос.УстановитьПараметр("УчетнаяЗапись", УчетнаяЗапись);
	Запрос.УстановитьПараметр("ДатаЗакрытия", ПолучитьПустуюДатуЗавершенияЦиклаОбмена());
	Запрос.УстановитьПараметр("ТипыАвтоотправляемыхСообщений",
		ЭлектронныйДокументооборотСКонтролирующимиОрганами.ТипыАвтоотправляемыхСообщений());
	
	Возврат Запрос.Выполнить().Выгрузить().ВыгрузитьКолонку("Ссылка");
	
КонецФункции

// Процедура заполнения регистра сведений "Свойства транспортных сообщений"
//
// Параметры:
//	СообщенияРезультат
//		Тип: Массив
//
Процедура ЗаполнитьСвойстваТранспортныхСообщений(СообщенияРезультат) Экспорт
	
	ДатаПолучения = ТекущаяДатаСеанса();
	
	Запрос = Новый Запрос;
	Запрос.Текст = "ВЫБРАТЬ
	               |	ТранспортноеСообщение.Ссылка КАК Сообщение,
	               |	ВЫБОР
	               |		КОГДА ЕСТЬNULL(ТребуетВнимания.Сообщение, 0) = 0
	               |			ТОГДА ЛОЖЬ
	               |		ИНАЧЕ ИСТИНА
	               |	КОНЕЦ КАК ТребуетВнимания,
	               |	ВЫБОР
	               |		КОГДА ЕСТЬNULL(ТребуетРасшифровки.Сообщение, 0) = 0
	               |			ТОГДА ЛОЖЬ
	               |		ИНАЧЕ ИСТИНА
	               |	КОНЕЦ КАК ТребуетРасшифровки
	               |ИЗ
	               |	Документ.ТранспортноеСообщение КАК ТранспортноеСообщение
	               |		ЛЕВОЕ СОЕДИНЕНИЕ (ВЫБРАТЬ РАЗЛИЧНЫЕ
	               |			ТранспортноеСообщение.Ссылка КАК Сообщение
	               |		ИЗ
	               |			Документ.ТранспортноеСообщение КАК ТранспортноеСообщение
	               |		ГДЕ
	               |			ТранспортноеСообщение.Ссылка В(&Сообщения)
	               |			И (ТранспортноеСообщение.Тип В (&ТипКритическиеОшибки)
	               |					ИЛИ ТранспортноеСообщение.Тип В (&ТипВходящиеДокументы)
	               |					ИЛИ ТранспортноеСообщение.Тип В (&ТипПротоколы)
	               |						И ТранспортноеСообщение.ПротоколСОшибкой)) КАК ТребуетВнимания
	               |		ПО ТранспортноеСообщение.Ссылка = ТребуетВнимания.Сообщение
	               |		ЛЕВОЕ СОЕДИНЕНИЕ (ВЫБРАТЬ РАЗЛИЧНЫЕ
	               |			ТранспортныеКонтейнеры.ТранспортноеСообщение КАК Сообщение
	               |		ИЗ
	               |			РегистрСведений.ТранспортныеКонтейнеры КАК ТранспортныеКонтейнеры
	               |		ГДЕ
	               |			НЕ ТранспортныеКонтейнеры.ТранспортноеСообщение В
	               |						(ВЫБРАТЬ
	               |							СодержимоеТранспортныхКонтейнеров.ТранспортноеСообщение
	               |						ИЗ
	               |							РегистрСведений.СодержимоеТранспортныхКонтейнеров КАК СодержимоеТранспортныхКонтейнеров
	               |						ГДЕ
	               |							СодержимоеТранспортныхКонтейнеров.ТранспортноеСообщение В (&Сообщения)
	               |							И НЕ СодержимоеТранспортныхКонтейнеров.ТранспортноеСообщение.ПометкаУдаления
	               |							И НЕ СодержимоеТранспортныхКонтейнеров.ТранспортноеСообщение.ЦиклОбмена.ПометкаУдаления)) КАК ТребуетРасшифровки
	               |		ПО ТранспортноеСообщение.Ссылка = ТребуетРасшифровки.Сообщение
	               |ГДЕ
	               |	ТранспортноеСообщение.Ссылка В(&Сообщения)";
	
	ТипВходящиеДокументы = Новый Массив;
	ТипВходящиеДокументы.Добавить(Перечисления.ТипыТранспортныхСообщений.ПисьмоВходящееПФР);
	ТипВходящиеДокументы.Добавить(Перечисления.ТипыТранспортныхСообщений.ПисьмоВходящееФСГС);
	Запрос.УстановитьПараметр("ТипВходящиеДокументы", ТипВходящиеДокументы);
	
	ТипПротоколы = Новый Массив;
	ТипПротоколы.Добавить(Перечисления.ТипыТранспортныхСообщений.ПротоколПФР);
	ТипПротоколы.Добавить(Перечисления.ТипыТранспортныхСообщений.ПротоколВходногоКонтроляОтчетностиФСГС);
	ТипПротоколы.Добавить(Перечисления.ТипыТранспортныхСообщений.РезультатПриемаДекларацияНО);
	ТипПротоколы.Добавить(Перечисления.ТипыТранспортныхСообщений.РезультатОбработкиДекларацияНО);
	Запрос.УстановитьПараметр("ТипПротоколы", ТипПротоколы);
	
	ТипКритическиеОшибки = Новый Массив;
	ТипКритическиеОшибки.Добавить(Перечисления.ТипыТранспортныхСообщений.КритическаяОшибка);
	ТипКритическиеОшибки.Добавить(Перечисления.ТипыТранспортныхСообщений.УведомлениеОбОшибкеПФР);
	ТипКритическиеОшибки.Добавить(Перечисления.ТипыТранспортныхСообщений.УведомлениеОбОшибкеФНС);
	ТипКритическиеОшибки.Добавить(Перечисления.ТипыТранспортныхСообщений.УведомлениеОбОшибкеФСГС);
	Запрос.УстановитьПараметр("ТипКритическиеОшибки", ТипКритическиеОшибки);
	
	Запрос.УстановитьПараметр("Сообщения", СообщенияРезультат);
	Запрос.УстановитьПараметр("ТипКритическиеОшибки", ТипКритическиеОшибки);
	Запрос.УстановитьПараметр("ТипВходящиеДокументы", ТипВходящиеДокументы);
	Запрос.УстановитьПараметр("ТипПротоколы", ТипПротоколы);
	
	Выборка = Запрос.Выполнить().Выбрать();
	
	Пока Выборка.Следующий() Цикл
		
		НаборЗаписей = РегистрыСведений.СвойстваТранспортныхСообщений.СоздатьНаборЗаписей(); 
		
		НаборЗаписей.Отбор.УчетнаяЗапись.Установить(Выборка.Сообщение.УчетнаяЗапись);
		НаборЗаписей.Отбор.ТранспортноеСообщение.Установить(Выборка.Сообщение);
		
		НоваяЗапись = НаборЗаписей.Добавить();
		НоваяЗапись.УчетнаяЗапись 			= Выборка.Сообщение.УчетнаяЗапись;
		НоваяЗапись.ТранспортноеСообщение 	= Выборка.Сообщение;
		НоваяЗапись.ДатаПолучения			= ДатаПолучения;
		НоваяЗапись.ТребуетВнимания			= Выборка.ТребуетВнимания;
		НоваяЗапись.ТребуетРасшифровки		= Выборка.ТребуетРасшифровки;
		
		НаборЗаписей.Записать();
		
	КонецЦикла;
	
	
КонецПроцедуры

// не полный аналог ф-ции в Контейнере
Функция ПодключитьсяКПочтовомуСерверу(ПочтовыйПрофиль) Экспорт
	
	Попытка
		Почта = Новый ИнтернетПочта;
		Почта.Подключиться(ПочтовыйПрофиль);
		Возврат Почта;
	Исключение
		ТекстСообщенияОбОшибке =
			СтроковыеФункцииКлиентСервер.ПодставитьПараметрыВСтроку(
			НСтр("ru = 'Не удалось подключиться к почтовому серверу!
				 |%1'"), ИнформацияОбОшибке().Описание);
		ЗаписатьСобытиеВЖурнал(УровеньЖурналаРегистрации.Ошибка, ТекстСообщенияОбОшибке);
		Возврат Неопределено;
	КонецПопытки;
	
КонецФункции

// не полный аналог ф-ции в Контейнере
Функция ОтключитьсяОтПочтовогоСервера(Почта) Экспорт
	
	Попытка
		Почта.Отключиться();
		Возврат Истина;
	Исключение
		ТекстСообщенияОбОшибке =
			СтроковыеФункцииКлиентСервер.ПодставитьПараметрыВСтроку(
			НСтр("ru = 'Не удалось отключиться от сервера документооборота!
				 |%1'"), ИнформацияОбОшибке().Описание);
		ЗаписатьСобытиеВЖурнал(УровеньЖурналаРегистрации.Ошибка, ТекстСообщенияОбОшибке);
		Возврат Ложь;
	КонецПопытки;
	
КонецФункции

Функция СобытиеЖурналаРегистрации()
	
	Возврат НСтр("ru = 'Автоматический обмен с контролирующими органами'");
	
КонецФункции

Процедура ЗаписатьСобытиеВЖурнал(Уровень, ТекстСобытия, Данные = Неопределено)
	
	РежимВключен = (ПолучитьИспользованиеЖурналаРегистрации().Найти(Уровень) <> Неопределено);
	Если НЕ РежимВключен Тогда
		Возврат;
	КонецЕсли;
	
	ЗаписьЖурналаРегистрации(СобытиеЖурналаРегистрации(), Уровень, , Данные, ТекстСобытия);
	
КонецПроцедуры

// есть в Контейнере
Функция ОпределитьОрганФСГСОрганизации(Организация) Экспорт
	
	КодОрганаФСГС = СокрЛП(РегламентированнаяОтчетностьВызовСервера.ПолучитьСведенияОбОрганизации(Организация, , "КодОрганаФСГС").КодОрганаФСГС);
	Если НЕ ЗначениеЗаполнено(КодОрганаФСГС) Тогда
		Возврат Неопределено;
	Иначе
		Возврат Справочники.ОрганыФСГС.НайтиПоКоду(КодОрганаФСГС);
	КонецЕсли;
	
КонецФункции

// есть в Контейнере
Функция СтрокаСодержитРусскиеСимволы(Стр)
	
	// Исключение из стандарта 456.
	РусскиеСимволы = "АБВГДЕЁЖЗИЙКЛМНОПРСТУФХЦЧШЩЪЫЬЭЮЯ";
	Для Инд = 1 По СтрДлина(Стр) Цикл
		Если СтрНайти(РусскиеСимволы, ВРЕГ(Сред(Стр, Инд, 1))) <> 0 Тогда
			Возврат Истина;
		КонецЕсли;
	КонецЦикла;
	Возврат Ложь;
	
КонецФункции

// есть в Контейнере
Функция ОпределитьАдресИПредставлениеПолучателяТранспортногоСообщения(ОбъектСообщение, СсылкаПолучатель)
	
	// если это сообщение, предназначенное для СОС, то отправляем ровно на тот адрес,
	// с которого пришло сообщение-основание
	Если ОбъектСообщение.Тип = Перечисления.ТипыТранспортныхСообщений.ИзвещениеПодтверждениеНП Тогда
		ОтКогоАдрес = ОбъектСообщение.Основание.ОтКогоАдрес;
		ОтКогоПредставление = ОбъектСообщение.Основание.ОтКогоПредставление;
		Если ЗначениеЗаполнено(ОтКогоАдрес) Тогда
			Возврат Новый Структура("Адрес, Представление", ОтКогоАдрес, ОтКогоПредставление);
		КонецЕсли;
	КонецЕсли;
	
	// иначе определяем результат по свойствам получателя
	Если ТипЗнч(СсылкаПолучатель) = Тип("СправочникСсылка.НалоговыеОрганы") Тогда
		АдресПолучателя = СсылкаПолучатель.АдресЭлектроннойПочтыДляЦелейДокументооборотаСНалогоплательщиками;
	Иначе
		Если ОбъектСообщение.ЦиклОбмена.Вид = Перечисления.ВидыЦикловОбмена.ЦиклОбменаСФНС Тогда
			АдресПолучателя = СсылкаПолучатель.АдресЭлектроннойПочтыФНС;
		ИначеЕсли ОбъектСообщение.ЦиклОбмена.Вид = Перечисления.ВидыЦикловОбмена.ЦиклОбменаСФСГС Тогда
			АдресПолучателя = СсылкаПолучатель.АдресЭлектроннойПочтыФСГС;
		Иначе
			АдресПолучателя = СсылкаПолучатель.АдресЭлектроннойПочтыПФР;
		КонецЕсли;
	КонецЕсли;
	Возврат Новый Структура("Адрес, Представление", АдресПолучателя, СокрЛП(СсылкаПолучатель.Наименование));
	
КонецФункции

// есть в Контейнере
Функция ОпределитьПолучателяТранспортногоСообщения(Сообщение)
	
	Если НЕ Сообщение.УчетнаяЗапись.ОбменНапрямую Тогда
		Возврат Сообщение.УчетнаяЗапись.СерверДокументооборота;
	Иначе
		ТипСообщения = Сообщение.Тип;
		Если ТипСообщения = Перечисления.ТипыТранспортныхСообщений.ИзвещениеПодтверждениеНП Тогда
			Возврат Сообщение.УчетнаяЗапись.СерверДокументооборота;
		Иначе
			Возврат Сообщение.УчетнаяЗапись.НалоговыйОрган;
		КонецЕсли;
	КонецЕсли;
	
КонецФункции

// есть в Контейнере
Функция ПолучитьИдентификаторПервичногоСообщения(Объект, ДополнительныйРеквизитИдентификатора = Неопределено)
	
	Если ЗначениеЗаполнено(ДополнительныйРеквизитИдентификатора) Тогда
		СтрИДПервичного = Объект.ДополнительныеРеквизитыЗаголовка.Найти(ДополнительныйРеквизитИдентификатора, "Тип");
		Если СтрИДПервичного <> Неопределено И ЗначениеЗаполнено(СтрИДПервичного.Значение) Тогда
			Возврат СокрЛП(СтрИДПервичного.Значение);
		КонецЕсли;
	КонецЕсли;
	
	СтрИДПервичного = Объект.ДополнительныеРеквизитыЗаголовка.Найти(Перечисления.ТипыДополнительныхРеквизитов.ИдентификаторПервичногоСообщения, "Тип");
	Если СтрИДПервичного = Неопределено Тогда
		СтрИДПервичного = Объект.ДополнительныеРеквизитыЗаголовка.Найти("X-Message-ID", "Тип");
	КонецЕсли;
	
	Если СтрИДПервичного = Неопределено Тогда
		Возврат Неопределено;
	КонецЕсли;
	
	Возврат СокрЛП(СтрИДПервичного.Значение);
	
КонецФункции

// есть в Контейнере
Процедура РаспознатьТранспортноеСообщение(парамОбъект, Записывать = Истина) Экспорт
	
	Если парамОбъект.Статус <> Перечисления.СтатусыПисем.Полученное Тогда
		Возврат;
	КонецЕсли;
	
	Если ТипЗнч(парамОбъект) = Тип("ДокументСсылка.ТранспортноеСообщение") Тогда
		Объект = парамОбъект.ПолучитьОбъект();
	Иначе
		Объект = парамОбъект;
	КонецЕсли;
	
	ШапкаОшибки = "Не удалось записать транспортное сообщение """ + ПредставлениеТранспортногоСообщения(Объект) + """:";
	
	// распознаем тип
	Объект.Тип = ОпределитьТипСообщения(Объект);
	
	// распознаем, с ошибкой ли протокол (если это протокол)
	Объект.ПротоколСОшибкой = ЯвляетсяОтрицательнымПротоколом(Объект);
	
	// распознаем отправителя
	Объект.Отправитель = ОпределитьОтправителяВходящегоТранспортногоСообщения(Объект);
	
	// распознаем получателя
	Объект.Получатель = ОпределитьПолучателяВходящегоТранспортногоСообщения(Объект);
	
	// распознаем цикл обмена
	Если НЕ ЗначениеЗаполнено(Объект.ЦиклОбмена) Тогда
		
		// если это документ от налогового органа, то создаем цикл обмена - основание
		Если Объект.Тип = Перечисления.ТипыТранспортныхСообщений.ПисьмоВходящееПФР Тогда
			
			Если ЗначениеЗаполнено(Объект.Получатель) И ЗначениеЗаполнено(Объект.Отправитель) Тогда// если получатель или отправитель не заполнены - цикл обмена не запишется
			
				ЦиклОбменаОбъект = Справочники.ЦиклыОбмена.СоздатьЭлемент();
				ИнформацияОбИдентификатореДокументооборота = ИзвлечьИдентификаторДокументооборотаИзПакета(Объект.Ссылка, Истина);
				ЦиклОбменаОбъект.Идентификатор = ИнформацияОбИдентификатореДокументооборота.Идентификатор;
				ЦиклОбменаОбъект.Организация = Объект.Получатель;
				ЦиклОбменаОбъект.УчетнаяЗапись = Объект.УчетнаяЗапись;
				ЦиклОбменаОбъект.ВнешняяОрганизация = Объект.Отправитель;
				ЦиклОбменаОбъект.ДатаПоследнегоСообщения = Объект.ДатаТранспорта;
				ЦиклОбменаОбъект.Тип = Перечисления.ТипыЦикловОбмена.НеформализованнаяПерепискаПФРВходящие;
				ЦиклОбменаОбъект.Вид = Перечисления.ВидыЦикловОбмена.ЦиклОбменаСПФР;
				Если ЗначениеЗаполнено(ИнформацияОбИдентификатореДокументооборота.ФорматДокументооборота) Тогда
					ЦиклОбменаОбъект.ФорматДокументооборота = ИнформацияОбИдентификатореДокументооборота.ФорматДокументооборота;
				КонецЕсли;
				ЦиклОбменаОбъект.Записать();
				
				Объект.ЦиклОбмена = ЦиклОбменаОбъект.Ссылка;
				
			КонецЕсли;
			
		// если это письмо от Росстата, то создаем цикл обмена - основание
		ИначеЕсли Объект.Тип = Перечисления.ТипыТранспортныхСообщений.ПисьмоВходящееФСГС Тогда
			
			Если ЗначениеЗаполнено(Объект.Получатель) И ЗначениеЗаполнено(Объект.Отправитель) Тогда// если получатель или отправитель не заполнены - цикл обмена не запишется
			
				ЦиклОбменаОбъект = Справочники.ЦиклыОбмена.СоздатьЭлемент();
				ЦиклОбменаОбъект.Идентификатор = ИзвлечьИдентификаторДокументооборотаИзПакета(Объект.Ссылка).Идентификатор;
				ЦиклОбменаОбъект.Организация = Объект.Получатель;
				ЦиклОбменаОбъект.УчетнаяЗапись = Объект.УчетнаяЗапись;
				ЦиклОбменаОбъект.ВнешняяОрганизация = Объект.Отправитель;
				ЦиклОбменаОбъект.ДатаПоследнегоСообщения = Объект.ДатаТранспорта;
				ЦиклОбменаОбъект.Тип = Перечисления.ТипыЦикловОбмена.ИндивидуальноеИнформированиеФСГС;
				ЦиклОбменаОбъект.Вид = Перечисления.ВидыЦикловОбмена.ЦиклОбменаСФСГС;
				ЦиклОбменаОбъект.Записать();
				
				Объект.ЦиклОбмена = ЦиклОбменаОбъект.Ссылка;
				
			КонецЕсли;
			
		ИначеЕсли Объект.Тип = Перечисления.ТипыТранспортныхСообщений.РассылкаФСГС
			Или Объект.Тип = Перечисления.ТипыТранспортныхСообщений.РассылкаШаблоновФСГС Тогда
			
			Если ЗначениеЗаполнено(Объект.Получатель) И ЗначениеЗаполнено(Объект.Отправитель) Тогда// если получатель или отправитель не заполнены - цикл обмена не запишется
			
				ЦиклОбменаОбъект = Справочники.ЦиклыОбмена.СоздатьЭлемент();
				ЦиклОбменаОбъект.Идентификатор = ИзвлечьИдентификаторДокументооборотаИзПакета(Объект.Ссылка).Идентификатор;
				ЦиклОбменаОбъект.Организация = Объект.Получатель;
				ЦиклОбменаОбъект.УчетнаяЗапись = Объект.УчетнаяЗапись;
				ЦиклОбменаОбъект.ВнешняяОрганизация = Объект.Отправитель;
				ЦиклОбменаОбъект.ДатаПоследнегоСообщения = Объект.ДатаТранспорта;
				
				Если Объект.Тип = Перечисления.ТипыТранспортныхСообщений.РассылкаФСГС Тогда
					ЦиклОбменаОбъект.Тип = Перечисления.ТипыЦикловОбмена.РассылкаФСГС;
				ИначеЕсли Объект.Тип = Перечисления.ТипыТранспортныхСообщений.РассылкаШаблоновФСГС Тогда
					ЦиклОбменаОбъект.Тип = Перечисления.ТипыЦикловОбмена.РассылкаШаблоновФСГС;
				КонецЕсли;				
			
				ЦиклОбменаОбъект.Вид = Перечисления.ВидыЦикловОбмена.ЦиклОбменаСФСГС;
				ЦиклОбменаОбъект.Записать();
				
				Объект.ЦиклОбмена = ЦиклОбменаОбъект.Ссылка;
				
			КонецЕсли;
			
		// если это письмо от ФНС, то создаем цикл обмена - основание
		ИначеЕсли Объект.Тип = Перечисления.ТипыТранспортныхСообщений.ПисьмоНО Тогда
			
			Если ЗначениеЗаполнено(Объект.Получатель) И ЗначениеЗаполнено(Объект.Отправитель) Тогда// если получатель или отправитель не заполнены - цикл обмена не запишется
			
				ЦиклОбменаОбъект = Справочники.ЦиклыОбмена.СоздатьЭлемент();
				ЦиклОбменаОбъект.Идентификатор = ПолучитьИдентификаторПервичногоСообщения(Объект);
				ЦиклОбменаОбъект.Организация = Объект.Получатель;
				ЦиклОбменаОбъект.УчетнаяЗапись = Объект.УчетнаяЗапись;
				ЦиклОбменаОбъект.ВнешняяОрганизация = Объект.Отправитель;
				ЦиклОбменаОбъект.ДатаПоследнегоСообщения = Объект.ДатаТранспорта;
				ЦиклОбменаОбъект.Тип = Перечисления.ТипыЦикловОбмена.ПисьмоНО;
				ЦиклОбменаОбъект.Вид = Перечисления.ВидыЦикловОбмена.ЦиклОбменаСФНС;
				ЦиклОбменаОбъект.ФорматДокументооборота = Перечисления.ФорматыДокументооборотаСФНС.Приказ534;
				ЦиклОбменаОбъект.Записать();
				
				Объект.ЦиклОбмена = ЦиклОбменаОбъект.Ссылка;
				
			КонецЕсли;
			
		// если это рассылка от ФНС, то создаем цикл обмена - основание
		ИначеЕсли Объект.Тип = Перечисления.ТипыТранспортныхСообщений.РассылкаНО Тогда
			
			Если ЗначениеЗаполнено(Объект.Получатель) И ЗначениеЗаполнено(Объект.Отправитель) Тогда// если получатель или отправитель не заполнены - цикл обмена не запишется
			
				ЦиклОбменаОбъект = Справочники.ЦиклыОбмена.СоздатьЭлемент();
				ЦиклОбменаОбъект.Идентификатор = ПолучитьИдентификаторПервичногоСообщения(Объект);
				ЦиклОбменаОбъект.Организация = Объект.Получатель;
				ЦиклОбменаОбъект.УчетнаяЗапись = Объект.УчетнаяЗапись;
				ЦиклОбменаОбъект.ВнешняяОрганизация = Объект.Отправитель;
				ЦиклОбменаОбъект.ДатаПоследнегоСообщения = Объект.ДатаТранспорта;
				ЦиклОбменаОбъект.Тип = ОпределитьТипЦиклаОбменаПоСообщениюРассылки(Объект.Ссылка);
				ЦиклОбменаОбъект.Вид = Перечисления.ВидыЦикловОбмена.ЦиклОбменаСФНС;
				ЦиклОбменаОбъект.ФорматДокументооборота = Перечисления.ФорматыДокументооборотаСФНС.Приказ534;
				ЦиклОбменаОбъект.Записать();
				
				Объект.ЦиклОбмена = ЦиклОбменаОбъект.Ссылка;
				
			КонецЕсли;
			
		// если это документ реализации полномочий от ФНС, то создаем цикл обмена - основание
		ИначеЕсли Объект.Тип = Перечисления.ТипыТранспортныхСообщений.ДокументНО Тогда
			
			// если получатель или отправитель не заполнены - цикл обмена не запишется
			Если ЗначениеЗаполнено(Объект.Получатель) И ЗначениеЗаполнено(Объект.Отправитель) И ТипЗнч(Объект.Отправитель) = Тип("СправочникСсылка.НалоговыеОрганы") Тогда 
				
				ЦиклОбменаОбъект = Справочники.ЦиклыОбмена.СоздатьЭлемент();
				ЦиклОбменаОбъект.Идентификатор = ПолучитьИдентификаторПервичногоСообщения(Объект);
				ЦиклОбменаОбъект.Организация = Объект.Получатель;
				ЦиклОбменаОбъект.УчетнаяЗапись = Объект.УчетнаяЗапись;
				ЦиклОбменаОбъект.ВнешняяОрганизация = Объект.Отправитель;
				ЦиклОбменаОбъект.ДатаПоследнегоСообщения = Объект.ДатаТранспорта;
				ЦиклОбменаОбъект.Тип = Перечисления.ТипыЦикловОбмена.Документ;
				ЦиклОбменаОбъект.Вид = Перечисления.ВидыЦикловОбмена.ЦиклОбменаСФНС;
				ЦиклОбменаОбъект.ФорматДокументооборота = Перечисления.ФорматыДокументооборотаСФНС.Приказ534;
				ЦиклОбменаОбъект.Записать();
				
				Объект.ЦиклОбмена = ЦиклОбменаОбъект.Ссылка;
				
			КонецЕсли;	
			
		// если это прочее сообщение от ПФР, то находим цикл обмена по
		// идентификатору документооборота из описания пакета
		ИначеЕсли Объект.Тип = Перечисления.ТипыТранспортныхСообщений.ПервичноеСообщениеСодержащееОтчетностьПФР
		ИЛИ Объект.Тип = Перечисления.ТипыТранспортныхСообщений.ПодтверждениеПолученияОтчетностиПФР
		ИЛИ Объект.Тип = Перечисления.ТипыТранспортныхСообщений.ПротоколПФР
		ИЛИ Объект.Тип = Перечисления.ТипыТранспортныхСообщений.ПротоколКвитанцияПФР
		ИЛИ Объект.Тип = Перечисления.ТипыТранспортныхСообщений.ПисьмоИсходящееКвитанцияПФР
		ИЛИ Объект.Тип = Перечисления.ТипыТранспортныхСообщений.УведомлениеОбОшибкеПФР
		ИЛИ Объект.Тип = Перечисления.ТипыТранспортныхСообщений.ПодтверждениеПолученияЗапросаПФР
		ИЛИ Объект.Тип = Перечисления.ТипыТранспортныхСообщений.ОтветНаЗапросПФР Тогда
			
			ИнформацияОбИдентификатореДокументооборота = ИзвлечьИдентификаторДокументооборотаИзПакета(Объект,
				Объект.Тип = Перечисления.ТипыТранспортныхСообщений.ПервичноеСообщениеСодержащееОтчетностьПФР);
			ИдентификаторДокументооборота = ?(ИнформацияОбИдентификатореДокументооборота.ФорматДокументооборота =
				Перечисления.ФорматыДокументооборотаСПФР.ЭДОК,
				ПолучитьИдентификаторПервичногоСообщения(Объект, "X-PFR-Message-ID"), Неопределено);
			Если НЕ ЗначениеЗаполнено(ИдентификаторДокументооборота) Тогда
				ИдентификаторДокументооборота = ИнформацияОбИдентификатореДокументооборота.Идентификатор;
			КонецЕсли;
			Если ЗначениеЗаполнено(ИдентификаторДокументооборота) Тогда
				НайденныйЦиклОбмена = НайтиЦиклОбменаПоИдентификатору(ИдентификаторДокументооборота);
				Если НЕ ЗначениеЗаполнено(НайденныйЦиклОбмена) Тогда
					ИдентификаторДокументооборотаНРег = нрег(ИдентификаторДокументооборота);
					НайденныйЦиклОбмена = НайтиЦиклОбменаПоИдентификатору(ИдентификаторДокументооборотаНРег);
				КонецЕсли;
			КонецЕсли;
			
			Если ЗначениеЗаполнено(НайденныйЦиклОбмена) Тогда
				Объект.ЦиклОбмена = НайденныйЦиклОбмена;
			КонецЕсли;
			
		// если это другое сообщение от Росстата, то находим цикл обмена по
		// идентификатору документооборота из описания пакета
		ИначеЕсли Объект.Тип = Перечисления.ТипыТранспортныхСообщений.ПервичноеСообщениеСодержащееОтчетностьФСГС
			  ИЛИ Объект.Тип = Перечисления.ТипыТранспортныхСообщений.ИзвещениеОПолученииОтчетностиФСГС
			  ИЛИ Объект.Тип = Перечисления.ТипыТранспортныхСообщений.ПодтверждениеДатыОтправкиФСГС
			  ИЛИ Объект.Тип = Перечисления.ТипыТранспортныхСообщений.ИзвещениеПодтверждениеДатыОтправкиФСГС
			  ИЛИ Объект.Тип = Перечисления.ТипыТранспортныхСообщений.ПротоколВходногоКонтроляОтчетностиФСГС
			  ИЛИ Объект.Тип = Перечисления.ТипыТранспортныхСообщений.ИзвещениеПротоколВходногоКонтроляОтчетностиФСГС
			  ИЛИ Объект.Тип = Перечисления.ТипыТранспортныхСообщений.ИзвещениеПисьмоИсходящееФСГС
			  ИЛИ Объект.Тип = Перечисления.ТипыТранспортныхСообщений.УведомлениеОбОшибкеФСГС Тогда
			
			ИдентификаторДокументооборота = ИзвлечьИдентификаторДокументооборотаИзПакета(Объект).Идентификатор;
			Если ЗначениеЗаполнено(ИдентификаторДокументооборота) Тогда
				НайденныйЦиклОбмена = НайтиЦиклОбменаПоИдентификатору(ИдентификаторДокументооборота);
			КонецЕсли;
			
			Если ЗначениеЗаполнено(НайденныйЦиклОбмена) Тогда
				Объект.ЦиклОбмена = НайденныйЦиклОбмена;
			КонецЕсли;
			
		// если это прочее сообщение от ФНС, то находим цикл обмена по
		// идентификатору первичного сообщения цикла из дополнительных реквизитов
		// заголовка почтового сообщения
		Иначе
			
			// пробуем найти цикл обмена по идентификатору первичного
			НайденныйЦиклОбмена = Неопределено;
			стрИдентификаторПервичногоСообщения = ПолучитьИдентификаторПервичногоСообщения(Объект);
			
			Если стрИдентификаторПервичногоСообщения <> Неопределено  Тогда
				НайденныйЦиклОбмена = НайтиЦиклОбменаПоИдентификатору(стрИдентификаторПервичногоСообщения);
			КонецЕсли;
			
			// если ничего не помогло...
			Если НЕ ЗначениеЗаполнено(НайденныйЦиклОбмена) Тогда
				
				// обход особенностей различных версий реализации серверного ПО ФНС
				Если Объект.Тип = Перечисления.ТипыТранспортныхСообщений.КритическаяОшибка Тогда
					
					ВхождениеДвоеточия = СтрНайти(Объект.Тема, ":");
					Если ВхождениеДвоеточия <> 0 Тогда
						
						ИсходнаяТема = СокрЛ(Сред(Объект.Тема, ВхождениеДвоеточия + 1));
						
						Запрос = Новый Запрос("ВЫБРАТЬ РАЗРЕШЕННЫЕ ПЕРВЫЕ 1
											  |	ТранспортноеСообщение.ЦиклОбмена
											  |ИЗ
											  |	Документ.ТранспортноеСообщение КАК ТранспортноеСообщение
											  |ГДЕ
											  |	ТранспортноеСообщение.Тема ПОДОБНО &Тема
											  |	И ТранспортноеСообщение.Статус = &Статус
											  |	И ТранспортноеСообщение.УчетнаяЗапись = &УчетнаяЗапись
											  |
											  |УПОРЯДОЧИТЬ ПО
											  |	ТранспортноеСообщение.ДатаТранспорта УБЫВ");
						Запрос.УстановитьПараметр("Тема", ИсходнаяТема);
						Запрос.УстановитьПараметр("Статус", Перечисления.СтатусыПисем.Отправленное);
						Запрос.УстановитьПараметр("УчетнаяЗапись", Объект.УчетнаяЗапись);
						РезультатЗапроса = Запрос.Выполнить();
						Если НЕ РезультатЗапроса.Пустой() Тогда
							Выборка = РезультатЗапроса.Выбрать();
							Если Выборка.Следующий() Тогда
								НайденныйЦиклОбмена = Выборка.ЦиклОбмена;
							КонецЕсли;
						КонецЕсли;
						
						Если НЕ ЗначениеЗаполнено(Объект.ЦиклОбмена) Тогда
							Запрос = Новый Запрос("ВЫБРАТЬ РАЗРЕШЕННЫЕ ПЕРВЫЕ 1
												  |	СодержимоеТранспортныхКонтейнеров.ТранспортноеСообщение.ЦиклОбмена КАК ЦиклОбмена
												  |ИЗ
												  |	РегистрСведений.СодержимоеТранспортныхКонтейнеров КАК СодержимоеТранспортныхКонтейнеров
												  |ГДЕ
												  |	СодержимоеТранспортныхКонтейнеров.ИмяФайла = &ИмяФайла
												  |	И СодержимоеТранспортныхКонтейнеров.ТранспортноеСообщение.Статус = &Статус
												  |	И СодержимоеТранспортныхКонтейнеров.ТранспортноеСообщение.УчетнаяЗапись = &УчетнаяЗапись
												  |
												  |УПОРЯДОЧИТЬ ПО
												  |	СодержимоеТранспортныхКонтейнеров.ТранспортноеСообщение.ДатаТранспорта УБЫВ");
							Запрос.УстановитьПараметр("ИмяФайла", СокрЛП(ИсходнаяТема));
							Запрос.УстановитьПараметр("Статус", Перечисления.СтатусыПисем.Отправленное);
							Запрос.УстановитьПараметр("УчетнаяЗапись", Объект.УчетнаяЗапись);
							РезультатЗапроса = Запрос.Выполнить();
							Если НЕ РезультатЗапроса.Пустой() Тогда
								Выборка = РезультатЗапроса.Выбрать();
								Если Выборка.Следующий() Тогда
									НайденныйЦиклОбмена = Выборка.ЦиклОбмена;
								КонецЕсли;
							КонецЕсли;
						КонецЕсли;
							
					КонецЕсли;
					
				КонецЕсли;
			КонецЕсли;
			
			Если ЗначениеЗаполнено(НайденныйЦиклОбмена) Тогда
				Объект.ЦиклОбмена = НайденныйЦиклОбмена;
			КонецЕсли;
			
		КонецЕсли;
		
	КонецЕсли;
	
	// если распознали цикл обмена, то пробуем еще раз распознать нераспознанных отправителя и получателя
	Если ЗначениеЗаполнено(Объект.ЦиклОбмена) Тогда
		Если НЕ ЗначениеЗаполнено(Объект.Получатель) 
		ИЛИ Объект.Тип = Перечисления.ТипыТранспортныхСообщений.ПодтверждениеПредставлениеНО
		ИЛИ Объект.Тип = Перечисления.ТипыТранспортныхСообщений.ИзвещениеПредставлениеНО
		ИЛИ Объект.Тип = Перечисления.ТипыТранспортныхСообщений.РезультатПриемаПредставлениеНО Тогда
			Объект.Получатель = ОпределитьПолучателяВходящегоТранспортногоСообщения(Объект);
		КонецЕсли;
		Если НЕ ЗначениеЗаполнено(Объект.Отправитель) Тогда
			Объект.Отправитель = ОпределитьОтправителяВходящегоТранспортногоСообщения(Объект);
		КонецЕсли;
	КонецЕсли;
	
	// фиксируем изменения
	Если Записывать И Объект.Модифицированность() Тогда
		Объект.Записать();
	КонецЕсли;
	
КонецПроцедуры

// есть в Контейнере
Функция НайтиЦиклОбменаПоИдентификатору(ИдентификаторДокументооборота)
	
	Результат = Неопределено;
	
	Запрос = Новый Запрос("ВЫБРАТЬ РАЗРЕШЕННЫЕ
						  |	ЦиклыОбмена.Ссылка КАК Ссылка,
						  |	ВЫБОР
						  |		КОГДА ИсходящееТранспортноеСообщение.Ссылка ЕСТЬ NULL
						  |			ТОГДА ВЫБОР
						  |				КОГДА НЕ ОтправленноеТранспортноеСообщение.Ссылка ЕСТЬ NULL
						  |					ТОГДА 3
						  |				ИНАЧЕ 2
						  |			КОНЕЦ
						  |		ИНАЧЕ 1
						  |	КОНЕЦ КАК НаличиеОтправокОтсутствиеОшибок
						  |ИЗ
						  |	Справочник.ЦиклыОбмена КАК ЦиклыОбмена
						  |		ЛЕВОЕ СОЕДИНЕНИЕ (ВЫБРАТЬ
						  |			ТранспортноеСообщение.Ссылка КАК Ссылка,
						  |			ТранспортноеСообщение.ЦиклОбмена КАК ЦиклОбмена
						  |		ИЗ
						  |			Документ.ТранспортноеСообщение КАК ТранспортноеСообщение
						  |		ГДЕ
						  |			ТранспортноеСообщение.ЦиклОбмена.Идентификатор = &ИдентификаторДокументооборота
						  |			И ТранспортноеСообщение.Статус = ЗНАЧЕНИЕ(Перечисление.СтатусыПисем.Исходящее)) КАК ИсходящееТранспортноеСообщение
						  |		ПО ЦиклыОбмена.Ссылка = ИсходящееТранспортноеСообщение.ЦиклОбмена
						  |		ЛЕВОЕ СОЕДИНЕНИЕ (ВЫБРАТЬ
						  |			ТранспортноеСообщение.Ссылка КАК Ссылка,
						  |			ТранспортноеСообщение.ЦиклОбмена КАК ЦиклОбмена
						  |		ИЗ
						  |			Документ.ТранспортноеСообщение КАК ТранспортноеСообщение
						  |		ГДЕ
						  |			ТранспортноеСообщение.ЦиклОбмена.Идентификатор = &ИдентификаторДокументооборота
						  |			И ТранспортноеСообщение.Статус = ЗНАЧЕНИЕ(Перечисление.СтатусыПисем.Отправленное)) КАК ОтправленноеТранспортноеСообщение
						  |		ПО ЦиклыОбмена.Ссылка = ОтправленноеТранспортноеСообщение.ЦиклОбмена
						  |	ГДЕ
						  |		ЦиклыОбмена.Идентификатор = &ИдентификаторДокументооборота
						  |	УПОРЯДОЧИТЬ ПО
						  |		НаличиеОтправокОтсутствиеОшибок УБЫВ,
						  |		ЦиклыОбмена.ДатаСоздания УБЫВ");
	
	Запрос.УстановитьПараметр("ИдентификаторДокументооборота", ИдентификаторДокументооборота);
	
	Выборка = Запрос.Выполнить().Выбрать();
	Если Выборка.Следующий() Тогда
		Результат = Выборка.Ссылка;
	КонецЕсли;
	
	Возврат Результат;
	
КонецФункции

// есть в Контейнере
Функция ОпределитьТипЦиклаОбменаПоСообщениюРассылки(Сообщение)
	
	Контейнеры = ПолучитьТранспортныйКонтейнер(Сообщение);
	Если Контейнеры.Количество() = 0 Тогда
		Возврат Перечисления.ТипыЦикловОбмена.ПустаяСсылка();
	КонецЕсли;
	ИмяКонтейнера = нрег(Контейнеры[0].ИмяФайла);
	
	СуффиксКонтейнераРассылкаГрупповая = "05_01_01.zip";
	Если Прав(ИмяКонтейнера, СтрДлина(СуффиксКонтейнераРассылкаГрупповая)) = СуффиксКонтейнераРассылкаГрупповая Тогда
		Возврат Перечисления.ТипыЦикловОбмена.РассылкаГрупповая;
	Иначе
		Возврат Перечисления.ТипыЦикловОбмена.Рассылка;
	КонецЕсли;
	
КонецФункции

// есть в Контейнере и в Общем модуле РегламентированнаяОтчетность
Функция ИзвлечьИдентификаторДокументооборотаИзПакета(ОбъектСообщение, ЭтоПервичноеСообщение = Неопределено)
	
	Результат = Новый Структура;
	Результат.Вставить("Идентификатор", 			Неопределено);
	Результат.Вставить("ФорматДокументооборота", 	Неопределено);
	
	// выгружаем контейнер во временный файл
	ИмяФайлаКонтейнера = ПолучитьИмяВременногоФайла();
	Контейнер = ПолучитьТранспортныйКонтейнер(ОбъектСообщение.Ссылка, Истина);
	Если НЕ ЗначениеЗаполнено(Контейнер) ИЛИ Контейнер.Количество() = 0 Тогда
		Возврат Результат;
	КонецЕсли;
	ДанныеКонтейнера = Контейнер[0].Данные.Получить();
	Если ДанныеКонтейнера = Неопределено Тогда
		Возврат Результат;
	КонецЕсли;
	ДанныеКонтейнера.Записать(ИмяФайлаКонтейнера);
	
	// распаковываем файл описания сведений архива
	ИмяФайлаОписанияСведений = "packageDescription.xml";
	Попытка
		ЧтениеЗИП = Новый ЧтениеZipФайла(ИмяФайлаКонтейнера);
	Исключение
		ОперацииСФайламиЭДКО.УдалитьВременныйФайл(ИмяФайлаКонтейнера);
		Возврат Результат;
	КонецПопытки;
	ЭлементОписаниеСведений = ЧтениеЗИП.Элементы.Найти(ИмяФайлаОписанияСведений);
	Если ЭлементОписаниеСведений = Неопределено Тогда
		ТипСообщения = Неопределено;
		Если ОбъектСообщение.Тип = Перечисления.ТипыТранспортныхСообщений.ПервичноеСообщениеСодержащееОтчетностьПФР
			ИЛИ ОбъектСообщение.Тип = Перечисления.ТипыТранспортныхСообщений.ПодтверждениеПолученияОтчетностиПФР
			ИЛИ ОбъектСообщение.Тип = Перечисления.ТипыТранспортныхСообщений.ПротоколПФР
			ИЛИ ОбъектСообщение.Тип = Перечисления.ТипыТранспортныхСообщений.ПротоколКвитанцияПФР
			ИЛИ ОбъектСообщение.Тип = Перечисления.ТипыТранспортныхСообщений.ПисьмоВходящееПФР
			ИЛИ ОбъектСообщение.Тип = Перечисления.ТипыТранспортныхСообщений.ПисьмоВходящееКвитанцияПФР
			ИЛИ ОбъектСообщение.Тип = Перечисления.ТипыТранспортныхСообщений.ПисьмоИсходящееПФР
			ИЛИ ОбъектСообщение.Тип = Перечисления.ТипыТранспортныхСообщений.ПисьмоИсходящееКвитанцияПФР
			ИЛИ ОбъектСообщение.Тип = Перечисления.ТипыТранспортныхСообщений.УведомлениеОбОшибкеПФР
			ИЛИ ОбъектСообщение.Тип = Перечисления.ТипыТранспортныхСообщений.ПервичноеСообщениеСодержащееЗапросПФР
			ИЛИ ОбъектСообщение.Тип = Перечисления.ТипыТранспортныхСообщений.ПодтверждениеПолученияЗапросаПФР
			ИЛИ ОбъектСообщение.Тип = Перечисления.ТипыТранспортныхСообщений.ОтветНаЗапросПФР
			ИЛИ ОбъектСообщение.Тип = Перечисления.ТипыТранспортныхСообщений.ОтветНаЗапросКвитанцияПФР Тогда
			
			ТипСообщения = "ПФР";
		КонецЕсли;
		
		Если ТипСообщения = "ПФР" Тогда
			// обход ошибки платформы для архива zip с кодировкой имен файлов cp866, сформированного в Linux
			Попытка
				ЧтениеЗИП.Закрыть();
				МассивОписанийИДанных = ОбщегоНазначенияЭДКО.МассивОписанийИДанныхZipФайла(ИмяФайлаКонтейнера);
				Если МассивОписанийИДанных <> Неопределено Тогда
					ОперацииСФайламиЭДКО.УдалитьВременныйФайл(ИмяФайлаКонтейнера);
					ИмяФайлаКонтейнера = ИмяФайлаКонтейнера + ".zip";
					ОбщегоНазначенияЭДКО.СформироватьZip(МассивОписанийИДанных, ИмяФайлаКонтейнера);
				КонецЕсли;
				ЧтениеЗИП = Новый ЧтениеZipФайла(ИмяФайлаКонтейнера);
			Исключение
				ОперацииСФайламиЭДКО.УдалитьВременныйФайл(ИмяФайлаКонтейнера);
				Возврат Результат;
			КонецПопытки;
			
			Для каждого ЭлементКонтейнера Из ЧтениеЗИП.Элементы Цикл
				ИмяФайлаВРег = СокрЛП(ЭлементКонтейнера.ИмяБезРасширения);
				ИмяФайлаВРег = ВРег(ИмяФайлаВРег);
				РасширениеФайла = СокрЛП(ЭлементКонтейнера.Расширение);
				
				Если Лев(ИмяФайлаВРег, 4) = "ПФР_" И СтрНайти(ИмяФайлаВРег, "_ОСП_") > 0 И нрег(РасширениеФайла) = "xml" Тогда
					ИмяФайлаОписанияСведений = ЭлементКонтейнера.Имя;
					ЭлементОписаниеСведений = ЭлементКонтейнера;
					Результат.ФорматДокументооборота = Перечисления.ФорматыДокументооборотаСПФР.ЭДОК;
					Прервать;
				КонецЕсли;
			КонецЦикла;
		КонецЕсли;
		
		Если ЭлементОписаниеСведений = Неопределено Тогда
			Возврат Результат;
		КонецЕсли;
	КонецЕсли;
	
	КаталогРаспаковки = ОперацииСФайламиЭДКО.СоздатьВременныйКаталог();
	ЧтениеЗИП.Извлечь(ЭлементОписаниеСведений, КаталогРаспаковки, РежимВосстановленияПутейФайловZIP.НеВосстанавливать);
	
	// читаем XML
	ТекстXML = ПрочитатьТекстИзФайла(КаталогРаспаковки + ИмяФайлаОписанияСведений, , Истина);
	Если НЕ ЗначениеЗаполнено(ТекстXML) Тогда
		ЧтениеЗИП.Закрыть();
		ОперацииСФайламиЭДКО.УдалитьВременныйФайл(ИмяФайлаКонтейнера);
		ОперацииСФайламиЭДКО.УдалитьВременныйФайл(КаталогРаспаковки);
		Возврат Результат;
	КонецЕсли;
	
	// загружаем XML в дерево
	ДеревоXML = ЗагрузитьСтрокуXMLВДеревоЗначений(ТекстXML);
	Если НЕ ЗначениеЗаполнено(ДеревоXML) Тогда
		ЧтениеЗИП.Закрыть();
		ОперацииСФайламиЭДКО.УдалитьВременныйФайл(ИмяФайлаКонтейнера);
		ОперацииСФайламиЭДКО.УдалитьВременныйФайл(КаталогРаспаковки);
		Возврат Результат;
	КонецЕсли;
	
	// в случае уведомления об ошибке, источником для получения идентификатора пакета должен служить
	// документ описаниеОшибочногоПакета, на который ссылается описание пакета
	Если ОбъектСообщение.Тип = Перечисления.ТипыТранспортныхСообщений.УведомлениеОбОшибкеПФР
		И Результат.ФорматДокументооборота <> Перечисления.ФорматыДокументооборотаСПФР.ЭДОК
		ИЛИ ОбъектСообщение.Тип = Перечисления.ТипыТранспортныхСообщений.УведомлениеОбОшибкеФНС
		ИЛИ ОбъектСообщение.Тип = Перечисления.ТипыТранспортныхСообщений.УведомлениеОбОшибкеФСГС
		ИЛИ ОбъектСообщение.Тип = Перечисления.ТипыТранспортныхСообщений.КритическаяОшибка Тогда
		
		// находим элемент описаниеОшибочногоПакета
		Узлы_ТипДокументаОписаниеОшибочногоПакета = ДеревоXML.Строки.НайтиСтроки(Новый Структура("Имя, Тип, Значение", "типДокумента", "А", "описаниеОшибочногоПакета"), Истина);
		Если Узлы_ТипДокументаОписаниеОшибочногоПакета.Количество() > 0 Тогда
			
			Узел_ОписаниеОшибочногоПакета = Узлы_ТипДокументаОписаниеОшибочногоПакета[0].Родитель;
			
			// ищем в подчинении элемент Содержимое
			УзлыСодержимое = Узел_ОписаниеОшибочногоПакета.Строки.НайтиСтроки(Новый Структура("Имя, Тип", "содержимое", "Э"));
			Если УзлыСодержимое.Количество() > 0 Тогда
				
				// ищем в подчинении атрибут имяФайла
				УзелСодержимое = УзлыСодержимое[0];
				УзлыИмяФайла = УзелСодержимое.Строки.НайтиСтроки(Новый Структура("Имя, Тип", "имяФайла", "А"));
				Если УзлыИмяФайла.Количество() > 0 Тогда
					
					УзелИмяФайла = УзлыИмяФайла[0];
					ИмяФайлаОписанияОшибочногоПакета = СокрЛП(УзелИмяФайла.Значение);
					
					ЭлементОписаниеСведений = ЧтениеЗИП.Элементы.Найти(ИмяФайлаОписанияОшибочногоПакета);
					Если ЭлементОписаниеСведений <> Неопределено Тогда
						
						ЧтениеЗИП.Извлечь(ЭлементОписаниеСведений, КаталогРаспаковки, РежимВосстановленияПутейФайловZIP.НеВосстанавливать);
						
						// распаковываем
						ПолноеИмяФайлаДокумента = РазархивироватьФайл(КаталогРаспаковки + ИмяФайлаОписанияОшибочногоПакета);
						Если ПолноеИмяФайлаДокумента <> Неопределено Тогда
						
							// читаем XML
							ТекстXML = ПрочитатьТекстИзФайла(ПолноеИмяФайлаДокумента, , Истина);
							Если ЗначениеЗаполнено(ТекстXML) Тогда
								
								// загружаем XML в дерево
								ДеревоЗначенийИзСтрокиXML = ЗагрузитьСтрокуXMLВДеревоЗначений(ТекстXML);
								
								// подменяем дерево описания сведений
								Если ДеревоЗначенийИзСтрокиXML <> Неопределено Тогда
									ДеревоXML = ДеревоЗначенийИзСтрокиXML;
								КонецЕсли;
								
							КонецЕсли;
							
							ОперацииСФайламиЭДКО.УдалитьВременныйФайл(ПолноеИмяФайлаДокумента);
							
						КонецЕсли;
						
					КонецЕсли;
					
				КонецЕсли;
				
			КонецЕсли;
			
		КонецЕсли;
		
	КонецЕсли;
	
	// закрываем архив и удаляем временный файл контейнера
	ЧтениеЗИП.Закрыть();
	ОперацииСФайламиЭДКО.УдалитьВременныйФайл(ИмяФайлаКонтейнера);
	ОперацииСФайламиЭДКО.УдалитьВременныйФайл(КаталогРаспаковки);
	
	Если Результат.ФорматДокументооборота = Перечисления.ФорматыДокументооборотаСПФР.ЭДОК Тогда
		УзелСлужебнаяИнформация = НайтиСтрокиДереваЗначений(ДеревоXML,,, "СлужебнаяИнформация", "Э");
		УзелИдентификатора = Неопределено;
		Если ЗначениеЗаполнено(УзелСлужебнаяИнформация) Тогда
			УзелИдентификатора = НайтиСтрокиДереваЗначений(УзелСлужебнаяИнформация,,,
				?(ЭтоПервичноеСообщение = Истина, "GUID", "ВОтветНа"), "Э");
		КонецЕсли;
		
		Если НЕ ЗначениеЗаполнено(УзелИдентификатора) Тогда
			Возврат Результат;
		КонецЕсли;
		
		Результат.Идентификатор = СокрЛП(УзелИдентификатора.Значение);
		
	Иначе
		// ищем атрибут "идентификаторДокументооборота"
		Узлы_идентификаторДокументооборота = ДеревоXML.Строки.НайтиСтроки(
			Новый Структура("Имя, Тип", "идентификаторДокументооборота", "А"), Истина);
		Если Узлы_идентификаторДокументооборота.Количество() = 0 Тогда
			Возврат Результат;
		КонецЕсли;
		
		// возвращаем первое найденное значение атрибута
		Результат.Идентификатор = СокрЛП(Узлы_идентификаторДокументооборота[0].Значение);
	КонецЕсли;
	
	Возврат Результат;
	
КонецФункции

// есть в Контейнере
Функция ОпределитьПолучателяВходящегоТранспортногоСообщения(Объект, ИгнорироватьЦиклОбмена = Ложь) Экспорт
	
	Если НЕ ИгнорироватьЦиклОбмена И ЗначениеЗаполнено(Объект.ЦиклОбмена) И Объект.Статус = Перечисления.СтатусыПисем.Полученное Тогда
		Возврат Объект.ЦиклОбмена.Организация;
	КонецЕсли;
	
	Если Объект.Тип = Перечисления.ТипыТранспортныхСообщений.ПисьмоВходящееПФР
	ИЛИ Объект.Тип = Перечисления.ТипыТранспортныхСообщений.ПисьмоИсходящееКвитанцияПФР
	ИЛИ Объект.Тип = Перечисления.ТипыТранспортныхСообщений.ПодтверждениеПолученияОтчетностиПФР
	ИЛИ Объект.Тип = Перечисления.ТипыТранспортныхСообщений.ПротоколПФР 
	ИЛИ Объект.Тип = Перечисления.ТипыТранспортныхСообщений.ОтветНаЗапросПФР
	ИЛИ Объект.Тип = Перечисления.ТипыТранспортныхСообщений.ПодтверждениеПолученияЗапросаПФР
	ИЛИ Объект.Тип = Перечисления.ТипыТранспортныхСообщений.УведомлениеОбОшибкеПФР Тогда
		
		Результат = ОпределитьПолучателяВходящегоСообщенияПФР(Объект);
		Если Результат <> Неопределено И Результат <> Справочники.Организации.ПустаяСсылка() Тогда
			Возврат Результат;
		КонецЕсли;
		
	ИначеЕсли Объект.Тип = Перечисления.ТипыТранспортныхСообщений.ИзвещениеОПолученииОтчетностиФСГС
	ИЛИ Объект.Тип = Перечисления.ТипыТранспортныхСообщений.ПодтверждениеДатыОтправкиФСГС
	ИЛИ Объект.Тип = Перечисления.ТипыТранспортныхСообщений.ПротоколВходногоКонтроляОтчетностиФСГС
	ИЛИ Объект.Тип = Перечисления.ТипыТранспортныхСообщений.ПисьмоВходящееФСГС
	ИЛИ Объект.Тип = Перечисления.ТипыТранспортныхСообщений.ИзвещениеПисьмоИсходящееФСГС
	ИЛИ Объект.Тип = Перечисления.ТипыТранспортныхСообщений.УведомлениеОбОшибкеФСГС Тогда
		
		Возврат ОпределитьПолучателяВходящегоСообщенияФСГС(Объект);
		
	КонецЕсли;
	
	// если нет возможности определить получателя по циклу обмена, то определим его по учетной записи
	// если с учетной записью сообщения связана головная организация, то считаем, что получатель - она
	Запрос = Новый Запрос("ВЫБРАТЬ РАЗРЕШЕННЫЕ
	                      |	Организации.Ссылка
	                      |ИЗ
	                      |	Справочник.Организации КАК Организации
	                      |ГДЕ
	                      |	Организации.УчетнаяЗаписьОбмена = &УчетнаяЗаписьОбмена
	                      |	И Организации.ВидОбменаСКонтролирующимиОрганами = &ВидОбменаСКонтролирующимиОрганами
	                      |	И Организации.ГоловнаяОрганизация = &ПустаяГоловнаяОрганизация
	                      |
	                      |ОБЪЕДИНИТЬ ВСЕ
	                      |
	                      |ВЫБРАТЬ
	                      |	Организации.Ссылка
	                      |ИЗ
	                      |	Справочник.Организации КАК Организации
	                      |ГДЕ
	                      |	Организации.УчетнаяЗаписьОбмена = &УчетнаяЗаписьОбмена
	                      |	И Организации.ВидОбменаСКонтролирующимиОрганами = &ВидОбменаСКонтролирующимиОрганами
	                      |	И Организации.ГоловнаяОрганизация <> &ПустаяГоловнаяОрганизация");
	Запрос.УстановитьПараметр("УчетнаяЗаписьОбмена", Объект.УчетнаяЗапись);
	Запрос.УстановитьПараметр("ВидОбменаСКонтролирующимиОрганами", Перечисления.ВидыОбменаСКонтролирующимиОрганами.ОбменВУниверсальномФормате);
	Запрос.УстановитьПараметр("ПустаяГоловнаяОрганизация", Справочники.Организации.ПустаяСсылка());
	Выборка = Запрос.Выполнить().Выбрать();
	МассивОрганизаций = Новый Массив;
	Пока Выборка.Следующий() Цикл
		Если ЗначениеЗаполнено(Выборка.Ссылка) Тогда
			МассивОрганизаций.Добавить(Выборка.Ссылка);
		КонецЕсли;
	КонецЦикла;
	
	Если МассивОрганизаций.Количество() = 1 Тогда
		Возврат МассивОрганизаций[0];
		
	ИначеЕсли МассивОрганизаций.Количество() = 0 Тогда
		Возврат Неопределено;
		
	Иначе
		ИННКППОтчета = Неопределено;
		Если Объект.Тип = Перечисления.ТипыТранспортныхСообщений.ДокументНО Тогда
			ИнформацияОФайлеОтчета = ПолучитьВложенияТранспортногоСообщения(
				Объект,
				Истина,
				Перечисления.ТипыСодержимогоТранспортногоКонтейнера.Документ);
			
			Если ИнформацияОФайлеОтчета.Количество() > 0 Тогда
				ДвоичныеДанныеОтчета = ИнформацияОФайлеОтчета[0].Данные.Получить();
				ИННКППОтчета = ИННКПППолучателяТребования(ИнформацияОФайлеОтчета[0].ИмяФайла, ДвоичныеДанныеОтчета);
				
			Иначе
				ИмяФайлаОтчета = ИзвлечьИмяФайлаОтчетаИзПакета(Объект, "ON_DOCNPNO_");
				Если НЕ ЗначениеЗаполнено(ИмяФайлаОтчета) Тогда
					ИмяФайлаОтчета = ИзвлечьИмяФайлаОтчетаИзПакета(Объект, "ON_INFSOOB_");
				КонецЕсли;
				
				Если ЗначениеЗаполнено(ИмяФайлаОтчета) Тогда
					ИННКППОтчета = ИННКПППолучателяТребования(ИмяФайлаОтчета);
				КонецЕсли;
			КонецЕсли;
		КонецЕсли;
		
		Если ЗначениеЗаполнено(ИННКППОтчета) Тогда
			Для каждого ОрганизацияСсылка Из МассивОрганизаций Цикл
				Если НЕ РегламентированнаяОтчетностьВызовСервера.ЭтоЮридическоеЛицо(ОрганизацияСсылка) Тогда
					СведенияОбОрганизации = РегламентированнаяОтчетностьВызовСервера.ПолучитьСведенияОбОрганизации(ОрганизацияСсылка, , "ИННФЛ");
					ИННКППОрганизации = СокрЛП(СведенияОбОрганизации.ИННФЛ);
				Иначе
					СведенияОбОрганизации = РегламентированнаяОтчетностьВызовСервера.ПолучитьСведенияОбОрганизации(ОрганизацияСсылка, , "ИННЮЛ, КППЮЛ");
					ИННКППОрганизации = СокрЛП(СведенияОбОрганизации.ИННЮЛ) + СокрЛП(СведенияОбОрганизации.КППЮЛ);
				КонецЕсли;
				
				Если ИННКППОрганизации = ИННКППОтчета Тогда
					Возврат ОрганизацияСсылка;
				КонецЕсли;
			КонецЦикла;
		КонецЕсли;
		
		ОрганизацияПоУмолчанию = Неопределено;
		Если РегламентированнаяОтчетностьВызовСервера.ИспользуетсяОднаОрганизация() Тогда
			Модуль = ОбщегоНазначения.ОбщийМодуль("Справочники.Организации");
			ОрганизацияПоУмолчанию = Модуль.ОрганизацияПоУмолчанию();
		КонецЕсли;
		Если НЕ ЗначениеЗаполнено(ОрганизацияПоУмолчанию) Тогда
			ОрганизацияПоУмолчанию = РегламентированнаяОтчетность.ПолучитьОрганизациюПоУмолчанию();
		КонецЕсли;
		
		Если ЗначениеЗаполнено(ОрганизацияПоУмолчанию) Тогда
			Для каждого ОрганизацияСсылка Из МассивОрганизаций Цикл
				Если ОрганизацияСсылка = ОрганизацияПоУмолчанию Тогда
					Возврат ОрганизацияСсылка;
				КонецЕсли;
			КонецЦикла;
		КонецЕсли;
		
		Возврат МассивОрганизаций[0];
	КонецЕсли;
	
КонецФункции

// есть в Контейнере
Функция ОпределитьПолучателяВходящегоСообщенияПФР(ОбъектСообщение)
	
	// сначала пробуем определить получателя из пакета
	ПолучательИзПакета = ОпределитьПолучателяВходящегоСообщенияПоОписаниюПакетаПФР(ОбъектСообщение);
	Если ЗначениеЗаполнено(ПолучательИзПакета) Тогда
		Возврат ПолучательИзПакета;
	КонецЕсли;
	
	// если по пакету получателя определить не удалось,
	// то пробуем получить из свойств цикла обмена
	ЦиклОбмена = ОбъектСообщение.ЦиклОбмена;
	Если ЗначениеЗаполнено(ЦиклОбмена) Тогда
		ОрганизацияЦиклаОбмена = ЦиклОбмена.Организация;
		Если ЗначениеЗаполнено(ОрганизацияЦиклаОбмена) Тогда
			Возврат ОрганизацияЦиклаОбмена;
		КонецЕсли;
	КонецЕсли;
	
	// если не удалось определить получателя ни по пакету,
	// ни по родительскому циклу обмена, то возвращаем текущее значение
	Возврат ОбъектСообщение.Получатель;
	
КонецФункции

// есть в Контейнере
Функция ОпределитьПолучателяВходящегоСообщенияПоОписаниюПакетаПФР(ОбъектСообщение) Экспорт
	
	// получаем дерево описания пакета
	ДеревоОписанияПакета = Неопределено;
	Если НЕ ОбъектСообщение.ДополнительныеСвойства.Свойство("ДеревоОписанияПакета", ДеревоОписанияПакета) Тогда
		ДеревоОписанияПакета = ЗагрузитьДеревоОписанияПакетаДляСообщения(ОбъектСообщение, "ПФР");
	КонецЕсли;
	
	// проверяем дерево на заполненность
	Если НЕ ЗначениеЗаполнено(ДеревоОписанияПакета) Тогда
		Возврат Справочники.Организации.ПустаяСсылка();
	КонецЕсли;
		
	// ищем узел "получатель"
	УзлыПолучатель = ДеревоОписанияПакета.Строки.НайтиСтроки(Новый Структура("Имя, Тип", "получатель", "Э"), Истина);
	Если УзлыПолучатель.Количество() = 0 Тогда
		Возврат Справочники.Организации.ПустаяСсылка();
	КонецЕсли;
	УзелПолучатель = УзлыПолучатель[0];
	
	// ищем атрибут "идентификаторСубъекта"
	УзелИдентификаторСубъекта = УзелПолучатель.Строки.Найти("идентификаторСубъекта", "Имя");
	Если УзелИдентификаторСубъекта = Неопределено Тогда
		Возврат Справочники.Организации.ПустаяСсылка();
	КонецЕсли;
	РегистрационныйНомерПФР = СокрЛП(УзелИдентификаторСубъекта.Значение);
	
	// ищем по справочнику
	Возврат ОрганизацияПоРегНомеруИУчетнойЗаписи(РегистрационныйНомерПФР, ОбъектСообщение.УчетнаяЗапись);
	
КонецФункции

Функция ОрганизацияПоРегНомеруИУчетнойЗаписи(РегистрационныйНомерПФР, УчетнаяЗапись) Экспорт
	
	// ищем по справочнику
	Запрос = Новый Запрос("ВЫБРАТЬ РАЗРЕШЕННЫЕ
	                      |	Организации.Ссылка КАК Организация
	                      |ИЗ
	                      |	Справочник.Организации КАК Организации
	                      |ГДЕ
	                      |	Организации.УчетнаяЗаписьОбмена = &УчетнаяЗаписьОбмена
	                      |	И Организации.ВидОбменаСКонтролирующимиОрганами = &ВидОбменаСКонтролирующимиОрганами");
						  
	Запрос.УстановитьПараметр("УчетнаяЗаписьОбмена", УчетнаяЗапись);
	Запрос.УстановитьПараметр("ВидОбменаСКонтролирующимиОрганами", Перечисления.ВидыОбменаСКонтролирующимиОрганами.ОбменВУниверсальномФормате);
	РезультатЗапроса = Запрос.Выполнить();
	
	Если РезультатЗапроса.Пустой() Тогда
		Возврат Справочники.Организации.ПустаяСсылка();
	Иначе
		
		Выборка = РезультатЗапроса.Выбрать();
		Пока Выборка.Следующий() Цикл
			
			Организация = Выборка.Организация;
			
			СведенияОбОрганизации = РегламентированнаяОтчетностьВызовСервера.ПолучитьСведенияОбОрганизации(
				Организация, , "РегНомПФР, РегНомПФРЗаСебя,");
				
			Если РегистрационныйНомерПФР = СведенияОбОрганизации.РегНомПФР
				ИЛИ РегистрационныйНомерПФР = СведенияОбОрганизации.РегНомПФРЗаСебя Тогда
				
				Возврат Организация;
				
			КонецЕсли;
			
		КонецЦикла;
		
	КонецЕсли;
	
	Возврат Справочники.Организации.ПустаяСсылка();
	
КонецФункции

// есть в Контейнере
Функция ОпределитьПолучателяВходящегоСообщенияФСГС(ОбъектСообщение)
	
	// сначала пробуем определить получателя из пакета
	ПолучательИзПакета = ОпределитьПолучателяВходящегоСообщенияПоОписаниюПакетаФСГС(ОбъектСообщение);
	Если ЗначениеЗаполнено(ПолучательИзПакета) Тогда
		Возврат ПолучательИзПакета;
	КонецЕсли;
	
	// если по пакету получателя определить не удалось,
	// то пробуем получить из свойств цикла обмена
	ЦиклОбмена = ОбъектСообщение.ЦиклОбмена;
	Если ЗначениеЗаполнено(ЦиклОбмена) Тогда
		ОрганизацияЦиклаОбмена = ЦиклОбмена.Организация;
		Если ЗначениеЗаполнено(ОрганизацияЦиклаОбмена) Тогда
			Возврат ОрганизацияЦиклаОбмена;
		КонецЕсли;
	КонецЕсли;
	
	// если не удалось определить получателя ни по пакету,
	// ни по родительскому циклу обмена, то возвращаем текущее значение
	Возврат ОбъектСообщение.Получатель;
	
КонецФункции

// есть в Контейнере
Функция ОпределитьПолучателяВходящегоСообщенияПоОписаниюПакетаФСГС(ОбъектСообщение)
	
	// получаем дерево описания пакета
	ДеревоОписанияПакета = Неопределено;
	Если НЕ ОбъектСообщение.ДополнительныеСвойства.Свойство("ДеревоОписанияПакета", ДеревоОписанияПакета) Тогда
		ДеревоОписанияПакета = ЗагрузитьДеревоОписанияПакетаДляСообщения(ОбъектСообщение, "ФСГС");
	КонецЕсли;
	
	// проверяем дерево на заполненность
	Если НЕ ЗначениеЗаполнено(ДеревоОписанияПакета) Тогда
		Возврат Справочники.Организации.ПустаяСсылка();
	КонецЕсли;
	
	// ищем узел "получатель"
	УзлыПолучатель = ДеревоОписанияПакета.Строки.НайтиСтроки(Новый Структура("Имя, Тип", "получатель", "Э"), Истина);
	Если УзлыПолучатель.Количество() = 0 Тогда
		Возврат Справочники.Организации.ПустаяСсылка();
	КонецЕсли;
	УзелПолучатель = УзлыПолучатель[0];
	
	// ищем атрибут "идентификаторСубъекта"
	УзелИдентификаторСубъекта = УзелПолучатель.Строки.Найти("идентификаторСубъекта", "Имя");
	Если УзелИдентификаторСубъекта = Неопределено Тогда
		Возврат Справочники.Организации.ПустаяСсылка();
	КонецЕсли;
	ИдентификаторПолучателя = СокрЛП(УзелИдентификаторСубъекта.Значение);
	КодПоОКПО = Сред(ИдентификаторПолучателя, СтрНайти(ИдентификаторПолучателя, ".") + 1);
	
	ИмяРеквКодПоОКПО = "КодПоОКПО";
	Если Метаданные.Справочники.Организации.Реквизиты.Найти("КодОКПО") <> Неопределено Тогда
		ИмяРеквКодПоОКПО = "КодОКПО";
	КонецЕсли;
	
	// ищем по справочнику
	Запрос = Новый Запрос("ВЫБРАТЬ РАЗРЕШЕННЫЕ ПЕРВЫЕ 1
	                      |	Организации.Ссылка
	                      |ИЗ
	                      |	Справочник.Организации КАК Организации
	                      |ГДЕ
	                      |	Организации." + ИмяРеквКодПоОКПО + " = &КодПоОКПО
	                      |	И Организации.УчетнаяЗаписьОбмена = &УчетнаяЗаписьОбмена
	                      |	И Организации.ВидОбменаСКонтролирующимиОрганами = &ВидОбменаСКонтролирующимиОрганами");
	Запрос.УстановитьПараметр("КодПоОКПО", КодПоОКПО);
	Запрос.УстановитьПараметр("УчетнаяЗаписьОбмена", ОбъектСообщение.УчетнаяЗапись);
	Запрос.УстановитьПараметр("ВидОбменаСКонтролирующимиОрганами", Перечисления.ВидыОбменаСКонтролирующимиОрганами.ОбменВУниверсальномФормате);
	РезультатЗапроса = Запрос.Выполнить();
	Если РезультатЗапроса.Пустой() Тогда
		Возврат Справочники.Организации.ПустаяСсылка();
	Иначе
		Выборка = РезультатЗапроса.Выбрать();
		Выборка.Следующий();
		Возврат Выборка.Получить(0);
	КонецЕсли;
	
КонецФункции

// есть в Контейнере
Функция ЗагрузитьДеревоОписанияПакетаДляСообщения(ОбъектСообщение, ТипСообщения = Неопределено)
	
	СообщениеСсылка = ОбъектСообщение.Ссылка;
	
	ТранспортныеКонтейнеры = ПолучитьТранспортныйКонтейнер(СообщениеСсылка, Истина);
	Если ТранспортныеКонтейнеры.Количество() = 0 Тогда
		Возврат Неопределено;
	КонецЕсли;
	ТранспортныйКонтейнер = ТранспортныеКонтейнеры[0];
	
	// выгружаем пакет во временный файл
	КаталогРаспаковки = ОперацииСФайламиЭДКО.СоздатьВременныйКаталог();
	ИмяВременногоФайла = КаталогРаспаковки + ТранспортныйКонтейнер.ИмяФайла;
	Попытка
		ТранспортныйКонтейнер.Данные.Получить().Записать(ИмяВременногоФайла);
	Исключение
		ТекстСообщения = СтроковыеФункцииКлиентСервер.ПодставитьПараметрыВСтроку(
			НСтр("ru = 'Ошибка выгрузки транспортного пакета во временный файл для анализа:
						|%1'"), ИнформацияОбОшибке().Описание);
		ЗаписатьСобытиеВЖурнал(УровеньЖурналаРегистрации.Ошибка, ТекстСообщения);
		Возврат Неопределено;
	КонецПопытки;
	
	// распаковываем файл описания сведений архива
	Попытка
		ЧтениеЗИП = Новый ЧтениеZipФайла(ИмяВременногоФайла);
	Исключение
		ОперацииСФайламиЭДКО.УдалитьВременныйФайл(КаталогРаспаковки);
		Возврат Неопределено;
	КонецПопытки;
	
	ИмяФайлаОписанияСведений = "packageDescription.xml";
	ЭлементОписаниеСведений = ЧтениеЗИП.Элементы.Найти(ИмяФайлаОписанияСведений);
	Если ЭлементОписаниеСведений = Неопределено Тогда
		Если ТипСообщения = "ПФР" ИЛИ ТипСообщения = Неопределено Тогда
			// обход ошибки платформы для архива zip с кодировкой имен файлов cp866, сформированного в Linux
			Попытка
				ЧтениеЗИП.Закрыть();
				МассивОписанийИДанных = ОбщегоНазначенияЭДКО.МассивОписанийИДанныхZipФайла(ИмяВременногоФайла);
				Если МассивОписанийИДанных <> Неопределено Тогда
					ИмяВременногоФайла = ИмяВременногоФайла + ".zip";
					ОбщегоНазначенияЭДКО.СформироватьZip(МассивОписанийИДанных, ИмяВременногоФайла);
				КонецЕсли;
				ЧтениеЗИП = Новый ЧтениеZipФайла(ИмяВременногоФайла);
			Исключение
				ОперацииСФайламиЭДКО.УдалитьВременныйФайл(КаталогРаспаковки);
				Возврат Неопределено;
			КонецПопытки;
			
			Для каждого ЭлементКонтейнера Из ЧтениеЗИП.Элементы Цикл
				ИмяФайлаВРег = СокрЛП(ЭлементКонтейнера.ИмяБезРасширения);
				ИмяФайлаВРег = ВРег(ИмяФайлаВРег);
				РасширениеФайла = СокрЛП(ЭлементКонтейнера.Расширение);
				
				Если Лев(ИмяФайлаВРег, 4) = "ПФР_" И СтрНайти(ИмяФайлаВРег, "_ОСП_") > 0 И нрег(РасширениеФайла) = "xml" Тогда
					ИмяФайлаОписанияСведений = ЭлементКонтейнера.Имя;
					ЭлементОписаниеСведений = ЭлементКонтейнера;
					Прервать;
				КонецЕсли;
			КонецЦикла;
		КонецЕсли;
		
		Если ЭлементОписаниеСведений = Неопределено Тогда
			ОперацииСФайламиЭДКО.УдалитьВременныйФайл(КаталогРаспаковки);
			Возврат Неопределено;
		КонецЕсли;
	КонецЕсли;
	
	ЧтениеЗИП.Извлечь(ЭлементОписаниеСведений, КаталогРаспаковки, РежимВосстановленияПутейФайловZIP.НеВосстанавливать);
	ЧтениеЗИП.Закрыть();
	
	// читаем XML
	ТекстXML = ПрочитатьТекстИзФайла(КаталогРаспаковки + ИмяФайлаОписанияСведений, , Истина);
	ОперацииСФайламиЭДКО.УдалитьВременныйФайл(КаталогРаспаковки);
	Если НЕ ЗначениеЗаполнено(ТекстXML) Тогда
		Возврат Неопределено;
	КонецЕсли;
	
	// загружаем XML в дерево
	ДеревоXML = ЗагрузитьСтрокуXMLВДеревоЗначений(ТекстXML);
	Если НЕ ЗначениеЗаполнено(ДеревоXML) Тогда
		Возврат Неопределено;
	КонецЕсли;
	
	// сохраняем дерево в свойствах объекта
	ОбъектСообщение.ДополнительныеСвойства.Вставить("ДеревоОписанияПакета", ДеревоXML);
	
	Возврат ДеревоXML;
	
КонецФункции

// есть в Контейнере
Функция ОпределитьОтправителяВходящегоТранспортногоСообщенияФНС(ОбъектСообщение)
	
	// сначала пробуем определить отправителя из пакета
	ОтправительИзДокумент = ОпределитьОтправителяВходящегоТранспортногоСообщенияПоСвойствамПакетаФНС(ОбъектСообщение);
	Если ТипЗнч(ОтправительИзДокумент) = Тип("СправочникСсылка.НалоговыеОрганы") И ЗначениеЗаполнено(ОтправительИзДокумент) Тогда
		Возврат ОтправительИзДокумент;
	КонецЕсли;
	
	// если не удалось, то пробуем получить из свойств учетной записи
	Если НЕ ОбъектСообщение.УчетнаяЗапись.ОбменНапрямую Тогда
		Возврат ОбъектСообщение.УчетнаяЗапись.СерверДокументооборота;
	Иначе
		Возврат ОбъектСообщение.УчетнаяЗапись.НалоговыйОрган;	
	КонецЕсли;
	
КонецФункции

// есть в Контейнере
Функция ОпределитьОтправителяВходящегоТранспортногоСообщенияПоСвойствамПакетаФНС(ОбъектСообщение)
	
	// получаем дерево описания пакета
	ДеревоОписанияПакета = Неопределено;
	Если НЕ ОбъектСообщение.ДополнительныеСвойства.Свойство("ДеревоОписанияПакета", ДеревоОписанияПакета) Тогда
		ДеревоОписанияПакета = ЗагрузитьДеревоОписанияПакетаДляСообщения(ОбъектСообщение, "ФНС");
	КонецЕсли;
	
	// проверяем дерево на заполненность
	Если НЕ ЗначениеЗаполнено(ДеревоОписанияПакета) Тогда
		Возврат Справочники.НалоговыеОрганы.ПустаяСсылка();
	КонецЕсли;
	
	// Берем код налогового органа из имени файла.
	// Для крупнейшего налогоплательщика код НО конечного (обособленного подразделения) и код НО промежуточного (основной НО) не совпадают.
	// ON_INFSOOB_7834_7811366337781101001_7811_20170921_77397713-accc-48b1-9cf7-b9272eb78db6
	
	КодОрганаФНС        = "";
	КодПромежуточнойФНС = "";
	
	Если ОбъектСообщение.Тип = Перечисления.ТипыТранспортныхСообщений.ДокументНО Тогда
	
		УзлыДокумент = ДеревоОписанияПакета.Строки.НайтиСтроки(Новый Структура("Имя, Тип", "документ", "Э"), Истина);
		Для каждого УзелДокумент Из УзлыДокумент Цикл
			УзелИсходноеИмяФайла = УзелДокумент.Строки.Найти("исходноеИмяФайла", "Имя"); 
			
			Если УзелИсходноеИмяФайла <> Неопределено Тогда
				ИсходноеИмяФайла = УзелИсходноеИмяФайла.Значение;
				
				Если СтрНайти(ИсходноеИмяФайла, "ON_INFSOOB_") > 0
					ИЛИ СтрНайти(ИсходноеИмяФайла, "ON_DOCNPNO_") > 0 Тогда
					
					ЧастиИмениФайла     = СтроковыеФункцииКлиентСервер.РазложитьСтрокуВМассивПодстрок(ИсходноеИмяФайла, "_", Истина, Истина);
					КодОрганаФНС        = ЧастиИмениФайла[4];
					КодПромежуточнойФНС = ЧастиИмениФайла[2];
					
				КонецЕсли;
				
			КонецЕсли;
			
		КонецЦикла;
		
	КонецЕсли;
	
	// ПисьмоНО или РассылкаНО
	Если ОбъектСообщение.Тип <> Перечисления.ТипыТранспортныхСообщений.ДокументНО ИЛИ КодОрганаФНС = "" Тогда
		
		// ищем узел "отправитель"
		УзлыОтправитель = ДеревоОписанияПакета.Строки.НайтиСтроки(Новый Структура("Имя, Тип", "отправитель", "Э"), Истина);
		Если УзлыОтправитель.Количество() = 0 Тогда
			Возврат Справочники.НалоговыеОрганы.ПустаяСсылка();
		КонецЕсли;
		УзелОтправитель = УзлыОтправитель[0];
		
		// ищем атрибут "идентификаторСубъекта"
		УзелИдентификаторСубъекта = УзелОтправитель.Строки.Найти("идентификаторСубъекта", "Имя");
		Если УзелИдентификаторСубъекта = Неопределено Тогда
			Возврат Справочники.НалоговыеОрганы.ПустаяСсылка();
		КонецЕсли;
		КодОрганаФНС = СокрЛП(УзелИдентификаторСубъекта.Значение);
		
	КонецЕсли;
	
	// ищем по справочнику
	РезультатПоиска = Справочники.НалоговыеОрганы.НайтиПоКоду(КодОрганаФНС);
	
	Если ЗначениеЗаполнено(КодОрганаФНС) И НЕ ЗначениеЗаполнено(РезультатПоиска) Тогда
		РезультатПоиска = СоздатьФНСПоКоду(КодОрганаФНС, КодПромежуточнойФНС);
	КонецЕсли;
	
	Возврат РезультатПоиска;
	
КонецФункции

Функция СоздатьФНСПоКоду(КодОрганаФНС, КодПромежуточнойФНС)
	
	НовыйФНС = Справочники.НалоговыеОрганы.СоздатьЭлемент();
	НовыйФНС.Код = КодОрганаФНС;
	НовыйФНС.Наименование = НСтр("ru = 'ИФНС '") + КодОрганаФНС;
	НовыйФНС.ПолноеНаименование = НовыйФНС.Наименование;
	
	// Копируем сертификат 
	ПромежуточнаяФНС = Справочники.НалоговыеОрганы.НайтиПоКоду(КодПромежуточнойФНС);
	Если ЗначениеЗаполнено(ПромежуточнаяФНС) Тогда
		Для каждого СертификатФНС Из ПромежуточнаяФНС.Сертификаты Цикл
			НоваяСтрока = НовыйФНС.Сертификаты.Добавить();
			НоваяСтрока.Сертификат = СертификатФНС.Сертификат;
		КонецЦикла; 
	КонецЕсли;
	
	НовыйФНС.Записать();
	
	Возврат НовыйФНС.Ссылка;
	
КонецФункции

// есть в Контейнере
Функция ОпределитьОтправителяВходящегоТранспортногоСообщения(Объект)
	
	Если ЗначениеЗаполнено(Объект.Отправитель) Тогда
		Возврат Объект.Отправитель;
	ИначеЕсли Объект.Статус <> Перечисления.СтатусыПисем.Полученное Тогда
		Возврат Неопределено;
	ИначеЕсли Объект.Тип = Перечисления.ТипыТранспортныхСообщений.ПодтверждениеПолученияОтчетностиПФР
	ИЛИ Объект.Тип = Перечисления.ТипыТранспортныхСообщений.ПротоколПФР
	ИЛИ Объект.Тип = Перечисления.ТипыТранспортныхСообщений.ПисьмоВходящееПФР
	ИЛИ Объект.Тип = Перечисления.ТипыТранспортныхСообщений.ПисьмоИсходящееКвитанцияПФР
	ИЛИ Объект.Тип = Перечисления.ТипыТранспортныхСообщений.ПодтверждениеПолученияЗапросаПФР
	ИЛИ Объект.Тип = Перечисления.ТипыТранспортныхСообщений.ОтветНаЗапросПФР
	ИЛИ Объект.Тип = Перечисления.ТипыТранспортныхСообщений.УведомлениеОбОшибкеПФР Тогда
		Возврат ОпределитьОтправителяВходящегоТранспортногоСообщенияПФР(Объект);
	ИначеЕсли Объект.Тип = Перечисления.ТипыТранспортныхСообщений.ИзвещениеОПолученииОтчетностиФСГС
	ИЛИ Объект.Тип = Перечисления.ТипыТранспортныхСообщений.ПодтверждениеДатыОтправкиФСГС
	ИЛИ Объект.Тип = Перечисления.ТипыТранспортныхСообщений.ПротоколВходногоКонтроляОтчетностиФСГС
	ИЛИ Объект.Тип = Перечисления.ТипыТранспортныхСообщений.ПисьмоВходящееФСГС
	ИЛИ Объект.Тип = Перечисления.ТипыТранспортныхСообщений.РассылкаФСГС
	ИЛИ Объект.Тип = Перечисления.ТипыТранспортныхСообщений.РассылкаШаблоновФСГС
	ИЛИ Объект.Тип = Перечисления.ТипыТранспортныхСообщений.ИзвещениеПисьмоИсходящееФСГС
	ИЛИ Объект.Тип = Перечисления.ТипыТранспортныхСообщений.УведомлениеОбОшибкеФСГС Тогда
		Возврат ОпределитьОтправителяВходящегоТранспортногоСообщенияФСГС(Объект);
	ИначеЕсли Объект.Тип = Перечисления.ТипыТранспортныхСообщений.ПисьмоНО
	ИЛИ Объект.Тип = Перечисления.ТипыТранспортныхСообщений.РассылкаНО
	ИЛИ Объект.Тип = Перечисления.ТипыТранспортныхСообщений.ДокументНО Тогда
		Возврат ОпределитьОтправителяВходящегоТранспортногоСообщенияФНС(Объект);
	ИначеЕсли НЕ Объект.УчетнаяЗапись.ОбменНапрямую Тогда
		Возврат Объект.УчетнаяЗапись.СерверДокументооборота;
	КонецЕсли;
	
	Если Объект.Тип = Перечисления.ТипыТранспортныхСообщений.ПодтверждениеДекларацияНО
	ИЛИ Объект.Тип = Перечисления.ТипыТранспортныхСообщений.ПодтверждениеЗаявлениеНО
	ИЛИ Объект.Тип = Перечисления.ТипыТранспортныхСообщений.ПодтверждениеОбращениеНО
	ИЛИ Объект.Тип = Перечисления.ТипыТранспортныхСообщений.ПодтверждениеПредставлениеНО
	ИЛИ Объект.Тип = Перечисления.ТипыТранспортныхСообщений.ПодтверждениеЗапросНО Тогда
		ОтправительСообщения = Объект.УчетнаяЗапись.СерверДокументооборота;
	ИначеЕсли Объект.Тип = Перечисления.ТипыТранспортныхСообщений.ИзвещениеДекларацияНО
	ИЛИ Объект.Тип = Перечисления.ТипыТранспортныхСообщений.РезультатПриемаДекларацияНО
	ИЛИ Объект.Тип = Перечисления.ТипыТранспортныхСообщений.РезультатОбработкиДекларацияНО
	ИЛИ Объект.Тип = Перечисления.ТипыТранспортныхСообщений.ИзвещениеОбращениеНО
	ИЛИ Объект.Тип = Перечисления.ТипыТранспортныхСообщений.ИзвещениеПредставлениеНО
	ИЛИ Объект.Тип = Перечисления.ТипыТранспортныхСообщений.ИзвещениеРезультатПриемаНО
	ИЛИ Объект.Тип = Перечисления.ТипыТранспортныхСообщений.РезультатПриемаОбращениеНО
	ИЛИ Объект.Тип = Перечисления.ТипыТранспортныхСообщений.РезультатПриемаПредставлениеНО
	ИЛИ Объект.Тип = Перечисления.ТипыТранспортныхСообщений.ИзвещениеЗапросНО
	ИЛИ Объект.Тип = Перечисления.ТипыТранспортныхСообщений.РезультатПриемаЗапросНО
	ИЛИ Объект.Тип = Перечисления.ТипыТранспортныхСообщений.РезультатОбработкиЗапросНО
	//заявление
	ИЛИ Объект.Тип = Перечисления.ТипыТранспортныхСообщений.ИзвещениеЗаявлениеНО
	ИЛИ Объект.Тип = Перечисления.ТипыТранспортныхСообщений.РезультатПриемаЗаявлениеНО
	
	ИЛИ Объект.Тип = Перечисления.ТипыТранспортныхСообщений.РезультатОбработкиЗаявлениеНО
	
	ИЛИ Объект.Тип = Перечисления.ТипыТранспортныхСообщений.РезультатОбработкиЗаявлениеРФНО
	ИЛИ Объект.Тип = Перечисления.ТипыТранспортныхСообщений.СообщениеОбОтзывеЗаявлениеРФНО
	ИЛИ Объект.Тип = Перечисления.ТипыТранспортныхСообщений.РезультатОбработкиЗаявлениеТСНО	
	ИЛИ Объект.Тип = Перечисления.ТипыТранспортныхСообщений.РезультатОбработкиЗаявлениеНО Тогда
		ОтправительСообщения = Объект.УчетнаяЗапись.НалоговыйОрган;



	ИначеЕсли Объект.Тип = Перечисления.ТипыТранспортныхСообщений.УведомлениеОбОшибкеФНС
	ИЛИ Объект.Тип = Перечисления.ТипыТранспортныхСообщений.КритическаяОшибка Тогда
		ОтправительСообщения = ОпределитьСерверДокументооборотаПоАдресуЭлектроннойПочты(Объект.ОтКогоАдрес);
		Если НЕ ЗначениеЗаполнено(ОтправительСообщения) Тогда
			ОтправительСообщения = ОпределитьНалоговыйОрганПоАдресуЭлектроннойПочты(Объект.ОтКогоАдрес);
		КонецЕсли;
	КонецЕсли;
	
	Возврат ОтправительСообщения;
	
КонецФункции

// есть в Контейнере
Функция ОпределитьНалоговыйОрганПоАдресуЭлектроннойПочты(АдресЭлектроннойПочты, ОпределитьСерверДокументооборотаПриНеобходимости = Истина) Экспорт
	
	Запрос = Новый Запрос("ВЫБРАТЬ РАЗРЕШЕННЫЕ ПЕРВЫЕ 1
	                      |	НалоговыеОрганы.Ссылка
	                      |ИЗ
	                      |	Справочник.НалоговыеОрганы КАК НалоговыеОрганы
	                      |ГДЕ
	                      |	НалоговыеОрганы.АдресЭлектроннойПочтыДляЦелейДокументооборотаСНалогоплательщиками = &АдресЭлектроннойПочты");
	Запрос.УстановитьПараметр("АдресЭлектроннойПочты", АдресЭлектроннойПочты);
	Выборка = Запрос.Выполнить().Выбрать();
	Если Выборка.Следующий() Тогда
		Возврат Выборка.Ссылка;
	КонецЕсли;
	
	Если ОпределитьСерверДокументооборотаПриНеобходимости Тогда
		Возврат ОпределитьСерверДокументооборотаПоАдресуЭлектроннойПочты(АдресЭлектроннойПочты, Ложь);
	Иначе
		Возврат Неопределено;
	КонецЕсли;
	
КонецФункции

// есть в Контейнере
Функция ОпределитьСерверДокументооборотаПоАдресуЭлектроннойПочты(АдресЭлектроннойПочты, ОпределитьНалоговыйОрганПриНеобходимости = Истина) Экспорт
	
	Запрос = Новый Запрос("ВЫБРАТЬ РАЗРЕШЕННЫЕ ПЕРВЫЕ 1
	                      |	СерверыДокументооборота.Ссылка
	                      |ИЗ
	                      |	Справочник.СерверыДокументооборота КАК СерверыДокументооборота
	                      |ГДЕ
	                      |	СерверыДокументооборота.АдресЭлектроннойПочтыФНС = &АдресЭлектроннойПочты
	                      |	ИЛИ СерверыДокументооборота.АдресЭлектроннойПочтыПФР = &АдресЭлектроннойПочты
	                      |	ИЛИ СерверыДокументооборота.АдресЭлектроннойПочтыФСГС = &АдресЭлектроннойПочты");
	Запрос.УстановитьПараметр("АдресЭлектроннойПочты", АдресЭлектроннойПочты);
	Выборка = Запрос.Выполнить().Выбрать();
	Если Выборка.Следующий() Тогда
		Возврат Выборка.Ссылка;
	КонецЕсли;
	
	Если ОпределитьНалоговыйОрганПриНеобходимости Тогда
		Возврат ОпределитьНалоговыйОрганПоАдресуЭлектроннойПочты(АдресЭлектроннойПочты, Ложь);
	Иначе
		Возврат Неопределено;
	КонецЕсли;
	
КонецФункции

// есть в Контейнере
Функция ОпределитьОтправителяВходящегоТранспортногоСообщенияПФР(ОбъектСообщение)
	
	// сначала пробуем определить отправителя из пакета
	ОтправительИзПакета = ОпределитьОтправителяВходящегоТранспортногоСообщенияПоСвойствамПакетаПФР(ОбъектСообщение);
	Если ЗначениеЗаполнено(ОтправительИзПакета) Тогда
		Возврат ОтправительИзПакета;
	КонецЕсли;
	
	// если по пакету отправителя определить не удалось,
	// то пробуем получить из свойств цикла обмена
	ЦиклОбмена = ОбъектСообщение.ЦиклОбмена;
	Если ЗначениеЗаполнено(ЦиклОбмена) Тогда
		ВнешняяОрганизация = ЦиклОбмена.ВнешняяОрганизация;
		Если ЗначениеЗаполнено(ВнешняяОрганизация) Тогда
			Возврат ВнешняяОрганизация;
		КонецЕсли;
	КонецЕсли;
	
	// если не удалось определить отправителя ни по пакету,
	// ни по родительскому циклу обмена, то возвращаем текущее значение
	Возврат ОбъектСообщение.Отправитель;
	
КонецФункции

// есть в Контейнере
Функция ОпределитьОтправителяВходящегоТранспортногоСообщенияПоСвойствамПакетаПФР(ОбъектСообщение)
	
	// получаем дерево описания пакета
	ДеревоОписанияПакета = Неопределено;
	Если НЕ ОбъектСообщение.ДополнительныеСвойства.Свойство("ДеревоОписанияПакета", ДеревоОписанияПакета) Тогда
		ДеревоОписанияПакета = ЗагрузитьДеревоОписанияПакетаДляСообщения(ОбъектСообщение, "ПФР");
	КонецЕсли;
	
	// проверяем дерево на заполненность
	Если НЕ ЗначениеЗаполнено(ДеревоОписанияПакета) Тогда
		Возврат Справочники.ОрганыПФР.ПустаяСсылка();
	КонецЕсли;
	
	// ищем узел "отправитель"
	УзлыОтправитель = ДеревоОписанияПакета.Строки.НайтиСтроки(Новый Структура("Имя, Тип", "отправитель", "Э"), Истина);
	Если УзлыОтправитель.Количество() = 0 Тогда
		Возврат Справочники.ОрганыПФР.ПустаяСсылка();
	КонецЕсли;
	УзелОтправитель = УзлыОтправитель[0];
	
	// ищем атрибут "идентификаторСубъекта"
	УзелИдентификаторСубъекта = УзелОтправитель.Строки.Найти("идентификаторСубъекта", "Имя");
	Если УзелИдентификаторСубъекта = Неопределено Тогда
		Возврат Справочники.ОрганыПФР.ПустаяСсылка();
	КонецЕсли;
	КодОрганаПФР = СокрЛП(УзелИдентификаторСубъекта.Значение);
	
	// ищем по справочнику
	РезультатПоиска = Справочники.ОрганыПФР.НайтиПоКоду(КодОрганаПФР);
	Если ЗначениеЗаполнено(РезультатПоиска) Тогда
		Возврат РезультатПоиска;
	КонецЕсли;
	
	// пробуем определить отправителя по циклу обмена
	ЦиклОбмена = ОбъектСообщение.ЦиклОбмена;
	Если ЗначениеЗаполнено(ЦиклОбмена) Тогда
		Возврат ЦиклОбмена.ВнешняяОрганизация;
	Иначе
		Возврат ОбъектСообщение.Отправитель;
	КонецЕсли;
	
КонецФункции

// есть в Контейнере
Функция ЯвляетсяОтрицательнымПротоколом(Объект) Экспорт
	
	Если Объект.Тип = Перечисления.ТипыТранспортныхСообщений.РезультатПриемаДекларацияНО Тогда
		КлючВТеме = "ФНС:1.0-уведомлениеОбОтказе-РезультатПриемаДекларацияНО";
		Возврат ВРЕГ(Лев(СокрЛП(Объект.Тема), СтрДлина(КлючВТеме))) = ВРЕГ(КлючВТеме);
	ИначеЕсли Объект.Тип = Перечисления.ТипыТранспортныхСообщений.РезультатПриемаОбращениеНО Тогда
		КлючВТеме = "ФНС:1.0-уведомлениеОбОтказе-РезультатПриемаОбращениеНО";
		Возврат ВРЕГ(Лев(СокрЛП(Объект.Тема), СтрДлина(КлючВТеме))) = ВРЕГ(КлючВТеме);
	ИначеЕсли Объект.Тип = Перечисления.ТипыТранспортныхСообщений.РезультатПриемаПредставлениеНО Тогда
		КлючВТеме = "ФНС:1.0-уведомлениеОбОтказе-РезультатПриемаПредставлениеНО";
		Возврат ВРЕГ(Лев(СокрЛП(Объект.Тема), СтрДлина(КлючВТеме))) = ВРЕГ(КлючВТеме);
	ИначеЕсли Объект.Тип = Перечисления.ТипыТранспортныхСообщений.РезультатПриемаЗаявлениеНО Тогда
		КлючВТеме = "ФНС:1.0-уведомлениеОбОтказе-РезультатПриемаЗаявлениеНО";
		Возврат ВРЕГ(Лев(СокрЛП(Объект.Тема), СтрДлина(КлючВТеме))) = ВРЕГ(КлючВТеме);
	ИначеЕсли Объект.Тип = Перечисления.ТипыТранспортныхСообщений.РезультатОбработкиЗаявлениеРФНО Тогда
		Возврат СообщениеОПроверкеЯвляетсяОтрицательным(Объект);
	ИначеЕсли Объект.Тип = Перечисления.ТипыТранспортныхСообщений.СообщениеОбОтзывеЗаявлениеРФНО 
	ИЛИ Объект.Тип = Перечисления.ТипыТранспортныхСообщений.РезультатОбработкиЗаявлениеТСНО Тогда
		Возврат Истина;
	ИначеЕсли Объект.Тип = Перечисления.ТипыТранспортныхСообщений.РезультатОбработкиЗаявлениеНО Тогда
		КлючВТеме = "ФНС:1.0-уведомлениеОбОтказеОтметки-РезультатОбработкиЗаявлениеНО";
		Возврат ВРЕГ(Лев(СокрЛП(Объект.Тема), СтрДлина(КлючВТеме))) = ВРЕГ(КлючВТеме);
	ИначеЕсли Объект.Тип = Перечисления.ТипыТранспортныхСообщений.РезультатПриемаЗапросНО Тогда
		КлючВТеме = "ФНС:1.0-уведомлениеОбОтказе-РезультатПриемаЗапросНО";
		Возврат ВРЕГ(Лев(СокрЛП(Объект.Тема), СтрДлина(КлючВТеме))) = ВРЕГ(КлючВТеме);
	ИначеЕсли Объект.Тип = Перечисления.ТипыТранспортныхСообщений.РезультатОбработкиДекларацияНО Тогда
		КлючВТеме = "ФНС:1.0-уведомлениеОбУточнении-РезультатОбработкиДекларацияНО";
		Возврат ВРЕГ(Лев(СокрЛП(Объект.Тема), СтрДлина(КлючВТеме))) = ВРЕГ(КлючВТеме);
	ИначеЕсли Объект.Тип = Перечисления.ТипыТранспортныхСообщений.ПротоколВходногоКонтроляЗапросаИОН Тогда
		КлючВТеме = "ФАЙЛ НЕ ПРИНЯТ";
		Возврат ВРЕГ(Лев(СокрЛП(Объект.Тема), СтрДлина(КлючВТеме))) = КлючВТеме;
	ИначеЕсли Объект.Тип = Перечисления.ТипыТранспортныхСообщений.РезультатПриемаФорма2НДФЛНО Тогда
		Возврат Протокол2НДФЛЯвляетсяОтрицательным(Объект);
	ИначеЕсли Объект.Тип = Перечисления.ТипыТранспортныхСообщений.ОтветНаЗапросИОН Тогда
		Возврат ОтветНаЗапросИОНЯвляетсяОтрицательным(Объект);
	ИначеЕсли Объект.Тип = Перечисления.ТипыТранспортныхСообщений.ОтветНаЗапросПФР Тогда
		Возврат ОтветНаЗапросИОСЯвляетсяОтрицательным(Объект);
	ИначеЕсли Объект.Тип = Перечисления.ТипыТранспортныхСообщений.ПодтверждениеПолученияОтчетностиПФР Тогда
		Возврат ПодтверждениеПолученияОтчетностиПФРЯвляетсяОтрицательным(Объект);
	ИначеЕсли Объект.Тип = Перечисления.ТипыТранспортныхСообщений.ПротоколПФР Тогда
		// Объект.Тема, содержащая "Pension-Protocol-N", может указывать на отрицательный протокол,
		// но для ЭДОК нельзя определить без расшифровки,
		// также при положительном ключе "Pension-Protocol-P" бывает отрицательный протокол
		Возврат ПротоколПФРЯвляетсяОтрицательным(Объект);
		
	ИначеЕсли Объект.Тип = Перечисления.ТипыТранспортныхСообщений.ПротоколВходногоКонтроляОтчетностиФСГС Тогда
		
		Возврат ПротоколФСГСЯвляетсяОтрицательным(Объект);
	ИначеЕсли Объект.Тип = Перечисления.ТипыТранспортныхСообщений.РезультатОбработкиЗапросаНаВыпискуЕРГЮЛ_ЕГРИП Тогда
		Возврат ВРЕГ(Объект.Тема) = ВРЕГ("СОС:1.0-уведомлениеОбОтказе-РезультатПриемаЗапрос");
	КонецЕсли;
	
	Возврат Ложь;
	
КонецФункции

Функция ОтветНаЗапросИОНЯвляетсяОтрицательным(Объект)
	
	// ищем ответ на запрос среди содержимого сообщения
	ОтветыНаЗапросыИОН = ПолучитьВложенияТранспортногоСообщения(Объект.Ссылка, Истина, Перечисления.ТипыСодержимогоТранспортногоКонтейнера.ОтветНаЗапросИОН);
	ЧислоОтветов = ОтветыНаЗапросыИОН.Количество();
	Если ЧислоОтветов = 0 Тогда
		Возврат Ложь;
	КонецЕсли;
	
	// ищем отказ
	ПрефиксИмениФайлаОтказа = "NO_NP";
	Для Каждого ОтветНаЗапросИОН Из ОтветыНаЗапросыИОН Цикл
		Если Лев(ОтветНаЗапросИОН.ИмяФайла, СтрДлина(ПрефиксИмениФайлаОтказа)) = ПрефиксИмениФайлаОтказа Тогда
			Возврат Истина;
		КонецЕсли;
	КонецЦикла;
	
	Возврат Ложь;
	
КонецФункции

Функция Протокол2НДФЛЯвляетсяОтрицательным(Объект)
	
	ТранспортноеСообщение = Объект.Ссылка;

	Если ЭтоПротокол2НДФЛ_КНД1110307(ТранспортноеСообщение) Тогда

		Сведения = СведенияИзПротокола2НДФЛ_1110307(ТранспортноеСообщение);
	    Возврат Сведения.Принятые = 0;
		
	Иначе
	
		// ищем протокол 2-НДФЛ среди содержимого сообщения
		Протоколы2НДФЛ = ПолучитьВложенияТранспортногоСообщения(Объект.Ссылка, Истина, Перечисления.ТипыСодержимогоТранспортногоКонтейнера.Протокол2НДФЛ);
		КоличествоПротоколов = Протоколы2НДФЛ.Количество();
		Если КоличествоПротоколов = 0 Тогда
			Возврат Ложь;
		Иначе
			Протокол2НДФЛ = Протоколы2НДФЛ[0];
		КонецЕсли;
		
		// загружаем протокол из файла в строку
		ТекстОшибки = "";
		ТекстПротокола = ТекстИзФайлаВСтроку(Протокол2НДФЛ, ТекстОшибки);
		Если НЕ ЗначениеЗаполнено(ТекстПротокола) Тогда
			Возврат Ложь;
		КонецЕсли;
		
		ТекстПротокола = ВРег(ТекстПротокола);
					
		ЕстьФайлНеПринят = СтрНайти(ТекстПротокола, "ФАЙЛ НЕ ПРИНЯТ") > 0;
				
		Возврат ЕстьФайлНеПринят ИЛИ Отчет2НДФЛСданЧастично(Объект);
		
	КонецЕсли;
	
КонецФункции

Функция ТекстИзФайлаВСтроку(УведомлениеОбУточнении, ТекстИсключения = Неопределено)
	
	ДанныеУведомления = УведомлениеОбУточнении.Данные;
	ИмяВременногоФайла = ПолучитьИмяВременногоФайла();
	
	Попытка
		ДанныеУведомления.Получить().Записать(ИмяВременногоФайла);
	Исключение
		ТекстИсключения = ИнформацияОбОшибке().Описание;
		Возврат Неопределено;
	КонецПопытки;
	
	ОбъектЧтение = НовыйЧтениеТекстаНаСервере(ИмяВременногоФайла);
	ТекстУведомления = ОбъектЧтение.Прочитать();
	ОбъектЧтение.Закрыть();
	
	УдалитьФайлы(ИмяВременногоФайла);
	
	Возврат ТекстУведомления;
	
КонецФункции

// есть в Контейнере
Функция ПротоколПФРЯвляетсяОтрицательным(Объект, ТребуетсяАнализПротокола = Истина)
	
	ЦиклОбмена = Объект.ЦиклОбмена;
	
	Если ЗначениеЗаполнено(ЦиклОбмена) Тогда
		СвойстваЦиклаОбмена = ЗначенияРеквизитовОбъекта(ЦиклОбмена, "ВидОтчета, ФорматДокументооборота");
	Иначе
		СвойстваЦиклаОбмена = Новый Структура("ВидОтчета, ФорматДокументооборота", Неопределено, Неопределено);
	КонецЕсли;
	
	ТребуетсяАнализПриложений = (СвойстваЦиклаОбмена.ВидОтчета =
		Справочники.ВидыОтправляемыхДокументов.СведенияОЗастрахованныхЛицахСЗВ_М
		ИЛИ СвойстваЦиклаОбмена.ВидОтчета =
		Справочники.ВидыОтправляемыхДокументов.СведенияОСтраховомСтажеЗастрахованныхЛицСЗВ_СТАЖ
		ИЛИ СвойстваЦиклаОбмена.ВидОтчета =
		Справочники.ВидыОтправляемыхДокументов.ДанныеОКорректировкеСведенийЗастрахованныхЛицСЗВ_КОРР
		ИЛИ СвойстваЦиклаОбмена.ВидОтчета =
		Справочники.ВидыОтправляемыхДокументов.СведенияОЗаработкеСтажеЗастрахованныхЛицСЗВ_ИСХ
		ИЛИ СвойстваЦиклаОбмена.ВидОтчета = Справочники.ВидыОтправляемыхДокументов.ОписьОДВ_1
		ИЛИ СвойстваЦиклаОбмена.ФорматДокументооборота = Перечисления.ФорматыДокументооборотаСПФР.ЭДОК);
	
	Если НЕ ТребуетсяАнализПротокола И НЕ ТребуетсяАнализПриложений Тогда
		Возврат Ложь;
	КонецЕсли;
	
	ТипыДИВ = Новый Массив;
	Если ТребуетсяАнализПротокола Тогда
		ТипыДИВ.Добавить(Перечисления.ТипыСодержимогоТранспортногоКонтейнера.ПротоколПФР);
	КонецЕсли;
	Если ТребуетсяАнализПриложений И СвойстваЦиклаОбмена.ФорматДокументооборота <>
		Перечисления.ФорматыДокументооборотаСПФР.ЭДОК Тогда
		ТипыДИВ.Добавить(Перечисления.ТипыСодержимогоТранспортногоКонтейнера.ПротоколПриложениеПФР);
	КонецЕсли;
	
	// получаем протокол из базы
	ОбъектСсылка = Объект.Ссылка;
	ВложенияТранспортногоСообщения = ПолучитьВложенияТранспортногоСообщения(ОбъектСсылка, Истина, ТипыДИВ);
	
	Если ТребуетсяАнализПротокола Тогда
		Для каждого ВложениеТранспортногоСообщения Из ВложенияТранспортногоСообщения Цикл
			Если ВложениеТранспортногоСообщения.Тип = Перечисления.ТипыСодержимогоТранспортногоКонтейнера.ПротоколПФР Тогда
				// выгружаем данные протокола во временный файл
				ИмяВременногоФайла = ПолучитьИмяВременногоФайла();
				Попытка
					ВложениеТранспортногоСообщения.Данные.Получить().Записать(ИмяВременногоФайла);
				Исключение
					ТекстСообщения = СтрШаблон(
						НСтр("ru = 'Не удалось выгрузить протокол во временный файл для анализа: %1'"),
																					Символы.ПС + ИнформацияОбОшибке().Описание);
					ОбщегоНазначения.СообщитьПользователю(ТекстСообщения);
					Возврат Неопределено;
				КонецПопытки;
				
				// загружаем XML из файла в память
				ДеревоXML = ЗагрузитьXMLВДеревоЗначений(ИмяВременногоФайла);
				ОперацииСФайламиЭДКО.УдалитьВременныйФайл(ИмяВременногоФайла);
				
				Если ДеревоXML <> Неопределено Тогда
					// ищем атрибут, содержащий статус проверки протокола
					РезультатыПоиска = ДеревоXML.Строки.НайтиСтроки(Новый Структура("Имя, Тип", "являетсяПоложительным", "Э"), Истина);
					
					Если РезультатыПоиска.Количество() > 0 Тогда
						// преобразовываем значение атрибута к Булево и возвращаем
						ЗначениеАтрибутаСтр = РезультатыПоиска[0].Значение;
						ЗначениеАтрибута = БулевоПоСтроке(ЗначениеАтрибутаСтр);
						Если ЗначениеАтрибута <> Неопределено И НЕ ЗначениеАтрибута Тогда
							Возврат Истина;
						КонецЕсли;
					КонецЕсли;
				КонецЕсли;
				
				Прервать;
			КонецЕсли;
		КонецЦикла;
	КонецЕсли;
	
	Если ТребуетсяАнализПриложений Тогда
		АнализПриложенияПротокола = ОтчетПФРСданЧастично(
			Объект,
			ВложенияТранспортногоСообщения,
			СвойстваЦиклаОбмена.ФорматДокументооборота);
		
		Возврат АнализПриложенияПротокола.ЕстьПриложение И (АнализПриложенияПротокола.СданЧастично ИЛИ АнализПриложенияПротокола.НеСдан);
	КонецЕсли;
	
	Возврат Ложь;
	
КонецФункции

Функция ПротоколФСГСОтчетСданПоТексту(СтатусПротокола, СообщениеПротокола) Экспорт
	
	Если НРег(СтатусПротокола) = "ok"
		ИЛИ (НРег(СтатусПротокола) = "warnings"
			   И (СтрНайти(НРег(СообщениеПротокола), "ошибок не обнаружено") > 0
			    ИЛИ СтрНайти(НРег(СообщениеПротокола), "отчет принят") > 0
			    ИЛИ СтрНайти(НРег(СообщениеПротокола), "отчёт принят") > 0
			    ИЛИ СтрНайти(НРег(СообщениеПротокола), "обнаружены предупреждения") > 0
				ИЛИ СтрНайти(НРег(СообщениеПротокола), "содержит предупреждения") > 0
				)) Тогда
		Возврат Истина;
	КонецЕсли;
	
	Возврат Ложь;
	
КонецФункции

// есть в Контейнере
Функция ПротоколФСГСЯвляетсяОтрицательным(Объект)
	
	// получаем протокол из базы
	ОбъектСсылка = Объект.Ссылка;
	
	ТипыДИВ = Новый Массив;
	ТипыДИВ.Добавить(Перечисления.ТипыСодержимогоТранспортногоКонтейнера.УведомлениеОПриемеВОбработкуОтчетаФСГС);
	ТипыДИВ.Добавить(Перечисления.ТипыСодержимогоТранспортногоКонтейнера.УведомлениеОНесоответствииФорматуОтчетаФСГС);
	ТипыДИВ.Добавить(Перечисления.ТипыСодержимогоТранспортногоКонтейнера.УведомлениеОбУточненииОтчетаФСГС);
	ТипыДИВ.Добавить(Перечисления.ТипыСодержимогоТранспортногоКонтейнера.УведомлениеОбОтказеОтчетаФСГС);
	ТипыДИВ.Добавить(Перечисления.ТипыСодержимогоТранспортногоКонтейнера.УведомлениеОбОтклоненииОтчетаФСГС);

	СтрПротоколы = ПолучитьВложенияТранспортногоСообщения(ОбъектСсылка, Истина, ТипыДИВ);
	Если СтрПротоколы.Количество() = 0 Тогда
		Возврат Ложь;
	КонецЕсли;
	
	// выгружаем данные протокола во временный файл
	СтрПротокол = СтрПротоколы[0];
	
	Если СтрПротокол.Тип = Перечисления.ТипыСодержимогоТранспортногоКонтейнера.УведомлениеОбОтклоненииОтчетаФСГС Тогда
		//	содержащий информацию о невозможности принять в обработку предоставленные респондентом 
		//	через оператора первичные статистические данные, ввиду того, что первичные статистические данные 
		//	этого же респондента за указанный отчетный период по указанной форме предоставлены посредством 
		//	другого способа сдачи отчетности
		Возврат Истина;
	КонецЕсли;
	
	ИмяВременногоФайла = ПолучитьИмяВременногоФайла();
	Попытка
		СтрПротокол.Данные.Получить().Записать(ИмяВременногоФайла);
	Исключение
		ОбщегоНазначения.СообщитьПользователю(НСтр("ru = 'Не удалось выгрузить протокол во временный файл для анализа'") + ":
				|" + ИнформацияОбОшибке().Описание);
		Возврат Неопределено;
	КонецПопытки;
	
	// считываем текст из файла уведомления
	Попытка
		ТекстПротокола = ПрочитатьТекстИзФайла(ИмяВременногоФайла, , Истина);
	Исключение
		ТекстСообщения = СтроковыеФункцииКлиентСервер.ПодставитьПараметрыВСтроку(НСтр("ru = 'Не удалось прочитать текст протокола:%1'"),
																					Символы.ПС + ИнформацияОбОшибке().Описание);
		ОбщегоНазначения.СообщитьПользователю(ТекстСообщения);
		
		Возврат Неопределено;
	КонецПопытки;
	
	// загружаем XML из строки в дерево
	ДеревоXML = ЗагрузитьСтрокуXMLВДеревоЗначений(ТекстПротокола);
	
	Если ДеревоXML <> Неопределено Тогда
	
		УзелПротокол = ДеревоXML.Строки.Найти("protocol", "Имя");
		
		Если УзелПротокол <> Неопределено Тогда
			
			УзелСтатусПротокола = УзелПротокол.Строки.Найти("status", "Имя");
			СтатусПротокола = "";
			Если УзелСтатусПротокола <> Неопределено Тогда
				СтатусПротокола = XMLЗначение(Тип("Строка"), УзелСтатусПротокола.Значение);
			КонецЕсли;
			УзелСообщениеПротокола = УзелПротокол.Строки.Найти("msg", "Имя");
			СообщениеПротокола = "";
			Если УзелСообщениеПротокола <> Неопределено Тогда
				СообщениеПротокола = XMLЗначение(Тип("Строка"), УзелСообщениеПротокола.Значение);
			КонецЕсли;
			
			ОтчетСдан = ПротоколФСГСОтчетСданПоТексту(СтатусПротокола, СообщениеПротокола);
			Если ОтчетСдан Тогда 
				Возврат Ложь;
			ИначеЕсли УзелПротокол.Строки.Найти("group", "Имя") <> Неопределено Тогда
				Возврат Истина;
			КонецЕсли;
				
		КонецЕсли;
		
	КонецЕсли;
	
	// Исключение из стандарта 456.
	// Требуется полное соответствие строке из внешнего файла
	Если СтрНайти(ВРег(ТекстПротокола), ВРег("Отчёт не принят")) > 0 ИЛИ СтрНайти(ВРег(ТекстПротокола), ВРег("Отчет не принят")) > 0 Тогда
	
		Возврат Истина;
	
	КонецЕсли;
	
	Возврат Ложь;
	
КонецФункции

// есть в Контейнере
Функция БулевоПоСтроке(СтрБулево) Экспорт
	
	Попытка
		Возврат XMLЗначение(Тип("Булево"), СокрЛП(СтрБулево));
	Исключение
		Возврат Неопределено;
	КонецПопытки;
	
КонецФункции

// есть и в модуле и в Контейнере
Функция ПодтверждениеПолученияОтчетностиПФРЯвляетсяОтрицательным(Объект)
	
	// получаем протокол из базы
	ОбъектСсылка = Объект.Ссылка;
	СтрПодтвержденияПолучения = ПолучитьВложенияТранспортногоСообщения(ОбъектСсылка, Истина, Перечисления.ТипыСодержимогоТранспортногоКонтейнера.ПодтверждениеПолученияОтчетностиПФР);
	Если СтрПодтвержденияПолучения.Количество() = 0 Тогда
		Возврат Ложь;
	КонецЕсли;
	
	// выгружаем данные протокола во временный файл
	СтрПодтверждениеПолучения = СтрПодтвержденияПолучения[0];
	ИмяВременногоФайла = ПолучитьИмяВременногоФайла();
	Попытка
		СтрПодтверждениеПолучения.Данные.Получить().Записать(ИмяВременногоФайла);
	Исключение
		ТекстСообщения = СтроковыеФункцииКлиентСервер.ПодставитьПараметрыВСтроку(НСтр("ru = 'Не удалось выгрузить подтверждение получения отчетности ПФР во временный файл для анализа:%1'"),
																					Символы.ПС + ИнформацияОбОшибке().Описание);
		ЗаписатьСобытиеВЖурнал(УровеньЖурналаРегистрации.Ошибка, ТекстСообщения);
		Возврат Неопределено;
	КонецПопытки;
	
	// загружаем XML из файла в память
	ДеревоXML = ЗагрузитьXMLВДеревоЗначений(ИмяВременногоФайла);
	ОперацииСФайламиЭДКО.УдалитьВременныйФайл(ИмяВременногоФайла);
	Если ДеревоXML = Неопределено Тогда
		Возврат Ложь;
	КонецЕсли;
	
	// ищем атрибут, содержащий статус проверки протокола
	РезультатыПоиска = ДеревоXML.Строки.НайтиСтроки(Новый Структура("Имя, Тип", "отчетПредставлен", "Э"), Истина);
	Если РезультатыПоиска.Количество() = 0 Тогда
		Возврат Ложь;
	КонецЕсли;
	
	// преобразовываем значение атрибута к Булево и возвращаем
	ЗначениеАтрибутаСтр = РезультатыПоиска[0].Значение;
	ЗначениеАтрибута = БулевоПоСтроке(ЗначениеАтрибутаСтр);
	Если ЗначениеАтрибута <> Неопределено Тогда
		Возврат НЕ ЗначениеАтрибута;
	КонецЕсли;
	
	Возврат Ложь;
	
КонецФункции

Функция СообщениеОПроверкеЯвляетсяОтрицательным(Объект)
	
	// получаем сообщение о проверке из базы
	ОбъектСсылка = Объект.Ссылка;
	СтрСообщения = ПолучитьВложенияТранспортногоСообщения(ОбъектСсылка, Истина, Перечисления.ТипыСодержимогоТранспортногоКонтейнера.СообщениеОПроверке);
	Если СтрСообщения.Количество() = 0 Тогда
		Возврат Ложь;
	КонецЕсли;
	
	// выгружаем данные протокола во временный файл
	СтрСообщениеОПроверке = СтрСообщения[0];
	ИмяВременногоФайла = ПолучитьИмяВременногоФайла();
	Попытка
		СтрСообщениеОПроверке.Данные.Получить().Записать(ИмяВременногоФайла);
	Исключение
		ТекстСообщения = СтроковыеФункцииКлиентСервер.ПодставитьПараметрыВСтроку(НСтр("ru = 'Не удалось выгрузить сообщение о проверке во временный файл для анализа:%1'"),
		Символы.ПС + ИнформацияОбОшибке().Описание);
		ЗаписатьСобытиеВЖурнал(УровеньЖурналаРегистрации.Ошибка, ТекстСообщения);
		
		Возврат Неопределено;
	КонецПопытки;
	
	// загружаем XML из файла в память
	ДеревоXML = ЗагрузитьXMLВДеревоЗначений(ИмяВременногоФайла);
	ОперацииСФайламиЭДКО.УдалитьВременныйФайл(ИмяВременногоФайла);
	Если ДеревоXML = Неопределено Тогда
		Возврат Неопределено;
	КонецЕсли;
	
	// ищем атрибут, содержащий статус проверки протокола
	РезультатыПоиска = ДеревоXML.Строки.НайтиСтроки(Новый Структура("Имя", "Отметка"), Истина);
	Если РезультатыПоиска.Количество() = 0 Тогда
		ТекстСообщения = НСтр("ru = 'Не удалось получить отметку о проверке заявления'");
		ЗаписатьСобытиеВЖурнал(УровеньЖурналаРегистрации.Ошибка, ТекстСообщения);
		Возврат Неопределено;
	КонецЕсли;
	
	// преобразовываем значение атрибута к Булево и возвращаем
	ЗначениеОтметки = СокрЛП(РезультатыПоиска[0].Значение);
	Если ЗначениеОтметки = "1" ИЛИ ЗначениеОтметки = "2" Тогда
		Возврат Ложь;
	ИначеЕсли ЗначениеОтметки = "3" Тогда
		Возврат Истина;
	Иначе 
		ТекстСообщения = НСтр("ru = 'Некорректное значение отметки о проверке заявления'");
		ЗаписатьСобытиеВЖурнал(УровеньЖурналаРегистрации.Ошибка, ТекстСообщения);
		Возврат Неопределено;
	КонецЕсли;
	
КонецФункции

// есть в Контейнере
Функция ОпределитьТипСообщения(Сообщение, ВложенияЕсть = Истина) Экспорт
	
	// получаем значения дополнительных реквизитов заголовка
	Отправитель = "";
	СтрокаОтправитель = Сообщение.ДополнительныеРеквизитыЗаголовка.Найти(Перечисления.ТипыДополнительныхРеквизитов.ИдентификаторОтправителя, "Тип");
	Если СтрокаОтправитель <> Неопределено Тогда
		Отправитель = СтрокаОтправитель.Значение;
	КонецЕсли;
	
	Получатель = "";
	СтрокаПолучатель = Сообщение.ДополнительныеРеквизитыЗаголовка.Найти(Перечисления.ТипыДополнительныхРеквизитов.ИдентификаторПолучателя, "Тип");
	Если СтрокаПолучатель <> Неопределено Тогда
		Получатель = СтрокаПолучатель.Значение;
	КонецЕсли;
	
	ИмяПередающейСистемы = "";
	СтрокаИмяПередающейСистемы = Сообщение.ДополнительныеРеквизитыЗаголовка.Найти(Перечисления.ТипыДополнительныхРеквизитов.ИмяПередающейСистемы, "Тип");
	Если СтрокаИмяПередающейСистемы <> Неопределено Тогда
		ИмяПередающейСистемы = СтрокаИмяПередающейСистемы.Значение;
	КонецЕсли;
	
	ТипПередаваемойИнформации = "";
	СтрокаТипПередаваемойИнформации = Сообщение.ДополнительныеРеквизитыЗаголовка.Найти(Перечисления.ТипыДополнительныхРеквизитов.ТипПередаваемойИнформации, "Тип");
	Если СтрокаТипПередаваемойИнформации <> Неопределено Тогда
		ТипПередаваемойИнформации = СтрокаТипПередаваемойИнформации.Значение;
	КонецЕсли;
	
	ИдентификаторПервичногоСообщения = "";
	СтрокаИдентификаторПервичногоСообщения = Сообщение.ДополнительныеРеквизитыЗаголовка.Найти(Перечисления.ТипыДополнительныхРеквизитов.ИдентификаторПервичногоСообщения, "Тип");
	Если СтрокаИдентификаторПервичногоСообщения <> Неопределено Тогда
		ИдентификаторПервичногоСообщения = СтрокаИдентификаторПервичногоСообщения.Значение;
	КонецЕсли;
	
	Если ЧислоКонтейнеровСообщения(Сообщение) = 0 И ЗначениеЗаполнено(ИдентификаторПервичногоСообщения) Тогда
		Возврат Перечисления.ТипыТранспортныхСообщений.КритическаяОшибка;
	КонецЕсли;
	
	Тема = Сообщение.Тема;
	ВРЕГСокрЛПТема = ВРЕГ(СокрЛП(Тема));
	
	// обход ситуации, когда тема модифицируется антиспам-фильтром
	Если Лев(ВРЕГСокрЛПТема, 5) = "[SPAM" Тогда
		ВхождениеЗакрывающейСкобки = СтрНайти(ВРЕГСокрЛПТема, "]");
		Если ВхождениеЗакрывающейСкобки <> 0 Тогда
			ВРЕГСокрЛПТема = СокрЛП(Сред(ВРЕГСокрЛПТема, ВхождениеЗакрывающейСкобки + 1));
		КонецЕсли;
	Конецесли;
	
	// ФНС 534
	Если ВРЕГСокрЛПТема = ВРЕГ("ФНС:1.0-подтверждениеДатыОтправки-ПодтверждениеДекларацияНО")
	ИЛИ ВРЕГСокрЛПТема = ВРЕГ("ФНС:1.0-подтверждениеДатыОтправки-ПодтверждениеДекларацияСОС") Тогда
		Возврат Перечисления.ТипыТранспортныхСообщений.ПодтверждениеДекларацияНО;
	ИначеЕсли ВРЕГСокрЛПТема = ВРЕГ("ФНС:1.0-извещениеОПолучении-ИзвещениеДекларацияНО") Тогда
		Возврат Перечисления.ТипыТранспортныхСообщений.ИзвещениеДекларацияНО;
	ИначеЕсли ВРЕГСокрЛПТема = ВРЕГ("ФНС:1.0-уведомлениеОбОтказе-РезультатПриемаДекларацияНО")
	ИЛИ ВРЕГСокрЛПТема = ВРЕГ("ФНС:1.0-квитанцияОПриеме-РезультатПриемаДекларацияНО") Тогда
		Возврат Перечисления.ТипыТранспортныхСообщений.РезультатПриемаДекларацияНО;
	ИначеЕсли ВРЕГСокрЛПТема = ВРЕГ("ФНС:1.0-уведомлениеОбУточнении-РезультатОбработкиДекларацияНО")
	ИЛИ ВРЕГСокрЛПТема = ВРЕГ("ФНС:1.0-извещениеОВводе-РезультатОбработкиДекларацияНО") Тогда
		Возврат Перечисления.ТипыТранспортныхСообщений.РезультатОбработкиДекларацияНО;
	ИначеЕсли ВРЕГСокрЛПТема = ВРЕГ("ФНС:1.0-сообщениеОбОшибке-СообщениеОбОшибке") Тогда
		Возврат Перечисления.ТипыТранспортныхСообщений.УведомлениеОбОшибкеФНС;
	ИначеЕсли ВРЕГСокрЛПТема = ВРЕГ("ФНС:1.0-подтверждениеДатыОтправки-ПодтверждениеОтправкиСОС") Тогда
		Если ЗначениеЗаполнено(ИдентификаторПервичногоСообщения) Тогда
			ЦиклОбменаСообщения = НайтиЦиклОбменаПоИдентификатору(ИдентификаторПервичногоСообщения);
			Если ЗначениеЗаполнено(ЦиклОбменаСообщения) Тогда
				Если ЦиклОбменаСообщения.Тип = Перечисления.ТипыЦикловОбмена.НалоговаяИлиБухгалтерскаяОтчетность Тогда
					Возврат Перечисления.ТипыТранспортныхСообщений.ПодтверждениеДекларацияНО;
				ИначеЕсли ЦиклОбменаСообщения.Тип = Перечисления.ТипыЦикловОбмена.Заявление Тогда
					Возврат Перечисления.ТипыТранспортныхСообщений.ПодтверждениеЗаявлениеНО;
				ИначеЕсли ЦиклОбменаСообщения.Тип = Перечисления.ТипыЦикловОбмена.Форма2НДФЛ Тогда
					Возврат Перечисления.ТипыТранспортныхСообщений.ПодтверждениеФорма2НДФЛНО;
				ИначеЕсли ЦиклОбменаСообщения.Тип = Перечисления.ТипыЦикловОбмена.ОбращениеНП Тогда
					Возврат Перечисления.ТипыТранспортныхСообщений.ПодтверждениеОбращениеНО;
				ИначеЕсли ЦиклОбменаСообщения.Тип = Перечисления.ТипыЦикловОбмена.Представление Тогда
					Возврат Перечисления.ТипыТранспортныхСообщений.ПодтверждениеПредставлениеНО;
				ИначеЕсли ЦиклОбменаСообщения.Тип = Перечисления.ТипыЦикловОбмена.ИОН Тогда
					Возврат Перечисления.ТипыТранспортныхСообщений.ПодтверждениеЗапросНО;
				КонецЕсли;
			Иначе
				Возврат Перечисления.ТипыТранспортныхСообщений.ПодтверждениеДекларацияНО;
			КонецЕсли;
		Иначе
			Возврат Перечисления.ТипыТранспортныхСообщений.ПодтверждениеДекларацияНО;
		КонецЕсли;

	// ФНС 534 ДРП НО
	ИначеЕсли ВРЕГСокрЛПТема = ВРЕГ("ФНС:1.0-документ-ДокументНО") Тогда
		Возврат Перечисления.ТипыТранспортныхСообщений.ДокументНО;
	ИначеЕсли ВРЕГСокрЛПТема = ВРЕГ("ФНС:1.0-извещениеОПолучении-ИзвещениеРезультатПриемаНО") Тогда
		Возврат Перечисления.ТипыТранспортныхСообщений.ИзвещениеРезультатПриемаНО;
		
	//Заявление
	ИначеЕсли ВРЕГСокрЛПТема = ВРЕГ("ФНС:1.0-подтверждениеДатыОтправки-ПодтверждениеЗаявлениеНО") 
	ИЛИ ВРЕГСокрЛПТема = ВРЕГ("ФНС:1.0-подтверждениеДатыОтправки-ПодтверждениеЗаявлениеСОС") Тогда
		Возврат Перечисления.ТипыТранспортныхСообщений.ПодтверждениеЗаявлениеНО;
	ИначеЕсли ВРЕГСокрЛПТема = ВРЕГ("ФНС:1.0-извещениеОПолучении-ИзвещениеЗаявлениеНО") Тогда
		Возврат Перечисления.ТипыТранспортныхСообщений.ИзвещениеЗаявлениеНО;
	ИначеЕсли ВРЕГСокрЛПТема = ВРЕГ("ФНС:1.0-уведомлениеОбОтказе-РезультатПриемаЗаявлениеНО")
	ИЛИ ВРЕГСокрЛПТема = ВРЕГ("ФНС:1.0-квитанцияОПриеме-РезультатПриемаЗаявлениеНО") Тогда
		Возврат Перечисления.ТипыТранспортныхСообщений.РезультатПриемаЗаявлениеНО;
	ИначеЕсли ВРЕГСокрЛПТема = ВРЕГ("ФНС:1.0-сообщениеОПроверке-РезультатОбработкиЗаявлениеРФНО") Тогда
		Возврат Перечисления.ТипыТранспортныхСообщений.РезультатОбработкиЗаявлениеРФНО;
	ИначеЕсли ВРЕГСокрЛПТема = ВРЕГ("ФНС:1.0-сообщениеОбОтзывеЗаявления-СообщениеОбОтзывеЗаявлениеРФНО") Тогда
		Возврат Перечисления.ТипыТранспортныхСообщений.СообщениеОбОтзывеЗаявлениеРФНО;
	ИначеЕсли ВРЕГСокрЛПТема = ВРЕГ("ФНС:1.0-сообщениеОНесоответствиях-РезультатОбработкиЗаявлениеТСНО") Тогда
		Возврат Перечисления.ТипыТранспортныхСообщений.РезультатОбработкиЗаявлениеТСНО;
		
	ИначеЕсли ВРЕГСокрЛПТема = ВРЕГ("ФНС:1.0-сообщениеОПростОтметки-РезультатОбработкиЗаявлениеНО") Тогда
		Возврат Перечисления.ТипыТранспортныхСообщений.РезультатОбработкиЗаявлениеНО;
	ИначеЕсли ВРЕГСокрЛПТема = ВРЕГ("ФНС:1.0-уведомлениеОбОтказеОтметки-РезультатОбработкиЗаявлениеНО") Тогда
		Возврат Перечисления.ТипыТранспортныхСообщений.РезультатОбработкиЗаявлениеНО;
		
	//Запрос на выписку из ЕГР(ЮЛ\ИП)
	ИначеЕсли ВРЕГСокрЛПТема = ВРЕГ("СОС:1.0-уведомлениеОбОтказе-РезультатПриемаЗапрос") ИЛИ
		ВРЕГСокрЛПТема = ВРЕГ("СОС:1.0-ответ-РезультатОбработкиЗапрос") Тогда
		Возврат Перечисления.ТипыТранспортныхСообщений.РезультатОбработкиЗапросаНаВыпискуЕРГЮЛ_ЕГРИП;
		
	// ФНС 534 2-НДФЛ
	ИначеЕсли ВРЕГСокрЛПТема = ВРЕГ("ФНС:1.0-подтверждениеДатыОтправки-ПодтверждениеФорма2НДФЛНО") Тогда
		Возврат Перечисления.ТипыТранспортныхСообщений.ПодтверждениеФорма2НДФЛНО;
	ИначеЕсли ВРЕГСокрЛПТема = ВРЕГ("ФНС:1.0-извещениеОПолучении-ИзвещениеФорма2НДФЛНО") Тогда
		Возврат Перечисления.ТипыТранспортныхСообщений.ИзвещениеФорма2НДФЛНО;
	ИначеЕсли ВРЕГСокрЛПТема = ВРЕГ("ФНС:1.0-протоколПриема2НДФЛ-РезультатПриемаФорма2НДФЛНО") Тогда
		Возврат Перечисления.ТипыТранспортныхСообщений.РезультатПриемаФорма2НДФЛНО;
		
	// ФНС 534 НФД НП
	ИначеЕсли ВРЕГСокрЛПТема = ВРЕГ("ФНС:1.0-подтверждениеДатыОтправки-ПодтверждениеОбращениеНО")
	ИЛИ ВРЕГСокрЛПТема = ВРЕГ("ФНС:1.0-подтверждениеДатыОтправки-ПодтверждениеОбращениеСОС")
	ИЛИ ВРЕГСокрЛПТема = ВРЕГ("ФНС:1.0-подтверждениеДатыПолучения-ПодтверждениеОбращениеНО")
	ИЛИ ВРЕГСокрЛПТема = ВРЕГ("ФНС:1.0-подтверждениеДатыПолучения-ПодтверждениеОбращениеСОС") Тогда
		Возврат Перечисления.ТипыТранспортныхСообщений.ПодтверждениеОбращениеНО;
	ИначеЕсли ВРЕГСокрЛПТема = ВРЕГ("ФНС:1.0-извещениеОПолучении-ИзвещениеОбращениеНО") Тогда
		Возврат Перечисления.ТипыТранспортныхСообщений.ИзвещениеОбращениеНО;
	ИначеЕсли ВРЕГСокрЛПТема = ВРЕГ("ФНС:1.0-уведомлениеОбОтказе-РезультатПриемаОбращениеНО") Тогда
		Возврат Перечисления.ТипыТранспортныхСообщений.РезультатПриемаОбращениеНО;
		
	// ФНС 534 Представление
	ИначеЕсли ВРЕГСокрЛПТема = ВРЕГ("ФНС:1.0-подтверждениеДатыОтправки-ПодтверждениеПредставлениеНО")
	ИЛИ ВРЕГСокрЛПТема = ВРЕГ("ФНС:1.0-подтверждениеДатыОтправки-ПодтверждениеПредставлениеСОС")
	ИЛИ ВРЕГСокрЛПТема = ВРЕГ("ФНС:1.0-подтверждениеДатыПолучения-ПодтверждениеПредставлениеНО")
	ИЛИ ВРЕГСокрЛПТема = ВРЕГ("ФНС:1.0-подтверждениеДатыПолучения-ПодтверждениеПредставлениеСОС") Тогда
		Возврат Перечисления.ТипыТранспортныхСообщений.ПодтверждениеПредставлениеНО;
	ИначеЕсли ВРЕГСокрЛПТема = ВРЕГ("ФНС:1.0-извещениеОПолучении-ИзвещениеПредставлениеНО") Тогда
		Возврат Перечисления.ТипыТранспортныхСообщений.ИзвещениеПредставлениеНО;
	ИначеЕсли ВРЕГСокрЛПТема = ВРЕГ("ФНС:1.0-уведомлениеОбОтказе-РезультатПриемаПредставлениеНО") Тогда
		Возврат Перечисления.ТипыТранспортныхСообщений.РезультатПриемаПредставлениеНО;
	ИначеЕсли ВРЕГСокрЛПТема = ВРЕГ("ФНС:1.0-квитанцияОПриеме-РезультатПриемаПредставлениеНО") Тогда
		Возврат Перечисления.ТипыТранспортныхСообщений.РезультатПриемаПредставлениеНО;
		
	// ФНС 534 НФД НО
	ИначеЕсли ВРЕГСокрЛПТема = ВРЕГ("ФНС:1.0-письмо-ПисьмоНО") Тогда
		Возврат Перечисления.ТипыТранспортныхСообщений.ПисьмоНО;
	ИначеЕсли ВРЕГСокрЛПТема = ВРЕГ("ФНС:1.0-рассылка-РассылкаНО") Тогда
		Возврат Перечисления.ТипыТранспортныхСообщений.РассылкаНО;
		
	// ФНС 534 ИОН
	ИначеЕсли ВРЕГСокрЛПТема = ВРЕГ("ФНС:1.0-подтверждениеДатыПолучения-ПодтверждениеЗапросНО")
	ИЛИ ВРЕГСокрЛПТема = ВРЕГ("ФНС:1.0-подтверждениеДатыПолучения-ПодтверждениеЗапросСОС")
	ИЛИ ВРЕГСокрЛПТема = ВРЕГ("ФНС:1.0-подтверждениеДатыОтправки-ПодтверждениеЗапросНО")
	ИЛИ ВРЕГСокрЛПТема = ВРЕГ("ФНС:1.0-подтверждениеДатыОтправки-ПодтверждениеЗапросСОС") Тогда
		Возврат Перечисления.ТипыТранспортныхСообщений.ПодтверждениеЗапросНО;
	ИначеЕсли ВРЕГСокрЛПТема = ВРЕГ("ФНС:1.0-извещениеОПолучении-ИзвещениеЗапросНО") Тогда
		Возврат Перечисления.ТипыТранспортныхСообщений.ИзвещениеЗапросНО;
	ИначеЕсли ВРЕГСокрЛПТема = ВРЕГ("ФНС:1.0-уведомлениеОбОтказе-РезультатПриемаЗапросНО")
	ИЛИ ВРЕГСокрЛПТема = ВРЕГ("ФНС:1.0-квитанцияОПриеме-РезультатПриемаЗапросНО") Тогда
		Возврат Перечисления.ТипыТранспортныхСообщений.РезультатПриемаЗапросНО;
	ИначеЕсли ВРЕГСокрЛПТема = ВРЕГ("ФНС:1.0-ответ-РезультатОбработкиЗапросНО") Тогда
		Возврат Перечисления.ТипыТранспортныхСообщений.РезультатОбработкиЗапросНО;
	// Росстат через Такском, Калуга-Астрал и др.
	ИначеЕсли СтрНайти(ВРЕГСокрЛПТема, ВРЕГ("Stat-Confirmation")) <> 0 Тогда
		Возврат Перечисления.ТипыТранспортныхСообщений.ПодтверждениеДатыОтправкиФСГС;
	ИначеЕсли СтрНайти(ВРЕГСокрЛПТема, ВРЕГ("ReSign: Stat-Unformal")) <> 0 Тогда
		Возврат Перечисления.ТипыТранспортныхСообщений.ИзвещениеПисьмоИсходящееФСГС;
	ИначеЕсли СтрНайти(ВРЕГСокрЛПТема, ВРЕГ("ReSign: Stat")) <> 0 Тогда
		Возврат Перечисления.ТипыТранспортныхСообщений.ИзвещениеОПолученииОтчетностиФСГС;
	ИначеЕсли СтрНайти(ВРЕГСокрЛПТема, ВРЕГ("Stat-Error-Notification")) <> 0 Тогда
		Возврат Перечисления.ТипыТранспортныхСообщений.УведомлениеОбОшибкеФСГС;
	ИначеЕсли СтрНайти(ВРЕГСокрЛПТема, ВРЕГ("Stat-Unformal")) <> 0 Тогда
		ДеревоОписанияПакета = Неопределено;
		Если НЕ Сообщение.ДополнительныеСвойства.Свойство("ДеревоОписанияПакета", ДеревоОписанияПакета) Тогда
			ДеревоОписанияПакета = ЗагрузитьДеревоОписанияПакетаДляСообщения(Сообщение, "ФСГС");
			Сообщение.ДополнительныеСвойства.Вставить("ДеревоОписанияПакета", ДеревоОписанияПакета);
		КонецЕсли;
		
		Если ДеревоОписанияПакета <> Неопределено Тогда
			ТипСообщения = ОпределитьТипСообщенияПоДеревуОписанияПакета(ДеревоОписанияПакета, "ФСГС");
		КонецЕсли;
		
		Если ТипСообщения = Перечисления.ТипыТранспортныхСообщений.РассылкаФСГС Тогда 
			Возврат Перечисления.ТипыТранспортныхСообщений.РассылкаФСГС;
		ИначеЕсли ТипСообщения = Перечисления.ТипыТранспортныхСообщений.РассылкаШаблоновФСГС Тогда 
			Возврат Перечисления.ТипыТранспортныхСообщений.РассылкаШаблоновФСГС;
		КонецЕсли;
		
		Возврат Перечисления.ТипыТранспортныхСообщений.ПисьмоВходящееФСГС;
	ИначеЕсли СтрНайти(ВРЕГСокрЛПТема, ВРЕГ("Stat-Protocol")) <> 0 Тогда
		Возврат Перечисления.ТипыТранспортныхСообщений.ПротоколВходногоКонтроляОтчетностиФСГС;
	// ПФР через Такском
	ИначеЕсли СтрНайти(ВРЕГСокрЛПТема, ВРЕГ("ReSign: Pension-Unformal")) <> 0 Тогда
		Возврат Перечисления.ТипыТранспортныхСообщений.ПисьмоИсходящееКвитанцияПФР;
	ИначеЕсли СтрНайти(ВРЕГСокрЛПТема, ВРЕГ("ReSign: Pension-Request")) <> 0 Тогда
		Возврат Перечисления.ТипыТранспортныхСообщений.ПодтверждениеПолученияЗапросаПФР;
	ИначеЕсли СтрНайти(ВРЕГСокрЛПТема, ВРЕГ("ReSign: Pension")) <> 0 Тогда
		Возврат Перечисления.ТипыТранспортныхСообщений.ПодтверждениеПолученияОтчетностиПФР;
	ИначеЕсли СтрНайти(ВРЕГСокрЛПТема, ВРЕГ("Pension-Error-Notification")) <> 0 Тогда
		Возврат Перечисления.ТипыТранспортныхСообщений.УведомлениеОбОшибкеПФР;
	ИначеЕсли СтрНайти(ВРЕГСокрЛПТема, ВРЕГ("Pension-Unformal")) <> 0 Тогда
		Возврат Перечисления.ТипыТранспортныхСообщений.ПисьмоВходящееПФР;
	ИначеЕсли СтрНайти(ВРЕГСокрЛПТема, ВРЕГ("Pension-Protocol-")) <> 0 Тогда
		Возврат Перечисления.ТипыТранспортныхСообщений.ПротоколПФР;
	ИначеЕсли СтрНайти(ВРЕГСокрЛПТема, ВРЕГ("Pension-Reply")) <> 0 Тогда
		Возврат Перечисления.ТипыТранспортныхСообщений.ОтветНаЗапросПФР;
		
	// ПФР через прочих
	Иначе
		
		// считываем с кэшированием
		ДеревоОписанияПакета = Неопределено;
		Если НЕ Сообщение.ДополнительныеСвойства.Свойство("ДеревоОписанияПакета", ДеревоОписанияПакета) Тогда
			ДеревоОписанияПакета = ЗагрузитьДеревоОписанияПакетаДляСообщения(Сообщение);
			Сообщение.ДополнительныеСвойства.Вставить("ДеревоОписанияПакета", ДеревоОписанияПакета);
		КонецЕсли;
		
		// определяем тип сообщения
		ТипСообщения = Неопределено;
		Если ДеревоОписанияПакета <> Неопределено Тогда
			ТипСообщения = ОпределитьТипСообщенияПоДеревуОписанияПакета(ДеревоОписанияПакета);
		КонецЕсли;
		
		// если тип определить не удалось, то устанавливаем тип Прочее
		Если ТипСообщения = Неопределено Тогда
			Возврат Перечисления.ТипыТранспортныхСообщений.Прочее;
		Иначе
			Возврат ТипСообщения;
		КонецЕсли;
		
	КонецЕсли;
	
КонецФункции

// есть в Контейнере
// Только для ПФР. Для ФНС определение производится по теме сообщения.
// 
Функция ОпределитьТипСообщенияПоДеревуОписанияПакета(ДеревоОписанияПакета, ТипСообщения = Неопределено)
	
	ТипДокументооборота = "";
	ТипТранзакции = "";
	ЭтоОписьСодержанияПакета = Ложь;
	
	// ищем ключевые узлы
	УзлыТипДокументооборота = ДеревоОписанияПакета.Строки.НайтиСтроки(Новый Структура("Имя, Тип", "типДокументооборота", "А"), Истина);
	УзлыТипТранзакции = ДеревоОписанияПакета.Строки.НайтиСтроки(Новый Структура("Имя, Тип", "типТранзакции", "А"), Истина);
	Если УзлыТипДокументооборота.Количество() = 0 ИЛИ УзлыТипТранзакции.Количество() = 0 Тогда
		Если ТипСообщения = "ПФР" ИЛИ ТипСообщения = Неопределено Тогда
			УзелОСП = НайтиСтрокиДереваЗначений(ДеревоОписанияПакета,,, "ОСП", "Э");
			Если ЗначениеЗаполнено(УзелОСП) Тогда
				ЭтоОписьСодержанияПакета = Истина; // ЭДОК
				УзелДокумент = НайтиСтрокиДереваЗначений(ДеревоОписанияПакета,,, "Документ", "Э");
				Если ЗначениеЗаполнено(УзелДокумент) Тогда
					УзелКод = НайтиСтрокиДереваЗначений(ДеревоОписанияПакета,,, "Код", "Э");
					Если ЗначениеЗаполнено(УзелКод) Тогда
						ТипТранзакции = СокрЛП(УзелКод.Значение);
						ТипТранзакции = ВРег(ТипТранзакции);
						
						Если ТипТранзакции = "УОПП" Тогда
							ТипДокументооборота = "ОшибкаОбработкиПакета";
						ИначеЕсли ЗначениеЗаполнено(ТипТранзакции) Тогда
							ТипДокументооборота = "СведенияПФР";
						КонецЕсли;
					КонецЕсли;
				КонецЕсли;
			КонецЕсли;
		КонецЕсли;
		Если НЕ ЗначениеЗаполнено(ТипТранзакции) И НЕ ЗначениеЗаполнено(ТипДокументооборота) Тогда
			Возврат Неопределено;
		КонецЕсли;
	КонецЕсли;


	
	Если НЕ ЭтоОписьСодержанияПакета Тогда
		УзелТипДокументооборота = УзлыТипДокументооборота[0];
		УзелТипТранзакции = УзлыТипТранзакции[0];
		
		// получаем значения узлов
		ТипДокументооборота = СокрЛП(УзелТипДокументооборота.Значение);
		ТипТранзакции = СокрЛП(УзелТипТранзакции.Значение);
	КонецЕсли;
	
	ЭтоФорматФСГС = Ложь;
	УзлыВерсияФормата = ДеревоОписанияПакета.Строки.НайтиСтроки(Новый Структура("Имя, Тип", "версияФормата", "А"), Истина);
	Если УзлыВерсияФормата.Количество() > 0 Тогда
		ВерсияФормата = СокрЛП(УзлыТипТранзакции[0].Значение);
		Если СтрНайти(ВРЕГ(ВерсияФормата), "СТАТ") > 0 Тогда
			ЭтоФорматФСГС = Истина;
		КонецЕсли; 
	КонецЕсли;
	
	// анализируем значения
	Если ТипДокументооборота = "СведенияПФР" Тогда
		Если ТипТранзакции = "сведения" Тогда
			Возврат Перечисления.ТипыТранспортныхСообщений.ПервичноеСообщениеСодержащееОтчетностьПФР;
		ИначеЕсли ТипТранзакции = "подтверждениеПолучения" ИЛИ ТипТранзакции = "УОД" Тогда
			Возврат Перечисления.ТипыТранспортныхСообщений.ПодтверждениеПолученияОтчетностиПФР;
		ИначеЕсли ТипТранзакции = "протокол" ИЛИ ТипТранзакции = "УОРР" ИЛИ ТипТранзакции = "УППО"
			ИЛИ ТипТранзакции = "УУОН-ПУ" Тогда
			Возврат Перечисления.ТипыТранспортныхСообщений.ПротоколПФР;
		ИначеЕсли ТипТранзакции = "протоколКвитанция" Тогда
			Возврат Перечисления.ТипыТранспортныхСообщений.ПротоколКвитанцияПФР;
		ИначеЕсли ЭтоОписьСодержанияПакета Тогда
			Возврат Перечисления.ТипыТранспортныхСообщений.ПервичноеСообщениеСодержащееОтчетностьПФР;
		КонецЕсли;
	ИначеЕсли ТипДокументооборота = "УточнениеПлатежей" Тогда
		Если ТипТранзакции = "запрос" Тогда
			Возврат Перечисления.ТипыТранспортныхСообщений.ПервичноеСообщениеСодержащееЗапросПФР;
		ИначеЕсли ТипТранзакции = "ответКвитанция" Тогда
			Возврат Перечисления.ТипыТранспортныхСообщений.ОтветНаЗапросКвитанцияПФР;
		ИначеЕсли ТипТранзакции = "ответ" Тогда
			Возврат Перечисления.ТипыТранспортныхСообщений.ОтветНаЗапросПФР;
		ИначеЕсли ТипТранзакции = "запросКвитанция" Тогда
			Возврат Перечисления.ТипыТранспортныхСообщений.ПодтверждениеПолученияЗапросаПФР;
		КонецЕсли;
	ИначеЕсли ТипДокументооборота = "Письмо" Тогда
		Если ТипТранзакции = "письмо" Тогда
			Возврат Перечисления.ТипыТранспортныхСообщений.ПисьмоВходящееПФР;
		ИначеЕсли ТипТранзакции = "письмоКвитанция" Тогда
			Возврат Перечисления.ТипыТранспортныхСообщений.ПисьмоИсходящееКвитанцияПФР;
		КонецЕсли;
	ИначеЕсли ТипДокументооборота = "ОшибкаОбработкиПакета" И НЕ ЭтоФорматФСГС Тогда
		Возврат Перечисления.ТипыТранспортныхСообщений.УведомлениеОбОшибкеПФР;
	ИначеЕсли ТипДокументооборота = "отчетСтат" Тогда
		Если ТипТранзакции = "отчет" Тогда
			Возврат Перечисления.ТипыТранспортныхСообщений.ПервичноеСообщениеСодержащееОтчетностьФСГС;
		ИначеЕсли ТипТранзакции = "отчетИзвещение" Тогда
			Возврат Перечисления.ТипыТранспортныхСообщений.ИзвещениеОПолученииОтчетностиФСГС;
		ИначеЕсли ТипТранзакции = "протокол" Тогда
			Возврат Перечисления.ТипыТранспортныхСообщений.ПротоколВходногоКонтроляОтчетностиФСГС;
		ИначеЕсли ТипТранзакции = "протоколИзвещение" Тогда
			Возврат Перечисления.ТипыТранспортныхСообщений.ИзвещениеПротоколВходногоКонтроляОтчетностиФСГС;
		ИначеЕсли ТипТранзакции = "подтверждениеОператора" Тогда
			Возврат Перечисления.ТипыТранспортныхСообщений.ПодтверждениеДатыОтправкиФСГС;
		ИначеЕсли ТипТранзакции = "подтверждениеКвитанция" Тогда
			Возврат Перечисления.ТипыТранспортныхСообщений.ИзвещениеПодтверждениеДатыОтправкиФСГС;
		КонецЕсли;
	ИначеЕсли ТипДокументооборота = "письмоОрганФСГС" Тогда
		Если ТипТранзакции = "письмо" Тогда
			Возврат Перечисления.ТипыТранспортныхСообщений.ПисьмоВходящееФСГС;
		ИначеЕсли ТипТранзакции = "извещение" Тогда
			Возврат Перечисления.ТипыТранспортныхСообщений.ИзвещениеПисьмоВходящееФСГС;
		КонецЕсли;
	ИначеЕсли ТипДокументооборота = "письмоРеспондент" И ЭтоФорматФСГС Тогда
		Если ТипТранзакции = "письмо" Тогда
			Возврат Перечисления.ТипыТранспортныхСообщений.ПисьмоИсходящееФСГС;
		ИначеЕсли ТипТранзакции = "извещение" Тогда
			Возврат Перечисления.ТипыТранспортныхСообщений.ИзвещениеПисьмоИсходящееФСГС;
		ИначеЕсли ТипТранзакции = "подтверждениеОператора" Тогда
			Возврат Перечисления.ТипыТранспортныхСообщений.ПодтверждениеДатыОтправкиФСГС;
		ИначеЕсли ТипТранзакции = "подтверждениеКвитанция" Тогда
			Возврат Перечисления.ТипыТранспортныхСообщений.ИзвещениеПодтверждениеДатыОтправкиФСГС;
		КонецЕсли;
	ИначеЕсли ТипДокументооборота = "ОшибкаОбработкиПакета" Тогда
		Если ТипТранзакции = "уведомлениеОбОшибке" Тогда
			Возврат Перечисления.ТипыТранспортныхСообщений.УведомлениеОбОшибкеФСГС;
		КонецЕсли; 
	ИначеЕсли ТипДокументооборота = "рассылка" Тогда
		Если ТипТранзакции = "рассылка" Тогда
			Возврат Перечисления.ТипыТранспортныхСообщений.РассылкаФСГС;
		КонецЕсли;
	ИначеЕсли ТипДокументооборота = "рассылкаШаблонов" Тогда
		Если ТипТранзакции = "рассылкаШаблонов" Тогда
			Возврат Перечисления.ТипыТранспортныхСообщений.РассылкаШаблоновФСГС;
		КонецЕсли;
	КонецЕсли;
	
	Возврат Неопределено;
	
КонецФункции

// есть в Контейнере
Функция ЧислоКонтейнеровСообщения(Сообщение)
	
	Запрос = Новый Запрос("ВЫБРАТЬ РАЗРЕШЕННЫЕ
						  |	КОЛИЧЕСТВО(Истина) КАК ЧислоСообщений
						  |ИЗ
						  |	РегистрСведений.ТранспортныеКонтейнеры КАК ТранспортныеКонтейнеры
						  |ГДЕ
						  |	ТранспортныеКонтейнеры.ТранспортноеСообщение = &ТранспортноеСообщение");
	Запрос.УстановитьПараметр("ТранспортноеСообщение", ?(Сообщение.ЭтоНовый(), Сообщение.ПолучитьСсылкуНового(), Сообщение.Ссылка));
	Выборка = Запрос.Выполнить().Выбрать();
	Если Выборка.Следующий() Тогда
		Возврат ?(ЗначениеЗаполнено(Выборка.ЧислоСообщений), Выборка.ЧислоСообщений, 0);
	Иначе
		Возврат 0;
	КонецЕсли;
	
КонецФункции

// есть в Контейнере
Функция ДобавитьТранспортныйКонтейнер(ТранспортноеСообщение, Данные, ИмяФайла, Размер = Неопределено) Экспорт
	
	Если Размер = Неопределено Тогда
		Попытка
			Размер = ОпределитьРазмер(Данные);
		Исключение
			ТекстСообщения = СтроковыеФункцииКлиентСервер.ПодставитьПараметрыВСтроку(
				НСтр("ru = 'Не удалось сохранить в информационной базе транспортный контейнер сообщения %1!%2'"),
				Символ(34) + ТранспортноеСообщение.Ссылка + Символ(34),
				Символы.ПС + ИнформацияОбОшибке().Описание);
			ЗаписатьСобытиеВЖурнал(УровеньЖурналаРегистрации.Ошибка, ТекстСообщения);
			Возврат Ложь;
		КонецПопытки;
	КонецЕсли;
	
	НачатьТранзакцию();
	
	МенеджерЗаписи = РегистрыСведений.ТранспортныеКонтейнеры.СоздатьМенеджерЗаписи();
	МенеджерЗаписи.ТранспортноеСообщение = ТранспортноеСообщение;
	МенеджерЗаписи.ИмяФайла = ИмяФайла;
	МенеджерЗаписи.Размер = Размер;
	
	ТипДанных = ТипЗнч(Данные);
	Если ТипДанных = Тип("ХранилищеЗначения") Тогда
		ДвоичныеДанныеФайла = Данные.Получить();
	ИначеЕсли ТипДанных = Тип("Строка") Тогда
		ДвоичныеДанныеФайла = Новый ДвоичныеДанные(Данные);
	Иначе // если двоичные данные
		ДвоичныеДанныеФайла = Данные;
	КонецЕсли;
	АдресФайла = ПоместитьВоВременноеХранилище(ДвоичныеДанныеФайла, Новый УникальныйИдентификатор);
	
	КомпонентыИмениФайла = ОбщегоНазначенияКлиентСервер.РазложитьПолноеИмяФайла(ИмяФайла);
	УникальноеИмяФайлаБезРасширения = ОбщегоНазначенияЭДКОКлиентСервер.УникальнаяСтрока(КомпонентыИмениФайла.ИмяБезРасширения, 150);
	РасширениеФайлаБезТочки = Сред(КомпонентыИмениФайла.Расширение, 2);
	
	ПараметрыФайла = Новый Структура;
	ПараметрыФайла.Вставить("Автор", 						Неопределено);
	ПараметрыФайла.Вставить("ВладелецФайлов", 				ТранспортноеСообщение);
	ПараметрыФайла.Вставить("ИмяБезРасширения", 			УникальноеИмяФайлаБезРасширения);
	ПараметрыФайла.Вставить("РасширениеБезТочки", 			РасширениеФайлаБезТочки);
	ПараметрыФайла.Вставить("ВремяИзмененияУниверсальное", 	Неопределено);
	
	ИнформацияОбОбновляемомФайле = Новый Структура;
	ИнформацияОбОбновляемомФайле.Вставить("АдресФайлаВоВременномХранилище", АдресФайла);
	ИнформацияОбОбновляемомФайле.Вставить("АдресВременногоХранилищаТекста", Неопределено);
	ИнформацияОбОбновляемомФайле.Вставить("ИмяБезРасширения", 				УникальноеИмяФайлаБезРасширения);
	ИнформацияОбОбновляемомФайле.Вставить("Расширение", 					РасширениеФайлаБезТочки);
	
	Попытка
		МенеджерЗаписи.Записать(Истина);
		
		МассивФайлов = ОбщегоНазначенияЭДКО.ПрикрепленныеФайлыКОбъектуИзСправочника(
			ТранспортноеСообщение,
			"ТранспортноеСообщениеКонтейнерыПрисоединенныеФайлы",
			ИмяФайла);
		
		Если МассивФайлов.Количество() > 0 Тогда
			ПрисоединенныйФайл = МассивФайлов[0];
			РаботаСФайлами.ОбновитьФайл(ПрисоединенныйФайл, ИнформацияОбОбновляемомФайле);
			
		Иначе
			НоваяСсылкаНаФайл = РаботаСФайлами.НоваяСсылкаНаФайл(ТранспортноеСообщение, "ТранспортноеСообщениеКонтейнерыПрисоединенныеФайлы");
			ПрисоединенныйФайл = РаботаСФайлами.ДобавитьФайл(ПараметрыФайла, АдресФайла,,, НоваяСсылкаНаФайл);
		КонецЕсли;
		
		ПрисоединенныйФайлОбъект = ПрисоединенныйФайл.ПолучитьОбъект();
		
		ПрисоединенныйФайлОбъект.ИсходноеИмяФайла = ИмяФайла;
		
		ПрисоединенныйФайлОбъект.Записать();
	Исключение
		ОтменитьТранзакцию();
		УдалитьИзВременногоХранилища(АдресФайла);
		ТекстСообщения = СтроковыеФункцииКлиентСервер.ПодставитьПараметрыВСтроку(
			НСтр("ru = 'Не удалось сохранить в информационной базе транспортный контейнер сообщения %1!%2'"),
			Символ(34) + ТранспортноеСообщение.Ссылка + Символ(34),
			Символы.ПС + ИнформацияОбОшибке().Описание);
		ЗаписатьСобытиеВЖурнал(УровеньЖурналаРегистрации.Ошибка, ТекстСообщения);
		Возврат Ложь;
	КонецПопытки;
	
	ЗафиксироватьТранзакцию();
	УдалитьИзВременногоХранилища(АдресФайла);
	Возврат Истина;
	
КонецФункции

// есть в Контейнере
Функция ОпределитьОтправителяВходящегоТранспортногоСообщенияФСГС(ОбъектСообщение)
	
	// сначала пробуем определить отправителя из пакета
	ОтправительИзПакета = ОпределитьОтправителяВходящегоТранспортногоСообщенияПоСвойствамПакетаФСГС(ОбъектСообщение);
	Если ЗначениеЗаполнено(ОтправительИзПакета) Тогда
		Возврат ОтправительИзПакета;
	КонецЕсли;
	
	// если по пакету отправителя определить не удалось,
	// то пробуем получить из свойств цикла обмена
	ЦиклОбмена = ОбъектСообщение.ЦиклОбмена;
	Если ЗначениеЗаполнено(ЦиклОбмена) Тогда
		ВнешняяОрганизация = ЦиклОбмена.ВнешняяОрганизация;
		Если ЗначениеЗаполнено(ВнешняяОрганизация) Тогда
			Возврат ВнешняяОрганизация;
		КонецЕсли;
	КонецЕсли;
	
	// если не удалось определить отправителя ни по пакету,
	// ни по родительскому циклу обмена, то возвращаем текущее значение
	Возврат ОбъектСообщение.Отправитель;
	
КонецФункции

// есть в Контейнере
Функция ОпределитьОтправителяВходящегоТранспортногоСообщенияПоСвойствамПакетаФСГС(ОбъектСообщение)
	
	// получаем дерево описания пакета
	ДеревоОписанияПакета = Неопределено;
	Если НЕ ОбъектСообщение.ДополнительныеСвойства.Свойство("ДеревоОписанияПакета", ДеревоОписанияПакета) Тогда
		ДеревоОписанияПакета = ЗагрузитьДеревоОписанияПакетаДляСообщения(ОбъектСообщение, "ФСГС");
	КонецЕсли;
	
	// проверяем дерево на заполненность
	Если НЕ ЗначениеЗаполнено(ДеревоОписанияПакета) Тогда
		Возврат Справочники.ОрганыФСГС.ПустаяСсылка();
	КонецЕсли;
	
	// ищем узел "отправитель"
	УзлыОтправитель = ДеревоОписанияПакета.Строки.НайтиСтроки(Новый Структура("Имя, Тип", "отправитель", "Э"), Истина);
	Если УзлыОтправитель.Количество() = 0 Тогда
		Возврат Справочники.ОрганыФСГС.ПустаяСсылка();
	КонецЕсли;
	УзелОтправитель = УзлыОтправитель[0];
	
	// ищем атрибут "идентификаторСубъекта"
	УзелИдентификаторСубъекта = УзелОтправитель.Строки.Найти("идентификаторСубъекта", "Имя");
	Если УзелИдентификаторСубъекта = Неопределено Тогда
		Возврат Справочники.ОрганыФСГС.ПустаяСсылка();
	КонецЕсли;
	КодОрганаФСГС = СокрЛП(УзелИдентификаторСубъекта.Значение);
	
	// ищем по справочнику
	РезультатПоиска = Справочники.ОрганыФСГС.НайтиПоКоду(КодОрганаФСГС);
	Если ЗначениеЗаполнено(РезультатПоиска) Тогда
		Возврат РезультатПоиска;
	КонецЕсли;
	
	// пробуем определить отправителя по циклу обмена
	ЦиклОбмена = ОбъектСообщение.ЦиклОбмена;
	Если ЗначениеЗаполнено(ЦиклОбмена) Тогда
		Возврат ЦиклОбмена.ВнешняяОрганизация;
	Иначе
		Возврат ОбъектСообщение.Отправитель;
	КонецЕсли;
	
КонецФункции

Функция ИзвлечьCNИздателя(РеквизитыИздателя) Экспорт
	
	ПоставщикCN = "";
	Если Не РеквизитыИздателя.Свойство("CN", ПоставщикCN) Тогда 
		Если Не РеквизитыИздателя.Свойство("OID2_5_4_3", ПоставщикCN) Тогда // 	CN, AKA 2.5.4.3
			Если Не РеквизитыИздателя.Свойство("OID2_5_4_10", ПоставщикCN) Тогда 
				ПоставщикCN = "";
			КонецЕсли;
		КонецЕсли;
	КонецЕсли;
	
	Возврат ПоставщикCN;
	
КонецФункции

Функция БелыйСписокПоставщиков() Экспорт
	ТочноеСовпадение = СтрРазделить(ВРег("ГМЦ Росстата,ФНС России,FNS Russia,УЦ 1 ИС ГУЦ"), ",");
	ТочноеСовпадение.Добавить(ВРег("Минкомсвязь России"));
	ТочноеСовпадение.Добавить(ВРег("Федеральная налоговая служба"));
	ТочноеСовпадение.Добавить(ВРег("Головной удостоверяющий центр"));
	
	НеточноеСовпадение = СтрРазделить(ВРег("УЦ&ОПФР,ОПФР&област,ОПФР&Республик,ОПФР&краю,ФНС России,ФГБУ НИИ&Восход"),
		",");
	НеточноеСовпадение.Добавить(ВРег("Фонд социального страхования"));
	НеточноеСовпадение.Добавить(ВРег("Головной удостоверяющий центр"));
	НеточноеСовпадение.Добавить(ВРег("Корневой удостоверяющий центр"));
	НеточноеСовпадение.Добавить(ВРег("Удостоверяющий центр ФСРАР"));
	НеточноеСовпадение.Добавить(ВРег("АДИКОМ"));
	
	Возврат Новый Структура("ТочноеСовпадение, НеточноеСовпадение", ТочноеСовпадение, НеточноеСовпадение);
КонецФункции

Функция СертификатЯвляетсяСторонним(ПоставщикCN, ОбщиеПараметры) Экспорт
	
	СтороннийСертификат = Ложь;
	Если Не ЗначениеЗаполнено(ПоставщикCN) Тогда 
		Возврат СтороннийСертификат;
	КонецЕсли;
	
	ТочноеСовпадение = ОбщиеПараметры.ТочноеСовпадение;
	НеточноеСовпадение = ОбщиеПараметры.НеточноеСовпадение;
	
	ЗаглПоставщикCN = ВРег(ПоставщикCN);				
	Если ТочноеСовпадение.Найти(ЗаглПоставщикCN) <> Неопределено Тогда 
		СтороннийСертификат = Истина;
	Иначе
		Для Каждого ВыражениеПроверки Из НеточноеСовпадение Цикл 
			Если СтрЧислоВхождений(ВыражениеПроверки, "&") = 0 Тогда 
				Если СтрНайти(ЗаглПоставщикCN, ВыражениеПроверки) > 0 Тогда 
					СтороннийСертификат = Истина;
					Прервать;
				КонецЕсли;
			Иначе
				Совпадений = 0;
				ПодстрокиПроверки = СтрРазделить(ВыражениеПроверки, "&");
				Для Каждого ПодстрокаПроверки Из ПодстрокиПроверки Цикл 
					Если СтрНайти(ЗаглПоставщикCN, ПодстрокаПроверки) > 0 Тогда 
						Совпадений = Совпадений + 1;
					КонецЕсли;
				КонецЦикла;
				Если Совпадений = ПодстрокиПроверки.Количество() И Совпадений > 0 Тогда 
					СтороннийСертификат = Истина;
					Прервать;
				КонецЕсли;
			КонецЕсли;
		КонецЦикла;
	КонецЕсли;
	
	Возврат СтороннийСертификат;
КонецФункции

// есть в Контейнере
Процедура ДополнитьТранспортноеСообщениеПолучателями(ТранспортноеСообщение, АдресФайлаДокумента) Экспорт
	
	Если ТранспортноеСообщение.СертификатыПолучателей.Количество() > 0 Тогда
		Возврат;
	КонецЕсли;
	
	Попытка
		ДанныеЗашифрованногоФайла = ПолучитьИзВременногоХранилища(АдресФайлаДокумента);
		СвойстваКриптосообщения = КриптографияЭДКОСлужебныйВызовСервера.ПолучитьСвойстваКриптосообщения(ДанныеЗашифрованногоФайла);
		
		Если СвойстваКриптосообщения.Тип <> "EnvelopedData" Тогда
			Возврат;
		КонецЕсли;	
		Если Не СвойстваКриптосообщения.Свойство("Получатели") Тогда
			Возврат;
		КонецЕсли;
		Если СвойстваКриптосообщения.Получатели.Количество() <= ТранспортноеСообщение.СертификатыПолучателей.Количество() Тогда
			Возврат;
		КонецЕсли;
		
		Если ТипЗнч(ТранспортноеСообщение) = Тип("ДокументСсылка.ТранспортноеСообщение") Тогда 
			ДокОбъект = ТранспортноеСообщение.ПолучитьОбъект();
		ИначеЕсли ТипЗнч(ТранспортноеСообщение) = Тип("ДокументОбъект.ТранспортноеСообщение") Тогда 
			ДокОбъект = ТранспортноеСообщение;
		Иначе
			Возврат;
		КонецЕсли;
		
		Если СвойстваКриптосообщения.Получатели.Количество() > 0 Тогда
			ОбщиеПараметры = БелыйСписокПоставщиков();
		КонецЕсли;
				
		ДокОбъект.СертификатыПолучателей.Очистить();
		Для Каждого ПолучательСообщения Из СвойстваКриптосообщения.Получатели Цикл
			
			ПоставщикCN = ИзвлечьCNИздателя(ПолучательСообщения.Издатель);			
			СерийныйНомерСтрокой = НРег(ПолучитьHexСтрокуИзДвоичныхДанных(ПолучательСообщения.СерийныйНомер));
			ПолучательВДокументе = ДокОбъект.СертификатыПолучателей.Добавить();
			ПолучательВДокументе.СерийныйНомер = СерийныйНомерСтрокой;
			ПолучательВДокументе.ПоставщикCN = ПоставщикCN;
			ПолучательВДокументе.Отпечаток = "";
			ПолучательВДокументе.Сторонний = СертификатЯвляетсяСторонним(ПоставщикCN, ОбщиеПараметры);
			
			ПараметрыОтбора = Новый Структура;
			ПараметрыОтбора.Вставить("СерийныйНомер", СерийныйНомерСтрокой);
			
			НайденныеСтроки = ДокОбъект.УчетнаяЗапись.СертификатыЛичные.НайтиСтроки(ПараметрыОтбора);
			Если НайденныеСтроки.Количество() > 0 Тогда 
				Для Каждого СтрСертификата Из НайденныеСтроки Цикл 
					Если СтрСертификата.ПоставщикCN = ПоставщикCN Тогда 
						ПолучательВДокументе.Отпечаток = СтрСертификата.Отпечаток;
						Если ПолучательВДокументе.Сторонний Тогда
							ПолучательВДокументе.Сторонний = Ложь;
						КонецЕсли;
					КонецЕсли;
				КонецЦикла;
			КонецЕсли;			

		КонецЦикла;
		Если ДокОбъект.СертификатыПолучателей.Количество() > 0 Тогда
			ДокОбъект.Записать(РежимЗаписиДокумента.Запись);
			ТранспортноеСообщение = ДокОбъект.Ссылка;
		КонецЕсли;
		
	Исключение
		Ошибка = КраткоеПредставлениеОшибки(ИнформацияОбОшибке());
		ТекстСообщения = СтрШаблон("Не удалось записать получателей криптосообщения в документ. %1", Ошибка);
		ЗаписьЖурналаРегистрации(
			НСтр("ru = 'Электронный документооборот с контролирующими органами.ДополнитьТранспортноеСообщениеПолучателями()'"), 
			УровеньЖурналаРегистрации.Предупреждение,, ТранспортноеСообщение.Ссылка, ТекстСообщения);
	КонецПопытки;
		
КонецПроцедуры

Функция СформироватьИзвещениеОПолученииФСГС(ТранспортноеСообщение) Экспорт
	
	// проверяем, не сформировано ли уже извещение
	ИмеющиесяИзвещения = ПолучитьВложенияТранспортногоСообщения(ТранспортноеСообщение, Ложь, Перечисления.ТипыСодержимогоТранспортногоКонтейнера.ИзвещениеОПолученииДокументаФСГС);
	Если ИмеющиесяИзвещения.Количество() > 0 Тогда
		Возврат Истина;
	КонецЕсли;
	
	// определяем возможные типы документа, извещение о получении которого формируется
	ТипыДИВ = Новый Массив;
	Если ТранспортноеСообщение.Тип = Перечисления.ТипыТранспортныхСообщений.ИзвещениеПротоколВходногоКонтроляОтчетностиФСГС Тогда
		ТипыДИВ.Добавить(Перечисления.ТипыСодержимогоТранспортногоКонтейнера.УведомлениеОПриемеВОбработкуОтчетаФСГС);
		ТипыДИВ.Добавить(Перечисления.ТипыСодержимогоТранспортногоКонтейнера.УведомлениеОНесоответствииФорматуОтчетаФСГС);
		ТипыДИВ.Добавить(Перечисления.ТипыСодержимогоТранспортногоКонтейнера.УведомлениеОбУточненииОтчетаФСГС);
		ТипыДИВ.Добавить(Перечисления.ТипыСодержимогоТранспортногоКонтейнера.УведомлениеОбОтказеОтчетаФСГС);
		ТипыДИВ.Добавить(Перечисления.ТипыСодержимогоТранспортногоКонтейнера.УведомлениеОбОтклоненииОтчетаФСГС);
	ИначеЕсли ТранспортноеСообщение.Тип = Перечисления.ТипыТранспортныхСообщений.ИзвещениеПисьмоВходящееФСГС Тогда
		ТипыДИВ.Добавить(Перечисления.ТипыСодержимогоТранспортногоКонтейнера.ПисьмоФСГС);
		ТипыДИВ.Добавить(Перечисления.ТипыСодержимогоТранспортногоКонтейнера.ПриложениеПисьмаФСГС);
	КонецЕсли;
	
	// ищем ДИВ исходного сообщения
	ДИВы = ПолучитьВложенияТранспортногоСообщения(ТранспортноеСообщение.Основание, Ложь, ТипыДИВ);
	Если ДИВы.Количество() = 0 Тогда
		ТекстСообщения = НСтр("ru = 'Не удалось создать извещение о получении документа: отсутствует исходный документ в сообщении-основании.'");
		ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
		Возврат Ложь;
	КонецЕсли;
	ДИВы.Колонки.Добавить("ПодписьBase64");
	
	Для Каждого СтрДИВ Из ДИВы Цикл
		
		ИмяФайлаДИВ = СтрДИВ.ИмяФайла;
		
		// ищем подпись под ДИВ
		ПодписиСообщения = ПолучитьВложенияТранспортногоСообщения(ТранспортноеСообщение.Основание, Истина, Перечисления.ТипыСодержимогоТранспортногоКонтейнера.ЭлектронноЦифроваяПодпись);
		ПодписьПодДИВ = ПодписиСообщения.Найти(ИмяФайлаДИВ, "ЭЦПИмяПодписанногоФайла");
		Если ПодписьПодДИВ = Неопределено Тогда
			ТекстСообщения = НСтр("ru = 'Не удалось создать извещение о получении документа: отсутствует подпись под исходным документом.'");
			ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
			Возврат Ложь;
		КонецЕсли;
		
		// пишем подпись во временный файл
		ВремФайл = ПолучитьИмяВременногоФайла();
		Попытка
			ПодписьПодДИВ.Данные.Получить().Записать(ВремФайл);
		Исключение
		КонецПопытки;
		
		// формируем Base64-представление подписи
		СтрДИВ.ПодписьBase64 = ПрочитатьBase64ИзФайла(ВремФайл);
		Если НЕ ЗначениеЗаполнено(СтрДИВ.ПодписьBase64) Тогда
			// Некоторые отделения присылают пустую подпись
			// По просьбе калуги-астрал от 22.05.2017 это больше не ошибка
			СтрДИВ.ПодписьBase64 = "";
		КонецЕсли;
		ОперацииСФайламиЭДКО.УдалитьВременныйФайл(ВремФайл);
		
	КонецЦикла;
	
	// формируем имя файла извещения
	ИмяФайлаИзвещения = СгенерироватьИмяФайлаДокументаВСоставеПакета();
	
	// формируем XML извещения о получении электронного документа
	ТекстИзвещения = СформироватьXMLИзвещенияОПолученииЭлектронногоДокументаФСГС(ТранспортноеСообщение, ДИВы);
	Если НЕ ЗначениеЗаполнено(ТекстИзвещения) Тогда
		Возврат Ложь;
	КонецЕсли;
	
	// пишем XML во временный файл в кодировке windows-1251
	ФайлИзвещения = ВыгрузитьТекстВФайл(ТекстИзвещения);
	Если НЕ ЗначениеЗаполнено(ФайлИзвещения) Тогда
		Возврат Ложь;
	КонецЕсли;
	
	// загружаем файл извещения в ИБ
	ТипСодержимогоТранспортногоКонтейнера = Перечисления.ТипыСодержимогоТранспортногоКонтейнера.ИзвещениеОПолученииДокументаФСГС;
	ДобавитьСодержимоеТранспортногоКонтейнера(ТранспортноеСообщение, ТипСодержимогоТранспортногоКонтейнера, ФайлИзвещения, ИмяФайлаИзвещения, , , , , , , СгенерироватьUUID(), Перечисления.ТипыСодержимогоФайлов.xml);
	
	// удаляем временный файл с извещением
	ОперацииСФайламиЭДКО.УдалитьВременныйФайл(ФайлИзвещения);
	
	Возврат Истина;
	
КонецФункции

Функция СформироватьXMLИзвещенияОПолученииЭлектронногоДокументаФСГС(ТранспортноеСообщение, ДИВы)
	
	// выгружаем XML из файла описания архива в дерево
	ДеревоXML = ИзвлечьДеревоОписанияСведенийИзПакетаФСГС(ТранспортноеСообщение.Основание);
	Если ДеревоXML = Неопределено Тогда
		Возврат Ложь;
	КонецЕсли;
	
	// ищем атрибут "идентификаторДокументооборота"
	Узлы_идентификаторДокументооборота = ДеревоXML.Строки.НайтиСтроки(Новый Структура("Имя, Тип", "идентификаторДокументооборота", "А"), Истина);
	Если Узлы_идентификаторДокументооборота.Количество() = 0 Тогда
		Возврат Ложь;
	КонецЕсли;
	
	// возвращаем первое найденное значение атрибута
	ИдентификаторДокументооборота = СокрЛП(Узлы_идентификаторДокументооборота[0].Значение);
	
	// создаем XML-дерево извещения
	ИзвещениеДеревоXML = СоздатьДеревоXML();
	
	// создаем узел "Посылка"
	УзелПосылка = ДобавитьУзелXML(ИзвещениеДеревоXML, "посылка");
	
	// создаем узел "Отправитель"
	УзелОтправитель = ДобавитьУзелXML(УзелПосылка, "отправитель");
	
	// извлекаем данные для заполнения узла "Отправитель"
	Узлы_Отправитель = ДеревоXML.Строки.НайтиСтроки(Новый Структура("Имя, Тип", "отправитель", "Э"), Истина);
	Если Узлы_Отправитель.Количество() = 0 Тогда
		Возврат Ложь;
	КонецЕсли;
	Узлы_идентификаторСубъекта_Отправитель = Узлы_Отправитель[0].Строки.НайтиСтроки(Новый Структура("Имя, Тип", "идентификаторСубъекта", "А"));
	Если Узлы_идентификаторСубъекта_Отправитель.Количество() = 0 Тогда
		Возврат Ложь;
	КонецЕсли;
	Узлы_типСубъекта_Отправитель = Узлы_Отправитель[0].Строки.НайтиСтроки(Новый Структура("Имя, Тип", "типСубъекта", "А"));
	Если Узлы_типСубъекта_Отправитель.Количество() = 0 Тогда
		Возврат Ложь;
	КонецЕсли;
	ИдентификаторСубъектаОтправитель = СокрЛП(Узлы_идентификаторСубъекта_Отправитель[0].Значение);
	ТипСубъектаОтправитель = СокрЛП(Узлы_типСубъекта_Отправитель[0].Значение);
	
	// создаем атрибуты и элементы узла "Отправитель"
	ДобавитьУзелXML(УзелОтправитель, "идентификатор",, ИдентификаторСубъектаОтправитель);
	ДобавитьУзелXML(УзелОтправитель, "название",, ИдентификаторСубъектаОтправитель);
	ДобавитьУзелXML(УзелОтправитель, "натуральныйИдентификатор",, ИдентификаторСубъектаОтправитель);
	ДобавитьУзелXML(УзелОтправитель, "тип",, ТипСубъектаОтправитель);
	
	// создаем узел "Получатель"
	УзелПолучатель = ДобавитьУзелXML(УзелПосылка, "получатель");
	
	// извлекаем данные для заполнения узла "Получатель"
	Узлы_Получатель = ДеревоXML.Строки.НайтиСтроки(Новый Структура("Имя, Тип", "получатель", "Э"), Истина);
	Если Узлы_Получатель.Количество() = 0 Тогда
		Возврат Ложь;
	КонецЕсли;
	Узлы_идентификаторСубъекта_Получатель = Узлы_Получатель[0].Строки.НайтиСтроки(Новый Структура("Имя, Тип", "идентификаторСубъекта", "А"));
	Если Узлы_идентификаторСубъекта_Получатель.Количество() = 0 Тогда
		Возврат Ложь;
	КонецЕсли;
	Узлы_типСубъекта_Получатель = Узлы_Получатель[0].Строки.НайтиСтроки(Новый Структура("Имя, Тип", "типСубъекта", "А"));
	Если Узлы_типСубъекта_Получатель.Количество() = 0 Тогда
		Возврат Ложь;
	КонецЕсли;
	ИдентификаторСубъектаПолучатель = СокрЛП(Узлы_идентификаторСубъекта_Получатель[0].Значение);
	ТипСубъектаПолучатель = СокрЛП(Узлы_типСубъекта_Получатель[0].Значение);
	
	// создаем атрибуты и элементы узла "Получатель"
	ДобавитьУзелXML(УзелПолучатель, "идентификатор",, ИдентификаторСубъектаПолучатель);
	ДобавитьУзелXML(УзелПолучатель, "название",, СокрЛП(ТранспортноеСообщение.Основание.Получатель));
	ДобавитьУзелXML(УзелПолучатель, "натуральныйИдентификатор",, НатуральныйИдентификатор(ИдентификаторСубъектаПолучатель));
	ДобавитьУзелXML(УзелПолучатель, "тип",, ТипСубъектаПолучатель);
	
	// создаем узел "Документы"
	УзелДокументы = ДобавитьУзелXML(УзелПосылка, "документы");
	ДобавитьУзелXML(УзелДокументы, "идентификаторДокументооборота",, ИдентификаторДокументооборота);
	
	Для Каждого СтрДИВ Из ДИВы Цикл
		
		// создаем узел "Документ"
		УзелДокумент = ДобавитьУзелXML(УзелДокументы, "документ");
		
		// создаем атрибут "идентификаторДокумента"
		ДобавитьУзелXML(УзелДокумент, "идентификаторДокумента", Истина, СтрДИВ.Идентификатор);
		
		// находим узел из описания архива сообщения-основания по идентификатору документа
		Узлы_ИдентификаторДокумента_Основание = ДеревоXML.Строки.НайтиСтроки(Новый Структура("Имя, Тип, Значение", "идентификаторДокумента", "А", СокрЛП(СтрДИВ.Идентификатор)), Истина);
		Если Узлы_ИдентификаторДокумента_Основание.Количество() = 0 Тогда
			Возврат Ложь;
		КонецЕсли;
		УзелДокументОснование = Узлы_ИдентификаторДокумента_Основание[0].Родитель;
		Узлы_ТипДокументаОснования = УзелДокументОснование.Строки.НайтиСтроки(Новый Структура("Имя, Тип", "типДокумента", "А"));
		Если Узлы_ТипДокументаОснования.Количество() = 0 Тогда
			Возврат Ложь;
		КонецЕсли;
		ДобавитьУзелXML(УзелДокумент, "типДокумента", Истина, СокрЛП(Узлы_ТипДокументаОснования[0].Значение));
		
		// добавляем подпись под ДИВ
		УзелПодпись = ДобавитьУзелXML(УзелДокумент, "подпись", , СтрДИВ.ПодписьBase64);
		ДобавитьУзелXML(УзелПодпись, "роль", Истина, "органФСГС");
		
	КонецЦикла;
	
	// формируем XML-строку на основе дерева
	ТекстXML = ВыгрузитьДеревоВXML_ПФР(ИзвещениеДеревоXML, "извещение");
	
	Возврат ТекстXML;
	
КонецФункции

Функция ИзвлечьДеревоОписанияСведенийИзПакетаФСГС(ОбъектСообщение)
	
	// выгружаем контейнер во временный файл
	ИмяФайлаКонтейнера = ПолучитьИмяВременногоФайла();
	Контейнер = ПолучитьТранспортныйКонтейнер(ОбъектСообщение.Ссылка, Истина);
	Если НЕ ЗначениеЗаполнено(Контейнер) ИЛИ Контейнер.Количество() = 0 Тогда
		Возврат Неопределено;
	КонецЕсли;
	ДанныеКонтейнера = Контейнер[0].Данные.Получить();
	Если ДанныеКонтейнера = Неопределено Тогда
		Возврат Неопределено;
	КонецЕсли;
	ДанныеКонтейнера.Записать(ИмяФайлаКонтейнера);
	
	// распаковываем файл описания сведений архива
	ИмяФайлаОписанияСведений = "packageDescription.xml";
	ЧтениеЗИП = Новый ЧтениеZipФайла(ИмяФайлаКонтейнера);
	ЭлементОписаниеСведений = ЧтениеЗИП.Элементы.Найти(ИмяФайлаОписанияСведений);
	Если ЭлементОписаниеСведений = Неопределено Тогда
		Возврат Неопределено;
	КонецЕсли;
	
	КаталогРаспаковки = ОперацииСФайламиЭДКО.СоздатьВременныйКаталог();
	ЧтениеЗИП.Извлечь(ЭлементОписаниеСведений, КаталогРаспаковки, РежимВосстановленияПутейФайловZIP.НеВосстанавливать);
	
	// читаем XML
	ТекстXML = ПрочитатьТекстИзФайла(КаталогРаспаковки + ИмяФайлаОписанияСведений, , Истина);
	Если НЕ ЗначениеЗаполнено(ТекстXML) Тогда
		ЧтениеЗИП.Закрыть();
		ОперацииСФайламиЭДКО.УдалитьВременныйФайл(ИмяФайлаКонтейнера);
		ОперацииСФайламиЭДКО.УдалитьВременныйФайл(КаталогРаспаковки);
		Возврат Неопределено;
	КонецЕсли;
	
	// загружаем XML в дерево
	ДеревоXML = ЗагрузитьСтрокуXMLВДеревоЗначений(ТекстXML);
	Если НЕ ЗначениеЗаполнено(ДеревоXML) Тогда
		ЧтениеЗИП.Закрыть();
		ОперацииСФайламиЭДКО.УдалитьВременныйФайл(ИмяФайлаКонтейнера);
		ОперацииСФайламиЭДКО.УдалитьВременныйФайл(КаталогРаспаковки);
		Возврат Неопределено;
	КонецЕсли;
	
	// в случае уведомления об ошибке, источником для получения имени файла отчета должен служить
	// документ описаниеОшибочногоПакета, на который ссылается описание пакета
	Если ОбъектСообщение.Тип = Перечисления.ТипыТранспортныхСообщений.УведомлениеОбОшибкеФСГС
	ИЛИ ОбъектСообщение.Тип = Перечисления.ТипыТранспортныхСообщений.КритическаяОшибка Тогда
		
		// находим элемент описаниеОшибочногоПакета
		Узлы_ТипДокументаОписаниеОшибочногоПакета = ДеревоXML.Строки.НайтиСтроки(Новый Структура("Имя, Тип, Значение", "типДокумента", "А", "описаниеОшибочногоПакета"), Истина);
		Если Узлы_ТипДокументаОписаниеОшибочногоПакета.Количество() > 0 Тогда
			
			Узел_ОписаниеОшибочногоПакета = Узлы_ТипДокументаОписаниеОшибочногоПакета[0].Родитель;
			
			// ищем в подчинении элемент Содержимое
			УзлыСодержимое = Узел_ОписаниеОшибочногоПакета.Строки.НайтиСтроки(Новый Структура("Имя, Тип", "содержимое", "Э"));
			Если УзлыСодержимое.Количество() > 0 Тогда
				
				// ищем в подчинении атрибут имяФайла
				УзелСодержимое = УзлыСодержимое[0];
				УзлыИмяФайла = УзелСодержимое.Строки.НайтиСтроки(Новый Структура("Имя, Тип", "имяФайла", "А"));
				Если УзлыИмяФайла.Количество() > 0 Тогда
					
					УзелИмяФайла = УзлыИмяФайла[0];
					ИмяФайлаОписанияОшибочногоПакета = СокрЛП(УзелИмяФайла.Значение);
					
					ЭлементОписаниеСведений = ЧтениеЗИП.Элементы.Найти(ИмяФайлаОписанияОшибочногоПакета);
					Если ЭлементОписаниеСведений <> Неопределено Тогда
						
						АтрибутСжат = Узел_ОписаниеОшибочногоПакета.Строки.НайтиСтроки(Новый Структура("Имя, Тип", "сжат", "А"));
						Если АтрибутСжат.Количество() > 0 Тогда
							ФайлСжат = АтрибутСжат[0].Значение = "true";
						Иначе
							ФайлСжат = Истина;
						КонецЕсли;
						
						ЧтениеЗИП.Извлечь(ЭлементОписаниеСведений, КаталогРаспаковки, РежимВосстановленияПутейФайловZIP.НеВосстанавливать);
						
						ПолноеИмяФайлаДокумента = КаталогРаспаковки + ИмяФайлаОписанияОшибочногоПакета;
						Если ФайлСжат Тогда
							// распаковываем
							ПолноеИмяФайлаДокумента = РазархивироватьФайл(ПолноеИмяФайлаДокумента);
						КонецЕсли;
						
						Если ПолноеИмяФайлаДокумента <> Неопределено Тогда
						
							// читаем XML
							ТекстXML = ПрочитатьТекстИзФайла(ПолноеИмяФайлаДокумента, , Истина);
							Если ЗначениеЗаполнено(ТекстXML) Тогда
								
								// загружаем XML в дерево
								ДеревоЗначенийИзСтрокиXML = ЗагрузитьСтрокуXMLВДеревоЗначений(ТекстXML);
								
								// подменяем дерево описания сведений
								Если ДеревоЗначенийИзСтрокиXML <> Неопределено Тогда
									ДеревоXML = ДеревоЗначенийИзСтрокиXML;
								КонецЕсли;
								
							КонецЕсли;
							
							ОперацииСФайламиЭДКО.УдалитьВременныйФайл(ПолноеИмяФайлаДокумента);
							
						КонецЕсли;
						
					КонецЕсли;
					
				КонецЕсли;
				
			КонецЕсли;
			
		КонецЕсли;
		
	КонецЕсли;
	
	// закрываем архив и удаляем временный файл контейнера
	ЧтениеЗИП.Закрыть();
	ОперацииСФайламиЭДКО.УдалитьВременныйФайл(ИмяФайлаКонтейнера);
	ОперацииСФайламиЭДКО.УдалитьВременныйФайл(КаталогРаспаковки);
	
	// возвращаем дерево из файла описания сведений
	Возврат ДеревоXML;
	
КонецФункции

Функция НатуральныйИдентификатор(ИдентификаторСубъекта)
	
	ДлинаИдентификатора = СтрДлина(ИдентификаторСубъекта);
	Для Инд = 1 По ДлинаИдентификатора Цикл
		ТекИнд = ДлинаИдентификатора - Инд + 1;
		Если Сред(ИдентификаторСубъекта, ТекИнд, 1) = "." Тогда
			Возврат Сред(ИдентификаторСубъекта, ТекИнд + 1);
		КонецЕсли;
	КонецЦикла;
	
	Возврат ИдентификаторСубъекта;
	
КонецФункции

Функция ОпределитьПолучателяТранспортногоКонтейнера(Сообщение) Экспорт
	
	ТипСообщения = Сообщение.Тип;
	
	// только следующие типы сообщений шифруются на сервер документооборота
	Если ТипСообщения = Перечисления.ТипыТранспортныхСообщений.ИзвещениеПодтверждениеНП
		ИЛИ ТипСообщения = Перечисления.ТипыТранспортныхСообщений.ЗапросНаВыпискуЕРГЮЛ_ЕГРИП Тогда
		Возврат Сообщение.УчетнаяЗапись.СерверДокументооборота;
		
	// все сообщения документооборота с ПФР шифруются на соответствующий орган ПФР
	ИначеЕсли ТипСообщения = Перечисления.ТипыТранспортныхСообщений.ПервичноеСообщениеСодержащееОтчетностьПФР
	ИЛИ ТипСообщения = Перечисления.ТипыТранспортныхСообщений.ПротоколКвитанцияПФР
	ИЛИ ТипСообщения = Перечисления.ТипыТранспортныхСообщений.ПисьмоИсходящееПФР
	ИЛИ ТипСообщения = Перечисления.ТипыТранспортныхСообщений.ПисьмоВходящееКвитанцияПФР
	ИЛИ ТипСообщения = Перечисления.ТипыТранспортныхСообщений.ПервичноеСообщениеСодержащееЗапросПФР
	ИЛИ ТипСообщения = Перечисления.ТипыТранспортныхСообщений.ОтветНаЗапросКвитанцияПФР Тогда
		Возврат Сообщение.Получатель;
	// все сообщения документооборота с ТОГС шифруются на соответствующий орган ТОГС
ИначеЕсли ТипСообщения = Перечисления.ТипыТранспортныхСообщений.ПервичноеСообщениеСодержащееОтчетностьФСГС
	ИЛИ ТипСообщения = Перечисления.ТипыТранспортныхСообщений.ИзвещениеПодтверждениеДатыОтправкиФСГС
	ИЛИ ТипСообщения = Перечисления.ТипыТранспортныхСообщений.ИзвещениеПротоколВходногоКонтроляОтчетностиФСГС
	ИЛИ ТипСообщения = Перечисления.ТипыТранспортныхСообщений.ПисьмоИсходящееФСГС
	ИЛИ ТипСообщения = Перечисления.ТипыТранспортныхСообщений.ИзвещениеПисьмоВходящееФСГС Тогда
		Возврат Сообщение.Получатель;
	// во всех остальных случаях (а это только обмен с ФНС)
	// получатель зависит от признака обмена напрямую
	ИначеЕсли НЕ Сообщение.УчетнаяЗапись.ОбменНапрямую Тогда
		
		// при обмене через СОС для крупнейших налогоплательщиков шифруем на налоговый орган - посредник
		Если Сообщение.Отправитель.КрупнейшийНалогоплательщик И ЗначениеЗаполнено(Сообщение.Отправитель.КодНалоговогоОрганаПолучателя)
			И НЕ ИгнорироватьНалоговыйОрганПосредника(Сообщение.ЦиклОбмена.Наименование) Тогда
			Возврат Сообщение.УчетнаяЗапись.НалоговыйОрган;
		Иначе
			Возврат Сообщение.ЦиклОбмена.ВнешняяОрганизация;
		КонецЕсли;
		
	Иначе
		Возврат Сообщение.УчетнаяЗапись.НалоговыйОрган;
	КонецЕсли;
	
КонецФункции

Функция НовыеДокументыПакета()

	// Внимание!
	// ---------
	// В Контейнере есть реквизит формы ДокументыПакета, который по структуре совпадает с таблицей Результат,
	// и используется в интерактивном режиме работы.
	// При изменениях необходимо синхронно менять в обоих местах.

	ОписаниеТипаСтрока = ОбщегоНазначения.ОписаниеТипаСтрока(0);

	Результат = Новый ТаблицаЗначений;
	
	Результат.Колонки.Добавить("ИмяФайла",             ОписаниеТипаСтрока);
	Результат.Колонки.Добавить("АдресФайла",           ОписаниеТипаСтрока);
	Результат.Колонки.Добавить("РазмерФайлаДокумента", ОписаниеТипаСтрока);
	Результат.Колонки.Добавить("ТипДокумента",         Новый ОписаниеТипов("ПеречислениеСсылка.ТипыСодержимогоТранспортногоКонтейнера"));
	Результат.Колонки.Добавить("Идентификатор",        ОписаниеТипаСтрока);
	Результат.Колонки.Добавить("ИмяФайлаПодписи",      ОписаниеТипаСтрока);
	Результат.Колонки.Добавить("АдресФайлаПодписи",    ОписаниеТипаСтрока);
	Результат.Колонки.Добавить("СтатусПроверкиПодписи",Новый ОписаниеТипов("ПеречислениеСсылка.СтатусыПроверкиЭЦП"));
	Результат.Колонки.Добавить("РазмерФайлаПодписи",   ОписаниеТипаСтрока);
	Результат.Колонки.Добавить("ТипСодержимого",       ОписаниеТипаСтрока);
	Результат.Колонки.Добавить("ОтпечатокСертификатаПодписи", ОписаниеТипаСтрока);
	Результат.Колонки.Добавить("ДокументЗашифрован",   Новый ОписаниеТипов("Булево"));
	Результат.Колонки.Добавить("ДокументСжат",         Новый ОписаниеТипов("Булево"));
	Результат.Колонки.Добавить("АлгоритмФайла",        ОписаниеТипаСтрока);
	Результат.Колонки.Добавить("АлгоритмФайлаПодписи", ОписаниеТипаСтрока);
	Результат.Колонки.Добавить("РезультатПроверкиПодписи", ОписаниеТипаСтрока);
	
	Возврат Результат;

КонецФункции

Функция НоваяТаблицаРаспакованныхФайлов() 

	// Внимание!
	// ---------
	// В Контейнере есть реквизит формы ТаблицаРаспакованныхФайлов, который по структуре совпадает с таблицей Результат,
	// и используется в интерактивном режиме работы.
	// При изменениях необходимо синхронно менять в обоих местах.

	ОписаниеТипаСтрока = ОбщегоНазначения.ОписаниеТипаСтрока(0);

	Результат = Новый ТаблицаЗначений;
	
	Результат.Колонки.Добавить("Адрес", ОписаниеТипаСтрока);
	Результат.Колонки.Добавить("Имя",   ОписаниеТипаСтрока);
	
	Возврат Результат;

КонецФункции

Функция РаспаковатьПакет(ФайлПакета, ТипСообщения = Неопределено)
	
	КаталогРаспаковки = КаталогВременныхФайлов();
	КаталогРаспаковки = ОбщегоНазначенияКлиентСервер.ДобавитьКонечныйРазделительПути(КаталогРаспаковки);
	КаталогРаспаковки = КаталогРаспаковки + Строка(Новый УникальныйИдентификатор);
	КаталогРаспаковки = ОбщегоНазначенияКлиентСервер.ДобавитьКонечныйРазделительПути(КаталогРаспаковки);
	
	СоздатьКаталог(КаталогРаспаковки);
	
	Попытка
		ЧтениеЗИП = Новый ЧтениеZipФайла(ФайлПакета);
		Если ТипСообщения = "ПФР" Тогда
			ИмяФайлаОписанияСведений = "packageDescription.xml";
			ЭлементОписаниеСведений = ЧтениеЗИП.Элементы.Найти(ИмяФайлаОписанияСведений);
			Если ЭлементОписаниеСведений = Неопределено Тогда
				// обход ошибки платформы для архива zip с кодировкой имен файлов cp866, сформированного в Linux
				ЧтениеЗИП.Закрыть();
				МассивОписанийИДанных = ОбщегоНазначенияЭДКО.МассивОписанийИДанныхZipФайла(ФайлПакета);
				Если МассивОписанийИДанных <> Неопределено Тогда
					ОперацииСФайламиЭДКО.УдалитьВременныйФайл(ФайлПакета);
					ФайлПакета = ФайлПакета + ".zip";
					ОбщегоНазначенияЭДКО.СформироватьZip(МассивОписанийИДанных, ФайлПакета);
				КонецЕсли;
				ЧтениеЗИП = Новый ЧтениеZipФайла(ФайлПакета);
			КонецЕсли;
		КонецЕсли;
		ЧтениеЗИП.ИзвлечьВсе(КаталогРаспаковки, РежимВосстановленияПутейФайловZIP.НеВосстанавливать);
		ЧтениеЗИП.Закрыть();
	Исключение
		ИнформацияОбОшибке = ИнформацияОбОшибке();
		ЗаписьЖурналаРегистрации(
			НСтр("ru = 'Электронный документооборот с контролирующими органами. Распаковка пакета'", ОбщегоНазначения.КодОсновногоЯзыка()),
			УровеньЖурналаРегистрации.Ошибка,,,
			ПодробноеПредставлениеОшибки(ИнформацияОбОшибке));
		ОперацииСФайламиЭДКО.УдалитьВременныйФайл(КаталогРаспаковки);
		Возврат Неопределено;
	КонецПопытки;
	
	Возврат КаталогРаспаковки;
	
КонецФункции

// Добавляет перед расшифровкой отметку времени в регистр
// И удаляет её после успешного завершения расшифровки
Процедура ЗафиксироватьПопыткуРасшифровки(ТранспортноеСообщение, Очистить = Ложь) Экспорт
	НаборЗаписей = РегистрыСведений.ПоследняяПопыткаРасшифровкиТранспортногоСообщения.СоздатьНаборЗаписей();
	НаборЗаписей.Отбор.ТранспортноеСообщение.Установить(ТранспортноеСообщение);	
	
	Если Не Очистить Тогда 
		НоваяЗапись = НаборЗаписей.Добавить();
		НоваяЗапись.ДатаПопытки = ТекущаяДатаСеанса();
		НоваяЗапись.ТранспортноеСообщение = ТранспортноеСообщение;
	КонецЕсли;
	
	НаборЗаписей.Записать(Истина);
КонецПроцедуры

Функция ОбработатьСодержимоеТранспортногоКонтейнера(Объект, ДокументыПакета, ТаблицаРаспакованныхФайлов) Экспорт

	ДокументыПакета.Очистить();
	ТаблицаРаспакованныхФайлов.Очистить();
	
	Результат = Новый Структура;
	
	Если Не ТранспортноеСообщениеОбрабатывается(Объект.Ссылка) Тогда 
		Возврат Ложь;
	КонецЕсли;
	
	ЗафиксироватьПопыткуРасшифровки(Объект.Ссылка);
	
	// получаем расшифровываемый контейнер
	ЗначениеДанныхПриОшибкеЧтения = "Исключение при получении присоединенного файла";
	ТранспортныеКонтейнеры = ПолучитьТранспортныйКонтейнер(Объект.Ссылка, Истина,, ЗначениеДанныхПриОшибкеЧтения);
	Если ТранспортныеКонтейнеры.Количество() = 0 Тогда
		Возврат Ложь;
	КонецЕсли;
	ТранспортныйКонтейнер = ТранспортныеКонтейнеры[0];
	
	// выгружаем контейнер во временный файл
	ТекстСообщения = "";
	ВремФайл = ПолучитьИмяВременногоФайла();
	Попытка
		ТранспортныйКонтейнерДвДанные = ТранспортныйКонтейнер.Данные.Получить();
		Если ТипЗнч(ТранспортныйКонтейнерДвДанные) = Тип("Строка")
			И ТранспортныйКонтейнерДвДанные = ЗначениеДанныхПриОшибкеЧтения Тогда
			ТекстСообщения = НСтр("ru = 'Ошибка при чтении присоединенного файла транспортного контейнера. Проверьте доступность томов на сетевом диске или в хранилище.'");
		ИначеЕсли ТранспортныйКонтейнерДвДанные = Неопределено Тогда
			ТекстСообщения = НСтр("ru = 'Пустое содержимое транспортного контейнера. Возможно, повреждена база.'");
		Иначе
			ТранспортныйКонтейнерДвДанные.Записать(ВремФайл);
		КонецЕсли;
	Исключение
		ТекстСообщения = СтроковыеФункцииКлиентСервер.ПодставитьПараметрыВСтроку(НСтр("ru = 'Ошибка сохранения во временный файл:%1'"), Символы.ПС + ОписаниеОшибки());
	КонецПопытки;
	Если ЗначениеЗаполнено(ТекстСообщения) Тогда
		ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения,,,,, НовыйКонтекстРасшифровки(Объект));
		Возврат Ложь;
	КонецЕсли;
	
	// сообщения ПФР
	Если Объект.Тип = Перечисления.ТипыТранспортныхСообщений.ПодтверждениеПолученияОтчетностиПФР
	ИЛИ Объект.Тип = Перечисления.ТипыТранспортныхСообщений.ПротоколПФР
	ИЛИ Объект.Тип = Перечисления.ТипыТранспортныхСообщений.ПисьмоВходящееПФР
	ИЛИ Объект.Тип = Перечисления.ТипыТранспортныхСообщений.ПисьмоИсходящееКвитанцияПФР
	ИЛИ Объект.Тип = Перечисления.ТипыТранспортныхСообщений.ПодтверждениеПолученияЗапросаПФР
	ИЛИ Объект.Тип = Перечисления.ТипыТранспортныхСообщений.ОтветНаЗапросПФР
	ИЛИ Объект.Тип = Перечисления.ТипыТранспортныхСообщений.УведомлениеОбОшибкеПФР Тогда
	
		Результат.Вставить("ТипСообщения", "ПФР");
	// сообщения ФСГС
	ИначеЕсли Объект.Тип = Перечисления.ТипыТранспортныхСообщений.ИзвещениеОПолученииОтчетностиФСГС
	ИЛИ Объект.Тип = Перечисления.ТипыТранспортныхСообщений.ПротоколВходногоКонтроляОтчетностиФСГС
	ИЛИ Объект.Тип = Перечисления.ТипыТранспортныхСообщений.ПодтверждениеДатыОтправкиФСГС
	ИЛИ Объект.Тип = Перечисления.ТипыТранспортныхСообщений.ПисьмоВходящееФСГС
	ИЛИ Объект.Тип = Перечисления.ТипыТранспортныхСообщений.РассылкаФСГС
	ИЛИ Объект.Тип = Перечисления.ТипыТранспортныхСообщений.РассылкаШаблоновФСГС
	ИЛИ Объект.Тип = Перечисления.ТипыТранспортныхСообщений.ИзвещениеПисьмоИсходящееФСГС
	ИЛИ Объект.Тип = Перечисления.ТипыТранспортныхСообщений.УведомлениеОбОшибкеФСГС Тогда
		Результат.Вставить("ТипСообщения", "ФСГС");

	// сообщения ФНС 534
	ИначеЕсли Объект.Тип = Перечисления.ТипыТранспортныхСообщений.ИзвещениеДекларацияНО
	ИЛИ Объект.Тип = Перечисления.ТипыТранспортныхСообщений.РезультатПриемаДекларацияНО
	ИЛИ Объект.Тип = Перечисления.ТипыТранспортныхСообщений.РезультатОбработкиДекларацияНО
	ИЛИ Объект.Тип = Перечисления.ТипыТранспортныхСообщений.ПодтверждениеДекларацияНО
	ИЛИ Объект.Тип = Перечисления.ТипыТранспортныхСообщений.ПодтверждениеФорма2НДФЛНО
	ИЛИ Объект.Тип = Перечисления.ТипыТранспортныхСообщений.ИзвещениеФорма2НДФЛНО
	ИЛИ Объект.Тип = Перечисления.ТипыТранспортныхСообщений.РезультатПриемаФорма2НДФЛНО
	ИЛИ Объект.Тип = Перечисления.ТипыТранспортныхСообщений.ИзвещениеОбращениеНО
	ИЛИ Объект.Тип = Перечисления.ТипыТранспортныхСообщений.РезультатПриемаОбращениеНО
	ИЛИ Объект.Тип = Перечисления.ТипыТранспортныхСообщений.ПодтверждениеОбращениеНО
	ИЛИ Объект.Тип = Перечисления.ТипыТранспортныхСообщений.УведомлениеОбОшибкеФНС
	ИЛИ Объект.Тип = Перечисления.ТипыТранспортныхСообщений.ПодтверждениеЗапросНО
	ИЛИ Объект.Тип = Перечисления.ТипыТранспортныхСообщений.ИзвещениеЗапросНО
	ИЛИ Объект.Тип = Перечисления.ТипыТранспортныхСообщений.РезультатПриемаЗапросНО
	ИЛИ Объект.Тип = Перечисления.ТипыТранспортныхСообщений.РезультатОбработкиЗапросНО
	ИЛИ Объект.Тип = Перечисления.ТипыТранспортныхСообщений.ПисьмоНО
	ИЛИ Объект.Тип = Перечисления.ТипыТранспортныхСообщений.РассылкаНО 
	
	ИЛИ Объект.Тип = Перечисления.ТипыТранспортныхСообщений.ИзвещениеПредставлениеНО
	ИЛИ Объект.Тип = Перечисления.ТипыТранспортныхСообщений.РезультатПриемаПредставлениеНО
	ИЛИ Объект.Тип = Перечисления.ТипыТранспортныхСообщений.ПодтверждениеПредставлениеНО
	
	ИЛИ Объект.Тип = Перечисления.ТипыТранспортныхСообщений.ПодтверждениеЗаявлениеНО
	ИЛИ Объект.Тип = Перечисления.ТипыТранспортныхСообщений.ИзвещениеЗаявлениеНО
	ИЛИ Объект.Тип = Перечисления.ТипыТранспортныхСообщений.РезультатПриемаЗаявлениеНО
	
	ИЛИ Объект.Тип = Перечисления.ТипыТранспортныхСообщений.РезультатОбработкиЗаявлениеНО
	
	ИЛИ Объект.Тип = Перечисления.ТипыТранспортныхСообщений.РезультатОбработкиЗаявлениеРФНО
	ИЛИ Объект.Тип = Перечисления.ТипыТранспортныхСообщений.СообщениеОбОтзывеЗаявлениеРФНО
	ИЛИ Объект.Тип = Перечисления.ТипыТранспортныхСообщений.РезультатОбработкиЗаявлениеТСНО
	
	ИЛИ Объект.Тип = Перечисления.ТипыТранспортныхСообщений.ДокументНО
	ИЛИ Объект.Тип = Перечисления.ТипыТранспортныхСообщений.ИзвещениеРезультатПриемаНО
	ИЛИ Объект.Тип = Перечисления.ТипыТранспортныхСообщений.РезультатОбработкиЗапросаНаВыпискуЕРГЮЛ_ЕГРИП Тогда
		Результат.Вставить("ТипСообщения", "ФНС_534");
	//ФНС 141
	Иначе // тип "ФНС_141" больше не используется
		Возврат Ложь;
	КонецЕсли;
	
	ТипСообщения = Неопределено;
	Результат.Свойство("ТипСообщения", ТипСообщения);
	
	// распаковываем пакет
	КаталогФайловПакета = РаспаковатьПакет(ВремФайл, ТипСообщения);
	// помещаем распакованные файлы во временное хранилище
	МассивРаспакованныхФайлов = НайтиФайлы(КаталогФайловПакета, "*");
	Попытка
		Для Каждого ЭлементМассива Из МассивРаспакованныхФайлов Цикл
			ГУИД = Новый УникальныйИдентификатор;
			Адрес = ПоместитьВоВременноеХранилище(Новый ДвоичныеДанные(ЭлементМассива.ПолноеИмя), ГУИД);
			НоваяСтрока = ТаблицаРаспакованныхФайлов.Добавить();
			НоваяСтрока.Адрес = Адрес;
			НоваяСтрока.Имя = ЭлементМассива.Имя;
		КонецЦикла;
	Исключение
		ЗаписьЖурналаРегистрации(
			НСтр("ru = 'Электронный документооборот с контролирующими органами. Чтение ZIP'", ОбщегоНазначения.КодОсновногоЯзыка()),
			УровеньЖурналаРегистрации.Ошибка,,,
			ПодробноеПредставлениеОшибки(ИнформацияОбОшибке()));
		Возврат Ложь;
	КонецПопытки;
	
	ФорматДокументооборота = Неопределено;
	ФайлОписанияСведений = НайтиФайлы(КаталогФайловПакета, "packageDescription.xml");
	Если ФайлОписанияСведений.Количество() = 0 Тогда
		Если ТипСообщения = "ПФР" Тогда
			ФайлОписанияСведений = НайтиФайлы(КаталогФайловПакета, "ПФР_*_ОСП_*.xml");
			Если ФайлОписанияСведений.Количество() <> 0 Тогда
				ФорматДокументооборота = Перечисления.ФорматыДокументооборотаСПФР.ЭДОК;
			КонецЕсли;
		КонецЕсли;
		
		Если ФайлОписанияСведений.Количество() = 0 Тогда
			ТекстСообщения = ?(ТипСообщения = "ПФР",
				НСтр("ru = 'Ошибка распаковки пакета: не найден файл описания пакета (ПФР_*_ОСП_*.xml или packageDescription.xml).'"),
				НСтр("ru = 'Ошибка распаковки пакета: не найден файл описания пакета (packageDescription.xml).'"));
			ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения,,,,, НовыйКонтекстРасшифровки(Объект));
			Возврат Ложь;
		КонецЕсли;
	КонецЕсли;
	
	ИмяФайлаОписанияСведений = ФайлОписанияСведений[0].ПолноеИмя;
	КороткоеИмяФайлаОписанияСведений = ОбщегоНазначенияКлиентСервер.РазложитьПолноеИмяФайла(ИмяФайлаОписанияСведений).Имя;
	
	// считываем файл описания пакета
	ТекстXML = Неопределено;
	ДеревоОписанияСведений = ЗагрузитьXMLВДеревоЗначений(ИмяФайлаОписанияСведений, ТекстXML);
	Если ДеревоОписанияСведений = Неопределено Тогда
		ОперацииСФайламиЭДКО.УдалитьВременныйФайл(ИмяФайлаОписанияСведений);
		Возврат Ложь;
	КонецЕсли;
	
	УбратьСообщениеИзОбработки = Неопределено;
	
	Если ТипСообщения = "ПФР" И ФорматДокументооборота = Перечисления.ФорматыДокументооборотаСПФР.ЭДОК Тогда
		УзелОСП = НайтиСтрокиДереваЗначений(ДеревоОписанияСведений,,, "ОСП", "Э");
		Если НЕ ЗначениеЗаполнено(УзелОСП) Тогда
			ОперацииСФайламиЭДКО.УдалитьВременныйФайл(ИмяФайлаОписанияСведений);
			Возврат Ложь;
		КонецЕсли;
		
		УзелЗашифровано = НайтиСтрокиДереваЗначений(УзелОСП,,, "Зашифровано", "Э", Ложь);
		ДокументыЗашифрованы = ?(ЗначениеЗаполнено(УзелЗашифровано), БулевоПоСтроке(УзелЗашифровано.Значение), Неопределено);
		
		УзлыДокумент = Новый Массив;
		НайтиСтрокиДереваЗначений(УзелОСП,, УзлыДокумент, "Документ", "Э", Ложь);
		УзелДополнительныеДокументы = НайтиСтрокиДереваЗначений(УзелОСП,,, "ДополнительныеДокументы", "Э", Ложь);
		УзлыДополнительныйДокумент = Неопределено;
		Если ЗначениеЗаполнено(УзелДополнительныеДокументы) Тогда
			УзлыДополнительныйДокумент = Новый Массив;
			НайтиСтрокиДереваЗначений(УзелДополнительныеДокументы,, УзлыДополнительныйДокумент, "Документ", "Э", Ложь);
		КонецЕсли;
		
		Для НомерУзловДокументов = 1 По ?(УзлыДополнительныйДокумент = Неопределено, 1, 2) Цикл
			ЭтоДополнительныйДокумент = (НомерУзловДокументов = 2);
			Если ЭтоДополнительныйДокумент Тогда
				УзлыДокумент = УзлыДополнительныйДокумент;
			КонецЕсли;
			
			Для каждого УзелДокумент Из УзлыДокумент Цикл
				УзелКод = НайтиСтрокиДереваЗначений(УзелДокумент,,, "Код", "Э", Ложь);
				КодДокумента = ?(ЗначениеЗаполнено(УзелКод), СокрЛП(УзелКод.Значение), "");
				
				УзелФайл = НайтиСтрокиДереваЗначений(УзелДокумент,,, "Файл", "Э", Ложь);
				ИмяФайлаДокумента = ?(ЗначениеЗаполнено(УзелФайл), СокрЛП(УзелФайл.Значение), "");
				Если (ВРег(КодДокумента) = "УОД" ИЛИ ВРег(КодДокумента) = "УОПП") И НЕ ЗначениеЗаполнено(УзелЗашифровано)
					И (нрег(Прав(ИмяФайлаДокумента, 4)) = ".enc") Тогда // Обход ошибки ПФР
					ИмяФайлаДокумента = Лев(ИмяФайлаДокумента, СтрДлина(ИмяФайлаДокумента) - 4);
				КонецЕсли;
				
				УзелТипФайла = НайтиСтрокиДереваЗначений(УзелДокумент,,, "ТипФайла", "Э", Ложь);
				ТипФайлаДокумента = ?(ЗначениеЗаполнено(УзелТипФайла), СокрЛП(УзелТипФайла.Значение), "");
				Если ТипФайлаДокумента = "application/gzip" Тогда // Обход ошибки ПФР
					ТипФайлаДокумента = "application/xml";
				КонецЕсли;
				
				Если ДокументыЗашифрованы <> Неопределено Тогда
					ДокументЗашифрован = ДокументыЗашифрованы;
				Иначе
					РасширениеДокументаНРег = ОбщегоНазначенияКлиентСервер.РазложитьПолноеИмяФайла(ИмяФайлаДокумента).Расширение;
					РасширениеДокументаНРег = нрег(РасширениеДокументаНРег);
					ДокументЗашифрован = (РасширениеДокументаНРег = ".enc"
						ИЛИ (РасширениеДокументаНРег <> ".gz" И РасширениеДокументаНРег <> ".der"
						И ТипСодержимогоПоИмениФайла(ИмяФайлаДокумента) = Перечисления.ТипыСодержимогоФайлов.Неизвестный));
				КонецЕсли;
				
				УзелСжатие = НайтиСтрокиДереваЗначений(УзелДокумент,,, "Сжатие", "Э", Ложь);
				ДокументСжатСтр = ?(ЗначениеЗаполнено(УзелСжатие), УзелСжатие.Значение, "");
				ДокументСжат = БулевоПоСтроке(ДокументСжатСтр);
				Если ДокументСжат = Неопределено Тогда
					ТекстСообщения = СтрШаблон(
						НСтр("ru = 'Ошибка при распаковке пакета: некорректная структура файла описания пакета (%1).'"),
						КороткоеИмяФайлаОписанияСведений);
					ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения,,,,, НовыйКонтекстРасшифровки(Объект));
					ОперацииСФайламиЭДКО.УдалитьВременныйФайл(ИмяФайлаОписанияСведений);
					Возврат Ложь;
				КонецЕсли;
				
				УзелФайлЭП = НайтиСтрокиДереваЗначений(УзелДокумент,,, "ФайлЭП", "Э", Ложь);
				ИмяФайлаПодписи = ?(ЗначениеЗаполнено(УзелФайлЭП), СокрЛП(УзелФайлЭП.Значение), "");
				ЕстьОткрепленнаяПодпись = ЗначениеЗаполнено(ИмяФайлаПодписи);
				
				НайденныеСтроки = ТаблицаРаспакованныхФайлов.НайтиСтроки(Новый Структура("Имя", ИмяФайлаДокумента));
				АдресФайлаДокумента = ?(НайденныеСтроки.Количество() > 0, НайденныеСтроки[0].Адрес, Неопределено);
				
				Если ДокументЗашифрован Тогда
					ДополнитьТранспортноеСообщениеПолучателями(Объект, АдресФайлаДокумента);
					Если УбратьСообщениеИзОбработки = Неопределено Тогда
						УбратьСообщениеИзОбработки = НЕ ТранспортноеСообщениеОбрабатывается(Объект);
					КонецЕсли;
				КонецЕсли;
				
				НайденныеСтроки = ТаблицаРаспакованныхФайлов.НайтиСтроки(Новый Структура("Имя", ИмяФайлаПодписи));
				АдресФайлаПодписи = ?(НайденныеСтроки.Количество() > 0, НайденныеСтроки[0].Адрес, Неопределено);
				
				ТипСодержимогоИРасширение = ТипСодержимогоИРасширениеПоMIMEТипу(ТипФайлаДокумента);
				
				ПозицияРазделителя = СтрНайти(ИмяФайлаДокумента, ".");
				Если ПозицияРазделителя > 1 Тогда
					ИмяФайлаДокумента = Лев(ИмяФайлаДокумента, ПозицияРазделителя - 1);
				КонецЕсли;
				ПозицияРазделителя = СтрНайти(ИмяФайлаДокумента, "_", НаправлениеПоиска.СКонца);
				
				ИдентификаторДокумента = "";
				Если ПозицияРазделителя > 0 Тогда
					ИдентификаторДокумента = Сред(ИмяФайлаДокумента, ПозицияРазделителя + 1);
					ИдентификаторДокумента = СокрЛП(ИдентификаторДокумента);
				КонецЕсли;
				Если СтрДлина(ИдентификаторДокумента) <> 36 И СтрДлина(ИдентификаторДокумента) <> 32 Тогда
					ИдентификаторДокумента = ИмяФайлаДокумента;
				КонецЕсли;
				
				Если ЗначениеЗаполнено(ТипСодержимогоИРасширение.Расширение) Тогда
					ИмяФайлаДокумента = ИмяФайлаДокумента + "." + ТипСодержимогоИРасширение.Расширение;
				КонецЕсли;
				
				ТипСодержимого = ОпределитьТипСодержимогоПоТипуДокументаПакета(КодДокумента, Объект.Тип,
					ЭтоДополнительныйДокумент);
				
				Если ЕстьОткрепленнаяПодпись Тогда
					// определяем строку в таблице документов пакета
					ОпределитьСтрокуВТаблицеДокументовПакета(ДокументыПакета,
															ИмяФайлаДокумента,
															АдресФайлаДокумента,
															ТипСодержимого,
															ИмяФайлаПодписи,
															АдресФайлаПодписи,
															Неопределено, // результат проверки подписи
															ИдентификаторДокумента,
															ТипСодержимогоИРасширение.ТипСодержимого,
															Неопределено, // отпечаток сертификата подписи
															ДокументЗашифрован,
															ДокументСжат);
					
				Иначе

					// определяем строку в таблице документов пакета
					ОпределитьСтрокуВТаблицеДокументовПакета(ДокументыПакета,
															ИмяФайлаДокумента,
															АдресФайлаДокумента,
															ТипСодержимого,
															,
															,
															,
															ИдентификаторДокумента,
															ТипСодержимогоИРасширение.ТипСодержимого,
															Неопределено, // отпечаток сертификата подписи
															ДокументЗашифрован,
															ДокументСжат);

				КонецЕсли;
			КонецЦикла;
		КонецЦикла;
		
	Иначе
		// находим узлы "документ"
		УзлыДокумент = ДеревоОписанияСведений.Строки.НайтиСтроки(Новый Структура("Имя, Тип", "документ", "Э"), Истина);
		Для Каждого УзелДокумент Из УзлыДокумент Цикл
			АтрибутыУзлаДокумент = ПолучитьАтрибутыУзла(УзелДокумент);
			
			// получаем узел "содержимое" с атрибутами
			УзелСодержимое = УзелДокумент.Строки.Найти("содержимое", "Имя");
			Если УзелСодержимое <> Неопределено Тогда
				АтрибутыУзлаСодержимое = ПолучитьАтрибутыУзла(УзелСодержимое);
			КонецЕсли;
			
			// получаем узел "подпись" с атрибутами
			УзелПодпись = УзелДокумент.Строки.Найти("подпись", "Имя");
			Если УзелПодпись <> Неопределено Тогда
				АтрибутыУзлаПодпись = ПолучитьАтрибутыУзла(УзелПодпись);
			КонецЕсли;
			
			// определяем тип документа
			ТипДокумента = Неопределено;
			АтрибутыУзлаДокумент.Свойство("типДокумента", ТипДокумента);
			ТипСодержимого = ОпределитьТипСодержимогоПоТипуДокументаПакета(ТипДокумента, Объект.Тип);
			Если ТипСодержимого = Неопределено И Объект.Тип <> Перечисления.ТипыТранспортныхСообщений.ПротоколПФР Тогда
				ТекстСообщения = НСтр("ru = 'Ошибка при распаковке пакета: некорректная структура файла описания пакета (packageDescription.xml).'");
				ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения,,,,, НовыйКонтекстРасшифровки(Объект));
				ОперацииСФайламиЭДКО.УдалитьВременныйФайл(ИмяФайлаОписанияСведений);
				Возврат Ложь;
			КонецЕсли;
			
			// определяем идентификатор документа
			ИдентификаторДокумента = Неопределено;
			АтрибутыУзлаДокумент.Свойство("идентификаторДокумента", ИдентификаторДокумента);
				
			// определяем исходное имя файла
			ИсходноеИмяФайла = Неопределено;
			АтрибутыУзлаДокумент.Свойство("исходноеИмяФайла", ИсходноеИмяФайла);
			
			// определяем тип содержимого
			ТипСодержимогоФайла = Неопределено;
			АтрибутыУзлаДокумент.Свойство("типСодержимого", ТипСодержимогоФайла);
			
			// определяем зашифрован ли документ
			ДокументЗашифрованСтр = Неопределено;
			АтрибутыУзлаДокумент.Свойство("зашифрован", ДокументЗашифрованСтр);
			ДокументЗашифрован = БулевоПоСтроке(ДокументЗашифрованСтр);
			Если ДокументЗашифрован = Неопределено Тогда
				ТекстСообщения = НСтр("ru = 'Ошибка при распаковке пакета: некорректная структура файла описания пакета (packageDescription.xml).'");
				ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения,,,,, НовыйКонтекстРасшифровки(Объект));
				ОперацииСФайламиЭДКО.УдалитьВременныйФайл(ИмяФайлаОписанияСведений);
				Возврат Ложь;
			КонецЕсли;
							
			// определяем сжат ли документ
			ДокументСжатСтр = Неопределено;
			АтрибутыУзлаДокумент.Свойство("сжат", ДокументСжатСтр);
			ДокументСжат = БулевоПоСтроке(ДокументСжатСтр);
			Если ДокументСжат = Неопределено Тогда
				ТекстСообщения = НСтр("ru = 'Ошибка при распаковке пакета: некорректная структура файла описания пакета (packageDescription.xml).'");
				ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения,,,,, НовыйКонтекстРасшифровки(Объект));
				ОперацииСФайламиЭДКО.УдалитьВременныйФайл(ИмяФайлаОписанияСведений);
				Возврат Ложь;
			КонецЕсли;

			// получаем имя файла содержимого
			Если УзелСодержимое <> Неопределено Тогда
				
				ИмяФайлаДокумента = "";
				АтрибутыУзлаСодержимое.Свойство("имяФайла", ИмяФайлаДокумента);
				Если НЕ ЗначениеЗаполнено(ИмяФайлаДокумента) Тогда
					ТекстСообщения = НСтр("ru = 'Ошибка при распаковке пакета: некорректная структура файла описания пакета (packageDescription.xml).'");
					ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения,,,,, НовыйКонтекстРасшифровки(Объект));
					ОперацииСФайламиЭДКО.УдалитьВременныйФайл(ИмяФайлаОписанияСведений);
					Возврат Ложь;
				КонецЕсли;
							
				ИмяФайлаДокумента = СокрЛП(ИмяФайлаДокумента);
				НайденныеСтроки = ТаблицаРаспакованныхФайлов.НайтиСтроки(Новый Структура("Имя", ИмяФайлаДокумента));
									
				Если НайденныеСтроки.Количество() <> 0 Тогда 
					АдресФайлаДокумента = НайденныеСтроки[0].Адрес;
					
					Если ДокументЗашифрован Тогда
						ДополнитьТранспортноеСообщениеПолучателями(Объект, АдресФайлаДокумента);
						Если УбратьСообщениеИзОбработки = Неопределено Тогда 
							УбратьСообщениеИзОбработки = Не ТранспортноеСообщениеОбрабатывается(Объект);
						КонецЕсли;					
					КонецЕсли;
				КонецЕсли;

				// 
				Если ТипСообщения = "ПФР" Тогда 
					
					Если НЕ ЗначениеЗаполнено(ФорматДокументооборота) И ЗначениеЗаполнено(Объект.ЦиклОбмена) Тогда
						ФорматДокументооборота = ОбщегоНазначения.ЗначениеРеквизитаОбъекта(Объект.ЦиклОбмена, "ФорматДокументооборота");
						Если НЕ ЗначениеЗаполнено(ФорматДокументооборота) Тогда
							ФорматДокументооборота = Перечисления.ФорматыДокументооборотаСПФР.БПИ;
						КонецЕсли;
					КонецЕсли;
					
					ЕстьОткрепленнаяПодпись = Объект.Тип <> Перечисления.ТипыТранспортныхСообщений.УведомлениеОбОшибкеПФР;
					Если ЕстьОткрепленнаяПодпись Тогда
						// получаем узел "подпись" с атрибутами
						УзелПодпись = УзелДокумент.Строки.Найти("подпись", "Имя");
						Если УзелПодпись = Неопределено Тогда
							Если ФорматДокументооборота = Перечисления.ФорматыДокументооборотаСПФР.ЭДОК Тогда
								ЕстьОткрепленнаяПодпись = Ложь;
							Иначе
								ТекстСообщения = НСтр("ru = 'Ошибка при распаковке пакета: некорректная структура файла описания пакета (packageDescription.xml).'");
								ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения,,,,, НовыйКонтекстРасшифровки(Объект));
								ОперацииСФайламиЭДКО.УдалитьВременныйФайл(КаталогФайловПакета);
								
								Если УбратьСообщениеИзОбработки <> Неопределено И УбратьСообщениеИзОбработки Тогда 
									ПодтвердитьРасшифровку(Объект.Ссылка);
									
									ТекстСообщения = СтрШаблон("Транспортное сообщение исключено из обработки правилом");
									ЗаписьЖурналаРегистрации(
									НСтр("ru = 'Электронный документооборот с контролирующими органами.ТранспортноеСообщениеОбрабатывается()'"), 
									УровеньЖурналаРегистрации.Предупреждение,, Объект.Ссылка, ТекстСообщения);							
								КонецЕсли;
							
								Возврат Ложь;
							КонецЕсли;
						КонецЕсли;
						
						Если ЕстьОткрепленнаяПодпись Тогда
							АтрибутыУзлаПодпись = ПолучитьАтрибутыУзла(УзелПодпись);
							
							// получаем имя файла подписи
							ИмяФайлаПодписи = Неопределено;
							АтрибутыУзлаПодпись.Свойство("имяФайла", ИмяФайлаПодписи);
							Если НЕ ЗначениеЗаполнено(ИмяФайлаПодписи) Тогда
								ТекстСообщения = СтрШаблон(
									НСтр("ru = 'Ошибка при распаковке пакета: не определен файл подписи под документом %1.'"),
									Символ(34) + ИмяФайлаДокумента + Символ(34));
								ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения,,,,, НовыйКонтекстРасшифровки(Объект));
								ОперацииСФайламиЭДКО.УдалитьВременныйФайл(КаталогФайловПакета);
								
								Если УбратьСообщениеИзОбработки <> Неопределено И УбратьСообщениеИзОбработки Тогда 
									ПодтвердитьРасшифровку(Объект.Ссылка);
									
									ТекстСообщения = СтрШаблон("Транспортное сообщение исключено из обработки правилом");
									ЗаписьЖурналаРегистрации(
									НСтр("ru = 'Электронный документооборот с контролирующими органами.ТранспортноеСообщениеОбрабатывается()'"), 
									УровеньЖурналаРегистрации.Предупреждение,, Объект.Ссылка, ТекстСообщения);							
								КонецЕсли;
								
								Возврат Ложь;
							КонецЕсли;
							
							ИмяФайлаПодписи = СокрЛП(ИмяФайлаПодписи);
							НайденныеСтроки = ТаблицаРаспакованныхФайлов.НайтиСтроки(Новый Структура("Имя", ИмяФайлаПодписи));
					
							Если НайденныеСтроки.Количество() <> 0 Тогда 
								АдресФайлаПодписи = НайденныеСтроки[0].Адрес;
							КонецЕсли;
						КонецЕсли;
					КонецЕсли;
					
					Если ЕстьОткрепленнаяПодпись Тогда
						// определяем строку в таблице документов пакета
						ОпределитьСтрокуВТаблицеДокументовПакета(ДокументыПакета,
																ИмяФайлаДокумента,
																АдресФайлаДокумента,
																ТипСодержимого,
																ИмяФайлаПодписи,
																АдресФайлаПодписи,
																Неопределено, //?(ЗначениеЗаполнено(РезультатПроверкиПодписи), РезультатПроверкиПодписи, Неопределено),
																ИдентификаторДокумента,
																ТипСодержимогоПоСтроке(ТипСодержимогоФайла),
																Неопределено, //ОтпечатокСертификатаПодписи);
																ДокументЗашифрован,
																ДокументСжат);
						
					Иначе
						
						// определяем строку в таблице документов пакета
						ОпределитьСтрокуВТаблицеДокументовПакета(ДокументыПакета,
																ИмяФайлаДокумента,
																АдресФайлаДокумента,
																ТипСодержимого,
																,
																,
																,
																ИдентификаторДокумента,
																ТипСодержимогоПоСтроке(ТипСодержимогоФайла),
																Неопределено, //ОтпечатокСертификатаПодписи);
																ДокументЗашифрован,
																ДокументСжат);
						
					КонецЕсли;

				ИначеЕсли ТипСообщения = "ФНС_534" Тогда 

					// получаем узел "подпись" с атрибутами
					УзелПодпись = УзелДокумент.Строки.Найти("подпись", "Имя");

					Если Объект.Тип <> Перечисления.ТипыТранспортныхСообщений.УведомлениеОбОшибкеФНС И УзелПодпись <> Неопределено Тогда

						АтрибутыУзлаПодпись = ПолучитьАтрибутыУзла(УзелПодпись);
						
						// получаем имя файла подписи
						ИмяФайлаПодписи = Неопределено;
						АтрибутыУзлаПодпись.Свойство("имяФайла", ИмяФайлаПодписи);
						Если НЕ ЗначениеЗаполнено(ИмяФайлаПодписи) Тогда
							ТекстСообщения = СтроковыеФункцииКлиентСервер.ПодставитьПараметрыВСтроку(
								НСтр("ru = 'Ошибка при распаковке пакета: не определен файл подписи под документом %1'"),
								Символ(34) + ИмяФайлаДокумента + Символ(34));
							ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения,,,,, НовыйКонтекстРасшифровки(Объект));
							ОперацииСФайламиЭДКО.УдалитьВременныйФайл(КаталогФайловПакета);
							
							Если УбратьСообщениеИзОбработки <> Неопределено И УбратьСообщениеИзОбработки Тогда 
								ПодтвердитьРасшифровку(Объект.Ссылка);
								
								ТекстСообщения = СтрШаблон("Транспортное сообщение исключено из обработки правилом");
								ЗаписьЖурналаРегистрации(
								НСтр("ru = 'Электронный документооборот с контролирующими органами.ТранспортноеСообщениеОбрабатывается()'"), 
								УровеньЖурналаРегистрации.Предупреждение,, Объект.Ссылка, ТекстСообщения);							
							КонецЕсли;
							
							Возврат Ложь;
						КонецЕсли;
						
						ИмяФайлаПодписи = СокрЛП(ИмяФайлаПодписи);
						НайденныеСтроки = ТаблицаРаспакованныхФайлов.НайтиСтроки(Новый Структура("Имя", ИмяФайлаПодписи));
				
						Если НайденныеСтроки.Количество() <> 0 Тогда 
							АдресФайлаПодписи = НайденныеСтроки[0].Адрес;
						КонецЕсли;
						
						// определяем строку в таблице документов пакета
						ОпределитьСтрокуВТаблицеДокументовПакета(ДокументыПакета,
																?(ЗначениеЗаполнено(ИсходноеИмяФайла), ИсходноеИмяФайла, ""), //
																АдресФайлаДокумента,
																ТипСодержимого,
																ИмяФайлаПодписи,
																АдресФайлаПодписи,
																Неопределено, 
																ИдентификаторДокумента,
																ТипСодержимогоПоСтроке(ТипСодержимогоФайла),
																Неопределено, 
																ДокументЗашифрован,
																ДокументСжат);
						
					Иначе
						
						// определяем строку в таблице документов пакета
						ОпределитьСтрокуВТаблицеДокументовПакета(ДокументыПакета,
																?(ЗначениеЗаполнено(ИсходноеИмяФайла), ИсходноеИмяФайла, ""), //
																АдресФайлаДокумента,
																ТипСодержимого,
																,
																,
																,
																ИдентификаторДокумента,
																ТипСодержимогоПоСтроке(ТипСодержимогоФайла),
																Неопределено,
																ДокументЗашифрован,
																ДокументСжат);
						
					КонецЕсли;

				ИначеЕсли ТипСообщения = "ФСГС" Тогда 

					Если Объект.Тип <> Перечисления.ТипыТранспортныхСообщений.УведомлениеОбОшибкеФСГС Тогда
						
						УзелТипДокумента = УзелДокумент.Строки.Найти("типДокумента", "Имя");
						СтрТипДокумента = Неопределено;
						Если УзелТипДокумента <> Неопределено Тогда
							СтрТипДокумента =  УзелТипДокумента.Значение;
						КонецЕсли;
						Если СтрТипДокумента = Неопределено Тогда
							 СтрТипДокумента = "";
						КонецЕсли;
						
						Если СтрТипДокумента <> "описаниеПисьма" Тогда
							
							// получаем узел "подпись" с атрибутами
							УзелПодпись = УзелДокумент.Строки.Найти("подпись", "Имя");
							Если УзелПодпись = Неопределено Тогда
								
								// ФСГС ПРИКАЗ от 7 июля 2011 г. N 313 (в ред. Приказов Росстата от 21.05.2012 N 312, от 04.05.2018 N 281)
								// Подпись опциональный элемент

								//ТекстСообщения = НСтр("ru = 'Ошибка при распаковке пакета: некорректная структура файла описания пакета (packageDescription.xml).'");
								//ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
								//ОперацииСФайламиЭДКО.УдалитьВременныйФайл(КаталогФайловПакета);
								//Возврат Ложь;
								
								ИмяФайлаПодписи = Неопределено;
								АдресФайлаПодписи = Неопределено;
							
							Иначе
								АтрибутыУзлаПодпись = ПолучитьАтрибутыУзла(УзелПодпись);
							
								// получаем имя файла подписи
								ИмяФайлаПодписи = Неопределено;
								АтрибутыУзлаПодпись.Свойство("имяФайла", ИмяФайлаПодписи);
								Если НЕ ЗначениеЗаполнено(ИмяФайлаПодписи) Тогда
									ТекстСообщения = СтроковыеФункцииКлиентСервер.ПодставитьПараметрыВСтроку(НСтр("ru = 'Ошибка при распаковке пакета: не определен файл подписи под документом %1.'"), Символ(34) + ИмяФайлаДокумента + Символ(34));
									ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения,,,,, НовыйКонтекстРасшифровки(Объект));
									ОперацииСФайламиЭДКО.УдалитьВременныйФайл(КаталогФайловПакета);
									
									Если УбратьСообщениеИзОбработки <> Неопределено И УбратьСообщениеИзОбработки Тогда 
										ПодтвердитьРасшифровку(Объект.Ссылка);
										
										ТекстСообщения = СтрШаблон("Транспортное сообщение исключено из обработки правилом");
										ЗаписьЖурналаРегистрации(
											НСтр("ru = 'Электронный документооборот с контролирующими органами.ТранспортноеСообщениеОбрабатывается()'"), 
											УровеньЖурналаРегистрации.Предупреждение,, Объект.Ссылка, ТекстСообщения);							
									КонецЕсли;
							
									Возврат Ложь;
								КонецЕсли;
								
								ИмяФайлаПодписи = СокрЛП(ИмяФайлаПодписи);
								НайденныеСтроки = ТаблицаРаспакованныхФайлов.НайтиСтроки(Новый Структура("Имя", ИмяФайлаПодписи));
						
								Если НайденныеСтроки.Количество() <> 0 Тогда 
									АдресФайлаПодписи = НайденныеСтроки[0].Адрес;
								КонецЕсли;
							КонецЕсли;						
							
						Иначе
							
							ИмяФайлаПодписи = Неопределено;
							АдресФайлаПодписи = Неопределено;
							
						КонецЕсли;
						// определяем строку в таблице документов пакета
						ОпределитьСтрокуВТаблицеДокументовПакета(ДокументыПакета,
																?(ЗначениеЗаполнено(ИсходноеИмяФайла), ИсходноеИмяФайла, ИмяФайлаДокумента),
																АдресФайлаДокумента,
																ТипСодержимого,
																ИмяФайлаПодписи,
																АдресФайлаПодписи,
																Неопределено, //?(ЗначениеЗаполнено(РезультатПроверкиПодписи), РезультатПроверкиПодписи, Неопределено),
																ИдентификаторДокумента,
																ТипСодержимогоПоСтроке(ТипСодержимогоФайла),
																Неопределено, //ОтпечатокСертификатаПодписи);
																ДокументЗашифрован,
																ДокументСжат);
												
					Иначе
						
						// определяем строку в таблице документов пакета
						ОпределитьСтрокуВТаблицеДокументовПакета(ДокументыПакета,
																ИмяФайлаДокумента,
																АдресФайлаДокумента,
																ТипСодержимого,
																,
																,
																,
																ИдентификаторДокумента,
																ТипСодержимогоПоСтроке(ТипСодержимогоФайла),
																Неопределено, //ОтпечатокСертификатаПодписи);
																ДокументЗашифрован,
																ДокументСжат);
						
					КонецЕсли;
				КонецЕсли;
				
			ИначеЕсли УзелПодпись <> Неопределено Тогда
				
				// получаем имя файла подписи
				ИмяФайлаПодписи = Неопределено;
				АтрибутыУзлаПодпись.Свойство("имяФайла", ИмяФайлаПодписи);
				Если НЕ ЗначениеЗаполнено(ИмяФайлаПодписи) Тогда
					ТекстСообщения = СтроковыеФункцииКлиентСервер.ПодставитьПараметрыВСтроку(НСтр("ru = 'Ошибка при распаковке пакета: не определен файл подписи под документом %1 .'"), Символ(34) + ИмяФайлаДокумента + Символ(34));
					ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения,,,,, НовыйКонтекстРасшифровки(Объект));
					ОперацииСФайламиЭДКО.УдалитьВременныйФайл(КаталогФайловПакета);
					
					Если УбратьСообщениеИзОбработки <> Неопределено И УбратьСообщениеИзОбработки Тогда 
						ПодтвердитьРасшифровку(Объект.Ссылка);
						
						ТекстСообщения = СтрШаблон("Транспортное сообщение исключено из обработки правилом");
						ЗаписьЖурналаРегистрации(
							НСтр("ru = 'Электронный документооборот с контролирующими органами.ТранспортноеСообщениеОбрабатывается()'"), 
							УровеньЖурналаРегистрации.Предупреждение,, Объект.Ссылка, ТекстСообщения);							
					КонецЕсли;
					
					Возврат Ложь;
				КонецЕсли;
				
				ИмяФайлаПодписи = СокрЛП(ИмяФайлаПодписи);
				НайденныеСтроки = ТаблицаРаспакованныхФайлов.НайтиСтроки(Новый Структура("Имя", ИмяФайлаПодписи));
				Если НайденныеСтроки.Количество() <> 0 Тогда 
					АдресФайлаПодписи = НайденныеСтроки[0].Адрес;
				КонецЕсли;
				
				// находим подписанный документ в архиве
				ИмяФайлаДокумента = Неопределено;
				ИдентификаторПодписанногоДокумента = Неопределено;
				Если АтрибутыУзлаДокумент.Свойство("идентификаторДокумента", ИдентификаторПодписанногоДокумента) Тогда
					ИмяФайлаДокумента = ДокументПоИдентификатору(ИдентификаторПодписанногоДокумента, КаталогФайловПакета);
					Если ИмяФайлаДокумента = Неопределено Тогда
						ТекстСообщения = СтроковыеФункцииКлиентСервер.ПодставитьПараметрыВСтроку(НСтр("ru = 'Ошибка при распаковке пакета: не удалось проверить подпись %1
													|в архиве документооборота не удалось найти исходный подписанный документ с идентификатором %2.'"), 
													ИмяФайлаПодписи, ИдентификаторПодписанногоДокумента);
						Если Объект.Тип = Перечисления.ТипыТранспортныхСообщений.РезультатПриемаДекларацияНО Тогда
							//Временное отключение интерактивной ошибки до исправления комплекса ФНС
							ЗаписьЖурналаРегистрации(
								НСтр("ru = 'Электронный документооборот с контролирующими органами.ОбработатьСодержимоеТранспортногоКонтейнера()'"), 
								УровеньЖурналаРегистрации.Предупреждение,, Объект.Ссылка, ТекстСообщения);
						Иначе
							ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения,,,,, НовыйКонтекстРасшифровки(Объект));
						КонецЕсли;
					Иначе // помещаем найденный файл во временное хранилище
						ГУИД = Новый УникальныйИдентификатор;
						Адрес = ПоместитьВоВременноеХранилище(Новый ДвоичныеДанные(КаталогФайловПакета + ИмяФайлаДокумента), ГУИД);
						АдресФайлаДокумента = Адрес;
					КонецЕсли;
				КонецЕсли;
				
				// определяем строку в таблице документов пакета
				ОпределитьСтрокуВТаблицеДокументовПакета(ДокументыПакета,
															ИмяФайлаДокумента,
															Неопределено,
															ТипСодержимого,
															ИмяФайлаПодписи,
															АдресФайлаПодписи,
															Неопределено, //РезультатПроверкиПодписи,
															ИдентификаторДокумента,
															ТипСодержимогоПоСтроке(ТипСодержимогоФайла),
															Неопределено, //ОтпечатокСертификатаПодписи);
															ДокументЗашифрован,
															ДокументСжат);
				
			КонецЕсли;
		КонецЦикла;
	КонецЕсли;

	// удаляем временные файлы
	УдалитьФайлы(ВремФайл);
	ОперацииСФайламиЭДКО.УдалитьВременныйФайл(КаталогФайловПакета);

	// Распаковка и внутренние проверки прошли без ошибок, но сообщение исключено из обработки правилом
	// по ссылке или сертификату получателя
	Если УбратьСообщениеИзОбработки <> Неопределено И УбратьСообщениеИзОбработки Тогда 
		ПодтвердитьРасшифровку(Объект.Ссылка);
		
		ТекстСообщения = СтрШаблон("Транспортное сообщение исключено из обработки правилом");
		ЗаписьЖурналаРегистрации(
			НСтр("ru = 'Электронный документооборот с контролирующими органами.ТранспортноеСообщениеОбрабатывается()'"), 
			УровеньЖурналаРегистрации.Предупреждение,, Объект.Ссылка, ТекстСообщения);
			
		Возврат Ложь;
	КонецЕсли;
	
	Результат.Вставить("ФорматДокументооборота", ФорматДокументооборота);
	
	Возврат Результат;

КонецФункции

Функция ОпределитьТипСодержимогоПоТипуДокументаПакета(ТипДокумента, ТипСообщения,
		ЭтоДополнительныйДокумент = Неопределено)
	
	// типы ПФР
	Если ТипДокумента = "подтверждениеПолучения" ИЛИ ЭтоДополнительныйДокумент = Ложь И ТипДокумента = "УОД" Тогда
		Если ТипСообщения = Перечисления.ТипыТранспортныхСообщений.ПисьмоИсходящееКвитанцияПФР Тогда
			Возврат Перечисления.ТипыСодержимогоТранспортногоКонтейнера.ПодтверждениеПолученияПисьма;
		ИначеЕсли ТипСообщения = Перечисления.ТипыТранспортныхСообщений.ПодтверждениеПолученияЗапросаПФР Тогда
			Возврат Перечисления.ТипыСодержимогоТранспортногоКонтейнера.ПодтверждениеПолученияЗапросПФР;
		Иначе
			Возврат Перечисления.ТипыСодержимогоТранспортногоКонтейнера.ПодтверждениеПолученияОтчетностиПФР;
		КонецЕсли;
	ИначеЕсли ТипДокумента = "ответПриложение" ИЛИ ЭтоДополнительныйДокумент = Ложь
		И ТипСообщения = Перечисления.ТипыТранспортныхСообщений.ОтветНаЗапросПФР Тогда
		Возврат Перечисления.ТипыСодержимогоТранспортногоКонтейнера.ОтветПриложениеНаЗапросПФР;
	ИначеЕсли ТипДокумента = "протокол" ИЛИ ЭтоДополнительныйДокумент = Ложь
		И (ТипДокумента = "УОРР" ИЛИ ТипДокумента = "УППО" ИЛИ ТипДокумента = "УУОН-ПУ") Тогда
		Возврат Перечисления.ТипыСодержимогоТранспортногоКонтейнера.ПротоколПФР;
	ИначеЕсли ТипДокумента = "протоколПриложение" ИЛИ ЭтоДополнительныйДокумент = Истина
		И ТипСообщения = Перечисления.ТипыТранспортныхСообщений.ПротоколПФР Тогда
		Возврат Перечисления.ТипыСодержимогоТранспортногоКонтейнера.ПротоколПриложениеПФР;
	ИначеЕсли ТипДокумента = "пачкаИС" ИЛИ ЭтоДополнительныйДокумент = Ложь
		И ТипСообщения = Перечисления.ТипыТранспортныхСообщений.ПисьмоВходящееПФР Тогда
		Возврат Перечисления.ТипыСодержимогоТранспортногоКонтейнера.ФайлОтчетностиПФР;
	ИначеЕсли ТипДокумента = "пачкаАДВ" Тогда
		Возврат Перечисления.ТипыСодержимогоТранспортногоКонтейнера.ФайлОтчетностиПФР;
	ИначеЕсли ТипДокумента = "пачкаДСВ" Тогда
		Возврат Перечисления.ТипыСодержимогоТранспортногоКонтейнера.ФайлОтчетностиПФР;
	ИначеЕсли ТипДокумента = "реестрДСВ" Тогда
		Возврат Перечисления.ТипыСодержимогоТранспортногоКонтейнера.ФайлОтчетностиПФР;
	ИначеЕсли ТипДокумента = "пачкаРСВ" Тогда
		Возврат Перечисления.ТипыСодержимогоТранспортногоКонтейнера.ФайлОтчетностиПФР;
	ИначеЕсли ТипДокумента = "пачкаСВВ" Тогда
		Возврат Перечисления.ТипыСодержимогоТранспортногоКонтейнера.ФайлОтчетностиПФР;
	ИначеЕсли ТипДокумента = "описаниеСведений" Тогда
		Возврат Перечисления.ТипыСодержимогоТранспортногоКонтейнера.ФайлОписанияСведенийПФР;
	ИначеЕсли ТипДокумента = "описаниеОшибки" ИЛИ ЭтоДополнительныйДокумент = Ложь И ТипДокумента = "УОПП"
		И ТипСообщения = Перечисления.ТипыТранспортныхСообщений.УведомлениеОбОшибкеПФР Тогда
		Если ТипСообщения = Перечисления.ТипыТранспортныхСообщений.УведомлениеОбОшибкеПФР Тогда
			Возврат Перечисления.ТипыСодержимогоТранспортногоКонтейнера.ОписаниеОшибкиПФР;
		ИначеЕсли ТипСообщения = Перечисления.ТипыТранспортныхСообщений.УведомлениеОбОшибкеФСГС Тогда
			Возврат Перечисления.ТипыСодержимогоТранспортногоКонтейнера.ОписаниеОшибкиФСГС;
		КонецЕсли;
	ИначеЕсли ТипДокумента = "описаниеОшибочногоПакета" Тогда
		Если ТипСообщения = Перечисления.ТипыТранспортныхСообщений.УведомлениеОбОшибкеПФР Тогда
			Возврат Перечисления.ТипыСодержимогоТранспортногоКонтейнера.ОписаниеОшибочногоПакетаПФР;
		ИначеЕсли ТипСообщения = Перечисления.ТипыТранспортныхСообщений.УведомлениеОбОшибкеФСГС Тогда
			Возврат Перечисления.ТипыСодержимогоТранспортногоКонтейнера.ОписаниеОшибочногоПакетаФСГС;
		Иначе
			Возврат Перечисления.ТипыСодержимогоТранспортногоКонтейнера.ОписаниеОшибочногоПакетаФНС;
		КонецЕсли;
	ИначеЕсли ТипДокумента = "письмоПриложение" Тогда
		Возврат Перечисления.ТипыСодержимогоТранспортногоКонтейнера.ПисьмоПриложение;
	ИначеЕсли ТипДокумента = "письмо" Тогда
		Если ТипСообщения = Перечисления.ТипыТранспортныхСообщений.ПисьмоВходящееФСГС
		 ИЛИ ТипСообщения = Перечисления.ТипыТранспортныхСообщений.ПисьмоИсходящееФСГС Тогда
			Возврат Перечисления.ТипыСодержимогоТранспортногоКонтейнера.ПисьмоФСГС;
		Иначе
			Возврат Перечисления.ТипыСодержимогоТранспортногоКонтейнера.Письмо;
		КонецЕсли; 
	ИначеЕсли ТипДокумента = "транспортнаяИнформация" Тогда
		Возврат Перечисления.ТипыСодержимогоТранспортногоКонтейнера.ТранспортнаяИнформация;
	ИначеЕсли ТипДокумента = "описаниеПисьма" Тогда
		Если ТипСообщения = Перечисления.ТипыТранспортныхСообщений.ПисьмоВходящееФСГС
		 ИЛИ ТипСообщения = Перечисления.ТипыТранспортныхСообщений.ПисьмоИсходящееФСГС
		 ИЛИ ТипСообщения = Перечисления.ТипыТранспортныхСообщений.РассылкаФСГС Тогда
			Возврат Перечисления.ТипыСодержимогоТранспортногоКонтейнера.ОписаниеПисьмаФСГС;
		Иначе
			Возврат Перечисления.ТипыСодержимогоТранспортногоКонтейнера.ОписаниеПисьма;
		КонецЕсли;
	ИначеЕсли ТипДокумента = "описаниеРассылкиШаблонов" Тогда
		Если ТипСообщения = Перечисления.ТипыТранспортныхСообщений.РассылкаШаблоновФСГС Тогда
			Возврат Перечисления.ТипыСодержимогоТранспортногоКонтейнера.ОписаниеРассылкиШаблоновФСГС;
		Иначе
			Возврат Перечисления.ТипыСодержимогоТранспортногоКонтейнера.ОписаниеПисьма;
		КонецЕсли;
	ИначеЕсли ТипДокумента = "сведенияПриложение" Тогда
		Возврат Перечисления.ТипыСодержимогоТранспортногоКонтейнера.СведенияПриложениеПФР;
	ИначеЕсли ТипДокумента = "сертификат" Тогда
		Возврат Перечисления.ТипыСодержимогоТранспортногоКонтейнера.Сертификат;
	// типы ФСГС
	ИначеЕсли (ТипДокумента = "извещениеОПолучении")
			И (ТипСообщения = Перечисления.ТипыТранспортныхСообщений.ИзвещениеПисьмоВходящееФСГС
		   ИЛИ ТипСообщения = Перечисления.ТипыТранспортныхСообщений.ИзвещениеПисьмоИсходящееФСГС
		   ИЛИ ТипСообщения = Перечисления.ТипыТранспортныхСообщений.ИзвещениеПротоколВходногоКонтроляОтчетностиФСГС
		   ИЛИ ТипСообщения = Перечисления.ТипыТранспортныхСообщений.ИзвещениеОПолученииОтчетностиФСГС) Тогда
		Возврат Перечисления.ТипыСодержимогоТранспортногоКонтейнера.ИзвещениеОПолученииДокументаФСГС;
	ИначеЕсли СтрНайти(ТипДокумента, "уведомлениеОПриеме") > 0 Тогда
		Возврат Перечисления.ТипыСодержимогоТранспортногоКонтейнера.УведомлениеОПриемеВОбработкуОтчетаФСГС;
	ИначеЕсли СтрНайти(ТипДокумента, "уведомлениеОНесоответствии") > 0 Тогда
		Возврат Перечисления.ТипыСодержимогоТранспортногоКонтейнера.УведомлениеОНесоответствииФорматуОтчетаФСГС;
	ИначеЕсли (СтрНайти(ТипДокумента, "уведомлениеОбОтказе") > 0)
			И (ТипСообщения = Перечисления.ТипыТранспортныхСообщений.ПротоколВходногоКонтроляОтчетностиФСГС) Тогда
		Возврат Перечисления.ТипыСодержимогоТранспортногоКонтейнера.УведомлениеОбОтказеОтчетаФСГС;
	ИначеЕсли (СтрНайти(ТипДокумента, "уведомлениеОбОтклоненииОтчета") > 0 или СтрНайти(ТипДокумента, "уведомлениеОбОтклонении") > 0)
			И (ТипСообщения = Перечисления.ТипыТранспортныхСообщений.ПротоколВходногоКонтроляОтчетностиФСГС) Тогда
		Возврат Перечисления.ТипыСодержимогоТранспортногоКонтейнера.УведомлениеОбОтклоненииОтчетаФСГС;
	ИначеЕсли (СтрНайти(ТипДокумента, "уведомлениеОбУточнении") > 0)
			И (ТипСообщения = Перечисления.ТипыТранспортныхСообщений.ПротоколВходногоКонтроляОтчетностиФСГС) Тогда
		Возврат Перечисления.ТипыСодержимогоТранспортногоКонтейнера.УведомлениеОбУточненииОтчетаФСГС;
	ИначеЕсли ТипДокумента = "отчет" Тогда
		Возврат Перечисления.ТипыСодержимогоТранспортногоКонтейнера.ФайлОтчетностиФСГС;
	ИначеЕсли ТипДокумента = "описаниеОтчета" Тогда
		Возврат Перечисления.ТипыСодержимогоТранспортногоКонтейнера.ОписаниеФайлаОтчетностиФСГС;
	ИначеЕсли ТипДокумента = "подтверждениеОператора" Тогда
		Возврат Перечисления.ТипыСодержимогоТранспортногоКонтейнера.ПодтверждениеОператораФСГС;
	ИначеЕсли ТипДокумента = "приложениеПисьма" Тогда
		Возврат Перечисления.ТипыСодержимогоТранспортногоКонтейнера.ПриложениеПисьмаФСГС;
	ИначеЕсли ТипДокумента = "рассылка" И ТипСообщения = Перечисления.ТипыТранспортныхСообщений.РассылкаФСГС Тогда
		Возврат Перечисления.ТипыСодержимогоТранспортногоКонтейнера.РассылкаФСГС;
	ИначеЕсли ТипДокумента = "шаблон" И ТипСообщения = Перечисления.ТипыТранспортныхСообщений.РассылкаШаблоновФСГС Тогда
		Возврат Перечисления.ТипыСодержимогоТранспортногоКонтейнера.ШаблонФСГС;
	// типы ФНС 534
	ИначеЕсли ТипДокумента = "декларация" Тогда
		Возврат Перечисления.ТипыСодержимогоТранспортногоКонтейнера.ФайлОтчетности;
	ИначеЕсли ТипДокумента = "описание" Тогда
		Если ТипСообщения = Перечисления.ТипыТранспортныхСообщений.ПисьмоНО
		ИЛИ ТипСообщения = Перечисления.ТипыТранспортныхСообщений.РассылкаНО Тогда
			Возврат Перечисления.ТипыСодержимогоТранспортногоКонтейнера.ОписаниеСведенийОбращениеПисьмоРассылка;
		ИначеЕсли ТипСообщения = Перечисления.ТипыТранспортныхСообщений.ДокументНО Тогда
			Возврат Перечисления.ТипыСодержимогоТранспортногоКонтейнера.ОписаниеДокумент;
		ИначеЕсли ТипСообщения = Перечисления.ТипыТранспортныхСообщений.ПредставлениеНП Тогда
			Возврат Перечисления.ТипыСодержимогоТранспортногоКонтейнера.ОписаниеПредставление;	
		ИначеЕсли ТипСообщения = Перечисления.ТипыТранспортныхСообщений.ЗаявлениеНП Тогда
			Возврат Перечисления.ТипыСодержимогоТранспортногоКонтейнера.ОписаниеСведенийЗаявление;
		ИначеЕсли ТипСообщения = Перечисления.ТипыТранспортныхСообщений.РезультатОбработкиЗапросаНаВыпискуЕРГЮЛ_ЕГРИП Тогда
			Возврат Перечисления.ТипыСодержимогоТранспортногоКонтейнера.РезультатОбработкиЗапросаНаВыпискуЕГРЮЛ_ЕГРИП;
		Иначе
			Возврат Перечисления.ТипыСодержимогоТранспортногоКонтейнера.ОписаниеСведений;
		КонецЕсли;
	ИначеЕсли ТипДокумента = "доверенность" Тогда
			Возврат Перечисления.ТипыСодержимогоТранспортногоКонтейнера.ДоверенностьПредставление;		
	ИначеЕсли ТипДокумента = "подтверждениеДатыОтправки" Тогда
			Возврат Перечисления.ТипыСодержимогоТранспортногоКонтейнера.ПодтверждениеДатыОтправкиПредставление;
	ИначеЕсли ТипДокумента = "уведомлениеОбОтказе" Тогда
		Если ТипСообщения = Перечисления.ТипыТранспортныхСообщений.РезультатПриемаЗаявлениеНО 
		ИЛИ ТипСообщения = Перечисления.ТипыТранспортныхСообщений.РезультатПриемаДокументНП 
		ИЛИ ТипСообщения = Перечисления.ТипыТранспортныхСообщений.РезультатПриемаПредставлениеНО Тогда
			Возврат Перечисления.ТипыСодержимогоТранспортногоКонтейнера.УведомлениеОбОтказеДокумент;
		Иначе
			Возврат Перечисления.ТипыСодержимогоТранспортногоКонтейнера.УведомлениеОбОтказе;
		КонецЕсли;
	ИначеЕсли ТипДокумента = "квитанцияОПриеме" Тогда
		Если ТипСообщения = Перечисления.ТипыТранспортныхСообщений.РезультатПриемаЗаявлениеНО Тогда
			Возврат Перечисления.ТипыСодержимогоТранспортногоКонтейнера.КвитанцияОПриемеЗаявления;
		ИначеЕсли ТипСообщения = Перечисления.ТипыТранспортныхСообщений.РезультатПриемаДокументНП 
		ИЛИ ТипСообщения = Перечисления.ТипыТранспортныхСообщений.РезультатПриемаПредставлениеНО Тогда
			Возврат Перечисления.ТипыСодержимогоТранспортногоКонтейнера.КвитанцияОПриемеДокумент;
		Иначе
			Возврат Перечисления.ТипыСодержимогоТранспортногоКонтейнера.КвитанцияОПриеме;
		КонецЕсли;
	ИначеЕсли ТипДокумента = "уведомлениеОбУточнении" Тогда
		Возврат Перечисления.ТипыСодержимогоТранспортногоКонтейнера.УведомлениеОбУточнении;
	ИначеЕсли ТипДокумента = "извещениеОВводе" Тогда
		Возврат Перечисления.ТипыСодержимогоТранспортногоКонтейнера.ИзвещениеОВводе;
	ИначеЕсли ТипДокумента = "извещениеОПолучении" Тогда
		Возврат Перечисления.ТипыСодержимогоТранспортногоКонтейнера.ИзвещениеОПолучении;
	ИначеЕсли ТипДокумента = "сообщениеОбОшибке" Тогда
		Возврат Перечисления.ТипыСодержимогоТранспортногоКонтейнера.ОписаниеОшибкиФНС;
	ИначеЕсли ТипДокумента = "обращение" Тогда
		Возврат Перечисления.ТипыСодержимогоТранспортногоКонтейнера.Обращение;
	ИначеЕсли ТипДокумента = "представление" Тогда
		Возврат Перечисления.ТипыСодержимогоТранспортногоКонтейнера.Представление;
	ИначеЕсли ТипДокумента = "приложение" Тогда
		Возврат Перечисления.ТипыСодержимогоТранспортногоКонтейнера.Приложение;
	ИначеЕсли ТипДокумента = "подтверждениеДатыПолучения" Тогда
		Возврат Перечисления.ТипыСодержимогоТранспортногоКонтейнера.ПодтверждениеДатыПолучения;
	ИначеЕсли ТипДокумента = "письмо" Тогда
		Возврат Перечисления.ТипыСодержимогоТранспортногоКонтейнера.Письмо;
	ИначеЕсли ТипДокумента = "рассылка" Тогда
		Возврат Перечисления.ТипыСодержимогоТранспортногоКонтейнера.Рассылка;
	ИначеЕсли ТипДокумента = "документ" Тогда
		Возврат Перечисления.ТипыСодержимогоТранспортногоКонтейнера.Документ;
	//заявление
	ИначеЕсли ТипДокумента = "заявление" Тогда
		Возврат Перечисления.ТипыСодержимогоТранспортногоКонтейнера.Заявление;
	ИначеЕсли ТипДокумента = "сообщениеОПроверке" Тогда
		Возврат Перечисления.ТипыСодержимогоТранспортногоКонтейнера.СообщениеОПроверке;
	ИначеЕсли ТипДокумента = "сообщениеОбОтзывеЗаявления" Тогда
		Возврат Перечисления.ТипыСодержимогоТранспортногоКонтейнера.СообщениеОбОтзывеЗаявления;
	ИначеЕсли ТипДокумента = "сообщениеОНесоответствиях" Тогда
		Возврат Перечисления.ТипыСодержимогоТранспортногоКонтейнера.СообщениеОНесоответствиях;
		
	ИначеЕсли ТипДокумента = "сообщениеОПростОтметки" Тогда
		Возврат Перечисления.ТипыСодержимогоТранспортногоКонтейнера.СообщениеОПростОтметки;
	ИначеЕсли ТипДокумента = "уведомлениеОбОтказеОтметки" Тогда
		Возврат Перечисления.ТипыСодержимогоТранспортногоКонтейнера.УведомлениеОбОтказеОтметки;
		
	ИначеЕсли ТипДокумента = "запрос" Тогда
		Возврат Перечисления.ТипыСодержимогоТранспортногоКонтейнера.Запрос;
	ИначеЕсли ТипДокумента = "ответ" Тогда
		Если ТипСообщения = Перечисления.ТипыТранспортныхСообщений.ОтветНаЗапросПФР Тогда
			Возврат Перечисления.ТипыСодержимогоТранспортногоКонтейнера.ОтветНаЗапросПФР;
		ИначеЕсли ТипСообщения = Перечисления.ТипыТранспортныхСообщений.РезультатОбработкиЗапросаНаВыпискуЕРГЮЛ_ЕГРИП Тогда
			Возврат Перечисления.ТипыСодержимогоТранспортногоКонтейнера.ОтветНаЗапросВыпискиИзЕГРЮЛ_ЕГРИП;
		Иначе
			Возврат Перечисления.ТипыСодержимогоТранспортногоКонтейнера.Ответ;
		КонецЕсли;
		
	// 2-НДФЛ
	ИначеЕсли ТипДокумента = "форма2НДФЛ" Тогда
		Возврат Перечисления.ТипыСодержимогоТранспортногоКонтейнера.Форма2НДФЛ;
	ИначеЕсли ТипДокумента = "протоколПриема2НДФЛ" Тогда
		Возврат Перечисления.ТипыСодержимогоТранспортногоКонтейнера.Протокол2НДФЛ;
	ИначеЕсли ТипДокумента = "реестрПринятыхДокументов" Тогда
		Возврат Перечисления.ТипыСодержимогоТранспортногоКонтейнера.Реестр2НДФЛ;
	КонецЕсли;
	
	Возврат Неопределено;
	
КонецФункции

Процедура ОпределитьСтрокуВТаблицеДокументовПакета(ДокументыПакета, ИмяФайлаДокумента, АдресФайлаДокумента, ТипДокумента, ИмяФайлаПодписи = Неопределено, АдресФайлаПодписи = Неопределено, РезультатПроверкиПодписи = Неопределено, Идентификатор = Неопределено, ТипСодержимого = Неопределено, ОтпечатокСертификатаПодписи = Неопределено, ДокументЗашифрован = Неопределено, ДокументСжат = Неопределено)
	
	НовСтр = ДокументыПакета.Добавить();
	НовСтр.ИмяФайла = ИмяФайлаДокумента;
	НовСтр.АдресФайла = АдресФайлаДокумента;
	НовСтр.ТипДокумента = ТипДокумента;
	НовСтр.ИмяФайлаПодписи = ИмяФайлаПодписи;
	НовСтр.АдресФайлаПодписи = АдресФайлаПодписи;
	НовСтр.Идентификатор = Идентификатор;
	НовСтр.ТипСодержимого = ТипСодержимого;
	НовСтр.ОтпечатокСертификатаПодписи = ОтпечатокСертификатаПодписи;
	НовСтр.ДокументЗашифрован = ДокументЗашифрован;
	НовСтр.ДокументСжат = ДокументСжат;
	// Для определения алгоритма можно использовать вызов:
	// ?(ДокументЗашифрован, КриптографияЭДКО.АлгоритмКриптосообщенияИлиСертификата(АдресФайлаДокумента), "")
	НовСтр.АлгоритмФайла = Неопределено;
	// Для определения алгоритма можно использовать вызов: КриптографияЭДКО.АлгоритмКриптосообщенияИлиСертификата(АдресФайлаПодписи)
	НовСтр.АлгоритмФайлаПодписи = Неопределено;
	
КонецПроцедуры

Функция ДокументПоИдентификатору(ИдентификаторПодписанногоДокумента, КаталогВыгрузки)
	
	Запрос =Новый Запрос("ВЫБРАТЬ РАЗРЕШЕННЫЕ ПЕРВЫЕ 1
						 |	СодержимоеТранспортныхКонтейнеров.ИмяФайла,
						 |	СодержимоеТранспортныхКонтейнеров.Данные,
						 |	СодержимоеТранспортныхКонтейнеров.ТранспортноеСообщение
						 |	, ВЫБОР
						 |		КОГДА СостояниеОбъектов.Архивный ЕСТЬ NULL ТОГДА Ложь
						 |		КОГДА СостояниеОбъектов.Архивный = Ложь ТОГДА Ложь
						 |	ИНАЧЕ
						 |		Истина
						 |	КОНЕЦ ВАрхиве
						 |ИЗ
						 |	РегистрСведений.СодержимоеТранспортныхКонтейнеров КАК СодержимоеТранспортныхКонтейнеров
						 |
						 |		ЛЕВОЕ СОЕДИНЕНИЕ РегистрСведений.ПризнакиАрхивированияФайловДОСКонтролирующимиОрганами КАК СостояниеОбъектов
						 |			ПО 		(СостояниеОбъектов.Объект = СодержимоеТранспортныхКонтейнеров.ТранспортноеСообщение)
						 |				И	(СостояниеОбъектов.ИмяФайла = СодержимоеТранспортныхКонтейнеров.ИмяФайла)
						 |				И	(СостояниеОбъектов.Владелец = Значение(Перечисление.ВидыАрхивируемыхМетаданныхДО.СодержимоеТранспортныхКонтейнеров))
						 |
						 |ГДЕ
						 |	СодержимоеТранспортныхКонтейнеров.Идентификатор = &Идентификатор");
	Запрос.УстановитьПараметр("Идентификатор", ИдентификаторПодписанногоДокумента);
	Выборка = Запрос.Выполнить().Выбрать();
	Если Выборка.Следующий() Тогда
		
		Если Выборка.ВАрхиве Тогда
			Возврат Неопределено;
		КонецЕсли;
		
		Попытка
			ДвоичныеДанныеФайла = Выборка.Данные.Получить();
			Если ДвоичныеДанныеФайла = Неопределено Тогда
				МассивФайлов = ОбщегоНазначенияЭДКО.ПрикрепленныеФайлыКОбъектуИзСправочника(
					Выборка.ТранспортноеСообщение,
					"ТранспортноеСообщениеПрисоединенныеФайлы",
					Выборка.ИмяФайла);
				Если МассивФайлов.Количество() > 0 Тогда
					ДвоичныеДанныеФайла = РаботаСФайлами.ДвоичныеДанныеФайла(МассивФайлов[0]);
				Иначе
					Возврат Неопределено;
				КонецЕсли;
			КонецЕсли;
			
			ДвоичныеДанныеФайла.Записать(КаталогВыгрузки + Выборка.ИмяФайла);
			Возврат Выборка.ИмяФайла;
		Исключение
			Возврат Неопределено;
		КонецПопытки;
	Иначе
		Возврат Неопределено;
	КонецЕсли;
	
КонецФункции

Функция РазархивироватьФайлИзВременногоХранилища(
		АдресВоВременномХранилище,
		ВыводитьОшибку = Истина,
		ТипАрхива = Неопределено) Экспорт
	
	// получаем двоичные данные
	ИмяФайлаАрхиваДокумента = ПолучитьИмяВременногоФайла();
	ДвФайлАрхива = ПолучитьИзВременногоХранилища(АдресВоВременномХранилище);
	ДвФайлАрхива.Записать(ИмяФайлаАрхиваДокумента);
	// Распаковываем
	ПолноеИмяФайлаДокумента = РазархивироватьФайл(ИмяФайлаАрхиваДокумента,, ТипАрхива);
	
	Если ПолноеИмяФайлаДокумента <> Неопределено Тогда 
		ГУИД = Новый УникальныйИдентификатор;
		Попытка
			Возврат ПоместитьВоВременноеХранилище(Новый ДвоичныеДанные(ПолноеИмяФайлаДокумента), ГУИД);
		Исключение
			ЗаписьЖурналаРегистрации(
				НСтр("ru = 'Электронный документооборот с контролирующими органами. Чтение ZIP'", ОбщегоНазначения.КодОсновногоЯзыка()),
				УровеньЖурналаРегистрации.Ошибка,,,
				ПодробноеПредставлениеОшибки(ИнформацияОбОшибке()));
			Возврат Неопределено;			
		КонецПопытки;
	Иначе
		Если ВыводитьОшибку Тогда
			ТекстСообщения = НСтр("ru = 'Ошибка распаковки файла документа из состава пакета.'");
			ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
		КонецЕсли;
		ОперацииСФайламиЭДКО.УдалитьВременныйФайл(ИмяФайлаАрхиваДокумента);
		Возврат Неопределено;
	КонецЕсли;
	
КонецФункции

Функция РазархивироватьФайл(ИмяФайлаАрхива, ИмяРаспакованногоФайла = Неопределено, ТипАрхива = "zip") Экспорт
	
	Попытка
		ИмяФайлаАрхиваZip = Неопределено;
		
		Если НЕ ЗначениеЗаполнено(ТипАрхива) ИЛИ ТипАрхива = "gzip" Тогда
			МассивОписанийИДанных = ОбщегоНазначенияЭДКО.МассивОписанийИДанныхGzipФайла(ИмяФайлаАрхива, Истина);
			Если МассивОписанийИДанных = Неопределено И ТипАрхива = "gzip" Тогда
				Возврат Неопределено;
			КонецЕсли;
			
			Если МассивОписанийИДанных <> Неопределено Тогда
				МассивОписанийИДанных = ОбщегоНазначенияКлиентСервер.ЗначениеВМассиве(МассивОписанийИДанных[0]);
				ИмяФайлаАрхиваZip = ПолучитьИмяВременногоФайла();
				ОбщегоНазначенияЭДКО.СформироватьZip(МассивОписанийИДанных, ИмяФайлаАрхиваZip);
			КонецЕсли;
		КонецЕсли;
		
		ОбъектЧтение = Новый ЧтениеZipФайла(?(ИмяФайлаАрхиваZip = Неопределено, ИмяФайлаАрхива, ИмяФайлаАрхиваZip));
		Если ОбъектЧтение.Элементы.Количество() = 0 Тогда
			ОбъектЧтение.Закрыть();
			Возврат Неопределено;
		КонецЕсли;
		
		КаталогРаспаковки = ПолучитьИмяВременногоФайла();
		КаталогРаспаковки = ОбщегоНазначенияКлиентСервер.ДобавитьКонечныйРазделительПути(КаталогРаспаковки);
		
		СоздатьКаталог(КаталогРаспаковки);
		ИмяФайлаИзАрхива = ОбъектЧтение.Элементы[0].Имя;
		
		Если Не ЗначениеЗаполнено(ИмяФайлаИзАрхива) Тогда
			Возврат Неопределено;
		КонецЕсли;
		
		ОбъектЧтение.Извлечь(ОбъектЧтение.Элементы[0], КаталогРаспаковки, РежимВосстановленияПутейФайловZIP.НеВосстанавливать);
		ОбъектЧтение.Закрыть();
		Если ИмяФайлаАрхиваZip <> Неопределено Тогда
			Если НЕ ЗначениеЗаполнено(ТипАрхива) Тогда
				ТипАрхива = "gzip";
			КонецЕсли;
			ОперацииСФайламиЭДКО.УдалитьВременныйФайл(ИмяФайлаАрхиваZip);
		КонецЕсли;
		
		Если ИмяРаспакованногоФайла = Неопределено Тогда
			Возврат КаталогРаспаковки + ИмяФайлаИзАрхива;
		Иначе
			ПереместитьФайл(КаталогРаспаковки + ИмяФайлаИзАрхива, ИмяРаспакованногоФайла);
			Возврат ИмяРаспакованногоФайла;
		КонецЕсли;
	Исключение
		ЗаписьЖурналаРегистрации(
			НСтр("ru = 'Электронный документооборот с контролирующими органами'", ОбщегоНазначения.КодОсновногоЯзыка()),
			УровеньЖурналаРегистрации.Ошибка,,,
			ПодробноеПредставлениеОшибки(ИнформацияОбОшибке()));
		Возврат Неопределено;
	КонецПопытки;
	
КонецФункции

#Область ОтправкаРегламентированногоОтчетаВФНС

Функция ОтправкаРегламентированногоОтчетаВФНС_ПередВыгрузкойФайла(Контекст, Док, УникальныйИдентификатор, ТекстСообщения) Экспорт

	Контекст = Новый Структура();
	Контекст.Вставить("Документ", Док);
	Контекст.Вставить("УникальныйИдентификатор", УникальныйИдентификатор);
	
	Если НЕ ЗначениеЗаполнено(Док) Тогда
		ТекстСообщения = НСтр("ru = 'Не выбран документ для отправки.'");
		Возврат Ложь;
	КонецЕсли;
	
	// проверяем, включен ли обмен по организации
	Организация = ОбщегоНазначения.ЗначениеРеквизитаОбъекта(Док, "Организация");
	Если Организация.ВидОбменаСКонтролирующимиОрганами <> Перечисления.ВидыОбменаСКонтролирующимиОрганами.ОбменВУниверсальномФормате Тогда
		
		// Текст ошибки будет выведен в виде формы
		Контекст.Вставить("ОтсутствуетУчетнаяЗапись", Истина);

		Возврат Ложь;
		
	КонецЕсли;
	
	// проверяем, предназначена ли эта учетная запись для документооборота с ФНС
	УчетнаяЗапись = УчетнаяЗаписьОрганизации(Организация);
	Если ОбщегоНазначения.ЗначениеРеквизитаОбъекта(УчетнаяЗапись, "ПредназначенаДляДокументооборотаСФНС") <> Истина Тогда
		
		// Текст ошибки будет выведен в виде формы
		Контекст.Вставить("НаправлениеНеПодключено", Истина);
		Возврат Ложь;
		
	КонецЕсли;
	
	ТипЗнчДок = ТипЗнч(Док);
	МетаданныеОтчета = Метаданные.НайтиПоТипу(ТипЗнчДок);
	Если МетаданныеОтчета = Неопределено Тогда
		ТекстСообщения = НСтр("ru = 'Неизвестный тип отчета.'");
		Возврат Ложь;
	КонецЕсли;
	
	// ВидОтправляемогоДокументаТипДокумента заполняется и анализиризуется лишь для внешних электронных представлений
	ВидОтправляемогоДокументаТипДокумента = Неопределено;
	
	ИмяМетаданныхОтчета = МетаданныеОтчета.Имя;
	Если ИмяМетаданныхОтчета = "РегламентированныйОтчет" Тогда
		
		ЗначенияРеквизитов = ОбщегоНазначения.ЗначенияРеквизитовОбъекта(Док, "КодНалоговогоОргана, ДатаНачала, ДатаОкончания, ИсточникОтчета");
		ДатаНачалаПериодаОтчета = ЗначенияРеквизитов.ДатаНачала;
		ДатаОкончанияПериодаОтчета = ЗначенияРеквизитов.ДатаОкончания;
		КодНалоговогоОргана = СокрЛП(ЗначенияРеквизитов.КодНалоговогоОргана);
		ВидОтправляемогоОтчета = ЗначенияРеквизитов.ИсточникОтчета;
		
		НеобходимоНараститьСчетчикФайлов = Ложь;
		
	ИначеЕсли ИмяМетаданныхОтчета = "УведомлениеОПолучателеДокументов" Тогда
		
		ЗначенияРеквизитов = ОбщегоНазначения.ЗначенияРеквизитовОбъекта(Док, "Получатель");
		ТекДата = ТекущаяДатаСеанса();
		ДатаНачалаПериодаОтчета = НачалоДня(ТекДата);
		ДатаОкончанияПериодаОтчета = ДатаНачалаПериодаОтчета;
		КодНалоговогоОргана = ОбщегоНазначения.ЗначенияРеквизитовОбъекта(ЗначенияРеквизитов.Получатель, "Код").Код;
		ВидОтправляемогоОтчета = "УведомлениеОПолучателеДокументов";
		
		НеобходимоНараститьСчетчикФайлов = Ложь;
		
	ИначеЕсли ИмяМетаданныхОтчета = ЭлектронныйДокументооборотСКонтролирующимиОрганамиКлиентСерверПереопределяемый.ИмяОбъектаМетаданных("СправкиНДФЛДляПередачиВНалоговыйОрган") Тогда
		
		ДатыПериодаОтчета = ЭлектронныйДокументооборотСКонтролирующимиОрганамиВызовСервераПереопределяемый.ПолучитьДатыПериодаДокумента(Док);
		ДатаНачалаПериодаОтчета = ДатыПериодаОтчета.ДатаНачала;
		ДатаОкончанияПериодаОтчета = ДатыПериодаОтчета.ДатаОкончания;
		
		Если Метаданные.Документы[ИмяМетаданныхОтчета].Реквизиты.Найти("КодИФНС") <> Неопределено
		И ЗначениеЗаполнено(Док.КодИФНС) Тогда
			КодНалоговогоОргана = СокрЛП(Док.КодИФНС);
		Иначе
			КодНалоговогоОргана = СокрЛП(РегламентированнаяОтчетностьВызовСервера.ПолучитьСведенияОбОрганизации(Организация, , "КодНО").КодНО);
		КонецЕсли;
		
		ВидОтправляемогоОтчета = "СправкиНДФЛДляПередачиВНалоговыйОрган";
		
		НеобходимоНараститьСчетчикФайлов = Ложь;
		
	ИначеЕсли ИмяМетаданныхОтчета = ЭлектронныйДокументооборотСКонтролирующимиОрганамиКлиентСерверПереопределяемый.ИмяОбъектаМетаданных("ЗаявлениеОПодтвержденииПраваНаЗачетАвансовПоНДФЛ") Тогда
		
		ДатыПериодаОтчета = ЭлектронныйДокументооборотСКонтролирующимиОрганамиВызовСервераПереопределяемый.ПолучитьДатыПериодаДокумента(Док);
		ДатаНачалаПериодаОтчета = ДатыПериодаОтчета.ДатаНачала;
		ДатаОкончанияПериодаОтчета = ДатыПериодаОтчета.ДатаОкончания;
		
		Если Метаданные.Документы[ИмяМетаданныхОтчета].Реквизиты.Найти("КодИФНС") <> Неопределено
		И ЗначениеЗаполнено(Док.КодИФНС) Тогда
			КодНалоговогоОргана = СокрЛП(Док.КодИФНС);
		Иначе
			КодНалоговогоОргана = СокрЛП(РегламентированнаяОтчетностьВызовСервера.ПолучитьСведенияОбОрганизации(Организация, , "КодНО").КодНО);
		КонецЕсли;
		
		ВидОтправляемогоОтчета = "ЗаявлениеОПодтвержденииПраваНаЗачетАвансовПоНДФЛ";
		
		НеобходимоНараститьСчетчикФайлов = Ложь;
		
	ИначеЕсли ИмяМетаданныхОтчета = ЭлектронныйДокументооборотСКонтролирующимиОрганамиКлиентСерверПереопределяемый.ИмяОбъектаМетаданных("ЗаявлениеОВвозеТоваров") Тогда
		
		ДатыПериодаОтчета = ЭлектронныйДокументооборотСКонтролирующимиОрганамиВызовСервераПереопределяемый.ПолучитьДатыПериодаДокумента(Док);
		ДатаНачалаПериодаОтчета = ДатыПериодаОтчета.ДатаНачала;
		ДатаОкончанияПериодаОтчета = ДатыПериодаОтчета.ДатаОкончания;
		
		КодНалоговогоОргана = ЭлектронныйДокументооборотСКонтролирующимиОрганамиВызовСервераПереопределяемый.ПолучитьКодИФНСПолучателяПоСсылке(Док);
		
		ВидОтправляемогоОтчета = "ЗаявлениеОВвозеТоваров";
		
		НеобходимоНараститьСчетчикФайлов = Ложь;
		
	ИначеЕсли ИмяМетаданныхОтчета = "ЭлектронныеПредставленияРегламентированныхОтчетов" Тогда // внешнее электронное представление
		
		ДатаНачалаПериодаОтчета = Док.ДатаНачала;
		ДатаОкончанияПериодаОтчета = Док.ДатаОкончания;
		КодНалоговогоОргана = СокрЛП(Док.Получатель.Код);
		ВидОтправляемогоОтчета = Док.ВидОтчета.Источник;
		
		ВидОтправляемогоДокументаТипДокумента = Док.ВидОтчета.ТипДокумента;
		
		Если ВидОтправляемогоДокументаТипДокумента = Перечисления.ТипыОтправляемыхДокументов.ИсходящееУведомлениеФНС
			И (Док.ВидОтчета = Справочники.ВидыОтправляемыхДокументов.УведомлениеОсвобождениеНалогНаПрибыльСколково
			И ОнлайнСервисыРегламентированнойОтчетностиВызовСервера.СобытиеНаступило("Отпр.ув.осв.приб.Сколк.01")
			ИЛИ Док.ВидОтчета = Справочники.ВидыОтправляемыхДокументов.УведомлениеОсвобождениеНДССколково
			И ОнлайнСервисыРегламентированнойОтчетностиВызовСервера.СобытиеНаступило("Отпр.ув.осв.НДС Сколк.01")) Тогда
			
			ВидОтправляемогоДокументаТипДокумента = Перечисления.ТипыОтправляемыхДокументов.ПрочаяОтчетностьФНС;
		КонецЕсли;
		
		НеобходимоНараститьСчетчикФайлов = Истина;
		
	ИначеЕсли ИмяМетаданныхОтчета = ЭлектронныйДокументооборотСКонтролирующимиОрганамиКлиентСерверПереопределяемый.ИмяОбъектаМетаданных("БухгалтерскийОтчетГосударственногоУчреждения") Тогда
		
		ДатыПериодаОтчета = ЭлектронныйДокументооборотСКонтролирующимиОрганамиВызовСервераПереопределяемый.ПолучитьДатыПериодаДокумента(Док);
		ДатаНачалаПериодаОтчета = ДатыПериодаОтчета.ДатаНачала;
		ДатаОкончанияПериодаОтчета = ДатыПериодаОтчета.ДатаОкончания;
		
		КодНалоговогоОргана = ЭлектронныйДокументооборотСКонтролирующимиОрганамиВызовСервераПереопределяемый.ПолучитьКодИФНСПолучателяПоСсылке(Док);
		ВидОтправляемогоОтчета = "";
		
		НеобходимоНараститьСчетчикФайлов = Ложь;
		
	Иначе
		
		ТекстСообщения = НСтр("ru = 'Неизвестный тип отчета.'");
		Возврат Ложь;
		
	КонецЕсли;
	
	// инициализируем переменные, зависящие от типа отчета
	Если ВидОтправляемогоОтчета = "СправкиНДФЛДляПередачиВНалоговыйОрган" Тогда
		ТипЦиклаОбмена = Перечисления.ТипыЦикловОбмена.Форма2НДФЛ;
		ТипТранспортногоСообщения = Перечисления.ТипыТранспортныхСообщений.Форма2НДФЛНП;
		ТипСодержимогоКонтейнера = Перечисления.ТипыСодержимогоТранспортногоКонтейнера.Форма2НДФЛ;
	ИначеЕсли ВидОтправляемогоОтчета = "РегламентированныйОтчетЗаявлениеОВвозеТоваров"
	ИЛИ ВидОтправляемогоОтчета = "ЗаявлениеОВвозеТоваров" Тогда
		ТипЦиклаОбмена = Перечисления.ТипыЦикловОбмена.Заявление;
		ТипТранспортногоСообщения = Перечисления.ТипыТранспортныхСообщений.ЗаявлениеНП;
		ТипСодержимогоКонтейнера = Перечисления.ТипыСодержимогоТранспортногоКонтейнера.Заявление;
	ИначеЕсли ВидОтправляемогоОтчета = "РегламентированныйОтчетРозничныеЦеныНаТабак"
	ИЛИ ВидОтправляемогоОтчета = "РегламентированныйОтчетПереченьЗаявленийОВвозеТоваров"
	ИЛИ ВидОтправляемогоОтчета = "РегламентированныйОтчетСтрановойОтчет"
	ИЛИ ВидОтправляемогоОтчета = "РегламентированныйОтчетУчастиеВМеждународнойГруппеКомпаний"
	ИЛИ ВидОтправляемогоОтчета = "РегламентированныйОтчетДоходыОтДеятельностиМузеяТеатраБиблиотеки"
	ИЛИ ВидОтправляемогоОтчета = "РегламентированныйОтчетДвижениеСредствПоСчетуВБанкеЗаПределамиРФ"
	ИЛИ ВидОтправляемогоОтчета = "РегламентированныйОтчетРеестрНДСДокументыДляКомпенсацииНалога"
	ИЛИ ВидОтправляемогоОтчета = "УведомлениеОПолучателеДокументов"
	ИЛИ ВидОтправляемогоОтчета = "ЗаявлениеОПодтвержденииПраваНаЗачетАвансовПоНДФЛ"
	ИЛИ ВидОтправляемогоДокументаТипДокумента = Перечисления.ТипыОтправляемыхДокументов.УведомлениеОКонтролируемыхСделках
	ИЛИ ВидОтправляемогоДокументаТипДокумента = Перечисления.ТипыОтправляемыхДокументов.ИсходящееУведомлениеФНС
	ИЛИ ВидОтправляемогоДокументаТипДокумента = Перечисления.ТипыОтправляемыхДокументов.УведомлениеОРозничныхЦенахНаТабак Тогда
		ТипЦиклаОбмена = Перечисления.ТипыЦикловОбмена.Представление;
		ТипТранспортногоСообщения = Перечисления.ТипыТранспортныхСообщений.ПредставлениеНП;
		ТипСодержимогоКонтейнера = Перечисления.ТипыСодержимогоТранспортногоКонтейнера.Представление;
	Иначе
		ТипЦиклаОбмена = Перечисления.ТипыЦикловОбмена.НалоговаяИлиБухгалтерскаяОтчетность;
		ТипТранспортногоСообщения = Перечисления.ТипыТранспортныхСообщений.ДекларацияНП;
		ТипСодержимогоКонтейнера = Перечисления.ТипыСодержимогоТранспортногоКонтейнера.ФайлОтчетности;
	КонецЕсли;
	
	// сертификаты подписи абонента
	СертификатРуководителя = УчетнаяЗапись.СертификатРуководителя;
	СертификатГлавногоБухгалтера = УчетнаяЗапись.СертификатГлавногоБухгалтера;
	СертификатДляШифрования = УчетнаяЗапись.СертификатДляШифрования;
	
	Контекст.Вставить("Организация",                          Организация);
	Контекст.Вставить("ДатаНачалаПериодаОтчета",              ДатаНачалаПериодаОтчета);
	Контекст.Вставить("ДатаОкончанияПериодаОтчета",           ДатаОкончанияПериодаОтчета);
	Контекст.Вставить("КодНалоговогоОргана",                  КодНалоговогоОргана);
	Контекст.Вставить("ВидОтправляемогоОтчета",               ВидОтправляемогоОтчета);
	Контекст.Вставить("ТипОтправляемогоДокумента",            ИмяМетаданныхОтчета);
	Контекст.Вставить("ТипЦиклаОбмена",                       ТипЦиклаОбмена);
	Контекст.Вставить("ТипТранспортногоСообщения",            ТипТранспортногоСообщения);
	Контекст.Вставить("ТипСодержимогоКонтейнера",             ТипСодержимогоКонтейнера);
	Контекст.Вставить("НеобходимоНараститьСчетчикФайлов",     НеобходимоНараститьСчетчикФайлов);
	Контекст.Вставить("УчетнаяЗапись",                        УчетнаяЗапись);
	Контекст.Вставить("СертификатРуководителя",               СертификатРуководителя);
	Контекст.Вставить("СертификатГлавногоБухгалтера",         СертификатГлавногоБухгалтера);
	Контекст.Вставить("СертификатОтветственногоЗаШифрование", СертификатДляШифрования);
	
	Возврат Истина;
	
КонецФункции

Функция ОтправкаРегламентированногоОтчетаВФНС_ОпределитьЦиклОбменаИСообщениеВИБ(Контекст) Экспорт
	
	// формируем цикл обмена
	ЦиклОбмена = Справочники.ЦиклыОбмена.СоздатьЭлемент();
	ЦиклОбменаСсылка = Справочники.ЦиклыОбмена.ПолучитьСсылку();
	ЦиклОбмена.УстановитьСсылкуНового(ЦиклОбменаСсылка);
	ЦиклОбмена.Вид = Перечисления.ВидыЦикловОбмена.ЦиклОбменаСФНС;
	ЦиклОбмена.ФорматДокументооборота = Перечисления.ФорматыДокументооборотаСФНС.Приказ534;
	ЦиклОбмена.Предмет = Контекст.Документ;
	ЦиклОбмена.Идентификатор = СгенерироватьUUID();
	
	ТипОтправляемогоДокумента = Контекст.ТипОтправляемогоДокумента;
	Если ТипОтправляемогоДокумента = "РегламентированныйОтчет" ИЛИ ТипОтправляемогоДокумента = "УведомлениеОПолучателеДокументов"
		ИЛИ ТипОтправляемогоДокумента = ЭлектронныйДокументооборотСКонтролирующимиОрганамиКлиентСерверПереопределяемый.ИмяОбъектаМетаданных("СправкиНДФЛДляПередачиВНалоговыйОрган")
		ИЛИ ТипОтправляемогоДокумента = ЭлектронныйДокументооборотСКонтролирующимиОрганамиКлиентСерверПереопределяемый.ИмяОбъектаМетаданных("ЗаявлениеОПодтвержденииПраваНаЗачетАвансовПоНДФЛ") Тогда
		
		РезультатПоиска = Справочники.ВидыОтправляемыхДокументов.НайтиПоРеквизиту("Источник", Контекст.ВидОтправляемогоОтчета);
		Если РезультатПоиска = Справочники.ВидыОтправляемыхДокументов.ПустаяСсылка() Тогда
			ТекстСообщения = СтроковыеФункцииКлиентСервер.ПодставитьПараметрыВСтроку(
								НСтр("ru = 'В справочнике ""Виды отправляемых документов"" не найден элемент с именем объекта метаданных %1!'"),
									Символ(34) + Контекст.ВидОтправляемогоОтчета + Символ(34));
			ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
			Возврат Ложь;
		КонецЕсли;
		ЦиклОбмена.ВидОтчета = РезультатПоиска;
		ЦиклОбмена.Тип = Контекст.ТипЦиклаОбмена;
		Если ТипОтправляемогоДокумента = "РегламентированныйОтчет" Тогда
			ЦиклОбмена.ВидДокумента = Контекст.Документ.Вид;
		КонецЕсли;
		
	ИначеЕсли ТипОтправляемогоДокумента = ЭлектронныйДокументооборотСКонтролирующимиОрганамиКлиентСерверПереопределяемый.ИмяОбъектаМетаданных("БухгалтерскийОтчетГосударственногоУчреждения") Тогда
		
		ЦиклОбмена.ВидОтчета = ЭлектронныйДокументооборотСКонтролирующимиОрганамиВызовСервераПереопределяемый.ПолучитьВидОтправляемогоДокументаПоСсылке(Контекст.Документ);
	    ЦиклОбмена.Тип = Контекст.ТипЦиклаОбмена;
		
	ИначеЕсли ТипОтправляемогоДокумента = ЭлектронныйДокументооборотСКонтролирующимиОрганамиКлиентСерверПереопределяемый.ИмяОбъектаМетаданных("ЗаявлениеОВвозеТоваров") Тогда
		
		ЦиклОбмена.ВидОтчета = Справочники.ВидыОтправляемыхДокументов.ЗаявлениеОВвозеТоваров;
	    ЦиклОбмена.Тип = Контекст.ТипЦиклаОбмена;
		
	Иначе
		
		ЦиклОбмена.ВидОтчета = Контекст.Документ.ВидОтчета;
		ЦиклОбмена.ВидДокумента = Контекст.Документ.Версия;
		ЦиклОбмена.Тип = Контекст.ТипЦиклаОбмена;
		
	КонецЕсли;
	
	ЦиклОбмена.ДатаНачалаПериода = Контекст.ДатаНачалаПериодаОтчета;
	ЦиклОбмена.ДатаОкончанияПериода = Контекст.ДатаОкончанияПериодаОтчета;
	ЦиклОбмена.Организация = Контекст.Организация;
	ЦиклОбмена.УчетнаяЗапись = Контекст.УчетнаяЗапись;
	
	// указываем ту инспекцию, в которую должен быть представлен отчет
	Если НЕ ЗначениеЗаполнено(Контекст.КодНалоговогоОргана) Тогда
		ТекстСообщения = НСтр("ru = 'Не удалось определить налоговый орган, в который представляется отчетность!'");
		ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
		Возврат Ложь;
	КонецЕсли;
	Если ТипЗнч(Контекст.КодНалоговогоОргана) = Тип("Строка") Тогда
		ИФНС = Справочники.НалоговыеОрганы.НайтиПоКоду(Контекст.КодНалоговогоОргана);
		Если ИФНС = Справочники.НалоговыеОрганы.ПустаяСсылка() Тогда
			ТекстСообщения = СтроковыеФункцииКлиентСервер.ПодставитьПараметрыВСтроку(
				НСтр("ru = 'Не найден налоговый орган с кодом %1 в справочнике ""Налоговые органы""!'"), Контекст.КодНалоговогоОргана);
			ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
			Возврат Ложь;
		КонецЕсли;
	Иначе
		ИФНС = Контекст.КодНалоговогоОргана;
	КонецЕсли;
	
	ЦиклОбмена.ВнешняяОрганизация = ИФНС;
	
	// формируем первичное транспортное сообщение
	ТранспортноеСообщение = Документы.ТранспортноеСообщение.СоздатьДокумент();
	ТранспортноеСообщение.ЦиклОбмена = ЦиклОбменаСсылка;
	ТранспортноеСообщение.Тип = Контекст.ТипТранспортногоСообщения;
	ТранспортноеСообщение.Отправитель = Контекст.Организация;
	ТранспортноеСообщение.Получатель = ?(НЕ Контекст.УчетнаяЗапись.ОбменНапрямую, Контекст.УчетнаяЗапись.СерверДокументооборота, Контекст.УчетнаяЗапись.НалоговыйОрган);
	ТранспортноеСообщение.УчетнаяЗапись = Контекст.УчетнаяЗапись;
	ТранспортноеСообщение.Статус = Перечисления.СтатусыПисем.Исходящее;
	Попытка
		ТранспортноеСообщение.Записать();
	Исключение
		ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ИнформацияОбОшибке().Описание);
		ТекстСообщения = НСтр("ru = 'Отправка отчета прервана.'");
		ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
		Возврат Ложь;
	КонецПопытки;
	
	// ДОБАВЛЯЕМ ВЛОЖЕНИЯ
	
	// определяем кодировку и тип содержимого
	Если нрег(Прав(Контекст.ИмяФайлаВыгрузки, 4)) = ".xml" Тогда
		КодировкаФайла = "windows-1251";
		ТипСодержимого = Перечисления.ТипыСодержимогоФайлов.xml;
	Иначе
		КодировкаФайла = "cp866";
		ТипСодержимого = Перечисления.ТипыСодержимогоФайлов.Текст866;
	КонецЕсли;
	
	// сохраняем выгрузку во временный файл
	ИмяВремФайла = ПолучитьИмяВременногоФайла();
	Если ЭтоАдресВременногоХранилища(Контекст.ТекстВыгрузки) Тогда
		ДвоичныеДанныеФайлаВыгрузки = ПолучитьИзВременногоХранилища(Контекст.ТекстВыгрузки);
		ДвоичныеДанныеФайлаВыгрузки.Записать(ИмяВремФайла);
	Иначе
		Текст = Новый ТекстовыйДокумент;
		Текст.УстановитьТекст(Контекст.ТекстВыгрузки);
		Текст.Записать(ИмяВремФайла, КодировкаФайла);
	КонецЕсли;
	
	// добавляем ДИВ
	Если НЕ ДобавитьСодержимоеТранспортногоКонтейнера(ТранспортноеСообщение.Ссылка, Контекст.ТипСодержимогоКонтейнера, ИмяВремФайла, Контекст.ИмяФайлаВыгрузки, , , , , , , СгенерироватьUUID(), ТипСодержимого) Тогда
		ТекстСообщения = НСтр("ru = 'Отправка отчета прервана.'");
		ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
		Возврат Ложь;
	КонецЕсли;
	
	// добавляем приложения, если они есть
	Если Контекст.Свойство("ПриложенияКБО") Тогда
		Для Каждого СтрПриложениеКБО Из Контекст.ПриложенияКБО Цикл
			Если НЕ ДобавитьСодержимоеТранспортногоКонтейнера(ТранспортноеСообщение.Ссылка, Перечисления.ТипыСодержимогоТранспортногоКонтейнера.Приложение, СтрПриложениеКБО.ПолноеИмяФайла, СтрПриложениеКБО.ИмяФайла, , , , , , , СгенерироватьUUID(), СтрПриложениеКБО.ТипСодержимого) Тогда
				ТекстСообщения = НСтр("ru = 'Отправка отчета прервана.'");
				ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
				Возврат Ложь;
			КонецЕсли;
		КонецЦикла;
	КонецЕсли;
		
	// добавляем приложения, если они есть
	Если Контекст.Свойство("Приложения") Тогда
		Для Каждого СтрПриложение Из Контекст.Приложения Цикл
			ТипСодержимогоФайла = "xml";
			РасширениеПриложения = ".xml";
			ИмяФайлаПриведенное = ВРЕГ(СтрПриложение.ИмяФайлаВыгрузки);
			Если Лев(ИмяФайлаПриведенное, 3) = "PR_" Тогда // нетекстовые приложения есть у уведомления о невозможности предоставления документов в срок
				ТипСодержимогоФайла = ТипСодержимогоПоИмениФайла(СтрПриложение.ИмяФайлаВыгрузки);
				РасширениеПриложения = ОбщегоНазначенияКлиентСервер.РазложитьПолноеИмяФайла(СтрПриложение.ИмяФайлаВыгрузки).Расширение;
			КонецЕсли;
			
			ПолноеИмяФайла = ПолучитьИмяВременногоФайла(РасширениеПриложения);
			ДвоичныеДанныеФайла = ПолучитьИзВременногоХранилища(СтрПриложение.АдресФайлаВыгрузки);
			ДвоичныеДанныеФайла.Записать(ПолноеИмяФайла);
			УдалитьИзВременногоХранилища(СтрПриложение.АдресФайлаВыгрузки);
						
			РезультатДобавления = ДобавитьСодержимоеТранспортногоКонтейнера(
				ТранспортноеСообщение.Ссылка,
				Перечисления.ТипыСодержимогоТранспортногоКонтейнера.Приложение,
				ПолноеИмяФайла,
				СтрПриложение.ИмяФайлаВыгрузки,,,,,,,
				СгенерироватьUUID(),				
				ТипСодержимогоФайла);
				
			УдалитьФайлы(ПолноеИмяФайла);
			
			Если НЕ РезультатДобавления Тогда
				ТекстСообщения = НСтр("ru = 'Отправка отчета прервана.'");
				ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
				Возврат Ложь;
			КонецЕсли;
		КонецЦикла;
	КонецЕсли;
	
	// добавляем доверенность, если она есть
	Если ЗначениеЗаполнено(Контекст.ФайлДоверенности) Тогда
		ДоверенностьТипСодержимого = Перечисления.ТипыСодержимогоТранспортногоКонтейнера.ДоверенностьПредставление;
		
		Если НЕ ДобавитьСодержимоеТранспортногоКонтейнера(ТранспортноеСообщение.Ссылка, ДоверенностьТипСодержимого, Контекст.ФайлДоверенности, Контекст.КороткоеИмяФайлаДоверенности, , , , , , , СгенерироватьUUID(), Перечисления.ТипыСодержимогоФайлов.xml) Тогда
			ТекстСообщения = НСтр("ru = 'Отправка отчета прервана.'");
			ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
			Возврат Ложь;
		КонецЕсли;
	КонецЕсли;
	
	// пишем цикл обмена
	Попытка
		ЦиклОбмена.Записать();
	Исключение
		ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ИнформацияОбОшибке().Описание);
		ТекстСообщения = НСтр("ru = 'Отправка отчета прервана.'");
		ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
		Возврат Ложь;
	КонецПопытки;
	
	Контекст.Вставить("ЦиклОбмена", ЦиклОбмена.Ссылка);
	Контекст.Вставить("ТранспортноеСообщение", ТранспортноеСообщение.Ссылка);
	
	// формируем файл описания сведений, если его нет
	РезультатФормирования = СформироватьОписаниеСведенийДляСообщенияФНС(Контекст);
	Если НЕ РезультатФормирования Тогда
		ТекстСообщения = НСтр("ru = 'Отправка отчета прервана.'");
		ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
		Возврат Ложь;
	КонецЕсли;
	
	Возврат Истина;
	
КонецФункции

Функция СформироватьОписаниеСведенийДляСообщенияФНС(Контекст) Экспорт
	
	Сообщение = Контекст.ТранспортноеСообщение;
	
	Если Сообщение.Тип = Перечисления.ТипыТранспортныхСообщений.Форма2НДФЛНП Тогда
		
		// получаем список вложений сообщения
		ТипыВложений = Новый Массив;
		ТипыВложений.Добавить(Перечисления.ТипыСодержимогоТранспортногоКонтейнера.Форма2НДФЛ);
		ТипыВложений.Добавить(Перечисления.ТипыСодержимогоТранспортногоКонтейнера.ДоверенностьПредставление);
		ТипыВложений.Добавить(Перечисления.ТипыСодержимогоТранспортногоКонтейнера.ОписаниеСведений2НДФЛ);
		Вложения = ПолучитьВложенияТранспортногоСообщения(Сообщение, , ТипыВложений);
		
		// если описание сведений уже сформировано, то прервемся
		Если Вложения.Найти(Перечисления.ТипыСодержимогоТранспортногоКонтейнера.ОписаниеСведений2НДФЛ, "Тип") <> Неопределено Тогда
			Возврат Истина;
		КонецЕсли;
		
		// генерируем файл описания сведений
		КороткоеИмяФайла = Неопределено;
		ФайлОписаниеСведений = СформироватьОписаниеСведений2НДФЛ(Сообщение, Вложения, КороткоеИмяФайла);
		Если ФайлОписаниеСведений = Неопределено Тогда
			Возврат Ложь;
		КонецЕсли;
		
		// загружаем файл в содержимое пакета
		ДобавитьСодержимоеТранспортногоКонтейнера(Сообщение, Перечисления.ТипыСодержимогоТранспортногоКонтейнера.ОписаниеСведений2НДФЛ, ФайлОписаниеСведений, "TR_NDFL2.xml", , , , , , , СгенерироватьUUID(), Перечисления.ТипыСодержимогоФайлов.xml);
		
	ИначеЕсли Сообщение.Тип = Перечисления.ТипыТранспортныхСообщений.ОбращениеНП Тогда
		
		// получаем список вложений сообщения
		ТипыВложений = Новый Массив;
		ТипыВложений.Добавить(Перечисления.ТипыСодержимогоТранспортногоКонтейнера.Обращение);
		ТипыВложений.Добавить(Перечисления.ТипыСодержимогоТранспортногоКонтейнера.Приложение);
		ТипыВложений.Добавить(Перечисления.ТипыСодержимогоТранспортногоКонтейнера.ДоверенностьПредставление);
		ТипыВложений.Добавить(Перечисления.ТипыСодержимогоТранспортногоКонтейнера.ОписаниеСведенийОбращениеПисьмоРассылка);
		Вложения = ПолучитьВложенияТранспортногоСообщения(Сообщение, , ТипыВложений);
		
		// если описание сведений уже сформировано, то прервемся
		Если Вложения.Найти(Перечисления.ТипыСодержимогоТранспортногоКонтейнера.ОписаниеСведенийОбращениеПисьмоРассылка, "Тип") <> Неопределено Тогда
			Возврат Истина;
		КонецЕсли;
		
		// генерируем файл описания сведений
		КороткоеИмяФайла = Неопределено;
		ФайлОписаниеСведений = СформироватьОписаниеСведенийОбращенияПисьмаРассылки(Сообщение, Вложения, КороткоеИмяФайла);
		Если ФайлОписаниеСведений = Неопределено Тогда
			Возврат Ложь;
		КонецЕсли;
		
		// загружаем файл в содержимое пакета
		ДобавитьСодержимоеТранспортногоКонтейнера(Сообщение, Перечисления.ТипыСодержимогоТранспортногоКонтейнера.ОписаниеСведенийОбращениеПисьмоРассылка, ФайлОписаниеСведений, "TR_PISRAS.xml", , , , , , , СгенерироватьUUID(), Перечисления.ТипыСодержимогоФайлов.xml);
		
	ИначеЕсли Сообщение.Тип = Перечисления.ТипыТранспортныхСообщений.ПредставлениеНП Тогда
		
		// получаем список вложений сообщения
		ТипыВложений = Новый Массив;
		ТипыВложений.Добавить(Перечисления.ТипыСодержимогоТранспортногоКонтейнера.Представление);
		ТипыВложений.Добавить(Перечисления.ТипыСодержимогоТранспортногоКонтейнера.ОписаниеПредставление);
		Вложения = ПолучитьВложенияТранспортногоСообщения(Сообщение, , ТипыВложений);
		
		// если описание сведений уже сформировано, то прервемся
		Если Вложения.Найти(Перечисления.ТипыСодержимогоТранспортногоКонтейнера.ОписаниеПредставление, "Тип") <> Неопределено Тогда
			Возврат Истина;
		КонецЕсли;

		// генерируем файл описания сведений
		КороткоеИмяФайла = Неопределено;
		ФайлОписаниеСведений = СформироватьОписаниеСведенийПредставлениеНП(Сообщение, Вложения, КороткоеИмяФайла);
		Если ФайлОписаниеСведений = Неопределено Тогда
			Возврат Ложь;
		КонецЕсли;
		
		// загружаем файл в содержимое пакета
		ДобавитьСодержимоеТранспортногоКонтейнера(Сообщение, Перечисления.ТипыСодержимогоТранспортногоКонтейнера.ОписаниеПредставление, ФайлОписаниеСведений, "TR_PROTDOC.xml", , , , , , , СгенерироватьUUID(), Перечисления.ТипыСодержимогоФайлов.xml);	
		
	ИначеЕсли Сообщение.Тип = Перечисления.ТипыТранспортныхСообщений.ЗапросНП Тогда
		
		// получаем список вложений сообщения
		ТипыВложений = Новый Массив;
		ТипыВложений.Добавить(Перечисления.ТипыСодержимогоТранспортногоКонтейнера.Запрос);
		ТипыВложений.Добавить(Перечисления.ТипыСодержимогоТранспортногоКонтейнера.ДоверенностьПредставление);
		ТипыВложений.Добавить(Перечисления.ТипыСодержимогоТранспортногоКонтейнера.ОписаниеСведенийЗапрос);
		Вложения = ПолучитьВложенияТранспортногоСообщения(Сообщение, , ТипыВложений);
		
		// если описание сведений уже сформировано, то прервемся
		Если Вложения.Найти(Перечисления.ТипыСодержимогоТранспортногоКонтейнера.ОписаниеСведенийЗапрос, "Тип") <> Неопределено Тогда
			Возврат Истина;
		КонецЕсли;
		
		// генерируем файл описания сведений
		КороткоеИмяФайла = Неопределено;
		ФайлОписаниеСведений = СформироватьОписаниеСведенийЗапроса(Сообщение, Вложения, КороткоеИмяФайла);
		Если ФайлОписаниеСведений = Неопределено Тогда
			Возврат Ложь;
		КонецЕсли;
		
		// загружаем файл в содержимое пакета
		ДобавитьСодержимоеТранспортногоКонтейнера(Сообщение, Перечисления.ТипыСодержимогоТранспортногоКонтейнера.ОписаниеСведенийЗапрос, ФайлОписаниеСведений, "TR_ZAPION.xml", , , , , , , СгенерироватьUUID(), Перечисления.ТипыСодержимогоФайлов.xml);
		
	ИначеЕсли Сообщение.Тип = Перечисления.ТипыТранспортныхСообщений.ЗаявлениеНП Тогда
		
		// получаем список вложений сообщения
		ТипыВложений = Новый Массив;
		ТипыВложений.Добавить(Перечисления.ТипыСодержимогоТранспортногоКонтейнера.Заявление);
		ТипыВложений.Добавить(Перечисления.ТипыСодержимогоТранспортногоКонтейнера.ДоверенностьПредставление);
		ТипыВложений.Добавить(Перечисления.ТипыСодержимогоТранспортногоКонтейнера.ОписаниеСведенийЗаявление);
		Вложения = ПолучитьВложенияТранспортногоСообщения(Сообщение, , ТипыВложений);
		
		// если описание сведений уже сформировано, то прервемся
		Если Вложения.Найти(Перечисления.ТипыСодержимогоТранспортногоКонтейнера.ОписаниеСведенийЗаявление, "Тип") <> Неопределено Тогда
			Возврат Истина;
		КонецЕсли;
		
		// генерируем файл описания сведений
		КороткоеИмяФайла = Неопределено;
		ФайлОписаниеСведений = СформироватьОписаниеСведенийЗаявление(Сообщение, Вложения, КороткоеИмяФайла);
		Если ФайлОписаниеСведений = Неопределено Тогда
			Возврат Ложь;
		КонецЕсли;
		
		// загружаем файл в содержимое пакета
		ДобавитьСодержимоеТранспортногоКонтейнера(Сообщение, Перечисления.ТипыСодержимогоТранспортногоКонтейнера.ОписаниеСведенийЗаявление, ФайлОписаниеСведений, "TR_ZVLRPOK.xml", , , , , , , СгенерироватьUUID(), Перечисления.ТипыСодержимогоФайлов.xml);
		
	Иначе
		
		// получаем список вложений сообщения
		ТипыВложений = Новый Массив;
		ТипыВложений.Добавить(Перечисления.ТипыСодержимогоТранспортногоКонтейнера.ФайлОтчетности);
		ТипыВложений.Добавить(Перечисления.ТипыСодержимогоТранспортногоКонтейнера.Приложение);
		ТипыВложений.Добавить(Перечисления.ТипыСодержимогоТранспортногоКонтейнера.ДоверенностьПредставление);
		ТипыВложений.Добавить(Перечисления.ТипыСодержимогоТранспортногоКонтейнера.ОписаниеСведений);
		Вложения = ПолучитьВложенияТранспортногоСообщения(Сообщение, , ТипыВложений);
		
		// если описание сведений уже сформировано, то прервемся
		Если Вложения.Найти(Перечисления.ТипыСодержимогоТранспортногоКонтейнера.ОписаниеСведений, "Тип") <> Неопределено Тогда
			Возврат Истина;
		КонецЕсли;
		
		// генерируем файл описания сведений
		КороткоеИмяФайла = Неопределено;
		ФайлОписаниеСведений = СформироватьОписаниеСведенийФНС(Сообщение, Вложения, КороткоеИмяФайла);
		Если ФайлОписаниеСведений = Неопределено Тогда
			Возврат Ложь;
		КонецЕсли;
		
		// загружаем файл в содержимое пакета
		ДобавитьСодержимоеТранспортногоКонтейнера(Сообщение, Перечисления.ТипыСодержимогоТранспортногоКонтейнера.ОписаниеСведений, ФайлОписаниеСведений, "TR_DEKL.xml", , , , , , , СгенерироватьUUID(), Перечисления.ТипыСодержимогоФайлов.xml);
		
	КонецЕсли;
	
	Возврат Истина;
	
КонецФункции

Функция СформироватьИзвещениеОПолученииДляСообщенияФНС(Контекст) Экспорт
	
	ТранспортноеСообщение = Контекст.ТранспортноеСообщение;
	
	// проверяем, не сформировано ли уже извещение
	ИмеющиесяИзвещения = ПолучитьВложенияТранспортногоСообщения(ТранспортноеСообщение, Ложь, Перечисления.ТипыСодержимогоТранспортногоКонтейнера.ИзвещениеОПолучении);
	Если ИмеющиесяИзвещения.Количество() > 0 Тогда
		Возврат Истина;
	КонецЕсли;
	
	// определяем возможные типы документа, извещение о получении которого формируется
	Если ТранспортноеСообщение.Тип = Перечисления.ТипыТранспортныхСообщений.ИзвещениеПодтверждениеНП Тогда
		ТипыДИВ = Новый Массив;
		ТипыДИВ.Добавить(Перечисления.ТипыСодержимогоТранспортногоКонтейнера.ПодтверждениеДатыОтправки);
		ТипыДИВ.Добавить(Перечисления.ТипыСодержимогоТранспортногоКонтейнера.ПодтверждениеДатыОтправкиПредставление);
		ТипыДИВ.Добавить(Перечисления.ТипыСодержимогоТранспортногоКонтейнера.ПодтверждениеДатыПолучения);
	ИначеЕсли ТранспортноеСообщение.Тип = Перечисления.ТипыТранспортныхСообщений.ИзвещениеРезультатПриемаНП Тогда
		ТипыДИВ = Новый Массив;
		ТипыДИВ.Добавить(Перечисления.ТипыСодержимогоТранспортногоКонтейнера.КвитанцияОПриеме);
		ТипыДИВ.Добавить(Перечисления.ТипыСодержимогоТранспортногоКонтейнера.КвитанцияОПриемеДокумент);
		ТипыДИВ.Добавить(Перечисления.ТипыСодержимогоТранспортногоКонтейнера.КвитанцияОПриемеЗаявления);
		ТипыДИВ.Добавить(Перечисления.ТипыСодержимогоТранспортногоКонтейнера.УведомлениеОбОтказе);
		ТипыДИВ.Добавить(Перечисления.ТипыСодержимогоТранспортногоКонтейнера.УведомлениеОбОтказеДокумент);
		ТипыДИВ.Добавить(Перечисления.ТипыСодержимогоТранспортногоКонтейнера.Протокол2НДФЛ);
		ТипыДИВ.Добавить(Перечисления.ТипыСодержимогоТранспортногоКонтейнера.Реестр2НДФЛ);
	ИначеЕсли ТранспортноеСообщение.Тип = Перечисления.ТипыТранспортныхСообщений.ИзвещениеРезультатОбработкиНП Тогда
		ТипыДИВ = Новый Массив;
		ТипыДИВ.Добавить(Перечисления.ТипыСодержимогоТранспортногоКонтейнера.ИзвещениеОВводе);
		ТипыДИВ.Добавить(Перечисления.ТипыСодержимогоТранспортногоКонтейнера.УведомлениеОбУточнении);
		ТипыДИВ.Добавить(Перечисления.ТипыСодержимогоТранспортногоКонтейнера.Ответ);
		
		//Заявление
		ТипыДИВ.Добавить(Перечисления.ТипыСодержимогоТранспортногоКонтейнера.СообщениеОПростОтметки);
		ТипыДИВ.Добавить(Перечисления.ТипыСодержимогоТранспортногоКонтейнера.УведомлениеОбОтказеОтметки);
	ИначеЕсли ТранспортноеСообщение.Тип = Перечисления.ТипыТранспортныхСообщений.ИзвещениеРезультатОбработкиРФНП Тогда
		ТипыДИВ = Перечисления.ТипыСодержимогоТранспортногоКонтейнера.СообщениеОПроверке;
	ИначеЕсли ТранспортноеСообщение.Тип = Перечисления.ТипыТранспортныхСообщений.ИзвещениеОбОтзывеЗаявлениеРФНП Тогда
		ТипыДИВ = Перечисления.ТипыСодержимогоТранспортногоКонтейнера.СообщениеОбОтзывеЗаявления;
	ИначеЕсли ТранспортноеСообщение.Тип = Перечисления.ТипыТранспортныхСообщений.ИзвещениеРезультатОбработкиТСНП Тогда
		ТипыДИВ = Перечисления.ТипыСодержимогоТранспортногоКонтейнера.СообщениеОНесоответствиях;
	ИначеЕсли ТранспортноеСообщение.Тип = Перечисления.ТипыТранспортныхСообщений.ИзвещениеДокументНП Тогда
		ТипыДИВ = Новый Массив;
		ТипыДИВ.Добавить(Перечисления.ТипыСодержимогоТранспортногоКонтейнера.Документ);
		ТипыДИВ.Добавить(Перечисления.ТипыСодержимогоТранспортногоКонтейнера.Приложение);
		ТипыДИВ.Добавить(Перечисления.ТипыСодержимогоТранспортногоКонтейнера.ПодтверждениеДатыОтправки);
	ИначеЕсли ТранспортноеСообщение.Тип = Перечисления.ТипыТранспортныхСообщений.ИзвещениеПисьмоНП Тогда
		ТипыДИВ = Новый Массив;
		ТипыДИВ.Добавить(Перечисления.ТипыСодержимогоТранспортногоКонтейнера.Письмо);
		ТипыДИВ.Добавить(Перечисления.ТипыСодержимогоТранспортногоКонтейнера.Приложение);
	ИначеЕсли ТранспортноеСообщение.Тип = Перечисления.ТипыТранспортныхСообщений.ИзвещениеРассылкаНП Тогда
		ТипыДИВ = Перечисления.ТипыСодержимогоТранспортногоКонтейнера.Рассылка;
	КонецЕсли;
	
	// ищем ДИВ исходного сообщения
	ДИВы = ПолучитьВложенияТранспортногоСообщения(ТранспортноеСообщение.Основание, Ложь, ТипыДИВ);
	Если ДИВы.Количество() = 0 Тогда
		ТекстСообщения = НСтр("ru = 'Не удалось создать извещение о получении документа: отсутствует исходный документ в сообщении-основании.'");
		ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
		Возврат Ложь;
	КонецЕсли;
	ДИВы.Колонки.Добавить("ПодписьBase64");
	
	Для Каждого СтрДИВ Из ДИВы Цикл
		
		ИмяФайлаДИВ = СтрДИВ.ИмяФайла;
		
		// ищем подпись под ДИВ
		ПодписиСообщения = ПолучитьВложенияТранспортногоСообщения(ТранспортноеСообщение.Основание, Истина, Перечисления.ТипыСодержимогоТранспортногоКонтейнера.ЭлектронноЦифроваяПодпись);
		ПодписьПодДИВ = ПодписиСообщения.Найти(ИмяФайлаДИВ, "ЭЦПИмяПодписанногоФайла");
		Если ПодписьПодДИВ = Неопределено Тогда
			ТекстСообщения = НСтр("ru = 'Не удалось создать извещение о получении документа: отсутствует подпись под исходным документом.'");
			ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
			Возврат Ложь;
		КонецЕсли;
		
		// пишем подпись во временный файл
		ВремФайл = ПолучитьИмяВременногоФайла();
		Попытка
			ПодписьПодДИВ.Данные.Получить().Записать(ВремФайл);
		Исключение
		КонецПопытки;
		
		// формируем Base64-представление подписи
		СтрДИВ.ПодписьBase64 = ПрочитатьBase64ИзФайла(ВремФайл);
		Если НЕ ЗначениеЗаполнено(СтрДИВ.ПодписьBase64) Тогда
			ТекстСообщения = НСтр("ru = 'Не удалось создать извещение о получении документа: не удалось прочитать данные из файла подписи абонента под исходным документом.'");
			ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
			Возврат Ложь;
		КонецЕсли;
		ОперацииСФайламиЭДКО.УдалитьВременныйФайл(ВремФайл);
		
	КонецЦикла;
	
	// формируем имя файла извещения
	UUIDИзвещения = СгенерироватьUUID();
	ИмяФайлаИзвещения = СформироватьИмяФайлаИзвещения(ТранспортноеСообщение, ДИВы, UUIDИзвещения);
	Если НЕ ЗначениеЗаполнено(ИмяФайлаИзвещения) Тогда
		ТекстСообщения = НСтр("ru = 'Не удалось создать извещение о получении документа: не удалось сформировать имя файла извещения.'");
		ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
		Возврат Ложь;
	КонецЕсли;
	
	// формируем XML извещения о получении электронного документа
	ТекстИзвещения = СформироватьXMLИзвещенияОПолученииЭлектронногоДокумента(ТранспортноеСообщение, ДИВы, ИмяФайлаИзвещения);
	Если НЕ ЗначениеЗаполнено(ТекстИзвещения) Тогда
		Возврат Ложь;
	КонецЕсли;
	
	// пишем XML во временный файл в кодировке windows-1251
	ФайлИзвещения = ВыгрузитьТекстВФайл(ТекстИзвещения);
	Если НЕ ЗначениеЗаполнено(ФайлИзвещения) Тогда
		Возврат Ложь;
	КонецЕсли;
	
	// загружаем файл извещения в ИБ
	ТипСодержимогоТранспортногоКонтейнера = Перечисления.ТипыСодержимогоТранспортногоКонтейнера.ИзвещениеОПолучении;
	ДобавитьСодержимоеТранспортногоКонтейнера(ТранспортноеСообщение, ТипСодержимогоТранспортногоКонтейнера, ФайлИзвещения, ИмяФайлаИзвещения, , , , , , , UUIDИзвещения, Перечисления.ТипыСодержимогоФайлов.xml);
	
	// удаляем временный файл с извещением
	ОперацииСФайламиЭДКО.УдалитьВременныйФайл(ФайлИзвещения);
	
	Возврат Истина;
	
КонецФункции

Функция ОпределитьКодПериодаОтчета(ЦиклОбмена, ПараметрыФайлаВыгрузки)
	
	ЭтоXML = ПараметрыФайлаВыгрузки["ЭтоXML"];
	Если ЭтоXML Тогда
		
		// если файл XML, то сначала пытаемся отыскать свойство Период
		КодПериода = ПараметрыФайлаВыгрузки["Период"];
		Если ЗначениеЗаполнено(КодПериода) Тогда
			Возврат СокрЛП(КодПериода);
		КонецЕсли;
		
		// пытаемся отыскать свойство ПериодОтч
		КодПериода = ПараметрыФайлаВыгрузки["ПериодОтч"];
		Если ЗначениеЗаполнено(КодПериода) Тогда
			Если ПараметрыФайлаВыгрузки["ТипИнф"] <> "ЕДУПР" ИЛИ ПараметрыФайлаВыгрузки["ВерсФорм"] <> "4.01" Тогда // ЕУНД в формате 4.01 - исключение
				Возврат СокрЛП(КодПериода);
			КонецЕсли;
		КонецЕсли;
		
		Возврат Неопределено;
		
	Иначе
		
		ПризПериодОтч = ПараметрыФайлаВыгрузки["ПризПериодОтч"];
		Если ПризПериодОтч = Неопределено Тогда
			ПризПериодОтч = ПараметрыФайлаВыгрузки["ПризПеридОтч"]; // в одном из форматов была опечатка
		КонецЕсли;
		
		НомерПериодОтч = ПараметрыФайлаВыгрузки["НомерПериодОтч"];
		
		// месяц
		Если ПризПериодОтч = "1" И НомерПериодОтч = "01" Тогда
			Возврат "01";
		ИначеЕсли ПризПериодОтч = "1" И НомерПериодОтч = "02" Тогда
			Возврат "02";
		ИначеЕсли ПризПериодОтч = "1" И НомерПериодОтч = "03" Тогда
			Возврат "03";
		ИначеЕсли ПризПериодОтч = "1" И НомерПериодОтч = "04" Тогда
			Возврат "04";
		ИначеЕсли ПризПериодОтч = "1" И НомерПериодОтч = "05" Тогда
			Возврат "05";
		ИначеЕсли ПризПериодОтч = "1" И НомерПериодОтч = "06" Тогда
			Возврат "06";
		ИначеЕсли ПризПериодОтч = "1" И НомерПериодОтч = "07" Тогда
			Возврат "07";
		ИначеЕсли ПризПериодОтч = "1" И НомерПериодОтч = "08" Тогда
			Возврат "08";
		ИначеЕсли ПризПериодОтч = "1" И НомерПериодОтч = "09" Тогда
			Возврат "09";
		ИначеЕсли ПризПериодОтч = "1" И НомерПериодОтч = "10" Тогда
			Возврат "10";
		ИначеЕсли ПризПериодОтч = "1" И НомерПериодОтч = "11" Тогда
			Возврат "11";
		ИначеЕсли ПризПериодОтч = "1" И НомерПериодОтч = "12" Тогда
			Возврат "12";
			
		// квартал
		ИначеЕсли ПризПериодОтч = "3" И НомерПериодОтч = "01" Тогда
			Возврат "21";
		ИначеЕсли ПризПериодОтч = "3" И НомерПериодОтч = "02" Тогда
			Возврат "22";
		ИначеЕсли ПризПериодОтч = "3" И НомерПериодОтч = "03" Тогда
			Возврат "23";
		ИначеЕсли ПризПериодОтч = "3" И НомерПериодОтч = "04" Тогда
			Возврат "24";
			
		// полугодие
		ИначеЕсли ПризПериодОтч = "6" ИЛИ (ПризПериодОтч = "6" И НомерПериодОтч = "02") Тогда
			Возврат "31";
			
		// 9 месяцев
		ИначеЕсли ПризПериодОтч = "9" ИЛИ (ПризПериодОтч = "9" И НомерПериодОтч = "03") Тогда
			Возврат "33";
			
		// год
		ИначеЕсли ПризПериодОтч = "0" ИЛИ (ПризПериодОтч = "0" И НомерПериодОтч = "04") Тогда
			Возврат "34";
			
		Иначе
			Возврат Неопределено;
		КонецЕсли;
		
	КонецЕсли;
	
	Возврат "99";
	
КонецФункции

Функция ОпределитьКлючевыеПараметрыФайлаВыгрузки(Сообщение, Вложения)
	
	// ищем файл выгрузки
	СтрФайлВыгрузки = Вложения.Найти(Перечисления.ТипыСодержимогоТранспортногоКонтейнера.ФайлОтчетности, "Тип");
	Если СтрФайлВыгрузки = Неопределено Тогда
		ТекстСообщения = НСтр("ru = 'Не удалось сформировать описание сведений: не удалось обнаружить файл выгрузки среди вложений сообщения.'");
		ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
		Возврат Неопределено;
	КонецЕсли;
	ФайлыВыгрузки = ПолучитьВложенияТранспортногоСообщения(Сообщение, Истина, , СтрФайлВыгрузки.ИмяФайла);
	
	// сохраняем файл выгрузки во временный файл
	ИмяВременногоФайлаВыгрузки = ПолучитьИмяВременногоФайла();
	Попытка
		ФайлыВыгрузки[0].Данные.Получить().Записать(ИмяВременногоФайлаВыгрузки);
	Исключение
		ТекстСообщения = НСтр("ru = 'Не удалось сформировать описание сведений: не удалось сохранить файл выгрузки во временный каталог.'");
		ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
		Возврат Неопределено;
	КонецПопытки;
	
	// разбираем содержимое файла выгрузки
	ПараметрыФайлаВыгрузки = СформироватьСтруктуруПараметровФайлаВыгрузкиДляОписанияСведений(ИмяВременногоФайлаВыгрузки);
	Если НЕ ЗначениеЗаполнено(ПараметрыФайлаВыгрузки) Тогда
		Возврат Неопределено;
	КонецЕсли;
	
	
	ЦиклОбмена = Сообщение.ЦиклОбмена;
	
	// определяем наименование отчета
	ВидОтчета = ЦиклОбмена.ВидОтчета;
	ИмяФормы = ?(ПустаяСтрока(ВидОтчета.Описание), СокрЛП(ВидОтчета.Наименование), СокрЛП(ВидОтчета.Описание));
	Если НЕ ЗначениеЗаполнено(ИмяФормы) Тогда
		ТекстСообщения = НСтр("ru = 'Не удалось сформировать описание сведений: не удалось определить имя представляемой формы.'");
		ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
		Возврат Неопределено;
	КонецЕсли;
	
	// ищем КНД
	КНДФормы = ПараметрыФайлаВыгрузки["КНД"];
	Если КНДФормы = Неопределено Тогда
		ТекстСообщения = НСтр("ru = 'Не удалось сформировать описание сведений: не удалось определить КНД по файлу выгрузки.'");
		ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
		Возврат Неопределено;
	КонецЕсли;
	
	// определяем год
	Год = Формат(Год(ЦиклОбмена.ДатаНачалаПериода), "ЧГ=");
	Если НЕ ЗначениеЗаполнено(Год) Тогда
		ТекстСообщения = НСтр("ru = 'Не удалось сформировать описание сведений: не удалось определить отчетный год.'");
		ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
		Возврат Неопределено;
	КонецЕсли;
	
	// определяем код периода
	КодПериода = ОпределитьКодПериодаОтчета(ЦиклОбмена, ПараметрыФайлаВыгрузки);
	
	// определяем инспекции по имени файла
	Инспекции = ОпределитьИнспекцииПоИмениФайла(СтрФайлВыгрузки.ИмяФайла);
	Если НЕ ЗначениеЗаполнено(Инспекции) Тогда
		ТекстСообщения = НСтр("ru = 'Не удалось сформировать описание сведений: не удалось выделить коды инспекций в имени файла выгрузки.'");
		ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
		Возврат Неопределено;
	КонецЕсли;
	
	// удаляем временный файл выгрузки
	ОперацииСФайламиЭДКО.УдалитьВременныйФайл(ИмяВременногоФайлаВыгрузки);
	
	
	// 
	Возврат Новый Структура("	имяФормы,	КНДФормы,	год,	кодПериода,	НОПоМестуУчета,		НОПоМестуНахождения",
								ИмяФормы,	КНДФормы,	Год,	КодПериода,	Инспекции.КодИФНС1,	Инспекции.КодИФНС2);
	
КонецФункции

Функция ОпределитьКлючевыеПараметрыФайлаВыгрузки2НДФЛ(Сообщение, Вложения)
	
	// ищем файл выгрузки
	СтрФайлВыгрузки = Вложения.Найти(Перечисления.ТипыСодержимогоТранспортногоКонтейнера.Форма2НДФЛ, "Тип");
	Если СтрФайлВыгрузки = Неопределено Тогда
		ТекстСообщения = НСтр("ru = 'Не удалось сформировать описание сведений: не удалось обнаружить файл выгрузки среди вложений сообщения.'");
		ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
		Возврат Неопределено;
	КонецЕсли;
	ФайлыВыгрузки = ПолучитьВложенияТранспортногоСообщения(Сообщение, Истина, , СтрФайлВыгрузки.ИмяФайла);
	
	// сохраняем файл выгрузки во временный файл
	ИмяВременногоФайлаВыгрузки = ПолучитьИмяВременногоФайла();
	Попытка
		ФайлыВыгрузки[0].Данные.Получить().Записать(ИмяВременногоФайлаВыгрузки);
	Исключение
		ТекстСообщения = НСтр("ru = 'Не удалось сформировать описание сведений: не удалось сохранить файл выгрузки во временный каталог.'");
		ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
		Возврат Неопределено;
	КонецПопытки;
	
	// разбираем содержимое файла выгрузки
	ПараметрыФайлаВыгрузки = СформироватьСтруктуруПараметровФайлаВыгрузкиДляОписанияСведений(ИмяВременногоФайлаВыгрузки);
	Если НЕ ЗначениеЗаполнено(ПараметрыФайлаВыгрузки) Тогда
		Возврат Неопределено;
	КонецЕсли;
	
	// определяем инспекции по имени файла
	Инспекции = ОпределитьИнспекцииПоИмениФайла(СтрФайлВыгрузки.ИмяФайла);
	Если НЕ ЗначениеЗаполнено(Инспекции) Тогда
		ТекстСообщения = НСтр("ru = 'Не удалось сформировать описание сведений: не удалось выделить коды инспекций в имени файла выгрузки.'");
		ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
		Возврат Неопределено;
	КонецЕсли;
	
	// удаляем временный файл выгрузки
	ОперацииСФайламиЭДКО.УдалитьВременныйФайл(ИмяВременногоФайлаВыгрузки);
	
	
	// 
	Возврат Новый Структура("НОПоМестуУчета, НОПоМестуНахождения", Инспекции.КодИФНС1, Инспекции.КодИФНС2);
	
КонецФункции

Функция ОпределитьКлючевыеПараметрыФайлаВыгрузкиЗаявления(Сообщение, Вложения)
	
	// ищем файл выгрузки
	СтрФайлВыгрузки = Вложения.Найти(Перечисления.ТипыСодержимогоТранспортногоКонтейнера.Заявление, "Тип");
	Если СтрФайлВыгрузки = Неопределено Тогда
		ТекстСообщения = НСтр("ru = 'Не удалось сформировать описание сведений заявления: не удалось обнаружить файл выгрузки заявления среди вложений сообщения.'");
		ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
		Возврат Неопределено;
	КонецЕсли;
	ФайлыВыгрузки = ПолучитьВложенияТранспортногоСообщения(Сообщение, Истина, , СтрФайлВыгрузки.ИмяФайла);
	
	// сохраняем файл выгрузки во временный файл
	ИмяВременногоФайлаВыгрузки = ПолучитьИмяВременногоФайла();
	Попытка
		ФайлыВыгрузки[0].Данные.Получить().Записать(ИмяВременногоФайлаВыгрузки);
	Исключение
		ТекстСообщения = НСтр("ru = 'Не удалось сформировать описание сведений заявления: не удалось сохранить файл выгрузки заявления во временный каталог.'");
		ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
		Возврат Неопределено;
	КонецПопытки;
	
	// разбираем содержимое файла выгрузки
	ПараметрыФайлаВыгрузки = СформироватьСтруктуруПараметровФайлаВыгрузкиДляОписанияСведений(ИмяВременногоФайлаВыгрузки);
	Если НЕ ЗначениеЗаполнено(ПараметрыФайлаВыгрузки) Тогда
		Возврат Неопределено;
	КонецЕсли;
	
	// определяем КодНО
	КодНО = ПараметрыФайлаВыгрузки["КодНО"];
	Если КодНО = Неопределено Тогда
		ТекстСообщения = НСтр("ru = 'Не удалось сформировать описание сведений заявления: не удалось определить код налогового органа - получателя.'");
		ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
		Возврат Неопределено;
	КонецЕсли;
	
	// определяем номер заявления
	НомерДокНП = ПараметрыФайлаВыгрузки["НомерДокНП"];
	Если НомерДокНП = Неопределено Тогда
		ТекстСообщения = НСтр("ru = 'Не удалось сформировать описание сведений заявления: не удалось определить номер заявления, указанный налогоплательщиком.'");
		ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
		Возврат Неопределено;
	КонецЕсли;
	
	// удаляем временный файл выгрузки
	ОперацииСФайламиЭДКО.УдалитьВременныйФайл(ИмяВременногоФайлаВыгрузки);
	
	Возврат Новый Структура("КодНО, НомерДокНП", КодНО, НомерДокНП);
	
КонецФункции

Функция ОпределитьКлючевыеПараметрыФайлаЗапроса(Сообщение, Вложения)
	
	// ищем файл запроса
	СтрФайлЗапроса = Вложения.Найти(Перечисления.ТипыСодержимогоТранспортногоКонтейнера.Запрос, "Тип");
	Если СтрФайлЗапроса = Неопределено Тогда
		ТекстСообщения = НСтр("ru = 'Не удалось сформировать описание сведений: не удалось обнаружить файл запроса среди вложений сообщения.'");
		ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
		Возврат Неопределено;
	КонецЕсли;
	ФайлыЗапроса = ПолучитьВложенияТранспортногоСообщения(Сообщение, Истина, , СтрФайлЗапроса.ИмяФайла);
	
	// сохраняем файл запроса во временный файл
	ИмяВременногоФайлаЗапроса = ПолучитьИмяВременногоФайла();
	Попытка
		ФайлыЗапроса[0].Данные.Получить().Записать(ИмяВременногоФайлаЗапроса);
	Исключение
		ТекстСообщения = НСтр("ru = 'Не удалось сформировать описание сведений: не удалось сохранить файл запроса во временный каталог'");
		ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
		Возврат Неопределено;
	КонецПопытки;
	
	// разбираем содержимое файла запроса
	ПараметрыФайлаЗапроса = СформироватьСтруктуруПараметровФайлаЗапросаДляОписанияСведений(ИмяВременногоФайлаЗапроса);
	Если НЕ ЗначениеЗаполнено(ПараметрыФайлаЗапроса) Тогда
		Возврат Неопределено;
	КонецЕсли;
	
	// ищем КНД
	КодЗапр = ПараметрыФайлаЗапроса["КодЗапр"];
	Если КодЗапр = Неопределено Тогда
		ТекстСообщения = НСтр("ru = 'Не удалось сформировать описание сведений: не удалось определить код типа запроса по файлу запроса.'");
		ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
		Возврат Неопределено;
	КонецЕсли;
	
	// определяем инспекции по имени файла
	Инспекции = ОпределитьИнспекцииПоИмениФайла(СтрФайлЗапроса.ИмяФайла);
	Если НЕ ЗначениеЗаполнено(Инспекции) Тогда
		ТекстСообщения = НСтр("ru = 'Не удалось сформировать описание сведений: не удалось выделить коды инспекций в имени файла запроса.'");
		ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
		Возврат Неопределено;
	КонецЕсли;
	
	// удаляем временный файл выгрузки
	ОперацииСФайламиЭДКО.УдалитьВременныйФайл(ИмяВременногоФайлаЗапроса);
	
	// 
	Возврат Новый Структура("КодЗапр,	НОПоМестуПредставления,	НОПоМестуНахождения",
							КодЗапр,	Инспекции.КодИФНС1,		Инспекции.КодИФНС2);
	
КонецФункции

Функция ОпределитьКНДФайлаВыгрузкиПредставления(Сообщение, Вложения)
	
	// ищем файл выгрузки
	СтрФайлВыгрузки = Вложения.Найти(Перечисления.ТипыСодержимогоТранспортногоКонтейнера.Представление, "Тип");
	Если СтрФайлВыгрузки = Неопределено Тогда
		ТекстСообщения = НСтр("ru = 'Не удалось сформировать описание сведений: не удалось обнаружить файл выгрузки среди вложений сообщения.'");
		ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
		Возврат Неопределено;
	КонецЕсли;
	ФайлыВыгрузки = ПолучитьВложенияТранспортногоСообщения(Сообщение, Истина, , СтрФайлВыгрузки.ИмяФайла);
	
	// сохраняем файл выгрузки во временный файл
	ИмяВременногоФайлаВыгрузки = ПолучитьИмяВременногоФайла();
	Попытка
		ФайлыВыгрузки[0].Данные.Получить().Записать(ИмяВременногоФайлаВыгрузки);
	Исключение
		ТекстСообщения = НСтр("ru = 'Не удалось сформировать описание сведений: не удалось сохранить файл выгрузки во временный каталог.'");
		ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
		Возврат Неопределено;
	КонецПопытки;
	
	// разбираем содержимое файла выгрузки
	ПараметрыФайлаВыгрузки = СформироватьСтруктуруПараметровФайлаВыгрузкиДляОписанияСведений(ИмяВременногоФайлаВыгрузки, Истина);
	Если НЕ ЗначениеЗаполнено(ПараметрыФайлаВыгрузки) Тогда
		Возврат Неопределено;
	КонецЕсли;
	
	// ищем КНД
	КНДФормы = ПараметрыФайлаВыгрузки["КНД"];
	Если КНДФормы = Неопределено Тогда
		ТекстСообщения = НСтр("ru = 'Не удалось сформировать описание сведений: не удалось определить КНД по файлу выгрузки.'");
		ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
		Возврат Неопределено;
	КонецЕсли;
	
	// удаляем временный файл выгрузки
	ОперацииСФайламиЭДКО.УдалитьВременныйФайл(ИмяВременногоФайлаВыгрузки);
	
	// 
	Возврат КНДФормы;
	
КонецФункции

Функция СформироватьСтруктуруПараметровФайлаВыгрузкиДляОписанияСведений(ФайлВыгрузки, ОстановитьПоискЕслиНайденКНД = Ложь)

	ПоказателиВыгрузки = Новый Соответствие;
	ОбъектЧтениеXML = Новый ЧтениеXML;
	Попытка
		ОбъектЧтениеXML.ОткрытьФайл(ФайлВыгрузки);
		ОбъектЧтениеXML.ИгнорироватьПробелы = Ложь;
		Пока ОбъектЧтениеXML.Прочитать() Цикл
			Если ОбъектЧтениеXML.ТипУзла = ТипУзлаXML.НачалоЭлемента Тогда
				Пока ОбъектЧтениеXML.ПрочитатьАтрибут() Цикл
					Если ПоказателиВыгрузки[ОбъектЧтениеXML.Имя] = Неопределено Тогда
						ПоказателиВыгрузки.Вставить(ОбъектЧтениеXML.Имя, ОбъектЧтениеXML.Значение);
						Если ОбъектЧтениеXML.Имя = "КНД" И ОстановитьПоискЕслиНайденКНД Тогда
							ПоказателиВыгрузки.Вставить("ЭтоXML", Истина);
							Возврат ПоказателиВыгрузки;
						КонецЕсли;
					КонецЕсли;
				КонецЦикла;
			КонецЕсли;
		КонецЦикла;	
		ОбъектЧтениеXML.Закрыть();
		ПоказателиВыгрузки.Вставить("ЭтоXML", Истина);
	Исключение
		Текст = Новый ЧтениеТекста;
		Текст.Открыть(ФайлВыгрузки, "cp866");
		ТекСтр = Текст.ПрочитатьСтроку();
		Пока ТекСтр <> Неопределено Цикл
			ТекСтр = СокрЛП(ТекСтр);
			ВхождениеДвоеточия = СтрНайти(ТекСтр, ":");
			ИмяПоказателя = СокрЛП(Лев(ТекСтр, ВхождениеДвоеточия - 1));
			ЗначениеПоказателя = СокрЛП(Сред(ТекСтр, ВхождениеДвоеточия + 1));
			Если ВхождениеДвоеточия <> 0 Тогда
				Если ПоказателиВыгрузки[ИмяПоказателя] = Неопределено Тогда
					ПоказателиВыгрузки.Вставить(ИмяПоказателя, ЗначениеПоказателя);
				КонецЕсли;
			КонецЕсли;
			ТекСтр = Текст.ПрочитатьСтроку();
		КонецЦикла;
		Текст.Закрыть();
		ПоказателиВыгрузки.Вставить("ЭтоXML", Ложь);
	КонецПопытки;
	Возврат ПоказателиВыгрузки;

КонецФункции

Функция СформироватьСтруктуруПараметровФайлаЗапросаДляОписанияСведений(ФайлВыгрузки)

	ПоказателиВыгрузки = Новый Соответствие;
	ОбъектЧтениеXML = Новый ЧтениеXML;
	ОбъектЧтениеXML.ОткрытьФайл(ФайлВыгрузки);
	ОбъектЧтениеXML.ИгнорироватьПробелы = Ложь;
	Пока ОбъектЧтениеXML.Прочитать() Цикл
		Если ОбъектЧтениеXML.ТипУзла = ТипУзлаXML.НачалоЭлемента Тогда
			Пока ОбъектЧтениеXML.ПрочитатьАтрибут() Цикл
				Если ПоказателиВыгрузки[ОбъектЧтениеXML.Имя] = Неопределено Тогда
					ПоказателиВыгрузки.Вставить(ОбъектЧтениеXML.Имя, ОбъектЧтениеXML.Значение);
				КонецЕсли;
			КонецЦикла;
		КонецЕсли;
	КонецЦикла;
	ОбъектЧтениеXML.Закрыть();
	Возврат ПоказателиВыгрузки;

КонецФункции

Функция СформироватьОписаниеСведений2НДФЛ(Сообщение, Вложения, КороткоеИмяФайла = Неопределено)
	
	// извлекаем ключевые параметры
	КлючевыеПараметрыФайлаВыгрузки = ОпределитьКлючевыеПараметрыФайлаВыгрузки2НДФЛ(Сообщение, Вложения);
	Если НЕ ЗначениеЗаполнено(КлючевыеПараметрыФайлаВыгрузки) Тогда
		Возврат Неопределено;
	КонецЕсли;

	// создаем дерево описания XML
	Дерево = СоздатьДеревоXML();
	
	ДобавитьУзелXML(Дерево, "имяФормы", , "2-НДФЛ");
	ДобавитьУзелXML(Дерево, "КНДФормы", , "1151078");
	ДобавитьУзелXML(Дерево, "год", , Формат(Год(Сообщение.ЦиклОбмена.ДатаНачалаПериода), "ЧГ="));
	ДобавитьУзелXML(Дерево, "НОПоМестуУчета", , 		КлючевыеПараметрыФайлаВыгрузки["НОПоМестуУчета"]);
	ДобавитьУзелXML(Дерево, "НОПоМестуНахождения", , 	КлючевыеПараметрыФайлаВыгрузки["НОПоМестуНахождения"]);
	
	// формируем строку XML на основе дерева
	СтрокаXML = ВыгрузитьДеревоВXML_ПФР(Дерево, "описание");
	
	// записываем строку в файл
	КороткоеИмяФайла = СгенерироватьИмяФайлаДокументаВСоставеПакета();
	ПолноеИмяФайлаДокументаВСоставеПакета = ПолучитьКаталогВременныхФайлов() + КороткоеИмяФайла;
	РезультатЗаписи = ЗаписатьXMLВФайл(СтрокаXML, ПолноеИмяФайлаДокументаВСоставеПакета);
	Если РезультатЗаписи = Ложь Тогда
		Возврат Неопределено;
	КонецЕсли;
	
	Возврат ПолноеИмяФайлаДокументаВСоставеПакета;
	
КонецФункции

Функция СформироватьОписаниеСведенийОбращенияПисьмаРассылки(Сообщение, Вложения, КороткоеИмяФайла)
	
	Предмет = Сообщение.ЦиклОбмена.Предмет;
	
	// создаем дерево описания XML
	Дерево = СоздатьДеревоXML();
	
	Если ЗначениеЗаполнено(Предмет.ИдентификаторОснования) Тогда
		ДобавитьУзелXML(Дерево, "ответНа", , Предмет.ИдентификаторОснования);
	КонецЕсли;
	
	ДобавитьУзелXML(Дерево, "тема", , СокрЛП(Предмет.Наименование));
	
	// формируем строку XML на основе дерева
	СтрокаXML = ВыгрузитьДеревоВXML_ПФР(Дерево, "описание");
	
	// записываем строку в файл
	КороткоеИмяФайла = СгенерироватьИмяФайлаДокументаВСоставеПакета();
	ПолноеИмяФайлаДокументаВСоставеПакета = ПолучитьКаталогВременныхФайлов() + КороткоеИмяФайла;
	РезультатЗаписи = ЗаписатьXMLВФайл(СтрокаXML, ПолноеИмяФайлаДокументаВСоставеПакета);
	Если РезультатЗаписи = Ложь Тогда
		Возврат Неопределено;
	КонецЕсли;
	
	Возврат ПолноеИмяФайлаДокументаВСоставеПакета;
	
КонецФункции

Функция СформироватьОписаниеСведенийПредставлениеНП(Сообщение, Вложения, КороткоеИмяФайла = Неопределено)
	
	// извлекаем ключевые параметры
	КНДФормы = ОпределитьКНДФайлаВыгрузкиПредставления(Сообщение, Вложения);
	Если НЕ ЗначениеЗаполнено(КНДФормы) Тогда
		Возврат Неопределено;
	КонецЕсли;
	
	// создаем дерево описания XML
	Дерево = СоздатьДеревоXML();
	
	ДобавитьУзелXML(Дерево, "КНД", , КНДФормы);
	ДобавитьУзелXML(Дерево, "КодНО", , СокрЛП(Сообщение.ЦиклОбмена.ВнешняяОрганизация.Код));
	
	// формируем строку XML на основе дерева
	СтрокаXML = ВыгрузитьДеревоВXML_ПФР(Дерево, "описание");
	
	// записываем строку в файл
	КороткоеИмяФайла = СгенерироватьИмяФайлаДокументаВСоставеПакета();
	ПолноеИмяФайлаДокументаВСоставеПакета = ПолучитьКаталогВременныхФайлов() + КороткоеИмяФайла;
	РезультатЗаписи = ЗаписатьXMLВФайл(СтрокаXML, ПолноеИмяФайлаДокументаВСоставеПакета);
	Если РезультатЗаписи = Ложь Тогда
		Возврат Неопределено;
	КонецЕсли;
	
	Возврат ПолноеИмяФайлаДокументаВСоставеПакета;
	
КонецФункции

Функция СформироватьОписаниеСведенийЗапроса(Сообщение, Вложения, КороткоеИмяФайла = Неопределено)
	
	// извлекаем ключевые параметры
	КлючевыеПараметрыФайлаВыгрузки = ОпределитьКлючевыеПараметрыФайлаЗапроса(Сообщение, Вложения);
	Если НЕ ЗначениеЗаполнено(КлючевыеПараметрыФайлаВыгрузки) Тогда
		Возврат Неопределено;
	КонецЕсли;
	
	// создаем дерево описания XML
	Дерево = СоздатьДеревоXML();
	
	ДобавитьУзелXML(Дерево, "кодЗапроса", , КлючевыеПараметрыФайлаВыгрузки["кодЗапр"]);
	ДобавитьУзелXML(Дерево, "НОПоМестуПредставления", , КлючевыеПараметрыФайлаВыгрузки["НОПоМестуПредставления"]);
	ДобавитьУзелXML(Дерево, "НОПоМестуНахождения", , КлючевыеПараметрыФайлаВыгрузки["НОПоМестуНахождения"]);
	
	// формируем строку XML на основе дерева
	СтрокаXML = ВыгрузитьДеревоВXML_ПФР(Дерево, "описание");
	
	// записываем строку в файл
	КороткоеИмяФайла = СгенерироватьИмяФайлаДокументаВСоставеПакета();
	ПолноеИмяФайлаДокументаВСоставеПакета = ПолучитьКаталогВременныхФайлов() + КороткоеИмяФайла;
	РезультатЗаписи = ЗаписатьXMLВФайл(СтрокаXML, ПолноеИмяФайлаДокументаВСоставеПакета);
	Если РезультатЗаписи = Ложь Тогда
		Возврат Неопределено;
	КонецЕсли;
	
	Возврат ПолноеИмяФайлаДокументаВСоставеПакета;
	
КонецФункции

Функция СформироватьОписаниеСведенийЗаявление(Сообщение, Вложения, КороткоеИмяФайла = Неопределено)
	
	// извлекаем ключевые параметры
	КлючевыеПараметрыФайлаВыгрузки = ОпределитьКлючевыеПараметрыФайлаВыгрузкиЗаявления(Сообщение, Вложения);
	Если НЕ ЗначениеЗаполнено(КлючевыеПараметрыФайлаВыгрузки) Тогда
		Возврат Неопределено;
	КонецЕсли;
	
	// создаем дерево описания XML
	Дерево = СоздатьДеревоXML();
	
	ДобавитьУзелXML(Дерево, "КНД", , "1110017");
	ДобавитьУзелXML(Дерево, "КодНО", , КлючевыеПараметрыФайлаВыгрузки["КодНО"]);
	ДобавитьУзелXML(Дерево, "НомерДокНП", , КлючевыеПараметрыФайлаВыгрузки["НомерДокНП"]);
	
	// формируем строку XML на основе дерева
	СтрокаXML = ВыгрузитьДеревоВXML_ПФР(Дерево, "описание");
	
	// записываем строку в файл
	КороткоеИмяФайла = СгенерироватьИмяФайлаДокументаВСоставеПакета();
	ПолноеИмяФайлаДокументаВСоставеПакета = ПолучитьКаталогВременныхФайлов() + КороткоеИмяФайла;
	РезультатЗаписи = ЗаписатьXMLВФайл(СтрокаXML, ПолноеИмяФайлаДокументаВСоставеПакета);
	Если РезультатЗаписи = Ложь Тогда
		Возврат Неопределено;
	КонецЕсли;
	
	Возврат ПолноеИмяФайлаДокументаВСоставеПакета;
	
КонецФункции

Функция СформироватьОписаниеСведенийФНС(Сообщение, Вложения, КороткоеИмяФайла = Неопределено)
	
	// извлекаем ключевые параметры
	КлючевыеПараметрыФайлаВыгрузки = ОпределитьКлючевыеПараметрыФайлаВыгрузки(Сообщение, Вложения);
	Если НЕ ЗначениеЗаполнено(КлючевыеПараметрыФайлаВыгрузки) Тогда
		Возврат Неопределено;
	КонецЕсли;
	
	// создаем дерево описания XML
	Дерево = СоздатьДеревоXML();
	
	ДобавитьУзелXML(Дерево, "имяФормы", , КлючевыеПараметрыФайлаВыгрузки["имяФормы"]);
	ДобавитьУзелXML(Дерево, "КНДФормы", , КлючевыеПараметрыФайлаВыгрузки["КНДФормы"]);
	ДобавитьУзелXML(Дерево, "год", , КлючевыеПараметрыФайлаВыгрузки["год"]);
	
	Если ЗначениеЗаполнено(КлючевыеПараметрыФайлаВыгрузки["кодПериода"]) Тогда
		ДобавитьУзелXML(Дерево, "кодПериода", , КлючевыеПараметрыФайлаВыгрузки["кодПериода"]);
	КонецЕсли;
	
	ДобавитьУзелXML(Дерево, "НОПоМестуУчета", , КлючевыеПараметрыФайлаВыгрузки["НОПоМестуУчета"]);
	ДобавитьУзелXML(Дерево, "НОПоМестуНахождения", , КлючевыеПараметрыФайлаВыгрузки["НОПоМестуНахождения"]);
	
	// формируем строку XML на основе дерева
	СтрокаXML = ВыгрузитьДеревоВXML_ПФР(Дерево, "описание");
	
	// записываем строку в файл
	КороткоеИмяФайла = СгенерироватьИмяФайлаДокументаВСоставеПакета();
	ПолноеИмяФайлаДокументаВСоставеПакета = ПолучитьКаталогВременныхФайлов() + КороткоеИмяФайла;
	РезультатЗаписи = ЗаписатьXMLВФайл(СтрокаXML, ПолноеИмяФайлаДокументаВСоставеПакета);
	Если РезультатЗаписи = Ложь Тогда
		Возврат Неопределено;
	КонецЕсли;
	
	Возврат ПолноеИмяФайлаДокументаВСоставеПакета;
	
КонецФункции

#КонецОбласти

#Область ОтправкаРегламентированногоОтчетаВПФР

Функция ОтправкаРегламентированногоОтчетаВПФР_ПередВыгрузкойФайла(Контекст, Док, УникальныйИдентификатор, ТекстСообщения) Экспорт

	Контекст = Новый Структура();
	Контекст.Вставить("Документ", Док);
	Контекст.Вставить("УникальныйИдентификатор", УникальныйИдентификатор);
	
	Если ТипЗнч(Контекст.Документ) = Тип("Массив") Тогда
		ОсновнойДок = Контекст.Документ[0];
	Иначе
		ОсновнойДок = Контекст.Документ;
	КонецЕсли;
	
	Если НЕ ЗначениеЗаполнено(ОсновнойДок) Тогда
		ТекстСообщения = НСтр("ru = 'Не выбран документ для отправки.'");
		ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
		Возврат Ложь;
	КонецЕсли;
	
	// проверяем, включен ли обмен по организации
	Организация = ОбщегоНазначения.ЗначениеРеквизитаОбъекта(ОсновнойДок, "Организация");
	Если Организация.ВидОбменаСКонтролирующимиОрганами <> Перечисления.ВидыОбменаСКонтролирующимиОрганами.ОбменВУниверсальномФормате Тогда
		
		// Текст ошибки будет выведен в виде формы
		Контекст.Вставить("ОтсутствуетУчетнаяЗапись", Истина);

		Возврат Ложь;
		
	КонецЕсли;
	
	// проверяем, предназначена ли эта учетная запись для документооборота с ФНС
	УчетнаяЗапись = УчетнаяЗаписьОрганизации(Организация);
	
	ПропуститьПроверку = 
		ТипЗнч(ОсновнойДок) = Тип("ДокументСсылка.ЗаявленияПоЭлДокументооборотуСПФР") 
		И ОсновнойДок.Вид = Перечисления.ВидыЗаявленийНаЭДОВПФР.НаОтключение;
	
	Если ОбщегоНазначения.ЗначениеРеквизитаОбъекта(УчетнаяЗапись, "ПредназначенаДляДокументооборотаСПФР") <> Истина И НЕ ПропуститьПроверку Тогда
		
		// Текст ошибки будет выведен в виде формы
		Контекст.Вставить("НаправлениеНеПодключено", Истина);
		Возврат Ложь;
		
	КонецЕсли;
	
	ТипЗнчДок = ТипЗнч(ОсновнойДок);
	МетаданныеОтчета = Метаданные.НайтиПоТипу(ТипЗнчДок);
	Если МетаданныеОтчета = Неопределено Тогда
		ТекстСообщения = НСтр("ru = 'Неизвестный тип отчета.'");
		ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
		Возврат Ложь;
	КонецЕсли;
	
	ИмяМетаданныхОтчета = МетаданныеОтчета.Имя;
	Если ИмяМетаданныхОтчета = "РегламентированныйОтчет" Тогда
		ЗначенияРеквизитов = ОбщегоНазначения.ЗначенияРеквизитовОбъекта(ОсновнойДок, "ДатаНачала, ДатаОкончания, ИсточникОтчета");
		
		ДатаНачалаПериодаОтчета         = ЗначенияРеквизитов.ДатаНачала;
		ДатаОкончанияПериодаОтчета      = ЗначенияРеквизитов.ДатаОкончания;
		ВидРегламентированногоОтчетаСтр = ЗначенияРеквизитов.ИсточникОтчета;
	ИначеЕсли ИмяМетаданныхОтчета = "ЭлектронныеПредставленияРегламентированныхОтчетов" Тогда
		ЗначенияРеквизитов = ОбщегоНазначения.ЗначенияРеквизитовОбъекта(ОсновнойДок, "ДатаНачала, ДатаОкончания, ВидОтчета");
		
		ДатаНачалаПериодаОтчета         = ЗначенияРеквизитов.ДатаНачала;
		ДатаОкончанияПериодаОтчета      = ЗначенияРеквизитов.ДатаОкончания;
		ВидРегламентированногоОтчетаСтр = ЗначенияРеквизитов.ВидОтчета;
	ИначеЕсли ИмяМетаданныхОтчета = "ЗаявленияПоЭлДокументооборотуСПФР" Тогда
		
		// Тут определены такие даты, но в цикл обмена они не пишутся
		ДатаНачалаПериодаОтчета         = ТекущаяДатаСеанса();
		ДатаОкончанияПериодаОтчета      = ТекущаяДатаСеанса();
		ВидРегламентированногоОтчетаСтр = ПредопределенноеЗначение("Справочник.ВидыОтправляемыхДокументов.ЗаявленияПоЭлДокументооборотуСПФР");
		
	Иначе
		ДатыПериодаОтчета = ЭлектронныйДокументооборотСКонтролирующимиОрганамиВызовСервераПереопределяемый.ПолучитьДатыПериодаДокумента(ОсновнойДок);
		
		ДатаНачалаПериодаОтчета         = ДатыПериодаОтчета.ДатаНачала;
		ДатаОкончанияПериодаОтчета      = ДатыПериодаОтчета.ДатаОкончания;
		ВидРегламентированногоОтчетаСтр = ИмяМетаданныхОтчета;
	КонецЕсли;
	
	КодОрганаПФР = Неопределено;
	РегНомерПФР = Неопределено;
	ОрганПФР = ОпределитьОрганПФРОрганизации(Организация, КодОрганаПФР, РегНомерПФР);
	Если ОрганПФР = Неопределено Тогда
		ТекстСообщения = НСтр("ru = 'В свойствах организации не задан код органа ПФР.'");
		ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
		Возврат Ложь;
	ИначеЕсли НЕ ЗначениеЗаполнено(ОрганПФР) Тогда
		ТекстСообщения = ?(ЗначениеЗаполнено(КодОрганаПФР) И СтрНайти(КодОрганаПФР, "-") = 0,
			НСтр("ru = 'Отсутствует дефис в поле ""Код территориального органа ПФР"" свойств организации, скорректируйте это значение и повторите попытку.'"),
			НСтр("ru = 'Не удалось найти в справочнике ""Органы ПФР"" получателя отчетности.'"));
		ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
		Возврат Ложь;
	КонецЕсли;
	
	ТипЦиклаОбмена = Перечисления.ТипыЦикловОбмена.ОтчетностьПФР;
	ТипТранспортногоСообщения = Перечисления.ТипыТранспортныхСообщений.ПервичноеСообщениеСодержащееОтчетностьПФР;
	ТипСодержимогоКонтейнера = Перечисления.ТипыСодержимогоТранспортногоКонтейнера.ФайлОтчетностиПФР;
	
	Если ТипЗнч(ВидРегламентированногоОтчетаСтр) = Тип("СправочникСсылка.ВидыОтправляемыхДокументов") Тогда
		ВидРегламентированногоОтчета = ВидРегламентированногоОтчетаСтр;
	Иначе
		ВидРегламентированногоОтчета = Справочники.ВидыОтправляемыхДокументов.НайтиПоРеквизиту("Источник", ВидРегламентированногоОтчетаСтр);
		Если ВидРегламентированногоОтчета = Справочники.ВидыОтправляемыхДокументов.ПустаяСсылка() Тогда
			
			ВидОтправляемогоДокументаПереопределяемый = ВидОтправляемогоДокументаПоСсылке(ОсновнойДок);
			
			Если ЗначениеЗаполнено(ВидОтправляемогоДокументаПереопределяемый) Тогда
				ВидРегламентированногоОтчета = ВидОтправляемогоДокументаПереопределяемый;
			Иначе
				ТекстСообщения = СтроковыеФункцииКлиентСервер.ПодставитьПараметрыВСтроку(
				НСтр("ru = 'В справочнике ""Виды отправляемых документов"" не найден соответствующий элемент для отправки объекта %1!'"), ОсновнойДок);
				ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
				Возврат Ложь;	
			КонецЕсли;
			
		КонецЕсли;
		// с 2014 года отправляются комплекты на основе РСВ-1 с приложениями
		Если (ВидРегламентированногоОтчета = Справочники.ВидыОтправляемыхДокументов.КомплектыОтчетностиПерсучета 
			ИЛИ ВидРегламентированногоОтчета = Справочники.ВидыОтправляемыхДокументов.РСВ1) И ДатаНачалаПериодаОтчета >= '20140101000000' Тогда
			ВидРегламентированногоОтчета = Справочники.ВидыОтправляемыхДокументов.КомплектыРСВ1;
		КонецЕсли;
	КонецЕсли;
	
	Контекст.Вставить("Организация",                  Организация);
	Контекст.Вставить("ДатаНачалаПериодаОтчета",      ДатаНачалаПериодаОтчета);
	Контекст.Вставить("ДатаОкончанияПериодаОтчета",   ДатаОкончанияПериодаОтчета);
	Контекст.Вставить("ТипОтправляемогоДокумента",    ИмяМетаданныхОтчета);
	Контекст.Вставить("ТипЦиклаОбмена",               ТипЦиклаОбмена);
	Контекст.Вставить("ТипТранспортногоСообщения",    ТипТранспортногоСообщения);
	Контекст.Вставить("ТипСодержимогоКонтейнера",     ТипСодержимогоКонтейнера);
	Контекст.Вставить("УчетнаяЗапись",                УчетнаяЗапись);
	Контекст.Вставить("ОрганПФР",                     ОрганПФР);
	Контекст.Вставить("РегНомерПФР",                  РегНомерПФР);
	Контекст.Вставить("ВидРегламентированногоОтчета", ВидРегламентированногоОтчета);
	Контекст.Вставить("ИсточникОтчета",               ВидРегламентированногоОтчетаСтр);
	
	Возврат Истина;
	
КонецФункции

Функция ОтправкаРегламентированногоОтчетаВПФР_ОпределитьЦиклОбменаИСообщениеВИБ(Контекст) Экспорт
	
	Отчет = Контекст.Документ;
	Организация = Контекст.Организация;
	ТипЦиклаОбмена = Контекст.ТипЦиклаОбмена;
	УчетнаяЗапись = Контекст.УчетнаяЗапись;
	ОрганПФР = Контекст.ОрганПФР;
	ТипТранспортногоСообщения = Контекст.ТипТранспортногоСообщения;
	ВидРегламентированногоОтчета = Контекст.ВидРегламентированногоОтчета;
	РезультатВыгрузкиОтчетности = Контекст.РезультатВыгрузки;
	
	// формируем массив предметов
	МассивПредметов = ПолучитьМассивПредметов(РезультатВыгрузкиОтчетности, Отчет);
	
	НачатьТранзакцию();
	
	// формируем цикл обмена
	ЦиклОбмена = Справочники.ЦиклыОбмена.СоздатьЭлемент();
	ЦиклОбменаСсылка = Справочники.ЦиклыОбмена.ПолучитьСсылку();
	ЦиклОбмена.УстановитьСсылкуНового(ЦиклОбменаСсылка);
	ЦиклОбмена.Вид = Перечисления.ВидыЦикловОбмена.ЦиклОбменаСПФР;

	ЦиклОбмена.ВидОтчета = ВидРегламентированногоОтчета;
	ЦиклОбмена.Организация = Организация;
	ЦиклОбмена.УчетнаяЗапись = УчетнаяЗапись;
	ЦиклОбмена.ВнешняяОрганизация = ОрганПФР;
	ЦиклОбмена.Тип = ТипЦиклаОбмена;
	
	ЦиклОбмена.Предмет = МассивПредметов[0];
	Для Инд = 1 По МассивПредметов.Количество() - 1 Цикл
		НовСтр = ЦиклОбмена.ДополнительныеПредметы.Добавить();
		НовСтр.Предмет = МассивПредметов[Инд];
	КонецЦикла;
	
	Если ТипЗнч(ЦиклОбмена.Предмет) <> Тип("ДокументСсылка.ЗаявленияПоЭлДокументооборотуСПФР") Тогда
		ЦиклОбмена.ДатаНачалаПериода = Контекст.ДатаНачалаПериодаОтчета;
		ЦиклОбмена.ДатаОкончанияПериода = Контекст.ДатаОкончанияПериодаОтчета;
	КонецЕсли;
	
	// формируем первичное транспортное сообщение
	ТранспортноеСообщение = Документы.ТранспортноеСообщение.СоздатьДокумент();
	ТранспортноеСообщение.ЦиклОбмена = ЦиклОбменаСсылка;
	ТранспортноеСообщение.Тип = ТипТранспортногоСообщения;
	ТранспортноеСообщение.Отправитель = Организация;
	ТранспортноеСообщение.Получатель = ОрганПФР;
	ТранспортноеСообщение.УчетнаяЗапись = УчетнаяЗапись;
	ТранспортноеСообщение.Статус = Перечисления.СтатусыПисем.Исходящее;
	Попытка
		ТранспортноеСообщение.Записать();
	Исключение
		ОтменитьТранзакцию();
		ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ИнформацияОбОшибке().Описание);
		ТекстСообщения = НСтр("ru = 'Отправка прервана.'");
		ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
		Возврат Ложь;
	КонецПопытки;
	
	ФорматДокументооборота = Перечисления.ФорматыДокументооборотаСПФР.БПИ;
	
	// добавляем ДИВы
	Для Каждого СтрРезультатВыгрузкиОтчетности Из РезультатВыгрузкиОтчетности Цикл
		
		ТипФайлаОтчетностиПФР = Неопределено;
		ИмяТипаДокументаПачкаДокументовСПВ_1 = "ПачкаДокументовСПВ_1";
		ИмяТипаДокументаПачкаДокументовСПВ_2 = "ПачкаДокументовСПВ_2";
		Если СтрРезультатВыгрузкиОтчетности.Свойство("Документ") И
			((Метаданные.Документы.Найти(ИмяТипаДокументаПачкаДокументовСПВ_1) <> Неопределено И ТипЗнч(СтрРезультатВыгрузкиОтчетности.Документ) = Тип("ДокументСсылка." + ИмяТипаДокументаПачкаДокументовСПВ_1))
			ИЛИ (Метаданные.Документы.Найти(ИмяТипаДокументаПачкаДокументовСПВ_2) <> Неопределено И ТипЗнч(СтрРезультатВыгрузкиОтчетности.Документ) = Тип("ДокументСсылка." + ИмяТипаДокументаПачкаДокументовСПВ_2))) Тогда
			ТипФайлаОтчетностиПФР = Перечисления.ТипыФайловОтчетностиПФР.ПачкаСВВ;
		КонецЕсли;
		
		Если РегламентированнаяОтчетностьКлиентСервер.СвойствоОпределено(СтрРезультатВыгрузкиОтчетности, "Тип") Тогда
			Если ТипЗнч(СтрРезультатВыгрузкиОтчетности.Тип) = Тип("ПеречислениеСсылка.ТипыФайловОтчетностиПФР") ИЛИ ТипФайлаОтчетностиПФР <> Неопределено
				ИЛИ (СтрРезультатВыгрузкиОтчетности.Свойство("ОпределитьТипПоСодержимому") И СтрРезультатВыгрузкиОтчетности.ОпределитьТипПоСодержимому) Тогда
				ТипСодержимогоКонтейнера = Перечисления.ТипыСодержимогоТранспортногоКонтейнера.ФайлОтчетностиПФР;
			ИначеЕсли ТипЗнч(СтрРезультатВыгрузкиОтчетности.Тип) = Тип("ПеречислениеСсылка.ТипыСодержимогоТранспортногоКонтейнера") Тогда
				ТипСодержимогоКонтейнера = СтрРезультатВыгрузкиОтчетности.Тип;
			Иначе
				ОтменитьТранзакцию();
				ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ИнформацияОбОшибке().Описание);
				ТекстСообщения = НСтр("ru = 'Отправка прервана.'");
				ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
				Возврат Ложь;
			КонецЕсли;
		Иначе	
			ТипСодержимогоКонтейнера = Перечисления.ТипыСодержимогоТранспортногоКонтейнера.ФайлОтчетностиПФР;
		КонецЕсли;
		
		Если ТипСодержимогоКонтейнера = Перечисления.ТипыСодержимогоТранспортногоКонтейнера.СведенияПриложениеПФР Тогда
			ТипСодержимогоФайла = Перечисления.ТипыСодержимогоФайлов.Неизвестный;
		Иначе
			ТипСодержимогоФайла = Перечисления.ТипыСодержимогоФайлов.xml;
		КонецЕсли;
		
		Если ТипФайлаОтчетностиПФР = Неопределено Тогда
			ТипФайлаОтчетностиПФР = СтрРезультатВыгрузкиОтчетности.Тип;
		КонецЕсли;
		ОпределитьТипПоСодержимому = ТипФайлаОтчетностиПФР = Неопределено
			И СтрРезультатВыгрузкиОтчетности.Свойство("ОпределитьТипПоСодержимому")
			И СтрРезультатВыгрузкиОтчетности.ОпределитьТипПоСодержимому;
		ИнформацияОФорматеДокументооборота = ФорматДокументооборотаСПФР(ВидРегламентированногоОтчета);
		
		Если ОпределитьТипПоСодержимому
			ИЛИ ИнформацияОФорматеДокументооборота.ФорматДокументооборота = Перечисления.ФорматыДокументооборотаСПФР.ЭДОК
			И ИнформацияОФорматеДокументооборота.ЭтоВозможныйФормат Тогда
			
			ИмяВремФайла = ПолучитьИмяВременногоФайла();
			ПолучитьИзВременногоХранилища(СтрРезультатВыгрузкиОтчетности.Адрес).Записать(ИмяВремФайла);
			ИнформацияОТипеФайла = Неопределено;
			Попытка
				ИнформацияОТипеФайла = ОпределитьТипФайлаПФР(ИмяВремФайла, СтрРезультатВыгрузкиОтчетности.Имя);
			Исключение
			КонецПопытки;
			ОперацииСФайламиЭДКО.УдалитьВременныйФайл(ИмяВремФайла);
			
			Если ОпределитьТипПоСодержимому Тогда
				ТипФайлаОтчетностиПФР = ?(ИнформацияОТипеФайла = Неопределено, Неопределено, ИнформацияОТипеФайла.ТипФайла);
			КонецЕсли;
			Если ИнформацияОФорматеДокументооборота.ФорматДокументооборота = Перечисления.ФорматыДокументооборотаСПФР.ЭДОК
				И ИнформацияОФорматеДокументооборота.ЭтоВозможныйФормат
				И ИнформацияОТипеФайла <> Неопределено
				И ИнформацияОТипеФайла.ФорматДокументооборота = Перечисления.ФорматыДокументооборотаСПФР.ЭДОК Тогда
				
				ФорматДокументооборота = Перечисления.ФорматыДокументооборотаСПФР.ЭДОК;
			КонецЕсли;
			
		ИначеЕсли ИнформацияОФорматеДокументооборота.ФорматДокументооборота = Перечисления.ФорматыДокументооборотаСПФР.ЭДОК
			И НЕ ИнформацияОФорматеДокументооборота.ЭтоВозможныйФормат Тогда
			
			ФорматДокументооборота = Перечисления.ФорматыДокументооборотаСПФР.ЭДОК;
		КонецЕсли;
		
		// удалить переводы строк при наличии для исключения ошибок проверки подписи
		Если ФорматДокументооборота = Перечисления.ФорматыДокументооборотаСПФР.ЭДОК Тогда
			ДвоичныеДанныеВыгрузки = ПолучитьИзВременногоХранилища(СтрРезультатВыгрузкиОтчетности.Адрес);
			ИмяВременногоФайлаВыгрузки = ПолучитьИмяВременногоФайла();
			ДвоичныеДанныеВыгрузки.Записать(ИмяВременногоФайлаВыгрузки);
			Попытка
				КодировкаВыгрузки = ОпределитьКодировку(ДвоичныеДанныеВыгрузки);
			Исключение
				ИнформацияОбОшибке = ПодробноеПредставлениеОшибки(ИнформацияОбОшибке());
				ЗаписьЖурналаРегистрации(
					НСтр("ru = 'Электронный документооборот с контролирующими органами.ПрочитатьТекстИзФайла()'"), 
					УровеньЖурналаРегистрации.Ошибка,,,
					СтрШаблон("%1: %2", "Определение кодировки не удалось", ИнформацияОбОшибке));
			КонецПопытки;
			ТекстВыгрузки = ПрочитатьТекстИзФайла(ИмяВременногоФайлаВыгрузки, КодировкаВыгрузки);
			ОперацииСФайламиЭДКО.УдалитьВременныйФайл(ИмяВременногоФайлаВыгрузки);
			
			ТекстВыгрузки = СтрЗаменить(ТекстВыгрузки, Символы.ПС, "");
			ТекстВыгрузки = СтрЗаменить(ТекстВыгрузки, Символы.ВК, "");
			
			ИмяВременногоФайлаВыгрузки = ПолучитьИмяВременногоФайла();
			ЗаписатьXMLВФайл(ТекстВыгрузки, ИмяВременногоФайлаВыгрузки, КодировкаВыгрузки, Истина);
			ДвоичныеДанныеВыгрузки = Новый ДвоичныеДанные(ИмяВременногоФайлаВыгрузки);
			СтрРезультатВыгрузкиОтчетности.Адрес = ПоместитьВоВременноеХранилище(ДвоичныеДанныеВыгрузки,
				СтрРезультатВыгрузкиОтчетности.Адрес);
			ОперацииСФайламиЭДКО.УдалитьВременныйФайл(ИмяВременногоФайлаВыгрузки);
		КонецЕсли;
		
		Если НЕ ДобавитьСодержимоеТранспортногоКонтейнера(ТранспортноеСообщение.Ссылка,
														ТипСодержимогоКонтейнера,
														СтрРезультатВыгрузкиОтчетности.Адрес,
														СтрРезультатВыгрузкиОтчетности.Имя,
														,
														,
														,
														,
														,
														ТипФайлаОтчетностиПФР,
														СгенерироватьUUID(),
														ТипСодержимогоФайла) Тогда
			ОтменитьТранзакцию();
			ТекстСообщения = НСтр("ru = 'Отправка отчета прервана.'");
			ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
			Возврат Ложь;
		КонецЕсли;
	КонецЦикла;
	
	ЦиклОбмена.Идентификатор = СгенерироватьUUID(ФорматДокументооборота);
	ЦиклОбмена.ФорматДокументооборота = ФорматДокументооборота;
	
	Если ФорматДокументооборота = Перечисления.ФорматыДокументооборотаСПФР.ЭДОК
		И НЕ ДокументооборотСКОВызовСервера.ЗаявленияПоЭДООтправляютсяВПФР() Тогда
		
		ОтменитьТранзакцию();
		ТекстСообщения = НСтр("ru = 'Отправка отчета невозможна, поскольку формат эл. документооборота с ПФР еще не вступил в силу.'");
		ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
		Возврат Ложь;
	КонецЕсли;
	
	// пишем цикл обмена только (!) после записи ДИВ, потому что при записи цикла
	// используется информация из связанных ДИВ
	Попытка
		ЦиклОбмена.Записать();
	Исключение
		ОтменитьТранзакцию();
		ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ИнформацияОбОшибке().Описание);
		ТекстСообщения = НСтр("ru = 'Отправка отчета прервана.'");
		ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
		Возврат Ложь;
	КонецПопытки;
	
	ЗафиксироватьТранзакцию();
	
	Контекст.Вставить("ЦиклОбмена", ЦиклОбмена.Ссылка);
	Контекст.Вставить("ТранспортноеСообщение", ТранспортноеСообщение.Ссылка);
	Контекст.Вставить("ФорматДокументооборота", ФорматДокументооборота);
	
	Возврат Истина;
	
КонецФункции

Функция ВыгрузитьРегламентированныйОтчетПФРИзВнешнихЭлектронныхПредставлений(Контекст) Экспорт
	
	Результат = Новый Массив;
	
	ФайлыДИВ = ИзвлечьЭлектронныеПредставленияИзХранилища(Контекст.Документ);
	Если ФайлыДИВ <> Неопределено Тогда
		Для Каждого ФайлДИВ Из ФайлыДИВ Цикл
			
			СтрРезультат = Новый Структура("Документ, Тип, Имя, Адрес, ВАрхиве");
			СтрРезультат.Документ = Контекст.Документ;
			СтрРезультат.Тип = ФайлДИВ.ТипФайлаОтчетности;
			СтрРезультат.Имя = ФайлДИВ.ИмяФайла;
			СтрРезультат.ВАрхиве = ФайлДИВ.ВАрхиве;			
			СтрРезультат.Адрес = ПоместитьВоВременноеХранилище(ФайлДИВ.Данные, Контекст.УникальныйИдентификатор);
			
			Результат.Добавить(СтрРезультат);
			
		КонецЦикла;
	КонецЕсли;
	
	Возврат Результат;
	
КонецФункции

Функция ТипПоИдентификаторуТипаФайлаВыгрузки(ИдентификаторТипаФайлаВыгрузки) Экспорт
	
	Если ТипЗнч(ИдентификаторТипаФайлаВыгрузки) <> Тип("Строка") Тогда
		Возврат ИдентификаторТипаФайлаВыгрузки;
	Иначе
		Если ВРЕГ(ИдентификаторТипаФайлаВыгрузки) = ВРЕГ("сведенияПриложение") ИЛИ НЕ ЗначениеЗаполнено(ИдентификаторТипаФайлаВыгрузки) Тогда
			Возврат Перечисления.ТипыСодержимогоТранспортногоКонтейнера.СведенияПриложениеПФР;
		Иначе
			МетаданныеЗначения = Метаданные.Перечисления.ТипыФайловОтчетностиПФР.ЗначенияПеречисления.Найти(ИдентификаторТипаФайлаВыгрузки);
			Если МетаданныеЗначения <> Неопределено Тогда
				ИндексМетаданныхЗначения = Метаданные.Перечисления.ТипыФайловОтчетностиПФР.ЗначенияПеречисления.Индекс(МетаданныеЗначения);
				Возврат Перечисления.ТипыФайловОтчетностиПФР.Получить(ИндексМетаданныхЗначения);
			Иначе
				Возврат Неопределено;
			КонецЕсли;	
		КонецЕсли;
	КонецЕсли;
	
КонецФункции

Функция ПолучитьМассивПредметов(РезультатВыгрузкиОтчетности, Отчет)
	
	// формируем массив предметов
	МассивПредметов = Новый Массив;
	Для Инд = 0 По РезультатВыгрузкиОтчетности.Количество() - 1 Цикл
		Если МассивПредметов.Найти(РезультатВыгрузкиОтчетности[Инд].Документ) = Неопределено Тогда
			МассивПредметов.Добавить(РезультатВыгрузкиОтчетности[Инд].Документ);
		КонецЕсли;
	КонецЦикла;
	
	// добавляем основной, если его нет в списке
	Если МассивПредметов.Найти(Отчет) = Неопределено Тогда
		МассивПредметов.Вставить(0, Отчет);
	КонецЕсли;
	
	Возврат МассивПредметов;
	
КонецФункции

Функция ОпределитьТипФайлаПФР(ПолноеИмяФайла, ИсходноеИмяФайла = Неопределено)
	
	Результат = Новый Структура;
	Результат.Вставить("ТипФайла", 					Неопределено);
	Результат.Вставить("ФорматДокументооборота", 	Перечисления.ФорматыДокументооборотаСПФР.БПИ);
	
	// 
	ДеревоXML = ДеревоXMLИзФайла(ПолноеИмяФайла);
	Если ДеревоXML = Неопределено Тогда
		Возврат Результат;
	КонецЕсли;
	
	ОбъектФайл = Новый Файл(?(ИсходноеИмяФайла = Неопределено, ПолноеИмяФайла, ИсходноеИмяФайла));
	КороткоеИмяФайла = ОбъектФайл.Имя;
	Если ВРЕГ(Лев(КороткоеИмяФайла, 4)) = "ПФР_" Тогда
		УзелПачкаВходящихДокументов = ДеревоXML;
		
	Иначе
		Если ДеревоXML.Свойства().Получить("ПачкаВходящихДокументов") = Неопределено Тогда
			Возврат Результат;
		КонецЕсли;
		УзелПачкаВходящихДокументов = ДеревоXML.ПачкаВходящихДокументов;
	КонецЕсли;
	
	СвойстваУзла = УзелПачкаВходящихДокументов.Свойства();
	Если СвойстваУзла.Получить("АНКЕТА_ЗЛ") <> Неопределено Тогда
		Результат.ТипФайла = Перечисления.ТипыФайловОтчетностиПФР.ПачкаИС;
	ИначеЕсли СвойстваУзла.Получить("ЗАЯВЛЕНИЕ_ОБ_ОБМЕНЕ") <> Неопределено Тогда
		Результат.ТипФайла = Перечисления.ТипыФайловОтчетностиПФР.ПачкаИС;
	ИначеЕсли СвойстваУзла.Получить("ЗАЯВЛЕНИЕ_О_ДУБЛИКАТЕ") <> Неопределено Тогда
		Результат.ТипФайла = Перечисления.ТипыФайловОтчетностиПФР.ПачкаИС;
	ИначеЕсли СвойстваУзла.Получить("СВЕДЕНИЯ_О_СТРАХОВЫХ_ВЗНОСАХ_И_СТРАХОВОМ_СТАЖЕ_ЗЛ") <> Неопределено Тогда
		
		УзелСведения = УзелПачкаВходящихДокументов.СВЕДЕНИЯ_О_СТРАХОВЫХ_ВЗНОСАХ_И_СТРАХОВОМ_СТАЖЕ_ЗЛ;
		Если ТипЗнч(УзелСведения) = Тип("ОбъектXDTO") Тогда
			УзелСведенияОсновной = УзелСведения;
		Иначе
			Если УзелСведения.Количество() > 0 Тогда
				УзелСведенияОсновной = УзелСведения.Получить(0);
			Иначе
				Возврат Результат;
			КонецЕсли;
		КонецЕсли;
		
		Если УзелСведенияОсновной.Свойства().Получить("ВидФормы") <> Неопределено
		И СокрЛП(УзелСведенияОсновной.ВидФормы) = "СПВ-1" Тогда
			Результат.ТипФайла = Перечисления.ТипыФайловОтчетностиПФР.ПачкаСВВ;
		Иначе
			Результат.ТипФайла = Перечисления.ТипыФайловОтчетностиПФР.ПачкаИС;
		КонецЕсли;
		
	ИначеЕсли СвойстваУзла.Получить("СВЕДЕНИЯ_О_СТРАХОВОМ_СТАЖЕ_ЗЛ_ДЛЯ_УСТАНОВЛЕНИЯ_ПЕНСИИ") <> Неопределено Тогда
		Результат.ТипФайла = Перечисления.ТипыФайловОтчетностиПФР.ПачкаСВВ;
	ИначеЕсли СвойстваУзла.Получить("ОПИСЬ_СВЕДЕНИЙ_ПЕРЕДАВАЕМЫХ_СТРАХОВАТЕЛЕМ") <> Неопределено Тогда
		Результат.ТипФайла = Перечисления.ТипыФайловОтчетностиПФР.ПачкаАДВ;
	ИначеЕсли СвойстваУзла.Получить("СВЕДЕНИЯ_О_СУММЕ_ВЫПЛАТ_И_ВОЗНАГРАЖДЕНИЙ") <> Неопределено Тогда
		Результат.ТипФайла = Перечисления.ТипыФайловОтчетностиПФР.ПачкаИС;
	ИначеЕсли СвойстваУзла.Получить("КОНВЕРТАЦИЯ") <> Неопределено Тогда
		Если ФорматДокументооборотаСЗВК() = Перечисления.ФорматыДокументооборотаСПФР.ЭДОК Тогда
			СвойстваУзлаКонвертация = УзелПачкаВходящихДокументов.КОНВЕРТАЦИЯ.Свойства();
			Если СвойстваУзлаКонвертация.Получить("ВидФормы") <> Неопределено
				И СвойстваУзлаКонвертация.Получить("ФактПроживанияКрым") <> Неопределено Тогда
				
				УзелКонвертация = УзелПачкаВходящихДокументов.КОНВЕРТАЦИЯ;
				Если УзелКонвертация.ВидФормы = "СЗВ-К" И Строка(УзелКонвертация.ФактПроживанияКрым) = "1" Тогда
					Результат.ФорматДокументооборота = Перечисления.ФорматыДокументооборотаСПФР.ЭДОК;
				КонецЕсли;
			КонецЕсли;
		КонецЕсли;
		Результат.ТипФайла = Перечисления.ТипыФайловОтчетностиПФР.ПачкаИС;
	ИначеЕсли СвойстваУзла.Получить("РАСЧЕТ_ПО_СТРАХОВЫМ_ВЗНОСАМ_НА_ОПС_И_ОМС_ПЛАТЕЛЬЩИКАМИ_ПРОИЗВОДЯЩИМИ_ВЫПЛАТЫ_ФЛ") <> Неопределено Тогда
		Результат.ТипФайла = Перечисления.ТипыФайловОтчетностиПФР.ПачкаРСВ;
	ИначеЕсли СвойстваУзла.Получить("РАСЧЕТ_ПО_СТРАХОВЫМ_ВЗНОСАМ_НА_ОПС_И_ОМС_ПЛАТЕЛЬЩИКАМИ_СВ_ПРОИЗВОДЯЩИМИ_ВЫПЛАТЫ_ФЛ_НАЧИНАЯ_С_2012_ГОДА") <> Неопределено Тогда
		Результат.ТипФайла = Перечисления.ТипыФайловОтчетностиПФР.ПачкаРСВ;
	ИначеЕсли СвойстваУзла.Получить("РАСЧЕТ_ПО_СТРАХОВЫМ_ВЗНОСАМ_НА_ОПС_И_ОМС_ПЛАТЕЛЬЩИКАМИ_СВ_ПРОИЗВОДЯЩИМИ_ВЫПЛАТЫ_ФЛ_НАЧИНАЯ_С_2013_ГОДА") <> Неопределено Тогда
		Результат.ТипФайла = Перечисления.ТипыФайловОтчетностиПФР.ПачкаРСВ;
	ИначеЕсли СвойстваУзла.Получить("РАСЧЕТ_ПО_СТРАХОВЫМ_ВЗНОСАМ_НА_ОПС_И_ОМС_ПЛАТЕЛЬЩИКАМИ_ПРОИЗВОДЯЩИМИ_ВЫПЛАТЫ_ФЛ_2014") <> Неопределено Тогда
		Результат.ТипФайла = Перечисления.ТипыФайловОтчетностиПФР.ПачкаРСВ;
	ИначеЕсли СвойстваУзла.Получить("РАСЧЕТ_ПО_СТРАХОВЫМ_ВЗНОСАМ_НА_ОПС_И_ОМС_ПЛАТЕЛЬЩИКАМИ_ПРОИЗВОДЯЩИМИ_ВЫПЛАТЫ_ФЛ_2015") <> Неопределено Тогда
		Результат.ТипФайла = Перечисления.ТипыФайловОтчетностиПФР.ПачкаРСВ;
	ИначеЕсли СвойстваУзла.Получить("СВЕДЕНИЯ_ПО_ПАЧКЕ_ДОКУМЕНТОВ_РАЗДЕЛА_6") <> Неопределено Тогда
		Результат.ТипФайла = Перечисления.ТипыФайловОтчетностиПФР.ПачкаСВВ;
	ИначеЕсли СвойстваУзла.Получить("СВЕДЕНИЯ_О_СУММЕ_ВЫПЛАТ_И_СТРАХОВОМ_СТАЖЕ_ЗЛ") <> Неопределено Тогда
		Результат.ТипФайла = Перечисления.ТипыФайловОтчетностиПФР.ПачкаСВВ;
	ИначеЕсли СвойстваУзла.Получить("РАСЧЕТ_ПО_СТРАХОВЫМ_ВЗНОСАМ_НА_ОПС_И_ОМС_ПЛАТЕЛЬЩИКАМИ_НЕ_ПРОИЗВОДЯЩИМИ_ВЫПЛАТЫ_ФЛ") <> Неопределено Тогда
		Результат.ТипФайла = Перечисления.ТипыФайловОтчетностиПФР.ПачкаРСВ;
	ИначеЕсли СвойстваУзла.Получить("РАСЧЕТ_ПО_СТРАХОВЫМ_ВЗНОСАМ_НА_ОПС_И_ОМС_ГЛАВАМИ_КФХ") <> Неопределено Тогда
		Результат.ТипФайла = Перечисления.ТипыФайловОтчетностиПФР.ПачкаРСВ;
	ИначеЕсли СвойстваУзла.Получить("РСВ-2") <> Неопределено ИЛИ СвойстваУзла.Получить("РСВ_2") <> Неопределено Тогда
		Результат.ТипФайла = Перечисления.ТипыФайловОтчетностиПФР.ПачкаРСВ;
	ИначеЕсли СвойстваУзла.Получить("РАСЧЕТ_ПО_ВЗНОСАМ_НА_ДОПОЛНИТЕЛЬНОЕ_СОЦИАЛЬНОЕ_ОБЕСПЕЧЕНИЕ") <> Неопределено Тогда
		Результат.ТипФайла = Перечисления.ТипыФайловОтчетностиПФР.ПачкаРСВ;
	ИначеЕсли СвойстваУзла.Получить("РВ-3") <> Неопределено ИЛИ СвойстваУзла.Получить("РВ_3") <> Неопределено Тогда
		Результат.ТипФайла = Перечисления.ТипыФайловОтчетностиПФР.ПачкаРСВ;
	ИначеЕсли СвойстваУзла.Получить("ЗАЯВЛЕНИЕ_О_ДОБРОВОЛЬНОМ_ВСТУПЛЕНИИ_В_ПРАВООТНОШЕНИЯ_В_ЦЕЛЯХ_УПЛАТЫ_ДСВ") <> Неопределено Тогда
		Результат.ТипФайла = Перечисления.ТипыФайловОтчетностиПФР.ПачкаДСВ;
	ИначеЕсли СвойстваУзла.Получить("РЕЕСТР_ДСВ_РАБОТОДАТЕЛЬ") <> Неопределено Тогда
		Результат.ТипФайла = Перечисления.ТипыФайловОтчетностиПФР.РеестрДСВ;
	ИначеЕсли СвойстваУзла.Получить("СВЕДЕНИЯ_О_СУММЕ_ВЫПЛАТ_О_СТРАХОВЫХ_ВЗНОСАХ_И_СТРАХОВОМ_СТАЖЕ_ЗЛ") <> Неопределено Тогда
		Результат.ТипФайла = Перечисления.ТипыФайловОтчетностиПФР.ПачкаИС;
	ИначеЕсли СвойстваУзла.Получить("ВЕДОМОСТЬ_УПЛАТЫ") <> Неопределено Тогда
		Результат.ТипФайла = Перечисления.ТипыФайловОтчетностиПФР.ПачкаАДВ;
	ИначеЕсли СвойстваУзла.Получить("ИС_СЗВ-4-1") <> Неопределено ИЛИ СвойстваУзла.Получить("ИС_СЗВ_4_1") <> Неопределено Тогда
		Результат.ТипФайла = Перечисления.ТипыФайловОтчетностиПФР.ПачкаИС;
	ИначеЕсли СвойстваУзла.Получить("ИС_СЗВ-4-2") <> Неопределено ИЛИ СвойстваУзла.Получить("ИС_СЗВ_4_2") <> Неопределено Тогда
		Результат.ТипФайла = Перечисления.ТипыФайловОтчетностиПФР.ПачкаИС;
	ИначеЕсли СвойстваУзла.Получить("СТАРЫЕ_ИС_СЗВ-1") <> Неопределено ИЛИ СвойстваУзла.Получить("СТАРЫЕ_ИС_СЗВ_1") <> Неопределено Тогда
		Результат.ТипФайла = Перечисления.ТипыФайловОтчетностиПФР.ПачкаИС;
	ИначеЕсли СвойстваУзла.Получить("СТАРЫЕ_ИС_СЗВ-3") <> Неопределено ИЛИ СвойстваУзла.Получить("СТАРЫЕ_ИС_СЗВ_3") <> Неопределено Тогда
		Результат.ТипФайла = Перечисления.ТипыФайловОтчетностиПФР.ПачкаИС;
	ИначеЕсли СвойстваУзла.Получить("СЗВ-М") <> Неопределено ИЛИ СвойстваУзла.Получить("СЗВ_М") <> Неопределено Тогда
		Результат.ТипФайла = Перечисления.ТипыФайловОтчетностиПФР.ПачкаИС;
	ИначеЕсли СвойстваУзла.Получить("СЗВ-СТАЖ") <> Неопределено ИЛИ СвойстваУзла.Получить("СЗВ_СТАЖ") <> Неопределено Тогда
		Результат.ТипФайла = Перечисления.ТипыФайловОтчетностиПФР.ПачкаИС;
	ИначеЕсли СвойстваУзла.Получить("СЗВ-КОРР") <> Неопределено ИЛИ СвойстваУзла.Получить("СЗВ_КОРР") <> Неопределено Тогда
		Результат.ТипФайла = Перечисления.ТипыФайловОтчетностиПФР.ПачкаИС;
	ИначеЕсли СвойстваУзла.Получить("СЗВ-ИСХ") <> Неопределено ИЛИ СвойстваУзла.Получить("СЗВ_ИСХ") <> Неопределено Тогда
		Результат.ТипФайла = Перечисления.ТипыФайловОтчетностиПФР.ПачкаИС;
	ИначеЕсли СвойстваУзла.Получить("ОДВ-1") <> Неопределено ИЛИ СвойстваУзла.Получить("ОДВ_1") <> Неопределено Тогда
		Результат.ТипФайла = Перечисления.ТипыФайловОтчетностиПФР.ПачкаИС;
	ИначеЕсли СвойстваУзла.Получить("СЗВ-ТД") <> Неопределено ИЛИ СвойстваУзла.Получить("СЗВ_ТД") <> Неопределено Тогда
		Результат.ТипФайла = Перечисления.ТипыФайловОтчетностиПФР.ПачкаИС;
		Результат.ФорматДокументооборота = Перечисления.ФорматыДокументооборотаСПФР.ЭДОК;
	ИначеЕсли СвойстваУзла.Получить("ЗПЭД") <> Неопределено Тогда
		Результат.ТипФайла = Перечисления.ТипыФайловОтчетностиПФР.ПачкаИС;
		Результат.ФорматДокументооборота = Перечисления.ФорматыДокументооборотаСПФР.ЭДОК;
	ИначеЕсли СвойстваУзла.Получить("ЗОЭД") <> Неопределено Тогда
		Результат.ТипФайла = Перечисления.ТипыФайловОтчетностиПФР.ПачкаИС;
		Результат.ФорматДокументооборота = Перечисления.ФорматыДокументооборотаСПФР.ЭДОК;
	ИначеЕсли СвойстваУзла.Получить("УПУП") <> Неопределено Тогда
		Результат.ТипФайла = Перечисления.ТипыФайловОтчетностиПФР.ПачкаИС;
		Результат.ФорматДокументооборота = Перечисления.ФорматыДокументооборотаСПФР.ЭДОК;
	ИначеЕсли СвойстваУзла.Получить("УПРУП") <> Неопределено Тогда
		Результат.ТипФайла = Перечисления.ТипыФайловОтчетностиПФР.ПачкаИС;
		Результат.ФорматДокументооборота = Перечисления.ФорматыДокументооборотаСПФР.ЭДОК;
	Иначе
		Результат.ТипФайла = Перечисления.ТипыФайловОтчетностиПФР.ПустаяСсылка();
	КонецЕсли;
	
	Возврат Результат;
	
КонецФункции

Функция ДеревоXMLИзФайла(ПутьКФайлу)
	
	Попытка
		ЧтениеXML = Новый ЧтениеXML;
		ЧтениеXML.ОткрытьФайл(ПутьКФайлу);
		Фабрика = Новый ФабрикаXDTO;
		ДеревоXML = Фабрика.ПрочитатьXML(ЧтениеXML);
		ЧтениеXML.Закрыть();
		Возврат ДеревоXML;
	Исключение
		Возврат Неопределено;
	КонецПопытки;
	
КонецФункции

Функция ФорматДокументооборотаСПФР(
		ОтчетИлиВидОтправляемогоОтчета,
		ПолноеИмяФайла = Неопределено,
		ИсходноеИмяФайла = Неопределено) Экспорт
	
	Если ЗначениеЗаполнено(ОтчетИлиВидОтправляемогоОтчета) И ТипЗнч(ОтчетИлиВидОтправляемогоОтчета) <>
		Тип("СправочникСсылка.ВидыОтправляемыхДокументов") Тогда
		
		ТипЗнчОтчета = ТипЗнч(ОтчетИлиВидОтправляемогоОтчета);
		МетаданныеОтчета = Метаданные.НайтиПоТипу(ТипЗнчОтчета);
		Если МетаданныеОтчета = Неопределено Тогда
			ВидОтправляемогоОтчета = ОтчетИлиВидОтправляемогоОтчета;
			
		Иначе
			ИмяМетаданныхОтчета = МетаданныеОтчета.Имя;
			Если ИмяМетаданныхОтчета = "РегламентированныйОтчет" Тогда
				ВидОтчетаИлиСтрока = ОбщегоНазначения.ЗначениеРеквизитаОбъекта(ОтчетИлиВидОтправляемогоОтчета, "ИсточникОтчета");
			ИначеЕсли ИмяМетаданныхОтчета = "ЭлектронныеПредставленияРегламентированныхОтчетов" Тогда
				ВидОтчетаИлиСтрока = ОбщегоНазначения.ЗначениеРеквизитаОбъекта(ОтчетИлиВидОтправляемогоОтчета, "ВидОтчета");
			Иначе
				ВидОтчетаИлиСтрока = ИмяМетаданныхОтчета;
			КонецЕсли;
			
			Если ТипЗнч(ВидОтчетаИлиСтрока) = Тип("СправочникСсылка.ВидыОтправляемыхДокументов") Тогда
				ВидОтправляемогоОтчета = ВидОтчетаИлиСтрока;
				
			ИначеЕсли ВидОтчетаИлиСтрока = "ПерепискаСКонтролирующимиОрганами"
				ИЛИ ВидОтчетаИлиСтрока = "ЗапросНаИнформационноеОбслуживаниеСтрахователя"
				ИЛИ ВидОтчетаИлиСтрока = "МакетыПенсионныхДел" ИЛИ ВидОтчетаИлиСтрока = "ЗаявлениеОНазначенииПенсии" Тогда
				
				ВидОтправляемогоОтчета = ОтчетИлиВидОтправляемогоОтчета;
				
			Иначе
				ВидОтправляемогоОтчета = Справочники.ВидыОтправляемыхДокументов.НайтиПоРеквизиту("Источник", ВидОтчетаИлиСтрока);
				Если ВидОтправляемогоОтчета = Справочники.ВидыОтправляемыхДокументов.ПустаяСсылка() Тогда
					ВидОтправляемогоОтчетаПереопределяемый = ВидОтправляемогоДокументаПоСсылке(ОтчетИлиВидОтправляемогоОтчета);
					Если ЗначениеЗаполнено(ВидОтправляемогоОтчетаПереопределяемый) Тогда
						ВидОтправляемогоОтчета = ВидОтправляемогоОтчетаПереопределяемый;
					Иначе
						ВидОтправляемогоОтчета = ОтчетИлиВидОтправляемогоОтчета;
					КонецЕсли;
				КонецЕсли;
			КонецЕсли;
		КонецЕсли;
		
	Иначе
		ВидОтправляемогоОтчета = ОтчетИлиВидОтправляемогоОтчета;
	КонецЕсли;
	
	Результат = Новый Структура;
	Результат.Вставить("ФорматДокументооборота", 	Перечисления.ФорматыДокументооборотаСПФР.БПИ);
	Результат.Вставить("ЭтоВозможныйФормат", 		Ложь);
	
	Если ВидОтправляемогоОтчета = Справочники.ВидыОтправляемыхДокументов.СведенияОТрудовойДеятельностиРаботниковСЗВ_ТД
		ИЛИ ВидОтправляемогоОтчета = Справочники.ВидыОтправляемыхДокументов.ЗаявленияПоЭлДокументооборотуСПФР Тогда
		Результат.ФорматДокументооборота 	= Перечисления.ФорматыДокументооборотаСПФР.ЭДОК;
		Результат.ЭтоВозможныйФормат 		= Ложь;
		
	ИначеЕсли ФорматДокументооборотаСЗВК() = Перечисления.ФорматыДокументооборотаСПФР.ЭДОК
		И ВидОтправляемогоОтчета = Справочники.ВидыОтправляемыхДокументов.СведенияОТрудовомСтажеЗастрахованногоЛицаСЗВК Тогда
		
		Если ПолноеИмяФайла = Неопределено Тогда
			Результат.ФорматДокументооборота 	= Перечисления.ФорматыДокументооборотаСПФР.ЭДОК;
			Результат.ЭтоВозможныйФормат 		= Истина;
			
		Иначе
			Результат.ФорматДокументооборота 	= ОпределитьТипФайлаПФР(ПолноеИмяФайла, ИсходноеИмяФайла).ФорматДокументооборота;
			Результат.ЭтоВозможныйФормат 		= Ложь;
		КонецЕсли;
	КонецЕсли;
	
	Возврат Результат;
	
КонецФункции

Функция ФорматДокументооборотаСЗВК()
	
	Возврат Перечисления.ФорматыДокументооборотаСПФР.БПИ;
	
КонецФункции

#КонецОбласти

///////////////////////////////////////////////////////////////////////////////
// Получение требований и представление документов в налоговые органы

Функция ПолучитьКоличествоОтветовНаТребования(Требования) Экспорт
	
	КоличествоОтветов = Новый Соответствие;
	
	Если ТипЗнч(Требования) = Тип("Массив") Тогда
		Для каждого Требование Из Требования Цикл
			КоличествоОтветов.Вставить(Требование, 0);
		КонецЦикла;
	Иначе
		КоличествоОтветов.Вставить(Требования, 0);
	КонецЕсли;
	
	Запрос = Новый Запрос("ВЫБРАТЬ РАЗРЕШЕННЫЕ
	                      |	ЕСТЬNULL(КОЛИЧЕСТВО(РАЗЛИЧНЫЕ ОписиИсходящихДокументовВНалоговыеОрганы.Ссылка), 0) КАК КоличествоОтветов,
	                      |	ОписиИсходящихДокументовВНалоговыеОрганы.Основание КАК Требование
	                      |ПОМЕСТИТЬ Ответы
	                      |ИЗ
	                      |	Справочник.ОписиИсходящихДокументовВНалоговыеОрганы КАК ОписиИсходящихДокументовВНалоговыеОрганы
	                      |ГДЕ
	                      |	ОписиИсходящихДокументовВНалоговыеОрганы.Основание В(&Требования)
	                      |
	                      |СГРУППИРОВАТЬ ПО
	                      |	ОписиИсходящихДокументовВНалоговыеОрганы.Основание
	                      |
	                      |ОБЪЕДИНИТЬ ВСЕ
	                      |
	                      |ВЫБРАТЬ
	                      |	ЕСТЬNULL(КОЛИЧЕСТВО(РАЗЛИЧНЫЕ ПоясненияКДекларацииПоНДС.Ссылка), 0),
	                      |	ПоясненияКДекларацииПоНДС.Требование
	                      |ИЗ
	                      |	Документ.ПоясненияКДекларацииПоНДС КАК ПоясненияКДекларацииПоНДС
	                      |ГДЕ
	                      |	ПоясненияКДекларацииПоНДС.Требование В(&Требования)
	                      |
	                      |СГРУППИРОВАТЬ ПО
	                      |	ПоясненияКДекларацииПоНДС.Требование
	                      |
	                      |ОБЪЕДИНИТЬ ВСЕ
	                      |
	                      |ВЫБРАТЬ
	                      |	ЕСТЬNULL(КОЛИЧЕСТВО(РАЗЛИЧНЫЕ ПерепискаСКонтролирующимиОрганами.Ссылка), 0),
	                      |	ПерепискаСКонтролирующимиОрганами.Требование
	                      |ИЗ
	                      |	Справочник.ПерепискаСКонтролирующимиОрганами КАК ПерепискаСКонтролирующимиОрганами
	                      |ГДЕ
	                      |	ПерепискаСКонтролирующимиОрганами.Требование В(&Требования)
	                      |
	                      |СГРУППИРОВАТЬ ПО
	                      |	ПерепискаСКонтролирующимиОрганами.Требование
	                      |;
	                      |
	                      |////////////////////////////////////////////////////////////////////////////////
	                      |ВЫБРАТЬ
	                      |	Ответы.Требование КАК Требование,
	                      |	СУММА(Ответы.КоличествоОтветов) КАК КоличествоОтветов
	                      |ИЗ
	                      |	Ответы КАК Ответы
	                      |
	                      |СГРУППИРОВАТЬ ПО
	                      |	Ответы.Требование");
						  
	Запрос.УстановитьПараметр("Требования", Требования);
	Выборка = Запрос.Выполнить().Выбрать();
	Пока Выборка.Следующий() Цикл
		КоличествоОтветов.Вставить(Выборка.Требование, Выборка.КоличествоОтветов);
	КонецЦикла;
	
	Для каждого ТекущаяЗапись Из КоличествоОтветов Цикл
		
		Требование = ТекущаяЗапись.Ключ;
		
		Если Требование.ВидДокумента = Перечисления.ВидыНалоговыхДокументов.ТребованиеОПредставленииДокументов Тогда
			
			Ответы1 = УведомлениеОСпецрежимахНалогообложения.ОтветыНаТребование(
				Требование, 
				Перечисления.ВидыУведомленийОСпецрежимахНалогообложения.РеестрДокументовПодтверждающихЛьготы);
				
			Ответы2 = УведомлениеОСпецрежимахНалогообложения.ОтветыНаТребование(
				Требование, 
				Перечисления.ВидыУведомленийОСпецрежимахНалогообложения.НевозможностьПредоставленияДокументов);
			
			КоличествоОтветовУведомлениями = Ответы1.Количество() + Ответы2.Количество();
			
			НовоеКоличество = ТекущаяЗапись.Значение + КоличествоОтветовУведомлениями;
			КоличествоОтветов.Вставить(Требование, НовоеКоличество);
			
		КонецЕсли;
		
	КонецЦикла;

	Возврат КоличествоОтветов;
	
КонецФункции

Функция ПолучитьМассивТребованийПоОписиВходящихДокументов(ОписьВходящихДокументовСсылка, ВидДокумента = Неопределено) Экспорт
	
	МассивТребований = Новый Массив;
	
	Запрос = Новый Запрос;
	Запрос.Текст = 
		"ВЫБРАТЬ РАЗРЕШЕННЫЕ 
		|	ОписиВходящихДокументовИзНалоговыхОргановВходящиеДокументы.СсылкаНаОбъект
		|ИЗ
		|	Справочник.ОписиВходящихДокументовИзНалоговыхОрганов.ВходящиеДокументы КАК ОписиВходящихДокументовИзНалоговыхОргановВходящиеДокументы
		|ГДЕ
		|	ОписиВходящихДокументовИзНалоговыхОргановВходящиеДокументы.Ссылка = &ОписьВходящихДокументовСсылка
		|	И ОписиВходящихДокументовИзНалоговыхОргановВходящиеДокументы.СсылкаНаОбъект.ВидДокумента в (&ВидДокументаТребование)";

	Запрос.УстановитьПараметр("ОписьВходящихДокументовСсылка", ОписьВходящихДокументовСсылка);
	
	ВидыДокументов = Новый Массив;
	Если ВидДокумента = Неопределено Тогда
		ВидыДокументов.Добавить(Перечисления.ВидыНалоговыхДокументов.ТребованиеОПредставленииДокументов);
		ВидыДокументов.Добавить(Перечисления.ВидыНалоговыхДокументов.ТребованиеОПредставленииПоясненийКДекларацииНДС);
	Иначе
		ВидыДокументов.Добавить(ВидДокумента);
	КонецЕсли;
	
	Запрос.УстановитьПараметр("ВидДокументаТребование", ВидыДокументов);

	Результат = Запрос.Выполнить();

	Выборка = Результат.Выбрать();

	Пока Выборка.Следующий() Цикл
		МассивТребований.Добавить(Выборка.СсылкаНаОбъект);
	КонецЦикла;
	
	Возврат МассивТребований;
	
КонецФункции

Функция ПолучитьТекстКоличествоОтветов(ЧислоОтветов) Экспорт
	
	ТекстКоличествоОтветов = СтрЗаменить(ЧислоПрописью(ЧислоОтветов, "НП=Истина, НД=Ложь", "ответ, ответа, ответов, м, , , , ,0"),
										 ЧислоПрописью(ЧислоОтветов, "НП=Ложь, НД=Ложь", " , , , , , , , ,0"),
										 "");
	Возврат Формат(ЧислоОтветов, "ЧГ=") + " " + ТекстКоличествоОтветов;
	
КонецФункции 

Процедура ПередЗаписьюСканированныеДокументыДляПередачиВЭлектронномВиде(Объект, Отказ)

	Если Объект.ПометкаУдаления Тогда
		Возврат;
	КонецЕсли;
	
	Если Объект.ВерсияПриказа = Перечисления.ПриказОписиИсходящихДокументов.ПриказММВ_7_6_16 Тогда
		Объект.Наименование = Объект.СведенияДокумента;
	Иначе
		
		Если ЗначениеЗаполнено(Объект.ДатаДокумента) Тогда
			НомерИДата = Объект.НомерДокумента + " от " + Формат(Объект.ДатаДокумента, "ДФ='dd.MM.yyyy ""г.""'");
		Иначе
			НомерИДата = Объект.НомерДокумента;
		КонецЕсли;
		Наименование = Строка(Объект.ВидДокумента) + " " + НомерИДата;
		
		Если ЗначениеЗаполнено(Наименование) Тогда
			Объект.Наименование = Наименование;
		КонецЕсли; 
	
	КонецЕсли;
	
КонецПроцедуры

Процедура ПередЗаписьюОписиВходящихДокументов(Объект, Отказ)
	
	Если Объект.ПометкаУдаления Тогда
		Возврат;
	КонецЕсли;
	
	Если НЕ ЗначениеЗаполнено(Объект.Наименование) Тогда
	
		Объект.Наименование = "Опись входящих документов от НО " + Объект.НалоговыйОрган + " от " + Формат(Объект.ДатаСообщения, "ДЛФ=D");
	
	КонецЕсли;

КонецПроцедуры

Процедура ПередЗаписьюОписиИсходящихДокументов(Объект, Отказ)
	
	Если Объект.ПометкаУдаления Тогда
		Возврат;
	КонецЕсли;
	
	Объект.Наименование = НаименованиеОписиИсхДок(Объект);

КонецПроцедуры

Функция НаименованиеОписиИсхДок(Объект) Экспорт
	
	Основание = Объект.Основание;
	
	Если ЗначениеЗаполнено(Основание) Тогда
		
		Если ДокументооборотСКОВызовСервера.ЭтоПакетСДопДокументами(Объект) Тогда 
			
			СведенияПоОбъекту = СведенияПоОтправляемымОбъектам(Объект.Основание);
			Наименование = ДокументооборотСКОКлиентСервер.ПредставлениеПакетаСДопДокументами() + НСтр("ru = ' к:'") + Символы.ПС + СведенияПоОбъекту.Наименование;
			
		Иначе
			
			Наименование = НСтр("ru = 'Ответ на требование о представлении документов'");
			Наименование = Наименование + " " + Строка(Основание.НомерДокумента) + " от " + Формат(Основание.ДатаДокумента, "ДЛФ=D");
			
	КонецЕсли;
	
	Иначе
		Наименование = ДокументооборотСКОКлиентСервер.ПредставлениеПакетаСДопДокументами();
	КонецЕсли;
	
	Возврат Наименование;
	
КонецФункции

Функция ЭтоПакетСДопДокументами(ОписьИсхДок) Экспорт
	
	ТипОписьСсылка = Тип("СправочникСсылка.ОписиИсходящихДокументовВНалоговыеОрганы");
	ТипОписьОбъект = Тип("СправочникОбъект.ОписиИсходящихДокументовВНалоговыеОрганы");
	
	Если ТипЗнч(ОписьИсхДок) = ТипОписьСсылка
		ИЛИ ТипЗнч(ОписьИсхДок) = ТипОписьОбъект
		ИЛИ (ТипЗнч(ОписьИсхДок) = Тип("ДанныеФормыСтруктура") И ТипЗнч(ОписьИсхДок.Ссылка) = ТипОписьСсылка) Тогда
		
		Если ТипЗнч(ОписьИсхДок.Основание) = Тип("СправочникСсылка.ДокументыРеализацииПолномочийНалоговыхОрганов")
			И ОписьИсхДок.Основание.ВидДокумента = Перечисления.ВидыНалоговыхДокументов.ТребованиеОПредставленииДокументов Тогда
		
			ЭтоПакетСДопДокументами = Ложь;
			
		Иначе
			
			ЭтоПакетСДопДокументами = Истина;
			
		КонецЕсли;
		
	Иначе
		
		ЭтоПакетСДопДокументами = Ложь;
		
	КонецЕсли;
	
	Возврат ЭтоПакетСДопДокументами;
	
КонецФункции

Процедура ОбработкаЗаполненияОписиИсходящихДокументов(Объект, ДанныеЗаполнения)
	
	Если ДанныеЗаполнения = Неопределено Тогда
		Возврат;
	КонецЕсли;
	
	Если НЕ ДанныеЗаполнения.Свойство("ОснованиеСсылка") Тогда
		Возврат;
	КонецЕсли;
	
	Основание = ДанныеЗаполнения.ОснованиеСсылка;
	
	Если ТипЗнч(Основание) = Тип("СправочникСсылка.ДокументыРеализацииПолномочийНалоговыхОрганов") Тогда	
		
		Объект.ДатаСоздания = ТекущаяДатаСеанса();
		Объект.Основание = Основание;    
		Объект.НалоговыйОрган = Основание.НалоговыйОрган;
		Объект.Организация = Основание.Организация;
		Объект.ОписьВходящихДокументов = ДокументооборотСКОВызовСервера.ПолучитьОписьВходящихДокументовПоТребованию(Основание);
		
	ИначеЕсли ТипЗнч(Основание) = Тип("СправочникСсылка.ОписиВходящихДокументовИзНалоговыхОрганов") Тогда
		
		Объект.ДатаСоздания = ТекущаяДатаСеанса();
		Объект.НалоговыйОрган = Основание.НалоговыйОрган;
		Объект.Организация = Основание.Организация;
		Объект.ОписьВходящихДокументов = Основание;
		
		// Здесь интересуют только требования представления документов,
		// потому что опись исходящих документов является ответом только
		// для требования представления документов
		МассивТребований = ПолучитьМассивТребованийПоОписиВходящихДокументов(
			Основание,
			Перечисления.ВидыНалоговыхДокументов.ТребованиеОПредставленииДокументов);
		
		Если МассивТребований.Количество() = 1 Тогда
			Объект.Основание = МассивТребований[0];	
		КонецЕсли;
		
	КонецЕсли;
	
КонецПроцедуры

// есть в контейнере
Функция ПолучитьФайлыДокументовРеализацииПолномочийНалоговыхОрганов(СсылкаДокумент, ИменаВложений = Неопределено) Экспорт
	
	Запрос = Новый Запрос("ВЫБРАТЬ РАЗРЕШЕННЫЕ
						  |	ФайлыДокументовРеализацииПолномочийНалоговыхОрганов.ИмяФайла КАК ИмяФайла,
						  |	ФайлыДокументовРеализацииПолномочийНалоговыхОрганов.Данные КАК Данные
						  |	, ВЫБОР
					  	  |		КОГДА СостояниеОбъектов.Архивный ЕСТЬ NULL ТОГДА Ложь
					   	  |		КОГДА СостояниеОбъектов.Архивный = Ложь ТОГДА Ложь
					   	  |	ИНАЧЕ
					   	  |		Истина
					   	  |	КОНЕЦ ВАрхиве
						  |ИЗ
						  |	РегистрСведений.ФайлыДокументовРеализацииПолномочийНалоговыхОрганов КАК ФайлыДокументовРеализацииПолномочийНалоговыхОрганов
						  |
						  |		ЛЕВОЕ СОЕДИНЕНИЕ РегистрСведений.ПризнакиАрхивированияФайловДОСКонтролирующимиОрганами КАК СостояниеОбъектов
						  |			ПО 		(СостояниеОбъектов.Объект = ФайлыДокументовРеализацииПолномочийНалоговыхОрганов.Документ)
						  |				И	(СостояниеОбъектов.ИмяФайла = ФайлыДокументовРеализацииПолномочийНалоговыхОрганов.ИмяФайла)
						  |				И	(СостояниеОбъектов.Владелец = Значение(Перечисление.ВидыАрхивируемыхМетаданныхДО.ФайлыДокументовРеализацииПолномочийНалоговыхОрганов))
						  |
						  |ГДЕ
						  |	ФайлыДокументовРеализацииПолномочийНалоговыхОрганов.Документ = &СсылкаДокумент");
	Запрос.УстановитьПараметр("СсылкаДокумент", СсылкаДокумент);
	Если ИменаВложений <> Неопределено Тогда
		Запрос.Текст = Запрос.Текст + "
						  |	И ФайлыДокументовРеализацииПолномочийНалоговыхОрганов.ИмяФайла В(&ИмяФайла)";
		Запрос.УстановитьПараметр("ИмяФайла", ИменаВложений);
	КонецЕсли;
	Возврат Запрос.Выполнить().Выгрузить();
	
КонецФункции

// помещает в регистр сведений ФайлыДокументовРеализацииПолномочийНалоговыхОрганов данные файлов, расположенных во временном хранилище
// ТаблицаДобавляемыхФайлов - таблица значений: ИмяФайла, АдресФайла
Функция ПоместитьФайлыДокументовРеализацииПолномочийНалоговыхОрганов(СсылкаДокумент, ТаблицаДобавляемыхФайлов) Экспорт
	
	Попытка
		// Наполняем регистр сведений ФайлыДокументовРеализацииПолномочийНалоговыхОрганов файлами
		НаборЗаписей = РегистрыСведений.ФайлыДокументовРеализацииПолномочийНалоговыхОрганов.СоздатьНаборЗаписей(); 
		НаборЗаписей.Отбор.Документ.Установить(СсылкаДокумент); 
		НаборЗаписей.Прочитать();
		
		Для каждого СтрокаТЗ Из ТаблицаДобавляемыхФайлов Цикл
			
			ИмяФайла 					= СтрокаТЗ.ИмяФайла;
			АдресВоВременномХранилище 	= СтрокаТЗ.АдресФайла;
			
			РасширениеСТочкой = нрег(Прав(ИмяФайла, 4));
			Если РасширениеСТочкой = ".xml" Тогда
				ТипСодержимого = Перечисления.ТипыСодержимогоФайлов.xml;
			Иначе
				ТипСодержимого = Перечисления.ТипыСодержимогоФайлов.Неизвестный;
			КонецЕсли;
			
			ФайлДвоичныеДанные = ПолучитьИзВременногоХранилища(АдресВоВременномХранилище);
			РазмерФайла = ФайлДвоичныеДанные.Размер();
			
			НоваяЗапись = НаборЗаписей.Добавить();
			
			НоваяЗапись.Документ = СсылкаДокумент;
			НоваяЗапись.ИмяФайла = ИмяФайла;
			НоваяЗапись.ТипСодержимого = ТипСодержимого;
			НоваяЗапись.Размер = РазмерФайла;
			НоваяЗапись.Данные = Новый ХранилищеЗначения(ФайлДвоичныеДанные, Новый СжатиеДанных(9));;
			
		КонецЦикла;
		
		НаборЗаписей.Записать();
		
	Исключение
		
		ИнформацияОбОшибке = ИнформацияОбОшибке();
		
		ЗаписьЖурналаРегистрации(
			НСтр("ru = 'Запись вложений документов реализации полномочий налогоых органов'", ОбщегоНазначения.КодОсновногоЯзыка()), 
			УровеньЖурналаРегистрации.Ошибка,,,
			ПодробноеПредставлениеОшибки(ИнформацияОбОшибке));
		
		Возврат Ложь;
		
	КонецПопытки;
	
	Возврат Истина;
	
КонецФункции

Функция УдалитьФайлыДокументовРеализацииПолномочийНалоговыхОргановПоВладельцу(ВладелецСсылка, МассивИменФайлов = Неопределено) Экспорт
	
	НаборЗаписей = РегистрыСведений.ФайлыДокументовРеализацииПолномочийНалоговыхОрганов.СоздатьНаборЗаписей(); 
	НаборЗаписей.Отбор.Документ.Установить(ВладелецСсылка);
	
	Если МассивИменФайлов = Неопределено Тогда
		//Требуется удалить все файлы владельца		
		НаборЗаписей.Записать();
	Иначе
		Для каждого ИмяФайла Из МассивИменФайлов Цикл
			НаборЗаписей.Отбор.ИмяФайла.Установить(ИмяФайла);
			НаборЗаписей.Записать();
		КонецЦикла;
	КонецЕсли;
	
КонецФункции

Функция ИзменитьВладельцаФайловДокументовРеализацииПолномочийНалоговыхОрганов(ОписьСсылка, ТаблицаФайловИзменяющихВладельца)
	
	Попытка
		
		МенЗап = РегистрыСведений.ФайлыДокументовРеализацииПолномочийНалоговыхОрганов.СоздатьМенеджерЗаписи();
		
		Для каждого СтрокаТЗ Из ТаблицаФайловИзменяющихВладельца Цикл
			
			МенЗап.Документ = ОписьСсылка;
			МенЗап.ИмяФайла = СтрокаТЗ.ИмяФайла;
			МенЗап.Прочитать();
			Если МенЗап.Выбран() Тогда
				МенЗап.Документ = СтрокаТЗ.НовыйВладелец;
				МенЗап.Записать(Истина);
			КонецЕсли;
			
		КонецЦикла;
		
	Исключение
		
		Возврат Ложь;
		
	КонецПопытки;
	
	Возврат Истина;
	
КонецФункции

Функция ОриентировочныйРазмерОписиИсходящихДокументовПриОтправке(ОписьСсылка) Экспорт
	
	//Вычислим размер файлов скандокументов описи
	Запрос = Новый Запрос("ВЫБРАТЬ РАЗРЕШЕННЫЕ
	|	ОписиИсходящихДокументовВНалоговыеОрганыПредставляемыеДокументы.СсылкаНаОбъект
	|ИЗ
	|	Справочник.ОписиИсходящихДокументовВНалоговыеОрганы.ПредставляемыеДокументы КАК ОписиИсходящихДокументовВНалоговыеОрганыПредставляемыеДокументы
	|ГДЕ
	|	ОписиИсходящихДокументовВНалоговыеОрганыПредставляемыеДокументы.Ссылка = &ОписьСсылка
	|	И ОписиИсходящихДокументовВНалоговыеОрганыПредставляемыеДокументы.СсылкаНаОбъект ССЫЛКА Справочник.СканированныеДокументыДляПередачиВЭлектронномВиде");
	
	Запрос.УстановитьПараметр("ОписьСсылка", ОписьСсылка);
	
	МасивСканДокументов = Запрос.Выполнить().Выгрузить().ВыгрузитьКолонку("СсылкаНаОбъект");
	
	РазмерСканДокументов = ПолучитьСтруктуруРазмеровСканДокументов(МасивСканДокументов).Размер;
	
	//Вычислим размер файлов описи
	//Сформируем список сжимаемых типов содержимого файлов
	СписокСжимаемыхТипов = Новый СписокЗначений;
	СписокСжимаемыхТипов.Добавить(Перечисления.ТипыСодержимогоФайлов.xml);
	СписокСжимаемыхТипов.Добавить(Перечисления.ТипыСодержимогоФайлов.Текст1251);
	СписокСжимаемыхТипов.Добавить(Перечисления.ТипыСодержимогоФайлов.Текст866);
	
	// Определим ориентировочный размер описи
	Запрос = Новый Запрос("ВЫБРАТЬ РАЗРЕШЕННЫЕ
	|	ЕСТЬNULL(СУММА(ВЫБОР
	|				КОГДА ФайлыДокументовРеализацииПолномочийНалоговыхОрганов.ТипСодержимого В (&СписокСжимаемыхТипов)
	|					ТОГДА ФайлыДокументовРеализацииПолномочийНалоговыхОрганов.Размер * &КоэффициентСжатия
	|				ИНАЧЕ ФайлыДокументовРеализацииПолномочийНалоговыхОрганов.Размер
	|			КОНЕЦ), 0) КАК РазмерФайловОписи
	|ИЗ
	|	РегистрСведений.ФайлыДокументовРеализацииПолномочийНалоговыхОрганов КАК ФайлыДокументовРеализацииПолномочийНалоговыхОрганов
	|ГДЕ
	|	ФайлыДокументовРеализацииПолномочийНалоговыхОрганов.Документ  = &ОписьСсылка");
	
	Запрос.УстановитьПараметр("ОписьСсылка", ОписьСсылка);
	Запрос.УстановитьПараметр("СписокСжимаемыхТипов", СписокСжимаемыхТипов);
	Запрос.УстановитьПараметр("КоэффициентСжатия", 0.15);
	
	Выборка = Запрос.Выполнить().Выбрать();
	
	РазмерФайловОписи = 0;
	Если Выборка.Следующий() Тогда
		РазмерФайловОписи = Выборка.РазмерФайловОписи;
	КонецЕсли;
	
	Возврат РазмерФайловОписи + РазмерСканДокументов;
	
КонецФункции

Функция КоличествоФайловОписиИсходящихДокументовПриОтправке(ОписьСсылка) Экспорт
	
	// Общее количество файлов состоит из:
	// Документы Такском и Документы НДС - каждый состоит из двух файлов (файл выгрузки и файл подписи), документы типов "акт" и "ТОРГ-12" состоят из 4 файлов
	// Сканированные документы - каждый состоит из произвольного количества файлов изображений
	// Файлы подписи на каждый из файлов из пунктов 1 и 2 (то есть количество передаваемых файлов увеличивается вдвое)

	// Определелим количество файлов всех скандокументов данной описи
	Запрос = Новый Запрос("ВЫБРАТЬ РАЗРЕШЕННЫЕ
	|	КОЛИЧЕСТВО(СканированныеДокументыДляПередачиВЭлектронномВидеПрисоединенныеФайлы.Ссылка) КАК КоличествоФайловСканДокументовОписи
	|ИЗ
	|	Справочник.ОписиИсходящихДокументовВНалоговыеОрганы.ПредставляемыеДокументы КАК ОписиИсходящихДокументовВНалоговыеОрганыПредставляемыеДокументы
	|		ЛЕВОЕ СОЕДИНЕНИЕ Справочник.СканированныеДокументыДляПередачиВЭлектронномВидеПрисоединенныеФайлы КАК СканированныеДокументыДляПередачиВЭлектронномВидеПрисоединенныеФайлы
	|		ПО ОписиИсходящихДокументовВНалоговыеОрганыПредставляемыеДокументы.СсылкаНаОбъект = СканированныеДокументыДляПередачиВЭлектронномВидеПрисоединенныеФайлы.ВладелецФайла
	|ГДЕ
	|	ОписиИсходящихДокументовВНалоговыеОрганыПредставляемыеДокументы.Ссылка = &ОписьСсылка
	|	И ОписиИсходящихДокументовВНалоговыеОрганыПредставляемыеДокументы.СсылкаНаОбъект ССЫЛКА Справочник.СканированныеДокументыДляПередачиВЭлектронномВиде");
	
	Запрос.УстановитьПараметр("ОписьСсылка", ОписьСсылка);
	
	Выборка = Запрос.Выполнить().Выбрать();
	
	КоличествоФайловСканДокументовОписи = 0;
	
	Если Выборка.Следующий() Тогда
		КоличествоФайловСканДокументовОписи = Выборка.КоличествоФайловСканДокументовОписи;
	КонецЕсли;
	
	// Определим количество файлов выгрузки, прикрепленных к документам, входящим в состав описи (сюда войдут документы БЭД, документы НДС и документы, загруженные из пакета обмена)
	Запрос = Новый Запрос("ВЫБРАТЬ РАЗРЕШЕННЫЕ
	|	КОЛИЧЕСТВО(ОписиИсходящихДокументовВНалоговыеОрганыПредставляемыеДокументы.ИмяФайлаДанных) КАК КоличествоФайловВыгрузки
	|ИЗ
	|	Справочник.ОписиИсходящихДокументовВНалоговыеОрганы.ПредставляемыеДокументы КАК ОписиИсходящихДокументовВНалоговыеОрганыПредставляемыеДокументы
	|ГДЕ
	|	ОписиИсходящихДокументовВНалоговыеОрганыПредставляемыеДокументы.Ссылка = &ОписьСсылка
	|	И ОписиИсходящихДокументовВНалоговыеОрганыПредставляемыеДокументы.ИмяФайлаДанных > """"");

	Запрос.УстановитьПараметр("ОписьСсылка", ОписьСсылка);
	
	Выборка = Запрос.Выполнить().Выбрать();
	
	КоличествоФайловВыгрузки = 0;
	
	Если Выборка.Следующий() Тогда
		КоличествоФайловВыгрузки = Выборка.КоличествоФайловВыгрузки;
	КонецЕсли;
	
	КоличествоФайловВложений = (КоличествоФайловВыгрузки * 2) + КоличествоФайловСканДокументовОписи;
	
	// Определим количество файлов подтверждений (сюда войдут документы БЭД видов "Акт" и "ТОРГ-12")
	Запрос = Новый Запрос("ВЫБРАТЬ РАЗРЕШЕННЫЕ
	|	КОЛИЧЕСТВО(ОписиИсходящихДокументовВНалоговыеОрганыПредставляемыеДокументы.ПодтверждениеИмяФайлаДанных) КАК КоличествоФайловПодтверждений
	|ИЗ
	|	Справочник.ОписиИсходящихДокументовВНалоговыеОрганы.ПредставляемыеДокументы КАК ОписиИсходящихДокументовВНалоговыеОрганыПредставляемыеДокументы
	|ГДЕ
	|	ОписиИсходящихДокументовВНалоговыеОрганыПредставляемыеДокументы.Ссылка = &ОписьСсылка
	|	И ОписиИсходящихДокументовВНалоговыеОрганыПредставляемыеДокументы.ПодтверждениеИмяФайлаДанных > """"");

	Запрос.УстановитьПараметр("ОписьСсылка", ОписьСсылка);
	
	Выборка = Запрос.Выполнить().Выбрать();
	
	КоличествоФайловПодтверждений = 0;
	
	Если Выборка.Следующий() Тогда
		КоличествоФайловПодтверждений = Выборка.КоличествоФайловПодтверждений;
	КонецЕсли;
	
	КоличествоФайловВложений = КоличествоФайловВложений + (КоличествоФайловПодтверждений * 2);

	Возврат КоличествоФайловВложений;
	
КонецФункции

Функция ПолучитьТаблицуДокументовОписиИРазмерыФайлов(ОписьСсылка) Экспорт
	
	Запрос = Новый Запрос("ВЫБРАТЬ РАЗРЕШЕННЫЕ
	                      |	ОписиИсходящихДокументовВНалоговыеОрганыПредставляемыеДокументы.ИмяФайлаДанных
	                      |ПОМЕСТИТЬ ТЗ_ИменФайловДанных
	                      |ИЗ
	                      |	Справочник.ОписиИсходящихДокументовВНалоговыеОрганы.ПредставляемыеДокументы КАК ОписиИсходящихДокументовВНалоговыеОрганыПредставляемыеДокументы
	                      |ГДЕ
	                      |	ОписиИсходящихДокументовВНалоговыеОрганыПредставляемыеДокументы.Ссылка = &ОписьСсылка
	                      |	И ОписиИсходящихДокументовВНалоговыеОрганыПредставляемыеДокументы.ИмяФайлаДанных <> &ПустаяСтрока
	                      |;
	                      |
	                      |////////////////////////////////////////////////////////////////////////////////
	                      |ВЫБРАТЬ
	                      |	ТЗ_ИменФайловДанных.ИмяФайлаДанных,
	                      |	СУММА(4) КАК КолвоФайлов,
	                      |	ЕСТЬNULL(СУММА(ФайлыДокументовРеализацииПолномочийНалоговыхОрганов.Размер), 0) КАК РазмерФайлов
						  |	, ЕстьNULL(Сумма(ВЫБОР
						  |		КОГДА СостояниеОбъектов.Архивный ЕСТЬ NULL ТОГДА 0
						  |		КОГДА СостояниеОбъектов.Архивный = Ложь ТОГДА 0
						  |	ИНАЧЕ
						  |		1
						  |	КОНЕЦ), 0) ВАрхиве
	                      |ПОМЕСТИТЬ ТЗ_ФайлыОписи
	                      |ИЗ
	                      |	ТЗ_ИменФайловДанных КАК ТЗ_ИменФайловДанных
	                      |		ВНУТРЕННЕЕ СОЕДИНЕНИЕ РегистрСведений.ФайлыДокументовРеализацииПолномочийНалоговыхОрганов КАК ФайлыДокументовРеализацииПолномочийНалоговыхОрганов
	                      |		ПО ТЗ_ИменФайловДанных.ИмяФайлаДанных = ФайлыДокументовРеализацииПолномочийНалоговыхОрганов.ИмяФайла
						  |
						  |		ЛЕВОЕ СОЕДИНЕНИЕ РегистрСведений.ПризнакиАрхивированияФайловДОСКонтролирующимиОрганами КАК СостояниеОбъектов
						  |			ПО 		(СостояниеОбъектов.Объект = ФайлыДокументовРеализацииПолномочийНалоговыхОрганов.Документ)
						  |				И	(СостояниеОбъектов.ИмяФайла = ФайлыДокументовРеализацииПолномочийНалоговыхОрганов.ИмяФайла)
						  |				И	(СостояниеОбъектов.Владелец = Значение(Перечисление.ВидыАрхивируемыхМетаданныхДО.ФайлыДокументовРеализацииПолномочийНалоговыхОрганов))
						  |
	                      |ГДЕ
	                      |	ФайлыДокументовРеализацииПолномочийНалоговыхОрганов.Документ = &ОписьСсылка
	                      |
	                      |СГРУППИРОВАТЬ ПО
	                      |	ТЗ_ИменФайловДанных.ИмяФайлаДанных
	                      |;
	                      |
	                      |////////////////////////////////////////////////////////////////////////////////
	                      |ВЫБРАТЬ
	                      |	ОписиИсходящихДокументовВНалоговыеОрганыПредставляемыеДокументы.СсылкаНаОбъект КАК СканСсылка
	                      |ПОМЕСТИТЬ ТЗ_Сканы
	                      |ИЗ
	                      |	Справочник.ОписиИсходящихДокументовВНалоговыеОрганы.ПредставляемыеДокументы КАК ОписиИсходящихДокументовВНалоговыеОрганыПредставляемыеДокументы
	                      |ГДЕ
	                      |	ОписиИсходящихДокументовВНалоговыеОрганыПредставляемыеДокументы.Ссылка = &ОписьСсылка
	                      |	И ОписиИсходящихДокументовВНалоговыеОрганыПредставляемыеДокументы.СсылкаНаОбъект ССЫЛКА Справочник.СканированныеДокументыДляПередачиВЭлектронномВиде
	                      |	И ОписиИсходящихДокументовВНалоговыеОрганыПредставляемыеДокументы.СсылкаНаОбъект <> ЗНАЧЕНИЕ(Справочник.СканированныеДокументыДляПередачиВЭлектронномВиде.ПустаяСсылка)
	                      |;
	                      |
	                      |////////////////////////////////////////////////////////////////////////////////
	                      |ВЫБРАТЬ
	                      |	ТЗ_Сканы.СканСсылка,
	                      |	СУММА(1) КАК КолвоФайлов,
	                      |	ЕСТЬNULL(СУММА(ПрисоединенныеФайлыСканов.Размер), 0) КАК РазмерФайлов
	                      |ПОМЕСТИТЬ ТЗ_ФайлыСканов
	                      |ИЗ
	                      |	ТЗ_Сканы КАК ТЗ_Сканы
	                      |		ВНУТРЕННЕЕ СОЕДИНЕНИЕ Справочник.СканированныеДокументыДляПередачиВЭлектронномВидеПрисоединенныеФайлы КАК ПрисоединенныеФайлыСканов
	                      |		ПО ТЗ_Сканы.СканСсылка = ПрисоединенныеФайлыСканов.ВладелецФайла
	                      |
	                      |СГРУППИРОВАТЬ ПО
	                      |	ТЗ_Сканы.СканСсылка
	                      |;
	                      |
	                      |////////////////////////////////////////////////////////////////////////////////
	                      |ВЫБРАТЬ
	                      |	ОписиИсходящихДокументовВНалоговыеОрганыПредставляемыеДокументы.ИмяФайлаДанных КАК ИдентификаторДокумента,
	                      |	ОписиИсходящихДокументовВНалоговыеОрганыПредставляемыеДокументы.ЯвляетсяИнформацией КАК ЯвляетсяИнформацией,
	                      |	ОписиИсходящихДокументовВНалоговыеОрганыПредставляемыеДокументы.ПорядковыйНомер КАК ПорядковыйНомер,
	                      |	ОписиИсходящихДокументовВНалоговыеОрганыПредставляемыеДокументы.ВидДокумента,
	                      |	ОписиИсходящихДокументовВНалоговыеОрганыПредставляемыеДокументы.СсылкаНаОбъект,
	                      |	ОписиИсходящихДокументовВНалоговыеОрганыПредставляемыеДокументы.Загружен,
	                      |	ОписиИсходящихДокументовВНалоговыеОрганыПредставляемыеДокументы.Загружен_Номер,
	                      |	ОписиИсходящихДокументовВНалоговыеОрганыПредставляемыеДокументы.Загружен_Дата,
	                      |	ОписиИсходящихДокументовВНалоговыеОрганыПредставляемыеДокументы.Загружен_Контрагент,
	                      |	ОписиИсходящихДокументовВНалоговыеОрганыПредставляемыеДокументы.НомерДокументаОснования,
	                      |	ОписиИсходящихДокументовВНалоговыеОрганыПредставляемыеДокументы.ДатаДокументаОснования,
	                      |	ОписиИсходящихДокументовВНалоговыеОрганыПредставляемыеДокументы.КНД,
	                      |	ОписиИсходящихДокументовВНалоговыеОрганыПредставляемыеДокументы.Загружен_Направление,
	                      |	ОписиИсходящихДокументовВНалоговыеОрганыПредставляемыеДокументы.ИмяФайлаДанных,
	                      |	ОписиИсходящихДокументовВНалоговыеОрганыПредставляемыеДокументы.ИмяФайлаПодписи,
	                      |	ОписиИсходящихДокументовВНалоговыеОрганыПредставляемыеДокументы.ПодтверждениеКНД,
	                      |	ОписиИсходящихДокументовВНалоговыеОрганыПредставляемыеДокументы.ПодтверждениеИмяФайлаДанных,
	                      |	ОписиИсходящихДокументовВНалоговыеОрганыПредставляемыеДокументы.ПодтверждениеИмяФайлаПодписи,
	                      |	ЕСТЬNULL(ТЗ_ФайлыОписи.КолвоФайлов, 0) КАК КоличествоФайлов,
	                      |	ЕСТЬNULL(ТЗ_ФайлыОписи.РазмерФайлов, 0) КАК РазмерФайлов,
						  |	ЕСТЬNULL(ТЗ_ФайлыОписи.ВАрхиве, 0) КАК ВАрхиве
	                      |ИЗ
	                      |	Справочник.ОписиИсходящихДокументовВНалоговыеОрганы.ПредставляемыеДокументы КАК ОписиИсходящихДокументовВНалоговыеОрганыПредставляемыеДокументы
	                      |		ВНУТРЕННЕЕ СОЕДИНЕНИЕ ТЗ_ФайлыОписи КАК ТЗ_ФайлыОписи
	                      |		ПО ОписиИсходящихДокументовВНалоговыеОрганыПредставляемыеДокументы.ИмяФайлаДанных = ТЗ_ФайлыОписи.ИмяФайлаДанных
	                      |ГДЕ
	                      |	ОписиИсходящихДокументовВНалоговыеОрганыПредставляемыеДокументы.Ссылка = &ОписьСсылка
	                      |
	                      |ОБЪЕДИНИТЬ ВСЕ
	                      |
	                      |ВЫБРАТЬ
	                      |	ОписиИсходящихДокументовВНалоговыеОрганыПредставляемыеДокументы.СсылкаНаОбъект,
	                      |	ОписиИсходящихДокументовВНалоговыеОрганыПредставляемыеДокументы.ЯвляетсяИнформацией,
	                      |	ОписиИсходящихДокументовВНалоговыеОрганыПредставляемыеДокументы.ПорядковыйНомер,
	                      |	ОписиИсходящихДокументовВНалоговыеОрганыПредставляемыеДокументы.ВидДокумента,
	                      |	ОписиИсходящихДокументовВНалоговыеОрганыПредставляемыеДокументы.СсылкаНаОбъект,
	                      |	ОписиИсходящихДокументовВНалоговыеОрганыПредставляемыеДокументы.Загружен,
	                      |	ОписиИсходящихДокументовВНалоговыеОрганыПредставляемыеДокументы.Загружен_Номер,
	                      |	ОписиИсходящихДокументовВНалоговыеОрганыПредставляемыеДокументы.Загружен_Дата,
	                      |	ОписиИсходящихДокументовВНалоговыеОрганыПредставляемыеДокументы.Загружен_Контрагент,
	                      |	ОписиИсходящихДокументовВНалоговыеОрганыПредставляемыеДокументы.НомерДокументаОснования,
	                      |	ОписиИсходящихДокументовВНалоговыеОрганыПредставляемыеДокументы.ДатаДокументаОснования,
	                      |	ОписиИсходящихДокументовВНалоговыеОрганыПредставляемыеДокументы.КНД,
	                      |	ОписиИсходящихДокументовВНалоговыеОрганыПредставляемыеДокументы.Загружен_Направление,
	                      |	ОписиИсходящихДокументовВНалоговыеОрганыПредставляемыеДокументы.ИмяФайлаДанных,
	                      |	ОписиИсходящихДокументовВНалоговыеОрганыПредставляемыеДокументы.ИмяФайлаПодписи,
	                      |	ОписиИсходящихДокументовВНалоговыеОрганыПредставляемыеДокументы.ПодтверждениеКНД,
	                      |	ОписиИсходящихДокументовВНалоговыеОрганыПредставляемыеДокументы.ПодтверждениеИмяФайлаДанных,
	                      |	ОписиИсходящихДокументовВНалоговыеОрганыПредставляемыеДокументы.ПодтверждениеИмяФайлаПодписи,
	                      |	ЕСТЬNULL(ТЗ_ФайлыСканов.КолвоФайлов, 0),
	                      |	ЕСТЬNULL(ТЗ_ФайлыСканов.РазмерФайлов, 0),
						  |	0 КАК ВАрхиве
	                      |ИЗ
	                      |	Справочник.ОписиИсходящихДокументовВНалоговыеОрганы.ПредставляемыеДокументы КАК ОписиИсходящихДокументовВНалоговыеОрганыПредставляемыеДокументы
	                      |		ВНУТРЕННЕЕ СОЕДИНЕНИЕ ТЗ_ФайлыСканов КАК ТЗ_ФайлыСканов
	                      |		ПО ОписиИсходящихДокументовВНалоговыеОрганыПредставляемыеДокументы.СсылкаНаОбъект = ТЗ_ФайлыСканов.СканСсылка
	                      |ГДЕ
	                      |	ОписиИсходящихДокументовВНалоговыеОрганыПредставляемыеДокументы.Ссылка = &ОписьСсылка
	                      |
	                      |УПОРЯДОЧИТЬ ПО
	                      |	РазмерФайлов"); //если требуется разбивать по другому принципу, можно добавить упорядочивание по ЯвляетсяИнформацией и ПорядковыйНомер
	
	Запрос.УстановитьПараметр("ОписьСсылка", ОписьСсылка);
	Запрос.УстановитьПараметр("ПустаяСтрока", "");
	
	Возврат Запрос.Выполнить().Выгрузить();
	
КонецФункции

Функция КритичныеДокументыОписиИсходящихДокументовПриОтправке(ОписьСсылка) Экспорт
	
	// Сформируем массив скандокументов описи
	Запрос = Новый Запрос("ВЫБРАТЬ РАЗРЕШЕННЫЕ
	|	ОписиИсходящихДокументовВНалоговыеОрганыПредставляемыеДокументы.СсылкаНаОбъект
	|ИЗ
	|	Справочник.ОписиИсходящихДокументовВНалоговыеОрганы.ПредставляемыеДокументы КАК ОписиИсходящихДокументовВНалоговыеОрганыПредставляемыеДокументы
	|ГДЕ
	|	ОписиИсходящихДокументовВНалоговыеОрганыПредставляемыеДокументы.Ссылка = &ОписьСсылка
	|	И ОписиИсходящихДокументовВНалоговыеОрганыПредставляемыеДокументы.СсылкаНаОбъект ССЫЛКА Справочник.СканированныеДокументыДляПередачиВЭлектронномВиде");
	
	Запрос.УстановитьПараметр("ОписьСсылка", ОписьСсылка);
	
	МасивСканДокументов = Запрос.Выполнить().Выгрузить().ВыгрузитьКолонку("СсылкаНаОбъект");
	
	// Сформируем список владельцев файлов
	СписокВладельцевФайлов = Новый СписокЗначений;
	СписокВладельцевФайлов.ЗагрузитьЗначения(МасивСканДокументов);
	// Добавим в список саму опись
	СписокВладельцевФайлов.Добавить(ОписьСсылка);
	
	//Сформируем список сжимаемых типов содержимого файлов
	СписокСжимаемыхТипов = Новый СписокЗначений;
	СписокСжимаемыхТипов.Добавить(Перечисления.ТипыСодержимогоФайлов.xml);
	СписокСжимаемыхТипов.Добавить(Перечисления.ТипыСодержимогоФайлов.Текст1251);
	СписокСжимаемыхТипов.Добавить(Перечисления.ТипыСодержимогоФайлов.Текст866);
	
	// Определим ориентировочный размер описи
	Запрос = Новый Запрос("ВЫБРАТЬ РАЗРЕШЕННЫЕ
	                      |	ФайлыДокументовРеализацииПолномочийНалоговыхОрганов.Документ,
	                      |	ФайлыДокументовРеализацииПолномочийНалоговыхОрганов.ИмяФайла,
	                      |	ФайлыДокументовРеализацииПолномочийНалоговыхОрганов.ТипСодержимого,
	                      |	ФайлыДокументовРеализацииПолномочийНалоговыхОрганов.Размер,
	                      |	ВЫБОР
	                      |		КОГДА ФайлыДокументовРеализацииПолномочийНалоговыхОрганов.Размер > 1000 * 1024 * 1024
	                      |			ТОГДА 2
	                      |		ИНАЧЕ 1
	                      |	КОНЕЦ КАК СтатусКритичности
	                      |ИЗ
	                      |	РегистрСведений.ФайлыДокументовРеализацииПолномочийНалоговыхОрганов КАК ФайлыДокументовРеализацииПолномочийНалоговыхОрганов
	                      |ГДЕ
	                      |	ФайлыДокументовРеализацииПолномочийНалоговыхОрганов.Документ В(&СписокВладельцевФайлов)
	                      |	И ВЫБОР
	                      |			КОГДА ФайлыДокументовРеализацииПолномочийНалоговыхОрганов.ТипСодержимого В (&СписокСжимаемыхТипов)
	                      |				ТОГДА ФайлыДокументовРеализацииПолномочийНалоговыхОрганов.Размер * &КоэффициентСжатия
	                      |			ИНАЧЕ ФайлыДокументовРеализацииПолномочийНалоговыхОрганов.Размер
	                      |		КОНЕЦ > 60 * 1024 * 1024");
	
	Запрос.УстановитьПараметр("СписокВладельцевФайлов", СписокВладельцевФайлов);
	Запрос.УстановитьПараметр("СписокСжимаемыхТипов", СписокСжимаемыхТипов);
	Запрос.УстановитьПараметр("КоэффициентСжатия", 0.15);
	
	Возврат Запрос.Выполнить().Выгрузить();
	
КонецФункции

Функция ПолучитьОбщееСоответствиеФайловЭДиНДС(СоответствиеЭД, СоответствиеНДС)
	
	ОбщееСоответствие = Новый Соответствие;
	
	Если ТипЗнч(СоответствиеНДС) = Тип("Соответствие") Тогда
		Для каждого Соотв Из СоответствиеНДС Цикл
			СтруктураЗначение = Новый Структура("ИмяФайлаДанных, ИмяФайлаПодписи, АдресФайлаДанных, АдресФайлаПодписи, ПодтверждениеИмяФайлаДанных, ПодтверждениеИмяФайлаПодписи, ПодтверждениеАдресФайлаДанных, ПодтверждениеАдресФайлаПодписи", "", "", "", "", "", "", "", "");
			
			МассивФайлов = Соотв.Значение;
			Для каждого ДанныеФайла Из МассивФайлов Цикл
				Если ДанныеФайла.ТипФайла = "ФайлВыгрузки" Тогда
					СтруктураЗначение.ИмяФайлаДанных = ДанныеФайла.ИмяФайла;
					СтруктураЗначение.АдресФайлаДанных = ДанныеФайла.АдресВременногоХранилища;
				КонецЕсли;
			КонецЦикла;
			
			ОбщееСоответствие.Вставить(Соотв.Ключ, СтруктураЗначение);
			
		КонецЦикла;
	КонецЕсли;

	Если ТипЗнч(СоответствиеЭД) = Тип("Соответствие") Тогда
		Для каждого Соотв Из СоответствиеЭД Цикл
			СтруктураЗначение = Новый Структура("ИмяФайлаДанных, ИмяФайлаПодписи, АдресФайлаДанных, АдресФайлаПодписи, ПодтверждениеИмяФайлаДанных, ПодтверждениеИмяФайлаПодписи, ПодтверждениеАдресФайлаДанных, ПодтверждениеАдресФайлаПодписи", "", "", "", "", "", "", "", "");
			
			МассивФайлов = Соотв.Значение;
			
			Для каждого ДанныеФайла Из МассивФайлов Цикл
				Если ДанныеФайла.ТипФайла = "ФайлВыгрузки" Тогда
					СтруктураЗначение.ИмяФайлаДанных   = ДанныеФайла.ИмяФайла;
					СтруктураЗначение.АдресФайлаДанных = ДанныеФайла.АдресВременногоХранилища;
				ИначеЕсли ДанныеФайла.ТипФайла = "ЭЦП" Тогда
					СтруктураЗначение.АдресФайлаПодписи = ДанныеФайла.АдресВременногоХранилища;
				ИначеЕсли ДанныеФайла.ТипФайла = "ФайлПодтверждения" Тогда
					СтруктураЗначение.ПодтверждениеИмяФайлаДанных   = ДанныеФайла.ИмяФайла;
					СтруктураЗначение.ПодтверждениеАдресФайлаДанных = ДанныеФайла.АдресВременногоХранилища;
				ИначеЕсли ДанныеФайла.ТипФайла = "ЭЦППодтверждения" Тогда
					СтруктураЗначение.ПодтверждениеАдресФайлаПодписи = ДанныеФайла.АдресВременногоХранилища;
				КонецЕсли;
			КонецЦикла;
			
			//относительно имени файла данных генерируем имя файла подписи
			СтруктураЗначение.ИмяФайлаПодписи = ИмяФайлаЭПФайлаОписиИсходящихДокументов(СтруктураЗначение.ИмяФайлаДанных);
			
			Если ЗначениеЗаполнено(СтруктураЗначение.ПодтверждениеИмяФайлаДанных) Тогда
				СтруктураЗначение.ПодтверждениеИмяФайлаПодписи = ИмяФайлаЭПФайлаОписиИсходящихДокументов(СтруктураЗначение.ПодтверждениеИмяФайлаДанных);
			КонецЕсли;
			
			ОбщееСоответствие.Вставить(Соотв.Ключ, СтруктураЗначение);
			
		КонецЦикла
	КонецЕсли;

	Возврат ОбщееСоответствие;
	
КонецФункции

Функция ПолучитьСтруктуруРеквизитовОписиИсходящихДокументов(ОписьСсылка)
	
	СтруктураРеквизитовОписи = Новый Структура;
	СтруктураРеквизитовОписи.Вставить("Организация", ОписьСсылка);
	СтруктураРеквизитовОписи.Вставить("ОписьВходящихДокументов", );
	СтруктураРеквизитовОписи.Вставить("Основание", );
	СтруктураРеквизитовОписи.Вставить("НалоговыйОрган", );

	Запрос = Новый Запрос;
	Запрос.Текст = 
		"ВЫБРАТЬ РАЗРЕШЕННЫЕ
		|	ОписиИсходящихДокументовВНалоговыеОрганы.Организация,
		|	ОписиИсходящихДокументовВНалоговыеОрганы.ОписьВходящихДокументов,
		|	ОписиИсходящихДокументовВНалоговыеОрганы.Основание,
		|	ОписиИсходящихДокументовВНалоговыеОрганы.НалоговыйОрган
		|ИЗ
		|	Справочник.ОписиИсходящихДокументовВНалоговыеОрганы КАК ОписиИсходящихДокументовВНалоговыеОрганы
		|ГДЕ
		|	ОписиИсходящихДокументовВНалоговыеОрганы.Ссылка = &ОписьСсылка";

	Запрос.УстановитьПараметр("ОписьСсылка", ОписьСсылка);

	Выборка = Запрос.Выполнить().Выбрать();

	Выборка.Следующий();

	ЗаполнитьЗначенияСвойств(СтруктураРеквизитовОписи, Выборка);

	Возврат СтруктураРеквизитовОписи;

КонецФункции 

Функция РазбитьОписьИсходящихДокументовНаНесколько(ОписьСсылка, ТекстПредупреждения) Экспорт
	
	// Проверка ситуации:
	// Размер вложения превышает 1024 Мб.
	// Разбиением на несколько описей ситуацию не исправить.
	ТЗКритичныеФайлы = КритичныеДокументыОписиИсходящихДокументовПриОтправке(ОписьСсылка);
	
	ЕстьНедопустимыеФайлы = Ложь;
	Для каждого КритичныйФайл Из ТЗКритичныеФайлы Цикл
		Если КритичныйФайл.СтатусКритичности = 2 Тогда
			ЕстьНедопустимыеФайлы = Истина;
			Прервать;
		КонецЕсли;
	КонецЦикла;

	Если ЕстьНедопустимыеФайлы Тогда
		ТекстПредупреждения = "Размеры некоторых файлов превышают допустимые! Требуется устранить нарушение до разбиения описи на несколько!";
		Возврат Неопределено;
	КонецЕсли;
	
	//ситации, при которых нельзя разбить проверили, начинаем разбивать
	ТЗДокументыФайлыРазмер = ПолучитьТаблицуДокументовОписиИРазмерыФайлов(ОписьСсылка);
	
	ДеревоСоздаваемыхОписей = Новый ДеревоЗначений;
	ДеревоСоздаваемыхОписей.Колонки.Добавить("ЯвляетсяИнформацией");
	ДеревоСоздаваемыхОписей.Колонки.Добавить("ПорядковыйНомер");
	ДеревоСоздаваемыхОписей.Колонки.Добавить("ВидДокумента");
	ДеревоСоздаваемыхОписей.Колонки.Добавить("СсылкаНаОбъект");
	ДеревоСоздаваемыхОписей.Колонки.Добавить("Загружен");
	ДеревоСоздаваемыхОписей.Колонки.Добавить("Загружен_Номер");
	ДеревоСоздаваемыхОписей.Колонки.Добавить("Загружен_Дата");
	ДеревоСоздаваемыхОписей.Колонки.Добавить("Загружен_Контрагент");
	ДеревоСоздаваемыхОписей.Колонки.Добавить("НомерДокументаОснования");
	ДеревоСоздаваемыхОписей.Колонки.Добавить("ДатаДокументаОснования");
	ДеревоСоздаваемыхОписей.Колонки.Добавить("Загружен_Направление");
	ДеревоСоздаваемыхОписей.Колонки.Добавить("ИмяФайлаДанных");
	ДеревоСоздаваемыхОписей.Колонки.Добавить("ИмяФайлаПодписи");
	ДеревоСоздаваемыхОписей.Колонки.Добавить("КНД");
	ДеревоСоздаваемыхОписей.Колонки.Добавить("ПодтверждениеКНД");
	ДеревоСоздаваемыхОписей.Колонки.Добавить("ПодтверждениеИмяФайлаДанных");
	ДеревоСоздаваемыхОписей.Колонки.Добавить("ПодтверждениеИмяФайлаПодписи");
	
	СуммаКолвоФайлов 	= 0;
	СуммаРазмерФайлов 	= 0;
	МаксКолвоФайлов 	= ЭлектронныйДокументооборотСКонтролирующимиОрганамиКлиентСервер.МаксимальнойКоличествоФайловОписи();
	МаксРазмерФайлов 	= 72*1024*1024;
	
	НоваяСтрокаОпись = ДеревоСоздаваемыхОписей.Строки.Добавить();
	
	Для каждого СтрокаДокумент Из ТЗДокументыФайлыРазмер Цикл
		
		СуммаКолвоФайлов = СуммаКолвоФайлов + СтрокаДокумент.КоличествоФайлов;
		СуммаРазмерФайлов = СуммаРазмерФайлов + СтрокаДокумент.РазмерФайлов;

		Если (СуммаКолвоФайлов > МаксКолвоФайлов ИЛИ СуммаРазмерФайлов > МаксРазмерФайлов) И НоваяСтрокаОпись.Строки.Количество() > 0 Тогда
			//начинаем новую опись
			ДобавленаНоваяОпись = Истина;
			НоваяСтрокаОпись = ДеревоСоздаваемыхОписей.Строки.Добавить();
			
			СуммаКолвоФайлов = СтрокаДокумент.КоличествоФайлов;
			СуммаРазмерФайлов = СтрокаДокумент.РазмерФайлов;

		КонецЕсли;
		
		НоваяСтрокаДокумент = НоваяСтрокаОпись.Строки.Добавить();
		ЗаполнитьЗначенияСвойств(НоваяСтрокаДокумент, СтрокаДокумент); 
	
	КонецЦикла;
	
	СтрокиОписей = ДеревоСоздаваемыхОписей.Строки;
	
	Если СтрокиОписей.Количество() = 1 Тогда
		//все файлы уместились в текущей описи, разбития не происходило
		ТекстПредупреждения = "Разбития описи не требуется.";
		Возврат Неопределено;
	КонецЕсли;
	
	СтруктураРеквизитовОписи = ПолучитьСтруктуруРеквизитовОписиИсходящихДокументов(ОписьСсылка);
	
	ТаблицаФайловИзменяющихВладельца = Новый ТаблицаЗначений;
	ТаблицаФайловИзменяющихВладельца.Колонки.Добавить("ИмяФайла");
	ТаблицаФайловИзменяющихВладельца.Колонки.Добавить("НовыйВладелец");
	
	МассивСозданныхОписей = Новый Массив;
	// спроектировано наполнение описей, начинаем работать с метаданными
	НачатьТранзакцию();
	
	Попытка
		
		НачальнаяОписьЗаполнена = Ложь;
		
		РедактируемаяОписьСсылка = ОписьСсылка;
		РедактируемаяОписьОбъект = ОписьСсылка.ПолучитьОбъект();
		
		РедактируемаяОписьОбъект.ПредставляемыеДокументы.Очистить();
		
		Для каждого СтрокаОпись Из СтрокиОписей Цикл
			
			Если НачальнаяОписьЗаполнена Тогда
				//создаем новую опись
				РедактируемаяОписьОбъект = Справочники.ОписиИсходящихДокументовВНалоговыеОрганы.СоздатьЭлемент();
				ЗаполнитьЗначенияСвойств(РедактируемаяОписьОбъект, СтруктураРеквизитовОписи);
				РедактируемаяОписьОбъект.ДатаСоздания = ТекущаяДатаСеанса();
				РедактируемаяОписьОбъект.Записать();
				РедактируемаяОписьСсылка = РедактируемаяОписьОбъект.Ссылка;
				
				МассивСозданныхОписей.Добавить(РедактируемаяОписьСсылка);
			КонецЕсли;
			
			//заполняем табличную часть
			Для каждого СтрокаДокумент Из СтрокаОпись.Строки Цикл
				
				НоваяСтрокаДокумент = РедактируемаяОписьОбъект.ПредставляемыеДокументы.Добавить();
				ЗаполнитьЗначенияСвойств(НоваяСтрокаДокумент, СтрокаДокумент);
				
				Если НачальнаяОписьЗаполнена И ЗначениеЗаполнено(СтрокаДокумент.ИмяФайлаДанных) Тогда
					//надо изменить владельца файла данных
					НоваяСтрокаФайл = ТаблицаФайловИзменяющихВладельца.Добавить();
					НоваяСтрокаФайл.ИмяФайла = СтрокаДокумент.ИмяФайлаДанных;
					НоваяСтрокаФайл.НовыйВладелец = РедактируемаяОписьСсылка;
					
					Если ЗначениеЗаполнено(СтрокаДокумент.ИмяФайлаПодписи) Тогда
						//надо изменить владельца файла подписи
						НоваяСтрокаФайл = ТаблицаФайловИзменяющихВладельца.Добавить();
						НоваяСтрокаФайл.ИмяФайла = СтрокаДокумент.ИмяФайлаПодписи;
						НоваяСтрокаФайл.НовыйВладелец = РедактируемаяОписьСсылка;
					КонецЕсли;
					
				КонецЕсли;
				
			КонецЦикла;
			
			РедактируемаяОписьОбъект.Записать();
			НачальнаяОписьЗаполнена = Истина;
			
		КонецЦикла;
		
		//изменяем владельца у "переброшенных" файлов
		Если НЕ ИзменитьВладельцаФайловДокументовРеализацииПолномочийНалоговыхОрганов(ОписьСсылка, ТаблицаФайловИзменяющихВладельца) Тогда
			ОтменитьТранзакцию();
			Возврат Неопределено;
		КонецЕсли;
		
	Исключение
		
		ОтменитьТранзакцию();
		Возврат Неопределено;

	КонецПопытки; 
	
	ЗафиксироватьТранзакцию();
	
	Возврат МассивСозданныхОписей;
	
КонецФункции

Функция ОбновитьФайлыДокументовИБОписиИсходящихДокументов(ОписьСсылка) Экспорт
	
	Запрос = Новый Запрос("ВЫБРАТЬ РАЗРЕШЕННЫЕ
	                      |	ОписиИсходящихДокументовВНалоговыеОрганыПредставляемыеДокументы.СсылкаНаОбъект,
	                      |	ВЫБОР
	                      |		КОГДА ОписиИсходящихДокументовВНалоговыеОрганыПредставляемыеДокументы.ВидДокумента В (&СписокВидовДокументовЭД)
	                      |			ТОГДА ""ЭД""
	                      |		ИНАЧЕ ""НДС""
	                      |	КОНЕЦ КАК КатегорияВидаДокумента,
	                      |	ОписиИсходящихДокументовВНалоговыеОрганыПредставляемыеДокументы.ИмяФайлаДанных,
	                      |	ОписиИсходящихДокументовВНалоговыеОрганыПредставляемыеДокументы.ИмяФайлаПодписи,
						  |	ОписиИсходящихДокументовВНалоговыеОрганыПредставляемыеДокументы.ПодтверждениеИмяФайлаДанных,
	                      |	ОписиИсходящихДокументовВНалоговыеОрганыПредставляемыеДокументы.ПодтверждениеИмяФайлаПодписи,
	                      |	ОписиИсходящихДокументовВНалоговыеОрганыПредставляемыеДокументы.ВерсияФайлаДанных
	                      |ИЗ
	                      |	Справочник.ОписиИсходящихДокументовВНалоговыеОрганы.ПредставляемыеДокументы КАК ОписиИсходящихДокументовВНалоговыеОрганыПредставляемыеДокументы
	                      |ГДЕ
	                      |	ОписиИсходящихДокументовВНалоговыеОрганыПредставляемыеДокументы.Ссылка = &ОписьСсылка
	                      |	И НЕ ОписиИсходящихДокументовВНалоговыеОрганыПредставляемыеДокументы.СсылкаНаОбъект ССЫЛКА Справочник.СканированныеДокументыДляПередачиВЭлектронномВиде
	                      |	И ОписиИсходящихДокументовВНалоговыеОрганыПредставляемыеДокументы.Загружен = ЛОЖЬ");
	
	Запрос.УстановитьПараметр("ОписьСсылка", ОписьСсылка);
	Запрос.УстановитьПараметр("СписокВидовДокументовЭД", ДокументЭДОСЭД());
	
	Выборка = Запрос.Выполнить().Выбрать();
	
	ТаблицаДобавляемыхФайлов = Новый ТаблицаЗначений;
	ТаблицаДобавляемыхФайлов.Колонки.Добавить("ИмяФайла");
	ТаблицаДобавляемыхФайлов.Колонки.Добавить("АдресФайла");
	
	МассивУдаляемыхФайлов = Новый Массив;
	
	МассивЭД = Новый Массив;
	МассивНДС = Новый Массив;
	
	СоответствиеВерсияДанныхПоСсылке = Новый Соответствие;
		
	//проходим выборку для заполнения МассивУдаляемыхФайлов, МассивЭД, МассивНДС
	Пока Выборка.Следующий() Цикл
		
		ВерсияДанныхПоСсылке = Выборка.СсылкаНаОбъект.ВерсияДанных;
						  
		Если Выборка.ВерсияФайлаДанных = ВерсияДанныхПоСсылке Тогда
			Продолжить;
		Иначе
			СоответствиеВерсияДанныхПоСсылке.Вставить(Выборка.СсылкаНаОбъект, ВерсияДанныхПоСсылке);
		КонецЕсли;
		
		Если ЗначениеЗаполнено(Выборка.ИмяФайлаДанных) Тогда
			МассивУдаляемыхФайлов.Добавить(Выборка.ИмяФайлаДанных);
		КонецЕсли;	
		Если ЗначениеЗаполнено(Выборка.ИмяФайлаПодписи) Тогда
			МассивУдаляемыхФайлов.Добавить(Выборка.ИмяФайлаПодписи);
		КонецЕсли;	
		Если ЗначениеЗаполнено(Выборка.ПодтверждениеИмяФайлаДанных) Тогда
			МассивУдаляемыхФайлов.Добавить(Выборка.ПодтверждениеИмяФайлаДанных);
		КонецЕсли;	
		Если ЗначениеЗаполнено(Выборка.ПодтверждениеИмяФайлаПодписи) Тогда
			МассивУдаляемыхФайлов.Добавить(Выборка.ПодтверждениеИмяФайлаПодписи);
		КонецЕсли;	
		
		Если Выборка.КатегорияВидаДокумента = "ЭД" Тогда
			МассивЭД.Добавить(Выборка.СсылкаНаОбъект);
		Иначе
			МассивНДС.Добавить(Выборка.СсылкаНаОбъект);
		КонецЕсли;
	КонецЦикла;
	
	Если СоответствиеВерсияДанныхПоСсылке.Количество() = 0 Тогда
		//обновлять файлы не требуется, в описи нет документов ЭД и НДС либо версия данных у них не менялась
		Возврат Истина;
	КонецЕсли;
	
	СоответствиеНДС = ЭлектронныйДокументооборотСКонтролирующимиОрганамиВызовСервераПереопределяемый.ПолучитьФайлыВыгрузкиНДС(МассивНДС, Новый УникальныйИдентификатор);
	СоответствиеЭД 	= ЭлектронныйДокументооборотСКонтролирующимиОрганамиВызовСервераПереопределяемый.ПолучитьФайлыВыгрузкиЭД(МассивЭД, Новый УникальныйИдентификатор);
	
	ОбщееСоответствиеФайлов = ПолучитьОбщееСоответствиеФайловЭДиНДС(СоответствиеЭД, СоответствиеНДС);
	
	// Заполняем ТаблицаДобавляемыхФайлов
	Для каждого Соотв Из ОбщееСоответствиеФайлов Цикл
		
		СтруктураРеквизитов = Соотв.Значение;
		Если ЗначениеЗаполнено(СтруктураРеквизитов.ИмяФайлаДанных) Тогда
			НоваяСтрока = ТаблицаДобавляемыхФайлов.Добавить();
			НоваяСтрока.ИмяФайла = СтруктураРеквизитов.ИмяФайлаДанных;
			НоваяСтрока.АдресФайла = СтруктураРеквизитов.АдресФайлаДанных;
		КонецЕсли;
		
		Если ЗначениеЗаполнено(СтруктураРеквизитов.ИмяФайлаПодписи) Тогда
			НоваяСтрока = ТаблицаДобавляемыхФайлов.Добавить();
			НоваяСтрока.ИмяФайла = СтруктураРеквизитов.ИмяФайлаПодписи;
			НоваяСтрока.АдресФайла = СтруктураРеквизитов.АдресФайлаПодписи;
		КонецЕсли;
		
		Если ЗначениеЗаполнено(СтруктураРеквизитов.ПодтверждениеИмяФайлаДанных) Тогда
			НоваяСтрока = ТаблицаДобавляемыхФайлов.Добавить();
			НоваяСтрока.ИмяФайла = СтруктураРеквизитов.ПодтверждениеИмяФайлаДанных;
			НоваяСтрока.АдресФайла = СтруктураРеквизитов.ПодтверждениеАдресФайлаДанных;
		КонецЕсли;
		
		Если ЗначениеЗаполнено(СтруктураРеквизитов.ПодтверждениеИмяФайлаПодписи) Тогда
			НоваяСтрока = ТаблицаДобавляемыхФайлов.Добавить();
			НоваяСтрока.ИмяФайла = СтруктураРеквизитов.ПодтверждениеИмяФайлаПодписи;
			НоваяСтрока.АдресФайла = СтруктураРеквизитов.ПодтверждениеАдресФайлаПодписи;
		КонецЕсли;
			
	КонецЦикла;
	
	НачатьТранзакцию();
	
	Попытка
		//удаляем старые файлы
		УдалитьФайлыДокументовРеализацииПолномочийНалоговыхОргановПоВладельцу(ОписьСсылка, МассивУдаляемыхФайлов);
		
		//записываем новые файлы
		Если НЕ ПоместитьФайлыДокументовРеализацииПолномочийНалоговыхОрганов(ОписьСсылка, ТаблицаДобавляемыхФайлов) Тогда
			ОтменитьТранзакцию();
			Возврат Ложь;
		КонецЕсли;
		
		//обновляем сведения в табличной части
		ТипСкан = Тип("СправочникСсылка.СканированныеДокументыДляПередачиВЭлектронномВиде");
		
		ОписьОбъект = ОписьСсылка.ПолучитьОбъект();
		Для каждого СтрокаТЧ Из ОписьОбъект.ПредставляемыеДокументы Цикл
			ДокументСсылка = СтрокаТЧ.СсылкаНаОбъект;
			Если НЕ СтрокаТЧ.Загружен И ТипЗнч(ДокументСсылка) <> ТипСкан Тогда
				
				РекизитыСтроки = ОбщееСоответствиеФайлов[ДокументСсылка];
				Если РекизитыСтроки <> Неопределено Тогда
					СтрокаТЧ.ИмяФайлаДанных = РекизитыСтроки.ИмяФайлаДанных;
					СтрокаТЧ.ИмяФайлаПодписи = РекизитыСтроки.ИмяФайлаПодписи;
					
					СтрокаТЧ.ПодтверждениеИмяФайлаДанных = РекизитыСтроки.ПодтверждениеИмяФайлаДанных;
					СтрокаТЧ.ПодтверждениеИмяФайлаПодписи = РекизитыСтроки.ПодтверждениеИмяФайлаПодписи;
				КонецЕсли;
				
				СтрокаТЧ.ВерсияФайлаДанных = СоответствиеВерсияДанныхПоСсылке[ДокументСсылка];
				
			КонецЕсли;
		КонецЦикла;
		
		ОписьОбъект.Записать();
	
	Исключение
		
	    ОтменитьТранзакцию();
		Возврат Ложь;
		
	КонецПопытки;
	
	ЗафиксироватьТранзакцию();
	
	Возврат Истина;
	
КонецФункции

Функция ИННКПППолучателяТребования(ИмяФайла, Данные = Неопределено) Экспорт
	
	ПрефиксИмениФайла = Лев(ИмяФайла, 11);
	Если ВРег(ПрефиксИмениФайла) <> "ON_INFSOOB_"
		И ВРег(ПрефиксИмениФайла) <> "ON_DOCNPNO_" Тогда
		Возврат Неопределено;
	КонецЕсли;
	
	Если ЗначениеЗаполнено(Данные) Тогда
		ИмяВременногоФайлаОтчета = ПолучитьИмяВременногоФайла();
		Данные.Записать(ИмяВременногоФайлаОтчета);
		ТекстXML = ПрочитатьТекстИзФайла(ИмяВременногоФайлаОтчета, , Истина);
		ОперацииСФайламиЭДКО.УдалитьВременныйФайл(ИмяВременногоФайлаОтчета);
		
		ДеревоXML = ЗагрузитьСтрокуXMLВДеревоЗначений(ТекстXML);
		Если ЗначениеЗаполнено(ДеревоXML) Тогда
			
			УзлыСвНП = ДеревоXML.Строки.НайтиСтроки(Новый Структура("Имя", "СвНП"), Истина);
								
			Если УзлыСвНП.Количество() > 0 Тогда
				УзелСвНП = УзлыСвНП[0];
				
				УзлыНПЮЛ = УзелСвНП.Строки.НайтиСтроки(Новый Структура("Имя, Тип", "НПЮЛ", "Э"));
				Если УзлыНПЮЛ.Количество() > 0 Тогда
					УзелНПЮЛ = УзлыНПЮЛ[0];
					
					УзлыИННЮЛ = УзелНПЮЛ.Строки.НайтиСтроки(Новый Структура("Имя, Тип", "ИННЮЛ", "А"));
					УзлыКПП = УзелНПЮЛ.Строки.НайтиСтроки(Новый Структура("Имя, Тип", "КПП", "А"));
					Если УзлыИННЮЛ.Количество() > 0 И УзлыКПП.Количество() > 0 Тогда
						УзелИННЮЛ = УзлыИННЮЛ[0];
						УзелКПП = УзлыКПП[0];
						
						Если ЗначениеЗаполнено(УзелИННЮЛ.Значение) И ЗначениеЗаполнено(УзелКПП.Значение) Тогда
							Возврат СокрЛП(УзелИННЮЛ.Значение) + СокрЛП(УзелКПП.Значение);
						КонецЕсли;
					КонецЕсли;
					
				Иначе
					УзлыНПФЛ = УзелСвНП.Строки.НайтиСтроки(Новый Структура("Имя, Тип", "НПФЛ", "Э"));
					Если УзлыНПФЛ.Количество() > 0 Тогда
						УзелНПФЛ = УзлыНПФЛ[0];
						
						УзлыИННФЛ = УзелНПФЛ.Строки.НайтиСтроки(Новый Структура("Имя, Тип", "ИННФЛ", "А"));
						Если УзлыИННФЛ.Количество() > 0 Тогда
							УзелИННФЛ = УзлыИННФЛ[0];
							
							Если ЗначениеЗаполнено(УзелИННФЛ.Значение) Тогда
								Возврат СокрЛП(УзелИННФЛ.Значение);
							КонецЕсли;
						КонецЕсли;
					КонецЕсли;
				КонецЕсли;
			КонецЕсли;
		КонецЕсли;
	КонецЕсли;
	
	КомпонентыИмени = СтроковыеФункцииКлиентСервер.РазложитьСтрокуВМассивПодстрок(ИмяФайла, "_");
	Если КомпонентыИмени.Количество() >= 4 Тогда
		Возврат КомпонентыИмени[3];
	КонецЕсли;
	
	Возврат Неопределено;
	
КонецФункции

// Получение требований и представление документов в налоговые органы
///////////////////////////////////////////////////////////////////////////////

// есть в Контейнере
Функция ОтветНаЗапросИОСЯвляетсяОтрицательным(Объект)
	
	// получаем протокол из базы
	ОбъектСсылка = Объект.Ссылка;
	СтрПротоколы = ПолучитьВложенияТранспортногоСообщения(ОбъектСсылка, Истина, Перечисления.ТипыСодержимогоТранспортногоКонтейнера.ОтветНаЗапросПФР);
	Если СтрПротоколы.Количество() = 0 Тогда
		Возврат Ложь;
	КонецЕсли;
	
	// выгружаем данные протокола во временный файл
	СтрПротокол = СтрПротоколы[0];
	ИмяВременногоФайла = ПолучитьИмяВременногоФайла();
	Попытка
		СтрПротокол.Данные.Получить().Записать(ИмяВременногоФайла);
	Исключение
		ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(НСтр("ru = 'Не удалось выгрузить ответ на запрос во временный файл для анализа'") + ":
				|" + ИнформацияОбОшибке().Описание);
		Возврат Неопределено;
	КонецПопытки;
	
	// загружаем XML из файла в память
	ДеревоXML = ЗагрузитьXMLВДеревоЗначений(ИмяВременногоФайла);
	ОперацииСФайламиЭДКО.УдалитьВременныйФайл(ИмяВременногоФайла);
	Если ДеревоXML = Неопределено Тогда
		Возврат Ложь;
	КонецЕсли;
	
	// ищем атрибут, содержащий статус проверки протокола
	РезультатыПоиска = ДеревоXML.Строки.НайтиСтроки(Новый Структура("Имя, Тип", "запросОбработанУспешно", "Э"), Истина);
	Если РезультатыПоиска.Количество() = 0 Тогда
		Возврат Ложь;
	КонецЕсли;
	
	// преобразовываем значение атрибута к Булево и возвращаем
	ЗначениеАтрибутаСтр = РезультатыПоиска[0].Значение;
	ЗначениеАтрибута = БулевоПоСтроке(ЗначениеАтрибутаСтр);
	Если ЗначениеАтрибута <> Неопределено Тогда
		Возврат НЕ ЗначениеАтрибута;
	КонецЕсли;
	
	Возврат Ложь;
	
КонецФункции

Процедура ПередЗаписьюЗапросаНаИнформационноеОбслуживаниеСтрахователя(Объект, Отказ, РежимЗаписи, РежимПроведения)
	
	Если Объект.ВидУслуги = Перечисления.ВидыУслугПриИОС.СправкаОСостоянииРасчетов Тогда
		Объект.ЗастрахованныеЛица.Очистить();
	КонецЕсли;
	
КонецПроцедуры

Функция ПолучитьОтветыНаЗапросИОС(парамЗапрос, СДанными = Ложь) Экспорт
	
	Запрос = Новый Запрос;
	Запрос.Текст = Запрос.Текст + "ВЫБРАТЬ РАЗРЕШЕННЫЕ
						  |	ОтветыНаЗапросыИОС.ИмяФайла,
						  |	ОтветыНаЗапросыИОС.Размер,
						  |	ОтветыНаЗапросыИОС.ОтветОтрицательный,
						  |	ОтветыНаЗапросыИОС.ТипСодержимогоФайла
						  |	, ВЫБОР
						  |		КОГДА СостояниеОбъектов.Архивный ЕСТЬ NULL ТОГДА Ложь
						  |		КОГДА СостояниеОбъектов.Архивный = Ложь ТОГДА Ложь
						  |	ИНАЧЕ
						  |		Истина
						  |	КОНЕЦ ВАрхиве";
	Если СДанными Тогда
		Запрос.Текст = Запрос.Текст + ",
						  |	ОтветыНаЗапросыИОС.Данные";
	КонецЕсли;
	Запрос.Текст = Запрос.Текст + "
						  |ИЗ
						  |	РегистрСведений.ОтветыНаЗапросыИОС КАК ОтветыНаЗапросыИОС
						  |
						  |		ЛЕВОЕ СОЕДИНЕНИЕ РегистрСведений.ПризнакиАрхивированияФайловДОСКонтролирующимиОрганами КАК СостояниеОбъектов
						  |			ПО 		(СостояниеОбъектов.Объект = ОтветыНаЗапросыИОС.Запрос)
						  |				И	(СостояниеОбъектов.ИмяФайла = ОтветыНаЗапросыИОС.ИмяФайла)
						  |				И	(СостояниеОбъектов.Владелец = Значение(Перечисление.ВидыАрхивируемыхМетаданныхДО.ОтветыНаЗапросыИОС))
						  |
						  |ГДЕ
						  |	ОтветыНаЗапросыИОС.Запрос = &Запрос";
	Запрос.УстановитьПараметр("Запрос", парамЗапрос);
	Возврат Запрос.Выполнить().Выгрузить();
	
КонецФункции

Процедура ОбновитьОтветНаЗапросИОСВРегистреОтветовПриНеобходимости(Объект)
	
	// заполняем регистр
	ЗапросИОС = Объект.ЦиклОбмена.Предмет;
	ВложенияОтветы = ПолучитьВложенияТранспортногоСообщения(Объект.Ссылка, Истина, Перечисления.ТипыСодержимогоТранспортногоКонтейнера.ОтветПриложениеНаЗапросПФР);
	Для Каждого ВложениеОтвет Из ВложенияОтветы Цикл
		ДобавитьОтветНаЗапросИОС(ЗапросИОС, ВложениеОтвет.ИмяФайла, ВложениеОтвет.Данные, ВложениеОтвет.Размер, Объект.ПротоколСОшибкой, ВложениеОтвет.ТипСодержимогоФайла);
	КонецЦикла;
	
КонецПроцедуры

Функция ДобавитьОтветНаЗапросИОС(Запрос, ИмяФайла, Данные, Размер = Неопределено, ОтветОтрицательный = Ложь, ТипСодержимогоФайла = Неопределено) Экспорт
	
	Если Размер = Неопределено Тогда
		Размер = ОпределитьРазмер(Данные);
	КонецЕсли;
	
	МенеджерЗаписи = РегистрыСведений.ОтветыНаЗапросыИОС.СоздатьМенеджерЗаписи();
	МенеджерЗаписи.Запрос = Запрос;
	МенеджерЗаписи.ИмяФайла = ИмяФайла;
	МенеджерЗаписи.Размер = Размер;
	МенеджерЗаписи.ОтветОтрицательный = ОтветОтрицательный;
	МенеджерЗаписи.ТипСодержимогоФайла = ТипСодержимогоФайла;
	
	ТипДанных = ТипЗнч(Данные);
	Если ТипДанных = Тип("ХранилищеЗначения") Тогда // если хранилище значения
		МенеджерЗаписи.Данные = Данные;
	ИначеЕсли ТипДанных = Тип("Строка") Тогда // если полное имя файла
		МенеджерЗаписи.Данные = Новый ХранилищеЗначения(Новый ДвоичныеДанные(Данные), Новый СжатиеДанных(9));
	Иначе // если двоичные данные
		МенеджерЗаписи.Данные = Новый ХранилищеЗначения(Данные, Новый СжатиеДанных(9));
	КонецЕсли;
	
	Попытка
		МенеджерЗаписи.Записать(Истина);
		Отказ = Ложь;
		ЭлектронныйДокументооборотСКонтролирующимиОрганами.ЗаписьОбъектовРегламентированнойОтчетности(Запрос, Отказ);
		Возврат Истина;
	Исключение
		ТекстСообщения = СтроковыеФункцииКлиентСервер.ПодставитьПараметрыВСтроку(НСтр("ru='Не удалось сохранить в информационной базе файл ответа на запрос ИОС %1!'"),
			Символы.ПС + ИнформацияОбОшибке().Описание);
		ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
		Возврат Ложь;
	КонецПопытки;
	
КонецФункции

///////////////////////////////////////////////////////////////////////////////
// Заявление абонента оператора электронного документооборота

////////////////////////////////////////////////////////////////////////////////
// ЗАПРОС НА ВЫПИСКУ ИЗ ЕДИНОГО ГОСУДАРСТВЕННОГО РЕЕСТРА
//
Функция ПолучитьОтветыНаЗапросыВыпискиИзЕГРЮЛ_ЕГРИП(парамЗапрос, СДанными = Ложь) Экспорт
	
	Запрос = Новый Запрос;
	Запрос.Текст = Запрос.Текст + "ВЫБРАТЬ РАЗРЕШЕННЫЕ
	                      |	ОтветыНаЗапросыЕГР.ИмяФайла,
	                      |	ОтветыНаЗапросыЕГР.Размер,						  
	                      |	ОтветыНаЗапросыЕГР.РезультатОбработкиЗапроса
						  |	, ВЫБОР
						  |		КОГДА СостояниеОбъектов.Архивный ЕСТЬ NULL ТОГДА Ложь
						  |		КОГДА СостояниеОбъектов.Архивный = Ложь ТОГДА Ложь
						  |	ИНАЧЕ
						  |		Истина
						  |	КОНЕЦ ВАрхиве";
	Если СДанными Тогда
		Запрос.Текст = Запрос.Текст + ",
	                      |	ОтветыНаЗапросыЕГР.Данные";
	КонецЕсли;
	Запрос.Текст = Запрос.Текст + "
	                      |ИЗ
	                      |	РегистрСведений.ОтветыНаЗапросыВыпискиИзЕГРЮЛ_ЕГРИП КАК ОтветыНаЗапросыЕГР
						  |
						  |		ЛЕВОЕ СОЕДИНЕНИЕ РегистрСведений.ПризнакиАрхивированияФайловДОСКонтролирующимиОрганами КАК СостояниеОбъектов
						  |			ПО 		(СостояниеОбъектов.Объект = ОтветыНаЗапросыЕГР.Запрос)
						  |				И	(СостояниеОбъектов.ИмяФайла = ОтветыНаЗапросыЕГР.ИмяФайла)
						  |				И	(СостояниеОбъектов.Владелец = Значение(Перечисление.ВидыАрхивируемыхМетаданныхДО.ОтветыНаЗапросыВыпискиИзЕГРЮЛ_ЕГРИП))
						  |
	                      |ГДЕ
	                      |	ОтветыНаЗапросыЕГР.Запрос = &Запрос
						  |	И ОтветыНаЗапросыЕГР.Размер > 0";
	Запрос.УстановитьПараметр("Запрос", парамЗапрос);
	Возврат Запрос.Выполнить().Выгрузить();
	
КонецФункции

Процедура ОбновитьОтветНаЗапросВыпискиЕГРЮЛ_ЕГРИП_ПриНеобходимости(Объект)
	
	ЗапросЕГР = Объект.ЦиклОбмена.Предмет;
	ОтветыНаЗапрос = ПолучитьОтветыНаЗапросыВыпискиИзЕГРЮЛ_ЕГРИП(ЗапросЕГР);
	
	// Получим результат обработки запроса
	РезультатОбработкиЗапроса = "";
	РезультатОбработкиЗапросаЗаписан = Ложь;
	
	ОписаниеСведений = ПолучитьВложенияТранспортногоСообщения(Объект.Ссылка, Истина,Перечисления.ТипыСодержимогоТранспортногоКонтейнера.РезультатОбработкиЗапросаНаВыпискуЕГРЮЛ_ЕГРИП);
	
	Если ЗначениеЗаполнено(ОписаниеСведений) И НЕ ОписаниеСведений.Количество() = 0 Тогда
		
		// выгружаем описание сведений во временный файл
		ВремФайл = ПолучитьИмяВременногоФайла();
		Попытка
			ОписаниеСведений[0].Данные.Получить().Записать(ВремФайл);
		Исключение
			ТекстСообщения = СтроковыеФункцииКлиентСервер.ПодставитьПараметрыВСтроку(
				НСтр("ru = 'Ошибка сохранения во временный файл описание сведений:
	                  |%1'"), КраткоеПредставлениеОшибки(ИнформацияОбОшибке()));
			ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
		КонецПопытки;
		РезультатОбработкиЗапроса = ПолучитьРезультатОбработкиЗапросаЕГРИзФайла(ВремФайл);
				
		ОперацииСФайламиЭДКО.УдалитьВременныйФайл(ВремФайл);
	КонецЕсли;
	
	
	Если НЕ ЗначениеЗаполнено(ОтветыНаЗапрос) ИЛИ ОтветыНаЗапрос.Количество() = 0 Тогда
		
		ВложенияОтветы = ПолучитьВложенияТранспортногоСообщения(Объект.Ссылка, Истина, Перечисления.ТипыСодержимогоТранспортногоКонтейнера.ОтветНаЗапросВыпискиИзЕГРЮЛ_ЕГРИП);
		
		Для Каждого ВложениеОтвет Из ВложенияОтветы Цикл
			ДобавитьОтветНаЗапросНаВыпискуИзЕГРЮЛ_ЕГРИП(ЗапросЕГР, ВложениеОтвет.ИмяФайла, ВложениеОтвет.Данные, ВложениеОтвет.Размер, РезультатОбработкиЗапроса);
			РезультатОбработкиЗапросаЗаписан = Истина;
		КонецЦикла;
		
	КонецЕсли;
	
	// запишем статус обработки запроса для пакетов не имеющих файла ответа
	Если НЕ ПустаяСтрока(РезультатОбработкиЗапроса) И НЕ РезультатОбработкиЗапросаЗаписан Тогда
		ДобавитьОтветНаЗапросНаВыпискуИзЕГРЮЛ_ЕГРИП(ЗапросЕГР, , , , РезультатОбработкиЗапроса);
	КонецЕсли;
	
КонецПроцедуры

Функция ДобавитьОтветНаЗапросНаВыпискуИзЕГРЮЛ_ЕГРИП(Запрос, ИмяФайла = Неопределено, Данные = Неопределено, Размер = Неопределено, РезультатОбработкиЗапроса = Неопределено)
	
	Если Размер = Неопределено Тогда
		Размер = ОпределитьРазмер(Данные);
	КонецЕсли;
	
	МенеджерЗаписи = РегистрыСведений.ОтветыНаЗапросыВыпискиИзЕГРЮЛ_ЕГРИП.СоздатьМенеджерЗаписи();
	МенеджерЗаписи.Запрос = Запрос;
	МенеджерЗаписи.ИмяФайла = ИмяФайла;
	МенеджерЗаписи.Размер = Размер;
	МенеджерЗаписи.РезультатОбработкиЗапроса = РезультатОбработкиЗапроса;
	
	ТипДанных = ТипЗнч(Данные);
	Если ТипДанных = Тип("ХранилищеЗначения") Тогда // если хранилище значения
		МенеджерЗаписи.Данные = Данные;
	ИначеЕсли ТипДанных = Тип("Строка") Тогда // если полное имя файла
		МенеджерЗаписи.Данные = Новый ХранилищеЗначения(Новый ДвоичныеДанные(Данные), Новый СжатиеДанных(9));
	Иначе // если двоичные данные
		МенеджерЗаписи.Данные = Новый ХранилищеЗначения(Данные, Новый СжатиеДанных(9));
	КонецЕсли;
	
	Попытка
		МенеджерЗаписи.Записать(Истина);
		Отказ = Ложь;
		ЭлектронныйДокументооборотСКонтролирующимиОрганами.ЗаписьОбъектовРегламентированнойОтчетности(Запрос, Отказ);
		Возврат Истина;
	Исключение
		ТекстСообщения = СтроковыеФункцииКлиентСервер.ПодставитьПараметрыВСтроку(
			НСтр("ru = 'Не удалось сохранить в информационной базе файл ответа на запрос выписки из ЕГРЮЛ/ЕГРИП ""%1"" по причине:
                  |%2'"), Запрос, КраткоеПредставлениеОшибки(ИнформацияОбОшибке()));
		ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
		Возврат Ложь;
	КонецПопытки;
	
КонецФункции

Функция ПолучитьРезультатОбработкиЗапросаЕГРИзФайла(ИмяФайла)
	
	РезультатОбработкиЗапроса = "";
	
	// считываем файл описания ответа
	ТекстXML = Неопределено;
	ДеревоОписанияСведений = ЗагрузитьXMLВДеревоЗначений(ИмяФайла, ТекстXML);
	Если НЕ ДеревоОписанияСведений = Неопределено Тогда
		
		РезультатОбработки = ДеревоОписанияСведений.Строки.Найти("РезультатОбработки");
		
		Если НЕ РезультатОбработки = Неопределено Тогда
			РезультатЗначение = РезультатОбработки.Строки.Найти("Результат");
			Если НЕ РезультатЗначение = Неопределено Тогда
				РезультатОбработкиЗапроса = СокрЛП(РезультатЗначение.Значение);
			КонецЕсли;
		КонецЕсли;
	КонецЕсли;
	
	Возврат РезультатОбработкиЗапроса;
КонецФункции

Функция ДатаИзСтроки(СтрДата) Экспорт
	Если СтрДата = "" Тогда
		ВозвращаемаяДата = Дата(1, 1, 1);
	Иначе
		МассивПодстрок = СтроковыеФункцииКлиентСервер.РазложитьСтрокуВМассивПодстрок(СтрДата, ".");
		Если Число(МассивПодстрок[0]) = 0 Тогда
			МассивПодстрок[0] = "1";
		КонецЕсли;
		Если Число(МассивПодстрок[1]) = 0 Тогда
			МассивПодстрок[1] = "1";
		КонецЕсли;
		Если Число(МассивПодстрок[2]) = 0 Тогда
			МассивПодстрок[2] = "1";
		КонецЕсли;
		ВозвращаемаяДата = Дата(МассивПодстрок[2], МассивПодстрок[1], МассивПодстрок[0]);
	КонецЕсли;
	
	Возврат ВозвращаемаяДата;
	
КонецФункции

Функция РазбитьБольшойФайлУведомленияНаНесколькоМаленьких(ОписаниеФайлаВыгрузки, УникальныйИдентификаторВызывающейФормы) Экспорт
	
	// фрагмент кода для управляемого приложения
	ИмяФайлаВыгрузки = ПолучитьКаталогВременныхФайлов() + ОписаниеФайлаВыгрузки.Имя;
	ПолучитьИзВременногоХранилища(ОписаниеФайлаВыгрузки.АдресДанных).Записать(ИмяФайлаВыгрузки);
	
	// делаем запас на погрешность расчетов в 1 Мб
	МаксимальныйРазмерФайлаУведомленияОКонтролируемыхСделках = МаксимальныйРазмерФайлаУведомленияОКонтролируемыхСделках() - 1024 * 1024 * 1;
	
	ТекстШапкиФайла 		= "";
	ТекстПодвалаФайла 		= "";
	ОткрывающийТэг			= "<УвКонтрСд>";
	ЗакрывающийТэг			= "</УвКонтрСд>";
	ЗакрывающийТэгСвКонтрСд = "</СвКонтрСд>";
	
	МассивФайлов = Новый Массив;
	
	// построчно считываем файл, если читать сразу весь текст, то возникает ошибка переполнения памяти
	ОбъектЧтениеТекста 		= Новый ЧтениеТекста(ИмяФайлаВыгрузки, "windows-1251");
	ФайлВыгрузки 			= Новый Файл(ИмяФайлаВыгрузки);
	РазмерИсходногоФайла 	= ФайлВыгрузки.Размер();
	ФайлВыгрузки 			= Неопределено;
	
	// считываем строку до тэга <УвКонтрСд>
	ТекстШапкиФайла 	= ОбъектЧтениеТекста.ПрочитатьСтроку(ОткрывающийТэг) + ОткрывающийТэг;
	ТекстПодвалаФайла 	= 	"		</УвКонтрСд>
							|	</Документ>
							|</Файл>";
	МаксимальныйРазмерФайлаУведомленияОКонтролируемыхСделкахБезРазмераПодвала = МаксимальныйРазмерФайлаУведомленияОКонтролируемыхСделках - РазмерВБайтах(ТекстПодвалаФайла);

	НовыйТекстШапкиФайла = "";
	РазмерСформированнойШапки = 0;
	ВремФайл 	= "";
	ИдФайлПерв 	= "";
	НомФайл 	= 1;
	КолФайл 	= (РазмерИсходногоФайла - РазмерВБайтах(ТекстШапкиФайла) + РазмерВБайтах(ТекстПодвалаФайла)) / (МаксимальныйРазмерФайлаУведомленияОКонтролируемыхСделках - РазмерВБайтах(ТекстШапкиФайла) - РазмерВБайтах(ТекстПодвалаФайла));
	Если КолФайл <> Цел(КолФайл) Тогда
		КолФайл = Цел(КолФайл) + 1;
	КонецЕсли;
	
	НовыйЗапись = НоваяЗаписьФайлаКомплекта(ИмяФайлаВыгрузки, ИдФайлПерв, ТекстШапкиФайла, ВремФайл, КолФайл, НомФайл, РазмерСформированнойШапки, НовыйТекстШапкиФайла);
	РазмерТекстаСодержимогоФайла = РазмерСформированнойШапки;
	РазмерЗакрывающегоТэгаСвКонтрСд = РазмерВБайтах(ЗакрывающийТэгСвКонтрСд);
	
	ЗакрывающийТэгСвКонтрСдСОтступом = "   " + ЗакрывающийТэгСвКонтрСд;
	
	// читаем текст до тэга </СвКонтрСд>
	ТекущаяСтрока = ОбъектЧтениеТекста.ПрочитатьСтроку(ЗакрывающийТэгСвКонтрСд);
	ЗакрывающийТэгСвКонтрСдСОтступом = "   " + ЗакрывающийТэгСвКонтрСд;
	
	Пока ТекущаяСтрока <> Неопределено Цикл
		
		РазмерТекущейСтроки = РазмерВБайтах(ТекущаяСтрока);
		РазмерТекстаСодержимогоФайла = РазмерТекстаСодержимогоФайла + РазмерТекущейСтроки + РазмерЗакрывающегоТэгаСвКонтрСд;
		
		Если РазмерТекстаСодержимогоФайла > МаксимальныйРазмерФайлаУведомленияОКонтролируемыхСделках Тогда
			// закрываем файл
			ПозицияЗакрывающегоТэгаУвКонтрСд = СтрНайти(ТекущаяСтрока, ЗакрывающийТэг);
			Если ПозицияЗакрывающегоТэгаУвКонтрСд = 0 Тогда
				НовыйЗапись.Записать(Символы.ПС + ТекстПодвалаФайла);
			КонецЕсли; 
				
			НовыйЗапись.Закрыть();
			НовыйЗапись = Неопределено;
			МассивФайлов.Добавить(ВремФайл);
			
			// если найден закрывающий тэг, то нового файла не создаем, цикл прерываем
			Если НЕ ПозицияЗакрывающегоТэгаУвКонтрСд = 0 Тогда
				Прервать;
			КонецЕсли;
			// открываем новый файл
			НомФайл = НомФайл + 1;
			НовыйЗапись = НоваяЗаписьФайлаКомплекта(ИмяФайлаВыгрузки, ИдФайлПерв, ТекстШапкиФайла, ВремФайл, КолФайл, НомФайл, РазмерСформированнойШапки, НовыйТекстШапкиФайла);
			РазмерТекстаСодержимогоФайла = РазмерСформированнойШапки + РазмерТекущейСтроки;
		КонецЕсли;
		
        ПредыдущееЗначениеТекущейСтроки = ТекущаяСтрока;
		ТекущаяСтрока = ОбъектЧтениеТекста.ПрочитатьСтроку(ЗакрывающийТэгСвКонтрСд);
		
		Если ТекущаяСтрока = Неопределено Тогда
			НовыйЗапись.Записать(ПредыдущееЗначениеТекущейСтроки);
		Иначе 
			НовыйЗапись.Записать(ПредыдущееЗначениеТекущейСтроки + ЗакрывающийТэгСвКонтрСдСОтступом);
		КонецЕсли;
	КонецЦикла;
	
	ОбъектЧтениеТекста.Закрыть();

	// если мы еще не закрыли этот файл
	Если МассивФайлов.Найти(ВремФайл) = Неопределено Тогда
		// подвал не добавляем в файл, поскольку сюда записался конец общего файла
		НовыйЗапись.Закрыть();
		НовыйЗапись = Неопределено;
		МассивФайлов.Добавить(ВремФайл);
	КонецЕсли;
	
	ОбъектЧтениеТекста = Неопределено;
	
	// фрагмент кода для управляемого приложения
	МассивОписанийФайлов = Новый Массив;
	Для каждого ФайлМассива Из МассивФайлов Цикл
		Файл = Новый Файл(ФайлМассива);
		АдресВоВременномХранилище = ПоместитьВоВременноеХранилище(Новый ДвоичныеДанные(ФайлМассива), УникальныйИдентификаторВызывающейФормы);
		МассивОписанийФайлов.Добавить(Новый Структура("Имя, ПолноеИмя, Расширение, АдресДанных", Файл.Имя, Файл.ПолноеИмя, Файл.Расширение, АдресВоВременномХранилище));
	КонецЦикла; 
	
	Возврат МассивОписанийФайлов;
	
КонецФункции

Функция ПолучитьКаталогВременныхФайлов() Экспорт
	
	ВременныйКаталог = КаталогВременныхФайлов();
	РазделительПутиОС = ПолучитьРазделительПути();
	Возврат ?(Прав(ВременныйКаталог, 1) = РазделительПутиОС, ВременныйКаталог, ВременныйКаталог + РазделительПутиОС);
	
КонецФункции

Функция НоваяЗаписьФайлаКомплекта(ИмяФайлаВыгрузки, ИдФайлПерв, Знач ТекстШапкиФайла, ВремФайл, КолФайл, НомФайл, РазмерСформированнойШапки, НовыйТекстШапкиФайла)
	
	КороткоеИмяФайлаБезГУИДНаКонце = КороткоеИмяФайлаБезГУИДНаКонце(ИмяФайлаВыгрузки);
	
	ИдентификационныйНомерФайла = Новый УникальныйИдентификатор;
	ВремФайл = ПолучитьКаталогВременныхФайлов() + КороткоеИмяФайлаБезГУИДНаКонце + ИдентификационныйНомерФайла + ".xml";
	НовыйЗапись = Новый ЗаписьТекста(ВремФайл, "windows-1251");

	Если НЕ ЗначениеЗаполнено(ИдФайлПерв) Тогда
		ИдФайлПерв = КороткоеИмяФайлаБезГУИДНаКонце + ИдентификационныйНомерФайла;
	КонецЕсли;
	
	ФайлВыгрузки = Новый Файл(ИмяФайлаВыгрузки);
	ИдФайлИсх = ФайлВыгрузки.ИмяБезРасширения;
	ФайлВыгрузки = Неопределено;
	
	ДополнениеФайлаВерсии502 = "ВерсФорм=""%1"" ИдФайлИсх=""%2"" ИдФайлПерв=""%3"" КолФайл=""%4"" НомФайл=""%5""";
	ДополнениеФайлаВерсии502 = СтрЗаменить(ДополнениеФайлаВерсии502, "%1", "5.02");
	ДополнениеФайлаВерсии502 = СтрЗаменить(ДополнениеФайлаВерсии502, "%2", ИдФайлИсх);
	ДополнениеФайлаВерсии502 = СтрЗаменить(ДополнениеФайлаВерсии502, "%3", ИдФайлПерв);
	ДополнениеФайлаВерсии502 = СтрЗаменить(ДополнениеФайлаВерсии502, "%4", КолФайл);
	ДополнениеФайлаВерсии502 = СтрЗаменить(ДополнениеФайлаВерсии502, "%5", НомФайл);
	
	ОбъектФайл = Новый Файл(ИмяФайлаВыгрузки);
	ИмяБезРасширения = ОбъектФайл.ИмяБезРасширения;

	ТекстШапкиФайла = СтрЗаменить(ТекстШапкиФайла, "ВерсФорм=""5.01""", ДополнениеФайлаВерсии502);
	ТекстШапкиФайла = СтрЗаменить(ТекстШапкиФайла, "ИдФайл=" + """" + ИмяБезРасширения + """", "ИдФайл=" + """" + КороткоеИмяФайлаБезГУИДНаКонце + ИдентификационныйНомерФайла + """");
	
	НовыйТекстШапкиФайла = ТекстШапкиФайла;
	РазмерСформированнойШапки = РазмерВБайтах(НовыйТекстШапкиФайла);
	НовыйЗапись.Записать(СокрЛП(ТекстШапкиФайла));
	
	Возврат НовыйЗапись;

КонецФункции

Функция КороткоеИмяФайлаБезГУИДНаКонце(ИмяФайлаВыгрузки)
	ФайлНаДиске = Новый Файл(ИмяФайлаВыгрузки);
	КороткоеИмяФайла = ФайлНаДиске.ИмяБезРасширения;
	
	КороткоеИмяФайлаБезГУИДНаКонце = "";
	Строки = СтрЗаменить(КороткоеИмяФайла,"_",Символы.ПС);
	Для НомерСтроки = 1 По 6 Цикл
		КороткоеИмяФайлаБезГУИДНаКонце = КороткоеИмяФайлаБезГУИДНаКонце + СтрПолучитьСтроку(Строки,НомерСтроки) + "_";
	КонецЦикла;
	
	Возврат КороткоеИмяФайлаБезГУИДНаКонце;
	
КонецФункции

Функция РазмерВБайтах(Знач ПроверяемыйТекст)
	
	// каждый символ строки кодируется 1 байтом, каждый символ переноса строки кодируется 2 байтами
	КоличествоВхожденийПС 	= СтрЧислоВхождений(ПроверяемыйТекст, Символы.ПС); 
	ПолнаяДлинаСтроки 		= СтрДлина(ПроверяемыйТекст);
	ДлинаСтрокиБезСимволаПС = ПолнаяДлинаСтроки - КоличествоВхожденийПС;
	Результат = ДлинаСтрокиБезСимволаПС + КоличествоВхожденийПС * 2;
	
	Возврат Результат;
	
КонецФункции


Функция ЭтоФайлВыгрузкиУведомленияОКонтролируемыхСделках(КороткоеИмяФайла) Экспорт
	
	Возврат ВРег(Лев(КороткоеИмяФайла, 10)) = "UT_UVKNRSD";
	
КонецФункции

Функция ЭтоФайлВыгрузкиУведомленияОРозничныхЦенахНаТабак(КороткоеИмяФайла) Экспорт
	
	Возврат ВРег(Лев(КороткоеИмяФайла, 11)) = "ON_UVCENTAB";
	
КонецФункции

Функция ЭтоФайлВыгрузкиДекларацияНДС(КороткоеИмяФайла) Экспорт
	
	Возврат ВРег(Лев(КороткоеИмяФайла, 7)) = "NO_NDS_";	
	
КонецФункции



Функция МаксимальныйРазмерФайлаУведомленияОКонтролируемыхСделках() Экспорт
	Возврат 1024 * 1024 * 100;
КонецФункции


//////////////////////////////////////////////////////////////////////////////////
//// ДОКУМЕНТООБОРОТ С ФСС
////

#Область ДокументооборотСФСС

Функция СформироватьФайлОтправкиВФССССервера(
		КороткоеИмяФайлаПакета,
		СтрокаBase64ФайлаПакета,
		ЭтоАдреса = Истина,
		Адрес = Неопределено,
		ТипОтправляемогоДокумента = Неопределено,
		ВидОтправляемогоДокумента = Неопределено) Экспорт
	
	Результат = Новый Структура;
	Результат.Вставить("СтрокаBase64ФайлаПакета", 	СтрокаBase64ФайлаПакета);
	Результат.Вставить("СтрокаBase64ФайлаОтправки", Неопределено);
	
	ЭтоДанныеЭЛН = (ТипОтправляемогоДокумента = Перечисления.ТипыОтправляемыхДокументов.ДанныеЭЛНФСС);
	ЭтоПодтверждениеВидаДеятельности =
		(ТипОтправляемогоДокумента = Перечисления.ТипыОтправляемыхДокументов.ПрочаяОтчетностьФСС
		И ВидОтправляемогоДокумента = Справочники.ВидыОтправляемыхДокументов.ПодтверждениеВидаДеятельности);
	ЭтоРеестрСтимулирующихВыплатМедицинскимИСоциальнымРаботникам =
		(ТипОтправляемогоДокумента = Перечисления.ТипыОтправляемыхДокументов.ПрочаяОтчетностьФСС
		И ВидОтправляемогоДокумента =
		Справочники.ВидыОтправляемыхДокументов.РеестрСтимулирующихВыплатМедицинскимИСоциальнымРаботникам);
	ЭтоАдресТекстаТребуетКонвертации = ЭтоДанныеЭЛН ИЛИ ЭтоПодтверждениеВидаДеятельности
		ИЛИ ЭтоРеестрСтимулирующихВыплатМедицинскимИСоциальнымРаботникам;
	
	Если ЭтоАдресТекстаТребуетКонвертации Тогда
		ТекстФайлаПакета = ПолучитьИзВременногоХранилища(СтрокаBase64ФайлаПакета);
		ИмяФайлаПакета = ПолучитьИмяВременногоФайла();
		ЗаписьФайлаПакета = Новый ЗаписьТекста(ИмяФайлаПакета, "utf-8");
		ЗаписьФайлаПакета.Записать(ТекстФайлаПакета);
		ЗаписьФайлаПакета.Закрыть();
		ДвоичныеДанныеФайлаПакета = Новый ДвоичныеДанные(ИмяФайлаПакета);
	КонецЕсли;
	
	//файл отправки является файлом пакета
	Если ЭтоАдреса Тогда
		Если ЭтоАдресТекстаТребуетКонвертации Тогда
			Результат.СтрокаBase64ФайлаПакета = ПоместитьВоВременноеХранилище(ДвоичныеДанныеФайлаПакета,
				СтрокаBase64ФайлаПакета);
		Иначе
			ДвоичныеДанныеФайлаПакета = ПолучитьИзВременногоХранилища(СтрокаBase64ФайлаПакета);
		КонецЕсли;
		Результат.СтрокаBase64ФайлаОтправки = ПоместитьВоВременноеХранилище(ДвоичныеДанныеФайлаПакета, Адрес);
		
	Иначе
		Если ЭтоАдресТекстаТребуетКонвертации Тогда
			Результат.СтрокаBase64ФайлаПакета = Base64Строка(ДвоичныеДанныеФайлаПакета);
		КонецЕсли;
		Результат.СтрокаBase64ФайлаОтправки = Результат.СтрокаBase64ФайлаПакета;
	КонецЕсли;
	
	Если ЭтоАдресТекстаТребуетКонвертации Тогда
		ОперацииСФайламиЭДКО.УдалитьВременныйФайл(ИмяФайлаПакета);
	КонецЕсли;
	
	Возврат Результат;
	
КонецФункции

Процедура ОбновитьРезультатКонкретнойОтправкиФССССервера(
		ОтправкаСсылка,
		ПолученРезультатОтСервераФСС,
		НастройкиССервера,
		АдресЗашифрованногоЗапросаSOAP = Неопределено,
		АдресЗашифрованногоОтветаSOAP = Неопределено) Экспорт
	
	ИнформацияОбОтправляемомДокументе = ПолучитьТипВидаОтправляемогоДокументаПоСсылкеНаОбъектОтправкиДляФСС(
		ОтправкаСсылка,
		Истина);
	ТипОтправляемогоДокумента = ИнформацияОбОтправляемомДокументе.ТипОтправляемогоДокумента;
	ВидОтправляемогоДокумента = ИнформацияОбОтправляемомДокументе.ВидОтправляемогоДокумента;
	
	АдресСервера = СерверПриемаОтчетностиФСС(ТипОтправляемогоДокумента, ВидОтправляемогоДокумента);
	
	Если АдресСервера = Неопределено Тогда
		
		ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(НСтр("ru = 'Не удалось определить тип документа!'"));
		Возврат;
	
	КонецЕсли;
	
	НастройкиССервера = Новый Структура("АдресСервера, РазделениеВключено", АдресСервера, ОбщегоНазначения.РазделениеВключено());
	
	РезультатОтправки = ПолучитьРезультатОтправкиФСС(
		АдресСервера,
		ОтправкаСсылка.ИдентификаторОтправкиНаСервере,
		ОтправкаСсылка,
		ТипОтправляемогоДокумента,
		ВидОтправляемогоДокумента,
		АдресЗашифрованногоЗапросаSOAP);
	ПолученРезультатОтСервераФСС = (РезультатОтправки <> Неопределено);
	Если РезультатОтправки <> Неопределено И РезультатОтправки.Свойство("АдресЗашифрованногоОтветаSOAP") Тогда
		АдресЗашифрованногоОтветаSOAP = РезультатОтправки.АдресЗашифрованногоОтветаSOAP;
	Иначе
		СохранитьРезультатОтправкиФСС(ОтправкаСсылка, РезультатОтправки);
	КонецЕсли;
	
	Если НЕ ПолученРезультатОтСервераФСС Тогда
		ПредставлениеОтчетаОтправки = ПредставлениеОтчета(ОтправкаСсылка.ОтчетСсылка);
		НастройкиССервера.Вставить("ПредставлениеОтчетаОшибки", ПредставлениеОтчетаОтправки);
	КонецЕсли;
	
КонецПроцедуры


Функция ПолучитьРезультатОтправкиФСС(
		АдресСервера,
		ИдентификаторОтправкиНаСервере,
		ОтправкаСсылка,
		ТипОтправляемогоДокумента = Неопределено,
		ВидОтправляемогоДокумента = Неопределено,
		АдресЗашифрованногоЗапросаSOAP = Неопределено)
	
	Результат = Новый Структура;
	
	НаименованиеОтчета = "";
	
	СсылкаКвитанция = "";
	СсылкаПротоколОшибок = "";
	СтатусОтчета = "";
	ЦветСтатуса = ЦветаСтиля.ЦветНезавершившейсяОтправкиБРО;
	СтатусОтправки = Перечисления.СтатусыОтправки.Отправлен;
	ЕстьОшибки = Ложь;
	
	//запрос протокола обработки
	КодировкаФайла = "utf-8";
	ОтчетСдан = Неопределено;
	ОтчетСданСОшибками = Неопределено;
	ЕстьОшибкиФорматногоКонтроля = Неопределено;
	ЕстьПрочиеОшибки = Неопределено;
	ТекстПоследнейОшибкиВебСервиса = "";
	КвитанцияОтсутствует = Неопределено;
	ДвоичныеДанныеКвитанции = Неопределено;
	ТЗОбработки = Новый ТаблицаЗначений;
	
	ЭтоПодтверждениеВидаДеятельности =
		(ТипОтправляемогоДокумента = Перечисления.ТипыОтправляемыхДокументов.ПрочаяОтчетностьФСС
		И ВидОтправляемогоДокумента = Справочники.ВидыОтправляемыхДокументов.ПодтверждениеВидаДеятельности);
	ЭтоПодписьИШифрованиеSOAP = ЭтоПодтверждениеВидаДеятельности;
	
	ИспользуетсяВебСервис = СтрНайти(ИдентификаторОтправкиНаСервере, "-") <> 9 ИЛИ ЭтоПодписьИШифрованиеSOAP;
	
	Если ИспользуетсяВебСервис Тогда
		// если отправка с использованием API
		
		// устанавливаем соединение с сервером
		Если ЭтоПодписьИШифрованиеSOAP Тогда
			Соединение = ДокументооборотСКО.УстановитьСоединениеССерверомИнтернета("https://" + АдресСервера,, 120);
		Иначе
			Соединение = ДокументооборотСКО.УстановитьСоединениеССерверомИнтернета(АдресСервера);
		КонецЕсли;
		Если Соединение = Неопределено Тогда
			Возврат Неопределено;
		КонецЕсли;
		
		Если ЭтоПодписьИШифрованиеSOAP Тогда
			РесурсНаСервере = "/ws-gtw-okved-confirm/api/soap/v1/OkvedconfirmGatewayService";
			
		ИначеЕсли ТипОтправляемогоДокумента <> Перечисления.ТипыОтправляемыхДокументов.Отчет4аФСС
		И ТипОтправляемогоДокумента <> Перечисления.ТипыОтправляемыхДокументов.РеестрСведенийФСС Тогда
			РесурсНаСервере = "/F4ExtService/ExtService.svc";
			ПространствоИменСервиса = "http://tempuri.org/";
			ИмяСервиса = "IExtService";
			
		Иначе
			РесурсНаСервере = "/ExtService/GatewayService.svc";
			ПространствоИменСервиса = "http://asystems.fss";
			ИмяСервиса = "IGatewayService";
		КонецЕсли;
		
		РесурсНаСервере = ДокументооборотСКО.ПолучитьЗаменяемоеЗначениеТестовогоСервера1С(РесурсНаСервере, "{FSS}" + РесурсНаСервере, Соединение);
		
		Если ЭтоПодписьИШифрованиеSOAP Тогда
			// формируем общие заголовки зашифрованного запроса SOAP
			ЗаголовкиHTTP = Новый Соответствие();
			ЗаголовкиHTTP.Вставить("Content-Type", "text/xml; charset=utf-8");
			ЗаголовкиHTTP.Вставить("SOAPAction", "http://gtw.ws.fss.ru/openservice/okvedconfirm/getResultById");
			ЗаголовкиHTTP.Вставить("Proxy-Connection", "Keep-Alive");
			
			ТекстЗапросаSOAP = ПолучитьИзВременногоХранилища(АдресЗашифрованногоЗапросаSOAP);
			УдалитьИзВременногоХранилища(АдресЗашифрованногоЗапросаSOAP);
			
		Иначе
			// формируем общие заголовки запроса SOAP
			ЗаголовкиHTTP = Новый Соответствие();
			ЗаголовкиHTTP.Вставить("Content-Type", "application/soap+xml; charset=utf-8");
			ЗаголовкиHTTP.Вставить("Proxy-Connection", "Keep-Alive");
			
			ТекстЗапросаSOAP =
				"<s:Envelope xmlns:s=""http://www.w3.org/2003/05/soap-envelope"" xmlns:a=""http://www.w3.org/2005/08/addressing"">
				|	<s:Header>
				|		<a:Action s:mustUnderstand=""1"">" + ПространствоИменСервиса + ?(Прав(ПространствоИменСервиса, 1) <> "/", "/", "")
							+ ИмяСервиса + "/UploadGetByExtID</a:Action>
				|		<a:To s:mustUnderstand=""1"">http://" + АдресСервера + РесурсНаСервере + "</a:To>
				|	</s:Header>
				|	<s:Body>
				|		<UploadGetByExtID xmlns=""" + ПространствоИменСервиса + """>
				|			<id>" + ИдентификаторОтправкиНаСервере + "</id>
				|		</UploadGetByExtID>
				|	</s:Body>
				|</s:Envelope>";
		КонецЕсли;
		
		ИмяФайлаSOAP = ПолучитьИмяВременногоФайла();
		ТекстовыйФайлSOAP = Новый ЗаписьТекста(ИмяФайлаSOAP, "utf-8");
		ТекстовыйФайлSOAP.Записать(ТекстЗапросаSOAP);
		ТекстовыйФайлSOAP.Закрыть();
		
		// посылаем запрос
		ФайлРезультата = ПолучитьИмяВременногоФайла();
		Попытка
			Соединение.ОтправитьДляОбработки(ИмяФайлаSOAP, РесурсНаСервере, ФайлРезультата, ЗаголовкиHTTP);
		Исключение
			ОперацииСФайламиЭДКО.УдалитьВременныйФайл(ИмяФайлаSOAP);
			ИнформацияОбОшибке = ИнформацияОбОшибке();
			
			Для ИндексПричиныОшибки = 1 По 5 Цикл
				Если НЕ ЭтоПодписьИШифрованиеSOAP И СтрНайти(ИнформацияОбОшибке.Описание, "ExtServiceException") > 0 Тогда
					ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(НСтр("ru = 'Сбой на сервере ФСС.'"));
					Возврат Результат;
					
				ИначеЕсли ЭтоПодписьИШифрованиеSOAP
					И СтрНайти(ИнформацияОбОшибке.Описание, "http://schemas.xmlsoap.org/soap/envelope/") > 0
					И СтрНайти(ИнформацияОбОшибке.Описание, "<faultstring>") > 0 Тогда
					
					ОписаниеОшибкиСервером = ТекстПослеПрефикса(ИнформацияОбОшибке.Описание, "<faultstring>", "<");
					ОписаниеОшибкиСервером = ТекстИзHTML(ОписаниеОшибкиСервером);
					ОписаниеОшибкиСервером = НСтр("ru = 'Ошибка сервера ФСС.'") + " " + СокрЛ(ОписаниеОшибкиСервером);
					ОписаниеОшибкиСервером = СокрЛП(ОписаниеОшибкиСервером);
					ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ОписаниеОшибкиСервером);
					Возврат Результат;
				КонецЕсли;
				
				Если ИнформацияОбОшибке.Причина = Неопределено Тогда
					Прервать;
				КонецЕсли;
				ИнформацияОбОшибке = ИнформацияОбОшибке.Причина;
			КонецЦикла;
			
			Возврат Неопределено;
		КонецПопытки;
		
		ТекстовыйФайлРезультата = Новый ЧтениеТекста(ФайлРезультата, "utf-8");
		ТекстРезультата = ТекстовыйФайлРезультата.Прочитать();
		ТекстовыйФайлРезультата.Закрыть();
		ОперацииСФайламиЭДКО.УдалитьВременныйФайл(ИмяФайлаSOAP);
		ОперацииСФайламиЭДКО.УдалитьВременныйФайл(ФайлРезультата);
		
		Если НЕ ЗначениеЗаполнено(ТекстРезультата) Тогда
			ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(НСтр("ru = 'Сбой на сервере ФСС.'"));
			Возврат Результат;
		КонецЕсли;
		
		РезультатИзвлечения = ИзвлечьРезультатSOAP(
			ТекстРезультата,
			?(ЭтоПодписьИШифрованиеSOAP, "", "UploadGetByExtID"),
			ЭтоПодписьИШифрованиеSOAP);
		УзелРезультата 	= РезультатИзвлечения.УзелРезультата;
		ТекстОшибки 	= РезультатИзвлечения.ТекстОшибки;
		Если УзелРезультата = Неопределено Тогда
			Если ЗначениеЗаполнено(ТекстОшибки) Тогда
				ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстОшибки);
			Иначе
				ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(НСтр("ru = 'Ошибка формата ответа сервера ФСС!'"));
			КонецЕсли;
			Возврат Результат;
		КонецЕсли;
		
		Если ЭтоПодписьИШифрованиеSOAP Тогда
			АдресЗашифрованногоОтветаSOAP = ПоместитьВоВременноеХранилище(ТекстРезультата, Новый УникальныйИдентификатор);
			Возврат Новый Структура("АдресЗашифрованногоОтветаSOAP", АдресЗашифрованногоОтветаSOAP);
		КонецЕсли;
		
		ЗначениеСостояния = Неопределено;
		УзелСостояния = НайтиУзел(УзелРезультата, "STATE_ID");
		Если УзелСостояния <> Неопределено Тогда
			Попытка
				ЗначениеСостояния = XMLЗначение(Тип("Число"), УзелСостояния.Значение);
			Исключение
			КонецПопытки;
		КонецЕсли;
		
		ТекстСостояния = "";
		УзелТекстаСостояния = НайтиУзел(УзелРезультата, "STATE");
		Если УзелТекстаСостояния <> Неопределено Тогда
			ТекстСостояния = СокрЛП(XMLЗначение(Тип("Строка"), УзелТекстаСостояния.Значение));
		КонецЕсли;
		
		ЗначениеДатыОбновления = Неопределено;
		УзелДатыОбновления = НайтиУзел(УзелРезультата, "MD_DATE");
		Если УзелДатыОбновления <> Неопределено Тогда
			Попытка
				ЗначениеДатыОбновления = XMLЗначение(Тип("Дата"), УзелДатыОбновления.Значение);
			Исключение
			КонецПопытки;
		КонецЕсли;
		
		Если ЗначениеСостояния <> Неопределено Тогда
			УзелОшибки = НайтиУзел(УзелРезультата, "LAST_EXCEPTION");
			ТекстОшибки = "";
			Если УзелОшибки <> Неопределено Тогда
				ТекстОшибки = СокрЛП(XMLЗначение(Тип("Строка"), УзелОшибки.Значение));
				
				Если ЗначениеЗаполнено(ТекстОшибки) Тогда
					ВременныйФайлОшибки = ПолучитьИмяВременногоФайла();
					ДвоичныеДанныеОшибки = Base64Значение(ТекстОшибки);
					ДвоичныеДанныеОшибки.Записать(ВременныйФайлОшибки);
					
					ОбъектЧтениеТекста = Новый ЧтениеТекста(ВременныйФайлОшибки, "windows-1251");
					ТекстОшибки = ОбъектЧтениеТекста.Прочитать();
					ОбъектЧтениеТекста.Закрыть();
					ОперацииСФайламиЭДКО.УдалитьВременныйФайл(ВременныйФайлОшибки);
				КонецЕсли;
				ТекстПоследнейОшибкиВебСервиса = ТекстОшибки;
			КонецЕсли;
		КонецЕсли;
		
		УзелПротоколаРезультата = НайтиУзел(УзелРезультата, "FILE_TICKET");
		ЕстьПротокол = (УзелПротоколаРезультата <> Неопределено);
		Если ЕстьПротокол Тогда
			ФайлРезультата = ПолучитьИмяВременногоФайла();
			ДвоичныеДанныеПротоколаРезультата = Base64Значение(УзелПротоколаРезультата.Значение);
			ДвоичныеДанныеПротоколаРезультата.Записать(ФайлРезультата);
			КодировкаФайла = "windows-1251";
			
		Иначе
			ФайлРезультата = Неопределено;
			
			Если ЗначениеСостояния = Неопределено Тогда
				ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(НСтр("ru = 'Ошибка формата ответа сервера ФСС!'"));
				Возврат Результат;
			КонецЕсли;
			
		КонецЕсли;
		
		Если ЗначениеСостояния <> Неопределено Тогда
			// проанализировать состояние обработки
			РезультатФормирования = СформироватьТаблицуОбработки(
				ЗначениеСостояния,
				ТекстСостояния,
				ЗначениеДатыОбновления,
				ТекстОшибки,
				ЕстьПротокол,
				ТипОтправляемогоДокумента);
			
			Если НЕ ЕстьПротокол Тогда
				ТЗОбработки 				= РезультатФормирования.ТЗОбработки;
			КонецЕсли;
			ОтчетСдан 						= РезультатФормирования.ОтчетСдан;
			ОтчетСданСОшибками				= РезультатФормирования.ОтчетСданСОшибками;
			ЕстьОшибкиФорматногоКонтроля 	= РезультатФормирования.ЕстьОшибкиФорматногоКонтроля;
			ЕстьПрочиеОшибки 				= РезультатФормирования.ЕстьПрочиеОшибки;
		КонецЕсли;
		
		УзелЗашифрованнойКвитанции = НайтиУзел(УзелРезультата, "FILE_TICKET_ENC");
		Если УзелЗашифрованнойКвитанции <> Неопределено Тогда
			ДвоичныеДанныеКвитанции = Base64Значение(УзелЗашифрованнойКвитанции.Значение);
		Иначе
			КвитанцияОтсутствует = Истина;
		КонецЕсли;
		
	ИначеЕсли ТипОтправляемогоДокумента <> Перечисления.ТипыОтправляемыхДокументов.Отчет4аФСС
	И ТипОтправляемогоДокумента <> Перечисления.ТипыОтправляемыхДокументов.РеестрСведенийФСС Тогда
		РесурсСервера = ДокументооборотСКО.ПолучитьЗаменяемоеЗначениеТестовогоСервера1С("/fss/statusreport", "{FSS}/fss/statusreport", Соединение) + "?id=" + ИдентификаторОтправкиНаСервере;
		ФайлРезультата = ПолучитьРезультатЗапросаGet(АдресСервера, РесурсСервера);
		
	Иначе
		// если реестр был отправлен без использования API
		РесурсСервера = ДокументооборотСКО.ПолучитьЗаменяемоеЗначениеТестовогоСервера1С("/home/Results", "{FSS}/home/Results", Соединение) + "?id=" + ИдентификаторОтправкиНаСервере;
		ФайлРезультата = ПолучитьРезультатЗапросаGet(АдресСервера, РесурсСервера);
	КонецЕсли;
	
	Если ФайлРезультата <> Неопределено ИЛИ ТипОтправляемогоДокумента = Перечисления.ТипыОтправляемыхДокументов.Отчет4аФСС
		ИЛИ ТипОтправляемогоДокумента = Перечисления.ТипыОтправляемыхДокументов.РеестрСведенийФСС ИЛИ ИспользуетсяВебСервис Тогда
		
		Если ФайлРезультата <> Неопределено Тогда
			// получен протокол обработки
			Попытка
				// извлекаем таблицу обработки из файла результата
				ТЗОбработки = ИзвлечьИзФайлаТаблицуОбработки(ФайлРезультата, ТипОтправляемогоДокумента, КодировкаФайла, ИспользуетсяВебСервис);
			Исключение
				// обрабатываем ошибки, возникшие в случае, если невозможно разобрать ответ от сервера
				ОбъектЧтениеТекста = Новый ЧтениеТекста;
				ЧтениеТекстаОткрытьНаСервере(ОбъектЧтениеТекста, ФайлРезультата);
				ТекстОтвета = ОбъектЧтениеТекста.Прочитать();
				ОбъектЧтениеТекста.Закрыть();
				
				Если (ТипОтправляемогоДокумента = Перечисления.ТипыОтправляемыхДокументов.Отчет4аФСС
				ИЛИ ТипОтправляемогоДокумента = Перечисления.ТипыОтправляемыхДокументов.РеестрСведенийФСС ИЛИ ИспользуетсяВебСервис)
				И (СокрЛП(ТекстОтвета) = "OK;") Тогда
					//ситуация, когда отчет еще не обработан
					ФайлРезультата = Неопределено;
				ИначеЕсли НЕ ИспользуетсяВебСервис Тогда
					
					Если СтрНайти(ВРЕГ(ТекстОтвета), НСтр("ru = 'НЕДОСТУПЕН'")) <> 0 Тогда
						ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(НСтр("ru = 'Сервер ФСС недоступен'"));
					Иначе
						ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(НСтр("ru = 'Не удалось разобрать ответ, полученный от сервера ФСС'"));
					КонецЕсли;
					
					Возврат Результат;
				КонецЕсли;
			КонецПопытки;
		КонецЕсли;
		
		Если ФайлРезультата <> Неопределено Тогда
			// читаем содержимое протокола обработки
			ОбъектЧтение = Новый ЧтениеТекста(ФайлРезультата, КодировкаФайла);
			стрРезультатОбработки = СокрЛП(ОбъектЧтение.Прочитать());
			
			ОбъектЧтение.Закрыть();
		Иначе
			//ситуация, когда отчет еще не обработан
			стрРезультатОбработки = "";
		КонецЕсли;
		
		// анализируем содержимое страницы протокола обработки
		Если (СтрНайти(стрРезультатОбработки, "invoice?id") > 0 И НЕ ИспользуетсяВебСервис)
			ИЛИ ОтчетСдан = Истина ИЛИ ОтчетСданСОшибками = Истина Тогда
			
			// есть ссылка на квитанцию - отчет принят
			Если СтрНайти(стрРезультатОбработки, "invoice?id") > 0 И КвитанцияОтсутствует <> Истина Тогда
				Если ТипОтправляемогоДокумента <> Перечисления.ТипыОтправляемыхДокументов.Отчет4аФСС
				И ТипОтправляемогоДокумента <> Перечисления.ТипыОтправляемыхДокументов.РеестрСведенийФСС
				И НЕ ИспользуетсяВебСервис Тогда
					СсылкаКвитанция = "invoice?id=" + ИдентификаторОтправкиНаСервере;
				Иначе
					НомерОтправкиНаСервере = ТекстПослеПрефикса(стрРезультатОбработки, "invoice?id=", """");
					СсылкаКвитанция = "invoice?id=" + НомерОтправкиНаСервере;
				КонецЕсли;
			КонецЕсли;
			
			Если СтрНайти(стрРезультатОбработки, "invoice?lid") > 0 ИЛИ ОтчетСданСОшибками = Истина Тогда
				// есть ссылка на протокол ошибок логического контроля
				СтатусОтправки = Перечисления.СтатусыОтправки.ПринятЕстьОшибки;
				Если СтрНайти(стрРезультатОбработки, "invoice?lid") > 0 Тогда
					СсылкаПротоколОшибок = "invoice?lid=" + ИдентификаторОтправкиНаСервере;
				КонецЕсли;
				СтатусОтчета = НСтр("ru = 'Сдан, обнаружены ошибки при логическом контроле'");
				ЦветСтатуса = ЦветаСтиля.ЦветОшибкиВПротоколеБРО;
				ЕстьОшибки = Истина;
			Иначе
				СтатусОтправки = Перечисления.СтатусыОтправки.Сдан;
				СтатусОтчета = НСтр("ru = 'Сдан, ошибок не обнаружено.'");
				ЦветСтатуса = ЦветаСтиля.ЦветПоложительногоПротокола;
			КонецЕсли;
			
		ИначеЕсли СтрНайти(стрРезультатОбработки, "invoice?fid") > 0 ИЛИ ЕстьОшибкиФорматногоКонтроля = Истина
			ИЛИ ((ТипОтправляемогоДокумента = Перечисления.ТипыОтправляемыхДокументов.Отчет4аФСС
			ИЛИ ТипОтправляемогоДокумента = Перечисления.ТипыОтправляемыхДокументов.РеестрСведенийФСС ИЛИ ИспользуетсяВебСервис)
			И (ТЗОбработки.Количество() = 0) И (стрРезультатОбработки <> "OK;") И (стрРезультатОбработки <> "")) Тогда
			
			// есть ссылка на протокол ошибок форматного контроля - отчет не принят
			СтатусОтправки = Перечисления.СтатусыОтправки.НеПринят;
			Если СтрНайти(стрРезультатОбработки, "invoice?fid") > 0 Тогда
				СсылкаПротоколОшибок = "invoice?fid=" + ИдентификаторОтправкиНаСервере;
			КонецЕсли;
			СтатусОтчета = НСтр("ru = 'Не принят, обнаружены ошибки при форматном контроле'");
			ЦветСтатуса = ЦветаСтиля.ЦветОшибкиВПротоколеБРО;
			ЕстьОшибки = Истина;
		ИначеЕсли ЕстьОшибкиСтатуса(ТЗОбработки) ИЛИ ЕстьПрочиеОшибки = Истина Тогда
			СтатусОтправки = Перечисления.СтатусыОтправки.НеПринят;
			СтатусОтчета = НСтр("ru = 'Не принят, обнаружены ошибки'");
			ЦветСтатуса = ЦветаСтиля.ЦветОшибкиВПротоколеБРО;
			ЕстьОшибки = Истина;
		Иначе
			//отчет еще не обработан
			СтатусОтчета = НСтр("ru = 'Находится в обработке, попробуйте через некоторое время использовать в отчете кнопку ""Обновить""'");
		КонецЕсли;
		
		ПротоколОшибок = "";
		ПротоколОшибокЭтоHTML = Ложь;
		ПротоколОшибокВозвращенВебСервисом = (ЗначениеЗаполнено(ТекстПоследнейОшибкиВебСервиса)
			И (ЕстьОшибкиФорматногоКонтроля = Истина ИЛИ ОтчетСданСОшибками = Истина)
			И ТипОтправляемогоДокумента <> Перечисления.ТипыОтправляемыхДокументов.Отчет4аФСС
			И ТипОтправляемогоДокумента <> Перечисления.ТипыОтправляемыхДокументов.РеестрСведенийФСС);
		
		Если СсылкаПротоколОшибок <> "" ИЛИ ПротоколОшибокВозвращенВебСервисом
			ИЛИ ((ТипОтправляемогоДокумента = Перечисления.ТипыОтправляемыхДокументов.Отчет4аФСС
			ИЛИ ТипОтправляемогоДокумента = Перечисления.ТипыОтправляемыхДокументов.РеестрСведенийФСС
			ИЛИ (ИспользуетсяВебСервис И ТЗОбработки.Количество() = 0))
			И (ФайлРезультата <> Неопределено)) Тогда
			
			Если ПротоколОшибокВозвращенВебСервисом Тогда
				ПротоколОшибок = ТекстПоследнейОшибкиВебСервиса;
				
			Иначе
				Если СсылкаПротоколОшибок <> "" Тогда
					ФайлРезультата = ПолучитьРезультатЗапросаGet(АдресСервера, СсылкаПротоколОшибок);
					КодировкаФайла = "utf-8";
				КонецЕсли;
				// считаем содержимое протокола ошибок
				ОбъектЧтение = НовыйЧтениеТекстаНаСервере(ФайлРезультата);
				ПротоколОшибок = СокрЛП(ОбъектЧтение.Прочитать());
			КонецЕсли;
			
			ПротоколОшибокЭтоHTML = СтрНайти(ВРЕГ(ПротоколОшибок), "HTML") > 0
				И ((СсылкаПротоколОшибок <> "" И НЕ ИспользуетсяВебСервис)
				ИЛИ ТипОтправляемогоДокумента = Перечисления.ТипыОтправляемыхДокументов.Отчет4аФСС
				ИЛИ ТипОтправляемогоДокумента = Перечисления.ТипыОтправляемыхДокументов.РеестрСведенийФСС
				ИЛИ (ИспользуетсяВебСервис И ТЗОбработки.Количество() = 0));
			
			Если ПротоколОшибокЭтоHTML И НЕ ПротоколОшибокВозвращенВебСервисом Тогда
				ОбъектЧтение = Новый ЧтениеТекста(ФайлРезультата, КодировкаФайла);
				ПротоколОшибок = СокрЛП(ОбъектЧтение.Прочитать());
			КонецЕсли;
		КонецЕсли;
		
		Если ПротоколОшибокЭтоHTML Тогда
			Протокол = ПротоколОшибок;
			
			Если ТипОтправляемогоДокумента = Перечисления.ТипыОтправляемыхДокументов.Отчет4аФСС
			ИЛИ ТипОтправляемогоДокумента = Перечисления.ТипыОтправляемыхДокументов.РеестрСведенийФСС
			ИЛИ ИспользуетсяВебСервис Тогда
				Протокол = ПротоколHTMLБезКартинокИСсылок(Протокол);
			КонецЕсли;
			
		Иначе
			Протокол = ГенерироватьHTMLПротоколаОбработкиФСС(ИдентификаторОтправкиНаСервере, ТЗОбработки, ПротоколОшибок, СтатусОтчета, ЦветСтатуса, ОтправкаСсылка, ЕстьОшибки);
		КонецЕсли;
		
		//заполняем результирующую структуру
		ДатаСеанса = ТекущаяДатаСеанса();
		Результат = Новый Структура;
		Результат.Вставить("ДатаПолученияРезультата", 	ДатаСеанса);
		Результат.Вставить("СтатусОтправки", 			СтатусОтправки);
		Результат.Вставить("Протокол", 					Протокол);
		Результат.Вставить("Квитанция", 				Неопределено);
		
		Если СсылкаКвитанция <> "" ИЛИ ДвоичныеДанныеКвитанции <> Неопределено Тогда
			//по полученной ссылке скачаем квитанцию
			Если ДвоичныеДанныеКвитанции = Неопределено Тогда
				ФайлРезультата = ПолучитьРезультатЗапросаGet(АдресСервера, СсылкаКвитанция);
				ДвоичныеДанныеКвитанции = Новый ДвоичныеДанные(ФайлРезультата);
			КонецЕсли;
			Результат.Квитанция = ДвоичныеДанныеКвитанции;
		КонецЕсли;
		
	КонецЕсли;
	
	Возврат Результат;
	
КонецФункции

Функция СохранитьРезультатОтправкиФСС(ОправкаСсылка, РезультатОтправки)
	
	Если РезультатОтправки <> Неопределено И РезультатОтправки.Свойство("Протокол") Тогда
		ОтправкаОбъект = ОправкаСсылка.ПолучитьОбъект();
		ОтправкаОбъект.ДатаПолученияРезультата = РезультатОтправки.ДатаПолученияРезультата;
		ОтправкаОбъект.СтатусОтправки = РезультатОтправки.СтатусОтправки;
		ОтправкаОбъект.Протокол = Новый ХранилищеЗначения(РезультатОтправки.Протокол);
		Если РезультатОтправки.Квитанция <> Неопределено Тогда
			ОтправкаОбъект.Квитанция = Новый ХранилищеЗначения(РезультатОтправки.Квитанция);
		КонецЕсли;
		Если РезультатОтправки.Свойство("ПакетКвитанции") И РезультатОтправки.ПакетКвитанции <> Неопределено Тогда
			ОтправкаОбъект.ПакетКвитанции = Новый ХранилищеЗначения(РезультатОтправки.ПакетКвитанции);
		КонецЕсли;
		
		ОтправкаОбъект.Записать();
		Возврат Истина;
	Иначе
		Возврат Ложь;
	КонецЕсли;
	
КонецФункции

Функция ПолучитьРезультатЗапросаGet(АдресСервера, РесурсСервера)

	// устанавливаем соединение с сервером
	Соединение = ДокументооборотСКО.УстановитьСоединениеССерверомИнтернета(АдресСервера);
	Если Соединение = Неопределено Тогда
		Возврат Неопределено;
	КонецЕсли;
	
	// посылаем запрос
	ФайлРезультата = ПолучитьИмяВременногоФайла();
	Попытка
		Соединение.Получить(РесурсСервера, ФайлРезультата);
		Соединение = Неопределено;
	Исключение
		Возврат Неопределено;
	КонецПопытки;
	
	Возврат ФайлРезультата;
	
КонецФункции

Функция ГенерироватьHTMLПротоколаОбработкиФСС(ИдентификаторОтчета, ТЗОбработки, ПротоколОшибок, СтатусОтчета, ЦветСтатуса, ОтправкаСсылка, ЕстьОшибки)
	
	ТабДок = Новый ТабличныйДокумент;
	
	Шаблон = ПолучитьМакет("ФССШаблонДляHTML");
	
	СекцияШапка = Шаблон.ПолучитьОбласть("Шапка");
	
	СвойстваОрганизации 			= РегламентированнаяОтчетностьВызовСервера.ПолучитьСведенияОбОрганизации(ОтправкаСсылка.Организация, ,"НаимЮЛПол, ИННЮЛ");
	ПолноеНаименованиеОрганизации 	= СвойстваОрганизации.НаимЮЛПол;
	ИННОрганизации 					= СвойстваОрганизации.ИННЮЛ;
	
	// Наименование протокола 
	НаименованиеПротокола = "";
	Если ЕстьОшибки Тогда
		НаименованиеПротокола = НСтр("ru = 'Протокол ошибок'");
	Иначе
		НаименованиеПротокола = НСтр("ru = 'Протокол о сдаче'");
	КонецЕсли;
	СекцияШапка.Параметры.НаименованиеПротокола = НаименованиеПротокола;
	
	СведенияПоОбъекту 	= СведенияПоОтправляемымОбъектам(ОтправкаСсылка.ОтчетСсылка);
	НаименованиеОргана 	= СведенияПоОбъекту.ПредставлениеКонтролирующегоОргана;
	НаименованиеОтчета 	= СведенияПоОбъекту.Наименование;
	
	СекцияШапка.Параметры.Организация 	= ПолноеНаименованиеОрганизации;
	СекцияШапка.Параметры.ИНН 			= НСтр("ru = 'ИНН '") + ИННОрганизации;
	СекцияШапка.Параметры.Получатель 	= НаименованиеОргана;
	СекцияШапка.Параметры.Отчет 		= НаименованиеОтчета;
	СекцияШапка.Параметры.СтатусОтчета 	= СтатусОтчета;
	СекцияШапка.Параметры.Файл 			= ИдентификаторОтчета;
	СекцияШапка.Области.СтатусОтчета.ЦветТекста = ЦветСтатуса;
	
	ТабДок.Вывести(СекцияШапка);
	
	ПротоколОшибокОтображаемый = СокрЛП(ПротоколОшибок);
	
	СекцияСтрока = Шаблон.ПолучитьОбласть("Строка");
	Для каждого СтрокаТЗ Из ТЗОбработки Цикл
		ОписаниеОшибкиИзТаблицы = СокрЛП(СтрокаТЗ.ОписаниеОшибки);
		Если ЗначениеЗаполнено(ОписаниеОшибкиИзТаблицы) И СтрДлина(ПротоколОшибокОтображаемый) >= СтрДлина(ОписаниеОшибкиИзТаблицы)
			И СтрДлина(ПротоколОшибокОтображаемый) <= СтрДлина(ОписаниеОшибкиИзТаблицы) + 20
			И Прав(ВРег(ПротоколОшибокОтображаемый), СтрДлина(ОписаниеОшибкиИзТаблицы)) = ВРег(ОписаниеОшибкиИзТаблицы) Тогда
			ПротоколОшибокОтображаемый = "";
		КонецЕсли;
		
		СекцияСтрока.Параметры.Заполнить(СтрокаТЗ);
		ТабДок.Вывести(СекцияСтрока);
	КонецЦикла;
	
	СекцияПодвал = Шаблон.ПолучитьОбласть("Подвал");
	ТабДок.Вывести(СекцияПодвал);
	
	Если ПротоколОшибокОтображаемый <> "" Тогда
		СекцияПротоколОшибок = Шаблон.ПолучитьОбласть("ПротоколОшибок");
		СекцияПротоколОшибок.Параметры.ПротоколОшибок = ПротоколОшибокОтображаемый;
		ТабДок.Вывести(СекцияПротоколОшибок);
	КонецЕсли;
	
	ИмяТемпФайла = ПолучитьИмяВременногоФайла();
	ТабДок.Записать(ИмяТемпФайла, ТипФайлаТабличногоДокумента.HTML);
	ТекстHTML = Новый ТекстовыйДокумент;
	ТекстHTML.Прочитать(ИмяТемпФайла);
	ТекстHTML = ТекстHTML.ПолучитьТекст();
	
	Возврат ТекстHTML;
	
КонецФункции

Функция ЕстьОшибкиСтатуса(ТЗОбработки)
	
	Для каждого СтрокаТЗ Из ТЗОбработки Цикл
		Если СтрокаТЗ.Статус = "Ошибка" Тогда
			Возврат Истина;
		КонецЕсли;
	КонецЦикла;
	
	Возврат Ложь;
	
КонецФункции

Функция ИзвлечьИзФайлаТаблицуОбработки(ФайлОтвета, ТипОтправляемогоДокумента = Неопределено, КодировкаФайла = "utf-8", ИспользуетсяВебСервис = Ложь)
	
	ТЗОбработки = Новый ТаблицаЗначений;
	ТЗОбработки.Колонки.Добавить("СтадияОбработки");
	ТЗОбработки.Колонки.Добавить("Статус");
	ТЗОбработки.Колонки.Добавить("Дата");
	ТЗОбработки.Колонки.Добавить("КодОшибки");
	ТЗОбработки.Колонки.Добавить("ОписаниеОшибки");
	ТЗОбработки.Колонки.Добавить("Действие");
	
	ЧтениеHTML = Новый ЧтениеHTML;
	ЧтениеHTML.ОткрытьФайл(ФайлОтвета, КодировкаФайла);
	ПостроительDOM = Новый ПостроительDOM;
	ДокументDOM = ПостроительDOM.Прочитать(ЧтениеHTML);
	
	ЭлементыTABLE = ДокументDOM.ПолучитьЭлементыПоИмени("TABLE");
	
	СтрокиТаблицыHTML = ЭлементыTABLE[?(ТипОтправляемогоДокумента <> Перечисления.ТипыОтправляемыхДокументов.Отчет4аФСС
		И ТипОтправляемогоДокумента <> Перечисления.ТипыОтправляемыхДокументов.РеестрСведенийФСС И НЕ ИспользуетсяВебСервис, 2, 0)].ДочерниеУзлы;
	
	//перебираем все строки из таблицы обработки, первая строка таблицы (индекс 0)- заголовки - не используем
	КолвоСтрок = СтрокиТаблицыHTML.Количество();
	Для НомСтроки = 1 По (КолвоСтрок - 1) Цикл
		СтрокаТаблицыHTML = СтрокиТаблицыHTML[НомСтроки];
		НоваяСтрокаТЗ = ТЗОбработки.Добавить();
		//перебираем ячейки строки таблицы (выделяя их из общего списка узлов по типу), заполняем ТЗ
		УзлыСтрокиHTML = СтрокаТаблицыHTML.ДочерниеУзлы;
		НомЯчейки = 0;
		Для каждого УзелСтрокиHTML Из УзлыСтрокиHTML Цикл
			Если УзелСтрокиHTML.ТипУзла = ТипУзлаDOM.Элемент Тогда
				НоваяСтрокаТЗ[НомЯчейки] = СокрЛП(УзелСтрокиHTML.ТекстовоеСодержимое);
				НомЯчейки = НомЯчейки + 1;
			КонецЕсли;
		КонецЦикла;
	КонецЦикла;
	
	Возврат ТЗОбработки;
	
КонецФункции

Функция ПротоколHTMLБезКартинокИСсылок(ПротоколHTML)
	
	Результат = ПротоколHTML;
	
	Для НомерУдаляемогоТэга = 1 По 2 Цикл
		
		Если НомерУдаляемогоТэга = 1 Тогда
			НачалоТэга = "<IMG ";
			ЗавершениеТэга = ">";
		ИначеЕсли НомерУдаляемогоТэга = 2 Тогда
			НачалоТэга = "<A ";
			ЗавершениеТэга = "</A>";
		КонецЕсли;
		
		Пока Истина Цикл
			
			ПозицияТэга = СтрНайти(ВРег(Результат), НачалоТэга);
			Если ПозицияТэга = 0 Тогда
				Прервать;
			КонецЕсли;
			
			ПротоколДоТэга = Лев(Результат, ПозицияТэга - 1);
			ПротоколСТэга = Сред(Результат, ПозицияТэга);
			
			ПозицияЗавершенияТэга = СтрНайти(ВРег(ПротоколСТэга), ЗавершениеТэга);
			ПротоколПослеТэга = "";
			Если ПозицияЗавершенияТэга > 0 Тогда
				ПротоколПослеТэга = Сред(ПротоколСТэга, ПозицияЗавершенияТэга + СтрДлина(ЗавершениеТэга));
			КонецЕсли;
			
			Результат = ПротоколДоТэга + ПротоколПослеТэга;
			
		КонецЦикла;
		
	КонецЦикла;
	
	Возврат Результат;
	
КонецФункции

Функция СформироватьТаблицуОбработки(
	ПолноеСостояние,
	ТекстСостояния,
	ДатаОбновления,
	ТекстОшибки,
	ТолькоПроанализироватьСостояния = Ложь,
	ТипОтправляемогоДокумента = Неопределено)
	
	СостояниеПолучен = 0;
	СостояниеОшибкаПолучения = 10;
	СостояниеЭППроверена = 1;
	СостояниеОшибкаПроверкиЭП = 11;
	СостояниеПереданоНаРассмотрение = 2;
	СостояниеФЛКНеПройден = 21;
	
	Состояние = ПолноеСостояние;
	ОтчетСданСОшибками = Ложь;
	
	Если ТипОтправляемогоДокумента <> Перечисления.ТипыОтправляемыхДокументов.Отчет4аФСС
		И ТипОтправляемогоДокумента <> Перечисления.ТипыОтправляемыхДокументов.РеестрСведенийФСС Тогда
		
		Если ПолноеСостояние = -1 Тогда
			Состояние = СостояниеПолучен;
		ИначеЕсли ПолноеСостояние = -11 Тогда
			Состояние = СостояниеОшибкаПолучения;
		ИначеЕсли ПолноеСостояние = 1 Тогда
			Состояние = СостояниеЭППроверена;
		ИначеЕсли ПолноеСостояние = 11 Тогда
			Состояние = СостояниеОшибкаПроверкиЭП;
		ИначеЕсли ПолноеСостояние = 2 Тогда
			Состояние = СостояниеЭППроверена;
		ИначеЕсли ПолноеСостояние = 21 Тогда
			Состояние = СостояниеФЛКНеПройден;
		ИначеЕсли ПолноеСостояние = 3 Тогда
			Состояние = СостояниеЭППроверена;
		ИначеЕсли ПолноеСостояние = 31 Тогда
			Состояние = СостояниеПереданоНаРассмотрение;
			ОтчетСданСОшибками = Истина;
		ИначеЕсли ПолноеСостояние = 4 Тогда
			Состояние = СостояниеПереданоНаРассмотрение;
		ИначеЕсли ПолноеСостояние = 41 Тогда
			Состояние = СостояниеПереданоНаРассмотрение;
			ОтчетСданСОшибками = Истина;
		КонецЕсли;
		
	КонецЕсли;
	
	ЭтоНеизвестноеСостояние = (Состояние <> СостояниеПолучен И Состояние <> СостояниеОшибкаПолучения И Состояние <> СостояниеЭППроверена
		И Состояние <> СостояниеОшибкаПроверкиЭП И Состояние <> СостояниеПереданоНаРассмотрение И Состояние <> СостояниеФЛКНеПройден);
	
	ТЗОбработки = Неопределено;
	Если НЕ ТолькоПроанализироватьСостояния Тогда
		
		ТЗОбработки = Новый ТаблицаЗначений;
		ТЗОбработки.Колонки.Добавить("СтадияОбработки");
		ТЗОбработки.Колонки.Добавить("Статус");
		ТЗОбработки.Колонки.Добавить("Дата");
		ТЗОбработки.Колонки.Добавить("КодОшибки");
		ТЗОбработки.Колонки.Добавить("ОписаниеОшибки");
		ТЗОбработки.Колонки.Добавить("Действие");
		
		НоваяСтрокаТЗ = ТЗОбработки.Добавить();
		НоваяСтрокаТЗ.СтадияОбработки = ?(НЕ ЭтоНеизвестноеСостояние, НСтр("ru = '1. Получение файла'"), ТекстСостояния);
		НоваяСтрокаТЗ.Статус = ?(НЕ ЭтоНеизвестноеСостояние, ?(Состояние <> СостояниеОшибкаПолучения, НСтр("ru = 'Успешно'"), НСтр("ru = 'Ошибка'")), "");
		НоваяСтрокаТЗ.Дата = ?(ЗначениеЗаполнено(ДатаОбновления), Формат(ДатаОбновления, "ДФ='дд.ММ.гггг ЧЧ:мм:сс'"), "");
		НоваяСтрокаТЗ.КодОшибки = "";
		НоваяСтрокаТЗ.ОписаниеОшибки = ?(ЭтоНеизвестноеСостояние ИЛИ Состояние = СостояниеПолучен ИЛИ Состояние = СостояниеОшибкаПолучения, ТекстОшибки, "");
		НоваяСтрокаТЗ.Действие = "";
		
		Если Состояние = СостояниеЭППроверена ИЛИ Состояние = СостояниеОшибкаПроверкиЭП ИЛИ Состояние = СостояниеПереданоНаРассмотрение
			ИЛИ Состояние = СостояниеФЛКНеПройден Тогда
			
			НоваяСтрокаТЗ = ТЗОбработки.Добавить();
			НоваяСтрокаТЗ.СтадияОбработки = НСтр("ru = '2. Расшифровка файла и проверка ЭП'");
			НоваяСтрокаТЗ.Статус = ?(Состояние <> СостояниеОшибкаПроверкиЭП, НСтр("ru = 'Успешно'"), НСтр("ru = 'Ошибка'"));
			НоваяСтрокаТЗ.Дата = ?(ЗначениеЗаполнено(ДатаОбновления), Формат(ДатаОбновления, "ДФ='дд.ММ.гггг ЧЧ:мм:сс'"), "");
			НоваяСтрокаТЗ.КодОшибки = "";
			НоваяСтрокаТЗ.ОписаниеОшибки = ?(Состояние = СостояниеЭППроверена ИЛИ Состояние = СостояниеОшибкаПроверкиЭП, ТекстОшибки, "");
			НоваяСтрокаТЗ.Действие = "";
			
			Если Состояние = СостояниеПереданоНаРассмотрение ИЛИ Состояние = СостояниеФЛКНеПройден Тогда
				НоваяСтрокаТЗ = ТЗОбработки.Добавить();
				НоваяСтрокаТЗ.СтадияОбработки = НСтр("ru = '3. Форматно-логический контроль'");
				НоваяСтрокаТЗ.Статус = ?(Состояние <> СостояниеФЛКНеПройден, НСтр("ru = 'Успешно'"), НСтр("ru = 'Ошибка'"));
				НоваяСтрокаТЗ.Дата = ?(ЗначениеЗаполнено(ДатаОбновления), Формат(ДатаОбновления, "ДФ='дд.ММ.гггг ЧЧ:мм:сс'"), "");
				НоваяСтрокаТЗ.КодОшибки = "";
				НоваяСтрокаТЗ.ОписаниеОшибки = ТекстОшибки;
				НоваяСтрокаТЗ.Действие = "";
			КонецЕсли;
		КонецЕсли;
		
	КонецЕсли;
	
	ОтчетСдан = (Состояние = СостояниеПереданоНаРассмотрение);
	ЕстьОшибкиФорматногоКонтроля = (Состояние = СостояниеФЛКНеПройден);
	ЕстьПрочиеОшибки = (Состояние = СостояниеОшибкаПолучения ИЛИ Состояние = СостояниеОшибкаПроверкиЭП
		ИЛИ (ЭтоНеизвестноеСостояние И ЗначениеЗаполнено(ТекстОшибки)));
	
	Результат = Новый Структура;
	Результат.Вставить("ТЗОбработки", 					ТЗОбработки);
	Результат.Вставить("ОтчетСдан", 					ОтчетСдан);
	Результат.Вставить("ОтчетСданСОшибками", 			ОтчетСданСОшибками);
	Результат.Вставить("ЕстьОшибкиФорматногоКонтроля", 	ЕстьОшибкиФорматногоКонтроля);
	Результат.Вставить("ЕстьПрочиеОшибки", 				ЕстьПрочиеОшибки);
	Возврат Результат;
	
КонецФункции

Функция ОтправкаНаСерверФСС(
		ЭтоЭлектроннаяПодписьВМоделиСервиса,
		АдресСервера,
		КороткоеИмяФайлаПакета,
		СтрокаBase64ФайлаПакета,
		ИмяФайлаОтправки,
		ПовторятьСоединение,
		НастройкиОтправки)
	
	ТипОтправляемогоДокумента 	= НастройкиОтправки.ТипОтправляемогоДокумента;
	ВидОтправляемогоДокумента 	= НастройкиОтправки.ВидОтправляемогоДокумента;
	
	Идентификатор = Неопределено;
	ОписаниеОшибкиСервером = Неопределено;
	ОтветПолучен = ОтправкаЗашифрованногоПакетаНаСерверФСС(
		АдресСервера,
		ИмяФайлаОтправки,
		ТипОтправляемогоДокумента,
		ВидОтправляемогоДокумента,
		КороткоеИмяФайлаПакета,
		Идентификатор,
		ОписаниеОшибкиСервером);
	
	Если ОтветПолучен Тогда
		ПовторятьСоединение = Ложь;
		
		ЭтоДанныеЭЛН = (ТипОтправляемогоДокумента = Перечисления.ТипыОтправляемыхДокументов.ДанныеЭЛНФСС);
		ЭтоПодтверждениеВидаДеятельности =
			(ТипОтправляемогоДокумента = Перечисления.ТипыОтправляемыхДокументов.ПрочаяОтчетностьФСС
			И ВидОтправляемогоДокумента = Справочники.ВидыОтправляемыхДокументов.ПодтверждениеВидаДеятельности);
		ЭтоРеестрСтимулирующихВыплатМедицинскимИСоциальнымРаботникам =
			(ТипОтправляемогоДокумента = Перечисления.ТипыОтправляемыхДокументов.ПрочаяОтчетностьФСС
			И ВидОтправляемогоДокумента =
			Справочники.ВидыОтправляемыхДокументов.РеестрСтимулирующихВыплатМедицинскимИСоциальнымРаботникам);
		ЭтоПодписьИШифрованиеSOAP = ЭтоДанныеЭЛН ИЛИ ЭтоПодтверждениеВидаДеятельности
			ИЛИ ЭтоРеестрСтимулирующихВыплатМедицинскимИСоциальнымРаботникам;
		
		Если Идентификатор <> Неопределено И ЭтоПодписьИШифрованиеSOAP Тогда
			ТекстРезультата = Идентификатор;
			Возврат ПоместитьВоВременноеХранилище(ТекстРезультата, Новый УникальныйИдентификатор);
		КонецЕсли;
		
		Если Идентификатор <> Неопределено Тогда
			//отправленный файл принят сервером, отправке присвоен идентификатор
			Возврат ДобавитьОтправкуФСС(
				ЭтоЭлектроннаяПодписьВМоделиСервиса,
				КороткоеИмяФайлаПакета,
				СтрокаBase64ФайлаПакета,
				Идентификатор,
				НастройкиОтправки);
			
		Иначе
			Если ОписаниеОшибкиСервером <> Неопределено Тогда
				//отправленный файл не был принят сервером, получено описание ошибки
				Если СтрНайти(ОписаниеОшибкиСервером, "зашифровано на ключе отличном от ключа уполномоченного лица") > 0 Тогда
					ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(НСтр("ru = 'Ошибка расшифровки отчета на сервере ФСС: выбран некорректный сертификата ФСС ЭЛН, ПОВЭД, СЭДО в настройках обмена с ФСС.'"));
					
					КонтекстОснования = Новый ФиксированнаяСтруктура("АдресСсылки", "НастройкиОбменаФСС");
					ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(
						НСтр("ru = 'Перейти в форму настроек обмена с ФСС.'"),,,,,
						НовыйКонтекстОбработки(КонтекстОснования));
					
				Иначе
					ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(
						НСтр("ru = 'Отчет не принят сервером.'") + " " + ОписаниеОшибкиСервером);
				КонецЕсли;
				
			Иначе
				// в ответе сервера отсутствует как идентификатор, так и описание ошибки
				ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(НСтр("ru = 'Зашифрованный пакет не удалось отправить на сервер ФСС.'"));
			КонецЕсли;
			Возврат Неопределено;
		КонецЕсли;
		
	Иначе
		ПовторятьСоединение = Истина;
		Возврат Неопределено;
	КонецЕсли;
	
КонецФункции

Функция ДобавитьОтправкуФСС(
		ЭтоЭлектроннаяПодписьВМоделиСервиса,
		КороткоеИмяФайлаПакета,
		СтрокаBase64ФайлаПакета,
		Идентификатор,
		НастройкиОтправки)
	
	НастройкиВызова = Новый Структура;
	НастройкиВызова.Вставить("ОтчетСсылка", 			Неопределено);
	НастройкиВызова.Вставить("ПоляДляЗаписиОтправки", 	Неопределено);
	НастройкиВызова.Вставить("СтатусОтправки", 			Перечисления.СтатусыОтправки.Отправлен);
	НастройкиВызова.Вставить("Протокол", 				Неопределено);
	НастройкиВызова.Вставить("Квитанция", 				Неопределено);
	НастройкиВызова.Вставить("ПодписанныйПакет", 		Неопределено);
	НастройкиВызова.Вставить("ПакетКвитанции", 			Неопределено);
	Если НастройкиОтправки <> Неопределено Тогда
		ЗаполнитьЗначенияСвойств(НастройкиВызова, НастройкиОтправки);
	КонецЕсли;
	ОтчетСсылка 			= НастройкиВызова.ОтчетСсылка;
	ПоляДляЗаписиОтправки 	= НастройкиВызова.ПоляДляЗаписиОтправки;
	
	ДатаСеанса = ТекущаяДатаСеанса();
	
	ТипЗнчСсылкаНаОтчет = ТипЗнч(ОтчетСсылка);
	
	ИменаРеквизитовОтчета = "Организация"
		+ ?(ТипЗнчСсылкаНаОтчет = Тип("ДокументСсылка.РегламентированныйОтчет"), ", ИсточникОтчета", "")
		+ ?(ТипЗнчСсылкаНаОтчет = Тип("СправочникСсылка.ЭлектронныеПредставленияРегламентированныхОтчетов"),
		", ВидОтчета", "");
	РеквизитыОтчета = ОбщегоНазначения.ЗначенияРеквизитовОбъекта(ОтчетСсылка, ИменаРеквизитовОтчета);
	
	ЭтоРеестрСведений = ДокументооборотСФССКлиентСервер.ЭтоРеестрСведенийНаВыплатуПособийФСС(ОтчетСсылка);
	ОбъектПоУмолчаниюРеестрЭЛНСуществует =
		Метаданные.Документы.Найти("РеестрДанныхЭЛНЗаполняемыхРаботодателем") <> Неопределено;
	ЭтоРеестрЭЛН = ДокументооборотСФССКлиентСервер.ЭтоРеестрДанныхЭЛНЗаполняемыхРаботодателем(ОтчетСсылка,
		ОбъектПоУмолчаниюРеестрЭЛНСуществует);
	ОбъектПоУмолчаниюРеестрСтимулирующихВыплатМедицинскимИСоциальнымРаботникамСуществует =
		Метаданные.Документы.Найти("РеестрСтимулирующихВыплатМедицинскимИСоциальнымРаботникам") <> Неопределено;
	ЭтоРеестрСтимулирующихВыплатМедицинскимИСоциальнымРаботникам =
		ДокументооборотСФССКлиентСервер.ЭтоРеестрСтимулирующихВыплатМедицинскимИСоциальнымРаботникам(ОтчетСсылка,
		ОбъектПоУмолчаниюРеестрСтимулирующихВыплатМедицинскимИСоциальнымРаботникамСуществует);
	
	ВидОтчета = Неопределено;
	Если ТипЗнчСсылкаНаОтчет = Тип("ДокументСсылка.РегламентированныйОтчет") Тогда
		// ОбъектФСС - рег отчет
		ВидОтчета = Справочники.ВидыОтправляемыхДокументов.НайтиПоРеквизиту("Источник", РеквизитыОтчета.ИсточникОтчета);
	ИначеЕсли ЭтоРеестрСведений Тогда
		ВидОтчета = Справочники.ВидыОтправляемыхДокументов.РеестрСведенийВФСС;
	ИначеЕсли ЭтоРеестрЭЛН Тогда
		ВидОтчета = Справочники.ВидыОтправляемыхДокументов.РеестрДанныхЭЛНЗаполняемыхРаботодателем;
	ИначеЕсли ЭтоРеестрСтимулирующихВыплатМедицинскимИСоциальнымРаботникам Тогда
		ВидОтчета = Справочники.ВидыОтправляемыхДокументов.РеестрСтимулирующихВыплатМедицинскимИСоциальнымРаботникам;
	ИначеЕсли ТипЗнчСсылкаНаОтчет = Тип("СправочникСсылка.ЭлектронныеПредставленияРегламентированныхОтчетов") Тогда
		ВидОтчета = РеквизитыОтчета.ВидОтчета;
	КонецЕсли;
	
	Если ВидОтчета = Неопределено Тогда
		ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(НСтр("ru = 'Не удалось определить тип документа!'"));
		Возврат Неопределено;
	КонецЕсли;
	
	НоваяЗапись = Справочники.ОтправкиФСС.СоздатьЭлемент();
	НоваяЗапись.ОтчетСсылка = ОтчетСсылка;
	НоваяЗапись.ИдентификаторОтправкиНаСервере = Идентификатор;
	
	Если ЭтоЭлектроннаяПодписьВМоделиСервиса Тогда
		ДвоичныеДанныеФайлаПакета = ПолучитьИзВременногоХранилища(СтрокаBase64ФайлаПакета);
	Иначе
		ДвоичныеДанныеФайлаПакета = Base64Значение(СтрокаBase64ФайлаПакета);
	КонецЕсли;
	
	НоваяЗапись.ЗашифрованныйПакет = Новый ХранилищеЗначения(ДвоичныеДанныеФайлаПакета);
	НоваяЗапись.ИмяФайлаПакета = КороткоеИмяФайлаПакета;
	НоваяЗапись.СтатусОтправки = НастройкиВызова.СтатусОтправки;
	НоваяЗапись.ДатаОтправки = ДатаСеанса;
	НоваяЗапись.Организация = РеквизитыОтчета.Организация;
	НоваяЗапись.КодОрганаФСС = ПоляДляЗаписиОтправки.КодОрганаФСС;
	НоваяЗапись.ДатаНачалаПериода = ПоляДляЗаписиОтправки.ДатаНачалаПериода;
	НоваяЗапись.ДатаОкончанияПериода = ПоляДляЗаписиОтправки.ДатаОкончанияПериода;
	НоваяЗапись.Версия = ПоляДляЗаписиОтправки.Версия;
	НоваяЗапись.ПредставлениеПериода = ПредставлениеПериода(НоваяЗапись.ДатаНачалаПериода,
		КонецДня(НоваяЗапись.ДатаОкончанияПериода), "ФП=Истина");
	НоваяЗапись.ПредставлениеВидаДокумента = РегламентированнаяОтчетность.ПредставлениеВидаДокумента(НоваяЗапись.Версия);
	НоваяЗапись.ВидОтчета = ВидОтчета;
	
	Если НастройкиВызова.Протокол <> Неопределено Тогда
		НоваяЗапись.ДатаПолученияРезультата = ДатаСеанса;
		НоваяЗапись.Протокол = Новый ХранилищеЗначения(НастройкиВызова.Протокол);
	КонецЕсли;
	
	Если НастройкиВызова.Квитанция <> Неопределено Тогда
		НоваяЗапись.Квитанция = Новый ХранилищеЗначения(НастройкиВызова.Квитанция);
	КонецЕсли;
	
	Если НастройкиВызова.ПодписанныйПакет <> Неопределено Тогда
		Если ЭтоЭлектроннаяПодписьВМоделиСервиса Тогда
			ДвоичныеДанныеПодписанногоПакета = ПолучитьИзВременногоХранилища(НастройкиВызова.ПодписанныйПакет);
		Иначе
			ДвоичныеДанныеПодписанногоПакета = Base64Значение(НастройкиВызова.ПодписанныйПакет);
		КонецЕсли;
		НоваяЗапись.ПодписанныйПакет = Новый ХранилищеЗначения(ДвоичныеДанныеПодписанногоПакета);
	КонецЕсли;
	Если НастройкиВызова.ПакетКвитанции <> Неопределено Тогда
		НоваяЗапись.ПакетКвитанции = Новый ХранилищеЗначения(НастройкиВызова.ПакетКвитанции);
	КонецЕсли;
	
	НоваяЗапись.Записать();
	
	Возврат НоваяЗапись.Ссылка;
	
КонецФункции

Функция ОтправитьВФССССервера(
		ЭтоЭлектроннаяПодписьВМоделиСервиса,
		КороткоеИмяФайлаПакета,
		СтрокаBase64ФайлаОтправки,
		СтрокаBase64ФайлаПакета,
		ОтчетСсылка,
		ПовторятьСоединение,
		ПараметрыОтправкиНаСервере) Экспорт
	
	ВременныйКаталог = КаталогВременныхФайлов();
	РазделительПутиОС = ПолучитьРазделительПути();
	Если Прав(ВременныйКаталог, 1) <> РазделительПутиОС Тогда
		ВременныйКаталог = ВременныйКаталог + РазделительПутиОС;
	КонецЕсли;
	
	ИмяФайлаОтправки = ВременныйКаталог + КороткоеИмяФайлаПакета;
	
	Если ЭтоЭлектроннаяПодписьВМоделиСервиса Тогда
		ДвоичныеДанныеФайлаОтправки = ПолучитьИзВременногоХранилища(СтрокаBase64ФайлаОтправки);
	Иначе
		ДвоичныеДанныеФайлаОтправки = Base64Значение(СтрокаBase64ФайлаОтправки);
	КонецЕсли;
	ДвоичныеДанныеФайлаОтправки.Записать(ИмяФайлаОтправки);
	
	ПараметрыОтправкиНаСервере.РазделениеВключено = ОбщегоНазначения.РазделениеВключено();
	
	НастройкиОтправки = Новый Структура;
	НастройкиОтправки.Вставить("ОтчетСсылка", 				ОтчетСсылка);
	НастройкиОтправки.Вставить("ПоляДляЗаписиОтправки", 	ПараметрыОтправкиНаСервере.ПоляДляЗаписиОтправки);
	НастройкиОтправки.Вставить("ТипОтправляемогоДокумента", ПараметрыОтправкиНаСервере.ТипОтправляемогоДокумента);
	НастройкиОтправки.Вставить("ВидОтправляемогоДокумента", ПараметрыОтправкиНаСервере.ВидОтправляемогоДокумента);
	Результат = ОтправкаНаСерверФСС(
		ЭтоЭлектроннаяПодписьВМоделиСервиса,
		ПараметрыОтправкиНаСервере.АдресСервера,
		КороткоеИмяФайлаПакета,
		СтрокаBase64ФайлаПакета,
		ИмяФайлаОтправки,
		ПовторятьСоединение,
		НастройкиОтправки);
	
	ОперацииСФайламиЭДКО.УдалитьВременныйФайл(ИмяФайлаОтправки);
	
	Возврат Результат;
	
КонецФункции

Функция ПолучитьОтпечаткиСертификатовИзНастроекОрганизацииДляФСС(ОрганизацияСсылка) Экспорт
	
	Запрос = Новый Запрос;
	ТекстЗапроса =
		"ВЫБРАТЬ РАЗРЕШЕННЫЕ
		|	НастройкиОбменаФСС.СертификатСтрахователяОтпечаток КАК СертификатСтрахователяОтпечаток,
		|	НастройкиОбменаФСС.СертификатФССОтпечаток КАК СертификатФССОтпечаток,
		|	НастройкиОбменаФСС.СертификатФССЭЛНОтпечаток КАК СертификатФССЭЛНОтпечаток,
		|	НастройкиОбменаФСС.ТестовыйСерверФССЭЛН КАК ТестовыйСерверФССЭЛН,
		|	НастройкиОбменаФСС.ИспользоватьНесколько КАК ИспользоватьНесколько,
		|	НастройкиОбменаФССПользователей.СертификатСтрахователяОтпечаток КАК СертификатСтрахователяПользователяОтпечаток,
		|	НастройкиОбменаФСС.СтраховательПодписанНаУведомленияПоЭЛН КАК СтраховательПодписанНаУведомленияПоЭЛН
		|ИЗ
		|	РегистрСведений.НастройкиОбменаФСС КАК НастройкиОбменаФСС
		|		ЛЕВОЕ СОЕДИНЕНИЕ РегистрСведений.НастройкиОбменаФСС КАК НастройкиОбменаФССПользователей
		|		ПО НастройкиОбменаФСС.Организация = НастройкиОбменаФССПользователей.Организация
		|			И (НастройкиОбменаФССПользователей.Пользователь = &ПользовательСсылка)
		|ГДЕ
		|	НастройкиОбменаФСС.Организация = &ОрганизацияСсылка
		|	И (НастройкиОбменаФСС.Пользователь ЕСТЬ NULL
		|			ИЛИ НастройкиОбменаФСС.Пользователь = ЗНАЧЕНИЕ(Справочник.Пользователи.ПустаяСсылка))
		|	И НастройкиОбменаФСС.ИспользоватьОбмен = ИСТИНА";
	
	ТекущийПользователь = Пользователи.ТекущийПользователь();
	
	Запрос.Текст = ТекстЗапроса;
	Запрос.УстановитьПараметр("ОрганизацияСсылка", 	ОрганизацияСсылка);
	Запрос.УстановитьПараметр("ПользовательСсылка", ТекущийПользователь);
	Выборка = Запрос.Выполнить().Выбрать();
	
	Если НЕ Выборка.Следующий() Тогда
		Результат = Неопределено;
		
	Иначе
		ОтпечатокСертификатаСтрахователя = ?(Выборка.ИспользоватьНесколько,
			Выборка.СертификатСтрахователяПользователяОтпечаток, "");
		Если НЕ ЗначениеЗаполнено(ОтпечатокСертификатаСтрахователя) Тогда
			ОтпечатокСертификатаСтрахователя = Выборка.СертификатСтрахователяОтпечаток;
		КонецЕсли;
		
		Результат = Новый Структура;
		Результат.Вставить("СертификатСтрахователяОтпечаток", 			ОтпечатокСертификатаСтрахователя);
		Результат.Вставить("СертификатФССОтпечаток", 					Выборка.СертификатФССОтпечаток);
		Результат.Вставить("СертификатФССЭЛНОтпечаток", 				Выборка.СертификатФССЭЛНОтпечаток);
		Результат.Вставить("ТестовыйСерверФССЭЛН", 						Выборка.ТестовыйСерверФССЭЛН);
		Результат.Вставить("СтраховательПодписанНаУведомленияПоЭЛН", 	Выборка.СтраховательПодписанНаУведомленияПоЭЛН);
	КонецЕсли;
	
	Возврат Результат;
	
КонецФункции

Функция ПолучитьПоследнююОтправкуОтчетаВФСС(ОтчетСсылка) Экспорт
	
	Отправка = Неопределено;
	
	Если ЗначениеЗаполнено(ОтчетСсылка) Тогда
		Запрос = Новый Запрос;
		Запрос.Текст = "ВЫБРАТЬ ПЕРВЫЕ 1
					   |	ОтправкиФСС.Ссылка
					   |ИЗ
					   |	Справочник.ОтправкиФСС КАК ОтправкиФСС
					   |ГДЕ
					   |	ОтправкиФСС.ОтчетСсылка = &ЭтотОтчет
					   |	И ОтправкиФСС.ПометкаУдаления = ЛОЖЬ
					   |
					   |УПОРЯДОЧИТЬ ПО
					   |	ОтправкиФСС.ДатаОтправки УБЫВ";
		Запрос.Параметры.Вставить("ЭтотОтчет", ОтчетСсылка);
		Результат = Запрос.Выполнить();
		Если НЕ Результат.Пустой() Тогда
			Выборка = Результат.Выбрать();
			Выборка.Следующий();
			Отправка = Выборка.Ссылка;
		КонецЕсли;
	КонецЕсли;
	Возврат Отправка;
	
КонецФункции

Процедура ОбновитьСтатусОтправкиФССОтчета(Отчет)
	
	// находим последнюю не помеченную на удаление отправку отчета
	ПоследняяОтправка = ПолучитьПоследнююОтправкуОтчетаВФСС(Отчет);
	
	// вычисляем значение статуса отправки (Перечисления.СтатусыОтправки)
	Если ЗначениеЗаполнено(ПоследняяОтправка) Тогда
		СтатусОтправки = ПоследняяОтправка.СтатусОтправки;
		Если СтатусОтправки = Перечисления.СтатусыОтправки.ПринятЕстьОшибки
			И ПоследняяОтправка.ВидОтчета <>
			Справочники.ВидыОтправляемыхДокументов.РеестрСтимулирующихВыплатМедицинскимИСоциальнымРаботникам Тогда
			//введено после добавления дополнительного статуса ПринятЕстьОшибки, при котором отчет считается сданным
			СтатусОтправки = Перечисления.СтатусыОтправки.Сдан;
		КонецЕсли;
	Иначе
		СтатусОтправки = Неопределено;
	КонецЕсли;
	
	// сохраняем статус в базе
	ЗаписатьСтатусОтправкиФССОтчета(Отчет, СтатусОтправки, ПоследняяОтправка);
	
КонецПроцедуры

Процедура ЗаписатьСтатусОтправкиФССОтчета(Отчет, СтатусОтправки, ОснованиеСтатуса)
	
	Если ЗначениеЗаполнено(СтатусОтправки) Тогда
		МенЗап = РегистрыСведений.СтатусыОтправки.СоздатьМенеджерЗаписи();
		МенЗап.Объект = Отчет;
		МенЗап.Статус = СтатусОтправки;
		МенЗап.Основание = ОснованиеСтатуса;
		МенЗап.Записать(Истина);
	Иначе
		МенЗап = РегистрыСведений.СтатусыОтправки.СоздатьМенеджерЗаписи();
		МенЗап.Объект = Отчет;
		МенЗап.Прочитать();
		Если МенЗап.Выбран() Тогда
			МенЗап.Удалить();
		КонецЕсли;
	КонецЕсли;
	
	// вызываем переопределяемую процедуру отработки изменения статуса отправки
	ЭлектронныйДокументооборотСКонтролирующимиОрганамиВызовСервераПереопределяемый.ПриИзмененииСтатусаОтправкиДокумента(Отчет, СтатусОтправки);
	
КонецПроцедуры

Функция ПолучитьПустуюДатуЗавершенияОтправкиФСС()
	
	Возврат '39991231235959';
	
КонецФункции

Процедура ПередЗаписьюОтправкиФСС(Объект, Отказ)
	
	Если НЕ ЗначениеЗаполнено(Объект.Период) Тогда
		Объект.Период = СокрЛП(Формат(Объект.ДатаОкончанияПериода, "ДФ=yyyyMMdd") + Формат('39991231' - Объект.ДатаНачалаПериода, "ДФ=yyyyMMdd"));
	КонецЕсли;
	
	Если Объект.СтатусОтправки = Перечисления.СтатусыОтправки.Отправлен Тогда
		
		Если НЕ ЗначениеЗаполнено(Объект.ДатаЗакрытия) Тогда
			Объект.ДатаЗакрытия = ПолучитьПустуюДатуЗавершенияОтправкиФСС();
		КонецЕсли;
		
	Иначе
		Объект.ДатаЗакрытия = Объект.ДатаПолученияРезультата;
	КонецЕсли;
	
КонецПроцедуры

Процедура ПриЗаписиОтправкиФСС(Объект, Отказ)
	
	Если Объект.ОтчетСсылка <> Неопределено Тогда
		// отражаем изменения в регистре статусов отправки
		ОбновитьСтатусОтправкиФССОтчета(Объект.ОтчетСсылка);
		ЭлектронныйДокументооборотСКонтролирующимиОрганами.ЗаписьОбъектовРегламентированнойОтчетности(Объект.ОтчетСсылка, Отказ);
	КонецЕсли;
	
КонецПроцедуры

Функция СписокДопустимыхОрганизацийВОбъектахОбменаФСС() Экспорт
	
	Запрос = Новый Запрос;
	ТекстЗапроса =
		"ВЫБРАТЬ РАЗРЕШЕННЫЕ
		|	НастройкиОбменаФСС.Организация КАК Организация
		|ИЗ
		|	РегистрСведений.НастройкиОбменаФСС КАК НастройкиОбменаФСС
		|ГДЕ
		|	НастройкиОбменаФСС.ИспользоватьОбмен = Истина
		|	И (НастройкиОбменаФСС.Пользователь ЕСТЬ NULL
		|	ИЛИ НастройкиОбменаФСС.Пользователь = ЗНАЧЕНИЕ(Справочник.Пользователи.ПустаяСсылка))";
	
	Запрос.Текст = ТекстЗапроса;
	Возврат Запрос.Выполнить().Выгрузить().ВыгрузитьКолонку("Организация");
	
КонецФункции

Функция ОрганизацияИспользуетОбменСФСС(ОрганизацияСсылка, ИспользуетсяАвтонастройка = Ложь, ДоступностьОнлайнПроверки = Ложь) Экспорт
	
	Если ДоступностьОнлайнПроверки Тогда
		СвойстваОрганизации = ЗначенияРеквизитовОбъекта(ОрганизацияСсылка, "ВидОбменаСКонтролирующимиОрганами, УчетнаяЗаписьОбмена");
		
		Попытка
			ОбменИОнлайнПроверкаВключены = СвойстваОрганизации.ВидОбменаСКонтролирующимиОрганами = Перечисления.ВидыОбменаСКонтролирующимиОрганами.ОбменВУниверсальномФормате И ЗначениеЗаполнено(СвойстваОрганизации.УчетнаяЗаписьОбмена)
				И НЕ СвойстваОрганизации.УчетнаяЗаписьОбмена.ОбменНапрямую И СвойстваОрганизации.УчетнаяЗаписьОбмена.ИспользоватьСервисОнлайнПроверкиОтчетов;
		Исключение
			ОбменИОнлайнПроверкаВключены = Ложь;
		КонецПопытки;
		
		ОнлайнПроверкаКонтролирующиеОрганы = ?(ОбменИОнлайнПроверкаВключены,
			?(СвойстваОрганизации.УчетнаяЗаписьОбмена.СпецоператорСвязи = Перечисления.СпецоператорыСвязи.Такском,
			"ФНС;ПФР;ФСС;ФСРАР;ФТС",
			ЭлектронныйДокументооборотСКонтролирующимиОрганамиВызовСервера.ПолучитьПараметрСпецоператора(
			СвойстваОрганизации.УчетнаяЗаписьОбмена.СпецоператорСвязи, "ОнлайнПроверкаКонтролирующиеОрганы")), "");
		ОнлайнПроверкаКонтролирующиеОрганы = СокрЛП(ОнлайнПроверкаКонтролирующиеОрганы);
		ОнлайнПроверкаКонтролирующиеОрганы = ВРег(ОнлайнПроверкаКонтролирующиеОрганы);
		
		ДоступностьОнлайнПроверки = СтрНайти(";" + ОнлайнПроверкаКонтролирующиеОрганы + ";", ";ФСС;") > 0;
	КонецЕсли;
	
	Запрос = Новый Запрос;
	ТекстЗапроса =
		"ВЫБРАТЬ РАЗРЕШЕННЫЕ
		|	НастройкиОбменаФСС.ИспользоватьОбмен КАК ИспользоватьОбмен,
		|	НастройкиОбменаФСС.ИспользоватьАвтонастройку КАК ИспользоватьАвтонастройку
		|ИЗ
		|	РегистрСведений.НастройкиОбменаФСС КАК НастройкиОбменаФСС
		|ГДЕ
		|	НастройкиОбменаФСС.Организация = &ОрганизацияСсылка
		|	И (НастройкиОбменаФСС.Пользователь ЕСТЬ NULL
		|	ИЛИ НастройкиОбменаФСС.Пользователь = ЗНАЧЕНИЕ(Справочник.Пользователи.ПустаяСсылка))";
	
	Запрос.Текст = ТекстЗапроса;
	Запрос.УстановитьПараметр("ОрганизацияСсылка", ОрганизацияСсылка);
	Выборка = Запрос.Выполнить().Выбрать();
	
	Если Выборка.Следующий() И Выборка.ИспользоватьОбмен Тогда
		ИспользуетсяАвтонастройка = Выборка.ИспользоватьАвтонастройку;
		Возврат Истина;
	Иначе
		Возврат Ложь;
	КонецЕсли;
	
КонецФункции

Функция ОрганизацияИспользуетОбменСФСРАР(ОрганизацияСсылка) Экспорт
	
	Возврат НастройкиФСРАР(ОрганизацияСсылка).ИспользоватьОбмен;
	
КонецФункции

Функция ОрганизацияИспользуетОбменСРПН(ОрганизацияСсылка) Экспорт
	
	Возврат НастройкиРПН(ОрганизацияСсылка).ИспользоватьОбмен;
	
КонецФункции

Функция ОрганизацияИспользуетОбменСФТС(ОрганизацияСсылка) Экспорт
	
	Возврат НастройкиФТС(ОрганизацияСсылка).ИспользоватьОбмен;
	
КонецФункции

Функция ОрганизацияИспользуетОбменСФНС(ОрганизацияСсылка) Экспорт
	
	УчетнаяЗапись = УчетнаяЗаписьОрганизации(ОрганизацияСсылка);
	Если УчетнаяЗапись = Справочники.УчетныеЗаписиДокументооборота.ПустаяСсылка() Тогда
		ДокументооборотПодключен = Ложь;
	Иначе
		ДокументооборотПодключен = ДокументооборотСКОВызовСервера.УчетнаяЗаписьПредназначенаДляДокументооборотаСФНС(УчетнаяЗапись) = Истина
			И ПользователюРазрешеноВыполнятьОбменПоУчетнойЗаписи(УчетнаяЗапись);
	КонецЕсли;
	
	Возврат ДокументооборотПодключен;
	
КонецФункции

Функция ОрганизацияИспользуетОбменСПФР(ОрганизацияСсылка) Экспорт
	
	УчетнаяЗапись = УчетнаяЗаписьОрганизации(ОрганизацияСсылка);
	Если УчетнаяЗапись = Справочники.УчетныеЗаписиДокументооборота.ПустаяСсылка() Тогда
		ДокументооборотПодключен = Ложь;
	Иначе
		ДокументооборотПодключен = ДокументооборотСКОВызовСервера.УчетнаяЗаписьПредназначенаДляДокументооборотаСПФР(УчетнаяЗапись) = Истина
			И ПользователюРазрешеноВыполнятьОбменПоУчетнойЗаписи(УчетнаяЗапись);
	КонецЕсли;
	
	Возврат ДокументооборотПодключен;
	
КонецФункции

Функция ОрганизацияИспользуетОбменСФСГС(ОрганизацияСсылка) Экспорт
	
	УчетнаяЗапись = УчетнаяЗаписьОрганизации(ОрганизацияСсылка);
	Если УчетнаяЗапись = Справочники.УчетныеЗаписиДокументооборота.ПустаяСсылка() Тогда
		ДокументооборотПодключен = Ложь;
	Иначе
		ДокументооборотПодключен = ДокументооборотСКОВызовСервера.УчетнаяЗаписьПредназначенаДляДокументооборотаСФСГС(УчетнаяЗапись) = Истина
			И ПользователюРазрешеноВыполнятьОбменПоУчетнойЗаписи(УчетнаяЗапись);
	КонецЕсли;
	
	Возврат ДокументооборотПодключен;
	
КонецФункции

Процедура ПолучитьСвойстваОрганизацииДляФСС(
		Организация,
		НастроенОбменВУниверсальномФормате,
		ОрганизацияИспользуетОбменСФСС,
		ОрганизацияИспользуетАвтонастройкуПоУчетнойЗаписи,
		УчетнаяЗаписьДокументооборота = Неопределено,
		ПараметрыФорматов = Неопределено) Экспорт
	
	ОрганизацияИспользуетОбменСФСС = Ложь;
	НастроенОбменВУниверсальномФормате = Ложь;
	
	ОрганизацияИспользуетОбменСФСС = ОрганизацияИспользуетОбменСФСС(Организация, ОрганизацияИспользуетАвтонастройкуПоУчетнойЗаписи);
	
	СвойстваОрганизации = ОбщегоНазначения.ЗначенияРеквизитовОбъекта(Организация, "ВидОбменаСКонтролирующимиОрганами, УчетнаяЗаписьОбмена");
	
	УчетнаяЗаписьДокументооборота = СвойстваОрганизации.УчетнаяЗаписьОбмена;
	
	Если СвойстваОрганизации.ВидОбменаСКонтролирующимиОрганами = Перечисления.ВидыОбменаСКонтролирующимиОрганами.ОбменВУниверсальномФормате
		И ЗначениеЗаполнено(УчетнаяЗаписьДокументооборота) Тогда
		НастроенОбменВУниверсальномФормате = Истина;
	КонецЕсли;
	
	Если ПараметрыФорматов <> Неопределено И ПараметрыФорматов.Свойство("ПараметрыФорматаЭЛН")
		И ПараметрыФорматов.ПараметрыФорматаЭЛН = Неопределено Тогда
		
		ПараметрыФорматов.ПараметрыФорматаЭЛН = ПараметрыФорматаЭЛН();
	КонецЕсли;
	
КонецПроцедуры

Функция ОтправкаЗашифрованногоПакетаНаСерверФСС(
		АдресСервера,
		ИмяФайлаОтправки,
		ТипОтправляемогоДокумента = Неопределено,
		ВидОтправляемогоДокумента = Неопределено,
		КороткоеИмяФайлаПакета = Неопределено,
		Идентификатор = Неопределено,
		ОписаниеОшибкиСервером = Неопределено)
	
	Идентификатор = Неопределено;
	ОписаниеОшибкиСервером = Неопределено;
	ОтветПолучен = Ложь;
	
	ЭтоДанныеЭЛН = (ТипОтправляемогоДокумента = Перечисления.ТипыОтправляемыхДокументов.ДанныеЭЛНФСС);
	ЭтоПодтверждениеВидаДеятельности =
		(ТипОтправляемогоДокумента = Перечисления.ТипыОтправляемыхДокументов.ПрочаяОтчетностьФСС
		И ВидОтправляемогоДокумента = Справочники.ВидыОтправляемыхДокументов.ПодтверждениеВидаДеятельности);
	ЭтоРеестрСтимулирующихВыплатМедицинскимИСоциальнымРаботникам =
		(ТипОтправляемогоДокумента = Перечисления.ТипыОтправляемыхДокументов.ПрочаяОтчетностьФСС
		И ВидОтправляемогоДокумента =
		Справочники.ВидыОтправляемыхДокументов.РеестрСтимулирующихВыплатМедицинскимИСоциальнымРаботникам);
	ЭтоПодписьИШифрованиеSOAP = ЭтоДанныеЭЛН ИЛИ ЭтоПодтверждениеВидаДеятельности
		ИЛИ ЭтоРеестрСтимулирующихВыплатМедицинскимИСоциальнымРаботникам;
	
	// устанавливаем соединение с сервером
	ОписаниеОшибкиУстановкиСоединения = "";
	Если ЭтоПодписьИШифрованиеSOAP Тогда
		Соединение = ДокументооборотСКО.УстановитьСоединениеССерверомИнтернета(
			"https://" + АдресСервера,
			ОписаниеОшибкиУстановкиСоединения,
			120);
	Иначе
		Соединение = ДокументооборотСКО.УстановитьСоединениеССерверомИнтернета(
			АдресСервера,
			ОписаниеОшибкиУстановкиСоединения);
	КонецЕсли;
	Если Соединение = Неопределено Тогда
		ТекстОшибки = НСтр("ru = 'Не удалось установить соединение с сервером:
								 |%1'");
		ТекстОшибки = СтрШаблон(ТекстОшибки, ОписаниеОшибкиУстановкиСоединения);
		ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстОшибки);
		Возврат Ложь;
	КонецЕсли;
	
	Если ЭтоДанныеЭЛН Тогда
		ПараметрыФормата = ПараметрыФорматаЭЛН();
		ЭтоВерсия1X = Лев(ПараметрыФормата.Версия, 2) = "1.";
		РесурсНаСервере = ?(ЭтоВерсия1X, "/ws-insurer-crypto-v11/FileOperationsLnPort",
				"/ws-insurer-crypto-v20/FileOperationsLnService");
		
	ИначеЕсли ЭтоПодтверждениеВидаДеятельности Тогда
		РесурсНаСервере = "/ws-gtw-okved-confirm/api/soap/v1/OkvedconfirmGatewayService";
		
	ИначеЕсли ЭтоРеестрСтимулирующихВыплатМедицинскимИСоциальнымРаботникам Тогда
		Если ДокументооборотСКОВызовСервера.ИспользуетсяРежимТестирования() Тогда
			РесурсНаСервере = "/ws-quarantine-crypto/api/soap/QuarantineStatementServicePorts?wsdl";
		Иначе
			РесурсНаСервере = "/ws-quarantine/api/soap/QuarantineStatementServicePorts?wsdl";
		КонецЕсли;
		
	ИначеЕсли ТипОтправляемогоДокумента <> Перечисления.ТипыОтправляемыхДокументов.Отчет4аФСС
		И ТипОтправляемогоДокумента <> Перечисления.ТипыОтправляемыхДокументов.РеестрСведенийФСС Тогда
		
		РесурсНаСервере = "/F4ExtService/ExtService.svc";
		ПространствоИменСервиса = "http://tempuri.org/";
		ИмяСервиса = "IExtService";
		
	Иначе
		РесурсНаСервере = "/ExtService/GatewayService.svc";
		ПространствоИменСервиса = "http://asystems.fss";
		ИмяСервиса = "IGatewayService";
	КонецЕсли;
	
	РесурсНаСервере = ДокументооборотСКО.ПолучитьЗаменяемоеЗначениеТестовогоСервера1С(РесурсНаСервере, "{FSS}" + РесурсНаСервере, Соединение);
	
	Если ЭтоПодписьИШифрованиеSOAP Тогда
		// формируем общие заголовки зашифрованного запроса SOAP
		ЗаголовкиHTTP = Новый Соответствие();
		ЗаголовкиHTTP.Вставить("Content-Type", "text/xml; charset=utf-8");
		Если ЭтоПодтверждениеВидаДеятельности Тогда
			ЗаголовкиHTTP.Вставить("SOAPAction", "http://gtw.ws.fss.ru/openservice/okvedconfirm/confirmOkved");
		ИначеЕсли ЭтоРеестрСтимулирующихВыплатМедицинскимИСоциальнымРаботникам Тогда
			ЗаголовкиHTTP.Вставить("SOAPAction",
				"http://www.fss.ru/integration/ws/eln/quarantineStatement/SubmitCovidWorkerRegist/v01");
		КонецЕсли;
		ЗаголовкиHTTP.Вставить("Proxy-Connection", "Keep-Alive");
		
		ИмяФайлаSOAP = ИмяФайлаОтправки;
		
	Иначе
		ДвоичныеДанныеПакета = Новый ДвоичныеДанные(ИмяФайлаОтправки);
		ДанныеПакетаВBase64 = Base64Строка(ДвоичныеДанныеПакета);
		
		// формируем общие заголовки запроса SOAP
		ЗаголовкиHTTP = Новый Соответствие();
		ЗаголовкиHTTP.Вставить("Content-Type", "application/soap+xml; charset=utf-8");
		ЗаголовкиHTTP.Вставить("Proxy-Connection", "Keep-Alive");
		
		ТекстЗапросаSOAP =
			"<s:Envelope xmlns:s=""http://www.w3.org/2003/05/soap-envelope"" xmlns:a=""http://www.w3.org/2005/08/addressing"">
			|	<s:Header>
			|		<a:Action s:mustUnderstand=""1"">" + ПространствоИменСервиса + ?(Прав(ПространствоИменСервиса, 1) <> "/", "/", "")
						+ ИмяСервиса + "/SendFile</a:Action>
			|		<a:To s:mustUnderstand=""1"">http://" + АдресСервера + РесурсНаСервере + "</a:To>
			|	</s:Header>
			|	<s:Body>
			|		<SendFile xmlns=""" + ПространствоИменСервиса + """>
			|			<data>" + ДанныеПакетаВBase64 + "</data>
			|			<fileName>" + КороткоеИмяФайлаПакета + "</fileName>
			|		</SendFile>
			|	</s:Body>
			|</s:Envelope>";
		
		ИмяФайлаSOAP = ПолучитьИмяВременногоФайла();
		ТекстовыйФайлSOAP = Новый ЗаписьТекста(ИмяФайлаSOAP, "utf-8");
		ТекстовыйФайлSOAP.Записать(ТекстЗапросаSOAP);
		ТекстовыйФайлSOAP.Закрыть();
	КонецЕсли;
	
	ДокументооборотСКО.ДобавитьЗаголовкиДляТестовогоСервера1С(ЗаголовкиHTTP, Соединение);
	
	// посылаем запрос
	ОтветПолучен = Истина;
	
	ФайлРезультата = ПолучитьИмяВременногоФайла();
	Попытка
		Соединение.ОтправитьДляОбработки(ИмяФайлаSOAP, РесурсНаСервере, ФайлРезультата, ЗаголовкиHTTP);
		Соединение = Неопределено;
	Исключение
		Если НЕ ЭтоПодписьИШифрованиеSOAP Тогда
			ОперацииСФайламиЭДКО.УдалитьВременныйФайл(ИмяФайлаSOAP);
		КонецЕсли;
		ИнформацияОбОшибке = ИнформацияОбОшибке();
		
		Для ИндексПричиныОшибки = 1 По 5 Цикл
			Если НЕ ЭтоПодписьИШифрованиеSOAP И СтрНайти(ИнформацияОбОшибке.Описание, "ExtServiceException") > 0 Тогда
				Если СтрНайти(ИнформацияОбОшибке.Описание, "Неверное имя файла") > 0 Тогда
					ОписаниеОшибкиСервером = НСтр("ru = 'Неверное имя файла.'");
				Иначе
					ОписаниеОшибкиСервером = НСтр("ru = 'Сбой на сервере ФСС.'");
				КонецЕсли;
				Возврат ОтветПолучен;
				
			ИначеЕсли ЭтоПодписьИШифрованиеSOAP
				И СтрНайти(ИнформацияОбОшибке.Описание, "http://schemas.xmlsoap.org/soap/envelope/") > 0
				И СтрНайти(ИнформацияОбОшибке.Описание, "<faultstring>") > 0 Тогда
				
				ОписаниеОшибкиСервером = ТекстПослеПрефикса(ИнформацияОбОшибке.Описание, "<faultstring>", "<");
				ОписаниеОшибкиСервером = ТекстИзHTML(ОписаниеОшибкиСервером);
				ОписаниеОшибкиСервером = НСтр("ru = 'Ошибка сервера ФСС.'") + " " + СокрЛ(ОписаниеОшибкиСервером);
				ОписаниеОшибкиСервером = СокрЛП(ОписаниеОшибкиСервером);
				Возврат ОтветПолучен;
				
			ИначеЕсли ТипОтправляемогоДокумента = Перечисления.ТипыОтправляемыхДокументов.РеестрСведенийФСС
				И (СтрНайти(ИнформацияОбОшибке.Описание, "<ErrorMessage>") > 0
				ИЛИ СтрНайти(ИнформацияОбОшибке.Описание, "Fault>") > 0) Тогда
				
				ТекстОшибки = ТекстПослеПрефикса(ИнформацияОбОшибке.Описание,
					"<ErrorMessage>", "</");
				Если НЕ ЗначениеЗаполнено(ТекстОшибки) Тогда
					ТекстОшибки = ТекстПослеПрефикса(ИнформацияОбОшибке.Описание,
						"Text xml:lang=""ru-RU"">", "</");
				КонецЕсли;
				Если ЗначениеЗаполнено(ТекстОшибки) Тогда
					ОписаниеОшибкиСервером = СокрЛП(ТекстОшибки);
					ОписаниеОшибкиСервером = НСтр("ru = 'Ошибка сервера ФСС.'") + " " + ТекстИзHTML(ОписаниеОшибкиСервером);
					Возврат ОтветПолучен;
					
				КонецЕсли;
				
			ИначеЕсли ТипОтправляемогоДокумента = Перечисления.ТипыОтправляемыхДокументов.РеестрСведенийФСС
				И (СтрНайти(ИнформацияОбОшибке.Описание, "внутренняя ошибка сервера") > 0
				ИЛИ СтрНайти(ИнформацияОбОшибке.Описание, "(500)") > 0) Тогда
				
				ОписаниеОшибкиСервером = НСтр("ru = 'Ошибка сервера ФСС. '") + ИнформацияОбОшибке.Описание;
				Возврат ОтветПолучен;
			КонецЕсли;
			
			Если ИнформацияОбОшибке.Причина = Неопределено Тогда
				Прервать;
			КонецЕсли;
			ИнформацияОбОшибке = ИнформацияОбОшибке.Причина;
		КонецЦикла;
		
		ОтветПолучен = Ложь;
		Возврат ОтветПолучен;
	КонецПопытки;
	
	ТекстовыйФайлРезультата = Новый ЧтениеТекста(ФайлРезультата, "utf-8");
	ТекстРезультата = ТекстовыйФайлРезультата.Прочитать();
	ТекстовыйФайлРезультата.Закрыть();
	Если НЕ ЭтоПодписьИШифрованиеSOAP Тогда
		ОперацииСФайламиЭДКО.УдалитьВременныйФайл(ИмяФайлаSOAP);
	КонецЕсли;
	ОперацииСФайламиЭДКО.УдалитьВременныйФайл(ФайлРезультата);
	
	Если НЕ ЗначениеЗаполнено(ТекстРезультата) Тогда
		ОписаниеОшибкиСервером = НСтр("ru = 'Сбой на сервере ФСС.'");
		Возврат ОтветПолучен;
	КонецЕсли;
	
	РезультатИзвлечения = ИзвлечьРезультатSOAP(
		ТекстРезультата,
		?(ЭтоПодписьИШифрованиеSOAP, "", "SendFile"),
		ЭтоПодписьИШифрованиеSOAP);
	УзелРезультата 	= РезультатИзвлечения.УзелРезультата;
	ТекстОшибки 	= РезультатИзвлечения.ТекстОшибки;
	
	Если ЭтоПодписьИШифрованиеSOAP Тогда
		Идентификатор = ?(УзелРезультата <> Неопределено, ТекстРезультата, "");
		
	Иначе
		УзелExtID = НайтиУзел(УзелРезультата, "ExtID");
		
		Идентификатор = ?(УзелExtID <> Неопределено, СокрЛП(УзелExtID.Значение), Неопределено);
	КонецЕсли;
	
	Если НЕ ЗначениеЗаполнено(Идентификатор) Тогда
		ОписаниеОшибкиСервером = ?(ЗначениеЗаполнено(ТекстОшибки), ТекстОшибки, НСтр("ru = 'Ошибка формата ответа сервера ФСС!'"));
	КонецЕсли;
	
	Возврат ОтветПолучен;
	
КонецФункции

Функция ИзвлечьРезультатSOAP(ТекстРезультата, ИмяФункции, ЭтоПодписьИШифрованиеSOAP = Ложь)
	
	Результат = Новый Структура;
	Результат.Вставить("УзелРезультата", 	Неопределено);
	Результат.Вставить("ТекстОшибки", 		"");
	
	ДеревоXML = ЗагрузитьXMLВДеревоЗначений(Неопределено, ТекстРезультата);
	Если ДеревоXML = Неопределено Тогда
		Возврат Результат;
	КонецЕсли;
	
	УзелEnvelope = НайтиУзел(ДеревоXML, "Envelope");
	Если УзелEnvelope = Неопределено Тогда
		Возврат Результат;
	КонецЕсли;
	
	УзелBody = НайтиУзел(УзелEnvelope, "Body");
	Если УзелBody = Неопределено Тогда
		Возврат Результат;
	КонецЕсли;
	
	Если ЭтоПодписьИШифрованиеSOAP Тогда
		УзелЗашифрованныхДанных = НайтиУзел(УзелBody, "EncryptedData");
		Если УзелЗашифрованныхДанных <> Неопределено Тогда
			
			Результат.УзелРезультата = УзелЗашифрованныхДанных;
			Возврат Результат;
			
		КонецЕсли;
		
	Иначе
		УзелОтветаФункции = НайтиУзел(УзелBody, ИмяФункции + "Response");
		Если УзелОтветаФункции <> Неопределено Тогда
			
			УзелРезультата = НайтиУзел(УзелОтветаФункции, ИмяФункции + "Result");
			Если УзелРезультата <> Неопределено Тогда
				
				Результат.УзелРезультата = УзелРезультата;
				Возврат Результат;
				
			КонецЕсли;
			
		КонецЕсли;
	КонецЕсли;
	
	УзелFault = НайтиУзел(УзелBody, "Fault");
	Если УзелFault = Неопределено Тогда
		Возврат Результат;
	КонецЕсли;
	
	УзелDetail = НайтиУзел(УзелFault, "Detail");
	Если УзелDetail = Неопределено Тогда
		УзелFaultString = НайтиУзел(УзелFault, "faultstring");
		Если УзелFaultString = Неопределено Тогда
			Возврат Результат;
		КонецЕсли;
		
		Результат.ТекстОшибки = XMLЗначение(Тип("Строка"), УзелFaultString.Значение);
		Возврат Результат;
	КонецЕсли;
	
	УзелGeneralFault = НайтиУзел(УзелDetail, "GeneralFault");
	Если УзелGeneralFault = Неопределено Тогда
		Возврат Результат;
	КонецЕсли;
	
	УзелErrorMessage = НайтиУзел(УзелGeneralFault, "ErrorMessage");
	Если УзелErrorMessage = Неопределено Тогда
		Возврат Результат;
	КонецЕсли;
	
	Результат.ТекстОшибки = XMLЗначение(Тип("Строка"), УзелErrorMessage.Значение);
	Возврат Результат;
	
КонецФункции

Функция НайтиУзел(ОбъектXML, Имя, ВернутьВсе = Ложь)
	
	Если ОбъектXML = Неопределено Тогда
		Возврат Неопределено;
	КонецЕсли;
	
	Результат = ?(ВернутьВсе, Новый Массив, Неопределено);
	ДлинаИмени = СтрДлина(Имя);
	Для каждого СтрокаXML Из ОбъектXML.Строки Цикл
		// поиск без учета пространств имен
		Если СтрокаXML.Имя = Имя ИЛИ Прав(СтрокаXML.Имя, ДлинаИмени + 1) = ":" + Имя Тогда
			Если ВернутьВсе Тогда
				Результат.Добавить(СтрокаXML);
				
			Иначе
				Результат = СтрокаXML;
				Прервать;
			КонецЕсли;
		КонецЕсли;
	КонецЦикла;
	
	Возврат Результат;
	
КонецФункции

Процедура ИзвлечьЭлектронноеПредставлениеИзХранилищаИПерекодировать(
		Владелец,
		ТекстВыгрузки,
		ИмяФайлаВыгрузки,
		КодировкаИсходника = Неопределено,
		ПреобразоватьИзКодировки = Неопределено) Экспорт
	
	// получаем электронное представление
	ХранилищеВыгрузки = "";
	ИмяФайлаВыгрузки = "";

	ФайлВАрхиве = Ложь;
	
	Запрос = Новый Запрос("ВЫБРАТЬ РАЗРЕШЕННЫЕ ПЕРВЫЕ 1
						  |	ХранилищеЭлектронныхПредставленийРегламентированныхОтчетов.ИмяФайла,
						  |	ХранилищеЭлектронныхПредставленийРегламентированныхОтчетов.Данные
						  |	, ВЫБОР
						  |		КОГДА СостояниеОбъектов.Архивный ЕСТЬ NULL ТОГДА Ложь
						  |		КОГДА СостояниеОбъектов.Архивный = Ложь ТОГДА Ложь
						  |	ИНАЧЕ
						  |		Истина
						  |	КОНЕЦ ВАрхиве
						  |ИЗ
						  |	РегистрСведений.ХранилищеЭлектронныхПредставленийРегламентированныхОтчетов КАК ХранилищеЭлектронныхПредставленийРегламентированныхОтчетов
						  |
						  |		ЛЕВОЕ СОЕДИНЕНИЕ РегистрСведений.ПризнакиАрхивированияФайловДОСКонтролирующимиОрганами КАК СостояниеОбъектов
						  |			ПО 		(СостояниеОбъектов.Объект = ХранилищеЭлектронныхПредставленийРегламентированныхОтчетов.ЭлектронноеПредставление)
						  |				И	(СостояниеОбъектов.ИмяФайла = ХранилищеЭлектронныхПредставленийРегламентированныхОтчетов.ИмяФайла)
						  |				И	(СостояниеОбъектов.Владелец = Значение(Перечисление.ВидыАрхивируемыхМетаданныхДО.ХранилищеЭлектронныхПредставленийРегламентированныхОтчетов))
						  |
						  |ГДЕ
						  |	ХранилищеЭлектронныхПредставленийРегламентированныхОтчетов.ЭлектронноеПредставление = &ЭлектронноеПредставление");
	
	Запрос.УстановитьПараметр("ЭлектронноеПредставление", Владелец);
	РезультатЗапроса = Запрос.Выполнить();
	Если НЕ РезультатЗапроса.Пустой() Тогда
		Выборка = РезультатЗапроса.Выбрать();
		Если Выборка.Следующий() Тогда
			Если Выборка.ВАрхиве Тогда
				ИмяФайлаВыгрузки = "";
				ХранилищеВыгрузки = Неопределено;
				ФайлВАрхиве = Истина;
			Иначе
				ИмяФайлаВыгрузки = СокрЛП(Выборка.ИмяФайла);
				ХранилищеВыгрузки = Выборка.Данные;
			КонецЕсли;
		КонецЕсли;
	КонецЕсли;
	
	Если ИмяФайлаВыгрузки = "" ИЛИ ХранилищеВыгрузки = Неопределено Тогда
		Если ФайлВАрхиве Тогда
			ТекстСообщения = НСтр("ru = 'Файл загруженного отчета был ранее перемещен в архив. Извлечение содержимого невозможно!'");
		Иначе
			ТекстСообщения = НСтр("ru = 'Не обнаружена информация для отправки!'");
		КонецЕсли;
		СообщитьПользователю(ТекстСообщения);
		ИмяФайлаВыгрузки = "";
		ТекстВыгрузки = "";
		Возврат;
	КонецЕсли;
	
	// пишем электронное представление в файл
	
	ЭтоXML = нрег(Прав(ИмяФайлаВыгрузки, 4)) = ".xml";
	
	ВремФайл = ПолучитьИмяВременногоФайла();
	ХранилищеВыгрузки.Получить().Записать(ВремФайл);
	
	КодировкаПоУмолчанию = "windows-1251";
	КодировкаИсходника = КодировкаПоУмолчанию;
	
	ОбъектЧтениеТекста = Новый ЧтениеТекста(ВремФайл, ?(ПреобразоватьИзКодировки <> Неопределено, ПреобразоватьИзКодировки,
		?(ЭтоXML, КодировкаПоУмолчанию, "cp866")));
	ТекстВыгрузки = ОбъектЧтениеТекста.Прочитать();
	ОбъектЧтениеТекста.Закрыть();
	
	УдалитьФайлы(ВремФайл);
	
КонецПроцедуры

Процедура ПолучитьСвойстваФайлаВыгрузки(
		ОбъектСсылка,
		ТекстВыгрузки,
		ИмяФайлаВыгрузки,
		КодировкаФайлаВыгрузки = Неопределено,
		СвойстваФайлаВыгрузки = Неопределено) Экспорт
	
	СвойстваФайлаВыгрузки = ЭлектронныйДокументооборотСКонтролирующимиОрганамиВызовСервераПереопределяемый.ВыгрузитьДокумент(ОбъектСсылка);
	
	Если СвойстваФайлаВыгрузки = Неопределено Тогда
		Возврат;
	КонецЕсли;
	
	Если СвойстваФайлаВыгрузки.КодировкаФайлаВыгрузки = КодировкаТекста.ANSI Тогда
		КодировкаФайлаВыгрузки = "windows-1251";
	ИначеЕсли СвойстваФайлаВыгрузки.КодировкаФайлаВыгрузки = КодировкаТекста.OEM Тогда
		КодировкаФайлаВыгрузки = "cp866";
	Иначе
		КодировкаФайлаВыгрузки = СвойстваФайлаВыгрузки.КодировкаФайлаВыгрузки;
	КонецЕсли;
	
	ВремФайл = ПолучитьИмяВременногоФайла();
	ДвоичныеДанныеФайла = ПолучитьИзВременногоХранилища(СвойстваФайлаВыгрузки.АдресФайлаВыгрузки);
	ДвоичныеДанныеФайла.Записать(ВремФайл);
	ОбъектЧтениеТекста = Новый ЧтениеТекста(ВремФайл, КодировкаФайлаВыгрузки);
	
	ТекстВыгрузки = ОбъектЧтениеТекста.Прочитать();
	ОбъектЧтениеТекста.Закрыть();
	УдалитьФайлы(ВремФайл);
	
	ИмяФайлаВыгрузки = СвойстваФайлаВыгрузки.ИмяФайлаВыгрузки;
	
КонецПроцедуры

Функция ПоляНаСервереДляЗаписиОтправкиВФСС(ОбъектСсылка) Экспорт
	
	ВидОтправляемогоДокумента = ОбъектСсылка.ВидОтчета;
	ТипОтправляемогоДокумента = ОбщегоНазначения.ЗначениеРеквизитаОбъекта(ВидОтправляемогоДокумента, "ТипДокумента");
	
	Результат = Новый Структура;
	Результат.Вставить("КодОрганаФСС", 			ОбъектСсылка.Получатель);
	Результат.Вставить("ДатаНачалаПериода", 	ОбъектСсылка.ДатаНачала);
	Результат.Вставить("ДатаОкончанияПериода", 	ОбъектСсылка.ДатаОкончания);
	Результат.Вставить("Версия", 				ОбъектСсылка.Версия);
	Результат.Вставить("ВидОтчета", 			ВидОтправляемогоДокумента);
	Результат.Вставить("ТипДокумента", 			ТипОтправляемогоДокумента);
	
	Возврат Результат;
	
КонецФункции

Функция ПолучитьТипВидаОтправляемогоДокументаПоСсылкеНаОбъектОтправкиДляФСС(
		ОбъектСсылка,
		ПолучитьВидОтправляемогоДокумента = Ложь) Экспорт
	
	ТипЗнчОбъектСсылка = ТипЗнч(ОбъектСсылка);
	
	ВидОтправляемогоДокумента = Неопределено;
	
	ОбъектПоУмолчаниюРеестрЭЛНСуществует =
		Метаданные.Документы.Найти("РеестрДанныхЭЛНЗаполняемыхРаботодателем") <> Неопределено;
	ОбъектПоУмолчаниюРеестрСтимулирующихВыплатМедицинскимИСоциальнымРаботникамСуществует =
		Метаданные.Документы.Найти("РеестрСтимулирующихВыплатМедицинскимИСоциальнымРаботникам") <> Неопределено;
	
	Если ТипЗнчОбъектСсылка = Тип("ДокументСсылка.РегламентированныйОтчет") Тогда
		
		// ищем отчет в справочнике "ВидыОтправляемыхДокументов"
		ИсточникОтчета = ОбъектСсылка.ИсточникОтчета;	
		РезультатПоиска = Справочники.ВидыОтправляемыхДокументов.НайтиПоРеквизиту("Источник", ИсточникОтчета);
		Если РезультатПоиска = Справочники.ВидыОтправляемыхДокументов.ПустаяСсылка() Тогда
			ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(СтроковыеФункцииКлиентСервер.ПодставитьПараметрыВСтроку(НСтр("ru = 'В справочнике ""Виды отправляемых документов"" не найден элемент с именем объекта метаданных ""%1""!'"), ИсточникОтчета));
			ВидОтправляемогоДокумента = Неопределено;
		Иначе
			ВидОтправляемогоДокумента = РезультатПоиска; 
		КонецЕсли;
		
	ИначеЕсли ТипЗнчОбъектСсылка = Тип("СправочникСсылка.ЭлектронныеПредставленияРегламентированныхОтчетов") 
		ИЛИ ТипЗнчОбъектСсылка = Тип("СправочникОбъект.ЭлектронныеПредставленияРегламентированныхОтчетов") Тогда
		
		ВидОтправляемогоДокумента = ОбъектСсылка.ВидОтчета;
		
	ИначеЕсли ТипЗнчОбъектСсылка = Тип("СправочникСсылка.ОтправкиФСС") Тогда
		
		ВидОтправляемогоДокумента = ОбъектСсылка.ВидОтчета;
		
	ИначеЕсли ДокументооборотСФССКлиентСервер.ЭтоРеестрСведенийНаВыплатуПособийФСС(ОбъектСсылка) Тогда
		
		ВидОтправляемогоДокумента = Справочники.ВидыОтправляемыхДокументов.РеестрСведенийВФСС;
		
	ИначеЕсли ДокументооборотСФССКлиентСервер.ЭтоРеестрДанныхЭЛНЗаполняемыхРаботодателем(ОбъектСсылка,
		ОбъектПоУмолчаниюРеестрЭЛНСуществует) Тогда
		
		ВидОтправляемогоДокумента = Справочники.ВидыОтправляемыхДокументов.РеестрДанныхЭЛНЗаполняемыхРаботодателем;
		
	ИначеЕсли ДокументооборотСФССКлиентСервер.ЭтоРеестрСтимулирующихВыплатМедицинскимИСоциальнымРаботникам(ОбъектСсылка,
		ОбъектПоУмолчаниюРеестрСтимулирующихВыплатМедицинскимИСоциальнымРаботникамСуществует) Тогда
		
		ВидОтправляемогоДокумента =
			Справочники.ВидыОтправляемыхДокументов.РеестрСтимулирующихВыплатМедицинскимИСоциальнымРаботникам;
		
	КонецЕсли;

	Если ВидОтправляемогоДокумента <> Неопределено Тогда
		
		Если ПолучитьВидОтправляемогоДокумента Тогда
			ТипОтправляемогоДокумента = ОбщегоНазначения.ЗначениеРеквизитаОбъекта(ВидОтправляемогоДокумента, "ТипДокумента");
			
			Результат = Новый Структура();
			Результат.Вставить("ВидОтправляемогоДокумента", ВидОтправляемогоДокумента);
			Результат.Вставить("ТипОтправляемогоДокумента", ТипОтправляемогоДокумента);
			Возврат Результат;
			
		Иначе
			Возврат ВидОтправляемогоДокумента.ТипДокумента;
		КонецЕсли;
		
	Иначе
		
		Возврат Неопределено;
	
	КонецЕсли;
	
КонецФункции

Функция ДанныеРеестраЭЛН(ВыгрузкаXML) Экспорт
	
	ПараметрыФормата = ПараметрыФорматаЭЛН();
	ЭтоВерсия1X = Лев(ПараметрыФормата.Версия, 2) = "1.";
	
	ИмяМетодаОтправки = ?(ЭтоВерсия1X, "prParseReestrFile", "prParseReestrFileRequest");
	Результат = ТекстПослеПрефикса(ВыгрузкаXML, ":" + ИмяМетодаОтправки);
	Если НЕ ЗначениеЗаполнено(Результат) Тогда
		Результат = ТекстПослеПрефикса(ВыгрузкаXML, "<" + ИмяМетодаОтправки);
		Если НЕ ЗначениеЗаполнено(Результат) Тогда
			Возврат ВыгрузкаXML;
		КонецЕсли;
	КонецЕсли;
	
	ИмяУзлаКорняЗаписей = ?(ЭтоВерсия1X, "ROWSET", "rowset");
	ИмяУзлаЗаписей = ?(ЭтоВерсия1X, "ROW", "row");
	
	КореньУзлаЗаписейБезПрефикса = Ложь;
	НачалоНаименованияУзлаЗаписей = СтрНайти(Результат, ":" + ИмяУзлаКорняЗаписей);
	Если НачалоНаименованияУзлаЗаписей = 0 Тогда
		НачалоНаименованияУзлаЗаписей = СтрНайти(Результат, "<" + ИмяУзлаКорняЗаписей);
	КонецЕсли;
	Если НачалоНаименованияУзлаЗаписей = 0 Тогда
		КореньУзлаЗаписейБезПрефикса = Истина;
		НачалоНаименованияУзлаЗаписей = СтрНайти(Результат, ":" + ИмяУзлаЗаписей);
		Если НачалоНаименованияУзлаЗаписей = 0 Тогда
			НачалоНаименованияУзлаЗаписей = СтрНайти(Результат, "<" + ИмяУзлаЗаписей);
		КонецЕсли;
		Если НачалоНаименованияУзлаЗаписей = 0 Тогда
			Возврат ВыгрузкаXML;
		КонецЕсли;
	КонецЕсли;
	
	НачалоУзлаЗаписей = НачалоНаименованияУзлаЗаписей;
	Пока НачалоУзлаЗаписей > 1 Цикл
		НачалоУзлаЗаписей = НачалоУзлаЗаписей - 1;
		Если Сред(Результат, НачалоУзлаЗаписей, 1) = "<" Тогда
			Прервать;
		КонецЕсли;
	КонецЦикла;
	ПрефиксУзлаЗаписей = Сред(Результат, НачалоУзлаЗаписей + 1,
		НачалоНаименованияУзлаЗаписей - НачалоУзлаЗаписей - 1);
	
	ПространствоИменЭЛН = ?(ЭтоВерсия1X, "http://ru/ibs/fss/ln/ws/FileOperationsLn.wsdl",
		"http://www.fss.ru/integration/types/eln/ins/v01");
	ПространствоИменАтрибутов = ?(ЭтоВерсия1X, Неопределено, "http://www.fss.ru/integration/types/eln/v01");
	
	ПрефиксАтрибутовУзлаЗаписей = ПрефиксУзлаЗаписей;
	Если КореньУзлаЗаписейБезПрефикса Тогда
		ПрефиксАтрибутовУзлаЗаписей = ТекстПослеПрефикса(Результат, "xmlns:", "=""" + ПространствоИменЭЛН + """");
	КонецЕсли;
	
	ПрефиксАтрибутов = Неопределено;
	Если ПространствоИменАтрибутов <> Неопределено Тогда
		НачалоЗначенияПространстваИменАтрибутов = СтрНайти(ВыгрузкаXML, "=""" + ПространствоИменАтрибутов + """");
		
		Если НачалоЗначенияПространстваИменАтрибутов <> 0 Тогда
			НачалоПрефиксаАтрибутов = НачалоЗначенияПространстваИменАтрибутов;
			Пока НачалоПрефиксаАтрибутов > 1 Цикл
				Если Сред(ВыгрузкаXML, НачалоПрефиксаАтрибутов - 1, 1) = ":"
					ИЛИ Сред(ВыгрузкаXML, НачалоПрефиксаАтрибутов - 1, 1) = " " Тогда
					Прервать;
				КонецЕсли;
				НачалоПрефиксаАтрибутов = НачалоПрефиксаАтрибутов - 1;
			КонецЦикла;
			
			ПрефиксАтрибутов = Сред(ВыгрузкаXML, НачалоПрефиксаАтрибутов,
				НачалоЗначенияПространстваИменАтрибутов - НачалоПрефиксаАтрибутов);
		КонецЕсли;
	КонецЕсли;
	
	Результат = СтрЗаменить(Результат, "<" + ПрефиксУзлаЗаписей + ":", "<");
	Результат = СтрЗаменить(Результат, "</" + ПрефиксУзлаЗаписей + ":", "</");
	Результат = СтрЗаменить(Результат, " xmlns:" + ПрефиксАтрибутовУзлаЗаписей + "=""" + ПространствоИменЭЛН + """", "");
	Если ПространствоИменАтрибутов <> Неопределено И ПрефиксАтрибутов <> Неопределено Тогда
		Результат = СтрЗаменить(Результат, " xmlns:" + ПрефиксАтрибутов + "=""" + ПространствоИменАтрибутов + """", "");
	КонецЕсли;
	Результат = СтрЗаменить(Результат, " xmlns:" + ПрефиксУзлаЗаписей
		+ "=""" + ПространствоИменЭЛН + """ xmlns:xsi=""http://www.w3.org/2001/XMLSchema-instance""", "");
	Если ЗначениеЗаполнено(ПрефиксАтрибутов) Тогда
		Результат = СтрЗаменить(Результат, " " + ПрефиксАтрибутов + ":", " ");
	КонецЕсли;
	
	Пока Истина Цикл
		// убрать подстроки " wsu:Id=""ELN_123456789012"""
		НачалоУзлаЗаписи = СтрНайти(Результат, "<" + ИмяУзлаЗаписей + " ");
		Если НачалоУзлаЗаписи = 0 Тогда
			Прервать;
		КонецЕсли;
		
		УзелЗаписи = Сред(Результат, НачалоУзлаЗаписи);
		ДлинаУзлаЗаписи = СтрНайти(УзелЗаписи, ">");
		Если ДлинаУзлаЗаписи = 0 Тогда
			Прервать;
		КонецЕсли;
		
		Результат = Лев(Результат, НачалоУзлаЗаписи - 1) + "<" + ИмяУзлаЗаписей + ">"
			+ Сред(Результат, НачалоУзлаЗаписи + ДлинаУзлаЗаписи);
	КонецЦикла;
	
	Результат = СтрЗаменить(Результат, " " + ПрефиксАтрибутовУзлаЗаписей + ":", " d1p1:");
	Результат = СтрЗаменить(Результат, "<" + ИмяУзлаКорняЗаписей, "<" + ИмяУзлаКорняЗаписей
		+ " xmlns=""" + ПространствоИменЭЛН + """"
		+ " xmlns:d1p1=""" + ПространствоИменЭЛН + """"
		+ " xmlns:xs=""http://www.w3.org/2001/XMLSchema"""
		+ " xmlns:xsi=""http://www.w3.org/2001/XMLSchema-instance""");
	
	Результат = ТекстПослеПрефикса(Результат, "<" + ИмяУзлаКорняЗаписей + " ", "</" + ИмяУзлаКорняЗаписей + ">");
	Если НЕ ЗначениеЗаполнено(Результат) Тогда
		Возврат ВыгрузкаXML;
	КонецЕсли;
	Результат = "<" + ИмяУзлаКорняЗаписей + " " + Результат + "</" + ИмяУзлаКорняЗаписей + ">";
	
	Возврат Результат;
	
КонецФункции

Процедура ПриЗаписиНастройкиОбменаФСС(НаборЗаписей, Отказ)
	
	Если НаборЗаписей.Количество() = 0 Тогда
		Возврат;
	КонецЕсли;
	
	// Все организации где подключен обмен с ФСС.
	Организации = СписокДопустимыхОрганизацийВОбъектахОбменаФСС();
	
	ИспользоватьОбмен = Ложь;
	Для каждого Запись из НаборЗаписей Цикл
		
		Организация = Запись.Организация;
		
		Индекс = Организации.Найти(Организация);
		Если Индекс <> Неопределено Тогда
			// Удаляем из массива организации из набора.
			Организации.Удалить(Индекс);
		КонецЕсли;
		
		ТекущаяОрганизацияВМассиве = Новый Массив;
		ТекущаяОрганизацияВМассиве.Добавить(Организация);
		
		Если Запись.ИспользоватьОбмен
			И (Запись.СтраховательПодписанНаУведомленияПоЭЛН
			ИЛИ ТребуетсяЗапускРегламентногоЗаданияОбменаСФСС(ТекущаяОрганизацияВМассиве)) Тогда
			// Включаем обмен по организациям где есть подписка на ЭЛН или требуется запуск РЗ для других типов документов.
			ИспользоватьОбмен = Истина;
			Прервать;
		КонецЕсли;
		
	КонецЦикла;
	
	Если (Организации.Количество() > 0
			И ТребуетсяЗапускРегламентногоЗаданияОбменаСФСС(Организации))
		ИЛИ ИспользоватьОбмен Тогда
			ЭлектронныйДокументооборотСФСС.ВключитьОбменСФСС();
	Иначе
		ЭлектронныйДокументооборотСФСС.ОтключитьОбменСФСС();
	КонецЕсли;
	
КонецПроцедуры

#КонецОбласти

///////////////////////////////////////////////////////////////////////////////
// Функции с использованием общих модулей и дублирующие функции для организации
// вызовов общих модулей при документообороте с ФСРАР

Функция СформироватьФайлОтправкиВФСРАРССервера(КороткоеИмяФайлаПакета, СхемаСдачиОтчетности, СтрокаBase64ФайлаПакета, ЭтоАдреса = Истина, Адрес = Неопределено, ЗначениеCAPTCHA = "") Экспорт
	
	Результат = Неопределено;
	
	//файл отправки получаем, объединяя файл пакета с файлами начала и окончания POST-запроса
	
	// файл 1
	ФедеральныйСервер = ПредопределенноеЗначение("Перечисление.СхемыСдачиОтчетностиФСРАР.ФедеральныйСервер");
	ТиповойРегиональныйИФедеральныйСерверы = ПредопределенноеЗначение("Перечисление.СхемыСдачиОтчетностиФСРАР.ТиповойРегиональныйИФедеральныйСерверы");
	МосковскийРегиональныйИФедеральныйСерверы = ПредопределенноеЗначение("Перечисление.СхемыСдачиОтчетностиФСРАР.МосковскийРегиональныйИФедеральныйСерверы");
	
	// формируем POST-данные.
	СодержимоеФайла = "--My1cV8bNdr"
	+ ?(СхемаСдачиОтчетности = ТиповойРегиональныйИФедеральныйСерверы, "",
	Символы.ВК + Символы.ПС + "Content-Disposition: form-data; name=""" + ?(СхемаСдачиОтчетности = МосковскийРегиональныйИФедеральныйСерверы, "import", "format") + """"
	+ Символы.ВК + Символы.ПС
	+ Символы.ВК + Символы.ПС + ?(СхемаСдачиОтчетности = МосковскийРегиональныйИФедеральныйСерверы, "yes", "4.20" + ?(СхемаСдачиОтчетности = ФедеральныйСервер, "", ".region"))
	+ Символы.ВК + Символы.ПС + "--My1cV8bNdr")
	+ ?(НЕ ЗначениеЗаполнено(ЗначениеCAPTCHA), "",
	Символы.ВК + Символы.ПС + "Content-Disposition: form-data; name=""captcha_code"""
	+ Символы.ВК + Символы.ПС
	+ Символы.ВК + Символы.ПС + ЗначениеCAPTCHA
	+ Символы.ВК + Символы.ПС + "--My1cV8bNdr")
	+ Символы.ВК + Символы.ПС + "Content-Disposition: form-data; name=""" + ?(СхемаСдачиОтчетности = ТиповойРегиональныйИФедеральныйСерверы, "fileUpload",
	?(СхемаСдачиОтчетности = МосковскийРегиональныйИФедеральныйСерверы, "file", "file_")) + """; filename=""" + КороткоеИмяФайлаПакета + """"
	+ Символы.ВК + Символы.ПС + "Content-Type: application/octet-stream"
	+ Символы.ВК + Символы.ПС
	+ Символы.ВК + Символы.ПС;
	
	ИмяФайлаЗапросаНачало = ПолучитьИмяВременногоФайла();
	ТекстовыйФайл = Новый ТекстовыйДокумент;
	ТекстовыйФайл.УстановитьТекст(СодержимоеФайла);
	ТекстовыйФайл.Записать(ИмяФайлаЗапросаНачало, "windows-1251", "");
	
	// файл 2
	ИмяФайлаПакета = ПолучитьИмяВременногоФайла();
	
	Если ЭтоАдреса Тогда
		ПолучитьИзВременногоХранилища(СтрокаBase64ФайлаПакета).Записать(ИмяФайлаПакета);
	Иначе
		ДвоичныеДанныеФайлаПакета = Base64Значение(СтрокаBase64ФайлаПакета);
		ДвоичныеДанныеФайлаПакета.Записать(ИмяФайлаПакета);
	КонецЕсли;
	
	// файл 3
	СодержимоеФайла = Символы.ВК + Символы.ПС + "--My1cV8bNdr--";
	
	ИмяФайлаЗапросаОкончание = ПолучитьИмяВременногоФайла();
	ТекстовыйФайл = Новый ТекстовыйДокумент;
	ТекстовыйФайл.УстановитьТекст(СодержимоеФайла);
	ТекстовыйФайл.Записать(ИмяФайлаЗапросаОкончание, "windows-1251", "");
	
	// объединение
	МассивИмен = Новый Массив;
	МассивИмен.Добавить(ИмяФайлаЗапросаНачало);
	МассивИмен.Добавить(ИмяФайлаПакета);
	МассивИмен.Добавить(ИмяФайлаЗапросаОкончание);
	ИмяФайлаОтправки = ПолучитьИмяВременногоФайла();
	ОбъединитьФайлы(МассивИмен, ИмяФайлаОтправки);
	
	Если ЭтоАдреса Тогда
		Результат = ПоместитьВоВременноеХранилище(Новый ДвоичныеДанные(ИмяФайлаОтправки), Адрес);
	Иначе
		ДвоичныеДанныеФайлаПакета = Новый ДвоичныеДанные(ИмяФайлаОтправки);
		Результат = Base64Строка(ДвоичныеДанныеФайлаПакета);
	КонецЕсли;
	
	Возврат Результат;

КонецФункции

Функция ПолучитьСвойстваСертификатаПоОтпечаткуССервера(Отпечаток, ЛогическоеХранилище = Неопределено) Экспорт
	
	Сертификат  = Новый Структура("Отпечаток", Отпечаток);
	Возврат КриптографияЭДКО.НайтиСертификат(Сертификат, Ложь);
	
КонецФункции

Функция ПодставитьПараметрыВСтроку(Знач СтрокаПодстановки,
	Знач Параметр1, Знач Параметр2 = Неопределено, Знач Параметр3 = Неопределено,
	Знач Параметр4 = Неопределено, Знач Параметр5 = Неопределено, Знач Параметр6 = Неопределено,
	Знач Параметр7 = Неопределено, Знач Параметр8 = Неопределено, Знач Параметр9 = Неопределено)
	
	Возврат СтроковыеФункцииКлиентСервер.ПодставитьПараметрыВСтроку(СтрокаПодстановки,
		Параметр1, Параметр2, Параметр3, Параметр4, Параметр5, Параметр6, Параметр7, Параметр8, Параметр9);
		
КонецФункции

Процедура СообщитьПользователю(ТекстСообщения, СообщенияПользователю = Неопределено)
	
	Если СообщенияПользователю <> Неопределено Тогда
		СообщенияПользователю.Добавить(ТекстСообщения);
	Иначе
		ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
	КонецЕсли;
	
КонецПроцедуры

Функция ЗначенияРеквизитовОбъекта(Ссылка, Реквизиты, ВыбратьРазрешенные = Ложь)
	
	Возврат ОбщегоНазначения.ЗначенияРеквизитовОбъекта(Ссылка, Реквизиты, ВыбратьРазрешенные);
	
КонецФункции

Функция ПредставлениеВидаДокумента(Вид)
	
	Возврат РегламентированнаяОтчетность.ПредставлениеВидаДокумента(Вид);
	
КонецФункции

Функция ИННОрганизации(Организация, ЗначениеПоУмолчаниюДляИНН, ПолноеНаименованиеОрганизации = "")
	
	Если Организация <> Неопределено Тогда
		
		Если РегламентированнаяОтчетностьВызовСервера.ЭтоЮридическоеЛицо(Организация) Тогда
			СвойстваОрганизации = РегламентированнаяОтчетностьВызовСервера.ПолучитьСведенияОбОрганизации(Организация, , "НаимЮЛПол, ИННЮЛ");
			Результат = СвойстваОрганизации.ИННЮЛ;
		Иначе
			СвойстваОрганизации = РегламентированнаяОтчетностьВызовСервера.ПолучитьСведенияОбОрганизации(Организация, , "НаимЮЛПол, ИННФЛ");
			Результат = СвойстваОрганизации.ИННФЛ;
		КонецЕсли;
		
		ПолноеНаименованиеОрганизации = СвойстваОрганизации.НаимЮЛПол;
		Возврат Результат;
		
	Иначе
		Возврат ЗначениеПоУмолчаниюДляИНН;
	КонецЕсли;
	
КонецФункции

Процедура ПриИзмененииСтатусаОтправкиДокумента(Ссылка, СтатусОтправки)
	
	ЭлектронныйДокументооборотСКонтролирующимиОрганамиВызовСервераПереопределяемый.ПриИзмененииСтатусаОтправкиДокумента(Ссылка, СтатусОтправки);
	
КонецПроцедуры

//////////////////////////////////////////////////////////////////////////////////
//// ДОКУМЕНТООБОРОТ С ФСРАР, код без использования вызовов общих модулей
////

Функция ОбновитьРезультатКонкретнойОтправкиФСРАРССервера(Знач ОтправкаСсылка, Знач ОтправкаОбъектЗагружен, Знач ПодписанныйПакетДляАвторизации, Знач ПараметрыОтправки, ПараметрыСоединения, ПодписанныйРегиональнымДляОтправкиНаФедеральныйСерверПакетBase64 = Неопределено, Знач ЭтоВызовИзСписка = Ложь, ОсновныеПоляРезультатаОтправки = Неопределено) Экспорт
	
	Если ОтправкаОбъектЗагружен Тогда
		ОсновныеРеквизитыОтправки = Новый Структура("ИмяФайлаПакета, СтатусОтправки, СхемаСдачиОтчетности, СтатусОтправкиНаРегиональныйСервер, ПротоколРегиональногоСервера, ЗаверенныйРегиональнымСерверомПакет, ЗаверенныйПакетОтправлен",
			ОтправкаСсылка.ИмяФайлаПакета, ОтправкаСсылка.СтатусОтправки, ОтправкаСсылка.СхемаСдачиОтчетности, ОтправкаСсылка.СтатусОтправкиНаРегиональныйСервер, ОтправкаСсылка.ПротоколРегиональногоСервера, ОтправкаСсылка.ЗаверенныйРегиональнымСерверомПакет, ОтправкаСсылка.ЗаверенныйПакетОтправлен);
	Иначе
		ОсновныеРеквизитыОтправки = ЗначенияРеквизитовОбъекта(ОтправкаСсылка, "ИмяФайлаПакета, СтатусОтправки, СхемаСдачиОтчетности, СтатусОтправкиНаРегиональныйСервер, ПротоколРегиональногоСервера, ЗаверенныйРегиональнымСерверомПакет, ЗаверенныйПакетОтправлен");
	КонецЕсли;
	
	РезультатОтправки = ПолучитьРезультатОтправкиФСРАР(ОсновныеРеквизитыОтправки, ПодписанныйПакетДляАвторизации, ПараметрыОтправки, ПараметрыСоединения, ЭтоВызовИзСписка, ОтправкаСсылка);
	
	ПараметрыСоединения.СоединениеHTTP = Неопределено;
	
	СохранитьРезультатОтправкиФСРАР(ОтправкаСсылка, РезультатОтправки);
	
	Если (РезультатОтправки <> Неопределено И (ОсновныеРеквизитыОтправки.СхемаСдачиОтчетности = Перечисления.СхемыСдачиОтчетностиФСРАР.ТиповойРегиональныйИФедеральныйСерверы
		ИЛИ ОсновныеРеквизитыОтправки.СхемаСдачиОтчетности = Перечисления.СхемыСдачиОтчетностиФСРАР.МосковскийРегиональныйИФедеральныйСерверы)
		И (ОсновныеРеквизитыОтправки.СтатусОтправкиНаРегиональныйСервер = Перечисления.СтатусыОтправки.Сдан ИЛИ РезультатОтправки.СтатусОтправкиНаРегиональныйСервер = Перечисления.СтатусыОтправки.Сдан)
		И ОсновныеРеквизитыОтправки.ЗаверенныйПакетОтправлен <> Истина И РезультатОтправки.ЗаверенныйПакетОтправлен <> Истина) Тогда
		Если РезультатОтправки.ЗаверенныйРегиональнымСерверомПакет = Неопределено Тогда
			РезультатОтправки.ЗаверенныйРегиональнымСерверомПакет = ОсновныеРеквизитыОтправки.ЗаверенныйРегиональнымСерверомПакет.Получить();
		КонецЕсли;
		ПодписанныйРегиональнымДляОтправкиНаФедеральныйСерверПакетBase64 = Base64Строка(РезультатОтправки.ЗаверенныйРегиональнымСерверомПакет);
	Иначе
		ПодписанныйРегиональнымДляОтправкиНаФедеральныйСерверПакетBase64 = Неопределено;
	КонецЕсли;
	
	Если ОсновныеПоляРезультатаОтправки <> Неопределено И РезультатОтправки <> Неопределено Тогда
		ОсновныеПоляРезультатаОтправки.ДатаПолученияРезультата = РезультатОтправки.ДатаПолученияРезультата;
		ОсновныеПоляРезультатаОтправки.СтатусОтправки = РезультатОтправки.СтатусОтправки;
		ОсновныеПоляРезультатаОтправки.Протокол = РезультатОтправки.Протокол;
		ОсновныеПоляРезультатаОтправки.СтатусОтправкиНаРегиональныйСервер = РезультатОтправки.СтатусОтправкиНаРегиональныйСервер;
		ОсновныеПоляРезультатаОтправки.ЗаверенныйПакетОтправлен = РезультатОтправки.ЗаверенныйПакетОтправлен;
	КонецЕсли;
	
	Возврат (РезультатОтправки <> Неопределено);
	
КонецФункции

Функция СохранитьРезультатОтправкиФСРАР(ОтправкаСсылка, РезультатОтправки)
	
	Если РезультатОтправки <> Неопределено Тогда
		ОтправкаОбъект = ОтправкаСсылка.ПолучитьОбъект();
		ОтправкаОбъект.ДатаПолученияРезультата = РезультатОтправки.ДатаПолученияРезультата;
		ОтправкаОбъект.СтатусОтправки = РезультатОтправки.СтатусОтправки;
		ОтправкаОбъект.Протокол = Новый ХранилищеЗначения(РезультатОтправки.Протокол);
		Если РезультатОтправки.СтатусОтправкиНаРегиональныйСервер <> Неопределено Тогда
			ОтправкаОбъект.СтатусОтправкиНаРегиональныйСервер = РезультатОтправки.СтатусОтправкиНаРегиональныйСервер;
			ОтправкаОбъект.ПротоколРегиональногоСервера = Новый ХранилищеЗначения(РезультатОтправки.Протокол);
		КонецЕсли;
		Если РезультатОтправки.ЗаверенныйРегиональнымСерверомПакет <> Неопределено Тогда
			ОтправкаОбъект.ЗаверенныйРегиональнымСерверомПакет = Новый ХранилищеЗначения(РезультатОтправки.ЗаверенныйРегиональнымСерверомПакет);
		КонецЕсли;
		Если РезультатОтправки.ЗаверенныйПакетОтправлен <> Неопределено Тогда
			ОтправкаОбъект.ЗаверенныйПакетОтправлен = РезультатОтправки.ЗаверенныйПакетОтправлен;
		КонецЕсли;
		ОтправкаОбъект.Записать();
		Возврат Истина;
	Иначе
		Возврат Ложь;
	КонецЕсли;
	
КонецФункции

Функция ПолучитьРезультатОтправкиФСРАР(ОсновныеРеквизитыОтправки, ПодписанныйПакетДляАвторизации, ПараметрыОтправки, ПараметрыСоединения, ЭтоВызовИзСписка, ОтправкаСсылка)
	
	СтатусОтчета = "";
	ЦветСтатуса = ЦветаСтиля.ЦветНезавершившейсяОтправкиБРО;
	СтатусОтправки = Перечисления.СтатусыОтправки.Отправлен;
	
	//запрос протокола обработки
	ПараметрыСоединения.ОписаниеОшибкиСервером = "";
	ПараметрыСоединения.ПовторятьСоединение = Ложь;
	ПараметрыСоединения.ПовторятьЗапросПароля = Ложь;
	
	ПолучениеРезультатовОтправкиПослеРегиональногоНаФедеральномПортале = ((ПараметрыОтправки.СхемаСдачиОтчетности = Перечисления.СхемыСдачиОтчетностиФСРАР.ТиповойРегиональныйИФедеральныйСерверы
		ИЛИ ПараметрыОтправки.СхемаСдачиОтчетности = Перечисления.СхемыСдачиОтчетностиФСРАР.МосковскийРегиональныйИФедеральныйСерверы)
		И ОсновныеРеквизитыОтправки.ЗаверенныйПакетОтправлен = Истина);
	ПолученыРезультатыРегиональногоПередОтправкойНаФедеральныйПортал = ((ПараметрыОтправки.СхемаСдачиОтчетности = Перечисления.СхемыСдачиОтчетностиФСРАР.ТиповойРегиональныйИФедеральныйСерверы
		ИЛИ ПараметрыОтправки.СхемаСдачиОтчетности = Перечисления.СхемыСдачиОтчетностиФСРАР.МосковскийРегиональныйИФедеральныйСерверы)
		И НЕ ПолучениеРезультатовОтправкиПослеРегиональногоНаФедеральномПортале И ОсновныеРеквизитыОтправки.СтатусОтправкиНаРегиональныйСервер = Перечисления.СтатусыОтправки.Сдан);
	ПолучениеРезультатовОтправкиНаРегиональномПортале = ((ПараметрыОтправки.СхемаСдачиОтчетности = Перечисления.СхемыСдачиОтчетностиФСРАР.ТиповойРегиональныйИФедеральныйСерверы
		ИЛИ ПараметрыОтправки.СхемаСдачиОтчетности = Перечисления.СхемыСдачиОтчетностиФСРАР.МосковскийРегиональныйИФедеральныйСерверы)
		И НЕ ПолучениеРезультатовОтправкиПослеРегиональногоНаФедеральномПортале И НЕ ПолученыРезультатыРегиональногоПередОтправкойНаФедеральныйПортал);
	ФайлЗаверенногоРегиональнымСерверомПакета = Неопределено;
	НаходитсяВОчередиЗагрузки = Ложь;
	
	Если ПолучениеРезультатовОтправкиПослеРегиональногоНаФедеральномПортале Тогда
		// декларация загружена на региональный портал и на федеральный, получение протокола с федерального
		ПараметрыОтправкиНаФедеральныйПортал = Новый Структура("Организация, ФедеральныйПортал, КодРегиона, КодИНаименованиеРегиона, СсылкаНаПорталОнлайнПроверки, СхемаСдачиОтчетности, ПоляДляЗаписиОтправки",
			ПараметрыОтправки.Организация, Истина, ПараметрыОтправки.КодРегиона, ПараметрыОтправки.КодИНаименованиеРегиона, "", Перечисления.СхемыСдачиОтчетностиФСРАР.ФедеральныйСервер, Неопределено);
		ЗаголовокДекларации = ПолучитьИнформациюОДекларацииНаПорталеФСРАР(ОсновныеРеквизитыОтправки.ИмяФайлаПакета + ".sig", ПодписанныйПакетДляАвторизации, ПараметрыОтправкиНаФедеральныйПортал, ПараметрыСоединения);
		Если ЗаголовокДекларации <> Неопределено Тогда
			ЗаголовокДекларации.РегиональныйПротокол = ОсновныеРеквизитыОтправки.ПротоколРегиональногоСервера;
		КонецЕсли;
		
	ИначеЕсли ПолученыРезультатыРегиональногоПередОтправкойНаФедеральныйПортал Тогда
		// декларация загружена на региональный портал и успешно обработана, но еще не отправлялась на федеральный, возвращает протокол регионального портала в виде хранилища значения
		ЗаголовокДекларации = Новый Структура("ИдентификаторЗаписи, ИмяФайла, Состояние, ЗначениеСостояния, ДатаЗагрузки, РегиональныйПротокол, ОшибкаПолученияРегиональногоПротокола, ФедеральныйПротокол, ОшибкаПолученияФедеральногоПротокола",
			"", ОсновныеРеквизитыОтправки.ИмяФайлаПакета, "", Неопределено, Неопределено, ОсновныеРеквизитыОтправки.ПротоколРегиональногоСервера, "", Неопределено, "");
		
	Иначе
		// декларация загружена на общий портал
		ЗаголовокДекларации = ПолучитьИнформациюОДекларацииНаПорталеФСРАР(ОсновныеРеквизитыОтправки.ИмяФайлаПакета, ПодписанныйПакетДляАвторизации, ПараметрыОтправки, ПараметрыСоединения);
	КонецЕсли;
	
	УспешноОбработана = ПолученыРезультатыРегиональногоПередОтправкойНаФедеральныйПортал;
	ОбработанаСОшибками = Ложь;
	ОшибкаЗапросаПодписанногоРегиональнымПорталомПакета = Ложь;
	Если ЗаголовокДекларации <> Неопределено И НЕ ПолученыРезультатыРегиональногоПередОтправкойНаФедеральныйПортал Тогда
		
		ОсновнойПротоколПуст = Неопределено;
		Если ПолучениеРезультатовОтправкиНаРегиональномПортале Тогда
			УспешноОбработанаПоОсновномуПротоколу = УспешноОбработанаДекларацияФСРАР(ЗаголовокДекларации.РегиональныйПротокол, ПараметрыОтправки.СхемаСдачиОтчетности, НаходитсяВОчередиЗагрузки, ОсновнойПротоколПуст);
		Иначе
			УспешноОбработанаПоОсновномуПротоколу = УспешноОбработанаДекларацияФСРАР(ЗаголовокДекларации.ФедеральныйПротокол, Перечисления.СхемыСдачиОтчетностиФСРАР.ФедеральныйСервер, НаходитсяВОчередиЗагрузки, ОсновнойПротоколПуст);
		КонецЕсли;
		ОбработанаСОшибкамиПоДополнительномуПротоколу = Ложь;
		Если НЕ ПараметрыОтправки.ФедеральныйПортал И НЕ ПолучениеРезультатовОтправкиПослеРегиональногоНаФедеральномПортале И НЕ ПолучениеРезультатовОтправкиНаРегиональномПортале Тогда
			УспешноОбработанаПоРегиональномуПротоколу = УспешноОбработанаДекларацияФСРАР(ЗаголовокДекларации.РегиональныйПротокол, ПараметрыОтправки.СхемаСдачиОтчетности, НаходитсяВОчередиЗагрузки);
			ОбработанаСОшибкамиПоДополнительномуПротоколу = (УспешноОбработанаПоРегиональномуПротоколу <> Неопределено И НЕ УспешноОбработанаПоРегиональномуПротоколу);
		КонецЕсли;
		
		Если ОтправкаСсылка.ВидОтчета = Справочники.ВидыОтправляемыхДокументов.ОбъемСобранногоВиноградаДляПроизводстваВинодельческойПродукции
			И УспешноОбработанаПоОсновномуПротоколу = Неопределено
			И ЗаголовокДекларации.ФедеральныйПротокол <> Неопределено И ЗаголовокДекларации.ФедеральныйПротокол.Количество() = 0 Тогда
			
			УспешноОбработанаПоОсновномуПротоколу = УспешноОбработанаПоРегиональномуПротоколу;
		КонецЕсли;
		
		УспешноОбработана = УспешноОбработанаПоОсновномуПротоколу <> Неопределено И УспешноОбработанаПоОсновномуПротоколу И НЕ ОбработанаСОшибкамиПоДополнительномуПротоколу;
		
		Если УспешноОбработана И ПолучениеРезультатовОтправкиНаРегиональномПортале Тогда
			
			Если НЕ ЗначениеЗаполнено(ФайлЗаверенногоРегиональнымСерверомПакета) И НЕ ПараметрыСоединения.ПовторятьСоединение Тогда
				Если ЗначениеЗаполнено(ПараметрыСоединения.ОписаниеОшибкиСервером) Тогда
					СообщитьПользователю(ПодставитьПараметрыВСтроку(НСтр("ru = 'Подписанная декларация не получена с сервера. %1'"), ПараметрыСоединения.ОписаниеОшибкиСервером), ПараметрыСоединения.СообщенияПользователю);
				Иначе
					СообщитьПользователю(НСтр("ru = 'Не удалось получить декларацию с подписью регионального сервера Росалкогольрегулирования.'"), ПараметрыСоединения.СообщенияПользователю);
				КонецЕсли;
				ПараметрыСоединения.ОписаниеОшибкиСервером = "";
				
				УспешноОбработанаПоОсновномуПротоколу = Неопределено;
				УспешноОбработана = Ложь;
				ОшибкаЗапросаПодписанногоРегиональнымПорталомПакета = Истина;
			КонецЕсли;
			
		КонецЕсли;
		
		ОбработанаСОшибками = (УспешноОбработанаПоОсновномуПротоколу <> Неопределено И НЕ УспешноОбработана) ИЛИ (ОбработанаСОшибкамиПоДополнительномуПротоколу
			И ОсновнойПротоколПуст И ПараметрыОтправки.СхемаСдачиОтчетности = Перечисления.СхемыСдачиОтчетностиФСРАР.ОбщийРегиональныйИФедеральныйСервер);
		
	КонецЕсли;
	
	Если ЗаголовокДекларации <> Неопределено И НЕ ПараметрыСоединения.ПовторятьСоединение Тогда
		
		// получен протокол обработки и отсутствовали ошибки соединения, в том числе при получении подписанного региональным порталом пакета
		
		// анализируем содержимое страницы протокола обработки
		Если УспешноОбработана Тогда
			// есть ссылка на квитанцию - отчет принят
			Если ПолученыРезультатыРегиональногоПередОтправкойНаФедеральныйПортал ИЛИ ПолучениеРезультатовОтправкиНаРегиональномПортале Тогда
				СтатусОтправкиНаРегиональныйСервер = Перечисления.СтатусыОтправки.Сдан;
				СтатусОтчета = НСтр("ru = 'Сдан, ошибок не обнаружено'");
			Иначе
				СтатусОтправки = Перечисления.СтатусыОтправки.Сдан;
				СтатусОтчета = НСтр("ru = 'Сдан, ошибок не обнаружено'");
				ЦветСтатуса = ЦветаСтиля.ЦветПоложительногоПротокола;
			КонецЕсли;
			
		ИначеЕсли ОбработанаСОшибками Тогда
			// отчет не принят
			СтатусОтправки = Перечисления.СтатусыОтправки.НеПринят;
			Если ПолучениеРезультатовОтправкиНаРегиональномПортале Тогда
				СтатусОтправкиНаРегиональныйСервер = Перечисления.СтатусыОтправки.НеПринят;
			КонецЕсли;
			Если ОшибкаЗапросаПодписанногоРегиональнымПорталомПакета Тогда
				СтатусОтчета = НСтр("ru = 'Не принят, обнаружены ошибки'");
			Иначе
				СтатусОтчета = НСтр("ru = 'Не принят, обнаружены ошибки'");
			КонецЕсли;
			ЦветСтатуса = ЦветаСтиля.ЦветОшибкиВПротоколеБРО;
			
		Иначе
			// отчет еще не обработан
			СтатусОтчета = НСтр("ru = 'Находится в обработке, попробуйте через некоторое время использовать в отчете кнопку ""Обновить""'");
		КонецЕсли;
		
		Протокол = ГенерироватьHTMLПротоколаОбработкиФСРАР(
			ЗаголовокДекларации,
			СтатусОтчета,
			ЦветСтатуса,
			ПараметрыОтправки.ФедеральныйПортал,
			ОтправкаСсылка,
			ОбработанаСОшибками,
			СтатусОтправки);
		
		ЗаверенныйРегиональнымСерверомПакет = Неопределено;
		Если ЗначениеЗаполнено(ФайлЗаверенногоРегиональнымСерверомПакета) Тогда
			ЗаверенныйРегиональнымСерверомПакет = Новый ДвоичныеДанные(ФайлЗаверенногоРегиональнымСерверомПакета);
		КонецЕсли;
		
		//заполняем результирующую структуру
		Результат = Новый Структура("ДатаПолученияРезультата, СтатусОтправки, Протокол, СтатусОтправкиНаРегиональныйСервер, ЗаверенныйРегиональнымСерверомПакет, ЗаверенныйПакетОтправлен",
			ТекущаяДатаСеанса(), СтатусОтправки, Протокол, ?(ПолучениеРезультатовОтправкиНаРегиональномПортале, СтатусОтправкиНаРегиональныйСервер, Неопределено), ЗаверенныйРегиональнымСерверомПакет, Неопределено);
		
	ИначеЕсли ЗначениеЗаполнено(ПараметрыСоединения.ОписаниеОшибкиСервером) ИЛИ НЕ ПараметрыСоединения.ПовторятьСоединение Тогда
		
		Если ЗначениеЗаполнено(ПараметрыСоединения.ОписаниеОшибкиСервером) Тогда
			Если ПараметрыСоединения.ОписаниеОшибкиСервером <> НСтр("ru = 'Выполнение операции не поддерживается'") Тогда
				ТекстСообщения = СтрШаблон(
					НСтр("ru = 'Результаты отправки не получены с сервера. %1'"),
					ПараметрыСоединения.ОписаниеОшибкиСервером);
				
				Если ЭтоВызовИзСписка Тогда
					ПредставлениеОтчетаОтправки = ПредставлениеОтчета(ОтправкаСсылка.ОтчетСсылка);
					
					ТекстСообщения = СтрШаблон(
						НСтр("ru = '%1 Попробуйте через некоторое время использовать кнопку ""Обновить"" в отчете %2.'"),
						ТекстСообщения,
						ПредставлениеОтчетаОтправки);
				КонецЕсли;
				
				СообщитьПользователю(ТекстСообщения, ПараметрыСоединения.СообщенияПользователю);
			КонецЕсли;
			
		Иначе
			ТекстСообщения = НСтр("ru = 'Не удалось получить результаты отправки с сервера Росалкогольрегулирования.'");
			
			Если ЭтоВызовИзСписка Тогда
				ПредставлениеОтчетаОтправки = ПредставлениеОтчета(ОтправкаСсылка.ОтчетСсылка);
				
				ТекстСообщения = СтрШаблон(
					НСтр("ru = '%1 Возможно, сервер перегружен. Попробуйте через некоторое время использовать кнопку ""Обновить"" в отчете %2.'"),
					ТекстСообщения,
					ПредставлениеОтчетаОтправки);
			КонецЕсли;
			
			СообщитьПользователю(ТекстСообщения, ПараметрыСоединения.СообщенияПользователю);
		КонецЕсли;
		
		Результат = Неопределено;
		
	Иначе
		
		// возможно, проблемы с доступом в интернет
		Результат = Неопределено;
		
	КонецЕсли;
	
	Если ЗначениеЗаполнено(ФайлЗаверенногоРегиональнымСерверомПакета) Тогда
		ОперацииСФайламиЭДКО.УдалитьВременныйФайл(ФайлЗаверенногоРегиональнымСерверомПакета);
	КонецЕсли;
	
	ПараметрыСоединения.ОписаниеОшибкиСервером = "";
	
	Возврат Результат;
	
КонецФункции

Функция ОтправкаНаСерверФСРАР(ФайлыИДанныеОтправки, ОтчетСсылка, ПараметрыОтправки, ПараметрыСоединения, Данные64CAPTCHA = "")
	
	Данные64CAPTCHA = "";
	ПараметрыСоединения.ОписаниеОшибкиСервером = "";
	ПараметрыСоединения.ПовторятьСоединение = Ложь;
	ПараметрыСоединения.ПовторятьЗапросПароля = Ложь;
	ЗаголовокДекларации = Неопределено;
	
	ТекстСообщенияПриУспехе = ОтправитьЗашифрованныйПакетНаСерверФСРАР(ФайлыИДанныеОтправки, ПараметрыОтправки, ПараметрыСоединения, Данные64CAPTCHA);
	ОтправкаПроизведена = (ТекстСообщенияПриУспехе <> Неопределено);
	
	Если ОтправкаПроизведена Тогда
		
		ПараметрыСоединения.ПовторятьСоединение = Ложь;
		
		УспешноОбработана = Неопределено;
		РегиональныйПротоколHTML = "";
		
		Если ЗаголовокДекларации <> Неопределено Тогда
			
			УспешноОбработана = УспешноОбработанаДекларацияФСРАР(ЗаголовокДекларации.РегиональныйПротокол, ПараметрыОтправки.СхемаСдачиОтчетности, Истина);
			
			Если УспешноОбработана = Ложь Тогда
				СтатусОтчета = НСтр("ru = 'Отчет не принят, обнаружены ошибки.'");
				ЦветСтатуса = Новый Цвет(255, 0, 0);
			Иначе
				СтатусОтчета = НСтр("ru = 'Отчет принят региональным сервером и будет отправлен на федеральный сервер.'");
				ЦветСтатуса = Новый Цвет(0, 0, 192);
			КонецЕсли;
			
			ОтправкаСсылка = ПолучитьПоследнююОтправкуОтчетаВФСРАР(ОтчетСсылка);
			РегиональныйПротоколHTML = ГенерироватьHTMLПротоколаОбработкиФСРАР(ЗаголовокДекларации, СтатусОтчета, ЦветСтатуса, Ложь, ОтправкаСсылка, НЕ УспешноОбработана);
			
		КонецЕсли;
		
		//отправленный файл принят сервером
		НоваяЗапись = Справочники.ОтправкиФСРАР.СоздатьЭлемент();
		НоваяЗапись.ОтчетСсылка = ОтчетСсылка;
		НоваяЗапись.ИдентификаторОтправкиНаСервере = ПараметрыСоединения.ИдентификаторОтправкиНаСервере;
		ДвоичныеДанныеПакета = Новый ДвоичныеДанные(ФайлыИДанныеОтправки.ИмяФайлаПакета);
		НоваяЗапись.ЗашифрованныйПакет = Новый ХранилищеЗначения(ДвоичныеДанныеПакета);
		НоваяЗапись.ИмяФайлаПакета = ФайлыИДанныеОтправки.КороткоеИмяФайлаПакета;
		
		Если УспешноОбработана = Ложь Тогда
			НоваяЗапись.СтатусОтправки = Перечисления.СтатусыОтправки.НеПринят;
			НоваяЗапись.Протокол = Новый ХранилищеЗначения(РегиональныйПротоколHTML);
			
		Иначе
			НоваяЗапись.СтатусОтправки = Перечисления.СтатусыОтправки.Отправлен;
		КонецЕсли;
		
		НоваяЗапись.ДатаОтправки = ТекущаяДатаСеанса();
		НоваяЗапись.Организация = ?(ОтчетСсылка <> Неопределено, ОтчетСсылка.Организация, Неопределено);
		НоваяЗапись.СхемаСдачиОтчетности = ПараметрыОтправки.СхемаСдачиОтчетности;
		
		Если ПараметрыОтправки.СхемаСдачиОтчетности = Перечисления.СхемыСдачиОтчетностиФСРАР.ТиповойРегиональныйИФедеральныйСерверы
			ИЛИ ПараметрыОтправки.СхемаСдачиОтчетности = Перечисления.СхемыСдачиОтчетностиФСРАР.МосковскийРегиональныйИФедеральныйСерверы Тогда
			
			Если УспешноОбработана = Ложь Тогда
				НоваяЗапись.СтатусОтправкиНаРегиональныйСервер = Перечисления.СтатусыОтправки.НеПринят;
			Иначе
				НоваяЗапись.СтатусОтправкиНаРегиональныйСервер = Перечисления.СтатусыОтправки.Отправлен;
			КонецЕсли;
			
			Если ЗначениеЗаполнено(РегиональныйПротоколHTML) Тогда
				НоваяЗапись.ПротоколРегиональногоСервера = Новый ХранилищеЗначения(РегиональныйПротоколHTML);
			КонецЕсли;
			
			НоваяЗапись.ЗаверенныйПакетОтправлен = Ложь;
			
		КонецЕсли;
		
		НоваяЗапись.ДатаНачалаПериода = ПараметрыОтправки.ПоляДляЗаписиОтправки.ДатаНачалаПериода;
		НоваяЗапись.ДатаОкончанияПериода = ПараметрыОтправки.ПоляДляЗаписиОтправки.ДатаОкончанияПериода;
		
		Если ОтчетСсылка <> Неопределено И ТипЗнч(ОтчетСсылка) = Тип("ДокументСсылка.РегламентированныйОтчет") Тогда
			НоваяЗапись.ВидОтчета = ВидОтправляемыхДокументовДляФСРАР(ОтчетСсылка.ИсточникОтчета);
			
		Иначе // электронные представления
			НоваяЗапись.ВидОтчета = ?(ОтчетСсылка <> Неопределено, ОтчетСсылка.ВидОтчета, Неопределено);
		КонецЕсли;
		
		НоваяЗапись.Версия = ПараметрыОтправки.ПоляДляЗаписиОтправки.Версия;
		НоваяЗапись.ПредставлениеПериода = ПредставлениеПериода(НоваяЗапись.ДатаНачалаПериода, КонецДня(НоваяЗапись.ДатаОкончанияПериода), "ФП=Истина");
		НоваяЗапись.ПредставлениеВидаДокумента = ПредставлениеВидаДокумента(НоваяЗапись.Версия);
		НоваяЗапись.Записать();
		
		Результат = Новый Структура("НоваяОтправкаСсылка, УспешноОбработана", НоваяЗапись.Ссылка, УспешноОбработана);
		
		Возврат Результат;
		
	ИначеЕсли (ЗначениеЗаполнено(ПараметрыСоединения.ОписаниеОшибкиСервером) ИЛИ НЕ ПараметрыСоединения.ПовторятьСоединение)
		И НЕ ЗначениеЗаполнено(Данные64CAPTCHA) И ПараметрыСоединения.ТребуетсяРегистрация <> Истина И ПараметрыСоединения.ТребуетсяПовторнаяАвторизация <> Истина Тогда
		
		ЭтоРегламентированныйОтчет = (ТипЗнч(ОтчетСсылка) = Тип("ДокументСсылка.РегламентированныйОтчет") ИЛИ ТипЗнч(ОтчетСсылка) = Тип("Неопределено"));
		
		Если ЗначениеЗаполнено(ПараметрыСоединения.ОписаниеОшибкиСервером) Тогда
			
			//отправленный файл не был принят сервером, получено описание ошибки
			Если Прав(ПараметрыСоединения.ОписаниеОшибкиСервером, 1) <> "." И Прав(ПараметрыСоединения.ОписаниеОшибкиСервером, 1) <> "!" Тогда
				ПараметрыСоединения.ОписаниеОшибкиСервером = ПараметрыСоединения.ОписаниеОшибкиСервером + ".";
			КонецЕсли;
			СообщитьПользователю(ПодставитьПараметрыВСтроку(НСтр("ru = 'Отчет не принят сервером. %1'"), ПараметрыСоединения.ОписаниеОшибкиСервером) + " " + ?(ЭтоРегламентированныйОтчет,
				НСтр("ru = 'Для выгрузки отчета в файл используйте меню ""Выгрузка"" - ""Выгрузить для портала ФСРАР"".'"), НСтр("ru = 'Для выгрузки отчета в файл используйте меню ""Еще"" - ""Выгрузить"".'"))
				+ " " + НСтр("ru = 'С информацией о порядке представления отчета ознакомьтесь на сайте http://service.fsrar.ru'"), ПараметрыСоединения.СообщенияПользователю);
			
		Иначе
			// в ответе сервера отсутствует описание ошибки и установлен признак повтора соединения
			СообщитьПользователю(НСтр("ru = 'Зашифрованный пакет не удалось отправить на сервер Росалкогольрегулирования.'") + " " + ?(ЭтоРегламентированныйОтчет,
				НСтр("ru = 'Для выгрузки отчета в файл используйте меню ""Выгрузка"" - ""Выгрузить для портала ФСРАР"".'"), НСтр("ru = 'Для выгрузки отчета в файл используйте меню ""Еще"" - ""Выгрузить"".'"))
				+ " " + НСтр("ru = 'С информацией о порядке представления отчета ознакомьтесь на сайте http://service.fsrar.ru'"), ПараметрыСоединения.СообщенияПользователю);
		КонецЕсли;
		
	КонецЕсли;
	
	Возврат Неопределено;
	
КонецФункции

Функция ОтправитьВФСРАРССервера(ЭтоЭлектроннаяПодписьВМоделиСервиса, Знач СтрокаBase64ФайлаОтправки, Знач СтрокаBase64ФайлаПакета, Знач ПодписанныйПакетДляАвторизации, Знач КороткоеИмяФайлаПакета, Знач ОтчетСсылка, Знач ПараметрыОтправки, ПараметрыСоединения, Данные64CAPTCHA = "") Экспорт
	
	// получаем двоичные данные файлов
	Если ЭтоЭлектроннаяПодписьВМоделиСервиса Тогда
		ДвоичныеДанныеФайлаОтправки = ПолучитьИзВременногоХранилища(СтрокаBase64ФайлаОтправки);
		ДвоичныеДанныеФайлаПакета = ПолучитьИзВременногоХранилища(СтрокаBase64ФайлаПакета);
	Иначе
		ДвоичныеДанныеФайлаОтправки = Base64Значение(СтрокаBase64ФайлаОтправки);
		ДвоичныеДанныеФайлаПакета = Base64Значение(СтрокаBase64ФайлаПакета);
	КонецЕсли;
	
	// выгружаем двоичные данные в файл
	ИмяФайлаОтправки = ПолучитьИмяВременногоФайла();
	ДвоичныеДанныеФайлаОтправки.Записать(ИмяФайлаОтправки);
	
	ИмяФайлаПакета = ПолучитьИмяВременногоФайла();
	ДвоичныеДанныеФайлаПакета.Записать(ИмяФайлаПакета);
	
	ФайлыИДанныеОтправки = Новый Структура("ПодписанныйПакетДляАвторизации, КороткоеИмяФайлаПакета, ИмяФайлаОтправки, ИмяФайлаПакета", ПодписанныйПакетДляАвторизации, КороткоеИмяФайлаПакета, ИмяФайлаОтправки, ИмяФайлаПакета);
	
	Результат = ОтправкаНаСерверФСРАР(ФайлыИДанныеОтправки, ОтчетСсылка, ПараметрыОтправки, ПараметрыСоединения, Данные64CAPTCHA);
	
	ПараметрыСоединения.СоединениеHTTP = Неопределено;
	ПараметрыСоединения.ОписаниеОшибкиСервером = "";
	
	ОперацииСФайламиЭДКО.УдалитьВременныйФайл(ИмяФайлаОтправки);
	ОперацииСФайламиЭДКО.УдалитьВременныйФайл(ИмяФайлаПакета);
	
	Возврат Результат;
	
КонецФункции

Функция ГенерироватьHTMLПротоколаОбработкиФСРАР(
	ЗаголовокДекларации,
	СтатусОтчета,
	ЦветСтатуса,
	ФедеральныйПортал,
	ОтправкаСсылка,
	ЕстьОшибки = Ложь,
	СтатусОтправки = Неопределено)
	
	ТабДок = Новый ТабличныйДокумент;
	
	Шаблон = ПолучитьМакет("ФСРАРШаблонДляHTML");
	
	СекцияШапка = Шаблон.ПолучитьОбласть("ШапкаСЗаголовком");

	СвойстваОрганизации 			= РегламентированнаяОтчетностьВызовСервера.ПолучитьСведенияОбОрганизации(ОтправкаСсылка.Организация, ,"НаимЮЛПол, ИННЮЛ");
	ПолноеНаименованиеОрганизации 	= СвойстваОрганизации.НаимЮЛПол;
	ИННОрганизации 					= СвойстваОрганизации.ИННЮЛ;

	// Наименование протокола 
	НаименованиеПротокола = "";
	Если ЕстьОшибки Тогда
		НаименованиеПротокола = НСтр("ru = 'Протокол ошибок'");
	Иначе
		НаименованиеПротокола = НСтр("ru = 'Протокол о сдаче'");
	КонецЕсли;
	СекцияШапка.Параметры.НаименованиеПротокола = НаименованиеПротокола;
	
	СведенияПоОбъекту 	= СведенияПоОтправляемымОбъектам(ОтправкаСсылка.ОтчетСсылка);
	НаименованиеОргана 	= СведенияПоОбъекту.ПредставлениеКонтролирующегоОргана;
	НаименованиеОтчета 	= СведенияПоОбъекту.Наименование;

	СекцияШапка.Параметры.Организация 	= ПолноеНаименованиеОрганизации;
	СекцияШапка.Параметры.ИНН 			= НСтр("ru = 'ИНН '") + ИННОрганизации;
	СекцияШапка.Параметры.Получатель 	= НаименованиеОргана;
	СекцияШапка.Параметры.Отчет 		= НаименованиеОтчета;
	СекцияШапка.Параметры.Файл 			= ЗаголовокДекларации.ИмяФайла;
	СекцияШапка.Параметры.СтатусОтчета 	= СтатусОтчета;
	СекцияШапка.Области.СтатусОтчета.ЦветТекста = ЦветСтатуса;
			
	ТабДок.Вывести(СекцияШапка);
	
	Если СтатусОтправки = Перечисления.СтатусыОтправки.Сдан Тогда
		РезультатыСверкиОстатковПолучены = Ложь;
		СверкаОстатковУспешна = Ложь;
		РезультатыСверкиКонтрагентовПолучены = Ложь;
		СверкаКонтрагентовУспешна = Ложь;
		
		ПротоколыДекларации = Новый Массив;
		Если ЗаголовокДекларации.ФедеральныйПротокол <> Неопределено Тогда
			ПротоколыДекларации.Добавить(ЗаголовокДекларации.ФедеральныйПротокол);
		КонецЕсли;
		Если ЗаголовокДекларации.РегиональныйПротокол <> Неопределено Тогда
			ПротоколыДекларации.Добавить(ЗаголовокДекларации.РегиональныйПротокол);
		КонецЕсли;
		
		Для каждого ПротоколДекларации Из ПротоколыДекларации Цикл
			Для каждого СтрокаПротокола Из ПротоколДекларации Цикл
				Если НЕ РезультатыСверкиОстатковПолучены
					И СтрНайти(СтрокаПротокола.Сообщение, "Сверка остатков") > 0 Тогда
					РезультатыСверкиОстатковПолучены = Истина;
					
					Если СтрНайти(СтрокаПротокола.Сообщение, "прошла успешно")
						ИЛИ СтрНайти(СтрокаПротокола.Сообщение, "не требуется")
						ИЛИ СтрНайти(СтрокаПротокола.Сообщение, "не проведена") Тогда
						СверкаОстатковУспешна = Истина;
					КонецЕсли;
				КонецЕсли;
				
				Если НЕ РезультатыСверкиКонтрагентовПолучены
					И (СтрНайти(СтрокаПротокола.Сообщение, "Сверка с контрагентами") > 0
					ИЛИ СтрНайти(СтрокаПротокола.Сообщение, "сверки по контрагентам") > 0) Тогда
					РезультатыСверкиКонтрагентовПолучены = Истина;
					
					Если СтрНайти(СтрокаПротокола.Сообщение, "прошла успешно")
						ИЛИ СтрНайти(СтрокаПротокола.Сообщение, "не требуется")
						ИЛИ СтрНайти(СтрокаПротокола.Сообщение, "не проведена")
						ИЛИ СтрНайти(СтрокаПротокола.Сообщение, "сверки по контрагентам") Тогда
						СверкаКонтрагентовУспешна = Истина;
					КонецЕсли;
				КонецЕсли;
			КонецЦикла;
			
			Если РезультатыСверкиОстатковПолучены И РезультатыСверкиКонтрагентовПолучены Тогда
				Прервать;
			КонецЕсли;
		КонецЦикла;
		
		Если НЕ СверкаОстатковУспешна ИЛИ НЕ СверкаКонтрагентовУспешна Тогда
			СекцияШапка = Шаблон.ПолучитьОбласть("ШапкаДополнениеЗаголовка");
			СекцияШапка.Параметры.ДополнительныйСтатусОтчета = ?(НЕ РезультатыСверкиОстатковПолучены ИЛИ НЕ РезультатыСверкиКонтрагентовПолучены,
				НСтр("ru = 'Для получения информации о результатах сверки с контрагентами и проверки остатков
						   |обновите состояние отчета позже.
						   |При выявлении расхождений отправьте корректирующую декларацию.'"),
				НСтр("ru = 'При выявлении расхождений по результатам сверки с контрагентами или проверки остатков
						   |отправьте корректирующую декларацию.'"));
			СекцияШапка.Области.ДополнительныйСтатусОтчета.ЦветТекста = Новый Цвет(0, 0, 255);
			ТабДок.Вывести(СекцияШапка);
		КонецЕсли;
	КонецЕсли;
	
	СекцияШапка = Шаблон.ПолучитьОбласть("ШапкаРазделительПослеЗаголовка");
	ТабДок.Вывести(СекцияШапка);
	
	Если НЕ ФедеральныйПортал Тогда
		
		// выводим заголовок и региональный протокол
		
		СекцияШапка = Шаблон.ПолучитьОбласть("Шапка");
		ТабДок.Вывести(СекцияШапка);
		
		Если ЗаголовокДекларации.РегиональныйПротокол <> Неопределено И ТипЗнч(ЗаголовокДекларации.РегиональныйПротокол) <> Тип("ХранилищеЗначения") Тогда
			СекцияСтрока = Шаблон.ПолучитьОбласть("Строка");
			Для Каждого СтрокаРегиональногоПротокола Из ЗаголовокДекларации.РегиональныйПротокол Цикл
				
				СекцияСтрока.Параметры.Дата = ДатаИзСтрокиРазныхФорматов(СтрокаРегиональногоПротокола.Дата);
				СекцияСтрока.Параметры.Модуль = СтрокаРегиональногоПротокола.Модуль;
				СекцияСтрока.Параметры.Сообщение = СтрокаРегиональногоПротокола.Сообщение;
				ТабДок.Вывести(СекцияСтрока);
				
			КонецЦикла;
			
		КонецЕсли;
		
		СекцияПодвал = Шаблон.ПолучитьОбласть("Подвал");
		ТабДок.Вывести(СекцияПодвал);
		
		Если ЗначениеЗаполнено(ЗаголовокДекларации.ОшибкаПолученияРегиональногоПротокола) Тогда
			СекцияОписаниеОшибки = Шаблон.ПолучитьОбласть("ОписаниеОшибки");
			СекцияОписаниеОшибки.Параметры.ОписаниеОшибки = ЗаголовокДекларации.ОшибкаПолученияРегиональногоПротокола;
			ТабДок.Вывести(СекцияОписаниеОшибки);
		КонецЕсли;
		
		СекцияРазделительПротоколов = Шаблон.ПолучитьОбласть("РазделительПротоколов");
		ТабДок.Вывести(СекцияРазделительПротоколов);
		
	КонецЕсли;
	
	// выводим федеральный протокол и статус
	
	СекцияШапка = Шаблон.ПолучитьОбласть("ШапкаФедеральный");
	ТабДок.Вывести(СекцияШапка);
	
	Если ЗаголовокДекларации.ФедеральныйПротокол <> Неопределено Тогда
		СекцияСтрока = Шаблон.ПолучитьОбласть("СтрокаФедеральный");
		Для Каждого СтрокаФедеральногоПротокола Из ЗаголовокДекларации.ФедеральныйПротокол Цикл
			
			СекцияСтрока.Параметры.ДатаФедеральный = ДатаИзСтрокиРазныхФорматов(СтрокаФедеральногоПротокола.Дата);
			СекцияСтрока.Параметры.МодульФедеральный = СтрокаФедеральногоПротокола.Модуль;
			СекцияСтрока.Параметры.СообщениеФедеральный = СтрокаФедеральногоПротокола.Сообщение;
			ТабДок.Вывести(СекцияСтрока);
			
		КонецЦикла;
	КонецЕсли;
	
	СекцияПодвал = Шаблон.ПолучитьОбласть("ПодвалФедеральный");
	ТабДок.Вывести(СекцияПодвал);
	
	Если ЗначениеЗаполнено(ЗаголовокДекларации.ОшибкаПолученияФедеральногоПротокола) Тогда
		СекцияОписаниеОшибки = Шаблон.ПолучитьОбласть("ОписаниеОшибкиФедеральный");
		СекцияОписаниеОшибки.Параметры.ОписаниеОшибкиФедеральный = ЗаголовокДекларации.ОшибкаПолученияФедеральногоПротокола;
		ТабДок.Вывести(СекцияОписаниеОшибки);
	КонецЕсли;

	ИмяВременногоФайла = ПолучитьИмяВременногоФайла();
	ТабДок.Записать(ИмяВременногоФайла, ТипФайлаТабличногоДокумента.HTML);
	ТекстHTML = Новый ТекстовыйДокумент;
	ТекстHTML.Прочитать(ИмяВременногоФайла);
	ТекстHTML = ТекстHTML.ПолучитьТекст();
	ТекстHTML = СтрЗаменить(ТекстHTML, "МеткаРазделителяПротоколов", "<!--МеткаРазделителяПротоколов-->");
	
	// соединить сгенерированный ранее текст регионального протокола и сгенериованный сейчас текст федерального протокола
	
	Если ЗаголовокДекларации.РегиональныйПротокол <> Неопределено И ТипЗнч(ЗаголовокДекларации.РегиональныйПротокол) = Тип("ХранилищеЗначения") Тогда
		
		ТекстHTMLРегиональныйПротокол = ЗаголовокДекларации.РегиональныйПротокол.Получить();
		ТекстHTMLФедеральныйПротокол = ТекстHTML;
		ТекстHTMLФедеральныйПротокол = СтрЗаменить(ТекстHTMLФедеральныйПротокол, "CLASS=R", "CLASS=FR");
		ТекстHTMLФедеральныйПротокол = СтрЗаменить(ТекстHTMLФедеральныйПротокол, "CLASS=""R", "CLASS=""FR");
		ТекстHTMLФедеральныйПротокол = СтрЗаменить(ТекстHTMLФедеральныйПротокол, "tr.R", "tr.FR");
		ТекстHTMLФедеральныйПротокол = СтрЗаменить(ТекстHTMLФедеральныйПротокол, "td.R", "td.FR");
		
		НачалоСтилейТаблиц = СтрНайти(ТекстHTMLФедеральныйПротокол, "tr.FR");
		КонецСтилейТаблиц = СтрНайти(ТекстHTMLФедеральныйПротокол, "table {");
		Если КонецСтилейТаблиц = 0 Тогда
			КонецСтилейТаблиц = СтрНайти(ТекстHTMLФедеральныйПротокол, "table{");
		КонецЕсли;
		СтилиТаблицФедеральныйПротокол = "";
		Если НачалоСтилейТаблиц > 0 И КонецСтилейТаблиц > 0 И КонецСтилейТаблиц > НачалоСтилейТаблиц Тогда
			СтилиТаблицФедеральныйПротокол = Сред(ТекстHTMLФедеральныйПротокол, НачалоСтилейТаблиц, КонецСтилейТаблиц - НачалоСтилейТаблиц);
		КонецЕсли;
		
		ПозицияОкончанияРегиональногоПротокола = СтрНайти(ТекстHTMLРегиональныйПротокол, "<!--МеткаРазделителяПротоколов-->");
		ПозицияНачалаФедеральногоПротокола = СтрНайти(ТекстHTMLФедеральныйПротокол, "<!--МеткаРазделителяПротоколов-->");
		Если ПозицияОкончанияРегиональногоПротокола > 0 И ПозицияНачалаФедеральногоПротокола > 0 Тогда
			
			ТекстHTML = Лев(ТекстHTMLРегиональныйПротокол, ПозицияОкончанияРегиональногоПротокола - 1)
				+ Сред(ТекстHTMLФедеральныйПротокол, ПозицияНачалаФедеральногоПротокола);
				
			Если ЗначениеЗаполнено(СтилиТаблицФедеральныйПротокол) Тогда
				НачалоСтилейТаблиц = СтрНайти(ТекстHTMLРегиональныйПротокол, "tr.R");
				Если НачалоСтилейТаблиц > 0 Тогда
					ТекстHTML = Лев(ТекстHTML, НачалоСтилейТаблиц - 1) + СтилиТаблицФедеральныйПротокол + Сред(ТекстHTML, НачалоСтилейТаблиц);
				КонецЕсли;
			КонецЕсли;
			
		КонецЕсли;
			
	КонецЕсли;
	
	Возврат ТекстHTML;
	
КонецФункции

Функция ПолучитьОсновныеСвойстваПоследнейОтправкиОтчетаВФСРАР(Знач ОтчетСсылка)
	
	Запрос = Новый Запрос;
	Запрос.Текст =
		"ВЫБРАТЬ ПЕРВЫЕ 1
		|	ОтправкиФСРАР.Ссылка,
		|	ОтправкиФСРАР.ИдентификаторОтправкиНаСервере КАК Идентификатор,
		|	ОтправкиФСРАР.ИмяФайлаПакета КАК ИмяФайлаПакета,
		|	ОтправкиФСРАР.СтатусОтправки КАК Статус,
		|	ОтправкиФСРАР.СхемаСдачиОтчетности КАК СхемаСдачиОтчетности,
		|	ОтправкиФСРАР.СтатусОтправкиНаРегиональныйСервер КАК СтатусОтправкиНаРегиональныйСервер,
		|	ОтправкиФСРАР.ЗаверенныйПакетОтправлен КАК ЗаверенныйПакетОтправлен
		|ИЗ
		|	Справочник.ОтправкиФСРАР КАК ОтправкиФСРАР
		|ГДЕ
		|	ОтправкиФСРАР.ОтчетСсылка = &ЭтотОтчет
		|	И ОтправкиФСРАР.ПометкаУдаления = ЛОЖЬ
		|
		|УПОРЯДОЧИТЬ ПО
		|	ОтправкиФСРАР.ДатаОтправки УБЫВ";
	Запрос.Параметры.Вставить("ЭтотОтчет", ОтчетСсылка);
	Результат = Запрос.Выполнить();
	Если НЕ Результат.Пустой() Тогда
		Выборка = Результат.Выбрать();
		Выборка.Следующий();
		Возврат Выборка;
	Иначе
		Возврат Неопределено;
	КонецЕсли;
	
КонецФункции

Функция ПолучитьПоследнююОтправкуОтчетаВФСРАР(ОтчетСсылка) Экспорт
	
	Отправка = Неопределено;
	
	Запрос = Новый Запрос;
	Запрос.Текст =
		"ВЫБРАТЬ ПЕРВЫЕ 1
		|	ОтправкиФСРАР.Ссылка
		|ИЗ
		|	Справочник.ОтправкиФСРАР КАК ОтправкиФСРАР
		|ГДЕ
		|	ОтправкиФСРАР.ОтчетСсылка = &ЭтотОтчет
		|	И ОтправкиФСРАР.ПометкаУдаления = ЛОЖЬ
		|
		|УПОРЯДОЧИТЬ ПО
		|	ОтправкиФСРАР.ДатаОтправки УБЫВ";
	Запрос.Параметры.Вставить("ЭтотОтчет", ОтчетСсылка);
	Результат = Запрос.Выполнить();
	
	Если НЕ Результат.Пустой() Тогда
		
		Выборка = Результат.Выбрать();
		Выборка.Следующий();
		Отправка = Выборка.Ссылка;
		
	КонецЕсли;
	
	Возврат Отправка;
	
КонецФункции

Процедура ОбновитьСтатусОтправкиФСРАРОтчета(Отчет)
	
	// находим последнюю не помеченную на удаление отправку отчета
	ПоследняяОтправка = ПолучитьПоследнююОтправкуОтчетаВФСРАР(Отчет);
	
	// вычисляем значение статуса отправки (ПеречислениеСсылка.СтатусыОтправки)
	Если ЗначениеЗаполнено(ПоследняяОтправка) Тогда
		
		СтатусОтправки = ПоследняяОтправка.СтатусОтправки;
		Если СтатусОтправки = Перечисления.СтатусыОтправки.ПринятЕстьОшибки Тогда
			//введено после добавления дополнительного статуса ПринятЕстьОшибки, при котором отчет считается сданным
			СтатусОтправки = Перечисления.СтатусыОтправки.Сдан;
		КонецЕсли;
		
	Иначе
		СтатусОтправки = Неопределено;
	КонецЕсли;
	
	// сохраняем статус в базе
	ЗаписатьСтатусОтправкиФСРАРОтчета(Отчет, СтатусОтправки, ПоследняяОтправка);
	
КонецПроцедуры

Процедура ЗаписатьСтатусОтправкиФСРАРОтчета(Отчет, СтатусОтправки, ОснованиеСтатуса)
	
	Если ЗначениеЗаполнено(СтатусОтправки) Тогда
		МенЗап = РегистрыСведений.СтатусыОтправки.СоздатьМенеджерЗаписи();
		МенЗап.Объект = Отчет;
		МенЗап.Статус = СтатусОтправки;
		МенЗап.Основание = ОснованиеСтатуса;
		МенЗап.Записать(Истина);
	Иначе
		МенЗап = РегистрыСведений.СтатусыОтправки.СоздатьМенеджерЗаписи();
		МенЗап.Объект = Отчет;
		МенЗап.Прочитать();
		Если МенЗап.Выбран() Тогда
			МенЗап.Удалить();
		КонецЕсли;
	КонецЕсли;
	
	// вызываем переопределяемую процедуру отработки изменения статуса отправки
	ПриИзмененииСтатусаОтправкиДокумента(Отчет, СтатусОтправки);
	
КонецПроцедуры

Функция ПолучитьПустуюДатуЗавершенияОтправкиФСРАР() Экспорт
	
	Возврат '39991231235959';
	
КонецФункции

Процедура ПриЗаписиОтправкиФСРАР(Объект, Отказ) Экспорт
	
	Если Объект.ОтчетСсылка <> Неопределено Тогда
		// отражаем изменения в регистре статусов отправки
		ОбновитьСтатусОтправкиФСРАРОтчета(Объект.ОтчетСсылка);
		ЭлектронныйДокументооборотСКонтролирующимиОрганами.ЗаписьОбъектовРегламентированнойОтчетности(Объект.ОтчетСсылка, Отказ);
	КонецЕсли;
	
КонецПроцедуры

Функция ПоляНаСервереДляЗаписиОтправкиВФСРАР(ОбъектСсылка) Экспорт
	
	ФедеральныйПортал = (ОбъектСсылка.ВидОтчета <> Неопределено И ОбъектСсылка.ВидОтчета.ТипДокумента = Перечисления.ТипыОтправляемыхДокументов.ОтчетФСРАРОпт);
	
	Возврат Новый Структура("ДатаНачалаПериода, ДатаОкончанияПериода, Версия, ФедеральныйПортал",
		ОбъектСсылка.ДатаНачала, ОбъектСсылка.ДатаОкончания, ОбъектСсылка.Версия, ФедеральныйПортал);
	
КонецФункции

Процедура ПередЗаписьюОтправкиФСРАР(Объект, Отказ)
	
	Если НЕ ЗначениеЗаполнено(Объект.Период) Тогда
		Объект.Период = СокрЛП(Формат(Объект.ДатаОкончанияПериода, "ДФ=yyyyMMdd") + Формат('39991231' - Объект.ДатаНачалаПериода, "ДФ=yyyyMMdd"));
	КонецЕсли;
	
	Если Объект.СтатусОтправки = Перечисления.СтатусыОтправки.Отправлен Тогда
		
		Если НЕ ЗначениеЗаполнено(Объект.ДатаЗакрытия) Тогда
			Объект.ДатаЗакрытия = ПолучитьПустуюДатуЗавершенияОтправкиФСРАР();
		КонецЕсли;
		
	Иначе
		Объект.ДатаЗакрытия = Объект.ДатаПолученияРезультата;
	КонецЕсли;
	
КонецПроцедуры

Функция СписокДопустимыхОрганизацийВОбъектахОбменаФСРАР() Экспорт
	
	Запрос = Новый Запрос;
	ТекстЗапроса = 
	"ВЫБРАТЬ РАЗРЕШЕННЫЕ
	|	НастройкиОбменаФСРАР.Организация КАК Организация
	|ИЗ
	|	РегистрСведений.НастройкиОбменаФСРАР КАК НастройкиОбменаФСРАР
	|ГДЕ
	|	НастройкиОбменаФСРАР.ИспользоватьОбмен = ИСТИНА";
	
	Запрос.Текст = ТекстЗапроса;
	Возврат Запрос.Выполнить().Выгрузить().ВыгрузитьКолонку("Организация");
	
КонецФункции

////////////////////////////////////////////////////////////////////////////////
// Обмен с общим региональным и федеральным сервером на площадке ФСРАР

Функция ЗарегистрироватьсяВФСРАРССервера(Знач ПодписанныйПакетДляРегистрации, Знач ПараметрыОтправки, ПараметрыСоединения, ОтправкаРегламентированногоОтчета = Неопределено) Экспорт
	
	ПараметрыСоединения.ИдентификаторОтправкиНаСервере = "";
	ПараметрыСоединения.ПовторятьСоединение = Ложь;
	
	Если ПараметрыОтправки.ФедеральныйПортал ИЛИ ПараметрыОтправки.СхемаСдачиОтчетности = Перечисления.СхемыСдачиОтчетностиФСРАР.ТиповойРегиональныйИФедеральныйСерверы
		ИЛИ ПараметрыОтправки.СхемаСдачиОтчетности = Перечисления.СхемыСдачиОтчетностиФСРАР.МосковскийРегиональныйИФедеральныйСерверы Тогда
		АдресСервера = "service.fsrar.ru";
	Иначе
		АдресСервера = "service.alcolicenziat.ru";
	КонецЕсли;
	
	АдресСервера = ДокументооборотСКО.ПолучитьЗаменяемоеЗначениеТестовогоСервера1С(АдресСервера, , ПараметрыСоединения.СоединениеHTTP);
	
	// устанавливаем соединение с сервером
	ОписаниеОшибкиУстановкиСоединения = "";
	
	Если ПараметрыСоединения.СоединениеHTTP = Неопределено Тогда
		ПараметрыСоединения.СоединениеHTTP = ДокументооборотСКО.УстановитьСоединениеССерверомИнтернета("https://" + АдресСервера, ОписаниеОшибкиУстановкиСоединения);
	КонецЕсли;
	
	Если ПараметрыСоединения.СоединениеHTTP = Неопределено Тогда
		ПараметрыСоединения.ПовторятьСоединение = Истина;
		СообщитьПользователю(		
			СтрШаблон(НСтр("ru = 'Не удалось установить соединение с сервером онлайн-проверки:
                            |%1'"), ОписаниеОшибкиУстановкиСоединения), ПараметрыСоединения.СообщенияПользователю);
		Возврат Ложь;
	КонецЕсли;
	
	// отправка данных регистрации
	
	ЗаголовкиHTTP = Новый Соответствие();
	ЗаголовкиHTTP.Вставить("Content-Type", "application/x-www-form-urlencoded");
	ЗаголовкиHTTP.Вставить("Proxy-Connection", "Keep-Alive");
	ЗаголовкиHTTP.Вставить("Pragma", "no-cache");
	Если ЗначениеЗаполнено(ПараметрыСоединения.ЗначениеCookie) Тогда
		ЗаголовкиHTTP.Вставить("Cookie", ПараметрыСоединения.ЗначениеCookie);
	КонецЕсли;
	
	ИмяФайлаПодписанногоПакета = ПолучитьИмяВременногоФайла();
	ЗаписываемыйФайл = Новый ЗаписьТекста(ИмяФайлаПодписанногоПакета, "windows-1251");
	ЗаписываемыйФайл.Записать(ПодписанныйПакетДляРегистрации);
	ЗаписываемыйФайл.Закрыть();
	
	ИмяФайлаОтвета = ПолучитьИмяВременногоФайла();
	
	Попытка
		РесурсНаСервере = ДокументооборотСКО.ПолучитьЗаменяемоеЗначениеТестовогоСервера1С("/registration", "{FSRAR}/registration", ПараметрыСоединения.СоединениеHTTP);
		ОтветHTTP = ПараметрыСоединения.СоединениеHTTP.ОтправитьДляОбработки(ИмяФайлаПодписанногоПакета, 
																				РесурсНаСервере, 
																				ИмяФайлаОтвета, 
																				ЗаголовкиHTTP);
	Исключение
		
		ПараметрыСоединения.СоединениеHTTP = Неопределено;
		ПараметрыСоединения.ПовторятьСоединение = Истина;
		ОперацииСФайламиЭДКО.УдалитьВременныйФайл(ИмяФайлаПодписанногоПакета);
		ОперацииСФайламиЭДКО.УдалитьВременныйФайл(ИмяФайлаОтвета);
		Возврат Ложь;
		
	КонецПопытки;
	
	ФайлОтвета = Новый ЧтениеТекста(ИмяФайлаОтвета, "utf-8");
	ТекстОтвета = ФайлОтвета.Прочитать();
	ФайлОтвета.Закрыть();
	
	ЗначениеSetCookie = ОтветHTTP.Заголовки.Получить("Set-Cookie");
	ПараметрыСоединения.ЗначениеCookie = ОбновленноеЗначениеCookie(ПараметрыСоединения.ЗначениеCookie, ЗначениеSetCookie);
	
	ОперацииСФайламиЭДКО.УдалитьВременныйФайл(ИмяФайлаПодписанногоПакета);
	ОперацииСФайламиЭДКО.УдалитьВременныйФайл(ИмяФайлаОтвета);
	
	ПараметрыСоединения.СоединениеHTTP = Неопределено;
	
	ТекстОшибки = ТекстПослеПрефикса(ТекстОтвета, "<center><b>Ошибка.<", "</center>");
	Если ЗначениеЗаполнено(ТекстОшибки) Тогда
		ЗаголовокОшибки = "";
		ТекстОшибки = Строка(ТекстПослеПрефикса(ТекстОшибки, "'")); // текст после завершение строки "Ошибка"
		ТекстОшибки = Строка(ТекстПослеПрефикса(ТекстОшибки, "'")); // текст с начала заголовка информации об ошибке
		ПозицияКонцаЗаголовка = СтрНайти(ТекстОшибки, "'");
		Если ПозицияКонцаЗаголовка > 0 Тогда
			ЗаголовокОшибки = Лев(ТекстОшибки, ПозицияКонцаЗаголовка - 1);
			ЗаголовокОшибки = СтрЗаменить(ЗаголовокОшибки, "<br>", " ");
			ЗаголовокОшибки = СокрЛП(ЗаголовокОшибки);
			ТекстОшибки = Сред(ТекстОшибки, ПозицияКонцаЗаголовка + 1);
			ТекстОшибки = Строка(ТекстПослеПрефикса(ТекстОшибки, "'", "'"));  // текст описания ошибки
		КонецЕсли;
		ТекстОшибки = СтрЗаменить(ТекстОшибки, "<br>", " ");
		ТекстОшибки = СокрЛП(ТекстОшибки);
		ТекстОшибки = ЗаголовокОшибки + ?(ЗаголовокОшибки <> "" И ТекстОшибки <> "", ". ", "") + ТекстОшибки;
		
	Иначе
		ТекстОшибки = ТекстПослеПрефикса(ТекстОтвета, "<font color=""red""><b>", "</");
	КонецЕсли;
	
	Если ЗначениеЗаполнено(ТекстОшибки) Тогда
		ПараметрыСоединения.ОписаниеОшибкиСервером = ТекстИзHTML(ТекстОшибки);
		ПараметрыСоединения.ОписаниеОшибкиСервером = СокрЛП(ПараметрыСоединения.ОписаниеОшибкиСервером);
		Если НЕ ЗначениеЗаполнено(ПараметрыСоединения.ОписаниеОшибкиСервером) Тогда
			ПараметрыСоединения.ОписаниеОшибкиСервером = НСтр("ru = 'Ошибка при регистрации на портале ФСРАР.'");
		КонецЕсли;
		Если Прав(ПараметрыСоединения.ОписаниеОшибкиСервером, 1) <> "." И Прав(ПараметрыСоединения.ОписаниеОшибкиСервером, 1) <> "!" Тогда
			ПараметрыСоединения.ОписаниеОшибкиСервером = ПараметрыСоединения.ОписаниеОшибкиСервером + ".";
		КонецЕсли;
		
		СообщитьПользователю(ПараметрыСоединения.ОписаниеОшибкиСервером + ?(ОтправкаРегламентированногоОтчета = Неопределено, "", " " + ?(ОтправкаРегламентированногоОтчета = Истина,
			НСтр("ru = 'Для выгрузки отчета в файл используйте меню ""Выгрузка"" - ""Выгрузить для портала ФСРАР"".'"), НСтр("ru = 'Для выгрузки отчета в файл используйте меню ""Еще"" - ""Выгрузить"".'"))
			+ " " + НСтр("ru = 'С информацией о порядке представления отчета ознакомьтесь на сайте http://service.fsrar.ru'")), ПараметрыСоединения.СообщенияПользователю);
		
		Возврат Ложь;
		
	Иначе
		ПараметрыСоединения.ТребуетсяРегистрация = Ложь;
		Возврат Истина;
	КонецЕсли;
	
КонецФункции

Функция ПолучитьПодписываемыеДанныеПорталаФСРАРССервера(СлучайныйКлюч, Данные64CAPTCHA, Знач ПараметрыОтправки, ПараметрыСоединения, ВызовИзОтправки = Истина, ДляРегистрации = Ложь) Экспорт
	
	Если ПараметрыОтправки.ФедеральныйПортал ИЛИ ПараметрыОтправки.СхемаСдачиОтчетности = Перечисления.СхемыСдачиОтчетностиФСРАР.ТиповойРегиональныйИФедеральныйСерверы
		ИЛИ ПараметрыОтправки.СхемаСдачиОтчетности = Перечисления.СхемыСдачиОтчетностиФСРАР.МосковскийРегиональныйИФедеральныйСерверы Тогда
		АдресСервераОнлайнПроверки = "service.fsrar.ru";
	Иначе
		АдресСервераОнлайнПроверки = "service.alcolicenziat.ru";
	КонецЕсли;
	
	АдресСервераОнлайнПроверки = ДокументооборотСКО.ПолучитьЗаменяемоеЗначениеТестовогоСервера1С(АдресСервераОнлайнПроверки, , ПараметрыСоединения.СоединениеHTTP);
	
	СлучайныйКлюч = "";
	ПараметрыСоединения.ПовторятьСоединение = Ложь;
	
	// устанавливаем соединение с сервером
	ОписаниеОшибки = "";
	
	Если ПараметрыСоединения.СоединениеHTTP = Неопределено Тогда
		ПараметрыСоединения.СоединениеHTTP = ДокументооборотСКО.УстановитьСоединениеССерверомИнтернета("https://" + АдресСервераОнлайнПроверки, ОписаниеОшибки);
	КонецЕсли;
	
	Если ПараметрыСоединения.СоединениеHTTP = Неопределено Тогда
		
		ПараметрыСоединения.ПовторятьСоединение = Истина;
		СообщитьПользователю(
			СтрШаблон(
				НСтр("ru = 'Не удалось установить соединение с сервером онлайн-проверки: 
                      |%1.'"), ОписаниеОшибки), ПараметрыСоединения.СообщенияПользователю);
		Возврат Неопределено;
		
	КонецЕсли;
	
	ЗаголовкиHTTP = Новый Соответствие();
	ЗаголовкиHTTP.Вставить("Proxy-Connection", "Keep-Alive");
	ЗаголовкиHTTP.Вставить("Pragma", "no-cache");
	Если ЗначениеЗаполнено(ПараметрыСоединения.ЗначениеCookie) Тогда
		ЗаголовкиHTTP.Вставить("Cookie", ПараметрыСоединения.ЗначениеCookie);
	КонецЕсли;
	
	Если НЕ ДляРегистрации Тогда
		РесурсНаСервере = ДокументооборотСКО.ПолучитьЗаменяемоеЗначениеТестовогоСервера1С("/auth/ecplogin", "{FSRAR}/auth/ecplogin", ПараметрыСоединения.СоединениеHTTP);
	Иначе
		РесурсНаСервере = ДокументооборотСКО.ПолучитьЗаменяемоеЗначениеТестовогоСервера1С("/registration", "{FSRAR}/registration", ПараметрыСоединения.СоединениеHTTP);
	КонецЕсли;
	ЗапросHTTP = Новый HTTPЗапрос(РесурсНаСервере, ЗаголовкиHTTP);
	
	ИмяФайлаОтвета = ПолучитьИмяВременногоФайла();
	
	Попытка
		ОтветHTTP = ПараметрыСоединения.СоединениеHTTP.Получить(ЗапросHTTP, ИмяФайлаОтвета);
	Исключение
		
		ПараметрыСоединения.СоединениеHTTP = Неопределено;
		ПараметрыСоединения.ПовторятьСоединение = Истина;
		Возврат Неопределено;
		
	КонецПопытки;
	
	ФайлОтвета = Новый ЧтениеТекста(ИмяФайлаОтвета, "windows-1251");
	ТекстОтвета = ФайлОтвета.Прочитать();
	ФайлОтвета.Закрыть();
	
	ЗначениеSetCookie = ОтветHTTP.Заголовки.Получить("Set-Cookie");
	ПараметрыСоединения.ЗначениеCookie = ОбновленноеЗначениеCookie(ПараметрыСоединения.ЗначениеCookie, ЗначениеSetCookie);
	
	ОперацииСФайламиЭДКО.УдалитьВременныйФайл(ИмяФайлаОтвета);
	
	НачалоСлучайногоКлюча = СтрНайти(ТекстОтвета, "name: 'csrf'");
	Если НачалоСлучайногоКлюча = 0 Тогда
		НачалоСлучайногоКлюча = СтрНайти(ТекстОтвета, "name: 'csrf_Vseh_pokarau)'");
	КонецЕсли;
	
	Если НачалоСлучайногоКлюча <> 0 Тогда
		
		ТекстОтветаСМетки = Прав(ТекстОтвета, СтрДлина(ТекстОтвета) - НачалоСлучайногоКлюча + 1);
		ПрефиксЗначенияСлучайногоКлюча = "value: '";
		НачалоЗначенияСлучайногоКлюча = СтрНайти(ТекстОтветаСМетки, ПрефиксЗначенияСлучайногоКлюча);
		
		Если НачалоЗначенияСлучайногоКлюча <> 0 Тогда
			
			ТекстОтветаСМетки = Прав(ТекстОтветаСМетки, СтрДлина(ТекстОтветаСМетки) - НачалоЗначенияСлучайногоКлюча - СтрДлина(ПрефиксЗначенияСлучайногоКлюча) + 1);
			КонецЗначенияСлучайногоКлюча = СтрНайти(ТекстОтветаСМетки, "'");
			Если КонецЗначенияСлучайногоКлюча <> 0 Тогда
				СлучайныйКлюч = Лев(ТекстОтветаСМетки, КонецЗначенияСлучайногоКлюча - 1);
			КонецЕсли;
			
		КонецЕсли;
		
	КонецЕсли;
	
	Результат = СлучайныйКлюч;
	
	СсылкаCAPTCHA = "";
	НачалоCAPTCHA = СтрНайти(ТекстОтвета, "id: 'capcha_go'");
	Если НачалоCAPTCHA <> 0 Тогда
		
		ТекстОтветаСМетки = Прав(ТекстОтвета, СтрДлина(ТекстОтвета) - НачалоCAPTCHA + 1);
		ПрефиксЗначенияCAPTCHA = "src : '";
		НачалоЗначенияCAPTCHA = СтрНайти(ТекстОтветаСМетки, ПрефиксЗначенияCAPTCHA);
		
		Если НачалоЗначенияCAPTCHA <> 0 Тогда
			
			ТекстПараметровCAPTCHA = Лев(ТекстОтветаСМетки, НачалоЗначенияCAPTCHA - 1);
			НачалоCAPTCHAСкрыта = СтрНайти(ТекстОтветаСМетки, "hidden: true");
			Если НачалоCAPTCHAСкрыта = 0 Тогда
				
				ТекстОтветаСМетки = Прав(ТекстОтветаСМетки, СтрДлина(ТекстОтветаСМетки) - НачалоЗначенияCAPTCHA - СтрДлина(ПрефиксЗначенияCAPTCHA) + 1);
				КонецЗначенияCAPTCHA = СтрНайти(ТекстОтветаСМетки, "'");
				Если КонецЗначенияCAPTCHA <> 0 Тогда
					СсылкаCAPTCHA = Лев(ТекстОтветаСМетки, КонецЗначенияCAPTCHA - 1);
				КонецЕсли;
				
			КонецЕсли;
			
		КонецЕсли;
		
	КонецЕсли;
	
	Если ЗначениеЗаполнено(Результат) И ЗначениеЗаполнено(СсылкаCAPTCHA) Тогда
		Данные64CAPTCHA = ПолучитьДанные64КартинкиCAPTCHAСПорталаФСРАР(СсылкаCAPTCHA, ПараметрыСоединения);
	КонецЕсли;
	
	ПараметрыСоединения.СоединениеHTTP = Неопределено;
	
	Если НЕ ЗначениеЗаполнено(Результат) Тогда
		Если НЕ ДляРегистрации Тогда
			СообщитьПользователю(НСтр("ru = 'Ошибка формата страницы авторизации'"), ПараметрыСоединения.СообщенияПользователю);
		Иначе
			СообщитьПользователю(НСтр("ru = 'Ошибка формата страницы регистрации'"), ПараметрыСоединения.СообщенияПользователю);
		КонецЕсли;
		Возврат Неопределено;
	КонецЕсли;
	
	Возврат Результат;
	
КонецФункции

Функция ОтправитьЗашифрованныйПакетНаСерверФСРАР(ФайлыИДанныеОтправки, ПараметрыОтправки, ПараметрыСоединения, Данные64CAPTCHA = "")
	
	ПараметрыСоединения.ИдентификаторОтправкиНаСервере = "";
	ПараметрыСоединения.ПовторятьСоединение = Ложь;
	
	Если ПараметрыОтправки.ФедеральныйПортал ИЛИ ПараметрыОтправки.СхемаСдачиОтчетности = Перечисления.СхемыСдачиОтчетностиФСРАР.ТиповойРегиональныйИФедеральныйСерверы
		ИЛИ ПараметрыОтправки.СхемаСдачиОтчетности = Перечисления.СхемыСдачиОтчетностиФСРАР.МосковскийРегиональныйИФедеральныйСерверы Тогда
		АдресСервераОнлайнПроверки = "service.fsrar.ru";
	Иначе
		АдресСервераОнлайнПроверки = "service.alcolicenziat.ru";
	КонецЕсли;
	
	АдресСервераОнлайнПроверки = ДокументооборотСКО.ПолучитьЗаменяемоеЗначениеТестовогоСервера1С(АдресСервераОнлайнПроверки, , ПараметрыСоединения.СоединениеHTTP);
	
	// устанавливаем соединение с сервером
	ОписаниеОшибки = "";
	
	Если ПараметрыСоединения.СоединениеHTTP = Неопределено Тогда
		ПараметрыСоединения.СоединениеHTTP = ДокументооборотСКО.УстановитьСоединениеССерверомИнтернета("https://" + АдресСервераОнлайнПроверки, ОписаниеОшибки);
	КонецЕсли;
	
	Если ПараметрыСоединения.СоединениеHTTP = Неопределено Тогда
		
		ПараметрыСоединения.ПовторятьСоединение = Истина;
		СообщитьПользователю(
			СтрШаблон(
				НСтр("ru = 'Не удалось установить соединение с сервером онлайн-проверки: 
                      |%1.'"), ОписаниеОшибки), ПараметрыСоединения.СообщенияПользователю);
		Возврат Неопределено;
		
	КонецЕсли;

	
	ТекстСообщенияПриУспехе = Неопределено;
	
	Если НЕ ЗначениеЗаполнено(ФайлыИДанныеОтправки.ПодписанныйПакетДляАвторизации) ИЛИ АвторизоватьсяНаПорталеФСРАР(ФайлыИДанныеОтправки.ПодписанныйПакетДляАвторизации, ПараметрыСоединения, ПараметрыОтправки) Тогда
		
		Если ЗначениеЗаполнено(ФайлыИДанныеОтправки.ПодписанныйПакетДляАвторизации) Тогда
			РесурсНаСервере = ДокументооборотСКО.ПолучитьЗаменяемоеЗначениеТестовогоСервера1С("/sapcha/declar?_dc=", "{FSRAR}/sapcha/declar?_dc=", ПараметрыСоединения.СоединениеHTTP);
			Данные64CAPTCHA = ПолучитьДанные64КартинкиCAPTCHAСПорталаФСРАР(РесурсНаСервере, ПараметрыСоединения);
			Возврат Неопределено;
		КонецЕсли;
	
		ТекстСообщенияПриУспехе = ЗагрузитьДекларациюНаПорталФСРАР(ФайлыИДанныеОтправки.КороткоеИмяФайлаПакета, ФайлыИДанныеОтправки.ИмяФайлаОтправки, ПараметрыСоединения);
		Если ТекстСообщенияПриУспехе <> Неопределено Тогда
			ПараметрыСоединения.ИдентификаторОтправкиНаСервере = Формат(ТекущаяДатаСеанса(), "ДФ=yyyyMMddhhmmss") + ФайлыИДанныеОтправки.КороткоеИмяФайлаПакета;
			
		Иначе
			ОписаниеОшибкиСерверомВВерхнемРегистре = ВРег(ПараметрыСоединения.ОписаниеОшибкиСервером);
			ОшибкаВводаCAPTCHAВВерхнемРегистре = ВРег("Неверный код с картинки");
			
			Если СтрНайти(ОписаниеОшибкиСерверомВВерхнемРегистре, ОшибкаВводаCAPTCHAВВерхнемРегистре) > 0 Тогда
				РесурсНаСервере = ДокументооборотСКО.ПолучитьЗаменяемоеЗначениеТестовогоСервера1С("/sapcha/declar?_dc=", "{FSRAR}/sapcha/declar?_dc=", ПараметрыСоединения.СоединениеHTTP);
				Данные64CAPTCHA = ПолучитьДанные64КартинкиCAPTCHAСПорталаФСРАР(РесурсНаСервере, ПараметрыСоединения);
				Возврат Неопределено;
			КонецЕсли;
		КонецЕсли;
		
	КонецЕсли;
	
	Возврат ТекстСообщенияПриУспехе;
	
КонецФункции

Функция АвторизоватьсяНаПорталеФСРАР(ПодписанныйПакет, ПараметрыСоединения, ПараметрыОтправки)
	
	ПараметрыСоединения.ПовторятьСоединение = Ложь;
	
	ЗаголовкиHTTP = Новый Соответствие();
	ЗаголовкиHTTP.Вставить("Content-Type", "application/x-www-form-urlencoded");
	ЗаголовкиHTTP.Вставить("Proxy-Connection", "Keep-Alive");
	ЗаголовкиHTTP.Вставить("Pragma", "no-cache");
	Если ЗначениеЗаполнено(ПараметрыСоединения.ЗначениеCookie) Тогда
		ЗаголовкиHTTP.Вставить("Cookie", ПараметрыСоединения.ЗначениеCookie);
	КонецЕсли;
	
	ИмяФайлаПодписанногоПакета = ПолучитьИмяВременногоФайла();
	ЗаписываемыйФайл = Новый ЗаписьТекста(ИмяФайлаПодписанногоПакета, "windows-1251");
	ЗаписываемыйФайл.Записать(ПодписанныйПакет);
	ЗаписываемыйФайл.Закрыть();
	
	ИмяФайлаОтвета = ПолучитьИмяВременногоФайла();
	
	Попытка
		РесурсНаСервере = ДокументооборотСКО.ПолучитьЗаменяемоеЗначениеТестовогоСервера1С("/auth/ecplogin", "{FSRAR}/auth/ecplogin", ПараметрыСоединения.СоединениеHTTP);
		ОтветHTTP = ПараметрыСоединения.СоединениеHTTP.ОтправитьДляОбработки(ИмяФайлаПодписанногоПакета, РесурсНаСервере, ИмяФайлаОтвета, ЗаголовкиHTTP);
	Исключение
		
		ПараметрыСоединения.ПовторятьСоединение = Истина;
		ОперацииСФайламиЭДКО.УдалитьВременныйФайл(ИмяФайлаПодписанногоПакета);
		ОперацииСФайламиЭДКО.УдалитьВременныйФайл(ИмяФайлаОтвета);
		Возврат Ложь;
		
	КонецПопытки;
	
	ЗначениеSetCookie = ОтветHTTP.Заголовки.Получить("Set-Cookie");
	ПараметрыСоединения.ЗначениеCookie = ОбновленноеЗначениеCookie(ПараметрыСоединения.ЗначениеCookie, ЗначениеSetCookie);
	
	ФайлОтвета = Новый ЧтениеТекста(ИмяФайлаОтвета, "utf-8");
	ТекстОтвета = ФайлОтвета.Прочитать();
	ФайлОтвета.Закрыть();
	
	ОперацииСФайламиЭДКО.УдалитьВременныйФайл(ИмяФайлаПодписанногоПакета);
	ОперацииСФайламиЭДКО.УдалитьВременныйФайл(ИмяФайлаОтвета);
	
	Если СтрНайти(ТекстОтвета, "/auth/login") > 0 Тогда
		
		ЗаголовкиHTTP = Новый Соответствие();
		ЗаголовкиHTTP.Вставить("Proxy-Connection", "Keep-Alive");
		ЗаголовкиHTTP.Вставить("Pragma", "no-cache");
		Если ЗначениеЗаполнено(ПараметрыСоединения.ЗначениеCookie) Тогда
			ЗаголовкиHTTP.Вставить("Cookie", ПараметрыСоединения.ЗначениеCookie);
		КонецЕсли;
		
		РесурсНаСервере = ДокументооборотСКО.ПолучитьЗаменяемоеЗначениеТестовогоСервера1С("/auth/login", "{FSRAR}/auth/login", ПараметрыСоединения.СоединениеHTTP);
		ЗапросHTTP = Новый HTTPЗапрос(РесурсНаСервере, ЗаголовкиHTTP);
		
		ИмяФайлаОтвета = ПолучитьИмяВременногоФайла();
		
		Попытка
			ОтветHTTP = ПараметрыСоединения.СоединениеHTTP.Получить(ЗапросHTTP, ИмяФайлаОтвета);
		Исключение
			
			ПараметрыСоединения.ПовторятьСоединение = Истина;
			ОперацииСФайламиЭДКО.УдалитьВременныйФайл(ИмяФайлаОтвета);
			Возврат Ложь;
			
		КонецПопытки;
		
		ЗначениеSetCookie = ОтветHTTP.Заголовки.Получить("Set-Cookie");
		ПараметрыСоединения.ЗначениеCookie = ОбновленноеЗначениеCookie(ПараметрыСоединения.ЗначениеCookie, ЗначениеSetCookie);
		
		ФайлОтвета = Новый ЧтениеТекста(ИмяФайлаОтвета, "utf-8");
		ТекстОтвета = ФайлОтвета.Прочитать();
		ФайлОтвета.Закрыть();
		
		ОперацииСФайламиЭДКО.УдалитьВременныйФайл(ИмяФайлаОтвета);
		
	КонецЕсли;
	
	ТекстОшибки = ТекстПослеПрефикса(ТекстОтвета, "<font color=""red""><b>", "</");
	ТекстОшибкиБезВнутреннихТегов = ТекстПослеПрефикса(ТекстОшибки, "<font color=""red""><b>", "</");
	ТекстОшибки = ?(ЗначениеЗаполнено(ТекстОшибкиБезВнутреннихТегов), ТекстОшибкиБезВнутреннихТегов, ТекстОшибки);
	
	Если ЗначениеЗаполнено(ТекстОшибки) Тогда
		ТекстОшибки = ТекстИзHTML(ТекстОшибки);
		Если ПараметрыСоединения.ТребуетсяРегистрация <> Неопределено И СтрНайти(ТекстОшибки, "не зарегистрирован") > 0 Тогда
			ПараметрыСоединения.ТребуетсяРегистрация = Истина;
		Иначе
			ПараметрыСоединения.ОписаниеОшибкиСервером = СокрЛП(ТекстОшибки);
		КонецЕсли;
		Возврат Ложь;
	КонецЕсли;
	
	// проверка кода региона после авторизации
	
	Если НЕ ПараметрыОтправки.ФедеральныйПортал И ПараметрыСоединения.ТребуетсяПовторнаяАвторизация <> Неопределено Тогда
		
		ЗаголовкиHTTP = Новый Соответствие();
		ЗаголовкиHTTP.Вставить("Proxy-Connection", "Keep-Alive");
		ЗаголовкиHTTP.Вставить("Pragma", "no-cache");
		Если ЗначениеЗаполнено(ПараметрыСоединения.ЗначениеCookie) Тогда
			ЗаголовкиHTTP.Вставить("Cookie", ПараметрыСоединения.ЗначениеCookie);
		КонецЕсли;
		
		РесурсНаСервере = ДокументооборотСКО.ПолучитьЗаменяемоеЗначениеТестовогоСервера1С("/cabinet/home", "{FSRAR}/cabinet/home", ПараметрыСоединения.СоединениеHTTP);
		ЗапросHTTP = Новый HTTPЗапрос(РесурсНаСервере, ЗаголовкиHTTP);
		
		ИмяФайлаОтвета = ПолучитьИмяВременногоФайла();
		
		Попытка
			ОтветHTTP = ПараметрыСоединения.СоединениеHTTP.Получить(ЗапросHTTP, ИмяФайлаОтвета);
		Исключение
			
			ПараметрыСоединения.ПовторятьСоединение = Истина;
			ОперацииСФайламиЭДКО.УдалитьВременныйФайл(ИмяФайлаОтвета);
			Возврат Ложь;
			
		КонецПопытки;
		
		ЗначениеSetCookie = ОтветHTTP.Заголовки.Получить("Set-Cookie");
		ПараметрыСоединения.ЗначениеCookie = ОбновленноеЗначениеCookie(ПараметрыСоединения.ЗначениеCookie, ЗначениеSetCookie);
		
		ФайлОтвета = Новый ЧтениеТекста(ИмяФайлаОтвета, "utf-8");
		ТекстОтвета = ФайлОтвета.Прочитать();
		ФайлОтвета.Закрыть();
		
		ОперацииСФайламиЭДКО.УдалитьВременныйФайл(ИмяФайлаОтвета);
		
		Если СтрНайти(ТекстОтвета, """region_name"":""""") <> 0 Тогда
			КодРегионаУказанныйНаПортале = ТекстПослеПрефикса(ТекстОтвета, """region_choose"":""", """");
			
			Если СокрЛП(ПараметрыОтправки.КодРегиона) <> СокрЛП(КодРегионаУказанныйНаПортале) Тогда
				ЗаголовкиHTTP = Новый Соответствие();
				ЗаголовкиHTTP.Вставить("Content-Type", "application/x-www-form-urlencoded");
				ЗаголовкиHTTP.Вставить("X-Requested-With", "XMLHttpRequest");
				ЗаголовкиHTTP.Вставить("Proxy-Connection", "Keep-Alive");
				ЗаголовкиHTTP.Вставить("Pragma", "no-cache");
				Если ЗначениеЗаполнено(ПараметрыСоединения.ЗначениеCookie) Тогда
					ЗаголовкиHTTP.Вставить("Cookie", ПараметрыСоединения.ЗначениеCookie);
				КонецЕсли;
				
				ТекстНастройки = СтрШаблон("region_id=%1", ПараметрыОтправки.КодРегиона);
				
				ИмяФайлаНастройки = ПолучитьИмяВременногоФайла();
				ЗаписываемыйФайл = Новый ЗаписьТекста(ИмяФайлаНастройки, "windows-1251");
				ЗаписываемыйФайл.Записать(ТекстНастройки);
				ЗаписываемыйФайл.Закрыть();
				
				ИмяФайлаОтвета = ПолучитьИмяВременногоФайла();
				
				Попытка
					РесурсНаСервере = ДокументооборотСКО.ПолучитьЗаменяемоеЗначениеТестовогоСервера1С("/user/setregion/", "{FSRAR}/user/setregion/", ПараметрыСоединения.СоединениеHTTP);
					ОтветHTTP = ПараметрыСоединения.СоединениеHTTP.ОтправитьДляОбработки(ИмяФайлаНастройки, РесурсНаСервере, ИмяФайлаОтвета, ЗаголовкиHTTP);
				Исключение
					
					ПараметрыСоединения.ПовторятьСоединение = Истина;
					ОперацииСФайламиЭДКО.УдалитьВременныйФайл(ИмяФайлаНастройки);
					ОперацииСФайламиЭДКО.УдалитьВременныйФайл(ИмяФайлаОтвета);
					Возврат Ложь;
					
				КонецПопытки;
				
				ЗначениеSetCookie = ОтветHTTP.Заголовки.Получить("Set-Cookie");
				ПараметрыСоединения.ЗначениеCookie = ОбновленноеЗначениеCookie(ПараметрыСоединения.ЗначениеCookie, ЗначениеSetCookie);
				
				ОперацииСФайламиЭДКО.УдалитьВременныйФайл(ИмяФайлаНастройки);
				ОперацииСФайламиЭДКО.УдалитьВременныйФайл(ИмяФайлаОтвета);
				
				ПараметрыСоединения.ТребуетсяПовторнаяАвторизация = Истина;
				Возврат Ложь;
			КонецЕсли;
		КонецЕсли;
		
	КонецЕсли;
	
	Возврат Истина;
	
КонецФункции

Функция ЗагрузитьДекларациюНаПорталФСРАР(КороткоеИмяФайлаПакета, ИмяФайлаОтправки, ПараметрыСоединения)
	
	ПараметрыСоединения.ОписаниеОшибкиСервером = "";
	ПараметрыСоединения.ПовторятьСоединение = Ложь;
	
	// формируем общие заголовки
	ЗаголовкиHTTP = Новый Соответствие();
	
	ЗаголовкиHTTP.Вставить("Content-Type", "multipart/form-data; boundary=My1cV8bNdr");
	ЗаголовкиHTTP.Вставить("Proxy-Connection", "Keep-Alive");
	ЗаголовкиHTTP.Вставить("Pragma", "no-cache");
	Если ЗначениеЗаполнено(ПараметрыСоединения.ЗначениеCookie) Тогда
		ЗаголовкиHTTP.Вставить("Cookie", ПараметрыСоединения.ЗначениеCookie);
	КонецЕсли;
	
	ДокументооборотСКО.ДобавитьЗаголовкиДляТестовогоСервера1С(ЗаголовкиHTTP, ПараметрыСоединения.СоединениеHTTP);
	
	// посылаем запрос
	ИмяФайлаОтвета = ПолучитьИмяВременногоФайла();
	
	Попытка
		РесурсНаСервере = ДокументооборотСКО.ПолучитьЗаменяемоеЗначениеТестовогоСервера1С("/declaring/uploaddecl", "{FSRAR}/declaring/uploaddecl", ПараметрыСоединения.СоединениеHTTP);
		ОтветHTTP = ПараметрыСоединения.СоединениеHTTP.ОтправитьДляОбработки(ИмяФайлаОтправки, РесурсНаСервере, ИмяФайлаОтвета, ЗаголовкиHTTP);
	Исключение
		
		ПараметрыСоединения.ПовторятьСоединение = Истина;
		Возврат Неопределено;
		
	КонецПопытки;
	
	ЗначениеSetCookie = ОтветHTTP.Заголовки.Получить("Set-Cookie");
	ПараметрыСоединения.ЗначениеCookie = ОбновленноеЗначениеCookie(ПараметрыСоединения.ЗначениеCookie, ЗначениеSetCookie);
	
	ФайлОтвета = Новый ЧтениеТекста(ИмяФайлаОтвета, "windows-1251");
	ТекстОтвета = ФайлОтвета.Прочитать();
	ФайлОтвета.Закрыть();
	
	ОперацииСФайламиЭДКО.УдалитьВременныйФайл(ИмяФайлаОтвета);
	
	Если СтрНайти(ТекстОтвета, "Application_Model_Declaration::getMessage()") > 0 Тогда
		ПараметрыСоединения.ОписаниеОшибкиСервером = "";
		ТекстСообщенияПриУспехе = "Загружена декларация " + КороткоеИмяФайлаПакета;
		
	Иначе
		ТекстСообщенияПриУспехе = "";
		ПараметрыСоединения.ОписаниеОшибкиСервером = ТекстОшибкиПорталаФСРАР(ТекстОтвета, ТекстСообщенияПриУспехе);
	КонецЕсли;
	
	Если ЗначениеЗаполнено(ПараметрыСоединения.ОписаниеОшибкиСервером) Тогда
		Возврат Неопределено;
	КонецЕсли;
	
	Возврат ТекстСообщенияПриУспехе;
	
КонецФункции

Функция ПолучитьДанные64КартинкиCAPTCHAСПорталаФСРАР(СсылкаCAPTCHA, ПараметрыСоединения)
	
	ЗаголовкиHTTP = Новый Соответствие();
	ЗаголовкиHTTP.Вставить("Proxy-Connection", "Keep-Alive");
	ЗаголовкиHTTP.Вставить("Pragma", "no-cache");
	Если ЗначениеЗаполнено(ПараметрыСоединения.ЗначениеCookie) Тогда
		ЗаголовкиHTTP.Вставить("Cookie", ПараметрыСоединения.ЗначениеCookie);
	КонецЕсли;
	
	ЗапросHTTP = Новый HTTPЗапрос(СсылкаCAPTCHA, ЗаголовкиHTTP);
	
	ИмяФайлаОтвета = ПолучитьИмяВременногоФайла("jpg");
	
	Попытка
		ОтветHTTP = ПараметрыСоединения.СоединениеHTTP.Получить(ЗапросHTTP, ИмяФайлаОтвета);
	Исключение
		
		ПараметрыСоединения.СоединениеHTTP = Неопределено;
		ПараметрыСоединения.ПовторятьСоединение = Истина;
		Возврат Неопределено;
		
	КонецПопытки;
	
	ДанныеCAPTCHA = Новый ДвоичныеДанные(ИмяФайлаОтвета);
	Данные64CAPTCHA = Base64Строка(ДанныеCAPTCHA);
	
	ОперацииСФайламиЭДКО.УдалитьВременныйФайл(ИмяФайлаОтвета);
	
	Возврат Данные64CAPTCHA;
	
КонецФункции

Функция ТекстОшибкиПорталаФСРАР(ТекстОтвета, ТекстСообщенияПриУспехе = "")
	
	ТекстСообщенияПриУспехе = "";
	
	ЗавершеноУспешно = Неопределено;
	Если СтрНайти(ТекстОтвета, """success"":true") > 0 Тогда
		ЗавершеноУспешно = Истина;
	ИначеЕсли СтрНайти(ТекстОтвета, """success"":false") > 0 Тогда
		ЗавершеноУспешно = Ложь;
	КонецЕсли;
	
	Если ЗавершеноУспешно <> Неопределено Тогда
		
		Позиция = 1;
		Разделитель = "";
		ОжидаемыйРазделитель = "{";
		Пока Позиция <= СтрДлина(ТекстОтвета) Цикл
			
			ИмяПараметра = СледующееЗначениеJSON(ТекстОтвета, Позиция, Разделитель);
			Если Разделитель <> ОжидаемыйРазделитель Тогда
				Прервать;
			КонецЕсли;
			
			ЗначениеПараметра = СледующееЗначениеJSON(ТекстОтвета, Позиция, Разделитель);
			Если Разделитель <> ":" Тогда
				Прервать;
			КонецЕсли;
			
			Если ИмяПараметра = "msg" ИЛИ (ИмяПараметра = "error" И НЕ ЗавершеноУспешно) Тогда
				
				ЗначениеПараметра = ТекстЗначенияJSONВШестнадцатеричнойUTF16(ЗначениеПараметра);
				ЗначениеПараметра = ТекстИзHTML(ЗначениеПараметра);
				ЗначениеПараметра = СтрЗаменить(ЗначениеПараметра, "<br>", " ");
				ЗначениеПараметра = СтрЗаменить(ЗначениеПараметра, "   ", " ");
				ЗначениеПараметра = СтрЗаменить(ЗначениеПараметра, "  ", " ");
				
				Если ЗначениеПараметра <> "" Тогда
					
					Если ЗавершеноУспешно Тогда
						ТекстСообщенияПриУспехе = ЗначениеПараметра;
						Возврат "";
					Иначе
						ЗначениеПараметра = СтрЗаменить(ЗначениеПараметра, "Не верный код", "Неверный код"); // исправление ошибки портала
						Возврат ЗначениеПараметра;
					КонецЕсли;
					
				КонецЕсли;
				
			КонецЕсли;
			
			ОжидаемыйРазделитель = ",";
			
		КонецЦикла;
		
		Если ЗавершеноУспешно Тогда
			Возврат "";
		КонецЕсли;
		
	КонецЕсли;
	
	Возврат "Ошибочный ответ сервера";
	
КонецФункции

Функция ПолучитьИнформациюОДекларацииНаПорталеФСРАР(ИмяФайлаВыгрузки, ПодписанныйПакетДляАвторизации, ПараметрыОтправки, ПараметрыСоединения)
	
	ПараметрыСоединения.ОписаниеОшибкиСервером = "";
	ПараметрыСоединения.ПовторятьСоединение = Ложь;
	
	Если ПараметрыОтправки.ФедеральныйПортал ИЛИ ПараметрыОтправки.СхемаСдачиОтчетности = Перечисления.СхемыСдачиОтчетностиФСРАР.ТиповойРегиональныйИФедеральныйСерверы
		ИЛИ ПараметрыОтправки.СхемаСдачиОтчетности = Перечисления.СхемыСдачиОтчетностиФСРАР.МосковскийРегиональныйИФедеральныйСерверы Тогда
		АдресСервераОнлайнПроверки = "service.fsrar.ru";
	Иначе
		АдресСервераОнлайнПроверки = "service.alcolicenziat.ru";
	КонецЕсли;
	
	АдресСервераОнлайнПроверки = ДокументооборотСКО.ПолучитьЗаменяемоеЗначениеТестовогоСервера1С(АдресСервераОнлайнПроверки, , ПараметрыСоединения.СоединениеHTTP);
	
	Если ПараметрыСоединения.СоединениеHTTP = Неопределено Тогда
		
		// устанавливаем соединение с сервером
		ОписаниеОшибки = "";
		ПараметрыСоединения.СоединениеHTTP = ДокументооборотСКО.УстановитьСоединениеССерверомИнтернета("https://" + АдресСервераОнлайнПроверки, ОписаниеОшибки);
		Если ПараметрыСоединения.СоединениеHTTP = Неопределено Тогда
			
			ПараметрыСоединения.ПовторятьСоединение = Истина;
			СообщитьПользователю(
				СтрШаблон(
					НСтр("ru = 'Не удалось установить соединение с сервером онлайн-проверки: 
	                      |%1.'"), ОписаниеОшибки), ПараметрыСоединения.СообщенияПользователю);
			Возврат Неопределено;
			
		КонецЕсли;
		
	КонецЕсли;
	
	АвторизацияПроизведена = (НЕ ЗначениеЗаполнено(ПодписанныйПакетДляАвторизации));
	
	Если НЕ АвторизацияПроизведена Тогда
		АвторизацияПроизведена = АвторизоватьсяНаПорталеФСРАР(ПодписанныйПакетДляАвторизации, ПараметрыСоединения, ПараметрыОтправки);
	КонецЕсли;
	
	Если АвторизацияПроизведена Тогда
		
		ЗаголовокДекларации = НайтиЗаголовокДекларацииНаПорталеФСРАР(ИмяФайлаВыгрузки, ПараметрыСоединения);
		
		Если ЗаголовокДекларации <> Неопределено Тогда
			
			// получение регионального протокола
			
			ЗаголовокДекларации.РегиональныйПротокол = Неопределено;
			ЗаголовокДекларации.ОшибкаПолученияРегиональногоПротокола = "";
			
			ОшибкаСоединенияПриПолученииПротокола = Ложь;
			ОшибкаПолученияРегиональногоПротокола = "";
			РегиональныйПротокол = Неопределено;
			
			Если НЕ ПараметрыОтправки.ФедеральныйПортал Тогда
				
				ЗаголовокДекларации.РегиональныйПротокол = ПолучитьПротоколОбработкиДекларацииНаПорталеФСРАР(ЗаголовокДекларации.ИдентификаторЗаписи, ЗаголовокДекларации.ИмяФайла, Ложь, ПараметрыСоединения);
				ЗаголовокДекларации.ОшибкаПолученияРегиональногоПротокола = ПараметрыСоединения.ОписаниеОшибкиСервером;
				
				Если ЗаголовокДекларации.РегиональныйПротокол = Неопределено И НЕ ЗначениеЗаполнено(ЗаголовокДекларации.ОшибкаПолученияРегиональногоПротокола) Тогда
					ЗаголовокДекларации.ОшибкаПолученияРегиональногоПротокола = ?(ПараметрыСоединения.ПовторятьСоединение, НСтр("ru = 'Ошибка при обращении к серверу Росалкогольрегулирования'"), НСтр("ru = 'Не удалось получить региональный протокол'"));
				КонецЕсли;
				
				ПараметрыСоединения.ОписаниеОшибкиСервером = "";
				ПараметрыСоединения.ПовторятьСоединение = Ложь;
				
			КонецЕсли;
			
			// получение федерального протокола
			
			ОшибкаСоединенияПриПолученииПротокола = Ложь;
			ОшибкаПолученияФедеральногоПротокола = "";
			
			ЗаголовокДекларации.ФедеральныйПротокол = ПолучитьПротоколОбработкиДекларацииНаПорталеФСРАР(ЗаголовокДекларации.ИдентификаторЗаписи, ЗаголовокДекларации.ИмяФайла, Истина, ПараметрыСоединения);
			ЗаголовокДекларации.ОшибкаПолученияФедеральногоПротокола = ПараметрыСоединения.ОписаниеОшибкиСервером;
			
			Если ЗаголовокДекларации.ФедеральныйПротокол = Неопределено И НЕ ЗначениеЗаполнено(ЗаголовокДекларации.ОшибкаПолученияФедеральногоПротокола) Тогда
				ЗаголовокДекларации.ОшибкаПолученияФедеральногоПротокола = ?(ПараметрыСоединения.ПовторятьСоединение, НСтр("ru = 'Ошибка при обращении к серверу Росалкогольрегулирования'"), НСтр("ru = 'Не удалось получить федеральный протокол'"));
			КонецЕсли;
			
			ПараметрыСоединения.ОписаниеОшибкиСервером = "";
			ПараметрыСоединения.ПовторятьСоединение = Ложь;
			
			Возврат ЗаголовокДекларации;
			
		КонецЕсли;
		
	Иначе
		ПараметрыСоединения.СоединениеHTTP = Неопределено;
	КонецЕсли;
	
	Возврат Неопределено;
	
КонецФункции

Функция НайтиЗаголовокДекларацииНаПорталеФСРАР(ИмяФайлаВыгрузки, ПараметрыСоединения)
	
	ПараметрыСоединения.ОписаниеОшибкиСервером = "";
	ПараметрыСоединения.ПовторятьСоединение = Ложь;
	
	ЭлементовНаСтранице = 25;
	НомерСтраницы = 1;
	
	Пока Истина Цикл
		
		ЗаголовкиHTTP = Новый Соответствие();
		ЗаголовкиHTTP.Вставить("Content-Type", "application/x-www-form-urlencoded");
		ЗаголовкиHTTP.Вставить("Proxy-Connection", "Keep-Alive");
		ЗаголовкиHTTP.Вставить("Pragma", "no-cache");
		Если ЗначениеЗаполнено(ПараметрыСоединения.ЗначениеCookie) Тогда
			ЗаголовкиHTTP.Вставить("Cookie", ПараметрыСоединения.ЗначениеCookie);
		КонецЕсли;
		
		ТекстЗапроса = "page=" + Формат(НомерСтраницы, "ЧН=0; ЧГ=") + "&start=" + Формат((НомерСтраницы - 1) * ЭлементовНаСтранице, "ЧН=0; ЧГ=")
			+ "&limit=" + Формат(ЭлементовНаСтранице, "ЧН=0; ЧГ=") + "&sort=%5B%7B%22property%22%3A%22insdate%22%2C%22direction%22%3A%22DESC%22%7D%5D";
		
		ИмяФайлаЗапроса = ПолучитьИмяВременногоФайла();
		ЗаписываемыйФайл = Новый ЗаписьТекста(ИмяФайлаЗапроса, "windows-1251");
		ЗаписываемыйФайл.Записать(ТекстЗапроса);
		ЗаписываемыйФайл.Закрыть();
		
		ИмяФайлаОтвета = ПолучитьИмяВременногоФайла();
		
		Попытка
			РесурсНаСервере = ДокументооборотСКО.ПолучитьЗаменяемоеЗначениеТестовогоСервера1С("/declaring/getdeclarations?_dc=", "{FSRAR}/declaring/getdeclarations?_dc=", ПараметрыСоединения.СоединениеHTTP);
			ОтветHTTP = ПараметрыСоединения.СоединениеHTTP.ОтправитьДляОбработки(ИмяФайлаЗапроса, РесурсНаСервере, ИмяФайлаОтвета, ЗаголовкиHTTP);
		Исключение
			
			ПараметрыСоединения.ПовторятьСоединение = Истина;
			ОперацииСФайламиЭДКО.УдалитьВременныйФайл(ИмяФайлаЗапроса);
			ОперацииСФайламиЭДКО.УдалитьВременныйФайл(ИмяФайлаОтвета);
			Возврат Неопределено;
			
		КонецПопытки;
		
		ФайлОтвета = Новый ЧтениеТекста(ИмяФайлаОтвета, "windows-1251");
		ТекстОтвета = ФайлОтвета.Прочитать();
		ФайлОтвета.Закрыть();
		
		ЗначениеSetCookie = ОтветHTTP.Заголовки.Получить("Set-Cookie");
		ПараметрыСоединения.ЗначениеCookie = ОбновленноеЗначениеCookie(ПараметрыСоединения.ЗначениеCookie, ЗначениеSetCookie);
		
		ОперацииСФайламиЭДКО.УдалитьВременныйФайл(ИмяФайлаЗапроса);
		ОперацииСФайламиЭДКО.УдалитьВременныйФайл(ИмяФайлаОтвета);
		
		// разбор списка заголовков деклараций
		ЗаголовкиДеклараций = ТаблицаJSON(ТекстОтвета, """decls"":[",
			"ИдентификаторЗаписи, ИмяФайла, СостояниеJSON, КодСостояния, ДатаЗагрузки, СтатусJSON",
			"id, filename, curr_state_Descr, curr_state, insdate, status_name");
			
		Если ЗаголовкиДеклараций = Неопределено Тогда
			
			ПараметрыСоединения.ОписаниеОшибкиСервером = ТекстОшибкиПорталаФСРАР(ТекстОтвета);
			Возврат Неопределено;
			
		КонецЕсли;
			
		Для Каждого СтрокаЗаголовковДеклараций Из ЗаголовкиДеклараций Цикл
			Если ВРег(СтрокаЗаголовковДеклараций.ИмяФайла) = ВРег(ИмяФайлаВыгрузки) Тогда
				
				СостояниеДекларации = ?(ЗначениеЗаполнено(СтрокаЗаголовковДеклараций.СостояниеJSON),
					СтрокаЗаголовковДеклараций.СостояниеJSON, СтрокаЗаголовковДеклараций.СтатусJSON);
				СостояниеДекларации = ТекстЗначенияJSONВШестнадцатеричнойUTF16(СостояниеДекларации);
				СостояниеДекларации = СокрЛП(ТекстИзHTML(СостояниеДекларации));
				
				ЗаголовокДекларации = Новый Структура("ИдентификаторЗаписи, ИмяФайла, Состояние, ЗначениеСостояния, ДатаЗагрузки, РегиональныйПротокол, ОшибкаПолученияРегиональногоПротокола, ФедеральныйПротокол, ОшибкаПолученияФедеральногоПротокола",
					СтрокаЗаголовковДеклараций.ИдентификаторЗаписи, СтрокаЗаголовковДеклараций.ИмяФайла, СостояниеДекларации,
					СтрокаЗаголовковДеклараций.КодСостояния, СтрокаЗаголовковДеклараций.ДатаЗагрузки, Неопределено, "", Неопределено, "");
				
				Возврат ЗаголовокДекларации;
				
			КонецЕсли;
		КонецЦикла;
		
		Если ЗаголовкиДеклараций.Количество() < ЭлементовНаСтранице Тогда
			Возврат Неопределено;
		КонецЕсли;
		
		НомерСтраницы = НомерСтраницы + 1;
		
	КонецЦикла;
	
КонецФункции

Функция ПолучитьПротоколОбработкиДекларацииНаПорталеФСРАР(ИдентификаторЗаписи, ИмяФайлаВыгрузки, ФедеральныйПротокол, ПараметрыСоединения)
	
	ПараметрыСоединения.ОписаниеОшибкиСервером = "";
	ПараметрыСоединения.ПовторятьСоединение = Ложь;
	
	ЗаголовкиHTTP = Новый Соответствие();
	ЗаголовкиHTTP.Вставить("Content-Type", "application/x-www-form-urlencoded");
	ЗаголовкиHTTP.Вставить("Proxy-Connection", "Keep-Alive");
	ЗаголовкиHTTP.Вставить("Pragma", "no-cache");
	Если ЗначениеЗаполнено(ПараметрыСоединения.ЗначениеCookie) Тогда
		ЗаголовкиHTTP.Вставить("Cookie", ПараметрыСоединения.ЗначениеCookie);
	КонецЕсли;
	
	ТекстЗапроса = ?(ФедеральныйПротокол, "decl_id=" + ИдентификаторЗаписи, "filename=" + ИмяФайлаВыгрузки) + "&page=1&start=0&limit=25";
	
	ИмяФайлаЗапроса = ПолучитьИмяВременногоФайла();
	ЗаписываемыйФайл = Новый ЗаписьТекста(ИмяФайлаЗапроса, "windows-1251");
	ЗаписываемыйФайл.Записать(ТекстЗапроса);
	ЗаписываемыйФайл.Закрыть();
	
	ИмяФайлаОтвета = ПолучитьИмяВременногоФайла();
	
	Попытка
		РесурсНаСервере = ?(ФедеральныйПротокол, ДокументооборотСКО.ПолучитьЗаменяемоеЗначениеТестовогоСервера1С("/declaring/getprotocols?_dc=", "{FSRAR}/declaring/getprotocols?_dc=", ПараметрыСоединения.СоединениеHTTP), 
												 ДокументооборотСКО.ПолучитьЗаменяемоеЗначениеТестовогоСервера1С("/declaring/getregprotocols?_dc=", "{FSRAR}/declaring/getregprotocols?_dc=", ПараметрыСоединения.СоединениеHTTP));
		ОтветHTTP = ПараметрыСоединения.СоединениеHTTP.ОтправитьДляОбработки(ИмяФайлаЗапроса, РесурсНаСервере, ИмяФайлаОтвета, ЗаголовкиHTTP);
	Исключение
		
		ПараметрыСоединения.ПовторятьСоединение = Истина;
		ОперацииСФайламиЭДКО.УдалитьВременныйФайл(ИмяФайлаЗапроса);
		ОперацииСФайламиЭДКО.УдалитьВременныйФайл(ИмяФайлаОтвета);
		Возврат Неопределено;
		
	КонецПопытки;
	
	ФайлОтвета = Новый ЧтениеТекста(ИмяФайлаОтвета, "windows-1251");
	ТекстОтвета = ФайлОтвета.Прочитать();
	ФайлОтвета.Закрыть();
	
	ЗначениеSetCookie = ОтветHTTP.Заголовки.Получить("Set-Cookie");
	ПараметрыСоединения.ЗначениеCookie = ОбновленноеЗначениеCookie(ПараметрыСоединения.ЗначениеCookie, ЗначениеSetCookie);
	
	ОперацииСФайламиЭДКО.УдалитьВременныйФайл(ИмяФайлаЗапроса);
	ОперацииСФайламиЭДКО.УдалитьВременныйФайл(ИмяФайлаОтвета);
	
	// разбор протокола
	Протокол = ТаблицаJSON(ТекстОтвета, """protocols"":[",
		"Дата, Модуль, Сообщение",
		"controltime, Context, Message");
		
	Если Протокол = Неопределено Тогда
		
		ПараметрыСоединения.ОписаниеОшибкиСервером = ТекстОшибкиПорталаФСРАР(ТекстОтвета);
		Возврат Неопределено;
		
	КонецЕсли;
		
	Для Каждого СтрокаПротокола Из Протокол Цикл
		Для ИндексКолонки = 0 По Протокол.Колонки.Количество() - 1 Цикл
			
			ЗначениеКолонки = СтрокаПротокола.Получить(ИндексКолонки);
			ЗначениеКолонки = ТекстЗначенияJSONВШестнадцатеричнойUTF16(ЗначениеКолонки);
			ЗначениеКолонки = СокрЛП(ТекстИзHTML(ЗначениеКолонки));
			ЗначениеКолонки = СтрЗаменить(ЗначениеКолонки, "\r\n", " ");
			СтрокаПротокола.Установить(ИндексКолонки, ЗначениеКолонки);
			
		КонецЦикла;
	КонецЦикла;
	
	Возврат Протокол;
	
КонецФункции

////////////////////////////////////////////////////////////////////////////////
// Служебные функции ФСРАР

Функция ОтчетПодаетсяЧерезФедеральныйПортал(ОтчетСсылка, ОтправкаСсылка = Неопределено) Экспорт
	
	Если ОтчетСсылка = Неопределено Тогда
		ОтчетСсылка = ОтправкаСсылка.ОтчетСсылка;
	КонецЕсли;
	
	Если ТипЗнч(ОтчетСсылка) = Тип("ДокументСсылка.РегламентированныйОтчет") Тогда
		ВидОтчета = ВидОтправляемыхДокументовДляФСРАР(ОтчетСсылка.ИсточникОтчета);
		
	Иначе // электронные представления
		ВидОтчета = ОтчетСсылка.ВидОтчета;
	КонецЕсли;
	
	Результат = (ВидОтчета <> Неопределено И ВидОтчета.ТипДокумента = Перечисления.ТипыОтправляемыхДокументов.ОтчетФСРАРОпт);
	Возврат Результат;
	
КонецФункции

Функция НастройкиФСРАР(Знач ОрганизацияСсылка, Знач ПолучитьПараметрыОнлайнПроверки = Ложь) Экспорт
	
	Результат = Новый Структура("ИспользоватьОбмен, СертификатАбонентаОтпечаток, СертификатСубъектаРФОтпечаток, СертификатФСРАРОтпечаток, КодРегиона, ИспользоватьАвтонастройку, ПарольПорталаСубъектаРФ", Ложь, "", "", "", "", Ложь, "");
	
	Если ПолучитьПараметрыОнлайнПроверки Тогда
		СвойстваОрганизации = ЗначенияРеквизитовОбъекта(ОрганизацияСсылка, "ВидОбменаСКонтролирующимиОрганами, УчетнаяЗаписьОбмена");
		
		Результат.Вставить("УчетнаяЗаписьОбмена", СвойстваОрганизации.УчетнаяЗаписьОбмена);
		Результат.Вставить("НастроенОбменВУниверсальномФормате", СвойстваОрганизации.ВидОбменаСКонтролирующимиОрганами = Перечисления.ВидыОбменаСКонтролирующимиОрганами.ОбменВУниверсальномФормате
			И ЗначениеЗаполнено(СвойстваОрганизации.УчетнаяЗаписьОбмена));
		
		Попытка
			ОбменИОнлайнПроверкаВключены = Результат.НастроенОбменВУниверсальномФормате И НЕ СвойстваОрганизации.УчетнаяЗаписьОбмена.ОбменНапрямую И СвойстваОрганизации.УчетнаяЗаписьОбмена.ИспользоватьСервисОнлайнПроверкиОтчетов;
		Исключение
			ОбменИОнлайнПроверкаВключены = Ложь;
		КонецПопытки;
		
		ОнлайнПроверкаКонтролирующиеОрганы = ?(ОбменИОнлайнПроверкаВключены,
			?(СвойстваОрганизации.УчетнаяЗаписьОбмена.СпецоператорСвязи = Перечисления.СпецоператорыСвязи.Такском,
			"ФНС;ПФР;ФСС;ФСРАР;ФТС",
			ЭлектронныйДокументооборотСКонтролирующимиОрганамиВызовСервера.ПолучитьПараметрСпецоператора(
			СвойстваОрганизации.УчетнаяЗаписьОбмена.СпецоператорСвязи, "ОнлайнПроверкаКонтролирующиеОрганы")), "");
		ОнлайнПроверкаКонтролирующиеОрганы = СокрЛП(ОнлайнПроверкаКонтролирующиеОрганы);
		ОнлайнПроверкаКонтролирующиеОрганы = ВРег(ОнлайнПроверкаКонтролирующиеОрганы);
		
		ОнлайнПроверкаДоступна = СтрНайти(";" + ОнлайнПроверкаКонтролирующиеОрганы + ";", ";ФСРАР;") > 0;
		
		Результат.Вставить("ОнлайнПроверкаДоступна", ОнлайнПроверкаДоступна);
	КонецЕсли;
	
	Запрос = Новый Запрос;
	Запрос.Текст = "ВЫБРАТЬ РАЗРЕШЕННЫЕ ПЕРВЫЕ 1
				   |	НастройкиОбменаФСРАР.ИспользоватьОбмен КАК ИспользоватьОбмен,
				   |	НастройкиОбменаФСРАР.СертификатАбонентаОтпечаток КАК СертификатАбонентаОтпечаток,
				   |	НастройкиОбменаФСРАР.СертификатСубъектаРФОтпечаток КАК СертификатСубъектаРФОтпечаток,
				   |	НастройкиОбменаФСРАР.СертификатФСРАРОтпечаток КАК СертификатФСРАРОтпечаток,
				   |	НастройкиОбменаФСРАР.КодРегиона КАК КодРегиона,
				   |	НастройкиОбменаФСРАР.ИспользоватьАвтонастройку КАК ИспользоватьАвтонастройку,
				   |	НастройкиОбменаФСРАР.ПарольПорталаСубъектаРФ КАК ПарольПорталаСубъектаРФ
				   |ИЗ
				   |	РегистрСведений.НастройкиОбменаФСРАР КАК НастройкиОбменаФСРАР
				   |ГДЕ
				   |	НастройкиОбменаФСРАР.Организация = &ОрганизацияСсылка";
	Запрос.УстановитьПараметр("ОрганизацияСсылка", ОрганизацияСсылка);
	РезультатЗапроса = Запрос.Выполнить();
	Если НЕ РезультатЗапроса.Пустой() Тогда
		ЗаписьЗапроса = РезультатЗапроса.Выгрузить()[0];
		Если ЗаписьЗапроса.ИспользоватьОбмен Тогда
			
			Результат.ИспользоватьОбмен = ЗаписьЗапроса.ИспользоватьОбмен;
			Результат.СертификатАбонентаОтпечаток = СокрЛП(ЗаписьЗапроса.СертификатАбонентаОтпечаток);
			Результат.СертификатСубъектаРФОтпечаток = СокрЛП(ЗаписьЗапроса.СертификатСубъектаРФОтпечаток);
			Результат.СертификатФСРАРОтпечаток = СокрЛП(ЗаписьЗапроса.СертификатФСРАРОтпечаток);
			Результат.КодРегиона = СокрЛП(ЗаписьЗапроса.КодРегиона);
			Результат.ИспользоватьАвтонастройку = ЗаписьЗапроса.ИспользоватьАвтонастройку;
			Результат.ПарольПорталаСубъектаРФ = ЗаписьЗапроса.ПарольПорталаСубъектаРФ;
			
		КонецЕсли;
	КонецЕсли;
	
	Возврат Результат;
	
КонецФункции

// ОтправкаСсылка ссылается на загруженный объект
Функция СвойстваОбменаОрганизации(Знач ОрганизацияСсылка, Знач ОпределитьОтправкуЧерезФедеральныйПорталДляОтчета, Знач ОпределитьНастроенностьОбменаВУниверсальномФормате, Знач ПолучитьОсновныеСвойстваПоследнейОтправки, Знач ПолучитьИнформациюОРегионе = Ложь, Знач ОтправкаСсылка = Неопределено, ОтчетСсылка = Неопределено) Экспорт
	
	Результат = Новый Структура("ОтправкаЧерезФедеральныйПортал, НастроенОбменВУниверсальномФормате, СвойстваОтправки, НастройкиОбмена, ИнформацияОРегионе, ОрганизацияИзОтправки, ПравоИзмененияУчетнойЗаписи, ЭтоРегламентированныйОтчет, ЭтоЭлектроннаяПодписьВМоделиСервиса", Неопределено, Неопределено, Неопределено, Неопределено, Неопределено, Неопределено, Неопределено, Неопределено, Неопределено);
	
	Если ОпределитьОтправкуЧерезФедеральныйПорталДляОтчета Тогда
		
		Результат.ОтправкаЧерезФедеральныйПортал = ОтчетПодаетсяЧерезФедеральныйПортал(ОтчетСсылка, ОтправкаСсылка);
		
		Если Результат.ОтправкаЧерезФедеральныйПортал Тогда
			ПолучитьИнформациюОРегионе = Ложь;
		КонецЕсли;
		
	КонецЕсли;
	
	Если ОрганизацияСсылка = Неопределено Тогда
		
		ОрганизацияСсылка = ОтправкаСсылка.Организация;
		Результат.ОрганизацияИзОтправки = ОрганизацияСсылка;
		
	КонецЕсли;
	
	Результат.ЭтоЭлектроннаяПодписьВМоделиСервиса = ЭлектроннаяПодписьВМоделиСервисаБРОВызовСервера.ЭтоЭлектроннаяПодписьВМоделиСервиса(ОрганизацияСсылка);
	
	Если ОпределитьНастроенностьОбменаВУниверсальномФормате Тогда
		
		СвойстваОрганизации = ЗначенияРеквизитовОбъекта(ОрганизацияСсылка, "ВидОбменаСКонтролирующимиОрганами, УчетнаяЗаписьОбмена");
		
		Результат.НастроенОбменВУниверсальномФормате = (СвойстваОрганизации.ВидОбменаСКонтролирующимиОрганами = Перечисления.ВидыОбменаСКонтролирующимиОрганами.ОбменВУниверсальномФормате
			И ЗначениеЗаполнено(СвойстваОрганизации.УчетнаяЗаписьОбмена));
		
	КонецЕсли;
	
	Если ПолучитьОсновныеСвойстваПоследнейОтправки Тогда
		
		СвойстваОтправки = ПолучитьОсновныеСвойстваПоследнейОтправкиОтчетаВФСРАР(ОтчетСсылка);
		Если СвойстваОтправки <> Неопределено Тогда
			Результат.СвойстваОтправки = Новый Структура("Ссылка, Идентификатор, ИмяФайлаПакета, Статус, СхемаСдачиОтчетности, СтатусОтправкиНаРегиональныйСервер, ЗаверенныйПакетОтправлен",
				СвойстваОтправки.Ссылка, СвойстваОтправки.Идентификатор, СвойстваОтправки.ИмяФайлаПакета, СвойстваОтправки.Статус, СвойстваОтправки.СхемаСдачиОтчетности, СвойстваОтправки.СтатусОтправкиНаРегиональныйСервер, СвойстваОтправки.ЗаверенныйПакетОтправлен);
		КонецЕсли;
		
	КонецЕсли;
	
	Результат.НастройкиОбмена = НастройкиФСРАР(ОрганизацияСсылка);
	
	Результат.ИнформацияОРегионе = ИнформацияОРегионеФСРАР(?(ПолучитьИнформациюОРегионе, Результат.НастройкиОбмена.КодРегиона, ""));
	
	Результат.ПравоИзмененияУчетнойЗаписи = ПравоДоступа("Изменение", Метаданные.Справочники.УчетныеЗаписиДокументооборота);
	
	Если ОтчетСсылка <> Неопределено ИЛИ ОтправкаСсылка <> Неопределено Тогда
		
		Если ОтчетСсылка <> Неопределено Тогда
			ТипЗнчСсылкаНаОтчет = ТипЗнч(ОтчетСсылка);
		Иначе
			ТипЗнчСсылкаНаОтчет = ТипЗнч(ОтправкаСсылка.ОтчетСсылка);
		КонецЕсли;
		
		Результат.ЭтоРегламентированныйОтчет = (ТипЗнчСсылкаНаОтчет = Тип("ДокументСсылка.РегламентированныйОтчет") ИЛИ ТипЗнчСсылкаНаОтчет = Тип("Неопределено"));
		
	КонецЕсли;
	
	Возврат Результат;
	
КонецФункции

Функция ИнформацияОРегионеФСРАР(Знач КодРегиона) Экспорт
	
	Результат = Новый Структура("НаименованиеРегиона, ТипПортала, СсылкаНаПортал", "", "", "");
	
	Если ЗначениеЗаполнено(КодРегиона) Тогда
		
		Макет = ПолучитьМакет("ФСРАРПорталыРегионов");
		НомерКолонкиКодРегиона = Макет.Область("КодРегиона").Лево;
		ОбластьПоиска = Макет.Область(1, НомерКолонкиКодРегиона, Макет.ВысотаТаблицы, НомерКолонкиКодРегиона);
		
		Ячейка = Макет.НайтиТекст(КодРегиона, , ОбластьПоиска, , Истина);
		
		Если Ячейка <> Неопределено Тогда
			
			НомерСтроки = Ячейка.Верх;
			
			НомерКолонкиНаименованиеРегиона = Макет.Область("НаименованиеРегиона").Лево;
			Результат.НаименованиеРегиона = Макет.Область(НомерСтроки, НомерКолонкиНаименованиеРегиона).Текст;
			НомерКолонкиТипПортала = Макет.Область("ТипПортала").Лево;
			Результат.ТипПортала = Макет.Область(НомерСтроки, НомерКолонкиТипПортала).Текст;
			НомерКолонкиСсылкаНаПортал = Макет.Область("СсылкаНаПортал").Лево;
			Результат.СсылкаНаПортал = Макет.Область(НомерСтроки, НомерКолонкиСсылкаНаПортал).Текст;
			
		КонецЕсли;
	
	КонецЕсли;
	
	Возврат Результат;
	
КонецФункции

Функция УспешноОбработанаДекларацияФСРАР(Протокол, СхемаСдачиОтчетности, НаходитсяВОчередиЗагрузки, ПротоколПуст = Неопределено)
	
	КоличествоСтрокПротокола = ?(Протокол <> Неопределено, Протокол.Количество(), 0);
	ПротоколПуст = (КоличествоСтрокПротокола = 0);
	
	Если СхемаСдачиОтчетности = Перечисления.СхемыСдачиОтчетностиФСРАР.МосковскийРегиональныйИФедеральныйСерверы Тогда
		Если КоличествоСтрокПротокола > 0 Тогда
			ПоследняяСтрокаПротокола = Протокол.Получить(КоличествоСтрокПротокола - 1).Сообщение;
			
			Результат = (Лев(ПоследняяСтрокаПротокола, 1) <> "-" И СтрНайти(ПоследняяСтрокаПротокола, "не может быть загружена") = 0 И СтрНайти(ПоследняяСтрокаПротокола, "Ошибка при") = 0);
			Если Результат И НаходитсяВОчередиЗагрузки Тогда
				Результат = Неопределено;
			КонецЕсли;
			Возврат Результат;
			
		Иначе
			Возврат Неопределено;
		КонецЕсли;
		
	Иначе
		НомерСтроки = КоличествоСтрокПротокола - 1;
		Пока НомерСтроки >= 0 Цикл
			СтрокаПротокола = Протокол.Получить(НомерСтроки).Сообщение;
			
			Если СтрНайти(СтрокаПротокола, "Декларация успешно обработана и сохранена в базу.") > 0 Тогда
				Возврат Истина;
				
			ИначеЕсли СтрНайти(СтрокаПротокола, "содержит ошибки.") > 0 ИЛИ СтрНайти(СтрокаПротокола, "Подробнее об ошибке") > 0
				ИЛИ СтрНайти(СтрокаПротокола, "Ошибка при") > 0 ИЛИ СтрНайти(СтрокаПротокола, "Декларация уже есть в базе.") > 0
				ИЛИ СтрНайти(СтрокаПротокола, "Процедура или функция") > 0 ИЛИ СтрНайти(СтрокаПротокола, "не был указан") > 0 Тогда
				Возврат Ложь;
				
			ИначеЕсли СтрНайти(СтрокаПротокола, "Файл загружен и ожидает проверки") > 0 ИЛИ СтрНайти(СтрокаПротокола, "Краткая информация о декларации") > 0 Тогда
				Возврат Неопределено;
			КонецЕсли;
			
			НомерСтроки = НомерСтроки - 1;
		КонецЦикла;
	КонецЕсли;
	
КонецФункции

Функция ВидОтправляемыхДокументовДляФСРАР(ИсточникОтчета)
	
	Если ИсточникОтчета = "РегламентированныйОтчетАлкоПриложение19" Тогда
		Результат = Справочники.ВидыОтправляемыхДокументов.ПроизводствоИОборотАлкогольнойИСпиртосодержащейПродукции;
	ИначеЕсли ИсточникОтчета = "РегламентированныйОтчетАлкоПриложение20" Тогда
		Результат = Справочники.ВидыОтправляемыхДокументов.ОборотЭтиловогоСпиртаАлкогольнойИСпиртосодержащейПродукции;
	ИначеЕсли ИсточникОтчета = "РегламентированныйОтчетАлкоПриложение21" Тогда
		Результат = Справочники.ВидыОтправляемыхДокументов.ПоставкаЭтиловогоСпиртаАлкогольнойИСпиртосодержащейПродукции;
	ИначеЕсли ИсточникОтчета = "РегламентированныйОтчетАлкоПриложение22" Тогда
		Результат = Справочники.ВидыОтправляемыхДокументов.ЗакупкаЭтиловогоСпиртаАлкогольнойИСпиртосодержащейПродукции;
	ИначеЕсли ИсточникОтчета = "РегламентированныйОтчетАлкоПриложение23" Тогда
		Результат = Справочники.ВидыОтправляемыхДокументов.ОбъемПеревозкиЭтиловогоСпиртаАлкогольнойИСпиртосодержащейПродукции;
	ИначеЕсли ИсточникОтчета = "РегламентированныйОтчетАлкоПриложение24" Тогда
		Результат = Справочники.ВидыОтправляемыхДокументов.ИспользованиеМощностейПоПроизводствуЭтиловогоСпиртаИАлкогольнойПродукции;
	ИначеЕсли ИсточникОтчета = "РегламентированныйОтчетАлкоПриложение25" Тогда
		Результат = Справочники.ВидыОтправляемыхДокументов.РозничнаяПродажаАлкогольнойИСпиртосодержащейПродукции;
	ИначеЕсли ИсточникОтчета = "РегламентированныйОтчетАлкоПриложение26" Тогда
		Результат = Справочники.ВидыОтправляемыхДокументов.РозничнаяПродажаПиваИПивныхНапитков;
	ИначеЕсли ИсточникОтчета = "РегламентированныйОтчетАлкоПриложение27" Тогда
		Результат = Справочники.ВидыОтправляемыхДокументов.ОбъемСобранногоВиноградаДляПроизводстваВинодельческойПродукции;
	ИначеЕсли ИсточникОтчета = "РегламентированныйОтчетАлкоПриложение28" Тогда
		Результат = Справочники.ВидыОтправляемыхДокументов.ОбъемВиноградаИспользованногоДляПроизводстваВинаИгристогоВинаШампанского;
	ИначеЕсли ИсточникОтчета = "РегламентированныйОтчетАлкоПриложение29" Тогда
		Результат = Справочники.ВидыОтправляемыхДокументов.ОбъемВиноградаИспользованногоДляПроизводстваЗащищенныхВин;
	Иначе
		Результат = Справочники.ВидыОтправляемыхДокументов.НайтиПоРеквизиту("Источник", ИсточникОтчета);
		Если НЕ ЗначениеЗаполнено(Результат) И Лев(ИсточникОтчета, 37) = "РегламентированныйОтчетАлкоПриложение" Тогда
			Результат = Справочники.ВидыОтправляемыхДокументов.ОбъемВиноградаИспользованногоДляПроизводстваЗащищенныхВин;
		КонецЕсли;
	КонецЕсли;
	
	Возврат Результат;
	
КонецФункции

////////////////////////////////////////////////////////////////////////////////
// Общие служебные функции, используются при документообороте с ФСРАР

Функция ЗаписатьZipФайлПакета(Знач СтрокаBase64, Знач ИмяАрхивируемогоФайла) Экспорт
	
	// создаем временный каталог, в который помещаем пакуемый файл
	ВременныйКаталог = КаталогВременныхФайлов();
	РазделительПутиОС = ПолучитьРазделительПути();
	Если Прав(ВременныйКаталог, 1) <> РазделительПутиОС Тогда
		ВременныйКаталог = ВременныйКаталог + РазделительПутиОС;
	КонецЕсли;
	ВременныйКаталог = ВременныйКаталог + Строка(Новый УникальныйИдентификатор) + РазделительПутиОС;
	СоздатьКаталог(ВременныйКаталог);
	
	// выгружаем двоичные данные в файл
	ПолноеИмяАрхивируемогоФайла = ВременныйКаталог + ИмяАрхивируемогоФайла;
	Если ЭтоАдресВременногоХранилища(СтрокаBase64) Тогда
		ДвоичныеДанныеФайла = ПолучитьИзВременногоХранилища(СтрокаBase64);
	Иначе
		ДвоичныеДанныеФайла = Base64Значение(СтрокаBase64);
	КонецЕсли;
	ДвоичныеДанныеФайла.Записать(ПолноеИмяАрхивируемогоФайла);
	
	// пишем архив
	КороткоеИмяФайлаАрхива = Строка(Новый УникальныйИдентификатор);
	ПолноеИмяФайлаАрхива = ВременныйКаталог + КороткоеИмяФайлаАрхива;
	
	Попытка
		
		ОбъектЗаписьZip = Новый ЗаписьZipФайла(ПолноеИмяФайлаАрхива);
		ОбъектЗаписьZip.Добавить(ПолноеИмяАрхивируемогоФайла);
		ОбъектЗаписьZip.Записать();
		
	Исключение
		
		// уведомляем об ошибке, если она была
		СообщитьПользователю(ПодставитьПараметрыВСтроку(НСтр("ru = 'Ошибка создания архива: %1'"), ИнформацияОбОшибке().Описание));
		
		Попытка
			УдалитьФайлы(ВременныйКаталог);
		Исключение
		КонецПопытки;
		
		Возврат Неопределено;
		
	КонецПопытки;
	
	// помещаем архив обратно во временное хранилище
	Результат = ПоместитьВоВременноеХранилище(Новый ДвоичныеДанные(ПолноеИмяФайлаАрхива), Новый УникальныйИдентификатор);
	
	// удаляем временный каталог
	Попытка
		УдалитьФайлы(ВременныйКаталог);
	Исключение
	КонецПопытки;
	
	Возврат Результат;
	
КонецФункции

Функция ПоместитьФайлВоВременноеХранилищеНаСервере(СтрокаBase64Файла) Экспорт
	
	// получаем двоичные данные файла, переданного в виде строки Base64
	ДвоичныеДанныеФайла = Base64Значение(СтрокаBase64Файла);
	
	// помещаем данные обратно во временное хранилище
	Результат = ПоместитьВоВременноеХранилище(ДвоичныеДанныеФайла);
	
	Возврат Результат;
	
КонецФункции

Функция ЧислоИзШестнадцатеричного(ШестнадцатеричноеЗначение, ЗначениеПриОшибке = 0, ПозицияОшибки = 0)
	
	Результат = 0;
	ПозицияОшибки = 0;
	
	Для Позиция = 1 По СтрДлина(ШестнадцатеричноеЗначение) Цикл
		
		ТекущийСимвол = Сред(ШестнадцатеричноеЗначение, Позиция, 1);
		КодТекущегоСимвола = КодСимвола(ТекущийСимвол);
		
		Если КодТекущегоСимвола >= КодСимвола("0") И КодТекущегоСимвола <= КодСимвола("9") Тогда
			
			Результат = Результат * 16 + (КодТекущегоСимвола - КодСимвола("0"));
			
		ИначеЕсли КодТекущегоСимвола >= КодСимвола("A") И КодТекущегоСимвола <= КодСимвола("F") Тогда
			
			Результат = Результат * 16 + (КодТекущегоСимвола - КодСимвола("A") + 10);
			
		ИначеЕсли КодТекущегоСимвола >= КодСимвола("a") И КодТекущегоСимвола <= КодСимвола("f") Тогда
			
			Результат = Результат * 16 + (КодТекущегоСимвола - КодСимвола("a") + 10);
			
		Иначе
			
			ПозицияОшибки = Позиция;
			Результат = ЗначениеПриОшибке;
			Прервать;
			
		КонецЕсли;
		
	КонецЦикла;
	
	Возврат Результат;
	
КонецФункции

Функция ТекстИзHTML(ТекстHTML)
	
	Результат = "";
	Позиция = 1;
	
	Пока Позиция <= СтрДлина(ТекстHTML) Цикл
		
		ТекущийСимвол = Сред(ТекстHTML, Позиция, 1);
		СимволHTML = Неопределено;
		
		Если ТекущийСимвол = "&" Тогда
			
			Для Длина = 2 По 10 Цикл
				Если Сред(ТекстHTML, Позиция + Длина - 1, 1) = ";" Тогда
					СимволHTML = Сред(ТекстHTML, Позиция, Длина);
					Прервать;
				КонецЕсли;
			КонецЦикла;
			
			Если СимволHTML <> Неопределено Тогда
				
				Если СимволHTML = "&quot;" ИЛИ СимволHTML = "&laquo;" ИЛИ СимволHTML = "&raquo;" Тогда
					Результат = Результат + """";
				ИначеЕсли СимволHTML = "&nbsp;" Тогда
					Результат = Результат + " ";
				ИначеЕсли СимволHTML = "&amp;" Тогда
					Результат = Результат + "&";
				ИначеЕсли СимволHTML = "&lt;" Тогда
					Результат = Результат + "<";
				ИначеЕсли СимволHTML = "&gt;" Тогда
					Результат = Результат + ">";
				ИначеЕсли Сред(СимволHTML, 2, 1) = "#" И СтрДлина(СимволHTML) > 3 Тогда
				
					ПодстрокаСКодомСимволаВUTF16 = Сред(СимволHTML, 3, СтрДлина(СимволHTML) - 3);
					КодСимволаВUTF16 = Неопределено;
					Попытка
						КодСимволаВUTF16 = Число(ПодстрокаСКодомСимволаВUTF16);
					Исключение
					КонецПопытки;
					
					Если Формат(КодСимволаВUTF16, "ЧН=0; ЧГ=") <> ПодстрокаСКодомСимволаВUTF16 Тогда
						КодСимволаВUTF16 = Неопределено;
					КонецЕсли;
					
					Если КодСимволаВUTF16 <> Неопределено И КодСимволаВUTF16 >= 0 И КодСимволаВUTF16 <= 65535 Тогда
						Результат = Результат + Символ(КодСимволаВUTF16);
					Иначе
						СимволHTML = Неопределено;
					КонецЕсли;
				
				Иначе
					СимволHTML = Неопределено;
				КонецЕсли;
				
			КонецЕсли;
			
		КонецЕсли;
		
		Если СимволHTML = Неопределено Тогда
			Результат = Результат + ТекущийСимвол;
			Позиция = Позиция + 1;
		Иначе
			Позиция = Позиция + СтрДлина(СимволHTML);
		КонецЕсли;
		
	КонецЦикла;
	
	Возврат Результат;
	
КонецФункции

Функция СледующееЗначениеJSON(ТекстJSON, Позиция, ПредшествующийСимволРазделителя)
	
	Пока Позиция <= СтрДлина(ТекстJSON) Цикл
		
		Если Сред(ТекстJSON, Позиция, 1) <= " " Тогда
			Позиция = Позиция + 1;
		Иначе
			Прервать;
		КонецЕсли;
		
	КонецЦикла;
	
	Если Позиция <= СтрДлина(ТекстJSON) Тогда
		
		ТекущийСимвол = Сред(ТекстJSON, Позиция, 1);
		Если ТекущийСимвол = ":" ИЛИ ТекущийСимвол = "," ИЛИ ТекущийСимвол = ";" ИЛИ ТекущийСимвол = "{" ИЛИ ТекущийСимвол = "}"
			ИЛИ ТекущийСимвол = "[" ИЛИ ТекущийСимвол = "]" Тогда
			ПредшествующийСимволРазделителя = ТекущийСимвол;
			Позиция = Позиция + 1;
		КонецЕсли;
		
	КонецЕсли;
	
	Пока Позиция <= СтрДлина(ТекстJSON) Цикл
		
		Если Сред(ТекстJSON, Позиция, 1) <= " " Тогда
			Позиция = Позиция + 1;
		Иначе
			Прервать;
		КонецЕсли;
		
	КонецЦикла;
	
	ЗначениеВКавычках = Истина;
	
	Если Позиция <= СтрДлина(ТекстJSON) Тогда
		
		Если Сред(ТекстJSON, Позиция, 1) = """" Тогда
			Позиция = Позиция + 1;
		Иначе
			ЗначениеВКавычках = Ложь;
		КонецЕсли;
		
	КонецЕсли;
	
	ПозицияНачала = Позиция;
	ПозицияКонца = СтрДлина(ТекстJSON);
	
	Пока Позиция <= СтрДлина(ТекстJSON) Цикл
		
		ТекущийСимвол = Сред(ТекстJSON, Позиция, 1);
		Если ТекущийСимвол = """" И ЗначениеВКавычках И Сред(ТекстJSON, Позиция - 1, 1) <> "\" Тогда
			
			ПозицияКонца = Позиция - 1;
			Позиция = Позиция + 1;
			Прервать;
			
		ИначеЕсли (ТекущийСимвол = ":" ИЛИ ТекущийСимвол = "," ИЛИ ТекущийСимвол = ";" ИЛИ ТекущийСимвол = "{" ИЛИ ТекущийСимвол = "}"
			ИЛИ ТекущийСимвол = "[" ИЛИ ТекущийСимвол = "]") И НЕ ЗначениеВКавычках Тогда
			
			ПозицияКонца = Позиция - 1;
			Прервать;
			
		Иначе
			
			Позиция = Позиция + 1;
			
		КонецЕсли;
		
	КонецЦикла;
	
	Результат = Сред(ТекстJSON, ПозицияНачала, ПозицияКонца - ПозицияНачала + 1);
	Если ЗначениеВКавычках Тогда
		Результат = СтрЗаменить(Результат, "\""", """");
	КонецЕсли;
	Возврат Результат;
	
КонецФункции

Функция ТаблицаJSON(
		ТекстJSON,
		Префикс,
		ИменаКолонокЧерезЗапятую,
		ИдентификаторыКолонокЧерезЗапятую,
		ПодстрокаНаличияДанных = """success"":true",
		ЗначенияМассивыРазрешены = Ложь,
		ВнутренниеВложенныеЗначенияРазрешены = Ложь)
	
	Если ЗначениеЗаполнено(ПодстрокаНаличияДанных) И СтрНайти(ТекстJSON, ПодстрокаНаличияДанных) = 0 Тогда
		Возврат Неопределено;
	КонецЕсли;
	
	Позиция = СтрНайти(ТекстJSON, Префикс);
	Если Позиция = 0 Тогда
		Возврат Неопределено;
	КонецЕсли;
	Позиция = Позиция + СтрДлина(Префикс);
	
	Результат = Новый ТаблицаЗначений;
	СтрокаРезультата = Неопределено;
	
	ИменаКолонокСтроками = СтрЗаменить(ИменаКолонокЧерезЗапятую, ",", Символы.ПС);
	Для НомерСтроки = 1 По СтрЧислоСтрок(ИменаКолонокСтроками) Цикл
		ИмяКолонки = СокрЛП(СтрПолучитьСтроку(ИменаКолонокСтроками, НомерСтроки));
		Результат.Колонки.Добавить(ИмяКолонки);
	КонецЦикла;
	
	ИдентификаторыКолонок = Новый Массив;
	ИдентификаторыКолонокСтроками = СтрЗаменить(ИдентификаторыКолонокЧерезЗапятую, ",", Символы.ПС);
	Для НомерСтроки = 1 По СтрЧислоСтрок(ИдентификаторыКолонокСтроками) Цикл
		ИдентификаторКолонки = СокрЛП(СтрПолучитьСтроку(ИдентификаторыКолонокСтроками, НомерСтроки));
		ИдентификаторыКолонок.Добавить(ИдентификаторКолонки);
	КонецЦикла;
	
	Разделитель = "";
	ОжидаемыйРазделитель = "{";
	Пока Позиция <= СтрДлина(ТекстJSON) Цикл
		
		ИмяПараметра = СледующееЗначениеJSON(ТекстJSON, Позиция, Разделитель);
		
		Если Разделитель <> ОжидаемыйРазделитель И ВнутренниеВложенныеЗначенияРазрешены И Разделитель = "{" Тогда
			УровеньВложенности = 1;
			
			Пока Истина Цикл
				СледующееЗначениеJSON(ТекстJSON, Позиция, Разделитель);
				Если Разделитель = "{" Тогда
					УровеньВложенности = УровеньВложенности + 1;
				ИначеЕсли Разделитель = "}" Тогда
					УровеньВложенности = УровеньВложенности - 1;
				КонецЕсли;
				
				Если УровеньВложенности < 1 ИЛИ Позиция > СтрДлина(ТекстJSON) Тогда
					Прервать;
				КонецЕсли;
			КонецЦикла;
			
			ИмяПараметра = СледующееЗначениеJSON(ТекстJSON, Позиция, Разделитель);
		КонецЕсли;
		
		Если Разделитель <> ОжидаемыйРазделитель Тогда
			
			Если Разделитель <> "}" Тогда
				Прервать;
			КонецЕсли;
			СтрокаРезультата = Неопределено;
			
			СледующееЗначениеJSON(ТекстJSON, Позиция, Разделитель);
			Если Разделитель <> "," Тогда
				Прервать;
			КонецЕсли;
			
			ИмяПараметра = СледующееЗначениеJSON(ТекстJSON, Позиция, Разделитель);
			Если Разделитель <> "{" Тогда
				Прервать;
			КонецЕсли;
			
		КонецЕсли;
		
		ЗначениеПараметра = СледующееЗначениеJSON(ТекстJSON, Позиция, Разделитель);
		
		Если Разделитель <> ":" Тогда
			Прервать;
		КонецЕсли;
		
		Если ЗначенияМассивыРазрешены И ПустаяСтрока(ЗначениеПараметра) И Сред(ТекстJSON, Позиция, 1) = "[" Тогда
			
			УровеньВложенности = 1;
			ЗначениеПараметра = Новый Массив;
			ЭлементМассива = СледующееЗначениеJSON(ТекстJSON, Позиция, Разделитель);
			
			Пока Истина Цикл
				
				ЗначениеПараметра.Добавить(ЭлементМассива);
				
				ЭлементМассива = СледующееЗначениеJSON(ТекстJSON, Позиция, Разделитель);
				
				Если ВнутренниеВложенныеЗначенияРазрешены Тогда
					Если Разделитель = "[" Тогда
						УровеньВложенности = УровеньВложенности + 1;
					ИначеЕсли Разделитель = "]" Тогда
						УровеньВложенности = УровеньВложенности - 1;
					КонецЕсли;
					
					Если УровеньВложенности < 1 ИЛИ Позиция > СтрДлина(ТекстJSON) Тогда
						Прервать;
					КонецЕсли;
					
				Иначе
					Если Разделитель <> "," Тогда
						Прервать;
					КонецЕсли;
				КонецЕсли;
				
			КонецЦикла;
			
			Если Разделитель <> "]" Тогда
				Прервать;
			КонецЕсли;
			
		КонецЕсли;
		
		ИндексКолонки = ИдентификаторыКолонок.Найти(ИмяПараметра);
		Если ИндексКолонки <> Неопределено И ИндексКолонки < Результат.Колонки.Количество() Тогда
			
			Если СтрокаРезультата = Неопределено Тогда
				СтрокаРезультата = Результат.Добавить();
			КонецЕсли;
			СтрокаРезультата.Установить(ИндексКолонки, ЗначениеПараметра);
			
		КонецЕсли;
		
		ОжидаемыйРазделитель = ",";
		
	КонецЦикла;
	
	Возврат Результат;
	
КонецФункции

Функция ТекстЗначенияJSONВШестнадцатеричнойUTF16(ЗначениеJSON)
	
	Результат = "";
	Позиция = 1;
	Пока Позиция <= СтрДлина(ЗначениеJSON) Цикл
		
		ТекущийСимвол = Сред(ЗначениеJSON, Позиция, 1);
		Если ТекущийСимвол = "\" И Сред(ЗначениеJSON, Позиция + 1, 1) = "u" Тогда
			
			ШестнадцатеричныйКодСимволаUTF8 = Сред(ЗначениеJSON, Позиция + 2, 4);
			КодСимволаUTF8 = ЧислоИзШестнадцатеричного(ШестнадцатеричныйКодСимволаUTF8, Неопределено);
			Если КодСимволаUTF8 <> Неопределено Тогда
				
				Результат = Результат + Символ(КодСимволаUTF8);
				Позиция = Позиция + 6;
				ТекущийСимвол = Неопределено;
				
			КонецЕсли;
			
		КонецЕсли;
		
		Если ТекущийСимвол <> Неопределено Тогда
			
			Результат = Результат + ТекущийСимвол;
			Позиция = Позиция + 1;
			
		КонецЕсли;
		
	КонецЦикла;
	
	Возврат Результат;
	
КонецФункции

Функция ТекстИзСтрокиJSON(СтрокаJSON, ЗаменаПереводаСтроки = Неопределено)
	
	Результат = "";
	
	НомерСимвола = 1;
	НачалоНедобавленногоФрагмента = 1;
	Пока НомерСимвола + 5 <= СтрДлина(СтрокаJSON) Цикл
		Если Сред(СтрокаJSON, НомерСимвола, 2) = "\u" Тогда
			ШестнадцатеричныйКодСимвола = Сред(СтрокаJSON, НомерСимвола + 2, 4);
			КодСимволаЧислом = ЧислоИзШестнадцатеричного(ШестнадцатеричныйКодСимвола, -1);
			Если КодСимволаЧислом <> -1 Тогда
				Результат = Результат + Сред(СтрокаJSON, НачалоНедобавленногоФрагмента, НомерСимвола - НачалоНедобавленногоФрагмента) + Символ(КодСимволаЧислом);
				НомерСимвола = НомерСимвола + 6;
				НачалоНедобавленногоФрагмента = НомерСимвола;
				Продолжить;
			КонецЕсли;
		КонецЕсли;
		НомерСимвола = НомерСимвола + 1;
	КонецЦикла;
	Результат = Результат + Сред(СтрокаJSON, НачалоНедобавленногоФрагмента);
	
	Если ЗаменаПереводаСтроки <> Неопределено Тогда
		Результат = СтрЗаменить(Результат, "\r\n", ЗаменаПереводаСтроки);
		Результат = СтрЗаменить(Результат, "\r", ЗаменаПереводаСтроки);
		Результат = СтрЗаменить(Результат, "\n", ЗаменаПереводаСтроки);
	Иначе
		Результат = СтрЗаменить(Результат, "\r", Символы.ВК);
		Результат = СтрЗаменить(Результат, "\n", Символы.ПС);
	КонецЕсли;
	Результат = СтрЗаменить(Результат, "\t", Символы.Таб);
	Результат = СтрЗаменить(Результат, "\v", Символы.ВТаб);
	Результат = СтрЗаменить(Результат, "\f", Символы.ПФ);
	Результат = СтрЗаменить(Результат, "\\", "\");
	
	Возврат Результат;
	
КонецФункции

Функция ТекстИзUTF8(ТекстВКодировкеUTF8)
	
	Результат = "";
	
	// Исключение из стандарта 456.
	СимволыСКодами1056ДалееС152По187 = "И       Р   Ф ЦЧ Щ ЫЬЭЮ аб   ежз   л";
	СимволыСКодами1056ДалееС1025По1169 = "Ш ЁЪнвЯУ     С                                                                  иА когпмКМОН ТП                                                Хд";
	СимволыСКодами1056ДалееС8211По8250 = "ЖЗ   БВ  ГД    Е                       Л";
	СимволыСКодами1056ДалееС8470По8482 = "й           Й";
	СимволыСКодами1057ДалееС1026По1107 = "рс     ъьюэ  я                                                                   у";
	СимволыСКодами1057ДалееС8216По8364 = "ё т   ф цч    х         щ        ы                                                                                                                  ш";
	
	НомерСимвола = 1;
	Пока НомерСимвола <= СтрДлина(ТекстВКодировкеUTF8) Цикл
		
		ПервыйСимвол = Сред(ТекстВКодировкеUTF8, НомерСимвола, 1);
		КодПервогоСимвола = КодСимвола(ПервыйСимвол);
		
		Если КодПервогоСимвола <= 128 Тогда
			Результат = Результат + ПервыйСимвол;
			
		ИначеЕсли НомерСимвола < СтрДлина(ТекстВКодировкеUTF8) Тогда
			НомерСимвола = НомерСимвола + 1;
			ВторойСимвол = Сред(ТекстВКодировкеUTF8, НомерСимвола, 1);
			КодВторогоСимвола = КодСимвола(ВторойСимвол);
			
			Если КодПервогоСимвола = 1056 Тогда
				Если КодВторогоСимвола >= 152 И КодВторогоСимвола <= 187 Тогда
					Результат = Результат + Сред(СимволыСКодами1056ДалееС152По187, КодВторогоСимвола - 151, 1);
				ИначеЕсли КодВторогоСимвола >= 1025 И КодВторогоСимвола <= 1169 Тогда
					Результат = Результат + Сред(СимволыСКодами1056ДалееС1025По1169, КодВторогоСимвола - 1024, 1);
				ИначеЕсли КодВторогоСимвола >= 8211 И КодВторогоСимвола <= 8250 Тогда
					Результат = Результат + Сред(СимволыСКодами1056ДалееС8211По8250, КодВторогоСимвола - 8210, 1);
				ИначеЕсли КодВторогоСимвола >= 8470 И КодВторогоСимвола <= 8482 Тогда
					Результат = Результат + Сред(СимволыСКодами1056ДалееС8470По8482, КодВторогоСимвола - 8469, 1);
				Иначе
					Результат = Результат + " ";
				КонецЕсли
				
			ИначеЕсли КодПервогоСимвола = 1057 Тогда
				Если КодВторогоСимвола >= 1026 И КодВторогоСимвола <= 1107 Тогда
					Результат = Результат + Сред(СимволыСКодами1057ДалееС1026По1107, КодВторогоСимвола - 1025, 1);
				ИначеЕсли КодВторогоСимвола >= 8216 И КодВторогоСимвола <= 8364 Тогда
					Результат = Результат + Сред(СимволыСКодами1057ДалееС8216По8364, КодВторогоСимвола - 8215, 1);
				Иначе
					Результат = Результат + " ";
				КонецЕсли;
				
			ИначеЕсли КодПервогоСимвола = 1042 И (КодВторогоСимвола = 171 ИЛИ КодВторогоСимвола = 187) Тогда
				Результат = Результат + """";
				
			ИначеЕсли КодПервогоСимвола = 1074 И КодВторогоСимвола = 8222 И НомерСимвола < СтрДлина(ТекстВКодировкеUTF8) Тогда
				НомерСимвола = НомерСимвола + 1;
				ТретийСимвол = Сред(ТекстВКодировкеUTF8, НомерСимвола, 1);
				КодТретьегоСимвола = КодСимвола(ТретийСимвол);
				Если КодТретьегоСимвола = 8211 Тогда
					Результат = Результат + "N";
				Иначе
					Результат = Результат + " ";
				КонецЕсли;
				
			Иначе
				Результат = Результат + " ";
			КонецЕсли;
			
		КонецЕсли;
		
		НомерСимвола = НомерСимвола + 1;
		
	КонецЦикла;
	
	Возврат Результат;
	
КонецФункции

Функция ЭлементыСсылкиНаПортал(СсылкаНаПортал, УбратьСлэшВКонцеСтраницы = Ложь) Экспорт
	
	ЗначениеСсылкиНаПортал = СокрЛП(СсылкаНаПортал);
	ЗащищенноеСоединение = Ложь;
	АдресСервера = ЗначениеСсылкиНаПортал;
	Страница = "";
	
	Если нрег(Лев(ЗначениеСсылкиНаПортал, 7)) = "http://" Тогда
		ЗначениеСсылкиНаПортал = Сред(ЗначениеСсылкиНаПортал, 8);
	ИначеЕсли нрег(Лев(ЗначениеСсылкиНаПортал, 8)) = "https://" Тогда
		ЗначениеСсылкиНаПортал = Сред(ЗначениеСсылкиНаПортал, 9);
		ЗащищенноеСоединение = Истина;
	КонецЕсли;
	
	ПозицияСлэша = СтрНайти(ЗначениеСсылкиНаПортал, "/");
	ПозицияВопроса = СтрНайти(ЗначениеСсылкиНаПортал, "?");
	ПозицияРазделителя = ПозицияСлэша;
	Если ПозицияСлэша > 0 И ПозицияВопроса > 0 И ПозицияВопроса < ПозицияСлэша Тогда
		ПозицияРазделителя = ПозицияВопроса;
	КонецЕсли;
	
	Если ПозицияРазделителя > 0 Тогда
		
		АдресСервера = Лев(ЗначениеСсылкиНаПортал, ПозицияРазделителя - 1);
		Страница = Сред(ЗначениеСсылкиНаПортал, ПозицияРазделителя);
		
		Если УбратьСлэшВКонцеСтраницы И Прав(Страница, 1) = "/" Тогда
			Страница = Лев(Страница, СтрДлина(Страница) - 1);
		КонецЕсли;
			
	КонецЕсли;
	
	Результат = Новый Структура("ЗащищенноеСоединение, АдресСервера, Страница", ЗащищенноеСоединение, АдресСервера, Страница);
	Возврат Результат;
	
КонецФункции

Функция ОбновленноеЗначениеCookie(ЗначениеCookie, ЗначениеSetCookie)
	
	Результат = ЗначениеCookie;
	
	Если НЕ ЗначениеЗаполнено(ЗначениеSetCookie) Тогда
		Возврат Результат;
	КонецЕсли;
	
	Результат = СтрЗаменить(Результат, ",", ";");
	
	ПредыдущаяПозицияSetCookie = 1;
	
	Для ПозицияSetCookie = 1 По СтрДлина(ЗначениеSetCookie) + 1 Цикл
		Если ПозицияSetCookie > СтрДлина(ЗначениеSetCookie) ИЛИ Сред(ЗначениеSetCookie, ПозицияSetCookie, 1) = ";" ИЛИ Сред(ЗначениеSetCookie, ПозицияSetCookie, 1) = "," Тогда
			
			ЭлементSetCookie = СокрЛП(Сред(ЗначениеSetCookie, ПредыдущаяПозицияSetCookie, ПозицияSetCookie - ПредыдущаяПозицияSetCookie));
			ИмяЭлементаSetCookie = "";
			ЗначениеЭлементаSetCookie = "";
			ПозицияРазделителя = СтрНайти(ЭлементSetCookie, "=");
			Если ПозицияРазделителя > 0 Тогда
				ИмяЭлементаSetCookie = СокрП(Лев(ЭлементSetCookie, ПозицияРазделителя - 1));
				ИмяЭлементаSetCookie = ВРег(ИмяЭлементаSetCookie);
				ЗначениеЭлементаSetCookie = СокрП(Сред(ЭлементSetCookie, ПозицияРазделителя + 1));
			КонецЕсли;
			
			Если ИмяЭлементаSetCookie <> "" И ИмяЭлементаSetCookie <> "PATH" И ИмяЭлементаSetCookie <> "MAX-AGE"
				И ИмяЭлементаSetCookie <> "EXPIRES" И ИмяЭлементаSetCookie <> "DOMAIN" И ИмяЭлементаSetCookie <> "SECURE"
				И ИмяЭлементаSetCookie <> "VERSION" И ИмяЭлементаSetCookie <> "COMMENT" И ИмяЭлементаSetCookie <> "COMMENTURL"
				И ИмяЭлементаSetCookie <> "DISCARD" И ИмяЭлементаSetCookie <> "PORT"
				И ЗначениеЭлементаSetCookie <> "" И ЗначениеЭлементаSetCookie <> """""" Тогда
				
				// Поиск элемента по ЗначениеCookie
				ПредыдущаяПозицияCookie = 1;
				ЭлементSetCookieНайден = Ложь;
				
				ПозицияCookie = 1;
				Пока ПозицияCookie <= СтрДлина(Результат) + 1 Цикл
					
					Если ПозицияCookie > СтрДлина(Результат) ИЛИ Сред(Результат, ПозицияCookie, 1) = ";" Тогда
						
						ЭлементCookie = СокрЛП(Сред(Результат, ПредыдущаяПозицияCookie, ПозицияCookie - ПредыдущаяПозицияCookie));
						ИмяЭлементаCookie = ЭлементCookie;
						ПозицияРазделителя = СтрНайти(ЭлементCookie, "=");
						Если ПозицияРазделителя > 0 Тогда
							ИмяЭлементаCookie = СокрП(Лев(ЭлементCookie, ПозицияРазделителя - 1));
						КонецЕсли;
						ИмяЭлементаCookie = ВРег(ИмяЭлементаCookie);
						
						Если ИмяЭлементаCookie = ИмяЭлементаSetCookie Тогда
							
							Результат = Лев(Результат, ПредыдущаяПозицияCookie - 1) + ЭлементSetCookie + Сред(Результат, ПозицияCookie);
							
							ПриростДлиныCookie = СтрДлина(ЭлементSetCookie) - (ПозицияCookie - ПредыдущаяПозицияCookie);
							ПозицияCookie = ПозицияCookie + ПриростДлиныCookie;
							
							ЭлементSetCookieНайден = Истина;
							
						КонецЕсли;
						
						ПредыдущаяПозицияCookie = ПозицияCookie + 1;
						
					КонецЕсли;
					
					ПозицияCookie = ПозицияCookie + 1;
					
				КонецЦикла;
				
				Если НЕ ЭлементSetCookieНайден Тогда
					
					РазделительВКонце = (Прав(СокрП(Результат), 1) = ";");
					Результат = Результат + ?(ПустаяСтрока(Результат) ИЛИ РазделительВКонце, "", ";") + ЭлементSetCookie;
					
				КонецЕсли;
				
			КонецЕсли;
			
			ПредыдущаяПозицияSetCookie = ПозицияSetCookie + 1;
			
		КонецЕсли;
	КонецЦикла;
	
	Возврат Результат;
	
КонецФункции

Функция ДатаИзСтрокиРазныхФорматов(СтрокаДаты)
	
	Результат = СокрЛП(СтрокаДаты);
	
	Попытка
		Возврат Дата(Результат);
	Исключение
	КонецПопытки;
	
	ЧисловаяДата = ТекстПослеПрефикса(Результат, "\/Date(", ")\/");
	Если ЧисловаяДата <> Неопределено Тогда
		Попытка
			Возврат '00010101000000' + Число(ЧисловаяДата) / 1000 + ('19700101000000' - '00010101000000');
		Исключение
		КонецПопытки;
	КонецЕсли;
	
	Результат = СтрЗаменить(Результат, "T", " "); // поддержка формата "2015-03-13T18:34:13"
	
	Если СтрДлина(Результат) = 10 Тогда
		Результат = Результат + " 00:00:00";
		
	ИначеЕсли СтрДлина(Результат) = 16 Тогда
		Результат = Результат + ":00"
	КонецЕсли;
	
	Если (СтрДлина(Результат) = 19 ИЛИ СтрДлина(Результат) = 23) И Сред(Результат, 11, 1) = " " И
		(Сред(Результат, 14, 1) = ":" ИЛИ Сред(Результат, 14, 1) = ".") И (Сред(Результат, 17, 1) = ":" ИЛИ Сред(Результат, 17, 1) = ".") Тогда
		
		Если (Сред(Результат, 3, 1) = "." ИЛИ Сред(Результат, 3, 1) = "-") И (Сред(Результат, 6, 1) = "." ИЛИ Сред(Результат, 6, 1) = "-") Тогда
			Результат = Сред(Результат, 7, 4) + Сред(Результат, 4, 2) + Сред(Результат, 1, 2) + Сред(Результат, 12, 2) + Сред(Результат, 15, 2) + Сред(Результат, 18, 2);
			
		ИначеЕсли (Сред(Результат, 5, 1) = "." ИЛИ Сред(Результат, 5, 1) = "-") И (Сред(Результат, 8, 1) = "." ИЛИ Сред(Результат, 8, 1) = "-") Тогда
			Результат = Сред(Результат, 1, 4) + Сред(Результат, 6, 2) + Сред(Результат, 9, 2) + Сред(Результат, 12, 2) + Сред(Результат, 15, 2) + Сред(Результат, 18, 2);
		КонецЕсли;
		
	КонецЕсли;
	
	Попытка
		Возврат Дата(Результат);
	Исключение
	КонецПопытки;
	
	Возврат СтрокаДаты;
	
КонецФункции

// Вторичное заявление на подключение к 1С-Отчетности  
//

Функция ИзменившиесяПараметрыПодключения(ПолныеСвойстваВсехСертификатов) Экспорт
	
	Результат = Новый Структура;
	Результат.Вставить("НапомнитьОбновитьКриптопровайдер", 	Ложь);
	Результат.Вставить("ИзменившиесяПараметрыПодключения", 	Неопределено);
	
	ТекДата = ТекущаяДатаСеанса();
	
	ИгнорироватьДатуПоследнегоОтображения = Ложь;
	
	// Сертификаты	
	КлючевыеДанныеОрганизацийТаблица = СертификатыУчетныхЗаписейПользователяСОрганизациями();
	КлючевыеДанныеОрганизацийМассив  = ОбщегоНазначения.ТаблицаЗначенийВМассив(КлючевыеДанныеОрганизацийТаблица);
	
	СертификатыТребующиеВнимания = ОставитьТолькоСертификатыТребующиеВнимания(
		КлючевыеДанныеОрганизацийТаблица,
		ПолныеСвойстваВсехСертификатов,
		ТекДата,
		ИгнорироватьДатуПоследнегоОтображения);
		
	// Лицензии
	ВсеЛицензии = ЛицензииУчетныхЗаписейПользователяСОрганизациями();
	ИстекающиеЛицензии = ОставитьТолькоИстекающиеЛицензииХранилища(
		ВсеЛицензии, 
		ТекДата,
		ИгнорироватьДатуПоследнегоОтображения);
	
	// Изменившиеся параметры подключения 
	ИзменившиесяРеквизитыОрганизаций = ИзменившиесяРеквизитыУчетнойЗаписи();
	СведенияДляБезбумажногоПродления = ИзменившиесяРеквизитыОрганизаций.Скопировать(, "Организация, УчетнаяЗаписьДокументооборота, СведенияДляБезбумажногоПродления");
	
	ОставитьТолькоИзменившиесяРеквизитыТребующиеНапоминания(
		ИзменившиесяРеквизитыОрганизаций, 
		ТекДата, 
		ИгнорироватьДатуПоследнегоОтображения);
	
	// Объединение данных об истекающих сертификатах, лицензиях, измененных параметрах в одну таблицу
	ИсключаемыеУчетныеЗаписи = УчетныеЗаписиТребующиеНапоминанияОПереходе();
	ИзменившиесяПараметрыПодключения = ОбъединитьПричиныОтправкиВторичногоЗаявления(
		СертификатыТребующиеВнимания, 
		ИстекающиеЛицензии, 
		ИзменившиесяРеквизитыОрганизаций,
		СведенияДляБезбумажногоПродления,
		ИсключаемыеУчетныеЗаписи);
		
	Результат.НапомнитьОбновитьКриптопровайдер = НапомнитьОбновитьКриптопровайдер(
		КлючевыеДанныеОрганизацийМассив,
		ПолныеСвойстваВсехСертификатов,
		ТекДата,
		ИсключаемыеУчетныеЗаписи,
		ИгнорироватьДатуПоследнегоОтображения);
	
	// Добавляем колонку с признаком поддержки вторичных заявлений
	ОпределитьДляКаждойОрганизацииВозможностьПоддержкиВторичныхЗаявлений(ИзменившиесяПараметрыПодключения);
	
	// Переделываем таблицу значений в массив структур для передачи на клиент.
	Результат.ИзменившиесяПараметрыПодключения = ОбщегоНазначения.ТаблицаЗначенийВМассив(ИзменившиесяПараметрыПодключения);
	
	Возврат Результат;
	
КонецФункции

Функция ИзменилсяСоставРеквизитов(ИзменившиесяРеквизиты, ИзменившиесяРеквизитыИзХранилища)
	
	СоставИзменился = Ложь;
	
	Если ИзменившиесяРеквизиты = Неопределено И ИзменившиесяРеквизитыИзХранилища <> Неопределено
		ИЛИ ИзменившиесяРеквизиты <> Неопределено И ИзменившиесяРеквизитыИзХранилища = Неопределено Тогда
		// Если один из списков пустой
		СоставИзменился = Истина;
	ИначеЕсли ИзменившиесяРеквизиты.Количество() <> ИзменившиесяРеквизитыИзХранилища.Количество() Тогда
		// Если количество элементов различно
		СоставИзменился = Истина;
	Иначе
		// Если количество элементов одинаково
		Для каждого ИзменившийсяРеквизит Из ИзменившиесяРеквизиты Цикл
			НайденныйИзменившийсяРеквизит = ИзменившиесяРеквизитыИзХранилища.НайтиПоЗначению(ИзменившийсяРеквизит.Значение);
			Если НайденныйИзменившийсяРеквизит = Неопределено Тогда 
				СоставИзменился = Истина;
				Прервать;
			КонецЕсли;
		КонецЦикла;
	КонецЕсли;
	
	Возврат СоставИзменился;
	
КонецФункции

Функция ИзменившиесяРеквизитыУчетнойЗаписи() Экспорт
	
	Запрос = Новый Запрос("ВЫБРАТЬ РАЗРЕШЕННЫЕ РАЗЛИЧНЫЕ
	                      |	ПользователиУчетныхЗаписейДокументооборота.УчетнаяЗапись КАК УчетнаяЗапись
	                      |ПОМЕСТИТЬ УчетныеЗаписиПользователя
	                      |ИЗ
	                      |	РегистрСведений.ПользователиУчетныхЗаписейДокументооборота КАК ПользователиУчетныхЗаписейДокументооборота
	                      |ГДЕ
	                      |	ПользователиУчетныхЗаписейДокументооборота.Пользователь = &Пользователь
	                      |;
	                      |
	                      |////////////////////////////////////////////////////////////////////////////////
	                      |ВЫБРАТЬ РАЗРЕШЕННЫЕ РАЗЛИЧНЫЕ
	                      |	Организации.Ссылка КАК Организация,
	                      |	УчетныеЗаписиДокументооборота.Ссылка КАК УчетнаяЗаписьДокументооборота
	                      |ИЗ
	                      |	Справочник.Организации КАК Организации
	                      |		ВНУТРЕННЕЕ СОЕДИНЕНИЕ Справочник.УчетныеЗаписиДокументооборота КАК УчетныеЗаписиДокументооборота
	                      |		ПО Организации.УчетнаяЗаписьОбмена = УчетныеЗаписиДокументооборота.Ссылка
	                      |ГДЕ
	                      |	Организации.ВидОбменаСКонтролирующимиОрганами = &ОбменВУниверсальномФормате
	                      |	И НЕ УчетныеЗаписиДокументооборота.ПометкаУдаления
	                      |	И Организации.ПометкаУдаления = ЛОЖЬ
	                      |	И (&НуженОтборПоПользователям
	                      |				И УчетныеЗаписиДокументооборота.Ссылка В
	                      |					(ВЫБРАТЬ
	                      |						УчетныеЗаписиПользователя.УчетнаяЗапись
	                      |					ИЗ
	                      |						УчетныеЗаписиПользователя)
	                      |			ИЛИ НЕ &НуженОтборПоПользователям)");
						  
	НуженОтборПоПользователям = ОбщегоНазначения.РазделениеВключено() 
		ИЛИ НЕ Пользователи.ЭтоПолноправныйПользователь();
						  
	Запрос.УстановитьПараметр("Пользователь", 				Пользователи.ТекущийПользователь());
	Запрос.УстановитьПараметр("ОбменВУниверсальномФормате", Перечисления.ВидыОбменаСКонтролирующимиОрганами.ОбменВУниверсальномФормате);
	Запрос.УстановитьПараметр("НуженОтборПоПользователям",  НуженОтборПоПользователям);
	ИзменившиесяРеквизитыОрганизаций = Запрос.Выполнить().Выгрузить();
	
	ИзменившиесяРеквизитыОрганизаций.Колонки.Добавить("СписокИзменившихсяРеквизитов");
	ИзменившиесяРеквизитыОрганизаций.Колонки.Добавить("СведенияДляБезбумажногоПродления");
	ИзменившиесяРеквизитыОрганизаций.Колонки.Добавить("ПредставлениеИзменившихсяРеквизитов", Новый ОписаниеТипов("Строка", , Новый КвалификаторыСтроки(300)));
	
	Для каждого ИзменившиесяРеквизитыОрганизации Из ИзменившиесяРеквизитыОрганизаций Цикл
		ДобавитьВТаблицуСписокИзменившихсяРеквизитовПодключенияК1СОтчетности(ИзменившиесяРеквизитыОрганизации);
	КонецЦикла; 
		
	Возврат ИзменившиесяРеквизитыОрганизаций;
	
КонецФункции

Функция ОбъединитьПричиныОтправкиВторичногоЗаявления(
		СертификатыТребующиеВнимания, 
		ИстекающиеЛицензии, 
		ИзменившиесяРеквизитыОрганизаций, 
		СведенияДляБезбумажногоПродления,
		ИсключаемыеУчетныеЗаписи)
		
	// Исключение из стандарта 282.
	// Возможны одинаковые записи, требуется их исключение из результатов запроса
	Запрос = Новый Запрос();
	Запрос.МенеджерВременныхТаблиц = Новый МенеджерВременныхТаблиц;
	ТекстЗапроса = "ВЫБРАТЬ РАЗРЕШЕННЫЕ РАЗЛИЧНЫЕ
	               |	СертификатыТребующиеВнимания.Организация КАК Организация,
	               |	СертификатыТребующиеВнимания.УчетнаяЗаписьДокументооборота КАК УчетнаяЗаписьДокументооборота,
	               |	СертификатыТребующиеВнимания.НаименованиеСертификата КАК НаименованиеСертификата,
	               |	СертификатыТребующиеВнимания.СертификатДействителенПо КАК СертификатДействителенПо,
	               |	СертификатыТребующиеВнимания.ОтпечатокСертификата КАК ОтпечатокСертификата,
				   |	СертификатыТребующиеВнимания.ОтпечатокНовогоСертификата КАК ОтпечатокНовогоСертификата,
				   |	СертификатыТребующиеВнимания.НовыйСертификатДействителенС КАК НовыйСертификатДействителенС,
				   |	СертификатыТребующиеВнимания.НовыйСертификатДействителенПо КАК НовыйСертификатДействителенПо
	               |ПОМЕСТИТЬ СертификатыТребующиеВнимания
	               |ИЗ
	               |	&СертификатыТребующиеВнимания КАК СертификатыТребующиеВнимания
	               |;
	               |
	               |////////////////////////////////////////////////////////////////////////////////
	               |ВЫБРАТЬ РАЗРЕШЕННЫЕ РАЗЛИЧНЫЕ
	               |	ИстекающиеЛицензии.Организация КАК Организация,
	               |	ИстекающиеЛицензии.УчетнаяЗаписьДокументооборота КАК УчетнаяЗаписьДокументооборота,
	               |	ИстекающиеЛицензии.ЛицензияНаименование КАК ЛицензияНаименование,
	               |	ИстекающиеЛицензии.ЛицензияДатаОкончания КАК ЛицензияДатаОкончания
	               |ПОМЕСТИТЬ ИстекающиеЛицензии
	               |ИЗ
	               |	&ИстекающиеЛицензии КАК ИстекающиеЛицензии
	               |;
	               |
	               |////////////////////////////////////////////////////////////////////////////////
	               |ВЫБРАТЬ РАЗРЕШЕННЫЕ РАЗЛИЧНЫЕ
	               |	ИзменившиесяРеквизитыОрганизаций.Организация КАК Организация,
	               |	ИзменившиесяРеквизитыОрганизаций.УчетнаяЗаписьДокументооборота КАК УчетнаяЗаписьДокументооборота,
	               |	ИзменившиесяРеквизитыОрганизаций.ПредставлениеИзменившихсяРеквизитов КАК ПредставлениеИзменившихсяРеквизитов
	               |ПОМЕСТИТЬ ИзменившиесяРеквизитыОрганизаций
	               |ИЗ
	               |	&ИзменившиесяРеквизитыОрганизаций КАК ИзменившиесяРеквизитыОрганизаций
	               |;
	               |
	               |////////////////////////////////////////////////////////////////////////////////
	               |ВЫБРАТЬ РАЗРЕШЕННЫЕ РАЗЛИЧНЫЕ
	               |	СертификатыТребующиеВнимания.Организация,
	               |	СертификатыТребующиеВнимания.УчетнаяЗаписьДокументооборота
	               |ПОМЕСТИТЬ УчетныеЗаписиОрганизаций
	               |ИЗ
	               |	СертификатыТребующиеВнимания КАК СертификатыТребующиеВнимания
	               |
	               |ОБЪЕДИНИТЬ ВСЕ
	               |
	               |ВЫБРАТЬ РАЗЛИЧНЫЕ
	               |	ИстекающиеЛицензии.Организация,
	               |	ИстекающиеЛицензии.УчетнаяЗаписьДокументооборота
	               |ИЗ
	               |	ИстекающиеЛицензии КАК ИстекающиеЛицензии
	               |
	               |ОБЪЕДИНИТЬ ВСЕ
	               |
	               |ВЫБРАТЬ РАЗЛИЧНЫЕ
	               |	ИзменившиесяРеквизитыОрганизаций.Организация,
	               |	ИзменившиесяРеквизитыОрганизаций.УчетнаяЗаписьДокументооборота
	               |ИЗ
	               |	ИзменившиесяРеквизитыОрганизаций КАК ИзменившиесяРеквизитыОрганизаций
	               |;
	               |
	               |////////////////////////////////////////////////////////////////////////////////
	               |ВЫБРАТЬ РАЗРЕШЕННЫЕ РАЗЛИЧНЫЕ
	               |	УчетныеЗаписиОрганизаций.Организация,
	               |	УчетныеЗаписиОрганизаций.УчетнаяЗаписьДокументооборота,
	               |	СертификатыТребующиеВнимания.ОтпечатокСертификата КАК ОтпечатокСертификата,
	               |	СертификатыТребующиеВнимания.НаименованиеСертификата КАК НаименованиеСертификата,
	               |	СертификатыТребующиеВнимания.СертификатДействителенПо КАК СертификатДействителенПо,
				   |	СертификатыТребующиеВнимания.ОтпечатокНовогоСертификата КАК ОтпечатокНовогоСертификата,
				   |	СертификатыТребующиеВнимания.НовыйСертификатДействителенС КАК НовыйСертификатДействителенС,
				   |	СертификатыТребующиеВнимания.НовыйСертификатДействителенПо КАК НовыйСертификатДействителенПо,				   
	               |	ИстекающиеЛицензии.ЛицензияНаименование КАК ЛицензияНаименование,
	               |	ИстекающиеЛицензии.ЛицензияДатаОкончания КАК ЛицензияДатаОкончания,
	               |	ИзменившиесяРеквизитыОрганизаций.ПредставлениеИзменившихсяРеквизитов КАК ПредставлениеИзменившихсяРеквизитов
	               |ИЗ
	               |	УчетныеЗаписиОрганизаций КАК УчетныеЗаписиОрганизаций
	               |		ЛЕВОЕ СОЕДИНЕНИЕ СертификатыТребующиеВнимания КАК СертификатыТребующиеВнимания
	               |		ПО УчетныеЗаписиОрганизаций.Организация = СертификатыТребующиеВнимания.Организация
	               |			И УчетныеЗаписиОрганизаций.УчетнаяЗаписьДокументооборота = СертификатыТребующиеВнимания.УчетнаяЗаписьДокументооборота
	               |		ЛЕВОЕ СОЕДИНЕНИЕ ИстекающиеЛицензии КАК ИстекающиеЛицензии
	               |		ПО УчетныеЗаписиОрганизаций.Организация = ИстекающиеЛицензии.Организация
	               |			И УчетныеЗаписиОрганизаций.УчетнаяЗаписьДокументооборота = ИстекающиеЛицензии.УчетнаяЗаписьДокументооборота
	               |		ЛЕВОЕ СОЕДИНЕНИЕ ИзменившиесяРеквизитыОрганизаций КАК ИзменившиесяРеквизитыОрганизаций
	               |		ПО УчетныеЗаписиОрганизаций.Организация = ИзменившиесяРеквизитыОрганизаций.Организация
	               |			И УчетныеЗаписиОрганизаций.УчетнаяЗаписьДокументооборота = ИзменившиесяРеквизитыОрганизаций.УчетнаяЗаписьДокументооборота
	               |ГДЕ
	               |	НЕ УчетныеЗаписиОрганизаций.УчетнаяЗаписьДокументооборота В (&ИсключаемыеУчетныеЗаписи)
	               |
	               |УПОРЯДОЧИТЬ ПО
	               |	УчетныеЗаписиОрганизаций.Организация";
	
	Запрос.Текст = ТекстЗапроса;
	Запрос.УстановитьПараметр("СертификатыТребующиеВнимания", 		СертификатыТребующиеВнимания);
	Запрос.УстановитьПараметр("ИстекающиеЛицензии", 				ИстекающиеЛицензии);
	Запрос.УстановитьПараметр("ИзменившиесяРеквизитыОрганизаций",	ИзменившиесяРеквизитыОрганизаций);
	Запрос.УстановитьПараметр("ИсключаемыеУчетныеЗаписи", 			ИсключаемыеУчетныеЗаписи);

	Результат = Запрос.Выполнить().Выгрузить();
	
	// Добавляем в результирующую таблицу нужные колонки.
	Результат.Колонки.Добавить("СписокИзменившихсяРеквизитов");
	Результат.Колонки.Добавить("СведенияДляБезбумажногоПродления");
	Результат.Колонки.Добавить("НовыйСертификат");
	
	Индекс = 1;
	Для каждого СтрокаРезультата Из Результат Цикл
		
		ВосстановитьСписокИзменившихсяРеквизитов(СтрокаРезультата, ИзменившиесяРеквизитыОрганизаций);
		ВосстановитьСведенияДляБезбумажногоПродления(СтрокаРезультата, СведенияДляБезбумажногоПродления);
		ВосстановитьОтпечатокНовогоСертификата(СтрокаРезультата, СертификатыТребующиеВнимания);
		
		Индекс = Индекс + 1;
	КонецЦикла; 
	
	Возврат Результат;
	
КонецФункции

Процедура ВосстановитьСведенияДляБезбумажногоПродления(СтрокаРезультата, СведенияДляБезбумажногоПродления)

	Отбор = Новый Структура();
	Отбор.Вставить("Организация", СтрокаРезультата.Организация);
	Отбор.Вставить("УчетнаяЗаписьДокументооборота", СтрокаРезультата.УчетнаяЗаписьДокументооборота);
	
	// Восстанавливаем СведенияДляБезбумажногоПродления, которые удалились запросом.
	// Если безбумажное продление невозможно, то восстанавливать реквизиты не имеет смысла.
	ЗаканчиваетсяЛицензия   = СтрокаРезультата.ЛицензияНаименование <> Null;
	ЗаканчиваетсяСертификат = СтрокаРезультата.ОтпечатокСертификата <> Null;
	
	БезбумажноеПродлениеВозможно = ЗаканчиваетсяЛицензия ИЛИ ЗаканчиваетсяСертификат;
	
	Если БезбумажноеПродлениеВозможно Тогда
		НайденныеСтроки = СведенияДляБезбумажногоПродления.НайтиСтроки(Отбор);
		
		Если НайденныеСтроки.Количество() > 0 Тогда 
			СтрокаРезультата.СведенияДляБезбумажногоПродления = НайденныеСтроки[0].СведенияДляБезбумажногоПродления;
		КонецЕсли;
	КонецЕсли;
	
КонецПроцедуры

Процедура ВосстановитьОтпечатокНовогоСертификата(СтрокаРезультата, СертификатыТребующиеВнимания)

	Отбор = Новый Структура();
	Отбор.Вставить("Организация", СтрокаРезультата.Организация);
	Отбор.Вставить("УчетнаяЗаписьДокументооборота", СтрокаРезультата.УчетнаяЗаписьДокументооборота);
	
	НайденныеСтроки = СертификатыТребующиеВнимания.НайтиСтроки(Отбор);
	
	Если НайденныеСтроки.Количество() > 0 Тогда 
		СтрокаРезультата.НовыйСертификат = НайденныеСтроки[0].НовыйСертификат;
	КонецЕсли;
	
КонецПроцедуры

Процедура ВосстановитьСписокИзменившихсяРеквизитов(СтрокаРезультата, ИзменившиесяРеквизитыОрганизаций)

	Отбор = Новый Структура();
	Отбор.Вставить("Организация", СтрокаРезультата.Организация);
	Отбор.Вставить("УчетнаяЗаписьДокументооборота", СтрокаРезультата.УчетнаяЗаписьДокументооборота);
	
	НайденныеСтроки = ИзменившиесяРеквизитыОрганизаций.НайтиСтроки(Отбор);
	
	// Восстанавливаем СписокИзменившихсяРеквизитов, который удалился запросом
	Если НайденныеСтроки.Количество() > 0 Тогда 
		СтрокаРезультата.СписокИзменившихсяРеквизитов = НайденныеСтроки[0].СписокИзменившихсяРеквизитов;
	КонецЕсли;
	
КонецПроцедуры

Функция СертификатыУчетныхЗаписейПользователяСОрганизациями()
	
	Запрос = Новый Запрос;
	
	Запрос.Текст = 
	"ВЫБРАТЬ РАЗРЕШЕННЫЕ РАЗЛИЧНЫЕ
	|	ПользователиУчетныхЗаписейДокументооборота.УчетнаяЗапись КАК УчетнаяЗапись
	|ПОМЕСТИТЬ УчетныеЗаписиПользователя
	|ИЗ
	|	РегистрСведений.ПользователиУчетныхЗаписейДокументооборота КАК ПользователиУчетныхЗаписейДокументооборота
	|ГДЕ
	|	ПользователиУчетныхЗаписейДокументооборота.Пользователь = &Пользователь
	|;
	|
	|////////////////////////////////////////////////////////////////////////////////
	|ВЫБРАТЬ РАЗРЕШЕННЫЕ РАЗЛИЧНЫЕ
	|	Организации.Ссылка КАК Организация,
	|	УчетныеЗаписиДокументооборота.Ссылка КАК УчетнаяЗаписьДокументооборота,
	|	УчетныеЗаписиДокументооборота.СертификатРуководителя КАК СертификатРуководителя
	|ИЗ
	|	Справочник.Организации КАК Организации
	|		ВНУТРЕННЕЕ СОЕДИНЕНИЕ Справочник.УчетныеЗаписиДокументооборота КАК УчетныеЗаписиДокументооборота
	|		ПО Организации.УчетнаяЗаписьОбмена = УчетныеЗаписиДокументооборота.Ссылка
	|ГДЕ
	|	Организации.ВидОбменаСКонтролирующимиОрганами = &ОбменВУниверсальномФормате
	|	И НЕ УчетныеЗаписиДокументооборота.ПометкаУдаления
	|	И Организации.ПометкаУдаления = ЛОЖЬ
	|	И (&НуженОтборПоПользователям
	|				И УчетныеЗаписиДокументооборота.Ссылка В
	|					(ВЫБРАТЬ
	|						УчетныеЗаписиПользователя.УчетнаяЗапись
	|					ИЗ
	|						УчетныеЗаписиПользователя)
	|			ИЛИ НЕ &НуженОтборПоПользователям)";
	
	НуженОтборПоПользователям = ОбщегоНазначения.РазделениеВключено() 
		ИЛИ НЕ Пользователи.ЭтоПолноправныйПользователь();
						  
	Запрос.УстановитьПараметр("Пользователь", 				Пользователи.ТекущийПользователь());
	Запрос.УстановитьПараметр("ОбменВУниверсальномФормате", Перечисления.ВидыОбменаСКонтролирующимиОрганами.ОбменВУниверсальномФормате);
	Запрос.УстановитьПараметр("НуженОтборПоПользователям",  НуженОтборПоПользователям);
	
	Результат = Новый ТаблицаЗначений;
	Результат.Колонки.Добавить("УчетнаяЗаписьДокументооборота", Новый ОписаниеТипов("СправочникСсылка.УчетныеЗаписиДокументооборота"));
	Результат.Колонки.Добавить("ОтпечатокСертификата", Новый ОписаниеТипов("Строка", , Новый КвалификаторыСтроки(40)));
	Результат.Колонки.Добавить("Организация", Новый ОписаниеТипов("СправочникСсылка.Организации"));
	Результат.Колонки.Добавить("НаименованиеСертификата", Новый ОписаниеТипов("Строка", , Новый КвалификаторыСтроки(100)));
	Результат.Колонки.Добавить("СертификатДействителенПо", Новый ОписаниеТипов("Дата", , Новый КвалификаторыДаты(ЧастиДаты.ДатаВремя)));
	Результат.Колонки.Добавить("ОтпечатокНовогоСертификата", Новый ОписаниеТипов("Строка", , Новый КвалификаторыСтроки(40)));
	Результат.Колонки.Добавить("НовыйСертификатДействителенС", Новый ОписаниеТипов("Дата", , Новый КвалификаторыДаты(ЧастиДаты.ДатаВремя)));
	Результат.Колонки.Добавить("НовыйСертификатДействителенПо", Новый ОписаниеТипов("Дата", , Новый КвалификаторыДаты(ЧастиДаты.ДатаВремя)));
	Результат.Колонки.Добавить("НовыйСертификат", Новый ОписаниеТипов("Структура"));
	
	Попытка
		
		Выборка = Запрос.Выполнить().Выбрать();
		
		Пока Выборка.Следующий() Цикл
			
			СертификатРуководителя 	= нрег(СокрЛП(Выборка.СертификатРуководителя));
			
			Если НЕ ПустаяСтрока(СертификатРуководителя) Тогда
				
				НоваяСтрокаРезультата = Результат.Добавить();
				НоваяСтрокаРезультата.УчетнаяЗаписьДокументооборота = Выборка.УчетнаяЗаписьДокументооборота;
				НоваяСтрокаРезультата.ОтпечатокСертификата 			= СертификатРуководителя;
				НоваяСтрокаРезультата.Организация 					= Выборка.Организация;
				
				ДанныеПереизданногоСертификата = ДанныеПереизданногоСертификата(Выборка.УчетнаяЗаписьДокументооборота);
				
				Если ДанныеПереизданногоСертификата <> Неопределено Тогда
					НоваяСтрокаРезультата.ОтпечатокНовогоСертификата 	= ДанныеПереизданногоСертификата.Отпечаток;
					НоваяСтрокаРезультата.НовыйСертификатДействителенС 	= ДанныеПереизданногоСертификата.ДатаНачала;
					НоваяСтрокаРезультата.НовыйСертификатДействителенПо = ДанныеПереизданногоСертификата.ДатаКонца;
				КонецЕсли;				
				
			КонецЕсли;
			
		КонецЦикла;
		
	Исключение
	КонецПопытки;
	
	Возврат Результат;
	
КонецФункции

Функция ДанныеПереизданногоСертификата(УчетнаяЗапись)
	
	СпецоператорСвязи = УчетнаяЗапись.СпецоператорСвязи;
	
	Сервис = ЭлектронныйДокументооборотСКонтролирующимиОрганами.ОбменССерверомСоздатьСервис(
		СпецоператорСвязи, 
		, 
		, , 
		Ложь);
	
	Если Сервис = Неопределено Тогда
		Возврат Неопределено;
	КонецЕсли;
	
	Попытка
		XDTOРезультат = Сервис.GetAlternativeCertInfo(УчетнаяЗапись.СертификатРуководителя);
	Исключение
		
		ИнформацияОбОшибке = ПодробноеПредставлениеОшибки(ИнформацияОбОшибке());
		ТекстОшибки = НСтр("ru = 'Не удалось получить данные с сервера Калуги-Астрал по причине: '") + ИнформацияОбОшибке;
		
		ЗаписьЖурналаРегистрации(
			НСтр("ru = 'Электронный документооборот с контролирующими органами. GetAlternativeCertInfo'"),
			УровеньЖурналаРегистрации.Ошибка,,,
			ТекстОшибки);
			
		Возврат Неопределено;
		
	КонецПопытки;
	
	ЧтениеXML = Новый ЧтениеXML;
	ЧтениеXML.УстановитьСтроку(XDTOРезультат);
	
	ПостроительДОМ 	= Новый ПостроительDOM();
	ДОМ 			= ПостроительДОМ.Прочитать(ЧтениеXML);
	
	Отпечаток = ЭлектронныйДокументооборотСКонтролирующимиОрганами.ПолучитьЗначениеУзлаXML(ДОМ, "thumbPrint");
	ДатаНачала = ЭлектронныйДокументооборотСКонтролирующимиОрганами.ПолучитьЗначениеУзлаXML(ДОМ, "startDate");
	ДатаКонца = ЭлектронныйДокументооборотСКонтролирующимиОрганами.ПолучитьЗначениеУзлаXML(ДОМ, "endDate");
	
	Если Отпечаток = "" Тогда
		// Нет такого сертификата 
		ДополнительныеПараметры = Неопределено;
	Иначе
		
		ДополнительныеПараметры = Новый Структура();
		ДополнительныеПараметры.Вставить("Отпечаток", НРег(Отпечаток));
		ДополнительныеПараметры.Вставить("ДатаНачала", МестноеВремя(ДатаНачала));
		ДополнительныеПараметры.Вставить("ДатаКонца", МестноеВремя(ДатаКонца));
		
	КонецЕсли;
	
	Возврат ДополнительныеПараметры;
	
	ЧтениеXML.Закрыть();
	
КонецФункции

Функция CNsДоверенныхСтороннихУЦ(ДоверенныеСторонниеУЦ = Неопределено) Экспорт
	
	CNs = Новый Массив;
	
	Если ДоверенныеСторонниеУЦ = Неопределено Тогда
		ДоверенныеСторонниеУЦ = ДоверенныеСторонниеУЦ();
	КонецЕсли;
		
	Если ДоверенныеСторонниеУЦ <> Неопределено Тогда
		Для каждого УдостоверяющийЦентр Из ДоверенныеСторонниеУЦ Цикл
			ОбщегоНазначенияКлиентСервер.ДополнитьМассив(
				CNs, 
				УдостоверяющийЦентр.Наименования, 
				Истина);
		КонецЦикла;
	КонецЕсли;
		
	Возврат CNs;
	
КонецФункции

Функция ЭтоДоверенныйСтороннийУЦ(CN, CNs = Неопределено) Экспорт
	
	Если CNs = Неопределено Тогда
		CNs = CNsДоверенныхСтороннихУЦ();
	КонецЕсли;
	
	Возврат CNs.Найти(CN) <> Неопределено;
	
КонецФункции

Функция ДоверенныеСторонниеУЦ() Экспорт
	
	Оператор = Перечисления.СпецоператорыСвязи.КалугаАстрал;
	ВебСервисОпределение = ЭлектронныйДокументооборотСКонтролирующимиОрганамиВызовСервера.ПолучитьПараметрСпецоператора(Оператор, "ВебСервисОпределение");
	
	Адрес = СтрРазделить(ВебСервисОпределение, "?")[0];
	
	Соединение = ОбщегоНазначенияЭДКО.СоединениеССерверомИнтернета(Адрес + "/GetSolidCaInfo");
	
	Заголовки = Новый Соответствие;
	Заголовки.Вставить("Content-Type", "application/x-www-form-urlencoded");
	Запрос = Новый HTTPЗапрос("/regservice.asmx/GetSolidCaInfo", Заголовки);
	
	Попытка
		Ответ = Соединение.ОтправитьДляОбработки(Запрос);
	Исключение
		
		ИнформацияОбОшибке = ПодробноеПредставлениеОшибки(ИнформацияОбОшибке());
		ТекстОшибки = НСтр("ru = 'Не удалось получить данные с сервера Калуги-Астрал по причине: '") + ИнформацияОбОшибке;
		
		ЗаписьЖурналаРегистрации(
			НСтр("ru = 'Электронный документооборот с контролирующими органами. GetSolidCaInfo'"),
			УровеньЖурналаРегистрации.Ошибка,,,
			ТекстОшибки);
			
		Возврат Неопределено;
		
	КонецПопытки;
	
	ОтветСтрокой = Ответ.ПолучитьТелоКакСтроку();
	
	ЧтениеXML = Новый ЧтениеXML;
	ЧтениеXML.УстановитьСтроку(ОтветСтрокой);
	
	ПостроительДОМ 	= Новый ПостроительDOM();
	ДОМ = ПостроительДОМ.Прочитать(ЧтениеXML);
	
	Содержимое = ЭлектронныйДокументооборотСКонтролирующимиОрганами.ПолучитьЗначениеУзлаXML(ДОМ, "string");
	Содержимое = "<?xml version=""1.0"" encoding=""windows-1251""?>" + Символы.ПС + Содержимое;
	
	ЧтениеXML2 = Новый ЧтениеXML;
	ЧтениеXML2.УстановитьСтроку(Содержимое);
	
	ПостроительДОМ2 	= Новый ПостроительDOM();
	ДОМ2 = ПостроительДОМ2.Прочитать(ЧтениеXML2);
	
	СторонниеУЦ = Новый Массив;
	
	УзлыУЦ = ДОМ2.ПолучитьЭлементыПоИмени("Ca");
	Для каждого УзелУЦ Из УзлыУЦ Цикл
		
		СтороннийУЦ = Новый Структура;

		ДочерниеУзлы = УзелУЦ.ДочерниеУзлы;
		Для каждого ДочернийУзел Из ДочерниеУзлы Цикл
			Если ДочернийУзел.ИмяУзла = "CaName" Тогда
				
				СтороннийУЦ.Вставить("Представление", ДочернийУзел.ТекстовоеСодержимое);
				
			ИначеЕсли ДочернийУзел.ИмяУзла = "Skids" Тогда
				
				УзлыИдентификаторы = ДочернийУзел.ДочерниеУзлы;
				Skids = Новый Массив;
				Для каждого УзелИдентификаторы Из УзлыИдентификаторы Цикл
					Skids.Добавить(УзелИдентификаторы.ТекстовоеСодержимое);
				КонецЦикла;
				СтороннийУЦ.Вставить("Skids", Skids);
				
			ИначеЕсли ДочернийУзел.ИмяУзла = "CNs" Тогда
				
				Наименования = Новый Массив;
				УзлыНаименования = ДочернийУзел.ДочерниеУзлы;
				Для каждого УзелНаименование Из УзлыНаименования Цикл
					Наименования.Добавить(УзелНаименование.ТекстовоеСодержимое);
				КонецЦикла;
				СтороннийУЦ.Вставить("Наименования", Наименования);
			
			КонецЕсли;
		КонецЦикла;
		
		СторонниеУЦ.Добавить(СтороннийУЦ);
		
	КонецЦикла;

	ЧтениеXML.Закрыть();
	
	Возврат СторонниеУЦ; 
	
КонецФункции

Функция ЛицензииУчетныхЗаписейПользователяСОрганизациями()
	
	Запрос = Новый Запрос("ВЫБРАТЬ РАЗРЕШЕННЫЕ РАЗЛИЧНЫЕ
	                      |	ПользователиУчетныхЗаписейДокументооборота.УчетнаяЗапись КАК УчетнаяЗапись
	                      |ПОМЕСТИТЬ УчетныеЗаписиПользователя
	                      |ИЗ
	                      |	РегистрСведений.ПользователиУчетныхЗаписейДокументооборота КАК ПользователиУчетныхЗаписейДокументооборота
	                      |ГДЕ
	                      |	ПользователиУчетныхЗаписейДокументооборота.Пользователь = &Пользователь
	                      |;
	                      |
	                      |////////////////////////////////////////////////////////////////////////////////
	                      |ВЫБРАТЬ РАЗРЕШЕННЫЕ РАЗЛИЧНЫЕ
	                      |	Организации.Ссылка КАК Организация,
	                      |	УчетныеЗаписиДокументооборота.Ссылка КАК УчетнаяЗаписьДокументооборота,
	                      |	УчетныеЗаписиДокументооборота.ЛицензияНаименование КАК ЛицензияНаименование,
	                      |	УчетныеЗаписиДокументооборота.ЛицензияДатаОкончания КАК ЛицензияДатаОкончания
	                      |ИЗ
	                      |	Справочник.Организации КАК Организации
	                      |		ВНУТРЕННЕЕ СОЕДИНЕНИЕ Справочник.УчетныеЗаписиДокументооборота КАК УчетныеЗаписиДокументооборота
	                      |		ПО Организации.УчетнаяЗаписьОбмена = УчетныеЗаписиДокументооборота.Ссылка
	                      |ГДЕ
	                      |	НЕ УчетныеЗаписиДокументооборота.ПометкаУдаления
	                      |	И УчетныеЗаписиДокументооборота.ЛицензияТребуетсяНапоминаниеОбОкончанииСрокаДействия = ИСТИНА
	                      |	И УчетныеЗаписиДокументооборота.ЛицензияДатаОкончания <> ДАТАВРЕМЯ(1, 1, 1)
	                      |	И Организации.ВидОбменаСКонтролирующимиОрганами = &ОбменВУниверсальномФормате
	                      |	И Организации.ПометкаУдаления = ЛОЖЬ
	                      |	И (&НуженОтборПоПользователям
	                      |				И УчетныеЗаписиДокументооборота.Ссылка В
	                      |					(ВЫБРАТЬ
	                      |						УчетныеЗаписиПользователя.УчетнаяЗапись
	                      |					ИЗ
	                      |						УчетныеЗаписиПользователя)
	                      |			ИЛИ НЕ &НуженОтборПоПользователям)");
						  
	НуженОтборПоПользователям = ОбщегоНазначения.РазделениеВключено() 
		ИЛИ НЕ Пользователи.ЭтоПолноправныйПользователь();
						  
	Запрос.УстановитьПараметр("Пользователь", 				Пользователи.ТекущийПользователь());
	Запрос.УстановитьПараметр("ОбменВУниверсальномФормате", Перечисления.ВидыОбменаСКонтролирующимиОрганами.ОбменВУниверсальномФормате);
	Запрос.УстановитьПараметр("НуженОтборПоПользователям",  НуженОтборПоПользователям);
	
	Результат = Запрос.Выполнить().Выгрузить();
		
	Возврат Результат;
	
КонецФункции

Функция ЗаявлениеАбонентаДляЗаменыСертификатаБылоСоздано(ОтпечатокСертификата, ДатаОтсчета)
	
	Запрос = Новый Запрос;
	Запрос.Текст = 
		"ВЫБРАТЬ РАЗРЕШЕННЫЕ РАЗЛИЧНЫЕ
		|	ЗаявлениеАбонентаСпецоператораСвязи.Ссылка
		|ИЗ
		|	Документ.ЗаявлениеАбонентаСпецоператораСвязи.ИзменившиесяРеквизитыВторичногоЗаявления КАК ЗаявлениеАбонентаСпецоператораСвязи
		|ГДЕ
		|	ЗаявлениеАбонентаСпецоператораСвязи.Ссылка.ТипЗаявления = &ТипЗаявления
		|	И ЗаявлениеАбонентаСпецоператораСвязи.Ссылка.Статус = &Статус
		|	И ЗаявлениеАбонентаСпецоператораСвязи.Ссылка.УчетнаяЗапись.СертификатРуководителя = &ОтпечатокСертификата
		|	И РАЗНОСТЬДАТ(НАЧАЛОПЕРИОДА(ЗаявлениеАбонентаСпецоператораСвязи.Ссылка.Дата, ДЕНЬ), НАЧАЛОПЕРИОДА(&ДатаОтсчета, ДЕНЬ), ДЕНЬ) <= 10
		|	И ЗаявлениеАбонентаСпецоператораСвязи.ИзмененныйРеквизит = &ИзмененныйРеквизит";

	Запрос.УстановитьПараметр("ОтпечатокСертификата", ОтпечатокСертификата);
	Запрос.УстановитьПараметр("Статус", Перечисления.СтатусыЗаявленияАбонентаСпецоператораСвязи.Отправлено);
	Запрос.УстановитьПараметр("ТипЗаявления", Перечисления.ТипыЗаявленияАбонентаСпецоператораСвязи.Изменение);
	Запрос.УстановитьПараметр("ДатаОтсчета", ДатаОтсчета);
	Запрос.УстановитьПараметр("ИзмененныйРеквизит", Перечисления.ПараметрыПодключенияК1СОтчетности.ПереизданиеСертификата);

	Результат = Запрос.Выполнить();
 	ЗаявленияАбонентаСпецоператораСвязи = Результат.Выбрать();

	Если ЗаявленияАбонентаСпецоператораСвязи.Количество() = 0 Тогда
		Возврат Ложь;
	Иначе
		Возврат Истина;
	КонецЕсли;
	
КонецФункции

Функция ЗаявлениеАбонентаДляПродленияЛицензииБылоСоздано(УчетнаяЗапись, ЛицензияНаименование, ЛицензияДатаОкончания, ДатаОтсчета)
	
	Запрос = Новый Запрос;
	Запрос.Текст = 
		"ВЫБРАТЬ РАЗРЕШЕННЫЕ РАЗЛИЧНЫЕ
		|	ЗаявлениеАбонентаСпецоператораСвязи.Ссылка
		|ИЗ
		|	Документ.ЗаявлениеАбонентаСпецоператораСвязи.ИзменившиесяРеквизитыВторичногоЗаявления КАК ЗаявлениеАбонентаСпецоператораСвязи
		|ГДЕ
		|	ЗаявлениеАбонентаСпецоператораСвязи.Ссылка.ТипЗаявления = &ТипЗаявления
		|	И ЗаявлениеАбонентаСпецоператораСвязи.Ссылка.Статус = &Статус
		|	И РАЗНОСТЬДАТ(НАЧАЛОПЕРИОДА(ЗаявлениеАбонентаСпецоператораСвязи.Ссылка.Дата, ДЕНЬ), НАЧАЛОПЕРИОДА(&ДатаОтсчета, ДЕНЬ), ДЕНЬ) <= 10
		|	И ЗаявлениеАбонентаСпецоператораСвязи.ИзмененныйРеквизит = &ИзмененныйРеквизит
		|	И ЗаявлениеАбонентаСпецоператораСвязи.Ссылка.УчетнаяЗапись = &УчетнаяЗапись
		|	И ЗаявлениеАбонентаСпецоператораСвязи.Ссылка.УчетнаяЗапись.ЛицензияНаименование = &ЛицензияНаименование
		|	И ЗаявлениеАбонентаСпецоператораСвязи.Ссылка.УчетнаяЗапись.ЛицензияТребуетсяНапоминаниеОбОкончанииСрокаДействия = ИСТИНА
		|	И ЗаявлениеАбонентаСпецоператораСвязи.Ссылка.УчетнаяЗапись.ЛицензияДатаОкончания = &ЛицензияДатаОкончания";

	Запрос.УстановитьПараметр("Статус", Перечисления.СтатусыЗаявленияАбонентаСпецоператораСвязи.Отправлено);
	Запрос.УстановитьПараметр("ТипЗаявления", Перечисления.ТипыЗаявленияАбонентаСпецоператораСвязи.Изменение);
	Запрос.УстановитьПараметр("ДатаОтсчета", ДатаОтсчета);
	Запрос.УстановитьПараметр("ИзмененныйРеквизит", Перечисления.ПараметрыПодключенияК1СОтчетности.ПродлениеЛицензии);
	Запрос.УстановитьПараметр("УчетнаяЗапись", УчетнаяЗапись);
	Запрос.УстановитьПараметр("ЛицензияНаименование", ЛицензияНаименование);
	Запрос.УстановитьПараметр("ЛицензияДатаОкончания", ЛицензияДатаОкончания);

	Результат = Запрос.Выполнить();
 	ЗаявленияАбонентаСпецоператораСвязи = Результат.Выбрать();

	Если ЗаявленияАбонентаСпецоператораСвязи.Количество() = 0 Тогда
		Возврат Ложь;
	Иначе
		Возврат Истина;
	КонецЕсли;
	
КонецФункции

Функция ЗаявлениеАбонентаДляОбновленияРеквизитовБылоСоздано(УчетнаяЗапись, СписокИзменившихсяРеквизитов, ДатаОтсчета)
	
	Если СписокИзменившихсяРеквизитов = Неопределено Тогда
		Возврат Ложь;
	КонецЕсли;
	
	МассивИзменившихсяРеквизитов = Новый Массив;
	Для каждого ИзменившийсяРеквизит Из СписокИзменившихсяРеквизитов Цикл
		МассивИзменившихсяРеквизитов.Добавить(ИзменившийсяРеквизит.Значение);
	КонецЦикла; 
	
	Запрос = Новый Запрос;
	Запрос.Текст = 
		"ВЫБРАТЬ РАЗРЕШЕННЫЕ РАЗЛИЧНЫЕ
		|	КОЛИЧЕСТВО(РАЗЛИЧНЫЕ ЗаявлениеАбонентаСпецоператораСвязи.ИзмененныйРеквизит) КАК ИзмененныйРеквизит
		|ИЗ
		|	(ВЫБРАТЬ
		|		ЗаявлениеАбонентаСпецоператораСвязи.ИзмененныйРеквизит КАК ИзмененныйРеквизит
		|	ИЗ
		|		Документ.ЗаявлениеАбонентаСпецоператораСвязи.ИзменившиесяРеквизитыВторичногоЗаявления КАК ЗаявлениеАбонентаСпецоператораСвязи
		|	ГДЕ
		|		ЗаявлениеАбонентаСпецоператораСвязи.Ссылка.ТипЗаявления = &ТипЗаявления
		|		И ЗаявлениеАбонентаСпецоператораСвязи.Ссылка.Статус = &Статус
		|		И РАЗНОСТЬДАТ(НАЧАЛОПЕРИОДА(ЗаявлениеАбонентаСпецоператораСвязи.Ссылка.Дата, ДЕНЬ), НАЧАЛОПЕРИОДА(&ДатаОтсчета, ДЕНЬ), ДЕНЬ) <= 10
		|		И ЗаявлениеАбонентаСпецоператораСвязи.Ссылка.УчетнаяЗапись = &УчетнаяЗапись
		|		И ЗаявлениеАбонентаСпецоператораСвязи.Ссылка.ПометкаУдаления = ЛОЖЬ) КАК ЗаявлениеАбонентаСпецоператораСвязи
		|ГДЕ
		|	ЗаявлениеАбонентаСпецоператораСвязи.ИзмененныйРеквизит В(&МассивИзменившихсяРеквизитов)
		|
		|ИМЕЮЩИЕ
		|	КОЛИЧЕСТВО(РАЗЛИЧНЫЕ ЗаявлениеАбонентаСпецоператораСвязи.ИзмененныйРеквизит) >= &КоличествоИзменившихсяРеквизитов";

	Запрос.УстановитьПараметр("Статус", Перечисления.СтатусыЗаявленияАбонентаСпецоператораСвязи.Отправлено);
	Запрос.УстановитьПараметр("ТипЗаявления", Перечисления.ТипыЗаявленияАбонентаСпецоператораСвязи.Изменение);
	Запрос.УстановитьПараметр("ДатаОтсчета", ДатаОтсчета);
	Запрос.УстановитьПараметр("МассивИзменившихсяРеквизитов", МассивИзменившихсяРеквизитов);
	Запрос.УстановитьПараметр("КоличествоИзменившихсяРеквизитов", МассивИзменившихсяРеквизитов.Количество());
	Запрос.УстановитьПараметр("УчетнаяЗапись", УчетнаяЗапись);

	Результат = Запрос.Выполнить();
 	ЗаявленияАбонентаСпецоператораСвязи = Результат.Выбрать();

	Если ЗаявленияАбонентаСпецоператораСвязи.Количество() = 0 Тогда
		Возврат Ложь;
	Иначе
		Возврат Истина;
	КонецЕсли;
	
КонецФункции

Процедура ОставитьТолькоИзменившиесяРеквизитыТребующиеНапоминания(ИзменившиесяРеквизитыОрганизаций, ДатаОтсчета, ИгнорироватьДатуПоследнегоОтображения = Ложь) Экспорт

	// Достаем из хранилища общих настроек информацию о предыдущих показах этого предупреждения
	ДатыНапоминанийПоИзменившимсяРеквизитам = ХранилищеОбщихНастроек.Загрузить("ДокументооборотСКонтролирующимиОрганами_ДатыНапоминанийПоИзменившимсяРеквизитам");
	
	// Если приедыдущих показов предупреждений не было, то создаем пустую таблицу 
	Если ДатыНапоминанийПоИзменившимсяРеквизитам = Неопределено ИЛИ ИгнорироватьДатуПоследнегоОтображения Тогда
		
		ДатыНапоминанийПоИзменившимсяРеквизитам = Новый ТаблицаЗначений;
		ДатыНапоминанийПоИзменившимсяРеквизитам.Колонки.Добавить("Организация");
		ДатыНапоминанийПоИзменившимсяРеквизитам.Колонки.Добавить("УчетнаяЗапись");
		ДатыНапоминанийПоИзменившимсяРеквизитам.Колонки.Добавить("СписокИзменившихсяРеквизитов");
		ДатыНапоминанийПоИзменившимсяРеквизитам.Колонки.Добавить("ДатаНапоминания");
		
	КонецЕсли;
	
	ИндексТекущейСтроки = ИзменившиесяРеквизитыОрганизаций.Количество() - 1;
	НачальныйИндексТекущейСтроки = 0;
	
	Пока ИндексТекущейСтроки >= НачальныйИндексТекущейСтроки Цикл
		
		ИзменившиесяРеквизитыОрганизации = ИзменившиесяРеквизитыОрганизаций[ИндексТекущейСтроки];
		ТребуетсяНапоминание = Ложь;
		
		ДатаНапоминанияПоИзмененнымРеквизитам = Неопределено;
		
		ПоддерживаетсяВторичноеЗаявление = ПоддерживаетсяВторичноеЗаявление(ИзменившиесяРеквизитыОрганизации.Организация);
		Если НЕ ПоддерживаетсяВторичноеЗаявление Тогда
			ТребуетсяНапоминание = Ложь;
		ИначеЕсли ЗначениеЗаполнено(ИзменившиесяРеквизитыОрганизации.СписокИзменившихсяРеквизитов) Тогда
			
			// Было ли раньше показано напоминание ?
			Отбор = Новый Структура; 
			Отбор.Вставить("Организация", ИзменившиесяРеквизитыОрганизации.Организация);
			Отбор.Вставить("УчетнаяЗапись", ИзменившиесяРеквизитыОрганизации.УчетнаяЗаписьДокументооборота);
			СтрокиДатыНапоминанияПоИзменившимсяРеквизитам = ДатыНапоминанийПоИзменившимсяРеквизитам.НайтиСтроки(Отбор);
			Если СтрокиДатыНапоминанияПоИзменившимсяРеквизитам.Количество() > 0 Тогда
				СтрокаДатыНапоминанияПоИзменившимсяРеквизитам = СтрокиДатыНапоминанияПоИзменившимсяРеквизитам[0];
				ДатаНапоминанияПоИзмененнымРеквизитам = СтрокаДатыНапоминанияПоИзменившимсяРеквизитам.ДатаНапоминания;
			Иначе
				СтрокаДатыНапоминанияПоИзменившимсяРеквизитам = Неопределено;
			КонецЕсли;
			
			// Проверяем, отправлялось ли заявление для обновления этих реквизитов
			ЗаявлениеАбонентаДляОбновленияРеквизитовБылоСоздано = ЗаявлениеАбонентаДляОбновленияРеквизитовБылоСоздано(
					ИзменившиесяРеквизитыОрганизации.УчетнаяЗаписьДокументооборота, ИзменившиесяРеквизитыОрганизации.СписокИзменившихсяРеквизитов, ДатаОтсчета);
			
			// Определяем, нужно ли показывать предупреждения
			Если ДатаНапоминанияПоИзмененнымРеквизитам = Неопределено Тогда
				// Напоминания раньше не было
				ТребуетсяНапоминание = Истина;
			ИначеЕсли ДатаНапоминанияПоИзмененнымРеквизитам <= ДатаОтсчета И ЗначениеЗаполнено(ДатаНапоминанияПоИзмененнымРеквизитам) Тогда 
				// напоминание не откладывали навседа
				Если НЕ ЗаявлениеАбонентаДляОбновленияРеквизитовБылоСоздано Тогда
					ТребуетсяНапоминание = Истина;
				КонецЕсли;
			Иначе
				// Проверяем, изменился ли состав измененных реквизитов с момента прошлого показа предупреждения
				ИзменилсяСоставРеквизитов = ИзменилсяСоставРеквизитов(ИзменившиесяРеквизитыОрганизации.СписокИзменившихсяРеквизитов, 
						СтрокаДатыНапоминанияПоИзменившимсяРеквизитам.СписокИзменившихсяРеквизитов);
						
				// Если пользователь отложил навсегда показ предупреждения, но состав реквизитов изменился, то 
				// предупреждение показываем все равно.
				Если ИзменилсяСоставРеквизитов И НЕ ЗаявлениеАбонентаДляОбновленияРеквизитовБылоСоздано Тогда
					ТребуетсяНапоминание = Истина;
				КонецЕсли;
				
			КонецЕсли;
			
		КонецЕсли;
		
		Если НЕ ТребуетсяНапоминание Тогда
			// Если напоминание не требуется, то удаляем из таблицы эту строку
			ИзменившиесяРеквизитыОрганизации = Неопределено;
			ИзменившиесяРеквизитыОрганизаций.Удалить(ИндексТекущейСтроки);
		Иначе
			// Заполняем представление на основе списка изменившихся реквизитов
			ИзменившиесяРеквизитыОрганизации.ПредставлениеИзменившихсяРеквизитов = ПредставлениеИзменившихсяРеквизитов(
				ИзменившиесяРеквизитыОрганизации.СписокИзменившихсяРеквизитов);
		КонецЕсли;
		
		ИндексТекущейСтроки = ИндексТекущейСтроки - 1;
		
	КонецЦикла;
	
КонецПроцедуры

Функция ОставитьТолькоИстекающиеЛицензииХранилища(
			ЛицензииУчетныхЗаписейПользователя, 
			ТекДата,
			ИгнорироватьДатуПоследнегоОтображения = Ложь)
			
	ЭтоРежимОграниченнойФункциональности = ЭлектронныйДокументооборотСКонтролирующимиОрганами.ЭтоРежимОграниченнойФункциональности();
	Если ЭтоРежимОграниченнойФункциональности Тогда
		ДатаИстечения = ТекДата + 7 * 24 * 60 * 60; // За 7 дней.
	Иначе
		ДатаИстечения = ТекДата + 60 * 24 * 60 * 60; // За 60 дней.
	КонецЕсли;
	
	ДатыНапоминанийПоИстекающимЛицензиям = ХранилищеОбщихНастроек.Загрузить("ДокументооборотСКонтролирующимиОрганами_ДатыНапоминанийПоИстекающимЛицензиям");
	
	Если ДатыНапоминанийПоИстекающимЛицензиям = Неопределено ИЛИ ИгнорироватьДатуПоследнегоОтображения Тогда
		
		ДатыНапоминанийПоИстекающимЛицензиям = Новый ТаблицаЗначений;
		ДатыНапоминанийПоИстекающимЛицензиям.Колонки.Добавить("Организация");
		ДатыНапоминанийПоИстекающимЛицензиям.Колонки.Добавить("УчетнаяЗапись");
		ДатыНапоминанийПоИстекающимЛицензиям.Колонки.Добавить("ЛицензияНаименование");
		ДатыНапоминанийПоИстекающимЛицензиям.Колонки.Добавить("ЛицензияДатаОкончания");
		ДатыНапоминанийПоИстекающимЛицензиям.Колонки.Добавить("ДатаНапоминания");
		
	КонецЕсли;
	
	ИндексТекущейСтроки = ЛицензииУчетныхЗаписейПользователя.Количество() - 1;
	НачальныйИндексТекущейСтроки = 0;
	
	Пока ИндексТекущейСтроки >= НачальныйИндексТекущейСтроки Цикл
		
		ЛицензияУчетнойЗаписиПользователя = ЛицензииУчетныхЗаписейПользователя[ИндексТекущейСтроки];
		ЕстьПросроченные = Ложь;
		ТребуетсяНапоминание = Ложь;
		
		// Подходит срок окончания лицензии
		ПоддерживаетсяВторичноеЗаявление = ПоддерживаетсяВторичноеЗаявление(ЛицензияУчетнойЗаписиПользователя.Организация);
		Если НЕ ПоддерживаетсяВторичноеЗаявление Тогда
			ТребуетсяНапоминание = Ложь;
		ИначеЕсли РазницаДатВМесяцах(ТекущаяДатаСеанса(), ЛицензияУчетнойЗаписиПользователя.ЛицензияДатаОкончания) > 13 Тогда
			ТребуетсяНапоминание = Ложь;
		ИначеЕсли ЛицензияУчетнойЗаписиПользователя.ЛицензияДатаОкончания <= ДатаИстечения Тогда
			
			ДатаНапоминанияПоИстекшейЛицензии = Неопределено;
			
			// Было ли раньше показано напоминание?
			// Ищем среди лицензий, сохраненных в хранилище по наименованию и окончнию лицензии
			ПараметрыОтбора =  Новый Структура();
			ПараметрыОтбора.Вставить("Организация", ЛицензияУчетнойЗаписиПользователя.Организация);
			ПараметрыОтбора.Вставить("УчетнаяЗапись", ЛицензияУчетнойЗаписиПользователя.УчетнаяЗаписьДокументооборота);
			ПараметрыОтбора.Вставить("ЛицензияНаименование", ЛицензияУчетнойЗаписиПользователя.ЛицензияНаименование);
			ПараметрыОтбора.Вставить("ЛицензияДатаОкончания", ЛицензияУчетнойЗаписиПользователя.ЛицензияДатаОкончания);
			
			СтрокиДатыНапоминанияПоИстекающейЛицензии = ДатыНапоминанийПоИстекающимЛицензиям.НайтиСтроки(ПараметрыОтбора);
				
			Если СтрокиДатыНапоминанияПоИстекающейЛицензии.Количество() > 0 Тогда
				ДатаНапоминанияПоИстекшейЛицензии = СтрокиДатыНапоминанияПоИстекающейЛицензии[0].ДатаНапоминания;
			КонецЕсли;
						
			Если ДатаНапоминанияПоИстекшейЛицензии = Неопределено Тогда
				// Напоминания раньше не было
				ТребуетсяНапоминание = Истина;
			ИначеЕсли ДатаНапоминанияПоИстекшейЛицензии <= ТекДата И ЗначениеЗаполнено(ДатаНапоминанияПоИстекшейЛицензии) Тогда
				// Напоминание было, но срок показа подошел
				УчетнаяЗапись = ЛицензияУчетнойЗаписиПользователя.УчетнаяЗаписьДокументооборота;
				ЛицензияНаименование = ЛицензияУчетнойЗаписиПользователя.ЛицензияНаименование;
				ЛицензияДатаОкончания = ЛицензияУчетнойЗаписиПользователя.ЛицензияДатаОкончания;
				
				ЗаявлениеАбонентаДляПродленияЛицензииБылоСоздано =
					ЗаявлениеАбонентаДляПродленияЛицензииБылоСоздано(УчетнаяЗапись, ЛицензияНаименование, ЛицензияДатаОкончания, ТекДата);

				Если НЕ ЗаявлениеАбонентаДляПродленияЛицензииБылоСоздано Тогда
					ТребуетсяНапоминание = Истина;
				КонецЕсли;
				
			КонецЕсли;
			
		КонецЕсли;
		
		// Если напоминания по лицензии не требуется, то удаляем ее из таблицы
		Если НЕ ТребуетсяНапоминание Тогда
			ЛицензияУчетнойЗаписиПользователя = Неопределено;
			ЛицензииУчетныхЗаписейПользователя.Удалить(ИндексТекущейСтроки);
		КонецЕсли;
		ИндексТекущейСтроки = ИндексТекущейСтроки - 1;
		
	КонецЦикла;
	
	Возврат ЛицензииУчетныхЗаписейПользователя;
	
КонецФункции

Функция ДатыНапоминанийПоИстекающимСертификатам(ИгнорироватьДатуПоследнегоОтображения)

	ДатыНапоминаний = ХранилищеОбщихНастроек.Загрузить("ДокументооборотСКонтролирующимиОрганами_НапоминаниеПоИстекающимСертификатам");
	
	Если ДатыНапоминаний = Неопределено ИЛИ ИгнорироватьДатуПоследнегоОтображения Тогда
		
		ДатыНапоминаний = Новый ТаблицаЗначений;
		ДатыНапоминаний.Колонки.Добавить("Организация");
		ДатыНапоминаний.Колонки.Добавить("Отпечаток");
		ДатыНапоминаний.Колонки.Добавить("ДействителенПо");
		ДатыНапоминаний.Колонки.Добавить("ДатаНапоминания");
		
	КонецЕсли;
	
	Возврат ДатыНапоминаний;
	
КонецФункции

Функция ДатыНапоминанийПоПереизданнымСертификатам(ИгнорироватьДатуПоследнегоОтображения)

	ДатыНапоминаний = ХранилищеОбщихНастроек.Загрузить("ДокументооборотСКонтролирующимиОрганами_НапоминаниеПоСертификатамИмеющимПереизданные");
	
	Если ДатыНапоминаний = Неопределено ИЛИ ИгнорироватьДатуПоследнегоОтображения Тогда
		
		ДатыНапоминаний = Новый ТаблицаЗначений;
		ДатыНапоминаний.Колонки.Добавить("Организация");
		ДатыНапоминаний.Колонки.Добавить("Отпечаток");
		ДатыНапоминаний.Колонки.Добавить("ДатаНапоминания");
		
	КонецЕсли;
	
	Возврат ДатыНапоминаний;
	
КонецФункции

Функция ТребуетсяНапоминаниеПоИстекающемуСертификату(
		ПолныеСвойстваСертификата, 
		КлючевыеСвойстваОрганизации, 
		ТекДата, 
		ДатыНапоминаний, 
		СертификатПросрочен)

	ТребуетсяНапоминание  = Ложь;
	ДатаИстечения         = ТекДата + 60 * 24 * 60 * 60; // За 60 дней.
	Организация           = КлючевыеСвойстваОрганизации.Организация;
	Отпечаток             = ПолныеСвойстваСертификата.Отпечаток;
	ИстечетКДатеИстечения = ПолныеСвойстваСертификата.ДействителенПо <= ДатаИстечения;
	ИстекМенееГодаНазад   = ПолныеСвойстваСертификата.ДействителенПо > ДобавитьМесяц(ТекДата, -12);
	
	Если ИстечетКДатеИстечения И ИстекМенееГодаНазад Тогда
		
		ДатаНапоминания = Неопределено;
		
		// Было ли раньше показано напоминание ?
		Отбор = Новый Структура();
		Отбор.Вставить("Организация", Организация);
		Отбор.Вставить("Отпечаток",   Отпечаток);
		НайденыеСтроки = ДатыНапоминаний.НайтиСтроки(Отбор);
		
		Если НайденыеСтроки.Количество() > 0 Тогда
			ДатаНапоминания = НайденыеСтроки[0].ДатаНапоминания;
		КонецЕсли;
		
		Если ДатаНапоминания = Неопределено Тогда
			// Напоминания раньше не было
			ТребуетсяНапоминание = Истина;
		ИначеЕсли ДатаНапоминания <= ТекДата И ЗначениеЗаполнено(ДатаНапоминания) Тогда
			// Напоминание было, но срок показа подошел
			ЗаявлениеБылоСоздано = ЗаявлениеАбонентаДляЗаменыСертификатаБылоСоздано(Отпечаток, ТекДата);
				
			Если НЕ ЗаявлениеБылоСоздано Тогда
				ТребуетсяНапоминание = Истина;
			КонецЕсли;
		КонецЕсли;
		
	КонецЕсли;
	
	Если ТребуетсяНапоминание Тогда
			
		КлючевыеСвойстваОрганизации.НаименованиеСертификата	 = ПолныеСвойстваСертификата.Наименование;
		КлючевыеСвойстваОрганизации.СертификатДействителенПо = ПолныеСвойстваСертификата.ДействителенПо;
		 
		Если КлючевыеСвойстваОрганизации.СертификатДействителенПо <= ТекДата Тогда
			СертификатПросрочен = Истина;
		КонецЕсли;
		
	КонецЕсли;
	
	Возврат ТребуетсяНапоминание;

КонецФункции

Функция ТребуетсяНапоминаниеПоПереизданномуСертификату(
		ПолныеСвойстваВсехСертификатов,
		ПолныеСвойстваСертификата, 
		КлючевыеСвойстваОрганизации, 
		ТекДата, 
		ДатыНапоминаний)

	ТребуетсяНапоминание = Ложь;
	Организация          = КлючевыеСвойстваОрганизации.Организация;
	Отпечаток            = ПолныеСвойстваСертификата.Отпечаток;
	НовыйОтпечаток       = КлючевыеСвойстваОрганизации.ОтпечатокНовогоСертификата;
	
	ЕстьНовый = ЗначениеЗаполнено(НовыйОтпечаток);
	
	Если ЕстьНовый Тогда
		
		ДатаНапоминания = Неопределено;
		
		// Было ли раньше показано напоминание ?
		Отбор = Новый Структура();
		Отбор.Вставить("Организация", Организация);
		Отбор.Вставить("Отпечаток",   Отпечаток);
		НайденыеСтроки = ДатыНапоминаний.НайтиСтроки(Отбор);
		
		Если НайденыеСтроки.Количество() > 0 Тогда
			ДатаНапоминания = НайденыеСтроки[0].ДатаНапоминания;
		КонецЕсли;
		
		Если ДатаНапоминания = Неопределено Тогда
			// Напоминания раньше не было
			ТребуетсяНапоминание = Истина;
		ИначеЕсли ДатаНапоминания <= ТекДата И ЗначениеЗаполнено(ДатаНапоминания) Тогда
			// Напоминание было, но срок показа подошел
			ЗаявлениеБылоСоздано = ЗаявлениеАбонентаДляЗаменыСертификатаБылоСоздано(Отпечаток, ТекДата);
				
			Если НЕ ЗаявлениеБылоСоздано Тогда
				ТребуетсяНапоминание = Истина;
			КонецЕсли;
		КонецЕсли;
		
	КонецЕсли;
	
	Если ТребуетсяНапоминание Тогда
		
		ПолныеСвойстваНовогоСертификата = ПолныеСвойстваВсехСертификатов.Получить(КлючевыеСвойстваОрганизации.ОтпечатокНовогоСертификата);
		
		КлючевыеСвойстваОрганизации.НаименованиеСертификата	= ПолныеСвойстваСертификата.Наименование;
		Если ПолныеСвойстваНовогоСертификата <> Неопределено Тогда
			КлючевыеСвойстваОрганизации.НовыйСертификат = ПолныеСвойстваНовогоСертификата;
		КонецЕсли;
	Иначе
		КлючевыеСвойстваОрганизации.ОтпечатокНовогоСертификата    = Неопределено;
		КлючевыеСвойстваОрганизации.НовыйСертификатДействителенС  = Неопределено;
		КлючевыеСвойстваОрганизации.НовыйСертификатДействителенПо = Неопределено;
	КонецЕсли;
	
	Возврат ТребуетсяНапоминание;

КонецФункции

Функция ОставитьТолькоСертификатыТребующиеВнимания(
			КлючевыеДанныеОрганизацийТаблица, 
			ПолныеСвойстваВсехСертификатов, 
			ТекДата,
			ИгнорироватьДатуПоследнегоОтображения = Ложь)

	ДатаИстечения = ТекДата + 60 * 24 * 60 * 60; // За 60 дней.
	
	ДатыНапоминаний1 = ДатыНапоминанийПоИстекающимСертификатам(ИгнорироватьДатуПоследнегоОтображения);
	ДатыНапоминаний2 = ДатыНапоминанийПоПереизданнымСертификатам(ИгнорироватьДатуПоследнегоОтображения);
	
	КоличествоСтрок     = КлючевыеДанныеОрганизацийТаблица.Количество() - 1;
	ИндексТекущейСтроки = 0;
	
	Пока КоличествоСтрок >= ИндексТекущейСтроки Цикл
		
		СертификатПросрочен   = Ложь;
		ТребуетсяНапоминание1 = Ложь;
		ТребуетсяНапоминание2 = Ложь;

		Если ПолныеСвойстваВсехСертификатов <> Неопределено Тогда
			
			КлючевыеСвойстваОрганизации = КлючевыеДанныеОрганизацийТаблица[КоличествоСтрок]; // Двигаемся с конца в начало, чтобы можно было удалять строки
			ПолныеСвойстваСертификата   = ПолныеСвойстваВсехСертификатов.Получить(КлючевыеСвойстваОрганизации.ОтпечатокСертификата);
			
			Если ПолныеСвойстваСертификата <> Неопределено Тогда
				
				ТребуетсяНапоминание1 = ТребуетсяНапоминаниеПоИстекающемуСертификату(
					ПолныеСвойстваСертификата, 
					КлючевыеСвойстваОрганизации, 
					ТекДата, 
					ДатыНапоминаний1,
					СертификатПросрочен);
					
				ТребуетсяНапоминание2 = ТребуетсяНапоминаниеПоПереизданномуСертификату(
					ПолныеСвойстваВсехСертификатов,
					ПолныеСвойстваСертификата, 
					КлючевыеСвойстваОрганизации, 
					ТекДата, 
					ДатыНапоминаний2);
					
				КонецЕсли;
				
		КонецЕсли;
		
		Если СертификатПросрочен Тогда
			// Сертификаты, которые вот-вот истекут перемещаем на самый верх
			Если КоличествоСтрок > 0 Тогда
				// Сдвинуть(<Строка>, <Смещение>) 
				// Смещение - Количество строк, на которое необходимо переместить строку. 
				// Положительное значение означает, что строка будет передвинута ближе к концу таблицы значений (вниз), 
				// отрицательное - ближе к началу (вверх).
				// Смещение = - КоличествоСтрок - это сдвиг в самое начало.
				КлючевыеДанныеОрганизацийТаблица.Сдвинуть(КоличествоСтрок, - КоличествоСтрок);
			КонецЕсли;
			ИндексТекущейСтроки = ИндексТекущейСтроки + 1;
		Иначе
			Если НЕ ТребуетсяНапоминание1 И НЕ ТребуетсяНапоминание2 Тогда
				СертификатУчетнойЗаписиПользователя = Неопределено;
				КлючевыеДанныеОрганизацийТаблица.Удалить(КоличествоСтрок);
			КонецЕсли;
			КоличествоСтрок = КоличествоСтрок - 1;
		КонецЕсли;
		
	КонецЦикла;
	
	Возврат КлючевыеДанныеОрганизацийТаблица;
	
КонецФункции

Функция СформироватьТекстПредупрежденияОбИзменившихсяПараметрахПодключения(ИзменившиесяПараметрыПодключения) Экспорт
	
	ТекстМакета = ПолучитьМакет("ПредупреждениеОбИстеченииСпискаСертификатов").ПолучитьТекст();
	
	НачалоИнформацииОбОрганизации 	= СтрНайти(ТекстМакета, "<!--НачалоБлока-->");
	КонецИнформацииОбОрганизации 	= СтрНайти(ТекстМакета, "<!--КонецБлока-->");
	БлокHTMLТаблицы 				= Сред(ТекстМакета, НачалоИнформацииОбОрганизации, КонецИнформацииОбОрганизации - НачалоИнформацииОбОрганизации);
	
	НачалоКнопки 		= СтрНайти(БлокHTMLТаблицы, "<!--НачалоКартинки-->");
	КонецКнопки 		= СтрНайти(БлокHTMLТаблицы, "<!--КонецКартинки-->");
	КодКартинкиКонопки 	= Сред(БлокHTMLТаблицы, НачалоКнопки, КонецКнопки - НачалоКнопки);
	
	НачалоНадписиБезбумажноеПродление = СтрНайти(БлокHTMLТаблицы, "<!--НачалоНадписьБезбумажноеПродление-->");
	КонецНадписиБезбумажноеПродление  = СтрНайти(БлокHTMLТаблицы, "<!--КонецНадписьБезбумажноеПродление-->");
	КодНадписиБезбумажноеПродление    = Сред(БлокHTMLТаблицы, НачалоНадписиБезбумажноеПродление, КонецНадписиБезбумажноеПродление - НачалоНадписиБезбумажноеПродление);
	
	ЧтоИзменилось = ЧтоИзменилосьВПараметрахПодключения(ИзменившиесяПараметрыПодключения);
	
	// Устанавливаем заголовок предупреждения
	ПолныйHTMLТекстПредупреждения 	= ТекстМакета;
	ЗаголовокПредупреждения 		= ЗаголовокПредупреждения(ИзменившиесяПараметрыПодключения, ЧтоИзменилось);
	ПолныйHTMLТекстПредупреждения 	= СтрЗаменить(ПолныйHTMLТекстПредупреждения, "#Заголовок#", ЗаголовокПредупреждения);
	
	// Устанавливаем текст предупреждения
	ТекстПредупреждения 			= ТекстПредупреждения(ИзменившиесяПараметрыПодключения, ЧтоИзменилось);
	ПолныйHTMLТекстПредупреждения 	= СтрЗаменить(ПолныйHTMLТекстПредупреждения, "#ТекстПредупреждения#", ТекстПредупреждения);
	
	КоличествоОрганизаций = ИзменившиесяПараметрыПодключения.Количество();
	
	// Формируем строки HTML таблицы
	HTMLКодТаблицы = "";
	Для НомерСтроки = 1 По КоличествоОрганизаций Цикл
		
		ИзменившиесяПараметрыПодключенияПоОднойУчетнойЗаписи = ИзменившиесяПараметрыПодключения[НомерСтроки - 1];
		НоваяСтрокаHTMLТаблицы 		= ТекстБлока(ИзменившиесяПараметрыПодключенияПоОднойУчетнойЗаписи, БлокHTMLТаблицы);
		
		// Организация
		ИнформацияОбОрганизацииHTML = "<A style=""color: black"" href=""&amp;ПросмотрОрганизации" + Формат(НомерСтроки, "ЧН=0; ЧГ=") + "КонецСсылки"">" 
			+ ТекстВHTML(ИзменившиесяПараметрыПодключенияПоОднойУчетнойЗаписи.Организация.Наименование) + "</A>";
		НоваяСтрокаHTMLТаблицы		= СтрЗаменить(НоваяСтрокаHTMLТаблицы, "#ИнформацияОбОрганизации#", ИнформацияОбОрганизацииHTML);
			
		// Сертификат
		ИнформацияПоСертификату		= ИнформацияПоСертификату(ИзменившиесяПараметрыПодключенияПоОднойУчетнойЗаписи, НомерСтроки);
		НоваяСтрокаHTMLТаблицы		= СтрЗаменить(НоваяСтрокаHTMLТаблицы, "#Сертификат#", ИнформацияПоСертификату);
				
		// Лицензия
		ИнформацияПоЛицензии		= ИнформацияПоЛицензии(ИзменившиесяПараметрыПодключенияПоОднойУчетнойЗаписи);
		НоваяСтрокаHTMLТаблицы		= СтрЗаменить(НоваяСтрокаHTMLТаблицы, "#Лицензия#", ИнформацияПоЛицензии);
			
		// Изменившиеся реквизиты
		ИнформацияПоРеквизитам		= ИнформацияПоРеквизитам(ИзменившиесяПараметрыПодключенияПоОднойУчетнойЗаписи);
		НоваяСтрокаHTMLТаблицы		= СтрЗаменить(НоваяСтрокаHTMLТаблицы, "#Реквизиты#", ИнформацияПоРеквизитам);
		
		// Кнопка "Отправить заявление"
		Результат                   = КнопкаОтправкиЗаявление(ИзменившиесяПараметрыПодключенияПоОднойУчетнойЗаписи, НоваяСтрокаHTMLТаблицы, НомерСтроки);
		НоваяСтрокаHTMLТаблицы 		= СтрЗаменить(НоваяСтрокаHTMLТаблицы, КодКартинкиКонопки, Результат.Кнопка);
		НоваяСтрокаHTMLТаблицы 		= СтрЗаменить(НоваяСтрокаHTMLТаблицы, КодНадписиБезбумажноеПродление, Результат.Надпись);
		
		// Добавляем строку в HTML-документ
		HTMLКодТаблицы = HTMLКодТаблицы + НоваяСтрокаHTMLТаблицы;
		
	КонецЦикла;
	
	// Подставляем в макет сформированную таблицу
	ПолныйHTMLТекстПредупреждения = СтрЗаменить(ПолныйHTMLТекстПредупреждения, БлокHTMLТаблицы, HTMLКодТаблицы);
	ПолныйHTMLТекстПредупреждения = УбратьФорматированиеДляFirefox(ПолныйHTMLТекстПредупреждения);
	
	Возврат ПолныйHTMLТекстПредупреждения;
	
КонецФункции

Функция ИспользуетсяFirefox() Экспорт
	
	ИспользуетсяFirefox = Ложь;
	
	СисИнфо = Новый СистемнаяИнформация;
	ВебБраузер = СисИнфо.ИнформацияПрограммыПросмотра;
	
	ИспользуетсяFirefox = (СтрЧислоВхождений(ВРег(ВебБраузер), "FIREFOX") > 0);
	
	Возврат ИспользуетсяFirefox;
	
КонецФункции

Функция УбратьФорматированиеДляFirefox(Текст) Экспорт
	
	Результат = Текст;
	Если ИспользуетсяFirefox() Тогда
		
		Результат = СтрЗаменить(Результат, "class=leftline", "");
		Результат = СтрЗаменить(Результат, "border=""1""", "border=""0""");
		
	КонецЕсли;
	
	Возврат Результат;
	
КонецФункции

Функция НапомнитьОбновитьКриптопровайдер(
		СертификатыПользователяМассив,
		ПолныеСвойстваВсехСертификатов,
		ТекДата,
		ИсключаемыеУчетныеЗаписи,
		ИгнорироватьДатуПоследнегоОтображения = Ложь)
	
	Если НЕ ИгнорироватьДатуПоследнегоОтображения Тогда
		ДатаНапоминанияОбОбновленииКриптопровайдера = ХранилищеОбщихНастроек.Загрузить(
			"ДокументооборотСКонтролирующимиОрганами_ДатаНапоминанияОбОбновленииКриптопровайдера");
		Если ДатаНапоминанияОбОбновленииКриптопровайдера <> Неопределено
			И ДатаНапоминанияОбОбновленииКриптопровайдера > ТекДата Тогда
			Возврат Ложь;
		КонецЕсли;
	КонецЕсли;
	
	Если ПолныеСвойстваВсехСертификатов <> Неопределено Тогда
		Для каждого СертификатУчетнойЗаписиПользователя Из СертификатыПользователяМассив Цикл
			Если ИсключаемыеУчетныеЗаписи.Найти(СертификатУчетнойЗаписиПользователя.УчетнаяЗаписьДокументооборота) = Неопределено Тогда
				СвойстваСертификата = ПолныеСвойстваВсехСертификатов[СертификатУчетнойЗаписиПользователя.ОтпечатокСертификата];
				Если СвойстваСертификата <> Неопределено И СвойстваСертификата.Хранилище.ЭтоЛокальноеХранилище
					И СвойстваСертификата.ДействителенПо > ТекДата Тогда
					Возврат Истина;
				КонецЕсли;
			КонецЕсли;
		КонецЦикла;
	КонецЕсли;
	
	Возврат Ложь;
	
КонецФункции

Функция ТекстБлока(ИзменившиесяПараметры, Знач ТекстДляЗамены)
		
	НачалоЯчейкиОрганизация = СтрНайти(ТекстДляЗамены,"<!--НачалоЯчейкиОрганизация-->");
	КонецЯчейкиОрганизация 	= СтрНайти(ТекстДляЗамены,"<!--КонецЯчейкиОрганизация-->");
	ЯчейкаОрганизация 		= Сред(ТекстДляЗамены, НачалоЯчейкиОрганизация, КонецЯчейкиОрганизация - НачалоЯчейкиОрганизация);
	
	НачалоЯчейкиСертификат 	= СтрНайти(ТекстДляЗамены,"<!--НачалоЯчейкиСертификат-->");
	КонецЯчейкиСертификат 	= СтрНайти(ТекстДляЗамены,"<!--КонецЯчейкиСертификат-->");
	ЯчейкаСертификат 		= Сред(ТекстДляЗамены, НачалоЯчейкиСертификат, КонецЯчейкиСертификат - НачалоЯчейкиСертификат);
	
	НачалоЯчейкиЗаявление 	= СтрНайти(ТекстДляЗамены,"<!--НачалоЯчейкиЗаявление-->");
	КонецЯчейкиЗаявление 	= СтрНайти(ТекстДляЗамены,"<!--КонецЯчейкиЗаявление-->");
	ЯчейкаЗаявление 		= Сред(ТекстДляЗамены, НачалоЯчейкиЗаявление, КонецЯчейкиЗаявление - НачалоЯчейкиЗаявление);
	
	НачалоЯчейкиЛицензия 	= СтрНайти(ТекстДляЗамены,"<!--НачалоЯчейкиЛицензия-->");
	КонецЯчейкиЛицензия 	= СтрНайти(ТекстДляЗамены,"<!--КонецЯчейкиЛицензия-->");
	ЯчейкаЛицензия 			= Сред(ТекстДляЗамены, НачалоЯчейкиЛицензия, КонецЯчейкиЛицензия - НачалоЯчейкиЛицензия);
	
	НачалоЯчейкиНастроки 	= СтрНайти(ТекстДляЗамены,"<!--НачалоЯчейкиРеквизиты-->");
	КонецЯчейкиНастроки 	= СтрНайти(ТекстДляЗамены,"<!--КонецЯчейкиРеквизиты-->");
	ЯчейкиНастроки			= Сред(ТекстДляЗамены, НачалоЯчейкиНастроки, КонецЯчейкиНастроки - НачалоЯчейкиНастроки);
	
	НачалоВысотыБлока 		= СтрНайти(ТекстДляЗамены,"<!--НачалоВысотыБлока-->");
	КонецВысотыБлока 		= СтрНайти(ТекстДляЗамены,"<!--КонецВысотыБлока-->");
	ТэгВысотыБлока 			= Сред(ТекстДляЗамены, НачалоВысотыБлока, КонецВысотыБлока - НачалоВысотыБлока);
	
	НачалоЯчейкиОтступ 		= СтрНайти(ТекстДляЗамены,"<!--НачалоЯчейкиОтступ-->");
	КонецЯчейкиОтступ 		= СтрНайти(ТекстДляЗамены,"<!--КонецЯчейкиОтступ-->");
	ЯчейкиОтступ 			= Сред(ТекстДляЗамены, НачалоЯчейкиОтступ, КонецЯчейкиОтступ - НачалоЯчейкиОтступ);
	
	НачалоВысотыБлокаЗаявления 	= СтрНайти(ТекстДляЗамены,"<!--НачалоВысотыБлокаЗаявления-->");
	КонецВысотыБлокаЗаявления 	= СтрНайти(ТекстДляЗамены,"<!--КонецВысотыБлокаЗаявления-->");
	ТэгВысотыБлокаЗаявления 	= Сред(ТекстДляЗамены, НачалоВысотыБлокаЗаявления, КонецВысотыБлокаЗаявления - НачалоВысотыБлокаЗаявления);	
	
	// В зависимости от количества строк в блоке, оставляем один блок
	ЕстьСертификат = 
		ЗначениеЗаполнено(ИзменившиесяПараметры.СертификатДействителенПо) И ЗначениеЗаполнено(ИзменившиесяПараметры.НаименованиеСертификата)
		ИЛИ ЗначениеЗаполнено(ИзменившиесяПараметры.ОтпечатокНовогоСертификата);
	
	ЕстьЛицензия = ЗначениеЗаполнено(ИзменившиесяПараметры.ЛицензияНаименование) 
		И ЗначениеЗаполнено(ИзменившиесяПараметры.ЛицензияДатаОкончания);
	
	ЕстьРеквизиты = ЗначениеЗаполнено(ИзменившиесяПараметры.ПредставлениеИзменившихсяРеквизитов);
	
	ВысотаБлока = 0;
	Если ЕстьСертификат И ЕстьЛицензия И ЕстьРеквизиты Тогда
		ВысотаБлока = 3;
	ИначеЕсли ЕстьСертификат И ЕстьЛицензия И НЕ ЕстьРеквизиты 
		ИЛИ ЕстьСертификат И НЕ ЕстьЛицензия И ЕстьРеквизиты 
		ИЛИ НЕ ЕстьСертификат И ЕстьЛицензия И ЕстьРеквизиты Тогда
		ВысотаБлока = 2;
	Иначе 
		ВысотаБлока = 1;
	КонецЕсли;
	
	// Собираем текст блока
	СертификатУжеБыл = Ложь;
	ЛицензияУжеБыла = Ложь;
	РеквизитыУжеБыли = Ложь;
	
	БлокHTMLТаблицы = "";
	// Первая строка
	БлокHTMLТаблицы = БлокHTMLТаблицы + "<TR>" + СтрЗаменить(ЯчейкаОрганизация, ТэгВысотыБлока, "<TD class=rightline rowSpan=" + Строка(ВысотаБлока) + ">" ) + Символы.ПС;
	Если ЕстьСертификат Тогда
		БлокHTMLТаблицы = БлокHTMLТаблицы + ЯчейкаСертификат + Символы.ПС;
		СертификатУжеБыл = Истина;
	ИначеЕсли ЕстьЛицензия Тогда
		БлокHTMLТаблицы = БлокHTMLТаблицы + ЯчейкаЛицензия + Символы.ПС;
		ЛицензияУжеБыла = Истина; 
	ИначеЕсли ЕстьРеквизиты Тогда
		БлокHTMLТаблицы = БлокHTMLТаблицы + ЯчейкиНастроки + Символы.ПС;
		РеквизитыУжеБыли = Истина;
	КонецЕсли;
	БлокHTMLТаблицы = БлокHTMLТаблицы + СтрЗаменить(ЯчейкаЗаявление, ТэгВысотыБлокаЗаявления, "<TD class=leftline align=center rowSpan=" + Строка(ВысотаБлока) + ">" ) + "</TR>" + Символы.ПС;
	// Вторая строка
	Если ЕстьЛицензия И НЕ ЛицензияУжеБыла ИЛИ ЕстьРеквизиты И НЕ РеквизитыУжеБыли Тогда
		БлокHTMLТаблицы = БлокHTMLТаблицы + "<TR>";
	КонецЕсли;
	Если ЕстьЛицензия И НЕ ЛицензияУжеБыла Тогда
		БлокHTMLТаблицы = БлокHTMLТаблицы + ЯчейкаЛицензия + Символы.ПС;
		ЛицензияУжеБыла = Истина; 
	ИначеЕсли ЕстьРеквизиты И НЕ РеквизитыУжеБыли Тогда
		БлокHTMLТаблицы = БлокHTMLТаблицы + ЯчейкиНастроки + Символы.ПС;
		РеквизитыУжеБыли = Истина;
	КонецЕсли;
	Если ЕстьЛицензия И НЕ ЛицензияУжеБыла ИЛИ ЕстьРеквизиты И НЕ РеквизитыУжеБыли Тогда
		БлокHTMLТаблицы = БлокHTMLТаблицы + "</TR>";
	КонецЕсли;
	// Третья строка
	Если ЕстьРеквизиты И НЕ РеквизитыУжеБыли Тогда
		БлокHTMLТаблицы = БлокHTMLТаблицы + "<TR>" + ЯчейкиНастроки + "</TR>";
		РеквизитыУжеБыли = Истина;
	КонецЕсли;
	
	БлокHTMLТаблицы = БлокHTMLТаблицы + ЯчейкиОтступ;
	
	Возврат БлокHTMLТаблицы;
	
КонецФункции

Функция ИнформацияПоЛицензии(ИзменившиесяПараметры)

	ИнформацияПоЛицензии = "";
	
	ДвеНедели = 2 * 7 * 24 * 60 * 60;
	ТекДата = ТекущаяДатаСеанса();

	ДатаОкончанияЛицензии = ИзменившиесяПараметры.ЛицензияДатаОкончания;
	
	Если ЗначениеЗаполнено(ДатаОкончанияЛицензии) Тогда
		
		// Прибавляем один день, так как в день окончания лицензия еще действует.
		ДатаОкончанияЛицензии = ДатаОкончанияЛицензии + 24*60*60;
		
		ЛицензияНаименование = """" + ТекстВHTML(ИзменившиесяПараметры.ЛицензияНаименование) + """";
		
		СрокИстеченияЛицензии 			= ТекстЧерезСколькоЛетМесяцевНедельДней(ТекДата, ДатаОкончанияЛицензии, "<nobr>", "</nobr>");
		СрокИстеченияЛицензииВФормате 	= Формат(ДатаОкончанияЛицензии, "ДФ=""дд ММММ гггг 'г.'""");

		Если СрокИстеченияЛицензии = Неопределено Тогда
			// Истекшие лицензии выводим красным
			СрокИстеченияЛицензии	= НСтр("ru='. Истекла '") + ТекстБезПереносов(СрокИстеченияЛицензииВФормате);
			ИнформацияПоЛицензии	= "<li style=""color: b22222"">" + НСтр("ru = 'Лицензия '") + ЛицензияНаименование + " "+ СрокИстеченияЛицензии;
			
		Иначе
			Если НачалоДня(ДатаОкончанияЛицензии) - НачалоДня(ТекДата) < ДвеНедели Тогда
				// Лицензии, срок действия которых заканчивается менее чем через 2 недели
				СрокИстеченияЛицензии	= НСтр("ru='. Истекает через '") + СрокИстеченияЛицензии+ " (" + СрокИстеченияЛицензииВФормате+ ")";
				ИнформацияПоЛицензии	= "<li style=""color: b22222"">" + НСтр("ru = 'Лицензия '") + ЛицензияНаименование + " "+ СрокИстеченияЛицензии;
			Иначе
				// Лицензии, срок действия которых заканчивается более чем через 2 недели
				СрокИстеченияЛицензии	= НСтр("ru='. Истекает через '")  + СрокИстеченияЛицензии+ " (" + СрокИстеченияЛицензииВФормате+ ")";
				ИнформацияПоЛицензии	= "<li style=""color: black"">" + НСтр("ru = 'Лицензия '") + ЛицензияНаименование + " "+ СрокИстеченияЛицензии;
			КонецЕсли;
		КонецЕсли;
		
	КонецЕсли;
	
	Возврат ИнформацияПоЛицензии;
	
КонецФункции

Функция ИнформацияПоРеквизитам(ИзменившиесяПараметры)

	ИнформацияПоРеквизитам = "";
	
	ЕстьИзменившиесяРеквизиты = ЗначениеЗаполнено(ИзменившиесяПараметры.ПредставлениеИзменившихсяРеквизитов);
	Если ЕстьИзменившиесяРеквизиты Тогда
		
		ИнформацияПоРеквизитам 	= "<li>" + НСтр("ru = 'Изменились реквизиты подключения - '") 
			+ ИзменившиесяПараметры.ПредставлениеИзменившихсяРеквизитов;
		
	КонецЕсли;
	
	Возврат ИнформацияПоРеквизитам;
	
КонецФункции

Функция ИнформацияПоСертификату(ИзменившиесяПараметры, НомерСтроки)
	
	ДвеНедели = 2 * 7 * 24 * 60 * 60;
	ТекДата   = ТекущаяДатаСеанса();
	
	ИнформацияПоСертификату  = "";
	СертификатДействителенПо = ИзменившиесяПараметры.СертификатДействителенПо;
	НаименованиеСертификата  = ТекстВHTML(ИзменившиесяПараметры.НаименованиеСертификата);
	ГиперссылкаСертификата 	 = "<A style=""color: #color#"" href=""&amp;ПросмотрСертификата" + Формат(НомерСтроки, "ЧН=0; ЧГ=") + "КонецСсылки"">" + НаименованиеСертификата + "</A>";
	
	Если ЗначениеЗаполнено(СертификатДействителенПо) Тогда
		
		СрокИстеченияСертификата 		 = ТекстЧерезСколькоЛетМесяцевНедельДней(ТекДата, СертификатДействителенПо, "<nobr>", "</nobr>");
		СрокИстеченияСертификатаВФормате = Формат(СертификатДействителенПо, "дд ММММ гггг ЧЧ:мм:сс 'г.'");

		Если СрокИстеченияСертификата = Неопределено Тогда
			// Истекшие сертификаты
			СрокИстеченияСертификата = НСтр("ru='истек '") + ТекстБезПереносов(СрокИстеченияСертификатаВФормате);
			Цвет = "b22222";

		Иначе
			СрокИстеченияСертификата = НСтр("ru='истекает через '") + СрокИстеченияСертификата + " (" + СрокИстеченияСертификатаВФормате+ ")";
			Если НачалоДня(СертификатДействителенПо) - НачалоДня(ТекДата) < ДвеНедели Тогда
				Цвет = "b22222";
			Иначе
				Цвет = "black";
			КонецЕсли;
		КонецЕсли;
		
		Если ЗначениеЗаполнено(ИзменившиесяПараметры.ОтпечатокНовогоСертификата) Тогда
			ТекстПроПереизданныйСертификат = НСтр("ru = '. %1 этот сертификат был продлен в другой программе и действует до %2. Вы можете перейти на использование нового сертификата, выбрав его в настройках заявления.'");
			ТекстПроПереизданныйСертификат = СтрШаблон(ТекстПроПереизданныйСертификат, ИзменившиесяПараметры.НовыйСертификатДействителенС, ИзменившиесяПараметры.НовыйСертификатДействителенПо);
		Иначе
			ТекстПроПереизданныйСертификат = НСтр("ru = ''");
		КонецЕсли;
		
		ЗаголовокСертификата    = НСтр("ru = 'Срок действия сертификата '");
		ИнформацияПоСертификату = "<li style=""color: #color#"">" + ЗаголовокСертификата + " " + ГиперссылкаСертификата + " " + СрокИстеченияСертификата + ТекстПроПереизданныйСертификат;
		ИнформацияПоСертификату	= СтрЗаменить(ИнформацияПоСертификату, "#color#", Цвет);
		
	Иначе
		
		Если ЗначениеЗаполнено(ИзменившиесяПараметры.ОтпечатокНовогоСертификата) Тогда
			
			Цвет = "black";
			
			ТекстПроПереизданныйСертификат = НСтр("ru = '%1 сертификат %2 был продлен в другой программе и действует до %3. Вы можете перейти на использование нового сертификата, выбрав его в настройках заявления.'");
			
			ТекстПроПереизданныйСертификат = СтрШаблон(
				ТекстПроПереизданныйСертификат,
				ИзменившиесяПараметры.НовыйСертификатДействителенС,
				ГиперссылкаСертификата,
				ИзменившиесяПараметры.НовыйСертификатДействителенПо);
				
			ИнформацияПоСертификату = "<li style=""color: #color#"">" + ТекстПроПереизданныйСертификат;
			ИнформацияПоСертификату	= СтрЗаменить(ИнформацияПоСертификату, "#color#", Цвет);
			
		КонецЕсли;

	КонецЕсли;
	
	Возврат ИнформацияПоСертификату;

КонецФункции

Функция ЧтоИзменилосьВПараметрахПодключения(ИзменившиесяПараметрыПодключения)

	// Определяем, о чем нужно предупреждать
	ЕстьЗакончившиесяЛицензии 		= Ложь;
	ЕстьЗакончившиесяСертификаты 	= Ложь;
	ЕстьПереизданныеСертификаты		= Ложь;
	ЕстьИзменившиесяРеквизиты 		= Ложь;
	ЕстьПоддержкаВторичного			= Ложь;
	
	Для каждого ИзменившиесяПараметрыОрганизации Из ИзменившиесяПараметрыПодключения Цикл
		
		Если ИзменившиесяПараметрыОрганизации.ЛицензияНаименование <> Null
			И ЗначениеЗаполнено(ИзменившиесяПараметрыОрганизации.ЛицензияНаименование) Тогда
			ЕстьЗакончившиесяЛицензии = Истина;
		КонецЕсли;
		
		Если ИзменившиесяПараметрыОрганизации.ОтпечатокСертификата <> Null 
			И ЗначениеЗаполнено(ИзменившиесяПараметрыОрганизации.ОтпечатокСертификата)
			И ЗначениеЗаполнено(ИзменившиесяПараметрыОрганизации.СертификатДействителенПо) Тогда
			ЕстьЗакончившиесяСертификаты = Истина;
		КонецЕсли;
		
		Если ЗначениеЗаполнено(ИзменившиесяПараметрыОрганизации.ОтпечатокНовогоСертификата) Тогда
			ЕстьПереизданныеСертификаты = Истина;
		КонецЕсли;
		
		Если ИзменившиесяПараметрыОрганизации.СписокИзменившихсяРеквизитов <> Null
			И ЗначениеЗаполнено(ИзменившиесяПараметрыОрганизации.СписокИзменившихсяРеквизитов) Тогда
			ЕстьИзменившиесяРеквизиты = Истина;
		КонецЕсли;
		
		Если ИзменившиесяПараметрыОрганизации.ПоддерживаетсяОтправкаВторичногоЗаявления = Истина Тогда
			ЕстьПоддержкаВторичного = Истина;
		КонецЕсли;
		
	КонецЦикла;
	
	ДополнительныеПараметры = Новый Структура();
	ДополнительныеПараметры.Вставить("ЕстьЗакончившиесяЛицензии",    ЕстьЗакончившиесяЛицензии);
	ДополнительныеПараметры.Вставить("ЕстьЗакончившиесяСертификаты", ЕстьЗакончившиесяСертификаты);
	ДополнительныеПараметры.Вставить("ЕстьПереизданныеСертификаты",  ЕстьПереизданныеСертификаты);
	ДополнительныеПараметры.Вставить("ЕстьИзменившиесяРеквизиты",    ЕстьИзменившиесяРеквизиты);
	ДополнительныеПараметры.Вставить("ЕстьПоддержкаВторичного",      ЕстьПоддержкаВторичного);
	
	Возврат ДополнительныеПараметры;

КонецФункции

Функция ЗаголовокПредупреждения(ИзменившиесяПараметрыПодключения, ЧтоИзменилось)

	// Формируем заголовок в зависимости от того, о чем нужно предупреждать
	Если ЧтоИзменилось.ЕстьЗакончившиесяЛицензии И НЕ ЧтоИзменилось.ЕстьЗакончившиесяСертификаты И НЕ ЧтоИзменилось.ЕстьИзменившиесяРеквизиты Тогда
		Результат = НСтр("ru = 'Истекает срок действия лицензии на 1С-Отчетность'");
	ИначеЕсли НЕ ЧтоИзменилось.ЕстьЗакончившиесяЛицензии И ЧтоИзменилось.ЕстьЗакончившиесяСертификаты И НЕ ЧтоИзменилось.ЕстьИзменившиесяРеквизиты Тогда
		Результат = НСтр("ru = 'Истекает срок действия сертификата'");
	ИначеЕсли ЧтоИзменилось.ЕстьЗакончившиесяЛицензии ИЛИ ЧтоИзменилось.ЕстьЗакончившиесяСертификаты Тогда
		Результат = НСтр("ru = 'Истекает срок действия лицензии (сертификата) на 1С-Отчетность'");
	ИначеЕсли ЧтоИзменилось.ЕстьПереизданныеСертификаты Тогда
		Результат = НСтр("ru = 'Изменился сертификат 1С-Отчетности'");
	Иначе
		Результат = НСтр("ru = 'Изменились настройки подключения к 1С-Отчетности'");
	КонецЕсли;
	
	// Если ни одна из организаций не поддерживает вторичные заявления,
	// то убираем понятие 1С-Отчетность из предупреждения
	Если НЕ ЧтоИзменилось.ЕстьПоддержкаВторичного Тогда
		Результат = СтрЗаменить(Результат, НСтр("ru = ' к 1С-Отчетности'"), "");
		Результат = СтрЗаменить(Результат, НСтр("ru = ' на 1С-Отчетность'"), "");
	КонецЕсли;
	
	Возврат Результат;

КонецФункции

Функция ТекстПредупреждения(ИзменившиесяПараметрыПодключения, ЧтоИзменилось)

	ЕстьЗакончившиесяЛицензии 		= Ложь;
	ЕстьЗакончившиесяСертификаты 	= Ложь;
	ЕстьИзменившиесяРеквизиты 		= Ложь;

	Если ЧтоИзменилось.ЕстьЗакончившиесяЛицензии ИЛИ ЧтоИзменилось.ЕстьЗакончившиесяСертификаты ИЛИ ЧтоИзменилось.ЕстьИзменившиесяРеквизиты Тогда
		
		КоличествоОрганизаций = ИзменившиесяПараметрыПодключения.Количество();
		ПоддерживаютВторичные = 0;
		
		Для каждого ПараметрыОрганизации Из ИзменившиесяПараметрыПодключения Цикл
			Если ПараметрыОрганизации.ПоддерживаетсяОтправкаВторичногоЗаявления = Истина Тогда
				ПоддерживаютВторичные = ПоддерживаютВторичные + 1;
			КонецЕсли;
		КонецЦикла;
		
		НиктоНеПоддерживаетВторичные = ПоддерживаютВторичные = 0;
		ВсеПоддерживаютВторичные 	 = ПоддерживаютВторичные = КоличествоОрганизаций;
		
		Если НиктоНеПоддерживаетВторичные Тогда
			Результат = НСтр("ru = 'Сообщите об изменении настроек подключения в службу тех. поддержки оператор электронного документооборота.'");
		ИначеЕсли ВсеПоддерживаютВторичные Тогда
			Результат = НСтр("ru = 'Отправьте заявление на изменение настроек подключения к 1С-Отчетности из программы.'");
		Иначе 
			Результат = НСтр("ru = 'Сообщите об изменении настроек подключения в службу тех. поддержки оператора электронного документооборота или отправьте заявление из программы.'");
		КонецЕсли;
		
		Результат = НСтр("ru = 'Отправка отчетов станет недоступной после окончания срока действия сертификата или лицензии, а также при изменении реквизитов организации или сотрудника-владельца электронной подписи.'") + Результат;
			
		Если ЧтоИзменилось.ЕстьПереизданныеСертификаты Тогда
			ТекстДляПереизданнияСертификатов = НСтр("ru = 'Также ранее из этой программы было отправлено заявление на использование сертификата, изданного для другой программы. Сертификат был переиздан в другой программе. Вы можете использовать его так же и в этой программе, для чего необходимо отправить заявление на изменение.'");
			Результат = Результат + "<br><br>" + ТекстДляПереизданнияСертификатов;
		КонецЕсли;

	ИначеЕсли ЧтоИзменилось.ЕстьПереизданныеСертификаты Тогда
		
		Результат = НСтр("ru = 'Ранее из этой программы было отправлено заявление на использование сертификата, изданного для другой программы. Сертификат был переиздан в другой программе. Вы можете использовать его так же и в этой программе, для чего необходимо отправить заявление на изменение.'");
		
	КонецЕсли;
			
	Возврат Результат;

КонецФункции

Функция КнопкаОтправкиЗаявление(ИзменившиесяПараметрыПодключенияПоОднойУчетнойЗаписи, НоваяСтрокаHTMLТаблицы, НомерСтроки)
	
	НачалоКнопки 		= СтрНайти(НоваяСтрокаHTMLТаблицы, "<!--НачалоКартинки-->");
	КонецКнопки 		= СтрНайти(НоваяСтрокаHTMLТаблицы, "<!--КонецКартинки-->");
	КодКартинкиКонопки 	= Сред(НоваяСтрокаHTMLТаблицы, НачалоКнопки, КонецКнопки - НачалоКнопки);
	
	НачалоНадписиБезбумажноеПродление = СтрНайти(НоваяСтрокаHTMLТаблицы, "<!--НачалоНадписьБезбумажноеПродление-->");
	КонецНадписиБезбумажноеПродление  = СтрНайти(НоваяСтрокаHTMLТаблицы, "<!--КонецНадписьБезбумажноеПродление-->");
	КодНадписиБезбумажноеПродление    = Сред(НоваяСтрокаHTMLТаблицы, НачалоНадписиБезбумажноеПродление, КонецНадписиБезбумажноеПродление - НачалоНадписиБезбумажноеПродление);

	
	КнопкаОтправкиЗаявление = "";

	ПоддерживаетсяОтправкаВторичногоЗаявления = ИзменившиесяПараметрыПодключенияПоОднойУчетнойЗаписи.ПоддерживаетсяОтправкаВторичногоЗаявления;
	ВозможноБезбумажноеПродление = ИзменившиесяПараметрыПодключенияПоОднойУчетнойЗаписи.ВозможноБезбумажноеПродление;
	
	Если ПоддерживаетсяОтправкаВторичногоЗаявления Тогда
		// Показываем гиперссылку на заявление, если оператор электронного документооборота поддерживает вторичные заявления
		КнопкаОтправкиЗаявление = "<A href=""&amp;ОтправкаЗаявленияНаИзменениеРеквизитовПодключения"
			+ Формат(НомерСтроки, "ЧН=0; ЧГ=") + "КонецСсылки"">" + КодКартинкиКонопки + "</A>";
	Иначе
		// Определяем текст надписи для случая, когда оператор электронного документооборота не поддерживает вторичные заявления
		СпецоператорСвязи = ИзменившиесяПараметрыПодключенияПоОднойУчетнойЗаписи.УчетнаяЗаписьДокументооборота.СпецоператорСвязи;
		Если СпецоператорСвязи = Перечисления.СпецоператорыСвязи.Прочие Тогда
			// Если оператор электронного документооборота Прочий, тогда выводим 'Обратитесь к своему оператору электронного документооборота'
			КнопкаОтправкиЗаявление = "<FONT class=head2>" + НСтр("ru = 'Обратитесь к своему оператору электронного документооборота'") + "</FONT>";
		Иначе
			// Если оператор электронного документооборота конкретный, а не Прочий, тогда выводим 'Обратитесь к оператору электронного документооборота Калуга Астрал'
			КнопкаОтправкиЗаявление = НСтр("ru = 'Обратитесь к оператору электронного документооборота %1'");
			КнопкаОтправкиЗаявление = "<FONT class=head2>" + СтрЗаменить(КнопкаОтправкиЗаявление, "%1", СпецоператорСвязи) + "</FONT>";
		КонецЕсли;
		
	КонецЕсли;
	
	Результат = Новый Структура("Кнопка, Надпись", КнопкаОтправкиЗаявление, ?(ВозможноБезбумажноеПродление, КодНадписиБезбумажноеПродление, "")); 
	
	Возврат Результат;
	
КонецФункции

Процедура ОпределитьДляКаждойОрганизацииВозможностьПоддержкиВторичныхЗаявлений(ИзменившиесяПараметрыПодключения)
	
	ИзменившиесяПараметрыПодключения.Колонки.Добавить("ПоддерживаетсяОтправкаВторичногоЗаявления");
	
	КоличествоОрганизаций = ИзменившиесяПараметрыПодключения.Количество();
	
	Для НомерСтроки = 1 По КоличествоОрганизаций Цикл
		
		ИзменившиесяПараметрыПодключенияПоОднойУчетнойЗаписи = ИзменившиесяПараметрыПодключения[НомерСтроки - 1];
		
		// Гиперссылка на заявление. Не показывается, если оператор электронного документооборота не поддерживает вторичные заявления
		СпецоператорСвязи = ИзменившиесяПараметрыПодключенияПоОднойУчетнойЗаписи.УчетнаяЗаписьДокументооборота.СпецоператорСвязи;
		ПоддерживаетсяОтправкаВторичногоЗаявления = ЭлектронныйДокументооборотСКонтролирующимиОрганамиВызовСервера.ПолучитьПараметрСпецоператора(СпецоператорСвязи, "ПоддерживаетсяОтправкаВторичногоЗаявления");
		ИзменившиесяПараметрыПодключенияПоОднойУчетнойЗаписи.ПоддерживаетсяОтправкаВторичногоЗаявления = ПоддерживаетсяОтправкаВторичногоЗаявления = "Истина";
		
	КонецЦикла;
	
КонецПроцедуры

Функция ТекстБезПереносов(Текст)
	
	Возврат "<nobr>" + Текст + "</nobr>";
	
КонецФункции

Функция ТекстВHTML(Текст) Экспорт
	
	Возврат СтрЗаменить(СтрЗаменить(СтрЗаменить(СтрЗаменить(Текст, "<", "&lt;"), ">", "&gt;"), """", "&quot;"), Символы.ПС, " &#13;&#10;");
	
КонецФункции

Функция ТекстЧерезСколькоЛетМесяцевНедельДней(ДатаОтсчета, ДатаЗавершения, ПрефиксЧисла, ПостфиксСуществительного) Экспорт
	
	Если ДатаЗавершения <= ДатаОтсчета Тогда
		Возврат Неопределено;
	КонецЕсли;
	
	ГодОтсчета = Год(ДатаОтсчета);
	МесяцОтсчета = Месяц(ДатаОтсчета);
	ДеньОтсчета = День(ДатаОтсчета);
	ЧасОтсчета = Час(ДатаОтсчета);
	МинутаОтсчета = Минута(ДатаОтсчета);
	СекундаОтсчета = Секунда(ДатаОтсчета);
	
	ГодЗавершения = Год(ДатаЗавершения);
	МесяцЗавершения = Месяц(ДатаЗавершения);
	ДеньЗавершения = День(ДатаЗавершения);
	ЧасЗавершения = Час(ДатаЗавершения);
	МинутаЗавершения = Минута(ДатаЗавершения);
	СекундаЗавершения = Секунда(ДатаЗавершения);
	
	РазницаВГодах = ГодЗавершения - ГодОтсчета;
	РазницаВМесяцах = МесяцЗавершения - МесяцОтсчета;
	РазницаВДнях = ДеньЗавершения - ДеньОтсчета;
	РазницаВЧасах = ЧасЗавершения - ЧасОтсчета;
	РазницаВМинутах = МинутаЗавершения - МинутаОтсчета;
	РазницаВСекундах = СекундаЗавершения - СекундаОтсчета;
	
	Если РазницаВСекундах < 0 Тогда
		РазницаВМинутах = РазницаВМинутах - 1;
		РазницаВСекундах = РазницаВСекундах + 60;
	КонецЕсли;
	
	Если РазницаВМинутах < 0 Тогда
		РазницаВЧасах = РазницаВЧасах - 1;
		РазницаВМинутах = РазницаВМинутах + 60;
	КонецЕсли;
	
	Если РазницаВЧасах < 0 Тогда
		РазницаВДнях = РазницаВДнях - 1;
		РазницаВЧасах = РазницаВЧасах + 24;
	КонецЕсли;
	
	Если РазницаВДнях < 0 Тогда
		
		РазницаВМесяцах = РазницаВМесяцах - 1;
		Если МесяцЗавершения = 3 Тогда
			// берем год отсчета, в который существуют все дни месяца, например, с 29.02.2008 по 1.03.2009
			// прошло 1 год 1 день: c 29.02.2008 по 1.03.2008 прошел 1 день, далее 1 год
			Если (ГодОтсчета % 4 = 0) И ((ГодОтсчета % 100 <> 0) ИЛИ (ГодОтсчета % 400 = 0)) Тогда
				РазницаВДнях = РазницаВДнях + 29;
			Иначе
				РазницаВДнях = РазницаВДнях + 28;
			КонецЕсли;
			
		ИначеЕсли (МесяцЗавершения = 5) ИЛИ (МесяцЗавершения = 7) ИЛИ (МесяцЗавершения = 10)
			ИЛИ (МесяцЗавершения = 12) Тогда
			РазницаВДнях = РазницаВДнях + 30;
			
		Иначе
			РазницаВДнях = РазницаВДнях + 31;
		КонецЕсли;
		
	КонецЕсли;
	
	Если РазницаВМесяцах < 0 Тогда
		РазницаВМесяцах = РазницаВМесяцах + 12;
		РазницаВГодах = РазницаВГодах - 1;
	КонецЕсли;
	РазницаВНеделях = Цел(РазницаВДнях / 7);
	РазницаВДнях = РазницаВДнях % 7;
	
	Результат = "";
	Если РазницаВГодах > 0 Тогда
		Результат = ПрефиксЧисла + ЧислоССуществительным(РазницаВГодах, НСтр("ru='год'"), НСтр("ru='года'"), НСтр("ru='лет'")) + ПостфиксСуществительного;
	КонецЕсли;
	
	Если РазницаВМесяцах > 0 Тогда
		НомерМесяца = ЧислоССуществительным(РазницаВМесяцах, НСтр("ru='месяц'"), НСтр("ru='месяца'"), НСтр("ru='месяцев'"));
		Разделитель = ?(РазницаВНеделях = 0 И РазницаВДнях = 0, " " + НСтр("ru='и'") + " ", ", ");
		Результат = СоединитьЧерез(Результат, Разделитель, ПрефиксЧисла + НомерМесяца + ПостфиксСуществительного);
	КонецЕсли;
	
	Если РазницаВНеделях > 0 Тогда
		НомерНедели = ЧислоССуществительным(РазницаВНеделях, НСтр("ru='неделю'"), НСтр("ru='недели'"), НСтр("ru='недель'"));
		Разделитель = ?(РазницаВДнях = 0, " " + НСтр("ru='и'") + " ", ", ");
		Результат = СоединитьЧерез(Результат, Разделитель, ПрефиксЧисла + НомерНедели + ПостфиксСуществительного);
	КонецЕсли;
	
	Если РазницаВДнях > 0 Тогда
		НомерДня = ЧислоССуществительным(РазницаВДнях, НСтр("ru='день'"), НСтр("ru='дня'"), НСтр("ru='дней'"));
		Результат = СоединитьЧерез(Результат, " " + НСтр("ru='и'") + " ", ПрефиксЧисла + НомерДня + ПостфиксСуществительного);
	ИначеЕсли (РазницаВДнях = 0) И (Результат = "") Тогда
		
		Если (РазницаВЧасах > 0) ИЛИ (РазницаВМинутах > 0) Тогда
			Если РазницаВЧасах > 0 Тогда
				НомерЧаса = ЧислоССуществительным(РазницаВЧасах, НСтр("ru='час'"), НСтр("ru='часа'"), НСтр("ru='часов'"));
				Результат = ПрефиксЧисла + НомерЧаса + ПостфиксСуществительного;
			КонецЕсли;
			Если РазницаВМинутах > 0 Тогда
				НомерМинуты = ЧислоССуществительным(РазницаВМинутах, НСтр("ru='минуту'"), НСтр("ru='минуты'"), НСтр("ru='минут'"));
				Результат = СоединитьЧерез(Результат, " " + НСтр("ru='и'") + " ", ПрефиксЧисла + НомерМинуты + ПостфиксСуществительного);
			КонецЕсли;
		Иначе
			НомерСекунды = ЧислоССуществительным(РазницаВСекундах, НСтр("ru='секунду'"), НСтр("ru='секунды'"), НСтр("ru='секунд'"));
			Результат = ПрефиксЧисла + НомерСекунды + ПостфиксСуществительного;
		КонецЕсли;
		
	КонецЕсли;
	
	Возврат Результат;
	
КонецФункции

Функция ЧислоССуществительным(Количество, ИменительныйИлиВинительныйПадежЕдинственноеЧисло, РодительныйПадежЕдинственноеЧисло, РодительныйПадежМножественноеЧисло)
	
	ОстатокДеленияКоличестваНа100 = Количество % 100;
	ОстатокДеленияКоличестваНа10 = Количество % 10;
	
	Если (ОстатокДеленияКоличестваНа10 = 1) И (ОстатокДеленияКоличестваНа100 <> 11) Тогда
		Возврат Строка(Количество) + " " + ИменительныйИлиВинительныйПадежЕдинственноеЧисло;
		
	ИначеЕсли (ОстатокДеленияКоличестваНа10 >= 2) И (ОстатокДеленияКоличестваНа10 <= 4) И ((ОстатокДеленияКоличестваНа100 < 12) ИЛИ (ОстатокДеленияКоличестваНа100 > 14)) Тогда
		Возврат Строка(Количество) + " " + РодительныйПадежЕдинственноеЧисло;
		
	Иначе
		Возврат Строка(Количество) + " " + РодительныйПадежМножественноеЧисло;
	КонецЕсли;
	
КонецФункции

Функция СоединитьЧерез(Строка1, Разделитель, Строка2)
	
	Если ПустаяСтрока(Строка(Строка1)) Тогда
		Возврат Строка2;
	ИначеЕсли ПустаяСтрока(Строка(Строка2)) Тогда
		Возврат Строка1;
	Иначе
		Возврат Строка1 + Разделитель + Строка2;
	КонецЕсли;
	
КонецФункции

Процедура ДобавитьВТаблицуСписокИзменившихсяРеквизитовПодключенияК1СОтчетности(ИзменившиесяРеквизитыОрганизации) Экспорт
	
	Организация = ИзменившиесяРеквизитыОрганизации.Организация;
	УчетнаяЗапись = ИзменившиесяРеквизитыОрганизации.УчетнаяЗаписьДокументооборота;
	
	ДанныеЗаполнения = Неопределено;
	
	// Заполняем текущие реквизиты организации 
	СтруктураРеквизитов = Новый Структура("Организация, ПриОткрытии", Организация, НЕ ДанныеЗаполнения = Неопределено);
	Если ДанныеЗаполнения <> Неопределено Тогда
		СтруктураРеквизитов.Вставить("АдресЮридический",);
		СтруктураРеквизитов.Вставить("АдресФактический",);
	КонецЕсли;
	ЗаполнитьДанныеОрганизации(СтруктураРеквизитов);
	ДанныеЗаполнения = ДополнитьДанныеОрганизацииДаннымиПоОтветственнымЛицам(СтруктураРеквизитов);
	ДанныеОрганизации = ДанныеЗаполнения.СтруктураДанныхОрганизации;
	
	// Получаем реквизиты организации
	Руководитель	= ЭлектронныйДокументооборотСКонтролирующимиОрганамиВызовСервераПереопределяемый.Руководитель(Организация); 
	ГлБухгалтер		= ЭлектронныйДокументооборотСКонтролирующимиОрганамиВызовСервераПереопределяемый.ГлБухгалтер(Организация);
	
	ЭтоЮридическоеЛицо		 = ДанныеОрганизации.ТипОрганизации;
	ПолноеНаименование		 = ДанныеОрганизации.НаимЮЛПол;
	КраткоеНаименование		 = ДанныеОрганизации.КраткоеНаименование;
	ИНН						 = ДанныеОрганизации.ИННЮЛ;
	КПП						 = ДанныеОрганизации.КППЮЛ;
	ОГРН					 = ДанныеОрганизации.ОГРН;
	РегНомерПФР				 = ДанныеОрганизации.РегНомПФР;
	ТелефонОсновной			 = ДанныеОрганизации.ТелОрганизации;
	ТелефонДополнительный	 = ДанныеОрганизации.ТелРук;
	АдрЮР					 = ДанныеЗаполнения.АдресЮридический;
	АдрФакт				 	 = ДанныеЗаполнения.АдресФактический;
	АдресЮридический		 = ДанныеЗаполнения.ЮрАдрес;
	АдресФактический		 = ДанныеЗаполнения.ФактАдрес;
	
	Если АдресЮридический = ",,,,,,,,," Тогда
		АдресЮридический = "";
	КонецЕсли;
	
	Если АдресФактический = ",,,,,,,,," Тогда
		АдресФактический = "";
	КонецЕсли;
	
	ЭлектроннаяПочтаИсходная = ДанныеОрганизации.ЭлектроннаяПочта;
	КодРегионаФСРАР = РегламентированнаяОтчетностьКлиентСервер.РазложитьАдрес(АдрЮр).Регион;
	
	КодПФР = КодПФР(ДанныеОрганизации);
	
	// Оператор электронного документооборота
	ОператорПоддерживаетСМСУведомление = ЭлектронныйДокументооборотСКонтролирующимиОрганамиВызовСервера.ПолучитьПараметрСпецоператора(УчетнаяЗапись.СпецоператорСвязи, "ПризнакПоддержкиСМС") = "Истина";
	Спецоператор = УчетнаяЗапись.СпецоператорСвязи;
	
	// Определяем исходные параметры подключенния к 1С-Отчетности
	Запрос = Новый Запрос;
	Запрос.Текст = 
		"ВЫБРАТЬ РАЗРЕШЕННЫЕ
		|	ДополнительныеРеквизитыУчетнойЗаписи.ТипКриптопровайдера,
		|	ДополнительныеРеквизитыУчетнойЗаписи.НомерОсновнойПоставки1с,
		|	ДополнительныеРеквизитыУчетнойЗаписи.КраткоеНаименование,
		|	ДополнительныеРеквизитыУчетнойЗаписи.ПолноеНаименование,
		|	ДополнительныеРеквизитыУчетнойЗаписи.ИНН,
		|	ДополнительныеРеквизитыУчетнойЗаписи.КПП,
		|	ДополнительныеРеквизитыУчетнойЗаписи.ОГРН,
		|	ДополнительныеРеквизитыУчетнойЗаписи.РегНомерПФР,
		|	ДополнительныеРеквизитыУчетнойЗаписи.ЭлектроннаяПочта,
		|	ДополнительныеРеквизитыУчетнойЗаписи.ВладелецЭЦПФамилия,
		|	ДополнительныеРеквизитыУчетнойЗаписи.ВладелецЭЦПИмя,
		|	ДополнительныеРеквизитыУчетнойЗаписи.ВладелецЭЦПОтчество,
		|	ДополнительныеРеквизитыУчетнойЗаписи.ВладелецЭЦППодразделение,
		|	ДополнительныеРеквизитыУчетнойЗаписи.ВладелецЭЦПСНИЛС,
		|	ДополнительныеРеквизитыУчетнойЗаписи.ВладелецЭЦПДолжность,
		|	ДополнительныеРеквизитыУчетнойЗаписи.ТелефонМобильный,
		|	ДополнительныеРеквизитыУчетнойЗаписи.ТелефонОсновной,
		|	ДополнительныеРеквизитыУчетнойЗаписи.ТелефонДополнительный,
		|	ДополнительныеРеквизитыУчетнойЗаписи.КодРегионаФСРАР
		|ИЗ
		|	РегистрСведений.ДополнительныеРеквизитыУчетнойЗаписи КАК ДополнительныеРеквизитыУчетнойЗаписи
		|ГДЕ
		|	ДополнительныеРеквизитыУчетнойЗаписи.УчетнаяЗапись = &УчетнаяЗапись";

	Запрос.УстановитьПараметр("УчетнаяЗапись", УчетнаяЗапись);
	ДополнительныеРеквизитыУчетнойЗаписи = Запрос.Выполнить().Выгрузить();
	
	Если ДополнительныеРеквизитыУчетнойЗаписи.Количество() > 0 Тогда
		ТипКриптопровайдераИсходный 		= ДополнительныеРеквизитыУчетнойЗаписи[0].ТипКриптопровайдера;
		КраткоеНаименованиеИсходное 		= ДополнительныеРеквизитыУчетнойЗаписи[0].КраткоеНаименование;
		ПолноеНаименованиеИсходное 			= ДополнительныеРеквизитыУчетнойЗаписи[0].ПолноеНаименование;
		КППИсходный 						= ДополнительныеРеквизитыУчетнойЗаписи[0].КПП;
		ОГРНИсходный 						= ДополнительныеРеквизитыУчетнойЗаписи[0].ОГРН;
		РегНомерПФРИсходный 				= ДополнительныеРеквизитыУчетнойЗаписи[0].РегНомерПФР;
		ЭлектроннаяПочтаИсходная 			= ДополнительныеРеквизитыУчетнойЗаписи[0].ЭлектроннаяПочта;
		ВладелецЭЦПФамилияИсходный 			= ДополнительныеРеквизитыУчетнойЗаписи[0].ВладелецЭЦПФамилия;
		ВладелецЭЦПИмяИсходный 				= ДополнительныеРеквизитыУчетнойЗаписи[0].ВладелецЭЦПИмя;
		ВладелецЭЦПОтчествоИсходный 		= ДополнительныеРеквизитыУчетнойЗаписи[0].ВладелецЭЦПОтчество;
		ВладелецЭЦППодразделениеИсходное 	= ДополнительныеРеквизитыУчетнойЗаписи[0].ВладелецЭЦППодразделение;
		ВладелецЭЦПСНИЛСИсходный 			= ДополнительныеРеквизитыУчетнойЗаписи[0].ВладелецЭЦПСНИЛС;
		ВладелецЭЦПДолжностьИсходная 		= ДополнительныеРеквизитыУчетнойЗаписи[0].ВладелецЭЦПДолжность;
		ТелефонОсновнойИсходный 			= ДополнительныеРеквизитыУчетнойЗаписи[0].ТелефонОсновной;
		ТелефонДополнительныйИсходный 		= ДополнительныеРеквизитыУчетнойЗаписи[0].ТелефонДополнительный;
		КодРегионаФСРАРИсходный 			= ДополнительныеРеквизитыУчетнойЗаписи[0].КодРегионаФСРАР;
	Иначе
		// Если в регистре ДополнительныеРеквизитыУчетнойЗаписи ничего нет, то расхождения реквизитов найти невозможно
		ИзменившиесяРеквизитыОрганизации.СписокИзменившихсяРеквизитов = Неопределено;
		Возврат;
	КонецЕсли;
	
	Запрос = Новый Запрос;
	Запрос.Текст = 
		"ВЫБРАТЬ РАЗРЕШЕННЫЕ
		|	ДополнительныеРеквизитыУчетнойЗаписиПолучатели.ТипПолучателя,
		|	ДополнительныеРеквизитыУчетнойЗаписиПолучатели.КодПолучателя,
		|	ДополнительныеРеквизитыУчетнойЗаписиПолучатели.КПП
		|ИЗ
		|	РегистрСведений.ДополнительныеРеквизитыУчетнойЗаписиПолучатели КАК ДополнительныеРеквизитыУчетнойЗаписиПолучатели
		|ГДЕ
		|	ДополнительныеРеквизитыУчетнойЗаписиПолучатели.УчетнаяЗапись = &УчетнаяЗапись";

	Запрос.УстановитьПараметр("УчетнаяЗапись", УчетнаяЗапись);
	Результат = Запрос.Выполнить().Выгрузить();
	
	// Направляения сдачи отчетности и коды
	СдаватьВФССИсходный 	= Ложь;
	СдаватьВФСРАРИсходный 	= Ложь;
	СдаватьВРПНИсходный 	= Ложь;
	СдаватьВФТСИсходный 	= Ложь;
	СдаватьВПФРИсходный 	= Ложь;
	КодПФРИсходный 			= "";
	СдаватьВРосстатИсходный = Ложь;
	
	Для Каждого СтрокаНаправлений Из Результат Цикл
		Если СтрокаНаправлений.ТипПолучателя = Перечисления.ТипыКонтролирующихОрганов.ПФР Тогда
			СдаватьВПФРИсходный = Истина;
			КодПФРИсходный = ЭлектронныйДокументооборотСКонтролирующимиОрганамиКлиентСервер.СтрокаВФорматеДляСравнения(СтрокаНаправлений.КодПолучателя);
		ИначеЕсли СтрокаНаправлений.ТипПолучателя = Перечисления.ТипыКонтролирующихОрганов.ФСС Тогда
			СдаватьВФССИсходный = Истина;
		ИначеЕсли СтрокаНаправлений.ТипПолучателя = Перечисления.ТипыКонтролирующихОрганов.ФСРАР Тогда
			СдаватьВФСРАРИсходный = Истина;
		ИначеЕсли СтрокаНаправлений.ТипПолучателя = Перечисления.ТипыКонтролирующихОрганов.РПН Тогда
			СдаватьВРПНИсходный = Истина;
		ИначеЕсли СтрокаНаправлений.ТипПолучателя = Перечисления.ТипыКонтролирующихОрганов.ФТС Тогда
			СдаватьВФТСИсходный = Истина;
		КонецЕсли;
	КонецЦикла;
	
	// Если не удалось определить тип владельца ЭЦП из-за того что не был доступен сертификат, то 
	// пытаемся определить тип исходя из ФИО
	СтруктураФИОРуководителя = ЭлектронныйДокументооборотСКонтролирующимиОрганамиВызовСервера.ФИОФизЛица(Руководитель);
	СтруктураФИОГлБухгалтер = ЭлектронныйДокументооборотСКонтролирующимиОрганамиВызовСервера.ФИОФизЛица(ГлБухгалтер);
	// Сравниваем с ФИО руководителя
	Если ЭлектронныйДокументооборотСКонтролирующимиОрганамиКлиентСервер.СтрокаВФорматеДляСравнения(СтруктураФИОРуководителя.Имя) 
			= ЭлектронныйДокументооборотСКонтролирующимиОрганамиКлиентСервер.СтрокаВФорматеДляСравнения(ВладелецЭЦПИмяИсходный) 
		И ЭлектронныйДокументооборотСКонтролирующимиОрганамиКлиентСервер.СтрокаВФорматеДляСравнения(СтруктураФИОРуководителя.Отчество) 
			= ЭлектронныйДокументооборотСКонтролирующимиОрганамиКлиентСервер.СтрокаВФорматеДляСравнения(ВладелецЭЦПОтчествоИсходный)
		И ЭлектронныйДокументооборотСКонтролирующимиОрганамиКлиентСервер.СтрокаВФорматеДляСравнения(СтруктураФИОРуководителя.Фамилия) 
			= ЭлектронныйДокументооборотСКонтролирующимиОрганамиКлиентСервер.СтрокаВФорматеДляСравнения(ВладелецЭЦПФамилияИсходный) Тогда
		ВладелецЭЦПТипИсходный = 1;
	// Сравниваем с ФИО бухгалтера
	ИначеЕсли ЭлектронныйДокументооборотСКонтролирующимиОрганамиКлиентСервер.СтрокаВФорматеДляСравнения(СтруктураФИОГлБухгалтер.Имя) 
			= ЭлектронныйДокументооборотСКонтролирующимиОрганамиКлиентСервер.СтрокаВФорматеДляСравнения(ВладелецЭЦПИмяИсходный)
		И ЭлектронныйДокументооборотСКонтролирующимиОрганамиКлиентСервер.СтрокаВФорматеДляСравнения(СтруктураФИОГлБухгалтер.Отчество) 
			= ЭлектронныйДокументооборотСКонтролирующимиОрганамиКлиентСервер.СтрокаВФорматеДляСравнения(ВладелецЭЦПОтчествоИсходный)
		И ЭлектронныйДокументооборотСКонтролирующимиОрганамиКлиентСервер.СтрокаВФорматеДляСравнения(СтруктураФИОГлБухгалтер.Фамилия) 
			= ЭлектронныйДокументооборотСКонтролирующимиОрганамиКлиентСервер.СтрокаВФорматеДляСравнения(ВладелецЭЦПФамилияИсходный) Тогда
		ВладелецЭЦПТипИсходный = 2;
	Иначе
		// Если владелец ЭЦП не руководитель и не бухгалтер, то мы можем найти его только по ФИО 
		ВладелецЭЦПИсходный = ЭлектронныйДокументооборотСКонтролирующимиОрганамиВызовСервераПереопределяемый.ФизЛицоПоФИО(
			ВладелецЭЦПФамилияИсходный, ВладелецЭЦПИмяИсходный, ВладелецЭЦПОтчествоИсходный, ВладелецЭЦПСНИЛСИсходный, Организация);

		СотрудникВыборИсходный  = ВладелецЭЦПИсходный;
		СотрудникВыбор 			= СотрудникВыборИсходный;
		
		ВладелецЭЦПТипИсходный = 3;
	КонецЕсли; 
	
	ВладелецЭЦП = Неопределено;
	
	Если ВладелецЭЦПТипИсходный = 1 Тогда
		ВладелецЭЦП     = Руководитель;
		ВидВладельцаЭЦП = Перечисления.ТипыВладельцевЭЦП.Руководитель;
	ИначеЕсли ВладелецЭЦПТипИсходный = 2 Тогда
		ВладелецЭЦП     = ГлБухгалтер;
		ВидВладельцаЭЦП = Перечисления.ТипыВладельцевЭЦП.ГлавныйБухгалтер;
	ИначеЕсли ВладелецЭЦПТипИсходный = 3 Тогда
		ВладелецЭЦП     = СотрудникВыбор;
		ВидВладельцаЭЦП = Перечисления.ТипыВладельцевЭЦП.ДругойСотрудник;
	КонецЕсли;
	
 	ДанныеОрганизации.Вставить("Организация", Организация);
	
	ДанныеСотрудника = ЭлектронныйДокументооборотСКонтролирующимиОрганамиВызовСервера.ПолучитьДанныеСотрудника(
		ВидВладельцаЭЦП, 
		ДанныеОрганизации, 
		ВладелецЭЦП);
	
	ВладелецЭЦПИмя					= ДанныеСотрудника.ФИО.Имя;
	ВладелецЭЦПФамилия				= ДанныеСотрудника.ФИО.Фамилия;
	ВладелецЭЦПОтчество				= ДанныеСотрудника.ФИО.Отчество;
	ВладелецЭЦПДолжность           	= ДанныеСотрудника.Должность;
	ВладелецЭЦППодразделение       	= ДанныеСотрудника.Подразделение;
	ВладелецЭЦПСНИЛС			   	= ДанныеСотрудника.СНИЛС;
	
	ВладелецЭЦПИсходныйНайден = ВладелецЭЦПИсходный <> ПредопределенноеЗначение("Справочник.ФизическиеЛица.ПустаяСсылка");
	
	// Сравниваем исходные и текущие параметры подключения к 1С-Отчетности
	// Изменился ли владелец ЭЦП ?
	//
	ВладелецЭЦПИзменился = ((ЭлектронныйДокументооборотСКонтролирующимиОрганамиКлиентСервер.СтрокаВФорматеДляСравнения(ВладелецЭЦПФамилия) 
								<> ЭлектронныйДокументооборотСКонтролирующимиОрганамиКлиентСервер.СтрокаВФорматеДляСравнения(ВладелецЭЦПФамилияИсходный) 
							ИЛИ ЭлектронныйДокументооборотСКонтролирующимиОрганамиКлиентСервер.СтрокаВФорматеДляСравнения(ВладелецЭЦПИмя) 
								<> ЭлектронныйДокументооборотСКонтролирующимиОрганамиКлиентСервер.СтрокаВФорматеДляСравнения(ВладелецЭЦПИмяИсходный) 
							ИЛИ ЭлектронныйДокументооборотСКонтролирующимиОрганамиКлиентСервер.СтрокаВФорматеДляСравнения(ВладелецЭЦПОтчество) 
								<> ЭлектронныйДокументооборотСКонтролирующимиОрганамиКлиентСервер.СтрокаВФорматеДляСравнения(ВладелецЭЦПОтчествоИсходный)))
							И ВладелецЭЦПИсходныйНайден;// если в базе не найден владелец - значит говорим, что он не изменился
	ВладелецЭЦПИзменился = Ложь; // для целей предупреждения временно не отслеживаем
	
	// Изменились ли реквизиты подключения к 1С-Отчетности ?
	//
	// Краткое наименование
	КраткоеНаименованиеИзменилось = Ложь;// для целей предупреждения не отслеживаем
	
	// Полное наименование
	ПолноеНаименованиеИзменилось = Ложь;// для целей предупреждения не отслеживаем
	
	// КПП
	КППИзменился = ЭлектронныйДокументооборотСКонтролирующимиОрганамиКлиентСервер.СтрокаВФорматеДляСравнения(КПП) 
		<> ЭлектронныйДокументооборотСКонтролирующимиОрганамиКлиентСервер.СтрокаВФорматеДляСравнения(КППИсходный) И ЗначениеЗаполнено(КПП);
	
	// ОГРН
	ОГРНИзменился = ЭлектронныйДокументооборотСКонтролирующимиОрганамиКлиентСервер.СтрокаВФорматеДляСравнения(ОГРН) 
		<> ЭлектронныйДокументооборотСКонтролирующимиОрганамиКлиентСервер.СтрокаВФорматеДляСравнения(ОГРНИсходный) И ЗначениеЗаполнено(СтрокаБезДефисов(ОГРН));
	
	// Регистрационный номер в ПФР
	РегНомерПФРИзменился = ЭлектронныйДокументооборотСКонтролирующимиОрганамиКлиентСервер.СтрокаВФорматеДляСравнения(РегНомерПФР) 
		<> ЭлектронныйДокументооборотСКонтролирующимиОрганамиКлиентСервер.СтрокаВФорматеДляСравнения(РегНомерПФРИсходный) 
		И СдаватьВПФРИсходный И ЗначениеЗаполнено(СтрокаБезДефисов(РегНомерПФР));
	
	// Регион
	КодРегионаФСРАРИзменился = ЭлектронныйДокументооборотСКонтролирующимиОрганамиКлиентСервер.СтрокаВФорматеДляСравнения(КодРегионаФСРАРИсходный) 
		<> ЭлектронныйДокументооборотСКонтролирующимиОрганамиКлиентСервер.СтрокаВФорматеДляСравнения(КодРегионаФСРАР) 
		И СдаватьВФСРАРИсходный И ЗначениеЗаполнено(КодРегионаФСРАР) И ЗначениеЗаполнено(КодРегионаФСРАРИсходный);
	
	// Электронная почта
	ЭлектроннаяПочтаИзменилась = Ложь; // для целей предупреждения не отслеживаем
	
	// Телефон основной
	ТелефонОсновнойИзменился = Ложь;// для целей предупреждения не отслеживаем
	
	// Телефон дополнительный
	ТелефонДополнительныйИзменился = Ложь;// для целей предупреждения не отслеживаем
	
	// Сведения о сотруднике - владельце электронной подписи
	
	// Подразделение
	ВладелецЭЦППодразделениеИзменилось = ЭлектронныйДокументооборотСКонтролирующимиОрганамиКлиентСервер.СтрокаВФорматеДляСравнения(ВладелецЭЦППодразделение) 
		<> ЭлектронныйДокументооборотСКонтролирующимиОрганамиКлиентСервер.СтрокаВФорматеДляСравнения(ВладелецЭЦППодразделениеИсходное) И ВладелецЭЦПИсходныйНайден
		И ЗначениеЗаполнено(ЭлектронныйДокументооборотСКонтролирующимиОрганамиКлиентСервер.СтрокаВФорматеДляСравнения(ВладелецЭЦППодразделение));
	ВладелецЭЦППодразделениеИзменилось = Ложь; // для целей предупреждения временно не отслеживаем
	
	// Должность
	ВладелецЭЦПДолжностьИзменилась = ЭлектронныйДокументооборотСКонтролирующимиОрганамиКлиентСервер.СтрокаВФорматеДляСравнения(ВладелецЭЦПДолжность) 
		<> ЭлектронныйДокументооборотСКонтролирующимиОрганамиКлиентСервер.СтрокаВФорматеДляСравнения(ВладелецЭЦПДолжностьИсходная) И ВладелецЭЦПИсходныйНайден
		И ЗначениеЗаполнено(ЭлектронныйДокументооборотСКонтролирующимиОрганамиКлиентСервер.СтрокаВФорматеДляСравнения(ВладелецЭЦПДолжность));
	ВладелецЭЦПДолжностьИзменилась = Ложь; // для целей предупреждения временно не отслеживаем
	
	// СНИЛС
	ВладелецЭЦПСНИЛСИзменился = ЭлектронныйДокументооборотСКонтролирующимиОрганамиКлиентСервер.СтрокаВФорматеДляСравнения(ВладелецЭЦПСНИЛС) 
		<> ЭлектронныйДокументооборотСКонтролирующимиОрганамиКлиентСервер.СтрокаВФорматеДляСравнения(ВладелецЭЦПСНИЛСИсходный)
		И ВладелецЭЦПИсходныйНайден // владельца ЭЦП удалось определить
		И ЗначениеЗаполнено(СтрокаБезДефисов(ВладелецЭЦПСНИЛС))// СНИЛС не пустой
		И ВладелецЭЦПСНИЛС <> "000-000-000 00" // СНИЛС не нулевой
		И (ВладелецЭЦПТипИсходный = 1 ИЛИ ВладелецЭЦПТипИсходный = 2); // это бухгалтер или директор
	ВладелецЭЦПСНИЛСИзменился = Ложь; // для целей предупреждения временно не отслеживаем
	
	// КодПФР
	КодПФРИзменился = ЭлектронныйДокументооборотСКонтролирующимиОрганамиКлиентСервер.СтрокаВФорматеДляСравнения(КодПФР) 
		<> ЭлектронныйДокументооборотСКонтролирующимиОрганамиКлиентСервер.СтрокаВФорматеДляСравнения(КодПФРИсходный) 
		И СдаватьВПФРИсходный И ЗначениеЗаполнено(СтрокаБезДефисов(КодПФР));
	
	// Добавляем измененные реквизиты в список
	//
	СписокИзменившихсяРеквизитов = Новый СписокЗначений;
	
	// Краткое наименование
	ДобавитьРеквизитВСписокИзменившихсяРеквизитов(КраткоеНаименованиеИзменилось, СписокИзменившихсяРеквизитов,
		Перечисления.ПараметрыПодключенияК1СОтчетности.КраткоеНаименование);
	
	// Полное наименование
	ДобавитьРеквизитВСписокИзменившихсяРеквизитов(ПолноеНаименованиеИзменилось, СписокИзменившихсяРеквизитов,  
		Перечисления.ПараметрыПодключенияК1СОтчетности.ПолноеНаименование);
	
	// КПП
	ДобавитьРеквизитВСписокИзменившихсяРеквизитов(КППИзменился, СписокИзменившихсяРеквизитов,  
		Перечисления.ПараметрыПодключенияК1СОтчетности.КПП);
	
	// ОГРН
	ДобавитьРеквизитВСписокИзменившихсяРеквизитов(ОГРНИзменился, СписокИзменившихсяРеквизитов,  
		Перечисления.ПараметрыПодключенияК1СОтчетности.ОГРН);
	
	// Регистрационный номер в ПФР
	ДобавитьРеквизитВСписокИзменившихсяРеквизитов(РегНомерПФРИзменился, СписокИзменившихсяРеквизитов,  
		Перечисления.ПараметрыПодключенияК1СОтчетности.РегНомерПФР);
	
	// Регион
	ДобавитьРеквизитВСписокИзменившихсяРеквизитов(КодРегионаФСРАРИзменился, СписокИзменившихсяРеквизитов,  
		Перечисления.ПараметрыПодключенияК1СОтчетности.КодРегионаФСРАР);
	
	// Электронная почта
	ДобавитьРеквизитВСписокИзменившихсяРеквизитов(ЭлектроннаяПочтаИзменилась, СписокИзменившихсяРеквизитов,  
		Перечисления.ПараметрыПодключенияК1СОтчетности.ЭлектроннаяПочта);
		
	// Телефон основной
	ДобавитьРеквизитВСписокИзменившихсяРеквизитов(ТелефонОсновнойИзменился, СписокИзменившихсяРеквизитов,  
		Перечисления.ПараметрыПодключенияК1СОтчетности.ТелефонОсновной);
		
	// КодПФР
	ДобавитьРеквизитВСписокИзменившихсяРеквизитов(КодПФРИзменился, СписокИзменившихсяРеквизитов,  
		Перечисления.ПараметрыПодключенияК1СОтчетности.КодПФР);
		
	// Сведения о сотруднике - владельце электронной подписи
	
	// Владелец ЭЦП
    Если ВладелецЭЦПИзменился Тогда
		ДобавитьРеквизитВСписокИзменившихсяРеквизитов(ВладелецЭЦПИзменился, СписокИзменившихсяРеквизитов,  
		Перечисления.ПараметрыПодключенияК1СОтчетности.ВладелецЭЦП);
	КонецЕсли;
	
	// Подразделение
	ДобавитьРеквизитВСписокИзменившихсяРеквизитов(ВладелецЭЦППодразделениеИзменилось, СписокИзменившихсяРеквизитов,  
		Перечисления.ПараметрыПодключенияК1СОтчетности.ВладелецЭЦППодразделение);
	
	// Должность
	ДобавитьРеквизитВСписокИзменившихсяРеквизитов(ВладелецЭЦПДолжностьИзменилась, СписокИзменившихсяРеквизитов,  
		Перечисления.ПараметрыПодключенияК1СОтчетности.ВладелецЭЦПДолжность);
	
	// СНИЛС
	ДобавитьРеквизитВСписокИзменившихсяРеквизитов(ВладелецЭЦПСНИЛСИзменился, СписокИзменившихсяРеквизитов,  
		Перечисления.ПараметрыПодключенияК1СОтчетности.ВладелецЭЦПСНИЛС);
		
	Если НЕ ВладелецЭЦПИзменился Тогда
		
		Отпечаток = ИзменившиесяРеквизитыОрганизации.УчетнаяЗаписьДокументооборота.СертификатРуководителя;
		Сертификат = Новый Структура("Отпечаток", Отпечаток);
		
		ФИО = Новый Массив; 
		ФИО.Добавить(СокрЛП(ВладелецЭЦПФамилия));
		ФИО.Добавить(СокрЛП(ВладелецЭЦПИмя));
		ФИО.Добавить(СокрЛП(ВладелецЭЦПОтчество));
		
		ФИО = СтрСоединить(ФИО, " ");
		
		ДополнительныеПараметры = Новый Структура();
		ДополнительныеПараметры.Вставить("ФИО",   		ФИО);
		ДополнительныеПараметры.Вставить("ИНН",   		ИНН);
		ДополнительныеПараметры.Вставить("СНИЛС", 		ВладелецЭЦПСНИЛС);
		ДополнительныеПараметры.Вставить("Сертификат",  Сертификат);
		
		ИзменившиесяРеквизитыОрганизации.СведенияДляБезбумажногоПродления = ДополнительныеПараметры;
		
	КонецЕсли;
		
	Если СписокИзменившихсяРеквизитов.Количество() = 0 Тогда
		ИзменившиесяРеквизитыОрганизации.СписокИзменившихсяРеквизитов = Неопределено;
	Иначе
		ИзменившиесяРеквизитыОрганизации.СписокИзменившихсяРеквизитов = СписокИзменившихсяРеквизитов;
	КонецЕсли;

КонецПроцедуры

Функция СтрокаБезДефисов(Текст) Экспорт
	
	Возврат СтрЗаменить(Текст, "-", "");
	
КонецФункции

Функция КодПФР(РеквизитыОрганизации)
	
	ПолученыйКодПФР = "";
	
	Если ПустаяСтрока(РеквизитыОрганизации.КодОрганаПФР) ИЛИ СтрДлина(РеквизитыОрганизации.КодОрганаПФР) < 7 Тогда
		ПолученыйКодПФР = Лев(РеквизитыОрганизации.КодОрганаПФР,7); 
	Иначе
		ПолученыйКодПФР = РеквизитыОрганизации.КодОрганаПФР;
	КонецЕсли;
	
	Возврат ПолученыйКодПФР;
	
КонецФункции

Функция ПредставлениеИзменившихсяРеквизитов(СписокИзменившихсяРеквизитов) Экспорт
	
	Если СписокИзменившихсяРеквизитов = Неопределено Тогда
		Возврат Неопределено;
	КонецЕсли;
	
	ИзмененныеРеквизиты = "";
	
	// Краткое наименование
	ДобавитьРеквизитКСтроке(Перечисления.ПараметрыПодключенияК1СОтчетности.КраткоеНаименование, СписокИзменившихсяРеквизитов, ИзмененныеРеквизиты);
	
	// Полное наименование
	ДобавитьРеквизитКСтроке(Перечисления.ПараметрыПодключенияК1СОтчетности.ПолноеНаименование, СписокИзменившихсяРеквизитов, ИзмененныеРеквизиты);
	
	// КПП
	ДобавитьРеквизитКСтроке(Перечисления.ПараметрыПодключенияК1СОтчетности.КПП, СписокИзменившихсяРеквизитов, ИзмененныеРеквизиты);
	
	// ОГРН
	ДобавитьРеквизитКСтроке(Перечисления.ПараметрыПодключенияК1СОтчетности.ОГРН, СписокИзменившихсяРеквизитов, ИзмененныеРеквизиты);
	
	// Регистрационный номер в ПФР
	ДобавитьРеквизитКСтроке(Перечисления.ПараметрыПодключенияК1СОтчетности.РегНомерПФР, СписокИзменившихсяРеквизитов, ИзмененныеРеквизиты);
	
	// Регион
	ДобавитьРеквизитКСтроке(Перечисления.ПараметрыПодключенияК1СОтчетности.КодРегионаФСРАР, СписокИзменившихсяРеквизитов, ИзмененныеРеквизиты);
	
	// Электронная почта
	ДобавитьРеквизитКСтроке(Перечисления.ПараметрыПодключенияК1СОтчетности.ЭлектроннаяПочта, СписокИзменившихсяРеквизитов, ИзмененныеРеквизиты);
	
	// КодПФР
	ДобавитьРеквизитКСтроке(Перечисления.ПараметрыПодключенияК1СОтчетности.КодПФР, СписокИзменившихсяРеквизитов, ИзмененныеРеквизиты);
	
	// КодРосстата
	ДобавитьРеквизитКСтроке(Перечисления.ПараметрыПодключенияК1СОтчетности.КодРосстата, СписокИзменившихсяРеквизитов, ИзмененныеРеквизиты);
	
	Если НЕ ПустаяСтрока(ИзмененныеРеквизиты) Тогда
		ИзмененныеРеквизиты = ИзмененныеРеквизиты + НСтр("ru = ' организации'");
	КонецЕсли;
	
	// Сведения о сотруднике - владельце электронной подписи
	
	// Владелец ЭЦП
	ДобавитьРеквизитКСтроке(Перечисления.ПараметрыПодключенияК1СОтчетности.ВладелецЭЦП, СписокИзменившихсяРеквизитов, ИзмененныеРеквизиты);
		
	// Должность
	ДобавитьРеквизитКСтроке(Перечисления.ПараметрыПодключенияК1СОтчетности.ВладелецЭЦПДолжность, СписокИзменившихсяРеквизитов, ИзмененныеРеквизиты);
	
	// СНИЛС
	ДобавитьРеквизитКСтроке(Перечисления.ПараметрыПодключенияК1СОтчетности.ВладелецЭЦПСНИЛС, СписокИзменившихсяРеквизитов, ИзмененныеРеквизиты);
		
	ВладелецЭЦПДолжностьИзменилась = СписокИзменившихсяРеквизитов.НайтиПоЗначению(Перечисления.ПараметрыПодключенияК1СОтчетности.ВладелецЭЦПДолжность) <> Неопределено;
	ВладелецЭЦПСНИЛСИзменился = СписокИзменившихсяРеквизитов.НайтиПоЗначению(Перечисления.ПараметрыПодключенияК1СОтчетности.ВладелецЭЦПСНИЛС) <> Неопределено;
	
	Если ВладелецЭЦПДолжностьИзменилась ИЛИ ВладелецЭЦПСНИЛСИзменился Тогда
		ИзмененныеРеквизиты = ИзмененныеРеквизиты + НСтр("ru = ' сотрудника-владельца ЭП'");
	КонецЕсли;
	
	ЗаменитьЗапятуюНаИВСтроке(ИзмененныеРеквизиты);
	
	Возврат ИзмененныеРеквизиты;
	
КонецФункции

Процедура ДобавитьРеквизитВСписокИзменившихсяРеквизитов(РеквизитИзменился, СписокИзменившихсяРеквизитов, Перечисление)
	
	РеквизитыНеХранящиесяВБазе = ЭлектронныйДокументооборотСКонтролирующимиОрганамиВызовСервера.РеквизитыНеХранящиесяВБазе();
	
	РеквизитЕстьВСправочникеОрганизации = РеквизитыНеХранящиесяВБазе.Найти(Перечисление) = Неопределено;
	
	Если РеквизитИзменился И РеквизитЕстьВСправочникеОрганизации Тогда
		СписокИзменившихсяРеквизитов.Добавить(Перечисление);
	КонецЕсли;
	
КонецПроцедуры

Процедура ДобавитьРеквизитКСтроке(Перечисление, СписокИзменившихсяРеквизитов, ИзмененныеРеквизиты) Экспорт
	
	НазваниеРеквизита = Строка(Перечисление);
	
	// Делаем так, чтобы все слова начинались с маленькой буквы
	// Если вторая буква большая, то считаем что слово целиком состоит из больших букв (например ИНН) и не переводим это слово в нижний регистр
	Если Сред(НазваниеРеквизита, 2, 1) <> Врег(Сред(НазваниеРеквизита, 2, 1)) Тогда
		НазваниеРеквизита = НРег(Лев(НазваниеРеквизита, 1)) + Сред(НазваниеРеквизита, 2);
	КонецЕсли;

	РеквизитИзменился = СписокИзменившихсяРеквизитов.НайтиПоЗначению(Перечисление) <> Неопределено;
	
	Если РеквизитИзменился Тогда
		Если ИзмененныеРеквизиты = "" Тогда
			ИзмененныеРеквизиты = НазваниеРеквизита;
		Иначе
			ИзмененныеРеквизиты = ИзмененныеРеквизиты + "%1" + НазваниеРеквизита;
		КонецЕсли;
	КонецЕсли;
	
КонецПроцедуры

Процедура ЗаменитьЗапятуюНаИВСтроке(ИзмененныеРеквизиты) Экспорт

	// В списке реквизитов заменяем последнюю запятую на букву "и"
	Если СтрЧислоВхождений(ИзмененныеРеквизиты, "%1") = 1 Тогда
		ИзмененныеРеквизиты = СтрЗаменить(ИзмененныеРеквизиты, "%1", " и ");
	ИначеЕсли СтрЧислоВхождений(ИзмененныеРеквизиты, "%1") > 1 Тогда
		МассивПодстрок = СтроковыеФункцииКлиентСервер.РазложитьСтрокуВМассивПодстрок(ИзмененныеРеквизиты, "%1");
		ПоследнийЭлементМассива = МассивПодстрок[МассивПодстрок.Количество() - 1];
		ИзмененныеРеквизиты = СтрЗаменить(ИзмененныеРеквизиты, "%1" + ПоследнийЭлементМассива, " и " + ПоследнийЭлементМассива);
	КонецЕсли;
	ИзмененныеРеквизиты = СтрЗаменить(ИзмененныеРеквизиты, "%1", ", ");

КонецПроцедуры

Функция ПолучитьТипИсполнителя(Тип) Экспорт
	
	Если Тип = Перечисления.ТипыВладельцевЭЦП.Руководитель Тогда
		ТипИсполнителя = 1;
	ИначеЕсли Тип = Перечисления.ТипыВладельцевЭЦП.ГлавныйБухгалтер Тогда
		ТипИсполнителя = 2;
	ИначеЕсли Тип = Перечисления.ТипыВладельцевЭЦП.ДругойСотрудник Тогда
		ТипИсполнителя = 3;
	Иначе
		ТипИсполнителя = 0;
	КонецЕсли;
	
	Возврат ТипИсполнителя;
КонецФункции

Функция ПолучитьТипВладельцаЭЦП(Тип) Экспорт
	
	Если Тип = 1 Тогда
		ТипИсполнителя = Перечисления.ТипыВладельцевЭЦП.Руководитель;
	ИначеЕсли Тип = 2 Тогда
		ТипИсполнителя = Перечисления.ТипыВладельцевЭЦП.ГлавныйБухгалтер;
	Иначе
		ТипИсполнителя = Перечисления.ТипыВладельцевЭЦП.ДругойСотрудник;
	КонецЕсли;
	
	Возврат ТипИсполнителя;
	
КонецФункции

Функция РазложитьСтрокуВМассивПодстрокКлиент(Знач Строка, Знач Разделитель = ",", Знач ПропускатьПустыеСтроки = Неопределено) Экспорт
	
	Результат = Новый Массив;
	
	// для обеспечения обратной совместимости
	Если ПропускатьПустыеСтроки = Неопределено Тогда
		ПропускатьПустыеСтроки = ?(Разделитель = " ", Истина, Ложь);
		Если ПустаяСтрока(Строка) Тогда 
			Если Разделитель = " " Тогда
				Результат.Добавить("");
			КонецЕсли;
			Возврат Результат;
		КонецЕсли;
	КонецЕсли;
	
	Позиция = СтрНайти(Строка, Разделитель);
	Пока Позиция > 0 Цикл
		Подстрока = Лев(Строка, Позиция - 1);
		Если Не ПропускатьПустыеСтроки Или Не ПустаяСтрока(Подстрока) Тогда
			Результат.Добавить(Подстрока);
		КонецЕсли;
		Строка = Сред(Строка, Позиция + СтрДлина(Разделитель));
		Позиция = СтрНайти(Строка, Разделитель);
	КонецЦикла;
	
	Если Не ПропускатьПустыеСтроки Или Не ПустаяСтрока(Строка) Тогда
		Результат.Добавить(Строка);
	КонецЕсли;
	
	Возврат Результат;
	
КонецФункции

Функция ПолучитьМакетОбработки(ИмяМакета)  Экспорт
	
	ОбработкаЭДО = ДокументооборотСКО.ПолучитьОбработкуЭДО();
	Возврат ОбработкаЭДО.ПолучитьМакет(ИмяМакета);
	
КонецФункции

Функция КодРегионаИзАдреса(Знач Адрес)
	
	Если ТипЗнч(Адрес) <> Тип("Структура") Тогда
		Адрес = РегламентированнаяОтчетностьКлиентСервер.РазложитьАдрес(Адрес);
	КонецЕсли;
	
	Если НЕ ЗначениеЗаполнено(Адрес.Регион) Тогда
		Возврат "";
	КонецЕсли;
	
	МакетРегионы = ПолучитьМакетОбработки("Регионы");
	нрегАдресРегион = нрег(Адрес.Регион);
	Для Инд = 1 По МакетРегионы.ВысотаТаблицы Цикл
		ТекРегион = СокрЛП(МакетРегионы.Область(Инд, 1, Инд, 1).Текст);
		Если Лев(нрегАдресРегион, СтрДлина(ТекРегион)) = нрег(ТекРегион) Тогда
			Возврат СокрЛП(МакетРегионы.Область(Инд, 2, Инд, 2).Текст);
		КонецЕсли;
	КонецЦикла;
	
	Возврат РегламентированнаяОтчетностьВызовСервера.КодРегионаПоНазванию(Адрес.Регион);
	
КонецФункции

Функция ПолучитьВидДокументаПоНаименованию(Знач Наименование) Экспорт
	
	УстановитьПривилегированныйРежим(Истина);
	
	Наименование = Строка(Наименование);
	
	Результат = Неопределено;
	
	Если НЕ ЗначениеЗаполнено(Наименование) Тогда
		
		Результат = ОбщегоНазначения.ПредопределенныйЭлемент(
			"Справочник.ВидыДокументовФизическихЛиц.ПаспортРФ");
		Возврат Результат;
		
	КонецЕсли;
		
	Запрос = Новый Запрос;
	Запрос.Текст = 
		"ВЫБРАТЬ
		|	ВидыДокументовФизическихЛиц.Ссылка КАК Ссылка
		|ИЗ
		|	Справочник.ВидыДокументовФизическихЛиц КАК ВидыДокументовФизическихЛиц
		|ГДЕ
		|	ВидыДокументовФизическихЛиц.Наименование = &Наименование
		|	И ВидыДокументовФизическихЛиц.ПометкаУдаления = Ложь";
	
	Запрос.УстановитьПараметр("Наименование", Наименование);
	
	РезультатЗапроса = Запрос.Выполнить();
	
	ВыборкаДетальныеЗаписи = РезультатЗапроса.Выбрать();
	
	Пока ВыборкаДетальныеЗаписи.Следующий() Цикл
		
		КодВидаДокумента = РегламентированнаяОтчетностьПереопределяемый.ПолучитьКодВидаДокументаФизическогоЛица(ВыборкаДетальныеЗаписи.Ссылка);
		Если ЗначениеЗаполнено(КодВидаДокумента) Тогда
			Результат = ВыборкаДетальныеЗаписи.Ссылка;
			Прервать;
		КонецЕсли;
		
	КонецЦикла;
	
	// Нет видов документа с таким наименованием и заполненным кодом
	Если Результат = Неопределено И ВыборкаДетальныеЗаписи.Количество() > 0 Тогда
		Результат = Справочники.ВидыДокументовФизическихЛиц.НайтиПоНаименованию(Наименование, Ложь);
	КонецЕсли;
	
	Если Результат = Неопределено Тогда
		Возврат Справочники.ВидыДокументовФизическихЛиц.ПустаяСсылка();
	Иначе 	
		Возврат Результат;
	КонецЕсли;

КонецФункции

Функция ПолучитьНомерОсновнойПоставки1с() Экспорт
	
	НомерОсновнойПоставки1с = "";
	
	Возврат НомерОсновнойПоставки1с;
	
КонецФункции

Функция ПолучитьСпецоператоровСвязи()
	
	Запрос = Новый Запрос;
	Массив = Новый Массив();
	
	Запрос.Текст = 
	"ВЫБРАТЬ РАЗРЕШЕННЫЕ
	|	СпецоператорыСвязи.Ссылка
	|ИЗ
	|	Перечисление.СпецоператорыСвязи КАК СпецоператорыСвязи";
	
	Попытка
		Выборка = Запрос.Выполнить().Выбрать();
		Пока Выборка.Следующий() Цикл
			
			СпецоператорыСвязи = Выборка.Ссылка;
			ИмяСпецоператорыСвязи = ЭлектронныйДокументооборотСКонтролирующимиОрганамиВызовСервера.ПолучитьИмяЗначенияПеречисленияСпецоператорыСвязиПоСинониму(СпецоператорыСвязи);
			Массив.Добавить(Новый Структура("Ссылка,Имя", СпецоператорыСвязи, ИмяСпецоператорыСвязи));
		КонецЦикла;
	Исключение
		Массив = Неопределено;
	КонецПопытки;
	
	Возврат Массив;
	
КонецФункции

Функция ПросмотрМакета(ИмяМакета)
	ШаблонМакета = ПолучитьМакетОбработки(ИмяМакета);
	Макет = ШаблонМакета.ПолучитьТекст();
	Возврат Макет;
КонецФункции

Процедура СохранитьДатуНапоминанияДляИзменившихсяРеквизитов(ИзмененныеПараметрыПодключения, ДатаНапоминания) Экспорт
	
	// Получаем из хранилища общих настроек сведения по изменившимся реквизитам
	ДатыНапоминанийПоИзменившимсяРеквизитам = ХранилищеОбщихНастроек.Загрузить("ДокументооборотСКонтролирующимиОрганами_ДатыНапоминанийПоИзменившимсяРеквизитам");
	
	Если ДатыНапоминанийПоИзменившимсяРеквизитам = Неопределено Тогда
		
		ДатыНапоминанийПоИзменившимсяРеквизитам = Новый ТаблицаЗначений;
		ДатыНапоминанийПоИзменившимсяРеквизитам.Колонки.Добавить("Организация");
		ДатыНапоминанийПоИзменившимсяРеквизитам.Колонки.Добавить("УчетнаяЗапись");
		ДатыНапоминанийПоИзменившимсяРеквизитам.Колонки.Добавить("СписокИзменившихсяРеквизитов");
		ДатыНапоминанийПоИзменившимсяРеквизитам.Колонки.Добавить("ДатаНапоминания");
		
	КонецЕсли;
	
	// Если запись в хранилище общих настроек существует, то обновляем дату напоминания, иначе создаем новую запись 
	Для Каждого СтрокаИзмененныхПараметровПодключения Из ИзмененныеПараметрыПодключения Цикл
		Если ЗначениеЗаполнено(СтрокаИзмененныхПараметровПодключения.СписокИзменившихсяРеквизитов) Тогда
			
			// Ищем строки среди запомненных
			Отбор = Новый Структура; 
			Отбор.Вставить("Организация", СтрокаИзмененныхПараметровПодключения.Организация);
			Отбор.Вставить("УчетнаяЗапись", СтрокаИзмененныхПараметровПодключения.УчетнаяЗаписьДокументооборота);
			СтрокиДатыНапоминанияПоИзменившимсяРеквизитам = ДатыНапоминанийПоИзменившимсяРеквизитам.НайтиСтроки(Отбор);
			Если СтрокиДатыНапоминанияПоИзменившимсяРеквизитам.Количество() > 0 Тогда
				СтрокаДатыНапоминанияПоИзменившимсяРеквизитам = СтрокиДатыНапоминанияПоИзменившимсяРеквизитам[0];
				ДатаНапоминанияПоИзмененнымРеквизитам = СтрокаДатыНапоминанияПоИзменившимсяРеквизитам.ДатаНапоминания;
			Иначе
				СтрокаДатыНапоминанияПоИзменившимсяРеквизитам = Неопределено;
			КонецЕсли;

			// Заполнение данных в строке
			Если СтрокаДатыНапоминанияПоИзменившимсяРеквизитам = Неопределено Тогда
				СтрокаДатыНапоминанияПоИзменившимсяРеквизитам = ДатыНапоминанийПоИзменившимсяРеквизитам.Добавить();
				СтрокаДатыНапоминанияПоИзменившимсяРеквизитам.Организация = СтрокаИзмененныхПараметровПодключения.Организация;
				СтрокаДатыНапоминанияПоИзменившимсяРеквизитам.УчетнаяЗапись = СтрокаИзмененныхПараметровПодключения.УчетнаяЗаписьДокументооборота;
			КонецЕсли;
			СтрокаДатыНапоминанияПоИзменившимсяРеквизитам.СписокИзменившихсяРеквизитов = СтрокаИзмененныхПараметровПодключения.СписокИзменившихсяРеквизитов;
			СтрокаДатыНапоминанияПоИзменившимсяРеквизитам.ДатаНапоминания = ДатаНапоминания;
		КонецЕсли;
	КонецЦикла;
	
	// Если сейчас расхождений нет, но раньше были, то удаляем такие строки из запомненых 
	Счетчик = ИзмененныеПараметрыПодключения.Количество() - 1;
	Пока Счетчик >= 0 Цикл
		
		// Ищем строки среди запомненных
		Отбор = Новый Структура; 
		Отбор.Вставить("Организация", ИзмененныеПараметрыПодключения[Счетчик].Организация);
		Отбор.Вставить("УчетнаяЗапись", ИзмененныеПараметрыПодключения[Счетчик].УчетнаяЗаписьДокументооборота);
		СтрокиДатыНапоминанияПоИзменившимсяРеквизитам = ДатыНапоминанийПоИзменившимсяРеквизитам.НайтиСтроки(Отбор);
		Если СтрокиДатыНапоминанияПоИзменившимсяРеквизитам.Количество() > 0 Тогда
			СтрокаДатыНапоминанияПоИзменившимсяРеквизитам = СтрокиДатыНапоминанияПоИзменившимсяРеквизитам[0];
		Иначе
			СтрокаДатыНапоминанияПоИзменившимсяРеквизитам = Неопределено;
		КонецЕсли;
		
		// Удаляем лишние строки
		Если НЕ ЗначениеЗаполнено(ИзмененныеПараметрыПодключения[Счетчик].СписокИзменившихсяРеквизитов) // сейчас нет изменившихся реквизитов 
			И СтрокаДатыНапоминанияПоИзменившимсяРеквизитам <> Неопределено Тогда // раньше были изменившиеся реквизиты
			ДатыНапоминанийПоИзменившимсяРеквизитам.Удалить(СтрокаДатыНапоминанияПоИзменившимсяРеквизитам);
		КонецЕсли;
		Счетчик = Счетчик - 1;
	КонецЦикла;
	
	ХранилищеОбщихНастроек.Сохранить("ДокументооборотСКонтролирующимиОрганами_ДатыНапоминанийПоИзменившимсяРеквизитам", , ДатыНапоминанийПоИзменившимсяРеквизитам);
	
КонецПроцедуры

Процедура СохранитьДатуНапоминанияДляСертификатов(ИзмененныеПараметрыПодключения, ДатаНапоминания) Экспорт
	
	// Получаем из хранилища общих настроек сведения по сертификатам
	ДатыНапоминанийПоИстекающимСертификатам = ХранилищеОбщихНастроек.Загрузить("ДокументооборотСКонтролирующимиОрганами_НапоминаниеПоИстекающимСертификатам");
	Если ДатыНапоминанийПоИстекающимСертификатам = Неопределено Тогда
		ДатыНапоминанийПоИстекающимСертификатам = Новый ТаблицаЗначений;
		ДатыНапоминанийПоИстекающимСертификатам.Колонки.Добавить("Организация");
		ДатыНапоминанийПоИстекающимСертификатам.Колонки.Добавить("Отпечаток");
		ДатыНапоминанийПоИстекающимСертификатам.Колонки.Добавить("ДействителенПо");
		ДатыНапоминанийПоИстекающимСертификатам.Колонки.Добавить("ДатаНапоминания");
	КонецЕсли;
	
	// Если серификат найден в хранилище общих настроек, то обновляем дату напоминания, иначе создаем новую запись 
	Для Каждого ОтпечатокИстекающегоСертификата Из ИзмененныеПараметрыПодключения Цикл
		Если ЗначениеЗаполнено(ОтпечатокИстекающегоСертификата.ОтпечатокСертификата) Тогда
			
			Отбор = Новый Структура();
			Отбор.Вставить("Организация", ОтпечатокИстекающегоСертификата.Организация);
			Отбор.Вставить("Отпечаток", ОтпечатокИстекающегоСертификата.ОтпечатокСертификата);
			СтрокиДатыНапоминанияПоИстекающемуСертификату = ДатыНапоминанийПоИстекающимСертификатам.НайтиСтроки(Отбор);
			
			Если СтрокиДатыНапоминанияПоИстекающемуСертификату.Количество() > 0 Тогда
				СтрокаДатыНапоминанияПоИстекающемуСертификату = СтрокиДатыНапоминанияПоИстекающемуСертификату[0];
				ДатаНапоминанияПоИстекающемуСертификату = СтрокаДатыНапоминанияПоИстекающемуСертификату.ДатаНапоминания;
			Иначе
				СтрокаДатыНапоминанияПоИстекающемуСертификату = Неопределено;
			КонецЕсли;
			
			Если СтрокаДатыНапоминанияПоИстекающемуСертификату = Неопределено Тогда
				СтрокаДатыНапоминанияПоИстекающемуСертификату = ДатыНапоминанийПоИстекающимСертификатам.Добавить();
				СтрокаДатыНапоминанияПоИстекающемуСертификату.Организация = ОтпечатокИстекающегоСертификата.Организация;
				СтрокаДатыНапоминанияПоИстекающемуСертификату.Отпечаток = ОтпечатокИстекающегоСертификата.ОтпечатокСертификата;
			КонецЕсли;
			СтрокаДатыНапоминанияПоИстекающемуСертификату.ДействителенПо = ОтпечатокИстекающегоСертификата.СертификатДействителенПо;
			СтрокаДатыНапоминанияПоИстекающемуСертификату.ДатаНапоминания = ДатаНапоминания;
		КонецЕсли;
	КонецЦикла;
	
	// если сертификат истек более полугода назад, то делаем ему дату напоминания - Дата(1,1,1) - никогда не напоминать 
	ТекДата = ТекущаяДатаСеанса();
	ИндексДатыНапоминания = ДатыНапоминанийПоИстекающимСертификатам.Количество() - 1;
	Пока ИндексДатыНапоминания >= 0 Цикл
		Если ДатыНапоминанийПоИстекающимСертификатам[ИндексДатыНапоминания].ДействителенПо <= ДобавитьМесяц(ТекДата, -6) Тогда
			ДатыНапоминанийПоИстекающимСертификатам[ИндексДатыНапоминания].ДатаНапоминания = Дата(1,1,1);
		КонецЕсли;
		ИндексДатыНапоминания = ИндексДатыНапоминания - 1;
	КонецЦикла;
	
	ХранилищеОбщихНастроек.Сохранить("ДокументооборотСКонтролирующимиОрганами_НапоминаниеПоИстекающимСертификатам", , ДатыНапоминанийПоИстекающимСертификатам);
	
КонецПроцедуры

Процедура СохранитьДатуНапоминанияДляЛицензий(ИзмененныеПараметрыПодключения,ДатаНапоминания) Экспорт
	
	// Получаем из хранилища общих настроек сведения по лицензиям
	ДатыНапоминанийПоИстекающимЛицензиям = ХранилищеОбщихНастроек.Загрузить("ДокументооборотСКонтролирующимиОрганами_ДатыНапоминанийПоИстекающимЛицензиям");
	
	Если ДатыНапоминанийПоИстекающимЛицензиям = Неопределено Тогда
		
		ДатыНапоминанийПоИстекающимЛицензиям = Новый ТаблицаЗначений;
		ДатыНапоминанийПоИстекающимЛицензиям.Колонки.Добавить("Организация");
		ДатыНапоминанийПоИстекающимЛицензиям.Колонки.Добавить("УчетнаяЗапись");
		ДатыНапоминанийПоИстекающимЛицензиям.Колонки.Добавить("ЛицензияНаименование");
		ДатыНапоминанийПоИстекающимЛицензиям.Колонки.Добавить("ЛицензияДатаОкончания");
		ДатыНапоминанийПоИстекающимЛицензиям.Колонки.Добавить("ДатаНапоминания");
		
	КонецЕсли;
	
	// Если лицензия найдена в хранилище общих настроек, то обновляем дату напоминания, иначе создаем новую запись 
	Для Каждого ЛицензияУчетнойЗаписиПользователя Из ИзмененныеПараметрыПодключения Цикл
		
		Если ЗначениеЗаполнено(ЛицензияУчетнойЗаписиПользователя.ЛицензияНаименование) Тогда
			ПараметрыОтбора =  Новый Структура();
			ПараметрыОтбора.Вставить("Организация", ЛицензияУчетнойЗаписиПользователя.Организация);
			ПараметрыОтбора.Вставить("УчетнаяЗапись", ЛицензияУчетнойЗаписиПользователя.УчетнаяЗаписьДокументооборота);
			ПараметрыОтбора.Вставить("ЛицензияНаименование", ЛицензияУчетнойЗаписиПользователя.ЛицензияНаименование);
			ПараметрыОтбора.Вставить("ЛицензияДатаОкончания", ЛицензияУчетнойЗаписиПользователя.ЛицензияДатаОкончания);
			
			СтрокиДатыНапоминанияПоИстекающейЛицензии = ДатыНапоминанийПоИстекающимЛицензиям.НайтиСтроки(ПараметрыОтбора);
			
			Если СтрокиДатыНапоминанияПоИстекающейЛицензии.Количество() = 0 Тогда
				СтрокаДатыНапоминанияПоИстекающейЛицензии = ДатыНапоминанийПоИстекающимЛицензиям.Добавить();
				СтрокаДатыНапоминанияПоИстекающейЛицензии.Организация = ЛицензияУчетнойЗаписиПользователя.Организация;
				СтрокаДатыНапоминанияПоИстекающейЛицензии.УчетнаяЗапись = ЛицензияУчетнойЗаписиПользователя.УчетнаяЗаписьДокументооборота;
				СтрокаДатыНапоминанияПоИстекающейЛицензии.ЛицензияНаименование = ЛицензияУчетнойЗаписиПользователя.ЛицензияНаименование;
				СтрокаДатыНапоминанияПоИстекающейЛицензии.ЛицензияДатаОкончания = ЛицензияУчетнойЗаписиПользователя.ЛицензияДатаОкончания;
			Иначе
				СтрокаДатыНапоминанияПоИстекающейЛицензии = СтрокиДатыНапоминанияПоИстекающейЛицензии[0]; 
			КонецЕсли;
			СтрокаДатыНапоминанияПоИстекающейЛицензии.ДатаНапоминания = ДатаНапоминания;
		КонецЕсли;
			
	КонецЦикла;
	
	// если лицензия истекла более полугода назад, то делаем ей дату напоминания - Дата(1,1,1) - никогда не напоминать 
	ТекДата = ТекущаяДатаСеанса();
	ИндексДатыНапоминания = ДатыНапоминанийПоИстекающимЛицензиям.Количество() - 1;
	Пока ИндексДатыНапоминания >= 0 Цикл
		Если ДатыНапоминанийПоИстекающимЛицензиям[ИндексДатыНапоминания].ЛицензияДатаОкончания <= ДобавитьМесяц(ТекДата, -6) Тогда
			ДатыНапоминанийПоИстекающимЛицензиям[ИндексДатыНапоминания].ДатаНапоминания = Дата(1,1,1);
		КонецЕсли;
		ИндексДатыНапоминания = ИндексДатыНапоминания - 1;
	КонецЦикла;
	
	ХранилищеОбщихНастроек.Сохранить("ДокументооборотСКонтролирующимиОрганами_ДатыНапоминанийПоИстекающимЛицензиям", , ДатыНапоминанийПоИстекающимЛицензиям);
	
КонецПроцедуры

Процедура СохранитьДатуНапоминанияДляНовогоСертификата(ИзмененныеПараметрыПодключения, ДатаНапоминания) Экспорт
	
	// Получаем из хранилища общих настроек сведения по сертификатам
	ДатыНапоминаний = ХранилищеОбщихНастроек.Загрузить("ДокументооборотСКонтролирующимиОрганами_НапоминаниеПоСертификатамИмеющимПереизданные");
	Если ДатыНапоминаний = Неопределено Тогда
		ДатыНапоминаний = Новый ТаблицаЗначений;
		ДатыНапоминаний.Колонки.Добавить("Организация");
		ДатыНапоминаний.Колонки.Добавить("Отпечаток");
		ДатыНапоминаний.Колонки.Добавить("ДатаНапоминания");
	КонецЕсли;
	
	// Если серификат найден в хранилище общих настроек, то обновляем дату напоминания, иначе создаем новую запись 
	Для Каждого ТекущаяОрганизация Из ИзмененныеПараметрыПодключения Цикл
		Если ЗначениеЗаполнено(ТекущаяОрганизация.ОтпечатокНовогоСертификата) Тогда
			
			Отбор = Новый Структура();
			Отбор.Вставить("Организация", ТекущаяОрганизация.Организация);
			Отбор.Вставить("Отпечаток",   ТекущаяОрганизация.ОтпечатокНовогоСертификата);
			НайденныеСтроки = ДатыНапоминаний.НайтиСтроки(Отбор);
			
			Если НайденныеСтроки.Количество() = 0 Тогда
				НовоеНапоминание = ДатыНапоминаний.Добавить();
				НовоеНапоминание.Организация = ТекущаяОрганизация.Организация;
				НовоеНапоминание.Отпечаток   = ТекущаяОрганизация.ОтпечатокСертификата;
			Иначе
				НовоеНапоминание = НайденныеСтроки[0];
			КонецЕсли;
			
			НовоеНапоминание.ДатаНапоминания = ДатаНапоминания;
			
		КонецЕсли;
	КонецЦикла;
	
	ХранилищеОбщихНастроек.Сохранить("ДокументооборотСКонтролирующимиОрганами_НапоминаниеПоСертификатамИмеющимПереизданные", , ДатыНапоминаний);
	
КонецПроцедуры

Процедура СохранитьДатуНапоминанияДляКриптопровайдера(ДатаНапоминания) Экспорт
	
	ХранилищеОбщихНастроек.Сохранить("ДокументооборотСКонтролирующимиОрганами_ДатаНапоминанияОбОбновленииКриптопровайдера",, ДатаНапоминания);
	
КонецПроцедуры

Функция ЭтотПараметрИзменился(ТабличнаяЧасть, НазваниеРеквизита) Экспорт
	
	СтруктураПоиска = Новый Структура("ИзмененныйРеквизит", НазваниеРеквизита);
	Возврат ТабличнаяЧасть.НайтиСтроки(СтруктураПоиска).Количество() > 0 ;
	
КонецФункции

Функция ПоддерживаетсяВторичноеЗаявление(Организация) Экспорт
	
	УчетнаяЗапись = УчетнаяЗаписьОрганизации(Организация);
	Попытка
		Спецоператор = УчетнаяЗапись.СпецоператорСвязи;
	Исключение
		Возврат Ложь;
	КонецПопытки;
	
	ПоддерживаетсяВторичноеЗаявление = Истина;
	
	СпецоператорПоддерживаетВторичныеЗаявления = ЭлектронныйДокументооборотСКонтролирующимиОрганамиВызовСервера.ПолучитьПараметрСпецоператора(Спецоператор, "ПоддерживаетсяОтправкаВторичногоЗаявления") = "Истина";
	
	Если НЕ ЗначениеЗаполнено(УчетнаяЗапись) ИЛИ НЕ ЗначениеЗаполнено(Организация) ИЛИ НЕ СпецоператорПоддерживаетВторичныеЗаявления Тогда
		ПоддерживаетсяВторичноеЗаявление = Ложь;
	КонецЕсли;
	
	Возврат ПоддерживаетсяВторичноеЗаявление;
	
КонецФункции

Функция БылиИзменененыРеквизитыПодключенияК1СОтчетности(ТабличнаяЧасть) Экспорт
	
	Возврат ЭтотПараметрИзменился(ТабличнаяЧасть, Перечисления.ПараметрыПодключенияК1СОтчетности.КраткоеНаименование)
		ИЛИ ЭтотПараметрИзменился(ТабличнаяЧасть, Перечисления.ПараметрыПодключенияК1СОтчетности.ПолноеНаименование)
		ИЛИ ЭтотПараметрИзменился(ТабличнаяЧасть, Перечисления.ПараметрыПодключенияК1СОтчетности.КПП)
		ИЛИ ЭтотПараметрИзменился(ТабличнаяЧасть, Перечисления.ПараметрыПодключенияК1СОтчетности.ОГРН)
		ИЛИ ЭтотПараметрИзменился(ТабличнаяЧасть, Перечисления.ПараметрыПодключенияК1СОтчетности.РегНомерПФР)
		ИЛИ ЭтотПараметрИзменился(ТабличнаяЧасть, Перечисления.ПараметрыПодключенияК1СОтчетности.КодРегионаФСРАР)
		ИЛИ ЭтотПараметрИзменился(ТабличнаяЧасть, Перечисления.ПараметрыПодключенияК1СОтчетности.ЭлектроннаяПочта)
		ИЛИ ЭтотПараметрИзменился(ТабличнаяЧасть, Перечисления.ПараметрыПодключенияК1СОтчетности.ТелефонОсновной)
		ИЛИ ЭтотПараметрИзменился(ТабличнаяЧасть, Перечисления.ПараметрыПодключенияК1СОтчетности.ТелефонДополнительный)
		ИЛИ ЭтотПараметрИзменился(ТабличнаяЧасть, Перечисления.ПараметрыПодключенияК1СОтчетности.НомерОсновнойПоставки1С);
	
КонецФункции
	
Функция БылИзмененСоставКонтролирующихОрганов(ТабличнаяЧасть) Экспорт
	
	СдаватьВФНСИзменился 		= ЭтотПараметрИзменился(ТабличнаяЧасть, Перечисления.ПараметрыПодключенияК1СОтчетности.СдаватьВФНС);
	КодыФНСИзменились 			= ЭтотПараметрИзменился(ТабличнаяЧасть, Перечисления.ПараметрыПодключенияК1СОтчетности.КодыФНС);
	СдаватьВПФРИзменился 		= ЭтотПараметрИзменился(ТабличнаяЧасть, Перечисления.ПараметрыПодключенияК1СОтчетности.СдаватьВПФР);
	КодПФРИзменился 			= ЭтотПараметрИзменился(ТабличнаяЧасть, Перечисления.ПараметрыПодключенияК1СОтчетности.КодПФР); 
	СдаватьВФССИзменился 		= ЭтотПараметрИзменился(ТабличнаяЧасть, Перечисления.ПараметрыПодключенияК1СОтчетности.СдаватьВФСС);
	СдаватьВРосстатИзменился 	= ЭтотПараметрИзменился(ТабличнаяЧасть, Перечисления.ПараметрыПодключенияК1СОтчетности.СдаватьВРосстат);
	КодРосстатаИзменился		= ЭтотПараметрИзменился(ТабличнаяЧасть, Перечисления.ПараметрыПодключенияК1СОтчетности.КодРосстата); 
	СдаватьВФСРАРИзменился 		= ЭтотПараметрИзменился(ТабличнаяЧасть, Перечисления.ПараметрыПодключенияК1СОтчетности.СдаватьВФСРАР);
	СдаватьВРПНИзменился 		= ЭтотПараметрИзменился(ТабличнаяЧасть, Перечисления.ПараметрыПодключенияК1СОтчетности.СдаватьВРПН);
	СдаватьВФТСИзменился 		= ЭтотПараметрИзменился(ТабличнаяЧасть, Перечисления.ПараметрыПодключенияК1СОтчетности.СдаватьВФТС);
	
	Возврат СдаватьВФНСИзменился ИЛИ КодыФНСИзменились ИЛИ СдаватьВПФРИзменился ИЛИ КодПФРИзменился
		ИЛИ СдаватьВФССИзменился ИЛИ СдаватьВРосстатИзменился ИЛИ КодРосстатаИзменился ИЛИ СдаватьВФСРАРИзменился
		ИЛИ СдаватьВРПНИзменился ИЛИ СдаватьВФТСИзменился;
	
КонецФункции

Функция УчетнаяЗаписьОрганизации(Организация) Экспорт
	
	ВидОбменаСКонтролирующимиОрганами = ОбщегоНазначения.ЗначениеРеквизитаОбъекта(Организация, "ВидОбменаСКонтролирующимиОрганами");
	Если ВидОбменаСКонтролирующимиОрганами = Перечисления.ВидыОбменаСКонтролирующимиОрганами.ОбменВУниверсальномФормате Тогда
		Возврат Организация.УчетнаяЗаписьОбмена;
	Иначе
		Возврат Справочники.УчетныеЗаписиДокументооборота.ПустаяСсылка();
	КонецЕсли;
	
КонецФункции

Функция ОрганизацияУчетнойЗаписи(УчетнаяЗапись)

	Если НЕ ЗначениеЗаполнено(УчетнаяЗапись) Тогда
		Возврат Справочники.Организации.ПустаяСсылка();
	КонецЕсли;

	Организации = ОрганизацииУчетнойЗаписи(УчетнаяЗапись);

	Если Организации.Количество() = 0 Тогда
		Возврат Справочники.Организации.ПустаяСсылка();
	Иначе
		Возврат Организации[0];
	КонецЕсли;

КонецФункции

Функция УчетнаяЗаписьПоОбъектуОтправки(ОбъектСсылка) Экспорт
	
	Если ЗначениеЗаполнено(ОбъектСсылка) Тогда
		Организация = ОбщегоНазначения.ЗначениеРеквизитаОбъекта(ОбъектСсылка.Ссылка, "Организация");
		Если ЗначениеЗаполнено(Организация) Тогда
			УчетнаяЗапись = УчетнаяЗаписьОрганизации(Организация);
			Если ЗначениеЗаполнено(УчетнаяЗапись) Тогда
				Возврат УчетнаяЗапись;
			КонецЕсли;
		КонецЕсли;		
	КонецЕсли;
	
	Возврат Неопределено;
	
КонецФункции 

Функция ПолучитьНастройкиУчетнойЗаписиИзИБ(УчетнаяЗапись) Экспорт
	
	Результат = Новый Структура;
	ТипУчетнойЗаписи = ТипЗнч(УчетнаяЗапись);
	Если ТипУчетнойЗаписи = Тип("СправочникСсылка.УчетныеЗаписиДокументооборота") Тогда
	
		Запрос = Новый Запрос("ВЫБРАТЬ РАЗРЕШЕННЫЕ
							  |	УчетныеЗаписиДокументооборота.АдресЭлектроннойПочты,
							  |	УчетныеЗаписиДокументооборота.НалоговыйОрган.Код КАК КодНалоговогоОргана,
							  |	УчетныеЗаписиДокументооборота.НалоговыйОрган.АдресЭлектроннойПочтыДляЦелейДокументооборотаСНалогоплательщиками КАК АдресНалоговогоОргана,
							  |	УчетныеЗаписиДокументооборота.СерверДокументооборота.Сертификат КАК СертификатПочтовогоСервера,
							  |	УчетныеЗаписиДокументооборота.СерверДокументооборота.АдресЭлектроннойПочтыФНС КАК АдресЭлектроннойПочтыПочтовогоСервера,
							  |	УчетныеЗаписиДокументооборота.СерверДокументооборота.АдресЭлектроннойПочтыПФР КАК АдресЭлектроннойПочтыПочтовогоСервераПФР,
							  |	УчетныеЗаписиДокументооборота.СерверДокументооборота.АдресЭлектроннойПочтыФСГС КАК АдресЭлектроннойПочтыПочтовогоСервераФСГС,
							  |	УчетныеЗаписиДокументооборота.СертификатРуководителя,
							  |	УчетныеЗаписиДокументооборота.СертификатГлавногоБухгалтера,
							  |	УчетныеЗаписиДокументооборота.СертификатДляШифрования,
							  |	УчетныеЗаписиДокументооборота.ОбменНапрямую,
							  |	УчетныеЗаписиДокументооборота.СерверДокументооборота КАК СерверДокументооборота,
							  |	УчетныеЗаписиДокументооборота.ИмяПользователяPOP3 КАК ИмяПользователяPOP3,
							  |	УчетныеЗаписиДокументооборота.ПарольPOP3 КАК ПарольPOP3,
							  |	УчетныеЗаписиДокументооборота.СерверPOP3 КАК СерверPOP3,
							  |	УчетныеЗаписиДокументооборота.ПортPOP3 КАК ПортPOP3,
							  |	УчетныеЗаписиДокументооборота.ИмяПользователяSMTP КАК ИмяПользователяSMTP,
							  |	УчетныеЗаписиДокументооборота.ПарольSMTP КАК ПарольSMTP,
							  |	УчетныеЗаписиДокументооборота.СерверSMTP КАК СерверSMTP,
							  |	УчетныеЗаписиДокументооборота.ПортSMTP КАК ПортSMTP,
							  |	УчетныеЗаписиДокументооборота.ТребуетсяSMTPАутентификация КАК ТребуетсяSMTPАутентификация,
							  |	УчетныеЗаписиДокументооборота.РежимАвтонастройки,
							  |	УчетныеЗаписиДокументооборота.ПредназначенаДляДокументооборотаСФНС,
							  |	УчетныеЗаписиДокументооборота.ПредназначенаДляДокументооборотаСПФР,
							  |	УчетныеЗаписиДокументооборота.ПредназначенаДляДокументооборотаСФСГС,
							  |	УчетныеЗаписиДокументооборота.ИдентификаторАбонента,
							  |	УчетныеЗаписиДокументооборота.ИдентификаторСпецоператора,
							  |	УчетныеЗаписиДокументооборота.СпецоператорСвязи,
							  |	УчетныеЗаписиДокументооборота.ЭлектроннаяПодписьВМоделиСервиса,
							  |	УчетныеЗаписиДокументооборота.ТелефонМобильныйДляАвторизации КАК ТелефонМобильныйДляАвторизации,
							  |	УчетныеЗаписиДокументооборота.ИдентификаторДокументооборота,
							  |	УчетныеЗаписиДокументооборота.ОператорРегНомерПФР КАК ОператорРегНомерПФР,
							  |	УчетныеЗаписиДокументооборота.ОператорНаименованиеПолное КАК ОператорНаименованиеПолное,
							  |	УчетныеЗаписиДокументооборота.ОператорНаименованиеКраткое КАК ОператорНаименованиеКраткое,
							  |	УчетныеЗаписиДокументооборота.ОператорИНН КАК ОператорИНН,
							  |	УчетныеЗаписиДокументооборота.ОператорКПП КАК ОператорКПП
							  |ИЗ
							  |	Справочник.УчетныеЗаписиДокументооборота КАК УчетныеЗаписиДокументооборота
							  |ГДЕ
							  |	УчетныеЗаписиДокументооборота.Ссылка = &Ссылка");
		Запрос.УстановитьПараметр("Ссылка", УчетнаяЗапись);
		Выборка = Запрос.Выполнить().Выбрать();
		Если НЕ Выборка.Следующий() Тогда
			Возврат Неопределено;
		КонецЕсли;
		
		Результат.Вставить("АдресЭлектроннойПочты",							Выборка.АдресЭлектроннойПочты);
		Результат.Вставить("КодНалоговогоОргана",							Выборка.КодНалоговогоОргана);
		Результат.Вставить("АдресНалоговогоОргана",							Выборка.АдресНалоговогоОргана);
		Результат.Вставить("СертификатПочтовогоСервера",					Выборка.СертификатПочтовогоСервера);
		Результат.Вставить("АдресЭлектроннойПочтыПочтовогоСервера",			Выборка.АдресЭлектроннойПочтыПочтовогоСервера);
		Результат.Вставить("АдресЭлектроннойПочтыПочтовогоСервераПФР",		Выборка.АдресЭлектроннойПочтыПочтовогоСервераПФР);
		Результат.Вставить("АдресЭлектроннойПочтыПочтовогоСервераФСГС",		Выборка.АдресЭлектроннойПочтыПочтовогоСервераФСГС);
		Результат.Вставить("СертификатРуководителя",						Выборка.СертификатРуководителя);
		Результат.Вставить("СертификатГлавногоБухгалтера",					Выборка.СертификатГлавногоБухгалтера);
		Результат.Вставить("СертификатДляШифрования",						Выборка.СертификатДляШифрования);
		Результат.Вставить("ОбменНапрямую",									Выборка.ОбменНапрямую);
		Результат.Вставить("СерверДокументооборота",						Выборка.СерверДокументооборота);
		Результат.Вставить("ИмяПользователяPOP3",							Выборка.ИмяПользователяPOP3);
		Результат.Вставить("ПарольPOP3",									Выборка.ПарольPOP3);
		Результат.Вставить("СерверPOP3",									Выборка.СерверPOP3);
		Результат.Вставить("ПортPOP3",										Выборка.ПортPOP3);
		Результат.Вставить("ИмяПользователяSMTP",							Выборка.ИмяПользователяSMTP);
		Результат.Вставить("ПарольSMTP",									Выборка.ПарольSMTP);
		Результат.Вставить("СерверSMTP",									Выборка.СерверSMTP);
		Результат.Вставить("ПортSMTP",										Выборка.ПортSMTP);
		Результат.Вставить("ТребуетсяSMTPАутентификация",					Выборка.ТребуетсяSMTPАутентификация);
		Результат.Вставить("РежимАвтонастройки",							Выборка.РежимАвтонастройки);
		Результат.Вставить("ПредназначенаДляДокументооборотаСФНС",			Выборка.ПредназначенаДляДокументооборотаСФНС);
		Результат.Вставить("ПредназначенаДляДокументооборотаСПФР",			Выборка.ПредназначенаДляДокументооборотаСПФР);
		Результат.Вставить("ПредназначенаДляДокументооборотаСФСГС",			Выборка.ПредназначенаДляДокументооборотаСФСГС);
		Результат.Вставить("ИдентификаторАбонента",							Выборка.ИдентификаторАбонента);
		Результат.Вставить("ИдентификаторСпецоператора",					Выборка.ИдентификаторСпецоператора);
		Результат.Вставить("СпецоператорСвязи",								Выборка.СпецоператорСвязи);
		Результат.Вставить("ЭлектроннаяПодписьВМоделиСервиса", 				Выборка.ЭлектроннаяПодписьВМоделиСервиса);
		Результат.Вставить("ТелефонМобильныйДляАвторизации", 				Выборка.ТелефонМобильныйДляАвторизации);
		Результат.Вставить("ИдентификаторДокументооборота", 				Выборка.ИдентификаторДокументооборота);
		Результат.Вставить("ОператорРегНомерПФР", 							Выборка.ОператорРегНомерПФР);
		Результат.Вставить("ОператорНаименованиеПолное", 					Выборка.ОператорНаименованиеПолное);
		Результат.Вставить("ОператорНаименованиеКраткое", 					Выборка.ОператорНаименованиеКраткое);
		Результат.Вставить("ОператорИНН", 									Выборка.ОператорИНН);
		Результат.Вставить("ОператорКПП", 									Выборка.ОператорКПП);
		
	ИначеЕсли ТипУчетнойЗаписи = Тип("СправочникОбъект.УчетныеЗаписиДокументооборота") Тогда
		
		Результат.Вставить("АдресЭлектроннойПочты",							УчетнаяЗапись.АдресЭлектроннойПочты);
		Результат.Вставить("КодНалоговогоОргана",							УчетнаяЗапись.НалоговыйОрган.Код);
		Результат.Вставить("АдресНалоговогоОргана",							УчетнаяЗапись.НалоговыйОрган.АдресЭлектроннойПочтыДляЦелейДокументооборотаСНалогоплательщиками);
		Результат.Вставить("СертификатПочтовогоСервера",					УчетнаяЗапись.СерверДокументооборота.Сертификат);
		Результат.Вставить("АдресЭлектроннойПочтыПочтовогоСервера",			УчетнаяЗапись.СерверДокументооборота.АдресЭлектроннойПочтыФНС);
		Результат.Вставить("АдресЭлектроннойПочтыПочтовогоСервераПФР",		УчетнаяЗапись.СерверДокументооборота.АдресЭлектроннойПочтыПФР);
		Результат.Вставить("АдресЭлектроннойПочтыПочтовогоСервераФСГС",		УчетнаяЗапись.СерверДокументооборота.АдресЭлектроннойПочтыФСГС);
		Результат.Вставить("СертификатРуководителя",						УчетнаяЗапись.СертификатРуководителя);
		Результат.Вставить("СертификатГлавногоБухгалтера",					УчетнаяЗапись.СертификатГлавногоБухгалтера);
		Результат.Вставить("СертификатДляШифрования",						УчетнаяЗапись.СертификатДляШифрования);
		Результат.Вставить("ОбменНапрямую",									УчетнаяЗапись.ОбменНапрямую);
		Результат.Вставить("СерверДокументооборота",						УчетнаяЗапись.СерверДокументооборота);
		Результат.Вставить("ИмяПользователяPOP3",							УчетнаяЗапись.ИмяПользователяPOP3);
		Результат.Вставить("ПарольPOP3",									УчетнаяЗапись.ПарольPOP3);
		Результат.Вставить("СерверPOP3",									УчетнаяЗапись.СерверPOP3);
		Результат.Вставить("ПортPOP3",										УчетнаяЗапись.ПортPOP3);
		Результат.Вставить("ИмяПользователяSMTP",							УчетнаяЗапись.ИмяПользователяSMTP);
		Результат.Вставить("ПарольSMTP",									УчетнаяЗапись.ПарольSMTP);
		Результат.Вставить("СерверSMTP",									УчетнаяЗапись.СерверSMTP);
		Результат.Вставить("ПортSMTP",										УчетнаяЗапись.ПортSMTP);
		Результат.Вставить("ТребуетсяSMTPАутентификация",					УчетнаяЗапись.ТребуетсяSMTPАутентификация);
		Результат.Вставить("РежимАвтонастройки",							УчетнаяЗапись.РежимАвтонастройки);
		Результат.Вставить("ПредназначенаДляДокументооборотаСФНС",			УчетнаяЗапись.ПредназначенаДляДокументооборотаСФНС);
		Результат.Вставить("ПредназначенаДляДокументооборотаСПФР",			УчетнаяЗапись.ПредназначенаДляДокументооборотаСПФР);
		Результат.Вставить("ПредназначенаДляДокументооборотаСФСГС",			УчетнаяЗапись.ПредназначенаДляДокументооборотаСФСГС);
		Результат.Вставить("ИдентификаторАбонента",							УчетнаяЗапись.ИдентификаторАбонента);
		Результат.Вставить("ИдентификаторСпецоператора",					УчетнаяЗапись.ИдентификаторСпецоператора);
		Результат.Вставить("СпецоператорСвязи",								УчетнаяЗапись.СпецоператорСвязи);
		Результат.Вставить("ЭлектроннаяПодписьВМоделиСервиса", 				УчетнаяЗапись.ЭлектроннаяПодписьВМоделиСервиса);
		Результат.Вставить("ТелефонМобильныйДляАвторизации", 				УчетнаяЗапись.ТелефонМобильныйДляАвторизации);
		Результат.Вставить("ИдентификаторДокументооборота", 				УчетнаяЗапись.ИдентификаторДокументооборота);
		Результат.Вставить("ОператорРегНомерПФР", 							УчетнаяЗапись.ОператорРегНомерПФР);
		Результат.Вставить("ОператорНаименованиеПолное", 					УчетнаяЗапись.ОператорНаименованиеПолное);
		Результат.Вставить("ОператорНаименованиеКраткое", 					УчетнаяЗапись.ОператорНаименованиеКраткое);
		Результат.Вставить("ОператорИНН", 									УчетнаяЗапись.ОператорИНН);
		Результат.Вставить("ОператорКПП", 									УчетнаяЗапись.ОператорКПП);

		
	Иначе
		
		Результат.Вставить("АдресЭлектроннойПочты",							"");
		Результат.Вставить("КодНалоговогоОргана",							"");
		Результат.Вставить("АдресНалоговогоОргана",							"");
		Результат.Вставить("СертификатПочтовогоСервера",					"");
		Результат.Вставить("АдресЭлектроннойПочтыПочтовогоСервера",			"");
		Результат.Вставить("АдресЭлектроннойПочтыПочтовогоСервераПФР",		"");
		Результат.Вставить("АдресЭлектроннойПочтыПочтовогоСервераФСГС",		"");
		Результат.Вставить("СертификатРуководителя",						"");
		Результат.Вставить("СертификатГлавногоБухгалтера",					"");
		Результат.Вставить("СертификатДляШифрования",						"");
		Результат.Вставить("ОбменНапрямую",									Ложь);
		Результат.Вставить("СерверДокументооборота",						"");
		Результат.Вставить("ИмяПользователяPOP3",							"");
		Результат.Вставить("ПарольPOP3",									"");
		Результат.Вставить("СерверPOP3",									"");
		Результат.Вставить("ПортPOP3",										0);
		Результат.Вставить("ИмяПользователяSMTP",							"");
		Результат.Вставить("ПарольSMTP",									"");
		Результат.Вставить("СерверSMTP",									"");
		Результат.Вставить("ПортSMTP",										0);
		Результат.Вставить("ТребуетсяSMTPАутентификация",					Ложь);
		Результат.Вставить("РежимАвтонастройки",							Перечисления.РежимыАвтонастройкиУчетнойЗаписиНалогоплательщика.Отключена);
		Результат.Вставить("ПредназначенаДляДокументооборотаСФНС",			Ложь);
		Результат.Вставить("ПредназначенаДляДокументооборотаСПФР",			Ложь);
		Результат.Вставить("ПредназначенаДляДокументооборотаСФСГС",			Ложь);
		Результат.Вставить("ИдентификаторАбонента",							"");
		Результат.Вставить("ИдентификаторСпецоператора",					"");
		Результат.Вставить("СпецоператорСвязи",								"");
		Результат.Вставить("ЭлектроннаяПодписьВМоделиСервиса",                     Ложь);
		Результат.Вставить("ТелефонМобильныйДляАвторизации",                "");
		Результат.Вставить("ИдентификаторДокументооборота",                 "");
		Результат.Вставить("ОператорРегНомерПФР", 							"");
		Результат.Вставить("ОператорНаименованиеПолное", 					"");
		Результат.Вставить("ОператорНаименованиеКраткое", 					"");
		Результат.Вставить("ОператорИНН", 									"");
		Результат.Вставить("ОператорКПП", 									"");

	КонецЕсли;
	
	Возврат Результат;
	
КонецФункции

Функция ПодключенДокументооборотСКонтролирующимОрганом(Организация, КонтролирующийОрган = Неопределено) Экспорт
	
	ДокументооборотПодключен = Ложь;
	
	Если КонтролирующийОрган = Перечисления.ТипыКонтролирующихОрганов.ФНС Тогда
		
		ДокументооборотПодключен = ОрганизацияИспользуетОбменСФНС(Организация);

	ИначеЕсли КонтролирующийОрган = Перечисления.ТипыКонтролирующихОрганов.ПФР Тогда
		
		ДокументооборотПодключен = ОрганизацияИспользуетОбменСПФР(Организация);
		
	ИначеЕсли КонтролирующийОрган = Перечисления.ТипыКонтролирующихОрганов.ФСГС Тогда
		
		ДокументооборотПодключен = ОрганизацияИспользуетОбменСФСГС(Организация);
		
	ИначеЕсли КонтролирующийОрган = Перечисления.ТипыКонтролирующихОрганов.ФСС Тогда
		
		ДокументооборотПодключен = ОрганизацияИспользуетОбменСФСС(Организация);
		
	ИначеЕсли КонтролирующийОрган = Перечисления.ТипыКонтролирующихОрганов.ФСРАР Тогда
		
		ДокументооборотПодключен = ОрганизацияИспользуетОбменСФСРАР(Организация);

	ИначеЕсли КонтролирующийОрган = Перечисления.ТипыКонтролирующихОрганов.РПН Тогда
		
		ДокументооборотПодключен = ОрганизацияИспользуетОбменСРПН(Организация);
		
	ИначеЕсли КонтролирующийОрган = Перечисления.ТипыКонтролирующихОрганов.ФТС Тогда
		
		ДокументооборотПодключен = ОрганизацияИспользуетОбменСФТС(Организация);
		
	ИначеЕсли КонтролирующийОрган = Перечисления.ТипыКонтролирующихОрганов.БанкРоссии Тогда
		
		ДокументооборотПодключен = ОрганизацияИспользуетОбменСБанкомРоссии(Организация);
		
	ИначеЕсли КонтролирующийОрган = Перечисления.ТипыКонтролирующихОрганов.Минобороны Тогда
		
		ДокументооборотПодключен = ОрганизацияИспользуетОбменСМинобороны(Организация);
		
	ИначеЕсли КонтролирующийОрган = Неопределено Тогда
		
		ДокументооборотПодключен = 
			ОрганизацияИспользуетОбменСФНС(Организация) = Истина
			ИЛИ ОрганизацияИспользуетОбменСПФР(Организация) = Истина
			ИЛИ ОрганизацияИспользуетОбменСФСГС(Организация) = Истина
			ИЛИ ОрганизацияИспользуетОбменСФСС(Организация) = Истина
			ИЛИ ОрганизацияИспользуетОбменСФСРАР(Организация) = Истина
			ИЛИ ОрганизацияИспользуетОбменСРПН(Организация) = Истина
			ИЛИ ОрганизацияИспользуетОбменСФТС(Организация) = Истина
			ИЛИ ОрганизацияИспользуетОбменСБанкомРоссии(Организация) = Истина;
		
	КонецЕсли;
	
	Возврат ДокументооборотПодключен = Истина; // может вернуться Неопределено
	
КонецФункции

Функция ПараметрыПредупрежденияОСменеРеквизитовПодключенияК1СОтчетности(ТекущаяДатаСервер) Экспорт
	
	ТекущаяДатаСервер = ТекущаяДатаСеанса();
	СписокПараметровПредупреждения = Новый СписокЗначений;
	
	ПараметрыОтбора = Новый Структура();
	ПараметрыОтбора.Вставить("КлючОбъекта","ПараметрыПредупрежденияОСменеРеквизитовПодключенияК1СОтчетности");
	ПараметрыОтбора.Вставить("Пользователь",ИмяПользователя());
	
	ВыборкаНастроек = ХранилищеОбщихНастроек.Выбрать(ПараметрыОтбора);
	
	Пока ВыборкаНастроек.Следующий() Цикл
		СписокПараметровПредупреждения.Добавить(ВыборкаНастроек.Настройки, ВыборкаНастроек.КлючНастроек)
	КонецЦикла;
	
	Возврат СписокПараметровПредупреждения;
	
КонецФункции


Функция ПодключенныеОрганизации(ОтборОрганизации = Неопределено) Экспорт
	
	ТипОтбораОрганизации = ТипЗнч(ОтборОрганизации);
	Запрос = Новый Запрос("ВЫБРАТЬ РАЗРЕШЕННЫЕ
						  |	Организации.Ссылка КАК Ссылка
						  |ИЗ
						  |	Справочник.Организации КАК Организации
						  |ГДЕ
						  |	Организации.ВидОбменаСКонтролирующимиОрганами = &ВидОбменаСКонтролирующимиОрганами
						  |	И Организации.УчетнаяЗаписьОбмена <> &ПустаяУчетнаяЗаписьОбмена"
						  + ?(ОтборОрганизации <> Неопределено, ?(ТипОтбораОрганизации <> Тип("Массив"), "
						  |	И Организации.Ссылка = &ОтборОрганизации", "
						  |	И Организации.Ссылка В (&ОтборОрганизации)"), "") + "
						  |	И НЕ Организации.ПометкаУдаления
						  |	И НЕ Организации.УчетнаяЗаписьОбмена.ПометкаУдаления");
	Запрос.УстановитьПараметр("ВидОбменаСКонтролирующимиОрганами", Перечисления.ВидыОбменаСКонтролирующимиОрганами.ОбменВУниверсальномФормате);
	Запрос.УстановитьПараметр("ПустаяУчетнаяЗаписьОбмена", Справочники.УчетныеЗаписиДокументооборота.ПустаяСсылка());
	Запрос.УстановитьПараметр("ОтборОрганизации", ОтборОрганизации);
	
	Попытка
		ТаблицаЗапроса = Запрос.Выполнить().Выгрузить();
	Исключение
		Возврат Неопределено;
	КонецПопытки;
	
	Если ОтборОрганизации <> Неопределено И ТипОтбораОрганизации <> Тип("Массив") Тогда
		Результат = (ТаблицаЗапроса.Количество() > 0);
		
	Иначе
		Результат = Новый Массив;
		Для Каждого СтрокаЗапроса Из ТаблицаЗапроса Цикл
			Результат.Добавить(СтрокаЗапроса.Ссылка);
		КонецЦикла;
	КонецЕсли;
	
	Возврат Результат;
	
КонецФункции

Функция СерверПриемаОтчетностиФСС(
		ТипОтправляемогоДокумента,
		ВидОтправляемогоДокумента = Неопределено,
		Организация = Неопределено) Экспорт
	
	Если ТипОтправляемогоДокумента = Перечисления.ТипыОтправляемыхДокументов.ДанныеЭЛНФСС
		И ЗначениеЗаполнено(Организация) Тогда
		
		ИспользуетсяТестовыйСерверФСС = ИспользоватьТестовыйСерверФССЭЛН(Организация);
	Иначе
		ИспользуетсяТестовыйСерверФСС = ДокументооборотСКОВызовСервера.ИспользуетсяРежимТестирования();
	КонецЕсли;
	
	Если ТипОтправляемогоДокумента = Перечисления.ТипыОтправляемыхДокументов.Отчет4ФСС Тогда
		
		Если ИспользуетсяТестовыйСерверФСС Тогда
			// Возвращаем адрес тестового сервера
			Возврат ДокументооборотСКО.ПолучитьЗаменяемоеЗначениеТестовогоСервера1С("forma4.fss.ru");
			
		Иначе
			// Возвращаем адрес реального сервера
			Возврат ДокументооборотСКО.ПолучитьЗаменяемоеЗначениеТестовогоСервера1С("f4.fss.ru");
			
		КонецЕсли;
		
	ИначеЕсли ТипОтправляемогоДокумента = Перечисления.ТипыОтправляемыхДокументов.Отчет4аФСС
		ИЛИ ТипОтправляемогоДокумента = Перечисления.ТипыОтправляемыхДокументов.РеестрСведенийФСС
		ИЛИ ТипОтправляемогоДокумента = Перечисления.ТипыОтправляемыхДокументов.ПрочаяОтчетностьФСС
		И (ВидОтправляемогоДокумента = Справочники.ВидыОтправляемыхДокументов.ПодтверждениеВидаДеятельности
		ИЛИ ВидОтправляемогоДокумента =
		Справочники.ВидыОтправляемыхДокументов.РеестрСтимулирующихВыплатМедицинскимИСоциальнымРаботникам)
		ИЛИ ТипОтправляемогоДокумента = Перечисления.ТипыОтправляемыхДокументов.ДанныеЭЛНФСС Тогда
		
		Если ИспользуетсяТестовыйСерверФСС Тогда
			// Возвращаем адрес тестового сервера
			Возврат ДокументооборотСКО.ПолучитьЗаменяемоеЗначениеТестовогоСервера1С("docs-test.fss.ru");
			
		Иначе
			// Возвращаем адрес реального сервера
			Возврат ДокументооборотСКО.ПолучитьЗаменяемоеЗначениеТестовогоСервера1С("docs.fss.ru");
			
		КонецЕсли;
	
	Иначе
		
		Возврат Неопределено;
		
	КонецЕсли;
	
КонецФункции

Функция СформироватьИмяФайла(ИсходноеИмяФайла) Экспорт
	
	ИмяФайла = СтрЗаменить(ИсходноеИмяФайла, "/", "_");
	ИмяФайла = СтрЗаменить(ИмяФайла, "\", "_");
	ИмяФайла = СтрЗаменить(ИмяФайла, ":", "_");
	ИмяФайла = СтрЗаменить(ИмяФайла, "*", "_");
	ИмяФайла = СтрЗаменить(ИмяФайла, "?", "_");
	ИмяФайла = СтрЗаменить(ИмяФайла, """","_");
	ИмяФайла = СтрЗаменить(ИмяФайла, "<", "_");
	ИмяФайла = СтрЗаменить(ИмяФайла, ">", "_");
	Возврат СтрЗаменить(ИмяФайла, "|", "_");
	
КонецФункции

Функция РазницаДатВМесяцах(Дата1, Дата2) Экспорт
	
	ДатаНач	= ?(Дата1 < Дата2, Дата1, Дата2);
	ДатаКон	= ?(Дата1 < Дата2, Дата2, Дата1);
		
	Годы    = Год( датаКон ) - Год( датаНач );
	Месяцы  = Месяц( датаКон ) - Месяц( датаНач );

	Разность = месяцы + годы * 12;

	Возврат Разность;

КонецФункции

Процедура ПриЗаписиЭлектронногоПредставленияРегламентированногоОтчета(Объект, Отказ)
	
	Возврат;
КонецПроцедуры

// есть в Контейнере
Функция ОпределитьОрганПФРОрганизации(Организация, КодОрганаПФР = Неопределено, РегНомерПФР = Неопределено) Экспорт
     
	ПараметрыОтправкиВПФР = СформироватьПараметрыОтправкиВПФР(Организация);
	КодОрганаПФР = СокрЛП(ПараметрыОтправкиВПФР.КодОрганаПФР);
	РегНомерПФР = СокрЛП(ПараметрыОтправкиВПФР.РегНомПФР);
	Если НЕ ЗначениеЗаполнено(КодОрганаПФР) Тогда
		Возврат Неопределено;
	Иначе
		Возврат Справочники.ОрганыПФР.НайтиПоКоду(КодОрганаПФР);
	КонецЕсли;
     
 КонецФункции

///////////////////////////////////////////////////////////////////////////////
// Функции с использованием общих модулей и дублирующие функции для организации
// вызовов общих модулей при документообороте с РПН

Функция СформироватьФайлОтправкиВРПНССервера(КороткоеИмяФайлаПакета, СтрокаBase64ФайлаПакета, ЭтоАдреса = Истина, Адрес = Неопределено) Экспорт
	
	Результат = Неопределено;
	
	//файл отправки получаем, объединяя файл пакета с файлами начала и окончания POST-запроса
	
	// файл 1
	СодержимоеФайла = "--My1cV8bNdr"
	+ Символы.ВК + Символы.ПС + "Content-Disposition: form-data; name=""fileUpload""; filename=""" + КороткоеИмяФайлаПакета + """"
	+ Символы.ВК + Символы.ПС + "Content-Type: application/octet-stream"
	+ Символы.ВК + Символы.ПС
	+ Символы.ВК + Символы.ПС;
	
	ИмяФайлаЗапросаНачало = ПолучитьИмяВременногоФайла();
	ТекстовыйФайл = Новый ТекстовыйДокумент;
	ТекстовыйФайл.УстановитьТекст(СодержимоеФайла);
	ТекстовыйФайл.Записать(ИмяФайлаЗапросаНачало, "utf-8", "");
	
	// файл 2
	ИмяФайлаПакета = ПолучитьИмяВременногоФайла();
	
	Если ЭтоАдреса Тогда
		ПолучитьИзВременногоХранилища(СтрокаBase64ФайлаПакета).Записать(ИмяФайлаПакета);
	Иначе
		ДвоичныеДанныеФайлаПакета = Base64Значение(СтрокаBase64ФайлаПакета);
		ДвоичныеДанныеФайлаПакета.Записать(ИмяФайлаПакета);
	КонецЕсли;
	
	// файл 3
	СодержимоеФайла = Символы.ВК + Символы.ПС + "--My1cV8bNdr--";
	
	ИмяФайлаЗапросаОкончание = ПолучитьИмяВременногоФайла();
	ТекстовыйФайл = Новый ТекстовыйДокумент;
	ТекстовыйФайл.УстановитьТекст(СодержимоеФайла);
	ТекстовыйФайл.Записать(ИмяФайлаЗапросаОкончание, "windows-1251", "");
	
	// объединение
	МассивИмен = Новый Массив;
	МассивИмен.Добавить(ИмяФайлаЗапросаНачало);
	МассивИмен.Добавить(ИмяФайлаПакета);
	МассивИмен.Добавить(ИмяФайлаЗапросаОкончание);
	ИмяФайлаОтправки = ПолучитьИмяВременногоФайла();
	ОбъединитьФайлы(МассивИмен, ИмяФайлаОтправки);
	
	ДвоичныеДанныеФайлаПакета = Новый ДвоичныеДанные(ИмяФайлаОтправки);
	Результат = Base64Строка(ДвоичныеДанныеФайлаПакета);
	Результат = Сред(Результат, 5);// удалим из файла в кодировке "UTF-8" сигнатуру BOM
	
	Если ЭтоАдреса Тогда
		ДвоичныеДанныеФайлаПакетаБезСигнатуны = Base64Значение(Результат);
		Результат = ПоместитьВоВременноеХранилище(ДвоичныеДанныеФайлаПакетаБезСигнатуны, Адрес);
	КонецЕсли;
	
	Возврат Результат;
	
КонецФункции

//////////////////////////////////////////////////////////////////////////////////
//// ДОКУМЕНТООБОРОТ С РПН, код без использования вызовов общих модулей
////

Функция ОбновитьРезультатКонкретнойОтправкиРПНССервера(Знач ОтправкаСсылка, Знач ОтправкаОбъектЗагружен, Знач ПараметрыОтправки, ПараметрыСоединения, Знач ЭтоВызовИзСписка = Ложь) Экспорт
	
	Если ОтправкаОбъектЗагружен Тогда
		ОсновныеРеквизитыОтправки = Новый Структура("ОтчетСсылка, ИмяФайлаПакета, ИдентификаторОтправкиНаСервере, СтатусОтправки, СтатусПроверкиНаПортале, ИдентификаторОтправкиОператору",
			ОтправкаСсылка.ОтчетСсылка, ОтправкаСсылка.ИмяФайлаПакета, ОтправкаСсылка.ИдентификаторОтправкиНаСервере, ОтправкаСсылка.СтатусОтправки, ОтправкаСсылка.СтатусПроверкиНаПортале, ОтправкаСсылка.ИдентификаторОтправкиОператору);
	Иначе
		ОсновныеРеквизитыОтправки = ЗначенияРеквизитовОбъекта(ОтправкаСсылка, "ОтчетСсылка, ИмяФайлаПакета, ИдентификаторОтправкиНаСервере, СтатусОтправки, СтатусПроверкиНаПортале, ИдентификаторОтправкиОператору");
	КонецЕсли;
	Если НЕ ЗначениеЗаполнено(ОсновныеРеквизитыОтправки.ИдентификаторОтправкиОператору) Тогда
		ОсновныеРеквизитыОтправки.ИдентификаторОтправкиОператору = 0;
	КонецЕсли;
	
	Если НЕ ЗначениеЗаполнено(ПараметрыСоединения.ИдентификаторОтправкиНаСервере) Тогда
		ПараметрыСоединения.ИдентификаторОтправкиНаСервере = ОсновныеРеквизитыОтправки.ИдентификаторОтправкиНаСервере;
	КонецЕсли;
	Если ПараметрыСоединения.ИдентификаторОтправкиОператору = Неопределено Тогда
		ПараметрыСоединения.ИдентификаторОтправкиОператору = ОсновныеРеквизитыОтправки.ИдентификаторОтправкиОператору;
	КонецЕсли;
	
	РезультатОтправки = ПолучитьРезультатОтправкиРПН(ОсновныеРеквизитыОтправки, ПараметрыОтправки, ПараметрыСоединения, ЭтоВызовИзСписка, ОтправкаСсылка);
	
	ПараметрыСоединения.СоединениеHTTP = Неопределено;
	
	СохранитьРезультатОтправкиРПН(ОтправкаСсылка, РезультатОтправки);
	
	Возврат (РезультатОтправки <> Неопределено);
	
КонецФункции

Функция СохранитьРезультатОтправкиРПН(ОтправкаСсылка, РезультатОтправки)
	
	Если РезультатОтправки <> Неопределено Тогда
		ОтправкаОбъект = ОтправкаСсылка.ПолучитьОбъект();
		ОтправкаОбъект.ДатаПолученияРезультата = РезультатОтправки.ДатаПолученияРезультата;
		ОтправкаОбъект.ИдентификаторОтправкиНаСервере = РезультатОтправки.ИдентификаторОтправкиНаСервере;
		ОтправкаОбъект.СтатусОтправки = РезультатОтправки.СтатусОтправки;
		ОтправкаОбъект.СтатусПроверкиНаПортале = РезультатОтправки.СтатусПроверкиНаПортале;
		ОтправкаОбъект.Протокол = Новый ХранилищеЗначения(РезультатОтправки.Протокол);
		ОтправкаОбъект.Записать();
		Возврат Истина;
	Иначе
		Возврат Ложь;
	КонецЕсли;
	
КонецФункции

Функция ПолучитьРезультатОтправкиРПН(ОсновныеРеквизитыОтправки, ПараметрыОтправки, ПараметрыСоединения, ЭтоВызовИзСписка, ОтправкаСсылка)
	
	СтатусОтчета = "";
	ЦветСтатуса = Новый Цвет(0, 0, 192);
	СтатусОтправки = Перечисления.СтатусыОтправки.Отправлен;
	
	//запрос результатов обработки
	ПараметрыСоединения.ОписаниеОшибкиСервером = "";
	ПараметрыСоединения.ПовторятьСоединение = Ложь;
	ПараметрыСоединения.ПовторятьЗапросПароля = Ложь;
	
	ЗаголовокОтчета = ПолучитьИнформациюОбОтчетеНаПорталеРПН(ОсновныеРеквизитыОтправки.ИмяФайлаПакета, ПараметрыОтправки, ПараметрыСоединения);
	
	УспешноОбработан = Неопределено;
	ПройденаПроверкаНаПортале = Неопределено;
	Если ЗаголовокОтчета <> Неопределено Тогда
		УспешноОбработан = УспешноОбработанОтчетРПН(ЗаголовокОтчета.ИдентификаторЗаписи, ЗаголовокОтчета.Состояние, ПройденаПроверкаНаПортале);
	КонецЕсли;
	
	Если ЗаголовокОтчета <> Неопределено И НЕ ПараметрыСоединения.ПовторятьСоединение Тогда
		
		// получен протокол обработки и отсутствовали ошибки соединения
		
		// анализируем содержимое страницы протокола обработки
		Если УспешноОбработан = Истина Тогда
			// отчет принят
			СтатусОтправки = Перечисления.СтатусыОтправки.Сдан;
			СтатусПроверкиНаПортале = Перечисления.СтатусыОтправки.Сдан;
			СтатусОтчета = НСтр("ru = 'Отчет принят, ошибок не обнаружено.'");
			ЦветСтатуса = Новый Цвет(0, 179, 16);
			
		ИначеЕсли УспешноОбработан = Ложь Тогда
			// отчет не принят
			СтатусОтправки = Перечисления.СтатусыОтправки.НеПринят;
			Если ПройденаПроверкаНаПортале = Истина Тогда
				СтатусПроверкиНаПортале = Перечисления.СтатусыОтправки.Сдан;
				СтатусОтчета = НСтр("ru = 'Отчет не принят, обнаружены ошибки при проверке в управлении.'");
			Иначе
				СтатусПроверкиНаПортале = Перечисления.СтатусыОтправки.НеПринят;
				СтатусОтчета = НСтр("ru = 'Отчет не принят, обнаружены ошибки.'");
			КонецЕсли;
			ЦветСтатуса = Новый Цвет(255, 0, 0);
			
		Иначе
			// отчет еще не обработан
			Если ПройденаПроверкаНаПортале = Истина Тогда
				СтатусПроверкиНаПортале = Перечисления.СтатусыОтправки.Сдан;
			КонецЕсли;
			СтатусОтчета = НСтр("ru = 'Находится в обработке, попробуйте через некоторое время использовать в отчете кнопку ""Обновить""'");
		КонецЕсли;
		
		Протокол = ГенерироватьHTMLПротоколаОбработкиРПН(ЗаголовокОтчета, СтатусОтчета, ЦветСтатуса, СтатусОтправки, ПараметрыОтправки, ОсновныеРеквизитыОтправки.ОтчетСсылка, ПараметрыСоединения.ИдентификаторОтправкиОператору <> 0);
		
		//заполняем результирующую структуру
		Результат = Новый Структура("ДатаПолученияРезультата, ИдентификаторОтправкиНаСервере, СтатусОтправки, СтатусПроверкиНаПортале, Протокол",
			ТекущаяДатаСеанса(), ЗаголовокОтчета.ИдентификаторЗаписи, СтатусОтправки, СтатусПроверкиНаПортале, Протокол);
		
	ИначеЕсли ЗначениеЗаполнено(ПараметрыСоединения.ОписаниеОшибкиСервером) ИЛИ НЕ ПараметрыСоединения.ПовторятьСоединение Тогда
		
		Если ЗначениеЗаполнено(ПараметрыСоединения.ОписаниеОшибкиСервером) Тогда
			ТекстСообщения = СтрШаблон(
				НСтр("ru = 'Результаты отправки не получены с портала. %1'"),
				ПараметрыСоединения.ОписаниеОшибкиСервером);
			
			Если ЭтоВызовИзСписка Тогда
				ПредставлениеОтчетаОтправки = ПредставлениеОтчета(ОтправкаСсылка.ОтчетСсылка);
				
				ТекстСообщения = СтрШаблон(
					НСтр("ru = '%1 Попробуйте через некоторое время использовать кнопку ""Обновить"" в отчете %2.'"),
					ТекстСообщения,
					ПредставлениеОтчетаОтправки);
			КонецЕсли;
			
			СообщитьПользователю(ТекстСообщения, ПараметрыСоединения.СообщенияПользователю);
			
		Иначе
			ТекстСообщения = НСтр("ru = 'Не удалось получить результаты отправки с портала Росприроднадзора.'");
			
			Если ЭтоВызовИзСписка Тогда
				ПредставлениеОтчетаОтправки = ПредставлениеОтчета(ОтправкаСсылка.ОтчетСсылка);
				
				ТекстСообщения = СтрШаблон(
					НСтр("ru = '%1 Возможно, портал перегружен. Попробуйте через некоторое время использовать кнопку ""Обновить""  в отчете %2.'"),
					ТекстСообщения,
					ПредставлениеОтчетаОтправки);
			КонецЕсли;
			
			СообщитьПользователю(ТекстСообщения, ПараметрыСоединения.СообщенияПользователю);
		КонецЕсли;
		
		Результат = Неопределено;
		
	Иначе
		
		// возможно, проблемы с доступом в интернет
		Результат = Неопределено;
		
	КонецЕсли;
	
	ПараметрыСоединения.ОписаниеОшибкиСервером = "";
	
	Возврат Результат;
	
КонецФункции

Функция ОтправкаНаСерверРПН(ФайлыИДанныеОтправки, ОтчетСсылка, ПараметрыОтправки, ПараметрыСоединения)
	
	ПараметрыСоединения.ОписаниеОшибкиСервером = "";
	ПараметрыСоединения.ПовторятьСоединение = Ложь;
	ПараметрыСоединения.ПовторятьЗапросПароля = Ложь;
	ЗаголовокОтчета = Неопределено;
	
	ОтправкаПроизведена = ОтправитьЗашифрованныйПакетНаСерверРПН(ФайлыИДанныеОтправки, ПараметрыОтправки, ПараметрыСоединения);
	
	Если ОтправкаПроизведена Тогда
		
		ПараметрыСоединения.ПовторятьСоединение = Ложь;
		
		//отправленный файл принят сервером
		НоваяЗапись = Справочники.ОтправкиРПН.СоздатьЭлемент();
		НоваяЗапись.ОтчетСсылка = ОтчетСсылка;
		НоваяЗапись.ИдентификаторОтправкиНаСервере = ПараметрыСоединения.ИдентификаторОтправкиНаСервере;
		ДвоичныеДанныеПакета = Новый ДвоичныеДанные(ФайлыИДанныеОтправки.ИмяФайлаПакета);
		НоваяЗапись.ЗашифрованныйПакет = Новый ХранилищеЗначения(ДвоичныеДанныеПакета);
		НоваяЗапись.ИмяФайлаПакета = ФайлыИДанныеОтправки.КороткоеИмяФайлаПакета;
		НоваяЗапись.СтатусОтправки = Перечисления.СтатусыОтправки.Отправлен;
		НоваяЗапись.ДатаОтправки = ТекущаяДатаСеанса();
		НоваяЗапись.Организация = ?(ОтчетСсылка <> Неопределено, ОтчетСсылка.Организация, Неопределено);
		НоваяЗапись.ДатаНачалаПериода = ПараметрыОтправки.ПоляДляЗаписиОтправки.ДатаНачалаПериода;
		НоваяЗапись.ДатаОкончанияПериода = ПараметрыОтправки.ПоляДляЗаписиОтправки.ДатаОкончанияПериода;
		
		Если ОтчетСсылка <> Неопределено И ТипЗнч(ОтчетСсылка) = Тип("ДокументСсылка.РегламентированныйОтчет") Тогда
			НоваяЗапись.ВидОтчета = Справочники.ВидыОтправляемыхДокументов.НайтиПоРеквизиту("Источник", ОтчетСсылка.ИсточникОтчета);
			
		ИначеЕсли ТипЗнч(ОтчетСсылка) = Тип("ДокументСсылка.УведомлениеОСпецрежимахНалогообложения") Тогда
			НоваяЗапись.ВидОтчета = ЭлектронныйДокументооборотСКонтролирующимиОрганамиВызовСервера.ПолучитьВидОтправляемогоДокументаПоСсылке(ОтчетСсылка);
			
		Иначе // электронные представления
			НоваяЗапись.ВидОтчета = ?(ОтчетСсылка <> Неопределено, ОтчетСсылка.ВидОтчета, Неопределено);
		КонецЕсли;
		
		НоваяЗапись.Версия = ПараметрыОтправки.ПоляДляЗаписиОтправки.Версия;
		НоваяЗапись.СтатусПроверкиНаПортале = Перечисления.СтатусыОтправки.Отправлен;
		НоваяЗапись.ПредставлениеПериода = ПредставлениеПериода(НоваяЗапись.ДатаНачалаПериода, КонецДня(НоваяЗапись.ДатаОкончанияПериода), "ФП=Истина");
		НоваяЗапись.ПредставлениеВидаДокумента = ПредставлениеВидаДокумента(НоваяЗапись.Версия);
		НоваяЗапись.ИдентификаторОтправкиОператору = ПараметрыСоединения.ИдентификаторОтправкиОператору;
		НоваяЗапись.Записать();
		
		Результат = НоваяЗапись.Ссылка;
		
		Возврат Результат;
		
	ИначеЕсли (ЗначениеЗаполнено(ПараметрыСоединения.ОписаниеОшибкиСервером) ИЛИ НЕ ПараметрыСоединения.ПовторятьСоединение) Тогда
		
		ЭтоРегламентированныйОтчетИлиУведомление = (ТипЗнч(ОтчетСсылка) = Тип("ДокументСсылка.РегламентированныйОтчет")
			ИЛИ ТипЗнч(ОтчетСсылка) = Тип("ДокументСсылка.УведомлениеОСпецрежимахНалогообложения")
			ИЛИ ТипЗнч(ОтчетСсылка) = Тип("Неопределено"));
		
		Если ЗначениеЗаполнено(ПараметрыСоединения.ОписаниеОшибкиСервером) Тогда
			
			//отправленный файл не был принят сервером, получено описание ошибки
			Если Прав(ПараметрыСоединения.ОписаниеОшибкиСервером, 1) <> "." И Прав(ПараметрыСоединения.ОписаниеОшибкиСервером, 1) <> "!" Тогда
				ПараметрыСоединения.ОписаниеОшибкиСервером = ПараметрыСоединения.ОписаниеОшибкиСервером + ".";
			КонецЕсли;
			СообщитьПользователю(ПодставитьПараметрыВСтроку(НСтр("ru = 'Отчет не принят порталом. %1'"), ПараметрыСоединения.ОписаниеОшибкиСервером) + " " + НСтр("ru = 'Портал находится на стадии пилотного проекта.'") + " " + ?(ЭтоРегламентированныйОтчетИлиУведомление И НЕ ПараметрыОтправки.БумажнаяПодачаОтчета,
				НСтр("ru = 'Для отправки отчета выгрузите его в файл, используя меню ""Выгрузка"" - ""Выгрузить для портала Росприроднадзора"".'"), НСтр("ru = 'Для отправки отчета выгрузите его в файл, используя меню ""Еще"" - ""Выгрузить"".'"))
				+ " " + ПодставитьПараметрыВСтроку(НСтр("ru = 'Далее отправьте данный файл на портале %1'"), "https://" + ПараметрыОтправки.АдресСервера), ПараметрыСоединения.СообщенияПользователю);
			
		Иначе
			// в ответе сервера отсутствует описание ошибки и установлен признак повтора соединения
			СообщитьПользователю(НСтр("ru = 'Зашифрованный пакет не удалось отправить на портал Росприроднадзора.'") + " " + НСтр("ru = 'Портал находится на стадии пилотного проекта.'") + " " + ?(ЭтоРегламентированныйОтчетИлиУведомление И НЕ ПараметрыОтправки.БумажнаяПодачаОтчета,
				НСтр("ru = 'Для отправки отчета выгрузите его в файл, используя меню ""Выгрузка"" - ""Выгрузить для портала Росприроднадзора"".'"), НСтр("ru = 'Для отправки отчета выгрузите его в файл, используя меню ""Еще"" - ""Выгрузить"".'"))
				+ " " + ПодставитьПараметрыВСтроку(НСтр("ru = 'Далее отправьте данный файл на портале %1'"), "https://" + ПараметрыОтправки.АдресСервера), ПараметрыСоединения.СообщенияПользователю);
		КонецЕсли;
		
	КонецЕсли;
	
	Возврат Неопределено;
	
КонецФункции

Функция ОтправитьВРПНССервера(ЭтоЭлектроннаяПодписьВМоделиСервиса, Знач СтрокаBase64ФайлаОтправки, Знач СтрокаBase64ФайлаПакета, Знач КороткоеИмяФайлаПакета, Знач ОтчетСсылка, Знач ПараметрыОтправки, ПараметрыСоединения) Экспорт
	
	// получаем двоичные данные файлов
	Если ЭтоАдресВременногоХранилища(СтрокаBase64ФайлаПакета) ИЛИ ЭтоЭлектроннаяПодписьВМоделиСервиса Тогда
		ДвоичныеДанныеФайлаОтправки = ПолучитьИзВременногоХранилища(СтрокаBase64ФайлаОтправки);
		ДвоичныеДанныеФайлаПакета = ПолучитьИзВременногоХранилища(СтрокаBase64ФайлаПакета);
	Иначе
		ДвоичныеДанныеФайлаОтправки = Base64Значение(СтрокаBase64ФайлаОтправки);
		ДвоичныеДанныеФайлаПакета = Base64Значение(СтрокаBase64ФайлаПакета);
	КонецЕсли;
	
	// выгружаем двоичные данные в файл
	ИмяФайлаОтправки = ПолучитьИмяВременногоФайла();
	ДвоичныеДанныеФайлаОтправки.Записать(ИмяФайлаОтправки);
	
	ИмяФайлаПакета = ПолучитьИмяВременногоФайла();
	ДвоичныеДанныеФайлаПакета.Записать(ИмяФайлаПакета);
	
	ФайлыИДанныеОтправки = Новый Структура("КороткоеИмяФайлаПакета, ИмяФайлаОтправки, ИмяФайлаПакета", КороткоеИмяФайлаПакета, ИмяФайлаОтправки, ИмяФайлаПакета);
	
	Результат = ОтправкаНаСерверРПН(ФайлыИДанныеОтправки, ОтчетСсылка, ПараметрыОтправки, ПараметрыСоединения);
	
	ПараметрыСоединения.СоединениеHTTP = Неопределено;
	ПараметрыСоединения.ОписаниеОшибкиСервером = "";
	
	ОперацииСФайламиЭДКО.УдалитьВременныйФайл(ИмяФайлаОтправки);
	ОперацииСФайламиЭДКО.УдалитьВременныйФайл(ИмяФайлаПакета);
	
	Возврат Результат;
	
КонецФункции

Функция ГенерироватьHTMLПротоколаОбработкиРПН(ЗаголовокОтчета, СтатусОтчета, ЦветСтатуса, СтатусОтправки, ПараметрыОтправки, ОтчетСсылка, СкрытьПримечания)
	
	ТабДок = Новый ТабличныйДокумент;
	
	Шаблон = ПолучитьМакет("РПНШаблонДляHTML");
	
	НаименованиеОрганизации = "";
	ИНН = ИННОрганизации(ПараметрыОтправки.Организация, "", НаименованиеОрганизации);
	СведенияПоОбъекту 	= СведенияПоОтправляемымОбъектам(ОтчетСсылка);
	НаименованиеОргана 	= СведенияПоОбъекту.ПредставлениеКонтролирующегоОргана;
	НаименованиеОтчета 	= СведенияПоОбъекту.Наименование;
	
	СекцияШапкаСЗаголовком = Шаблон.ПолучитьОбласть("ШапкаСЗаголовком");
	Если СтатусОтправки = Перечисления.СтатусыОтправки.Сдан Тогда
		СекцияШапкаСЗаголовком.Параметры.НаименованиеПротокола = НСтр("ru = 'Протокол о сдаче'") + ?(НЕ СкрытьПримечания, "*", "");
	ИначеЕсли СтатусОтправки = Перечисления.СтатусыОтправки.НеПринят Тогда
		СекцияШапкаСЗаголовком.Параметры.НаименованиеПротокола = НСтр("ru = 'Протокол ошибок'") + ?(НЕ СкрытьПримечания, "*", "");
	Иначе
		СекцияШапкаСЗаголовком.Параметры.НаименованиеПротокола = НСтр("ru = 'Протокол'") + ?(НЕ СкрытьПримечания, "*", "");
	КонецЕсли;
	СекцияШапкаСЗаголовком.Параметры.Отправитель = НаименованиеОрганизации;
	СекцияШапкаСЗаголовком.Параметры.ИНН = НСтр("ru = 'ИНН'") + " " + ИНН;
	СекцияШапкаСЗаголовком.Параметры.Получатель = НаименованиеОргана;
	СекцияШапкаСЗаголовком.Параметры.Отчет = НаименованиеОтчета;
	СекцияШапкаСЗаголовком.Параметры.СтатусОтчета = СтатусОтчета;
	СекцияШапкаСЗаголовком.Области.СтатусОтчета.ЦветТекста = ЦветСтатуса;
	ТабДок.Вывести(СекцияШапкаСЗаголовком);
	
	СекцияСтроки = Шаблон.ПолучитьОбласть("Строки");
	СекцияСтроки.Параметры.НомерНаименование = НСтр("ru = 'Входящий номер отчета'") + ?(ПараметрыОтправки.БумажнаяПодачаОтчета И НЕ СкрытьПримечания, "**", "");
	СекцияСтроки.Параметры.Обновлено = ЗаголовокОтчета.ДатаОбновления;
	СекцияСтроки.Параметры.Номер = ЗаголовокОтчета.ИдентификаторЗаписи;
	СекцияСтроки.Параметры.Подано = ЗаголовокОтчета.ДатаЗагрузки;
	СекцияСтроки.Параметры.Статус = ЗаголовокОтчета.Состояние;
	СекцияСтроки.Параметры.Комментарий = ЗаголовокОтчета.ТекстСостояния;
	ТабДок.Вывести(СекцияСтроки);
	
	СекцияПодвал = Шаблон.ПолучитьОбласть("Подвал");
	ТабДок.Вывести(СекцияПодвал);
	
	Если НЕ СкрытьПримечания Тогда
	СекцияПримечание = Шаблон.ПолучитьОбласть("Примечание");
	СекцияПримечание.Параметры.СсылкаНаПортал = "https://" + ПараметрыОтправки.АдресСервера;
	ТабДок.Вывести(СекцияПримечание);
	
		Если ПараметрыОтправки.БумажнаяПодачаОтчета Тогда
		СекцияПримечаниеНеподписываемые = Шаблон.ПолучитьОбласть("ПримечаниеНеподписываемые");
		ТабДок.Вывести(СекцияПримечаниеНеподписываемые);
	КонецЕсли;
	КонецЕсли;
	
	ИмяВременногоФайла = ПолучитьИмяВременногоФайла();
	ТабДок.Записать(ИмяВременногоФайла, ТипФайлаТабличногоДокумента.HTML);
	ТекстHTML = Новый ТекстовыйДокумент;
	ТекстHTML.Прочитать(ИмяВременногоФайла);
	ТекстHTML = ТекстHTML.ПолучитьТекст();
	ОперацииСФайламиЭДКО.УдалитьВременныйФайл(ИмяВременногоФайла);
	
	Возврат ТекстHTML;
	
КонецФункции

Функция ПолучитьОсновныеСвойстваПоследнейОтправкиОтчетаВРПН(Знач ОтчетСсылка)
	
	Запрос = Новый Запрос;
	Запрос.Текст =
		"ВЫБРАТЬ ПЕРВЫЕ 1
		|	ОтправкиРПН.Ссылка,
		|	ОтправкиРПН.ИдентификаторОтправкиНаСервере КАК Идентификатор,
		|	ОтправкиРПН.ИмяФайлаПакета КАК ИмяФайлаПакета,
		|	ОтправкиРПН.СтатусОтправки КАК Статус,
		|	ОтправкиРПН.СтатусПроверкиНаПортале КАК СтатусПроверкиНаПортале,
		|	ОтправкиРПН.ИдентификаторОтправкиОператору КАК ИдентификаторОтправкиОператору
		|ИЗ
		|	Справочник.ОтправкиРПН КАК ОтправкиРПН
		|ГДЕ
		|	ОтправкиРПН.ОтчетСсылка = &ЭтотОтчет
		|	И ОтправкиРПН.ПометкаУдаления = ЛОЖЬ
		|
		|УПОРЯДОЧИТЬ ПО
		|	ОтправкиРПН.ДатаОтправки УБЫВ";
	Запрос.Параметры.Вставить("ЭтотОтчет", ОтчетСсылка);
	Результат = Запрос.Выполнить();
	Если НЕ Результат.Пустой() Тогда
		Выборка = Результат.Выбрать();
		Выборка.Следующий();
		Возврат Выборка;
	Иначе
		Возврат Неопределено;
	КонецЕсли;
	
КонецФункции

Функция ПолучитьПоследнююОтправкуОтчетаВРПН(ОтчетСсылка) Экспорт
	
	Отправка = Неопределено;
	
	Запрос = Новый Запрос;
	Запрос.Текст =
		"ВЫБРАТЬ ПЕРВЫЕ 1
		|	ОтправкиРПН.Ссылка
		|ИЗ
		|	Справочник.ОтправкиРПН КАК ОтправкиРПН
		|ГДЕ
		|	ОтправкиРПН.ОтчетСсылка = &ЭтотОтчет
		|	И ОтправкиРПН.ПометкаУдаления = ЛОЖЬ
		|
		|УПОРЯДОЧИТЬ ПО
		|	ОтправкиРПН.ДатаОтправки УБЫВ";
	Запрос.Параметры.Вставить("ЭтотОтчет", ОтчетСсылка);
	Результат = Запрос.Выполнить();
	
	Если НЕ Результат.Пустой() Тогда
		
		Выборка = Результат.Выбрать();
		Выборка.Следующий();
		Отправка = Выборка.Ссылка;
		
	КонецЕсли;
	
	Возврат Отправка;
	
КонецФункции

Процедура ОбновитьСтатусОтправкиРПНОтчета(Отчет)
	
	// находим последнюю не помеченную на удаление отправку отчета
	ПоследняяОтправка = ПолучитьПоследнююОтправкуОтчетаВРПН(Отчет);
	
	// вычисляем значение статуса отправки (ПеречислениеСсылка.СтатусыОтправки)
	Если ЗначениеЗаполнено(ПоследняяОтправка) Тогда
		
		СтатусОтправки = ПоследняяОтправка.СтатусОтправки;
		Если СтатусОтправки = Перечисления.СтатусыОтправки.ПринятЕстьОшибки Тогда
			//введено после добавления дополнительного статуса ПринятЕстьОшибки, при котором отчет считается сданным
			СтатусОтправки = Перечисления.СтатусыОтправки.Сдан;
		КонецЕсли;
		
	Иначе
		СтатусОтправки = Неопределено;
	КонецЕсли;
	
	// сохраняем статус в базе
	ЗаписатьСтатусОтправкиРПНОтчета(Отчет, СтатусОтправки, ПоследняяОтправка);
	
КонецПроцедуры

Процедура ЗаписатьСтатусОтправкиРПНОтчета(Отчет, СтатусОтправки, ОснованиеСтатуса)
	
	Если ЗначениеЗаполнено(СтатусОтправки) Тогда
		МенЗап = РегистрыСведений.СтатусыОтправки.СоздатьМенеджерЗаписи();
		МенЗап.Объект = Отчет;
		МенЗап.Статус = СтатусОтправки;
		МенЗап.Основание = ОснованиеСтатуса;
		МенЗап.Записать(Истина);
	Иначе
		МенЗап = РегистрыСведений.СтатусыОтправки.СоздатьМенеджерЗаписи();
		МенЗап.Объект = Отчет;
		МенЗап.Прочитать();
		Если МенЗап.Выбран() Тогда
			МенЗап.Удалить();
		КонецЕсли;
	КонецЕсли;
	
	// вызываем переопределяемую процедуру отработки изменения статуса отправки
	ПриИзмененииСтатусаОтправкиДокумента(Отчет, СтатусОтправки);
	
КонецПроцедуры

Функция ПолучитьПустуюДатуЗавершенияОтправкиРПН() Экспорт
	
	Возврат '39991231235959';
	
КонецФункции

Процедура ПриЗаписиОтправкиРПН(Объект, Отказ) Экспорт
	
	Если Объект.ОтчетСсылка <> Неопределено Тогда
		// отражаем изменения в регистре статусов отправки
		ОбновитьСтатусОтправкиРПНОтчета(Объект.ОтчетСсылка);
		ЭлектронныйДокументооборотСКонтролирующимиОрганами.ЗаписьОбъектовРегламентированнойОтчетности(Объект.ОтчетСсылка, Отказ);
	КонецЕсли;
	
КонецПроцедуры

Функция ПолучитьСтрокуСостоянияОтправкиОтчетаИДополнительныеСвойстваДляРПН(ОтчетСсылка, ОрганизацияСсылка, ЦветТекста, ПоказыватьКнопкуОткрытияПротокола) Экспорт
	
	ПоказыватьКнопкуОткрытияПротокола = Ложь;
	ЦветТекста = Новый Цвет(0, 0, 192);
	
	// определяем последнюю отправку
	ОтправкаСсылка = ПолучитьПоследнююОтправкуОтчетаВРПН(ОтчетСсылка);
	Если НЕ ЗначениеЗаполнено(ОтправкаСсылка) Тогда
		СтрокаСостояния = НСтр("ru = 'Отчет не отправлялся.'");
	Иначе
		СтрокаСостояния = ПодставитьПараметрыВСтроку(НСтр("ru = 'Отчет отправлен %1.'"), Формат(ОтправкаСсылка.ДатаОтправки, "ДЛФ=DDT"));
		
		Если ОтправкаСсылка.СтатусОтправки = Перечисления.СтатусыОтправки.НеПринят Тогда
			
			Если ОтправкаСсылка.СтатусПроверкиНаПортале = Перечисления.СтатусыОтправки.Сдан Тогда
				СтрокаСостояния = СтрокаСостояния + " " + НСтр("ru = 'Отчет не принят: получен протокол, обнаружены ошибки.'");
			Иначе
				СтрокаСостояния = СтрокаСостояния + " " + НСтр("ru = 'Отчет не принят, получен протокол ошибок.'");
			КонецЕсли;
			ЦветТекста = Новый Цвет(255, 0, 0);
			ПоказыватьКнопкуОткрытияПротокола = Истина;
			
		ИначеЕсли ОтправкаСсылка.СтатусОтправки = Перечисления.СтатусыОтправки.Сдан Тогда
			
			ОтчетСдан = Истина;
			СтрокаСостояния = СтрокаСостояния + " " + НСтр("ru = 'Отчет сдан: получен протокол, ошибок не обнаружено.'");
			ЦветТекста = Новый Цвет(0, 179, 16);
			ПоказыватьКнопкуОткрытияПротокола = Истина;
			
		ИначеЕсли ОтправкаСсылка.СтатусПроверкиНаПортале = Перечисления.СтатусыОтправки.Сдан Тогда
			
			// неполный идентификатор начинается с пяти звездочек
			Если Лев(ОтправкаСсылка.ИдентификаторОтправкиНаСервере, 5) = "*****" Тогда
				СтрокаСостояния = СтрокаСостояния + " " + НСтр("ru = 'Отчет обработан на портале и ждет доставки в управление.'");
			Иначе
				СтрокаСостояния = СтрокаСостояния + " " + НСтр("ru = 'Отчет обработан на портале и находится на проверке в управлении.'");
			КонецЕсли;
			ПоказыватьКнопкуОткрытияПротокола = Истина;
			
		ИначеЕсли ОтправкаСсылка.СтатусОтправки = Перечисления.СтатусыОтправки.Отправлен Тогда
			
			ПоказыватьКнопкуОткрытияПротокола = ЗначениеЗаполнено(ОтправкаСсылка.Протокол.Получить());
			
		КонецЕсли;
	КонецЕсли;
	
	Возврат СтрокаСостояния;
	
КонецФункции

Функция ПоляНаСервереДляЗаписиОтправкиВРПН(ОбъектСсылка) Экспорт
	
	Возврат Новый Структура("ДатаНачалаПериода, ДатаОкончанияПериода, Версия",
		ОбъектСсылка.ДатаНачала, ОбъектСсылка.ДатаОкончания, ОбъектСсылка.Версия);
	
КонецФункции

Процедура ПередЗаписьюОтправкиРПН(Объект, Отказ)
	
	Если НЕ ЗначениеЗаполнено(Объект.Период) Тогда
		Объект.Период = СокрЛП(Формат(Объект.ДатаОкончанияПериода, "ДФ=yyyyMMdd") + Формат('39991231' - Объект.ДатаНачалаПериода, "ДФ=yyyyMMdd"));
	КонецЕсли;
	
	Если Объект.СтатусОтправки = Перечисления.СтатусыОтправки.Отправлен Тогда
		
		Если НЕ ЗначениеЗаполнено(Объект.ДатаЗакрытия) Тогда
			Объект.ДатаЗакрытия = ПолучитьПустуюДатуЗавершенияОтправкиРПН();
		КонецЕсли;
		
	Иначе
		Объект.ДатаЗакрытия = Объект.ДатаПолученияРезультата;
	КонецЕсли;
	
КонецПроцедуры

Функция СписокДопустимыхОрганизацийВОбъектахОбменаРПН() Экспорт
	
	Запрос = Новый Запрос;
	ТекстЗапроса = 
	"ВЫБРАТЬ РАЗРЕШЕННЫЕ
	|	НастройкиОбменаРПН.Организация КАК Организация
	|ИЗ
	|	РегистрСведений.НастройкиОбменаРПН КАК НастройкиОбменаРПН
	|ГДЕ
	|	НастройкиОбменаРПН.ИспользоватьОбмен = ИСТИНА";
	
	Запрос.Текст = ТекстЗапроса;
	Возврат Запрос.Выполнить().Выгрузить().ВыгрузитьКолонку("Организация");
	
КонецФункции

////////////////////////////////////////////////////////////////////////////////
// Обмен сервером

Функция ОтправитьЗашифрованныйПакетНаСерверРПН(ФайлыИДанныеОтправки, ПараметрыОтправки, ПараметрыСоединения)
	
	ПараметрыСоединения.ИдентификаторОтправкиНаСервере = "";
	ПараметрыСоединения.ПовторятьСоединение = Ложь;
	ЗагрузкаПроизведена = Ложь;
	АдресСервера = ПараметрыОтправки.АдресСервера;
	
	АвторизацияПроизведена = (ПараметрыСоединения.СоединениеHTTP <> Неопределено);
	
	Если НЕ АвторизацияПроизведена Тогда
		
		Если НЕ ПараметрыСоединения.ОбменНапрямую Тогда
			
			// устанавливаем соединение с сервером
			
			СерверПриемаОтчетности = СерверПриемаОтчетностиРПН(Ложь);
			
			Попытка
				
				ПараметрыПодключения = ОбщегоНазначения.ПараметрыПодключенияWSПрокси();
				ПараметрыПодключения.АдресWSDL 				= СерверПриемаОтчетности.Определение;
				ПараметрыПодключения.URIПространстваИмен 	= СерверПриемаОтчетности.URIПространстваИменСервиса;
				ПараметрыПодключения.ИмяСервиса 			= СерверПриемаОтчетности.ИмяСервиса;
				ПараметрыПодключения.ИмяТочкиПодключения 	= СерверПриемаОтчетности.ИмяТочкиПодключения;
				ПараметрыПодключения.ИмяПользователя 		= "";
				ПараметрыПодключения.Пароль 				= "";
				ПараметрыПодключения.Таймаут 				= 90;
				
				Сервис = ОбщегоНазначения.СоздатьWSПрокси(ПараметрыПодключения);
					
			Исключение
				
				ПараметрыСоединения.ПовторятьСоединение = Истина;
				СообщитьПользователю(ПодставитьПараметрыВСтроку(НСтр("ru = 'Не удалось установить соединение с сервером: %1'"), Символы.ПС + ИнформацияОбОшибке().Описание), ПараметрыСоединения.СообщенияПользователю);
				Возврат Ложь;
				
			КонецПопытки;
			
			ПараметрыСоединения.СоединениеHTTP = Сервис;
			
		Иначе
			
		// очистить пароль, если в диалоге сохранения пароля не установлена галочка сохранения
		Если НЕ ПараметрыСоединения.СохранитьПароль И ЗначениеЗаполнено(ПараметрыСоединения.СохраненныйПароль) Тогда
			ЗаписатьEMailИПарольПорталаРПН(ПараметрыОтправки.Организация, , "");
			ПараметрыСоединения.СохраненныйПароль = "";
		КонецЕсли;
		
		// устанавливаем соединение с сервером
		ОписаниеОшибки = "";
		ПараметрыСоединения.СоединениеHTTP = ДокументооборотСКО.УстановитьСоединениеССерверомИнтернета("https://" + АдресСервера, ОписаниеОшибки);
		Если ПараметрыСоединения.СоединениеHTTP = Неопределено Тогда
			
			ПараметрыСоединения.ПовторятьСоединение = Истина;
			СообщитьПользователю(
				СтрШаблон(
						НСтр("ru = 'Не удалось установить соединение с сервером:
                          |%1.'"), ОписаниеОшибки), ПараметрыСоединения.СообщенияПользователю);
			Возврат Неопределено;
			
		КонецЕсли;
		
		КонецЕсли;
		
		ПараметрыСоединения.ЗначениеCookie = "";
		АвторизацияПроизведена = АвторизоватьсяНаПорталеРПН(ПараметрыОтправки, ПараметрыСоединения);
		
	КонецЕсли;
	
	Если АвторизацияПроизведена Тогда
		
		ИмяЗагружаемогоФайла = ?(НЕ ПараметрыСоединения.ОбменНапрямую, ФайлыИДанныеОтправки.ИмяФайлаПакета, ФайлыИДанныеОтправки.ИмяФайлаОтправки);
		ЗагрузкаПроизведена = ЗагрузитьОтчетНаПорталРПН(ИмяЗагружаемогоФайла, ПараметрыСоединения, ФайлыИДанныеОтправки.КороткоеИмяФайлаПакета);
		
	Иначе
		ПараметрыСоединения.СоединениеHTTP = Неопределено;
	КонецЕсли;
	
	Возврат ЗагрузкаПроизведена;
	
КонецФункции

Функция АвторизоватьсяНаПорталеРПН(ПараметрыОтправки, ПараметрыСоединения)
	
	ПараметрыСоединения.ОписаниеОшибкиСервером = "";
	ПараметрыСоединения.ПовторятьСоединение = Ложь;
	
	ОбменНапрямую = (ПараметрыСоединения.ИдентификаторОтправкиОператору = Неопределено И ПараметрыСоединения.ОбменНапрямую) ИЛИ ПараметрыСоединения.ИдентификаторОтправкиОператору = 0;
	
	Если ОбменНапрямую Тогда
		
	ЗаголовкиHTTP = Новый Соответствие();
	ЗаголовкиHTTP.Вставить("Content-Type", "application/x-www-form-urlencoded");
	ЗаголовкиHTTP.Вставить("Proxy-Connection", "Keep-Alive");
	ЗаголовкиHTTP.Вставить("Pragma", "no-cache");
	Если ЗначениеЗаполнено(ПараметрыСоединения.ЗначениеCookie) Тогда
		ЗаголовкиHTTP.Вставить("Cookie", ПараметрыСоединения.ЗначениеCookie);
	КонецЕсли;
	
	ИмяФайлаЗапроса = ПолучитьИмяВременногоФайла();
	ЗаписываемыйФайл = Новый ЗаписьТекста(ИмяФайлаЗапроса, "windows-1251");
	ЗаписываемыйФайл.Записать("UserName=" + ПараметрыСоединения.EMail + "&Password=" + ПараметрыСоединения.Пароль + "&RememberMe=false");
	ЗаписываемыйФайл.Закрыть();
	
	ИмяФайлаОтвета = ПолучитьИмяВременногоФайла();
	
	Попытка
		РесурсНаСервере = ДокументооборотСКО.ПолучитьЗаменяемоеЗначениеТестовогоСервера1С("/Account/Account/LogOn", "{RPN}/Account/Account/LogOn", ПараметрыСоединения.СоединениеHTTP);
		ОтветHTTP = ПараметрыСоединения.СоединениеHTTP.ОтправитьДляОбработки(ИмяФайлаЗапроса, 
																				РесурсНаСервере, 
																				ИмяФайлаОтвета, 
																				ЗаголовкиHTTP);
	
	Исключение
		
		ПараметрыСоединения.ПовторятьСоединение = Истина;
			ОперацииСФайламиЭДКО.УдалитьВременныйФайл(ИмяФайлаЗапроса);
			ОперацииСФайламиЭДКО.УдалитьВременныйФайл(ИмяФайлаОтвета);
		Возврат Ложь;
		
	КонецПопытки;
	
	ФайлОтвета = Новый ЧтениеТекста(ИмяФайлаОтвета, "windows-1251");
	ТекстОтвета = ФайлОтвета.Прочитать();
	ФайлОтвета.Закрыть();
	
	ЗначениеSetCookie = ОтветHTTP.Заголовки.Получить("Set-Cookie");
	ПараметрыСоединения.ЗначениеCookie = ОбновленноеЗначениеCookie(ПараметрыСоединения.ЗначениеCookie, ЗначениеSetCookie);
	
		ОперацииСФайламиЭДКО.УдалитьВременныйФайл(ИмяФайлаЗапроса);
		ОперацииСФайламиЭДКО.УдалитьВременныйФайл(ИмяФайлаОтвета);
	
	ПараметрыСоединения.ОписаниеОшибкиСервером = ТекстОшибкиПорталаРПН(ТекстОтвета);
	
	ПараметрыСоединения.ПовторятьЗапросПароля = ЗначениеЗаполнено(ПараметрыСоединения.ОписаниеОшибкиСервером);
	
	// сохранить пароль при успешной авторизации
	
	Если НЕ ПараметрыСоединения.ПовторятьЗапросПароля И (ПараметрыСоединения.EMail <> ПараметрыСоединения.СохраненныйEMail ИЛИ (ПараметрыСоединения.СохранитьПароль И ПараметрыСоединения.Пароль <> ПараметрыСоединения.СохраненныйПароль)) Тогда
		ПарольДляСохранения = ?(ПараметрыСоединения.СохранитьПароль, ПараметрыСоединения.Пароль, "");
		ЗаписатьEMailИПарольПорталаРПН(ПараметрыОтправки.Организация, ПараметрыСоединения.EMail, ПарольДляСохранения);
		ПараметрыСоединения.СохраненныйEMail = ПараметрыСоединения.EMail;
		ПараметрыСоединения.СохраненныйПароль = ПарольДляСохранения;
	КонецЕсли;
	
	КонецЕсли;
	
	Возврат (НЕ ЗначениеЗаполнено(ПараметрыСоединения.ОписаниеОшибкиСервером));
	
КонецФункции

Функция ЗагрузитьОтчетНаПорталРПН(ИмяЗагружаемогоФайла, ПараметрыСоединения, КороткоеИмяЗагружаемогоФайла = Неопределено)
	
	ПараметрыСоединения.ОписаниеОшибкиСервером = "";
	ПараметрыСоединения.ПовторятьСоединение = Ложь;
	
	Если НЕ ПараметрыСоединения.ОбменНапрямую Тогда
		
		Сервис = ПараметрыСоединения.СоединениеHTTP;
		
		ДвоичныеДанныеПакета = Новый ДвоичныеДанные(ИмяЗагружаемогоФайла);
		ДанныеПакетаВBase64 = Base64Строка(ДвоичныеДанныеПакета);
		
		Попытка
			
			ПараметрыСоединения.ИдентификаторОтправкиОператору = Сервис.Send("1CO", ДанныеПакетаВBase64);
			РезультатОбработки = Сервис.Check(ПараметрыСоединения.ИдентификаторОтправкиОператору);
			
		Исключение
			
			ПараметрыСоединения.ПовторятьСоединение = Истина;
			Возврат Ложь;
			
		КонецПопытки;
		
		Если РезультатОбработки.Свойства().Получить("Ticket") <> Неопределено И ЗначениеЗаполнено(РезультатОбработки.Ticket) Тогда
			ПараметрыСоединения.ИдентификаторОтправкиНаСервере = СокрЛП(РезультатОбработки.Ticket);
		Иначе
			ПараметрыСоединения.ИдентификаторОтправкиНаСервере = Формат(ПараметрыСоединения.ИдентификаторОтправкиОператору, "ЧН=0; ЧГ=");
		КонецЕсли;
		
	Иначе
		
		ЗаголовкиHTTP = Новый Соответствие();
		ЗаголовкиHTTP.Вставить("Content-Type", "multipart/form-data; boundary=My1cV8bNdr");
		ЗаголовкиHTTP.Вставить("Proxy-Connection", "Keep-Alive");
		ЗаголовкиHTTP.Вставить("Pragma", "no-cache");
		Если ЗначениеЗаполнено(ПараметрыСоединения.ЗначениеCookie) Тогда
			ЗаголовкиHTTP.Вставить("Cookie", ПараметрыСоединения.ЗначениеCookie);
		КонецЕсли;
		
		// посылаем запрос
		ИмяФайлаОтвета = ПолучитьИмяВременногоФайла();
		
		Попытка
			ОтветHTTP = ПараметрыСоединения.СоединениеHTTP.ОтправитьДляОбработки(ИмяЗагружаемогоФайла, "/Index/UploadForm", ИмяФайлаОтвета, ЗаголовкиHTTP);
		Исключение
			
			ПараметрыСоединения.ПовторятьСоединение = Истина;
			Возврат Ложь;
			
		КонецПопытки;
		
		ЗначениеSetCookie = ОтветHTTP.Заголовки.Получить("Set-Cookie");
		ПараметрыСоединения.ЗначениеCookie = ОбновленноеЗначениеCookie(ПараметрыСоединения.ЗначениеCookie, ЗначениеSetCookie);
		
		ФайлОтвета = Новый ЧтениеТекста(ИмяФайлаОтвета, "windows-1251");
		ТекстОтвета = ФайлОтвета.Прочитать();
		ФайлОтвета.Закрыть();
		
		ОперацииСФайламиЭДКО.УдалитьВременныйФайл(ИмяФайлаОтвета);
		
		ПараметрыСоединения.ОписаниеОшибкиСервером = ТекстОшибкиПорталаРПН(ТекстОтвета);
		
		ПараметрыСоединения.ИдентификаторОтправкиНаСервере = ТекстПослеПрефикса(ТекстОтвета, "<h2 class=""text-danger"">", "</h2>");
			ПараметрыСоединения.ИдентификаторОтправкиОператору = 0;
		Если НЕ ЗначениеЗаполнено(ПараметрыСоединения.ИдентификаторОтправкиНаСервере) Тогда
			ПараметрыСоединения.ОписаниеОшибкиСервером = "Ошибочный ответ сервера";
		КонецЕсли;
		
	КонецЕсли;
	
	Возврат НЕ ЗначениеЗаполнено(ПараметрыСоединения.ОписаниеОшибкиСервером);
	
КонецФункции

Функция ТекстОшибкиПорталаРПН(ТекстОтвета)
	
	ТекстОшибкиВHTML = ТекстПослеПрефикса(ТекстОтвета, "<div class=""validation-summary-errors""><span>", "<");
	Если ЗначениеЗаполнено(ТекстОшибкиВHTML) Тогда
		ТекстОшибкиВHTML = ТекстПослеПрефикса(ТекстОтвета, "<div class=""validation-summary-errors""><span>", "</ul>");
		ТекстОшибкиВHTML = СтрЗаменить(ТекстОшибкиВHTML, "</span>", "");
		ТекстОшибкиВHTML = СтрЗаменить(ТекстОшибкиВHTML, "<ul>", "");
		ТекстОшибкиВHTML = СтрЗаменить(ТекстОшибкиВHTML, "<li>", " ");
		ТекстОшибкиВHTML = СтрЗаменить(ТекстОшибкиВHTML, "<li style=""display:none"">", " ");
		ТекстОшибкиВHTML = СтрЗаменить(ТекстОшибкиВHTML, "</li>", "");
		ТекстОшибкиВHTML = СтрЗаменить(ТекстОшибкиВHTML, Символы.ВК + Символы.ПС, "");
		ТекстОшибкиВHTML = СтрЗаменить(ТекстОшибкиВHTML, Символы.ПС, "");
	КонецЕсли;
	Если НЕ ЗначениеЗаполнено(ТекстОшибкиВHTML) Тогда
		ТекстОшибкиВHTML = ТекстПослеПрефикса(ТекстОтвета, "<p class='error alert alert-danger' id=""errorText"">", "<")
	КонецЕсли;
	
	Если НЕ ЗначениеЗаполнено(ТекстОшибкиВHTML) Тогда
		Возврат "";
	КонецЕсли;
	
	ТекстОшибки = ТекстИзHTML(ТекстОшибкиВHTML);
	ТекстОшибки = ТекстИзUTF8(ТекстОшибки);
	ВнутренняяОшибка = "An error occurred while updating the entries. See the inner exception for details.";
	Если Лев(ТекстОшибки, СтрДлина(ВнутренняяОшибка)) = ВнутренняяОшибка Тогда
		ТекстОшибки = "Произошла ошибка. Возможно, неверный формат отчета или слишком частые попытки отправки отчета.";
	КонецЕсли;
	
	Возврат СокрЛП(ТекстОшибки);
	
КонецФункции

Функция ТекстПослеПрефикса(
		ПолныйТекст,
		Префикс,
		Постфикс = "",
		ПозицияПослеПостфикса = Неопределено,
		НомерВхожденияПрефикса = 1,
		НачалоПрефикса = 0)
	
	НачалоПрефикса = НайтиНомерВхождения(ПолныйТекст, Префикс, НомерВхожденияПрефикса);
	Если НачалоПрефикса = 0 Тогда
		ПозицияПослеПостфикса = СтрДлина(ПолныйТекст) + 1;
		Возврат Неопределено;
	КонецЕсли;
	
	ДлинаПрефикса = СтрДлина(Префикс);
	Результат = Сред(ПолныйТекст, НачалоПрефикса + ДлинаПрефикса);
	ПозицияПослеПостфикса = НачалоПрефикса + ДлинаПрефикса;
	
	Если ЗначениеЗаполнено(Постфикс) Тогда
		КонецПрефикса = СтрНайти(Результат, Постфикс);
		
		Если КонецПрефикса > 0 Тогда
			Результат = Лев(Результат, КонецПрефикса - 1);
			ПозицияПослеПостфикса = НачалоПрефикса + ДлинаПрефикса + КонецПрефикса - 1 + СтрДлина(Постфикс);
		Иначе
			ПозицияПослеПостфикса = СтрДлина(ПолныйТекст) + 1;
		КонецЕсли;
	КонецЕсли;
	
	Возврат Результат;
	
КонецФункции

// отрицательный НомерВхождения - отсчитать с конца, вернется номер вхождения сначала
Функция НайтиНомерВхождения(Строка, ПодстрокаПоиска, НомерВхождения)
	
	СтрокаВыполненияПоиска = Строка;
	МассивПозиций = Новый Массив;
	ПозицияПодстрокиВИсходнойСтроке = 0;
	НомерНайденнойПодстроки = 0;
	
	Пока СтрокаВыполненияПоиска <> "" Цикл
		ПозицияПодстроки = СтрНайти(СтрокаВыполненияПоиска, ПодстрокаПоиска);
		Если ПозицияПодстроки = 0 Тогда
			Прервать;
		КонецЕсли;
		ПозицияПодстрокиВИсходнойСтроке = ПозицияПодстрокиВИсходнойСтроке + ПозицияПодстроки;
		НомерНайденнойПодстроки = НомерНайденнойПодстроки + 1;
		
		Если НомерВхождения >= 0 Тогда
			Если НомерНайденнойПодстроки >= НомерВхождения Тогда
				Возврат ПозицияПодстрокиВИсходнойСтроке;
			КонецЕсли;
			
		Иначе
			МассивПозиций.Добавить(ПозицияПодстрокиВИсходнойСтроке);
		КонецЕсли;
		
		СтрокаВыполненияПоиска = Сред(СтрокаВыполненияПоиска, ПозицияПодстроки + 1);
	КонецЦикла;
	
	Если НомерВхождения < 0 И МассивПозиций.Количество() >= - НомерВхождения Тогда
		НомерВхождения = МассивПозиций.Количество() + НомерВхождения + 1;
		Возврат МассивПозиций[НомерВхождения - 1];
	КонецЕсли;
		
	Возврат 0;
	
КонецФункции

Функция ПолучитьИнформациюОбОтчетеНаПорталеРПН(ИмяФайлаВыгрузки, ПараметрыОтправки, ПараметрыСоединения)
	
	ПараметрыСоединения.ОписаниеОшибкиСервером = "";
	ПараметрыСоединения.ПовторятьСоединение = Ложь;
	АдресСервера = ПараметрыОтправки.АдресСервера;
	
	АвторизацияПроизведена = (ПараметрыСоединения.СоединениеHTTP <> Неопределено);
	
	Если НЕ АвторизацияПроизведена Тогда
		
		Если ПараметрыСоединения.ИдентификаторОтправкиОператору <> 0 Тогда
			
			// устанавливаем соединение с сервером
			
			СерверПриемаОтчетности = СерверПриемаОтчетностиРПН(Ложь);
			
			Попытка
				
				ПараметрыПодключения = ОбщегоНазначения.ПараметрыПодключенияWSПрокси();
				ПараметрыПодключения.АдресWSDL 				= СерверПриемаОтчетности.Определение;
				ПараметрыПодключения.URIПространстваИмен 	= СерверПриемаОтчетности.URIПространстваИменСервиса;
				ПараметрыПодключения.ИмяСервиса 			= СерверПриемаОтчетности.ИмяСервиса;
				ПараметрыПодключения.ИмяТочкиПодключения 	= СерверПриемаОтчетности.ИмяТочкиПодключения;
				ПараметрыПодключения.ИмяПользователя 		= "";
				ПараметрыПодключения.Пароль 				= "";
				ПараметрыПодключения.Таймаут 				= 90;
				
				Сервис = ОбщегоНазначения.СоздатьWSПрокси(ПараметрыПодключения);
				
			Исключение
				
				ПараметрыСоединения.ПовторятьСоединение = Истина;
				СообщитьПользователю(ПодставитьПараметрыВСтроку(НСтр("ru = 'Не удалось установить соединение с сервером: %1'"), Символы.ПС + ИнформацияОбОшибке().Описание), ПараметрыСоединения.СообщенияПользователю);
				Возврат Неопределено;
				
			КонецПопытки;
			
			ПараметрыСоединения.СоединениеHTTP = Сервис;
			
		Иначе
			
		// очистить пароль, если в диалоге сохранения пароля не установлена галочка сохранения
		Если НЕ ПараметрыСоединения.СохранитьПароль И ЗначениеЗаполнено(ПараметрыСоединения.СохраненныйПароль) Тогда
			ЗаписатьEMailИПарольПорталаРПН(ПараметрыОтправки.Организация, , "");
			ПараметрыСоединения.СохраненныйПароль = "";
		КонецЕсли;
		
		// устанавливаем соединение с сервером
		ОписаниеОшибки = "";
		ПараметрыСоединения.СоединениеHTTP = ДокументооборотСКО.УстановитьСоединениеССерверомИнтернета("https://" + АдресСервера, ОписаниеОшибки);
		Если ПараметрыСоединения.СоединениеHTTP = Неопределено Тогда
			
			ПараметрыСоединения.ПовторятьСоединение = Истина;
			СообщитьПользователю(
				СтрШаблон(
						НСтр("ru = 'Не удалось установить соединение с сервером:
                          |%1.'"), ОписаниеОшибки), ПараметрыСоединения.СообщенияПользователю);
			Возврат Неопределено;
			
		КонецЕсли;
		
		КонецЕсли;
		
		ПараметрыСоединения.ЗначениеCookie = "";
		АвторизацияПроизведена = АвторизоватьсяНаПорталеРПН(ПараметрыОтправки, ПараметрыСоединения);
		
	КонецЕсли;
	
	Если АвторизацияПроизведена Тогда
		
		ЗаголовокОтчета = НайтиЗаголовокОтчетаНаПорталеРПН(ИмяФайлаВыгрузки, ПараметрыСоединения);
		
		Если ЗаголовокОтчета <> Неопределено Тогда
			
			ПараметрыСоединения.ОписаниеОшибкиСервером = "";
			ПараметрыСоединения.ПовторятьСоединение = Ложь;
			
			Возврат ЗаголовокОтчета;
			
		КонецЕсли;
		
	Иначе
		ПараметрыСоединения.СоединениеHTTP = Неопределено;
	КонецЕсли;
	
	Возврат Неопределено;
	
КонецФункции

Функция НайтиЗаголовокОтчетаНаПорталеРПН(ИмяФайлаВыгрузки, ПараметрыСоединения)
	
	ПараметрыСоединения.ОписаниеОшибкиСервером = "";
	ПараметрыСоединения.ПовторятьСоединение = Ложь;
	
	Если ПараметрыСоединения.ИдентификаторОтправкиОператору <> 0 Тогда
		
		Сервис = ПараметрыСоединения.СоединениеHTTP;
		
		Попытка
			РезультатОбработки = Сервис.Check(ПараметрыСоединения.ИдентификаторОтправкиОператору);
		Исключение
			
			ИнформацияОбОшибке = ИнформацияОбОшибке();
			Пока Истина Цикл
				Если СтрНайти(нрег(ИнформацияОбОшибке.Описание), "report not found") > 0 Тогда
					ПараметрыСоединения.ОписаниеОшибкиСервером = НСтр("ru = 'Отчет не найден или попытка обновления состояния устаревшего отчета.'");
					Возврат Неопределено;
				КонецЕсли;
				
				Если ИнформацияОбОшибке.Причина = Неопределено Тогда
					Прервать;
				КонецЕсли;
				ИнформацияОбОшибке = ИнформацияОбОшибке.Причина;
			КонецЦикла;
			
			ПараметрыСоединения.ПовторятьСоединение = Истина;
			Возврат Неопределено;
			
		КонецПопытки;
		
		ЗаголовокОтчета = Новый Структура;
		ЗаголовокОтчета.Вставить("ИдентификаторЗаписи", ПараметрыСоединения.ИдентификаторОтправкиНаСервере);
		ЗаголовокОтчета.Вставить("ИмяФайла", 			ИмяФайлаВыгрузки);
		ЗаголовокОтчета.Вставить("Состояние", 			РезультатОбработки.Status);
		ЗаголовокОтчета.Вставить("ДатаЗагрузки", 		Неопределено);
		ЗаголовокОтчета.Вставить("ДатаОбновления", 		Неопределено);
		ЗаголовокОтчета.Вставить("ТекстСостояния", 		РезультатОбработки.Comment);
		
		Если ЗаголовокОтчета.Состояние = "Ошибка АПИ" Тогда
			ЗаголовокОтчета.Состояние = "Отчет не принят Росприроднадзором";
		ИначеЕсли ЗаголовокОтчета.Состояние = "Загружен в РПН" Тогда
			ЗаголовокОтчета.Состояние = "Загружен в Росприроднадзор";
		КонецЕсли;
		
		Если РезультатОбработки.Свойства().Получить("Ticket") <> Неопределено И ЗначениеЗаполнено(РезультатОбработки.Ticket) Тогда
			ЗаголовокОтчета.ИдентификаторЗаписи = СокрЛП(РезультатОбработки.Ticket);
		КонецЕсли;
		Если РезультатОбработки.Свойства().Получить("SendAt") <> Неопределено Тогда
			ЗаголовокОтчета.ДатаЗагрузки = РезультатОбработки.SendAt;
		КонецЕсли;
		Если РезультатОбработки.Свойства().Получить("UpdatedAt") <> Неопределено Тогда
			ЗаголовокОтчета.ДатаОбновления = РезультатОбработки.UpdatedAt;
		КонецЕсли;
		
		ТекстСостояния = ТекстПослеПрефикса(ЗаголовокОтчета.ТекстСостояния, "<div class=""message""", "</div>");
		Если ЗначениеЗаполнено(ТекстСостояния) Тогда
			ЗаголовокОтчета.ТекстСостояния = ТекстПослеПрефикса(ТекстСостояния, ">");
			ЗаголовокОтчета.ТекстСостояния = СокрЛП(ЗаголовокОтчета.ТекстСостояния);
			
		Иначе
			ТекстСостояния = ТекстПослеПрефикса(ЗаголовокОтчета.ТекстСостояния, "<h2>", "</h2>");
			Если НЕ ЗначениеЗаполнено(ТекстСостояния) Тогда
				ТекстСостояния = ТекстПослеПрефикса(ЗаголовокОтчета.ТекстСостояния, "<h1>", "</h1>");
				Если НЕ ЗначениеЗаполнено(ТекстСостояния) Тогда
					ТекстСостояния = ТекстПослеПрефикса(ЗаголовокОтчета.ТекстСостояния, "<p>", "</p>");
				КонецЕсли;
			КонецЕсли;
			Если ЗначениеЗаполнено(ТекстСостояния) Тогда
				ЗаголовокОтчета.ТекстСостояния = СтрШаблон(
					НСтр("ru = 'Попробуйте обновить состояние отчета позже. %1'"),
					СокрЛП(ТекстСостояния));
				
			Иначе
				ВнутреннийУникальныйИдентификаторОтчетаВРПН = ТекстПослеПрефикса(ЗаголовокОтчета.ТекстСостояния,
					"<uuid type=""object"" ref=""Uuid"">", "</uuid>");
				Если ЗначениеЗаполнено(ВнутреннийУникальныйИдентификаторОтчетаВРПН) Тогда
					ИдентификаторОтправкиОператору = "";
					ИдентификаторОтчетаВРПН = "";
					ДатаФормирования = Неопределено;
					ДатаОбновления = Неопределено;
					ДатаЗавершения = Неопределено;
					КомментарийИнспектораВРПН = "";
					СообщенияОбОшибках = Новый Массив;
					СообщенияЖурналаОбработки = Новый Массив;
					
					Если ТипЗнч(ПараметрыСоединения.ИдентификаторОтправкиОператору) = Тип("Число") Тогда
						ИдентификаторОтправкиОператору = Формат(ПараметрыСоединения.ИдентификаторОтправкиОператору, "ЧДЦ=; ЧН=; ЧГ=");
					Иначе
						ИдентификаторОтправкиОператору = СокрЛП(ПараметрыСоединения.ИдентификаторОтправкиОператору);
					КонецЕсли;
					
					ДеревоXML = ЗагрузитьXMLВДеревоЗначений(Неопределено, ЗаголовокОтчета.ТекстСостояния);
					Если ДеревоXML <> Неопределено Тогда
						УзелДанные = ДеревоXML.Строки.Найти("data", "Имя", Истина);
						Если УзелДанные <> Неопределено Тогда
							УзелЗапрос = УзелДанные.Строки.Найти("service_request", "Имя", Истина);
							Если УзелЗапрос <> Неопределено Тогда
								УзелИдентификатор = УзелЗапрос.Строки.Найти("id", "Имя");
								Если УзелИдентификатор <> Неопределено Тогда
									ИдентификаторОтчетаВРПН = XMLЗначение(Тип("Строка"), УзелИдентификатор.Значение);
									ИдентификаторОтчетаВРПН = СокрЛП(ИдентификаторОтчетаВРПН);
								КонецЕсли;
							КонецЕсли;
							
							УзелДатаФормирования = УзелДанные.Строки.Найти("created_at", "Имя", Истина);
							Если УзелДатаФормирования <> Неопределено Тогда
								ДатаФормирования = ДатаИзСтрокиРазныхФорматов(УзелДатаФормирования.Значение);
							КонецЕсли;
							
							УзелДатаОбновления = УзелДанные.Строки.Найти("updated_at", "Имя", Истина);
							Если УзелДатаОбновления <> Неопределено Тогда
								ДатаОбновления = ДатаИзСтрокиРазныхФорматов(УзелДатаОбновления.Значение);
							КонецЕсли;
							
							УзелДатаЗавершения = УзелДанные.Строки.Найти("finished_at", "Имя", Истина);
							Если УзелДатаЗавершения <> Неопределено Тогда
								ДатаЗавершения = ДатаИзСтрокиРазныхФорматов(УзелДатаЗавершения.Значение);
							КонецЕсли;
							
							УзелКомментарийИнспектора = УзелДанные.Строки.Найти("result_comment", "Имя", Истина);
							Если УзелКомментарийИнспектора <> Неопределено Тогда
								КомментарийИнспектораВРПН = XMLЗначение(Тип("Строка"), УзелКомментарийИнспектора.Значение);
								КомментарийИнспектораВРПН = ОчиститьФорматированиеHTML(КомментарийИнспектораВРПН);
							КонецЕсли;
							
							УзелОшибки = УзелДанные.Строки.Найти("errors", "Имя", Истина);
							Если УзелОшибки <> Неопределено Тогда
								ФильтрСтрок = Новый Структура("Имя", "message");
								УзлыСообщенияОбОшибках = УзелОшибки.Строки.НайтиСтроки(ФильтрСтрок, Истина);
								Для каждого УзелСообщениеОбОшибке Из УзлыСообщенияОбОшибках Цикл
									СообщениеОбОшибке = XMLЗначение(Тип("Строка"), УзелСообщениеОбОшибке.Значение);
									Если ЗначениеЗаполнено(СообщениеОбОшибке) Тогда
										СообщениеОбОшибке = ОчиститьФорматированиеHTML(СообщениеОбОшибке);
										СообщенияОбОшибках.Добавить(СообщениеОбОшибке);
									КонецЕсли;
								КонецЦикла;
							КонецЕсли;
							
							ФильтрСтрок = Новый Структура("Имя", "log");
							УзлыСообщенийЖурналаОбработки = УзелДанные.Строки.НайтиСтроки(ФильтрСтрок, Истина);
							Для каждого УзелСообщенияЖурналаОбработки Из УзлыСообщенийЖурналаОбработки Цикл
								СообщениеЖурналаОбработки = XMLЗначение(Тип("Строка"), УзелСообщенияЖурналаОбработки.Значение);
								Если ЗначениеЗаполнено(СообщениеЖурналаОбработки) Тогда
									СообщениеЖурналаОбработки = ОчиститьФорматированиеHTML(СообщениеЖурналаОбработки);
									СообщенияЖурналаОбработки.Добавить(СообщениеЖурналаОбработки);
								КонецЕсли;
							КонецЦикла;
						КонецЕсли;
					КонецЕсли;
					
					СтрокиТекстаСостояния = Новый Массив;
					
					Если ЗначениеЗаполнено(ИдентификаторОтправкиОператору) Тогда
						СтрокаТекстаСостояния = СтрШаблон(
							НСтр("ru = 'Номер отчета у оператора: %1'"),
							ИдентификаторОтправкиОператору);
						СтрокиТекстаСостояния.Добавить(СтрокаТекстаСостояния);
					КонецЕсли;
					
					Если ЗначениеЗаполнено(ИдентификаторОтчетаВРПН) Тогда
						СтрокаТекстаСостояния = СтрШаблон(
							НСтр("ru = 'Номер отчета в личном кабинете Росприроднадзора: %1'"),
							ИдентификаторОтчетаВРПН);
						СтрокиТекстаСостояния.Добавить(СтрокаТекстаСостояния);
					КонецЕсли;
					
					Если ЗначениеЗаполнено(ВнутреннийУникальныйИдентификаторОтчетаВРПН) Тогда
						СтрокаТекстаСостояния = СтрШаблон(
							НСтр("ru = 'Уникальный идентификатор отчета в Росприроднадзоре: %1'"),
							ВнутреннийУникальныйИдентификаторОтчетаВРПН);
						СтрокиТекстаСостояния.Добавить(СтрокаТекстаСостояния);
					КонецЕсли;
					
					Если ЗначениеЗаполнено(ДатаФормирования) Тогда
						ДатаФормированияСтрокой = Формат(ДатаФормирования, "ДЛФ=ДВ");
						СтрокаТекстаСостояния = СтрШаблон(
							?(ЗначениеЗаполнено(ДатаЗавершения), НСтр("ru = 'Дата начала формирования: %1'"),
								НСтр("ru = 'Дата формирования: %1'")),
							ДатаФормированияСтрокой);
						СтрокиТекстаСостояния.Добавить(СтрокаТекстаСостояния);
					КонецЕсли;
					
					Если ЗначениеЗаполнено(ДатаОбновления) Тогда
						ДатаОбновленияСтрокой = Формат(ДатаОбновления, "ДЛФ=ДВ");
						СтрокаТекстаСостояния = СтрШаблон(
							НСтр("ru = 'Дата обновления: %1'"),
							ДатаОбновленияСтрокой);
						СтрокиТекстаСостояния.Добавить(СтрокаТекстаСостояния);
					КонецЕсли;
					
					Если ЗначениеЗаполнено(ДатаЗавершения) Тогда
						ДатаЗавершенияСтрокой = Формат(ДатаЗавершения, "ДЛФ=ДВ");
						СтрокаТекстаСостояния = СтрШаблон(
							НСтр("ru = 'Дата завершения формирования: %1'"),
							ДатаЗавершенияСтрокой);
						СтрокиТекстаСостояния.Добавить(СтрокаТекстаСостояния);
					КонецЕсли;
					
					Если ЗначениеЗаполнено(КомментарийИнспектораВРПН) Тогда
						Если СтрокиТекстаСостояния.Количество() > 0 Тогда
							СтрокиТекстаСостояния.Добавить("");
						КонецЕсли;
						СтрокиТекстаСостояния.Добавить(НСтр("ru = 'Комментарий инспектора Росприроднадзора'") + ":");
						СтрокиТекстаСостояния.Добавить(КомментарийИнспектораВРПН);
					КонецЕсли;
					
					Если СообщенияОбОшибках.Количество() >= 1 Тогда
						Если СтрокиТекстаСостояния.Количество() > 0 Тогда
							СтрокиТекстаСостояния.Добавить("");
						КонецЕсли;
						Если СообщенияОбОшибках.Количество() = 1 Тогда
							СтрокиТекстаСостояния.Добавить(НСтр("ru = 'Обнаружена ошибка'") + ":");
						Иначе
							СтрокиТекстаСостояния.Добавить(НСтр("ru = 'Обнаружены ошибки'") + ":");
						КонецЕсли;
						Для каждого СообщениеОбОшибке Из СообщенияОбОшибках Цикл
							СтрокиТекстаСостояния.Добавить(СообщениеОбОшибке);
						КонецЦикла;
					КонецЕсли;
					
					Если СообщенияЖурналаОбработки.Количество() >= 1 Тогда
						Если СтрокиТекстаСостояния.Количество() > 0 Тогда
							СтрокиТекстаСостояния.Добавить("");
						КонецЕсли;
						СтрокиТекстаСостояния.Добавить(НСтр("ru = 'Журнал обработки'") + ":");
						Для каждого СообщениеЖурналаОбработки Из СообщенияЖурналаОбработки Цикл
							СтрокиТекстаСостояния.Добавить(СообщениеЖурналаОбработки);
						КонецЦикла;
					КонецЕсли;
					
					ПолучатьПодробныйПротоколРПН =
						ХранилищеОбщихНастроек.Загрузить("ДокументооборотСКонтролирующимиОрганами_ПолучатьПодробныйПротоколРПН") = Истина
						ИЛИ ОнлайнСервисыРегламентированнойОтчетностиВызовСервера.СобытиеНаступило("Подрбный протокол РПН");
					
					ЗаголовокОтчета.ТекстСостояния = СтрСоединить(СтрокиТекстаСостояния, "
						|") + ?(ПолучатьПодробныйПротоколРПН, "
						|
						|" + НСтр("ru = 'Подробные результаты обработки'") + ":
						|" + ЗаголовокОтчета.ТекстСостояния, "");
				КонецЕсли;
			КонецЕсли;
		КонецЕсли;
		
	Иначе
		
		ЗаголовкиHTTP = Новый Соответствие();
		ЗаголовкиHTTP.Вставить("Proxy-Connection", "Keep-Alive");
		ЗаголовкиHTTP.Вставить("Pragma", "no-cache");
		Если ЗначениеЗаполнено(ПараметрыСоединения.ЗначениеCookie) Тогда
			ЗаголовкиHTTP.Вставить("Cookie", ПараметрыСоединения.ЗначениеCookie);
		КонецЕсли;
		
		ИмяФайлаОтвета = ПолучитьИмяВременногоФайла();
		
		Попытка
			РесурсНаСервере = ДокументооборотСКО.ПолучитьЗаменяемоеЗначениеТестовогоСервера1С("/Index/StatusGrid", "{RPN}/Index/StatusGrid", ПараметрыСоединения.СоединениеHTTP);
			ОтветHTTP = ПараметрыСоединения.СоединениеHTTP.Получить(РесурсНаСервере, ИмяФайлаОтвета, ЗаголовкиHTTP);
		Исключение
			
			ПараметрыСоединения.ПовторятьСоединение = Истина;
			ОперацииСФайламиЭДКО.УдалитьВременныйФайл(ИмяФайлаОтвета);
			Возврат Неопределено;
			
		КонецПопытки;
		
		ФайлОтвета = Новый ЧтениеТекста(ИмяФайлаОтвета, "windows-1251");
		ТекстОтвета = ФайлОтвета.Прочитать();
		ФайлОтвета.Закрыть();
		
		ЗначениеSetCookie = ОтветHTTP.Заголовки.Получить("Set-Cookie");
		ПараметрыСоединения.ЗначениеCookie = ОбновленноеЗначениеCookie(ПараметрыСоединения.ЗначениеCookie, ЗначениеSetCookie);
		
		ОперацииСФайламиЭДКО.УдалитьВременныйФайл(ИмяФайлаОтвета);
		
		ПозицияИдентификатора = СтрНайти(ТекстОтвета, """ticket"":""" + ПараметрыСоединения.ИдентификаторОтправкиНаСервере + """");
		Если ПозицияИдентификатора = 0 И Лев(ПараметрыСоединения.ИдентификаторОтправкиНаСервере, 5) = "*****" Тогда
			ПозицияИдентификатора = СтрНайти(ТекстОтвета, Сред(ПараметрыСоединения.ИдентификаторОтправкиНаСервере, 6) + """,""") - 15;
			Если ПозицияИдентификатора <= 0 Тогда
				ПараметрыСоединения.ОписаниеОшибкиСервером = ТекстОшибкиПорталаРПН(ТекстОтвета);
				Возврат Неопределено;
			КонецЕсли;
		КонецЕсли;
		
		ЗаголовокОтчета = Новый Структура("ИдентификаторЗаписи, ИмяФайла, Состояние, ДатаЗагрузки, ДатаОбновления, ТекстСостояния",
			ПараметрыСоединения.ИдентификаторОтправкиНаСервере, ИмяФайлаВыгрузки, "", Неопределено, Неопределено, "");
		
		ПозицияНачала = 1;
		ПозицияВТексте = ПозицияИдентификатора;
		Пока ПозицияВТексте > 0 Цикл
			Если Сред(ТекстОтвета, ПозицияВТексте, 1) = "{" Тогда
				ПозицияНачала = ПозицияВТексте;
				Прервать;
			КонецЕсли;
			ПозицияВТексте = ПозицияВТексте - 1;
		КонецЦикла;
		
		ОжидаемыйСимволРазделителя = "{";
		СимволРазделителя = "";
		ЧасовойПоясСеанса = ЧасовойПоясСеанса();
		Пока Истина Цикл
			ИмяПараметра = СледующееЗначениеJSON(ТекстОтвета, ПозицияНачала, СимволРазделителя);
			Если СимволРазделителя <> ОжидаемыйСимволРазделителя Тогда
				Прервать;
			КонецЕсли;
			
			ЗначениеПараметра = СледующееЗначениеJSON(ТекстОтвета, ПозицияНачала, СимволРазделителя);
			Если СимволРазделителя <> ":" Тогда
				Прервать;
			КонецЕсли;
			ОжидаемыйСимволРазделителя = ",";
			
			Если ИмяПараметра = "ticket" Тогда
				ЗаголовокОтчета.ИдентификаторЗаписи = ЗначениеПараметра;
			ИначеЕсли ИмяПараметра = "status" Тогда
				ЗаголовокОтчета.Состояние = ТекстИзUTF8(ЗначениеПараметра);
				ЗаголовокОтчета.Состояние = ТекстИзСтрокиJSON(ЗаголовокОтчета.Состояние, " ");
			ИначеЕсли ИмяПараметра = "uploadDate" Тогда
				ЗаголовокОтчета.ДатаЗагрузки = ДатаИзСтрокиРазныхФорматов(ЗначениеПараметра);
				Если ТипЗнч(ЗаголовокОтчета.ДатаЗагрузки) = Тип("Дата") Тогда
					ЗаголовокОтчета.ДатаЗагрузки = МестноеВремя(ЗаголовокОтчета.ДатаЗагрузки, ЧасовойПоясСеанса);
					ЗаголовокОтчета.ДатаЗагрузки = МестноеВремя(ЗаголовокОтчета.ДатаЗагрузки, ЧасовойПоясСеанса); // коррекция ошибки на портале
				КонецЕсли;
			ИначеЕсли ИмяПараметра = "changeDate" Тогда
				ЗаголовокОтчета.ДатаОбновления = ДатаИзСтрокиРазныхФорматов(ЗначениеПараметра);
				Если ТипЗнч(ЗаголовокОтчета.ДатаОбновления) = Тип("Дата") Тогда
					ЗаголовокОтчета.ДатаОбновления = МестноеВремя(ЗаголовокОтчета.ДатаОбновления, ЧасовойПоясСеанса);
					ЗаголовокОтчета.ДатаОбновления = МестноеВремя(ЗаголовокОтчета.ДатаОбновления, ЧасовойПоясСеанса); // коррекция ошибки на портале
				КонецЕсли;
			КонецЕсли;
		КонецЦикла;
		
		Если НЕ ЗначениеЗаполнено(ЗаголовокОтчета.Состояние) Тогда
			ПараметрыСоединения.ОписаниеОшибкиСервером = ТекстОшибкиПорталаРПН(ТекстОтвета);
			Возврат Неопределено;
		КонецЕсли;
		
		Если НЕ ЗначениеЗаполнено(ЗаголовокОтчета.ДатаОбновления) Тогда
			ЗаголовокОтчета.ДатаОбновления = ЗаголовокОтчета.ДатаЗагрузки;
		КонецЕсли;
		
		// чтение подробного комментария
		
		ЗаголовкиHTTP = Новый Соответствие();
		ЗаголовкиHTTP.Вставить("Proxy-Connection", "Keep-Alive");
		ЗаголовкиHTTP.Вставить("Pragma", "no-cache");
		Если ЗначениеЗаполнено(ПараметрыСоединения.ЗначениеCookie) Тогда
			ЗаголовкиHTTP.Вставить("Cookie", ПараметрыСоединения.ЗначениеCookie);
		КонецЕсли;
		
		ИмяФайлаОтвета = ПолучитьИмяВременногоФайла();
		
		Попытка
			РесурсНаСервере = ДокументооборотСКО.ПолучитьЗаменяемоеЗначениеТестовогоСервера1С("/Index/PrintPage?ticket=", 
																								"{RPN}/Index/PrintPage?ticket=", 
																								ПараметрыСоединения.СоединениеHTTP) 
				+ ПараметрыСоединения.ИдентификаторОтправкиНаСервере;
			ОтветHTTP = ПараметрыСоединения.СоединениеHTTP.Получить(РесурсНаСервере,
				ИмяФайлаОтвета, ЗаголовкиHTTP);
		Исключение
			
			ПараметрыСоединения.ПовторятьСоединение = Истина;
			ОперацииСФайламиЭДКО.УдалитьВременныйФайл(ИмяФайлаОтвета);
			Возврат Неопределено;
			
		КонецПопытки;
		
		ФайлОтвета = Новый ЧтениеТекста(ИмяФайлаОтвета, "utf-8");
		ТекстОтвета = ФайлОтвета.Прочитать();
		ФайлОтвета.Закрыть();
		
		ЗначениеSetCookie = ОтветHTTP.Заголовки.Получить("Set-Cookie");
		ПараметрыСоединения.ЗначениеCookie = ОбновленноеЗначениеCookie(ПараметрыСоединения.ЗначениеCookie, ЗначениеSetCookie);
		
		ОперацииСФайламиЭДКО.УдалитьВременныйФайл(ИмяФайлаОтвета);
		
		ЗаголовокОтчета.ТекстСостояния = ТекстПослеПрефикса(ТекстОтвета, "html(marked('", "'))");
		ЗаголовокОтчета.ТекстСостояния = СтрЗаменить(ЗаголовокОтчета.ТекстСостояния, ".\n", ". ");
		ЗаголовокОтчета.ТекстСостояния = СтрЗаменить(ЗаголовокОтчета.ТекстСостояния, "\n", "");
		ЗаголовокОтчета.ТекстСостояния = СтрЗаменить(ЗаголовокОтчета.ТекстСостояния, "### ", "");
		ЗаголовокОтчета.ТекстСостояния = СтрЗаменить(ЗаголовокОтчета.ТекстСостояния, Символы.ВК + Символы.ПС, " ");
		ЗаголовокОтчета.ТекстСостояния = ТекстИзHTML(ЗаголовокОтчета.ТекстСостояния);
		
		Если НЕ ЗначениеЗаполнено(ЗаголовокОтчета.ТекстСостояния) Тогда
			ЗаголовокОтчета.ТекстСостояния = ЗаголовокОтчета.Состояние;
		КонецЕсли;
		
	КонецЕсли;
	
	Возврат ЗаголовокОтчета;
	
КонецФункции

////////////////////////////////////////////////////////////////////////////////
// Служебные функции РПН

Функция СерверПриемаОтчетностиРПН(ОбменНапрямую) Экспорт
	
	ИспользуетсяШифрование = Ложь;
	
	СоединениеHTTP = Неопределено;
	
	Если НЕ ОбменНапрямую Тогда
		
		Если ИспользуетсяШифрование Тогда
			Результат = Новый Структура("Определение, URIПространстваИменСервиса, ИмяСервиса, ИмяТочкиПодключения",
				ДокументооборотСКО.ПолучитьЗаменяемоеЗначениеТестовогоСервера1С("http://213.171.56.46/rpn?wsdl", , СоединениеHTTP), "tns", 
				ДокументооборотСКО.ПолучитьЗаменяемоеЗначениеТестовогоСервера1С("RpnReportService", "{RPN}RpnReportService", СоединениеHTTP), 
				ДокументооборотСКО.ПолучитьЗаменяемоеЗначениеТестовогоСервера1С("RpnReportService", "{RPN}RpnReportServiceSoap", СоединениеHTTP));
			
		Иначе
			Если ДокументооборотСКОВызовСервера.ИспользуетсяРежимТестирования() Тогда
				Результат = Новый Структура("Определение, URIПространстваИменСервиса, ИмяСервиса, ИмяТочкиПодключения",
					ДокументооборотСКО.ПолучитьЗаменяемоеЗначениеТестовогоСервера1С("http://rpn.staging.keydisk.ru/rpn?wsdl", , СоединениеHTTP), "tns",
					ДокументооборотСКО.ПолучитьЗаменяемоеЗначениеТестовогоСервера1С("RpnReportService", "{RPN}RpnReportService", СоединениеHTTP), 
					ДокументооборотСКО.ПолучитьЗаменяемоеЗначениеТестовогоСервера1С("RpnReportService", "{RPN}RpnReportServiceSoap", СоединениеHTTP));
				
			Иначе
				Результат = Новый Структура("Определение, URIПространстваИменСервиса, ИмяСервиса, ИмяТочкиПодключения",
					ДокументооборотСКО.ПолучитьЗаменяемоеЗначениеТестовогоСервера1С("https://rpn.keydisk.ru/rpn?wsdl", , СоединениеHTTP), "tns",
					ДокументооборотСКО.ПолучитьЗаменяемоеЗначениеТестовогоСервера1С("RpnReportService", "{RPN}RpnReportService", СоединениеHTTP), 
					ДокументооборотСКО.ПолучитьЗаменяемоеЗначениеТестовогоСервера1С("RpnReportService", "{RPN}RpnReportServiceSoap", СоединениеHTTP));
			КонецЕсли;
		КонецЕсли;
		
		Возврат Результат;

	Иначе
	
		Если ДокументооборотСКОВызовСервера.ИспользуетсяРежимТестирования() Тогда
		// Возвращаем адрес тестового сервера
		Возврат ДокументооборотСКО.ПолучитьЗаменяемоеЗначениеТестовогоСервера1С("rpn.gov.ru:8090", , СоединениеHTTP);
		
	Иначе
		// Возвращаем адрес реального сервера
		Возврат ДокументооборотСКО.ПолучитьЗаменяемоеЗначениеТестовогоСервера1С("pnv-rpn.ru", , СоединениеHTTP);
		
	КонецЕсли;
	
	КонецЕсли;
	
КонецФункции

Функция НастройкиРПН(Знач ОрганизацияСсылка, Знач ПолучитьПараметрыОнлайнПроверки = Ложь) Экспорт
	
	Результат = Новый Структура("ИспользоватьОбмен, СертификатАбонентаОтпечаток, СертификатРПНОтпечаток, EMail, Пароль, ИспользоватьАвтонастройку, ОбменНапрямую", Ложь, "", "", "", "", Ложь, Ложь);
	
	Если ПолучитьПараметрыОнлайнПроверки Тогда
		СвойстваОрганизации = ЗначенияРеквизитовОбъекта(ОрганизацияСсылка, "ВидОбменаСКонтролирующимиОрганами, УчетнаяЗаписьОбмена");
		
		Результат.Вставить("УчетнаяЗаписьОбмена", СвойстваОрганизации.УчетнаяЗаписьОбмена);
		Результат.Вставить("НастроенОбменВУниверсальномФормате", СвойстваОрганизации.ВидОбменаСКонтролирующимиОрганами = Перечисления.ВидыОбменаСКонтролирующимиОрганами.ОбменВУниверсальномФормате
			И ЗначениеЗаполнено(СвойстваОрганизации.УчетнаяЗаписьОбмена));
		
		Попытка
			ОбменИОнлайнПроверкаВключены = Результат.НастроенОбменВУниверсальномФормате И НЕ СвойстваОрганизации.УчетнаяЗаписьОбмена.ОбменНапрямую И СвойстваОрганизации.УчетнаяЗаписьОбмена.ИспользоватьСервисОнлайнПроверкиОтчетов;
		Исключение
			ОбменИОнлайнПроверкаВключены = Ложь;
		КонецПопытки;
		
		ОнлайнПроверкаКонтролирующиеОрганы = ?(ОбменИОнлайнПроверкаВключены,
			?(СвойстваОрганизации.УчетнаяЗаписьОбмена.СпецоператорСвязи = Перечисления.СпецоператорыСвязи.Такском,
			"ФНС;ПФР;ФСС;ФСРАР;ФТС",
			ЭлектронныйДокументооборотСКонтролирующимиОрганамиВызовСервера.ПолучитьПараметрСпецоператора(
			СвойстваОрганизации.УчетнаяЗаписьОбмена.СпецоператорСвязи, "ОнлайнПроверкаКонтролирующиеОрганы")), "");
		ОнлайнПроверкаКонтролирующиеОрганы = СокрЛП(ОнлайнПроверкаКонтролирующиеОрганы);
		ОнлайнПроверкаКонтролирующиеОрганы = ВРег(ОнлайнПроверкаКонтролирующиеОрганы);
		
		ОнлайнПроверкаДоступна = СтрНайти(";" + ОнлайнПроверкаКонтролирующиеОрганы + ";", ";РПН;") > 0;
		
		Результат.Вставить("ОнлайнПроверкаДоступна", ОнлайнПроверкаДоступна);
	КонецЕсли;
	
	Запрос = Новый Запрос;
	Запрос.Текст = "ВЫБРАТЬ РАЗРЕШЕННЫЕ ПЕРВЫЕ 1
				   |	НастройкиОбменаРПН.ИспользоватьОбмен КАК ИспользоватьОбмен,
				   |	НастройкиОбменаРПН.СертификатАбонентаОтпечаток КАК СертификатАбонентаОтпечаток,
				   |	НастройкиОбменаРПН.СертификатРПНОтпечаток КАК СертификатРПНОтпечаток,
				   |	НастройкиОбменаРПН.EMail КАК EMail,
				   |	НастройкиОбменаРПН.Пароль КАК Пароль,
				   |	НастройкиОбменаРПН.ИспользоватьАвтонастройку КАК ИспользоватьАвтонастройку,
				   |	НастройкиОбменаРПН.ОбменНапрямую КАК ОбменНапрямую
				   |ИЗ
				   |	РегистрСведений.НастройкиОбменаРПН КАК НастройкиОбменаРПН
				   |ГДЕ
				   |	НастройкиОбменаРПН.Организация = &ОрганизацияСсылка";
	Запрос.УстановитьПараметр("ОрганизацияСсылка", ОрганизацияСсылка);
	РезультатЗапроса = Запрос.Выполнить();
	Если НЕ РезультатЗапроса.Пустой() Тогда
		ЗаписьЗапроса = РезультатЗапроса.Выгрузить()[0];
		Если ЗаписьЗапроса.ИспользоватьОбмен Тогда
			
			Результат.ИспользоватьОбмен = ЗаписьЗапроса.ИспользоватьОбмен;
			Результат.СертификатАбонентаОтпечаток = СокрЛП(ЗаписьЗапроса.СертификатАбонентаОтпечаток);
			Результат.СертификатРПНОтпечаток = СокрЛП(ЗаписьЗапроса.СертификатРПНОтпечаток);
			Результат.EMail = СокрЛП(ЗаписьЗапроса.EMail);
			Результат.Пароль = СокрЛП(ЗаписьЗапроса.Пароль);
			Результат.ИспользоватьАвтонастройку = ЗаписьЗапроса.ИспользоватьАвтонастройку;
			Результат.ОбменНапрямую = (ЗаписьЗапроса.ОбменНапрямую = Истина);
			
		КонецЕсли;
	КонецЕсли;
	
	Возврат Результат;
	
КонецФункции

Функция СвойстваОбменаОрганизацииДляРПН(Знач ОрганизацияСсылка, Знач ОпределитьНастроенностьОбменаВУниверсальномФормате, Знач ПолучитьОсновныеСвойстваПоследнейОтправки, Знач ОтправкаСсылка = Неопределено, ОтчетСсылка = Неопределено, ПараметрыПолученияИнформацииОбОтчете = Неопределено, СвойстваПоследнейОтправки = Неопределено) Экспорт
	
	Результат = Новый Структура;
	Результат.Вставить("НастроенОбменВУниверсальномФормате", 	Неопределено);
	Результат.Вставить("СвойстваОтправки", 						Неопределено);
	Результат.Вставить("НастройкиОбмена", 						Неопределено);
	Результат.Вставить("ОрганизацияИзОтправки", 				Неопределено);
	Результат.Вставить("ПравоИзмененияУчетнойЗаписи", 			Неопределено);
	Результат.Вставить("ЭтоРегламентированныйОтчет", 			Неопределено);
	Результат.Вставить("ЭтоРегламентированноеУведомление", 		Неопределено);
	Результат.Вставить("ЭтоЭлектроннаяПодписьВМоделиСервиса", 	Неопределено);
	Результат.Вставить("ЭтоНеподписываемыйОтчет", 				Неопределено);
	Результат.Вставить("БумажнаяПодачаОтчета", 					Неопределено);
	
	Если ОрганизацияСсылка = Неопределено Тогда
		
		ОрганизацияСсылка = ОтправкаСсылка.Организация;
		Результат.ОрганизацияИзОтправки = ОрганизацияСсылка;
		
	КонецЕсли;
	
	Результат.ЭтоЭлектроннаяПодписьВМоделиСервиса = ЭлектроннаяПодписьВМоделиСервисаБРОВызовСервера.ЭтоЭлектроннаяПодписьВМоделиСервиса(ОрганизацияСсылка);
	
	Если ОпределитьНастроенностьОбменаВУниверсальномФормате Тогда
		
		СвойстваОрганизации = ЗначенияРеквизитовОбъекта(ОрганизацияСсылка, "ВидОбменаСКонтролирующимиОрганами, УчетнаяЗаписьОбмена");
		
		Результат.НастроенОбменВУниверсальномФормате = (СвойстваОрганизации.ВидОбменаСКонтролирующимиОрганами = Перечисления.ВидыОбменаСКонтролирующимиОрганами.ОбменВУниверсальномФормате
			И ЗначениеЗаполнено(СвойстваОрганизации.УчетнаяЗаписьОбмена));
		
	КонецЕсли;
	
	Если ПолучитьОсновныеСвойстваПоследнейОтправки Тогда
		
		Если ОтчетСсылка <> Неопределено Тогда
		СвойстваОтправки = ПолучитьОсновныеСвойстваПоследнейОтправкиОтчетаВРПН(ОтчетСсылка);
		Если СвойстваОтправки <> Неопределено Тогда
				Результат.СвойстваОтправки = Новый Структура("Ссылка, Идентификатор, ИмяФайлаПакета, Статус, СтатусПроверкиНаПортале, ИдентификаторОтправкиОператору",
					СвойстваОтправки.Ссылка, СвойстваОтправки.Идентификатор, СвойстваОтправки.ИмяФайлаПакета, СвойстваОтправки.Статус, СвойстваОтправки.СтатусПроверкиНаПортале, СвойстваОтправки.ИдентификаторОтправкиОператору);
			КонецЕсли;
			
		ИначеЕсли ОтправкаСсылка <> Неопределено Тогда
			Результат.СвойстваОтправки = Новый Структура("Ссылка, Идентификатор, ИмяФайлаПакета, Статус, СтатусПроверкиНаПортале, ИдентификаторОтправкиОператору",
				ОтправкаСсылка.Ссылка, ОтправкаСсылка.ИдентификаторОтправкиНаСервере, ОтправкаСсылка.ИмяФайлаПакета, ОтправкаСсылка.СтатусОтправки, ОтправкаСсылка.СтатусПроверкиНаПортале, ОтправкаСсылка.ИдентификаторОтправкиОператору);
		КонецЕсли;
		
		Если Результат.СвойстваОтправки <> Неопределено И НЕ ЗначениеЗаполнено(Результат.СвойстваОтправки.ИдентификаторОтправкиОператору) Тогда
			Результат.СвойстваОтправки.ИдентификаторОтправкиОператору = 0;
		КонецЕсли;
		
	КонецЕсли;
	
	Результат.НастройкиОбмена = НастройкиРПН(ОрганизацияСсылка);
	
	Результат.ПравоИзмененияУчетнойЗаписи = ПравоДоступа("Изменение", Метаданные.Справочники.УчетныеЗаписиДокументооборота);
	
	Если ОтчетСсылка <> Неопределено ИЛИ ОтправкаСсылка <> Неопределено Тогда
		
		СсылкаНаОтчет = ?(ОтчетСсылка <> Неопределено, ОтчетСсылка, ОтправкаСсылка.ОтчетСсылка);
		ТипЗнчСсылкаНаОтчет = ТипЗнч(СсылкаНаОтчет);
		Результат.ЭтоРегламентированныйОтчет = (ТипЗнчСсылкаНаОтчет = Тип("ДокументСсылка.РегламентированныйОтчет") ИЛИ ТипЗнчСсылкаНаОтчет = Тип("Неопределено"));
		Результат.ЭтоРегламентированноеУведомление = (ТипЗнчСсылкаНаОтчет = Тип("ДокументСсылка.УведомлениеОСпецрежимахНалогообложения"));
		
		Если ПараметрыПолученияИнформацииОбОтчете = Неопределено ИЛИ НЕ ПараметрыПолученияИнформацииОбОтчете.ОтключитьОпределениеНеподписываемыхОтчетов Тогда
			Если Результат.ЭтоРегламентированныйОтчет Тогда
				ВидОтчета = Справочники.ВидыОтправляемыхДокументов.НайтиПоРеквизиту("Источник", СсылкаНаОтчет.ИсточникОтчета);
				
			ИначеЕсли Результат.ЭтоРегламентированноеУведомление Тогда
				ВидОтчета = ЭлектронныйДокументооборотСКонтролирующимиОрганамиВызовСервера.ПолучитьВидОтправляемогоДокументаПоСсылке(СсылкаНаОтчет);
				
			Иначе // электронные представления
				ВидОтчета = СсылкаНаОтчет.ВидОтчета;
			КонецЕсли;
			
			Результат.БумажнаяПодачаОтчета = (ВидОтчета = Справочники.ВидыОтправляемыхДокументов.ОтходыСубъектовМСП);
		КонецЕсли;
		
	ИначеЕсли ПараметрыПолученияИнформацииОбОтчете <> Неопределено И НЕ ПараметрыПолученияИнформацииОбОтчете.ОтключитьОпределениеНеподписываемыхОтчетов Тогда
		
		Если НЕ ЗначениеЗаполнено(ПараметрыПолученияИнформацииОбОтчете.ВидОтчетаФормыЭлектронногоПредставления) Тогда
			Результат.БумажнаяПодачаОтчета = (СтрНайти(ПараметрыПолученияИнформацииОбОтчете.ИмяФормы, "ОтходыСубъектовМСП") > 0);
			
		Иначе // электронные представления
			Результат.БумажнаяПодачаОтчета = (ПараметрыПолученияИнформацииОбОтчете.ВидОтчетаФормыЭлектронногоПредставления = Справочники.ВидыОтправляемыхДокументов.ОтходыСубъектовМСП);
		КонецЕсли;
		
		КонецЕсли;
		
	Если ПараметрыПолученияИнформацииОбОтчете = Неопределено ИЛИ НЕ ПараметрыПолученияИнформацииОбОтчете.ОтключитьОпределениеНеподписываемыхОтчетов Тогда
		СвойстваОтправки = ?(Результат.СвойстваОтправки <> Неопределено, Результат.СвойстваОтправки, СвойстваПоследнейОтправки);
		ОбменНапрямую = (СвойстваОтправки = Неопределено И Результат.НастройкиОбмена.ОбменНапрямую) ИЛИ (СвойстваОтправки <> Неопределено И СвойстваОтправки.ИдентификаторОтправкиОператору = 0);
		Результат.ЭтоНеподписываемыйОтчет = ОбменНапрямую И Результат.БумажнаяПодачаОтчета;
	КонецЕсли;
	
	Возврат Результат;
	
КонецФункции

Процедура ЗаписатьEMailИПарольПорталаРПН(Организация, EMail = Неопределено, Пароль = "")
	
	Попытка
		
		МенеджерЗаписи = РегистрыСведений.НастройкиОбменаРПН.СоздатьМенеджерЗаписи();
		МенеджерЗаписи.Организация = Организация;
		МенеджерЗаписи.Прочитать();
		
		Если МенеджерЗаписи.Выбран() Тогда
			Если МенеджерЗаписи.ИспользоватьОбмен <> Истина Тогда
				МенеджерЗаписи.ИспользоватьОбмен = Истина;
				МенеджерЗаписи.ИспользоватьАвтонастройку = Ложь;
			КонецЕсли;
			Если EMail <> Неопределено Тогда
				МенеджерЗаписи.EMail = EMail;
			КонецЕсли;
			МенеджерЗаписи.Пароль = Пароль;
			МенеджерЗаписи.Записать();
			
		ИначеЕсли ЗначениеЗаполнено(EMail) ИЛИ ЗначениеЗаполнено(Пароль) Тогда
			МенеджерЗаписи.Организация = Организация;
			МенеджерЗаписи.ИспользоватьОбмен = Истина;
			МенеджерЗаписи.СертификатАбонентаОтпечаток = "";
			МенеджерЗаписи.СертификатРПНОтпечаток = "";
			МенеджерЗаписи.ИспользоватьАвтонастройку = Ложь;
			МенеджерЗаписи.EMail = Строка(EMail);
			МенеджерЗаписи.Пароль = Пароль;
			МенеджерЗаписи.Записать();
		КонецЕсли;
		
	Исключение
	КонецПопытки;
	
КонецПроцедуры

Функция УспешноОбработанОтчетРПН(ИдентификаторЗаписи, СтрокаСостояния, ПройденаПроверкаНаПортале = Неопределено)
	
	Если СтрокаСостояния = "Обработан"
		ИЛИ Лев(ИдентификаторЗаписи, 5) <> "*****" И ЗначениеЗаполнено(СтрокаСостояния) Тогда
		
		ПройденаПроверкаНаПортале = Истина;
		
	Иначе
		ПройденаПроверкаНаПортале = Неопределено;
	КонецЕсли;
	
	ПрефиксОшибка = "Ошибка";
	ДлинаПрефиксаОшибка = СтрДлина(ПрефиксОшибка);
	ПрефиксОтклонен = "Отклон";
	ДлинаПрефиксаОтклонен = СтрДлина(ПрефиксОтклонен);
	ПрефиксНеПринят = "Не принят";
	ДлинаПрефиксаНеПринят = СтрДлина(ПрефиксНеПринят);
	ПрефиксНеУтвержден = "Не утвержд";
	ДлинаПрефиксаНеУтвержден = СтрДлина(ПрефиксНеУтвержден);
	ПрефиксНекорректный = "Некорректн";
	ДлинаПрефиксаНекорректный = СтрДлина(ПрефиксНекорректный);
	ПрефиксОтменен = "Отмен";
	ДлинаПрефиксаОтменен = СтрДлина(ПрефиксОтменен);
	ПрефиксУдален = "Удал";
	ДлинаПрефиксаУдален = СтрДлина(ПрефиксУдален);
	ПрефиксОтчетОтклонен = "Отчет отклон";
	ДлинаПрефиксаОтчетОтклонен = СтрДлина(ПрефиксОтчетОтклонен);
	ПрефиксОтчетОтклоненАльтернативно = "Отчёт отклон";
	ДлинаПрефиксаОтчетОтклоненАльтернативно = СтрДлина(ПрефиксОтчетОтклоненАльтернативно);
	ПрефиксОтчетНеПринят = "Отчет не принят";
	ДлинаПрефиксаОтчетНеПринят = СтрДлина(ПрефиксОтчетНеПринят);
	ПрефиксОтчетНеПринятАльтернативно = "Отчёт не принят";
	ДлинаПрефиксаОтчетНеПринятАльтернативно = СтрДлина(ПрефиксОтчетНеПринятАльтернативно);
	ПрефиксОтчетНеУтвержден = "Отчет не утвержд";
	ДлинаПрефиксаОтчетНеУтвержден = СтрДлина(ПрефиксОтчетНеУтвержден);
	ПрефиксОтчетНеУтвержденАльтернативно = "Отчёт не утвержд";
	ДлинаПрефиксаОтчетНеУтвержденАльтернативно = СтрДлина(ПрефиксОтчетНеУтвержденАльтернативно);
	ПрефиксОтчетОтменен = "Отчет отмен";
	ДлинаПрефиксаОтчетОтменен = СтрДлина(ПрефиксОтчетОтменен);
	ПрефиксОтчетОтмененАльтернативно = "Отчёт отмен";
	ДлинаПрефиксаОтчетОтмененАльтернативно = СтрДлина(ПрефиксОтчетОтмененАльтернативно);
	ПрефиксОтчетУдален = "Отчет удал";
	ДлинаПрефиксаОтчетУдален = СтрДлина(ПрефиксОтчетУдален);
	ПрефиксОтчетУдаленАльтернативно = "Отчёт удал";
	ДлинаПрефиксаОтчетУдаленАльтернативно = СтрДлина(ПрефиксОтчетУдаленАльтернативно);
	ПрефиксЗаявкаУстарела = "Заявка устарела";
	ДлинаПрефиксаЗаявкаУстарела = СтрДлина(ПрефиксЗаявкаУстарела);
	ПрефиксЗаявкаНеПрошла = "Заявка не прошла";
	ДлинаПрефиксаЗаявкаНеПрошла = СтрДлина(ПрефиксЗаявкаНеПрошла);
	ПрефиксУтвержден = "Утвержд";
	ДлинаПрефиксаУтвержден = СтрДлина(ПрефиксУтвержден);
	ПрефиксПринят = "Принят";
	ДлинаПрефиксаПринят = СтрДлина(ПрефиксПринят);
	ПрефиксСдан = "Сдан";
	ДлинаПрефиксаСдан = СтрДлина(ПрефиксСдан);
	ПрефиксОтчетУтвержден = "Отчет утвержд";
	ДлинаПрефиксаОтчетУтвержден = СтрДлина(ПрефиксОтчетУтвержден);
	ПрефиксОтчетУтвержденАльтернативно = "Отчёт утвержд";
	ДлинаПрефиксаОтчетУтвержденАльтернативно = СтрДлина(ПрефиксОтчетУтвержденАльтернативно);
	ПрефиксОтчетПринят = "Отчет принят";
	ДлинаПрефиксаОтчетПринят = СтрДлина(ПрефиксОтчетПринят);
	ПрефиксОтчетПринятАльтернативно = "Отчёт принят";
	ДлинаПрефиксаОтчетПринятАльтернативно = СтрДлина(ПрефиксОтчетПринятАльтернативно);
	ПрефиксОтчетСдан = "Отчет сдан";
	ДлинаПрефиксаОтчетСдан = СтрДлина(ПрефиксОтчетСдан);
	ПрефиксОтчетСданАльтернативно = "Отчёт сдан";
	ДлинаПрефиксаОтчетСданАльтернативно = СтрДлина(ПрефиксОтчетСданАльтернативно);
	ПрефиксОтчетЗагруженВоФГИС = "Отчет загружен во ФГИС";
	ДлинаПрефиксаОтчетЗагруженВоФГИС = СтрДлина(ПрефиксОтчетЗагруженВоФГИС);
	ПрефиксОтчетЗагруженВоФГИСАльтернативно = "Отчёт загружен во ФГИС";
	ДлинаПрефиксаОтчетЗагруженВоФГИСАльтернативно = СтрДлина(ПрефиксОтчетЗагруженВоФГИСАльтернативно);
	ПрефиксОтчетСЭПЗагруженВоФГИС = "Отчет с ЭП принят и загружен во ФГИС";
	ДлинаПрефиксаОтчетСЭПЗагруженВоФГИС = СтрДлина(ПрефиксОтчетСЭПЗагруженВоФГИС);
	ПрефиксОтчетСЭПЗагруженВоФГИСАльтернативно = "Отчёт с ЭП принят и загружен во ФГИС";
	ДлинаПрефиксаОтчетСЭПЗагруженВоФГИСАльтернативно = СтрДлина(ПрефиксОтчетСЭПЗагруженВоФГИСАльтернативно);
	ПрефиксЗаявкаУтверждена = "Заявка утвержд";
	ДлинаПрефиксаЗаявкаУтверждена = СтрДлина(ПрефиксЗаявкаУтверждена);
	ПрефиксЗаявкаПринята = "Заявка принят";
	ДлинаПрефиксаЗаявкаПринята = СтрДлина(ПрефиксЗаявкаПринята);
	ПрефиксЗаявкаЗагружена = "Заявка загружен";
	ДлинаПрефиксаЗаявкаЗагружена = СтрДлина(ПрефиксЗаявкаЗагружена);
	
	Если Лев(СтрокаСостояния, ДлинаПрефиксаОшибка) = ПрефиксОшибка
		ИЛИ Лев(СтрокаСостояния, ДлинаПрефиксаОтклонен) = ПрефиксОтклонен
		ИЛИ Лев(СтрокаСостояния, ДлинаПрефиксаНеПринят) = ПрефиксНеПринят
		ИЛИ Лев(СтрокаСостояния, ДлинаПрефиксаНеУтвержден) = ПрефиксНеУтвержден
		ИЛИ Лев(СтрокаСостояния, ДлинаПрефиксаНекорректный) = ПрефиксНекорректный
		ИЛИ Лев(СтрокаСостояния, ДлинаПрефиксаОтменен) = ПрефиксОтменен
		ИЛИ Лев(СтрокаСостояния, ДлинаПрефиксаУдален) = ПрефиксУдален
		ИЛИ Лев(СтрокаСостояния, ДлинаПрефиксаОтчетОтклонен) = ПрефиксОтчетОтклонен
		ИЛИ Лев(СтрокаСостояния, ДлинаПрефиксаОтчетОтклоненАльтернативно) = ПрефиксОтчетОтклоненАльтернативно
		ИЛИ Лев(СтрокаСостояния, ДлинаПрефиксаОтчетНеПринят) = ПрефиксОтчетНеПринят
		ИЛИ Лев(СтрокаСостояния, ДлинаПрефиксаОтчетНеПринятАльтернативно) = ПрефиксОтчетНеПринятАльтернативно
		ИЛИ Лев(СтрокаСостояния, ДлинаПрефиксаОтчетНеУтвержден) = ПрефиксОтчетНеУтвержден
		ИЛИ Лев(СтрокаСостояния, ДлинаПрефиксаОтчетНеУтвержденАльтернативно) = ПрефиксОтчетНеУтвержденАльтернативно
		ИЛИ Лев(СтрокаСостояния, ДлинаПрефиксаОтчетОтменен) = ПрефиксОтчетОтменен
		ИЛИ Лев(СтрокаСостояния, ДлинаПрефиксаОтчетОтмененАльтернативно) = ПрефиксОтчетОтмененАльтернативно
		ИЛИ Лев(СтрокаСостояния, ДлинаПрефиксаОтчетУдален) = ПрефиксОтчетУдален
		ИЛИ Лев(СтрокаСостояния, ДлинаПрефиксаОтчетУдаленАльтернативно) = ПрефиксОтчетУдаленАльтернативно
		ИЛИ Лев(СтрокаСостояния, ДлинаПрефиксаЗаявкаУстарела) = ПрефиксЗаявкаУстарела
		ИЛИ Лев(СтрокаСостояния, ДлинаПрефиксаЗаявкаНеПрошла) = ПрефиксЗаявкаНеПрошла Тогда
		
		Если ПройденаПроверкаНаПортале <> Истина Тогда
			ПройденаПроверкаНаПортале = Ложь;
		КонецЕсли;
		Возврат Ложь;
		
	ИначеЕсли (Лев(СтрокаСостояния, ДлинаПрефиксаУтвержден) = ПрефиксУтвержден
		ИЛИ Лев(СтрокаСостояния, ДлинаПрефиксаПринят) = ПрефиксПринят
		ИЛИ Лев(СтрокаСостояния, ДлинаПрефиксаСдан) = ПрефиксСдан
		ИЛИ Лев(СтрокаСостояния, ДлинаПрефиксаОтчетУтвержден) = ПрефиксОтчетУтвержден
		ИЛИ Лев(СтрокаСостояния, ДлинаПрефиксаОтчетУтвержденАльтернативно) = ПрефиксОтчетУтвержденАльтернативно
		ИЛИ Лев(СтрокаСостояния, ДлинаПрефиксаОтчетПринят) = ПрефиксОтчетПринят
		ИЛИ Лев(СтрокаСостояния, ДлинаПрефиксаОтчетПринятАльтернативно) = ПрефиксОтчетПринятАльтернативно
		ИЛИ Лев(СтрокаСостояния, ДлинаПрефиксаОтчетСдан) = ПрефиксОтчетСдан
		ИЛИ Лев(СтрокаСостояния, ДлинаПрефиксаОтчетСданАльтернативно) = ПрефиксОтчетСданАльтернативно
		ИЛИ Лев(СтрокаСостояния, ДлинаПрефиксаОтчетЗагруженВоФГИС) = ПрефиксОтчетЗагруженВоФГИС
		ИЛИ Лев(СтрокаСостояния, ДлинаПрефиксаОтчетЗагруженВоФГИСАльтернативно) = ПрефиксОтчетЗагруженВоФГИСАльтернативно
		ИЛИ Лев(СтрокаСостояния, ДлинаПрефиксаОтчетСЭПЗагруженВоФГИС) = ПрефиксОтчетСЭПЗагруженВоФГИС
		ИЛИ Лев(СтрокаСостояния, ДлинаПрефиксаОтчетСЭПЗагруженВоФГИСАльтернативно) = ПрефиксОтчетСЭПЗагруженВоФГИСАльтернативно
		ИЛИ Лев(СтрокаСостояния, ДлинаПрефиксаЗаявкаУтверждена) = ПрефиксЗаявкаУтверждена
		ИЛИ Лев(СтрокаСостояния, ДлинаПрефиксаЗаявкаПринята) = ПрефиксЗаявкаПринята
		ИЛИ Лев(СтрокаСостояния, ДлинаПрефиксаЗаявкаЗагружена) = ПрефиксЗаявкаЗагружена) И ПройденаПроверкаНаПортале = Истина Тогда
		
		Возврат Истина;
		
	Иначе
		Возврат Неопределено;
	КонецЕсли;
	
КонецФункции

Функция ОчиститьФорматированиеHTML(ТекстСообщения)
	
	Результат = ТекстСообщения;
	Результат = СтрЗаменить(Результат, "<b>", "");
	Результат = СтрЗаменить(Результат, "</b>", "");
	Результат = СтрЗаменить(Результат, "<B>", "");
	Результат = СтрЗаменить(Результат, "</B>", "");
	Результат = СтрЗаменить(Результат, "<i>", "");
	Результат = СтрЗаменить(Результат, "</i>", "");
	Результат = СтрЗаменить(Результат, "<I>", "");
	Результат = СтрЗаменить(Результат, "</I>", "");
	Результат = СтрЗаменить(Результат, "<br>", Символы.ВК + Символы.ПС);
	Результат = СтрЗаменить(Результат, "<BR>", Символы.ВК + Символы.ПС);
	Возврат Результат;
	
КонецФункции

Функция ЕстьВозможностьАвтонастройкиВУниверсальномФормате(Организация, ВернутьСвойстваОрганизацииИУчетнойЗаписи = Ложь) Экспорт
	
	Результат 						= Ложь;
	ВозможностьАвтонастройкиПоРОКИ 	= Ложь;
	ВозможностьАвтонастройки 		= Ложь;
	СвойстваОрганизации 			= Неопределено;
	СвойстваУчетнойЗаписи 			= Неопределено;
	Если Организация <> Неопределено И Организация <> Справочники.Организации.ПустаяСсылка() Тогда
		СвойстваОрганизации = ЗначенияРеквизитовОбъекта(Организация, "ВидОбменаСКонтролирующимиОрганами, УчетнаяЗаписьОбмена");
		Если СвойстваОрганизации <> Неопределено И СвойстваОрганизации.ВидОбменаСКонтролирующимиОрганами = Перечисления.ВидыОбменаСКонтролирующимиОрганами.ОбменВУниверсальномФормате
			И ЗначениеЗаполнено(СвойстваОрганизации.УчетнаяЗаписьОбмена) Тогда
			
			Попытка
				СвойстваУчетнойЗаписи = ЗначенияРеквизитовОбъекта(СвойстваОрганизации.УчетнаяЗаписьОбмена, "СпецоператорСвязи, РежимАвтонастройки");
				
				Результат = СвойстваУчетнойЗаписи.СпецоператорСвязи <> Перечисления.СпецоператорыСвязи.Такском
					И СвойстваУчетнойЗаписи.СпецоператорСвязи <> Перечисления.СпецоператорыСвязи.Прочие
					И СвойстваУчетнойЗаписи.РежимАвтонастройки <> Перечисления.РежимыАвтонастройкиУчетнойЗаписиНалогоплательщика.Отключена;
				ВозможностьАвтонастройкиПоРОКИ = СвойстваУчетнойЗаписи.СпецоператорСвязи = Перечисления.СпецоператорыСвязи.Такском
					И СвойстваУчетнойЗаписи.РежимАвтонастройки <> Перечисления.РежимыАвтонастройкиУчетнойЗаписиНалогоплательщика.Отключена;
				ВозможностьАвтонастройки = Результат ИЛИ ВозможностьАвтонастройкиПоРОКИ;
			Исключение
			КонецПопытки;
		КонецЕсли;
	КонецЕсли;
	
	Если ВернутьСвойстваОрганизацииИУчетнойЗаписи Тогда
		СвойстваОрганизацииИУчетнойЗаписи = Новый Структура;
		СвойстваОрганизацииИУчетнойЗаписи.Вставить("ЕстьВозможностьАвтонастройкиВУниверсальномФормате", Результат);
		СвойстваОрганизацииИУчетнойЗаписи.Вставить("ЕстьВозможностьАвтонастройкиПоРОКИ", 				ВозможностьАвтонастройкиПоРОКИ);
		СвойстваОрганизацииИУчетнойЗаписи.Вставить("ЕстьВозможностьАвтонастройки", 						ВозможностьАвтонастройки);
		СвойстваОрганизацииИУчетнойЗаписи.Вставить("ВидОбменаСКонтролирующимиОрганами", 				Неопределено);
		СвойстваОрганизацииИУчетнойЗаписи.Вставить("УчетнаяЗаписьОбмена", 								Неопределено);
		СвойстваОрганизацииИУчетнойЗаписи.Вставить("СпецоператорСвязи", 								Неопределено);
		СвойстваОрганизацииИУчетнойЗаписи.Вставить("РежимАвтонастройки", 								Неопределено);
		
		Если СвойстваОрганизации <> Неопределено Тогда
			ЗаполнитьЗначенияСвойств(СвойстваОрганизацииИУчетнойЗаписи, СвойстваОрганизации);
		КонецЕсли;
		Если СвойстваУчетнойЗаписи <> Неопределено Тогда
			ЗаполнитьЗначенияСвойств(СвойстваОрганизацииИУчетнойЗаписи, СвойстваУчетнойЗаписи);
		КонецЕсли;
		
		Возврат СвойстваОрганизацииИУчетнойЗаписи;
		
	Иначе
		Возврат Результат;
	КонецЕсли;
	
КонецФункции

Функция ПолучитьПараметрыИнтернета() Экспорт
	
	ПараметрыСервераИнтернета = Новый Структура("URLСервера, КаталогФайловНаСервере, ИмяКлючевогоФайлаНаСервере");
	ПараметрыСервераИнтернета.URLСервера = "downloads.1c.ru";
	ПараметрыСервераИнтернета.КаталогФайловНаСервере = "/RO_OnlineServices/XCHG_FNS";
	Если ДокументооборотСКОВызовСервера.ИспользуетсяРежимТестирования() Тогда
		ПараметрыСервераИнтернета.КаталогФайловНаСервере = ПараметрыСервераИнтернета.КаталогФайловНаСервере + "/Test"; 
	КонецЕсли;
	ПараметрыСервераИнтернета.ИмяКлючевогоФайлаНаСервере = "/infomap.dat";
	Возврат ПараметрыСервераИнтернета;
	
КонецФункции

Функция КнопкаОбновленияВЗаявленииНаПодключениеДоступна(Ссылка) Экспорт

	Отправлено = ЗначениеЗаполнено(Ссылка.Статус)
		И Ссылка.Статус = Перечисления.СтатусыЗаявленияАбонентаСпецоператораСвязи.Отправлено;
		
	ОдобреноНастройкаНЕЗавершена = 
		Ссылка.Статус = Перечисления.СтатусыЗаявленияАбонентаСпецоператораСвязи.Одобрено


		И НЕ Ссылка.НастройкаЗавершена;
		
	ОтклоненоНастройкаНЕЗавершена = 
		Ссылка.Статус = Перечисления.СтатусыЗаявленияАбонентаСпецоператораСвязи.Отклонено
		И НЕ Ссылка.НастройкаЗавершена;
		
	ОдобреноНастройкиЭДОНЕЗавершена = 
		Ссылка.Статус = Перечисления.СтатусыЗаявленияАбонентаСпецоператораСвязи.Одобрено
		И Ссылка.НастройкаЗавершена 
		И НЕ Ссылка.НастройкаЭДОЗавершена 
		И ЗначениеЗаполнено(Ссылка.НастройкиЭДО);
	
	КнопкаДоступна = 
		Отправлено 
		ИЛИ ОдобреноНастройкаНЕЗавершена 
		ИЛИ ОтклоненоНастройкаНЕЗавершена 
		ИЛИ ОдобреноНастройкиЭДОНЕЗавершена;
	
	Возврат КнопкаДоступна;
	
КонецФункции

Функция ПолучитьКодВидаДокументаФизическогоЛицаПредопределенного(Ссылка) Экспорт
	
	ИмяПредопределенного = Справочники.ВидыДокументовФизическихЛиц.ПолучитьИмяПредопределенного(Ссылка);
	
	Если ИмяПредопределенного = "ПаспортРФ" Тогда
		Возврат "21";
	ИначеЕсли ИмяПредопределенного = "ВодительскоеУдостоверение" Тогда
		Возврат "91";
	ИначеЕсли ИмяПредопределенного = "ВоенныйБилет" Тогда
		Возврат "07";
	ИначеЕсли ИмяПредопределенного = "ВоенныйБилетОфицераЗапаса" Тогда
		Возврат "27";
	ИначеЕсли ИмяПредопределенного = "ДипломатическийПаспорт" Тогда
		Возврат "09";
	ИначеЕсли ИмяПредопределенного = "ЗагранпаспортРФ" Тогда
		Возврат "22";
	ИначеЕсли ИмяПредопределенного = "ЗагранпаспортСССР" Тогда
		Возврат "02";
	ИначеЕсли ИмяПредопределенного = "ПаспортСССР" Тогда
		Возврат "01";
	ИначеЕсли ИмяПредопределенного = "ПаспортМинморфлота" Тогда
		Возврат "06";
	ИначеЕсли ИмяПредопределенного = "ПаспортМоряка" Тогда
		Возврат "26";
	ИначеЕсли ИмяПредопределенного = "СвидетельствоОРождении" Тогда
		Возврат "03";
	ИначеЕсли ИмяПредопределенного = "УдостоверениеОфицера" Тогда
		Возврат "04";
	Иначе
		Возврат "";
	КонецЕсли;
	
КонецФункции

Функция ПолучитьКодВидаДокументаФизическогоЛица(Знач ДокументФизическогоЛица) Экспорт
	
	Если ДокументФизическогоЛица = Перечисления.ВидыУдостоверенийВладельцаЭП.ПаспортРФ Тогда
		Возврат "21";
	ИначеЕсли ДокументФизическогоЛица = Перечисления.ВидыУдостоверенийВладельцаЭП.ИностранныйПаспорт Тогда
		Возврат "10";
	ИначеЕсли ДокументФизическогоЛица = Перечисления.ВидыУдостоверенийВладельцаЭП.ПустаяСсылка()
		ИЛИ НЕ ЗначениеЗаполнено(ДокументФизическогоЛица) Тогда
		Возврат "91";
	Иначе
	
		Если ДокументФизическогоЛица.Предопределенный Тогда
			КодИМНС = ПолучитьКодВидаДокументаФизическогоЛицаПредопределенного(ДокументФизическогоЛица);
			Если НЕ ПустаяСтрока(КодИМНС) Тогда
				Возврат КодИМНС;
			КонецЕсли;
		КонецЕсли;
		
		Макет = ПолучитьМакетОбработки("ВидыИКодыДокументовФизическихЛиц");
		СтрДокументФизическогоЛица = СокрЛП(ДокументФизическогоЛица);
		
		Для Инд = 1 По Макет.ВысотаТаблицы Цикл
			ТекРегион = СокрЛП(Макет.Область(Инд, 2, Инд, 2).Текст);
			
			Если НРег(ТекРегион) = НРег(СтрДокументФизическогоЛица) Тогда
				Возврат СокрЛП(Макет.Область(Инд, 1, Инд, 1).Текст);
			КонецЕсли;
			
		КонецЦикла;
		
	КонецЕсли;
	
	Если ТипЗнч(ДокументФизическогоЛица) = Тип("СправочникСсылка.ВидыДокументовФизическихЛиц")
		И Метаданные.Справочники.ВидыДокументовФизическихЛиц.Реквизиты.Найти("КодМВД") <> Неопределено Тогда
		
		КодМВД = ДокументФизическогоЛица["КодМВД"];
		Если ЗначениеЗаполнено(КодМВД) Тогда
			Возврат КодМВД;
		КонецЕсли;
		
	КонецЕсли;
		
	Возврат "91";
	
КонецФункции

Функция ЗначениеЗаполненоШумом(Знач Значение) Экспорт
	
	Если ДокументооборотСКОВызовСервера.ИспользуетсяРежимТестирования() Тогда
		Возврат Ложь;
	КонецЕсли;
	
	БезРазделителей = ЗначениеБезРазделителей(Значение);
	ЗначениеЗаполненоШумом = ЗначениеЗаполнено(Значение) И НЕ ЗначениеЗаполнено(БезРазделителей);
	 
	Возврат ЗначениеЗаполненоШумом;
		
КонецФункции

Функция АдресЗаполненШумом(АдресXML)
	
	Возврат Ложь;
	
КонецФункции

Функция ЗначениеБезРазделителей(Знач Значение)
	
	УдаляемыеСимволы = ",.0-_д";
	
	Для Счетчик = 1 По СтрДлина(УдаляемыеСимволы) Цикл
		Символ = Сред(УдаляемыеСимволы, Счетчик, 1);
		Значение = СтрЗаменить(Значение, Символ, "");
	КонецЦикла;

	Возврат СокрЛП(Значение);

КонецФункции

Функция ПроверитьАдресОрганизацииЗаявления(
		ЗначениеАдреса, 
		РеквизитФормыАдреса, 
		ПредставлениеАдреса, 
		ПредставлениеАдресаРодительныйПадеж, 
		МастерДалее,
		ТихийРежим = Ложь,
		ПроверятьТолькоНаПустоту = Ложь) Экспорт
		
	ТекстОшибки 	  = "";
	РезультатПроверки = ДокументооборотСКОКлиентСервер.РезультатПроверкиРеквизитов();
	РезультатПроверки.Поле = РеквизитФормыАдреса;
		
	ЭтоАдресПоФИАСу = УправлениеКонтактнойИнформациейКлиентСервер.ЭтоКонтактнаяИнформацияВXML(ЗначениеАдреса);
	
	Если ПустаяСтрока(СтрЗаменить(ЗначениеАдреса,",","")) Тогда
		
		ТекстОшибки = НСтр("ru = 'Заполните поле ""%1""'");
		ТекстОшибки = СтрШаблон(ТекстОшибки, ПредставлениеАдреса);
			
		РезультатПроверки.ТекстОшибки = ТекстОшибки;
		РезультатПроверки.Пустой	  = Истина;
		
	КонецЕсли;

	Если Не ПроверятьТолькоНаПустоту Тогда
		
		Если ЭтоАдресПоФИАСу Тогда
			
			// - Какая максимальная длина полей 2.5.4.7, 2.5.4.8 и 2.5.4.9 ?
			// - 128 символов для всех.
			
			НаименованиеСубъектаРФПоКодуСубъекта = ПолеСертификата_2_5_4_8(ЗначениеАдреса);
			Если НЕ ЗначениеЗаполнено(НаименованиеСубъектаРФПоКодуСубъекта) Тогда
				РезультатПроверки.ТекстОшибки = СтрШаблон(НСтр("ru = '%1: не удалось определить субъект РФ'"), ПредставлениеАдреса);
			КонецЕсли;
			
			НаименованиеНаселенногоПункта = ПолеСертификата_2_5_4_7(ЗначениеАдреса);
			Если НЕ ЗначениеЗаполнено(НаименованиеНаселенногоПункта) Тогда
				РезультатПроверки.ТекстОшибки = СтрШаблон(НСтр("ru = '%1: не удалось определить город или населенный пункт'"), ПредставлениеАдреса);
			КонецЕсли;
			
			АдресМестаНахождения = ПолеСертификата_2_5_4_9(ЗначениеАдреса);
			Если СтрДлина(АдресМестаНахождения) > 128 Тогда
				
				ТекстОшибки = СтрШаблон(НСтр("ru = 'Часть %1 ""%2"" превышает допустимые 128 символов.
					|Сократите длину адреса, убрав некоторые его части (например, оставив только дом и корпус).'"), 
					НРег(ПредставлениеАдресаРодительныйПадеж), АдресМестаНахождения);
						
				РезультатПроверки.ТекстОшибки = ТекстОшибки;
				
			КонецЕсли;
			
			Если АдресЗаполненШумом(ЗначениеАдреса) Тогда
				РезультатПроверки.ТекстОшибки = СтрШаблон(НСтр("ru = '%1: некоторые части адреса заполнены, но не несут в себе полезной информации'"), ПредставлениеАдреса);
			КонецЕсли;
			
		Иначе
			
			СтрокаОшибкаАдреса = ПроверитьАдрес(ЗначениеАдреса);
			Если НЕ ПустаяСтрока(СтрокаОшибкаАдреса) Тогда
				
				ТекстОшибки = НСтр("ru = '%1: %2'");
				ТекстОшибки = СтроковыеФункцииКлиентСервер.ПодставитьПараметрыВСтроку(ТекстОшибки, ПредставлениеАдреса, СтрокаОшибкаАдреса);
				РезультатПроверки.ТекстОшибки = ТекстОшибки;
				
			ИначеЕсли ДокументооборотСКОКлиентСервер.НайденыЗапрещенныеСимволы(
					ЗначениеАдреса, 
					ПредставлениеАдреса, 
					РеквизитФормыАдреса,
					Истина, 
					ТекстОшибки) Тогда
					
				РезультатПроверки.ТекстОшибки = ТекстОшибки;
				
			Иначе
				
				ПредставлениеАдресаРодительныйПадеж = НРег(ПредставлениеАдресаРодительныйПадеж);
				
				СтруктураАдреса = РегламентированнаяОтчетностьКлиентСервер.РазложитьАдрес(ЗначениеАдреса);
				
				ТекстОшибки = "";
				
				ЕстьОшибка = Ложь;
				
				ПроверитьЧастьАдреса(СтруктураАдреса.Индекс, 6, ПредставлениеАдресаРодительныйПадеж, РеквизитФормыАдреса, ЕстьОшибка);
				
				ПроверитьЧастьАдреса(СтруктураАдреса.Район, 50, ПредставлениеАдресаРодительныйПадеж, РеквизитФормыАдреса, ЕстьОшибка);
				ПроверитьЧастьАдреса(СтруктураАдреса.Город, 50, ПредставлениеАдресаРодительныйПадеж, РеквизитФормыАдреса, ЕстьОшибка);
				ПроверитьЧастьАдреса(СтруктураАдреса.НаселенныйПункт, 50, ПредставлениеАдресаРодительныйПадеж, РеквизитФормыАдреса, ЕстьОшибка);
				ПроверитьЧастьАдреса(СтруктураАдреса.Улица, 50, ПредставлениеАдресаРодительныйПадеж, РеквизитФормыАдреса, ЕстьОшибка);
				
				ПроверитьЧастьАдреса(СтруктураАдреса.Квартира, 8, ПредставлениеАдресаРодительныйПадеж, РеквизитФормыАдреса, ЕстьОшибка);
				ПроверитьЧастьАдреса(СтруктураАдреса.Дом, 8, ПредставлениеАдресаРодительныйПадеж, РеквизитФормыАдреса, ЕстьОшибка);
				ПроверитьЧастьАдреса(СтруктураАдреса.Корпус, 8, ПредставлениеАдресаРодительныйПадеж, РеквизитФормыАдреса, ЕстьОшибка);
				
				// Вывод ошибки
				Если ЕстьОшибка Тогда
					
					ТекстОшибки = НСтр("ru = 'Сократите длину %1 организации, убрав некоторые его части (например, оставив только дом и корпус).'");
					ТекстОшибки = СтрШаблон(ТекстОшибки, ПредставлениеАдресаРодительныйПадеж);
					РезультатПроверки.ТекстОшибки = ТекстОшибки;
					
				КонецЕсли;
				
			КонецЕсли;
		КонецЕсли;
		
	КонецЕсли;
	
	ДокументооборотСКОКлиентСервер.ВывестиОшибкуПроверкиРеквизита(МастерДалее, РезультатПроверки, НЕ ТихийРежим);
	
	Возврат РезультатПроверки;
	
КонецФункции

Процедура ПроверитьЧастьАдреса(
		ФрагментАдреса, 
		МаксимальнаяДлинаФрагмента, 
		ПредставлениеАдресаРодительныйПадеж, 
		РеквизитФормыАдреса, 
		ЕстьОшибка,
		ТихийРежим = Ложь)
	
	Если СтрДлина(ФрагментАдреса) > МаксимальнаяДлинаФрагмента Тогда
		
		Если ТихийРежим Тогда
			МастерДалее = Ложь;
		Иначе
			
			ТекстОшибки = НСтр("ru = 'Часть %1 ""%2"" превышает допустимые %3 символов.'");
			
			ТекстОшибки = СтрШаблон(ТекстОшибки, 
				НРег(ПредставлениеАдресаРодительныйПадеж), 
				ФрагментАдреса, 
				Строка(МаксимальнаяДлинаФрагмента));
				
			ОбщегоНазначения.СообщитьПользователю(ТекстОшибки, ,РеквизитФормыАдреса);
			
			ЕстьОшибка = Истина;
			
		КонецЕсли;
			
	КонецЕсли;
	
КонецПроцедуры

Функция ПроверитьАдрес(СтрокаАдреса)  Экспорт
	
	СтрокаОшибки = "";
	
	Если ТипЗнч(СтрокаАдреса)=Тип("Строка") Тогда
		
		СтруктураАдреса = РегламентированнаяОтчетностьКлиентСервер.РазложитьАдрес(СтрокаАдреса);
		
		Если СтруктураАдреса.Количество()  >= 9 Тогда 
			
			Если СтрДлина(СтруктураАдреса.Индекс)=0 ИЛИ СтрДлина(СтруктураАдреса.Индекс) = 6 Тогда
				
			Иначе
				
				СтрокаОшибки = "индекс либо 6 цифр, либо не заполнять.";
				
			КонецЕсли;
			
			Если ПустаяСтрока(СтруктураАдреса.Регион) Тогда
				СтрокаОшибки = "не указан регион";
			ИначеЕсли НЕ СтрДлина(СтруктураАдреса.Регион) = 2 Тогда
				СтрокаОшибки = "не удалось определить код региона по его наименованию";
			КонецЕсли;
		Иначе
			СтрокаОшибки = "Неизвестный формат адреса.";
		КонецЕсли;
		
	КонецЕсли;
	
	Возврат СтрокаОшибки;
	
КонецФункции

Функция ОрганизацияПодключенаКИнспекции(СсылкаНаОбъект) Экспорт

	СведенияОбОбъекте 	= СведенияПоОтправляемымОбъектам(СсылкаНаОбъект);
	Организация			= СведенияОбОбъекте.Организация;
	УчетнаяЗапись 		= УчетнаяЗаписьОрганизации(Организация);
	
	Если НЕ ЗначениеЗаполнено(УчетнаяЗапись) Тогда
		Возврат Истина; 
	КонецЕсли;
	
	Если УчетнаяЗапись.СпецоператорСвязи <> Перечисления.СпецоператорыСвязи.КалугаАстрал Тогда
		// У других операторов нет файлов автонастройки и не заполняется данный регистр.
		Возврат Истина; 
	КонецЕсли;
	
	КодПолучателя = СведенияОбОбъекте.КодКонтролирующегоОргана;
	
	Запрос = Новый Запрос;
	Запрос.Текст =
		"ВЫБРАТЬ РАЗРЕШЕННЫЕ
		|	ДополнительныеРеквизитыУчетнойЗаписиПолучатели.ТипПолучателя КАК ТипПолучателя,
		|	ДополнительныеРеквизитыУчетнойЗаписиПолучатели.КодПолучателя КАК КодПолучателя,
		|	ДополнительныеРеквизитыУчетнойЗаписиПолучатели.КПП КАК КПП
		|ИЗ
		|	РегистрСведений.ДополнительныеРеквизитыУчетнойЗаписиПолучатели КАК ДополнительныеРеквизитыУчетнойЗаписиПолучатели
		|ГДЕ
		|	ДополнительныеРеквизитыУчетнойЗаписиПолучатели.УчетнаяЗапись = &УчетнаяЗапись
		|	И ДополнительныеРеквизитыУчетнойЗаписиПолучатели.КодПолучателя = &КодПолучателя
		|	И ДополнительныеРеквизитыУчетнойЗаписиПолучатели.ТипПолучателя = Значение(Перечисление.ТипыКонтролирующихОрганов.ФНС)";
	
	Запрос.УстановитьПараметр("УчетнаяЗапись", УчетнаяЗапись);
	Запрос.УстановитьПараметр("КодПолучателя", КодПолучателя);
	
	Возврат НЕ Запрос.Выполнить().Пустой();
	
КонецФункции

#Область ВыгрузкаПакетаПоТребованию

Функция ПолучитьВыгружаемыеПакетыПоДокументооборотамСдачиОтчетностиВФНС(
		ЦиклыОбмена, 
		УникальныйИдентификатор, 
		ТолькоОценка = Ложь,
		ВключитьДополнительныеСведения = Ложь) Экспорт
		
	ТаблицаФайлов 				= ВыгрузкаОтчетовПоМестуТребования_Выборка(ЦиклыОбмена, ВключитьДополнительныеСведения);
	ТранспортныеСообщения 		= ВыгрузкаОтчетовПоМестуТребования_ТранспортныеСообщения(ТаблицаФайлов);
	ТаблицаПрисоединенныхФайлов = ВыгрузкаОтчетовПоМестуТребования_ТаблицаПрисоединенныхФайлов(ТранспортныеСообщения);
	
	ВыгрузкаОтчетовПоМестуТребования_ИзвлечьДвДанныеФайлов(ТаблицаФайлов, ТаблицаПрисоединенныхФайлов);
	
	Результат = Новый Соответствие;
	
	БазовыйКонтекст = Новый Структура();
	БазовыйКонтекст.Вставить("ТаблицаФайлов", 	  ТаблицаФайлов);
	БазовыйКонтекст.Вставить("ТолькоОценка", 	  ТолькоОценка);
	БазовыйКонтекст.Вставить("УникальныйИдентификатор", УникальныйИдентификатор);
	БазовыйКонтекст.Вставить("ВключитьДополнительныеСведения", ВключитьДополнительныеСведения);
	
	Для Каждого ЦиклОбмена Из ЦиклыОбмена Цикл
		
		ЭтоОтчет = ТипЗнч(ЦиклОбмена.Предмет) <> Тип("ДокументСсылка.ЗапросНаИнформационноеОбслуживаниеНалогоплательщика");
		
		ВходящийКонтекст = ОбщегоНазначенияКлиентСервер.СкопироватьРекурсивно(БазовыйКонтекст);
		ВходящийКонтекст.Вставить("Объектов", 		  0);
		ВходящийКонтекст.Вставить("ОбъектовВАрхиве",  0);
		ВходящийКонтекст.Вставить("ЦиклОбмена", 	  ЦиклОбмена);
		ВходящийКонтекст.Вставить("ВременныйКаталог", ОперацииСФайламиЭДКО.СоздатьВременныйКаталог());
		ВходящийКонтекст.Вставить("Представление",	  ПредставлениеЦиклаОбмена(ЦиклОбмена));
		ВходящийКонтекст.Вставить("ТекстОшибки", 	  "");
		
		Если ЭтоОтчет Тогда
			Успешно = ВыгрузкаОтчетовПоМестуТребования_Отчеты(ВходящийКонтекст);
		Иначе
			Успешно = ВыгрузкаОтчетовПоМестуТребования_ЗапросыНаСверку(ВходящийКонтекст);
		КонецЕсли;
			
		Если НЕ Успешно Тогда
			Результат.Вставить(ЦиклОбмена, ВходящийКонтекст.ТекстОшибки);
			Продолжить;
		КонецЕсли;
		
		Если ТолькоОценка Тогда
			Результат.Вставить(ЦиклОбмена, Новый Структура("Всего, ВАрхиве", ВходящийКонтекст.Объектов, ВходящийКонтекст.ОбъектовВАрхиве));
			Продолжить;
		КонецЕсли;
			
		ВыгрузкаОтчетовПоМестуТребования_СформироватьАрхив(ВходящийКонтекст);
		ВыгрузкаОтчетовПоМестуТребования_СохранитьРезультат(ВходящийКонтекст, Результат);
		
	КонецЦикла;
	
	Возврат Результат;
	
КонецФункции

Функция ИдентификаторыВыгрузки()
	
	Идентификаторы = Новый Структура;
	
	Идентификаторы.Вставить("ИдентификаторОтчетность", "ОТЧЕТНОСТЬ");
	Идентификаторы.Вставить("ИдентификаторОтчетностьЭЦПНалогоплательщика", "ОТЧЕТНОСТЬ_ЭЦПНАЛОГОПЛАТЕЛЬЩИКА");
	Идентификаторы.Вставить("ИдентификаторКвитанцияОПриеме", "КВИТАНЦИЯОПРИЕМЕ");
	Идентификаторы.Вставить("ИдентификаторЭЦПНалоговогоОргана", "ЭЦПНАЛОГОВОГООРГАНА");
	Идентификаторы.Вставить("ИдентификаторРезультатаСверки", "РЕЗУЛЬТАТСВЕРКИ");
	Идентификаторы.Вставить("ИдентификаторИзвещениеОВВоде", "ИЗВЕЩЕНИЕОВВОДЕ");
	
	Возврат Идентификаторы;
	
КонецФункции

Функция ВыгрузкаОтчетовПоМестуТребования_Выборка(
		ЦиклыОбмена, 
		ВключитьДополнительныеСведения) Экспорт
	
	Запрос = Новый Запрос("ВЫБРАТЬ
	                      |	ВЫБОР
	                      |		КОГДА СодержимоеТранспортныхКонтейнеров.Тип В (&ЭлектронноЦифроваяПодпись)
	                      |				И (ТранспортныеСообщения.Тип В (&РезультатПриемаДекларацияНО, &РезультатОбработкиЗапросНО)
	                      |					ИЛИ ТранспортныеСообщения.Тип = &РезультатОбработкиДекларацияНО
	                      |						И &ВключитьДополнительныеСведения)
	                      |				И НЕ СодержимоеТранспортныхКонтейнеров.ЭЦПЭтоПодписьАбонента
	                      |			ТОГДА &ИдентификаторЭЦПНалоговогоОргана
	                      |		КОГДА СодержимоеТранспортныхКонтейнеров.Тип В (&ЭлектронноЦифроваяПодпись)
	                      |				И ТранспортныеСообщения.Тип В (&ДекларацияНП)
	                      |				И СодержимоеТранспортныхКонтейнеров.ЭЦПЭтоПодписьАбонента
	                      |			ТОГДА &ИдентификаторОтчетностьЭЦПНалогоплательщика
	                      |		КОГДА СодержимоеТранспортныхКонтейнеров.Тип В (&ОтветНаЗапросИОН)
	                      |			ТОГДА &ИдентификаторРезультатаСверки
	                      |		КОГДА СодержимоеТранспортныхКонтейнеров.Тип В (&КвитанцияОПриеме)
	                      |			ТОГДА &ИдентификаторКвитанцияОПриеме
	                      |		КОГДА СодержимоеТранспортныхКонтейнеров.Тип В (&ТипыСодержимогоОтчетность)
	                      |				И ТранспортныеСообщения.Тип В (&ДекларацияНП)
	                      |			ТОГДА &ИдентификаторОтчетность
	                      |		КОГДА СодержимоеТранспортныхКонтейнеров.Тип = &ИзвещениеОВводе
	                      |				И &ВключитьДополнительныеСведения
	                      |			ТОГДА &ИдентификаторИзвещениеОВВоде
	                      |	КОНЕЦ КАК ИдентификаторТипа,
	                      |	СодержимоеТранспортныхКонтейнеров.ИмяФайла КАК ИмяФайла,
	                      |	СодержимоеТранспортныхКонтейнеров.Данные КАК Данные,
	                      |	СодержимоеТранспортныхКонтейнеров.Размер КАК Размер,
	                      |	СодержимоеТранспортныхКонтейнеров.ЭЦПИмяПодписанногоФайла КАК ЭЦПИмяПодписанногоФайла,
	                      |	ТранспортныеСообщения.Ссылка КАК ТранспортноеСообщение,
	                      |	ТранспортныеСообщения.ЦиклОбмена КАК ЦиклОбмена,
	                      |	ТранспортныеСообщения.ЦиклОбмена.Тип КАК ТипЦиклаОбмена,
	                      |	ВЫБОР
	                      |		КОГДА СостояниеОбъектов.Архивный ЕСТЬ NULL
	                      |			ТОГДА 0
	                      |		КОГДА СостояниеОбъектов.Архивный = ЛОЖЬ
	                      |			ТОГДА 0
	                      |		ИНАЧЕ 1
	                      |	КОНЕЦ КАК ВАрхиве
	                      |ИЗ
	                      |	РегистрСведений.СодержимоеТранспортныхКонтейнеров КАК СодержимоеТранспортныхКонтейнеров
	                      |		ВНУТРЕННЕЕ СОЕДИНЕНИЕ Документ.ТранспортноеСообщение КАК ТранспортныеСообщения
	                      |		ПО СодержимоеТранспортныхКонтейнеров.ТранспортноеСообщение = ТранспортныеСообщения.Ссылка
	                      |		ЛЕВОЕ СОЕДИНЕНИЕ РегистрСведений.ПризнакиАрхивированияФайловДОСКонтролирующимиОрганами КАК СостояниеОбъектов
	                      |		ПО (СостояниеОбъектов.Объект = ТранспортныеСообщения.Ссылка)
	                      |			И (СостояниеОбъектов.ИмяФайла = СодержимоеТранспортныхКонтейнеров.ИмяФайла)
	                      |			И (СостояниеОбъектов.Владелец = ЗНАЧЕНИЕ(Перечисление.ВидыАрхивируемыхМетаданныхДО.СодержимоеТранспортныхКонтейнеров))
	                      |ГДЕ
	                      |	ТранспортныеСообщения.ЦиклОбмена В(&ЦиклыОбмена)
	                      |	И (СодержимоеТранспортныхКонтейнеров.Тип В (&ТипыСодержимогоОтчетность)
	                      |				И ТранспортныеСообщения.Тип В (&ДекларацияНП)
	                      |			ИЛИ СодержимоеТранспортныхКонтейнеров.Тип В (&ЭлектронноЦифроваяПодпись)
	                      |				И ТранспортныеСообщения.Тип В (&ДекларацияНП)
	                      |				И СодержимоеТранспортныхКонтейнеров.ЭЦПЭтоПодписьАбонента
	                      |			ИЛИ СодержимоеТранспортныхКонтейнеров.Тип В (&КвитанцияОПриеме, &ОтветНаЗапросИОН)
	                      |			ИЛИ СодержимоеТранспортныхКонтейнеров.Тип = &ИзвещениеОВводе
	                      |				И &ВключитьДополнительныеСведения
	                      |			ИЛИ СодержимоеТранспортныхКонтейнеров.Тип В (&ЭлектронноЦифроваяПодпись)
	                      |				И (ТранспортныеСообщения.Тип В (&РезультатПриемаДекларацияНО, &РезультатОбработкиЗапросНО)
	                      |					ИЛИ ТранспортныеСообщения.Тип = &РезультатОбработкиДекларацияНО
	                      |						И &ВключитьДополнительныеСведения)
	                      |				И НЕ СодержимоеТранспортныхКонтейнеров.ЭЦПЭтоПодписьАбонента)");
	
	
	ОтветНаЗапросИОН = Новый Массив;
	ОтветНаЗапросИОН.Добавить(Перечисления.ТипыСодержимогоТранспортногоКонтейнера.ОтветНаЗапросИОН);
	ОтветНаЗапросИОН.Добавить(Перечисления.ТипыСодержимогоТранспортногоКонтейнера.Ответ);
	
	Для каждого Идентификатор Из ИдентификаторыВыгрузки() Цикл
		Запрос.Параметры.Вставить(Идентификатор.Ключ, Идентификатор.Значение);
	КонецЦикла;  
	
	Запрос.УстановитьПараметр("ЦиклыОбмена", 					ЦиклыОбмена);
	Запрос.УстановитьПараметр("ОтветНаЗапросИОН", 				ОтветНаЗапросИОН);
	Запрос.УстановитьПараметр("ВключитьДополнительныеСведения", ВключитьДополнительныеСведения);
	
	Запрос.УстановитьПараметр("ТипыСодержимогоОтчетность", 	Перечисления.ТипыСодержимогоТранспортногоКонтейнера.ФайлОтчетности);
	Запрос.УстановитьПараметр("ЭлектронноЦифроваяПодпись", 	Перечисления.ТипыСодержимогоТранспортногоКонтейнера.ЭлектронноЦифроваяПодпись);
	Запрос.УстановитьПараметр("КвитанцияОПриеме", 			Перечисления.ТипыСодержимогоТранспортногоКонтейнера.КвитанцияОПриеме);
	Запрос.УстановитьПараметр("ИзвещениеОВводе", 			Перечисления.ТипыСодержимогоТранспортногоКонтейнера.ИзвещениеОВводе);
	
	Запрос.УстановитьПараметр("РезультатПриемаДекларацияНО", 	Перечисления.ТипыТранспортныхСообщений.РезультатПриемаДекларацияНО);
	Запрос.УстановитьПараметр("РезультатОбработкиДекларацияНО", Перечисления.ТипыТранспортныхСообщений.РезультатОбработкиДекларацияНО);
	Запрос.УстановитьПараметр("РезультатОбработкиЗапросНО",  	Перечисления.ТипыТранспортныхСообщений.РезультатОбработкиЗапросНО);
	Запрос.УстановитьПараметр("ДекларацияНП", 				 	Перечисления.ТипыТранспортныхСообщений.ДекларацияНП);

	ТаблицаФайлов = Запрос.Выполнить().Выгрузить();
	Возврат ТаблицаФайлов;
	
КонецФункции

Функция ВыгрузкаОтчетовПоМестуТребования_ТранспортныеСообщения(ТаблицаФайлов) Экспорт
	
	ТранспортныеСообщения = Новый Массив;
	Для каждого СтрокаТаблицыФайлов Из ТаблицаФайлов Цикл
		Если ТранспортныеСообщения.Найти(СтрокаТаблицыФайлов.ТранспортноеСообщение) = Неопределено
			И СтрокаТаблицыФайлов.Данные.Получить() = Неопределено Тогда
			
			ТранспортныеСообщения.Добавить(СтрокаТаблицыФайлов.ТранспортноеСообщение);
			
		КонецЕсли;
	КонецЦикла;
	
	Возврат ТранспортныеСообщения;
	
КонецФункции

Функция ВыгрузкаОтчетовПоМестуТребования_ТаблицаПрисоединенныхФайлов(ТранспортныеСообщения) Экспорт
	
	ТаблицаПрисоединенныхФайлов = Неопределено;
	Если ТранспортныеСообщения.Количество() > 0 Тогда
		ТаблицаПрисоединенныхФайлов = ОбщегоНазначенияЭДКО.ПрикрепленныеФайлыКОбъектуИзСправочника(
			ТранспортныеСообщения,
			"ТранспортноеСообщениеПрисоединенныеФайлы",
			,
			"ВладелецФайла, ИсходноеИмяФайла");
		
		Для каждого СтрокаТаблицы Из ТаблицаПрисоединенныхФайлов Цикл
			СтрокаТаблицы.ИсходноеИмяФайла = ВРЕГ(СтрокаТаблицы.ИсходноеИмяФайла);
		КонецЦикла;
	КонецЕсли;
	
	Возврат ТаблицаПрисоединенныхФайлов;
	
КонецФункции

Процедура ВыгрузкаОтчетовПоМестуТребования_ИзвлечьДвДанныеФайлов(ТаблицаФайлов, ТаблицаПрисоединенныхФайлов) Экспорт
	
	Для каждого СтрокаТаблицыФайлов Из ТаблицаФайлов Цикл
		ДвоичныеДанныеФайла = СтрокаТаблицыФайлов.Данные.Получить();
		Если ДвоичныеДанныеФайла = Неопределено Тогда
			ИсходноеИмяФайла = ВРЕГ(СтрокаТаблицыФайлов.ИмяФайла);
			
			ПараметрыОтбора = Новый Структура("ВладелецФайла, ИсходноеИмяФайла",
				СтрокаТаблицыФайлов.ТранспортноеСообщение, ИсходноеИмяФайла);
			СтрокиТаблицы = ТаблицаПрисоединенныхФайлов.НайтиСтроки(ПараметрыОтбора);
			Если СтрокиТаблицы.Количество() > 0 Тогда
				ДвоичныеДанныеФайла = РаботаСФайлами.ДвоичныеДанныеФайла(СтрокиТаблицы[0].Ссылка);
			КонецЕсли;
		КонецЕсли;
		
		Если ДвоичныеДанныеФайла <> Неопределено Тогда
			СтрокаТаблицыФайлов.Данные = Новый ХранилищеЗначения(ДвоичныеДанныеФайла, Новый СжатиеДанных(0));
		КонецЕсли;
	КонецЦикла;
	
КонецПроцедуры

Функция РезультатПоискаФайлов(ВходящийКонтекст, ТекстОшики, ИдентификаторТипа = Неопределено, ЭЦПИмяПодписанногоФайла = Неопределено)
	
	Отбор = Новый Структура();
	Отбор.Вставить("ЦиклОбмена", ВходящийКонтекст.ЦиклОбмена);
	Если ИдентификаторТипа <> Неопределено Тогда
		Отбор.Вставить("ИдентификаторТипа", ИдентификаторТипа);
	КонецЕсли;
	
	Если ЭЦПИмяПодписанногоФайла <> Неопределено Тогда
		Отбор.Вставить("ЭЦПИмяПодписанногоФайла", ЭЦПИмяПодписанногоФайла);
	КонецЕсли;
	
	РезультатПоиска = ВходящийКонтекст.ТаблицаФайлов.НайтиСтроки(Отбор);
		
	Если РезультатПоиска.Количество() = 0 Тогда
		ВходящийКонтекст.ТекстОшибки = ТекстОшики;
		Возврат Неопределено;
	КонецЕсли;
	
	Возврат РезультатПоиска;
	
КонецФункции

Процедура ИнкрементироватьОбъект(Строка, ВходящийКонтекст)
	
	ВходящийКонтекст.Объектов = ВходящийКонтекст.Объектов + 1;
	Если Строка.ВАрхиве > 0 Тогда
		ВходящийКонтекст.ОбъектовВАрхиве = ВходящийКонтекст.ОбъектовВАрхиве + 1;
	КонецЕсли;
	
КонецПроцедуры

Функция СтрокаВыгрузки(ВходящийКонтекст, ТекстОшики, ИдентификаторТипа = Неопределено, ЭЦПИмяПодписанногоФайла = Неопределено)

	РезультатПоиска = РезультатПоискаФайлов(
		ВходящийКонтекст, 
		ТекстОшики,  
		ИдентификаторТипа,
		ЭЦПИмяПодписанногоФайла);
		
	Если РезультатПоиска = Неопределено Тогда
		Возврат Неопределено;
	КонецЕсли;
	
	СтрВыгрузки = РезультатПоиска[0];
	ИнкрементироватьОбъект(СтрВыгрузки, ВходящийКонтекст);
	
	Возврат СтрВыгрузки;

КонецФункции

Функция ВыгрузкаОтчетовПоМестуТребования_Отчеты(ВходящийКонтекст) Экспорт
	
	ВыгруженОтчет = ВыгрузкаОтчетовПоМестуТребования_Отчеты_Отчет(ВходящийКонтекст);
	Если НЕ ВыгруженОтчет Тогда
		Возврат Ложь; 
	КонецЕсли;
	
	ВыгруженаЭП   		= ВыгрузкаОтчетовПоМестуТребования_Отчеты_ПодписьФНСПодОтчетом(ВходящийКонтекст);
	ВыгруженаКвитанция 	= ВыгрузкаОтчетовПоМестуТребования_Отчеты_КвитанцияОПриеме(ВходящийКонтекст);
	ВыгруженоИзвещение 	= ВыгрузкаОтчетовПоМестуТребования_Отчеты_ИзвещениеОВводе(ВходящийКонтекст);
	
	Выгружено = ВыгруженОтчет И ВыгруженаЭП И ВыгруженаКвитанция И ВыгруженоИзвещение;
	
	Возврат Выгружено;
	
КонецФункции

Функция ВыгрузкаОтчетовПоМестуТребования_Отчеты_ИзвещениеОВводе(ВходящийКонтекст) Экспорт
	
	ЦиклОбмена 		   = ВходящийКонтекст.ЦиклОбмена;
	ТолькоОценка	   = ВходящийКонтекст.ТолькоОценка;
	ВременныйКаталог   = ВходящийКонтекст.ВременныйКаталог;
	ИмяФайлаОтчетности = ВходящийКонтекст.ИмяФайлаОтчетности;
	Представление      = ВходящийКонтекст.Представление;
	
	Если НЕ ВходящийКонтекст.ВключитьДополнительныеСведения Тогда
		Возврат Истина;
	КонецЕсли;
		
	// Извещение о вводе
	СтрИзвещенияОВводе = СтрокаВыгрузки(
		ВходящийКонтекст, 
		"Выгрузка пакета по циклу обмена """ + Представление + """ прервана: отсутствует извещение о вводе.", 
		ИдентификаторыВыгрузки().ИдентификаторИзвещениеОВВоде);
	
	Если СтрИзвещенияОВводе <> Неопределено Тогда
			
		ИмяИзвещенияОВводе = СтрИзвещенияОВводе.ИмяФайла;
		
		// Подпись ФНС под извещением о вводе
		СтрПодписьФНСПодИзвещениемОВводе = СтрокаВыгрузки(
			ВходящийКонтекст, 
			"Выгрузка пакета по циклу обмена """ + Представление + """ прервана: отсутствует подпись налогового органа под извещением о вводе.",
			ИдентификаторыВыгрузки().ИдентификаторЭЦПНалоговогоОргана, 
			ИмяИзвещенияОВводе);
			
		Если НЕ ТолькоОценка И СтрПодписьФНСПодИзвещениемОВводе <> Неопределено Тогда
			ВыгрузитьПечатнуюФормуИзвещенияОВводе(ВходящийКонтекст, СтрИзвещенияОВводе, СтрПодписьФНСПодИзвещениемОВводе);
		КонецЕсли;
		
	КонецЕсли;
	
	Если ТолькоОценка Тогда 
		Возврат Истина;
	КонецЕсли;
	
	Если СтрИзвещенияОВводе = Неопределено ИЛИ СтрПодписьФНСПодИзвещениемОВводе = Неопределено Тогда 
		Возврат Ложь;
	КонецЕсли;
	
	// выгружаем файлы
	Попытка
	
		СтрИзвещенияОВводе.Данные.Получить().Записать(ВременныйКаталог + ИмяФайлаОтчетности + ".IV.xml");
		СтрПодписьФНСПодИзвещениемОВводе.Данные.Получить().Записать(ВременныйКаталог + ИмяФайлаОтчетности + ".IV.p7s");
		
	Исключение
		СтрСообщение = "Выгрузка пакета по циклу обмена """ + Представление + """ прервана: не удалось сохранить файлы пакета во временный каталог (" + ИнформацияОбОшибке().Описание + ").";
		ВходящийКонтекст.ТекстОшибки = СтрСообщение;
		Возврат Ложь;
	КонецПопытки;
	
	Возврат Истина;
	
КонецФункции

Процедура ВыгрузитьПечатнуюФормуИзвещенияОВводе(ВходящийКонтекст, ДанныеФайла, ДанныеПодписи) Экспорт
	
	ВременныйКаталог   = ВходящийКонтекст.ВременныйКаталог;
	ИмяФайлаОтчетности = ВходящийКонтекст.ИмяФайлаОтчетности;
	Представление      = ВходящийКонтекст.Представление;
		
	ДвДанные            = ДанныеФайла.Данные.Получить();
	ДвДанныеПодписи     = ДанныеПодписи.Данные.Получить();
	СтрокаBase64        = Base64Строка(ДвДанные);
	Тело                = ПрочитатьТекстИзФайлаСтрокойBase64Сервер(СтрокаBase64, "windows-1251");
	Свойства            = ПолучитьОсновныеСвойстваИзИзвещенияОВводе(Тело);
	
	Если Свойства = Неопределено Тогда
		Возврат;
	КонецЕсли;
	
	ТабДокумент = ТабДокументИзвещенияОВводе(Свойства);
	
	ДобавитьПодпись(ДанныеФайла.ТранспортноеСообщение, ТабДокумент, ДвДанныеПодписи, Ложь);
	
	ТабДокумент.Записать(ВременныйКаталог + ИмяФайлаОтчетности + ".IV.pdf" , ТипФайлаТабличногоДокумента.PDF);
	
КонецПроцедуры

Процедура ВыгрузитьПечатнуюФормуКвитанцииОПриеме(ВходящийКонтекст, ДанныеФайла, ДанныеПодписи) Экспорт
	
	ВременныйКаталог   = ВходящийКонтекст.ВременныйКаталог;
	ИмяФайлаОтчетности = ВходящийКонтекст.ИмяФайлаОтчетности;
	Представление      = ВходящийКонтекст.Представление;
		
	ДвДанные            = ДанныеФайла.Данные.Получить();
	ДвДанныеПодписи     = ДанныеПодписи.Данные.Получить();
	СтрокаBase64        = Base64Строка(ДвДанные);
	Тело                = ПрочитатьТекстИзФайлаСтрокойBase64Сервер(СтрокаBase64, "windows-1251");
	Свойства			= ПолучитьОсновныеСвойстваИзКвитанцииОПриемеНалоговойДекларации(Тело);
	
	Если Свойства = Неопределено Тогда
		Возврат;
	КонецЕсли;
	
	ТабДокумент = ТабДокументКвитанцияОПриеме(Свойства);
	
	ДобавитьПодпись(ДанныеФайла.ТранспортноеСообщение, ТабДокумент, ДвДанныеПодписи, Ложь);
	
	ТабДокумент.Записать(ВременныйКаталог + ИмяФайлаОтчетности + ".KV.pdf" , ТипФайлаТабличногоДокумента.PDF);
	
КонецПроцедуры

Функция ПолучитьОсновныеСвойстваИзКвитанцииОПриемеНалоговойДекларации(СтрКвитанция) Экспорт
	
	Если НЕ ЗначениеЗаполнено(СтрКвитанция) Тогда
		Возврат Неопределено;
	КонецЕсли;
	
	// создаем дерево на основе строки XML
	ОписаниеОшибки = "";
	ДеревоXML = ЗагрузитьXMLВДеревоЗначений( , СтрКвитанция, ОписаниеОшибки);
	Если НЕ ЗначениеЗаполнено(ДеревоXML) Тогда
		ТекстСообщения = СтроковыеФункцииКлиентСервер.ПодставитьПараметрыВСтроку(НСтр("ru = 'Ошибка разбора XML квитанции о приеме:%1'"),
			Символы.ПС + ИнформацияОбОшибке().Описание);
		ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
		Возврат Неопределено;
	КонецЕсли;
	
	// анализируем XML
	УзелФайл = ДеревоXML.Строки.Найти("Файл", "Имя");
	Если НЕ ЗначениеЗаполнено(УзелФайл) Тогда
		ТекстСообщения = НСтр("ru = 'Некорректная структура XML квитанции о приеме: не обнаружен узел ""Файл"".'");
		ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
		Возврат Неопределено;
	КонецЕсли;
	
	УзелДокумент = УзелФайл.Строки.Найти("Документ", "Имя");
	Если НЕ ЗначениеЗаполнено(УзелДокумент) Тогда
		ТекстСообщения = НСтр("ru = 'Некорректная структура XML квитанции о приеме: не обнаружен узел ""Документ"".'");
		ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
		Возврат Неопределено;
	КонецЕсли;
	
	УзелСвКвит = УзелДокумент.Строки.Найти("СвКвит", "Имя");
	Если НЕ ЗначениеЗаполнено(УзелСвКвит) Тогда
		ТекстСообщения = НСтр("ru = 'Некорректная структура XML квитанции о приеме: не обнаружен узел ""СвКвит"".'");
		ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
		Возврат Неопределено;
	КонецЕсли;
	
	// разбираем узел с общими сведениями
	ОбщиеСведения = Новый Структура;
	Для Каждого УзелОбщСвед Из УзелСвКвит.Строки Цикл
		ОбщиеСведения.Вставить(УзелОбщСвед.Имя, СокрЛП(УзелОбщСвед.Значение));
	КонецЦикла;
	
	Если ОбщиеСведения.Свойство("ДатаПредст") И ОбщиеСведения.Свойство("ВремПредст") Тогда
		ОбщиеСведения.Вставить("ДатаВремяПредст", ОбщиеСведения.ДатаПредст + " " + ОбщиеСведения.ВремПредст);
	КонецЕсли;
	Если ОбщиеСведения.Свойство("Период") И ОбщиеСведения.Свойство("ПериодНаим") Тогда
		ОбщиеСведения.Вставить("ОтчетПериод", ОбщиеСведения.Период + " (" + ОбщиеСведения.ПериодНаим + ")");
	КонецЕсли;
	
	// разбираем сведения о налоговом органе который подтвердил получение отчетности
	НалОргПодт = СокрЛП(УзелДокумент.Строки.Найти("НаимНОПодт", "Имя").Значение);
	КодНалОргПодт = СокрЛП(УзелДокумент.Строки.Найти("КодНОПодт", "Имя").Значение);
	
	Если НалОргПодт <> КодНалОргПодт Тогда
		НалОргПодт = КодНалОргПодт + " (" + НалОргПодт + ")";
	КонецЕсли;
	
	// разбираем сведения о налоговом органе в который поступила отчетность
	УзелСвНалОргПост = УзелДокумент.Строки.Найти("СвНалОргПост", "Имя");
	Если НЕ ЗначениеЗаполнено(УзелСвНалОргПост) Тогда
		ТекстСообщения = НСтр("ru = 'Некорректная структура XML квитанции: не обнаружен узел ""СвНалОргПост"".'");
		ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
		Отказ = Истина;
		Возврат Неопределено;
	КонецЕсли;
	
	НалоговыйОрганПост = "";
	
	УзелКодНО = УзелСвНалОргПост.Строки.Найти("КодНО", "Имя");
	Если ЗначениеЗаполнено(УзелКодНО) Тогда
		НалоговыйОрганПост = СокрЛП(УзелКодНО.Значение);
	КонецЕсли;
	
	УзелНаимНО = УзелСвНалОргПост.Строки.Найти("НаимНО", "Имя");
	Если ЗначениеЗаполнено(УзелНаимНО) Тогда
		Если ЗначениеЗаполнено(НалоговыйОрганПост) Тогда
			НаименованиеНО = СокрЛП(УзелНаимНО.Значение);
			Если НалоговыйОрганПост <> НаименованиеНО Тогда
				НалоговыйОрганПост = НалоговыйОрганПост + " (" + НаименованиеНО + ")";
			КонецЕсли;
		Иначе
			НалоговыйОрганПост = СокрЛП(УзелНаимНО.Значение);
		КонецЕсли;
	КонецЕсли;
	
	// Сведения об отправителе отчетности
	ПредставлениеОтпр = "";
	ДанныеОтправителя = УзелДокумент.Строки.Найти("СвОтпр", "Имя");
	ОтпрЮЛ = ДанныеОтправителя.Строки.Найти("ОтпрЮЛ", "Имя");
	ОтпрФЛ = ДанныеОтправителя.Строки.Найти("ОтпрФЛ", "Имя");
	
	Если ЗначениеЗаполнено(ОтпрФЛ) Тогда
		ПредставлениеОтпр = ПолучитьПредставлениеНП(Ложь,ОтпрФЛ);
	ИначеЕсли ЗначениеЗаполнено(ОтпрЮЛ) Тогда
		ПредставлениеОтпр = ПолучитьПредставлениеНП(Истина,ОтпрЮЛ);
	КонецЕсли;
	
	// Сведения о налогоплательщике
	ПредставлениеНП = ""; 	
	ДанныеОтправителя = УзелДокумент.Строки.Найти("СвНП", "Имя");
	ОтпрФЛ = ДанныеОтправителя.Строки.Найти("НПФЛ", "Имя");
	ОтпрЮЛ = ДанныеОтправителя.Строки.Найти("НПЮЛ", "Имя");
	
	Если ЗначениеЗаполнено(ОтпрФЛ) Тогда
		ПредставлениеНП = ПолучитьПредставлениеНП(Ложь,ОтпрФЛ);
	ИначеЕсли ЗначениеЗаполнено(ОтпрЮЛ) Тогда
		ПредставлениеНП = ПолучитьПредставлениеНП(Истина,ОтпрЮЛ);
	КонецЕсли;
	
	Возврат Новый Структура("НалоговыйОрганПодтверждающий, НалоговыйОрганПолучатель, ОбщиеСведения, ПредставлениеНП, ПредставлениеОтпр", НалОргПодт, НалоговыйОрганПост, ОбщиеСведения, ПредставлениеНП, ПредставлениеОтпр);
	
КонецФункции

Функция ДобавитьПодпись(ТранспортноеСообщение, ТабДокумент, ДвДанныеПодписи, ЭЦПЭтоПодписьАбонента)
	
	Свойства = ЭлектронныйДокументооборотСКонтролирующимиОрганамиКлиентСервер.СвойстваПодписиДляШтампа();
	Свойства.ДвДанные = ДвДанныеПодписи;
	ЗаполнитьСвойстваПодписиДляШтампа(ТранспортноеСообщение, ЭЦПЭтоПодписьАбонента, Свойства); 
	
	ДвДанныеФайлаПодписи = Новый Массив;
	ДвДанныеФайлаПодписи.Добавить(Свойства);
		
	ДобавитьШтампПодписи(ДвДанныеФайлаПодписи, ТабДокумент, 2, Ложь);
	
КонецФункции

Функция ВыгрузкаОтчетовПоМестуТребования_Отчеты_КвитанцияОПриеме(ВходящийКонтекст) Экспорт
		
	ЦиклОбмена 		   = ВходящийКонтекст.ЦиклОбмена;
	ТолькоОценка	   = ВходящийКонтекст.ТолькоОценка;
	ВременныйКаталог   = ВходящийКонтекст.ВременныйКаталог;
	ИмяФайлаОтчетности = ВходящийКонтекст.ИмяФайлаОтчетности;
	Представление      = ВходящийКонтекст.Представление;
	
	// Квитанция о приеме
	СтрКвитанцияОПриеме = СтрокаВыгрузки(
		ВходящийКонтекст, 
		"Выгрузка пакета по циклу обмена """ + Представление + """ прервана: отсутствует квитанция о приеме.", 
		ИдентификаторыВыгрузки().ИдентификаторКвитанцияОПриеме);
	
	Если СтрКвитанцияОПриеме = Неопределено Тогда
		Возврат Ложь;
	КонецЕсли;
	ИмяФайлаКвитанции = СтрКвитанцияОПриеме.ИмяФайла;
	
	// Подпись ФНС под квитанцией о приеме
	СтрЭЦПНалоговогоОрганаПодКвитанциейОПриеме = СтрокаВыгрузки(
		ВходящийКонтекст, 
		"Выгрузка пакета по циклу обмена """ + Представление + """ прервана: отсутствует подпись налогового органа под квитанцией о приеме.",
		ИдентификаторыВыгрузки().ИдентификаторЭЦПНалоговогоОргана, 
		ИмяФайлаКвитанции);
		
	Если НЕ ТолькоОценка И ВходящийКонтекст.ВключитьДополнительныеСведения Тогда
		ВыгрузитьПечатнуюФормуКвитанцииОПриеме(ВходящийКонтекст, СтрКвитанцияОПриеме, СтрЭЦПНалоговогоОрганаПодКвитанциейОПриеме);
	КонецЕсли;
		
	Если СтрЭЦПНалоговогоОрганаПодКвитанциейОПриеме = Неопределено Тогда
		Возврат Ложь;
	КонецЕсли;
	
	Если ТолькоОценка Тогда 
		Возврат Истина;
	КонецЕсли;
	
	// выгружаем файлы
	Попытка
	
		СтрКвитанцияОПриеме.Данные.Получить().Записать(ВременныйКаталог + ИмяФайлаОтчетности + ".KV.xml");
		СтрЭЦПНалоговогоОрганаПодКвитанциейОПриеме.Данные.Получить().Записать(ВременныйКаталог + ИмяФайлаОтчетности + ".KV.p7s");
		
	Исключение
		СтрСообщение = "Выгрузка пакета по циклу обмена """ + Представление + """ прервана: не удалось сохранить файлы пакета во временный каталог (" + ИнформацияОбОшибке().Описание + ").";
		ВходящийКонтекст.ТекстОшибки = СтрСообщение;
		Возврат Ложь;
	КонецПопытки;
	
	Возврат Истина;
	
КонецФункции

Функция ВыгрузкаОтчетовПоМестуТребования_Отчеты_ПодписьФНСПодОтчетом(ВходящийКонтекст) Экспорт
		
	ЦиклОбмена 		   = ВходящийКонтекст.ЦиклОбмена;
	ТолькоОценка	   = ВходящийКонтекст.ТолькоОценка;
	ВременныйКаталог   = ВходящийКонтекст.ВременныйКаталог;
	ИмяФайлаОтчетности = ВходящийКонтекст.ИмяФайлаОтчетности;
	Представление      = ВходящийКонтекст.Представление;
	
	// Подпись ФНС под отчетом
	СтрЭЦПНалоговогоОрганаПодФайломОтчетности = СтрокаВыгрузки(
		ВходящийКонтекст, 
		"Выгрузка пакета по циклу обмена """ + ПредставлениеЦиклаОбмена(ЦиклОбмена) + """ прервана: отсутствует подпись налогового органа под файлом отчетности.", 
		ИдентификаторыВыгрузки().ИдентификаторЭЦПНалоговогоОргана, 
		ИмяФайлаОтчетности);
		
	Если СтрЭЦПНалоговогоОрганаПодФайломОтчетности = Неопределено Тогда
		Возврат Ложь;
	КонецЕсли;
	
	Если ТолькоОценка Тогда 
		Возврат Истина;
	КонецЕсли;
	
	// выгружаем файлы
	Попытка
	
		СтрЭЦПНалоговогоОрганаПодФайломОтчетности.Данные.Получить().Записать(ВременныйКаталог + ИмяФайлаОтчетности + ".FNS.p7s");
		
	Исключение
		СтрСообщение = "Выгрузка пакета по циклу обмена """ + Представление + """ прервана: не удалось сохранить файлы пакета во временный каталог (" + ИнформацияОбОшибке().Описание + ").";
		ВходящийКонтекст.ТекстОшибки = СтрСообщение;
		Возврат Ложь;
	КонецПопытки;
	
	Возврат Истина;
	
КонецФункции

Функция ВыгрузкаОтчетовПоМестуТребования_Отчеты_Отчет(ВходящийКонтекст) Экспорт
		
	ЦиклОбмена 		   = ВходящийКонтекст.ЦиклОбмена;
	ТолькоОценка	   = ВходящийКонтекст.ТолькоОценка;
	ВременныйКаталог   = ВходящийКонтекст.ВременныйКаталог;
	Представление      = ВходящийКонтекст.Представление;
	
	Если ЦиклОбмена.Тип <> Перечисления.ТипыЦикловОбмена.НалоговаяИлиБухгалтерскаяОтчетность Тогда
		СтрСообщение = "Выгрузка пакета по циклу обмена """ + Представление + """ прервана: функция поддерживается только для циклов обмена по бухгалтерской или налоговой отчетности (кроме форм 2-НДФЛ и ""Заявление о ввозе товаров"").";
	Иначе
		СтрСообщение = "Выгрузка пакета по циклу обмена """ + Представление + """ прервана: отсутствует файл отчетности.";
	КонецЕсли;
	
	// Отчет
	СтрОтчетность = СтрокаВыгрузки(
		ВходящийКонтекст,
		СтрСообщение, 
		ИдентификаторыВыгрузки().ИдентификаторОтчетность);
		
	Если СтрОтчетность = Неопределено Тогда
		Возврат Ложь;
	КонецЕсли;
	ИмяФайлаОтчетности = СтрОтчетность.ИмяФайла;
	ВходящийКонтекст.Вставить("ИмяФайлаОтчетности", ИмяФайлаОтчетности);
	
	// Подпись налогоплательщика под отчетом
	СтрЭЦПНалогоплательщикаПодФайломОтчетности = СтрокаВыгрузки(
		ВходящийКонтекст, 
		"Выгрузка пакета по циклу обмена """ + Представление + """ прервана: отсутствует подпись налогоплательщика под файлом отчетности.", 
		ИдентификаторыВыгрузки().ИдентификаторОтчетностьЭЦПНалогоплательщика, 
		ИмяФайлаОтчетности);
		
	Если СтрЭЦПНалогоплательщикаПодФайломОтчетности = Неопределено Тогда
		Возврат Ложь;
	КонецЕсли;
	
	Если ТолькоОценка Тогда 
		Возврат Истина;
	КонецЕсли;
	
	// выгружаем файлы
	Попытка
	
		СтрОтчетность.Данные.Получить().Записать(ВременныйКаталог + ИмяФайлаОтчетности);
		СтрЭЦПНалогоплательщикаПодФайломОтчетности.Данные.Получить().Записать(ВременныйКаталог + ИмяФайлаОтчетности + ".p7s");
		
	Исключение
		СтрСообщение = "Выгрузка пакета по циклу обмена """ + Представление + """ прервана: не удалось сохранить файлы пакета во временный каталог (" + ИнформацияОбОшибке().Описание + ").";
		ВходящийКонтекст.ТекстОшибки = СтрСообщение;
		Возврат Ложь;
	КонецПопытки;
	
	Возврат Истина;
	
КонецФункции

Функция ВыгрузкаОтчетовПоМестуТребования_ЗапросыНаСверку(ВходящийКонтекст) Экспорт
		
	ЦиклОбмена 		 = ВходящийКонтекст.ЦиклОбмена;
	ТолькоОценка	 = ВходящийКонтекст.ТолькоОценка;
	ВременныйКаталог = ВходящийКонтекст.ВременныйКаталог;
	Представление    = ВходящийКонтекст.Представление;
	
	// ищем результат сверки
	РезультатПоиска = РезультатПоискаФайлов(
		ВходящийКонтекст, 
		"Выгрузка пакета по циклу обмена """ + Представление + """ прервана: отсутствует ответ на запрос сверки.",
		ИдентификаторыВыгрузки().ИдентификаторРезультатаСверки);
		
	Если РезультатПоиска = Неопределено Тогда
		Возврат Ложь;
	КонецЕсли;
	
	СтрРезультатСверки = РезультатПоиска[0];
	ИмяРезультатаСверки = СтрРезультатСверки.ИмяФайла;
	
	ВходящийКонтекст.Вставить("ИмяФайлаОтчетности", ИмяРезультатаСверки);
	
	ИнкрементироватьОбъект(СтрРезультатСверки, ВходящийКонтекст);
	
	// ищем ЭЦП налогового органа под результатом сверки
	РезультатПоиска = РезультатПоискаФайлов(
		ВходящийКонтекст, 
		"Выгрузка пакета по циклу обмена """ + Представление + """ прервана: отсутствует подпись налогового органа под ответом на запрос сверки.", 
		ИдентификаторыВыгрузки().ИдентификаторЭЦПНалоговогоОргана, 
		ИмяРезультатаСверки);
		
	Если РезультатПоиска = Неопределено Тогда
		Возврат Ложь;
	КонецЕсли;
	
	СтрЭЦПНалоговогоОрганаПодРезультатомСверки = РезультатПоиска[0];
	ИнкрементироватьОбъект(СтрЭЦПНалоговогоОрганаПодРезультатомСверки, ВходящийКонтекст);
	
	Если ТолькоОценка Тогда 
		Возврат Истина;
	КонецЕсли;
	
	// выгружаем файлы
	Попытка
		СтрРезультатСверки.Данные.Получить().Записать(ВременныйКаталог + ИмяРезультатаСверки);
		СтрЭЦПНалоговогоОрганаПодРезультатомСверки.Данные.Получить().Записать(ВременныйКаталог + ИмяРезультатаСверки + ".FNS.p7s");
	Исключение
		СтрСообщение = "Выгрузка пакета по циклу обмена """ + Представление + """ прервана: не удалось сохранить файлы пакета во временный каталог (" + ИнформацияОбОшибке().Описание + ").";
		ВходящийКонтекст.ТекстОшибки = СтрСообщение;
		Возврат Ложь;
	КонецПопытки;
	
	Возврат Истина;
	
КонецФункции

Функция ВыгрузкаОтчетовПоМестуТребования_СохранитьРезультат(ВходящийКонтекст, Результат) Экспорт
	
	// фиксируем результат выгрузки пакета по циклу обмена
	ДополнительныеПараметры = Новый Структура();
	ДополнительныеПараметры.Вставить("ИмяФайла", ВходящийКонтекст.ИмяФайлаПакета);
	ДополнительныеПараметры.Вставить("Адрес",    ВходящийКонтекст.АдресВоВременномХранилище);
	ДополнительныеПараметры.Вставить("Всего",    ВходящийКонтекст.Объектов);
	ДополнительныеПараметры.Вставить("ВАрхиве",  ВходящийКонтекст.ОбъектовВАрхиве);
	
	Результат.Вставить(ВходящийКонтекст.ЦиклОбмена, ДополнительныеПараметры);
	
КонецФункции

Функция ВыгрузкаОтчетовПоМестуТребования_СформироватьАрхив(ВходящийКонтекст) Экспорт
		
	ЦиклОбмена       = ВходящийКонтекст.ЦиклОбмена;
	Представление    = ВходящийКонтекст.Представление;
	ВременныйКаталог = ВходящийКонтекст.ВременныйКаталог;
		
	// формируем zip-архив
	Попытка
	
		ИмяФайлаПакета = ИмяФайлаБезРасширения(ВходящийКонтекст.ИмяФайлаОтчетности) + ".zip";
		ПолноеИмяФайлаПакета = ПолучитьКаталогВременныхФайлов() + ИмяФайлаПакета;
		ОбъектЗапись = Новый ЗаписьZipФайла(ПолноеИмяФайлаПакета);
		ОбъектЗапись.Добавить(ВременныйКаталог + "*.*");
		ОбъектЗапись.Записать();
		
	Исключение
		СтрСообщение = "Выгрузка пакета по циклу обмена """ + Представление + """ прервана: не удалось сохранить файл пакета во временный каталог (" + ИнформацияОбОшибке().Описание + ").";
		ВходящийКонтекст.ТекстОшибки  = СтрСообщение;
		Возврат Ложь;
	КонецПопытки;
	
	// удаляем временный каталог
	ОперацииСФайламиЭДКО.УдалитьВременныйФайл(ВременныйКаталог);
	
	// сохраняем во временном хранилище
	АдресВоВременномХранилище = ПоместитьВоВременноеХранилище(Новый ДвоичныеДанные(ПолноеИмяФайлаПакета), ВходящийКонтекст.УникальныйИдентификатор);
	
	ВходящийКонтекст.Вставить("ИмяФайлаПакета", ИмяФайлаПакета);
	ВходящийКонтекст.Вставить("АдресВоВременномХранилище", АдресВоВременномХранилище);
	
	Возврат Истина;
	
КонецФункции

Функция ПрочитатьТекстИзФайлаСтрокойBase64(ИмяФайла, Знач ЗаданнаяКодировкаТекста = Неопределено, Знач АвтоопределениеКодировкиXML = Ложь) Экспорт 
	
	ДвДанные = Base64Значение(ИмяФайла);
	ИмяФайлаНаДиске = ПолучитьИмяВременногоФайла();
	ДвДанные.Записать(ИмяФайлаНаДиске);
	
	Если ЗаданнаяКодировкаТекста = Неопределено Тогда
		АвтоопределениеКодировкиXML = Истина;
	КонецЕсли;
			
	Если АвтоопределениеКодировкиXML Тогда
		Попытка
			КодировкаДанных = ОпределитьКодировку(ДвДанные);
		Исключение
			ИнформацияОбОшибке = ПодробноеПредставлениеОшибки(ИнформацияОбОшибке());
			ЗаписьЖурналаРегистрации(
				НСтр("ru = 'Электронный документооборот с контролирующими органами.ПрочитатьТекстИзФайлаСтрокойBase64()'"), 
				УровеньЖурналаРегистрации.Ошибка,,,
				СтрШаблон("%1: %2", "Определение кодировки не удалось", ИнформацияОбОшибке));
				
			КодировкаДанных = Неопределено;
		КонецПопытки;
	Иначе
		КодировкаДанных = ЗаданнаяКодировкаТекста;
	КонецЕсли;
	
	ОбъектЧтение = НовыйЧтениеТекстаНаСервере(ИмяФайлаНаДиске, КодировкаДанных);
	ТекстИзФайла = ОбъектЧтение.Прочитать();
	ОбъектЧтение.Закрыть();
	
	Возврат ТекстИзФайла;
	
КонецФункции

Функция ПрочитатьТекстИзФайлаСтрокойBase64Сервер(СтрокаBase64, Знач ЗаданнаяКодировкаТекста = Неопределено, Знач АвтоопределениеКодировкиXML = Ложь) Экспорт
	
	Попытка
		ТекстИзФайла = ПрочитатьТекстИзФайлаСтрокойBase64(СтрокаBase64, ЗаданнаяКодировкаТекста, АвтоопределениеКодировкиXML);
	Исключение
		// Не требует специальной обработки.
		// Сделано для обхода ошибки, когда ФНС должно присылать квитанции в формате windows-1251, а присылают в формате Utf-8
		ТекстИзФайла = ПрочитатьТекстИзФайлаСтрокойBase64(СтрокаBase64, , Истина);	
	КонецПопытки; 
	
	Возврат ТекстИзФайла;
	
КонецФункции

Функция ТабДокументИзвещенияОВводе(СвойстваИзИзвещения, ТаблицаПодписей = Неопределено) Экспорт
	
	ТабДокумент = Новый ТабличныйДокумент;
	
	Макет = ПолучитьМакетОбработки("ИзвещениеОВводе");
	ОбластьМакета = Макет.ПолучитьОбласть("ИзвещениеОВводе");
	ОбластьМакета.Параметры.КодНалоговогоОргана = СвойстваИзИзвещения.НалоговыйОрган;
	ОбластьМакета.Параметры.НаименованиеФайла = ВРег(СвойстваИзИзвещения.ОбщиеСведения.ИмяОбрабФайла);
	ОбластьМакета.Параметры.Отправитель = СвойстваИзИзвещения.ПредставлениеОтпр;
	ОбластьМакета.Параметры.Налогоплательщик = СвойстваИзИзвещения.ПредставлениеНП;
	
	НаименованиеНалоговогоОргана = "";
	Запрос = Новый Запрос("ВЫБРАТЬ РАЗРЕШЕННЫЕ ПЕРВЫЕ 1
	                      |	НалоговыеОрганы.Наименование
	                      |ИЗ
	                      |	Справочник.НалоговыеОрганы КАК НалоговыеОрганы
	                      |ГДЕ
	                      |	НалоговыеОрганы.Код = &КодНалоговогоОргана");
	Запрос.УстановитьПараметр("КодНалоговогоОргана", СвойстваИзИзвещения.НалоговыйОрган);
	Выборка = Запрос.Выполнить().Выбрать();
	Если Выборка.Следующий() Тогда
		НаименованиеНалоговогоОргана = Выборка.Наименование;
	КонецЕсли;
	Если ЗначениеЗаполнено(НаименованиеНалоговогоОргана) Тогда
		ОбластьМакета.Параметры.НаименованиеИКодНалоговогоОргана = НаименованиеНалоговогоОргана + ", " + НСтр("ru = 'код'") + " " + СвойстваИзИзвещения.НалоговыйОрган;
	Иначе
		ОбластьМакета.Параметры.НаименованиеИКодНалоговогоОргана = НСтр("ru = 'ФНС'") + " " + СвойстваИзИзвещения.НалоговыйОрган;
	КонецЕсли;
	
	ОбластьМакета.Параметры.НаименованиеДекларации = СформироватьТекстовуюИнформациюОДекларации(СвойстваИзИзвещения.ОбщиеСведения);
	
	ТабДокумент.Вывести(ОбластьМакета); // ИзвещениеОВводе
	
	Если ТаблицаПодписей <> Неопределено Тогда
		СформироватьТабличныйДокументСекцияПодписи(ТабДокумент, Макет, ТаблицаПодписей);
	КонецЕсли;
	
	Возврат ТабДокумент;
	
КонецФункции

Функция ТабДокументКвитанцияОПриеме(СвойстваИзУведомления, ТаблицаПодписей = Неопределено) Экспорт
	
	ТабДокумент = Новый ТабличныйДокумент;
	Макет = ПолучитьМакетОбработки("КвитанцияОПриемеДекларации");
	
	ОбластьМакета = Макет.ПолучитьОбласть("КвитанцияОПриемеДекларации");
	ОбластьМакета.Параметры.НалоговыйОрганПодтверждающий = СвойстваИзУведомления.НалоговыйОрганПодтверждающий;
	ОбластьМакета.Параметры.НалоговыйОрганПолучатель = СвойстваИзУведомления.НалоговыйОрганПолучатель;
	ОбластьМакета.Параметры.ИмяФайла = ВРег(СвойстваИзУведомления.ОбщиеСведения.ИмяОбрабФайла);
	ОбластьМакета.Параметры.Отправитель = СвойстваИзУведомления.ПредставлениеОтпр;
	ОбластьМакета.Параметры.Налогоплательщик = СвойстваИзУведомления.ПредставлениеНП;
	ОбластьМакета.Параметры.Дата = СвойстваИзУведомления.ОбщиеСведения.ДатаПредст;
	ОбластьМакета.Параметры.Время = СтрЗаменить(СвойстваИзУведомления.ОбщиеСведения.ВремПредст,".",":");
	ОбластьМакета.Параметры.ДатаПоступления = СвойстваИзУведомления.ОбщиеСведения.ДатаПост;
	ОбластьМакета.Параметры.ДатаПриема = СвойстваИзУведомления.ОбщиеСведения.ДатаПрин;
	
	ОбластьМакета.Параметры.НаименованиеДекларации = СформироватьТекстовуюИнформациюОДекларации(СвойстваИзУведомления.ОбщиеСведения);
	
	ОбластьМакета.Параметры.РегистрационныйНомер = СвойстваИзУведомления.ОбщиеСведения.РегНом+".";
	ТабДокумент.Вывести(ОбластьМакета); // КвитанцияОПриемеДекларации
	
	Если ТаблицаПодписей <> Неопределено Тогда
		СформироватьТабличныйДокументСекцияПодписи(ТабДокумент, Макет, ТаблицаПодписей);
	КонецЕсли;
	
	Возврат ТабДокумент;
	
КонецФункции

Функция СформироватьТабличныйДокументСекцияПодписи(ТабДокумент, Макет, Подписи) Экспорт
	
	Если Подписи <> Неопределено И Подписи.Количество() <> 0 Тогда
		
		КонтекстЭДОСервер = ДокументооборотСКО.ПолучитьОбработкуЭДО();
		
		ДвДанныеФайлаПодписи = Новый Массив;
		Для каждого Подпись Из Подписи Цикл
			
			Свойства = ЭлектронныйДокументооборотСКонтролирующимиОрганамиКлиентСервер.СвойстваПодписиДляШтампа();
			Свойства.ДвДанные = Base64Значение(Подпись.Файл);
			КонтекстЭДОСервер.ЗаполнитьСвойстваПодписиДляШтампа(Подпись.ТранспортноеСообщение, Подпись.ЭЦПЭтоПодписьАбонента, Свойства); 
			
			ДвДанныеФайлаПодписи.Добавить(Свойства);
			
		КонецЦикла; 
		
		КонтекстЭДОСервер.ДобавитьШтампПодписи(ДвДанныеФайлаПодписи, ТабДокумент, 2, Ложь);
		
	КонецЕсли;
	
КонецФункции

Функция СформироватьТекстовуюИнформациюОДекларации(СведенияОДекларации) Экспорт
	
	Если СведенияОДекларации.Свойство("НомКорр") Тогда
		НомКорр = СведенияОДекларации.НомКорр;
	Иначе
		НомКорр = "";
	КонецЕсли;
	
	Если ПустаяСтрока(НомКорр) ИЛИ НомКорр = "0" Тогда
		ВидДокумента = "первичный";
	Иначе
		ВидДокумента = "корректирующий("+ НомКорр+"),";
	КонецЕсли;
	
	Если СведенияОДекларации.Свойство("Период") Тогда
		Период = ПолучитьНаименованиеНалоговогоПериодаПоКоду(СведенияОДекларации.Период);
	Иначе
		Период = "";
	КонецЕсли;
	
	МассивСведений = Новый Массив;
	Если СведенияОДекларации.Свойство("НаимОтч") Тогда
		МассивСведений.Добавить(СведенияОДекларации.НаимОтч);
	КонецЕсли;
	Если СведенияОДекларации.Свойство("КНД") Тогда
		МассивСведений.Добавить(СведенияОДекларации.КНД);
	КонецЕсли;
	Если ЗначениеЗаполнено(ВидДокумента) Тогда
		МассивСведений.Добавить(ВидДокумента);
	КонецЕсли;
	Если ЗначениеЗаполнено(Период) Тогда
		МассивСведений.Добавить(Период);
	КонецЕсли;
	Если СведенияОДекларации.Свойство("ОтчетГод") Тогда
		МассивСведений.Добавить(СведенияОДекларации.ОтчетГод + "г.");
	КонецЕсли;
	
	Если МассивСведений.Количество() > 0 Тогда
		Возврат СтрСоединить(МассивСведений, ",");
	Иначе
		Возврат "";
	КонецЕсли;
	
КонецФункции

Функция ПолучитьНаименованиеНалоговогоПериодаПоКоду(КодПериода) Экспорт
	
	Если ПустаяСтрока(КодПериода) Тогда
		КодПериода = "99";
	КонецЕсли;
	
	Если СтроковыеФункцииКлиентСервер.ТолькоЦифрыВСтроке(СокрЛП(КодПериода)) Тогда
		КодПериода = Формат(Число(КодПериода), "ЧЦ=2; ЧВН=");
	Иначе
		КодПериода = СокрЛП(КодПериода);
	КонецЕсли;
	
	МакетРегионы = ПолучитьМакетОбработки("КодыНалоговыхПериодов");
	
	Для Инд = 1 По МакетРегионы.ВысотаТаблицы Цикл
		ТекКод = МакетРегионы.Область(Инд, 1, Инд, 1).Текст;
		
		НаименованиеПериода = СокрЛП(МакетРегионы.Область(Инд, 2, Инд, 2).Текст);
		
		Если ТекКод = КодПериода Тогда
			Возврат НаименованиеПериода;
		КонецЕсли;
		
	КонецЦикла;
	
	Возврат НаименованиеПериода;
	
КонецФункции

Функция ПолучитьОсновныеСвойстваИзИзвещенияОВводе(СтрУведомление) Экспорт
	
	Если НЕ ЗначениеЗаполнено(СтрУведомление) Тогда
		Возврат Неопределено;
	КонецЕсли;
	
	// считываем подтверждение из файла в дерево XML
	ОписаниеОшибки = "";
	ДеревоXML = ЗагрузитьXMLВДеревоЗначений(, СтрУведомление, ОписаниеОшибки);
	Если НЕ ЗначениеЗаполнено(ДеревоXML) Тогда
		ТекстСообщения = СтроковыеФункцииКлиентСервер.ПодставитьПараметрыВСтроку(НСтр("ru = 'Ошибка чтения XML извещения о вводе:%1'"), Символы.ПС + ИнформацияОбОшибке().Описание);
		ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
		Возврат Неопределено;
	КонецЕсли;
	
	// анализируем XML
	УзелФайл = ДеревоXML.Строки.Найти("Файл", "Имя");
	Если НЕ ЗначениеЗаполнено(УзелФайл) Тогда
		ТекстСообщения = НСтр("ru = 'Некорректная структура XML извещения о вводе: не обнаружен узел ""Файл"".'");
		ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
		Возврат Неопределено;
	КонецЕсли;
	
	УзелДокумент = УзелФайл.Строки.Найти("Документ", "Имя");
	Если НЕ ЗначениеЗаполнено(УзелДокумент) Тогда
		ТекстСообщения = НСтр("ru = 'Некорректная структура XML извещения о вводе: не обнаружен узел ""Документ"".'");
		ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
		Возврат Неопределено;
	КонецЕсли;
	
	УзелСвИзвещВ = УзелДокумент.Строки.Найти("СвИзвещВ", "Имя");
	Если НЕ ЗначениеЗаполнено(УзелСвИзвещВ) Тогда
		ТекстСообщения = НСтр("ru = 'Некорректная структура XML извещения о вводе: не обнаружен узел ""СвИзвещВ"".'");
		ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
		Возврат Неопределено;
	КонецЕсли;
	
	// разбираем узел с общими сведениями
	ОбщиеСведения = Новый Структура;
	Для Каждого УзелОбщСвед Из УзелСвИзвещВ.Строки Цикл
		ОбщиеСведения.Вставить(УзелОбщСвед.Имя, СокрЛП(УзелОбщСвед.Значение));
	КонецЦикла;
	Если ОбщиеСведения.Свойство("Период") И ОбщиеСведения.Свойство("ПериодНаим") Тогда
		ОбщиеСведения.Вставить("ОтчетныйПериод", ОбщиеСведения.Период + " (" + ОбщиеСведения.ПериодНаим + ")");
	КонецЕсли;
	
	// разбираем сведения о налоговом органе
	УзелКодНоОтпр = УзелДокумент.Строки.Найти("КодНоОтпр", "Имя");
	Если НЕ ЗначениеЗаполнено(УзелКодНоОтпр) Тогда
		ТекстСообщения = НСтр("ru = 'Некорректная структура XML извещения о вводе: не обнаружен узел ""КодНоОтпр"".'");
		ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
		Возврат Неопределено;
	КонецЕсли;
	НалоговыйОрган = СокрЛП(УзелКодНоОтпр.Значение);
	
	// Сведения об отправителе отчетности
	ПредставлениеОтпр = "";
	ДанныеОтправителя = УзелДокумент.Строки.Найти("СвПол", "Имя");
	ОтпрЮЛ = ДанныеОтправителя.Строки.Найти("ОтпрЮЛ", "Имя");
	ОтпрФЛ = ДанныеОтправителя.Строки.Найти("ОтпрФЛ", "Имя");
	
	Если ЗначениеЗаполнено(ОтпрФЛ) Тогда
		ПредставлениеОтпр = ПолучитьПредставлениеНП(Ложь,ОтпрФЛ);
	ИначеЕсли ЗначениеЗаполнено(ОтпрЮЛ) Тогда
		ПредставлениеОтпр = ПолучитьПредставлениеНП(Истина,ОтпрЮЛ);
	КонецЕсли;
	
	// Сведения о налогоплательщике
	ПредставлениеНП = "";
	ДанныеОтправителя = УзелДокумент.Строки.Найти("СвНП", "Имя");
	ОтпрФЛ = ДанныеОтправителя.Строки.Найти("НПФЛ", "Имя");
	ОтпрЮЛ = ДанныеОтправителя.Строки.Найти("НПЮЛ", "Имя");
	
	Если ЗначениеЗаполнено(ОтпрФЛ) Тогда
		ПредставлениеНП = ПолучитьПредставлениеНП(Ложь,ОтпрФЛ);
	ИначеЕсли ЗначениеЗаполнено(ОтпрЮЛ) Тогда
		ПредставлениеНП = ПолучитьПредставлениеНП(Истина,ОтпрЮЛ);
	КонецЕсли;
	
	Возврат Новый Структура("НалоговыйОрган, ОбщиеСведения, ПредставлениеНП, ПредставлениеОтпр", НалоговыйОрган, ОбщиеСведения, ПредставлениеНП, ПредставлениеОтпр);
	
КонецФункции

Функция ПолучитьПредставлениеНП(ЮрФизЛицо,ДанныеНП) Экспорт
	
	Представление = "";
	
	Если ЮрФизЛицо Тогда
		Если ДанныеНП.Строки.Количество() > 0 Тогда
			
			ИННСтрока = "";
			КППСтрока = "";
			НаимСтрока = "";
			Для Каждого УзелОбщСведНП Из ДанныеНП.Строки Цикл
				ДанныеСтроки = СокрЛП(УзелОбщСведНП.Значение);
				Если Лев(УзелОбщСведНП.Имя,3) = "ИНН" Тогда
					ИННСтрока = ", "+ДанныеСтроки;
				ИначеЕсли УзелОбщСведНП.Имя = "КПП" Тогда
					КППСтрока = " / "+ДанныеСтроки;
				ИначеЕсли УзелОбщСведНП.Имя = "НаимОрг" Тогда
					НаимСтрока = ДанныеСтроки;
				КонецЕсли;
			КонецЦикла;
			
			Представление = НаимСтрока + ИННСтрока + КППСтрока;
			
		КонецЕсли;
	Иначе
		
		ФИОДанныеНП = ДанныеНП.Строки.Найти("ФИО", "Имя");
		
		Если НЕ ЗначениеЗаполнено(ФИОДанныеНП) Тогда
			ФИОДанныеНП = ДанныеНП.Родитель.Строки.Найти("ФИО", "Имя");
		КонецЕсли;
		
		Если ЗначениеЗаполнено(ФИОДанныеНП) Тогда
			Для Каждого СтрокаФИО Из ФИОДанныеНП.Строки Цикл
				Представление = Представление+ " " +СтрокаФИО.Значение;
			КонецЦикла; 
		КонецЕсли;
		
		ИННДанныеНП = ДанныеНП.Строки.Найти("ИННФЛ", "Имя");
		
		Если ЗначениеЗаполнено(ИННДанныеНП) Тогда
			Представление = Представление + ", " + ИННДанныеНП.Значение;
		КонецЕсли;
	
	КонецЕсли;
	
	Возврат СокрЛП(Представление);
	
КонецФункции

#КонецОбласти

Процедура ПолучитьИзображенияПрисоединенныхФайловИсточников(ФайлыИсточников, ИдентификаторФормыВладельца) Экспорт
	
	Для каждого ФайлыИсточника Из ФайлыИсточников Цикл
		ИсточникСсылка = ФайлыИсточника.Ключ;	
		
		МассивФайлов = Новый Массив;
		РаботаСФайлами.ЗаполнитьПрисоединенныеФайлыКОбъекту(ИсточникСсылка, МассивФайлов);
		
		СвойстваФайлов = Новый Массив;
		
		Для каждого ПрисоединенныйФайл Из МассивФайлов Цикл
			
			ДополнительныеПараметры = РаботаСФайламиКлиентСервер.ПараметрыДанныхФайла();
			ДополнительныеПараметры.ИдентификаторФормы = ИдентификаторФормыВладельца;
			ДанныеФайла = РаботаСФайлами.ДанныеФайла(ПрисоединенныйФайл, ДополнительныеПараметры);
			
			РасширениеФайла = нрег(ДанныеФайла.Расширение);
			Если РасширениеФайла = "tif"
			 ИЛИ РасширениеФайла = "jpg" 
			 ИЛИ РасширениеФайла = "jpeg" 
			 ИЛИ РасширениеФайла = "png" 
			 ИЛИ РасширениеФайла = "pdf" Тогда
			    СвойстваФайла = Новый Структура();
				СвойстваФайла.Вставить("Имя", 			ДанныеФайла.ИмяФайла);
				СвойстваФайла.Вставить("Размер", 		ДанныеФайла.Размер);
				СвойстваФайла.Вставить("АдресДанных", 	ДанныеФайла.СсылкаНаДвоичныеДанныеФайла);
				
				СвойстваФайлов.Добавить(СвойстваФайла);
			КонецЕсли;
			
		КонецЦикла;
		
		ФайлыИсточников.Вставить(ИсточникСсылка, СвойстваФайлов);
		
	КонецЦикла;
	
КонецПроцедуры

Функция СписокВыбораТиповПервичныйДокументВОтветеНаТребование(МножественныйВыбор) Экспорт
	
	МассивТипов = Метаданные.ОпределяемыеТипы.ИсточникДокументаПоТребованиюФНСБРО.Тип.Типы();
	
	ТаблицаТипов = Новый ТаблицаЗначений;
	ТаблицаТипов.Колонки.Добавить("Тип");
	ТаблицаТипов.Колонки.Добавить("НаименованиеТипа");
	
	Для каждого ТипДокумента Из МассивТипов Цикл
		
		НоваяСтрока = ТаблицаТипов.Добавить();
		НоваяСтрока.Тип = ТипДокумента;
		НоваяСтрока.НаименованиеТипа = Строка(ТипДокумента);
		
	КонецЦикла;
	
	ТаблицаТипов.Сортировать("НаименованиеТипа");
	
	СписокЗначений = Новый СписокЗначений;
	Для каждого СтрокаТаблицыТипов Из ТаблицаТипов Цикл
		СписокЗначений.Добавить(СтрокаТаблицыТипов.Тип, Строка(СтрокаТаблицыТипов.Тип));
	КонецЦикла;
	
	Если МножественныйВыбор Тогда
		
		СписокЗначений.Добавить(
			Тип("СправочникСсылка.СканированныеДокументыДляПередачиВЭлектронномВиде"), 
			НСтр("ru = 'Сканы из предыдущих ответов'"));
			
	КонецЕсли;
	
	Возврат СписокЗначений;
	
КонецФункции

Функция ЭлектронныеДокументыЭДО(ДокументИсточник) Экспорт
	
	ВидДокументаФНС         = Неопределено;
	ЕстьЭлектронныйДокумент = Ложь;
	Виды = ВидыДокументовЭДОСПодтверждением();
	
	Попытка
	
		ЭлектронныеДокументы = ЭлектронныйДокументооборотСКонтролирующимиОрганамиВызовСервераПереопределяемый.ПолучитьФайлыВыгрузкиЭД(
			ОбщегоНазначенияКлиентСервер.ЗначениеВМассиве(ДокументИсточник), Новый УникальныйИдентификатор);
		
	Исключение
		
		// Не требует специальных обработок
		ЭлектронныеДокументы = Неопределено;
		
	КонецПопытки;
	
	Если ЭлектронныеДокументы <> Неопределено Тогда
		
		СписокДокументов = ЭлектронныеДокументы.Получить(ДокументИсточник);
		
		Если СписокДокументов <> Неопределено Тогда
			
			СвойстваВладельцевЭД = Новый Соответствие;
			ЭлектронныйДокументооборотСКонтролирующимиОрганамиПереопределяемый.ОпределитьСвойстваВладельцевЭлектронныхДокументов(
				СвойстваВладельцевЭД, ДокументИсточник);
				
			СвойстваИсточника = СвойстваВладельцевЭД[ДокументИсточник];
			СоответствиеВидов = ЭлектронныйДокументооборотСКонтролирующимиОрганами.СоответствиеВидовДокументовФНС();
			ВидДокументаФНС   = СоответствиеВидов[СвойстваИсточника];
			
			Если ВидДокументаФНС <> Неопределено Тогда
				
				// Проверка на наличие двух необходимых подписей
				
				Если Виды.Найти(ВидДокументаФНС) <> Неопределено Тогда
					
					// "ЭЦП", "ФайлВыгрузки", "ФайлПодтверждения", "ЭЦППодтверждения"
					Если ТипЗнч(СписокДокументов) = Тип("Массив") 
						И СписокДокументов.Количество() = 4 Тогда
						ЕстьЭлектронныйДокумент = Истина; 
					КонецЕсли;
					
				Иначе
					ЕстьЭлектронныйДокумент = Истина;
				КонецЕсли;
					
			КонецЕсли;
			
		КонецЕсли;
		
	КонецЕсли;
	
	ДополнительныеПараметры = Новый Структура();
	ДополнительныеПараметры.Вставить("ВидДокументаФНС",         ВидДокументаФНС);
	ДополнительныеПараметры.Вставить("ЕстьЭлектронныйДокумент", ЕстьЭлектронныйДокумент);
	
	Возврат ДополнительныеПараметры;
		
КонецФункции

Процедура ЗавершитьНастройкуЗаявлений() Экспорт
	
	Запрос = Новый Запрос;
	Запрос.Текст = 
		"ВЫБРАТЬ
		|	ЗаявлениеАбонентаСпецоператораСвязи.Ссылка КАК Ссылка
		|ИЗ
		|	Документ.ЗаявлениеАбонентаСпецоператораСвязи КАК ЗаявлениеАбонентаСпецоператораСвязи
		|ГДЕ
		|	ЗаявлениеАбонентаСпецоператораСвязи.НастройкаЗавершена = Ложь
		|	И ЗаявлениеАбонентаСпецоператораСвязи.Статус В (ЗНАЧЕНИЕ(Перечисление.СтатусыЗаявленияАбонентаСпецоператораСвязи.Отклонено), ЗНАЧЕНИЕ(Перечисление.СтатусыЗаявленияАбонентаСпецоператораСвязи.Одобрено))";
	
	РезультатЗапроса = Запрос.Выполнить();
	
	ВыборкаДетальныеЗаписи = РезультатЗапроса.Выбрать();
	
	Пока ВыборкаДетальныеЗаписи.Следующий() Цикл
		ЗаявлениеОбъект = ВыборкаДетальныеЗаписи.Ссылка.ПолучитьОбъект();
		ЗаявлениеОбъект.НастройкаЗавершена = Истина;
		ЗаявлениеОбъект.Записать();
	КонецЦикла;
	
КонецПроцедуры

Процедура ЗаполнитьСпособПолученияСертификата() Экспорт
	
	Запрос = Новый Запрос;
	Запрос.Текст = 
		"ВЫБРАТЬ РАЗРЕШЕННЫЕ
		|	ЗаявлениеАбонентаСпецоператораСвязи.Ссылка КАК Ссылка
		|ИЗ
		|	Документ.ЗаявлениеАбонентаСпецоператораСвязи КАК ЗаявлениеАбонентаСпецоператораСвязи
		|ГДЕ
		|	ЗаявлениеАбонентаСпецоператораСвязи.СпособПолученияСертификата = ЗНАЧЕНИЕ(Перечисление.СпособПолученияСертификата.ПустаяСсылка)";
	
	РезультатЗапроса = Запрос.Выполнить();
	
	ВыборкаДетальныеЗаписи = РезультатЗапроса.Выбрать();
	
	Пока ВыборкаДетальныеЗаписи.Следующий() Цикл
		
		Попытка
			ЗаявлениеСсылка = ВыборкаДетальныеЗаписи.Ссылка;
			ЗаявлениеОбъект = ЗаявлениеСсылка.ПолучитьОбъект();
			ЗаявлениеОбъект.СпособПолученияСертификата = Перечисления.СпособПолученияСертификата.ИздатьНовый;
			ЗаявлениеОбъект.Записать();
		Исключение
		КонецПопытки; 
		
	КонецЦикла;
	
КонецПроцедуры

Процедура ЗаполнитьТипСверкиИОН() Экспорт
	
	Запрос = Новый Запрос;
	Запрос.Текст = 
		"ВЫБРАТЬ
		|	ЗапросНаИнформационноеОбслуживаниеНалогоплательщика.Ссылка КАК Ссылка
		|ИЗ
		|	Документ.ЗапросНаИнформационноеОбслуживаниеНалогоплательщика КАК ЗапросНаИнформационноеОбслуживаниеНалогоплательщика
		|ГДЕ
		|	ЗапросНаИнформационноеОбслуживаниеНалогоплательщика.ТипСверки = ЗНАЧЕНИЕ(Перечисление.ТипыСверокИОН.ПустаяСсылка)";
	
	РезультатЗапроса = Запрос.Выполнить();
	
	ВыборкаДетальныеЗаписи = РезультатЗапроса.Выбрать();
	
	Пока ВыборкаДетальныеЗаписи.Следующий() Цикл
		
		ЗапросИОНСсылка = ВыборкаДетальныеЗаписи.Ссылка;
		ЗапросИОНОбъект = ЗапросИОНСсылка.ПолучитьОбъект();
		
		Если ЗапросИОНСсылка.ВидУслуги = ПредопределенноеЗначение("Перечисление.ВидыУслугПриИОН.ПредставлениеСправкиОбИсполненииОбязанностейПоУплате") Тогда
			ТипСверки = Перечисления.ТипыСверокИОН.ВЦеломПоОрганизации;
		Иначе
			ТипСверки = Перечисления.ТипыСверокИОН.ПоОдномуКППВОднуФНС;
		КонецЕсли;
		
		ЗапросИОНОбъект.ТипСверки = ТипСверки;
		ЗапросИОНОбъект.Записать();
		
	КонецЦикла;
	
КонецПроцедуры

Процедура ЗаполнитьПериодЗапросовИОН() Экспорт
	
	Запрос = Новый Запрос;
	Запрос.Текст = 
		"ВЫБРАТЬ
		|	ЖурналОтправокВКонтролирующиеОрганы.Ссылка КАК Ссылка
		|ИЗ
		|	РегистрСведений.ЖурналОтправокВКонтролирующиеОрганы КАК ЖурналОтправокВКонтролирующиеОрганы
		|ГДЕ
		|	(ЖурналОтправокВКонтролирующиеОрганы.ДатаНачала = ДАТАВРЕМЯ(1, 1, 1)
		|			ИЛИ ЖурналОтправокВКонтролирующиеОрганы.ДатаОкончания = ДАТАВРЕМЯ(1, 1, 1))
		|	И ЖурналОтправокВКонтролирующиеОрганы.СтраницаЖурнала = ЗНАЧЕНИЕ(Перечисление.СтраницыЖурналаОтчетность.Сверки)";
	
	РезультатЗапроса = Запрос.Выполнить();
	
	ВыборкаДетальныеЗаписи = РезультатЗапроса.Выбрать();
	
	Пока ВыборкаДетальныеЗаписи.Следующий() Цикл
		
		Если НЕ ЗначениеЗаполнено(ВыборкаДетальныеЗаписи.Ссылка) Тогда
			Продолжить;
		КонецЕсли;
		
		Свойства = СвойстваЗапросаНаИнформационноеОбслуживание(ВыборкаДетальныеЗаписи.Ссылка);
		
		ЭлектронныйДокументооборотСКонтролирующимиОрганамиВызовСервера.ИзменитьЗначениеВФорме1СОтчетность(
			ВыборкаДетальныеЗаписи.Ссылка, 
			"ДатаНачала", 
			Свойства.ДатаНачала);
			
		ЭлектронныйДокументооборотСКонтролирующимиОрганамиВызовСервера.ИзменитьЗначениеВФорме1СОтчетность(
			ВыборкаДетальныеЗаписи.Ссылка, 
			"ДатаОкончания", 
			Свойства.ДатаОкончания);
			
	КонецЦикла;
	
КонецПроцедуры

#Область ПечатьПояснения

Функция ТаблицаКонтрагентовПояснения(Пояснение) Экспорт
	
	ТаблицаИНН = Пояснение.Контрагенты.Выгрузить(, "ИНН, КПП");

	// Дополняем таблицу с ИНН значениями, указанными в колонках табличных частей
	ДобавитьКонтрагентов(ТаблицаИНН, Пояснение.КнигаПокупок, "ПосредникИНН");
	ДобавитьКонтрагентов(ТаблицаИНН, Пояснение.КнигаПокупок, "ПосредникИННРасхождение");
	ДобавитьКонтрагентов(ТаблицаИНН, Пояснение.КнигаПокупокДл, "ПосредникИНН");
	ДобавитьКонтрагентов(ТаблицаИНН, Пояснение.КнигаПокупокДл, "ПосредникИННРасхождение");
	ДобавитьКонтрагентов(ТаблицаИНН, Пояснение.КнигаПродаж, "ПосредникИНН");
	ДобавитьКонтрагентов(ТаблицаИНН, Пояснение.КнигаПродаж, "ПосредникИННРасхождение");
	ДобавитьКонтрагентов(ТаблицаИНН, Пояснение.КнигаПродажДл, "ПосредникИНН");
	ДобавитьКонтрагентов(ТаблицаИНН, Пояснение.КнигаПродажДл, "ПосредникИННРасхождение");
	ДобавитьКонтрагентов(ТаблицаИНН, Пояснение.ЖурналУчетаВыставленныхСчетовФактур, "ПокупательИНН");
	ДобавитьКонтрагентов(ТаблицаИНН, Пояснение.ЖурналУчетаВыставленныхСчетовФактур, "ПокупательИННРасхождение");
	ДобавитьКонтрагентов(ТаблицаИНН, Пояснение.ЖурналУчетаПолученныхСчетовФактур, "ПродавецИНН");
	ДобавитьКонтрагентов(ТаблицаИНН, Пояснение.ЖурналУчетаПолученныхСчетовФактур, "ПродавецИННРасхождение");
	ДобавитьКонтрагентов(ТаблицаИНН, Пояснение.ЖурналУчетаПолученныхСчетовФактур, "СубкомиссионерИНН");
	ДобавитьКонтрагентов(ТаблицаИНН, Пояснение.ЖурналУчетаПолученныхСчетовФактур, "СубкомиссионерИННРасхождение");
	ДобавитьКонтрагентов(ТаблицаИНН, Пояснение.ВыставленныеСчетаФактурыНеплательщиковНДС, "ПокупательИНН");
	ДобавитьКонтрагентов(ТаблицаИНН, Пояснение.ВыставленныеСчетаФактурыНеплательщиковНДС, "ПокупательИННРасхождение");
	ДобавитьКонтрагентов(ТаблицаИНН, Пояснение.НетКнигаПрод, "ПокупательИНН", "ПокупательКПП");
	ДобавитьКонтрагентов(ТаблицаИНН, Пояснение.НетКнигаПрод, "ПокупательИННРасхождение", "ПокупательКППРасхождение");
	ДобавитьКонтрагентов(ТаблицаИНН, Пояснение.НетКнигаПрод, "ПосредникИНН", "ПосредникКПП");
	ДобавитьКонтрагентов(ТаблицаИНН, Пояснение.НетКнигаПрод, "ПосредникИННРасхождение", "ПосредникКППРасхождение");
	
	ТаблицаКонтрагентов = Документы.ПоясненияКДекларацииПоНДС.КонтрагентыПоСпискуИНН(ТаблицаИНН);
	
	Возврат ТаблицаКонтрагентов;
	
КонецФункции
	
Функция СведенияОКонтрагентахПояснения(Пояснение, ТаблицаКонтрагентов) Экспорт	
	
	СведенияОКонтрагентах = Пояснение.Контрагенты.Выгрузить();
	СведенияОКонтрагентах.Индексы.Добавить("ИдентификаторЗначения");
	СведенияОКонтрагентах.Колонки.Добавить("Контрагенты", Новый ОписаниеТипов("СписокЗначений"));
	СведенияОКонтрагентах.Колонки.Добавить("ПредставлениеКонтрагента", Новый ОписаниеТипов("Строка"));
	
	Для Каждого СведениеОКонтрагенте ИЗ СведенияОКонтрагентах Цикл
		
		СведениеОКонтрагенте.Контрагенты = Новый СписокЗначений();
		
		ПредставленияКонтрагента = Новый Массив;
		Если ТаблицаКонтрагентов <> Неопределено Тогда
			
			Отбор = Новый Структура();
			Отбор.Вставить("ИНН", СведениеОКонтрагенте.ИНН);
			
			Если ЗначениеЗаполнено(СведениеОКонтрагенте.КПП) Тогда
				Отбор.Вставить("КПП", СведениеОКонтрагенте.КПП);
			КонецЕсли;
			
			СтрокиКонтрагента = ТаблицаКонтрагентов.НайтиСтроки(Отбор);
			
			Для Каждого СтрокаКонтрагента Из СтрокиКонтрагента Цикл
				СведениеОКонтрагенте.Контрагенты.Добавить(СтрокаКонтрагента.Контрагент);
				ПредставленияКонтрагента.Добавить(СтрокаКонтрагента.КонтрагентПредставление);
			КонецЦикла;
		КонецЕсли;
		
		СведениеОКонтрагенте.ПредставлениеКонтрагента = Документы.ПоясненияКДекларацииПоНДС.ПредставлениеКонтрагента(
					СведениеОКонтрагенте, СтрСоединить(ПредставленияКонтрагента, ";"));
	КонецЦикла;
				
	Возврат СведенияОКонтрагентах;
				
КонецФункции

Процедура ДобавитьКонтрагентов(Таблица, ТаблицаДанных, ПолеИНН, ПолеКПП = "")
	
	Для Каждого СтрокаТаблицыДанных Из ТаблицаДанных Цикл
		
		Если ЗначениеЗаполнено(СтрокаТаблицыДанных[ПолеИНН]) Тогда
			
			СтрокаИНН = Таблица.Добавить();
			СтрокаИНН.ИНН = СтрокаТаблицыДанных[ПолеИНН];
			
			Если ЗначениеЗаполнено(ПолеКПП) Тогда
				СтрокаИНН.КПП = СтрокаТаблицыДанных[ПолеКПП];
			КонецЕсли;
			
		КонецЕсли;
		
	КонецЦикла;
	
КонецПроцедуры
				
Процедура ВывестиСтрокуЖурнала(Бланк, ИмяОбласти, СтрокаТЧ, СтрокаПосредническойДеятельности, ТабДокумент)
	
	СтрокаТабДока = Бланк.ПолучитьОбласть(ИмяОбласти);
	СтрокаТабДока.Параметры.Заполнить(СтрокаТЧ);
	СтрокаТабДока.Параметры.Заполнить(СтрокаПосредническойДеятельности);
	ТабДокумент.Вывести(СтрокаТабДока);
	
КонецПроцедуры

Процедура ВывестиСтрокуЖурналаРасхождение(Бланк, ИмяОбласти, СтрокаТЧ, СтрокиПосредническойДеятельностиРасхождение, НомерСтрокиРасхождение, СтрокиПосредническойДеятельности, ТабДокумент)
	
	СтрокаПосредническойДеятельностиРасхождение = СтрокиПосредническойДеятельностиРасхождение[НомерСтрокиРасхождение];
	
	СтрокаТабДока = Бланк.ПолучитьОбласть(ИмяОбласти);
	СтрокаТабДока.Параметры.Заполнить(СтрокаТЧ);
	СтрокаТабДока.Параметры.Заполнить(СтрокаПосредническойДеятельностиРасхождение);
	
	Колонки = Новый Массив;
	Колонки.Добавить("ДатаСчетаФактуры");
	Колонки.Добавить("КодВалюты");
	Колонки.Добавить("НомерСчетаФактуры");
	Колонки.Добавить("ПродавецИНН");
	Колонки.Добавить("РазницаСтоимостиУвеличение");
	Колонки.Добавить("РазницаСтоимостиУменьшение");
	Колонки.Добавить("РазницаСуммыНДСУвеличение");
	Колонки.Добавить("РазницаСуммыНДСУменьшение");
	Колонки.Добавить("Стоимость");
	Колонки.Добавить("СуммаНДС");
	
	ЕстьСовпадение = Ложь;
	Для НомерСтроки = 0 По СтрокиПосредническойДеятельности.Количество() - 1 Цикл
		
		ВсеКолонкиСовпадают = Истина;
		Для каждого Колонка Из Колонки Цикл
			Если СтрокаПосредническойДеятельностиРасхождение[Колонка] <> СтрокиПосредническойДеятельности[НомерСтроки][Колонка] Тогда 
				ВсеКолонкиСовпадают = Ложь;
				Прервать;
			КонецЕсли;
		КонецЦикла; 
		
		Если ВсеКолонкиСовпадают И НомерСтрокиРасхождение = НомерСтроки Тогда
			ЕстьСовпадение = Истина;
		КонецЕсли;
		
	КонецЦикла;
	
	Если НЕ ЕстьСовпадение Тогда
		СтрокаТабДока.Области[ИмяОбласти + "__ПосредническаяДеятельность"].Шрифт = Новый Шрифт(,,Истина);
	КонецЕсли;
	
	ВыделитьРасхождениеВСтрокеПояснения(СтрокаТабДока, СтрокаТЧ, ИмяОбласти);
	
	ТабДокумент.Вывести(СтрокаТабДока);
	
КонецПроцедуры

Процедура ВывестиСтрокиПосредническойДеятельностиРасхождение(СтрокиПосредническойДеятельности, СтрокиПосредническойДеятельностиРасхождение, СтрокаТЧ, ТабДокумент, Бланк)
	
	Если СтрокиПосредническойДеятельностиРасхождение.Количество() = 1 Тогда
		// Одна строка расхожений
		ВывестиСтрокуЖурналаРасхождение(Бланк, "СтрокаЖурналУчетаВыставленныхСчетовФактур_Расхождения_Один", СтрокаТЧ, СтрокиПосредническойДеятельностиРасхождение, 0, СтрокиПосредническойДеятельности, ТабДокумент);
	Иначе
		// Много строк расхожений
		Для Номер = 0 По СтрокиПосредническойДеятельностиРасхождение.Количество() - 1 Цикл
		
			Если Номер = 0 Тогда
				ИмяОбласти = "СтрокаЖурналУчетаВыставленныхСчетовФактур_Расхождения_Много_Начало";
			ИначеЕсли Номер = СтрокиПосредническойДеятельностиРасхождение.Количество() - 1 Тогда
				ИмяОбласти = "СтрокаЖурналУчетаВыставленныхСчетовФактур_Расхождения_Много_Конец";
			Иначе
				ИмяОбласти = "СтрокаЖурналУчетаВыставленныхСчетовФактур_Расхождения_Много_Середина";
			КонецЕсли;
			
			ВывестиСтрокуЖурналаРасхождение(Бланк, ИмяОбласти, СтрокаТЧ, СтрокиПосредническойДеятельностиРасхождение, Номер, СтрокиПосредническойДеятельности, ТабДокумент);
			
		КонецЦикла;
		
	КонецЕсли;
	
КонецПроцедуры

Процедура ВывестиСтрокиПосредническойДеятельности(СтрокиПосредническойДеятельности, СтрокиПосредническойДеятельностиРасхождение, СтрокаТЧ, ТабДокумент, Бланк)
	
	Если СтрокиПосредническойДеятельности.Количество() = 1 Тогда
		
		Если СтрокаТЧ.РезультатПроверки = Перечисления.РезультатПроверкиСтрокиДекларации.ВыявленыРасхождения Тогда
			ИмяОбласти = "СтрокаЖурналУчетаВыставленныхСчетовФактур_ФНС_Один_Расхождения_Начало";
		Иначе
			ИмяОбласти = "СтрокаЖурналУчетаВыставленныхСчетовФактур_ФНС_1_0";
		КонецЕсли;
		
		ВывестиСтрокуЖурнала(Бланк, ИмяОбласти, СтрокаТЧ, СтрокиПосредническойДеятельности[0], ТабДокумент);
		
	Иначе
		Для Номер = 0 По СтрокиПосредническойДеятельности.Количество() - 1 Цикл
			
			Если Номер = 0 Тогда
				ИмяОбласти = "СтрокаЖурналУчетаВыставленныхСчетовФактур_ФНС_Много_Начало";
			ИначеЕсли Номер = СтрокиПосредническойДеятельности.Количество() - 1 Тогда
				Если СтрокаТЧ.РезультатПроверки = Перечисления.РезультатПроверкиСтрокиДекларации.ВыявленыРасхождения Тогда
					ИмяОбласти = "СтрокаЖурналУчетаВыставленныхСчетовФактур_ФНС_Много_Расхождения_Конец";
				Иначе
					ИмяОбласти = "СтрокаЖурналУчетаВыставленныхСчетовФактур_ФНС_Много_Конец";
				КонецЕсли;
			Иначе
				ИмяОбласти = "СтрокаЖурналУчетаВыставленныхСчетовФактур_ФНС_Много_Середина";
			КонецЕсли;
			
			ВывестиСтрокуЖурнала(Бланк, ИмяОбласти, СтрокаТЧ, СтрокиПосредническойДеятельности[Номер], ТабДокумент);
			
		КонецЦикла;
	КонецЕсли;
		
КонецПроцедуры

Функция ПредставлениеКонтрагентовПоясненияПоИННиКПП(СтрокаТаблицы, ПолеИНН, ТаблицаКонтрагентов, ПолеКПП = "")
	
	СведенияОКонтрагенте = Новый Структура("ИНН, КПП");
	СведенияОКонтрагенте.ИНН = СтрокаТаблицы[ПолеИНН];
	
	Если ЗначениеЗаполнено(ПолеКПП) Тогда
		
		СведенияОКонтрагенте.КПП = СтрокаТаблицы[ПолеКПП];
		
		Сведения = Документы.ПоясненияКДекларацииПоНДС.СведенияОКонтрагентеПоТаблицеКонтрагентов(
			СведенияОКонтрагенте.ИНН, 
			ТаблицаКонтрагентов, 
			СведенияОКонтрагенте.КПП);
		
		Контрагенты = Сведения.Контрагенты;
		
	Иначе
			
		Сведения = Документы.ПоясненияКДекларацииПоНДС.СведенияОКонтрагентеПоТаблицеКонтрагентов(
			СведенияОКонтрагенте.ИНН, 
			ТаблицаКонтрагентов);
			
		Контрагенты = Сведения.Контрагенты;
		
	КонецЕсли;
	
	Представление = ЭлектронныйДокументооборотСКонтролирующимиОрганамиКлиентСервер.ПредставлениеПоСпискуСтрок(Контрагенты);
	Представление = Документы.ПоясненияКДекларацииПоНДС.ПредставлениеКонтрагента(СведенияОКонтрагенте, Представление);
	Возврат Представление;
	
КонецФункции

Процедура ДополнитьПояснениеПредставлениямиПолей(Пояснение, ВыбранныеСтроки)
	
	ВТребованииЕстьXMLФайл = ЕстьТребованияКРазделам8_12(Пояснение.Требование);
	ДанныеТребования       = Справочники.ДокументыРеализацииПолномочийНалоговыхОрганов.ДанныеТребованияОПредставленииПоясненийКДекларацииНДС(Пояснение.Требование);
	
	Если ВТребованииЕстьXMLФайл Тогда
		ВерсияФоматаТребования = ДанныеТребования.ВерсФорм;
	КонецЕсли;
	
	// Добавляем представление в документы оплаты
	ДокументыОплаты = Пояснение.ДокументыОплаты.Выгрузить();
	ДокументыОплаты.Колонки.Добавить("Представление", Новый ОписаниеТипов("Строка"));
	
	Для каждого ДокументОплаты Из ДокументыОплаты Цикл
		ДокументОплаты.Представление = Документы.ПоясненияКДекларацииПоНДС.ПредставлениеДокументаОплаты(ДокументОплаты);
	КонецЦикла; 
	
	// Добавляем представление в даты принятия на учет
	ДатыПринятияНаУчет = Пояснение.ДатыПринятияНаУчет.Выгрузить();
	ДатыПринятияНаУчет.Колонки.Добавить("Представление", Новый ОписаниеТипов("Строка"));
	
	Для каждого ДатаПринятияНаУчет Из ДатыПринятияНаУчет Цикл
		ДатаПринятияНаУчет.Представление = Документы.ПоясненияКДекларацииПоНДС.ПредставлениеДатыПринятияНаУчет(ДатаПринятияНаУчет);
	КонецЦикла;
	
	// СведенияОКонтрагентах
	ТаблицаКонтрагентов = ТаблицаКонтрагентовПояснения(Пояснение);
	СведенияОКонтрагентах = СведенияОКонтрагентахПояснения(Пояснение, ТаблицаКонтрагентов);
	СведенияОКонтрагентах.Колонки.ПредставлениеКонтрагента.Имя = "Представление";
		
	// Помещаем недостающие реквизиты в таблицу
	Для каждого ДанныеПоРазделу Из ВыбранныеСтроки Цикл
		
		Имя    = ДанныеПоРазделу.Ключ;
		Строки = ДанныеПоРазделу.Значение;
		
		Если Имя = "СведКС" ИЛИ Имя = "ПояснИнОсн" Тогда
			Продолжить;
		КонецЕсли;
		
		Для каждого СтрокаТЧ Из Строки Цикл
			
			
			Если Имя = "КнигаПокупок" ИЛИ Имя = "КнигаПокупокДл" Тогда
				
				СтрокаТЧ.Вставить("ОшибкаПредставление", Документы.ПоясненияКДекларацииПоНДС.ПредставлениеОшибкиРаздела8(СтрокаТЧ.КодОшибки, СтрокаТЧ.ГрафаОшибки));
				
				СтрокаТЧ.Вставить("ПродавецПредставление", ПредставлениеМножественногоЗначенияПояснения(СтрокаТЧ.Продавец, СведенияОКонтрагентах));
				СтрокаТЧ.Вставить("ПродавецРасхождениеПредставление", ПредставлениеМножественногоЗначенияПояснения(СтрокаТЧ.ПродавецРасхождение, СведенияОКонтрагентах));
				
				СтрокаТЧ.Вставить("ПосредникИННПредставление", ПредставлениеКонтрагентовПоясненияПоИННиКПП(СтрокаТЧ, "ПосредникИНН", ТаблицаКонтрагентов));
				СтрокаТЧ.Вставить("ПосредникИННРасхождениеПредставление", ПредставлениеКонтрагентовПоясненияПоИННиКПП(СтрокаТЧ, "ПосредникИННРасхождение", ТаблицаКонтрагентов));
				
			КонецЕсли;

			Если Имя = "КнигаПродаж" ИЛИ Имя = "КнигаПродажДл" Тогда
				
				СтрокаТЧ.Вставить("ОшибкаПредставление", Документы.ПоясненияКДекларацииПоНДС.ПредставлениеОшибкиРаздела9(СтрокаТЧ.КодОшибки, СтрокаТЧ.ГрафаОшибки, ВерсияФоматаТребования));
				
				СтрокаТЧ.Вставить("ПокупательПредставление", ПредставлениеМножественногоЗначенияПояснения(СтрокаТЧ.Покупатель, СведенияОКонтрагентах));
				СтрокаТЧ.Вставить("ПокупательРасхождениеПредставление", ПредставлениеМножественногоЗначенияПояснения(СтрокаТЧ.ПокупательРасхождение, СведенияОКонтрагентах));

				СтрокаТЧ.Вставить("ПосредникИННПредставление", ПредставлениеКонтрагентовПоясненияПоИННиКПП(СтрокаТЧ, "ПосредникИНН", ТаблицаКонтрагентов));
				СтрокаТЧ.Вставить("ПосредникИННРасхождениеПредставление", ПредставлениеКонтрагентовПоясненияПоИННиКПП(СтрокаТЧ, "ПосредникИННРасхождение", ТаблицаКонтрагентов));
				
			КонецЕсли;

			Если Имя = "ЖурналУчетаВыставленныхСчетовФактур" Тогда
				
				СтрокаТЧ.Вставить("ОшибкаПредставление", Документы.ПоясненияКДекларацииПоНДС.ПредставлениеОшибкиРаздела10(СтрокаТЧ.КодОшибки, СтрокаТЧ.ГрафаОшибки));
				
				СтрокаТЧ.Вставить("ПокупательИННПредставление", ПредставлениеКонтрагентовПоясненияПоИННиКПП(СтрокаТЧ, "ПокупательИНН", ТаблицаКонтрагентов));
				СтрокаТЧ.Вставить("ПокупательИННРасхождениеПредставление", ПредставлениеКонтрагентовПоясненияПоИННиКПП(СтрокаТЧ, "ПокупательИННРасхождение", ТаблицаКонтрагентов));

			КонецЕсли;

			Если Имя = "ЖурналУчетаПолученныхСчетовФактур" Тогда
				
				СтрокаТЧ.Вставить("ОшибкаПредставление", Документы.ПоясненияКДекларацииПоНДС.ПредставлениеОшибкиРаздела11(СтрокаТЧ.КодОшибки, СтрокаТЧ.ГрафаОшибки));
				
				СтрокаТЧ.Вставить("ПродавецИННПредставление", ПредставлениеКонтрагентовПоясненияПоИННиКПП(СтрокаТЧ, "ПродавецИНН", ТаблицаКонтрагентов));
				СтрокаТЧ.Вставить("ПродавецИННРасхождениеПредставление", ПредставлениеКонтрагентовПоясненияПоИННиКПП(СтрокаТЧ, "ПродавецИННРасхождение", ТаблицаКонтрагентов));
				
				СтрокаТЧ.Вставить("СубкомиссионерИННПредставление", ПредставлениеКонтрагентовПоясненияПоИННиКПП(СтрокаТЧ, "СубкомиссионерИНН", ТаблицаКонтрагентов));
				СтрокаТЧ.Вставить("СубкомиссионерИННРасхождениеПредставление", ПредставлениеКонтрагентовПоясненияПоИННиКПП(СтрокаТЧ, "СубкомиссионерИННРасхождение", ТаблицаКонтрагентов));
				
			КонецЕсли;

			Если Имя = "ВыставленныеСчетаФактурыНеплательщиковНДС" Тогда
				
				СтрокаТЧ.Вставить("ОшибкаПредставление", Документы.ПоясненияКДекларацииПоНДС.ПредставлениеОшибкиРаздела12(СтрокаТЧ.КодОшибки, СтрокаТЧ.ГрафаОшибки));
				
				СтрокаТЧ.Вставить("ПокупательИННПредставление", ПредставлениеКонтрагентовПоясненияПоИННиКПП(СтрокаТЧ, "ПокупательИНН", ТаблицаКонтрагентов));
				СтрокаТЧ.Вставить("ПокупательИННРасхождениеПредставление", ПредставлениеКонтрагентовПоясненияПоИННиКПП(СтрокаТЧ, "ПокупательИННРасхождение", ТаблицаКонтрагентов));
				
			КонецЕсли;

			Если Имя <> "НетКнигаПрод" Тогда
				СтрокаТЧ.Вставить("ПериодОтраженияЗаписиПредставление", Документы.ПоясненияКДекларацииПоНДС.ПредставлениеПериодаОтраженияЗаписи(СтрокаТЧ));
			КонецЕсли;
			
			// Представление СФ
			Если Имя <> "ВыставленныеСчетаФактурыНеплательщиковНДС" И Имя <> "НетКнигаПрод" Тогда
				
				РеквизитыСчетаФактуры = ЭлектронныйДокументооборотСКонтролирующимиОрганамиКлиентСервер.ПредставлениеСчетаФактурыПояснения(СтрокаТЧ, "");
			    СтрокаТЧ.Вставить("РеквизитыСчетаФактуры", РеквизитыСчетаФактуры);
				
				РеквизитыСчетаФактурыРасхождение = ЭлектронныйДокументооборотСКонтролирующимиОрганамиКлиентСервер.ПредставлениеСчетаФактурыПояснения(СтрокаТЧ, "Расхождение");
				СтрокаТЧ.Вставить("РеквизитыСчетаФактурыРасхождение", РеквизитыСчетаФактурыРасхождение);
				
			КонецЕсли;
			
			// Док. оплаты
			Если Имя = "КнигаПокупок" ИЛИ Имя = "КнигаПокупокДл" ИЛИ Имя = "КнигаПродаж" ИЛИ Имя = "КнигаПродажДл" Тогда
				
				ДокументОплатыПредставление = ПредставлениеМножественногоЗначенияПояснения(СтрокаТЧ.ДокументОплаты, ДокументыОплаты);
				ДокументОплатыРасхождениеПредставление = ПредставлениеМножественногоЗначенияПояснения(СтрокаТЧ.ДокументОплатыРасхождение, ДокументыОплаты);
				
				СтрокаТЧ.Вставить("ДокументОплатыПредставление", ДокументОплатыПредставление);
				СтрокаТЧ.Вставить("ДокументОплатыРасхождениеПредставление", ДокументОплатыРасхождениеПредставление);
				
			КонецЕсли;
			
			// ДатаПринятияНаУчет
			Если Имя = "КнигаПокупок" ИЛИ Имя = "КнигаПокупокДл" Тогда
				ДатаПринятияНаУчет = ПредставлениеМножественногоЗначенияПояснения(СтрокаТЧ.ДатаПринятияНаУчет, ДатыПринятияНаУчет);
				ДатаПринятияНаУчетРасхождение = ПредставлениеМножественногоЗначенияПояснения(СтрокаТЧ.ДатаПринятияНаУчетРасхождение, ДатыПринятияНаУчет);
				
				СтрокаТЧ.Вставить("ДатаПринятияНаУчетДата", ДатаПринятияНаУчет);
				СтрокаТЧ.Вставить("ДатаПринятияНаУчетРасхождениеДата", ДатаПринятияНаУчетРасхождение);
			КонецЕсли;
			
		КонецЦикла;
		
	КонецЦикла;
	
КонецПроцедуры

Функция ПредставлениеМножественногоЗначенияПояснения(ИдентификаторЗначения, Таблица)
	
	Отбор = Новый Структура();
	Отбор.Вставить("ИдентификаторЗначения", ИдентификаторЗначения);
	СписокСтрок = Таблица.НайтиСтроки(Отбор);
	Представление = ЭлектронныйДокументооборотСКонтролирующимиОрганамиКлиентСервер.ПредставлениеПоСпискуСтрок(СписокСтрок);
	
	Возврат Представление;
	
КонецФункции	

Функция ТабличныйДокументПояснения(Пояснение, ВыбранныеСтроки, ЭтоПечатьИзОткрытогоПояснения) Экспорт
	
	Если НЕ ЭтоПечатьИзОткрытогоПояснения Тогда
		ДополнитьПояснениеПредставлениямиПолей(Пояснение, ВыбранныеСтроки);
	КонецЕсли;
	
	КонтекстЭДОСервер = ДокументооборотСКО.ПолучитьОбработкуЭДО();
	ТабДокумент       = Новый ТабличныйДокумент;
	
	// Шапка
	Бланк = КонтекстЭДОСервер.ПолучитьМакет("ПояснениеПоНДС");
	Шапка = Бланк.ПолучитьОбласть("Шапка");
	Шапка.Параметры["Наименование"] = Строка(Пояснение);
	ТабДокумент.Вывести(Шапка);
	
	СведенияВерны       = Перечисления.РезультатПроверкиСтрокиДекларации.СведенияВерны;
	ВыявленыРасхождения = Перечисления.РезультатПроверкиСтрокиДекларации.ВыявленыРасхождения;
	
	Для каждого ДанныеПоРазделу Из ВыбранныеСтроки Цикл
		
		Имя = ДанныеПоРазделу.Ключ;
		
		Если Имя = "ПояснИнОсн" Тогда
			
			Если ЗначениеЗаполнено(Пояснение.ПояснИнОсн) Тогда
				ОбластьПояснИнОсн = Бланк.ПолучитьОбласть("ПояснИнОсн");
				ОбластьПояснИнОсн.Параметры.ПояснИнОсн = Пояснение.ПояснИнОсн;
				ТабДокумент.Вывести(ОбластьПояснИнОсн);
			КонецЕсли;
			
		Иначе
		
			Строки = ДанныеПоРазделу.Значение;
			
			Если Строки.Количество() = 0 Тогда
				Продолжить;
			КонецЕсли;
			
			Если Прав(Имя, 2) = "Дл" Тогда
				ИмяБезДл = Сред(Имя, 0, СтрДлина(Имя) - 2);
			Иначе
				ИмяБезДл = Имя;
			КонецЕсли;
			
			// Шапка ТЧ
			ЗаголовокТЧ = Бланк.ПолучитьОбласть("Заголовок" + Имя);
			ТабДокумент.Вывести(ЗаголовокТЧ);

			ШапкаТЧ = Бланк.ПолучитьОбласть("Шапка" + ИмяБезДл);
			
			ТабДокумент.Вывести(ШапкаТЧ);
			
			// Строка ТЧ
			Для каждого СтрокаТЧ Из Строки Цикл
				
				Если ИмяБезДл = "ЖурналУчетаВыставленныхСчетовФактур" Тогда
					
					Отбор = Новый Структура();
					Отбор.Вставить("ИдентификаторЗначения", СтрокаТЧ.ПосредническаяДеятельность);
					СтрокиПосредническойДеятельности = Пояснение.ПосредническаяДеятельность.НайтиСтроки(Отбор);
					
					Отбор = Новый Структура();
					Отбор.Вставить("ИдентификаторЗначения", СтрокаТЧ.ПосредническаяДеятельностьРасхождение);
					СтрокиПосредническойДеятельностиРасхождение = Пояснение.ПосредническаяДеятельность.НайтиСтроки(Отбор);
					
					ВывестиСтрокиПосредническойДеятельности(СтрокиПосредническойДеятельности, СтрокиПосредническойДеятельностиРасхождение, СтрокаТЧ, ТабДокумент, Бланк);
					
					Если СтрокаТЧ.РезультатПроверки = ВыявленыРасхождения Тогда
						ВывестиСтрокиПосредническойДеятельностиРасхождение(СтрокиПосредническойДеятельности, СтрокиПосредническойДеятельностиРасхождение, СтрокаТЧ, ТабДокумент, Бланк);
					КонецЕсли;
					
				Иначе
					
					
					Если ИмяБезДл <> "СведКС" И СтрокаТЧ.РезультатПроверки = ВыявленыРасхождения Тогда
						ИмяОбласти = "Строка" + ИмяБезДл + "СРасхождением";
					Иначе
						ИмяОбласти = "Строка" + ИмяБезДл;
					КонецЕсли;
					СтрокаТабДока = Бланк.ПолучитьОбласть(ИмяОбласти);
					
					СтрокаТабДока.Параметры.Заполнить(СтрокаТЧ);
					
					Если ИмяБезДл = "НетКнигаПрод" Тогда
						Если СтрокаТЧ.РезультатПроверки = СведенияВерны Тогда
							СтрокаТабДока.Параметры.РезультатПроверки = НСтр("ru = 'Не подтверждается'");
						ИначеЕсли СтрокаТЧ.РезультатПроверки = ВыявленыРасхождения Тогда
							СтрокаТабДока.Параметры.РезультатПроверки = НСтр("ru = 'Выявлены расхождения'");
						КонецЕсли;
					КонецЕсли;
					
					ВыделитьРасхождениеВСтрокеПояснения(СтрокаТабДока, СтрокаТЧ, ИмяОбласти);
					
					ТабДокумент.Вывести(СтрокаТабДока);
					
				КонецЕсли;
				
			КонецЦикла;
			
		КонецЕсли;
	
	КонецЦикла;
	
	ТабДокумент.АвтоМасштаб = Истина;
	ТабДокумент.ОриентацияСтраницы = ОриентацияСтраницы.Ландшафт;
	
	ДобавитьШтампПодписиПодДокументом(Пояснение, Перечисления.ТипыТранспортныхСообщений.ПредставлениеНП, ТабДокумент, 3);
	
	Возврат ТабДокумент; 
	
КонецФункции

Процедура ВыделитьРасхождениеВСтрокеПояснения(СтрокаТабДока, СтрокаТЧ, ИмяОбласти)
	
	Для каждого ОбластьТабДока Из СтрокаТабДока.Области Цикл
		
		Имя = ОбластьТабДока.Имя;
		Позиция = СтрНайти(Имя, "__");
		Если Позиция = 0 Тогда
			Продолжить;
		КонецЕсли;
		
		ИмяПараметраРасхождения = Сред(ОбластьТабДока.Имя, Позиция + 2);
		ИмяПараметра = СтрЗаменить(ИмяПараметраРасхождения, "Расхождение", "");
		
		Попытка
			Если СтрокаТЧ[ИмяПараметра] <> СтрокаТЧ[ИмяПараметраРасхождения] Тогда
				СтрокаТабДока.Области[ИмяОбласти + "__" + ИмяПараметраРасхождения].Шрифт = Новый Шрифт(,,Истина);
			КонецЕсли;
		Исключение
			// Не нужна специальная обработка - нет такого параметра или области
		КонецПопытки;
		
	КонецЦикла;
	
КонецПроцедуры 

#КонецОбласти

#Область РазборИВыводАктаСверкиСФНСвФорматеXML

// Функция - разбирает акт сверки с ФНС в формате XML, по данным файла создает структуру данных.
//
// Параметры:
//  АдресХранилища	 - 	Строка - адрес хранилища XML файла.
// 
// Возвращаемое значение:
//  Структура - данные акта сверки с ФНС.
//
Функция РазборАктаСверкиВФорматеXML(АдресХранилища) Экспорт
	
	ИмяВременногоФайла = ПолучитьИмяВременногоФайла("xml");
	ДвоичныеДанныеФайла = ПолучитьИзВременногоХранилища(АдресХранилища);
	ДвоичныеДанныеФайла.Записать(ИмяВременногоФайла);
	
	ЧтениеXML = Новый ЧтениеXML;
	ЧтениеXML.ОткрытьФайл(ИмяВременногоФайла);
	ЧтениеXML.ПерейтиКСодержимому();
	
	Фабрика = ФабрикаXDTO;
	ОтветФНСXDTO = Фабрика.ПрочитатьXML(ЧтениеXML);
	ЧтениеXML.Закрыть();
	
	УдалитьФайлы(ИмяВременногоФайла);
	
	ДокументXDTO = ОтветФНСXDTO.Документ;
	
	АктыСверок = НовыеАктыСверок();
	
	// В схеме АктСвер обязательный элемент, но иногда может отсутствовать
	СвойствоXDTOАктСвер = ДокументXDTO.Свойства().Получить("АктСвер"); 
	Если СвойствоXDTOАктСвер = Неопределено Тогда
		Возврат АктыСверок;
	КонецЕсли;
	
	Если ТипЗнч(ДокументXDTO.АктСвер) = Тип("СписокXDTO") Тогда
		
		Для Каждого АктСверXDTO Из ДокументXDTO.АктСвер Цикл
			
			ЗаполнитьАктСверки(АктСверXDTO, АктыСверок);
			
		КонецЦикла;
		
	Иначе
		
		ЗаполнитьАктСверки(ДокументXDTO.АктСвер, АктыСверок);
		
	КонецЕсли;
	
	Возврат АктыСверок;
	
КонецФункции

// Функция - получает табличное представление акта сверки
//
// Параметры:
//  ДанныеАктаСверки - 	ТаблицаЗначений - разобранные данные акта сверки.
//  ИндексСверки - 		Число - индекс cтроки cверки.
//
// Возвращаемое значение:
//  Табличный документ - табличный документ с данными акта сверки с ФНС.
//
Функция ПолучитьТабличноеПредставление(ЗапросИОН, ДанныеАктаСверки, ИндексСверки) Экспорт
	
	КонтекстЭДОСервер = ДокументооборотСКО.ПолучитьОбработкуЭДО();
	
	АктСверки = Новый ТабличныйДокумент;
	МакетАкта = КонтекстЭДОСервер.ПолучитьМакет("АктСверкиСФНС");
	
	// ШАПКА -------------------------------------------------
	
	ДанныеАктаСверки.НаименованиеИФНС = НаименованиеИФНСПоКоду(ДанныеАктаСверки.КодИФНС);
	
	Шапка = МакетАкта.ПолучитьОбласть("Шапка");
	Шапка.Параметры.НомерАкта                = ДанныеАктаСверки.НомерАкта;
	Шапка.Параметры.ДатаАкта                 = Формат(ДанныеАктаСверки.ДатаАкта, "ДЛФ=D");
	Шапка.Параметры.НаименованиеИФНС         = ДанныеАктаСверки.НаименованиеИФНС;
	Шапка.Параметры.НаименованиеОрганизации  = ДанныеАктаСверки.НаименованиеОрганизации;
	Шапка.Параметры.ИННОрганизации           = ДанныеАктаСверки.ИНН;
	Если ДанныеАктаСверки.ЭтоЮрЛицо Тогда
		Шапка.Параметры.ЗаголовокИНН         = НСтр("ru = 'ИНН / КПП:'");
		Шапка.Параметры.КППОрганизации       = " / " + ДанныеАктаСверки.КПП
	Иначе
		Шапка.Параметры.ЗаголовокИНН         = НСтр("ru = 'ИНН:'");
	КонецЕсли;
	
	АктСверки.Вывести(Шапка);
	
	// ШАПКА ТАБЛИЦЫ -----------------------------------------
	
	ШапкаТаблицы = МакетАкта.ПолучитьОбласть("ШапкаТаблицы");
	ШапкаТаблицы.Параметры.ДатаНачала = Формат(ДанныеАктаСверки.ДатаНачала, "ДЛФ=D");
	ШапкаТаблицы.Параметры.ДатаОкончания = Формат(ДанныеАктаСверки.ДатаОкончания, "ДЛФ=D");
	
	АктСверки.Вывести(ШапкаТаблицы);
	
	// ВЫВОД ДАННЫХ ПО НАЛОГАМ В ТАБЛИЧНЫЙ ДОКУМЕНТ ----------
	
	СодержаниеСверки = ДанныеАктаСверки.Раздел2;
	РасчетДополнительныхПолей(СодержаниеСверки); // контроль наличия данных, расчет доп. полей
	
	АктСверки.НачатьАвтогруппировкуСтрок();
	КолВидовНалогов = 0;
	МакетСтрокиПоВидамНалогов = МакетАкта.ПолучитьОбласть("ВидНалога");
	Для Каждого СтрокаВидаНалога Из СодержаниеСверки Цикл
		
		Если СтрокаВидаНалога.ВидНалогаКонтроль = 0 Тогда // если по виду налога все по нулям, не выводим его
			Продолжить;
		КонецЕсли;
		
		// Если имеется несколько строк налогов с одинаковым КБК, выводим ОКТМО
		МассивВидовНалогов = СодержаниеСверки.НайтиСтроки(Новый Структура("КБК", СтрокаВидаНалога.КБК));
		КолКБК = 0;
		Для Каждого ЭлементМассива Из МассивВидовНалогов Цикл
			
			Если ЭлементМассива.ВидНалогаКонтроль > 0 Тогда
				КолКБК = КолКБК + 1;
			КонецЕсли;
			
		КонецЦикла;
		Если КолКБК > 1 Тогда
			СтрокаВидаНалога.НаименованиеНалога = СтрокаВидаНалога.НаименованиеНалога
				+ " " + НСтр("ru = 'по ОКТМО'") + " " + СтрокаВидаНалога.ОКТМО;
		КонецЕсли;
		
		КолВидовНалогов = КолВидовНалогов + 1;
		НомерСтрокиСверки = СодержаниеСверки.Индекс(СтрокаВидаНалога);
		МакетСтрокиПоВидамНалогов.Параметры.Заполнить(СтрокаВидаНалога);
		МакетСтрокиПоВидамНалогов.Параметры.СтрокаДанных = Формат(ИндексСверки, "ЧН=0; ЧГ=0")
			+ ";" + Формат(НомерСтрокиСверки, "ЧН=0; ЧГ=0"); // данные для расшифровки
		
		АктСверки.Вывести(МакетСтрокиПоВидамНалогов, 1,, Истина);
		
		МассивПодвидовНалога = Новый Массив;
		Если СтрокаВидаНалога.НалогКонтроль > 0 Тогда
			МассивПодвидовНалога.Добавить("Налог");
		КонецЕсли;
		Если СтрокаВидаНалога.ПеняКонтроль > 0 Тогда
			МассивПодвидовНалога.Добавить("Пеня");
		КонецЕсли;
		Если СтрокаВидаНалога.ШтрафКонтроль > 0 Тогда
			МассивПодвидовНалога.Добавить("Штраф");
		КонецЕсли;
		Если СтрокаВидаНалога.ПрочееКонтроль > 0 Тогда
			МассивПодвидовНалога.Добавить("Прочее");
		КонецЕсли;
		
		Если Не (МассивПодвидовНалога.Количество() = 1 И МассивПодвидовНалога[0] = "Налог") Тогда
			
			Для Каждого ПодвидНалога Из МассивПодвидовНалога Цикл
				
				МакетСтрокиДетализацияРасчетов = МакетАкта.ПолучитьОбласть(ПодвидНалога);
				МакетСтрокиДетализацияРасчетов.Параметры.Заполнить(СтрокаВидаНалога);
				МакетСтрокиДетализацияРасчетов.Параметры.СтрокаДанных = Формат(ИндексСверки, "ЧН=0; ЧГ=0")
					+ ";" + Формат(НомерСтрокиСверки, "ЧН=0; ЧГ=0");
				
				АктСверки.Вывести(МакетСтрокиДетализацияРасчетов, 2,, Истина);
				
			КонецЦикла;
			
		КонецЕсли;
		
	КонецЦикла;
	АктСверки.ЗакончитьАвтогруппировкуСтрок();
	
	Если КолВидовНалогов > 1 Тогда // если только 1 вид налога, итоги не выводим
		
		// вывод итогов
		МакетИтого = МакетАкта.ПолучитьОбласть("Итого");
		ТаблицаИтого = СодержаниеСверки.Скопировать();
		ТаблицаИтого.Колонки.Добавить("Свертка", Новый ОписаниеТипов("Булево"));
		ТаблицаИтого.Свернуть("Свертка",
			"ЗадолженностьНаНачало, ПереплатаНаНачало, НачисленоЗаПериод, УплаченоЗаПериод, ЗадолженностьНаКонец, ПереплатаНаКонец");
			
		МакетИтого.Параметры.Заполнить(ТаблицаИтого[0]);
			
		АктСверки.Вывести(МакетИтого);
		
	КонецЕсли;
	
	// незачисленные средства, вывод
	Если СодержаниеСверки.Итог("НеЗачисленныеСредстваКонтроль") > 0 Тогда

		МакетШапки = МакетАкта.ПолучитьОбласть("НезачисленныеСредстваШапка");
		
		АктСверки.Вывести(МакетШапки);
		АктСверки.НачатьАвтогруппировкуСтрок();
		Для Каждого СтрокаСверки Из СодержаниеСверки Цикл
			
			Если СтрокаСверки.НеЗачисленныеСредстваКонтроль = 0 Тогда
				Продолжить;
			КонецЕсли;
			
			МакетСтроки = МакетАкта.ПолучитьОбласть("НезачисленныеСредстваВидНалога");
			НомерСтрокиСверки = СодержаниеСверки.Индекс(СтрокаВидаНалога);
			МакетСтроки.Параметры.Заполнить(СтрокаСверки);
			МакетСтроки.Параметры.СтрокаДанных = Формат(ИндексСверки, "ЧН=0; ЧГ=0")
				+ ";" + Формат(НомерСтрокиСверки, "ЧН=0; ЧГ=0");
			
			АктСверки.Вывести(МакетСтроки, 1);
			
			МассивПодвидовНалога = Новый Массив;
			Если СтрокаСверки.НеЗачисленныеСредстваНалог <> 0 Тогда
				МассивПодвидовНалога.Добавить("НезачисленныеСредстваНалог");
			КонецЕсли;
			Если СтрокаСверки.НеЗачисленныеСредстваПени <> 0 Тогда
				МассивПодвидовНалога.Добавить("НеЗачисленныеСредстваПени");
			КонецЕсли;
			Если СтрокаСверки.НеЗачисленныеСредстваШтраф <> 0 Тогда
				МассивПодвидовНалога.Добавить("НеЗачисленныеСредстваШтраф");
			КонецЕсли;
			Если СтрокаСверки.НезачисленныеСредстваНеуплаченныеПроценты <> 0 Тогда
				МассивПодвидовНалога.Добавить("НезачисленныеСредстваНеуплаченныеПроценты");
			КонецЕсли;
			Если СтрокаСверки.НеЗачисленныеСредстваПроценты <> 0 Тогда
				МассивПодвидовНалога.Добавить("НеЗачисленныеСредстваПроценты");
			КонецЕсли;
			
			Если Не (МассивПодвидовНалога.Количество() = 1 И МассивПодвидовНалога[0] = "НезачисленныеСредстваНалог") Тогда
				
				Для Каждого ПодвидНалога Из МассивПодвидовНалога Цикл
					
					МакетСтроки = МакетАкта.ПолучитьОбласть(ПодвидНалога);
					МакетСтроки.Параметры.Заполнить(СтрокаСверки);
					МакетСтроки.Параметры.СтрокаДанных = "" + ИндексСверки + ";" + НомерСтрокиСверки;
					
					АктСверки.Вывести(МакетСтроки, 2,, Истина);
					
				КонецЦикла;
				
			КонецЕсли;
			
		КонецЦикла;
		АктСверки.ЗакончитьАвтогруппировкуСтрок();
		
	КонецЕсли;
	
	ДобавитьШтампПодписиВРезультатЗапросаИОН(ЗапросИОН, АктСверки, 3, Ложь);
	
	Возврат АктСверки;
	
КонецФункции

// Функция - формирует табличный документ с расшифровкой поля акта сверки.
//
// Параметры:
//  ДанныеАктаСверки	 - 	Структура - данные акта сверки с ФНС.
//  НомерСтрокиСверки	 - 	Число - строка, по которой нужно сформировать расшифровку.
// 
// Возвращаемое значение:
//  ТабличныйДокумент - табличный документ с расшифровкой строки акта сверки.
//
Функция СформироватьРасшифровку(ДанныеАктаСверки, НомерСтрокиСверки) Экспорт
	
	КонтекстЭДОСервер = ДокументооборотСКО.ПолучитьОбработкуЭДО();
	
	МакетРасшифровки      = КонтекстЭДОСервер.ПолучитьМакет("РасшифровкаАктаСверкиСФНС");
	СтрокаДанныхНалога    = ДанныеАктаСверки.Раздел2[НомерСтрокиСверки];
	РасшифровкаАктаСверки = Новый ТабличныйДокумент;
	
	// ШАПКА -------------------------------------------------
	Шапка = МакетРасшифровки.ПолучитьОбласть("Шапка");
	Шапка.Параметры.НаименованиеИФНС         = ДанныеАктаСверки.НаименованиеИФНС;
	Шапка.Параметры.НаименованиеОрганизации  = ДанныеАктаСверки.НаименованиеОрганизации;
	Шапка.Параметры.ИННОрганизации           = ДанныеАктаСверки.ИНН;
	Если ДанныеАктаСверки.ЭтоЮрЛицо Тогда
		Шапка.Параметры.ЗаголовокИНН         = НСтр("ru = 'ИНН / КПП:'");
		Шапка.Параметры.КППОрганизации       = " / " + ДанныеАктаСверки.КПП;
	Иначе
		Шапка.Параметры.ЗаголовокИНН         = НСтр("ru = 'ИНН:'");
	КонецЕсли;
	
	Шапка.Параметры.НаименованиеНалога       = СтрокаДанныхНалога.НаименованиеНалога;
	Шапка.Параметры.НаименованиеПолное       = СтрокаДанныхНалога.НаименованиеПолное;
	Шапка.Параметры.КБК                      = СтрокаДанныхНалога.КБК;
	Шапка.Параметры.ОКТМО                    = СтрокаДанныхНалога.ОКТМО;
	
	РасшифровкаАктаСверки.Вывести(Шапка);
	
	// САЛЬДО НА НАЧАЛО --------------------------------------
	
	ДанныеРаздела = ПодготовкаДанныхРасшифровки(МакетРасшифровки, "СальдоНаНачалоТело", СтрокаДанныхНалога);
	Если ДанныеРаздела.ЕстьДанные Тогда
		
		МакетДанных = МакетРасшифровки.ПолучитьОбласть("СальдоНаНачалоШапка");
		МакетДанных.Параметры.ДатаНачала = Формат(ДанныеАктаСверки.ДатаНачала, "ДЛФ=D");
		
		РасшифровкаАктаСверки.Вывести(МакетДанных);
		
		РасшифровкаАктаСверки.Вывести(ДанныеРаздела.ТабДокумент);
		
	КонецЕсли;
	
	// ОБОРОТЫ ЗА ПЕРИОД --------------------------------------
	
	ДанныеРаздела = ПодготовкаДанныхРасшифровки(МакетРасшифровки, "ОборотыЗаПериодТело", СтрокаДанныхНалога);
	Если ДанныеРаздела.ЕстьДанные Тогда
		
		МакетДанных = МакетРасшифровки.ПолучитьОбласть("ОборотыЗаПериодШапка");
		МакетДанных.Параметры.ПериодСверки = ПредставлениеПериода(
			НачалоДня(ДанныеАктаСверки.ДатаНачала), КонецДня(ДанныеАктаСверки.ДатаОкончания), "ФП = Истина");
		
		РасшифровкаАктаСверки.Вывести(МакетДанных);
		
		РасшифровкаАктаСверки.Вывести(ДанныеРаздела.ТабДокумент);
		
	КонецЕсли;
	
	// САЛЬДО НА КОНЕЦ ---------------------------------------
	
	ДанныеРаздела = ПодготовкаДанныхРасшифровки(МакетРасшифровки, "СальдоНаКонецТело", СтрокаДанныхНалога);
	Если ДанныеРаздела.ЕстьДанные Тогда
	
		МакетДанных = МакетРасшифровки.ПолучитьОбласть("СальдоНаКонецШапка");
		МакетДанных.Параметры.ДатаОкончания = Формат(ДанныеАктаСверки.ДатаОкончания, "ДЛФ=D");
		
		РасшифровкаАктаСверки.Вывести(МакетДанных);
	
		РасшифровкаАктаСверки.Вывести(ДанныеРаздела.ТабДокумент);
		
	КонецЕсли;
	
	// НЕЗАЧИСЛЕННЫЕ ПЛАТЕЖИ ---------------------------------
	
	ДанныеРаздела = ПодготовкаДанныхРасшифровки(МакетРасшифровки, "НезачисленныеСредстваТело", СтрокаДанныхНалога);
	Если ДанныеРаздела.ЕстьДанные Тогда
		
		МакетДанных = МакетРасшифровки.ПолучитьОбласть("НезачисленныеСредстваШапка");
		
		РасшифровкаАктаСверки.Вывести(МакетДанных);
		
		РасшифровкаАктаСверки.Вывести(ДанныеРаздела.ТабДокумент);
		
	КонецЕсли;
	
	Возврат РасшифровкаАктаСверки;
	
КонецФункции

Процедура РасчетДополнительныхПолей(СодержаниеСверки)
	
	ОписаниеКолонкиСТипомЧисло = ОбщегоНазначения.ОписаниеТипаЧисло(17, 2);
	
	СодержаниеСверки.Колонки.Добавить("НаименованиеПолное",          Новый ОписаниеТипов("Строка"));
	
	СодержаниеСверки.Колонки.Добавить("ЗадолженностьНаНачало",       ОписаниеКолонкиСТипомЧисло);
	СодержаниеСверки.Колонки.Добавить("ПереплатаНаНачало",           ОписаниеКолонкиСТипомЧисло);
	СодержаниеСверки.Колонки.Добавить("НачисленоЗаПериод",           ОписаниеКолонкиСТипомЧисло);
	СодержаниеСверки.Колонки.Добавить("УплаченоЗаПериод",            ОписаниеКолонкиСТипомЧисло);
	СодержаниеСверки.Колонки.Добавить("ЗадолженностьНаКонец",        ОписаниеКолонкиСТипомЧисло);
	СодержаниеСверки.Колонки.Добавить("ПереплатаНаКонец",            ОписаниеКолонкиСТипомЧисло);
	
	СодержаниеСверки.Колонки.Добавить("НалогЗадолженностьНаНачало",  ОписаниеКолонкиСТипомЧисло);
	СодержаниеСверки.Колонки.Добавить("НалогПереплатаНаНачало",      ОписаниеКолонкиСТипомЧисло);
	СодержаниеСверки.Колонки.Добавить("НалогНачисленоЗаПериод",      ОписаниеКолонкиСТипомЧисло);
	СодержаниеСверки.Колонки.Добавить("НалогУплаченоЗаПериод",       ОписаниеКолонкиСТипомЧисло);
	СодержаниеСверки.Колонки.Добавить("НалогЗадолженностьНаКонец",   ОписаниеКолонкиСТипомЧисло);
	СодержаниеСверки.Колонки.Добавить("НалогПереплатаНаКонец",       ОписаниеКолонкиСТипомЧисло);
	
	СодержаниеСверки.Колонки.Добавить("ПеняЗадолженностьНаНачало",   ОписаниеКолонкиСТипомЧисло);
	СодержаниеСверки.Колонки.Добавить("ПеняПереплатаНаНачало",       ОписаниеКолонкиСТипомЧисло);
	СодержаниеСверки.Колонки.Добавить("ПеняНачисленоЗаПериод",       ОписаниеКолонкиСТипомЧисло);
	СодержаниеСверки.Колонки.Добавить("ПеняУплаченоЗаПериод",        ОписаниеКолонкиСТипомЧисло);
	СодержаниеСверки.Колонки.Добавить("ПеняЗадолженностьНаКонец",    ОписаниеКолонкиСТипомЧисло);
	СодержаниеСверки.Колонки.Добавить("ПеняПереплатаНаКонец",        ОписаниеКолонкиСТипомЧисло);
	
	СодержаниеСверки.Колонки.Добавить("ШтрафЗадолженностьНаНачало",  ОписаниеКолонкиСТипомЧисло);
	СодержаниеСверки.Колонки.Добавить("ШтрафПереплатаНаНачало",      ОписаниеКолонкиСТипомЧисло);
	СодержаниеСверки.Колонки.Добавить("ШтрафНачисленоЗаПериод",      ОписаниеКолонкиСТипомЧисло);
	СодержаниеСверки.Колонки.Добавить("ШтрафУплаченоЗаПериод",       ОписаниеКолонкиСТипомЧисло);
	СодержаниеСверки.Колонки.Добавить("ШтрафЗадолженностьНаКонец",   ОписаниеКолонкиСТипомЧисло);
	СодержаниеСверки.Колонки.Добавить("ШтрафПереплатаНаКонец",       ОписаниеКолонкиСТипомЧисло);
	
	СодержаниеСверки.Колонки.Добавить("ПрочееЗадолженностьНаНачало", ОписаниеКолонкиСТипомЧисло);
	СодержаниеСверки.Колонки.Добавить("ПрочееПереплатаНаНачало",     ОписаниеКолонкиСТипомЧисло);
	СодержаниеСверки.Колонки.Добавить("ПрочееНачисленоЗаПериод",     ОписаниеКолонкиСТипомЧисло);
	СодержаниеСверки.Колонки.Добавить("ПрочееУплаченоЗаПериод",      ОписаниеКолонкиСТипомЧисло);
	СодержаниеСверки.Колонки.Добавить("ПрочееЗадолженностьНаКонец",  ОписаниеКолонкиСТипомЧисло);
	СодержаниеСверки.Колонки.Добавить("ПрочееПереплатаНаКонец",      ОписаниеКолонкиСТипомЧисло);
	
	// Для проверки наличия сальдо и оборотов по виду расчетов.
	СодержаниеСверки.Колонки.Добавить("ВидНалогаКонтроль",           ОписаниеКолонкиСТипомЧисло);
	СодержаниеСверки.Колонки.Добавить("НалогКонтроль",               ОписаниеКолонкиСТипомЧисло);
	СодержаниеСверки.Колонки.Добавить("ПеняКонтроль",                ОписаниеКолонкиСТипомЧисло);
	СодержаниеСверки.Колонки.Добавить("ШтрафКонтроль",               ОписаниеКолонкиСТипомЧисло);
	СодержаниеСверки.Колонки.Добавить("ПрочееКонтроль",              ОписаниеКолонкиСТипомЧисло);
	
	// Данные для расшифровки.
	СодержаниеСверки.Колонки.Добавить("РазделНачислено",                 ОписаниеКолонкиСТипомЧисло);
	СодержаниеСверки.Колонки.Добавить("РазделУменьшено",                 ОписаниеКолонкиСТипомЧисло);
	СодержаниеСверки.Колонки.Добавить("РазделПринятаЗадолженность",      ОписаниеКолонкиСТипомЧисло);
	СодержаниеСверки.Колонки.Добавить("РазделПереданаЗадолженность",     ОписаниеКолонкиСТипомЧисло);
	СодержаниеСверки.Колонки.Добавить("РазделСписаниеПоПостановлениюРФ", ОписаниеКолонкиСТипомЧисло);
	СодержаниеСверки.Колонки.Добавить("РазделВозвращеноИзБюджета",       ОписаниеКолонкиСТипомЧисло);
	
	// Денежные средства, списанные с расчетных счетов налогоплательщика,
	// но не зачисленные на счета по учету доходов бюджетов на конец сверяемого периода.
	СодержаниеСверки.Колонки.Добавить("НеЗачисленныеСредства",           ОписаниеКолонкиСТипомЧисло);
	СодержаниеСверки.Колонки.Добавить("НеЗачисленныеСредстваКонтроль",   ОписаниеКолонкиСТипомЧисло);
	
	СодержаниеСверки.Индексы.Добавить("КБК");
	
	НалогиПоКБК = Обработки.ДокументооборотСКонтролирующимиОрганами.ПолучитьМакет("КБК");
	КолонкаКБК = НалогиПоКБК.Область("КБК"); // область таблицы с КБК
	
	Для Каждого СтрокаСверки Из СодержаниеСверки Цикл
		
		ПолучитьНаименованиеНалога(СтрокаСверки, НалогиПоКБК, КолонкаКБК);
		
		// сальдо на начало
		СтрокаСверки.ЗадолженностьНаНачало = СтрокаСверки.НачальныйОстатокНалог
			+ СтрокаСверки.НачальныйОстатокПени
			+ СтрокаСверки.НачальныйОстатокШтраф
			+ СтрокаСверки.НачальныйОстатокНеуплаченныеПроцентыВсего
			+ СтрокаСверки.НачальныйОстатокПроценты;
			
		СтрокаСверки.ПереплатаНаНачало = СтрокаСверки.НачальныйОстатокНалогПоложительноеСальдо
			+ СтрокаСверки.НачальныйОстатокПениПоложительноеСальдо
			+ СтрокаСверки.НачальныйОстатокШтрафПоложительноеСальдо
			+ СтрокаСверки.НачальныйОстатокПроцентыПоложительноеСальдо;
			
		// сальдо на начало, детализация по налогам, пени, штрафам и прочему
		СтрокаСверки.НалогЗадолженностьНаНачало  = СтрокаСверки.НачальныйОстатокНалог;
		СтрокаСверки.ПеняЗадолженностьНаНачало   = СтрокаСверки.НачальныйОстатокПени;
		СтрокаСверки.ШтрафЗадолженностьНаНачало  = СтрокаСверки.НачальныйОстатокШтраф;
		СтрокаСверки.ПрочееЗадолженностьНаНачало = СтрокаСверки.НачальныйОстатокНеуплаченныеПроцентыВсего
			+ СтрокаСверки.НачальныйОстатокПроценты;
			
		СтрокаСверки.НалогПереплатаНаНачало  = СтрокаСверки.НачальныйОстатокНалогПоложительноеСальдо;
		СтрокаСверки.ПеняПереплатаНаНачало   = СтрокаСверки.НачальныйОстатокПениПоложительноеСальдо;
		СтрокаСверки.ШтрафПереплатаНаНачало  = СтрокаСверки.НачальныйОстатокШтрафПоложительноеСальдо;
		СтрокаСверки.ПрочееПереплатаНаНачало = СтрокаСверки.НачальныйОстатокПроцентыПоложительноеСальдо;
			
		// обороты за период
		СтрокаСверки.НачисленоЗаПериод = СтрокаСверки.НачисленоНалог
			+ СтрокаСверки.НачисленоПени
			+ СтрокаСверки.НачисленоШтраф
			+ СтрокаСверки.НачисленоНеуплаченныеПроценты
			+ СтрокаСверки.НачисленоПроценты
			- СтрокаСверки.УменьшеноНалог
			- СтрокаСверки.УменьшеноПени
			- СтрокаСверки.УменьшеноШтраф
			- СтрокаСверки.СписаноПени
			- СтрокаСверки.СписаноШтраф
			- СтрокаСверки.СписаноПравительствомНалог
			- СтрокаСверки.СписаноПравительствомПени
			- СтрокаСверки.СписаноПравительствомШтраф
			- СтрокаСверки.СписаноПравительствомНеуплаченныеПроценты
			- СтрокаСверки.ВозвращеноНалог
			- СтрокаСверки.ВозвращеноПени
			- СтрокаСверки.ВозвращеноШтраф
			- СтрокаСверки.ВозвращеноПроценты
			- СтрокаСверки.ПринятоеСальдоНалог
			- СтрокаСверки.ПринятоеСальдоПени
			- СтрокаСверки.ПринятоеСальдоШтраф
			- СтрокаСверки.ПринятоеСальдоНеуплаченныеПроценты
			- СтрокаСверки.ПринятоеСальдоПроценты
			- СтрокаСверки.ПереданоНалог
			- СтрокаСверки.ПереданоПени
			- СтрокаСверки.ПереданоШтраф
			- СтрокаСверки.ПереданоНеуплаченныеПроценты
			- СтрокаСверки.ПереданоПроценты;
			
		СтрокаСверки.УплаченоЗаПериод = СтрокаСверки.УплаченоНалогВсего
			+ СтрокаСверки.УплаченоПениВсего
			+ СтрокаСверки.УплаченоШтрафВсего
			+ СтрокаСверки.УплаченоПроцентыВсего;
			
		// обороты за период, детализация по налогам, пени, штрафам и прочему
		СтрокаСверки.НалогНачисленоЗаПериод = СтрокаСверки.НачисленоНалог
			- СтрокаСверки.УменьшеноНалог
			- СтрокаСверки.СписаноПравительствомНалог
			- СтрокаСверки.ВозвращеноНалог
			- СтрокаСверки.ПринятоеСальдоНалог
			- СтрокаСверки.ПереданоНалог;
			
		СтрокаСверки.ПеняНачисленоЗаПериод = СтрокаСверки.НачисленоПени
			- СтрокаСверки.УменьшеноПени
			- СтрокаСверки.СписаноПени
			- СтрокаСверки.СписаноПравительствомПени
			- СтрокаСверки.ВозвращеноПени
			- СтрокаСверки.ПринятоеСальдоПени
			- СтрокаСверки.ПереданоПени;
			
		СтрокаСверки.ШтрафНачисленоЗаПериод = СтрокаСверки.НачисленоШтраф
			- СтрокаСверки.УменьшеноШтраф
			- СтрокаСверки.СписаноШтраф
			- СтрокаСверки.СписаноПравительствомШтраф
			- СтрокаСверки.ВозвращеноШтраф
			- СтрокаСверки.ПринятоеСальдоШтраф
			- СтрокаСверки.ПереданоШтраф;
			
		СтрокаСверки.ПрочееНачисленоЗаПериод = СтрокаСверки.НачисленоНеуплаченныеПроценты
			+ СтрокаСверки.НачисленоПроценты
			- СтрокаСверки.СписаноПравительствомНеуплаченныеПроценты
			- СтрокаСверки.ВозвращеноПроценты
			- СтрокаСверки.ПринятоеСальдоНеуплаченныеПроценты
			- СтрокаСверки.ПринятоеСальдоПроценты
			- СтрокаСверки.ПереданоНалог
			- СтрокаСверки.ПереданоНеуплаченныеПроценты
			- СтрокаСверки.ПереданоПроценты;
			
		СтрокаСверки.НалогУплаченоЗаПериод  = СтрокаСверки.УплаченоНалогВсего;
		СтрокаСверки.ПеняУплаченоЗаПериод   = СтрокаСверки.УплаченоПениВсего;
		СтрокаСверки.ШтрафУплаченоЗаПериод  = СтрокаСверки.УплаченоШтрафВсего;
		СтрокаСверки.ПрочееУплаченоЗаПериод = СтрокаСверки.УплаченоПроцентыВсего;
			
		// обороты разделов для расшифровки
		СтрокаСверки.РазделНачислено = СтрокаСверки.НачисленоНалог
			+ СтрокаСверки.НачисленоПени
			+ СтрокаСверки.НачисленоШтраф
			+ СтрокаСверки.НачисленоНеуплаченныеПроценты
			+ СтрокаСверки.НачисленоПроценты;
			
		СтрокаСверки.РазделУменьшено = СтрокаСверки.УменьшеноНалог
			+ СтрокаСверки.УменьшеноПени
			+ СтрокаСверки.УменьшеноШтраф;
			
		СтрокаСверки.РазделПринятаЗадолженность = СтрокаСверки.ПринятоеСальдоНалог
			+ СтрокаСверки.ПринятоеСальдоПени
			+ СтрокаСверки.ПринятоеСальдоШтраф
			+ СтрокаСверки.ПринятоеСальдоНеуплаченныеПроценты
			+ СтрокаСверки.ПринятоеСальдоПроценты;
			
		СтрокаСверки.РазделПереданаЗадолженность = СтрокаСверки.ПереданоНалог
			+ СтрокаСверки.ПереданоПени
			+ СтрокаСверки.ПереданоШтраф
			+ СтрокаСверки.ПереданоНеуплаченныеПроценты
			+ СтрокаСверки.ПереданоПроценты;
			
		СтрокаСверки.РазделСписаниеПоПостановлениюРФ = СтрокаСверки.СписаноПравительствомНалог
			+ СтрокаСверки.СписаноПравительствомПени
			+ СтрокаСверки.СписаноПравительствомШтраф
			+ СтрокаСверки.СписаноПравительствомНеуплаченныеПроценты;
			
		СтрокаСверки.РазделВозвращеноИзБюджета = СтрокаСверки.ВозвращеноНалог
			+ СтрокаСверки.ВозвращеноПени
			+ СтрокаСверки.ВозвращеноШтраф
			+ СтрокаСверки.ВозвращеноПроценты
			+ СтрокаСверки.ВозвращеноНеуплаченныеПроценты;
			
		// Сальдо на конец
		СтрокаСверки.ЗадолженностьНаКонец = СтрокаСверки.КонечныйОстатокНалог
			+ СтрокаСверки.КонечныйОстатокПени
			+ СтрокаСверки.КонечныйОстатокШтраф
			+ СтрокаСверки.КонечныйОстатокНеуплаченныеПроцентыВсего
			+ СтрокаСверки.КонечныйОстатокПроценты;
			
		СтрокаСверки.ПереплатаНаКонец = СтрокаСверки.КонечныйОстатокНалогПоложительноеСальдо
			+ СтрокаСверки.КонечныйОстатокПениПоложительноеСальдо
			+ СтрокаСверки.КонечныйОстатокШтрафПоложительноеСальдо
			+ СтрокаСверки.КонечныйОстатокПроцентыПоложительноеСальдо;
			
		// сальдо на конец, детализация по налогам, пени, штрафам и прочему
		СтрокаСверки.НалогЗадолженностьНаКонец  = СтрокаСверки.КонечныйОстатокНалог;
		СтрокаСверки.ПеняЗадолженностьНаКонец   = СтрокаСверки.КонечныйОстатокПени;
		СтрокаСверки.ШтрафЗадолженностьНаКонец  = СтрокаСверки.КонечныйОстатокШтраф;
		СтрокаСверки.ПрочееЗадолженностьНаКонец = СтрокаСверки.КонечныйОстатокНеуплаченныеПроцентыВсего
			+ СтрокаСверки.КонечныйОстатокПроценты;
		
		СтрокаСверки.НалогПереплатаНаКонец      = СтрокаСверки.КонечныйОстатокНалогПоложительноеСальдо;
		СтрокаСверки.ПеняПереплатаНаКонец       = СтрокаСверки.КонечныйОстатокПениПоложительноеСальдо;
		СтрокаСверки.ШтрафПереплатаНаКонец      = СтрокаСверки.КонечныйОстатокШтрафПоложительноеСальдо;
		СтрокаСверки.ПрочееПереплатаНаКонец     = СтрокаСверки.КонечныйОстатокПроцентыПоложительноеСальдо;
			
		// контроль наличия сальдо и оборотов
		СтрокаСверки.ВидНалогаКонтроль = ?(СтрокаСверки.ЗадолженностьНаНачало = 0, 0, 1)
			+ ?(СтрокаСверки.ПереплатаНаНачало         = 0, 0, 1)
			+ ?(СтрокаСверки.НачисленоЗаПериод         = 0, 0, 1)
			+ ?(СтрокаСверки.УплаченоЗаПериод          = 0, 0, 1)
			+ ?(СтрокаСверки.ЗадолженностьНаКонец      = 0, 0, 1)
			+ ?(СтрокаСверки.ПереплатаНаКонец          = 0, 0, 1);
		
		СтрокаСверки.НалогКонтроль = ?(СтрокаСверки.НалогЗадолженностьНаНачало = 0, 0, 1)
			+ ?(СтрокаСверки.НалогПереплатаНаНачало    = 0, 0, 1)
			+ ?(СтрокаСверки.НалогНачисленоЗаПериод    = 0, 0, 1)
			+ ?(СтрокаСверки.НалогУплаченоЗаПериод     = 0, 0, 1)
			+ ?(СтрокаСверки.НалогЗадолженностьНаКонец = 0, 0, 1)
			+ ?(СтрокаСверки.НалогПереплатаНаКонец     = 0, 0, 1);
			
		СтрокаСверки.ПеняКонтроль = ?(СтрокаСверки.ПеняЗадолженностьНаНачало = 0, 0, 1)
			+ ?(СтрокаСверки.ПеняПереплатаНаНачало     = 0, 0, 1)
			+ ?(СтрокаСверки.ПеняНачисленоЗаПериод     = 0, 0, 1)
			+ ?(СтрокаСверки.ПеняУплаченоЗаПериод      = 0, 0, 1)
			+ ?(СтрокаСверки.ПеняЗадолженностьНаКонец  = 0, 0, 1)
			+ ?(СтрокаСверки.ПеняПереплатаНаКонец      = 0, 0, 1);
			
		СтрокаСверки.ШтрафКонтроль = ?(СтрокаСверки.ШтрафЗадолженностьНаНачало = 0, 0, 1)
			+ ?(СтрокаСверки.ШтрафПереплатаНаНачало    = 0, 0, 1)
			+ ?(СтрокаСверки.ШтрафНачисленоЗаПериод    = 0, 0, 1)
			+ ?(СтрокаСверки.ШтрафУплаченоЗаПериод     = 0, 0, 1)
			+ ?(СтрокаСверки.ШтрафЗадолженностьНаКонец = 0, 0, 1)
			+ ?(СтрокаСверки.ШтрафПереплатаНаКонец     = 0, 0, 1);
			
		СтрокаСверки.ПрочееКонтроль = ?(СтрокаСверки.ПрочееЗадолженностьНаНачало = 0, 0, 1)
			+ ?(СтрокаСверки.ПрочееПереплатаНаНачало    = 0, 0, 1)
			+ ?(СтрокаСверки.ПрочееНачисленоЗаПериод    = 0, 0, 1)
			+ ?(СтрокаСверки.ПрочееУплаченоЗаПериод     = 0, 0, 1)
			+ ?(СтрокаСверки.ПрочееЗадолженностьНаКонец = 0, 0, 1)
			+ ?(СтрокаСверки.ПрочееПереплатаНаКонец     = 0, 0, 1);
		
		// Денежные средства, списанные с расчетных счетов налогоплательщика,
		// но не зачисленные на счета по учету доходов бюджетов на конец сверяемого периода.
		СтрокаСверки.НеЗачисленныеСредства = СтрокаСверки.НеЗачисленныеСредстваНалог
			+ СтрокаСверки.НеЗачисленныеСредстваПени
			+ СтрокаСверки.НеЗачисленныеСредстваШтраф
			+ СтрокаСверки.НеЗачисленныеСредстваНеуплаченныеПроценты
			+ СтрокаСверки.НеЗачисленныеСредстваПроценты;
			
		СтрокаСверки.НеЗачисленныеСредстваКонтроль =?(СтрокаСверки.НеЗачисленныеСредстваНалог = 0, 0, 1)
			+ ?(СтрокаСверки.НеЗачисленныеСредстваПени    = 0, 0, 1)
			+ ?(СтрокаСверки.НеЗачисленныеСредстваШтраф    = 0, 0, 1)
			+ ?(СтрокаСверки.НеЗачисленныеСредстваНеуплаченныеПроценты     = 0, 0, 1)
			+ ?(СтрокаСверки.НеЗачисленныеСредстваПроценты = 0, 0, 1);
			
	КонецЦикла;

КонецПроцедуры

Процедура ПолучитьНаименованиеНалога(СтрокаСверки, НалогиПоКБК, КолонкаКБК, НайденнаяСтрока = Неопределено)
	
	КБК = СокрЛП(СтрокаСверки.КБК);
	
	// Ищем по первым 13 значениям КБК, остальные цифры это подвиды доходов бюджета (штрафы, пени и т.д).
	Если НайденнаяСтрока = Неопределено Тогда
		НайденнаяСтрока = НалогиПоКБК.НайтиТекст(Лев(КБК, 13),, КолонкаКБК,,,,); 
	Иначе
		НайденнаяСтрока = НалогиПоКБК.НайтиТекст(Лев(КБК, 13), НайденнаяСтрока, КолонкаКБК,,,,); 
	КонецЕсли;
	
	Если НайденнаяСтрока <> Неопределено Тогда
		Если ЭтоСтраховыеВзносы(КБК) Тогда
			ПодвидДоходов = Сред(КБК, 16, 2);
			НайденноеКБК = СокрЛП(НалогиПоКБК.Область(НайденнаяСтрока.Верх, НалогиПоКБК.Область("КБК").Лево).Текст);
			ПодвидДоходовНайденногоКБК = Сред(НайденноеКБК, 16, 2);
			Если ПодвидДоходов <> ПодвидДоходовНайденногоКБК Тогда
				// Если подвид дохода не совпал, то рекурсивно начинаем поиск на следующей строке.
				ПолучитьНаименованиеНалога(СтрокаСверки, НалогиПоКБК, КолонкаКБК, НайденнаяСтрока);
				Возврат;
			КонецЕсли;
		КонецЕсли;
		НаименованиеКраткое = СокрЛП(НалогиПоКБК.Область(НайденнаяСтрока.Верх, НалогиПоКБК.Область("КраткоеНаименованиеНалога").Лево).Текст);
		НаименованиеПолное  = СокрЛП(НалогиПоКБК.Область(НайденнаяСтрока.Верх, НалогиПоКБК.Область("Наименование_КБК").Лево).Текст);
		СтрокаСверки.НаименованиеНалога = ?(ЗначениеЗаполнено(НаименованиеКраткое), НаименованиеКраткое, НаименованиеПолное);
		СтрокаСверки.НаименованиеПолное = НаименованиеПолное;
	Иначе
		СтрокаСверки.НаименованиеНалога = "КБК: " + КБК; // если не найден КБК в макете, выводим КБК
	КонецЕсли;

КонецПроцедуры

Функция ЭтоСтраховыеВзносы(КБК)
	
	Возврат Прав(КБК, 3) = "160"; // код 160 – страховые взносы на обязательное социальное страхование 
	
КонецФункции

Функция ПодготовкаДанныхРасшифровки(МакетРасшифровки, ИмяОбласти, СтрокаДанныхНалога)

	ТабДокумент = Новый ТабличныйДокумент;
	
	ТабДокумент.НачатьАвтогруппировкуСтрок();
	
	МакетДанных = МакетРасшифровки.ПолучитьОбласть(ИмяОбласти);
	МакетДанных.Параметры.Заполнить(СтрокаДанныхНалога);
	
	ЕстьДанные = Ложь;
	ВывестиУровень3 = Истина;
	Для НомерСтроки = 1 По МакетДанных.ВысотаТаблицы Цикл
		
		УровеньГруппировки = Число(МакетДанных.Область(НомерСтроки, 5).Текст);
		Если УровеньГруппировки = 3 И Не ВывестиУровень3 Тогда
			
			Продолжить; // не выводим 3 уровень группировки, в расшифровке дохода бюджета только 1 строка
			
		Иначе
			ВывестиУровень3 = Истина;
		КонецЕсли;
		
		// Проверка условий вывода 3 уровня
		НаименованиеПараметра = МакетДанных.Область(НомерСтроки, 6).Текст;
		Если (НаименованиеПараметра = "НалогЗадолженностьНаНачало"
			И СтрокаДанныхНалога.НалогЗадолженностьНаНачало = СтрокаДанныхНалога.НачальныйОстатокНалогОтрицательноеСальдо)
			
			ИЛИ (НаименованиеПараметра = "ПеняЗадолженностьНаНачало"
				И СтрокаДанныхНалога.ПеняЗадолженностьНаНачало = СтрокаДанныхНалога.НачальныйОстатокПениОтрицательноеСальдо)
				
			ИЛИ (НаименованиеПараметра = "ШтрафЗадолженностьНаНачало"
				И СтрокаДанныхНалога.ШтрафЗадолженностьНаНачало = СтрокаДанныхНалога.НачальныйОстатокШтрафОтрицательноеСальдо)
			
			ИЛИ (НаименованиеПараметра = "УплаченоНалогВсего"
				И СтрокаДанныхНалога.УплаченоНалогВсего = СтрокаДанныхНалога.УплаченоНалог)
				
			ИЛИ (НаименованиеПараметра = "УплаченоПениВсего"
				И СтрокаДанныхНалога.УплаченоПениВсего = СтрокаДанныхНалога.УплаченоПени)
				
			ИЛИ (НаименованиеПараметра = "УплаченоШтрафВсего"
				И СтрокаДанныхНалога.УплаченоШтрафВсего = СтрокаДанныхНалога.УплаченоШтраф)
				
			ИЛИ (НаименованиеПараметра = "НалогЗадолженностьНаКонец"
				И СтрокаДанныхНалога.НалогЗадолженностьНаКонец = СтрокаДанныхНалога.КонечныйОстатокНалогОтрицательноеСальдо)
				
			ИЛИ (НаименованиеПараметра = "ПеняЗадолженностьНаКонец"
				И СтрокаДанныхНалога.ПеняЗадолженностьНаКонец = СтрокаДанныхНалога.КонечныйОстатокПениОтрицательноеСальдо)
				
			ИЛИ (НаименованиеПараметра = "ШтрафЗадолженностьНаКонец"
				И СтрокаДанныхНалога.ШтрафЗадолженностьНаКонец = СтрокаДанныхНалога.КонечныйОстатокШтрафОтрицательноеСальдо)
				
			Тогда
			
			ВывестиУровень3 = Ложь;
			
		КонецЕсли;
		
		МакетСтрокиДанных = МакетДанных.ПолучитьОбласть(НомерСтроки, 1, НомерСтроки, 4);
		Для Каждого ПараметрМакета Из МакетСтрокиДанных.Параметры Цикл
			
			Если ЗначениеЗаполнено(ПараметрМакета) Тогда
				
				ТабДокумент.Вывести(МакетСтрокиДанных, УровеньГруппировки,, Истина);
				ЕстьДанные = Истина;
				
			КонецЕсли;
			
		КонецЦикла;
		
	КонецЦикла;
	
	ТабДокумент.ЗакончитьАвтогруппировкуСтрок();
	
	Возврат Новый Структура("ТабДокумент, ЕстьДанные", ТабДокумент, ЕстьДанные);
	
КонецФункции

////////////////////////////////////////////////////////////////////////
// РАЗБОР ФАЙЛА IU_AKTSVP_*
// ОТВЕТ НА ЗАПРОС "АКТ СВЕРКИ РАСЧЕТОВ ПО НАЛОГАМ, СБОРАМ, ПЕНЯМ, ШТРАФАМ, ПРОЦЕНТАМ" ПРИ ИНФОРМАЦИОННОМ ОБСЛУЖИВАНИИ И ИНФОРМИРОВАНИИ
// НАЛОГОПЛАТЕЛЬЩИКОВ В ЭЛЕКТРОННОЙ ФОРМЕ ПО ТЕЛЕКОММУНИКАЦИОННЫМ КАНАЛАМ СВЯЗИ

Функция XDTOПолучитьАтрибут(ОбъектXDTO, ИмяАтрибута, ЗначениеПоУмолчанию="")
	ЗначениеАтрибута = ОбъектXDTO.Свойства().Получить(ИмяАтрибута); 
	Если ЗначениеАтрибута = Неопределено Тогда
		Возврат ЗначениеПоУмолчанию;
	КонецЕсли;
	Возврат ОбъектXDTO.Получить(ИмяАтрибута);
КонецФункции

Процедура ЗаполнитьАктСверки(АктСверXDTO, АктыСверок)
	
	АктСверки = АктыСверок.Добавить();
	
	Если АктСверXDTO.СвНП.Свойства().Получить("НПЮЛ") <> Неопределено Тогда
		СведенияОЮридическомЛице = АктСверXDTO.СвНП.НПЮЛ;
		АктСверки.ИНН = СведенияОЮридическомЛице.ИННЮЛ;
		АктСверки.КПП = СведенияОЮридическомЛице.КПП;
		АктСверки.НаименованиеОрганизации = СведенияОЮридическомЛице.НаимОрг;
		АктСверки.ЭтоЮрЛицо = Истина;
	Иначе
		СведенияОФизическомЛице = АктСверXDTO.СвНП.НПФЛ;
		АктСверки.ИНН = XDTOПолучитьАтрибут(СведенияОФизическомЛице, "ИННФЛ");
		ФИОПредпринимателя = СведенияОФизическомЛице.ФИО;
		АктСверки.НаименованиеОрганизации = "ИП " + СокрЛП(ФИОПредпринимателя.Фамилия)
			+ " " + Лев(СокрЛП(ФИОПредпринимателя.Имя), 1) + ".";
		Если ФИОПредпринимателя.Свойства().Получить("Отчество") <> Неопределено Тогда
			
			АктСверки.НаименованиеОрганизации = АктСверки.НаименованиеОрганизации + " "
				+ Лев(СокрЛП(ФИОПредпринимателя.Отчество), 1) + ".";
			
		КонецЕсли;
		АктСверки.ЭтоЮрЛицо = Ложь;
	КонецЕсли;
	
	АктСверки.НомерАкта     = АктСверXDTO.НомАкт;
	АктСверки.ДатаАкта      = ДатаФНС(АктСверXDTO.ДатаСост);
	АктСверки.ДатаНачала    = ДатаФНС(АктСверXDTO.ДатаНачПер);
	АктСверки.ДатаОкончания = ДатаФНС(АктСверXDTO.ДатаКонПер);
	АктСверки.КодИФНС       = АктСверXDTO.КодНО;
	АктСверки.Раздел1       = НовоеСодержаниеРаздела1Акта();
	АктСверки.Раздел2       = НовоеСодержаниеРаздела2Акта();
	
	Если АктСверXDTO.СодАкт.Свойства().Получить("Раздел1") <> Неопределено Тогда
		
		Если ТипЗнч(АктСверXDTO.СодАкт.Раздел1) = Тип("СписокXDTO") Тогда
			
			Для Каждого Раздел1XDTO Из АктСверXDTO.СодАкт.Раздел1 Цикл
				
				ЗаполнитьСодержаниеРаздела1АктаСверки(Раздел1XDTO, АктСверки.Раздел1);
				
			КонецЦикла;
			
		Иначе
			
			ЗаполнитьСодержаниеРаздела1АктаСверки(АктСверXDTO.СодАкт.Раздел1, АктСверки.Раздел1);
			
		КонецЕсли;
		
	КонецЕсли;
	
	Если АктСверXDTO.СодАкт.Свойства().Получить("Раздел2") <> Неопределено Тогда
		
		Если ТипЗнч(АктСверXDTO.СодАкт.Раздел2) = Тип("СписокXDTO") Тогда
			
			Для Каждого Раздел2XDTO Из АктСверXDTO.СодАкт.Раздел2 Цикл
				
				ЗаполнитьСодержаниеРаздела2АктаСверки(Раздел2XDTO, АктСверки.Раздел2);
				
			КонецЦикла;
			
		Иначе
			
			ЗаполнитьСодержаниеРаздела2АктаСверки(АктСверXDTO.СодАкт.Раздел2, АктСверки.Раздел2);
			
		КонецЕсли;
		
	КонецЕсли;
	
КонецПроцедуры

Процедура ЗаполнитьСодержаниеРаздела1АктаСверки(СодАктXDTO, СодержаниеАктаСверки)
	
	Содержание = СодержаниеАктаСверки.Добавить();
	Содержание.КБК = СодАктXDTO.КБК;
	Содержание.НаименованиеНалога = СодАктXDTO.ВидНал;
	Содержание.КПП                = СодАктXDTO.КПП;
	// В содержании акта может быть указано либо ОКТМО, либо ОКАТО.
	// Заполним одно из этих полей.
	Если СодАктXDTO.Свойства().Получить("ОКТМО") <> Неопределено Тогда
		Содержание.ОКТМО = СодАктXDTO.ОКТМО;
	Иначе
		Содержание.ОКАТО = СодАктXDTO.ОКАТО;
	КонецЕсли;
	
	// Сальдо расчетов
	СальдоРасчетовXDTO = СодАктXDTO.СальдоРасч;
	Содержание.НеЗачисленныеСредства    = ЗначениеЧисловогоПоля(СальдоРасчетовXDTO, "ДенСрСп");
	Содержание.НевыясненныеПлатежи      = ЗначениеЧисловогоПоля(СальдоРасчетовXDTO, "ПлатНвсн");
	
	Содержание.НалогЗадолженностьВсего  = ЗначениеЧисловогоПоля(СальдоРасчетовXDTO.Налог, "ДолгВсего");
	Содержание.НалогОтрицательноеСальдо = ЗначениеЧисловогоПоля(СальдоРасчетовXDTO.Налог, "СальдоОтр");
	Содержание.НалогОтсроченный         = ЗначениеЧисловогоПоля(СальдоРасчетовXDTO.Налог, "ОтсрПлат");
	Содержание.НалогПриостановленный    = ЗначениеЧисловогоПоля(СальдоРасчетовXDTO.Налог, "ПриостПлат");
	Содержание.НалогРеструктурированный = ЗначениеЧисловогоПоля(СальдоРасчетовXDTO.Налог, "РеструкДолг");
	Содержание.НалогПоложительноеСальдо = ЗначениеЧисловогоПоля(СальдоРасчетовXDTO.Налог, "СальдоПол");
	
	Содержание.ПениЗадолженностьВсего   = ЗначениеЧисловогоПоля(СальдоРасчетовXDTO.Пени, "ДолгВсего");
	Содержание.ПениОтрицательноеСальдо  = ЗначениеЧисловогоПоля(СальдоРасчетовXDTO.Пени, "СальдоОтр");
	Содержание.ПениОтсроченный          = ЗначениеЧисловогоПоля(СальдоРасчетовXDTO.Пени, "ОтсрПлат");
	Содержание.ПениПриостановленный     = ЗначениеЧисловогоПоля(СальдоРасчетовXDTO.Пени, "ПриостПлат");
	Содержание.ПениРеструктурированный  = ЗначениеЧисловогоПоля(СальдоРасчетовXDTO.Пени, "РеструкДолг");
	Содержание.ПениПоложительноеСальдо  = ЗначениеЧисловогоПоля(СальдоРасчетовXDTO.Пени, "СальдоПол");
	
	Содержание.ШтрафЗадолженностьВсего  = ЗначениеЧисловогоПоля(СальдоРасчетовXDTO.Штрафы, "ДолгВсего");
	Содержание.ШтрафОтрицательноеСальдо = ЗначениеЧисловогоПоля(СальдоРасчетовXDTO.Штрафы, "СальдоОтр");
	Содержание.ШтрафОтсроченный         = ЗначениеЧисловогоПоля(СальдоРасчетовXDTO.Штрафы, "ОтсрПлат");
	Содержание.ШтрафПриостановленный    = ЗначениеЧисловогоПоля(СальдоРасчетовXDTO.Штрафы, "ПриостПлат");
	Содержание.ШтрафРеструктурированный = ЗначениеЧисловогоПоля(СальдоРасчетовXDTO.Штрафы, "РеструкДолг");
	Содержание.ШтрафПоложительноеСальдо = ЗначениеЧисловогоПоля(СальдоРасчетовXDTO.Штрафы, "СальдоПол");
	
	Содержание.ОстатокНеуплаченныеПроцентыДосрочные = ЗначениеЧисловогоПоля(СальдоРасчетовXDTO.ПроцПолБС, "НеупСумПр");
	Содержание.Проценты                             = ЗначениеЧисловогоПоля(СальдоРасчетовXDTO.ПроцПолБС, "ДолгВсего");
	Содержание.ОстатокНеуплаченныеПроценты          = ЗначениеЧисловогоПоля(СальдоРасчетовXDTO.ПроцПолБС, "НеупСум");
	Содержание.ПроцентыПриостановленный             = ЗначениеЧисловогоПоля(СальдоРасчетовXDTO.ПроцПолБС, "ПриостПлат");
	Содержание.ПроцентыПоложительноеСальдо          = ЗначениеЧисловогоПоля(СальдоРасчетовXDTO.ПроцПолБС, "СальдоПол");
	Содержание.ПроцентыПриБанкротстве               = ЗначениеЧисловогоПоля(СальдоРасчетовXDTO.ПроцПолБС, "Проц");
	
КонецПроцедуры

Процедура ЗаполнитьСодержаниеРаздела2АктаСверки(СодАктXDTO, СодержаниеАктаСверки)
	
	Содержание = СодержаниеАктаСверки.Добавить();
	Содержание.КБК = СодАктXDTO.КБК;
	Содержание.НаименованиеНалога = СодАктXDTO.ВидНал;
	Содержание.КПП                = СодАктXDTO.КПП;
	// В содержании акта может быть указано либо ОКТМО, либо ОКАТО.
	// Заполним одно из этих полей.
	Если СодАктXDTO.Свойства().Получить("ОКТМО") <> Неопределено Тогда
		Содержание.ОКТМО = СодАктXDTO.ОКТМО;
	Иначе
		Содержание.ОКАТО = СодАктXDTO.ОКАТО;
	КонецЕсли;
	
	// Сальдо на начало сверяемого периода
	СальдоНачXDTO = СодАктXDTO.СальдоНач;
	Содержание.НачальныйОстатокЗадолженность            = СуммаПоДаннымНалоговогоОргана(СальдоНачXDTO.ЗадолжВсего);
	
	Содержание.НачальныйОстатокНалог                    = СуммаПоДаннымНалоговогоОргана(СальдоНачXDTO.Налог.НалВсего);
	Содержание.НачальныйОстатокНалогОтрицательноеСальдо = СуммаПоДаннымНалоговогоОргана(СальдоНачXDTO.Налог.СальдоОтр);
	Содержание.НачальныйОстатокНалогОтсроченный         = СуммаПоДаннымНалоговогоОргана(СальдоНачXDTO.Налог.ОтсрПлат);
	Содержание.НачальныйОстатокНалогПриостановленный    = СуммаПоДаннымНалоговогоОргана(СальдоНачXDTO.Налог.ПриостПлат);
	Содержание.НачальныйОстатокНалогРеструктурированный = СуммаПоДаннымНалоговогоОргана(СальдоНачXDTO.Налог.РеструкДолг);
	
	Содержание.НачальныйОстатокПени                     = СуммаПоДаннымНалоговогоОргана(СальдоНачXDTO.Пени.ПениВсего);
	Содержание.НачальныйОстатокПениОтрицательноеСальдо  = СуммаПоДаннымНалоговогоОргана(СальдоНачXDTO.Пени.СальдоОтр);
	Содержание.НачальныйОстатокПениОтсроченный          = СуммаПоДаннымНалоговогоОргана(СальдоНачXDTO.Пени.ОтсрПлат);
	Содержание.НачальныйОстатокПениПриостановленный     = СуммаПоДаннымНалоговогоОргана(СальдоНачXDTO.Пени.ПриостПлат);
	Содержание.НачальныйОстатокПениРеструктурированный  = СуммаПоДаннымНалоговогоОргана(СальдоНачXDTO.Пени.РеструкДолг);
	
	Содержание.НачальныйОстатокШтраф                    = СуммаПоДаннымНалоговогоОргана(СальдоНачXDTO.Штраф.ШтрафВсего);
	Содержание.НачальныйОстатокШтрафОтрицательноеСальдо = СуммаПоДаннымНалоговогоОргана(СальдоНачXDTO.Штраф.СальдоОтр);
	Содержание.НачальныйОстатокШтрафОтсроченный         = СуммаПоДаннымНалоговогоОргана(СальдоНачXDTO.Штраф.ОтсрПлат);
	Содержание.НачальныйОстатокШтрафПриостановленный    = СуммаПоДаннымНалоговогоОргана(СальдоНачXDTO.Штраф.ПриостПлат);
	Содержание.НачальныйОстатокШтрафРеструктурированный = СуммаПоДаннымНалоговогоОргана(СальдоНачXDTO.Штраф.РеструкДолг);
	
	Содержание.НачальныйОстатокНеуплаченныеПроцентыВсего = СуммаПоДаннымНалоговогоОргана(СальдоНачXDTO.НеупСум.НеупСумВс);
	Содержание.НачальныйОстатокНеуплаченныеПроценты      = СуммаПоДаннымНалоговогоОргана(СальдоНачXDTO.НеупСум.НеупСумПр);
	Содержание.НачальныйОстатокНеуплаченныеПроцентыПриостановленный = СуммаПоДаннымНалоговогоОргана(СальдоНачXDTO.НеупСум.ПриосПл);
	
	Содержание.НачальныйОстатокПроценты                  = СуммаПоДаннымНалоговогоОргана(СальдоНачXDTO.ПроцНачБанкр);
	
	Содержание.НачальныйОстатокПоложительноеСальдо         = СуммаПоДаннымНалоговогоОргана(СальдоНачXDTO.СальдоПол.СальдоПолВс);
	Содержание.НачальныйОстатокНалогПоложительноеСальдо    = СуммаПоДаннымНалоговогоОргана(СальдоНачXDTO.СальдоПол.Налог);
	Содержание.НачальныйОстатокПениПоложительноеСальдо     = СуммаПоДаннымНалоговогоОргана(СальдоНачXDTO.СальдоПол.Пени);
	Содержание.НачальныйОстатокШтрафПоложительноеСальдо    = СуммаПоДаннымНалоговогоОргана(СальдоНачXDTO.СальдоПол.Штраф);
	Содержание.НачальныйОстатокПроцентыПоложительноеСальдо = СуммаПоДаннымНалоговогоОргана(СальдоНачXDTO.СальдоПол.Проц);
	
	// Данные за сверяемый период
	
	СверкаЗаПериодXDTO = СодАктXDTO.СверПериод;
	
	Содержание.ПринятоеСальдоНалог                = СуммаПоДаннымНалоговогоОргана(СверкаЗаПериодXDTO.СальдоПринят.Налог);
	Содержание.ПринятоеСальдоПени                 = СуммаПоДаннымНалоговогоОргана(СверкаЗаПериодXDTO.СальдоПринят.Пени);
	Содержание.ПринятоеСальдоШтраф                = СуммаПоДаннымНалоговогоОргана(СверкаЗаПериодXDTO.СальдоПринят.Штраф);
	Содержание.ПринятоеСальдоНеуплаченныеПроценты = СуммаПоДаннымНалоговогоОргана(СверкаЗаПериодXDTO.СальдоПринят.Остат);
	Содержание.ПринятоеСальдоПроценты             = СуммаПоДаннымНалоговогоОргана(СверкаЗаПериодXDTO.СальдоПринят.Процент);
	
	Содержание.НачисленоНалог                = СуммаПоДаннымНалоговогоОргана(СверкаЗаПериодXDTO.Начислено.Налог);
	Содержание.НачисленоПени                 = СуммаПоДаннымНалоговогоОргана(СверкаЗаПериодXDTO.Начислено.Пени);
	Содержание.НачисленоШтраф                = СуммаПоДаннымНалоговогоОргана(СверкаЗаПериодXDTO.Начислено.Штраф);
	Содержание.НачисленоНеуплаченныеПроценты = СуммаПоДаннымНалоговогоОргана(СверкаЗаПериодXDTO.Начислено.Остат);
	Содержание.НачисленоПроценты             = СуммаПоДаннымНалоговогоОргана(СверкаЗаПериодXDTO.Начислено.Процент);
	
	Содержание.УменьшеноНалог                = СуммаПоДаннымНалоговогоОргана(СверкаЗаПериодXDTO.Уменьшено.Налог);
	Содержание.УменьшеноПени                 = СуммаПоДаннымНалоговогоОргана(СверкаЗаПериодXDTO.Уменьшено.Пени);
	Содержание.УменьшеноШтраф                = СуммаПоДаннымНалоговогоОргана(СверкаЗаПериодXDTO.Уменьшено.Штраф);
	
	Содержание.УплаченоНалогВсего               = СуммаПоДаннымНалоговогоОргана(СверкаЗаПериодXDTO.Уплачено.НалогВсего);
	Содержание.УплаченоНалог                    = СуммаПоДаннымНалоговогоОргана(СверкаЗаПериодXDTO.Уплачено.НалУпл);
	Содержание.УплаченоНалогЗачтеноИзлишнего    = СуммаПоДаннымНалоговогоОргана(СверкаЗаПериодXDTO.Уплачено.ЗачИзлУплНал);
	Содержание.УплаченоНалогОтсроченный         = СуммаПоДаннымНалоговогоОргана(СверкаЗаПериодXDTO.Уплачено.НалОтсрПлат);
	Содержание.УплаченоНалогПриостановленный    = СуммаПоДаннымНалоговогоОргана(СверкаЗаПериодXDTO.Уплачено.НалПриостПлат);
	Содержание.УплаченоНалогРеструктурированный = СуммаПоДаннымНалоговогоОргана(СверкаЗаПериодXDTO.Уплачено.НалРеструкДолг);
	
	Содержание.УплаченоПениВсего                = СуммаПоДаннымНалоговогоОргана(СверкаЗаПериодXDTO.Уплачено.ПенВсего);
	Содержание.УплаченоПени                     = СуммаПоДаннымНалоговогоОргана(СверкаЗаПериодXDTO.Уплачено.ПенУпл);
	Содержание.УплаченоПениОтсроченный          = СуммаПоДаннымНалоговогоОргана(СверкаЗаПериодXDTO.Уплачено.ПенОтсрПлат);
	Содержание.УплаченоПениПриостановленный     = СуммаПоДаннымНалоговогоОргана(СверкаЗаПериодXDTO.Уплачено.ПенПриостПлат);
	Содержание.УплаченоПениРеструктурированный  = СуммаПоДаннымНалоговогоОргана(СверкаЗаПериодXDTO.Уплачено.ПенРеструкДолг);
	
	Содержание.УплаченоШтрафВсего               = СуммаПоДаннымНалоговогоОргана(СверкаЗаПериодXDTO.Уплачено.ШтрВсего);
	Содержание.УплаченоШтраф                    = СуммаПоДаннымНалоговогоОргана(СверкаЗаПериодXDTO.Уплачено.ШтрУпл);
	Содержание.УплаченоШтрафОтсроченный         = СуммаПоДаннымНалоговогоОргана(СверкаЗаПериодXDTO.Уплачено.ШтрОтсрПлат);
	Содержание.УплаченоШтрафПриостановленный    = СуммаПоДаннымНалоговогоОргана(СверкаЗаПериодXDTO.Уплачено.ШтрПриостПлат);
	Содержание.УплаченоШтрафРеструктурированный = СуммаПоДаннымНалоговогоОргана(СверкаЗаПериодXDTO.Уплачено.ШтрРеструкДолг);
	
	Содержание.УплаченоПроцентыВсего                        = СуммаПоДаннымНалоговогоОргана(СверкаЗаПериодXDTO.Уплачено.ПрВсего);
	Содержание.УплаченоНеуплаченныеПроценты                 = СуммаПоДаннымНалоговогоОргана(СверкаЗаПериодXDTO.Уплачено.ПрОстСумм);
	Содержание.УплаченоНеуплаченныеПроцентыПриостановленный = СуммаПоДаннымНалоговогоОргана(СверкаЗаПериодXDTO.Уплачено.ПрПриПлат);
	Содержание.УплаченоПроценты                             = СуммаПоДаннымНалоговогоОргана(СверкаЗаПериодXDTO.Уплачено.ПрБанкрот);
	
	Содержание.ПереданоНалог                 = СуммаПоДаннымНалоговогоОргана(СверкаЗаПериодXDTO.ПрдСальдоРЮЛ.Налог);
	Содержание.ПереданоПени                  = СуммаПоДаннымНалоговогоОргана(СверкаЗаПериодXDTO.ПрдСальдоРЮЛ.Пени);
	Содержание.ПереданоШтраф                 = СуммаПоДаннымНалоговогоОргана(СверкаЗаПериодXDTO.ПрдСальдоРЮЛ.Штрафы);
	Содержание.ПереданоНеуплаченныеПроценты  = СуммаПоДаннымНалоговогоОргана(СверкаЗаПериодXDTO.ПрдСальдоРЮЛ.НеупСум);
	Содержание.ПереданоПроценты              = СуммаПоДаннымНалоговогоОргана(СверкаЗаПериодXDTO.ПрдСальдоРЮЛ.ПрБанкрот);
	
	Содержание.СписаноПени                    = СуммаПоДаннымНалоговогоОргана(СверкаЗаПериодXDTO.СпЗадолжПен);
	Содержание.СписаноШтраф                   = СуммаПоДаннымНалоговогоОргана(СверкаЗаПериодXDTO.СпЗадолжШтр);
	
	Содержание.СписаноПравительствомНалог     = СуммаПоДаннымНалоговогоОргана(СверкаЗаПериодXDTO.СписЗПр.Налог);
	Содержание.СписаноПравительствомПени      = СуммаПоДаннымНалоговогоОргана(СверкаЗаПериодXDTO.СписЗПр.Пени);
	Содержание.СписаноПравительствомШтраф     = СуммаПоДаннымНалоговогоОргана(СверкаЗаПериодXDTO.СписЗПр.Штрафы);
	Содержание.СписаноПравительствомНеуплаченныеПроценты = СуммаПоДаннымНалоговогоОргана(СверкаЗаПериодXDTO.СписЗПр.НеупСум);
	
	Содержание.ВозвращеноНалог                = СуммаПоДаннымНалоговогоОргана(СверкаЗаПериодXDTO.Возвращено.Налог);
	Содержание.ВозвращеноНалогИзлишний        = СуммаПоДаннымНалоговогоОргана(СверкаЗаПериодXDTO.Возвращено.НалИзлУп);
	Содержание.ВозвращеноПени                 = СуммаПоДаннымНалоговогоОргана(СверкаЗаПериодXDTO.Возвращено.Пени);
	Содержание.ВозвращеноПениИзлишний         = СуммаПоДаннымНалоговогоОргана(СверкаЗаПериодXDTO.Возвращено.ПенИзлУп);
	Содержание.ВозвращеноШтраф                = СуммаПоДаннымНалоговогоОргана(СверкаЗаПериодXDTO.Возвращено.Штраф);
	Содержание.ВозвращеноШтрафИзлишний        = СуммаПоДаннымНалоговогоОргана(СверкаЗаПериодXDTO.Возвращено.ШтрафИзлУп);
	Содержание.ВозвращеноПроценты             = СуммаПоДаннымНалоговогоОргана(СверкаЗаПериодXDTO.Возвращено.НеупСум);
	Содержание.ВозвращеноНеуплаченныеПроценты = СуммаПоДаннымНалоговогоОргана(СверкаЗаПериодXDTO.Возвращено.Проц);
	
	// Сальдо на конец сверяемого периода
	СальдоКонXDTO = СодАктXDTO.СальдоКон;
	
	Содержание.КонечныйОстатокЗадолженность             = СуммаПоДаннымНалоговогоОргана(СальдоКонXDTO.ЗадолжВсего);
	
	Содержание.КонечныйОстатокНалог                     = СуммаПоДаннымНалоговогоОргана(СальдоКонXDTO.Налог.Всего);
	Содержание.КонечныйОстатокНалогОтрицательноеСальдо  = СуммаПоДаннымНалоговогоОргана(СальдоКонXDTO.Налог.СальдоОтр);
	Содержание.КонечныйОстатокНалогОтсроченный          = СуммаПоДаннымНалоговогоОргана(СальдоКонXDTO.Налог.ОтсрПлат);
	Содержание.КонечныйОстатокНалогПриостановленный     = СуммаПоДаннымНалоговогоОргана(СальдоКонXDTO.Налог.ПрПлат);
	Содержание.КонечныйОстатокНалогРеструктурированный  = СуммаПоДаннымНалоговогоОргана(СальдоКонXDTO.Налог.РестрДолг);
	
	Содержание.КонечныйОстатокПени                      = СуммаПоДаннымНалоговогоОргана(СальдоКонXDTO.Пени.Всего);
	Содержание.КонечныйОстатокПениОтрицательноеСальдо   = СуммаПоДаннымНалоговогоОргана(СальдоКонXDTO.Пени.СальдоОтр);
	Содержание.КонечныйОстатокПениОтсроченный           = СуммаПоДаннымНалоговогоОргана(СальдоКонXDTO.Пени.ОтсрПлат);
	Содержание.КонечныйОстатокПениПриостановленный      = СуммаПоДаннымНалоговогоОргана(СальдоКонXDTO.Пени.ПриостПлат);
	Содержание.КонечныйОстатокПениРеструктурированный   = СуммаПоДаннымНалоговогоОргана(СальдоКонXDTO.Пени.РеструкДолг);
	
	Содержание.КонечныйОстатокШтраф                     = СуммаПоДаннымНалоговогоОргана(СальдоКонXDTO.Штрафы.ШтрВсего);
	Содержание.КонечныйОстатокШтрафОтрицательноеСальдо  = СуммаПоДаннымНалоговогоОргана(СальдоКонXDTO.Штрафы.СальдоОтр);
	Содержание.КонечныйОстатокШтрафОтсроченный          = СуммаПоДаннымНалоговогоОргана(СальдоКонXDTO.Штрафы.ШтрОтсрПлат);
	Содержание.КонечныйОстатокШтрафПриостановленный     = СуммаПоДаннымНалоговогоОргана(СальдоКонXDTO.Штрафы.ШтрПриостПлат);
	Содержание.КонечныйОстатокШтрафРеструктурированный  = СуммаПоДаннымНалоговогоОргана(СальдоКонXDTO.Штрафы.ШтрРестрДолг);
	
	Содержание.КонечныйОстатокНеуплаченныеПроцентыВсего = СуммаПоДаннымНалоговогоОргана(СальдоКонXDTO.ОстНеупСум.НеупПрВс);
	Содержание.КонечныйОстатокНеуплаченныеПроценты      = СуммаПоДаннымНалоговогоОргана(СальдоКонXDTO.ОстНеупСум.ОстНеПр);
	Содержание.КонечныйОстатокНеуплаченныеПроцентыПриостановленный = СуммаПоДаннымНалоговогоОргана(СальдоКонXDTO.ОстНеупСум.ПриосВзПлат);
	
	Содержание.КонечныйОстатокПроценты                  = СуммаПоДаннымНалоговогоОргана(СальдоКонXDTO.ПроцНачБанкр);
	
	Содержание.КонечныйОстатокПоложительноеСальдо         = СуммаПоДаннымНалоговогоОргана(СальдоКонXDTO.СальдоПол.СальдоПолВс);
	Содержание.КонечныйОстатокНалогПоложительноеСальдо    = СуммаПоДаннымНалоговогоОргана(СальдоКонXDTO.СальдоПол.Налог);
	Содержание.КонечныйОстатокПениПоложительноеСальдо     = СуммаПоДаннымНалоговогоОргана(СальдоКонXDTO.СальдоПол.Пени);
	Содержание.КонечныйОстатокШтрафПоложительноеСальдо    = СуммаПоДаннымНалоговогоОргана(СальдоКонXDTO.СальдоПол.Штраф);
	Содержание.КонечныйОстатокПроцентыПоложительноеСальдо = СуммаПоДаннымНалоговогоОргана(СальдоКонXDTO.СальдоПол.Проц);
	
	// Денежные средства, списанные с расчетных счетов налогоплательщика, но не зачисленные на счета по учету доходов бюджетов на конец сверяемого периода
	НеЗачисленныеСредстваXDTO = СодАктXDTO.ДенСрСп;
	
	Содержание.НеЗачисленныеСредстваНалог                = СуммаПоДаннымНалоговогоОргана(НеЗачисленныеСредстваXDTO.Налог);
	Содержание.НеЗачисленныеСредстваПени                 = СуммаПоДаннымНалоговогоОргана(НеЗачисленныеСредстваXDTO.Пени);
	Содержание.НеЗачисленныеСредстваШтраф                = СуммаПоДаннымНалоговогоОргана(НеЗачисленныеСредстваXDTO.Штрафы);
	Содержание.НеЗачисленныеСредстваНеуплаченныеПроценты = СуммаПоДаннымНалоговогоОргана(НеЗачисленныеСредстваXDTO.Остаток);
	Содержание.НеЗачисленныеСредстваПроценты             = СуммаПоДаннымНалоговогоОргана(НеЗачисленныеСредстваXDTO.Процент);
	
КонецПроцедуры

Функция НовыеАктыСверок()
	
	АктыСверок = Новый ТаблицаЗначений();
	
	АктыСверок.Колонки.Добавить("НаименованиеОрганизации", Новый ОписаниеТипов("Строка"));
	АктыСверок.Колонки.Добавить("ИНН",                     Новый ОписаниеТипов("Строка"));
	АктыСверок.Колонки.Добавить("КПП",                     Новый ОписаниеТипов("Строка"));
	АктыСверок.Колонки.Добавить("ЭтоЮрЛицо",               Новый ОписаниеТипов("Булево"));
	АктыСверок.Колонки.Добавить("НомерАкта",               Новый ОписаниеТипов("Строка"));
	АктыСверок.Колонки.Добавить("ДатаАкта",                ОбщегоНазначения.ОписаниеТипаДата(Частидаты.Дата));
	АктыСверок.Колонки.Добавить("ДатаНачала",              ОбщегоНазначения.ОписаниеТипаДата(Частидаты.Дата));
	АктыСверок.Колонки.Добавить("ДатаОкончания",           ОбщегоНазначения.ОписаниеТипаДата(Частидаты.Дата));
	АктыСверок.Колонки.Добавить("КодИФНС",                 Новый ОписаниеТипов("Строка"));
	АктыСверок.Колонки.Добавить("НаименованиеИФНС",        Новый ОписаниеТипов("Строка"));
	АктыСверок.Колонки.Добавить("Раздел1",       Новый ОписаниеТипов("ТаблицаЗначений"));
	АктыСверок.Колонки.Добавить("Раздел2",       Новый ОписаниеТипов("ТаблицаЗначений"));
	
	Возврат АктыСверок;
	
КонецФункции

Функция НовоеСодержаниеРаздела1Акта()
	
	ОписаниеКолонкиСТипомЧисло = Новый ОписаниеТипов("Число", Новый КвалификаторыЧисла(17,2));
	
	СодержаниеАкта = Новый ТаблицаЗначений();
	
	СодержаниеАкта.Колонки.Добавить("КБК",                Новый ОписаниеТипов("Строка"));
	СодержаниеАкта.Колонки.Добавить("НаименованиеНалога", Новый ОписаниеТипов("Строка"));
	СодержаниеАкта.Колонки.Добавить("КПП",                Новый ОписаниеТипов("Строка"));
	СодержаниеАкта.Колонки.Добавить("ОКТМО",              Новый ОписаниеТипов("Строка"));
	СодержаниеАкта.Колонки.Добавить("ОКАТО",              Новый ОписаниеТипов("Строка"));
	
	// Сальдо расчетов
	СодержаниеАкта.Колонки.Добавить("НеЗачисленныеСредства",    ОписаниеКолонкиСТипомЧисло);
	СодержаниеАкта.Колонки.Добавить("НевыясненныеПлатежи",      ОписаниеКолонкиСТипомЧисло);
	
	// Сальдо расчетов по налогам
	СодержаниеАкта.Колонки.Добавить("НалогЗадолженностьВсего",  ОписаниеКолонкиСТипомЧисло);
	СодержаниеАкта.Колонки.Добавить("НалогОтрицательноеСальдо", ОписаниеКолонкиСТипомЧисло);
	СодержаниеАкта.Колонки.Добавить("НалогОтсроченный",         ОписаниеКолонкиСТипомЧисло);
	СодержаниеАкта.Колонки.Добавить("НалогПриостановленный",    ОписаниеКолонкиСТипомЧисло);
	СодержаниеАкта.Колонки.Добавить("НалогРеструктурированный", ОписаниеКолонкиСТипомЧисло);
	СодержаниеАкта.Колонки.Добавить("НалогПоложительноеСальдо", ОписаниеКолонкиСТипомЧисло);
	
	// Сальдо расчетов по пеням
	СодержаниеАкта.Колонки.Добавить("ПениЗадолженностьВсего",   ОписаниеКолонкиСТипомЧисло);
	СодержаниеАкта.Колонки.Добавить("ПениОтрицательноеСальдо",  ОписаниеКолонкиСТипомЧисло);
	СодержаниеАкта.Колонки.Добавить("ПениОтсроченный",          ОписаниеКолонкиСТипомЧисло);
	СодержаниеАкта.Колонки.Добавить("ПениПриостановленный",     ОписаниеКолонкиСТипомЧисло);
	СодержаниеАкта.Колонки.Добавить("ПениРеструктурированный",  ОписаниеКолонкиСТипомЧисло);
	СодержаниеАкта.Колонки.Добавить("ПениПоложительноеСальдо",  ОписаниеКолонкиСТипомЧисло);
	
	// Сальдо расчетов по штрафам
	СодержаниеАкта.Колонки.Добавить("ШтрафЗадолженностьВсего",  ОписаниеКолонкиСТипомЧисло);
	СодержаниеАкта.Колонки.Добавить("ШтрафОтрицательноеСальдо", ОписаниеКолонкиСТипомЧисло);
	СодержаниеАкта.Колонки.Добавить("ШтрафОтсроченный",         ОписаниеКолонкиСТипомЧисло);
	СодержаниеАкта.Колонки.Добавить("ШтрафПриостановленный",    ОписаниеКолонкиСТипомЧисло);
	СодержаниеАкта.Колонки.Добавить("ШтрафРеструктурированный", ОписаниеКолонкиСТипомЧисло);
	СодержаниеАкта.Колонки.Добавить("ШтрафПоложительноеСальдо", ОписаниеКолонкиСТипомЧисло);
	
	// Сальдо расчетов по процентам
	СодержаниеАкта.Колонки.Добавить("ОстатокНеуплаченныеПроцентыДосрочные", ОписаниеКолонкиСТипомЧисло);
	СодержаниеАкта.Колонки.Добавить("Проценты",                             ОписаниеКолонкиСТипомЧисло);
	СодержаниеАкта.Колонки.Добавить("ОстатокНеуплаченныеПроценты",          ОписаниеКолонкиСТипомЧисло);
	СодержаниеАкта.Колонки.Добавить("ПроцентыПриостановленный",             ОписаниеКолонкиСТипомЧисло);
	СодержаниеАкта.Колонки.Добавить("ПроцентыПоложительноеСальдо",          ОписаниеКолонкиСТипомЧисло);
	СодержаниеАкта.Колонки.Добавить("ПроцентыПриБанкротстве",               ОписаниеКолонкиСТипомЧисло);
	
	Возврат СодержаниеАкта;
	
КонецФункции

Функция НовоеСодержаниеРаздела2Акта()
	
	ОписаниеКолонкиСТипомЧисло = Новый ОписаниеТипов("Число", Новый КвалификаторыЧисла(17,2));
	
	СодержаниеАкта = Новый ТаблицаЗначений();
	
	СодержаниеАкта.Колонки.Добавить("КБК",                Новый ОписаниеТипов("Строка"));
	СодержаниеАкта.Колонки.Добавить("НаименованиеНалога", Новый ОписаниеТипов("Строка"));
	СодержаниеАкта.Колонки.Добавить("КПП",                Новый ОписаниеТипов("Строка"));
	СодержаниеАкта.Колонки.Добавить("ОКТМО",              Новый ОписаниеТипов("Строка"));
	СодержаниеАкта.Колонки.Добавить("ОКАТО",              Новый ОписаниеТипов("Строка"));
	
	СодержаниеАкта.Колонки.Добавить("НачальныйОстатокЗадолженность",            ОписаниеКолонкиСТипомЧисло);
	
	// Сальдо на начало сверяемого периода
	СодержаниеАкта.Колонки.Добавить("НачальныйОстатокНалог",                    ОписаниеКолонкиСТипомЧисло);
	СодержаниеАкта.Колонки.Добавить("НачальныйОстатокНалогОтрицательноеСальдо", ОписаниеКолонкиСТипомЧисло);
	СодержаниеАкта.Колонки.Добавить("НачальныйОстатокНалогОтсроченный",         ОписаниеКолонкиСТипомЧисло);
	СодержаниеАкта.Колонки.Добавить("НачальныйОстатокНалогПриостановленный",    ОписаниеКолонкиСТипомЧисло);
	СодержаниеАкта.Колонки.Добавить("НачальныйОстатокНалогРеструктурированный", ОписаниеКолонкиСТипомЧисло);
	
	СодержаниеАкта.Колонки.Добавить("НачальныйОстатокПени",                     ОписаниеКолонкиСТипомЧисло);
	СодержаниеАкта.Колонки.Добавить("НачальныйОстатокПениОтрицательноеСальдо",  ОписаниеКолонкиСТипомЧисло);
	СодержаниеАкта.Колонки.Добавить("НачальныйОстатокПениОтсроченный",          ОписаниеКолонкиСТипомЧисло);
	СодержаниеАкта.Колонки.Добавить("НачальныйОстатокПениПриостановленный",     ОписаниеКолонкиСТипомЧисло);
	СодержаниеАкта.Колонки.Добавить("НачальныйОстатокПениРеструктурированный",  ОписаниеКолонкиСТипомЧисло);
	
	СодержаниеАкта.Колонки.Добавить("НачальныйОстатокШтраф",                    ОписаниеКолонкиСТипомЧисло);
	СодержаниеАкта.Колонки.Добавить("НачальныйОстатокШтрафОтрицательноеСальдо", ОписаниеКолонкиСТипомЧисло);
	СодержаниеАкта.Колонки.Добавить("НачальныйОстатокШтрафОтсроченный",         ОписаниеКолонкиСТипомЧисло);
	СодержаниеАкта.Колонки.Добавить("НачальныйОстатокШтрафПриостановленный",    ОписаниеКолонкиСТипомЧисло);
	СодержаниеАкта.Колонки.Добавить("НачальныйОстатокШтрафРеструктурированный", ОписаниеКолонкиСТипомЧисло);

	СодержаниеАкта.Колонки.Добавить("НачальныйОстатокНеуплаченныеПроцентыВсего",            ОписаниеКолонкиСТипомЧисло);
	СодержаниеАкта.Колонки.Добавить("НачальныйОстатокНеуплаченныеПроценты",                 ОписаниеКолонкиСТипомЧисло);
	СодержаниеАкта.Колонки.Добавить("НачальныйОстатокНеуплаченныеПроцентыПриостановленный", ОписаниеКолонкиСТипомЧисло);
	СодержаниеАкта.Колонки.Добавить("НачальныйОстатокПроценты",                             ОписаниеКолонкиСТипомЧисло);
	
	СодержаниеАкта.Колонки.Добавить("НачальныйОстатокПоложительноеСальдо",                     ОписаниеКолонкиСТипомЧисло);
	СодержаниеАкта.Колонки.Добавить("НачальныйОстатокНалогПоложительноеСальдо",                ОписаниеКолонкиСТипомЧисло);
	СодержаниеАкта.Колонки.Добавить("НачальныйОстатокПениПоложительноеСальдо",                 ОписаниеКолонкиСТипомЧисло);
	СодержаниеАкта.Колонки.Добавить("НачальныйОстатокШтрафПоложительноеСальдо",                ОписаниеКолонкиСТипомЧисло);
	СодержаниеАкта.Колонки.Добавить("НачальныйОстатокПроцентыПоложительноеСальдо",             ОписаниеКолонкиСТипомЧисло);
	
	// Данные за сверяемый период
	СодержаниеАкта.Колонки.Добавить("ПринятоеСальдоНалог",                ОписаниеКолонкиСТипомЧисло);
	СодержаниеАкта.Колонки.Добавить("ПринятоеСальдоПени",                 ОписаниеКолонкиСТипомЧисло);
	СодержаниеАкта.Колонки.Добавить("ПринятоеСальдоШтраф",                ОписаниеКолонкиСТипомЧисло);
	СодержаниеАкта.Колонки.Добавить("ПринятоеСальдоНеуплаченныеПроценты", ОписаниеКолонкиСТипомЧисло);
	СодержаниеАкта.Колонки.Добавить("ПринятоеСальдоПроценты",             ОписаниеКолонкиСТипомЧисло);
	
	СодержаниеАкта.Колонки.Добавить("НачисленоНалог",                ОписаниеКолонкиСТипомЧисло);
	СодержаниеАкта.Колонки.Добавить("НачисленоПени",                 ОписаниеКолонкиСТипомЧисло);
	СодержаниеАкта.Колонки.Добавить("НачисленоШтраф",                ОписаниеКолонкиСТипомЧисло);
	СодержаниеАкта.Колонки.Добавить("НачисленоНеуплаченныеПроценты", ОписаниеКолонкиСТипомЧисло);
	СодержаниеАкта.Колонки.Добавить("НачисленоПроценты",             ОписаниеКолонкиСТипомЧисло);
	
	СодержаниеАкта.Колонки.Добавить("УменьшеноНалог",                ОписаниеКолонкиСТипомЧисло);
	СодержаниеАкта.Колонки.Добавить("УменьшеноПени",                 ОписаниеКолонкиСТипомЧисло);
	СодержаниеАкта.Колонки.Добавить("УменьшеноШтраф",                ОписаниеКолонкиСТипомЧисло);
	
	СодержаниеАкта.Колонки.Добавить("УплаченоНалогВсего",               ОписаниеКолонкиСТипомЧисло);
	СодержаниеАкта.Колонки.Добавить("УплаченоНалог",                    ОписаниеКолонкиСТипомЧисло);
	СодержаниеАкта.Колонки.Добавить("УплаченоНалогЗачтеноИзлишнего",    ОписаниеКолонкиСТипомЧисло);
	СодержаниеАкта.Колонки.Добавить("УплаченоНалогОтсроченный",         ОписаниеКолонкиСТипомЧисло);
	СодержаниеАкта.Колонки.Добавить("УплаченоНалогПриостановленный",    ОписаниеКолонкиСТипомЧисло);
	СодержаниеАкта.Колонки.Добавить("УплаченоНалогРеструктурированный", ОписаниеКолонкиСТипомЧисло);
	
	СодержаниеАкта.Колонки.Добавить("УплаченоПениВсего",                ОписаниеКолонкиСТипомЧисло);
	СодержаниеАкта.Колонки.Добавить("УплаченоПени",                     ОписаниеКолонкиСТипомЧисло);
	СодержаниеАкта.Колонки.Добавить("УплаченоПениОтсроченный",          ОписаниеКолонкиСТипомЧисло);
	СодержаниеАкта.Колонки.Добавить("УплаченоПениПриостановленный",     ОписаниеКолонкиСТипомЧисло);
	СодержаниеАкта.Колонки.Добавить("УплаченоПениРеструктурированный",  ОписаниеКолонкиСТипомЧисло);
	
	СодержаниеАкта.Колонки.Добавить("УплаченоШтрафВсего",               ОписаниеКолонкиСТипомЧисло);
	СодержаниеАкта.Колонки.Добавить("УплаченоШтраф",                    ОписаниеКолонкиСТипомЧисло);
	СодержаниеАкта.Колонки.Добавить("УплаченоШтрафОтсроченный",         ОписаниеКолонкиСТипомЧисло);
	СодержаниеАкта.Колонки.Добавить("УплаченоШтрафПриостановленный",    ОписаниеКолонкиСТипомЧисло);
	СодержаниеАкта.Колонки.Добавить("УплаченоШтрафРеструктурированный", ОписаниеКолонкиСТипомЧисло);
	
	СодержаниеАкта.Колонки.Добавить("УплаченоПроцентыВсего",                        ОписаниеКолонкиСТипомЧисло);
	СодержаниеАкта.Колонки.Добавить("УплаченоНеуплаченныеПроценты",                 ОписаниеКолонкиСТипомЧисло);
	СодержаниеАкта.Колонки.Добавить("УплаченоНеуплаченныеПроцентыПриостановленный", ОписаниеКолонкиСТипомЧисло);
	СодержаниеАкта.Колонки.Добавить("УплаченоПроценты",                             ОписаниеКолонкиСТипомЧисло);
	
	СодержаниеАкта.Колонки.Добавить("ПереданоНалог",                 ОписаниеКолонкиСТипомЧисло);
	СодержаниеАкта.Колонки.Добавить("ПереданоПени",                  ОписаниеКолонкиСТипомЧисло);
	СодержаниеАкта.Колонки.Добавить("ПереданоШтраф",                 ОписаниеКолонкиСТипомЧисло);
	СодержаниеАкта.Колонки.Добавить("ПереданоНеуплаченныеПроценты",  ОписаниеКолонкиСТипомЧисло);
	СодержаниеАкта.Колонки.Добавить("ПереданоПроценты",              ОписаниеКолонкиСТипомЧисло);
	
	СодержаниеАкта.Колонки.Добавить("СписаноПени",                 ОписаниеКолонкиСТипомЧисло);
	СодержаниеАкта.Колонки.Добавить("СписаноШтраф",                ОписаниеКолонкиСТипомЧисло);
	
	СодержаниеАкта.Колонки.Добавить("СписаноПравительствомНалог",  ОписаниеКолонкиСТипомЧисло);
	СодержаниеАкта.Колонки.Добавить("СписаноПравительствомПени",   ОписаниеКолонкиСТипомЧисло);
	СодержаниеАкта.Колонки.Добавить("СписаноПравительствомШтраф",  ОписаниеКолонкиСТипомЧисло);
	СодержаниеАкта.Колонки.Добавить("СписаноПравительствомНеуплаченныеПроценты", ОписаниеКолонкиСТипомЧисло);
	
	СодержаниеАкта.Колонки.Добавить("ВозвращеноНалог",                ОписаниеКолонкиСТипомЧисло);
	СодержаниеАкта.Колонки.Добавить("ВозвращеноНалогИзлишний",        ОписаниеКолонкиСТипомЧисло);
	СодержаниеАкта.Колонки.Добавить("ВозвращеноПени",                 ОписаниеКолонкиСТипомЧисло);
	СодержаниеАкта.Колонки.Добавить("ВозвращеноПениИзлишний",         ОписаниеКолонкиСТипомЧисло);
	СодержаниеАкта.Колонки.Добавить("ВозвращеноШтраф",                ОписаниеКолонкиСТипомЧисло);
	СодержаниеАкта.Колонки.Добавить("ВозвращеноШтрафИзлишний",        ОписаниеКолонкиСТипомЧисло);
	СодержаниеАкта.Колонки.Добавить("ВозвращеноПроценты",             ОписаниеКолонкиСТипомЧисло);
	СодержаниеАкта.Колонки.Добавить("ВозвращеноНеуплаченныеПроценты", ОписаниеКолонкиСТипомЧисло);
	
	// Сальдо на конец сверяемого периода
	СодержаниеАкта.Колонки.Добавить("КонечныйОстатокЗадолженность",            ОписаниеКолонкиСТипомЧисло);
	
	СодержаниеАкта.Колонки.Добавить("КонечныйОстатокНалог",                    ОписаниеКолонкиСТипомЧисло);
	СодержаниеАкта.Колонки.Добавить("КонечныйОстатокНалогОтрицательноеСальдо", ОписаниеКолонкиСТипомЧисло);
	СодержаниеАкта.Колонки.Добавить("КонечныйОстатокНалогОтсроченный",         ОписаниеКолонкиСТипомЧисло);
	СодержаниеАкта.Колонки.Добавить("КонечныйОстатокНалогПриостановленный",    ОписаниеКолонкиСТипомЧисло);
	СодержаниеАкта.Колонки.Добавить("КонечныйОстатокНалогРеструктурированный", ОписаниеКолонкиСТипомЧисло);
	
	СодержаниеАкта.Колонки.Добавить("КонечныйОстатокПени",                     ОписаниеКолонкиСТипомЧисло);
	СодержаниеАкта.Колонки.Добавить("КонечныйОстатокПениОтрицательноеСальдо",  ОписаниеКолонкиСТипомЧисло);
	СодержаниеАкта.Колонки.Добавить("КонечныйОстатокПениОтсроченный",          ОписаниеКолонкиСТипомЧисло);
	СодержаниеАкта.Колонки.Добавить("КонечныйОстатокПениПриостановленный",     ОписаниеКолонкиСТипомЧисло);
	СодержаниеАкта.Колонки.Добавить("КонечныйОстатокПениРеструктурированный",  ОписаниеКолонкиСТипомЧисло);
	
	СодержаниеАкта.Колонки.Добавить("КонечныйОстатокШтраф",                    ОписаниеКолонкиСТипомЧисло);
	СодержаниеАкта.Колонки.Добавить("КонечныйОстатокШтрафОтрицательноеСальдо", ОписаниеКолонкиСТипомЧисло);
	СодержаниеАкта.Колонки.Добавить("КонечныйОстатокШтрафОтсроченный",         ОписаниеКолонкиСТипомЧисло);
	СодержаниеАкта.Колонки.Добавить("КонечныйОстатокШтрафПриостановленный",    ОписаниеКолонкиСТипомЧисло);
	СодержаниеАкта.Колонки.Добавить("КонечныйОстатокШтрафРеструктурированный", ОписаниеКолонкиСТипомЧисло);
	
	СодержаниеАкта.Колонки.Добавить("КонечныйОстатокНеуплаченныеПроцентыВсего",            ОписаниеКолонкиСТипомЧисло);
	СодержаниеАкта.Колонки.Добавить("КонечныйОстатокНеуплаченныеПроценты",                 ОписаниеКолонкиСТипомЧисло);
	СодержаниеАкта.Колонки.Добавить("КонечныйОстатокНеуплаченныеПроцентыПриостановленный", ОписаниеКолонкиСТипомЧисло);
	СодержаниеАкта.Колонки.Добавить("КонечныйОстатокПроценты",                             ОписаниеКолонкиСТипомЧисло);
	
	СодержаниеАкта.Колонки.Добавить("КонечныйОстатокПоложительноеСальдо",         ОписаниеКолонкиСТипомЧисло);
	СодержаниеАкта.Колонки.Добавить("КонечныйОстатокНалогПоложительноеСальдо",    ОписаниеКолонкиСТипомЧисло);
	СодержаниеАкта.Колонки.Добавить("КонечныйОстатокПениПоложительноеСальдо",     ОписаниеКолонкиСТипомЧисло);
	СодержаниеАкта.Колонки.Добавить("КонечныйОстатокШтрафПоложительноеСальдо",    ОписаниеКолонкиСТипомЧисло);
	СодержаниеАкта.Колонки.Добавить("КонечныйОстатокПроцентыПоложительноеСальдо", ОписаниеКолонкиСТипомЧисло);
	
	// Денежные средства, списанные с расчетных счетов налогоплательщика, но не зачисленные на счета доходов бюджетов на конец сверяемого периода
	СодержаниеАкта.Колонки.Добавить("НеЗачисленныеСредстваНалог",                ОписаниеКолонкиСТипомЧисло);
	СодержаниеАкта.Колонки.Добавить("НеЗачисленныеСредстваПени",                 ОписаниеКолонкиСТипомЧисло);
	СодержаниеАкта.Колонки.Добавить("НеЗачисленныеСредстваШтраф",                ОписаниеКолонкиСТипомЧисло);
	СодержаниеАкта.Колонки.Добавить("НеЗачисленныеСредстваНеуплаченныеПроценты", ОписаниеКолонкиСТипомЧисло);
	СодержаниеАкта.Колонки.Добавить("НеЗачисленныеСредстваПроценты",             ОписаниеКолонкиСТипомЧисло);
	
	Возврат СодержаниеАкта;
	
КонецФункции

Функция ДатаФНС(Строка)
	
	Если ЗначениеЗаполнено(Строка) Тогда
		Возврат Дата(Прав(Строка, 4), Сред(Строка, 4, 2), Лев(Строка, 2));
	Иначе
		Возврат '00010101';
	КонецЕсли;
	
КонецФункции

Функция СуммаПоДаннымНалоговогоОргана(ОбъектXDTO)
	
	Если ОбъектXDTO.Свойства().Получить("НалОрг") <> Неопределено Тогда
		Возврат ЧислоФНС(ОбъектXDTO.НалОрг);
	КонецЕсли;
	
	Возврат 0;
	
КонецФункции

Функция ЗначениеЧисловогоПоля(ОбъектXDTO, Поле)
	
	Если ОбъектXDTO.Свойства().Получить(Поле) <> Неопределено Тогда
		Возврат ЧислоФНС(ОбъектXDTO[Поле]);
	КонецЕсли;
	
	Возврат 0;
	
КонецФункции

Функция ЧислоФНС(Строка)
	
	Если ЗначениеЗаполнено(Строка) Тогда
		ЧастиЧисла = СтрРазделить(Строка, ".");
		Если ЧастиЧисла.Количество() = 2 Тогда
			ЦелаяЧасть = Число(ЧастиЧисла[0]);
			ДробнаяЧасть = ЧастиЧисла[1]/100;
			Возврат ЦелаяЧасть + ?(ЦелаяЧасть < 0, -ДробнаяЧасть, ДробнаяЧасть);
		ИначеЕсли ЧастиЧисла.Количество() = 1 Тогда
			Возврат Число(ЧастиЧисла[0]);
		Иначе
			Сообщить(НСтр("ru = 'Произошла ошибка при преобразовании строки суммы в число.'"));
		КонецЕсли;
	КонецЕсли;
	
	Возврат 0;
	
КонецФункции

Функция НаименованиеИФНСПоКоду(КодИФНС)
	
	Запрос = Новый Запрос;
	Запрос.УстановитьПараметр("Код", КодИФНС);
	Запрос.Текст =
	"ВЫБРАТЬ ПЕРВЫЕ 1
	|	НалоговыеОрганы.Наименование КАК Наименование
	|ИЗ
	|	Справочник.НалоговыеОрганы КАК НалоговыеОрганы
	|ГДЕ
	|	НалоговыеОрганы.Код = &Код";
	Выборка = Запрос.Выполнить().Выбрать();
	Если Выборка.Следующий() Тогда
		Возврат Выборка.Наименование;
	Иначе
		Возврат "";
	КонецЕсли;
	
КонецФункции

#КонецОбласти

#Область ПечатьШтампаЭП

Процедура ДобавитьШтампПодписиВРезультатЗапросаИОН(ЗапросИОН, ТабДок, ПодписейВСтроке = 2, ВыводитьПустоеПространство = Истина) Экспорт
	
	ДобавитьШтампПодписиПодДокументом(
		ЗапросИОН,
		Перечисления.ТипыТранспортныхСообщений.РезультатОбработкиЗапросНО,
		ТабДок,
		ПодписейВСтроке,
		ВыводитьПустоеПространство);
		
КонецПроцедуры
	
Процедура ДобавитьШтампПодписиПодДокументом(Ссылка, ТипСообщения, ТабДок, ПодписейВСтроке = 2, ВыводитьПустоеПространство = Истина) Экспорт

	Если ТипЗнч(ТипСообщения) <> Тип("Массив") Тогда
		ТипыСообщений = Новый Массив;
		ТипыСообщений.Добавить(ТипСообщения);
	Иначе
		ТипыСообщений = ТипСообщения;
	КонецЕсли;
	
	Подписи = ПодписиПодСообщениями(Ссылка, ТипСообщения);
	
	СвойстваПодписей = Новый Массив;
	
	Для каждого Подпись Из Подписи Цикл
	
		Свойства = ЭлектронныйДокументооборотСКонтролирующимиОрганамиКлиентСервер.СвойстваПодписиДляШтампа();
		Если НЕ Подпись.ВАрхиве Тогда
			
			Если Подпись.Данные.Получить() = Неопределено Тогда
				
				МассивФайлов = ОбщегоНазначенияЭДКО.ПрикрепленныеФайлыКОбъектуИзСправочника(
					Подпись.ТранспортноеСообщение,
					"ТранспортноеСообщениеПрисоединенныеФайлы",
					Подпись.ИмяФайла);
					
				Если МассивФайлов.Количество() > 0 Тогда
					Свойства.ДвДанные = РаботаСФайлами.ДвоичныеДанныеФайла(МассивФайлов[0]);
				КонецЕсли;
				
			Иначе
				Свойства.ДвДанные = Подпись.Данные.Получить();
			КонецЕсли;
			
			СвойстваПодписей.Добавить(Свойства);
			
			ЗаполнитьСвойстваПодписиДляШтампа(
				Подпись.ТранспортноеСообщение, 
				Подпись.ЭЦПЭтоПодписьАбонента, 
				Свойства);
				
		КонецЕсли;
		
	КонецЦикла;
	
	ДобавитьШтампПодписи(СвойстваПодписей, ТабДок, ПодписейВСтроке, ВыводитьПустоеПространство);
	
КонецПроцедуры

Функция ПодписиПодСообщениями(Ссылка, ТипСообщения) Экспорт

	Если ТипЗнч(ТипСообщения) <> Тип("Массив") Тогда
		ТипыСообщений = Новый Массив;
		ТипыСообщений.Добавить(ТипСообщения);
	Иначе
		ТипыСообщений = ТипСообщения;
	КонецЕсли;
	
	Запрос = Новый Запрос;
	Запрос.Текст = "ВЫБРАТЬ РАЗРЕШЕННЫЕ

		|	ЦиклыОбмена.Ссылка КАК Ссылка
		|ПОМЕСТИТЬ ЦиклыПредмета
		|ИЗ
		|	Справочник.ЦиклыОбмена КАК ЦиклыОбмена
		|ГДЕ
		|	ЦиклыОбмена.Предмет = &Предмет
		|;
		|
		|////////////////////////////////////////////////////////////////////////////////
		|ВЫБРАТЬ РАЗРЕШЕННЫЕ
		|	СодержимоеТранспортныхКонтейнеров.Данные КАК Данные,
		|	СодержимоеТранспортныхКонтейнеров.ИмяФайла КАК ИмяФайла,
		|	СодержимоеТранспортныхКонтейнеров.ЭЦПИмяПодписанногоФайла КАК ЭЦПИмяПодписанногоФайла,
		|	СодержимоеТранспортныхКонтейнеров.ЭЦПСертификат КАК ЭЦПСертификат,
		|	ТрСообщение.Ссылка КАК ТранспортноеСообщение,
		|	ВЫБОР
		|		КОГДА СостояниеОбъектов.Архивный ЕСТЬ NULL
		|			ТОГДА ЛОЖЬ
		|		КОГДА СостояниеОбъектов.Архивный = ЛОЖЬ
		|			ТОГДА ЛОЖЬ
		|		ИНАЧЕ ИСТИНА
		|	КОНЕЦ КАК ВАрхиве,
		|	ВЫБОР
		|		КОГДА ТрСообщение.ЦиклОбмена.ФорматДокументооборота = &ЭДОК
		|			ТОГДА ВЫБОР
		|					КОГДА ТИПЗНАЧЕНИЯ(СодержимоеТранспортныхКонтейнеров.ТранспортноеСообщение.Отправитель) = ТИП(Справочник.Организации)
		|						ТОГДА ИСТИНА
		|					ИНАЧЕ ЛОЖЬ
		|				КОНЕЦ
		|		ИНАЧЕ СодержимоеТранспортныхКонтейнеров.ЭЦПЭтоПодписьАбонента
		|	КОНЕЦ КАК ЭЦПЭтоПодписьАбонента
		|ИЗ
		|	РегистрСведений.СодержимоеТранспортныхКонтейнеров КАК СодержимоеТранспортныхКонтейнеров
		|		ВНУТРЕННЕЕ СОЕДИНЕНИЕ Документ.ТранспортноеСообщение КАК ТрСообщение
		|		ПО СодержимоеТранспортныхКонтейнеров.ТранспортноеСообщение = ТрСообщение.Ссылка
		|		ЛЕВОЕ СОЕДИНЕНИЕ РегистрСведений.ПризнакиАрхивированияФайловДОСКонтролирующимиОрганами КАК СостояниеОбъектов
		|		ПО (СостояниеОбъектов.Объект = ТрСообщение.Ссылка)
		|			И (СостояниеОбъектов.ИмяФайла = СодержимоеТранспортныхКонтейнеров.ИмяФайла)
		|			И (СостояниеОбъектов.Владелец = ЗНАЧЕНИЕ(Перечисление.ВидыАрхивируемыхМетаданныхДО.СодержимоеТранспортныхКонтейнеров))
		|ГДЕ
		|	(СодержимоеТранспортныхКонтейнеров.Тип = &ЭЦП
		|				И ТрСообщение.ЦиклОбмена.ФорматДокументооборота <> &ЭДОК
		|			ИЛИ СодержимоеТранспортныхКонтейнеров.Тип <> &Сертификат
		|				И ТрСообщение.ЦиклОбмена.ФорматДокументооборота = &ЭДОК)
		|	И ТрСообщение.Тип В(&ТипыСообщений)
		|	И ТрСообщение.ЦиклОбмена В
		|			(ВЫБРАТЬ
		|				ЦиклыПредмета.Ссылка
		|			ИЗ
		|				ЦиклыПредмета)
		|
		|УПОРЯДОЧИТЬ ПО
		|	СодержимоеТранспортныхКонтейнеров.ТранспортноеСообщение.ДатаТранспорта";

	
	Запрос.УстановитьПараметр("ЭЦП", 		        Перечисления.ТипыСодержимогоТранспортногоКонтейнера.ЭлектронноЦифроваяПодпись);
	Запрос.УстановитьПараметр("Сертификат", 		Перечисления.ТипыСодержимогоТранспортногоКонтейнера.Сертификат);
	Запрос.УстановитьПараметр("ТипыСообщений",      ТипыСообщений);
	Запрос.УстановитьПараметр("ЭДОК", 				Перечисления.ФорматыДокументооборотаСПФР.ЭДОК);
	Запрос.УстановитьПараметр("Предмет", 	        Ссылка);
	
	РезультатЗапроса = Запрос.Выполнить();
	Подписи = РезультатЗапроса.Выгрузить();
	
	Возврат Подписи;
	
КонецФункции

Процедура ЗаполнитьСвойстваПодписиДляШтампа(ТранспортноеСообщение, ЭЦПЭтоПодписьАбонента, СвойстваПодписи) Экспорт 
	
	СвойстваПодписи.ТранспортноеСообщение = ТранспортноеСообщение;
	
	// Взято из процедуры ОформитьСтрокуСодержимого формы ФормаДокумента документа ТранспортноеСообщение
	Если ЭЦПЭтоПодписьАбонента Тогда
		СвойстваПодписи.ЭтоАбонент  = Истина;
	ИначеЕсли ТранспортноеСообщение.ЦиклОбмена.Вид = Перечисления.ВидыЦикловОбмена.ЦиклОбменаСПФР Тогда
		СвойстваПодписи.ЭтоПФР = Истина;
	Иначе
		Если (ТранспортноеСообщение.Тип = Перечисления.ТипыТранспортныхСообщений.ПодтверждениеДекларацияНО
		ИЛИ ТранспортноеСообщение.Тип = Перечисления.ТипыТранспортныхСообщений.ПодтверждениеЗаявлениеНО
		ИЛИ ТранспортноеСообщение.Тип = Перечисления.ТипыТранспортныхСообщений.ПодтверждениеОбращениеНО
		ИЛИ ТранспортноеСообщение.Тип = Перечисления.ТипыТранспортныхСообщений.ПодтверждениеПредставлениеНО
		ИЛИ ТранспортноеСообщение.Тип = Перечисления.ТипыТранспортныхСообщений.ПодтверждениеФорма2НДФЛНО
		ИЛИ ТранспортноеСообщение.Тип = Перечисления.ТипыТранспортныхСообщений.ПодтверждениеДатыОтправкиФСГС)
		И НЕ ТранспортноеСообщение.УчетнаяЗапись.ОбменНапрямую Тогда
			СвойстваПодписи.ЭтоСерверДО = Истина;
		Иначе
			Если ТранспортноеСообщение.ЦиклОбмена.Вид = Перечисления.ВидыЦикловОбмена.ЦиклОбменаСФСГС
			 ИЛИ ТранспортноеСообщение.Тип = Перечисления.ТипыТранспортныхСообщений.ИзвещениеОПолученииОтчетностиФСГС
			 ИЛИ ТранспортноеСообщение.Тип = Перечисления.ТипыТранспортныхСообщений.ПротоколВходногоКонтроляОтчетностиФСГС
			 ИЛИ ТранспортноеСообщение.Тип = Перечисления.ТипыТранспортныхСообщений.ИзвещениеПисьмоИсходящееФСГС Тогда
				СвойстваПодписи.ЭтоФСГС = Истина;
			Иначе
				СвойстваПодписи.ЭтоФНС = Истина;
			КонецЕсли;
		КонецЕсли;
	КонецЕсли;	
		
КонецПроцедуры

// Процедура - Добавить штамп подписи
//
// Параметры:
//  Подписи						 - Массив - Массив двоичных данных файлов подписи
//  ТабДок						 - ТабличныйДокумент - табличный документ, в конец которого будут добавлены штампы ЭП.
//  ПодписейВСтроке				 - Число - Сколько штампов помещать на одной строке, после чего переносить на другую строку
//  ВыводитьПустоеПространство	 - Булево - Если Истина, то при отсуствии подписей выводит пустую область такого же размера
//
Процедура ДобавитьШтампПодписи(СвойстваПодписей, ТабДок, Знач ПодписейВСтроке = 2, ВыводитьПустоеПространство = Истина) Экспорт

	Штампы = Новый Массив;
	Сертификаты = Новый Массив;
	Для каждого СтрокаСвойстваПодписи Из СвойстваПодписей Цикл
		
		ОтправляетсяПоЭДОК = ОтчетОтправляетсяПоЭДОК(СтрокаСвойстваПодписи.ТранспортноеСообщение.ЦиклОбмена);
		
		Если ОтправляетсяПоЭДОК Тогда
			СвойстваПодписи = СвойстваПодписиСообщенияПоЭДОК(СтрокаСвойстваПодписи);
		Иначе
			Попытка
				СвойстваПодписи = КриптографияЭДКОСлужебныйВызовСервера.ПолучитьСвойстваКриптосообщения(СтрокаСвойстваПодписи.ДвДанные);
			Исключение
				СвойстваПодписи = Неопределено;
			КонецПопытки;
		КонецЕсли;
		
		Если СвойстваПодписи = Неопределено Тогда
			Продолжить;
		КонецЕсли;
		
		Если НЕ СвойстваПодписи.Свойство("Подписанты") Тогда
			Продолжить;
		КонецЕсли;
		
		Картинка = КартинкаШтампа(СтрокаСвойстваПодписи);
		
		Для каждого Подписант Из СвойстваПодписи.Подписанты Цикл
			
			Если НЕ Подписант.Сертификат.Свойство("Сертификат") Тогда
				Продолжить;
			КонецЕсли;
			
			ДвДанныеСертификат = Подписант.Сертификат.Сертификат; 
			Сертификат = Новый СертификатКриптографии(ДвДанныеСертификат);
			
			// Исключаем повторение сертификатов в печатной форме
			Если Сертификаты.Найти(Сертификат) = Неопределено Тогда
				Сертификаты.Добавить(Сертификат);
			Иначе
				Продолжить;
			КонецЕсли;
			
			Если Подписант.Свойство("ПодписанныеАтрибуты") И Подписант.ПодписанныеАтрибуты.Свойство("ВремяПодписания") Тогда
				
				ВремяПодписания = Подписант.ПодписанныеАтрибуты.ВремяПодписания;
				
				Если СтрокаСвойстваПодписи.ЭтоСерверДО Тогда
					ВремяПодписания = МестноеВремя(ВремяПодписания, "GMT+3");
				Иначе
					ВремяПодписания = МестноеВремя(ВремяПодписания);
				КонецЕсли;
			
				Штамп = ШтампВизуализацииЭлектроннойПодписи(Сертификат,ВремяПодписания,,Картинка);
			Иначе
				Штамп = ШтампВизуализацииЭлектроннойПодписи(Сертификат,,,Картинка);
			КонецЕсли;
			Штампы.Добавить(Штамп);
		
		КонецЦикла; 
	
	КонецЦикла; 
	
	ПачкаШтампов = Новый Массив;
	КоличествоПодписей = Штампы.Количество();
	ПодписейВСтроке = Мин(ПодписейВСтроке,3);
	
	ШиринаПервойКолонки = ТабДок.Область(ТабДок.ВысотаСтраницы, 1, ТабДок.ВысотаСтраницы, 1).ШиринаКолонки;
	
	Если ВыводитьПустоеПространство И КоличествоПодписей = 0 Тогда
		
		ТабДокШтампов = ПолучитьМакетОбработки("ШтампыЭП");
		ТабДок.Вывести(ТабДокШтампов);
	Иначе
		
		Для НомерШтампа = 1 По КоличествоПодписей Цикл
		
			ПачкаШтампов.Добавить(Штампы[НомерШтампа - 1]);
			
			ЭтоПоследнийВСтроке = Цел(НомерШтампа/ПодписейВСтроке) = НомерШтампа/ПодписейВСтроке;
			ЭтоСамыйПоследний = НомерШтампа = КоличествоПодписей;
			
			// В строку выводим максимум столько подписей, сколько указано в параметре
			// Если подписей больше, выводим на следующей строке
			Если ЭтоПоследнийВСтроке ИЛИ ЭтоСамыйПоследний Тогда
				
				ТабДокШтампов = ПолучитьМакетОбработки("ШтампыЭП");
				ТабДокШтампов.Области.Отступ.ШиринаКолонки = ШиринаПервойКолонки;
				
				ЭлектроннаяПодпись.ДобавитьШтампыВТабличныйДокумент(ТабДокШтампов, ПачкаШтампов);
				ТабДок.Вывести(ТабДокШтампов);
				ПачкаШтампов = Новый Массив;
			КонецЕсли;
		
		КонецЦикла;
	КонецЕсли;

КонецПроцедуры

Функция КартинкаШтампа(СвойстваПодписи)
	
	Картинка = БиблиотекаКартинок.ЭлектроннаяПодписьСиняя;
	
	Если СвойстваПодписи.ЭтоАбонент ИЛИ СвойстваПодписи.ЭтоСерверДО Тогда
		Картинка  = БиблиотекаКартинок.ЭлектроннаяПодписьСиняя;
	ИначеЕсли СвойстваПодписи.ЭтоФНС Тогда
		Картинка  = БиблиотекаКартинок.ГербФНССиний;
	ИначеЕсли СвойстваПодписи.ЭтоПФР Тогда
		Картинка  = БиблиотекаКартинок.ГербПФРСиний;
	ИначеЕсли СвойстваПодписи.ЭтоФСГС Тогда
		Картинка  = БиблиотекаКартинок.ГербРосстатаСиний;
	КонецЕсли;
	
	Возврат Картинка;
	
КонецФункции

Функция ШтампВизуализацииЭлектроннойПодписи(Сертификат, ДатаПодписи = Неопределено, ТекстОтметки = "", ЛоготипОрганизации = Неопределено) Экспорт
	
	СертификатКриптографии = Сертификат;
	
	СвойстваСубъекта    = ЭлектроннаяПодпись.СвойстваСубъектаСертификата(СертификатКриптографии);
	СвойстваСертификата = ЭлектроннаяПодпись.СвойстваСертификата(СертификатКриптографии);
	ДатыСертификата     = ДатыСертификата(СертификатКриптографии);
	
	ПериодДействия = НСтр("ru = 'с %1 по %2'");
	ПериодДействия = СтроковыеФункцииКлиентСервер.ПодставитьПараметрыВСтроку(ПериодДействия,
		Формат(ДатыСертификата.ДатаНачала,    "ДЛФ=D"),
		Формат(ДатыСертификата.ДатаОкончания, "ДЛФ=D"));
	
	Штамп = ПолучитьМакетОбработки("Штамп");
	
	Штамп.Параметры.ДатаПодписи         = ДатаПодписи;
	Штамп.Параметры.НомерСертификата    = СертификатКриптографии.СерийныйНомер;
	Штамп.Параметры.ВладелецСертификата = СвойстваСертификата.КомуВыдан;
	Штамп.Параметры.СрокДействия        = ПериодДействия;
	Штамп.Параметры.ТекстОтметки        = ТекстОтметки;
	
	Если ЛоготипОрганизации <> Неопределено Тогда
		Штамп.Рисунки.Картинка.Картинка = ЛоготипОрганизации;
	КонецЕсли;
	
	Возврат Штамп;
	
КонецФункции

Функция ДатыСертификата(Сертификат) Экспорт
	
	ДобавкаВремени = ТекущаяДатаСеанса() - ТекущаяУниверсальнаяДата();
	
	ДатыСертификата = Новый Структура;
	ДатыСертификата.Вставить("ДатаНачала",    Сертификат.ДатаНачала    + ДобавкаВремени);
	ДатыСертификата.Вставить("ДатаОкончания", Сертификат.ДатаОкончания + ДобавкаВремени);
	
	Возврат ДатыСертификата;
	
КонецФункции

#КонецОбласти

#Область НоваяФормаРегламентированнойОтчетности

#Область ПолучениеТекущегоСостоянияОтправки

Функция ТребованияВОписи(Опись) Экспорт
	
	Запрос = Новый Запрос;
	Запрос.Текст = 
		"ВЫБРАТЬ РАЗРЕШЕННЫЕ
		|	ОписиВходящихДокументовИзНалоговыхОргановВходящиеДокументы.СсылкаНаОбъект КАК Требование
		|ИЗ
		|	Справочник.ОписиВходящихДокументовИзНалоговыхОрганов.ВходящиеДокументы КАК ОписиВходящихДокументовИзНалоговыхОргановВходящиеДокументы
		|ГДЕ
		|	ОписиВходящихДокументовИзНалоговыхОргановВходящиеДокументы.Ссылка = &Опись
		|	И ОписиВходящихДокументовИзНалоговыхОргановВходящиеДокументы.СсылкаНаОбъект.ВидДокумента В (ЗНАЧЕНИЕ(Перечисление.ВидыНалоговыхДокументов.ТребованиеОПредставленииДокументов), ЗНАЧЕНИЕ(Перечисление.ВидыНалоговыхДокументов.ТребованиеОПредставленииПоясненийКДекларацииНДС))";
	
	Запрос.УстановитьПараметр("Опись", Опись);
	Требования = Запрос.Выполнить().Выгрузить().ВыгрузитьКолонку("Требование");
	
	Возврат Требования; 
	
КонецФункции

// Текущее состояние отправки
//
// Параметры:
//  Ссылка					 - ДокументСсылка, СправочникСсылка - ссылка на объект, отправляемый в контролирующие органы.
//  КонтролирующийОрган		 - Строка или Перечисление.ТипыКонтролирующихОрганов
//  ДополнительныеПараметры	 - Структура:
//		Если содержит поле "ПолучатьОшибкиОтправки", то в результате возращаются поля НеотправленныеИзвещения и ЕстьКритическиеОшибки.
//		Если содержит поле "ПоказыватьПомеченныеНаУдаление", то анализируеются даже помеченные на удаление отправки.
//		Если содержит поле "ПолучатьДаты", то возвращаются даты смены состояния, иначе возврадается пустая дата.
//		Если содержит поле "Отправка", то возвращается информация по этой отправке(циклу обмена или отправке), иначе по последней.
// 
// Возвращаемое значение:
//	Структура с полями:
//	ТекущийЭтапОтправки - структура с полями:
//		ТекстНадписи - Строка - текст, отображаемый в панели отправки объекта
//		ТекстСтатуса - Строка - текст, отображаемый в колонке Состояние таблиц формы Отчетность
//		Дата - Дата - Дата текущего этапа. Возращается, если ДополнительныеПараметры содержат поле "ПолучатьДаты".
//		КомментарийКСостоянию
//		НаименованиеПротокола
//		Протокол	 - Ссылка на транспортное сообщение или отправку или Неопределено.
//		СостояниеСдачиОтчетности - ПеречислениеСсылка.СостояниеСдачиОтчетности
//		ЭтапПройден - Булево
//	НеотправленныеИзвещения - Структура, поля перечислены ниже. 
//		Возращается, если ДополнительныеПараметры содержат поле "ПолучатьОшибкиОтправки".
//			МассивСообщенийНеИмеющихПодтверждений - Массив
//			МассивНеотправленныхПодтверждений - Массив
//			ЕстьНеотправленныеИзвещения - Булево
//	ЕстьКритическиеОшибки - Булево.
//		Возращается, если ДополнительныеПараметры содержат поле "ПолучатьОшибкиОтправки".
//В случае ошибки или отсуствия данных, возвращается значение Неопределено
Функция ТекущееСостояниеОтправки(Знач Ссылка, Знач КонтролирующийОрган = "ФНС", ДополнительныеПараметры = Неопределено) Экспорт
	
	УстановитьПривилегированныйРежим(Истина);
	
	Попытка
		
		ТекущееСостояние = Новый Структура;
		ТекущееСостояние.Вставить("ТекущийЭтапОтправки");
		ТекущееСостояние.Вставить("НеотправленныеИзвещения");
		ТекущееСостояние.Вставить("ЕстьКритическиеОшибки");
		ТекущееСостояние.Вставить("ТаблицаЭтаповОтправки");
		ТекущееСостояние.Вставить("КонтролирующийОрган", КонтролирующийОрган);
		
		// Преобразуем контролирующий орган к нужному виду
		КонтролирующийОрган = ТипКонтролирующегоОргана(КонтролирующийОрган);
		
		// Вся таблица этапов
		ТаблицаЭтаповОтправки = ТаблицаЭтаповОтправки(Ссылка, КонтролирующийОрган, ДополнительныеПараметры);
		
		Если ТаблицаЭтаповОтправки = Неопределено Тогда
			Возврат Неопределено;
		КонецЕсли;
		
		// Текущий этап отправки (для прорисовки панели и определния статуса в списке отчетов)
		ТекущийЭтапОтправки = ТекущийЭтапОтправки(ТаблицаЭтаповОтправки);
		ТекущееСостояние.Вставить("ТекущийЭтапОтправки", ТекущийЭтапОтправки);
		
		ПолучатьОшибкиОтправки = ТипЗнч(ДополнительныеПараметры) = Тип("Структура") 
			И ДополнительныеПараметры.Свойство("ПолучатьОшибкиОтправки") 
			И ДополнительныеПараметры.ПолучатьОшибкиОтправки;
			
		Если ПолучатьОшибкиОтправки Тогда
			// Критические ошибки
			ТекущееСостояние.Вставить("ЕстьКритическиеОшибки", 
				ЭлектронныйДокументооборотСКонтролирующимиОрганами.ЕстьКритическиеСообщения(Ссылка, ДополнительныеПараметры));
			
			// Неотправленные или несозданные подтверждения
			ТекущееСостояние.Вставить("НеотправленныеИзвещения", НеотправленныеИзвещения(Ссылка, КонтролирующийОрган, ДополнительныеПараметры));
		КонецЕсли;
		
		ВозвращатьТаблицуЭтаповОтправки = ТипЗнч(ДополнительныеПараметры) = Тип("Структура") 
			И ДополнительныеПараметры.Свойство("ВозвращатьТаблицуЭтаповОтправки") 
			И ДополнительныеПараметры.ВозвращатьТаблицуЭтаповОтправки;
			
		Если ВозвращатьТаблицуЭтаповОтправки Тогда
			ТекущееСостояние.Вставить("ТаблицаЭтаповОтправки", ТаблицаЭтаповОтправки);
		КонецЕсли;
		
	Исключение
		
		ТекущееСостояние = Неопределено;
		ИнформацияОбОшибке = ИнформацияОбОшибке();
		ЗаписьЖурналаРегистрации(
			НСтр("ru = 'Получение текущего состояния отправки, критических ошибок и неотправленных извещений'", ОбщегоНазначения.КодОсновногоЯзыка()), 
			УровеньЖурналаРегистрации.Ошибка,,Ссылка,
			ПодробноеПредставлениеОшибки(ИнформацияОбОшибке));
		
	КонецПопытки;
		
	Возврат ТекущееСостояние;
		
КонецФункции

Функция ТекущийЭтапОтправки(ТаблицаЭтаповОтправки) Экспорт
	
	НомерЭтапа = ТаблицаЭтаповОтправки.Количество();
	
	// В таблице этапов определяем последний
	ТекущийЭтап = Неопределено;
	Пока НомерЭтапа >= 0 Цикл
		
		НомерЭтапа = НомерЭтапа - 1;
		
		ТекущийЭтап = ТаблицаЭтаповОтправки[НомерЭтапа];
		Если ТекущийЭтап.ЭтапПройден Тогда
			Прервать;
		Конецесли;
	
	КонецЦикла;
	
	// Строку таблицы значений последнего этапа преобразуем к структуре
	Если ТекущийЭтап <> Неопределено Тогда
		СтруктураТекущегоЭтапа = Новый Структура;
		Для каждого Колонка Из ТаблицаЭтаповОтправки.Колонки Цикл
			СтруктураТекущегоЭтапа.Вставить(Колонка.Имя, ТекущийЭтап[Колонка.Имя]);
		КонецЦикла;
		
		Возврат СтруктураТекущегоЭтапа;
	Иначе
		Возврат Неопределено;
	КонецЕсли;
	
КонецФункции

Функция НеотправленныеИзвещения(Знач ОтчетСсылка, Знач КонтролирующийОрган, ДополнительныеПараметры = Неопределено)
	
	МассивСообщенийНеИмеющихПодтверждений = Новый Массив;
	МассивНеотправленныхПодтверждений = Новый Массив;
	
	Результат = Новый Структура;
	Результат.Вставить("МассивСообщенийНеИмеющихПодтверждений", МассивСообщенийНеИмеющихПодтверждений);
	Результат.Вставить("МассивНеотправленныхПодтверждений", 	МассивНеотправленныхПодтверждений);
	Результат.Вставить("ЕстьНеотправленныеИзвещения",			Ложь);
	
	Если НЕ (КонтролирующийОрган = Перечисления.ТипыКонтролирующихОрганов.ФНС
			ИЛИ КонтролирующийОрган = Перечисления.ТипыКонтролирующихОрганов.ПФР
			ИЛИ КонтролирующийОрган = Перечисления.ТипыКонтролирующихОрганов.ФСГС) Тогда
			
		Возврат Результат;
			
	КонецЕсли;
	
	Если ОтчетСсылка <> Неопределено Тогда
		Ссылка = ОтчетСсылка.Ссылка;
		Если Не ЗначениеЗаполнено(Ссылка) Тогда
			Возврат Результат;
		КонецЕсли;
	Иначе
		Возврат Результат;
	КонецЕсли;
	
	Если ТипЗнч(ДополнительныеПараметры) = Тип("Структура")
		И ДополнительныеПараметры.Свойство("Отправка") Тогда
		ЦиклОбмена = ДополнительныеПараметры.Отправка;
	Иначе
		ЦиклОбмена = ДокументооборотСКОВызовСервера.ПолучитьПоследнийЦиклОбмена(ОтчетСсылка);
	КонецЕсли;
	
	ПоказыватьПомеченныеНаУдаление = ТипЗнч(ДополнительныеПараметры) = Тип("Структура")
		И ДополнительныеПараметры.Свойство("ПоказыватьПомеченныеНаУдаление")
		И ДополнительныеПараметры.ПоказыватьПомеченныеНаУдаление;
	
	Если ЗначениеЗаполнено(ЦиклОбмена) Тогда

		Сообщения = ПолучитьСообщенияЦиклаОбмена(ЦиклОбмена,,ПоказыватьПомеченныеНаУдаление);
		
		// определяем соответствие между типами сообщений-оснований и сообщений-подтверждений на них
		ТипыОснованийИПодтверждений = ЭлектронныйДокументооборотСКонтролирующимиОрганами.ТипыОснованийИПодтверждений(ЦиклОбмена);
		
		// перебираем соответствие подтверждений основаниям
		ОбменНапрямую = Неопределено;
		Для Каждого ТипОснованияИПодтверждения Из ТипыОснованийИПодтверждений Цикл
			
			ТипОснования = ТипОснованияИПодтверждения.Ключ;
			ТипПодтверждения = ТипОснованияИПодтверждения.Значение;
			
			// для каждого основания...
			СообщенияОснования = Сообщения.НайтиСтроки(Новый Структура("Тип", ТипОснования));
			Для Каждого СообщениеОснование Из СообщенияОснования Цикл
				
				// ... ищем подтверждение, и если не находим, то возвращаем Ложь
				СообщенияПодтверждения = Сообщения.НайтиСтроки(Новый Структура("Основание", СообщениеОснование.Ссылка));
				Если ТипОснования <> Перечисления.ТипыТранспортныхСообщений.ПисьмоВходящееПФР И НЕ ЭтоРассылкаПФР(СообщениеОснование) Тогда
					Если СообщенияПодтверждения.Количество() = 0 Тогда
						МассивСообщенийНеИмеющихПодтверждений.Добавить(СообщениеОснование.Ссылка);
					Иначе
						// формируем массив неотправленных подтверждений
						Для каждого СообщениеПодтверждения Из СообщенияПодтверждения Цикл
							Если СообщениеПодтверждения.Статус <> Перечисления.СтатусыПисем.Отправленное 
								И СообщениеПодтверждения.Статус <> Перечисления.СтатусыПисем.Полученное Тогда
								МассивНеотправленныхПодтверждений.Добавить(СообщениеПодтверждения.Ссылка);
							КонецЕсли;
						КонецЦикла;  
					КонецЕсли;
				КонецЕсли;
				
			КонецЦикла;
			
		КонецЦикла;
	КонецЕсли;
	
	Результат.Вставить("МассивСообщенийНеИмеющихПодтверждений", МассивСообщенийНеИмеющихПодтверждений);
	Результат.Вставить("МассивНеотправленныхПодтверждений", 	МассивНеотправленныхПодтверждений);
	Результат.Вставить("ЕстьНеотправленныеИзвещения", 			МассивСообщенийНеИмеющихПодтверждений.Количество() > 0 
																	ИЛИ МассивНеотправленныхПодтверждений.Количество() > 0);
	
	Возврат Результат;
	
КонецФункции

Функция ТаблицаЭтаповОтправки(Знач Ссылка, Знач КонтролирующийОрган = "ФНС", ДополнительныеПараметры = Неопределено) Экспорт
	
	// Преобразуем контролирующий орган к нужному виду
	ТипКонтролирующегоОргана = ТипКонтролирующегоОргана(КонтролирующийОрган);
	
	// Заявление абонента на подключение к 1С-Отчетности
	Если ТипЗнч(Ссылка) = Тип("ДокументСсылка.ЗаявлениеАбонентаСпецоператораСвязи") Тогда
		
		 ТаблицаЭтаповОтправки = ТаблицаЭтаповОтправкиЗаявленияАбонентаСпецоператораСвязи(Ссылка);
	
	// Документ реализации полномочий
	ИначеЕсли ТипЗнч(Ссылка) = Тип("СправочникСсылка.ДокументыРеализацииПолномочийНалоговыхОрганов") Тогда
		
		 ТаблицаЭтаповОтправки = ТаблицаЭтаповОтправкиДокументаРеализацииПолномочийНалоговыхОрганов(Ссылка, ДополнительныеПараметры);
			
	// Переписка
	ИначеЕсли ТипЗнч(Ссылка) = Тип("СправочникСсылка.ПерепискаСКонтролирующимиОрганами") Тогда
		
		ТаблицаЭтаповОтправки = ТаблицаЭтаповОтправкиПерепискиСКонтролирующимиОрганами(Ссылка, ДополнительныеПараметры);
				
	// Макеты ПД, Заявление о назначении пенсии
	ИначеЕсли ТипЗнч(Ссылка) = Тип("СправочникСсылка.МакетыПенсионныхДел")
		ИЛИ ТипЗнч(Ссылка) = Тип("СправочникСсылка.ЗаявлениеОНазначенииПенсии") Тогда
		
		ТаблицаЭтаповОтправки = ТаблицаЭтаповОтправкиМакетаПенсионногоДела(Ссылка, ДополнительныеПараметры);	
				
	// ЕГРЮЛ, ЕГРИП
	ИначеЕсли ТипЗнч(Ссылка) = Тип("ДокументСсылка.ЗапросНаВыпискуИзЕГРЮЛ_ЕГРИП") Тогда
		
		ТаблицаЭтаповОтправки = ТаблицаЭтаповОтправкиЗапросаНаВыпискуИзЕГРЮЛ_ЕГРИП(Ссылка, ДополнительныеПараметры);
				
	// Запрос ИОН
	ИначеЕсли ТипЗнч(Ссылка) = Тип("ДокументСсылка.ЗапросНаИнформационноеОбслуживаниеНалогоплательщика") Тогда
		
		ТаблицаЭтаповОтправки = ТаблицаЭтаповОтправкиЗапросаИОН(Ссылка, ДополнительныеПараметры);
		
	// Входящие описи
	ИначеЕсли ТипЗнч(Ссылка) = Тип("СправочникСсылка.ОписиВходящихДокументовИзНалоговыхОрганов") Тогда
		
		ТаблицаЭтаповОтправки = ТаблицаЭтаповОтправкиОписиВходящихДокументовВНалоговыеОрганы(Ссылка, ДополнительныеПараметры);
			
	// Ответы на требование
	ИначеЕсли ТипЗнч(Ссылка) = Тип("СправочникСсылка.ОписиИсходящихДокументовВНалоговыеОрганы")
		ИЛИ ТипЗнч(Ссылка) = Тип("ДокументСсылка.ПоясненияКДекларацииПоНДС") Тогда
		
		ТаблицаЭтаповОтправки = ТаблицаЭтаповОтправкиОтветовНаТребованияФНС(Ссылка, ДополнительныеПараметры);
			
	// Запрос ИОС
	ИначеЕсли ТипЗнч(Ссылка) = Тип("ДокументСсылка.ЗапросНаИнформационноеОбслуживаниеСтрахователя") Тогда
		
		ТаблицаЭтаповОтправки = ТаблицаЭтаповОтправкиЗапросаНаИнформационноеОбслуживаниеСтрахователя(Ссылка, ДополнительныеПараметры);

	// ФНС
	ИначеЕсли ТипКонтролирующегоОргана = Перечисления.ТипыКонтролирующихОрганов.ФНС Тогда
		
		ТаблицаЭтаповОтправки = ТаблицаЭтаповОтправкиФНС(Ссылка, ДополнительныеПараметры);
		
	// ПФР
	ИначеЕсли ТипКонтролирующегоОргана = Перечисления.ТипыКонтролирующихОрганов.ПФР Тогда
		
		ТаблицаЭтаповОтправки = ТаблицаЭтаповОтправкиПФР(Ссылка, ДополнительныеПараметры);
		
	// Росстат
	ИначеЕсли ТипКонтролирующегоОргана = Перечисления.ТипыКонтролирующихОрганов.ФСГС Тогда
		
		ТаблицаЭтаповОтправки = ТаблицаЭтаповОтправкиРосстат(Ссылка, ДополнительныеПараметры);
			
	// ФСС
	ИначеЕсли ТипКонтролирующегоОргана = Перечисления.ТипыКонтролирующихОрганов.ФСС Тогда
	
		ТаблицаЭтаповОтправки = ТаблицаЭтаповОтправкиФСС(Ссылка, ДополнительныеПараметры);

	// ФСРАР
	ИначеЕсли ТипКонтролирующегоОргана = Перечисления.ТипыКонтролирующихОрганов.ФСРАР Тогда
		
		ТаблицаЭтаповОтправки = ТаблицаЭтаповОтправкиФСРАР(Ссылка, ДополнительныеПараметры);
		
	// РПН
	ИначеЕсли ТипКонтролирующегоОргана = Перечисления.ТипыКонтролирующихОрганов.РПН Тогда
		
		ТаблицаЭтаповОтправки = ТаблицаЭтаповОтправкиРПН(Ссылка, ДополнительныеПараметры);
		
	// ФТС
	ИначеЕсли ТипКонтролирующегоОргана = Перечисления.ТипыКонтролирующихОрганов.ФТС Тогда
		
		ТаблицаЭтаповОтправки = ТаблицаЭтаповОтправкиФТС(Ссылка, ДополнительныеПараметры);
		
	// Банк России
	ИначеЕсли ТипКонтролирующегоОргана = Перечисления.ТипыКонтролирующихОрганов.БанкРоссии Тогда
		
		ТаблицаЭтаповОтправки = ТаблицаЭтаповОтправкиБанкРоссии(Ссылка, ДополнительныеПараметры);
		
	
	// Минобороны
	ИначеЕсли ТипКонтролирующегоОргана = Перечисления.ТипыКонтролирующихОрганов.Минобороны Тогда
		
		ТаблицаЭтаповОтправки = ТаблицаЭтаповОтправкиМинобороны(Ссылка, ДополнительныеПараметры);
		
	КонецЕсли;
	
	Возврат ТаблицаЭтаповОтправки;
	
КонецФункции

#Область ФормированиеТаблицЭтаповОтправки

Функция ТаблицаЭтаповОтправкиЗаявленияАбонентаСпецоператораСвязи(Ссылка);
	
	ТаблицаЭтаповОтправки = ЗаготовкаТаблицыЭтаповОтправки();
	
	СтрокаЭтапа = ТаблицаЭтаповОтправки.Добавить();
	СтрокаЭтапа.ТекстНадписи				= НСтр("ru = 'Не отправлено'");
	СтрокаЭтапа.ТекстСтатуса				= НСтр("ru = 'Не отправлено'");
	СтрокаЭтапа.Дата						= Дата(1,1,1);
	СтрокаЭтапа.КомментарийКСостоянию		= "";
	СтрокаЭтапа.НаименованиеПротокола		= "";
	СтрокаЭтапа.Протокол					= "";
	СтрокаЭтапа.СостояниеСдачиОтчетности	= Перечисления.СостояниеСдачиОтчетности.ДокументооборотНеНачат;
	СтрокаЭтапа.ЭтапПройден					= Истина;
	
	СтрокаЭтапа = ТаблицаЭтаповОтправки.Добавить();
	СтрокаЭтапа.ТекстНадписи				= НСтр("ru = 'Отправлено оператору'");
	СтрокаЭтапа.ТекстСтатуса				= НСтр("ru = 'Отправлено оператору'");
	СтрокаЭтапа.Дата						= Ссылка.ДатаОтправкиЗаявления;
	СтрокаЭтапа.КомментарийКСостоянию		= НСтр("ru = 'Ожидается результат обработки'");
	СтрокаЭтапа.НаименованиеПротокола		= "";
	СтрокаЭтапа.Протокол					= "";
	СтрокаЭтапа.СостояниеСдачиОтчетности	= Перечисления.СостояниеСдачиОтчетности.ДокументооборотНачат;
	СтрокаЭтапа.ЭтапПройден					= Ссылка.Статус <> Перечисления.СтатусыЗаявленияАбонентаСпецоператораСвязи.Подготовлено
												И ЗначениеЗаполнено(Ссылка.Статус);

	ЕстьКомментарий = НЕ ПустаяСтрока(Ссылка.СтатусКомментарий);
	Если Ссылка.Статус = Перечисления.СтатусыЗаявленияАбонентаСпецоператораСвязи.Отклонено Тогда
		
		Если Ссылка.НастройкаЗавершена Тогда
			Текст = НСтр("ru = 'Отклонено'");
		Иначе
			Текст = НСтр("ru = 'Отклонено, настройка не завершена'");
		КонецЕсли;
		
		СтрокаЭтапа = ТаблицаЭтаповОтправки.Добавить();
		СтрокаЭтапа.ТекстНадписи				= Текст;
		СтрокаЭтапа.ТекстСтатуса				= Текст;
		СтрокаЭтапа.Дата						= Ссылка.ДатаПолученияОтвета;
		СтрокаЭтапа.КомментарийКСостоянию		= НСтр("ru = 'Исправьте ошибки и сформируйте заявление еще раз'");
		СтрокаЭтапа.НаименованиеПротокола		= ?(ЕстьКомментарий, НСтр("ru = 'Протокол ошибок'"), "");
		СтрокаЭтапа.Протокол					= "";
		СтрокаЭтапа.СостояниеСдачиОтчетности	= Перечисления.СостояниеСдачиОтчетности.ОтрицательныйРезультатДокументооборота;
		СтрокаЭтапа.ЭтапПройден					= Истина;
		
	ИначеЕсли НЕ Ссылка.НастройкаЭДОЗавершена И ЗначениеЗаполнено(Ссылка.НастройкиЭДО)
		И Ссылка.Статус = Перечисления.СтатусыЗаявленияАбонентаСпецоператораСвязи.Одобрено
		И Ссылка.НастройкаЗавершена Тогда
		
		Текст = НСтр("ru = 'Одобрено, настройка 1С-ЭДО не завершена'");
		
		СтрокаЭтапа = ТаблицаЭтаповОтправки.Добавить();
		СтрокаЭтапа.ТекстНадписи				= Текст;
		СтрокаЭтапа.ТекстСтатуса				= Текст;
		СтрокаЭтапа.Дата						= Ссылка.ДатаПолученияОтвета;
		СтрокаЭтапа.КомментарийКСостоянию		= НСтр("ru = '       Для завершения настройки 1С-ЭДО нажмите кнопку ""Обновить""'");
		СтрокаЭтапа.НаименованиеПротокола		= "";
		СтрокаЭтапа.Протокол					= "";
		СтрокаЭтапа.СостояниеСдачиОтчетности	= Перечисления.СостояниеСдачиОтчетности.ПоложительныйРезультатДокументооборота;
		СтрокаЭтапа.ЭтапПройден					= Истина;

	Иначе
		Если ЕстьКомментарий Тогда
			Информация = Ссылка.СтатусКомментарий;
		Иначе
			Информация = "";
		КонецЕсли;
		
		ЭтапПройден = Ссылка.Статус = Перечисления.СтатусыЗаявленияАбонентаСпецоператораСвязи.Одобрено;
		
		Если Ссылка.НастройкаЗавершена Тогда
			Текст = НСтр("ru = 'Одобрено'");
		Иначе
			Текст = НСтр("ru = 'Одобрено, настройка не завершена'");
		КонецЕсли;
		
		СтрокаЭтапа = ТаблицаЭтаповОтправки.Добавить();
		СтрокаЭтапа.ТекстНадписи				= Текст;
		СтрокаЭтапа.ТекстСтатуса				= Текст;
		СтрокаЭтапа.Дата						= ?(ЭтапПройден, Ссылка.ДатаПолученияОтвета, Дата(1,1,1));
		СтрокаЭтапа.КомментарийКСостоянию		= ?(НЕ Ссылка.НастройкаЗавершена, НСтр("ru = '       Для завершения настройки нажмите кнопку ""Обновить""'"), "");
		СтрокаЭтапа.НаименованиеПротокола		= ?(ЕстьКомментарий И Ссылка.НастройкаЗавершена, НСтр("ru = 'Протокол'"), "");
		СтрокаЭтапа.Протокол					= "";
		СтрокаЭтапа.СостояниеСдачиОтчетности	= Перечисления.СостояниеСдачиОтчетности.ПоложительныйРезультатДокументооборота;
		СтрокаЭтапа.ЭтапПройден					= ЭтапПройден;
		
	КонецЕсли;
	
	Возврат ТаблицаЭтаповОтправки;

КонецФункции

Функция ТаблицаЭтаповОтправкиОписиВходящихДокументовВНалоговыеОрганы(СсылкаНаОтчет, ДополнительныеПараметры)
	
	ТаблицаЭтаповОтправки = ЗаготовкаТаблицыЭтаповОтправки();
	
	СтрокаЭтапа = ТаблицаЭтаповОтправки.Добавить();
	СтрокаЭтапа.ТекстНадписи				= НСтр("ru = 'Получено'");
	СтрокаЭтапа.ТекстСтатуса				= НСтр("ru = 'Получено'");
	СтрокаЭтапа.Дата						= Дата(1,1,1);
	СтрокаЭтапа.КомментарийКСостоянию		= "";
	СтрокаЭтапа.НаименованиеПротокола		= "";
	СтрокаЭтапа.Протокол					= "";
	СтрокаЭтапа.СостояниеСдачиОтчетности	= Перечисления.СостояниеСдачиОтчетности.ОтправленоИзКонтролирующегоОргана;
	СтрокаЭтапа.ЭтапПройден					= Истина;

	Возврат ТаблицаЭтаповОтправки;

КонецФункции

Функция ТаблицаЭтаповОтправкиДокументаРеализацииПолномочийНалоговыхОрганов(СсылкаНаОтчет, ДополнительныеПараметры)
	
	НеПолучатьДаты = НЕ (ТипЗнч(ДополнительныеПараметры) = Тип("Структура")
		И ДополнительныеПараметры.Свойство("ПолучатьДаты")
		И ДополнительныеПараметры.ПолучатьДаты);
		
	ПоказыватьПомеченныеНаУдаление = ТипЗнч(ДополнительныеПараметры) = Тип("Структура")
		И ДополнительныеПараметры.Свойство("ПоказыватьПомеченныеНаУдаление")
		И ДополнительныеПараметры.ПоказыватьПомеченныеНаУдаление;
		
	ТаблицаЭтаповОтправки = ЗаготовкаТаблицыЭтаповОтправки();
	
	// У описи не может быть пустого цикла обмена
	// получаем список сообщений
	Если ТипЗнч(ДополнительныеПараметры) = Тип("Структура")
		И ДополнительныеПараметры.Свойство("Отправка") Тогда
		ЦиклОбмена = ДополнительныеПараметры.Отправка;
	Иначе
		ЦиклОбмена = ДокументооборотСКОВызовСервера.ПолучитьПоследнийЦиклОбмена(СсылкаНаОтчет);
	КонецЕсли;
	
	Опись = ДокументооборотСКОВызовСервера.ПолучитьОписьВходящихДокументовПоТребованию(СсылкаНаОтчет);
	
	СообщенияЦикла = ПолучитьСообщенияЦиклаОбмена(ЦиклОбмена,,ПоказыватьПомеченныеНаУдаление,Истина);
	
	// выделяем основные типы сообщений
	СтрДокументНО 					= СообщенияЦикла.НайтиСтроки(Новый Структура("Тип", Перечисления.ТипыТранспортныхСообщений.ДокументНО));
	СтрИзвещениеДокументНП 			= СообщенияЦикла.НайтиСтроки(Новый Структура("Тип, Статус", Перечисления.ТипыТранспортныхСообщений.ИзвещениеДокументНП, Перечисления.СтатусыПисем.Отправленное));
	СтрРезультатПриемаДокументНП 	= СообщенияЦикла.НайтиСтроки(Новый Структура("Тип", Перечисления.ТипыТранспортныхСообщений.РезультатПриемаДокументНП));
	СтрИзвещениеРезультатПриемаНО 	= СообщенияЦикла.НайтиСтроки(Новый Структура("Тип", Перечисления.ТипыТранспортныхСообщений.ИзвещениеРезультатПриемаНО));
	
	Если НеПолучатьДаты Тогда
		ДатаОтправкиИзФНС 	= Дата(1,1,1);
		ДатаОтказаВПриеме 	= Дата(1,1,1);
		ДатаПриемаОписиНами = Дата(1,1,1);
	Иначе
		ДатаОтправкиИзФНС 	= Опись.ДатаОтправки;
		ДатаОтказаВПриеме 	= ?(СтрРезультатПриемаДокументНП.Количество() > 0, 	СтрРезультатПриемаДокументНП[0].ДатаТранспорта,	Дата(1,1,1));
		ДатаПриемаОписиНами = ?(СтрРезультатПриемаДокументНП.Количество() > 0, 	СтрРезультатПриемаДокументНП[0].Дата, 			Дата(1,1,1));
	КонецЕсли;
	
	СтрокаЭтапа = ТаблицаЭтаповОтправки.Добавить();
	СтрокаЭтапа.ТекстНадписи				= НСтр("ru = 'Отправлено'");
	СтрокаЭтапа.ТекстСтатуса				= НСтр("ru = 'Отправлено'");
	СтрокаЭтапа.Дата						= ДатаОтправкиИзФНС;
	СтрокаЭтапа.КомментарийКСостоянию		= "";
	СтрокаЭтапа.НаименованиеПротокола		= "";
	СтрокаЭтапа.Протокол					= "";
	СтрокаЭтапа.СостояниеСдачиОтчетности	= Перечисления.СостояниеСдачиОтчетности.ОтправленоИзКонтролирующегоОргана;
	СтрокаЭтапа.ЭтапПройден					= Истина;
	
	// Случай, когда в Описи нет требований
	Если СтрРезультатПриемаДокументНП.Количество() > 0 И ПолучитьМассивТребованийПоОписиВходящихДокументов(Опись).Количество() = 0 Тогда
		
		СтрокаЭтапа = ТаблицаЭтаповОтправки.Добавить();
		СтрокаЭтапа.ТекстНадписи				= НСтр("ru = 'Получено'");
		СтрокаЭтапа.ТекстСтатуса				= НСтр("ru = 'Получено'");
		СтрокаЭтапа.Дата						= ДатаПриемаОписиНами;
		СтрокаЭтапа.КомментарийКСостоянию		= "";
		СтрокаЭтапа.НаименованиеПротокола		= НСтр("ru = 'Квитанция о приеме'");
		СтрокаЭтапа.Протокол					= ?(СтрРезультатПриемаДокументНП.Количество() > 0, СтрРезультатПриемаДокументНП[0].Ссылка, "");
		СтрокаЭтапа.СостояниеСдачиОтчетности	= Перечисления.СостояниеСдачиОтчетности.ПриемПодтвержден;
		СтрокаЭтапа.ЭтапПройден					= Истина;

	Иначе
		
		ЭтоТребованиеОПредставленииДокументов = 
			СсылкаНаОтчет.ВидДокумента = Перечисления.ВидыНалоговыхДокументов.ТребованиеОПредставленииДокументов;
		ЭтоТребованиеОПредставленииПоясненийКДекларацииНДС = 
			СсылкаНаОтчет.ВидДокумента = Перечисления.ВидыНалоговыхДокументов.ТребованиеОПредставленииПоясненийКДекларацииНДС;
			
		ЭтоТребование = ЭтоТребованиеОПредставленииДокументов 
			ИЛИ ЭтоТребованиеОПредставленииПоясненийКДекларацииНДС;
		
		Требования = Новый Массив;
		Требования.Добавить(СсылкаНаОтчет);
		Результат = ПолучитьКоличествоОтветовНаТребования(Требования);
		КоличествоОтветов = Результат.Получить(СсылкаНаОтчет);
		
		// Формируем строку с количеством ответов
		Если КоличествоОтветов = 0 ИЛИ КоличествоОтветов = Неопределено Тогда
			КоличествоОтветовПредставление = "";
		Иначе
			
			Если ЭтоТребованиеОПредставленииПоясненийКДекларацииНДС Тогда
				КоличествоОтветовПредставление = НСтр("ru = 'Пояснения (%Ответы%)'");
			Иначе
				КоличествоОтветовПредставление = НСтр("ru = 'Ответы (%Ответы%)'");
			КонецЕсли;
			
			КоличествоОтветовПредставление = СтрЗаменить(КоличествоОтветовПредставление, "%Ответы%", Строка(КоличествоОтветов));
		КонецЕсли;
		
		// Формируем текст предупреждением
		Если ЭтоТребование Тогда
			Если РегламентированнаяОтчетностьПереопределяемый.ЭтоЮридическоеЛицо(СсылкаНаОтчет.Организация) Тогда
				КомментарийКСостоянию = НСтр("ru = 'Если прием требования не будет подтвержден в течение 6 рабочих дней с момента отправки, то ФНС вправе заблокировать расчетный счет организации'");
			Иначе
				КомментарийКСостоянию = НСтр("ru = 'Если прием требования не будет подтвержден в течение 1 рабочего дня с момента отправки из ФНС, то документы будут высланы по почте'");
			КонецЕсли;
		Иначе
			МассивТребований = ТребованияВОписи(Опись);
			Если МассивТребований.Количество() > 0 Тогда
				КомментарийКСостоянию = НСтр("ru = 'Этот документ получен вместе с требованием. Подтверждение приема происходит по всем документам одновременно'");
			Иначе
				КомментарийКСостоянию = НСтр("ru = 'Если прием документов не будет подтвержден в течение 1 рабочего дня с момента отправки из ФНС, то документы будут высланы по почте'");
			КонецЕсли;
		КонецЕсли;

		Если СтрРезультатПриемаДокументНП.Количество() > 0 Тогда
			ЧислоСообщений = СтрРезультатПриемаДокументНП.Количество();
			Если ЧислоСообщений = 1 Тогда
				РезультатПриема = СтрРезультатПриемаДокументНП[0];
			Иначе
				// если протоколов несколько, то ориентируемся на наиболее свежий из них
				НаиболееПозднийРезультат = Неопределено;
				ДатаНаиболееПозднегоРезультата = '00010101';
				Для Каждого РезультатПриема Из СтрРезультатПриемаДокументНП Цикл
					ДатаРезультатаПриема = РезультатПриема.Дата;
					Если ДатаРезультатаПриема >= ДатаНаиболееПозднегоРезультата Тогда
						ДатаНаиболееПозднегоРезультата = ДатаРезультатаПриема;
						НаиболееПозднийРезультат = РезультатПриема;
					КонецЕсли;
				КонецЦикла;
				РезультатПриема = НаиболееПозднийРезультат;
			КонецЕсли;
			
			Если РезультатПриема <> Неопределено И РезультатПриема.Ссылка.ПротоколСОшибкой Тогда
		
				СтрокаЭтапа = ТаблицаЭтаповОтправки.Добавить();
				СтрокаЭтапа.ТекстНадписи				= НСтр("ru = 'Отказано в приеме'");
				СтрокаЭтапа.ТекстСтатуса				= НСтр("ru = 'Отказано в приеме'");
				СтрокаЭтапа.Дата						= ДатаОтказаВПриеме;
				СтрокаЭтапа.КомментарийКСостоянию		= "";
				СтрокаЭтапа.НаименованиеПротокола		= НСтр("ru = 'Уведомление об отказе'");
				СтрокаЭтапа.КоличествоОтветов			= КоличествоОтветовПредставление;
				СтрокаЭтапа.Протокол					= РезультатПриема.Ссылка;
				СтрокаЭтапа.СостояниеСдачиОтчетности	= Перечисления.СостояниеСдачиОтчетности.ОтрицательныйРезультатДокументооборота;
				СтрокаЭтапа.ЭтапПройден					= СтрРезультатПриемаДокументНП.Количество() > 0;

			Иначе
				
				Если ЭтоТребование Тогда
					ТекстНадписи = НСтр("ru = 'Прием подтвержден'");
				Иначе
					ТекстНадписи = НСтр("ru = 'Получено'");
				КонецЕсли;
				
				СтрокаЭтапа = ТаблицаЭтаповОтправки.Добавить();
				СтрокаЭтапа.ТекстНадписи				= ТекстНадписи;
				СтрокаЭтапа.ТекстСтатуса				= ТекстНадписи;
				СтрокаЭтапа.Дата						= ДатаПриемаОписиНами;
				СтрокаЭтапа.КомментарийКСостоянию		= "";
				СтрокаЭтапа.НаименованиеПротокола		= НСтр("ru = 'Квитанция о приеме'");
				СтрокаЭтапа.КоличествоОтветов			= КоличествоОтветовПредставление;
				СтрокаЭтапа.Протокол					= ?(СтрРезультатПриемаДокументНП.Количество() > 0, СтрРезультатПриемаДокументНП[0].Ссылка, "");
				СтрокаЭтапа.СостояниеСдачиОтчетности	= Перечисления.СостояниеСдачиОтчетности.ПриемПодтвержден;
				СтрокаЭтапа.ЭтапПройден					= СтрРезультатПриемаДокументНП.Количество() > 0;
				
			КонецЕсли;
		Иначе
			
			// Случай, когда в Описи есть требования
			СтрокаЭтапа = ТаблицаЭтаповОтправки.Добавить();
			СтрокаЭтапа.ТекстНадписи				= НСтр("ru = 'Подтвердите прием'");
			СтрокаЭтапа.ТекстСтатуса				= НСтр("ru = 'Подтвердите прием'");
			СтрокаЭтапа.Дата						= Дата(1,1,1);
			СтрокаЭтапа.КомментарийКСостоянию		= КомментарийКСостоянию;
			СтрокаЭтапа.НаименованиеПротокола		= "";
			СтрокаЭтапа.КоличествоОтветов			= КоличествоОтветовПредставление;
			СтрокаЭтапа.Протокол					= "";
			СтрокаЭтапа.СостояниеСдачиОтчетности	= Перечисления.СостояниеСдачиОтчетности.ТребуетсяПодтверждениеПриема;
			СтрокаЭтапа.ЭтапПройден					= Истина;
			
		КонецЕсли;
				
	КонецЕсли;
	
	Возврат ТаблицаЭтаповОтправки;
		
КонецФункции

Функция ТаблицаЭтаповОтправкиЗапросаИОН(СсылкаНаОтчет, ДополнительныеПараметры)
	
	НеПолучатьДаты = НЕ (ТипЗнч(ДополнительныеПараметры) = Тип("Структура")
		И ДополнительныеПараметры.Свойство("ПолучатьДаты")
		И ДополнительныеПараметры.ПолучатьДаты);
		
	ПоказыватьПомеченныеНаУдаление = ТипЗнч(ДополнительныеПараметры) = Тип("Структура")
		И ДополнительныеПараметры.Свойство("ПоказыватьПомеченныеНаУдаление")
		И ДополнительныеПараметры.ПоказыватьПомеченныеНаУдаление;
	
	ТаблицаЭтаповОтправки = ЗаготовкаТаблицыЭтаповОтправки();
	 
	СтрокаЭтапа = ТаблицаЭтаповОтправки.Добавить();
	СтрокаЭтапа.ТекстНадписи				= НСтр("ru = 'Не отправлено'");
	СтрокаЭтапа.ТекстСтатуса				= НСтр("ru = 'Не отправлено'");
	СтрокаЭтапа.Дата						= Дата(1,1,1);
	СтрокаЭтапа.КомментарийКСостоянию		= "";
	СтрокаЭтапа.НаименованиеПротокола		= "";
	СтрокаЭтапа.Протокол					= "";
	СтрокаЭтапа.СостояниеСдачиОтчетности	= Перечисления.СостояниеСдачиОтчетности.ДокументооборотНеНачат;
	СтрокаЭтапа.ЭтапПройден					= Истина;

	// получаем список сообщений
	Если ТипЗнч(ДополнительныеПараметры) = Тип("Структура")
		И ДополнительныеПараметры.Свойство("Отправка") Тогда
		ЦиклОбмена = ДополнительныеПараметры.Отправка;
	Иначе
		ЦиклОбмена = ДокументооборотСКОВызовСервера.ПолучитьПоследнийЦиклОбмена(СсылкаНаОтчет);
	КонецЕсли;
	
	Если НЕ ЗначениеЗаполнено(ЦиклОбмена) Тогда
		Возврат ТаблицаЭтаповОтправки;
	КонецЕсли;

	СообщенияЦикла = ПолучитьСообщенияЦиклаОбмена(ЦиклОбмена,,ПоказыватьПомеченныеНаУдаление,Истина);
	
	// выделяем основные типы сообщений
	СтрЗапросыНП 						= СообщенияЦикла.НайтиСтроки(Новый Структура("Тип", Перечисления.ТипыТранспортныхСообщений.ЗапросНП));
	СтрПодтвержденияЗапросНО 			= СообщенияЦикла.НайтиСтроки(Новый Структура("Тип", Перечисления.ТипыТранспортныхСообщений.ПодтверждениеЗапросНО));
	СтрИзвещенияПодтверждениеНП 		= СообщенияЦикла.НайтиСтроки(Новый Структура("Тип", Перечисления.ТипыТранспортныхСообщений.ИзвещениеПодтверждениеНП));
	СтрИзвещенияЗапросНО 				= СообщенияЦикла.НайтиСтроки(Новый Структура("Тип", Перечисления.ТипыТранспортныхСообщений.ИзвещениеЗапросНО));
	СтрРезультатыПриемаЗапросНО 		= СообщенияЦикла.НайтиСтроки(Новый Структура("Тип", Перечисления.ТипыТранспортныхСообщений.РезультатПриемаЗапросНО));
	СтрИзвещенияРезультатПриемаНП 		= СообщенияЦикла.НайтиСтроки(Новый Структура("Тип", Перечисления.ТипыТранспортныхСообщений.ИзвещениеРезультатПриемаНП));
	СтрРезультатыОбработкиЗапросНО 		= СообщенияЦикла.НайтиСтроки(Новый Структура("Тип", Перечисления.ТипыТранспортныхСообщений.РезультатОбработкиЗапросНО));
	СтрИзвещенияРезультатОбработкиНП 	= СообщенияЦикла.НайтиСтроки(Новый Структура("Тип", Перечисления.ТипыТранспортныхСообщений.ИзвещениеРезультатОбработкиНП));
	
	// Если статус первичного - не отправлено, тогда выходим 
	Если СтрЗапросыНП.Количество() > 0 И СтрЗапросыНП[0].Статус <> Перечисления.СтатусыПисем.Отправленное Тогда
		Возврат ТаблицаЭтаповОтправки;
	КонецЕсли;
	
	СтрокаЭтапа = ТаблицаЭтаповОтправки.Добавить();
	СтрокаЭтапа.ТекстНадписи				= НСтр("ru = 'Отправлено оператору'");
	СтрокаЭтапа.ТекстСтатуса				= НСтр("ru = 'Отправлено оператору'");
	СтрокаЭтапа.Дата						= ДатаОтправки(СтрЗапросыНП, НеПолучатьДаты);;
	СтрокаЭтапа.КомментарийКСостоянию		= НСтр("ru = 'Ожидается подтверждение даты отправки'");
	СтрокаЭтапа.НаименованиеПротокола		= "";
	СтрокаЭтапа.Протокол					= "";
	СтрокаЭтапа.СостояниеСдачиОтчетности	= Перечисления.СостояниеСдачиОтчетности.ДокументооборотНачат;
	СтрокаЭтапа.ЭтапПройден					= СтрЗапросыНП.Количество() > 0;
	
	СтрокаЭтапа = ТаблицаЭтаповОтправки.Добавить();
	СтрокаЭтапа.ТекстНадписи				= НСтр("ru = 'Отправлено в ФНС'");
	СтрокаЭтапа.ТекстСтатуса				= НСтр("ru = 'Отправлено в ФНС'");
	СтрокаЭтапа.Дата						= ДатаИзОтвета(СтрПодтвержденияЗапросНО, НеПолучатьДаты);;
	СтрокаЭтапа.КомментарийКСостоянию		= НСтр("ru = 'Ожидается прием запроса в обработку'");
	СтрокаЭтапа.НаименованиеПротокола		= НСтр("ru = 'Подтверждение даты отправки'");
	СтрокаЭтапа.Протокол					= ?(СтрПодтвержденияЗапросНО.Количество() > 0, СтрПодтвержденияЗапросНО[0].Ссылка, "");
	СтрокаЭтапа.СостояниеСдачиОтчетности	= Перечисления.СостояниеСдачиОтчетности.ДокументооборотНачат;
	СтрокаЭтапа.ЭтапПройден					= СтрПодтвержденияЗапросНО.Количество() > 0;
	
	// определяем наиболее свежее сообщение с результатом приема
	ПоследнийРезультатПриема = Неопределено;
	Для Каждого СтрРезультатПриемаЗапросНО Из СтрРезультатыПриемаЗапросНО Цикл
		Если ПоследнийРезультатПриема = Неопределено ИЛИ СтрРезультатПриемаЗапросНО.Дата > ПоследнийРезультатПриема.Дата Тогда
			ПоследнийРезультатПриема = СтрРезультатПриемаЗапросНО;
		КонецЕсли;
	КонецЦикла;
	
	// если результат приема отрицательный, то выводим соответствующее сообщение
	Если ПоследнийРезультатПриема <> Неопределено И ПоследнийРезультатПриема.ПротоколСОшибкой Тогда
		
		СтрокаЭтапа = ТаблицаЭтаповОтправки.Добавить();
		СтрокаЭтапа.ТекстНадписи				= НСтр("ru = 'Отказано'");
		СтрокаЭтапа.ТекстСтатуса				= НСтр("ru = 'Отказано'");
		СтрокаЭтапа.Дата						= ДатаИзОтвета(СтрРезультатыПриемаЗапросНО, НеПолучатьДаты);
		СтрокаЭтапа.КомментарийКСостоянию		= НСтр("ru = 'Исправьте ошибки и отправьте новый запрос'");
		СтрокаЭтапа.НаименованиеПротокола		= НСтр("ru = 'Уведомление об отказе'");
		СтрокаЭтапа.Протокол					= ?(СтрРезультатыПриемаЗапросНО.Количество() > 0, СтрРезультатыПриемаЗапросНО[0].Ссылка, "");
		СтрокаЭтапа.СостояниеСдачиОтчетности	= Перечисления.СостояниеСдачиОтчетности.ОтрицательныйРезультатДокументооборота;
		СтрокаЭтапа.ЭтапПройден					= СтрРезультатыПриемаЗапросНО.Количество() > 0;
		
	Иначе
		
		СтрокаЭтапа = ТаблицаЭтаповОтправки.Добавить();
		СтрокаЭтапа.ТекстНадписи				= НСтр("ru = 'Принято в обработку'");
		СтрокаЭтапа.ТекстСтатуса				= НСтр("ru = 'Принято в обработку'");
		СтрокаЭтапа.Дата						= ДатаИзОтвета(СтрРезультатыПриемаЗапросНО, НеПолучатьДаты);
		СтрокаЭтапа.КомментарийКСостоянию		= НСтр("ru = 'Ожидается ответ на запрос'");
		СтрокаЭтапа.НаименованиеПротокола		= НСтр("ru = 'Квитанция о приеме'");
		СтрокаЭтапа.Протокол					= ?(СтрРезультатыПриемаЗапросНО.Количество() > 0, СтрРезультатыПриемаЗапросНО[0].Ссылка, "");
		СтрокаЭтапа.СостояниеСдачиОтчетности	= Перечисления.СостояниеСдачиОтчетности.ДокументооборотНачат;
		СтрокаЭтапа.ЭтапПройден					= СтрРезультатыПриемаЗапросНО.Количество() > 0;
		
		СтрокаЭтапа = ТаблицаЭтаповОтправки.Добавить();
		СтрокаЭтапа.ТекстНадписи				= НСтр("ru = 'Готово'");
		СтрокаЭтапа.ТекстСтатуса				= НСтр("ru = 'Готово'");
		СтрокаЭтапа.Дата						= ДатаИзОтвета(СтрРезультатыОбработкиЗапросНО, НеПолучатьДаты);
		СтрокаЭтапа.КомментарийКСостоянию		= "";
		СтрокаЭтапа.НаименованиеПротокола		= СсылкаНаОтчет.ВидУслуги;
		СтрокаЭтапа.Протокол					= ?(СтрРезультатыОбработкиЗапросНО.Количество() > 0, СтрРезультатыОбработкиЗапросНО[0].Ссылка, "");
		СтрокаЭтапа.СостояниеСдачиОтчетности	= Перечисления.СостояниеСдачиОтчетности.ПоложительныйРезультатДокументооборота;
		СтрокаЭтапа.ЭтапПройден					= СтрРезультатыОбработкиЗапросНО.Количество() > 0;
		
	КонецЕсли;
	
	Возврат ТаблицаЭтаповОтправки;
	
КонецФункции

Функция ТаблицаЭтаповОтправкиЗапросаНаВыпискуИзЕГРЮЛ_ЕГРИП(СсылкаНаОтчет, ДополнительныеПараметры)
	
	НеПолучатьДаты = НЕ (ТипЗнч(ДополнительныеПараметры) = Тип("Структура")
			И ДополнительныеПараметры.Свойство("ПолучатьДаты")
			И ДополнительныеПараметры.ПолучатьДаты);
			
	ПоказыватьПомеченныеНаУдаление = ТипЗнч(ДополнительныеПараметры) = Тип("Структура")
		И ДополнительныеПараметры.Свойство("ПоказыватьПомеченныеНаУдаление")
		И ДополнительныеПараметры.ПоказыватьПомеченныеНаУдаление;
	
	ТаблицаЭтаповОтправки = ЗаготовкаТаблицыЭтаповОтправки();
	
	СтрокаЭтапа = ТаблицаЭтаповОтправки.Добавить();
	СтрокаЭтапа.ТекстНадписи				= НСтр("ru = 'Не отправлено'");
	СтрокаЭтапа.ТекстСтатуса				= НСтр("ru = 'Не отправлено'");
	СтрокаЭтапа.Дата						= Дата(1,1,1);
	СтрокаЭтапа.КомментарийКСостоянию		= "";
	СтрокаЭтапа.НаименованиеПротокола		= "";
	СтрокаЭтапа.Протокол					= "";
	СтрокаЭтапа.СостояниеСдачиОтчетности	= Перечисления.СостояниеСдачиОтчетности.ДокументооборотНеНачат;
	СтрокаЭтапа.ЭтапПройден					= Истина;
	
	// получаем список сообщений
	Если ТипЗнч(ДополнительныеПараметры) = Тип("Структура")
		И ДополнительныеПараметры.Свойство("Отправка") Тогда
		ЦиклОбмена = ДополнительныеПараметры.Отправка;
	Иначе
		ЦиклОбмена = ДокументооборотСКОВызовСервера.ПолучитьПоследнийЦиклОбмена(СсылкаНаОтчет);
	КонецЕсли;
	
	Если НЕ ЗначениеЗаполнено(ЦиклОбмена) Тогда
		Возврат ТаблицаЭтаповОтправки;
	КонецЕсли;
	
	СообщенияЦикла = ПолучитьСообщенияЦиклаОбмена(ЦиклОбмена,,ПоказыватьПомеченныеНаУдаление,Истина);

	// выделяем основные типы сообщений
	СтрЗапросЕГР 					= СообщенияЦикла.НайтиСтроки(Новый Структура("Тип", Перечисления.ТипыТранспортныхСообщений.ЗапросНаВыпискуЕРГЮЛ_ЕГРИП));
	СтрРезультатыОбработкиЗапросЕГР = СообщенияЦикла.НайтиСтроки(Новый Структура("Тип", Перечисления.ТипыТранспортныхСообщений.РезультатОбработкиЗапросаНаВыпискуЕРГЮЛ_ЕГРИП));
	
	// Если статус первичного - не отправлено, тогда выходим 
	Если СтрЗапросЕГР.Количество() > 0 И СтрЗапросЕГР[0].Статус <> Перечисления.СтатусыПисем.Отправленное Тогда
		Возврат ТаблицаЭтаповОтправки;
	КонецЕсли;
	
	СтрокаЭтапа = ТаблицаЭтаповОтправки.Добавить();
	СтрокаЭтапа.ТекстНадписи				= НСтр("ru = 'Принято в обработку'");
	СтрокаЭтапа.ТекстСтатуса				= НСтр("ru = 'Принято в обработку'");
	СтрокаЭтапа.Дата						= ДатаОтправки(СтрЗапросЕГР, НеПолучатьДаты);;
	СтрокаЭтапа.КомментарийКСостоянию		= НСтр("ru = 'Ожидается ответ на запрос'");
	СтрокаЭтапа.НаименованиеПротокола		= "";
	СтрокаЭтапа.Протокол					= "";
	СтрокаЭтапа.СостояниеСдачиОтчетности	= Перечисления.СостояниеСдачиОтчетности.ДокументооборотНачат;
	СтрокаЭтапа.ЭтапПройден					= СтрЗапросЕГР.Количество() > 0;
	
	// определяем наиболее свежее сообщение с результатом приема
	ПоследнийРезультатПриема = Неопределено;
	Для Каждого СтрРезультатОбработкиЗапроса Из СтрРезультатыОбработкиЗапросЕГР Цикл
		Если ПоследнийРезультатПриема = Неопределено ИЛИ СтрРезультатОбработкиЗапроса.Дата > СтрРезультатОбработкиЗапроса.Дата Тогда
			ПоследнийРезультатПриема = СтрРезультатОбработкиЗапроса;
		КонецЕсли;
	КонецЦикла;
	
	// если результат приема отрицательный, то выводим соответствующее сообщение
	Если ПоследнийРезультатПриема <> Неопределено И ПоследнийРезультатПриема.ПротоколСОшибкой Тогда
		
		СтрокаЭтапа = ТаблицаЭтаповОтправки.Добавить();
		СтрокаЭтапа.ТекстНадписи				= НСтр("ru = 'Отказано'");
		СтрокаЭтапа.ТекстСтатуса				= НСтр("ru = 'Отказано'");
		СтрокаЭтапа.Дата						= ДатаИзОтвета(ПоследнийРезультатПриема, НеПолучатьДаты);
		СтрокаЭтапа.КомментарийКСостоянию		= НСтр("ru = 'Исправьте ошибки и отправьте новый запрос'");
		СтрокаЭтапа.НаименованиеПротокола		= НСтр("ru = 'Уведомление об отказе'");
		СтрокаЭтапа.Протокол					= ?(СтрРезультатыОбработкиЗапросЕГР.Количество() > 0, СтрРезультатыОбработкиЗапросЕГР[0].Ссылка, "");
		СтрокаЭтапа.СостояниеСдачиОтчетности	= Перечисления.СостояниеСдачиОтчетности.ОтрицательныйРезультатДокументооборота;
		СтрокаЭтапа.ЭтапПройден					= СтрРезультатыОбработкиЗапросЕГР.Количество() > 0;

	Иначе
		
		ВАрхиве = Ложь;
		ЕстьОтвет = ЕстьОтветНаЗапросыВыпискиИзЕГРЮЛ_ЕГРИП(СсылкаНаОтчет, ВАрхиве);
		
		Если СтрРезультатыОбработкиЗапросЕГР.Количество() > 0 Тогда
			ТранспортноеСообщение = СтрРезультатыОбработкиЗапросЕГР[0].Ссылка;
			СообщениеРаспаковано  = ПолучитьВложенияТранспортногоСообщения(ТранспортноеСообщение).Количество() > 0;
		Иначе
			СообщениеРаспаковано = Ложь;
		КонецЕсли;
		
		Если СтрРезультатыОбработкиЗапросЕГР.Количество() > 0 И СообщениеРаспаковано И НЕ ЕстьОтвет Тогда
			
			Если СсылкаНаОтчет.ВидПараметраЗапроса	= Перечисления.ВидыПараметровЗапросаНаВыпискуИзЕГРЮЛ_ЕГРИП.ИНН Тогда
				Комментарий = НСтр("ru = 'По запрошенному ИНН информация в реестре не найдена'");
			Иначе
				Комментарий = НСтр("ru = 'По запрошенному ОГРН информация в реестре не найдена'");
			КонецЕсли;
			
			СтрокаЭтапа = ТаблицаЭтаповОтправки.Добавить();
			СтрокаЭтапа.ТекстНадписи				= НСтр("ru = 'Не найдено в реестре'");
			СтрокаЭтапа.ТекстСтатуса				= НСтр("ru = 'Не найдено в реестре'");
			СтрокаЭтапа.Дата						= ДатаИзОтвета(СтрРезультатыОбработкиЗапросЕГР, НеПолучатьДаты);
			СтрокаЭтапа.КомментарийКСостоянию		= Комментарий;
			СтрокаЭтапа.НаименованиеПротокола		= "";
			СтрокаЭтапа.Протокол					= "";
			СтрокаЭтапа.СостояниеСдачиОтчетности	= Перечисления.СостояниеСдачиОтчетности.ОтрицательныйРезультатДокументооборота;
			СтрокаЭтапа.ЭтапПройден					= Истина;
			
		Иначе

			СтрокаЭтапа = ТаблицаЭтаповОтправки.Добавить();
			СтрокаЭтапа.ТекстНадписи				= НСтр("ru = 'Готово'");
			СтрокаЭтапа.ТекстСтатуса				= НСтр("ru = 'Готово'");
			СтрокаЭтапа.Дата						= ДатаИзОтвета(СтрРезультатыОбработкиЗапросЕГР, НеПолучатьДаты);
			СтрокаЭтапа.КомментарийКСостоянию		= "";
			СтрокаЭтапа.НаименованиеПротокола		= НСтр("ru = 'Выписка'");
			СтрокаЭтапа.Протокол					= ?(СтрРезультатыОбработкиЗапросЕГР.Количество() > 0, СтрРезультатыОбработкиЗапросЕГР[0].Ссылка, "");
			СтрокаЭтапа.СостояниеСдачиОтчетности	= Перечисления.СостояниеСдачиОтчетности.ПоложительныйРезультатДокументооборота;
			СтрокаЭтапа.ЭтапПройден					= СтрРезультатыОбработкиЗапросЕГР.Количество() > 0;
			
		КонецЕсли;
			
	КонецЕсли;
	
	Возврат ТаблицаЭтаповОтправки;
	
КонецФункции

Функция ТаблицаЭтаповОтправкиЗапросаНаИнформационноеОбслуживаниеСтрахователя(СсылкаНаОтчет, ДополнительныеПараметры)
	
	НеПолучатьДаты = НЕ (ТипЗнч(ДополнительныеПараметры) = Тип("Структура")
		И ДополнительныеПараметры.Свойство("ПолучатьДаты")
		И ДополнительныеПараметры.ПолучатьДаты);
		
	ПоказыватьПомеченныеНаУдаление = ТипЗнч(ДополнительныеПараметры) = Тип("Структура")
		И ДополнительныеПараметры.Свойство("ПоказыватьПомеченныеНаУдаление")
		И ДополнительныеПараметры.ПоказыватьПомеченныеНаУдаление;
	
	ТаблицаЭтаповОтправки = ЗаготовкаТаблицыЭтаповОтправки();
	
	СтрокаЭтапа = ТаблицаЭтаповОтправки.Добавить();
	СтрокаЭтапа.ТекстНадписи				= НСтр("ru = 'Не отправлено'");
	СтрокаЭтапа.ТекстСтатуса				= НСтр("ru = 'Не отправлено'");
	СтрокаЭтапа.Дата						= Дата(1,1,1);
	СтрокаЭтапа.КомментарийКСостоянию		= "";
	СтрокаЭтапа.НаименованиеПротокола		= "";
	СтрокаЭтапа.Протокол					= "";
	СтрокаЭтапа.СостояниеСдачиОтчетности	= Перечисления.СостояниеСдачиОтчетности.ДокументооборотНеНачат;
	СтрокаЭтапа.ЭтапПройден					= Истина;
	
	// получаем список сообщений
	Если ТипЗнч(ДополнительныеПараметры) = Тип("Структура")
		И ДополнительныеПараметры.Свойство("Отправка") Тогда
		ЦиклОбмена = ДополнительныеПараметры.Отправка;
	Иначе
		ЦиклОбмена = ДокументооборотСКОВызовСервера.ПолучитьПоследнийЦиклОбмена(СсылкаНаОтчет);
	КонецЕсли;	
	
	Если НЕ ЗначениеЗаполнено(ЦиклОбмена) Тогда
		Возврат ТаблицаЭтаповОтправки;
	КонецЕсли;
	
	СообщенияЦикла = ПолучитьСообщенияЦиклаОбмена(ЦиклОбмена,,ПоказыватьПомеченныеНаУдаление,Истина);
	
	// выделяем основные типы сообщений
	СтрПервичныеСообщенияСодержащееЗапросПФР 	= СообщенияЦикла.НайтиСтроки(Новый Структура("Тип", Перечисления.ТипыТранспортныхСообщений.ПервичноеСообщениеСодержащееЗапросПФР));
	СтрПодтвержденияПолученияЗапросаПФР 		= СообщенияЦикла.НайтиСтроки(Новый Структура("Тип", Перечисления.ТипыТранспортныхСообщений.ПодтверждениеПолученияЗапросаПФР));
	СтрОтветыНаЗапросПФР 						= СообщенияЦикла.НайтиСтроки(Новый Структура("Тип", Перечисления.ТипыТранспортныхСообщений.ОтветНаЗапросПФР));
	СтрОтветыНаЗапросКвитанцияПФР 				= СообщенияЦикла.НайтиСтроки(Новый Структура("Тип", Перечисления.ТипыТранспортныхСообщений.ОтветНаЗапросКвитанцияПФР));
		
	// Если статус первичного - не отправлено, тогда выходим 
	Если СтрПервичныеСообщенияСодержащееЗапросПФР.Количество() > 0 И СтрПервичныеСообщенияСодержащееЗапросПФР[0].Статус <> Перечисления.СтатусыПисем.Отправленное Тогда
		Возврат ТаблицаЭтаповОтправки;
	КонецЕсли;
	
	// Отправлено в ПФР
	СтрокаЭтапа = ТаблицаЭтаповОтправки.Добавить();
	СтрокаЭтапа.ТекстНадписи				= НСтр("ru = 'Отправлено в ПФР'");
	СтрокаЭтапа.ТекстСтатуса				= НСтр("ru = 'Отправлено в ПФР'");
	СтрокаЭтапа.Дата						= ДатаОтправки(СтрПервичныеСообщенияСодержащееЗапросПФР, НеПолучатьДаты);
	СтрокаЭтапа.КомментарийКСостоянию		= НСтр("ru = 'Ожидается прием запроса в обработку'");
	СтрокаЭтапа.НаименованиеПротокола		= "";
	СтрокаЭтапа.Протокол					= "";
	СтрокаЭтапа.СостояниеСдачиОтчетности	= Перечисления.СостояниеСдачиОтчетности.ДокументооборотНачат;
	СтрокаЭтапа.ЭтапПройден					= СтрПервичныеСообщенияСодержащееЗапросПФР.Количество() > 0;

	// Принято в обработку
	СтрокаЭтапа = ТаблицаЭтаповОтправки.Добавить();
	СтрокаЭтапа.ТекстНадписи				= НСтр("ru = 'Принято в обработку'");
	СтрокаЭтапа.ТекстСтатуса				= НСтр("ru = 'Принято в обработку'");
	СтрокаЭтапа.Дата						= ДатаИзОтвета(СтрПодтвержденияПолученияЗапросаПФР, НеПолучатьДаты);;
	СтрокаЭтапа.КомментарийКСостоянию		= НСтр("ru = 'Ожидается ответ на запрос'");
	СтрокаЭтапа.НаименованиеПротокола		= НСтр("ru = 'Подтверждение получения'");
	СтрокаЭтапа.Протокол					= ?(СтрПодтвержденияПолученияЗапросаПФР.Количество() > 0, СтрПодтвержденияПолученияЗапросаПФР[0].Ссылка, "");
	СтрокаЭтапа.СостояниеСдачиОтчетности	= Перечисления.СостояниеСдачиОтчетности.ДокументооборотНачат;
	СтрокаЭтапа.ЭтапПройден					= СтрПодтвержденияПолученияЗапросаПФР.Количество() > 0;
			
	// определяем наиболее свежее сообщение с результатом приема
	ПоследнийОтветНаЗапрос = Неопределено;
	Для Каждого СтрОтветНаЗапросПФР Из СтрОтветыНаЗапросПФР Цикл
		Если ПоследнийОтветНаЗапрос = Неопределено ИЛИ СтрОтветНаЗапросПФР.Дата > ПоследнийОтветНаЗапрос.Дата Тогда
			ПоследнийОтветНаЗапрос = СтрОтветНаЗапросПФР;
		КонецЕсли;
	КонецЦикла;
	
	// если результат отрицательный, то выводим соответствующее сообщение
	Если ПоследнийОтветНаЗапрос <> Неопределено И ПоследнийОтветНаЗапрос.ПротоколСОшибкой Тогда
		
		СтрокаЭтапа = ТаблицаЭтаповОтправки.Добавить();
		СтрокаЭтапа.ТекстНадписи				= НСтр("ru = 'Отказано'");
		СтрокаЭтапа.ТекстСтатуса				= НСтр("ru = 'Отказано'");
		СтрокаЭтапа.Дата						= ДатаИзОтвета(СтрОтветыНаЗапросПФР, НеПолучатьДаты);
		СтрокаЭтапа.КомментарийКСостоянию		= НСтр("ru = 'Исправьте ошибки и отправьте новый запрос'");
		СтрокаЭтапа.НаименованиеПротокола		= НСтр("ru = 'Уведомление об отказе'");
		СтрокаЭтапа.Протокол					= ?(СтрОтветыНаЗапросПФР.Количество() > 0, СтрОтветыНаЗапросПФР[0].Ссылка, "");
		СтрокаЭтапа.СостояниеСдачиОтчетности	= Перечисления.СостояниеСдачиОтчетности.ОтрицательныйРезультатДокументооборота;
		СтрокаЭтапа.ЭтапПройден					= СтрОтветыНаЗапросПФР.Количество() > 0;
		
	Иначе
		
		Если СсылкаНаОтчет.ВидУслуги = Перечисления.ВидыУслугПриИОС.СправкаОСостоянииРасчетов Тогда
			НаименованиеПротокола = НСтр("ru = 'Справка о состоянии расчетов'");
		Иначе
			НаименованиеПротокола = НСтр("ru = 'Сверка ФИО и СНИЛС'");
		КонецЕсли;
		
		СтрокаЭтапа = ТаблицаЭтаповОтправки.Добавить();
		СтрокаЭтапа.ТекстНадписи				= НСтр("ru = 'Готово'");
		СтрокаЭтапа.ТекстСтатуса				= НСтр("ru = 'Готово'");
		СтрокаЭтапа.Дата						= ДатаИзОтвета(СтрОтветыНаЗапросПФР, НеПолучатьДаты);
		СтрокаЭтапа.КомментарийКСостоянию		= "";
		СтрокаЭтапа.НаименованиеПротокола		= НаименованиеПротокола;
		СтрокаЭтапа.Протокол					= ?(СтрОтветыНаЗапросПФР.Количество() > 0, СтрОтветыНаЗапросПФР[0].Ссылка, "");
		СтрокаЭтапа.СостояниеСдачиОтчетности	= Перечисления.СостояниеСдачиОтчетности.ПоложительныйРезультатДокументооборота;
		СтрокаЭтапа.ЭтапПройден					= СтрОтветыНаЗапросПФР.Количество() > 0;
		
	КонецЕсли;
	
	Возврат ТаблицаЭтаповОтправки;
	
КонецФункции

Функция ТаблицаЭтаповОтправкиФСРАР(СсылкаНаОтчет, ДополнительныеПараметры)
	
	НачальныйСтатус = НачальныйСтатусПриФормированииТаблицыЭтаповОтправки(СсылкаНаОтчет);
	
	ТаблицаЭтаповОтправки = ЗаготовкаТаблицыЭтаповОтправки();
	
	СтрокаЭтапа = ТаблицаЭтаповОтправки.Добавить();
	СтрокаЭтапа.ТекстНадписи				= НачальныйСтатус;
	СтрокаЭтапа.ТекстСтатуса				= НачальныйСтатус;
	СтрокаЭтапа.Дата						= Дата(1,1,1);
	СтрокаЭтапа.КомментарийКСостоянию		= КомментарийКСостояниюНеОтправлено();
	СтрокаЭтапа.НаименованиеПротокола		= "";
	СтрокаЭтапа.Протокол					= "";
	СтрокаЭтапа.СостояниеСдачиОтчетности	= Перечисления.СостояниеСдачиОтчетности.ДокументооборотНеНачат;
	СтрокаЭтапа.ЭтапПройден					= Истина;
	
	// определяем последнюю отправку
	Если ТипЗнч(ДополнительныеПараметры) = Тип("Структура")
		И ДополнительныеПараметры.Свойство("Отправка") Тогда
		ОтправкаСсылка = ДополнительныеПараметры.Отправка;
	Иначе
		ОтправкаСсылка =  ПолучитьПоследнююОтправкуОтчетаВФСРАР(СсылкаНаОтчет);
	КонецЕсли;
	
	Если НЕ ЗначениеЗаполнено(ОтправкаСсылка) Тогда
		Возврат ТаблицаЭтаповОтправки;
	КонецЕсли;
	
	СтрокаЭтапа = ТаблицаЭтаповОтправки.Добавить();
	СтрокаЭтапа.ТекстНадписи				= НСтр("ru = 'Отправлено в ФСРАР'");
	СтрокаЭтапа.ТекстСтатуса				= НСтр("ru = 'Отправлено в ФСРАР'");
	СтрокаЭтапа.Дата						= ?(ЗначениеЗаполнено(ОтправкаСсылка), ОтправкаСсылка.ДатаОтправки, Дата(1,1,1));
	СтрокаЭтапа.КомментарийКСостоянию		= НСтр("ru = 'Ожидается результат обработки отчета'");
	СтрокаЭтапа.НаименованиеПротокола		= "";
	СтрокаЭтапа.Протокол					= "";
	СтрокаЭтапа.СостояниеСдачиОтчетности	= Перечисления.СостояниеСдачиОтчетности.ДокументооборотНачат;
	СтрокаЭтапа.ЭтапПройден					= ОтправкаСсылка <> Неопределено;
	
	Если ОтправкаСсылка <> Неопределено И  ОтправкаСсылка.СтатусОтправки = Перечисления.СтатусыОтправки.НеПринят Тогда
		
		СтрокаЭтапа = ТаблицаЭтаповОтправки.Добавить();
		СтрокаЭтапа.ТекстНадписи				= НСтр("ru = 'Не сдано'");
		СтрокаЭтапа.ТекстСтатуса				= НСтр("ru = 'Не сдано'");
		СтрокаЭтапа.Дата						= ?(ЗначениеЗаполнено(ОтправкаСсылка), ОтправкаСсылка.ДатаЗакрытия, Дата(1,1,1));
		СтрокаЭтапа.КомментарийКСостоянию		= НСтр("ru = 'Исправьте ошибки и отправьте отчет еще раз'");
		СтрокаЭтапа.НаименованиеПротокола		= НСтр("ru = 'Протокол ошибок'");
		СтрокаЭтапа.Протокол					= ОтправкаСсылка;
		СтрокаЭтапа.СостояниеСдачиОтчетности	= Перечисления.СостояниеСдачиОтчетности.ОтрицательныйРезультатДокументооборота;
		СтрокаЭтапа.ЭтапПройден					= Истина;
		
	Иначе
		
		СтрокаЭтапа = ТаблицаЭтаповОтправки.Добавить();
		СтрокаЭтапа.ТекстНадписи				= НСтр("ru = 'Сдано'");
		СтрокаЭтапа.ТекстСтатуса				= НСтр("ru = 'Сдано'");
		СтрокаЭтапа.Дата						= ?(ЗначениеЗаполнено(ОтправкаСсылка) И ОтправкаСсылка.СтатусОтправки = Перечисления.СтатусыОтправки.Сдан, ОтправкаСсылка.ДатаЗакрытия, Дата(1,1,1));
		СтрокаЭтапа.КомментарийКСостоянию		= "";
		СтрокаЭтапа.НаименованиеПротокола		= НСтр("ru = 'Протокол о сдаче'");
		СтрокаЭтапа.Протокол					= ОтправкаСсылка;
		СтрокаЭтапа.СостояниеСдачиОтчетности	= Перечисления.СостояниеСдачиОтчетности.ПоложительныйРезультатДокументооборота;
		СтрокаЭтапа.ЭтапПройден					= ОтправкаСсылка <> Неопределено И ОтправкаСсылка.СтатусОтправки = Перечисления.СтатусыОтправки.Сдан;
			
	КонецЕсли;
	
	Возврат ТаблицаЭтаповОтправки;
	
КонецФункции

Функция ТаблицаЭтаповОтправкиРПН(СсылкаНаОтчет, ДополнительныеПараметры)
	
	НачальныйСтатус = НачальныйСтатусПриФормированииТаблицыЭтаповОтправки(СсылкаНаОтчет);
	
	ТаблицаЭтаповОтправки = ЗаготовкаТаблицыЭтаповОтправки();
	
	СтрокаЭтапа = ТаблицаЭтаповОтправки.Добавить();
	СтрокаЭтапа.ТекстНадписи				= НачальныйСтатус;
	СтрокаЭтапа.ТекстСтатуса				= НачальныйСтатус;
	СтрокаЭтапа.Дата						= Дата(1,1,1);
	СтрокаЭтапа.КомментарийКСостоянию		= КомментарийКСостояниюНеОтправлено();
	СтрокаЭтапа.НаименованиеПротокола		= "";
	СтрокаЭтапа.Протокол					= "";
	СтрокаЭтапа.СостояниеСдачиОтчетности	= Перечисления.СостояниеСдачиОтчетности.ДокументооборотНеНачат;
	СтрокаЭтапа.ЭтапПройден					= Истина;
	
	// определяем последнюю отправку
	Если ТипЗнч(ДополнительныеПараметры) = Тип("Структура")
		И ДополнительныеПараметры.Свойство("Отправка") Тогда
		ОтправкаСсылка = ДополнительныеПараметры.Отправка;
	Иначе
		ОтправкаСсылка =  ПолучитьПоследнююОтправкуОтчетаВРПН(СсылкаНаОтчет);
	КонецЕсли;
	
	Если НЕ ЗначениеЗаполнено(ОтправкаСсылка) Тогда
		Возврат ТаблицаЭтаповОтправки;
	КонецЕсли;
	
	СтрокаЭтапа = ТаблицаЭтаповОтправки.Добавить();
	СтрокаЭтапа.ТекстНадписи				= НСтр("ru = 'Отправлено в РПН'");
	СтрокаЭтапа.ТекстСтатуса				= НСтр("ru = 'Отправлено в РПН'");
	СтрокаЭтапа.Дата						= ?(ЗначениеЗаполнено(ОтправкаСсылка), ОтправкаСсылка.ДатаОтправки, Дата(1,1,1));
	СтрокаЭтапа.КомментарийКСостоянию		= НСтр("ru = 'Ожидается результат обработки'");
	СтрокаЭтапа.НаименованиеПротокола		= "";
	СтрокаЭтапа.Протокол					= "";
	СтрокаЭтапа.СостояниеСдачиОтчетности	= Перечисления.СостояниеСдачиОтчетности.ДокументооборотНачат;
	СтрокаЭтапа.ЭтапПройден					= ОтправкаСсылка <> Неопределено;

	
	Если ОтправкаСсылка <> Неопределено И  ОтправкаСсылка.СтатусОтправки = Перечисления.СтатусыОтправки.НеПринят Тогда
		
		СтрокаЭтапа = ТаблицаЭтаповОтправки.Добавить();
		СтрокаЭтапа.ТекстНадписи				= НСтр("ru = 'Не сдано'");
		СтрокаЭтапа.ТекстСтатуса				= НСтр("ru = 'Не сдано'");
		СтрокаЭтапа.Дата						= ?(ЗначениеЗаполнено(ОтправкаСсылка), ОтправкаСсылка.ДатаЗакрытия, Дата(1,1,1));
		СтрокаЭтапа.КомментарийКСостоянию		= НСтр("ru = 'Исправьте ошибки и отправьте отчет еще раз'");
		СтрокаЭтапа.НаименованиеПротокола		= НСтр("ru = 'Протокол ошибок'");
		СтрокаЭтапа.Протокол					= ОтправкаСсылка;
		СтрокаЭтапа.СостояниеСдачиОтчетности	= Перечисления.СостояниеСдачиОтчетности.ОтрицательныйРезультатДокументооборота;
		СтрокаЭтапа.ЭтапПройден					= Истина;
		
	Иначе
		
		СтрокаЭтапа = ТаблицаЭтаповОтправки.Добавить();
		СтрокаЭтапа.ТекстНадписи				= НСтр("ru = 'Сдано'");
		СтрокаЭтапа.ТекстСтатуса				= НСтр("ru = 'Сдано'");
		СтрокаЭтапа.Дата						= ?(ЗначениеЗаполнено(ОтправкаСсылка) И ОтправкаСсылка.СтатусОтправки = Перечисления.СтатусыОтправки.Сдан, ОтправкаСсылка.ДатаЗакрытия, Дата(1,1,1));
		СтрокаЭтапа.КомментарийКСостоянию		= "";
		СтрокаЭтапа.НаименованиеПротокола		= НСтр("ru = 'Протокол о сдаче'");
		СтрокаЭтапа.Протокол					= ОтправкаСсылка;
		СтрокаЭтапа.СостояниеСдачиОтчетности	= Перечисления.СостояниеСдачиОтчетности.ПоложительныйРезультатДокументооборота;
		СтрокаЭтапа.ЭтапПройден					= ОтправкаСсылка <> Неопределено И ОтправкаСсылка.СтатусОтправки = Перечисления.СтатусыОтправки.Сдан;
			
	КонецЕсли;
	
	Возврат ТаблицаЭтаповОтправки;
	
КонецФункции

Функция ТаблицаЭтаповОтправкиФТС(СсылкаНаОтчет, ДополнительныеПараметры)
	
	НачальныйСтатус = НачальныйСтатусПриФормированииТаблицыЭтаповОтправки(СсылкаНаОтчет);
	
	ТаблицаЭтаповОтправки = ЗаготовкаТаблицыЭтаповОтправки();
	
	СтрокаЭтапа = ТаблицаЭтаповОтправки.Добавить();
	СтрокаЭтапа.ТекстНадписи				= НачальныйСтатус;
	СтрокаЭтапа.ТекстСтатуса				= НачальныйСтатус;
	СтрокаЭтапа.Дата						= Дата(1,1,1);
	СтрокаЭтапа.КомментарийКСостоянию		= КомментарийКСостояниюНеОтправлено();
	СтрокаЭтапа.НаименованиеПротокола		= "";
	СтрокаЭтапа.Протокол					= "";
	СтрокаЭтапа.СостояниеСдачиОтчетности	= Перечисления.СостояниеСдачиОтчетности.ДокументооборотНеНачат;
	СтрокаЭтапа.ЭтапПройден					= Истина;
	
	// определяем последнюю отправку
	Если ТипЗнч(ДополнительныеПараметры) = Тип("Структура")
		И ДополнительныеПараметры.Свойство("Отправка") Тогда
		ОтправкаСсылка = ДополнительныеПараметры.Отправка;
	Иначе
		ОтправкаСсылка = ПолучитьПоследнююОтправкуОтчетаВФТС(СсылкаНаОтчет);
	КонецЕсли;
	
	Если НЕ ЗначениеЗаполнено(ОтправкаСсылка) Тогда
		Возврат ТаблицаЭтаповОтправки;
	КонецЕсли;
	
	СтрокаЭтапа = ТаблицаЭтаповОтправки.Добавить();
	СтрокаЭтапа.ТекстНадписи				= НСтр("ru = 'Отправлено в ФТС'");
	СтрокаЭтапа.ТекстСтатуса				= НСтр("ru = 'Отправлено в ФТС'");
	СтрокаЭтапа.Дата						= ?(ЗначениеЗаполнено(ОтправкаСсылка), ОтправкаСсылка.ДатаОтправки, Дата(1,1,1));
	СтрокаЭтапа.КомментарийКСостоянию		= НСтр("ru = 'Ожидается результат обработки'");
	СтрокаЭтапа.НаименованиеПротокола		= "";
	СтрокаЭтапа.Протокол					= "";
	СтрокаЭтапа.СостояниеСдачиОтчетности	= Перечисления.СостояниеСдачиОтчетности.ДокументооборотНачат;
	СтрокаЭтапа.ЭтапПройден					= ОтправкаСсылка <> Неопределено;
	
	Если ОтправкаСсылка <> Неопределено И  ОтправкаСсылка.СтатусОтправки = Перечисления.СтатусыОтправки.НеПринят И НЕ ОтправкаСсылка.НаКонтроле Тогда
		
		СтрокаЭтапа = ТаблицаЭтаповОтправки.Добавить();
		СтрокаЭтапа.ТекстНадписи				= НСтр("ru = 'Не сдано'");
		СтрокаЭтапа.ТекстСтатуса				= НСтр("ru = 'Не сдано'");
		СтрокаЭтапа.Дата						= ?(ЗначениеЗаполнено(ОтправкаСсылка), ОтправкаСсылка.ДатаЗакрытия, Дата(1,1,1));
		СтрокаЭтапа.КомментарийКСостоянию		= НСтр("ru = 'Исправьте ошибки и отправьте отчет еще раз'");
		СтрокаЭтапа.НаименованиеПротокола		= НСтр("ru = 'Протокол ошибок'");
		СтрокаЭтапа.Протокол					= ОтправкаСсылка;
		СтрокаЭтапа.СостояниеСдачиОтчетности	= Перечисления.СостояниеСдачиОтчетности.ОтрицательныйРезультатДокументооборота;
		СтрокаЭтапа.ЭтапПройден					= Истина;
		
	Иначе
		
		СтрокаЭтапа = ТаблицаЭтаповОтправки.Добавить();
		СтрокаЭтапа.ТекстНадписи				= НСтр("ru = 'Сдано'");
		СтрокаЭтапа.ТекстСтатуса				= НСтр("ru = 'Сдано'");
		СтрокаЭтапа.Дата						= ?(ЗначениеЗаполнено(ОтправкаСсылка) И ОтправкаСсылка.СтатусОтправки = Перечисления.СтатусыОтправки.Сдан, ОтправкаСсылка.ДатаЗакрытия, Дата(1,1,1));
		СтрокаЭтапа.КомментарийКСостоянию		= "";
		Если НЕ ОтправкаСсылка.НаКонтроле Тогда
			СтрокаЭтапа.НаименованиеПротокола	= НСтр("ru = 'Протокол о сдаче'");
		Иначе
			СтрокаЭтапа.НаименованиеПротокола	= НСтр("ru = 'Протокол'");
		КонецЕсли;
		СтрокаЭтапа.Протокол					= ОтправкаСсылка;
		СтрокаЭтапа.СостояниеСдачиОтчетности	= Перечисления.СостояниеСдачиОтчетности.ПоложительныйРезультатДокументооборота;
		СтрокаЭтапа.ЭтапПройден					= ОтправкаСсылка <> Неопределено И (ОтправкаСсылка.СтатусОтправки = Перечисления.СтатусыОтправки.Сдан ИЛИ ОтправкаСсылка.НаКонтроле);
		
	КонецЕсли;
	
	Если ОтправкаСсылка.НаКонтроле Тогда
		
		СтрокаЭтапа = ТаблицаЭтаповОтправки.Добавить();
		СтрокаЭтапа.ТекстНадписи				= НСтр("ru = 'Получен комментарий'");
		СтрокаЭтапа.ТекстСтатуса				= НСтр("ru = 'Получен комментарий'");
		СтрокаЭтапа.Дата						= ?(ЗначениеЗаполнено(ОтправкаСсылка), ОтправкаСсылка.ДатаЗакрытия, Дата(1,1,1));
		СтрокаЭтапа.КомментарийКСостоянию		= НСтр("ru = 'Отчет находится на контроле, ознакомьтесь с сообщением от ФТС'");
		СтрокаЭтапа.НаименованиеПротокола		= НСтр("ru = 'Комментарий на портале ФТС'");
		СтрокаЭтапа.Протокол					= "https://edata.customs.ru/FtsPersonalCabinetWeb2017/#?service=StatEp&start=0&view=List";
		СтрокаЭтапа.СостояниеСдачиОтчетности	= Перечисления.СостояниеСдачиОтчетности.ОтрицательныйРезультатДокументооборота;
		СтрокаЭтапа.ЭтапПройден					= Истина;
		
	КонецЕсли;
	
	Возврат ТаблицаЭтаповОтправки;
	
КонецФункции

Функция ТаблицаЭтаповОтправкиФСС(СсылкаНаОтчет, ДополнительныеПараметры) Экспорт
	
	НачальныйСтатус = НачальныйСтатусПриФормированииТаблицыЭтаповОтправки(СсылкаНаОтчет);
	
	ТаблицаЭтаповОтправки = ЗаготовкаТаблицыЭтаповОтправки();
	
	// определяем последнюю отправку
	Если ТипЗнч(ДополнительныеПараметры) = Тип("Структура")
		И ДополнительныеПараметры.Свойство("Отправка") Тогда
		ОтправкаСсылка = ДополнительныеПараметры.Отправка;
	Иначе
		ОтправкаСсылка = ПолучитьПоследнююОтправкуОтчетаВФСС(СсылкаНаОтчет);
	КонецЕсли;	
	
	СтрокаЭтапа = ТаблицаЭтаповОтправки.Добавить();
	СтрокаЭтапа.ТекстНадписи				= НачальныйСтатус;
	СтрокаЭтапа.ТекстСтатуса				= НачальныйСтатус;
	СтрокаЭтапа.Дата						= Дата(1,1,1);
	СтрокаЭтапа.КомментарийКСостоянию		= КомментарийКСостояниюНеОтправлено();
	СтрокаЭтапа.НаименованиеПротокола		= "";
	СтрокаЭтапа.Протокол					= "";
	СтрокаЭтапа.СостояниеСдачиОтчетности	= Перечисления.СостояниеСдачиОтчетности.ДокументооборотНеНачат;
	СтрокаЭтапа.ЭтапПройден					= Истина;
	
	Если НЕ ЗначениеЗаполнено(ОтправкаСсылка) Тогда
		ОбъектПоУмолчаниюРеестрЭЛНСуществует =
			Метаданные.Документы.Найти("РеестрДанныхЭЛНЗаполняемыхРаботодателем") <> Неопределено;
		ЭтоРеестрЭЛН = ДокументооборотСФССКлиентСервер.ЭтоРеестрДанныхЭЛНЗаполняемыхРаботодателем(СсылкаНаОтчет,
			ОбъектПоУмолчаниюРеестрЭЛНСуществует);
		
		Если ЭтоРеестрЭЛН И (НачальныйСтатус = "Сдано" ИЛИ НачальныйСтатус = "Не сдано"
			ИЛИ НачальныйСтатус = "Сдан частично") Тогда
			
			СтрокаЭтапа = ТаблицаЭтаповОтправки.Добавить();
			СтрокаЭтапа.ТекстНадписи				= НачальныйСтатус;
			СтрокаЭтапа.ТекстСтатуса				= НачальныйСтатус;
			СтрокаЭтапа.Дата						= Дата(1,1,1);
			СтрокаЭтапа.КомментарийКСостоянию		= НСтр("ru = 'Информация об отправке не сохранена или статус изменен вручную'");
			СтрокаЭтапа.НаименованиеПротокола		= "";
			СтрокаЭтапа.Протокол					= "";
			СтрокаЭтапа.СостояниеСдачиОтчетности	= Перечисления.СостояниеСдачиОтчетности.ДокументооборотНеНачат;
			СтрокаЭтапа.ЭтапПройден					= Истина;
		КонецЕсли;
		
		Возврат ТаблицаЭтаповОтправки;
	КонецЕсли;
	
	СтрокаЭтапа = ТаблицаЭтаповОтправки.Добавить();
	СтрокаЭтапа.ТекстНадписи				= НСтр("ru = 'Отправлено в ФСС'");
	СтрокаЭтапа.ТекстСтатуса				= НСтр("ru = 'Отправлено в ФСС'");
	СтрокаЭтапа.Дата						= ?(ЗначениеЗаполнено(ОтправкаСсылка), ОтправкаСсылка.ДатаОтправки, Дата(1,1,1));
	СтрокаЭтапа.КомментарийКСостоянию		= НСтр("ru = 'Ожидается результат обработки отчета'");
	СтрокаЭтапа.НаименованиеПротокола		= "";
	СтрокаЭтапа.Протокол					= "";
	СтрокаЭтапа.СостояниеСдачиОтчетности	= Перечисления.СостояниеСдачиОтчетности.ДокументооборотНачат;
	СтрокаЭтапа.ЭтапПройден					= ОтправкаСсылка <> Неопределено;


	Если ОтправкаСсылка <> Неопределено И ОтправкаСсылка.СтатусОтправки = Перечисления.СтатусыОтправки.НеПринят Тогда
		
		СтрокаЭтапа = ТаблицаЭтаповОтправки.Добавить();
		СтрокаЭтапа.ТекстНадписи				= НСтр("ru = 'Не сдано'");
		СтрокаЭтапа.ТекстСтатуса				= НСтр("ru = 'Не сдано'");
		СтрокаЭтапа.Дата						= ?(ЗначениеЗаполнено(ОтправкаСсылка), ОтправкаСсылка.ДатаЗакрытия, Дата(1,1,1));
		СтрокаЭтапа.КомментарийКСостоянию		= НСтр("ru = 'Исправьте ошибки и отправьте отчет еще раз'");
		СтрокаЭтапа.НаименованиеПротокола		= НСтр("ru = 'Протокол ошибок'");
		СтрокаЭтапа.Протокол					= ОтправкаСсылка;
		СтрокаЭтапа.СостояниеСдачиОтчетности	= Перечисления.СостояниеСдачиОтчетности.ОтрицательныйРезультатДокументооборота;
		СтрокаЭтапа.ЭтапПройден					= Истина;
		
	ИначеЕсли ОтправкаСсылка <> Неопределено И ОтправкаСсылка.СтатусОтправки = Перечисления.СтатусыОтправки.ПринятЕстьОшибки Тогда
		
		ЭтоРеестрЭЛН = (ОтправкаСсылка.ВидОтчета =
			Справочники.ВидыОтправляемыхДокументов.РеестрДанныхЭЛНЗаполняемыхРаботодателем);
		ЭтоРеестрСтимулирующихВыплатМедицинскимИСоциальнымРаботникам = (ОтправкаСсылка.ВидОтчета =
			Справочники.ВидыОтправляемыхДокументов.РеестрСтимулирующихВыплатМедицинскимИСоциальнымРаботникам);
		
		СтрокаЭтапа = ТаблицаЭтаповОтправки.Добавить();
		СтрокаЭтапа.ТекстНадписи				= ?(ЭтоРеестрЭЛН ИЛИ ЭтоРеестрСтимулирующихВыплатМедицинскимИСоциальнымРаботникам,
												  НСтр("ru = 'Сдан частично'"), НСтр("ru = 'Сдано, требует уточнения'"));
		СтрокаЭтапа.ТекстСтатуса				= ?(ЭтоРеестрЭЛН ИЛИ ЭтоРеестрСтимулирующихВыплатМедицинскимИСоциальнымРаботникам,
												  НСтр("ru = 'Сдан частично'"), НСтр("ru = 'Сдано, требует уточнения'"));
		СтрокаЭтапа.Дата						= ?(ЗначениеЗаполнено(ОтправкаСсылка), ОтправкаСсылка.ДатаЗакрытия, Дата(1,1,1));
		СтрокаЭтапа.КомментарийКСостоянию		= ?(ЭтоРеестрЭЛН,
												  НСтр("ru = 'При необходимости, исправьте ошибки в непринятых ЭЛН и отправьте отчет еще раз'"),
												  ?(ЭтоРеестрСтимулирующихВыплатМедицинскимИСоциальнымРаботникам,
												  НСтр("ru = 'При необходимости, исправьте ошибки и отправьте отчет еще раз'"),
												  НСтр("ru = 'Подготовьте и отправьте корректирующий отчет'")));
		СтрокаЭтапа.НаименованиеПротокола		= ?(ЭтоРеестрЭЛН ИЛИ ЭтоРеестрСтимулирующихВыплатМедицинскимИСоциальнымРаботникам,
												  НСтр("ru = 'Протокол приема'"), НСтр("ru = 'Протокол ошибок'"));
		СтрокаЭтапа.Протокол					= ОтправкаСсылка;
		СтрокаЭтапа.СостояниеСдачиОтчетности	= Перечисления.СостояниеСдачиОтчетности.ОтрицательныйРезультатДокументооборота;
		СтрокаЭтапа.ЭтапПройден					= Истина;
		
	Иначе
		
		СтрокаЭтапа = ТаблицаЭтаповОтправки.Добавить();
		СтрокаЭтапа.ТекстНадписи				= НСтр("ru = 'Сдано'");
		СтрокаЭтапа.ТекстСтатуса				= НСтр("ru = 'Сдано'");
		СтрокаЭтапа.Дата						= ?(ЗначениеЗаполнено(ОтправкаСсылка) И ОтправкаСсылка.СтатусОтправки = Перечисления.СтатусыОтправки.Сдан, ОтправкаСсылка.ДатаЗакрытия, Дата(1,1,1));
		СтрокаЭтапа.КомментарийКСостоянию		= "";
		СтрокаЭтапа.НаименованиеПротокола		= НСтр("ru = 'Протокол о сдаче'");
		СтрокаЭтапа.Протокол					= ОтправкаСсылка;
		СтрокаЭтапа.СостояниеСдачиОтчетности	= Перечисления.СостояниеСдачиОтчетности.ПоложительныйРезультатДокументооборота;
		СтрокаЭтапа.ЭтапПройден					= ОтправкаСсылка <> Неопределено И ОтправкаСсылка.СтатусОтправки = Перечисления.СтатусыОтправки.Сдан;
		
	КонецЕсли;
		
	Возврат ТаблицаЭтаповОтправки;
	
КонецФункции

Функция ТаблицаЭтаповОтправкиРосстат(СсылкаНаОтчет, ДополнительныеПараметры)
	
	НеПолучатьДаты = НЕ (ТипЗнч(ДополнительныеПараметры) = Тип("Структура")
		И ДополнительныеПараметры.Свойство("ПолучатьДаты")
		И ДополнительныеПараметры.ПолучатьДаты);
		
	ПоказыватьПомеченныеНаУдаление = ТипЗнч(ДополнительныеПараметры) = Тип("Структура")
		И ДополнительныеПараметры.Свойство("ПоказыватьПомеченныеНаУдаление")
		И ДополнительныеПараметры.ПоказыватьПомеченныеНаУдаление;
	
	НачальныйСтатус = НачальныйСтатусПриФормированииТаблицыЭтаповОтправки(СсылкаНаОтчет);
	
	ТаблицаЭтаповОтправки = ЗаготовкаТаблицыЭтаповОтправки();
	
	СтрокаЭтапа = ТаблицаЭтаповОтправки.Добавить();
	СтрокаЭтапа.ТекстНадписи				= НачальныйСтатус;
	СтрокаЭтапа.ТекстСтатуса				= НачальныйСтатус;
	СтрокаЭтапа.Дата						= Дата(1,1,1);
	СтрокаЭтапа.КомментарийКСостоянию		= КомментарийКСостояниюНеОтправлено();
	СтрокаЭтапа.НаименованиеПротокола		= "";
	СтрокаЭтапа.Протокол					= "";
	СтрокаЭтапа.СостояниеСдачиОтчетности	= Перечисления.СостояниеСдачиОтчетности.ДокументооборотНеНачат;
	СтрокаЭтапа.ЭтапПройден					= Истина;
	
	Если ТипЗнч(ДополнительныеПараметры) = Тип("Структура")
		И ДополнительныеПараметры.Свойство("Отправка") Тогда
		ЦиклОбмена = ДополнительныеПараметры.Отправка;
	Иначе
		ЦиклОбмена = ДокументооборотСКОВызовСервера.ПолучитьПоследнийЦиклОбмена(СсылкаНаОтчет);
	КонецЕсли;
	
	// если у пользователя недостаточно прав для получения сведений об отправке, то скроем панель
	Если НЕ ЗначениеЗаполнено(ЦиклОбмена) Тогда
		Возврат ТаблицаЭтаповОтправки;
	КонецЕсли;
		
	// получаем список всех не помеченных на удаление сообщений последнего цикла
	СообщенияЦикла = ПолучитьСообщенияЦиклаОбмена(ЦиклОбмена,,ПоказыватьПомеченныеНаУдаление,Истина);
	
	// выделяем основные типы сообщений
	СтрПервичныеСообщения 					= СообщенияЦикла.НайтиСтроки(Новый Структура("Тип", Перечисления.ТипыТранспортныхСообщений.ПервичноеСообщениеСодержащееОтчетностьФСГС));
	СтрПодтвержденияДатыОтправки 			= СообщенияЦикла.НайтиСтроки(Новый Структура("Тип", Перечисления.ТипыТранспортныхСообщений.ПодтверждениеДатыОтправкиФСГС));
	СтрИзвещенияПодтвержденияДатыОтправки 	= СообщенияЦикла.НайтиСтроки(Новый Структура("Тип", Перечисления.ТипыТранспортныхСообщений.ИзвещениеПодтверждениеДатыОтправкиФСГС));
	СтрИзвещенияОПриеме 					= СообщенияЦикла.НайтиСтроки(Новый Структура("Тип", Перечисления.ТипыТранспортныхСообщений.ИзвещениеОПолученииОтчетностиФСГС));
	СтрПротоколыВходногоКонтроля 			= СообщенияЦикла.НайтиСтроки(Новый Структура("Тип", Перечисления.ТипыТранспортныхСообщений.ПротоколВходногоКонтроляОтчетностиФСГС));
	СтрИзвещенияПротоколыВходногоКонтроля 	= СообщенияЦикла.НайтиСтроки(Новый Структура("Тип", Перечисления.ТипыТранспортныхСообщений.ИзвещениеПротоколВходногоКонтроляОтчетностиФСГС));
	СтрКритическиеОшибки 					= СообщенияЦикла.НайтиСтроки(Новый Структура("Тип", Перечисления.ТипыТранспортныхСообщений.КритическаяОшибка));
	
	Если СтрПервичныеСообщения.Количество() > 0 И СтрПервичныеСообщения[0].Статус <> Перечисления.СтатусыПисем.Отправленное Тогда
		Возврат ТаблицаЭтаповОтправки;
	КонецЕсли;
	
	СтрокаЭтапа = ТаблицаЭтаповОтправки.Добавить();
	СтрокаЭтапа.ТекстНадписи				= НСтр("ru = 'Отправлено оператору'");
	СтрокаЭтапа.ТекстСтатуса				= НСтр("ru = 'Отправлено оператору'");
	СтрокаЭтапа.Дата						= ДатаОтправки(СтрПервичныеСообщения, НеПолучатьДаты);;
	СтрокаЭтапа.КомментарийКСостоянию		= НСтр("ru = 'Ожидается подтверждение даты отправки'");
	СтрокаЭтапа.НаименованиеПротокола		= "";
	СтрокаЭтапа.Протокол					= "";
	СтрокаЭтапа.СостояниеСдачиОтчетности	= Перечисления.СостояниеСдачиОтчетности.ДокументооборотНачат;
	СтрокаЭтапа.ЭтапПройден					= СтрПервичныеСообщения.Количество() > 0;
	
	СтрокаЭтапа = ТаблицаЭтаповОтправки.Добавить();
	СтрокаЭтапа.ТекстНадписи				= НСтр("ru = 'Отправлено в Росстат'");
	СтрокаЭтапа.ТекстСтатуса				= НСтр("ru = 'Отправлено в Росстат'");
	СтрокаЭтапа.Дата						= ДатаИзОтвета(СтрПодтвержденияДатыОтправки, НеПолучатьДаты);;
	СтрокаЭтапа.КомментарийКСостоянию		= НСтр("ru = 'Ожидается результат обработки отчета'");
	СтрокаЭтапа.НаименованиеПротокола		= НСтр("ru = 'Подтверждение даты отправки'");
	СтрокаЭтапа.Протокол					= ?(СтрПодтвержденияДатыОтправки.Количество() > 0, СтрПодтвержденияДатыОтправки[0].Ссылка, "");
	СтрокаЭтапа.СостояниеСдачиОтчетности	= Перечисления.СостояниеСдачиОтчетности.ДокументооборотНачат;
	СтрокаЭтапа.ЭтапПройден					= СтрПодтвержденияДатыОтправки.Количество() > 0;

	ПоследнийПротокол = Неопределено;
	Для Каждого СтрПротокол Из СтрПротоколыВходногоКонтроля Цикл
		Если ПоследнийПротокол = Неопределено ИЛИ СтрПротокол.Дата > ПоследнийПротокол.Дата Тогда
			ПоследнийПротокол = СтрПротокол;
		КонецЕсли;
	КонецЦикла;
	
	Если ПоследнийПротокол <> Неопределено И ПоследнийПротокол.ПротоколСОшибкой Тогда
		
		СтрокаЭтапа = ТаблицаЭтаповОтправки.Добавить();
		СтрокаЭтапа.ТекстНадписи				= НСтр("ru = 'Не сдано'");
		СтрокаЭтапа.ТекстСтатуса				= НСтр("ru = 'Не сдано'");
		СтрокаЭтапа.Дата						= ДатаИзОтвета(ПоследнийПротокол, НеПолучатьДаты);
		СтрокаЭтапа.КомментарийКСостоянию		= НСтр("ru = 'Исправьте ошибки и отправьте отчет еще раз'");
		СтрокаЭтапа.НаименованиеПротокола		= "Протокол ошибок";
		СтрокаЭтапа.Протокол					= ПоследнийПротокол.Ссылка;
		СтрокаЭтапа.СостояниеСдачиОтчетности	= Перечисления.СостояниеСдачиОтчетности.ОтрицательныйРезультатДокументооборота;
		СтрокаЭтапа.ЭтапПройден					= Истина;
		
	Иначе
		
		СтрокаЭтапа = ТаблицаЭтаповОтправки.Добавить();
		СтрокаЭтапа.ТекстНадписи				= НСтр("ru = 'Сдано'");
		СтрокаЭтапа.ТекстСтатуса				= НСтр("ru = 'Сдано'");
		СтрокаЭтапа.Дата						= ДатаИзОтвета(СтрПротоколыВходногоКонтроля, НеПолучатьДаты);
		СтрокаЭтапа.КомментарийКСостоянию		= "";
		СтрокаЭтапа.НаименованиеПротокола		= НСтр("ru = 'Протокол о сдаче'");
		СтрокаЭтапа.Протокол					= ?(СтрПротоколыВходногоКонтроля.Количество() > 0, СтрПротоколыВходногоКонтроля[0].Ссылка, "");
		СтрокаЭтапа.СостояниеСдачиОтчетности	= Перечисления.СостояниеСдачиОтчетности.ПоложительныйРезультатДокументооборота;
		СтрокаЭтапа.ЭтапПройден					= СтрПротоколыВходногоКонтроля.Количество() > 0;
		
	КонецЕсли;
	
	Возврат ТаблицаЭтаповОтправки;
	
КонецФункции

Функция ТаблицаЭтаповОтправкиФНС(СсылкаНаОтчет, ДополнительныеПараметры)
	
	НеПолучатьДаты = НЕ (ТипЗнч(ДополнительныеПараметры) = Тип("Структура")
		И ДополнительныеПараметры.Свойство("ПолучатьДаты")
		И ДополнительныеПараметры.ПолучатьДаты);
		
	ПоказыватьПомеченныеНаУдаление = ТипЗнч(ДополнительныеПараметры) = Тип("Структура")
		И ДополнительныеПараметры.Свойство("ПоказыватьПомеченныеНаУдаление")
		И ДополнительныеПараметры.ПоказыватьПомеченныеНаУдаление;
	
	НачальныйСтатус = НачальныйСтатусПриФормированииТаблицыЭтаповОтправки(СсылкаНаОтчет);
	
	ТаблицаЭтаповОтправки = ЗаготовкаТаблицыЭтаповОтправки();
	
	ЭтоУведомление = ТипЗнч(СсылкаНаОтчет) = Тип("ДокументСсылка.УведомлениеОСпецрежимахНалогообложения");
	Если ЭтоУведомление Тогда
		КомментарийКСостояниюНеОтправлено = "";
	Иначе
		КомментарийКСостояниюНеОтправлено = КомментарийКСостояниюНеОтправлено();
	КонецЕсли;

	СтрокаЭтапа = ТаблицаЭтаповОтправки.Добавить();
	СтрокаЭтапа.ТекстНадписи				= НачальныйСтатус;
	СтрокаЭтапа.ТекстСтатуса				= НачальныйСтатус;
	СтрокаЭтапа.Дата						= Дата(1,1,1);
	СтрокаЭтапа.КомментарийКСостоянию		= КомментарийКСостояниюНеОтправлено;
	СтрокаЭтапа.НаименованиеПротокола		= "";
	СтрокаЭтапа.Протокол					= "";
	СтрокаЭтапа.СостояниеСдачиОтчетности	= Перечисления.СостояниеСдачиОтчетности.ДокументооборотНеНачат;
	СтрокаЭтапа.ЭтапПройден					= Истина;
	
	Если ТипЗнч(ДополнительныеПараметры) = Тип("Структура")
		И ДополнительныеПараметры.Свойство("Отправка") Тогда
		ЦиклОбмена = ДополнительныеПараметры.Отправка;
	Иначе
		ЦиклОбмена = ДокументооборотСКОВызовСервера.ПолучитьПоследнийЦиклОбмена(СсылкаНаОтчет);
	КонецЕсли;
	
	// если у пользователя недостаточно прав для получения сведений об отправке, то скроем панель
	Если НЕ ЗначениеЗаполнено(ЦиклОбмена) Тогда
		Возврат ТаблицаЭтаповОтправки;
	КонецЕсли;
	
	// получаем список всех не помеченных на удаление сообщений последнего цикла
	СообщенияЦикла = ПолучитьСообщенияЦиклаОбмена(ЦиклОбмена,,ПоказыватьПомеченныеНаУдаление,Истина);
		
	Если ЦиклОбмена.Тип = Перечисления.ТипыЦикловОбмена.Заявление Тогда
		
		// выделяем основные типы сообщений
		СтрЗаявленияНП 							= СообщенияЦикла.НайтиСтроки(Новый Структура("Тип", Перечисления.ТипыТранспортныхСообщений.ЗаявлениеНП));
		СтрПодтвержденияЗаявлениеНО				= СообщенияЦикла.НайтиСтроки(Новый Структура("Тип", Перечисления.ТипыТранспортныхСообщений.ПодтверждениеЗаявлениеНО));
		СтрИзвещенияПодтверждениеНП 			= СообщенияЦикла.НайтиСтроки(Новый Структура("Тип", Перечисления.ТипыТранспортныхСообщений.ИзвещениеПодтверждениеНП));
		СтрИзвещенияЗаявлениеНО 				= СообщенияЦикла.НайтиСтроки(Новый Структура("Тип", Перечисления.ТипыТранспортныхСообщений.ИзвещениеЗаявлениеНО));
		СтрРезультатыПриемаЗаявлениеНО 			= СообщенияЦикла.НайтиСтроки(Новый Структура("Тип", Перечисления.ТипыТранспортныхСообщений.РезультатПриемаЗаявлениеНО));
		СтрИзвещенияРезультатПриемаНП 			= СообщенияЦикла.НайтиСтроки(Новый Структура("Тип", Перечисления.ТипыТранспортныхСообщений.ИзвещениеРезультатПриемаНП));
		
		СтрРезультатыОбработкиЗаявлениеНО		= СообщенияЦикла.НайтиСтроки(Новый Структура("Тип", Перечисления.ТипыТранспортныхСообщений.РезультатОбработкиЗаявлениеНО));
		СтрИзвещенияРезультатОбработкиНП 		= СообщенияЦикла.НайтиСтроки(Новый Структура("Тип", Перечисления.ТипыТранспортныхСообщений.ИзвещениеРезультатОбработкиНП));
	
		СтрРезультатыОбработкиЗаявлениеРФНО		= СообщенияЦикла.НайтиСтроки(Новый Структура("Тип", Перечисления.ТипыТранспортныхСообщений.РезультатОбработкиЗаявлениеРФНО));
		СтрИзвещенияРезультатОбработкиРФНП 		= СообщенияЦикла.НайтиСтроки(Новый Структура("Тип", Перечисления.ТипыТранспортныхСообщений.ИзвещениеРезультатОбработкиРФНП));
		СтрСообщенияОбОтзывеЗаявлениеРФНО 		= СообщенияЦикла.НайтиСтроки(Новый Структура("Тип", Перечисления.ТипыТранспортныхСообщений.СообщениеОбОтзывеЗаявлениеРФНО));
		СтрИзвещенияОбОтзывеЗаявлениеРФНП 		= СообщенияЦикла.НайтиСтроки(Новый Структура("Тип", Перечисления.ТипыТранспортныхСообщений.ИзвещениеОбОтзывеЗаявлениеРФНП));
		СтрРезультатыОбработкиЗаявлениеТСНО 	= СообщенияЦикла.НайтиСтроки(Новый Структура("Тип", Перечисления.ТипыТранспортныхСообщений.РезультатОбработкиЗаявлениеТСНО));
		СтрИзвещенияРезультатОбработкиТСНП 		= СообщенияЦикла.НайтиСтроки(Новый Структура("Тип", Перечисления.ТипыТранспортныхСообщений.ИзвещениеРезультатОбработкиТСНП));
		
		СтрКритическиеОшибки 					= СообщенияЦикла.НайтиСтроки(Новый Структура("Тип", Перечисления.ТипыТранспортныхСообщений.УведомлениеОбОшибкеФНС));
		
		Если СтрЗаявленияНП.Количество() > 0 И СтрЗаявленияНП[0].Статус <> Перечисления.СтатусыПисем.Отправленное Тогда
			Возврат ТаблицаЭтаповОтправки;
		КонецЕсли;
		
		СтрокаЭтапа = ТаблицаЭтаповОтправки.Добавить();
		СтрокаЭтапа.ТекстНадписи				= НСтр("ru = 'Отправлено оператору'");
		СтрокаЭтапа.ТекстСтатуса				= НСтр("ru = 'Отправлено оператору'");
		СтрокаЭтапа.Дата						= ДатаОтправки(СтрЗаявленияНП, НеПолучатьДаты);
		СтрокаЭтапа.КомментарийКСостоянию		= НСтр("ru = 'Ожидается подтверждение даты отправки'");
		СтрокаЭтапа.НаименованиеПротокола		= "";
		СтрокаЭтапа.Протокол					= "";
		СтрокаЭтапа.СостояниеСдачиОтчетности	= Перечисления.СостояниеСдачиОтчетности.ДокументооборотНачат;
		СтрокаЭтапа.ЭтапПройден					= СтрЗаявленияНП.Количество() > 0;
		
		СтрокаЭтапа = ТаблицаЭтаповОтправки.Добавить();
		СтрокаЭтапа.ТекстНадписи				= НСтр("ru = 'Отправлено в ФНС'");
		СтрокаЭтапа.ТекстСтатуса				= НСтр("ru = 'Отправлено в ФНС'");
		СтрокаЭтапа.Дата						= ДатаИзОтвета(СтрПодтвержденияЗаявлениеНО, НеПолучатьДаты);
		СтрокаЭтапа.КомментарийКСостоянию		= НСтр("ru = 'Ожидается прием отчета в обработку'");
		СтрокаЭтапа.НаименованиеПротокола		= НСтр("ru = 'Подтверждение даты отправки'");
		СтрокаЭтапа.Протокол					= ?(СтрПодтвержденияЗаявлениеНО.Количество() > 0, СтрПодтвержденияЗаявлениеНО[0].Ссылка, "");
		СтрокаЭтапа.СостояниеСдачиОтчетности	= Перечисления.СостояниеСдачиОтчетности.ДокументооборотНачат;
		СтрокаЭтапа.ЭтапПройден					= СтрПодтвержденияЗаявлениеНО.Количество() > 0;
		
		// определяем наиболее свежее сообщение с результатом приема
		ПоследнийРезультатПриема = Неопределено;
		Для Каждого СтрРезультатПриемаЗаявлениеНО Из СтрРезультатыПриемаЗаявлениеНО Цикл
			Если ПоследнийРезультатПриема = Неопределено ИЛИ СтрРезультатПриемаЗаявлениеНО.Дата > ПоследнийРезультатПриема.Дата Тогда
				ПоследнийРезультатПриема = СтрРезультатПриемаЗаявлениеНО;
			КонецЕсли;
		КонецЦикла;
		
		// если результат приема отрицательный, то выводим соответствующее сообщение
		Если ПоследнийРезультатПриема <> Неопределено И ПоследнийРезультатПриема.ПротоколСОшибкой Тогда
			
			СтрокаЭтапа = ТаблицаЭтаповОтправки.Добавить();
			СтрокаЭтапа.ТекстНадписи				= НСтр("ru = 'Не принято в обработку'");
			СтрокаЭтапа.ТекстСтатуса				= НСтр("ru = 'Не принято в обработку'");
			СтрокаЭтапа.Дата						= ДатаИзОтвета(ПоследнийРезультатПриема, НеПолучатьДаты);
			СтрокаЭтапа.КомментарийКСостоянию		= НСтр("ru = 'Исправьте ошибки и отправьте отчет еще раз'");
			СтрокаЭтапа.НаименованиеПротокола		= НСтр("ru = 'Уведомление об отказе'");
			СтрокаЭтапа.Протокол					= ПоследнийРезультатПриема.Ссылка;
			СтрокаЭтапа.СостояниеСдачиОтчетности	= Перечисления.СостояниеСдачиОтчетности.ОтрицательныйРезультатДокументооборота;
			СтрокаЭтапа.ЭтапПройден					= Истина;
			
		Иначе
			
			СтрокаЭтапа = ТаблицаЭтаповОтправки.Добавить();
			СтрокаЭтапа.ТекстНадписи				= НСтр("ru = 'Принято в обработку'");
			СтрокаЭтапа.ТекстСтатуса				= НСтр("ru = 'Принято в обработку'");
			СтрокаЭтапа.Дата						= ДатаИзОтвета(?(ПоследнийРезультатПриема <> Неопределено,
				ПоследнийРезультатПриема, СтрРезультатыПриемаЗаявлениеНО), НеПолучатьДаты);
			СтрокаЭтапа.КомментарийКСостоянию		= НСтр("ru = 'Ожидается результат обработки отчета'");
			СтрокаЭтапа.НаименованиеПротокола		= НСтр("ru = 'Квитанция о приеме'");
			СтрокаЭтапа.Протокол					= ?(ПоследнийРезультатПриема <> Неопределено, ПоследнийРезультатПриема.Ссылка, "");
			СтрокаЭтапа.СостояниеСдачиОтчетности	= Перечисления.СостояниеСдачиОтчетности.ДокументооборотНачат;
			СтрокаЭтапа.ЭтапПройден					= ПоследнийРезультатПриема <> Неопределено;
			
			// определяем наиболее свежее сообщение с результатом обработки
			ПоследнийРезультатОбработки = Неопределено;
			Для Каждого СтрРезультатОбработки Из СтрРезультатыОбработкиЗаявлениеРФНО Цикл
				Если ПоследнийРезультатОбработки = Неопределено ИЛИ СтрРезультатОбработки.Дата > ПоследнийРезультатОбработки.Дата Тогда
					ПоследнийРезультатОбработки = СтрРезультатОбработки;
				КонецЕсли;
			КонецЦикла;
			
			// если результат обработки отрицательный, то выводим соответствующее сообщение
			Если ПоследнийРезультатОбработки <> Неопределено И ПоследнийРезультатОбработки.ПротоколСОшибкой Тогда
				
				СтрокаЭтапа = ТаблицаЭтаповОтправки.Добавить();
				СтрокаЭтапа.ТекстНадписи				= НСтр("ru = 'Не сдано'");
				СтрокаЭтапа.ТекстСтатуса				= НСтр("ru = 'Не сдано'");
				СтрокаЭтапа.Дата						= ДатаИзОтвета(ПоследнийРезультатОбработки, НеПолучатьДаты);
				СтрокаЭтапа.КомментарийКСостоянию		= НСтр("ru = 'Исправьте ошибки и отправьте отчет еще раз'");
				СтрокаЭтапа.НаименованиеПротокола		= НСтр("ru = 'Сообщение о проверке'");
				СтрокаЭтапа.Протокол					= ПоследнийРезультатОбработки.Ссылка;
				СтрокаЭтапа.СостояниеСдачиОтчетности	= Перечисления.СостояниеСдачиОтчетности.ОтрицательныйРезультатДокументооборота;
				СтрокаЭтапа.ЭтапПройден					= Истина;
				
			Иначе
				
				Если СтрРезультатыОбработкиЗаявлениеНО.Количество() = 0 Тогда
					
					Если СтрСообщенияОбОтзывеЗаявлениеРФНО.Количество() <> 0 Тогда

						СтрокаЭтапа = ТаблицаЭтаповОтправки.Добавить();
						СтрокаЭтапа.ТекстНадписи				= НСтр("ru = 'Не сдано'");
						СтрокаЭтапа.ТекстСтатуса				= НСтр("ru = 'Не сдано'");
						СтрокаЭтапа.Дата						= ДатаИзОтвета(СтрСообщенияОбОтзывеЗаявлениеРФНО, НеПолучатьДаты);
						СтрокаЭтапа.КомментарийКСостоянию		= НСтр("ru = 'Исправьте ошибки и отправьте отчет еще раз'");
						СтрокаЭтапа.НаименованиеПротокола		= НСтр("ru = 'Сообщение об отзыве заявления'");
						СтрокаЭтапа.Протокол					= СтрСообщенияОбОтзывеЗаявлениеРФНО[0].Ссылка;
						СтрокаЭтапа.СостояниеСдачиОтчетности	= Перечисления.СостояниеСдачиОтчетности.ОтрицательныйРезультатДокументооборота;
						СтрокаЭтапа.ЭтапПройден					= Истина;
					
					Иначе
						Если СтрРезультатыОбработкиЗаявлениеТСНО.Количество() <> 0 Тогда
							
							СтрокаЭтапа = ТаблицаЭтаповОтправки.Добавить();
							СтрокаЭтапа.ТекстНадписи				= НСтр("ru = 'Не сдано'");
							СтрокаЭтапа.ТекстСтатуса				= НСтр("ru = 'Не сдано'");
							СтрокаЭтапа.Дата						= ДатаИзОтвета(СтрРезультатыОбработкиЗаявлениеТСНО, НеПолучатьДаты);
							СтрокаЭтапа.КомментарийКСостоянию		= НСтр("ru = 'Исправьте ошибки и отправьте отчет еще раз'");
							СтрокаЭтапа.НаименованиеПротокола		= НСтр("ru = 'Сообщение о несоответствиях'");
							СтрокаЭтапа.Протокол					= СтрРезультатыОбработкиЗаявлениеТСНО[0].Ссылка;
							СтрокаЭтапа.СостояниеСдачиОтчетности	= Перечисления.СостояниеСдачиОтчетности.ОтрицательныйРезультатДокументооборота;
							СтрокаЭтапа.ЭтапПройден					= Истина;

						Иначе
							
							СтрокаЭтапа = ТаблицаЭтаповОтправки.Добавить();
							СтрокаЭтапа.ТекстНадписи				= НСтр("ru = 'Сдано'");
							СтрокаЭтапа.ТекстСтатуса				= НСтр("ru = 'Сдано'");
							СтрокаЭтапа.Дата						= ДатаИзОтвета(СтрРезультатыПриемаЗаявлениеНО, НеПолучатьДаты);
							СтрокаЭтапа.КомментарийКСостоянию		= "";
							СтрокаЭтапа.НаименованиеПротокола		= НСтр("ru = 'Сообщение о проверке'");
							СтрокаЭтапа.Протокол					= ?(СтрРезультатыПриемаЗаявлениеНО.Количество() > 0, СтрРезультатыПриемаЗаявлениеНО[0].Ссылка, "");
							СтрокаЭтапа.СостояниеСдачиОтчетности	= Перечисления.СостояниеСдачиОтчетности.ПоложительныйРезультатДокументооборота;
							СтрокаЭтапа.ЭтапПройден					= СтрРезультатыОбработкиЗаявлениеТСНО.Количество() = 0 И СтрСообщенияОбОтзывеЗаявлениеРФНО.Количество() = 0
								И СтрРезультатыОбработкиЗаявлениеРФНО.Количество() <> 0 И НЕ ПоследнийРезультатОбработки.ПротоколСОшибкой;

						КонецЕсли;
					КонецЕсли;
			
				Иначе
					
					// определяем наиболее свежее сообщение с результатом обработки
					ПоследнийРезультатОбработки = Неопределено;
					Для Каждого СтрРезультатОбработки Из СтрРезультатыОбработкиЗаявлениеНО Цикл
						Если ПоследнийРезультатОбработки = Неопределено ИЛИ СтрРезультатОбработки.Дата > ПоследнийРезультатОбработки.Дата Тогда
							ПоследнийРезультатОбработки = СтрРезультатОбработки;
						КонецЕсли;
					КонецЦикла;
					
					// если результат обработки отрицательный, то выводим соответствующее сообщение
					Если ПоследнийРезультатОбработки <> Неопределено И ПоследнийРезультатОбработки.ПротоколСОшибкой Тогда
						
						СтрокаЭтапа = ТаблицаЭтаповОтправки.Добавить();
						СтрокаЭтапа.ТекстНадписи				= НСтр("ru = 'Не сдано'");
						СтрокаЭтапа.ТекстСтатуса				= НСтр("ru = 'Не сдано'");
						СтрокаЭтапа.Дата						= ДатаИзОтвета(ПоследнийРезультатОбработки, НеПолучатьДаты);
						СтрокаЭтапа.КомментарийКСостоянию		= НСтр("ru = 'Исправьте ошибки и отправьте отчет еще раз'");
						СтрокаЭтапа.НаименованиеПротокола		= НСтр("ru = 'Уведомление об отказе в проставлении пометки'");
						СтрокаЭтапа.Протокол					= ПоследнийРезультатОбработки.Ссылка;
						СтрокаЭтапа.СостояниеСдачиОтчетности	= Перечисления.СостояниеСдачиОтчетности.ОтрицательныйРезультатДокументооборота;
						СтрокаЭтапа.ЭтапПройден					= СтрРезультатыОбработкиЗаявлениеНО.Количество() > 0;
						
					Иначе
						
						СтрокаЭтапа = ТаблицаЭтаповОтправки.Добавить();
						СтрокаЭтапа.ТекстНадписи				= НСтр("ru = 'Сдано'");
						СтрокаЭтапа.ТекстСтатуса				= НСтр("ru = 'Сдано'");
						СтрокаЭтапа.Дата						= ДатаИзОтвета(ПоследнийРезультатОбработки, НеПолучатьДаты);
						СтрокаЭтапа.КомментарийКСостоянию		= "";
						СтрокаЭтапа.НаименованиеПротокола		= НСтр("ru = 'Квитанция о приеме'");
						СтрокаЭтапа.Протокол					= ?(СтрРезультатыОбработкиЗаявлениеНО.Количество() > 0, СтрРезультатыОбработкиЗаявлениеНО[0].Ссылка, "");
						СтрокаЭтапа.СостояниеСдачиОтчетности	= Перечисления.СостояниеСдачиОтчетности.ПоложительныйРезультатДокументооборота;
						СтрокаЭтапа.ЭтапПройден					= СтрРезультатыОбработкиЗаявлениеНО.Количество() > 0;
							
					КонецЕсли;
						
				КонецЕсли;
					
			КонецЕсли;
			
		КонецЕсли;

	ИначеЕсли ЦиклОбмена.Тип = Перечисления.ТипыЦикловОбмена.Форма2НДФЛ Тогда
		
		// выделяем основные типы сообщений
		СтрФормы2НДФЛНП							= СообщенияЦикла.НайтиСтроки(Новый Структура("Тип", Перечисления.ТипыТранспортныхСообщений.Форма2НДФЛНП));
		СтрПодтвержденияФорма2НДФЛНО			= СообщенияЦикла.НайтиСтроки(Новый Структура("Тип", Перечисления.ТипыТранспортныхСообщений.ПодтверждениеФорма2НДФЛНО));
		СтрИзвещенияПодтверждениеНП 			= СообщенияЦикла.НайтиСтроки(Новый Структура("Тип", Перечисления.ТипыТранспортныхСообщений.ИзвещениеПодтверждениеНП));
		СтрИзвещенияФорма2НДФЛНО 				= СообщенияЦикла.НайтиСтроки(Новый Структура("Тип", Перечисления.ТипыТранспортныхСообщений.ИзвещениеФорма2НДФЛНО));
		СтрРезультатыПриемаФорма2НДФЛНО			= СообщенияЦикла.НайтиСтроки(Новый Структура("Тип", Перечисления.ТипыТранспортныхСообщений.РезультатПриемаФорма2НДФЛНО));
		СтрИзвещенияРезультатПриемаНП 			= СообщенияЦикла.НайтиСтроки(Новый Структура("Тип", Перечисления.ТипыТранспортныхСообщений.ИзвещениеРезультатПриемаНП));
		
		СтрКритическиеОшибки 					= СообщенияЦикла.НайтиСтроки(Новый Структура("Тип", Перечисления.ТипыТранспортныхСообщений.УведомлениеОбОшибкеФНС));
		
		Если СтрФормы2НДФЛНП.Количество() > 0 И СтрФормы2НДФЛНП[0].Статус <> Перечисления.СтатусыПисем.Отправленное Тогда
			Возврат ТаблицаЭтаповОтправки;
		КонецЕсли;
		
		СтрокаЭтапа = ТаблицаЭтаповОтправки.Добавить();
		СтрокаЭтапа.ТекстНадписи				= НСтр("ru = 'Отправлено оператору'");
		СтрокаЭтапа.ТекстСтатуса				= НСтр("ru = 'Отправлено оператору'");
		СтрокаЭтапа.Дата						= ДатаОтправки(СтрФормы2НДФЛНП, НеПолучатьДаты);
		СтрокаЭтапа.КомментарийКСостоянию		= НСтр("ru = 'Ожидается подтверждение даты отправки'");
		СтрокаЭтапа.НаименованиеПротокола		= "";
		СтрокаЭтапа.Протокол					= ?(СтрФормы2НДФЛНП.Количество() > 0, СтрФормы2НДФЛНП[0].Ссылка, "");
		СтрокаЭтапа.СостояниеСдачиОтчетности	= Перечисления.СостояниеСдачиОтчетности.ДокументооборотНачат;
		СтрокаЭтапа.ЭтапПройден					= СтрФормы2НДФЛНП.Количество() > 0;

		СтрокаЭтапа = ТаблицаЭтаповОтправки.Добавить();
		СтрокаЭтапа.ТекстНадписи				= НСтр("ru = 'Отправлено в ФНС'");
		СтрокаЭтапа.ТекстСтатуса				= НСтр("ru = 'Отправлено в ФНС'");
		СтрокаЭтапа.Дата						= ДатаИзОтвета(СтрПодтвержденияФорма2НДФЛНО, НеПолучатьДаты);
		СтрокаЭтапа.КомментарийКСостоянию		= НСтр("ru = 'Ожидается прием отчета в обработку'");
		СтрокаЭтапа.НаименованиеПротокола		= НСтр("ru = 'Подтверждение даты отправки'");
		СтрокаЭтапа.Протокол					= ?(СтрПодтвержденияФорма2НДФЛНО.Количество() > 0, СтрПодтвержденияФорма2НДФЛНО[0].Ссылка, "");
		СтрокаЭтапа.СостояниеСдачиОтчетности	= Перечисления.СостояниеСдачиОтчетности.ДокументооборотНачат;
		СтрокаЭтапа.ЭтапПройден					= СтрПодтвержденияФорма2НДФЛНО.Количество() > 0;
		
		// определяем наиболее свежее сообщение с результатом приема
		ПоследнийРезультатПриема = Неопределено;
		Для Каждого СтрРезультатПриемаФорма2НДФЛНО Из СтрРезультатыПриемаФорма2НДФЛНО Цикл
			Если ПоследнийРезультатПриема = Неопределено ИЛИ СтрРезультатПриемаФорма2НДФЛНО.Дата > ПоследнийРезультатПриема.Дата Тогда
				ПоследнийРезультатПриема = СтрРезультатПриемаФорма2НДФЛНО;
			КонецЕсли;
		КонецЦикла;
		
		ОтчетСданЧастично = ПоследнийРезультатПриема <> Неопределено И Отчет2НДФЛСданЧастично(ПоследнийРезультатПриема);
		ПротоколСОшибкой  = ПоследнийРезультатПриема <> Неопределено И ПоследнийРезультатПриема.ПротоколСОшибкой;
		
		// если результат приема отрицательный, то выводим соответствующее сообщение
		Если ОтчетСданЧастично Тогда
			
			СтрокаЭтапа = ТаблицаЭтаповОтправки.Добавить();
			СтрокаЭтапа.ТекстНадписи				= НСтр("ru = 'Сдан частично'");
			СтрокаЭтапа.ТекстСтатуса				= НСтр("ru = 'Сдан частично'");
			СтрокаЭтапа.Дата						= ДатаИзОтвета(ПоследнийРезультатПриема, НеПолучатьДаты);
			СтрокаЭтапа.КомментарийКСостоянию		= НСтр("ru = 'Исправьте ошибки и отправьте отчет еще раз'");
			СтрокаЭтапа.НаименованиеПротокола		= "Протокол приема";
			СтрокаЭтапа.Протокол					= ПоследнийРезультатПриема.Ссылка;
			СтрокаЭтапа.СостояниеСдачиОтчетности	= Перечисления.СостояниеСдачиОтчетности.ОтрицательныйРезультатДокументооборота;
			СтрокаЭтапа.ЭтапПройден					= Истина;
	
		ИначеЕсли ПротоколСОшибкой Тогда
			
			СтрокаЭтапа = ТаблицаЭтаповОтправки.Добавить();
			СтрокаЭтапа.ТекстНадписи				= НСтр("ru = 'Не сдано'");
			СтрокаЭтапа.ТекстСтатуса				= НСтр("ru = 'Не сдано'");
			СтрокаЭтапа.Дата						= ДатаИзОтвета(ПоследнийРезультатПриема, НеПолучатьДаты);
			СтрокаЭтапа.КомментарийКСостоянию		= НСтр("ru = 'Исправьте ошибки и отправьте отчет еще раз'");
			СтрокаЭтапа.НаименованиеПротокола		= "Протокол ошибок";
			СтрокаЭтапа.Протокол					= ПоследнийРезультатПриема.Ссылка;
			СтрокаЭтапа.СостояниеСдачиОтчетности	= Перечисления.СостояниеСдачиОтчетности.ОтрицательныйРезультатДокументооборота;
			СтрокаЭтапа.ЭтапПройден					= Истина;
			
		Иначе
			
			СтрокаЭтапа = ТаблицаЭтаповОтправки.Добавить();
			СтрокаЭтапа.ТекстНадписи				= НСтр("ru = 'Сдано'");
			СтрокаЭтапа.ТекстСтатуса				= НСтр("ru = 'Сдано'");
			СтрокаЭтапа.Дата						= ДатаИзОтвета(СтрРезультатыПриемаФорма2НДФЛНО, НеПолучатьДаты);
			СтрокаЭтапа.КомментарийКСостоянию		= "";
			СтрокаЭтапа.НаименованиеПротокола		= "Протокол приема";
			СтрокаЭтапа.Протокол					= ?(СтрРезультатыПриемаФорма2НДФЛНО.Количество() > 0, СтрРезультатыПриемаФорма2НДФЛНО[0].Ссылка, "");
			СтрокаЭтапа.СостояниеСдачиОтчетности	= Перечисления.СостояниеСдачиОтчетности.ПоложительныйРезультатДокументооборота;
			СтрокаЭтапа.ЭтапПройден					= СтрРезультатыПриемаФорма2НДФЛНО.Количество() > 0;
			
		КонецЕсли;
		
	ИначеЕсли ЦиклОбмена.Тип = Перечисления.ТипыЦикловОбмена.Представление Тогда
		
		// выделяем основные типы сообщений
		СтрПредставленияНП					= СообщенияЦикла.НайтиСтроки(Новый Структура("Тип", Перечисления.ТипыТранспортныхСообщений.ПредставлениеНП));
		СтрПодтвержденияПредставлениеНО		= СообщенияЦикла.НайтиСтроки(Новый Структура("Тип", Перечисления.ТипыТранспортныхСообщений.ПодтверждениеПредставлениеНО));
		СтрИзвещенияПодтверждениеНП			= СообщенияЦикла.НайтиСтроки(Новый Структура("Тип", Перечисления.ТипыТранспортныхСообщений.ИзвещениеПодтверждениеНП));
		СтрИзвещенияПредставлениеНО			= СообщенияЦикла.НайтиСтроки(Новый Структура("Тип", Перечисления.ТипыТранспортныхСообщений.ИзвещениеПредставлениеНО));
		СтрРезультатыПриемаПредставлениеНО	= СообщенияЦикла.НайтиСтроки(Новый Структура("Тип", Перечисления.ТипыТранспортныхСообщений.РезультатПриемаПредставлениеНО));
		СтрИзвещенияРезультатПриемаНП		= СообщенияЦикла.НайтиСтроки(Новый Структура("Тип", Перечисления.ТипыТранспортныхСообщений.ИзвещениеРезультатПриемаНП));
		
		СтрКритическиеОшибки = СообщенияЦикла.НайтиСтроки(Новый Структура("Тип", Перечисления.ТипыТранспортныхСообщений.УведомлениеОбОшибкеФНС));
		
		// Если статус первичного - не отправлено, тогда выходим 
		Если СтрПредставленияНП.Количество() > 0 И СтрПредставленияНП[0].Статус <> Перечисления.СтатусыПисем.Отправленное Тогда
			Возврат ТаблицаЭтаповОтправки;
		КонецЕсли;
		
		СтрокаЭтапа = ТаблицаЭтаповОтправки.Добавить();
		СтрокаЭтапа.ТекстНадписи				= НСтр("ru = 'Отправлено оператору'");
		СтрокаЭтапа.ТекстСтатуса				= НСтр("ru = 'Отправлено оператору'");
		СтрокаЭтапа.Дата						= ДатаОтправки(СтрПредставленияНП, НеПолучатьДаты);
		СтрокаЭтапа.КомментарийКСостоянию		= НСтр("ru = 'Ожидается подтверждение даты отправки'");
		СтрокаЭтапа.НаименованиеПротокола		= "";
		СтрокаЭтапа.Протокол					= ?(СтрПредставленияНП.Количество() > 0, СтрПредставленияНП[0].Ссылка, "");
		СтрокаЭтапа.СостояниеСдачиОтчетности	= Перечисления.СостояниеСдачиОтчетности.ДокументооборотНачат;
		СтрокаЭтапа.ЭтапПройден					= СтрПредставленияНП.Количество() > 0;

		СтрокаЭтапа = ТаблицаЭтаповОтправки.Добавить();
		СтрокаЭтапа.ТекстНадписи				= НСтр("ru = 'Отправлено в ФНС'");
		СтрокаЭтапа.ТекстСтатуса				= НСтр("ru = 'Отправлено в ФНС'");
		СтрокаЭтапа.Дата						= ДатаИзОтвета(СтрПодтвержденияПредставлениеНО, НеПолучатьДаты);
		СтрокаЭтапа.КомментарийКСостоянию		= НСтр("ru = 'Ожидается прием уведомления в обработку'");
		СтрокаЭтапа.НаименованиеПротокола		= НСтр("ru = 'Подтверждение даты отправки'");
		СтрокаЭтапа.Протокол					= ?(СтрПодтвержденияПредставлениеНО.Количество() > 0, СтрПодтвержденияПредставлениеНО[0].Ссылка, "");
		СтрокаЭтапа.СостояниеСдачиОтчетности	= Перечисления.СостояниеСдачиОтчетности.ДокументооборотНачат;
		СтрокаЭтапа.ЭтапПройден					= СтрПодтвержденияПредставлениеНО.Количество() > 0;
		
		// определяем наиболее свежее сообщение с результатом приема
		ПоследнийРезультатПриема = Неопределено;
		Для Каждого СтрРезультатПриемаПредставлениеНО Из СтрРезультатыПриемаПредставлениеНО Цикл
			Если ПоследнийРезультатПриема = Неопределено ИЛИ СтрРезультатПриемаПредставлениеНО.Дата > ПоследнийРезультатПриема.Дата Тогда
				ПоследнийРезультатПриема = СтрРезультатПриемаПредставлениеНО;
			КонецЕсли;
		КонецЦикла;
		
		// если результат приема отрицательный, то выводим соответствующее сообщение
		Если ПоследнийРезультатПриема <> Неопределено И ПоследнийРезультатПриема.ПротоколСОшибкой Тогда
			
			СтрокаЭтапа = ТаблицаЭтаповОтправки.Добавить();
			СтрокаЭтапа.ТекстНадписи				= НСтр("ru = 'Не сдано'");
			СтрокаЭтапа.ТекстСтатуса				= НСтр("ru = 'Не сдано'");
			СтрокаЭтапа.Дата						= ДатаИзОтвета(ПоследнийРезультатПриема, НеПолучатьДаты);
			СтрокаЭтапа.КомментарийКСостоянию		= НСтр("ru = 'Исправьте ошибки и отправьте уведомление еще раз'");
			СтрокаЭтапа.НаименованиеПротокола		= НСтр("ru = 'Протокол ошибок'");
			СтрокаЭтапа.Протокол					= ПоследнийРезультатПриема.Ссылка;
			СтрокаЭтапа.СостояниеСдачиОтчетности	= Перечисления.СостояниеСдачиОтчетности.ОтрицательныйРезультатДокументооборота;
			СтрокаЭтапа.ЭтапПройден					= Истина;
			
		Иначе
			
			СтрокаЭтапа = ТаблицаЭтаповОтправки.Добавить();
			СтрокаЭтапа.ТекстНадписи				= НСтр("ru = 'Сдано'");
			СтрокаЭтапа.ТекстСтатуса				= НСтр("ru = 'Сдано'");
			СтрокаЭтапа.Дата						= ДатаИзОтвета(СтрРезультатыПриемаПредставлениеНО, НеПолучатьДаты);
			СтрокаЭтапа.КомментарийКСостоянию		= "";
			СтрокаЭтапа.НаименованиеПротокола		= НСтр("ru = 'Протокол приема'");
			СтрокаЭтапа.Протокол					= ?(СтрРезультатыПриемаПредставлениеНО.Количество() > 0, СтрРезультатыПриемаПредставлениеНО[0].Ссылка, "");
			СтрокаЭтапа.СостояниеСдачиОтчетности	= Перечисления.СостояниеСдачиОтчетности.ПоложительныйРезультатДокументооборота;
			СтрокаЭтапа.ЭтапПройден					= СтрРезультатыПриемаПредставлениеНО.Количество() > 0;
			
		КонецЕсли;

		
	Иначе
		
		// выделяем основные типы сообщений
		СтрДекларацииНП 					= СообщенияЦикла.НайтиСтроки(Новый Структура("Тип", Перечисления.ТипыТранспортныхСообщений.ДекларацияНП));// Отправлено оператору
		СтрПодтвержденияДекларацияНО 		= СообщенияЦикла.НайтиСтроки(Новый Структура("Тип", Перечисления.ТипыТранспортныхСообщений.ПодтверждениеДекларацияНО));// Отправлено в ФНС
		СтрИзвещенияПодтверждениеНП 		= СообщенияЦикла.НайтиСтроки(Новый Структура("Тип", Перечисления.ТипыТранспортныхСообщений.ИзвещениеПодтверждениеНП));
		СтрИзвещенияДекларацияНО 			= СообщенияЦикла.НайтиСтроки(Новый Структура("Тип", Перечисления.ТипыТранспортныхСообщений.ИзвещениеДекларацияНО));// Доставлено
		СтрРезультатыПриемаДекларацияНО 	= СообщенияЦикла.НайтиСтроки(Новый Структура("Тип", Перечисления.ТипыТранспортныхСообщений.РезультатПриемаДекларацияНО));// Не принято в обработку ИЛИ Принято в обработку
		СтрИзвещенияРезультатПриемаНП 		= СообщенияЦикла.НайтиСтроки(Новый Структура("Тип", Перечисления.ТипыТранспортныхСообщений.ИзвещениеРезультатПриемаНП));
		СтрРезультатыОбработкиДекларацияНО 	= СообщенияЦикла.НайтиСтроки(Новый Структура("Тип", Перечисления.ТипыТранспортныхСообщений.РезультатОбработкиДекларацияНО));// Сдано, требует уточнения ИЛИ Сдано
		СтрИзвещенияРезультатОбработкиНП	= СообщенияЦикла.НайтиСтроки(Новый Структура("Тип", Перечисления.ТипыТранспортныхСообщений.ИзвещениеРезультатОбработкиНП));
		СтрКритическиеОшибки 				= СообщенияЦикла.НайтиСтроки(Новый Структура("Тип", Перечисления.ТипыТранспортныхСообщений.УведомлениеОбОшибкеФНС));
		
		// Если статус первичного - не отправлено, тогда выходим 
		Если СтрДекларацииНП.Количество() > 0 И СтрДекларацииНП[0].Статус <> Перечисления.СтатусыПисем.Отправленное Тогда
			Возврат ТаблицаЭтаповОтправки;
		КонецЕсли;
		
		СтрокаЭтапа = ТаблицаЭтаповОтправки.Добавить();
		СтрокаЭтапа.ТекстНадписи				= НСтр("ru = 'Отправлено оператору'");
		СтрокаЭтапа.ТекстСтатуса				= НСтр("ru = 'Отправлено оператору'");
		СтрокаЭтапа.Дата						= ДатаОтправки(СтрДекларацииНП, НеПолучатьДаты);
		СтрокаЭтапа.КомментарийКСостоянию		= НСтр("ru = 'Ожидается подтверждение даты отправки'");
		СтрокаЭтапа.НаименованиеПротокола		= "";
		СтрокаЭтапа.Протокол					= "";
		СтрокаЭтапа.Отправка					= ЦиклОбмена;
		СтрокаЭтапа.Сообщение					= СсылкаНаСообщение(СтрДекларацииНП); 
		СтрокаЭтапа.СостояниеСдачиОтчетности	= Перечисления.СостояниеСдачиОтчетности.ДокументооборотНачат;
		СтрокаЭтапа.ЭтапПройден					= СтрДекларацииНП.Количество() > 0;
		
		// Отправлено в ФНС
		СтрокаЭтапа = ТаблицаЭтаповОтправки.Добавить();
		СтрокаЭтапа.ТекстНадписи				= НСтр("ru = 'Отправлено в ФНС'");
		СтрокаЭтапа.ТекстСтатуса				= НСтр("ru = 'Отправлено в ФНС'");
		СтрокаЭтапа.Дата						= ДатаИзОтвета(СтрПодтвержденияДекларацияНО, НеПолучатьДаты);;
		СтрокаЭтапа.КомментарийКСостоянию		= НСтр("ru = 'Ожидается прием отчета в обработку'");
		СтрокаЭтапа.НаименованиеПротокола		= "Подтверждение даты отправки";
		СтрокаЭтапа.Протокол					= СсылкаНаСообщение(СтрПодтвержденияДекларацияНО);
		СтрокаЭтапа.Отправка					= ЦиклОбмена;
		СтрокаЭтапа.Сообщение					= СсылкаНаСообщение(СтрПодтвержденияДекларацияНО); 
		СтрокаЭтапа.СостояниеСдачиОтчетности	= Перечисления.СостояниеСдачиОтчетности.ДокументооборотНачат;
		СтрокаЭтапа.ЭтапПройден					= СтрПодтвержденияДекларацияНО.Количество() > 0;
			
		// определяем наиболее свежее сообщение с результатом приема
		ПоследнийРезультатПриема = Неопределено;
		Для Каждого СтрРезультатПриемаДекларацияНО Из СтрРезультатыПриемаДекларацияНО Цикл
			Если ПоследнийРезультатПриема = Неопределено ИЛИ СтрРезультатПриемаДекларацияНО.Дата > ПоследнийРезультатПриема.Дата Тогда
				ПоследнийРезультатПриема = СтрРезультатПриемаДекларацияНО;
			КонецЕсли;
		КонецЦикла;
		
		// если результат приема отрицательный, то выводим соответствующее сообщение
		Если ПоследнийРезультатПриема <> Неопределено И ПоследнийРезультатПриема.ПротоколСОшибкой Тогда
			
			СтрокаЭтапа = ТаблицаЭтаповОтправки.Добавить();
			СтрокаЭтапа.ТекстНадписи				= НСтр("ru = 'Не принято в обработку'");
			СтрокаЭтапа.ТекстСтатуса				= НСтр("ru = 'Не принято в обработку'");
			СтрокаЭтапа.Дата						= ДатаИзОтвета(ПоследнийРезультатПриема, НеПолучатьДаты);;
			СтрокаЭтапа.КомментарийКСостоянию		= НСтр("ru = 'Исправьте ошибки и отправьте отчет еще раз'");
			СтрокаЭтапа.НаименованиеПротокола		= НСтр("ru = 'Уведомление об отказе'");
			СтрокаЭтапа.Протокол					= ПоследнийРезультатПриема.Ссылка;
			СтрокаЭтапа.Отправка					= ЦиклОбмена;
			СтрокаЭтапа.Сообщение					= ПоследнийРезультатПриема.Ссылка; 
			СтрокаЭтапа.СостояниеСдачиОтчетности	= Перечисления.СостояниеСдачиОтчетности.ОтрицательныйРезультатДокументооборота;
			СтрокаЭтапа.ЭтапПройден					= СтрРезультатыПриемаДекларацияНО.Количество() > 0;
			
		Иначе
			
			// Принято в обработку
			СтрокаЭтапа = ТаблицаЭтаповОтправки.Добавить();
			СтрокаЭтапа.ТекстНадписи				= НСтр("ru = 'Принято в обработку'");
			СтрокаЭтапа.ТекстСтатуса				= НСтр("ru = 'Принято в обработку'");
			СтрокаЭтапа.Дата						= ДатаИзОтвета(СтрРезультатыПриемаДекларацияНО, НеПолучатьДаты);
			СтрокаЭтапа.КомментарийКСостоянию		= НСтр("ru = 'Ожидается результат обработки отчета'");
			СтрокаЭтапа.НаименованиеПротокола		= НСтр("ru = 'Квитанция о приеме'");
			СтрокаЭтапа.Протокол					= СсылкаНаСообщение(СтрРезультатыПриемаДекларацияНО);
			СтрокаЭтапа.Отправка					= ЦиклОбмена;
			СтрокаЭтапа.Сообщение					= СсылкаНаСообщение(СтрРезультатыПриемаДекларацияНО); 
			СтрокаЭтапа.СостояниеСдачиОтчетности	= Перечисления.СостояниеСдачиОтчетности.ДокументооборотНачат;
			СтрокаЭтапа.ЭтапПройден					= СтрРезультатыПриемаДекларацияНО.Количество() > 0;
			
			// определяем наиболее свежее сообщение с результатом обработки
			ПоследнийРезультатОбработки = Неопределено;
			Для Каждого СтрРезультатОбработки Из СтрРезультатыОбработкиДекларацияНО Цикл
				Если ПоследнийРезультатОбработки = Неопределено ИЛИ СтрРезультатОбработки.Дата > ПоследнийРезультатОбработки.Дата Тогда
					ПоследнийРезультатОбработки = СтрРезультатОбработки;
				КонецЕсли;
			КонецЦикла;
			
			// если результат обработки отрицательный, то выводим соответствующее сообщение
			Если ПоследнийРезультатОбработки <> Неопределено И ПоследнийРезультатОбработки.ПротоколСОшибкой Тогда
				
				// Сдано, требует уточнения
				СтрокаЭтапа = ТаблицаЭтаповОтправки.Добавить();
				СтрокаЭтапа.ТекстНадписи				= НСтр("ru = 'Сдано, требует уточнения'");
				СтрокаЭтапа.ТекстСтатуса				= НСтр("ru = 'Сдано, требует уточнения'");
				СтрокаЭтапа.Дата						= ДатаИзОтвета(ПоследнийРезультатОбработки, НеПолучатьДаты);
				СтрокаЭтапа.КомментарийКСостоянию		= НСтр("ru = 'Подготовьте и отправьте корректирующий отчет'");
				СтрокаЭтапа.НаименованиеПротокола		= НСтр("ru = 'Уведомление об уточнении'");
				СтрокаЭтапа.Протокол					= ПоследнийРезультатОбработки.Ссылка;
				СтрокаЭтапа.Отправка					= ЦиклОбмена;
				СтрокаЭтапа.Сообщение					= ПоследнийРезультатОбработки.Ссылка;
				СтрокаЭтапа.СостояниеСдачиОтчетности	= Перечисления.СостояниеСдачиОтчетности.ОтрицательныйРезультатДокументооборота;
				СтрокаЭтапа.ЭтапПройден					= СтрРезультатыОбработкиДекларацияНО.Количество() > 0;
				
			Иначе
				
				Текст = НСтр("ru = 'Сдано'");
				
				Если ЭтоНужныйВидОтчетаСПАРК(СсылкаНаОтчет) Тогда
					СвойстваОтчета = СвойстваОтчетаСПАРК(СсылкаНаОтчет);
					ИмяПеречисленияСПАРК = "СтатусыОтправкиВСПАРК"; // Чтобы расширенная проверка не выдавала ошибку
					Если СвойстваОтчета <> Неопределено
						И СвойстваОтчета.СтатусОтправки = Перечисления[ИмяПеречисленияСПАРК].ОтправленУспешно Тогда
						
						Текст = НСтр("ru = 'Сдано в ФНС, копия передана в СПАРК'");
						
					КонецЕсли;
					
				КонецЕсли;
				
				// Сдано
				СтрокаЭтапа = ТаблицаЭтаповОтправки.Добавить();
				СтрокаЭтапа.ТекстНадписи				= Текст;
				СтрокаЭтапа.ТекстСтатуса				= Текст;
				СтрокаЭтапа.Дата						= ДатаИзОтвета(СтрРезультатыОбработкиДекларацияНО, НеПолучатьДаты);
				СтрокаЭтапа.КомментарийКСостоянию		= НСтр("ru = ''");
				СтрокаЭтапа.НаименованиеПротокола		= НСтр("ru = 'Извещение о вводе'");
				СтрокаЭтапа.Протокол					= СсылкаНаСообщение(СтрРезультатыОбработкиДекларацияНО);
				СтрокаЭтапа.Отправка					= ЦиклОбмена;
				СтрокаЭтапа.Сообщение					= СсылкаНаСообщение(СтрРезультатыОбработкиДекларацияНО);
				СтрокаЭтапа.СостояниеСдачиОтчетности	= Перечисления.СостояниеСдачиОтчетности.ПоложительныйРезультатДокументооборота;
				СтрокаЭтапа.ЭтапПройден					= СтрРезультатыОбработкиДекларацияНО.Количество() > 0;
				
			КонецЕсли;
			
		КонецЕсли;
		
	КонецЕсли;
			
	Возврат ТаблицаЭтаповОтправки;
	
КонецФункции

Функция ТаблицаЭтаповОтправкиПФР(СсылкаНаОтчет, ДополнительныеПараметры)
	
	Если ТипЗнч(СсылкаНаОтчет) = Тип("ДокументСсылка.ЗаявленияПоЭлДокументооборотуСПФР") Тогда
		ПредставлениеВИмПадеже = НСтр("ru = 'заявление'");
		ПредставлениеВРодПадеже = НСтр("ru = 'заявления'");
	Иначе
		ПредставлениеВИмПадеже = НСтр("ru = 'отчет'");
		ПредставлениеВРодПадеже = НСтр("ru = 'отчета'");
	КонецЕсли;
	
	НеПолучатьДаты = НЕ (ТипЗнч(ДополнительныеПараметры) = Тип("Структура")
		И ДополнительныеПараметры.Свойство("ПолучатьДаты")
		И ДополнительныеПараметры.ПолучатьДаты);
		
	ПоказыватьПомеченныеНаУдаление = ТипЗнч(ДополнительныеПараметры) = Тип("Структура")
		И ДополнительныеПараметры.Свойство("ПоказыватьПомеченныеНаУдаление")
		И ДополнительныеПараметры.ПоказыватьПомеченныеНаУдаление;
	
	НачальныйСтатус = НачальныйСтатусПриФормированииТаблицыЭтаповОтправки(СсылкаНаОтчет);
	
	ТаблицаЭтаповОтправки = ЗаготовкаТаблицыЭтаповОтправки();
	
	// Не отправлено
	СтрокаЭтапа = ТаблицаЭтаповОтправки.Добавить();
	СтрокаЭтапа.ТекстНадписи				= НачальныйСтатус;
	СтрокаЭтапа.ТекстСтатуса				= НачальныйСтатус;
	СтрокаЭтапа.Дата						= Дата(1,1,1);
	СтрокаЭтапа.КомментарийКСостоянию		= КомментарийКСостояниюНеОтправленоПФР(СсылкаНаОтчет);
	СтрокаЭтапа.НаименованиеПротокола		= "";
	СтрокаЭтапа.Протокол					= "";
	СтрокаЭтапа.СостояниеСдачиОтчетности	= Перечисления.СостояниеСдачиОтчетности.ДокументооборотНеНачат;
	СтрокаЭтапа.ЭтапПройден					= Истина;
	
	Если ТипЗнч(ДополнительныеПараметры) = Тип("Структура")
		И ДополнительныеПараметры.Свойство("Отправка") Тогда
		ЦиклОбмена = ДополнительныеПараметры.Отправка;
	Иначе
		ЦиклОбмена = ДокументооборотСКОВызовСервера.ПолучитьПоследнийЦиклОбмена(СсылкаНаОтчет);
	КонецЕсли;
	
	// если у пользователя недостаточно прав для получения сведений об отправке, то скроем панель
	Если НЕ ЗначениеЗаполнено(ЦиклОбмена) Тогда
		Возврат ТаблицаЭтаповОтправки;
	КонецЕсли;
	
	// получаем список всех не помеченных на удаление сообщений последнего цикла
	СообщенияЦикла = ПолучитьСообщенияЦиклаОбмена(ЦиклОбмена,,ПоказыватьПомеченныеНаУдаление,Истина);
	
	// выделяем основные типы сообщений
	СтрПервичныеСообщения 						= СообщенияЦикла.НайтиСтроки(Новый Структура("Тип", Перечисления.ТипыТранспортныхСообщений.ПервичноеСообщениеСодержащееОтчетностьПФР));
	СтрКвитанцииОПриеме 						= СообщенияЦикла.НайтиСтроки(Новый Структура("Тип", Перечисления.ТипыТранспортныхСообщений.ПодтверждениеПолученияОтчетностиПФР));
	СтрПротоколыВходногоКонтроля 				= СообщенияЦикла.НайтиСтроки(Новый Структура("Тип", Перечисления.ТипыТранспортныхСообщений.ПротоколПФР));
	СтрПереподписанныеПротоколыВходногоКонтроля = СообщенияЦикла.НайтиСтроки(Новый Структура("Тип", Перечисления.ТипыТранспортныхСообщений.ПротоколКвитанцияПФР));
	СтрКритическиеОшибки			 			= СообщенияЦикла.НайтиСтроки(Новый Структура("Тип", Перечисления.ТипыТранспортныхСообщений.КритическаяОшибка));
	
	// Если статус первичного - не отправлено, тогда выходим 
	Если СтрПервичныеСообщения.Количество() > 0 И СтрПервичныеСообщения[0].Статус <> Перечисления.СтатусыПисем.Отправленное Тогда
		Возврат ТаблицаЭтаповОтправки;
	КонецЕсли;
		
	// Отправлено в ПФР
	СтрокаЭтапа = ТаблицаЭтаповОтправки.Добавить();
	СтрокаЭтапа.ТекстНадписи				= НСтр("ru = 'Отправлено в ПФР'");
	СтрокаЭтапа.ТекстСтатуса				= НСтр("ru = 'Отправлено в ПФР'");
	СтрокаЭтапа.Дата						= ДатаОтправки(СтрПервичныеСообщения, НеПолучатьДаты);;
	СтрокаЭтапа.КомментарийКСостоянию		= НСтр("ru = 'Ожидается результат обработки '") + ПредставлениеВРодПадеже;
	СтрокаЭтапа.НаименованиеПротокола		= "";
	СтрокаЭтапа.Протокол					= "";
	СтрокаЭтапа.СостояниеСдачиОтчетности	= Перечисления.СостояниеСдачиОтчетности.ДокументооборотНачат;
	СтрокаЭтапа.ЭтапПройден					= СтрПервичныеСообщения.Количество() > 0;
	
	// Принято в обработку
	СтрокаЭтапа = ТаблицаЭтаповОтправки.Добавить();
	СтрокаЭтапа.ТекстНадписи				= НСтр("ru = 'Принято в обработку'");
	СтрокаЭтапа.ТекстСтатуса				= НСтр("ru = 'Принято в обработку'");
	СтрокаЭтапа.Дата						= ДатаИзОтвета(СтрКвитанцииОПриеме, НеПолучатьДаты);;
	СтрокаЭтапа.КомментарийКСостоянию		= НСтр("ru = 'Ожидается результат обработки '") + ПредставлениеВРодПадеже;
	СтрокаЭтапа.НаименованиеПротокола		= НСтр("ru = 'Квитанция о доставке'");
	СтрокаЭтапа.Протокол					= ?(СтрКвитанцииОПриеме.Количество() > 0, СтрКвитанцииОПриеме[0].Ссылка, "");
	СтрокаЭтапа.СостояниеСдачиОтчетности	= Перечисления.СостояниеСдачиОтчетности.ДокументооборотНачат;
	СтрокаЭтапа.ЭтапПройден					= СтрКвитанцииОПриеме.Количество() > 0;
	
	
	ПоследнийПротокол = Неопределено;
	Для Каждого СтрПротокол Из СтрПротоколыВходногоКонтроля Цикл
		Если ПоследнийПротокол = Неопределено ИЛИ СтрПротокол.Дата > ПоследнийПротокол.Дата Тогда
			ПоследнийПротокол = СтрПротокол;
		КонецЕсли;
	КонецЦикла;
	
	АнализПриложенияПротокола = ?(ПоследнийПротокол <> Неопределено
		И (ЦиклОбмена.ВидОтчета = Справочники.ВидыОтправляемыхДокументов.СведенияОЗастрахованныхЛицахСЗВ_М
		ИЛИ ЦиклОбмена.ВидОтчета = Справочники.ВидыОтправляемыхДокументов.СведенияОСтраховомСтажеЗастрахованныхЛицСЗВ_СТАЖ
		ИЛИ ЦиклОбмена.ВидОтчета = Справочники.ВидыОтправляемыхДокументов.ДанныеОКорректировкеСведенийЗастрахованныхЛицСЗВ_КОРР
		ИЛИ ЦиклОбмена.ВидОтчета = Справочники.ВидыОтправляемыхДокументов.СведенияОЗаработкеСтажеЗастрахованныхЛицСЗВ_ИСХ
		ИЛИ ЦиклОбмена.ВидОтчета = Справочники.ВидыОтправляемыхДокументов.ОписьОДВ_1
		ИЛИ ЦиклОбмена.ФорматДокументооборота = Перечисления.ФорматыДокументооборотаСПФР.ЭДОК),
		ОтчетПФРСданЧастично(ПоследнийПротокол,, ЦиклОбмена.ФорматДокументооборота), Неопределено);
	ПротоколСОшибкой = (ПоследнийПротокол <> Неопределено И ПоследнийПротокол.ПротоколСОшибкой); // ошибка - не сдан / сдан частично
	
	Если АнализПриложенияПротокола <> Неопределено И НЕ АнализПриложенияПротокола.ЕстьПриложение
		И (НЕ АнализПриложенияПротокола.ЕстьВложения
		ИЛИ ЦиклОбмена.ФорматДокументооборота = Перечисления.ФорматыДокументооборотаСПФР.ЭДОК) Тогда
		
		// Получен протокол: еще не расшифрован, но отчет поддерживает частичную сдачу, определяемую по приложению
		СтрокаЭтапа = ТаблицаЭтаповОтправки.Добавить();
		СтрокаЭтапа.ТекстНадписи				= НСтр("ru = 'Получен протокол'");
		СтрокаЭтапа.ТекстСтатуса				= НСтр("ru = 'Принято в обработку'");
		СтрокаЭтапа.Дата						= ДатаИзОтвета(ПоследнийПротокол, НеПолучатьДаты);
		СтрокаЭтапа.КомментарийКСостоянию		= НСтр("ru = 'Для расшифровки, обновите состояние, откройте протокол'");
		СтрокаЭтапа.НаименованиеПротокола		= НСтр("ru = 'Протокол приема'");
		СтрокаЭтапа.Протокол					= ПоследнийПротокол.Ссылка;
		СтрокаЭтапа.СостояниеСдачиОтчетности	= Перечисления.СостояниеСдачиОтчетности.ДокументооборотНачат;
		СтрокаЭтапа.ЭтапПройден					= Истина;
		
	ИначеЕсли АнализПриложенияПротокола <> Неопределено И АнализПриложенияПротокола.СданЧастично Тогда
		
		// Сдан частично
		СтрокаЭтапа = ТаблицаЭтаповОтправки.Добавить();
		СтрокаЭтапа.ТекстНадписи				= НСтр("ru = 'Сдан частично'");
		СтрокаЭтапа.ТекстСтатуса				= НСтр("ru = 'Сдан частично'");
		СтрокаЭтапа.Дата						= ДатаИзОтвета(ПоследнийПротокол, НеПолучатьДаты);
		СтрокаЭтапа.КомментарийКСостоянию		= СтрШаблон(НСтр("ru = 'Исправьте ошибки и отправьте %1 еще раз'"), ПредставлениеВИмПадеже);
		СтрокаЭтапа.НаименованиеПротокола		= НСтр("ru = 'Протокол приема'");
		СтрокаЭтапа.Протокол					= ПоследнийПротокол.Ссылка;
		СтрокаЭтапа.СостояниеСдачиОтчетности	= Перечисления.СостояниеСдачиОтчетности.ОтрицательныйРезультатДокументооборота;
		СтрокаЭтапа.ЭтапПройден					= Истина;
		
	ИначеЕсли ПротоколСОшибкой ИЛИ АнализПриложенияПротокола <> Неопределено И АнализПриложенияПротокола.НеСдан Тогда

		// Не сдано
		СтрокаЭтапа = ТаблицаЭтаповОтправки.Добавить();
		СтрокаЭтапа.ТекстНадписи				= НСтр("ru = 'Не сдано'");
		СтрокаЭтапа.ТекстСтатуса				= НСтр("ru = 'Не сдано'");
		СтрокаЭтапа.Дата						= ДатаИзОтвета(ПоследнийПротокол, НеПолучатьДаты);
		СтрокаЭтапа.КомментарийКСостоянию		= СтрШаблон(НСтр("ru = 'Исправьте ошибки и отправьте %1 еще раз'"), ПредставлениеВИмПадеже);
		СтрокаЭтапа.НаименованиеПротокола		= НСтр("ru = 'Протокол ошибок'");
		СтрокаЭтапа.Протокол					= ПоследнийПротокол.Ссылка;
		СтрокаЭтапа.СостояниеСдачиОтчетности	= Перечисления.СостояниеСдачиОтчетности.ОтрицательныйРезультатДокументооборота;
		СтрокаЭтапа.ЭтапПройден					= Истина;
		
	Иначе
		
		// сдано
		СтрокаЭтапа = ТаблицаЭтаповОтправки.Добавить();
		СтрокаЭтапа.ТекстНадписи				= НСтр("ru = 'Сдано'");
		СтрокаЭтапа.ТекстСтатуса				= НСтр("ru = 'Сдано'");
		СтрокаЭтапа.Дата						= ДатаИзОтвета(СтрПротоколыВходногоКонтроля, НеПолучатьДаты);
		СтрокаЭтапа.КомментарийКСостоянию		= "";
		СтрокаЭтапа.НаименованиеПротокола		= НСтр("ru = 'Протокол о сдаче'");
		СтрокаЭтапа.Протокол					= ?(СтрПротоколыВходногоКонтроля.Количество() > 0, СтрПротоколыВходногоКонтроля[0].Ссылка, "");
		СтрокаЭтапа.СостояниеСдачиОтчетности	= Перечисления.СостояниеСдачиОтчетности.ПоложительныйРезультатДокументооборота;
		СтрокаЭтапа.ЭтапПройден					= СтрПротоколыВходногоКонтроля.Количество() > 0;
		
	КонецЕсли;
	
	Возврат ТаблицаЭтаповОтправки;
	
КонецФункции

Функция ТаблицаЭтаповОтправкиПерепискиСКонтролирующимиОрганами(Объект, ДополнительныеПараметры)
	
	НеПолучатьДаты = НЕ (ТипЗнч(ДополнительныеПараметры) = Тип("Структура")
		И ДополнительныеПараметры.Свойство("ПолучатьДаты")
		И ДополнительныеПараметры.ПолучатьДаты);
		
	ПоказыватьПомеченныеНаУдаление = ТипЗнч(ДополнительныеПараметры) = Тип("Структура")
		И ДополнительныеПараметры.Свойство("ПоказыватьПомеченныеНаУдаление")
		И ДополнительныеПараметры.ПоказыватьПомеченныеНаУдаление;
	
	ТаблицаЭтаповОтправки = ЗаготовкаТаблицыЭтаповОтправки();
	
	Если ТипЗнч(ДополнительныеПараметры) = Тип("Структура")
		И ДополнительныеПараметры.Свойство("Отправка") Тогда
		ЦиклОбмена = ДополнительныеПараметры.Отправка;
	Иначе
		ЦиклОбмена = ДокументооборотСКОВызовСервера.ПолучитьПоследнийЦиклОбмена(Объект.Ссылка);
	КонецЕсли;
	
	СообщенияЦикла = ПолучитьСообщенияЦиклаОбмена(ЦиклОбмена,,ПоказыватьПомеченныеНаУдаление,Истина);

	// если сообщение входящее, то выводим дату получения
	Если Объект.Статус = Перечисления.СтатусыПисем.Полученное Тогда
		
		СтрокаЭтапа = ТаблицаЭтаповОтправки.Добавить();
		СтрокаЭтапа.ТекстНадписи				= НСтр("ru = 'Отправлено'");
		СтрокаЭтапа.ТекстСтатуса				= НСтр("ru = 'Отправлено'");
		СтрокаЭтапа.Дата						= Объект.ДатаОтправки;
		СтрокаЭтапа.КомментарийКСостоянию		= "";
		СтрокаЭтапа.НаименованиеПротокола		= "";
		СтрокаЭтапа.Протокол					= "";
		СтрокаЭтапа.СостояниеСдачиОтчетности	= Перечисления.СостояниеСдачиОтчетности.ОтправленоИзКонтролирующегоОргана;
		СтрокаЭтапа.ЭтапПройден					= Истина;
		
		СтрокаЭтапа = ТаблицаЭтаповОтправки.Добавить();
		СтрокаЭтапа.ТекстНадписи				= НСтр("ru = 'Получено'");
		СтрокаЭтапа.ТекстСтатуса				= НСтр("ru = 'Получено'");
		СтрокаЭтапа.Дата						= Объект.ДатаСообщения;
		СтрокаЭтапа.КомментарийКСостоянию		= "";
		СтрокаЭтапа.НаименованиеПротокола		= "";
		СтрокаЭтапа.Протокол					= "";
		СтрокаЭтапа.СостояниеСдачиОтчетности	= Перечисления.СостояниеСдачиОтчетности.ПриемПодтвержден;
		СтрокаЭтапа.ЭтапПройден					= Истина;

	Иначе
		
		// Не отправлено
		СтрокаЭтапа = ТаблицаЭтаповОтправки.Добавить();
		СтрокаЭтапа.ТекстНадписи				= НСтр("ru = 'Не отправлено'");
		СтрокаЭтапа.ТекстСтатуса				= НСтр("ru = 'Не отправлено'");
		СтрокаЭтапа.Дата						= Дата(1,1,1);
		СтрокаЭтапа.КомментарийКСостоянию		= "";
		СтрокаЭтапа.НаименованиеПротокола		= "";
		СтрокаЭтапа.Протокол					= "";
		СтрокаЭтапа.СостояниеСдачиОтчетности	= Перечисления.СостояниеСдачиОтчетности.ДокументооборотНеНачат;
		СтрокаЭтапа.ЭтапПройден					= Истина;
	
		Если НЕ ЗначениеЗаполнено(ЦиклОбмена) Тогда
			Возврат ТаблицаЭтаповОтправки;
		КонецЕсли;
		
		Если Объект.Тип = Перечисления.ТипыПерепискиСКонтролирующимиОрганами.ПерепискаСПФР Тогда
			
			// выделяем основные типы сообщений
			СтрПервичныеСообщения 	= СообщенияЦикла.НайтиСтроки(Новый Структура("Тип", Перечисления.ТипыТранспортныхСообщений.ПисьмоИсходящееПФР));
			СтрКвитанции 			= СообщенияЦикла.НайтиСтроки(Новый Структура("Тип", Перечисления.ТипыТранспортныхСообщений.ПисьмоИсходящееКвитанцияПФР));
			
			Если СтрПервичныеСообщения.Количество() > 0 И СтрПервичныеСообщения[0].Статус = Перечисления.СтатусыПисем.Исходящее Тогда
				Возврат ТаблицаЭтаповОтправки;
			КонецЕсли;
			
			Если НеПолучатьДаты Тогда
				ДатаОтправки 			= Дата(1,1,1);
				ДатаПолученияКвитанции 	= Дата(1,1,1);
			Иначе
				ДатаОтправки 			= ?(ЗначениеЗаполнено(Объект.ДатаСообщения) , " " + Формат(Объект.ДатаСообщения, "ДЛФ=DT"), Дата(1,1,1));
				ДатаПолученияКвитанции 	= ?(СтрКвитанции.Количество() > 0, СтрКвитанции[0].ДатаТранспорта, Дата(1,1,1));
			КонецЕсли;
			
			СтрокаЭтапа = ТаблицаЭтаповОтправки.Добавить();
			СтрокаЭтапа.ТекстНадписи				= НСтр("ru = 'Отправлено в ПФР'");
			СтрокаЭтапа.ТекстСтатуса				= НСтр("ru = 'Отправлено в ПФР'");
			СтрокаЭтапа.Дата						= ДатаОтправки;
			СтрокаЭтапа.КомментарийКСостоянию		= НСтр("ru = 'Ожидается доставка письма в ПФР'");
			СтрокаЭтапа.НаименованиеПротокола		= "";
			СтрокаЭтапа.Протокол					= "";
			СтрокаЭтапа.СостояниеСдачиОтчетности	= Перечисления.СостояниеСдачиОтчетности.ДокументооборотНачат;
			СтрокаЭтапа.ЭтапПройден					= СтрПервичныеСообщения.Количество() > 0;

			СтрокаЭтапа = ТаблицаЭтаповОтправки.Добавить();
			СтрокаЭтапа.ТекстНадписи				= НСтр("ru = 'Доставлено в ПФР'");
			СтрокаЭтапа.ТекстСтатуса				= НСтр("ru = 'Доставлено'");
			СтрокаЭтапа.Дата						= ДатаПолученияКвитанции;
			СтрокаЭтапа.КомментарийКСостоянию		= "";
			СтрокаЭтапа.НаименованиеПротокола		= НСтр("ru = 'Квитанция о получении'");
			СтрокаЭтапа.Протокол					= ?(СтрКвитанции.Количество() > 0, СтрКвитанции[0].Ссылка, "");
			СтрокаЭтапа.СостояниеСдачиОтчетности	= Перечисления.СостояниеСдачиОтчетности.ПоложительныйРезультатДокументооборота;
			СтрокаЭтапа.ЭтапПройден					= СтрКвитанции.Количество() > 0;
			
		// переписка с Росстатом
		ИначеЕсли Объект.Тип = Перечисления.ТипыПерепискиСКонтролирующимиОрганами.ПерепискаСФСГС Тогда
			
			// выделяем основные типы сообщений
			СтрПервичныеСообщения 					= СообщенияЦикла.НайтиСтроки(Новый Структура("Тип", Перечисления.ТипыТранспортныхСообщений.ПисьмоИсходящееФСГС));
			СтрПодтвержденияДатыОтправки 			= СообщенияЦикла.НайтиСтроки(Новый Структура("Тип", Перечисления.ТипыТранспортныхСообщений.ПодтверждениеДатыОтправкиФСГС));
			СтрИзвещенияПодтвержденияДатыОтправки 	= СообщенияЦикла.НайтиСтроки(Новый Структура("Тип", Перечисления.ТипыТранспортныхСообщений.ИзвещениеПодтверждениеДатыОтправкиФСГС));
			СтрКвитанции 							= СообщенияЦикла.НайтиСтроки(Новый Структура("Тип", Перечисления.ТипыТранспортныхСообщений.ИзвещениеПисьмоИсходящееФСГС));
			
			Если СтрПервичныеСообщения.Количество() > 0 И СтрПервичныеСообщения[0].Статус = Перечисления.СтатусыПисем.Исходящее Тогда
				Возврат ТаблицаЭтаповОтправки;
			КонецЕсли;
			
			Если НеПолучатьДаты Тогда
				ДатаОтправкиПервичного 			= Дата(1,1,1);
				ДатаПолученияКвитанции 			= Дата(1,1,1);
			Иначе
				ДатаОтправкиПервичного 			= ?(СтрПервичныеСообщения.Количество() > 0, СтрПервичныеСообщения[0].Дата, Дата(1,1,1));
				ДатаПолученияКвитанции 			= ?(СтрКвитанции.Количество() > 0, СтрКвитанции[0].ДатаТранспорта, Дата(1,1,1));
			КонецЕсли;
						
			СтрокаЭтапа = ТаблицаЭтаповОтправки.Добавить();
			СтрокаЭтапа.ТекстНадписи				= НСтр("ru = 'Отправлено оператору'");
			СтрокаЭтапа.ТекстСтатуса				= НСтр("ru = 'Отправлено оператору'");
			СтрокаЭтапа.Дата						= ДатаОтправкиПервичного;
			СтрокаЭтапа.КомментарийКСостоянию		= НСтр("ru = 'Ожидается подтверждение даты отправки'");
			СтрокаЭтапа.НаименованиеПротокола		= "";
			СтрокаЭтапа.Протокол					= "";
			СтрокаЭтапа.СостояниеСдачиОтчетности	= Перечисления.СостояниеСдачиОтчетности.ДокументооборотНачат;
			СтрокаЭтапа.ЭтапПройден					= СтрПервичныеСообщения.Количество() > 0;
			
			СтрокаЭтапа = ТаблицаЭтаповОтправки.Добавить();
			СтрокаЭтапа.ТекстНадписи				= НСтр("ru = 'Отправлено в Росстат'");
			СтрокаЭтапа.ТекстСтатуса				= НСтр("ru = 'Отправлено в Росстат'");
			СтрокаЭтапа.Дата						= ДатаИзОтвета(СтрПодтвержденияДатыОтправки, НеПолучатьДаты);
			СтрокаЭтапа.КомментарийКСостоянию		= НСтр("ru = 'Ожидается доставка письма в Росстат'");
			СтрокаЭтапа.НаименованиеПротокола		= НСтр("ru = 'Подтверждение даты отправки'");
			СтрокаЭтапа.Протокол					= ?(СтрПодтвержденияДатыОтправки.Количество() > 0, СтрПодтвержденияДатыОтправки[0].Ссылка, "");
			СтрокаЭтапа.СостояниеСдачиОтчетности	= Перечисления.СостояниеСдачиОтчетности.ДокументооборотНачат;
			СтрокаЭтапа.ЭтапПройден					= СтрПодтвержденияДатыОтправки.Количество() > 0;
			
			СтрокаЭтапа = ТаблицаЭтаповОтправки.Добавить();
			СтрокаЭтапа.ТекстНадписи				= НСтр("ru = 'Доставлено в Росстат'");
			СтрокаЭтапа.ТекстСтатуса				= НСтр("ru = 'Доставлено'");
			СтрокаЭтапа.Дата						= ДатаИзОтвета(СтрКвитанции, НеПолучатьДаты);
			СтрокаЭтапа.КомментарийКСостоянию		= "";
			СтрокаЭтапа.НаименованиеПротокола		= НСтр("ru = 'Извещение о получении'");
			СтрокаЭтапа.Протокол					= ?(СтрКвитанции.Количество() > 0, СтрКвитанции[0].Ссылка, "");
			СтрокаЭтапа.СостояниеСдачиОтчетности	= Перечисления.СостояниеСдачиОтчетности.ПоложительныйРезультатДокументооборота;
			СтрокаЭтапа.ЭтапПройден					= СтрКвитанции.Количество() > 0;
			
		Иначе // если переписка с ФНС
			
			// выделяем основные типы сообщений
			СтрОбращенияНП 					= СообщенияЦикла.НайтиСтроки(Новый Структура("Тип", Перечисления.ТипыТранспортныхСообщений.ОбращениеНП));
			СтрПодтвержденияОбращениеНО 	= СообщенияЦикла.НайтиСтроки(Новый Структура("Тип", Перечисления.ТипыТранспортныхСообщений.ПодтверждениеОбращениеНО));
			СтрИзвещенияПодтверждениеНП 	= СообщенияЦикла.НайтиСтроки(Новый Структура("Тип", Перечисления.ТипыТранспортныхСообщений.ИзвещениеПодтверждениеНП));
			СтрИзвещенияОбращениеНО 		= СообщенияЦикла.НайтиСтроки(Новый Структура("Тип", Перечисления.ТипыТранспортныхСообщений.ИзвещениеОбращениеНО));
			// эти статусы есть только в случае ошибки
			СтрРезультатыПриемаОбращениеНО 	= СообщенияЦикла.НайтиСтроки(Новый Структура("Тип", Перечисления.ТипыТранспортныхСообщений.РезультатПриемаОбращениеНО));
			СтрИзвещенияРезультатПриемаНП 	= СообщенияЦикла.НайтиСтроки(Новый Структура("Тип", Перечисления.ТипыТранспортныхСообщений.ИзвещениеРезультатПриемаНП));
			
			Если СтрОбращенияНП.Количество() > 0 И СтрОбращенияНП[0].Статус = Перечисления.СтатусыПисем.Исходящее Тогда
				Возврат ТаблицаЭтаповОтправки;
			КонецЕсли;

			Если НеПолучатьДаты Тогда
				ДатаОтправкиПервичного 	= Дата(1,1,1);
			Иначе
				ДатаОтправкиПервичного 	= ?(СтрОбращенияНП.Количество() > 0, СтрОбращенияНП[0].ДатаТранспорта, Дата(1,1,1));
			КонецЕсли;
			
			СтрокаЭтапа = ТаблицаЭтаповОтправки.Добавить();
			СтрокаЭтапа.ТекстНадписи				= НСтр("ru = 'Отправлено оператору'");
			СтрокаЭтапа.ТекстСтатуса				= НСтр("ru = 'Отправлено оператору'");
			СтрокаЭтапа.Дата						= ДатаОтправкиПервичного;
			СтрокаЭтапа.КомментарийКСостоянию		= НСтр("ru = 'Ожидается подтверждение даты отправки'");
			СтрокаЭтапа.НаименованиеПротокола		= "";
			СтрокаЭтапа.Протокол					= "";
			СтрокаЭтапа.СостояниеСдачиОтчетности	= Перечисления.СостояниеСдачиОтчетности.ДокументооборотНачат;
			СтрокаЭтапа.ЭтапПройден					= СтрОбращенияНП.Количество() > 0;
					
			СтрокаЭтапа = ТаблицаЭтаповОтправки.Добавить();
			СтрокаЭтапа.ТекстНадписи				= НСтр("ru = 'Отправлено в ФНС'");
			СтрокаЭтапа.ТекстСтатуса				= НСтр("ru = 'Отправлено в ФНС'");
			СтрокаЭтапа.Дата						= ДатаИзОтвета(СтрПодтвержденияОбращениеНО, НеПолучатьДаты);;
			СтрокаЭтапа.КомментарийКСостоянию		= НСтр("ru = 'Ожидается доставка письма в ФНС'");
			СтрокаЭтапа.НаименованиеПротокола		= НСтр("ru = 'Подтверждение даты отправки'");
			СтрокаЭтапа.Протокол					= ?(СтрПодтвержденияОбращениеНО.Количество() > 0, СтрПодтвержденияОбращениеНО[0].Ссылка, Дата(1,1,1));
			СтрокаЭтапа.СостояниеСдачиОтчетности	= Перечисления.СостояниеСдачиОтчетности.ДокументооборотНачат;
			СтрокаЭтапа.ЭтапПройден					= СтрПодтвержденияОбращениеНО.Количество() > 0;
			
			Если СтрРезультатыПриемаОбращениеНО.Количество() <> 0 Тогда
				
				СтрокаЭтапа = ТаблицаЭтаповОтправки.Добавить();
				СтрокаЭтапа.ТекстНадписи				= НСтр("ru = 'Не принято'");
				СтрокаЭтапа.ТекстСтатуса				= НСтр("ru = 'Не принято'");
				СтрокаЭтапа.Дата						= ДатаИзОтвета(СтрРезультатыПриемаОбращениеНО, НеПолучатьДаты);
				СтрокаЭтапа.КомментарийКСостоянию		= "";
				СтрокаЭтапа.НаименованиеПротокола		= НСтр("ru = 'Уведомление об отказе'");
				СтрокаЭтапа.Протокол					= ?(СтрРезультатыПриемаОбращениеНО.Количество() > 0, СтрРезультатыПриемаОбращениеНО[0].Ссылка, Дата(1,1,1));
				СтрокаЭтапа.СостояниеСдачиОтчетности	= Перечисления.СостояниеСдачиОтчетности.ОтрицательныйРезультатДокументооборота;
				СтрокаЭтапа.ЭтапПройден					= СтрРезультатыПриемаОбращениеНО.Количество() > 0;

			Иначе
				
				СтрокаЭтапа = ТаблицаЭтаповОтправки.Добавить();
				СтрокаЭтапа.ТекстНадписи				= НСтр("ru = 'Доставлено в ФНС'");
				СтрокаЭтапа.ТекстСтатуса				= НСтр("ru = 'Доставлено'");
				СтрокаЭтапа.Дата						= ДатаИзОтвета(СтрИзвещенияОбращениеНО, НеПолучатьДаты);
				СтрокаЭтапа.КомментарийКСостоянию		= "";
				СтрокаЭтапа.НаименованиеПротокола		= НСтр("ru = 'Извещение о получении'");
				СтрокаЭтапа.Протокол					= ?(СтрИзвещенияОбращениеНО.Количество() > 0, СтрИзвещенияОбращениеНО[0].Ссылка, Дата(1,1,1));
				СтрокаЭтапа.СостояниеСдачиОтчетности	= Перечисления.СостояниеСдачиОтчетности.ПоложительныйРезультатДокументооборота;
				СтрокаЭтапа.ЭтапПройден					= СтрИзвещенияОбращениеНО.Количество() > 0;
				
			КонецЕсли;
				
		КонецЕсли;
		
	КонецЕсли;
	
	Возврат ТаблицаЭтаповОтправки;
	
КонецФункции

Функция ТаблицаЭтаповОтправкиМакетаПенсионногоДела(Объект, ДополнительныеПараметры)
	
	НеПолучатьДаты = НЕ (ТипЗнч(ДополнительныеПараметры) = Тип("Структура")
		И ДополнительныеПараметры.Свойство("ПолучатьДаты")
		И ДополнительныеПараметры.ПолучатьДаты);
		
	ПоказыватьПомеченныеНаУдаление = ТипЗнч(ДополнительныеПараметры) = Тип("Структура")
		И ДополнительныеПараметры.Свойство("ПоказыватьПомеченныеНаУдаление")
		И ДополнительныеПараметры.ПоказыватьПомеченныеНаУдаление;
	
	НачальныйСтатус = НачальныйСтатусПриФормированииТаблицыЭтаповОтправки(Объект.Ссылка);
	
	ТаблицаЭтаповОтправки = ЗаготовкаТаблицыЭтаповОтправки();
	
	Если ТипЗнч(ДополнительныеПараметры) = Тип("Структура")
		И ДополнительныеПараметры.Свойство("Отправка") Тогда
		ЦиклОбмена = ДополнительныеПараметры.Отправка;
	Иначе
		ЦиклОбмена = ДокументооборотСКОВызовСервера.ПолучитьПоследнийЦиклОбмена(Объект.Ссылка);
	КонецЕсли;
	
	СообщенияЦикла = ПолучитьСообщенияЦиклаОбмена(ЦиклОбмена,,ПоказыватьПомеченныеНаУдаление,Истина);
		
	// Не отправлено
	СтрокаЭтапа = ТаблицаЭтаповОтправки.Добавить();
	СтрокаЭтапа.ТекстНадписи				= НачальныйСтатус;
	СтрокаЭтапа.ТекстСтатуса				= НачальныйСтатус;
	СтрокаЭтапа.Дата						= Дата(1,1,1);
	СтрокаЭтапа.КомментарийКСостоянию		= "";
	СтрокаЭтапа.НаименованиеПротокола		= "";
	СтрокаЭтапа.Протокол					= "";
	СтрокаЭтапа.СостояниеСдачиОтчетности	= Перечисления.СостояниеСдачиОтчетности.ДокументооборотНеНачат;
	СтрокаЭтапа.ЭтапПройден					= Истина;

	Если НЕ ЗначениеЗаполнено(ЦиклОбмена) Тогда
		Возврат ТаблицаЭтаповОтправки;
	КонецЕсли;
	
	Если Объект.Тип = Перечисления.ТипыПерепискиСКонтролирующимиОрганами.ПерепискаСПФР Тогда
		
		// выделяем основные типы сообщений
		СтрПервичныеСообщения 	= СообщенияЦикла.НайтиСтроки(Новый Структура("Тип", Перечисления.ТипыТранспортныхСообщений.ПисьмоИсходящееПФР));
		СтрКвитанции 			= СообщенияЦикла.НайтиСтроки(Новый Структура("Тип", Перечисления.ТипыТранспортныхСообщений.ПисьмоИсходящееКвитанцияПФР));
		
		Если СтрПервичныеСообщения.Количество() > 0 И СтрПервичныеСообщения[0].Статус = Перечисления.СтатусыПисем.Исходящее Тогда
			Возврат ТаблицаЭтаповОтправки;
		КонецЕсли;
		
		Если НеПолучатьДаты Тогда
			ДатаОтправки 			= Дата(1,1,1);
			ДатаПолученияКвитанции 	= Дата(1,1,1);
		Иначе
			ДатаОтправки 			= ?(ЗначениеЗаполнено(Объект.ДатаСоздания) , " " + Формат(Объект.ДатаСоздания, "ДЛФ=DT"), Дата(1,1,1));
			ДатаПолученияКвитанции 	= ?(СтрКвитанции.Количество() > 0, СтрКвитанции[0].ДатаТранспорта, Дата(1,1,1));
		КонецЕсли;
		
		СтрокаЭтапа = ТаблицаЭтаповОтправки.Добавить();
		СтрокаЭтапа.ТекстНадписи				= НСтр("ru = 'Отправлено в ПФР'");
		СтрокаЭтапа.ТекстСтатуса				= НСтр("ru = 'Отправлено в ПФР'");
		СтрокаЭтапа.Дата						= ДатаОтправки;
		СтрокаЭтапа.КомментарийКСостоянию		= НСтр("ru = 'Ожидается доставка письма в ПФР'");
		СтрокаЭтапа.НаименованиеПротокола		= "";
		СтрокаЭтапа.Протокол					= "";
		СтрокаЭтапа.СостояниеСдачиОтчетности	= Перечисления.СостояниеСдачиОтчетности.ДокументооборотНачат;
		СтрокаЭтапа.ЭтапПройден					= СтрПервичныеСообщения.Количество() > 0;

		СтрокаЭтапа = ТаблицаЭтаповОтправки.Добавить();
		СтрокаЭтапа.ТекстНадписи				= НСтр("ru = 'Доставлено в ПФР'");
		СтрокаЭтапа.ТекстСтатуса				= НСтр("ru = 'Доставлено'");
		СтрокаЭтапа.Дата						= ДатаПолученияКвитанции;
		СтрокаЭтапа.КомментарийКСостоянию		= "";
		СтрокаЭтапа.НаименованиеПротокола		= НСтр("ru = 'Квитанция о получении'");
		СтрокаЭтапа.Протокол					= ?(СтрКвитанции.Количество() > 0, СтрКвитанции[0].Ссылка, "");
		СтрокаЭтапа.СостояниеСдачиОтчетности	= Перечисления.СостояниеСдачиОтчетности.ПоложительныйРезультатДокументооборота;
		СтрокаЭтапа.ЭтапПройден					= СтрКвитанции.Количество() > 0;
	КонецЕсли;
	
	Возврат ТаблицаЭтаповОтправки;
	
КонецФункции

Функция ТаблицаЭтаповОтправкиОтветовНаТребованияФНС(СсылкаНаОтчет, ДополнительныеПараметры)
	
	НеПолучатьДаты = НЕ (ТипЗнч(ДополнительныеПараметры) = Тип("Структура")
		И ДополнительныеПараметры.Свойство("ПолучатьДаты")
		И ДополнительныеПараметры.ПолучатьДаты);
		
	ПоказыватьПомеченныеНаУдаление = ТипЗнч(ДополнительныеПараметры) = Тип("Структура")
		И ДополнительныеПараметры.Свойство("ПоказыватьПомеченныеНаУдаление")
		И ДополнительныеПараметры.ПоказыватьПомеченныеНаУдаление;
	
	ТаблицаЭтаповОтправки = ЗаготовкаТаблицыЭтаповОтправки();
	
	СтрокаЭтапа = ТаблицаЭтаповОтправки.Добавить();
	СтрокаЭтапа.ТекстНадписи				= НСтр("ru = 'Не отправлено'");
	СтрокаЭтапа.ТекстСтатуса				= НСтр("ru = 'Не отправлено'");
	СтрокаЭтапа.Дата						= Дата(1,1,1);
	СтрокаЭтапа.КомментарийКСостоянию		= "";
	СтрокаЭтапа.НаименованиеПротокола		= "";
	СтрокаЭтапа.Протокол					= "";
	СтрокаЭтапа.СостояниеСдачиОтчетности	= Перечисления.СостояниеСдачиОтчетности.ДокументооборотНеНачат;
	СтрокаЭтапа.ЭтапПройден					= Истина;
	
	// получаем список сообщений
	Если ТипЗнч(ДополнительныеПараметры) = Тип("Структура")
		И ДополнительныеПараметры.Свойство("Отправка") Тогда
		ЦиклОбмена = ДополнительныеПараметры.Отправка;
	Иначе
		ЦиклОбмена = ДокументооборотСКОВызовСервера.ПолучитьПоследнийЦиклОбмена(СсылкаНаОтчет);
	КонецЕсли;	
	
	Если НЕ ЗначениеЗаполнено(ЦиклОбмена) Тогда
		Возврат ТаблицаЭтаповОтправки;
	КонецЕсли;
	
	СообщенияЦикла = ПолучитьСообщенияЦиклаОбмена(ЦиклОбмена,,ПоказыватьПомеченныеНаУдаление,Истина);
	
	// выделяем основные типы сообщений
	СтрПредставленияНП					= СообщенияЦикла.НайтиСтроки(Новый Структура("Тип", Перечисления.ТипыТранспортныхСообщений.ПредставлениеНП));
	СтрПодтвержденияПредставлениеНО		= СообщенияЦикла.НайтиСтроки(Новый Структура("Тип", Перечисления.ТипыТранспортныхСообщений.ПодтверждениеПредставлениеНО));
	СтрИзвещенияПодтверждениеНП			= СообщенияЦикла.НайтиСтроки(Новый Структура("Тип", Перечисления.ТипыТранспортныхСообщений.ИзвещениеПодтверждениеНП));
	СтрИзвещенияПредставлениеНО			= СообщенияЦикла.НайтиСтроки(Новый Структура("Тип", Перечисления.ТипыТранспортныхСообщений.ИзвещениеПредставлениеНО));
	СтрРезультатыПриемаПредставлениеНО	= СообщенияЦикла.НайтиСтроки(Новый Структура("Тип", Перечисления.ТипыТранспортныхСообщений.РезультатПриемаПредставлениеНО));
	СтрИзвещенияРезультатПриемаНП		= СообщенияЦикла.НайтиСтроки(Новый Структура("Тип", Перечисления.ТипыТранспортныхСообщений.ИзвещениеРезультатПриемаНП));
	
	// Если статус первичного - не отправлено, тогда выходим 
	Если СтрПредставленияНП.Количество() > 0 И СтрПредставленияНП[0].Статус <> Перечисления.СтатусыПисем.Отправленное Тогда
		Возврат ТаблицаЭтаповОтправки;
	КонецЕсли;

	// получаем свойства для анализа
	Если НеПолучатьДаты Тогда
		ДатаОтправкиПервичного 		= Дата(1,1,1);
		ДатаИзПодтверждения			= Дата(1,1,1);
		ДатаИзИзвещенияПолучения	= Дата(1,1,1);
	Иначе
		ДатаОтправкиПервичного 		= ?(СтрПредставленияНП.Количество() > 0, 				СтрПредставленияНП[0].ДатаТранспорта, 											Дата(1,1,1));
		ДатаИзПодтверждения			= ?(СтрПодтвержденияПредставлениеНО.Количество() > 0, 	ДатаИзОтветовКонтролирующихОрганов(СтрПодтвержденияПредставлениеНО[0].Ссылка), 	Дата(1,1,1));
		ДатаИзИзвещенияПолучения	= ?(СтрИзвещенияПредставлениеНО.Количество() > 0,		ДатаИзОтветовКонтролирующихОрганов(СтрИзвещенияПредставлениеНО[0].Ссылка), 		Дата(1,1,1));
	КонецЕсли;
	
	СтрокаЭтапа = ТаблицаЭтаповОтправки.Добавить();
	СтрокаЭтапа.ТекстНадписи				= НСтр("ru = 'Отправлено оператору'");
	СтрокаЭтапа.ТекстСтатуса				= НСтр("ru = 'Отправлено оператору'");
	СтрокаЭтапа.Дата						= ДатаОтправкиПервичного;
	СтрокаЭтапа.КомментарийКСостоянию		= НСтр("ru = 'Ожидается подтверждение даты отправки'");
	СтрокаЭтапа.НаименованиеПротокола		= "";
	СтрокаЭтапа.Протокол					= "";
	СтрокаЭтапа.СостояниеСдачиОтчетности	= Перечисления.СостояниеСдачиОтчетности.ДокументооборотНачат;
	СтрокаЭтапа.ЭтапПройден					= СтрПредставленияНП.Количество() > 0;

	СтрокаЭтапа = ТаблицаЭтаповОтправки.Добавить();
	СтрокаЭтапа.ТекстНадписи				= НСтр("ru = 'Отправлено в ФНС'");
	СтрокаЭтапа.ТекстСтатуса				= НСтр("ru = 'Отправлено в ФНС'");
	СтрокаЭтапа.Дата						= ДатаИзПодтверждения;
	СтрокаЭтапа.КомментарийКСостоянию		= НСтр("ru = 'Ожидается результат обработки'");
	СтрокаЭтапа.НаименованиеПротокола		= НСтр("ru = 'Подтверждение даты отправки'");
	СтрокаЭтапа.Протокол					= ?(СтрПодтвержденияПредставлениеНО.Количество() > 0, СтрПодтвержденияПредставлениеНО[0].Ссылка, "");
	СтрокаЭтапа.СостояниеСдачиОтчетности	= Перечисления.СостояниеСдачиОтчетности.ДокументооборотНачат;
	СтрокаЭтапа.ЭтапПройден					= СтрПодтвержденияПредставлениеНО.Количество() > 0;
	
	// определяем наиболее свежее сообщение с результатом приема
	ПоследнийРезультатПриема = Неопределено;
	Для Каждого СтрРезультатПриемаПредставлениеНО Из СтрРезультатыПриемаПредставлениеНО Цикл
		Если ПоследнийРезультатПриема = Неопределено ИЛИ СтрРезультатПриемаПредставлениеНО.Дата > ПоследнийРезультатПриема.Дата Тогда
			ПоследнийРезультатПриема = СтрРезультатПриемаПредставлениеНО;
		КонецЕсли;
	КонецЦикла;
	
	// если результат приема отрицательный, то выводим соответствующее сообщение
	Если ПоследнийРезультатПриема <> Неопределено И ПоследнийРезультатПриема.ПротоколСОшибкой Тогда
		
		Если ТипЗнч(СсылкаНаОтчет) = Тип("ДокументСсылка.ПоясненияКДекларацииПоНДС") Тогда
			КомментарийКСостоянию = НСтр("ru = 'Исправьте ошибки и отправьте новый ответ'");
		Иначе
			КомментарийКСостоянию = НСтр("ru = 'Исправьте ошибки и отправьте новую опись'");
		КонецЕсли;
		
		СтрокаЭтапа = ТаблицаЭтаповОтправки.Добавить();
		СтрокаЭтапа.ТекстНадписи				= НСтр("ru = 'Не принято'");
		СтрокаЭтапа.ТекстСтатуса				= НСтр("ru = 'Не принято'");
		СтрокаЭтапа.Дата						= Дата(1,1,1);
		СтрокаЭтапа.КомментарийКСостоянию		= КомментарийКСостоянию;
		СтрокаЭтапа.НаименованиеПротокола		= НСтр("ru = 'Уведомление об отказе'");
		СтрокаЭтапа.Протокол					= ?(СтрРезультатыПриемаПредставлениеНО.Количество() > 0, СтрРезультатыПриемаПредставлениеНО[0].Ссылка, "");
		СтрокаЭтапа.СостояниеСдачиОтчетности	= Перечисления.СостояниеСдачиОтчетности.ОтрицательныйРезультатДокументооборота;
		СтрокаЭтапа.ЭтапПройден					= СтрРезультатыПриемаПредставлениеНО.Количество() > 0;

	Иначе
		
		СтрокаЭтапа = ТаблицаЭтаповОтправки.Добавить();
		СтрокаЭтапа.ТекстНадписи				= НСтр("ru = 'Принято'");
		СтрокаЭтапа.ТекстСтатуса				= НСтр("ru = 'Принято'");
		СтрокаЭтапа.Дата						= Дата(1,1,1);
		СтрокаЭтапа.КомментарийКСостоянию		= "";
		СтрокаЭтапа.НаименованиеПротокола		= НСтр("ru = 'Квитанция о приеме'");
		СтрокаЭтапа.Протокол					= ?(СтрРезультатыПриемаПредставлениеНО.Количество() > 0, СтрРезультатыПриемаПредставлениеНО[0].Ссылка, "");
		СтрокаЭтапа.СостояниеСдачиОтчетности	= Перечисления.СостояниеСдачиОтчетности.ПоложительныйРезультатДокументооборота;
		СтрокаЭтапа.ЭтапПройден					= СтрРезультатыПриемаПредставлениеНО.Количество() > 0;
		
	КонецЕсли;
	
	Возврат ТаблицаЭтаповОтправки;
	
КонецФункции

#КонецОбласти

#Область РучнойВводСтатусовОтправки

Функция НачальныйСтатусПриФормированииТаблицыЭтаповОтправки(СсылкаНаОбъект, КонтролирующийОрган = Неопределено)
	
	Если ТипЗнч(СсылкаНаОбъект) = Тип("СправочникСсылка.ЭлектронныеПредставленияРегламентированныхОтчетов") Тогда
		
		Возврат НСтр("ru = 'Подготовлено'");
		
	ИначеЕсли ТипЗнч(СсылкаНаОбъект) = Тип("СправочникСсылка.МакетыПенсионныхДел")
		ИЛИ ТипЗнч(СсылкаНаОбъект) = Тип("СправочникСсылка.ЗаявлениеОНазначенииПенсии") Тогда
		
		Возврат НСтр("ru = 'В работе'");
		
	Иначе
		
		ТекСтатус = Неопределено;
		
		Если ТипЗнч(СсылкаНаОбъект) = Тип("ДокументСсылка.УведомлениеОСпецрежимахНалогообложения")
			ИЛИ ТипЗнч(СсылкаНаОбъект) = Тип("ДокументСсылка.УведомлениеОПолучателеДокументов") Тогда
			ВыборкаЖОКО = РегистрыСведений.ЖурналОтправокВКонтролирующиеОрганы.Выбрать(Новый Структура("Ссылка", СсылкаНаОбъект));
			Если ВыборкаЖОКО.Следующий() Тогда
				ТекСтатус = ВыборкаЖОКО.СтатусОтправки;
			КонецЕсли;
		Иначе
			ВыборкаЖОС = РегистрыСведений.ЖурналОтчетовСтатусы.Выбрать(Новый Структура("Ссылка", СсылкаНаОбъект));
			Если ВыборкаЖОС.Следующий() Тогда
				ТекСтатус = ВыборкаЖОС.Статус;
			КонецЕсли;
		КонецЕсли;
		
		Если ТекСтатус = Неопределено Тогда
			Возврат НСтр("ru = 'В работе'");
		КонецЕсли;
		
		ТекСтатус = СтроковыеФункцииКлиентСервер.ПодставитьПараметрыВСтроку(НСтр("ru = '%1'"), ТекСтатус);
		
		СтатусыОбъектов =
			РегламентированнаяОтчетностьКлиентСервер.СтатусыОбъектовРеглОтчетностиПриРучномВводе(СсылкаНаОбъект);
		
		Если СтатусыОбъектов.Найти(ТекСтатус) = Неопределено И КонтролирующийОрган <> "Минобороны" Тогда
			Возврат НСтр("ru = 'В работе'");
		КонецЕсли;
		
		Возврат ТекСтатус;
		
	КонецЕсли;
	
КонецФункции

#КонецОбласти

#КонецОбласти

#Область ПолучениеДатПротоколов

Функция ДатаИзОтветовКонтролирующихОрганов(ТранспортноеСообщение)
	
	ДатаИзОтвета = ТранспортноеСообщение.ДатаТранспорта;
	
	ВложенияТранспортногоСообщения = ПолучитьВложенияТранспортногоСообщения(ТранспортноеСообщение);
	СоответствиеТиповСодержимого = ПолучитьСоответствиеТиповСодержимого();
	
	Для Каждого Вложение Из ВложенияТранспортногоСообщения Цикл
		
		ТипСодержимого 	= Вложение.Тип;
		ТипДокумента 	= СоответствиеТиповСодержимого.Получить(Вложение.Тип);
		
		// отчетность ПФР
		Если ТипДокумента = "ФайлОтчетностиПФР" Тогда
			// Дата отсутствует в файле.
		ИначеЕсли ТипДокумента = "ПодтверждениеПолученияОтчетностиПФР" Тогда
			
			ИмяПоляДаты 	= "датаВремяПолучения";
			ДатаИзОтвета 	= ДатаИзXMLФайлаОтвета(ТранспортноеСообщение, Вложение.Тип, ИмяПоляДаты);
			
		ИначеЕсли ТипДокумента = "СведенияПриложениеПФР" Тогда
			// Дата отсутствует в файле.
		ИначеЕсли ТипДокумента = "ПротоколПФР" Тогда
			
			ИмяПоляДаты 	= "датаВремяОтправки";
			ДатаИзОтвета 	= ДатаИзXMLФайлаОтвета(ТранспортноеСообщение, Вложение.Тип, ИмяПоляДаты);
			
		ИначеЕсли ТипДокумента = "ПротоколПриложениеПФР" Тогда
			// Дата отсутствует в файле.
		ИначеЕсли ТипДокумента = "ФайлОписанияСведенийПФР" Тогда
			// Дата отсутствует в файле.
		ИначеЕсли ТипДокумента = "ЭлектронноЦифроваяПодпись" Тогда
			// Дата отсутствует в файле.
			
		// неформализованные ПФР
		ИначеЕсли ТипДокумента = "Письмо" Тогда
			// Дата отсутствует в файле.
		ИначеЕсли ТипДокумента = "ПисьмоПриложение" Тогда
			// Дата отсутствует в файле.
		ИначеЕсли ТипДокумента = "ПодтверждениеПолученияПисьма" Тогда
			
			ИмяПоляДаты 	= "датаВремяПолучения";
			ДатаИзОтвета 	= ДатаИзXMLФайлаОтвета(ТранспортноеСообщение, Вложение.Тип, ИмяПоляДаты);
			
		ИначеЕсли ТипДокумента = "ОписаниеПисьма" Тогда
			// Дата отсутствует в файле.
		ИначеЕсли ТипДокумента = "ТранспортнаяИнформация" Тогда
			// Дата отсутствует в файле.
		
		// запросы ПФР
		ИначеЕсли ТипДокумента = "ЗапросИОС" Тогда
			// Дата отсутствует в файле.
		ИначеЕсли ТипДокумента = "ФайлОписанияЗапросаПФР" Тогда
			// Дата отсутствует в файле.
		ИначеЕсли ТипДокумента = "ОтветНаЗапросПФР" Тогда
			
			ИмяПоляДаты 	= "датаВремяОтправки";
			ДатаИзОтвета 	= ДатаИзXMLФайлаОтвета(ТранспортноеСообщение, Вложение.Тип, ИмяПоляДаты);
			
		ИначеЕсли ТипДокумента = "ОтветПриложениеНаЗапросПФР" Тогда
			// Дата отсутствует в файле.
		ИначеЕсли ТипДокумента = "ПодтверждениеПолученияЗапросПФР" Тогда
			
			ИмяПоляДаты 	= "датаВремяПолучения";
			ДатаИзОтвета 	= ДатаИзXMLФайлаОтвета(ТранспортноеСообщение, Вложение.Тип, ИмяПоляДаты);
		
		// отчетность ФНС
		ИначеЕсли ТипДокумента = "ФайлОтчетности" Тогда
			// Дата отсутствует в файле.
		ИначеЕсли ТипДокумента = "Форма2НДФЛ" Тогда
			// Дата отсутствует в файле.
		ИначеЕсли ТипДокумента = "Доверенность" Тогда
			// Дата отсутствует в файле.
		ИначеЕсли ТипДокумента = "ОписаниеСведений" Тогда
			// Дата отсутствует в файле.
		ИначеЕсли ТипДокумента = "ОписаниеСведений2НДФЛ" Тогда
			// Дата отсутствует в файле.
		ИначеЕсли ТипДокумента = "ПодтверждениеДатыОтправки" Тогда
			
			ИмяПоляДаты 	= "ДатаОтпр";
			ИмяПоляВремени	= "ВремяОтпр";
			ДатаИзОтвета 	= ДатаИзXMLФайлаОтвета(ТранспортноеСообщение, Вложение.Тип, ИмяПоляДаты, ИмяПоляВремени);
			
		ИначеЕсли ТипДокумента = "УведомлениеОбОтказе" Тогда

			Если ТранспортноеСообщение.Тип = Перечисления.ТипыТранспортныхСообщений.РезультатПриемаЗапросНО 
				ИЛИ ТранспортноеСообщение.Тип = Перечисления.ТипыТранспортныхСообщений.РезультатПриемаОбращениеНО Тогда
				
				ИмяПоляДаты 	= "ДатаПост";
				ДатаИзОтвета 	= ДатаИзXMLФайлаОтвета(ТранспортноеСообщение, Вложение.Тип, ИмяПоляДаты);
				
			Иначе
				ИмяПоляДаты 	= "ДатаФормУвед";
				ДатаИзОтвета 	= ДатаИзXMLФайлаОтвета(ТранспортноеСообщение, Вложение.Тип, ИмяПоляДаты);
			КонецЕсли;
			
		ИначеЕсли ТипДокумента = "КвитанцияОПриеме" Тогда
			
			Если ТранспортноеСообщение.Тип = Перечисления.ТипыТранспортныхСообщений.РезультатПриемаЗапросНО Тогда
				ИмяПоляДаты 	= "ДатаПрин";
				ДатаИзОтвета 	= ДатаИзXMLФайлаОтвета(ТранспортноеСообщение, Вложение.Тип, ИмяПоляДаты);
			Иначе
				ИмяПоляДаты 	= "ДатаПредст";
				ДатаИзОтвета 	= ДатаИзXMLФайлаОтвета(ТранспортноеСообщение, Вложение.Тип, ИмяПоляДаты);
			КонецЕсли;
			
		ИначеЕсли ТипДокумента = "ИзвещениеОВводе" Тогда
			// Дата отсутствует в файле.
		ИначеЕсли ТипДокумента = "УведомлениеОбУточнении" Тогда
			
			ИмяПоляДаты 	= "ДатаФормУвед";
			ДатаИзОтвета 	= ДатаИзXMLФайлаОтвета(ТранспортноеСообщение, Вложение.Тип, ИмяПоляДаты);
				
		ИначеЕсли ТипДокумента = "ИзвещениеОПолучении" Тогда
			
			ИмяПоляДаты 	= "ДатаПол";
			ИмяПоляВремени	= "ВремяПол";
			ДатаИзОтвета 	= ДатаИзXMLФайлаОтвета(ТранспортноеСообщение, Вложение.Тип, ИмяПоляДаты, ИмяПоляВремени);
			
		//Заявление
		ИначеЕсли ТипДокумента = "КвитанцияОПриемеЗаявления" Тогда
			
			ИмяПоляДаты 	= "ДатаРег";
			ДатаИзОтвета 	= ДатаИзXMLФайлаОтвета(ТранспортноеСообщение, Вложение.Тип, ИмяПоляДаты);
			
		ИначеЕсли ТипДокумента = "СообщениеОПростОтметки" Тогда
			
			ИмяПоляДаты 	= "ДатаСообОтм";
			ДатаИзОтвета 	= ДатаИзXMLФайлаОтвета(ТранспортноеСообщение, Вложение.Тип, ИмяПоляДаты);
			
		ИначеЕсли ТипДокумента = "УведомлениеОбОтказеОтметки" Тогда
			// Дата отсутствует в файле.
		ИначеЕсли ТипДокумента = "СообщениеОПроверке" Тогда
			
			ИмяПоляДаты 	= "ДатаУведОткз";
			ДатаИзОтвета 	= ДатаИзXMLФайлаОтвета(ТранспортноеСообщение, Вложение.Тип, ИмяПоляДаты);
			
		ИначеЕсли ТипДокумента = "СообщениеОбОтзывеЗаявления" Тогда
			
			ИмяПоляДаты 	= "ДатаОснОтз";
			ДатаИзОтвета 	= ДатаИзXMLФайлаОтвета(ТранспортноеСообщение, Вложение.Тип, ИмяПоляДаты);
			
		ИначеЕсли ТипДокумента = "СообщениеОНесоответствиях" Тогда
			
			ИмяПоляДаты 	= "ДатаФЛК";
			ДатаИзОтвета 	= ДатаИзXMLФайлаОтвета(ТранспортноеСообщение, Вложение.Тип, ИмяПоляДаты);
			
		ИначеЕсли ТипДокумента = "Заявление" Тогда
			// Дата отсутствует в файле.
		ИначеЕсли ТипДокумента = "ОписаниеСведенийЗаявление" Тогда
			// Дата отсутствует в файле.
			
		//Документ
		ИначеЕсли ТипДокумента = "Документ" Тогда
			// Дата отсутствует в файле.
		ИначеЕсли ТипДокумента = "ОписаниеДокумент" Тогда
			// Дата отсутствует в файле.
		ИначеЕсли ТипДокумента = "УведомлениеОбОтказеДокумент" Тогда
			
			ИмяПоляДаты 	= "ДатаНапр";
			ДатаИзОтвета 	= ДатаИзXMLФайлаОтвета(ТранспортноеСообщение, Вложение.Тип, ИмяПоляДаты);
			
		ИначеЕсли ТипДокумента = "КвитанцияОПриемеДокумент" Тогда
			
			ИмяПоляДаты 	= "ДатаПрин";
			ДатаИзОтвета 	= ДатаИзXMLФайлаОтвета(ТранспортноеСообщение, Вложение.Тип, ИмяПоляДаты);
			
		// 2-НДФЛ
		ИначеЕсли ТипДокумента = "Протокол2НДФЛ" Тогда
			// Дата отсутствует в файле.
		ИначеЕсли ТипДокумента = "Реестр2НДФЛ" Тогда
			// Дата отсутствует в файле.
			
		// обращение
		ИначеЕсли ТипДокумента = "Обращение" Тогда
			// Дата отсутствует в файле.
		ИначеЕсли ТипДокумента = "Приложение" Тогда
			// Дата отсутствует в файле.
		ИначеЕсли ТипДокумента = "ПодтверждениеДатыПолучения" Тогда
			
			ИмяПоляДаты 	= "ДатаОтпр";
			ИмяПоляВремени	= "ВремяОтпр";
			ДатаИзОтвета 	= ДатаИзXMLФайлаОтвета(ТранспортноеСообщение, Вложение.Тип, ИмяПоляДаты, ИмяПоляВремени);
			
		ИначеЕсли ТипДокумента = "ОписаниеСведенийОбращениеПисьмоРассылка" Тогда
			// Дата отсутствует в файле.
			
		// представление
		ИначеЕсли ТипДокумента = "Представление" Тогда
			// Дата отсутствует в файле.
		ИначеЕсли ТипДокумента = "ДоверенностьПредставление" Тогда
			// Дата отсутствует в файле.
		ИначеЕсли ТипДокумента = "ОписаниеПредставление" Тогда
			// Дата отсутствует в файле.
		ИначеЕсли ТипДокумента = "ПодтверждениеДатыОтправкиПредставление" Тогда
			
			ИмяПоляДаты 	= "ДатаОтпр";
			ИмяПоляВремени	= "ВремяОтпр";
			ДатаИзОтвета 	= ДатаИзXMLФайлаОтвета(ТранспортноеСообщение, Вложение.Тип, ИмяПоляДаты, ИмяПоляВремени);
			
		// входящие неформализованные
		ИначеЕсли ТипДокумента = "Рассылка" Тогда
			// Дата отсутствует в файле.
			
		// запросы
		ИначеЕсли ТипДокумента = "Запрос" Тогда
			// Дата отсутствует в файле.
		ИначеЕсли ТипДокумента = "Ответ" Тогда
			// Дата отсутствует в файле.
		ИначеЕсли ТипДокумента = "ОписаниеСведенийЗапрос" Тогда
			
			ИмяПоляДаты 	= "ДатаВремяФормирования";
			ДатаИзОтвета 	= ДатаИзXMLФайлаОтвета(ТранспортноеСообщение, Вложение.Тип, ИмяПоляДаты);
			
		// отчетность в Росстат
		ИначеЕсли ТипДокумента = "ФайлОтчетностиФСГС" Тогда
			// Дата отсутствует в файле.
		ИначеЕсли ТипДокумента = "ИзвещениеОПолученииДокументаФСГС" Тогда
			// Дата отсутствует в файле.
		ИначеЕсли ТипДокумента = "ПодтверждениеОператораФСГС" Тогда
			
			ИмяПоляДаты 	= "ДатаОтпр";
			ИмяПоляВремени	= "ВремяОтпр";
			ДатаИзОтвета 	= ДатаИзXMLФайлаОтвета(ТранспортноеСообщение, Вложение.Тип, ИмяПоляДаты, ИмяПоляВремени);
			
		ИначеЕсли ТипДокумента = "УведомлениеОПриемеВОбработкуОтчетаФСГС" Тогда
			ДатаИзОтвета 	= ДатаИзПротоколаФСГС(ТранспортноеСообщение, Вложение.Тип);
		ИначеЕсли ТипДокумента = "УведомлениеОбУточненииОтчетаФСГС" Тогда
			ДатаИзОтвета 	= ДатаИзПротоколаФСГС(ТранспортноеСообщение, Вложение.Тип);
		ИначеЕсли ТипДокумента = "УведомлениеОНесоответствииФорматуОтчетаФСГС" Тогда
			ДатаИзОтвета 	= ДатаИзПротоколаФСГС(ТранспортноеСообщение, Вложение.Тип);
		ИначеЕсли ТипДокумента = "УведомлениеОбОтказеОтчетаФСГС" Тогда
			ДатаИзОтвета 	= ДатаИзПротоколаФСГС(ТранспортноеСообщение, Вложение.Тип);
		ИначеЕсли ТипДокумента = "УведомлениеОбОтклоненииОтчетаФСГС" Тогда
			ДатаИзОтвета 	= ДатаИзПротоколаФСГС(ТранспортноеСообщение, Вложение.Тип);
		ИначеЕсли ТипДокумента = "ОписаниеФайлаОтчетностиФСГС" Тогда
			// Дата отсутствует в файле.
			
		// переписка с Росстатом
		ИначеЕсли ТипДокумента = "ПисьмоФСГС" Тогда
			// Дата отсутствует в файле.
		ИначеЕсли ТипДокумента = "РассылкаФСГС" Тогда
			// Дата отсутствует в файле.
		ИначеЕсли ТипДокумента = "РассылкаШаблоновФСГС" Тогда
			// Дата отсутствует в файле.
		ИначеЕсли ТипДокумента = "ПриложениеПисьмаФСГС" Тогда
			// Дата отсутствует в файле.
		ИначеЕсли ТипДокумента = "ОписаниеПисьмаФСГС" Тогда
			// Дата отсутствует в файле.
			
		КонецЕсли;
		
	КонецЦикла;
	
	Возврат ДатаИзОтвета;

КонецФункции

Функция ДатаВремяИзСтрокиXML(ЗначениеСтр) Экспорт
	
	Попытка
		Возврат XMLЗначение(Тип("Дата"), ЗначениеСтр);
	Исключение
		Попытка
			Возврат ДатаИзСтрокиРазныхФорматов(ЗначениеСтр);
		Исключение
			Возврат '00010101';
		КонецПопытки;
	КонецПопытки;
	
КонецФункции

Функция ПолучитьРасширениеФайлаФССПоТипуОтправляемогоДокумента(
		ТипОтправляемогоДокумента,
		ВидОтправляемогоДокумента = Неопределено) Экспорт
	
	Если ТипОтправляемогоДокумента = Перечисления.ТипыОтправляемыхДокументов.Отчет4ФСС  Тогда
		
		Возврат "ef4";
		
	ИначеЕсли ТипОтправляемогоДокумента = Перечисления.ТипыОтправляемыхДокументов.РеестрСведенийФСС Тогда
		
		Возврат "esl";
		
	ИначеЕсли ТипОтправляемогоДокумента = Перечисления.ТипыОтправляемыхДокументов.Отчет4аФСС Тогда
		
		Возврат "e4a";
		
	ИначеЕсли ТипОтправляемогоДокумента = Перечисления.ТипыОтправляемыхДокументов.ПрочаяОтчетностьФСС
		И (ВидОтправляемогоДокумента = Справочники.ВидыОтправляемыхДокументов.ПодтверждениеВидаДеятельности
		ИЛИ ВидОтправляемогоДокумента =
		Справочники.ВидыОтправляемыхДокументов.РеестрСтимулирующихВыплатМедицинскимИСоциальнымРаботникам)
		ИЛИ ТипОтправляемогоДокумента = Перечисления.ТипыОтправляемыхДокументов.ДанныеЭЛНФСС Тогда
		
		Возврат "xml";
		
	Иначе
		
		Возврат Неопределено;
		
	КонецЕсли;

КонецФункции

Функция СведенияПоОтправляемымОбъектам(Ссылка) Экспорт
	
	Результат = Новый Структура;
	Результат.Вставить("Ссылка");
	Результат.Вставить("ВидКонтролирующегоОргана");
	Результат.Вставить("КодКонтролирующегоОргана");
	Результат.Вставить("ПредставлениеКонтролирующегоОргана");
	Результат.Вставить("Организация");
	Результат.Вставить("Наименование");
	Результат.Вставить("ПредставлениеПериода");
	Результат.Вставить("Дата");
	Результат.Вставить("ВариантОтчета");
	Результат.Вставить("СтраницаЖурнала");
	
	Если ТипЗнч(Ссылка) = Тип("ДокументСсылка.ЗаявлениеАбонентаСпецоператораСвязи") Тогда
		
		Результат.Ссылка 								= Ссылка;
		Результат.ВидКонтролирующегоОргана 				= Неопределено;
		Результат.КодКонтролирующегоОргана 				= "";
		Результат.ПредставлениеКонтролирующегоОргана	= "";
		Результат.Организация							= Ссылка.Организация;
		Результат.Наименование 							= ?(Ссылка.ТипЗаявления = Перечисления.ТипыЗаявленияАбонентаСпецоператораСвязи.Изменение,
			НСтр("ru = 'Заявление на изменение подключения к 1С-Отчетности'"), НСтр("ru = 'Заявление на подключение'"));
		Результат.ПредставлениеПериода					= "";
		Результат.Дата									= Ссылка.Дата;
		Результат.ВариантОтчета							= "-";
		Результат.СтраницаЖурнала						= Неопределено;

	Иначе
		
		Свойства = СвойстваОбъектовФормы1СОтчетность(Ссылка);
		
		// Если свойства получить не удалось, то возвращаем пустую структура
		Если Свойства = Неопределено Тогда
			Возврат Результат;
		КонецЕсли;
		
		// Ссылка
		Если Свойства.Свойство("ДокСсылка") Тогда
			Результат.Ссылка = Свойства.ДокСсылка;
		ИначеЕсли Свойства.Свойство("Ссылка") Тогда
			Результат.Ссылка = Свойства.Ссылка;
		КонецЕсли;
		
		// ВидКонтролирующегоОргана
		Если Свойства.Свойство("ВидКонтролирующегоОргана") Тогда
			Результат.ВидКонтролирующегоОргана = Свойства.ВидКонтролирующегоОргана;
		КонецЕсли;
		
		// КодКонтролирующегоОргана
		Если Свойства.Свойство("КодКонтролирующегоОргана") Тогда
			Результат.КодКонтролирующегоОргана = Свойства.КодКонтролирующегоОргана;
		КонецЕсли;
		
		// ПредставлениеКонтролирующегоОргана
		Если Свойства.Свойство("ВидКонтролирующегоОргана") И Свойства.Свойство("КодКонтролирующегоОргана") Тогда
			Результат.ПредставлениеКонтролирующегоОргана = РегламентированнаяОтчетностьВызовСервера.ПредставлениеКонтролирующегоОргана(Свойства);
		КонецЕсли;
		
		// Организация
		Если Свойства.Свойство("Организация") Тогда
			Результат.Организация = Свойства.Организация;
		КонецЕсли;
		
		// Наименование
		Если Свойства.Свойство("НаименованиеОтчета") Тогда
			Результат.Наименование = Свойства.НаименованиеОтчета;
		ИначеЕсли Свойства.Свойство("Наименование") Тогда
			Результат.Наименование = Свойства.Наименование;
		КонецЕсли;

		// ПредставлениеПериода
		Если Свойства.Свойство("ПредставлениеПериода") Тогда
			Результат.ПредставлениеПериода = Свойства.ПредставлениеПериода;
		ИначеЕсли Свойства.Свойство("ДатаНачалаОП") И Свойства.Свойство("ДатаОкончанияОП") Тогда
			Результат.ПредставлениеПериода = РегламентированнаяОтчетностьВызовСервера.ПредставлениеФинансовогоПериода(Свойства.ДатаНачалаОП, Свойства.ДатаОкончанияОП);
		КонецЕсли;
		
		// Дата
		Если Свойства.Свойство("ДатаСоздания") Тогда
			Результат.Дата = Свойства.ДатаСоздания;
		Иначе
			Результат.Дата = Дата(1,1,1);
		КонецЕсли;
		
		// ВариантОтчета
		Если Свойства.Свойство("ПредставлениеВида") Тогда
			Результат.ВариантОтчета = Свойства.ПредставлениеВида;
		Иначе
			Результат.ВариантОтчета = "";
		КонецЕсли;
		
		// СтраницаЖурнала
		Если Свойства.Свойство("СтраницаЖурнала") Тогда
			Результат.СтраницаЖурнала = Свойства.СтраницаЖурнала;
		Иначе
			Результат.СтраницаЖурнала = Перечисления.СтраницыЖурналаОтчетность.Отчеты;
		КонецЕсли;
		
	КонецЕсли;
	
	Возврат Результат;
	
КонецФункции

#КонецОбласти

Функция СсылкаНаСообщение(ТранспотрныеСообщения) Экспорт

	Возврат ?(ТранспотрныеСообщения.Количество() > 0, ТранспотрныеСообщения[0].Ссылка, Неопределено);
	
КонецФункции

#Область ЗаписьОбъектовВРегистр

Процедура ЗаписатьЗапросНаИнформационноеОбслуживаниеВРегистрЖурналОтправокВКонтролирующиеОрганы(Объект) Экспорт
	
	Свойства = СвойстваЗапросаНаИнформационноеОбслуживание(Объект);
	ЭлектронныйДокументооборотСКонтролирующимиОрганами.СоздатьЗаписьВРегистреЖурналОтправокВКонтролирующиеОрганы(Свойства);
	
КонецПроцедуры

Процедура ЗаписатьЗапросНаВыпискуИзЕГРЮЛ_ЕГРИПВРегистрЖурналОтправокВКонтролирующиеОрганы(Объект) Экспорт
	
	Свойства = СвойстваЗапросаНаВыпискуИзЕГРЮЛ_ЕГРИП(Объект);
	ЭлектронныйДокументооборотСКонтролирующимиОрганами.СоздатьЗаписьВРегистреЖурналОтправокВКонтролирующиеОрганы(Свойства);
	
КонецПроцедуры
	
Процедура ЗаписатьПерепискаСКонтролирующимиОрганамиВРегистрЖурналОтправокВКонтролирующиеОрганы(Объект) Экспорт

	Свойства = СвойстваПерепискиСКонтролирующими(Объект);
	ЭлектронныйДокументооборотСКонтролирующимиОрганами.СоздатьЗаписьВРегистреЖурналОтправокВКонтролирующиеОрганы(Свойства);
	
КонецПроцедуры

Процедура ЗаписатьОписиИсходящихДокументовВНалоговыеОрганыВРегистрЖурналОтправокВКонтролирующиеОрганы(Объект) Экспорт

	Свойства = СвойстваОписиИсходящихДокументовВНалоговыеОрганы(Объект);
	ЭлектронныйДокументооборотСКонтролирующимиОрганами.СоздатьЗаписьВРегистреЖурналОтправокВКонтролирующиеОрганы(Свойства);
	
КонецПроцедуры

Процедура ЗаписатьДокументыРеализацииПолномочийНалоговыхОргановВРегистрЖурналОтправокВКонтролирующиеОрганы(Объект) Экспорт

	Свойства = СвойстваДокументаРеализацииПолномочий(Объект);
	ЭлектронныйДокументооборотСКонтролирующимиОрганами.СоздатьЗаписьВРегистреЖурналОтправокВКонтролирующиеОрганы(Свойства);
	
КонецПроцедуры

Процедура ЗаписатьЗаявленияВПФРВРегистрЖурналОтправокВКонтролирующиеОрганы(Объект) Экспорт

	Свойства = СвойстваЗаявленияВПФР(Объект);
	ЭлектронныйДокументооборотСКонтролирующимиОрганами.СоздатьЗаписьВРегистреЖурналОтправокВКонтролирующиеОрганы(Свойства);
	
КонецПроцедуры

Процедура ЗаписатьОписиВходящихДокументовИзНалоговыхОргановВРегистрЖурналОтправокВКонтролирующиеОрганы(Объект) Экспорт

	Ссылка = Объект.Ссылка;
	Для каждого Строка Из Ссылка.ВходящиеДокументы Цикл
		ДокументРеализацииПолномочий = Строка.СсылкаНаОбъект;
		ЗаписатьДокументыРеализацииПолномочийНалоговыхОргановВРегистрЖурналОтправокВКонтролирующиеОрганы(ДокументРеализацииПолномочий);
	КонецЦикла; 
	
КонецПроцедуры

Процедура ЗаписатьУведомлениеОПолучателеДокументовВРегистрЖурналОтправокВКонтролирующиеОрганы(Объект) Экспорт
	
	Свойства = СвойстваУведомленияОПолучателеДокументов(Объект);
	ЭлектронныйДокументооборотСКонтролирующимиОрганами.СоздатьЗаписьВРегистреЖурналОтправокВКонтролирующиеОрганы(Свойства);
	
КонецПроцедуры

Процедура ЗаписатьЭлектронныеПредставленияРегламентированныхОтчетовВРегистрЖурналОтчетовСтатусы(Объект) Экспорт

	Ссылка = Объект.Ссылка;
	Свойства = СвойстваЭлектронногоПредставления(Ссылка);
	
	ЭтоЗакладкаУведомления = ЭтоЭлектронноеПредставлениеОтображаетсяВРазеделеУведомления(Ссылка);
	Если ЭтоЗакладкаУведомления Тогда
		
		ЭлектронныйДокументооборотСКонтролирующимиОрганами.СоздатьЗаписьВРегистреЖурналОтправокВКонтролирующиеОрганы(Свойства);
		
		// Удаление записи из регистра ЖурналОтчетовСтатусы
		Если СсылкаПрисутствуетВРегистреЖурналОтчетовСтатусы(Ссылка) Тогда
			НаборЗаписей = РегистрыСведений.ЖурналОтчетовСтатусы.СоздатьНаборЗаписей();
			НаборЗаписей.Отбор.Ссылка.Установить(Ссылка);
			НаборЗаписей.Очистить();
			НаборЗаписей.Записать();
		КонецЕсли;
		
	Иначе
		
		// Удаление записи из регистра ЖурналОтправокВКонтролирующиеОрганы
		Если СсылкаПрисутствуетВРегистреЖурналОтправокВКонтролирующиеОрганы(Ссылка) Тогда
			НаборЗаписей = РегистрыСведений.ЖурналОтправокВКонтролирующиеОрганы.СоздатьНаборЗаписей();
			НаборЗаписей.Отбор.Ссылка.Установить(Ссылка);
			НаборЗаписей.Очистить();
			НаборЗаписей.Записать();
		КонецЕсли;
		
		// Вызываем метод после всех обращений к БД
		РегламентированнаяОтчетностьВызовСервера.ОтразитьВЖурналеОтчетов(Свойства);
		
	КонецЕсли;
	
КонецПроцедуры

#КонецОбласти

#Область ВспомогательныеПроцедурыИФункции

#Область СвойстваОбъектов

Функция СвойстваОбъектовФормы1СОтчетность(Источник) Экспорт
	
	Если НЕ ЗначениеЗаполнено(Источник)
		ИЛИ (НЕ Справочники.ТипВсеСсылки().СодержитТип(ТипЗнч(Источник))
			И НЕ Документы.ТипВсеСсылки().СодержитТип(ТипЗнч(Источник))) Тогда
		Возврат Неопределено;
	КонецЕсли;
	
	Ссылка = Источник.Ссылка;
	Свойства = Неопределено;
	
	// Обработка объектов типов, входящих в БРО
	Если ТипЗнч(Ссылка) = Тип("ДокументСсылка.РегламентированныйОтчет") Тогда
		Свойства = РегламентированнаяОтчетность.СвойстваРегламентированногоОтчета(Ссылка);
	ИначеЕсли ТипЗнч(Ссылка) = Тип("СправочникСсылка.ЭлектронныеПредставленияРегламентированныхОтчетов") Тогда
		Свойства = СвойстваЭлектронногоПредставления(Ссылка);
	ИначеЕсли ТипЗнч(Ссылка) = Тип("ДокументСсылка.ЗапросНаИнформационноеОбслуживаниеНалогоплательщика") 
		ИЛИ ТипЗнч(Ссылка) = Тип("ДокументСсылка.ЗапросНаИнформационноеОбслуживаниеСтрахователя") Тогда
		Свойства = СвойстваЗапросаНаИнформационноеОбслуживание(Ссылка);
	ИначеЕсли ТипЗнч(Ссылка) = Тип("ДокументСсылка.ЗапросНаВыпискуИзЕГРЮЛ_ЕГРИП") Тогда
		Свойства = СвойстваЗапросаНаВыпискуИзЕГРЮЛ_ЕГРИП(Ссылка);
	ИначеЕсли ТипЗнч(Ссылка) = Тип("СправочникСсылка.ПерепискаСКонтролирующимиОрганами") Тогда
		Свойства = СвойстваПерепискиСКонтролирующими(Ссылка);
	ИначеЕсли ТипЗнч(Ссылка) = Тип("СправочникСсылка.МакетыПенсионныхДел")
		ИЛИ ТипЗнч(Ссылка) = Тип("СправочникСсылка.ЗаявлениеОНазначенииПенсии") Тогда
		Свойства = СвойстваМакетаПенсионногоДела(Ссылка);
	ИначеЕсли ТипЗнч(Ссылка) = Тип("СправочникСсылка.ОписиИсходящихДокументовВНалоговыеОрганы") Тогда
		Свойства = СвойстваОписиИсходящихДокументовВНалоговыеОрганы(Ссылка);
	ИначеЕсли ТипЗнч(Ссылка) = Тип("ДокументСсылка.ПоясненияКДекларацииПоНДС") Тогда
		Свойства = СвойстваОтветаНаТребованиеПоясненийКДекларацииНДС(Ссылка);
	ИначеЕсли ТипЗнч(Ссылка) = Тип("ДокументСсылка.УведомлениеОСпецрежимахНалогообложения") Тогда
		Свойства = ЭлектронныйДокументооборотСКонтролирующимиОрганами.СвойстваУведомленияОСпецрежимахНалогообложения(Ссылка);
	ИначеЕсли ТипЗнч(Ссылка) = Тип("СправочникСсылка.ДокументыРеализацииПолномочийНалоговыхОрганов") Тогда 
		Свойства = СвойстваДокументаРеализацииПолномочий(Ссылка);
	ИначеЕсли ТипЗнч(Ссылка) = Тип("ДокументСсылка.УведомлениеОПолучателеДокументов") Тогда
		Свойства = СвойстваУведомленияОПолучателеДокументов(Ссылка);
	ИначеЕсли ТипЗнч(Ссылка) = Тип("ДокументСсылка.ЗаявленияПоЭлДокументооборотуСПФР") Тогда
		Свойства = СвойстваЗаявленияВПФР(Ссылка);
	Иначе
		// Определяем, является ли этот объект объектом, не входящим в БРО
		ТаблицаОписанияОбъектовНеВходящихВБРО = РегламентированнаяОтчетность.ТаблицаОписанияОбъектовРегламентированнойОтчетности();
		СтрокаТаблицыОписания = ТаблицаОписанияОбъектовНеВходящихВБРО.Найти(ТипЗнч(Ссылка), "ТипОбъекта");
		Если СтрокаТаблицыОписания <> Неопределено Тогда
			
			РазделФормы1СОтчетность = СтрокаТаблицыОписания.ВидДокумента;
			Если РазделФормы1СОтчетность = Перечисления.СтраницыЖурналаОтчетность.Уведомления Тогда
				// Это объект закладки Уведомления
				Свойства = ЭлектронныйДокументооборотСКонтролирующимиОрганами.СвойстваПрочихУведомлений(Ссылка);
			ИначеЕсли РазделФормы1СОтчетность = Перечисления.СтраницыЖурналаОтчетность.Отчеты Тогда
				// Это объект закладки Отчеты
				Свойства = РегламентированнаяОтчетность.СвойстваПрочихОтчетов(Ссылка);
			КонецЕсли;
		КонецЕсли;
		
	КонецЕсли;
	
	Возврат Свойства;
	
КонецФункции

Функция СвойстваЗапросаНаИнформационноеОбслуживание(Объект) Экспорт
	
	Ссылка = Объект.Ссылка;
	
	// Наименование
	Наименование = Строка(Объект.ВидУслуги);
	
	ДатаНачала    = Ссылка.Дата;
	ДатаОкончания = Ссылка.Дата;	
	
	// Вид и код контролирующего органа
	Ответы = Неопределено;
	ЭтоЗапросИОН = ТипЗнч(Ссылка) = Тип("ДокументСсылка.ЗапросНаИнформационноеОбслуживаниеНалогоплательщика");
	Если ЭтоЗапросИОН Тогда
		
		ВидКонтролирующегоОргана = Перечисления.ТипыКонтролирующихОрганов.ФНС;
		
		Если Ссылка.ВидУслуги = Перечисления.ВидыУслугПриИОН.ПредставлениеАктовСверкиРасчетов
			ИЛИ Ссылка.ВидУслуги = Перечисления.ВидыУслугПриИОН.ПредставлениеВыпискиОперацийИзКарточкиРасчетыСБюджетом Тогда
			
			// В акте сверки указываются Год и НаДату.
			// В колонку Период формы 1С-Отчетности должен идти год.
			// За год отвечает начальная дата.
			ПредставлениеПериода = ПредставлениеФинансовогоПериода(
				НачалоГода(Ссылка.ДатаНачалаПериода), 
				КонецГода(Ссылка.ДатаНачалаПериода));
				
			ДатаНачала    = НачалоГода(Ссылка.ДатаНачалаПериода);
			ДатаОкончания = КонецГода(Ссылка.ДатаНачалаПериода);
				
		ИначеЕсли Ссылка.ВидУслуги = Перечисления.ВидыУслугПриИОН.ПредставлениеСправкиОСостоянииРасчетовСБюджетом
			ИЛИ Ссылка.ВидУслуги = Перечисления.ВидыУслугПриИОН.ПредставлениеСправкиОбИсполненииОбязанностейПоУплате Тогда
			
			// На дату
			ПредставлениеПериода = "На " + Формат(Ссылка.ДатаОкончанияПериода, "ДЛФ=D");
			
			ДатаНачала    = Ссылка.ДатаОкончанияПериода;
			ДатаОкончания = Ссылка.ДатаОкончанияПериода;
			
		Иначе
			
			ПредставлениеПериода = ПредставлениеФинансовогоПериода(
				Объект.ДатаНачалаПериода,
				Объект.ДатаОкончанияПериода);
				
			ДатаНачала    = Ссылка.ДатаНачалаПериода;
			ДатаОкончания = Ссылка.ДатаОкончанияПериода;
			
		КонецЕсли;
		
		Ответы = ПолучитьОтветыНаЗапросИОН(Объект.Ссылка);

	Иначе
		ВидКонтролирующегоОргана = Перечисления.ТипыКонтролирующихОрганов.ПФР;
		
		Ответы = ПолучитьОтветыНаЗапросИОС(Объект.Ссылка);
		
		ПредставлениеПериода = "";
		
		НаДату = Дата(1,1,1);
		Если Объект.ВидУслуги  = Перечисления.ВидыУслугПриИОС.СправкаОСостоянииРасчетов Тогда
			НаДату = Объект.НаДату;
		Иначе
			ПоследнийЦиклОбмена = ДокументооборотСКОВызовСервера.ПолучитьПоследнийЦиклОбмена(Ссылка);
			Если ЗначениеЗаполнено(ПоследнийЦиклОбмена) Тогда
				СообщенияЦикла = ПолучитьСообщенияЦиклаОбмена(ПоследнийЦиклОбмена,,,Истина);
				СтрОтветыНаЗапросПФР = СообщенияЦикла.НайтиСтроки(Новый Структура("Тип", Перечисления.ТипыТранспортныхСообщений.ОтветНаЗапросПФР));
				// Получаем дату отправки ответа из ПФР
				
				Попытка
					НаДату = ?(СтрОтветыНаЗапросПФР.Количество() > 0, ДатаИзОтветовКонтролирующихОрганов(СтрОтветыНаЗапросПФР[0].Ссылка), Дата(1,1,1));
				Исключение
					ИнформацияОбОшибке = ИнформацияОбОшибке();
						ЗаписьЖурналаРегистрации(
							НСтр("ru = 'Электронный документооборот с контролирующими органами. Получение даты запроса ИОС'"),
							УровеньЖурналаРегистрации.Ошибка,,, ПодробноеПредставлениеОшибки(ИнформацияОбОшибке));
				КонецПопытки;
						
			КонецЕсли;
		КонецЕсли;
		
		Если ЗначениеЗаполнено(НаДату) Тогда
			
			ПредставлениеПериода = "На " + Формат(НаДату, "ДЛФ=D");
			
			ДатаНачала    = Ссылка.НаДату;
			ДатаОкончания = Ссылка.НаДату;
			
		Иначе
			ПредставлениеПериода = "";
		КонецЕсли;
		
	КонецЕсли;
	
	ЕстьОтвет = Ложь;
	Если Ответы.Количество() > 0 Тогда
		ЕстьОтвет = Истина;
	КонецЕсли;
	
	КодКонтролирующегоОргана = "";
	Получатель = Объект.Получатель;
	Если ЗначениеЗаполнено(Получатель) Тогда
		КодКонтролирующегоОргана = Получатель.Код;
	ИначеЕсли ЭтоЗапросИОН Тогда
		Если Ссылка.ВидУслуги = Перечисления.ВидыУслугПриИОН.ПредставлениеСправкиОбИсполненииОбязанностейПоУплате
			ИЛИ Ссылка.ТипСверки = Перечисления.ТипыСверокИОН.ВЦеломПоОрганизации Тогда
			РеквизитыОрганизации 	 = РеквизитыОрганизацииНаСервере(Ссылка.Организация);
			КодКонтролирующегоОргана = Строка(РеквизитыОрганизации.КодНалоговогоОргана);
		КонецЕсли;
	КонецЕсли;
	
	Организация = Объект.Организация;
	
	// Запись в регистр
	ЗначенияЗаполнения = ЭлектронныйДокументооборотСКонтролирующимиОрганами.ПараметрыПроцедурыСоздатьЗаписьВРегистреЖурналОтправокВКонтролирующиеОрганы();
	ЗначенияЗаполнения.Ссылка 					= Ссылка;
	ЗначенияЗаполнения.ДатаСоздания				= Ссылка.Дата;
	ЗначенияЗаполнения.Наименование 			= Наименование;
	ЗначенияЗаполнения.ПредставлениеПериода 	= ПредставлениеПериода;
	ЗначенияЗаполнения.ВидКонтролирующегоОргана = ВидКонтролирующегоОргана;
	ЗначенияЗаполнения.КодКонтролирующегоОргана = КодКонтролирующегоОргана;
	ЗначенияЗаполнения.Организация 				= Организация;
	ЗначенияЗаполнения.ЕстьОтвет 				= ЕстьОтвет;
	ЗначенияЗаполнения.СтраницаЖурнала			= Перечисления.СтраницыЖурналаОтчетность.Сверки;
	ЗначенияЗаполнения.ДатаНачала				= ДатаНачала;
	ЗначенияЗаполнения.ДатаОкончания			= ДатаОкончания;
	
	Если Ссылка.Метаданные().Реквизиты.Найти("Комментарий") <> Неопределено Тогда
		// Отсутсвует в Документ.УдалитьЗапросНаИнформационноеОбслуживаниеНалогоплательщика
		ЗначенияЗаполнения.Комментарий = Ссылка.Комментарий;
	КонецЕсли;
	
	Возврат ЗначенияЗаполнения; 
	
КонецФункции

Функция РеквизитыОрганизацииНаСервере(Организация) Экспорт
	
	Результат = ОбщегоНазначения.ЗначенияРеквизитовОбъекта(Организация, "КрупнейшийНалогоплательщик, КодНалоговогоОрганаПолучателя, РегистрацияВНалоговомОргане.КодПоОКАТО, РегистрацияВНалоговомОргане.КодПоОКТМО");
	Если Результат.КрупнейшийНалогоплательщик <> Ложь Тогда
		КодНалоговогоОргана = Результат.КодНалоговогоОрганаПолучателя;
	Иначе
		КодНалоговогоОргана = СокрЛП(РегламентированнаяОтчетностьВызовСервера.ПолучитьСведенияОбОрганизации(Организация, , "КодНО").КодНО);
	КонецЕсли;
	Результат.Вставить("КодНалоговогоОргана", КодНалоговогоОргана);
	Возврат Результат;
	
КонецФункции

Функция СвойстваЭлектронногоПредставления(Объект)
	
	Ссылка = Объект.Ссылка;
	
	// Наименование
	Наименование = Строка(Ссылка.ВидОтчета);
	
	// Вид и код контролирующего органа
	ВидКонтролирующегоОргана	= Ссылка.ТипПолучателя;
	КодКонтролирующегоОргана	= "";
	
	Получатель = Ссылка.Получатель;
	Если ЗначениеЗаполнено(Получатель) Тогда
		Если ТипЗнч(Получатель) = Тип("Строка") Тогда
			КодКонтролирующегоОргана = Получатель;
		Иначе
			КодКонтролирующегоОргана = Получатель.Код;
		КонецЕсли;
	КонецЕсли;
	
	ВидОтчета = Ссылка.ВидОтчета; 
	Если ТипЗнч(ВидОтчета) = Тип("СправочникСсылка.ВидыОтправляемыхДокументов") Тогда
		ТипДокумента = ВидОтчета.ТипДокумента;
	Иначе
		ТипДокумента = Неопределено;
	КонецЕсли;
	
	// Даты
	ДатаНачала		= Дата(1,1,1);
	ДатаОкончания	= Дата(1,1,1);
	Если ТипДокумента <> Перечисления.ТипыОтправляемыхДокументов.ИсходящееУведомлениеФНС
		И ТипДокумента <> Перечисления.ТипыОтправляемыхДокументов.РеестрСведенийФСС Тогда
		
		ДатаНачала		= Ссылка.ДатаНачала;
		ДатаОкончания	= Ссылка.ДатаОкончания;
		
	КонецЕсли;
	
	// ПредставлениеВерсии
	ПредставлениеВерсии = Ссылка.ПредставлениеВерсии;
	Если ТипДокумента = Перечисления.ТипыОтправляемыхДокументов.ЗаявлениеОВвозеТоваров
		ИЛИ (ТипДокумента = Перечисления.ТипыОтправляемыхДокументов.ПрочаяОтчетностьПФР
			И ВидОтчета <> Справочники.ВидыОтправляемыхДокументов.СведенияОТрудовомСтажеЗастрахованногоЛицаСЗВК
			И ВидОтчета <> Справочники.ВидыОтправляемыхДокументов.СведенияОВзносахИСтраховомСтажеСПВ1
			И ВидОтчета <> Справочники.ВидыОтправляемыхДокументов.СведенияОНачисленномЗаработкеСЗВ63
			И ВидОтчета <> Справочники.ВидыОтправляемыхДокументов.ПачкаДокументовСЗВ61
			И ВидОтчета <> Справочники.ВидыОтправляемыхДокументов.РеестрСЗВ62
			И ВидОтчета <> Справочники.ВидыОтправляемыхДокументов.СведенияСЗВ64)
		ИЛИ ТипДокумента = Перечисления.ТипыОтправляемыхДокументов.КомплектОтчетностиПФР
		ИЛИ ТипДокумента = Перечисления.ТипыОтправляемыхДокументов.РеестрСведенийФСС
		ИЛИ ТипДокумента = Перечисления.ТипыОтправляемыхДокументов.ОтчетФСГС
		ИЛИ ТипДокумента = Перечисления.ТипыОтправляемыхДокументов.ИсходящееУведомлениеФНС Тогда
		ПредставлениеВерсии = "";
	КонецЕсли;
	
	ЭтоЗакладкаУведомления = ЭтоЭлектронноеПредставлениеОтображаетсяВРазеделеУведомления(Ссылка);
	Если ЭтоЗакладкаУведомления Тогда
		
		// Добавление записи в регистр ЖурналОтправокВКонтролирующиеОрганы
		ПараметрыУведомления = ЭлектронныйДокументооборотСКонтролирующимиОрганами.ПараметрыПроцедурыСоздатьЗаписьВРегистреЖурналОтправокВКонтролирующиеОрганы();
		ПараметрыУведомления.Ссылка 					= Ссылка;
		ПараметрыУведомления.Наименование 				= Наименование;
		ПараметрыУведомления.ДатаСоздания				= Ссылка.ДатаИмпорта;
		ПараметрыУведомления.ВидКонтролирующегоОргана 	= ВидКонтролирующегоОргана;
		ПараметрыУведомления.КодКонтролирующегоОргана 	= КодКонтролирующегоОргана;
		ПараметрыУведомления.Организация 				= Ссылка.Организация;
		ПараметрыУведомления.СтраницаЖурнала			= Перечисления.СтраницыЖурналаОтчетность.Уведомления;
		ПараметрыУведомления.Комментарий				= Ссылка.Комментарий;
		
		Возврат ПараметрыУведомления;

	Иначе
		
		// Добавление записи в регистр ЖурналОтчетовСтатусы
		ПараметрыОтчета = ЭлектронныйДокументооборотСКонтролирующимиОрганамиВызовСервера.ПараметрыОтображенияВЖурналеОтчетов();
			
		ПараметрыОтчета.ДокСсылка 					= Ссылка;
		ПараметрыОтчета.НаименованиеОтчета 			= Наименование;
		ПараметрыОтчета.ДатаСоздания				= Ссылка.ДатаИмпорта;
		ПараметрыОтчета.ВидКонтролирующегоОргана 	= ВидКонтролирующегоОргана;
		ПараметрыОтчета.КодКонтролирующегоОргана 	= КодКонтролирующегоОргана;
		ПараметрыОтчета.Организация 				= Ссылка.Организация;
		ПараметрыОтчета.ДатаНачалаОП 				= ДатаНачала;
		ПараметрыОтчета.ДатаОкончанияОП 			= ДатаОкончания;
		ПараметрыОтчета.ПредставлениеВида 			= ПредставлениеВерсии;
		ПараметрыОтчета.Комментарий 				= Ссылка.Комментарий;
		
		Возврат ПараметрыОтчета;

	КонецЕсли;
	
КонецФункции

Функция СвойстваЗапросаНаВыпискуИзЕГРЮЛ_ЕГРИП(Объект) Экспорт
	
	Ссылка = Объект.Ссылка;
	
	// Ответы
	ЕстьОтвет = ЕстьОтветНаЗапросыВыпискиИзЕГРЮЛ_ЕГРИП(Ссылка);

	Организация = Ссылка.Организация;
	
	// Запись в регистр
	ЗначенияЗаполнения = ЭлектронныйДокументооборотСКонтролирующимиОрганами.ПараметрыПроцедурыСоздатьЗаписьВРегистреЖурналОтправокВКонтролирующиеОрганы();
	ЗначенияЗаполнения.Ссылка 					= Ссылка;
	ЗначенияЗаполнения.ДатаСоздания				= Ссылка.Дата;
	ЗначенияЗаполнения.Наименование 			= НаименованиеЕГРЮЛ_ЕГРИП(Объект);
	ЗначенияЗаполнения.ВидКонтролирующегоОргана = Неопределено;
	ЗначенияЗаполнения.КодКонтролирующегоОргана = Неопределено;
	ЗначенияЗаполнения.Организация 				= Организация;
	ЗначенияЗаполнения.ЕстьОтвет 				= ЕстьОтвет;
	ЗначенияЗаполнения.СтраницаЖурнала			= Перечисления.СтраницыЖурналаОтчетность.ЕГРЮЛ;
	ЗначенияЗаполнения.Комментарий				= Ссылка.Комментарий;
	
	Возврат ЗначенияЗаполнения;
	
КонецФункции

Функция СвойстваПерепискиСКонтролирующими(Объект) Экспорт

	Ссылка = Объект.Ссылка;
	
	// Наименование
	Наименование = Объект.Наименование;
	
	// Вид и код контролирующего органа
	ЭтоИсходящаяПереписка =	Объект.Статус <> Перечисления.СтатусыПисем.Полученное;
	
	ДанныеКонтролирующегоОргана = ВидИКодКонтролирующегоОрганаДляПерепискиСКонтролирующимиОрганами(Ссылка);
	
	ВидКонтролирующегоОргана	= ДанныеКонтролирующегоОргана.Вид;
	КодКонтролирующегоОргана	= ДанныеКонтролирующегоОргана.Код;
	
	Организация = Объект.Организация;
	
	// Запись в регистр
	ЗначенияЗаполнения = ЭлектронныйДокументооборотСКонтролирующимиОрганами.ПараметрыПроцедурыСоздатьЗаписьВРегистреЖурналОтправокВКонтролирующиеОрганы();
	ЗначенияЗаполнения.Ссылка 					= Объект;
	ЗначенияЗаполнения.Наименование 			= Наименование;
	ЗначенияЗаполнения.ВидКонтролирующегоОргана = ВидКонтролирующегоОргана;
	ЗначенияЗаполнения.КодКонтролирующегоОргана = КодКонтролирующегоОргана;
	ЗначенияЗаполнения.Организация 				= Организация;
	ЗначенияЗаполнения.Комментарий				= Ссылка.Комментарий;
	Если ЭтоИсходящаяПереписка Тогда
		ЗначенияЗаполнения.СтраницаЖурнала			= Перечисления.СтраницыЖурналаОтчетность.Письма;
		ЗначенияЗаполнения.ДатаСоздания				= Объект.ДатаСообщения;
	Иначе
		ЗначенияЗаполнения.СтраницаЖурнала			= Перечисления.СтраницыЖурналаОтчетность.Входящие;
		ЗначенияЗаполнения.ДатаСоздания				= Объект.ДатаОтправки;
	КонецЕсли;
	
	Возврат ЗначенияЗаполнения;
	
КонецФункции

Функция СвойстваМакетаПенсионногоДела(Объект) Экспорт

	Ссылка = Объект.Ссылка;
	
	// Наименование
	Наименование = Объект.Наименование;
	
	// Вид и код контролирующего органа
	ЭтоИсходящаяПереписка =	Объект.Статус <> Перечисления.СтатусыПисем.Полученное;
	
	ДанныеКонтролирующегоОргана = ВидИКодКонтролирующегоОрганаДляПерепискиСКонтролирующимиОрганами(Ссылка);
	
	ВидКонтролирующегоОргана	= ДанныеКонтролирующегоОргана.Вид;
	КодКонтролирующегоОргана	= ДанныеКонтролирующегоОргана.Код;
	
	Организация = Объект.Организация;
	
	// Запись в регистр
	ЗначенияЗаполнения = ЭлектронныйДокументооборотСКонтролирующимиОрганами.ПараметрыПроцедурыСоздатьЗаписьВРегистреЖурналОтправокВКонтролирующиеОрганы();
	ЗначенияЗаполнения.Ссылка 					= Объект;
	ЗначенияЗаполнения.Наименование 			= Наименование;
	ЗначенияЗаполнения.ВидКонтролирующегоОргана = ВидКонтролирующегоОргана;
	ЗначенияЗаполнения.КодКонтролирующегоОргана = КодКонтролирующегоОргана;
	ЗначенияЗаполнения.Организация 				= Организация;
	ЗначенияЗаполнения.СтраницаЖурнала			= Перечисления.СтраницыЖурналаОтчетность.Отчеты;
	ЗначенияЗаполнения.ДатаСоздания				= Объект.ДатаСоздания;
	
	Возврат ЗначенияЗаполнения;
	
КонецФункции

Функция СвойстваОписиИсходящихДокументовВНалоговыеОрганы(Объект) Экспорт

	Ссылка = Объект.Ссылка;
	
	// Наименование
	
	Наименование = НаименованиеОписиИсхДок(Объект);
	
	// Вид и код контролирующего органа
	ВидКонтролирующегоОргана	= Перечисления.ТипыКонтролирующихОрганов.ФНС;
	Если ЗначениеЗаполнено(Объект.НалоговыйОрган) Тогда
		КодКонтролирующегоОргана	= Объект.НалоговыйОрган.Код;
	Иначе
		КодКонтролирующегоОргана	= "";
	КонецЕсли;
	
	Организация = Объект.Организация;
	
	// Запись в регистр
	ЗначенияЗаполнения = ЭлектронныйДокументооборотСКонтролирующимиОрганами.ПараметрыПроцедурыСоздатьЗаписьВРегистреЖурналОтправокВКонтролирующиеОрганы();
	ЗначенияЗаполнения.Ссылка 					= Ссылка;
	ЗначенияЗаполнения.ДатаСоздания				= Ссылка.ДатаСоздания;
	ЗначенияЗаполнения.Наименование 			= Наименование;
	ЗначенияЗаполнения.ВидКонтролирующегоОргана = ВидКонтролирующегоОргана;
	ЗначенияЗаполнения.КодКонтролирующегоОргана = КодКонтролирующегоОргана;
	ЗначенияЗаполнения.Организация 				= Организация;
	ЗначенияЗаполнения.СтраницаЖурнала			= Перечисления.СтраницыЖурналаОтчетность.Письма;
	ЗначенияЗаполнения.Комментарий				= Ссылка.Комментарий;
	
	Возврат ЗначенияЗаполнения;
	
КонецФункции

Функция СвойстваУведомленияОПолучателеДокументов(Объект) Экспорт
	
	Ссылка = Объект.Ссылка;
	
	// Наименование
	Наименование = НСтр("ru = 'Уведомление о получателе документов'");
	
	// Вид и код контролирующего органа
	ВидКонтролирующегоОргана = Перечисления.ТипыКонтролирующихОрганов.ФНС;
	
	КодКонтролирующегоОргана = "";
	Получатель = Объект.Получатель;
	Если ЗначениеЗаполнено(Получатель) Тогда
		КодКонтролирующегоОргана = ОбщегоНазначения.ЗначениеРеквизитаОбъекта(Получатель, "Код");
	КонецЕсли;
	
	Организация = Объект.Организация;
	
	// Запись в регистр
	ЗначенияЗаполнения = ЭлектронныйДокументооборотСКонтролирующимиОрганами.ПараметрыПроцедурыСоздатьЗаписьВРегистреЖурналОтправокВКонтролирующиеОрганы();
	ЗначенияЗаполнения.Ссылка 					= Ссылка;
	ЗначенияЗаполнения.ДатаСоздания				= Объект.Дата;
	ЗначенияЗаполнения.Наименование 			= Наименование;
	ЗначенияЗаполнения.ВидКонтролирующегоОргана = ВидКонтролирующегоОргана;
	ЗначенияЗаполнения.КодКонтролирующегоОргана = КодКонтролирующегоОргана;
	ЗначенияЗаполнения.Организация 				= Организация;
	ЗначенияЗаполнения.СтраницаЖурнала			= Перечисления.СтраницыЖурналаОтчетность.Уведомления;
	ЗначенияЗаполнения.Комментарий				= Ссылка.Комментарий;
	
	Возврат ЗначенияЗаполнения;
	
КонецФункции

Функция СвойстваДокументаРеализацииПолномочий(Объект) Экспорт

	Ссылка = Объект.Ссылка;
	
	// Наименование
	Наименование = Ссылка.Наименование;
	
	// Вид и код контролирующего органа
	ВидКонтролирующегоОргана	= Перечисления.ТипыКонтролирующихОрганов.ФНС;
	Если ЗначениеЗаполнено(Объект.НалоговыйОрган) Тогда
		КодКонтролирующегоОргана	= Объект.НалоговыйОрган.Код;
	Иначе
		КодКонтролирующегоОргана	= "";
	КонецЕсли;
	
	Организация = Объект.Организация;
	
	// Ответы
	Результат 			= ПолучитьКоличествоОтветовНаТребования(Ссылка);
	КоличествоОтветов 	= Результат.Получить(Ссылка);
	
	Опись = ДокументооборотСКОВызовСервера.ПолучитьОписьВходящихДокументовПоТребованию(Ссылка);
	Если Опись = Неопределено Тогда
		ДатаСоздания = Ссылка.ДатаДокумента;
	Иначе
		ДатаСоздания = Опись.ДатаОтправки;
	КонецЕсли;
	
	// Запись в регистр
	ЗначенияЗаполнения = ЭлектронныйДокументооборотСКонтролирующимиОрганами.ПараметрыПроцедурыСоздатьЗаписьВРегистреЖурналОтправокВКонтролирующиеОрганы();
	ЗначенияЗаполнения.Ссылка 					= Ссылка;
	ЗначенияЗаполнения.ДатаСоздания				= ДатаСоздания;
	ЗначенияЗаполнения.Наименование 			= Наименование;
	ЗначенияЗаполнения.ВидКонтролирующегоОргана = ВидКонтролирующегоОргана;
	ЗначенияЗаполнения.КодКонтролирующегоОргана = КодКонтролирующегоОргана;
	ЗначенияЗаполнения.Организация 				= Организация;
	ЗначенияЗаполнения.СтраницаЖурнала			= Перечисления.СтраницыЖурналаОтчетность.Входящие;
	ЗначенияЗаполнения.ЕстьОтвет				= КоличествоОтветов;
	ЗначенияЗаполнения.Комментарий				= Ссылка.Комментарий;
	
	Возврат ЗначенияЗаполнения;
	
КонецФункции

Функция СвойстваЗаявленияВПФР(Объект) Экспорт

	Ссылка = Объект.Ссылка;
	
	// Наименование
	Наименование = Ссылка.Вид;
	
	// Вид и код контролирующего органа
	ВидКонтролирующегоОргана	= Перечисления.ТипыКонтролирующихОрганов.ПФР;
	Если ЗначениеЗаполнено(Объект.Получатель) Тогда
		КодКонтролирующегоОргана = Объект.Получатель.Код;
	Иначе
		КодКонтролирующегоОргана = "";
	КонецЕсли;
	
	Организация = Объект.Организация;
	ДатаСоздания = Объект.Дата;
	
	// Запись в регистр
	ЗначенияЗаполнения = ЭлектронныйДокументооборотСКонтролирующимиОрганами.ПараметрыПроцедурыСоздатьЗаписьВРегистреЖурналОтправокВКонтролирующиеОрганы();
	ЗначенияЗаполнения.Ссылка 					= Ссылка;
	ЗначенияЗаполнения.ДатаСоздания				= ДатаСоздания;
	ЗначенияЗаполнения.Наименование 			= Наименование;
	ЗначенияЗаполнения.ВидКонтролирующегоОргана = ВидКонтролирующегоОргана;
	ЗначенияЗаполнения.КодКонтролирующегоОргана = КодКонтролирующегоОргана;
	ЗначенияЗаполнения.Организация 				= Организация;
	ЗначенияЗаполнения.СтраницаЖурнала			= Перечисления.СтраницыЖурналаОтчетность.Уведомления;
	ЗначенияЗаполнения.Комментарий				= Ссылка.Комментарий;
	
	Возврат ЗначенияЗаполнения;
	
КонецФункции

Функция ЭтоЭлектронноеПредставлениеОтображаетсяВРазеделеУведомления(Ссылка)
	
	ВидОтчета 				= Ссылка.ВидОтчета;
	ЭтоЗакладкаУведомления 	= Ложь;
	
	ВидыОтчетовРазделаУведомления = ЭлектронныйДокументооборотСКонтролирующимиОрганами.ВидыЭлектронныхПредставленийВРазделеУведомления();
	
	Если ВидыОтчетовРазделаУведомления.Найти(ВидОтчета) <> Неопределено Тогда
		ЭтоЗакладкаУведомления = Истина;
	КонецЕсли;
	
	Возврат ЭтоЗакладкаУведомления;
	
КонецФункции

Функция ПредставлениеФинансовогоПериода(Знач ДатаНачалаОП, Знач ДатаОкончанияОП) Экспорт
	
	ДатаНачала 		= НачалоДня(Мин(ДатаНачалаОП, ДатаОкончанияОП)); 
	ДатаОкончания 	= КонецДня(Макс(ДатаНачалаОП, ДатаОкончанияОП)); 

	ПредставлениеФинПериода = ПредставлениеПериода(ДатаНачала, ДатаОкончания, "Л=ru_RU; ФП=Истина");
	
	Если НачалоДня(ДатаНачала) = НачалоМесяца(ДатаНачала)
		И КонецДня(ДатаОкончания) = КонецМесяца(ДатаОкончания) Тогда
		
		Если ЗначениеЗаполнено(ДатаОкончания) Тогда
			ПредставлениеФинПериодаГод = ПредставлениеПериода(НачалоГода(ДатаОкончания), КонецГода(ДатаОкончания), "ФП=Истина");
			ПредставлениеФинПериода = СтрЗаменить(СтрЗаменить(ПредставлениеФинПериода, ПредставлениеФинПериодаГод, ""), "  ", " ")
				+ ПредставлениеФинПериодаГод;
		КонецЕсли;
			
	КонецЕсли;
		
	Возврат ПредставлениеФинПериода;
	
КонецФункции

Функция ТипКонтролирующегоОргана(Знач КонтролирующийОрган = "ФНС") Экспорт
	
	Результат = КонтролирующийОрган;
	
	// Определяем контролирующий орган
	Если ТипЗнч(КонтролирующийОрган) = Тип("Строка") Тогда
		// Получаем из перечисления
		Результат = Перечисления.ТипыКонтролирующихОрганов[КонтролирующийОрган];
	КонецЕсли;
	
	Возврат Результат;
		
КонецФункции

Функция ЕстьОтветНаЗапросыВыпискиИзЕГРЮЛ_ЕГРИП(Ссылка, Знач ВАрхиве = Ложь) Экспорт
	
	ЕстьОтвет = Ложь;
	ВАрхиве = Ложь;
	
	Ответы = ПолучитьОтветыНаЗапросыВыпискиИзЕГРЮЛ_ЕГРИП(Ссылка, ВАрхиве);
	Если Ответы.Количество() > 0 Тогда
		
		ОбщийРазмер = 0;
		Для каждого Ответ Из Ответы Цикл
			ОбщийРазмер = ОбщийРазмер + Ответ.Размер;
			Если Ответ.ВАрхиве Тогда
				ВАрхиве = Истина;
			КонецЕсли;
		КонецЦикла; 
		
		Если ОбщийРазмер <> 0 Тогда 
			ЕстьОтвет = Истина;
		КонецЕсли;
	КонецЕсли;
	
	Возврат ЕстьОтвет;
	
КонецФункции

Функция ВидИКодКонтролирующегоОрганаДляПерепискиСКонтролирующимиОрганами(Ссылка)
	
	// Вид и код контролирующего органа
	ЭтоИсходящаяПереписка =	Ссылка.Статус <> Перечисления.СтатусыПисем.Полученное;
	
	Если ЭтоИсходящаяПереписка Тогда
		КонтролирующийОрган = Ссылка.Получатель;
	Иначе
		КонтролирующийОрган = Ссылка.Отправитель;
	КонецЕсли;

	Результат = Новый Структура("Вид, Код");
	
	Если ЗначениеЗаполнено(КонтролирующийОрган) Тогда
		
		// Код
		ТипКонтролирующегоОргана = ТипЗнч(КонтролирующийОрган);
		Если ТипКонтролирующегоОргана = Тип("СправочникСсылка.НалоговыеОрганы")
			ИЛИ ТипКонтролирующегоОргана = Тип("СправочникСсылка.ОрганыФСГС")
			ИЛИ ТипКонтролирующегоОргана = Тип("СправочникСсылка.ОрганыПФР") Тогда
			Результат.Код = КонтролирующийОрган.Код;
		Иначе
			Результат.Код = "";
		КонецЕсли;
		
		// Вид
		Если Ссылка.Тип = Перечисления.ТипыПерепискиСКонтролирующимиОрганами.ПерепискаСФНС Тогда
			Результат.Вид = Перечисления.ТипыКонтролирующихОрганов.ФНС;
		ИначеЕсли Ссылка.Тип = Перечисления.ТипыПерепискиСКонтролирующимиОрганами.ПерепискаСПФР Тогда
			Результат.Вид = Перечисления.ТипыКонтролирующихОрганов.ПФР;
		ИначеЕсли Ссылка.Тип = Перечисления.ТипыПерепискиСКонтролирующимиОрганами.ПерепискаСФСГС Тогда
			Результат.Вид = Перечисления.ТипыКонтролирующихОрганов.ФСГС;
		КонецЕсли;
		
	КонецЕсли;
	
	Возврат Результат;
	
КонецФункции

Функция ПротоколыДляПечати(ПечатаемыеЦиклыОбмена = Неопределено, ПечатаемыеТранспортныеСообщения = Неопределено) Экспорт
	
	Если ТипЗнч(ПечатаемыеЦиклыОбмена) <> Тип("Массив") Тогда
		ЦиклыОбмена = Новый Массив;
		ЦиклыОбмена.Добавить(ПечатаемыеЦиклыОбмена);
	Иначе
		ЦиклыОбмена = ПечатаемыеЦиклыОбмена;
	КонецЕсли;
	
	Если ЦиклыОбмена.Количество() = 0 Тогда
		Возврат Неопределено;
	КонецЕсли;
	
	Если ТипЗнч(ПечатаемыеТранспортныеСообщения) <> Тип("Массив") И ПечатаемыеТранспортныеСообщения <> Неопределено Тогда
		ТранспортныеСообщения = Новый Массив;
		ТранспортныеСообщения.Добавить(ПечатаемыеТранспортныеСообщения);
	Иначе
		ТранспортныеСообщения = ПечатаемыеТранспортныеСообщения;
	КонецЕсли;
	
	РезультатНастройки = ДополнитьНастройкиПечати(, Ложь);
	ПолныеНастройки    = ДополнитьНастройкиПечати(, Истина);
	
	Для каждого ЦиклОбмена Из ЦиклыОбмена Цикл
		
		Если ЦиклОбмена.ФорматДокументооборота = Перечисления.ФорматыДокументооборотаСФНС.Приказ534 Тогда 
			ТаблицаЦиклаОбмена = ПолучитьТаблицуФайловЦиклаОбмена534(ЦиклОбмена, ПолныеНастройки, ТранспортныеСообщения);
		Иначе
			ТаблицаЦиклаОбмена = ПолучитьТаблицуФайловЦиклаОбмена(ЦиклОбмена, ПолныеНастройки, ТранспортныеСообщения);
		КонецЕсли;
		
		Если ТаблицаЦиклаОбмена <> Неопределено Тогда
			
			ТаблицаЦиклаОбмена.Свернуть("Настройка");
			
			Настройки = ТаблицаЦиклаОбмена.ВыгрузитьКолонку("Настройка");
			Для каждого Настройка Из Настройки Цикл
				РезультатНастройки[Настройка] = Истина;
			КонецЦикла; 
			
		КонецЕсли;
			
	КонецЦикла;
	
	РезультатНастройки.ПечататьСостояние = Ложь;
	РезультатНастройки.ПечататьДокумент  = Ложь;
	
	Возврат РезультатНастройки;
	
КонецФункции

Функция ЗаготовкаТаблицыЭтаповОтправки()
	
	ТаблицаЭтаповОтправки = Новый ТаблицаЗначений;
	ТаблицаЭтаповОтправки.Колонки.Добавить("ТекстНадписи", 				Новый ОписаниеТипов("Строка"));
	ТаблицаЭтаповОтправки.Колонки.Добавить("ТекстСтатуса", 				Новый ОписаниеТипов("Строка"));
	ТаблицаЭтаповОтправки.Колонки.Добавить("Дата" 						);
	ТаблицаЭтаповОтправки.Колонки.Добавить("КомментарийКСостоянию", 	Новый ОписаниеТипов("Строка,ФорматированнаяСтрока"));
	ТаблицаЭтаповОтправки.Колонки.Добавить("НаименованиеПротокола", 	Новый ОписаниеТипов("Строка"));
	ТаблицаЭтаповОтправки.Колонки.Добавить("Протокол");
	ТаблицаЭтаповОтправки.Колонки.Добавить("Отправка");
	ТаблицаЭтаповОтправки.Колонки.Добавить("Сообщение");
	ТаблицаЭтаповОтправки.Колонки.Добавить("СостояниеСдачиОтчетности", 	Новый ОписаниеТипов("ПеречислениеСсылка.СостояниеСдачиОтчетности"));
	ТаблицаЭтаповОтправки.Колонки.Добавить("ЭтапПройден", 				Новый ОписаниеТипов("Булево"));
	ТаблицаЭтаповОтправки.Колонки.Добавить("КоличествоОтветов", 		Новый ОписаниеТипов("Строка"));
	
	Возврат ТаблицаЭтаповОтправки;
	
КонецФункции

Функция КомментарийКСостояниюНеОтправлено() Экспорт
	
	Комментарий = "";
	
	ЭлектронныйДокументооборотДоступен = ХотяБыОднаОрганизацияИспользуетДокументооборот();
	 
	Если НЕ ЭлектронныйДокументооборотДоступен Тогда
		
		Комментарий = ПредложениеПодключить1СОтчетностьИзПанели();
		
	КонецЕсли;
		
	Возврат Комментарий;
	
КонецФункции

Функция ПредложениеПодключить1СОтчетностьИзПанели() Экспорт
	
	СсылкаНаПредложениеОформитьЗаявлениеНаПодключение = "e1cib/command/ОбщаяКоманда.ПредложениеОформитьЗаявлениеНаПодключение";
	
	СсылкаНаПрочиеСпособыСдачиОтчетности = "";
	ЭлектронныйДокументооборотСКонтролирующимиОрганамиПереопределяемый.ПолучитьСсылкуНаСтатьюСпособыСдачиОтчетности(СсылкаНаПрочиеСпособыСдачиОтчетности);
	
	ЭлементыСтроки = Новый Массив;
	ЭлементыСтроки.Добавить(НСтр("ru = 'Отправьте отчет через '"));
	ЭлементыСтроки.Добавить(Новый ФорматированнаяСтрока(НСтр("ru = '1С-Отчетность'"),,,,СсылкаНаПредложениеОформитьЗаявлениеНаПодключение));
	ЭлементыСтроки.Добавить(НСтр("ru = ' - самый удобный способ сдачи отчетности. '"));
	
	Если ЗначениеЗаполнено(СсылкаНаПрочиеСпособыСдачиОтчетности) Тогда
		ЭлементыСтроки.Добавить(Новый ФорматированнаяСтрока(НСтр("ru = 'Все способы'"),,,, СсылкаНаПрочиеСпособыСдачиОтчетности));
	КонецЕсли;
	
	Комментарий = Новый ФорматированнаяСтрока(ЭлементыСтроки);
		
	Возврат Комментарий;
	
КонецФункции

Функция КомментарийКСостояниюНеОтправленоМинобороны()
	
	Комментарий = НСтр("ru = 'Отправка отчета в электронном виде не предусмотрена. Для передачи отчета воспользуйтесь меню ""Выгрузка"".'");
	
	Возврат Комментарий;
	
КонецФункции

Функция СкрыватьПанельОтправки(Ссылка, Организация, КонтролирующийОрган) Экспорт 

	СкрыватьПанель = Ложь;
	МассивТипов = ДокументооборотСКОВызовСервера.ТипыОбъектовЭДООтображаемыхВФорме1СОтчетность();
	
	Если МассивТипов.Найти(ТипЗнч(Ссылка)) <> Неопределено Тогда
		
		СкрыватьПанель = Ложь;
		
	Иначе

		Попытка
		
			Если Организация <> Неопределено И ТипЗнч(Организация) = Тип("СправочникСсылка.Организации") Тогда
			
				Если КонтролирующийОрган = "ФСС" Тогда
					СкрыватьПанель = НЕ ОрганизацияИспользуетОбменСФСС(Организация);
				ИначеЕсли КонтролирующийОрган = "ФСРАР" Тогда
					СкрыватьПанель = НЕ НастройкиФСРАР(Организация).ИспользоватьОбмен;
				ИначеЕсли КонтролирующийОрган = "РПН" Тогда
					СкрыватьПанель = Ложь;
				ИначеЕсли КонтролирующийОрган = "ФТС" Тогда
					СкрыватьПанель = Ложь;
				Иначе
					
					УчетнаяЗаписьПредназначенаДляДокументооборотаСКО = Ложь;
					СвойстваОрганизации = ОбщегоНазначения.ЗначенияРеквизитовОбъекта(Организация,	"ВидОбменаСКонтролирующимиОрганами, УчетнаяЗаписьОбмена");
					
					УчетнаяЗаписьДокументооборота = СвойстваОрганизации.УчетнаяЗаписьОбмена;
					Если СвойстваОрганизации.ВидОбменаСКонтролирующимиОрганами = ПредопределенноеЗначение("Перечисление.ВидыОбменаСКонтролирующимиОрганами.ОбменВУниверсальномФормате")
					И ЗначениеЗаполнено(УчетнаяЗаписьДокументооборота) Тогда
						НастроенОбменВУниверсальномФормате = Истина;
						Если КонтролирующийОрган = "ФНС" Тогда
							УчетнаяЗаписьПредназначенаДляДокументооборотаСКО = 
								ДокументооборотСКОВызовСервера.УчетнаяЗаписьПредназначенаДляДокументооборотаСФНС(УчетнаяЗаписьДокументооборота);
						ИначеЕсли КонтролирующийОрган = "ПФР" Тогда
							УчетнаяЗаписьПредназначенаДляДокументооборотаСКО = 
								ДокументооборотСКОВызовСервера.УчетнаяЗаписьПредназначенаДляДокументооборотаСПФР(УчетнаяЗаписьДокументооборота);
						ИначеЕсли КонтролирующийОрган = "ФСГС" Тогда
							УчетнаяЗаписьПредназначенаДляДокументооборотаСКО = 
								ДокументооборотСКОВызовСервера.УчетнаяЗаписьПредназначенаДляДокументооборотаСФСГС(УчетнаяЗаписьДокументооборота);
						КонецЕсли;
					КонецЕсли;
					
					СкрыватьПанель = УчетнаяЗаписьПредназначенаДляДокументооборотаСКО = Ложь;
					
				КонецЕсли;
			КонецЕсли;
		
		Исключение
			СкрыватьПанель = Истина;
		КонецПопытки;
		
	КонецЕсли;
	
	Возврат СкрыватьПанель;
	
КонецФункции

Функция СсылкаПрисутствуетВРегистреЖурналОтчетовСтатусы(Ссылка) Экспорт
	
	Запрос = Новый Запрос;
	Запрос.Текст = "ВЫБРАТЬ РАЗРЕШЕННЫЕ РАЗЛИЧНЫЕ
	               |	Журнал.Ссылка
	               |ИЗ
	               |	РегистрСведений.ЖурналОтчетовСтатусы КАК Журнал
	               |ГДЕ
	               |	Журнал.Ссылка = &Ссылка";
	
	Запрос.УстановитьПараметр("Ссылка", Ссылка);
	Результат = Запрос.Выполнить();
	Выборка = Результат.Выбрать();
	Возврат Выборка.Количество() > 0;

КонецФункции

Функция СсылкаПрисутствуетВРегистреЖурналОтправокВКонтролирующиеОрганы(Ссылка) Экспорт
	
	Запрос = Новый Запрос;
	Запрос.Текст = "ВЫБРАТЬ РАЗРЕШЕННЫЕ РАЗЛИЧНЫЕ
	               |	Журнал.Ссылка
	               |ИЗ
	               |	РегистрСведений.ЖурналОтправокВКонтролирующиеОрганы КАК Журнал
	               |ГДЕ
	               |	Журнал.Ссылка = &Ссылка";
	
	Запрос.УстановитьПараметр("Ссылка", Ссылка);
	Результат = Запрос.Выполнить();
	Выборка = Результат.Выбрать();
	Возврат Выборка.Количество() > 0;

КонецФункции

Функция ХотяБыОднаОрганизацияИспользуетДокументооборот() Экспорт
	
	ОрганизацииСДокументооборотом = Новый Массив;
	
	// Есть учетные записи
	ЭлектронныйДокументооборотАктивен = ЭлектронныйДокументооборотИспользуется();
	Если ЭлектронныйДокументооборотАктивен Тогда
		Возврат Истина;
	КонецЕсли;
	
	// Настроен ЭДО с ФСС
	СписокДопустимыхОрганизацийВОбъектахОбменаФСС = СписокДопустимыхОрганизацийВОбъектахОбменаФСС();
	Для каждого Организация Из СписокДопустимыхОрганизацийВОбъектахОбменаФСС Цикл
		Если Организация.ПометкаУдаления = Ложь Тогда
			Возврат Истина;
		Конецесли;
	КонецЦикла;
	
	// Настроен ЭДО с ФСРАР
	СписокДопустимыхОрганизацийВОбъектахОбменаФСРАР = СписокДопустимыхОрганизацийВОбъектахОбменаФСРАР();
	Для каждого Организация Из СписокДопустимыхОрганизацийВОбъектахОбменаФСРАР Цикл
		Если Организация.ПометкаУдаления = Ложь Тогда
			Возврат Истина;
		Конецесли;
	КонецЦикла;
	
	// Настроен ЭДО с РПН
	СписокДопустимыхОрганизацийВОбъектахОбменаРПН = СписокДопустимыхОрганизацийВОбъектахОбменаРПН();
	Для каждого Организация Из СписокДопустимыхОрганизацийВОбъектахОбменаРПН Цикл
		Если Организация.ПометкаУдаления = Ложь Тогда
			Возврат Истина;
		Конецесли;
	КонецЦикла;
	
	// Настроен ЭДО с ФТС
	СписокДопустимыхОрганизацийВОбъектахОбменаФТС = СписокДопустимыхОрганизацийВОбъектахОбменаФТС();
	Для каждого Организация Из СписокДопустимыхОрганизацийВОбъектахОбменаФТС Цикл
		Если Организация.ПометкаУдаления = Ложь Тогда
			Возврат Истина;
		Конецесли;
	КонецЦикла; 
	
	Возврат Ложь;
	
КонецФункции

Функция ТипКонтролирующегоОрганаДляОбъектаОтправки(Ссылка) 

	Результат = Перечисления.ТипыКонтролирующихОрганов.ПустаяСсылка();

	Если НЕ ЗначениеЗаполнено(Ссылка) Тогда
		Возврат Результат;
	КонецЕсли;
	
	СвойстваОбъекта = СвойстваОбъектовФормы1СОтчетность(Ссылка);
	Если ТипЗнч(СвойстваОбъекта) = Тип("Структура")
		И СвойстваОбъекта.Свойство("ВидКонтролирующегоОргана") Тогда
		Результат = СвойстваОбъекта.ВидКонтролирующегоОргана;
	КонецЕсли;
	
	Возврат Результат;

КонецФункции

Функция ДополнитьНастройкиПечати(Настройки = Неопределено, ЗначениеПоУмолчанию = Ложь) Экспорт
	
	Если Настройки = Неопределено Тогда
		Настройки = Новый Структура;
	КонецЕсли;
	
	// Настройки дополняем недостающими свойствами
	Если НЕ Настройки.Свойство("ПечататьДокумент") Тогда
		Настройки.Вставить("ПечататьДокумент", ЗначениеПоУмолчанию);
	КонецЕсли;
	
	Если НЕ Настройки.Свойство("ПечататьПодтверждениеОтправки") Тогда
		Настройки.Вставить("ПечататьПодтверждениеОтправки", ЗначениеПоУмолчанию);
	КонецЕсли;
	
	Если НЕ Настройки.Свойство("ПечататьИзвещениеОбОтказеПФР") Тогда
		Настройки.Вставить("ПечататьИзвещениеОбОтказеПФР", ЗначениеПоУмолчанию);
	КонецЕсли;
	
	Если НЕ Настройки.Свойство("ПечататьПротокол") Тогда
		Настройки.Вставить("ПечататьПротокол", ЗначениеПоУмолчанию);
	КонецЕсли;
	
	Если НЕ Настройки.Свойство("ПечататьУведомлениеОбУточнении") Тогда
		Настройки.Вставить("ПечататьУведомлениеОбУточнении", ЗначениеПоУмолчанию);
	КонецЕсли;
	
	Если НЕ Настройки.Свойство("ПечататьПротоколПриема2НДФЛ") Тогда
		Настройки.Вставить("ПечататьПротоколПриема2НДФЛ", ЗначениеПоУмолчанию);
	КонецЕсли;
	
	Если НЕ Настройки.Свойство("ПечататьРеестрСведений2НДФЛ") Тогда
		Настройки.Вставить("ПечататьРеестрСведений2НДФЛ", ЗначениеПоУмолчанию);
	КонецЕсли;
	
	Если НЕ Настройки.Свойство("ПечататьСостояние") Тогда
		Настройки.Вставить("ПечататьСостояние", ЗначениеПоУмолчанию);
	КонецЕсли;	
	
	Если НЕ Настройки.Свойство("ПечататьПодтверждениеДатыОтправки") Тогда
		Настройки.Вставить("ПечататьПодтверждениеДатыОтправки", ЗначениеПоУмолчанию);
	КонецЕсли;	
	
	Если НЕ Настройки.Свойство("ПечататьРезультатПриема") Тогда
		Настройки.Вставить("ПечататьРезультатПриема", ЗначениеПоУмолчанию);
	КонецЕсли;

	Если НЕ Настройки.Свойство("ПечататьРезультатОбработки") Тогда
		Настройки.Вставить("ПечататьРезультатОбработки", ЗначениеПоУмолчанию);
	КонецЕсли;
	
	Если НЕ Настройки.Свойство("ПечататьСообщениеОбОтзыве") Тогда
		Настройки.Вставить("ПечататьСообщениеОбОтзыве", ЗначениеПоУмолчанию);
	КонецЕсли;

	Если НЕ Настройки.Свойство("ПечататьСообщениеОНесоответствиях") Тогда
		Настройки.Вставить("ПечататьСообщениеОНесоответствиях", ЗначениеПоУмолчанию);
	КонецЕсли;
	
	Если НЕ Настройки.Свойство("ПечататьИзвещенияОПолучении") Тогда
		Настройки.Вставить("ПечататьИзвещенияОПолучении", ЗначениеПоУмолчанию);
	КонецЕсли;
	
	Если НЕ Настройки.Свойство("ПечататьОтветНаЗапросИОС") Тогда
		Настройки.Вставить("ПечататьОтветНаЗапросИОС", ЗначениеПоУмолчанию);
	КонецЕсли;
	
	Если НЕ Настройки.Свойство("ПечататьПодтвержденияПолученияЗапросаПФР") Тогда
		Настройки.Вставить("ПечататьПодтвержденияПолученияЗапросаПФР", ЗначениеПоУмолчанию);
	КонецЕсли;
	
	Если НЕ Настройки.Свойство("ПодтверждениеПолученияПисьмаПФР") Тогда
		Настройки.Вставить("ПодтверждениеПолученияПисьмаПФР", ЗначениеПоУмолчанию);
	КонецЕсли;
	
	Если НЕ Настройки.Свойство("ПросмотрПодтвержденияПолученияФСГС") Тогда
		Настройки.Вставить("ПросмотрПодтвержденияПолученияФСГС", ЗначениеПоУмолчанию);
	КонецЕсли;
	
	Возврат Настройки;
	
КонецФункции

Функция ПолучитьТаблицуФайловЦиклаОбмена534(ЦиклОбмена, Настройки, ТранспортныеСообщения = Неопределено) Экспорт
	
	Если ТранспортныеСообщения = Неопределено Тогда
		ТранспортныеСообщения = ПолучитьСообщенияЦиклаОбмена(ЦиклОбмена).ВыгрузитьКолонку("Ссылка");
	КонецЕсли;
	
	Запрос = Новый Запрос;
	Запрос.УстановитьПараметр("ТранспортныеСообщения", ТранспортныеСообщения);
	
	Подзапросы = Новый Массив;
	
	Если Настройки.ПечататьДокумент Тогда
		Подзапросы.Добавить("ВЫБРАТЬ
		                    |	""ПечататьДокумент"" КАК Настройка,
		                    |	""ФайлОтчетности"" КАК ИдТипа,
		                    |	СодержимоеТранспортныхКонтейнеров.Данные КАК Данные,
		                    |	СодержимоеТранспортныхКонтейнеров.Тип КАК Тип,
		                    |	СодержимоеТранспортныхКонтейнеров.ИмяФайла КАК ИмяФайла,
		                    |	"""" КАК ИмяПодписанногоФайла,
		                    |	"""" КАК Сертификат,
		                    |	СодержимоеТранспортныхКонтейнеров.ТранспортноеСообщение КАК ТранспортноеСообщение,
		                    |	ВЫБОР
		                    |		КОГДА СостояниеОбъектов.Архивный ЕСТЬ NULL
		                    |			ТОГДА ЛОЖЬ
		                    |		КОГДА СостояниеОбъектов.Архивный = ЛОЖЬ
		                    |			ТОГДА ЛОЖЬ
		                    |		ИНАЧЕ ИСТИНА
		                    |	КОНЕЦ КАК ВАрхиве
		                    |ИЗ
		                    |	РегистрСведений.СодержимоеТранспортныхКонтейнеров КАК СодержимоеТранспортныхКонтейнеров
		                    |		ЛЕВОЕ СОЕДИНЕНИЕ РегистрСведений.ПризнакиАрхивированияФайловДОСКонтролирующимиОрганами КАК СостояниеОбъектов
		                    |		ПО (СостояниеОбъектов.Объект = СодержимоеТранспортныхКонтейнеров.ТранспортноеСообщение)
		                    |			И (СостояниеОбъектов.ИмяФайла = СодержимоеТранспортныхКонтейнеров.ИмяФайла)
		                    |			И (СостояниеОбъектов.Владелец = ЗНАЧЕНИЕ(Перечисление.ВидыАрхивируемыхМетаданныхДО.СодержимоеТранспортныхКонтейнеров))
		                    |ГДЕ
		                    |	СодержимоеТранспортныхКонтейнеров.Тип В(&ТипСодержимогоФайлОтчетности)
		                    |	И СодержимоеТранспортныхКонтейнеров.ТранспортноеСообщение.Тип В(&ТипСообщенияПервичноеСообщениеСодержащееОтчетность)
		                    |	И СодержимоеТранспортныхКонтейнеров.ТранспортноеСообщение В (&ТранспортныеСообщения)
		                    |
		                    |ОБЪЕДИНИТЬ ВСЕ
		                    |
		                    |ВЫБРАТЬ
		                    |	""ПечататьДокумент"" КАК Настройка,
		                    |	""ФайлОтчетностиПодписьНалогоплательщика"",
		                    |	СодержимоеТранспортныхКонтейнеров.Данные,
		                    |	СодержимоеТранспортныхКонтейнеров.Тип,
		                    |	СодержимоеТранспортныхКонтейнеров.ИмяФайла,
		                    |	СодержимоеТранспортныхКонтейнеров.ЭЦПИмяПодписанногоФайла,
		                    |	СодержимоеТранспортныхКонтейнеров.ЭЦПСертификат,
		                    |	СодержимоеТранспортныхКонтейнеров.ТранспортноеСообщение,
		                    |	ВЫБОР
		                    |		КОГДА СостояниеОбъектов.Архивный ЕСТЬ NULL
		                    |			ТОГДА ЛОЖЬ
		                    |		КОГДА СостояниеОбъектов.Архивный = ЛОЖЬ
		                    |			ТОГДА ЛОЖЬ
		                    |		ИНАЧЕ ИСТИНА
		                    |	КОНЕЦ
		                    |ИЗ
		                    |	РегистрСведений.СодержимоеТранспортныхКонтейнеров КАК СодержимоеТранспортныхКонтейнеров
		                    |		ЛЕВОЕ СОЕДИНЕНИЕ РегистрСведений.ПризнакиАрхивированияФайловДОСКонтролирующимиОрганами КАК СостояниеОбъектов
		                    |		ПО (СостояниеОбъектов.Объект = СодержимоеТранспортныхКонтейнеров.ТранспортноеСообщение)
		                    |			И (СостояниеОбъектов.ИмяФайла = СодержимоеТранспортныхКонтейнеров.ИмяФайла)
		                    |			И (СостояниеОбъектов.Владелец = ЗНАЧЕНИЕ(Перечисление.ВидыАрхивируемыхМетаданныхДО.СодержимоеТранспортныхКонтейнеров))
		                    |ГДЕ
		                    |	СодержимоеТранспортныхКонтейнеров.Тип = &ТипСодержимогоЭлектронноЦифроваяПодпись
		                    |	И СодержимоеТранспортныхКонтейнеров.ТранспортноеСообщение.Тип В(&ТипСообщенияПервичноеСообщениеСодержащееОтчетность)
		                    |	И СодержимоеТранспортныхКонтейнеров.ТранспортноеСообщение В (&ТранспортныеСообщения)
		                    |
		                    |ОБЪЕДИНИТЬ ВСЕ
		                    |
		                    |ВЫБРАТЬ
		                    |	""ПечататьДокумент"" КАК Настройка,
		                    |	""ФайлОтчетностиПодписьНалоговогоОргана"",
		                    |	СодержимоеТранспортныхКонтейнеров.Данные,
		                    |	СодержимоеТранспортныхКонтейнеров.Тип,
		                    |	СодержимоеТранспортныхКонтейнеров.ИмяФайла,
		                    |	СодержимоеТранспортныхКонтейнеров.ЭЦПИмяПодписанногоФайла,
		                    |	СодержимоеТранспортныхКонтейнеров.ЭЦПСертификат,
		                    |	СодержимоеТранспортныхКонтейнеров.ТранспортноеСообщение,
		                    |	ВЫБОР
		                    |		КОГДА СостояниеОбъектов.Архивный ЕСТЬ NULL
		                    |			ТОГДА ЛОЖЬ
		                    |		КОГДА СостояниеОбъектов.Архивный = ЛОЖЬ
		                    |			ТОГДА ЛОЖЬ
		                    |		ИНАЧЕ ИСТИНА
		                    |	КОНЕЦ
		                    |ИЗ
		                    |	РегистрСведений.СодержимоеТранспортныхКонтейнеров КАК СодержимоеТранспортныхКонтейнеров
		                    |		ЛЕВОЕ СОЕДИНЕНИЕ РегистрСведений.ПризнакиАрхивированияФайловДОСКонтролирующимиОрганами КАК СостояниеОбъектов
		                    |		ПО (СостояниеОбъектов.Объект = СодержимоеТранспортныхКонтейнеров.ТранспортноеСообщение)
		                    |			И (СостояниеОбъектов.ИмяФайла = СодержимоеТранспортныхКонтейнеров.ИмяФайла)
		                    |			И (СостояниеОбъектов.Владелец = ЗНАЧЕНИЕ(Перечисление.ВидыАрхивируемыхМетаданныхДО.СодержимоеТранспортныхКонтейнеров))
		                    |ГДЕ
		                    |	СодержимоеТранспортныхКонтейнеров.Тип = &ТипСодержимогоЭлектронноЦифроваяПодпись
		                    |	И СодержимоеТранспортныхКонтейнеров.ТранспортноеСообщение.Тип В(&ТипСообщенияКвитанцияОПриемеФайлаОтчетности)
		                    |	И СодержимоеТранспортныхКонтейнеров.ТранспортноеСообщение В (&ТранспортныеСообщения)");
		
		ТипСодержимогоФайлОтчетности = Новый Массив;
		ТипСодержимогоФайлОтчетности.Добавить(Перечисления.ТипыСодержимогоТранспортногоКонтейнера.ФайлОтчетности);
		ТипСодержимогоФайлОтчетности.Добавить(Перечисления.ТипыСодержимогоТранспортногоКонтейнера.Заявление);
		ТипСодержимогоФайлОтчетности.Добавить(Перечисления.ТипыСодержимогоТранспортногоКонтейнера.Форма2НДФЛ);
		Запрос.УстановитьПараметр("ТипСодержимогоФайлОтчетности", ТипСодержимогоФайлОтчетности);
		
		ТипСообщенияПервичноеСообщениеСодержащееОтчетность = Новый Массив;
		ТипСообщенияПервичноеСообщениеСодержащееОтчетность.Добавить(Перечисления.ТипыТранспортныхСообщений.ДекларацияНП);
		ТипСообщенияПервичноеСообщениеСодержащееОтчетность.Добавить(Перечисления.ТипыТранспортныхСообщений.ЗаявлениеНП);
		ТипСообщенияПервичноеСообщениеСодержащееОтчетность.Добавить(Перечисления.ТипыТранспортныхСообщений.Форма2НДФЛНП);
		Запрос.УстановитьПараметр("ТипСообщенияПервичноеСообщениеСодержащееОтчетность", ТипСообщенияПервичноеСообщениеСодержащееОтчетность);
		
		Запрос.УстановитьПараметр("ТипСодержимогоЭлектронноЦифроваяПодпись", Перечисления.ТипыСодержимогоТранспортногоКонтейнера.ЭлектронноЦифроваяПодпись);
		
		ТипСообщенияКвитанцияОПриемеФайлаОтчетности = Новый Массив;
		ТипСообщенияКвитанцияОПриемеФайлаОтчетности.Добавить(Перечисления.ТипыТранспортныхСообщений.РезультатПриемаДекларацияНО);
		ТипСообщенияКвитанцияОПриемеФайлаОтчетности.Добавить(Перечисления.ТипыТранспортныхСообщений.РезультатПриемаЗаявлениеНО);
		ТипСообщенияКвитанцияОПриемеФайлаОтчетности.Добавить(Перечисления.ТипыТранспортныхСообщений.РезультатПриемаФорма2НДФЛНО);
		Запрос.УстановитьПараметр("ТипСообщенияКвитанцияОПриемеФайлаОтчетности", ТипСообщенияКвитанцияОПриемеФайлаОтчетности);
	КонецЕсли;
	
	Если Настройки.ПечататьПодтверждениеДатыОтправки Тогда
		Подзапросы.Добавить("ВЫБРАТЬ
		                    |	""ПечататьПодтверждениеДатыОтправки"" КАК Настройка,
		                    |	""ПодтверждениеДатыОтправки"" КАК ИдТипа,
		                    |	СодержимоеТранспортныхКонтейнеров.Данные КАК Данные,
		                    |	СодержимоеТранспортныхКонтейнеров.Тип КАК Тип,
		                    |	СодержимоеТранспортныхКонтейнеров.ИмяФайла КАК ИмяФайла,
		                    |	"""" КАК ИмяПодписанногоФайла,
		                    |	"""" КАК Сертификат,
		                    |	СодержимоеТранспортныхКонтейнеров.ТранспортноеСообщение КАК ТранспортноеСообщение,
		                    |	ВЫБОР
		                    |		КОГДА СостояниеОбъектов.Архивный ЕСТЬ NULL
		                    |			ТОГДА ЛОЖЬ
		                    |		КОГДА СостояниеОбъектов.Архивный = ЛОЖЬ
		                    |			ТОГДА ЛОЖЬ
		                    |		ИНАЧЕ ИСТИНА
		                    |	КОНЕЦ КАК ВАрхиве
		                    |ИЗ
		                    |	РегистрСведений.СодержимоеТранспортныхКонтейнеров КАК СодержимоеТранспортныхКонтейнеров
		                    |		ЛЕВОЕ СОЕДИНЕНИЕ РегистрСведений.ПризнакиАрхивированияФайловДОСКонтролирующимиОрганами КАК СостояниеОбъектов
		                    |		ПО (СостояниеОбъектов.Объект = СодержимоеТранспортныхКонтейнеров.ТранспортноеСообщение)
		                    |			И (СостояниеОбъектов.ИмяФайла = СодержимоеТранспортныхКонтейнеров.ИмяФайла)
		                    |			И (СостояниеОбъектов.Владелец = ЗНАЧЕНИЕ(Перечисление.ВидыАрхивируемыхМетаданныхДО.СодержимоеТранспортныхКонтейнеров))
		                    |ГДЕ
		                    |	СодержимоеТранспортныхКонтейнеров.Тип В(&ТипСодержимогоПодтверждениеОбОтправке)
		                    |	И СодержимоеТранспортныхКонтейнеров.ТранспортноеСообщение.Тип В(&ТипСообщенияПодтверждениеОтправкиОтчетности)
		                    |	И СодержимоеТранспортныхКонтейнеров.ТранспортноеСообщение В (&ТранспортныеСообщения)
		                    |
		                    |ОБЪЕДИНИТЬ ВСЕ
		                    |
		                    |ВЫБРАТЬ
		                    |	""ПечататьПодтверждениеДатыОтправки"" КАК Настройка,
		                    |	""ПодтверждениеДатыОтправкиПодписьНалогоплательщика"",
		                    |	СодержимоеТранспортныхКонтейнеров.Данные,
		                    |	СодержимоеТранспортныхКонтейнеров.Тип,
		                    |	СодержимоеТранспортныхКонтейнеров.ИмяФайла,
		                    |	СодержимоеТранспортныхКонтейнеров.ЭЦПИмяПодписанногоФайла,
		                    |	СодержимоеТранспортныхКонтейнеров.ЭЦПСертификат,
		                    |	СодержимоеТранспортныхКонтейнеров.ТранспортноеСообщение,
		                    |	ВЫБОР
		                    |		КОГДА СостояниеОбъектов.Архивный ЕСТЬ NULL
		                    |			ТОГДА ЛОЖЬ
		                    |		КОГДА СостояниеОбъектов.Архивный = ЛОЖЬ
		                    |			ТОГДА ЛОЖЬ
		                    |		ИНАЧЕ ИСТИНА
		                    |	КОНЕЦ
		                    |ИЗ
		                    |	РегистрСведений.СодержимоеТранспортныхКонтейнеров КАК СодержимоеТранспортныхКонтейнеров
		                    |		ЛЕВОЕ СОЕДИНЕНИЕ РегистрСведений.ПризнакиАрхивированияФайловДОСКонтролирующимиОрганами КАК СостояниеОбъектов
		                    |		ПО (СостояниеОбъектов.Объект = СодержимоеТранспортныхКонтейнеров.ТранспортноеСообщение)
		                    |			И (СостояниеОбъектов.ИмяФайла = СодержимоеТранспортныхКонтейнеров.ИмяФайла)
		                    |			И (СостояниеОбъектов.Владелец = ЗНАЧЕНИЕ(Перечисление.ВидыАрхивируемыхМетаданныхДО.СодержимоеТранспортныхКонтейнеров))
		                    |ГДЕ
		                    |	СодержимоеТранспортныхКонтейнеров.Тип = &ТипСодержимогоЭлектронноЦифроваяПодпись
		                    |	И СодержимоеТранспортныхКонтейнеров.ТранспортноеСообщение.Тип = &ТипСообщенияПереподписанноеПодтверждениеОтправкиОтчетности
		                    |	И СодержимоеТранспортныхКонтейнеров.ТранспортноеСообщение В (&ТранспортныеСообщения)
		                    |
		                    |ОБЪЕДИНИТЬ ВСЕ
		                    |
		                    |ВЫБРАТЬ
		                    |	""ПечататьПодтверждениеДатыОтправки"" КАК Настройка,
		                    |	""ПодтверждениеДатыОтправкиПодписьПочтовогоСервера"",
		                    |	СодержимоеТранспортныхКонтейнеров.Данные,
		                    |	СодержимоеТранспортныхКонтейнеров.Тип,
		                    |	СодержимоеТранспортныхКонтейнеров.ИмяФайла,
		                    |	СодержимоеТранспортныхКонтейнеров.ЭЦПИмяПодписанногоФайла,
		                    |	СодержимоеТранспортныхКонтейнеров.ЭЦПСертификат,
		                    |	СодержимоеТранспортныхКонтейнеров.ТранспортноеСообщение,
		                    |	ВЫБОР
		                    |		КОГДА СостояниеОбъектов.Архивный ЕСТЬ NULL
		                    |			ТОГДА ЛОЖЬ
		                    |		КОГДА СостояниеОбъектов.Архивный = ЛОЖЬ
		                    |			ТОГДА ЛОЖЬ
		                    |		ИНАЧЕ ИСТИНА
		                    |	КОНЕЦ
		                    |ИЗ
		                    |	РегистрСведений.СодержимоеТранспортныхКонтейнеров КАК СодержимоеТранспортныхКонтейнеров
		                    |		ЛЕВОЕ СОЕДИНЕНИЕ РегистрСведений.ПризнакиАрхивированияФайловДОСКонтролирующимиОрганами КАК СостояниеОбъектов
		                    |		ПО (СостояниеОбъектов.Объект = СодержимоеТранспортныхКонтейнеров.ТранспортноеСообщение)
		                    |			И (СостояниеОбъектов.ИмяФайла = СодержимоеТранспортныхКонтейнеров.ИмяФайла)
		                    |			И (СостояниеОбъектов.Владелец = ЗНАЧЕНИЕ(Перечисление.ВидыАрхивируемыхМетаданныхДО.СодержимоеТранспортныхКонтейнеров))
		                    |ГДЕ
		                    |	СодержимоеТранспортныхКонтейнеров.Тип = &ТипСодержимогоЭлектронноЦифроваяПодпись
		                    |	И СодержимоеТранспортныхКонтейнеров.ТранспортноеСообщение.Тип В(&ТипСообщенияПодтверждениеОтправкиОтчетности)
		                    |	И СодержимоеТранспортныхКонтейнеров.ТранспортноеСообщение В (&ТранспортныеСообщения)");
		
		ТипСодержимогоПодтверждениеОбОтправке = Новый Массив;
		ТипСодержимогоПодтверждениеОбОтправке.Добавить(Перечисления.ТипыСодержимогоТранспортногоКонтейнера.ПодтверждениеДатыОтправки);
		ТипСодержимогоПодтверждениеОбОтправке.Добавить(Перечисления.ТипыСодержимогоТранспортногоКонтейнера.ПодтверждениеДатыОтправкиПредставление);
		Запрос.УстановитьПараметр("ТипСодержимогоПодтверждениеОбОтправке", ТипСодержимогоПодтверждениеОбОтправке);
		
		ТипСообщенияПодтверждениеОтправкиОтчетности = Новый Массив;
		ТипСообщенияПодтверждениеОтправкиОтчетности.Добавить(Перечисления.ТипыТранспортныхСообщений.ПодтверждениеДекларацияНО);
		ТипСообщенияПодтверждениеОтправкиОтчетности.Добавить(Перечисления.ТипыТранспортныхСообщений.ПодтверждениеЗаявлениеНО);
		ТипСообщенияПодтверждениеОтправкиОтчетности.Добавить(Перечисления.ТипыТранспортныхСообщений.ПодтверждениеФорма2НДФЛНО);
		ТипСообщенияПодтверждениеОтправкиОтчетности.Добавить(Перечисления.ТипыТранспортныхСообщений.ПодтверждениеЗапросНО);
		ТипСообщенияПодтверждениеОтправкиОтчетности.Добавить(Перечисления.ТипыТранспортныхСообщений.ПодтверждениеПредставлениеНО);
		ТипСообщенияПодтверждениеОтправкиОтчетности.Добавить(Перечисления.ТипыТранспортныхСообщений.ПодтверждениеОбращениеНО);
		
		Запрос.УстановитьПараметр("ТипСообщенияПодтверждениеОтправкиОтчетности", ТипСообщенияПодтверждениеОтправкиОтчетности);
		
		Запрос.УстановитьПараметр("ТипСодержимогоЭлектронноЦифроваяПодпись", Перечисления.ТипыСодержимогоТранспортногоКонтейнера.ЭлектронноЦифроваяПодпись);
		Запрос.УстановитьПараметр("ТипСообщенияПереподписанноеПодтверждениеОтправкиОтчетности", Перечисления.ТипыТранспортныхСообщений.ИзвещениеПодтверждениеНП);
	КонецЕсли;
	
	Если Настройки.ПечататьРезультатПриема Тогда
		Подзапросы.Добавить("ВЫБРАТЬ
		                    |	""ПечататьРезультатПриема"" КАК Настройка,
		                    |	""РезультатПриема"" КАК ИдТипа,
		                    |	СодержимоеТранспортныхКонтейнеров.Данные КАК Данные,
		                    |	СодержимоеТранспортныхКонтейнеров.Тип КАК Тип,
		                    |	СодержимоеТранспортныхКонтейнеров.ИмяФайла КАК ИмяФайла,
		                    |	"""" КАК ИмяПодписанногоФайла,
		                    |	"""" КАК Сертификат,
		                    |	СодержимоеТранспортныхКонтейнеров.ТранспортноеСообщение КАК ТранспортноеСообщение,
		                    |	ВЫБОР
		                    |		КОГДА СостояниеОбъектов.Архивный ЕСТЬ NULL
		                    |			ТОГДА ЛОЖЬ
		                    |		КОГДА СостояниеОбъектов.Архивный = ЛОЖЬ
		                    |			ТОГДА ЛОЖЬ
		                    |		ИНАЧЕ ИСТИНА
		                    |	КОНЕЦ КАК ВАрхиве
		                    |ИЗ
		                    |	РегистрСведений.СодержимоеТранспортныхКонтейнеров КАК СодержимоеТранспортныхКонтейнеров
		                    |		ЛЕВОЕ СОЕДИНЕНИЕ РегистрСведений.ПризнакиАрхивированияФайловДОСКонтролирующимиОрганами КАК СостояниеОбъектов
		                    |		ПО (СостояниеОбъектов.Объект = СодержимоеТранспортныхКонтейнеров.ТранспортноеСообщение)
		                    |			И (СостояниеОбъектов.ИмяФайла = СодержимоеТранспортныхКонтейнеров.ИмяФайла)
		                    |			И (СостояниеОбъектов.Владелец = ЗНАЧЕНИЕ(Перечисление.ВидыАрхивируемыхМетаданныхДО.СодержимоеТранспортныхКонтейнеров))
		                    |ГДЕ
		                    |	СодержимоеТранспортныхКонтейнеров.Тип В(&ТипСодержимогоПротоколВходногоКонтроля)
		                    |	И СодержимоеТранспортныхКонтейнеров.ТранспортноеСообщение.Тип В(&ТипСообщенияПротоколВходногоКонтроляОтчетности)
		                    |	И СодержимоеТранспортныхКонтейнеров.ТранспортноеСообщение В (&ТранспортныеСообщения)
		                    |
		                    |ОБЪЕДИНИТЬ ВСЕ
		                    |
		                    |ВЫБРАТЬ
		                    |	""ПечататьРезультатПриема"" КАК Настройка,
		                    |	""РезультатПриемаПодписьНалогоплательщика"",
		                    |	СодержимоеТранспортныхКонтейнеров.Данные,
		                    |	СодержимоеТранспортныхКонтейнеров.Тип,
		                    |	СодержимоеТранспортныхКонтейнеров.ИмяФайла,
		                    |	СодержимоеТранспортныхКонтейнеров.ЭЦПИмяПодписанногоФайла,
		                    |	СодержимоеТранспортныхКонтейнеров.ЭЦПСертификат,
		                    |	СодержимоеТранспортныхКонтейнеров.ТранспортноеСообщение,
		                    |	ВЫБОР
		                    |		КОГДА СостояниеОбъектов.Архивный ЕСТЬ NULL
		                    |			ТОГДА ЛОЖЬ
		                    |		КОГДА СостояниеОбъектов.Архивный = ЛОЖЬ
		                    |			ТОГДА ЛОЖЬ
		                    |		ИНАЧЕ ИСТИНА
		                    |	КОНЕЦ
		                    |ИЗ
		                    |	РегистрСведений.СодержимоеТранспортныхКонтейнеров КАК СодержимоеТранспортныхКонтейнеров
		                    |		ЛЕВОЕ СОЕДИНЕНИЕ РегистрСведений.ПризнакиАрхивированияФайловДОСКонтролирующимиОрганами КАК СостояниеОбъектов
		                    |		ПО (СостояниеОбъектов.Объект = СодержимоеТранспортныхКонтейнеров.ТранспортноеСообщение)
		                    |			И (СостояниеОбъектов.ИмяФайла = СодержимоеТранспортныхКонтейнеров.ИмяФайла)
		                    |			И (СостояниеОбъектов.Владелец = ЗНАЧЕНИЕ(Перечисление.ВидыАрхивируемыхМетаданныхДО.СодержимоеТранспортныхКонтейнеров))
		                    |ГДЕ
		                    |	СодержимоеТранспортныхКонтейнеров.Тип = &ТипСодержимогоЭлектронноЦифроваяПодпись
		                    |	И СодержимоеТранспортныхКонтейнеров.ТранспортноеСообщение.Тип = &ТипСообщенияПереподписанныйПротоколВходногоКонтроляОтчетности
		                    |	И СодержимоеТранспортныхКонтейнеров.ТранспортноеСообщение В (&ТранспортныеСообщения)
		                    |
		                    |ОБЪЕДИНИТЬ ВСЕ
		                    |
		                    |ВЫБРАТЬ
		                    |	""ПечататьРезультатПриема"" КАК Настройка,
		                    |	""РезультатПриемаПодписьНалоговогоОргана"",
		                    |	СодержимоеТранспортныхКонтейнеров.Данные,
		                    |	СодержимоеТранспортныхКонтейнеров.Тип,
		                    |	СодержимоеТранспортныхКонтейнеров.ИмяФайла,
		                    |	СодержимоеТранспортныхКонтейнеров.ЭЦПИмяПодписанногоФайла,
		                    |	СодержимоеТранспортныхКонтейнеров.ЭЦПСертификат,
		                    |	СодержимоеТранспортныхКонтейнеров.ТранспортноеСообщение,
		                    |	ВЫБОР
		                    |		КОГДА СостояниеОбъектов.Архивный ЕСТЬ NULL
		                    |			ТОГДА ЛОЖЬ
		                    |		КОГДА СостояниеОбъектов.Архивный = ЛОЖЬ
		                    |			ТОГДА ЛОЖЬ
		                    |		ИНАЧЕ ИСТИНА
		                    |	КОНЕЦ
		                    |ИЗ
		                    |	РегистрСведений.СодержимоеТранспортныхКонтейнеров КАК СодержимоеТранспортныхКонтейнеров
		                    |		ЛЕВОЕ СОЕДИНЕНИЕ РегистрСведений.ПризнакиАрхивированияФайловДОСКонтролирующимиОрганами КАК СостояниеОбъектов
		                    |		ПО (СостояниеОбъектов.Объект = СодержимоеТранспортныхКонтейнеров.ТранспортноеСообщение)
		                    |			И (СостояниеОбъектов.ИмяФайла = СодержимоеТранспортныхКонтейнеров.ИмяФайла)
		                    |			И (СостояниеОбъектов.Владелец = ЗНАЧЕНИЕ(Перечисление.ВидыАрхивируемыхМетаданныхДО.СодержимоеТранспортныхКонтейнеров))
		                    |ГДЕ
		                    |	СодержимоеТранспортныхКонтейнеров.Тип = &ТипСодержимогоЭлектронноЦифроваяПодпись
		                    |	И СодержимоеТранспортныхКонтейнеров.ТранспортноеСообщение.Тип В(&ТипСообщенияПротоколВходногоКонтроляОтчетности)
		                    |	И СодержимоеТранспортныхКонтейнеров.ТранспортноеСообщение В (&ТранспортныеСообщения)");
						 
		ТипСодержимогоПротоколВходногоКонтроля = Новый Массив;
		ТипСодержимогоПротоколВходногоКонтроля.Добавить(Перечисления.ТипыСодержимогоТранспортногоКонтейнера.КвитанцияОПриеме);
		ТипСодержимогоПротоколВходногоКонтроля.Добавить(Перечисления.ТипыСодержимогоТранспортногоКонтейнера.УведомлениеОбОтказе);
		ТипСодержимогоПротоколВходногоКонтроля.Добавить(Перечисления.ТипыСодержимогоТранспортногоКонтейнера.КвитанцияОПриемеЗаявления);
		ТипСодержимогоПротоколВходногоКонтроля.Добавить(Перечисления.ТипыСодержимогоТранспортногоКонтейнера.УведомлениеОбОтказеДокумент);
		ТипСодержимогоПротоколВходногоКонтроля.Добавить(Перечисления.ТипыСодержимогоТранспортногоКонтейнера.Протокол2НДФЛ);
		ТипСодержимогоПротоколВходногоКонтроля.Добавить(Перечисления.ТипыСодержимогоТранспортногоКонтейнера.Реестр2НДФЛ);
		ТипСодержимогоПротоколВходногоКонтроля.Добавить(Перечисления.ТипыСодержимогоТранспортногоКонтейнера.КвитанцияОПриемеДокумент);
		
		Запрос.УстановитьПараметр("ТипСодержимогоПротоколВходногоКонтроля", ТипСодержимогоПротоколВходногоКонтроля);
		
		ТипСообщенияПротоколВходногоКонтроляОтчетности = Новый Массив;
		ТипСообщенияПротоколВходногоКонтроляОтчетности.Добавить(Перечисления.ТипыТранспортныхСообщений.РезультатПриемаДекларацияНО);
		ТипСообщенияПротоколВходногоКонтроляОтчетности.Добавить(Перечисления.ТипыТранспортныхСообщений.РезультатПриемаЗаявлениеНО);
		ТипСообщенияПротоколВходногоКонтроляОтчетности.Добавить(Перечисления.ТипыТранспортныхСообщений.РезультатПриемаФорма2НДФЛНО);
		ТипСообщенияПротоколВходногоКонтроляОтчетности.Добавить(Перечисления.ТипыТранспортныхСообщений.РезультатПриемаЗапросНО);
		ТипСообщенияПротоколВходногоКонтроляОтчетности.Добавить(Перечисления.ТипыТранспортныхСообщений.РезультатПриемаПредставлениеНО);
		ТипСообщенияПротоколВходногоКонтроляОтчетности.Добавить(Перечисления.ТипыТранспортныхСообщений.РезультатПриемаДокументНП);
		
		Запрос.УстановитьПараметр("ТипСообщенияПротоколВходногоКонтроляОтчетности", ТипСообщенияПротоколВходногоКонтроляОтчетности);
		
		Запрос.УстановитьПараметр("ТипСодержимогоЭлектронноЦифроваяПодпись", Перечисления.ТипыСодержимогоТранспортногоКонтейнера.ЭлектронноЦифроваяПодпись);
		Запрос.УстановитьПараметр("ТипСообщенияПереподписанныйПротоколВходногоКонтроляОтчетности", Перечисления.ТипыТранспортныхСообщений.ИзвещениеРезультатПриемаНП);
	КонецЕсли;
	
	Если Настройки.ПечататьРезультатОбработки Тогда
		Подзапросы.Добавить("ВЫБРАТЬ
		                    |	""ПечататьРезультатОбработки"" КАК Настройка,
		                    |	""РезультатОбработки"" КАК ИдТипа,
		                    |	СодержимоеТранспортныхКонтейнеров.Данные КАК Данные,
		                    |	СодержимоеТранспортныхКонтейнеров.Тип КАК Тип,
		                    |	СодержимоеТранспортныхКонтейнеров.ИмяФайла КАК ИмяФайла,
		                    |	"""" КАК ИмяПодписанногоФайла,
		                    |	"""" КАК Сертификат,
		                    |	СодержимоеТранспортныхКонтейнеров.ТранспортноеСообщение КАК ТранспортноеСообщение,
		                    |	ВЫБОР
		                    |		КОГДА СостояниеОбъектов.Архивный ЕСТЬ NULL
		                    |			ТОГДА ЛОЖЬ
		                    |		КОГДА СостояниеОбъектов.Архивный = ЛОЖЬ
		                    |			ТОГДА ЛОЖЬ
		                    |		ИНАЧЕ ИСТИНА
		                    |	КОНЕЦ КАК ВАрхиве
		                    |ИЗ
		                    |	РегистрСведений.СодержимоеТранспортныхКонтейнеров КАК СодержимоеТранспортныхКонтейнеров
		                    |		ЛЕВОЕ СОЕДИНЕНИЕ РегистрСведений.ПризнакиАрхивированияФайловДОСКонтролирующимиОрганами КАК СостояниеОбъектов
		                    |		ПО (СостояниеОбъектов.Объект = СодержимоеТранспортныхКонтейнеров.ТранспортноеСообщение)
		                    |			И (СостояниеОбъектов.ИмяФайла = СодержимоеТранспортныхКонтейнеров.ИмяФайла)
		                    |			И (СостояниеОбъектов.Владелец = ЗНАЧЕНИЕ(Перечисление.ВидыАрхивируемыхМетаданныхДО.СодержимоеТранспортныхКонтейнеров))
		                    |ГДЕ
		                    |	СодержимоеТранспортныхКонтейнеров.Тип В(&ТипСодержимогоРезультатаОбработки)
		                    |	И (СодержимоеТранспортныхКонтейнеров.ТранспортноеСообщение.Тип = &ТипСообщенияРезультатОбработкиДекларацияНО
		                    |			ИЛИ СодержимоеТранспортныхКонтейнеров.ТранспортноеСообщение.Тип = &ТипСообщенияРезультатОбработкиЗаявлениеРФНО
		                    |			ИЛИ СодержимоеТранспортныхКонтейнеров.ТранспортноеСообщение.Тип = &ТипСообщенияРезультатОбработкиЗаявлениеНО)
		                    |	И СодержимоеТранспортныхКонтейнеров.ТранспортноеСообщение В (&ТранспортныеСообщения)
		                    |
		                    |ОБЪЕДИНИТЬ ВСЕ
		                    |
		                    |ВЫБРАТЬ
		                    |	""ПечататьРезультатОбработки"" КАК Настройка,
		                    |	""РезультатОбработкиПодписьНалогоплательщика"",
		                    |	СодержимоеТранспортныхКонтейнеров.Данные,
		                    |	СодержимоеТранспортныхКонтейнеров.Тип,
		                    |	СодержимоеТранспортныхКонтейнеров.ИмяФайла,
		                    |	СодержимоеТранспортныхКонтейнеров.ЭЦПИмяПодписанногоФайла,
		                    |	СодержимоеТранспортныхКонтейнеров.ЭЦПСертификат,
		                    |	СодержимоеТранспортныхКонтейнеров.ТранспортноеСообщение,
		                    |	ВЫБОР
		                    |		КОГДА СостояниеОбъектов.Архивный ЕСТЬ NULL
		                    |			ТОГДА ЛОЖЬ
		                    |		КОГДА СостояниеОбъектов.Архивный = ЛОЖЬ
		                    |			ТОГДА ЛОЖЬ
		                    |		ИНАЧЕ ИСТИНА
		                    |	КОНЕЦ
		                    |ИЗ
		                    |	РегистрСведений.СодержимоеТранспортныхКонтейнеров КАК СодержимоеТранспортныхКонтейнеров
		                    |		ЛЕВОЕ СОЕДИНЕНИЕ РегистрСведений.ПризнакиАрхивированияФайловДОСКонтролирующимиОрганами КАК СостояниеОбъектов
		                    |		ПО (СостояниеОбъектов.Объект = СодержимоеТранспортныхКонтейнеров.ТранспортноеСообщение)
		                    |			И (СостояниеОбъектов.ИмяФайла = СодержимоеТранспортныхКонтейнеров.ИмяФайла)
		                    |			И (СостояниеОбъектов.Владелец = ЗНАЧЕНИЕ(Перечисление.ВидыАрхивируемыхМетаданныхДО.СодержимоеТранспортныхКонтейнеров))
		                    |ГДЕ
		                    |	СодержимоеТранспортныхКонтейнеров.Тип = &ТипСодержимогоЭлектронноЦифроваяПодпись
		                    |	И (СодержимоеТранспортныхКонтейнеров.ТранспортноеСообщение.Тип = &ТипСообщенияИзвещениеРезультатОбработкиНП
		                    |			ИЛИ СодержимоеТранспортныхКонтейнеров.ТранспортноеСообщение.Тип = &ТипСообщенияИзвещениеРезультатОбработкиРФНП)
		                    |	И СодержимоеТранспортныхКонтейнеров.ТранспортноеСообщение В (&ТранспортныеСообщения)
		                    |
		                    |ОБЪЕДИНИТЬ ВСЕ
		                    |
		                    |ВЫБРАТЬ
		                    |	""ПечататьРезультатОбработки"" КАК Настройка,
		                    |	""РезультатОбработкиПодписьНалоговогоОргана"",
		                    |	СодержимоеТранспортныхКонтейнеров.Данные,
		                    |	СодержимоеТранспортныхКонтейнеров.Тип,
		                    |	СодержимоеТранспортныхКонтейнеров.ИмяФайла,
		                    |	СодержимоеТранспортныхКонтейнеров.ЭЦПИмяПодписанногоФайла,
		                    |	СодержимоеТранспортныхКонтейнеров.ЭЦПСертификат,
		                    |	СодержимоеТранспортныхКонтейнеров.ТранспортноеСообщение,
		                    |	ВЫБОР
		                    |		КОГДА СостояниеОбъектов.Архивный ЕСТЬ NULL
		                    |			ТОГДА ЛОЖЬ
		                    |		КОГДА СостояниеОбъектов.Архивный = ЛОЖЬ
		                    |			ТОГДА ЛОЖЬ
		                    |		ИНАЧЕ ИСТИНА
		                    |	КОНЕЦ
		                    |ИЗ
		                    |	РегистрСведений.СодержимоеТранспортныхКонтейнеров КАК СодержимоеТранспортныхКонтейнеров
		                    |		ЛЕВОЕ СОЕДИНЕНИЕ РегистрСведений.ПризнакиАрхивированияФайловДОСКонтролирующимиОрганами КАК СостояниеОбъектов
		                    |		ПО (СостояниеОбъектов.Объект = СодержимоеТранспортныхКонтейнеров.ТранспортноеСообщение)
		                    |			И (СостояниеОбъектов.ИмяФайла = СодержимоеТранспортныхКонтейнеров.ИмяФайла)
		                    |			И (СостояниеОбъектов.Владелец = ЗНАЧЕНИЕ(Перечисление.ВидыАрхивируемыхМетаданныхДО.СодержимоеТранспортныхКонтейнеров))
		                    |ГДЕ
		                    |	СодержимоеТранспортныхКонтейнеров.Тип = &ТипСодержимогоЭлектронноЦифроваяПодпись
		                    |	И (СодержимоеТранспортныхКонтейнеров.ТранспортноеСообщение.Тип = &ТипСообщенияРезультатОбработкиДекларацияНО
		                    |			ИЛИ СодержимоеТранспортныхКонтейнеров.ТранспортноеСообщение.Тип = &ТипСообщенияРезультатОбработкиЗаявлениеРФНО
		                    |			ИЛИ СодержимоеТранспортныхКонтейнеров.ТранспортноеСообщение.Тип = &ТипСообщенияРезультатОбработкиЗаявлениеНО)
		                    |	И СодержимоеТранспортныхКонтейнеров.ТранспортноеСообщение В (&ТранспортныеСообщения)");
						 
		ТипСодержимогоРезультатаОбработки = Новый Массив;
		ТипСодержимогоРезультатаОбработки.Добавить(Перечисления.ТипыСодержимогоТранспортногоКонтейнера.ИзвещениеОВводе);
		ТипСодержимогоРезультатаОбработки.Добавить(Перечисления.ТипыСодержимогоТранспортногоКонтейнера.УведомлениеОбУточнении);
		ТипСодержимогоРезультатаОбработки.Добавить(Перечисления.ТипыСодержимогоТранспортногоКонтейнера.СообщениеОПроверке);
		
		ТипСодержимогоРезультатаОбработки.Добавить(Перечисления.ТипыСодержимогоТранспортногоКонтейнера.СообщениеОПростОтметки);
		ТипСодержимогоРезультатаОбработки.Добавить(Перечисления.ТипыСодержимогоТранспортногоКонтейнера.УведомлениеОбОтказеОтметки);
		
		Запрос.УстановитьПараметр("ТипСодержимогоРезультатаОбработки", ТипСодержимогоРезультатаОбработки);
		
		Запрос.УстановитьПараметр("ТипСообщенияРезультатОбработкиДекларацияНО", Перечисления.ТипыТранспортныхСообщений.РезультатОбработкиДекларацияНО);
		Запрос.УстановитьПараметр("ТипСодержимогоЭлектронноЦифроваяПодпись", Перечисления.ТипыСодержимогоТранспортногоКонтейнера.ЭлектронноЦифроваяПодпись);
		Запрос.УстановитьПараметр("ТипСообщенияИзвещениеРезультатОбработкиНП", Перечисления.ТипыТранспортныхСообщений.ИзвещениеРезультатОбработкиНП);
		
		Запрос.УстановитьПараметр("ТипСообщенияРезультатОбработкиЗаявлениеНО", Перечисления.ТипыТранспортныхСообщений.РезультатОбработкиЗаявлениеНО);
		
		Запрос.УстановитьПараметр("ТипСообщенияРезультатОбработкиЗаявлениеРФНО", Перечисления.ТипыТранспортныхСообщений.РезультатОбработкиЗаявлениеРФНО);
		Запрос.УстановитьПараметр("ТипСообщенияИзвещениеРезультатОбработкиРФНП", Перечисления.ТипыТранспортныхСообщений.ИзвещениеРезультатОбработкиРФНП);
	КонецЕсли;
	
	Если Настройки.ПечататьСообщениеОбОтзыве Тогда
		Подзапросы.Добавить("ВЫБРАТЬ
		                    |	""ПечататьСообщениеОбОтзыве"" КАК Настройка,
		                    |	""СообщениеОбОтзыве"" КАК ИдТипа,
		                    |	СодержимоеТранспортныхКонтейнеров.Данные КАК Данные,
		                    |	СодержимоеТранспортныхКонтейнеров.Тип КАК Тип,
		                    |	СодержимоеТранспортныхКонтейнеров.ИмяФайла КАК ИмяФайла,
		                    |	"""" КАК ИмяПодписанногоФайла,
		                    |	"""" КАК Сертификат,
		                    |	СодержимоеТранспортныхКонтейнеров.ТранспортноеСообщение КАК ТранспортноеСообщение,
		                    |	ВЫБОР
		                    |		КОГДА СостояниеОбъектов.Архивный ЕСТЬ NULL
		                    |			ТОГДА ЛОЖЬ
		                    |		КОГДА СостояниеОбъектов.Архивный = ЛОЖЬ
		                    |			ТОГДА ЛОЖЬ
		                    |		ИНАЧЕ ИСТИНА
		                    |	КОНЕЦ КАК ВАрхиве
		                    |ИЗ
		                    |	РегистрСведений.СодержимоеТранспортныхКонтейнеров КАК СодержимоеТранспортныхКонтейнеров
		                    |		ЛЕВОЕ СОЕДИНЕНИЕ РегистрСведений.ПризнакиАрхивированияФайловДОСКонтролирующимиОрганами КАК СостояниеОбъектов
		                    |		ПО (СостояниеОбъектов.Объект = СодержимоеТранспортныхКонтейнеров.ТранспортноеСообщение)
		                    |			И (СостояниеОбъектов.ИмяФайла = СодержимоеТранспортныхКонтейнеров.ИмяФайла)
		                    |			И (СостояниеОбъектов.Владелец = ЗНАЧЕНИЕ(Перечисление.ВидыАрхивируемыхМетаданныхДО.СодержимоеТранспортныхКонтейнеров))
		                    |ГДЕ
		                    |	СодержимоеТранспортныхКонтейнеров.Тип В(&ТипСодержимогоСообщенияОбОтзыве)
		                    |	И СодержимоеТранспортныхКонтейнеров.ТранспортноеСообщение.Тип = &ТипСообщенияСообщениеОбОтзывеЗаявлениеРФНО
		                    |	И СодержимоеТранспортныхКонтейнеров.ТранспортноеСообщение В (&ТранспортныеСообщения)
		                    |
		                    |ОБЪЕДИНИТЬ ВСЕ
		                    |
		                    |ВЫБРАТЬ
		                    |	""ПечататьСообщениеОбОтзыве"",
		                    |	""СообщениеОбОтзывеПодписьНалогоплательщика"",
		                    |	СодержимоеТранспортныхКонтейнеров.Данные,
		                    |	СодержимоеТранспортныхКонтейнеров.Тип,
		                    |	СодержимоеТранспортныхКонтейнеров.ИмяФайла,
		                    |	СодержимоеТранспортныхКонтейнеров.ЭЦПИмяПодписанногоФайла,
		                    |	СодержимоеТранспортныхКонтейнеров.ЭЦПСертификат,
		                    |	СодержимоеТранспортныхКонтейнеров.ТранспортноеСообщение,
		                    |	ВЫБОР
		                    |		КОГДА СостояниеОбъектов.Архивный ЕСТЬ NULL
		                    |			ТОГДА ЛОЖЬ
		                    |		КОГДА СостояниеОбъектов.Архивный = ЛОЖЬ
		                    |			ТОГДА ЛОЖЬ
		                    |		ИНАЧЕ ИСТИНА
		                    |	КОНЕЦ
		                    |ИЗ
		                    |	РегистрСведений.СодержимоеТранспортныхКонтейнеров КАК СодержимоеТранспортныхКонтейнеров
		                    |		ЛЕВОЕ СОЕДИНЕНИЕ РегистрСведений.ПризнакиАрхивированияФайловДОСКонтролирующимиОрганами КАК СостояниеОбъектов
		                    |		ПО (СостояниеОбъектов.Объект = СодержимоеТранспортныхКонтейнеров.ТранспортноеСообщение)
		                    |			И (СостояниеОбъектов.ИмяФайла = СодержимоеТранспортныхКонтейнеров.ИмяФайла)
		                    |			И (СостояниеОбъектов.Владелец = ЗНАЧЕНИЕ(Перечисление.ВидыАрхивируемыхМетаданныхДО.СодержимоеТранспортныхКонтейнеров))
		                    |ГДЕ
		                    |	СодержимоеТранспортныхКонтейнеров.Тип = &ТипСодержимогоЭлектронноЦифроваяПодпись
		                    |	И СодержимоеТранспортныхКонтейнеров.ТранспортноеСообщение.Тип = &ТипСообщенияИзвещениеОбОтзывеЗаявлениеРФНП
		                    |	И СодержимоеТранспортныхКонтейнеров.ТранспортноеСообщение В (&ТранспортныеСообщения)
		                    |
		                    |ОБЪЕДИНИТЬ ВСЕ
		                    |
		                    |ВЫБРАТЬ
		                    |	""ПечататьСообщениеОбОтзыве"",
		                    |	""СообщениеОбОтзывеПодписьНалоговогоОргана"",
		                    |	СодержимоеТранспортныхКонтейнеров.Данные,
		                    |	СодержимоеТранспортныхКонтейнеров.Тип,
		                    |	СодержимоеТранспортныхКонтейнеров.ИмяФайла,
		                    |	СодержимоеТранспортныхКонтейнеров.ЭЦПИмяПодписанногоФайла,
		                    |	СодержимоеТранспортныхКонтейнеров.ЭЦПСертификат,
		                    |	СодержимоеТранспортныхКонтейнеров.ТранспортноеСообщение,
		                    |	ВЫБОР
		                    |		КОГДА СостояниеОбъектов.Архивный ЕСТЬ NULL
		                    |			ТОГДА ЛОЖЬ
		                    |		КОГДА СостояниеОбъектов.Архивный = ЛОЖЬ
		                    |			ТОГДА ЛОЖЬ
		                    |		ИНАЧЕ ИСТИНА
		                    |	КОНЕЦ
		                    |ИЗ
		                    |	РегистрСведений.СодержимоеТранспортныхКонтейнеров КАК СодержимоеТранспортныхКонтейнеров
		                    |		ЛЕВОЕ СОЕДИНЕНИЕ РегистрСведений.ПризнакиАрхивированияФайловДОСКонтролирующимиОрганами КАК СостояниеОбъектов
		                    |		ПО (СостояниеОбъектов.Объект = СодержимоеТранспортныхКонтейнеров.ТранспортноеСообщение)
		                    |			И (СостояниеОбъектов.ИмяФайла = СодержимоеТранспортныхКонтейнеров.ИмяФайла)
		                    |			И (СостояниеОбъектов.Владелец = ЗНАЧЕНИЕ(Перечисление.ВидыАрхивируемыхМетаданныхДО.СодержимоеТранспортныхКонтейнеров))
		                    |ГДЕ
		                    |	СодержимоеТранспортныхКонтейнеров.Тип = &ТипСодержимогоЭлектронноЦифроваяПодпись
		                    |	И СодержимоеТранспортныхКонтейнеров.ТранспортноеСообщение.Тип = &ТипСообщенияСообщениеОбОтзывеЗаявлениеРФНО
		                    |	И СодержимоеТранспортныхКонтейнеров.ТранспортноеСообщение В (&ТранспортныеСообщения)");
						 
		ТипСодержимогоСообщенияОбОтзыве = Новый Массив;
		ТипСодержимогоСообщенияОбОтзыве.Добавить(Перечисления.ТипыСодержимогоТранспортногоКонтейнера.СообщениеОбОтзывеЗаявления);
		Запрос.УстановитьПараметр("ТипСодержимогоСообщенияОбОтзыве", ТипСодержимогоСообщенияОбОтзыве);
		
		Запрос.УстановитьПараметр("ТипСообщенияСообщениеОбОтзывеЗаявлениеРФНО", Перечисления.ТипыТранспортныхСообщений.СообщениеОбОтзывеЗаявлениеРФНО);
		Запрос.УстановитьПараметр("ТипСодержимогоЭлектронноЦифроваяПодпись", Перечисления.ТипыСодержимогоТранспортногоКонтейнера.ЭлектронноЦифроваяПодпись);
		Запрос.УстановитьПараметр("ТипСообщенияИзвещениеОбОтзывеЗаявлениеРФНП", Перечисления.ТипыТранспортныхСообщений.ИзвещениеОбОтзывеЗаявлениеРФНП);
	КонецЕсли;
	
	Если Настройки.ПечататьСообщениеОНесоответствиях Тогда
		Подзапросы.Добавить("ВЫБРАТЬ
		                    |	""ПечататьСообщениеОНесоответствиях"" КАК Настройка,
		                    |	""СообщениеОНесоответствиях"" КАК ИдТипа,
		                    |	СодержимоеТранспортныхКонтейнеров.Данные КАК Данные,
		                    |	СодержимоеТранспортныхКонтейнеров.Тип КАК Тип,
		                    |	СодержимоеТранспортныхКонтейнеров.ИмяФайла КАК ИмяФайла,
		                    |	"""" КАК ИмяПодписанногоФайла,
		                    |	"""" КАК Сертификат,
		                    |	СодержимоеТранспортныхКонтейнеров.ТранспортноеСообщение КАК ТранспортноеСообщение,
		                    |	ВЫБОР
		                    |		КОГДА СостояниеОбъектов.Архивный ЕСТЬ NULL
		                    |			ТОГДА ЛОЖЬ
		                    |		КОГДА СостояниеОбъектов.Архивный = ЛОЖЬ
		                    |			ТОГДА ЛОЖЬ
		                    |		ИНАЧЕ ИСТИНА
		                    |	КОНЕЦ КАК ВАрхиве
		                    |ИЗ
		                    |	РегистрСведений.СодержимоеТранспортныхКонтейнеров КАК СодержимоеТранспортныхКонтейнеров
		                    |		ЛЕВОЕ СОЕДИНЕНИЕ РегистрСведений.ПризнакиАрхивированияФайловДОСКонтролирующимиОрганами КАК СостояниеОбъектов
		                    |		ПО (СостояниеОбъектов.Объект = СодержимоеТранспортныхКонтейнеров.ТранспортноеСообщение)
		                    |			И (СостояниеОбъектов.ИмяФайла = СодержимоеТранспортныхКонтейнеров.ИмяФайла)
		                    |			И (СостояниеОбъектов.Владелец = ЗНАЧЕНИЕ(Перечисление.ВидыАрхивируемыхМетаданныхДО.СодержимоеТранспортныхКонтейнеров))
		                    |ГДЕ
		                    |	СодержимоеТранспортныхКонтейнеров.Тип В(&ТипСодержимогоСообщенияОНесоответствиях)
		                    |	И СодержимоеТранспортныхКонтейнеров.ТранспортноеСообщение.Тип = &ТипСообщенияРезультатОбработкиЗаявлениеТСНО
		                    |	И СодержимоеТранспортныхКонтейнеров.ТранспортноеСообщение В (&ТранспортныеСообщения)
		                    |
		                    |ОБЪЕДИНИТЬ ВСЕ
		                    |
		                    |ВЫБРАТЬ
		                    |	""ПечататьСообщениеОНесоответствиях"",
		                    |	""СообщениеОНесоответствияхПодписьНалогоплательщика"",
		                    |	СодержимоеТранспортныхКонтейнеров.Данные,
		                    |	СодержимоеТранспортныхКонтейнеров.Тип,
		                    |	СодержимоеТранспортныхКонтейнеров.ИмяФайла,
		                    |	СодержимоеТранспортныхКонтейнеров.ЭЦПИмяПодписанногоФайла,
		                    |	СодержимоеТранспортныхКонтейнеров.ЭЦПСертификат,
		                    |	СодержимоеТранспортныхКонтейнеров.ТранспортноеСообщение,
		                    |	ВЫБОР
		                    |		КОГДА СостояниеОбъектов.Архивный ЕСТЬ NULL
		                    |			ТОГДА ЛОЖЬ
		                    |		КОГДА СостояниеОбъектов.Архивный = ЛОЖЬ
		                    |			ТОГДА ЛОЖЬ
		                    |		ИНАЧЕ ИСТИНА
		                    |	КОНЕЦ
		                    |ИЗ
		                    |	РегистрСведений.СодержимоеТранспортныхКонтейнеров КАК СодержимоеТранспортныхКонтейнеров
		                    |		ЛЕВОЕ СОЕДИНЕНИЕ РегистрСведений.ПризнакиАрхивированияФайловДОСКонтролирующимиОрганами КАК СостояниеОбъектов
		                    |		ПО (СостояниеОбъектов.Объект = СодержимоеТранспортныхКонтейнеров.ТранспортноеСообщение)
		                    |			И (СостояниеОбъектов.ИмяФайла = СодержимоеТранспортныхКонтейнеров.ИмяФайла)
		                    |			И (СостояниеОбъектов.Владелец = ЗНАЧЕНИЕ(Перечисление.ВидыАрхивируемыхМетаданныхДО.СодержимоеТранспортныхКонтейнеров))
		                    |ГДЕ
		                    |	СодержимоеТранспортныхКонтейнеров.Тип = &ТипСодержимогоЭлектронноЦифроваяПодпись
		                    |	И СодержимоеТранспортныхКонтейнеров.ТранспортноеСообщение.Тип = &ТипСообщенияИзвещениеРезультатОбработкиТСНП
		                    |	И СодержимоеТранспортныхКонтейнеров.ТранспортноеСообщение В (&ТранспортныеСообщения)
		                    |
		                    |ОБЪЕДИНИТЬ ВСЕ
		                    |
		                    |ВЫБРАТЬ
		                    |	""ПечататьСообщениеОНесоответствиях"",
		                    |	""СообщениеОНесоответствияхПодписьНалоговогоОргана"",
		                    |	СодержимоеТранспортныхКонтейнеров.Данные,
		                    |	СодержимоеТранспортныхКонтейнеров.Тип,
		                    |	СодержимоеТранспортныхКонтейнеров.ИмяФайла,
		                    |	СодержимоеТранспортныхКонтейнеров.ЭЦПИмяПодписанногоФайла,
		                    |	СодержимоеТранспортныхКонтейнеров.ЭЦПСертификат,
		                    |	СодержимоеТранспортныхКонтейнеров.ТранспортноеСообщение,
		                    |	ВЫБОР
		                    |		КОГДА СостояниеОбъектов.Архивный ЕСТЬ NULL
		                    |			ТОГДА ЛОЖЬ
		                    |		КОГДА СостояниеОбъектов.Архивный = ЛОЖЬ
		                    |			ТОГДА ЛОЖЬ
		                    |		ИНАЧЕ ИСТИНА
		                    |	КОНЕЦ
		                    |ИЗ
		                    |	РегистрСведений.СодержимоеТранспортныхКонтейнеров КАК СодержимоеТранспортныхКонтейнеров
		                    |		ЛЕВОЕ СОЕДИНЕНИЕ РегистрСведений.ПризнакиАрхивированияФайловДОСКонтролирующимиОрганами КАК СостояниеОбъектов
		                    |		ПО (СостояниеОбъектов.Объект = СодержимоеТранспортныхКонтейнеров.ТранспортноеСообщение)
		                    |			И (СостояниеОбъектов.ИмяФайла = СодержимоеТранспортныхКонтейнеров.ИмяФайла)
		                    |			И (СостояниеОбъектов.Владелец = ЗНАЧЕНИЕ(Перечисление.ВидыАрхивируемыхМетаданныхДО.СодержимоеТранспортныхКонтейнеров))
		                    |ГДЕ
		                    |	СодержимоеТранспортныхКонтейнеров.Тип = &ТипСодержимогоЭлектронноЦифроваяПодпись
		                    |	И СодержимоеТранспортныхКонтейнеров.ТранспортноеСообщение.Тип = &ТипСообщенияРезультатОбработкиЗаявлениеТСНО
		                    |	И СодержимоеТранспортныхКонтейнеров.ТранспортноеСообщение В (&ТранспортныеСообщения)");
						 
		ТипСодержимогоСообщенияОНесоответствиях = Новый Массив;
		ТипСодержимогоСообщенияОНесоответствиях.Добавить(Перечисления.ТипыСодержимогоТранспортногоКонтейнера.СообщениеОНесоответствиях);
		Запрос.УстановитьПараметр("ТипСодержимогоСообщенияОНесоответствиях", ТипСодержимогоСообщенияОНесоответствиях);
		
		Запрос.УстановитьПараметр("ТипСообщенияРезультатОбработкиЗаявлениеТСНО", Перечисления.ТипыТранспортныхСообщений.РезультатОбработкиЗаявлениеТСНО);
		Запрос.УстановитьПараметр("ТипСодержимогоЭлектронноЦифроваяПодпись", Перечисления.ТипыСодержимогоТранспортногоКонтейнера.ЭлектронноЦифроваяПодпись);
		Запрос.УстановитьПараметр("ТипСообщенияИзвещениеРезультатОбработкиТСНП", Перечисления.ТипыТранспортныхСообщений.ИзвещениеРезультатОбработкиТСНП);
	КонецЕсли;
	
	Если Настройки.ПечататьИзвещенияОПолучении Тогда
		Подзапросы.Добавить("ВЫБРАТЬ
		                    |	""ПечататьИзвещенияОПолучении"" КАК Настройка,
		                    |	""ИзвещениеОПолучении"" КАК ИдТипа,
		                    |	СодержимоеТранспортныхКонтейнеров.Данные КАК Данные,
		                    |	СодержимоеТранспортныхКонтейнеров.Тип КАК Тип,
		                    |	СодержимоеТранспортныхКонтейнеров.ИмяФайла КАК ИмяФайла,
		                    |	"""" КАК ИмяПодписанногоФайла,
		                    |	"""" КАК Сертификат,
		                    |	СодержимоеТранспортныхКонтейнеров.ТранспортноеСообщение КАК ТранспортноеСообщение,
		                    |	ВЫБОР
		                    |		КОГДА СостояниеОбъектов.Архивный ЕСТЬ NULL
		                    |			ТОГДА ЛОЖЬ
		                    |		КОГДА СостояниеОбъектов.Архивный = ЛОЖЬ
		                    |			ТОГДА ЛОЖЬ
		                    |		ИНАЧЕ ИСТИНА
		                    |	КОНЕЦ КАК ВАрхиве
		                    |ИЗ
		                    |	РегистрСведений.СодержимоеТранспортныхКонтейнеров КАК СодержимоеТранспортныхКонтейнеров
		                    |		ЛЕВОЕ СОЕДИНЕНИЕ РегистрСведений.ПризнакиАрхивированияФайловДОСКонтролирующимиОрганами КАК СостояниеОбъектов
		                    |		ПО (СостояниеОбъектов.Объект = СодержимоеТранспортныхКонтейнеров.ТранспортноеСообщение)
		                    |			И (СостояниеОбъектов.ИмяФайла = СодержимоеТранспортныхКонтейнеров.ИмяФайла)
		                    |			И (СостояниеОбъектов.Владелец = ЗНАЧЕНИЕ(Перечисление.ВидыАрхивируемыхМетаданныхДО.СодержимоеТранспортныхКонтейнеров))
		                    |ГДЕ
		                    |	СодержимоеТранспортныхКонтейнеров.Тип В(&ТипСодержимогоИзвещенияОПолучении)
		                    |	И СодержимоеТранспортныхКонтейнеров.ТранспортноеСообщение.Тип В(&ТипСообщенияИзвещенияОПолучении)
		                    |	И СодержимоеТранспортныхКонтейнеров.ТранспортноеСообщение  В (&ТранспортныеСообщения)
		                    |
		                    |ОБЪЕДИНИТЬ ВСЕ
		                    |
		                    |ВЫБРАТЬ
		                    |	""ПечататьИзвещенияОПолучении"",
		                    |	""ИзвещениеОПолученииПодписьНалогоплательщика"",
		                    |	СодержимоеТранспортныхКонтейнеров.Данные,
		                    |	СодержимоеТранспортныхКонтейнеров.Тип,
		                    |	СодержимоеТранспортныхКонтейнеров.ИмяФайла,
		                    |	СодержимоеТранспортныхКонтейнеров.ЭЦПИмяПодписанногоФайла,
		                    |	СодержимоеТранспортныхКонтейнеров.ЭЦПСертификат,
		                    |	СодержимоеТранспортныхКонтейнеров.ТранспортноеСообщение,
		                    |	ВЫБОР
		                    |		КОГДА СостояниеОбъектов.Архивный ЕСТЬ NULL
		                    |			ТОГДА ЛОЖЬ
		                    |		КОГДА СостояниеОбъектов.Архивный = ЛОЖЬ
		                    |			ТОГДА ЛОЖЬ
		                    |		ИНАЧЕ ИСТИНА
		                    |	КОНЕЦ
		                    |ИЗ
		                    |	РегистрСведений.СодержимоеТранспортныхКонтейнеров КАК СодержимоеТранспортныхКонтейнеров
		                    |		ЛЕВОЕ СОЕДИНЕНИЕ РегистрСведений.ПризнакиАрхивированияФайловДОСКонтролирующимиОрганами КАК СостояниеОбъектов
		                    |		ПО (СостояниеОбъектов.Объект = СодержимоеТранспортныхКонтейнеров.ТранспортноеСообщение)
		                    |			И (СостояниеОбъектов.ИмяФайла = СодержимоеТранспортныхКонтейнеров.ИмяФайла)
		                    |			И (СостояниеОбъектов.Владелец = ЗНАЧЕНИЕ(Перечисление.ВидыАрхивируемыхМетаданныхДО.СодержимоеТранспортныхКонтейнеров))
		                    |ГДЕ
		                    |	СодержимоеТранспортныхКонтейнеров.Тип = &ТипСодержимогоЭлектронноЦифроваяПодпись
		                    |	И СодержимоеТранспортныхКонтейнеров.ТранспортноеСообщение.Тип В(&ТипСообщенияИзвещенияОПолучении)
		                    |	И СодержимоеТранспортныхКонтейнеров.ЭЦПЭтоПодписьАбонента = ИСТИНА
		                    |	И СодержимоеТранспортныхКонтейнеров.ТранспортноеСообщение В (&ТранспортныеСообщения)
		                    |
		                    |ОБЪЕДИНИТЬ ВСЕ
		                    |
		                    |ВЫБРАТЬ
		                    |	""ПечататьИзвещенияОПолучении"",
		                    |	""ИзвещениеОПолученииПочтовогоСервера"",
		                    |	СодержимоеТранспортныхКонтейнеров.Данные,
		                    |	СодержимоеТранспортныхКонтейнеров.Тип,
		                    |	СодержимоеТранспортныхКонтейнеров.ИмяФайла,
		                    |	СодержимоеТранспортныхКонтейнеров.ЭЦПИмяПодписанногоФайла,
		                    |	СодержимоеТранспортныхКонтейнеров.ЭЦПСертификат,
		                    |	СодержимоеТранспортныхКонтейнеров.ТранспортноеСообщение,
		                    |	ВЫБОР
		                    |		КОГДА СостояниеОбъектов.Архивный ЕСТЬ NULL
		                    |			ТОГДА ЛОЖЬ
		                    |		КОГДА СостояниеОбъектов.Архивный = ЛОЖЬ
		                    |			ТОГДА ЛОЖЬ
		                    |		ИНАЧЕ ИСТИНА
		                    |	КОНЕЦ
		                    |ИЗ
		                    |	РегистрСведений.СодержимоеТранспортныхКонтейнеров КАК СодержимоеТранспортныхКонтейнеров
		                    |		ЛЕВОЕ СОЕДИНЕНИЕ РегистрСведений.ПризнакиАрхивированияФайловДОСКонтролирующимиОрганами КАК СостояниеОбъектов
		                    |		ПО (СостояниеОбъектов.Объект = СодержимоеТранспортныхКонтейнеров.ТранспортноеСообщение)
		                    |			И (СостояниеОбъектов.ИмяФайла = СодержимоеТранспортныхКонтейнеров.ИмяФайла)
		                    |			И (СостояниеОбъектов.Владелец = ЗНАЧЕНИЕ(Перечисление.ВидыАрхивируемыхМетаданныхДО.СодержимоеТранспортныхКонтейнеров))
		                    |ГДЕ
		                    |	СодержимоеТранспортныхКонтейнеров.Тип = &ТипСодержимогоЭлектронноЦифроваяПодпись
		                    |	И СодержимоеТранспортныхКонтейнеров.ТранспортноеСообщение.Тип В(&ТипСообщенияИзвещенияОПолучении)
		                    |	И СодержимоеТранспортныхКонтейнеров.ЭЦПЭтоПодписьАбонента = ЛОЖЬ
		                    |	И СодержимоеТранспортныхКонтейнеров.ТранспортноеСообщение В (&ТранспортныеСообщения)");
		
		ТипСодержимогоИзвещенияОПолучении = Новый Массив;
		ТипСодержимогоИзвещенияОПолучении.Добавить(Перечисления.ТипыСодержимогоТранспортногоКонтейнера.ИзвещениеОПолучении);
		
		Запрос.УстановитьПараметр("ТипСодержимогоИзвещенияОПолучении", ТипСодержимогоИзвещенияОПолучении);
		
		КонтекстЭДОСервер = ДокументооборотСКО.ПолучитьОбработкуЭДО();
		ТипСообщенияИзвещенияОПолучении = КонтекстЭДОСервер.ТипыИзвещенийОПолучении(); 
		
		Запрос.УстановитьПараметр("ТипСообщенияИзвещенияОПолучении", ТипСообщенияИзвещенияОПолучении);
		
		Запрос.УстановитьПараметр("ТипСодержимогоЭлектронноЦифроваяПодпись", Перечисления.ТипыСодержимогоТранспортногоКонтейнера.ЭлектронноЦифроваяПодпись);
	КонецЕсли;
	
	// компилируем запрос из подзапросов
	Для Инд = 0 По Подзапросы.Количество() - 1 Цикл
		ТекЗапрос = Подзапросы[Инд];
		Если Инд = 0 Тогда
			Запрос.Текст = "ВЫБРАТЬ РАЗРЕШЕННЫЕ" + Сред(ТекЗапрос, 8);
		Иначе
			Запрос.Текст = Запрос.Текст + "
						  |
						  |ОБЪЕДИНИТЬ ВСЕ
						  |
						  |" + ТекЗапрос;
		КонецЕсли;
	КонецЦикла;
	
	// выполняем запрос, сохраняем файлы во временный каталог и формируем таблицу файлов
	Если ЗначениеЗаполнено(Запрос.Текст) Тогда
		ТаблицаЗапроса = Запрос.Выполнить().Выгрузить();
	Иначе
		ТаблицаЗапроса = Неопределено;
	КонецЕсли;
	
	Возврат ТаблицаЗапроса; 
	
КонецФункции

Функция ПолучитьТаблицуФайловЦиклаОбмена(ЦиклОбмена, Настройки, ТранспортныеСообщения = Неопределено) Экспорт
	
	Если ТранспортныеСообщения = Неопределено Тогда
		ТранспортныеСообщения = ПолучитьСообщенияЦиклаОбмена(ЦиклОбмена).ВыгрузитьКолонку("Ссылка");
	КонецЕсли;
	
	Запрос = Новый Запрос;
	Запрос.УстановитьПараметр("ТранспортныеСообщения", ТранспортныеСообщения);
	
	Подзапросы = Новый Массив;
	
	ТипЦиклаОбмена = ЦиклОбмена.Тип;
	Если ТипЦиклаОбмена = Перечисления.ТипыЦикловОбмена.НалоговаяИлиБухгалтерскаяОтчетность Тогда
		
		Если Настройки.ПечататьДокумент Тогда
			Подзапросы.Добавить("ВЫБРАТЬ
			                    |	""ПечататьДокумент"" КАК Настройка,
			                    |	""ФайлОтчетности"" КАК ИдТипа,
			                    |	СодержимоеТранспортныхКонтейнеров.Данные КАК Данные,
			                    |	СодержимоеТранспортныхКонтейнеров.Тип КАК Тип,
			                    |	СодержимоеТранспортныхКонтейнеров.ИмяФайла КАК ИмяФайла,
			                    |	СодержимоеТранспортныхКонтейнеров.ТранспортноеСообщение КАК ТранспортноеСообщение,
			                    |	ВЫБОР
			                    |		КОГДА СостояниеОбъектов.Архивный ЕСТЬ NULL
			                    |			ТОГДА ЛОЖЬ
			                    |		КОГДА СостояниеОбъектов.Архивный = ЛОЖЬ
			                    |			ТОГДА ЛОЖЬ
			                    |		ИНАЧЕ ИСТИНА
			                    |	КОНЕЦ КАК ВАрхиве
			                    |ИЗ
			                    |	РегистрСведений.СодержимоеТранспортныхКонтейнеров КАК СодержимоеТранспортныхКонтейнеров
			                    |		ЛЕВОЕ СОЕДИНЕНИЕ РегистрСведений.ПризнакиАрхивированияФайловДОСКонтролирующимиОрганами КАК СостояниеОбъектов
			                    |		ПО (СостояниеОбъектов.Объект = СодержимоеТранспортныхКонтейнеров.ТранспортноеСообщение)
			                    |			И (СостояниеОбъектов.ИмяФайла = СодержимоеТранспортныхКонтейнеров.ИмяФайла)
			                    |			И (СостояниеОбъектов.Владелец = ЗНАЧЕНИЕ(Перечисление.ВидыАрхивируемыхМетаданныхДО.СодержимоеТранспортныхКонтейнеров))
			                    |ГДЕ
			                    |	СодержимоеТранспортныхКонтейнеров.Тип = &ТипСодержимогоФайлОтчетности
			                    |	И СодержимоеТранспортныхКонтейнеров.ТранспортноеСообщение.Тип = &ТипСообщенияПервичноеСообщениеСодержащееОтчетность
			                    |	И СодержимоеТранспортныхКонтейнеров.ТранспортноеСообщение В (&ТранспортныеСообщения)
			                    |
			                    |ОБЪЕДИНИТЬ ВСЕ
			                    |
			                    |ВЫБРАТЬ
			                    |	""ПечататьДокумент"",
			                    |	""ФайлОтчетностиПодписьНалогоплательщика"",
			                    |	СодержимоеТранспортныхКонтейнеров.Данные,
			                    |	СодержимоеТранспортныхКонтейнеров.Тип,
			                    |	СодержимоеТранспортныхКонтейнеров.ИмяФайла,
			                    |	СодержимоеТранспортныхКонтейнеров.ТранспортноеСообщение,
			                    |	ВЫБОР
			                    |		КОГДА СостояниеОбъектов.Архивный ЕСТЬ NULL
			                    |			ТОГДА ЛОЖЬ
			                    |		КОГДА СостояниеОбъектов.Архивный = ЛОЖЬ
			                    |			ТОГДА ЛОЖЬ
			                    |		ИНАЧЕ ИСТИНА
			                    |	КОНЕЦ
			                    |ИЗ
			                    |	РегистрСведений.СодержимоеТранспортныхКонтейнеров КАК СодержимоеТранспортныхКонтейнеров
			                    |		ЛЕВОЕ СОЕДИНЕНИЕ РегистрСведений.ПризнакиАрхивированияФайловДОСКонтролирующимиОрганами КАК СостояниеОбъектов
			                    |		ПО (СостояниеОбъектов.Объект = СодержимоеТранспортныхКонтейнеров.ТранспортноеСообщение)
			                    |			И (СостояниеОбъектов.ИмяФайла = СодержимоеТранспортныхКонтейнеров.ИмяФайла)
			                    |			И (СостояниеОбъектов.Владелец = ЗНАЧЕНИЕ(Перечисление.ВидыАрхивируемыхМетаданныхДО.СодержимоеТранспортныхКонтейнеров))
			                    |ГДЕ
			                    |	СодержимоеТранспортныхКонтейнеров.Тип = &ТипСодержимогоЭлектронноЦифроваяПодпись
			                    |	И СодержимоеТранспортныхКонтейнеров.ТранспортноеСообщение В (&ТранспортныеСообщения)
			                    |
			                    |ОБЪЕДИНИТЬ ВСЕ
			                    |
			                    |ВЫБРАТЬ
			                    |	""ПечататьДокумент"",
			                    |	""ФайлОтчетностиПодписьНалоговогоОргана"",
			                    |	СодержимоеТранспортныхКонтейнеров.Данные,
			                    |	СодержимоеТранспортныхКонтейнеров.Тип,
			                    |	СодержимоеТранспортныхКонтейнеров.ИмяФайла,
			                    |	СодержимоеТранспортныхКонтейнеров.ТранспортноеСообщение,
			                    |	ВЫБОР
			                    |		КОГДА СостояниеОбъектов.Архивный ЕСТЬ NULL
			                    |			ТОГДА ЛОЖЬ
			                    |		КОГДА СостояниеОбъектов.Архивный = ЛОЖЬ
			                    |			ТОГДА ЛОЖЬ
			                    |		ИНАЧЕ ИСТИНА
			                    |	КОНЕЦ
			                    |ИЗ
			                    |	РегистрСведений.СодержимоеТранспортныхКонтейнеров КАК СодержимоеТранспортныхКонтейнеров
			                    |		ЛЕВОЕ СОЕДИНЕНИЕ РегистрСведений.ПризнакиАрхивированияФайловДОСКонтролирующимиОрганами КАК СостояниеОбъектов
			                    |		ПО (СостояниеОбъектов.Объект = СодержимоеТранспортныхКонтейнеров.ТранспортноеСообщение)
			                    |			И (СостояниеОбъектов.ИмяФайла = СодержимоеТранспортныхКонтейнеров.ИмяФайла)
			                    |			И (СостояниеОбъектов.Владелец = ЗНАЧЕНИЕ(Перечисление.ВидыАрхивируемыхМетаданныхДО.СодержимоеТранспортныхКонтейнеров))
			                    |ГДЕ
			                    |	СодержимоеТранспортныхКонтейнеров.Тип = &ТипСодержимогоЭлектронноЦифроваяПодпись
			                    |	И СодержимоеТранспортныхКонтейнеров.ТранспортноеСообщение В (&ТранспортныеСообщения)");
			
			Запрос.УстановитьПараметр("ТипСодержимогоФайлОтчетности", Перечисления.ТипыСодержимогоТранспортногоКонтейнера.ФайлОтчетности);
			Запрос.УстановитьПараметр("ТипСодержимогоЭлектронноЦифроваяПодпись", Перечисления.ТипыСодержимогоТранспортногоКонтейнера.ЭлектронноЦифроваяПодпись);
			
		КонецЕсли;
		
		Если Настройки.ПечататьПодтверждениеОтправки Тогда
			Подзапросы.Добавить("ВЫБРАТЬ
			                    |	""ПечататьПодтверждениеОтправки"" КАК Настройка,
			                    |	""ПодтверждениеОтправки"" КАК ИдТипа,
			                    |	СодержимоеТранспортныхКонтейнеров.Данные КАК Данные,
			                    |	СодержимоеТранспортныхКонтейнеров.Тип КАК Тип,
			                    |	СодержимоеТранспортныхКонтейнеров.ИмяФайла КАК ИмяФайла,
			                    |	СодержимоеТранспортныхКонтейнеров.ТранспортноеСообщение КАК ТранспортноеСообщение,
			                    |	ВЫБОР
			                    |		КОГДА СостояниеОбъектов.Архивный ЕСТЬ NULL
			                    |			ТОГДА ЛОЖЬ
			                    |		КОГДА СостояниеОбъектов.Архивный = ЛОЖЬ
			                    |			ТОГДА ЛОЖЬ
			                    |		ИНАЧЕ ИСТИНА
			                    |	КОНЕЦ КАК ВАрхиве
			                    |ИЗ
			                    |	РегистрСведений.СодержимоеТранспортныхКонтейнеров КАК СодержимоеТранспортныхКонтейнеров
			                    |		ЛЕВОЕ СОЕДИНЕНИЕ РегистрСведений.ПризнакиАрхивированияФайловДОСКонтролирующимиОрганами КАК СостояниеОбъектов
			                    |		ПО (СостояниеОбъектов.Объект = СодержимоеТранспортныхКонтейнеров.ТранспортноеСообщение)
			                    |			И (СостояниеОбъектов.ИмяФайла = СодержимоеТранспортныхКонтейнеров.ИмяФайла)
			                    |			И (СостояниеОбъектов.Владелец = ЗНАЧЕНИЕ(Перечисление.ВидыАрхивируемыхМетаданныхДО.СодержимоеТранспортныхКонтейнеров))
			                    |ГДЕ
			                    |	СодержимоеТранспортныхКонтейнеров.Тип = &ТипСодержимогоПодтверждениеОбОтправке
			                    |	И СодержимоеТранспортныхКонтейнеров.ТранспортноеСообщение В (&ТранспортныеСообщения)
			                    |
			                    |ОБЪЕДИНИТЬ ВСЕ
			                    |
			                    |ВЫБРАТЬ
			                    |	""ПечататьПодтверждениеОтправки"",
			                    |	""ПодтверждениеОтправкиПодписьНалогоплательщика"",
			                    |	СодержимоеТранспортныхКонтейнеров.Данные,
			                    |	СодержимоеТранспортныхКонтейнеров.Тип,
			                    |	СодержимоеТранспортныхКонтейнеров.ИмяФайла,
			                    |	СодержимоеТранспортныхКонтейнеров.ТранспортноеСообщение,
			                    |	ВЫБОР
			                    |		КОГДА СостояниеОбъектов.Архивный ЕСТЬ NULL
			                    |			ТОГДА ЛОЖЬ
			                    |		КОГДА СостояниеОбъектов.Архивный = ЛОЖЬ
			                    |			ТОГДА ЛОЖЬ
			                    |		ИНАЧЕ ИСТИНА
			                    |	КОНЕЦ
			                    |ИЗ
			                    |	РегистрСведений.СодержимоеТранспортныхКонтейнеров КАК СодержимоеТранспортныхКонтейнеров
			                    |		ЛЕВОЕ СОЕДИНЕНИЕ РегистрСведений.ПризнакиАрхивированияФайловДОСКонтролирующимиОрганами КАК СостояниеОбъектов
			                    |		ПО (СостояниеОбъектов.Объект = СодержимоеТранспортныхКонтейнеров.ТранспортноеСообщение)
			                    |			И (СостояниеОбъектов.ИмяФайла = СодержимоеТранспортныхКонтейнеров.ИмяФайла)
			                    |			И (СостояниеОбъектов.Владелец = ЗНАЧЕНИЕ(Перечисление.ВидыАрхивируемыхМетаданныхДО.СодержимоеТранспортныхКонтейнеров))
			                    |ГДЕ
			                    |	СодержимоеТранспортныхКонтейнеров.Тип = &ТипСодержимогоЭлектронноЦифроваяПодпись
			                    |	И СодержимоеТранспортныхКонтейнеров.ТранспортноеСообщение В (&ТранспортныеСообщения)
			                    |
			                    |ОБЪЕДИНИТЬ ВСЕ
			                    |
			                    |ВЫБРАТЬ
			                    |	""ПечататьПодтверждениеОтправки"",
			                    |	""ПодтверждениеОтправкиПодписьПочтовогоСервера"",
			                    |	СодержимоеТранспортныхКонтейнеров.Данные,
			                    |	СодержимоеТранспортныхКонтейнеров.Тип,
			                    |	СодержимоеТранспортныхКонтейнеров.ИмяФайла,
			                    |	СодержимоеТранспортныхКонтейнеров.ТранспортноеСообщение,
			                    |	ВЫБОР
			                    |		КОГДА СостояниеОбъектов.Архивный ЕСТЬ NULL
			                    |			ТОГДА ЛОЖЬ
			                    |		КОГДА СостояниеОбъектов.Архивный = ЛОЖЬ
			                    |			ТОГДА ЛОЖЬ
			                    |		ИНАЧЕ ИСТИНА
			                    |	КОНЕЦ
			                    |ИЗ
			                    |	РегистрСведений.СодержимоеТранспортныхКонтейнеров КАК СодержимоеТранспортныхКонтейнеров
			                    |		ЛЕВОЕ СОЕДИНЕНИЕ РегистрСведений.ПризнакиАрхивированияФайловДОСКонтролирующимиОрганами КАК СостояниеОбъектов
			                    |		ПО (СостояниеОбъектов.Объект = СодержимоеТранспортныхКонтейнеров.ТранспортноеСообщение)
			                    |			И (СостояниеОбъектов.ИмяФайла = СодержимоеТранспортныхКонтейнеров.ИмяФайла)
			                    |			И (СостояниеОбъектов.Владелец = ЗНАЧЕНИЕ(Перечисление.ВидыАрхивируемыхМетаданныхДО.СодержимоеТранспортныхКонтейнеров))
			                    |ГДЕ
			                    |	СодержимоеТранспортныхКонтейнеров.Тип = &ТипСодержимогоЭлектронноЦифроваяПодпись
			                    |	И СодержимоеТранспортныхКонтейнеров.ТранспортноеСообщение В (&ТранспортныеСообщения)");
			
			Запрос.УстановитьПараметр("ТипСодержимогоПодтверждениеОбОтправке", Перечисления.ТипыСодержимогоТранспортногоКонтейнера.ПодтверждениеОбОтправке);
			Запрос.УстановитьПараметр("ТипСодержимогоЭлектронноЦифроваяПодпись", Перечисления.ТипыСодержимогоТранспортногоКонтейнера.ЭлектронноЦифроваяПодпись);
		КонецЕсли;
		
		Если Настройки.ПечататьПротокол Тогда
			Подзапросы.Добавить("ВЫБРАТЬ
			                    |	""ПечататьПротокол"" КАК Настройка,
			                    |	""Протокол"" КАК ИдТипа,
			                    |	СодержимоеТранспортныхКонтейнеров.Данные КАК Данные,
			                    |	СодержимоеТранспортныхКонтейнеров.Тип КАК Тип,
			                    |	СодержимоеТранспортныхКонтейнеров.ИмяФайла КАК ИмяФайла,
			                    |	СодержимоеТранспортныхКонтейнеров.ТранспортноеСообщение КАК ТранспортноеСообщение,
			                    |	ВЫБОР
			                    |		КОГДА СостояниеОбъектов.Архивный ЕСТЬ NULL
			                    |			ТОГДА ЛОЖЬ
			                    |		КОГДА СостояниеОбъектов.Архивный = ЛОЖЬ
			                    |			ТОГДА ЛОЖЬ
			                    |		ИНАЧЕ ИСТИНА
			                    |	КОНЕЦ КАК ВАрхиве
			                    |ИЗ
			                    |	РегистрСведений.СодержимоеТранспортныхКонтейнеров КАК СодержимоеТранспортныхКонтейнеров
			                    |		ЛЕВОЕ СОЕДИНЕНИЕ РегистрСведений.ПризнакиАрхивированияФайловДОСКонтролирующимиОрганами КАК СостояниеОбъектов
			                    |		ПО (СостояниеОбъектов.Объект = СодержимоеТранспортныхКонтейнеров.ТранспортноеСообщение)
			                    |			И (СостояниеОбъектов.ИмяФайла = СодержимоеТранспортныхКонтейнеров.ИмяФайла)
			                    |			И (СостояниеОбъектов.Владелец = ЗНАЧЕНИЕ(Перечисление.ВидыАрхивируемыхМетаданныхДО.СодержимоеТранспортныхКонтейнеров))
			                    |ГДЕ
			                    |	СодержимоеТранспортныхКонтейнеров.Тип = &ТипСодержимогоПротоколВходногоКонтроля
			                    |	И СодержимоеТранспортныхКонтейнеров.ТранспортноеСообщение В (&ТранспортныеСообщения)
			                    |
			                    |ОБЪЕДИНИТЬ ВСЕ
			                    |
			                    |ВЫБРАТЬ
			                    |	""ПечататьПротокол"",
			                    |	""ПротоколПодписьНалогоплательщика"",
			                    |	СодержимоеТранспортныхКонтейнеров.Данные,
			                    |	СодержимоеТранспортныхКонтейнеров.Тип,
			                    |	СодержимоеТранспортныхКонтейнеров.ИмяФайла,
			                    |	СодержимоеТранспортныхКонтейнеров.ТранспортноеСообщение,
			                    |	ВЫБОР
			                    |		КОГДА СостояниеОбъектов.Архивный ЕСТЬ NULL
			                    |			ТОГДА ЛОЖЬ
			                    |		КОГДА СостояниеОбъектов.Архивный = ЛОЖЬ
			                    |			ТОГДА ЛОЖЬ
			                    |		ИНАЧЕ ИСТИНА
			                    |	КОНЕЦ
			                    |ИЗ
			                    |	РегистрСведений.СодержимоеТранспортныхКонтейнеров КАК СодержимоеТранспортныхКонтейнеров
			                    |		ЛЕВОЕ СОЕДИНЕНИЕ РегистрСведений.ПризнакиАрхивированияФайловДОСКонтролирующимиОрганами КАК СостояниеОбъектов
			                    |		ПО (СостояниеОбъектов.Объект = СодержимоеТранспортныхКонтейнеров.ТранспортноеСообщение)
			                    |			И (СостояниеОбъектов.ИмяФайла = СодержимоеТранспортныхКонтейнеров.ИмяФайла)
			                    |			И (СостояниеОбъектов.Владелец = ЗНАЧЕНИЕ(Перечисление.ВидыАрхивируемыхМетаданныхДО.СодержимоеТранспортныхКонтейнеров))
			                    |ГДЕ
			                    |	СодержимоеТранспортныхКонтейнеров.Тип = &ТипСодержимогоЭлектронноЦифроваяПодпись
			                    |	И СодержимоеТранспортныхКонтейнеров.ТранспортноеСообщение В (&ТранспортныеСообщения)
			                    |
			                    |ОБЪЕДИНИТЬ ВСЕ
			                    |
			                    |ВЫБРАТЬ
			                    |	""ПечататьПротокол"",
			                    |	""ПротоколПодписьНалоговогоОргана"",
			                    |	СодержимоеТранспортныхКонтейнеров.Данные,
			                    |	СодержимоеТранспортныхКонтейнеров.Тип,
			                    |	СодержимоеТранспортныхКонтейнеров.ИмяФайла,
			                    |	СодержимоеТранспортныхКонтейнеров.ТранспортноеСообщение,
			                    |	ВЫБОР
			                    |		КОГДА СостояниеОбъектов.Архивный ЕСТЬ NULL
			                    |			ТОГДА ЛОЖЬ
			                    |		КОГДА СостояниеОбъектов.Архивный = ЛОЖЬ
			                    |			ТОГДА ЛОЖЬ
			                    |		ИНАЧЕ ИСТИНА
			                    |	КОНЕЦ
			                    |ИЗ
			                    |	РегистрСведений.СодержимоеТранспортныхКонтейнеров КАК СодержимоеТранспортныхКонтейнеров
			                    |		ЛЕВОЕ СОЕДИНЕНИЕ РегистрСведений.ПризнакиАрхивированияФайловДОСКонтролирующимиОрганами КАК СостояниеОбъектов
			                    |		ПО (СостояниеОбъектов.Объект = СодержимоеТранспортныхКонтейнеров.ТранспортноеСообщение)
			                    |			И (СостояниеОбъектов.ИмяФайла = СодержимоеТранспортныхКонтейнеров.ИмяФайла)
			                    |			И (СостояниеОбъектов.Владелец = ЗНАЧЕНИЕ(Перечисление.ВидыАрхивируемыхМетаданныхДО.СодержимоеТранспортныхКонтейнеров))
			                    |ГДЕ
			                    |	СодержимоеТранспортныхКонтейнеров.Тип = &ТипСодержимогоЭлектронноЦифроваяПодпись
			                    |	И СодержимоеТранспортныхКонтейнеров.ТранспортноеСообщение В (&ТранспортныеСообщения)");
			
			Запрос.УстановитьПараметр("ТипСодержимогоПротоколВходногоКонтроля", Перечисления.ТипыСодержимогоТранспортногоКонтейнера.ПротоколВходногоКонтроля);
			Запрос.УстановитьПараметр("ТипСодержимогоЭлектронноЦифроваяПодпись", Перечисления.ТипыСодержимогоТранспортногоКонтейнера.ЭлектронноЦифроваяПодпись);
		КонецЕсли;
		
		Если Настройки.ПечататьУведомлениеОбУточнении Тогда
			Подзапросы.Добавить("ВЫБРАТЬ
			                    |	""ПечататьУведомлениеОбУточнении"" КАК Настройка,
			                    |	""Уведомление"" КАК ИдТипа,
			                    |	СодержимоеТранспортныхКонтейнеров.Данные КАК Данные,
			                    |	СодержимоеТранспортныхКонтейнеров.Тип КАК Тип,
			                    |	СодержимоеТранспортныхКонтейнеров.ИмяФайла КАК ИмяФайла,
			                    |	СодержимоеТранспортныхКонтейнеров.ТранспортноеСообщение КАК ТранспортноеСообщение,
			                    |	ВЫБОР
			                    |		КОГДА СостояниеОбъектов.Архивный ЕСТЬ NULL
			                    |			ТОГДА ЛОЖЬ
			                    |		КОГДА СостояниеОбъектов.Архивный = ЛОЖЬ
			                    |			ТОГДА ЛОЖЬ
			                    |		ИНАЧЕ ИСТИНА
			                    |	КОНЕЦ КАК ВАрхиве
			                    |ИЗ
			                    |	РегистрСведений.СодержимоеТранспортныхКонтейнеров КАК СодержимоеТранспортныхКонтейнеров
			                    |		ЛЕВОЕ СОЕДИНЕНИЕ РегистрСведений.ПризнакиАрхивированияФайловДОСКонтролирующимиОрганами КАК СостояниеОбъектов
			                    |		ПО (СостояниеОбъектов.Объект = СодержимоеТранспортныхКонтейнеров.ТранспортноеСообщение)
			                    |			И (СостояниеОбъектов.ИмяФайла = СодержимоеТранспортныхКонтейнеров.ИмяФайла)
			                    |			И (СостояниеОбъектов.Владелец = ЗНАЧЕНИЕ(Перечисление.ВидыАрхивируемыхМетаданныхДО.СодержимоеТранспортныхКонтейнеров))
			                    |ГДЕ
			                    |	СодержимоеТранспортныхКонтейнеров.ТранспортноеСообщение В (&ТранспортныеСообщения)
			                    |
			                    |ОБЪЕДИНИТЬ ВСЕ
			                    |
			                    |ВЫБРАТЬ
			                    |	""ПечататьУведомлениеОбУточнении"",
			                    |	""УведомлениеПодписьНалоговогоОргана"",
			                    |	СодержимоеТранспортныхКонтейнеров.Данные,
			                    |	СодержимоеТранспортныхКонтейнеров.Тип,
			                    |	СодержимоеТранспортныхКонтейнеров.ИмяФайла,
			                    |	СодержимоеТранспортныхКонтейнеров.ТранспортноеСообщение,
			                    |	ВЫБОР
			                    |		КОГДА СостояниеОбъектов.Архивный ЕСТЬ NULL
			                    |			ТОГДА ЛОЖЬ
			                    |		КОГДА СостояниеОбъектов.Архивный = ЛОЖЬ
			                    |			ТОГДА ЛОЖЬ
			                    |		ИНАЧЕ ИСТИНА
			                    |	КОНЕЦ
			                    |ИЗ
			                    |	РегистрСведений.СодержимоеТранспортныхКонтейнеров КАК СодержимоеТранспортныхКонтейнеров
			                    |		ЛЕВОЕ СОЕДИНЕНИЕ РегистрСведений.ПризнакиАрхивированияФайловДОСКонтролирующимиОрганами КАК СостояниеОбъектов
			                    |		ПО (СостояниеОбъектов.Объект = СодержимоеТранспортныхКонтейнеров.ТранспортноеСообщение)
			                    |			И (СостояниеОбъектов.ИмяФайла = СодержимоеТранспортныхКонтейнеров.ИмяФайла)
			                    |			И (СостояниеОбъектов.Владелец = ЗНАЧЕНИЕ(Перечисление.ВидыАрхивируемыхМетаданныхДО.СодержимоеТранспортныхКонтейнеров))
			                    |ГДЕ
			                    |	СодержимоеТранспортныхКонтейнеров.Тип = &ТипСодержимогоЭлектронноЦифроваяПодпись
			                    |	И СодержимоеТранспортныхКонтейнеров.ТранспортноеСообщение В (&ТранспортныеСообщения)");
			
			Запрос.УстановитьПараметр("ТипСодержимогоЭлектронноЦифроваяПодпись", Перечисления.ТипыСодержимогоТранспортногоКонтейнера.ЭлектронноЦифроваяПодпись);
		КонецЕсли;
		
		Если Настройки.ПечататьПротоколПриема2НДФЛ Тогда
			Подзапросы.Добавить("ВЫБРАТЬ
			                    |	""ПечататьПротоколПриема2НДФЛ"" КАК Настройка,
			                    |	""Протокол2НДФЛ"" КАК ИдТипа,
			                    |	СодержимоеТранспортныхКонтейнеров.Данные КАК Данные,
			                    |	СодержимоеТранспортныхКонтейнеров.Тип КАК Тип,
			                    |	СодержимоеТранспортныхКонтейнеров.ИмяФайла КАК ИмяФайла,
			                    |	СодержимоеТранспортныхКонтейнеров.ТранспортноеСообщение КАК ТранспортноеСообщение,
			                    |	ВЫБОР
			                    |		КОГДА СостояниеОбъектов.Архивный ЕСТЬ NULL
			                    |			ТОГДА ЛОЖЬ
			                    |		КОГДА СостояниеОбъектов.Архивный = ЛОЖЬ
			                    |			ТОГДА ЛОЖЬ
			                    |		ИНАЧЕ ИСТИНА
			                    |	КОНЕЦ КАК ВАрхиве
			                    |ИЗ
			                    |	РегистрСведений.СодержимоеТранспортныхКонтейнеров КАК СодержимоеТранспортныхКонтейнеров
			                    |		ЛЕВОЕ СОЕДИНЕНИЕ РегистрСведений.ПризнакиАрхивированияФайловДОСКонтролирующимиОрганами КАК СостояниеОбъектов
			                    |		ПО (СостояниеОбъектов.Объект = СодержимоеТранспортныхКонтейнеров.ТранспортноеСообщение)
			                    |			И (СостояниеОбъектов.ИмяФайла = СодержимоеТранспортныхКонтейнеров.ИмяФайла)
			                    |			И (СостояниеОбъектов.Владелец = ЗНАЧЕНИЕ(Перечисление.ВидыАрхивируемыхМетаданныхДО.СодержимоеТранспортныхКонтейнеров))
			                    |ГДЕ
			                    |	СодержимоеТранспортныхКонтейнеров.Тип = &ТипСодержимогоПротокол2НДФЛ
			                    |	И СодержимоеТранспортныхКонтейнеров.ТранспортноеСообщение В (&ТранспортныеСообщения)
			                    |
			                    |ОБЪЕДИНИТЬ ВСЕ
			                    |
			                    |ВЫБРАТЬ
			                    |	""ПечататьПротоколПриема2НДФЛ"",
			                    |	""Протокол2НДФЛПодписьНалоговогоОргана"",
			                    |	СодержимоеТранспортныхКонтейнеров.Данные,
			                    |	СодержимоеТранспортныхКонтейнеров.Тип,
			                    |	СодержимоеТранспортныхКонтейнеров.ИмяФайла,
			                    |	СодержимоеТранспортныхКонтейнеров.ТранспортноеСообщение,
			                    |	ВЫБОР
			                    |		КОГДА СостояниеОбъектов.Архивный ЕСТЬ NULL
			                    |			ТОГДА ЛОЖЬ
			                    |		КОГДА СостояниеОбъектов.Архивный = ЛОЖЬ
			                    |			ТОГДА ЛОЖЬ
			                    |		ИНАЧЕ ИСТИНА
			                    |	КОНЕЦ
			                    |ИЗ
			                    |	РегистрСведений.СодержимоеТранспортныхКонтейнеров КАК СодержимоеТранспортныхКонтейнеров
			                    |		ЛЕВОЕ СОЕДИНЕНИЕ РегистрСведений.ПризнакиАрхивированияФайловДОСКонтролирующимиОрганами КАК СостояниеОбъектов
			                    |		ПО (СостояниеОбъектов.Объект = СодержимоеТранспортныхКонтейнеров.ТранспортноеСообщение)
			                    |			И (СостояниеОбъектов.ИмяФайла = СодержимоеТранспортныхКонтейнеров.ИмяФайла)
			                    |			И (СостояниеОбъектов.Владелец = ЗНАЧЕНИЕ(Перечисление.ВидыАрхивируемыхМетаданныхДО.СодержимоеТранспортныхКонтейнеров))
			                    |ГДЕ
			                    |	СодержимоеТранспортныхКонтейнеров.Тип = &ТипСодержимогоЭлектронноЦифроваяПодпись
			                    |	И СодержимоеТранспортныхКонтейнеров.ТранспортноеСообщение В (&ТранспортныеСообщения)
			                    |	И СодержимоеТранспортныхКонтейнеров.ИмяФайла ПОДОБНО &ПрефиксИмениФайлаПодписиПодПротоколом");
			
			Запрос.УстановитьПараметр("ТипСодержимогоПротокол2НДФЛ", Перечисления.ТипыСодержимогоТранспортногоКонтейнера.Протокол2НДФЛ);
			Запрос.УстановитьПараметр("ТипСодержимогоЭлектронноЦифроваяПодпись", Перечисления.ТипыСодержимогоТранспортногоКонтейнера.ЭлектронноЦифроваяПодпись);
			Запрос.УстановитьПараметр("ПрефиксИмениФайлаПодписиПодПротоколом", "прот_%");
		КонецЕсли;
		
		Если Настройки.ПечататьРеестрСведений2НДФЛ Тогда
			Подзапросы.Добавить("ВЫБРАТЬ
			                    |	""ПечататьРеестрСведений2НДФЛ"" КАК Настройка,
			                    |	""Реестр2НДФЛ"" КАК ИдТипа,
			                    |	СодержимоеТранспортныхКонтейнеров.Данные КАК Данные,
			                    |	СодержимоеТранспортныхКонтейнеров.Тип КАК Тип,
			                    |	СодержимоеТранспортныхКонтейнеров.ИмяФайла КАК ИмяФайла,
			                    |	СодержимоеТранспортныхКонтейнеров.ТранспортноеСообщение КАК ТранспортноеСообщение,
			                    |	ВЫБОР
			                    |		КОГДА СостояниеОбъектов.Архивный ЕСТЬ NULL
			                    |			ТОГДА ЛОЖЬ
			                    |		КОГДА СостояниеОбъектов.Архивный = ЛОЖЬ
			                    |			ТОГДА ЛОЖЬ
			                    |		ИНАЧЕ ИСТИНА
			                    |	КОНЕЦ КАК ВАрхиве
			                    |ИЗ
			                    |	РегистрСведений.СодержимоеТранспортныхКонтейнеров КАК СодержимоеТранспортныхКонтейнеров
			                    |		ЛЕВОЕ СОЕДИНЕНИЕ РегистрСведений.ПризнакиАрхивированияФайловДОСКонтролирующимиОрганами КАК СостояниеОбъектов
			                    |		ПО (СостояниеОбъектов.Объект = СодержимоеТранспортныхКонтейнеров.ТранспортноеСообщение)
			                    |			И (СостояниеОбъектов.ИмяФайла = СодержимоеТранспортныхКонтейнеров.ИмяФайла)
			                    |			И (СостояниеОбъектов.Владелец = ЗНАЧЕНИЕ(Перечисление.ВидыАрхивируемыхМетаданныхДО.СодержимоеТранспортныхКонтейнеров))
			                    |ГДЕ
			                    |	СодержимоеТранспортныхКонтейнеров.Тип = &ТипСодержимогоРеестр2НДФЛ
			                    |	И СодержимоеТранспортныхКонтейнеров.ТранспортноеСообщение В (&ТранспортныеСообщения)
			                    |
			                    |ОБЪЕДИНИТЬ ВСЕ
			                    |
			                    |ВЫБРАТЬ
			                    |	""ПечататьРеестрСведений2НДФЛ"",
			                    |	""Реестр2НДФЛПодписьНалоговогоОргана"",
			                    |	СодержимоеТранспортныхКонтейнеров.Данные,
			                    |	СодержимоеТранспортныхКонтейнеров.Тип,
			                    |	СодержимоеТранспортныхКонтейнеров.ИмяФайла,
			                    |	СодержимоеТранспортныхКонтейнеров.ТранспортноеСообщение,
			                    |	ВЫБОР
			                    |		КОГДА СостояниеОбъектов.Архивный ЕСТЬ NULL
			                    |			ТОГДА ЛОЖЬ
			                    |		КОГДА СостояниеОбъектов.Архивный = ЛОЖЬ
			                    |			ТОГДА ЛОЖЬ
			                    |		ИНАЧЕ ИСТИНА
			                    |	КОНЕЦ
			                    |ИЗ
			                    |	РегистрСведений.СодержимоеТранспортныхКонтейнеров КАК СодержимоеТранспортныхКонтейнеров
			                    |		ЛЕВОЕ СОЕДИНЕНИЕ РегистрСведений.ПризнакиАрхивированияФайловДОСКонтролирующимиОрганами КАК СостояниеОбъектов
			                    |		ПО (СостояниеОбъектов.Объект = СодержимоеТранспортныхКонтейнеров.ТранспортноеСообщение)
			                    |			И (СостояниеОбъектов.ИмяФайла = СодержимоеТранспортныхКонтейнеров.ИмяФайла)
			                    |			И (СостояниеОбъектов.Владелец = ЗНАЧЕНИЕ(Перечисление.ВидыАрхивируемыхМетаданныхДО.СодержимоеТранспортныхКонтейнеров))
			                    |ГДЕ
			                    |	СодержимоеТранспортныхКонтейнеров.Тип = &ТипСодержимогоЭлектронноЦифроваяПодпись
			                    |	И СодержимоеТранспортныхКонтейнеров.ТранспортноеСообщение В (&ТранспортныеСообщения)
			                    |	И СодержимоеТранспортныхКонтейнеров.ИмяФайла ПОДОБНО &ПрефиксИмениФайлаПодписиПодРеестром");
			
			Запрос.УстановитьПараметр("ТипСодержимогоРеестр2НДФЛ", Перечисления.ТипыСодержимогоТранспортногоКонтейнера.Реестр2НДФЛ);
			Запрос.УстановитьПараметр("ТипСодержимогоЭлектронноЦифроваяПодпись", Перечисления.ТипыСодержимогоТранспортногоКонтейнера.ЭлектронноЦифроваяПодпись);
			Запрос.УстановитьПараметр("ПрефиксИмениФайлаПодписиПодРеестром", "реестр_%");
		КонецЕсли;
		
	ИначеЕсли ТипЦиклаОбмена = Перечисления.ТипыЦикловОбмена.НеформализованныеДокументыНалогоплательщика Тогда
		
	ИначеЕсли ТипЦиклаОбмена = Перечисления.ТипыЦикловОбмена.НеформализованныеДокументыНалоговогоОргана Тогда
		
	ИначеЕсли ТипЦиклаОбмена = Перечисления.ТипыЦикловОбмена.ЗапросыНаИнформационноеОбслуживание Тогда
		
	ИначеЕсли ТипЦиклаОбмена = Перечисления.ТипыЦикловОбмена.ОтчетностьПФР Тогда
		
		Если Настройки.ПечататьДокумент Тогда
			Подзапросы.Добавить("ВЫБРАТЬ
			                    |	""ПечататьДокумент"" КАК Настройка,
			                    |	""ФайлОтчетностиПФР"" КАК ИдТипа,
			                    |	СодержимоеТранспортныхКонтейнеров.Данные КАК Данные,
			                    |	СодержимоеТранспортныхКонтейнеров.Тип КАК Тип,
			                    |	СодержимоеТранспортныхКонтейнеров.ИмяФайла КАК ИмяФайла,
			                    |	"""" КАК ИмяПодписанногоФайла,
			                    |	"""" КАК Сертификат,
			                    |	СодержимоеТранспортныхКонтейнеров.ТранспортноеСообщение КАК ТранспортноеСообщение,
			                    |	ВЫБОР
			                    |		КОГДА СостояниеОбъектов.Архивный ЕСТЬ NULL
			                    |			ТОГДА ЛОЖЬ
			                    |		КОГДА СостояниеОбъектов.Архивный = ЛОЖЬ
			                    |			ТОГДА ЛОЖЬ
			                    |		ИНАЧЕ ИСТИНА
			                    |	КОНЕЦ КАК ВАрхиве
			                    |ИЗ
			                    |	РегистрСведений.СодержимоеТранспортныхКонтейнеров КАК СодержимоеТранспортныхКонтейнеров
			                    |		ЛЕВОЕ СОЕДИНЕНИЕ РегистрСведений.ПризнакиАрхивированияФайловДОСКонтролирующимиОрганами КАК СостояниеОбъектов
			                    |		ПО (СостояниеОбъектов.Объект = СодержимоеТранспортныхКонтейнеров.ТранспортноеСообщение)
			                    |			И (СостояниеОбъектов.ИмяФайла = СодержимоеТранспортныхКонтейнеров.ИмяФайла)
			                    |			И (СостояниеОбъектов.Владелец = ЗНАЧЕНИЕ(Перечисление.ВидыАрхивируемыхМетаданныхДО.СодержимоеТранспортныхКонтейнеров))
			                    |ГДЕ
			                    |	СодержимоеТранспортныхКонтейнеров.Тип = &ТипСодержимогоФайлОтчетности
			                    |	И СодержимоеТранспортныхКонтейнеров.ТранспортноеСообщение.Тип = &ТипСообщенияПервичноеСообщениеСодержащееОтчетность
			                    |	И СодержимоеТранспортныхКонтейнеров.ТранспортноеСообщение В (&ТранспортныеСообщения)
			                    |
			                    |ОБЪЕДИНИТЬ ВСЕ
			                    |
			                    |ВЫБРАТЬ
			                    |	""ПечататьДокумент"",
			                    |	""ФайлОтчетностиПодписьСтрахователя"",
			                    |	СодержимоеТранспортныхКонтейнеров.Данные,
			                    |	СодержимоеТранспортныхКонтейнеров.Тип,
			                    |	СодержимоеТранспортныхКонтейнеров.ИмяФайла,
			                    |	СодержимоеТранспортныхКонтейнеров.ЭЦПИмяПодписанногоФайла,
			                    |	СодержимоеТранспортныхКонтейнеров.ЭЦПСертификат,
			                    |	СодержимоеТранспортныхКонтейнеров.ТранспортноеСообщение,
			                    |	ВЫБОР
			                    |		КОГДА СостояниеОбъектов.Архивный ЕСТЬ NULL
			                    |			ТОГДА ЛОЖЬ
			                    |		КОГДА СостояниеОбъектов.Архивный = ЛОЖЬ
			                    |			ТОГДА ЛОЖЬ
			                    |		ИНАЧЕ ИСТИНА
			                    |	КОНЕЦ
			                    |ИЗ
			                    |	РегистрСведений.СодержимоеТранспортныхКонтейнеров КАК СодержимоеТранспортныхКонтейнеров
			                    |		ЛЕВОЕ СОЕДИНЕНИЕ РегистрСведений.ПризнакиАрхивированияФайловДОСКонтролирующимиОрганами КАК СостояниеОбъектов
			                    |		ПО (СостояниеОбъектов.Объект = СодержимоеТранспортныхКонтейнеров.ТранспортноеСообщение)
			                    |			И (СостояниеОбъектов.ИмяФайла = СодержимоеТранспортныхКонтейнеров.ИмяФайла)
			                    |			И (СостояниеОбъектов.Владелец = ЗНАЧЕНИЕ(Перечисление.ВидыАрхивируемыхМетаданныхДО.СодержимоеТранспортныхКонтейнеров))
			                    |ГДЕ
			                    |	СодержимоеТранспортныхКонтейнеров.Тип = &ТипСодержимогоЭлектронноЦифроваяПодпись
			                    |	И СодержимоеТранспортныхКонтейнеров.ТранспортноеСообщение.Тип = &ТипСообщенияПервичноеСообщениеСодержащееОтчетность
			                    |	И СодержимоеТранспортныхКонтейнеров.ТранспортноеСообщение В (&ТранспортныеСообщения)
			                    |	И СодержимоеТранспортныхКонтейнеров.ЭЦПЭтоПодписьАбонента = &парамИстина
			                    |
			                    |ОБЪЕДИНИТЬ ВСЕ
			                    |
			                    |ВЫБРАТЬ
			                    |	""ПечататьДокумент"",
			                    |	""ФайлОтчетностиПодписьОрганаПФР"",
			                    |	СодержимоеТранспортныхКонтейнеров.Данные,
			                    |	СодержимоеТранспортныхКонтейнеров.Тип,
			                    |	СодержимоеТранспортныхКонтейнеров.ИмяФайла,
			                    |	СодержимоеТранспортныхКонтейнеров.ЭЦПИмяПодписанногоФайла,
			                    |	СодержимоеТранспортныхКонтейнеров.ЭЦПСертификат,
			                    |	СодержимоеТранспортныхКонтейнеров.ТранспортноеСообщение,
			                    |	ВЫБОР
			                    |		КОГДА СостояниеОбъектов.Архивный ЕСТЬ NULL
			                    |			ТОГДА ЛОЖЬ
			                    |		КОГДА СостояниеОбъектов.Архивный = ЛОЖЬ
			                    |			ТОГДА ЛОЖЬ
			                    |		ИНАЧЕ ИСТИНА
			                    |	КОНЕЦ
			                    |ИЗ
			                    |	РегистрСведений.СодержимоеТранспортныхКонтейнеров КАК СодержимоеТранспортныхКонтейнеров
			                    |		ЛЕВОЕ СОЕДИНЕНИЕ РегистрСведений.ПризнакиАрхивированияФайловДОСКонтролирующимиОрганами КАК СостояниеОбъектов
			                    |		ПО (СостояниеОбъектов.Объект = СодержимоеТранспортныхКонтейнеров.ТранспортноеСообщение)
			                    |			И (СостояниеОбъектов.ИмяФайла = СодержимоеТранспортныхКонтейнеров.ИмяФайла)
			                    |			И (СостояниеОбъектов.Владелец = ЗНАЧЕНИЕ(Перечисление.ВидыАрхивируемыхМетаданныхДО.СодержимоеТранспортныхКонтейнеров))
			                    |ГДЕ
			                    |	СодержимоеТранспортныхКонтейнеров.Тип = &ТипСодержимогоЭлектронноЦифроваяПодпись
			                    |	И СодержимоеТранспортныхКонтейнеров.ТранспортноеСообщение.Тип = &ТипСообщенияКвитанцияОПриемеФайлаОтчетности
			                    |	И СодержимоеТранспортныхКонтейнеров.ТранспортноеСообщение В (&ТранспортныеСообщения)
			                    |	И СодержимоеТранспортныхКонтейнеров.ЭЦПЭтоПодписьАбонента <> &парамИстина");
			Запрос.УстановитьПараметр("ТипСодержимогоФайлОтчетности", Перечисления.ТипыСодержимогоТранспортногоКонтейнера.ФайлОтчетностиПФР);
			Запрос.УстановитьПараметр("ТипСообщенияПервичноеСообщениеСодержащееОтчетность", Перечисления.ТипыТранспортныхСообщений.ПервичноеСообщениеСодержащееОтчетностьПФР);
			Запрос.УстановитьПараметр("ТипСодержимогоЭлектронноЦифроваяПодпись", Перечисления.ТипыСодержимогоТранспортногоКонтейнера.ЭлектронноЦифроваяПодпись);
			Запрос.УстановитьПараметр("ТипСообщенияКвитанцияОПриемеФайлаОтчетности", Перечисления.ТипыТранспортныхСообщений.ПротоколПФР);
			Запрос.УстановитьПараметр("парамИстина", Истина);
		КонецЕсли;
		
		Если Настройки.ПечататьИзвещениеОбОтказеПФР Тогда
			Подзапросы.Добавить("ВЫБРАТЬ
			                    |	""ПечататьИзвещениеОбОтказеПФР"" КАК Настройка,
			                    |	""ПодтверждениеПолученияПФР"" КАК ИдТипа,
			                    |	СодержимоеТранспортныхКонтейнеров.Данные КАК Данные,
			                    |	СодержимоеТранспортныхКонтейнеров.Тип КАК Тип,
			                    |	СодержимоеТранспортныхКонтейнеров.ИмяФайла КАК ИмяФайла,
			                    |	"""" КАК ИмяПодписанногоФайла,
			                    |	"""" КАК Сертификат,
			                    |	СодержимоеТранспортныхКонтейнеров.ТранспортноеСообщение КАК ТранспортноеСообщение,
			                    |	ВЫБОР
			                    |		КОГДА СостояниеОбъектов.Архивный ЕСТЬ NULL
			                    |			ТОГДА ЛОЖЬ
			                    |		КОГДА СостояниеОбъектов.Архивный = ЛОЖЬ
			                    |			ТОГДА ЛОЖЬ
			                    |		ИНАЧЕ ИСТИНА
			                    |	КОНЕЦ КАК ВАрхиве
			                    |ИЗ
			                    |	РегистрСведений.СодержимоеТранспортныхКонтейнеров КАК СодержимоеТранспортныхКонтейнеров
			                    |		ЛЕВОЕ СОЕДИНЕНИЕ РегистрСведений.ПризнакиАрхивированияФайловДОСКонтролирующимиОрганами КАК СостояниеОбъектов
			                    |		ПО (СостояниеОбъектов.Объект = СодержимоеТранспортныхКонтейнеров.ТранспортноеСообщение)
			                    |			И (СостояниеОбъектов.ИмяФайла = СодержимоеТранспортныхКонтейнеров.ИмяФайла)
			                    |			И (СостояниеОбъектов.Владелец = ЗНАЧЕНИЕ(Перечисление.ВидыАрхивируемыхМетаданныхДО.СодержимоеТранспортныхКонтейнеров))
			                    |ГДЕ
			                    |	СодержимоеТранспортныхКонтейнеров.Тип = &ТипСодержимогоПодтверждениеПолученияОтчетностиПФР
			                    |	И СодержимоеТранспортныхКонтейнеров.ТранспортноеСообщение.Тип = &ТипСообщенияПодтверждениеПолученияОтчетностиПФР
			                    |	И СодержимоеТранспортныхКонтейнеров.ТранспортноеСообщение В (&ТранспортныеСообщения)
			                    |
			                    |ОБЪЕДИНИТЬ ВСЕ
			                    |
			                    |ВЫБРАТЬ
			                    |	""ПечататьИзвещениеОбОтказеПФР"",
			                    |	""ПодтверждениеПолученияПФРПодписьОрганаПФР"",
			                    |	СодержимоеТранспортныхКонтейнеров.Данные,
			                    |	СодержимоеТранспортныхКонтейнеров.Тип,
			                    |	СодержимоеТранспортныхКонтейнеров.ИмяФайла,
			                    |	СодержимоеТранспортныхКонтейнеров.ЭЦПИмяПодписанногоФайла,
			                    |	СодержимоеТранспортныхКонтейнеров.ЭЦПСертификат,
			                    |	СодержимоеТранспортныхКонтейнеров.ТранспортноеСообщение,
			                    |	ВЫБОР
			                    |		КОГДА СостояниеОбъектов.Архивный ЕСТЬ NULL
			                    |			ТОГДА ЛОЖЬ
			                    |		КОГДА СостояниеОбъектов.Архивный = ЛОЖЬ
			                    |			ТОГДА ЛОЖЬ
			                    |		ИНАЧЕ ИСТИНА
			                    |	КОНЕЦ
			                    |ИЗ
			                    |	РегистрСведений.СодержимоеТранспортныхКонтейнеров КАК СодержимоеТранспортныхКонтейнеров
			                    |		ЛЕВОЕ СОЕДИНЕНИЕ РегистрСведений.ПризнакиАрхивированияФайловДОСКонтролирующимиОрганами КАК СостояниеОбъектов
			                    |		ПО (СостояниеОбъектов.Объект = СодержимоеТранспортныхКонтейнеров.ТранспортноеСообщение)
			                    |			И (СостояниеОбъектов.ИмяФайла = СодержимоеТранспортныхКонтейнеров.ИмяФайла)
			                    |			И (СостояниеОбъектов.Владелец = ЗНАЧЕНИЕ(Перечисление.ВидыАрхивируемыхМетаданныхДО.СодержимоеТранспортныхКонтейнеров))
			                    |ГДЕ
			                    |	СодержимоеТранспортныхКонтейнеров.Тип = &ТипСодержимогоЭлектронноЦифроваяПодпись
			                    |	И СодержимоеТранспортныхКонтейнеров.ТранспортноеСообщение.Тип = &ТипСообщенияПодтверждениеПолученияОтчетностиПФР
			                    |	И СодержимоеТранспортныхКонтейнеров.ТранспортноеСообщение В (&ТранспортныеСообщения)
			                    |	И СодержимоеТранспортныхКонтейнеров.ЭЦПЭтоПодписьАбонента <> &парамИстина");
			Запрос.УстановитьПараметр("ТипСодержимогоПодтверждениеПолученияОтчетностиПФР", Перечисления.ТипыСодержимогоТранспортногоКонтейнера.ПодтверждениеПолученияОтчетностиПФР);
			Запрос.УстановитьПараметр("ТипСообщенияПодтверждениеПолученияОтчетностиПФР", Перечисления.ТипыТранспортныхСообщений.ПодтверждениеПолученияОтчетностиПФР);
			Запрос.УстановитьПараметр("ТипСодержимогоЭлектронноЦифроваяПодпись", Перечисления.ТипыСодержимогоТранспортногоКонтейнера.ЭлектронноЦифроваяПодпись);
			Запрос.УстановитьПараметр("парамИстина", Истина);
		КонецЕсли;
		
		Если Настройки.ПечататьПротокол Тогда
			Подзапросы.Добавить("ВЫБРАТЬ
			                    |	""ПечататьПротокол"" КАК Настройка,
			                    |	""ПротоколПФР"" КАК ИдТипа,
			                    |	СодержимоеТранспортныхКонтейнеров.Данные КАК Данные,
			                    |	СодержимоеТранспортныхКонтейнеров.Тип КАК Тип,
			                    |	СодержимоеТранспортныхКонтейнеров.ИмяФайла КАК ИмяФайла,
			                    |	"""" КАК ИмяПодписанногоФайла,
			                    |	"""" КАК Сертификат,
			                    |	СодержимоеТранспортныхКонтейнеров.ТранспортноеСообщение КАК ТранспортноеСообщение,
			                    |	ВЫБОР
			                    |		КОГДА СостояниеОбъектов.Архивный ЕСТЬ NULL
			                    |			ТОГДА ЛОЖЬ
			                    |		КОГДА СостояниеОбъектов.Архивный = ЛОЖЬ
			                    |			ТОГДА ЛОЖЬ
			                    |		ИНАЧЕ ИСТИНА
			                    |	КОНЕЦ КАК ВАрхиве
			                    |ИЗ
			                    |	РегистрСведений.СодержимоеТранспортныхКонтейнеров КАК СодержимоеТранспортныхКонтейнеров
			                    |		ЛЕВОЕ СОЕДИНЕНИЕ РегистрСведений.ПризнакиАрхивированияФайловДОСКонтролирующимиОрганами КАК СостояниеОбъектов
			                    |		ПО (СостояниеОбъектов.Объект = СодержимоеТранспортныхКонтейнеров.ТранспортноеСообщение)
			                    |			И (СостояниеОбъектов.ИмяФайла = СодержимоеТранспортныхКонтейнеров.ИмяФайла)
			                    |			И (СостояниеОбъектов.Владелец = ЗНАЧЕНИЕ(Перечисление.ВидыАрхивируемыхМетаданныхДО.СодержимоеТранспортныхКонтейнеров))
			                    |ГДЕ
			                    |	СодержимоеТранспортныхКонтейнеров.Тип = &ТипСодержимогоПротоколВходногоКонтроля
			                    |	И СодержимоеТранспортныхКонтейнеров.ТранспортноеСообщение.Тип = &ТипСообщенияПротоколВходногоКонтроляОтчетности
			                    |	И СодержимоеТранспортныхКонтейнеров.ТранспортноеСообщение В (&ТранспортныеСообщения)
			                    |
			                    |ОБЪЕДИНИТЬ ВСЕ
			                    |
			                    |ВЫБРАТЬ
			                    |	""ПечататьПротокол"",
			                    |	""ПротоколПодписьСтрахователя"",
			                    |	СодержимоеТранспортныхКонтейнеров.Данные,
			                    |	СодержимоеТранспортныхКонтейнеров.Тип,
			                    |	СодержимоеТранспортныхКонтейнеров.ИмяФайла,
			                    |	СодержимоеТранспортныхКонтейнеров.ЭЦПИмяПодписанногоФайла,
			                    |	СодержимоеТранспортныхКонтейнеров.ЭЦПСертификат,
			                    |	СодержимоеТранспортныхКонтейнеров.ТранспортноеСообщение,
			                    |	ВЫБОР
			                    |		КОГДА СостояниеОбъектов.Архивный ЕСТЬ NULL
			                    |			ТОГДА ЛОЖЬ
			                    |		КОГДА СостояниеОбъектов.Архивный = ЛОЖЬ
			                    |			ТОГДА ЛОЖЬ
			                    |		ИНАЧЕ ИСТИНА
			                    |	КОНЕЦ
			                    |ИЗ
			                    |	РегистрСведений.СодержимоеТранспортныхКонтейнеров КАК СодержимоеТранспортныхКонтейнеров
			                    |		ЛЕВОЕ СОЕДИНЕНИЕ РегистрСведений.ПризнакиАрхивированияФайловДОСКонтролирующимиОрганами КАК СостояниеОбъектов
			                    |		ПО (СостояниеОбъектов.Объект = СодержимоеТранспортныхКонтейнеров.ТранспортноеСообщение)
			                    |			И (СостояниеОбъектов.ИмяФайла = СодержимоеТранспортныхКонтейнеров.ИмяФайла)
			                    |			И (СостояниеОбъектов.Владелец = ЗНАЧЕНИЕ(Перечисление.ВидыАрхивируемыхМетаданныхДО.СодержимоеТранспортныхКонтейнеров))
			                    |ГДЕ
			                    |	СодержимоеТранспортныхКонтейнеров.Тип = &ТипСодержимогоЭлектронноЦифроваяПодпись
			                    |	И СодержимоеТранспортныхКонтейнеров.ТранспортноеСообщение.Тип = &ТипСообщенияПереподписанныйПротоколВходногоКонтроляОтчетности
			                    |	И СодержимоеТранспортныхКонтейнеров.ТранспортноеСообщение В (&ТранспортныеСообщения)
			                    |	И СодержимоеТранспортныхКонтейнеров.ЭЦПЭтоПодписьАбонента = &парамИстина
			                    |
			                    |ОБЪЕДИНИТЬ ВСЕ
			                    |
			                    |ВЫБРАТЬ
			                    |	""ПечататьПротокол"",
			                    |	""ПротоколПодписьОрганаПФР"",
			                    |	СодержимоеТранспортныхКонтейнеров.Данные,
			                    |	СодержимоеТранспортныхКонтейнеров.Тип,
			                    |	СодержимоеТранспортныхКонтейнеров.ИмяФайла,
			                    |	СодержимоеТранспортныхКонтейнеров.ЭЦПИмяПодписанногоФайла,
			                    |	СодержимоеТранспортныхКонтейнеров.ЭЦПСертификат,
			                    |	СодержимоеТранспортныхКонтейнеров.ТранспортноеСообщение,
			                    |	ВЫБОР
			                    |		КОГДА СостояниеОбъектов.Архивный ЕСТЬ NULL
			                    |			ТОГДА ЛОЖЬ
			                    |		КОГДА СостояниеОбъектов.Архивный = ЛОЖЬ
			                    |			ТОГДА ЛОЖЬ
			                    |		ИНАЧЕ ИСТИНА
			                    |	КОНЕЦ
			                    |ИЗ
			                    |	РегистрСведений.СодержимоеТранспортныхКонтейнеров КАК СодержимоеТранспортныхКонтейнеров
			                    |		ЛЕВОЕ СОЕДИНЕНИЕ РегистрСведений.ПризнакиАрхивированияФайловДОСКонтролирующимиОрганами КАК СостояниеОбъектов
			                    |		ПО (СостояниеОбъектов.Объект = СодержимоеТранспортныхКонтейнеров.ТранспортноеСообщение)
			                    |			И (СостояниеОбъектов.ИмяФайла = СодержимоеТранспортныхКонтейнеров.ИмяФайла)
			                    |			И (СостояниеОбъектов.Владелец = ЗНАЧЕНИЕ(Перечисление.ВидыАрхивируемыхМетаданныхДО.СодержимоеТранспортныхКонтейнеров))
			                    |ГДЕ
			                    |	СодержимоеТранспортныхКонтейнеров.Тип = &ТипСодержимогоЭлектронноЦифроваяПодпись
			                    |	И СодержимоеТранспортныхКонтейнеров.ТранспортноеСообщение.Тип = &ТипСообщенияПротоколВходногоКонтроляОтчетности
			                    |	И СодержимоеТранспортныхКонтейнеров.ТранспортноеСообщение В (&ТранспортныеСообщения)
			                    |	И СодержимоеТранспортныхКонтейнеров.ЭЦПЭтоПодписьАбонента <> &парамИстина");
			Запрос.УстановитьПараметр("ТипСодержимогоПротоколВходногоКонтроля", Перечисления.ТипыСодержимогоТранспортногоКонтейнера.ПротоколПФР);
			Запрос.УстановитьПараметр("ТипСообщенияПротоколВходногоКонтроляОтчетности", Перечисления.ТипыТранспортныхСообщений.ПротоколПФР);
			Запрос.УстановитьПараметр("ТипСодержимогоЭлектронноЦифроваяПодпись", Перечисления.ТипыСодержимогоТранспортногоКонтейнера.ЭлектронноЦифроваяПодпись);
			Запрос.УстановитьПараметр("ТипСообщенияПереподписанныйПротоколВходногоКонтроляОтчетности", Перечисления.ТипыТранспортныхСообщений.ПротоколКвитанцияПФР);
			Запрос.УстановитьПараметр("парамИстина", Истина);
		КонецЕсли;
		
	ИначеЕсли ТипЦиклаОбмена = Перечисления.ТипыЦикловОбмена.ОтчетностьФСГС
		ИЛИ ТипЦиклаОбмена = Перечисления.ТипыЦикловОбмена.ПисьменноеОбращениеВФСГС Тогда
		
		Если Настройки.ПечататьДокумент Тогда
			Подзапросы.Добавить("ВЫБРАТЬ
			                    |	""ПечататьДокумент"" КАК Настройка,
			                    |	""ФайлОтчетностиФСГС"" КАК ИдТипа,
			                    |	СодержимоеТранспортныхКонтейнеров.Данные КАК Данные,
			                    |	СодержимоеТранспортныхКонтейнеров.Тип КАК Тип,
			                    |	СодержимоеТранспортныхКонтейнеров.ИмяФайла КАК ИмяФайла,
			                    |	"""" КАК ИмяПодписанногоФайла,
			                    |	"""" КАК Сертификат,
			                    |	СодержимоеТранспортныхКонтейнеров.ТранспортноеСообщение КАК ТранспортноеСообщение,
			                    |	ВЫБОР
			                    |		КОГДА СостояниеОбъектов.Архивный ЕСТЬ NULL
			                    |			ТОГДА ЛОЖЬ
			                    |		КОГДА СостояниеОбъектов.Архивный = ЛОЖЬ
			                    |			ТОГДА ЛОЖЬ
			                    |		ИНАЧЕ ИСТИНА
			                    |	КОНЕЦ КАК ВАрхиве
			                    |ИЗ
			                    |	РегистрСведений.СодержимоеТранспортныхКонтейнеров КАК СодержимоеТранспортныхКонтейнеров
			                    |		ЛЕВОЕ СОЕДИНЕНИЕ РегистрСведений.ПризнакиАрхивированияФайловДОСКонтролирующимиОрганами КАК СостояниеОбъектов
			                    |		ПО (СостояниеОбъектов.Объект = СодержимоеТранспортныхКонтейнеров.ТранспортноеСообщение)
			                    |			И (СостояниеОбъектов.ИмяФайла = СодержимоеТранспортныхКонтейнеров.ИмяФайла)
			                    |			И (СостояниеОбъектов.Владелец = ЗНАЧЕНИЕ(Перечисление.ВидыАрхивируемыхМетаданныхДО.СодержимоеТранспортныхКонтейнеров))
			                    |ГДЕ
			                    |	СодержимоеТранспортныхКонтейнеров.Тип = &ТипСодержимогоФайлОтчетности
			                    |	И СодержимоеТранспортныхКонтейнеров.ТранспортноеСообщение.Тип = &ТипСообщенияПервичноеСообщениеСодержащееОтчетность
			                    |	И СодержимоеТранспортныхКонтейнеров.ТранспортноеСообщение В (&ТранспортныеСообщения)
			                    |
			                    |ОБЪЕДИНИТЬ ВСЕ
			                    |
			                    |ВЫБРАТЬ
			                    |	""ПечататьДокумент"",
			                    |	""ФайлОтчетностиПодписьРеспондента"",
			                    |	СодержимоеТранспортныхКонтейнеров.Данные,
			                    |	СодержимоеТранспортныхКонтейнеров.Тип,
			                    |	СодержимоеТранспортныхКонтейнеров.ИмяФайла,
			                    |	СодержимоеТранспортныхКонтейнеров.ЭЦПИмяПодписанногоФайла,
			                    |	СодержимоеТранспортныхКонтейнеров.ЭЦПСертификат,
			                    |	СодержимоеТранспортныхКонтейнеров.ТранспортноеСообщение,
			                    |	ВЫБОР
			                    |		КОГДА СостояниеОбъектов.Архивный ЕСТЬ NULL
			                    |			ТОГДА ЛОЖЬ
			                    |		КОГДА СостояниеОбъектов.Архивный = ЛОЖЬ
			                    |			ТОГДА ЛОЖЬ
			                    |		ИНАЧЕ ИСТИНА
			                    |	КОНЕЦ
			                    |ИЗ
			                    |	РегистрСведений.СодержимоеТранспортныхКонтейнеров КАК СодержимоеТранспортныхКонтейнеров
			                    |		ЛЕВОЕ СОЕДИНЕНИЕ РегистрСведений.ПризнакиАрхивированияФайловДОСКонтролирующимиОрганами КАК СостояниеОбъектов
			                    |		ПО (СостояниеОбъектов.Объект = СодержимоеТранспортныхКонтейнеров.ТранспортноеСообщение)
			                    |			И (СостояниеОбъектов.ИмяФайла = СодержимоеТранспортныхКонтейнеров.ИмяФайла)
			                    |			И (СостояниеОбъектов.Владелец = ЗНАЧЕНИЕ(Перечисление.ВидыАрхивируемыхМетаданныхДО.СодержимоеТранспортныхКонтейнеров))
			                    |ГДЕ
			                    |	СодержимоеТранспортныхКонтейнеров.Тип = &ТипСодержимогоЭлектронноЦифроваяПодпись
			                    |	И СодержимоеТранспортныхКонтейнеров.ТранспортноеСообщение.Тип = &ТипСообщенияПервичноеСообщениеСодержащееОтчетность
			                    |	И СодержимоеТранспортныхКонтейнеров.ТранспортноеСообщение В (&ТранспортныеСообщения)
			                    |	И СодержимоеТранспортныхКонтейнеров.ЭЦПЭтоПодписьАбонента = &парамИстина
			                    |
			                    |ОБЪЕДИНИТЬ ВСЕ
			                    |
			                    |ВЫБРАТЬ
			                    |	""ПечататьДокумент"",
			                    |	""ФайлОтчетностиПодписьОрганаФСГС"",
			                    |	СодержимоеТранспортныхКонтейнеров.Данные,
			                    |	СодержимоеТранспортныхКонтейнеров.Тип,
			                    |	СодержимоеТранспортныхКонтейнеров.ИмяФайла,
			                    |	СодержимоеТранспортныхКонтейнеров.ЭЦПИмяПодписанногоФайла,
			                    |	СодержимоеТранспортныхКонтейнеров.ЭЦПСертификат,
			                    |	СодержимоеТранспортныхКонтейнеров.ТранспортноеСообщение,
			                    |	ВЫБОР
			                    |		КОГДА СостояниеОбъектов.Архивный ЕСТЬ NULL
			                    |			ТОГДА ЛОЖЬ
			                    |		КОГДА СостояниеОбъектов.Архивный = ЛОЖЬ
			                    |			ТОГДА ЛОЖЬ
			                    |		ИНАЧЕ ИСТИНА
			                    |	КОНЕЦ
			                    |ИЗ
			                    |	РегистрСведений.СодержимоеТранспортныхКонтейнеров КАК СодержимоеТранспортныхКонтейнеров
			                    |		ЛЕВОЕ СОЕДИНЕНИЕ РегистрСведений.ПризнакиАрхивированияФайловДОСКонтролирующимиОрганами КАК СостояниеОбъектов
			                    |		ПО (СостояниеОбъектов.Объект = СодержимоеТранспортныхКонтейнеров.ТранспортноеСообщение)
			                    |			И (СостояниеОбъектов.ИмяФайла = СодержимоеТранспортныхКонтейнеров.ИмяФайла)
			                    |			И (СостояниеОбъектов.Владелец = ЗНАЧЕНИЕ(Перечисление.ВидыАрхивируемыхМетаданныхДО.СодержимоеТранспортныхКонтейнеров))
			                    |ГДЕ
			                    |	СодержимоеТранспортныхКонтейнеров.Тип = &ТипСодержимогоЭлектронноЦифроваяПодпись
			                    |	И СодержимоеТранспортныхКонтейнеров.ТранспортноеСообщение.Тип = &ТипСообщенияКвитанцияОПриемеФайлаОтчетности
			                    |	И СодержимоеТранспортныхКонтейнеров.ТранспортноеСообщение В (&ТранспортныеСообщения)
			                    |	И СодержимоеТранспортныхКонтейнеров.ЭЦПЭтоПодписьАбонента <> &парамИстина");
			Запрос.УстановитьПараметр("ТипСодержимогоФайлОтчетности", Перечисления.ТипыСодержимогоТранспортногоКонтейнера.ФайлОтчетностиФСГС);
			Запрос.УстановитьПараметр("ТипСообщенияПервичноеСообщениеСодержащееОтчетность", Перечисления.ТипыТранспортныхСообщений.ПервичноеСообщениеСодержащееОтчетностьФСГС);
			Запрос.УстановитьПараметр("ТипСодержимогоЭлектронноЦифроваяПодпись", Перечисления.ТипыСодержимогоТранспортногоКонтейнера.ЭлектронноЦифроваяПодпись);
			Запрос.УстановитьПараметр("ТипСообщенияКвитанцияОПриемеФайлаОтчетности", Перечисления.ТипыТранспортныхСообщений.ИзвещениеОПолученииОтчетностиФСГС);
			Запрос.УстановитьПараметр("парамИстина", Истина);
		КонецЕсли;
		
		Если Настройки.ПечататьПодтверждениеОтправки Тогда
			Подзапросы.Добавить("ВЫБРАТЬ
			                    |	""ПечататьПодтверждениеОтправки"" КАК Настройка,
			                    |	""ПодтверждениеДатыОтправкиФСГС"" КАК ИдТипа,
			                    |	СодержимоеТранспортныхКонтейнеров.Данные КАК Данные,
			                    |	СодержимоеТранспортныхКонтейнеров.Тип КАК Тип,
			                    |	СодержимоеТранспортныхКонтейнеров.ИмяФайла КАК ИмяФайла,
			                    |	"""" КАК ИмяПодписанногоФайла,
			                    |	"""" КАК Сертификат,
			                    |	СодержимоеТранспортныхКонтейнеров.ТранспортноеСообщение КАК ТранспортноеСообщение,
			                    |	ВЫБОР
			                    |		КОГДА СостояниеОбъектов.Архивный ЕСТЬ NULL
			                    |			ТОГДА ЛОЖЬ
			                    |		КОГДА СостояниеОбъектов.Архивный = ЛОЖЬ
			                    |			ТОГДА ЛОЖЬ
			                    |		ИНАЧЕ ИСТИНА
			                    |	КОНЕЦ КАК ВАрхиве
			                    |ИЗ
			                    |	РегистрСведений.СодержимоеТранспортныхКонтейнеров КАК СодержимоеТранспортныхКонтейнеров
			                    |		ЛЕВОЕ СОЕДИНЕНИЕ РегистрСведений.ПризнакиАрхивированияФайловДОСКонтролирующимиОрганами КАК СостояниеОбъектов
			                    |		ПО (СостояниеОбъектов.Объект = СодержимоеТранспортныхКонтейнеров.ТранспортноеСообщение)
			                    |			И (СостояниеОбъектов.ИмяФайла = СодержимоеТранспортныхКонтейнеров.ИмяФайла)
			                    |			И (СостояниеОбъектов.Владелец = ЗНАЧЕНИЕ(Перечисление.ВидыАрхивируемыхМетаданныхДО.СодержимоеТранспортныхКонтейнеров))
			                    |ГДЕ
			                    |	СодержимоеТранспортныхКонтейнеров.Тип = &ТипСодержимогоПодтверждениеДатыОтправки
			                    |	И СодержимоеТранспортныхКонтейнеров.ТранспортноеСообщение.Тип = &ТипСообщенияПодтверждениеДатыОтправкиОтчетности
			                    |	И СодержимоеТранспортныхКонтейнеров.ТранспортноеСообщение В (&ТранспортныеСообщения)
			                    |
			                    |ОБЪЕДИНИТЬ ВСЕ
			                    |
			                    |ВЫБРАТЬ
			                    |	""ПечататьПодтверждениеОтправки"",
			                    |	""ПодтверждениеОтправкиПодписьРеспондента"",
			                    |	СодержимоеТранспортныхКонтейнеров.Данные,
			                    |	СодержимоеТранспортныхКонтейнеров.Тип,
			                    |	СодержимоеТранспортныхКонтейнеров.ИмяФайла,
			                    |	СодержимоеТранспортныхКонтейнеров.ЭЦПИмяПодписанногоФайла,
			                    |	СодержимоеТранспортныхКонтейнеров.ЭЦПСертификат,
			                    |	СодержимоеТранспортныхКонтейнеров.ТранспортноеСообщение,
			                    |	ВЫБОР
			                    |		КОГДА СостояниеОбъектов.Архивный ЕСТЬ NULL
			                    |			ТОГДА ЛОЖЬ
			                    |		КОГДА СостояниеОбъектов.Архивный = ЛОЖЬ
			                    |			ТОГДА ЛОЖЬ
			                    |		ИНАЧЕ ИСТИНА
			                    |	КОНЕЦ
			                    |ИЗ
			                    |	РегистрСведений.СодержимоеТранспортныхКонтейнеров КАК СодержимоеТранспортныхКонтейнеров
			                    |		ЛЕВОЕ СОЕДИНЕНИЕ РегистрСведений.ПризнакиАрхивированияФайловДОСКонтролирующимиОрганами КАК СостояниеОбъектов
			                    |		ПО (СостояниеОбъектов.Объект = СодержимоеТранспортныхКонтейнеров.ТранспортноеСообщение)
			                    |			И (СостояниеОбъектов.ИмяФайла = СодержимоеТранспортныхКонтейнеров.ИмяФайла)
			                    |			И (СостояниеОбъектов.Владелец = ЗНАЧЕНИЕ(Перечисление.ВидыАрхивируемыхМетаданныхДО.СодержимоеТранспортныхКонтейнеров))
			                    |ГДЕ
			                    |	СодержимоеТранспортныхКонтейнеров.Тип = &ТипСодержимогоЭлектронноЦифроваяПодпись
			                    |	И СодержимоеТранспортныхКонтейнеров.ТранспортноеСообщение.Тип = &ТипСообщенияИзвещениеПодтверждениеДатыОтправкиОтчетности
			                    |	И СодержимоеТранспортныхКонтейнеров.ТранспортноеСообщение В (&ТранспортныеСообщения)
			                    |
			                    |ОБЪЕДИНИТЬ ВСЕ
			                    |
			                    |ВЫБРАТЬ
			                    |	""ПечататьПодтверждениеОтправки"",
			                    |	""ПодтверждениеДатыОтправкиПодписьСОС"",
			                    |	СодержимоеТранспортныхКонтейнеров.Данные,
			                    |	СодержимоеТранспортныхКонтейнеров.Тип,
			                    |	СодержимоеТранспортныхКонтейнеров.ИмяФайла,
			                    |	СодержимоеТранспортныхКонтейнеров.ЭЦПИмяПодписанногоФайла,
			                    |	СодержимоеТранспортныхКонтейнеров.ЭЦПСертификат,
			                    |	СодержимоеТранспортныхКонтейнеров.ТранспортноеСообщение,
			                    |	ВЫБОР
			                    |		КОГДА СостояниеОбъектов.Архивный ЕСТЬ NULL
			                    |			ТОГДА ЛОЖЬ
			                    |		КОГДА СостояниеОбъектов.Архивный = ЛОЖЬ
			                    |			ТОГДА ЛОЖЬ
			                    |		ИНАЧЕ ИСТИНА
			                    |	КОНЕЦ
			                    |ИЗ
			                    |	РегистрСведений.СодержимоеТранспортныхКонтейнеров КАК СодержимоеТранспортныхКонтейнеров
			                    |		ЛЕВОЕ СОЕДИНЕНИЕ РегистрСведений.ПризнакиАрхивированияФайловДОСКонтролирующимиОрганами КАК СостояниеОбъектов
			                    |		ПО (СостояниеОбъектов.Объект = СодержимоеТранспортныхКонтейнеров.ТранспортноеСообщение)
			                    |			И (СостояниеОбъектов.ИмяФайла = СодержимоеТранспортныхКонтейнеров.ИмяФайла)
			                    |			И (СостояниеОбъектов.Владелец = ЗНАЧЕНИЕ(Перечисление.ВидыАрхивируемыхМетаданныхДО.СодержимоеТранспортныхКонтейнеров))
			                    |ГДЕ
			                    |	СодержимоеТранспортныхКонтейнеров.Тип = &ТипСодержимогоЭлектронноЦифроваяПодпись
			                    |	И СодержимоеТранспортныхКонтейнеров.ТранспортноеСообщение.Тип = &ТипСообщенияПодтверждениеДатыОтправкиОтчетности
			                    |	И СодержимоеТранспортныхКонтейнеров.ТранспортноеСообщение В (&ТранспортныеСообщения)");
			Запрос.УстановитьПараметр("ТипСодержимогоПодтверждениеДатыОтправки", Перечисления.ТипыСодержимогоТранспортногоКонтейнера.ПодтверждениеОператораФСГС);
			Запрос.УстановитьПараметр("ТипСообщенияПодтверждениеДатыОтправкиОтчетности", Перечисления.ТипыТранспортныхСообщений.ПодтверждениеДатыОтправкиФСГС);
			Запрос.УстановитьПараметр("ТипСодержимогоЭлектронноЦифроваяПодпись", Перечисления.ТипыСодержимогоТранспортногоКонтейнера.ЭлектронноЦифроваяПодпись);
			Запрос.УстановитьПараметр("ТипСообщенияИзвещениеПодтверждениеДатыОтправкиОтчетности", Перечисления.ТипыТранспортныхСообщений.ИзвещениеПодтверждениеДатыОтправкиФСГС);
		КонецЕсли;

		Если Настройки.ПросмотрПодтвержденияПолученияФСГС Тогда
			Подзапросы.Добавить("ВЫБРАТЬ
			                    |	""ПросмотрПодтвержденияПолученияФСГС"" КАК Настройка,
			                    |	""ПодтверждениеПолученияФСГС"" КАК ИдТипа,
			                    |	СодержимоеТранспортныхКонтейнеров.Данные КАК Данные,
			                    |	СодержимоеТранспортныхКонтейнеров.Тип КАК Тип,
			                    |	СодержимоеТранспортныхКонтейнеров.ИмяФайла КАК ИмяФайла,
			                    |	"""" КАК ИмяПодписанногоФайла,
			                    |	"""" КАК Сертификат,
			                    |	СодержимоеТранспортныхКонтейнеров.ТранспортноеСообщение КАК ТранспортноеСообщение,
			                    |	ВЫБОР
			                    |		КОГДА СостояниеОбъектов.Архивный ЕСТЬ NULL
			                    |			ТОГДА ЛОЖЬ
			                    |		КОГДА СостояниеОбъектов.Архивный = ЛОЖЬ
			                    |			ТОГДА ЛОЖЬ
			                    |		ИНАЧЕ ИСТИНА
			                    |	КОНЕЦ КАК ВАрхиве
			                    |ИЗ
			                    |	РегистрСведений.СодержимоеТранспортныхКонтейнеров КАК СодержимоеТранспортныхКонтейнеров
			                    |		ЛЕВОЕ СОЕДИНЕНИЕ РегистрСведений.ПризнакиАрхивированияФайловДОСКонтролирующимиОрганами КАК СостояниеОбъектов
			                    |		ПО (СостояниеОбъектов.Объект = СодержимоеТранспортныхКонтейнеров.ТранспортноеСообщение)
			                    |			И (СостояниеОбъектов.ИмяФайла = СодержимоеТранспортныхКонтейнеров.ИмяФайла)
			                    |			И (СостояниеОбъектов.Владелец = ЗНАЧЕНИЕ(Перечисление.ВидыАрхивируемыхМетаданныхДО.СодержимоеТранспортныхКонтейнеров))
			                    |ГДЕ
			                    |	СодержимоеТранспортныхКонтейнеров.Тип = &ТипСодержимогоПодтверждениеПолученияОтчетностиФСГС
			                    |	И СодержимоеТранспортныхКонтейнеров.ТранспортноеСообщение.Тип В(&ТипСообщенияПодтверждениеПолученияОтчетностиФСГС)
			                    |	И СодержимоеТранспортныхКонтейнеров.ТранспортноеСообщение В (&ТранспортныеСообщения)
			                    |	И (СодержимоеТранспортныхКонтейнеров.ТранспортноеСообщение = &ТранспортноеСообщение
			                    |			ИЛИ &ТранспортноеСообщение = &ПустоеТранспортноеСообщение)
			                    |
			                    |ОБЪЕДИНИТЬ ВСЕ
			                    |
			                    |ВЫБРАТЬ
			                    |	""ПросмотрПодтвержденияПолученияФСГС"",
			                    |	ВЫБОР
			                    |		КОГДА СодержимоеТранспортныхКонтейнеров.ЭЦПЭтоПодписьАбонента
			                    |			ТОГДА ""ПодтверждениеПолученияФСГСПодписьРеспондента""
			                    |		ИНАЧЕ ""ПодтверждениеПолученияФСГСОрганаФСГС""
			                    |	КОНЕЦ,
			                    |	СодержимоеТранспортныхКонтейнеров.Данные,
			                    |	СодержимоеТранспортныхКонтейнеров.Тип,
			                    |	СодержимоеТранспортныхКонтейнеров.ИмяФайла,
			                    |	СодержимоеТранспортныхКонтейнеров.ЭЦПИмяПодписанногоФайла,
			                    |	СодержимоеТранспортныхКонтейнеров.ЭЦПСертификат,
			                    |	СодержимоеТранспортныхКонтейнеров.ТранспортноеСообщение,
			                    |	ВЫБОР
			                    |		КОГДА СостояниеОбъектов.Архивный ЕСТЬ NULL
			                    |			ТОГДА ЛОЖЬ
			                    |		КОГДА СостояниеОбъектов.Архивный = ЛОЖЬ
			                    |			ТОГДА ЛОЖЬ
			                    |		ИНАЧЕ ИСТИНА
			                    |	КОНЕЦ
			                    |ИЗ
			                    |	РегистрСведений.СодержимоеТранспортныхКонтейнеров КАК СодержимоеТранспортныхКонтейнеров
			                    |		ЛЕВОЕ СОЕДИНЕНИЕ РегистрСведений.ПризнакиАрхивированияФайловДОСКонтролирующимиОрганами КАК СостояниеОбъектов
			                    |		ПО (СостояниеОбъектов.Объект = СодержимоеТранспортныхКонтейнеров.ТранспортноеСообщение)
			                    |			И (СостояниеОбъектов.ИмяФайла = СодержимоеТранспортныхКонтейнеров.ИмяФайла)
			                    |			И (СостояниеОбъектов.Владелец = ЗНАЧЕНИЕ(Перечисление.ВидыАрхивируемыхМетаданныхДО.СодержимоеТранспортныхКонтейнеров))
			                    |ГДЕ
			                    |	СодержимоеТранспортныхКонтейнеров.Тип = &ТипСодержимогоЭлектронноЦифроваяПодпись
			                    |	И СодержимоеТранспортныхКонтейнеров.ТранспортноеСообщение.Тип В(&ТипСообщенияПодтверждениеПолученияОтчетностиФСГС)
			                    |	И СодержимоеТранспортныхКонтейнеров.ТранспортноеСообщение В (&ТранспортныеСообщения)
			                    |	И (СодержимоеТранспортныхКонтейнеров.ТранспортноеСообщение = &ТранспортноеСообщение
			                    |			ИЛИ &ТранспортноеСообщение = &ПустоеТранспортноеСообщение)");
			
			Если Настройки.Свойство("ТранспортноеСообщение") Тогда
				Запрос.УстановитьПараметр("ТранспортноеСообщение", Настройки.ТранспортноеСообщение);
			Иначе
				Запрос.УстановитьПараметр("ТранспортноеСообщение", Документы.ТранспортноеСообщение.ПустаяСсылка());
			КонецЕсли;
			Запрос.УстановитьПараметр("ПустоеТранспортноеСообщение", Документы.ТранспортноеСообщение.ПустаяСсылка());
			
			ТипыСообщений = Новый Массив;
			ТипыСообщений.Добавить(Перечисления.ТипыТранспортныхСообщений.ИзвещениеПротоколВходногоКонтроляОтчетностиФСГС);
			ТипыСообщений.Добавить(Перечисления.ТипыТранспортныхСообщений.ИзвещениеОПолученииОтчетностиФСГС);
			ТипыСообщений.Добавить(Перечисления.ТипыТранспортныхСообщений.ИзвещениеПисьмоИсходящееФСГС);
			
			Запрос.УстановитьПараметр("ТипСодержимогоПодтверждениеПолученияОтчетностиФСГС", Перечисления.ТипыСодержимогоТранспортногоКонтейнера.ИзвещениеОПолученииДокументаФСГС);
			Запрос.УстановитьПараметр("ТипСообщенияПодтверждениеПолученияОтчетностиФСГС", ТипыСообщений);
			Запрос.УстановитьПараметр("ТипСодержимогоЭлектронноЦифроваяПодпись", Перечисления.ТипыСодержимогоТранспортногоКонтейнера.ЭлектронноЦифроваяПодпись);
			
		КонецЕсли;
		
		Если Настройки.ПечататьПротокол Тогда
			Подзапросы.Добавить("ВЫБРАТЬ
			                    |	""ПечататьПротокол"" КАК Настройка,
			                    |	""ПротоколФСГС"" КАК ИдТипа,
			                    |	СодержимоеТранспортныхКонтейнеров.Данные КАК Данные,
			                    |	СодержимоеТранспортныхКонтейнеров.Тип КАК Тип,
			                    |	СодержимоеТранспортныхКонтейнеров.ИмяФайла КАК ИмяФайла,
			                    |	"""" КАК ИмяПодписанногоФайла,
			                    |	"""" КАК Сертификат,
			                    |	СодержимоеТранспортныхКонтейнеров.ТранспортноеСообщение КАК ТранспортноеСообщение,
			                    |	ВЫБОР
			                    |		КОГДА СостояниеОбъектов.Архивный ЕСТЬ NULL
			                    |			ТОГДА ЛОЖЬ
			                    |		КОГДА СостояниеОбъектов.Архивный = ЛОЖЬ
			                    |			ТОГДА ЛОЖЬ
			                    |		ИНАЧЕ ИСТИНА
			                    |	КОНЕЦ КАК ВАрхиве
			                    |ИЗ
			                    |	РегистрСведений.СодержимоеТранспортныхКонтейнеров КАК СодержимоеТранспортныхКонтейнеров
			                    |		ЛЕВОЕ СОЕДИНЕНИЕ РегистрСведений.ПризнакиАрхивированияФайловДОСКонтролирующимиОрганами КАК СостояниеОбъектов
			                    |		ПО (СостояниеОбъектов.Объект = СодержимоеТранспортныхКонтейнеров.ТранспортноеСообщение)
			                    |			И (СостояниеОбъектов.ИмяФайла = СодержимоеТранспортныхКонтейнеров.ИмяФайла)
			                    |			И (СостояниеОбъектов.Владелец = ЗНАЧЕНИЕ(Перечисление.ВидыАрхивируемыхМетаданныхДО.СодержимоеТранспортныхКонтейнеров))
			                    |ГДЕ
			                    |	СодержимоеТранспортныхКонтейнеров.Тип В(&ТипСодержимогоПротоколВходногоКонтроля)
			                    |	И СодержимоеТранспортныхКонтейнеров.ТранспортноеСообщение.Тип = &ТипСообщенияПротоколВходногоКонтроляОтчетности
			                    |	И СодержимоеТранспортныхКонтейнеров.ТранспортноеСообщение В (&ТранспортныеСообщения)
			                    |
			                    |ОБЪЕДИНИТЬ ВСЕ
			                    |
			                    |ВЫБРАТЬ
			                    |	""ПечататьПротокол"",
			                    |	""ПротоколПодписьРеспондента"",
			                    |	СодержимоеТранспортныхКонтейнеров.Данные,
			                    |	СодержимоеТранспортныхКонтейнеров.Тип,
			                    |	СодержимоеТранспортныхКонтейнеров.ИмяФайла,
			                    |	СодержимоеТранспортныхКонтейнеров.ЭЦПИмяПодписанногоФайла,
			                    |	СодержимоеТранспортныхКонтейнеров.ЭЦПСертификат,
			                    |	СодержимоеТранспортныхКонтейнеров.ТранспортноеСообщение,
			                    |	ВЫБОР
			                    |		КОГДА СостояниеОбъектов.Архивный ЕСТЬ NULL
			                    |			ТОГДА ЛОЖЬ
			                    |		КОГДА СостояниеОбъектов.Архивный = ЛОЖЬ
			                    |			ТОГДА ЛОЖЬ
			                    |		ИНАЧЕ ИСТИНА
			                    |	КОНЕЦ
			                    |ИЗ
			                    |	РегистрСведений.СодержимоеТранспортныхКонтейнеров КАК СодержимоеТранспортныхКонтейнеров
			                    |		ЛЕВОЕ СОЕДИНЕНИЕ РегистрСведений.ПризнакиАрхивированияФайловДОСКонтролирующимиОрганами КАК СостояниеОбъектов
			                    |		ПО (СостояниеОбъектов.Объект = СодержимоеТранспортныхКонтейнеров.ТранспортноеСообщение)
			                    |			И (СостояниеОбъектов.ИмяФайла = СодержимоеТранспортныхКонтейнеров.ИмяФайла)
			                    |			И (СостояниеОбъектов.Владелец = ЗНАЧЕНИЕ(Перечисление.ВидыАрхивируемыхМетаданныхДО.СодержимоеТранспортныхКонтейнеров))
			                    |ГДЕ
			                    |	СодержимоеТранспортныхКонтейнеров.Тип = &ТипСодержимогоЭлектронноЦифроваяПодпись
			                    |	И СодержимоеТранспортныхКонтейнеров.ТранспортноеСообщение.Тип = &ТипСообщенияИзвещениеПротоколВходногоКонтроляОтчетности
			                    |	И СодержимоеТранспортныхКонтейнеров.ТранспортноеСообщение В (&ТранспортныеСообщения)
			                    |	И СодержимоеТранспортныхКонтейнеров.ЭЦПЭтоПодписьАбонента = &парамИстина
			                    |
			                    |ОБЪЕДИНИТЬ ВСЕ
			                    |
			                    |ВЫБРАТЬ
			                    |	""ПечататьПротокол"",
			                    |	""ПротоколПодписьОрганаФСГС"",
			                    |	СодержимоеТранспортныхКонтейнеров.Данные,
			                    |	СодержимоеТранспортныхКонтейнеров.Тип,
			                    |	СодержимоеТранспортныхКонтейнеров.ИмяФайла,
			                    |	СодержимоеТранспортныхКонтейнеров.ЭЦПИмяПодписанногоФайла,
			                    |	СодержимоеТранспортныхКонтейнеров.ЭЦПСертификат,
			                    |	СодержимоеТранспортныхКонтейнеров.ТранспортноеСообщение,
			                    |	ВЫБОР
			                    |		КОГДА СостояниеОбъектов.Архивный ЕСТЬ NULL
			                    |			ТОГДА ЛОЖЬ
			                    |		КОГДА СостояниеОбъектов.Архивный = ЛОЖЬ
			                    |			ТОГДА ЛОЖЬ
			                    |		ИНАЧЕ ИСТИНА
			                    |	КОНЕЦ
			                    |ИЗ
			                    |	РегистрСведений.СодержимоеТранспортныхКонтейнеров КАК СодержимоеТранспортныхКонтейнеров
			                    |		ЛЕВОЕ СОЕДИНЕНИЕ РегистрСведений.ПризнакиАрхивированияФайловДОСКонтролирующимиОрганами КАК СостояниеОбъектов
			                    |		ПО (СостояниеОбъектов.Объект = СодержимоеТранспортныхКонтейнеров.ТранспортноеСообщение)
			                    |			И (СостояниеОбъектов.ИмяФайла = СодержимоеТранспортныхКонтейнеров.ИмяФайла)
			                    |			И (СостояниеОбъектов.Владелец = ЗНАЧЕНИЕ(Перечисление.ВидыАрхивируемыхМетаданныхДО.СодержимоеТранспортныхКонтейнеров))
			                    |ГДЕ
			                    |	СодержимоеТранспортныхКонтейнеров.Тип = &ТипСодержимогоЭлектронноЦифроваяПодпись
			                    |	И СодержимоеТранспортныхКонтейнеров.ТранспортноеСообщение.Тип = &ТипСообщенияПротоколВходногоКонтроляОтчетности
			                    |	И СодержимоеТранспортныхКонтейнеров.ТранспортноеСообщение В (&ТранспортныеСообщения)
			                    |	И СодержимоеТранспортныхКонтейнеров.ЭЦПЭтоПодписьАбонента <> &парамИстина");
			
			ТипыПротоколовВходногоКонтроля = Новый Массив;
			ТипыПротоколовВходногоКонтроля.Добавить(Перечисления.ТипыСодержимогоТранспортногоКонтейнера.УведомлениеОПриемеВОбработкуОтчетаФСГС);
			ТипыПротоколовВходногоКонтроля.Добавить(Перечисления.ТипыСодержимогоТранспортногоКонтейнера.УведомлениеОНесоответствииФорматуОтчетаФСГС);
			ТипыПротоколовВходногоКонтроля.Добавить(Перечисления.ТипыСодержимогоТранспортногоКонтейнера.УведомлениеОбУточненииОтчетаФСГС);
			ТипыПротоколовВходногоКонтроля.Добавить(Перечисления.ТипыСодержимогоТранспортногоКонтейнера.УведомлениеОбОтказеОтчетаФСГС);
			ТипыПротоколовВходногоКонтроля.Добавить(Перечисления.ТипыСодержимогоТранспортногоКонтейнера.УведомлениеОбОтклоненииОтчетаФСГС);
			Запрос.УстановитьПараметр("ТипСодержимогоПротоколВходногоКонтроля", ТипыПротоколовВходногоКонтроля);
			Запрос.УстановитьПараметр("ТипСообщенияПротоколВходногоКонтроляОтчетности", Перечисления.ТипыТранспортныхСообщений.ПротоколВходногоКонтроляОтчетностиФСГС);
			Запрос.УстановитьПараметр("ТипСодержимогоЭлектронноЦифроваяПодпись", Перечисления.ТипыСодержимогоТранспортногоКонтейнера.ЭлектронноЦифроваяПодпись);
			Запрос.УстановитьПараметр("ТипСообщенияИзвещениеПротоколВходногоКонтроляОтчетности", Перечисления.ТипыТранспортныхСообщений.ИзвещениеПротоколВходногоКонтроляОтчетностиФСГС);
			Запрос.УстановитьПараметр("парамИстина", Истина);
			
		КонецЕсли;
		
		
	ИначеЕсли ТипЦиклаОбмена = Перечисления.ТипыЦикловОбмена.ИОС Тогда
			
		Если Настройки.ПечататьОтветНаЗапросИОС Тогда
			
			Подзапросы.Добавить("ВЫБРАТЬ
			                    |	""ПечататьОтветНаЗапросИОС"" КАК Настройка,
			                    |	&ИдФайлаОтветНаЗапросПФР КАК ИдТипа,
			                    |	СодержимоеТранспортныхКонтейнеров.Данные КАК Данные,
			                    |	СодержимоеТранспортныхКонтейнеров.Тип КАК Тип,
			                    |	СодержимоеТранспортныхКонтейнеров.ИмяФайла КАК ИмяФайла,
			                    |	"""" КАК ИмяПодписанногоФайла,
			                    |	"""" КАК Сертификат,
			                    |	СодержимоеТранспортныхКонтейнеров.ТранспортноеСообщение КАК ТранспортноеСообщение,
			                    |	ВЫБОР
			                    |		КОГДА СостояниеОбъектов.Архивный ЕСТЬ NULL
			                    |			ТОГДА ЛОЖЬ
			                    |		КОГДА СостояниеОбъектов.Архивный = ЛОЖЬ
			                    |			ТОГДА ЛОЖЬ
			                    |		ИНАЧЕ ИСТИНА
			                    |	КОНЕЦ КАК ВАрхиве
			                    |ИЗ
			                    |	РегистрСведений.СодержимоеТранспортныхКонтейнеров КАК СодержимоеТранспортныхКонтейнеров
			                    |		ЛЕВОЕ СОЕДИНЕНИЕ РегистрСведений.ПризнакиАрхивированияФайловДОСКонтролирующимиОрганами КАК СостояниеОбъектов
			                    |		ПО (СостояниеОбъектов.Объект = СодержимоеТранспортныхКонтейнеров.ТранспортноеСообщение)
			                    |			И (СостояниеОбъектов.ИмяФайла = СодержимоеТранспортныхКонтейнеров.ИмяФайла)
			                    |			И (СостояниеОбъектов.Владелец = ЗНАЧЕНИЕ(Перечисление.ВидыАрхивируемыхМетаданныхДО.СодержимоеТранспортныхКонтейнеров))
			                    |ГДЕ
			                    |	СодержимоеТранспортныхКонтейнеров.Тип = &СодержимоеОтветНаЗапросПФР
			                    |	И СодержимоеТранспортныхКонтейнеров.ТранспортноеСообщение.Тип = &ТранспортноеСообщениеОтветНаЗапросПФР
			                    |	И СодержимоеТранспортныхКонтейнеров.ТранспортноеСообщение В (&ТранспортныеСообщения)
			                    |
			                    |ОБЪЕДИНИТЬ ВСЕ
			                    |
			                    |ВЫБРАТЬ
			                    |	""ПечататьОтветНаЗапросИОС"",
			                    |	&ИдПодписиОтветНаЗапросПФР,
			                    |	СодержимоеТранспортныхКонтейнеров.Данные,
			                    |	СодержимоеТранспортныхКонтейнеров.Тип,
			                    |	СодержимоеТранспортныхКонтейнеров.ИмяФайла,
			                    |	СодержимоеТранспортныхКонтейнеров.ЭЦПИмяПодписанногоФайла,
			                    |	СодержимоеТранспортныхКонтейнеров.ЭЦПСертификат,
			                    |	СодержимоеТранспортныхКонтейнеров.ТранспортноеСообщение,
			                    |	ВЫБОР
			                    |		КОГДА СостояниеОбъектов.Архивный ЕСТЬ NULL
			                    |			ТОГДА ЛОЖЬ
			                    |		КОГДА СостояниеОбъектов.Архивный = ЛОЖЬ
			                    |			ТОГДА ЛОЖЬ
			                    |		ИНАЧЕ ИСТИНА
			                    |	КОНЕЦ
			                    |ИЗ
			                    |	РегистрСведений.СодержимоеТранспортныхКонтейнеров КАК СодержимоеТранспортныхКонтейнеров
			                    |		ЛЕВОЕ СОЕДИНЕНИЕ РегистрСведений.ПризнакиАрхивированияФайловДОСКонтролирующимиОрганами КАК СостояниеОбъектов
			                    |		ПО (СостояниеОбъектов.Объект = СодержимоеТранспортныхКонтейнеров.ТранспортноеСообщение)
			                    |			И (СостояниеОбъектов.ИмяФайла = СодержимоеТранспортныхКонтейнеров.ИмяФайла)
			                    |			И (СостояниеОбъектов.Владелец = ЗНАЧЕНИЕ(Перечисление.ВидыАрхивируемыхМетаданныхДО.СодержимоеТранспортныхКонтейнеров))
			                    |ГДЕ
			                    |	СодержимоеТранспортныхКонтейнеров.Тип = &СодержимоеОтветНаЗапросПФР
			                    |	И СодержимоеТранспортныхКонтейнеров.ТранспортноеСообщение.Тип = &ТранспортноеСообщениеОтветНаЗапросПФР
			                    |	И СодержимоеТранспортныхКонтейнеров.ТранспортноеСообщение В (&ТранспортныеСообщения)
			                    |	И СодержимоеТранспортныхКонтейнеров.ЭЦПЭтоПодписьАбонента <> &парамИстина");
			
			
			Запрос.УстановитьПараметр("ИдФайлаОтветНаЗапросПФР",   НСтр("ru = 'ОтветНаЗапросИОС'"));
			Запрос.УстановитьПараметр("ИдПодписиОтветНаЗапросПФР", НСтр("ru = 'ОтветНаЗапросИОСПодписьОрганаПФР'"));
			Запрос.УстановитьПараметр("СодержимоеОтветНаЗапросПФР", Перечисления.ТипыСодержимогоТранспортногоКонтейнера.ОтветНаЗапросПФР);
			Запрос.УстановитьПараметр("ТранспортноеСообщениеОтветНаЗапросПФР", Перечисления.ТипыТранспортныхСообщений.ОтветНаЗапросПФР);
				
			Запрос.УстановитьПараметр("ТипСодержимогоЭлектронноЦифроваяПодпись", Перечисления.ТипыСодержимогоТранспортногоКонтейнера.ЭлектронноЦифроваяПодпись);
			Запрос.УстановитьПараметр("парамИстина", Истина);
			
		КонецЕсли;
		
		Если Настройки.ПечататьПодтвержденияПолученияЗапросаПФР Тогда
			
			Подзапросы.Добавить("ВЫБРАТЬ
			                    |	""ПечататьПодтвержденияПолученияЗапросаПФР"" КАК Настройка,
			                    |	&ИдФайла КАК ИдТипа,
			                    |	СодержимоеТранспортныхКонтейнеров.Данные КАК Данные,
			                    |	СодержимоеТранспортныхКонтейнеров.Тип КАК Тип,
			                    |	СодержимоеТранспортныхКонтейнеров.ИмяФайла КАК ИмяФайла,
			                    |	"""" КАК ИмяПодписанногоФайла,
			                    |	"""" КАК Сертификат,
			                    |	СодержимоеТранспортныхКонтейнеров.ТранспортноеСообщение КАК ТранспортноеСообщение,
			                    |	ВЫБОР
			                    |		КОГДА СостояниеОбъектов.Архивный ЕСТЬ NULL
			                    |			ТОГДА ЛОЖЬ
			                    |		КОГДА СостояниеОбъектов.Архивный = ЛОЖЬ
			                    |			ТОГДА ЛОЖЬ
			                    |		ИНАЧЕ ИСТИНА
			                    |	КОНЕЦ КАК ВАрхиве
			                    |ИЗ
			                    |	РегистрСведений.СодержимоеТранспортныхКонтейнеров КАК СодержимоеТранспортныхКонтейнеров
			                    |		ЛЕВОЕ СОЕДИНЕНИЕ РегистрСведений.ПризнакиАрхивированияФайловДОСКонтролирующимиОрганами КАК СостояниеОбъектов
			                    |		ПО (СостояниеОбъектов.Объект = СодержимоеТранспортныхКонтейнеров.ТранспортноеСообщение)
			                    |			И (СостояниеОбъектов.ИмяФайла = СодержимоеТранспортныхКонтейнеров.ИмяФайла)
			                    |			И (СостояниеОбъектов.Владелец = ЗНАЧЕНИЕ(Перечисление.ВидыАрхивируемыхМетаданныхДО.СодержимоеТранспортныхКонтейнеров))
			                    |ГДЕ
			                    |	СодержимоеТранспортныхКонтейнеров.Тип = &СодержимоеПодтверждениеПолученияЗапросПФР
			                    |	И СодержимоеТранспортныхКонтейнеров.ТранспортноеСообщение.Тип В(&ТранспортноеПодтверждениеПолученияЗапросаПФР)
			                    |	И СодержимоеТранспортныхКонтейнеров.ТранспортноеСообщение В (&ТранспортныеСообщения)
			                    |
			                    |ОБЪЕДИНИТЬ ВСЕ
			                    |
			                    |ВЫБРАТЬ
			                    |	""ПечататьПодтвержденияПолученияЗапросаПФР"",
			                    |	&ИдПодписи,
			                    |	СодержимоеТранспортныхКонтейнеров.Данные,
			                    |	СодержимоеТранспортныхКонтейнеров.Тип,
			                    |	СодержимоеТранспортныхКонтейнеров.ИмяФайла,
			                    |	СодержимоеТранспортныхКонтейнеров.ЭЦПИмяПодписанногоФайла,
			                    |	СодержимоеТранспортныхКонтейнеров.ЭЦПСертификат,
			                    |	СодержимоеТранспортныхКонтейнеров.ТранспортноеСообщение,
			                    |	ВЫБОР
			                    |		КОГДА СостояниеОбъектов.Архивный ЕСТЬ NULL
			                    |			ТОГДА ЛОЖЬ
			                    |		КОГДА СостояниеОбъектов.Архивный = ЛОЖЬ
			                    |			ТОГДА ЛОЖЬ
			                    |		ИНАЧЕ ИСТИНА
			                    |	КОНЕЦ
			                    |ИЗ
			                    |	РегистрСведений.СодержимоеТранспортныхКонтейнеров КАК СодержимоеТранспортныхКонтейнеров
			                    |		ЛЕВОЕ СОЕДИНЕНИЕ РегистрСведений.ПризнакиАрхивированияФайловДОСКонтролирующимиОрганами КАК СостояниеОбъектов
			                    |		ПО (СостояниеОбъектов.Объект = СодержимоеТранспортныхКонтейнеров.ТранспортноеСообщение)
			                    |			И (СостояниеОбъектов.ИмяФайла = СодержимоеТранспортныхКонтейнеров.ИмяФайла)
			                    |			И (СостояниеОбъектов.Владелец = ЗНАЧЕНИЕ(Перечисление.ВидыАрхивируемыхМетаданныхДО.СодержимоеТранспортныхКонтейнеров))
			                    |ГДЕ
			                    |	СодержимоеТранспортныхКонтейнеров.Тип = &СодержимоеПодтверждениеПолученияЗапросПФР
			                    |	И СодержимоеТранспортныхКонтейнеров.ТранспортноеСообщение.Тип В(&ТранспортноеПодтверждениеПолученияЗапросаПФР)
			                    |	И СодержимоеТранспортныхКонтейнеров.ТранспортноеСообщение В (&ТранспортныеСообщения)
			                    |	И СодержимоеТранспортныхКонтейнеров.ЭЦПЭтоПодписьАбонента <> &парамИстина");
			
			Запрос.УстановитьПараметр("ИдФайла",   НСтр("ru = 'ПодтверждениеПолученияЗапросаПФР'"));
			Запрос.УстановитьПараметр("ИдПодписи", НСтр("ru = 'ПодтверждениеПолученияЗапросаПФРПодписьОрганаПФР'"));
			Запрос.УстановитьПараметр("СодержимоеПодтверждениеПолученияЗапросПФР", Перечисления.ТипыСодержимогоТранспортногоКонтейнера.ПодтверждениеПолученияЗапросПФР);
			
			ТранспортноеПодтверждениеПолученияЗапросаПФР = Новый Массив;
			ТранспортноеПодтверждениеПолученияЗапросаПФР.Добавить(Перечисления.ТипыТранспортныхСообщений.ПодтверждениеПолученияЗапросаПФР);
			ТранспортноеПодтверждениеПолученияЗапросаПФР.Добавить(Перечисления.ТипыТранспортныхСообщений.ОтветНаЗапросКвитанцияПФР);
			
			Запрос.УстановитьПараметр("ТранспортноеПодтверждениеПолученияЗапросаПФР", ТранспортноеПодтверждениеПолученияЗапросаПФР);
				
			Запрос.УстановитьПараметр("ТипСодержимогоЭлектронноЦифроваяПодпись", Перечисления.ТипыСодержимогоТранспортногоКонтейнера.ЭлектронноЦифроваяПодпись);
			Запрос.УстановитьПараметр("парамИстина", Истина);
			
		КонецЕсли;
		
	КонецЕсли;
	
	Если ТипЦиклаОбмена = Перечисления.ТипыЦикловОбмена.НеформализованнаяПерепискаПФРИсходящие
		ИЛИ ТипЦиклаОбмена = Перечисления.ТипыЦикловОбмена.НеформализованнаяПерепискаПФРВходящие Тогда
			
		Если Настройки.ПодтверждениеПолученияПисьмаПФР Тогда
			
			Подзапросы.Добавить("ВЫБРАТЬ
			                    |	""ПодтверждениеПолученияПисьмаПФР"" КАК Настройка,
			                    |	&ПодтверждениеПолученияПисьмаПФР КАК ИдТипа,
			                    |	СодержимоеТранспортныхКонтейнеров.Данные КАК Данные,
			                    |	СодержимоеТранспортныхКонтейнеров.Тип КАК Тип,
			                    |	СодержимоеТранспортныхКонтейнеров.ИмяФайла КАК ИмяФайла,
			                    |	"""" КАК ИмяПодписанногоФайла,
			                    |	"""" КАК Сертификат,
			                    |	СодержимоеТранспортныхКонтейнеров.ТранспортноеСообщение КАК ТранспортноеСообщение,
			                    |	ВЫБОР
			                    |		КОГДА СостояниеОбъектов.Архивный ЕСТЬ NULL
			                    |			ТОГДА ЛОЖЬ
			                    |		КОГДА СостояниеОбъектов.Архивный = ЛОЖЬ
			                    |			ТОГДА ЛОЖЬ
			                    |		ИНАЧЕ ИСТИНА
			                    |	КОНЕЦ КАК ВАрхиве
			                    |ИЗ
			                    |	РегистрСведений.СодержимоеТранспортныхКонтейнеров КАК СодержимоеТранспортныхКонтейнеров
			                    |		ЛЕВОЕ СОЕДИНЕНИЕ РегистрСведений.ПризнакиАрхивированияФайловДОСКонтролирующимиОрганами КАК СостояниеОбъектов
			                    |		ПО (СостояниеОбъектов.Объект = СодержимоеТранспортныхКонтейнеров.ТранспортноеСообщение)
			                    |			И (СостояниеОбъектов.ИмяФайла = СодержимоеТранспортныхКонтейнеров.ИмяФайла)
			                    |			И (СостояниеОбъектов.Владелец = ЗНАЧЕНИЕ(Перечисление.ВидыАрхивируемыхМетаданныхДО.СодержимоеТранспортныхКонтейнеров))
			                    |ГДЕ
			                    |	СодержимоеТранспортныхКонтейнеров.Тип = &ТипыСодержимогоТранспортногоКонтейнера
			                    |	И СодержимоеТранспортныхКонтейнеров.ТранспортноеСообщение.Тип В(&ПисьмоКвитанцияПФР)
			                    |	И СодержимоеТранспортныхКонтейнеров.ТранспортноеСообщение В (&ТранспортныеСообщения)
			                    |
			                    |ОБЪЕДИНИТЬ ВСЕ
			                    |
			                    |ВЫБРАТЬ
			                    |	""ПодтверждениеПолученияПисьмаПФР"",
			                    |	&ПодтверждениеПолученияПисьмаПФРПодписьОрганаПФР,
			                    |	СодержимоеТранспортныхКонтейнеров.Данные,
			                    |	СодержимоеТранспортныхКонтейнеров.Тип,
			                    |	СодержимоеТранспортныхКонтейнеров.ИмяФайла,
			                    |	СодержимоеТранспортныхКонтейнеров.ЭЦПИмяПодписанногоФайла,
			                    |	СодержимоеТранспортныхКонтейнеров.ЭЦПСертификат,
			                    |	СодержимоеТранспортныхКонтейнеров.ТранспортноеСообщение,
			                    |	ВЫБОР
			                    |		КОГДА СостояниеОбъектов.Архивный ЕСТЬ NULL
			                    |			ТОГДА ЛОЖЬ
			                    |		КОГДА СостояниеОбъектов.Архивный = ЛОЖЬ
			                    |			ТОГДА ЛОЖЬ
			                    |		ИНАЧЕ ИСТИНА
			                    |	КОНЕЦ
			                    |ИЗ
			                    |	РегистрСведений.СодержимоеТранспортныхКонтейнеров КАК СодержимоеТранспортныхКонтейнеров
			                    |		ЛЕВОЕ СОЕДИНЕНИЕ РегистрСведений.ПризнакиАрхивированияФайловДОСКонтролирующимиОрганами КАК СостояниеОбъектов
			                    |		ПО (СостояниеОбъектов.Объект = СодержимоеТранспортныхКонтейнеров.ТранспортноеСообщение)
			                    |			И (СостояниеОбъектов.ИмяФайла = СодержимоеТранспортныхКонтейнеров.ИмяФайла)
			                    |			И (СостояниеОбъектов.Владелец = ЗНАЧЕНИЕ(Перечисление.ВидыАрхивируемыхМетаданныхДО.СодержимоеТранспортныхКонтейнеров))
			                    |ГДЕ
			                    |	СодержимоеТранспортныхКонтейнеров.Тип = &ТипСодержимогоЭлектронноЦифроваяПодпись
			                    |	И СодержимоеТранспортныхКонтейнеров.ТранспортноеСообщение.Тип В(&ПисьмоКвитанцияПФР)
			                    |	И СодержимоеТранспортныхКонтейнеров.ТранспортноеСообщение В (&ТранспортныеСообщения)
			                    |	И СодержимоеТранспортныхКонтейнеров.ЭЦПЭтоПодписьАбонента <> &парамИстина");
			
			Запрос.УстановитьПараметр("ПодтверждениеПолученияПисьмаПФР",   НСтр("ru = 'ПодтверждениеПолученияПисьмаПФР'"));
			Запрос.УстановитьПараметр("ПодтверждениеПолученияПисьмаПФРПодписьОрганаПФР", НСтр("ru = 'ПодтверждениеПолученияПисьмаПФРПодписьОрганаПФР'"));
			Запрос.УстановитьПараметр("ТипыСодержимогоТранспортногоКонтейнера", Перечисления.ТипыСодержимогоТранспортногоКонтейнера.ПодтверждениеПолученияПисьма);
			
			ПисьмоКвитанцияПФР = Новый Массив;
			ПисьмоКвитанцияПФР.Добавить(Перечисления.ТипыТранспортныхСообщений.ПисьмоИсходящееКвитанцияПФР);
			ПисьмоКвитанцияПФР.Добавить(Перечисления.ТипыТранспортныхСообщений.ПисьмоВходящееКвитанцияПФР);
			
			Запрос.УстановитьПараметр("ПисьмоКвитанцияПФР", ПисьмоКвитанцияПФР);
				
			Запрос.УстановитьПараметр("ТипСодержимогоЭлектронноЦифроваяПодпись", Перечисления.ТипыСодержимогоТранспортногоКонтейнера.ЭлектронноЦифроваяПодпись);
			Запрос.УстановитьПараметр("парамИстина", Истина);
			
		КонецЕсли;
		
	КонецЕсли;
	
	// компилируем запрос из подзапросов
	Для Инд = 0 По Подзапросы.Количество() - 1 Цикл
		ТекЗапрос = Подзапросы[Инд];
		Если Инд = 0 Тогда
			Запрос.Текст = "ВЫБРАТЬ РАЗРЕШЕННЫЕ" + Сред(ТекЗапрос, 8);
		Иначе
			Запрос.Текст = Запрос.Текст + "
						  |
						  |ОБЪЕДИНИТЬ ВСЕ
						  |
						  |" + ТекЗапрос;
		КонецЕсли;
	КонецЦикла;
	
	// выполняем запрос, сохраняем файлы во временный каталог и формируем таблицу файлов
	Если ЗначениеЗаполнено(Запрос.Текст) Тогда
		ТаблицаЗапроса = Запрос.Выполнить().Выгрузить();
	Иначе
		ТаблицаЗапроса = Неопределено;
	КонецЕсли;
	
	Возврат ТаблицаЗапроса;
	
КонецФункции

Функция ТипыИзвещенийОПолучении() Экспорт
	
	ТипСообщенияПодтверждениеОтправкиОтчетности = Новый Массив;
	ТипСообщенияПодтверждениеОтправкиОтчетности.Добавить(Перечисления.ТипыТранспортныхСообщений.ИзвещениеДекларацияНО);
	ТипСообщенияПодтверждениеОтправкиОтчетности.Добавить(Перечисления.ТипыТранспортныхСообщений.ИзвещениеОбращениеНО);
	ТипСообщенияПодтверждениеОтправкиОтчетности.Добавить(Перечисления.ТипыТранспортныхСообщений.ИзвещениеПисьмоНП);
	ТипСообщенияПодтверждениеОтправкиОтчетности.Добавить(Перечисления.ТипыТранспортныхСообщений.ИзвещениеРассылкаНП);
	ТипСообщенияПодтверждениеОтправкиОтчетности.Добавить(Перечисления.ТипыТранспортныхСообщений.ИзвещениеЗапросНО);
	ТипСообщенияПодтверждениеОтправкиОтчетности.Добавить(Перечисления.ТипыТранспортныхСообщений.ИзвещениеФорма2НДФЛНО);
	ТипСообщенияПодтверждениеОтправкиОтчетности.Добавить(Перечисления.ТипыТранспортныхСообщений.ИзвещениеЗаявлениеНО);
	ТипСообщенияПодтверждениеОтправкиОтчетности.Добавить(Перечисления.ТипыТранспортныхСообщений.ИзвещениеДокументНП);
	ТипСообщенияПодтверждениеОтправкиОтчетности.Добавить(Перечисления.ТипыТранспортныхСообщений.ИзвещениеПредставлениеНО);
	ТипСообщенияПодтверждениеОтправкиОтчетности.Добавить(Перечисления.ТипыТранспортныхСообщений.ИзвещениеРезультатПриемаНП);
	ТипСообщенияПодтверждениеОтправкиОтчетности.Добавить(Перечисления.ТипыТранспортныхСообщений.ИзвещениеПодтверждениеНП);
	ТипСообщенияПодтверждениеОтправкиОтчетности.Добавить(Перечисления.ТипыТранспортныхСообщений.ИзвещениеРезультатОбработкиНП);
	ТипСообщенияПодтверждениеОтправкиОтчетности.Добавить(Перечисления.ТипыТранспортныхСообщений.ИзвещениеРезультатОбработкиРФНП);
	ТипСообщенияПодтверждениеОтправкиОтчетности.Добавить(Перечисления.ТипыТранспортныхСообщений.ИзвещениеОбОтзывеЗаявлениеРФНП);
	ТипСообщенияПодтверждениеОтправкиОтчетности.Добавить(Перечисления.ТипыТранспортныхСообщений.ИзвещениеРезультатОбработкиТСНП);

	Возврат ТипСообщенияПодтверждениеОтправкиОтчетности;
	
КонецФункции

Функция ПолучитьДатуОтправки(Объект, Знач КонтролирующийОрган = "ФНС") Экспорт
	
	НеПолучатьДаты = Ложь;
	ДатаОтправки   = Дата(1,1,1);
	
	ТипКонтролирующегоОргана = ТипКонтролирующегоОргана(КонтролирующийОрган);
	
	// <Объект не найден>
	Если СтрНайти(Строка(Объект),"Объект не найден") > 0 Тогда
		ДатаОтправки = Дата(1,1,1);
	// Переписка
	ИначеЕсли ТипЗнч(Объект.Ссылка) = Тип("СправочникСсылка.ПерепискаСКонтролирующимиОрганами") Тогда
		
		Если Объект.Статус = Перечисления.СтатусыПисем.Полученное Тогда
			ДатаОтправки = Дата(1,1,1);
		Иначе
			
			ЦиклОбмена = ДокументооборотСКОВызовСервера.ПолучитьПоследнийЦиклОбмена(Объект.Ссылка);
			
			Если ЗначениеЗаполнено(ЦиклОбмена) Тогда

				СообщенияЦикла = ПолучитьСообщенияЦиклаОбмена(ЦиклОбмена,,Ложь,Истина);
				
				Если Объект.Тип = Перечисления.ТипыПерепискиСКонтролирующимиОрганами.ПерепискаСПФР Тогда
					ТипСообщения = Перечисления.ТипыТранспортныхСообщений.ПисьмоИсходящееПФР;
				ИначеЕсли Объект.Тип = Перечисления.ТипыПерепискиСКонтролирующимиОрганами.ПерепискаСФСГС Тогда
					ТипСообщения = Перечисления.ТипыТранспортныхСообщений.ПисьмоИсходящееФСГС;
				Иначе
					ТипСообщения = Перечисления.ТипыТранспортныхСообщений.ОбращениеНП;
				КонецЕсли;
				
				СтрПервичныеСообщения = СообщенияЦикла.НайтиСтроки(Новый Структура("Тип", ТипСообщения));
					
				ДатаОтправки = ?(СтрПервичныеСообщения.Количество() > 0, СтрПервичныеСообщения[0].Дата, Дата(1,1,1));
				
			КонецЕсли;
			
		КонецЕсли;
		
	// Макеты ПД, Заявления о пенсии
	ИначеЕсли ТипЗнч(Объект.Ссылка) = Тип("СправочникСсылка.МакетыПенсионныхДел")
		ИЛИ ТипЗнч(Объект.Ссылка) = Тип("СправочникСсылка.ЗаявлениеОНазначенииПенсии") Тогда
		
		ДатаОтправки = ?(ЗначениеЗаполнено(Объект.ДатаСоздания) , " " + Формат(Объект.ДатаСоздания, "ДЛФ=DT"), Дата(1,1,1));
		
	// ЕГРЮЛ, ЕГРИП, Запрос ИОН, Запрос ИОС, ФНС, ПФР, Росстат
	ИначеЕсли ТипЗнч(Объект.Ссылка) = Тип("ДокументСсылка.ЗапросНаВыпискуИзЕГРЮЛ_ЕГРИП")
		ИЛИ ТипЗнч(Объект.Ссылка) = Тип("ДокументСсылка.ЗапросНаИнформационноеОбслуживаниеНалогоплательщика")
		ИЛИ ТипЗнч(Объект.Ссылка) = Тип("ДокументСсылка.ЗапросНаИнформационноеОбслуживаниеСтрахователя")
		ИЛИ ТипКонтролирующегоОргана = Перечисления.ТипыКонтролирующихОрганов.ФНС
		ИЛИ ТипКонтролирующегоОргана = Перечисления.ТипыКонтролирующихОрганов.ПФР
		ИЛИ ТипКонтролирующегоОргана = Перечисления.ТипыКонтролирующихОрганов.ФСГС Тогда
		
		ЦиклОбмена = ДокументооборотСКОВызовСервера.ПолучитьПоследнийЦиклОбмена(Объект.Ссылка);
		
		Если ЗначениеЗаполнено(ЦиклОбмена) Тогда

			СообщенияЦикла = ПолучитьСообщенияЦиклаОбмена(ЦиклОбмена,,Ложь,Истина);
			
			Если ТипЗнч(Объект.Ссылка) = Тип("ДокументСсылка.ЗапросНаВыпискуИзЕГРЮЛ_ЕГРИП") Тогда
				ТипСообщения = Перечисления.ТипыТранспортныхСообщений.ЗапросНаВыпискуЕРГЮЛ_ЕГРИП;
			ИначеЕсли ТипЗнч(Объект.Ссылка) = Тип("ДокументСсылка.ЗапросНаИнформационноеОбслуживаниеНалогоплательщика") Тогда
				ТипСообщения = Перечисления.ТипыТранспортныхСообщений.ЗапросНП;
			ИначеЕсли ТипЗнч(Объект.Ссылка) = Тип("ДокументСсылка.ЗапросНаИнформационноеОбслуживаниеСтрахователя") Тогда
				ТипСообщения = Перечисления.ТипыТранспортныхСообщений.ПервичноеСообщениеСодержащееЗапросПФР;
			ИначеЕсли ТипКонтролирующегоОргана = Перечисления.ТипыКонтролирующихОрганов.ФНС Тогда
				Если ЦиклОбмена.Тип = Перечисления.ТипыЦикловОбмена.Заявление Тогда
					ТипСообщения = Перечисления.ТипыТранспортныхСообщений.ЗаявлениеНП;
				ИначеЕсли ЦиклОбмена.Тип = Перечисления.ТипыЦикловОбмена.Форма2НДФЛ Тогда
					ТипСообщения = Перечисления.ТипыТранспортныхСообщений.Форма2НДФЛНП;
				ИначеЕсли ЦиклОбмена.Тип = Перечисления.ТипыЦикловОбмена.Представление Тогда
					ТипСообщения = Перечисления.ТипыТранспортныхСообщений.ПредставлениеНП;
				Иначе
					ТипСообщения = Перечисления.ТипыТранспортныхСообщений.ДекларацияНП;
				КонецЕсли;
			ИначеЕсли ТипКонтролирующегоОргана = Перечисления.ТипыКонтролирующихОрганов.ПФР Тогда
				ТипСообщения = Перечисления.ТипыТранспортныхСообщений.ПервичноеСообщениеСодержащееОтчетностьПФР;
			ИначеЕсли ТипКонтролирующегоОргана = Перечисления.ТипыКонтролирующихОрганов.ФСГС Тогда
				ТипСообщения = Перечисления.ТипыТранспортныхСообщений.ПервичноеСообщениеСодержащееОтчетностьФСГС;
			КонецЕсли;
			
			СтрПервичныеСообщения = СообщенияЦикла.НайтиСтроки(Новый Структура("Тип", ТипСообщения));
				
			ДатаОтправки = ДатаОтправки(СтрПервичныеСообщения, НеПолучатьДаты);
			
		КонецЕсли;
		
	// ФСС, ФСРАР, РПН, ФТС, БанкРоссии
	ИначеЕсли ТипКонтролирующегоОргана = Перечисления.ТипыКонтролирующихОрганов.ФСС
		ИЛИ ТипКонтролирующегоОргана = Перечисления.ТипыКонтролирующихОрганов.ФСРАР
		ИЛИ ТипКонтролирующегоОргана = Перечисления.ТипыКонтролирующихОрганов.РПН
		ИЛИ ТипКонтролирующегоОргана = Перечисления.ТипыКонтролирующихОрганов.ФТС Тогда
	
		Если ТипКонтролирующегоОргана = Перечисления.ТипыКонтролирующихОрганов.ФСС Тогда
			ОтправкаСсылка = ПолучитьПоследнююОтправкуОтчетаВФСС(Объект.Ссылка);
		ИначеЕсли ТипКонтролирующегоОргана = Перечисления.ТипыКонтролирующихОрганов.ФСРАР Тогда
			ОтправкаСсылка = ПолучитьПоследнююОтправкуОтчетаВФСРАР(Объект.Ссылка);
		ИначеЕсли ТипКонтролирующегоОргана = Перечисления.ТипыКонтролирующихОрганов.РПН Тогда
			ОтправкаСсылка = ПолучитьПоследнююОтправкуОтчетаВРПН(Объект.Ссылка);
		ИначеЕсли ТипКонтролирующегоОргана = Перечисления.ТипыКонтролирующихОрганов.ФТС Тогда
			ОтправкаСсылка =  ПолучитьПоследнююОтправкуОтчетаВФТС(Объект.Ссылка);
		ИначеЕсли ТипКонтролирующегоОргана = Перечисления.ТипыКонтролирующихОрганов.БанкРоссии Тогда
			ОтправкаСсылка =  ПолучитьПоследнююОтправкуОтчетаВБанкРоссии(Объект.Ссылка);
		КонецЕсли; 
		
		ДатаОтправки = ?(ЗначениеЗаполнено(ОтправкаСсылка), ОтправкаСсылка.ДатаОтправки, Дата(1,1,1));
		
	КонецЕсли;
	
	Возврат ДатаОтправки;

КонецФункции

#КонецОбласти

// Реализация метода частотного определения кодировки ДвоичныхДанных, для случаев когда не удается
// однозначно определить кодировку используется анализ BOM и encoding для XML, поддерживается распознавание
// Windows-1251, UTF-8, UTF-16, OEM866
//
// Параметры:
//  ДДИлиСсылка - адрес ДвоичныхДанных во временном хранилище или непосредственно ДвоичныеДанные
//
// Возвращаемое значение:
//  "windows-1251"/КодировкаТекста.UTF8/КодировкаТекста.UTF16/"cp866"
//
Функция ОпределитьКодировку(Знач ДДИлиСсылка) Экспорт
	
	
	Если ТипЗнч(ДДИлиСсылка) = Тип("Строка") Тогда 
		Если ЭтоАдресВременногоХранилища(ДДИлиСсылка) Тогда 
			ДД = ПолучитьИзВременногоХранилища(ДДИлиСсылка);
		Иначе
			Возврат "windows-1251";
		КонецЕсли;
	ИначеЕсли ТипЗнч(ДДИлиСсылка) = Тип("ДвоичныеДанные") Тогда 
		ДД = ДДИлиСсылка;
	Иначе
		Возврат "windows-1251";
	КонецЕсли;
	
	Стр = Лев(ПолучитьHexСтрокуИзДвоичныхДанных(ДД), 4096);
	
	Если СтрДлина(Стр) < 2 Тогда 
		Возврат "windows-1251";
	КонецЕсли;
			
	ИндексДД = СтрНайти(Стр, "00000000"); //Двоичные данные, не отобразятся корректно
	Если ИндексДД > 0 Тогда 
		Возврат "windows-1251";
	КонецЕсли;
	
	Всего = 0;
	// В windows-1251 кириллица располагается в диапазоне
	// 192-255 А-Я, а-я
	
	// В cp866 кириллица располагается в диапазоне 
	// 128-175 А-Я, а-п
	// 224-241 р-я, Ёё
	
	// В UTF-8 Кириллица располагается в диапазоне 
	// D090 - D0BF А-Я, а-п	D000 + 144-191
	// D180 - D18F р-я 		D100 + 128-143
	
	// В UTF-16 Кириллица располагается в диапазоне 
	// 0410 - 042F
	// 0430 - 044F
	
	КодировкаПоBOM = Неопределено;
	BOM = Лев(Стр, 8);	
	Если СтрНайти(BOM, "EFBBBF") > 0 Тогда 
		КодировкаПоBOM = КодировкаТекста.UTF8;
		Стр = Сред(Стр, 7);
	ИначеЕсли СтрНайти(BOM, "FEFF") > 0 Тогда //BE
		КодировкаПоBOM = КодировкаТекста.UTF16;
		Стр = Сред(Стр, 5);
	ИначеЕсли СтрНайти(BOM, "FFFE") > 0 Тогда //LE
		КодировкаПоBOM = КодировкаТекста.UTF16;
		Стр = Сред(Стр, 5);
	КонецЕсли;
	
	//decl = "A1B2C3D4E5F6"; //Test value
	decl = "3C3F786D6C";
	enc = "656E636F64696E67"; // encoding word
	ansi_s = "31323531";
	ДлинаВыборкиXML = 150;
	Если КодировкаПоBOM = КодировкаТекста.UTF16 Тогда		
		СтрМакс = СтрДлина(decl) / 2;
		Для ш = 1 по СтрМакс Цикл 
			decl = Лев(decl, ш * 2 + (ш-1) * 2) + "00" + Прав(decl, СтрМакс * 2 - ш * 2);
		КонецЦикла;
		СтрМакс = СтрДлина(enc) / 2;
		Для ш = 1 по СтрМакс Цикл 
			enc = Лев(enc, ш * 2 + (ш-1) * 2) + "00" + Прав(enc, СтрМакс * 2 - ш * 2);
		КонецЦикла;
		СтрМакс = СтрДлина(ansi_s) / 2;
		Для ш = 1 по СтрМакс Цикл 
			ansi_s = Лев(ansi_s, ш * 2 + (ш-1) * 2) + "00" + Прав(ansi_s, СтрМакс * 2 - ш * 2);
		КонецЦикла;
		ДлинаВыборкиXML = 200;
	КонецЕсли;
	
	КодировкаПоXML = Неопределено;
	XMLpfx = Лев(Стр, ДлинаВыборкиXML);	
	ИндексXML = СтрНайти(XMLpfx, decl);
	Если ИндексXML > 0 и ИндексXML < 15 Тогда //xml declaration должно быть в самом начале файла
		
		Индекс = СтрНайти(XMLpfx, enc);
		Если Индекс  = 0 Тогда 
			КодировкаПоXML = КодировкаТекста.UTF8;
		Иначе
			Открывающая = СтрНайти(XMLpfx, "22",, Индекс); // symbol "
			Закрывающая = СтрНайти(XMLpfx, "22",, Открывающая + 2); // symbol "			
			Если Открывающая <> 0 И Закрывающая <> 0 Тогда 
				ТекстКодировки = Сред(XMLpfx, Открывающая + 2, Закрывающая - Открывающая - 2); //Без обрамляющих кавычек
				Если СтрНайти(ТекстКодировки, ansi_s) > 0 Тогда //1251
					КодировкаПоXML = "windows-1251";
				ИначеЕсли СтрНайти(ТекстКодировки, "38") > 0 Тогда //8
					КодировкаПоXML = КодировкаТекста.UTF8;
				ИначеЕсли СтрНайти(ТекстКодировки, "310036") > 0 Тогда //16
					КодировкаПоXML = КодировкаТекста.UTF16;
				КонецЕсли;				
			КонецЕсли;
		КонецЕсли;		
	КонецЕсли;
	
	Симв16 = Новый Соответствие;
	Для ш = 0 по 9 Цикл 
		Симв16.Вставить(Строка(ш), ш);
	КонецЦикла;
	Для ш = КодСимвола("A") по КодСимвола("F") Цикл 
		Симв16.Вставить(Символ(ш), 10 + ш - КодСимвола("A"));
	КонецЦикла;
	
	КодировкаПоЧастоте = Неопределено;
	КириллицыAnsi = 0;
	КириллицыOem = 0;
	КириллицыUtf8 = 0;
	КириллицыUtf16 = 0;
	Служебных = 0;
	
	Предыдущий = "";
	СтаршихUtf16 = 0;
	
	Пока СтрДлина(Стр) > 2 Цикл
		Код16 = Лев(Стр, 2);
		Стр = Сред(Стр, 3);
		Дес = Симв16[Лев(Код16, 1)] * 16 + Симв16[Сред(Код16, 2, 1)];
		
		Код16След = Лев(Стр, 2);
		Если СтрДлина(Код16След) = 2 Тогда 
			ДесСлед = Симв16[Лев(Код16След, 1)] * 16 + Симв16[Сред(Код16След, 2, 1)];
		Иначе
			ДесСлед = 0;
		КонецЕсли;
		
		Если Предыдущий = "D0" И (Дес>=144 И Дес <=191) Тогда // UTF-8 cyr //D090-D0BF
			КириллицыUtf8 = КириллицыUtf8 + 1;
		ИначеЕсли Предыдущий = "D1" И (Дес>=128 И Дес <=143) Тогда // UTF-8 cyr //D180 - D18F
			КириллицыUtf8 = КириллицыUtf8 + 1;
			
		ИначеЕсли Предыдущий = "04" И Дес>=16 И Дес <=79 Тогда // UTF-16 cyr //0410 - 044F
			СтаршихUtf16 = СтаршихUtf16 + 1;
			КириллицыUtf16 = КириллицыUtf16 + 1;
		ИначеЕсли Код16 = "00" Или Код16 = "04" Тогда // UTF-16 lat, num
			СтаршихUtf16 = СтаршихUtf16 + 1;
			
		Иначе
			Если (Код16 = "D0" И (ДесСлед>=144 И ДесСлед <=191)) Или (Код16 = "D1" И (ДесСлед >= 128 И ДесСлед <=143)) Тогда 
				//Forward lookup, ничего не делаем
			Иначе
				Если Дес>=192 И Дес <=255 Тогда //C0-FF
					КириллицыAnsi = КириллицыAnsi + 1;
				КонецЕсли;
				Если (Дес>=128 И Дес <=175) Или (Дес>=224 И Дес <=241) Тогда //80-AF, E0-F1
					КириллицыOem = КириллицыOem + 1;
				КонецЕсли;
				Если (Дес<32 И Дес <> 10 И Дес <> 13) Тогда // Control chars
					Служебных = Служебных + 1;
				КонецЕсли;
			КонецЕсли;		
		КонецЕсли;
		
		Предыдущий = Код16;
		Всего = Всего + 1;
	КонецЦикла;

	Если Всего = 0 Тогда 
		Возврат "windows-1251";
	КонецЕсли;
	
	Процент8 = (((КириллицыUtf8) / Всего) * 100);	//Процент кириллицы в Utf8 диапазоне
	Процент16 = (((КириллицыUtf16) / Всего) * 100);	//Процент кириллицы в Utf16 диапазоне
	ПроцентAnsi = ((КириллицыAnsi / Всего) * 100);	//Процент кириллицы в ANSI диапазоне
	ПроцентOem = ((КириллицыOem / Всего) * 100);	//Процент кириллицы в OEM диапазоне
	ПроцентСлужебных = ((Служебных / Всего) * 100);	//Процент служебных
	
	Процент00 = (((СтаршихUtf16) / Всего) * 100);	//Процент старших нулевых байтов Utf16
	
	//Точность = 
	//	"UTF-8 " + Формат(Процент8, "ЧДЦ=2; ЧН=0; ЧГ=") + "% " + 
	//	"UTF-16 " + Формат(Процент16, "ЧДЦ=2; ЧН=0; ЧГ=") + "% " +
	//	"Ansi " + Формат(ПроцентAnsi, "ЧДЦ=2; ЧН=0; ЧГ=") + "% " +
	//	"Oem " + Формат(ПроцентOem, "ЧДЦ=2; ЧН=0; ЧГ=") + "%";
	
	Если (ПроцентСлужебных > 15 И 
		ПроцентСлужебных > Процент8 И 
		ПроцентСлужебных > Процент16 И 
		ПроцентСлужебных > ПроцентAnsi И 
		ПроцентСлужебных > ПроцентOem) Тогда
		
		Возврат КодировкаТекста.ANSI;	// Двоичные данные
	КонецЕсли;
	
	Если (Процент16 > 0 Или Процент00 > 30) Тогда
		Возврат КодировкаТекста.UTF16;		
	КонецЕсли;
	
	Если (Процент8 > ПроцентAnsi И Процент8 > ПроцентOem)
		Или (Процент8 >= ПроцентAnsi И Процент8 >= ПроцентOem И (КодировкаПоBOM = КодировкаТекста.UTF8 Или КодировкаПоXML = КодировкаТекста.UTF8)) Тогда
		КодировкаПоЧастоте = КодировкаТекста.UTF8;
	КонецЕсли;
	
	Если (КодировкаПоЧастоте = Неопределено И ПроцентAnsi > Процент8 И ПроцентAnsi > ПроцентOem)
		Или (ПроцентAnsi >= Процент8 И ПроцентAnsi >= ПроцентOem И (КодировкаПоXML = "windows-1251")) Тогда
		КодировкаПоЧастоте = "windows-1251";
	КонецЕсли;
	
	Если КодировкаПоЧастоте = Неопределено И ПроцентOem > Процент8 И ПроцентOem >= ПроцентAnsi Тогда
		КодировкаПоЧастоте = "cp866";
	КонецЕсли;
	
	Если КодировкаПоЧастоте = Неопределено И КодировкаПоBOM = Неопределено И КодировкаПоXML = Неопределено Тогда 
		Возврат "windows-1251";
	ИначеЕсли КодировкаПоЧастоте <> Неопределено Тогда 
		Возврат КодировкаПоЧастоте;
	ИначеЕсли КодировкаПоXML <> Неопределено Тогда 
		Возврат КодировкаПоXML;
	ИначеЕсли КодировкаПоBOM <> Неопределено Тогда 
		Возврат КодировкаПоBOM;
	КонецЕсли;
	
	Возврат "windows-1251";
	
КонецФункции

Функция ТипСодержимогоПоСтроке(СтрТипСодержимого) Экспорт
	
	Если НРег(СтрТипСодержимого) = "plain866" Тогда
		Возврат Перечисления.ТипыСодержимогоФайлов.Текст866;
	ИначеЕсли НРег(СтрТипСодержимого) = "текст1251" Тогда
		Возврат Перечисления.ТипыСодержимогоФайлов.Текст1251;
	ИначеЕсли НРег(СтрТипСодержимого) = "plain1251" Тогда
		Возврат Перечисления.ТипыСодержимогоФайлов.Текст1251;
	ИначеЕсли НРег(СтрТипСодержимого) = "xml" Тогда
		Возврат Перечисления.ТипыСодержимогоФайлов.xml;
	ИначеЕсли НРег(СтрТипСодержимого) = "html" Тогда
		Возврат Перечисления.ТипыСодержимогоФайлов.html;
	ИначеЕсли НРег(СтрТипСодержимого) = "pdf" Тогда
		Возврат Перечисления.ТипыСодержимогоФайлов.pdf;
	ИначеЕсли НРег(СтрТипСодержимого) = "rtf" Тогда
		Возврат Перечисления.ТипыСодержимогоФайлов.rtf;
	ИначеЕсли НРег(СтрТипСодержимого) = "tiff" Тогда
		Возврат Перечисления.ТипыСодержимогоФайлов.tiff;
	ИначеЕсли НРег(СтрТипСодержимого) = "jpeg" Тогда
		Возврат Перечисления.ТипыСодержимогоФайлов.jpeg;
	ИначеЕсли НРег(СтрТипСодержимого) = "png" Тогда
		Возврат Перечисления.ТипыСодержимогоФайлов.png;
	ИначеЕсли НРег(СтрТипСодержимого) = "doc" Тогда
		Возврат Перечисления.ТипыСодержимогоФайлов.ms_word;
	ИначеЕсли НРег(СтрТипСодержимого) = "xls" Тогда
		Возврат Перечисления.ТипыСодержимогоФайлов.ms_excel;
	ИначеЕсли НРег(СтрТипСодержимого) = "odt" Тогда
		Возврат Перечисления.ТипыСодержимогоФайлов.odf_text;
	ИначеЕсли НРег(СтрТипСодержимого) = "ods" Тогда
		Возврат Перечисления.ТипыСодержимогоФайлов.odf_spreadsheet;
	ИначеЕсли НРег(СтрТипСодержимого) = "docx" Тогда
		Возврат Перечисления.ТипыСодержимогоФайлов.oxml_word;
	ИначеЕсли НРег(СтрТипСодержимого) = "xlsx" Тогда
		Возврат Перечисления.ТипыСодержимогоФайлов.oxml_spreadsheet;
	ИначеЕсли НРег(СтрТипСодержимого) = "sgn" Тогда
		Возврат Перечисления.ТипыСодержимогоФайлов.sgn;
	ИначеЕсли НРег(СтрТипСодержимого) = "unknown" Тогда
		Возврат Перечисления.ТипыСодержимогоФайлов.Неизвестный;
	Иначе
		Возврат Перечисления.ТипыСодержимогоФайлов.ПустаяСсылка();
	КонецЕсли;
	
КонецФункции

Функция ДвоичныеДанныеИзСтроки64НаСервере(Данные)
	
	Возврат Base64Значение(Данные);
	
КонецФункции

Функция ПолучитьСтруктуруРазмеровСканДокументов(СканДокументы) Экспорт
	
	
	Если ТипЗнч(СканДокументы) <> Тип("Массив")  Тогда
		МасивСканДокументов = Новый Массив;
		МасивСканДокументов.Добавить(СканДокументы);
	Иначе
		МасивСканДокументов = СканДокументы;
	КонецЕсли;
	
	Запрос = Новый Запрос("ВЫБРАТЬ РАЗРЕШЕННЫЕ
	|	ЕстьNull(СУММА(СканированныеДокументыДляПередачиВЭлектронномВидеПрисоединенныеФайлы.Размер), 0) КАК Размер,
	|	ЕстьNull(СУММА(1), 0) КАК КолвоФайлов
	|ИЗ
	|	Справочник.СканированныеДокументыДляПередачиВЭлектронномВидеПрисоединенныеФайлы КАК СканированныеДокументыДляПередачиВЭлектронномВидеПрисоединенныеФайлы
	|ГДЕ
	|	СканированныеДокументыДляПередачиВЭлектронномВидеПрисоединенныеФайлы.ВладелецФайла В(&МасивСканДокументов)");
	
	Запрос.УстановитьПараметр("МасивСканДокументов", МасивСканДокументов);
	
	Выборка = Запрос.Выполнить().Выбрать();
	
	СтруктураРезультат = Новый Структура("Размер, КолвоФайлов", 0, 0);
	
	Если Выборка.Следующий() Тогда
		ЗаполнитьЗначенияСвойств(СтруктураРезультат,Выборка); 
	КонецЕсли;
	
	Возврат СтруктураРезультат;

КонецФункции

Процедура УдалитьПрисоединенныеФайлыСканированныеДокументыДляПередачиВЭлектронномВиде(МассивФайлов) Экспорт
	
	УстановитьПривилегированныйРежим(Истина);
	
	Для каждого ПрисоединенныйФайлСсылка Из МассивФайлов Цикл
		ПрисоединенныйФайлОбъект = ПрисоединенныйФайлСсылка.ПолучитьОбъект();
		ПрисоединенныйФайлОбъект.Удалить()
	КонецЦикла;
	
КонецПроцедуры

#Область ПредупреждениеОНекорректныхСтатусахОтправки2НДФЛ

Функция ТребуетсяПредупредитьОбОшибкеВСтатусахОтправки2НДФЛ() Экспорт
	
	ТребуетсяПредупредить 	= Ложь;
	АдресОтчетов			= Неопределено;
	
	Если НеобходимоПоказатьПредупреждениеОНекорректныхСтатусахОтправки2НДФЛ() Тогда
		
		АдресОтчетов = АдресОтчетов2НДФЛСНекорректнымиСтатусами();
	
		Если АдресОтчетов = Неопределено Тогда
			НапомнитьПозжеОНекорректныхСтатусахОтправки2НДФЛ(Ложь);
		Иначе
			ТребуетсяПредупредить = Истина;
		КонецЕсли;
		
	КонецЕсли;
	
	Возврат Новый Структура("ТребуетсяПредупредить, АдресОтчетов", ТребуетсяПредупредить, АдресОтчетов);
	
КонецФункции

Функция НеобходимоПоказатьПредупреждениеОНекорректныхСтатусахОтправки2НДФЛ() Экспорт
	
	ПоказатьПредупреждение = ХранилищеОбщихНастроек.Загрузить("ИзменениеСтатуса2НДФЛСоСданоНаСданоЧастично2019_НапомнитьПозже");
	Возврат ПоказатьПредупреждение <> Ложь;
	
КонецФункции

Процедура НапомнитьПозжеОНекорректныхСтатусахОтправки2НДФЛ(Ответ) Экспорт
	
	ХранилищеОбщихНастроек.Сохранить("ИзменениеСтатуса2НДФЛСоСданоНаСданоЧастично2019_НапомнитьПозже", , Ответ);
	
КонецПроцедуры

Функция АдресОтчетов2НДФЛСНекорректнымиСтатусами()
	
	Адрес = Неопределено;
	
	Запрос = Новый Запрос;
	Запрос.Текст = 
		"ВЫБРАТЬ РАЗРЕШЕННЫЕ РАЗЛИЧНЫЕ
		|	ТранспортноеСообщение.ЦиклОбмена.Предмет КАК Отчет,
		|	МАКСИМУМ(ТранспортноеСообщение.Ссылка) КАК ТранспортноеСообщение,
		|	МАКСИМУМ(ТранспортноеСообщение.ЦиклОбмена) КАК ЦиклОбмена
		|ПОМЕСТИТЬ ПоследниеОтправки
		|ИЗ
		|	Документ.ТранспортноеСообщение КАК ТранспортноеСообщение
		|ГДЕ
		|	ТранспортноеСообщение.Тип = &РезультатПриемаФорма2НДФЛНО
		|	И ТранспортноеСообщение.ПометкаУдаления = ЛОЖЬ
		|	И ТранспортноеСообщение.ЦиклОбмена.ПометкаУдаления = ЛОЖЬ
		|	И ТранспортноеСообщение.Получатель В(&Организации)
		|
		|СГРУППИРОВАТЬ ПО
		|	ТранспортноеСообщение.ЦиклОбмена.Предмет
		|;
		|
		|////////////////////////////////////////////////////////////////////////////////
		|ВЫБРАТЬ РАЗРЕШЕННЫЕ РАЗЛИЧНЫЕ
		|	ТранспортныеСообщения.Ссылка.ДатаТранспорта КАК ДатаОтправки,
		|	ТранспортныеСообщения.ЦиклОбмена КАК ЦиклОбмена
		|ПОМЕСТИТЬ ДатыОтправки
		|ИЗ
		|	Документ.ТранспортноеСообщение КАК ТранспортныеСообщения
		|		ВНУТРЕННЕЕ СОЕДИНЕНИЕ ПоследниеОтправки КАК ПоследниеОтправки
		|		ПО (ПоследниеОтправки.ЦиклОбмена = ПоследниеОтправки.ТранспортноеСообщение.ЦиклОбмена)
		|ГДЕ
		|	ТранспортныеСообщения.Ссылка.Тип = &Форма2НДФЛНП
		|	И ТранспортныеСообщения.ПометкаУдаления = ЛОЖЬ
		|	И ТранспортныеСообщения.ЦиклОбмена.ПометкаУдаления = ЛОЖЬ
		|	И ТранспортныеСообщения.Отправитель В(&Организации)
		|;
		|
		|////////////////////////////////////////////////////////////////////////////////
		|ВЫБРАТЬ
		|	ЖурналОтчетовСтатусы.Организация,
		|	ЖурналОтчетовСтатусы.НаименованиеОтчета КАК Представление,
		|	ПоследниеОтправки.Отчет,
		|	ЖурналОтчетовСтатусы.Статус КАК Состояние,
		|	ЖурналОтчетовСтатусы.СостояниеСдачиОтчетности КАК СостояниеСдачиОтчетности,
		|	ДатыОтправки.ДатаОтправки КАК ДатаОтправки,
		|	ПоследниеОтправки.ТранспортноеСообщение КАК Протокол,
		|	ПоследниеОтправки.ЦиклОбмена КАК ЦиклОбмена,
		|	""Протокол ошибок"" КАК НаименованиеПротокола
		|ИЗ
		|	ПоследниеОтправки КАК ПоследниеОтправки
		|		ВНУТРЕННЕЕ СОЕДИНЕНИЕ РегистрСведений.ЖурналОтчетовСтатусы КАК ЖурналОтчетовСтатусы
		|		ПО ПоследниеОтправки.Отчет = ЖурналОтчетовСтатусы.Ссылка
		|		ВНУТРЕННЕЕ СОЕДИНЕНИЕ ДатыОтправки КАК ДатыОтправки
		|		ПО (ДатыОтправки.ЦиклОбмена = ПоследниеОтправки.ЦиклОбмена)
		|ГДЕ
		|	(ЖурналОтчетовСтатусы.Статус = ""Сдан частично"" ИЛИ ЖурналОтчетовСтатусы.Статус = ""Не сдано"")
		|	И ПоследниеОтправки.ТранспортноеСообщение.Тип = &РезультатПриемаФорма2НДФЛНО
		|	И ЖурналОтчетовСтатусы.ДатаОтправки >= ДАТАВРЕМЯ(2019, 1, 1, 0, 0, 0)
		|	И ЖурналОтчетовСтатусы.Организация В (&Организации)";
	
	
	Запрос.УстановитьПараметр("РезультатПриемаФорма2НДФЛНО", 	Перечисления.ТипыТранспортныхСообщений.РезультатПриемаФорма2НДФЛНО);
	Запрос.УстановитьПараметр("Форма2НДФЛНП", 					Перечисления.ТипыТранспортныхСообщений.Форма2НДФЛНП);
	Запрос.УстановитьПараметр("Организации", 					ОрганизацииПоУчетнымЗаписямПользователя());
	
	Отчеты2НДФЛ = Запрос.Выполнить().Выгрузить();
	
	ОтчетыСОшибками = Отчеты2НДФЛ.Скопировать(); // Чтобы скопировать колонки
	ОтчетыСОшибками.Очистить(); // Чтобы скопировать колонки
	
	Для каждого Отчет Из Отчеты2НДФЛ Цикл
		Если ЭтоПротокол2НДФЛ_КНД1110307(Отчет.Протокол) Тогда
			НоваяСтрока = ОтчетыСОшибками.Добавить();
			ЗаполнитьЗначенияСвойств(НоваяСтрока, Отчет); 
		КонецЕсли;
	КонецЦикла; 
	
	Если ОтчетыСОшибками.Количество() > 0 Тогда
		Адрес = ПоместитьВоВременноеХранилище(ОтчетыСОшибками, Новый УникальныйИдентификатор);
	КонецЕсли;
	
	Возврат Адрес;
	
КонецФункции

Процедура ИсправитьНекорректныеСостоянияОтправок2НДФЛ() Экспорт
	
	Запрос = Новый Запрос;
	Запрос.Текст = 
		"ВЫБРАТЬ РАЗРЕШЕННЫЕ РАЗЛИЧНЫЕ
		|	ТранспортноеСообщение.Ссылка КАК ТранспортноеСообщение,
		|	ТранспортноеСообщение.ЦиклОбмена КАК ЦиклОбмена,
		|	ТранспортноеСообщение.ЦиклОбмена.Предмет КАК Отчет,
		|	ТранспортноеСообщение.ЦиклОбмена.Организация КАК Организация,
		|	ЛОЖЬ КАК ТребуетсяКорректировкаСтатуса
		|ИЗ
		|	Документ.ТранспортноеСообщение КАК ТранспортноеСообщение
		|ГДЕ
		|	ТранспортноеСообщение.ПротоколСОшибкой = ЛОЖЬ
		|	И ТранспортноеСообщение.Тип = &Тип";
	
	Запрос.УстановитьПараметр("Тип", Перечисления.ТипыТранспортныхСообщений.РезультатПриемаФорма2НДФЛНО);
	
	РезультатПроверкиСостоянийОтчетов = Запрос.Выполнить();
	Сообщения = РезультатПроверкиСостоянийОтчетов.Выгрузить();
	
	Для каждого Строка Из Сообщения Цикл
	    УстановитьКорректноеЗначениеОшибкиПротокола2НДФЛ(Строка);
	КонецЦикла;
	
	Отказ = Ложь;
	// Статусы отчета корректируем отдельно от записи транспортного сообщения, так как на один отчет может быть 
	// несколько транспортных сообщений одного вида.
	Для каждого Строка Из Сообщения Цикл
		Если Строка.ТребуетсяКорректировкаСтатуса Тогда
			// Корректировка регистра ЖурналОтчетовСтатусы 
			УстановитьКорректныеСтатусыОтчетов2НДФЛ(Строка.Отчет, Строка.Организация);
			// Корректировка регистра СтатусыОтправки
			ОбновитьСтатусыСвязанногоОбъектаПриНеобходимости(Строка.ЦиклОбмена, Истина);
		КонецЕсли;
	КонецЦикла;
	
КонецПроцедуры

Процедура УстановитьКорректныеСтатусыОтчетов2НДФЛ(Отчет, Организация)
	
	НачатьТранзакцию();
	
	Попытка
		
		ВидКонтролирующегоОргана 	= Перечисления.ТипыКонтролирующихОрганов.ФНС;
		СтатусОтправки 				= Неопределено;
		СостояниеСдачиОтчетности 	= Неопределено;
		
		ЭлектронныйДокументооборотСКонтролирующимиОрганами.ОпределитьСтатусИСостояниеСдачиОтчетности(
			Отчет, Организация, ВидКонтролирующегоОргана, СостояниеСдачиОтчетности, СтатусОтправки);
		
		СтруктураКлюча = Новый Структура("Ссылка", Отчет);
		Ключ = РегистрыСведений.ЖурналОтчетовСтатусы.СоздатьКлючЗаписи(СтруктураКлюча);
		ЗаблокироватьДанныеДляРедактирования(Ключ);
		
		Блокировка = Новый БлокировкаДанных;
		ЭлементБлокировки = Блокировка.Добавить("РегистрСведений.ЖурналОтчетовСтатусы");
		ЭлементБлокировки.УстановитьЗначение("Ссылка", Отчет);
		Блокировка.Заблокировать();
		
		НаборЗаписей = РегистрыСведений.ЖурналОтчетовСтатусы.СоздатьНаборЗаписей();
		НаборЗаписей.Отбор.Ссылка.Установить(Отчет);
		НаборЗаписей.Прочитать();
		
		Для каждого Запись из НаборЗаписей Цикл
			Запись.СостояниеСдачиОтчетности = СостояниеСдачиОтчетности;
			Запись.Статус                   = СтатусОтправки;
		КонецЦикла;
		
		НаборЗаписей.Записать();
		
		ЗафиксироватьТранзакцию();
		
	Исключение
		
		ОтменитьТранзакцию();
		
		ИнформацияОбОшибке = ИнформацияОбОшибке();
		
		ЗаписьЖурналаРегистрации(
			НСтр("ru = 'Корректировка состояния отправки 2-НДФЛ'", ОбщегоНазначения.КодОсновногоЯзыка()), 
			УровеньЖурналаРегистрации.Ошибка,,,
			ПодробноеПредставлениеОшибки(ИнформацияОбОшибке));
			
	КонецПопытки;
	
КонецПроцедуры

Процедура УстановитьКорректноеЗначениеОшибкиПротокола2НДФЛ(Строка)
	
	ТранспортноеСообщение = Строка.ТранспортноеСообщение;
	
	Если ЯвляетсяОтрицательнымПротоколом(ТранспортноеСообщение) <> Ложь Тогда

		ТранспортноеСообщениеОбъект = ТранспортноеСообщение.ПолучитьОбъект();
		ТранспортноеСообщениеОбъект.ПротоколСОшибкой = Истина;
		ТранспортноеСообщениеОбъект.Записать();
		
		Строка.ТребуетсяКорректировкаСтатуса = Истина;
		
	КонецЕсли;
	
КонецПроцедуры

Процедура ИсправитьНекорректныеСостоянияОтправок2НДФЛ_2() Экспорт
	
	// Ранее в протоколе указывалась строка
	// КОЛИЧЕСТВО ПРИНЯТЫХ СВЕДЕНИЙ
	// теперь строка:
	// КОЛИЧЕСТВО СВЕДЕНИЙ, ГОТОВЫХ К ПРИЕМУ
	// Из-за этого некорректно отпределились статусы как Сдан частично, хотя отчет здан полностью.
	
	Запрос = Новый Запрос;
	Запрос.Текст = 
		"ВЫБРАТЬ РАЗРЕШЕННЫЕ РАЗЛИЧНЫЕ
		|	ТранспортноеСообщение.ЦиклОбмена.Предмет КАК Отчет
		|ПОМЕСТИТЬ Отчеты2НДФЛ
		|ИЗ
		|	Документ.ТранспортноеСообщение КАК ТранспортноеСообщение
		|ГДЕ
		|	ТранспортноеСообщение.Тип = ЗНАЧЕНИЕ(Перечисление.ТипыТранспортныхСообщений.РезультатПриемаФорма2НДФЛНО)
		|	И ТранспортноеСообщение.ПометкаУдаления = ЛОЖЬ
		|;
		|
		|////////////////////////////////////////////////////////////////////////////////
		|ВЫБРАТЬ
		|	ЖурналОтчетовСтатусы.Ссылка КАК Ссылка,
		|	ЖурналОтчетовСтатусы.Организация КАК Организация
		|ИЗ
		|	РегистрСведений.ЖурналОтчетовСтатусы КАК ЖурналОтчетовСтатусы
		|ГДЕ
		|	ЖурналОтчетовСтатусы.Статус = ""Сдан частично""
		|	И ЖурналОтчетовСтатусы.ДатаНачала >= ДАТАВРЕМЯ(2019, 1, 1, 0, 0, 0)
		|	И ЖурналОтчетовСтатусы.Ссылка В
		|			(ВЫБРАТЬ
		|				Отчеты2НДФЛ.Отчет
		|			ИЗ
		|				Отчеты2НДФЛ)";
	
	РезультатЗапроса = Запрос.Выполнить();
	
	ВыборкаДетальныеЗаписи = РезультатЗапроса.Выбрать();
	
	Пока ВыборкаДетальныеЗаписи.Следующий() Цикл
		
		Отчет       = ВыборкаДетальныеЗаписи.Ссылка; 
		Организация = ВыборкаДетальныеЗаписи.Организация;
		
		ЦиклОбмена = ДокументооборотСКОВызовСервера.ПолучитьПоследнийЦиклОбмена(Отчет);
		
		Если ЦиклОбмена = Неопределено ИЛИ ЦиклОбмена.Тип <> Перечисления.ТипыЦикловОбмена.Форма2НДФЛ Тогда
			Продолжить;
		КонецЕсли;
		
		СообщенияЦиклаОбмена = ПолучитьСообщенияЦиклаОбмена(ЦиклОбмена);
		ПротоколыПриема      = СообщенияЦиклаОбмена.НайтиСтроки(Новый Структура("Тип", Перечисления.ТипыТранспортныхСообщений.РезультатПриемаФорма2НДФЛНО));
		
		НаиболееПозднийПротокол = Неопределено;
		ДатаНаиболееПозднегоПротокола = '00010101';
		Для Каждого ПротоколПриема Из ПротоколыПриема Цикл
			ДатаПротоколаПриема = ПротоколПриема.Дата;
			Если ДатаПротоколаПриема = Неопределено Тогда
				ДатаПротоколаПриема = '00010101';
			КонецЕсли;
			Если ДатаПротоколаПриема >= ДатаНаиболееПозднегоПротокола Тогда
				ДатаНаиболееПозднегоПротокола = ДатаПротоколаПриема;
				НаиболееПозднийПротокол = ПротоколПриема;
			КонецЕсли;
		КонецЦикла;
		
		СданЧастично = Отчет2НДФЛСданЧастично(НаиболееПозднийПротокол.Ссылка);
		Если НЕ СданЧастично Тогда
			
			УстановитьКорректныеСтатусыОтчетов2НДФЛ(Отчет, Организация);
			
			ТранспортноеСообщениеОбъект = НаиболееПозднийПротокол.Ссылка.ПолучитьОбъект();
			ТранспортноеСообщениеОбъект.ПротоколСОшибкой = Ложь;
			ТранспортноеСообщениеОбъект.Записать();
			
			// Корректировка регистра СтатусыОтправки
			ОбновитьСтатусыСвязанногоОбъектаПриНеобходимости(ЦиклОбмена, Истина);
			
		КонецЕсли
		
	КонецЦикла;
	
КонецПроцедуры

Процедура ИсправитьНекорректныеСостоянияОтправок2НДФЛ_1110307() Экспорт
	
	Запрос = Новый Запрос;
	Запрос.Текст = 
		"ВЫБРАТЬ РАЗРЕШЕННЫЕ РАЗЛИЧНЫЕ
		|	ТранспортноеСообщение.ЦиклОбмена.Предмет КАК Отчет
		|ПОМЕСТИТЬ Отчеты2НДФЛ
		|ИЗ
		|	Документ.ТранспортноеСообщение КАК ТранспортноеСообщение
		|ГДЕ
		|	ТранспортноеСообщение.Тип = ЗНАЧЕНИЕ(Перечисление.ТипыТранспортныхСообщений.РезультатПриемаФорма2НДФЛНО)
		|	И ТранспортноеСообщение.ПометкаУдаления = ЛОЖЬ
		|;
		|
		|////////////////////////////////////////////////////////////////////////////////
		|ВЫБРАТЬ
		|	ЖурналОтчетовСтатусы.Ссылка КАК Ссылка,
		|	ЖурналОтчетовСтатусы.Организация КАК Организация
		|ИЗ
		|	РегистрСведений.ЖурналОтчетовСтатусы КАК ЖурналОтчетовСтатусы
		|ГДЕ
		|	ЖурналОтчетовСтатусы.Статус = ""Сдано""
		|	И ЖурналОтчетовСтатусы.ДатаОтправки >= ДАТАВРЕМЯ(2019, 1, 1, 0, 0, 0)
		|	И ЖурналОтчетовСтатусы.Ссылка В
		|			(ВЫБРАТЬ
		|				Отчеты2НДФЛ.Отчет
		|			ИЗ
		|				Отчеты2НДФЛ)";
	
	РезультатЗапроса = Запрос.Выполнить();
	
	ВыборкаДетальныеЗаписи = РезультатЗапроса.Выбрать();
	
	Пока ВыборкаДетальныеЗаписи.Следующий() Цикл
		
		Отчет       = ВыборкаДетальныеЗаписи.Ссылка; 
		Организация = ВыборкаДетальныеЗаписи.Организация;
		
		ЦиклОбмена = ДокументооборотСКОВызовСервера.ПолучитьПоследнийЦиклОбмена(Отчет);
		
		Если ЦиклОбмена = Неопределено ИЛИ ЦиклОбмена.Тип <> Перечисления.ТипыЦикловОбмена.Форма2НДФЛ Тогда
			Продолжить;
		КонецЕсли;
		
		СообщенияЦиклаОбмена = ПолучитьСообщенияЦиклаОбмена(ЦиклОбмена);
		ПротоколыПриема      = СообщенияЦиклаОбмена.НайтиСтроки(Новый Структура("Тип", Перечисления.ТипыТранспортныхСообщений.РезультатПриемаФорма2НДФЛНО));
		
		НаиболееПозднийПротокол = Неопределено;
		ДатаНаиболееПозднегоПротокола = '00010101';
		Для Каждого ПротоколПриема Из ПротоколыПриема Цикл
			ДатаПротоколаПриема = ПротоколПриема.Дата;
			Если ДатаПротоколаПриема = Неопределено Тогда
				ДатаПротоколаПриема = '00010101';
			КонецЕсли;
			Если ДатаПротоколаПриема >= ДатаНаиболееПозднегоПротокола Тогда
				ДатаНаиболееПозднегоПротокола = ДатаПротоколаПриема;
				НаиболееПозднийПротокол = ПротоколПриема;
			КонецЕсли;
		КонецЦикла;
		
		ТранспортноеСообщение = НаиболееПозднийПротокол.Ссылка;
		Если ЭтоПротокол2НДФЛ_КНД1110307(ТранспортноеСообщение) Тогда
			
			Сведения = СведенияИзПротокола2НДФЛ_1110307(ТранспортноеСообщение);
			
			Если Сведения.Представленные <> Сведения.Принятые Тогда
				РаспознатьТранспортноеСообщение(ТранспортноеСообщение, Истина);
				
				// Корректировка регистра СтатусыОтправки
				ОбновитьСтатусыСвязанногоОбъектаПриНеобходимости(ЦиклОбмена, Истина);
			КонецЕсли;
			
		КонецЕсли;
		
	КонецЦикла;
	
КонецПроцедуры

Функция ЭтоПротокол2НДФЛ_КНД1110307(ТранспортноеСообщение) Экспорт
	
	// Приказ ФНС России от 02.10.2018 N ММВ-7-11/566@
	
	Результат = СведенияИзПротокола2НДФЛ_1110307(ТранспортноеСообщение);
	
	Возврат Результат.Представленные > 0
		ИЛИ Результат.ПринятыхНаОбработку > 0
		ИЛИ Результат.СправокСОшибками > 0
		ИЛИ Результат.Принятые > 0;
	
КонецФункции

Функция СведенияИзПротокола2НДФЛ_1110307(ТранспортноеСообщение)
	
	// Приказ ФНС России от 02.10.2018 N ММВ-7-11/566@
	
	Результат = Новый Структура();
	Результат.Вставить("Представленные", 0);
	Результат.Вставить("ПринятыхНаОбработку", 0);
	Результат.Вставить("СправокСОшибками", 0);
	Результат.Вставить("Принятые", 0);
	
	// ищем протокол 2-НДФЛ среди содержимого сообщения
	Попытка 
		Протоколы2НДФЛ = ПолучитьВложенияТранспортногоСообщения(
			ТранспортноеСообщение, 
			Истина, 
			Перечисления.ТипыСодержимогоТранспортногоКонтейнера.Протокол2НДФЛ);
	Исключение
		ИнформацияОбОшибке = ИнформацияОбОшибке();
			ЗаписьЖурналаРегистрации(
				НСтр("ru = 'Электронный документооборот с контролирующими органами. Обновление статуса 2-НДФЛ'"),
				УровеньЖурналаРегистрации.Ошибка,,, ПодробноеПредставлениеОшибки(ИнформацияОбОшибке));
		Возврат Результат;
	КонецПопытки;
		
	КоличествоПротоколов = Протоколы2НДФЛ.Количество();
	Если КоличествоПротоколов = 0 Тогда
		Возврат Результат;
	Иначе
		Протокол2НДФЛ = Протоколы2НДФЛ[0];
	КонецЕсли;
	
	// загружаем протокол из файла в строку
	ТекстОшибки = "";
	ТекстПротокола = ТекстИзФайлаВСтроку(Протокол2НДФЛ, ТекстОшибки);
	Если НЕ ЗначениеЗаполнено(ТекстПротокола) Тогда
		Возврат Результат;
	КонецЕсли;
	
	ТекстПротокола = ВРег(ТекстПротокола);
	
	Количество = КоличествоСведенийВПротоколе2НДФЛ(ТекстПротокола, "КОЛИЧЕСТВО СПРАВОК, ПРЕДСТАВЛЕННЫХ НАЛОГОВЫМ АГЕНТОМ");
	Результат.Вставить("Представленные", Количество);	
	
	Количество = КоличествоСведенийВПротоколе2НДФЛ(ТекстПротокола, "КОЛИЧЕСТВО ПРИНЯТЫХ НА ОБРАБОТКУ СПРАВОК");
	Результат.Вставить("ПринятыхНаОбработку", Количество);	
	
	Количество = КоличествоСведенийВПротоколе2НДФЛ(ТекстПротокола, "КОЛИЧЕСТВО СПРАВОК С ОШИБКАМИ");
	Результат.Вставить("СправокСОшибками", Количество);	
	
	Количество = КоличествоСведенийВПротоколе2НДФЛ(ТекстПротокола, "КОЛИЧЕСТВО ПРИНЯТЫХ СПРАВОК");
	Результат.Вставить("Принятые", Количество);
	
	Возврат Результат;
	
КонецФункции

#КонецОбласти

#Область ЛичныеКабинеты

Процедура ЗаполнитьПараметрыСпецоператора(Знач Спецоператор, ПараметрыСтруктура) Экспорт
	
	ПараметрыСпецоператора = ПолучитьПараметрыОператораСвязи(Спецоператор);
	ЗаполнитьЗначенияСвойств(ПараметрыСтруктура, ПараметрыСпецоператора);
	
КонецПроцедуры

Функция ПолучитьМассивИспользуемыхСпецоператоров() Экспорт
	
	МассивРезультат = Новый Массив;
	
	Запрос = Новый Запрос("ВЫБРАТЬ РАЗРЕШЕННЫЕ РАЗЛИЧНЫЕ
	                      |	УчетныеЗаписиДокументооборота.СпецоператорСвязи
	                      |ИЗ
	                      |	Справочник.Организации КАК Организации
	                      |		ЛЕВОЕ СОЕДИНЕНИЕ Справочник.УчетныеЗаписиДокументооборота КАК УчетныеЗаписиДокументооборота
	                      |		ПО Организации.УчетнаяЗаписьОбмена = УчетныеЗаписиДокументооборота.Ссылка
	                      |ГДЕ
	                      |	Организации.ВидОбменаСКонтролирующимиОрганами = &ВидОбменаСКонтролирующимиОрганами
	                      |	И УчетныеЗаписиДокументооборота.СпецоператорСвязи <> ЗНАЧЕНИЕ(Перечисление.СпецоператорыСвязи.ПустаяСсылка)
						  |	И НЕ Организации.ПометкаУдаления
	                      |	И НЕ УчетныеЗаписиДокументооборота.ПометкаУдаления");
						  
	Запрос.УстановитьПараметр("ВидОбменаСКонтролирующимиОрганами", Перечисления.ВидыОбменаСКонтролирующимиОрганами.ОбменВУниверсальномФормате);
	
	Выборка = Запрос.Выполнить().Выбрать();
	
	Пока Выборка.Следующий() Цикл
		МассивРезультат.Добавить(Выборка.СпецоператорСвязи);
	КонецЦикла; 
	
	Возврат МассивРезультат;
	
КонецФункции

Функция ПолучитьТаблицуИспользуемыхСпецоператоровУчетныхЗаписейОрганизаций() Экспорт
	
	Запрос = Новый Запрос("ВЫБРАТЬ РАЗРЕШЕННЫЕ РАЗЛИЧНЫЕ
	                      |	Организации.Ссылка КАК Организация,
	                      |	УчетныеЗаписиДокументооборота.Ссылка КАК УчетнаяЗапись,
	                      |	УчетныеЗаписиДокументооборота.СпецоператорСвязи КАК Спецоператор,
	                      |	РегистрацииВНалоговомОргане.Код КАК КодНО
	                      |ИЗ
	                      |	Справочник.Организации КАК Организации
	                      |		ЛЕВОЕ СОЕДИНЕНИЕ Справочник.УчетныеЗаписиДокументооборота КАК УчетныеЗаписиДокументооборота
	                      |		ПО Организации.УчетнаяЗаписьОбмена = УчетныеЗаписиДокументооборота.Ссылка
	                      |		ЛЕВОЕ СОЕДИНЕНИЕ Справочник.РегистрацииВНалоговомОргане КАК РегистрацииВНалоговомОргане
	                      |		ПО Организации.РегистрацияВНалоговомОргане = РегистрацииВНалоговомОргане.Ссылка
	                      |ГДЕ
	                      |	Организации.ВидОбменаСКонтролирующимиОрганами = &ВидОбменаСКонтролирующимиОрганами
	                      |	И УчетныеЗаписиДокументооборота.СпецоператорСвязи <> ЗНАЧЕНИЕ(Перечисление.СпецоператорыСвязи.ПустаяСсылка)
						  |	И НЕ Организации.ПометкаУдаления
	                      |	И НЕ УчетныеЗаписиДокументооборота.ПометкаУдаления");
						  
	Запрос.УстановитьПараметр("ВидОбменаСКонтролирующимиОрганами", Перечисления.ВидыОбменаСКонтролирующимиОрганами.ОбменВУниверсальномФормате);
	
	Результат = Запрос.Выполнить();
	
	Возврат Результат.Выгрузить();
	
КонецФункции

Процедура ПриЗаписиОтветаНаТребованиеФНС(Объект, Отказ) Экспорт
	
	Если Объект.Ссылка <> Неопределено Тогда
		
		Если ТипЗнч(Объект.Ссылка) = Тип("СправочникСсылка.ОписиИсходящихДокументовВНалоговыеОрганы") Тогда 
			Основание = Объект.Ссылка.Основание;
		Иначе
			Основание = Объект.Ссылка.Требование;
		КонецЕсли;
		
		Если ТипЗнч(Основание) = Тип("СправочникСсылка.ДокументыРеализацииПолномочийНалоговыхОрганов") Тогда
	
			// Ответы
			Результат 			= ПолучитьКоличествоОтветовНаТребования(Основание);
			КоличествоОтветов 	= Результат.Получить(Основание);
			
			ЭлектронныйДокументооборотСКонтролирующимиОрганамиВызовСервера.ИзменитьЗначениеВФорме1СОтчетность(
				Основание, 
				"ЕстьОтвет", 
				КоличествоОтветов <> Неопределено И КоличествоОтветов > 0);
				
		КонецЕсли;
		
	КонецЕсли;
	
КонецПроцедуры

Процедура ЗаписатьПоясненияКДекларацииПоНДСВРегистрЖурналОтправокВКонтролирующиеОрганы(Объект) Экспорт

	Свойства = СвойстваОтветаНаТребованиеПоясненийКДекларацииНДС(Объект);
	ЭлектронныйДокументооборотСКонтролирующимиОрганами.СоздатьЗаписьВРегистреЖурналОтправокВКонтролирующиеОрганы(Свойства);
	
КонецПроцедуры

Функция СвойстваОтветаНаТребованиеПоясненийКДекларацииНДС(Объект) Экспорт

	Ссылка = Объект.Ссылка;
	
	// Наименование
	Требование = Объект.Требование;
	Наименование = Документы.ПоясненияКДекларацииПоНДС.НаименованиеОтветаПоТребованию(Объект.Требование);
	
	// Вид и код контролирующего органа
	ВидКонтролирующегоОргана	= Перечисления.ТипыКонтролирующихОрганов.ФНС;
	Если ЗначениеЗаполнено(Объект.НалоговыйОрган) Тогда
		КодКонтролирующегоОргана	= Объект.НалоговыйОрган.Код;
	Иначе
		КодКонтролирующегоОргана	= "";
	КонецЕсли;
	
	Организация = Объект.Организация;
	
	// Запись в регистр
	ЗначенияЗаполнения = ЭлектронныйДокументооборотСКонтролирующимиОрганами.ПараметрыПроцедурыСоздатьЗаписьВРегистреЖурналОтправокВКонтролирующиеОрганы();
	ЗначенияЗаполнения.Ссылка 					= Ссылка;
	ЗначенияЗаполнения.ДатаСоздания				= Ссылка.Дата;
	ЗначенияЗаполнения.Наименование 			= Наименование;
	ЗначенияЗаполнения.ВидКонтролирующегоОргана = ВидКонтролирующегоОргана;
	ЗначенияЗаполнения.КодКонтролирующегоОргана = КодКонтролирующегоОргана;
	ЗначенияЗаполнения.Организация 				= Организация;
	ЗначенияЗаполнения.СтраницаЖурнала			= Перечисления.СтраницыЖурналаОтчетность.Письма;
	ЗначенияЗаполнения.Комментарий				= Ссылка.Комментарий;
	
	Возврат ЗначенияЗаполнения;
	
КонецФункции

Функция ОпределитьОрганизациюПоИННиКПП(ИНН, КПП, НаходитьПомеченныеНаУдаление = Ложь) Экспорт
	
	Запрос = Новый Запрос("ВЫБРАТЬ РАЗРЕШЕННЫЕ ПЕРВЫЕ 1
	                      |	Организации.Ссылка
	                      |ИЗ
	                      |	Справочник.Организации КАК Организации");
	
	Если НЕ ЗначениеЗаполнено(КПП) Тогда
		Запрос.Текст = Запрос.Текст + "	ГДЕ ";
	Иначе
		Запрос.Текст = Запрос.Текст + "
							|		ВНУТРЕННЕЕ СОЕДИНЕНИЕ Справочник.РегистрацииВНалоговомОргане КАК РегистрацииВНалоговомОргане
							|		ПО Организации.Ссылка = РегистрацииВНалоговомОргане.Владелец
							|	ГДЕ РегистрацииВНалоговомОргане.КПП = &КПП
							|		И ";
		Запрос.УстановитьПараметр("КПП", КПП);
	КонецЕсли;
	
	Запрос.Текст = Запрос.Текст + " Организации.ИНН = &ИНН"
		+ ?(НаходитьПомеченныеНаУдаление, "", " И Организации.ПометкаУдаления = ЛОЖЬ");
	Запрос.УстановитьПараметр("ИНН", ИНН);
	
	РезультатЗапроса = Запрос.Выполнить();
	Если НЕ РезультатЗапроса.Пустой() Тогда
		Выборка = РезультатЗапроса.Выбрать();
		Если Выборка.Следующий() Тогда
			Возврат Выборка.Ссылка;
		КонецЕсли;
	КонецЕсли;
	Возврат Справочники.Организации.ПустаяСсылка();
	
КонецФункции

#КонецОбласти

#Область ЗагрузкаТребованиеОПредставленииПоясненийКДекларацииНДС

Функция ЭтоРешениеПоЖалобе(Требование) Экспорт

	Возврат Требование.ВидДокумента = Перечисления.ВидыНалоговыхДокументов.РешениеПоЖалобе;

КонецФункции

Функция НоваяОписьВходящихДокументовИзНалоговыхОрганов(
		СвойстваОписиВходящихДокументов,
		ВложенияПриложение,
		ТранспортноеСообщение) Экспорт
		
	ТаблицаДокументов 		= СвойстваОписиВходящихДокументов.ТаблицаДокументов;
	ИдентификаторОписи 		= СвойстваОписиВходящихДокументов.Идентификатор;
	ИдентификаторОснования 	= СвойстваОписиВходящихДокументов.ИдентификаторОснования;
	
	Если СвойстваОписиВходящихДокументов.Свойство("Подписант") Тогда
		Подписант = СвойстваОписиВходящихДокументов.Подписант;
	Иначе
		Подписант = Неопределено;
	КонецЕсли;
	
	ЦиклОбмена 		= ТранспортноеСообщение.ЦиклОбмена; 
	ДатаТранспорта 	= ТранспортноеСообщение.Дата; // Это дата отправки почтового сообщения
	
	// создаем документ опись входящих документов в ИБ
	ОписьДокументов = Справочники.ОписиВходящихДокументовИзНалоговыхОрганов.СоздатьЭлемент();
	ОписьДокументов.НалоговыйОрган 			= ЦиклОбмена.ВнешняяОрганизация;
	ОписьДокументов.Организация 			= ЦиклОбмена.Организация;
	ОписьДокументов.ДатаСообщения 			= ТекущаяДатаСеанса();
	ОписьДокументов.ДатаОтправки 			= ДатаТранспорта;
	ОписьДокументов.Идентификатор 			= ИдентификаторОписи;
	ОписьДокументов.ИдентификаторОснования 	= ИдентификаторОснования;

	ДокументОснование = ОснованиеДокументаРеализацииПолномочий(ИдентификаторОснования);

	// создаем документы реализации полномочий в ИБ
	Для каждого СтрокаДокумент Из ТаблицаДокументов Цикл
		
		РеквизитыДокумента = СтрокаДокумент.РеквизитыДокумента;
		СписокИменФайловВложений = СтрокаДокумент.СписокИменФайловВложений;
		
		Документ = Справочники.ДокументыРеализацииПолномочийНалоговыхОрганов.СоздатьЭлемент();
		ДокументСсылка = Справочники.ДокументыРеализацииПолномочийНалоговыхОрганов.ПолучитьСсылку();
		Документ.УстановитьСсылкуНового(ДокументСсылка);
		
		Документ.ВидДокумента 		= ПолучитьВидНалоговогоДокументаПоКНД(РеквизитыДокумента.ВидДокумента);
		Документ.ДатаДокумента	 	= РеквизитыДокумента.ДатаДокумента;
		Документ.НомерДокумента 	= СокрЛП(СтрЗаменить(РеквизитыДокумента.НомерДокумента, "номер", ""));
		Документ.НалоговыйОрган 	= ЦиклОбмена.ВнешняяОрганизация;
		Документ.Организация 		= ЦиклОбмена.Организация;
		Документ.ДокументОснование 	= ДокументОснование;
		
		Если Подписант <> Неопределено Тогда
			Документ.ПодписантДолжность = Подписант.ПодписантДолжность;
			Документ.ПодписантТелефон 	= Подписант.ПодписантТелефон;
			Документ.ПодписантПочта 	= Подписант.ПодписантПочта;
			Документ.ПодписантФИО 		= Подписант.ПодписантФИО;
		КонецЕсли;

		// добавляем вложения документа
		ВложенияДанногоТребования = Новый Массив;
		
		Для Каждого Приложение Из ВложенияПриложение Цикл
			
			Если СписокИменФайловВложений.НайтиПоЗначению(ИмяФайлаБезРасширения(Приложение.ИмяФайла)) <> Неопределено
				ИЛИ СписокИменФайловВложений.НайтиПоЗначению(Приложение.ИмяФайла) <> Неопределено Тогда
				
				УстановитьПривилегированныйРежим(Истина);
				
				// Корректируем номер требования
				Если Приложение.ТипСодержимогоФайла = Перечисления.ТипыСодержимогоФайлов.XML Тогда
					
					Если Документ.ВидДокумента = Перечисления.ВидыНалоговыхДокументов.РешениеПоЖалобе Тогда
						ЗаполнитьРеквизитыРешенияПоЖалобе(Приложение, Документ);
					Иначе
						НомерДокумента = НомерТребованияИзXMLВложения(Приложение);
						Если ЗначениеЗаполнено(НомерДокумента) Тогда
							Документ.НомерДокумента = НомерДокумента;
						КонецЕсли;
					КонецЕсли;
					
				КонецЕсли;
				
				ТипСодержимого = ТипСодержимогоПоИмениФайла(Приложение.ИмяФайла);
				
				ВидРешенияНаЖалобу = ВидРешенияНаЖалобу(ВложенияПриложение, Документ, Приложение.ИмяФайла);
				
				ДобавитьВложенияДокумента(
					ДокументСсылка, 
					Приложение.ИмяФайла, 
					Приложение.Данные, 
					ВидРешенияНаЖалобу,
					ТипСодержимого, 
					Приложение.Размер);
					
				УстановитьПривилегированныйРежим(Ложь);
				
				ВложенияДанногоТребования.Добавить(Приложение);
				
			КонецЕсли;	
		КонецЦикла;
		
		Документ.Наименование = Справочники.ДокументыРеализацииПолномочийНалоговыхОрганов.НаименованиеДокументаРеализацииПолномочий(Документ, ВложенияДанногоТребования);
		
		Попытка
			Документ.Записать();
		Исключение
			ТекстСообщения = СтроковыеФункцииКлиентСервер.ПодставитьПараметрыВСтроку(
			НСтр("ru = 'Ошибка записи входящего письма от ФНС:%1'"), Символы.ПС + ИнформацияОбОшибке().Описание);
			ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
			Возврат Неопределено;
		КонецПопытки;
		
		НоваяСтрокаВходящийДокумент = ОписьДокументов.ВходящиеДокументы.Добавить();
		НоваяСтрокаВходящийДокумент.СсылкаНаОбъект = Документ.Ссылка;
		
	КонецЦикла;
	
	Попытка
		ОписьДокументов.Записать();
	Исключение
		ОтменитьТранзакцию();
		ТекстСообщения = СтроковыеФункцииКлиентСервер.ПодставитьПараметрыВСтроку(
			НСтр("ru = 'Ошибка записи документа налогового органа:
                  |%1'"), КраткоеПредставлениеОшибки(ИнформацияОбОшибке()));
		ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
		Возврат Неопределено;
	КонецПопытки;
	
	Если НЕ ЭлектронныйДокументооборотСКонтролирующимиОрганами.ОбновитьСвойстваЦиклаОбмена(ЦиклОбмена, ОписьДокументов.Ссылка) Тогда
		Если ТранзакцияАктивна() Тогда 
			ОтменитьТранзакцию();
		КонецЕсли;
		Возврат Неопределено;
	КонецЕсли;
	
	Возврат ОписьДокументов;
			
КонецФункции

Функция ВидыРешенийНаЖалобу()
	
	ВидыРешенийНаЖалобу = Новый Соответствие;
	ВидыРешенийНаЖалобу.Вставить("1", Перечисления.ВидыРешенийНаЖалобу.РешениеПоЖалобе);
	ВидыРешенийНаЖалобу.Вставить("2", Перечисления.ВидыРешенийНаЖалобу.РешениеОПродленииСрокаРассмотренияЖалобы);
	ВидыРешенийНаЖалобу.Вставить("3", Перечисления.ВидыРешенийНаЖалобу.РешениеОПриостановлении);
	ВидыРешенийНаЖалобу.Вставить("4", Перечисления.ВидыРешенийНаЖалобу.РешениеОбОтказеВПриостановлении);
	ВидыРешенийНаЖалобу.Вставить("5", Перечисления.ВидыРешенийНаЖалобу.РешениеОПриостановленииИсполненияРешенияОбОтказеВПривлеченииКОтветственности);
	ВидыРешенийНаЖалобу.Вставить("6", Перечисления.ВидыРешенийНаЖалобу.РешениеОбОтказеВПриостановленииИсполненияРешенияОбОтказе);
	ВидыРешенийНаЖалобу.Вставить("7", Перечисления.ВидыРешенийНаЖалобу.ИзвещениеОВремениИМестеРассмотренияЖалобы);
	
	Возврат ВидыРешенийНаЖалобу;
	
КонецФункции

Функция ВидРешенияНаЖалобу(ВложенияПриложение, Документ, ИмяТекущегоПриложения)
	
	Если Документ.ВидДокумента <> Перечисления.ВидыНалоговыхДокументов.РешениеПоЖалобе Тогда
		Возврат Неопределено;
	КонецЕсли;
	
	Для Каждого Приложение Из ВложенияПриложение Цикл
		Если Приложение.ТипСодержимогоФайла = Перечисления.ТипыСодержимогоФайлов.XML Тогда
			XMLПриложение = Приложение;
			Прервать;
		КонецЕсли;
	КонецЦикла;
	
	Если XMLПриложение = Неопределено Тогда
		Возврат Неопределено;
	КонецЕсли;
	
	ДанныеПриложения = XMLПриложение.Данные.Получить();
	ИмяФайлаXML      = ПолучитьИмяВременногоФайла("xml");
	ДанныеПриложения.Записать(ИмяФайлаXML);
	
	ЧтениеXML = Новый ЧтениеXML;
	ЧтениеXML.ОткрытьФайл(ИмяФайлаXML);
	ЧтениеXML.ПерейтиКСодержимому();
	
	Фабрика = ФабрикаXDTO;
	Объект  = Фабрика.ПрочитатьXML(ЧтениеXML);
	ЧтениеXML.Закрыть();
	
	ОперацииСФайламиЭДКО.УдалитьВременныйФайл(ИмяФайлаXML);
	
	СвДокиЖалоб = Объект.Документ.СвДокЖалоб;
	Тип = ТипЗнч(СвДокиЖалоб);
	Если Тип = Тип("ОбъектXDTO") Тогда 
		СвДокиЖалоб = ОбщегоНазначенияКлиентСервер.ЗначениеВМассиве(СвДокиЖалоб);
	КонецЕсли;
		
	Для каждого СвДокЖалоб Из СвДокиЖалоб Цикл
		РешЖалоб = ВидРешенияНаЖалобуПоИмениФайла(ИмяТекущегоПриложения, СвДокЖалоб);
		Если ЗначениеЗаполнено(РешЖалоб) Тогда
			Возврат РешЖалоб;
		КонецЕсли;
	КонецЦикла;
	
КонецФункции

Функция ВидРешенияНаЖалобуПоИмениФайла(ИмяТекущегоПриложения, СвДокЖалоб)
	
	Если ТипЗнч(СвДокЖалоб.ИмяФайл) = Тип("СписокXDTO") Тогда
		Имена = СвДокЖалоб.ИмяФайл;
	Иначе
		Имя = СвДокЖалоб.ИмяФайл;
		Имена = Новый Массив;
		Имена.Добавить(Имя);
	КонецЕсли;
	
	Для каждого Имя Из Имена Цикл
	
		Если Имя = ИмяТекущегоПриложения Тогда
			
			ВидыРешенийНаЖалобу = ВидыРешенийНаЖалобу();
			РешЖалоб = ВидыРешенийНаЖалобу.Получить(СвДокЖалоб.РешЖалоб);
			
			Возврат РешЖалоб;
			
		КонецЕсли;
		
	КонецЦикла; 
	
	Возврат Неопределено;
	
КонецФункции

Процедура ЗаполнитьРеквизитыРешенияПоЖалобе(Приложение, Документ)
	
	ДанныеПриложения = Приложение.Данные.Получить();
	ИмяФайлаXML      = ПолучитьИмяВременногоФайла("xml");
	ДанныеПриложения.Записать(ИмяФайлаXML);
	
	ЧтениеXML = Новый ЧтениеXML;
	ЧтениеXML.ОткрытьФайл(ИмяФайлаXML);
	ЧтениеXML.ПерейтиКСодержимому();
	
	Фабрика = ФабрикаXDTO;
	Объект  = Фабрика.ПрочитатьXML(ЧтениеXML);
	ЧтениеXML.Закрыть();
	
	ОперацииСФайламиЭДКО.УдалитьВременныйФайл(ИмяФайлаXML);
	
	Если Объект.Свойства().Получить("Документ") <> Неопределено Тогда
		
		СвЖалоб = Объект.Документ.СвЖалоб;
		
		Если СвЖалоб.Свойства().Получить("НомЖалоб") <> Неопределено Тогда
			Документ.НомерЖалобы = СвЖалоб.НомЖалоб;
		КонецЕсли;
		
		Документ.ДатаЖалобы = ДатаВремяИзСтрокиXML(СвЖалоб.ДатаЖалоб);
		
	КонецЕсли;
	
КонецПроцедуры

	
Функция ОснованиеДокументаРеализацииПолномочий(Знач ИДФайлЗаяв) Экспорт
	
	Если НЕ ЗначениеЗаполнено(ИДФайлЗаяв) Тогда
		Возврат Неопределено;
	Конецесли;
	
	УстановитьПривилегированныйРежим(Истина);
	
	Запрос = Новый Запрос;
	Запрос.Текст = 
		"ВЫБРАТЬ ПЕРВЫЕ 1
		|	СодержимоеТранспортныхКонтейнеров.ТранспортноеСообщение КАК ТранспортноеСообщение
		|ИЗ
		|	РегистрСведений.СодержимоеТранспортныхКонтейнеров КАК СодержимоеТранспортныхКонтейнеров
		|ГДЕ
		|	СодержимоеТранспортныхКонтейнеров.ИмяФайла ПОДОБНО &ИмяФайла";
	
	Запрос.УстановитьПараметр("ИмяФайла", ИДФайлЗаяв + "%.%");
	
	РезультатЗапроса = Запрос.Выполнить();
	
	ВыборкаДетальныеЗаписи = РезультатЗапроса.Выбрать();
	
	ДокументОснования = Неопределено;
	Пока ВыборкаДетальныеЗаписи.Следующий() Цикл
		ДокументОснования = ВыборкаДетальныеЗаписи.ТранспортноеСообщение.ЦиклОбмена.Предмет;
	КонецЦикла;
	
	Возврат ДокументОснования;

КонецФункции

Функция ПолучитьВидНалоговогоДокументаПоКНД(КНД) Экспорт
	
	СоответствиеВидаДокументаКНД = Новый Соответствие;
	
	СоответствиеВидаДокументаКНД.Вставить("1160071", Перечисления.ВидыНалоговыхДокументов.РешениеОПриостановленииПроведенияВыезднойНалоговойПроверки);
	СоответствиеВидаДокументаКНД.Вставить("1160073", Перечисления.ВидыНалоговыхДокументов.РешениеОВозобновленииПроведенияВыезднойНалоговойПроверки);
	СоответствиеВидаДокументаКНД.Вставить("1160075", Перечисления.ВидыНалоговыхДокументов.РешениеОПродленииИлиОтказеВПродленииСроковПредставленияДокументов);
	СоответствиеВидаДокументаКНД.Вставить("1160076", Перечисления.ВидыНалоговыхДокументов.РешениеОбОтложенииРассмотренияМатериаловНалоговойПроверкиВСвязиСНеявкойЛица);
	СоответствиеВидаДокументаКНД.Вставить("1165010", Перечисления.ВидыНалоговыхДокументов.СправкаОПроведеннойВыезднойНалоговойПроверке);
	СоответствиеВидаДокументаКНД.Вставить("1165014", Перечисления.ВидыНалоговыхДокументов.ПоручениеОбИстребованииДокументов);
	СоответствиеВидаДокументаКНД.Вставить("1165009", Перечисления.ВидыНалоговыхДокументов.УведомлениеОВызовеНалогоплательщика);
	СоответствиеВидаДокументаКНД.Вставить("1165019", Перечисления.ВидыНалоговыхДокументов.РешениеОПроведенииДополнительныхМероприятийНалоговогоКонтроля);
	СоответствиеВидаДокументаКНД.Вставить("1165020", Перечисления.ВидыНалоговыхДокументов.РешениеОПривлеченииКОтветственностиЗаСовершениеНалоговогоПравонарушения);
	СоответствиеВидаДокументаКНД.Вставить("1165021", Перечисления.ВидыНалоговыхДокументов.РешениеОбОтказеВПривлеченииКОтветственностиЗаСовершениеНалоговогоПравонарушения);
	СоответствиеВидаДокументаКНД.Вставить("1165022", Перечисления.ВидыНалоговыхДокументов.РешениеОПривлеченииКОтветственностиЗаНалоговоеПравонарушение);
	СоответствиеВидаДокументаКНД.Вставить("1165023", Перечисления.ВидыНалоговыхДокументов.РешениеОбОтказеВПривлеченииКОтветственностиЛицаЗаНалоговоеПравонарушение);
	СоответствиеВидаДокументаКНД.Вставить("1165031", Перечисления.ВидыНалоговыхДокументов.РешениеОПриостановленииИсполненияРешенийФизЛицо);
	СоответствиеВидаДокументаКНД.Вставить("1165032", Перечисления.ВидыНалоговыхДокументов.РешениеОВозобновленииИсполненияРешенийФизЛицо);
	СоответствиеВидаДокументаКНД.Вставить("1165033", Перечисления.ВидыНалоговыхДокументов.РешениеОбОтменеРешенияОПривлеченииКОтветственностиФизЛица);

	СоответствиеВидаДокументаКНД.Вставить("1160002", Перечисления.ВидыНалоговыхДокументов.ТребованиеОбУплатеНалогаСбораПениШтрафаФизЛицу);
	СоответствиеВидаДокументаКНД.Вставить("1170001", Перечисления.ВидыНалоговыхДокументов.ПлатежныйДокумент);
	СоответствиеВидаДокументаКНД.Вставить("1165004", Перечисления.ВидыНалоговыхДокументов.РешениеОВозмещенииНДС);
	СоответствиеВидаДокументаКНД.Вставить("1165005", Перечисления.ВидыНалоговыхДокументов.РешениеОбОтказеВВозмещенииНДС);
	СоответствиеВидаДокументаКНД.Вставить("1165026", Перечисления.ВидыНалоговыхДокументов.РешениеОВозмещенииНДСЗаявительныйПорядок);
	СоответствиеВидаДокументаКНД.Вставить("1165027", Перечисления.ВидыНалоговыхДокументов.РешениеОбОтказеВВозмещенииНДСЗаявительныйПорядок);
	СоответствиеВидаДокументаКНД.Вставить("1165028", Перечисления.ВидыНалоговыхДокументов.РешениеОбОтменеРешенияОВозмещенииНДСЗаявительныйПорядок);
	СоответствиеВидаДокументаКНД.Вставить("1165030", Перечисления.ВидыНалоговыхДокументов.РешениеОбОтменеРешенияОВозмещенииНДСЗаявительныйПорядокУточненнаяДекларация);
	СоответствиеВидаДокументаКНД.Вставить("1165006", Перечисления.ВидыНалоговыхДокументов.РешениеОВозмещенииСуммыАкциза);
	СоответствиеВидаДокументаКНД.Вставить("1165007", Перечисления.ВидыНалоговыхДокументов.РешениеОбОтказеВВозмещенииСуммыАкциза);
	СоответствиеВидаДокументаКНД.Вставить("1165008", Перечисления.ВидыНалоговыхДокументов.МотивированноеЗаключение);
	СоответствиеВидаДокументаКНД.Вставить("1121007", Перечисления.ВидыНалоговыхДокументов.СвидетельствоОПостановкеНаУчетРоссийскойОрганизации);
	СоответствиеВидаДокументаКНД.Вставить("1121029", Перечисления.ВидыНалоговыхДокументов.УведомлениеОПостановкеНаУчетРоссийскойОрганизации);
	СоответствиеВидаДокументаКНД.Вставить("1121013", Перечисления.ВидыНалоговыхДокументов.УведомлениеОСнятииСУчетаРоссийскойОрганизации);
	СоответствиеВидаДокументаКНД.Вставить("1122022", Перечисления.ВидыНалоговыхДокументов.СвидетельствоОПостановкеНаУчетФизЛица);
	СоответствиеВидаДокументаКНД.Вставить("1122024", Перечисления.ВидыНалоговыхДокументов.УведомлениеОПостановкеНаУчетФизЛица);
	СоответствиеВидаДокументаКНД.Вставить("1122023", Перечисления.ВидыНалоговыхДокументов.УведомлениеОСнятииСУчетаФизЛица);
	СоответствиеВидаДокументаКНД.Вставить("1165025", Перечисления.ВидыНалоговыхДокументов.НалоговоеУведомление);
	СоответствиеВидаДокументаКНД.Вставить("1160031", Перечисления.ВидыНалоговыхДокументов.РешениеОПроведенииВыезднойНалоговойПроверки);
	СоответствиеВидаДокументаКНД.Вставить("1160087", Перечисления.ВидыНалоговыхДокументов.РешениеОПроведенииВыезднойНалоговойПроверкиГруппыНалогоплательщиков);
	СоответствиеВидаДокументаКНД.Вставить("1160084", Перечисления.ВидыНалоговыхДокументов.РешениеОПриостановленииВыезднойНалоговойПроверкиГруппыНалогоплательщиков);
	СоответствиеВидаДокументаКНД.Вставить("1160085", Перечисления.ВидыНалоговыхДокументов.РешениеОВозобновленииПроведенияВыезднойНалоговойПроверкиГруппыНалогоплательщиков);
	СоответствиеВидаДокументаКНД.Вставить("1160074", Перечисления.ВидыНалоговыхДокументов.АктОВоспрепятствованииДоступуДолжностныхЛицНаТерриториюИлиВПомещение);
	СоответствиеВидаДокументаКНД.Вставить("1165048", Перечисления.ВидыНалоговыхДокументов.ПостановлениеОПроведенииОсмотраТерриторийПомещенийДокументовИПредметов);
	СоответствиеВидаДокументаКНД.Вставить("1165015", Перечисления.ВидыНалоговыхДокументов.ПостановлениеОПроизводствеВыемкиИзъятияДокументовИПредметов);
	СоответствиеВидаДокументаКНД.Вставить("1165016", Перечисления.ВидыНалоговыхДокументов.ПротоколОПроизводствеВыемкиИзъятияДокументовИПредметов);
	СоответствиеВидаДокументаКНД.Вставить("1165017", Перечисления.ВидыНалоговыхДокументов.ПостановлениеОНазначенииЭкспертизы);
	СоответствиеВидаДокументаКНД.Вставить("1165018", Перечисления.ВидыНалоговыхДокументов.ПротоколОбОзнакомленииПроверяемогоЛицаСПостановлениемОНазначенииЭкспертизы);
	СоответствиеВидаДокументаКНД.Вставить("1160098", Перечисления.ВидыНалоговыхДокументов.АктНалоговойПроверки);
	СоответствиеВидаДокументаКНД.Вставить("1165049", Перечисления.ВидыНалоговыхДокументов.РешениеОПривлеченииКУчастиюСвидетеляЭкспертаСпециалиста);
	СоответствиеВидаДокументаКНД.Вставить("1165052", Перечисления.ВидыНалоговыхДокументов.ПротоколРассмотренияМатериаловНалоговойПроверки);
	СоответствиеВидаДокументаКНД.Вставить("1165037", Перечисления.ВидыНалоговыхДокументов.РешениеОПринятииОбеспечительныхМер);
	СоответствиеВидаДокументаКНД.Вставить("1165038", Перечисления.ВидыНалоговыхДокументов.РешениеОбОтменеОбеспечительныхМер);
	СоответствиеВидаДокументаКНД.Вставить("1165039", Перечисления.ВидыНалоговыхДокументов.РешениеОЗаменеОбеспечительныхМер);
	СоответствиеВидаДокументаКНД.Вставить("1160100", Перечисления.ВидыНалоговыхДокументов.АктОбОбнаруженииФактовСвидетельствующихОНалоговыхПравонарушениях);
	СоответствиеВидаДокументаКНД.Вставить("1165022", Перечисления.ВидыНалоговыхДокументов.РешениеОПривлеченииЛицаКОтветственностиЗаНалоговоеПравонарушение);
	СоответствиеВидаДокументаКНД.Вставить("1165023", Перечисления.ВидыНалоговыхДокументов.РешениеОбОтказеВПривлеченииЛицаКОтветственностиЗаНалоговоеПравонарушение);
	СоответствиеВидаДокументаКНД.Вставить("1165005", Перечисления.ВидыНалоговыхДокументов.РешениеОВозмещенииСуммыНалогаНаДобавленнуюСтоимость);
	СоответствиеВидаДокументаКНД.Вставить("1165004", Перечисления.ВидыНалоговыхДокументов.РешениеОбОтказеВВозмещенииСуммыНалогаНаДобавленнуюСтоимость);
	СоответствиеВидаДокументаКНД.Вставить("1160167", Перечисления.ВидыНалоговыхДокументов.ТребованиеОПредставленииУведомленияОКонтролируемыхИностранныхКомпаниях);
	СоответствиеВидаДокументаКНД.Вставить("1160166", Перечисления.ВидыНалоговыхДокументов.УведомлениеОНепредставленииРасчетаПоСтраховымВзносам);
	СоответствиеВидаДокументаКНД.Вставить("1160043", Перечисления.ВидыНалоговыхДокументов.ТребованиеОПредоставленииСтрановогоОтчета);
	СоответствиеВидаДокументаКНД.Вставить("1160001", Перечисления.ВидыНалоговыхДокументов.ТребованиеОбУплатеНалогаСбораПениШтрафа);
	СоответствиеВидаДокументаКНД.Вставить("1120459", Перечисления.ВидыНалоговыхДокументов.РешениеПоЖалобе);
	
	СоответствиеВидаДокументаКНД.Вставить("1165013", Перечисления.ВидыНалоговыхДокументов.ТребованиеОПредставленииДокументов);
	СоответствиеВидаДокументаКНД.Вставить("1165050", Перечисления.ВидыНалоговыхДокументов.ТребованиеОПредставленииПоясненийКДекларацииНДС);

	ВидДокумента = СоответствиеВидаДокументаКНД[СокрЛП(КНД)];
	Если ВидДокумента = Неопределено Тогда
		ВидДокумента = "Документ налогового органа КНД " + СокрЛП(КНД);
	КонецЕсли;
	
	Возврат ВидДокумента; 

КонецФункции

Функция ТипСодержимогоПоИмениФайла(ИмяФайла, ДляПФР = Ложь) Экспорт
	
	нрегРасширениеФайла = нрег(РасширениеФайла(ИмяФайла));
	Если нрегРасширениеФайла = "txt" Тогда
		Возврат Перечисления.ТипыСодержимогоФайлов.Текст1251; // необходимо анализировать наполнение файла с целью автоопределения кодировки (Текст866 или Текст1251)
	ИначеЕсли нрегРасширениеФайла = "xml" Тогда
		Возврат Перечисления.ТипыСодержимогоФайлов.xml;
	ИначеЕсли нрегРасширениеФайла = "htm" ИЛИ нрегРасширениеФайла = "html" Тогда
		Возврат Перечисления.ТипыСодержимогоФайлов.html;
	ИначеЕсли НЕ ДляПФР Тогда
		Если нрегРасширениеФайла = "pdf" Тогда
			Возврат Перечисления.ТипыСодержимогоФайлов.pdf;
		ИначеЕсли нрегРасширениеФайла = "rtf" Тогда
			Возврат Перечисления.ТипыСодержимогоФайлов.rtf;
		ИначеЕсли нрегРасширениеФайла = "tiff" ИЛИ нрегРасширениеФайла = "tif" Тогда
			Возврат Перечисления.ТипыСодержимогоФайлов.tiff;
		ИначеЕсли нрегРасширениеФайла = "jpeg" ИЛИ нрегРасширениеФайла = "jpg" Тогда
			Возврат Перечисления.ТипыСодержимогоФайлов.jpeg;
		ИначеЕсли нрегРасширениеФайла = "png" Тогда
			Возврат Перечисления.ТипыСодержимогоФайлов.png;
		ИначеЕсли нрегРасширениеФайла = "doc" Тогда
			Возврат Перечисления.ТипыСодержимогоФайлов.ms_word;
		ИначеЕсли нрегРасширениеФайла = "xls" Тогда
			Возврат Перечисления.ТипыСодержимогоФайлов.ms_excel;
		ИначеЕсли нрегРасширениеФайла = "odt" Тогда
			Возврат Перечисления.ТипыСодержимогоФайлов.odf_text;
		ИначеЕсли нрегРасширениеФайла = "ods" Тогда
			Возврат Перечисления.ТипыСодержимогоФайлов.odf_spreadsheet;
		ИначеЕсли нрегРасширениеФайла = "docx" Тогда
			Возврат Перечисления.ТипыСодержимогоФайлов.oxml_word;
		ИначеЕсли нрегРасширениеФайла = "xlsx" Тогда
			Возврат Перечисления.ТипыСодержимогоФайлов.oxml_spreadsheet;
		ИначеЕсли нрегРасширениеФайла = "sgn" Тогда
			Возврат Перечисления.ТипыСодержимогоФайлов.sgn;
		Иначе
			Возврат Перечисления.ТипыСодержимогоФайлов.Неизвестный;
		КонецЕсли;
	Иначе
		Возврат Перечисления.ТипыСодержимогоФайлов.Неизвестный;
	КонецЕсли;
		
КонецФункции

Функция ДобавитьВложенияДокумента(Документ, ИмяФайла, Данные, ВидРешенияНаЖалобу, Тип, Размер = Неопределено) Экспорт
	
	Если НЕ ЗначениеЗаполнено(Документ) Тогда
		Возврат Ложь;
	КонецЕсли;
	
	Если Размер = Неопределено Тогда
		Размер = ОпределитьРазмер(Данные);
	КонецЕсли;
	
	МенеджерЗаписи = РегистрыСведений.ФайлыДокументовРеализацииПолномочийНалоговыхОрганов.СоздатьМенеджерЗаписи();
	МенеджерЗаписи.Документ = Документ;
	МенеджерЗаписи.ИмяФайла = ИмяФайла;
	МенеджерЗаписи.ТипСодержимого = Тип;
	МенеджерЗаписи.Размер = Размер;
	МенеджерЗаписи.ВидРешенияНаЖалобу = ВидРешенияНаЖалобу;
	
	ТипДанных = ТипЗнч(Данные);
	Если ТипДанных = Тип("ХранилищеЗначения") Тогда
		МенеджерЗаписи.Данные = Данные;
	ИначеЕсли ТипДанных = Тип("Строка") Тогда
		МенеджерЗаписи.Данные = Новый ХранилищеЗначения(Новый ДвоичныеДанные(Данные), Новый СжатиеДанных(9));
	Иначе
		МенеджерЗаписи.Данные = Новый ХранилищеЗначения(Данные, Новый СжатиеДанных(9));
	КонецЕсли;
	Попытка
		МенеджерЗаписи.Записать(Истина);
		Возврат Истина;
	Исключение

		ПодробноеПредставление = ПодробноеПредставлениеОшибки(ИнформацияОбОшибке());
		
		ИмяСобытия = НСтр("ru = 'Электронный документооборот с контролирующими органами.Добавить вложения документа'", 
		ОбщегоНазначения.КодОсновногоЯзыка());

		ЗаписьЖурналаРегистрации(
			ИмяСобытия ,
			УровеньЖурналаРегистрации.Ошибка,,,
			ПодробноеПредставление);
			
		ТекстСообщения = СтроковыеФункцииКлиентСервер.ПодставитьПараметрыВСтроку(НСтр("ru = 'Не удалось сохранить в информационной базе вложение %1 входящего документа %2!%3'"),
																					ИмяФайла, Документ, ПодробноеПредставление);
		ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
			
		Возврат Ложь;
		
	КонецПопытки;
	
КонецФункции

Функция ДанныеТребованияОПредставленииПоясненийКДекларацииНДС(Требование) Экспорт
	
	Результат = Новый Структура();
	
	Если ТипЗнч(Требование) <> Тип("СправочникСсылка.ДокументыРеализацииПолномочийНалоговыхОрганов")
		ИЛИ Требование.ВидДокумента <> Перечисления.ВидыНалоговыхДокументов.ТребованиеОПредставленииПоясненийКДекларацииНДС Тогда
		Возврат Неопределено;
	КонецЕсли;
	
	ПутьКXMLФайлаТребования  = XMLФайлТребованияОПредставленииПоясненийКДекларацииНДСИзРегистра(Требование);
	Если ПутьКXMLФайлаТребования = Неопределено Тогда
		Возврат Неопределено;
	КонецЕсли;
	
	ТребованиеXDTO = XDTOФайлаТребованияОПредставленииПоясненийКДекларацииНДС(ПутьКXMLФайлаТребования);
	
	Результат.Вставить("ВерсФорм", ТребованиеXDTO.ВерсФорм);
	Результат.Вставить("Раздел8", Документы.ПоясненияКДекларацииПоНДС.ТаблицаРаздела8());
	Результат.Вставить("Раздел8_1", Документы.ПоясненияКДекларацииПоНДС.ТаблицаРаздела8_1());
	Результат.Вставить("Раздел9", Документы.ПоясненияКДекларацииПоНДС.ТаблицаРаздела9());
	Результат.Вставить("Раздел9_1", Документы.ПоясненияКДекларацииПоНДС.ТаблицаРаздела9_1());
	Результат.Вставить("Раздел10", Документы.ПоясненияКДекларацииПоНДС.ТаблицаРаздела10());
	Результат.Вставить("Раздел11", Документы.ПоясненияКДекларацииПоНДС.ТаблицаРаздела11());
	Результат.Вставить("Раздел12", Документы.ПоясненияКДекларацииПоНДС.ТаблицаРаздела12());
	Результат.Вставить("НетКнигаПрод", Документы.ПоясненияКДекларацииПоНДС.ТаблицаНетКнигаПрод());
	Результат.Вставить("СведКС", Документы.ПоясненияКДекларацииПоНДС.ТаблицаТребованияСведКС());
	
	ЗаполнитьРаздел8(Результат, ТребованиеXDTO);
	ЗаполнитьРаздел8_1(Результат, ТребованиеXDTO);
	ЗаполнитьРаздел9(Результат, ТребованиеXDTO);
	ЗаполнитьРаздел9_1(Результат, ТребованиеXDTO);
	ЗаполнитьРаздел10(Результат, ТребованиеXDTO);
	ЗаполнитьРаздел11(Результат, ТребованиеXDTO);
	ЗаполнитьРаздел12(Результат, ТребованиеXDTO);
	ЗаполнитьНетКнигаПрод(Результат, ТребованиеXDTO);
	ЗаполнитьСведКС(Результат, ТребованиеXDTO);
	
	Возврат Результат;
	
КонецФункции

Функция XMLФайлТребованияОПредставленииПоясненийКДекларацииНДСИзРегистра(Требование) Экспорт
	
	Если ТипЗнч(Требование) = Тип("СправочникОбъект.ДокументыРеализацииПолномочийНалоговыхОрганов")
		И Требование.ЭтоНовый() Тогда
		Требование = Требование.ПолучитьСсылкуНового();
	КонецЕсли;
	
	ПриложенияТребования = ПолучитьФайлыДокументовРеализацииПолномочийНалоговыхОрганов(Требование);
	Возврат XMLФайлТребованияОПредставленииПоясненийКДекларацииНДССредиПриложенийТребования(ПриложенияТребования);
	
КонецФункции

Функция XMLФайлТребованияОПредставленииПоясненийКДекларацииНДССредиПриложенийТребования(ПриложенияТребования) Экспорт
	
	Для каждого ПриложениеТребования Из ПриложенияТребования Цикл
		СвойстваФайла = ОбщегоНазначенияКлиентСервер.РазложитьПолноеИмяФайла(ПриложениеТребования.ИмяФайла);
		
		Если ВРЕГ(СвойстваФайла.Расширение) = ВРЕГ(".xml") Тогда
			
			ВременныйФайлТребований = ПолучитьИмяВременногоФайла("xml");
			ПриложениеТребования.Данные.Получить().Записать(ВременныйФайлТребований);
			
			Возврат ВременныйФайлТребований;
		КонецЕсли;
	
	КонецЦикла;
	
	Возврат Неопределено;
	
КонецФункции

Функция XDTOФайлаТребованияОПредставленииПоясненийКДекларацииНДС(ФайлТребования)
	
	ЧтениеXML = Новый ЧтениеXML;
	ЧтениеXML.ОткрытьФайл(ФайлТребования);
	ЧтениеXML.ПерейтиКСодержимому();
	
	Фабрика = ФабрикаXDTO;
	Объект = Фабрика.ПрочитатьXML(ЧтениеXML);
	ЧтениеXML.Закрыть();

	Возврат Объект;
	
КонецФункции

Функция ЕстьТребованияКРазделам8_12(ДокументыРеализацииПолномочий) Экспорт

	ЭтоТребованиеОПредставленииПоясненийКДекларацииНДС = 
		ДокументыРеализацииПолномочий.ВидДокумента = 
		Перечисления.ВидыНалоговыхДокументов.ТребованиеОПредставленииПоясненийКДекларацииНДС;
	
	ЕстьТребования = ЭтоТребованиеОПредставленииПоясненийКДекларацииНДС И
		XMLФайлТребованияОПредставленииПоясненийКДекларацииНДСИзРегистра(ДокументыРеализацииПолномочий) <> Неопределено;

	Возврат ЕстьТребования;
		
КонецФункции
	
Функция СвойстваДекларацииИзФайлаТребованияОПредставленииПоясненийКДекларацииНДС(Требование, ПриложенияТребования = Неопределено) Экспорт
	
	Если ПриложенияТребования = Неопределено Тогда
		// Если приложения не указаны, сами ищем в регистре
		ФайлТребования  = XMLФайлТребованияОПредставленииПоясненийКДекларацииНДСИзРегистра(Требование);
	Иначе
		// Если приложения указаны, ищем среди них xml-файл требования
		ФайлТребования  = XMLФайлТребованияОПредставленииПоясненийКДекларацииНДССредиПриложенийТребования(ПриложенияТребования);
	КонецЕсли;
	
	Если ФайлТребования = Неопределено Тогда
		Возврат Неопределено;
	КонецЕсли;
		
	XDTOФайлаТребования = XDTOФайлаТребованияОПредставленииПоясненийКДекларацииНДС(ФайлТребования);
	
	Документ 				= XDTOФайлаТребования.Документ;
	Год 					= Документ.ОтчетГод;
	СвойстваПериодичности 	= СвойстваПериодичностиДекларацииВТребовании(Документ.Период, Год);
	
	Результат = Новый Структура();
	
	Результат.Вставить("Год", 					Год);
	Результат.Вставить("НомерКорректировки", 	Число(Документ.НомКорр));
	Результат.Вставить("Период", 				СвойстваПериодичности.НазваниеПериода);
	Результат.Вставить("Периодичность", 		СвойстваПериодичности.Периодичность);
	Результат.Вставить("КонецПериода", 			СвойстваПериодичности.КонецПериода);
	
	Результат.Вставить("Декларация", 			ДекларацияИзФайлаТребованияОПредставленииПоясненийКДекларацииНДС(Требование));
	Результат.Вставить("Организация", 			Требование.Организация);
	
	Результат.Вставить("ТребованиеПериод", 		Документ.Период);
	Результат.Вставить("ТребованиеОтчетГод", 	Документ.ОтчетГод);
	Результат.Вставить("ТребованиеНомКорр", 	Документ.НомКорр);
	
	Возврат Результат;
	
КонецФункции

Функция ПериодОтраженияЗаписиПоясненияКДекларацииНДС(Требование) Экспорт
	
	ФайлТребования  = XMLФайлТребованияОПредставленииПоясненийКДекларацииНДСИзРегистра(Требование);
	
	Если ФайлТребования = Неопределено Тогда
		Возврат Неопределено;
	КонецЕсли;
		
	XDTOФайлаТребования = XDTOФайлаТребованияОПредставленииПоясненийКДекларацииНДС(ФайлТребования);
	
	Документ 			= XDTOФайлаТребования.Документ;
	
	Период 				= Документ.Период;
	НомерКорректировки 	= Число(Документ.НомКорр);
	Год					= Документ.ОтчетГод;
	
	Возврат Строка(Период) + ";" + Строка(Год);

КонецФункции

Функция ДекларацияИзФайлаТребованияОПредставленииПоясненийКДекларацииНДС(Требование, ПриложенияТребования = Неопределено) Экспорт
	
	Если ПриложенияТребования = Неопределено Тогда
		// Если приложения не указаны, сами ищем в регистре
		ФайлТребования  = XMLФайлТребованияОПредставленииПоясненийКДекларацииНДСИзРегистра(Требование);
	Иначе
		// Если приложения указаны, ищем среди них xml-файл требования
		ФайлТребования  = XMLФайлТребованияОПредставленииПоясненийКДекларацииНДССредиПриложенийТребования(ПриложенияТребования);
	КонецЕсли;
	
	Если ФайлТребования = Неопределено Тогда
		Возврат Неопределено;
	КонецЕсли;
		
	XDTOФайлаТребования = XDTOФайлаТребованияОПредставленииПоясненийКДекларацииНДС(ФайлТребования);
	
	Документ 			= XDTOФайлаТребования.Документ;
	
	Период 				= Документ.Период;
	НомерКорректировки 	= Число(Документ.НомКорр);
	Год					= Документ.ОтчетГод;
	
	НачальнаяИКонечнаяДата 	= НачальнаяИКонечнаяДатаПоНалоговомуПериодуИГоду(Период, Год);
	НачальнаяДата 			= НачальнаяИКонечнаяДата.ДатаНачала;
	ДатаОкончания 			= НачальнаяИКонечнаяДата.ДатаОкончания;
	
	Запрос = Новый Запрос;
	Запрос.Текст = 
		"ВЫБРАТЬ РАЗРЕШЕННЫЕ
		|	РегламентированныйОтчет.Ссылка
		|ИЗ
		|	Документ.РегламентированныйОтчет КАК РегламентированныйОтчет
		|ГДЕ
		|	РегламентированныйОтчет.Вид = &НомерКорректировки
		|	И НАЧАЛОПЕРИОДА(РегламентированныйОтчет.ДатаНачала, ДЕНЬ) = &ДатаНачала
		|	И НАЧАЛОПЕРИОДА(РегламентированныйОтчет.ДатаОкончания, ДЕНЬ) = &ДатаОкончания
		|	И РегламентированныйОтчет.ИсточникОтчета = &ИсточникОтчета
		|	И РегламентированныйОтчет.Организация = &Организация
		|
		|ОБЪЕДИНИТЬ ВСЕ
		|
		|ВЫБРАТЬ
		|	ЭлектронныеПредставленияРегламентированныхОтчетов.Ссылка
		|ИЗ
		|	Справочник.ЭлектронныеПредставленияРегламентированныхОтчетов КАК ЭлектронныеПредставленияРегламентированныхОтчетов
		|ГДЕ
		|	НАЧАЛОПЕРИОДА(ЭлектронныеПредставленияРегламентированныхОтчетов.ДатаНачала, ДЕНЬ) = &ДатаНачала
		|	И НАЧАЛОПЕРИОДА(ЭлектронныеПредставленияРегламентированныхОтчетов.ДатаОкончания, ДЕНЬ) = &ДатаОкончания
		|	И ЭлектронныеПредставленияРегламентированныхОтчетов.ВидОтчета = &ВидОтчета
		|	И ЭлектронныеПредставленияРегламентированныхОтчетов.Версия = &НомерКорректировки
		|	И ЭлектронныеПредставленияРегламентированныхОтчетов.Организация = &Организация";
	
	Запрос.УстановитьПараметр("НомерКорректировки", НомерКорректировки);
	Запрос.УстановитьПараметр("ВидОтчета", 			Справочники.ВидыОтправляемыхДокументов.НДС);
	Запрос.УстановитьПараметр("ДатаНачала", 		НачалоДня(НачальнаяДата));
	Запрос.УстановитьПараметр("ДатаОкончания", 		НачалоДня(ДатаОкончания));
	Запрос.УстановитьПараметр("ИсточникОтчета", 	"РегламентированныйОтчетНДС");
	Запрос.УстановитьПараметр("Организация", 		Требование.Организация);
	
	РезультатЗапроса = Запрос.Выполнить();
	
	ВыборкаДетальныеЗаписи = РезультатЗапроса.Выбрать();
	
	Пока ВыборкаДетальныеЗаписи.Следующий() Цикл
		
		ПоследнийЦиклОбмена = ДокументооборотСКОВызовСервера.ПолучитьПоследнийЦиклОбмена(ВыборкаДетальныеЗаписи.Ссылка);
		
		Если ПоследнийЦиклОбмена = Справочники.ЦиклыОбмена.ПустаяСсылка() Тогда
			Продолжить;
		КонецЕсли;
			
		СообщенияЦикла 		= ПолучитьСообщенияЦиклаОбмена(ПоследнийЦиклОбмена,,,Истина);
		СтрРезультатыОбработкиДекларацияНО = СообщенияЦикла.НайтиСтроки(Новый Структура("Тип", Перечисления.ТипыТранспортныхСообщений.РезультатОбработкиДекларацияНО));
					
		// Определяем наиболее свежее сообщение с результатом обработки
		ПоследнийРезультатОбработки = Неопределено;
		Для Каждого СтрРезультатОбработки Из СтрРезультатыОбработкиДекларацияНО Цикл
			Если ПоследнийРезультатОбработки = Неопределено ИЛИ СтрРезультатОбработки.Дата > ПоследнийРезультатОбработки.Дата Тогда
				ПоследнийРезультатОбработки = СтрРезультатОбработки;
			КонецЕсли;
		КонецЦикла;

		// Среди результата приема ищем "Сдано"
		Если ПоследнийРезультатОбработки <> Неопределено 
			И СтрРезультатыОбработкиДекларацияНО.Количество() > 0
			И НЕ ПоследнийРезультатОбработки.ПротоколСОшибкой Тогда
			
			Возврат ВыборкаДетальныеЗаписи.Ссылка;
			
		КонецЕсли;

	КонецЦикла;
	
	Возврат Неопределено;
	
КонецФункции

Функция КонецПериодаТребованияПоДекларацииНДС(Требование) Экспорт
	
	// Если приложения не указаны, сами ищем в регистре
	ФайлТребования = XMLФайлТребованияОПредставленииПоясненийКДекларацииНДСИзРегистра(Требование);
	
	Если ФайлТребования = Неопределено Тогда
		Возврат Неопределено;
	КонецЕсли;
		
	XDTOФайлаТребования = XDTOФайлаТребованияОПредставленииПоясненийКДекларацииНДС(ФайлТребования);
	
	Документ = XDTOФайлаТребования.Документ;
	
	Период 	 = Документ.Период;
	Год		 = Документ.ОтчетГод;
	
	НачальнаяИКонечнаяДата = НачальнаяИКонечнаяДатаПоНалоговомуПериодуИГоду(Период, Год);
	
	ДатаОкончания = КонецДня(НачальнаяИКонечнаяДата.ДатаОкончания);
	
	Возврат ДатаОкончания;
	
КонецФункции
	
Функция ИдФайлаДекларацииНДС(ДекларацияНДС) Экспорт
	
	ПоследнийЦиклОбмена = ДокументооборотСКОВызовСервера.ПолучитьПоследнийЦиклОбмена(ДекларацияНДС);
	
	Если ПоследнийЦиклОбмена = Справочники.ЦиклыОбмена.ПустаяСсылка() Тогда
		Возврат Неопределено;
	КонецЕсли;
		
	СообщенияЦикла						= ПолучитьСообщенияЦиклаОбмена(ПоследнийЦиклОбмена,,,Истина);
	СтрРезультатыОбработкиДекларацияНО	= СообщенияЦикла.НайтиСтроки(Новый Структура("Тип", Перечисления.ТипыТранспортныхСообщений.РезультатОбработкиДекларацияНО));
				
	// Определяем наиболее свежее сообщение с результатом обработки
	ПоследнийРезультатОбработки = Неопределено;
	Для Каждого СтрРезультатОбработки Из СтрРезультатыОбработкиДекларацияНО Цикл
		Если ПоследнийРезультатОбработки = Неопределено ИЛИ СтрРезультатОбработки.Дата > ПоследнийРезультатОбработки.Дата Тогда
			ПоследнийРезультатОбработки = СтрРезультатОбработки;
		КонецЕсли;
	КонецЦикла;

	// Среди результата приема ищем "Сдано"
	Если ПоследнийРезультатОбработки <> Неопределено 
		И СтрРезультатыОбработкиДекларацияНО.Количество() > 0 Тогда
		
		СтрДекларацииНП = СообщенияЦикла.НайтиСтроки(Новый Структура("Тип", Перечисления.ТипыТранспортныхСообщений.ДекларацияНП));
		
		Если СтрДекларацииНП.Количество() = 0 Тогда
			
			ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(
				НСтр("ru = 'Не удалось определить имя файла декларации НДС.
				|Убедитесь, что декларация отправлена.'"));
				
			Возврат Неопределено;
		КонецЕсли;
		
		ТранспортноеСообщение 	= СтрДекларацииНП[0];
		
		ВложенияТранспортногоСообщения = ПолучитьВложенияТранспортногоСообщения(ТранспортноеСообщение, , Перечисления.ТипыСодержимогоТранспортногоКонтейнера.ФайлОтчетности);
		Если ВложенияТранспортногоСообщения.Количество() > 0 Тогда
			ИмяФайла = ВложенияТранспортногоСообщения[0].ИмяФайла;
			СвойстваФайла = ОбщегоНазначенияКлиентСервер.РазложитьПолноеИмяФайла(ИмяФайла);
			Возврат СвойстваФайла.ИмяБезРасширения;
		Иначе
			Возврат Неопределено;
		КонецЕсли;
		
	КонецЕсли;
	
	Возврат Неопределено;
	
КонецФункции

Функция СвойстваПериодичностиДекларацииВТребовании(Знач НалоговыйПериод, Знач Год)
	
	НазваниеПериода = "";
	Периодичность 	= Неопределено;
	Год				= Число(Год);
	КонецПериода	= Неопределено;
	
	НалоговыйПериод = Число(НалоговыйПериод);
	
	Если НалоговыйПериод <= 12 
		ИЛИ НалоговыйПериод > 70 Тогда
		// Это номера месяцов

		НомерМесяца = Число(НалоговыйПериод);
		// Номера месяцов при реорганизации (ликвидации)
		Если НомерМесяца > 70 Тогда
			НомерМесяца = НомерМесяца - 70;
		КонецЕсли;
		
		НачалоПериода 	= Дата(Год, НомерМесяца, 1);
		НазваниеПериода = Формат(НачалоПериода, "ДФ=MMMM");
		Периодичность 	= Перечисления.Периодичность.Месяц;
		КонецПериода 	= Формат(КонецМесяца(НачалоПериода), "ггггммдд");
		
	ИначеЕсли НалоговыйПериод = 21
		ИЛИ НалоговыйПериод = 51 Тогда
		
		НазваниеПериода = НСтр("ru = '1 квартал'");
		Периодичность 	= Перечисления.Периодичность.Квартал;
		
		НачалоПериода 	= Дата(Год, 1, 1);
		КонецПериода 	= Формат(КонецКвартала(НачалоПериода), "ггггммдд");
		
	ИначеЕсли НалоговыйПериод = 22
		ИЛИ НалоговыйПериод = 54 Тогда
		
		НазваниеПериода = НСтр("ru = '2 квартал'");
		Периодичность = Перечисления.Периодичность.Квартал;
		
		НачалоПериода 	= Дата(Год, 4, 1);
		КонецПериода 	= Формат(КонецКвартала(НачалоПериода), "ггггммдд");
		
	ИначеЕсли НалоговыйПериод = 23
		ИЛИ НалоговыйПериод = 55 Тогда 
		
		НазваниеПериода = НСтр("ru = '3 квартал'");
		Периодичность = Перечисления.Периодичность.Квартал;
		
		НачалоПериода 	= Дата(Год, 7, 1);
		КонецПериода 	= Формат(КонецКвартала(НачалоПериода), "ггггммдд");
		
	ИначеЕсли НалоговыйПериод = 24
		ИЛИ НалоговыйПериод = 56 Тогда
		
		НазваниеПериода = НСтр("ru = '4 квартал'");
		Периодичность = Перечисления.Периодичность.Квартал;
		
		НачалоПериода 	= Дата(Год, 10, 1);
		КонецПериода 	= Формат(КонецКвартала(НачалоПериода), "ггггммдд");
		
	КонецЕсли;
	
	Результат = Новый Структура;
	Результат.Вставить("НазваниеПериода", 	НазваниеПериода);
	Результат.Вставить("Периодичность", 	Периодичность);
	Результат.Вставить("КонецПериода", 		КонецПериода);
	
	Возврат Результат;
	
КонецФункции

Функция НачальнаяИКонечнаяДатаПоНалоговомуПериодуИГоду(Знач НалоговыйПериод, Знач Год) Экспорт
	
	Год = Число(Год);
	
	ДатаНачала 		= Дата(Год, 1, 1);
	ДатаОкончания 	= Дата(Год, 1, 1);
	
	НалоговыйПериод = Число(НалоговыйПериод);
	
	Если НалоговыйПериод <= 12 
		ИЛИ НалоговыйПериод > 70 Тогда
		// Это номера месяцов
			
		НомерМесяца = Число(НалоговыйПериод);
		// Номера месяцов при реорганизации (ликвидации)
		Если НомерМесяца > 70 Тогда
			НомерМесяца = НомерМесяца - 70;
		КонецЕсли;
		
		ДатаНачала 		= Дата(Год, НомерМесяца, 1);
		ДатаОкончания 	= НачалоДня(КонецМесяца(ДатаНачала));
		
	ИначеЕсли НалоговыйПериод > 20 И НалоговыйПериод < 25 Тогда
		// Это 1,2,3,4 квартал
		
		НомерМесяца = (НалоговыйПериод - 20)*3;
		
		ДатаОкончания 	= НачалоДня(КонецМесяца(Дата(Год, НомерМесяца, 1)));
		ДатаНачала 		= НачалоКвартала(ДатаОкончания);
		
	ИначеЕсли НалоговыйПериод > 50 И НалоговыйПериод < 60 Тогда
		// Это 1,2,3,4 квартал при реорганизации (ликвидации)
		
		Если НалоговыйПериод = 51 Тогда
			НомерМесяца = 1;
		ИначеЕсли НалоговыйПериод = 54 Тогда 
			НомерМесяца = 4;
		ИначеЕсли НалоговыйПериод = 55 Тогда 
			НомерМесяца = 7;
		ИначеЕсли НалоговыйПериод = 56 Тогда 
			НомерМесяца = 10;
		КонецЕсли;
		
		ДатаНачала 		= Дата(Год, НомерМесяца, 1);
		ДатаОкончания 	= НачалоДня(КонецКвартала(ДатаНачала));
		
	КонецЕсли;
	
	Результат = Новый Структура();
	Результат.Вставить("ДатаНачала", 	ДатаНачала);
	Результат.Вставить("ДатаОкончания", ДатаОкончания);
	
	Возврат Результат;
	
КонецФункции

Процедура ЗаполнитьКоллекциюИзXDTO(Приемник, Источник, ПараметрыОтображения)
	
	АтрибутыПриемника = Новый Структура;
	
	Для Каждого Колонка Из Приемник.Колонки Цикл
		ПараметрыАтрибута = Новый Структура;
		ПараметрыАтрибута.Вставить("Тип", Колонка.ТипЗначения);
		АтрибутыПриемника.Вставить(Колонка.Имя, ПараметрыАтрибута);	
	КонецЦикла;
	
	КоллекцияXDTO = ПоместитьВКоллекциюПриНеобходимости(Источник);
	Для Каждого СтрокаКоллекции Из КоллекцияXDTO Цикл
		НоваяСтрока = Приемник.Добавить();
		ЗаполнитьЭлементКоллекцииИзXDTO(НоваяСтрока, СтрокаКоллекции, АтрибутыПриемника, ПараметрыОтображения);
	КонецЦикла;
	
КонецПроцедуры

Функция АтрибутПростогоТипа(ПараметрыАтрибута)
	
	Если ПараметрыАтрибута.Тип.Типы().Количество() = 1 И ПростойТип(ПараметрыАтрибута.Тип.Типы()[0]) Тогда
		Возврат Истина;
	Иначе
		Возврат Ложь;
	КонецЕсли;
	
КонецФункции

Функция АтрибутКоллекция(ПараметрыАтрибута)
	
	Возврат ОбщегоНазначения.ОписаниеТипаСостоитИзТипа(ПараметрыАтрибута.Тип, Тип("Массив"));
	
КонецФункции

Функция ПоместитьВКоллекциюПриНеобходимости(ОбъектXDTO)
	
	Коллекция = ОбъектXDTO;
	Если ТипЗнч(ОбъектXDTO) <> Тип("СписокXDTO") Тогда
		Коллекция = Новый Массив;
		Коллекция.Добавить(ОбъектXDTO);
	КонецЕсли;
	
	Возврат Коллекция;
	
КонецФункции

Функция ПростойТип(Тип)
	
	ПростыеТипы = Новый Массив;
	ПростыеТипы.Добавить(Тип("Дата"));
	ПростыеТипы.Добавить(Тип("Строка"));
	ПростыеТипы.Добавить(Тип("Булево"));
	ПростыеТипы.Добавить(Тип("Число"));
	
	Возврат ПростыеТипы.Найти(Тип) <> Неопределено;
	
КонецФункции
	
Процедура ЗаполнитьЭлементКоллекцииИзXDTO(Приемник, Источник, АтрибутыПриемника, ПараметрыОтображения)
	
	Для Каждого АтрибутПриемника Из АтрибутыПриемника Цикл
		Если Источник.Свойства().Получить(АтрибутПриемника.Ключ) <> Неопределено Тогда
			ОпределяемыйТип = Неопределено;
			// Значение приемника и источника имеют простой тип.
			Если АтрибутПростогоТипа(АтрибутПриемника.Значение) И ПростойТип(ТипЗнч(Источник[АтрибутПриемника.Ключ])) Тогда				
				Если ОбщегоНазначения.ОписаниеТипаСостоитИзТипа(АтрибутПриемника.Значение.Тип, Тип("Дата")) Тогда					
					Приемник[АтрибутПриемника.Ключ] = СтрокаКакДата(Источник[АтрибутПриемника.Ключ], ПараметрыОтображения.ФорматДаты);
				Иначе
					Приемник[АтрибутПриемника.Ключ] = Источник[АтрибутПриемника.Ключ];
				КонецЕсли;
				
			// Значение приемника уточнено определяемым типом
			ИначеЕсли ПараметрыОтображения.ОпределяемыеТипы.Свойство(АтрибутПриемника.Ключ, ОпределяемыйТип) Тогда
				КоллекцияXDTO = ПоместитьВКоллекциюПриНеобходимости(Источник[АтрибутПриемника.Ключ]);
				Для Каждого Элемент Из КоллекцияXDTO Цикл
					Если ПростойТип(ТипЗнч(ОпределяемыйТип)) Тогда
						ЗначениеОбъекта = ОпределяемыйТип;
						ЗаполнитьПолеПоСвойству(ЗначениеОбъекта, ПолучитьРекурсивноВложенныйЭлемент(Элемент), Неопределено, ПараметрыОтображения);
					Иначе
						ЗначениеОбъекта = ОбщегоНазначения.СкопироватьРекурсивно(ОпределяемыйТип);
						Для Каждого КлючЗначение Из ЗначениеОбъекта Цикл 
							ЗаполнитьПолеПоСвойству(ЗначениеОбъекта[КлючЗначение.Ключ], Элемент, КлючЗначение.Ключ, ПараметрыОтображения);
						КонецЦикла;
					КонецЕсли;
					Если АтрибутКоллекция(АтрибутПриемника.Значение) Тогда
						Приемник[АтрибутПриемника.Ключ].Добавить(ЗначениеОбъекта);	
					Иначе
						Приемник[АтрибутПриемника.Ключ] = ЗначениеОбъекта;
					КонецЕсли;
				КонецЦикла;
				
			// Значение источника при развороте по иерархии имеет простой тип.
			Иначе 
				КоллекцияXDTO = ПоместитьВКоллекциюПриНеобходимости(Источник[АтрибутПриемника.Ключ]);
				Для Каждого Элемент Из КоллекцияXDTO Цикл
					ВложенныйЭлемент = ПолучитьРекурсивноВложенныйЭлемент(Элемент);
					Если ВложенныйЭлемент <> Неопределено Тогда
						Если АтрибутПростогоТипа(АтрибутПриемника.Значение) Тогда
							Приемник[АтрибутПриемника.Ключ] = ВложенныйЭлемент;
						Иначе
							Приемник[АтрибутПриемника.Ключ].Добавить(ВложенныйЭлемент);
						КонецЕсли;
					КонецЕсли;
				КонецЦикла;
			КонецЕсли;
		КонецЕсли;
	КонецЦикла;
	
КонецПроцедуры

Функция ПолучитьРекурсивноВложенныйЭлемент(Элемент)
	
	ВложенныйЭлемент = Неопределено;
	Если ПростойТип(ТипЗнч(Элемент)) Тогда
		ВложенныйЭлемент = Элемент;
	ИначеЕсли ТипЗнч(Элемент) = Тип("ОбъектXDTO") Тогда
		Свойства = Элемент.Свойства();
		Если Свойства.Количество() = 0 Тогда
			ВложенныйЭлемент = Неопределено;
		ИначеЕсли Свойства.Количество() = 1 Тогда
			ВложенныйЭлемент = ПолучитьРекурсивноВложенныйЭлемент(Элемент[Свойства[0].Имя]);
		Иначе
			
			ВложенныйЭлемент = Новый Структура;
			Для каждого Свойство Из Свойства Цикл
				ЗначениеЭлемента = ПолучитьРекурсивноВложенныйЭлемент(Элемент[Свойство.Имя]);
				ВложенныйЭлемент.Вставить(Свойство.Имя, ЗначениеЭлемента);
			КонецЦикла;
			
		КонецЕсли;
	КонецЕсли;
	
	Возврат ВложенныйЭлемент;
	
КонецФункции

Функция СтрокаКакДата(Строка, Формат) 
	
	Если Формат = "dd.MM.yyyy" Тогда
		Возврат Дата(Прав(Строка, 4), Сред(Строка, 4, 2), Лев(Строка, 2));
	Иначе
		ВызватьИсключение(НСтр("ru = 'Формат преобразования строки в дату не поддерживается.'"));
	КонецЕсли;
		
КонецФункции

Функция ПараметрыОтображенияПоУмолчанию(ФорматДаты = "", ОпределяемыеТипы = Неопределено)
	
	ПараметрыОтображения = Новый Структура;
	ПараметрыОтображения.Вставить("ФорматДаты", ФорматДаты);
	ПараметрыОтображения.Вставить("ОпределяемыеТипы", ОпределяемыеТипы);
	
	Возврат ПараметрыОтображения;
	
КонецФункции

Функция ПараметрыОтображенияРаздел8()
	
	ОпределяемыеТипы = Новый Структура;
	ОпределяемыеТипы.Вставить("СпрКодОш", Документы.ПоясненияКДекларацииПоНДС.СтруктураСпрКодОш());
	ОпределяемыеТипы.Вставить("ДокПдтвУпл", Документы.ПоясненияКДекларацииПоНДС.СтруктураДокПдтвУпл());
	ОпределяемыеТипы.Вставить("ДатаУчТов", '00010101');
	ОпределяемыеТипы.Вставить("ПериодЗап", Документы.ПоясненияКДекларацииПоНДС.СтруктураПериодЗап());
	
	Возврат ПараметрыОтображенияПоУмолчанию("dd.MM.yyyy", ОпределяемыеТипы);
	
КонецФункции

Функция ПараметрыОтображенияРаздел9()
	
	ОпределяемыеТипы = Новый Структура;
	ОпределяемыеТипы.Вставить("СпрКодОш", Документы.ПоясненияКДекларацииПоНДС.СтруктураСпрКодОш());
	ОпределяемыеТипы.Вставить("ДокПдтвОпл", Документы.ПоясненияКДекларацииПоНДС.СтруктураДокПдтвОпл());
	ОпределяемыеТипы.Вставить("ДатаУчТов", '00010101');
	ОпределяемыеТипы.Вставить("ПериодЗап", Документы.ПоясненияКДекларацииПоНДС.СтруктураПериодЗап());
	
	Возврат ПараметрыОтображенияПоУмолчанию("dd.MM.yyyy", ОпределяемыеТипы);
	
КонецФункции

Функция ПараметрыОтображенияРаздел10()
	
	ОпределяемыеТипы = Новый Структура;
	ОпределяемыеТипы.Вставить("СпрКодОш", Документы.ПоясненияКДекларацииПоНДС.СтруктураСпрКодОш());
	ОпределяемыеТипы.Вставить("СвПосрДеят", Документы.ПоясненияКДекларацииПоНДС.СтруктураСвПосрДеят());
	ОпределяемыеТипы.Вставить("ПериодЗап", Документы.ПоясненияКДекларацииПоНДС.СтруктураПериодЗап());
	
	Возврат ПараметрыОтображенияПоУмолчанию("dd.MM.yyyy", ОпределяемыеТипы);
	
КонецФункции

Функция ПараметрыОтображенияРаздел11()
	
	ОпределяемыеТипы = Новый Структура;
	ОпределяемыеТипы.Вставить("СпрКодОш", Документы.ПоясненияКДекларацииПоНДС.СтруктураСпрКодОш());
	ОпределяемыеТипы.Вставить("ПериодЗап", Документы.ПоясненияКДекларацииПоНДС.СтруктураПериодЗап());
	
	Возврат ПараметрыОтображенияПоУмолчанию("dd.MM.yyyy", ОпределяемыеТипы);
	
КонецФункции

Функция ПараметрыОтображенияРаздел12()
	
	ОпределяемыеТипы = Новый Структура;
	ОпределяемыеТипы.Вставить("СпрКодОш", Документы.ПоясненияКДекларацииПоНДС.СтруктураСпрКодОш());
	ОпределяемыеТипы.Вставить("ПериодЗап", Документы.ПоясненияКДекларацииПоНДС.СтруктураПериодЗап());
	
	Возврат ПараметрыОтображенияПоУмолчанию("dd.MM.yyyy", ОпределяемыеТипы);
	
КонецФункции

Функция ПараметрыОтображенияНетКнигаПрод()
	
	ОпределяемыеТипы = Новый Структура;
	ОпределяемыеТипы.Вставить("СвКонтрагент", Документы.ПоясненияКДекларацииПоНДС.СтруктураСвКонтрагент());
	ОпределяемыеТипы.Вставить("СведСчФ", Документы.ПоясненияКДекларацииПоНДС.СтруктураСведСчФ());
	
	Возврат ПараметрыОтображенияПоУмолчанию("dd.MM.yyyy", ОпределяемыеТипы);
	
КонецФункции

Функция ПараметрыОтображенияСведКС()
	
	ОпределяемыеТипы = Новый Структура;
	Возврат ПараметрыОтображенияПоУмолчанию("dd.MM.yyyy", ОпределяемыеТипы);
	
КонецФункции

Процедура ЗаполнитьРаздел8(ДанныеТребования, ТребованиеXDTO)
	
	Если ТребованиеXDTO.Документ.ПрилТребНДС.Свойства().Получить("СведРазд8") = Неопределено Тогда
		Возврат;
	КонецЕсли;
	
	СведенияПоСтрокам = ТребованиеXDTO.Документ.ПрилТребНДС.СведРазд8.КнПокСтр;
	ЗаполнитьКоллекциюИзXDTO(ДанныеТребования.Раздел8, СведенияПоСтрокам, ПараметрыОтображенияРаздел8());
		
КонецПроцедуры

Процедура ЗаполнитьРаздел8_1(ДанныеТребования, ТребованиеXDTO)
	
	Если ТребованиеXDTO.Документ.ПрилТребНДС.Свойства().Получить("СведРазд8_1") = Неопределено Тогда
		Возврат;
	КонецЕсли;
	
	СведенияПоСтрокам = ТребованиеXDTO.Документ.ПрилТребНДС.СведРазд8_1.КнПокДЛСтр;
	ЗаполнитьКоллекциюИзXDTO(ДанныеТребования.Раздел8_1, СведенияПоСтрокам, ПараметрыОтображенияРаздел8());
	
КонецПроцедуры

Процедура ЗаполнитьРаздел9(ДанныеТребования, ТребованиеXDTO)
	
	Если ТребованиеXDTO.Документ.ПрилТребНДС.Свойства().Получить("СведРазд9") = Неопределено Тогда
		Возврат;
	КонецЕсли;
	
	СведенияПоСтрокам = ТребованиеXDTO.Документ.ПрилТребНДС.СведРазд9.КнПродСтр;
	
	ЗаполнитьКоллекциюИзXDTO(ДанныеТребования.Раздел9, СведенияПоСтрокам, ПараметрыОтображенияРаздел9());
	
КонецПроцедуры

Процедура ЗаполнитьРаздел9_1(ДанныеТребования, ТребованиеXDTO)
	
	Если ТребованиеXDTO.Документ.ПрилТребНДС.Свойства().Получить("СведРазд9_1") = Неопределено Тогда
		Возврат;
	КонецЕсли;
	
	СведенияПоСтрокам = ТребованиеXDTO.Документ.ПрилТребНДС.СведРазд9_1.КнПродДЛСтр;
	
	ЗаполнитьКоллекциюИзXDTO(ДанныеТребования.Раздел9_1, СведенияПоСтрокам, ПараметрыОтображенияРаздел9());
	
КонецПроцедуры

Процедура ЗаполнитьРаздел10(ДанныеТребования, ТребованиеXDTO)
	
	Если ТребованиеXDTO.Документ.ПрилТребНДС.Свойства().Получить("СведРазд10") = Неопределено Тогда
		Возврат;
	КонецЕсли;
	
	СведенияПоСтрокам = ТребованиеXDTO.Документ.ПрилТребНДС.СведРазд10.ЖУчВыстСчФСтр;
	
	ЗаполнитьКоллекциюИзXDTO(ДанныеТребования.Раздел10, СведенияПоСтрокам, ПараметрыОтображенияРаздел10());
	
КонецПроцедуры

Процедура ЗаполнитьРаздел11(ДанныеТребования, ТребованиеXDTO)
	
	Если ТребованиеXDTO.Документ.ПрилТребНДС.Свойства().Получить("СведРазд11") = Неопределено Тогда
		Возврат;
	КонецЕсли;
	
	СведенияПоСтрокам = ТребованиеXDTO.Документ.ПрилТребНДС.СведРазд11.ЖУчПолучСчФСтр;
	
	ЗаполнитьКоллекциюИзXDTO(ДанныеТребования.Раздел11, СведенияПоСтрокам, ПараметрыОтображенияРаздел11());
	
КонецПроцедуры

Процедура ЗаполнитьРаздел12(ДанныеТребования, ТребованиеXDTO)
	
	Если ТребованиеXDTO.Документ.ПрилТребНДС.Свойства().Получить("СведРазд12") = Неопределено Тогда
		Возврат;
	КонецЕсли;
	
	СведенияПоСтрокам = ТребованиеXDTO.Документ.ПрилТребНДС.СведРазд12.ВыстСчФ_173_5;
	
	ЗаполнитьКоллекциюИзXDTO(ДанныеТребования.Раздел12, СведенияПоСтрокам, ПараметрыОтображенияРаздел12());
	
КонецПроцедуры

Процедура ЗаполнитьНетКнигаПрод(ДанныеТребования, ТребованиеXDTO)
	
	Если ТребованиеXDTO.Документ.ПрилТребНДС.Свойства().Получить("НетКнигаПрод") = Неопределено Тогда
		Возврат;
	КонецЕсли;
	
	СведенияПоСтрокам = ТребованиеXDTO.Документ.ПрилТребНДС.НетКнигаПрод.НеподтвЗаписи;
	
	ЗаполнитьКоллекциюИзXDTO(ДанныеТребования.НетКнигаПрод, СведенияПоСтрокам, ПараметрыОтображенияНетКнигаПрод());
	
КонецПроцедуры

Процедура ЗаполнитьСведКС(ДанныеТребования, ТребованиеXDTO)
	
	Если ТребованиеXDTO.Документ.ПрилТребНДС.Свойства().Получить("СведКС") = Неопределено Тогда
		Возврат;
	КонецЕсли;
	
	СведенияПоСтрокам = ТребованиеXDTO.Документ.ПрилТребНДС.СведКС.СведКСОдин;
	
	ЗаполнитьКоллекциюИзXDTO(ДанныеТребования.СведКС, СведенияПоСтрокам, ПараметрыОтображенияСведКС());
	
КонецПроцедуры

Процедура ЗаполнитьПолеПоСвойству(Приемник, Источник, Свойство, ПараметрыОтображения = Неопределено);
	
	Если НЕ ЗначениеЗаполнено(Свойство) Тогда
		ЗначениеСвойства = Источник;
	ИначеЕсли Источник.Свойства().Получить(Свойство) <> Неопределено Тогда
		Если ТипЗнч(Источник[Свойство]) = Тип("СписокXDTO") Тогда
			ЗначениеСвойства = Источник[Свойство];
		Иначе
			ЗначениеСвойства = ПолучитьРекурсивноВложенныйЭлемент(Источник[Свойство]);
		КонецЕсли;
	Иначе
		Возврат;
	КонецЕсли;
	
	Если ЗначениеЗаполнено(ПараметрыОтображения) И ПараметрыОтображения.Свойство("ФорматДаты")
		И ТипЗнч(Приемник) = Тип("Дата") Тогда
		Приемник = СтрокаКакДата(ЗначениеСвойства, ПараметрыОтображения.ФорматДаты);
	ИначеЕсли ТипЗнч(Приемник) = Тип("Число") Тогда
		Приемник = Число(ЗначениеСвойства);
	ИначеЕсли ТипЗнч(Приемник) = Тип("Массив") И ТипЗнч(ЗначениеСвойства) = Тип("СписокXDTO") Тогда
		Для Каждого Элемент Из ЗначениеСвойства Цикл
			Приемник.Добавить(Элемент);
		КонецЦикла;
	ИначеЕсли ТипЗнч(Приемник) = Тип("Массив") Тогда
		Приемник.Добавить(ЗначениеСвойства);
	Иначе
		Приемник = ЗначениеСвойства;
	КонецЕсли;
	
КонецПроцедуры

Функция НомерТребованияИзXMLВложения(Приложение)
	
	ДанныеПриложения = Приложение.Данные.Получить();
	ИмяФайлаXML      = ПолучитьИмяВременногоФайла("xml");
	ДанныеПриложения.Записать(ИмяФайлаXML);
	
	ЧтениеXML = Новый ЧтениеXML;
	ЧтениеXML.ОткрытьФайл(ИмяФайлаXML);
	ЧтениеXML.ПерейтиКСодержимому();
	
	Фабрика = ФабрикаXDTO;
	Объект  = Фабрика.ПрочитатьXML(ЧтениеXML);
	ЧтениеXML.Закрыть();
	
	ОперацииСФайламиЭДКО.УдалитьВременныйФайл(ИмяФайлаXML);
	
	Если Объект.Свойства().Получить("Документ") = Неопределено Тогда
		Возврат "";
	Иначе
		
		Документ = Объект.Документ;
		Если Документ.Свойства().Получить("НомТреб") = Неопределено Тогда
			Возврат "";
		Иначе
		Возврат Объект.Документ.НомТреб;
	КонецЕсли;
	
	КонецЕсли;
	
КонецФункции

Функция ТребуетсяОтправкаПакетаСДопДокументами(Ссылка) Экспорт
	
	Если ТипЗнч(Ссылка) = Тип("СправочникСсылка.ДокументыРеализацииПолномочийНалоговыхОрганов") Тогда
		
		Возврат Ссылка.ВидДокумента = Перечисления.ВидыНалоговыхДокументов.ТребованиеОПредставленииУведомленияОКонтролируемыхИностранныхКомпаниях
			ИЛИ Ссылка.ВидДокумента = Перечисления.ВидыНалоговыхДокументов.УведомлениеОНепредставленииРасчетаПоСтраховымВзносам
			ИЛИ Ссылка.ВидДокумента = Перечисления.ВидыНалоговыхДокументов.ТребованиеОПредоставленииСтрановогоОтчета
			ИЛИ Ссылка.ВидДокумента = Перечисления.ВидыНалоговыхДокументов.ТребованиеОбУплатеНалогаСбораПениШтрафа
			ИЛИ Ссылка.ВидДокумента = Перечисления.ВидыНалоговыхДокументов.ТребованиеОПредставленииПоясненийКДекларацииНДС И НЕ ЕстьТребованияКРазделам8_12(Ссылка);
			
	Иначе
		Возврат Ложь;
	КонецЕсли;
		
КонецФункции

#КонецОбласти 

#Область ВыгрузкаОтветаНаТребованиеПоясненийКДекларацииНДС

Функция ПолучитьФайлВыгрузкиОтветаНаТребованиеПоясненийКДекларацииНДС(ДанныеДляВыгрузки, УникальныйИдентификатор) Экспорт
	
	Если Не ЗначениеЗаполнено(ДанныеДляВыгрузки) Тогда
		Возврат Неопределено;
	КонецЕсли;
	
	ИмяФайла = ПолучитьИмяВременногоФайла("xml");
	
	ЗаписьXML = Новый ЗаписьXML;
	ЗаписьXML.ОткрытьФайл(ИмяФайла, "windows-1251");
	ЗаписьXML.ЗаписатьОбъявлениеXML();
	
	ЗаписьXML.ЗаписатьНачалоЭлемента("Файл");
	ИдентификаторФайла = ИдентификаторФайла(ДанныеДляВыгрузки.Организация, ДанныеДляВыгрузки.КодНалоговогоОргана);
	
	ЗаписьXML.ЗаписатьАтрибут("xmlns:xsi", "http://www.w3.org/2001/XMLSchema-instance");
		
	ЗаписьXML.ЗаписатьАтрибут("ИдФайл", ИдентификаторФайла);
	ЗаписьXML.ЗаписатьАтрибут("ВерсПрог", РегламентированнаяОтчетность.НазваниеИВерсияПрограммы());
	
	ЗаписьXML.ЗаписатьАтрибут("ВерсФорм", "5.02");
	
	ЗаписьXML.ЗаписатьНачалоЭлемента("Документ");
	ЗаписьXML.ЗаписатьАтрибут("КНД", "1160200");
	ЗаписьXML.ЗаписатьАтрибут("ДатаДок", Формат(ТекущаяДатаСеанса(), "ДФ=dd.MM.yyyy"));
	
	ЗаписьXML.ЗаписатьНачалоЭлемента("СведТреб");
	
	ВыгрузитьКнигу(
		ЗаписьXML, 
		ДанныеДляВыгрузки.КнигаПокупок.СведенияСоответствуют, 
		ДанныеДляВыгрузки.КнигаПокупок.СведенияРасходятся, 
		"КнигаПокуп", 
		"КнигаПокупок");
		
	ВыгрузитьКнигу(
		ЗаписьXML, 
		ДанныеДляВыгрузки.КнигаПокупокДЛ.СведенияСоответствуют, 
		ДанныеДляВыгрузки.КнигаПокупокДЛ.СведенияРасходятся, 
		"КнигаПокупДЛ", 
		"КнигаПокупокДЛ");
		
	ВыгрузитьКнигу(
		ЗаписьXML, 
		ДанныеДляВыгрузки.КнигаПродаж.СведенияСоответствуют, 
		ДанныеДляВыгрузки.КнигаПродаж.СведенияРасходятся, 
		"КнигаПрод", 
		"КнигаПродаж");
		
	ВыгрузитьКнигу(
		ЗаписьXML, 
		ДанныеДляВыгрузки.КнигаПродажДЛ.СведенияСоответствуют, 
		ДанныеДляВыгрузки.КнигаПродажДЛ.СведенияРасходятся, 
		"КнигаПродДЛ", 
		"КнигаПродажДЛ");
		
	ВыгрузитьЖурналУчета(
		ЗаписьXML, 
		ДанныеДляВыгрузки.ЖурналУчетаВыставленныхСФ.СведенияСоответствуют, 
		ДанныеДляВыгрузки.ЖурналУчетаВыставленныхСФ.СведенияРасходятся, 
		"ЖУчВыстСчФ", 
		"ЖурналУчетаВыставленныхСФ");
		
	ВыгрузитьЖурналУчета(
		ЗаписьXML, 
		ДанныеДляВыгрузки.ЖурналУчетаПолученныхСФ.СведенияСоответствуют, 
		ДанныеДляВыгрузки.ЖурналУчетаПолученныхСФ.СведенияРасходятся, 
		"ЖУчПолучСчФ", 
		"ЖурналУчетаПолученныхСФ");
		
	ВыгрузитьВыставленныеСФ_173_5(
		ЗаписьXML, 
		ДанныеДляВыгрузки.ВыставленныеСФ_173_5.СведенияСоответствуют, 
		ДанныеДляВыгрузки.ВыставленныеСФ_173_5.СведенияРасходятся);
		
	ВыгрузитьСведКС(
		ЗаписьXML, 
		ДанныеДляВыгрузки.СведКС);
		
	ВыгрузитьНетКнигаПрод(
		ЗаписьXML, 
		ДанныеДляВыгрузки.НетКнигаПрод.НеподтвЗаписи, 
		ДанныеДляВыгрузки.НетКнигаПрод.СведенияРасходятся);
		
	ВыгрузитьПояснИнОсн(
		ЗаписьXML, 
		ДанныеДляВыгрузки.ПояснИнОсн);
		
	ЗаписьXML.ЗаписатьКонецЭлемента();
	ЗаписьXML.ЗаписатьКонецЭлемента();
	ЗаписьXML.ЗаписатьКонецЭлемента();
	
	ЗаписьXML.Закрыть();
	
	АдресФайла = ПоместитьВоВременноеХранилище(Новый ДвоичныеДанные(ИмяФайла), УникальныйИдентификатор);
	
	ИдФайл 		= ИдентификаторФайла + ".xml";
	ИдФайлЭП 	= ИмяФайлаЭПФайлаОписиИсходящихДокументов(ИдФайл);
	
	ДополнительныеПараметры = Новый Структура();
	ДополнительныеПараметры.Вставить("ИдФайл", 	 ИдФайл);
	ДополнительныеПараметры.Вставить("ИдФайлЭП", ИдФайлЭП);
	ДополнительныеПараметры.Вставить("АдресВременногоХранилища", АдресФайла);
	
	Возврат ДополнительныеПараметры;
	
КонецФункции

Функция ПолучитьФайлыПринадлежащиеОписи(ОписьСсылка, УникальныйИдентификаторФормы, СписокИменФайлов = Неопределено) Экспорт
	
	ВложенияСДанными = ПолучитьФайлыДокументовРеализацииПолномочийНалоговыхОрганов(ОписьСсылка, СписокИменФайлов);
	
	СоответствиеРезультат = Новый Соответствие;
	Для Каждого СтрВложение Из ВложенияСДанными Цикл
		
		СсылкаНаДвоичныеДанныеФайла = ПоместитьВоВременноеХранилище(СтрВложение.Данные.Получить(), УникальныйИдентификаторФормы);
		СоответствиеРезультат.Вставить(СтрВложение.ИмяФайла, СсылкаНаДвоичныеДанныеФайла);
		
	КонецЦикла;
	
	Возврат СоответствиеРезультат;
	
КонецФункции

Функция ПрименяетсяФорматОтветаНаТребованиеПояснений_5_02() Экспорт
	
	Возврат Истина;
	
КонецФункции

Процедура ЗаписатьАтрибутыСчетФактур(ЗаписьXML, СтрокаТаблицы, Колонки = Неопределено)
	
	ЗаписатьАтрибутПриНеобходимости(
		ЗаписьXML, "НомСчФПрод", XMLСтрока(СтрокаТаблицы.НомСчФПрод), Колонки);		
	ЗаписатьАтрибутПриНеобходимости(
		ЗаписьXML, "ДатаСчФПрод", Формат(СтрокаТаблицы.ДатаСчФПрод, "ДФ=dd.MM.yyyy"), Колонки);
	ЗаписатьАтрибутПриНеобходимости(
		ЗаписьXML, "НомИспрСчФ", ?(ЗначениеЗаполнено(СтрокаТаблицы.НомИспрСчФ), XMLСтрока(СтрокаТаблицы.НомИспрСчФ), ""), Колонки);		
	ЗаписатьАтрибутПриНеобходимости(
		ЗаписьXML, "ДатаИспрСчФ", Формат(СтрокаТаблицы.ДатаИспрСчФ, "ДФ=dd.MM.yyyy"), Колонки);
	ЗаписатьАтрибутПриНеобходимости(
		ЗаписьXML, "НомКСчФПрод", ?(ЗначениеЗаполнено(СтрокаТаблицы.НомКСчФПрод), XMLСтрока(СтрокаТаблицы.НомКСчФПрод), ""), Колонки);
	ЗаписатьАтрибутПриНеобходимости(
		ЗаписьXML, "ДатаКСчФПрод", Формат(СтрокаТаблицы.ДатаКСчФПрод, "ДФ=dd.MM.yyyy"), Колонки);
	ЗаписатьАтрибутПриНеобходимости(
		ЗаписьXML, "НомИспрКСчФ", ?(ЗначениеЗаполнено(СтрокаТаблицы.НомИспрКСчФ), XMLСтрока(СтрокаТаблицы.НомИспрКСчФ), ""), Колонки);
	ЗаписатьАтрибутПриНеобходимости(
		ЗаписьXML, "ДатаИспрКСчФ", Формат(СтрокаТаблицы.ДатаИспрКСчФ, "ДФ=dd.MM.yyyy"), Колонки);	

КонецПроцедуры

Процедура ЗаписатьАтрибутПриНеобходимости(ЗаписьXML, Имя, Значение, Колонки = Неопределено)
	
	Если (Не ЗначениеЗаполнено(Колонки) ИЛИ Колонки.Найти(Имя) <> Неопределено)
		И ЗначениеЗаполнено(Значение) Тогда
		ЗаписьXML.ЗаписатьАтрибут(Имя, Значение);
	КонецЕсли;
		
КонецПроцедуры

Процедура ЗаписатьСтрокуКниги(ЗаписьXML, СтрокаТаблицы, ТипКниги, Колонки = Неопределено)
	
	Если ТипКниги = "КнигаПокупок" Тогда
		ИмяУзла = "КнПокСтр";
	ИначеЕсли ТипКниги = "КнигаПокупокДЛ" Тогда
		ИмяУзла = "КнПокДЛСтр";
	ИначеЕсли ТипКниги = "КнигаПродаж" Тогда
		ИмяУзла = "КнПродСтр";
	Иначе
		ИмяУзла = "КнПродДЛСтр";
	КонецЕсли;
	
	ЗаписьXML.ЗаписатьНачалоЭлемента(ИмяУзла);
	ЗаписатьАтрибутыСчетФактур(ЗаписьXML, СтрокаТаблицы, Колонки);
	Если ТипКниги = "КнигаПокупок" ИЛИ ТипКниги = "КнигаПокупокДЛ" Тогда
		ЗаписатьАтрибутПриНеобходимости(ЗаписьXML, "НомТД", СтрокаТаблицы.НомТД, Колонки);
		ЗаписатьАтрибутПриНеобходимости(
			ЗаписьXML, "СтоимПокупВ", Формат(СтрокаТаблицы.СтоимПокупВ, "ЧДЦ=2; ЧРД=.; ЧН=; ЧГ="), Колонки);
	Иначе
		
		// Таблица 4.59
		// Сведения по строке из книги продаж (дополнительных листов книги продаж) (КнПродСтрТип)
		// СтоимПродСФВ - НУ - Элемент обязателен при наличии элемента <ОКВ> и значение элемента <ОКВ> не равно 643
		Если ЗначениеЗаполнено(Колонки) И Колонки.Найти("ОКВ") <> Неопределено Тогда
			Если ЗначениеЗаполнено(СтрокаТаблицы.СтоимПродСФВ) Тогда
				ЗаписатьАтрибутПриНеобходимости(
					ЗаписьXML, "СтоимПродСФВ", Формат(СтрокаТаблицы.СтоимПродСФВ, "ЧДЦ=2; ЧРД=.; ЧН=; ЧГ="));
			КонецЕсли;
		Иначе
			Если ЗначениеЗаполнено(СтрокаТаблицы.СтоимПродСФВ) Тогда
				ЗаписатьАтрибутПриНеобходимости(
					ЗаписьXML, "СтоимПродСФВ", Формат(СтрокаТаблицы.СтоимПродСФВ, "ЧДЦ=2; ЧРД=.; ЧН=; ЧГ="), Колонки);
			КонецЕсли;
		КонецЕсли;
				
	КонецЕсли;
	
	ЗаписатьАтрибутПриНеобходимости(ЗаписьXML, "ОКВ", СтрокаТаблицы.ОКВ, Колонки);	
	
	Если Не ЗначениеЗаполнено(Колонки) ИЛИ Колонки.Найти("КодВидОпер") <> Неопределено Тогда
		Для Каждого Элемент Из СтрокаТаблицы.КодВидОпер Цикл
			ЗаписьXML.ЗаписатьНачалоЭлемента("КодВидОпер");
			ЗаписьXML.ЗаписатьТекст(Элемент);
			ЗаписьXML.ЗаписатьКонецЭлемента();
		КонецЦикла;
	КонецЕсли;

	Если Не ЗначениеЗаполнено(Колонки) ИЛИ Колонки.Найти("ДокПдтвОпл") <> Неопределено Тогда
		Для Каждого Элемент Из СтрокаТаблицы.ДокПдтвОпл Цикл
			ЗаписатьДокПдтвОпл(ЗаписьXML, Элемент);
		КонецЦикла;
	КонецЕсли;
	
	Если ТипКниги = "КнигаПокупок" ИЛИ ТипКниги = "КнигаПокупокДЛ" Тогда
		Если Не ЗначениеЗаполнено(Колонки) ИЛИ Колонки.Найти("ДатаУчТов") <> Неопределено Тогда
			Для Каждого Элемент Из СтрокаТаблицы.ДатаУчТов Цикл
				ЗаписьXML.ЗаписатьНачалоЭлемента("ДатаУчТов");
				ЗаписьXML.ЗаписатьТекст(Формат(Элемент, "ДФ=dd.MM.yyyy"));
				ЗаписьXML.ЗаписатьКонецЭлемента();
			КонецЦикла;
		КонецЕсли;
		
		Если Не ЗначениеЗаполнено(Колонки) ИЛИ Колонки.Найти("СвПрод") <> Неопределено Тогда			
			Для Каждого Элемент Из СтрокаТаблицы.СвПрод Цикл
				ЗаписатьДанныеКонтрагента(ЗаписьXML, Элемент, "СвПрод");
			КонецЦикла;
		КонецЕсли;
	Иначе
		Если Не ЗначениеЗаполнено(Колонки) ИЛИ Колонки.Найти("СвПокуп") <> Неопределено Тогда
			Для Каждого Элемент Из СтрокаТаблицы.СвПокуп Цикл
				ЗаписатьДанныеКонтрагента(ЗаписьXML, Элемент, "СвПокуп");
			КонецЦикла;
		КонецЕсли;
	КонецЕсли;
	Если Не ЗначениеЗаполнено(Колонки) ИЛИ Колонки.Найти("СвПос") <> Неопределено Тогда
		ЗаписатьДанныеКонтрагента(ЗаписьXML, СтрокаТаблицы.СвПос, "СвПос");
	КонецЕсли;

	ЗаписьXML.ЗаписатьКонецЭлемента();
	
КонецПроцедуры

Процедура ЗаписатьСтрокуЖурнала(ЗаписьXML, СтрокаТаблицы, ТипЖурнала, Колонки = Неопределено)
	
	Если ТипЖурнала = "ЖурналУчетаВыставленныхСФ" Тогда
		ИмяУзла = "ЖУчВСтр";
	Иначе
		ИмяУзла = "ЖУчПСтр";
	КонецЕсли;
	ЗаписьXML.ЗаписатьНачалоЭлемента(ИмяУзла);
	ЗаписатьАтрибутыСчетФактур(ЗаписьXML, СтрокаТаблицы, Колонки);
	
	Если ТипЖурнала = "ЖурналУчетаПолученныхСФ" Тогда
		ЗаписатьАтрибутПриНеобходимости(
			ЗаписьXML, "КодВидСд", СтрокаТаблицы.КодВидСд, Колонки);
		ЗаписатьАтрибутПриНеобходимости(
			ЗаписьXML, "ОКВ", СтрокаТаблицы.ОКВ, Колонки);
		ЗаписатьАтрибутПриНеобходимости(
			ЗаписьXML, "СтоимТовСчФВс", Формат(СтрокаТаблицы.СтоимТовСчФВс, "ЧДЦ=2; ЧРД=.; ЧН=; ЧГ="), Колонки);		
	КонецЕсли;
	
	Если Не ЗначениеЗаполнено(Колонки) ИЛИ Колонки.Найти("КодВидОпер") <> Неопределено Тогда
		Для Каждого Элемент Из СтрокаТаблицы.КодВидОпер Цикл
			ЗаписьXML.ЗаписатьНачалоЭлемента("КодВидОпер");
			ЗаписьXML.ЗаписатьТекст(Элемент);
			ЗаписьXML.ЗаписатьКонецЭлемента();
		КонецЦикла;
	КонецЕсли;

	Если ТипЖурнала = "ЖурналУчетаВыставленныхСФ" Тогда
		Если Не ЗначениеЗаполнено(Колонки) ИЛИ Колонки.Найти("СвПокуп") <> Неопределено Тогда
			ЗаписатьДанныеКонтрагента(ЗаписьXML, СтрокаТаблицы.СвПокуп, "СвПокуп");
		КонецЕсли;
		
		Если Не ЗначениеЗаполнено(Колонки) ИЛИ Колонки.Найти("СвПосрДеят") <> Неопределено Тогда
			Для Каждого Элемент Из СтрокаТаблицы.СвПосрДеят Цикл
				ЗаписьXML.ЗаписатьНачалоЭлемента("СвПосрДеят");
				ЗаписьXML.ЗаписатьАтрибут("НомСчФОтПрод", Элемент.НомСчФОтПрод);
				ЗаписьXML.ЗаписатьАтрибут("ДатаСчФОтПрод", Формат(Элемент.ДатаСчФОтПрод, "ДФ=dd.MM.yyyy"));			
				ЗаписатьАтрибутПриНеобходимости(ЗаписьXML,"ОКВ", Элемент.ОКВ);
				ЗаписатьАтрибутПриНеобходимости(ЗаписьXML,"СтоимТовСчФВс", Формат(Элемент.СтоимТовСчФВс, "ЧДЦ=2; ЧРД=.; ЧН=; ЧГ="));
				ЗаписатьДанныеКонтрагента(ЗаписьXML, Элемент.СвПрод, "СвПрод");
				ЗаписьXML.ЗаписатьКонецЭлемента();
			КонецЦикла;
		КонецЕсли;
	Иначе
		Если Не ЗначениеЗаполнено(Колонки) ИЛИ Колонки.Найти("СвПрод") <> Неопределено Тогда
			ЗаписатьДанныеКонтрагента(ЗаписьXML, СтрокаТаблицы.СвПрод, "СвПрод");
		КонецЕсли;
		Если Не ЗначениеЗаполнено(Колонки) ИЛИ Колонки.Найти("СвКомис") <> Неопределено Тогда
			ЗаписатьДанныеКонтрагента(ЗаписьXML, СтрокаТаблицы.СвКомис, "СвКомис");
		КонецЕсли;
	КонецЕсли;
	
	ЗаписьXML.ЗаписатьКонецЭлемента();
	
КонецПроцедуры
	
Процедура ЗаписатьДокПдтвОпл(ЗаписьXML, ДокПдтвОпл)
	
	ЗаписьXML.ЗаписатьНачалоЭлемента("ДокПдтвОпл");
	ЗаписьXML.ЗаписатьАтрибут("НомДокПдтвОпл", ДокПдтвОпл.НомДокПдтвОпл);
	ЗаписьXML.ЗаписатьАтрибут("ДатаДокПдтвОпл", Формат(ДокПдтвОпл.ДатаДокПдтвОпл, "ДФ=dd.MM.yyyy"));
	ЗаписьXML.ЗаписатьКонецЭлемента();
	
КонецПроцедуры

Процедура ЗаписатьДанныеКонтрагента(ЗаписьXML, ДанныеКонтрагента, ИмяУзла)
	
	Если ТипЗнч(ДанныеКонтрагента) = Тип("Структура") Тогда
		ИНН = СокрЛП(ДанныеКонтрагента.ИНН);
		КПП = СокрЛП(ДанныеКонтрагента.КПП);
	Иначе
		ИНН = СокрЛП(ДанныеКонтрагента);
		КПП = "";
	КонецЕсли;
	
	Если ЗначениеЗаполнено(ИНН) ИЛИ ЗначениеЗаполнено(КПП) Тогда
		ЗаписьXML.ЗаписатьНачалоЭлемента(ИмяУзла);
		Если СтрДлина(ИНН) = 10 Тогда
			ЗаписьXML.ЗаписатьНачалоЭлемента("СведЮЛ");
			
			Если ЗначениеЗаполнено(ИНН) Тогда
				ЗаписьXML.ЗаписатьАтрибут("ИННЮЛ", ИНН);
			КонецЕсли;
			Если ЗначениеЗаполнено(КПП) Тогда
				ЗаписьXML.ЗаписатьАтрибут("КПП", КПП);
			КонецЕсли;
			
			ЗаписьXML.ЗаписатьКонецЭлемента();
		Иначе
			ЗаписьXML.ЗаписатьНачалоЭлемента("СведИП");
			ЗаписьXML.ЗаписатьАтрибут("ИННФЛ", ИНН);
			ЗаписьXML.ЗаписатьКонецЭлемента();
		КонецЕсли;
		ЗаписьXML.ЗаписатьКонецЭлемента();
	КонецЕсли;	
	
КонецПроцедуры

Процедура ВыгрузитьКнигу(ЗаписьXML, СведенияСоответствуют, СведенияРасходятся, ИмяУзла, ТипКниги)
	
	Если Не ЗначениеЗаполнено(СведенияСоответствуют)
		И Не ЗначениеЗаполнено(СведенияРасходятся.Расхождение) Тогда
		Возврат;
	КонецЕсли;
	
	ЗаписьXML.ЗаписатьНачалоЭлемента(ИмяУзла);
	
	Если ЗначениеЗаполнено(СведенияСоответствуют) Тогда
		ЗаписьXML.ЗаписатьНачалоЭлемента("СведСоотв");
		
		Для Каждого СтрокаТаблицы Из СведенияСоответствуют Цикл
			ЗаписьXML.ЗаписатьНачалоЭлемента("СоотвСтр");
			ЗаписьXML.ЗаписатьАтрибут("НомерПор", XMLСтрока(СтрокаТаблицы.НомерПор));
			ЗаписатьСтрокуКниги(ЗаписьXML, СтрокаТаблицы, ТипКниги);
			ЗаписьXML.ЗаписатьКонецЭлемента();
		КонецЦикла;
		
		ЗаписьXML.ЗаписатьКонецЭлемента();
	КонецЕсли;

	Если ЗначениеЗаполнено(СведенияРасходятся.Расхождение) Тогда
		ЗаписьXML.ЗаписатьНачалоЭлемента("СведРасх");
		
		СведенияРасходятся.Пояснение.Индексы.Добавить("НомерПор");
		Для Каждого СтрокаТаблицы Из СведенияРасходятся.Расхождение Цикл
			НайденнаяСтрока = СведенияРасходятся.Пояснение.Найти(СтрокаТаблицы.НомерПор, "НомерПор");
			
			Колонки = ПолучитьКолонкиВКоторыхЗначенияОтличаются(СтрокаТаблицы, НайденнаяСтрока);
			
			ЗаписьXML.ЗаписатьНачалоЭлемента("РасхСтр");
			ЗаписьXML.ЗаписатьАтрибут("НомерПор", XMLСтрока(СтрокаТаблицы.НомерПор));
			
			ЗаписьXML.ЗаписатьНачалоЭлемента("Расхожд");
			ЗаписьXML.ЗаписатьАтрибут("ТипИнф", "1");
			ЗаписатьСтрокуКниги(ЗаписьXML, СтрокаТаблицы, ТипКниги);
			ЗаписьXML.ЗаписатьКонецЭлемента();
			
			
			Если НайденнаяСтрока <> Неопределено Тогда
				ЗаписьXML.ЗаписатьНачалоЭлемента("Пояснен");
				ЗаписьXML.ЗаписатьАтрибут("ТипИнф", "2");
				ЗаписатьСтрокуКниги(ЗаписьXML, НайденнаяСтрока, ТипКниги, Колонки);
				ЗаписьXML.ЗаписатьКонецЭлемента();
			КонецЕсли;
			
			ЗаписьXML.ЗаписатьКонецЭлемента();
		КонецЦикла;
		
		ЗаписьXML.ЗаписатьКонецЭлемента();
	КонецЕсли;
	
	ЗаписьXML.ЗаписатьКонецЭлемента();
	
КонецПроцедуры

Функция ПолучитьКолонкиВКоторыхЗначенияОтличаются(Строка1, Строка2)
	
	КолонкиСРазличиями = Новый Массив;
	
	ВсеКолонки = Строка1.Владелец().Колонки;
	Для Каждого Колонка Из ВсеКолонки Цикл
		Если НЕ ОбщегоНазначения.ДанныеСовпадают(Строка1[Колонка.Имя], Строка2[Колонка.Имя]) <> 0 Тогда
			КолонкиСРазличиями.Добавить(Колонка.Имя);
		КонецЕсли;
	КонецЦикла;
	
	Возврат КолонкиСРазличиями;
	
КонецФункции

Процедура ВыгрузитьЖурналУчета(ЗаписьXML, СведенияСоответствуют, СведенияРасходятся, ИмяУзла, ТипЖурнала)
	
	Если Не ЗначениеЗаполнено(СведенияСоответствуют)
		И Не ЗначениеЗаполнено(СведенияРасходятся.Расхождение) Тогда
		Возврат;
	КонецЕсли;
	
	ЗаписьXML.ЗаписатьНачалоЭлемента(ИмяУзла);
	
	Если ЗначениеЗаполнено(СведенияСоответствуют) Тогда
		ЗаписьXML.ЗаписатьНачалоЭлемента("СведСоотв");
		
		Для Каждого СтрокаТаблицы Из СведенияСоответствуют Цикл
			ЗаписьXML.ЗаписатьНачалоЭлемента("СоотвСтр");
			ЗаписьXML.ЗаписатьАтрибут("НомерПор", XMLСтрока(СтрокаТаблицы.НомерПор));		
			ЗаписатьСтрокуЖурнала(ЗаписьXML, СтрокаТаблицы, ТипЖурнала);		
			ЗаписьXML.ЗаписатьКонецЭлемента();
		КонецЦикла;
		
		ЗаписьXML.ЗаписатьКонецЭлемента();
	КонецЕсли;
	
	Если ЗначениеЗаполнено(СведенияРасходятся.Расхождение) Тогда
		ЗаписьXML.ЗаписатьНачалоЭлемента("СведРасх");
		
		СведенияРасходятся.Пояснение.Индексы.Добавить("НомерПор");
		Для Каждого СтрокаТаблицы Из СведенияРасходятся.Расхождение Цикл
			НайденнаяСтрока = СведенияРасходятся.Пояснение.Найти(СтрокаТаблицы.НомерПор, "НомерПор");
			
			Колонки = ПолучитьКолонкиВКоторыхЗначенияОтличаются(СтрокаТаблицы, НайденнаяСтрока);
			
			ЗаписьXML.ЗаписатьНачалоЭлемента("РасхСтр");
			ЗаписьXML.ЗаписатьАтрибут("НомерПор", XMLСтрока(СтрокаТаблицы.НомерПор));
			
			ЗаписьXML.ЗаписатьНачалоЭлемента("Расхожд");
			ЗаписьXML.ЗаписатьАтрибут("ТипИнф", "1");
			ЗаписатьСтрокуЖурнала(ЗаписьXML, СтрокаТаблицы, ТипЖурнала);
			ЗаписьXML.ЗаписатьКонецЭлемента();
			
			Если НайденнаяСтрока <> Неопределено Тогда
				ЗаписьXML.ЗаписатьНачалоЭлемента("Пояснен");
				ЗаписьXML.ЗаписатьАтрибут("ТипИнф", "2");
				ЗаписатьСтрокуЖурнала(ЗаписьXML, НайденнаяСтрока, ТипЖурнала, Колонки);
				ЗаписьXML.ЗаписатьКонецЭлемента();
			КонецЕсли;
			
			ЗаписьXML.ЗаписатьКонецЭлемента();
		КонецЦикла;
		
		ЗаписьXML.ЗаписатьКонецЭлемента();
	КонецЕсли;

	ЗаписьXML.ЗаписатьКонецЭлемента();
	
КонецПроцедуры

Процедура ЗаписатьСтрокуВыставленныеСФ_173_5(ЗаписьXML, СтрокаТаблицы, Колонки = Неопределено)
	
	ЗаписатьАтрибутПриНеобходимости(
		ЗаписьXML, "НомСчФ", СтрокаТаблицы.НомСчФ, Колонки);
	ЗаписатьАтрибутПриНеобходимости(
		ЗаписьXML, "ДатаСчФ", Формат(СтрокаТаблицы.ДатаСчФ, "ДФ=dd.MM.yyyy"), Колонки);
	ЗаписатьАтрибутПриНеобходимости(
		ЗаписьXML, "ОКВ", СтрокаТаблицы.ОКВ, Колонки);
	ЗаписатьАтрибутПриНеобходимости(
		ЗаписьXML, "СтоимТовБНалВс", Формат(СтрокаТаблицы.СтоимТовБНалВс, "ЧДЦ=2; ЧРД=.; ЧН=; ЧГ="), Колонки);
	ЗаписатьАтрибутПриНеобходимости(
		ЗаписьXML, "СтоимТовСНалВс", Формат(СтрокаТаблицы.СтоимТовСНалВс, "ЧДЦ=2; ЧРД=.; ЧН=; ЧГ="), Колонки);
		
	Если Не ЗначениеЗаполнено(Колонки) ИЛИ Колонки.Найти("СвПокуп") <> Неопределено Тогда	
		ЗаписатьДанныеКонтрагента(ЗаписьXML, СтрокаТаблицы.СвПокуп, "СвПокуп");
	КонецЕсли;

КонецПроцедуры

Процедура ВыгрузитьВыставленныеСФ_173_5(ЗаписьXML, СведенияСоответствуют, СведенияРасходятся)
	
	Если Не ЗначениеЗаполнено(СведенияСоответствуют)
		И Не ЗначениеЗаполнено(СведенияРасходятся.Расхождение) Тогда
		Возврат;
	КонецЕсли;
	
	ЗаписьXML.ЗаписатьНачалоЭлемента("ВыстСчФ_173.5");
	
	Если ЗначениеЗаполнено(СведенияСоответствуют) Тогда
		ЗаписьXML.ЗаписатьНачалоЭлемента("СведСоотв");
		
		Для Каждого СтрокаТаблицы Из СведенияСоответствуют Цикл
			ЗаписьXML.ЗаписатьНачалоЭлемента("СведСоотвСчФ");
			ЗаписатьСтрокуВыставленныеСФ_173_5(ЗаписьXML, СтрокаТаблицы);		
			ЗаписьXML.ЗаписатьКонецЭлемента();
		КонецЦикла;
		
		ЗаписьXML.ЗаписатьКонецЭлемента();
	КонецЕсли;
	
	Если ЗначениеЗаполнено(СведенияРасходятся.Расхождение) Тогда
		ЗаписьXML.ЗаписатьНачалоЭлемента("СведРасх");
		
		СведенияРасходятся.Пояснение.Индексы.Добавить("НомерПор");
		Для Каждого СтрокаТаблицы Из СведенияРасходятся.Расхождение Цикл
			НайденнаяСтрока = СведенияРасходятся.Пояснение.Найти(СтрокаТаблицы.НомерПор, "НомерПор");
			
			Колонки = ПолучитьКолонкиВКоторыхЗначенияОтличаются(СтрокаТаблицы, НайденнаяСтрока);
			
			ЗаписьXML.ЗаписатьНачалоЭлемента("СведРасхСчФ");
			ЗаписьXML.ЗаписатьНачалоЭлемента("Расхожд");
			ЗаписьXML.ЗаписатьАтрибут("ТипИнф", "1");
			ЗаписьXML.ЗаписатьНачалоЭлемента("ВСчФ_173.5");
			ЗаписатьСтрокуВыставленныеСФ_173_5(ЗаписьXML, СтрокаТаблицы);
			ЗаписьXML.ЗаписатьКонецЭлемента();
			ЗаписьXML.ЗаписатьКонецЭлемента();
			
			Если НайденнаяСтрока <> Неопределено Тогда
				ЗаписьXML.ЗаписатьНачалоЭлемента("Пояснен");
				ЗаписьXML.ЗаписатьАтрибут("ТипИнф", "2");
				ЗаписьXML.ЗаписатьНачалоЭлемента("ВСчФ_173.5");
				ЗаписатьСтрокуВыставленныеСФ_173_5(ЗаписьXML, НайденнаяСтрока, Колонки);
				ЗаписьXML.ЗаписатьКонецЭлемента();
				ЗаписьXML.ЗаписатьКонецЭлемента();
			КонецЕсли;
			
			ЗаписьXML.ЗаписатьКонецЭлемента();
		КонецЦикла;
		
		ЗаписьXML.ЗаписатьКонецЭлемента();
	КонецЕсли;
	
	ЗаписьXML.ЗаписатьКонецЭлемента();
	
КонецПроцедуры

Процедура ВыгрузитьСведКС(ЗаписьXML, СведКС)
	
	Если НЕ ЗначениеЗаполнено(СведКС)
		ИЛИ СведКС.Количество() = 0 Тогда
		Возврат;
	КонецЕсли;
	
	Для Каждого СтрокаТаблицы Из СведКС Цикл
		
		ЗаписьXML.ЗаписатьНачалоЭлемента("СведКС");
		
		ЗаписатьАтрибутПриНеобходимости(
			ЗаписьXML, "НомКС", СтрокаТаблицы.НомКС);
			
		ЗаписатьАтрибутПриНеобходимости(
			ЗаписьXML, "ПоясненКС", СтрокаТаблицы.ПоясненКС);
			
		ЗаписьXML.ЗаписатьКонецЭлемента();
		
	КонецЦикла;
	
КонецПроцедуры

Процедура ВыгрузитьНетКнигаПрод(ЗаписьXML, НеподтвЗаписи, СведенияРасходятся)
	
	Если Не ЗначениеЗаполнено(НеподтвЗаписи)
		И Не ЗначениеЗаполнено(СведенияРасходятся.Расхождение) Тогда
		Возврат;
	КонецЕсли;
	
	ЗаписьXML.ЗаписатьНачалоЭлемента("НетКнигаПрод");
	
	Если ЗначениеЗаполнено(НеподтвЗаписи) Тогда
		
		Для Каждого СтрокаТаблицы Из НеподтвЗаписи Цикл
			
			ЗаписьXML.ЗаписатьНачалоЭлемента("НеподтвЗаписи");
			
			ЗаписатьАтрибутПриНеобходимости(
				ЗаписьXML, "НомСчФПрод", СтрокаТаблицы.НомСчФПрод);
				
			ЗаписатьАтрибутПриНеобходимости(
				ЗаписьXML, "ДатаСчФПрод", Формат(СтрокаТаблицы.ДатаСчФПрод, "ДФ=dd.MM.yyyy"));
				
			ЗаписатьДанныеКонтрагента(ЗаписьXML, СтрокаТаблицы.СвКонтрагент, "СвКонтрагент");
			
			ЗаписьXML.ЗаписатьКонецЭлемента();
			
		КонецЦикла;
		
	КонецЕсли;
	
	Если ЗначениеЗаполнено(СведенияРасходятся.Расхождение) Тогда
		
		Для Каждого СтрокаТаблицы Из СведенияРасходятся.Расхождение Цикл
			
			ЗаписьXML.ЗаписатьНачалоЭлемента("СведРасх");
			
			НайденнаяСтрока = СведенияРасходятся.Пояснение.Найти(СтрокаТаблицы.НомерСтроки, "НомерСтроки");
			
			ЗаписьXML.ЗаписатьНачалоЭлемента("Расхожд");
			ЗаписьXML.ЗаписатьАтрибут("ТипИнф", "1");
			ЗаписатьСтрокуНетКнигаПрод(ЗаписьXML, СтрокаТаблицы);
			ЗаписьXML.ЗаписатьКонецЭлемента();
			
			Если НайденнаяСтрока <> Неопределено Тогда
				ЗаписьXML.ЗаписатьНачалоЭлемента("Пояснен");
				ЗаписьXML.ЗаписатьАтрибут("ТипИнф", "2");
				ЗаписатьСтрокуНетКнигаПрод(ЗаписьXML, НайденнаяСтрока);
				ЗаписьXML.ЗаписатьКонецЭлемента();
			КонецЕсли;

			ЗаписьXML.ЗаписатьКонецЭлемента();
			
		КонецЦикла;
		
	КонецЕсли;
	
	ЗаписьXML.ЗаписатьКонецЭлемента();
	
КонецПроцедуры

Процедура ЗаписатьСтрокуНетКнигаПрод(ЗаписьXML, СтрокаТаблицы, Колонки = Неопределено)
	
	ЗаписьXML.ЗаписатьНачалоЭлемента("НеКнПрСтр");
	
	ЗаписатьАтрибутПриНеобходимости(
		ЗаписьXML, "НомСчФПрод", СтрокаТаблицы.НомСчФПрод, Колонки);
		
	Если ЗначениеЗаполнено(СтрокаТаблицы.ДатаСчФПрод) Тогда	
		ЗаписатьАтрибутПриНеобходимости(
			ЗаписьXML, "ДатаСчФПрод", Формат(СтрокаТаблицы.ДатаСчФПрод, "ДФ=dd.MM.yyyy"), Колонки);
	КонецЕсли;
	
	Если ЗначениеЗаполнено(СтрокаТаблицы.СтоимПродСФВ) Тогда
		ЗаписатьАтрибутПриНеобходимости(
			ЗаписьXML, "СтоимПродСФВ", Формат(СтрокаТаблицы.СтоимПродСФВ, "ЧДЦ=2; ЧРД=.; ЧН=; ЧГ="), Колонки);
	КонецЕсли;
	
	Если ЗначениеЗаполнено(СтрокаТаблицы.СтоимПродСФ) Тогда
		ЗаписатьАтрибутПриНеобходимости(
			ЗаписьXML, "СтоимПродСФ", Формат(СтрокаТаблицы.СтоимПродСФ, "ЧДЦ=2; ЧРД=.; ЧН=; ЧГ="), Колонки);
	КонецЕсли;
			
	ЗаписатьАтрибутПриНеобходимости(
		ЗаписьXML, "Раздел", СтрокаТаблицы.Раздел, Колонки);
		
	Если Не ЗначениеЗаполнено(Колонки) ИЛИ Колонки.Найти("СвПокуп") <> Неопределено Тогда	
		ЗаписатьДанныеКонтрагента(ЗаписьXML, СтрокаТаблицы.СвПокуп, "СвПокуп");
	КонецЕсли;
	
	Если Не ЗначениеЗаполнено(Колонки) ИЛИ Колонки.Найти("СвПосред") <> Неопределено Тогда	
		ЗаписатьДанныеКонтрагента(ЗаписьXML, СтрокаТаблицы.СвПосред, "СвПосред");
	КонецЕсли;
	
	Если Не ЗначениеЗаполнено(Колонки) ИЛИ Колонки.Найти("КодВидОпер") <> Неопределено Тогда
		Если ЗначениеЗаполнено(СтрокаТаблицы.КодВидОпер) Тогда
			ЗаписьXML.ЗаписатьНачалоЭлемента("КодВидОпер");
			ЗаписьXML.ЗаписатьТекст(СтрокаТаблицы.КодВидОпер);
			ЗаписьXML.ЗаписатьКонецЭлемента();
		КонецЕсли;
	КонецЕсли;
	
	ЗаписьXML.ЗаписатьКонецЭлемента();
	
КонецПроцедуры

Процедура ВыгрузитьПояснИнОсн(ЗаписьXML, ПояснИнОсн)
	
	Если НЕ ЗначениеЗаполнено(ПояснИнОсн) Тогда
		Возврат;
	КонецЕсли;
	
	ЗаписьXML.ЗаписатьНачалоЭлемента("ПояснИнОсн");
	ЗаписьXML.ЗаписатьТекст(ПояснИнОсн);
	ЗаписьXML.ЗаписатьКонецЭлемента();
	
КонецПроцедуры

Функция ИдентификаторФайла(Организация, КодНалоговогоОргана)
	
	ЭлРегистрацияВИФНС = РегистрацияВИФНСПоОрганизацииИКодуНО(Организация, КодНалоговогоОргана);
	
	ПараметрыОписи = СформироватьПараметрыОписиИсходящихДокументов(Организация, ЭлРегистрацияВИФНС, КодНалоговогоОргана);
	
	// формируем идентификатор отправителя
	Если ПараметрыОписи.ПрПодп = 2 Тогда
		СвПред = ПараметрыОписи.Доверенность.СвУпПред.СвПред;
		Если СвПред.Свойство("СвОрг") Тогда
			ИННОтправителя = СокрЛП(СвПред.СвОрг.ИННЮЛ);
			КППОтправителя = СокрЛП(СвПред.СвОрг.КПП);
		Иначе
			Если НЕ ПараметрыОписи.Доверенность.СвУпПред.СвПред.СведФизЛ.ЯвляетсяСотрудникомОрганизации Тогда
				ИННОтправителя = СокрЛП(СвПред.СведФизЛ.ИННФЛ);
				КППОтправителя = "";
			Иначе
				ИННОтправителя = ?(ПараметрыОписи.ЭтоПБОЮЛ, СокрЛП(ПараметрыОписи.ИННФЛ), СокрЛП(ПараметрыОписи.ИННЮЛ));
				КППОтправителя = ?(ПараметрыОписи.ЭтоПБОЮЛ, "", СокрЛП(ПараметрыОписи.КППЮЛ));
			КонецЕсли;
		КонецЕсли;
	Иначе
		ИННОтправителя = ?(ПараметрыОписи.ЭтоПБОЮЛ, СокрЛП(ПараметрыОписи.ИННФЛ), СокрЛП(ПараметрыОписи.ИННЮЛ));
		КППОтправителя = ?(ПараметрыОписи.ЭтоПБОЮЛ, "", СокрЛП(ПараметрыОписи.КППЮЛ));
	КонецЕсли;
	
	R_T = "ON_OTTRNDS";
	
	Если Организация.КрупнейшийНалогоплательщик 
		И ЗначениеЗаполнено(Организация.КодНалоговогоОрганаПолучателя) Тогда
		КодНалоговогоОрганаПосредника = СокрЛП(Организация.КодНалоговогоОрганаПолучателя);
	Иначе
		КодНалоговогоОрганаПосредника = КодНалоговогоОргана;
	КонецЕсли;
	
	A_K = КодНалоговогоОрганаПосредника + "_" + КодНалоговогоОргана;
	
	Если СтрДлина(ИННОтправителя) = 10 Тогда
		O = ИННОтправителя + КППОтправителя;
	ИначеЕсли СтрДлина(ИННОтправителя) = 12 Тогда
		O = ИННОтправителя;
	Иначе
		O = "000000000000";
	КонецЕсли;
	
	GGGGMMDD = Формат(ТекущаяДатаСеанса(), "ДФ=yyyyMMdd");
	
	N = ВРЕГ(Строка(Новый УникальныйИдентификатор));
	
	Возврат R_T + "_" + A_K + "_" + O + "_" + GGGGMMDD + "_" + N;
	
КонецФункции

Функция СформироватьПараметрыОписиИсходящихДокументов(Организация, ЭлРегистрацияВИФНС, КодНалоговогоОргана) Экспорт
	
	СведенияОбОрганизации = РегламентированнаяОтчетностьВызовСервера.ПолучитьСведенияОбОрганизации(Организация);
	
	ЭтоПБОЮЛ = НЕ РегламентированнаяОтчетностьВызовСервера.ЭтоЮридическоеЛицо(Организация);
	СведенияОбОрганизации.Вставить("ЭтоПБОЮЛ", ЭтоПБОЮЛ);
	
	// ищем РегистрацияВИФНС по организации и коду налогового органа
	СведенияОбОрганизации.Вставить("КодНО", КодНалоговогоОргана);
	Если ЗначениеЗаполнено(ЭлРегистрацияВИФНС) И ЗначениеЗаполнено(ЭлРегистрацияВИФНС.Доверенность) Тогда
		ДоверенностьПредставителя = ЭлРегистрацияВИФНС.Доверенность;
		// признак подписания представителем
		СведенияОбОрганизации.Вставить("ПрПодп", 2);
		СведенияОбОрганизации.Вставить("Доверенность", ПолучитьСведенияДоверенности(ЭлРегистрацияВИФНС, Организация));
	Иначе
		// признак подписания налогоплательщиком
		СведенияОбОрганизации.Вставить("ПрПодп", 1);
	КонецЕсли;
	
	Если ЭтоПБОЮЛ Тогда
		ФИОИП = ФИОИндивидуальногоПредпринимателя(Организация, СведенияОбОрганизации);
		Если ФИОИП = Неопределено Тогда
			СведенияОбОрганизации.Вставить("ИПФамилия", "");
			СведенияОбОрганизации.Вставить("ИПИмя", "");
			СведенияОбОрганизации.Вставить("ИПОтчество", "");
		Иначе
			СведенияОбОрганизации.Вставить("ИПФамилия", ФИОИП.Фамилия);
			СведенияОбОрганизации.Вставить("ИПИмя", ФИОИП.Имя);
			СведенияОбОрганизации.Вставить("ИПОтчество", ФИОИП.Отчество);
		КонецЕсли;
	Иначе
		Если ЗначениеЗаполнено(ЭлРегистрацияВИФНС) Тогда
			СведенияОбОрганизации.Вставить("КППЮЛ", СокрЛП(ЭлРегистрацияВИФНС.КПП));
		Иначе
			СведенияОбОрганизации.Вставить("КППЮЛ", СокрЛП(СведенияОбОрганизации.КППЮЛ));
		КонецЕсли;
	КонецЕсли;
	
	Возврат СведенияОбОрганизации;
	
КонецФункции

#КонецОбласти

#Область ПодписьФайлаXMLDSig

Функция КанонизированныйУзелПодписьВBase64СПодставленнымХеш(
		КанонизированныйУзелПодписьВBase64,
		ХешСтрокаВBase64Ключа,
		ХешСтрокаВBase64,
		СконвертироватьИзШестнадцатеричногоПредставленияХеша,
		ДобавляемыеАтрибуты = Неопределено,
		НастройкиПодписи = Неопределено) Экспорт
		
	ИспользоватьПреобразованиеККаноническомуXML =
		ОбщегоНазначенияКлиентСервер.СвойствоСтруктуры(НастройкиПодписи, "ИспользоватьПреобразованиеККаноническомуXML", Ложь);
	
	ИмяВременногоФайла = ПолучитьИмяВременногоФайла();
	ИспользоватьВременноеХранилище = ЭтоАдресВременногоХранилища(КанонизированныйУзелПодписьВBase64);
	Если ИспользоватьВременноеХранилище Тогда
		ДвоичныеДанныеКанонизированныйУзелПодпись = ПолучитьИзВременногоХранилища(КанонизированныйУзелПодписьВBase64);
	Иначе
		ДвоичныеДанныеКанонизированныйУзелПодпись = Base64Значение(КанонизированныйУзелПодписьВBase64);
	КонецЕсли;
	ДвоичныеДанныеКанонизированныйУзелПодпись.Записать(ИмяВременногоФайла);
	
	ЧтениеТекста = Новый ЧтениеТекста(ИмяВременногоФайла, "UTF-8");
	СтрокаКанонизированныйУзелПодпись = ЧтениеТекста.Прочитать();
	ЧтениеТекста.Закрыть();
	ОперацииСФайламиЭДКО.УдалитьВременныйФайл(ИмяВременногоФайла);
	
	Если СконвертироватьИзШестнадцатеричногоПредставленияХеша Тогда
		Если ХешСтрокаВBase64Ключа <> Неопределено Тогда
			ХешСтрокаВBase64Ключа = СтрокаВШестнадцатеричномПредставленииВBase64(ХешСтрокаВBase64Ключа);
		КонецЕсли;
		
		ХешСтрокаВBase64 = СтрокаВШестнадцатеричномПредставленииВBase64(ХешСтрокаВBase64);
	КонецЕсли;
	
	Если ХешСтрокаВBase64Ключа <> Неопределено Тогда
		СтрокаКанонизированныйУзелПодпись = СтрЗаменить(СтрокаКанонизированныйУзелПодпись, "%KeyInfoDigestValue%", ХешСтрокаВBase64Ключа);
	КонецЕсли;
	СтрокаКанонизированныйУзелПодпись = СтрЗаменить(СтрокаКанонизированныйУзелПодпись, "%DigestValue%", ХешСтрокаВBase64);
	Если ДобавляемыеАтрибуты <> Неопределено Тогда
		Если ИспользоватьПреобразованиеККаноническомуXML Тогда
			СтрокаКанонизированныйУзелПодпись = СтрЗаменить(СтрокаКанонизированныйУзелПодпись,
				"<SignedInfo>",
				"<SignedInfo " + "xmlns=""http://www.w3.org/2000/09/xmldsig#"">");
		Иначе
			СтрокаКанонизированныйУзелПодпись = СтрЗаменить(СтрокаКанонизированныйУзелПодпись,
				"<SignedInfo>",
				"<SignedInfo " + ДобавляемыеАтрибуты + ">");
		КонецЕсли;
	КонецЕсли;
	
	ИмяВременногоФайла = ПолучитьИмяВременногоФайла();
	ЗаписьТекста = Новый ЗаписьТекста(ИмяВременногоФайла, "utf-8");
	ЗаписьТекста.Записать(СтрокаКанонизированныйУзелПодпись);
	ЗаписьТекста.Закрыть();
	ДвоичныеДанныеСтрокиНаПодпись = Новый ДвоичныеДанные(ИмяВременногоФайла);
	СтрокаНаПодписьBase64 = Base64Строка(ДвоичныеДанныеСтрокиНаПодпись);
	// удалим из файла в кодировке "UTF-8" сигнатуру BOM
	СтрокаНаПодписьBase64 = Сред(СтрокаНаПодписьBase64, 5);
	
	Если ИспользоватьВременноеХранилище Тогда
		ДвоичныеДанныеСтрокиНаПодпись = Base64Значение(СтрокаНаПодписьBase64);
		СтрокаНаПодписьBase64 = ПоместитьВоВременноеХранилище(ДвоичныеДанныеСтрокиНаПодпись, Новый УникальныйИдентификатор);
	КонецЕсли;
	ОперацииСФайламиЭДКО.УдалитьВременныйФайл(ИмяВременногоФайла);
	
	Возврат СтрокаНаПодписьBase64;
	
КонецФункции

Функция ПодписьИзПодписанныхДанныхBase64(ПодписанныеДанныеВBase64)
	
	ИмяВременногоФайла = ПолучитьИмяВременногоФайла();
	
	ИспользоватьВременноеХранилище = ЭтоАдресВременногоХранилища(ПодписанныеДанныеВBase64);
	Если ИспользоватьВременноеХранилище Тогда
		ДвоичныеДанныеПодписанныхДанных = ПолучитьИзВременногоХранилища(ПодписанныеДанныеВBase64);
	Иначе
		ДвоичныеДанныеПодписанныхДанных = Base64Значение(ПодписанныеДанныеВBase64);
	КонецЕсли;
	ДвоичныеДанныеПодписанныхДанных.Записать(ИмяВременногоФайла);
	
	ИсходныйМассивБайтов = ПрочитатьФайлВМассив(ИмяВременногоФайла);
	ОперацииСФайламиЭДКО.УдалитьВременныйФайл(ИмяВременногоФайла);
	
	МассивБайтов = Новый Массив;
	Для Инд = 1 По 64 Цикл
		ЗначениеБайта = ИсходныйМассивБайтов[ИсходныйМассивБайтов.ВГраница() + 1 - Инд];
		МассивБайтов.Добавить(ЗначениеБайта);
	КонецЦикла;
	
	ИмяВременногоФайла = ПолучитьИмяВременногоФайла();
	ЗаписатьФайлИзМассива(ИмяВременногоФайла, МассивБайтов);
	ДвоичныеДанныеПодпись = Новый ДвоичныеДанные(ИмяВременногоФайла);
	
	Если ИспользоватьВременноеХранилище Тогда
		ПодписьВBase64 = ПоместитьВоВременноеХранилище(ДвоичныеДанныеПодпись, Новый УникальныйИдентификатор);
	Иначе
		ПодписьВBase64 = Base64Строка(ДвоичныеДанныеПодпись);
	КонецЕсли;
	ОперацииСФайламиЭДКО.УдалитьВременныйФайл(ИмяВременногоФайла);
	
	Возврат ПодписьВBase64;
	
КонецФункции

Функция АдресДанныхДляПодписиФайлаXML(
		СодержимоеФайлаXMLВBase64,
		АдресСодержимогоПодготовленногоФайлаXML = Неопределено,
		СертификатПолучателяВBase64 = Неопределено,
		Кодировка = Неопределено,
		НастройкиПодписи = Неопределено,
		Алгоритм = Неопределено) Экспорт
	
	Трансформация1_1 = ?(НастройкиПодписи <> Неопределено И НастройкиПодписи.Свойство("Трансформация1_1"),
		НастройкиПодписи.Трансформация1_1, Ложь);
	ВключатьХэшСертификата = ?(НастройкиПодписи <> Неопределено И НастройкиПодписи.Свойство("ВключатьХэшСертификата"),
		НастройкиПодписи.ВключатьХэшСертификата, Ложь);
	НеВключатьЗаголовокXML = ?(НастройкиПодписи <> Неопределено И НастройкиПодписи.Свойство("НеВключатьЗаголовокXML"),
		НастройкиПодписи.НеВключатьЗаголовокXML, Ложь);
	ЗаменаУзлаОбъекта = ?(НастройкиПодписи <> Неопределено И НастройкиПодписи.Свойство("ЗаменаУзлаОбъекта"),
		НастройкиПодписи.ЗаменаУзлаОбъекта, Неопределено);
	НормализацияОтключена = ?(НастройкиПодписи <> Неопределено И НастройкиПодписи.Свойство("НормализацияОтключена"),
		НастройкиПодписи.НормализацияОтключена, Ложь);
	ДанныеВнутриПодписи = ?(НастройкиПодписи <> Неопределено И НастройкиПодписи.Свойство("ДанныеВнутриПодписи"),
		НастройкиПодписи.ДанныеВнутриПодписи, Ложь);
	ИдентификаторПодписываемыхДанных = ?(НастройкиПодписи <> Неопределено И НастройкиПодписи.Свойство("ИдентификаторПодписываемыхДанных"),
		НастройкиПодписи.ИдентификаторПодписываемыхДанных, Неопределено);
	ДобавляемыеАтрибуты = ?(НастройкиПодписи <> Неопределено И НастройкиПодписи.Свойство("ДобавляемыеАтрибуты"),
		НастройкиПодписи.ДобавляемыеАтрибуты, Неопределено);
	НеВключатьИнформациюОСертификате = ?(НастройкиПодписи <> Неопределено И НастройкиПодписи.Свойство("НеВключатьИнформациюОСертификате"),
		НастройкиПодписи.НеВключатьИнформациюОСертификате, Ложь);
	НеВключатьИнформациюОбОбъекте = ?(НастройкиПодписи <> Неопределено И НастройкиПодписи.Свойство("НеВключатьИнформациюОбОбъекте"),
		НастройкиПодписи.НеВключатьИнформациюОбОбъекте, Ложь);
	ДополнительныеТрансформации = ?(НастройкиПодписи <> Неопределено И НастройкиПодписи.Свойство("ДополнительныеТрансформации"),
		НастройкиПодписи.ДополнительныеТрансформации, Неопределено);
	ДополнительныеОбратныеТрансформации = ?(НастройкиПодписи <> Неопределено И НастройкиПодписи.Свойство("ДополнительныеОбратныеТрансформации"),
		НастройкиПодписи.ДополнительныеОбратныеТрансформации, Неопределено);
	ПереданХешВместоОбъекта = ?(НастройкиПодписи <> Неопределено И НастройкиПодписи.Свойство("ПереданХешВместоОбъекта"),
		НастройкиПодписи.ПереданХешВместоОбъекта, Ложь);
	ИспользуетсяУзелТрансформации = ?(НастройкиПодписи <> Неопределено
		И НастройкиПодписи.Свойство("ИспользуетсяУзелТрансформации"),
		НастройкиПодписи.ИспользуетсяУзелТрансформации, Ложь);
	ЭтоИсключающаяКанонизация = ?(НастройкиПодписи <> Неопределено
		И НастройкиПодписи.Свойство("ЭтоИсключающаяКанонизация"),
		НастройкиПодписи.ЭтоИсключающаяКанонизация, Ложь);
	ДополнитьПространстваИменУзлаПодписи = ?(НастройкиПодписи <> Неопределено
		И НастройкиПодписи.Свойство("ДополнитьПространстваИменУзлаПодписи"),
		НастройкиПодписи.ДополнитьПространстваИменУзлаПодписи, Ложь);
	ИспользоватьПреобразованиеККаноническомуXML =
		ОбщегоНазначенияКлиентСервер.СвойствоСтруктуры(НастройкиПодписи,
			"ИспользоватьПреобразованиеККаноническомуXML",
			Ложь);
	ТипКаноническогоXMLФайла =
		ОбщегоНазначенияКлиентСервер.СвойствоСтруктуры(
			НастройкиПодписи,
			"ТипКаноническогоXML",
			"ИсключающийКаноническийXMLСКомментариями");
	ТипКаноническогоXMLФайла = ПредопределенноеЗначение("ТипКаноническогоXML." + ТипКаноническогоXMLФайла);
		
	
	ИспользоватьВременноеХранилище = ЭтоАдресВременногоХранилища(СодержимоеФайлаXMLВBase64);
	ПодписываемыйФайлXML = ПолучитьИмяВременногоФайла();
	Если ИспользоватьВременноеХранилище Тогда
		ДвоичныеДанныеФайлаXML = ПолучитьИзВременногоХранилища(СодержимоеФайлаXMLВBase64);
	Иначе
		ДвоичныеДанныеФайлаXML = ДвоичныеДанныеИзСтроки64НаСервере(СодержимоеФайлаXMLВBase64);
	КонецЕсли;
	ДвоичныеДанныеФайлаXML.Записать(ПодписываемыйФайлXML);
	
	Если ИспользоватьПреобразованиеККаноническомуXML Тогда
		
		// Для каноникализации используется объект ПреобразованиеККаноническомуXML.
		ЧтениеXML = Новый ЧтениеXML;
		ЧтениеXML.ОткрытьФайл(ПодписываемыйФайлXML);
		ПостроительDOM = Новый ПостроительDOM;
		ДокументDOM = ПостроительDOM.Прочитать(ЧтениеXML);
		ЧтениеXML.Закрыть();
		
		КанонизированныйПодписываемыйФайлXML = ФайлПреобразованиеККаноническомуXML(ДокументDOM, ТипКаноническогоXMLФайла);
		
	Иначе
		
		// Для каноникализации используется объект КаноническийDOM, при этом xml предвательно адаптируется.
		ПрефиксыПространствИмен = Неопределено;
		ПрефиксОбхода = Неопределено;
		Если Кодировка <> Неопределено ИЛИ Трансформация1_1 ИЛИ ВключатьХэшСертификата
			ИЛИ ДополнитьПространстваИменУзлаПодписи Тогда
			
			ИсходнаяКодировка = ?(Кодировка <> Неопределено, "windows-1251", "utf-8");
			ЧтениеФайлаXML = Новый ЧтениеТекста(ПодписываемыйФайлXML, ИсходнаяКодировка);
			СодержимоеФайлаXML = ЧтениеФайлаXML.Прочитать();
			СодержимоеФайлаXMLБезЗамен = СодержимоеФайлаXML;
			СодержимоеФайлаXMLБезДополнительныхТрансформаций = СодержимоеФайлаXML;
			ЧтениеФайлаXML.Закрыть();
			
			Если Кодировка <> Неопределено ИЛИ Трансформация1_1 ИЛИ ДополнитьПространстваИменУзлаПодписи Тогда
				Если Трансформация1_1 ИЛИ ДополнитьПространстваИменУзлаПодписи Тогда
					// преобразования обхода ошибки канонизации платформы
					ПрефиксыПространствИмен = Новый Массив;
					ТекстXML = СодержимоеФайлаXML;
					Пока ЗначениеЗаполнено(ТекстXML) Цикл
						ПозицияПослеПостфикса = 0;
						ПрефиксПространстваИмен = ТекстПослеПрефикса(ТекстXML, " xmlns:", "=", ПозицияПослеПостфикса);
						Если ЗначениеЗаполнено(ПрефиксПространстваИмен) И ПрефиксыПространствИмен.Найти(ПрефиксПространстваИмен) = Неопределено Тогда
							ПрефиксыПространствИмен.Добавить(ПрефиксПространстваИмен);
						КонецЕсли;
						ТекстXML = Сред(ТекстXML, ПозицияПослеПостфикса);
					КонецЦикла;
					
					ПрефиксОбхода = "EQERLRLQKE";
					СодержимоеФайлаXML = СтрЗаменить(СодержимоеФайлаXML, " xmlns=""", " xmlns=""" + ПрефиксОбхода);
					Для каждого ПрефиксПространстваИмен Из ПрефиксыПространствИмен Цикл
						СодержимоеФайлаXML = СтрЗаменить(СодержимоеФайлаXML, " xmlns:" + ПрефиксПространстваИмен + "=""",
							" xmlns:" + ПрефиксПространстваИмен + "=""" + ПрефиксОбхода);
					КонецЦикла;
					
					СодержимоеФайлаXMLБезДополнительныхТрансформаций = СодержимоеФайлаXML;
					Если ДополнительныеТрансформации <> Неопределено Тогда
						ИндексТрансформации = 0;
						Пока ИндексТрансформации < ДополнительныеТрансформации.Количество() Цикл
							СодержимоеФайлаXML = СтрЗаменить(СодержимоеФайлаXML, ДополнительныеТрансформации[ИндексТрансформации],
								ДополнительныеТрансформации[ИндексТрансформации + 1]);
							ИндексТрансформации = ИндексТрансформации + 2;
						КонецЦикла;
					КонецЕсли;
				КонецЕсли;
				
				Кодировка = ?(Кодировка <> Неопределено, Кодировка, ИсходнаяКодировка);
				ПерекодированныйФайлXML = ПолучитьИмяВременногоФайла();
				ЗаписьФайлаXML = Новый ЗаписьТекста(ПерекодированныйФайлXML, Кодировка);
				ЗаписьФайлаXML.Записать(СодержимоеФайлаXML);
				ЗаписьФайлаXML.Закрыть();
				
				ОперацииСФайламиЭДКО.УдалитьВременныйФайл(ПодписываемыйФайлXML);
				ПодписываемыйФайлXML = ПерекодированныйФайлXML;
			КонецЕсли;
		КонецЕсли;
		
		ЧтениеXML = Новый ЧтениеXML;
		ЧтениеXML.ОткрытьФайл(ПодписываемыйФайлXML);
		ПостроительDOM = Новый ПостроительDOM;
		ДокументDOM = ПостроительDOM.Прочитать(ЧтениеXML);
		ЧтениеXML.Закрыть();
		
		Если ДополнитьПространстваИменУзлаПодписи И НЕ ЭтоИсключающаяКанонизация Тогда
			ПространстваИменКорневогоУзла = Новый СписокЗначений;
			КорневыеУзлы = ДокументDOM.ДочерниеУзлы;
			Если КорневыеУзлы.Количество() > 0 Тогда
				Для каждого АтрибутКорневогоУзла Из КорневыеУзлы[0].Атрибуты Цикл
					Если АтрибутКорневогоУзла.Префикс = "xmlns" И ЗначениеЗаполнено(АтрибутКорневогоУзла.ЛокальноеИмя) Тогда
						ПространстваИменКорневогоУзла.Добавить(АтрибутКорневогоУзла.ЛокальноеИмя, АтрибутКорневогоУзла.Значение);
					КонецЕсли;
				КонецЦикла;
			КонецЕсли;
			ПространстваИменКорневогоУзла.СортироватьПоЗначению();
		КонецЕсли;
		
		НастройкиКонвертации = Новый Структура;
		НастройкиКонвертации.Вставить("ПрефиксОбхода", 					ПрефиксОбхода);
		НастройкиКонвертации.Вставить("ПрефиксыПространствИмен", 		ПрефиксыПространствИмен);
		НастройкиКонвертации.Вставить("ПространствоИменПоУмолчанию", 	Неопределено);
		КанонизированныйПодписываемыйФайлXML = ФайлКанонизированныйУзелDOM(
			ДокументDOM,,,
			Трансформация1_1 ИЛИ ДополнитьПространстваИменУзлаПодписи,
			Трансформация1_1 И НЕ НормализацияОтключена,
			ДополнительныеОбратныеТрансформации,
			НастройкиКонвертации);
			
	КонецЕсли;
		
	Если ВключатьХэшСертификата ИЛИ ДанныеВнутриПодписи Тогда
		ЧтениеФайлаXML = Новый ЧтениеТекста(КанонизированныйПодписываемыйФайлXML, "windows-1251");
		СодержимоеКанонизированногоФайлаXML = ЧтениеФайлаXML.Прочитать();
		ЧтениеФайлаXML.Закрыть();
		КанонизированныйФайлXMLВBase64 = Неопределено;
		
	Иначе
		Если ИспользоватьВременноеХранилище Тогда
			ДвоичныеДанныеКанонизированногоФайлаXML = Новый ДвоичныеДанные(КанонизированныйПодписываемыйФайлXML);
			КанонизированныйФайлXMLВBase64 = ПоместитьВоВременноеХранилище(ДвоичныеДанныеКанонизированногоФайлаXML, Новый УникальныйИдентификатор);
		Иначе
			КанонизированныйФайлXMLВBase64 = ПрочитатьBase64ИзФайла(КанонизированныйПодписываемыйФайлXML);
		КонецЕсли;
	КонецЕсли;
	
	// Добавляем в документ DOM узел для описания ЭЦП
	КорневойУзел = ДокументDOM.ДочерниеУзлы[0]; //report
	
	УзелПодпись = КорневойУзел.ДобавитьДочерний(ДокументDOM.СоздатьЭлемент("Signature"));
	
	УзелПодписьИнфо = УзелПодпись.ДобавитьДочерний(ДокументDOM.СоздатьЭлемент("SignedInfo"));
	
	УзелМетодКанонизации = УзелПодписьИнфо.ДобавитьДочерний(ДокументDOM.СоздатьЭлемент("CanonicalizationMethod"));
	УзелМетодПодписи = УзелПодписьИнфо.ДобавитьДочерний(ДокументDOM.СоздатьЭлемент("SignatureMethod"));
	Если ВключатьХэшСертификата Тогда
		УзелСсылкаКлюч = УзелПодписьИнфо.ДобавитьДочерний(ДокументDOM.СоздатьЭлемент("Reference"));
	КонецЕсли;
	УзелСсылка = УзелПодписьИнфо.ДобавитьДочерний(ДокументDOM.СоздатьЭлемент("Reference"));
	
	Если НЕ Трансформация1_1 ИЛИ НЕ НормализацияОтключена ИЛИ ИспользуетсяУзелТрансформации Тогда
		Если ВключатьХэшСертификата Тогда
			УзелПреобразованияКлюч = УзелСсылкаКлюч.ДобавитьДочерний(ДокументDOM.СоздатьЭлемент("Transforms"));
		КонецЕсли;
		УзелПреобразования = УзелСсылка.ДобавитьДочерний(ДокументDOM.СоздатьЭлемент("Transforms"));
		
		Если ВключатьХэшСертификата Тогда
			Если Трансформация1_1 И НЕ ИспользуетсяУзелТрансформации Тогда
				УзелПреобразование2Ключ = УзелПреобразованияКлюч.ДобавитьДочерний(ДокументDOM.СоздатьЭлемент("Transform"));
			Иначе
				Если НЕ ИспользуетсяУзелТрансформации Тогда
					УзелПреобразование1Ключ = УзелПреобразованияКлюч.ДобавитьДочерний(ДокументDOM.СоздатьЭлемент("Transform"));
				КонецЕсли;
				УзелПреобразование2Ключ = УзелПреобразованияКлюч.ДобавитьДочерний(ДокументDOM.СоздатьЭлемент("Transform"));
			КонецЕсли;
		КонецЕсли;
		Если Трансформация1_1 И НЕ ИспользуетсяУзелТрансформации Тогда
			УзелПреобразование2 = УзелПреобразования.ДобавитьДочерний(ДокументDOM.СоздатьЭлемент("Transform"));
		Иначе
			Если НЕ ИспользуетсяУзелТрансформации Тогда
				УзелПреобразование1 = УзелПреобразования.ДобавитьДочерний(ДокументDOM.СоздатьЭлемент("Transform"));
			КонецЕсли;
			УзелПреобразование2 = УзелПреобразования.ДобавитьДочерний(ДокументDOM.СоздатьЭлемент("Transform"));
		КонецЕсли;
	КонецЕсли;
	
	Если ВключатьХэшСертификата Тогда
		УзелМетодХэшированияКлюч = УзелСсылкаКлюч.ДобавитьДочерний(ДокументDOM.СоздатьЭлемент("DigestMethod"));
		УзелРезультатХэшированияКлюч = УзелСсылкаКлюч.ДобавитьДочерний(ДокументDOM.СоздатьЭлемент("DigestValue"));
	КонецЕсли;
	УзелМетодХэширования = УзелСсылка.ДобавитьДочерний(ДокументDOM.СоздатьЭлемент("DigestMethod"));
	УзелРезультатХэширования = УзелСсылка.ДобавитьДочерний(ДокументDOM.СоздатьЭлемент("DigestValue"));
	
	УзелРезультатПодписания = УзелПодпись.ДобавитьДочерний(ДокументDOM.СоздатьЭлемент("SignatureValue"));
	Если НЕ НеВключатьИнформациюОСертификате Тогда
		УзелИнформацияОКлючах = УзелПодпись.ДобавитьДочерний(ДокументDOM.СоздатьЭлемент("KeyInfo"));
	КонецЕсли;
	Если (ВключатьХэшСертификата ИЛИ ДанныеВнутриПодписи) И НЕ НеВключатьИнформациюОбОбъекте Тогда
		УзелИнформацияОбОбъекте = УзелПодпись.ДобавитьДочерний(ДокументDOM.СоздатьЭлемент("Object"));
	КонецЕсли;
	
	Если НЕ НеВключатьИнформациюОСертификате Тогда
		УзелДанныеX509 = УзелИнформацияОКлючах.ДобавитьДочерний(ДокументDOM.СоздатьЭлемент("X509Data"));
		УзелСертификатX509 = УзелДанныеX509.ДобавитьДочерний(ДокументDOM.СоздатьЭлемент("X509Certificate"));
	КонецЕсли;
	
	АлгоритмКанонизацииДляУзловТрансформации = ?(ЭтоИсключающаяКанонизация,
		"http://www.w3.org/2001/10/xml-exc-c14n#WithComments",
		"http://www.w3.org/TR/2001/REC-xml-c14n-20010315");
	АлгоритмКанонизации = АлгоритмКанонизацииДляУзловТрансформации + ?(ЭтоИсключающаяКанонизация, "", "#WithComments");
	
	Если ВключатьХэшСертификата ИЛИ ДанныеВнутриПодписи Тогда
		Если ВключатьХэшСертификата Тогда
			УзелСсылкаКлюч.УстановитьАтрибут("URI", "#KeyInfo");
		КонецЕсли;
		УзелСсылка.УстановитьАтрибут("URI", "#" + ?(ИдентификаторПодписываемыхДанных = Неопределено, "Object", ИдентификаторПодписываемыхДанных));
	Иначе
		УзелСсылка.УстановитьАтрибут("URI", "");
	КонецЕсли;
	УзелПодпись.УстановитьАтрибут("xmlns", "http://www.w3.org/2000/09/xmldsig#");
	АлгоритмПодписи = ?(Алгоритм = "GOST R 34.10-2012-256", "urn:ietf:params:xml:ns:cpxmlsec:algorithms:gostr34102012-gostr34112012-256",
					  ?(Алгоритм = "GOST R 34.10-2012-512", "urn:ietf:params:xml:ns:cpxmlsec:algorithms:gostr34102012-gostr34112012-512",
															"http://www.w3.org/2001/04/xmldsig-more#gostr34102001-gostr3411"));
	УзелМетодПодписи.УстановитьАтрибут("Algorithm", АлгоритмПодписи);
	Если Трансформация1_1 Тогда
		УзелМетодКанонизации.УстановитьАтрибут("Algorithm", ?(НЕ НормализацияОтключена, "urn:xml-dsig:transformation:v1.1",
			АлгоритмКанонизации));
	Иначе
		УзелМетодКанонизации.УстановитьАтрибут("Algorithm", АлгоритмКанонизацииДляУзловТрансформации);
	КонецЕсли;
	Если НЕ Трансформация1_1 ИЛИ НЕ НормализацияОтключена ИЛИ ИспользуетсяУзелТрансформации Тогда
		Если ВключатьХэшСертификата Тогда
			Если Трансформация1_1 И НЕ ИспользуетсяУзелТрансформации Тогда
				УзелПреобразование2Ключ.УстановитьАтрибут("Algorithm", "urn:xml-dsig:transformation:v1.1");
			Иначе
				Если НЕ ИспользуетсяУзелТрансформации Тогда
					УзелПреобразование1Ключ.УстановитьАтрибут("Algorithm", "http://www.w3.org/2000/09/xmldsig#enveloped-signature");
				КонецЕсли;
				УзелПреобразование2Ключ.УстановитьАтрибут("Algorithm", АлгоритмКанонизацииДляУзловТрансформации);
			КонецЕсли;
		КонецЕсли;
		Если Трансформация1_1 И НЕ ИспользуетсяУзелТрансформации Тогда
			УзелПреобразование2.УстановитьАтрибут("Algorithm", "urn:xml-dsig:transformation:v1.1");
		Иначе
			Если НЕ ИспользуетсяУзелТрансформации Тогда
				УзелПреобразование1.УстановитьАтрибут("Algorithm", "http://www.w3.org/2000/09/xmldsig#enveloped-signature");
			КонецЕсли;
			УзелПреобразование2.УстановитьАтрибут("Algorithm", АлгоритмКанонизацииДляУзловТрансформации);
		КонецЕсли;
	КонецЕсли;
	АлгоритмХеша = ?(Алгоритм = "GOST R 34.10-2012-256", "urn:ietf:params:xml:ns:cpxmlsec:algorithms:gostr34112012-256",
				   ?(Алгоритм = "GOST R 34.10-2012-512", "urn:ietf:params:xml:ns:cpxmlsec:algorithms:gostr34112012-512",
														 "http://www.w3.org/2001/04/xmldsig-more#gostr3411"));
	Если ВключатьХэшСертификата Тогда
		УзелМетодХэшированияКлюч.УстановитьАтрибут("Algorithm", АлгоритмХеша);
	КонецЕсли;
	УзелМетодХэширования.УстановитьАтрибут("Algorithm", АлгоритмХеша);
	Если ВключатьХэшСертификата ИЛИ ДанныеВнутриПодписи Тогда
		Если ВключатьХэшСертификата И НЕ НеВключатьИнформациюОСертификате Тогда
			УзелИнформацияОКлючах.УстановитьАтрибут("Id", "KeyInfo");
		КонецЕсли;
		Если НЕ НеВключатьИнформациюОбОбъекте Тогда
			УзелИнформацияОбОбъекте.УстановитьАтрибут("Id", ?(ИдентификаторПодписываемыхДанных = Неопределено, "Object", ИдентификаторПодписываемыхДанных));
		КонецЕсли;
	КонецЕсли;
	
	Если ВключатьХэшСертификата Тогда
		УзелРезультатХэшированияКлюч.ТекстовоеСодержимое = "%KeyInfoDigestValue%";
	КонецЕсли;
	Если ПереданХешВместоОбъекта Тогда
		УзелРезультатХэширования.ТекстовоеСодержимое = ТекстПослеПрефикса(СодержимоеФайлаXMLБезЗамен, "<Object xmlns=""http://www.w3.org/2000/09/xmldsig#"" Id=""Object"">", "</Object>");
	Иначе
		УзелРезультатХэширования.ТекстовоеСодержимое = "%DigestValue%";
	КонецЕсли;
	УзелРезультатПодписания.ТекстовоеСодержимое = "%SignatureValue%";
	Если НЕ НеВключатьИнформациюОСертификате Тогда
		ЗначениеСертификатПолучателяВBase64 = СтрЗаменить(СертификатПолучателяВBase64, Символы.ВК, "");
		УзелСертификатX509.ТекстовоеСодержимое = СтрЗаменить(ЗначениеСертификатПолучателяВBase64, Символы.ПС, "");
	КонецЕсли;
	Если (ВключатьХэшСертификата ИЛИ ДанныеВнутриПодписи) И НЕ НеВключатьИнформациюОбОбъекте Тогда
		УзелИнформацияОбОбъекте.ТекстовоеСодержимое = "%SigningXML%";
	КонецЕсли;
	
	ЗаписьXML = Новый ЗаписьXML;
	ЗаписьXML.Отступ = Ложь;
	Если НЕ НеВключатьЗаголовокXML Тогда
		ЗаписьXML.УстановитьСтроку("utf-8");
	Иначе
		ЗаписьXML.УстановитьСтроку("windows-1251");
	КонецЕсли;
	ЗаписьDOM = Новый ЗаписьDOM;
	Если ВключатьХэшСертификата ИЛИ ДанныеВнутриПодписи Тогда
		ЗаписьDOM.Записать(УзелПодпись, ЗаписьXML);
	Иначе
		ЗаписьDOM.Записать(ДокументDOM, ЗаписьXML);
	КонецЕсли;
	
	ТекстовоеСодержимоеФайлаXML = ЗаписьXML.Закрыть();
	Если ВключатьХэшСертификата ИЛИ ДанныеВнутриПодписи Тогда
		Если ЗаменаУзлаОбъекта <> Неопределено ИЛИ ПереданХешВместоОбъекта Тогда
			ТекстовоеСодержимоеФайлаXML = СтрЗаменить(ТекстовоеСодержимоеФайлаXML, "<Object Id=""" + ?(ИдентификаторПодписываемыхДанных = Неопределено,
				"Object", ИдентификаторПодписываемыхДанных) + """>%SigningXML%</Object>",
				?(ЗаменаУзлаОбъекта <> Неопределено, ЗаменаУзлаОбъекта, "<Object xmlns=""http://www.w3.org/2000/09/xmldsig#"" Id="""
				+ ?(ИдентификаторПодписываемыхДанных = Неопределено, "Object", ИдентификаторПодписываемыхДанных) + """/>"));
		Иначе
			ТекстовоеСодержимоеФайлаXML = СтрЗаменить(ТекстовоеСодержимоеФайлаXML, "%SigningXML%", СодержимоеФайлаXMLБезДополнительныхТрансформаций);
			ТекстовоеСодержимоеФайлаXML = СтрЗаменить(ТекстовоеСодержимоеФайлаXML, Символы.ВК, "");
		КонецЕсли;
	КонецЕсли;
	Если (Трансформация1_1 ИЛИ ДополнитьПространстваИменУзлаПодписи) И ПрефиксыПространствИмен <> Неопределено Тогда
		// обратные преобразования обхода ошибки канонизации
		Для ИндексЭлемента = 0 По ПрефиксыПространствИмен.Количество() - 1 Цикл
			ПрефиксПространстваИмен = ПрефиксыПространствИмен[ПрефиксыПространствИмен.Количество() - 1 - ИндексЭлемента];
			ТекстовоеСодержимоеФайлаXML = СтрЗаменить(ТекстовоеСодержимоеФайлаXML,
				" xmlns:" + ПрефиксПространстваИмен + "=""" + ПрефиксОбхода, " xmlns:" + ПрефиксПространстваИмен + "=""");
			ТекстовоеСодержимоеФайлаXML = СтрЗаменить(ТекстовоеСодержимоеФайлаXML,
				" :" + ПрефиксПространстваИмен + "=""" + ПрефиксОбхода, " xmlns:" + ПрефиксПространстваИмен + "="""); // обход ошибки платформы
		КонецЦикла;
		
		ТекстовоеСодержимоеФайлаXML = СтрЗаменить(ТекстовоеСодержимоеФайлаXML, " xmlns=""" + ПрефиксОбхода, " xmlns=""");
		ТекстовоеСодержимоеФайлаXML = СтрЗаменить(ТекстовоеСодержимоеФайлаXML, " =""" + ПрефиксОбхода, " xmlns="""); // обход ошибки платформы
		ТекстовоеСодержимоеФайлаXML = СтрЗаменить(ТекстовоеСодержимоеФайлаXML, " ="""">", ">"); // обход ошибки платформы
	КонецЕсли;
	
	ОперацииСФайламиЭДКО.УдалитьВременныйФайл(ПодписываемыйФайлXML);
	
	АдресСодержимогоПодготовленногоФайлаXML = ПоместитьВоВременноеХранилище(ТекстовоеСодержимоеФайлаXML, Новый УникальныйИдентификатор);
	
	ПространствоИменПоУмолчанию = Неопределено;
	Если ДобавляемыеАтрибуты = Неопределено Тогда
		Если ДополнитьПространстваИменУзлаПодписи И НЕ ЭтоИсключающаяКанонизация
			И ПространстваИменКорневогоУзла.Количество() > 0 Тогда
			
			// обход ошибки платформы сортировки пространств имен при канонизации дочернего узла
			ПространствоИменПоУмолчанию = "AAAAAAAAAA";
			Пока ПространстваИменКорневогоУзла.НайтиПоЗначению(ПространствоИменПоУмолчанию) <> Неопределено Цикл
				ПространствоИменПоУмолчанию = ПространствоИменПоУмолчанию + "A";
			КонецЦикла;
			УзелПодписьИнфо.УстановитьСоответствиеПространстваИмен(ПространствоИменПоУмолчанию,
				"http://www.w3.org/2000/09/xmldsig#");
			Для каждого ПространствоИменКорневогоУзла Из ПространстваИменКорневогоУзла Цикл
				УзелПодписьИнфо.УстановитьСоответствиеПространстваИмен(ПространствоИменКорневогоУзла.Значение,
					ПространствоИменКорневогоУзла.Представление);
			КонецЦикла;
			
		Иначе
			УзелПодписьИнфо.УстановитьАтрибут("xmlns", "http://www.w3.org/2000/09/xmldsig#");
		КонецЕсли;
	КонецЕсли;
	НастройкиКонвертации = Новый Структура;
	НастройкиКонвертации.Вставить("ПрефиксОбхода", 					ПрефиксОбхода);
	НастройкиКонвертации.Вставить("ПрефиксыПространствИмен", 		ПрефиксыПространствИмен);
	НастройкиКонвертации.Вставить("ПространствоИменПоУмолчанию", 	ПространствоИменПоУмолчанию);
	
	// Каноникализации узла подписи.
	Если ИспользоватьПреобразованиеККаноническомуXML Тогда
		// Для канонокализации используется объект ПреобразованиеККаноническомуXML.
		ФайлКанонизированныйУзелПодпись = ФайлПреобразованиеККаноническомуXML(
			ДокументDOM,
			ТипКаноническогоXMLФайла,
			"(//. | //@* | //namespace::*)[ancestor-or-self::*[local-name()='SignedInfo']]");
	Иначе
		// Для канонокализации используется объект КаноническийDOM.
		ФайлКанонизированныйУзелПодпись = ФайлКанонизированныйУзелDOM(
			УзелПодписьИнфо,,,
			Трансформация1_1 ИЛИ ДополнитьПространстваИменУзлаПодписи,
			Трансформация1_1 И НЕ НормализацияОтключена,,
			НастройкиКонвертации);
	КонецЕсли;
		
	Если ИспользоватьВременноеХранилище Тогда
		ДвоичныеДанныеКанонизированногоУзлаПодписать = Новый ДвоичныеДанные(ФайлКанонизированныйУзелПодпись);
		КанонизированныйУзелПодписьВBase64 = ПоместитьВоВременноеХранилище(ДвоичныеДанныеКанонизированногоУзлаПодписать, Новый УникальныйИдентификатор);
	Иначе
		КанонизированныйУзелПодписьВBase64 = ПрочитатьBase64ИзФайла(ФайлКанонизированныйУзелПодпись);
	КонецЕсли;
	
	ДанныеДляПодписи = Новый Структура("КанонизированныйФайлXMLВBase64, КанонизированныйУзелПодписьВBase64", КанонизированныйФайлXMLВBase64, КанонизированныйУзелПодписьВBase64);
	Если ВключатьХэшСертификата ИЛИ ДанныеВнутриПодписи Тогда
		Если ВключатьХэшСертификата И НЕ НеВключатьИнформациюОСертификате Тогда
			НастройкиКонвертации = Новый Структура;
			НастройкиКонвертации.Вставить("ПрефиксОбхода", 					ПрефиксОбхода);
			НастройкиКонвертации.Вставить("ПрефиксыПространствИмен", 		ПрефиксыПространствИмен);
			НастройкиКонвертации.Вставить("ПространствоИменПоУмолчанию", 	Неопределено);
			ФайлКанонизированныйУзелИнформацияОКлючах = ФайлКанонизированныйУзелDOM(
				УзелИнформацияОКлючах,,,
				Трансформация1_1 ИЛИ ДополнитьПространстваИменУзлаПодписи,
				Трансформация1_1 И НЕ НормализацияОтключена,,
				НастройкиКонвертации);
			Если Трансформация1_1 Тогда
				ЧтениеФайлаУзелИнформацияОКлючах = Новый ЧтениеТекста(ФайлКанонизированныйУзелИнформацияОКлючах, "windows-1251");
				СодержимоеФайлаУзелИнформацияОКлючах = ЧтениеФайлаУзелИнформацияОКлючах.Прочитать();
				ЧтениеФайлаУзелИнформацияОКлючах.Закрыть();
				СодержимоеФайлаУзелИнформацияОКлючах = СтрЗаменить(СодержимоеФайлаУзелИнформацияОКлючах,
					"<n1:KeyInfo Id=""KeyInfo"">", "<n1:KeyInfo xmlns:n1=""http://www.w3.org/2000/09/xmldsig#"" Id=""KeyInfo"">");
				ЗаписьФайлаУзелИнформацияОКлючах = Новый ЗаписьТекста(ФайлКанонизированныйУзелИнформацияОКлючах, "windows-1251");
				ЗаписьФайлаУзелИнформацияОКлючах.Записать(СодержимоеФайлаУзелИнформацияОКлючах);
				ЗаписьФайлаУзелИнформацияОКлючах.Закрыть();
			КонецЕсли;
			Если ИспользоватьВременноеХранилище Тогда
				ДвоичныеДанныеКанонизированногоУзлаИнформацииОКлючах = Новый ДвоичныеДанные(ФайлКанонизированныйУзелИнформацияОКлючах);
				КанонизированныйУзелИнформацияОКлючахВBase64 = ПоместитьВоВременноеХранилище(ДвоичныеДанныеКанонизированногоУзлаИнформацииОКлючах, Новый УникальныйИдентификатор);
			Иначе
				КанонизированныйУзелИнформацияОКлючахВBase64 = ПрочитатьBase64ИзФайла(ФайлКанонизированныйУзелИнформацияОКлючах);
			КонецЕсли;
			ДанныеДляПодписи.Вставить("КанонизированныйУзелИнформацияОКлючахXMLВBase64", КанонизированныйУзелИнформацияОКлючахВBase64);
		КонецЕсли;
		
		Если НЕ ДанныеВнутриПодписи И НЕ ПереданХешВместоОбъекта Тогда
			НастройкиКонвертации = Новый Структура;
			НастройкиКонвертации.Вставить("ПрефиксОбхода", 					ПрефиксОбхода);
			НастройкиКонвертации.Вставить("ПрефиксыПространствИмен", 		ПрефиксыПространствИмен);
			НастройкиКонвертации.Вставить("ПространствоИменПоУмолчанию", 	Неопределено);
			ФайлКанонизированныйУзелИнформацияОбОбъекте = ФайлКанонизированныйУзелDOM(
				УзелИнформацияОбОбъекте,,,
				Трансформация1_1 ИЛИ ДополнитьПространстваИменУзлаПодписи,
				Трансформация1_1 И НЕ НормализацияОтключена,
				ДополнительныеОбратныеТрансформации,
				НастройкиКонвертации);

			ЧтениеФайлаУзелИнформацияОбОбъекте = Новый ЧтениеТекста(ФайлКанонизированныйУзелИнформацияОбОбъекте, "windows-1251");
			СодержимоеФайлаУзелИнформацияОбОбъекте = ЧтениеФайлаУзелИнформацияОбОбъекте.Прочитать();
			ЧтениеФайлаУзелИнформацияОбОбъекте.Закрыть();
			Если Трансформация1_1 Тогда
				ЗначениеИдентификатораПодписываемыхДанных = ?(ИдентификаторПодписываемыхДанных = Неопределено, "Object", ИдентификаторПодписываемыхДанных);
				СодержимоеФайлаУзелИнформацияОбОбъекте = СтрЗаменить(СодержимоеФайлаУзелИнформацияОбОбъекте,
					"<n1:Object Id=""" + ЗначениеИдентификатораПодписываемыхДанных + """>",
					"<n1:Object xmlns:n1=""http://www.w3.org/2000/09/xmldsig#"" Id=""" + ЗначениеИдентификатораПодписываемыхДанных + """>");
			КонецЕсли;
			СодержимоеФайлаУзелИнформацияОбОбъекте = СтрЗаменить(СодержимоеФайлаУзелИнформацияОбОбъекте, "%SigningXML%", СодержимоеКанонизированногоФайлаXML);
		Иначе
			ФайлКанонизированныйУзелИнформацияОбОбъекте = ПолучитьИмяВременногоФайла();
			СодержимоеФайлаУзелИнформацияОбОбъекте = СодержимоеКанонизированногоФайлаXML;
		КонецЕсли;
		ЗаписьФайлаУзелИнформацияОбОбъекте = Новый ЗаписьТекста(ФайлКанонизированныйУзелИнформацияОбОбъекте, "windows-1251");
		ЗаписьФайлаУзелИнформацияОбОбъекте.Записать(СодержимоеФайлаУзелИнформацияОбОбъекте);
		ЗаписьФайлаУзелИнформацияОбОбъекте.Закрыть();
		Если ИспользоватьВременноеХранилище Тогда
			ДвоичныеДанныеКанонизированногоУзлаИнформацииОбОбъекте = Новый ДвоичныеДанные(ФайлКанонизированныйУзелИнформацияОбОбъекте);
			КанонизированныйУзелИнформацияОбОбъектеВBase64 = ПоместитьВоВременноеХранилище(ДвоичныеДанныеКанонизированногоУзлаИнформацииОбОбъекте, Новый УникальныйИдентификатор);
		Иначе
			КанонизированныйУзелИнформацияОбОбъектеВBase64 = ПрочитатьBase64ИзФайла(ФайлКанонизированныйУзелИнформацияОбОбъекте);
		КонецЕсли;
		ДанныеДляПодписи.Вставить("КанонизированныйУзелИнформацияОбОбъектеXMLВBase64", КанонизированныйУзелИнформацияОбОбъектеВBase64);
	КонецЕсли;
	
	Если ИспользоватьВременноеХранилище Тогда
		// элементы структуры уже помещены во временное хранилище
		Возврат ДанныеДляПодписи;
	Иначе
		Возврат ПоместитьВоВременноеХранилище(ДанныеДляПодписи, Новый УникальныйИдентификатор);
	КонецЕсли;
	
КонецФункции

Функция ФайлПреобразованиеККаноническомуXML(ДокументDOM,
	ТипКаноническогоXMLФайла = Неопределено,
	ФильтрXPath = Неопределено,
	Знач ИмяВыходногоФайла = Неопределено)
	
	Если НЕ ЗначениеЗаполнено(ИмяВыходногоФайла) Тогда
		ИмяВыходногоФайла = ПолучитьИмяВременногоФайла();
	КонецЕсли;
	
	Преобразование = Новый ПреобразованиеККаноническомуXML;
	Преобразование.КанонизироватьВФайл(ДокументDOM, ИмяВыходногоФайла, ТипКаноническогоXMLФайла, ФильтрXPath);
	
	Возврат ИмяВыходногоФайла;
	
КонецФункции

Функция ФайлКанонизированныйУзелDOM(
	УзелDOM,
	ИмяФайлаУзелDOM = Неопределено,
	РезультатXPath = Неопределено,
	КоррекцияКанонизации = Ложь,
	Нормализация1_1 = Ложь,
	ДополнительныеОбратныеТрансформации = Неопределено,
	НастройкиКонвертации = Неопределено)

	
	ПрефиксОбхода 				= НастройкиКонвертации.ПрефиксОбхода;
	ПрефиксыПространствИмен 	= НастройкиКонвертации.ПрефиксыПространствИмен;
	ПространствоИменПоУмолчанию = НастройкиКонвертации.ПространствоИменПоУмолчанию;
	
	Если НЕ ЗначениеЗаполнено(ИмяФайлаУзелDOM) Тогда
		ИмяФайлаУзелDOM = ПолучитьИмяВременногоФайла();
	КонецЕсли;
	
	ЗаписьXML = Новый КаноническаяЗаписьXML;
	ЗаписьXML.УстановитьСтроку(ТипКаноническогоXML.КаноническийXML);
	
	КаноническийDOM = Новый КаноническийDOM;
	КаноническийDOM.Канонизировать(УзелDOM, ЗаписьXML, РезультатXPath);
	
	СтрокаXML = ЗаписьXML.Закрыть();
	
	// обход ошибки платформы
	Пока Истина Цикл
		ПозицияНачала = СтрНайти(СтрокаXML, "&#xD;" + Символы.ПС);
		Если ПозицияНачала = 0 Тогда
			Прервать;
		КонецЕсли;
		
		ПозицияКонца = ПозицияНачала + 6;
		Пока ПозицияКонца <= СтрДлина(СтрокаXML) Цикл
			СимволСтроки = Сред(СтрокаXML, ПозицияКонца, 1);
			Если СокрЛП(СимволСтроки) <> "" Тогда
				Прервать;
			КонецЕсли;
			ПозицияКонца = ПозицияКонца + 1;
		КонецЦикла;
		
		Если СимволСтроки <> "<" Тогда // если "&#xD;" внутри значения, а не между тегами
			ПозицияКонца = ПозицияНачала + 5;
		КонецЕсли;
		
		СтрокаXML = Лев(СтрокаXML, ПозицияНачала - 1) + Сред(СтрокаXML, ПозицияКонца);
	КонецЦикла;
	
	// для обхода ошибки платформы, аналоги замены :xsi="" на xmlns:xsi=""
	
	Если КоррекцияКанонизации Тогда
		Если ДополнительныеОбратныеТрансформации <> Неопределено Тогда
			ИндексТрансформации = ДополнительныеОбратныеТрансформации.Количество() - 2;
			Пока ИндексТрансформации >= 0 Цикл
				СтрокаXML = СтрЗаменить(СтрокаXML, ДополнительныеОбратныеТрансформации[ИндексТрансформации],
					ДополнительныеОбратныеТрансформации[ИндексТрансформации + 1]);
				ИндексТрансформации = ИндексТрансформации - 2;
			КонецЦикла;
		КонецЕсли;
		Если ПрефиксОбхода <> Неопределено И ПрефиксыПространствИмен <> Неопределено Тогда
			// обратные преобразования обхода ошибки канонизации
			Для ИндексЭлемента = 0 По ПрефиксыПространствИмен.Количество() - 1 Цикл
				ПрефиксПространстваИмен = ПрефиксыПространствИмен[ПрефиксыПространствИмен.Количество() - 1 - ИндексЭлемента];
				СтрокаXML = СтрЗаменить(СтрокаXML,
					" xmlns:" + ПрефиксПространстваИмен + "=""" + ПрефиксОбхода, " xmlns:" + ПрефиксПространстваИмен + "=""");
				СтрокаXML = СтрЗаменить(СтрокаXML,
					" :" + ПрефиксПространстваИмен + "=""" + ПрефиксОбхода, " xmlns:" + ПрефиксПространстваИмен + "="""); // обход ошибки платформы
			КонецЦикла;
			СтрокаXML = СтрЗаменить(СтрокаXML, " xmlns=""" + ПрефиксОбхода, " xmlns=""");
			СтрокаXML = СтрЗаменить(СтрокаXML, " =""" + ПрефиксОбхода, " xmlns="""); // обход ошибки платформы
			СтрокаXML = СтрЗаменить(СтрокаXML, " ="""">", ">"); // обход ошибки платформы
		КонецЕсли;
	КонецЕсли;
	Если Нормализация1_1 Тогда
		СтрокаXML = XMLDSigНормализация1_1(СтрокаXML);
	КонецЕсли;
	Если ЗначениеЗаполнено(ПространствоИменПоУмолчанию) Тогда
		// обход ошибки платформы сортировки пространств имен при канонизации дочернего узла
		СтрокаXML = СтрЗаменить(СтрокаXML, " xmlns:" + ПространствоИменПоУмолчанию + "=", " xmlns=");
		СтрокаXML = СтрЗаменить(СтрокаXML, " :" + ПространствоИменПоУмолчанию + "=", " xmlns=");
	КонецЕсли;
	ЗаписьТекста = Новый ЗаписьТекста(ИмяФайлаУзелDOM, "UTF-8");
	ЗаписьТекста.Записать(СтрокаXML);
	ЗаписьТекста.Закрыть();
	
	// удалим из файла в кодировке "UTF-8" сигнатуру BOM
	ДанныеЧтение = Новый ДвоичныеДанные(ИмяФайлаУзелDOM);
	ДанныеЗапись = Base64Значение(Сред(Base64Строка(ДанныеЧтение), 5));
	ДанныеЗапись.Записать(ИмяФайлаУзелDOM);
	
	Возврат ИмяФайлаУзелDOM;
	
КонецФункции

Функция XMLDSigНормализация1_1(СтрокаXML)
	
	Результат = СтрЗаменить(СтрокаXML, "<", "<n1:");
	Результат = СтрЗаменить(Результат, "<n1:/", "</n1:");
	Результат = СтрЗаменить(Результат, " xmlns=", " xmlns:n1=");
	Возврат Результат;
	
КонецФункции

Функция ХешСтрокаВBase64КанонизированногоФайлаXMLИзПодписи(
		ПодписьКлючаВBase64,
		ПодписьВBase64,
		АдресДанныхДляПодписиФайлаXML,
		УдалитьПодписиИзВременногоХранилища = Ложь,
		Алгоритм = Неопределено) Экспорт
	
	Результат = Новый Структура("ХешСтрокаВBase64", Неопределено);
	
	Если ПодписьКлючаВBase64 <> Неопределено Тогда
		Результат.Вставить("ХешСтрокаВBase64Ключа", Неопределено);
		Результат.ХешСтрокаВBase64Ключа = ХешСтрокаВBase64ИзПодписи(ПодписьКлючаВBase64, УдалитьПодписиИзВременногоХранилища, Алгоритм);
	КонецЕсли;
	
	Если ПодписьКлючаВBase64 = Неопределено ИЛИ Результат.ХешСтрокаВBase64Ключа <> Неопределено Тогда
		Результат.ХешСтрокаВBase64 = ХешСтрокаВBase64ИзПодписи(ПодписьВBase64, УдалитьПодписиИзВременногоХранилища, Алгоритм);
	КонецЕсли;
	
	Если Результат.ХешСтрокаВBase64 = Неопределено И Алгоритм <> "GOST R 34.10-2012-256" И Алгоритм <> "GOST R 34.10-2012-512" Тогда
		Результат = ХешСтрокаВBase64КанонизированногоФайлаXML(АдресДанныхДляПодписиФайлаXML);
	КонецЕсли;
	
	Возврат Результат;
	
КонецФункции

Функция ХешСтрокаВBase64ИзПодписи(ПодписьВBase64, УдалитьПодписьИзВременногоХранилища = Ложь, Алгоритм = Неопределено)
	
	Перем ХешСтрокаВBase64;
	
	ИмяВременногоФайла = ПолучитьИмяВременногоФайла();
	Если ЭтоАдресВременногоХранилища(ПодписьВBase64) Тогда
		ДвоичныеДанныеПодписи = ПолучитьИзВременногоХранилища(ПодписьВBase64);
		Если УдалитьПодписьИзВременногоХранилища Тогда
			УдалитьИзВременногоХранилища(ПодписьВBase64);
		КонецЕсли;
	Иначе
		ДвоичныеДанныеПодписи = Base64Значение(ПодписьВBase64);
	КонецЕсли;
	ДвоичныеДанныеПодписи.Записать(ИмяВременногоФайла);
	МассивПодписи = ПрочитатьФайлВМассив(ИмяВременногоФайла);
	ОперацииСФайламиЭДКО.УдалитьВременныйФайл(ИмяВременногоФайла);
	
	МассивБайтовДляПоиска = ПолучитьМассивБайтовИзСтрокиВШестнадцатеричномПредставлении("30 2F 06 09 2A 86 48 86 F7 0D 01 09 04 31 22 04 20");
	МаксИндексДанныхПоиска = МассивБайтовДляПоиска.ВГраница();
	МаксИндексДанныхПодписи = МассивПодписи.ВГраница() - МаксИндексДанныхПоиска;
	ИндНайдено = -1;
	Для Инд = 500 По МаксИндексДанныхПодписи Цикл
		ИндНайдено = Инд;
		Для ИндПоиска = 0 По МаксИндексДанныхПоиска Цикл
			Если МассивПодписи[Инд + ИндПоиска] <> МассивБайтовДляПоиска[ИндПоиска] Тогда
				ИндНайдено = -1;
				Прервать;
			КонецЕсли;
		КонецЦикла;
		Если ИндНайдено > -1 Тогда
			Прервать;
		КонецЕсли;
	КонецЦикла;
	
	Если ИндНайдено >= 0 Тогда
		
		МассивБайт = Новый Массив;
		Попытка
			Для Инд = 1 По ?(Алгоритм = "GOST R 34.10-2012-512", 64, 32) Цикл
				МассивБайт.Добавить(МассивПодписи[ИндНайдено + МаксИндексДанныхПоиска + Инд]);
			КонецЦикла; 
		Исключение
			ТекстСообщения = НСтр("ru = 'Ошибка извлечения данных хеша из файла подписи!'");
			ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
			Возврат "";
		КонецПопытки;
		
		ИмяВременногоФайла = ПолучитьИмяВременногоФайла();
		ЗаписатьФайлИзМассива(ИмяВременногоФайла, МассивБайт);
		
		ДвоичныеДанныеФайла = Новый ДвоичныеДанные(ИмяВременногоФайла);
		ХешСтрокаВBase64 = Base64Строка(ДвоичныеДанныеФайла);
		ОперацииСФайламиЭДКО.УдалитьВременныйФайл(ИмяВременногоФайла);
		
	Иначе
		
		ХешСтрокаВBase64 = Неопределено;
		
	КонецЕсли;
	
	Возврат ХешСтрокаВBase64;
	
КонецФункции

Функция ХешСтрокаВBase64КанонизированногоФайлаXML(АдресДанныхДляПодписиФайлаXML)
	
	Результат = Новый Структура("ХешСтрокаВBase64", Неопределено);
	
	ИспользоватьВременноеХранилище = (ТипЗнч(АдресДанныхДляПодписиФайлаXML) <> Тип("Строка") ИЛИ НЕ ЭтоАдресВременногоХранилища(АдресДанныхДляПодписиФайлаXML));
	Если ИспользоватьВременноеХранилище Тогда
		ДанныеДляПодписиФайлаXML = АдресДанныхДляПодписиФайлаXML
	Иначе
		ДанныеДляПодписиФайлаXML = ПолучитьИзВременногоХранилища(АдресДанныхДляПодписиФайлаXML);
	КонецЕсли;
	
	Если ДанныеДляПодписиФайлаXML.Свойство("КанонизированныйУзелИнформацияОКлючахXMLВBase64") Тогда
		Результат.Вставить("ХешСтрокаВBase64Ключа", Неопределено);
		Если ИспользоватьВременноеХранилище И ЭтоАдресВременногоХранилища(ДанныеДляПодписиФайлаXML.КанонизированныйУзелИнформацияОКлючахXMLВBase64) Тогда
			ДвДанныеДляПодписиИнформацииОКлючахXML = ПолучитьИзВременногоХранилища(ДанныеДляПодписиФайлаXML.КанонизированныйУзелИнформацияОКлючахXMLВBase64);
		Иначе
			ДвДанныеДляПодписиИнформацииОКлючахXML = Base64Значение(ДанныеДляПодписиФайлаXML.КанонизированныйУзелИнформацияОКлючахXMLВBase64);
		КонецЕсли;
		Результат.ХешСтрокаВBase64Ключа = ВычислитьХешПоАлгоритмуГОСТР3411_94(ДвДанныеДляПодписиИнформацииОКлючахXML, Истина);
	КонецЕсли;
	
	КанонизированныйОбъектXMLВBase64 = ?(НЕ ДанныеДляПодписиФайлаXML.Свойство("КанонизированныйУзелИнформацияОбОбъектеXMLВBase64"),
		ДанныеДляПодписиФайлаXML.КанонизированныйФайлXMLВBase64, ДанныеДляПодписиФайлаXML.КанонизированныйУзелИнформацияОбОбъектеXMLВBase64);
	Если ИспользоватьВременноеХранилище И ЭтоАдресВременногоХранилища(КанонизированныйОбъектXMLВBase64) Тогда
		ДвДанныеДляПодписиОбъектаXML = ПолучитьИзВременногоХранилища(КанонизированныйОбъектXMLВBase64);
	Иначе
		ДвДанныеДляПодписиОбъектаXML = Base64Значение(КанонизированныйОбъектXMLВBase64);
	КонецЕсли;
	Результат.ХешСтрокаВBase64 = ВычислитьХешПоАлгоритмуГОСТР3411_94(ДвДанныеДляПодписиОбъектаXML, Истина);
	
	Возврат Результат;
	
КонецФункции

Функция ПодписанныйФайлXMLВBase64(
		АдресСодержимогоПодготовленногоФайлаXML,
		ХешСтрокаВBase64Ключа,
		ХешСтрокаВBase64,
		ПодписанныеДанныеВBase64,
		ВозвращатьАдресВоВременномХранилище = Ложь,
		ВключатьХэшСертификата = Ложь) Экспорт
	
	Если ВключатьХэшСертификата Тогда
		Если ВозвращатьАдресВоВременномХранилище Тогда
			ДанныеПодписи = ПолучитьИзВременногоХранилища(ПодписанныеДанныеВBase64);
			ПодписьВBase64 = Base64Строка(ДанныеПодписи);
		Иначе
			ПодписьВBase64 = ПодписанныеДанныеВBase64;
		КонецЕсли;
	Иначе
		ПодписьВBase64 = ПодписьИзПодписанныхДанныхBase64(ПодписанныеДанныеВBase64);
	КонецЕсли;
	
	Если Лев(ПодписьВBase64, 6) = "e1cib/" Тогда 
		ДанныеПодписи = ПолучитьИзВременногоХранилища(ПодписьВBase64);
		ПодписьВBase64 = Base64Строка(ДанныеПодписи);
	КонецЕсли;
	
	Если ХешСтрокаВBase64Ключа <> Неопределено Тогда
		ХешСтрокаВBase64Ключа = СтрЗаменить(ХешСтрокаВBase64Ключа, Символы.ВК, "");
		ХешСтрокаВBase64Ключа = СтрЗаменить(ХешСтрокаВBase64Ключа, Символы.ПС, "");
	КонецЕсли;
	ХешСтрокаВBase64 = СтрЗаменить(ХешСтрокаВBase64, Символы.ВК, "");
	ХешСтрокаВBase64 = СтрЗаменить(ХешСтрокаВBase64, Символы.ПС, "");
	ПодписьВBase64 = СтрЗаменить(ПодписьВBase64, Символы.ВК, "");
	ПодписьВBase64 = СтрЗаменить(ПодписьВBase64, Символы.ПС, "");
	
	ТекстовоеСодержимоеПодписанногоФайлаXML = ПолучитьИзВременногоХранилища(АдресСодержимогоПодготовленногоФайлаXML);
	Если ХешСтрокаВBase64Ключа <> Неопределено Тогда
		ТекстовоеСодержимоеПодписанногоФайлаXML = СтрЗаменить(ТекстовоеСодержимоеПодписанногоФайлаXML, "%KeyInfoDigestValue%", ХешСтрокаВBase64Ключа);
	КонецЕсли;
	ТекстовоеСодержимоеПодписанногоФайлаXML = СтрЗаменить(ТекстовоеСодержимоеПодписанногоФайлаXML, "%DigestValue%", ХешСтрокаВBase64);
	ТекстовоеСодержимоеПодписанногоФайлаXML = СтрЗаменить(ТекстовоеСодержимоеПодписанногоФайлаXML, "%SignatureValue%", ПодписьВBase64);
	
	ИмяФайлаXML = ПолучитьИмяВременногоФайла("xml");
	
	ЗаписьТекста = Новый ЗаписьТекста(ИмяФайлаXML, "UTF-8");
	ЗаписьТекста.Записать(ТекстовоеСодержимоеПодписанногоФайлаXML);
	ЗаписьТекста.Закрыть();
	
	ДвоичныеДанные = Новый ДвоичныеДанные(ИмяФайлаXML);
	
	// удалим из файла в кодировке "UTF-8" сигнатуру BOM
	ПодготовленныйФайлXMLВBase64 = Сред(Base64Строка(ДвоичныеДанные), 5);
	
	ОперацииСФайламиЭДКО.УдалитьВременныйФайл(ИмяФайлаXML);
	
	Если ВозвращатьАдресВоВременномХранилище Тогда
		ДвоичныеДанныеПодготовленногоФайлаXML = Base64Значение(ПодготовленныйФайлXMLВBase64);
		Возврат ПоместитьВоВременноеХранилище(ДвоичныеДанныеПодготовленногоФайлаXML, Новый УникальныйИдентификатор);
	Иначе
		Возврат ПодготовленныйФайлXMLВBase64;
	КонецЕсли;
	
КонецФункции

Функция ВычислитьХешПоАлгоритмуГОСТР3411_94(
		ХешируемыеДанные,
		ПредставитьРезультатХешированияВBase64 = Истина,
		ИмяНабораПараметров = "ГОСТР3411_94_НаборПараметровКриптоПро")
	
	Перем Данные;
	Перем РезультатХеширования;
	
	БуферДанных = Новый Массив;
	МаксРазмерБуфера = 262144;
	
	Если ТипЗнч(ХешируемыеДанные) = Тип("ДвоичныеДанные") Тогда
		Данные = ПолучитьМассивБайтовИзДвоичныхДанных(ХешируемыеДанные);
	ИначеЕсли ТипЗнч(ХешируемыеДанные) = Тип("Строка") Тогда
		Данные = ПолучитьМассивБайтовИзСтроки(ХешируемыеДанные);
	ИначеЕсли ТипЗнч(ХешируемыеДанные) = Тип("Массив") Тогда
		Данные = ХешируемыеДанные;
	Иначе
		Возврат Неопределено;
	КонецЕсли;
	
	РазмерДанных = Данные.Количество();
	
	ХешКонтекст = ИнициализироватьХешКонтекст(ИмяНабораПараметров);
	
	ОбработаноБайтов = 0;
	Пока ОбработаноБайтов < РазмерДанных Цикл
		
		РазмерБуфера = 0;
		Для Инд = ОбработаноБайтов По РазмерДанных - 1 Цикл
			БуферДанных.Добавить(Данные[Инд]);
			РазмерБуфера = РазмерБуфера + 1;
			Если РазмерБуфера >= МаксРазмерБуфера Тогда
				Прервать;
			КонецЕсли;
		КонецЦикла;
		
		Если РазмерБуфера = 0 Тогда
			Прервать;
		КонецЕсли;
		
		ВыполнитьОперациюХешированияДанныхБуфера(ХешКонтекст, БуферДанных);
		
		БуферДанных.Очистить();
		
		ОбработаноБайтов = ОбработаноБайтов + РазмерБуфера;
		
	КонецЦикла;
	
	ВыполнитьЗавершающуюОперациюХеширования(ХешКонтекст, РезультатХеширования);
	
	Если ПредставитьРезультатХешированияВBase64 Тогда
		Возврат ПолучитьСтрокуBase64ИзМассиваБайтов(РезультатХеширования);
	КонецЕсли;
	
	РультатВШестнадцатеричномПредставлении = "";
	Для каждого ЗначениеБайта Из РезультатХеширования Цикл
		РультатВШестнадцатеричномПредставлении = РультатВШестнадцатеричномПредставлении + ЧислоВШестнадцатеричномПредставлении(ЗначениеБайта, 1);
	КонецЦикла;
	
	Возврат РультатВШестнадцатеричномПредставлении;
	
КонецФункции

Функция ПрочитатьФайлВМассив(ИмяФайла, Позиция = 0, Количество = Неопределено)
	
	Результат = Новый Массив;
	
	ЧтениеТекста = Новый ЧтениеТекста(ИмяФайла, "ISO-8859-1", Символы.ПС, Символы.ПС);
	
	Прочтено = 0;
	Пока Позиция > Прочтено Цикл
		ДлинаБлока = Позиция - Прочтено;
		ДлинаБлока = ?(ДлинаБлока <= 1000000, ДлинаБлока, 1000000);
		ТекстИзФайла = ЧтениеТекста.Прочитать(ДлинаБлока);
		Если ТекстИзФайла = Неопределено ИЛИ СтрДлина(ТекстИзФайла) < ДлинаБлока Тогда
			ЧтениеТекста.Закрыть();
			Возврат Результат;
		КонецЕсли;
		Прочтено = Прочтено + ДлинаБлока;
	КонецЦикла;
	
	Осталось = Количество;
	Пока Осталось = Неопределено ИЛИ Осталось > 0 Цикл
		СимволТекста = ЧтениеТекста.Прочитать(1);
		Если СимволТекста = Неопределено Тогда
			Прервать;
		КонецЕсли;
		КодСимволаТекста = КодСимвола(СимволТекста);
		
		Результат.Добавить(КодСимволаТекста);
		
		Если Осталось <> Неопределено Тогда
			Осталось = Осталось - 1;
		КонецЕсли;
	КонецЦикла;
	ЧтениеТекста.Закрыть();
	
	Возврат Результат;
	
КонецФункции

Процедура ЗаписатьФайлИзМассива(ИмяФайла, Массив)
	
	ЗаписьТекста = Новый ЗаписьТекста(ИмяФайла, "ISO-8859-1", Символы.ПС, , Символы.ПС);
	
	Для ИндексВМассиве = 0 По Массив.ВГраница() Цикл
		СимволИзМассива = Символ(Массив[ИндексВМассиве]);
		ЗаписьТекста.Записать(СимволИзМассива);
	КонецЦикла;
	ЗаписьТекста.Закрыть();
	
КонецПроцедуры

Функция СкопироватьФрагментИзФайла(ИмяФайла, ИмяФайлаРезультата, Позиция = 0, Количество = Неопределено)
	
	Скопировано = 0;
	
	ЧтениеТекста = Новый ЧтениеТекста(ИмяФайла, "ISO-8859-1", Символы.ПС, Символы.ПС);
	ЗаписьТекста = Новый ЗаписьТекста(ИмяФайлаРезультата, "ISO-8859-1", Символы.ПС, , Символы.ПС);
	
	Прочтено = 0;
	Пока Прочтено < Позиция Цикл
		ДлинаБлока = Позиция - Прочтено;
		ДлинаБлока = ?(ДлинаБлока <= 1000000, ДлинаБлока, 1000000);
		ТекстИзФайла = ЧтениеТекста.Прочитать(ДлинаБлока);
		Если ТекстИзФайла = Неопределено ИЛИ СтрДлина(ТекстИзФайла) < ДлинаБлока Тогда
			ЗаписьТекста.Закрыть();
			ЧтениеТекста.Закрыть();
			Возврат Скопировано;
		КонецЕсли;
		Прочтено = Прочтено + ДлинаБлока;
	КонецЦикла;
	
	Пока Количество = Неопределено ИЛИ Скопировано < Количество Цикл
		ДлинаБлока = ?(Количество <> Неопределено И Количество - Скопировано <= 1000000, Количество - Скопировано, 1000000);
		ТекстИзФайла = ЧтениеТекста.Прочитать(ДлинаБлока);
		Если ТекстИзФайла <> Неопределено Тогда
			ЗаписьТекста.Записать(ТекстИзФайла);
			Скопировано = Скопировано + СтрДлина(ТекстИзФайла);
		КонецЕсли;
		Если ТекстИзФайла = Неопределено ИЛИ СтрДлина(ТекстИзФайла) < ДлинаБлока Тогда
			Прервать;
		КонецЕсли;
	КонецЦикла;
	ЗаписьТекста.Закрыть();
	ЧтениеТекста.Закрыть();
	
	Возврат Скопировано;
	
КонецФункции

// Вспомогательные процедуры и функции для вычисления хеш-функции по алгоритму ГОСТ Р 34.11-94

Функция ИнициализироватьХешКонтекст(ИмяНабораПараметров)
	
	Перем Блок;
	
	// Набор тестовых параметров ГОСТ Р 34.11-94
	ГОСТР3411_94_НаборТестовыхПараметров = Новый Структура("К8, К7, К6, К5, К4, К3, К2, К1",
		ПолучитьМассивБайтовИзСтрокиВШестнадцатеричномПредставлении("01 0F 0D 00 05 07 0A 04 09 02 03 0E 06 0B 08 0C"),
		ПолучитьМассивБайтовИзСтрокиВШестнадцатеричномПредставлении("0D 0B 04 01 03 0F 05 09 00 0A 0E 07 06 08 02 0C"),
		ПолучитьМассивБайтовИзСтрокиВШестнадцатеричномПредставлении("04 0B 0A 00 07 02 01 0D 03 06 08 05 09 0C 0F 0E"),
		ПолучитьМассивБайтовИзСтрокиВШестнадцатеричномПредставлении("06 0C 07 01 05 0F 0D 08 04 0A 09 0E 00 03 0B 02"),
		ПолучитьМассивБайтовИзСтрокиВШестнадцатеричномПредставлении("07 0D 0A 01 00 08 09 0F 0E 04 06 0C 0B 02 05 03"),
		ПолучитьМассивБайтовИзСтрокиВШестнадцатеричномПредставлении("05 08 01 0D 0A 03 04 02 0E 0F 0C 07 06 00 09 0B"),
		ПолучитьМассивБайтовИзСтрокиВШестнадцатеричномПредставлении("0E 0B 04 0C 06 0D 0F 0A 02 03 08 01 00 07 05 09"),
		ПолучитьМассивБайтовИзСтрокиВШестнадцатеричномПредставлении("04 0A 09 02 0D 08 00 0E 06 0B 01 0C 07 0F 05 03"));
	
	// Набор параметров для хеш-функции 1.2.643.2.9.1.6.1
	ГОСТР3411_94_НаборПараметровКриптоПро = Новый Структура("К8, К7, К6, К5, К4, К3, К2, К1",
		ПолучитьМассивБайтовИзСтрокиВШестнадцатеричномПредставлении("01 03 0A 09 05 0B 04 0F 08 06 07 0E 0D 00 02 0C"),
		ПолучитьМассивБайтовИзСтрокиВШестнадцатеричномПредставлении("0D 0E 04 01 07 00 05 0A 03 0C 08 0F 06 02 09 0B"),
		ПолучитьМассивБайтовИзСтрокиВШестнадцатеричномПредставлении("07 06 02 04 0D 09 0F 00 0A 01 05 0B 08 0E 0C 03"),
		ПолучитьМассивБайтовИзСтрокиВШестнадцатеричномПредставлении("07 06 04 0B 09 0C 02 0A 01 08 00 0E 0F 0D 03 05"),
		ПолучитьМассивБайтовИзСтрокиВШестнадцатеричномПредставлении("04 0A 07 0C 00 0F 02 08 0E 01 06 05 0D 0B 09 03"),
		ПолучитьМассивБайтовИзСтрокиВШестнадцатеричномПредставлении("07 0F 0C 0E 09 04 01 00 03 0B 05 02 06 0A 08 0D"),
		ПолучитьМассивБайтовИзСтрокиВШестнадцатеричномПредставлении("05 0F 04 00 02 0D 0B 09 01 07 06 03 0C 0E 0A 08"),
		ПолучитьМассивБайтовИзСтрокиВШестнадцатеричномПредставлении("0A 04 05 06 08 01 03 07 0D 0C 0E 00 09 02 0B 0F"));
	
	// Набор тестовых параметров ГОСТ 28147
	ГОСТ28147_НаборТестовыхПараметров = Новый Структура("К8, К7, К6, К5, К4, К3, К2, К1",
		ПолучитьМассивБайтовИзСтрокиВШестнадцатеричномПредставлении("0C 06 05 02 0B 00 09 0D 03 0E 07 0A 0F 04 01 08"),
		ПолучитьМассивБайтовИзСтрокиВШестнадцатеричномПредставлении("09 0B 0C 00 03 06 07 05 04 08 0E 0F 01 0A 02 0D"),
		ПолучитьМассивБайтовИзСтрокиВШестнадцатеричномПредставлении("08 0F 06 0B 01 09 0C 05 0D 03 07 0A 00 0E 02 04"),
		ПолучитьМассивБайтовИзСтрокиВШестнадцатеричномПредставлении("03 0E 05 09 06 08 00 0D 0A 0B 07 0C 02 01 0F 04"),
		ПолучитьМассивБайтовИзСтрокиВШестнадцатеричномПредставлении("0E 09 0B 02 05 0F 07 01 00 0D 0C 06 0A 04 03 08"),
		ПолучитьМассивБайтовИзСтрокиВШестнадцатеричномПредставлении("0D 08 0E 0C 07 03 09 0A 01 05 02 04 06 0F 00 0B"),
		ПолучитьМассивБайтовИзСтрокиВШестнадцатеричномПредставлении("0C 09 0F 0E 08 01 03 0A 02 07 04 0D 06 00 0B 05"),
		ПолучитьМассивБайтовИзСтрокиВШестнадцатеричномПредставлении("04 02 0F 05 09 01 00 08 0E 03 0B 0C 0D 07 0A 06"));
	
	// 1.2.643.2.2.31.1
	ГОСТ28147_НаборПараметровКриптоПро_A = Новый Структура("К8, К7, К6, К5, К4, К3, К2, К1",
		ПолучитьМассивБайтовИзСтрокиВШестнадцатеричномПредставлении("0B 0A 0F 05 00 0C 0E 08 06 02 03 09 01 07 0D 04"),
		ПолучитьМассивБайтовИзСтрокиВШестнадцатеричномПредставлении("01 0D 02 09 07 0A 06 00 08 0C 04 05 0F 03 0B 0E"),
		ПолучитьМассивБайтовИзСтрокиВШестнадцатеричномПредставлении("03 0A 0D 0C 01 02 00 0B 07 05 09 04 08 0F 0E 06"),
		ПолучитьМассивБайтовИзСтрокиВШестнадцатеричномПредставлении("0B 05 01 09 08 0D 0F 00 0E 04 02 03 0C 07 0A 06"),
		ПолучитьМассивБайтовИзСтрокиВШестнадцатеричномПредставлении("0E 07 0A 0C 0D 01 03 09 00 02 0B 04 0F 08 05 06"),
		ПолучитьМассивБайтовИзСтрокиВШестнадцатеричномПредставлении("0E 04 06 02 0B 03 0D 08 0C 0F 05 0A 00 07 01 09"),
		ПолучитьМассивБайтовИзСтрокиВШестнадцатеричномПредставлении("03 07 0E 09 08 0A 0F 00 05 02 06 0C 0B 04 0D 01"),
		ПолучитьМассивБайтовИзСтрокиВШестнадцатеричномПредставлении("09 06 03 02 08 0B 01 07 0A 04 0E 0F 0C 00 0D 05"));
	
	// 1.2.643.2.2.31.2
	ГОСТ28147_НаборПараметровКриптоПро_B = Новый Структура("К8, К7, К6, К5, К4, К3, К2, К1",
		ПолучитьМассивБайтовИзСтрокиВШестнадцатеричномПредставлении("00 04 0B 0E 08 03 07 01 0A 02 09 06 0F 0D 05 0C"),
		ПолучитьМассивБайтовИзСтрокиВШестнадцатеричномПредставлении("05 02 0A 0B 09 01 0C 03 07 04 0D 00 06 0F 08 0E"),
		ПолучитьМассивБайтовИзСтрокиВШестнадцатеричномПредставлении("08 03 02 06 04 0D 0E 0B 0C 01 07 0F 0A 00 09 05"),
		ПолучитьМассивБайтовИзСтрокиВШестнадцатеричномПредставлении("02 07 0C 0F 09 05 0A 0B 01 04 00 0D 06 08 0E 03"),
		ПолучитьМассивБайтовИзСтрокиВШестнадцатеричномПредставлении("07 05 00 0D 0B 06 01 02 03 0A 0C 0F 04 0E 09 08"),
		ПолучитьМассивБайтовИзСтрокиВШестнадцатеричномПредставлении("0E 0C 00 0A 09 02 0D 0B 07 05 08 0F 03 06 01 04"),
		ПолучитьМассивБайтовИзСтрокиВШестнадцатеричномПредставлении("00 01 02 0A 04 0D 05 0C 09 07 03 0F 0B 08 06 0E"),
		ПолучитьМассивБайтовИзСтрокиВШестнадцатеричномПредставлении("08 04 0B 01 03 05 00 09 02 0E 0A 0C 0D 06 07 0F"));
	
	// 1.2.643.2.2.31.3
	ГОСТ28147_НаборПараметровКриптоПро_C = Новый Структура("К8, К7, К6, К5, К4, К3, К2, К1",
		ПолучитьМассивБайтовИзСтрокиВШестнадцатеричномПредставлении("07 04 00 05 0A 02 0F 0E 0C 06 01 0B 0D 09 03 08"),
		ПолучитьМассивБайтовИзСтрокиВШестнадцатеричномПредставлении("0A 09 06 08 0D 0E 02 00 0F 03 05 0B 04 01 0C 07"),
		ПолучитьМассивБайтовИзСтрокиВШестнадцатеричномПредставлении("0C 09 0B 01 08 0E 02 04 07 03 06 05 0A 00 0F 0D"),
		ПолучитьМассивБайтовИзСтрокиВШестнадцатеричномПредставлении("08 0D 0B 00 04 05 01 02 09 03 0C 0E 06 0F 0A 07"),
		ПолучитьМассивБайтовИзСтрокиВШестнадцатеричномПредставлении("03 06 00 01 05 0D 0A 08 0B 02 09 07 0E 0F 0C 04"),
		ПолучитьМассивБайтовИзСтрокиВШестнадцатеричномПредставлении("08 02 05 00 04 09 0F 0A 03 07 0C 0D 06 0E 01 0B"),
		ПолучитьМассивБайтовИзСтрокиВШестнадцатеричномПредставлении("00 01 07 0D 0B 04 05 02 08 0E 0F 0C 09 0A 06 03"),
		ПолучитьМассивБайтовИзСтрокиВШестнадцатеричномПредставлении("01 0B 0C 02 09 0D 00 0F 04 05 08 0E 0A 07 06 03"));
	
	// 1.2.643.2.2.31.4
	ГОСТ28147_НаборПараметровКриптоПро_D = Новый Структура("К8, К7, К6, К5, К4, К3, К2, К1",
		ПолучитьМассивБайтовИзСтрокиВШестнадцатеричномПредставлении("01 0A 06 08 0F 0B 00 04 0C 03 05 09 07 0D 02 0E"),
		ПолучитьМассивБайтовИзСтрокиВШестнадцатеричномПредставлении("03 00 06 0F 01 0E 09 02 0D 08 0C 04 0B 0A 05 07"),
		ПолучитьМассивБайтовИзСтрокиВШестнадцатеричномПредставлении("08 00 0F 03 02 05 0E 0B 01 0A 04 07 0C 09 0D 06"),
		ПолучитьМассивБайтовИзСтрокиВШестнадцатеричномПредставлении("00 0C 08 09 0D 02 0A 0B 07 03 06 05 04 0E 0F 01"),
		ПолучитьМассивБайтовИзСтрокиВШестнадцатеричномПредставлении("01 05 0E 0C 0A 07 00 0D 06 02 0B 04 09 03 0F 08"),
		ПолучитьМассивБайтовИзСтрокиВШестнадцатеричномПредставлении("01 0C 0B 00 0F 0E 06 05 0A 0D 04 08 09 03 07 02"),
		ПолучитьМассивБайтовИзСтрокиВШестнадцатеричномПредставлении("0B 06 03 04 0C 0F 0E 02 07 0D 08 00 05 0A 09 01"),
		ПолучитьМассивБайтовИзСтрокиВШестнадцатеричномПредставлении("0F 0C 02 0A 06 04 05 00 07 09 0E 0D 01 0B 08 03"));
		
	НаборыПараметров = Новый Структура();
	НаборыПараметров.Вставить("ГОСТР3411_94_НаборТестовыхПараметров",  ГОСТР3411_94_НаборТестовыхПараметров);
	НаборыПараметров.Вставить("ГОСТР3411_94_НаборПараметровКриптоПро", ГОСТР3411_94_НаборПараметровКриптоПро);
	НаборыПараметров.Вставить("ГОСТ28147_НаборТестовыхПараметров",     ГОСТ28147_НаборТестовыхПараметров);
	НаборыПараметров.Вставить("ГОСТ28147_НаборПараметровКриптоПро_A",  ГОСТ28147_НаборПараметровКриптоПро_A);
	НаборыПараметров.Вставить("ГОСТ28147_НаборПараметровКриптоПро_B",  ГОСТ28147_НаборПараметровКриптоПро_B);
	НаборыПараметров.Вставить("ГОСТ28147_НаборПараметровКриптоПро_C",  ГОСТ28147_НаборПараметровКриптоПро_C);
	НаборыПараметров.Вставить("ГОСТ28147_НаборПараметровКриптоПро_D",  ГОСТ28147_НаборПараметровКриптоПро_D);
	
	// Шифроконтекст включает в себя ключ и обработанный блок параметров
	ГОСТ_Шифроконтекст = Новый Структура("К, К87, К65, К43, К21", Новый Массив(8), Новый Массив(256), Новый Массив(256), Новый Массив(256), Новый Массив(256));
	
	НаборыПараметров.Свойство(ИмяНабораПараметров, Блок);
	Если Блок = Неопределено Тогда
		Блок = НаборыПараметров["ГОСТР3411_94_НаборТестовыхПараметров"];
	КонецЕсли;
	
	Для Инд = 0 По 255 Цикл
		Инд1 = Цел(Инд / 16); // >>4
		Инд2 = Инд % 16;      // &15
		ГОСТ_Шифроконтекст.К87[Инд] = ЧислоВБинарныйМассив(Блок.К8[Инд1] * 16 + Блок.К7[Инд2], 8);
		ГОСТ_Шифроконтекст.К65[Инд] = ЧислоВБинарныйМассив(Блок.К6[Инд1] * 16 + Блок.К5[Инд2], 8);
		ГОСТ_Шифроконтекст.К43[Инд] = ЧислоВБинарныйМассив(Блок.К4[Инд1] * 16 + Блок.К3[Инд2], 8);
		ГОСТ_Шифроконтекст.К21[Инд] = ЧислоВБинарныйМассив(Блок.К2[Инд1] * 16 + Блок.К1[Инд2], 8);
	КонецЦикла;
	
	ГОСТ_ХешКонтекст = Новый Структура("Длина, Шифроконтекст, Обработано, БуферH, БуферS, Остатки", 0, ГОСТ_Шифроконтекст, 0, Новый Массив(32), Новый Массив(32), Новый Массив(32));
	Для Инд = 0 По 31 Цикл
		ГОСТ_ХешКонтекст.БуферH[Инд]  = ЧислоВБинарныйМассив(0, 8);
		ГОСТ_ХешКонтекст.БуферS[Инд]  = ЧислоВБинарныйМассив(0, 8);
		ГОСТ_ХешКонтекст.Остатки[Инд] = ЧислоВБинарныйМассив(0, 8);
	КонецЦикла;
	
	Возврат ГОСТ_ХешКонтекст;
	
КонецФункции

Процедура ВыполнитьОперациюХешированияДанныхБуфера(ХешКонтекст, Буфер)
	
	ТекИндекс = 0;
	
	РазмерБуфераВБайтах = Буфер.Количество();
	
	Если ХешКонтекст.Обработано > 0 Тогда
		ОбработатьБайтов = 32 - ХешКонтекст.Обработано;
		Если ОбработатьБайтов > РазмерБуфераВБайтах Тогда
			ОбработатьБайтов = РазмерБуфераВБайтах;
		КонецЕсли;
		Для Инд = 0 По ОбработатьБайтов - 1 Цикл
			ХешКонтекст.Остатки[ХешКонтекст.Обработано + Инд] = ЧислоВБинарныйМассив(Буфер[Инд], 8);
		КонецЦикла;
		ХешКонтекст.Обработано = ХешКонтекст.Обработано + ОбработатьБайтов;
		Если ХешКонтекст.Обработано < 32 Тогда
			Возврат;
		КонецЕсли;
		ТекИндекс = ОбработатьБайтов;
		ВыполнитьОперациюХеширования(ХешКонтекст.Шифроконтекст, ХешКонтекст.БуферH, ХешКонтекст.Остатки);
		СуммироватьБлоки(ХешКонтекст.Остатки, ХешКонтекст.БуферS, 32);
		ХешКонтекст.Длина = ХешКонтекст.Длина + 32;
		ХешКонтекст.Обработано = 0;
	КонецЕсли;
	
	Пока ТекИндекс <= РазмерБуфераВБайтах - 32 Цикл
		Блок = Новый Массив(32);
		Для Инд = 0 По 31 Цикл
			Блок[Инд] = ЧислоВБинарныйМассив(Буфер[ТекИндекс + Инд], 8);
		КонецЦикла;
		ВыполнитьОперациюХеширования(ХешКонтекст.Шифроконтекст, ХешКонтекст.БуферH, Блок);
		СуммироватьБлоки(Блок, ХешКонтекст.БуферS, 32);
		ХешКонтекст.Длина = ХешКонтекст.Длина + 32;
		ТекИндекс = ТекИндекс + 32;
	КонецЦикла;
	
	Если ТекИндекс <> РазмерБуфераВБайтах Тогда
		ХешКонтекст.Обработано = РазмерБуфераВБайтах - ТекИндекс;
		Для Инд = 0 По ХешКонтекст.Обработано - 1 Цикл
			ХешКонтекст.Остатки[Инд] = ЧислоВБинарныйМассив(Буфер[ТекИндекс + Инд], 8);
		КонецЦикла;
	КонецЕсли;
	
КонецПроцедуры

Процедура ВыполнитьОперациюХеширования(Шифроконтекст, БуферH, БуферM)
	
	БуферU = Новый Массив(32);
	БуферW = Новый Массив(32);
	БуферV = Новый Массив(32);
	БуферS = Новый Массив(32);
	
	Ключ   = Новый Массив(32);
	
	Для Инд = 0 По 31 Цикл
		БуферS[Инд] = ЧислоВБинарныйМассив(0, 8);
	КонецЦикла;
	
	// Вычисляем первый ключ
	ИсключающееИЛИДляБлоков(БуферW, БуферH, БуферM, 32);
	РаспределениеБайтовДляБлоков(БуферW, Ключ);
	// Шифруем первые 8 байт буфера H первым ключем
	ЗашифроватьСКлючемПоГОСТ(Шифроконтекст, Ключ, БуферH, БуферS);
	
	// Вычисляем второй ключ
	ЦиклическоеИсключающееИЛИДляБлоков(БуферH, БуферU);
	ЦиклическоеИсключающееИЛИДляБлоков(БуферM, БуферV);
	ЦиклическоеИсключающееИЛИДляБлоков(БуферV, БуферV);
	ИсключающееИЛИДляБлоков(БуферW, БуферU, БуферV, 32);
	РаспределениеБайтовДляБлоков(БуферW, Ключ);
	// Шифруем следующие 8 байт буфера H вторым ключем
	ЗашифроватьСКлючемПоГОСТ(Шифроконтекст, Ключ, БуферH, БуферS, 8);
	
	// Вычисляем третий ключ
	ЦиклическоеИсключающееИЛИДляБлоков(БуферU, БуферU);
	БуферU[31] = ОперацияПобитовоеНЕ(БуферU[31]);
	БуферU[29] = ОперацияПобитовоеНЕ(БуферU[29]);
	БуферU[28] = ОперацияПобитовоеНЕ(БуферU[28]);
	БуферU[24] = ОперацияПобитовоеНЕ(БуферU[24]);
	БуферU[23] = ОперацияПобитовоеНЕ(БуферU[23]);
	БуферU[20] = ОперацияПобитовоеНЕ(БуферU[20]);
	БуферU[18] = ОперацияПобитовоеНЕ(БуферU[18]);
	БуферU[17] = ОперацияПобитовоеНЕ(БуферU[17]);
	БуферU[14] = ОперацияПобитовоеНЕ(БуферU[14]);
	БуферU[12] = ОперацияПобитовоеНЕ(БуферU[12]);
	БуферU[10] = ОперацияПобитовоеНЕ(БуферU[10]);
	БуферU[8]  = ОперацияПобитовоеНЕ(БуферU[8]);
	БуферU[7]  = ОперацияПобитовоеНЕ(БуферU[7]);
	БуферU[5]  = ОперацияПобитовоеНЕ(БуферU[5]);
	БуферU[3]  = ОперацияПобитовоеНЕ(БуферU[3]);
	БуферU[1]  = ОперацияПобитовоеНЕ(БуферU[1]);
	ЦиклическоеИсключающееИЛИДляБлоков(БуферV, БуферV);
	ЦиклическоеИсключающееИЛИДляБлоков(БуферV, БуферV);
	ИсключающееИЛИДляБлоков(БуферW, БуферU, БуферV, 32);
	РаспределениеБайтовДляБлоков(БуферW, Ключ);
	// Шифруем следующие 8 байт буфера H третим ключем
	ЗашифроватьСКлючемПоГОСТ(Шифроконтекст, Ключ, БуферH, БуферS, 16);
	
	// Вычисляем четвертый ключ
	ЦиклическоеИсключающееИЛИДляБлоков(БуферU, БуферU);
	ЦиклическоеИсключающееИЛИДляБлоков(БуферV, БуферV);
	ЦиклическоеИсключающееИЛИДляБлоков(БуферV, БуферV);
	ИсключающееИЛИДляБлоков(БуферW, БуферU, БуферV, 32);
	РаспределениеБайтовДляБлоков(БуферW, Ключ);
	// Шифруем последние 8 байт буфера H четвертым ключем
	ЗашифроватьСКлючемПоГОСТ(Шифроконтекст, Ключ, БуферH, БуферS, 24);
	
	Для Инд = 0 По 11 Цикл
		ПреобразованиеДляБлока(БуферS);
	КонецЦикла;
	ИсключающееИЛИДляБлоков(БуферS, БуферS, БуферM, 32);
	ПреобразованиеДляБлока(БуферS);
	ИсключающееИЛИДляБлоков(БуферS, БуферS, БуферH, 32);
	Для Инд = 0 По 60 Цикл
		ПреобразованиеДляБлока(БуферS);
	КонецЦикла;
	Для Инд = 0 По 31 Цикл
		БуферH[Инд] = КопияМассива(БуферS[Инд]);
	КонецЦикла;
	
КонецПроцедуры

Процедура ВыполнитьЗавершающуюОперациюХеширования(ХешКонтекст, РезультатХеширования)
	
	Буфер  = Новый Массив(32);
	БуферH = Новый Массив(32);
	БуферS = Новый Массив(32);
	
	ИтоговаяДлина = ХешКонтекст.Длина;
	
	Для Инд = 0 По 31 Цикл
		БуферH[Инд] = КопияМассива(ХешКонтекст.БуферH[Инд]);
		БуферS[Инд] = КопияМассива(ХешКонтекст.БуферS[Инд]);
	КонецЦикла;
	
	Если ХешКонтекст.Обработано > 0 Тогда
		Для Инд = 0 По 31 Цикл
			Буфер[Инд]  = ЧислоВБинарныйМассив(0, 8);
		КонецЦикла;
		Для Инд = 0 По ХешКонтекст.Обработано - 1 Цикл
			Буфер[Инд] = КопияМассива(ХешКонтекст.Остатки[Инд]);
		КонецЦикла;
		ВыполнитьОперациюХеширования(ХешКонтекст.Шифроконтекст, БуферH, Буфер);
		СуммироватьБлоки(Буфер, БуферS, 32);
		ИтоговаяДлина = ИтоговаяДлина + ХешКонтекст.Обработано;
	КонецЕсли;
	
	Для Инд = 0 По 31 Цикл
		Буфер[Инд] = ЧислоВБинарныйМассив(0, 8);
	КонецЦикла;
	
	// Итоговая длина хеша в битах
	ИтоговаяДлина = ИтоговаяДлина * 8;
	
	ТекИндБуфера = 0;
	Пока ИтоговаяДлина > 0 Цикл
		Буфер[ТекИндБуфера] = ЧислоВБинарныйМассив(ИтоговаяДлина % 256, 8);
		ТекИндБуфера = ТекИндБуфера + 1;
		ИтоговаяДлина = Цел(ИтоговаяДлина / 256);
	КонецЦикла;
	
	ВыполнитьОперациюХеширования(ХешКонтекст.Шифроконтекст, БуферH, Буфер);
	ВыполнитьОперациюХеширования(ХешКонтекст.Шифроконтекст, БуферH, БуферS);
	
	РезультатХеширования = Новый Массив(32);
	
	Для Инд = 0 По 31 Цикл
		РезультатХеширования[Инд] = БинарныйМассивВЧисло(БуферH[Инд]);
	КонецЦикла;
	
КонецПроцедуры

Процедура ЗашифроватьСКлючемПоГОСТ(Шифроконтекст, Ключ, БлокИсточник, БлокПриемник, НачальныйИндекс = 0)
	
	// Установим 256-битный ключ
	Инд2 = 0;
	Для Инд1 = 0 По 7 Цикл
		Результат = Новый Массив(32);
		Байт0 = Ключ[Инд2    ];
		Байт1 = Ключ[Инд2 + 1];
		Байт2 = Ключ[Инд2 + 2];
		Байт3 = Ключ[Инд2 + 3];
		Для Инд = 0 По 7 Цикл
			Результат[Инд     ] = Байт0[Инд];
			Результат[Инд + 8 ] = Байт1[Инд];
			Результат[Инд + 16] = Байт2[Инд];
			Результат[Инд + 24] = Байт3[Инд];
		КонецЦикла;
		Шифроконтекст.К[Инд1] = Результат;
		Инд2 = Инд2 + 4;
	КонецЦикла;
	
	// Шифруем один 64-битный блок
	M1 = Новый Массив(32);
	M2 = Новый Массив(32);
	Байт0 = БлокИсточник[НачальныйИндекс    ];
	Байт1 = БлокИсточник[НачальныйИндекс + 1];
	Байт2 = БлокИсточник[НачальныйИндекс + 2];
	Байт3 = БлокИсточник[НачальныйИндекс + 3];
	Байт4 = БлокИсточник[НачальныйИндекс + 4];
	Байт5 = БлокИсточник[НачальныйИндекс + 5];
	Байт6 = БлокИсточник[НачальныйИндекс + 6];
	Байт7 = БлокИсточник[НачальныйИндекс + 7];
	Для Инд = 0 По 7 Цикл
		M1[Инд     ] = Байт0[Инд];
		M1[Инд + 8 ] = Байт1[Инд];
		M1[Инд + 16] = Байт2[Инд];
		M1[Инд + 24] = Байт3[Инд];
		M2[Инд     ] = Байт4[Инд];
		M2[Инд + 8 ] = Байт5[Инд];
		M2[Инд + 16] = Байт6[Инд];
		M2[Инд + 24] = Байт7[Инд];
	КонецЦикла;
	
	Для Инд = 0 По 2 Цикл
		M2 = ОперацияПобитовоеИсключающееИЛИ(M2, ПреобразованиеСлово32(Шифроконтекст, БинарнаяСумма(M1, Шифроконтекст.К[0])));
		M1 = ОперацияПобитовоеИсключающееИЛИ(M1, ПреобразованиеСлово32(Шифроконтекст, БинарнаяСумма(M2, Шифроконтекст.К[1])));
		M2 = ОперацияПобитовоеИсключающееИЛИ(M2, ПреобразованиеСлово32(Шифроконтекст, БинарнаяСумма(M1, Шифроконтекст.К[2])));
		M1 = ОперацияПобитовоеИсключающееИЛИ(M1, ПреобразованиеСлово32(Шифроконтекст, БинарнаяСумма(M2, Шифроконтекст.К[3])));
		M2 = ОперацияПобитовоеИсключающееИЛИ(M2, ПреобразованиеСлово32(Шифроконтекст, БинарнаяСумма(M1, Шифроконтекст.К[4])));
		M1 = ОперацияПобитовоеИсключающееИЛИ(M1, ПреобразованиеСлово32(Шифроконтекст, БинарнаяСумма(M2, Шифроконтекст.К[5])));
		M2 = ОперацияПобитовоеИсключающееИЛИ(M2, ПреобразованиеСлово32(Шифроконтекст, БинарнаяСумма(M1, Шифроконтекст.К[6])));
		M1 = ОперацияПобитовоеИсключающееИЛИ(M1, ПреобразованиеСлово32(Шифроконтекст, БинарнаяСумма(M2, Шифроконтекст.К[7])));
	КонецЦикла;
	
	M2 = ОперацияПобитовоеИсключающееИЛИ(M2, ПреобразованиеСлово32(Шифроконтекст, БинарнаяСумма(M1, Шифроконтекст.К[7])));
	M1 = ОперацияПобитовоеИсключающееИЛИ(M1, ПреобразованиеСлово32(Шифроконтекст, БинарнаяСумма(M2, Шифроконтекст.К[6])));
	M2 = ОперацияПобитовоеИсключающееИЛИ(M2, ПреобразованиеСлово32(Шифроконтекст, БинарнаяСумма(M1, Шифроконтекст.К[5])));
	M1 = ОперацияПобитовоеИсключающееИЛИ(M1, ПреобразованиеСлово32(Шифроконтекст, БинарнаяСумма(M2, Шифроконтекст.К[4])));
	M2 = ОперацияПобитовоеИсключающееИЛИ(M2, ПреобразованиеСлово32(Шифроконтекст, БинарнаяСумма(M1, Шифроконтекст.К[3])));
	M1 = ОперацияПобитовоеИсключающееИЛИ(M1, ПреобразованиеСлово32(Шифроконтекст, БинарнаяСумма(M2, Шифроконтекст.К[2])));
	M2 = ОперацияПобитовоеИсключающееИЛИ(M2, ПреобразованиеСлово32(Шифроконтекст, БинарнаяСумма(M1, Шифроконтекст.К[1])));
	M1 = ОперацияПобитовоеИсключающееИЛИ(M1, ПреобразованиеСлово32(Шифроконтекст, БинарнаяСумма(M2, Шифроконтекст.К[0])));
	
	Байт0 = БлокПриемник[НачальныйИндекс    ];
	Байт1 = БлокПриемник[НачальныйИндекс + 1];
	Байт2 = БлокПриемник[НачальныйИндекс + 2];
	Байт3 = БлокПриемник[НачальныйИндекс + 3];
	Байт4 = БлокПриемник[НачальныйИндекс + 4];
	Байт5 = БлокПриемник[НачальныйИндекс + 5];
	Байт6 = БлокПриемник[НачальныйИндекс + 6];
	Байт7 = БлокПриемник[НачальныйИндекс + 7];
	Для Инд = 0 По 7 Цикл
		Байт0[Инд] = M2[Инд     ];
		Байт1[Инд] = M2[Инд + 8 ];
		Байт2[Инд] = M2[Инд + 16];
		Байт3[Инд] = M2[Инд + 24];
		Байт4[Инд] = M1[Инд     ];
		Байт5[Инд] = M1[Инд + 8 ];
		Байт6[Инд] = M1[Инд + 16];
		Байт7[Инд] = M1[Инд + 24];
	КонецЦикла;
	
КонецПроцедуры

Функция ЧислоВБинарныйМассив(Знач Число, Знач ВсегоРазрядов = 32)
	Результат = Новый Массив;
	Для ПозицияРазряда = 1 По ВсегоРазрядов Цикл
		Результат.Добавить(Булево(Число % 2));
		Число = Цел(Число / 2);
	КонецЦикла;
	Возврат Результат;
КонецФункции

Функция БинарныйМассивВЧисло(Массив)
	Результат = 0;
	Для НомерРазряда = -(Массив.ВГраница()) По 0 Цикл
		Результат = Результат * 2 + Число(Массив[-НомерРазряда]);
	КонецЦикла;
	Возврат Результат;
КонецФункции

Функция ОперацияПобитовоеИсключающееИЛИ(БинарныйМассив1, БинарныйМассив2)
	МаксИндекс = БинарныйМассив1.ВГраница();
	Результат = Новый Массив(МаксИндекс + 1);
	Для Индекс = 0 По МаксИндекс Цикл
		Результат[Индекс] = БинарныйМассив1[Индекс] <> БинарныйМассив2[Индекс];
	КонецЦикла;
	Возврат Результат;
КонецФункции

Функция ОперацияПобитовоеНЕ(БинарныйМассив)
	Результат = Новый Массив;
	Для Каждого Элемент Из БинарныйМассив Цикл
		Результат.Добавить(Не Элемент);
	КонецЦикла;
	Возврат Результат;
КонецФункции

Функция ЦиклическийСдвигВлево(БинарныйМассив, КоличествоРазрядов)
	Размер = БинарныйМассив.Количество();
	Результат = Новый Массив(Размер);
	МаксИндекс = Размер - 1;
	Для Разряд = 0 По МаксИндекс Цикл
		Результат[(Разряд + КоличествоРазрядов) % Размер] = БинарныйМассив[Разряд];
	КонецЦикла;
	Возврат Результат;
КонецФункции

Функция БинарнаяСумма(БинарныйМассив1, БинарныйМассив2, ДобавитьКРазряду = Ложь)
	МаксИндекс = БинарныйМассив1.ВГраница();
	Результат = Новый Массив(МаксИндекс + 1);
	Для Индекс = 0 По МаксИндекс Цикл
		Результат[Индекс] = БинарныйМассив1[Индекс] <> БинарныйМассив2[Индекс] <> ДобавитьКРазряду;
		ДобавитьКРазряду = (0 + БинарныйМассив1[Индекс] + БинарныйМассив2[Индекс] + ДобавитьКРазряду) > 1;
	КонецЦикла;
	Возврат Результат;
КонецФункции

Функция КопияМассива(Массив)
	Результат = Новый Массив;
	Для Каждого Элемент Из Массив Цикл
		Результат.Добавить(Элемент);
	КонецЦикла;
	Возврат Результат;
КонецФункции

Функция СуммироватьБлоки(БлокИсточник, БлокПриемник, КоличествоБайтов)
	
	Перенос = Ложь;
	
	Для Инд = 0 По КоличествоБайтов - 1 Цикл
		БлокПриемник[Инд] = БинарнаяСумма(БлокПриемник[Инд], БлокИсточник[Инд], Перенос);
	КонецЦикла;
	
	Возврат Перенос;
	
КонецФункции

Процедура ИсключающееИЛИДляБлоков(Результат, Блок1, Блок2, Знач КоличествоБайтов)
	
	Для Инд = 0 По КоличествоБайтов - 1 Цикл
		Результат[Инд] = ОперацияПобитовоеИсключающееИЛИ(Блок1[Инд], Блок2[Инд]);
	КонецЦикла;
	
КонецПроцедуры

Процедура РаспределениеБайтовДляБлоков(БлокИсточник, БлокПриемник)
	
	Для Инд1 = 0 По 3 Цикл
		Для Инд2 = 0 По 7 Цикл
			БлокПриемник[Инд1 + 4 * Инд2] = КопияМассива(БлокИсточник[8 * Инд1 + Инд2]);
		КонецЦикла;
	КонецЦикла;

КонецПроцедуры

Процедура ЦиклическоеИсключающееИЛИДляБлоков(БлокИсточник, БлокПриемник)
	
	Буфер = Новый Массив(8);
	
	Для Инд = 0 По 7 Цикл
		Буфер[Инд] = КопияМассива(БлокИсточник[Инд]);
	КонецЦикла;
	Для Инд = 0 По 23 Цикл
		БлокПриемник[Инд] = КопияМассива(БлокИсточник[Инд + 8]);
	КонецЦикла;
	Для Инд = 0 По 7 Цикл
		БлокПриемник[Инд + 24] = ОперацияПобитовоеИсключающееИЛИ(Буфер[Инд], БлокПриемник[Инд]);
	КонецЦикла;
	
КонецПроцедуры

Процедура ПреобразованиеДляБлока(Блок)
	
	Значение1 = Новый Массив(8);
	Значение2 = Новый Массив(8);
	Для Инд = 0 По 7 Цикл
		Значение1[Инд] = Блок[0][Инд] <> Блок[2][Инд] <> Блок[4][Инд] <> Блок[6][Инд] <> Блок[24][Инд] <> Блок[30][Инд];
		Значение2[Инд] = Блок[1][Инд] <> Блок[3][Инд] <> Блок[5][Инд] <> Блок[7][Инд] <> Блок[25][Инд] <> Блок[31][Инд];
	КонецЦикла;
	
	//Сдвинуть массив на 2 позиции влево
	Блок.Удалить(0);
	Блок.Удалить(0);
	Блок.Добавить(Значение1);
	Блок.Добавить(Значение2);
	
КонецПроцедуры

Функция ПреобразованиеСлово32(Шифроконтекст, БитовыйМассив32)
	
	ЗначениеСлова = БинарныйМассивВЧисло(БитовыйМассив32);
	К21 = Шифроконтекст.К21[ЗначениеСлова % 256];
	ЗначениеСлова = Цел(ЗначениеСлова / 256);
	К43 = Шифроконтекст.К43[ЗначениеСлова % 256];
	ЗначениеСлова = Цел(ЗначениеСлова / 256);
	К65 = Шифроконтекст.К65[ЗначениеСлова % 256];
	ЗначениеСлова = Цел(ЗначениеСлова / 256);
	К87 = Шифроконтекст.К87[ЗначениеСлова % 256];
	
	Результат = Новый Массив(32);
	Для Инд = 0 По 7 Цикл
		Результат[Инд     ] = К21[Инд];
		Результат[Инд + 8 ] = К43[Инд];
		Результат[Инд + 16] = К65[Инд];
		Результат[Инд + 24] = К87[Инд];
	КонецЦикла;
	
	Возврат ЦиклическийСдвигВлево(Результат, 11);
	
КонецФункции

Функция ЧислоВШестнадцатеричномПредставлении(Знач Число, Знач КоличествоБайт = 32)
	
	Результат = "";
	СтрокаСимволов = "0123456789ABCDEF";
	Для НомерБайта = 1 По КоличествоБайт Цикл
		ПредставлениеБайта = "";
		Для Счетчик = 1 По 2 Цикл
			ПредставлениеБайта = Сред(СтрокаСимволов, Число % 16 + 1, 1) + ПредставлениеБайта;
			Число = Цел(Число / 16);
		КонецЦикла;
		// порядок байтов: начиная с младшего
		Результат = Результат + ПредставлениеБайта;
	КонецЦикла;
	Возврат Результат;
	
КонецФункции

Функция ПолучитьМассивБайтовИзСтроки(Строка)
	
	Результат = Новый Массив;
	
	Для Позиция = 1 По СтрДлина(Строка) Цикл
		Результат.Добавить(КодСимвола(Сред(Строка, Позиция, 1)));
	КонецЦикла;
	
	Возврат Результат;
	
КонецФункции

Функция ПолучитьМассивБайтовИзСтрокиВШестнадцатеричномПредставлении(Строка)
	
	Результат = Новый Массив;
	
	СтрокаСимволов = "0123456789ABCDEF";
	Число = 0;
	ЭтоМладшийПолубайт = Ложь;
	Для НомерСимвола = 1 По СтрДлина(Строка) Цикл
		ТекЦифра = СтрНайти(СтрокаСимволов, Сред(Строка, НомерСимвола, 1)) - 1;
		Если ТекЦифра >= 0 Тогда
			Если ЭтоМладшийПолубайт Тогда
				Число = Число + ТекЦифра;
				Результат.Добавить(Число);
			Иначе
				Число = 16 * ТекЦифра;
			КонецЕсли;
			ЭтоМладшийПолубайт = НЕ ЭтоМладшийПолубайт;
		КонецЕсли;
	КонецЦикла; 
	
	Возврат Результат;
	
КонецФункции

Функция СтрокаВШестнадцатеричномПредставленииВBase64(СтрокаВШестнадцатеричномПредставлении) Экспорт
	
	МассивБайт = ПолучитьМассивБайтовИзСтрокиВШестнадцатеричномПредставлении(СтрокаВШестнадцатеричномПредставлении);
	Для ИндексБайта = 0 По МассивБайт.ВГраница() Цикл
		МладшийРазряд = МассивБайт[ИндексБайта] % 16;
		СтаршийРазряд = (МассивБайт[ИндексБайта] - МладшийРазряд) / 16;
		МассивБайт[ИндексБайта] = МладшийРазряд * 16 + СтаршийРазряд;
	КонецЦикла;
	ИмяВременногоФайла = ПолучитьИмяВременногоФайла();
	ЗаписатьФайлИзМассива(ИмяВременногоФайла, МассивБайт);
	ДвоичныеДанныеФайла = Новый ДвоичныеДанные(ИмяВременногоФайла);
	СтрокаВBase64 = Base64Строка(ДвоичныеДанныеФайла);
	ОперацииСФайламиЭДКО.УдалитьВременныйФайл(ИмяВременногоФайла);
	
	Возврат СтрокаВBase64;
	
КонецФункции

Функция ПолучитьМассивБайтовИзДвоичныхДанных(ДвоичныеДанные)
	
	СимволыBase64 = "ABCDEFGHIJKLMNOPQRSTUVWXYZabcdefghijklmnopqrstuvwxyz0123456789+/";
	
	Результат = Новый Массив;
	
	СтрокаBase64 = Base64Строка(ДвоичныеДанные);
	ДлинаСтрокиBase64 = СтрДлина(СтрокаBase64);
	
	Буфер24Бита = 0;
	НомерСимволаBase64 = 0;
	
	Для Позиция = 1 По ДлинаСтрокиBase64 Цикл
		СимволBase64 = Сред(СтрокаBase64, Позиция, 1);
		Индекс = СтрНайти(СимволыBase64, СимволBase64) - 1;
		Если Индекс < 0 Тогда
			Если СимволBase64 = "=" Тогда
				Буфер24Бита = Буфер24Бита * Pow(64, 4 - НомерСимволаBase64);
				НомерСимволаBase64 = 4;
			Иначе
				Продолжить;
			КонецЕсли;
		Иначе
			Буфер24Бита = Буфер24Бита * 64 + Индекс;
			НомерСимволаBase64 = НомерСимволаBase64 + 1;
		КонецЕсли;
		Если НомерСимволаBase64 = 4 Тогда
			ЗначениеБайта3 = Буфер24Бита % 256;
			Буфер24Бита = Цел(Буфер24Бита / 256);
			ЗначениеБайта2 = Буфер24Бита % 256;
			Буфер24Бита = Цел(Буфер24Бита / 256);
			ЗначениеБайта1 = Буфер24Бита % 256;
			Буфер24Бита = 0;
			
			Результат.Добавить(ЗначениеБайта1);
			Результат.Добавить(ЗначениеБайта2);
			Результат.Добавить(ЗначениеБайта3);
			
			НомерСимволаBase64 = 0;
		КонецЕсли;
		Если СимволBase64 = "=" Тогда
			Результат.Удалить(Результат.ВГраница());
			Если СтрНайти(Прав(СтрокаBase64, ДлинаСтрокиBase64 - Позиция), "=") > 0 Тогда
				Результат.Удалить(Результат.ВГраница());
			КонецЕсли;
			Прервать;
		КонецЕсли;
	КонецЦикла;
	
	Возврат Результат;
	
КонецФункции

Функция ПолучитьСтрокуBase64ИзМассиваБайтов(МассивБайтов)
	
	СимволыBase64 = "ABCDEFGHIJKLMNOPQRSTUVWXYZabcdefghijklmnopqrstuvwxyz0123456789+/";
	
	РазмерДанных = МассивБайтов.Количество();
	
	Буфер3Байта = Новый Массив(3);
	
	СтрокаBase64 = "";
	
	Инд = 0;
	
	Пока Инд < РазмерДанных Цикл
		
		Буфер3Байта[0] = МассивБайтов[Инд];
		Буфер3Байта[1] = ?(РазмерДанных - Инд > 1, МассивБайтов[Инд + 1], 0);
		Буфер3Байта[2] = ?(РазмерДанных - Инд > 2, МассивБайтов[Инд + 2], 0);
		
		СтрокаBase64 = СтрокаBase64 + Сред(СимволыBase64, Цел(Буфер3Байта[0] / 4) + 1, 1);
		СтрокаBase64 = СтрокаBase64 + Сред(СимволыBase64, Буфер3Байта[0] % 4 * 16 + Цел(Буфер3Байта[1] / 16) + 1, 1);
		СтрокаBase64 = СтрокаBase64 + ?(РазмерДанных - Инд > 1, Сред(СимволыBase64, Буфер3Байта[1] % 16 * 4 + Цел(Буфер3Байта[2] / 64) + 1, 1), "=");
		СтрокаBase64 = СтрокаBase64 + ?(РазмерДанных - Инд > 2, Сред(СимволыBase64, Буфер3Байта[2] % 64 + 1, 1), "=");
		
		Инд = Инд + 3;
		
	КонецЦикла;
	
	Возврат СтрокаBase64;
	
КонецФункции

Функция РазобратьXMLDSig(
		XMLDSigВBase64, 
		ЗаписыватьОшибкиВЖурналРегистрации = Ложь,
		УдалятьЛишниеАтрибуты = Ложь,
		ИспользоватьПреобразованиеККаноническомуXML = Ложь) Экспорт
	
	// прочитать Base64 XMLDSig
	
	ИспользоватьВременноеХранилище = ЭтоАдресВременногоХранилища(XMLDSigВBase64);
	
	ИмяВременногоФайлаXMLDSig = ПолучитьИмяВременногоФайла();
	Если НЕ ИспользоватьВременноеХранилище Тогда
		ДвоичныеДанныеXMLDSig = Base64Значение(XMLDSigВBase64);
	Иначе
		ДвоичныеДанныеXMLDSig = ПолучитьИзВременногоХранилища(XMLDSigВBase64);
	КонецЕсли;
	
	ДвоичныеДанныеXMLDSig.Записать(ИмяВременногоФайлаXMLDSig);
	
	ЧтениеТекстаXMLDSig = Новый ЧтениеТекста(ИмяВременногоФайлаXMLDSig, "utf-8");
	ТекстXMLDSig = ЧтениеТекстаXMLDSig.Прочитать();
	ЧтениеТекстаXMLDSig.Закрыть();
	ОперацииСФайламиЭДКО.УдалитьВременныйФайл(ИмяВременногоФайлаXMLDSig);
	
	// преобразования обхода ошибки канонизации платформы
	
	ПрефиксыПространствИмен = Новый Массив;
	ТекстXML = ТекстXMLDSig;
	Если НЕ ИспользоватьПреобразованиеККаноническомуXML Тогда
		
		// преобразования обхода ошибки канонизации платформы
		Пока ЗначениеЗаполнено(ТекстXML) Цикл
			ПозицияПослеПостфикса = 0;
			ПрефиксПространстваИмен = ТекстПослеПрефикса(ТекстXML, " xmlns:", "=", ПозицияПослеПостфикса);
			Если ЗначениеЗаполнено(ПрефиксПространстваИмен) И ПрефиксыПространствИмен.Найти(ПрефиксПространстваИмен) = Неопределено Тогда
				ПрефиксыПространствИмен.Добавить(ПрефиксПространстваИмен);
			КонецЕсли;
			ТекстXML = Сред(ТекстXML, ПозицияПослеПостфикса);
		КонецЦикла;
		
		ПрефиксОбхода = "EQERLRLQKE";
		ТекстXML = СтрЗаменить(ТекстXMLDSig, " xmlns=""", " xmlns=""" + ПрефиксОбхода);
		Для каждого ПрефиксПространстваИмен Из ПрефиксыПространствИмен Цикл
			ТекстXML = СтрЗаменить(ТекстXML, " xmlns:" + ПрефиксПространстваИмен + "=""", " xmlns:" + ПрефиксПространстваИмен + "=""" + ПрефиксОбхода);
		КонецЦикла;
		
	КонецЕсли;
	
	// создать файл XMLDSig
	
	ИмяВременногоФайлаXMLDSig = ПолучитьИмяВременногоФайла();
	ЗапистТекстаXMLDSig = Новый ЗаписьТекста(ИмяВременногоФайлаXMLDSig, "utf-8");
	ЗапистТекстаXMLDSig.Записать(ТекстXML);
	ЗапистТекстаXMLDSig.Закрыть();
	
	ЧтениеXML = Новый ЧтениеXML;
	ЧтениеXML.ОткрытьФайл(ИмяВременногоФайлаXMLDSig);
	ПостроительDOM = Новый ПостроительDOM;
	Попытка
		ДокументDOM = ПостроительDOM.Прочитать(ЧтениеXML);
	Исключение
		Шаблон = НСтр("ru = 'Ошибка при проверке подписи: неверный формат данных.
					|Подробное описание ошибки:
					|%1'");
		ТекстОшибки = СтрШаблон(Шаблон, ПодробноеПредставлениеОшибки(ИнформацияОбОшибке()));
		Если ЗаписыватьОшибкиВЖурналРегистрации Тогда
			ЗаписьЖурналаРегистрации(
				НСтр("ru = 'Электронный документооборот с контролирующими органами. Проверка подписи XMLDSig'"),
				УровеньЖурналаРегистрации.Ошибка,,,
				ТекстОшибки);
		Иначе
			ОбщегоНазначения.СообщитьПользователю(ТекстОшибки);
		КонецЕсли;
		Возврат Неопределено;
	КонецПопытки;
	ЧтениеXML.Закрыть();
	
	// получить необходимые узлы
	
	УзлыКонверт = ДокументDOM.ПолучитьЭлементыПоИмени("Envelope");
	Если УзлыКонверт.Количество() = 0 Тогда
		ТекстОшибки = НСтр("ru = 'Ошибка при проверке подписи: неверный формат данных.'");
		Если ЗаписыватьОшибкиВЖурналРегистрации Тогда
			ЗаписьЖурналаРегистрации(
				НСтр("ru = 'Электронный документооборот с контролирующими органами. Проверка подписи XMLDSig'"),
				УровеньЖурналаРегистрации.Ошибка,,,
				ТекстОшибки);
		Иначе
			ОбщегоНазначения.СообщитьПользователю(ТекстОшибки);
		КонецЕсли;
		Возврат Неопределено;
	КонецЕсли;
	
	УзелКонверт = УзлыКонверт[0];
	
	УзлыЗаголовок = УзелКонверт.ПолучитьЭлементыПоИмени("Header");
	Если УзлыЗаголовок.Количество() = 0 Тогда
		ТекстОшибки = НСтр("ru = 'Ошибка при проверке подписи: неверный формат данных.'");
		Если ЗаписыватьОшибкиВЖурналРегистрации Тогда
			ЗаписьЖурналаРегистрации(
				НСтр("ru = 'Электронный документооборот с контролирующими органами. Проверка подписи XMLDSig'"),
				УровеньЖурналаРегистрации.Ошибка,,,
				ТекстОшибки);
		Иначе
			ОбщегоНазначения.СообщитьПользователю(ТекстОшибки);
		КонецЕсли;
		Возврат Неопределено;
	КонецЕсли;
	
	УзелЗаголовок = УзлыЗаголовок[0];
	
	УзлыТело = УзелКонверт.ПолучитьЭлементыПоИмени("Body");
	Если УзлыТело.Количество() = 0 Тогда
		ТекстОшибки = НСтр("ru = 'Ошибка при проверке подписи: неверный формат данных.'");
		Если ЗаписыватьОшибкиВЖурналРегистрации Тогда
			ЗаписьЖурналаРегистрации(
				НСтр("ru = 'Электронный документооборот с контролирующими органами. Проверка подписи XMLDSig'"),
				УровеньЖурналаРегистрации.Ошибка,,,
				ТекстОшибки);
		Иначе
			ОбщегоНазначения.СообщитьПользователю(ТекстОшибки);
		КонецЕсли;
		Возврат Неопределено;
	КонецЕсли;
	
	УзелТело = УзлыТело[0];
	
	УзлыБезопасность = УзелЗаголовок.ПолучитьЭлементыПоИмени("Security");
	Если УзлыБезопасность.Количество() = 0 Тогда
		ТекстОшибки = НСтр("ru = 'Ошибка при проверке подписи: неверный формат данных.'");
		Если ЗаписыватьОшибкиВЖурналРегистрации Тогда
			ЗаписьЖурналаРегистрации(
				НСтр("ru = 'Электронный документооборот с контролирующими органами. Проверка подписи XMLDSig'"),
				УровеньЖурналаРегистрации.Ошибка,,,
				ТекстОшибки);
		Иначе
			ОбщегоНазначения.СообщитьПользователю(ТекстОшибки);
		КонецЕсли;
		Возврат Неопределено;
	КонецЕсли;
	
	УзелБезопасность = УзлыБезопасность[0];
	
	УзлыДвоичныйМаркерБезопасности = УзелБезопасность.ПолучитьЭлементыПоИмени("BinarySecurityToken");
	Если УзлыДвоичныйМаркерБезопасности.Количество() = 0 Тогда
		ТекстОшибки = НСтр("ru = 'Ошибка при проверке подписи: неверный формат данных.'");
		Если ЗаписыватьОшибкиВЖурналРегистрации Тогда
			ЗаписьЖурналаРегистрации(
				НСтр("ru = 'Электронный документооборот с контролирующими органами. Проверка подписи XMLDSig'"),
				УровеньЖурналаРегистрации.Ошибка,,,
				ТекстОшибки);
		Иначе
			ОбщегоНазначения.СообщитьПользователю(ТекстОшибки);
		КонецЕсли;
		Возврат Неопределено;
	КонецЕсли;
	
	УзелДвоичныйМаркерБезопасности = УзлыДвоичныйМаркерБезопасности[0];
	
	УзлыПодпись = УзелБезопасность.ПолучитьЭлементыПоИмени("Signature");
	Если УзлыПодпись.Количество() = 0 Тогда
		ТекстОшибки = НСтр("ru = 'Ошибка при проверке подписи: данные не подписаны.'");
		Если ЗаписыватьОшибкиВЖурналРегистрации Тогда
			ЗаписьЖурналаРегистрации(
				НСтр("ru = 'Электронный документооборот с контролирующими органами. Проверка подписи XMLDSig'"),
				УровеньЖурналаРегистрации.Ошибка,,,
				ТекстОшибки);
		Иначе
			ОбщегоНазначения.СообщитьПользователю(ТекстОшибки);
		КонецЕсли;
		Возврат Неопределено;
	КонецЕсли;
	
	УзелПодпись = УзлыПодпись[0];
	
	УзлыПодписьИнфо = УзелПодпись.ПолучитьЭлементыПоИмени("SignedInfo");
	Если УзлыПодписьИнфо.Количество() = 0 Тогда
		ТекстОшибки = НСтр("ru = 'Ошибка при проверке подписи: данные не подписаны.'");
		Если ЗаписыватьОшибкиВЖурналРегистрации Тогда
			ЗаписьЖурналаРегистрации(
				НСтр("ru = 'Электронный документооборот с контролирующими органами. Проверка подписи XMLDSig'"),
				УровеньЖурналаРегистрации.Ошибка,,,
				ТекстОшибки);
		Иначе
			ОбщегоНазначения.СообщитьПользователю(ТекстОшибки);
		КонецЕсли;
		Возврат Неопределено;
	КонецЕсли;
	
	УзелПодписьИнфо = УзлыПодписьИнфо[0];
	
	УзлыСсылка = УзелПодписьИнфо.ПолучитьЭлементыПоИмени("Reference");
	Если УзлыСсылка.Количество() = 0 Тогда
		ТекстОшибки = НСтр("ru = 'Ошибка при проверке подписи: неверный формат данных.'");
		Если ЗаписыватьОшибкиВЖурналРегистрации Тогда
			ЗаписьЖурналаРегистрации(
				НСтр("ru = 'Электронный документооборот с контролирующими органами. Проверка подписи XMLDSig'"),
				УровеньЖурналаРегистрации.Ошибка,,,
				ТекстОшибки);
		Иначе
			ОбщегоНазначения.СообщитьПользователю(ТекстОшибки);
		КонецЕсли;
		Возврат Неопределено;
	КонецЕсли;
	
	УзелСсылка = УзлыСсылка[0];
	
	УзлыРезультатХеширования = УзелСсылка.ПолучитьЭлементыПоИмени("DigestValue");
	Если УзлыРезультатХеширования.Количество() = 0 Тогда
		ТекстОшибки = НСтр("ru = 'Ошибка при проверке подписи: данные не подписаны.'");
		Если ЗаписыватьОшибкиВЖурналРегистрации Тогда
			ЗаписьЖурналаРегистрации(
				НСтр("ru = 'Электронный документооборот с контролирующими органами. Проверка подписи XMLDSig'"),
				УровеньЖурналаРегистрации.Ошибка,,,
				ТекстОшибки);
		Иначе
			ОбщегоНазначения.СообщитьПользователю(ТекстОшибки);
		КонецЕсли;
		Возврат Неопределено;
	КонецЕсли;
	
	УзелРезультатХеширования = УзлыРезультатХеширования[0];
	
	УзлыРезультатПодписания = УзелПодпись.ПолучитьЭлементыПоИмени("SignatureValue");
	Если УзлыРезультатПодписания.Количество() = 0 Тогда
		ТекстОшибки = НСтр("ru = 'Ошибка при проверке подписи: данные не подписаны.'");
		Если ЗаписыватьОшибкиВЖурналРегистрации Тогда
			ЗаписьЖурналаРегистрации(
				НСтр("ru = 'Электронный документооборот с контролирующими органами. Проверка подписи XMLDSig'"),
				УровеньЖурналаРегистрации.Ошибка,,,
				ТекстОшибки);
		Иначе
			ОбщегоНазначения.СообщитьПользователю(ТекстОшибки);
		КонецЕсли;
		Возврат Неопределено;
	КонецЕсли;
	
	УзелРезультатПодписания = УзлыРезультатПодписания[0];
	
	// прочитать значения, убрав переводы строк, перевернуть подпись
	
	СертификатПодписантаВBase64 = СтрЗаменить(УзелДвоичныйМаркерБезопасности.ТекстовоеСодержимое, Символы.ВК, "");
	СертификатПодписантаВBase64 = СтрЗаменить(СертификатПодписантаВBase64, Символы.ПС, "");
	СертификатПодписантаВBase64 = СтрЗаменить(СертификатПодписантаВBase64, " ", "");
	Если ИспользоватьВременноеХранилище Тогда
		ДвоичныеДанныеСертификатаПодписанта = Base64Значение(СертификатПодписантаВBase64);
		СертификатПодписантаВBase64 = ПоместитьВоВременноеХранилище(ДвоичныеДанныеСертификатаПодписанта, Новый УникальныйИдентификатор);
	КонецЕсли;
	
	ХешВBase64 = СтрЗаменить(УзелРезультатХеширования.ТекстовоеСодержимое, Символы.ВК, "");
	ХешВBase64 = СтрЗаменить(ХешВBase64, Символы.ПС, "");
	
	ПодписьВBase64 = УзелРезультатПодписания.ТекстовоеСодержимое;
	ПодписьВBase64 = СтрЗаменить(ПодписьВBase64, " ", "");
	ПодписьВBase64 = СтрЗаменить(ПодписьВBase64, Символы.ПС, "");
	ПодписьВBase64 = СтрЗаменить(ПодписьВBase64, Символы.ВК, "");
	ПодписьВBase64 = ПодписьИзПодписанныхДанныхBase64(УзелРезультатПодписания.ТекстовоеСодержимое);
	Если ИспользоватьВременноеХранилище Тогда
		ДвоичныеДанныеПодписи = Base64Значение(ПодписьВBase64);
		ПодписьВBase64 = ПоместитьВоВременноеХранилище(ДвоичныеДанныеПодписи, Новый УникальныйИдентификатор);
	КонецЕсли;
	
	// добавление недостающих пространств имен и удаление неиспользуемых при исключающей канонизации для обхода ошибки канонизации
	
	Узел = УзелПодписьИнфо;
	Пока Узел.РодительскийУзел <> Неопределено Цикл
		Если Узел.РодительскийУзел.Атрибуты <> Неопределено Тогда
			Для каждого Атрибут Из Узел.РодительскийУзел.Атрибуты Цикл
				Если (Атрибут.Префикс = "xmlns" ИЛИ Атрибут.Имя = "xmlns")
					И УзелПодписьИнфо.Атрибуты.ПолучитьИменованныйЭлемент(Атрибут.URIПространстваИмен, Атрибут.ЛокальноеИмя) = Неопределено Тогда
					АтрибутУзелПодписьИнфо = ДокументDOM.СоздатьАтрибут(Атрибут.URIПространстваИмен, Атрибут.Имя);
					АтрибутУзелПодписьИнфо.Значение = Атрибут.Значение;
					УзелПодписьИнфо.Атрибуты.УстановитьИменованныйЭлемент(АтрибутУзелПодписьИнфо);
				КонецЕсли;
			КонецЦикла;
		КонецЕсли;
		Узел = Узел.РодительскийУзел;
	КонецЦикла;
	
	Узел = УзелТело;
	Пока Узел.РодительскийУзел <> Неопределено Цикл
		Если Узел.РодительскийУзел.Атрибуты <> Неопределено Тогда
			Для каждого Атрибут Из Узел.РодительскийУзел.Атрибуты Цикл
				Если (Атрибут.Префикс = "xmlns" ИЛИ Атрибут.Имя = "xmlns")
					И УзелТело.Атрибуты.ПолучитьИменованныйЭлемент(Атрибут.URIПространстваИмен, Атрибут.ЛокальноеИмя) = Неопределено Тогда
					АтрибутУзелТело = ДокументDOM.СоздатьАтрибут(Атрибут.URIПространстваИмен, Атрибут.Имя);
					АтрибутУзелТело.Значение = Атрибут.Значение;
					УзелТело.Атрибуты.УстановитьИменованныйЭлемент(АтрибутУзелТело);
				КонецЕсли;
			КонецЦикла;
		КонецЕсли;
		Узел = Узел.РодительскийУзел;
	КонецЦикла;
	
	УзлыМетодКанонизации = УзелПодписьИнфо.ПолучитьЭлементыПоИмени("CanonicalizationMethod");
	АлгоритмКанонизации = ?(УзлыМетодКанонизации.Количество() > 0, УзлыМетодКанонизации[0].ПолучитьАтрибут("Algorithm"), "");
	// также встречается и поддерживается алгоритм канонизации "http://www.w3.org/TR/2001/REC-xml-c14n-20010315#WithComments"
	ЭтоИсключающаяКанонизация = (Лев(АлгоритмКанонизации, 39) = "http://www.w3.org/2001/10/xml-exc-c14n#");
	
	УдаляемыеПространстваИменУзлаПодписьИнфо = Новый Массив;
	УдаляемыеПространстваИменУзлаТело = Новый Массив;
	Если ЭтоИсключающаяКанонизация Тогда
		ПолучитьНеиспользуемыеПространстваИмен(УзелПодписьИнфо, УдаляемыеПространстваИменУзлаПодписьИнфо);
		ПолучитьНеиспользуемыеПространстваИмен(УзелТело, УдаляемыеПространстваИменУзлаТело);
	КонецЕсли;
	
	// канонизация
	Если ИспользоватьПреобразованиеККаноническомуXML Тогда
		
		Преобразование = Новый ПреобразованиеККаноническомуXML;
		
		// Определяем тип канонизации.
		ТипКаноническогоXMLДанных = Неопределено;
		Если АлгоритмКанонизации = "http://www.w3.org/2001/10/xml-exc-c14n#WithComments" Тогда
			ТипКаноническогоXMLДанных = ТипКаноническогоXML.ИсключающийКаноническийXMLСКомментариями;
		ИначеЕсли АлгоритмКанонизации = "http://www.w3.org/2001/10/xml-exc-c14n#" Тогда
			ТипКаноническогоXMLДанных = ТипКаноническогоXML.ИсключающийКаноническийXMLСКомментариями;
		ИначеЕсли АлгоритмКанонизации = "http://www.w3.org/TR/2001/REC-xml-c14n-20010315#" Тогда
			ТипКаноническогоXMLДанных = ТипКаноническогоXML.КаноническийXML;
		ИначеЕсли АлгоритмКанонизации = "http://www.w3.org/TR/2001/REC-xml-c14n-20010315#WithComments" Тогда
			ТипКаноническогоXMLДанных = ТипКаноническогоXML.КаноническийXMLСКомментариями;
		КонецЕсли;
		
		Шаблон = "(//. | //@* | //namespace::*)[ancestor-or-self::*[local-name()='%1']]";
		
		// Канонизация тела.
		ФильтрXPath = СтрШаблон(Шаблон, "Body");
		КанонизированныйУзелИнформацияОбОбъекте = Преобразование.КанонизироватьВСтроку(
			ДокументDOM,
			ТипКаноническогоXMLДанных,
			ФильтрXPath);
		
		// Канонизация подписи.
		ФильтрXPath = СтрШаблон(Шаблон, "SignedInfo");
		КанонизированныйУзелПодписываемаяИнформация = Преобразование.КанонизироватьВСтроку(
			ДокументDOM,
			ТипКаноническогоXMLДанных,
			ФильтрXPath);
		
	Иначе
			
		ЗаписьXML = Новый КаноническаяЗаписьXML;
		ЗаписьXML.УстановитьСтроку(ТипКаноническогоXML.КаноническийXML);
		КаноническийDOM = Новый КаноническийDOM;
		КаноническийDOM.Канонизировать(УзелПодписьИнфо, ЗаписьXML);
		КанонизированныйУзелПодписываемаяИнформация = ЗаписьXML.Закрыть();
		
		ЗаписьXML = Новый КаноническаяЗаписьXML;
		ЗаписьXML.УстановитьСтроку(ТипКаноническогоXML.КаноническийXML);
		КаноническийDOM = Новый КаноническийDOM;
		КаноническийDOM.Канонизировать(УзелТело, ЗаписьXML);
		КанонизированныйУзелИнформацияОбОбъекте = ЗаписьXML.Закрыть();
		КанонизированныйУзелИнформацияОбОбъекте = СтрЗаменить(КанонизированныйУзелИнформацияОбОбъекте, "&#xD;" + Символы.ПС, Символы.ПС); // обход ошибки платформы
		
		// обратные преобразования обхода ошибки канонизации
		
		Для ИндексЭлемента = 0 По ПрефиксыПространствИмен.Количество() - 1 Цикл
			ПрефиксПространстваИмен = ПрефиксыПространствИмен[ПрефиксыПространствИмен.Количество() - 1 - ИндексЭлемента];
			КанонизированныйУзелПодписываемаяИнформация = СтрЗаменить(КанонизированныйУзелПодписываемаяИнформация,
				" xmlns:" + ПрефиксПространстваИмен + "=""" + ПрефиксОбхода, " xmlns:" + ПрефиксПространстваИмен + "=""");
			КанонизированныйУзелПодписываемаяИнформация = СтрЗаменить(КанонизированныйУзелПодписываемаяИнформация,
				" :" + ПрефиксПространстваИмен + "=""" + ПрефиксОбхода, " xmlns:" + ПрефиксПространстваИмен + "="""); // обход ошибки платформы
		КонецЦикла;
		КанонизированныйУзелПодписываемаяИнформация = СтрЗаменить(КанонизированныйУзелПодписываемаяИнформация, " xmlns=""" + ПрефиксОбхода, " xmlns=""");
		КанонизированныйУзелПодписываемаяИнформация = СтрЗаменить(КанонизированныйУзелПодписываемаяИнформация, " =""" + ПрефиксОбхода, " xmlns="""); // обход ошибки платформы
		КанонизированныйУзелПодписываемаяИнформация = СтрЗаменить(КанонизированныйУзелПодписываемаяИнформация, " ="""">", ">"); // обход ошибки платформы
		Для каждого УдаляемоеПространствоИменУзлаПодписьИнфо Из УдаляемыеПространстваИменУзлаПодписьИнфо Цикл
			УдаляемаяПодстрока = " " + СтрЗаменить(УдаляемоеПространствоИменУзлаПодписьИнфо, "=""" + ПрефиксОбхода, "=""");
			КанонизированныйУзелПодписываемаяИнформация = СтрЗаменить(КанонизированныйУзелПодписываемаяИнформация, УдаляемаяПодстрока, "");
		КонецЦикла;
		
		Для ИндексЭлемента = 0 По ПрефиксыПространствИмен.Количество() - 1 Цикл
			ПрефиксПространстваИмен = ПрефиксыПространствИмен[ПрефиксыПространствИмен.Количество() - 1 - ИндексЭлемента];
			КанонизированныйУзелИнформацияОбОбъекте = СтрЗаменить(КанонизированныйУзелИнформацияОбОбъекте,
				" xmlns:" + ПрефиксПространстваИмен + "=""" + ПрефиксОбхода, " xmlns:" + ПрефиксПространстваИмен + "=""");
			КанонизированныйУзелИнформацияОбОбъекте = СтрЗаменить(КанонизированныйУзелИнформацияОбОбъекте,
				" :" + ПрефиксПространстваИмен + "=""" + ПрефиксОбхода, " xmlns:" + ПрефиксПространстваИмен + "="""); // обход ошибки платформы
		КонецЦикла;
		КанонизированныйУзелИнформацияОбОбъекте = СтрЗаменить(КанонизированныйУзелИнформацияОбОбъекте, " xmlns=""" + ПрефиксОбхода, " xmlns=""");
		КанонизированныйУзелИнформацияОбОбъекте = СтрЗаменить(КанонизированныйУзелИнформацияОбОбъекте, " =""" + ПрефиксОбхода, " xmlns="""); // обход ошибки платформы
		КанонизированныйУзелИнформацияОбОбъекте = СтрЗаменить(КанонизированныйУзелИнформацияОбОбъекте, " ="""">", ">"); // обход ошибки платформы
		Для каждого УдаляемоеПространствоИменУзлаТело Из УдаляемыеПространстваИменУзлаТело Цикл
			УдаляемаяПодстрока = " " + СтрЗаменить(УдаляемоеПространствоИменУзлаТело, "=""" + ПрефиксОбхода, "=""");
			КанонизированныйУзелИнформацияОбОбъекте = СтрЗаменить(КанонизированныйУзелИнформацияОбОбъекте, УдаляемаяПодстрока, "");
		КонецЦикла;
		
		Если УдалятьЛишниеАтрибуты Тогда
			КанонизированныйУзелИнформацияОбОбъекте = СтрЗаменить(КанонизированныйУзелИнформацияОбОбъекте, " xmlns=""""", "");
		КонецЕсли;
		
	КонецЕсли;
	
	ОперацииСФайламиЭДКО.УдалитьВременныйФайл(ИмяВременногоФайлаXMLDSig);
	
	// канонизированная информация в Base64
	
	ИмяВременногоФайлаПодписываемойИнформации = ПолучитьИмяВременногоФайла();
	ЗапистТекстаПодписываемойИнформации = Новый ЗаписьТекста(ИмяВременногоФайлаПодписываемойИнформации, "utf-8");
	ЗапистТекстаПодписываемойИнформации.Записать(КанонизированныйУзелПодписываемаяИнформация);
	ЗапистТекстаПодписываемойИнформации.Закрыть();
	
	ДвоичныеДанныеПродписываемойИнформации = Новый ДвоичныеДанные(ИмяВременногоФайлаПодписываемойИнформации);
	КанонизированныйУзелПодписываемаяИнформацияXMLВBase64 = Base64Строка(ДвоичныеДанныеПродписываемойИнформации);
	КанонизированныйУзелПодписываемаяИнформацияXMLВBase64 = Сред(КанонизированныйУзелПодписываемаяИнформацияXMLВBase64, 5); // удалим сигнатуру BOM из файла в кодировке "UTF-8"
	
	ОперацииСФайламиЭДКО.УдалитьВременныйФайл(ИмяВременногоФайлаПодписываемойИнформации);
	
	Если ИспользоватьВременноеХранилище Тогда
		ДвоичныеДанныеПродписываемойИнформации = Base64Значение(КанонизированныйУзелПодписываемаяИнформацияXMLВBase64);
		КанонизированныйУзелПодписываемаяИнформацияXMLВBase64 = ПоместитьВоВременноеХранилище(ДвоичныеДанныеПродписываемойИнформации, Новый УникальныйИдентификатор);
	КонецЕсли;
	
	ИмяВременногоФайлаОбъекта = ПолучитьИмяВременногоФайла();
	ЗапистТекстаОбъекта = Новый ЗаписьТекста(ИмяВременногоФайлаОбъекта, "utf-8", Символы.ПС, , Символы.ПС);
	ЗапистТекстаОбъекта.Записать(КанонизированныйУзелИнформацияОбОбъекте);
	ЗапистТекстаОбъекта.Закрыть();
	
	ДвоичныеДанныеОбъекта = Новый ДвоичныеДанные(ИмяВременногоФайлаОбъекта);
	КанонизированныйУзелИнформацияОбОбъектеXMLВBase64 = Base64Строка(ДвоичныеДанныеОбъекта);
	КанонизированныйУзелИнформацияОбОбъектеXMLВBase64 = Сред(КанонизированныйУзелИнформацияОбОбъектеXMLВBase64, 5); // удалим сигнатуру BOM из файла в кодировке "UTF-8"
	
	ОперацииСФайламиЭДКО.УдалитьВременныйФайл(ИмяВременногоФайлаОбъекта);
	
	Если ИспользоватьВременноеХранилище Тогда
		ДвоичныеДанныеОбъекта = Base64Значение(КанонизированныйУзелИнформацияОбОбъектеXMLВBase64);
		КанонизированныйУзелИнформацияОбОбъектеXMLВBase64 = ПоместитьВоВременноеХранилище(ДвоичныеДанныеОбъекта, Новый УникальныйИдентификатор);
	КонецЕсли;
	
	Результат = Новый Структура;
	Результат.Вставить("СертификатПодписантаВBase64", 							СертификатПодписантаВBase64);
	Результат.Вставить("ХешВBase64", 											ХешВBase64);
	Результат.Вставить("ПодписьВBase64", 										ПодписьВBase64);
	Результат.Вставить("КанонизированныйУзелПодписываемаяИнформацияXMLВBase64", КанонизированныйУзелПодписываемаяИнформацияXMLВBase64);
	Результат.Вставить("КанонизированныйУзелИнформацияОбОбъектеXMLВBase64", 	КанонизированныйУзелИнформацияОбОбъектеXMLВBase64);
	
	Возврат Результат;
	
КонецФункции

Процедура ПолучитьНеиспользуемыеПространстваИмен(Узел, НеиспользуемыеПространстваИмен)
	
	Если Узел.Атрибуты <> Неопределено Тогда
		Для каждого Атрибут Из Узел.Атрибуты Цикл
			Если Атрибут.Префикс = "xmlns" И ЗначениеЗаполнено(Атрибут.ЛокальноеИмя)
				И НЕ ПрефиксПространстваИменИспользуется(Атрибут.ЛокальноеИмя, Узел) Тогда
				
				НеиспользуемыеПространстваИмен.Добавить(Атрибут.Имя + "=""" + Атрибут.Значение + """");
				
			КонецЕсли;
		КонецЦикла;
	КонецЕсли;
	
	Для каждого ДочернийУзел Из Узел.ДочерниеУзлы Цикл
		ПолучитьНеиспользуемыеПространстваИмен(ДочернийУзел, НеиспользуемыеПространстваИмен);
	КонецЦикла;
	
КонецПроцедуры

Функция ПрефиксПространстваИменИспользуется(Префикс, Узел)
	
	Если Узел.Префикс = Префикс Тогда
		Возврат Истина;
	КонецЕсли;
	
	Если Узел.Атрибуты <> Неопределено Тогда
		Для каждого Атрибут Из Узел.Атрибуты Цикл
			Если Атрибут.Префикс = Префикс Тогда
				Возврат Истина;
			КонецЕсли;
		КонецЦикла;
	КонецЕсли;
	
	Для каждого ДочернийУзел Из Узел.ДочерниеУзлы Цикл
		Если ПрефиксПространстваИменИспользуется(Префикс, ДочернийУзел) Тогда
			Возврат Истина;
		КонецЕсли;
	КонецЦикла;
	
	Возврат Ложь;
	
КонецФункции

#КонецОбласти

Функция СвойстваОрганизацииДляОбмена(
		ОрганизацияСсылка,
		ПолучитьРегистрационныйНомерФСС = Истина,
		ПолучитьОГРН = Ложь) Экспорт
	
	Результат = Новый Структура;
	
	РегистрационныйНомерФСС = Неопределено;
	Если ПолучитьРегистрационныйНомерФСС
		И Метаданные.Справочники.Организации.Реквизиты.Найти("ДополнительныйКодФСС") <> Неопределено Тогда
		
		РегистрационныйНомерФСС = ОбщегоНазначения.ЗначениеРеквизитаОбъекта(ОрганизацияСсылка, "ДополнительныйКодФСС");
	КонецЕсли;
	
	ДанныеОрганизации = Неопределено;
	Если ПолучитьРегистрационныйНомерФСС И НЕ ЗначениеЗаполнено(РегистрационныйНомерФСС) ИЛИ ПолучитьОГРН Тогда
		СписокПоказателей = ?(ПолучитьОГРН, "ОГРН", "")
			+ ?(ПолучитьОГРН И ПолучитьРегистрационныйНомерФСС И НЕ ЗначениеЗаполнено(РегистрационныйНомерФСС), ", ", "")
			+ ?(ПолучитьРегистрационныйНомерФСС И НЕ ЗначениеЗаполнено(РегистрационныйНомерФСС),
			"РегистрационныйНомерФСС, РегистрационныйНомерФССДобрСтрах", "");
		ДанныеОрганизации = РегламентированнаяОтчетностьВызовСервера.ПолучитьСведенияОбОрганизации(
			ОрганизацияСсылка,,
			СписокПоказателей);
		
		Если ПолучитьРегистрационныйНомерФСС И НЕ ЗначениеЗаполнено(РегистрационныйНомерФСС) Тогда
			РегистрационныйНомерФСС = ?(ЭлектронныйДокументооборотСКонтролирующимиОрганамиВызовСервера.ЭтоЮрЛицо(
				ОрганизацияСсылка) ИЛИ РегламентированнаяОтчетность.ИПИспользуетТрудНаемныхРаботников(ОрганизацияСсылка),
				ДанныеОрганизации.РегистрационныйНомерФСС,
				ДанныеОрганизации.РегистрационныйНомерФССДобрСтрах);
		КонецЕсли;
	КонецЕсли;
	
	Если ПолучитьРегистрационныйНомерФСС Тогда
		Результат.Вставить("РегистрационныйНомерФСС", РегистрационныйНомерФСС);
	КонецЕсли;
	
	Если ПолучитьОГРН Тогда
		Результат.Вставить("ОГРН", ДанныеОрганизации.ОГРН);
	КонецЕсли;
	
	Возврат Результат;
	
КонецФункции

#Область ПодписьSOAP

Функция ЗаписиЭЛНДляПодписиSOAP64(
		Знач ВыгрузкаЭЛН,
		Знач ДобавляемыеАтрибутыЗаписи,
		МассивНомеровЛистковНетрудоспособности = Неопределено) Экспорт
	
	ВременныйФайлВыгрузки = ПолучитьИмяВременногоФайла();
	ЗаписьТекста = Новый ЗаписьТекста(ВременныйФайлВыгрузки, "utf-8");
	ЗаписьТекста.Записать(ВыгрузкаЭЛН);
	ЗаписьТекста.Закрыть();
	
	ЧтениеXML = Новый ЧтениеXML;
	ЧтениеXML.ОткрытьФайл(ВременныйФайлВыгрузки);
	ПостроительDOM = Новый ПостроительDOM;
	ДокументDOM = ПостроительDOM.Прочитать(ЧтениеXML);
	ЧтениеXML.Закрыть();
	
	ОперацииСФайламиЭДКО.УдалитьВременныйФайл(ВременныйФайлВыгрузки);
	
	УзелROWSET = Неопределено;
	Для каждого УзелROWSETВДочернихУзлах Из ДокументDOM.ДочерниеУзлы Цикл
		Если УзелROWSETВДочернихУзлах.ТипУзла = ТипУзлаDOM.Комментарий Тогда
			Продолжить;
		КонецЕсли;
		УзелROWSET = УзелROWSETВДочернихУзлах;
		Прервать;
	КонецЦикла;
	
	Если УзелROWSET = Неопределено Тогда
		Возврат Неопределено;
	КонецЕсли;
	
	УзелROW = Неопределено;
	Для каждого УзелROWВДочернихУзлах Из УзелROWSET.ДочерниеУзлы Цикл
		Если УзелROWВДочернихУзлах.ТипУзла = ТипУзлаDOM.Комментарий Тогда
			Продолжить;
		КонецЕсли;
		УзелROW = УзелROWВДочернихУзлах;
		Прервать;
	КонецЦикла;
	
	Если УзелROW = Неопределено Тогда
		Возврат Неопределено;
	КонецЕсли;
	
	ПараметрыФормата = ПараметрыФорматаЭЛН();
	ЭтоВерсия1X = Лев(ПараметрыФормата.Версия, 2) = "1.";
	
	ИмяУзлаЗаписей = ?(ЭтоВерсия1X, "ROW", "row");
	ИмяУзлаКодаЛН = ?(ЭтоВерсия1X, "LN_CODE", "lnCode");
	
	Результат = Новый Массив;
	МассивНомеровЛистковНетрудоспособности = Новый Массив;
	
	Для каждого УзелROW Из УзелROWSET.ДочерниеУзлы Цикл
		Если УзелROW.ТипУзла = ТипУзлаDOM.Комментарий Тогда
			Продолжить;
		КонецЕсли;
		
		НомерЛисткаНетрудоспособностиСтрокой = УзелROW.ПолучитьЭлементыПоИмени(ИмяУзлаКодаЛН)[0].ТекстовоеСодержимое;
		НомерЗаписиСтрокой = "ELN_" + НомерЛисткаНетрудоспособностиСтрокой;
		МассивНомеровЛистковНетрудоспособности.Добавить(НомерЛисткаНетрудоспособностиСтрокой);
		
		ЗаписьXML = Новый ЗаписьXML;
		ЗаписьXML.УстановитьСтроку();
		ЗаписьDOM = Новый ЗаписьDOM;
		ЗаписьDOM.Записать(УзелROW, ЗаписьXML);
		СтрокаXML = ЗаписьXML.Закрыть();
		
		СтрокаXML = СтрЗаменить(СтрокаXML, "<", "<eln:");
		СтрокаXML = СтрЗаменить(СтрокаXML, "<eln:/", "</eln:");
		СтрокаXML = СтрЗаменить(СтрокаXML, "<eln:!--", "<!--");
		СтрокаXML = СтрЗаменить(СтрокаXML, "<eln:" + ИмяУзлаЗаписей, "<eln:" + ИмяУзлаЗаписей
			+ ?(ДобавляемыеАтрибутыЗаписи <> "", " " + ДобавляемыеАтрибутыЗаписи, "")
			+ " wsu:Id=""" + НомерЗаписиСтрокой + """");
		
		ВременныйФайлЗаписи = ПолучитьИмяВременногоФайла();
		ЗаписьТекста = Новый ЗаписьТекста(ВременныйФайлЗаписи, "utf-8");
		ЗаписьТекста.Записать(СтрокаXML);
		ЗаписьТекста.Закрыть();
		
		ДвоичныеДанныеЗаписи = Новый ДвоичныеДанные(ВременныйФайлЗаписи);
		СтрокаXML64 = Base64Строка(ДвоичныеДанныеЗаписи);
		
		ОперацииСФайламиЭДКО.УдалитьВременныйФайл(ВременныйФайлЗаписи);
		
		Результат.Добавить(СтрокаXML64);
	КонецЦикла;
	
	Возврат Результат;
	
КонецФункции

Функция АдресSOAPЗагрузкиЭЛНВФССПоМассивуПодписей(
		Знач РегистрационныйНомерФСС,
		Знач ВыгрузкаЭЛН,
		Знач МассивПодписейЭЛН64,
		Знач МассивНомеровЛистковНетрудоспособности,
		Знач СертификатПолучателя64,
		Знач ДобавляемыеАтрибутыЗаписи,
		Знач ДобавляемыеАтрибутыПодписи,
		Знач ВозвращатьТекстВBase64 = Ложь) Экспорт
	
	ПараметрыФормата = ПараметрыФорматаЭЛН();
	
	ТекстУзловПодписи = "";
	ИспользуетсяВременноеХранилище = Ложь;
	НомерЗаписи = 1;
	Для каждого ПодписьЭЛН64 Из МассивПодписейЭЛН64 Цикл
		
		ВременныйФайлПодписи = ПолучитьИмяВременногоФайла();
		Если ЭтоАдресВременногоХранилища(ПодписьЭЛН64) Тогда
			ИспользуетсяВременноеХранилище = Истина;
			ДвоичныеДанныеПодписи = ПолучитьИзВременногоХранилища(ПодписьЭЛН64);
		Иначе
			ДвоичныеДанныеПодписи = Base64Значение(ПодписьЭЛН64);
		КонецЕсли;
		ДвоичныеДанныеПодписи.Записать(ВременныйФайлПодписи);
		
		ЧтениеXML = Новый ЧтениеXML;
		ЧтениеXML.ОткрытьФайл(ВременныйФайлПодписи);
		ПостроительDOM = Новый ПостроительDOM;
		ДокументDOM = ПостроительDOM.Прочитать(ЧтениеXML);
		ЧтениеXML.Закрыть();
		
		ОперацииСФайламиЭДКО.УдалитьВременныйФайл(ВременныйФайлПодписи);
		
		УзелПодписываемаяИнформация = ДокументDOM.ПолучитьЭлементыПоИмени("SignedInfo")[0];
		
		ЗаписьXML = Новый ЗаписьXML;
		ЗаписьXML.УстановитьСтроку();
		ЗаписьDOM = Новый ЗаписьDOM;
		ЗаписьDOM.Записать(УзелПодписываемаяИнформация, ЗаписьXML);
		ПодписываемаяИнформацияXML = ЗаписьXML.Закрыть();
		Если ДобавляемыеАтрибутыПодписи <> "" Тогда
			ПодписываемаяИнформацияXML = СтрЗаменить(ПодписываемаяИнформацияXML, " " + ДобавляемыеАтрибутыПодписи, "");
		КонецЕсли;
		
		СодержимоеПодписи = ДокументDOM.ПолучитьЭлементыПоИмени("SignatureValue")[0].ТекстовоеСодержимое;
		СодержимоеПодписи = СтрЗаменить(СодержимоеПодписи, Символы.ПС, "");
		СодержимоеПодписи = СтрЗаменить(СодержимоеПодписи, Символы.ВК, "");
		
		КоличествоПодписантов = ?(ПараметрыФормата.Версия = "1.0", 1, 3);
		
		Для НомерПодписанта = 1 По КоличествоПодписантов Цикл
			Если НомерПодписанта = 1 Тогда
				ОбозначениеПодписанта = "insurer";
			ИначеЕсли НомерПодписанта = 2 Тогда
				ОбозначениеПодписанта = "chief";
			ИначеЕсли НомерПодписанта = 3 Тогда
				ОбозначениеПодписанта = "accountant";
			КонецЕсли;
			
			ТекстУзловПодписи = ТекстУзловПодписи + СтрШаблон("
				|		<wsse:Security soapenv:actor=""http://eln.fss.ru/actor/%4/%5/%6"">
				|			<wsse:BinarySecurityToken EncodingType=""http://docs.oasis-open.org/wss/2004/01/oasis-200401-wss-soap-message-security-1.0#Base64Binary"" ValueType=""http://docs.oasis-open.org/wss/2004/01/oasis-200401-wss-x509-token-profile-1.0#X509v3"" wsu:Id=""http://eln.fss.ru/actor/%4/%5/%6"">%7
				|			</wsse:BinarySecurityToken>
				|			<Signature %1>
				|%2
				|				<SignatureValue>%3</SignatureValue>
				|				<KeyInfo>
				|					<wsse:SecurityTokenReference>
				|						<wsse:Reference URI=""#http://eln.fss.ru/actor/%4/%5/%6"" ValueType=""http://docs.oasis-open.org/wss/2004/01/oasis-200401-wss-x509-token-profile-1.0#X509v3""/>
				|					</wsse:SecurityTokenReference>
				|				</KeyInfo>
				|			</Signature>
				|		</wsse:Security>",
				ДобавляемыеАтрибутыПодписи,
				ПодписываемаяИнформацияXML,
				СодержимоеПодписи,
				ОбозначениеПодписанта,
				РегистрационныйНомерФСС,
				МассивНомеровЛистковНетрудоспособности[НомерЗаписи - 1],
				СертификатПолучателя64);
		КонецЦикла;
		
		НомерЗаписи = НомерЗаписи + 1;
		
	КонецЦикла;
	
	ЭтоВерсия1X = Лев(ПараметрыФормата.Версия, 2) = "1.";
	
	Если НЕ ЭтоВерсия1X Тогда
		ТекстУзлаСертификата = "
			|		<X509Certificate xmlns=""http://www.w3.org/2000/09/xmldsig#"">%1</X509Certificate>";
		ТекстУзлаСертификата = СтрШаблон(ТекстУзлаСертификата, СертификатПолучателя64);
	КонецЕсли;
	
	ВременныйФайлВыгрузки = ПолучитьИмяВременногоФайла();
	ЗаписьТекста = Новый ЗаписьТекста(ВременныйФайлВыгрузки, "utf-8");
	ЗаписьТекста.Записать(ВыгрузкаЭЛН);
	ЗаписьТекста.Закрыть();
	
	ЧтениеXML = Новый ЧтениеXML;
	ЧтениеXML.ОткрытьФайл(ВременныйФайлВыгрузки);
	ПостроительDOM = Новый ПостроительDOM;
	ДокументDOM = ПостроительDOM.Прочитать(ЧтениеXML);
	ЧтениеXML.Закрыть();
	
	ОперацииСФайламиЭДКО.УдалитьВременныйФайл(ВременныйФайлВыгрузки);
	
	ЗаписьXML = Новый ЗаписьXML;
	ЗаписьXML.УстановитьСтроку();
	ЗаписьDOM = Новый ЗаписьDOM;
	ЗаписьDOM.Записать(ДокументDOM, ЗаписьXML);
	СодержимоеВыгрузкиXML = ЗаписьXML.Закрыть();
	
	ИмяУзлаКорняЗаписей = ?(ЭтоВерсия1X, "ROWSET", "rowset");
	ИмяУзлаЗаписей = ?(ЭтоВерсия1X, "ROW", "row");
	ПрефиксАтрибутовНачалаЗаписи = ?(ЭтоВерсия1X, "eln", "v011");
	
	ПозицияПослеУзлаНачалаЗаписей = 0;
	ТекстУзлаНачалаЗаписей = ТекстПослеПрефикса(СодержимоеВыгрузкиXML, "<" + ИмяУзлаКорняЗаписей, ">",
		ПозицияПослеУзлаНачалаЗаписей);
	Если ТекстУзлаНачалаЗаписей <> Неопределено Тогда
		ТекстПослеУзла = Сред(СодержимоеВыгрузкиXML, ПозицияПослеУзлаНачалаЗаписей);
		
		ТекстУзлаНачалаЗаписей = СтрЗаменить(ТекстУзлаНачалаЗаписей,
			" xmlns:xsi=""http://www.w3.org/2001/XMLSchema-instance""", "");
		ТекстУзлаНачалаЗаписей = СтрЗаменить(ТекстУзлаНачалаЗаписей,
			" version=", " " + ПрефиксАтрибутовНачалаЗаписи + ":version=");
		ТекстУзлаНачалаЗаписей = СтрЗаменить(ТекстУзлаНачалаЗаписей,
			" software=", " " + ПрефиксАтрибутовНачалаЗаписи + ":software=");
		ТекстУзлаНачалаЗаписей = СтрЗаменить(ТекстУзлаНачалаЗаписей,
			" version_software=", " " + ПрефиксАтрибутовНачалаЗаписи + ":version_software=");
		ТекстУзлаНачалаЗаписей = СтрЗаменить(ТекстУзлаНачалаЗаписей,
			" author=", " " + ПрефиксАтрибутовНачалаЗаписи + ":author=");
		ТекстУзлаНачалаЗаписей = СтрЗаменить(ТекстУзлаНачалаЗаписей,
			" phone=", " " + ПрефиксАтрибутовНачалаЗаписи + ":phone=");
		ТекстУзлаНачалаЗаписей = СтрЗаменить(ТекстУзлаНачалаЗаписей,
			" email=", " " + ПрефиксАтрибутовНачалаЗаписи + ":email=");
		
		СодержимоеВыгрузкиXML = "<" + ИмяУзлаКорняЗаписей + ТекстУзлаНачалаЗаписей + ">";
		НомерЗаписи = 1;
		ПозицияПослеУзлаЗаписи = 0;
		
		Пока Истина Цикл
			ПозицияНачалаУзла = 0;
			ТекстУзлаЗаписи = ТекстПослеПрефикса(ТекстПослеУзла, "<" + ИмяУзлаЗаписей, "</" + ИмяУзлаЗаписей + ">",
				ПозицияПослеУзлаЗаписи,, ПозицияНачалаУзла);
			Если ТекстУзлаЗаписи = Неопределено Тогда
				СодержимоеВыгрузкиXML = СодержимоеВыгрузкиXML + ТекстПослеУзла;
				Прервать;
				
			Иначе
				НомерЗаписиСтрокой = "ELN_" + МассивНомеровЛистковНетрудоспособности[НомерЗаписи - 1];
				
				ТекстУзлаЗаписи = "<" + ИмяУзлаЗаписей + " wsu:Id=""" + НомерЗаписиСтрокой + """" + ТекстУзлаЗаписи
					+ "</" + ИмяУзлаЗаписей + ">";
				СодержимоеВыгрузкиXML = СодержимоеВыгрузкиXML + Лев(ТекстПослеУзла, ПозицияНачалаУзла - 1) + ТекстУзлаЗаписи;
				
				ТекстПослеУзла = Сред(ТекстПослеУзла, ПозицияПослеУзлаЗаписи);
				НомерЗаписи = НомерЗаписи + 1;
			КонецЕсли;
		КонецЦикла;
	КонецЕсли;
	
	СодержимоеВыгрузкиXML = СтрЗаменить(СодержимоеВыгрузкиXML, "<", "<eln:");
	СодержимоеВыгрузкиXML = СтрЗаменить(СодержимоеВыгрузкиXML, "<eln:/", "</eln:");
	
	ТекстSOAP = СтрШаблон(
		"<soapenv:Envelope %1>
		|	<soapenv:Header>%3%6
		|	</soapenv:Header>
		|	<soapenv:Body>
		|		<eln:%7>
		|			<eln:request>
		|				<eln:regNum>%4</eln:regNum>
		|				<eln:pXmlFile>
		|%5
		|				</eln:pXmlFile>
		|			</eln:request>
		|		</eln:%7>
		|	</soapenv:Body>
		|</soapenv:Envelope>",
		ДобавляемыеАтрибутыЗаписи,
		СертификатПолучателя64,
		ТекстУзловПодписи,
		РегистрационныйНомерФСС,
		СодержимоеВыгрузкиXML,
		ТекстУзлаСертификата,
		?(ЭтоВерсия1X, "prParseReestrFile", "prParseReestrFileRequest"));
	
	ТекстSOAP = СтрЗаменить(ТекстSOAP, Символы.Таб, "");
	ТекстSOAP = СтрЗаменить(ТекстSOAP, Символы.ВК, "");
	ТекстSOAP = СтрЗаменить(ТекстSOAP, Символы.ПС, "");
	
	ВременныйФайлТекстаSOAP = ПолучитьИмяВременногоФайла();
	ЗаписываемыйФайлТекстаSOAP = Новый ЗаписьТекста(ВременныйФайлТекстаSOAP, "utf-8");
	ЗаписываемыйФайлТекстаSOAP.Записать(ТекстSOAP);
	ЗаписываемыйФайлТекстаSOAP.Закрыть();
	
	ДвоичныеДанныеТекстаSOAP = Новый ДвоичныеДанные(ВременныйФайлТекстаSOAP);
	ТекстSOAP64 = Base64Строка(ДвоичныеДанныеТекстаSOAP);
	Результат = Сред(ТекстSOAP64, 5); // удалим сигнатуру BOM из файла в кодировке "UTF-8"
	
	Если НЕ ВозвращатьТекстВBase64 ИЛИ ИспользуетсяВременноеХранилище Тогда
		ДвоичныеДанныеТекстаSOAP = Base64Значение(Результат);
		Результат = ПоместитьВоВременноеХранилище(ДвоичныеДанныеТекстаSOAP, Новый УникальныйИдентификатор);
	КонецЕсли;
	
	ОперацииСФайламиЭДКО.УдалитьВременныйФайл(ВременныйФайлТекстаSOAP);
	
	Возврат Результат;
	
КонецФункции

Функция ЗапросОперацииСЭЛНДляПодписиSOAP64(
		Знач ВыгрузкаЗапросаОперацииСЭЛН,
		Знач ДобавляемыеАтрибутыЗапроса,
		Знач РегистрационныйНомерФСС) Экспорт
	
	ТекстXMLЗапроса = СтрЗаменить(ВыгрузкаЗапросаОперацииСЭЛН, "<", "<fil:");
	ТекстXMLЗапроса = СтрЗаменить(ТекстXMLЗапроса, "<fil:/", "</fil:");
	
	ТекстXMLЗапроса = СтрШаблон(
		"<soapenv:Body %1 wsu:Id=""REGNO_%2"">%3</soapenv:Body>",
		ДобавляемыеАтрибутыЗапроса,
		РегистрационныйНомерФСС,
		ТекстXMLЗапроса);
	
	ВременныйФайлЗапроса = ПолучитьИмяВременногоФайла();
	ЗаписьТекстаЗапроса = Новый ЗаписьТекста(ВременныйФайлЗапроса, "utf-8");
	ЗаписьТекстаЗапроса.Записать(ТекстXMLЗапроса);
	ЗаписьТекстаЗапроса.Закрыть();
	
	ДвоичныеДанныеЗапроса = Новый ДвоичныеДанные(ВременныйФайлЗапроса);
	ТекстXMLЗапроса64 = Base64Строка(ДвоичныеДанныеЗапроса);
	
	ОперацииСФайламиЭДКО.УдалитьВременныйФайл(ВременныйФайлЗапроса);
	
	Возврат ТекстXMLЗапроса64;
	
КонецФункции

Функция АдресSOAPЗапросОперацииСЭДОФССПоПодписи(
		Знач РегистрационныйНомерФСС,
		Знач ВыгрузкаЗапроса,
		Знач ПодписьЗапросаЭЛН64,
		Знач СертификатПолучателя64,
		Знач ДобавляемыеАтрибутыЗапроса,
		Знач ДобавляемыеАтрибутыПодписи,
		Знач ВозвращатьТекстВBase64 = Ложь) Экспорт
	
	ВременныйФайлПодписи = ПолучитьИмяВременногоФайла();
	ИспользуетсяВременноеХранилище = ЭтоАдресВременногоХранилища(ПодписьЗапросаЭЛН64);
	Если ИспользуетсяВременноеХранилище Тогда
		ДвоичныеДанныеПодписи = ПолучитьИзВременногоХранилища(ПодписьЗапросаЭЛН64);
	Иначе
		ДвоичныеДанныеПодписи = Base64Значение(ПодписьЗапросаЭЛН64);
	КонецЕсли;
	ДвоичныеДанныеПодписи.Записать(ВременныйФайлПодписи);
	
	ЧтениеXML = Новый ЧтениеXML;
	ЧтениеXML.ОткрытьФайл(ВременныйФайлПодписи);
	ПостроительDOM = Новый ПостроительDOM;
	ДокументDOM = ПостроительDOM.Прочитать(ЧтениеXML);
	ЧтениеXML.Закрыть();
	
	ОперацииСФайламиЭДКО.УдалитьВременныйФайл(ВременныйФайлПодписи);
	
	УзелПодписываемаяИнформация = ДокументDOM.ПолучитьЭлементыПоИмени("SignedInfo")[0];
	
	ЗаписьXML = Новый ЗаписьXML;
	ЗаписьXML.Отступ = Ложь;
	ЗаписьXML.УстановитьСтроку();
	ЗаписьDOM = Новый ЗаписьDOM;
	ЗаписьDOM.Записать(УзелПодписываемаяИнформация, ЗаписьXML);
	
	ПодписываемаяИнформацияXML = ЗаписьXML.Закрыть();
	Если ДобавляемыеАтрибутыПодписи <> "" Тогда
		ПодписываемаяИнформацияXML = СтрЗаменить(ПодписываемаяИнформацияXML, " " + ДобавляемыеАтрибутыПодписи, "");
	КонецЕсли;
	
	СодержимоеПодписи = ДокументDOM.ПолучитьЭлементыПоИмени("SignatureValue")[0].ТекстовоеСодержимое;
	СодержимоеПодписи = СтрЗаменить(СодержимоеПодписи, Символы.ПС, "");
	СодержимоеПодписи = СтрЗаменить(СодержимоеПодписи, Символы.ВК, "");
	
	ТекстУзлаПодписи = СтрШаблон("
		|			<Signature %1>
		|%2
		|				<SignatureValue>%3</SignatureValue>
		|				<KeyInfo>
		|					<wsse:SecurityTokenReference>
		|						<wsse:Reference URI=""#http://eln.fss.ru/actor/insurer/%4"" ValueType=""http://docs.oasis-open.org/wss/2004/01/oasis-200401-wss-x509-token-profile-1.0#X509v3""/>
		|					</wsse:SecurityTokenReference>
		|				</KeyInfo>
		|			</Signature>",
		ДобавляемыеАтрибутыПодписи,
		ПодписываемаяИнформацияXML,
		СодержимоеПодписи,
		РегистрационныйНомерФСС);
		
	ТекстУзлаПодписи = СтрЗаменить(ТекстУзлаПодписи, Символы.ПС, "");
	
	// Добавляем узел X509Certificate для СЭДО.
	ТекстУзлаСертификата = "<X509Certificate xmlns=""http://www.w3.org/2000/09/xmldsig#"">%1</X509Certificate>";
	ТекстУзлаСертификата = СтрШаблон(ТекстУзлаСертификата, СертификатПолучателя64);
	
	Чтение = Новый ЧтениеXML;
	Чтение.УстановитьСтроку(ВыгрузкаЗапроса);
	ПостроительDOM = Новый ПостроительDOM();
	ДокументДОМ = ПостроительDOM.Прочитать(Чтение);
	Чтение.Закрыть();
	
	УзелТелоЗапроса = ДокументДОМ.ПолучитьЭлементыПоИмени("soapenv:Body")[0];
	
	ЗаписьXML = Новый ЗаписьXML;
	ЗаписьXML.Отступ = Ложь;
	ЗаписьXML.УстановитьСтроку();
	ЗаписьDOM = Новый ЗаписьDOM;
	ЗаписьDOM.Записать(УзелТелоЗапроса, ЗаписьXML);
	ТелоЗапроса = ЗаписьXML.Закрыть();
	
	ТекстSOAP = СтрШаблон(
		"<soapenv:Envelope %1>
		|	<soapenv:Header>
		|		<wsse:Security soapenv:actor=""http://eln.fss.ru/actor/insurer/%4"">
		|			<wsse:BinarySecurityToken EncodingType=""http://docs.oasis-open.org/wss/2004/01/oasis-200401-wss-soap-message-security-1.0#Base64Binary"" ValueType=""http://docs.oasis-open.org/wss/2004/01/oasis-200401-wss-x509-token-profile-1.0#X509v3"" wsu:Id=""http://eln.fss.ru/actor/insurer/%4"">%2
		|			</wsse:BinarySecurityToken>%3
		|		</wsse:Security>
		|		%6
		|	</soapenv:Header>
		|%5
		|</soapenv:Envelope>",
		ДобавляемыеАтрибутыЗапроса,
		СертификатПолучателя64,
		ТекстУзлаПодписи,
		РегистрационныйНомерФСС,
		ТелоЗапроса,
		ТекстУзлаСертификата);
	
	ТекстSOAP = СтрЗаменить(ТекстSOAP, Символы.Таб, "");
	ТекстSOAP = СтрЗаменить(ТекстSOAP, Символы.ВК, "");
	ТекстSOAP = СтрЗаменить(ТекстSOAP, Символы.ПС, "");
	
	// Записываем сформированный запрос в файл.
	ВременныйФайлТекстаSOAP = ПолучитьИмяВременногоФайла();
	ЗаписываемыйФайлТекстаSOAP = Новый ЗаписьТекста(ВременныйФайлТекстаSOAP, "utf-8");
	ЗаписываемыйФайлТекстаSOAP.Записать(ТекстSOAP);
	ЗаписываемыйФайлТекстаSOAP.Закрыть();
	
	ДвоичныеДанныеТекстаSOAP = Новый ДвоичныеДанные(ВременныйФайлТекстаSOAP);
	ТекстSOAP64 = Base64Строка(ДвоичныеДанныеТекстаSOAP);
	Результат = Сред(ТекстSOAP64, 5); // удалим сигнатуру BOM из файла в кодировке "UTF-8"
	
	Если НЕ ВозвращатьТекстВBase64 ИЛИ ИспользуетсяВременноеХранилище Тогда
		ДвоичныеДанныеТекстаSOAP = Base64Значение(Результат);
		Результат = ПоместитьВоВременноеХранилище(ДвоичныеДанныеТекстаSOAP, Новый УникальныйИдентификатор);
	КонецЕсли;
	
	ОперацииСФайламиЭДКО.УдалитьВременныйФайл(ВременныйФайлТекстаSOAP);
	
	Возврат Результат;
	
КонецФункции

Функция АдресSOAPЗапросОперацииСЭЛНВФССПоПодписи(
		Знач РегистрационныйНомерФСС,
		Знач ВыгрузкаЗапроса,
		Знач ПодписьЗапросаЭЛН64,
		Знач СертификатПолучателя64,
		Знач ДобавляемыеАтрибутыЗапроса,
		Знач ДобавляемыеАтрибутыПодписи,
		Знач ВозвращатьТекстВBase64 = Ложь) Экспорт
	
	ВременныйФайлПодписи = ПолучитьИмяВременногоФайла();
	ИспользуетсяВременноеХранилище = ЭтоАдресВременногоХранилища(ПодписьЗапросаЭЛН64);
	Если ИспользуетсяВременноеХранилище Тогда
		ДвоичныеДанныеПодписи = ПолучитьИзВременногоХранилища(ПодписьЗапросаЭЛН64);
	Иначе
		ДвоичныеДанныеПодписи = Base64Значение(ПодписьЗапросаЭЛН64);
	КонецЕсли;
	ДвоичныеДанныеПодписи.Записать(ВременныйФайлПодписи);
	
	ЧтениеXML = Новый ЧтениеXML;
	ЧтениеXML.ОткрытьФайл(ВременныйФайлПодписи);
	ПостроительDOM = Новый ПостроительDOM;
	ДокументDOM = ПостроительDOM.Прочитать(ЧтениеXML);
	ЧтениеXML.Закрыть();
	
	ОперацииСФайламиЭДКО.УдалитьВременныйФайл(ВременныйФайлПодписи);
	
	УзелПодписываемаяИнформация = ДокументDOM.ПолучитьЭлементыПоИмени("SignedInfo")[0];
	
	ЗаписьXML = Новый ЗаписьXML;
	ЗаписьXML.УстановитьСтроку();
	ЗаписьXML.Отступ = Ложь;
	ЗаписьDOM = Новый ЗаписьDOM;
	ЗаписьDOM.Записать(УзелПодписываемаяИнформация, ЗаписьXML);
	ПодписываемаяИнформацияXML = ЗаписьXML.Закрыть();
	Если ДобавляемыеАтрибутыПодписи <> "" Тогда
		ПодписываемаяИнформацияXML = СтрЗаменить(ПодписываемаяИнформацияXML, " " + ДобавляемыеАтрибутыПодписи, "");
	КонецЕсли;
	
	СодержимоеПодписи = ДокументDOM.ПолучитьЭлементыПоИмени("SignatureValue")[0].ТекстовоеСодержимое;
	СодержимоеПодписи = СтрЗаменить(СодержимоеПодписи, Символы.ПС, "");
	СодержимоеПодписи = СтрЗаменить(СодержимоеПодписи, Символы.ВК, "");
	
	ТекстУзлаПодписи = СтрШаблон("
		|			<Signature %1>
		|%2
		|				<SignatureValue>%3</SignatureValue>
		|				<KeyInfo>
		|					<wsse:SecurityTokenReference>
		|						<wsse:Reference URI=""#http://eln.fss.ru/actor/insurer/%4"" ValueType=""http://docs.oasis-open.org/wss/2004/01/oasis-200401-wss-x509-token-profile-1.0#X509v3""/>
		|					</wsse:SecurityTokenReference>
		|				</KeyInfo>
		|			</Signature>",
		ДобавляемыеАтрибутыПодписи,
		ПодписываемаяИнформацияXML,
		СодержимоеПодписи,
		РегистрационныйНомерФСС);
	
	ПараметрыФормата = ПараметрыФорматаЭЛН();
	ЭтоВерсия1X = Лев(ПараметрыФормата.Версия, 2) = "1.";
	
	Если НЕ ЭтоВерсия1X Тогда
		ТекстУзлаСертификата = "
			|		<X509Certificate xmlns=""http://www.w3.org/2000/09/xmldsig#"">%1</X509Certificate>";
		ТекстУзлаСертификата = СтрШаблон(ТекстУзлаСертификата, СертификатПолучателя64);
	КонецЕсли;
	
	ВыгрузкаЗапроса = СтрЗаменить(ВыгрузкаЗапроса, "<", "<fil:");
	ВыгрузкаЗапроса = СтрЗаменить(ВыгрузкаЗапроса, "<fil:/", "</fil:");
	
	ВыгрузкаЗапроса = СтрШаблон(
		"<soapenv:Body wsu:Id=""REGNO_%1"">%2</soapenv:Body>",
		РегистрационныйНомерФСС,
		ВыгрузкаЗапроса);
	
	ТекстSOAP = СтрШаблон(
		"<soapenv:Envelope %1>
		|	<soapenv:Header>
		|		<wsse:Security soapenv:actor=""http://eln.fss.ru/actor/insurer/%4"">
		|			<wsse:BinarySecurityToken EncodingType=""http://docs.oasis-open.org/wss/2004/01/oasis-200401-wss-soap-message-security-1.0#Base64Binary"" ValueType=""http://docs.oasis-open.org/wss/2004/01/oasis-200401-wss-x509-token-profile-1.0#X509v3"" wsu:Id=""http://eln.fss.ru/actor/insurer/%4"">%2
		|			</wsse:BinarySecurityToken>%3
		|		</wsse:Security>%6
		|	</soapenv:Header>
		|%5
		|</soapenv:Envelope>",
		ДобавляемыеАтрибутыЗапроса,
		СертификатПолучателя64,
		ТекстУзлаПодписи,
		РегистрационныйНомерФСС,
		ВыгрузкаЗапроса,
		ТекстУзлаСертификата);
	
	ТекстSOAP = СтрЗаменить(ТекстSOAP, Символы.Таб, "");
	ТекстSOAP = СтрЗаменить(ТекстSOAP, Символы.ВК, "");
	ТекстSOAP = СтрЗаменить(ТекстSOAP, Символы.ПС, "");
	
	ВременныйФайлТекстаSOAP = ПолучитьИмяВременногоФайла();
	ЗаписываемыйФайлТекстаSOAP = Новый ЗаписьТекста(ВременныйФайлТекстаSOAP, "utf-8");
	ЗаписываемыйФайлТекстаSOAP.Записать(ТекстSOAP);
	ЗаписываемыйФайлТекстаSOAP.Закрыть();
	
	ДвоичныеДанныеТекстаSOAP = Новый ДвоичныеДанные(ВременныйФайлТекстаSOAP);
	ТекстSOAP64 = Base64Строка(ДвоичныеДанныеТекстаSOAP);
	Результат = Сред(ТекстSOAP64, 5); // удалим сигнатуру BOM из файла в кодировке "UTF-8"
	
	Если НЕ ВозвращатьТекстВBase64 ИЛИ ИспользуетсяВременноеХранилище Тогда
		ДвоичныеДанныеТекстаSOAP = Base64Значение(Результат);
		Результат = ПоместитьВоВременноеХранилище(ДвоичныеДанныеТекстаSOAP, Новый УникальныйИдентификатор);
	КонецЕсли;
	
	ОперацииСФайламиЭДКО.УдалитьВременныйФайл(ВременныйФайлТекстаSOAP);
	
	Возврат Результат;
	
КонецФункции

Функция ЗапросОперацииСОКВЭДВФССДляПодписиSOAP64(
		Знач ВыгрузкаЗапросаОперацииСЭЛН,
		Знач ДобавляемыеАтрибутыЗапроса,
		Знач СвойстваОрганизации,
		Знач ЭтоИсключающаяКанонизация = Ложь) Экспорт
	
	ТекстXMLЗапроса = ВыгрузкаЗапросаОперацииСЭЛН;
	Если НЕ ЭтоИсключающаяКанонизация Тогда
		ТекстXMLЗапроса = СтрЗаменить(ТекстXMLЗапроса, "<", "<fil:");
		ТекстXMLЗапроса = СтрЗаменить(ТекстXMLЗапроса, "<fil:/", "</fil:");
	КонецЕсли;
	
	ТекстXMLЗапроса = СтрШаблон(
		"<s:Body %1 wsu:Id=""%2"">%3</s:Body>",
		ДобавляемыеАтрибутыЗапроса,
		?(ЗначениеЗаполнено(СвойстваОрганизации.ОГРН), "OGRN_" + СвойстваОрганизации.ОГРН, "body"),
		ТекстXMLЗапроса);
	
	ВременныйФайлЗапроса = ПолучитьИмяВременногоФайла();
	ЗаписьТекстаЗапроса = Новый ЗаписьТекста(ВременныйФайлЗапроса, "utf-8");
	ЗаписьТекстаЗапроса.Записать(ТекстXMLЗапроса);
	ЗаписьТекстаЗапроса.Закрыть();
	
	ДвоичныеДанныеЗапроса = Новый ДвоичныеДанные(ВременныйФайлЗапроса);
	ТекстXMLЗапроса64 = Base64Строка(ДвоичныеДанныеЗапроса);
	
	ОперацииСФайламиЭДКО.УдалитьВременныйФайл(ВременныйФайлЗапроса);
	
	Возврат ТекстXMLЗапроса64;
	
КонецФункции

Функция АдресSOAPЗапросОперацииСОКВЭДВФССПоПодписи(
		Знач ВыгрузкаЗапроса,
		Знач ПодписьЗапроса64,
		Знач СертификатПолучателя64,
		Знач ДобавляемыеАтрибутыКонверта,
		Знач ДобавляемыеАтрибутыБезопасности,
		Знач ДобавляемыеАтрибутыПодписи,
		Знач НастройкиОперации = Неопределено) Экспорт
	
	НастройкиВызова = Новый Структура;
	НастройкиВызова.Вставить("РегистрационныйНомерФСС", 			Неопределено);
	НастройкиВызова.Вставить("ОГРН", 								Неопределено);
	НастройкиВызова.Вставить("Организация", 						Неопределено);
	НастройкиВызова.Вставить("ВозвращатьТекстВBase64", 				Ложь);
	НастройкиВызова.Вставить("ЭтоИсключающаяКанонизация", 			Ложь);
	НастройкиВызова.Вставить("ПрефиксИдентификатораОГРН", 			"");
	НастройкиВызова.Вставить("СертификатШифрованияОтветаВПодписи", 	Ложь);
	Если НастройкиОперации <> Неопределено Тогда
		ЗаполнитьЗначенияСвойств(НастройкиВызова, НастройкиОперации);
	КонецЕсли;
	Организация 						= НастройкиВызова.Организация;
	ВозвращатьТекстВBase64 				= НастройкиВызова.ВозвращатьТекстВBase64;
	ЭтоИсключающаяКанонизация 			= НастройкиВызова.ЭтоИсключающаяКанонизация;
	ПрефиксИдентификатораОГРН 			= НастройкиВызова.ПрефиксИдентификатораОГРН;
	СертификатШифрованияОтветаВПодписи 	= НастройкиВызова.СертификатШифрованияОтветаВПодписи;
	
	ВременныйФайлПодписи = ПолучитьИмяВременногоФайла();
	ИспользуетсяВременноеХранилище = ЭтоАдресВременногоХранилища(ПодписьЗапроса64);
	Если ИспользуетсяВременноеХранилище Тогда
		ДвоичныеДанныеПодписи = ПолучитьИзВременногоХранилища(ПодписьЗапроса64);
	Иначе
		ДвоичныеДанныеПодписи = Base64Значение(ПодписьЗапроса64);
	КонецЕсли;
	ДвоичныеДанныеПодписи.Записать(ВременныйФайлПодписи);
	
	ЧтениеXML = Новый ЧтениеXML;
	ЧтениеXML.ОткрытьФайл(ВременныйФайлПодписи);
	ПостроительDOM = Новый ПостроительDOM;
	ДокументDOM = ПостроительDOM.Прочитать(ЧтениеXML);
	ЧтениеXML.Закрыть();
	
	ОперацииСФайламиЭДКО.УдалитьВременныйФайл(ВременныйФайлПодписи);
	
	УзелПодписываемаяИнформация = ДокументDOM.ПолучитьЭлементыПоИмени("SignedInfo")[0];
	
	ЗаписьXML = Новый ЗаписьXML;
	ЗаписьXML.УстановитьСтроку();
	ЗаписьDOM = Новый ЗаписьDOM;
	ЗаписьDOM.Записать(УзелПодписываемаяИнформация, ЗаписьXML);
	ПодписываемаяИнформацияXML = ЗаписьXML.Закрыть();
	Если ДобавляемыеАтрибутыПодписи <> "" Тогда
		ПодписываемаяИнформацияXML = СтрЗаменить(ПодписываемаяИнформацияXML, " " + ДобавляемыеАтрибутыПодписи, "");
	КонецЕсли;
	
	СодержимоеПодписи = ДокументDOM.ПолучитьЭлементыПоИмени("SignatureValue")[0].ТекстовоеСодержимое;
	СодержимоеПодписи = СтрЗаменить(СодержимоеПодписи, Символы.ПС, "");
	СодержимоеПодписи = СтрЗаменить(СодержимоеПодписи, Символы.ВК, "");
	
	ИдентификаторСертификатаСНомером = Истина;
	ТекстУзлаПодписи = СтрШаблон("
		|			<Signature %1>
		|%2
		|				<SignatureValue>%3</SignatureValue>
		|				<KeyInfo>
		|					<wsse:SecurityTokenReference>
		|						<wsse:Reference URI=""%4"" ValueType=""http://docs.oasis-open.org/wss/2004/01/oasis-200401-wss-x509-token-profile-1.0#X509v3""/>
		|					</wsse:SecurityTokenReference>
		|				</KeyInfo>
		|			</Signature>",
		ДобавляемыеАтрибутыПодписи,
		ПодписываемаяИнформацияXML,
		СодержимоеПодписи,
		?(ЗначениеЗаполнено(НастройкиВызова.ОГРН) И ИдентификаторСертификатаСНомером,
			?(ЗначениеЗаполнено(НастройкиВызова.РегистрационныйНомерФСС),
			"#http://okved.fss.ru/actor/insurer/" + Строка(НастройкиВызова.РегистрационныйНомерФСС),
			?(ЗначениеЗаполнено(НастройкиВызова.ПрефиксИдентификатораОГРН), НастройкиВызова.ПрефиксИдентификатораОГРН,
			"#OGRN_") + НастройкиВызова.ОГРН), "#CertId"));
	
	ТекстУзлаСертификата = "";
	Если СертификатШифрованияОтветаВПодписи Тогда
		ТекстУзлаСертификата = СтрШаблон("
			|		<X509Certificate xmlns=""http://www.w3.org/2000/09/xmldsig#"">%1</X509Certificate>",
			СертификатПолучателя64);
	КонецЕсли;
	
	Если НЕ ЭтоИсключающаяКанонизация Тогда
		ВыгрузкаЗапроса = СтрЗаменить(ВыгрузкаЗапроса, "<", "<fil:");
		ВыгрузкаЗапроса = СтрЗаменить(ВыгрузкаЗапроса, "<fil:/", "</fil:");
	КонецЕсли;
	
	ВыгрузкаЗапроса = СтрШаблон(
		"<s:Body wsu:Id=""%1"">%2</s:Body>",
		?(ЗначениеЗаполнено(НастройкиВызова.ОГРН), "OGRN_" + НастройкиВызова.ОГРН, "body"),
		ВыгрузкаЗапроса);
	
	ТекстSOAP = СтрШаблон(
		"<s:Envelope %1>
		|	<s:Header>
		|		<wsse:Security s:actor=""%4""%6>
		|			<wsse:BinarySecurityToken"
		+ " EncodingType=""http://docs.oasis-open.org/wss/2004/01/oasis-200401-wss-soap-message-security-1.0#Base64Binary"""
		+ " ValueType=""http://docs.oasis-open.org/wss/2004/01/oasis-200401-wss-x509-token-profile-1.0#X509v3"" wsu:Id=""%5"">%2
		|			</wsse:BinarySecurityToken>%3
		|		</wsse:Security>%8
		|	</s:Header>
		|%7
		|</s:Envelope>",
		ДобавляемыеАтрибутыКонверта,
		СертификатПолучателя64,
		ТекстУзлаПодписи,
		?(ЗначениеЗаполнено(НастройкиВызова.ОГРН) И НЕ ЗначениеЗаполнено(НастройкиВызова.РегистрационныйНомерФСС),
			?(ЗначениеЗаполнено(НастройкиВызова.ПрефиксИдентификатораОГРН), НастройкиВызова.ПрефиксИдентификатораОГРН,
			"OGRN_") + НастройкиВызова.ОГРН,
			"http://okved.fss.ru/actor/insurer/" + Строка(НастройкиВызова.РегистрационныйНомерФСС)),
		?(ЗначениеЗаполнено(НастройкиВызова.ОГРН) И ИдентификаторСертификатаСНомером,
			?(ЗначениеЗаполнено(НастройкиВызова.РегистрационныйНомерФСС),
			"http://okved.fss.ru/actor/insurer/" + Строка(НастройкиВызова.РегистрационныйНомерФСС),
			?(ЗначениеЗаполнено(НастройкиВызова.ПрефиксИдентификатораОГРН), НастройкиВызова.ПрефиксИдентификатораОГРН,
			"OGRN_") + НастройкиВызова.ОГРН), "CertId"),
		?(ЗначениеЗаполнено(ДобавляемыеАтрибутыБезопасности), " " + ДобавляемыеАтрибутыБезопасности, ""),
		ВыгрузкаЗапроса,
		ТекстУзлаСертификата);
	
	ТекстSOAP = СтрЗаменить(ТекстSOAP, Символы.Таб, "");
	ТекстSOAP = СтрЗаменить(ТекстSOAP, Символы.ВК, "");
	ТекстSOAP = СтрЗаменить(ТекстSOAP, Символы.ПС, "");
	
	ВременныйФайлТекстаSOAP = ПолучитьИмяВременногоФайла();
	ЗаписываемыйФайлТекстаSOAP = Новый ЗаписьТекста(ВременныйФайлТекстаSOAP, "utf-8");
	ЗаписываемыйФайлТекстаSOAP.Записать(ТекстSOAP);
	ЗаписываемыйФайлТекстаSOAP.Закрыть();
	
	ДвоичныеДанныеТекстаSOAP = Новый ДвоичныеДанные(ВременныйФайлТекстаSOAP);
	ТекстSOAP64 = Base64Строка(ДвоичныеДанныеТекстаSOAP);
	Результат = Сред(ТекстSOAP64, 5); // удалим сигнатуру BOM из файла в кодировке "UTF-8"
	
	Если НЕ ВозвращатьТекстВBase64 ИЛИ ИспользуетсяВременноеХранилище Тогда
		ДвоичныеДанныеТекстаSOAP = Base64Значение(Результат);
		Результат = ПоместитьВоВременноеХранилище(ДвоичныеДанныеТекстаSOAP, Новый УникальныйИдентификатор);
	КонецЕсли;
	
	ОперацииСФайламиЭДКО.УдалитьВременныйФайл(ВременныйФайлТекстаSOAP);
	
	Возврат Результат;
	
КонецФункции

#КонецОбласти

#Область ШифрованиеSOAP

Функция АдресЗашифрованногоXMLПоДанным(
		Знач СимметричныйКлючВBase64,
		Знач ПубличныйКлючВBase64,
		Знач ИнициализационныйВекторВBase64,
		Знач ЗашифрованныеДанныеВBase64,
		Знач СертификатШифрованияОтветаВBase64,
		Знач ВозвращатьТекстВBase64 = Ложь,
		Знач НастройкиОперации = Неопределено) Экспорт
	
	НастройкиВызова = Новый Структура;
	НастройкиВызова.Вставить("ШаблонРезультата", 	Неопределено);
	НастройкиВызова.Вставить("Алгоритм", 			Неопределено);
	НастройкиВызова.Вставить("ЭтоСодержимоеOASIS", 	Ложь);
	Если НастройкиОперации <> Неопределено Тогда
		ЗаполнитьЗначенияСвойств(НастройкиВызова, НастройкиОперации);
	КонецЕсли;
	ЭтоШифрованиеЭлемента = Истина;
	
	СимметричныйКлючДляXMLВBase64 = Неопределено;
	ЗашифрованныеДанныеДляXMLВBase64 = Неопределено;
	ДанныеДляЗашифрованногоXML = ПолучитьДанныеДляЗашифрованногоXML(
		СимметричныйКлючВBase64,
		ПубличныйКлючВBase64,
		ИнициализационныйВекторВBase64,
		ЗашифрованныеДанныеВBase64,
		НастройкиВызова.Алгоритм);
	Если ДанныеДляЗашифрованногоXML = Неопределено Тогда
		Возврат Неопределено;
	КонецЕсли;
	СимметричныйКлючДляXMLВBase64 		= ДанныеДляЗашифрованногоXML.СимметричныйКлючДляXMLВBase64;
	ЗашифрованныеДанныеДляXMLВBase64 	= ДанныеДляЗашифрованногоXML.ЗашифрованныеДанныеДляXMLВBase64;
	
	СертификатШифрованияОтветаВBase64 = СтрЗаменить(СертификатШифрованияОтветаВBase64, Символы.ВК, "");
	СимметричныйКлючДляXMLВBase64 = СтрЗаменить(СимметричныйКлючДляXMLВBase64, Символы.ВК, "");
	ЗашифрованныеДанныеДляXMLВBase64 = СтрЗаменить(ЗашифрованныеДанныеДляXMLВBase64, Символы.ВК, "");
	ТекстЗашифрованногоXML = СтрШаблон(
		"<xenc:EncryptedData" + ?(НастройкиВызова.ЭтоСодержимоеOASIS И НЕ ЭтоШифрованиеЭлемента,
		" Type=""http://www.w3.org/2001/04/xmlenc#Content"""
		+ " xmlns:ds=""http://www.w3.org/2000/09/xmldsig#"""
		+ " xmlns:sch=""http://gost34.ibs.ru/WrapperService/Schema"""
		+ " xmlns:wsse=""http://docs.oasis-open.org/wss/2004/01/oasis-200401-wss-wssecurity-secext-1.0.xsd"""
		+ " xmlns:wsu=""http://docs.oasis-open.org/wss/2004/01/oasis-200401-wss-wssecurity-utility-1.0.xsd""",
		" Type=""http://www.w3.org/2001/04/xmlenc#Element""") + " xmlns:xenc=""http://www.w3.org/2001/04/xmlenc#"">
		|	<xenc:EncryptionMethod Algorithm=""urn:ietf:params:xml:ns:cpxmlsec:algorithms:gost28147""/>
		|	<ds:KeyInfo xmlns:ds=""http://www.w3.org/2000/09/xmldsig#"">
		|		<xenc:EncryptedKey"
		+ ?(НастройкиВызова.ЭтоСодержимоеOASIS, " xmlns:xenc=""http://www.w3.org/2001/04/xmlenc#""", "") + ">
		|			<xenc:EncryptionMethod Algorithm=""urn:ietf:params:xml:ns:cpxmlsec:algorithms:transport-gost2001""/>
		|			<ds:KeyInfo>
		|				<ds:X509Data>
		|					<ds:X509Certificate>%1</ds:X509Certificate>
		|				</ds:X509Data>
		|			</ds:KeyInfo>
		|			<xenc:CipherData>
		|				<xenc:CipherValue>%2</xenc:CipherValue>
		|			</xenc:CipherData>
		|		</xenc:EncryptedKey>
		|	</ds:KeyInfo>
		|	<xenc:CipherData>
		|		<xenc:CipherValue>%3</xenc:CipherValue>
		|	</xenc:CipherData>
		|</xenc:EncryptedData>",
		СертификатШифрованияОтветаВBase64,
		СимметричныйКлючДляXMLВBase64,
		ЗашифрованныеДанныеДляXMLВBase64);
	
	Если НастройкиВызова.ШаблонРезультата <> Неопределено Тогда
		ТекстЗашифрованногоXML = СтрШаблон(НастройкиВызова.ШаблонРезультата, ТекстЗашифрованногоXML);
	КонецЕсли;
	
	Если НЕ ВозвращатьТекстВBase64 Тогда
		Результат = ПоместитьВоВременноеХранилище(ТекстЗашифрованногоXML, Новый УникальныйИдентификатор);
		
	Иначе
		ВременныйФайлТекстаЗашифрованногоXML = ПолучитьИмяВременногоФайла();
		ЗаписываемыйФайлТекстаЗашифрованногоXML = Новый ЗаписьТекста(ВременныйФайлТекстаЗашифрованногоXML, "utf-8");
		ЗаписываемыйФайлТекстаЗашифрованногоXML.Записать(ТекстЗашифрованногоXML);
		ЗаписываемыйФайлТекстаЗашифрованногоXML.Закрыть();
		
		ДвоичныеДанныеТекстаЗашифрованногоXML = Новый ДвоичныеДанные(ВременныйФайлТекстаЗашифрованногоXML);
		Результат = Base64Строка(ДвоичныеДанныеТекстаЗашифрованногоXML);
		Результат = Сред(Результат, 5); // удалим сигнатуру BOM из файла в кодировке "UTF-8"
		
		ОперацииСФайламиЭДКО.УдалитьВременныйФайл(ВременныйФайлТекстаЗашифрованногоXML);
	КонецЕсли;
	
	Возврат Результат;
	
КонецФункции

Функция ПолучитьДанныеДляЗашифрованногоXML(
		Знач СимметричныйКлючВBase64,
		Знач ПубличныйКлючВBase64,
		Знач ИнициализационныйВекторВBase64,
		Знач ЗашифрованныеДанныеВBase64,
		Знач Алгоритм = Неопределено)
	
	// формирование симметричного ключа для вставки в XML
	
	ВременныйФайлСимметричногоКлюча = ПолучитьИмяВременногоФайла();
	Если ЭтоАдресВременногоХранилища(СимметричныйКлючВBase64) Тогда
		ДвоичныеДанныеСимметричногоКлюча = ПолучитьИзВременногоХранилища(СимметричныйКлючВBase64);
	Иначе
		ДвоичныеДанныеСимметричногоКлюча = Base64Значение(СимметричныйКлючВBase64);
	КонецЕсли;
	ДвоичныеДанныеСимметричногоКлюча.Записать(ВременныйФайлСимметричногоКлюча);
	МассивСимметричногоКлюча = ПрочитатьФайлВМассив(ВременныйФайлСимметричногоКлюча);
	ОперацииСФайламиЭДКО.УдалитьВременныйФайл(ВременныйФайлСимметричногоКлюча);
	Если МассивСимметричногоКлюча.Количество() < 60 Тогда
		ОбщегоНазначения.СообщитьПользователю(НСтр("ru = 'Неверный формат данных при шифрации'"));
		Возврат Неопределено;
	КонецЕсли;
	
	ВременныйФайлПубличногоКлюча = ПолучитьИмяВременногоФайла();
	Если ЭтоАдресВременногоХранилища(ПубличныйКлючВBase64) Тогда
		ДвоичныеДанныеПубличногоКлюча = ПолучитьИзВременногоХранилища(ПубличныйКлючВBase64);
	Иначе
		ДвоичныеДанныеПубличногоКлюча = Base64Значение(ПубличныйКлючВBase64);
	КонецЕсли;
	ДвоичныеДанныеПубличногоКлюча.Записать(ВременныйФайлПубличногоКлюча);
	МассивПубличногоКлюча = ПрочитатьФайлВМассив(ВременныйФайлПубличногоКлюча);
	ОперацииСФайламиЭДКО.УдалитьВременныйФайл(ВременныйФайлПубличногоКлюча);
	Если МассивСимметричногоКлюча.Количество() < 64 Тогда
		ОбщегоНазначения.СообщитьПользователю(НСтр("ru = 'Неверный формат данных при шифрации'"));
		Возврат Неопределено;
	КонецЕсли;
	
	// заголовок симметричного ключа для XML
	Если Алгоритм = "GOST R 34.10-2012-256" Тогда
		МассивСимметричногоКлючаДляXML = ПолучитьМассивБайтовИзСтрокиВШестнадцатеричномПредставлении("30 81 A9 30 28 04 20");
	ИначеЕсли Алгоритм = "GOST R 34.10-2012-512" Тогда
		МассивСимметричногоКлючаДляXML = ПолучитьМассивБайтовИзСтрокиВШестнадцатеричномПредставлении("30 81 EF 30 28 04 20");
	Иначе
		МассивСимметричногоКлючаДляXML = ПолучитьМассивБайтовИзСтрокиВШестнадцатеричномПредставлении("30 81 A4 30 28 04 20");
	КонецЕсли;
	
	// добавление симметричного ключа
	Для ИндексМассива = 24 По 55 Цикл
		МассивСимметричногоКлючаДляXML.Добавить(МассивСимметричногоКлюча[ИндексМассива]);
	КонецЦикла;
	
	МассивСимметричногоКлючаДляXML.Добавить(4);
	МассивСимметричногоКлючаДляXML.Добавить(4);
	
	// добавление имитовставки
	Для ИндексМассива = 56 По 59 Цикл
		МассивСимметричногоКлючаДляXML.Добавить(МассивСимметричногоКлюча[ИндексМассива]);
	КонецЦикла;
	
	Если Алгоритм = "GOST R 34.10-2012-256" Тогда
		МассивИнформацииОбАлгоритмах = ПолучитьМассивБайтовИзСтрокиВШестнадцатеричномПредставлении(
			"A0 7D 06 09 2A 85 03 07 01 02 05 01 01 A0 66 30 1F 06 08 2A 85 03 07 01 01 01 01 30 13 06 07 2A 85 03 02 02 24 00 06 08 2A 85 03 07 01 01 02 02 03 43 00 04 40");
	ИначеЕсли Алгоритм = "GOST R 34.10-2012-512" Тогда
		МассивИнформацииОбАлгоритмах = ПолучитьМассивБайтовИзСтрокиВШестнадцатеричномПредставлении(
			"A0 81 C2 06 09 2A 85 03 07 01 02 05 01 01 A0 81 AA 30 21 06 08 2A 85 03 07 01 01 01 02 30 15 06 09 2A 85 03 07 01 02 01 02 01 06 08 2A 85 03 07 01 01 02 03 03 81 84 00 04");
	Иначе
		МассивИнформацииОбАлгоритмах = ПолучитьМассивБайтовИзСтрокиВШестнадцатеричномПредставлении(
			"A0 78 06 07 2A 85 03 02 02 1F 01 A0 63 30 1C 06 06 2A 85 03 02 02 13 30 12 06 07 2A 85 03 02 02 24 00 06 07 2A 85 03 02 02 1E 01 03 43 00 04 40");
	КонецЕсли;
	Для каждого ЭлементМассива Из МассивИнформацииОбАлгоритмах Цикл
		МассивСимметричногоКлючаДляXML.Добавить(ЭлементМассива);
	КонецЦикла;
	
	// добавление публичного эфемерного ключа отправителя
	ИндексНачалаДанныхПубличногоКлюча = МассивПубличногоКлюча.Количество() - ?(Алгоритм = "GOST R 34.10-2012-512", 128, 64);
	ИндексКонцаДанныхПубличногоКлюча = МассивПубличногоКлюча.Количество() - 1;
	Для ИндексМассива = ИндексНачалаДанныхПубличногоКлюча По ИндексКонцаДанныхПубличногоКлюча Цикл
		МассивСимметричногоКлючаДляXML.Добавить(МассивПубличногоКлюча[ИндексМассива]);
	КонецЦикла;
	
	МассивСимметричногоКлючаДляXML.Добавить(4);
	МассивСимметричногоКлючаДляXML.Добавить(8);
	
	// добавление инициализационного вектора симметричного ключа
	Для ИндексМассива = 16 По 23 Цикл
		МассивСимметричногоКлючаДляXML.Добавить(МассивСимметричногоКлюча[ИндексМассива]);
	КонецЦикла;
	
	ВременныйФайлСимметричногоКлючаДляXML = ПолучитьИмяВременногоФайла();
	ЗаписатьФайлИзМассива(ВременныйФайлСимметричногоКлючаДляXML, МассивСимметричногоКлючаДляXML);
	ДвоичныеДанныеСимметричногоКлючаДляXML = Новый ДвоичныеДанные(ВременныйФайлСимметричногоКлючаДляXML);
	СимметричныйКлючДляXMLВBase64 = Base64Строка(ДвоичныеДанныеСимметричногоКлючаДляXML);
	ОперацииСФайламиЭДКО.УдалитьВременныйФайл(ВременныйФайлСимметричногоКлючаДляXML);
	
	// формирование зашифрованных данных для вставки в XML
	
	ВременныйФайлИнициализационногоВектора = ПолучитьИмяВременногоФайла();
	Если ЭтоАдресВременногоХранилища(ИнициализационныйВекторВBase64) Тогда
		ДвоичныеДанныеИнициализационногоВектора = ПолучитьИзВременногоХранилища(ИнициализационныйВекторВBase64);
	Иначе
		ДвоичныеДанныеИнициализационногоВектора = Base64Значение(ИнициализационныйВекторВBase64);
	КонецЕсли;
	ДвоичныеДанныеИнициализационногоВектора.Записать(ВременныйФайлИнициализационногоВектора);
	Если ДвоичныеДанныеИнициализационногоВектора.Размер() <> 8 Тогда
		ОбщегоНазначения.СообщитьПользователю(НСтр("ru = 'Неверный формат данных при шифрации'"));
		Возврат Неопределено;
	КонецЕсли;
	
	ВременныйФайлЗашифрованныхДанных = ПолучитьИмяВременногоФайла();
	Если ЭтоАдресВременногоХранилища(ЗашифрованныеДанныеВBase64) Тогда
		ДвоичныеДанныеЗашифрованныхДанных = ПолучитьИзВременногоХранилища(ЗашифрованныеДанныеВBase64);
	Иначе
		ДвоичныеДанныеЗашифрованныхДанных = Base64Значение(ЗашифрованныеДанныеВBase64);
	КонецЕсли;
	ДвоичныеДанныеЗашифрованныхДанных.Записать(ВременныйФайлЗашифрованныхДанных);
	
	МассивИмен = Новый Массив;
	МассивИмен.Добавить(ВременныйФайлИнициализационногоВектора);
	МассивИмен.Добавить(ВременныйФайлЗашифрованныхДанных);
	ВременныйФайлЗашифрованныхДанныхДляXML = ПолучитьИмяВременногоФайла();
	ОбъединитьФайлы(МассивИмен, ВременныйФайлЗашифрованныхДанныхДляXML);
	
	ОперацииСФайламиЭДКО.УдалитьВременныйФайл(ВременныйФайлИнициализационногоВектора);
	ОперацииСФайламиЭДКО.УдалитьВременныйФайл(ВременныйФайлЗашифрованныхДанных);
	
	ДвоичныеДанныеЗашифрованныхДанныхДляXML = Новый ДвоичныеДанные(ВременныйФайлЗашифрованныхДанныхДляXML);
	ЗашифрованныеДанныеДляXMLВBase64 = Base64Строка(ДвоичныеДанныеЗашифрованныхДанныхДляXML);
	ОперацииСФайламиЭДКО.УдалитьВременныйФайл(ВременныйФайлЗашифрованныхДанныхДляXML);
	
	Результат = Новый Структура;
	Результат.Вставить("СимметричныйКлючДляXMLВBase64", 	СимметричныйКлючДляXMLВBase64);
	Результат.Вставить("ЗашифрованныеДанныеДляXMLВBase64", 	ЗашифрованныеДанныеДляXMLВBase64);
	Возврат Результат;
	
КонецФункции

Функция ДанныеПоАдресуЗашифрованногоXML(
		Знач АдресЗашифрованногоXML,
		Знач ПереданТекстВBase64 = Ложь,
		Знач МассивРодительскихУзловЗашифрованныхДанных = Неопределено,
		Знач ВозвращатьТекстВBase64 = Истина,
		Знач СертификатПолучателя64 = Неопределено) Экспорт
	
	// прочитать зашифрованный XML
	
	ИмяВременногоФайлаЗашифрованногоXML = ПолучитьИмяВременногоФайла();
	Если ПереданТекстВBase64 Тогда
		ДвоичныеДанныеЗашифрованногоXML = Base64Значение(АдресЗашифрованногоXML);
		ДвоичныеДанныеЗашифрованногоXML.Записать(ИмяВременногоФайлаЗашифрованногоXML);
	Иначе
		ТекстЗашифрованногоXML = ПолучитьИзВременногоХранилища(АдресЗашифрованногоXML);
		
		Если ТипЗнч(ТекстЗашифрованногоXML) = Тип("Строка") Тогда
			ФайлЗашифрованногоXML = ПолучитьИмяВременногоФайла();
			ТекстовыйФайлЗашифрованногоXML = Новый ЗаписьТекста(ИмяВременногоФайлаЗашифрованногоXML, "windows-1251");
			ТекстовыйФайлЗашифрованногоXML.Записать(ТекстЗашифрованногоXML);
			ТекстовыйФайлЗашифрованногоXML.Закрыть();
		ИначеЕсли ТипЗнч(ТекстЗашифрованногоXML) = Тип("ДвоичныеДанные") Тогда
			ТекстЗашифрованногоXML.Записать(ИмяВременногоФайлаЗашифрованногоXML);
		Иначе
			ВызватьИсключение(НСтр("ru='Неизвестный тип данных.'"));
		КонецЕсли;
	КонецЕсли;
	
	ЧтениеXML = Новый ЧтениеXML;
	ЧтениеXML.ОткрытьФайл(ИмяВременногоФайлаЗашифрованногоXML);
	ПостроительDOM = Новый ПостроительDOM;
	Попытка
		ДокументDOM = ПостроительDOM.Прочитать(ЧтениеXML);
	Исключение
		Шаблон = НСтр("ru = 'Чтение зашифрованных данных Xml: неверный формат данных.
					|Подробное описание ошибки:
					|%1'");
		ТекстОшибки = СтрШаблон(Шаблон, ПодробноеПредставлениеОшибки(ИнформацияОбОшибке()));
		ЗаписьЖурналаРегистрации(
			НСтр("ru = 'Электронный документооборот с контролирующими органами. Чтение зашифрованных данных XMLDSig'"),
			УровеньЖурналаРегистрации.Ошибка,,,
			ТекстОшибки);
		ОбщегоНазначения.СообщитьПользователю(НСтр("ru = 'Ошибка при дешифрации: неверный формат данных.'"));
		Возврат Неопределено;
	КонецПопытки;
	ЧтениеXML.Закрыть();
	
	ОперацииСФайламиЭДКО.УдалитьВременныйФайл(ИмяВременногоФайлаЗашифрованногоXML);
	
	// получить необходимые узлы
	
	РодительскийУзел = ДокументDOM;
	Если МассивРодительскихУзловЗашифрованныхДанных <> Неопределено Тогда
		Для каждого ИмяРодительскогоУзла Из МассивРодительскихУзловЗашифрованныхДанных Цикл
			РодительскиеУзлы = РодительскийУзел.ПолучитьЭлементыПоИмени(ИмяРодительскогоУзла);
			Если РодительскиеУзлы.Количество() = 0 Тогда
				ОбщегоНазначения.СообщитьПользователю(НСтр("ru = 'Ошибка при дешифрации: неверный формат данных.'"));
				Возврат Неопределено;
			КонецЕсли;
			
			РодительскийУзел = РодительскиеУзлы[0];
		КонецЦикла;
	КонецЕсли;
	
	УзлыЗашифрованныхДанных = РодительскийУзел.ПолучитьЭлементыПоИмени("EncryptedData");
	Если УзлыЗашифрованныхДанных.Количество() = 0 Тогда
		ОбщегоНазначения.СообщитьПользователю(НСтр("ru = 'Ошибка при дешифрации: неверный формат данных.'"));
		Возврат Неопределено;
	КонецЕсли;
	
	УзелЗашифрованныхДанных = УзлыЗашифрованныхДанных[0];
	
	УзлыИнформацияОКлючах = УзелЗашифрованныхДанных.ПолучитьЭлементыПоИмени("KeyInfo");
	Если УзлыИнформацияОКлючах.Количество() = 0 Тогда
		ОбщегоНазначения.СообщитьПользователю(НСтр("ru = 'Ошибка при дешифрации: неверный формат данных.'"));
		Возврат Неопределено;
	КонецЕсли;
	
	УзелИнформацияОКлючах = УзлыИнформацияОКлючах[0];
	
	УзлыЗашифрованныйКлюч = УзелИнформацияОКлючах.ПолучитьЭлементыПоИмени("EncryptedKey");
	Если УзлыЗашифрованныйКлюч.Количество() = 0 Тогда
		ОбщегоНазначения.СообщитьПользователю(НСтр("ru = 'Ошибка при дешифрации: неверный формат данных.'"));
		Возврат Неопределено;
	КонецЕсли;
	
	УзелЗашифрованныйКлюч = УзлыЗашифрованныйКлюч[0];
	
	УзлыИнформацияОКлючахШифрования = УзелЗашифрованныйКлюч.ПолучитьЭлементыПоИмени("KeyInfo");
	Если УзлыИнформацияОКлючахШифрования.Количество() = 0 Тогда
		ОбщегоНазначения.СообщитьПользователю(НСтр("ru = 'Ошибка при дешифрации: неверный формат данных.'"));
		Возврат Неопределено;
	КонецЕсли;
	
	УзелИнформацияОКлючахШифрования = УзлыИнформацияОКлючахШифрования[0];
	
	УзлыДанныеX509 = УзелИнформацияОКлючахШифрования.ПолучитьЭлементыПоИмени("X509Data");
	Если УзлыИнформацияОКлючахШифрования.Количество() = 0 Тогда
		ОбщегоНазначения.СообщитьПользователю(НСтр("ru = 'Ошибка при дешифрации: неверный формат данных.'"));
		Возврат Неопределено;
	КонецЕсли;
	
	УзелДанныеX509 = УзлыДанныеX509[0];
	
	УзлыСертификатX509 = УзелДанныеX509.ПолучитьЭлементыПоИмени("X509Certificate");
	Если УзлыСертификатX509.Количество() = 0 Тогда
		ОбщегоНазначения.СообщитьПользователю(НСтр("ru = 'Ошибка при дешифрации: неверный формат данных.'"));
		Возврат Неопределено;
	КонецЕсли;
	
	УзелСертификатX509 = УзлыСертификатX509[0];
	
	УзлыЗашифрованныеДанныеКлюча = УзелЗашифрованныйКлюч.ПолучитьЭлементыПоИмени("CipherData");
	Если УзлыЗашифрованныеДанныеКлюча.Количество() = 0 Тогда
		ОбщегоНазначения.СообщитьПользователю(НСтр("ru = 'Ошибка при дешифрации: неверный формат данных.'"));
		Возврат Неопределено;
	КонецЕсли;
	
	УзелЗашифрованныеДанныеКлюча = УзлыЗашифрованныеДанныеКлюча[0];
	
	УзлыЗашифрованноеЗначениеКлюча = УзелЗашифрованныеДанныеКлюча.ПолучитьЭлементыПоИмени("CipherValue");
	Если УзлыЗашифрованноеЗначениеКлюча.Количество() = 0 Тогда
		ОбщегоНазначения.СообщитьПользователю(НСтр("ru = 'Ошибка при дешифрации: неверный формат данных.'"));
		Возврат Неопределено;
	КонецЕсли;
	
	УзелЗашифрованноеЗначениеКлюча = УзлыЗашифрованноеЗначениеКлюча[0];
	
	УзлыЗашифрованныеДанные = УзелЗашифрованныхДанных.ПолучитьЭлементыПоИмени("CipherData");
	Если УзлыЗашифрованныеДанные.Количество() < 2 Тогда
		ОбщегоНазначения.СообщитьПользователю(НСтр("ru = 'Ошибка при дешифрации: неверный формат данных.'"));
		Возврат Неопределено;
	КонецЕсли;
	
	УзелЗашифрованныеДанные = УзлыЗашифрованныеДанные[1];
	
	УзлыЗашифрованноеЗначение = УзелЗашифрованныеДанные.ПолучитьЭлементыПоИмени("CipherValue");
	Если УзлыЗашифрованноеЗначение.Количество() = 0 Тогда
		ОбщегоНазначения.СообщитьПользователю(НСтр("ru = 'Ошибка при дешифрации: неверный формат данных.'"));
		Возврат Неопределено;
	КонецЕсли;
	
	УзелЗашифрованноеЗначение = УзлыЗашифрованноеЗначение[0];
	
	// прочитать значения, убрав переводы строк
	
	СертификатШифрованияОтветаВBase64 = СтрЗаменить(УзелСертификатX509.ТекстовоеСодержимое, Символы.ВК, "");
	СертификатШифрованияОтветаВBase64 = СтрЗаменить(СертификатШифрованияОтветаВBase64, Символы.ПС, "");
	
	СимметричныйКлючИзXMLВBase64 = СтрЗаменить(УзелЗашифрованноеЗначениеКлюча.ТекстовоеСодержимое, Символы.ВК, "");
	СимметричныйКлючИзXMLВBase64 = СтрЗаменить(СимметричныйКлючИзXMLВBase64, Символы.ПС, "");
	
	ЗашифрованныеДанныеИзXMLВBase64 = СтрЗаменить(УзелЗашифрованноеЗначение.ТекстовоеСодержимое, Символы.ВК, "");
	ЗашифрованныеДанныеИзXMLВBase64 = СтрЗаменить(ЗашифрованныеДанныеИзXMLВBase64, Символы.ПС, "");
	
	Результат = РазобратьДанныеИзЗашифрованногоXML(
		СимметричныйКлючИзXMLВBase64,
		ЗашифрованныеДанныеИзXMLВBase64,
		ВозвращатьТекстВBase64,
		СертификатПолучателя64);
	Если Результат = Неопределено Тогда
		Возврат Неопределено;
	КонецЕсли;
	
	Результат.Вставить("СертификатШифрованияОтветаВBase64", СертификатШифрованияОтветаВBase64);
	Возврат Результат;
	
КонецФункции

Функция РазобратьДанныеИзЗашифрованногоXML(
		Знач СимметричныйКлючИзXMLВBase64,
		Знач ЗашифрованныеДанныеИзXMLВBase64,
		Знач ВозвращатьТекстВBase64 = Истина,
		Знач СертификатПолучателя64 = Неопределено)
	
	ЭтоЭлектроннаяПодписьВМоделиСервиса = НЕ ВозвращатьТекстВBase64;
	Если ЗначениеЗаполнено(СертификатПолучателя64) Тогда
		Алгоритм = КриптографияЭДКО.АлгоритмКриптосообщенияИлиСертификата(
			СертификатПолучателя64,
			Истина,
			Истина,
			ЭтоЭлектроннаяПодписьВМоделиСервиса);
	Иначе
		Алгоритм = КриптографияЭДКОКлиентСервер.АлгоритмПоУмолчанию();
	КонецЕсли;
	
	// извлечение симметричного ключа из данных XML
	
	ВременныйФайлСимметричногоКлючаИзXML = ПолучитьИмяВременногоФайла();
	ДвоичныеДанныеСимметричногоКлючаИзXML = Base64Значение(СимметричныйКлючИзXMLВBase64);
	ДвоичныеДанныеСимметричногоКлючаИзXML.Записать(ВременныйФайлСимметричногоКлючаИзXML);
	МассивСимметричногоКлючаИзXML = ПрочитатьФайлВМассив(ВременныйФайлСимметричногоКлючаИзXML);
	ОперацииСФайламиЭДКО.УдалитьВременныйФайл(ВременныйФайлСимметричногоКлючаИзXML);
	Если МассивСимметричногоКлючаИзXML.Количество() < 167 Тогда
		ОбщегоНазначения.СообщитьПользователю(НСтр("ru = 'Неверный формат данных при дешифрации'"));
		Возврат Неопределено;
	КонецЕсли;
	
	// заголовок симметричного ключа
	МассивСимметричногоКлюча = ПолучитьМассивБайтовИзСтрокиВШестнадцатеричномПредставлении("01 20 00 00 1E 66 00 00 FD 51 4A 37 1E 66 00 00");
	
	// добавление инициализационного вектора симметричного ключа
	ИндексНачала = ?(Алгоритм = "GOST R 34.10-2012-256", 164, ?(Алгоритм = "GOST R 34.10-2012-512", 232, 159));
	ИндексКонца = ?(Алгоритм = "GOST R 34.10-2012-256", 171, ?(Алгоритм = "GOST R 34.10-2012-512", 239, 166));
	Для ИндексМассива = ИндексНачала По ИндексКонца Цикл
		МассивСимметричногоКлюча.Добавить(МассивСимметричногоКлючаИзXML[ИндексМассива]);
	КонецЦикла;
	
	// добавление симметричного ключа
	Для ИндексМассива = 7 По 38 Цикл
		МассивСимметричногоКлюча.Добавить(МассивСимметричногоКлючаИзXML[ИндексМассива]);
	КонецЦикла;
	
	// добавление имитовставки
	Для ИндексМассива = 41 По 44 Цикл
		МассивСимметричногоКлюча.Добавить(МассивСимметричногоКлючаИзXML[ИндексМассива]);
	КонецЦикла;
	
	// добавление завершения симметричного ключа
	Если Алгоритм = "GOST R 34.10-2012-256" ИЛИ Алгоритм = "GOST R 34.10-2012-512" Тогда
		МассивЗавершенияСимметричногоКлюча = ПолучитьМассивБайтовИзСтрокиВШестнадцатеричномПредставлении(
			"30 0B 06 09 2A 85 03 07 01 02 05 01 01");
	Иначе
		МассивЗавершенияСимметричногоКлюча = ПолучитьМассивБайтовИзСтрокиВШестнадцатеричномПредставлении(
			"30 09 06 07 2A 85 03 02 02 1F 01");
	КонецЕсли;
	Для ИндексМассива = 0 По МассивЗавершенияСимметричногоКлюча.ВГраница() Цикл
		МассивСимметричногоКлюча.Добавить(МассивЗавершенияСимметричногоКлюча[ИндексМассива]);
	КонецЦикла;
	
	ВременныйФайлСимметричногоКлюча = ПолучитьИмяВременногоФайла();
	ЗаписатьФайлИзМассива(ВременныйФайлСимметричногоКлюча, МассивСимметричногоКлюча);
	ДвоичныеДанныеСимметричногоКлюча = Новый ДвоичныеДанные(ВременныйФайлСимметричногоКлюча);
	Если ВозвращатьТекстВBase64 Тогда
		СимметричныйКлючВBase64 = Base64Строка(ДвоичныеДанныеСимметричногоКлюча);
	Иначе
		СимметричныйКлючВBase64 = ПоместитьВоВременноеХранилище(ДвоичныеДанныеСимметричногоКлюча, Новый УникальныйИдентификатор);
	КонецЕсли;
	ОперацииСФайламиЭДКО.УдалитьВременныйФайл(ВременныйФайлСимметричногоКлюча);
	
	// извлечение публичного ключа из данных XML
	
	// заголовок публичного ключа
	Если Алгоритм = "GOST R 34.10-2012-256" Тогда
		МассивПубличногоКлюча = ПолучитьМассивБайтовИзСтрокиВШестнадцатеричномПредставлении(
			"06 20 00 00 49 2E 00 00 4D 41 47 31 00 02 00 00 30 13 06 07 2A 85 03 02 02 24 00 06 08 2A 85 03 07 01 01 02 02");
	ИначеЕсли Алгоритм = "GOST R 34.10-2012-512" Тогда
		МассивПубличногоКлюча = ПолучитьМассивБайтовИзСтрокиВШестнадцатеричномПредставлении(
			"06 20 00 00 3D 2E 00 00 4D 41 47 31 00 04 00 00 30 15 06 09 2A 85 03 07 01 02 01 02 01 06 08 2A 85 03 07 01 01 02 03");
	Иначе
		МассивПубличногоКлюча = ПолучитьМассивБайтовИзСтрокиВШестнадцатеричномПредставлении(
			"06 20 00 00 23 2E 00 00 4D 41 47 31 00 02 00 00 30 12 06 07 2A 85 03 02 02 24 00 06 07 2A 85 03 02 02 1E 01");
	КонецЕсли;
	
	// добавление публичного ключа
	ИндексНачала = ?(Алгоритм = "GOST R 34.10-2012-256", 98, ?(Алгоритм = "GOST R 34.10-2012-512", 102, 93));
	ИндексКонца = ?(Алгоритм = "GOST R 34.10-2012-256", 161, ?(Алгоритм = "GOST R 34.10-2012-512", 229, 156));
	Для ИндексМассива = ИндексНачала По ИндексКонца Цикл
		МассивПубличногоКлюча.Добавить(МассивСимметричногоКлючаИзXML[ИндексМассива]);
	КонецЦикла;
	
	ВременныйФайлПубличногоКлюча = ПолучитьИмяВременногоФайла();
	ЗаписатьФайлИзМассива(ВременныйФайлПубличногоКлюча, МассивПубличногоКлюча);
	ДвоичныеДанныеПубличногоКлюча = Новый ДвоичныеДанные(ВременныйФайлПубличногоКлюча);
	Если ВозвращатьТекстВBase64 Тогда
		ПубличныйКлючВBase64 = Base64Строка(ДвоичныеДанныеПубличногоКлюча);
	Иначе
		ПубличныйКлючВBase64 = ПоместитьВоВременноеХранилище(ДвоичныеДанныеПубличногоКлюча, Новый УникальныйИдентификатор);
	КонецЕсли;
	ОперацииСФайламиЭДКО.УдалитьВременныйФайл(ВременныйФайлПубличногоКлюча);
	
	// извлечение инициализационного вектора из данных XML
	
	ВременныйФайлЗашифрованныхДанныхИзXML = ПолучитьИмяВременногоФайла();
	ДвоичныеДанныеЗашифрованныхДанныхИзXML = Base64Значение(ЗашифрованныеДанныеИзXMLВBase64);
	ДвоичныеДанныеЗашифрованныхДанныхИзXML.Записать(ВременныйФайлЗашифрованныхДанныхИзXML);
	МассивИнициализационногоВектора = ПрочитатьФайлВМассив(ВременныйФайлЗашифрованныхДанныхИзXML, 0, 8);
	Если МассивИнициализационногоВектора.Количество() < 8 Тогда
		ОбщегоНазначения.СообщитьПользователю(НСтр("ru = 'Неверный формат данных при дешифрации'"));
		ОперацииСФайламиЭДКО.УдалитьВременныйФайл(ВременныйФайлЗашифрованныхДанныхИзXML);
		Возврат Неопределено;
	КонецЕсли;
	
	ВременныйФайлИнициализационногоВектора = ПолучитьИмяВременногоФайла();
	ЗаписатьФайлИзМассива(ВременныйФайлИнициализационногоВектора, МассивИнициализационногоВектора);
	ДвоичныеДанныеИнициализационногоВектора = Новый ДвоичныеДанные(ВременныйФайлИнициализационногоВектора);
	Если ВозвращатьТекстВBase64 Тогда
		ИнициализационныйВекторВBase64 = Base64Строка(ДвоичныеДанныеИнициализационногоВектора);
	Иначе
		ИнициализационныйВекторВBase64 = ПоместитьВоВременноеХранилище(ДвоичныеДанныеИнициализационногоВектора, Новый УникальныйИдентификатор);
	КонецЕсли;
	ОперацииСФайламиЭДКО.УдалитьВременныйФайл(ВременныйФайлИнициализационногоВектора);
	
	ВременныйФайлЗашифрованныхДанных = ПолучитьИмяВременногоФайла();
	СкопироватьФрагментИзФайла(ВременныйФайлЗашифрованныхДанныхИзXML, ВременныйФайлЗашифрованныхДанных, 8);
	ОперацииСФайламиЭДКО.УдалитьВременныйФайл(ВременныйФайлЗашифрованныхДанныхИзXML);
	
	ДвоичныеДанныеЗашифрованныхДанных = Новый ДвоичныеДанные(ВременныйФайлЗашифрованныхДанных);
	Если ВозвращатьТекстВBase64 Тогда
		ЗашифрованныеДанныеВBase64 = Base64Строка(ДвоичныеДанныеЗашифрованныхДанных);
	Иначе
		ЗашифрованныеДанныеВBase64 = ПоместитьВоВременноеХранилище(ДвоичныеДанныеЗашифрованныхДанных, Новый УникальныйИдентификатор);
	КонецЕсли;
	ОперацииСФайламиЭДКО.УдалитьВременныйФайл(ВременныйФайлЗашифрованныхДанных);
	
	Результат = Новый Структура;
	Результат.Вставить("СимметричныйКлючВBase64", 			СимметричныйКлючВBase64);
	Результат.Вставить("ПубличныйКлючВBase64", 				ПубличныйКлючВBase64);
	Результат.Вставить("ИнициализационныйВекторВBase64", 	ИнициализационныйВекторВBase64);
	Результат.Вставить("ЗашифрованныеДанныеВBase64", 		ЗашифрованныеДанныеВBase64);
	Возврат Результат;
	
КонецФункции

#КонецОбласти

#Область ОбменСЭДОФСС

Функция СтатусОшибкиСЭДО()
	
	Возврат "ERROR";
	
КонецФункции

Функция СтатусУспехаСЭДО()
	
	Возврат "SUCCESS";
	
КонецФункции

Функция ТипСообщенийПодпискиСтрахователяНаОповещенияЭЛНСЭДО()
	
	Возврат "7";
	
КонецФункции

Функция ТипСообщенийОтпискиСтрахователяНаОповещенияЭЛНСЭДО()
	
	Возврат "8";
	
КонецФункции

Функция ЗначениеВКоллекцию(Значение)
	
	Если ТипЗнч(Значение) <> Тип("Массив")
		И ТипЗнч(Значение) <> Тип("ТаблицаЗначений")
		И ТипЗнч(Значение) <> Тип("СписокXDTO") Тогда
		
		Коллекция = Новый Массив;
		Коллекция.Добавить(Значение);
		Возврат Коллекция;
		
	КонецЕсли;
	
	Возврат Значение;
	
КонецФункции

Процедура УстановитьПризнакСтраховательПодписанНаУведомленияПоЭЛН(Организация, Значение) Экспорт
	
	Запись = РегистрыСведений.НастройкиОбменаФСС.СоздатьМенеджерЗаписи();
	Запись.Организация = Организация;
	Запись.Пользователь = Справочники.Пользователи.ПустаяСсылка();
	Запись.Прочитать();
	Если Запись.СтраховательПодписанНаУведомленияПоЭЛН <> Значение Тогда
		Запись.Организация = Организация;
		Запись.Пользователь = Справочники.Пользователи.ПустаяСсылка();
		Запись.СтраховательПодписанНаУведомленияПоЭЛН = Значение;
		Запись.Записать();
	КонецЕсли;
	
КонецПроцедуры

Функция РазобратьРезультатПодпискиСтрахователя(Организация, Знач ДанныеСообщения) Экспорт
	
	//<?xml version="1.0" encoding="UTF-8" standalone="yes"?>
	//<subscriptionResponse responseOn="1bcd0ddb-120d-42c5-a17c-e32bf9895095"
	//xmlns="http://www.fss.ru/integration/types/eln/event/v01" xmlns:ns2="http://www.fss.ru/integration/types/common/v01">
	//	<result>
	//		<regNum>7700000003</regNum>
	//		<status>SUCCESS</status>
	//	</result>
	//</subscriptionResponse>
	
	Чтение = Новый ЧтениеXML;
	Чтение.УстановитьСтроку(ДанныеСообщения.ТекстСообщения);
	Данные = ФабрикаXDTO.ПрочитатьXML(Чтение);
	Чтение.Закрыть();
	
	ИдентификаторИсходногоСообщения = Данные.responseOn;
	РезультатПоиска = ДанныеИсходящегоСообщенияСЭДОФСС(Организация, ИдентификаторИсходногоСообщения);
	
	Если НЕ РезультатПоиска.Выполнено Тогда
		// Исходное сообщение не найдено.
		Возврат Ложь;
	КонецЕсли;
	
	ДанныеИсходногоСообщения = РезультатПоиска.ДанныеСообщения;
	РезультатОперации = Данные.result;
	Если РезультатОперации.status = СтатусУспехаСЭДО() Тогда
		// Обрабатываем только успешные изменения статуса подписки страхователя.
		Если ДанныеИсходногоСообщения.Тип =
			ТипСообщенийПодпискиСтрахователяНаОповещенияЭЛНСЭДО() Тогда
				УстановитьПризнакСтраховательПодписанНаУведомленияПоЭЛН(Организация, Истина);
		ИначеЕсли ДанныеИсходногоСообщения.Тип =
			ТипСообщенийОтпискиСтрахователяНаОповещенияЭЛНСЭДО() Тогда
				УстановитьПризнакСтраховательПодписанНаУведомленияПоЭЛН(Организация, Ложь);
		КонецЕсли;
	КонецЕсли;
	
	Возврат Истина;
	
КонецФункции

Функция РазобратьОтветВходящиеСообщенияСЭДОФСС(СодержимоеОтвета, ДатаСообщений = Неопределено) Экспорт
	
	Результат = Новый Структура;
	Результат.Вставить("ДанныеСообщений", Новый Массив);
	Результат.Вставить("Выполнено", Ложь);
	Результат.Вставить("ОписаниеОшибки", "");
	
	Чтение = Новый ЧтениеXML;
	Чтение.УстановитьСтроку(СодержимоеОтвета);
	ДанныеОтвета = ФабрикаXDTO.ПрочитатьXML(Чтение);
	Чтение.Закрыть();
	
	// Пример ответа сервера:
	//<soap:Body wsu:Id="OGRN_1027739443236">
	//   <ns2:messageListResponse xmlns:ns2="http://www.fss.ru/integration/types/sedo/v01" xmlns:ns3="http://www.fss.ru/integration/types/fault/v01">
	//      <result>
	//         <messageCount>1</messageCount>
	//         <messageList>
	//            <uuid>d675d4bf-abc6-49cd-ac97-5d8e7b9ff360</uuid>
	//            <messageType>5</messageType>
	//            <recipientId>7423022153</recipientId>
	//            <needConfirm>false</needConfirm>
	//         </messageList>
	//      </result>
	//   </ns2:messageListResponse>
	//</soap:Body>
	
	ДанныеСообщенияШаблон = Новый Структура;
	ДанныеСообщенияШаблон.Вставить("Идентификатор", "");
	ДанныеСообщенияШаблон.Вставить("Тип", 0);
	ДанныеСообщенияШаблон.Вставить("Получатель", "");
	ДанныеСообщенияШаблон.Вставить("ТребуетсяПодтверждение", Ложь);
	ДанныеСообщенияШаблон.Вставить("Новое", Ложь);
	ДанныеСообщенияШаблон.Вставить("Дата", Неопределено);
	
	УзелДанных = ДанныеОтвета.Body.messageListResponse;
	
	Если УзелДанных.Свойства().Получить("errorList") <> Неопределено Тогда
		Ошибки = Новый Массив;
		Для каждого Ошибка Из ЗначениеВКоллекцию(УзелДанных.errorList.message) Цикл
			Ошибки.Добавить(Ошибка);
		КонецЦикла;
		ТекстОшибки = СтрСоединить(Ошибки, Символы.ПС);
		Шаблон = НСтр("ru='Ответ сервера СЭДО ФСС: ""%1""'");
		Результат.ОписаниеОшибки = СтрШаблон(Шаблон, ТекстОшибки);
		Возврат Результат;
	КонецЕсли;
	
	Количество = XMLЗначение(Тип("Число"), УзелДанных.result.messageCount);
	Если Количество > 0 Тогда
		Для каждого Сообщение Из ЗначениеВКоллекцию(УзелДанных.result.messageList) Цикл
			
			ДанныеСообщения = ОбщегоНазначения.СкопироватьРекурсивно(ДанныеСообщенияШаблон);
			ДанныеСообщения.Идентификатор = Сообщение.uuid;
			ДанныеСообщения.Тип = XMLЗначение(Тип("Число"), Сообщение.messageType);
			ДанныеСообщения.Получатель = Сообщение.recipientId;
			ДанныеСообщения.ТребуетсяПодтверждение = XMLЗначение(Тип("Булево"), Сообщение.needConfirm);
			
			Если ЗначениеЗаполнено(ДатаСообщений) Тогда
				ДанныеСообщения.Дата = ДатаСообщений;
			Иначе
				ДанныеСообщения.Дата = ТекущаяДатаСеанса();
			КонецЕсли;
				
			Результат.ДанныеСообщений.Добавить(ДанныеСообщения);
			
		КонецЦикла;
	КонецЕсли;
	
	Результат.Выполнено = Истина;
	
	Возврат Результат;
	
КонецФункции

// Разбор документа, предназначеного для получения сведений о найденных ошибках
// логического контроля в конверте сообщения.
Функция РазобратьСообщениеСОшибкойЛогическогоКонтроляСЭДОФСС(СодержимоеСообщения)
	
	Результат = Новый Структура;
	Результат.Вставить("Выполнено", Ложь);
	Результат.Вставить("ИдентификаторИсходногоСообщения", "");
	Результат.Вставить("ОшибкиКонтроля", "");
	
	// Пример ответа сервера:
	//<?xml version="1.0" encoding="UTF-8" standalone="yes"?>
	//<invalidDataFault xmlns="http://www.fss.ru/integration/types/fault/v01">
	//	<uuidInbound>4900e566-d7ed-4a80-91d2-8dd9f26d5f89</uuidInbound>
	//	<faultList>
	//		<fault>
	//			<uuid>4900e566-d7ed-4a80-91d2-8dd9f26d5f89</uuid>
	//			<code>E_SEDO_1025</code>
	//			<message>Пара рег.номер 7711000003 из запроса и ОГРН 1027739443236 из сертификата не найдены в справочнике страхователей</message>
	//		</fault>
	//	</faultList>
	//</invalidDataFault>
	
	Чтение = Новый ЧтениеXML;
	Чтение.УстановитьСтроку(СодержимоеСообщения);
	
	УзелДанных = ФабрикаXDTO.ПрочитатьXML(Чтение);
	
	Результат.ИдентификаторИсходногоСообщения = УзелДанных.uuidInbound;
	Ошибки = Новый Массив;
	Если УзелДанных.Свойства().Получить("faultList") <> Неопределено Тогда
		Для каждого Ошибка Из ЗначениеВКоллекцию(УзелДанных.faultList.fault) Цикл
			Ошибки.Добавить(Ошибка.message);
		КонецЦикла;
	КонецЕсли;
	
	Результат.ОшибкиКонтроля = СтрСоединить(Ошибки, Символы.ПС);
	Результат.Выполнено = Истина;
	
	Возврат Результат;
	
КонецФункции

Процедура СохранитьДатуПолученияОтветаНаПодтверждениеОПрочтении(Организация,
		ИдентификаторСообщения,
		ОшибкаПодтверждения = "")

	Запись = РегистрыСведений.ВходящиеСообщенияСЭДОФСС.СоздатьМенеджерЗаписи();
	Запись.Организация = Организация;
	Запись.Идентификатор = ИдентификаторСообщения;
	Запись.Прочитать();
	
	Запись.Организация = Организация;
	Запись.Идентификатор = ИдентификаторСообщения;
	
	Если ПустаяСтрока(ОшибкаПодтверждения) Тогда
		Если НЕ ЗначениеЗаполнено(Запись.ДатаПолученияИзвещенияОПолученииПодтверждения) Тогда
			Запись.ДатаПолученияИзвещенияОПолученииПодтверждения = ТекущаяДатаСеанса();
			Запись.Записать();
		КонецЕсли;
	Иначе
		Если Запись.ПодтверждениеОтправлено
			И НЕ ЗначениеЗаполнено(Запись.ДатаПолученияИзвещенияОПолученииПодтверждения) Тогда
				// Сбрасываем дату отправки подтверждения и значение признака
				// если ещё не получили подтверждение о получении ранее.
				Запись.ДатаОтправкиПодтверждения = Неопределено;
				Запись.ПодтверждениеОтправлено = Ложь;
		КонецЕсли;
		Запись.ОшибкаПодтверждения = ОшибкаПодтверждения;
		Запись.ДатаПолученияОшибкиПодтверждения = ТекущаяДатаСеанса();
		Запись.Записать();
	КонецЕсли;
	
КонецПроцедуры

Функция РазобратьОтветНаПодтверждениеОПОлучении(Организация, ДанныеСообщения) Экспорт
	
	Результат = Новый Структура;
	Результат.Вставить("Выполнено", Ложь);
	
	// Пример ответа сервера:
	//<ns3:confirmMessageReadResponse xmlns:ns2="http://www.fss.ru/integration/types/common/v01"
	//xmlns:ns3="http://www.fss.ru/integration/types/sedo/arm/v01">
	//	<confirmResults>
	//		<uuid>09709829-4967-48cb-9dcb-f30d3043088f</uuid>
	//		<status>ERROR</status>
	//		<warn>
	//			<ns2:code>W_SEDO_3140</ns2:code>
	//			<ns2:details>Сообщение не требует подтверждения о прочтении</ns2:details>
	//		</warn>
	//	</confirmResults>
	//	<confirmResults>
	//		<uuid>40966e45-4388-4894-8a98-502e8d2340ae</uuid>
	//		<status>SUCCESS</status>
	//	</confirmResults>
	//</ns3:confirmMessageReadResponse>
	
	Чтение = Новый ЧтениеXML;
	Чтение.УстановитьСтроку(ДанныеСообщения.ТекстСообщения);
	Содержимое = ФабрикаXDTO.ПрочитатьXML(Чтение);
	Чтение.Закрыть();
	
	Для каждого Элемент Из ЗначениеВКоллекцию(Содержимое.confirmResults) Цикл
		Идентификатор = Элемент.uuid;
		Если Элемент.status = СтатусОшибкиСЭДО() Тогда
			Если Элемент.Свойства().Получить("warn") <> Неопределено Тогда
				ТекстОшибки = Элемент.warn.details;
			Иначе
				ТекстОшибки = Элемент.error.details;
			КонецЕсли;
			
			СохранитьДатуПолученияОтветаНаПодтверждениеОПрочтении(
				Организация,
				Идентификатор,
				ТекстОшибки);
		ИначеЕсли Элемент.status = СтатусУспехаСЭДО() Тогда
			СохранитьДатуПолученияОтветаНаПодтверждениеОПрочтении(
				Организация,
				Идентификатор);
		КонецЕсли;
	КонецЦикла;
	
	Возврат Результат;
	
КонецФункции

Функция ОперацияОтправкиСообщенияСЭДОФСС()
	
	Возврат "PutMessage";
	
КонецФункции

Функция ДанныеИсходящегоСообщенияСЭДОФСС(Организация, Идентификатор)
	
	Результат = Новый Структура;
	Результат.Вставить("Выполнено", Ложь);
	Результат.Вставить("ДанныеСообщения", Неопределено);
	
	ДанныеСообщения = Новый Структура;
	ДанныеСообщения.Вставить("Тип", "");
	ДанныеСообщения.Вставить("Идентификатор", Идентификатор);
	ДанныеСообщения.Вставить("Дата", Неопределено);
	ДанныеСообщения.Вставить("ТекстСообщения", "");
	
	Результат.ДанныеСообщения = ДанныеСообщения;
	
	ТекстЗапроса = "ВЫБРАТЬ РАЗРЕШЕННЫЕ ПЕРВЫЕ 1
	               |	ЖурналЗапросовКСерверамФСС.ТипСообщенияСЭДО КАК ТипСообщенияСЭДО,
	               |	ЖурналЗапросовКСерверамФСС.Дата КАК Дата,
	               |	ЖурналЗапросовКСерверамФСС.ДанныеЗапроса КАК ДанныеЗапроса
	               |ИЗ
	               |	РегистрСведений.ЖурналЗапросовКСерверамФСС КАК ЖурналЗапросовКСерверамФСС
	               |ГДЕ
	               |	ЖурналЗапросовКСерверамФСС.Операция = &Операция
	               |	И ЖурналЗапросовКСерверамФСС.ИдентификаторФСС = &ИдентификаторФСС
	               |	И ЖурналЗапросовКСерверамФСС.Организация = &Организация";
	
	Запрос = Новый Запрос;
	Запрос.Текст = ТекстЗапроса;
	Запрос.УстановитьПараметр("Операция", ОперацияОтправкиСообщенияСЭДОФСС());
	Запрос.УстановитьПараметр("ИдентификаторФСС", Идентификатор);
	Запрос.УстановитьПараметр("Организация", Организация);
	
	Выборка = Запрос.Выполнить().Выбрать();
	Если Выборка.Количество() > 0 Тогда
		Выборка.Следующий();
		Результат.Выполнено = Истина;
		Результат.ДанныеСообщения.Тип  = Выборка.ТипСообщенияСЭДО;
		Результат.ДанныеСообщения.Дата = Выборка.Дата;
		ТекстЗапроса = Выборка.ДанныеЗапроса.Получить();
		Чтение = Новый ЧтениеXML;
		Чтение.УстановитьСтроку(ТекстЗапроса);
		ДанныеЗапроса = ФабрикаXDTO.ПрочитатьXML(Чтение);
		Чтение.Закрыть();
		ДвоичныеДанныеСообщения = Base64Значение(ДанныеЗапроса.Body.putMessageRequest.message);
		Чтение = Новый ЧтениеДанных(ДвоичныеДанныеСообщения);
		ДанныеСообщения.ТекстСообщения = Чтение.ПрочитатьСимволы();
		Чтение.Закрыть();
	КонецЕсли;
	
	Возврат Результат;
	
КонецФункции

Функция ДанныеОшибокЛогическогоКонтроляСЭДОФСС(Организация, ДанныеСообщения) Экспорт
	
	Результат = Новый Структура;
	Результат.Вставить("ДанныеИсходногоСообщения", Неопределено);
	Результат.Вставить("ОшибкиКонтроля", "");
	Результат.Вставить("Организация", Организация);
	Результат.Вставить("ИсходноеСообщениеНайдено", Ложь);
	
	РезультатРазбора = РазобратьСообщениеСОшибкойЛогическогоКонтроляСЭДОФСС(ДанныеСообщения.ТекстСообщения);
	
	ОшибкиКонтроля = РезультатРазбора.ОшибкиКонтроля;
	Идентификатор = РезультатРазбора.ИдентификаторИсходногоСообщения;
	
	// Поиск исходного сообщения.
	РезультатПоиска = ДанныеИсходящегоСообщенияСЭДОФСС(
		Организация,
		Идентификатор);
	Если НЕ РезультатПоиска.Выполнено Тогда
		Шаблон = НСтр("ru='Сообщение с идентификатором %1 не найдено в исходящих запросах для организации %2.'");
		ТекстОшибки = СтрШаблон(Шаблон, Идентификатор, Организация);
		ИмяСобытия = НСтр("ru='Электронный документооборот с контролирующими органами. Обмен с СЭДО ФСС'");
		ЗаписьЖурналаРегистрации(ИмяСобытия,
			УровеньЖурналаРегистрации.Ошибка,,,
			ТекстОшибки);
	Иначе
		Результат.ИсходноеСообщениеНайдено = Истина;
		Результат.ДанныеИсходногоСообщения = РезультатПоиска.ДанныеСообщения;
	КонецЕсли;
		
	Результат.ОшибкиКонтроля = ОшибкиКонтроля;
	
	Возврат Результат;
	
КонецФункции

Функция ОбработатьОшибкуЛогическогоКонтроляПодтвержденияОПолучении(ДанныеОшибки) Экспорт
	
	ТекстСообщения = ДанныеОшибки.ДанныеИсходногоСообщения.ТекстСообщения;
	Чтение = Новый ЧтениеXML;
	Чтение.УстановитьСтроку(ТекстСообщения);
	ДанныеИсходногоСообщения = ФабрикаXDTO.ПрочитатьXML(Чтение);
	Чтение.Закрыть();
	Для каждого Идентификатор Из ЗначениеВКоллекцию(ДанныеИсходногоСообщения.uuids.uuid) Цикл
		СохранитьДатуПолученияОтветаНаПодтверждениеОПрочтении(
			ДанныеОшибки.Организация,
			Идентификатор,
			ДанныеОшибки.ОшибкиКонтроля);
	КонецЦикла;
			
	Возврат Истина;
	
КонецФункции

Функция ТекстЗапросаПолученияКоличестваСообщенийСЭДОФСС(РегистрационныйНомерФСС,
			ДатаСообщений = Неопределено) Экспорт
	
	ИнформацияОПрограмме = ИнформацияОПрограмме();
	ТипВзаимодействияСЭДО = ТипВзаимодействияСтраховательСЭДО();
	
	УзелДаты = "";
	Если ЗначениеЗаполнено(ДатаСообщений) Тогда
		Шаблон = "
			|         <messageDate>%1</messageDate>";
		УзелДаты = СтрШаблон(Шаблон, XMLДата(ДатаСообщений));
	КонецЕсли;
	
	Шаблон = "<soapenv:Envelope xmlns:soapenv=""http://schemas.xmlsoap.org/soap/envelope/"" xmlns:v01=""http://www.fss.ru/integration/types/sedo/v01"">
		|   <soapenv:Header/>
		|   <soapenv:Body>
		|      <v01:getCntMessageRequest>
		|         <systemInfo>
		|            <specVersion>1.0</specVersion>
		|            <software>%3</software>
		|            <softwareVersion>%4</softwareVersion>
		|         </systemInfo>
		|         <interactionType>%1</interactionType>
		|         <recipientId>%2</recipientId>%5
		|      </v01:getCntMessageRequest>
		|   </soapenv:Body>
		|</soapenv:Envelope>";
	
	Текст = СтрШаблон(Шаблон,
		ТипВзаимодействияСЭДО,
		РегистрационныйНомерФСС,
		ИнформацияОПрограмме.ИДКонфигурации,
		ИнформацияОПрограмме.ВерсияКонфигурации,
		УзелДаты);
	
	Возврат Текст;
	
КонецФункции

// Вызывается из фонового задания обмена с ФСС.
Функция МетаданныеВходящихСообщенийСЭДОФСС(Организация,
		ДатаСообщений = Неопределено) Экспорт
		
	Результат = Новый Структура;
	Результат.Вставить("Выполнено", Ложь);
	Результат.Вставить("ОписаниеОшибки", "");
	Результат.Вставить("ОшибкаСоединения", Ложь);
	Результат.Вставить("ДанныеСообщений", Неопределено);
	
	РегистрационныйНомерФСС = ЭлектронныйДокументооборотСФСС.РегистрационныйНомерФССОрганизации(Организация);
	
	Если ПустаяСтрока(РегистрационныйНомерФСС) Тогда
		Шаблон = НСтр("ru='Не задан рег.номер ФСС для организации %1.'");
		Результат.ОписаниеОшибки = СтрШаблон(Шаблон, Организация);
		Возврат Результат;
	КонецЕсли;
	
	Операция = "GetCountMessages";
	
	ТекстЗапроса = ТекстЗапросаПолученияКоличестваСообщенийСЭДОФСС(
			РегистрационныйНомерФСС,
			ДатаСообщений);
	АдресЗашифрованногоЗапросаSOAP = ПоместитьВоВременноеХранилище(ТекстЗапроса);
	// Отправка запроса.
	РезультатОтправки = ОтправитьЗапросСервисуСЭДОФСС(
		Организация,
		Операция,
		АдресЗашифрованногоЗапросаSOAP);
	Если НЕ РезультатОтправки.Выполнено Тогда
		Шаблон = НСтр("ru='Ошибка получения данных о входящих сообщениях на сервере СЭДО ФСС:
		|%1'");
		Результат.ОписаниеОшибки = СтрШаблон(Шаблон, РезультатОтправки.ОписаниеОшибки);
		Результат.ОшибкаСоединения = РезультатОтправки.ОшибкаСоединения;
		Возврат Результат;
	КонецЕсли;
	
	АдресОтветаSOAP = РезультатОтправки.АдресЗашифрованногоОтветаSOAP;
	
	ТекстОтвета = ПолучитьИзВременногоХранилища(АдресОтветаSOAP);
	УдалитьИзВременногоХранилища(АдресОтветаSOAP);
	УдалитьИзВременногоХранилища(РезультатОтправки.АдресЗашифрованногоОтветаSOAPДвоичныеДанные);
	
	РезультатРазбора = РазобратьОтветВходящиеСообщенияСЭДОФСС(ТекстОтвета, ДатаСообщений);
	Если НЕ РезультатРазбора.Выполнено Тогда
		Результат.ОписаниеОшибки = РезультатРазбора.ОписаниеОшибки;
		Возврат Результат;
	КонецЕсли;
	
	ДанныеСообщений = РезультатРазбора.ДанныеСообщений;
	
	РезультатСохранения = СохранитьДанныеВходящихСообщенийСЭДОФСС(Организация, ДанныеСообщений);
	
	Результат.ДанныеСообщений = РезультатСохранения.ДанныеСообщений;
	Результат.Выполнено = Истина;
	
	Возврат Результат;
	
КонецФункции

Функция СохранитьДанныеВходящихСообщенийСЭДОФСС(Организация, ДанныеСообщений) Экспорт
	
	Результат = Новый Структура;
	Результат.Вставить("ДанныеСообщений", ДанныеСообщений);
	Результат.Вставить("НовыеСообщения", Новый Массив);
	Результат.Вставить("ОшибкиОбработки", Новый Массив);
	Результат.Вставить("БылиОшибкиОбработки", Ложь);
	
	Идентификаторы = Новый Массив;
	Для каждого ДанныеСообщения Из ДанныеСообщений Цикл
		Идентификаторы.Добавить(ДанныеСообщения.Идентификатор);
	КонецЦикла;
	
	Запрос = Новый Запрос;
	Запрос.Текст = 
	"ВЫБРАТЬ
	|	ВходящиеСообщенияСЭДОФСС.Идентификатор КАК Идентификатор
	|ИЗ
	|	РегистрСведений.ВходящиеСообщенияСЭДОФСС КАК ВходящиеСообщенияСЭДОФСС
	|ГДЕ
	|	ВходящиеСообщенияСЭДОФСС.Идентификатор В(&Идентификаторы)
	|	И ВходящиеСообщенияСЭДОФСС.Новое = ЛОЖЬ";
	Запрос.УстановитьПараметр("Идентификаторы", Идентификаторы);
	РезультатЗапроса = Запрос.Выполнить().Выгрузить();
	
	РегистрационныйНомерФСС = ЭлектронныйДокументооборотСФСС.РегистрационныйНомерФССОрганизации(Организация);
	
	// Добавляем в регистр новые сообщений.
	Для каждого ДанныеСообщения Из ДанныеСообщений Цикл
		Идентификатор = ДанныеСообщения.Идентификатор;
		Если НЕ ДанныеСообщения.Свойство("Новое") Тогда
			ДанныеСообщения.Вставить("Новое", Ложь);
		КонецЕсли;
		
		Если РезультатЗапроса.Найти(Идентификатор, "Идентификатор") = Неопределено
			ИЛИ ДанныеСообщения.Свойство("ТекстСообщения") Тогда
			
			Получатель = ДанныеСообщения.Получатель;
			Если СокрЛП(Получатель) <> СокрЛП(РегистрационныйНомерФСС) Тогда
				Шаблон = НСтр("ru='Регистрационный номер ФСС в данных сервера %1 не соответсвует рег.номеру организации %1.'");
				ТекстОшибки = СтрШаблон(Шаблон, Получатель, РегистрационныйНомерФСС);
				ЗаписьЖурналаРегистрации(НСтр("ru='Электронный документооборот с контролирующими органами. Обмен с СЭДО ФСС'"),
					УровеньЖурналаРегистрации.Ошибка,,,
					ТекстОшибки);
			КонецЕсли;
				
			Запись = РегистрыСведений.ВходящиеСообщенияСЭДОФСС.СоздатьМенеджерЗаписи();
			Запись.Организация = Организация;
			Запись.Идентификатор = Идентификатор;
			Запись.Прочитать();
			Если НЕ Запись.Выбран() Тогда
				Запись.Организация = Организация;
				Запись.Идентификатор = Идентификатор;
			КонецЕсли;
			Запись.Получатель = Получатель;
			Запись.Тип = ДанныеСообщения.Тип;
			Запись.ТребуетсяПодтверждение = ДанныеСообщения.ТребуетсяПодтверждение;
			Если НЕ ЗначениеЗаполнено(Запись.ДатаСоздания) Тогда
				Запись.ДатаСоздания = ТекущаяДатаСеанса();
			КонецЕсли;
			Если НЕ ЗначениеЗаполнено(Запись.Дата) Тогда
				Запись.Дата = ДанныеСообщения.Дата;
			КонецЕсли;
			Если ДанныеСообщения.Свойство("ТекстСообщения") Тогда
				Запись.ДатаЗагрузки = ТекущаяДатаСеанса();
				Запись.Содержимое = Новый ХранилищеЗначения(
					ДанныеСообщения.ТекстСообщения,
					Новый СжатиеДанных(9));
				// Снимаем признак нового сообщения если получено содержимое.
				Запись.Новое = Ложь;
				// Вызов предопределенного метода обработки содержимого сообщения.
				ОписаниеОшибки = "";
				Попытка
					// Вызов предопределенного метода.
					РезультатОбработки = ЭлектронныйДокументооборотСФСС.
						ПриПолученииСодержимогоСообщенияСЭДО(Организация, ДанныеСообщения);
					Если РезультатОбработки.ОшибкаОбработки Тогда
						Результат.БылиОшибкиОбработки = Истина;
						ОписаниеОшибки = РезультатОбработки.ОписаниеОшибки;
						Результат.ОшибкиОбработки.Добавить(ОписаниеОшибки);
					КонецЕсли;
				Исключение
					ИнформацияОбОшибке = ИнформацияОбОшибке();
					ОписаниеОшибки = КраткоеПредставлениеОшибки(ИнформацияОбОшибке);
					Результат.БылиОшибкиОбработки = Истина;
					Результат.ОшибкиОбработки.Добавить(ОписаниеОшибки);
					// Запись в журнал.
					ЗаписьЖурналаРегистрации(
						НСтр("ru = 'Электронный документооборот с контролирующими органами. Обмен СЭДО ФСС'"),
						УровеньЖурналаРегистрации.Ошибка,,, ПодробноеПредставлениеОшибки(ИнформацияОбОшибке));
				КонецПопытки;
				Если НЕ ПустаяСтрока(ОписаниеОшибки) Тогда
					// Сохраняем ошибку обработки в регистре.
					Запись.ОшибкаОбработкиСообщения = ОписаниеОшибки;
				Иначе
					Запись.ОшибкаОбработкиСообщения = "";
				КонецЕсли;
			Иначе
				// Сохраняем метаданные сообщения с признаком Новое.
				Запись.Новое = Истина;
				Если НЕ ЗначениеЗаполнено(Запись.ДатаСоздания) Тогда
					Запись.ДатаСоздания = ТекущаяДатаСеанса();
				КонецЕсли;
				ДанныеСообщения.Новое = Истина;
			КонецЕсли;
			
			Запись.Записать();
			
		КонецЕсли;
	КонецЦикла;
	
	НовыеСообщения = Новый Массив;
	Для каждого Сообщение Из ДанныеСообщений Цикл
		Если Сообщение.Новое Тогда
			НовыеСообщения.Добавить(Сообщение);
		КонецЕсли;
	КонецЦикла;
	
	Если НовыеСообщения.Количество() > 0 Тогда
		Обработанные = ЭлектронныйДокументооборотСФСС.
			ПриПолученииСпискаНовыхСообщенийСЭДО(Организация, НовыеСообщения);
	КонецЕсли;
	
	Результат.НовыеСообщения = НовыеСообщения;
	Результат.ДанныеСообщений = ДанныеСообщений;
	
	Возврат Результат;
	
КонецФункции

Процедура УстановитьУсловиеИПараметрЗапросаИзОтбора(Отбор,
		Ключ,
		Запрос,
		ВыражениеУсловия,
		ПреобразоватьЗначениеВМассив = Истина,
		ИндексПакета = 0,
		ИндексОператора = 0)
		
	ЗначениеПараметра = Неопределено;
	Если Отбор <> Неопределено Тогда
		Отбор.Свойство(Ключ, ЗначениеПараметра);
	КонецЕсли;
	ПоВсем = (Отбор = Неопределено ИЛИ ЗначениеПараметра = Неопределено);
	
	Если ПоВсем Тогда
		Возврат;
	КонецЕсли;
		
	// Добавляем условие запроса.
	Схема = Новый СхемаЗапроса;
	Схема.УстановитьТекстЗапроса(Запрос.Текст);
	ОтборЗапроса = Схема.ПакетЗапросов[ИндексПакета].Операторы[ИндексОператора].Отбор;
	ОтборЗапроса.Добавить(ВыражениеУсловия);
	
	ТекстЗапроса = Схема.ПолучитьТекстЗапроса();
	Запрос.Текст = ТекстЗапроса;
	
	// Устанавливаем параметр запроса.
	Если ПреобразоватьЗначениеВМассив Тогда
		ЗначениеПараметра = ЗначениеВКоллекцию(ЗначениеПараметра);
	КонецЕсли;
	Запрос.УстановитьПараметр(Ключ, ЗначениеПараметра);
		
КонецПроцедуры

Процедура ДобавитьНаборФильтровПоСообщениямСЭДОФСС(Отбор, Запрос)
	
	УстановитьУсловиеИПараметрЗапросаИзОтбора(Отбор,
		"Организация",
		Запрос,
		"ВходящиеСообщенияСЭДОФСС.Организация В (&Организация)");
		
	УстановитьУсловиеИПараметрЗапросаИзОтбора(Отбор,
		"Получатель",
		Запрос,
		"ВходящиеСообщенияСЭДОФСС.Получатель В (&Получатель)");
		
	УстановитьУсловиеИПараметрЗапросаИзОтбора(Отбор,
		"Тип",
		Запрос,
		"ВходящиеСообщенияСЭДОФСС.Тип В (&Тип)");
		
	УстановитьУсловиеИПараметрЗапросаИзОтбора(Отбор,
		"ТребуетсяПодтверждение",
		Запрос,
		"ВходящиеСообщенияСЭДОФСС.ТребуетсяПодтверждение В (&ТребуетсяПодтверждение)");
		
	УстановитьУсловиеИПараметрЗапросаИзОтбора(Отбор,
		"Новое",
		Запрос,
		"ВходящиеСообщенияСЭДОФСС.Новое В (&Новое)");
		
	УстановитьУсловиеИПараметрЗапросаИзОтбора(Отбор,
		"Идентификатор",
		Запрос,
		"ВходящиеСообщенияСЭДОФСС.Идентификатор В (&Идентификатор)");
		
КонецПроцедуры

// Возвращает данный входящих сообщений СЭДО.
Функция СообщенияСЭДОФСС(Отбор = Неопределено) Экспорт
	
	Запрос = Новый Запрос;
	
	Запрос.Текст = 
	"ВЫБРАТЬ РАЗРЕШЕННЫЕ
	|	ВходящиеСообщенияСЭДОФСС.Идентификатор КАК Идентификатор,
	|	ВходящиеСообщенияСЭДОФСС.Организация КАК Организация,
	|	ВходящиеСообщенияСЭДОФСС.Тип КАК Тип,
	|	ВходящиеСообщенияСЭДОФСС.Получатель КАК Получатель,
	|	ВходящиеСообщенияСЭДОФСС.ТребуетсяПодтверждение КАК ТребуетсяПодтверждение,
	|	ВходящиеСообщенияСЭДОФСС.Содержимое КАК Содержимое,
	|	ВходящиеСообщенияСЭДОФСС.Новое КАК Новое,
	|	ВходящиеСообщенияСЭДОФСС.Дата КАК Дата,
	|	ВходящиеСообщенияСЭДОФСС.ДатаЗагрузки КАК ДатаЗагрузки,
	|	ВходящиеСообщенияСЭДОФСС.ДатаОтправкиПодтверждения КАК ДатаОтправкиПодтверждения,
	|	ВходящиеСообщенияСЭДОФСС.ДатаПолученияИзвещенияОПолученииПодтверждения КАК ДатаПолученияИзвещенияОПолученииПодтверждения,
	|	ВходящиеСообщенияСЭДОФСС.ДатаПолученияОшибкиПодтверждения КАК ДатаПолученияОшибкиПодтверждения,
	|	ВходящиеСообщенияСЭДОФСС.ДатаСоздания КАК ДатаСоздания,
	|	ВходящиеСообщенияСЭДОФСС.ОшибкаОбработкиСообщения КАК ОшибкаОбработкиСообщения,
	|	ВходящиеСообщенияСЭДОФСС.ОшибкаПодтверждения КАК ОшибкаПодтверждения,
	|	ВходящиеСообщенияСЭДОФСС.ПодтверждениеОтправлено КАК ПодтверждениеОтправлено
	|ИЗ
	|	РегистрСведений.ВходящиеСообщенияСЭДОФСС КАК ВходящиеСообщенияСЭДОФСС";
	
	ДобавитьНаборФильтровПоСообщениямСЭДОФСС(Отбор, Запрос);
	
	РезультатЗапроса = Запрос.Выполнить().Выгрузить();
	
	Возврат РезультатЗапроса;
	
КонецФункции

Функция НовыеСообщенияСЭДОФСС(Организация, Знач Отбор = Неопределено) Экспорт
	
	Если Отбор = Неопределено Тогда
		Отбор = Новый Структура;
		Отбор.Вставить("Организация", Организация);
	КонецЕсли;
	
	Отбор.Вставить("Новое", Истина);
	
	// Отбор по рег.номеру ФСС добавляется чтобы отсечь сообщения, полученные для
	// другого рег.номера, например, при смене рег.номера в справочнике организаций.
	Если ТипЗнч(Отбор.Организация) = Тип("Массив") Тогда
		РегистрационныйНомерФСС = Новый Массив;
		Для каждого ОрганизацияОтбора Из Отбор.Организация Цикл
			РегистрационныйНомерФССЭлемента
				= ЭлектронныйДокументооборотСФСС.РегистрационныйНомерФССОрганизации(ОрганизацияОтбора);
			РегистрационныйНомерФСС.Добавить(РегистрационныйНомерФССЭлемента);
		КонецЦикла;
	Иначе
		РегистрационныйНомерФСС
			= ЭлектронныйДокументооборотСФСС.РегистрационныйНомерФССОрганизации(Организация);
	КонецЕсли;
	Отбор.Вставить("Получатель", РегистрационныйНомерФСС);
	
	Возврат СообщенияСЭДОФСС(Отбор);
	
КонецФункции

Функция СтрахователиПодписаныНаУведомленияПоЭЛН(Страхователи)
	
	Запрос = Новый Запрос;
	ТекстЗапроса =
		"ВЫБРАТЬ РАЗРЕШЕННЫЕ
		|	НастройкиОбменаФСС.Организация КАК Организация
		|ИЗ
		|	РегистрСведений.НастройкиОбменаФСС КАК НастройкиОбменаФСС
		|ГДЕ
		|	НастройкиОбменаФСС.ИспользоватьОбмен = ИСТИНА
		|	И НастройкиОбменаФСС.СтраховательПодписанНаУведомленияПоЭЛН = ИСТИНА
		|	И НастройкиОбменаФСС.Организация В(&Страхователи)
		|	И (НастройкиОбменаФСС.Пользователь ЕСТЬ NULL
		|			ИЛИ НастройкиОбменаФСС.Пользователь = ЗНАЧЕНИЕ(Справочник.Пользователи.ПустаяСсылка))";
	
	Запрос.Текст = ТекстЗапроса;
	Запрос.УстановитьПараметр("Страхователи", Страхователи);
	Возврат НЕ Запрос.Выполнить().Пустой();
	
КонецФункции

Функция ТребуетсяЗапускРегламентногоЗаданияОбменаСФСС(Организации) Экспорт
	
	УстановитьПривилегированныйРежим(Истина);
	ТребуетсяЗапуск = СтрахователиПодписаныНаУведомленияПоЭЛН(Организации);
	УстановитьПривилегированныйРежим(Ложь);
	Если НЕ ТребуетсяЗапуск Тогда
		ЭлектронныйДокументооборотСФССПереопределяемый.
			ПроверитьНеобходимостьЗапускаРегламентногоЗадания(Организации, ТребуетсяЗапуск);
	КонецЕсли;
	Возврат ТребуетсяЗапуск;
	
КонецФункции

// копия функции из клиентского модуля
Функция ТипВзаимодействияСтраховательСЭДО()
	
	Возврат 2;
	
КонецФункции

Функция СерверОбменаСообщениямиСЭДОФСС(ИспользоватьТестовыйСервер)
	
	Результат = Новый Структура;
	
	Если НЕ ИспользоватьТестовыйСервер Тогда
		Результат.Вставить("Определение", "https://sedo.fss.ru/sedo-gateway/api/soap/SedoGateway?wsdl");
	Иначе
		Результат.Вставить("Определение", "https://docs-test.fss.ru/sedo-gateway/api/soap/SedoGateway?wsdl");
	КонецЕсли;
	
	Возврат Результат;
	
КонецФункции

Функция СтраховательПодписанНаУведомленияПоЭЛН(ОрганизацияСсылка) Экспорт
	
	ОтпечаткиСертификатов = ПолучитьОтпечаткиСертификатовИзНастроекОрганизацииДляФСС(ОрганизацияСсылка);
	Возврат ОтпечаткиСертификатов <> Неопределено
		И ОтпечаткиСертификатов.СтраховательПодписанНаУведомленияПоЭЛН = Истина;
	
КонецФункции

Функция ИспользоватьТестовыйСерверСЭДОФСС(ОрганизацияСсылка)
	
	Возврат ИспользоватьТестовыйСерверФССЭЛН(ОрганизацияСсылка);
	
КонецФункции

Функция ПроверитьДоступностьСервераSOAP(АдресСервера, АдресРесурса)
	
	Результат = Новый Структура;
	Результат.Вставить("Выполнено", Ложь);
	Результат.Вставить("ОписаниеОшибки", "");
	
	// Проверка соединения с коротким таймаутом.
	Таймаут = 10;
	ОписаниеОшибкиУстановкиСоединения = "";
	СоединениеHTTP = ДокументооборотСКО.УстановитьСоединениеССерверомИнтернета(
		АдресСервера,
		ОписаниеОшибкиУстановкиСоединения,
		Таймаут);
	Если СоединениеHTTP = Неопределено Тогда
		Шаблон = НСтр("ru = 'Не удалось установить соединение с сервером %2:
								 |%1'");
		ТекстОшибки = СтрШаблон(
			Шаблон,
			ОписаниеОшибкиУстановкиСоединения,
			АдресСервера);
		Результат.ОписаниеОшибки = ТекстОшибки;
		Возврат Результат;
	КонецЕсли;
	
	// Скачиваем определение веб-сервиса.
	HTTPЗапрос = Новый HTTPЗапрос(АдресРесурса + "?wsdl");
	Попытка
		Ответ = СоединениеHTTP.Получить(HTTPЗапрос);
	Исключение
		ОписаниеОшибкиОтправки = ОписаниеОшибки();
		Шаблон = НСтр("ru = 'Не удалось установить соединение с сервером %2:
								 |%1'");
		ТекстОшибки = СтрШаблон(
			Шаблон,
			ОписаниеОшибкиОтправки,
			АдресСервера);
		Результат.ОписаниеОшибки = ТекстОшибки;
		Возврат Результат;
	КонецПопытки;
	
	РезультатАнализа = АнализВыполненияHTTPЗапроса(Ответ, АдресРесурса);
	Если НЕ РезультатАнализа.Выполнено Тогда
		Шаблон = НСтр("ru = 'Ошибка подключения к веб-серверу %2:
								 |%1'");
		ТекстОшибки = СтрШаблон(Шаблон,
			РезультатАнализа.ОписаниеОшибки,
			АдресСервера);
		Результат.ОписаниеОшибки = ТекстОшибки;
		Возврат Результат;
	КонецЕсли;
	
	Результат.Выполнено = Истина;
	
	Возврат Результат;
	
КонецФункции

Функция ОтправитьЗапросСервисуСЭДОФСС(
		Организация,
		Операция,
		АдресЗашифрованногоЗапросаSOAP) Экспорт
		
	Результат = Новый Структура;
	Результат.Вставить("Выполнено", Ложь);
	Результат.Вставить("ОписаниеОшибки", "");
	Результат.Вставить("АдресЗашифрованногоОтветаSOAP", Неопределено);
	Результат.Вставить("АдресЗашифрованногоОтветаSOAPДвоичныеДанные", Неопределено);
	Результат.Вставить("ОшибкаСоединения", Ложь);
	Результат.Вставить("КодСостояния", 0);
	Результат.Вставить("АдресРесурса", "");
	Результат.Вставить("АдресСервера", "");
	Результат.Вставить("ТребуетсяРасшифровкаОтвета", Ложь);
	
	ИспользоватьТестовыйСервер = ИспользоватьТестовыйСерверСЭДОФСС(Организация);
	
	НастройкиWS = СерверОбменаСообщениямиСЭДОФСС(ИспользоватьТестовыйСервер);
	Если НастройкиWS = Неопределено Тогда
		Возврат Неопределено;
	КонецЕсли;
	
	ТекстЗашифрованногоЗапросаSOAP = ПолучитьИзВременногоХранилища(АдресЗашифрованногоЗапросаSOAP);
	
	АдресСервера = НастройкиWS.Определение;
	АдресРесурса = "";
	ПозицияЗавершенияСхемы = СтрНайти(АдресСервера, "://");
	Если ПозицияЗавершенияСхемы > 0 Тогда
		СтрокаURI = Сред(АдресСервера, ПозицияЗавершенияСхемы + 3);
		ПозицияЗавершенияСоединения = СтрНайти(СтрокаURI, "/");
		ДлинаСоединения = ?(ПозицияЗавершенияСоединения > 0, ПозицияЗавершенияСоединения - 1, СтрДлина(СтрокаURI));
		АдресРесурса = Сред(АдресСервера, ПозицияЗавершенияСхемы + 3 + ДлинаСоединения);
		АдресСервера = Лев(АдресСервера, ПозицияЗавершенияСхемы + 2 + ДлинаСоединения);
		
		ПозицияПараметров = СтрНайти(АдресРесурса, "?");
		ДлинаРесурсаНаСервере = ?(ПозицияПараметров > 0, ПозицияПараметров - 1, СтрДлина(АдресРесурса));
		АдресРесурса = Лев(АдресРесурса, ДлинаРесурсаНаСервере);
	КонецЕсли;
	
	Результат.АдресРесурса = АдресРесурса;
	Результат.АдресСервера = АдресСервера;
	
	РезультатПинга = ПроверитьДоступностьСервераSOAP(АдресСервера, АдресРесурса);
	Если НЕ РезультатПинга.Выполнено Тогда
		ТекстОшибки = РезультатПинга.ОписаниеОшибки;
		ОбщегоНазначения.СообщитьПользователю(ТекстОшибки);
		ЗаписьЖурналаРегистрации(НСтр("ru='Электронный документооборот с контролирующими органами. Обмен с СЭДО ФСС'"),
			УровеньЖурналаРегистрации.Ошибка,,,
			ТекстОшибки);
		Результат.ОписаниеОшибки = ТекстОшибки;
		Результат.ОшибкаСоединения = Истина;
		Возврат Результат;
	КонецЕсли;
	
	ОписаниеОшибкиУстановкиСоединения = "";
	Таймаут = 120;
	СоединениеHTTP = ДокументооборотСКО.УстановитьСоединениеССерверомИнтернета(
		АдресСервера,
		ОписаниеОшибкиУстановкиСоединения,
		Таймаут);
	Если СоединениеHTTP = Неопределено Тогда
		ТекстОшибки = НСтр("ru = 'Не удалось установить соединение с сервером:
								 |%1'");
		ТекстОшибки = СтрШаблон(
			ТекстОшибки,
			ОписаниеОшибкиУстановкиСоединения);
		ОбщегоНазначения.СообщитьПользователю(ТекстОшибки);
		ЗаписьЖурналаРегистрации(НСтр("ru='Электронный документооборот с контролирующими органами. Обмен с СЭДО ФСС'"),
			УровеньЖурналаРегистрации.Ошибка,,,
			ТекстОшибки);
		Результат.ОписаниеОшибки = ТекстОшибки;
		Результат.ОшибкаСоединения = Истина;
		Возврат Результат;
	КонецЕсли;
	
	Шаблон = """http://www.fss.ru/integration/ws/gateway/sedo/%1/v01""";
	ОперацияSOAP = СтрШаблон(Шаблон, Операция);
	
	// Формируем заголовки запроса SOAP.
	HTTPЗапрос = Новый HTTPЗапрос(АдресРесурса);
	HTTPЗапрос.Заголовки.Вставить("Content-Type", "text/xml; charset=utf-8");
	HTTPЗапрос.Заголовки.Вставить("Proxy-Connection", "Keep-Alive");
	HTTPЗапрос.Заголовки.Вставить("SOAPAction", ОперацияSOAP);
	
	HTTPЗапрос.УстановитьТелоИзСтроки(ТекстЗашифрованногоЗапросаSOAP);
	
	// Посылаем запрос.
	Попытка
		Ответ = СоединениеHTTP.ОтправитьДляОбработки(HTTPЗапрос);
	Исключение
		ОписаниеОшибкиОтправки = ОписаниеОшибки();
		ТекстОшибки = НСтр("ru = 'Не удалось установить соединение с сервером:
								 |%1'");
		ТекстОшибки = СтрШаблон(
			ТекстОшибки,
			ОписаниеОшибкиОтправки);
		ОбщегоНазначения.СообщитьПользователю(ТекстОшибки);
		ЗаписьЖурналаРегистрации(НСтр("ru='Электронный документооборот с контролирующими органами. Обмен с СЭДО ФСС'"),
			УровеньЖурналаРегистрации.Ошибка,,,
			ТекстОшибки);
		Результат.ОписаниеОшибки = ТекстОшибки;
		Результат.ОшибкаСоединения = Истина;
		Возврат Результат;
	КонецПопытки;
	
	ТекстЗашифрованногоОтветаSOAP = Ответ.ПолучитьТелоКакСтроку(КодировкаТекста.UTF8);
	Результат.АдресЗашифрованногоОтветаSOAP = ПоместитьВоВременноеХранилище(
		ТекстЗашифрованногоОтветаSOAP,
		Новый УникальныйИдентификатор());
	ДвоичныеДанныеОтвета = Ответ.ПолучитьТелоКакДвоичныеДанные();
	Результат.АдресЗашифрованногоОтветаSOAPДвоичныеДанные = ПоместитьВоВременноеХранилище(
		ДвоичныеДанныеОтвета,
		Новый УникальныйИдентификатор());
		
	Если Найти(ТекстЗашифрованногоОтветаSOAP, "<xenc:EncryptedData") > 0 Тогда
		Результат.ТребуетсяРасшифровкаОтвета = Истина;
	КонецЕсли;
	Результат.КодСостояния = Ответ.КодСостояния;
	
	РезультатАнализа = АнализВыполненияHTTPЗапроса(Ответ, АдресРесурса);
	Если НЕ РезультатАнализа.Выполнено Тогда
		Шаблон = НСтр("ru='При выполнении запроса к ресурсу %1 сервера %2 произошла ошибка:
			|%3'");
		ОписаниеОшибки = СтрШаблон(Шаблон, АдресРесурса, АдресСервера, РезультатАнализа.ОписаниеОшибки);
		ЗаписьЖурналаРегистрации(НСтр("ru='Электронный документооборот с контролирующими органами. Обмен с СЭДО ФСС'"),
			УровеньЖурналаРегистрации.Ошибка,,,
			ОписаниеОшибки);
		Результат.ОписаниеОшибки = ОписаниеОшибки;
		Возврат Результат;
	КонецЕсли;
	
	Результат.Выполнено = Истина;
	Возврат Результат;
	
КонецФункции

Функция АнализВыполненияHTTPЗапроса(Ответ, АдресРесурса)
	
	Результат = Новый Структура;
	Результат.Вставить("Выполнено", Ложь);
	Результат.Вставить("ОписаниеОшибки", "");
	
	Если Ответ.КодСостояния < 300 Тогда
		Результат.Выполнено = Истина;
	ИначеЕсли Ответ.КодСостояния >= 400 И Ответ.КодСостояния < 500 Тогда
		ОписаниеОшибки = НСтр("ru='Ошибка клиента, код состояния %1.'");
		Результат.ОписаниеОшибки = СтрШаблон(ОписаниеОшибки, Ответ.КодСостояния);
	ИначеЕсли Ответ.КодСостояния >= 500 И Ответ.КодСостояния < 600 Тогда
		Если Ответ.КодСостояния = 501 Тогда
			Результат.ОписаниеОшибки = СтрШаблон("Операция %1 не реализована.", АдресРесурса);
		Иначе
			ОписаниеОшибки = НСтр("ru='Ошибка сервера, код состояния %1.'");
			Результат.ОписаниеОшибки = СтрШаблон(ОписаниеОшибки, Ответ.КодСостояния);
		КонецЕсли;
	Иначе
		ОписаниеОшибки = НСтр("ru='Ошибка выполнения запроса, код состояния %1.'");
		Результат.ОписаниеОшибки = СтрШаблон(ОписаниеОшибки, Ответ.КодСостояния);
	КонецЕсли;
	
	Возврат Результат;
	
КонецФункции

#КонецОбласти

#Область ОбменЛисткамиНетрудоспособностиСФСС

Функция СерверПриемаОтчетностиФССЭЛН(ИспользоватьТестовыйСервер, Параметры) Экспорт
	
	ПараметрыФормата = ПараметрыФорматаЭЛН();
	
	Результат = Новый Структура;
	
	Если ПараметрыФормата.Версия = "1.0" Тогда
		Если НЕ ИспользоватьТестовыйСервер Тогда
			Результат.Вставить("Определение", "https://docs.fss.ru/WSInsurerCrypto/FileOperationsLnPort?wsdl");
		Иначе
			Результат.Вставить("Определение", "https://docs-test.fss.ru/WSInsurerCrypto/FileOperationsLnPort?wsdl");
		КонецЕсли;
		
	ИначеЕсли ПараметрыФормата.Версия = "1.1" Тогда
		Если НЕ ИспользоватьТестовыйСервер Тогда
			Результат.Вставить("Определение", "https://docs.fss.ru/ws-insurer-crypto-v11/FileOperationsLnPort?wsdl");
		Иначе
			Результат.Вставить("Определение", "https://docs-test.fss.ru/ws-insurer-crypto-v11/FileOperationsLnPort?wsdl");
		КонецЕсли;
		
	Иначе // 2.0
		Если НЕ ИспользоватьТестовыйСервер Тогда
			Результат.Вставить("Определение", "https://docs.fss.ru/ws-insurer-crypto-v20/FileOperationsLnService?wsdl");
		Иначе
			Результат.Вставить("Определение", "https://docs-test.fss.ru/ws-insurer-crypto-v20/FileOperationsLnService?wsdl");
		КонецЕсли;
	КонецЕсли;
	
	ЭтоВерсия1X = Лев(ПараметрыФормата.Версия, 2) = "1.";
	
	Результат.Определение = ДокументооборотСКО.ПолучитьЗаменяемоеЗначениеТестовогоСервера1С(Результат.Определение);
	
	Результат.Вставить("URIПространстваИменСервиса", 	ПараметрыФормата.ПространствоИмен);
	Результат.Вставить("ИмяСервиса", 					?(ЭтоВерсия1X, "FileOperationsLnImplService", "FileOperationsLnService"));
	Результат.Вставить("ИмяТочкиПодключения", 			"FileOperationsLnPort");
	Результат.Вставить("ВерсияОбмена", 					ПараметрыФормата.Версия);
	Результат.Вставить("WSПрокси", 						Неопределено);
	Результат.Вставить("ТипОшибки", 					"");
	Результат.Вставить("КодСостояния", 					0);
	Результат.Вставить("СообщениеОбОшибке", 			"");
	
	Если Параметры.СоздаватьWSПрокси Тогда
		СтруктураURI = ОбщегоНазначенияКлиентСервер.СтруктураURI(Результат.Определение);
		Схема = ?(ЗначениеЗаполнено(СтруктураURI.Схема), СтруктураURI.Схема, "http");
		Прокси = ПолучениеФайловИзИнтернета.ПолучитьПрокси(Схема);
		
		ЗащищенноеСоединение = Неопределено;
		Если (Схема = "https" Или Схема = "ftps")  Тогда
			ЗащищенноеСоединение = Новый ЗащищенноеСоединениеOpenSSL;
		КонецЕсли;
		
		// загрузка или получение из кэша описания веб-сервиса
		ВерсияНастроек = ?(ПараметрыФормата.Версия = "1.0", "", СтрЗаменить(ПараметрыФормата.Версия, ".", "_"));
		КлючНастроек = ?(НЕ ИспользоватьТестовыйСервер, "ЭЛН", "ТестовыеЭЛН");
		
		ДатаОписанияВебСервиса = Неопределено;
		Если БезопасныйРежим() = Ложь Тогда
			УстановитьПривилегированныйРежим(Истина);
			
			ДатаОписанияВебСервиса = ХранилищеОбщихНастроек.Загрузить(
				"ДокументооборотСКонтролирующимиОрганами_ДатаОписанияВебСервиса" + ВерсияНастроек,
				КлючНастроек);
			
			УстановитьПривилегированныйРежим(Ложь);
		КонецЕсли;
		
		ТекущаяДатаСервера = ТекущаяДатаСеанса();
		
		ТребуетсяОбновлениеОписания = (Параметры.ВремяХраненияОписанияВебСервиса = 0 ИЛИ НЕ ЗначениеЗаполнено(ДатаОписанияВебСервиса)
			ИЛИ ТекущаяДатаСервера - ДатаОписанияВебСервиса >= Параметры.ВремяХраненияОписанияВебСервиса ИЛИ ТекущаяДатаСервера < ДатаОписанияВебСервиса);
		
		Если ТребуетсяОбновлениеОписания Тогда
			ПараметрыПолучения = Новый Структура;
			ПараметрыПолучения.Вставить("Таймаут", Параметры.ТаймаутСоединения);
			
			РезультатСкачивания = ПолучениеФайловИзИнтернета.СкачатьФайлВоВременноеХранилище(Результат.Определение, ПараметрыПолучения);
			Если НЕ РезультатСкачивания.Статус Тогда
				Если Параметры.ВозвращатьОписаниеОшибки Тогда
					Результат.ТипОшибки = ?(РезультатСкачивания.КодСостояния = 0 ИЛИ РезультатСкачивания.КодСостояния = 1,
						"ОшибкаСоединения", "ОшибкаНаСервере");
					Результат.КодСостояния = РезультатСкачивания.КодСостояния;
					Результат.СообщениеОбОшибке = РезультатСкачивания.СообщениеОбОшибке;
					Возврат Результат;
				Иначе
					ВызватьИсключение(РезультатСкачивания.СообщениеОбОшибке);
				КонецЕсли;
			КонецЕсли;
			
			ДвоичныеДанныеОписания = ПолучитьИзВременногоХранилища(РезультатСкачивания.Путь);
			
		Иначе
			УстановитьПривилегированныйРежим(Истина);
			
			ДвоичныеДанныеОписания = ХранилищеОбщихНастроек.Загрузить(
				"ДокументооборотСКонтролирующимиОрганами_ОписаниеВебСервиса" + ВерсияНастроек,
				КлючНастроек);
			
			УстановитьПривилегированныйРежим(Ложь);
		КонецЕсли;
		
		// получение объекта доступа к веб-сервису и обновление кэша при необходимости
		ИмяФайлаОписания = ПолучитьИмяВременногоФайла("xml");
		ДвоичныеДанныеОписания.Записать(ИмяФайлаОписания);
		
		Попытка
			WSОпределения = Новый WSОпределения(
				ИмяФайлаОписания,
				"",
				"",
				Прокси,
				Параметры.ТаймаутСоединения,
				ЗащищенноеСоединение);
			
			Результат.WSПрокси = Новый WSПрокси(
				WSОпределения,
				Результат.URIПространстваИменСервиса,
				Результат.ИмяСервиса,
				Результат.ИмяТочкиПодключения,
				Прокси,
				Параметры.ТаймаутВызовов,
				ЗащищенноеСоединение);
		Исключение
			Если Параметры.ВозвращатьОписаниеОшибки Тогда
				ИнформацияОбОшибкеОбмена = ИнформацияОбОшибке();
				ЗаписьЖурналаРегистрации(
					НСтр("ru = 'Обмен данными ЭЛН'"),
					УровеньЖурналаРегистрации.Ошибка,,,
					ПодробноеПредставлениеОшибки(ИнформацияОбОшибкеОбмена));
				
				ПричинаОшибки = ИнформацияОбОшибкеОбмена;
				Пока ПричинаОшибки.Причина <> Неопределено Цикл
					ПричинаОшибки = ПричинаОшибки.Причина;
				КонецЦикла;
				Результат.ТипОшибки = "ОшибкаНаСервере";
				Результат.СообщениеОбОшибке = ПричинаОшибки.Описание;
				Возврат Результат;
				
			ИначеЕсли ЭтоВерсия1X Тогда
				ВызватьИсключение;
			КонецЕсли;
		КонецПопытки;
		
		Если ТребуетсяОбновлениеОписания И БезопасныйРежим() = Ложь Тогда
			УстановитьПривилегированныйРежим(Истина);
			
			ХранилищеОбщихНастроек.Сохранить(
				"ДокументооборотСКонтролирующимиОрганами_ОписаниеВебСервиса" + ВерсияНастроек,
				КлючНастроек,
				ДвоичныеДанныеОписания);
			
			ХранилищеОбщихНастроек.Сохранить(
				"ДокументооборотСКонтролирующимиОрганами_ДатаОписанияВебСервиса" + ВерсияНастроек,
				КлючНастроек,
				ТекущаяДатаСервера);
			
			УстановитьПривилегированныйРежим(Ложь);
		КонецЕсли;
		
		ОперацииСФайламиЭДКО.УдалитьВременныйФайл(ИмяФайлаОписания);
	КонецЕсли;
	
	Возврат Результат;
	
КонецФункции

Функция ИспользоватьТестовыйСерверФССЭЛН(ОрганизацияСсылка) Экспорт
	
	ОтпечаткиСертификатов = ПолучитьОтпечаткиСертификатовИзНастроекОрганизацииДляФСС(ОрганизацияСсылка);
	Возврат ОтпечаткиСертификатов <> Неопределено И ОтпечаткиСертификатов.ТестовыйСерверФССЭЛН = Истина;
	
КонецФункции

Функция ПараметрыФорматаЭЛН() Экспорт
	
	Результат = Новый Структура;
	Результат.Вставить("Версия", 			"2.0");
	Результат.Вставить("ПространствоИмен", 	"http://www.fss.ru/integration/types/eln/ins/v01");
	
	Если ОбщегоНазначения.ПодсистемаСуществует("ЗарплатаКадрыПодсистемы.ПособияСоциальногоСтрахования") Тогда
		МодульОбменЛисткамиНетрудоспособностиФСС = ОбщегоНазначения.ОбщийМодуль("ОбменЛисткамиНетрудоспособностиФСС");
		
		Попытка
			Результат.Версия 			= МодульОбменЛисткамиНетрудоспособностиФСС.Версия();
			Результат.ПространствоИмен 	= МодульОбменЛисткамиНетрудоспособностиФСС.ПространствоИмен();
		Исключение
			ИнформацияОбОшибке = ИнформацияОбОшибке();
			ЗаписьЖурналаРегистрации(
				НСтр("ru = 'Электронный документооборот с контролирующими органами. Обмен ЭЛН'"),
				УровеньЖурналаРегистрации.Ошибка,,, ПодробноеПредставлениеОшибки(ИнформацияОбОшибке));
		КонецПопытки;
	КонецЕсли;
	
	Возврат Результат;
	
КонецФункции

Функция ОтправитьЗапросСервисуФССЭЛН(
		Организация,
		Операция,
		АдресЗашифрованногоЗапросаSOAP) Экспорт
	
	Результат = Новый Структура("АдресЗашифрованногоОтветаSOAP, ТекстОшибки", Неопределено, "");
	
	ПараметрыВызова = Новый Структура;
	ПараметрыВызова.Вставить("СоздаватьWSПрокси", 				Истина);
	ПараметрыВызова.Вставить("ТаймаутСоединения", 				30);
	ПараметрыВызова.Вставить("ТаймаутВызовов", 					600);
	ПараметрыВызова.Вставить("ВозвращатьОписаниеОшибки", 		Ложь);
	ПараметрыВызова.Вставить("ВремяХраненияОписанияВебСервиса", 2 * 60 * 60);
	
	ИспользоватьТестовыйСервер = ИспользоватьТестовыйСерверФССЭЛН(Организация);
	
	НастройкиWS = СерверПриемаОтчетностиФССЭЛН(ИспользоватьТестовыйСервер, ПараметрыВызова);
	Если НастройкиWS = Неопределено Тогда
		Возврат Результат;
	КонецЕсли;
	
	ТекстЗашифрованногоЗапросаSOAP = ПолучитьИзВременногоХранилища(АдресЗашифрованногоЗапросаSOAP);
	УдалитьИзВременногоХранилища(АдресЗашифрованногоЗапросаSOAP);
	
	АдресСервера = НастройкиWS.Определение;
	РесурсНаСервере = "";
	ПозицияЗавершенияСхемы = СтрНайти(АдресСервера, "://");
	Если ПозицияЗавершенияСхемы > 0 Тогда
		СтрокаURI = Сред(АдресСервера, ПозицияЗавершенияСхемы + 3);
		ПозицияЗавершенияСоединения = СтрНайти(СтрокаURI, "/");
		ДлинаСоединения = ?(ПозицияЗавершенияСоединения > 0, ПозицияЗавершенияСоединения - 1, СтрДлина(СтрокаURI));
		РесурсНаСервере = Сред(АдресСервера, ПозицияЗавершенияСхемы + 3 + ДлинаСоединения);
		АдресСервера = Лев(АдресСервера, ПозицияЗавершенияСхемы + 2 + ДлинаСоединения);
		
		ПозицияПараметров = СтрНайти(РесурсНаСервере, "?");
		ДлинаРесурсаНаСервере = ?(ПозицияПараметров > 0, ПозицияПараметров - 1, СтрДлина(РесурсНаСервере));
		РесурсНаСервере = Лев(РесурсНаСервере, ДлинаРесурсаНаСервере);
	КонецЕсли;
	
	ОписаниеОшибкиУстановкиСоединения = "";
	СоединениеHTTP = ДокументооборотСКО.УстановитьСоединениеССерверомИнтернета(АдресСервера, ОписаниеОшибкиУстановкиСоединения);
	Если СоединениеHTTP = Неопределено Тогда
		ТекстОшибки = НСтр("ru = 'Не удалось установить соединение с сервером:
								 |%1'");
		ТекстОшибки = СтрШаблон(
			ТекстОшибки,
			ОписаниеОшибкиУстановкиСоединения);
		Результат.ТекстОшибки = ТекстОшибки;
		Возврат Результат;
	КонецЕсли;
	
	// формируем общие заголовки запроса SOAP
	ЗаголовкиHTTP = Новый Соответствие();
	ЗаголовкиHTTP.Вставить("Content-Type", "text/xml; charset=utf-8");
	ЗаголовкиHTTP.Вставить("Proxy-Connection", "Keep-Alive");
	
	ФайлЗапросаSOAP = ПолучитьИмяВременногоФайла();
	ТекстовыйФайлSOAP = Новый ЗаписьТекста(ФайлЗапросаSOAP, "utf-8");
	ТекстовыйФайлSOAP.Записать(ТекстЗашифрованногоЗапросаSOAP);
	ТекстовыйФайлSOAP.Закрыть();
	
	// посылаем запрос
	ФайлОтветаSOAP = ПолучитьИмяВременногоФайла();
	Попытка
		СоединениеHTTP.ОтправитьДляОбработки(ФайлЗапросаSOAP, РесурсНаСервере, ФайлОтветаSOAP, ЗаголовкиHTTP);
	Исключение
		ОперацииСФайламиЭДКО.УдалитьВременныйФайл(ФайлЗапросаSOAP);
		ОписаниеОшибкиОтправки = ОписаниеОшибки();
		ТекстОшибки = НСтр("ru = 'Не удалось установить соединение с сервером:
								 |%1'");
		ТекстОшибки = СтрШаблон(
			ТекстОшибки,
			ОписаниеОшибкиОтправки);
		
		Результат.ТекстОшибки = ТекстОшибки;
		Возврат Результат;
	КонецПопытки;
	
	ТекстовыйФайлОтветаSOAP = Новый ЧтениеТекста(ФайлОтветаSOAP, "utf-8");
	ТекстЗашифрованногоОтветаSOAP = ТекстовыйФайлОтветаSOAP.Прочитать();
	ТекстовыйФайлОтветаSOAP.Закрыть();
	АдресЗашифрованногоОтветаSOAP = ПоместитьВоВременноеХранилище(ТекстЗашифрованногоОтветаSOAP, Новый УникальныйИдентификатор());
	
	ОперацииСФайламиЭДКО.УдалитьВременныйФайл(ФайлЗапросаSOAP);
	ОперацииСФайламиЭДКО.УдалитьВременныйФайл(ФайлОтветаSOAP);
	
	Результат.АдресЗашифрованногоОтветаSOAP = АдресЗашифрованногоОтветаSOAP;
	Возврат Результат;
КонецФункции

Функция ОбработатьРезультатОтправкиРеестраЭЛН(
		ОтчетСсылка,
		ОрганизацияСсылка,
		ЭтоЭлектроннаяПодписьВМоделиСервиса,
		КороткоеИмяФайлаПакета,
		СтрокаBase64ФайлаПакета,
		ПараметрыРезультата) Экспорт
	
	ПараметрыВызова = Новый Структура;
	ПараметрыВызова.Вставить("АдресРасшифрованногоОтветаSOAP", 			Неопределено);
	ПараметрыВызова.Вставить("АдресЗашифрованногоОтветаSOAP", 			Неопределено);
	ПараметрыВызова.Вставить("ПоляДляЗаписиОтправки", 					Неопределено);
	ПараметрыВызова.Вставить("СохранятьАдресРасшифрованногоОтветаSOAP", Ложь);
	ПараметрыВызова.Вставить("ПодписанныйSOAP64", 						Неопределено);
	Если ПараметрыРезультата <> Неопределено Тогда
		ЗаполнитьЗначенияСвойств(ПараметрыВызова, ПараметрыРезультата);
	КонецЕсли;
	АдресРасшифрованногоОтветаSOAP 			= ПараметрыВызова.АдресРасшифрованногоОтветаSOAP;
	АдресЗашифрованногоОтветаSOAP 			= ПараметрыВызова.АдресЗашифрованногоОтветаSOAP;
	ПоляДляЗаписиОтправки 					= ПараметрыВызова.ПоляДляЗаписиОтправки;
	СохранятьАдресРасшифрованногоОтветаSOAP = ПараметрыВызова.СохранятьАдресРасшифрованногоОтветаSOAP;
	ПодписанныйSOAP64 						= ПараметрыВызова.ПодписанныйSOAP64;
	
	ТекстРезультата = ПолучитьИзВременногоХранилища(АдресРасшифрованногоОтветаSOAP);
	Если НЕ СохранятьАдресРасшифрованногоОтветаSOAP Тогда
		УдалитьИзВременногоХранилища(АдресРасшифрованногоОтветаSOAP);
	КонецЕсли;
	
	ИмяФайлаРезультата = ПолучитьИмяВременногоФайла();
	ФайлРезультата = Новый ЗаписьТекста(ИмяФайлаРезультата, "utf-8");
	ФайлРезультата.Записать(ТекстРезультата);
	ФайлРезультата.Закрыть();
	ДвоичныеДанныеРезультата = Новый ДвоичныеДанные(ИмяФайлаРезультата);
	
	ТекстПакетаРезультата = ПолучитьИзВременногоХранилища(АдресЗашифрованногоОтветаSOAP);
	ИмяФайлаПакетаРезультата = ПолучитьИмяВременногоФайла();
	ФайлПакетаРезультата = Новый ЗаписьТекста(ИмяФайлаПакетаРезультата, "utf-8");
	ФайлПакетаРезультата.Записать(ТекстПакетаРезультата);
	ФайлПакетаРезультата.Закрыть();
	ДвоичныеДанныеПакетаРезультата = Новый ДвоичныеДанные(ИмяФайлаПакетаРезультата);
	
	РезультатыОперации = РезультатыОтправкиРеестраЭЛН(ТекстРезультата);
	СтатусОперации = СтатусОтправкиРеестраЭЛН(РезультатыОперации);
	
	ТекстHTML = ГенерироватьHTMLПротоколаОбработкиРеестраЭЛН(
		ОтчетСсылка,
		ОрганизацияСсылка,
		РезультатыОперации,
		СтатусОперации);
	
	НастройкиОтправки = Новый Структура;
	НастройкиОтправки.Вставить("ОтчетСсылка", 				ОтчетСсылка);
	НастройкиОтправки.Вставить("ПоляДляЗаписиОтправки", 	ПоляДляЗаписиОтправки);
	НастройкиОтправки.Вставить("СтатусОтправки", 			СтатусОперации.СтатусОтправки);
	НастройкиОтправки.Вставить("Протокол", 					ТекстHTML);
	НастройкиОтправки.Вставить("Квитанция", 				ДвоичныеДанныеРезультата);
	НастройкиОтправки.Вставить("ПодписанныйПакет", 			ПодписанныйSOAP64);
	НастройкиОтправки.Вставить("ПакетКвитанции", 			ДвоичныеДанныеПакетаРезультата);
	НоваяОтправкаСсылка = ДобавитьОтправкуФСС(
		ЭтоЭлектроннаяПодписьВМоделиСервиса,
		КороткоеИмяФайлаПакета,
		СтрокаBase64ФайлаПакета,
		РезультатыОперации.ИдентификаторЗапроса,
		НастройкиОтправки);
	
	ОперацииСФайламиЭДКО.УдалитьВременныйФайл(ИмяФайлаРезультата);
	УдалитьИзВременногоХранилища(АдресЗашифрованногоОтветаSOAP);
	Если ПодписанныйSOAP64 <> Неопределено И ЭтоЭлектроннаяПодписьВМоделиСервиса Тогда
		УдалитьИзВременногоХранилища(ПодписанныйSOAP64);
	КонецЕсли;
	
	Результат = Новый Структура;
	Результат.Вставить("НоваяОтправкаСсылка", 	НоваяОтправкаСсылка);
	Результат.Вставить("СтатусОтправки", 		СтатусОперации.СтатусОтправки);
	Возврат Результат;
	
КонецФункции

Функция РезультатыОтправкиРеестраЭЛН(ТекстРезультата)
	
	Результат = Новый Структура;
	Результат.Вставить("ИдентификаторЗапроса", 	"");
	Результат.Вставить("СтатусЗапроса", 		Неопределено);
	Результат.Вставить("СообщениеОбОшибке", 	"");
	Результат.Вставить("РезультатыОбработки", 	Новый Массив);
	
	ДеревоXML = ЗагрузитьXMLВДеревоЗначений(Неопределено, ТекстРезультата);
	Если ДеревоXML = Неопределено Тогда
		Возврат Результат;
	КонецЕсли;
	
	УзелКонверт = НайтиУзел(ДеревоXML, "Envelope");
	Если УзелКонверт = Неопределено Тогда
		Возврат Результат;
	КонецЕсли;
	
	УзелТело = НайтиУзел(УзелКонверт, "Body");
	Если УзелТело = Неопределено Тогда
		Возврат Результат;
	КонецЕсли;
	
	УзелОтвета = НайтиУзел(УзелТело, "prParseReestrFileResponse");
	Если УзелОтвета = Неопределено Тогда
		Возврат Результат;
	КонецЕсли;
	
	УзелРезультата = НайтиУзел(УзелОтвета, "WSResult");
	Если УзелРезультата = Неопределено Тогда
		УзелРезультата = НайтиУзел(УзелОтвета, "wsResult");
		Если УзелРезультата = Неопределено Тогда
			Возврат Результат;
		КонецЕсли;
	КонецЕсли;
	
	УзелИдентификаторЗапроса = НайтиУзел(УзелРезультата, "REQUEST_ID");
	Если УзелИдентификаторЗапроса = Неопределено Тогда
		УзелИдентификаторЗапроса = НайтиУзел(УзелРезультата, "requestId");
	КонецЕсли;
	Если УзелИдентификаторЗапроса <> Неопределено Тогда
		Результат.ИдентификаторЗапроса = XMLЗначение(Тип("Строка"), УзелИдентификаторЗапроса.Значение);
		Результат.ИдентификаторЗапроса = СокрЛП(Результат.ИдентификаторЗапроса);
	КонецЕсли;
	
	УзелСтатусЗапроса = НайтиУзел(УзелРезультата, "STATUS");
	Если УзелСтатусЗапроса = Неопределено Тогда
		УзелСтатусЗапроса = НайтиУзел(УзелРезультата, "status");
	КонецЕсли;
	Если УзелСтатусЗапроса <> Неопределено И ЗначениеЗаполнено(УзелСтатусЗапроса.Значение) Тогда
		Результат.СтатусЗапроса = XMLЗначение(Тип("Число"), УзелСтатусЗапроса.Значение);
	КонецЕсли;
	
	УзелСообщениеОбОшибке = НайтиУзел(УзелРезультата, "MESS");
	Если УзелСообщениеОбОшибке = Неопределено Тогда
		УзелСообщениеОбОшибке = НайтиУзел(УзелРезультата, "mess");
	КонецЕсли;
	Если УзелСообщениеОбОшибке <> Неопределено Тогда
		Результат.СообщениеОбОшибке = XMLЗначение(Тип("Строка"), УзелСообщениеОбОшибке.Значение);
		Результат.СообщениеОбОшибке = СокрЛП(Результат.СообщениеОбОшибке);
	КонецЕсли;
	
	УзелИнформация = НайтиУзел(УзелРезультата, "INFO");
	Если УзелИнформация = Неопределено Тогда
		УзелИнформация = НайтиУзел(УзелРезультата, "info");
	КонецЕсли;
	Если УзелИнформация = Неопределено Тогда
		Возврат Результат;
	КонецЕсли;
	
	УзелНаборЗаписей = НайтиУзел(УзелИнформация, "ROWSET");
	Если УзелНаборЗаписей = Неопределено Тогда
		УзелНаборЗаписей = НайтиУзел(УзелИнформация, "infoRowset");
		Если УзелНаборЗаписей = Неопределено Тогда
			Возврат Результат;
		КонецЕсли;
	КонецЕсли;
	
	УзлыЗапись = НайтиУзел(УзелНаборЗаписей, "ROW", Истина);
	Если УзлыЗапись.Количество() = 0 Тогда
		УзлыЗапись = НайтиУзел(УзелНаборЗаписей, "infoRow", Истина);
	КонецЕсли;
	Для каждого УзелЗапись Из УзлыЗапись Цикл
		РезультатОбработки = Новый Структура;
		РезультатОбработки.Вставить("СтатусОбработки", 	Неопределено);
		РезультатОбработки.Вставить("НомерЗаписиЛН", 	Неопределено);
		РезультатОбработки.Вставить("НомерЛН", 			"");
		РезультатОбработки.Вставить("ХешЛН", 			"");
		РезультатОбработки.Вставить("КодСостоянияЛН", 	"");
		РезультатОбработки.Вставить("ОшибкиОбработки", 	Новый Массив);
		
		УзелСтатусОбработки = НайтиУзел(УзелЗапись, "STATUS");
		Если УзелСтатусОбработки = Неопределено Тогда
			УзелСтатусОбработки = НайтиУзел(УзелЗапись, "status");
		КонецЕсли;
		Если УзелСтатусОбработки <> Неопределено И ЗначениеЗаполнено(УзелСтатусОбработки.Значение) Тогда
			РезультатОбработки.СтатусОбработки = XMLЗначение(Тип("Число"), УзелСтатусОбработки.Значение);
		КонецЕсли;
		
		УзелНомерЗаписиЛН = НайтиУзел(УзелЗапись, "ROW_NO");
		Если УзелНомерЗаписиЛН = Неопределено Тогда
			УзелНомерЗаписиЛН = НайтиУзел(УзелЗапись, "rowNo");
		КонецЕсли;
		Если УзелНомерЗаписиЛН <> Неопределено И ЗначениеЗаполнено(УзелНомерЗаписиЛН.Значение) Тогда
			РезультатОбработки.НомерЗаписиЛН = XMLЗначение(Тип("Число"), УзелНомерЗаписиЛН.Значение);
		КонецЕсли;
		
		УзелНомерЛН = НайтиУзел(УзелЗапись, "LN_CODE");
		Если УзелНомерЛН = Неопределено Тогда
			УзелНомерЛН = НайтиУзел(УзелЗапись, "lnCode");
		КонецЕсли;
		Если УзелНомерЛН <> Неопределено Тогда
			РезультатОбработки.НомерЛН = XMLЗначение(Тип("Строка"), УзелНомерЛН.Значение);
			РезультатОбработки.НомерЛН = СокрЛП(РезультатОбработки.НомерЛН);
		КонецЕсли;
		
		Если РезультатОбработки.СтатусОбработки = 1 Тогда
			УзелХешЛН = НайтиУзел(УзелЗапись, "LN_HASH");
			Если УзелХешЛН = Неопределено Тогда
				УзелХешЛН = НайтиУзел(УзелЗапись, "lnHash");
			КонецЕсли;
			Если УзелХешЛН <> Неопределено Тогда
				РезультатОбработки.ХешЛН = XMLЗначение(Тип("Строка"), УзелХешЛН.Значение);
				РезультатОбработки.ХешЛН = СокрЛП(РезультатОбработки.ХешЛН);
			КонецЕсли;
			
			УзелКодСостоянияЛН = НайтиУзел(УзелЗапись, "LN_STATE");
			Если УзелКодСостоянияЛН = Неопределено Тогда
				УзелКодСостоянияЛН = НайтиУзел(УзелЗапись, "lnState");
			КонецЕсли;
			Если УзелКодСостоянияЛН <> Неопределено Тогда
				РезультатОбработки.КодСостоянияЛН = XMLЗначение(Тип("Строка"), УзелКодСостоянияЛН.Значение);
				РезультатОбработки.КодСостоянияЛН = СокрЛП(РезультатОбработки.КодСостоянияЛН);
			КонецЕсли;
			
		Иначе
			УзелОшибкиОбработки = НайтиУзел(УзелЗапись, "ERRORS");
			Если УзелОшибкиОбработки = Неопределено Тогда
				УзелОшибкиОбработки = НайтиУзел(УзелЗапись, "errors");
			КонецЕсли;
			Если УзелОшибкиОбработки <> Неопределено Тогда
				УзлыОшибкаОбработки = НайтиУзел(УзелОшибкиОбработки, "ERROR", Истина);
				Если УзлыОшибкаОбработки.Количество() = 0 Тогда
					УзлыОшибкаОбработки = НайтиУзел(УзелОшибкиОбработки, "error", Истина);
				КонецЕсли;
				Для каждого УзелОшибкаОбработки Из УзлыОшибкаОбработки Цикл
					ОшибкаОбработки = Новый Структура;
					ОшибкаОбработки.Вставить("КодОшибки", 	"");
					ОшибкаОбработки.Вставить("ТекстОшибки", "");
					
					УзелКодОшибки = НайтиУзел(УзелОшибкаОбработки, "ERR_CODE");
					Если УзелКодОшибки = Неопределено Тогда
						УзелКодОшибки = НайтиУзел(УзелОшибкаОбработки, "errCode");
					КонецЕсли;
					Если УзелКодОшибки <> Неопределено Тогда
						ОшибкаОбработки.КодОшибки = XMLЗначение(Тип("Строка"), УзелКодОшибки.Значение);
						ОшибкаОбработки.КодОшибки = СокрЛП(ОшибкаОбработки.КодОшибки);
					КонецЕсли;
					
					УзелТекстОшибки = НайтиУзел(УзелОшибкаОбработки, "ERR_MESS");
					Если УзелТекстОшибки = Неопределено Тогда
						УзелТекстОшибки = НайтиУзел(УзелОшибкаОбработки, "errMess");
					КонецЕсли;
					Если УзелТекстОшибки <> Неопределено Тогда
						ОшибкаОбработки.ТекстОшибки = XMLЗначение(Тип("Строка"), УзелТекстОшибки.Значение);
						ОшибкаОбработки.ТекстОшибки = СокрЛП(ОшибкаОбработки.ТекстОшибки);
					КонецЕсли;
					
					РезультатОбработки.ОшибкиОбработки.Добавить(ОшибкаОбработки);
				КонецЦикла;
			КонецЕсли;
		КонецЕсли;
		
		Результат.РезультатыОбработки.Добавить(РезультатОбработки);
	КонецЦикла;
	
	Возврат Результат;
	
КонецФункции

Функция СтатусОтправкиРеестраЭЛН(РезультатыОперации)
	
	Результат = Новый Структура;
	Результат.Вставить("СтатусОтправки", 	Неопределено);
	Результат.Вставить("ЦветСтатуса", 		Неопределено);
	Результат.Вставить("СтатусОтчета", 		"");
	
	КоличествоСданных 		= 0;
	КоличествоОшибок 		= 0;
	КоличествоСданныхРанее 	= 0;
	Для каждого РезультатОбработки Из РезультатыОперации.РезультатыОбработки Цикл
		Если РезультатОбработки.СтатусОбработки = 1 Тогда
			КоличествоСданных = КоличествоСданных + 1;
			
		ИначеЕсли РезультатОбработки.ОшибкиОбработки.Количество() = 1
			И СтрНайти(РезультатОбработки.ОшибкиОбработки[0].ТекстОшибки,
			"Направленные данные страхователя уже присутствуют в системе") > 0 Тогда
			
			КоличествоСданныхРанее = КоличествоСданныхРанее + 1;
			
		Иначе
			КоличествоОшибок = КоличествоОшибок + 1;
		КонецЕсли;
	КонецЦикла;
	
	ЭЛНСданыРанее = "";
	Если КоличествоСданныхРанее > 0 Тогда
		ЭЛНСданРанее = НСтр("ru = '%1 ЭЛН был сдан ранее'");
		ЭЛНСданыРанее = НСтр("ru = '%1 ЭЛН были сданы ранее'");
		ЭЛНСданыРанее = ПростоеСклонение(КоличествоСданныхРанее, ЭЛНСданРанее, ЭЛНСданыРанее, ЭЛНСданыРанее);
		КоличествоСданныхРанееТекстом = Формат(КоличествоСданныхРанее, "ЧДЦ=; ЧН=; ЧГ=");
		ЭЛНСданыРанее = ". " + СтрШаблон(
			ЭЛНСданыРанее,
			КоличествоСданныхРанееТекстом);
	КонецЕсли;
	
	Если КоличествоСданных > 0 Тогда
		Если КоличествоОшибок > 0 Тогда
			ЭЛНСдан = НСтр("ru = '%1 ЭЛН сдан'");
			ЭЛНСданы = НСтр("ru = '%1 ЭЛН сданы'");
			ЭЛНСданы = ПростоеСклонение(КоличествоСданных, ЭЛНСдан, ЭЛНСданы, ЭЛНСданы);
			КоличествоСданныхТекстом = Формат(КоличествоСданных, "ЧДЦ=; ЧН=; ЧГ=");
			ЭЛНСданы = СтрШаблон(
				ЭЛНСданы,
				КоличествоСданныхТекстом);
			
			ЭЛННеПринят = НСтр("ru = '%1 ЭЛН не принят, обнаружены ошибки'");
			ЭЛННеПриняты = НСтр("ru = '%1 ЭЛН не приняты, обнаружены ошибки'");
			ЭЛННеПриняты = ПростоеСклонение(КоличествоОшибок, ЭЛННеПринят, ЭЛННеПриняты, ЭЛННеПриняты);
			КоличествоОшибокТекстом = Формат(КоличествоОшибок, "ЧДЦ=; ЧН=; ЧГ=");
			ЭЛННеПриняты = ". " + СтрШаблон(
				ЭЛННеПриняты,
				КоличествоОшибокТекстом);
			
			Результат.СтатусОтправки 	= Перечисления.СтатусыОтправки.ПринятЕстьОшибки;
			Результат.ЦветСтатуса 		= ЦветаСтиля.ЦветОшибкиВПротоколеБРО;
			Результат.СтатусОтчета 		= ЭЛНСданы + ЭЛНСданыРанее + ЭЛННеПриняты;
			
		Иначе
			Результат.СтатусОтправки 	= Перечисления.СтатусыОтправки.Сдан;
			Результат.ЦветСтатуса 		= ЦветаСтиля.ЦветПоложительногоПротокола;
			Результат.СтатусОтчета 		= НСтр("ru = 'Сдан, ошибок не обнаружено'") + ЭЛНСданыРанее;
		КонецЕсли;
		
	Иначе
		Результат.СтатусОтправки 	= Перечисления.СтатусыОтправки.НеПринят;
		Результат.ЦветСтатуса 		= ЦветаСтиля.ЦветОшибкиВПротоколеБРО;
		Результат.СтатусОтчета 		= НСтр("ru = 'Не принят, обнаружены ошибки'") + ЭЛНСданыРанее;
	КонецЕсли;
	
	Возврат Результат;
	
КонецФункции

Функция ГенерироватьHTMLПротоколаОбработкиРеестраЭЛН(
		ОтчетСсылка,
		ОрганизацияСсылка,
		РезультатыОперации,
		СтатусОперации)
	
	ТабДок = Новый ТабличныйДокумент;
	
	Шаблон = ПолучитьМакет("ФССЭЛНШаблонДляHTML");
	
	СекцияШапка = Шаблон.ПолучитьОбласть("Шапка");
	
	НаименованиеПротокола = ?(СтатусОперации.СтатусОтправки = Перечисления.СтатусыОтправки.Сдан,
		НСтр("ru = 'Протокол о сдаче'"), ?(СтатусОперации.СтатусОтправки = Перечисления.СтатусыОтправки.НеПринят,
		НСтр("ru = 'Протокол ошибок'"), НСтр("ru = 'Протокол о частичной сдаче'")));
	
	СвойстваОрганизации = РегламентированнаяОтчетностьВызовСервера.ПолучитьСведенияОбОрганизации(
		ОрганизацияСсылка,,
		"НаимЮЛПол, ИННЮЛ");
	ПолноеНаименованиеОрганизации 	= СвойстваОрганизации.НаимЮЛПол;
	ИННОрганизации 					= СвойстваОрганизации.ИННЮЛ;
	
	СведенияПоОбъекту = СведенияПоОтправляемымОбъектам(ОтчетСсылка);
	
	Если РезультатыОперации.СтатусЗапроса = 1 Тогда
		СтатусОбработкиТекстом = НСтр("ru = 'Реестр ЭЛН успешно загружен'");
	ИначеЕсли РезультатыОперации.СтатусЗапроса = 2 Тогда
		СтатусОбработкиТекстом = НСтр("ru = 'Реестр ЭЛН загружен с ошибками'");
	ИначеЕсли РезультатыОперации.СтатусЗапроса = 0 Тогда
		СтатусОбработкиТекстом = НСтр("ru = 'Реестр ЭЛН не загружен'");
	Иначе
		СтатусОбработкиТекстом = Строка(РезультатыОперации.СтатусЗапроса);
	КонецЕсли;
	
	ДатаСеанса = ТекущаяДатаСеанса();
	
	СекцияШапка.Параметры.НаименованиеПротокола 	= НаименованиеПротокола;
	СекцияШапка.Параметры.Организация 				= ПолноеНаименованиеОрганизации;
	СекцияШапка.Параметры.ИНН 						= НСтр("ru = 'ИНН'") + " " + ИННОрганизации;
	СекцияШапка.Параметры.Получатель 				= СведенияПоОбъекту.ПредставлениеКонтролирующегоОргана;
	СекцияШапка.Параметры.Отчет 					= СведенияПоОбъекту.Наименование;
	СекцияШапка.Параметры.СтатусОтчета 				= СтатусОперации.СтатусОтчета;
	СекцияШапка.Области.СтатусОтчета.ЦветТекста 	= СтатусОперации.ЦветСтатуса;
	СекцияШапка.Параметры.ИдентификаторЗапроса 		= РезультатыОперации.ИдентификаторЗапроса;
	СекцияШапка.Параметры.СтатусОбработки 			= СтатусОбработкиТекстом;
	СекцияШапка.Параметры.ДатаПротокола 			= Формат(ДатаСеанса, "ДФ='дд.ММ.гггг ЧЧ:мм:сс'");
	
	ТабДок.Вывести(СекцияШапка);
	
	Если ЗначениеЗаполнено(РезультатыОперации.СообщениеОбОшибке) И ВРег(РезультатыОперации.СообщениеОбОшибке) <> "OK"
		И ВРег(РезультатыОперации.СообщениеОбОшибке) <> "ОК" Тогда
		
		СекцияПротоколОшибок = Шаблон.ПолучитьОбласть("ПротоколОшибок");
		СекцияПротоколОшибок.Параметры.СообщениеОбОшибке = РезультатыОперации.СообщениеОбОшибке;
		ТабДок.Вывести(СекцияПротоколОшибок);
	КонецЕсли;
	
	ВывестиПримечаниеКСданным = Ложь;
	Если РезультатыОперации.РезультатыОбработки.Количество() > 0 Тогда
		СекцияРезультатыОбработки = Шаблон.ПолучитьОбласть("РезультатыОбработки");
		ТабДок.Вывести(СекцияРезультатыОбработки);
		
		Для ИндексРезультата = 0 По РезультатыОперации.РезультатыОбработки.Количество() - 1 Цикл
			РезультатОбработки = РезультатыОперации.РезультатыОбработки[ИндексРезультата];
			
			СекцияСтрокаРезультатов = Шаблон.ПолучитьОбласть("СтрокаРезультатов");
			ЛинияГраницыСверху = СекцияСтрокаРезультатов.Области.ПоляСтрокиРезультатов.ГраницаСверху;
			ЛинияГраницыСверху = Новый Линия(
				ЛинияГраницыСверху.ТипЛинии,
				?(ИндексРезультата = 0, 2, 1),
				ЛинияГраницыСверху.Отступ);
			ЛинияГраницыСнизу = СекцияСтрокаРезультатов.Области.ПоляСтрокиРезультатов.ГраницаСнизу;
			ЛинияГраницыСнизу = Новый Линия(
				ЛинияГраницыСнизу.ТипЛинии,
				?(ИндексРезультата = РезультатыОперации.РезультатыОбработки.Количество() - 1
				И РезультатОбработки.ОшибкиОбработки.Количество() <= 1, 2,
				?(РезультатОбработки.ОшибкиОбработки.Количество() > 1, 0, 1)),
				ЛинияГраницыСнизу.Отступ);
			СекцияСтрокаРезультатов.Области.ПоляСтрокиРезультатов.ГраницаСверху = ЛинияГраницыСверху;
			СекцияСтрокаРезультатов.Области.ПоляСтрокиРезультатов.ГраницаСнизу = ЛинияГраницыСнизу;
			
			ЛинияГраницыСверху = СекцияСтрокаРезультатов.Области.ПоляСтрокиРезультатовКолонкиОшибки.ГраницаСверху;
			ЛинияГраницыСверху = Новый Линия(
				ЛинияГраницыСверху.ТипЛинии,
				?(ИндексРезультата = 0, 2, 1),
				ЛинияГраницыСверху.Отступ);
			ЛинияГраницыСнизу = СекцияСтрокаРезультатов.Области.ПоляСтрокиРезультатовКолонкиОшибки.ГраницаСнизу;
			ЛинияГраницыСнизу = Новый Линия(
				ЛинияГраницыСнизу.ТипЛинии,
				?(ИндексРезультата = РезультатыОперации.РезультатыОбработки.Количество() - 1
				И РезультатОбработки.ОшибкиОбработки.Количество() <= 1, 2, 1),
				ЛинияГраницыСнизу.Отступ);
			СекцияСтрокаРезультатов.Области.ПоляСтрокиРезультатовКолонкиОшибки.ГраницаСверху = ЛинияГраницыСверху;
			СекцияСтрокаРезультатов.Области.ПоляСтрокиРезультатовКолонкиОшибки.ГраницаСнизу = ЛинияГраницыСнизу;
			
			Если РезультатОбработки.СтатусОбработки = 1 Тогда
				ВывестиПримечаниеКСданным = Истина;
			КонецЕсли;
			
			НомерЗаписиЛНТекстом = Формат(РезультатОбработки.НомерЗаписиЛН, "ЧДЦ=; ЧН=; ЧГ=");
			СтатусОбработкиТекстом = ?(РезультатОбработки.СтатусОбработки = 1, НСтр("ru = 'Сдано'") + "*",
				?(РезультатОбработки.СтатусОбработки = 0, НСтр("ru = 'Ошибка'"), Строка(РезультатОбработки.СтатусОбработки)));
			Если РезультатОбработки.КодСостоянияЛН = "010" Тогда
				КодСостоянияЛНТекстом = НСтр("ru = 'Открыт'");
			ИначеЕсли РезультатОбработки.КодСостоянияЛН = "020" Тогда
				КодСостоянияЛНТекстом = НСтр("ru = 'Продлен'");
			ИначеЕсли РезультатОбработки.КодСостоянияЛН = "030" Тогда
				КодСостоянияЛНТекстом = НСтр("ru = 'Закрыт'");
			ИначеЕсли РезультатОбработки.КодСостоянияЛН = "040" Тогда
				КодСостоянияЛНТекстом = НСтр("ru = 'Направление на МСЭ'");
			ИначеЕсли РезультатОбработки.КодСостоянияЛН = "050" Тогда
				КодСостоянияЛНТекстом = НСтр("ru = 'Дополнен данными МСЭ'");
			ИначеЕсли РезультатОбработки.КодСостоянияЛН = "060" Тогда
				КодСостоянияЛНТекстом = НСтр("ru = 'Заполнен Страхователем'");
			ИначеЕсли РезультатОбработки.КодСостоянияЛН = "070" Тогда
				КодСостоянияЛНТекстом = НСтр("ru = 'Пособие начислено Страховщиком (ПВСО)'");
			ИначеЕсли РезультатОбработки.КодСостоянияЛН = "080" Тогда
				КодСостоянияЛНТекстом = НСтр("ru = 'Пособие выплачено'");
			ИначеЕсли РезультатОбработки.КодСостоянияЛН = "090" Тогда
				КодСостоянияЛНТекстом = НСтр("ru = 'Действия прекращены'");
			Иначе
				КодСостоянияЛНТекстом = Строка(РезультатОбработки.КодСостоянияЛН);
			КонецЕсли;
			
			КодПервойОшибки = ?(РезультатОбработки.ОшибкиОбработки.Количество() > 0,
				РезультатОбработки.ОшибкиОбработки[0].КодОшибки, "");
			ТекстПервойОшибки = ?(РезультатОбработки.ОшибкиОбработки.Количество() > 0,
				РезультатОбработки.ОшибкиОбработки[0].ТекстОшибки, "");
			
			СекцияСтрокаРезультатов.Параметры.НомерЗаписиЛН 	= НомерЗаписиЛНТекстом;
			СекцияСтрокаРезультатов.Параметры.НомерЛН 			= РезультатОбработки.НомерЛН;
			СекцияСтрокаРезультатов.Параметры.СтатусОбработки 	= СтатусОбработкиТекстом;
			СекцияСтрокаРезультатов.Параметры.КодСостоянияЛН 	= КодСостоянияЛНТекстом;
			СекцияСтрокаРезультатов.Параметры.ХешЛН 			= РезультатОбработки.ХешЛН;
			СекцияСтрокаРезультатов.Параметры.КодОшибки 		= КодПервойОшибки;
			СекцияСтрокаРезультатов.Параметры.ТекстОшибки 		= ТекстПервойОшибки;
			
			ТабДок.Вывести(СекцияСтрокаРезультатов);
			
			Для ИндексОшибки = 1 По РезультатОбработки.ОшибкиОбработки.Количество() - 1 Цикл
				ОшибкаОбработки = РезультатОбработки.ОшибкиОбработки[ИндексОшибки];
				
				СекцияСтрокаРезультатов = Шаблон.ПолучитьОбласть("СтрокаРезультатов");
				ЛинияГраницыСверху = СекцияСтрокаРезультатов.Области.ПоляСтрокиРезультатов.ГраницаСверху;
				ЛинияГраницыСверху = Новый Линия(
					ЛинияГраницыСверху.ТипЛинии,
					0,
					ЛинияГраницыСверху.Отступ);
				ЛинияГраницыСнизу = СекцияСтрокаРезультатов.Области.ПоляСтрокиРезультатов.ГраницаСнизу;
				ЛинияГраницыСнизу = Новый Линия(
					ЛинияГраницыСнизу.ТипЛинии,
					?(ИндексОшибки = РезультатОбработки.ОшибкиОбработки.Количество() - 1,
					?(ИндексРезультата = РезультатыОперации.РезультатыОбработки.Количество() - 1, 2, 1), 0),
					ЛинияГраницыСнизу.Отступ);
				СекцияСтрокаРезультатов.Области.ПоляСтрокиРезультатов.ГраницаСверху = ЛинияГраницыСверху;
				СекцияСтрокаРезультатов.Области.ПоляСтрокиРезультатов.ГраницаСнизу = ЛинияГраницыСнизу;
				
				ЛинияГраницыСверху = СекцияСтрокаРезультатов.Области.ПоляСтрокиРезультатовКолонкиОшибки.ГраницаСверху;
				ЛинияГраницыСверху = Новый Линия(
					ЛинияГраницыСверху.ТипЛинии,
					0,
					ЛинияГраницыСверху.Отступ);
				ЛинияГраницыСнизу = СекцияСтрокаРезультатов.Области.ПоляСтрокиРезультатовКолонкиОшибки.ГраницаСнизу;
				ЛинияГраницыСнизу = Новый Линия(
					ЛинияГраницыСнизу.ТипЛинии,
					?(ИндексРезультата = РезультатыОперации.РезультатыОбработки.Количество()
					И ИндексОшибки = РезультатОбработки.ОшибкиОбработки.Количество() - 1, 2, 1),
					ЛинияГраницыСнизу.Отступ);
				СекцияСтрокаРезультатов.Области.ПоляСтрокиРезультатовКолонкиОшибки.ГраницаСверху = ЛинияГраницыСверху;
				СекцияСтрокаРезультатов.Области.ПоляСтрокиРезультатовКолонкиОшибки.ГраницаСнизу = ЛинияГраницыСнизу;
				
				СекцияСтрокаРезультатов.Параметры.НомерЗаписиЛН 	= "";
				СекцияСтрокаРезультатов.Параметры.НомерЛН 			= "";
				СекцияСтрокаРезультатов.Параметры.СтатусОбработки 	= "";
				СекцияСтрокаРезультатов.Параметры.КодСостоянияЛН 	= "";
				СекцияСтрокаРезультатов.Параметры.ХешЛН 			= "";
				СекцияСтрокаРезультатов.Параметры.КодОшибки 		= ОшибкаОбработки.КодОшибки;
				СекцияСтрокаРезультатов.Параметры.ТекстОшибки 		= ОшибкаОбработки.ТекстОшибки;
				
				ТабДок.Вывести(СекцияСтрокаРезультатов);
			КонецЦикла;
		КонецЦикла;
	КонецЕсли;
	
	Если ВывестиПримечаниеКСданным Тогда
		СекцияПримечаниеКСданным = Шаблон.ПолучитьОбласть("ПримечаниеКСданным");
		ТабДок.Вывести(СекцияПримечаниеКСданным);
	КонецЕсли;
	
	СекцияПодвал = Шаблон.ПолучитьОбласть("Подвал");
	ТабДок.Вывести(СекцияПодвал);
	
	ИмяВременногоФайла = ПолучитьИмяВременногоФайла();
	ТабДок.Записать(ИмяВременногоФайла, ТипФайлаТабличногоДокумента.HTML);
	ТекстHTML = Новый ТекстовыйДокумент;
	ТекстHTML.Прочитать(ИмяВременногоФайла);
	ТекстHTML = ТекстHTML.ПолучитьТекст();
	
	Возврат ТекстHTML;
	
КонецФункции

#КонецОбласти

#Область ОтправкаПодтвержденияВидаДеятельностиФСС

Функция СохранитьПротоколПриОперацииСОКВЭД(
		ОтправкаСсылка,
		ОтчетСсылка,
		ОрганизацияСсылка,
		ПараметрыПротокола) Экспорт
	
	ПараметрыВызова = Новый Структура;
	ПараметрыВызова.Вставить("АдресРасшифрованногоОтветаSOAP", 	Неопределено);
	ПараметрыВызова.Вставить("АдресЗашифрованногоОтветаSOAP", 	Неопределено);
	ПараметрыВызова.Вставить("ИдентификаторОтправкиНаСервере", 	"");
	Если ПараметрыПротокола <> Неопределено Тогда
		ЗаполнитьЗначенияСвойств(ПараметрыВызова, ПараметрыПротокола);
	КонецЕсли;
	АдресРасшифрованногоОтветаSOAP 	= ПараметрыВызова.АдресРасшифрованногоОтветаSOAP;
	АдресЗашифрованногоОтветаSOAP 	= ПараметрыВызова.АдресЗашифрованногоОтветаSOAP;
	ИдентификаторОтправкиНаСервере 	= ПараметрыВызова.ИдентификаторОтправкиНаСервере;
	
	ДатаСеанса = ТекущаяДатаСеанса();
	
	ТекстРезультата = ПолучитьИзВременногоХранилища(АдресРасшифрованногоОтветаSOAP);
	УдалитьИзВременногоХранилища(АдресРасшифрованногоОтветаSOAP);
	ТекстПакетаРезультата = ПолучитьИзВременногоХранилища(АдресЗашифрованногоОтветаSOAP);
	УдалитьИзВременногоХранилища(АдресЗашифрованногоОтветаSOAP);
	
	ИмяФайлаРезультата = ПолучитьИмяВременногоФайла();
	ФайлРезультата = Новый ЗаписьТекста(ИмяФайлаРезультата, "utf-8");
	ФайлРезультата.Записать(ТекстРезультата);
	ФайлРезультата.Закрыть();
	ДвоичныеДанныеРезультата = Новый ДвоичныеДанные(ИмяФайлаРезультата);
	
	ИмяФайлаПакетаРезультата = ПолучитьИмяВременногоФайла();
	ФайлПакетаРезультата = Новый ЗаписьТекста(ИмяФайлаПакетаРезультата, "utf-8");
	ФайлПакетаРезультата.Записать(ТекстПакетаРезультата);
	ФайлПакетаРезультата.Закрыть();
	ДвоичныеДанныеПакетаРезультата = Новый ДвоичныеДанные(ИмяФайлаПакетаРезультата);
	
	РезультатыОперации = РезультатыОперацииСОКВЭД(ТекстРезультата);
	СтатусОперации = СтатусОперацииСОКВЭД(РезультатыОперации);
	
	ТекстHTML = ГенерироватьHTMLПротоколаОбработкиОперацииСОКВЭД(
		ОтчетСсылка,
		ОрганизацияСсылка,
		РезультатыОперации,
		СтатусОперации,
		ИдентификаторОтправкиНаСервере);
	
	РезультатОтправки = Новый Структура;
	РезультатОтправки.Вставить("ДатаПолученияРезультата", 	ДатаСеанса);
	РезультатОтправки.Вставить("СтатусОтправки", 			СтатусОперации.СтатусОтправки);
	РезультатОтправки.Вставить("Протокол", 					ТекстHTML);
	РезультатОтправки.Вставить("Квитанция", 				ДвоичныеДанныеРезультата);
	РезультатОтправки.Вставить("ПакетКвитанции", 			ДвоичныеДанныеПакетаРезультата);
	
	СохранитьРезультатОтправкиФСС(ОтправкаСсылка, РезультатОтправки);
	
	ОперацииСФайламиЭДКО.УдалитьВременныйФайл(ИмяФайлаРезультата);
	
	Результат = Новый Структура;
	Результат.Вставить("СтатусОтправки", СтатусОперации.СтатусОтправки);
	Результат.Вставить("Рассматривается", СтатусОперации.Рассматривается);
	Возврат Результат;
	
КонецФункции

Функция ОбработатьРезультатОтправкиПриОперацииСОКВЭД(
		ОтчетСсылка,
		ОрганизацияСсылка,
		ЭтоЭлектроннаяПодписьВМоделиСервиса,
		КороткоеИмяФайлаПакета,
		СтрокаBase64ФайлаПакета,
		ПараметрыРезультата) Экспорт
	
	ПараметрыВызова = Новый Структура;
	ПараметрыВызова.Вставить("АдресРасшифрованногоОтветаSOAP", 	Неопределено);
	ПараметрыВызова.Вставить("АдресЗашифрованногоОтветаSOAP", 	Неопределено);
	ПараметрыВызова.Вставить("ПоляДляЗаписиОтправки", 			Неопределено);
	ПараметрыВызова.Вставить("ПодписанныйSOAP64", 				Неопределено);
	Если ПараметрыРезультата <> Неопределено Тогда
		ЗаполнитьЗначенияСвойств(ПараметрыВызова, ПараметрыРезультата);
	КонецЕсли;
	АдресРасшифрованногоОтветаSOAP 	= ПараметрыВызова.АдресРасшифрованногоОтветаSOAP;
	АдресЗашифрованногоОтветаSOAP 	= ПараметрыВызова.АдресЗашифрованногоОтветаSOAP;
	ПоляДляЗаписиОтправки 			= ПараметрыВызова.ПоляДляЗаписиОтправки;
	ПодписанныйSOAP64 				= ПараметрыВызова.ПодписанныйSOAP64;
	
	ТекстРезультата = ПолучитьИзВременногоХранилища(АдресРасшифрованногоОтветаSOAP);
	УдалитьИзВременногоХранилища(АдресРасшифрованногоОтветаSOAP);
	
	ИмяФайлаРезультата = ПолучитьИмяВременногоФайла();
	ФайлРезультата = Новый ЗаписьТекста(ИмяФайлаРезультата, "utf-8");
	ФайлРезультата.Записать(ТекстРезультата);
	ФайлРезультата.Закрыть();
	ДвоичныеДанныеРезультата = Новый ДвоичныеДанные(ИмяФайлаРезультата);
	
	ТекстПакетаРезультата = ПолучитьИзВременногоХранилища(АдресЗашифрованногоОтветаSOAP);
	ИмяФайлаПакетаРезультата = ПолучитьИмяВременногоФайла();
	ФайлПакетаРезультата = Новый ЗаписьТекста(ИмяФайлаПакетаРезультата, "utf-8");
	ФайлПакетаРезультата.Записать(ТекстПакетаРезультата);
	ФайлПакетаРезультата.Закрыть();
	ДвоичныеДанныеПакетаРезультата = Новый ДвоичныеДанные(ИмяФайлаПакетаРезультата);
	
	РезультатыОперации = РезультатыОперацииСОКВЭД(ТекстРезультата);
	СтатусОперации = СтатусОперацииСОКВЭД(РезультатыОперации);
	
	ТекстHTML = ГенерироватьHTMLПротоколаОбработкиОперацииСОКВЭД(
		ОтчетСсылка,
		ОрганизацияСсылка,
		РезультатыОперации,
		СтатусОперации);
	
	НастройкиОтправки = Новый Структура;
	НастройкиОтправки.Вставить("ОтчетСсылка", 				ОтчетСсылка);
	НастройкиОтправки.Вставить("ПоляДляЗаписиОтправки", 	ПоляДляЗаписиОтправки);
	НастройкиОтправки.Вставить("СтатусОтправки", 			СтатусОперации.СтатусОтправки);
	НастройкиОтправки.Вставить("Протокол", 					ТекстHTML);
	НастройкиОтправки.Вставить("Квитанция", 				ДвоичныеДанныеРезультата);
	НастройкиОтправки.Вставить("ПодписанныйПакет", 			ПодписанныйSOAP64);
	НастройкиОтправки.Вставить("ПакетКвитанции", 			ДвоичныеДанныеПакетаРезультата);
	НоваяОтправкаСсылка = ДобавитьОтправкуФСС(
		ЭтоЭлектроннаяПодписьВМоделиСервиса,
		КороткоеИмяФайлаПакета,
		СтрокаBase64ФайлаПакета,
		РезультатыОперации.ИдентификаторЗапроса,
		НастройкиОтправки);
	
	ОперацииСФайламиЭДКО.УдалитьВременныйФайл(ИмяФайлаРезультата);
	УдалитьИзВременногоХранилища(АдресЗашифрованногоОтветаSOAP);
	Если ЭтоЭлектроннаяПодписьВМоделиСервиса Тогда
		УдалитьИзВременногоХранилища(ПодписанныйSOAP64);
	КонецЕсли;
	
	Результат = Новый Структура;
	Результат.Вставить("НоваяОтправкаСсылка", 	НоваяОтправкаСсылка);
	Результат.Вставить("СтатусОтправки", 		СтатусОперации.СтатусОтправки);
	Результат.Вставить("Рассматривается", 		СтатусОперации.Рассматривается);
	Возврат Результат;
	
КонецФункции

Функция РезультатыОперацииСОКВЭД(ТекстРезультата)
	
	Результат = Новый Структура;
	Результат.Вставить("ТипРезультата", 		"");
	Результат.Вставить("ИдентификаторЗапроса", 	"");
	Результат.Вставить("СтатусЗапроса", 		"");
	Результат.Вставить("ДатаЗапроса", 			Неопределено);
	Результат.Вставить("СообщениеОбОшибке", 	"");
	Результат.Вставить("РезультатыВЦелом", 		Новый Массив);
	Результат.Вставить("ИсторияСменыСтатусов", 	Новый Массив);
	
	ДеревоXML = ЗагрузитьXMLВДеревоЗначений(Неопределено, ТекстРезультата);
	Если ДеревоXML = Неопределено Тогда
		Возврат Результат;
	КонецЕсли;
	
	УзелКонверт = НайтиУзел(ДеревоXML, "Envelope");
	Если УзелКонверт = Неопределено Тогда
		Возврат Результат;
	КонецЕсли;
	
	УзелТело = НайтиУзел(УзелКонверт, "Body");
	Если УзелТело = Неопределено Тогда
		Возврат Результат;
	КонецЕсли;
	
	УзелОтвета = НайтиУзел(УзелТело, "OKVEDConfirmationResponse");
	Если УзелОтвета = Неопределено Тогда
		УзелОтвета = НайтиУзел(УзелТело, "getResultByIdResponse");
		
		Если УзелОтвета = Неопределено Тогда
			Если УзелТело.Строки.Количество() >= 1 Тогда
				УзелОтвета = УзелТело.Строки[0];
			Иначе
				Возврат Результат;
			КонецЕсли;
			
		Иначе
			Результат.ТипРезультата = "РезультатОбработки";
		КонецЕсли;
		
	Иначе
		Результат.ТипРезультата = "РезультатОтправки";
	КонецЕсли;
	
	УзелИдентификаторЗапроса = НайтиУзел(УзелОтвета, "transportId");
	Если УзелИдентификаторЗапроса = Неопределено Тогда
		УзелИдентификаторЗапроса = НайтиУзел(УзелОтвета, "requestId");
	КонецЕсли;
	Если УзелИдентификаторЗапроса <> Неопределено Тогда
		Результат.ИдентификаторЗапроса = XMLЗначение(Тип("Строка"), УзелИдентификаторЗапроса.Значение);
	КонецЕсли;
	
	УзелСтатусЗапроса = НайтиУзел(УзелОтвета, "requestStatus");
	Если УзелСтатусЗапроса <> Неопределено Тогда
		Результат.СтатусЗапроса = XMLЗначение(Тип("Строка"), УзелСтатусЗапроса.Значение);
		Результат.СтатусЗапроса = СокрЛП(Результат.СтатусЗапроса);
		Результат.СтатусЗапроса = ВРег(Результат.СтатусЗапроса);
	КонецЕсли;
	
	УзелДатаЗапроса = НайтиУзел(УзелОтвета, "crDate");
	Если УзелДатаЗапроса <> Неопределено И ЗначениеЗаполнено(УзелДатаЗапроса.Значение) Тогда
		Результат.ДатаЗапроса = XMLЗначение(Тип("Дата"), УзелДатаЗапроса.Значение);
	КонецЕсли;
	
	Если Результат.ТипРезультата <> "РезультатОбработки" Тогда
		УзелСообщениеОбОшибке = НайтиУзел(УзелОтвета, "errorMessage");
		Если УзелСообщениеОбОшибке <> Неопределено Тогда
			Результат.СообщениеОбОшибке = XMLЗначение(Тип("Строка"), УзелСообщениеОбОшибке.Значение);
		КонецЕсли;
	КонецЕсли;
	
	Если Результат.ТипРезультата <> "РезультатОтправки" Тогда
		УзелРезультатОбработки = НайтиУзел(УзелОтвета, "processingResult");
		Если УзелРезультатОбработки <> Неопределено Тогда
			УзелРезультатыВЦелом = НайтиУзел(УзелРезультатОбработки, "results");
			Если УзелРезультатыВЦелом <> Неопределено Тогда
				УзлыХранилищаРезультатов = НайтиУзел(УзелРезультатыВЦелом, "storage", Истина);
				Для каждого УзелХранилищаРезультатов Из УзлыХранилищаРезультатов Цикл
					РезультатВЦелом = Новый Структура;
					РезультатВЦелом.Вставить("КодСообщения", 	"");
					РезультатВЦелом.Вставить("ТекстСообщения", 	"");
					
					УзелКодСообщения = НайтиУзел(УзелХранилищаРезультатов, "code");
					Если УзелКодСообщения <> Неопределено Тогда
						РезультатВЦелом.КодСообщения = XMLЗначение(Тип("Строка"), УзелКодСообщения.Значение);
					КонецЕсли;
					
					УзелТекстСообщения = НайтиУзел(УзелХранилищаРезультатов, "message");
					Если УзелТекстСообщения <> Неопределено Тогда
						РезультатВЦелом.ТекстСообщения = XMLЗначение(Тип("Строка"), УзелТекстСообщения.Значение);
					КонецЕсли;
					
					Результат.РезультатыВЦелом.Добавить(РезультатВЦелом);
				КонецЦикла;
			КонецЕсли;
			
			УзлыИсторияСменыСтатусов = НайтиУзел(УзелРезультатОбработки, "claimStatusInfo", Истина);
			Для каждого УзелИсторияСменыСтатусов Из УзлыИсторияСменыСтатусов Цикл
				СтатусЗапроса = Новый Структура;
				СтатусЗапроса.Вставить("СтатусРассмотрения", 	"");
				СтатусЗапроса.Вставить("КомментарийСтатуса", 	"");
				СтатусЗапроса.Вставить("ДатаСтатуса", 			Неопределено);
				
				УзелСтатусРассмотрения = НайтиУзел(УзелИсторияСменыСтатусов, "status");
				Если УзелСтатусРассмотрения <> Неопределено Тогда
					СтатусЗапроса.СтатусРассмотрения = XMLЗначение(Тип("Строка"), УзелСтатусРассмотрения.Значение);
					СтатусЗапроса.СтатусРассмотрения = СокрЛП(СтатусЗапроса.СтатусРассмотрения);
					СтатусЗапроса.СтатусРассмотрения = ВРег(СтатусЗапроса.СтатусРассмотрения);
				КонецЕсли;
				
				УзелКомментарийСтатуса = НайтиУзел(УзелИсторияСменыСтатусов, "reason");
				Если УзелКомментарийСтатуса <> Неопределено Тогда
					СтатусЗапроса.КомментарийСтатуса = XMLЗначение(Тип("Строка"), УзелКомментарийСтатуса.Значение);
				КонецЕсли;
				
				УзелДатаСтатуса = НайтиУзел(УзелИсторияСменыСтатусов, "createDate");
				Если УзелДатаСтатуса <> Неопределено И ЗначениеЗаполнено(УзелДатаСтатуса.Значение) Тогда
					СтатусЗапроса.ДатаСтатуса = XMLЗначение(Тип("Дата"), УзелДатаСтатуса.Значение);
				КонецЕсли;
				
				Результат.ИсторияСменыСтатусов.Добавить(СтатусЗапроса);
			КонецЦикла;
		КонецЕсли;
	КонецЕсли;
	
	Возврат Результат;
	
КонецФункции

Функция СтатусОперацииСОКВЭД(РезультатыОперации)
	
	Результат = Новый Структура;
	Результат.Вставить("СтатусРассмотрения", 	"");
	Результат.Вставить("ДатаСтатуса", 			Неопределено);
	Результат.Вставить("СтатусОтправки", 		Перечисления.СтатусыОтправки.Отправлен);
	Результат.Вставить("ЦветСтатуса", 			ЦветаСтиля.ЦветНезавершившейсяОтправкиБРО);
	Результат.Вставить("СтатусОтчета", 			"");
	Результат.Вставить("Рассматривается", 		Ложь);
	
	Результат.СтатусРассмотрения = "";
	Результат.ДатаСтатуса = Неопределено;
	Для каждого СтатусЗапроса Из РезультатыОперации.ИсторияСменыСтатусов Цикл
		ПриоритетСтатуса = ?(НЕ ЗначениеЗаполнено(Результат.СтатусРассмотрения), 0,
			?(Результат.СтатусРассмотрения = "REGISTERED", 1, 2));
		ПриоритетСтатусаЗапроса = ?(НЕ ЗначениеЗаполнено(СтатусЗапроса.СтатусРассмотрения), 0,
			?(СтатусЗапроса.СтатусРассмотрения = "REGISTERED", 1, 2));
		
		Если ПриоритетСтатусаЗапроса > ПриоритетСтатуса
			ИЛИ ПриоритетСтатусаЗапроса = ПриоритетСтатуса И (НЕ ЗначениеЗаполнено(Результат.ДатаСтатуса)
			ИЛИ ЗначениеЗаполнено(СтатусЗапроса.ДатаСтатуса) И СтатусЗапроса.ДатаСтатуса >= Результат.ДатаСтатуса) Тогда
			
			Результат.СтатусРассмотрения 	= СтатусЗапроса.СтатусРассмотрения;
			Результат.ДатаСтатуса 			= СтатусЗапроса.ДатаСтатуса;
		КонецЕсли;
	КонецЦикла;
	
	Если Результат.СтатусРассмотрения = "CONFIRMED" Тогда
		
		Результат.СтатусОтправки 	= Перечисления.СтатусыОтправки.Сдан;
		Результат.ЦветСтатуса 		= ЦветаСтиля.ЦветПоложительногоПротокола;
		Результат.СтатусОтчета 		= НСтр("ru = 'Сдан, ошибок не обнаружено'");
		
	ИначеЕсли Результат.СтатусРассмотрения = "CANCELED" Тогда
		
		Результат.СтатусОтправки 	= Перечисления.СтатусыОтправки.НеПринят;
		Результат.ЦветСтатуса 		= ЦветаСтиля.ЦветОшибкиВПротоколеБРО;
		Результат.СтатусОтчета 		= НСтр("ru = 'Заявление на подтверждение отменено сотрудником ФСС'");
		
	ИначеЕсли Результат.СтатусРассмотрения = "REJECTED" Тогда
		
		Результат.СтатусОтправки 	= Перечисления.СтатусыОтправки.НеПринят;
		Результат.ЦветСтатуса 		= ЦветаСтиля.ЦветОшибкиВПротоколеБРО;
		Результат.СтатусОтчета 		= НСтр("ru = 'Не подтвержден сотрудником ФСС'");
		
	ИначеЕсли РезультатыОперации.СтатусЗапроса = "PROCESSING_ERROR" Тогда
		Результат.СтатусОтправки 	= Перечисления.СтатусыОтправки.НеПринят;
		Результат.ЦветСтатуса 		= ЦветаСтиля.ЦветОшибкиВПротоколеБРО;
		Результат.СтатусОтчета 		= НСтр("ru = 'Не принят, обнаружены ошибки'");
		
	ИначеЕсли Результат.СтатусРассмотрения = "REGISTERED" ИЛИ Результат.СтатусРассмотрения = "SEND"
		ИЛИ РезультатыОперации.СтатусЗапроса = "PROCESSED" Тогда
		
		Результат.Рассматривается = Истина;
		Результат.СтатусОтчета =
			НСтр("ru = 'Отчет рассматривается ФСС.
					   |Окончательный протокол должен быть сформирован по результатам рассмотрения сотрудником ФСС в срок от 1 дня до 2-х недель.
					   |Для обновления статуса отправки отчета используйте кнопку ""Обновить"" на верхней панели отчета.'");
		
	Иначе
		Результат.СтатусОтчета =
			НСтр("ru = 'Находится в обработке, попробуйте через некоторое время использовать кнопку ""Обновить"" на верхней панели отчета'");
	КонецЕсли;
	
	Возврат Результат;
	
КонецФункции

Функция ГенерироватьHTMLПротоколаОбработкиОперацииСОКВЭД(
		ОтчетСсылка,
		ОрганизацияСсылка,
		РезультатыОперации,
		СтатусОперации,
		ИдентификаторОтправкиНаСервере = Неопределено)
	
	ТабДок = Новый ТабличныйДокумент;
	
	Шаблон = ПолучитьМакет("ФССОКВЭДШаблонДляHTML");
	
	СекцияШапка = Шаблон.ПолучитьОбласть("Шапка");
	
	НаименованиеПротокола = ?(СтатусОперации.СтатусОтправки = Перечисления.СтатусыОтправки.Сдан,
		НСтр("ru = 'Протокол о сдаче'"), ?(СтатусОперации.СтатусОтправки = Перечисления.СтатусыОтправки.НеПринят,
		НСтр("ru = 'Протокол ошибок'"), НСтр("ru = 'Промежуточный протокол'")));
	
	СвойстваОрганизации = РегламентированнаяОтчетностьВызовСервера.ПолучитьСведенияОбОрганизации(
		ОрганизацияСсылка,,
		"НаимЮЛПол, ИННЮЛ");
	ПолноеНаименованиеОрганизации 	= СвойстваОрганизации.НаимЮЛПол;
	ИННОрганизации 					= СвойстваОрганизации.ИННЮЛ;
	
	СведенияПоОбъекту = СведенияПоОтправляемымОбъектам(ОтчетСсылка);
	
	ИдентификаторОтправки = ?(ЗначениеЗаполнено(ИдентификаторОтправкиНаСервере),
		ИдентификаторОтправкиНаСервере, РезультатыОперации.ИдентификаторЗапроса);
	
	Если РезультатыОперации.СтатусЗапроса = "REGISTERED" Тогда
		СтатусЗапросаТекстом = НСтр("ru = 'Сообщение зарегистрировано'");
	ИначеЕсли РезультатыОперации.СтатусЗапроса = "READY_TO_PROCESS" Тогда
		СтатусЗапросаТекстом = НСтр("ru = 'Сообщение готово к обработке'");
	ИначеЕсли РезультатыОперации.СтатусЗапроса = "PROCESSING" Тогда
		СтатусЗапросаТекстом = НСтр("ru = 'Обрабатывается'");
	ИначеЕсли РезультатыОперации.СтатусЗапроса = "PROCESSED" Тогда
		СтатусЗапросаТекстом = НСтр("ru = 'Сообщение успешно обработано'");
	ИначеЕсли РезультатыОперации.СтатусЗапроса = "PROCESSING_ERROR" Тогда
		СтатусЗапросаТекстом = НСтр("ru = 'Ошибка обработки сообщения'");
	Иначе
		СтатусЗапросаТекстом = РезультатыОперации.СтатусЗапроса;
	КонецЕсли;
	
	ДатаЗапросаСВременем = ЗначениеЗаполнено(РезультатыОперации.ДатаЗапроса) И (Час(РезультатыОперации.ДатаЗапроса) <> 0
		ИЛИ Минута(РезультатыОперации.ДатаЗапроса) <> 0 ИЛИ Секунда(РезультатыОперации.ДатаЗапроса) <> 0);
	ДатаЗапросаТекстом = Формат(РезультатыОперации.ДатаЗапроса,
		?(ДатаЗапросаСВременем, "ДФ='дд.ММ.гггг ЧЧ:мм:сс'", "ДФ='дд.ММ.гггг'"));
	
	СекцияШапка.Параметры.НаименованиеПротокола 	= НаименованиеПротокола;
	СекцияШапка.Параметры.Организация 				= ПолноеНаименованиеОрганизации;
	СекцияШапка.Параметры.ИНН 						= НСтр("ru = 'ИНН '") + ИННОрганизации;
	СекцияШапка.Параметры.Получатель 				= СведенияПоОбъекту.ПредставлениеКонтролирующегоОргана;
	СекцияШапка.Параметры.Отчет 					= СведенияПоОбъекту.Наименование;
	СекцияШапка.Параметры.СтатусОтчета 				= СтатусОперации.СтатусОтчета;
	СекцияШапка.Области.СтатусОтчета.ЦветТекста 	= СтатусОперации.ЦветСтатуса;
	СекцияШапка.Параметры.ИдентификаторЗапроса 		= ИдентификаторОтправки;
	СекцияШапка.Параметры.СтатусОбработкиЗапроса 	= СтатусЗапросаТекстом;
	СекцияШапка.Параметры.ДатаОтправки 				= ДатаЗапросаТекстом;
	
	ТабДок.Вывести(СекцияШапка);
	
	Если ЗначениеЗаполнено(РезультатыОперации.СообщениеОбОшибке) Тогда
		СекцияПротоколОшибок = Шаблон.ПолучитьОбласть("ПротоколОшибок");
		СекцияПротоколОшибок.Параметры.СообщениеОбОшибке = РезультатыОперации.СообщениеОбОшибке;
		ТабДок.Вывести(СекцияПротоколОшибок);
		
		Если РезультатыОперации.РезультатыВЦелом.Количество() > 0
			ИЛИ РезультатыОперации.РезультатыВЦелом.Количество() > 0 Тогда
			
			СекцияРазделительПротокола = Шаблон.ПолучитьОбласть("РазделительПротокола");
			ТабДок.Вывести(СекцияРазделительПротокола);
		КонецЕсли;
	КонецЕсли;
	
	Если РезультатыОперации.РезультатыВЦелом.Количество() > 0 Тогда
		СекцияРезультатыОбработки = Шаблон.ПолучитьОбласть("РезультатыОбработки");
		ТабДок.Вывести(СекцияРезультатыОбработки);
		
		Для ИндексРезультата = 0 По РезультатыОперации.РезультатыВЦелом.Количество() - 1 Цикл
			РезультатВЦелом = РезультатыОперации.РезультатыВЦелом[ИндексРезультата];
			
			СекцияСтрокаРезультатов = Шаблон.ПолучитьОбласть("СтрокаРезультатов");
			ЛинияГраницыСверху = СекцияСтрокаРезультатов.Области.ПоляСтрокиРезультатов.ГраницаСверху;
			ЛинияГраницыСверху = Новый Линия(
				ЛинияГраницыСверху.ТипЛинии,
				?(ИндексРезультата = 0, 2, 1),
				ЛинияГраницыСверху.Отступ);
			ЛинияГраницыСнизу = СекцияСтрокаРезультатов.Области.ПоляСтрокиРезультатов.ГраницаСнизу;
			ЛинияГраницыСнизу = Новый Линия(
				ЛинияГраницыСнизу.ТипЛинии,
				?(ИндексРезультата = РезультатыОперации.РезультатыВЦелом.Количество() - 1, 2, 1),
				ЛинияГраницыСнизу.Отступ);
			СекцияСтрокаРезультатов.Области.ПоляСтрокиРезультатов.ГраницаСверху = ЛинияГраницыСверху;
			СекцияСтрокаРезультатов.Области.ПоляСтрокиРезультатов.ГраницаСнизу = ЛинияГраницыСнизу;
			
			СекцияСтрокаРезультатов.Параметры.КодСообщения = РезультатВЦелом.КодСообщения;
			СекцияСтрокаРезультатов.Параметры.ТекстСообщения = РезультатВЦелом.ТекстСообщения;
			
			ТабДок.Вывести(СекцияСтрокаРезультатов);
		КонецЦикла;
		
		Если РезультатыОперации.РезультатыВЦелом.Количество() > 0 Тогда
			СекцияРазделительРезультатов = Шаблон.ПолучитьОбласть("РазделительРезультатов");
			ТабДок.Вывести(СекцияРазделительРезультатов);
		КонецЕсли;
	КонецЕсли;
	
	Если РезультатыОперации.ИсторияСменыСтатусов.Количество() > 0 Тогда
		СекцияШапкаИстории = Шаблон.ПолучитьОбласть("ШапкаИстории");
		ТабДок.Вывести(СекцияШапкаИстории);
		
		Для ИндексСтатуса = 0 По РезультатыОперации.ИсторияСменыСтатусов.Количество() - 1 Цикл
			СтатусЗапроса = РезультатыОперации.ИсторияСменыСтатусов[ИндексСтатуса];
			
			СекцияСтрокаИстории = Шаблон.ПолучитьОбласть("СтрокаИстории");
			ЛинияГраницыСверху = СекцияСтрокаИстории.Области.ПоляСтрокиИстории.ГраницаСверху;
			ЛинияГраницыСверху = Новый Линия(
				ЛинияГраницыСверху.ТипЛинии,
				?(ИндексСтатуса = 0, 2, 1),
				ЛинияГраницыСверху.Отступ);
			ЛинияГраницыСнизу = СекцияСтрокаИстории.Области.ПоляСтрокиИстории.ГраницаСнизу;
			ЛинияГраницыСнизу = Новый Линия(
				ЛинияГраницыСнизу.ТипЛинии,
				?(ИндексСтатуса = РезультатыОперации.ИсторияСменыСтатусов.Количество() - 1, 2, 1),
				ЛинияГраницыСнизу.Отступ);
			СекцияСтрокаИстории.Области.ПоляСтрокиИстории.ГраницаСверху = ЛинияГраницыСверху;
			СекцияСтрокаИстории.Области.ПоляСтрокиИстории.ГраницаСнизу = ЛинияГраницыСнизу;
			
			ДатаСтатусаСВременем = ЗначениеЗаполнено(СтатусЗапроса.ДатаСтатуса) И (Час(СтатусЗапроса.ДатаСтатуса) <> 0
				ИЛИ Минута(СтатусЗапроса.ДатаСтатуса) <> 0 ИЛИ Секунда(СтатусЗапроса.ДатаСтатуса) <> 0);
			ДатаЗапросаТекстом = Формат(СтатусЗапроса.ДатаСтатуса,
				?(ДатаСтатусаСВременем, "ДФ='дд.ММ.гггг ЧЧ:мм:сс'", "ДФ='дд.ММ.гггг'"));
				
			Если СтатусЗапроса.СтатусРассмотрения = "REGISTERED" Тогда
				СтатусРассмотренияТекстом = НСтр("ru = 'Создано заявление'");
			ИначеЕсли СтатусЗапроса.СтатусРассмотрения = "CONFIRMED" Тогда
				СтатусРассмотренияТекстом = НСтр("ru = 'Подтвержден'");
			ИначеЕсли СтатусЗапроса.СтатусРассмотрения = "CANCELED" Тогда
				СтатусРассмотренияТекстом = НСтр("ru = 'Заявление отменено'");
			ИначеЕсли СтатусЗапроса.СтатусРассмотрения = "REJECTED" Тогда
				СтатусРассмотренияТекстом = НСтр("ru = 'Не подтвержден'");
			ИначеЕсли СтатусЗапроса.СтатусРассмотрения = "SEND" Тогда
				СтатусРассмотренияТекстом = НСтр("ru = 'Заявление отправлено'");
			Иначе
				СтатусРассмотренияТекстом = СтатусЗапроса.СтатусРассмотрения;
			КонецЕсли;
			
			СекцияСтрокаИстории.Параметры.ДатаСтатуса 			= ДатаЗапросаТекстом;
			СекцияСтрокаИстории.Параметры.СтатусРассмотрения 	= СтатусРассмотренияТекстом;
			СекцияСтрокаИстории.Параметры.КомментарийСтатуса 	= СтатусЗапроса.КомментарийСтатуса;
			
			ТабДок.Вывести(СекцияСтрокаИстории);
		КонецЦикла;
	КонецЕсли;
	
	СекцияПодвал = Шаблон.ПолучитьОбласть("Подвал");
	ТабДок.Вывести(СекцияПодвал);
	
	ИмяВременногоФайла = ПолучитьИмяВременногоФайла();
	ТабДок.Записать(ИмяВременногоФайла, ТипФайлаТабличногоДокумента.HTML);
	ТекстHTML = Новый ТекстовыйДокумент;
	ТекстHTML.Прочитать(ИмяВременногоФайла);
	ТекстHTML = ТекстHTML.ПолучитьТекст();
	
	Возврат ТекстHTML;
	
КонецФункции

#КонецОбласти

#Область ОтправкаРеестраСтимулирующихВыплатМедицинскимИСоциальнымРаботникам

Функция ОбработатьРезультатОтправкиРеестраСтимулирующихВыплатМедицинскимИСоциальнымРаботникам(
		ОтчетСсылка,
		ОрганизацияСсылка,
		ЭтоЭлектроннаяПодписьВМоделиСервиса,
		КороткоеИмяФайлаПакета,
		СтрокаBase64ФайлаПакета,
		ПараметрыРезультата) Экспорт
	
	ПараметрыВызова = Новый Структура;
	ПараметрыВызова.Вставить("АдресРасшифрованногоОтветаSOAP", 			Неопределено);
	ПараметрыВызова.Вставить("АдресЗашифрованногоОтветаSOAP", 			Неопределено);
	ПараметрыВызова.Вставить("ПоляДляЗаписиОтправки", 					Неопределено);
	ПараметрыВызова.Вставить("СохранятьАдресРасшифрованногоОтветаSOAP", Ложь);
	ПараметрыВызова.Вставить("ПодписанныйSOAP64", 						Неопределено);
	Если ПараметрыРезультата <> Неопределено Тогда
		ЗаполнитьЗначенияСвойств(ПараметрыВызова, ПараметрыРезультата);
	КонецЕсли;
	АдресРасшифрованногоОтветаSOAP 			= ПараметрыВызова.АдресРасшифрованногоОтветаSOAP;
	АдресЗашифрованногоОтветаSOAP 			= ПараметрыВызова.АдресЗашифрованногоОтветаSOAP;
	ПоляДляЗаписиОтправки 					= ПараметрыВызова.ПоляДляЗаписиОтправки;
	СохранятьАдресРасшифрованногоОтветаSOAP = ПараметрыВызова.СохранятьАдресРасшифрованногоОтветаSOAP;
	ПодписанныйSOAP64 						= ПараметрыВызова.ПодписанныйSOAP64;
	
	ТекстРезультата = ПолучитьИзВременногоХранилища(АдресРасшифрованногоОтветаSOAP);
	Если НЕ СохранятьАдресРасшифрованногоОтветаSOAP Тогда
		УдалитьИзВременногоХранилища(АдресРасшифрованногоОтветаSOAP);
	КонецЕсли;
	
	ИмяФайлаРезультата = ПолучитьИмяВременногоФайла();
	ФайлРезультата = Новый ЗаписьТекста(ИмяФайлаРезультата, "utf-8");
	ФайлРезультата.Записать(ТекстРезультата);
	ФайлРезультата.Закрыть();
	ДвоичныеДанныеРезультата = Новый ДвоичныеДанные(ИмяФайлаРезультата);
	
	ТекстПакетаРезультата = ПолучитьИзВременногоХранилища(АдресЗашифрованногоОтветаSOAP);
	ИмяФайлаПакетаРезультата = ПолучитьИмяВременногоФайла();
	ФайлПакетаРезультата = Новый ЗаписьТекста(ИмяФайлаПакетаРезультата, "utf-8");
	ФайлПакетаРезультата.Записать(ТекстПакетаРезультата);
	ФайлПакетаРезультата.Закрыть();
	ДвоичныеДанныеПакетаРезультата = Новый ДвоичныеДанные(ИмяФайлаПакетаРезультата);
	
	РезультатыОперации = РезультатыОтправкиРеестраСтимулирующихВыплатМедицинскимИСоциальнымРаботникам(
		ТекстРезультата,
		ЭтоЭлектроннаяПодписьВМоделиСервиса,
		ПодписанныйSOAP64);
	СтатусОперации = СтатусОтправкиРеестраСтимулирующихВыплатМедицинскимИСоциальнымРаботникам(РезультатыОперации);
	
	ТекстHTML = ГенерироватьHTMLПротоколаОбработкиРеестраСтимулирующихВыплатМедицинскимИСоциальнымРаботникам(
		ОтчетСсылка,
		ОрганизацияСсылка,
		РезультатыОперации,
		СтатусОперации);
	
	НастройкиОтправки = Новый Структура;
	НастройкиОтправки.Вставить("ОтчетСсылка", 				ОтчетСсылка);
	НастройкиОтправки.Вставить("ПоляДляЗаписиОтправки", 	ПоляДляЗаписиОтправки);
	НастройкиОтправки.Вставить("СтатусОтправки", 			СтатусОперации.СтатусОтправки);
	НастройкиОтправки.Вставить("Протокол", 					ТекстHTML);
	НастройкиОтправки.Вставить("Квитанция", 				ДвоичныеДанныеРезультата);
	НастройкиОтправки.Вставить("ПодписанныйПакет", 			ПодписанныйSOAP64);
	НастройкиОтправки.Вставить("ПакетКвитанции", 			ДвоичныеДанныеПакетаРезультата);
	НоваяОтправкаСсылка = ДобавитьОтправкуФСС(
		ЭтоЭлектроннаяПодписьВМоделиСервиса,
		КороткоеИмяФайлаПакета,
		СтрокаBase64ФайлаПакета,
		РезультатыОперации.ИдентификаторОтправки,
		НастройкиОтправки);
	
	ОперацииСФайламиЭДКО.УдалитьВременныйФайл(ИмяФайлаРезультата);
	УдалитьИзВременногоХранилища(АдресЗашифрованногоОтветаSOAP);
	Если ПодписанныйSOAP64 <> Неопределено И ЭтоЭлектроннаяПодписьВМоделиСервиса Тогда
		УдалитьИзВременногоХранилища(ПодписанныйSOAP64);
	КонецЕсли;
	
	Результат = Новый Структура;
	Результат.Вставить("НоваяОтправкаСсылка", 	НоваяОтправкаСсылка);
	Результат.Вставить("СтатусОтправки", 		СтатусОперации.СтатусОтправки);
	Возврат Результат;
	
КонецФункции

Функция РезультатыОтправкиРеестраСтимулирующихВыплатМедицинскимИСоциальнымРаботникам(
		ТекстРезультата,
		ЭтоЭлектроннаяПодписьВМоделиСервиса = Ложь,
		ПодписанныйПакет = Неопределено) Экспорт
	
	Результат = Новый Структура;
	Результат.Вставить("ИдентификаторОтправки", 			"");
	Результат.Вставить("СтатусОбработки", 					Неопределено);
	Результат.Вставить("ДатаВремяОбработки", 				Неопределено);
	Результат.Вставить("СообщенияОбОшибках", 				"");
	Результат.Вставить("ОшибкиПервичнойОбработки", 			Новый Массив);
	Результат.Вставить("ПредупрежденияПервичнойОбработки", 	Новый Массив);
	Результат.Вставить("ПротоколОбработки", 				Новый Массив);
	Результат.Вставить("НомераСданныхЗаписей", 				Новый Массив);
	Результат.Вставить("НомераНеПринятыхЗаписей", 			Новый Массив);
	
	НомераЗаписейВыгрузки = Новый Массив;
	Если ЗначениеЗаполнено(ПодписанныйПакет) Тогда
		Если ЭтоЭлектроннаяПодписьВМоделиСервиса И ЭтоАдресВременногоХранилища(ПодписанныйПакет) Тогда
			ДвоичныеДанныеПодписанногоПакета = ПолучитьИзВременногоХранилища(ПодписанныйПакет);
		Иначе
			ДвоичныеДанныеПодписанногоПакета = Base64Значение(ПодписанныйПакет);
		КонецЕсли;
		Если ТипЗнч(ДвоичныеДанныеПодписанногоПакета) = Тип("ДвоичныеДанные") Тогда
			ФайлПодписанногоПакета = ПолучитьИмяВременногоФайла("xml");
			ДвоичныеДанныеПодписанногоПакета.Записать(ФайлПодписанногоПакета);
			ДеревоXMLВыгрузки = ЗагрузитьXMLВДеревоЗначений(ФайлПодписанногоПакета);
			Если ДеревоXMLВыгрузки <> Неопределено Тогда
				ФильтрСтрокВыгрузки = Новый Структура("Имя", "batchNo");
				СтрокиНомеровЗаписейВыгрузки = ДеревоXMLВыгрузки.Строки.НайтиСтроки(ФильтрСтрокВыгрузки, Истина);
				Для каждого СтрокаНомераЗаписиВыгрузки Из СтрокиНомеровЗаписейВыгрузки Цикл
					НомераЗаписейВыгрузки.Добавить(СтрокаНомераЗаписиВыгрузки.Значение);
				КонецЦикла;
			КонецЕсли;
			ОперацииСФайламиЭДКО.УдалитьВременныйФайл(ФайлПодписанногоПакета);
		КонецЕсли;
	КонецЕсли;
	
	ДеревоXML = ЗагрузитьXMLВДеревоЗначений(Неопределено, ТекстРезультата);
	Если ДеревоXML = Неопределено Тогда
		Возврат Результат;
	КонецЕсли;
	
	УзелКонверт = НайтиУзел(ДеревоXML, "Envelope");
	Если УзелКонверт = Неопределено Тогда
		Возврат Результат;
	КонецЕсли;
	
	УзелТело = НайтиУзел(УзелКонверт, "Body");
	Если УзелТело = Неопределено Тогда
		Возврат Результат;
	КонецЕсли;
	
	УзелОтвета = НайтиУзел(УзелТело, "submitCovidWorkerRegistrResponse");
	Если УзелОтвета = Неопределено Тогда
		Возврат Результат;
	КонецЕсли;
	
	УзелИдентификаторОтправки = НайтиУзел(УзелОтвета, "recriveID");
	Если УзелИдентификаторОтправки <> Неопределено Тогда
		Результат.ИдентификаторОтправки = XMLЗначениеВнутр(Тип("Строка"), УзелИдентификаторОтправки.Значение);
		Результат.ИдентификаторОтправки = СокрЛП(Результат.ИдентификаторОтправки);
	КонецЕсли;
	
	УзелСтатусОбработки = НайтиУзел(УзелОтвета, "statusGeneral");
	Если УзелСтатусОбработки <> Неопределено И ЗначениеЗаполнено(УзелСтатусОбработки.Значение) Тогда
		Результат.СтатусОбработки = XMLЗначениеВнутр(Тип("Число"), УзелСтатусОбработки.Значение);
	КонецЕсли;
	
	УзелДатаВремяОбработки = НайтиУзел(УзелОтвета, "dateTimeReceive");
	Если УзелДатаВремяОбработки <> Неопределено И ЗначениеЗаполнено(УзелДатаВремяОбработки.Значение) Тогда
		Результат.ДатаВремяОбработки = XMLЗначениеВнутр(Тип("Дата"), УзелДатаВремяОбработки.Значение);
	КонецЕсли;
	
	УзелСообщенияОбОшибкахПервичнойПроверки = НайтиУзел(УзелОтвета, "firstErrorList");
	Если УзелСообщенияОбОшибкахПервичнойПроверки <> Неопределено Тогда
		УзлыОшибка = НайтиУзел(УзелСообщенияОбОшибкахПервичнойПроверки, "error", Истина);
		Для каждого УзелОшибка Из УзлыОшибка Цикл
			ОшибкаОбработки = Новый Структура;
			ОшибкаОбработки.Вставить("КодОшибки", 		"");
			ОшибкаОбработки.Вставить("ОписаниеОшибки", 	"");
			
			УзелКодОшибки = НайтиУзел(УзелОшибка, "errorCode");
			Если УзелКодОшибки <> Неопределено Тогда
				ОшибкаОбработки.КодОшибки = XMLЗначениеВнутр(Тип("Строка"), УзелКодОшибки.Значение);
				ОшибкаОбработки.КодОшибки = СокрЛП(ОшибкаОбработки.КодОшибки);
			КонецЕсли;
			
			УзелОписаниеОшибки = НайтиУзел(УзелОшибка, "errorDescription");
			Если УзелОписаниеОшибки <> Неопределено Тогда
				ОшибкаОбработки.ОписаниеОшибки = XMLЗначениеВнутр(Тип("Строка"), УзелОписаниеОшибки.Значение);
				ОшибкаОбработки.ОписаниеОшибки = СокрЛП(ОшибкаОбработки.ОписаниеОшибки);
			КонецЕсли;
			
			Результат.СообщенияОбОшибках = Результат.СообщенияОбОшибках
				+ ?(ЗначениеЗаполнено(Результат.СообщенияОбОшибках), Символы.ВК + Символы.ПС, "")
				+ ?(ЗначениеЗаполнено(ОшибкаОбработки.ОписаниеОшибки),
					СокрЛП(ОшибкаОбработки.ОписаниеОшибки) + ?(ЗначениеЗаполнено(ОшибкаОбработки.КодОшибки),
						" (" + ОшибкаОбработки.КодОшибки + ")", ""),
					?(ЗначениеЗаполнено(ОшибкаОбработки.КодОшибки), ОшибкаОбработки.КодОшибки,
						НСтр("ru = 'Сбой формирования информации об ошибках'")));
			
			Результат.ОшибкиПервичнойОбработки.Добавить(ОшибкаОбработки);
			Если Лев(ОшибкаОбработки.КодОшибки, 4) = "WARN" Тогда
				Результат.ПредупрежденияПервичнойОбработки.Добавить(ОшибкаОбработки);
			КонецЕсли;
		КонецЦикла;
	КонецЕсли;
	
	УзелПротоколОбработки = НайтиУзел(УзелОтвета, "protocol");
	Если УзелПротоколОбработки <> Неопределено Тогда
		УзелПереченьПроблем = НайтиУзел(УзелПротоколОбработки, "failedList");
		Если УзелПереченьПроблем <> Неопределено Тогда
			УзлыДеталиЗаписи = НайтиУзел(УзелПереченьПроблем, "failedBatchNoDetail", Истина);
			Для каждого УзелДеталиЗаписи Из УзлыДеталиЗаписи Цикл
				РезультатОбработки = Новый Структура;
				РезультатОбработки.Вставить("НомерЗаписи", 		"");
				РезультатОбработки.Вставить("ОшибкиОбработки", 	Новый Массив);
				
				УзелНомерЗаписи = НайтиУзел(УзелДеталиЗаписи, "batchNo");
				Если УзелНомерЗаписи <> Неопределено Тогда
					РезультатОбработки.НомерЗаписи = XMLЗначениеВнутр(Тип("Строка"), УзелНомерЗаписи.Значение);
					РезультатОбработки.НомерЗаписи = СокрЛП(РезультатОбработки.НомерЗаписи);
				КонецЕсли;
				ИндексЗаписи = НомераЗаписейВыгрузки.Найти(РезультатОбработки.НомерЗаписи);
				Если ИндексЗаписи <> Неопределено Тогда
					НомераЗаписейВыгрузки.Удалить(ИндексЗаписи);
				КонецЕсли;
				
				ЕстьОшибки = Ложь;
				ЕстьПредупреждения = Ложь;
				УзелСообщенияОбОшибках = НайтиУзел(УзелДеталиЗаписи, "errorList");
				Если УзелСообщенияОбОшибках <> Неопределено Тогда
					УзлыОшибка = НайтиУзел(УзелСообщенияОбОшибках, "error", Истина);
					Для каждого УзелОшибка Из УзлыОшибка Цикл
						ОшибкаОбработки = Новый Структура;
						ОшибкаОбработки.Вставить("КодОшибки", 		"");
						ОшибкаОбработки.Вставить("ОписаниеОшибки", 	"");
						
						УзелКодОшибки = НайтиУзел(УзелОшибка, "errorCode");
						Если УзелКодОшибки <> Неопределено Тогда
							ОшибкаОбработки.КодОшибки = XMLЗначениеВнутр(Тип("Строка"), УзелКодОшибки.Значение);
							ОшибкаОбработки.КодОшибки = СокрЛП(ОшибкаОбработки.КодОшибки);
						КонецЕсли;
						Если Лев(ОшибкаОбработки.КодОшибки, 4) = "WARN" Тогда
							ЕстьПредупреждения = Истина;
						Иначе
							ЕстьОшибки = Истина;
						КонецЕсли;
						
						УзелОписаниеОшибки = НайтиУзел(УзелОшибка, "errorDescription");
						Если УзелОписаниеОшибки <> Неопределено Тогда
							ОшибкаОбработки.ОписаниеОшибки = XMLЗначениеВнутр(Тип("Строка"), УзелОписаниеОшибки.Значение);
							ОшибкаОбработки.ОписаниеОшибки = СокрЛП(ОшибкаОбработки.ОписаниеОшибки);
						КонецЕсли;
						
						РезультатОбработки.ОшибкиОбработки.Добавить(ОшибкаОбработки);
					КонецЦикла;
				КонецЕсли;
				
				Результат.ПротоколОбработки.Добавить(РезультатОбработки);
				
				Если ЕстьОшибки Тогда
					Результат.НомераНеПринятыхЗаписей.Добавить(РезультатОбработки.НомерЗаписи);
				ИначеЕсли ЕстьПредупреждения Тогда
					Результат.НомераСданныхЗаписей.Добавить(РезультатОбработки.НомерЗаписи);
				КонецЕсли;
			КонецЦикла;
		КонецЕсли;
	КонецЕсли;
	Для каждого НомерЗаписиВыгрузки Из НомераЗаписейВыгрузки Цикл
		Результат.НомераСданныхЗаписей.Добавить(НомерЗаписиВыгрузки);
	КонецЦикла;
	
	Возврат Результат;
	
КонецФункции

Функция СтатусОтправкиРеестраСтимулирующихВыплатМедицинскимИСоциальнымРаботникам(РезультатыОперации)
	
	Результат = Новый Структура;
	Результат.Вставить("СтатусОтправки", 	Неопределено);
	Результат.Вставить("ЦветСтатуса", 		Неопределено);
	Результат.Вставить("СтатусОтчета", 		"");
	
	Если РезультатыОперации.СтатусОбработки = 2 ИЛИ РезультатыОперации.СтатусОбработки = 1 Тогда
		Если РезультатыОперации.НомераНеПринятыхЗаписей.Количество() > 0 Тогда
			Результат.СтатусОтправки 	= Перечисления.СтатусыОтправки.ПринятЕстьОшибки;
			Результат.ЦветСтатуса 		= ЦветаСтиля.ЦветОшибкиВПротоколеБРО;
			Результат.СтатусОтчета 		= НСтр("ru = 'Сдан, обнаружены ошибки'");
			
		Иначе
			Результат.СтатусОтправки 	= Перечисления.СтатусыОтправки.Сдан;
			Результат.ЦветСтатуса 		= ЦветаСтиля.ЦветПоложительногоПротокола;
			Результат.СтатусОтчета 		= НСтр("ru = 'Сдан, ошибок не обнаружено'");
		КонецЕсли;
		
	Иначе // РезультатыОперации.СтатусОбработки = 0
		Если РезультатыОперации.ОшибкиПервичнойОбработки.Количество() =
			РезультатыОперации.ПредупрежденияПервичнойОбработки.Количество()
			И РезультатыОперации.НомераСданныхЗаписей.Количество() > 0 Тогда
			Результат.СтатусОтправки 	= Перечисления.СтатусыОтправки.ПринятЕстьОшибки;
			Результат.ЦветСтатуса 		= ЦветаСтиля.ЦветОшибкиВПротоколеБРО;
			Результат.СтатусОтчета 		= НСтр("ru = 'Сдан, обнаружены ошибки'");
			
		Иначе
			Результат.СтатусОтправки 	= Перечисления.СтатусыОтправки.НеПринят;
			Результат.ЦветСтатуса 		= ЦветаСтиля.ЦветОшибкиВПротоколеБРО;
			Результат.СтатусОтчета 		= НСтр("ru = 'Не принят, обнаружены ошибки'");
		КонецЕсли;
	КонецЕсли;
	
	Возврат Результат;
	
КонецФункции

Функция ГенерироватьHTMLПротоколаОбработкиРеестраСтимулирующихВыплатМедицинскимИСоциальнымРаботникам(
		ОтчетСсылка,
		ОрганизацияСсылка,
		РезультатыОперации,
		СтатусОперации)
	
	ТабДок = Новый ТабличныйДокумент;
	
	Шаблон = ПолучитьМакет("ФСССтимулирующиеВыплатыШаблонДляHTML");
	
	СекцияШапка = Шаблон.ПолучитьОбласть("Шапка");
	
	НаименованиеПротокола = ?(СтатусОперации.СтатусОтправки = Перечисления.СтатусыОтправки.Сдан,
		НСтр("ru = 'Протокол о сдаче'"), ?(СтатусОперации.СтатусОтправки = Перечисления.СтатусыОтправки.НеПринят,
		НСтр("ru = 'Протокол ошибок'"), НСтр("ru = 'Протокол о частичной сдаче'")));
	
	СвойстваОрганизации = РегламентированнаяОтчетностьВызовСервера.ПолучитьСведенияОбОрганизации(
		ОрганизацияСсылка,,
		"НаимЮЛПол, ИННЮЛ");
	ПолноеНаименованиеОрганизации 	= СвойстваОрганизации.НаимЮЛПол;
	ИННОрганизации 					= СвойстваОрганизации.ИННЮЛ;
	
	СведенияПоОбъекту = СведенияПоОтправляемымОбъектам(ОтчетСсылка);
	
	Если СтатусОперации.СтатусОтправки = Перечисления.СтатусыОтправки.ПринятЕстьОшибки Тогда
		СтатусОбработкиТекстом = НСтр("ru = 'Реестр загружен частично. Отправьте повторно только записи с ошибками, исправив ошибки'");
	ИначеЕсли РезультатыОперации.СтатусОбработки = 2 ИЛИ РезультатыОперации.СтатусОбработки = 1 Тогда
		СтатусОбработкиТекстом = НСтр("ru = 'Реестр успешно загружен'");
	ИначеЕсли РезультатыОперации.СтатусОбработки = 0 Тогда
		СтатусОбработкиТекстом = НСтр("ru = 'Реестр не загружен'");
	Иначе
		СтатусОбработкиТекстом = Строка(РезультатыОперации.СтатусОбработки);
	КонецЕсли;
	
	СекцияШапка.Параметры.НаименованиеПротокола 	= НаименованиеПротокола;
	СекцияШапка.Параметры.Организация 				= ПолноеНаименованиеОрганизации;
	СекцияШапка.Параметры.ИНН 						= НСтр("ru = 'ИНН'") + " " + ИННОрганизации;
	СекцияШапка.Параметры.Получатель 				= СведенияПоОбъекту.ПредставлениеКонтролирующегоОргана;
	СекцияШапка.Параметры.Отчет 					= СведенияПоОбъекту.Наименование;
	СекцияШапка.Параметры.СтатусОтчета 				= СтатусОперации.СтатусОтчета;
	СекцияШапка.Области.СтатусОтчета.ЦветТекста 	= СтатусОперации.ЦветСтатуса;
	СекцияШапка.Параметры.ИдентификаторОтправки 	= РезультатыОперации.ИдентификаторОтправки;
	СекцияШапка.Параметры.СтатусОбработки 			= СтатусОбработкиТекстом;
	СекцияШапка.Параметры.ДатаПротокола 			= Формат(РезультатыОперации.ДатаВремяОбработки, "ДФ='дд.ММ.гггг ЧЧ:мм:сс'");
	
	ТабДок.Вывести(СекцияШапка);
	
	Если ЗначениеЗаполнено(РезультатыОперации.СообщенияОбОшибках) Тогда
		СекцияПротоколОшибок = Шаблон.ПолучитьОбласть("ПротоколОшибокПервичнойПроверки");
		СекцияПротоколОшибок.Параметры.СообщенияОбОшибках = РезультатыОперации.СообщенияОбОшибках;
		ТабДок.Вывести(СекцияПротоколОшибок);
	КонецЕсли;
	
	Если РезультатыОперации.ПротоколОбработки.Количество() > 0 Тогда
		СекцияРезультатыОбработки = Шаблон.ПолучитьОбласть("РезультатыОбработки");
		ТабДок.Вывести(СекцияРезультатыОбработки);
		
		Для ИндексРезультата = 0 По РезультатыОперации.ПротоколОбработки.Количество() - 1 Цикл
			РезультатОбработки = РезультатыОперации.ПротоколОбработки[ИндексРезультата];
			
			СекцияСтрокаРезультатов = Шаблон.ПолучитьОбласть("СтрокаРезультатов");
			ЛинияГраницыСверху = СекцияСтрокаРезультатов.Области.ПоляСтрокиРезультатов.ГраницаСверху;
			ЛинияГраницыСверху = Новый Линия(
				ЛинияГраницыСверху.ТипЛинии,
				?(ИндексРезультата = 0, 2, 1),
				ЛинияГраницыСверху.Отступ);
			ЛинияГраницыСнизу = СекцияСтрокаРезультатов.Области.ПоляСтрокиРезультатов.ГраницаСнизу;
			ЛинияГраницыСнизу = Новый Линия(
				ЛинияГраницыСнизу.ТипЛинии,
				?(ИндексРезультата = РезультатыОперации.ПротоколОбработки.Количество() - 1
				И РезультатОбработки.ОшибкиОбработки.Количество() <= 1, 2,
				?(РезультатОбработки.ОшибкиОбработки.Количество() > 1, 0, 1)),
				ЛинияГраницыСнизу.Отступ);
			СекцияСтрокаРезультатов.Области.ПоляСтрокиРезультатов.ГраницаСверху = ЛинияГраницыСверху;
			СекцияСтрокаРезультатов.Области.ПоляСтрокиРезультатов.ГраницаСнизу = ЛинияГраницыСнизу;
			
			ЛинияГраницыСверху = СекцияСтрокаРезультатов.Области.ПоляСтрокиРезультатовКолонкиОшибки.ГраницаСверху;
			ЛинияГраницыСверху = Новый Линия(
				ЛинияГраницыСверху.ТипЛинии,
				?(ИндексРезультата = 0, 2, 1),
				ЛинияГраницыСверху.Отступ);
			ЛинияГраницыСнизу = СекцияСтрокаРезультатов.Области.ПоляСтрокиРезультатовКолонкиОшибки.ГраницаСнизу;
			ЛинияГраницыСнизу = Новый Линия(
				ЛинияГраницыСнизу.ТипЛинии,
				?(ИндексРезультата = РезультатыОперации.ПротоколОбработки.Количество() - 1
				И РезультатОбработки.ОшибкиОбработки.Количество() <= 1, 2, 1),
				ЛинияГраницыСнизу.Отступ);
			СекцияСтрокаРезультатов.Области.ПоляСтрокиРезультатовКолонкиОшибки.ГраницаСверху = ЛинияГраницыСверху;
			СекцияСтрокаРезультатов.Области.ПоляСтрокиРезультатовКолонкиОшибки.ГраницаСнизу = ЛинияГраницыСнизу;
			
			
			КодПервойОшибки = ?(РезультатОбработки.ОшибкиОбработки.Количество() > 0,
				РезультатОбработки.ОшибкиОбработки[0].КодОшибки, "");
			ОписаниеПервойОшибки = ?(РезультатОбработки.ОшибкиОбработки.Количество() > 0,
				РезультатОбработки.ОшибкиОбработки[0].ОписаниеОшибки, "");
			
			СекцияСтрокаРезультатов.Параметры.НомерЗаписи 		= РезультатОбработки.НомерЗаписи;
			СекцияСтрокаРезультатов.Параметры.КодОшибки 		= КодПервойОшибки;
			СекцияСтрокаРезультатов.Параметры.ОписаниеОшибки 	= ОписаниеПервойОшибки;
			
			ТабДок.Вывести(СекцияСтрокаРезультатов);
			
			Для ИндексОшибки = 1 По РезультатОбработки.ОшибкиОбработки.Количество() - 1 Цикл
				ОшибкаОбработки = РезультатОбработки.ОшибкиОбработки[ИндексОшибки];
				
				СекцияСтрокаРезультатов = Шаблон.ПолучитьОбласть("СтрокаРезультатов");
				ЛинияГраницыСверху = СекцияСтрокаРезультатов.Области.ПоляСтрокиРезультатов.ГраницаСверху;
				ЛинияГраницыСверху = Новый Линия(
					ЛинияГраницыСверху.ТипЛинии,
					0,
					ЛинияГраницыСверху.Отступ);
				ЛинияГраницыСнизу = СекцияСтрокаРезультатов.Области.ПоляСтрокиРезультатов.ГраницаСнизу;
				ЛинияГраницыСнизу = Новый Линия(
					ЛинияГраницыСнизу.ТипЛинии,
					?(ИндексОшибки = РезультатОбработки.ОшибкиОбработки.Количество() - 1,
					?(ИндексРезультата = РезультатыОперации.ПротоколОбработки.Количество() - 1, 2, 1), 0),
					ЛинияГраницыСнизу.Отступ);
				СекцияСтрокаРезультатов.Области.ПоляСтрокиРезультатов.ГраницаСверху = ЛинияГраницыСверху;
				СекцияСтрокаРезультатов.Области.ПоляСтрокиРезультатов.ГраницаСнизу = ЛинияГраницыСнизу;
				
				ЛинияГраницыСверху = СекцияСтрокаРезультатов.Области.ПоляСтрокиРезультатовКолонкиОшибки.ГраницаСверху;
				ЛинияГраницыСверху = Новый Линия(
					ЛинияГраницыСверху.ТипЛинии,
					0,
					ЛинияГраницыСверху.Отступ);
				ЛинияГраницыСнизу = СекцияСтрокаРезультатов.Области.ПоляСтрокиРезультатовКолонкиОшибки.ГраницаСнизу;
				ЛинияГраницыСнизу = Новый Линия(
					ЛинияГраницыСнизу.ТипЛинии,
					?(ИндексРезультата = РезультатыОперации.ПротоколОбработки.Количество() - 1
					И ИндексОшибки = РезультатОбработки.ОшибкиОбработки.Количество() - 1, 2, 1),
					ЛинияГраницыСнизу.Отступ);
				СекцияСтрокаРезультатов.Области.ПоляСтрокиРезультатовКолонкиОшибки.ГраницаСверху = ЛинияГраницыСверху;
				СекцияСтрокаРезультатов.Области.ПоляСтрокиРезультатовКолонкиОшибки.ГраницаСнизу = ЛинияГраницыСнизу;
				
				СекцияСтрокаРезультатов.Параметры.НомерЗаписи 		= "";
				СекцияСтрокаРезультатов.Параметры.КодОшибки 		= ОшибкаОбработки.КодОшибки;
				СекцияСтрокаРезультатов.Параметры.ОписаниеОшибки 	= ОшибкаОбработки.ОписаниеОшибки;
				
				ТабДок.Вывести(СекцияСтрокаРезультатов);
			КонецЦикла;
		КонецЦикла;
	КонецЕсли;
	
	СекцияПодвал = Шаблон.ПолучитьОбласть("Подвал");
	ТабДок.Вывести(СекцияПодвал);
	
	ИмяВременногоФайла = ПолучитьИмяВременногоФайла();
	ТабДок.Записать(ИмяВременногоФайла, ТипФайлаТабличногоДокумента.HTML);
	ТекстHTML = Новый ТекстовыйДокумент;
	ТекстHTML.Прочитать(ИмяВременногоФайла);
	ТекстHTML = ТекстHTML.ПолучитьТекст();
	
	Возврат ТекстHTML;
	
КонецФункции

#КонецОбласти

Функция НовыйЧтениеТекстаНаСервере(ИмяФайла, Кодировка = Неопределено) Экспорт
	
	// Обход ошибки платформы.
	// ЧтениеТекста может неверно определять кодировку, если
	// кодировка не задана явно.
	НоваяКодировка 	= ИсправленнаяКодировкаЧтенияТекстаНаСервере(ИмяФайла, Кодировка);
	Конструктор 	= Новый ЧтениеТекста(ИмяФайла, НоваяКодировка);
	
	Возврат Конструктор;
	
КонецФункции

Функция ТекстHTMLСАвтоопределениемКодировки(ИмяФайла) Экспорт
	
	Текст = Новый ЧтениеТекста(ИмяФайла, "windows-1251");
	ТекстСодержимого = Текст.Прочитать();
	
	ТекстСодержимогоБезПробелов = СтрЗаменить(ТекстСодержимого, " ", "");
	ТекстСодержимогоБезПробеловВНижнемРегистре = нрег(ТекстСодержимогоБезПробелов);
	Если СтрНайти(ТекстСодержимогоБезПробеловВНижнемРегистре, "content=""text/html;charset=utf-8""") > 0 Тогда
		Текст = Новый ЧтениеТекста(ИмяФайла, "utf-8");
		ТекстСодержимого = Текст.Прочитать();
	КонецЕсли;
	
	Возврат ТекстСодержимого;
	
КонецФункции

Процедура ЧтениеТекстаОткрытьНаСервере(ОбъектЧтениеТекста, ИмяФайла) Экспорт
	
	// Обход ошибки платформы.
	// ЧтениеТекста может неверно определять кодировку, если
	// кодировка не задана явно.
	НоваяКодировка 	= ИсправленнаяКодировкаЧтенияТекстаНаСервере(ИмяФайла);
	ОбъектЧтениеТекста.Открыть(ИмяФайла, НоваяКодировка);
	
КонецПроцедуры

Функция ИсправленнаяКодировкаЧтенияТекстаНаСервере(ИмяФайла, Знач Кодировка = Неопределено)
	
	Если ЗначениеЗаполнено(Кодировка) Тогда
		
		НоваяКодировка = Кодировка;
		
	Иначе
		
		// Для utf-8 без BOM требуется самостоятельно определять кодировку
		Если КодировкаНаСервереОпределяетсяВерно() Тогда
			НоваяКодировка = ОпределитьКодировку(Новый ДвоичныеДанные(ИмяФайла));
		Иначе
			Если ФайлИмеетBOM(ИмяФайла) Тогда
				НоваяКодировка = Неопределено;
			Иначе
				НоваяКодировка = ОпределитьКодировку(Новый ДвоичныеДанные(ИмяФайла));
			КонецЕсли;
		КонецЕсли;
		
	КонецЕсли;
	
	Возврат НоваяКодировка; 
	
КонецФункции

Функция КодировкаНаСервереОпределяетсяВерно() Экспорт
	
	ИсходнаяСтрока 	= "АБВГДЕЖЗИКЛМНОПРСТУФХЦЧШЭЮЯабвгдежзиклмнопрстуфхцчшэюя";
	ИмяФайла 		= ПолучитьИмяВременногоФайла();
	
	// Записываем текст с кодировкой
	ЗаписьКонтрольногоФайла = Новый ЗаписьТекста(ИмяФайла, "windows-1251");
	ЗаписьКонтрольногоФайла.Записать(ИсходнаяСтрока);
	ЗаписьКонтрольногоФайла.Закрыть();
	
	// Читаем текст без кодировки
	ЧтениеФайлаБезКодировки 		= Новый ЧтениеТекста(ИмяФайла);
	ПрочтеннаяСтрокаБезКодировки 	= ЧтениеФайлаБезКодировки.Прочитать();
	ЧтениеФайлаБезКодировки.Закрыть();
	
	УдалитьФайлы(ИмяФайла);
	
	Возврат ИсходнаяСтрока = ПрочтеннаяСтрокаБезКодировки;
	
КонецФункции

Функция ФайлИмеетBOM(ИмяФайла) Экспорт
	
	ПредставлениеКодировкиUTF8 	= ПредставлениеКодировкиФайла(ИмяФайла, 3);
	ПредставлениеКодировкиUTF16 = ПредставлениеКодировкиФайла(ИмяФайла, 2);
	ПредставлениеКодировкиUTF32 = ПредставлениеКодировкиФайла(ИмяФайла, 4);
	ПредставлениеКодировкиUTF7 	= ПредставлениеКодировкиФайла(ИмяФайла, 16);
	
	Возврат ПредставлениеКодировкиUTF8 = "239187191"
		ИЛИ ПредставлениеКодировкиUTF16 = "254255"
		ИЛИ ПредставлениеКодировкиUTF16 = "255254"
		ИЛИ ПредставлениеКодировкиUTF32 = "00254255"
		ИЛИ ПредставлениеКодировкиUTF32 = "25525400"
		ИЛИ ПредставлениеКодировкиUTF7 = "434711856434711857434711843434711847";
	
КонецФункции

Функция ПредставлениеКодировкиФайла(ИмяФайла, КоличествоБайт) Экспорт
	
	ПредставлениеКодировки = "";
	
	ТекстовыйПоток = Новый ЧтениеТекста;
	ТекстовыйПоток.Открыть(ИмяФайла, "ISO-8859-1");
	
	Для Инд = 1 По КоличествоБайт Цикл
		
		ТекущийСимвол = ТекстовыйПоток.Прочитать(1);
		Если ТекущийСимвол = Неопределено Тогда
			ТекстовыйПоток.Закрыть();
			Возврат ПредставлениеКодировки;
		КонецЕсли;
		ТекущийБайт = КодСимвола(ТекущийСимвол); // Значение байта 0-255
		
		ПредставлениеКодировки = ПредставлениеКодировки + Строка(ТекущийБайт);
		
	КонецЦикла;
	
	ТекстовыйПоток.Закрыть();
	
	Возврат ПредставлениеКодировки;
	
КонецФункции

#Область ОбменСКонтролирующимиОрганами

Процедура ВыполнитьОбмен(УчетнаяЗапись) Экспорт

	УстановитьПривилегированныйРежим(Истина);

	АвторасшифровкаДоступна = ЭлектронныйДокументооборотСКонтролирующимиОрганами.ДоступнаАвторасшифровкаТранспортныхСообщений(УчетнаяЗапись);
	
	Если АвторасшифровкаДоступна Тогда
		// Подготовим список ошибок текущей итерации обмена.
		ИнициализироватьСохранениеОшибокОбмена(УчетнаяЗапись);
	КонецЕсли;
	
	ПолучитьСообщения(УчетнаяЗапись, АвторасшифровкаДоступна);
	
	ОтправитьСообщения(УчетнаяЗапись);

	Если АвторасшифровкаДоступна Тогда
		// Сохраним возникшие в процессе обмена ошибки.
		СохранитьОшибкиОбмена(УчетнаяЗапись);
	КонецЕсли;

КонецПроцедуры

Функция ПолучитьСообщения(УчетнаяЗапись, АвторасшифровкаДоступна = Ложь) Экспорт
	
	УстановитьПривилегированныйРежим(Истина);
	
	ПочтовыеСообщения = ПолучитьПочтовыеСообщения(УчетнаяЗапись);
	
	ПолученныеТранспортныеСообщения = СоздатьТранспортныеСообщения(УчетнаяЗапись, ПочтовыеСообщения);
	
	РаспознатьТранспортныеСообщения(УчетнаяЗапись, АвторасшифровкаДоступна);
	
	Возврат ПолученныеТранспортныеСообщения;
	
КонецФункции

Процедура ОтправитьСообщения(УчетнаяЗапись) Экспорт
	
	УстановитьПривилегированныйРежим(Истина);
	
	СообщенияДляОтправки = ПолучитьСообщенияДляОтправки(УчетнаяЗапись);
	
	ПараметрыПодключения = 
	ПолучитьПараметрыПодключенияПоУчетнойЗаписи(УчетнаяЗапись);
	Соединение = ДокументооборотСКО.ОткрытьСоединениеДляОтправкиПочты(ПараметрыПодключения);
	
	Для Каждого СообщениеДляОтправки Из СообщенияДляОтправки Цикл
		ОтправитьТранспортноеСообщение(СообщениеДляОтправки, Соединение);
	КонецЦикла;
	
	ДокументооборотСКО.ЗакрытьПочтовоеСоединение(Соединение);
	
КонецПроцедуры

Процедура РаспознатьТранспортныеСообщения(УчетнаяЗапись, АвторасшифровкаДоступна)

	Выборка = ПолучитьТранспортныеСообщенияДляРаспознавания();
	
	Пока Выборка.Следующий() Цикл
		
		Ссылка = Выборка.Ссылка;
		Если НЕ Ссылка.ПометкаУдаления
			И Ссылка.Тип = Перечисления.ТипыТранспортныхСообщений.ПустаяСсылка() Тогда
		
			РаспознатьТранспортноеСообщение(Выборка.Ссылка, Истина);
			Если АвторасшифровкаДоступна Тогда
				РасшифроватьТранспортноеСообщение(Выборка.Ссылка);
			КонецЕсли;
			ОбновитьСтатусыСвязанногоОбъектаПриНеобходимости(Выборка.Ссылка, Истина);
			
		КонецЕсли;
		
	КонецЦикла;
	
КонецПроцедуры

Функция ПолучитьТранспортныеСообщенияДляРаспознавания()
	
	Запрос = Новый Запрос;
	Запрос.Текст =
	"ВЫБРАТЬ
	|	ТранспортноеСообщение.Ссылка
	|ИЗ
	|	Документ.ТранспортноеСообщение КАК ТранспортноеСообщение
	|ГДЕ
	|	НЕ ТранспортноеСообщение.ПометкаУдаления
	|	И ТранспортноеСообщение.Статус = ЗНАЧЕНИЕ(Перечисление.СтатусыПисем.Полученное)
	|	И ТранспортноеСообщение.Тип = ЗНАЧЕНИЕ(Перечисление.ТипыТранспортныхСообщений.ПустаяСсылка)";
	
	Возврат Запрос.Выполнить().Выбрать();
	
КонецФункции

Функция ПолучитьПараметрыПодключенияПоУчетнойЗаписи(УчетнаяЗапись)
	
	ПараметрыПодключения = Новый Структура;
	
	Поля = "ИмяПользователяPOP3, ИмяПользователяSMTP, ПарольPOP3, ПарольSMTP, ПортPOP3,
			|ПортSMTP, СерверPOP3, СерверSMTP, ТребуетсяSMTPАутентификация, 
			|СерверДокументооборота.ДлительностьОжиданияСервера";
	
	ЗначенияРеквизитов = ОбщегоНазначения.ЗначенияРеквизитовОбъекта(УчетнаяЗапись, Поля);
	
	ПараметрыPOP3 = Новый Структура;
	ПараметрыPOP3.Вставить("Сервер", ЗначенияРеквизитов.СерверPOP3);
	ПараметрыPOP3.Вставить("Пользователь", ЗначенияРеквизитов.ИмяПользователяPOP3);
	ПараметрыPOP3.Вставить("Пароль", ЗначенияРеквизитов.ПарольPOP3);
	ПараметрыPOP3.Вставить("Порт", ЗначенияРеквизитов.ПортPOP3);
	СпособАутентификации = СпособPOP3Аутентификации.Обычная;
	ПараметрыPOP3.Вставить("СпособАутентификации", СпособАутентификации);
	
	ПараметрыSMTP = Новый Структура;
	ПараметрыSMTP.Вставить("Сервер", ЗначенияРеквизитов.СерверSMTP);
	ПараметрыSMTP.Вставить("Пользователь", ЗначенияРеквизитов.ИмяПользователяSMTP);
	ПараметрыSMTP.Вставить("Пароль", ЗначенияРеквизитов.ПарольSMTP);
	ПараметрыSMTP.Вставить("Порт", ЗначенияРеквизитов.ПортSMTP);
	СпособАутентификации = ?(ЗначенияРеквизитов.ТребуетсяSMTPАутентификация, СпособSMTPАутентификации.Login, СпособSMTPАутентификации.БезАутентификации);
	ПараметрыSMTP.Вставить("СпособАутентификации", СпособАутентификации);
	
	ПараметрыПодключения.Вставить("POP3", ПараметрыPOP3);
	ПараметрыПодключения.Вставить("SMTP", ПараметрыSMTP);
	
	ПараметрыПодключения.Вставить("Таймаут", ЗначенияРеквизитов.СерверДокументооборотаДлительностьОжиданияСервера);

	Возврат ПараметрыПодключения;
	
КонецФункции

Функция ПолучитьПочтовыеСообщения(УчетнаяЗапись)
	
	ПараметрыПодключения = ПолучитьПараметрыПодключенияПоУчетнойЗаписи(УчетнаяЗапись);
	Соединение = ДокументооборотСКО.ОткрытьСоединениеДляПолученияПочты(ПараметрыПодключения);	
	
	ЗаголовкиПочтовыхСообщений = Соединение.ПолучитьЗаголовки();
	
	ПочтовыеСообщения = Новый Массив;
	СообщенияДляПолучения = ОтобратьПочтовыеСообщения(УчетнаяЗапись, ЗаголовкиПочтовыхСообщений, Ложь);
	Если ЗначениеЗаполнено(СообщенияДляПолучения) Тогда
		ПочтовыеСообщения = Соединение.Выбрать(Ложь, СообщенияДляПолучения);
	КонецЕсли;
	
	// Сообщения по завершенным циклам обмена считаем более не нужными и удаляем с сервера.
	СообщенияДляУдаления = ОтобратьПочтовыеСообщения(УчетнаяЗапись, ЗаголовкиПочтовыхСообщений, Истина);	
	Если ЗначениеЗаполнено(СообщенияДляУдаления) Тогда
		Соединение.УдалитьСообщения(СообщенияДляУдаления);
	КонецЕсли;
	
	ДокументооборотСКО.ЗакрытьПочтовоеСоединение(Соединение);
	
	Возврат ПочтовыеСообщения;
	
КонецФункции

Функция ОтобратьПочтовыеСообщения(УчетнаяЗапись, ЗаголовкиСообщений, ДляУдаления)
	
	ИдентификаторыПолученныхСообщений = Новый ТаблицаЗначений;
	ИдентификаторыПолученныхСообщений.Колонки.Добавить("Идентификатор", Новый ОписаниеТипов("Строка",,,, Новый КвалификаторыСтроки(255)));
	Для Каждого ЗаголовокСообщения Из ЗаголовкиСообщений Цикл
		НоваяСтрока = ИдентификаторыПолученныхСообщений.Добавить();
		НоваяСтрока.Идентификатор = ЗаголовокСообщения.ИдентификаторСообщения;
	КонецЦикла;
	
	Если ДляУдаления Тогда
		ИдентификаторыСообщений = ПолучитьИдентификаторыСообщенийДляУдаления(УчетнаяЗапись, ИдентификаторыПолученныхСообщений);
	Иначе
		ИдентификаторыСообщений = ПолучитьИдентификаторыСообщенийДляПолучения(УчетнаяЗапись, ИдентификаторыПолученныхСообщений);
	КонецЕсли;
	
	ОтобранныеСообщения = Новый Массив;
	Если ЗначениеЗаполнено(ИдентификаторыСообщений) Тогда
		Для Каждого ЗаголовокСообщения Из ЗаголовкиСообщений Цикл
			Если ИдентификаторыСообщений.Найти(ЗаголовокСообщения.ИдентификаторСообщения) <> Неопределено Тогда
				ОтобранныеСообщения.Добавить(ЗаголовокСообщения);
			КонецЕсли;
		КонецЦикла;
	КонецЕсли;
	
	Возврат ОтобранныеСообщения;
	
КонецФункции

Функция ПолучитьИдентификаторыСообщенийДляПолучения(УчетнаяЗапись, ИдентификаторыПолученныхСообщений)

	Запрос = Новый Запрос;
	Запрос.Текст = 
	"ВЫБРАТЬ
	|	ИдентификаторыПолученныхСообщений.Идентификатор КАК Идентификатор
	|ПОМЕСТИТЬ ИдентификаторыПолученныхСообщений
	|ИЗ
	|	&ИдентификаторыПолученныхСообщений КАК ИдентификаторыПолученныхСообщений
	|
	|ИНДЕКСИРОВАТЬ ПО
	|	Идентификатор
	|;
	|
	|////////////////////////////////////////////////////////////////////////////////
	|ВЫБРАТЬ
	|	ТранспортноеСообщение.Ссылка,
	|	ТранспортноеСообщение.ИдентификаторСообщения
	|ПОМЕСТИТЬ ТранспортныеСообщенияПоУчетнойЗаписи
	|ИЗ
	|	Документ.ТранспортноеСообщение КАК ТранспортноеСообщение
	|ГДЕ
	|	ТранспортноеСообщение.УчетнаяЗапись = &УчетнаяЗапись
	|	И ТранспортноеСообщение.Статус = ЗНАЧЕНИЕ(Перечисление.СтатусыПисем.Полученное)
	|;
	|
	|////////////////////////////////////////////////////////////////////////////////
	|ВЫБРАТЬ
	|	ИдентификаторыПолученныхСообщений.Идентификатор КАК ИдентификаторСообщения,
	|	ТранспортныеСообщенияПоУчетнойЗаписи.Ссылка
	|ИЗ
	|	ИдентификаторыПолученныхСообщений КАК ИдентификаторыПолученныхСообщений
	|		ЛЕВОЕ СОЕДИНЕНИЕ ТранспортныеСообщенияПоУчетнойЗаписи КАК ТранспортныеСообщенияПоУчетнойЗаписи
	|		ПО ИдентификаторыПолученныхСообщений.Идентификатор = ТранспортныеСообщенияПоУчетнойЗаписи.ИдентификаторСообщения";
	Запрос.УстановитьПараметр("УчетнаяЗапись", УчетнаяЗапись);
	Запрос.УстановитьПараметр("ИдентификаторыПолученныхСообщений", ИдентификаторыПолученныхСообщений);
	
	ИдентификаторыСообщений = Новый Массив;
	Выборка = Запрос.Выполнить().Выбрать();
	Пока Выборка.Следующий() Цикл
		Если Не ЗначениеЗаполнено(Выборка.Ссылка) Тогда
			ИдентификаторыСообщений.Добавить(Выборка.ИдентификаторСообщения);
		КонецЕсли;
	КонецЦикла;
	
	Возврат ИдентификаторыСообщений;

КонецФункции

Функция ПолучитьИдентификаторыСообщенийДляУдаления(УчетнаяЗапись, ИдентификаторыПолученныхСообщений)
	
	Запрос = Новый Запрос;
	Запрос.Текст = 
	"ВЫБРАТЬ
	|	ИдентификаторыПолученныхСообщений.Идентификатор КАК Идентификатор
	|ПОМЕСТИТЬ ИдентификаторыПолученныхСообщений
	|ИЗ
	|	&ИдентификаторыПолученныхСообщений КАК ИдентификаторыПолученныхСообщений
	|
	|ИНДЕКСИРОВАТЬ ПО
	|	Идентификатор
	|;
	|
	|////////////////////////////////////////////////////////////////////////////////
	|ВЫБРАТЬ
	|	ТранспортноеСообщение.Ссылка,
	|	ТранспортноеСообщение.ИдентификаторСообщения
	|ПОМЕСТИТЬ ТранспортныеСообщенияПоУчетнойЗаписи
	|ИЗ
	|	Документ.ТранспортноеСообщение КАК ТранспортноеСообщение
	|ГДЕ
	|	ТранспортноеСообщение.Статус = ЗНАЧЕНИЕ(Перечисление.СтатусыПисем.Полученное)
	|	И ТранспортноеСообщение.ЦиклОбмена <> ЗНАЧЕНИЕ(Справочник.ЦиклыОбмена.ПустаяСсылка)
	|	И ТранспортноеСообщение.ЦиклОбмена.ДатаЗакрытия <> ДАТАВРЕМЯ(3999, 12, 31, 23, 59, 59)
	|	И ТранспортноеСообщение.ЦиклОбмена.ДатаЗакрытия <> ДАТАВРЕМЯ(1, 1, 1)
	|	И ТранспортноеСообщение.УчетнаяЗапись = &УчетнаяЗапись
	|	И (НЕ ТранспортноеСообщение.Тип В (ЗНАЧЕНИЕ(Перечисление.ТипыТранспортныхСообщений.ПисьмоВходящееПФР), ЗНАЧЕНИЕ(Перечисление.ТипыТранспортныхСообщений.ПисьмоВходящееФСГС), ЗНАЧЕНИЕ(Перечисление.ТипыТранспортныхСообщений.ПисьмоНО), ЗНАЧЕНИЕ(Перечисление.ТипыТранспортныхСообщений.ДокументНО))
	|			ИЛИ ТранспортноеСообщение.УчетнаяЗапись.СпецоператорСвязи = ЗНАЧЕНИЕ(Перечисление.СпецоператорыСвязи.Прочие))
	|;
	|
	|////////////////////////////////////////////////////////////////////////////////
	|ВЫБРАТЬ
	|	ИдентификаторыПолученныхСообщений.Идентификатор КАК ИдентификаторСообщения,
	|	ТранспортныеСообщенияПоУчетнойЗаписи.Ссылка
	|ИЗ
	|	ИдентификаторыПолученныхСообщений КАК ИдентификаторыПолученныхСообщений
	|		ВНУТРЕННЕЕ СОЕДИНЕНИЕ ТранспортныеСообщенияПоУчетнойЗаписи КАК ТранспортныеСообщенияПоУчетнойЗаписи
	|		ПО ИдентификаторыПолученныхСообщений.Идентификатор = ТранспортныеСообщенияПоУчетнойЗаписи.ИдентификаторСообщения";
	Запрос.УстановитьПараметр("УчетнаяЗапись", УчетнаяЗапись);
	Запрос.УстановитьПараметр("ИдентификаторыПолученныхСообщений", ИдентификаторыПолученныхСообщений);
	
	ИдентификаторыСообщений = Новый Массив;
	ИдентификаторыСообщений = Запрос.Выполнить().Выгрузить().ВыгрузитьКолонку("ИдентификаторСообщения");
	
	Возврат ИдентификаторыСообщений;

КонецФункции

Функция СоздатьТранспортныеСообщения(УчетнаяЗапись, ПочтовыеСообщения)
	
	ПолученныеСообщения = Новый Массив;
	Для Каждого ПочтовоеСообщение Из ПочтовыеСообщения Цикл
		ПолученныеСообщения.Добавить(ЗаполнитьТранспортноеСообщение(УчетнаяЗапись, ПочтовоеСообщение));
	КонецЦикла;
	
	ЗаполнитьСвойстваТранспортныхСообщений(ПолученныеСообщения);
	
	Возврат ПолученныеСообщения;
	
КонецФункции

Функция ЗаполнитьТранспортноеСообщение(УчетнаяЗапись, ПочтовоеСообщение)

	НачатьТранзакцию();
	Попытка
		ТранспортноеСообщение = Документы.ТранспортноеСообщение.СоздатьДокумент();
		
		ТранспортноеСообщение.УчетнаяЗапись = УчетнаяЗапись; 
		
		ТранспортноеСообщение.Статус = Перечисления.СтатусыПисем.Полученное;
		ТранспортноеСообщение.Тема = ОбщегоНазначенияЭДКОКлиентСервер.ЗаменитьНечитаемыеСимволы(СокрЛП(ПочтовоеСообщение.Тема));
		ТранспортноеСообщение.ИдентификаторСообщения = ПочтовоеСообщение.ИдентификаторСообщения;
		ТранспортноеСообщение.ДатаТранспорта = ПочтовоеСообщение.ДатаПолучения;
		ТранспортноеСообщение.Дата = ПочтовоеСообщение.ДатаОтправления;
		ТранспортноеСообщение.Кодировка = ПочтовоеСообщение.Кодировка;
		
		ТранспортноеСообщение.ОтКогоАдрес = ПочтовоеСообщение.Отправитель.Адрес;
		ТранспортноеСообщение.ОтКогоПредставление = ПочтовоеСообщение.Отправитель.ОтображаемоеИмя;
		
		
		ЗаполнитьАдресатов(ТранспортноеСообщение.Кому, ПочтовоеСообщение.Получатели);
		ЗаполнитьАдресатов(ТранспортноеСообщение.Копии, ПочтовоеСообщение.Копии);
		ЗаполнитьТекстПисьма(ТранспортноеСообщение, ПочтовоеСообщение);
		ЗаполнитьДополнительныеРеквизитыЗаголовка(ТранспортноеСообщение, ПочтовоеСообщение);
		
		Ссылка = Документы.ТранспортноеСообщение.ПолучитьСсылку();
		ТранспортноеСообщение.УстановитьСсылкуНового(Ссылка);
		
		ТранспортноеСообщение.Записать();
		
		РазобратьВложения(Ссылка, ПочтовоеСообщение);
		
		ЗафиксироватьТранзакцию(); 
	Исключение
		ИнформацияОбОшибке = ИнформацияОбОшибке();
		ЗаписьЖурналаРегистрации(
			НСтр("ru = 'Электронный документооборот с контролирующими органами. Транспортное сообщение'", ОбщегоНазначения.КодОсновногоЯзыка()),
			УровеньЖурналаРегистрации.Ошибка,,, ПодробноеПредставлениеОшибки(ИнформацияОбОшибке()));
			
		ОтменитьТранзакцию();
		ВызватьИсключение;
	КонецПопытки;
	
	Возврат ТранспортноеСообщение.Ссылка;

КонецФункции

Процедура ЗаполнитьАдресатов(Приемник, Источник)
	
	Для Каждого Адресат Из Источник Цикл
		НоваяСтрока = Приемник.Добавить();
		НоваяСтрока.АдресЭлектроннойПочты = Адресат.Адрес;
		НоваяСтрока.Представление = Адресат.ОтображаемоеИмя;
	КонецЦикла;
	
КонецПроцедуры

Процедура ЗаполнитьТекстПисьма(ТранспортноеСообщение, Сообщение)
	
	Для Каждого ТекстПисьма Из Сообщение.Тексты Цикл
		Если ЗначениеЗаполнено(ТекстПисьма.Текст) Тогда
			ТранспортноеСообщение.ТекстПисьма = ОбщегоНазначенияЭДКОКлиентСервер.ЗаменитьНечитаемыеСимволы(ТекстПисьма.Текст);
			Прервать;
		КонецЕсли;	
	КонецЦикла;
	
КонецПроцедуры

Процедура ЗаполнитьДополнительныеРеквизитыЗаголовка(ТранспортноеСообщение, Сообщение)
	
	ТипыДополнительныхРеквизитов = Новый Соответствие;
	ТипыДополнительныхРеквизитов.Вставить("X-Tax-System", Перечисления.ТипыДополнительныхРеквизитов.ИмяПередающейСистемы);
	ТипыДополнительныхРеквизитов.Вставить("X-Tax-Type", Перечисления.ТипыДополнительныхРеквизитов.ТипПередаваемойИнформации);
	ТипыДополнительныхРеквизитов.Вставить("X-Tax-Reciever", Перечисления.ТипыДополнительныхРеквизитов.ИдентификаторПолучателя);
	ТипыДополнительныхРеквизитов.Вставить("X-Tax-Sender", Перечисления.ТипыДополнительныхРеквизитов.ИдентификаторОтправителя);
	ТипыДополнительныхРеквизитов.Вставить("X-Message-ID", Перечисления.ТипыДополнительныхРеквизитов.ИдентификаторПервичногоСообщения);
	ТипыДополнительныхРеквизитов.Вставить("X-PFR-Message-ID", "X-PFR-Message-ID");
	
	Для Каждого ТипРеквизита Из ТипыДополнительныхРеквизитов Цикл
		ПолеЗаголовка = Сообщение.ПолучитьПолеЗаголовка(ТипРеквизита.Ключ, "");
		Если ЗначениеЗаполнено(ПолеЗаголовка) Тогда 
			НоваяСтрока = ТранспортноеСообщение.ДополнительныеРеквизитыЗаголовка.Добавить();
			НоваяСтрока.Тип = ТипРеквизита.Значение;
			НоваяСтрока.Значение = ПолеЗаголовка;
		КонецЕсли;
	КонецЦикла;
	
КонецПроцедуры

Процедура РазобратьВложения(ТранспортноеСообщение, Сообщение)
	
	Для Каждого Вложение Из Сообщение.Вложения Цикл
		ДобавитьТранспортныйКонтейнерИзДвоичныхДанных(ТранспортноеСообщение, Вложение.Данные, Вложение.ИмяФайла);
	КонецЦикла;
	
КонецПроцедуры

Функция ДобавитьТранспортныйКонтейнерИзДвоичныхДанных(ТранспортноеСообщение, Данные, ИмяФайла, Размер = Неопределено) Экспорт
	
	ОбработанноеИмяФайла = ОбщегоНазначенияЭДКОКлиентСервер.ЗаменитьНечитаемыеСимволы(ИмяФайла);
	
	НачатьТранзакцию();
	
	НовыйНаборЗаписей = РегистрыСведений.ТранспортныеКонтейнеры.СоздатьНаборЗаписей();
	НовыйНаборЗаписей.Отбор.ТранспортноеСообщение.Установить(ТранспортноеСообщение);
	НовыйНаборЗаписей.Отбор.ИмяФайла.Установить(ОбработанноеИмяФайла);
	
	НоваяЗапись = НовыйНаборЗаписей.Добавить();
	НоваяЗапись.ТранспортноеСообщение = ТранспортноеСообщение;
	НоваяЗапись.ИмяФайла = ОбработанноеИмяФайла;
	НоваяЗапись.Размер = Данные.Размер();
	
	КомпонентыИмениФайла = ОбщегоНазначенияКлиентСервер.РазложитьПолноеИмяФайла(ОбработанноеИмяФайла);
	УникальноеИмяФайлаБезРасширения = ОбщегоНазначенияЭДКОКлиентСервер.УникальнаяСтрока(КомпонентыИмениФайла.ИмяБезРасширения, 150);
	РасширениеФайлаБезТочки = Сред(КомпонентыИмениФайла.Расширение, 2);
	
	НовыйНаборЗаписей.Записать(Истина);
	
	АдресФайла = ПоместитьВоВременноеХранилище(Данные, Новый УникальныйИдентификатор);
	
	МассивФайлов = ОбщегоНазначенияЭДКО.ПрикрепленныеФайлыКОбъектуИзСправочника(
		ТранспортноеСообщение,
		"ТранспортноеСообщениеКонтейнерыПрисоединенныеФайлы",
		ОбработанноеИмяФайла);
	
	Если МассивФайлов.Количество() > 0 Тогда
		ПрисоединенныйФайл = МассивФайлов[0];
		ИнформацияОбОбновляемомФайле = Новый Структура;
		ИнформацияОбОбновляемомФайле.Вставить("АдресФайлаВоВременномХранилище", АдресФайла);
		ИнформацияОбОбновляемомФайле.Вставить("АдресВременногоХранилищаТекста", Неопределено);
		ИнформацияОбОбновляемомФайле.Вставить("ИмяБезРасширения", 				УникальноеИмяФайлаБезРасширения);
		ИнформацияОбОбновляемомФайле.Вставить("Расширение", 					РасширениеФайлаБезТочки);
		
		РаботаСФайлами.ОбновитьФайл(ПрисоединенныйФайл, ИнформацияОбОбновляемомФайле);
		
	Иначе
		ПараметрыФайла = Новый Структура;
		ПараметрыФайла.Вставить("Автор", 						Неопределено);
		ПараметрыФайла.Вставить("ВладелецФайлов", 				ТранспортноеСообщение);
		ПараметрыФайла.Вставить("ИмяБезРасширения", 			УникальноеИмяФайлаБезРасширения);
		ПараметрыФайла.Вставить("РасширениеБезТочки", 			РасширениеФайлаБезТочки);
		ПараметрыФайла.Вставить("ВремяИзмененияУниверсальное", 	Неопределено);
		
		НоваяСсылкаНаФайл = РаботаСФайлами.НоваяСсылкаНаФайл(ТранспортноеСообщение, "ТранспортноеСообщениеКонтейнерыПрисоединенныеФайлы");
		ПрисоединенныйФайл = РаботаСФайлами.ДобавитьФайл(ПараметрыФайла, АдресФайла,,, НоваяСсылкаНаФайл);
	КонецЕсли;
	
	ПрисоединенныйФайлОбъект = ПрисоединенныйФайл.ПолучитьОбъект();
	
	ПрисоединенныйФайлОбъект.ИсходноеИмяФайла = ОбработанноеИмяФайла;
	
	ПрисоединенныйФайлОбъект.Записать();
	
	ЗафиксироватьТранзакцию();
	УдалитьИзВременногоХранилища(АдресФайла);
	
КонецФункции

Функция ИменаПользователейИБКоторымРазрещенаРаботаСУчетнойЗаписью(УчетнаяЗапись)

	Результат = Новый Массив;

	Запрос = Новый Запрос;
	Запрос.УстановитьПараметр("УчетнаяЗапись", УчетнаяЗапись);
	
	Запрос.Текст = 
	"ВЫБРАТЬ РАЗЛИЧНЫЕ
	|	ПользователиУчетныхЗаписейДокументооборота.Пользователь.ИдентификаторПользователяИБ КАК ИдентификаторПользователяИБ
	|ИЗ
	|	РегистрСведений.ПользователиУчетныхЗаписейДокументооборота КАК ПользователиУчетныхЗаписейДокументооборота
	|ГДЕ
	|	ПользователиУчетныхЗаписейДокументооборота.УчетнаяЗапись = &УчетнаяЗапись";
	
	УстановитьПривилегированныйРежим(Истина);
	
	Выборка = Запрос.Выполнить().Выбрать();
	Пока Выборка.Следующий() Цикл
		
		Если НЕ ЗначениеЗаполнено(Выборка.ИдентификаторПользователяИБ) Тогда
			Продолжить;
		КонецЕсли;
		
		ПользовательИБ = ПользователиИнформационнойБазы.НайтиПоУникальномуИдентификатору(Выборка.ИдентификаторПользователяИБ);
		Если ПользовательИБ = Неопределено Тогда
			Продолжить;
		КонецЕсли;
		
		Результат.Добавить(ПользовательИБ.Имя);
	
	КонецЦикла;

	УстановитьПривилегированныйРежим(Ложь);

	Возврат Результат;

КонецФункции

Процедура ИнициализироватьСохранениеОшибокОбмена(УчетнаяЗапись)

	ДлительнаяОтправкаКлиентСервер.ОчиститьПараметрыДлительнойОтправки();
	
	// Установим организацию из учетной записи  и идентификатор получателя сообщений об ошибках,
	// чтобы при последующих вызовах они накапливались в списке.
	ДлительнаяОтправкаКлиентСервер.ИзменитьПараметрыДлительнойОтправки("ТекущаяОрганизация", ОрганизацияУчетнойЗаписи(УчетнаяЗапись));
	
	ДлительнаяОтправкаКлиентСервер.ИзменитьПараметрыДлительнойОтправки("ИдентификаторПолучателя", Новый УникальныйИдентификатор());

КонецПроцедуры

Процедура СохранитьОшибкиОбмена(УчетнаяЗапись)

	Ошибки = ДлительнаяОтправкаКлиентСервер.ЗначениеПараметраДлительнойОтправки("Ошибки");
	
	// Определим список пользователей, которые могут работать с учетной записью, чтобы для них сохранять ошибки.
	ИменаПользователейИБ = ИменаПользователейИБКоторымРазрещенаРаботаСУчетнойЗаписью(УчетнаяЗапись);
	
	Если ИменаПользователейИБ.Количество() = 0 Тогда
		// В обычном случае сюда не должны попадать, т.к. нет пользователей, 
		// которым разрешено работать с учетной записью, некому показывать ошибки.
		ТекстСообщения = СтрШаблон(НСтр("ru = 'Отсутствуют пользователи, которым разрешена работа с учетной записью %1'"), УчетнаяЗапись);
		ЗаписатьСобытиеВЖурнал(УровеньЖурналаРегистрации.Предупреждение, ТекстСообщения, УчетнаяЗапись);
		Возврат;
	КонецЕсли;

	Если Ошибки.Количество() > 0 Тогда
		// Подготовим структуру для сохранения информации об ошибках.
		СведенияПоОшибкам = ДлительнаяОтправкаКлиентСервер.НовыеПараметрыСохранения(Ложь);
		СведенияПоОшибкам.Ошибки         = Ошибки;
		СведенияПоОшибкам.ЕстьОшибки     = Истина;
		СведенияПоОшибкам.ЭтоРасшифровка = Истина;
	Иначе
		// На текущей итерации все прошло успешно, очистим возможные ошибки, возникшие на предыдущих итерациях.
		СведенияПоОшибкам = Неопределено;
	КонецЕсли;
	
	Для каждого ИмяПользователя Из ИменаПользователейИБ Цикл
		ДлительнаяОтправкаВызовСервера.СохранитьОшибкиПоследнегоОбмена(СведенияПоОшибкам, ИмяПользователя);
	КонецЦикла;

КонецПроцедуры

#КонецОбласти

#Область ОбменСФТС

///////////////////////////////////////////////////////////////////////////////
// Функции с использованием общих модулей и дублирующие функции для организации
// вызовов общих модулей при документообороте с ФТС

Функция СформироватьФайлОтправкиВФТСССервера(
		КороткоеИмяФайлаВыгрузки,
		СтрокаBase64ФайлаВыгрузки,
		ЭтоАдреса,
		Адрес,
		Кодировка,
		ПараметрыОтправки,
		ВерсияПортала = Неопределено) Экспорт
	
	Если ВерсияПортала = Неопределено Тогда
		ВерсииПортала = Новый Массив;
		ВерсииПортала.Добавить("");
		ВерсииПортала.Добавить("2017");
		
		Результат = Новый Массив;
		Для каждого ПоддерживаемаяВерсияПортала Из ВерсииПортала Цикл
			ФайлОтправки = СформироватьФайлОтправкиВФТСССервера(
				КороткоеИмяФайлаВыгрузки,
				СтрокаBase64ФайлаВыгрузки,
				ЭтоАдреса,
				"",
				Кодировка,
				ПараметрыОтправки,
				ПоддерживаемаяВерсияПортала);
			Результат.Добавить(ФайлОтправки);
		КонецЦикла;
		Если ЭтоАдреса Тогда
			Результат = ПоместитьВоВременноеХранилище(Результат, Адрес);
		КонецЕсли;
		
		Возврат Результат;
	КонецЕсли;
	
	РежимыОтправки = ПараметрыОтправки.РежимыОтправки;
	
	//файл отправки получаем, объединяя файл пакета с файлами начала и окончания POST-запроса
	
	// файл 1
	СодержимоеФайла = "--My1cV8bNdr"
		+ ?(ВерсияПортала = "2017",
		Символы.ПС + "Content-Disposition: form-data; name=""file[0]""; filename=""" + КороткоеИмяФайлаВыгрузки + """",
		Символы.ПС + "Content-Disposition: form-data; name=""ctl55"""
		+ Символы.ПС + Символы.ПС + Символы.ПС + "--My1cV8bNdr"
		+ Символы.ПС + "Content-Disposition: form-data; name=""__EVENTTARGET"""
		+ Символы.ПС + Символы.ПС + "ctl02"
		+ Символы.ПС + "--My1cV8bNdr"
		+ Символы.ПС + "Content-Disposition: form-data; name=""__EVENTARGUMENT"""
		+ Символы.ПС + Символы.ПС + "-|public|UploadFile"
		+ Символы.ПС + "--My1cV8bNdr"
		+ Символы.ПС + "Content-Disposition: form-data; name=""TextFieldNote"""
		+ Символы.ПС + Символы.ПС + Символы.ПС + "--My1cV8bNdr"
		+ Символы.ПС + "Content-Disposition: form-data; name=""DropDownCreateVersion"""
		+ Символы.ПС + Символы.ПС + ПараметрыОтправки.ВерсияФормированияВыгрузки + "/" + ПараметрыОтправки.ВерсияОтправкиПортала
		+ Символы.ПС + "--My1cV8bNdr"
		+ Символы.ПС + "Content-Disposition: form-data; name=""actionUpload""; filename=""" + КороткоеИмяФайлаВыгрузки + """")
		+ Символы.ПС + "Content-Type: text/xml"
		+ Символы.ПС + Символы.ПС;
	
	// запишем файл в кодировке "UTF-8", затем удалим из него сигнатуру BOM
	ИмяФайлаЗапросаНачалоUTF8ССигнатурой = ПолучитьИмяВременногоФайла();
	ЗаписываемыйФайл = Новый ЗаписьТекста(ИмяФайлаЗапросаНачалоUTF8ССигнатурой, "utf-8");
	ЗаписываемыйФайл.Записать(СодержимоеФайла);
	ЗаписываемыйФайл.Закрыть();
	
	ИмяФайлаЗапросаНачало = ПолучитьИмяВременногоФайла();
	ДвоичныеДанныеФайлаЗапросаНачалоUTF8ССигнатурой = Новый ДвоичныеДанные(ИмяФайлаЗапросаНачалоUTF8ССигнатурой);
	Данные64ФайлаЗапросаНачалоUTF8ССигнатурой = Base64Строка(ДвоичныеДанныеФайлаЗапросаНачалоUTF8ССигнатурой);
	Данные64ФайлаЗапросаНачалоUTF8БезСигнатуры = Сред(Данные64ФайлаЗапросаНачалоUTF8ССигнатурой, 5);
	ДвоичныеДанныеФайлаЗапросаНачалоUTF8БезСигнатуры = Base64Значение(Данные64ФайлаЗапросаНачалоUTF8БезСигнатуры);
	ДвоичныеДанныеФайлаЗапросаНачалоUTF8БезСигнатуры.Записать(ИмяФайлаЗапросаНачало);
	ОперацииСФайламиЭДКО.УдалитьВременныйФайл(ИмяФайлаЗапросаНачалоUTF8ССигнатурой);
	
	// файл 2
	ИмяФайлаВыгрузки = ПолучитьИмяВременногоФайла();
	
	Если ЭтоАдреса Тогда
		ПолучитьИзВременногоХранилища(СтрокаBase64ФайлаВыгрузки).Записать(ИмяФайлаВыгрузки);
	Иначе
		ДвоичныеДанныеФайлаВыгрузки = Base64Значение(СтрокаBase64ФайлаВыгрузки);
		ДвоичныеДанныеФайлаВыгрузки.Записать(ИмяФайлаВыгрузки);
	КонецЕсли;
	
	Если Кодировка <> Неопределено Тогда
		ТекстовыйФайлВыгрузки = Новый ЧтениеТекста(ИмяФайлаВыгрузки, "windows-1251");
		ТекстВыгрузки = ТекстовыйФайлВыгрузки.Прочитать();
		ТекстовыйФайлВыгрузки.Закрыть();
		ТекстовыйФайлВыгрузки = Новый ЗаписьТекста(ИмяФайлаВыгрузки, Кодировка);
		ТекстовыйФайлВыгрузки.Записать(ТекстВыгрузки);
		ТекстовыйФайлВыгрузки.Закрыть();
		
		// в случае кодировки "UTF-8" удалим из файла сигнатуру BOM
		Если нрег(Кодировка) = "utf-8" Тогда
			ДанныеФайлаВыгрузкиUTF8ССигнатурой = Новый ДвоичныеДанные(ИмяФайлаВыгрузки);
			Данные64ФайлаВыгрузкиUTF8ССигнатурой = Base64Строка(ДанныеФайлаВыгрузкиUTF8ССигнатурой);
			Данные64ФайлаВыгрузкиUTF8БезСигнатуры = Сред(Данные64ФайлаВыгрузкиUTF8ССигнатурой, 5);
			ДанныеФайлаВыгрузкиUTF8БезСигнатуры = Base64Значение(Данные64ФайлаВыгрузкиUTF8БезСигнатуры);
			ДанныеФайлаВыгрузкиUTF8БезСигнатуры.Записать(ИмяФайлаВыгрузки);
		КонецЕсли;
	КонецЕсли;
	
	// файл 3
	СодержимоеФайла = ?(ВерсияПортала = "2017", "", Символы.ПС + Символы.ПС + "--My1cV8bNdr"
		+ Символы.ПС + "Content-Disposition: form-data; name=""submitDirectEventConfig"""
		+ Символы.ПС + Символы.ПС + "{""config"":{""extraParams"":{""target"":""" + РежимыОтправки.ИдентификаторНеПринятойОтправкиНаСервере + """}}}"
		+ Символы.ПС + "--My1cV8bNdr"
		+ Символы.ПС + "Content-Disposition: form-data; name=""__ExtNetDirectEventMarker"""
		+ Символы.ПС + Символы.ПС + "delta=true")
		+ Символы.ПС + "--My1cV8bNdr--";
	
	// запишем файл в кодировке "UTF-8", затем удалим из него сигнатуру BOM
	ИмяФайлаЗапросаОкончаниеUTF8ССигнатурой = ПолучитьИмяВременногоФайла();
	ЗаписываемыйФайл = Новый ЗаписьТекста(ИмяФайлаЗапросаОкончаниеUTF8ССигнатурой, "utf-8");
	ЗаписываемыйФайл.Записать(СодержимоеФайла);
	ЗаписываемыйФайл.Закрыть();
	
	ИмяФайлаЗапросаОкончание = ПолучитьИмяВременногоФайла();
	ДвоичныеДанныеФайлаЗапросаОкончаниеUTF8ССигнатурой = Новый ДвоичныеДанные(ИмяФайлаЗапросаОкончаниеUTF8ССигнатурой);
	Данные64ФайлаЗапросаОкончаниеUTF8ССигнатурой = Base64Строка(ДвоичныеДанныеФайлаЗапросаОкончаниеUTF8ССигнатурой);
	Данные64ФайлаЗапросаОкончаниеUTF8БезСигнатуры = Сред(Данные64ФайлаЗапросаОкончаниеUTF8ССигнатурой, 5);
	ДвоичныеДанныеФайлаЗапросаОкончаниеUTF8БезСигнатуры = Base64Значение(Данные64ФайлаЗапросаОкончаниеUTF8БезСигнатуры);
	ДвоичныеДанныеФайлаЗапросаОкончаниеUTF8БезСигнатуры.Записать(ИмяФайлаЗапросаОкончание);
	ОперацииСФайламиЭДКО.УдалитьВременныйФайл(ИмяФайлаЗапросаОкончаниеUTF8ССигнатурой);
	
	// объединение
	МассивИмен = Новый Массив;
	МассивИмен.Добавить(ИмяФайлаЗапросаНачало);
	МассивИмен.Добавить(ИмяФайлаВыгрузки);
	МассивИмен.Добавить(ИмяФайлаЗапросаОкончание);
	ИмяФайлаОтправки = ПолучитьИмяВременногоФайла();
	ОбъединитьФайлы(МассивИмен, ИмяФайлаОтправки);
	
	Если ЭтоАдреса Тогда
		Результат = Новый ДвоичныеДанные(ИмяФайлаОтправки);
		Если Адрес <> "" Тогда
			Результат = ПоместитьВоВременноеХранилище(Результат, Адрес);
		КонецЕсли;
	Иначе
		ДвоичныеДанныеФайлаОтправки = Новый ДвоичныеДанные(ИмяФайлаОтправки);
		Результат = Base64Строка(ДвоичныеДанныеФайлаОтправки);
	КонецЕсли;
	
	Возврат Результат;
	
КонецФункции

//////////////////////////////////////////////////////////////////////////////////
//// ДОКУМЕНТООБОРОТ С ФТС, код без использования вызовов общих модулей
////

Функция ОбновитьРезультатКонкретнойОтправкиФТСССервера(Знач ОтправкаСсылка, Знач ОтправкаОбъектЗагружен, Знач ПараметрыОтправки, ПараметрыСоединения, Знач ЭтоВызовИзСписка = Ложь) Экспорт
	
	Если ОтправкаОбъектЗагружен Тогда
		ОсновныеРеквизитыОтправки = Новый Структура("ОтчетСсылка, ИмяФайлаВыгрузки, ИдентификаторОтправкиНаСервере, СтатусОтправки",
			ОтправкаСсылка.ОтчетСсылка, ОтправкаСсылка.ИмяФайлаВыгрузки, ОтправкаСсылка.ИдентификаторОтправкиНаСервере, ОтправкаСсылка.СтатусОтправки);
	Иначе
		ОсновныеРеквизитыОтправки = ЗначенияРеквизитовОбъекта(ОтправкаСсылка, "ОтчетСсылка, ИмяФайлаВыгрузки, ИдентификаторОтправкиНаСервере, СтатусОтправки");
	КонецЕсли;
	Если НЕ ЗначениеЗаполнено(ПараметрыСоединения.ИдентификаторОтправкиНаСервере) Тогда
		ПараметрыСоединения.ИдентификаторОтправкиНаСервере = ОсновныеРеквизитыОтправки.ИдентификаторОтправкиНаСервере;
	КонецЕсли;
	
	РезультатОтправки = ПолучитьРезультатОтправкиФТС(ОсновныеРеквизитыОтправки, ПараметрыОтправки, ПараметрыСоединения, ЭтоВызовИзСписка, ОтправкаСсылка);
	
	ПараметрыСоединения.СоединениеHTTP = Неопределено;
	
	СохранитьРезультатОтправкиФТС(ОтправкаСсылка, РезультатОтправки, ПараметрыОтправки);
	
	Возврат (РезультатОтправки <> Неопределено);
	
КонецФункции

Функция СохранитьРезультатОтправкиФТС(ОтправкаСсылка, РезультатОтправки, ПараметрыОтправки)
	
	Если РезультатОтправки <> Неопределено Тогда
		ОтправкаОбъект = ОтправкаСсылка.ПолучитьОбъект();
		Если СокрЛП(РезультатОтправки.ИдентификаторОтправкиНаСервере) <> СокрЛП(ОтправкаОбъект.ИдентификаторОтправкиНаСервере)
			ИЛИ РезультатОтправки.СтатусОтправки <> ОтправкаОбъект.СтатусОтправки
			ИЛИ РезультатОтправки.СтатусОтправки <> Перечисления.СтатусыОтправки.Сдан
			ИЛИ РезультатОтправки.НаКонтроле <> ОтправкаОбъект.НаКонтроле ИЛИ РезультатОтправки.НаКонтроле
			ИЛИ НЕ ЗначениеЗаполнено(РезультатОтправки.Протокол) ИЛИ НЕ ЗначениеЗаполнено(ОтправкаОбъект.Протокол)
			ИЛИ РезультатОтправки.Протокол <> ОтправкаОбъект.Протокол.Получить() Тогда
			
			ИдентификаторАннулированнойОтправки = ?(СокрЛП(РезультатОтправки.ИдентификаторОтправкиНаСервере) <>
				СокрЛП(ОтправкаОбъект.ИдентификаторОтправкиНаСервере), ОтправкаОбъект.ИдентификаторОтправкиНаСервере, "");
			ОтчетСсылка = ОтправкаОбъект.ОтчетСсылка;
			
			ОтправкаОбъект.ИдентификаторОтправкиНаСервере = РезультатОтправки.ИдентификаторОтправкиНаСервере; // аннулирование с подачей
			ОтправкаОбъект.ДатаПолученияРезультата = РезультатОтправки.ДатаПолученияРезультата;
			ОтправкаОбъект.СтатусОтправки = РезультатОтправки.СтатусОтправки;
			ОтправкаОбъект.Протокол = Новый ХранилищеЗначения(РезультатОтправки.Протокол);
			ОтправкаОбъект.НаКонтроле = РезультатОтправки.НаКонтроле;
			ОтправкаОбъект.Квитанция = Новый ХранилищеЗначения(РезультатОтправки.Квитанция);
			ОтправкаОбъект.Записать();
			
			Если ЗначениеЗаполнено(ИдентификаторАннулированнойОтправки) Тогда
				АннулированнаяОтправкаСсылка = Неопределено;
				Запрос = Новый Запрос;
				Запрос.Текст =
					"ВЫБРАТЬ РАЗРЕШЕННЫЕ ПЕРВЫЕ 1
					|	ОтправкиФТС.Ссылка КАК Ссылка
					|ИЗ
					|	Справочник.ОтправкиФТС КАК ОтправкиФТС
					|ГДЕ
					|	ОтправкиФТС.ОтчетСсылка = &ОтчетСсылка
					|	И ОтправкиФТС.ИдентификаторОтправкиНаСервере = &ИдентификаторОтправкиНаСервере
					|	И ОтправкиФТС.ПометкаУдаления = ЛОЖЬ
					|
					|УПОРЯДОЧИТЬ ПО
					|	ОтправкиФТС.ДатаОтправки УБЫВ";
				Запрос.Параметры.Вставить("ОтчетСсылка", 					ОтчетСсылка);
				Запрос.Параметры.Вставить("ИдентификаторОтправкиНаСервере", ИдентификаторАннулированнойОтправки);
				
				РезультатЗапроса = Запрос.Выполнить();
				Если НЕ РезультатЗапроса.Пустой() Тогда
					ЗаписьЗапроса = РезультатЗапроса.Выгрузить()[0];
					АннулированнаяОтправкаСсылка = ЗаписьЗапроса.Ссылка;
				КонецЕсли;
				
				Если ЗначениеЗаполнено(АннулированнаяОтправкаСсылка) Тогда
					Протокол = ГенерироватьHTMLПротоколаОбработкиФТС(
						Новый Структура("Протокол", ""),
						НСтр("ru = 'Отчет аннулирован пользователем.'"),
						Новый Цвет(255, 0, 0),
						Перечисления.СтатусыОтправки.НеПринят,
						Ложь,
						ПараметрыОтправки,
						ОтчетСсылка);
					
					ОтправкаОбъект = АннулированнаяОтправкаСсылка.ПолучитьОбъект();
					ОтправкаОбъект.СтатусОтправки = Перечисления.СтатусыОтправки.НеПринят;
					ОтправкаОбъект.Протокол = Новый ХранилищеЗначения(Протокол);
					ОтправкаОбъект.Записать();
				КонецЕсли;
			КонецЕсли;
		КонецЕсли;
		Возврат Истина;
	Иначе
		Возврат Ложь;
	КонецЕсли;
	
КонецФункции

Функция ПолучитьРезультатОтправкиФТС(ОсновныеРеквизитыОтправки, ПараметрыОтправки, ПараметрыСоединения, ЭтоВызовИзСписка, ОтправкаСсылка)
	
	СтатусОтчета = "";
	ЦветСтатуса = Новый Цвет(0, 0, 192);
	СтатусОтправки = Перечисления.СтатусыОтправки.Отправлен;
	НаКонтроле = Ложь;
	
	//запрос результатов обработки
	ПараметрыСоединения.ОписаниеОшибкиСервером = "";
	ПараметрыСоединения.ПовторятьСоединение = Ложь;
	ПараметрыСоединения.ПовторятьЗапросПароля = Ложь;
	
	ЗаголовокОтчета = ПолучитьИнформациюОбОтчетеНаПорталеФТС(ПараметрыОтправки, ПараметрыСоединения);
	
	УспешноОбработан = Неопределено;
	ОтчетНаКонтроле = Ложь;
	ОтчетАннулирован = Ложь;
	ОтказаноВАннулировании = Ложь;
	ЭтоЧерновик = Ложь;
	Если ЗаголовокОтчета <> Неопределено Тогда
		УспешноОбработан = УспешноОбработанОтчетФТС(
			ЗаголовокОтчета.Состояние,
			ЗаголовокОтчета.РасширенноеСостояниеНаКонтроле,
			ПараметрыСоединения.ВерсияПортала,
			ОтчетНаКонтроле,
			ОтчетАннулирован,
			ОтказаноВАннулировании,
			ЭтоЧерновик);
	КонецЕсли;
	
	Если ЗаголовокОтчета <> Неопределено И НЕ ПараметрыСоединения.ПовторятьСоединение Тогда
		
		// получен протокол обработки и отсутствовали ошибки соединения
		
		// анализируем содержимое страницы протокола обработки
		Если УспешноОбработан = Истина Тогда
			// отчет принят
			СтатусОтправки = Перечисления.СтатусыОтправки.Сдан;
			СтатусОтчета = НСтр("ru = 'Отчет принят, ошибок не обнаружено.'");
			ЦветСтатуса = Новый Цвет(0, 179, 16);
			
		ИначеЕсли УспешноОбработан = Ложь ИЛИ (УспешноОбработан = Неопределено И ЭтоЧерновик) Тогда
			// отчет не принят
			СтатусОтправки = Перечисления.СтатусыОтправки.НеПринят;
			Если УспешноОбработан = Неопределено И ЭтоЧерновик Тогда
				СтатусОтчета = НСтр("ru = 'Отчет не принят в обработку, повторите отправку.'");
			ИначеЕсли ОтчетНаКонтроле Тогда
				НаКонтроле = Истина;
				СтатусОтчета = НСтр("ru = 'Отчет зарегистрирован, поставлен на контроль с комментарием должностного лица на портале ФТС.'");
			ИначеЕсли ОтчетАннулирован Тогда
				СтатусОтчета = НСтр("ru = 'Отчет аннулирован пользователем.'");
			ИначеЕсли ОтказаноВАннулировании Тогда
				СтатусОтчета = НСтр("ru = 'Отказано в аннулировании отчета.'");
			Иначе
				СтатусОтчета = НСтр("ru = 'Отчет не принят, обнаружены ошибки.'");
			КонецЕсли;
			ЦветСтатуса = Новый Цвет(255, 0, 0);
			
		Иначе
			// отчет еще не обработан
			СтатусОтчета = НСтр("ru = 'Находится в обработке, попробуйте через некоторое время использовать в отчете кнопку ""Обновить""'");
		КонецЕсли;
		
		Протокол = ГенерироватьHTMLПротоколаОбработкиФТС(ЗаголовокОтчета, СтатусОтчета, ЦветСтатуса, СтатусОтправки, НаКонтроле, ПараметрыОтправки, ОсновныеРеквизитыОтправки.ОтчетСсылка);
		
		//заполняем результирующую структуру
		Результат = Новый Структура("ДатаПолученияРезультата, ИдентификаторОтправкиНаСервере, СтатусОтправки, Протокол, НаКонтроле, Квитанция",
			ТекущаяДатаСеанса(), ЗаголовокОтчета.ИдентификаторОтправки, СтатусОтправки, Протокол, НаКонтроле, ЗаголовокОтчета.Квитанция);
		
	ИначеЕсли ЗначениеЗаполнено(ПараметрыСоединения.ОписаниеОшибкиСервером) ИЛИ НЕ ПараметрыСоединения.ПовторятьСоединение Тогда
		
		Если ЗначениеЗаполнено(ПараметрыСоединения.ОписаниеОшибкиСервером) Тогда
			ПараметрыСоединения.ОписаниеОшибкиСервером = ТекстОшибкиБезТехническойИнформацииФТС(ПараметрыСоединения.ОписаниеОшибкиСервером);
			
			ТекстСообщения = СтрШаблон(
				НСтр("ru = 'Результаты отправки не получены с портала. %1'"),
				ПараметрыСоединения.ОписаниеОшибкиСервером);
			
			Если ЭтоВызовИзСписка Тогда
				ПредставлениеОтчетаОтправки = ПредставлениеОтчета(ОтправкаСсылка.ОтчетСсылка);
				
				ТекстСообщения = СтрШаблон(
					НСтр("ru = '%1 Попробуйте через некоторое время использовать кнопку ""Обновить""  в отчете %2.'"),
					ТекстСообщения,
					ПредставлениеОтчетаОтправки);
			КонецЕсли;
			
			СообщитьПользователю(ТекстСообщения, ПараметрыСоединения.СообщенияПользователю);
			
		Иначе
			ТекстСообщения = НСтр("ru = 'Не удалось получить результаты отправки с портала Федеральной таможенной службы.'");
			
			Если ЭтоВызовИзСписка Тогда
				ПредставлениеОтчетаОтправки = ПредставлениеОтчета(ОтправкаСсылка.ОтчетСсылка);
				
				ТекстСообщения = СтрШаблон(
					НСтр("ru = '%1 Возможно, портал перегружен. Попробуйте через некоторое время использовать кнопку ""Обновить"" в отчете %2.'"),
					ТекстСообщения,
					ПредставлениеОтчетаОтправки);
			КонецЕсли;
			
			СообщитьПользователю(ТекстСообщения, ПараметрыСоединения.СообщенияПользователю);
		КонецЕсли;
		
		Результат = Неопределено;
		
	Иначе
		
		// возможно, проблемы с доступом в интернет
		Результат = Неопределено;
		
	КонецЕсли;
	
	ПараметрыСоединения.ОписаниеОшибкиСервером = "";
	
	Возврат Результат;
	
КонецФункции

Функция ОтправкаНаСерверФТС(ФайлыИДанныеОтправки, ОтчетСсылка, ПараметрыОтправки, ПараметрыСоединения)
	
	ПараметрыСоединения.ОписаниеОшибкиСервером = "";
	ПараметрыСоединения.ПовторятьСоединение = Ложь;
	ПараметрыСоединения.ПовторятьЗапросПароля = Ложь;
	
	ОтправкаПроизведена = ОтправитьВыгрузкуНаСерверФТС(ФайлыИДанныеОтправки, ПараметрыОтправки, ПараметрыСоединения);
	
	Если ОтправкаПроизведена Тогда
		
		ПараметрыСоединения.ПовторятьСоединение = Ложь;
		
		Возврат Истина;
		
	ИначеЕсли (ЗначениеЗаполнено(ПараметрыСоединения.ОписаниеОшибкиСервером) ИЛИ НЕ ПараметрыСоединения.ПовторятьСоединение) Тогда
		
		ЭтоРегламентированныйОтчет = (ТипЗнч(ОтчетСсылка) = Тип("ДокументСсылка.РегламентированныйОтчет") ИЛИ ТипЗнч(ОтчетСсылка) = Тип("Неопределено"));
		
		Если ЗначениеЗаполнено(ПараметрыСоединения.ОписаниеОшибкиСервером) Тогда
			
			ЭтоОшибкиПроверки = Ложь;
			ПараметрыСоединения.ОписаниеОшибкиСервером = ТекстОшибкиБезТехническойИнформацииФТС(ПараметрыСоединения.ОписаниеОшибкиСервером, ЭтоОшибкиПроверки);
			
			Если ЭтоОшибкиПроверки Тогда
				ПараметрыСоединения.ОписаниеОшибокПроверки = ПараметрыСоединения.ОписаниеОшибкиСервером;
				
			Иначе
				//отправленный файл не был принят сервером, получено описание ошибки
				Если Прав(ПараметрыСоединения.ОписаниеОшибкиСервером, 1) <> "." И Прав(ПараметрыСоединения.ОписаниеОшибкиСервером, 1) <> "!" Тогда
					ПараметрыСоединения.ОписаниеОшибкиСервером = ПараметрыСоединения.ОписаниеОшибкиСервером + ".";
				КонецЕсли;
				СообщитьПользователю(ПодставитьПараметрыВСтроку(
						НСтр("ru = 'Отчет не принят порталом. %1'"),
						ПараметрыСоединения.ОписаниеОшибкиСервером) + "
					|" + ПодставитьПараметрыВСтроку(?(ЭтоРегламентированныйОтчет,
						НСтр("ru = 'Для отправки отчета возможно выгрузить его в файл, используя кнопку ""Выгрузить"", далее отправить этот файл на портале %1'"),
						НСтр("ru = 'Существует возможность отправить файл выгрузки отчета на портале %1'")),
						"https://" + ПараметрыОтправки.АдресСервера + "/FtsPersonalCabinetWeb2017/#?service=StatEp&start=0&view=List"),
					ПараметрыСоединения.СообщенияПользователю);
			КонецЕсли;
			
		Иначе
			// в ответе сервера отсутствует описание ошибки и установлен признак повтора соединения
			СообщитьПользователю(НСтр("ru = 'Отчет не удалось отправить на портал Федеральной таможенной службы.'")
				+ " " + ПодставитьПараметрыВСтроку(?(ЭтоРегламентированныйОтчет,
					НСтр("ru = 'Для отправки отчета возможно выгрузить его в файл, используя кнопку ""Выгрузить"", далее отправить этот файл на портале %1'"),
					НСтр("ru = 'Существует возможность отправить файл выгрузки отчета на портале %1'")),
					"https://" + ПараметрыОтправки.АдресСервера + "/FtsPersonalCabinetWeb2017/#?service=StatEp&start=0&view=List"),
				ПараметрыСоединения.СообщенияПользователю);
		КонецЕсли;
		
	КонецЕсли;
	
	Возврат Ложь;
	
КонецФункции

Функция ОтправкаПодписанногоЗапросаНаСерверФТС(ФайлыИДанныеОтправки, ОтчетСсылка, ПараметрыОтправки, ПараметрыСоединения)
	
	ПараметрыСоединения.ОписаниеОшибкиСервером = "";
	ПараметрыСоединения.ПовторятьСоединение = Ложь;
	
	ОтправкаПроизведена = ОтправитьПодписанныйЗапросНаСерверФТС(ФайлыИДанныеОтправки, ПараметрыОтправки, ПараметрыСоединения);
	
	Если ОтправкаПроизведена Тогда
		
		ПараметрыСоединения.ПовторятьСоединение = Ложь;
		
	ИначеЕсли ПараметрыСоединения.ОрганизацияПодтверждена <> Истина Тогда
		
		ЭтоРегламентированныйОтчет = (ТипЗнч(ОтчетСсылка) = Тип("ДокументСсылка.РегламентированныйОтчет") ИЛИ ТипЗнч(ОтчетСсылка) = Тип("Неопределено"));
		
		Если ЗначениеЗаполнено(ПараметрыСоединения.ОписаниеОшибкиСервером) Тогда
			
			ЭтоОшибкиПроверки = Ложь;
			ПараметрыСоединения.ОписаниеОшибкиСервером = ТекстОшибкиБезТехническойИнформацииФТС(ПараметрыСоединения.ОписаниеОшибкиСервером, ЭтоОшибкиПроверки);
			
			Если ЭтоОшибкиПроверки Тогда
				ПараметрыСоединения.ОписаниеОшибокПроверки = ПараметрыСоединения.ОписаниеОшибкиСервером;
				
			Иначе
				//отправленный файл не был принят сервером, получено описание ошибки
				Если Прав(ПараметрыСоединения.ОписаниеОшибкиСервером, 1) <> "." И Прав(ПараметрыСоединения.ОписаниеОшибкиСервером, 1) <> "!" Тогда
					ПараметрыСоединения.ОписаниеОшибкиСервером = ПараметрыСоединения.ОписаниеОшибкиСервером + ".";
				КонецЕсли;
				СообщитьПользователю(ПодставитьПараметрыВСтроку(
						НСтр("ru = 'Запрос подтверждения организации не принят порталом. %1'"),
						ПараметрыСоединения.ОписаниеОшибкиСервером)
					+ " " + ПодставитьПараметрыВСтроку(?(ЭтоРегламентированныйОтчет,
						НСтр("ru = 'Для отправки отчета возможно выгрузить его в файл, используя кнопку ""Выгрузить"", далее отправить данный файл на портале %1'"),
						НСтр("ru = 'Существует возможность отправить файл выгрузки отчета на портале %1'")),
						"https://" + ПараметрыОтправки.АдресСервера + "/FtsPersonalCabinetWeb2017/#?service=StatEp&start=0&view=List"),
					ПараметрыСоединения.СообщенияПользователю);
			КонецЕсли;
			
		Иначе
			// в ответе сервера отсутствует описание ошибки и установлен признак повтора соединения
			СообщитьПользователю(НСтр("ru = 'Подписанный запрос подтверждения организации не удалось отправить на портал Федеральной таможенной службы.'")
				+ " " + ПодставитьПараметрыВСтроку(?(ЭтоРегламентированныйОтчет,
					НСтр("ru = 'Для отправки отчета выгрузите его в файл, используя кнопку ""Выгрузить"". Далее отправьте данный файл на портале %1'"),
					НСтр("ru = 'Для отправки отчета отправьте файл выгрузки отчета на портале %1'")),
					"https://" + ПараметрыОтправки.АдресСервера + "/FtsPersonalCabinetWeb2017/#?service=StatEp&start=0&view=List"),
				ПараметрыСоединения.СообщенияПользователю);
		КонецЕсли;
		
	КонецЕсли;
	
	Возврат ОтправкаПроизведена;
	
КонецФункции

Функция ОтправкаПодписиНаСерверФТС(ФайлыИДанныеОтправки, ОтчетСсылка, ПараметрыОтправки, ПараметрыСоединения)
	
	ПараметрыСоединения.ОписаниеОшибкиСервером = "";
	ПараметрыСоединения.ПовторятьСоединение = Ложь;
	
	ОтправкаПроизведена = ОтправитьПодписьНаСерверФТС(ФайлыИДанныеОтправки, ПараметрыОтправки, ПараметрыСоединения);
	
	Если ОтправкаПроизведена Тогда
		
		ПараметрыСоединения.ПовторятьСоединение = Ложь;
		
		//отправленный файл принят сервером
		НоваяЗапись = Справочники.ОтправкиФТС.СоздатьЭлемент();
		НоваяЗапись.ОтчетСсылка = ОтчетСсылка;
		НоваяЗапись.ИдентификаторОтправкиНаСервере = ПараметрыСоединения.ИдентификаторОтправкиНаСервере;
		ДвоичныеДанныеПодписываемойВыгрузки = Новый ДвоичныеДанные(ФайлыИДанныеОтправки.ИмяФайлаПодписываемойВыгрузки);
		НоваяЗапись.ВыгрузкаДляПодписи = Новый ХранилищеЗначения(ДвоичныеДанныеПодписываемойВыгрузки);
		ДвоичныеДанныеПодписи = Новый ДвоичныеДанные(ФайлыИДанныеОтправки.ИмяФайлаПодписи);
		НоваяЗапись.Подпись = Новый ХранилищеЗначения(ДвоичныеДанныеПодписи);
		НоваяЗапись.ИмяФайлаВыгрузки = ФайлыИДанныеОтправки.КороткоеИмяФайлаВыгрузки;
		НоваяЗапись.СтатусОтправки = Перечисления.СтатусыОтправки.Отправлен;
		НоваяЗапись.ДатаОтправки = ТекущаяДатаСеанса();
		НоваяЗапись.Организация = ?(ОтчетСсылка <> Неопределено, ОтчетСсылка.Организация, Неопределено);
		НоваяЗапись.ДатаНачалаПериода = ПараметрыОтправки.ПоляДляЗаписиОтправки.ДатаНачалаПериода;
		НоваяЗапись.ДатаОкончанияПериода = ПараметрыОтправки.ПоляДляЗаписиОтправки.ДатаОкончанияПериода;
		
		Если ОтчетСсылка <> Неопределено И ТипЗнч(ОтчетСсылка) = Тип("ДокументСсылка.РегламентированныйОтчет") Тогда
			НоваяЗапись.ВидОтчета = Справочники.ВидыОтправляемыхДокументов.НайтиПоРеквизиту("Источник", ОтчетСсылка.ИсточникОтчета);
			
		Иначе // электронные представления
			НоваяЗапись.ВидОтчета = ?(ОтчетСсылка <> Неопределено, ОтчетСсылка.ВидОтчета, Неопределено);
		КонецЕсли;
		
		НоваяЗапись.Версия = ПараметрыОтправки.ПоляДляЗаписиОтправки.Версия;
		НоваяЗапись.ПредставлениеПериода = ПредставлениеПериода(НоваяЗапись.ДатаНачалаПериода, КонецДня(НоваяЗапись.ДатаОкончанияПериода), "ФП=Истина");
		НоваяЗапись.ПредставлениеВидаДокумента = ПредставлениеВидаДокумента(НоваяЗапись.Версия);
		НоваяЗапись.Записать();
		
		Результат = НоваяЗапись.Ссылка;
		
		Возврат Результат;
		
	ИначеЕсли (ЗначениеЗаполнено(ПараметрыСоединения.ОписаниеОшибкиСервером) ИЛИ НЕ ПараметрыСоединения.ПовторятьСоединение) Тогда
		
		ЭтоРегламентированныйОтчет = (ТипЗнч(ОтчетСсылка) = Тип("ДокументСсылка.РегламентированныйОтчет") ИЛИ ТипЗнч(ОтчетСсылка) = Тип("Неопределено"));
		
		Если ЗначениеЗаполнено(ПараметрыСоединения.ОписаниеОшибкиСервером) Тогда
			
			ЭтоОшибкиПроверки = Ложь;
			ПараметрыСоединения.ОписаниеОшибкиСервером = ТекстОшибкиБезТехническойИнформацииФТС(ПараметрыСоединения.ОписаниеОшибкиСервером, ЭтоОшибкиПроверки);
			
			Если ЭтоОшибкиПроверки Тогда
				ПараметрыСоединения.ОписаниеОшибокПроверки = ПараметрыСоединения.ОписаниеОшибкиСервером;
				
			Иначе
				//отправленный файл не был принят сервером, получено описание ошибки
				Если Прав(ПараметрыСоединения.ОписаниеОшибкиСервером, 1) <> "." И Прав(ПараметрыСоединения.ОписаниеОшибкиСервером, 1) <> "!" Тогда
					ПараметрыСоединения.ОписаниеОшибкиСервером = ПараметрыСоединения.ОписаниеОшибкиСервером + ".";
				КонецЕсли;
				СообщитьПользователю(ПодставитьПараметрыВСтроку(
						НСтр("ru = 'Отчет не принят порталом. %1'"),
						ПараметрыСоединения.ОписаниеОшибкиСервером) + "
					|" + ПодставитьПараметрыВСтроку(?(ЭтоРегламентированныйОтчет,
						НСтр("ru = 'Для отправки отчета возможно выгрузить его в файл, используя кнопку ""Выгрузить"", далее отправить этот файл на портале %1'"),
						НСтр("ru = 'Существует возможность отправить файл выгрузки отчета на портале %1'")),
						"https://" + ПараметрыОтправки.АдресСервера + "/FtsPersonalCabinetWeb2017/#?service=StatEp&start=0&view=List"),
					ПараметрыСоединения.СообщенияПользователю);
			КонецЕсли;
			
		Иначе
			// в ответе сервера отсутствует описание ошибки и установлен признак повтора соединения
			СообщитьПользователю(НСтр("ru = 'Подписанный пакет не удалось отправить на портал Федеральной таможенной службы.'")
				+ " " + ПодставитьПараметрыВСтроку(?(ЭтоРегламентированныйОтчет,
					НСтр("ru = 'Для отправки отчета возможно выгрузить его в файл, используя кнопку ""Выгрузить"", далее отправить этот файл на портале %1'"),
					НСтр("ru = 'Существует возможность отправить файл выгрузки отчета на портале %1'")),
					"https://" + ПараметрыОтправки.АдресСервера + "/FtsPersonalCabinetWeb2017/#?service=StatEp&start=0&view=List"),
				ПараметрыСоединения.СообщенияПользователю);
		КонецЕсли;
		
	КонецЕсли;
	
	Возврат Неопределено;
	
КонецФункции

Функция ОтправитьПодписанныйЗапросНаСерверФТС(ФайлыИДанныеОтправки, ПараметрыОтправки, ПараметрыСоединения)
	
	ПараметрыСоединения.ПовторятьСоединение = Ложь;
	ЗагрузкаПроизведена = Ложь;
	АдресСервера = ПараметрыСоединения.ТекущийАдресСервера;
	
	СоединениеУстановлено = (ПараметрыСоединения.СоединениеHTTP <> Неопределено);
	
	Если НЕ СоединениеУстановлено Тогда
		
		// устанавливаем соединение с сервером
		ОписаниеОшибкиУстановкиСоединения = "";
		ПараметрыСоединения.СоединениеHTTP = ДокументооборотСКО.УстановитьСоединениеССерверомИнтернета("https://" + АдресСервера, ОписаниеОшибкиУстановкиСоединения);
		
		Если ПараметрыСоединения.СоединениеHTTP = Неопределено Тогда
			ПараметрыСоединения.ПовторятьСоединение = Истина;
			СообщитьПользователю(
				СтрШаблон(НСтр("ru = 'Не удалось установить соединение с сервером:
								|%1'"), ОписаниеОшибкиУстановкиСоединения), ПараметрыСоединения.СообщенияПользователю);
			Возврат Ложь;
		КонецЕсли;
		
		СоединениеУстановлено = Истина;
		
	КонецЕсли;
	
	Если СоединениеУстановлено Тогда
		
		ЗагрузкаПроизведена = ЗагрузитьПодписанныйЗапросНаПорталФТС(ФайлыИДанныеОтправки.ИмяФайлаПодписанногоЗапроса, ПараметрыОтправки, ПараметрыСоединения);
		Если ЗагрузкаПроизведена Тогда
			НастройкиПользователя = НастройкиПользователяФТС(ПараметрыОтправки, ПараметрыСоединения);
			Если НастройкиПользователя <> Неопределено И НЕ НастройкиПользователя.ОтправленЗапросНаПодтверждениеОрганизации И НЕ НастройкиПользователя.ОрганизацияПодтверждена Тогда
				ЗагрузкаПроизведена = Ложь;
			КонецЕсли;
		КонецЕсли;
		
	Иначе
		ПараметрыСоединения.СоединениеHTTP = Неопределено;
	КонецЕсли;
	
	Возврат ЗагрузкаПроизведена;
	
КонецФункции

Функция НастройкиПользователяФТС(
		ПараметрыОтправки,
		ПараметрыСоединения,
		СформироватьПодписываемыйЗапрос = Ложь,
		ПомещатьВоВременноеХранилище = Ложь)
	
	Если ПараметрыСоединения.ВерсияПортала = "2017" Тогда
		Возврат НастройкиПользователяФТС2017(
			ПараметрыОтправки,
			ПараметрыСоединения,
			СформироватьПодписываемыйЗапрос,
			ПомещатьВоВременноеХранилище);
	КонецЕсли;
	
	// получим свойства профиля
	ЗаголовкиHTTP = Новый Соответствие();
	ЗаголовкиHTTP.Вставить("Proxy-Connection", "Keep-Alive");
	ЗаголовкиHTTP.Вставить("Pragma", "no-cache");
	Если ЗначениеЗаполнено(ПараметрыСоединения.ЗначениеCookie) Тогда
		ЗаголовкиHTTP.Вставить("Cookie", ПараметрыСоединения.ЗначениеCookie);
	КонецЕсли;
	
	ИмяФайлаОтвета = ПолучитьИмяВременногоФайла();
	
	Попытка
		РесурсНаСервере = ДокументооборотСКО.ПолучитьЗаменяемоеЗначениеТестовогоСервера1С("/FtsPersonalCabinetWeb/Api/GetConfig", 
																							"{FTS}/FtsPersonalCabinetWeb/Api/GetConfig", 
																							ПараметрыСоединения.СоединениеHTTP);
		ОтветHTTP = ПараметрыСоединения.СоединениеHTTP.Получить(РесурсНаСервере, ИмяФайлаОтвета, ЗаголовкиHTTP);
	Исключение
		
		ПараметрыСоединения.СоединениеHTTP = Неопределено;
		ПараметрыСоединения.ПовторятьСоединение = Истина;
		ОперацииСФайламиЭДКО.УдалитьВременныйФайл(ИмяФайлаОтвета);
		Возврат Неопределено;
		
	КонецПопытки;
	
	ФайлОтвета = Новый ЧтениеТекста(ИмяФайлаОтвета, "utf-8");
	ТекстОтвета = ФайлОтвета.Прочитать();
	ФайлОтвета.Закрыть();
	
	ЗначениеSetCookie = ОтветHTTP.Заголовки.Получить("Set-Cookie");
	ПараметрыСоединения.ЗначениеCookie = ОбновленноеЗначениеCookie(ПараметрыСоединения.ЗначениеCookie, ЗначениеSetCookie);
	
	ОперацииСФайламиЭДКО.УдалитьВременныйФайл(ИмяФайлаОтвета);
	
	// проанализируем свойства профиля
	Результат = Новый Структура("ОрганизацияПодтверждена, ОтправленЗапросНаПодтверждениеОрганизации, ПодписываемыйЗапрос64", Ложь, Ложь, "");
	
	ЭлементОрганизации = """Organizations"":";
	ПозицияОрганизации = СтрНайти(ТекстОтвета, ЭлементОрганизации);
	
	Если ПозицияОрганизации > 0 Тогда
		ПозицияОрганизации = ПозицияОрганизации + СтрДлина(ЭлементОрганизации);
		ТекстОтвета = Сред(ТекстОтвета, ПозицияОрганизации);
		
		Если Лев(ТекстОтвета, 2) <> "[]" Тогда
			ЭлементОрганизацияАктивна = """IsActive"":";
			ПозицияОрганизацияАктивна = СтрНайти(ТекстОтвета, ЭлементОрганизацияАктивна);
			
			Если ПозицияОрганизацияАктивна > 0 Тогда
				ПозицияОрганизацияАктивна = ПозицияОрганизацияАктивна + СтрДлина(ЭлементОрганизацияАктивна);
				
				Результат.ОрганизацияПодтверждена = (Сред(ТекстОтвета, ПозицияОрганизацияАктивна, 4) = "true");
				Результат.ОтправленЗапросНаПодтверждениеОрганизации = НЕ Результат.ОрганизацияПодтверждена;
			КонецЕсли;
		КонецЕсли;
		
	КонецЕсли;
	
	// сформируем запрос
	Если НЕ Результат.ОтправленЗапросНаПодтверждениеОрганизации И СформироватьПодписываемыйЗапрос Тогда
		ЗаголовкиHTTP = Новый Соответствие();
		ЗаголовкиHTTP.Вставить("Proxy-Connection", "Keep-Alive");
		ЗаголовкиHTTP.Вставить("Pragma", "no-cache");
		Если ЗначениеЗаполнено(ПараметрыСоединения.ЗначениеCookie) Тогда
			ЗаголовкиHTTP.Вставить("Cookie", ПараметрыСоединения.ЗначениеCookie);
		КонецЕсли;
		
		ИмяФайлаОтвета = ПолучитьИмяВременногоФайла();
		
		Попытка
			РесурсНаСервере = ДокументооборотСКО.ПолучитьЗаменяемоеЗначениеТестовогоСервера1С("/FtsPersonalCabinetWeb/Api/CreateOrganizationRequest", 
																								"{FTS}/FtsPersonalCabinetWeb/Api/CreateOrganizationRequest", 
																								ПараметрыСоединения.СоединениеHTTP);
			ОтветHTTP = ПараметрыСоединения.СоединениеHTTP.Получить(РесурсНаСервере, ИмяФайлаОтвета, ЗаголовкиHTTP);
		Исключение
			
			ПараметрыСоединения.СоединениеHTTP = Неопределено;
			ПараметрыСоединения.ПовторятьСоединение = Истина;
			ОперацииСФайламиЭДКО.УдалитьВременныйФайл(ИмяФайлаОтвета);
			Возврат Неопределено;
			
		КонецПопытки;
		
		ФайлОтвета = Новый ЧтениеТекста(ИмяФайлаОтвета, "utf-8");
		ТекстОтвета = ФайлОтвета.Прочитать();
		ФайлОтвета.Закрыть();
		
		ЗначениеSetCookie = ОтветHTTP.Заголовки.Получить("Set-Cookie");
		ПараметрыСоединения.ЗначениеCookie = ОбновленноеЗначениеCookie(ПараметрыСоединения.ЗначениеCookie, ЗначениеSetCookie);
		
		ОперацииСФайламиЭДКО.УдалитьВременныйФайл(ИмяФайлаОтвета);
		
		Если Лев(ТекстОтвета, 1) = """" И Прав(ТекстОтвета, 1) = """" Тогда
			ДлинаОтвета = СтрДлина(ТекстОтвета);
			ТекстОтвета = Сред(ТекстОтвета, 2, ДлинаОтвета - 2);
		КонецЕсли;
		ТекстОтвета = СтрЗаменить(ТекстОтвета, "\""", """");
		
		ИмяФайлаЗапроса = ПолучитьИмяВременногоФайла();
		ЗаписываемыйФайл = Новый ЗаписьТекста(ИмяФайлаЗапроса, "utf-8");
		ЗаписываемыйФайл.Записать(ТекстОтвета);
		ЗаписываемыйФайл.Закрыть();
		
		ДанныеЗапроса = Новый ДвоичныеДанные(ИмяФайлаЗапроса);
		ПодписываемыйЗапрос64 = Base64Строка(ДанныеЗапроса);
		ПодписываемыйЗапрос64 = Сред(ПодписываемыйЗапрос64, 5);
		// удалим из файла в кодировке "UTF-8" сигнатуру BOM
		Если ПомещатьВоВременноеХранилище Тогда
			ДанныеЗапроса = Base64Значение(ПодписываемыйЗапрос64);
			Результат.ПодписываемыйЗапрос64 = ПоместитьВоВременноеХранилище(ДанныеЗапроса, Новый УникальныйИдентификатор);
		Иначе
			Результат.ПодписываемыйЗапрос64 = ПодписываемыйЗапрос64;
		КонецЕсли;
		
		ОперацииСФайламиЭДКО.УдалитьВременныйФайл(ИмяФайлаЗапроса);
		
	КонецЕсли;
	
	Возврат Результат;
	
КонецФункции

Функция НастройкиПользователяФТС2017(
		ПараметрыОтправки,
		ПараметрыСоединения,
		СформироватьПодписываемыйЗапрос = Ложь,
		ПомещатьВоВременноеХранилище = Ложь)
	
	// проанализируем свойства профиля
	Результат = Новый Структура("ОрганизацияПодтверждена, ОтправленЗапросНаПодтверждениеОрганизации, ПодписываемыйЗапрос64", Ложь, Ложь, "");
	
	СтатусОрганизации = ТекстПослеПрефикса(ПараметрыСоединения.ТекстОтветаАвторизации, """Status"":{", "}");
	Если ЗначениеЗаполнено(СтатусОрганизации) Тогда
		Результат.ОрганизацияПодтверждена = (СтрНайти(СтатусОрганизации, """Waiting"":true") = 0);
		Результат.ОтправленЗапросНаПодтверждениеОрганизации = НЕ Результат.ОрганизацияПодтверждена;
	КонецЕсли;
	
	Если НЕ Результат.ОтправленЗапросНаПодтверждениеОрганизации И СформироватьПодписываемыйЗапрос Тогда
		// загрузим профиль организации
		
		ЗаголовкиHTTP = Новый Соответствие();
		ЗаголовкиHTTP.Вставить("Proxy-Connection", "Keep-Alive");
		ЗаголовкиHTTP.Вставить("Pragma", "no-cache");
		Если ЗначениеЗаполнено(ПараметрыСоединения.ЗначениеCookie) Тогда
			ЗаголовкиHTTP.Вставить("Cookie", ПараметрыСоединения.ЗначениеCookie);
		КонецЕсли;
		
		ИмяФайлаОтвета = ПолучитьИмяВременногоФайла();
		
		Попытка
			РесурсНаСервере = ДокументооборотСКО.ПолучитьЗаменяемоеЗначениеТестовогоСервера1С("/FtsPersonalCabinetWeb2017/Service/GetOrganizationStoreObject?key=AccLs", 
																								"{FTS}/FtsPersonalCabinetWeb2017/Service/GetOrganizationStoreObject?key=AccLs", 
																								ПараметрыСоединения.СоединениеHTTP);			
			ОтветHTTP = ПараметрыСоединения.СоединениеHTTP.Получить(
				РесурсНаСервере,
				ИмяФайлаОтвета,
				ЗаголовкиHTTP);
		Исключение
			ОбъектФайлОтвета = Новый Файл(ИмяФайлаОтвета);
			Если НЕ ОбъектФайлОтвета.Существует() Тогда
				ПараметрыСоединения.СоединениеHTTP = Неопределено;
				ПараметрыСоединения.ПовторятьСоединение = Истина;
				ОперацииСФайламиЭДКО.УдалитьВременныйФайл(ИмяФайлаОтвета);
				Возврат Неопределено;
			КонецЕсли;
		КонецПопытки;
		
		Если ОтветHTTP <> Неопределено Тогда
			ЗначениеSetCookie = ОтветHTTP.Заголовки.Получить("Set-Cookie");
			ПараметрыСоединения.ЗначениеCookie = ОбновленноеЗначениеCookie(ПараметрыСоединения.ЗначениеCookie, ЗначениеSetCookie);
		КонецЕсли;
		
		ОперацииСФайламиЭДКО.УдалитьВременныйФайл(ИмяФайлаОтвета);
		
		// прочая загрузка организации
		
		ЗаголовкиHTTP = Новый Соответствие();
		ЗаголовкиHTTP.Вставить("Proxy-Connection", "Keep-Alive");
		ЗаголовкиHTTP.Вставить("Pragma", "no-cache");
		Если ЗначениеЗаполнено(ПараметрыСоединения.ЗначениеCookie) Тогда
			ЗаголовкиHTTP.Вставить("Cookie", ПараметрыСоединения.ЗначениеCookie);
		КонецЕсли;
		
		ИмяФайлаОтвета = ПолучитьИмяВременногоФайла();
		
		Попытка
			РесурсНаСервере = ДокументооборотСКО.ПолучитьЗаменяемоеЗначениеТестовогоСервера1С("/FtsPersonalCabinetWeb2017/Service/GetOrganizationStoreObject?key=DtBankNoLoad", 
																								"{FTS}/FtsPersonalCabinetWeb2017/Service/GetOrganizationStoreObject?key=DtBankNoLoad", 
																								ПараметрыСоединения.СоединениеHTTP);
			ОтветHTTP = ПараметрыСоединения.СоединениеHTTP.Получить(
				РесурсНаСервере,
				ИмяФайлаОтвета,
				ЗаголовкиHTTP);
		Исключение
			ОбъектФайлОтвета = Новый Файл(ИмяФайлаОтвета);
			Если НЕ ОбъектФайлОтвета.Существует() Тогда
				ПараметрыСоединения.СоединениеHTTP = Неопределено;
				ПараметрыСоединения.ПовторятьСоединение = Истина;
				ОперацииСФайламиЭДКО.УдалитьВременныйФайл(ИмяФайлаОтвета);
				Возврат Неопределено;
			КонецЕсли;
		КонецПопытки;
		
		Если ОтветHTTP <> Неопределено Тогда
			ЗначениеSetCookie = ОтветHTTP.Заголовки.Получить("Set-Cookie");
			ПараметрыСоединения.ЗначениеCookie = ОбновленноеЗначениеCookie(ПараметрыСоединения.ЗначениеCookie, ЗначениеSetCookie);
		КонецЕсли;
		
		ОперацииСФайламиЭДКО.УдалитьВременныйФайл(ИмяФайлаОтвета);
		
		// сформируем запрос
		
		ЗаголовкиHTTP = Новый Соответствие();
		ЗаголовкиHTTP.Вставить("Content-Type", "application/json;charset=utf-8");
		ЗаголовкиHTTP.Вставить("Proxy-Connection", "Keep-Alive");
		ЗаголовкиHTTP.Вставить("Pragma", "no-cache");
		Если ЗначениеЗаполнено(ПараметрыСоединения.ЗначениеCookie) Тогда
			ЗаголовкиHTTP.Вставить("Cookie", ПараметрыСоединения.ЗначениеCookie);
		КонецЕсли;
		
		ПакетДляЗапроса = "{""signedToken"":null}";
		ИмяФайлаЗапроса = ПолучитьИмяВременногоФайла();
		ЗаписываемыйФайл = Новый ЗаписьТекста(ИмяФайлаЗапроса, "utf-8");
		ЗаписываемыйФайл.Записать(ПакетДляЗапроса);
		ЗаписываемыйФайл.Закрыть();
		
		ИмяФайлаОтвета = ПолучитьИмяВременногоФайла();
		
		ОтветHTTP = Неопределено;
		Попытка
			РесурсНаСервере = ДокументооборотСКО.ПолучитьЗаменяемоеЗначениеТестовогоСервера1С("/FtsPersonalCabinetWeb2017/Service/CreateOrganizationRequest", 
																								"{FTS}/FtsPersonalCabinetWeb2017/Service/CreateOrganizationRequest", 
																								ПараметрыСоединения.СоединениеHTTP);				
			ОтветHTTP = ПараметрыСоединения.СоединениеHTTP.ОтправитьДляОбработки(
				ИмяФайлаЗапроса,
				РесурсНаСервере,
				ИмяФайлаОтвета,
				ЗаголовкиHTTP);
		Исключение
			
			ПараметрыСоединения.СоединениеHTTP = Неопределено;
			ПараметрыСоединения.ПовторятьСоединение = Истина;
			ОперацииСФайламиЭДКО.УдалитьВременныйФайл(ИмяФайлаОтвета);
			Возврат Неопределено;
			
		КонецПопытки;
		
		ЗначениеSetCookie = ОтветHTTP.Заголовки.Получить("Set-Cookie");
		ПараметрыСоединения.ЗначениеCookie = ОбновленноеЗначениеCookie(ПараметрыСоединения.ЗначениеCookie, ЗначениеSetCookie);
		
		ФайлОтвета = Новый ЧтениеТекста(ИмяФайлаОтвета, "utf-8");
		ТекстОтвета = ФайлОтвета.Прочитать();
		ФайлОтвета.Закрыть();
		
		ОперацииСФайламиЭДКО.УдалитьВременныйФайл(ИмяФайлаОтвета);
		
		Если Лев(ТекстОтвета, 1) = """" И Прав(ТекстОтвета, 1) = """" Тогда
			ДлинаОтвета = СтрДлина(ТекстОтвета);
			ТекстОтвета = Сред(ТекстОтвета, 2, ДлинаОтвета - 2);
		КонецЕсли;
		ТекстОтвета = СтрЗаменить(ТекстОтвета, "\""", """");
		
		ИмяФайлаЗапроса = ПолучитьИмяВременногоФайла();
		ЗаписываемыйФайл = Новый ЗаписьТекста(ИмяФайлаЗапроса, "utf-8");
		ЗаписываемыйФайл.Записать(ТекстОтвета);
		ЗаписываемыйФайл.Закрыть();
		
		ДанныеЗапроса = Новый ДвоичныеДанные(ИмяФайлаЗапроса);
		ПодписываемыйЗапрос64 = Base64Строка(ДанныеЗапроса);
		ПодписываемыйЗапрос64 = Сред(ПодписываемыйЗапрос64, 5);
		// удалим из файла в кодировке "UTF-8" сигнатуру BOM
		Если ПомещатьВоВременноеХранилище Тогда
			ДанныеЗапроса = Base64Значение(ПодписываемыйЗапрос64);
			Результат.ПодписываемыйЗапрос64 = ПоместитьВоВременноеХранилище(ДанныеЗапроса, Новый УникальныйИдентификатор);
		Иначе
			Результат.ПодписываемыйЗапрос64 = ПодписываемыйЗапрос64;
		КонецЕсли;
		
		ОперацииСФайламиЭДКО.УдалитьВременныйФайл(ИмяФайлаЗапроса);
		
	КонецЕсли;
	
	Возврат Результат;
	
КонецФункции

Функция ЗагрузитьПодписанныйЗапросНаПорталФТС(
		ИмяФайлаПодписанногоЗапроса,
		ПараметрыОтправки,
		ПараметрыСоединения)
	
	Если ПараметрыСоединения.ВерсияПортала = "2017" Тогда
		Возврат ЗагрузитьПодписанныйЗапросНаПорталФТС2017(
			ИмяФайлаПодписанногоЗапроса,
			ПараметрыОтправки,
			ПараметрыСоединения);
	КонецЕсли;
	
	ФайлПодписанногоЗапроса = Новый ЧтениеТекста(ИмяФайлаПодписанногоЗапроса, "utf-8");
	ТекстПодписанногоЗапроса = ФайлПодписанногоЗапроса.Прочитать();
	ФайлПодписанногоЗапроса.Закрыть();
	
	ЗаголовкиHTTP = Новый Соответствие();
	ЗаголовкиHTTP.Вставить("Content-Type", "application/json");
	ЗаголовкиHTTP.Вставить("Proxy-Connection", "Keep-Alive");
	ЗаголовкиHTTP.Вставить("Pragma", "no-cache");
	ЗаголовкиHTTP.Вставить("X-Ext.Net", "delta=true");
	Если ЗначениеЗаполнено(ПараметрыСоединения.ЗначениеCookie) Тогда
		ЗаголовкиHTTP.Вставить("Cookie", ПараметрыСоединения.ЗначениеCookie);
	КонецЕсли;
	
	ТекстПодписанногоЗапросаКодированный = СтрЗаменить(ТекстПодписанногоЗапроса, """", "\""");
	ПакетДляОтправкиПодписанногоЗапроса = СтрШаблон(
		"{""signedData"":""%1""}",
		ТекстПодписанногоЗапросаКодированный);
	
	ИмяФайлаЗапроса = ПолучитьИмяВременногоФайла();
	ЗаписываемыйФайл = Новый ЗаписьТекста(ИмяФайлаЗапроса, "windows-1251");
	ЗаписываемыйФайл.Записать(ПакетДляОтправкиПодписанногоЗапроса);
	ЗаписываемыйФайл.Закрыть();
	
	ИмяФайлаОтвета = ПолучитьИмяВременногоФайла();
	
	ОтветHTTP = Неопределено;
	Попытка
		РесурсНаСервере = ДокументооборотСКО.ПолучитьЗаменяемоеЗначениеТестовогоСервера1С("/FtsPersonalCabinetWeb/Api/CreateOrganizationRequest", 
																							"{FTS}/FtsPersonalCabinetWeb/Api/CreateOrganizationRequest", 
																							ПараметрыСоединения.СоединениеHTTP);
		ОтветHTTP = ПараметрыСоединения.СоединениеHTTP.ОтправитьДляОбработки(ИмяФайлаЗапроса, РесурсНаСервере, ИмяФайлаОтвета, ЗаголовкиHTTP);
	Исключение
		ОбъектФайлОтвета = Новый Файл(ИмяФайлаОтвета);
		Если НЕ ОбъектФайлОтвета.Существует() Тогда
			ПараметрыСоединения.ПовторятьСоединение = Истина;
			Возврат Ложь;
		КонецЕсли;
	КонецПопытки;
	
	Если ОтветHTTP <> Неопределено Тогда
		ЗначениеSetCookie = ОтветHTTP.Заголовки.Получить("Set-Cookie");
		ПараметрыСоединения.ЗначениеCookie = ОбновленноеЗначениеCookie(ПараметрыСоединения.ЗначениеCookie, ЗначениеSetCookie);
	КонецЕсли;
	
	ФайлОтвета = Новый ЧтениеТекста(ИмяФайлаОтвета, "utf-8");
	ТекстОтвета = ФайлОтвета.Прочитать();
	ФайлОтвета.Закрыть();
	
	ОперацииСФайламиЭДКО.УдалитьВременныйФайл(ИмяФайлаОтвета);
	
	ПараметрыСоединения.ОписаниеОшибкиСервером = ТекстОшибкиПорталаФТС(ТекстОтвета);
	Если НЕ ЗначениеЗаполнено(ПараметрыСоединения.ОписаниеОшибкиСервером) Тогда
		ОписаниеОшибкиСервером = ТекстПослеПрефикса(ТекстОтвета, "System.Exception:", "---");
		ОписаниеОшибкиСервером = СокрЛП(ОписаниеОшибкиСервером);
		ПараметрыСоединения.ОписаниеОшибкиСервером = ТекстИзHTML(ОписаниеОшибкиСервером);
	КонецЕсли;
	
	Возврат НЕ ЗначениеЗаполнено(ПараметрыСоединения.ОписаниеОшибкиСервером);
	
КонецФункции

Функция ЗагрузитьПодписанныйЗапросНаПорталФТС2017(
		ИмяФайлаПодписанногоЗапроса,
		ПараметрыОтправки,
		ПараметрыСоединения)
	
	// загрузим организацию
	
	ЗаголовкиHTTP = Новый Соответствие();
	ЗаголовкиHTTP.Вставить("Proxy-Connection", "Keep-Alive");
	ЗаголовкиHTTP.Вставить("Pragma", "no-cache");
	Если ЗначениеЗаполнено(ПараметрыСоединения.ЗначениеCookie) Тогда
		ЗаголовкиHTTP.Вставить("Cookie", ПараметрыСоединения.ЗначениеCookie);
	КонецЕсли;
	
	ИмяФайлаОтвета = ПолучитьИмяВременногоФайла();
	
	Попытка
		РесурсНаСервере = ДокументооборотСКО.ПолучитьЗаменяемоеЗначениеТестовогоСервера1С("/FtsPersonalCabinetWeb2017/Service/GetOrganizationStoreObject?key=DtBankNoLoad", 
																							"{FTS}/FtsPersonalCabinetWeb2017/Service/GetOrganizationStoreObject?key=DtBankNoLoad", 
																							ПараметрыСоединения.СоединениеHTTP);
		ОтветHTTP = ПараметрыСоединения.СоединениеHTTP.Получить(
			РесурсНаСервере,
			ИмяФайлаОтвета,
			ЗаголовкиHTTP);
	Исключение
		ОбъектФайлОтвета = Новый Файл(ИмяФайлаОтвета);
		Если НЕ ОбъектФайлОтвета.Существует() Тогда
			ПараметрыСоединения.ПовторятьСоединение = Истина;
			Возврат Ложь;
		КонецЕсли;
	КонецПопытки;
	
	Если ОтветHTTP <> Неопределено Тогда
		ЗначениеSetCookie = ОтветHTTP.Заголовки.Получить("Set-Cookie");
		ПараметрыСоединения.ЗначениеCookie = ОбновленноеЗначениеCookie(ПараметрыСоединения.ЗначениеCookie, ЗначениеSetCookie);
	КонецЕсли;
	
	ОперацииСФайламиЭДКО.УдалитьВременныйФайл(ИмяФайлаОтвета);
	
	// сформируем запрос
	
	ФайлПодписанногоЗапроса = Новый ЧтениеТекста(ИмяФайлаПодписанногоЗапроса, "utf-8");
	ТекстПодписанногоЗапроса = ФайлПодписанногоЗапроса.Прочитать();
	ФайлПодписанногоЗапроса.Закрыть();
	
	ЗаголовкиHTTP = Новый Соответствие();
	ЗаголовкиHTTP.Вставить("Content-Type", "application/json;charset=utf-8");
	ЗаголовкиHTTP.Вставить("Proxy-Connection", "Keep-Alive");
	ЗаголовкиHTTP.Вставить("Pragma", "no-cache");
	Если ЗначениеЗаполнено(ПараметрыСоединения.ЗначениеCookie) Тогда
		ЗаголовкиHTTP.Вставить("Cookie", ПараметрыСоединения.ЗначениеCookie);
	КонецЕсли;
	
	ТекстПодписанногоЗапросаКодированный = СтрЗаменить(ТекстПодписанногоЗапроса, """", "\""");
	ПакетДляОтправкиПодписанногоЗапроса = СтрШаблон(
		"{""signedToken"":""%1""}",
		ТекстПодписанногоЗапросаКодированный);
	
	ИмяФайлаЗапроса = ПолучитьИмяВременногоФайла();
	ЗаписываемыйФайл = Новый ЗаписьТекста(ИмяФайлаЗапроса, "utf-8");
	ЗаписываемыйФайл.Записать(ПакетДляОтправкиПодписанногоЗапроса);
	ЗаписываемыйФайл.Закрыть();
	
	ИмяФайлаОтвета = ПолучитьИмяВременногоФайла();
	
	ОтветHTTP = Неопределено;
	Попытка
		РесурсНаСервере = ДокументооборотСКО.ПолучитьЗаменяемоеЗначениеТестовогоСервера1С("/FtsPersonalCabinetWeb2017/Service/CreateOrganizationRequest", 
																							"{FTS}/FtsPersonalCabinetWeb2017/Service/CreateOrganizationRequest", 
																							ПараметрыСоединения.СоединениеHTTP);
		ОтветHTTP = ПараметрыСоединения.СоединениеHTTP.ОтправитьДляОбработки(
			ИмяФайлаЗапроса,
			РесурсНаСервере,
			ИмяФайлаОтвета,
			ЗаголовкиHTTP);
	Исключение
		ОбъектФайлОтвета = Новый Файл(ИмяФайлаОтвета);
		Если НЕ ОбъектФайлОтвета.Существует() Тогда
			ПараметрыСоединения.ПовторятьСоединение = Истина;
			Возврат Ложь;
		КонецЕсли;
	КонецПопытки;
	
	Если ОтветHTTP <> Неопределено Тогда
		ЗначениеSetCookie = ОтветHTTP.Заголовки.Получить("Set-Cookie");
		ПараметрыСоединения.ЗначениеCookie = ОбновленноеЗначениеCookie(ПараметрыСоединения.ЗначениеCookie, ЗначениеSetCookie);
	КонецЕсли;
	
	ФайлОтвета = Новый ЧтениеТекста(ИмяФайлаОтвета, "utf-8");
	ТекстОтвета = ФайлОтвета.Прочитать();
	ФайлОтвета.Закрыть();
	
	ОперацииСФайламиЭДКО.УдалитьВременныйФайл(ИмяФайлаОтвета);
	
	ПараметрыСоединения.ОписаниеОшибкиСервером = ТекстОшибкиПорталаФТС(ТекстОтвета, Истина, Истина);
	Если НЕ ЗначениеЗаполнено(ПараметрыСоединения.ОписаниеОшибкиСервером) Тогда
		ОписаниеОшибкиСервером = ТекстПослеПрефикса(ТекстОтвета, "System.Exception:", "---");
		ОписаниеОшибкиСервером = СокрЛП(ОписаниеОшибкиСервером);
		ПараметрыСоединения.ОписаниеОшибкиСервером = ТекстИзHTML(ОписаниеОшибкиСервером);
	КонецЕсли;
	
	Если НЕ ЗначениеЗаполнено(ПараметрыСоединения.ОписаниеОшибкиСервером) Тогда
		ПараметрыСоединения.ТекстОтветаАвторизации = ТекстОтвета;
	КонецЕсли;
	
	Возврат НЕ ЗначениеЗаполнено(ПараметрыСоединения.ОписаниеОшибкиСервером);
	
КонецФункции

Функция ОтправитьВФТСССервера(ЭтоЭлектроннаяПодписьВМоделиСервиса, Знач СтрокаBase64ФайлаОтправки, Знач КороткоеИмяФайлаВыгрузки, Знач ОтчетСсылка, Знач ПараметрыОтправки, ПараметрыСоединения) Экспорт
	
	// получаем двоичные данные файлов
	Если ЭтоЭлектроннаяПодписьВМоделиСервиса Тогда
		МассивДвоичныхДанныхФайлаОтправки = ПолучитьИзВременногоХранилища(СтрокаBase64ФайлаОтправки);
	Иначе
		МассивДвоичныхДанныхФайлаОтправки = СтрокаBase64ФайлаОтправки;
	КонецЕсли;
	МассивДанныхФайлаОтправки = ?(ТипЗнч(МассивДвоичныхДанныхФайлаОтправки) = Тип("Массив"),
		МассивДвоичныхДанныхФайлаОтправки, ОбщегоНазначенияКлиентСервер.ЗначениеВМассиве(МассивДвоичныхДанныхФайлаОтправки));
	
	// выгружаем двоичные данные в файл
	МассивИменФайловОтправки = Новый Массив;
	Для каждого ДанныеФайлаОтправки Из МассивДанныхФайлаОтправки Цикл
		Если ЭтоЭлектроннаяПодписьВМоделиСервиса Тогда
			ДвоичныеДанныеФайлаОтправки = ДанныеФайлаОтправки;
		Иначе
			ДвоичныеДанныеФайлаОтправки = Base64Значение(ДанныеФайлаОтправки);
		КонецЕсли;
		
		ИмяФайлаОтправки = ПолучитьИмяВременногоФайла();
		ДвоичныеДанныеФайлаОтправки.Записать(ИмяФайлаОтправки);
		
		МассивИменФайловОтправки.Добавить(ИмяФайлаОтправки);
	КонецЦикла;
	ИмяФайлаОтправки = ?(ТипЗнч(МассивДвоичныхДанныхФайлаОтправки) = Тип("Массив"),
		МассивИменФайловОтправки, МассивИменФайловОтправки[0]);
	
	ФайлыИДанныеОтправки = Новый Структура("КороткоеИмяФайлаВыгрузки, ИмяФайлаОтправки, ЭтоЭлектроннаяПодписьВМоделиСервиса", КороткоеИмяФайлаВыгрузки, ИмяФайлаОтправки, ЭтоЭлектроннаяПодписьВМоделиСервиса);
	
	Результат = ОтправкаНаСерверФТС(ФайлыИДанныеОтправки, ОтчетСсылка, ПараметрыОтправки, ПараметрыСоединения);
	
	Если ЭтоЭлектроннаяПодписьВМоделиСервиса И Результат И ЗначениеЗаполнено(ПараметрыСоединения.ПодписываемоеСодержимоеВыгрузки64) Тогда
		ДанныеПодписываемогоСодержимогоВыгрузки = Base64Значение(ПараметрыСоединения.ПодписываемоеСодержимоеВыгрузки64);
		ПараметрыСоединения.ПодписываемоеСодержимоеВыгрузки64 = ПоместитьВоВременноеХранилище(ДанныеПодписываемогоСодержимогоВыгрузки, Новый УникальныйИдентификатор);
	КонецЕсли;
	ПараметрыСоединения.СоединениеHTTP = Неопределено;
	ПараметрыСоединения.ОписаниеОшибкиСервером = "";
	
	Для каждого ИмяФайлаОтправки Из МассивИменФайловОтправки Цикл
		ОперацииСФайламиЭДКО.УдалитьВременныйФайл(ИмяФайлаОтправки);
	КонецЦикла;
	
	Возврат Результат;
	
КонецФункции

Функция ОтправитьПодписьВФТСССервера(ЭтоЭлектроннаяПодписьВМоделиСервиса, Знач СтрокаBase64ФайлаПодписи, Знач КороткоеИмяФайлаВыгрузки, Знач ОтчетСсылка, Знач ПараметрыОтправки, ПараметрыСоединения) Экспорт
	
	// получаем двоичные данные файлов
	Если ЭтоЭлектроннаяПодписьВМоделиСервиса Тогда
		ДвоичныеДанныеФайлаПодписи = ПолучитьИзВременногоХранилища(СтрокаBase64ФайлаПодписи);
		ДвоичныеДанныеФайлаПодписываемойВыгрузки = ПолучитьИзВременногоХранилища(ПараметрыСоединения.ПодписываемоеСодержимоеВыгрузки64);
	Иначе
		ДвоичныеДанныеФайлаПодписи = Base64Значение(СтрокаBase64ФайлаПодписи);
		ДвоичныеДанныеФайлаПодписываемойВыгрузки = Base64Значение(ПараметрыСоединения.ПодписываемоеСодержимоеВыгрузки64);
	КонецЕсли;
	
	// выгружаем двоичные данные в файл
	ИмяФайлаПодписи = ПолучитьИмяВременногоФайла();
	ДвоичныеДанныеФайлаПодписи.Записать(ИмяФайлаПодписи);
	
	ИмяФайлаПодписываемойВыгрузки = ПолучитьИмяВременногоФайла();
	ДвоичныеДанныеФайлаПодписываемойВыгрузки.Записать(ИмяФайлаПодписываемойВыгрузки);
	
	ФайлыИДанныеОтправки = Новый Структура;
	ФайлыИДанныеОтправки.Вставить("КороткоеИмяФайлаВыгрузки", 				КороткоеИмяФайлаВыгрузки);
	ФайлыИДанныеОтправки.Вставить("ИмяФайлаПодписи", 						ИмяФайлаПодписи);
	ФайлыИДанныеОтправки.Вставить("ИмяФайлаПодписываемойВыгрузки", 			ИмяФайлаПодписываемойВыгрузки);
	ФайлыИДанныеОтправки.Вставить("ЭтоЭлектроннаяПодписьВМоделиСервиса", 	ЭтоЭлектроннаяПодписьВМоделиСервиса);
	
	Результат = ОтправкаПодписиНаСерверФТС(ФайлыИДанныеОтправки, ОтчетСсылка, ПараметрыОтправки, ПараметрыСоединения);
	
	ПараметрыСоединения.СоединениеHTTP = Неопределено;
	ПараметрыСоединения.ОписаниеОшибкиСервером = "";
	
	ОперацииСФайламиЭДКО.УдалитьВременныйФайл(ИмяФайлаПодписи);
	ОперацииСФайламиЭДКО.УдалитьВременныйФайл(ИмяФайлаПодписываемойВыгрузки);
	Если Результат <> Неопределено Тогда
		ПараметрыСоединения.ПодписываемоеСодержимоеВыгрузки64 = "";
	КонецЕсли;
	
	Возврат Результат;
	
КонецФункции

Функция ОтправитьПодписанныйЗапросВФТСССервера(ЭтоЭлектроннаяПодписьВМоделиСервиса, Знач СтрокаBase64ФайлаПодписанногоЗапроса, Знач ОтчетСсылка, Знач ПараметрыОтправки, ПараметрыСоединения) Экспорт
	
	// получаем двоичные данные файлов
	Если ЭтоЭлектроннаяПодписьВМоделиСервиса Тогда
		ДвоичныеДанныеФайлаПодписанногоЗапроса = ПолучитьИзВременногоХранилища(СтрокаBase64ФайлаПодписанногоЗапроса);
	Иначе
		ДвоичныеДанныеФайлаПодписанногоЗапроса = Base64Значение(СтрокаBase64ФайлаПодписанногоЗапроса);
	КонецЕсли;
	
	// выгружаем двоичные данные в файл
	ИмяФайлаПодписанногоЗапроса = ПолучитьИмяВременногоФайла();
	ДвоичныеДанныеФайлаПодписанногоЗапроса.Записать(ИмяФайлаПодписанногоЗапроса);
	
	ФайлыИДанныеОтправки = Новый Структура("ИмяФайлаПодписанногоЗапроса, ЭтоЭлектроннаяПодписьВМоделиСервиса", ИмяФайлаПодписанногоЗапроса, ЭтоЭлектроннаяПодписьВМоделиСервиса);
	
	Результат = ОтправкаПодписанногоЗапросаНаСерверФТС(ФайлыИДанныеОтправки, ОтчетСсылка, ПараметрыОтправки, ПараметрыСоединения);
	
	ПараметрыСоединения.СоединениеHTTP = Неопределено;
	
	ОперацииСФайламиЭДКО.УдалитьВременныйФайл(ИмяФайлаПодписанногоЗапроса);
	Если Результат И ЗначениеЗаполнено(ПараметрыСоединения.ПодписываемыйЗапрос64) Тогда
		Если ЭтоЭлектроннаяПодписьВМоделиСервиса Тогда
			УдалитьИзВременногоХранилища(ПараметрыСоединения.ПодписываемыйЗапрос64);
		КонецЕсли;
		ПараметрыСоединения.ПодписываемыйЗапрос64 = "";
	КонецЕсли;
	
	Возврат Результат;
	
КонецФункции

Функция ГенерироватьHTMLПротоколаОбработкиФТС(ЗаголовокОтчета, СтатусОтчета, ЦветСтатуса, СтатусОтправки, НаКонтроле, ПараметрыОтправки, ОтчетСсылка)
	
	// формирование заголовка протокола
	
	ТабДок = Новый ТабличныйДокумент;
	
	Шаблон = ПолучитьМакет("ФТСШаблонДляHTML");
	
	НаименованиеОрганизации = "";
	ИНН = ИННОрганизации(ПараметрыОтправки.Организация, "", НаименованиеОрганизации);
	СведенияПоОбъекту 	= СведенияПоОтправляемымОбъектам(ОтчетСсылка);
	НаименованиеОргана 	= СведенияПоОбъекту.ПредставлениеКонтролирующегоОргана;
	НаименованиеОтчета 	= СведенияПоОбъекту.Наименование;
	НаименованиеОтчета 	= СведенияПоОбъекту.Наименование;
	Если ТипЗнч(ОтчетСсылка) = Тип("ДокументСсылка.РегламентированныйОтчет") Тогда
		ВидОтчета = Справочники.ВидыОтправляемыхДокументов.НайтиПоРеквизиту("Источник", ОтчетСсылка.ИсточникОтчета);
	ИначеЕсли ТипЗнч(ОтчетСсылка) = Тип("СправочникСсылка.ЭлектронныеПредставленияРегламентированныхОтчетов") Тогда
		ВидОтчета = ОтчетСсылка.ВидОтчета;
	Иначе
		ВидОтчета = Неопределено;
	КонецЕсли;
	Если ВидОтчета = Справочники.ВидыОтправляемыхДокументов.СтатистикаФормаУчетаПеремещенияТоваровТС Тогда
		НаименованиеОтчета = НСтр("ru = 'Статистическая форма учета перемещения товаров'");
	КонецЕсли;
	
	СекцияШапкаСЗаголовком = Шаблон.ПолучитьОбласть("ШапкаСЗаголовком");
	Если НаКонтроле Тогда
		СекцияШапкаСЗаголовком.Параметры.НаименованиеПротокола = НСтр("ru = 'Протокол'");
	ИначеЕсли СтатусОтправки = Перечисления.СтатусыОтправки.Сдан Тогда
		СекцияШапкаСЗаголовком.Параметры.НаименованиеПротокола = НСтр("ru = 'Протокол о сдаче'");
	ИначеЕсли СтатусОтправки = Перечисления.СтатусыОтправки.НеПринят Тогда
		СекцияШапкаСЗаголовком.Параметры.НаименованиеПротокола = НСтр("ru = 'Протокол ошибок'");
	Иначе
		СекцияШапкаСЗаголовком.Параметры.НаименованиеПротокола = НСтр("ru = 'Протокол'");
	КонецЕсли;
	СекцияШапкаСЗаголовком.Параметры.Отправитель = НаименованиеОрганизации;
	СекцияШапкаСЗаголовком.Параметры.ИНН = НСтр("ru = 'ИНН'") + " " + ИНН;
	СекцияШапкаСЗаголовком.Параметры.Получатель = НаименованиеОргана;
	СекцияШапкаСЗаголовком.Параметры.Отчет = НаименованиеОтчета;
	СекцияШапкаСЗаголовком.Параметры.СтатусОтчета = СтатусОтчета;
	СекцияШапкаСЗаголовком.Области.СтатусОтчета.ЦветТекста = ЦветСтатуса;
	ТабДок.Вывести(СекцияШапкаСЗаголовком);
	
	Если ЗаголовокОтчета.ПоляПротокола <> Неопределено Тогда
		ПоляПротокола = ЗаголовокОтчета.ПоляПротокола;
		
		СекцияСтроки = Шаблон.ПолучитьОбласть("Строки");
		СекцияСтроки.Параметры.ИдентификаторВидаДокумента 			= ПоляПротокола.ИдентификаторВидаДокумента;
		СекцияСтроки.Параметры.ДатаНаправления 						= ПоляПротокола.ДатаНаправления;
		СекцияСтроки.Параметры.ВремяНаправления 					= ПоляПротокола.ВремяНаправления;
		СекцияСтроки.Параметры.КодТаможенногоОргана 				= ПоляПротокола.КодТаможенногоОргана;
		СекцияСтроки.Параметры.РегистрационныйНомерСтатистическойФормы =
			ПоляПротокола.РегистрационныйНомерСтатистическойФормы;
		СекцияСтроки.Параметры.УникальныйИдентификаторДокумента 	= ПоляПротокола.УникальныйИдентификаторДокумента;
		СекцияСтроки.Параметры.ТранспортныйИдентификаторКонверта 	= ПоляПротокола.ТранспортныйИдентификаторКонверта;
		СекцияСтроки.Параметры.ТранспортныйИдентификаторИсходногоКонверта =
			ПоляПротокола.ТранспортныйИдентификаторИсходногоКонверта;
		СекцияСтроки.Параметры.ИнформацияОбОтправителе 				= ПоляПротокола.ИнформацияОбОтправителе;
		СекцияСтроки.Параметры.ИнформацияОПолучателе = ПоляПротокола.ИнформацияОПолучателе;
		СекцияСтроки.Параметры.ДатаИВремяСозданияЭлектронногоСообщения =
			ПоляПротокола.ДатаИВремяСозданияЭлектронногоСообщения;
		СекцияСтроки.Параметры.ПриоритетСообщения 					= ПоляПротокола.ПриоритетСообщения;
		СекцияСтроки.Параметры.ТипСообщения 						= ПоляПротокола.ТипСообщения;
		СекцияСтроки.Параметры.УникальныйИдентификаторПроцесса 		= ПоляПротокола.УникальныйИдентификаторПроцесса;
		СекцияСтроки.Параметры.ТипПрограммногоОбеспечения 			= ПоляПротокола.ТипПрограммногоОбеспечения;
		СекцияСтроки.Параметры.ВерсияПрограммногоОбеспечения 		= ПоляПротокола.ВерсияПрограммногоОбеспечения;
		ТабДок.Вывести(СекцияСтроки);
	КонецЕсли;
	
	ИмяВременногоФайла = ПолучитьИмяВременногоФайла();
	ТабДок.Записать(ИмяВременногоФайла, ТипФайлаТабличногоДокумента.HTML);
	ТекстHTML = Новый ТекстовыйДокумент;
	ТекстHTML.Прочитать(ИмяВременногоФайла);
	ТекстHTMLЗаголовкаПротокола = ТекстHTML.ПолучитьТекст();
	ОперацииСФайламиЭДКО.УдалитьВременныйФайл(ИмяВременногоФайла);
	
	ТекстHTMLПротокола = ЗаголовокОтчета.Протокол;
	Если НЕ ЗначениеЗаполнено(ТекстHTMLПротокола) Тогда
		Возврат ТекстHTMLЗаголовкаПротокола;
	КонецЕсли;
	
	// подготовка содержимого по полученному протоколу
	
	ТекстHTMLПротокола = СтрЗаменить(ТекстHTMLПротокола, " <span class=""required"">*</span>", "");
	ТекстHTMLПротокола = СтрЗаменить(ТекстHTMLПротокола, "<span title=""FtsAlbum.Envelope.EnvelopeType"" >Конверт</span>", "");
	
	// преобразовать поля ввода в статические
	НеобработанныйТекст = ТекстHTMLПротокола;
	ОбработанныйТекст = "";
	Пока НеобработанныйТекст <> "" Цикл
		ПозицияПоля = СтрНайти(НеобработанныйТекст, "<input");
		Если ПозицияПоля = 0 Тогда
			ОбработанныйТекст = ОбработанныйТекст + НеобработанныйТекст;
			Прервать;
		КонецЕсли;
		ОбработанныйТекст = ОбработанныйТекст + Лев(НеобработанныйТекст, ПозицияПоля - 1);
		НеобработанныйТекст = Сред(НеобработанныйТекст, ПозицияПоля + 6);
		
		ПозицияЗавершения = СтрНайти(НеобработанныйТекст, "/>");
		Если ПозицияЗавершения = 0 Тогда
			ОбработанныйТекст = ОбработанныйТекст + НеобработанныйТекст;
			Прервать;
		КонецЕсли;
		ТекстАтрибутовТэга = Лев(НеобработанныйТекст, ПозицияЗавершения);
		
		ПозицияЗначения = СтрНайти(ТекстАтрибутовТэга, " value=""");
		ТекстЗначения = "";
		Если ПозицияЗначения > 0 Тогда
			ТекстЗначения = Сред(ТекстАтрибутовТэга, ПозицияЗначения + 8);
			ТекстАтрибутовТэга = Лев(ТекстАтрибутовТэга, ПозицияЗначения - 1);
			ПозицияЗавершенияЗначения = СтрНайти(ТекстЗначения, """");
			Если ПозицияЗавершенияЗначения > 0 Тогда
				ТекстЗначения = Лев(ТекстЗначения, ПозицияЗавершенияЗначения - 1);
			КонецЕсли;
		КонецЕсли;
		
		ТекстПоля = "<div style=""border-style: solid; border-width: 1px; margin: 1px;""><label" + ТекстАтрибутовТэга + ">" + ТекстЗначения + "</label></div>";
		
		НеобработанныйТекст = Сред(НеобработанныйТекст, ПозицияЗавершения + 2);
		ОбработанныйТекст = ОбработанныйТекст + ТекстПоля;
	КонецЦикла;
	ТекстHTMLПротокола = ОбработанныйТекст;
	
	// преобразовать переводы строк в многострочных полях
	НеобработанныйТекст = ТекстHTMLПротокола;
	ОбработанныйТекст = "";
	Пока НеобработанныйТекст <> "" Цикл
		ПозицияПоля = СтрНайти(НеобработанныйТекст, "<textarea");
		Если ПозицияПоля = 0 Тогда
			ОбработанныйТекст = ОбработанныйТекст + НеобработанныйТекст;
			Прервать;
		КонецЕсли;
		ОбработанныйТекст = ОбработанныйТекст + Лев(НеобработанныйТекст, ПозицияПоля - 1);
		НеобработанныйТекст = Сред(НеобработанныйТекст, ПозицияПоля + 9);
		
		ПозицияЗавершения = СтрНайти(НеобработанныйТекст, "</textarea>");
		Если ПозицияЗавершения = 0 Тогда
			ОбработанныйТекст = ОбработанныйТекст + НеобработанныйТекст;
			Прервать;
		КонецЕсли;
		ТекстАтрибутовТэгаИСодержимого = Лев(НеобработанныйТекст, ПозицияЗавершения - 1);
		
		ПозицияЗакрытия = СтрНайти(ТекстПоля, ">");
		ТекстАтрибутовТэга = ТекстАтрибутовТэгаИСодержимого;
		ТекстСодержимого = "";
		Если ПозицияЗакрытия > 0 Тогда
			ТекстАтрибутовТэга = Лев(ТекстАтрибутовТэгаИСодержимого, ПозицияЗакрытия - 1);
			ТекстСодержимого = Сред(ТекстАтрибутовТэгаИСодержимого, ПозицияЗакрытия + 1);
			ТекстСодержимого = СтрЗаменить(ТекстСодержимого, Символы.ВК + Символы.ПС, "<br>");
			ТекстСодержимого = СтрЗаменить(ТекстСодержимого, Символы.ВК, "<br>");
			ТекстСодержимого = СтрЗаменить(ТекстСодержимого, Символы.ПС, "<br>");
		КонецЕсли;
		
		ТекстМногострочногоПоля = "<div style=""border-style: solid; border-width: 1px; margin: 1px;""><label" + ТекстАтрибутовТэга + ">" + ТекстСодержимого + "</label></div>";
		
		НеобработанныйТекст = Сред(НеобработанныйТекст, ПозицияЗавершения + 11);
		ОбработанныйТекст = ОбработанныйТекст + ТекстМногострочногоПоля;
	КонецЦикла;
	ТекстHTMLПротокола = ОбработанныйТекст;
	
	ТекстHTMLПротокола = СтрЗаменить(ТекстHTMLПротокола, "></label></div>", ">&nbsp;</label></div>");
	
	ТекстHTMLПротокола = ТекстПослеПрефикса(ТекстHTMLПротокола, "<form", "</form>");
	ТекстHTMLПротокола = ТекстПослеПрефикса(ТекстHTMLПротокола, ">");
	
	// объединение заголовка и содержимого
	
	// коррекция стилей
	ТекстHTMLЗаголовкаПротокола = СтрЗаменить(ТекстHTMLЗаголовкаПротокола, "body {", ".bodyh {");
	ТекстHTMLЗаголовкаПротокола = СтрЗаменить(ТекстHTMLЗаголовкаПротокола, "table {", ".tableh {");
	ТекстHTMLЗаголовкаПротокола = СтрЗаменить(ТекстHTMLЗаголовкаПротокола, "td {", ".tdh {");
	ТекстHTMLЗаголовкаПротокола = СтрЗаменить(ТекстHTMLЗаголовкаПротокола, "<TABLE ", "<TABLE class=""tableh"" ");
	
	// добавление стилей портала
	ТекстHTMLЗаголовкаПротокола = СтрЗаменить(ТекстHTMLЗаголовкаПротокола, "</STYLE>",
		"table {border-collapse: collapse; border-spacing: 0px; padding: 0px 0px 0px 0px; margin: 0px 0px 0px 0px; width: 100%;}
		|tr, td {padding: 0px 0px 0px 0px; margin: 0px 0px 0px 0px;}
		|.scroller {zoom: 1; overflow: auto; position: relative;}
		|.editor {zoom: 1; padding: 5px;}
		|.header {font-size: 18px; font-weight: bold;}
		|.fieldBorder {border: solid 1px #dddddd; margin: 6px 0px 0px 0px;}
		|.fieldLabel {width: 300px; text-align: right; padding-right: 4px;}
		|.fieldHeader {padding: 4px; font-weight: 600; background-image: none; background-color: #f3f3f3;
		| background-image: -webkit-gradient(linear,50% 0,50% 100%,color-stop(0%,#fbfbfb),color-stop(100%,#e9e9e9));
		| background-image: -webkit-linear-gradient(top,#fbfbfb,#e9e9e9);
		| background-image: -moz-linear-gradient(top,#fbfbfb,#e9e9e9);
		| background-image: -o-linear-gradient(top,#fbfbfb,#e9e9e9);
		| background-image: -ms-linear-gradient(top,#fbfbfb,#e9e9e9);
		| background-image: linear-gradient(top,#fbfbfb,#e9e9e9);}
		|.fieldHeader > a {font-weight: 900;}
		|.fieldPadding {padding: 4px;}
		|</STYLE>");
	
	ТекстHTMLПротокола = СтрЗаменить(ТекстHTMLЗаголовкаПротокола, "</BODY>", ТекстHTMLПротокола + "
		|</BODY>");
	
	Возврат ТекстHTMLПротокола;
	
КонецФункции

Функция ПолучитьОсновныеСвойстваПоследнейОтправкиОтчетаВФТС(Знач ОтчетСсылка)
	
	Запрос = Новый Запрос;
	Запрос.Текст =
		"ВЫБРАТЬ ПЕРВЫЕ 1
		|	ОтправкиФТС.Ссылка,
		|	ОтправкиФТС.ИдентификаторОтправкиНаСервере КАК Идентификатор,
		|	ОтправкиФТС.ИмяФайлаВыгрузки КАК ИмяФайлаВыгрузки,
		|	ОтправкиФТС.СтатусОтправки КАК Статус
		|ИЗ
		|	Справочник.ОтправкиФТС КАК ОтправкиФТС
		|ГДЕ
		|	ОтправкиФТС.ОтчетСсылка = &ЭтотОтчет
		|	И ОтправкиФТС.ПометкаУдаления = ЛОЖЬ
		|
		|УПОРЯДОЧИТЬ ПО
		|	ОтправкиФТС.ДатаОтправки УБЫВ";
	Запрос.Параметры.Вставить("ЭтотОтчет", ОтчетСсылка);
	Результат = Запрос.Выполнить();
	Если НЕ Результат.Пустой() Тогда
		Выборка = Результат.Выбрать();
		Выборка.Следующий();
		Возврат Выборка;
	Иначе
		Возврат Неопределено;
	КонецЕсли;
	
КонецФункции

Функция ПолучитьПоследнююОтправкуОтчетаВФТС(ОтчетСсылка, ИдентификаторОтправкиНаСервере = "") Экспорт
	
	Отправка = Неопределено;
	
	Запрос = Новый Запрос;
	Запрос.Текст =
		"ВЫБРАТЬ ПЕРВЫЕ 1
		|	ОтправкиФТС.Ссылка, 
		|	ОтправкиФТС.ИдентификаторОтправкиНаСервере КАК Идентификатор
		|ИЗ
		|	Справочник.ОтправкиФТС КАК ОтправкиФТС
		|ГДЕ
		|	ОтправкиФТС.ОтчетСсылка = &ЭтотОтчет
		|	И ОтправкиФТС.ПометкаУдаления = ЛОЖЬ
		|
		|УПОРЯДОЧИТЬ ПО
		|	ОтправкиФТС.ДатаОтправки УБЫВ";
	Запрос.Параметры.Вставить("ЭтотОтчет", ОтчетСсылка);
	Результат = Запрос.Выполнить();
	
	Если НЕ Результат.Пустой() Тогда
		Выборка = Результат.Выбрать();
		Выборка.Следующий();
		Отправка = Выборка.Ссылка;
		ИдентификаторОтправкиНаСервере = Выборка.Идентификатор;
		
	Иначе
		ИдентификаторОтправкиНаСервере = "";
	КонецЕсли;
	
	Возврат Отправка;
	
КонецФункции

Процедура ОбновитьСтатусОтправкиФТСОтчета(Отчет)
	
	// находим последнюю не помеченную на удаление отправку отчета
	ПоследняяОтправка = ПолучитьПоследнююОтправкуОтчетаВФТС(Отчет);
	
	// вычисляем значение статуса отправки (ПеречислениеСсылка.СтатусыОтправки)
	Если ЗначениеЗаполнено(ПоследняяОтправка) Тогда
		
		СтатусОтправки = ПоследняяОтправка.СтатусОтправки;
		Если СтатусОтправки = Перечисления.СтатусыОтправки.ПринятЕстьОшибки Тогда
			//введено после добавления дополнительного статуса ПринятЕстьОшибки, при котором отчет считается сданным
			СтатусОтправки = Перечисления.СтатусыОтправки.Сдан;
		КонецЕсли;
		
	Иначе
		СтатусОтправки = Неопределено;
	КонецЕсли;
	
	// сохраняем статус в базе
	ЗаписатьСтатусОтправкиФТСОтчета(Отчет, СтатусОтправки, ПоследняяОтправка);
	
КонецПроцедуры

Процедура ЗаписатьСтатусОтправкиФТСОтчета(Отчет, СтатусОтправки, ОснованиеСтатуса)
	
	Если ЗначениеЗаполнено(СтатусОтправки) Тогда
		МенЗап = РегистрыСведений.СтатусыОтправки.СоздатьМенеджерЗаписи();
		МенЗап.Объект = Отчет;
		МенЗап.Статус = СтатусОтправки;
		МенЗап.Основание = ОснованиеСтатуса;
		МенЗап.Записать(Истина);
	Иначе
		МенЗап = РегистрыСведений.СтатусыОтправки.СоздатьМенеджерЗаписи();
		МенЗап.Объект = Отчет;
		МенЗап.Прочитать();
		Если МенЗап.Выбран() Тогда
			МенЗап.Удалить();
		КонецЕсли;
	КонецЕсли;
	
	// вызываем переопределяемую процедуру отработки изменения статуса отправки
	ПриИзмененииСтатусаОтправкиДокумента(Отчет, СтатусОтправки);
	
КонецПроцедуры

Функция ПолучитьПустуюДатуЗавершенияОтправкиФТС() Экспорт
	
	Возврат '39991231235959';
	
КонецФункции

Процедура ПриЗаписиОтправкиФТС(Объект, Отказ) Экспорт
	
	Если Объект.ОтчетСсылка <> Неопределено Тогда
		// отражаем изменения в регистре статусов отправки
		ОбновитьСтатусОтправкиФТСОтчета(Объект.ОтчетСсылка);
		ЭлектронныйДокументооборотСКонтролирующимиОрганами.ЗаписьОбъектовРегламентированнойОтчетности(Объект.ОтчетСсылка, Отказ);
	КонецЕсли;
	
КонецПроцедуры

Функция ПолучитьСтрокуСостоянияОтправкиОтчетаИДополнительныеСвойстваДляФТС(ОтчетСсылка, ОрганизацияСсылка, ЦветТекста, ПоказыватьКнопкуОткрытияПротокола) Экспорт
	
	ПоказыватьКнопкуОткрытияПротокола = Ложь;
	ЦветТекста = Новый Цвет(0, 0, 192);
	
	// определяем последнюю отправку
	ОтправкаСсылка = ПолучитьПоследнююОтправкуОтчетаВФТС(ОтчетСсылка);
	Если НЕ ЗначениеЗаполнено(ОтправкаСсылка) Тогда
		СтрокаСостояния = НСтр("ru = 'Отчет не отправлялся.'");
	Иначе
		СтрокаСостояния = ПодставитьПараметрыВСтроку(НСтр("ru = 'Отчет отправлен %1.'"), Формат(ОтправкаСсылка.ДатаОтправки, "ДЛФ=DDT"));
		
		Если ОтправкаСсылка.СтатусОтправки = Перечисления.СтатусыОтправки.НеПринят Тогда
			
			СтрокаСостояния = СтрокаСостояния + " " + НСтр("ru = 'Отчет не принят, получен протокол ошибок.'");
			ЦветТекста = Новый Цвет(255, 0, 0);
			ПоказыватьКнопкуОткрытияПротокола = Истина;
			
		ИначеЕсли ОтправкаСсылка.СтатусОтправки = Перечисления.СтатусыОтправки.Сдан Тогда
			
			ОтчетСдан = Истина;
			СтрокаСостояния = СтрокаСостояния + " " + НСтр("ru = 'Отчет сдан: получен протокол, ошибок не обнаружено.'");
			ЦветТекста = Новый Цвет(0, 179, 16);
			ПоказыватьКнопкуОткрытияПротокола = Истина;
			
		ИначеЕсли ОтправкаСсылка.СтатусОтправки = Перечисления.СтатусыОтправки.Отправлен Тогда
			
			ПоказыватьКнопкуОткрытияПротокола = ЗначениеЗаполнено(ОтправкаСсылка.Протокол.Получить());
			
		КонецЕсли;
	КонецЕсли;
	
	Возврат СтрокаСостояния;
	
КонецФункции

Функция ПоляНаСервереДляЗаписиОтправкиВФТС(ОбъектСсылка) Экспорт
	
	Возврат Новый Структура("ДатаНачалаПериода, ДатаОкончанияПериода, Версия",
		ОбъектСсылка.ДатаНачала, ОбъектСсылка.ДатаОкончания, ОбъектСсылка.Версия);
	
КонецФункции

Процедура ПередЗаписьюОтправкиФТС(Объект, Отказ)
	
	Если НЕ ЗначениеЗаполнено(Объект.Период) Тогда
		Объект.Период = СокрЛП(Формат(Объект.ДатаОкончанияПериода, "ДФ=yyyyMMdd") + Формат('39991231' - Объект.ДатаНачалаПериода, "ДФ=yyyyMMdd"));
	КонецЕсли;
	
	Если Объект.СтатусОтправки = Перечисления.СтатусыОтправки.Отправлен Тогда
		
		Если НЕ ЗначениеЗаполнено(Объект.ДатаЗакрытия) Тогда
			Объект.ДатаЗакрытия = ПолучитьПустуюДатуЗавершенияОтправкиФТС();
		КонецЕсли;
		
	Иначе
		Объект.ДатаЗакрытия = Объект.ДатаПолученияРезультата;
	КонецЕсли;
	
КонецПроцедуры

Функция СписокДопустимыхОрганизацийВОбъектахОбменаФТС() Экспорт
	
	Запрос = Новый Запрос;
	ТекстЗапроса = 
	"ВЫБРАТЬ РАЗРЕШЕННЫЕ
	|	НастройкиОбменаФТС.Организация КАК Организация
	|ИЗ
	|	РегистрСведений.НастройкиОбменаФТС КАК НастройкиОбменаФТС
	|ГДЕ
	|	НастройкиОбменаФТС.ИспользоватьОбмен = ИСТИНА";
	
	Запрос.Текст = ТекстЗапроса;
	Возврат Запрос.Выполнить().Выгрузить().ВыгрузитьКолонку("Организация");
	
КонецФункции

////////////////////////////////////////////////////////////////////////////////
// Обмен сервером

Функция ОтправитьВыгрузкуНаСерверФТС(ФайлыИДанныеОтправки, ПараметрыОтправки, ПараметрыСоединения)
	
	ПараметрыСоединения.ИдентификаторОтправкиНаСервере 				= "";
	ПараметрыСоединения.ПовторятьСоединение 						= Ложь;
	ПараметрыСоединения.ОрганизацияПодтверждена 					= Неопределено;
	ПараметрыСоединения.ОтправленЗапросНаПодтверждениеОрганизации 	= Ложь;
	ПараметрыСоединения.ТекстОтветаАвторизации 						= "";
	
	ЗагрузкаПроизведена = Ложь;
	
	АвторизацияПроизведена = (ПараметрыСоединения.СоединениеHTTP <> Неопределено);
	
	Если НЕ АвторизацияПроизведена Тогда
		// очистить пароль, если в диалоге сохранения пароля не установлена галочка сохранения
		Если НЕ ПараметрыСоединения.СохранитьПароль И ЗначениеЗаполнено(ПараметрыСоединения.СохраненныйПароль) Тогда
			ЗаписатьЛогинИПарольПорталаФТС(ПараметрыОтправки.Организация, , "");
			ПараметрыСоединения.СохраненныйПароль = "";
		КонецЕсли;
	КонецЕсли;
	
	Для НомерСервера = 1 По 2 Цикл
		АдресСервера = ПараметрыСоединения.ТекущийАдресСервера;
		
		Если НЕ АвторизацияПроизведена Тогда
			
			// устанавливаем соединение с сервером
			ОписаниеОшибкиУстановкиСоединения = "";
			ПараметрыСоединения.СоединениеHTTP = ДокументооборотСКО.УстановитьСоединениеССерверомИнтернета("https://" + АдресСервера,
				ОписаниеОшибкиУстановкиСоединения);
			
			Если ПараметрыСоединения.СоединениеHTTP = Неопределено Тогда
				ПараметрыСоединения.ПовторятьСоединение = Истина;
				СообщитьПользователю(
					СтрШаблон(НСтр("ru = 'Не удалось установить соединение с сервером:
									|%1'"), ОписаниеОшибкиУстановкиСоединения), ПараметрыСоединения.СообщенияПользователю);
				Возврат Ложь;
			КонецЕсли;
			
			ПараметрыСоединения.ЗначениеCookie = "";
			
			Если НомерСервера = 1 Тогда
				ЭтоВызовПередАвторизацией = Истина;
				Если НЕ УточнитьАдресСервераФТС(ПараметрыОтправки, ПараметрыСоединения, ЭтоВызовПередАвторизацией) Тогда
					ПараметрыСоединения.СоединениеHTTP = Неопределено;
					Возврат Ложь;
				КонецЕсли;
				
				Если ПараметрыСоединения.СоединениеHTTP = Неопределено Тогда
					// сервер сменился, нужно повторно соединиться
					Продолжить;
				КонецЕсли;
			КонецЕсли;
			
			АвторизацияПроизведена = АвторизоватьсяНаПорталеФТС(ПараметрыОтправки, ПараметрыСоединения);
			
		КонецЕсли;
		
		Если АвторизацияПроизведена Тогда
			Если НомерСервера = 1 Тогда
				ЭтоВызовПередАвторизацией = Ложь;
				Если НЕ УточнитьАдресСервераФТС(ПараметрыОтправки, ПараметрыСоединения, ЭтоВызовПередАвторизацией) Тогда
					ПараметрыСоединения.СоединениеHTTP = Неопределено;
					Возврат Ложь;
				КонецЕсли;
				
				Если ПараметрыСоединения.СоединениеHTTP = Неопределено Тогда
					// сервер сменился, нужно повторно соединиться
					АвторизацияПроизведена = Ложь;
					Продолжить;
				КонецЕсли;
			КонецЕсли;
		КонецЕсли;
		
		Прервать;
	КонецЦикла;
	
	Если АвторизацияПроизведена Тогда
		
		ЗагрузкаПроизведена = ЗагрузитьОтчетНаПорталФТС(ФайлыИДанныеОтправки.ИмяФайлаОтправки, ФайлыИДанныеОтправки.КороткоеИмяФайлаВыгрузки, ПараметрыОтправки, ПараметрыСоединения);
		
		Если НЕ ЗагрузкаПроизведена И ЗначениеЗаполнено(ПараметрыСоединения.ОписаниеОшибкиСервером) И НЕ ПараметрыСоединения.ПовторятьСоединение Тогда
			НастройкиПользователя = НастройкиПользователяФТС(ПараметрыОтправки, ПараметрыСоединения, Истина, ФайлыИДанныеОтправки.ЭтоЭлектроннаяПодписьВМоделиСервиса);
			Если НастройкиПользователя <> Неопределено Тогда
				ЗаполнитьЗначенияСвойств(ПараметрыСоединения, НастройкиПользователя);
			КонецЕсли;
		КонецЕсли;
		
	Иначе
		ПараметрыСоединения.СоединениеHTTP = Неопределено;
	КонецЕсли;
	
	Возврат ЗагрузкаПроизведена;
	
КонецФункции

Функция ОтправитьПодписьНаСерверФТС(ФайлыИДанныеОтправки, ПараметрыОтправки, ПараметрыСоединения)
	
	ПараметрыСоединения.ПовторятьСоединение = Ложь;
	ЗагрузкаПроизведена = Ложь;
	АдресСервера = ПараметрыСоединения.ТекущийАдресСервера;
	
	АвторизацияПроизведена = (ПараметрыСоединения.СоединениеHTTP <> Неопределено);
	
	Если НЕ АвторизацияПроизведена Тогда
		
		// устанавливаем соединение с сервером
		ОписаниеОшибкиУстановкиСоединения = "";
		ПараметрыСоединения.СоединениеHTTP = ДокументооборотСКО.УстановитьСоединениеССерверомИнтернета("https://" + АдресСервера, ОписаниеОшибкиУстановкиСоединения);
		
		Если ПараметрыСоединения.СоединениеHTTP = Неопределено Тогда
			ПараметрыСоединения.ПовторятьСоединение = Истина;
			СообщитьПользователю(
				СтрШаблон(НСтр("ru = 'Не удалось установить соединение с сервером:
								|%1'"), ОписаниеОшибкиУстановкиСоединения), ПараметрыСоединения.СообщенияПользователю);
			Возврат Ложь;
		КонецЕсли;
		
		ПараметрыСоединения.ЗначениеCookie = "";
		АвторизацияПроизведена = АвторизоватьсяНаПорталеФТС(ПараметрыОтправки, ПараметрыСоединения);
		
	КонецЕсли;
	
	Если АвторизацияПроизведена Тогда
		
		ЗагрузкаПроизведена = ЗагрузитьПодписьНаПорталФТС(ФайлыИДанныеОтправки.ИмяФайлаПодписи, ПараметрыОтправки, ПараметрыСоединения);
		
		Если НЕ ЗагрузкаПроизведена И ЗначениеЗаполнено(ПараметрыСоединения.ОписаниеОшибкиСервером) И НЕ ПараметрыСоединения.ПовторятьСоединение Тогда
			НастройкиПользователя = НастройкиПользователяФТС(ПараметрыОтправки, ПараметрыСоединения, Истина, ФайлыИДанныеОтправки.ЭтоЭлектроннаяПодписьВМоделиСервиса);
			Если НастройкиПользователя <> Неопределено Тогда
				ЗаполнитьЗначенияСвойств(ПараметрыСоединения, НастройкиПользователя);
			КонецЕсли;
		КонецЕсли;
		
	Иначе
		ПараметрыСоединения.СоединениеHTTP = Неопределено;
	КонецЕсли;
	
	Возврат ЗагрузкаПроизведена;
	
КонецФункции

Функция УточнитьАдресСервераФТС(
		ПараметрыОтправки,
		ПараметрыСоединения,
		ЭтоВызовПередАвторизацией,
		ВерсияПортала = Неопределено)
	
	ИспользуемаяВерсияПортала = ?(ВерсияПортала <> Неопределено, ВерсияПортала, ПараметрыСоединения.ВерсияПортала);
	
	АдресСервера = ПараметрыСоединения.ТекущийАдресСервера;
	
	Если ИспользуемаяВерсияПортала = "2017" И НЕ ЭтоВызовПередАвторизацией Тогда
		Возврат Истина;
	КонецЕсли;
	
	ЗаголовкиHTTP = Новый Соответствие();
	ЗаголовкиHTTP.Вставить("Proxy-Connection", "Keep-Alive");
	ЗаголовкиHTTP.Вставить("Pragma", "no-cache");
	Если ЗначениеЗаполнено(ПараметрыСоединения.ЗначениеCookie) Тогда
		ЗаголовкиHTTP.Вставить("Cookie", ПараметрыСоединения.ЗначениеCookie);
	КонецЕсли;
	
	ИмяФайлаОтвета = ПолучитьИмяВременногоФайла();
	
	Попытка
		АдресРесурса = ?(ИспользуемаяВерсияПортала = "2017",
						?(ЭтоВызовПередАвторизацией, "/FtsPersonalCabinetWeb2017/Service/GetConfig", "/FtsPersonalCabinetWeb2017/Service/GetVersion"),
						?(ЭтоВызовПередАвторизацией, "/", "/FtsPersonalCabinetWeb/Services/Obtain/Stat"));
						
		АдресРесурса = ДокументооборотСКО.ПолучитьЗаменяемоеЗначениеТестовогоСервера1С(АдресРесурса, "{FTS}" + АдресРесурса, ПараметрыСоединения.СоединениеHTTP);
		
		ОтветHTTP = ПараметрыСоединения.СоединениеHTTP.Получить(
			АдресРесурса,
			ИмяФайлаОтвета,
			ЗаголовкиHTTP);
	Исключение
		
		ПараметрыСоединения.ПовторятьСоединение = Истина;
		ОперацииСФайламиЭДКО.УдалитьВременныйФайл(ИмяФайлаОтвета);
		Возврат Ложь;
		
	КонецПопытки;
	
	ФайлОтвета = Новый ЧтениеТекста(ИмяФайлаОтвета, "utf-8");
	ТекстОтвета = ФайлОтвета.Прочитать();
	ФайлОтвета.Закрыть();
	
	ЗначениеSetCookie = ОтветHTTP.Заголовки.Получить("Set-Cookie");
	ПараметрыСоединения.ЗначениеCookie = ОбновленноеЗначениеCookie(ПараметрыСоединения.ЗначениеCookie, ЗначениеSetCookie);
	
	ОперацииСФайламиЭДКО.УдалитьВременныйФайл(ИмяФайлаОтвета);
	
	АдресСервераПолучен = Ложь;
	Если ОтветHTTP.КодСостояния >= ?(ИспользуемаяВерсияПортала = "2017", 200, 300) И ОтветHTTP.КодСостояния < 400 Тогда
		ТекстОтвета = нрег(ТекстОтвета);
		Если ИспользуемаяВерсияПортала = "2017" И ЭтоВызовПередАвторизацией Тогда
			ТекстОтвета = ТекстПослеПрефикса(ТекстОтвета, """scope"":""ftsstat""");
		КонецЕсли;
		ПолученныйАдресСервера = ТекстПослеПрефикса(ТекстОтвета,
			?(ИспользуемаяВерсияПортала = "2017", ?(ЭтоВызовПередАвторизацией, """host"":""", """viewurl"":""https://"),
			"<a href=""https://"), """");
		ПозицияРазделителя = СтрНайти(ПолученныйАдресСервера, "/");
		Если ПозицияРазделителя > 0 Тогда
			ПолученныйАдресСервера = Лев(ПолученныйАдресСервера, ПозицияРазделителя - 1);
		КонецЕсли;
		Если ЗначениеЗаполнено(ПолученныйАдресСервера) И Прав(ПолученныйАдресСервера, 11) = ".customs.ru" Тогда
			АдресСервера = ПолученныйАдресСервера;
			АдресСервераПолучен = Истина;
		КонецЕсли;
	КонецЕсли;
	
	Если АдресСервера <> ПараметрыСоединения.ТекущийАдресСервера Тогда
		ПараметрыСоединения.ТекущийАдресСервера = АдресСервера;
		ПараметрыСоединения.СоединениеHTTP = Неопределено;
	КонецЕсли;
	
	Если ЭтоВызовПередАвторизацией И НЕ АдресСервераПолучен И ИспользуемаяВерсияПортала <> "2017" Тогда
		УточнитьАдресСервераФТС(
			ПараметрыОтправки,
			ПараметрыСоединения,
			ЭтоВызовПередАвторизацией,
			"2017");
		ПараметрыСоединения.ПовторятьСоединение = Ложь;
	КонецЕсли;
	
	Возврат Истина;
	
КонецФункции

Функция АвторизоватьсяНаПорталеФТС(ПараметрыОтправки, ПараметрыСоединения)
	
	Если ПараметрыСоединения.ВерсияПортала = "2017" Тогда
		Возврат АвторизоватьсяНаПорталеФТС2017(ПараметрыОтправки, ПараметрыСоединения);
	КонецЕсли;
	
	ПараметрыСоединения.ОписаниеОшибкиСервером = "";
	ПараметрыСоединения.ПовторятьСоединение = Ложь;
	
	ЗаголовкиHTTP = Новый Соответствие();
	ЗаголовкиHTTP.Вставить("Content-Type", "application/json;charset=utf-8");
	ЗаголовкиHTTP.Вставить("Proxy-Connection", "Keep-Alive");
	ЗаголовкиHTTP.Вставить("Pragma", "no-cache");
	ЗаголовкиHTTP.Вставить("X-Ext.Net", "delta=true");
	Если ЗначениеЗаполнено(ПараметрыСоединения.ЗначениеCookie) Тогда
		ЗаголовкиHTTP.Вставить("Cookie", ПараметрыСоединения.ЗначениеCookie);
	КонецЕсли;
	
	ПакетДляАвторизации = СтрШаблон(
		"{""login"":""%1"",""password"":""%2""}",
		ПараметрыСоединения.Логин,
		ПараметрыСоединения.Пароль);
	ИмяФайлаЗапроса = ПолучитьИмяВременногоФайла();
	ЗаписываемыйФайл = Новый ЗаписьТекста(ИмяФайлаЗапроса, "utf-8");
	ЗаписываемыйФайл.Записать(ПакетДляАвторизации);
	ЗаписываемыйФайл.Закрыть();
	
	ИмяФайлаОтвета = ПолучитьИмяВременногоФайла();
	
	ОтветHTTP = Неопределено;
	Попытка
		ОтветHTTP = ПараметрыСоединения.СоединениеHTTP.ОтправитьДляОбработки(ИмяФайлаЗапроса, "/FtsPersonalCabinetWeb/Auth/Authenticate", ИмяФайлаОтвета, ЗаголовкиHTTP);
	Исключение
		
		ОбъектФайлОтвета = Новый Файл(ИмяФайлаОтвета);
		Если НЕ ОбъектФайлОтвета.Существует() Тогда
			ПараметрыСоединения.ПовторятьСоединение = Истина;
			ОперацииСФайламиЭДКО.УдалитьВременныйФайл(ИмяФайлаЗапроса);
			ОперацииСФайламиЭДКО.УдалитьВременныйФайл(ИмяФайлаОтвета);
			Возврат Ложь;
		КонецЕсли;
		
	КонецПопытки;
	
	Если ОтветHTTP <> Неопределено Тогда
		ЗначениеSetCookie = ОтветHTTP.Заголовки.Получить("Set-Cookie");
		ПараметрыСоединения.ЗначениеCookie = ОбновленноеЗначениеCookie(ПараметрыСоединения.ЗначениеCookie, ЗначениеSetCookie);
	КонецЕсли;
	
	ФайлОтвета = Новый ЧтениеТекста(ИмяФайлаОтвета, "utf-8");
	ТекстОтвета = ФайлОтвета.Прочитать();
	ФайлОтвета.Закрыть();
	
	ОперацииСФайламиЭДКО.УдалитьВременныйФайл(ИмяФайлаЗапроса);
	ОперацииСФайламиЭДКО.УдалитьВременныйФайл(ИмяФайлаОтвета);
	
	ПараметрыСоединения.ОписаниеОшибкиСервером = ТекстОшибкиПорталаФТС(ТекстОтвета);
	Если НЕ ЗначениеЗаполнено(ПараметрыСоединения.ОписаниеОшибкиСервером) Тогда
		ПараметрыСоединения.ОписаниеОшибкиСервером = ТекстПослеПрефикса(ТекстОтвета, "System.Exception:", " at ");
		ПараметрыСоединения.ОписаниеОшибкиСервером = СокрЛП(ПараметрыСоединения.ОписаниеОшибкиСервером);
	КонецЕсли;
	
	Если ЗначениеЗаполнено(ПараметрыСоединения.ОписаниеОшибкиСервером) ИЛИ СтрНайти(ТекстОтвета, "HTTP 404") > 0 Тогда
		ПараметрыСоединения.ВерсияПортала = "2017";
		Возврат АвторизоватьсяНаПорталеФТС2017(ПараметрыОтправки, ПараметрыСоединения);
	КонецЕсли;
	
	ПараметрыСоединения.ПовторятьЗапросПароля = ЗначениеЗаполнено(ПараметрыСоединения.ОписаниеОшибкиСервером);
	
	// сохранить пароль при успешной авторизации
	
	Если НЕ ПараметрыСоединения.ПовторятьЗапросПароля И (ПараметрыСоединения.Логин <> ПараметрыСоединения.СохраненныйЛогин ИЛИ (ПараметрыСоединения.СохранитьПароль И ПараметрыСоединения.Пароль <> ПараметрыСоединения.СохраненныйПароль)) Тогда
		ПарольДляСохранения = ?(ПараметрыСоединения.СохранитьПароль, ПараметрыСоединения.Пароль, "");
		ЗаписатьЛогинИПарольПорталаФТС(ПараметрыОтправки.Организация, ПараметрыСоединения.Логин, ПарольДляСохранения);
		ПараметрыСоединения.СохраненныйЛогин = ПараметрыСоединения.Логин;
		ПараметрыСоединения.СохраненныйПароль = ПарольДляСохранения;
	КонецЕсли;
	
	Результат = НЕ ЗначениеЗаполнено(ПараметрыСоединения.ОписаниеОшибкиСервером);
	
	// подтвердить организацию
	
	Если Результат Тогда
		ЗаголовкиHTTP = Новый Соответствие();
		ЗаголовкиHTTP.Вставить("Proxy-Connection", "Keep-Alive");
		ЗаголовкиHTTP.Вставить("Pragma", "no-cache");
		Если ЗначениеЗаполнено(ПараметрыСоединения.ЗначениеCookie) Тогда
			ЗаголовкиHTTP.Вставить("Cookie", ПараметрыСоединения.ЗначениеCookie);
		КонецЕсли;
		
		ИмяФайлаОтвета = ПолучитьИмяВременногоФайла();
		
		Попытка
			ОтветHTTP = ПараметрыСоединения.СоединениеHTTP.Получить("/FtsPersonalCabinetWeb/Auth/Organization", ИмяФайлаОтвета, ЗаголовкиHTTP);
		Исключение
			
			ПараметрыСоединения.ПовторятьСоединение = Истина;
			ОперацииСФайламиЭДКО.УдалитьВременныйФайл(ИмяФайлаОтвета);
			Возврат Ложь;
			
		КонецПопытки;
		
		ЗначениеSetCookie = ОтветHTTP.Заголовки.Получить("Set-Cookie");
		ПараметрыСоединения.ЗначениеCookie = ОбновленноеЗначениеCookie(ПараметрыСоединения.ЗначениеCookie, ЗначениеSetCookie);
		
		// получить свойства профиля
		
		ЗаголовкиHTTP = Новый Соответствие();
		ЗаголовкиHTTP.Вставить("Proxy-Connection", "Keep-Alive");
		ЗаголовкиHTTP.Вставить("Pragma", "no-cache");
		Если ЗначениеЗаполнено(ПараметрыСоединения.ЗначениеCookie) Тогда
			ЗаголовкиHTTP.Вставить("Cookie", ПараметрыСоединения.ЗначениеCookie);
		КонецЕсли;
		
		ИмяФайлаОтвета = ПолучитьИмяВременногоФайла();
		
		Попытка
			ОтветHTTP = ПараметрыСоединения.СоединениеHTTP.Получить("/FtsPersonalCabinetWeb/Api/GetConfig", ИмяФайлаОтвета, ЗаголовкиHTTP);
		Исключение
			
			ПараметрыСоединения.СоединениеHTTP = Неопределено;
			ПараметрыСоединения.ПовторятьСоединение = Истина;
			ОперацииСФайламиЭДКО.УдалитьВременныйФайл(ИмяФайлаОтвета);
			Возврат Неопределено;
			
		КонецПопытки;
		
		ФайлОтвета = Новый ЧтениеТекста(ИмяФайлаОтвета, "utf-8");
		ТекстОтвета = ФайлОтвета.Прочитать();
		ФайлОтвета.Закрыть();
		
		ЗначениеSetCookie = ОтветHTTP.Заголовки.Получить("Set-Cookie");
		ПараметрыСоединения.ЗначениеCookie = ОбновленноеЗначениеCookie(ПараметрыСоединения.ЗначениеCookie, ЗначениеSetCookie);
		
		ОперацииСФайламиЭДКО.УдалитьВременныйФайл(ИмяФайлаОтвета);
		
		ИНН = ИННОрганизации(ПараметрыОтправки.Организация, "");
		ИНН = СокрЛП(ИНН);
		Если СтрДлина(ИНН) = 10 Тогда
			ИНН = "00" + ИНН;
		КонецЕсли;
		ИдентификаторОрганизации = ТекстПослеПрефиксаПодобрать(
			ТекстОтвета,
			",""OrganizationId"":""",
			"""",
			",""ИНН"":""" + ИНН + """");
		
		Если ЗначениеЗаполнено(ИдентификаторОрганизации) Тогда
			ЗаголовкиHTTP = Новый Соответствие();
			ЗаголовкиHTTP.Вставить("Proxy-Connection", "Keep-Alive");
			ЗаголовкиHTTP.Вставить("Pragma", "no-cache");
			Если ЗначениеЗаполнено(ПараметрыСоединения.ЗначениеCookie) Тогда
				ЗаголовкиHTTP.Вставить("Cookie", ПараметрыСоединения.ЗначениеCookie);
			КонецЕсли;
			
			ИмяФайлаОтвета = ПолучитьИмяВременногоФайла();
			
			Попытка
				ОтветHTTP = ПараметрыСоединения.СоединениеHTTP.Получить("/FtsPersonalCabinetWeb/Auth/OrganizationSelect/" + ИдентификаторОрганизации, ИмяФайлаОтвета, ЗаголовкиHTTP);
			Исключение
				
				ПараметрыСоединения.СоединениеHTTP = Неопределено;
				ПараметрыСоединения.ПовторятьСоединение = Истина;
				ОперацииСФайламиЭДКО.УдалитьВременныйФайл(ИмяФайлаОтвета);
				Возврат Неопределено;
				
			КонецПопытки;
			
			ЗначениеSetCookie = ОтветHTTP.Заголовки.Получить("Set-Cookie");
			ПараметрыСоединения.ЗначениеCookie = ОбновленноеЗначениеCookie(ПараметрыСоединения.ЗначениеCookie, ЗначениеSetCookie);
			
			ОперацииСФайламиЭДКО.УдалитьВременныйФайл(ИмяФайлаОтвета);
		КонецЕсли;
	КонецЕсли;
	
	ОперацииСФайламиЭДКО.УдалитьВременныйФайл(ИмяФайлаОтвета);
	
	Возврат Результат;
	
КонецФункции

Функция АвторизоватьсяНаПорталеФТС2017(ПараметрыОтправки, ПараметрыСоединения)
	
	ПараметрыСоединения.ОписаниеОшибкиСервером = "";
	ПараметрыСоединения.ПовторятьСоединение = Ложь;
	
	ЗаголовкиHTTP = Новый Соответствие();
	ЗаголовкиHTTP.Вставить("Content-Type", "application/json;charset=utf-8");
	ЗаголовкиHTTP.Вставить("Proxy-Connection", "Keep-Alive");
	ЗаголовкиHTTP.Вставить("Pragma", "no-cache");
	Если ЗначениеЗаполнено(ПараметрыСоединения.ЗначениеCookie) Тогда
		ЗаголовкиHTTP.Вставить("Cookie", ПараметрыСоединения.ЗначениеCookie);
	КонецЕсли;
	
	ПакетДляАвторизации = СтрШаблон(
		"{""userName"":""%1"",""password"":""%2""}",
		ПараметрыСоединения.Логин,
		ПараметрыСоединения.Пароль);
	ИмяФайлаЗапроса = ПолучитьИмяВременногоФайла();
	ЗаписываемыйФайл = Новый ЗаписьТекста(ИмяФайлаЗапроса, "utf-8");
	ЗаписываемыйФайл.Записать(ПакетДляАвторизации);
	ЗаписываемыйФайл.Закрыть();
	
	ИмяФайлаОтвета = ПолучитьИмяВременногоФайла();
	
	ОтветHTTP = Неопределено;
	Попытка
		РесурсНаСервере = ДокументооборотСКО.ПолучитьЗаменяемоеЗначениеТестовогоСервера1С("/FtsPersonalCabinetWeb2017/Service/AuthenticateUserName", 
																							"{FTS}/FtsPersonalCabinetWeb2017/Service/AuthenticateUserName", 
																							ПараметрыСоединения.СоединениеHTTP);
		ОтветHTTP = ПараметрыСоединения.СоединениеHTTP.ОтправитьДляОбработки(
			ИмяФайлаЗапроса,
			РесурсНаСервере,
			ИмяФайлаОтвета,
			ЗаголовкиHTTP);
	Исключение
		
		ОбъектФайлОтвета = Новый Файл(ИмяФайлаОтвета);
		Если НЕ ОбъектФайлОтвета.Существует() Тогда
			ПараметрыСоединения.ПовторятьСоединение = Истина;
			ОперацииСФайламиЭДКО.УдалитьВременныйФайл(ИмяФайлаЗапроса);
			ОперацииСФайламиЭДКО.УдалитьВременныйФайл(ИмяФайлаОтвета);
			Возврат Ложь;
		КонецЕсли;
		
	КонецПопытки;
	
	Если ОтветHTTP <> Неопределено Тогда
		ЗначениеSetCookie = ОтветHTTP.Заголовки.Получить("Set-Cookie");
		ПараметрыСоединения.ЗначениеCookie = ОбновленноеЗначениеCookie(ПараметрыСоединения.ЗначениеCookie, ЗначениеSetCookie);
	КонецЕсли;
	
	ФайлОтвета = Новый ЧтениеТекста(ИмяФайлаОтвета, "utf-8");
	ТекстОтвета = ФайлОтвета.Прочитать();
	ФайлОтвета.Закрыть();
	
	ОперацииСФайламиЭДКО.УдалитьВременныйФайл(ИмяФайлаЗапроса);
	ОперацииСФайламиЭДКО.УдалитьВременныйФайл(ИмяФайлаОтвета);
	
	ПараметрыСоединения.ОписаниеОшибкиСервером = ТекстОшибкиПорталаФТС(ТекстОтвета, Истина);
	Если НЕ ЗначениеЗаполнено(ПараметрыСоединения.ОписаниеОшибкиСервером) Тогда
		ПараметрыСоединения.ОписаниеОшибкиСервером = ТекстПослеПрефикса(ТекстОтвета, "System.Exception:", " at ");
		ПараметрыСоединения.ОписаниеОшибкиСервером = СокрЛП(ПараметрыСоединения.ОписаниеОшибкиСервером);
	КонецЕсли;
	Если НЕ ЗначениеЗаполнено(ПараметрыСоединения.ОписаниеОшибкиСервером) И ОтветHTTP = Неопределено Тогда
		ПараметрыСоединения.ОписаниеОшибкиСервером = ТекстОтвета;
	КонецЕсли;
	
	ПараметрыСоединения.ПовторятьЗапросПароля = ЗначениеЗаполнено(ПараметрыСоединения.ОписаниеОшибкиСервером);
	
	Если НЕ ЗначениеЗаполнено(ПараметрыСоединения.ОписаниеОшибкиСервером) Тогда
		ПараметрыСоединения.ТекстОтветаАвторизации = ТекстОтвета;
	КонецЕсли;
	
	// сохранить пароль при успешной авторизации
	
	Если НЕ ПараметрыСоединения.ПовторятьЗапросПароля И (ПараметрыСоединения.Логин <> ПараметрыСоединения.СохраненныйЛогин ИЛИ (ПараметрыСоединения.СохранитьПароль И ПараметрыСоединения.Пароль <> ПараметрыСоединения.СохраненныйПароль)) Тогда
		ПарольДляСохранения = ?(ПараметрыСоединения.СохранитьПароль, ПараметрыСоединения.Пароль, "");
		ЗаписатьЛогинИПарольПорталаФТС(ПараметрыОтправки.Организация, ПараметрыСоединения.Логин, ПарольДляСохранения);
		ПараметрыСоединения.СохраненныйЛогин = ПараметрыСоединения.Логин;
		ПараметрыСоединения.СохраненныйПароль = ПарольДляСохранения;
	КонецЕсли;
	
	Результат = НЕ ЗначениеЗаполнено(ПараметрыСоединения.ОписаниеОшибкиСервером);
	
	// подтвердить организацию
	
	Если Результат Тогда
		ИНН = ИННОрганизации(ПараметрыОтправки.Организация, "");
		ИдентификаторОрганизации = ТекстПослеПрефиксаПодобрать(
			ТекстОтвета,
			",""OrganizationId"":""",
			"""",
			",""INN"":""" + СокрЛП(ИНН) + """",
			Истина);
		
		Если ЗначениеЗаполнено(ИдентификаторОрганизации) Тогда
			ЗаголовкиHTTP = Новый Соответствие();
			ЗаголовкиHTTP.Вставить("Content-Type", "application/json;charset=utf-8");
			ЗаголовкиHTTP.Вставить("Proxy-Connection", "Keep-Alive");
			ЗаголовкиHTTP.Вставить("Pragma", "no-cache");
			Если ЗначениеЗаполнено(ПараметрыСоединения.ЗначениеCookie) Тогда
				ЗаголовкиHTTP.Вставить("Cookie", ПараметрыСоединения.ЗначениеCookie);
			КонецЕсли;
			
			ПакетДляЗаданияОрганизации = СтрШаблон(
				"{""organizationId"":""%1""}",
				ИдентификаторОрганизации);
			ИмяФайлаЗапроса = ПолучитьИмяВременногоФайла();
			ЗаписываемыйФайл = Новый ЗаписьТекста(ИмяФайлаЗапроса, "utf-8");
			ЗаписываемыйФайл.Записать(ПакетДляЗаданияОрганизации);
			ЗаписываемыйФайл.Закрыть();
			
			ИмяФайлаОтвета = ПолучитьИмяВременногоФайла();
			
			Попытка
				РесурсНаСервере = ДокументооборотСКО.ПолучитьЗаменяемоеЗначениеТестовогоСервера1С("/FtsPersonalCabinetWeb2017/Service/SetActiveOrganization", 
																									"{FTS}/FtsPersonalCabinetWeb2017/Service/SetActiveOrganization", 
																									ПараметрыСоединения.СоединениеHTTP);
				ОтветHTTP = ПараметрыСоединения.СоединениеHTTP.ОтправитьДляОбработки(
					ИмяФайлаЗапроса,
					РесурсНаСервере,
					ИмяФайлаОтвета,
					ЗаголовкиHTTP);
			Исключение
				
				ПараметрыСоединения.СоединениеHTTP = Неопределено;
				ПараметрыСоединения.ПовторятьСоединение = Истина;
				ОперацииСФайламиЭДКО.УдалитьВременныйФайл(ИмяФайлаОтвета);
				Возврат Ложь;
				
			КонецПопытки;
			
			ЗначениеSetCookie = ОтветHTTP.Заголовки.Получить("Set-Cookie");
			ПараметрыСоединения.ЗначениеCookie = ОбновленноеЗначениеCookie(ПараметрыСоединения.ЗначениеCookie, ЗначениеSetCookie);
			
			ОперацииСФайламиЭДКО.УдалитьВременныйФайл(ИмяФайлаОтвета);
		КонецЕсли;
	КонецЕсли;
	
	ОперацииСФайламиЭДКО.УдалитьВременныйФайл(ИмяФайлаОтвета);
	
	Возврат Результат;
	
КонецФункции

Функция ЗагрузитьОтчетНаПорталФТС(
		МассивИменФайловОтправки,
		КороткоеИмяФайлаВыгрузки,
		ПараметрыОтправки,
		ПараметрыСоединения)
	
	ИмяФайлаОтправки = ?(ТипЗнч(МассивИменФайловОтправки) = Тип("Массив"),
		МассивИменФайловОтправки[?(ПараметрыСоединения.ВерсияПортала = "2017", 1, 0)],
		МассивИменФайловОтправки);
	
	Если ПараметрыСоединения.ВерсияПортала = "2017" Тогда
		Возврат ЗагрузитьОтчетНаПорталФТС2017(
			ИмяФайлаОтправки,
			КороткоеИмяФайлаВыгрузки,
			ПараметрыОтправки,
			ПараметрыСоединения);
	КонецЕсли;
	
	ПараметрыСоединения.ОписаниеОшибкиСервером = "";
	ПараметрыСоединения.ПовторятьСоединение = Ложь;
	
	РежимыОтправки = ПараметрыОтправки.РежимыОтправки;
	
	Если РежимыОтправки.ЭтоАннулированиеСОтправкойНового Тогда
		
		// посылаем запрос аннулирования с подачей
		
		ЗаголовкиHTTP = Новый Соответствие();
		ЗаголовкиHTTP.Вставить("Content-Type", "application/x-www-form-urlencoded");
		ЗаголовкиHTTP.Вставить("Proxy-Connection", "Keep-Alive");
		ЗаголовкиHTTP.Вставить("Pragma", "no-cache");
		ЗаголовкиHTTP.Вставить("X-Ext.Net", "delta=true");
		Если ЗначениеЗаполнено(ПараметрыСоединения.ЗначениеCookie) Тогда
			ЗаголовкиHTTP.Вставить("Cookie", ПараметрыСоединения.ЗначениеCookie);
		КонецЕсли;
		
		ПакетДляАннулированияСПодачей = "submitDirectEventConfig=" + ЗакодироватьПараметрСсылки(СтрШаблон(
			"{""config"":{""extraParams"":{""target"":""%1|%2|%3"",""action"":""Action_%4"",""prevAct"":""null""}}}",
			ПараметрыОтправки.ВерсияФормированияВыгрузки + "/" + ПараметрыОтправки.ВерсияОтправкиПортала,
			ТекстВШестнадцатеричныйUnicode("Подача статистической формы"),
			РежимыОтправки.ИдентификаторНеПринятойОтправкиНаСервере,
			ТекстВШестнадцатеричныйUnicode("Зарегистрирована_Аннулировать_с_подачей")))
			+ "&ctl55="
			+ "&__EVENTTARGET=ctl02"
			+ "&__EVENTARGUMENT=" + ЗакодироватьПараметрСсылки("-|public|ExecuteAction")
			+ "&TextFieldNote="
			+ "&DropDownCreateVersion=" + ЗакодироватьПараметрСсылки(ПараметрыОтправки.ВерсияФормированияВыгрузки + "/"
			+ ПараметрыОтправки.ВерсияОтправкиПортала);
		ИмяФайлаЗапроса = ПолучитьИмяВременногоФайла();
		ЗаписываемыйФайл = Новый ЗаписьТекста(ИмяФайлаЗапроса, "windows-1251");
		ЗаписываемыйФайл.Записать(ПакетДляАннулированияСПодачей);
		ЗаписываемыйФайл.Закрыть();
		
		ИмяФайлаОтвета = ПолучитьИмяВременногоФайла();
		
		Попытка
			РесурсНаСервере = ДокументооборотСКО.ПолучитьЗаменяемоеЗначениеТестовогоСервера1С("/FtsStatFormEpExtWeb/Stat/List.aspx", 
																								"{FTS}/FtsStatFormEpExtWeb/Stat/List.aspx", 
																								ПараметрыСоединения.СоединениеHTTP);				
			ОтветHTTP = ПараметрыСоединения.СоединениеHTTP.ОтправитьДляОбработки(
				ИмяФайлаЗапроса,
				РесурсНаСервере,
				ИмяФайлаОтвета,
				ЗаголовкиHTTP);
		Исключение
			
			ПараметрыСоединения.ПовторятьСоединение = Истина;
			Возврат Ложь;
			
		КонецПопытки;
		
		ЗначениеSetCookie = ОтветHTTP.Заголовки.Получить("Set-Cookie");
		ПараметрыСоединения.ЗначениеCookie = ОбновленноеЗначениеCookie(ПараметрыСоединения.ЗначениеCookie, ЗначениеSetCookie);
		
		ОперацииСФайламиЭДКО.УдалитьВременныйФайл(ИмяФайлаОтвета);
	КонецЕсли;
	
	// посылаем запрос для загрузки файла
	
	ЗаголовкиHTTP = Новый Соответствие();
	ЗаголовкиHTTP.Вставить("Content-Type", "multipart/form-data; boundary=My1cV8bNdr");
	ЗаголовкиHTTP.Вставить("Proxy-Connection", "Keep-Alive");
	ЗаголовкиHTTP.Вставить("Pragma", "no-cache");
	Если ЗначениеЗаполнено(ПараметрыСоединения.ЗначениеCookie) Тогда
		ЗаголовкиHTTP.Вставить("Cookie", ПараметрыСоединения.ЗначениеCookie);
	КонецЕсли;
	
	ИмяФайлаОтвета = ПолучитьИмяВременногоФайла();
	
	Попытка
		РесурсНаСервере = ДокументооборотСКО.ПолучитьЗаменяемоеЗначениеТестовогоСервера1С("/FtsStatFormEpExtWeb/Stat/List.aspx", 
																							"{FTS}/FtsStatFormEpExtWeb/Stat/List.aspx", 
																							ПараметрыСоединения.СоединениеHTTP);
		ОтветHTTP = ПараметрыСоединения.СоединениеHTTP.ОтправитьДляОбработки(ИмяФайлаОтправки, РесурсНаСервере, ИмяФайлаОтвета, ЗаголовкиHTTP);
	Исключение
		
		ПараметрыСоединения.ПовторятьСоединение = Истина;
		Возврат Ложь;
		
	КонецПопытки;
	
	ЗначениеSetCookie = ОтветHTTP.Заголовки.Получить("Set-Cookie");
	ПараметрыСоединения.ЗначениеCookie = ОбновленноеЗначениеCookie(ПараметрыСоединения.ЗначениеCookie, ЗначениеSetCookie);
	
	ФайлОтвета = Новый ЧтениеТекста(ИмяФайлаОтвета, "utf-8");
	ТекстОтвета = ФайлОтвета.Прочитать();
	ФайлОтвета.Закрыть();
	
	ОперацииСФайламиЭДКО.УдалитьВременныйФайл(ИмяФайлаОтвета);
	
	ПараметрыСоединения.ОписаниеОшибкиСервером = ТекстОшибкиПорталаФТС(ТекстОтвета);
	
	Если СтрНайти(ПараметрыСоединения.ОписаниеОшибкиСервером, "подтвердить данные об организации") > 0
		И СтрНайти(ПараметрыСоединения.ОписаниеОшибкиСервером, "Add_organization.html") > 0 Тогда
		ПараметрыСоединения.ОписаниеОшибкиСервером = НСтр("ru = 'Для отправки требуется подтвердить данные об ЭП организации, отправка запроса на подтверждение выполняется автоматически, повторите попытку отправки отчета позже.'");
	КонецЕсли;
	
	Если ЗначениеЗаполнено(ПараметрыСоединения.ОписаниеОшибкиСервером) Тогда
		Возврат Ложь;
	КонецЕсли;
	
	// сбрасываем отправки
	
	ЗаголовкиHTTP = Новый Соответствие();
	ЗаголовкиHTTP.Вставить("Content-Type", "application/x-www-form-urlencoded");
	ЗаголовкиHTTP.Вставить("Proxy-Connection", "Keep-Alive");
	ЗаголовкиHTTP.Вставить("Pragma", "no-cache");
	ЗаголовкиHTTP.Вставить("X-Ext.Net", "delta=true");
	Если ЗначениеЗаполнено(ПараметрыСоединения.ЗначениеCookie) Тогда
		ЗаголовкиHTTP.Вставить("Cookie", ПараметрыСоединения.ЗначениеCookie);
	КонецЕсли;
	
	ПакетДляСбросаОтправок = "submitDirectEventConfig=" + ЗакодироватьПараметрСсылки("{""config"":{""extraParams"":{""currentLastExchangeIn"":null}}}")
		+ "&ctl56=&__EVENTTARGET=ctl02&__EVENTARGUMENT=" + ЗакодироватьПараметрСсылки("-|public|Refresh")
		+ "&TextFieldNote=&DropDownCreateVersion=" + ЗакодироватьПараметрСсылки(ПараметрыОтправки.ВерсияФормированияВыгрузки + "/" + ПараметрыОтправки.ВерсияОтправкиПортала);
	ИмяФайлаЗапроса = ПолучитьИмяВременногоФайла();
	ЗаписываемыйФайл = Новый ЗаписьТекста(ИмяФайлаЗапроса, "windows-1251");
	ЗаписываемыйФайл.Записать(ПакетДляСбросаОтправок);
	ЗаписываемыйФайл.Закрыть();
	
	ИмяФайлаОтвета = ПолучитьИмяВременногоФайла();
	
	Попытка
		РесурсНаСервере = ДокументооборотСКО.ПолучитьЗаменяемоеЗначениеТестовогоСервера1С("/FtsStatFormEpExtWeb/Stat/List.aspx", 
																							"{FTS}/FtsStatFormEpExtWeb/Stat/List.aspx", 
																							ПараметрыСоединения.СоединениеHTTP);
		ОтветHTTP = ПараметрыСоединения.СоединениеHTTP.ОтправитьДляОбработки(ИмяФайлаЗапроса, РесурсНаСервере, ИмяФайлаОтвета, ЗаголовкиHTTP);
	Исключение
		
		ПараметрыСоединения.ПовторятьСоединение = Истина;
		Возврат Ложь;
		
	КонецПопытки;
	
	ЗначениеSetCookie = ОтветHTTP.Заголовки.Получить("Set-Cookie");
	ПараметрыСоединения.ЗначениеCookie = ОбновленноеЗначениеCookie(ПараметрыСоединения.ЗначениеCookie, ЗначениеSetCookie);
	
	ФайлОтвета = Новый ЧтениеТекста(ИмяФайлаОтвета, "utf-8");
	ТекстОтвета = ФайлОтвета.Прочитать();
	ФайлОтвета.Закрыть();
	
	ОперацииСФайламиЭДКО.УдалитьВременныйФайл(ИмяФайлаОтвета);
	
	ПараметрыСоединения.ОписаниеОшибкиСервером = ТекстОшибкиПорталаФТС(ТекстОтвета);
	
	Если ЗначениеЗаполнено(ПараметрыСоединения.ОписаниеОшибкиСервером) Тогда
		Возврат Ложь;
	КонецЕсли;
	
	Если РежимыОтправки.ЭтоАннулированиеСОтправкойНового Тогда
		ПараметрыСоединения.ИдентификаторОтправкиНаСервере = РежимыОтправки.ИдентификаторНеПринятойОтправкиНаСервере;
		
	Иначе
		
		// посылаем запрос списка для нахождения идентификатора загруженного отчета
		
		НомерСтраницы = 1;
		ЭлементовНаСтранице = 10;
		
		Пока Истина Цикл
			
			ЗаголовкиHTTP = Новый Соответствие();
			ЗаголовкиHTTP.Вставить("Content-Type", "application/x-www-form-urlencoded");
			ЗаголовкиHTTP.Вставить("Proxy-Connection", "Keep-Alive");
			ЗаголовкиHTTP.Вставить("Pragma", "no-cache");
			ЗаголовкиHTTP.Вставить("X-Ext.Net", "delta=true");
			Если ЗначениеЗаполнено(ПараметрыСоединения.ЗначениеCookie) Тогда
				ЗаголовкиHTTP.Вставить("Cookie", ПараметрыСоединения.ЗначениеCookie);
			КонецЕсли;
			
			НомерСтраницыСтрокой = Формат(НомерСтраницы, "ЧН=0; ЧГ=");
			ИндексНачальногоЭлементаСтрокой = Формат((НомерСтраницы - 1) * ЭлементовНаСтранице, "ЧН=0; ЧГ=");
			ЭлементовНаСтраницеСтрокой = Формат(ЭлементовНаСтранице, "ЧН=0; ЧГ=");
			ПакетДляПолученияСписка = "submitDirectEventConfig=" + ЗакодироватьПараметрСсылки("{""config"":{""extraParams"":{""page"":" + НомерСтраницыСтрокой + ",""start"":" + ИндексНачальногоЭлементаСтрокой + ",""limit"":" + ЭлементовНаСтраницеСтрокой + ",""sort"":""[{\""property\"":\""CreatedModifiedDate\"",\""direction\"":\""DESC\""}]""}}}")
				+ "&ctl55=&__EVENTTARGET=ctl02&__EVENTARGUMENT=" + ЗакодироватьПараметрСсылки("StoreStatForms|postback|read")
				+ "&TextFieldNote=&DropDownCreateVersion=" + ЗакодироватьПараметрСсылки(ПараметрыОтправки.ВерсияФормированияВыгрузки + "/" + ПараметрыОтправки.ВерсияОтправкиПортала);
			ИмяФайлаЗапроса = ПолучитьИмяВременногоФайла();
			ЗаписываемыйФайл = Новый ЗаписьТекста(ИмяФайлаЗапроса, "windows-1251");
			ЗаписываемыйФайл.Записать(ПакетДляПолученияСписка);
			ЗаписываемыйФайл.Закрыть();
			
			ИмяФайлаОтвета = ПолучитьИмяВременногоФайла();
			
			Попытка
				РесурсНаСервере = ДокументооборотСКО.ПолучитьЗаменяемоеЗначениеТестовогоСервера1С("/FtsStatFormEpExtWeb/Stat/List.aspx", 
																							"{FTS}/FtsStatFormEpExtWeb/Stat/List.aspx", 
																							ПараметрыСоединения.СоединениеHTTP);
				ОтветHTTP = ПараметрыСоединения.СоединениеHTTP.ОтправитьДляОбработки(ИмяФайлаЗапроса, РесурсНаСервере, ИмяФайлаОтвета, ЗаголовкиHTTP);
			Исключение
				
				ПараметрыСоединения.ПовторятьСоединение = Истина;
				Возврат Ложь;
				
			КонецПопытки;
			
			ЗначениеSetCookie = ОтветHTTP.Заголовки.Получить("Set-Cookie");
			ПараметрыСоединения.ЗначениеCookie = ОбновленноеЗначениеCookie(ПараметрыСоединения.ЗначениеCookie, ЗначениеSetCookie);
			
			ФайлОтвета = Новый ЧтениеТекста(ИмяФайлаОтвета, "utf-8");
			ТекстОтвета = ФайлОтвета.Прочитать();
			ФайлОтвета.Закрыть();
			
			ОперацииСФайламиЭДКО.УдалитьВременныйФайл(ИмяФайлаОтвета);
			
			ПараметрыСоединения.ОписаниеОшибкиСервером = ТекстОшибкиПорталаФТС(ТекстОтвета);
			
			Если ЗначениеЗаполнено(ПараметрыСоединения.ОписаниеОшибкиСервером) Тогда
				Возврат Ложь;
			КонецЕсли;
			
			ЗаголовкиОтчетов = ТаблицаJSON(ТекстОтвета, "data:{data:[", "ИдентификаторЗаписи, Наименование", "Id, Note", "success:true");
			
			Если ЗаголовкиОтчетов <> Неопределено Тогда
				Для каждого ЗаголовокОтчета Из ЗаголовкиОтчетов Цикл
					Если ЗаголовокОтчета.Наименование = КороткоеИмяФайлаВыгрузки Тогда
						ПараметрыСоединения.ИдентификаторОтправкиНаСервере = ЗаголовокОтчета.ИдентификаторЗаписи;
						Прервать;
					КонецЕсли;
				КонецЦикла;
			КонецЕсли;
			
			Если ЗначениеЗаполнено(ПараметрыСоединения.ИдентификаторОтправкиНаСервере) ИЛИ ЗаголовкиОтчетов = Неопределено ИЛИ ЗаголовкиОтчетов.Количество() < ЭлементовНаСтранице Тогда
				Прервать;
			КонецЕсли;
			
			НомерСтраницы = НомерСтраницы + 1;
			
		КонецЦикла;
	КонецЕсли;
	
	Если НЕ ЗначениеЗаполнено(ПараметрыСоединения.ИдентификаторОтправкиНаСервере) Тогда
		ПараметрыСоединения.ОписаниеОшибкиСервером = НСтр("ru = 'Ошибка загрузки отчета на портал Федеральной таможенной службы.'");
		Возврат Ложь;
	КонецЕсли;
	
	// посылаем запрос идентификатора отправки отчета
	
	ЗаголовкиHTTP = Новый Соответствие();
	ЗаголовкиHTTP.Вставить("Content-Type", "application/x-www-form-urlencoded");
	ЗаголовкиHTTP.Вставить("Proxy-Connection", "Keep-Alive");
	ЗаголовкиHTTP.Вставить("Pragma", "no-cache");
	ЗаголовкиHTTP.Вставить("X-Ext.Net", "delta=true");
	Если ЗначениеЗаполнено(ПараметрыСоединения.ЗначениеCookie) Тогда
		ЗаголовкиHTTP.Вставить("Cookie", ПараметрыСоединения.ЗначениеCookie);
	КонецЕсли;
	
	ПакетДляПолученияИдентификатораОтправки = "submitDirectEventConfig=" + ЗакодироватьПараметрСсылки(СтрШаблон(
		"{""config"":{""extraParams"":{""target"":""%1|%2|%3"",""action"":""Action_%4"",""prevAct"":""null""}}}",
		ПараметрыОтправки.ВерсияФормированияВыгрузки + "/" + ПараметрыОтправки.ВерсияОтправкиПортала,
		ТекстВШестнадцатеричныйUnicode("Подача статистической формы"),
		ПараметрыСоединения.ИдентификаторОтправкиНаСервере,
		?(РежимыОтправки.ЭтоАннулированиеСОтправкойНового,
		ТекстВШестнадцатеричныйUnicode("Подготовка_к_аннулированию_Отправить_аннулирование"),
		ТекстВШестнадцатеричныйUnicode("Отправить"))))
		+ "&ctl55="
		+ "&__EVENTTARGET=ctl02"
		+ "&__EVENTARGUMENT=" + ЗакодироватьПараметрСсылки("-|public|ExecuteAction")
		+ "&TextFieldNote="
		+ "&DropDownCreateVersion=" + ЗакодироватьПараметрСсылки(ПараметрыОтправки.ВерсияФормированияВыгрузки + "/"
		+ ПараметрыОтправки.ВерсияОтправкиПортала);
	ИмяФайлаЗапроса = ПолучитьИмяВременногоФайла();
	ЗаписываемыйФайл = Новый ЗаписьТекста(ИмяФайлаЗапроса, "windows-1251");
	ЗаписываемыйФайл.Записать(ПакетДляПолученияИдентификатораОтправки);
	ЗаписываемыйФайл.Закрыть();
	
	ИмяФайлаОтвета = ПолучитьИмяВременногоФайла();
	
	Попытка
		РесурсНаСервере = ДокументооборотСКО.ПолучитьЗаменяемоеЗначениеТестовогоСервера1С("/FtsStatFormEpExtWeb/Stat/List.aspx", 
																							"{FTS}/FtsStatFormEpExtWeb/Stat/List.aspx", 
																							ПараметрыСоединения.СоединениеHTTP);
		ОтветHTTP = ПараметрыСоединения.СоединениеHTTP.ОтправитьДляОбработки(
			ИмяФайлаЗапроса,
			РесурсНаСервере,
			ИмяФайлаОтвета,
			ЗаголовкиHTTP);
	Исключение
		
		ПараметрыСоединения.ПовторятьСоединение = Истина;
		Возврат Ложь;
		
	КонецПопытки;
	
	ЗначениеSetCookie = ОтветHTTP.Заголовки.Получить("Set-Cookie");
	ПараметрыСоединения.ЗначениеCookie = ОбновленноеЗначениеCookie(ПараметрыСоединения.ЗначениеCookie, ЗначениеSetCookie);
	
	ФайлОтвета = Новый ЧтениеТекста(ИмяФайлаОтвета, "utf-8");
	ТекстОтвета = ФайлОтвета.Прочитать();
	ФайлОтвета.Закрыть();
	
	ОперацииСФайламиЭДКО.УдалитьВременныйФайл(ИмяФайлаОтвета);
	
	ПараметрыСоединения.ОписаниеОшибкиСервером = ТекстОшибкиПорталаФТС(ТекстОтвета);
	
	Если ЗначениеЗаполнено(ПараметрыСоединения.ОписаниеОшибкиСервером) Тогда
		Возврат Ложь;
	КонецЕсли;
	
	ПараметрыСоединения.ИдентификаторВыполненияОтправки = ТекстПослеПрефикса(ТекстОтвета, "result:{""Id"":""", """");
	
	Если НЕ ЗначениеЗаполнено(ПараметрыСоединения.ИдентификаторВыполненияОтправки) Тогда
		ПараметрыСоединения.ОписаниеОшибкиСервером = НСтр("ru = 'Ошибка отправки отчета на портале Федеральной таможенной службы.'");
		Возврат Ложь;
	КонецЕсли;
	
	ИдентификаторВводаАннулирования = "";
	Если РежимыОтправки.ЭтоАннулированиеСОтправкойНового Тогда
		ИдентификаторВводаАннулирования = Строка(ТекстПослеПрефикса(ТекстОтвета, """Data"":""/FtsDocumentsMvc/PartEdit/Index/", """"));
		ПозицияРазделителя = СтрНайти(ИдентификаторВводаАннулирования, "?");
		Если ПозицияРазделителя > 0 Тогда
			ИдентификаторВводаАннулирования = Лев(ИдентификаторВводаАннулирования, ПозицияРазделителя - 1);
		КонецЕсли;
		
		Если ЗначениеЗаполнено(ИдентификаторВводаАннулирования) Тогда
			
			// открываем форму аннулирования
			
			ЗаголовкиHTTP = Новый Соответствие();
			ЗаголовкиHTTP.Вставить("Proxy-Connection", "Keep-Alive");
			ЗаголовкиHTTP.Вставить("Pragma", "no-cache");
			Если ЗначениеЗаполнено(ПараметрыСоединения.ЗначениеCookie) Тогда
				ЗаголовкиHTTP.Вставить("Cookie", ПараметрыСоединения.ЗначениеCookie);
			КонецЕсли;
			
			РесурсНаСервере = ДокументооборотСКО.ПолучитьЗаменяемоеЗначениеТестовогоСервера1С("/FtsDocumentsMvc/PartEdit/Index/", 
																								"{FTS}/FtsDocumentsMvc/PartEdit/Index/", 
																								ПараметрыСоединения.СоединениеHTTP) + ИдентификаторВводаАннулирования + "?HideMenu=False";			
			ЗапросHTTP = Новый HTTPЗапрос(
				РесурсНаСервере,
				ЗаголовкиHTTP);
			
			ИмяФайлаОтвета = ПолучитьИмяВременногоФайла();
			
			Попытка
				ОтветHTTP = ПараметрыСоединения.СоединениеHTTP.Получить(ЗапросHTTP, ИмяФайлаОтвета);
			Исключение
				
				ПараметрыСоединения.СоединениеHTTP = Неопределено;
				ПараметрыСоединения.ПовторятьСоединение = Истина;
				Возврат Неопределено;
				
			КонецПопытки;
			
			ЗначениеSetCookie = ОтветHTTP.Заголовки.Получить("Set-Cookie");
			ПараметрыСоединения.ЗначениеCookie = ОбновленноеЗначениеCookie(ПараметрыСоединения.ЗначениеCookie, ЗначениеSetCookie);
			
			ОперацииСФайламиЭДКО.УдалитьВременныйФайл(ИмяФайлаОтвета);
			
			// задаем текст причины аннулирования
			
			ЗаголовкиHTTP = Новый Соответствие();
			ЗаголовкиHTTP.Вставить("Content-Type", "application/x-www-form-urlencoded");
			ЗаголовкиHTTP.Вставить("Proxy-Connection", "Keep-Alive");
			ЗаголовкиHTTP.Вставить("Pragma", "no-cache");
			ЗаголовкиHTTP.Вставить("X-Ext.Net", "delta=true");
			Если ЗначениеЗаполнено(ПараметрыСоединения.ЗначениеCookie) Тогда
				ЗаголовкиHTTP.Вставить("Cookie", ПараметрыСоединения.ЗначениеCookie);
			КонецЕсли;
			
			ПакетДляЗаданияПричиныАннулирования = "InqRecallStaticFormType_RecallReason="
				+ ЗакодироватьПараметрСсылки(РежимыОтправки.ТекстПричиныАннулирования, "utf-8");
			ИмяФайлаЗапроса = ПолучитьИмяВременногоФайла();
			ЗаписываемыйФайл = Новый ЗаписьТекста(ИмяФайлаЗапроса, "windows-1251");
			ЗаписываемыйФайл.Записать(ПакетДляЗаданияПричиныАннулирования);
			ЗаписываемыйФайл.Закрыть();
			
			ИмяФайлаОтвета = ПолучитьИмяВременногоФайла();
			
			Попытка
				РесурсНаСервере = ДокументооборотСКО.ПолучитьЗаменяемоеЗначениеТестовогоСервера1С("/FtsDocumentsMvc/PartEdit/Update/", 
																									"{FTS}/FtsDocumentsMvc/PartEdit/Update/", 
																									ПараметрыСоединения.СоединениеHTTP) + ИдентификаторВводаАннулирования;
				ОтветHTTP = ПараметрыСоединения.СоединениеHTTP.ОтправитьДляОбработки(
					ИмяФайлаЗапроса,
					РесурсНаСервере,
					ИмяФайлаОтвета,
					ЗаголовкиHTTP);
			Исключение
				
				ПараметрыСоединения.ПовторятьСоединение = Истина;
				Возврат Ложь;
				
			КонецПопытки;
			
			ЗначениеSetCookie = ОтветHTTP.Заголовки.Получить("Set-Cookie");
			ПараметрыСоединения.ЗначениеCookie = ОбновленноеЗначениеCookie(ПараметрыСоединения.ЗначениеCookie, ЗначениеSetCookie);
			
			ОперацииСФайламиЭДКО.УдалитьВременныйФайл(ИмяФайлаОтвета);
			
			// сохраняем форму аннулирования
			
			ЗаголовкиHTTP = Новый Соответствие();
			ЗаголовкиHTTP.Вставить("Proxy-Connection", "Keep-Alive");
			ЗаголовкиHTTP.Вставить("Pragma", "no-cache");
			Если ЗначениеЗаполнено(ПараметрыСоединения.ЗначениеCookie) Тогда
				ЗаголовкиHTTP.Вставить("Cookie", ПараметрыСоединения.ЗначениеCookie);
			КонецЕсли;
			
			РесурсНаСервере = ДокументооборотСКО.ПолучитьЗаменяемоеЗначениеТестовогоСервера1С("/FtsDocumentsMvc/PartEdit/Save/", 
																								"{FTS}/FtsDocumentsMvc/PartEdit/Save/", 
																								ПараметрыСоединения.СоединениеHTTP) + ИдентификаторВводаАннулирования;			
			ЗапросHTTP = Новый HTTPЗапрос(
				РесурсНаСервере,
				ЗаголовкиHTTP);
			
			ИмяФайлаОтвета = ПолучитьИмяВременногоФайла();
			
			Попытка
				ОтветHTTP = ПараметрыСоединения.СоединениеHTTP.Получить(ЗапросHTTP, ИмяФайлаОтвета);
			Исключение
				
				ПараметрыСоединения.СоединениеHTTP = Неопределено;
				ПараметрыСоединения.ПовторятьСоединение = Истина;
				Возврат Неопределено;
				
			КонецПопытки;
			
			ЗначениеSetCookie = ОтветHTTP.Заголовки.Получить("Set-Cookie");
			ПараметрыСоединения.ЗначениеCookie = ОбновленноеЗначениеCookie(ПараметрыСоединения.ЗначениеCookie, ЗначениеSetCookie);
			
			ОперацииСФайламиЭДКО.УдалитьВременныйФайл(ИмяФайлаОтвета);
			
			// посылаем запрос подписываемых данных аннулирования
			
			ЗаголовкиHTTP = Новый Соответствие();
			ЗаголовкиHTTP.Вставить("Content-Type", "application/x-www-form-urlencoded");
			ЗаголовкиHTTP.Вставить("Proxy-Connection", "Keep-Alive");
			ЗаголовкиHTTP.Вставить("Pragma", "no-cache");
			ЗаголовкиHTTP.Вставить("X-Ext.Net", "delta=true");
			Если ЗначениеЗаполнено(ПараметрыСоединения.ЗначениеCookie) Тогда
				ЗаголовкиHTTP.Вставить("Cookie", ПараметрыСоединения.ЗначениеCookie);
			КонецЕсли;
			
			ПакетДляПолученияПодписываемыхДанных = "submitDirectEventConfig=" + ЗакодироватьПараметрСсылки(СтрШаблон(
				"{""config"":{""extraParams"":{""target"":""%1|%2|%3"",""action"":""Action_%4"","
				+ """prevAct"":""{\""Id\"":\""%5\"",\""Progress\"":null,\""Script\"":null,\""Function\"":null,"
				+ "\""DocumentForSign\"":null,\""DocumentForFill\"":"
				+ "{\""Name\"":\""%6\"",\""Data\"":\""/FtsDocumentsMvc/PartEdit/Index/%7?HideMenu=False\"","
				+ "\""StyleCls\"":\""documentEditorWindow\"",\""Params\"":{\""SelectDisable\"":\""true\""}},"
				+ "\""DocumentForFillSave\"":null,\""DocumentForView\"":null,\""DocumentForViewSign\"":null,\""DocumentForPrint\"":null,"
				+ "\""DocumentEditor\"":null,\""DocumentInEditor\"":null,\""SelectChoice\"":null,\""SelectNotedChoice\"":null,"
				+ "\""SelectMultipleChoice\"":null,\""ConfirmBox\"":null,\""MessageBox\"":null,\""InfoBox\"":null,\""InputBox\"":null,"
				+ "\""Window\"":null,\""Redirect\"":null,\""Download\"":null,\""Upload\"":null,\""Validation\"":null}""}}}",
				ПараметрыОтправки.ВерсияФормированияВыгрузки + "/" + ПараметрыОтправки.ВерсияОтправкиПортала,
				ТекстВШестнадцатеричныйUnicode("Подача статистической формы"),
				ПараметрыСоединения.ИдентификаторОтправкиНаСервере,
				ТекстВШестнадцатеричныйUnicode("Подготовка_к_аннулированию_Отправить_аннулирование"),
				ПараметрыСоединения.ИдентификаторВыполненияОтправки,
				СтрЗаменить(ТекстВШестнадцатеричныйUnicode("Заявление об аннулировании статистической формы"), "\", "\\"),
				ИдентификаторВводаАннулирования))
				+ "&ctl55="
				+ "&__EVENTTARGET=ctl02"
				+ "&__EVENTARGUMENT=" + ЗакодироватьПараметрСсылки("-|public|ExecuteAction")
				+ "&TextFieldNote="
				+ "&DropDownCreateVersion=" + ЗакодироватьПараметрСсылки(ПараметрыОтправки.ВерсияФормированияВыгрузки + "/"
				+ ПараметрыОтправки.ВерсияОтправкиПортала);
			ИмяФайлаЗапроса = ПолучитьИмяВременногоФайла();
			ЗаписываемыйФайл = Новый ЗаписьТекста(ИмяФайлаЗапроса, "windows-1251");
			ЗаписываемыйФайл.Записать(ПакетДляПолученияПодписываемыхДанных);
			ЗаписываемыйФайл.Закрыть();
			
			ИмяФайлаОтвета = ПолучитьИмяВременногоФайла();
			
			Попытка
				РесурсНаСервере = ДокументооборотСКО.ПолучитьЗаменяемоеЗначениеТестовогоСервера1С("/FtsStatFormEpExtWeb/Stat/List.aspx", 
																									"{FTS}/FtsStatFormEpExtWeb/Stat/List.aspx", 
																									ПараметрыСоединения.СоединениеHTTP);
				ОтветHTTP = ПараметрыСоединения.СоединениеHTTP.ОтправитьДляОбработки(
					ИмяФайлаЗапроса,
					РесурсНаСервере,
					ИмяФайлаОтвета,
					ЗаголовкиHTTP);
			Исключение
				
				ПараметрыСоединения.ПовторятьСоединение = Истина;
				Возврат Ложь;
				
			КонецПопытки;
			
			ЗначениеSetCookie = ОтветHTTP.Заголовки.Получить("Set-Cookie");
			ПараметрыСоединения.ЗначениеCookie = ОбновленноеЗначениеCookie(ПараметрыСоединения.ЗначениеCookie, ЗначениеSetCookie);
			
			ФайлОтвета = Новый ЧтениеТекста(ИмяФайлаОтвета, "utf-8");
			ТекстОтвета = ФайлОтвета.Прочитать();
			ФайлОтвета.Закрыть();
			
			ОперацииСФайламиЭДКО.УдалитьВременныйФайл(ИмяФайлаОтвета);
			
			ПараметрыСоединения.ОписаниеОшибкиСервером = ТекстОшибкиПорталаФТС(ТекстОтвета);
			
			Если ЗначениеЗаполнено(ПараметрыСоединения.ОписаниеОшибкиСервером) Тогда
				Возврат Ложь;
			КонецЕсли;
			
			ПараметрыСоединения.ИдентификаторВыполненияОтправки = ТекстПослеПрефикса(ТекстОтвета, "result:{""Id"":""", """");
			
			Если НЕ ЗначениеЗаполнено(ПараметрыСоединения.ИдентификаторВыполненияОтправки) Тогда
				ПараметрыСоединения.ОписаниеОшибкиСервером = НСтр("ru = 'Ошибка отправки отчета на портале Федеральной таможенной службы.'");
				Возврат Ложь;
			КонецЕсли;
		КонецЕсли;
	КонецЕсли;
	
	ТекстОтвета = Строка(ТекстПослеПрефикса(ТекстОтвета, """Data"":"""));
	ПозицияКонцаПараметров = СтрНайти(ТекстОтвета, """},""");
	ПозицияРазделителяПараметров = СтрНайти(ТекстОтвета, """,""");
	Если ПозицияКонцаПараметров > 0 ИЛИ ПозицияРазделителяПараметров > 0 Тогда
		Если (ПозицияКонцаПараметров > 0 И ПозицияРазделителяПараметров > 0 И ПозицияКонцаПараметров < ПозицияРазделителяПараметров) ИЛИ ПозицияРазделителяПараметров = 0 Тогда
			ПозицияРазделителяПараметров = ПозицияКонцаПараметров;
		КонецЕсли;
		ТекстОтвета = Лев(ТекстОтвета, ПозицияРазделителяПараметров - 1);
	КонецЕсли;
	ТекстПодписываемойВыгрузки = СтрЗаменить(ТекстОтвета, "\""", """");
	ТекстПодписываемойВыгрузки = СтрЗаменить(ТекстПодписываемойВыгрузки, "\r\n", Символы.ПС);
	
	Если НЕ ЗначениеЗаполнено(ТекстПодписываемойВыгрузки) И НЕ ЗначениеЗаполнено(ИдентификаторВводаАннулирования) Тогда
		
		// загружаем форму документа
		
		ЗаголовкиHTTP = Новый Соответствие();
		ЗаголовкиHTTP.Вставить("Content-Type", "application/x-www-form-urlencoded");
		ЗаголовкиHTTP.Вставить("Proxy-Connection", "Keep-Alive");
		ЗаголовкиHTTP.Вставить("Pragma", "no-cache");
		ЗаголовкиHTTP.Вставить("X-Ext.Net", "delta=true");
		Если ЗначениеЗаполнено(ПараметрыСоединения.ЗначениеCookie) Тогда
			ЗаголовкиHTTP.Вставить("Cookie", ПараметрыСоединения.ЗначениеCookie);
		КонецЕсли;
		
		ПакетДляПолученияИдентификатораОтправки = "submitDirectEventConfig=" + ЗакодироватьПараметрСсылки(СтрШаблон(
			"{""config"":{""extraParams"":{""target"":""%1""}}}",
			ПараметрыСоединения.ИдентификаторОтправкиНаСервере))
			+ "&ctl55="
			+ "&__EVENTTARGET="
			+ "ctl02"
			+ "&__EVENTARGUMENT=" + ЗакодироватьПараметрСсылки("-|public|BeginEditDocument")
			+ "&TextFieldNote="
			+ "&DropDownCreateVersion=" + ЗакодироватьПараметрСсылки(ПараметрыОтправки.ВерсияФормированияВыгрузки + "/"
			+ ПараметрыОтправки.ВерсияОтправкиПортала);
		ИмяФайлаЗапроса = ПолучитьИмяВременногоФайла();
		ЗаписываемыйФайл = Новый ЗаписьТекста(ИмяФайлаЗапроса, "windows-1251");
		ЗаписываемыйФайл.Записать(ПакетДляПолученияИдентификатораОтправки);
		ЗаписываемыйФайл.Закрыть();
		
		ИмяФайлаОтвета = ПолучитьИмяВременногоФайла();
		
		Попытка
			РесурсНаСервере = ДокументооборотСКО.ПолучитьЗаменяемоеЗначениеТестовогоСервера1С("/FtsStatFormEpExtWeb/Stat/List.aspx", 
																									"{FTS}/FtsStatFormEpExtWeb/Stat/List.aspx", 
																									ПараметрыСоединения.СоединениеHTTP);
			ОтветHTTP = ПараметрыСоединения.СоединениеHTTP.ОтправитьДляОбработки(ИмяФайлаЗапроса, РесурсНаСервере, ИмяФайлаОтвета, ЗаголовкиHTTP);
		Исключение
			
			ПараметрыСоединения.ПовторятьСоединение = Истина;
			Возврат Ложь;
			
		КонецПопытки;
		
		ЗначениеSetCookie = ОтветHTTP.Заголовки.Получить("Set-Cookie");
		ПараметрыСоединения.ЗначениеCookie = ОбновленноеЗначениеCookie(ПараметрыСоединения.ЗначениеCookie, ЗначениеSetCookie);
		
		ФайлОтвета = Новый ЧтениеТекста(ИмяФайлаОтвета, "utf-8");
		ТекстОтвета = ФайлОтвета.Прочитать();
		ФайлОтвета.Закрыть();
		
		ОперацииСФайламиЭДКО.УдалитьВременныйФайл(ИмяФайлаОтвета);
		
		ПараметрыСоединения.ОписаниеОшибкиСервером = ТекстОшибкиПорталаФТС(ТекстОтвета);
		
		Если ЗначениеЗаполнено(ПараметрыСоединения.ОписаниеОшибкиСервером) Тогда
			Возврат Ложь;
		КонецЕсли;
		
		ИдентификаторРедактированияФормы = ТекстПослеПрефикса(ТекстОтвета, "result:{""id"":""", """");
		Если НЕ ЗначениеЗаполнено(ПараметрыСоединения.ИдентификаторВыполненияОтправки) Тогда
			ПараметрыСоединения.ОписаниеОшибкиСервером = НСтр("ru = 'Ошибка отправки отчета на портале Федеральной таможенной службы.'");
			Возврат Ложь;
		КонецЕсли;
		
		// получаем сообщения об ошибках
		
		ЗаголовкиHTTP = Новый Соответствие();
		ЗаголовкиHTTP.Вставить("Proxy-Connection", "Keep-Alive");
		ЗаголовкиHTTP.Вставить("Pragma", "no-cache");
		Если ЗначениеЗаполнено(ПараметрыСоединения.ЗначениеCookie) Тогда
			ЗаголовкиHTTP.Вставить("Cookie", ПараметрыСоединения.ЗначениеCookie);
		КонецЕсли;
		
		РесурсНаСервере = ДокументооборотСКО.ПолучитьЗаменяемоеЗначениеТестовогоСервера1С("/FtsDocumentsMvc/PartEdit/Validate/", 
																							"{FTS}/FtsDocumentsMvc/PartEdit/Validate/", 
																							ПараметрыСоединения.СоединениеHTTP) + ИдентификаторРедактированияФормы;		
		ЗапросHTTP = Новый HTTPЗапрос(РесурсНаСервере, ЗаголовкиHTTP);
		
		ИмяФайлаОтвета = ПолучитьИмяВременногоФайла();
		
		Попытка
			ОтветHTTP = ПараметрыСоединения.СоединениеHTTP.Получить(ЗапросHTTP, ИмяФайлаОтвета);
		Исключение
			
			ПараметрыСоединения.СоединениеHTTP = Неопределено;
			ПараметрыСоединения.ПовторятьСоединение = Истина;
			Возврат Неопределено;
			
		КонецПопытки;
		
		ФайлОтвета = Новый ЧтениеТекста(ИмяФайлаОтвета, "utf-8");
		ТекстОтвета = ФайлОтвета.Прочитать();
		ФайлОтвета.Закрыть();
		
		ЗначениеSetCookie = ОтветHTTP.Заголовки.Получить("Set-Cookie");
		ПараметрыСоединения.ЗначениеCookie = ОбновленноеЗначениеCookie(ПараметрыСоединения.ЗначениеCookie, ЗначениеSetCookie);
		
		ОперацииСФайламиЭДКО.УдалитьВременныйФайл(ИмяФайлаОтвета);
		
		ТекстыПредупреждений = "";
		ПозицияПослеПостфикса = Неопределено;
		Пока ЗначениеЗаполнено(ТекстОтвета) Цикл
			ТекстУзлаПредупреждения = ТекстПослеПрефикса(ТекстОтвета, "<validateWarning", "</validateWarning>", ПозицияПослеПостфикса);
			Если ТекстУзлаПредупреждения = Неопределено Тогда
				Прервать;
			КонецЕсли;
			
			ТекстПредупреждения = ТекстПослеПрефикса(ТекстУзлаПредупреждения, ">");
			ТекстПредупреждения = Строка(ТекстПредупреждения);
			ТекстПредупреждения = ТекстИзHTML(ТекстПредупреждения);
			
			Если ЗначениеЗаполнено(ТекстПредупреждения) Тогда
				ЭлементПредупреждения = ТекстПослеПрефикса(ТекстУзлаПредупреждения, " elname='", "'");
				ЭлементПредупреждения = Строка(ЭлементПредупреждения);
				ЭлементПредупреждения = ТекстИзHTML(ЭлементПредупреждения);
				Если ЗначениеЗаполнено(ЭлементПредупреждения) Тогда
					ТекстПредупреждения = ЭлементПредупреждения + ": " + ТекстПредупреждения;
				КонецЕсли;
				
				ПолеПредупреждения = ТекстПослеПрефикса(ТекстУзлаПредупреждения, " path='", "'");
				ПолеПредупреждения = Строка(ПолеПредупреждения);
				ПолеПредупреждения = ТекстИзHTML(ПолеПредупреждения);
				Если ЗначениеЗаполнено(ПолеПредупреждения) Тогда
					ТекстПредупреждения = ТекстПредупреждения + " (" + ПолеПредупреждения + ")";
				КонецЕсли;
				
				ТекстыПредупреждений = ТекстыПредупреждений + ?(ЗначениеЗаполнено(ТекстыПредупреждений), "
					|", "") + ТекстПредупреждения;
			КонецЕсли;
			
			ТекстОтвета = Сред(ТекстОтвета, ПозицияПослеПостфикса);
		КонецЦикла;
		
		ПараметрыСоединения.ОписаниеОшибкиСервером = СтрШаблон(
			НСтр("ru = 'При проверке есть предупреждения, в том числе возможна недостоверность заявленных сведений о товарах"
					+ " по средним ценам, весу (в конце в скобках указано поле ошибки с индексом строки товара):
					   |%1
					   |
					   |В большинстве случаев, замечания при поверке достоверности сведений о товарах означают ошибки или опечатки"
					+ " в сведениях о товаре (цене, весе), информацию об этих товарах стоит уточнить у поставщика.
					   |Отправка статистической формы с недостоверными сведениями может стать причиной постановки ее на контроль"
					+ " и последующего привлечения заявителя к административной ответственности (АП) в соответствии с КОАП РФ.
					   |
					   |Существует возможность отправить файл выгрузки отчета с предупреждениями на портале %2'"),
			ТекстыПредупреждений,
			"https://" + ПараметрыОтправки.АдресСервера + "/FtsPersonalCabinetWeb2017/#?service=StatEp&start=0&view=List")
			+ " Для отображения полей с ошибками проведите проверку в web-форме статистической формы по кнопке ""Проверить""";
		Возврат Ложь;
	КонецЕсли;
	
	// вместо текста подписываемой выгрузки портал ФТС теперь возвращает его готовый хеш в Base64
	ИмяВременногоФайлаПодписываемойВыгрузки = ПолучитьИмяВременногоФайла();
	ДанныеПодписываемойВыгрузки = Base64Значение(ТекстПодписываемойВыгрузки);
	ДанныеПодписываемойВыгрузки.Записать(ИмяВременногоФайлаПодписываемойВыгрузки);
	ЧтениеФайлаПодписываемойВыгрузки = Новый ЧтениеТекста(ИмяВременногоФайлаПодписываемойВыгрузки, "windows-1251");
	ТекстПодписываемойВыгрузки = ЧтениеФайлаПодписываемойВыгрузки.Прочитать();
	ЧтениеФайлаПодписываемойВыгрузки.Закрыть();
	ОперацииСФайламиЭДКО.УдалитьВременныйФайл(ИмяВременногоФайлаПодписываемойВыгрузки);
	
	Если Лев(ТекстПодписываемойВыгрузки, 16) = "ObjectHashBase64" Тогда
		ТекстПодписываемойВыгрузки = Сред(ТекстПодписываемойВыгрузки, 17);
	КонецЕсли;
	ТекстПодписываемойВыгрузки = "<Object xmlns=""http://www.w3.org/2000/09/xmldsig#"" Id=""Object"">" + ТекстПодписываемойВыгрузки + "</Object>";
	
	ИмяВременногоФайлаПодписываемойВыгрузки = ПолучитьИмяВременногоФайла();
	ЗаписьФайлаПодписываемойВыгрузки = Новый ЗаписьТекста(ИмяВременногоФайлаПодписываемойВыгрузки, "windows-1251");
	ЗаписьФайлаПодписываемойВыгрузки.Записать(ТекстПодписываемойВыгрузки);
	ЗаписьФайлаПодписываемойВыгрузки.Закрыть();
	ДанныеПодписываемойВыгрузки = Новый ДвоичныеДанные(ИмяВременногоФайлаПодписываемойВыгрузки);
	ОперацииСФайламиЭДКО.УдалитьВременныйФайл(ИмяВременногоФайлаПодписываемойВыгрузки);
	ПараметрыСоединения.ПодписываемоеСодержимоеВыгрузки64 = Base64Строка(ДанныеПодписываемойВыгрузки);
	
	Если НЕ ЗначениеЗаполнено(ПараметрыСоединения.ПодписываемоеСодержимоеВыгрузки64) Тогда
		ПараметрыСоединения.ОписаниеОшибкиСервером = НСтр("ru = 'Ошибка отправки отчета на портале Федеральной таможенной службы.'");
		Возврат Ложь;
	КонецЕсли;
	
	Возврат НЕ ЗначениеЗаполнено(ПараметрыСоединения.ОписаниеОшибкиСервером);
	
КонецФункции

Функция ЗагрузитьОтчетНаПорталФТС2017(
		ИмяФайлаОтправки,
		КороткоеИмяФайлаВыгрузки,
		ПараметрыОтправки,
		ПараметрыСоединения)
	
	ПараметрыСоединения.ОписаниеОшибкиСервером = "";
	ПараметрыСоединения.ПовторятьСоединение = Ложь;
	
	РежимыОтправки = ПараметрыОтправки.РежимыОтправки;
	
	Если РежимыОтправки.ЭтоАннулированиеСОтправкойНового Тогда
		
		// посылаем запрос аннулирования с подачей
		
		ЗаголовкиHTTP = Новый Соответствие();
		ЗаголовкиHTTP.Вставить("Content-Type", "application/json;charset=utf-8");
		ЗаголовкиHTTP.Вставить("Proxy-Connection", "Keep-Alive");
		ЗаголовкиHTTP.Вставить("Pragma", "no-cache");
		Если ЗначениеЗаполнено(ПараметрыСоединения.ЗначениеCookie) Тогда
			ЗаголовкиHTTP.Вставить("Cookie", ПараметрыСоединения.ЗначениеCookie);
		КонецЕсли;
		
		ПакетДляАннулированияСПодачей = СтрШаблон(
			"{""Item"":{""SoftVersion"":""%1"""
			+ ",""ModelName"":""Подача статистической формы"""
			+ ",""Id"":""%2"""
			+ ",""Icon"":""PageCancel"""
			+ ",""Name"":""Action_Зарегистрирована_Аннулировать_с_подачей"""
			+ ",""DisplayName"":""Аннулировать с подачей новой статформы"""
			+ ",""executing"":true"
			+ ",""Action"":null}}",
			ПараметрыОтправки.ВерсияФормированияВыгрузки + "/" + ПараметрыОтправки.ВерсияОтправкиПортала,
			РежимыОтправки.ИдентификаторНеПринятойОтправкиНаСервере);
		ИмяФайлаЗапроса = ПолучитьИмяВременногоФайла();
		ЗаписываемыйФайл = Новый ЗаписьТекста(ИмяФайлаЗапроса, "utf-8");
		ЗаписываемыйФайл.Записать(ПакетДляАннулированияСПодачей);
		ЗаписываемыйФайл.Закрыть();
		
		ИмяФайлаОтвета = ПолучитьИмяВременногоФайла();
		
		ДокументооборотСКО.ДобавитьЗаголовкиДляТестовогоСервера1С(ЗаголовкиHTTP, ПараметрыСоединения.СоединениеHTTP);
		
		Попытка
			РесурсНаСервере = ДокументооборотСКО.ПолучитьЗаменяемоеЗначениеТестовогоСервера1С("/FtsPersonalCabinetWeb2017/NgActions/ExecuteAction", 
																								"{FTS}/FtsPersonalCabinetWeb2017/NgActions/ExecuteAction", 
																								ПараметрыСоединения.СоединениеHTTP);
			ОтветHTTP = ПараметрыСоединения.СоединениеHTTP.ОтправитьДляОбработки(
				ИмяФайлаЗапроса,
				РесурсНаСервере,
				ИмяФайлаОтвета,
				ЗаголовкиHTTP);
		Исключение
		КонецПопытки;
		
		Если ОтветHTTP <> Неопределено Тогда
			ЗначениеSetCookie = ОтветHTTP.Заголовки.Получить("Set-Cookie");
			ПараметрыСоединения.ЗначениеCookie = ОбновленноеЗначениеCookie(ПараметрыСоединения.ЗначениеCookie, ЗначениеSetCookie);
		КонецЕсли;
		
		ОбъектФайлОтвета = Новый Файл(ИмяФайлаОтвета);
		ТекстОтвета = "";
		Если ОбъектФайлОтвета.Существует() Тогда
			ФайлОтвета = Новый ЧтениеТекста(ИмяФайлаОтвета, "utf-8");
			ТекстОтвета = ФайлОтвета.Прочитать();
			ФайлОтвета.Закрыть();
		КонецЕсли;
		
		ИдентификаторЗапросаАннулирования = ТекстПослеПрефикса(ТекстОтвета, """Action"":{""Id"":""", """");
		
		ОперацииСФайламиЭДКО.УдалитьВременныйФайл(ИмяФайлаОтвета);
		
		// подтверждаем аннулирование
		
		ЗаголовкиHTTP = Новый Соответствие();
		ЗаголовкиHTTP.Вставить("Content-Type", "application/json;charset=utf-8");
		ЗаголовкиHTTP.Вставить("Proxy-Connection", "Keep-Alive");
		ЗаголовкиHTTP.Вставить("Pragma", "no-cache");
		Если ЗначениеЗаполнено(ПараметрыСоединения.ЗначениеCookie) Тогда
			ЗаголовкиHTTP.Вставить("Cookie", ПараметрыСоединения.ЗначениеCookie);
		КонецЕсли;
		
		ПакетДляАннулированияСПодачей = СтрШаблон(
			"{""Item"":{""SoftVersion"":""%1"""
			+ ",""ModelName"":""Подача статистической формы"""
			+ ",""Id"":""%2"""
			+ ",""Icon"":""PageCancel"""
			+ ",""Name"":""Action_Зарегистрирована_Аннулировать_с_подачей"""
			+ ",""DisplayName"":""Аннулировать с подачей новой статформы"""
			+ ",""executing"":true"
			+ ",""Action"":"
				+ "{""Id"":""%3"""
				+ ",""Progress"":null,""Script"":null,""Function"":null,""DocumentForSign"":null,""DocumentForFillSave"":null"
				+ ",""DocumentForView"":null,""DocumentForViewSign"":null,""DocumentForPrint"":null,""DocumentEditor"":null"
				+ ",""DocumentInEditor"":null,""SelectChoice"":null,""SelectNotedChoice"":null,""SelectMultipleChoice"":null"
				+ ",""ConfirmBox"":null,""MessageBox"":null,""InfoBox"":"
					+ "{""Title"":""Аннулировать с подачей новой статформы"""
					+ ",""Message"":""При аннулировании СФ с подачей новой СФ взамен - обратите внимание на следующее:"
						+ "<br/><br/>            - аннулируется СФ, поданная с ЭП."
						+ "<br/>            В списке создается отдельная строка;"
						+ "<br/><br/>            - аннулируется СФ, ранее поданная на бумажном носителе."
						+ "<br/>            Аналогично СФ с ЭП, создается строка."
						+ "<br/><br/>            Подробно процесс аннулирования СФ описан в разделе Справки.<br/>            """
					+ ",""ButtonText"":""OK"",""Width"":0,""Height"":0}"
				+ ",""InputBox"":null,""Window"":null,""Redirect"":null,""Download"":null,""Upload"":null"
				+ ",""Validation"":null,""Result"":null,""Success"":true,""Sending"":true}}}",
			ПараметрыОтправки.ВерсияФормированияВыгрузки + "/" + ПараметрыОтправки.ВерсияОтправкиПортала,
			РежимыОтправки.ИдентификаторНеПринятойОтправкиНаСервере,
			ИдентификаторЗапросаАннулирования);
		ИмяФайлаЗапроса = ПолучитьИмяВременногоФайла();
		ЗаписываемыйФайл = Новый ЗаписьТекста(ИмяФайлаЗапроса, "utf-8");
		ЗаписываемыйФайл.Записать(ПакетДляАннулированияСПодачей);
		ЗаписываемыйФайл.Закрыть();
		
		ИмяФайлаОтвета = ПолучитьИмяВременногоФайла();
		
		ДокументооборотСКО.ДобавитьЗаголовкиДляТестовогоСервера1С(ЗаголовкиHTTP, ПараметрыСоединения.СоединениеHTTP);
		
		Попытка
			РесурсНаСервере = ДокументооборотСКО.ПолучитьЗаменяемоеЗначениеТестовогоСервера1С("/FtsPersonalCabinetWeb2017/NgActions/ExecuteAction",
																								"{FTS}/FtsPersonalCabinetWeb2017/NgActions/ExecuteAction",
																								ПараметрыСоединения.СоединениеHTTP);
			ОтветHTTP = ПараметрыСоединения.СоединениеHTTP.ОтправитьДляОбработки(
				ИмяФайлаЗапроса,
				РесурсНаСервере,
				ИмяФайлаОтвета,
				ЗаголовкиHTTP);
		Исключение
		КонецПопытки;
		
		Если ОтветHTTP <> Неопределено Тогда
			ЗначениеSetCookie = ОтветHTTP.Заголовки.Получить("Set-Cookie");
			ПараметрыСоединения.ЗначениеCookie = ОбновленноеЗначениеCookie(ПараметрыСоединения.ЗначениеCookie, ЗначениеSetCookie);
		КонецЕсли;
		
		ОперацииСФайламиЭДКО.УдалитьВременныйФайл(ИмяФайлаОтвета);
	КонецЕсли;
	
	// посылаем запрос для загрузки файла
	
	ЗаголовкиHTTP = Новый Соответствие();
	ЗаголовкиHTTP.Вставить("Content-Type", "multipart/form-data; boundary=My1cV8bNdr");
	ЗаголовкиHTTP.Вставить("Proxy-Connection", "Keep-Alive");
	ЗаголовкиHTTP.Вставить("Pragma", "no-cache");
	Если ЗначениеЗаполнено(ПараметрыСоединения.ЗначениеCookie) Тогда
		ЗаголовкиHTTP.Вставить("Cookie", ПараметрыСоединения.ЗначениеCookie);
	КонецЕсли;
	
	ДокументооборотСКО.ДобавитьЗаголовкиДляТестовогоСервера1С(ЗаголовкиHTTP, ПараметрыСоединения.СоединениеHTTP);
	
	ИмяФайлаОтвета = ПолучитьИмяВременногоФайла();
	
	Попытка
		Если РежимыОтправки.ЭтоАннулированиеСОтправкойНового Тогда
			РесурсНаСервере = ДокументооборотСКО.ПолучитьЗаменяемоеЗначениеТестовогоСервера1С("/FtsPersonalCabinetWeb2017/Common/StatEpUpdateDataFromFile", 
																								"{FTS}/FtsPersonalCabinetWeb2017/Common/StatEpUpdateDataFromFile", 
																								ПараметрыСоединения.СоединениеHTTP) + "?id=" + РежимыОтправки.ИдентификаторНеПринятойОтправкиНаСервере;
		Иначе	
			РесурсНаСервере = ДокументооборотСКО.ПолучитьЗаменяемоеЗначениеТестовогоСервера1С("/FtsPersonalCabinetWeb2017/Common/StatEpUploadNew", 
																								"{FTS}/FtsPersonalCabinetWeb2017/Common/StatEpUploadNew", 
																								ПараметрыСоединения.СоединениеHTTP);
		КонецЕсли;	
		ОтветHTTP = ПараметрыСоединения.СоединениеHTTP.ОтправитьДляОбработки(
			ИмяФайлаОтправки,
			РесурсНаСервере,
			ИмяФайлаОтвета,
			ЗаголовкиHTTP);
	Исключение
		
		ОбъектФайлОтвета = Новый Файл(ИмяФайлаОтвета);
		Если НЕ ОбъектФайлОтвета.Существует() Тогда
			ПараметрыСоединения.ПовторятьСоединение = Истина;
			Возврат Ложь;
		КонецЕсли;
		
	КонецПопытки;
	
	Если ОтветHTTP <> Неопределено Тогда
		ЗначениеSetCookie = ОтветHTTP.Заголовки.Получить("Set-Cookie");
		ПараметрыСоединения.ЗначениеCookie = ОбновленноеЗначениеCookie(ПараметрыСоединения.ЗначениеCookie, ЗначениеSetCookie);
	КонецЕсли;
	
	ФайлОтвета = Новый ЧтениеТекста(ИмяФайлаОтвета, "utf-8");
	ТекстОтвета = ФайлОтвета.Прочитать();
	ФайлОтвета.Закрыть();
	
	ОперацииСФайламиЭДКО.УдалитьВременныйФайл(ИмяФайлаОтвета);
	
	ПараметрыСоединения.ОписаниеОшибкиСервером = ТекстОшибкиПорталаФТС(ТекстОтвета);
	Если НЕ ЗначениеЗаполнено(ПараметрыСоединения.ОписаниеОшибкиСервером) Тогда
		ОписаниеОшибкиСервером = ТекстПослеПрефикса(ТекстОтвета, "System.Exception:", "]");
		ОписаниеОшибкиСервером = СокрЛП(ОписаниеОшибкиСервером);
		ПараметрыСоединения.ОписаниеОшибкиСервером = ТекстИзHTML(ОписаниеОшибкиСервером);
	КонецЕсли;
	
	Если СтрНайти(ПараметрыСоединения.ОписаниеОшибкиСервером, "подтвердить данные об организации") > 0
		И СтрНайти(ПараметрыСоединения.ОписаниеОшибкиСервером, "Add_organization.html") > 0 Тогда
		ПараметрыСоединения.ОписаниеОшибкиСервером = НСтр("ru = 'Для отправки требуется подтвердить данные об ЭП организации, отправка запроса на подтверждение выполняется автоматически, повторите попытку отправки отчета позже.'");
	КонецЕсли;
	
	Если ЗначениеЗаполнено(ПараметрыСоединения.ОписаниеОшибкиСервером) Тогда
		Возврат Ложь;
	КонецЕсли;
	
	Если РежимыОтправки.ЭтоАннулированиеСОтправкойНового Тогда
		ПараметрыСоединения.ИдентификаторОтправкиНаСервере = РежимыОтправки.ИдентификаторНеПринятойОтправкиНаСервере;
		
	Иначе
		
		// посылаем запрос списка для нахождения идентификатора загруженного отчета
		
		НомерСтраницы = 1;
		ЭлементовНаСтранице = 10;
		
		Пока Истина Цикл
			
			ЗаголовкиHTTP = Новый Соответствие();
			ЗаголовкиHTTP.Вставить("Content-Type", "application/json;charset=utf-8");
			ЗаголовкиHTTP.Вставить("Proxy-Connection", "Keep-Alive");
			ЗаголовкиHTTP.Вставить("Pragma", "no-cache");
			Если ЗначениеЗаполнено(ПараметрыСоединения.ЗначениеCookie) Тогда
				ЗаголовкиHTTP.Вставить("Cookie", ПараметрыСоединения.ЗначениеCookie);
			КонецЕсли;
			
			ИндексНачальногоЭлементаСтрокой = Формат((НомерСтраницы - 1) * ЭлементовНаСтранице, "ЧН=0; ЧГ=");
			ПакетДляПолученияСписка = "{""OnlyMy"":0,""Filter"":"""",""ActionScopes"":""List""}";
			ИмяФайлаЗапроса = ПолучитьИмяВременногоФайла();
			ЗаписываемыйФайл = Новый ЗаписьТекста(ИмяФайлаЗапроса, "windows-1251");
			ЗаписываемыйФайл.Записать(ПакетДляПолученияСписка);
			ЗаписываемыйФайл.Закрыть();
			
			ИмяФайлаОтвета = ПолучитьИмяВременногоФайла();
			
			Попытка
				РесурсНаСервере = ДокументооборотСКО.ПолучитьЗаменяемоеЗначениеТестовогоСервера1С("/FtsPersonalCabinetWeb2017/UniversalDatabase/GetItems", 
																									"{FTS}/FtsPersonalCabinetWeb2017/UniversalDatabase/GetItems", 
																									ПараметрыСоединения.СоединениеHTTP) 
					+ "?scopeName=FtsStat&listName=StatDocuments&start=" + ИндексНачальногоЭлементаСтрокой;
					
				ОтветHTTP = ПараметрыСоединения.СоединениеHTTP.ОтправитьДляОбработки(
					ИмяФайлаЗапроса,
					РесурсНаСервере,
					ИмяФайлаОтвета,
					ЗаголовкиHTTP);
			Исключение
				
				ПараметрыСоединения.ПовторятьСоединение = Истина;
				Возврат Ложь;
				
			КонецПопытки;
			
			ЗначениеSetCookie = ОтветHTTP.Заголовки.Получить("Set-Cookie");
			ПараметрыСоединения.ЗначениеCookie = ОбновленноеЗначениеCookie(ПараметрыСоединения.ЗначениеCookie, ЗначениеSetCookie);
			
			ФайлОтвета = Новый ЧтениеТекста(ИмяФайлаОтвета, "utf-8");
			ТекстОтвета = ФайлОтвета.Прочитать();
			ФайлОтвета.Закрыть();
			
			ОперацииСФайламиЭДКО.УдалитьВременныйФайл(ИмяФайлаОтвета);
			
			ПараметрыСоединения.ОписаниеОшибкиСервером = ТекстОшибкиПорталаФТС(ТекстОтвета);
			
			Если ЗначениеЗаполнено(ПараметрыСоединения.ОписаниеОшибкиСервером) Тогда
				Возврат Ложь;
			КонецЕсли;
			
			ЗаголовкиОтчетов = ТаблицаJSON(
				ТекстОтвета,
				"""Items"":[",
				"ИдентификаторЗаписи, Наименование",
				"Id, Note",
				"""Total"":",
				Истина,
				Истина);
			
			Если ЗаголовкиОтчетов <> Неопределено Тогда
				Для каждого ЗаголовокОтчета Из ЗаголовкиОтчетов Цикл
					Если ЗаголовокОтчета.Наименование = КороткоеИмяФайлаВыгрузки Тогда
						ПараметрыСоединения.ИдентификаторОтправкиНаСервере = ЗаголовокОтчета.ИдентификаторЗаписи;
						Прервать;
					КонецЕсли;
				КонецЦикла;
			КонецЕсли;
			
			Если ЗначениеЗаполнено(ПараметрыСоединения.ИдентификаторОтправкиНаСервере) ИЛИ ЗаголовкиОтчетов = Неопределено ИЛИ ЗаголовкиОтчетов.Количество() < ЭлементовНаСтранице Тогда
				Прервать;
			КонецЕсли;
			
			НомерСтраницы = НомерСтраницы + 1;
			
		КонецЦикла;
	КонецЕсли;
	
	Если НЕ ЗначениеЗаполнено(ПараметрыСоединения.ИдентификаторОтправкиНаСервере) Тогда
		ПараметрыСоединения.ОписаниеОшибкиСервером = НСтр("ru = 'Ошибка загрузки отчета на портал Федеральной таможенной службы.'");
		Возврат Ложь;
	КонецЕсли;
	
	// посылаем запрос идентификатора отправки отчета
	
	ЗаголовкиHTTP = Новый Соответствие();
	ЗаголовкиHTTP.Вставить("Content-Type", "application/json;charset=utf-8");
	ЗаголовкиHTTP.Вставить("Proxy-Connection", "Keep-Alive");
	ЗаголовкиHTTP.Вставить("Pragma", "no-cache");
	Если ЗначениеЗаполнено(ПараметрыСоединения.ЗначениеCookie) Тогда
		ЗаголовкиHTTP.Вставить("Cookie", ПараметрыСоединения.ЗначениеCookie);
	КонецЕсли;
	
	ПакетДляПолученияИдентификатораОтправки = СтрШаблон(
		"{""Item"":{""SoftVersion"":""%1"""
		+ ",""ModelName"":""Подача статистической формы"""
		+ ",""Id"":""%2"""
		+ ",""Icon"":""EmailGo"""
		+ ",""Name"":""%3"""
		+ ",""DisplayName"":""%4"""
		+ ",""executing"":true"
		+ ",""Action"":null}}",
		ПараметрыОтправки.ВерсияФормированияВыгрузки + "/" + ПараметрыОтправки.ВерсияОтправкиПортала,
		ПараметрыСоединения.ИдентификаторОтправкиНаСервере,
		?(РежимыОтправки.ЭтоАннулированиеСОтправкойНового, "Action_Подготовка_к_аннулированию_Отправить_аннулирование",
		"Action_Отправить"),
		?(РежимыОтправки.ЭтоАннулированиеСОтправкойНового, "Отправить заявление об аннулировании", "Отправить"));
	ИмяФайлаЗапроса = ПолучитьИмяВременногоФайла();
	ЗаписываемыйФайл = Новый ЗаписьТекста(ИмяФайлаЗапроса, "utf-8");
	ЗаписываемыйФайл.Записать(ПакетДляПолученияИдентификатораОтправки);
	ЗаписываемыйФайл.Закрыть();
	
	ИмяФайлаОтвета = ПолучитьИмяВременногоФайла();
	
	Попытка
		РесурсНаСервере = ДокументооборотСКО.ПолучитьЗаменяемоеЗначениеТестовогоСервера1С("/FtsPersonalCabinetWeb2017/NgActions/ExecuteAction", 
																							"{FTS}/FtsPersonalCabinetWeb2017/NgActions/ExecuteAction", 
																							ПараметрыСоединения.СоединениеHTTP);
		ОтветHTTP = ПараметрыСоединения.СоединениеHTTP.ОтправитьДляОбработки(
			ИмяФайлаЗапроса,
			РесурсНаСервере,
			ИмяФайлаОтвета,
			ЗаголовкиHTTP);
	Исключение
		
		ОбъектФайлОтвета = Новый Файл(ИмяФайлаОтвета);
		Если НЕ ОбъектФайлОтвета.Существует() Тогда
			ПараметрыСоединения.ПовторятьСоединение = Истина;
			Возврат Ложь;
		КонецЕсли;
		
	КонецПопытки;
	
	Если ОтветHTTP <> Неопределено Тогда
		ЗначениеSetCookie = ОтветHTTP.Заголовки.Получить("Set-Cookie");
		ПараметрыСоединения.ЗначениеCookie = ОбновленноеЗначениеCookie(ПараметрыСоединения.ЗначениеCookie, ЗначениеSetCookie);
	КонецЕсли;
	
	ФайлОтвета = Новый ЧтениеТекста(ИмяФайлаОтвета, "utf-8");
	ТекстОтвета = ФайлОтвета.Прочитать();
	ФайлОтвета.Закрыть();
	
	ОперацииСФайламиЭДКО.УдалитьВременныйФайл(ИмяФайлаОтвета);
	
	ПараметрыСоединения.ОписаниеОшибкиСервером = ТекстОшибкиПорталаФТС(ТекстОтвета, Истина);
	
	Если ЗначениеЗаполнено(ПараметрыСоединения.ОписаниеОшибкиСервером) Тогда
		Возврат Ложь;
	КонецЕсли;
	
	ПараметрыСоединения.ИдентификаторВыполненияОтправки = ТекстПослеПрефикса(ТекстОтвета, """Action"":{""Id"":""", """");
	
	Если НЕ ЗначениеЗаполнено(ПараметрыСоединения.ИдентификаторВыполненияОтправки) Тогда
		ПараметрыСоединения.ОписаниеОшибкиСервером = НСтр("ru = 'Ошибка отправки отчета на портале Федеральной таможенной службы.'");
		Возврат Ложь;
	КонецЕсли;
	
	Если НЕ РежимыОтправки.ЭтоАннулированиеСОтправкойНового Тогда
		
		// получаем хеш
		
		ЗаголовкиHTTP = Новый Соответствие();
		ЗаголовкиHTTP.Вставить("Content-Type", "application/json;charset=utf-8");
		ЗаголовкиHTTP.Вставить("Proxy-Connection", "Keep-Alive");
		ЗаголовкиHTTP.Вставить("Pragma", "no-cache");
		Если ЗначениеЗаполнено(ПараметрыСоединения.ЗначениеCookie) Тогда
			ЗаголовкиHTTP.Вставить("Cookie", ПараметрыСоединения.ЗначениеCookie);
		КонецЕсли;
		
		ПараметрыДляПодписи = СтрШаблон(
			"{""CertificateThumbprint"":""%1""}",
			ВРег(ПараметрыСоединения.СертификатАбонента.Отпечаток));
		ДвоичныеДанныеПараметровДляПодписи = ПолучитьДвоичныеДанныеИзСтроки(ПараметрыДляПодписи, "windows-1251");
		ПараметрыДляПодписи64 = Base64Строка(ДвоичныеДанныеПараметровДляПодписи);
		ПараметрыДляПодписи64 = СтрЗаменить(ПараметрыДляПодписи64, Символы.ВК, "");
		ПараметрыДляПодписи64 = СтрЗаменить(ПараметрыДляПодписи64, Символы.ПС, "");
		
		ПакетДляПолученияИдентификатораОтправки = СтрШаблон(
			"{""Item"":{""SoftVersion"":""%1"""
			+ ",""ModelName"":""Подача статистической формы"""
			+ ",""Id"":""%2"""
			+ ",""Name"":""Action_Отправить"""
			+ ",""DisplayName"":""Отправить"""
			+ ",""executing"":true"
			+ ",""Action"":"
				+ "{""Id"":""%3"""
				+ ",""Progress"":null,""Script"":null,""Function"":null"
				+ ",""DocumentForSign"":"
					+ "{""Data"":""%4"""
					+ ",""Name"":""Статистическая форма учета перемещения товара (Документ: 0,01 Мб )"""
					+ ",""Text"":null}"
				+ ",""DocumentForFill"":null,""DocumentForFillSave"":null,""DocumentForView"":null,""DocumentForViewSign"":null"
				+ ",""DocumentForPrint"":null,""DocumentEditor"":null,""DocumentInEditor"":null,""SelectChoice"":null"
				+ ",""SelectNotedChoice"":null,""SelectMultipleChoice"":null,""ConfirmBox"":null,""MessageBox"":null"
				+ ",""InfoBox"":null,""InputBox"":null,""Window"":null,""Redirect"":null,""Download"":null,""Upload"":null"
				+ ",""Validation"":null,""Result"":null,""Success"":true,""Sending"":true}}}",
			ПараметрыОтправки.ВерсияФормированияВыгрузки + "/" + ПараметрыОтправки.ВерсияОтправкиПортала,
			ПараметрыСоединения.ИдентификаторОтправкиНаСервере,
			ПараметрыСоединения.ИдентификаторВыполненияОтправки,
			ПараметрыДляПодписи64);
		ИмяФайлаЗапроса = ПолучитьИмяВременногоФайла();
		ЗаписываемыйФайл = Новый ЗаписьТекста(ИмяФайлаЗапроса, "utf-8");
		ЗаписываемыйФайл.Записать(ПакетДляПолученияИдентификатораОтправки);
		ЗаписываемыйФайл.Закрыть();
		
		ИмяФайлаОтвета = ПолучитьИмяВременногоФайла();
		
		Попытка
			РесурсНаСервере = ДокументооборотСКО.ПолучитьЗаменяемоеЗначениеТестовогоСервера1С("/FtsPersonalCabinetWeb2017/NgActions/ExecuteAction",
																								"{FTS}/FtsPersonalCabinetWeb2017/NgActions/ExecuteAction",
																								ПараметрыСоединения.СоединениеHTTP);
			ОтветHTTP = ПараметрыСоединения.СоединениеHTTP.ОтправитьДляОбработки(
				ИмяФайлаЗапроса,
				РесурсНаСервере,
				ИмяФайлаОтвета,
				ЗаголовкиHTTP);
		Исключение
			
			ОбъектФайлОтвета = Новый Файл(ИмяФайлаОтвета);
			Если НЕ ОбъектФайлОтвета.Существует() Тогда
				ПараметрыСоединения.ПовторятьСоединение = Истина;
				Возврат Ложь;
			КонецЕсли;
			
		КонецПопытки;
		
		Если ОтветHTTP <> Неопределено Тогда
			ЗначениеSetCookie = ОтветHTTP.Заголовки.Получить("Set-Cookie");
			ПараметрыСоединения.ЗначениеCookie = ОбновленноеЗначениеCookie(ПараметрыСоединения.ЗначениеCookie, ЗначениеSetCookie);
		КонецЕсли;
		
		ФайлОтвета = Новый ЧтениеТекста(ИмяФайлаОтвета, "utf-8");
		ТекстОтвета = ФайлОтвета.Прочитать();
		ФайлОтвета.Закрыть();
		
		ОперацииСФайламиЭДКО.УдалитьВременныйФайл(ИмяФайлаОтвета);
		
		ПараметрыСоединения.ОписаниеОшибкиСервером = ТекстОшибкиПорталаФТС(ТекстОтвета, Истина);
		
		Если ЗначениеЗаполнено(ПараметрыСоединения.ОписаниеОшибкиСервером) Тогда
			Возврат Ложь;
		КонецЕсли;
		
		ПараметрыСоединения.ИдентификаторВыполненияОтправки = ТекстПослеПрефикса(ТекстОтвета, """Action"":{""Id"":""", """");
		
		Если НЕ ЗначениеЗаполнено(ПараметрыСоединения.ИдентификаторВыполненияОтправки) Тогда
			ПараметрыСоединения.ОписаниеОшибкиСервером = НСтр("ru = 'Ошибка отправки отчета на портале Федеральной таможенной службы.'");
			Возврат Ложь;
		КонецЕсли;
	КонецЕсли;
	
	ИдентификаторВводаАннулирования = "";
	Если РежимыОтправки.ЭтоАннулированиеСОтправкойНового Тогда
		ИдентификаторВводаАннулирования = Строка(ТекстПослеПрефикса(ТекстОтвета, """Data"":""../FtsDocumentsMvc/PartEdit/Index/", """"));
		ПозицияРазделителя = СтрНайти(ИдентификаторВводаАннулирования, "?");
		Если ПозицияРазделителя > 0 Тогда
			ИдентификаторВводаАннулирования = Лев(ИдентификаторВводаАннулирования, ПозицияРазделителя - 1);
		КонецЕсли;
		
		Если ЗначениеЗаполнено(ИдентификаторВводаАннулирования) Тогда
			
			// открываем форму аннулирования
			
			ЗаголовкиHTTP = Новый Соответствие();
			ЗаголовкиHTTP.Вставить("Proxy-Connection", "Keep-Alive");
			ЗаголовкиHTTP.Вставить("Pragma", "no-cache");
			Если ЗначениеЗаполнено(ПараметрыСоединения.ЗначениеCookie) Тогда
				ЗаголовкиHTTP.Вставить("Cookie", ПараметрыСоединения.ЗначениеCookie);
			КонецЕсли;
			
			РесурсНаСервере = ДокументооборотСКО.ПолучитьЗаменяемоеЗначениеТестовогоСервера1С("/FtsDocumentsMvc/PartEdit/Index/", 
																								"{FTS}/FtsDocumentsMvc/PartEdit/Index/", 
																								ПараметрыСоединения.СоединениеHTTP) + ИдентификаторВводаАннулирования + "?HideMenu=False";
			
			ЗапросHTTP = Новый HTTPЗапрос(
				РесурсНаСервере,
				ЗаголовкиHTTP);
			
			ИмяФайлаОтвета = ПолучитьИмяВременногоФайла();
			
			Попытка
				ОтветHTTP = ПараметрыСоединения.СоединениеHTTP.Получить(ЗапросHTTP, ИмяФайлаОтвета);
			Исключение
				
				ПараметрыСоединения.СоединениеHTTP = Неопределено;
				ПараметрыСоединения.ПовторятьСоединение = Истина;
				Возврат Неопределено;
				
			КонецПопытки;
			
			ЗначениеSetCookie = ОтветHTTP.Заголовки.Получить("Set-Cookie");
			ПараметрыСоединения.ЗначениеCookie = ОбновленноеЗначениеCookie(ПараметрыСоединения.ЗначениеCookie, ЗначениеSetCookie);
			
			ОперацииСФайламиЭДКО.УдалитьВременныйФайл(ИмяФайлаОтвета);
			
			// задаем текст причины аннулирования
			
			ЗаголовкиHTTP = Новый Соответствие();
			ЗаголовкиHTTP.Вставить("Content-Type", "application/x-www-form-urlencoded");
			ЗаголовкиHTTP.Вставить("Proxy-Connection", "Keep-Alive");
			ЗаголовкиHTTP.Вставить("Pragma", "no-cache");
			Если ЗначениеЗаполнено(ПараметрыСоединения.ЗначениеCookie) Тогда
				ЗаголовкиHTTP.Вставить("Cookie", ПараметрыСоединения.ЗначениеCookie);
			КонецЕсли;
			
			ПакетДляЗаданияПричиныАннулирования = "InqRecallStaticFormType_RecallReason="
				+ ЗакодироватьПараметрСсылки(РежимыОтправки.ТекстПричиныАннулирования, "utf-8");
			ИмяФайлаЗапроса = ПолучитьИмяВременногоФайла();
			ЗаписываемыйФайл = Новый ЗаписьТекста(ИмяФайлаЗапроса, "windows-1251");
			ЗаписываемыйФайл.Записать(ПакетДляЗаданияПричиныАннулирования);
			ЗаписываемыйФайл.Закрыть();
			
			ИмяФайлаОтвета = ПолучитьИмяВременногоФайла();
			
			Попытка
				РесурсНаСервере = ДокументооборотСКО.ПолучитьЗаменяемоеЗначениеТестовогоСервера1С("/FtsDocumentsMvc/PartEdit/Update/", 
																									"{FTS}/FtsDocumentsMvc/PartEdit/Update/", 
																									ПараметрыСоединения.СоединениеHTTP) + ИдентификаторВводаАннулирования;
				ОтветHTTP = ПараметрыСоединения.СоединениеHTTP.ОтправитьДляОбработки(
					ИмяФайлаЗапроса,
					РесурсНаСервере,
					ИмяФайлаОтвета,
					ЗаголовкиHTTP);
			Исключение
				
				ПараметрыСоединения.ПовторятьСоединение = Истина;
				Возврат Ложь;
				
			КонецПопытки;
			
			ЗначениеSetCookie = ОтветHTTP.Заголовки.Получить("Set-Cookie");
			ПараметрыСоединения.ЗначениеCookie = ОбновленноеЗначениеCookie(ПараметрыСоединения.ЗначениеCookie, ЗначениеSetCookie);
			
			ОперацииСФайламиЭДКО.УдалитьВременныйФайл(ИмяФайлаОтвета);
			
			// сохраняем форму аннулирования
			
			ЗаголовкиHTTP = Новый Соответствие();
			ЗаголовкиHTTP.Вставить("Proxy-Connection", "Keep-Alive");
			ЗаголовкиHTTP.Вставить("Pragma", "no-cache");
			Если ЗначениеЗаполнено(ПараметрыСоединения.ЗначениеCookie) Тогда
				ЗаголовкиHTTP.Вставить("Cookie", ПараметрыСоединения.ЗначениеCookie);
			КонецЕсли;
			
			РесурсНаСервере = ДокументооборотСКО.ПолучитьЗаменяемоеЗначениеТестовогоСервера1С("/FtsDocumentsMvc/PartEdit/Save/", 
																								"{FTS}/FtsDocumentsMvc/PartEdit/Save/", 
																								ПараметрыСоединения.СоединениеHTTP) + ИдентификаторВводаАннулирования;
			
			ЗапросHTTP = Новый HTTPЗапрос(
				РесурсНаСервере,
				ЗаголовкиHTTP);
			
			ИмяФайлаОтвета = ПолучитьИмяВременногоФайла();
			
			Попытка
				ОтветHTTP = ПараметрыСоединения.СоединениеHTTP.Получить(ЗапросHTTP, ИмяФайлаОтвета);
			Исключение
				
				ПараметрыСоединения.СоединениеHTTP = Неопределено;
				ПараметрыСоединения.ПовторятьСоединение = Истина;
				Возврат Неопределено;
				
			КонецПопытки;
			
			ЗначениеSetCookie = ОтветHTTP.Заголовки.Получить("Set-Cookie");
			ПараметрыСоединения.ЗначениеCookie = ОбновленноеЗначениеCookie(ПараметрыСоединения.ЗначениеCookie, ЗначениеSetCookie);
			
			ОперацииСФайламиЭДКО.УдалитьВременныйФайл(ИмяФайлаОтвета);
			
			// посылаем запрос подписываемых данных аннулирования
			
			ЗаголовкиHTTP = Новый Соответствие();
			ЗаголовкиHTTP.Вставить("Content-Type", "application/json;charset=utf-8");
			ЗаголовкиHTTP.Вставить("Proxy-Connection", "Keep-Alive");
			ЗаголовкиHTTP.Вставить("Pragma", "no-cache");
			Если ЗначениеЗаполнено(ПараметрыСоединения.ЗначениеCookie) Тогда
				ЗаголовкиHTTP.Вставить("Cookie", ПараметрыСоединения.ЗначениеCookie);
			КонецЕсли;
			
			ПакетДляПолученияПодписываемыхДанных = СтрШаблон(
				"{""Item"":{""SoftVersion"":""%1"""
				+ ",""ModelName"":""Подача статистической формы"""
				+ ",""Id"":""%2"""
				+ ",""Icon"":""EmailGo"""
				+ ",""Name"":""Action_Подготовка_к_аннулированию_Отправить_аннулирование"""
				+ ",""DisplayName"":""Отправить заявление об аннулировании"""
				+ ",""executing"":true"
				+ ",""Action"":"
					+ "{""Id"":""%3"""
					+ ",""Progress"":null,""Script"":null,""Function"":null,""DocumentForSign"":null"
					+ ",""DocumentForFill"":"
						+ "{""Name"":""Заявление об аннулировании статистической формы"""
						+ ",""Data"":""../FtsDocumentsMvc/PartEdit/Index/%4?HideMenu=True"""
						+ ",""Config"":"
							+ "{""DocumentName"":""Заявление об аннулировании статистической формы"""
							+ ",""CommandButtonsToEditor"":false,""EditorButtonsToCommand"":false,""EditorButtonsDisable"":false"
							+ ",""HeaderText"":null,""CancelText"":null,""OkText"":null"
							+ ",""ValidateBeforeOk"":false"
							+ ",""ValidateText"":null"
							+ ",""ValidateDisable"":false"
							+ ",""SaveFileText"":null,""LoadFileText"":null"
							+ ",""SaveFileDisable"":false,""LoadFileDisable"":false"
							+ ",""WindowClass"":""documentEditorWindow""}"
						+ ",""Params"":"
							+ "{""SelectDisable"":""true""}"
						+ ",""frameLoaded"":true}"
					+ ",""DocumentForFillSave"":null,""DocumentForView"":null,""DocumentForViewSign"":null,""DocumentForPrint"":null"
					+ ",""DocumentEditor"":null,""DocumentInEditor"":null,""SelectChoice"":null,""SelectNotedChoice"":null"
					+ ",""SelectMultipleChoice"":null,""ConfirmBox"":null,""MessageBox"":null,""InfoBox"":null,""InputBox"":null"
					+ ",""Window"":null,""Redirect"":null,""Download"":null,""Upload"":null,""Validation"":null"
					+ ",""Success"":true}}}",
				ПараметрыОтправки.ВерсияФормированияВыгрузки + "/" + ПараметрыОтправки.ВерсияОтправкиПортала,
				ПараметрыСоединения.ИдентификаторОтправкиНаСервере,
				ПараметрыСоединения.ИдентификаторВыполненияОтправки,
				ИдентификаторВводаАннулирования);
			ИмяФайлаЗапроса = ПолучитьИмяВременногоФайла();
			ЗаписываемыйФайл = Новый ЗаписьТекста(ИмяФайлаЗапроса, "utf-8");
			ЗаписываемыйФайл.Записать(ПакетДляПолученияПодписываемыхДанных);
			ЗаписываемыйФайл.Закрыть();
			
			ИмяФайлаОтвета = ПолучитьИмяВременногоФайла();
			
			Попытка
				РесурсНаСервере = ДокументооборотСКО.ПолучитьЗаменяемоеЗначениеТестовогоСервера1С("/FtsPersonalCabinetWeb2017/NgActions/ExecuteAction", 
																									"{FTS}/FtsPersonalCabinetWeb2017/NgActions/ExecuteAction", 
																									ПараметрыСоединения.СоединениеHTTP);
				ОтветHTTP = ПараметрыСоединения.СоединениеHTTP.ОтправитьДляОбработки(
					ИмяФайлаЗапроса,
					РесурсНаСервере,
					ИмяФайлаОтвета,
					ЗаголовкиHTTP);
			Исключение
				
				ПараметрыСоединения.ПовторятьСоединение = Истина;
				Возврат Ложь;
				
			КонецПопытки;
			
			ЗначениеSetCookie = ОтветHTTP.Заголовки.Получить("Set-Cookie");
			ПараметрыСоединения.ЗначениеCookie = ОбновленноеЗначениеCookie(ПараметрыСоединения.ЗначениеCookie, ЗначениеSetCookie);
			
			ФайлОтвета = Новый ЧтениеТекста(ИмяФайлаОтвета, "utf-8");
			ТекстОтвета = ФайлОтвета.Прочитать();
			ФайлОтвета.Закрыть();
			
			ОперацииСФайламиЭДКО.УдалитьВременныйФайл(ИмяФайлаОтвета);
			
			ПараметрыСоединения.ОписаниеОшибкиСервером = ТекстОшибкиПорталаФТС(ТекстОтвета);
			
			Если ЗначениеЗаполнено(ПараметрыСоединения.ОписаниеОшибкиСервером) Тогда
				Возврат Ложь;
			КонецЕсли;
			
			ПараметрыСоединения.ИдентификаторВыполненияОтправки = ТекстПослеПрефикса(ТекстОтвета, """Action"":{""Id"":""", """");
			
			Если НЕ ЗначениеЗаполнено(ПараметрыСоединения.ИдентификаторВыполненияОтправки) Тогда
				ПараметрыСоединения.ОписаниеОшибкиСервером = НСтр("ru = 'Ошибка отправки отчета на портале Федеральной таможенной службы.'");
				Возврат Ложь;
			КонецЕсли;
		КонецЕсли;
	КонецЕсли;
	
	ТекстОтвета = Строка(ТекстПослеПрефикса(ТекстОтвета, """Data"":"""));
	ПозицияКонцаПараметров = СтрНайти(ТекстОтвета, """},""");
	ПозицияРазделителяПараметров = СтрНайти(ТекстОтвета, """,""");
	Если ПозицияКонцаПараметров > 0 ИЛИ ПозицияРазделителяПараметров > 0 Тогда
		Если (ПозицияКонцаПараметров > 0 И ПозицияРазделителяПараметров > 0 И ПозицияКонцаПараметров < ПозицияРазделителяПараметров) ИЛИ ПозицияРазделителяПараметров = 0 Тогда
			ПозицияРазделителяПараметров = ПозицияКонцаПараметров;
		КонецЕсли;
		ТекстОтвета = Лев(ТекстОтвета, ПозицияРазделителяПараметров - 1);
	КонецЕсли;
	ТекстПодписываемойВыгрузки = СтрЗаменить(ТекстОтвета, "\""", """");
	ТекстПодписываемойВыгрузки = СтрЗаменить(ТекстПодписываемойВыгрузки, "\r\n", Символы.ПС);
	
	Если НЕ ЗначениеЗаполнено(ТекстПодписываемойВыгрузки) И НЕ ЗначениеЗаполнено(ИдентификаторВводаАннулирования) Тогда
		
		// получаем XML-содержимое документа
		
		ЗаголовкиHTTP = Новый Соответствие();
		ЗаголовкиHTTP.Вставить("Content-Type", "application/json;charset=UTF-8");
		ЗаголовкиHTTP.Вставить("Proxy-Connection", "Keep-Alive");
		ЗаголовкиHTTP.Вставить("Pragma", "no-cache");
		Если ЗначениеЗаполнено(ПараметрыСоединения.ЗначениеCookie) Тогда
			ЗаголовкиHTTP.Вставить("Cookie", ПараметрыСоединения.ЗначениеCookie);
		КонецЕсли;
		
		ПакетДляПолученияXMLСодержимого = "{}";
		ИмяФайлаЗапроса = ПолучитьИмяВременногоФайла();
		ЗаписываемыйФайл = Новый ЗаписьТекста(ИмяФайлаЗапроса, "utf-8");
		ЗаписываемыйФайл.Записать(ПакетДляПолученияXMLСодержимого);
		ЗаписываемыйФайл.Закрыть();
		
		ИмяФайлаОтвета = ПолучитьИмяВременногоФайла();
		
		Попытка
			РесурсНаСервере = ДокументооборотСКО.ПолучитьЗаменяемоеЗначениеТестовогоСервера1С("/FtsPersonalCabinetWeb2017/UniversalDatabase/GetItem", 
																								"{FTS}/FtsPersonalCabinetWeb2017/UniversalDatabase/GetItem", 
																								ПараметрыСоединения.СоединениеHTTP) 
				+ "?scopeName=FtsStat&listName=StatDocuments&id=" + ПараметрыСоединения.ИдентификаторОтправкиНаСервере;
				
			ОтветHTTP = ПараметрыСоединения.СоединениеHTTP.ОтправитьДляОбработки(
				ИмяФайлаЗапроса,
				РесурсНаСервере,
				ИмяФайлаОтвета,
				ЗаголовкиHTTP);
		Исключение
			
			ПараметрыСоединения.ПовторятьСоединение = Истина;
			Возврат Ложь;
			
		КонецПопытки;
		
		ЗначениеSetCookie = ОтветHTTP.Заголовки.Получить("Set-Cookie");
		ПараметрыСоединения.ЗначениеCookie = ОбновленноеЗначениеCookie(ПараметрыСоединения.ЗначениеCookie, ЗначениеSetCookie);
		
		ФайлОтвета = Новый ЧтениеТекста(ИмяФайлаОтвета, "utf-8");
		ТекстОтвета = ФайлОтвета.Прочитать();
		ФайлОтвета.Закрыть();
		
		ОперацииСФайламиЭДКО.УдалитьВременныйФайл(ИмяФайлаОтвета);
		
		ПараметрыСоединения.ОписаниеОшибкиСервером = ТекстОшибкиПорталаФТС(ТекстОтвета);
		
		Если ЗначениеЗаполнено(ПараметрыСоединения.ОписаниеОшибкиСервером) Тогда
			Возврат Ложь;
		КонецЕсли;
		
		СодержимоеXMLФормыJSONВнутриКорневогоУзла = ТекстПослеПрефикса(ТекстОтвета,
			"""Data"":""<StaticForm", "</StaticForm>""");
		Если НЕ ЗначениеЗаполнено(СодержимоеXMLФормыJSONВнутриКорневогоУзла) Тогда
			ПараметрыСоединения.ОписаниеОшибкиСервером = НСтр("ru = 'Ошибка отправки отчета на портале Федеральной таможенной службы.'");
			Возврат Ложь;
		КонецЕсли;
		
		// загружаем форму документа
		
		ИдентификаторРедактированияФормы = Строка(Новый УникальныйИдентификатор);
		
		ЗаголовкиHTTP = Новый Соответствие();
		ЗаголовкиHTTP.Вставить("Content-Type", "application/json;charset=UTF-8");
		ЗаголовкиHTTP.Вставить("Proxy-Connection", "Keep-Alive");
		ЗаголовкиHTTP.Вставить("Pragma", "no-cache");
		Если ЗначениеЗаполнено(ПараметрыСоединения.ЗначениеCookie) Тогда
			ЗаголовкиHTTP.Вставить("Cookie", ПараметрыСоединения.ЗначениеCookie);
		КонецЕсли;
		
		ПакетДляПолученияИдентификатораОтправки = СтрШаблон(
			"{""id"":""%1"",""xml"":""<StaticForm%2</StaticForm>"",""params"":null}",
			ИдентификаторРедактированияФормы,
			СодержимоеXMLФормыJSONВнутриКорневогоУзла);
		ИмяФайлаЗапроса = ПолучитьИмяВременногоФайла();
		ЗаписываемыйФайл = Новый ЗаписьТекста(ИмяФайлаЗапроса, "utf-8");
		ЗаписываемыйФайл.Записать(ПакетДляПолученияИдентификатораОтправки);
		ЗаписываемыйФайл.Закрыть();
		
		ИмяФайлаОтвета = ПолучитьИмяВременногоФайла();
		
		Попытка
			РесурсНаСервере = ДокументооборотСКО.ПолучитьЗаменяемоеЗначениеТестовогоСервера1С("/FtsPersonalCabinetWeb2017/NgEdit/PutDoc", 
																								"{FTS}/FtsPersonalCabinetWeb2017/NgEdit/PutDoc", 
																								ПараметрыСоединения.СоединениеHTTP);
			ОтветHTTP = ПараметрыСоединения.СоединениеHTTP.ОтправитьДляОбработки(
				ИмяФайлаЗапроса,
				РесурсНаСервере,
				ИмяФайлаОтвета,
				ЗаголовкиHTTP);
		Исключение
			
			ПараметрыСоединения.ПовторятьСоединение = Истина;
			Возврат Ложь;
			
		КонецПопытки;
		
		ЗначениеSetCookie = ОтветHTTP.Заголовки.Получить("Set-Cookie");
		ПараметрыСоединения.ЗначениеCookie = ОбновленноеЗначениеCookie(ПараметрыСоединения.ЗначениеCookie, ЗначениеSetCookie);
		
		ФайлОтвета = Новый ЧтениеТекста(ИмяФайлаОтвета, "utf-8");
		ТекстОтвета = ФайлОтвета.Прочитать();
		ФайлОтвета.Закрыть();
		
		ОперацииСФайламиЭДКО.УдалитьВременныйФайл(ИмяФайлаОтвета);
		
		ПараметрыСоединения.ОписаниеОшибкиСервером = ТекстОшибкиПорталаФТС(ТекстОтвета);
		
		Если ЗначениеЗаполнено(ПараметрыСоединения.ОписаниеОшибкиСервером) Тогда
			Возврат Ложь;
		КонецЕсли;
		
		// получаем сообщения об ошибках
		
		ЗаголовкиHTTP = Новый Соответствие();
		ЗаголовкиHTTP.Вставить("Proxy-Connection", "Keep-Alive");
		ЗаголовкиHTTP.Вставить("Pragma", "no-cache");
		Если ЗначениеЗаполнено(ПараметрыСоединения.ЗначениеCookie) Тогда
			ЗаголовкиHTTP.Вставить("Cookie", ПараметрыСоединения.ЗначениеCookie);
		КонецЕсли;
		
		РесурсНаСервере = ДокументооборотСКО.ПолучитьЗаменяемоеЗначениеТестовогоСервера1С("/FtsDocumentsMvc/PartEdit/Validate/", 
																							"{FTS}/FtsDocumentsMvc/PartEdit/Validate/", 
																							ПараметрыСоединения.СоединениеHTTP) + ИдентификаторРедактированияФормы;
		ЗапросHTTP = Новый HTTPЗапрос(РесурсНаСервере, ЗаголовкиHTTP);
		
		ИмяФайлаОтвета = ПолучитьИмяВременногоФайла();
		
		Попытка
			ОтветHTTP = ПараметрыСоединения.СоединениеHTTP.Получить(ЗапросHTTP, ИмяФайлаОтвета);
		Исключение
			
			ПараметрыСоединения.СоединениеHTTP = Неопределено;
			ПараметрыСоединения.ПовторятьСоединение = Истина;
			Возврат Неопределено;
			
		КонецПопытки;
		
		ФайлОтвета = Новый ЧтениеТекста(ИмяФайлаОтвета, "utf-8");
		ТекстОтвета = ФайлОтвета.Прочитать();
		ФайлОтвета.Закрыть();
		
		ЗначениеSetCookie = ОтветHTTP.Заголовки.Получить("Set-Cookie");
		ПараметрыСоединения.ЗначениеCookie = ОбновленноеЗначениеCookie(ПараметрыСоединения.ЗначениеCookie, ЗначениеSetCookie);
		
		ОперацииСФайламиЭДКО.УдалитьВременныйФайл(ИмяФайлаОтвета);
		
		ТекстыПредупреждений = "";
		ПозицияПослеПостфикса = Неопределено;
		Пока ЗначениеЗаполнено(ТекстОтвета) Цикл
			ТекстУзлаПредупреждения = ТекстПослеПрефикса(ТекстОтвета, "<validateWarning", "</validateWarning>", ПозицияПослеПостфикса);
			Если ТекстУзлаПредупреждения = Неопределено Тогда
				Прервать;
			КонецЕсли;
			
			ТекстПредупреждения = ТекстПослеПрефикса(ТекстУзлаПредупреждения, ">");
			ТекстПредупреждения = Строка(ТекстПредупреждения);
			ТекстПредупреждения = ТекстИзHTML(ТекстПредупреждения);
			
			Если ЗначениеЗаполнено(ТекстПредупреждения) Тогда
				ЭлементПредупреждения = ТекстПослеПрефикса(ТекстУзлаПредупреждения, " elname='", "'");
				ЭлементПредупреждения = Строка(ЭлементПредупреждения);
				ЭлементПредупреждения = ТекстИзHTML(ЭлементПредупреждения);
				Если ЗначениеЗаполнено(ЭлементПредупреждения) Тогда
					ТекстПредупреждения = ЭлементПредупреждения + ": " + ТекстПредупреждения;
				КонецЕсли;
				
				ПолеПредупреждения = ТекстПослеПрефикса(ТекстУзлаПредупреждения, " path='", "'");
				ПолеПредупреждения = Строка(ПолеПредупреждения);
				ПолеПредупреждения = ТекстИзHTML(ПолеПредупреждения);
				Если ЗначениеЗаполнено(ПолеПредупреждения) Тогда
					ТекстПредупреждения = ТекстПредупреждения + " (" + ПолеПредупреждения + ")";
				КонецЕсли;
				
				ТекстыПредупреждений = ТекстыПредупреждений + ?(ЗначениеЗаполнено(ТекстыПредупреждений), "
					|", "") + ТекстПредупреждения;
			КонецЕсли;
			
			ТекстОтвета = Сред(ТекстОтвета, ПозицияПослеПостфикса);
		КонецЦикла;
		
		ПараметрыСоединения.ОписаниеОшибкиСервером = СтрШаблон(
			НСтр("ru = 'При проверке есть предупреждения, в том числе возможна недостоверность заявленных сведений о товарах"
					+ " по средним ценам, весу (в конце в скобках указано поле ошибки с индексом строки товара):
					   |%1
					   |
					   |В большинстве случаев, замечания при поверке достоверности сведений о товарах означают ошибки или опечатки"
					+ " в сведениях о товаре (цене, весе), информацию об этих товарах стоит уточнить у поставщика.
					   |Отправка статистической формы с недостоверными сведениями может стать причиной постановки ее на контроль"
					+ " и последующего привлечения заявителя к административной ответственности (АП) в соответствии с КОАП РФ.
					   |
					   |Существует возможность отправить файл выгрузки отчета с предупреждениями на портале %2'"),
			ТекстыПредупреждений,
			"https://" + ПараметрыОтправки.АдресСервера + "/FtsPersonalCabinetWeb2017/#?service=StatEp&start=0&view=List")
			+ " Для отображения полей с ошибками проведите проверку в web-форме статистической формы по кнопке ""Проверить""";
		Возврат Ложь;
	КонецЕсли;
	
	// вместо текста подписываемой выгрузки портал ФТС теперь возвращает его готовый хеш в Base64
	ИмяВременногоФайлаПодписываемойВыгрузки = ПолучитьИмяВременногоФайла();
	ДанныеПодписываемойВыгрузки = Base64Значение(ТекстПодписываемойВыгрузки);
	ДанныеПодписываемойВыгрузки.Записать(ИмяВременногоФайлаПодписываемойВыгрузки);
	ЧтениеФайлаПодписываемойВыгрузки = Новый ЧтениеТекста(ИмяВременногоФайлаПодписываемойВыгрузки, "windows-1251");
	ТекстПодписываемойВыгрузки = ЧтениеФайлаПодписываемойВыгрузки.Прочитать();
	ЧтениеФайлаПодписываемойВыгрузки.Закрыть();
	ОперацииСФайламиЭДКО.УдалитьВременныйФайл(ИмяВременногоФайлаПодписываемойВыгрузки);
	
	Если Лев(ТекстПодписываемойВыгрузки, 16) = "ObjectHashBase64" Тогда
		ТекстПодписываемойВыгрузки = Сред(ТекстПодписываемойВыгрузки, 17);
		
	Иначе
		ИзвлеченныйТекст = ТекстПослеПрефикса(ТекстПодписываемойВыгрузки, """ObjectHashBase64"":""", """");
		Если ЗначениеЗаполнено(ИзвлеченныйТекст) Тогда
			ТекстПодписываемойВыгрузки = ИзвлеченныйТекст;
		КонецЕсли;
	КонецЕсли;
	ТекстПодписываемойВыгрузки = "<Object xmlns=""http://www.w3.org/2000/09/xmldsig#"" Id=""Object"">" + ТекстПодписываемойВыгрузки + "</Object>";
	
	ИмяВременногоФайлаПодписываемойВыгрузки = ПолучитьИмяВременногоФайла();
	ЗаписьФайлаПодписываемойВыгрузки = Новый ЗаписьТекста(ИмяВременногоФайлаПодписываемойВыгрузки, "windows-1251");
	ЗаписьФайлаПодписываемойВыгрузки.Записать(ТекстПодписываемойВыгрузки);
	ЗаписьФайлаПодписываемойВыгрузки.Закрыть();
	ДанныеПодписываемойВыгрузки = Новый ДвоичныеДанные(ИмяВременногоФайлаПодписываемойВыгрузки);
	ОперацииСФайламиЭДКО.УдалитьВременныйФайл(ИмяВременногоФайлаПодписываемойВыгрузки);
	ПараметрыСоединения.ПодписываемоеСодержимоеВыгрузки64 = Base64Строка(ДанныеПодписываемойВыгрузки);
	
	Если НЕ ЗначениеЗаполнено(ПараметрыСоединения.ПодписываемоеСодержимоеВыгрузки64) Тогда
		ПараметрыСоединения.ОписаниеОшибкиСервером = НСтр("ru = 'Ошибка отправки отчета на портале Федеральной таможенной службы.'");
		Возврат Ложь;
	КонецЕсли;
	
	Возврат НЕ ЗначениеЗаполнено(ПараметрыСоединения.ОписаниеОшибкиСервером);
	
КонецФункции

Функция ЗагрузитьПодписьНаПорталФТС(ИмяФайлаПодписи, ПараметрыОтправки, ПараметрыСоединения)
	
	Если ПараметрыСоединения.ВерсияПортала = "2017" Тогда
		Возврат ЗагрузитьПодписьНаПорталФТС2017(ИмяФайлаПодписи, ПараметрыОтправки, ПараметрыСоединения);
	КонецЕсли;
	
	// посылаем запрос отправки с подписью
	
	ЗаголовкиHTTP = Новый Соответствие();
	ЗаголовкиHTTP.Вставить("Content-Type", "application/x-www-form-urlencoded");
	ЗаголовкиHTTP.Вставить("Proxy-Connection", "Keep-Alive");
	ЗаголовкиHTTP.Вставить("Pragma", "no-cache");
	ЗаголовкиHTTP.Вставить("X-Ext.Net", "delta=true");
	Если ЗначениеЗаполнено(ПараметрыСоединения.ЗначениеCookie) Тогда
		ЗаголовкиHTTP.Вставить("Cookie", ПараметрыСоединения.ЗначениеCookie);
	КонецЕсли;
	
	ДвоичныеДанныеПодписи = Новый ДвоичныеДанные(ИмяФайлаПодписи);
	ТекстПодписиXMLDSig64 = Base64Строка(ДвоичныеДанныеПодписи);
	ТекстПодписиXMLDSig64 = СтрЗаменить(ТекстПодписиXMLDSig64, Символы.ВК, "");
	ТекстПодписиXMLDSig64 = СтрЗаменить(ТекстПодписиXMLDSig64, Символы.ПС, "");
	ПакетДляОтправкиПодписи = "submitDirectEventConfig=" + ЗакодироватьПараметрСсылки("{""config"":{""extraParams"":{""target"":""" + ПараметрыОтправки.ВерсияФормированияВыгрузки + "/" + ПараметрыОтправки.ВерсияОтправкиПортала
		+ "|" + ТекстВШестнадцатеричныйUnicode("Подача статистической формы") + "|" + ПараметрыСоединения.ИдентификаторОтправкиНаСервере + """,""action"":""Action_" + ТекстВШестнадцатеричныйUnicode("Отправить") + """,""prevAct"":""{\""Id\"":\"""
		+ ПараметрыСоединения.ИдентификаторВыполненияОтправки + "\"",\""DocumentForSign\"":{\""Name\"":\""" + СтрЗаменить(ТекстВШестнадцатеричныйUnicode("Статистическая форма учета перемещения товара"), "\", "\\") + "\"",\""Data\"":\"""
		+ ТекстПодписиXMLDSig64 + "\""},\""DocumentForFill\"":null,\""DocumentForView\"":null,\""DocumentForPrint\"":null,\""DocumentEditor\"":null,\""DocumentInEditor\"":null,"
		+ "\""SelectChoice\"":null,\""SelectNotedChoice\"":null,\""SelectMultipleChoice\"":null,\""MessageBox\"":null,\""InputBox\"":null,\""Window\"":null,\""Redirect\"":null,\""Download\"":null,\""Upload\"":null,\""Script\"":null,\""Progress\"":null,\""CancelResult\"":false,\""UseAllSelectors\"":false}""}}}")
		+ "&ctl55=&__EVENTTARGET=ctl02&__EVENTARGUMENT=" + ЗакодироватьПараметрСсылки("-|public|ExecuteAction")
		+ "&TextFieldNote=&DropDownCreateVersion=" + ЗакодироватьПараметрСсылки(ПараметрыОтправки.ВерсияФормированияВыгрузки + "/" + ПараметрыОтправки.ВерсияОтправкиПортала);
	ИмяФайлаЗапроса = ПолучитьИмяВременногоФайла();
	ЗаписываемыйФайл = Новый ЗаписьТекста(ИмяФайлаЗапроса, "windows-1251");
	ЗаписываемыйФайл.Записать(ПакетДляОтправкиПодписи);
	ЗаписываемыйФайл.Закрыть();
	
	ИмяФайлаОтвета = ПолучитьИмяВременногоФайла();
	
	Попытка
		РесурсНаСервере = ДокументооборотСКО.ПолучитьЗаменяемоеЗначениеТестовогоСервера1С("/FtsStatFormEpExtWeb/Stat/List.aspx", 
																							"{FTS}/FtsStatFormEpExtWeb/Stat/List.aspx", 
																							ПараметрыСоединения.СоединениеHTTP);
		ОтветHTTP = ПараметрыСоединения.СоединениеHTTP.ОтправитьДляОбработки(ИмяФайлаЗапроса, РесурсНаСервере, ИмяФайлаОтвета, ЗаголовкиHTTP);
	Исключение
		
		ПараметрыСоединения.ПовторятьСоединение = Истина;
		Возврат Ложь;
		
	КонецПопытки;
	
	ЗначениеSetCookie = ОтветHTTP.Заголовки.Получить("Set-Cookie");
	ПараметрыСоединения.ЗначениеCookie = ОбновленноеЗначениеCookie(ПараметрыСоединения.ЗначениеCookie, ЗначениеSetCookie);
	
	ФайлОтвета = Новый ЧтениеТекста(ИмяФайлаОтвета, "utf-8");
	ТекстОтвета = ФайлОтвета.Прочитать();
	ФайлОтвета.Закрыть();
	
	ОперацииСФайламиЭДКО.УдалитьВременныйФайл(ИмяФайлаОтвета);
	
	ПараметрыСоединения.ОписаниеОшибкиСервером = ТекстОшибкиПорталаФТС(ТекстОтвета);
	
	Если ЗначениеЗаполнено(ПараметрыСоединения.ОписаниеОшибкиСервером) Тогда
		Возврат Ложь;
	КонецЕсли;
	
	// посылаем подтверждение отправки при предупреждении о несовпадении реквизитов сертификата и имен в отчете
	
	Если СтрНайти(ТекстОтвета, """ConfirmBox"":{""Title"":""") <> 0 И ЗначениеЗаполнено(ТекстПослеПрефикса(ТекстОтвета, """ConfirmText"":""", """")) Тогда
		
		ПараметрыСоединения.ИдентификаторВыполненияОтправки = ТекстПослеПрефикса(ТекстОтвета, "result:{""Id"":""", """");
		
		ЗаголовкиHTTP = Новый Соответствие();
		ЗаголовкиHTTP.Вставить("Content-Type", "application/x-www-form-urlencoded");
		ЗаголовкиHTTP.Вставить("Proxy-Connection", "Keep-Alive");
		ЗаголовкиHTTP.Вставить("Pragma", "no-cache");
		ЗаголовкиHTTP.Вставить("X-Ext.Net", "delta=true");
		Если ЗначениеЗаполнено(ПараметрыСоединения.ЗначениеCookie) Тогда
			ЗаголовкиHTTP.Вставить("Cookie", ПараметрыСоединения.ЗначениеCookie);
		КонецЕсли;
		
		ПакетДляПодтвержденияОтправки = "submitDirectEventConfig=" + ЗакодироватьПараметрСсылки("{""config"":{""extraParams"":{""target"":""" + ПараметрыОтправки.ВерсияФормированияВыгрузки + "/" + ПараметрыОтправки.ВерсияОтправкиПортала
			+ "|" + ТекстВШестнадцатеричныйUnicode("Подача статистической формы") + "|" + ПараметрыСоединения.ИдентификаторОтправкиНаСервере + """,""action"":""Action_" + ТекстВШестнадцатеричныйUnicode("Отправить") + """,""prevAct"":""{\""Id\"":\"""
			+ ПараметрыСоединения.ИдентификаторВыполненияОтправки + "\"",\""Progress\"":null,\""Script\"":null,\""Function\"":null,\""DocumentForSign\"":null,\""DocumentForFill\"":null,\""DocumentForView\"":null,\""DocumentForPrint\"":null,"
			+ "\""DocumentEditor\"":null,\""DocumentInEditor\"":null,\""SelectChoice\"":null,\""SelectNotedChoice\"":null,\""SelectMultipleChoice\"":null,\""ConfirmBox\"":{\""Title\"":null,\""Message\"":null,\""ConfirmText\"":\"""
			+ СтрЗаменить(ТекстВШестнадцатеричныйUnicode("Все равно отправить"), "\", "\\") + "\"",\""CancelText\"":\""" + СтрЗаменить(ТекстВШестнадцатеричныйUnicode("Изменить и отправить позже"), "\", "\\") + "\"","
			+ "\""Result\"":false,\""ConfirmResult\"":true},\""MessageBox\"":null,\""InfoBox\"":null,\""InputBox\"":null,\""Window\"":null,\""Redirect\"":null,\""Download\"":null,\""Upload\"":null}""}}}")
			+ "&SearchTxt=&FilterTabs=&ctl55=&__EVENTTARGET=ctl02&__EVENTARGUMENT=" + ЗакодироватьПараметрСсылки("-|public|ExecuteAction")
			+ "&TextFieldNote=&DropDownCreateVersion=" + ЗакодироватьПараметрСсылки(ПараметрыОтправки.ВерсияФормированияВыгрузки + "/" + ПараметрыОтправки.ВерсияОтправкиПортала);
		ИмяФайлаЗапроса = ПолучитьИмяВременногоФайла();
		ЗаписываемыйФайл = Новый ЗаписьТекста(ИмяФайлаЗапроса, "windows-1251");
		ЗаписываемыйФайл.Записать(ПакетДляПодтвержденияОтправки);
		ЗаписываемыйФайл.Закрыть();
		
		ИмяФайлаОтвета = ПолучитьИмяВременногоФайла();
		
		Попытка
			РесурсНаСервере = ДокументооборотСКО.ПолучитьЗаменяемоеЗначениеТестовогоСервера1С("/FtsStatFormEpExtWeb/Stat/List.aspx", 
																							"{FTS}/FtsStatFormEpExtWeb/Stat/List.aspx", 
																							ПараметрыСоединения.СоединениеHTTP);
			ОтветHTTP = ПараметрыСоединения.СоединениеHTTP.ОтправитьДляОбработки(ИмяФайлаЗапроса, РесурсНаСервере, ИмяФайлаОтвета, ЗаголовкиHTTP);
		Исключение
			
			ПараметрыСоединения.ПовторятьСоединение = Истина;
			Возврат Ложь;
			
		КонецПопытки;
		
		ЗначениеSetCookie = ОтветHTTP.Заголовки.Получить("Set-Cookie");
		ПараметрыСоединения.ЗначениеCookie = ОбновленноеЗначениеCookie(ПараметрыСоединения.ЗначениеCookie, ЗначениеSetCookie);
		
		ФайлОтвета = Новый ЧтениеТекста(ИмяФайлаОтвета, "utf-8");
		ТекстОтвета = ФайлОтвета.Прочитать();
		ФайлОтвета.Закрыть();
		
		ОперацииСФайламиЭДКО.УдалитьВременныйФайл(ИмяФайлаОтвета);
		
		ПараметрыСоединения.ОписаниеОшибкиСервером = ТекстОшибкиПорталаФТС(ТекстОтвета);
		
		Если ЗначениеЗаполнено(ПараметрыСоединения.ОписаниеОшибкиСервером) Тогда
			Возврат Ложь;
		КонецЕсли;
		
	КонецЕсли;
	
	ЗаголовокОтчета = ПолучитьПротоколОбработкиОтчетаНаПорталеФТС(ПараметрыОтправки, ПараметрыСоединения, Истина);
	
	Если ЗаголовокОтчета = Неопределено ИЛИ ЗаголовокОтчета.Состояние = "Новая статформа" ИЛИ ЗаголовокОтчета.Состояние = "Черновик" Тогда
		ПараметрыСоединения.ОписаниеОшибкиСервером = НСтр("ru = 'Сбой при отправке в Федеральную таможенную службу РФ'");
		
		ИнформацияОСообщении = ТекстПослеПрефикса(ТекстОтвета, """InfoBox"":{""", "}");
		Если ЗначениеЗаполнено(ИнформацияОСообщении) Тогда
			ЗаголовокСообщения = ТекстПослеПрефикса("""" + ИнформацияОСообщении, """Title"":""", """");
			ЗаголовокСообщения = Строка(ЗаголовокСообщения);
			ЗаголовокСообщения = СтрЗаменить(ЗаголовокСообщения, "\""", """");
			ЗаголовокСообщения = СтрЗаменить(ЗаголовокСообщения, "<br>", " ");
			ЗаголовокСообщения = СтрЗаменить(ЗаголовокСообщения, "<br/>", " ");
			ЗаголовокСообщения = ТекстИзHTML(ЗаголовокСообщения);
			ЗаголовокСообщения = СокрЛП(ЗаголовокСообщения);
			ЗаголовокСообщения = СтрЗаменить(ЗаголовокСообщения, "Данные лица, ответственного за предоставление статистической формы не совпадают с данными ЭЦП",
				НСтр("ru = 'Данные лица, ответственного за предоставление статистической формы, не совпадают с данными сертификата ЭП'"));
			Если Прав(ЗаголовокСообщения, 1) <> "." И Прав(ЗаголовокСообщения, 1) <> "!" Тогда
				ЗаголовокСообщения = ЗаголовокСообщения + ".";
			КонецЕсли;
			
			ТекстСообщения = ТекстПослеПрефикса(ТекстОтвета, """Message"":""");
			ТекстСообщения = Строка(ТекстСообщения);
			КонецСообщения = СтрНайти(ТекстСообщения, """,""");
			Если КонецСообщения = 0 И Прав(ТекстСообщения, 1) = """" Тогда
				КонецСообщения = СтрДлина(КонецСообщения);
			КонецЕсли;
			Если КонецСообщения <> 0 Тогда
				ТекстСообщения = Лев(ТекстСообщения, КонецСообщения - 1);
			КонецЕсли;
			ТекстСообщения = СтрЗаменить(ТекстСообщения, "\""", """");
			ТекстСообщения = СтрЗаменить(ТекстСообщения, "Необходимо использовать электронную подпись из профиля Личного кабинета. Перейдите в раздел ""Профиль"" и нажмите ""Заменить ЭП"".",
				НСтр("ru = 'Необходимо использовать сертификат ЭП из профиля Личного кабинета на портале ФТС, будет автоматически отправлен запрос на замену сертификата ЭП в профиле на портале.'"));
			
			Если ЗначениеЗаполнено(ЗаголовокСообщения) И ЗначениеЗаполнено(ТекстСообщения) Тогда
				ПараметрыСоединения.ОписаниеОшибкиСервером = "";
				
				ПрефиксСтроки = "<td style=""border: solid 1px; font-weight: bold;"">";
				РазделительЗначений = "</td>\r\n    <td style=""border: solid 1px;"">";
				ЗавершениеСтроки = "</td>\r\n  </tr>\r\n";
				ТекстСообщенияВнутриТаблицы = ТекстПослеПрефикса(ТекстСообщения, ПрефиксСтроки, "</table>");
				Если НЕ ЗначениеЗаполнено(ТекстСообщенияВнутриТаблицы) Тогда
					ПараметрыСоединения.ОписаниеОшибкиСервером = ЗаголовокСообщения + ?(ЗаголовокСообщения <> "" И ТекстСообщения <> "", " ", "") + ТекстСообщения;
					
				Иначе
					ТекстСообщения = ПрефиксСтроки + Строка(ТекстСообщенияВнутриТаблицы);
					Пока Истина Цикл
						ПозицияПослеПостфикса = Неопределено;
						ИмяРасхождения = ТекстПослеПрефикса(ТекстСообщения, ПрефиксСтроки, РазделительЗначений, ПозицияПослеПостфикса);
						ИмяРасхождения = Строка(ИмяРасхождения);
						ИмяРасхождения = ТекстИзHTML(ИмяРасхождения);
						ТекстСообщения = Сред(ТекстСообщения, ПозицияПослеПостфикса);
						Если НЕ ЗначениеЗаполнено(ТекстСообщения) Тогда
							Прервать;
						КонецЕсли;
						ТекстСообщения = РазделительЗначений + ТекстСообщения;
						
						ЗначениеВОтчете = ТекстПослеПрефикса(ТекстСообщения, РазделительЗначений, РазделительЗначений, ПозицияПослеПостфикса);
						ЗначениеВОтчете = Строка(ЗначениеВОтчете);
						ЗначениеВОтчете = СтрЗаменить(ЗначениеВОтчете, "<br>", " ");
						ЗначениеВОтчете = СтрЗаменить(ЗначениеВОтчете, "<br/>", " ");
						ЗначениеВОтчете = ТекстИзHTML(ЗначениеВОтчете);
						ТекстСообщения = РазделительЗначений + Сред(ТекстСообщения, ПозицияПослеПостфикса);
						
						ЗначениеВСертификате = ТекстПослеПрефикса(ТекстСообщения, РазделительЗначений, ЗавершениеСтроки, ПозицияПослеПостфикса);
						ЗначениеВСертификате = Строка(ЗначениеВСертификате);
						ЗначениеВСертификате = СтрЗаменить(ЗначениеВСертификате, "<br>", " ");
						ЗначениеВСертификате = СтрЗаменить(ЗначениеВСертификате, "<br/>", " ");
						ЗначениеВСертификате = ТекстИзHTML(ЗначениеВСертификате);
						ТекстСообщения = РазделительЗначений + Сред(ТекстСообщения, ПозицияПослеПостфикса);
						
						Если СтрНайти(ЗначениеВОтчете, "style=""color: red;""") = 0 И СтрНайти(ЗначениеВСертификате, "style=""color: red;""") = 0 Тогда
							ПараметрыСоединения.ОписаниеОшибкиСервером = ПараметрыСоединения.ОписаниеОшибкиСервером
								+ " " + СтрШаблон("%1: ""%2"", в сертификате: ""%3"".", ИмяРасхождения, ЗначениеВОтчете, ЗначениеВСертификате);
						КонецЕсли;
					КонецЦикла;
					
					ПараметрыСоединения.ОписаниеОшибкиСервером = ЗаголовокСообщения + ПараметрыСоединения.ОписаниеОшибкиСервером
						+ ?(ПараметрыСоединения.ОписаниеОшибкиСервером <> "", " " + НСтр("ru = 'Исправьте ошибки и повторите отправку.'"), "");
				КонецЕсли;
			КонецЕсли;
		КонецЕсли;
	КонецЕсли;
	
	Если СтрНайти(ПараметрыСоединения.ОписаниеОшибкиСервером, "подтвердить данные об организации") > 0
		И СтрНайти(ПараметрыСоединения.ОписаниеОшибкиСервером, "Add_organization.html") > 0 Тогда
		ПараметрыСоединения.ОписаниеОшибкиСервером = НСтр("ru = 'Для отправки требуется подтвердить данные об ЭП организации, отправка запроса на подтверждение выполняется автоматически, повторите попытку отправки отчета позже.'");
	КонецЕсли;
	
	Возврат НЕ ЗначениеЗаполнено(ПараметрыСоединения.ОписаниеОшибкиСервером);
	
КонецФункции

Функция ЗагрузитьПодписьНаПорталФТС2017(ИмяФайлаПодписи, ПараметрыОтправки, ПараметрыСоединения)
	
	// посылаем запрос отправки с подписью
	
	ЗаголовкиHTTP = Новый Соответствие();
	ЗаголовкиHTTP.Вставить("Content-Type", "application/json;charset=UTF-8");
	ЗаголовкиHTTP.Вставить("Proxy-Connection", "Keep-Alive");
	ЗаголовкиHTTP.Вставить("Pragma", "no-cache");
	Если ЗначениеЗаполнено(ПараметрыСоединения.ЗначениеCookie) Тогда
		ЗаголовкиHTTP.Вставить("Cookie", ПараметрыСоединения.ЗначениеCookie);
	КонецЕсли;
	
	ДвоичныеДанныеПодписи = Новый ДвоичныеДанные(ИмяФайлаПодписи);
	ТекстПодписиXMLDSig64 = Base64Строка(ДвоичныеДанныеПодписи);
	ТекстПодписиXMLDSig64 = СтрЗаменить(ТекстПодписиXMLDSig64, Символы.ВК, "");
	ТекстПодписиXMLDSig64 = СтрЗаменить(ТекстПодписиXMLDSig64, Символы.ПС, "");
	
	ПараметрыДляПодписи = СтрШаблон(
		"{""CertificateThumbprint"":""%1"",""SignedDataBase64"":""%2""}",
		ВРег(ПараметрыСоединения.СертификатАбонента.Отпечаток),
		ТекстПодписиXMLDSig64);
	ДвоичныеДанныеПараметровДляПодписи = ПолучитьДвоичныеДанныеИзСтроки(ПараметрыДляПодписи, "windows-1251");
	ПараметрыДляПодписи64 = Base64Строка(ДвоичныеДанныеПараметровДляПодписи);
	ПараметрыДляПодписи64 = СтрЗаменить(ПараметрыДляПодписи64, Символы.ВК, "");
	ПараметрыДляПодписи64 = СтрЗаменить(ПараметрыДляПодписи64, Символы.ПС, "");
	
	ПакетДляОтправкиПодписи = СтрШаблон(
		"{""Item"":{""SoftVersion"":""%1"""
		+ ",""ModelName"":""Подача статистической формы"""
		+ ",""Id"":""%2"""
		+ ",""Icon"":""EmailGo"""
		+ ",""Name"":""Action_Отправить"""
		+ ",""DisplayName"":""Отправить"""
		+ ",""executing"":true"
		+ ",""Action"":"
			+ "{""Id"":""%3"""
			+ ",""Progress"":null,""Script"":null,""Function"":null"
			+ ",""DocumentForSign"":"
				+ "{""Data"":""%4"""
				+ ",""Name"":""Статистическая форма учета перемещения товара (Документ: 0,01 Мб )"""
				+ ",""Text"":null}"
			+ ",""DocumentForFill"":null,""DocumentForFillSave"":null,""DocumentForView"":null,""DocumentForViewSign"":null"
			+ ",""DocumentForPrint"":null,""DocumentEditor"":null,""DocumentInEditor"":null,""SelectChoice"":null"
			+ ",""SelectNotedChoice"":null,""SelectMultipleChoice"":null,""ConfirmBox"":null,""MessageBox"":null"
			+ ",""InfoBox"":null,""InputBox"":null,""Window"":null,""Redirect"":null,""Download"":null,""Upload"":null"
			+ ",""Validation"":null,""Result"":null,""Success"":true,""Sending"":true}}}",
		ПараметрыОтправки.ВерсияФормированияВыгрузки + "/" + ПараметрыОтправки.ВерсияОтправкиПортала,
		ПараметрыСоединения.ИдентификаторОтправкиНаСервере,
		ПараметрыСоединения.ИдентификаторВыполненияОтправки,
		ПараметрыДляПодписи64);
	ИмяФайлаЗапроса = ПолучитьИмяВременногоФайла();
	ЗаписываемыйФайл = Новый ЗаписьТекста(ИмяФайлаЗапроса, "utf-8");
	ЗаписываемыйФайл.Записать(ПакетДляОтправкиПодписи);
	ЗаписываемыйФайл.Закрыть();
	
	ИмяФайлаОтвета = ПолучитьИмяВременногоФайла();
	
	Попытка
		РесурсНаСервере = ДокументооборотСКО.ПолучитьЗаменяемоеЗначениеТестовогоСервера1С("/FtsPersonalCabinetWeb2017/NgActions/ExecuteAction", 
																							"{FTS}/FtsPersonalCabinetWeb2017/NgActions/ExecuteAction", 
																							ПараметрыСоединения.СоединениеHTTP);
		ОтветHTTP = ПараметрыСоединения.СоединениеHTTP.ОтправитьДляОбработки(
			ИмяФайлаЗапроса,
			РесурсНаСервере,
			ИмяФайлаОтвета,
			ЗаголовкиHTTP);
	Исключение
		
		ОбъектФайлОтвета = Новый Файл(ИмяФайлаОтвета);
		Если НЕ ОбъектФайлОтвета.Существует() Тогда
			ПараметрыСоединения.ПовторятьСоединение = Истина;
			Возврат Ложь;
		КонецЕсли;
		
	КонецПопытки;
	
	Если ОтветHTTP <> Неопределено Тогда
		ЗначениеSetCookie = ОтветHTTP.Заголовки.Получить("Set-Cookie");
		ПараметрыСоединения.ЗначениеCookie = ОбновленноеЗначениеCookie(ПараметрыСоединения.ЗначениеCookie, ЗначениеSetCookie);
	КонецЕсли;
	
	ФайлОтвета = Новый ЧтениеТекста(ИмяФайлаОтвета, "utf-8");
	ТекстОтвета = ФайлОтвета.Прочитать();
	ФайлОтвета.Закрыть();
	
	ОперацииСФайламиЭДКО.УдалитьВременныйФайл(ИмяФайлаОтвета);
	
	ПараметрыСоединения.ОписаниеОшибкиСервером = ТекстОшибкиПорталаФТС(ТекстОтвета);
	
	Если ЗначениеЗаполнено(ПараметрыСоединения.ОписаниеОшибкиСервером) Тогда
		Возврат Ложь;
	КонецЕсли;
	
	// посылаем подтверждение отправки при предупреждении о несовпадении реквизитов сертификата и имен в отчете
	
	Если СтрНайти(ТекстОтвета, """ConfirmBox"":{""Title"":""") <> 0 И ЗначениеЗаполнено(ТекстПослеПрефикса(ТекстОтвета, """ConfirmText"":""", """")) Тогда
		
		ПараметрыСоединения.ИдентификаторВыполненияОтправки = ТекстПослеПрефикса(ТекстОтвета, """Action"":{""Id"":""", """");
		
		ЗаголовкиHTTP = Новый Соответствие();
		ЗаголовкиHTTP.Вставить("Content-Type", "application/json;charset=UTF-8");
		ЗаголовкиHTTP.Вставить("Proxy-Connection", "Keep-Alive");
		ЗаголовкиHTTP.Вставить("Pragma", "no-cache");
		Если ЗначениеЗаполнено(ПараметрыСоединения.ЗначениеCookie) Тогда
			ЗаголовкиHTTP.Вставить("Cookie", ПараметрыСоединения.ЗначениеCookie);
		КонецЕсли;
		
		ПакетДляПодтвержденияОтправки = СтрШаблон(
			"{""Item"":{""SoftVersion"":""%1"""
			+ ",""ModelName"":""Подача статистической формы"""
			+ ",""Id"":""%2"""
			+ ",""Icon"":""EmailGo"""
			+ ",""Name"":""Action_Отправить"""
			+ ",""DisplayName"":""Отправить"""
			+ ",""executing"":true"
			+ ",""Action"":"
				+ "{""Id"":""%3"""
				+ ",""Progress"":null,""Script"":null,""Function"":null,""DocumentForSign"":null,""DocumentForFill"":null"
				+ ",""DocumentForFillSave"":null,""DocumentForView"":null,""DocumentForViewSign"":null,""DocumentForPrint"":null"
				+ ",""DocumentEditor"":null,""DocumentInEditor"":null,""SelectChoice"":null,""SelectNotedChoice"":null"
				+ ",""SelectMultipleChoice"":null"
				+ ",""ConfirmBox"":"
					+ "{""Title"":null,""Message"":null"
					+ ",""ConfirmText"":""Всё равно отправить"""
					+ ",""CancelText"":""Изменить и отправить позже"""
					+ ",""Result"":false"
					+ ",""ConfirmResult"":true}"
				+ ",""MessageBox"":null,""InfoBox"":null,""InputBox"":null,""Window"":null,""Redirect"":null,""Download"":null"
				+ ",""Upload"":null,""Validation"":null"
				+ ",""Success"":true}}}",
			ПараметрыОтправки.ВерсияФормированияВыгрузки + "/" + ПараметрыОтправки.ВерсияОтправкиПортала,
			ПараметрыСоединения.ИдентификаторОтправкиНаСервере,
			ПараметрыСоединения.ИдентификаторВыполненияОтправки);
		ИмяФайлаЗапроса = ПолучитьИмяВременногоФайла();
		ЗаписываемыйФайл = Новый ЗаписьТекста(ИмяФайлаЗапроса, "utf-8");
		ЗаписываемыйФайл.Записать(ПакетДляПодтвержденияОтправки);
		ЗаписываемыйФайл.Закрыть();
		
		ИмяФайлаОтвета = ПолучитьИмяВременногоФайла();
		
		Попытка
			РесурсНаСервере = ДокументооборотСКО.ПолучитьЗаменяемоеЗначениеТестовогоСервера1С("/FtsPersonalCabinetWeb2017/NgActions/ExecuteAction", 
																							"{FTS}/FtsPersonalCabinetWeb2017/NgActions/ExecuteAction", 
																							ПараметрыСоединения.СоединениеHTTP);
			ОтветHTTP = ПараметрыСоединения.СоединениеHTTP.ОтправитьДляОбработки(
				ИмяФайлаЗапроса,
				РесурсНаСервере,
				ИмяФайлаОтвета,
				ЗаголовкиHTTP);
		Исключение
			
			ОбъектФайлОтвета = Новый Файл(ИмяФайлаОтвета);
			Если НЕ ОбъектФайлОтвета.Существует() Тогда
				ПараметрыСоединения.ПовторятьСоединение = Истина;
				Возврат Ложь;
			КонецЕсли;
			
		КонецПопытки;
		
		Если ОтветHTTP <> Неопределено Тогда
			ЗначениеSetCookie = ОтветHTTP.Заголовки.Получить("Set-Cookie");
			ПараметрыСоединения.ЗначениеCookie = ОбновленноеЗначениеCookie(ПараметрыСоединения.ЗначениеCookie, ЗначениеSetCookie);
		КонецЕсли;
		
		ФайлОтвета = Новый ЧтениеТекста(ИмяФайлаОтвета, "utf-8");
		ТекстОтвета = ФайлОтвета.Прочитать();
		ФайлОтвета.Закрыть();
		
		ОперацииСФайламиЭДКО.УдалитьВременныйФайл(ИмяФайлаОтвета);
		
		ПараметрыСоединения.ОписаниеОшибкиСервером = ТекстОшибкиПорталаФТС(ТекстОтвета, Истина);
		
		Если ЗначениеЗаполнено(ПараметрыСоединения.ОписаниеОшибкиСервером) Тогда
			Возврат Ложь;
		КонецЕсли;
		
	КонецЕсли;
	
	ЗаголовокОтчета = ПолучитьПротоколОбработкиОтчетаНаПорталеФТС(ПараметрыОтправки, ПараметрыСоединения, Истина);
	
	Если ЗаголовокОтчета = Неопределено ИЛИ ЗаголовокОтчета.Состояние = "Новая статформа"
		ИЛИ ЗаголовокОтчета.Состояние = "Черновик" ИЛИ ЗаголовокОтчета.Состояние = "Подготовка к аннулированию" Тогда
		
		ПараметрыСоединения.ОписаниеОшибкиСервером = НСтр("ru = 'Сбой при отправке в Федеральную таможенную службу РФ'");
		
		ИнформацияОСообщении = ТекстПослеПрефикса(ТекстОтвета, """InfoBox"":{""", "}");
		Если ЗначениеЗаполнено(ИнформацияОСообщении) Тогда
			ЗаголовокСообщения = ТекстПослеПрефикса("""" + ИнформацияОСообщении, """Title"":""", """");
			ЗаголовокСообщения = Строка(ЗаголовокСообщения);
			ЗаголовокСообщения = СтрЗаменить(ЗаголовокСообщения, "\""", """");
			ЗаголовокСообщения = СтрЗаменить(ЗаголовокСообщения, "<br>", " ");
			ЗаголовокСообщения = СтрЗаменить(ЗаголовокСообщения, "<br/>", " ");
			ЗаголовокСообщения = ТекстИзHTML(ЗаголовокСообщения);
			ЗаголовокСообщения = СокрЛП(ЗаголовокСообщения);
			ЗаголовокСообщения = СтрЗаменить(ЗаголовокСообщения, "Данные лица, ответственного за предоставление статистической формы не совпадают с данными ЭЦП",
				НСтр("ru = 'Данные лица, ответственного за предоставление статистической формы, не совпадают с данными сертификата ЭП'"));
			Если Прав(ЗаголовокСообщения, 1) <> "." И Прав(ЗаголовокСообщения, 1) <> "!" Тогда
				ЗаголовокСообщения = ЗаголовокСообщения + ".";
			КонецЕсли;
			
			ТекстСообщения = ТекстПослеПрефикса(ТекстОтвета, """Message"":""");
			ТекстСообщения = Строка(ТекстСообщения);
			КонецСообщения = СтрНайти(ТекстСообщения, """,""");
			Если КонецСообщения = 0 И Прав(ТекстСообщения, 1) = """" Тогда
				КонецСообщения = СтрДлина(КонецСообщения);
			КонецЕсли;
			Если КонецСообщения <> 0 Тогда
				ТекстСообщения = Лев(ТекстСообщения, КонецСообщения - 1);
			КонецЕсли;
			ТекстСообщения = СтрЗаменить(ТекстСообщения, "\""", """");
			ТекстСообщения = СтрЗаменить(ТекстСообщения, "Необходимо использовать электронную подпись из профиля Личного кабинета. Перейдите в раздел ""Профиль"" и нажмите ""Заменить ЭП"".",
				НСтр("ru = 'Необходимо использовать сертификат ЭП из профиля Личного кабинета на портале ФТС, будет автоматически отправлен запрос на замену сертификата ЭП в профиле на портале.'"));
			
			Если ЗначениеЗаполнено(ЗаголовокСообщения) И ЗначениеЗаполнено(ТекстСообщения) Тогда
				ПараметрыСоединения.ОписаниеОшибкиСервером = "";
				
				ПрефиксСтроки = "<td style=""border: solid 1px; font-weight: bold;"">";
				РазделительЗначений = "</td>\r\n    <td style=""border: solid 1px;"">";
				ЗавершениеСтроки = "</td>\r\n  </tr>\r\n";
				ТекстСообщенияВнутриТаблицы = ТекстПослеПрефикса(ТекстСообщения, ПрефиксСтроки, "</table>");
				Если НЕ ЗначениеЗаполнено(ТекстСообщенияВнутриТаблицы) Тогда
					ПараметрыСоединения.ОписаниеОшибкиСервером = ЗаголовокСообщения + ?(ЗаголовокСообщения <> "" И ТекстСообщения <> "", " ", "") + ТекстСообщения;
					
				Иначе
					ТекстСообщения = ПрефиксСтроки + Строка(ТекстСообщенияВнутриТаблицы);
					Пока Истина Цикл
						ПозицияПослеПостфикса = Неопределено;
						ИмяРасхождения = ТекстПослеПрефикса(ТекстСообщения, ПрефиксСтроки, РазделительЗначений, ПозицияПослеПостфикса);
						ИмяРасхождения = Строка(ИмяРасхождения);
						ИмяРасхождения = ТекстИзHTML(ИмяРасхождения);
						ТекстСообщения = Сред(ТекстСообщения, ПозицияПослеПостфикса);
						Если НЕ ЗначениеЗаполнено(ТекстСообщения) Тогда
							Прервать;
						КонецЕсли;
						ТекстСообщения = РазделительЗначений + ТекстСообщения;
						
						ЗначениеВОтчете = ТекстПослеПрефикса(ТекстСообщения, РазделительЗначений, РазделительЗначений, ПозицияПослеПостфикса);
						ЗначениеВОтчете = Строка(ЗначениеВОтчете);
						ЗначениеВОтчете = СтрЗаменить(ЗначениеВОтчете, "<br>", " ");
						ЗначениеВОтчете = СтрЗаменить(ЗначениеВОтчете, "<br/>", " ");
						ЗначениеВОтчете = ТекстИзHTML(ЗначениеВОтчете);
						ТекстСообщения = РазделительЗначений + Сред(ТекстСообщения, ПозицияПослеПостфикса);
						
						ЗначениеВСертификате = ТекстПослеПрефикса(ТекстСообщения, РазделительЗначений, ЗавершениеСтроки, ПозицияПослеПостфикса);
						ЗначениеВСертификате = Строка(ЗначениеВСертификате);
						ЗначениеВСертификате = СтрЗаменить(ЗначениеВСертификате, "<br>", " ");
						ЗначениеВСертификате = СтрЗаменить(ЗначениеВСертификате, "<br/>", " ");
						ЗначениеВСертификате = ТекстИзHTML(ЗначениеВСертификате);
						ТекстСообщения = РазделительЗначений + Сред(ТекстСообщения, ПозицияПослеПостфикса);
						
						Если СтрНайти(ЗначениеВОтчете, "style=""color: red;""") = 0 И СтрНайти(ЗначениеВСертификате, "style=""color: red;""") = 0 Тогда
							ПараметрыСоединения.ОписаниеОшибкиСервером = ПараметрыСоединения.ОписаниеОшибкиСервером
								+ " " + СтрШаблон("%1: ""%2"", в сертификате: ""%3"".", ИмяРасхождения, ЗначениеВОтчете, ЗначениеВСертификате);
						КонецЕсли;
					КонецЦикла;
					
					ПараметрыСоединения.ОписаниеОшибкиСервером = ЗаголовокСообщения + ПараметрыСоединения.ОписаниеОшибкиСервером
						+ ?(ПараметрыСоединения.ОписаниеОшибкиСервером <> "", " " + НСтр("ru = 'Исправьте ошибки и повторите отправку.'"), "");
				КонецЕсли;
			КонецЕсли;
		КонецЕсли;
	КонецЕсли;
	
	Если СтрНайти(ПараметрыСоединения.ОписаниеОшибкиСервером, "подтвердить данные об организации") > 0
		И СтрНайти(ПараметрыСоединения.ОписаниеОшибкиСервером, "Add_organization.html") > 0 Тогда
		ПараметрыСоединения.ОписаниеОшибкиСервером = НСтр("ru = 'Для отправки требуется подтвердить данные об ЭП организации, отправка запроса на подтверждение выполняется автоматически, повторите попытку отправки отчета позже.'");
	КонецЕсли;
	
	Возврат НЕ ЗначениеЗаполнено(ПараметрыСоединения.ОписаниеОшибкиСервером);
	
КонецФункции

Функция ТекстОшибкиПорталаФТС(ТекстОтвета, ИзвлекатьИсключения = Ложь, ОшибкаЕслиНеJSON = Ложь)
	
	ТекстОшибки = "";
	
	Если ТекстОтвета = "Действие не выполняется" ИЛИ Лев(ТекстОтвета, 15) = "Указанный СНИЛС"
		ИЛИ ОшибкаЕслиНеJSON И ЗначениеЗаполнено(ТекстОтвета) И Лев(ТекстОтвета, 2) <> "{""" И ТекстОтвета <> "{}" Тогда
		Возврат ТекстОтвета;
	КонецЕсли;
	
	Если ИзвлекатьИсключения Тогда
		ТекстОшибки = ТекстПослеПрефикса(ТекстОтвета, "[Exception: ", "]");
		Если НЕ ЗначениеЗаполнено(ТекстОшибки) Тогда
			ТекстОшибки = ТекстПослеПрефикса(ТекстОтвета, "System.Exception:", "---");
			ТекстОшибки = СокрЛП(ТекстОшибки);
			ПозицияТехническойИнформации = СтрНайти(ТекстОшибки, " at ");
			Если ПозицияТехническойИнформации > 0 Тогда
				ТекстОшибки = Лев(ТекстОшибки, ПозицияТехническойИнформации - 1);
				ТекстОшибки = СокрП(ТекстОшибки);
			КонецЕсли;
			
			Если НЕ ЗначениеЗаполнено(ТекстОшибки) И Лев(ТекстОтвета, 26) = "Ошибка выполнения действия" Тогда
				ТекстОшибки = ТекстПослеПрефикса(ТекстОтвета, ":");
				ТекстОшибки = СокрЛП(ТекстОшибки);
			КонецЕсли;
		КонецЕсли;
		ТекстОшибки = ТекстИзHTML(ТекстОшибки);
		ТекстОшибки = СтрЗаменить(ТекстОшибки, "<br>", Символы.ВК + Символы.ПС);
		ТекстОшибки = СтрЗаменить(ТекстОшибки, "<br/>", Символы.ВК + Символы.ПС);
		ТекстОшибки = СтрЗаменить(ТекстОшибки, "<br />", Символы.ВК + Символы.ПС);
		ТекстОшибки = СтрЗаменить(ТекстОшибки, "\r\n", Символы.ВК + Символы.ПС);
		ТекстОшибки = СтрЗаменить(ТекстОшибки, "   ", " ");
		ТекстОшибки = СтрЗаменить(ТекстОшибки, "  ", " ");
		ТекстОшибки = СокрЛП(ТекстОшибки);
		Если ТекстОшибки <> "" Тогда
			Возврат ТекстОшибки;
		КонецЕсли;
	КонецЕсли;
	
	ЭтоОшибка = (СтрНайти(ТекстОтвета, "success:false") > 0);
	
	Если ЭтоОшибка ИЛИ (СтрНайти(ТекстОтвета, "success:true") = 0 И СтрНайти(ТекстОтвета, """error"":""") > 0) Тогда
		
		ТекстОшибки = НСтр("ru = 'Ошибочный ответ сервера'");
		
		Позиция = 1;
		Разделитель = "";
		ОжидаемыйРазделитель = "{";
		Пока Позиция <= СтрДлина(ТекстОтвета) Цикл
			
			Пока Позиция <= СтрДлина(ТекстОтвета) Цикл
				ИмяПараметра = СледующееЗначениеJSON(ТекстОтвета, Позиция, Разделитель);
				Если Разделитель = ОжидаемыйРазделитель Тогда
					Прервать;
				КонецЕсли;
			КонецЦикла;
			
			ЗначениеПараметра = СледующееЗначениеJSON(ТекстОтвета, Позиция, Разделитель);
			Если Разделитель <> ":" Тогда
				Прервать;
			КонецЕсли;
			
			Если ИмяПараметра = "error" ИЛИ ИмяПараметра = "errorMessage" ИЛИ (ИмяПараметра = "message" И ЭтоОшибка) Тогда
				
				ЗначениеПараметра = ТекстИзHTML(ЗначениеПараметра);
				ЗначениеПараметра = СтрЗаменить(ЗначениеПараметра, "<br>", Символы.ВК + Символы.ПС);
				ЗначениеПараметра = СтрЗаменить(ЗначениеПараметра, "<br/>", Символы.ВК + Символы.ПС);
				ЗначениеПараметра = СтрЗаменить(ЗначениеПараметра, "<br />", Символы.ВК + Символы.ПС);
				ЗначениеПараметра = СтрЗаменить(ЗначениеПараметра, "\r\n", Символы.ВК + Символы.ПС);
				ЗначениеПараметра = СтрЗаменить(ЗначениеПараметра, "   ", " ");
				ЗначениеПараметра = СтрЗаменить(ЗначениеПараметра, "  ", " ");
				
				Если ЗначениеЗаполнено(ЗначениеПараметра) Тогда
					ТекстОшибки = СокрЛП(ЗначениеПараметра);
					Прервать;
				КонецЕсли;
				
			КонецЕсли;
			
			ОжидаемыйРазделитель = ",";
			
		КонецЦикла;
		
	КонецЕсли;
	
	Возврат ТекстОшибки;
	
КонецФункции

Функция ТекстОшибкиБезТехническойИнформацииФТС(ТекстОшибки, ЭтоОшибкиПроверки = Ложь)
	
	Результат = ТекстОшибки;
	
	ЭтоОшибкиПроверки = (СтрДлина(Результат) >= 1250);
	
	ПрефиксОшибки = "System.Exception:";
	ДлинаПрефиксаОшибки = СтрДлина(ПрефиксОшибки);
	Если СтрДлина(Результат) > ДлинаПрефиксаОшибки И Лев(Результат, ДлинаПрефиксаОшибки) = ПрефиксОшибки Тогда
		Результат = Сред(Результат, ДлинаПрефиксаОшибки + 1);
		Результат = СокрЛ(Результат);
	КонецЕсли;
	
	ОкончаниеСпискаОшибок = СтрНайти(Результат,
		"Для отображения полей с ошибками проведите проверку в web-форме статистической формы по кнопке ""Проверить""");
	Если ОкончаниеСпискаОшибок > 1 Тогда
		ЭтоОшибкиПроверки = Истина;
		Результат = Лев(Результат, ОкончаниеСпискаОшибок - 1);
		Результат = СокрП(Результат);
	КонецЕсли;
	
	ПрефиксОписанияСТехническимиПодробностями = "--->";
	ОкончаниеОписанияСТехническимиПодробностями = СтрНайти(Результат, ПрефиксОписанияСТехническимиПодробностями);
	Если ОкончаниеОписанияСТехническимиПодробностями > 5 Тогда
		Результат = Лев(Результат, ОкончаниеОписанияСТехническимиПодробностями - 1);
		Результат = СокрП(Результат);
	КонецЕсли;
	
	Возврат Результат;
	
КонецФункции

Функция ПолучитьИнформациюОбОтчетеНаПорталеФТС(ПараметрыОтправки, ПараметрыСоединения)
	
	ПараметрыСоединения.ОписаниеОшибкиСервером = "";
	ПараметрыСоединения.ПовторятьСоединение = Ложь;
	АвторизацияПроизведена = (ПараметрыСоединения.СоединениеHTTP <> Неопределено);
	
	Если НЕ АвторизацияПроизведена Тогда
		// очистить пароль, если в диалоге сохранения пароля не установлена галочка сохранения
		Если НЕ ПараметрыСоединения.СохранитьПароль И ЗначениеЗаполнено(ПараметрыСоединения.СохраненныйПароль) Тогда
			ЗаписатьЛогинИПарольПорталаФТС(ПараметрыОтправки.Организация, , "");
			ПараметрыСоединения.СохраненныйПароль = "";
		КонецЕсли;
	КонецЕсли;
	
	Для НомерСервера = 1 По 2 Цикл
		АдресСервера = ПараметрыСоединения.ТекущийАдресСервера;
		
		Если НЕ АвторизацияПроизведена Тогда
			
			// устанавливаем соединение с сервером
			ОписаниеОшибкиУстановкиСоединения = "";
			ПараметрыСоединения.СоединениеHTTP = ДокументооборотСКО.УстановитьСоединениеССерверомИнтернета("https://" + АдресСервера, ОписаниеОшибкиУстановкиСоединения);
			
			Если ПараметрыСоединения.СоединениеHTTP = Неопределено Тогда
				ПараметрыСоединения.ПовторятьСоединение = Истина;
				СообщитьПользователю(
					СтрШаблон(НСтр("ru = 'Не удалось установить соединение с сервером:
									|%1'"), ОписаниеОшибкиУстановкиСоединения), ПараметрыСоединения.СообщенияПользователю);
				Возврат Неопределено;
			КонецЕсли;
			
			ПараметрыСоединения.ЗначениеCookie = "";
			
			Если НомерСервера = 1 Тогда
				ЭтоВызовПередАвторизацией = Истина;
				Если НЕ УточнитьАдресСервераФТС(ПараметрыОтправки, ПараметрыСоединения, ЭтоВызовПередАвторизацией) Тогда
					ПараметрыСоединения.СоединениеHTTP = Неопределено;
					Возврат Неопределено;
				КонецЕсли;
				
				Если ПараметрыСоединения.СоединениеHTTP = Неопределено Тогда
					// сервер сменился, нужно повторно соединиться
					Продолжить;
				КонецЕсли;
			КонецЕсли;
			
			АвторизацияПроизведена = АвторизоватьсяНаПорталеФТС(ПараметрыОтправки, ПараметрыСоединения);
			
		КонецЕсли;
		
		Если АвторизацияПроизведена Тогда
			Если НомерСервера = 1 Тогда
				ЭтоВызовПередАвторизацией = Ложь;
				Если НЕ УточнитьАдресСервераФТС(ПараметрыОтправки, ПараметрыСоединения, ЭтоВызовПередАвторизацией) Тогда
					ПараметрыСоединения.СоединениеHTTP = Неопределено;
					Возврат Неопределено;
				КонецЕсли;
				
				Если ПараметрыСоединения.СоединениеHTTP = Неопределено Тогда
					// сервер сменился, нужно повторно соединиться
					АвторизацияПроизведена = Ложь;
					Продолжить;
				КонецЕсли;
			КонецЕсли;
		КонецЕсли;
		
		Прервать;
	КонецЦикла;
	
	Если АвторизацияПроизведена Тогда
		
		ЗаголовокОтчета = ПолучитьПротоколОбработкиОтчетаНаПорталеФТС(ПараметрыОтправки, ПараметрыСоединения);
		
		Если ЗаголовокОтчета <> Неопределено Тогда
			
			ПараметрыСоединения.ОписаниеОшибкиСервером = "";
			ПараметрыСоединения.ПовторятьСоединение = Ложь;
			
			Возврат ЗаголовокОтчета;
			
		КонецЕсли;
		
	Иначе
		ПараметрыСоединения.СоединениеHTTP = Неопределено;
	КонецЕсли;
	
	Возврат Неопределено;
	
КонецФункции

Функция ПолучитьПротоколОбработкиОтчетаНаПорталеФТС(ПараметрыОтправки, ПараметрыСоединения, ТолькоНайтиЗаголовок = Ложь)
	
	Результат = Неопределено;
	
	Если НЕ ТолькоНайтиЗаголовок Тогда
		ПараметрыСоединения.ОписаниеОшибкиСервером = "";
		ПараметрыСоединения.ПовторятьСоединение = Ложь;
	КонецЕсли;
	
	// посылаем запрос списка для нахождения загруженного отчета по идентификатору
	
	РегистрационныйНомерАннулированного = "";
	КешЗаголовковОтчетов = Новый Массив;
	
	Пока Истина Цикл
		
		НомерСтраницы = 1;
		ЭлементовНаСтранице = 10;
		
		Пока Истина Цикл
			
			Если ЗначениеЗаполнено(РегистрационныйНомерАннулированного) Тогда
				ЗаголовкиОтчетов = КешЗаголовковОтчетов[НомерСтраницы - 1];
				
			Иначе
				ИндексНачальногоЭлементаСтрокой = Формат((НомерСтраницы - 1) * ЭлементовНаСтранице, "ЧН=0; ЧГ=");
				
				Если ПараметрыСоединения.ВерсияПортала = "2017" Тогда
					ЗаголовкиHTTP = Новый Соответствие();
					ЗаголовкиHTTP.Вставить("Content-Type", "application/json;charset=utf-8");
					ЗаголовкиHTTP.Вставить("Proxy-Connection", "Keep-Alive");
					ЗаголовкиHTTP.Вставить("Pragma", "no-cache");
					Если ЗначениеЗаполнено(ПараметрыСоединения.ЗначениеCookie) Тогда
						ЗаголовкиHTTP.Вставить("Cookie", ПараметрыСоединения.ЗначениеCookie);
					КонецЕсли;
					
					ПакетДляПолученияСписка = "{""OnlyMy"":0,""Filter"":"""",""ActionScopes"":""List""}";
					ИмяФайлаЗапроса = ПолучитьИмяВременногоФайла();
					ЗаписываемыйФайл = Новый ЗаписьТекста(ИмяФайлаЗапроса, "windows-1251");
					ЗаписываемыйФайл.Записать(ПакетДляПолученияСписка);
					ЗаписываемыйФайл.Закрыть();
					
					ИмяФайлаОтвета = ПолучитьИмяВременногоФайла();
					
					Попытка
						РесурсНаСервере = ДокументооборотСКО.ПолучитьЗаменяемоеЗначениеТестовогоСервера1С("/FtsPersonalCabinetWeb2017/UniversalDatabase/GetItems", 
																											"{FTS}/FtsPersonalCabinetWeb2017/UniversalDatabase/GetItems", 
																											ПараметрыСоединения.СоединениеHTTP) 
							+ "?scopeName=FtsStat&listName=StatDocuments&start=" + ИндексНачальногоЭлементаСтрокой;
						ОтветHTTP = ПараметрыСоединения.СоединениеHTTP.ОтправитьДляОбработки(
							ИмяФайлаЗапроса,
							РесурсНаСервере,
							ИмяФайлаОтвета,
							ЗаголовкиHTTP);
					Исключение
						
						Если НЕ ТолькоНайтиЗаголовок Тогда
							ПараметрыСоединения.ПовторятьСоединение = Истина;
							Возврат Неопределено;
							
						Иначе
							Результат = Новый Структура();
							Результат.Вставить("ИдентификаторОтправки", 			"");
							Результат.Вставить("Состояние", 						"");
							Результат.Вставить("РасширенноеСостояниеНаКонтроле", 	"");
							Результат.Вставить("ДатаИзменения", 					Неопределено);
							Результат.Вставить("СистемныйНомер", 					"");
							Результат.Вставить("РегистрационныйНомер", 				"");
							Результат.Вставить("Наименование", 						"");
							Результат.Вставить("Период", 							"");
							Результат.Вставить("Версия", 							"");
							Результат.Вставить("ИдентификаторПротокола", 			"");
							Результат.Вставить("ДатаСозданияПротокола", 			Неопределено);
							Результат.Вставить("ИдентификаторПросмотраПротокола", 	"");
							Результат.Вставить("Протокол", 							"");
							Результат.Вставить("Квитанция", 						"");
							Результат.Вставить("ПоляПротокола", 					Неопределено);
							Прервать;
						КонецЕсли;
						
					КонецПопытки;
					
					ЗначениеSetCookie = ОтветHTTP.Заголовки.Получить("Set-Cookie");
					ПараметрыСоединения.ЗначениеCookie = ОбновленноеЗначениеCookie(ПараметрыСоединения.ЗначениеCookie, ЗначениеSetCookie);
					
					ФайлОтвета = Новый ЧтениеТекста(ИмяФайлаОтвета, "utf-8");
					ТекстОтвета = ФайлОтвета.Прочитать();
					ФайлОтвета.Закрыть();
					
					ОперацииСФайламиЭДКО.УдалитьВременныйФайл(ИмяФайлаОтвета);
					
					ПараметрыСоединения.ОписаниеОшибкиСервером = ТекстОшибкиПорталаФТС(ТекстОтвета);
					
					Если ЗначениеЗаполнено(ПараметрыСоединения.ОписаниеОшибкиСервером) Тогда
						Возврат Неопределено;
					КонецЕсли;
					
					ЗаголовкиОтчетов = ТаблицаJSON(
						ТекстОтвета,
						"""Items"":[",
						"ИдентификаторОтправки, Состояние, РасширенноеСостояниеНаКонтроле, ДатаИзменения, СистемныйНомер, "
						+ "РегистрационныйНомер, Наименование, Период, Версия",
						"Id, StateName, OnControl, ModificationDateTime, SysNum, RegNum, Note, Period, SoftVersion",
						"""Total"":",
						Истина,
						Истина);
					
				Иначе
					ЗаголовкиHTTP = Новый Соответствие();
					ЗаголовкиHTTP.Вставить("Content-Type", "application/x-www-form-urlencoded");
					ЗаголовкиHTTP.Вставить("Proxy-Connection", "Keep-Alive");
					ЗаголовкиHTTP.Вставить("Pragma", "no-cache");
					ЗаголовкиHTTP.Вставить("X-Ext.Net", "delta=true");
					Если ЗначениеЗаполнено(ПараметрыСоединения.ЗначениеCookie) Тогда
						ЗаголовкиHTTP.Вставить("Cookie", ПараметрыСоединения.ЗначениеCookie);
					КонецЕсли;
					
					НомерСтраницыСтрокой = Формат(НомерСтраницы, "ЧН=0; ЧГ=");
					ЭлементовНаСтраницеСтрокой = Формат(ЭлементовНаСтранице, "ЧН=0; ЧГ=");
					ПакетДляПолученияСписка = "submitDirectEventConfig=" + ЗакодироватьПараметрСсылки(СтрШаблон(
						"{""config"":{""extraParams"":{""page"":%1,""start"":%2,""limit"":%3,""sort"":""[{\""property\"":"
						+ "\""CreatedModifiedDate\"",\""direction\"":\""DESC\""}]""}}}",
						НомерСтраницыСтрокой,
						ИндексНачальногоЭлементаСтрокой,
						ЭлементовНаСтраницеСтрокой))
						+ "&ctl55="
						+ "&__EVENTTARGET=ctl02"
						+ "&__EVENTARGUMENT=" + ЗакодироватьПараметрСсылки("StoreStatForms|postback|read")
						+ "&TextFieldNote="
						+ "&DropDownCreateVersion=" + ЗакодироватьПараметрСсылки(ПараметрыОтправки.ВерсияФормированияВыгрузки + "/"
						+ ПараметрыОтправки.ВерсияОтправкиПортала);
					
					ИмяФайлаЗапроса = ПолучитьИмяВременногоФайла();
					ЗаписываемыйФайл = Новый ЗаписьТекста(ИмяФайлаЗапроса, "windows-1251");
					ЗаписываемыйФайл.Записать(ПакетДляПолученияСписка);
					ЗаписываемыйФайл.Закрыть();
					
					ИмяФайлаОтвета = ПолучитьИмяВременногоФайла();
					
					Попытка
						РесурсНаСервере = ДокументооборотСКО.ПолучитьЗаменяемоеЗначениеТестовогоСервера1С("/FtsStatFormEpExtWeb/Stat/List.aspx", 
																											"{FTS}/FtsStatFormEpExtWeb/Stat/List.aspx", 
																											ПараметрыСоединения.СоединениеHTTP);
						ОтветHTTP = ПараметрыСоединения.СоединениеHTTP.ОтправитьДляОбработки(ИмяФайлаЗапроса, РесурсНаСервере, ИмяФайлаОтвета, ЗаголовкиHTTP);
					Исключение
						
						Если НЕ ТолькоНайтиЗаголовок Тогда
							ПараметрыСоединения.ПовторятьСоединение = Истина;
							Возврат Неопределено;
							
						Иначе
							Результат = Новый Структура();
							Результат.Вставить("ИдентификаторОтправки", 			"");
							Результат.Вставить("Состояние", 						"");
							Результат.Вставить("РасширенноеСостояниеНаКонтроле", 	"");
							Результат.Вставить("ДатаИзменения", 					Неопределено);
							Результат.Вставить("СистемныйНомер", 					"");
							Результат.Вставить("РегистрационныйНомер", 				"");
							Результат.Вставить("Наименование", 						"");
							Результат.Вставить("Период", 							"");
							Результат.Вставить("Версия", 							"");
							Результат.Вставить("ИдентификаторПротокола", 			"");
							Результат.Вставить("ДатаСозданияПротокола", 			Неопределено);
							Результат.Вставить("ИдентификаторПросмотраПротокола", 	"");
							Результат.Вставить("Протокол", 							"");
							Результат.Вставить("Квитанция", 						"");
							Результат.Вставить("ПоляПротокола", 					Неопределено);
							Прервать;
						КонецЕсли;
						
					КонецПопытки;
					
					ЗначениеSetCookie = ОтветHTTP.Заголовки.Получить("Set-Cookie");
					ПараметрыСоединения.ЗначениеCookie = ОбновленноеЗначениеCookie(ПараметрыСоединения.ЗначениеCookie, ЗначениеSetCookie);
					
					ФайлОтвета = Новый ЧтениеТекста(ИмяФайлаОтвета, "utf-8");
					ТекстОтвета = ФайлОтвета.Прочитать();
					ФайлОтвета.Закрыть();
					
					ОперацииСФайламиЭДКО.УдалитьВременныйФайл(ИмяФайлаОтвета);
					
					ПараметрыСоединения.ОписаниеОшибкиСервером = ТекстОшибкиПорталаФТС(ТекстОтвета);
					
					Если ЗначениеЗаполнено(ПараметрыСоединения.ОписаниеОшибкиСервером) Тогда
						Возврат Неопределено;
					КонецЕсли;
					
					ЗаголовкиОтчетов = ТаблицаJSON(
						ТекстОтвета,
						"data:{data:[",
						"ИдентификаторОтправки, Состояние, РасширенноеСостояниеНаКонтроле, ДатаИзменения, СистемныйНомер, "
						+ "РегистрационныйНомер, Наименование, Период, Версия",
						"Id, StateName, StateNameExtended, CreatedModifiedDate, SysNum, RegNum, Note, Period, SoftVersion",
						"success:true");
				КонецЕсли;
				
				КешЗаголовковОтчетов.Добавить(ЗаголовкиОтчетов);
			КонецЕсли;
			
			Если ЗаголовкиОтчетов <> Неопределено Тогда
				Для каждого ЗаголовокОтчета Из ЗаголовкиОтчетов Цикл
					Если ЗаголовокОтчета.ИдентификаторОтправки = ПараметрыСоединения.ИдентификаторОтправкиНаСервере
						ИЛИ СтрНайти(ЗаголовокОтчета.Наименование, " " + РегистрационныйНомерАннулированного + ")") > 0 Тогда
						
						ДатаИзмененияОтчета = ДатаИзСтрокиРазныхФорматов(ЗаголовокОтчета.ДатаИзменения);
						Результат = Новый Структура();
						Результат.Вставить("ИдентификаторОтправки", 			ЗаголовокОтчета.ИдентификаторОтправки);
						Результат.Вставить("Состояние", 						ЗаголовокОтчета.Состояние);
						Результат.Вставить("РасширенноеСостояниеНаКонтроле", 	ЗаголовокОтчета.РасширенноеСостояниеНаКонтроле);
						Результат.Вставить("ДатаИзменения", 					ДатаИзмененияОтчета);
						Результат.Вставить("СистемныйНомер", 					ЗаголовокОтчета.СистемныйНомер);
						Результат.Вставить("РегистрационныйНомер", 				ЗаголовокОтчета.РегистрационныйНомер);
						Результат.Вставить("Наименование", 						ЗаголовокОтчета.Наименование);
						Результат.Вставить("Период", 							ЗаголовокОтчета.Период);
						Результат.Вставить("Версия", 							ЗаголовокОтчета.Версия);
						Результат.Вставить("ИдентификаторПротокола", 			"");
						Результат.Вставить("ДатаСозданияПротокола", 			Неопределено);
						Результат.Вставить("ИдентификаторПросмотраПротокола", 	"");
						Результат.Вставить("Протокол", 							"");
						Результат.Вставить("Квитанция", 						"");
						Результат.Вставить("ПоляПротокола", 					Неопределено);
						Прервать;
					КонецЕсли;
				КонецЦикла;
			КонецЕсли;
			
			Если Результат <> Неопределено ИЛИ ЗаголовкиОтчетов = Неопределено ИЛИ ЗаголовкиОтчетов.Количество() < ЭлементовНаСтранице Тогда
				Прервать;
			КонецЕсли;
			
			НомерСтраницы = НомерСтраницы + 1;
			
		КонецЦикла;
		
		ЭтоАннулированныйОтчетПриПоискеОтправленногоВместоНего = Результат <> Неопределено И Результат.Состояние = "Аннулирована"
			И ЗначениеЗаполнено(Результат.РегистрационныйНомер) И НЕ ТолькоНайтиЗаголовок;
		
		Если НЕ ЭтоАннулированныйОтчетПриПоискеОтправленногоВместоНего
			ИЛИ Результат.РегистрационныйНомер = РегистрационныйНомерАннулированного Тогда
			
			Прервать;
		КонецЕсли;
		
		РегистрационныйНомерАннулированного = Результат.РегистрационныйНомер;
		
	КонецЦикла;
	
	Если Результат = Неопределено ИЛИ Результат.Состояние = "Новая статформа" ИЛИ Результат.Состояние = "Черновик" ИЛИ ТолькоНайтиЗаголовок Тогда
		Возврат Результат;
	КонецЕсли;
	
	// посылаем запрос журнала отправки отчета
	
	Если ПараметрыСоединения.ВерсияПортала = "2017" Тогда
		ЗаголовкиHTTP = Новый Соответствие();
		ЗаголовкиHTTP.Вставить("Content-Type", "application/json;charset=utf-8");
		ЗаголовкиHTTP.Вставить("Proxy-Connection", "Keep-Alive");
		ЗаголовкиHTTP.Вставить("Pragma", "no-cache");
		Если ЗначениеЗаполнено(ПараметрыСоединения.ЗначениеCookie) Тогда
			ЗаголовкиHTTP.Вставить("Cookie", ПараметрыСоединения.ЗначениеCookie);
		КонецЕсли;
		
		ПакетДляПолученияОтправок = СтрШаблон(
			"{""ItemId"":""%1"",""ModelName"":""Exchange""}",
			ПараметрыСоединения.ИдентификаторОтправкиНаСервере);
		ИмяФайлаЗапроса = ПолучитьИмяВременногоФайла();
		ЗаписываемыйФайл = Новый ЗаписьТекста(ИмяФайлаЗапроса, "windows-1251");
		ЗаписываемыйФайл.Записать(ПакетДляПолученияОтправок);
		ЗаписываемыйФайл.Закрыть();
		
		ИмяФайлаОтвета = ПолучитьИмяВременногоФайла();
		
		Попытка
			РесурсНаСервере = ДокументооборотСКО.ПолучитьЗаменяемоеЗначениеТестовогоСервера1С(
					"/FtsPersonalCabinetWeb2017/UniversalDatabase/GetItems",
					"{FTS}/FtsPersonalCabinetWeb2017/UniversalDatabase/GetItems",
					ПараметрыСоединения.СоединениеHTTP)
				+ "?scopeName=FtsStat&listName=vStatExchanges&start=0&format=JSON";
			ОтветHTTP = ПараметрыСоединения.СоединениеHTTP.ОтправитьДляОбработки(
				ИмяФайлаЗапроса,
				РесурсНаСервере,
				ИмяФайлаОтвета,
				ЗаголовкиHTTP);
		Исключение
			ПараметрыСоединения.ПовторятьСоединение = Истина;
			Возврат Неопределено;
		КонецПопытки;
		
		ЗначениеSetCookie = ОтветHTTP.Заголовки.Получить("Set-Cookie");
		ПараметрыСоединения.ЗначениеCookie = ОбновленноеЗначениеCookie(ПараметрыСоединения.ЗначениеCookie, ЗначениеSetCookie);
		
		ФайлОтвета = Новый ЧтениеТекста(ИмяФайлаОтвета, "utf-8");
		ТекстОтвета = ФайлОтвета.Прочитать();
		ФайлОтвета.Закрыть();
		
		ОперацииСФайламиЭДКО.УдалитьВременныйФайл(ИмяФайлаОтвета);
		
		ПараметрыСоединения.ОписаниеОшибкиСервером = ТекстОшибкиПорталаФТС(ТекстОтвета);
		
		Если ЗначениеЗаполнено(ПараметрыСоединения.ОписаниеОшибкиСервером) Тогда
			Возврат Неопределено;
		КонецЕсли;
		
		ЗаголовкиКвитанций = ТаблицаJSON(
			ТекстОтвета,
			"""Items"":[",
			"ИдентификаторКвитанции, ДатаСоздания, ЭтоВходящееСообщение, ОписаниеСообщения",
			"Id, CreationDateTime, IsIncoming, MessageDescription",
			"]}",
			Истина,
			Истина);
		
		Если ЗаголовкиКвитанций <> Неопределено И ЗаголовкиКвитанций.Количество() > 0 Тогда
			Для ИндексЗаголовка = 0 По ЗаголовкиКвитанций.Количество() - 1 Цикл
				Если ЗаголовкиКвитанций[ИндексЗаголовка].ЭтоВходящееСообщение <> "1" Тогда
					Прервать;
					
				ИначеЕсли СтрНайти(ЗаголовкиКвитанций[ИндексЗаголовка].ОписаниеСообщения, "возбуждении дела") = 0 Тогда
					Результат.ИдентификаторПротокола = ЗаголовкиКвитанций[ИндексЗаголовка].ИдентификаторКвитанции;
					Результат.ДатаСозданияПротокола = ДатаИзСтрокиРазныхФорматов(ЗаголовкиКвитанций[ИндексЗаголовка].ДатаСоздания);
					Прервать;
				КонецЕсли;
			КонецЦикла;
		КонецЕсли;
		
	Иначе
		ЗаголовкиHTTP = Новый Соответствие();
		ЗаголовкиHTTP.Вставить("Proxy-Connection", "Keep-Alive");
		ЗаголовкиHTTP.Вставить("Pragma", "no-cache");
		Если ЗначениеЗаполнено(ПараметрыСоединения.ЗначениеCookie) Тогда
			ЗаголовкиHTTP.Вставить("Cookie", ПараметрыСоединения.ЗначениеCookie);
		КонецЕсли;
		
		ИмяФайлаОтвета = ПолучитьИмяВременногоФайла();
		
		Попытка
			РесурсНаСервере = ДокументооборотСКО.ПолучитьЗаменяемоеЗначениеТестовогоСервера1С("/FtsStatFormEpExtWeb/Stat/Exchange.aspx", 
																								"{FTS}/FtsStatFormEpExtWeb/Stat/Exchange.aspx", 
																								ПараметрыСоединения.СоединениеHTTP) 
				+ "?Id=" + Результат.ИдентификаторОтправки;
			ОтветHTTP = ПараметрыСоединения.СоединениеHTTP.Получить(РесурсНаСервере, ИмяФайлаОтвета, ЗаголовкиHTTP);
		Исключение
			
			ПараметрыСоединения.СоединениеHTTP = Неопределено;
			ПараметрыСоединения.ПовторятьСоединение = Истина;
			ОперацииСФайламиЭДКО.УдалитьВременныйФайл(ИмяФайлаОтвета);
			Возврат Неопределено;
			
		КонецПопытки;
		
		ФайлОтвета = Новый ЧтениеТекста(ИмяФайлаОтвета, "utf-8");
		ТекстОтвета = ФайлОтвета.Прочитать();
		ФайлОтвета.Закрыть();
		
		ЗначениеSetCookie = ОтветHTTP.Заголовки.Получить("Set-Cookie");
		ПараметрыСоединения.ЗначениеCookie = ОбновленноеЗначениеCookie(ПараметрыСоединения.ЗначениеCookie, ЗначениеSetCookie);
		
		ОперацииСФайламиЭДКО.УдалитьВременныйФайл(ИмяФайлаОтвета);
		
		ЗаголовкиКвитанций = ТаблицаJSON(
			ТекстОтвета,
			"proxy:{data:[",
			"ИдентификаторКвитанции, ДатаСоздания, Направление, Сообщение",
			"Id, CreationDateTime, Direction, Message",
			Неопределено);
		
		Если ЗаголовкиКвитанций <> Неопределено И ЗаголовкиКвитанций.Количество() > 0 Тогда
			Для ИндексЗаголовка = 0 По ЗаголовкиКвитанций.Количество() - 1 Цикл
				Если ЗаголовкиКвитанций[ИндексЗаголовка].Направление <> "<-" Тогда
					Прервать;
					
				ИначеЕсли СтрНайти(ЗаголовкиКвитанций[ИндексЗаголовка].Сообщение, "Уведомление об отказе в возбуждении дела") = 0 Тогда
					Результат.ИдентификаторПротокола = ЗаголовкиКвитанций[ИндексЗаголовка].ИдентификаторКвитанции;
					Результат.ДатаСозданияПротокола = ДатаИзСтрокиРазныхФорматов(ЗаголовкиКвитанций[ИндексЗаголовка].ДатаСоздания);
					Прервать;
				КонецЕсли;
			КонецЦикла;
		КонецЕсли;
	КонецЕсли;
	
	Если НЕ ЗначениеЗаполнено(Результат.ИдентификаторПротокола) Тогда
		Возврат Результат;
	КонецЕсли;
	
	// посылаем запрос на просмотр протокола
	
	Если ПараметрыСоединения.ВерсияПортала = "2017" Тогда
		ЗаголовкиHTTP = Новый Соответствие();
		ЗаголовкиHTTP.Вставить("Content-Type", "application/json;charset=utf-8");
		ЗаголовкиHTTP.Вставить("Proxy-Connection", "Keep-Alive");
		ЗаголовкиHTTP.Вставить("Pragma", "no-cache");
		Если ЗначениеЗаполнено(ПараметрыСоединения.ЗначениеCookie) Тогда
			ЗаголовкиHTTP.Вставить("Cookie", ПараметрыСоединения.ЗначениеCookie);
		КонецЕсли;
		
		ПакетДляПолученияОтправок = СтрШаблон(
			"{""ItemId"":""%1"",""ModelName"":""Exchange""}",
			ПараметрыСоединения.ИдентификаторОтправкиНаСервере);
		ИмяФайлаЗапроса = ПолучитьИмяВременногоФайла();
		ЗаписываемыйФайл = Новый ЗаписьТекста(ИмяФайлаЗапроса, "windows-1251");
		ЗаписываемыйФайл.Записать(ПакетДляПолученияОтправок);
		ЗаписываемыйФайл.Закрыть();
		
		ИмяФайлаОтвета = ПолучитьИмяВременногоФайла();
		
		Попытка
			РесурсНаСервере = ДокументооборотСКО.ПолучитьЗаменяемоеЗначениеТестовогоСервера1С(
					"/FtsPersonalCabinetWeb2017/UniversalDatabase/GetItem",
					"{FTS}/FtsPersonalCabinetWeb2017/UniversalDatabase/GetItem",
					ПараметрыСоединения.СоединениеHTTP)
				+ "?scopeName=FtsStat&listName=vStatExchanges&id=" + Результат.ИдентификаторПротокола;
			ОтветHTTP = ПараметрыСоединения.СоединениеHTTP.ОтправитьДляОбработки(
				ИмяФайлаЗапроса,
				РесурсНаСервере,
				ИмяФайлаОтвета,
				ЗаголовкиHTTP);
		Исключение
			ПараметрыСоединения.ПовторятьСоединение = Истина;
			Возврат Неопределено;
		КонецПопытки;
		
		ЗначениеSetCookie = ОтветHTTP.Заголовки.Получить("Set-Cookie");
		ПараметрыСоединения.ЗначениеCookie = ОбновленноеЗначениеCookie(ПараметрыСоединения.ЗначениеCookie, ЗначениеSetCookie);
		
		ФайлОтвета = Новый ЧтениеТекста(ИмяФайлаОтвета, "utf-8");
		ТекстОтвета = ФайлОтвета.Прочитать();
		ФайлОтвета.Закрыть();
		
		ОперацииСФайламиЭДКО.УдалитьВременныйФайл(ИмяФайлаОтвета);
		
		ПараметрыСоединения.ОписаниеОшибкиСервером = ТекстОшибкиПорталаФТС(ТекстОтвета);
		
		Если ЗначениеЗаполнено(ПараметрыСоединения.ОписаниеОшибкиСервером) Тогда
			Возврат Неопределено;
		КонецЕсли;
		
		Квитанция64 = ТекстПослеПрефикса(ТекстОтвета, """EnvelopeZip"":""", """");
		Результат.Квитанция = Base64Значение(Квитанция64);
		
		Если НЕ ЗначениеЗаполнено(Квитанция64) Тогда
			Возврат Результат;
		КонецЕсли;
		
		// получаем JSON протокола
		
		ЗаголовкиHTTP = Новый Соответствие();
		ЗаголовкиHTTP.Вставить("Content-Type", "application/json;charset=utf-8");
		ЗаголовкиHTTP.Вставить("Proxy-Connection", "Keep-Alive");
		ЗаголовкиHTTP.Вставить("Pragma", "no-cache");
		Если ЗначениеЗаполнено(ПараметрыСоединения.ЗначениеCookie) Тогда
			ЗаголовкиHTTP.Вставить("Cookie", ПараметрыСоединения.ЗначениеCookie);
		КонецЕсли;
		
		ИмяФайлаЗапроса = ПолучитьИмяВременногоФайла();
		ЗаписываемыйФайл = Новый ЗаписьТекста(ИмяФайлаЗапроса, "windows-1251");
		ЗаписываемыйФайл.Записать(Квитанция64);
		ЗаписываемыйФайл.Закрыть();
		
		ИмяФайлаОтвета = ПолучитьИмяВременногоФайла();
		
		Попытка
			РесурсНаСервере = ДокументооборотСКО.ПолучитьЗаменяемоеЗначениеТестовогоСервера1С(
					"/FtsPersonalCabinetWeb2017/NgEdit/ConvertDoc",
					"{FTS}/FtsPersonalCabinetWeb2017/NgEdit/ConvertDoc",
					ПараметрыСоединения.СоединениеHTTP);
			ОтветHTTP = ПараметрыСоединения.СоединениеHTTP.ОтправитьДляОбработки(
				ИмяФайлаЗапроса,
				РесурсНаСервере,
				ИмяФайлаОтвета,
				ЗаголовкиHTTP);
		Исключение
			ПараметрыСоединения.ПовторятьСоединение = Истина;
			Возврат Неопределено;
		КонецПопытки;
		
		ЗначениеSetCookie = ОтветHTTP.Заголовки.Получить("Set-Cookie");
		ПараметрыСоединения.ЗначениеCookie = ОбновленноеЗначениеCookie(ПараметрыСоединения.ЗначениеCookie, ЗначениеSetCookie);
		
		ФайлОтвета = Новый ЧтениеТекста(ИмяФайлаОтвета, "utf-8");
		ТекстОтвета = ФайлОтвета.Прочитать();
		ФайлОтвета.Закрыть();
		
		ОперацииСФайламиЭДКО.УдалитьВременныйФайл(ИмяФайлаОтвета);
		
		ПараметрыСоединения.ОписаниеОшибкиСервером = ТекстОшибкиПорталаФТС(ТекстОтвета);
		
		Если ЗначениеЗаполнено(ПараметрыСоединения.ОписаниеОшибкиСервером) Тогда
			Возврат Неопределено;
		КонецЕсли;
		
		ИдентификаторыПолейПротокола = Новый Структура;
		ИдентификаторыПолейПротокола.Вставить("ИдентификаторВидаДокумента", 				"DocumentModeID");
		ИдентификаторыПолейПротокола.Вставить("ДатаНаправления", 							"SendDate");
		ИдентификаторыПолейПротокола.Вставить("ВремяНаправления", 							"SendTIme");
		ИдентификаторыПолейПротокола.Вставить("КодТаможенногоОргана", 						"Customs");
		ИдентификаторыПолейПротокола.Вставить("РегистрационныйНомерСтатистическойФормы", 	"StaticFormRegNumber");
		ИдентификаторыПолейПротокола.Вставить("УникальныйИдентификаторДокумента", 			"DocumentID");
		ИдентификаторыПолейПротокола.Вставить("ТранспортныйИдентификаторКонверта", 			"EnvelopeID");
		ИдентификаторыПолейПротокола.Вставить("ТранспортныйИдентификаторИсходногоКонверта", "InitialEnvelopeID");
		ИдентификаторыПолейПротокола.Вставить("ИнформацияОбОтправителе", 					"SenderInformation");
		ИдентификаторыПолейПротокола.Вставить("ИнформацияОПолучателе", 						"ReceiverInformation");
		ИдентификаторыПолейПротокола.Вставить("ДатаИВремяСозданияЭлектронногоСообщения", 	"PreparationDateTime");
		ИдентификаторыПолейПротокола.Вставить("ПриоритетСообщения", 						"Priority");
		ИдентификаторыПолейПротокола.Вставить("ТипСообщения", 								"MessageType");
		ИдентификаторыПолейПротокола.Вставить("УникальныйИдентификаторПроцесса", 			"ProccessID");
		ИдентификаторыПолейПротокола.Вставить("ТипПрограммногоОбеспечения", 				"SoftKind");
		ИдентификаторыПолейПротокола.Вставить("ВерсияПрограммногоОбеспечения", 				"SoftVersion");
		
		ПоляПротокола = Новый Структура;
		Для каждого ИдентификаторПоляПротокола Из ИдентификаторыПолейПротокола Цикл
			ТекстДоКонца = ТекстПослеПрефикса(ТекстОтвета, """" + ИдентификаторПоляПротокола.Значение + """:");
			Если ИдентификаторПоляПротокола.Ключ = "ВремяНаправления" И НЕ ЗначениеЗаполнено(ТекстДоКонца) Тогда
				ТекстДоКонца = ТекстПослеПрефикса(ТекстОтвета, """SendTime"":");
			КонецЕсли;
			
			ЗначениеПоляПротокола = "";
			Если ЗначениеЗаполнено(ТекстДоКонца) Тогда
				ЗначениеПоляПротокола = ТекстПослеПрефикса(ТекстДоКонца,
					?(ИдентификаторПоляПротокола.Ключ = "ИнформацияОПолучателе", """Value"":[""", """Value"":"""), """");
				ЗначениеПоляПротокола = Строка(ЗначениеПоляПротокола);
				ЗначениеПоляПротокола = СтрЗаменить(ЗначениеПоляПротокола, "\""", """");
			КонецЕсли;
			
			Если ЗначениеЗаполнено(ЗначениеПоляПротокола) Тогда
				Если ИдентификаторПоляПротокола.Ключ = "ДатаНаправления" ИЛИ ИдентификаторПоляПротокола.Ключ = "ВремяНаправления"
					ИЛИ ИдентификаторПоляПротокола.Ключ = "ДатаИВремяСозданияЭлектронногоСообщения" Тогда
					
					ЗначениеДатыВремени = ДатаИзСтрокиРазныхФорматов(ЗначениеПоляПротокола);
					Если ЗначениеЗаполнено(ЗначениеДатыВремени) Тогда
						
						Если ИдентификаторПоляПротокола.Ключ = "ДатаНаправления" Тогда
							ЗначениеПоляПротокола = Формат(ЗначениеДатыВремени, "ДЛФ=Д");
						ИначеЕсли ИдентификаторПоляПротокола.Ключ = "ВремяНаправления" Тогда
							ЗначениеПоляПротокола = Формат(ЗначениеДатыВремени, "ДЛФ=В");
						ИначеЕсли ИдентификаторПоляПротокола.Ключ = "ДатаИВремяСозданияЭлектронногоСообщения" Тогда
							ЗначениеПоляПротокола = Формат(ЗначениеДатыВремени, "ДЛФ=ДВ");
						КонецЕсли;
					КонецЕсли;
				КонецЕсли;
			КонецЕсли;
			
			ПоляПротокола.Вставить(ИдентификаторПоляПротокола.Ключ, ЗначениеПоляПротокола);
		КонецЦикла;
		
		Результат.ПоляПротокола = ПоляПротокола;
		
	Иначе
		ЗаголовкиHTTP = Новый Соответствие();
		ЗаголовкиHTTP.Вставить("Content-Type", "application/x-www-form-urlencoded");
		ЗаголовкиHTTP.Вставить("Proxy-Connection", "Keep-Alive");
		ЗаголовкиHTTP.Вставить("Pragma", "no-cache");
		ЗаголовкиHTTP.Вставить("X-Ext.Net", "delta=true");
		Если ЗначениеЗаполнено(ПараметрыСоединения.ЗначениеCookie) Тогда
			ЗаголовкиHTTP.Вставить("Cookie", ПараметрыСоединения.ЗначениеCookie);
		КонецЕсли;
		
		ПакетДляЗапросаПросмотраПротокола = "submitDirectEventConfig=" + ЗакодироватьПараметрСсылки("{""config"":{""extraParams"":{""target"":""" + Результат.ИдентификаторПротокола + """}}}")
			+ "&__EVENTTARGET=ctl02&__EVENTARGUMENT=" + ЗакодироватьПараметрСсылки("-|public|BeginViewDocument");
		ИмяФайлаЗапроса = ПолучитьИмяВременногоФайла();
		ЗаписываемыйФайл = Новый ЗаписьТекста(ИмяФайлаЗапроса, "windows-1251");
		ЗаписываемыйФайл.Записать(ПакетДляЗапросаПросмотраПротокола);
		ЗаписываемыйФайл.Закрыть();
		
		ИмяФайлаОтвета = ПолучитьИмяВременногоФайла();
		
		Попытка
			РесурсНаСервере = ДокументооборотСКО.ПолучитьЗаменяемоеЗначениеТестовогоСервера1С("/FtsStatFormEpExtWeb/Stat/Exchange.aspx", 
																								"{FTS}/FtsStatFormEpExtWeb/Stat/Exchange.aspx", 
																								ПараметрыСоединения.СоединениеHTTP) 
				+ "?Id=" + Результат.ИдентификаторОтправки;
			ОтветHTTP = ПараметрыСоединения.СоединениеHTTP.ОтправитьДляОбработки(ИмяФайлаЗапроса, РесурсНаСервере, ИмяФайлаОтвета, ЗаголовкиHTTP);
		Исключение
			
			ПараметрыСоединения.ПовторятьСоединение = Истина;
			Возврат Неопределено;
			
		КонецПопытки;
		
		ЗначениеSetCookie = ОтветHTTP.Заголовки.Получить("Set-Cookie");
		ПараметрыСоединения.ЗначениеCookie = ОбновленноеЗначениеCookie(ПараметрыСоединения.ЗначениеCookie, ЗначениеSetCookie);
		
		ФайлОтвета = Новый ЧтениеТекста(ИмяФайлаОтвета, "utf-8");
		ТекстОтвета = ФайлОтвета.Прочитать();
		ФайлОтвета.Закрыть();
		
		ОперацииСФайламиЭДКО.УдалитьВременныйФайл(ИмяФайлаОтвета);
		
		ПараметрыСоединения.ОписаниеОшибкиСервером = ТекстОшибкиПорталаФТС(ТекстОтвета);
		
		Если ЗначениеЗаполнено(ПараметрыСоединения.ОписаниеОшибкиСервером) Тогда
			Возврат Неопределено;
		КонецЕсли;
		
		Результат.ИдентификаторПросмотраПротокола = ТекстПослеПрефикса(ТекстОтвета, "result:{""id"":""", """");
		
		Если НЕ ЗначениеЗаполнено(Результат.ИдентификаторПросмотраПротокола) Тогда
			Возврат Результат;
		КонецЕсли;
	
		// посылаем запрос на html-протокол
		
		ЗаголовкиHTTP = Новый Соответствие();
		ЗаголовкиHTTP.Вставить("Proxy-Connection", "Keep-Alive");
		ЗаголовкиHTTP.Вставить("Pragma", "no-cache");
		Если ЗначениеЗаполнено(ПараметрыСоединения.ЗначениеCookie) Тогда
			ЗаголовкиHTTP.Вставить("Cookie", ПараметрыСоединения.ЗначениеCookie);
		КонецЕсли;
		
		РесурсНаСервере = ДокументооборотСКО.ПолучитьЗаменяемоеЗначениеТестовогоСервера1С("/FtsDocumentsMvc/PartPrint/Index/", 
																							"{FTS}/FtsDocumentsMvc/PartPrint/Index/", 
																							ПараметрыСоединения.СоединениеHTTP)
			+ Результат.ИдентификаторПросмотраПротокола 
			+ ДокументооборотСКО.ПолучитьЗаменяемоеЗначениеТестовогоСервера1С("?HideMenu=True", "{FTS}?HideMenu=True", ПараметрыСоединения.СоединениеHTTP);
		
		ЗапросHTTP = Новый HTTPЗапрос(РесурсНаСервере, ЗаголовкиHTTP);
		
		ИмяФайлаОтвета = ПолучитьИмяВременногоФайла();
		
		Попытка
			ОтветHTTP = ПараметрыСоединения.СоединениеHTTP.Получить(ЗапросHTTP, ИмяФайлаОтвета);
		Исключение
			
			ПараметрыСоединения.СоединениеHTTP = Неопределено;
			ПараметрыСоединения.ПовторятьСоединение = Истина;
			Возврат Неопределено;
			
		КонецПопытки;
		
		ФайлОтвета = Новый ЧтениеТекста(ИмяФайлаОтвета, "utf-8");
		Результат.Протокол = ФайлОтвета.Прочитать();
		ФайлОтвета.Закрыть();
		
		ЗначениеSetCookie = ОтветHTTP.Заголовки.Получить("Set-Cookie");
		ПараметрыСоединения.ЗначениеCookie = ОбновленноеЗначениеCookie(ПараметрыСоединения.ЗначениеCookie, ЗначениеSetCookie);
		
		ОперацииСФайламиЭДКО.УдалитьВременныйФайл(ИмяФайлаОтвета);
		
		// посылаем запрос на подписанную xml-квитанцию
		
		ЗаголовкиHTTP = Новый Соответствие();
		ЗаголовкиHTTP.Вставить("Proxy-Connection", "Keep-Alive");
		ЗаголовкиHTTP.Вставить("Pragma", "no-cache");
		Если ЗначениеЗаполнено(ПараметрыСоединения.ЗначениеCookie) Тогда
			ЗаголовкиHTTP.Вставить("Cookie", ПараметрыСоединения.ЗначениеCookie);
		КонецЕсли;
		
		РесурсНаСервере = ДокументооборотСКО.ПолучитьЗаменяемоеЗначениеТестовогоСервера1С("/FtsDocumentsMvc/PartView/FileDownload/", 
																							"{FTS}/FtsDocumentsMvc/PartView/FileDownload/", 
																							ПараметрыСоединения.СоединениеHTTP) 
			+ Результат.ИдентификаторПросмотраПротокола;
		ЗапросHTTP = Новый HTTPЗапрос(РесурсНаСервере, ЗаголовкиHTTP);
		
		ИмяФайлаОтвета = ПолучитьИмяВременногоФайла();
		
		Попытка
			ОтветHTTP = ПараметрыСоединения.СоединениеHTTP.Получить(ЗапросHTTP, ИмяФайлаОтвета);
		Исключение
			
			ПараметрыСоединения.СоединениеHTTP = Неопределено;
			ПараметрыСоединения.ПовторятьСоединение = Истина;
			Возврат Неопределено;
			
		КонецПопытки;
		
		ФайлОтвета = Новый ЧтениеТекста(ИмяФайлаОтвета, "utf-8");
		Результат.Квитанция = ФайлОтвета.Прочитать();
		ФайлОтвета.Закрыть();
		
		ЗначениеSetCookie = ОтветHTTP.Заголовки.Получить("Set-Cookie");
		ПараметрыСоединения.ЗначениеCookie = ОбновленноеЗначениеCookie(ПараметрыСоединения.ЗначениеCookie, ЗначениеSetCookie);
		
		ОперацииСФайламиЭДКО.УдалитьВременныйФайл(ИмяФайлаОтвета);
	КонецЕсли;
	
	Возврат Результат;
	
КонецФункции

Функция ЗакодироватьПараметрСсылки(ПараметрСсылки, КодировкаТекста = Неопределено)
	
	Если ЗначениеЗаполнено(КодировкаТекста) Тогда
		Результат = "";
		
		ДвоичныеДанныеСтроки = ПолучитьДвоичныеДанныеИзСтроки(ПараметрСсылки, КодировкаТекста);
		HexСтрокаДанных = ПолучитьHexСтрокуИзДвоичныхДанных(ДвоичныеДанныеСтроки);
		
		НомерСимвола = 1;
		Пока НомерСимвола <= СтрДлина(HexСтрокаДанных) Цикл
			Результат = Результат + "%" + Сред(HexСтрокаДанных, НомерСимвола, 2);
			НомерСимвола = НомерСимвола + 2;
		КонецЦикла;
		
	Иначе
		СимволыДляЗамены = "%" + Символы.Таб + Символы.ПС + Символы.ПФ + Символы.ВК + " !""#$&'()*+,/:;<=>?@[\]^`{|}~" + Символ(127);
		ПодставляемыеПодстроки = "%25%09%0A%0C%0D%20%21%22%23%24%26%27%28%29%2A%2B%2C%2F%3A%3B%3C%3D%3E%3F%40%5B%5C%5D%5E%60%7B%7C%7D%7E%7F";
		
		Результат = ПараметрСсылки;
		Для НомерСимвола = 1 По СтрДлина(СимволыДляЗамены) Цикл
			СимволДляЗамены = Сред(СимволыДляЗамены, НомерСимвола, 1);
			ПодставляемаяПодстрока = Сред(ПодставляемыеПодстроки, (НомерСимвола - 1) * 3 + 1, 3);
			Результат = СтрЗаменить(Результат, СимволДляЗамены, ПодставляемаяПодстрока);
		КонецЦикла;
	КонецЕсли;
	
	Возврат Результат;
	
КонецФункции

Функция ТекстВШестнадцатеричныйUnicode(Текст)
	
	Результат = "";
	Для НомерСимвола = 1 По СтрДлина(Текст) Цикл
		ТекущийСимвол = Сред(Текст, НомерСимвола, 1);
		Если КодСимвола(ТекущийСимвол) >= 256 Тогда
			КодТекущегоСимвола = КодСимвола(ТекущийСимвол);
			СтаршийБайтКодаТекущегоСимвола = Число(КодТекущегоСимвола / 256);
			ШестнадцатеричноеПредставлениеТекущегоСимвола = ЧислоВШестнадцатеричномПредставлении(СтаршийБайтКодаТекущегоСимвола, 1) + ЧислоВШестнадцатеричномПредставлении(КодТекущегоСимвола % 256, 1);
			Результат = Результат + "\u" + нрег(ШестнадцатеричноеПредставлениеТекущегоСимвола);
			
		Иначе
			Результат = Результат + ТекущийСимвол;
		КонецЕсли;
	КонецЦикла;
	Возврат Результат;
	
КонецФункции

Функция ТекстПослеПрефиксаПодобрать(
		ПолныйТекст,
		Префикс,
		Постфикс,
		ПодстрокаПоиска,
		ВернутьСледующий = Ложь)
	
	Результат = Неопределено;
	ТекстДоКонца = ТекстПослеПрефикса(ПолныйТекст, Префикс);
	
	НомерТекста = 1;
	ТекстСледующий = Неопределено;
	Пока ЗначениеЗаполнено(ТекстДоКонца) Цикл
		ПозицияПостфикса = СтрНайти(ТекстДоКонца, Постфикс);
		Если ПозицияПостфикса > 0 Тогда
			ТекстРезультата = Лев(ТекстДоКонца, ПозицияПостфикса - 1);
		Иначе
			ТекстРезультата = ТекстДоКонца;
		КонецЕсли;
		Если НомерТекста = 1 Тогда
			Результат = ТекстРезультата;
		КонецЕсли;
		
		ТекстСледующий = ТекстПослеПрефикса(ТекстДоКонца, Префикс);
		
		ДлинаТекстаМежду = СтрДлина(ТекстДоКонца) - СтрДлина(ТекстСледующий);
		ТекстМеждуПрефиксами = Лев(ТекстДоКонца, ДлинаТекстаМежду);
		
		Если СтрНайти(ТекстМеждуПрефиксами, ПодстрокаПоиска) > 0 Тогда
			Если ВернутьСледующий Тогда
				ПозицияПостфикса = СтрНайти(ТекстСледующий, Постфикс);
				Если ПозицияПостфикса > 0 Тогда
					ТекстРезультата = Лев(ТекстСледующий, ПозицияПостфикса - 1);
				Иначе
					ТекстРезультата = ТекстСледующий;
				КонецЕсли;
			КонецЕсли;
			Результат = ТекстРезультата;
			Прервать;
		КонецЕсли;
		
		НомерТекста = НомерТекста + 1;
		ТекстДоКонца = ТекстСледующий;
	КонецЦикла;
	
	Возврат Результат;
	
КонецФункции

#Область ТаможенноеДекларирование

//
Функция ПолучитьПроцедурыДекларированияФТСССервера(Знач ПараметрыОтправки, ПараметрыСоединения) Экспорт
	
	//запрос результатов обработки
	ПараметрыСоединения.ОписаниеОшибкиСервером = "";
	ПараметрыСоединения.ПовторятьСоединение = Ложь;
	ПараметрыСоединения.ПовторятьЗапросПароля = Ложь;
	
	ЗаголовкиОтчетов = ПолучитьИнформациюОПроцедурахДекларированияНаПорталеФТС(ПараметрыОтправки, ПараметрыСоединения);
	
	Если ЗаголовкиОтчетов <> Неопределено И НЕ ПараметрыСоединения.ПовторятьСоединение Тогда
		
		// получен протокол обработки и отсутствовали ошибки соединения
		
		Результат = ЗаголовкиОтчетов;
		
	ИначеЕсли ЗначениеЗаполнено(ПараметрыСоединения.ОписаниеОшибкиСервером) ИЛИ НЕ ПараметрыСоединения.ПовторятьСоединение Тогда
		
		Если ЗначениеЗаполнено(ПараметрыСоединения.ОписаниеОшибкиСервером) Тогда
			ПараметрыСоединения.ОписаниеОшибкиСервером = ТекстОшибкиБезТехническойИнформацииФТС(ПараметрыСоединения.ОписаниеОшибкиСервером);
			
			ТекстСообщения = СтрШаблон(
				НСтр("ru = 'Результаты отправки не получены с портала. %1'"),
				ПараметрыСоединения.ОписаниеОшибкиСервером);
			
			СообщитьПользователю(ТекстСообщения, ПараметрыСоединения.СообщенияПользователю);
			
		Иначе
			ТекстСообщения = НСтр("ru = 'Не удалось получить результаты отправки с портала Федеральной таможенной службы.'");
			
			СообщитьПользователю(ТекстСообщения, ПараметрыСоединения.СообщенияПользователю);
		КонецЕсли;
		
		Результат = Неопределено;
		
	Иначе
		
		// возможно, проблемы с доступом в интернет
		Результат = Неопределено;
		
	КонецЕсли;
	
	ПараметрыСоединения.СоединениеHTTP = Неопределено;
	ПараметрыСоединения.ОписаниеОшибкиСервером = "";
	
	Возврат Результат;
	
КонецФункции

Функция ПолучитьИнформациюОПроцедурахДекларированияНаПорталеФТС(ПараметрыОтправки, ПараметрыСоединения)
	
	ПараметрыСоединения.ОписаниеОшибкиСервером = "";
	ПараметрыСоединения.ПовторятьСоединение = Ложь;
	АвторизацияПроизведена = (ПараметрыСоединения.СоединениеHTTP <> Неопределено);
	
	Если НЕ АвторизацияПроизведена Тогда
		// очистить пароль, если в диалоге сохранения пароля не установлена галочка сохранения
		Если НЕ ПараметрыСоединения.СохранитьПароль И ЗначениеЗаполнено(ПараметрыСоединения.СохраненныйПароль) Тогда
			ЗаписатьЛогинИПарольПорталаФТС(ПараметрыОтправки.Организация,, "");
			ПараметрыСоединения.СохраненныйПароль = "";
		КонецЕсли;
	КонецЕсли;
	
	Для НомерСервера = 1 По 2 Цикл
		АдресСервера = ПараметрыСоединения.ТекущийАдресСервера;
		
		Если НЕ АвторизацияПроизведена Тогда
			
			// устанавливаем соединение с сервером
			ОписаниеОшибкиУстановкиСоединения = "";
			ПараметрыСоединения.СоединениеHTTP = ДокументооборотСКО.УстановитьСоединениеССерверомИнтернета(
				"https://" + АдресСервера, ОписаниеОшибкиУстановкиСоединения);
			
			Если ПараметрыСоединения.СоединениеHTTP = Неопределено Тогда
				ПараметрыСоединения.ПовторятьСоединение = Истина;
				СообщитьПользователю(
					СтрШаблон(НСтр("ru = 'Не удалось установить соединение с сервером:
								   |%1'"), ОписаниеОшибкиУстановкиСоединения), ПараметрыСоединения.СообщенияПользователю);
				Возврат Неопределено;
			КонецЕсли;
			
			ПараметрыСоединения.ЗначениеCookie = "";
			
			Если НомерСервера = 1 Тогда
				ЭтоВызовПередАвторизацией = Истина;
				Если НЕ УточнитьАдресСервераФТС(ПараметрыОтправки, ПараметрыСоединения, ЭтоВызовПередАвторизацией) Тогда
					ПараметрыСоединения.СоединениеHTTP = Неопределено;
					Возврат Неопределено;
				КонецЕсли;
				
				Если ПараметрыСоединения.СоединениеHTTP = Неопределено Тогда
					// сервер сменился, нужно повторно соединиться
					Продолжить;
				КонецЕсли;
			КонецЕсли;
			
			АвторизацияПроизведена = АвторизоватьсяНаПорталеФТС(ПараметрыОтправки, ПараметрыСоединения);
			
		КонецЕсли;
		
		Если АвторизацияПроизведена Тогда
			Если НомерСервера = 1 Тогда
				ЭтоВызовПередАвторизацией = Ложь;
				Если НЕ УточнитьАдресСервераФТС(ПараметрыОтправки, ПараметрыСоединения, ЭтоВызовПередАвторизацией) Тогда
					ПараметрыСоединения.СоединениеHTTP = Неопределено;
					Возврат Неопределено;
				КонецЕсли;
				
				Если ПараметрыСоединения.СоединениеHTTP = Неопределено Тогда
					// сервер сменился, нужно повторно соединиться
					АвторизацияПроизведена = Ложь;
					Продолжить;
				КонецЕсли;
			КонецЕсли;
		КонецЕсли;
		
		Прервать;
	КонецЦикла;
	
	Если АвторизацияПроизведена Тогда
		
		ЗаголовокОтчета = ПолучитьПроцедурыДекларированияНаПорталеФТС(ПараметрыОтправки, ПараметрыСоединения);
		
		Если ЗаголовокОтчета <> Неопределено Тогда
			
			ПараметрыСоединения.ОписаниеОшибкиСервером = "";
			ПараметрыСоединения.ПовторятьСоединение = Ложь;
			
			Возврат ЗаголовокОтчета;
			
		КонецЕсли;
		
	Иначе
		ПараметрыСоединения.СоединениеHTTP = Неопределено;
	КонецЕсли;
	
	Возврат Неопределено;
	
КонецФункции

Функция ПолучитьПроцедурыДекларированияНаПорталеФТС(ПараметрыОтправки, ПараметрыСоединения)
	
	Результат = Новый Массив;
	
	ЭтоВыпущенныеДТ = (ВРег(ПараметрыОтправки.РежимПолучения) = ВРег("ВыпущенныеДТ"));
	
	ОтборыВызова = Новый Структура;
	ОтборыВызова.Вставить("ТипПроцедуры", 			Неопределено);
	ОтборыВызова.Вставить("ДатаРешенияС", 			Неопределено);
	ОтборыВызова.Вставить("ДатаРешенияПо", 			Неопределено);
	ОтборыВызова.Вставить("РегистрационныйНомер", 	Неопределено);
	Если ПараметрыОтправки.Отборы <> Неопределено Тогда
		ЗаполнитьЗначенияСвойств(ОтборыВызова, ПараметрыОтправки.Отборы);
	КонецЕсли;
	Если ЗначениеЗаполнено(ОтборыВызова.ДатаРешенияПо) И НЕ ЗначениеЗаполнено(ОтборыВызова.ДатаРешенияС) Тогда
		ОтборыВызова.ДатаРешенияПо = '17530102';
	КонецЕсли;
	
	ПараметрыСоединения.ОписаниеОшибкиСервером = "";
	ПараметрыСоединения.ПовторятьСоединение = Ложь;
	
	// посылаем запрос списка
	
	НомерСтраницы = 1;
	ЭлементовНаСтранице = 10;
	
	Пока Истина Цикл
		ИндексНачальногоЭлементаСтрокой = Формат((НомерСтраницы - 1) * ЭлементовНаСтранице, "ЧН=0; ЧГ=");
		
		ЗаголовкиHTTP = Новый Соответствие();
		ЗаголовкиHTTP.Вставить("Content-Type", "application/json;charset=utf-8");
		ЗаголовкиHTTP.Вставить("Proxy-Connection", "Keep-Alive");
		ЗаголовкиHTTP.Вставить("Pragma", "no-cache");
		Если ЗначениеЗаполнено(ПараметрыСоединения.ЗначениеCookie) Тогда
			ЗаголовкиHTTP.Вставить("Cookie", ПараметрыСоединения.ЗначениеCookie);
		КонецЕсли;
		
		ПакетДляПолученияСписка = "{""OnlyMy"":0,""Filter"":"""""
			+ ?(ЗначениеЗаполнено(ОтборыВызова.ТипПроцедуры), ",""" + ?(ЭтоВыпущенныеДТ, "PROC", "ModelName")
				+ "_Filter_1"":""" + ОтборыВызова.ТипПроцедуры + """", "")
			+ ?(ЗначениеЗаполнено(ОтборыВызова.ДатаРешенияС), ",""" + ?(ЭтоВыпущенныеДТ, "GD1", "CreationDateTime")
				+ "_Filter_1"":""" + Формат(ОтборыВызова.ДатаРешенияС, "ДФ=дд.ММ.гггг") + """", "")
			+ ?(ЗначениеЗаполнено(ОтборыВызова.ДатаРешенияПо), ",""" + ?(ЭтоВыпущенныеДТ, "GD1", "CreationDateTime")
				+ "_Filter_1_1"":""" + Формат(ОтборыВызова.ДатаРешенияПо, "ДФ=дд.ММ.гггг") + """", "")
			+ ?(ЗначениеЗаполнено(ОтборыВызова.РегистрационныйНомер), ",""" + ?(ЭтоВыпущенныеДТ, "ND", "RegNum")
				+ "_Filter_1"":""" + ОтборыВызова.РегистрационныйНомер + """", "")
			+ "}";
		ИмяФайлаЗапроса = ПолучитьИмяВременногоФайла();
		ЗаписываемыйФайл = Новый ЗаписьТекста(ИмяФайлаЗапроса, "utf-8");
		ЗаписываемыйФайл.Записать(ПакетДляПолученияСписка);
		ЗаписываемыйФайл.Закрыть();
		
		ИмяФайлаОтвета = ПолучитьИмяВременногоФайла();
		
		Попытка
			РесурсНаСервере = ДокументооборотСКО.ПолучитьЗаменяемоеЗначениеТестовогоСервера1С(
					"/FtsPersonalCabinetWeb2017/UniversalDatabase/GetItems",
					"{FTS}/FtsPersonalCabinetWeb2017/UniversalDatabase/GetItems",
					ПараметрыСоединения.СоединениеHTTP)
				+ ?(ЭтоВыпущенныеДТ, "?scopeName=FtsDataSets&listName=DataSetDeclarHeaders&start=", "?scopeName=FtsPortalEd&listName=DeclProcedures&start=")
				+ ИндексНачальногоЭлементаСтрокой + "&format=JSON";
				
			ОтветHTTP = ПараметрыСоединения.СоединениеHTTP.ОтправитьДляОбработки(
				ИмяФайлаЗапроса,
				РесурсНаСервере,
				ИмяФайлаОтвета,
				ЗаголовкиHTTP);
		Исключение
			ОперацииСФайламиЭДКО.УдалитьВременныйФайл(ИмяФайлаЗапроса);
			ПараметрыСоединения.ПовторятьСоединение = Истина;
			Возврат Неопределено;
		КонецПопытки;
		
		ЗначениеSetCookie = ОтветHTTP.Заголовки.Получить("Set-Cookie");
		ПараметрыСоединения.ЗначениеCookie = ОбновленноеЗначениеCookie(ПараметрыСоединения.ЗначениеCookie, ЗначениеSetCookie);
		
		ФайлОтвета = Новый ЧтениеТекста(ИмяФайлаОтвета, "utf-8");
		ТекстОтвета = ФайлОтвета.Прочитать();
		ФайлОтвета.Закрыть();
		
		ОперацииСФайламиЭДКО.УдалитьВременныйФайл(ИмяФайлаЗапроса);
		ОперацииСФайламиЭДКО.УдалитьВременныйФайл(ИмяФайлаОтвета);
		
		ПараметрыСоединения.ОписаниеОшибкиСервером = ТекстОшибкиПорталаФТС(ТекстОтвета);
		
		Если ЗначениеЗаполнено(ПараметрыСоединения.ОписаниеОшибкиСервером) Тогда
			Возврат Неопределено;
		КонецЕсли;
		
		СписокПолей = Новый СписокЗначений;
		Если ЭтоВыпущенныеДТ Тогда
			СписокПолей.Добавить("РегистрационныйНомер", 				"ND");
			СписокПолей.Добавить("ДатаСоздания", 						"DTLOAD");
			СписокПолей.Добавить("ТипПроцедуры", 						"PROC");
			СписокПолей.Добавить("ВсегоТоваровМест", 					"AMOUNT");
			СписокПолей.Добавить("ОбщаяСтоимость", 						"COST");
			СписокПолей.Добавить("Статус", 								"GD0");
			СписокПолей.Добавить("ДатаРешения", 						"GD1");
			СписокПолей.Добавить("Отправитель", 						"SENDER");
			СписокПолей.Добавить("Получатель", 							"RECEIVER");
			СписокПолей.Добавить("СтранаОтправления", 					"G15");
			СписокПолей.Добавить("СтранаНазначения", 					"G17B");
			СписокПолей.Добавить("СтранаПроисхождения", 				"G16");
			СписокПолей.Добавить("ОтветственныйЗаФинУрегулирование", 	"FINANCE");
			СписокПолей.Добавить("Идентификатор", 						"Id");
			СписокПолей.Добавить("ПроцедураВЛК", 						"DeclProcId");
			СписокПолей.Добавить("ДатаРегистрации", 					"G072");
			СписокПолей.Добавить("КодРегистрации", 						"G071");
			СписокПолей.Добавить("ПорядковыйНомер", 					"G073");
			СписокПолей.Добавить("Декларант", 							"DECLARANT");
			СписокПолей.Добавить("ИмяОтправителя", 						"G022");
			СписокПолей.Добавить("ИмяПолучателя", 						"G082");
			СписокПолей.Добавить("Модель", 								"ModelName");
			СписокПолей.Добавить("ИмяСтатуса", 							"StateName");
			СписокПолей.Добавить("Версия", 								"SoftVersion");
			
			
		Иначе
			СписокПолей.Добавить("РегистрационныйНомер", 	"RegNum");
			СписокПолей.Добавить("ДатаСоздания", 			"CreationDateTime");
			СписокПолей.Добавить("ТипПроцедуры", 			"ModelName");
			СписокПолей.Добавить("Статус", 					"StateName");
			СписокПолей.Добавить("Примечание", 				"Note");
		КонецЕсли;
		
		МассивИменПолей = Новый Массив;
		МассивИдентификаторовПолей = Новый Массив;
		Для каждого ЭлементСписка Из СписокПолей Цикл
			МассивИменПолей.Добавить(ЭлементСписка.Значение);
			МассивИдентификаторовПолей.Добавить(ЭлементСписка.Представление);
		КонецЦикла;
		ИменаПолей = СтрСоединить(МассивИменПолей, ", ");
		ИдентификаторыПолей = СтрСоединить(МассивИдентификаторовПолей, ", ");
		
		ЗаголовкиОтчетов = ТаблицаJSON(
			ТекстОтвета,
			"""Items"":[",
			ИменаПолей,
			ИдентификаторыПолей,
			"]}",
			Истина,
			Истина);
		
		Если ЗаголовкиОтчетов <> Неопределено Тогда
			КоличествоКолонок = ЗаголовкиОтчетов.Колонки.Количество();
			Для каждого ЗаголовокОтчета Из ЗаголовкиОтчетов Цикл
				ПроцедураДекларирования = Новый Структура;
				Для ИндексКолонки = 0 По КоличествоКолонок - 1 Цикл
					ПроцедураДекларирования.Вставить(ЗаголовкиОтчетов.Колонки[ИндексКолонки].Имя, ЗаголовокОтчета[ИндексКолонки]);
				КонецЦикла;
				Результат.Добавить(ПроцедураДекларирования);
			КонецЦикла;
		КонецЕсли;
		
		Если ЗаголовкиОтчетов = Неопределено ИЛИ ЗаголовкиОтчетов.Количество() < ЭлементовНаСтранице Тогда
			Прервать;
		КонецЕсли;
		
		НомерСтраницы = НомерСтраницы + 1;
	КонецЦикла;
	
	Возврат Результат;
	
КонецФункции

#КонецОбласти

////////////////////////////////////////////////////////////////////////////////
// Служебные функции ФТС

Функция СерверПриемаОтчетностиФТС() Экспорт
	
	ВерсияПортала = "2017";
	
	// Возвращаем параметры и адрес сервера
	Результат = Новый Структура;
	Результат.Вставить("АдресСервера", 					"edata.customs.ru");
	Результат.Вставить("ВерсияФормированияВыгрузки", 	"5.16.0");
	Результат.Вставить("ВерсияОтправкиПортала", 		"3.3.26");
	Результат.Вставить("ВерсияПортала", 				ВерсияПортала);
	
	Результат.АдресСервера = ДокументооборотСКО.ПолучитьЗаменяемоеЗначениеТестовогоСервера1С(Результат.АдресСервера);
	
	Возврат Результат;
	
КонецФункции

Функция НастройкиФТС(Знач ОрганизацияСсылка, Знач ПолучитьПараметрыОнлайнПроверки = Ложь) Экспорт
	
	Результат = Новый Структура("ИспользоватьОбмен, СертификатАбонентаОтпечаток, Логин, Пароль, ИспользоватьАвтонастройку", Ложь, "", "", "", Ложь);
	
	Если ПолучитьПараметрыОнлайнПроверки Тогда
		СвойстваОрганизации = ЗначенияРеквизитовОбъекта(ОрганизацияСсылка, "ВидОбменаСКонтролирующимиОрганами, УчетнаяЗаписьОбмена");
		
		Результат.Вставить("УчетнаяЗаписьОбмена", СвойстваОрганизации.УчетнаяЗаписьОбмена);
		Результат.Вставить("НастроенОбменВУниверсальномФормате", СвойстваОрганизации.ВидОбменаСКонтролирующимиОрганами = Перечисления.ВидыОбменаСКонтролирующимиОрганами.ОбменВУниверсальномФормате
			И ЗначениеЗаполнено(СвойстваОрганизации.УчетнаяЗаписьОбмена));
		
		Попытка
			ОбменИОнлайнПроверкаВключены = Результат.НастроенОбменВУниверсальномФормате И НЕ СвойстваОрганизации.УчетнаяЗаписьОбмена.ОбменНапрямую И СвойстваОрганизации.УчетнаяЗаписьОбмена.ИспользоватьСервисОнлайнПроверкиОтчетов;
		Исключение
			ОбменИОнлайнПроверкаВключены = Ложь;
		КонецПопытки;
		
		ОнлайнПроверкаКонтролирующиеОрганы = ?(ОбменИОнлайнПроверкаВключены,
			?(СвойстваОрганизации.УчетнаяЗаписьОбмена.СпецоператорСвязи = Перечисления.СпецоператорыСвязи.Такском,
			"ФНС;ПФР;ФСС;ФСРАР;ФТС",
			ЭлектронныйДокументооборотСКонтролирующимиОрганамиВызовСервера.ПолучитьПараметрСпецоператора(
			СвойстваОрганизации.УчетнаяЗаписьОбмена.СпецоператорСвязи, "ОнлайнПроверкаКонтролирующиеОрганы")), "");
		ОнлайнПроверкаКонтролирующиеОрганы = СокрЛП(ОнлайнПроверкаКонтролирующиеОрганы);
		ОнлайнПроверкаКонтролирующиеОрганы = ВРег(ОнлайнПроверкаКонтролирующиеОрганы);
		
		ОнлайнПроверкаДоступна = СтрНайти(";" + ОнлайнПроверкаКонтролирующиеОрганы + ";", ";ФТС;") > 0;
		
		Результат.Вставить("ОнлайнПроверкаДоступна", ОнлайнПроверкаДоступна);
	КонецЕсли;
	
	Запрос = Новый Запрос;
	Запрос.Текст = "ВЫБРАТЬ РАЗРЕШЕННЫЕ ПЕРВЫЕ 1
				   |	НастройкиОбменаФТС.ИспользоватьОбмен КАК ИспользоватьОбмен,
				   |	НастройкиОбменаФТС.СертификатАбонентаОтпечаток КАК СертификатАбонентаОтпечаток,
				   |	НастройкиОбменаФТС.Логин КАК Логин,
				   |	НастройкиОбменаФТС.Пароль КАК Пароль,
				   |	НастройкиОбменаФТС.ИспользоватьАвтонастройку КАК ИспользоватьАвтонастройку
				   |ИЗ
				   |	РегистрСведений.НастройкиОбменаФТС КАК НастройкиОбменаФТС
				   |ГДЕ
				   |	НастройкиОбменаФТС.Организация = &ОрганизацияСсылка";
	Запрос.УстановитьПараметр("ОрганизацияСсылка", ОрганизацияСсылка);
	РезультатЗапроса = Запрос.Выполнить();
	Если НЕ РезультатЗапроса.Пустой() Тогда
		ЗаписьЗапроса = РезультатЗапроса.Выгрузить()[0];
		Если ЗаписьЗапроса.ИспользоватьОбмен Тогда
			
			Результат.ИспользоватьОбмен = ЗаписьЗапроса.ИспользоватьОбмен;
			Результат.СертификатАбонентаОтпечаток = СокрЛП(ЗаписьЗапроса.СертификатАбонентаОтпечаток);
			Результат.ИспользоватьАвтонастройку = ЗаписьЗапроса.ИспользоватьАвтонастройку;
			
		КонецЕсли;
		
		Результат.Логин = СокрЛП(ЗаписьЗапроса.Логин);
		Результат.Пароль = СокрЛП(ЗаписьЗапроса.Пароль);
	КонецЕсли;
	
	Возврат Результат;
	
КонецФункции

Функция СвойстваОбменаОрганизацииДляФТС(Знач ОрганизацияСсылка, Знач ОпределитьНастроенностьОбменаВУниверсальномФормате, Знач ПолучитьОсновныеСвойстваПоследнейОтправки, Знач ОтправкаСсылка = Неопределено, ОтчетСсылка = Неопределено) Экспорт
	
	Результат = Новый Структура("НастроенОбменВУниверсальномФормате, СвойстваОтправки, НастройкиОбмена, ОрганизацияИзОтправки, ПравоИзмененияУчетнойЗаписи, ЭтоРегламентированныйОтчет, ЭтоЭлектроннаяПодписьВМоделиСервиса", Неопределено, Неопределено, Неопределено, Неопределено, Неопределено, Неопределено, Неопределено);
	
	Если ОрганизацияСсылка = Неопределено Тогда
		
		ОрганизацияСсылка = ОтправкаСсылка.Организация;
		Результат.ОрганизацияИзОтправки = ОрганизацияСсылка;
		
	КонецЕсли;
	
	Результат.ЭтоЭлектроннаяПодписьВМоделиСервиса = ЭлектроннаяПодписьВМоделиСервисаБРОВызовСервера.ЭтоЭлектроннаяПодписьВМоделиСервиса(ОрганизацияСсылка);
	
	Если ОпределитьНастроенностьОбменаВУниверсальномФормате Тогда
		
		СвойстваОрганизации = ЗначенияРеквизитовОбъекта(ОрганизацияСсылка, "ВидОбменаСКонтролирующимиОрганами, УчетнаяЗаписьОбмена");
		
		Результат.НастроенОбменВУниверсальномФормате = (СвойстваОрганизации.ВидОбменаСКонтролирующимиОрганами = Перечисления.ВидыОбменаСКонтролирующимиОрганами.ОбменВУниверсальномФормате
			И ЗначениеЗаполнено(СвойстваОрганизации.УчетнаяЗаписьОбмена));
		
	КонецЕсли;
	
	Если ПолучитьОсновныеСвойстваПоследнейОтправки Тогда
		
		СвойстваОтправки = ПолучитьОсновныеСвойстваПоследнейОтправкиОтчетаВФТС(ОтчетСсылка);
		Если СвойстваОтправки <> Неопределено Тогда
			Результат.СвойстваОтправки = Новый Структура("Ссылка, Идентификатор, ИмяФайлаВыгрузки, Статус",
				СвойстваОтправки.Ссылка, СвойстваОтправки.Идентификатор, СвойстваОтправки.ИмяФайлаВыгрузки, СвойстваОтправки.Статус);
		КонецЕсли;
		
	КонецЕсли;
	
	Результат.НастройкиОбмена = НастройкиФТС(ОрганизацияСсылка);
	
	Результат.ПравоИзмененияУчетнойЗаписи = ПравоДоступа("Изменение", Метаданные.Справочники.УчетныеЗаписиДокументооборота);
	
	Если ОтчетСсылка <> Неопределено ИЛИ ОтправкаСсылка <> Неопределено Тогда
		
		СсылкаНаОтчет = ?(ОтчетСсылка <> Неопределено, ОтчетСсылка, ОтправкаСсылка.ОтчетСсылка);
		ТипЗнчСсылкаНаОтчет = ТипЗнч(СсылкаНаОтчет);
		Результат.ЭтоРегламентированныйОтчет = (ТипЗнчСсылкаНаОтчет = Тип("ДокументСсылка.РегламентированныйОтчет") ИЛИ ТипЗнчСсылкаНаОтчет = Тип("Неопределено"));
		
	КонецЕсли;
	
	Возврат Результат;
	
КонецФункции

Процедура ЗаписатьЛогинИПарольПорталаФТС(Организация, Логин = Неопределено, Пароль = "")
	
	Попытка
		
		МенеджерЗаписи = РегистрыСведений.НастройкиОбменаФТС.СоздатьМенеджерЗаписи();
		МенеджерЗаписи.Организация = Организация;
		МенеджерЗаписи.Прочитать();
		
		Если МенеджерЗаписи.Выбран() Тогда
			Если МенеджерЗаписи.ИспользоватьОбмен <> Истина Тогда
				МенеджерЗаписи.ИспользоватьОбмен = Истина;
				МенеджерЗаписи.ИспользоватьАвтонастройку = Ложь;
			КонецЕсли;
			Если Логин <> Неопределено Тогда
				МенеджерЗаписи.Логин = Логин;
			КонецЕсли;
			МенеджерЗаписи.Пароль = Пароль;
			МенеджерЗаписи.Записать();
			
		Иначе
			МенеджерЗаписи.Организация = Организация;
			МенеджерЗаписи.ИспользоватьОбмен = Истина;
			МенеджерЗаписи.ИспользоватьАвтонастройку = Ложь;
			МенеджерЗаписи.Логин = Логин;
			МенеджерЗаписи.Пароль = Пароль;
			МенеджерЗаписи.Записать();
		КонецЕсли;
		
	Исключение
	КонецПопытки;
	
КонецПроцедуры

Функция УспешноОбработанОтчетФТС(
	СтрокаСостояния,
	СтрокаРасширенногоСостоянияНаКонтроле,
	ВерсияПортала,
	ОтчетНаКонтроле = Ложь,
	ОтчетАннулирован = Ложь,
	ОтказаноВАннулировании = Ложь,
	ЭтоЧерновик = Ложь)
	
	СтрокаРасширенногоСостоянияНаКонтролеНРег = нрег(СтрокаРасширенногоСостоянияНаКонтроле);
	ОтчетНаКонтроле = СтрНайти(СтрокаСостояния, "контроле") > 0 ИЛИ СтрНайти(СтрокаРасширенногоСостоянияНаКонтролеНРег, "контроле") > 0
		ИЛИ ВерсияПортала = "2017" И СтрНайти(СтрокаРасширенногоСостоянияНаКонтролеНРег, "true") > 0;
	Если ОтчетНаКонтроле Тогда
		ОтчетАннулирован = Ложь;
		ОтказаноВАннулировании = Ложь;
		Возврат Ложь;
		
	Иначе
		ОтчетАннулирован = (СтрокаСостояния = "Аннулирована");
		Если ОтчетАннулирован Тогда
			ОтказаноВАннулировании = Ложь;
			Возврат Ложь;
			
		ИначеЕсли СтрНайти(СтрокаСостояния, "Отказано") > 0 Тогда
			ОтказаноВАннулировании = (СтрНайти(СтрокаСостояния, "аннулировании") > 0);
			Возврат Ложь;
			
		ИначеЕсли СтрокаСостояния = "Зарегистрирована" ИЛИ СтрокаСостояния = "Подготовка к аннулированию" Тогда
			Возврат Истина;
			
		Иначе
			ЭтоЧерновик = (СтрокаСостояния = "Новая статформа" ИЛИ СтрокаСостояния = "Черновик");
			Возврат Неопределено;
		КонецЕсли;
	КонецЕсли;
	
КонецФункции

Функция ДополнитьДанныеОрганизацииДаннымиПоОтветственнымЛицам(СтруктураРеквизитов) Экспорт
	
	Если СтруктураРеквизитов.Организация <> Неопределено Тогда
		ДанныеОрганизации = СтруктураРеквизитов.СтруктураДанныхОрганизации;
		
		ДанныеИсполнителей = ЭлектронныйДокументооборотСКонтролирующимиОрганамиВызовСервераПереопределяемый.ПолучитьДанныеОтветственныхЛиц(СтруктураРеквизитов.Организация, Истина);
		ДолжностьРук = "";
		ДолжностьБух = "";
		
		Если ДанныеИсполнителей <> Неопределено И ДанныеИсполнителей.Количество() > 0 Тогда
			ДанныеРук = ДанныеИсполнителей.Получить("Руководитель");
			ДанныеБух = ДанныеИсполнителей.Получить("ГлавныйБухгалтер");
		КонецЕсли;
		
		Если НЕ ПустаяСтрока(ДанныеОрганизации.ФИОРук) Тогда
			
			СтруктураФИО = Новый Структура();
			
			СтруктураФИО.Вставить("Фамилия", 	ДанныеОрганизации.ФамилияРук);
			СтруктураФИО.Вставить("Имя", 		ДанныеОрганизации.ИмяРук);
			СтруктураФИО.Вставить("Отчество", 	ДанныеОрганизации.ОтчествоРук);
			
			ДанныеОрганизации.Вставить("СтруктураФИОРук",СтруктураФИО);
			Если ДанныеРук <> Неопределено Тогда
				ДанныеОрганизации.Вставить("ДолжностьРук", ДанныеРук.Должность);
				ДанныеОрганизации.Вставить("СНИЛСРук", ДанныеРук.СНИЛС);
			КонецЕсли;
			
		КонецЕсли;
		
		Если НЕ ПустаяСтрока(ДанныеОрганизации.ФИОБух) Тогда
			
			Если ЗначениеЗаполнено(ДанныеОрганизации.ФамилияБух) ИЛИ ЗначениеЗаполнено(ДанныеОрганизации.ИмяБух)
			ИЛИ ЗначениеЗаполнено(ДанныеОрганизации.ОтчествоБух) Тогда
				СтруктураФИО = Новый Структура();

				СтруктураФИО.Вставить("Фамилия", 	ДанныеОрганизации.ФамилияБух);
				СтруктураФИО.Вставить("Имя", 		ДанныеОрганизации.ИмяБух);
				СтруктураФИО.Вставить("Отчество", 	ДанныеОрганизации.ОтчествоБух);
			Иначе
				СтруктураФИО = РегламентированнаяОтчетность.РазложитьФИО(ДанныеОрганизации.ФИОБух);
			КонецЕсли;
			
			ДанныеОрганизации.Вставить("СтруктураФИОБух",СтруктураФИО);
			Если ДанныеБух <> Неопределено Тогда
				ДанныеОрганизации.Вставить("ДолжностьБух", ДанныеБух.Должность);
				ДанныеОрганизации.Вставить("СНИЛСБух", ДанныеБух.СНИЛС);
			КонецЕсли;
			
		КонецЕсли;
		СтруктураРеквизитов.Вставить("СтруктураДанныхОрганизации",ДанныеОрганизации);
	КонецЕсли;
	
	Возврат СтруктураРеквизитов;
	
КонецФункции

Процедура ЗаполнитьДанныеОрганизации(СтруктураРеквизитов) Экспорт
	
	Если СтруктураРеквизитов.Организация <> Неопределено Тогда
		
		Организация = СтруктураРеквизитов.Организация;
		
		РеквизитыОрганизации = "НаимЮЛСокр,ОГРН,КодОрганаФСГС,КодПодчиненностиФСС,ТелОрганизации, 
		|АдресЭлПочтыИсп,НаимЮЛПол,ИННЮЛ,КППЮЛ,КодОрганаПФР,КодНО, НаимГоловнОрг,
		|АдрФакт ,АдрЮР, АдрМЖ,АдрПрописки,
		|АдрФакт_XML ,АдрЮР_XML, АдрМЖ_XML, АдрПрописки_XML,
		|АдрФакт_JSON ,АдрЮР_JSON, АдрМЖ_JSON, АдрПрописки_JSON,
		|РегНомПФР, РегНомПФРЗаСебя, РегистрационныйНомерФСС, РегистрационныйНомерФССДобрСтрах,
		|БанкСчетНомер, БанкСчетНаимБанка, БанкСчетКоррСчетБанка, БанкСчетБИКБанка,";
		
		РеквизитыРуководителя = "ФИОРук, ФамилияРук, ИмяРук, ОтчествоРук, НомерУдЛичнРук, СерияУдЛичнРук, 
		|ВидУдЛичнРук, ДатаУдЛичнРук, ОрганВыданУдЛичнРук,
		|ДатаРождРук, МестоРождРук, ПолРук, КодПодрУдЛичнРук, ГраждРук, ТелРук,";
		
		РеквизитыБухгалтера = "ФИОБух, ФамилияБух, ИмяБух, ОтчествоБух, НомерУдЛичнБух, СерияУдЛичнБух, 
		|ВидУдЛичнБух, ДатаУдЛичнБух, ОрганВыданУдЛичнБух,
		|ДатаРождБух, МестоРождБух, ПолБух, КодПодрУдЛичнБух, ГраждБух, ТелБух";
		
		ЗапрашиваемыеРеквизиты = РеквизитыОрганизации + РеквизитыРуководителя + РеквизитыБухгалтера;
		
		ДанныеОрганизации = РегламентированнаяОтчетностьВызовСервера.ПолучитьСведенияОбОрганизации(
			Организация,
			,
			ЗапрашиваемыеРеквизиты);
			
		ДанныеОрганизации.Вставить("Организация", Организация);
		
		Если Метаданные.Справочники.Организации.Реквизиты.Найти("НаименованиеСокращенное") = Неопределено Тогда
			КраткоеНаименованиеОрганизации = ДанныеОрганизации.НаимЮЛСокр;
			Если НЕ ЗначениеЗаполнено(КраткоеНаименованиеОрганизации) Тогда
				КраткоеНаименованиеОрганизации = СокрЛП(Организация.Наименование);
			КонецЕсли;
		Иначе
			КраткоеНаименованиеОрганизации = СокрЛП(Организация.НаименованиеСокращенное);
		КонецЕсли;
		ДанныеОрганизации.Вставить("КраткоеНаименование", КраткоеНаименованиеОрганизации);
		
		// Номер основной поставки 1с
		ДанныеОрганизации.Вставить("НомерОсновнойПоставки1с", ПолучитьНомерОсновнойПоставки1с());
		
		Если НЕ ПустаяСтрока(ДанныеОрганизации.ВидУдЛичнБух) Тогда
			ДанныеОрганизации.Вставить("ВидУдЛичнБух", ПолучитьВидДокументаПоНаименованию(ДанныеОрганизации.ВидУдЛичнБух));
		КонецЕсли;
		Если НЕ ПустаяСтрока(ДанныеОрганизации.ВидУдЛичнРук) Тогда
			ДанныеОрганизации.Вставить("ВидУдЛичнРук", ПолучитьВидДокументаПоНаименованию(ДанныеОрганизации.ВидУдЛичнРук));
		КонецЕсли;
		
		// Юр. лицо или нет
		ЭтоЮрЛицо = ЭлектронныйДокументооборотСКонтролирующимиОрганамиВызовСервера.ЭтоЮрЛицо(Организация);
		ДанныеОрганизации.Вставить("ТипОрганизации",ЭтоЮрЛицо);
		
		Если ЭтоЮрЛицо Тогда 
			Если Метаданные.Справочники.Организации.Реквизиты.Найти("ОбособленноеПодразделение") = Неопределено Тогда
				ПризнакОбособленногоПодразделения = НЕ Организация.ГоловнаяОрганизация.Ссылка = Справочники.Организации.ПустаяСсылка();
			Иначе
				ПризнакОбособленногоПодразделения = Организация.ОбособленноеПодразделение;
			КонецЕсли;
			ДанныеОрганизации.Вставить("ПризнакОбособленногоПодразделения", ПризнакОбособленногоПодразделения);
		Иначе
			ПризнакОбособленногоПодразделения = Ложь;
			
			// За себя и за сотрудников.
			ИПИспользуетТрудНаемныхРаботников = РегламентированнаяОтчетность.ИПИспользуетТрудНаемныхРаботников(Организация);  
			Если НЕ ИПИспользуетТрудНаемныхРаботников Тогда
				ДанныеОрганизации.Вставить("РегНомПФР", ДанныеОрганизации.РегНомПФРЗаСебя); 
				ДанныеОрганизации.Вставить("РегистрационныйНомерФСС", ДанныеОрганизации.РегистрационныйНомерФССДобрСтрах);
			КонецЕсли;
			
		КонецЕсли;
		
		ДанныеОрганизации.Вставить("РеквизитДопКодФСС","");
		
		// Электронная почта.
		ЭлектроннаяПочта = УправлениеКонтактнойИнформацией.КонтактнаяИнформацияОбъекта(
			Организация,
			Справочники.ВидыКонтактнойИнформации.EmailОрганизации,
			ТекущаяДатаСеанса());
			
		ДанныеОрганизации.Вставить("ЭлектроннаяПочта", ЭлектроннаяПочта);
		
		ЗаполнитьАдресаОрганизации_ВСтаромФормате(СтруктураРеквизитов, ДанныеОрганизации);
		ЗаполнитьАдресаОрганизации_ВНовомФормате(СтруктураРеквизитов, ДанныеОрганизации);
			
	КонецЕсли;
		
	СтруктураРеквизитов.Вставить("СтруктураДанныхОрганизации",ДанныеОрганизации);
	
	Если СтруктураРеквизитов.ПриОткрытии Тогда
		ЗаполнитьДанныеСпецоператораСвязи(СтруктураРеквизитов);
	КонецЕсли;
	
КонецПроцедуры

Процедура ЗаполнитьАдресаОрганизации_ВСтаромФормате(СтруктураРеквизитов, ДанныеОрганизации)
	
	Если НЕ СтруктураРеквизитов.Свойство("ПолучитьАдрес") тогда
			
		Если СтруктураРеквизитов.ПриОткрытии И 
			НЕ ПустаяСтрока(СтруктураРеквизитов.АдресЮридический) 
			И НЕ ПустаяСтрока(СтруктураРеквизитов.АдресФактический) Тогда
			
			АдресЮридический = СтруктураРеквизитов.АдресЮридический;
			АдресФактический = СтруктураРеквизитов.АдресФактический;
			
		Иначе
			Если ДанныеОрганизации.ТипОрганизации Тогда
				АдресЮридический = ДанныеОрганизации.АдрЮР;
				АдресФактический = ДанныеОрганизации.АдрФакт;
			Иначе
				АдресЮридический = ДанныеОрганизации.АдрМЖ;
				АдресФактический = ДанныеОрганизации.АдрПрописки;
				
				// БЗ 10194146 ЗУП
				// В ЗУП 3.1.6.6 при отправке заявления на изменение для индивидуального предпринимателя возникает ошибка:
				// "Юридический адрес: не указан регион",
				// если в свойствах организации не выбрано физическое лицо или у физического лица не задан адрес места проживания,
				// при этом в свойствах организации юридический адрес указан.
				
				Если АдресЮридический = ",,,,,,,,," Тогда
					АдресЮридический = ДанныеОрганизации.АдрЮР;
				КонецЕсли;
				
				Если АдресФактический = ",,,,,,,,," Тогда
					АдресФактический = ДанныеОрганизации.АдрПрописки;
				КонецЕсли;
				
			КонецЕсли;
		КонецЕсли;
		
		Если НЕ СтруктураРеквизитов.ПриОткрытии 
			ИЛИ ПустаяСтрока(СтруктураРеквизитов.АдресЮридический) И ПустаяСтрока(СтруктураРеквизитов.АдресФактический) Тогда
			
			СтруктураРеквизитов.Вставить("АдресЮридический",РегламентированнаяОтчетностьВызовСервера.ЗаменитьТекстРегионаНаКодРегиона(АдресЮридический));
			СтруктураРеквизитов.Вставить("АдресФактический",РегламентированнаяОтчетностьВызовСервера.ЗаменитьТекстРегионаНаКодРегиона(АдресФактический));
			
		КонецЕсли;
		
		ЮрАдресВФормате9Запятых = РегламентированнаяОтчетностьКлиентСервер.ПредставлениеАдресаВФормате9Запятых(
			АдресЮридический,
			Истина);
			
		СтруктураРеквизитов.Вставить("ЮрАдрес", ЮрАдресВФормате9Запятых);
		
		ФактическийАдресВФормате9Запятых = РегламентированнаяОтчетностьКлиентСервер.ПредставлениеАдресаВФормате9Запятых(
			АдресФактический,
			Истина);
			
		СтруктураРеквизитов.Вставить("ФактАдрес", ФактическийАдресВФормате9Запятых);
		
	КонецЕсли;
		
КонецПроцедуры

Процедура ЗаполнитьАдресаОрганизации_ВНовомФормате(СтруктураРеквизитов, ДанныеОрганизации)
	
	ЭтоЮрЛицо = ДанныеОрганизации.ТипОрганизации;
	Если ЭтоЮрЛицо Тогда
		
		АдресЮридический = АдресИзДанныхОрганизации(ДанныеОрганизации, "АдрЮР");
		АдресФактический = АдресИзДанныхОрганизации(ДанныеОрганизации, "АдрФакт");
		
	Иначе
		
		АдресЮридический = АдресИзДанныхОрганизации(ДанныеОрганизации, "АдрМЖ");
		АдресФактический = АдресИзДанныхОрганизации(ДанныеОрганизации, "АдрПрописки");
		
		// БЗ 10194146 ЗУП
		// В ЗУП 3.1.6.6 при отправке заявления на изменение для индивидуального предпринимателя возникает ошибка:
		// "Юридический адрес: не указан регион",
		// если в свойствах организации не выбрано физическое лицо или у физического лица не задан адрес места проживания,
		// при этом в свойствах организации юридический адрес указан.
		
		Если АдресЮридический.XML = "" Тогда
			АдресЮридический = АдресИзДанныхОрганизации(ДанныеОрганизации, "АдрЮР");
		КонецЕсли;
		
		Если АдресФактический.XML = "" Тогда
			АдресФактический = АдресИзДанныхОрганизации(ДанныеОрганизации, "АдрФакт");
		КонецЕсли;
		
	КонецЕсли;
	
	ПреобразоватьАдресВФорматXML(АдресЮридический, Справочники.ВидыКонтактнойИнформации.ЮрАдресОрганизации);
		
	СтруктураРеквизитов.Вставить("АдресЮридическийЗначение", 		АдресЮридический.Значение);
	СтруктураРеквизитов.Вставить("АдресЮридическийПредставление",	АдресЮридический.Представление);
	
	ПреобразоватьАдресВФорматXML(АдресФактический, Справочники.ВидыКонтактнойИнформации.ФактАдресОрганизации);
		
	СтруктураРеквизитов.Вставить("АдресФактическийЗначение", 		АдресФактический.Значение);
	СтруктураРеквизитов.Вставить("АдресФактическийПредставление", 	АдресФактический.Представление);
	
КонецПроцедуры

Функция АдресИзСкопированногоЗаявления(Реквизит) Экспорт

	// Заполнение юр. адреса
	ПараметрыАдреса = ПараметыАдресаМастера();
	ПараметрыАдреса.Строка   = Реквизит.АдресЮридический;
	ПараметрыАдреса.XML      = Реквизит.АдресЮридический;
	
	ПреобразоватьАдресВФорматXML(ПараметрыАдреса, Справочники.ВидыКонтактнойИнформации.ЮрАдресОрганизации);
	
	АдресЮридическийЗначение      = ПараметрыАдреса.Значение;
	АдресЮридическийПредставление = ПараметрыАдреса.Представление;
	
	// Заполнение факт. адреса
	ПараметрыАдреса = ПараметыАдресаМастера();
	ПараметрыАдреса.Строка   = Реквизит.АдресФактический;
	ПараметрыАдреса.XML      = Реквизит.АдресФактический;
	
	ПреобразоватьАдресВФорматXML(ПараметрыАдреса, Справочники.ВидыКонтактнойИнформации.ФактАдресОрганизации);
	
	АдресФактическийЗначение      = ПараметрыАдреса.Значение;
	АдресФактическийПредставление = ПараметрыАдреса.Представление;
	
	ДополнительныеПараметры = Новый Структура();
	ДополнительныеПараметры.Вставить("АдресЮридическийЗначение", АдресЮридическийЗначение);
	ДополнительныеПараметры.Вставить("АдресЮридическийПредставление", АдресЮридическийПредставление);
	ДополнительныеПараметры.Вставить("АдресФактическийЗначение", АдресФактическийЗначение);
	ДополнительныеПараметры.Вставить("АдресФактическийПредставление", АдресФактическийПредставление);
	
	Возврат ДополнительныеПараметры;
	
КонецФункции

Функция КодРегионаВМастереПоАдресу(Адрес) Экспорт

	ЭтоАдресПоФИАСу = УправлениеКонтактнойИнформациейКлиентСервер.ЭтоКонтактнаяИнформацияВXML(Адрес);
	Если ЭтоАдресПоФИАСу Тогда
		Возврат КодРегионаПоАдресуФИАССервер(Адрес); 
	Иначе
		Возврат РегламентированнаяОтчетностьКлиентСервер.РазложитьАдрес(Адрес).Регион; 
	КонецЕсли;

КонецФункции

Функция КодРегионаПоАдресуФИАССервер(Адрес)
	
	НазваниеРегиона = РаботаСАдресами.РегионАдресаКонтактнойИнформации(Адрес);
	Возврат РегламентированнаяОтчетностьВызовСервера.КодРегионаПоНазванию(НазваниеРегиона);
	
КонецФункции

Функция АдресXML_ДобавитьЭлемент(МойДокументDOM, ИмяЭлемента, НовоеЗначение, ИмяРодительскогоЭлемента = "")
	
	УзелСоставНижний = АдресXML_УзелСоставНижний(МойДокументDOM);
	
	Если ЗначениеЗаполнено(ИмяРодительскогоЭлемента) Тогда
		Элементы = УзелСоставНижний.ПолучитьЭлементыПоИмени(ИмяРодительскогоЭлемента);
		Если Элементы.Количество() = 0 Тогда 
			РодительскийЭлемент = АдресXML_НовыйЭлемент(МойДокументDOM, ИмяРодительскогоЭлемента);
			УзелСоставНижний.ДобавитьДочерний(РодительскийЭлемент);
		Иначе
			РодительскийЭлемент = Элементы[0];
		КонецЕсли;
	Иначе
		РодительскийЭлемент = УзелСоставНижний;
	КонецЕсли;
	
	НовыйЭлемент = АдресXML_НовыйЭлемент(МойДокументDOM, ИмяЭлемента, НовоеЗначение);
	РодительскийЭлемент.ДобавитьДочерний(НовыйЭлемент);

КонецФункции

Функция ЗамененаЧастьАдреса(МойДокументDOM, ИмяЭлемента, НовоеЗначение, ИмяРодительскогоЭлемента = "")
	
	БылоИзменение = Ложь;
	
	Если ЗначениеЗаполнено(НовоеЗначение) Тогда
		
		Элементы  = МойДокументDOM.ПолучитьЭлементыПоИмени(ИмяЭлемента);
		
		Если Элементы.Количество() > 0 Тогда
			БылоИзменение = АдресXML_УстановитьНовоеЗначение(Элементы, НовоеЗначение);
		Иначе
			БылоИзменение = Истина;
			АдресXML_ДобавитьЭлемент(МойДокументDOM, ИмяЭлемента, НовоеЗначение, ИмяРодительскогоЭлемента);
		КонецЕсли;
		
	КонецЕсли;
	
	Возврат БылоИзменение;
	
КонецФункции

Функция АдресXML_УстановитьНовоеЗначение(Элементы, НовоеЗначение)
	
	БылоИзменение = Ложь;
	
	Для каждого Элемент Из Элементы Цикл
		Если Элемент.ТекстовоеСодержимое <> НовоеЗначение И НЕ ЗначениеЗаполнено(Элемент.ТекстовоеСодержимое) Тогда
			Элемент.ТекстовоеСодержимое = НовоеЗначение;
			БылоИзменение = Истина;
		КонецЕсли;
	КонецЦикла;
	
	Возврат БылоИзменение;
	
КонецФункции

Функция АдресXML_НовыйЭлемент(МойДокументDOM, Имя, Значение = "")
	
	Элемент = МойДокументDOM.СоздатьЭлемент(Имя);
	
	Если ЗначениеЗаполнено(Значение) Тогда
		Элемент.ТекстовоеСодержимое = Значение;
	КонецЕсли;
	
	Возврат Элемент;
	
КонецФункции

Функция АдресXML_УзелСоставНижний(МойДокументDOM)
	
	// Узлы "Состав" вложены один в другой. Новый узел добавляем в самый внутренний
	
	//<?xml version="1.0" encoding="UTF-8"?>
	//<КонтактнаяИнформация xmlns="http://www.v8.1c.ru/ssl/contactinfo" xmlns:xs="http://www.w3.org/2001/XMLSchema" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" Представление="142651, Московская обл, Городской округ Орехово-Зуевский, 123, Абрамовка д, 1805 г, дом 1Б, корпус 2">
	//	<Состав xsi:type="Адрес" Страна="РОССИЯ">
	//		<Состав xsi:type="АдресРФ">
	//			<СубъектРФ>Московская обл</СубъектРФ>
	//			<СвРайМО>Городской округ Орехово-Зуевский</СвРайМО>
	//			....
	//		</Состав>
	//	</Состав>
	//</КонтактнаяИнформация>
	
	УзлыСоставВерхний = МойДокументDOM.ПолучитьЭлементыПоИмени("Состав");
	Для каждого УзелСоставВерхний Из УзлыСоставВерхний Цикл
		Узел = УзелСоставВерхний.ПолучитьЭлементыПоИмени("Состав")[0];
		Возврат Узел; 
	КонецЦикла;
	
	Возврат Неопределено;

КонецФункции

Функция DOMИзXML(XML) Экспорт

	ИмяФайла = ПолучитьИмяВременногоФайла();
	
	ЗаписьТекста = Новый ЗаписьТекста(ИмяФайла);
	ЗаписьТекста.Записать(XML);
	ЗаписьТекста.Закрыть();
	
	ДокументDOM = DOMИзXMLФайла(ИмяФайла);
	
	Возврат ДокументDOM; 
	
КонецФункции

Функция DOMИзXMLФайла(ИмяФайла) Экспорт
	
	ОбъектXML = Новый ЧтениеXML;
	ОбъектXML.ОткрытьФайл(ИмяФайла);
	ПостроительDOM = Новый ПостроительDOM;
	ДокументDOM    = ПостроительDOM.Прочитать(ОбъектXML);
	ОбъектXML.Закрыть();
	
	Возврат ДокументDOM; 
	
КонецФункции

Функция JSONИзСтроки(Строка) Экспорт
	
	ЧтениеJSON = Новый ЧтениеJSON;
	ЧтениеJSON.УстановитьСтроку(Строка);
	Данные = ПрочитатьJSON(ЧтениеJSON);
	ЧтениеJSON.Закрыть();
	
	Возврат Данные; 
	
КонецФункции

Процедура УбратьПробелыИзАдреса(ДанныеАдреса)
	
	Для каждого Запись Из ДанныеАдреса Цикл
		
		Значение = Запись.Значение;
		Если ТипЗнч(Значение) = Тип("Строка") Тогда
			
			ДанныеАдреса[Запись.Ключ] = СокрЛП(Запись.Значение);
			
		ИначеЕсли ТипЗнч(Значение) = Тип("Массив") Тогда
			
			Помещения = Запись.Значение;
			Для каждого Помещение Из Помещения Цикл
				Для каждого СвойстваПомещения Из Помещение Цикл
					Помещение[СвойстваПомещения.Ключ] = СокрЛП(СвойстваПомещения.Значение);
				КонецЦикла;
			КонецЦикла; 
			
		КонецЕсли;
	
	КонецЦикла;
	
КонецПроцедуры

Функция УбратьПробелыИзАдресов(АдресXML, АдресJSON) Экспорт
	
	Попытка
	
		// Преобразуем XML
		ДанныеАдреса = РаботаСАдресами.СведенияОбАдресе(АдресXML);
		УбратьПробелыИзАдреса(ДанныеАдреса);
		ДанныеАдреса.МуниципальноеПредставление = ДанныеАдреса.Представление; // Иначе сбрасывается
		Адрес = РаботаСАдресами.ПоляАдресаВJSON(ДанныеАдреса);
		АдресXML = УправлениеКонтактнойИнформацией.КонтактнаяИнформацияВXML(Адрес);
		
		// Преобразуем JSON
		Если ЗначениеЗаполнено(АдресJSON) Тогда
			ДанныеАдреса = РаботаСАдресами.СведенияОбАдресе(АдресJSON);
			ДанныеАдреса.Представление = ДанныеАдреса.МуниципальноеПредставление; // Иначе сбрасывается
			УбратьПробелыИзАдреса(ДанныеАдреса);
			АдресJSON = РаботаСАдресами.ПоляАдресаВJSON(ДанныеАдреса);
		КонецЕсли;
		
	Исключение
		
		ЗаписьЖурналаРегистрации(
			НСтр("ru = 'Электронный документооборот с контролирующими органами'", ОбщегоНазначения.КодОсновногоЯзыка()),
			УровеньЖурналаРегистрации.Ошибка,,,
			ПодробноеПредставлениеОшибки(ИнформацияОбОшибке()));
	
	КонецПопытки;
	
КонецФункции

Функция СкорректироватьАдресПоМуниципальному(АдресXML, АдресJSON) Экспорт
	
	УбратьПробелыИзАдресов(АдресXML, АдресJSON);
	
	Если НЕ ЗначениеЗаполнено(АдресJSON) Тогда
		Возврат АдресXML;
	КонецЕсли;
	
	СведенияОбАдресе = РаботаСАдресами.СведенияОбАдресе(АдресJSON);

	МуниципальныйРайон = СведенияОбАдресе.МуниципальныйРайон;
	Поселение = СведенияОбАдресе.Поселение;
	
	Если НЕ ЗначениеЗаполнено(МуниципальныйРайон) И НЕ ЗначениеЗаполнено(Поселение) Тогда
		// Это адрес в административно территориальном делении или пустой
		Возврат АдресXML;
	КонецЕсли;
	
	ИмяФайла = ПолучитьИмяВременногоФайла();
	
	ЗаписьТекста = Новый ЗаписьТекста(ИмяФайла);
	ЗаписьТекста.Записать(АдресXML);
	ЗаписьТекста.Закрыть();
	
	ДокументDOM = DOMИзXMLФайла(ИмяФайла);
	
	БылоИзменение1 = ЗамененаЧастьАдреса(ДокументDOM, "Район", МуниципальныйРайон, "СвРайМО");
	БылоИзменение2 = ЗамененаЧастьАдреса(ДокументDOM, "Город", Поселение);
	
	Если НЕ БылоИзменение1 И НЕ БылоИзменение2 Тогда
		Возврат АдресXML;
	КонецЕсли;
	
	ЗаписьXML = Новый ЗаписьXML;
	ЗаписьXML.ОткрытьФайл(ИмяФайла);
	ЗаписьDOM = Новый ЗаписьDOM;
	ЗаписьDOM.Записать(ДокументDOM, ЗаписьXML);
	ЗаписьXML.Закрыть();
	
	ЧтениеТекста = Новый ЧтениеТекста(ИмяФайла);
	АдресXML = ЧтениеТекста.Прочитать();
	ЧтениеТекста.Закрыть();

	Шапка = НСтр("ru = '<?xml version=""1.0"" encoding=""UTF-8""?>'");
	АдресXML = СокрЛП(СтрЗаменить(АдресXML, Шапка, ""));
	
	Возврат АдресXML;
		
КонецФункции

Функция ПараметыАдресаМастера() Экспорт
	
	Адрес = Новый Структура();
	Адрес.Вставить("Строка", "");
	Адрес.Вставить("XML", "");
	Адрес.Вставить("JSON", "");
	Адрес.Вставить("Значение", "");
	Адрес.Вставить("Представление", "");
	
	Возврат Адрес;
	
КонецФункции

Функция АдресИзДанныхОрганизации(ДанныеОрганизации, ИмяПоля)

	Адрес = ПараметыАдресаМастера();
	
	ИмяПоляСXML = ИмяПоля + "_XML";
	
	Если ЗначениеЗаполнено(ДанныеОрганизации[ИмяПоляСXML]) Тогда
		
		ИмяПоляСJSON = ИмяПоля + "_JSON";
		
	 	АдресXML   = ДанныеОрганизации[ИмяПоляСXML];
		АдресJSON  = ДанныеОрганизации[ИмяПоляСJSON];
		
		РезультатЗначение = СкорректироватьАдресПоМуниципальному(АдресXML, АдресJSON);
		
		Адрес.Вставить("Значение", РезультатЗначение);
		Адрес.Вставить("XML", АдресXML);
		Адрес.Вставить("JSON", АдресJSON);

	ИначеЕсли ДанныеОрганизации[ИмяПоля] = ",,,,,,,,," Тогда
		
		АдресСтрокой = "";
		Адрес.Вставить("Строка", АдресСтрокой);
		
	Иначе
		
		АдресСтрокой = ДанныеОрганизации[ИмяПоля];
		АдресСтрокой = РегламентированнаяОтчетностьВызовСервера.ЗаменитьТекстРегионаНаКодРегиона(АдресСтрокой);
		
		Адрес.Вставить("Строка", АдресСтрокой);
		
	КонецЕсли;
	
	Возврат Адрес;

КонецФункции

Функция ПредставлениеАдресаИзДанныхОрганизации(ЗначениеАдреса) Экспорт
	
	ЭтоАдресПоФИАСу = УправлениеКонтактнойИнформациейКлиентСервер.ЭтоКонтактнаяИнформацияВXML(ЗначениеАдреса); 
	
	Если НЕ ЗначениеЗаполнено(ЗначениеАдреса) Тогда
		ПредставлениеАдреса = "";
	ИначеЕсли ЭтоАдресПоФИАСу Тогда
		ПредставлениеАдреса = ПредставлениеКонтактнойИнформации(ЗначениеАдреса);
	Иначе
		ПредставлениеАдреса = РегламентированнаяОтчетностьКлиентСервер.ПредставлениеАдресаВФормате9Запятых(ЗначениеАдреса, Истина);
	КонецЕсли;
	
	Возврат ПредставлениеАдреса;
	
КонецФункции

Функция ПредставлениеКонтактнойИнформации(ЗначениеАдреса) Экспорт
	
	Попытка
		ПредставлениеАдреса = УправлениеКонтактнойИнформацией.ПредставлениеКонтактнойИнформации(ЗначениеАдреса);
	Исключение
		
		ЗаписьЖурналаРегистрации(
			НСтр("ru = 'Электронный документооборот с контролирующими органами'", ОбщегоНазначения.КодОсновногоЯзыка()),
			УровеньЖурналаРегистрации.Ошибка,,,
			ПодробноеПредставлениеОшибки(ИнформацияОбОшибке()));
			
		ПредставлениеАдреса = "";

	КонецПопытки;
	
	Возврат ПредставлениеАдреса;
	
КонецФункции

Функция АдресФИАСДляТелаЗаявления(ДокументЗаявление, ИмяРеквизита) Экспорт
	
	// Пример адреса:
	// В нем может быть два тэга <Состав; тэга <СубъектРФ> может не быть
	//
	//<КонтактнаяИнформация xmlns="http://www.v8.1c.ru/ssl/contactinfo" xmlns:xs="http://www.w3.org/2001/XMLSchema" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" Представление="368302, Дагестан Респ, Каспийск г, Кирова ул, дом № 55">
	//	<Состав xsi:type="Адрес" Страна="РОССИЯ">
	//		<Состав xsi:type="АдресРФ">
	//			<СубъектРФ>Дагестан Респ</СубъектРФ>
	//			<Город>Каспийск г</Город>
	//			<Улица>Кирова ул</Улица>
	//			<ДопАдрЭл ТипАдрЭл="10100000" Значение="368302"/>
	//			<ДопАдрЭл>
	//				<Номер Тип="1010" Значение="55"/>
	//			</ДопАдрЭл>
	//		</Состав>
	//	</Состав>
	//</КонтактнаяИнформация>

	Адрес = ДокументЗаявление[ИмяРеквизита];
	
	РаботаСАдресами.ПередЗаписьюXDTOКонтактнаяИнформация(Адрес);
	
	// Ищем вхождение второго тэга <Состав
	ПозицияНачала 	 = СтрНайти(Адрес, "<Состав", НаправлениеПоиска.СКонца);
	// Ищем начала любого следующего тэга, который и будем началом адреса
	ПозицияНачала 	 = СтрНайти(Адрес, "<",,ПозицияНачала + 1);
	ПозицияОкончания = Найти(Адрес, "</Состав>");
	АдресДляЗаявления = Сред(Адрес, ПозицияНачала, ПозицияОкончания - ПозицияНачала);
	АдресДляЗаявления = ИсправитьОКТМО(АдресДляЗаявления);
	 
	ИдентификаторАдреса = ДокументЗаявление[ИмяРеквизита + "ИдентификаторПоФИАС"];
	Если ЗначениеЗаполнено(ИдентификаторАдреса) Тогда
		АдресДляЗаявления = АдресДляЗаявления 
			+ "<ИдФиас>"
			+ ИдентификаторАдреса
			+ "</ИдФиас>";
	КонецЕсли;
			
	Возврат АдресДляЗаявления;
		
КонецФункции

Функция ИсправитьОКТМО(Адрес)
	
	ОКТМОНачало = "<ОКТМО>";
	ОКТМОКонец = "</ОКТМО>";
	
	НачалоТэгаОКТМО = СтрНайти(Адрес, ОКТМОНачало);
	КонецТэгаОКТМО = СтрНайти(Адрес, ОКТМОКонец);
	
	Если НачалоТэгаОКТМО <> 0 И КонецТэгаОКТМО <> 0 Тогда
		
		НачалоОКТМО = НачалоТэгаОКТМО + СтрДлина(ОКТМОНачало);
		КонецОКТМО = КонецТэгаОКТМО;
		
		Длина = КонецОКТМО - НачалоОКТМО;
		ОКТМОИсходный = Сред(Адрес, НачалоОКТМО, Длина);
		
		Если Длина < 8 Тогда
			
			ОКТМО = СтроковыеФункцииКлиентСервер.ДополнитьСтроку(ОКТМОИсходный, 8);
			
		ИначеЕсли Длина > 8 Тогда
			
			ОКТМО = Лев(ОКТМОИсходный, 8);
			
		КонецЕсли;
		
		Если Длина <> 8 Тогда
			
			 ЧтоЗаменять   = ОКТМОНачало + ОКТМОИсходный + ОКТМОКонец;
			 НаЧтоЗаменять = ОКТМОНачало + ОКТМО + ОКТМОКонец;
			 
			 Адрес = СтрЗаменить(Адрес, ЧтоЗаменять, НаЧтоЗаменять);
			
		КонецЕсли;
		
	КонецЕсли;
	
	Возврат Адрес;
	
КонецФункции

Процедура ПреобразоватьАдресВФорматXML(Адрес, ВидКонтактнойИнформации) Экспорт
	
	ЭтоАдресПоФИАСу = УправлениеКонтактнойИнформациейКлиентСервер.ЭтоКонтактнаяИнформацияВXML(Адрес.XML);
	
	Если ЭтоАдресПоФИАСу Тогда
		
		Попытка
			
			Если ЗначениеЗаполнено(Адрес.JSON) Тогда
				Адрес.Представление = РаботаСАдресами.СведенияОбАдресе(Адрес.JSON).Представление;
			Иначе
				Адрес.Представление = РаботаСАдресами.СведенияОбАдресе(Адрес.XML).Представление;
			КонецЕсли;
			Адрес.Значение = Адрес.XML;
			
		Исключение
			
			ТекстОшибки = НСтр("ru = 'Электронный документооборот с контролирующими органами. Мастер по 1С-Отчетности. Получение представления адреса'", 
				ОбщегоНазначения.КодОсновногоЯзыка());
			
			ЗаписьЖурналаРегистрации(
				ТекстОшибки,
				УровеньЖурналаРегистрации.Ошибка,,,
				ПодробноеПредставлениеОшибки(ИнформацияОбОшибке()));
				
		КонецПопытки;
			
	ИначеЕсли ЗначениеЗаполнено(Адрес.Строка) И Адрес.Строка <> ",,,,,,,,," Тогда
		
		// Сначала получаем представление, его преобразуем в xml.
		Адрес.Представление = РегламентированнаяОтчетностьКлиентСервер.ПредставлениеАдресаВФормате9Запятых(Адрес.Строка, Истина);
		
		Попытка
			Адрес.Значение = УправлениеКонтактнойИнформацией.КонтактнаяИнформацияXMLПоПредставлению(Адрес.Представление, ВидКонтактнойИнформации);
		Исключение
			
			ТекстОшибки = НСтр("ru = 'Электронный документооборот с контролирующими органами. Мастер по 1С-Отчетности. Получение XML по представлению адреса'", 
				ОбщегоНазначения.КодОсновногоЯзыка());
			
			ЗаписьЖурналаРегистрации(
				ТекстОшибки,
				УровеньЖурналаРегистрации.Ошибка,,,
				ПодробноеПредставлениеОшибки(ИнформацияОбОшибке()));
				
		КонецПопытки;
		
	КонецЕсли;
	
КонецПроцедуры

Функция ПолеСертификата_2_5_4_8(АдресЮридический) Экспорт
	
	// Наименование субъекта Российской Федерации
	// Приказ ФСБ России от 27.12.2011 № 795
	// 5) stateOrProvinceName (наименование штата или области).
	// В качестве значения данного атрибута имени следует использовать текстовую строку, 
	// содержащую наименование соответствующего субъекта Российской Федерации.
	
	// Для городов федерального значение Москва и Санкт Петербург, город должен писаться в 2.5.4.8
	// Мы записываем вот так -  77 г. Москва, 78 г. Санкт-Петербург,  92 г. Севастополь, 99 г. Байконур
	
	stateOrProvinceName = "";
	
	ЭтоАдресПоФИАСу = УправлениеКонтактнойИнформациейКлиентСервер.ЭтоКонтактнаяИнформацияВXML(АдресЮридический);
	
	Если ЭтоАдресПоФИАСу Тогда	
		
		СведенияОбАдресе 	= СведенияОбАдресе(АдресЮридический);
		КодРегиона 			= СведенияОбАдресе.КодРегиона;
		
		Если ЗначениеЗаполнено(КодРегиона) Тогда
			
			Если ЭтоГородФедеральногоЗначения(СведенияОбАдресе.Регион) Тогда
				
				// Мы записываем вот так -  77 г. Москва, 78 г. Санкт-Петербург,  92 г. Севастополь, 99 г. Байконур
				Регион = ГородФедеральногоЗначенияБезСокращенияРегиона(СведенияОбАдресе);
				stateOrProvinceName = КодРегиона + " г. " + Регион;
				
			Иначе
				stateOrProvinceName = КодИНазваниеРегионаДляСертификата(КодРегиона);
			КонецЕсли;
			
		КонецЕсли;
		
	Иначе
		
		КодРегиона = РазобратьСтроку(АдресЮридический, ",");
		
		Если ЗначениеЗаполнено(КодРегиона) Тогда
			stateOrProvinceName = КодИНазваниеРегионаДляСертификата(КодРегиона);
		КонецЕсли;
		
	КонецЕсли;
	
	Возврат stateOrProvinceName;
	
КонецФункции

Процедура ДобавитьОбразование(ЧастиL, Часть) Экспорт
	
	Если ЗначениеЗаполнено(Часть) Тогда
		ЧастиL.Добавить(Часть);
	КонецЕсли;
	
КонецПроцедуры

Функция ПолеСертификата_2_5_4_7(АдресЮридический) Экспорт
	
	// Наименование населенного пункта
	// Приказ ФСБ России от 27.12.2011 № 795
	// 6) localityName (наименование населенного пункта).
	// В качестве значения данного атрибута имени следует использовать текстовую строку, 
	// содержащую наименование соответствующего населенного пункта.
	
	// Пишем в L город (как в ЕГРЮЛ)
	// 2.5.4.7 - Москва г.
	
	localityName = "";
	
	ЭтоАдресПоФИАСу = УправлениеКонтактнойИнформациейКлиентСервер.ЭтоКонтактнаяИнформацияВXML(АдресЮридический);
	
	Если ЭтоАдресПоФИАСу Тогда
		
		СведенияОбАдресе     = СведенияОбАдресе(АдресЮридический);
		Регион               = СведенияОбАдресе.Регион;
		Район                = СведенияОбАдресе.Район;
		Город                = СведенияОбАдресе.Город;
		ВнутригородскойРайон = СведенияОбАдресе.ВнутригородскойРайон;
		НаселенныйПункт      = СведенияОбАдресе.НаселенныйПункт;
		Территория           = СведенияОбАдресе.Территория;

		
		ЧастиL = Новый Массив;
		ДобавитьОбразование(ЧастиL, Район);
		ДобавитьОбразование(ЧастиL, Город);
		ДобавитьОбразование(ЧастиL, ВнутригородскойРайон);
		ДобавитьОбразование(ЧастиL, НаселенныйПункт);
		ДобавитьОбразование(ЧастиL, Территория);
		
		Пока ЧастиL.Количество() >= 1 Цикл
			Адрес = СтрСоединить(ЧастиL, ", ");
			Если СтрДлина(Адрес) > 128 И ЧастиL.Количество() > 1 Тогда
				ЧастиL.Удалить(0);
			ИначеЕсли СтрДлина(Адрес) > 128 И ЧастиL.Количество() = 1 Тогда
				Адрес = Лев(Адрес, 128);
				Прервать;
			Иначе
				Прервать;
			КонецЕсли;
				
		КонецЦикла;
		
		localityName = Адрес;
		Если НЕ ЗначениеЗаполнено(localityName) 
			И ЭтоГородФедеральногоЗначения(Регион) Тогда
			localityName = Регион;
		КонецЕсли;
			
	Иначе
		
		ДокументМассив       = РазобратьСтроку(АдресЮридический, ",");
		Город  			     = ДокументМассив[4];
		НаселенныйПункт      = ДокументМассив[5];
		
		Если ЗначениеЗаполнено(Город) Тогда
			localityName = Город;
		ИначеЕсли ЗначениеЗаполнено(НаселенныйПункт) Тогда
			localityName = НаселенныйПункт;
		КонецЕсли;
		
	КонецЕсли;

	Возврат localityName;
	
КонецФункции

Функция ГородФедеральногоЗначенияБезСокращенияРегиона(СведенияОбАдресе)
	
	Регион 			 = СведенияОбАдресе.Регион;
	РегионСокращение = СведенияОбАдресе.РегионСокращение;
	
	ДлинаСокращенияРегиона = СтрДлина(РегионСокращение);
	
	Город = СокрЛП(Сред(Регион, 1, СтрДлина(Регион) - ДлинаСокращенияРегиона));
	
	Возврат Город;
	
КонецФункции

Функция ПолеСертификата_2_5_4_9(АдресЮридический) Экспорт
	
	// Адрес места нахождения
	// Приказ ФСБ России от 27.12.2011 № 795
	// 7) streetAddress (название улицы, номер дома).
	// В качестве значения данного атрибута имени следует использовать текстовую строку, 
	// содержащую часть адреса места нахождения соответствующего лица, включающую 
	// - наименование улицы, 
	// - номер дома, 
	// - а также корпуса,
	// - строения,
	// - квартиры,
	// - помещения. 
	// Объектный идентификатор типа атрибута streetAddress имеет вид 2.5.4.9;
	
	streetAddress = "";
	
	ЭтоАдресПоФИАСу = УправлениеКонтактнойИнформациейКлиентСервер.ЭтоКонтактнаяИнформацияВXML(АдресЮридический);
	
	Если ЭтоАдресПоФИАСу Тогда
		
		СведенияОбАдресе = СведенияОбАдресе(АдресЮридический);
		
		ЧастиАдреса = Новый Массив;
		
		// Улица
		Улица = СокрЛП(СведенияОбАдресе.Улица);
		ЧастиАдреса.Добавить(Улица);
		
		// Дом
		Здание = СведенияОбАдресе.Здание;
		Если Здание.Свойство("Номер") И ЗначениеЗаполнено(Здание.Номер) Тогда
			ТипЗдания = СокращеноеНазваниОбъектаАдресации(Здание.ТипЗдания);
			Дом = СокрЛП(ТипЗдания + " " + Здание.Номер);
			ЧастиАдреса.Добавить(Дом);
		КонецЕсли;
		
		Корпуса = СведенияОбАдресе.Корпуса;
		
		// Корпуса
		Для каждого Корпус Из Корпуса Цикл
			Если ЗначениеЗаполнено(Корпус.Номер) Тогда
				ТипКорпуса = СокращеноеНазваниОбъектаАдресации(Корпус.ТипКорпуса);
				Корпус = СокрЛП(ТипКорпуса + " " + Корпус.Номер);
				ЧастиАдреса.Добавить(Корпус);
			КонецЕсли;
		КонецЦикла;
		
		Помещения = СведенияОбАдресе.Помещения;
		
		// Помещения
		Для каждого Помещение Из Помещения Цикл
			Если ЗначениеЗаполнено(Помещение.Номер) Тогда
				ТипПомещения = СокращеноеНазваниОбъектаАдресации(Помещение.ТипПомещения);
				Помещение = СокрЛП(ТипПомещения + " " + Помещение.Номер);
				ЧастиАдреса.Добавить(Помещение);
			КонецЕсли;
		КонецЦикла;
		
		streetAddress = СтрСоединить(ЧастиАдреса, ", ");
		
	Иначе
	
		ЧастиАдреса = РазобратьСтроку(АдресЮридический, ",");
		
		Улица 	 = СокрЛП(ЧастиАдреса[6]);
		Дом 	 = СокрЛП(ЧастиАдреса[7]);
		Корпус 	 = СокрЛП(ЧастиАдреса[8]);
		Квартира = ?(ЧастиАдреса.Количество() = 10, СокрЛП(ЧастиАдреса[9]), "");
		
		ЧастиАдреса = Новый Массив;
		ЧастиАдреса.Добавить(Улица);
		ЧастиАдреса.Добавить(Дом);
		ЧастиАдреса.Добавить(Корпус);
		ЧастиАдреса.Добавить(Квартира);
		
		streetAddress = СтрСоединить(ЧастиАдреса, " ");
		
	КонецЕсли;
	
	Возврат streetAddress;

КонецФункции

Функция КодИНазваниеРегионаДляСертификата(КодРегиона) Экспорт
	
	Если ПустаяСтрока(КодРегиона) Тогда
		Возврат "0";
	КонецЕсли;
	
	КонтекстЭДОСервер = ДокументооборотСКО.ПолучитьОбработкуЭДО();
	МакетРегионы = КонтекстЭДОСервер.ПолучитьМакетОбработки("КодыРегионов353приказ");
	
	Для Инд = 1 По МакетРегионы.ВысотаТаблицы Цикл
		ТекРегион = МакетРегионы.Область(Инд, 1, Инд, 1).Текст;
		
		Если ТекРегион = КодРегиона Тогда
			Возврат КодРегиона + " " + СокрЛП(МакетРегионы.Область(Инд, 2, Инд, 2).Текст);
		КонецЕсли;
		
	КонецЦикла;
	
	Возврат "0";
КонецФункции

Функция СведенияОбАдресе(Адрес) Экспорт
	
	ДополнительныеПараметры = Новый Структура();
	ДополнительныеПараметры.Вставить("НаименованиеВключаетСокращение", Истина);
	
	Возврат РаботаСАдресами.СведенияОбАдресе(Адрес, ДополнительныеПараметры);
	
КонецФункции

Функция РазобратьСтроку(Строка, СтрокаРазделитель) экспорт
	
	НоваяСтрока = СтрЗаменить(Строка, СтрокаРазделитель, Символы.ПС);
	МассивСтрок = новый Массив();
	Для НомерСтроки = 1 По СтрЧислоСтрок(НоваяСтрока) Цикл
		МассивСтрок.Добавить(СтрПолучитьСтроку(НоваяСтрока, НомерСтроки));
	КонецЦикла;
	возврат МассивСтрок;
	
КонецФункции

Функция ЭтоГородФедеральногоЗначения(Город)
	
	Города = Новый Массив;
	Города.Добавить("МОСКВА Г");
	Города.Добавить("САНКТ-ПЕТЕРБУРГ Г");
	Города.Добавить("СЕВАСТОПОЛЬ Г");
	Города.Добавить("БАЙКОНУР Г");
	
	Результат = Города.Найти(ВРег(Город)) <> Неопределено;
	
	Возврат Результат;
	
КонецФункции

// Взята из ОМ УправлениеКонтактнойИнформациейКлиентСервер, процедура СокращенияОбъектовАдресацииАдресаРФ,
// поскольку не является программным интерфейсом.
Функция СокращеноеНазваниОбъектаАдресации(ПолноеНазвание)
	
	Результат = Новый Соответствие;
	
	Результат.Вставить(НСтр("ru = 'Дом'"), НСтр("ru = 'Д.'"));
	Результат.Вставить(НСтр("ru = 'Владение'"), НСтр("ru = 'Вл.'"));
	Результат.Вставить(НСтр("ru = 'Домовладение'"), НСтр("ru = 'Домовл.'"));
	
	Результат.Вставить(НСтр("ru = 'Корпус'"), НСтр("ru = 'Корп.'"));
	Результат.Вставить(НСтр("ru = 'Строение'"), НСтр("ru = 'Стр.'"));
	Результат.Вставить(НСтр("ru = 'Литера'"), НСтр("ru = 'Лит.'"));
	Результат.Вставить(НСтр("ru = 'Сооружение'"), НСтр("ru = 'Сооруж.'"));
	Результат.Вставить(НСтр("ru = 'Участок'"), НСтр("ru = 'Уч.'"));
	
	Результат.Вставить(НСтр("ru = 'Квартира'"), НСтр("ru = 'Кв.'"));
	Результат.Вставить(НСтр("ru = 'Офис'"), НСтр("ru = 'Оф.'"));
	Результат.Вставить(НСтр("ru = 'Бокс'"), НСтр("ru = 'Бокс'"));
	Результат.Вставить(НСтр("ru = 'Помещение'"), НСтр("ru = 'Пом.'"));
	Результат.Вставить(НСтр("ru = 'Комната'"), НСтр("ru = 'Ком.'"));
	Результат.Вставить(НСтр("ru = 'Этаж'"), НСтр("ru = 'Этаж'"));
	
	Возврат НРег(Результат[ПолноеНазвание]);
	
КонецФункции

Функция ДатаВремяИзСтроки(ЗначениеСтр) Экспорт
	
	ЗначСтр = СокрЛП(ЗначениеСтр);
	
	Поле = "";
	МассивПолейДаты = Новый Массив;
	Для Счетчик = 1 По СтрДлина(ЗначСтр) Цикл
		ТекСимвол = Сред(ЗначСтр, Счетчик, 1);
		Если СтрНайти("T -/:.", ТекСимвол) > 0 Тогда
			МассивПолейДаты.Добавить(Поле);
			Поле = "";
		Иначе
			Поле = Поле + ТекСимвол;
			Если Счетчик = СтрДлина(ЗначСтр) Тогда
				МассивПолейДаты.Добавить(Поле);
			КонецЕсли; 
		КонецЕсли;
	КонецЦикла;
	
	Попытка
		Возврат Дата(Число(МассивПолейДаты[2])
					,Число(МассивПолейДаты[1])
					,Число(МассивПолейДаты[0])
					,Число(?(МассивПолейДаты.ВГраница() >= 3, МассивПолейДаты[3], 0))
					,Число(?(МассивПолейДаты.ВГраница() >= 4, МассивПолейДаты[4], 0))
					,Число(?(МассивПолейДаты.ВГраница() >= 5, МассивПолейДаты[5], 0)));
	Исключение
		Возврат '00010101';
	КонецПопытки;
	
КонецФункции

Функция ДатаИзПротоколаФСГС(ТранспортноеСообщение, Типы)
	
	ДатаИзОтвета = Дата(1,1,1);
	
	// извлекаем файл уведомления из содержимого сообщения
	СтрУведомления = ПолучитьВложенияТранспортногоСообщения(ТранспортноеСообщение, Истина, Типы);
	СтрУведомление = СтрУведомления[0];
	
	// записываем вложение во временный файл
	ПутьКФайлу = ПолучитьИмяВременногоФайла("xml");
	Попытка
		СтрУведомление.Данные.Получить().Записать(ПутьКФайлу);
	Исключение
		Возврат ДатаИзОтвета;
	КонецПопытки;
	
	ЭтоФорматXML = Ложь;
	ДеревоXML = ЗагрузитьXMLВДеревоЗначений(ПутьКФайлу);
	Если ДеревоXML <> Неопределено Тогда
		ЭтоФорматXML = Истина;
	КонецЕсли;
	
	Если ЭтоФорматXML Тогда
		
		УзелПротокол = ДеревоXML.Строки.Найти("protocol", "Имя");
		
		Если УзелПротокол <> Неопределено Тогда
			
			УзелСтатусПротокола = УзелПротокол.Строки.Найти("status", "Имя");
			СтатусПротокола = "";
			Если УзелСтатусПротокола <> Неопределено Тогда
				СтатусПротокола = XMLЗначение(Тип("Строка"), УзелСтатусПротокола.Значение);
			КонецЕсли;
			УзелСообщениеПротокола = УзелПротокол.Строки.Найти("msg", "Имя");
			СообщениеПротокола = "";
			Если УзелСообщениеПротокола <> Неопределено Тогда
				СообщениеПротокола = XMLЗначение(Тип("Строка"), УзелСообщениеПротокола.Значение);
			КонецЕсли;
			
			УзелТитульный = УзелПротокол.Строки.Найти("title", "Имя");
			Если УзелТитульный <> Неопределено Тогда
				
				ЗначенияЭлементов = Новый Соответствие;
				УзлыЭлементы = УзелТитульный.Строки.НайтиСтроки(Новый Структура("Имя, Тип", "item", "Э"));
				Для каждого УзелЭлемент Из УзлыЭлементы Цикл
					УзелИмяЭлемента = УзелЭлемент.Строки.Найти("name", "Имя");
					Если УзелИмяЭлемента <> Неопределено И ЗначениеЗаполнено(УзелИмяЭлемента.Значение) Тогда
						УзелЗначениеЭлемента = УзелЭлемент.Строки.Найти("value", "Имя");
						ЗначенияЭлементов.Вставить(УзелИмяЭлемента.Значение, ?(УзелЗначениеЭлемента <> Неопределено, XMLЗначение(Тип("Строка"), УзелЗначениеЭлемента.Значение), Неопределено));
					КонецЕсли;
				КонецЦикла;
				
				// определяем дату-время отправки
				ДатаВремяОтправки = ДатаВремяИзСтроки(ЗначенияЭлементов["dt_send"]);
				
			Иначе
				ЭтоФорматXML = Ложь;
			КонецЕсли;
		Иначе
			ЭтоФорматXML = Ложь;
		КонецЕсли;
		
	КонецЕсли;
	
	Если НЕ ЭтоФорматXML Тогда
		
		ТекстИзФайла = ПрочитатьТекстИзФайла(ПутьКФайлу, , Истина);
		
		// определяем дату-время получения
		ДатаВремяОтправки = "";
		Для СчетчикСтрок = 1 По СтрЧислоСтрок(ТекстИзФайла) Цикл
			ТекСтрока = СтрПолучитьСтроку(ТекстИзФайла, СчетчикСтрок);
			ПозСтрДата = СтрНайти(ТекСтрока, "Дата передачи файла:");
			Если ПозСтрДата > 0 Тогда
				ДатаИзОтвета = ДатаВремяИзСтроки(СокрЛП(Сред(ТекСтрока, ПозСтрДата + 20)));
				Прервать;
			КонецЕсли; 
		КонецЦикла;
		
	КонецЕсли;
	
	Возврат ДатаИзОтвета;
	
КонецФункции

Функция ДатаИзXMLФайлаОтвета(ТранспортноеСообщение, Типы, ИмяПоляДаты, ИмяПоляВремени = Неопределено)
	
	ДатаИзОтвета = Дата(1,1,1);
	
	// извлекаем файл уведомления из содержимого сообщения
	СтрУведомления = ПолучитьВложенияТранспортногоСообщения(ТранспортноеСообщение, Истина, Типы);
	СтрУведомление = СтрУведомления[0];
	
	// записываем вложение во временный файл
	ФайлУведомления = ПолучитьИмяВременногоФайла("xml");
	Попытка
		СтрУведомление.Данные.Получить().Записать(ФайлУведомления);
	Исключение
		Возврат ДатаИзОтвета;
	КонецПопытки;
	
	ДеревоXML = ЗагрузитьXMLВДеревоЗначений(ФайлУведомления);
	ОперацииСФайламиЭДКО.УдалитьВременныйФайл(ФайлУведомления);
	Если ДеревоXML = Неопределено Тогда
		Возврат ДатаИзОтвета;
	КонецЕсли;
	
	// Дата, возможно без времени
	Узел = ДеревоXML.Строки.Найти(ИмяПоляДаты, "Имя", Истина);
	Если Узел <> Неопределено Тогда
		Дата = ДатаВремяИзСтрокиXML(Узел.Значение);
	КонецЕсли;
	
	// Время, если есть.
	Если ИмяПоляВремени = Неопределено Тогда
		
		ДатаИзОтвета = ДатаИзСтрокиРазныхФорматов(Дата);
		
	Иначе
		
		Узел = ДеревоXML.Строки.Найти(ИмяПоляВремени, "Имя", Истина);
		Если Узел = Неопределено Тогда
			ДатаИзОтвета = ДатаИзСтрокиРазныхФорматов(Дата);
		Иначе
			
			Время 		 = Узел.Значение;
			ДатаИзОтвета = СоединитьДатуИВремя(Дата, Время);
			
		КонецЕсли;
		
	КонецЕсли;
	
	Возврат ДатаИзОтвета;
	
КонецФункции

Функция СоединитьДатуИВремя(Дата, Время)
	
	Результат = Дата(1,1,1);
	
	Если ТипЗнч(Дата) <> Тип("Дата") Тогда
		Возврат Дата;
	КонецЕсли;
	
	ДатаИВремя = Формат(Дата, "ДЛФ=D") + " " + Время;
	
	Попытка
		Результат = ДатаИзСтрокиРазныхФорматов(ДатаИВремя);
	Исключение
		Результат = ДатаИВремя;
	КонецПопытки;
	
	Возврат Результат;
	
КонецФункции

Функция ПолучитьСоответствиеТиповСодержимого() Экспорт

	СоответствиеТиповСодержимого = Новый Соответствие;
	
	// отчетность ПФР
	СоответствиеТиповСодержимого.Вставить(Перечисления.ТипыСодержимогоТранспортногоКонтейнера.ФайлОтчетностиПФР, "ФайлОтчетностиПФР");
	СоответствиеТиповСодержимого.Вставить(Перечисления.ТипыСодержимогоТранспортногоКонтейнера.ПодтверждениеПолученияОтчетностиПФР, "ПодтверждениеПолученияОтчетностиПФР");
	СоответствиеТиповСодержимого.Вставить(Перечисления.ТипыСодержимогоТранспортногоКонтейнера.СведенияПриложениеПФР, "СведенияПриложениеПФР");
	СоответствиеТиповСодержимого.Вставить(Перечисления.ТипыСодержимогоТранспортногоКонтейнера.ПротоколПФР, "ПротоколПФР");
	СоответствиеТиповСодержимого.Вставить(Перечисления.ТипыСодержимогоТранспортногоКонтейнера.ПротоколПриложениеПФР, "ПротоколПриложениеПФР");
	СоответствиеТиповСодержимого.Вставить(Перечисления.ТипыСодержимогоТранспортногоКонтейнера.ФайлОписанияСведенийПФР, "ФайлОписанияСведенийПФР");
	СоответствиеТиповСодержимого.Вставить(Перечисления.ТипыСодержимогоТранспортногоКонтейнера.ЭлектронноЦифроваяПодпись, "ЭлектронноЦифроваяПодпись");
	
	// неформализованные ПФР
	СоответствиеТиповСодержимого.Вставить(Перечисления.ТипыСодержимогоТранспортногоКонтейнера.Письмо, "Письмо");
	СоответствиеТиповСодержимого.Вставить(Перечисления.ТипыСодержимогоТранспортногоКонтейнера.ПисьмоПриложение, "ПисьмоПриложение");
	СоответствиеТиповСодержимого.Вставить(Перечисления.ТипыСодержимогоТранспортногоКонтейнера.ПодтверждениеПолученияПисьма, "ПодтверждениеПолученияПисьма");
	СоответствиеТиповСодержимого.Вставить(Перечисления.ТипыСодержимогоТранспортногоКонтейнера.ОписаниеПисьма, "ОписаниеПисьма");
	СоответствиеТиповСодержимого.Вставить(Перечисления.ТипыСодержимогоТранспортногоКонтейнера.ТранспортнаяИнформация, "ТранспортнаяИнформация");
	
	// запросы ПФР
	СоответствиеТиповСодержимого.Вставить(Перечисления.ТипыСодержимогоТранспортногоКонтейнера.ЗапросИОС, "ЗапросИОС");
	СоответствиеТиповСодержимого.Вставить(Перечисления.ТипыСодержимогоТранспортногоКонтейнера.ФайлОписанияЗапросаПФР, "ФайлОписанияЗапросаПФР");
	СоответствиеТиповСодержимого.Вставить(Перечисления.ТипыСодержимогоТранспортногоКонтейнера.ОтветНаЗапросПФР, "ОтветНаЗапросПФР");
	СоответствиеТиповСодержимого.Вставить(Перечисления.ТипыСодержимогоТранспортногоКонтейнера.ОтветПриложениеНаЗапросПФР, "ОтветПриложениеНаЗапросПФР");
	СоответствиеТиповСодержимого.Вставить(Перечисления.ТипыСодержимогоТранспортногоКонтейнера.ПодтверждениеПолученияЗапросПФР, "ПодтверждениеПолученияЗапросПФР");
	
	// отчетность ФНС
	СоответствиеТиповСодержимого.Вставить(Перечисления.ТипыСодержимогоТранспортногоКонтейнера.ФайлОтчетности, "ФайлОтчетности");
	СоответствиеТиповСодержимого.Вставить(Перечисления.ТипыСодержимогоТранспортногоКонтейнера.Форма2НДФЛ, "Форма2НДФЛ");
	СоответствиеТиповСодержимого.Вставить(Перечисления.ТипыСодержимогоТранспортногоКонтейнера.Доверенность, "Доверенность");
	СоответствиеТиповСодержимого.Вставить(Перечисления.ТипыСодержимогоТранспортногоКонтейнера.ОписаниеСведений, "ОписаниеСведений");
	СоответствиеТиповСодержимого.Вставить(Перечисления.ТипыСодержимогоТранспортногоКонтейнера.ОписаниеСведений2НДФЛ, "ОписаниеСведений2НДФЛ");
	СоответствиеТиповСодержимого.Вставить(Перечисления.ТипыСодержимогоТранспортногоКонтейнера.ПодтверждениеДатыОтправки, "ПодтверждениеДатыОтправки");
	СоответствиеТиповСодержимого.Вставить(Перечисления.ТипыСодержимогоТранспортногоКонтейнера.УведомлениеОбОтказе, "УведомлениеОбОтказе");
	СоответствиеТиповСодержимого.Вставить(Перечисления.ТипыСодержимогоТранспортногоКонтейнера.КвитанцияОПриеме, "КвитанцияОПриеме");
	СоответствиеТиповСодержимого.Вставить(Перечисления.ТипыСодержимогоТранспортногоКонтейнера.ИзвещениеОВводе, "ИзвещениеОВводе");
	СоответствиеТиповСодержимого.Вставить(Перечисления.ТипыСодержимогоТранспортногоКонтейнера.УведомлениеОбУточнении, "УведомлениеОбУточнении");
	СоответствиеТиповСодержимого.Вставить(Перечисления.ТипыСодержимогоТранспортногоКонтейнера.ИзвещениеОПолучении, "ИзвещениеОПолучении");
	
	// 2-НДФЛ
	СоответствиеТиповСодержимого.Вставить(Перечисления.ТипыСодержимогоТранспортногоКонтейнера.Протокол2НДФЛ, "Протокол2НДФЛ");
	СоответствиеТиповСодержимого.Вставить(Перечисления.ТипыСодержимогоТранспортногоКонтейнера.Реестр2НДФЛ, "Реестр2НДФЛ");
	
	// обращение
	СоответствиеТиповСодержимого.Вставить(Перечисления.ТипыСодержимогоТранспортногоКонтейнера.Обращение, "Обращение");
	СоответствиеТиповСодержимого.Вставить(Перечисления.ТипыСодержимогоТранспортногоКонтейнера.Приложение, "Приложение");
	СоответствиеТиповСодержимого.Вставить(Перечисления.ТипыСодержимогоТранспортногоКонтейнера.ПодтверждениеДатыПолучения, "ПодтверждениеДатыПолучения");
	СоответствиеТиповСодержимого.Вставить(Перечисления.ТипыСодержимогоТранспортногоКонтейнера.ОписаниеСведенийОбращениеПисьмоРассылка, "ОписаниеСведенийОбращениеПисьмоРассылка");
	
	// представление
	СоответствиеТиповСодержимого.Вставить(Перечисления.ТипыСодержимогоТранспортногоКонтейнера.Представление, "Обращение");
	СоответствиеТиповСодержимого.Вставить(Перечисления.ТипыСодержимогоТранспортногоКонтейнера.ДоверенностьПредставление, "ДоверенностьПредставление");
	СоответствиеТиповСодержимого.Вставить(Перечисления.ТипыСодержимогоТранспортногоКонтейнера.ОписаниеПредставление, "ОписаниеПредставление");
	СоответствиеТиповСодержимого.Вставить(Перечисления.ТипыСодержимогоТранспортногоКонтейнера.ПодтверждениеДатыОтправкиПредставление, "ПодтверждениеДатыОтправкиПредставление");
	
	// входящие неформализованные
	СоответствиеТиповСодержимого.Вставить(Перечисления.ТипыСодержимогоТранспортногоКонтейнера.Рассылка, "Рассылка");
	
	// запросы
	СоответствиеТиповСодержимого.Вставить(Перечисления.ТипыСодержимогоТранспортногоКонтейнера.Запрос, "Запрос");
	СоответствиеТиповСодержимого.Вставить(Перечисления.ТипыСодержимогоТранспортногоКонтейнера.Ответ, "Ответ");
	СоответствиеТиповСодержимого.Вставить(Перечисления.ТипыСодержимогоТранспортногоКонтейнера.ОписаниеСведенийЗапрос, "ОписаниеСведенийЗапрос");
	
	// запросы на выписку из ЕГРЮЛ/ЕГРИП	
	СоответствиеТиповСодержимого.Вставить(Перечисления.ТипыСодержимогоТранспортногоКонтейнера.ЗапросНаВыпискуИзЕГРЮЛ_ЕГРИП, "Запрос");
	СоответствиеТиповСодержимого.Вставить(Перечисления.ТипыСодержимогоТранспортногоКонтейнера.ОтветНаЗапросВыпискиИзЕГРЮЛ_ЕГРИП, "Ответ");
	СоответствиеТиповСодержимого.Вставить(Перечисления.ТипыСодержимогоТранспортногоКонтейнера.РезультатОбработкиЗапросаНаВыпискуЕГРЮЛ_ЕГРИП, "ОписаниеСведенийЗапрос");
	
	//документ	
	СоответствиеТиповСодержимого.Вставить(Перечисления.ТипыСодержимогоТранспортногоКонтейнера.Документ, "Документ");
	СоответствиеТиповСодержимого.Вставить(Перечисления.ТипыСодержимогоТранспортногоКонтейнера.ОписаниеДокумент, "ОписаниеДокумент");
	СоответствиеТиповСодержимого.Вставить(Перечисления.ТипыСодержимогоТранспортногоКонтейнера.УведомлениеОбОтказеДокумент, "УведомлениеОбОтказеДокумент");
	СоответствиеТиповСодержимого.Вставить(Перечисления.ТипыСодержимогоТранспортногоКонтейнера.КвитанцияОПриемеДокумент, "КвитанцияОПриемеДокумент");
	
	//заявление	
	СоответствиеТиповСодержимого.Вставить(Перечисления.ТипыСодержимогоТранспортногоКонтейнера.Заявление, "Заявление");
	СоответствиеТиповСодержимого.Вставить(Перечисления.ТипыСодержимогоТранспортногоКонтейнера.ОписаниеСведенийЗаявление, "ОписаниеСведенийЗаявление");
	СоответствиеТиповСодержимого.Вставить(Перечисления.ТипыСодержимогоТранспортногоКонтейнера.КвитанцияОПриемеЗаявления, "КвитанцияОПриемеЗаявления");
	
	СоответствиеТиповСодержимого.Вставить(Перечисления.ТипыСодержимогоТранспортногоКонтейнера.СообщениеОПростОтметки, 		"СообщениеОПростОтметки");
	СоответствиеТиповСодержимого.Вставить(Перечисления.ТипыСодержимогоТранспортногоКонтейнера.УведомлениеОбОтказеОтметки, 	"УведомлениеОбОтказеОтметки");
	
	СоответствиеТиповСодержимого.Вставить(Перечисления.ТипыСодержимогоТранспортногоКонтейнера.СообщениеОПроверке, "СообщениеОПроверке");
	СоответствиеТиповСодержимого.Вставить(Перечисления.ТипыСодержимогоТранспортногоКонтейнера.СообщениеОбОтзывеЗаявления, "СообщениеОбОтзывеЗаявления");
	СоответствиеТиповСодержимого.Вставить(Перечисления.ТипыСодержимогоТранспортногоКонтейнера.СообщениеОНесоответствиях, "СообщениеОНесоответствиях");
	
	// отчетность в Росстат
	СоответствиеТиповСодержимого.Вставить(Перечисления.ТипыСодержимогоТранспортногоКонтейнера.ФайлОтчетностиФСГС, "ФайлОтчетностиФСГС");
	СоответствиеТиповСодержимого.Вставить(Перечисления.ТипыСодержимогоТранспортногоКонтейнера.ИзвещениеОПолученииДокументаФСГС, "ИзвещениеОПолученииДокументаФСГС");
	СоответствиеТиповСодержимого.Вставить(Перечисления.ТипыСодержимогоТранспортногоКонтейнера.ПодтверждениеОператораФСГС, "ПодтверждениеОператораФСГС");
	СоответствиеТиповСодержимого.Вставить(Перечисления.ТипыСодержимогоТранспортногоКонтейнера.УведомлениеОПриемеВОбработкуОтчетаФСГС, "УведомлениеОПриемеВОбработкуОтчетаФСГС");
	СоответствиеТиповСодержимого.Вставить(Перечисления.ТипыСодержимогоТранспортногоКонтейнера.УведомлениеОбУточненииОтчетаФСГС, "УведомлениеОбУточненииОтчетаФСГС");
	СоответствиеТиповСодержимого.Вставить(Перечисления.ТипыСодержимогоТранспортногоКонтейнера.УведомлениеОНесоответствииФорматуОтчетаФСГС, "УведомлениеОНесоответствииФорматуОтчетаФСГС");
	СоответствиеТиповСодержимого.Вставить(Перечисления.ТипыСодержимогоТранспортногоКонтейнера.УведомлениеОбОтказеОтчетаФСГС, "УведомлениеОбОтказеОтчетаФСГС");
	СоответствиеТиповСодержимого.Вставить(Перечисления.ТипыСодержимогоТранспортногоКонтейнера.УведомлениеОбОтклоненииОтчетаФСГС, "УведомлениеОбОтклоненииОтчетаФСГС");
	СоответствиеТиповСодержимого.Вставить(Перечисления.ТипыСодержимогоТранспортногоКонтейнера.ОписаниеФайлаОтчетностиФСГС, "ОписаниеФайлаОтчетностиФСГС");
	
	// переписка с Росстатом
	СоответствиеТиповСодержимого.Вставить(Перечисления.ТипыСодержимогоТранспортногоКонтейнера.ПисьмоФСГС, "ПисьмоФСГС");
	СоответствиеТиповСодержимого.Вставить(Перечисления.ТипыСодержимогоТранспортногоКонтейнера.РассылкаФСГС, "РассылкаФСГС");
	СоответствиеТиповСодержимого.Вставить(Перечисления.ТипыСодержимогоТранспортногоКонтейнера.ШаблонФСГС, "ШаблонФСГС");
	СоответствиеТиповСодержимого.Вставить(Перечисления.ТипыСодержимогоТранспортногоКонтейнера.ОписаниеРассылкиШаблоновФСГС, "ОписаниеРассылкиШаблоновФСГС");
	СоответствиеТиповСодержимого.Вставить(Перечисления.ТипыСодержимогоТранспортногоКонтейнера.ПриложениеПисьмаФСГС, "ПриложениеПисьмаФСГС");
	СоответствиеТиповСодержимого.Вставить(Перечисления.ТипыСодержимогоТранспортногоКонтейнера.ОписаниеПисьмаФСГС, "ОписаниеПисьмаФСГС");
	
	Возврат СоответствиеТиповСодержимого;
	
КонецФункции

Функция ДатаИзОтвета(ПоследнийРезультатПриема, НеПолучатьДаты)
	
	ДатаИзОтвета = Дата(1,1,1);
	
	Если НЕ НеПолучатьДаты Тогда
		
		Если ТипЗнч(ПоследнийРезультатПриема) = Тип("Массив") Тогда
			
			Если ПоследнийРезультатПриема.Количество() > 0 Тогда
				
				ТранспортноеСообщение 	= ПоследнийРезультатПриема[0].Ссылка;
				ДатаИзОтвета 			= ДатаИзОтветовКонтролирующихОрганов(ТранспортноеСообщение);
			
			КонецЕсли;
				
		Иначе
				
			ДатаИзОтвета = ДатаИзОтветовКонтролирующихОрганов(ПоследнийРезультатПриема.Ссылка);
			
		КонецЕсли;
		
	КонецЕсли;
		
	Возврат ДатаИзОтвета;
	
КонецФункции

Функция ДатаОтправки(ПоследнийРезультатПриема, НеПолучатьДаты) Экспорт
	
	ДатаИзОтвета = Дата(1,1,1);
	
	Если НеПолучатьДаты Тогда
		
		ДатаИзОтвета = Дата(1,1,1);
		
	Иначе
		
		Если ТипЗнч(ПоследнийРезультатПриема) = Тип("Массив") Тогда
			
			ДатаИзОтвета = ?(
				ПоследнийРезультатПриема.Количество() > 0, 
				ПоследнийРезультатПриема[0].ДатаТранспорта, 
				Дата(1,1,1));
			
		Иначе
		
			ДатаИзОтвета = ПоследнийРезультатПриема.ДатаТранспорта;
			
		КонецЕсли;
		
	КонецЕсли;
	
	Возврат ДатаИзОтвета;
	
КонецФункции

#Область БанкРоссии

Функция ТаблицаЭтаповОтправкиБанкРоссии(СсылкаНаОтчет, ДополнительныеПараметры)
	
	НачальныйСтатус = НачальныйСтатусПриФормированииТаблицыЭтаповОтправки(СсылкаНаОтчет);
	
	ТаблицаЭтаповОтправки = ЗаготовкаТаблицыЭтаповОтправки();
	
	СтрокаЭтапа = ТаблицаЭтаповОтправки.Добавить();
	СтрокаЭтапа.ТекстНадписи				= НачальныйСтатус;
	СтрокаЭтапа.ТекстСтатуса				= НачальныйСтатус;
	СтрокаЭтапа.Дата						= Дата(1,1,1);
	СтрокаЭтапа.КомментарийКСостоянию		= КомментарийКСостояниюНеОтправлено();
	СтрокаЭтапа.НаименованиеПротокола		= "";
	СтрокаЭтапа.Протокол					= "";
	СтрокаЭтапа.СостояниеСдачиОтчетности	= Перечисления.СостояниеСдачиОтчетности.ДокументооборотНеНачат;
	СтрокаЭтапа.ЭтапПройден					= Истина;
	
	// определяем последнюю отправку
	Если ТипЗнч(ДополнительныеПараметры) = Тип("Структура")
		И ДополнительныеПараметры.Свойство("Отправка") Тогда
		ОтправкаСсылка = ДополнительныеПараметры.Отправка;
	Иначе
		ОтправкаСсылка =  ПолучитьПоследнююОтправкуОтчетаВБанкРоссии(СсылкаНаОтчет);
	КонецЕсли;
	
	Если НЕ ЗначениеЗаполнено(ОтправкаСсылка) Тогда
		Возврат ТаблицаЭтаповОтправки;
	КонецЕсли;
	
	СтрокаЭтапа = ТаблицаЭтаповОтправки.Добавить();
	СтрокаЭтапа.ТекстНадписи				= НСтр("ru = 'Отправлено в Банк России'");
	СтрокаЭтапа.ТекстСтатуса				= НСтр("ru = 'Отправлено в Банк России'");
	СтрокаЭтапа.Дата						= ?(ЗначениеЗаполнено(ОтправкаСсылка), ОтправкаСсылка.ДатаОтправки, Дата(1,1,1));
	СтрокаЭтапа.КомментарийКСостоянию		= НСтр("ru = 'Ожидается результат обработки'");
	СтрокаЭтапа.НаименованиеПротокола		= "";
	СтрокаЭтапа.Протокол					= "";
	СтрокаЭтапа.СостояниеСдачиОтчетности	= Перечисления.СостояниеСдачиОтчетности.ДокументооборотНачат;
	СтрокаЭтапа.ЭтапПройден					= ОтправкаСсылка <> Неопределено;

	
	Если ОтправкаСсылка <> Неопределено И  ОтправкаСсылка.СтатусОтправки = Перечисления.СтатусыОтправки.НеПринят Тогда
		
		СтрокаЭтапа = ТаблицаЭтаповОтправки.Добавить();
		СтрокаЭтапа.ТекстНадписи				= НСтр("ru = 'Не сдано'");
		СтрокаЭтапа.ТекстСтатуса				= НСтр("ru = 'Не сдано'");
		СтрокаЭтапа.Дата						= ?(ЗначениеЗаполнено(ОтправкаСсылка), ОтправкаСсылка.ДатаЗакрытия, Дата(1,1,1));
		СтрокаЭтапа.КомментарийКСостоянию		= НСтр("ru = 'Исправьте ошибки и отправьте отчет еще раз'");
		СтрокаЭтапа.НаименованиеПротокола		= НСтр("ru = 'Протокол ошибок'");
		СтрокаЭтапа.Протокол					= ОтправкаСсылка;
		СтрокаЭтапа.СостояниеСдачиОтчетности	= Перечисления.СостояниеСдачиОтчетности.ОтрицательныйРезультатДокументооборота;
		СтрокаЭтапа.ЭтапПройден					= Истина;
		
	Иначе
		
		СтрокаЭтапа = ТаблицаЭтаповОтправки.Добавить();
		СтрокаЭтапа.ТекстНадписи				= НСтр("ru = 'Сдано'");
		СтрокаЭтапа.ТекстСтатуса				= НСтр("ru = 'Сдано'");
		СтрокаЭтапа.Дата						= ?(ЗначениеЗаполнено(ОтправкаСсылка) И ОтправкаСсылка.СтатусОтправки = Перечисления.СтатусыОтправки.Сдан, ОтправкаСсылка.ДатаЗакрытия, Дата(1,1,1));
		СтрокаЭтапа.КомментарийКСостоянию		= "";
		СтрокаЭтапа.НаименованиеПротокола		= НСтр("ru = 'Протокол о сдаче'");
		СтрокаЭтапа.Протокол					= ОтправкаСсылка;
		СтрокаЭтапа.СостояниеСдачиОтчетности	= Перечисления.СостояниеСдачиОтчетности.ПоложительныйРезультатДокументооборота;
		СтрокаЭтапа.ЭтапПройден					= ОтправкаСсылка <> Неопределено И ОтправкаСсылка.СтатусОтправки = Перечисления.СтатусыОтправки.Сдан;
			
	КонецЕсли;
	
	Возврат ТаблицаЭтаповОтправки;
	
КонецФункции

Функция ТаблицаЭтаповОтправкиМинобороны(СсылкаНаОтчет, ДополнительныеПараметры)
	
	НачальныйСтатус = НачальныйСтатусПриФормированииТаблицыЭтаповОтправки(СсылкаНаОтчет, "Минобороны");
	
	ТаблицаЭтаповОтправки = ЗаготовкаТаблицыЭтаповОтправки();
	ТаблицаЭтаповОтправки.Колонки.Добавить("СтатусУстановленВручную", Новый ОписаниеТипов("Булево"));
	
	ПоследняяОтправка = ПолучитьПоследнююОтправкуОтчетаВМинобороны(СсылкаНаОтчет);
	
	// определяем последнюю отправку
	Если ТипЗнч(ДополнительныеПараметры) = Тип("Структура")
		И ДополнительныеПараметры.Свойство("Отправка") Тогда
		
		ОтправкаСсылка = ДополнительныеПараметры.Отправка;
		
	Иначе
		
		ОтправкаСсылка = ПоследняяОтправка;
		
	КонецЕсли;
	
	// СтатусУстановленВручную устанавливаем только для последней отправки к ней относится статус в журнале
	Если ОтправкаСсылка = ПоследняяОтправка Тогда
		МодульДокументооборотСМинобороны = ОбщегоНазначения.ОбщийМодуль("ДокументооборотСМинобороны");
		СтатусУстановленВручную = МодульДокументооборотСМинобороны.СтатусУстановленВручную(ОтправкаСсылка, НачальныйСтатус);
	Иначе
		СтатусУстановленВручную = Ложь;
	КонецЕсли; 
	
	Если СтатусУстановленВручную Тогда
		СтрокаЭтапа = ТаблицаЭтаповОтправки.Добавить();
		СтрокаЭтапа.ТекстНадписи				= НачальныйСтатус;
		СтрокаЭтапа.ТекстСтатуса				= НачальныйСтатус;
		СтрокаЭтапа.Дата						= Дата(1,1,1);
		Если НачальныйСтатус = "В работе" Тогда
			СтрокаЭтапа.КомментарийКСостоянию		= КомментарийКСостояниюНеОтправленоМинобороны();
			СтрокаЭтапа.СостояниеСдачиОтчетности	= Перечисления.СостояниеСдачиОтчетности.ДокументооборотНеНачат;
		ИначеЕсли НачальныйСтатус = "Передано в Минобороны" ИЛИ НачальныйСтатус = "Передано" Тогда
			СтрокаЭтапа.ТекстНадписи				= НСтр("ru = 'Передано в Минобороны'");
			СтрокаЭтапа.ТекстСтатуса				= НСтр("ru = 'Передано'");
			СтрокаЭтапа.КомментарийКСостоянию		= "";
			СтрокаЭтапа.СостояниеСдачиОтчетности	= Перечисления.СостояниеСдачиОтчетности.ДокументооборотНачат;
		ИначеЕсли НачальныйСтатус = "Сдано" ИЛИ НачальныйСтатус = "Сдано, есть замечания" Тогда
			СтрокаЭтапа.КомментарийКСостоянию		= "";
			СтрокаЭтапа.СостояниеСдачиОтчетности	= Перечисления.СостояниеСдачиОтчетности.ПоложительныйРезультатДокументооборота;
		ИначеЕсли НачальныйСтатус = "Не сдано" Тогда
			СтрокаЭтапа.КомментарийКСостоянию		= "";
			СтрокаЭтапа.СостояниеСдачиОтчетности	= Перечисления.СостояниеСдачиОтчетности.ОтрицательныйРезультатДокументооборота;
		КонецЕсли;
		СтрокаЭтапа.НаименованиеПротокола		= "";
		СтрокаЭтапа.Протокол					= "";
		СтрокаЭтапа.ЭтапПройден					= Истина;
		СтрокаЭтапа.СтатусУстановленВручную = Истина;
 	КонецЕсли; 
	
	Если НЕ ЗначениеЗаполнено(ОтправкаСсылка) ИЛИ
		ОтправкаСсылка.СтатусОтправки = Перечисления.СтатусыОтправки.ПустаяСсылка()
		ИЛИ СтатусУстановленВручную Тогда
		
		// таблица должна содержать хотя бы одну строку
		Если ТаблицаЭтаповОтправки.Количество() = 0 Тогда
			СтрокаЭтапа = ТаблицаЭтаповОтправки.Добавить();
			СтрокаЭтапа.ТекстНадписи				= НСтр("ru = 'Передано в Минобороны'");
			СтрокаЭтапа.ТекстСтатуса				= НСтр("ru = 'Передано'");
			СтрокаЭтапа.Дата						= ?(ЗначениеЗаполнено(ОтправкаСсылка), ОтправкаСсылка.ДатаОтправки, Дата(1,1,1));
			СтрокаЭтапа.КомментарийКСостоянию		= "";
			СтрокаЭтапа.НаименованиеПротокола		= НСтр("ru = 'Загрузить протокол сдачи'", "ru");
			СтрокаЭтапа.Протокол					= "ЗагрузитьОтветнуюКвитанцию";
			СтрокаЭтапа.СостояниеСдачиОтчетности	= Перечисления.СостояниеСдачиОтчетности.ДокументооборотНачат;
			СтрокаЭтапа.ЭтапПройден					= ОтправкаСсылка <> Неопределено;
		КонецЕсли;
		
		Возврат ТаблицаЭтаповОтправки;
		
	КонецЕсли;
	
	// этапы отчетов отправленных в Минобороны
	СтрокаЭтапа = ТаблицаЭтаповОтправки.Добавить();
	СтрокаЭтапа.ТекстНадписи				= НСтр("ru = 'Передано в Минобороны'");
	СтрокаЭтапа.ТекстСтатуса				= НСтр("ru = 'Передано'");
	СтрокаЭтапа.Дата						= ?(ЗначениеЗаполнено(ОтправкаСсылка), ОтправкаСсылка.ДатаОтправки, Дата(1,1,1));
	СтрокаЭтапа.КомментарийКСостоянию		= "";
	СтрокаЭтапа.НаименованиеПротокола		= НСтр("ru = 'Загрузить протокол сдачи'", "ru");
	СтрокаЭтапа.Протокол					= "ЗагрузитьОтветнуюКвитанцию";
	СтрокаЭтапа.СостояниеСдачиОтчетности	= Перечисления.СостояниеСдачиОтчетности.ДокументооборотНачат;
	СтрокаЭтапа.ЭтапПройден					= ОтправкаСсылка <> Неопределено;
		
	Если ОтправкаСсылка.СтатусОтправки = Перечисления.СтатусыОтправки.НеПринят Тогда
		
		СтрокаЭтапа = ТаблицаЭтаповОтправки.Добавить();
		СтрокаЭтапа.ТекстНадписи				= НСтр("ru = 'Не сдано'");
		СтрокаЭтапа.ТекстСтатуса				= НСтр("ru = 'Не сдано'");
		СтрокаЭтапа.Дата						= ?(ЗначениеЗаполнено(ОтправкаСсылка), ОтправкаСсылка.ДатаЗакрытия, Дата(1,1,1));
		СтрокаЭтапа.НаименованиеПротокола		= НСтр("ru = 'Протокол ошибок'");
		СтрокаЭтапа.Протокол					= ОтправкаСсылка;
		СтрокаЭтапа.КомментарийКСостоянию		= НСтр("ru = 'Исправьте ошибки и передайте отчет еще раз'");
		СтрокаЭтапа.СостояниеСдачиОтчетности	= Перечисления.СостояниеСдачиОтчетности.ОтрицательныйРезультатДокументооборота;
		СтрокаЭтапа.ЭтапПройден					= Истина;
		
	ИначеЕсли ОтправкаСсылка.СтатусОтправки = Перечисления.СтатусыОтправки.ПринятЕстьОшибки Тогда 
		
		СтрокаЭтапа = ТаблицаЭтаповОтправки.Добавить();
		СтрокаЭтапа.ТекстНадписи				= НСтр("ru = 'Сдано, есть замечания'");
		СтрокаЭтапа.ТекстСтатуса				= НСтр("ru = 'Сдано, есть замечания'");
		СтрокаЭтапа.Дата						= ?(ЗначениеЗаполнено(ОтправкаСсылка) И ОтправкаСсылка.СтатусОтправки = Перечисления.СтатусыОтправки.ПринятЕстьОшибки, ОтправкаСсылка.ДатаЗакрытия, Дата(1,1,1));
		СтрокаЭтапа.КомментарийКСостоянию		= "";
		СтрокаЭтапа.НаименованиеПротокола		= НСтр("ru = 'Протокол сдачи'");
		СтрокаЭтапа.Протокол					= ОтправкаСсылка;
		СтрокаЭтапа.СостояниеСдачиОтчетности	= Перечисления.СостояниеСдачиОтчетности.ПоложительныйРезультатДокументооборота;
		СтрокаЭтапа.ЭтапПройден					= Истина;
		
	ИначеЕсли ОтправкаСсылка.СтатусОтправки = Перечисления.СтатусыОтправки.Сдан Тогда 
		
		СтрокаЭтапа = ТаблицаЭтаповОтправки.Добавить();
		СтрокаЭтапа.ТекстНадписи				= НСтр("ru = 'Сдано'");
		СтрокаЭтапа.ТекстСтатуса				= НСтр("ru = 'Сдано'");
		СтрокаЭтапа.Дата						= ?(ЗначениеЗаполнено(ОтправкаСсылка) И ОтправкаСсылка.СтатусОтправки = Перечисления.СтатусыОтправки.Сдан, ОтправкаСсылка.ДатаЗакрытия, Дата(1,1,1));
		СтрокаЭтапа.КомментарийКСостоянию		= "";
		// если сдано, то протокол ошибок не отображается
		СтрокаЭтапа.НаименованиеПротокола		= "";
		СтрокаЭтапа.Протокол					= ОтправкаСсылка;
		СтрокаЭтапа.СостояниеСдачиОтчетности	= Перечисления.СостояниеСдачиОтчетности.ПоложительныйРезультатДокументооборота;
		СтрокаЭтапа.ЭтапПройден					= Истина;
			
	КонецЕсли;
	
	Возврат ТаблицаЭтаповОтправки;
	
КонецФункции

Функция ПолучитьПоследнююОтправкуОтчетаВБанкРоссии(ОтчетСсылка) Экспорт
	
	Отправка = Неопределено;
	
	Если ОбщегоНазначения.ПодсистемаСуществует("РегламентированнаяОтчетность.ЭлектронныйДокументооборотСКонтролирующимиОрганами.СдачаОтчетностиВБанкРоссии") Тогда
		МодульДокументооборотСБанкомРоссии = ОбщегоНазначения.ОбщийМодуль("ДокументооборотСБанкомРоссии");
		Возврат МодульДокументооборотСБанкомРоссии.ПолучитьПоследнююОтправкуОтчета(ОтчетСсылка);
	КонецЕсли;
	
	Возврат Отправка;
	
КонецФункции

Функция ПолучитьПоследнююОтправкуОтчетаВМинобороны(ОтчетСсылка) Экспорт
	
	Отправка = Неопределено;
	
	Если ОбщегоНазначения.ПодсистемаСуществует("РегламентированнаяОтчетность.ЭлектронныйДокументооборотСКонтролирующимиОрганами.СдачаОтчетностиВМинобороны") Тогда
		МодульДокументооборотСМинобороны = ОбщегоНазначения.ОбщийМодуль("ДокументооборотСМинобороны");
		Возврат МодульДокументооборотСМинобороны.ПолучитьПоследнююОтправкуОтчета(ОтчетСсылка);
	КонецЕсли;
	
	Возврат Отправка;
	
КонецФункции

Процедура ПриЗаписиОтправкиБанкРоссии(Объект, Отказ) Экспорт
	
	Если Объект.ОтчетСсылка <> Неопределено Тогда
		// отражаем изменения в регистре статусов отправки
		ОбновитьСтатусОтправкиОтчетаВБанкРоссии(Объект.ОтчетСсылка);
		ЭлектронныйДокументооборотСКонтролирующимиОрганами.ЗаписьОбъектовРегламентированнойОтчетности(Объект.ОтчетСсылка, Отказ);
	КонецЕсли;
	
КонецПроцедуры

Процедура ПриЗаписиОтправкиМинобороны(Объект, Отказ) Экспорт
	
	Если Объект.ОтчетСсылка <> Неопределено Тогда
		// отражаем изменения в регистре статусов отправки
		ОбновитьСтатусОтправкиОтчетаВМинобороны(Объект.ОтчетСсылка);
		ЭлектронныйДокументооборотСКонтролирующимиОрганами.ЗаписьОбъектовРегламентированнойОтчетности(Объект.ОтчетСсылка, Отказ);
	КонецЕсли;
	
КонецПроцедуры

Процедура ОбновитьСтатусОтправкиОтчетаВМинобороны(Отчет)
	
	// находим последнюю не помеченную на удаление отправку отчета
	ПоследняяОтправка = ПолучитьПоследнююОтправкуОтчетаВМинобороны(Отчет);
	
	// вычисляем значение статуса отправки (ПеречислениеСсылка.СтатусыОтправки)
	Если ЗначениеЗаполнено(ПоследняяОтправка) Тогда
		
		СтатусОтправки = ПоследняяОтправка.СтатусОтправки;
		Если СтатусОтправки = Перечисления.СтатусыОтправки.ПринятЕстьОшибки Тогда
			//введено после добавления дополнительного статуса ПринятЕстьОшибки, при котором отчет считается сданным
			СтатусОтправки = Перечисления.СтатусыОтправки.Сдан;
		КонецЕсли;
		
	Иначе
		СтатусОтправки = Неопределено;
	КонецЕсли;
	
	// сохраняем статус в базе
	ЗаписатьСтатусОтправкиОтчетаВМинобороны(Отчет, СтатусОтправки, ПоследняяОтправка);
	
КонецПроцедуры

Процедура ЗаписатьСтатусОтправкиОтчетаВМинобороны(Отчет, СтатусОтправки, ОснованиеСтатуса)
	
	Если ЗначениеЗаполнено(СтатусОтправки) Тогда
		МенЗап = РегистрыСведений.СтатусыОтправки.СоздатьМенеджерЗаписи();
		МенЗап.Объект = Отчет;
		МенЗап.Статус = СтатусОтправки;
		МенЗап.Основание = ОснованиеСтатуса;
		МенЗап.Записать(Истина);
	Иначе
		МенЗап = РегистрыСведений.СтатусыОтправки.СоздатьМенеджерЗаписи();
		МенЗап.Объект = Отчет;
		МенЗап.Прочитать();
		Если МенЗап.Выбран() Тогда
			МенЗап.Удалить();
		КонецЕсли;
	КонецЕсли;
	
	// вызываем переопределяемую процедуру отработки изменения статуса отправки
	ПриИзмененииСтатусаОтправкиДокумента(Отчет, СтатусОтправки);
	
КонецПроцедуры

Процедура ОбновитьСтатусОтправкиОтчетаВБанкРоссии(Отчет)
	
	// находим последнюю не помеченную на удаление отправку отчета
	ПоследняяОтправка = ПолучитьПоследнююОтправкуОтчетаВБанкРоссии(Отчет);
	
	// вычисляем значение статуса отправки (ПеречислениеСсылка.СтатусыОтправки)
	Если ЗначениеЗаполнено(ПоследняяОтправка) Тогда
		
		СтатусОтправки = ПоследняяОтправка.СтатусОтправки;
		Если СтатусОтправки = Перечисления.СтатусыОтправки.ПринятЕстьОшибки Тогда
			//введено после добавления дополнительного статуса ПринятЕстьОшибки, при котором отчет считается сданным
			СтатусОтправки = Перечисления.СтатусыОтправки.Сдан;
		КонецЕсли;
		
	Иначе
		СтатусОтправки = Неопределено;
	КонецЕсли;
	
	// сохраняем статус в базе
	ЗаписатьСтатусОтправкиОтчетаВБанкРоссии(Отчет, СтатусОтправки, ПоследняяОтправка);
	
КонецПроцедуры

Процедура ЗаписатьСтатусОтправкиОтчетаВБанкРоссии(Отчет, СтатусОтправки, ОснованиеСтатуса)
	
	Если ЗначениеЗаполнено(СтатусОтправки) Тогда
		МенЗап = РегистрыСведений.СтатусыОтправки.СоздатьМенеджерЗаписи();
		МенЗап.Объект = Отчет;
		МенЗап.Статус = СтатусОтправки;
		МенЗап.Основание = ОснованиеСтатуса;
		МенЗап.Записать(Истина);
	Иначе
		МенЗап = РегистрыСведений.СтатусыОтправки.СоздатьМенеджерЗаписи();
		МенЗап.Объект = Отчет;
		МенЗап.Прочитать();
		Если МенЗап.Выбран() Тогда
			МенЗап.Удалить();
		КонецЕсли;
	КонецЕсли;
	
	// вызываем переопределяемую процедуру отработки изменения статуса отправки
	ПриИзмененииСтатусаОтправкиДокумента(Отчет, СтатусОтправки);
	
КонецПроцедуры

Функция ПолучитьПустуюДатуЗавершенияОтправкиВБанкРоссии() Экспорт
	
	Возврат '39991231235959';
	
КонецФункции

Процедура ПередЗаписьюОтправкиВБанкРоссии(Объект, Отказ)
	
	Если НЕ ЗначениеЗаполнено(Объект.Период) Тогда
		Объект.Период = Формат(Объект.ДатаОкончанияПериода, "ДФ=yyyyMMdd") + Формат('39991231' - Объект.ДатаНачалаПериода, "ДФ=yyyyMMdd");
	КонецЕсли;
	
	Если Объект.СтатусОтправки = Перечисления.СтатусыОтправки.Отправлен Тогда
		
		Если НЕ ЗначениеЗаполнено(Объект.ДатаЗакрытия) Тогда
			Объект.ДатаЗакрытия = ПолучитьПустуюДатуЗавершенияОтправкиВБанкРоссии();
		КонецЕсли;
		
	Иначе
		Объект.ДатаЗакрытия = Объект.ДатаПолученияРезультата;
	КонецЕсли;
	
КонецПроцедуры

Процедура ПередЗаписьюОтправкиВМинобороны(Объект, Отказ)
	
	Если НЕ ЗначениеЗаполнено(Объект.Период) Тогда
		Объект.Период = Формат(Объект.ДатаОкончанияПериода, "ДФ=yyyyMMdd") + Формат('39991231' - Объект.ДатаНачалаПериода, "ДФ=yyyyMMdd");
	КонецЕсли;
	
	Если Объект.СтатусОтправки = Перечисления.СтатусыОтправки.Отправлен Тогда
		
		Если НЕ ЗначениеЗаполнено(Объект.ДатаЗакрытия) Тогда
			Объект.ДатаЗакрытия = ПолучитьПустуюДатуЗавершенияОтправкиВБанкРоссии();
		КонецЕсли;
		
	Иначе
		Объект.ДатаЗакрытия = Объект.ДатаПолученияРезультата;
	КонецЕсли;
	
КонецПроцедуры

Функция ЭтоФайлВыгрузкиРеестраНДС(КороткоеИмяФайла) Экспорт
	
	Возврат ЭлектронныйДокументооборотСКонтролирующимиОрганамиКлиентСервер.ЭтоФайлВыгрузкиРеестраНДС(КороткоеИмяФайла);
	
КонецФункции

Функция ЭтоФайлВыгрузкиРеестраАкцизов(КороткоеИмяФайла) Экспорт
	
	Возврат ЭлектронныйДокументооборотСКонтролирующимиОрганамиКлиентСервер.ЭтоФайлВыгрузкиРеестраАкцизов(КороткоеИмяФайла);
	
КонецФункции

Функция КНДРеестраФНСПоПрефиксуФайла(КороткоеИмяФайла) Экспорт
	
	КНД = Неопределено;
	
	Если Найти(КороткоеИмяФайла, "KO_RRTDCN23") > 0 Тогда 
		// РегламентированныйОтчетРеестрНДСДекларацииИТаможенныеДекларации23
		КНД = "1155126";
	ИначеЕсли Найти(КороткоеИмяФайла, "KO_RRDTEG") > 0 Тогда 
		// РегламентированныйОтчетРеестрНДСДекларацииНаЭкспрессГрузы
		КНД = "1155128";
	ИначеЕсли Найти(КороткоеИмяФайла, "KO_RRTDNDS") > 0 Тогда 
		// РегламентированныйОтчетРеестрНДСПриложение1
		КНД = "1155110";
	ИначеЕсли Найти(КороткоеИмяФайла, "KO_RRNFTSNDS") > 0 Тогда 
		// РегламентированныйОтчетРеестрНДСПриложение2
		КНД = "1155119";
	ИначеЕсли Найти(КороткоеИмяФайла, "KO_RRGAZNDS") > 0 Тогда
		// РегламентированныйОтчетРеестрНДСПриложение3
		КНД = "1155121";
	ИначеЕсли Найти(КороткоеИмяФайла, "KO_RRNEFTNDS") > 0 Тогда
		// РегламентированныйОтчетРеестрНДСПриложение4
		КНД = "1155120";
	ИначеЕсли Найти(КороткоеИмяФайла, "KO_RRTRDNDS") > 0 Тогда
		// РегламентированныйОтчетРеестрНДСПриложение5
		КНД = "1155111";
	ИначеЕсли Найти(КороткоеИмяФайла, "KO_RRDPRNDS") > 0 Тогда
		// РегламентированныйОтчетРеестрНДСПриложение6
		КНД = "1155117";
	ИначеЕсли Найти(КороткоеИмяФайла, "KO_RRZDNNDS") > 0 Тогда
		// РегламентированныйОтчетРеестрНДСПриложение7
		КНД = "1155112";
	ИначеЕсли Найти(КороткоеИмяФайла, "KO_RRTRINNDS") > 0 Тогда
		// РегламентированныйОтчетРеестрНДСПриложение8
		КНД = "1155113";
	ИначеЕсли Найти(КороткоеИмяФайла, "KO_RRAVNNDS") > 0 Тогда
		// РегламентированныйОтчетРеестрНДСПриложение9
		КНД = "1155114";
	ИначеЕсли Найти(КороткоеИмяФайла, "KO_RRMORNDS") > 0 Тогда
		// РегламентированныйОтчетРеестрНДСПриложение10
		КНД = "1155115";
	ИначеЕсли Найти(КороткоеИмяФайла, "KO_RRGDPRVZNDS") > 0 Тогда
		// РегламентированныйОтчетРеестрНДСПриложение11
		КНД = "1155122";
	ИначеЕсли Найти(КороткоеИмяФайла, "KO_RRAVPRVZNDS") > 0 Тогда
		// РегламентированныйОтчетРеестрНДСПриложение12
		КНД = "1155123";
	ИначеЕсли Найти(КороткоеИмяФайла, "KO_RRGDTRNDS") > 0 Тогда
		// РегламентированныйОтчетРеестрНДСПриложение13
		КНД = "1155116";
	ИначеЕсли Найти(КороткоеИмяФайла, "KO_RRPRVZNDS") > 0 Тогда
		// РегламентированныйОтчетРеестрНДСПриложение14
		КНД = "1155118";
	ИначеЕсли Найти(КороткоеИмяФайла, "KO_RR198.7.3TD") > 0 Тогда
		// РегламентированныйОтчетРеестрАкцизыПриложение1
		КНД = "1155124";
	ИначеЕсли Найти(КороткоеИмяФайла, "KO_RR198.7.34TD") > 0 Тогда
		// РегламентированныйОтчетРеестрАкцизыПриложение2
		КНД = "1155125";
	КонецЕсли;
	
	Возврат КНД;
	
КонецФункции

#КонецОбласти

Процедура ОтметитьДеревоПросмотренным(Новое) Экспорт
	
	УстановитьПривилегированныйРежим(Истина);
	
	СтрокиДерева = Новое.ПолучитьЭлементы();
	
	СсылкиДляОтметки = Новый Массив;
	
	Для Каждого СтрокаДерева Из СтрокиДерева Цикл
		Если ЗначениеЗаполнено(СтрокаДерева.Ссылка) Тогда
			СсылкиДляОтметки.Добавить(СтрокаДерева.Ссылка);
		КонецЕсли;
		
		ПодстрокиДерева = СтрокаДерева.ПолучитьЭлементы();
		Для Каждого ПодстрокаДерева Из ПодстрокиДерева Цикл
			Если ЗначениеЗаполнено(ПодстрокаДерева.Ссылка) Тогда
				СсылкиДляОтметки.Добавить(ПодстрокаДерева.Ссылка);
			КонецЕсли;
		КонецЦикла;
	КонецЦикла;
	
	Запрос = Новый Запрос;
	Запрос.Текст = 
		"ВЫБРАТЬ РАЗРЕШЕННЫЕ
		|	ЖурналНовыхСобытий.Ссылка КАК Ссылка,
		|	ЖурналНовыхСобытий.ПоказатьПользователю КАК ПоказатьПользователю
		|ИЗ
		|	РегистрСведений.ЖурналНовыхСобытий КАК ЖурналНовыхСобытий
		|ГДЕ
		|	ЖурналНовыхСобытий.Ссылка В(&Ссылка)
		|	И ЖурналНовыхСобытий.ПоказатьПользователю = ИСТИНА";
	
	Запрос.УстановитьПараметр("Ссылка", СсылкиДляОтметки);
	
	РезультатЗапроса = Запрос.Выполнить();
	
	ВыборкаДетальныеЗаписи = РезультатЗапроса.Выбрать();
	
	Пока ВыборкаДетальныеЗаписи.Следующий() Цикл
		ОтметитьСсылкуПоказаннойПользователю(ВыборкаДетальныеЗаписи.Ссылка);
	КонецЦикла;
	
КонецПроцедуры

Процедура ОтметитьСсылкуПоказаннойПользователю(Ссылка) Экспорт
	
	НаборЗаписей = РегистрыСведений.ЖурналНовыхСобытий.СоздатьНаборЗаписей();
	НаборЗаписей.Отбор.Ссылка.Установить(Ссылка);
	НаборЗаписей.Прочитать();
	
	Для каждого ЗаписьНабора Из НаборЗаписей Цикл
		ЗаписьНабора["ПоказатьПользователю"] = Ложь;
	КонецЦикла;
	
	НаборЗаписей.Записать();
	
КонецПроцедуры

#КонецОбласти

Функция СформироватьПараметрыЗапроса(Запрос) Экспорт
	
	СведенияОбОрганизации = РегламентированнаяОтчетностьВызовСервера.ПолучитьСведенияОбОрганизации(Запрос.Организация);
	
	ЭтоПБОЮЛ = НЕ РегламентированнаяОтчетностьВызовСервера.ЭтоЮридическоеЛицо(Запрос.Организация);
	СведенияОбОрганизации.Вставить("ЭтоПБОЮЛ", ЭтоПБОЮЛ);
	
	// определяем РегистрацияВИФНС
	ЭлРегистрацияВИФНС = Запрос.Получатель;
	Если НЕ ЗначениеЗаполнено(ЭлРегистрацияВИФНС)
		И (Запрос.ВидУслуги = Перечисления.ВидыУслугПриИОН.ПредставлениеСправкиОбИсполненииОбязанностейПоУплате
		ИЛИ Запрос.ТипСверки = Перечисления.ТипыСверокИОН.ВЦеломПоОрганизации) Тогда
		
		КодНалоговогоОрганаКонечногоПолучателяДокумента = СокрЛП(Запрос.Получатель.Код);
		Если НЕ ЗначениеЗаполнено(КодНалоговогоОрганаКонечногоПолучателяДокумента) И СведенияОбОрганизации.Свойство("КодНО") Тогда
			КодНалоговогоОрганаКонечногоПолучателяДокумента = СокрЛП(СведенияОбОрганизации.КодНО);
		КонецЕсли;
		ЭлРегистрацияВИФНС = РегистрацияВИФНСПоОрганизацииИКодуНО(
			Запрос.Организация,
			КодНалоговогоОрганаКонечногоПолучателяДокумента,,
			НЕ ЭтоПБОЮЛ,
			СведенияОбОрганизации);
	КонецЕсли;
		
	Если ЗначениеЗаполнено(ЭлРегистрацияВИФНС) И ЗначениеЗаполнено(ЭлРегистрацияВИФНС.Доверенность) Тогда
		ДоверенностьПредставителя = ЭлРегистрацияВИФНС.Доверенность;
		// признак подписания представителем
		СведенияОбОрганизации.Вставить("ПрПодп", 2);
		СведенияОбОрганизации.Вставить("Доверенность", ПолучитьСведенияДоверенности(ЭлРегистрацияВИФНС, Запрос.Организация));
	Иначе
		// признак подписания налогоплательщиком
		СведенияОбОрганизации.Вставить("ПрПодп", 1);
	КонецЕсли;
	
	Если ЭтоПБОЮЛ Тогда
		ФИОИП = ФИОИндивидуальногоПредпринимателя(Запрос.Организация, СведенияОбОрганизации);
		Если ФИОИП = Неопределено Тогда
			СведенияОбОрганизации.Вставить("ИПФамилия", "");
			СведенияОбОрганизации.Вставить("ИПИмя", "");
			СведенияОбОрганизации.Вставить("ИПОтчество", "");
		Иначе
			СведенияОбОрганизации.Вставить("ИПФамилия", ФИОИП.Фамилия);
			СведенияОбОрганизации.Вставить("ИПИмя", ФИОИП.Имя);
			СведенияОбОрганизации.Вставить("ИПОтчество", ФИОИП.Отчество);
		КонецЕсли;
	Иначе
		Если ЗначениеЗаполнено(ЭлРегистрацияВИФНС) Тогда
			СведенияОбОрганизации.Вставить("КППЮЛ", СокрЛП(ЭлРегистрацияВИФНС.КПП));
		Иначе
			Если СведенияОбОрганизации.Свойство("КППЮЛ") Тогда
				СведенияОбОрганизации.Вставить("КППЮЛ", СокрЛП(СведенияОбОрганизации.КППЮЛ));
			КонецЕсли;
		КонецЕсли;
	КонецЕсли;
	
	Возврат СведенияОбОрганизации;
	
КонецФункции

Функция РегистрацияВИФНСПоОрганизацииИКодуНО(
		Организация,
		КодНО,
		КПП = "",
		ЭтоЮридическоеЛицо = Неопределено,
		СведенияОбОрганизации = Неопределено,
		ЛюбойКПП = Ложь) Экспорт
	
	КППРегистрации = КПП;
	МассивКППУчетнойЗаписи = Новый Массив;
	
	Если НЕ ЗначениеЗаполнено(КППРегистрации) И НЕ ЛюбойКПП Тогда
		Если ЭтоЮридическоеЛицо = Неопределено Тогда
			ЭтоЮридическоеЛицо = РегламентированнаяОтчетностьВызовСервера.ЭтоЮридическоеЛицо(Организация);
		КонецЕсли;
		
		Если ЭтоЮридическоеЛицо Тогда
			Если СведенияОбОрганизации = Неопределено Тогда
				СведенияОбОрганизации = РегламентированнаяОтчетностьВызовСервера.ПолучитьСведенияОбОрганизации(Организация, , "КодНО, КППЮЛ");
			КонецЕсли;
			
			Если СокрЛП(СведенияОбОрганизации.КодНО) = СокрЛП(КодНО) Тогда
				КППРегистрации = СокрЛП(СведенияОбОрганизации.КППЮЛ);
			КонецЕсли;
			
			Если НЕ ЗначениеЗаполнено(КППРегистрации) Тогда
				Запрос = Новый Запрос(
					"ВЫБРАТЬ РАЗРЕШЕННЫЕ РАЗЛИЧНЫЕ
					|	ДополнительныеРеквизитыУчетнойЗаписиПолучатели.КПП КАК КПП
					|ИЗ
					|	РегистрСведений.ДополнительныеРеквизитыУчетнойЗаписиПолучатели КАК ДополнительныеРеквизитыУчетнойЗаписиПолучатели
					|		ВНУТРЕННЕЕ СОЕДИНЕНИЕ Справочник.Организации КАК Организации
					|		ПО Организации.УчетнаяЗаписьОбмена = ДополнительныеРеквизитыУчетнойЗаписиПолучатели.УчетнаяЗапись
					|		ВНУТРЕННЕЕ СОЕДИНЕНИЕ Справочник.УчетныеЗаписиДокументооборота КАК УчетныеЗаписиДокументооборота
					|		ПО УчетныеЗаписиДокументооборота.Ссылка = ДополнительныеРеквизитыУчетнойЗаписиПолучатели.УчетнаяЗапись
					|ГДЕ
					|	Организации.Ссылка = &Организация
					|	И ДополнительныеРеквизитыУчетнойЗаписиПолучатели.КодПолучателя = &Код
					|	И ДополнительныеРеквизитыУчетнойЗаписиПолучатели.ТипПолучателя =
					|		ЗНАЧЕНИЕ(Перечисление.ТипыКонтролирующихОрганов.ФНС)
					|	И Организации.ВидОбменаСКонтролирующимиОрганами =
					|		ЗНАЧЕНИЕ(Перечисление.ВидыОбменаСКонтролирующимиОрганами.ОбменВУниверсальномФормате)
					|	И УчетныеЗаписиДокументооборота.СпецоператорСвязи В (ЗНАЧЕНИЕ(Перечисление.СпецоператорыСвязи.КалугаАстрал),
					|		ЗНАЧЕНИЕ(Перечисление.СпецоператорыСвязи.Форус))");
				
				Запрос.УстановитьПараметр("Организация", Организация);
				Запрос.УстановитьПараметр("Код", КодНО);
				
				МассивКППУчетнойЗаписи = Запрос.Выполнить().Выгрузить().ВыгрузитьКолонку("КПП");
				
				Если МассивКППУчетнойЗаписи.Количество() = 1 Тогда
					КППРегистрации = МассивКППУчетнойЗаписи[0];
				КонецЕсли;
			КонецЕсли;
		КонецЕсли;
	КонецЕсли;
	
	Запрос = Новый Запрос("ВЫБРАТЬ РАЗРЕШЕННЫЕ
	                      |	РегистрацияВИФНС.Ссылка КАК Ссылка,
	                      |	ВЫБОР
	                      |		КОГДА РегистрацияВИФНС.Доверенность <> &ПустаяСсылкаДоверенность
	                      |			ТОГДА 1
	                      |		ИНАЧЕ 0
	                      |	КОНЕЦ КАК ДоверенностьЗаполнена,
						  |	РегистрацияВИФНС.КПП КАК КПП
	                      |ИЗ
	                      |	Справочник.РегистрацииВНалоговомОргане КАК РегистрацияВИФНС
	                      |ГДЕ
	                      |	(РегистрацияВИФНС.Владелец = &Организация
	                      |		ИЛИ РегистрацияВИФНС.Владелец = &ГоловнаяОрганизация)
						  |	И РегистрацияВИФНС.Код = &Код
						  |" + ?(ЗначениеЗаполнено(КППРегистрации), "И РегистрацияВИФНС.КПП = &КПП", "") + "
	                      |	И РегистрацияВИФНС.ПометкаУдаления = &ПометкаУдаления
	                      |
	                      |УПОРЯДОЧИТЬ ПО
	                      |	ДоверенностьЗаполнена УБЫВ");
						  
	Запрос.УстановитьПараметр("ПометкаУдаления", Ложь);
	Запрос.УстановитьПараметр("Организация", Организация);
	Запрос.УстановитьПараметр("ГоловнаяОрганизация", РегламентированнаяОтчетность.ГоловнаяОрганизация(Организация));
	Запрос.УстановитьПараметр("Код", КодНО);
	Запрос.УстановитьПараметр("ПустаяСсылкаДоверенность", Справочники.ДоверенностиНалогоплательщика.ПустаяСсылка());
	
	Если ЗначениеЗаполнено(КППРегистрации) Тогда
		Запрос.УстановитьПараметр("КПП", КППРегистрации);
	КонецЕсли;
	
	Выборка = Запрос.Выполнить().Выбрать();
	ПервыйРезультат = Неопределено;
	Пока Выборка.Следующий() Цикл
		Результат = Выборка.Ссылка;
		Если ПервыйРезультат = Неопределено Тогда
			ПервыйРезультат = Результат;
		КонецЕсли;
		
		Если ЗначениеЗаполнено(КППРегистрации) ИЛИ МассивКППУчетнойЗаписи.Количество() = 0
			ИЛИ МассивКППУчетнойЗаписи.Найти(Выборка.КПП) <> Неопределено Тогда
			
			Возврат Результат;
		КонецЕсли;
	КонецЦикла;
	
	Если ПервыйРезультат <> Неопределено Тогда
		Возврат ПервыйРезультат;
		
	ИначеЕсли НЕ ЗначениеЗаполнено(КПП) И НЕ ЛюбойКПП Тогда
		Возврат РегистрацияВИФНСПоОрганизацииИКодуНО(
			Организация,
			КодНО,
			КПП,
			ЭтоЮридическоеЛицо,
			СведенияОбОрганизации,
			Истина);
		
	Иначе
		Возврат Неопределено;
	КонецЕсли;
	
КонецФункции

Функция КППРегламентированногоОтчета(Документ)
	
	МетаданныеОтчета = Метаданные.НайтиПоТипу(ТипЗнч(Документ));
	Если МетаданныеОтчета.Реквизиты.Найти("КПП") <> Неопределено Тогда
		Возврат СокрЛП(ОбщегоНазначения.ЗначениеРеквизитаОбъекта(Документ, "КПП"));
	Иначе
		Возврат "";
	КонецЕсли;
	
КонецФункции

Функция ПолучитьСведенияДоверенности(РегистрацияВИФНС, Организация = Неопределено) Экспорт
	
	
	Если НЕ ЗначениеЗаполнено(РегистрацияВИФНС) ИЛИ НЕ ЗначениеЗаполнено(РегистрацияВИФНС.Доверенность) Тогда
		Возврат Неопределено;
	КонецЕсли;
	
	
	// структура с результатом
	Сведения = Новый Структура;
	
	Доверенность = РегистрацияВИФНС.Доверенность;
	Если Организация = Неопределено Тогда
		Организация = РегистрацияВИФНС.Владелец;
	КонецЕсли;
	
	
	// добавляем код НО
	Сведения.Вставить("КодНО", СокрЛП(РегистрацияВИФНС.Код));
	
	
	// формируем структуру с учетными сведениями о самой доверенности
	УчетныеСведенияОДоверенности = Новый Структура("Номер, Дата, ДатаОкончания", ?(ЗначениеЗаполнено(Доверенность.НомерДовер), СокрЛП(Доверенность.НомерДовер), "БН"), Доверенность.ДатаВыдачи, Доверенность.ДатаОкончания);
	Сведения.Вставить("УчСвДов", УчетныеСведенияОДоверенности);
	
	
	// формируем сведения о представляемом лице
	СведенияОПредставляемомЛице = Новый Структура;
	Если НЕ РегламентированнаяОтчетностьВызовСервера.ЭтоЮридическоеЛицо(Организация) Тогда
		
		СведенияОПредставляемомЛице.Вставить("ЯвляетсяФЛ", Истина);
		
		// получаем все сведения о ФЛ
		СведенияОргФЛ = РегламентированнаяОтчетностьВызовСервера.ПолучитьСведенияОбОрганизации(Организация, , "ИННФЛ, ОГРН, Гражд, ДатаРожд, ФИО, ИндексМЖ, КодСубъектМЖ, РайонМЖ, ГородМЖ, НПунктМЖ, УлицаМЖ, ДомМЖ, КорпусМЖ, КвартираМЖ, КодУдЛичн, СерияУдЛичн, НомерУдЛичн, ДатаУдЛичн, ОрганВыданУдЛичн, АдрМЖ");
		
		// общие сведения
		СведенияОПредставляемомЛице.Вставить("ИННФЛ", СокрЛП(СведенияОргФЛ.ИННФЛ));
		СведенияОПредставляемомЛице.Вставить("ОГРНИП", СокрЛП(СведенияОргФЛ.ОГРН));
		СведенияОПредставляемомЛице.Вставить("Гражданство", СокрЛП(СведенияОргФЛ.Гражд));
		СведенияОПредставляемомЛице.Вставить("ДатаРожд", СведенияОргФЛ.ДатаРожд);
		
		// формируем структуру с Ф.И.О.
		СведенияОПредставляемомЛице.Вставить("ФИО", РегламентированнаяОтчетность.РазложитьФИО(СведенияОргФЛ.ФИО));
		
		// формируем структуру с адресом предпринимателя
		АдресПредпринимателя = Новый Структура("Индекс, КодРегион, Район, Город, НаселПункт, Улица, Дом, Корпус, Кварт",
												СокрЛП(СведенияОргФЛ.ИндексМЖ), СокрЛП(СведенияОргФЛ.КодСубъектМЖ), СокрЛП(СведенияОргФЛ.РайонМЖ), СокрЛП(СведенияОргФЛ.ГородМЖ),
												СокрЛП(СведенияОргФЛ.НПунктМЖ), СокрЛП(СведенияОргФЛ.УлицаМЖ), СокрЛП(СведенияОргФЛ.ДомМЖ), СокрЛП(СведенияОргФЛ.КорпусМЖ), СокрЛП(СведенияОргФЛ.КвартираМЖ));
		Если НЕ ЗначениеЗаполнено(АдресПредпринимателя.КодРегион) Тогда
			АдресПредпринимателяРазложением = РегламентированнаяОтчетностьКлиентСервер.РазложитьАдрес(СведенияОргФЛ.АдрМЖ);
			АдресПредпринимателя.КодРегион = КодРегионаИзАдреса(АдресПредпринимателяРазложением);
		КонецЕсли;
		СведенияОПредставляемомЛице.Вставить("АдрРФ", АдресПредпринимателя);
		
		// формируем структуру со сведениями об удостоверении личности
		УдЛичнПредпринимателя = Новый Структура("ВидДок, СерНомДок, ДатаДок, ВыдДок, КодВыдДок", СокрЛП(СведенияОргФЛ.КодУдЛичн), СокрЛП(СокрЛП(СведенияОргФЛ.СерияУдЛичн) + " " + СокрЛП(СведенияОргФЛ.НомерУдЛичн)), СведенияОргФЛ.ДатаУдЛичн, СокрЛП(СведенияОргФЛ.ОрганВыданУдЛичн), "");
		СведенияОПредставляемомЛице.Вставить("УдЛичн", УдЛичнПредпринимателя);
		
	Иначе
		
		СведенияОПредставляемомЛице.Вставить("ЯвляетсяФЛ", Ложь);
		
		// получаем сведения о ЮЛ
		СведенияОргЮЛ = РегламентированнаяОтчетностьВызовСервера.ПолучитьСведенияОбОрганизации(Организация, , "НаимЮЛПол, ИННЮЛ, ОГРН, ОргИндекс, ОргКодСубъект, ОргРайон, ОргГород, ОргНПункт, ОргУлица, ОргДом, ОргКорпус, ОргКвартира, ФИОРук, ИННРук, АдрЮР");
		
		// общие сведения
		СведенияОПредставляемомЛице.Вставить("НаимОрг", СокрЛП(СведенияОргЮЛ.НаимЮЛПол));
		СведенияОПредставляемомЛице.Вставить("ИННЮЛ", СокрЛП(СведенияОргЮЛ.ИННЮЛ));
		СведенияОПредставляемомЛице.Вставить("КПП", СокрЛП(РегистрацияВИФНС.КПП));
		СведенияОПредставляемомЛице.Вставить("ОГРН", СокрЛП(СведенияОргЮЛ.ОГРН));
		
		// формируем структуру с адресом предпринимателя
		АдресЮЛ = Новый Структура("Индекс, КодРегион, Район, Город, НаселПункт, Улица, Дом, Корпус, Кварт",
									СокрЛП(СведенияОргЮЛ.ОргИндекс), СокрЛП(СведенияОргЮЛ.ОргКодСубъект), СокрЛП(СведенияОргЮЛ.ОргРайон), СокрЛП(СведенияОргЮЛ.ОргГород),
									СокрЛП(СведенияОргЮЛ.ОргНПункт), СокрЛП(СведенияОргЮЛ.ОргУлица), СокрЛП(СведенияОргЮЛ.ОргДом), СокрЛП(СведенияОргЮЛ.ОргКорпус), СокрЛП(СведенияОргЮЛ.ОргКвартира));
		Если НЕ ЗначениеЗаполнено(АдресЮЛ.КодРегион) Тогда
			АдресЮЛРазложением = РегламентированнаяОтчетностьКлиентСервер.РазложитьАдрес(СведенияОргЮЛ.АдрЮР);
			АдресЮЛ.КодРегион = КодРегионаИзАдреса(АдресЮЛРазложением);
		КонецЕсли;
		СведенияОПредставляемомЛице.Вставить("АдрРФ", АдресЮЛ);
		
		// формируем структуру со сведениями о руководителе
		ОрганизацияВДоверенности = Доверенность.Владелец;
		Если ЗначениеЗаполнено(ОрганизацияВДоверенности) И ОрганизацияВДоверенности <> Организация Тогда
			СведенияОргЮЛДоверенность = РегламентированнаяОтчетностьВызовСервера.ПолучитьСведенияОбОрганизации(ОрганизацияВДоверенности,, "ФИОРук, ИННРук");
		Иначе
			СведенияОргЮЛДоверенность = СведенияОргЮЛ;
		КонецЕсли;
		СвРукОргЮЛ = Новый Структура("ИННФЛ, ФИО", СокрЛП(СведенияОргЮЛДоверенность.ИННРук), РегламентированнаяОтчетность.РазложитьФИО(СведенияОргЮЛДоверенность.ФИОРук));
		СведенияОПредставляемомЛице.Вставить("СвРукОрг", СвРукОргЮЛ);
		
	КонецЕсли;
	
	Сведения.Вставить("СвПредст", СведенияОПредставляемомЛице);
	
	
	// формируем сведения о доверителе
	ПризнакДоверителя = Доверенность.ПризнакДоверителя;
	СвДовер = Новый Структура("ПрДов", ПризнакДоверителя);
	
	Если ПризнакДоверителя = 2 ИЛИ ПризнакДоверителя = 3 Тогда
		
		// выгружаем сведения о доверителе-ЮЛ
		Если Доверенность.ДоверительЯвляетсяЮЛ Тогда
			
			// общие сведения
			ДоверительЮЛ = Новый Структура("НаимОрг, ИННЮЛ, КПП, ОГРН", СокрЛП(Доверенность.ДоверительЮЛ_НаимОрг), СокрЛП(Доверенность.ДоверительЮЛ_ИНН), СокрЛП(Доверенность.ДоверительЮЛ_КПП), СокрЛП(Доверенность.ДоверительЮЛ_ОГРН));
			
			// адрес в РФ
			ДоверительЮЛ.Вставить("АдрЮЛ", ПолучитьАдресДоверенности(Доверенность, Перечисления.СубъектыДоверенностиНалогоплательщика.ДоверительЮЛ));
			
			// сведения о руководителе
			СвРукОрг = Новый Структура("ИННФЛ, ФИО", СокрЛП(Доверенность.ДоверительРук_ИНН), ПолучитьФИОДоверенности(Доверенность, Перечисления.СубъектыДоверенностиНалогоплательщика.ДоверительРук));
			ДоверительЮЛ.Вставить("СвРукОрг", СвРукОрг);
			
			СвДовер.Вставить("СвЮЛ", ДоверительЮЛ);
			
		КонецЕсли;
		
		// выгружаем сведения о довериетеле-ФЛ или об УЛ доверителя-ЮЛ
		Если НЕ Доверенность.ДоверительЯвляетсяЮЛ ИЛИ Доверенность.ДоверительИмеетУЛ Тогда
			
			// общие сведения
			ДоверительФЛ = Новый Структура("ИННФЛ, ОГРНИП, Гражданство, ДатаРожд", СокрЛП(Доверенность.ДоверительФЛ_ИНН), СокрЛП(Доверенность.ДоверительФЛ_ОГРН), СокрЛП(Доверенность.ДоверительФЛ_Гражданство.Код), Доверенность.ДоверительФЛ_ДатаРождения);
			
			// формируем ФИО
			ДоверительФЛ.Вставить("ФИО", ПолучитьФИОДоверенности(Доверенность, Перечисления.СубъектыДоверенностиНалогоплательщика.ДоверительФЛ));
			
			// формируем адрес
			ДоверительФЛ.Вставить("АдрРФ", ПолучитьАдресДоверенности(Доверенность, Перечисления.СубъектыДоверенностиНалогоплательщика.ДоверительФЛ));
			
			// формируем сведения об удостоверении личности
			ДоверительФЛ.Вставить("УдЛичн", ПолучитьУдЛичнДоверенности(Доверенность, Перечисления.СубъектыДоверенностиНалогоплательщика.ДоверительФЛ));
			
			СвДовер.Вставить("СвФЛ", ДоверительФЛ);
			
		КонецЕсли;
		
	КонецЕсли;
	
	Сведения.Вставить("СвДовер", СвДовер);
	
	
	// выгружаем сведения о представителе и его полномочиях
	СвУпПред = Новый Структура;
	
	// выгружаем сведения о представителе
	СвПред = Новый Структура;
	
	Если Доверенность.ПредставительЯвляетсяЮЛ Тогда
		СвПредставительЮЛ = Новый Структура("НаимОрг, ИННЮЛ, КПП, ОГРН", СокрЛП(Доверенность.ПредставительЮЛ_НаимОрг), СокрЛП(Доверенность.ПредставительЮЛ_ИНН), СокрЛП(Доверенность.ПредставительЮЛ_КПП), СокрЛП(Доверенность.ПредставительЮЛ_ОГРН));
		СвПредставительЮЛ.Вставить("АдрРФ", ПолучитьАдресДоверенности(Доверенность, Перечисления.СубъектыДоверенностиНалогоплательщика.ПредставительЮЛ));
		СвПред.Вставить("СвОрг", СвПредставительЮЛ);
	КонецЕсли;
	
	СвПредставительФЛ = Новый Структура("ИННФЛ, ОГРНИП, Гражданство, ДатаРожд", СокрЛП(Доверенность.ПредставительФЛ_ИНН), СокрЛП(Доверенность.ПредставительФЛ_ОГРН), СокрЛП(Доверенность.ПредставительФЛ_Гражданство.Код), Доверенность.ПредставительФЛ_ДатаРождения);
	СвПредставительФЛ.Вставить("ФИО", ПолучитьФИОДоверенности(Доверенность, Перечисления.СубъектыДоверенностиНалогоплательщика.ПредставительФЛ));
	СвПредставительФЛ.Вставить("АдрРФ", ПолучитьАдресДоверенности(Доверенность, Перечисления.СубъектыДоверенностиНалогоплательщика.ПредставительФЛ));
	СвПредставительФЛ.Вставить("УдЛичн", ПолучитьУдЛичнДоверенности(Доверенность, Перечисления.СубъектыДоверенностиНалогоплательщика.ПредставительФЛ));
	СвПредставительФЛ.Вставить("ЯвляетсяСотрудникомОрганизации", Доверенность.ПредставительЯвляетсяСотрудником);
	СвПред.Вставить("СведФизЛ", СвПредставительФЛ);
	
	СвУпПред.Вставить("СвПред", СвПред);
	
	СоответствиеРеквизитовКодамПолномочий = ПолучитьСоответствиеРеквизитовКодамПолномочийДоверенности();
	
	// выгружаем сведения о полномочиях
	ПолнДов = Новый Массив;
	Для Каждого ПолномочиеПредставителя Из Доверенность.ПолномочияПредставителя Цикл
		
		// собираем области полномочий
		ОбластиПолномочий = Новый Массив;
		Для Каждого Эл Из СоответствиеРеквизитовКодамПолномочий Цикл
			Если ПолномочиеПредставителя[Эл.Ключ] Тогда
				ОбластиПолномочий.Добавить(Эл.Значение);
			КонецЕсли;
		КонецЦикла;
		
		// собираем сведения о физ./юр.-лице по месту осуществления полномочий
		Если ЗначениеЗаполнено(ПолномочиеПредставителя.КПП) Тогда
			ОргФЛ = Новый Структура("ИННЮЛ, КПП", СокрЛП(Организация.ИНН), СокрЛП(ПолномочиеПредставителя.КПП));
		Иначе
			ОргФЛ = Новый Структура("ФЛ", СокрЛП(Организация.ИНН));
		КонецЕсли;
		
		ПолнДов.Добавить(Новый Структура("ПрОблПолн, ОргФЛ, ОКАТО", ОбластиПолномочий, ОргФЛ, СокрЛП(ПолномочиеПредставителя.ОКАТО)));
		
	КонецЦикла;
	СвУпПред.Вставить("ПолнДов", ПолнДов);
	
	Сведения.Вставить("СвУпПред", СвУпПред);
	
	
	// выгружаем сведения о нотариусе
	Если Доверенность.ЗаверенаНотариально Тогда
		
		Нотариус = Новый Структура;
		
		Если Доверенность.НотариусЯвляетсяЮЛ Тогда
			НотариусСвОрган = Новый Структура("НаимОрг, ИННЮЛ, КПП, ОГРН", СокрЛП(Доверенность.НотариусЮЛ_НаимОрг), СокрЛП(Доверенность.НотариусЮЛ_ИНН), СокрЛП(Доверенность.НотариусЮЛ_КПП), СокрЛП(Доверенность.НотариусЮЛ_ОГРН));
			Нотариус.Вставить("СвОрган", НотариусСвОрган);
		КонецЕсли;
		
		НотариусФЛФИО = ПолучитьФИОДоверенности(Доверенность, Перечисления.СубъектыДоверенностиНалогоплательщика.НотариусФЛ);
		НотариусСвФизЛица = Новый Структура("ИННФЛ, ФИО", СокрЛП(Доверенность.НотариусФЛ_ИНН), НотариусФЛФИО);
		
		НотариусФЛАдрес = ПолучитьАдресДоверенности(Доверенность, Перечисления.СубъектыДоверенностиНалогоплательщика.НотариусФЛ);
		Если ЗначениеЗаполнено(НотариусФЛАдрес) Тогда
			НотариусСвФизЛица.Вставить("АдрРФ", НотариусФЛАдрес);
		КонецЕсли;
		
		Нотариус.Вставить("СвФизЛиц", НотариусСвФизЛица);
		
		Сведения.Вставить("Нотариус", Нотариус);
		
	КонецЕсли;
	
	
	// возвращаем полученную структуру
	Возврат Сведения;
	
КонецФункции

Функция ПолучитьАдресДоверенности(Доверенность, ТипСубъекта)
	
	АдресДоверенности = Доверенность.Адреса.Найти(ТипСубъекта, "Владелец");
	Если АдресДоверенности = Неопределено Тогда
		Возврат Неопределено;
	Иначе
		Возврат Новый Структура("Индекс, КодРегион, Район, Город, НаселПункт, Улица, Дом, Корпус, Кварт",
								СокрЛП(АдресДоверенности.Индекс), СокрЛП(АдресДоверенности.КодРегион), СокрЛП(АдресДоверенности.Район), СокрЛП(АдресДоверенности.Город),
								СокрЛП(АдресДоверенности.НаселПункт), СокрЛП(АдресДоверенности.Улица), СокрЛП(АдресДоверенности.Дом), СокрЛП(АдресДоверенности.Корпус), СокрЛП(АдресДоверенности.Кварт));
	КонецЕсли;
	
КонецФункции

Функция ПолучитьФИОДоверенности(Доверенность, ТипСубъекта) Экспорт
	
	ФИОДоверенности = Доверенность.ФИО.Найти(ТипСубъекта, "Владелец");
	Если ФИОДоверенности = Неопределено Тогда
		Возврат Неопределено;
	Иначе
		Возврат Новый Структура("Фамилия, Имя, Отчество", СокрЛП(ФИОДоверенности.Фамилия), СокрЛП(ФИОДоверенности.Имя), СокрЛП(ФИОДоверенности.Отчество));
	КонецЕсли;
	
КонецФункции

Функция ПолучитьУдЛичнДоверенности(Доверенность, ТипСубъекта)
	
	УдЛичнДоверенности = Доверенность.УдЛичности.Найти(ТипСубъекта, "Владелец");
	Если УдЛичнДоверенности = Неопределено Тогда
		Возврат Неопределено;
	Иначе
		Возврат Новый Структура("ВидДок, СерНомДок, ДатаДок, ВыдДок, КодВыдДок",
								СокрЛП(РегламентированнаяОтчетностьПереопределяемый.ПолучитьКодВидаДокументаФизическогоЛица(УдЛичнДоверенности.ВидДок)), СокрЛП(СокрЛП(УдЛичнДоверенности.СерДок) + " " + СокрЛП(УдЛичнДоверенности.НомДок)), УдЛичнДоверенности.ДатаДок, СокрЛП(УдЛичнДоверенности.ВыдДок), СокрЛП(УдЛичнДоверенности.КодВыдДок));
	КонецЕсли;
	
КонецФункции

Функция ПолучитьСоответствиеРеквизитовКодамПолномочийДоверенности()
	
	СоответствиеРеквизитовКодамПолномочий = Новый Соответствие;
	СоответствиеРеквизитовКодамПолномочий.Вставить("Признак01", "01");
	СоответствиеРеквизитовКодамПолномочий.Вставить("Признак02", "02");
	СоответствиеРеквизитовКодамПолномочий.Вставить("Признак03", "03");
	СоответствиеРеквизитовКодамПолномочий.Вставить("Признак04", "04");
	СоответствиеРеквизитовКодамПолномочий.Вставить("Признак05", "05");
	СоответствиеРеквизитовКодамПолномочий.Вставить("Признак06", "06");
	СоответствиеРеквизитовКодамПолномочий.Вставить("Признак07", "07");
	СоответствиеРеквизитовКодамПолномочий.Вставить("Признак08", "08");
	СоответствиеРеквизитовКодамПолномочий.Вставить("Признак09", "09");
	СоответствиеРеквизитовКодамПолномочий.Вставить("Признак10", "10");
	СоответствиеРеквизитовКодамПолномочий.Вставить("Признак11", "11");
	СоответствиеРеквизитовКодамПолномочий.Вставить("Признак12", "12");
	СоответствиеРеквизитовКодамПолномочий.Вставить("Признак13", "13");
	СоответствиеРеквизитовКодамПолномочий.Вставить("Признак14", "14");
	СоответствиеРеквизитовКодамПолномочий.Вставить("Признак15", "15");
	СоответствиеРеквизитовКодамПолномочий.Вставить("Признак16", "16");
	СоответствиеРеквизитовКодамПолномочий.Вставить("Признак17", "17");
	СоответствиеРеквизитовКодамПолномочий.Вставить("Признак18", "18");
	СоответствиеРеквизитовКодамПолномочий.Вставить("Признак19", "19");
	СоответствиеРеквизитовКодамПолномочий.Вставить("Признак20", "20");
	СоответствиеРеквизитовКодамПолномочий.Вставить("Признак21", "21");
	СоответствиеРеквизитовКодамПолномочий.Вставить("Признак22", "22");
	СоответствиеРеквизитовКодамПолномочий.Вставить("Признак23", "23");
	СоответствиеРеквизитовКодамПолномочий.Вставить("Признак24", "24");
	СоответствиеРеквизитовКодамПолномочий.Вставить("Признак25", "25");
	СоответствиеРеквизитовКодамПолномочий.Вставить("ПризнакПолныеПолномочия", "99");
	
	Возврат СоответствиеРеквизитовКодамПолномочий;
	
КонецФункции

Процедура НараститьСчетчикВИмениФайлаВыгрузкиПриНеобходимости(ИмяФайла, Организация, ДатаОкончанияПериода, КодНО) Экспорт
	
	РасширениеСТочкой = нрег(Прав(ИмяФайла, 4));
	
	Если РасширениеСТочкой = ".xml" Тогда
		Возврат;
	КонецЕсли;
	
	Если РасширениеСТочкой = ".txt" Тогда
		ВхождениеТочки = СтрНайти(ИмяФайла, ".");
		ВхождениеСчетчика = ВхождениеТочки - 8;
		Если ВхождениеТочки <> 0 Тогда
			СчетчикФайлов = РегламентированнаяОтчетность.ПолучитьСчетчикВыгруженныхФайлов(Организация, , ДатаОкончанияПериода, КодНО);
			ИмяФайла = Лев(ИмяФайла, ВхождениеСчетчика - 1) + Формат(СчетчикФайлов, "ЧЦ=8; ЧН=; ЧВН=; ЧГ=") + ".txt";
			РегламентированнаяОтчетностьВызовСервера.УстановитьСчетчикВыгруженныхФайлов(Организация, СчетчикФайлов, , ДатаОкончанияПериода, КодНО);
		КонецЕсли;
	Иначе
		ВхождениеТочки = СтрНайти(ИмяФайла, ".");
		Если ВхождениеТочки <> 0 Тогда
			ИмяФайла = Лев(ИмяФайла, ВхождениеТочки - 1);
		КонецЕсли;
		СчетчикФайлов = РегламентированнаяОтчетность.ПолучитьСчетчикВыгруженныхФайлов(Организация, , ДатаОкончанияПериода, КодНО);
		ИмяФайла = ИмяФайла + "." + Формат(СчетчикФайлов, "ЧЦ=3; ЧН=; ЧВН=; ЧГ=");
		РегламентированнаяОтчетностьВызовСервера.УстановитьСчетчикВыгруженныхФайлов(Организация, СчетчикФайлов, , ДатаОкончанияПериода, КодНО);
	КонецЕсли;
	
КонецПроцедуры

Функция ВыгрузитьДоверенностьИНараститьСчетчикФайловПриНеобходимости(Контекст) Экспорт
	
	АдресФайлаДоверенности = Неопределено;
	КороткоеИмяФайлаДоверенности = Неопределено;
	ИмяФайлаВыгрузкиБылоИзменено = Ложь;
	
	// определяем КПП по которому искать регистрацию в ИФНС
	Если Контекст.ТипОтправляемогоДокумента = "РегламентированныйОтчет" Тогда
		КППДляПоиска = КППРегламентированногоОтчета(Контекст.Документ);
		
	Иначе
		ИмяФайлаВыгрузки = ВРЕГ(Контекст.ИмяФайлаВыгрузки);
		КомпонентыИмени = Неопределено;
		Если Лев(ИмяФайлаВыгрузки, 9) = "NO_NDFL2_" ИЛИ Лев(ИмяФайлаВыгрузки, 12) = "SR_ZPRUMNAL_" Тогда
			КомпонентыИмени = РазложитьСтрокуВМассивПодстрок(ИмяФайлаВыгрузки, "_");
		КонецЕсли;
		
		Если КомпонентыИмени <> Неопределено И КомпонентыИмени.Количество() >= 5 И СтрДлина(КомпонентыИмени[4]) = 19 Тогда
			КППДляПоиска = Сред(КомпонентыИмени[4], 11);
		Иначе
			КППДляПоиска = "";
		КонецЕсли;
	КонецЕсли;
	
	// ищем РегистрацияВИФНС по организации и коду налогового органа, а для регл. отчета еще по КПП
	ЭлРегистрацияВИФНС = РегистрацияВИФНСПоОрганизацииИКодуНО(Контекст.Организация, Контекст.КодНалоговогоОргана, КППДляПоиска);
	Если ЗначениеЗаполнено(ЭлРегистрацияВИФНС) Тогда
		
		// определяем доверенность
		ДоверенностьПредставителя = ОбщегоНазначения.ЗначениеРеквизитаОбъекта(ЭлРегистрацияВИФНС, "Доверенность");
		Если ЗначениеЗаполнено(ДоверенностьПредставителя) Тогда
		
			// формируем файл доверенности
			ИгнорироватьПризнакКрупнейшегоНалогоплательщика = ИгнорироватьНалоговыйОрганПосредника(Контекст.ИмяФайлаВыгрузки);
			ФайлДоверенности = ВыгрузитьДоверенностьПредставление(ЭлРегистрацияВИФНС, КороткоеИмяФайлаДоверенности, Контекст.Организация,,
				ИгнорироватьПризнакКрупнейшегоНалогоплательщика);
			
			Если НЕ ЗначениеЗаполнено(ФайлДоверенности) Тогда
				Возврат Ложь;
			КонецЕсли;
			
			// помещаем файл доверенности во временное хранилище
			АдресФайлаДоверенности = ПоместитьВоВременноеХранилище(Новый ДвоичныеДанные(ФайлДоверенности), Контекст.УникальныйИдентификатор);
			
			// получаем реквизиты доверенности
			// если представитель ЮЛ или [ФЛ и не сотрудник организации], то подменяем имя файла
			РеквизитыДоверенности = ОбщегоНазначения.ЗначенияРеквизитовОбъекта(ДоверенностьПредставителя, "ПредставительЯвляетсяЮЛ, ПредставительЯвляетсяСотрудником");
			Если РеквизитыДоверенности.ПредставительЯвляетсяЮЛ
			ИЛИ (НЕ РеквизитыДоверенности.ПредставительЯвляетсяЮЛ И НЕ РеквизитыДоверенности.ПредставительЯвляетсяСотрудником) Тогда
				Если НЕ ИзменитьФайлВыгрузкиПриОтправкеЧерезУполномоченногоПредставителя(
					Контекст.ТекстВыгрузки,
					Контекст.ИмяФайлаВыгрузки,
					ДоверенностьПредставителя,
					Контекст.КодНалоговогоОргана,
					Контекст.ДатаОкончанияПериодаОтчета,
					Контекст.Организация) Тогда
					Возврат Ложь;
				КонецЕсли;
				ИмяФайлаВыгрузкиБылоИзменено = Истина;
			КонецЕсли;
			
		КонецЕсли;
		
	КонецЕсли;
	
	// если имя файла выгрузки не было изменено в связи со сдачей от представителя
	// и при этом файл выгрузки - внешний (а значит, если он текстовый, то следует изменить счетчики)
	// то изменим счетчики в имени
	Если НЕ ИмяФайлаВыгрузкиБылоИзменено И Контекст.НеобходимоНараститьСчетчикФайлов Тогда
		НараститьСчетчикВИмениФайлаВыгрузкиПриНеобходимости(Контекст.ИмяФайлаВыгрузки, Контекст.Организация, Контекст.ДатаОкончанияПериодаОтчета, Контекст.КодНалоговогоОргана);
	КонецЕсли;
	
	Контекст.Вставить("ФайлДоверенности", АдресФайлаДоверенности);
	Контекст.Вставить("КороткоеИмяФайлаДоверенности", КороткоеИмяФайлаДоверенности);
	
	Возврат Истина;
	
КонецФункции

Функция ИзменитьФайлВыгрузкиПриОтправкеЧерезУполномоченногоПредставителя(
		ТекстВыгрузки,
		ИмяФайлаВыгрузки,
		Доверенность,
		КодНО,
		ДатаПериода = '00010101',
		Организация = Неопределено)
	
	ИмяФайлаБезРасширения = Лев(ИмяФайлаВыгрузки, СтрДлина(ИмяФайлаВыгрузки) - 4);
	РасширениеФайла = Прав(ИмяФайлаВыгрузки, 4);
	Если нрег(РасширениеФайла) = ".xml" Тогда // формат xml - меняем ИдФайл и имя файла
		
		// разбираем имя файла на компоненты
		КомпонентыИмени = РазложитьСтрокуВМассивПодстрок(ИмяФайлаБезРасширения, "_");
		Если КомпонентыИмени.Количество() < 5 Тогда
			ТекстСообщения = НСтр("ru = 'Невозможно изменить реквизиты отправителя в имени файла отчета: некорректное исходное имя файла выгрузки.'");
			ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
			Возврат Ложь;
		КонецЕсли;
		
		// получаем рег. данные представителя
		СведенияОПредставителе = ПолучитьИННКППУполномоченногоПредставителя(Доверенность);
		Если СведенияОПредставителе.ПредставительЯвляетсяЮЛ Тогда
			ИННКПП = СокрЛП(СведенияОПредставителе.ИНН) + СокрЛП(СведенияОПредставителе.КПП);
		Иначе
			ИННКПП = СокрЛП(СведенияОПредставителе.ИНН);
		КонецЕсли;
		
		// подменяем ИНН и КПП в компонентах имени файла
		Если КомпонентыИмени[0] = "ДОХОД" Тогда // 2-НДФЛ, старая форма
			КомпонентыИмени.Установить(3, ИННКПП);
		Иначе // прочие отчеты
			КомпонентыИмени.Установить(4, ИННКПП);
		КонецЕсли;
		
		// формируем новое имя файла
		НовоеИмяФайлаВыгрузкиБезРасширения = СтрСоединить(КомпонентыИмени, "_");
		
		// подменяем старое имя на новое в тексте выгрузки
		ТекстФайлаВыгрузки = ТекстВыгрузки;
		ПоместитьТекстВыгрузкиВХранилище = Ложь;
		Если ЭтоАдресВременногоХранилища(ТекстВыгрузки) Тогда
			ДвоичныеДанныеВыгрузки = ПолучитьИзВременногоХранилища(ТекстВыгрузки);
			Если ТипЗнч(ДвоичныеДанныеВыгрузки) = Тип("ДвоичныеДанные") Тогда
				ИмяВременногоФайла = ПолучитьИмяВременногоФайла();
				ДвоичныеДанныеВыгрузки.Записать(ИмяВременногоФайла);
				
				ОбъектЧтениеТекста = Новый ЧтениеТекста(ИмяВременногоФайла, "windows-1251");
				ТекстФайлаВыгрузки = ОбъектЧтениеТекста.Прочитать();
				ОбъектЧтениеТекста.Закрыть();
				
				ОперацииСФайламиЭДКО.УдалитьВременныйФайл(ИмяВременногоФайла);
				ПоместитьТекстВыгрузкиВХранилище = Истина;
			КонецЕсли;
		КонецЕсли;
		
		ТекстФайлаВыгрузки = СтрЗаменить(ТекстФайлаВыгрузки, ИмяФайлаБезРасширения, НовоеИмяФайлаВыгрузкиБезРасширения);
		
		Если ПоместитьТекстВыгрузкиВХранилище Тогда
			ИмяВременногоФайла = ПолучитьИмяВременногоФайла();
			
			ОбъектЗаписьТекста = Новый ЗаписьТекста(ИмяВременногоФайла, "windows-1251");
			ОбъектЗаписьТекста.Записать(ТекстФайлаВыгрузки);
			ОбъектЗаписьТекста.Закрыть();
			
			ДвоичныеДанныеВыгрузки = Новый ДвоичныеДанные(ИмяВременногоФайла);
			ТекстВыгрузки = ПоместитьВоВременноеХранилище(ДвоичныеДанныеВыгрузки, Новый УникальныйИдентификатор);
			ОперацииСФайламиЭДКО.УдалитьВременныйФайл(ИмяВременногоФайла);
		Иначе
			ТекстВыгрузки = ТекстФайлаВыгрузки;
		КонецЕсли;
		
		// присваиваем значение нового имени файла переменной со старым именем
		ИмяФайлаВыгрузки = НовоеИмяФайлаВыгрузкиБезРасширения + РасширениеФайла;
		
	Иначе // текстовый формат - меняем имя файла
		
		// разбираем имя файла на компоненты
		КомпонентыИмени = РазложитьСтрокуВМассивПодстрок(ИмяФайлаБезРасширения, "_");
		ЧислоКомпонентИмени = КомпонентыИмени.Количество();
		Если ЧислоКомпонентИмени <> 4 И ЧислоКомпонентИмени <> 5 Тогда // непонятный формат
			ТекстСообщения = НСтр("ru = 'Невозможно изменить реквизиты отправителя в имени файла отчета: некорректное исходное имя файла выгрузки.'");
			ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
			Возврат Ложь;
		КонецЕсли;
		
		// если файл от юрлица, то объединяем ИНН и КПП (первую и вторую составляющие) в одну компоненту
		Если СтрДлина(КомпонентыИмени[1]) = 9 Тогда // вторая компонента - КПП, а значит файл от юрлица
			КомпонентыИмени.Установить(0, КомпонентыИмени[0] + "_" + КомпонентыИмени[1]);
			КомпонентыИмени.Удалить(1);
		КонецЕсли;
		
		// формируем первую компоненту нового имени (с ИНН и КПП)
		СведенияОПредставителе = ПолучитьИННКППУполномоченногоПредставителя(Доверенность);
		Если СведенияОПредставителе.ПредставительЯвляетсяЮЛ Тогда
			КомпонентыИмени.Установить(0, "O" + СведенияОПредставителе.ИНН + "_" + СведенияОПредставителе.КПП);
		Иначе
			КомпонентыИмени.Установить(0, "O" + СведенияОПредставителе.ИНН);
		КонецЕсли;
		
		// устанавливаем новый счетчик файлов
		Если Организация <> Неопределено Тогда
			СчетчикВыгруженныхФайлов = РегламентированнаяОтчетность.ПолучитьСчетчикВыгруженныхФайлов(
				Организация,
				Перечисления.ТипыСчетчиковВыгрузки.СчетчикФайлов,
				ДатаПериода,
				КодНО);
			КомпонентыИмени.Установить(3, Формат(СчетчикВыгруженныхФайлов, "ЧЦ=8; ЧН=00000000; ЧВН=; ЧГ="));
			РегламентированнаяОтчетностьВызовСервера.УстановитьСчетчикВыгруженныхФайлов(
				Организация,
				СчетчикВыгруженныхФайлов,
				Перечисления.ТипыСчетчиковВыгрузки.СчетчикФайлов,
				ДатаПериода,
				КодНО);
		КонецЕсли;
		
		// формируем новое имя файла
		ИмяФайлаВыгрузки = СтрСоединить(КомпонентыИмени, "_") + РасширениеФайла;
		
	КонецЕсли;
	
	Возврат Истина;
	
КонецФункции

// Игнорировать признак крупнейшего налогоплательщика согласно письму ФНС России от 23.03.2016 № БС-4-11/4891@.
Функция ИгнорироватьНалоговыйОрганПосредника(СтрокаНачинающаясяСИмениФайлаВыгрузки)
	
	Результат = Ложь;
	
	ПрефиксыИмениФайлаИсключения = Новый Массив();
	ПрефиксыИмениФайлаИсключения.Добавить("NO_IMUD_");
	ПрефиксыИмениФайлаИсключения.Добавить("NO_IMUR_");
	// Разъясняющие письма ФНС России от 23 января 2017 № БС-4-11/993, от 10 января 2017 № БС-4/11-100.
	ПрефиксыИмениФайлаИсключения.Добавить("NO_RASCHSV_");
	
	Для каждого ПрефиксИмениФайлаИсключения Из ПрефиксыИмениФайлаИсключения Цикл
		Если Лев(СтрокаНачинающаясяСИмениФайлаВыгрузки, СтрДлина(ПрефиксИмениФайлаИсключения)) = ПрефиксИмениФайлаИсключения Тогда
			Результат = Истина;
			Прервать;
		КонецЕсли;
	КонецЦикла;
	
	Если Результат Тогда
		Инспекции = ОпределитьИнспекцииПоИмениФайла(СтрокаНачинающаясяСИмениФайлаВыгрузки);
		Результат = (Инспекции <> Неопределено И Инспекции.КодИФНС1 = Инспекции.КодИФНС2);
	КонецЕсли;
	
	Возврат Результат;
	
КонецФункции

Функция ПолучитьИННКППУполномоченногоПредставителя(Доверенность)
	
	Если Доверенность.ПредставительЯвляетсяЮЛ Тогда
		Возврат Новый Структура("ПредставительЯвляетсяЮЛ, ИНН, КПП", Доверенность.ПредставительЯвляетсяЮЛ, Доверенность.ПредставительЮЛ_ИНН, Доверенность.ПредставительЮЛ_КПП);
	Иначе
		Возврат Новый Структура("ПредставительЯвляетсяЮЛ, ИНН", Доверенность.ПредставительЯвляетсяЮЛ, Доверенность.ПредставительФЛ_ИНН);
	КонецЕсли;
	
КонецФункции

Функция ДобавитьУзел(Узел, Имя)
	
	НовУзел = Узел.Строки.Добавить();
	НовУзел.Имя = Имя;
	НовУзел.Тип = "Э";
	
	Возврат НовУзел;
	
КонецФункции

Функция ДобавитьЗначениеВыгрузки(Узел, Имя, Тип, Значение, ОбрезатьДо = Неопределено, ТолькоЕслиЗаполнен = Ложь)
	
	Если ТолькоЕслиЗаполнен И НЕ ЗначениеЗаполнено(Значение) Тогда
		Возврат Неопределено;
	КонецЕсли;
	
	НовУзел = Узел.Строки.Добавить();
	НовУзел.Имя = Имя;
	НовУзел.Тип = Тип;
	
	ЗначениеСтрока = СтроковоеПредставлениеЗначенияВыгрузки(Значение);
	ЗначениеУзла = ?(ОбрезатьДо = Неопределено, ЗначениеСтрока, СокрЛП(Лев(ЗначениеСтрока, ОбрезатьДо)));
	НовУзел.Значение = ЗначениеУзла;
	
	Возврат НовУзел;
	
КонецФункции

Функция ВыгрузитьДеревоДоверенностиВФайл(Узел, ПолноеИмяФайлаДоверенности, ОбъектЗапись = Неопределено)
	
	ЭтоВерхнийУровеньРекурсии = (ОбъектЗапись = Неопределено);
	Если ЭтоВерхнийУровеньРекурсии Тогда
		ОбъектЗапись = Новый ЗаписьXML;
		ОбъектЗапись.ОткрытьФайл(ПолноеИмяФайлаДоверенности, "windows-1251");
		ОбъектЗапись.ЗаписатьОбъявлениеXML();
	КонецЕсли;
	
	// выгружаем текущий элемент и подчиненные
	Если НЕ ЭтоВерхнийУровеньРекурсии Тогда
		Если Узел.Тип = "А" ИЛИ Узел.Тип = "A" Тогда 
			ОбъектЗапись.ЗаписатьАтрибут(Узел.Имя, Строка(Узел.Значение));
		Иначе
			ОбъектЗапись.ЗаписатьНачалоЭлемента(Узел.Имя);
			Для каждого Лист из Узел.Строки Цикл
				ВыгрузитьДеревоДоверенностиВФайл(Лист, ПолноеИмяФайлаДоверенности, ОбъектЗапись);
			КонецЦикла;
			ОбъектЗапись.ЗаписатьТекст(Строка(Узел.Значение));
			ОбъектЗапись.ЗаписатьКонецЭлемента();
		КонецЕсли;
	Иначе
		Для Каждого СтрУзел Из Узел.Строки Цикл
			ВыгрузитьДеревоДоверенностиВФайл(СтрУзел, ПолноеИмяФайлаДоверенности, ОбъектЗапись);
		КонецЦикла;
	КонецЕсли;
	
	Если ЭтоВерхнийУровеньРекурсии Тогда
		ОбъектЗапись.Закрыть();
		Возврат Истина;
	КонецЕсли;
	
КонецФункции

Функция СтроковоеПредставлениеЗначенияВыгрузки(ЗначениеВыгрузки)
	
	ТипЗначенияВыгрузки = ТипЗнч(ЗначениеВыгрузки);
	Если ТипЗначенияВыгрузки = Неопределено Тогда
		Возврат "";
	ИначеЕсли ТипЗначенияВыгрузки = Тип("Дата") Тогда
		Возврат Формат(ЗначениеВыгрузки, "ДФ=dd.MM.yyyy; ДП=");
	ИначеЕсли ТипЗначенияВыгрузки = Тип("Число") Тогда
		Возврат Формат(ЗначениеВыгрузки, "ЧН=; ЧГ=");
	ИначеЕсли ТипЗначенияВыгрузки = Тип("Булево") Тогда
		Возврат ?(ЗначениеВыгрузки, "true", "false");
	Иначе
		Возврат ЗначениеВыгрузки;
	КонецЕсли;
	
КонецФункции

Функция СформироватьИдФайлДоверенности(РегистрацияВИФНС, СведенияДоверенности, Префикс = "", Организация = Неопределено,
	КодНалоговогоОрганаКонечногоПолучателя = Неопределено, ИгнорироватьПризнакКрупнейшегоНалогоплательщика = Ложь)
	
	Если Организация = Неопределено Тогда
		Организация = РегистрацияВИФНС.Владелец;
	КонецЕсли;
	
	// R_T
	Если ЗначениеЗаполнено(Префикс) Тогда
		//для документооборота Представление
		R_T = Префикс;
	Иначе
		R_T = "NO_DOVER";
	КонецЕсли;
	
	// A_K
	НОКонечныйПолучатель = СведенияДоверенности.КодНО;
	НОПосредник = ?(Организация.КрупнейшийНалогоплательщик И НЕ ИгнорироватьПризнакКрупнейшегоНалогоплательщика,
		СокрЛП(Организация.КодНалоговогоОрганаПолучателя), "");
	A_K = ?(ЗначениеЗаполнено(НОПосредник), НОПосредник, НОКонечныйПолучатель) + "_"
		+ ?(ЗначениеЗаполнено(КодНалоговогоОрганаКонечногоПолучателя), КодНалоговогоОрганаКонечногоПолучателя, НОКонечныйПолучатель);
	
	Если СведенияДоверенности.СвУпПред.СвПред.Свойство("СвОрг") Тогда // представитель - юрлицо
		СвПредст = СведенияДоверенности.СвУпПред.СвПред.СвОрг;
		O = СокрЛП(СвПредст.ИННЮЛ) + СокрЛП(СвПредст.КПП);
	Иначе // представитель - физлицо
		Если СведенияДоверенности.СвУпПред.СвПред.СведФизЛ.ЯвляетсяСотрудникомОрганизации Тогда
			УчСвПредст = СведенияДоверенности.СвПредст;
			Если НЕ РегламентированнаяОтчетностьВызовСервера.ЭтоЮридическоеЛицо(Организация) Тогда
				O = СокрЛП(УчСвПредст.ИННФЛ);
			Иначе
				O = СокрЛП(УчСвПредст.ИННЮЛ) + СокрЛП(УчСвПредст.КПП);
			КонецЕсли;
		Иначе
			СвПредст = СведенияДоверенности.СвУпПред.СвПред.СведФизЛ;
			O = СокрЛП(СвПредст.ИННФЛ);
		КонецЕсли;
	КонецЕсли;
	
	// GGGGMMDD
	GGGGMMDD = Формат(ТекущаяДатаСеанса(), "ДФ=yyyyMMdd");
	
	// N
	N = ВРЕГ(Строка(Новый УникальныйИдентификатор));
	
	Возврат R_T + "_" + A_K + "_" + O + "_" + GGGGMMDD + "_" + N;
	
КонецФункции

Функция СформироватьФайлВыгрузкиДоверенностиПредставление(РегистрацияВИФНС, СведенияДоверенности, КороткоеИмяФайлаДоверенности = Неопределено, Организация = Неопределено,
	КодНалоговогоОрганаКонечногоПолучателя = Неопределено, ИгнорироватьПризнакКрупнейшегоНалогоплательщика = Ложь)
	
	ДеревоВыгрузки = Новый ДеревоЗначений;
	ДеревоВыгрузки.Колонки.Добавить("Имя");
	ДеревоВыгрузки.Колонки.Добавить("Тип");
	ДеревоВыгрузки.Колонки.Добавить("Значение");
	
	ИдФайл = СформироватьИдФайлДоверенности(РегистрацияВИФНС, СведенияДоверенности, "ON_DOV", Организация,
		КодНалоговогоОрганаКонечногоПолучателя, ИгнорироватьПризнакКрупнейшегоНалогоплательщика);
	
	
	// узел Файл
	УзелФайл = ДобавитьУзел(ДеревоВыгрузки, "Файл");
	ДобавитьЗначениеВыгрузки(УзелФайл, "ИдФайл", "А", ИдФайл, 255);
	ДобавитьЗначениеВыгрузки(УзелФайл, "ВерсПрог", "А", РегламентированнаяОтчетность.НазваниеИВерсияПрограммы(), 40);
	ДобавитьЗначениеВыгрузки(УзелФайл, "ВерсФорм", "А", "5.02");
	
	
	// узел Документ
	УзелДокумент = ДобавитьУзел(УзелФайл, "Документ");
	ДобавитьЗначениеВыгрузки(УзелДокумент, "КНД", "А", "1167005");
	КодНО = ?(ЗначениеЗаполнено(КодНалоговогоОрганаКонечногоПолучателя), КодНалоговогоОрганаКонечногоПолучателя, СведенияДоверенности.КодНО);
	ДобавитьЗначениеВыгрузки(УзелДокумент, "КодНО", "А", КодНО, 4);
	
	
	// узел УчСвДов
	УзелУчСвДов = ДобавитьУзел(УзелДокумент, "УчСвДов");
	
	УчСвДов = СведенияДоверенности.УчСвДов;
	ДобавитьЗначениеВыгрузки(УзелУчСвДов, "НомДовер", "А", УчСвДов.Номер);
	ДобавитьЗначениеВыгрузки(УзелУчСвДов, "ДатаНач", "А", УчСвДов.Дата);
	Если ЗначениеЗаполнено(УчСвДов.ДатаОкончания) Тогда
		ДобавитьЗначениеВыгрузки(УзелУчСвДов, "ДатаОкон", "А", УчСвДов.ДатаОкончания, , Истина);
	КонецЕсли;
	
	
	// узел СвПредст
	УзелСвПредст = ДобавитьУзел(УзелДокумент, "СвПредст");
	
	УчСвПредст = СведенияДоверенности.СвПредст;
	Если УчСвПредст.ЯвляетсяФЛ Тогда
		
		// узел НПФЛ
		УзелНПФЛ = ДобавитьУзел(УзелСвПредст, "НПФЛ");
		ДобавитьЗначениеВыгрузки(УзелНПФЛ, "ИННФЛ", "А", УчСвПредст.ИННФЛ, 12, Истина);
		ДобавитьЗначениеВыгрузки(УзелНПФЛ, "ОГРНИП", "А", УчСвПредст.ОГРНИП, 15, Истина);
		ДобавитьЗначениеВыгрузки(УзелНПФЛ, "Гражданство", "А", УчСвПредст.Гражданство, 3, Истина);
		ДобавитьЗначениеВыгрузки(УзелНПФЛ, "ДатаРожд", "А", УчСвПредст.ДатаРожд, , Истина);
		
		// узел ФИО
		УчФИО = УчСвПредст.ФИО;
		УзелФИО = ДобавитьУзел(УзелНПФЛ, "ФИО");
		ДобавитьЗначениеВыгрузки(УзелФИО, "Фамилия", "А", УчФИО.Фамилия, 60);
		ДобавитьЗначениеВыгрузки(УзелФИО, "Имя", "А", УчФИО.Имя, 60);
		ДобавитьЗначениеВыгрузки(УзелФИО, "Отчество", "А", УчФИО.Отчество, 60, Истина);
		
		// узел УдЛичн
		УчУдЛичн = УчСвПредст.УдЛичн;
		УзелУдЛичн = ДобавитьУзел(УзелНПФЛ, "УдЛичн");
		ДобавитьЗначениеВыгрузки(УзелУдЛичн, "КодВидДок", "А", УчУдЛичн.ВидДок, 2);
		ДобавитьЗначениеВыгрузки(УзелУдЛичн, "СерНомДок", "А", УчУдЛичн.СерНомДок, 25);
		ДобавитьЗначениеВыгрузки(УзелУдЛичн, "ДатаДок", "А", УчУдЛичн.ДатаДок, 10);
		ДобавитьЗначениеВыгрузки(УзелУдЛичн, "ВыдДок", "А", УчУдЛичн.ВыдДок, 255);
		ДобавитьЗначениеВыгрузки(УзелУдЛичн, "КодВыдДок", "А", УчУдЛичн.КодВыдДок, 7, Истина);
		
	Иначе
		
		// узел НПЮЛ
		УзелНПЮЛ = ДобавитьУзел(УзелСвПредст, "НПЮЛ");
		ДобавитьЗначениеВыгрузки(УзелНПЮЛ, "НаимОрг", "А", УчСвПредст.НаимОрг, 1000);
		ДобавитьЗначениеВыгрузки(УзелНПЮЛ, "ИННЮЛ", "А", УчСвПредст.ИННЮЛ, 10);
		ДобавитьЗначениеВыгрузки(УзелНПЮЛ, "КПП", "А", УчСвПредст.КПП, 9);
		ДобавитьЗначениеВыгрузки(УзелНПЮЛ, "ОГРН", "А", УчСвПредст.ОГРН, 13, Истина);
		
		// узел СвРукОрг
		УчСвРукОрг = УчСвПредст.СвРукОрг;
		УзелСвРукОрг = ДобавитьУзел(УзелНПЮЛ, "СвРукОрг");
		ДобавитьЗначениеВыгрузки(УзелСвРукОрг, "ИННФЛ", "А", УчСвРукОрг.ИННФЛ, 12, Истина);
		
		// узел ФИО
		УчСвРукОргФИО = УчСвРукОрг.ФИО;
		УзелФИО = ДобавитьУзел(УзелСвРукОрг, "ФИО");
		ДобавитьЗначениеВыгрузки(УзелФИО, "Фамилия", "А", УчСвРукОргФИО.Фамилия, 60);
		ДобавитьЗначениеВыгрузки(УзелФИО, "Имя", "А", УчСвРукОргФИО.Имя, 60);
		ДобавитьЗначениеВыгрузки(УзелФИО, "Отчество", "А", УчСвРукОргФИО.Отчество, 60, Истина);
		
	КонецЕсли;
	
	
	// узел СвДовер
	УзелСвДовер = ДобавитьУзел(УзелДокумент, "СвДовер");
	
	УчСвДовер = СведенияДоверенности.СвДовер;
	ПризнакДоверителя = ?(ЗначениеЗаполнено(УчСвДовер.ПрДов), УчСвДовер.ПрДов, 1);
	ДобавитьЗначениеВыгрузки(УзелСвДовер, "ПрДов", "А", ПризнакДоверителя, 1);
	
	Если ПризнакДоверителя = 2 ИЛИ ПризнакДоверителя = 3 Тогда
		
		УзелСвЮЛФЛ = ДобавитьУзел(УзелСвДовер, "СвЮЛФЛ");
		
		// выгружаем сведения о доверителе-ЮЛ
		Если УчСвДовер.Свойство("СвЮЛ") Тогда
			
			УчСвЮЛ = УчСвДовер.СвЮЛ;
			
			// узел ЮЛ
			УзелЮЛ = ДобавитьУзел(УзелСвЮЛФЛ, "ЮЛ");
			ДобавитьЗначениеВыгрузки(УзелЮЛ, "НаимОрг", "А", УчСвЮЛ.НаимОрг, 1000);
			ДобавитьЗначениеВыгрузки(УзелЮЛ, "ИННЮЛ", "А", УчСвЮЛ.ИННЮЛ, 10);
			ДобавитьЗначениеВыгрузки(УзелЮЛ, "КПП", "А", УчСвЮЛ.КПП, 9);
			ДобавитьЗначениеВыгрузки(УзелЮЛ, "ОГРН", "А", УчСвЮЛ.ОГРН, 13, Истина);
			
			// узел СвРукОрг
			УчСвРукОрг = УчСвЮЛ.СвРукОрг;
			УзелСвРукОрг = ДобавитьУзел(УзелЮЛ, "СвРукОрг");
			ДобавитьЗначениеВыгрузки(УзелСвРукОрг, "ИННФЛ", "А", УчСвРукОрг.ИННФЛ, 12, Истина);
			
			// узел ФИО
			УчСвРукОргФИО = УчСвРукОрг.ФИО;
			УзелФИО = ДобавитьУзел(УзелСвРукОрг, "ФИО");
			ДобавитьЗначениеВыгрузки(УзелФИО, "Фамилия", "А", УчСвРукОргФИО.Фамилия, 60);
			ДобавитьЗначениеВыгрузки(УзелФИО, "Имя", "А", УчСвРукОргФИО.Имя, 60);
			ДобавитьЗначениеВыгрузки(УзелФИО, "Отчество", "А", УчСвРукОргФИО.Отчество, 60, Истина);
			
		КонецЕсли;
		
		// выгружаем сведения о доверителе-ФЛ
		Если УчСвДовер.Свойство("СвФЛ") Тогда
			
			УчСвФЛ = УчСвДовер.СвФЛ;
			
			// узел ФЛ
			УзелФЛ = ДобавитьУзел(УзелСвЮЛФЛ, "ФЛ");
			ДобавитьЗначениеВыгрузки(УзелФЛ, "ИННФЛ", "А", УчСвФЛ.ИННФЛ, 12, Истина);
			ДобавитьЗначениеВыгрузки(УзелФЛ, "ОГРНИП", "А", УчСвФЛ.ОГРНИП, 15, Истина);
			ДобавитьЗначениеВыгрузки(УзелФЛ, "Гражданство", "А", УчСвФЛ.Гражданство, 3, Истина);
			ДобавитьЗначениеВыгрузки(УзелФЛ, "ДатаРожд", "А", УчСвФЛ.ДатаРожд, , Истина);
			
			// узел ФИО
			УчФИО = УчСвФЛ.ФИО;
			УзелФИО = ДобавитьУзел(УзелФЛ, "ФИО");
			ДобавитьЗначениеВыгрузки(УзелФИО, "Фамилия", "А", УчФИО.Фамилия, 60);
			ДобавитьЗначениеВыгрузки(УзелФИО, "Имя", "А", УчФИО.Имя, 60);
			ДобавитьЗначениеВыгрузки(УзелФИО, "Отчество", "А", УчФИО.Отчество, 60, Истина);
			
			// узел УдЛичн
			УчУдЛичн = УчСвФЛ.УдЛичн;
			УзелУдЛичн = ДобавитьУзел(УзелФЛ, "УдЛичн");
			ДобавитьЗначениеВыгрузки(УзелУдЛичн, "КодВидДок", "А", УчУдЛичн.ВидДок, 2);
			ДобавитьЗначениеВыгрузки(УзелУдЛичн, "СерНомДок", "А", УчУдЛичн.СерНомДок, 25);
			ДобавитьЗначениеВыгрузки(УзелУдЛичн, "ДатаДок", "А", УчУдЛичн.ДатаДок, 10);
			ДобавитьЗначениеВыгрузки(УзелУдЛичн, "ВыдДок", "А", УчУдЛичн.ВыдДок, 255);
			ДобавитьЗначениеВыгрузки(УзелУдЛичн, "КодВыдДок", "А", УчУдЛичн.КодВыдДок, 7, Истина);
			
		КонецЕсли;
		
	КонецЕсли;
	
	// узел СвУпПред
	УзелСвУпПред = ДобавитьУзел(УзелДокумент, "СвУпПред");
	СвУпПред = СведенияДоверенности.СвУпПред;
	
	// сведения о полномочиях
	Для Каждого СтрПолнДов Из СвУпПред.ПолнДов Цикл
		
		Для Каждого ЭлПрОблПолн Из СтрПолнДов.ПрОблПолн Цикл
			ДобавитьЗначениеВыгрузки(УзелСвУпПред, "ПрОблПолн", "Э", ЭлПрОблПолн);
		КонецЦикла;
		
	КонецЦикла;
	
	СвПред = СвУппред.СвПред;
	
	// сведения о представителе
	УзелСвПред = ДобавитьУзел(УзелСвУпПред, "СвПред");
	
	// сведения о представителе-ЮЛ
	Если СвПред.Свойство("СвОрг") Тогда
		
		СвОрг = СвПред.СвОрг;
		
		УзелСвОрг = ДобавитьУзел(УзелСвПред, "СвОрг");
		ДобавитьЗначениеВыгрузки(УзелСвОрг, "НаимОрг", "А", СвОрг.НаимОрг, 1000);
		ДобавитьЗначениеВыгрузки(УзелСвОрг, "ИННЮЛ", "А", СвОрг.ИННЮЛ, 10);
		ДобавитьЗначениеВыгрузки(УзелСвОрг, "КПП", "А", СвОрг.КПП, 9);
		ДобавитьЗначениеВыгрузки(УзелСвОрг, "ОГРН", "А", СвОрг.ОГРН, 13, Истина);
		
	// если представителем является физлицо - сотрудником представляемой организации,
	// то выгружаем в узел СвОрг сведения о представляемой организации
	ИначеЕсли СвПред.СведФизЛ.ЯвляетсяСотрудникомОрганизации Тогда
		
		// выгружаем сведения об организации представителя
		Если НЕ УчСвПредст.ЯвляетсяФЛ Тогда
			
			// узел НПЮЛ
			УзелСвОрг = ДобавитьУзел(УзелСвПред, "СвОрг");
			ДобавитьЗначениеВыгрузки(УзелСвОрг, "НаимОрг", "А", УчСвПредст.НаимОрг, 1000);
			ДобавитьЗначениеВыгрузки(УзелСвОрг, "ИННЮЛ", "А", УчСвПредст.ИННЮЛ, 10);
			ДобавитьЗначениеВыгрузки(УзелСвОрг, "КПП", "А", УчСвПредст.КПП, 9);
			ДобавитьЗначениеВыгрузки(УзелСвОрг, "ОГРН", "А", УчСвПредст.ОГРН, 13, Истина);
			
		КонецЕсли;
		
	КонецЕсли;
	
	
	// сведения о представителе-ФЛ
	СведФизЛ = СвПред.СведФизЛ;
	
	// узел СведФизЛ
	УзелСведФизЛ = ДобавитьУзел(УзелСвПред, "СведФизЛ");
	ДобавитьЗначениеВыгрузки(УзелСведФизЛ, "ИННФЛ", "А", СведФизЛ.ИННФЛ, 12, Истина);
	ДобавитьЗначениеВыгрузки(УзелСведФизЛ, "ОГРНИП", "А", СведФизЛ.ОГРНИП, 15, Истина);
	ДобавитьЗначениеВыгрузки(УзелСведФизЛ, "Гражданство", "А", СведФизЛ.Гражданство, 3, Истина);
	ДобавитьЗначениеВыгрузки(УзелСведФизЛ, "ДатаРожд", "А", СведФизЛ.ДатаРожд, , Истина);
	
	// узел ФИО
	УчФИО = СведФизЛ.ФИО;
	УзелФИО = ДобавитьУзел(УзелСведФизЛ, "ФИО");
	ДобавитьЗначениеВыгрузки(УзелФИО, "Фамилия", "А", УчФИО.Фамилия, 60);
	ДобавитьЗначениеВыгрузки(УзелФИО, "Имя", "А", УчФИО.Имя, 60);
	ДобавитьЗначениеВыгрузки(УзелФИО, "Отчество", "А", УчФИО.Отчество, 60, Истина);
	
	// узел УдЛичн
	УчУдЛичн = СведФизЛ.УдЛичн;
	УзелУдЛичн = ДобавитьУзел(УзелСведФизЛ, "УдЛичн");
	ДобавитьЗначениеВыгрузки(УзелУдЛичн, "КодВидДок", "А", УчУдЛичн.ВидДок, 2);
	ДобавитьЗначениеВыгрузки(УзелУдЛичн, "СерНомДок", "А", УчУдЛичн.СерНомДок, 25);
	ДобавитьЗначениеВыгрузки(УзелУдЛичн, "ДатаДок", "А", УчУдЛичн.ДатаДок, 10);
	ДобавитьЗначениеВыгрузки(УзелУдЛичн, "ВыдДок", "А", УчУдЛичн.ВыдДок, 255);
	ДобавитьЗначениеВыгрузки(УзелУдЛичн, "КодВыдДок", "А", УчУдЛичн.КодВыдДок, 7, Истина);
	
	// сведения о нотариусе
	Если СведенияДоверенности.Свойство("Нотариус") Тогда
		
		СвНотариус = СведенияДоверенности.Нотариус;
		УзелУдостов = ДобавитьУзел(УзелДокумент, "Удостов");
		
		// сведения о нотариусе-ЮЛ
		Если СвНотариус.Свойство("СвОрган") Тогда
			
			СвОрган = СвНотариус.СвОрган;
			
			УзелСвОрган = ДобавитьУзел(УзелУдостов, "СвОрган");
			ДобавитьЗначениеВыгрузки(УзелСвОрган, "НаимОрг", "А", СвОрган.НаимОрг, 1000);
			ДобавитьЗначениеВыгрузки(УзелСвОрган, "ИННЮЛ", "А", СвОрган.ИННЮЛ, 10);
			ДобавитьЗначениеВыгрузки(УзелСвОрган, "КПП", "А", СвОрган.КПП, 9);
			ДобавитьЗначениеВыгрузки(УзелСвОрган, "ОГРН", "А", СвОрган.ОГРН, 13, Истина);
			
		КонецЕсли;
		
		// сведения о нотариусе-ФЛ
		СвФизЛиц = СвНотариус.СвФизЛиц;
		
		УзелСвФизЛиц = ДобавитьУзел(УзелУдостов, "СвФизЛиц");
		ДобавитьЗначениеВыгрузки(УзелСвФизЛиц, "ИННФЛ", "А", СвФизЛиц.ИННФЛ, 12, Истина);
		
		СвФИО = СвФизЛиц.ФИО;
		УзелФИО = ДобавитьУзел(УзелСвФизЛиц, "ФИО");
		ДобавитьЗначениеВыгрузки(УзелФИО, "Фамилия", "А", СвФИО.Фамилия, 60);
		ДобавитьЗначениеВыгрузки(УзелФИО, "Имя", "А", СвФИО.Имя, 60);
		ДобавитьЗначениеВыгрузки(УзелФИО, "Отчество", "А", СвФИО.Отчество, 60, Истина);
		
	КонецЕсли;
	
	// выгружаем дерево в файл
	КороткоеИмяФайлаДоверенности = ИдФайл + ".xml";
	ПолноеИмяФайлаДоверенности = КаталогВременныхФайлов() + КороткоеИмяФайлаДоверенности;
	Если НЕ ВыгрузитьДеревоДоверенностиВФайл(ДеревоВыгрузки, ПолноеИмяФайлаДоверенности) Тогда
		Возврат Неопределено;
	КонецЕсли;
	
	Возврат ПолноеИмяФайлаДоверенности;
	
КонецФункции

Функция ВыгрузитьДоверенностьПредставление(РегистрацияВИФНС, КороткоеИмяФайлаДоверенности = Неопределено, Организация = Неопределено,
	КодНалоговогоОрганаКонечногоПолучателя = Неопределено, ИгнорироватьПризнакКрупнейшегоНалогоплательщика = Ложь) Экспорт
	
	// получаем сведения доверенности из справочника
	СведенияДоверенности = ПолучитьСведенияДоверенности(РегистрацияВИФНС, Организация);
	Если НЕ ЗначениеЗаполнено(СведенияДоверенности) Тогда
		Возврат Неопределено;
	КонецЕсли;
	
	// проверяем, возможна ли выгрузка
	Если НЕ ПроверитьВозможностьВыгрузкиДоверенности(СведенияДоверенности) Тогда
		Возврат Неопределено;
	КонецЕсли;
	
	Возврат СформироватьФайлВыгрузкиДоверенностиПредставление(РегистрацияВИФНС, СведенияДоверенности, КороткоеИмяФайлаДоверенности, Организация,
		КодНалоговогоОрганаКонечногоПолучателя, ИгнорироватьПризнакКрупнейшегоНалогоплательщика);
	
КонецФункции

Функция ПроверитьВозможностьВыгрузкиДоверенности(СведенияДоверенности)
	
	ОшибкиПроверки = Новый Массив;
	ПризнакОшибки = Ложь;
	
	
	// код НО
	Если СтрДлина(СведенияДоверенности.КодНО) <> 4 Тогда
		ЗарегистрироватьОшибкуПроверки(ПризнакОшибки, ОшибкиПроверки, "Указан некорректный код налогового органа!");
	КонецЕсли;
	
	///////////////////////////////////////////////////////////////////////////////
	
	// учетные сведения доверенности
	УчСвДов = СведенияДоверенности.УчСвДов;
	
	// Заполненность УчСвДов.Номер не анализируется, при отсутствии номера проставляется "БН"
	
	Если НЕ ЗначениеЗаполнено(УчСвДов.Дата) Тогда
		ЗарегистрироватьОшибкуПроверки(ПризнакОшибки, ОшибкиПроверки, "Не указана дата выдачи доверенности!");
	КонецЕсли;
	
	///////////////////////////////////////////////////////////////////////////////
	
	// сведения о представляемом лице
	СвПредст = СведенияДоверенности.СвПредст;
	Если СвПредст.ЯвляетсяФЛ Тогда
		
		// общие сведения представителя - ФЛ
		Если ЗначениеЗаполнено(СвПредст.ИННФЛ) И НЕ РегламентированнаяОтчетностьВызовСервера.ИННСоответствуетТребованиямНаСервере(СвПредст.ИННФЛ, Истина) Тогда
			ЗарегистрироватьОшибкуПроверки(ПризнакОшибки, ОшибкиПроверки, "Указан некорректный ИНН представляемого лица (" + СвПредст.ИННФЛ + ")!");
		КонецЕсли;
		
		Если ЗначениеЗаполнено(СвПредст.ОГРНИП) И СтрДлина(СвПредст.ОГРНИП) <> 15 Тогда
			ЗарегистрироватьОшибкуПроверки(ПризнакОшибки, ОшибкиПроверки, "Указан некорректный ОГРНИП представляемого лица (" + СвПредст.ОГРНИП + ")!");
		КонецЕсли;
		
		Если ЗначениеЗаполнено(СвПредст.Гражданство) И СтрДлина(СвПредст.Гражданство) <> 3 Тогда
			ЗарегистрироватьОшибкуПроверки(ПризнакОшибки, ОшибкиПроверки, "Указан некорректный код страны гражданства представляемого лица (" + СвПредст.Гражданство + ")!");
		КонецЕсли;
		
		// ФИО
		ФИОПредстФЛ = СвПредст.ФИО;
		Если НЕ ЗначениеЗаполнено(ФИОПредстФЛ.Фамилия) Тогда
			ЗарегистрироватьОшибкуПроверки(ПризнакОшибки, ОшибкиПроверки, "Не указана фамилия представляемого лица!");
		КонецЕсли;
		
		Если НЕ ЗначениеЗаполнено(ФИОПредстФЛ.Имя) Тогда
			ЗарегистрироватьОшибкуПроверки(ПризнакОшибки, ОшибкиПроверки, "Не указано имя представляемого лица!");
		КонецЕсли;
		
		// удостоверение личности
		УдЛичнПредстФЛ = СвПредст.УдЛичн;
		
		Если НЕ ЗначениеЗаполнено(УдЛичнПредстФЛ.ВидДок) Тогда
			ЗарегистрироватьОшибкуПроверки(ПризнакОшибки, ОшибкиПроверки, "Не указан код вида документа, удостоверяющего личность представляемого лица!");
		ИначеЕсли СтрДлина(УдЛичнПредстФЛ.ВидДок) <> 2 Тогда
			ЗарегистрироватьОшибкуПроверки(ПризнакОшибки, ОшибкиПроверки, "Указан некорректный код вида документа, удостоверяющего личность представляемого лица!");
		КонецЕсли;
		
		Если НЕ ЗначениеЗаполнено(УдЛичнПредстФЛ.СерНомДок) Тогда
			ЗарегистрироватьОшибкуПроверки(ПризнакОшибки, ОшибкиПроверки, "Не указаны серия и номер документа, удостоверяющего личность представляемого лица!");
		КонецЕсли;
		
		Если НЕ ЗначениеЗаполнено(УдЛичнПредстФЛ.ДатаДок) Тогда
			ЗарегистрироватьОшибкуПроверки(ПризнакОшибки, ОшибкиПроверки, "Не указана дата документа, удостоверяющего личность представляемого лица!");
		КонецЕсли;
		
		Если НЕ ЗначениеЗаполнено(УдЛичнПредстФЛ.ВыдДок) Тогда
			ЗарегистрироватьОшибкуПроверки(ПризнакОшибки, ОшибкиПроверки, "Не указано наименование органа, выдавшего документ, удостоверяющий личность представляемого лица!");
		КонецЕсли;
		
		Если ЗначениеЗаполнено(УдЛичнПредстФЛ.КодВыдДок) И СтрДлина(УдЛичнПредстФЛ.КодВыдДок) <> 7 Тогда
			ЗарегистрироватьОшибкуПроверки(ПризнакОшибки, ОшибкиПроверки, "Указан некорректный код подразделения органа, выдавшего документ, удостоверяющий личность представляемого лица!");
		КонецЕсли;
		
	Иначе
		
		// общие сведения
		Если НЕ ЗначениеЗаполнено(СвПредст.НаимОрг) Тогда
			ЗарегистрироватьОшибкуПроверки(ПризнакОшибки, ОшибкиПроверки, "Не указано наименование организации - представляемого лица!");
		КонецЕсли;
		
		Если НЕ ЗначениеЗаполнено(СвПредст.ИННЮЛ) Тогда
			ЗарегистрироватьОшибкуПроверки(ПризнакОшибки, ОшибкиПроверки, "Не указан ИНН организации - представляемого лица!");
		ИначеЕсли НЕ РегламентированнаяОтчетностьВызовСервера.ИННСоответствуетТребованиямНаСервере(СвПредст.ИННЮЛ, Ложь) Тогда
			ЗарегистрироватьОшибкуПроверки(ПризнакОшибки, ОшибкиПроверки, "Указан некорректный ИНН организации - представляемого лица (" + СвПредст.ИННЮЛ + ")!");
		КонецЕсли;
		
		Если НЕ ЗначениеЗаполнено(СвПредст.КПП) Тогда
			ЗарегистрироватьОшибкуПроверки(ПризнакОшибки, ОшибкиПроверки, "Не указан КПП организации - представляемого лица!");
		ИначеЕсли СтрДлина(СвПредст.КПП) <> 9 Тогда
			ЗарегистрироватьОшибкуПроверки(ПризнакОшибки, ОшибкиПроверки, "Указан некорректный КПП организации - представляемого лица (" + СвПредст.КПП + ")!");
		КонецЕсли;
		
		Если ЗначениеЗаполнено(СвПредст.ОГРН) И СтрДлина(СвПредст.ОГРН) <> 13 Тогда
			ЗарегистрироватьОшибкуПроверки(ПризнакОшибки, ОшибкиПроверки, "Указан некорректный ОГРН организации - представляемого лица (" + СвПредст.ОГРН + ")!");
		КонецЕсли;
		
		// сведения о руководителе
		СвРукПредстЮЛ = СвПредст.СвРукОрг;
		
		Если ЗначениеЗаполнено(СвРукПредстЮЛ.ИННФЛ) И НЕ РегламентированнаяОтчетностьВызовСервера.ИННСоответствуетТребованиямНаСервере(СвРукПредстЮЛ.ИННФЛ, Истина) Тогда
			ЗарегистрироватьОшибкуПроверки(ПризнакОшибки, ОшибкиПроверки, "Указан некорректный ИНН руководителя организации - представляемого лица!");
		КонецЕсли;
		
		ФИОРукПредстЮЛ = СвРукПредстЮЛ.ФИО;
		Если НЕ ЗначениеЗаполнено(ФИОРукПредстЮЛ.Фамилия) Тогда
			ЗарегистрироватьОшибкуПроверки(ПризнакОшибки, ОшибкиПроверки, "Не указана фамилия руководителя организации - представляемого лица!");
		КонецЕсли;
		Если НЕ ЗначениеЗаполнено(ФИОРукПредстЮЛ.Имя) Тогда
			ЗарегистрироватьОшибкуПроверки(ПризнакОшибки, ОшибкиПроверки, "Не указано имя руководителя организации - представляемого лица!");
		КонецЕсли;
		
	КонецЕсли;
	
	///////////////////////////////////////////////////////////////////////////////
	
	// сведения о представительстве (доверителе)
	СвДовер = СведенияДоверенности.СвДовер;
	Если СвДовер.ПрДов = 2 ИЛИ СвДовер.ПрДов = 3 Тогда
		
		// сведения о юрлице
		Если СвДовер.Свойство("СвЮЛ") Тогда
			
			СвДоверЮЛ = СвДовер.СвЮЛ;
			
			// общие сведения
			Если НЕ ЗначениеЗаполнено(СвДоверЮЛ.НаимОрг) Тогда
				ЗарегистрироватьОшибкуПроверки(ПризнакОшибки, ОшибкиПроверки, "Не указано наименование организации - доверителя!");
			КонецЕсли;
			
			Если НЕ ЗначениеЗаполнено(СвДоверЮЛ.ИННЮЛ) Тогда
				ЗарегистрироватьОшибкуПроверки(ПризнакОшибки, ОшибкиПроверки, "Не указан ИНН организации - доверителя!");
			ИначеЕсли НЕ РегламентированнаяОтчетностьВызовСервера.ИННСоответствуетТребованиямНаСервере(СвДоверЮЛ.ИННЮЛ, Ложь) Тогда
				ЗарегистрироватьОшибкуПроверки(ПризнакОшибки, ОшибкиПроверки, "Указан некорректный ИНН организации - доверителя (" + СвДоверЮЛ.ИННЮЛ + ")!");
			КонецЕсли;
			
			Если НЕ ЗначениеЗаполнено(СвДоверЮЛ.КПП) Тогда
				ЗарегистрироватьОшибкуПроверки(ПризнакОшибки, ОшибкиПроверки, "Не указан КПП организации - доверителя!");
			ИначеЕсли СтрДлина(СвДоверЮЛ.КПП) <> 9 Тогда
				ЗарегистрироватьОшибкуПроверки(ПризнакОшибки, ОшибкиПроверки, "Указан некорректный КПП организации - доверителя (" + СвДоверЮЛ.КПП + ")!");
			КонецЕсли;
			
			Если ЗначениеЗаполнено(СвДоверЮЛ.ОГРН) И СтрДлина(СвДоверЮЛ.ОГРН) <> 13 Тогда
				ЗарегистрироватьОшибкуПроверки(ПризнакОшибки, ОшибкиПроверки, "Указан некорректный ОГРН организации - доверителя (" + СвДоверЮЛ.ОГРН + ")!");
			КонецЕсли;
			
			// сведения о руководителе
			СвРукДоверЮЛ = СвДоверЮЛ.СвРукОрг;
			
			Если ЗначениеЗаполнено(СвРукДоверЮЛ.ИННФЛ) И НЕ РегламентированнаяОтчетностьВызовСервера.ИННСоответствуетТребованиямНаСервере(СвРукДоверЮЛ.ИННФЛ, Истина) Тогда
				ЗарегистрироватьОшибкуПроверки(ПризнакОшибки, ОшибкиПроверки, "Указан некорректный ИНН руководителя организации - доверителя!");
			КонецЕсли;
			
			ФИОРукДоверЮЛ = СвРукДоверЮЛ.ФИО;
			Если ЗначениеЗаполнено(ФИОРукДоверЮЛ) Тогда
				Если НЕ ЗначениеЗаполнено(ФИОРукДоверЮЛ.Фамилия) Тогда
					ЗарегистрироватьОшибкуПроверки(ПризнакОшибки, ОшибкиПроверки, "Не указана фамилия руководителя организации - доверителя!");
				КонецЕсли;
				Если НЕ ЗначениеЗаполнено(ФИОРукДоверЮЛ.Имя) Тогда
					ЗарегистрироватьОшибкуПроверки(ПризнакОшибки, ОшибкиПроверки, "Не указано имя руководителя организации - доверителя!");
				КонецЕсли;
			Иначе
				ЗарегистрироватьОшибкуПроверки(ПризнакОшибки, ОшибкиПроверки, "Не указано ФИО руководителя организации - доверителя!");
			КонецЕсли;
			
		КонецЕсли;
		
		// сведения о физлице
		Если СвДовер.Свойство("СвФЛ") Тогда
			
			СвДоверФЛ = СвДовер.СвФЛ;
			
			// общие сведения представителя - ФЛ
			Если ЗначениеЗаполнено(СвДоверФЛ.ИННФЛ) И НЕ РегламентированнаяОтчетностьВызовСервера.ИННСоответствуетТребованиямНаСервере(СвДоверФЛ.ИННФЛ, Истина) Тогда
				ЗарегистрироватьОшибкуПроверки(ПризнакОшибки, ОшибкиПроверки, "Указан некорректный ИНН физического лица - доверителя (" + СвДоверФЛ.ИННФЛ + ")!");
			КонецЕсли;
			
			Если ЗначениеЗаполнено(СвДоверФЛ.ОГРНИП) И СтрДлина(СвДоверФЛ.ОГРНИП) <> 15 Тогда
				ЗарегистрироватьОшибкуПроверки(ПризнакОшибки, ОшибкиПроверки, "Указан некорректный ОГРНИП физического лица - доверителя (" + СвДоверФЛ.ОГРНИП + ")!");
			КонецЕсли;
			
			Если ЗначениеЗаполнено(СвДоверФЛ.Гражданство) И СтрДлина(СвДоверФЛ.Гражданство) <> 3 Тогда
				ЗарегистрироватьОшибкуПроверки(ПризнакОшибки, ОшибкиПроверки, "Указан некорректный код страны гражданства физического лица - доверителя (" + СвДоверФЛ.Гражданство + ")!");
			КонецЕсли;
			
			// ФИО
			ФИОДоверФЛ = СвДоверФЛ.ФИО;
			Если ЗначениеЗаполнено(ФИОДоверФЛ) Тогда
				Если НЕ ЗначениеЗаполнено(ФИОДоверФЛ.Фамилия) Тогда
					ЗарегистрироватьОшибкуПроверки(ПризнакОшибки, ОшибкиПроверки, "Не указана фамилия физического лица - доверителя!");
				КонецЕсли;
				Если НЕ ЗначениеЗаполнено(ФИОДоверФЛ.Имя) Тогда
					ЗарегистрироватьОшибкуПроверки(ПризнакОшибки, ОшибкиПроверки, "Не указано имя физического лица - доверителя!");
				КонецЕсли;
			Иначе
				ЗарегистрироватьОшибкуПроверки(ПризнакОшибки, ОшибкиПроверки, "Не указано ФИО физического лица - доверителя!");
			КонецЕсли;
			
			// удостоверение личности
			УдЛичнДоверФЛ = СвДоверФЛ.УдЛичн;
			Если ЗначениеЗаполнено(УдЛичнДоверФЛ) Тогда
				
				Если НЕ ЗначениеЗаполнено(УдЛичнДоверФЛ.ВидДок) Тогда
					ЗарегистрироватьОшибкуПроверки(ПризнакОшибки, ОшибкиПроверки, "Не указан код вида документа, удостоверяющего личность физического лица - доверителя!");
				ИначеЕсли СтрДлина(УдЛичнДоверФЛ.ВидДок) <> 2 Тогда
					ЗарегистрироватьОшибкуПроверки(ПризнакОшибки, ОшибкиПроверки, "Указан некорректный код вида документа, удостоверяющего личность физического лица - доверителя!");
				КонецЕсли;
				
				Если НЕ ЗначениеЗаполнено(УдЛичнДоверФЛ.СерНомДок) Тогда
					ЗарегистрироватьОшибкуПроверки(ПризнакОшибки, ОшибкиПроверки, "Не указаны серия и номер документа, удостоверяющего личность физического лица - доверителя!");
				КонецЕсли;
				
				Если НЕ ЗначениеЗаполнено(УдЛичнДоверФЛ.ДатаДок) Тогда
					ЗарегистрироватьОшибкуПроверки(ПризнакОшибки, ОшибкиПроверки, "Не указана дата документа, удостоверяющего личность физического лица - доверителя!");
				КонецЕсли;
				
				Если НЕ ЗначениеЗаполнено(УдЛичнДоверФЛ.ВыдДок) Тогда
					ЗарегистрироватьОшибкуПроверки(ПризнакОшибки, ОшибкиПроверки, "Не указано наименование органа, выдавшего документ, удостоверяющий личность физического лица - доверителя!");
				КонецЕсли;
				
				Если ЗначениеЗаполнено(УдЛичнДоверФЛ.КодВыдДок) И СтрДлина(УдЛичнДоверФЛ.КодВыдДок) <> 7 Тогда
					ЗарегистрироватьОшибкуПроверки(ПризнакОшибки, ОшибкиПроверки, "Указан некорректный код подразделения органа, выдавшего документ, удостоверяющий личность физического лица - доверителя!");
				КонецЕсли;
				
			Иначе
				ЗарегистрироватьОшибкуПроверки(ПризнакОшибки, ОшибкиПроверки, "Не указаны сведения о документе, удостоверяющем личность физического лица - доверителя!");
			КонецЕсли;
			
		КонецЕсли;
		
	КонецЕсли;
	
	///////////////////////////////////////////////////////////////////////////////
	
	// сведения об уполномоченном представителе и его полномочиях
	СвУпПред = СведенияДоверенности.СвУпПред;
	
	СвПред = СвУпПред.СвПред;
	
	
	// сведения о представителе-ЮЛ
	Если СвПред.Свойство("СвОрг") Тогда
		
		СвОрг = СвПред.СвОрг;
		
		// общие сведения
		Если НЕ ЗначениеЗаполнено(СвОрг.НаимОрг) Тогда
			ЗарегистрироватьОшибкуПроверки(ПризнакОшибки, ОшибкиПроверки, "Не указано наименование организации - представителя!");
		КонецЕсли;
		
		Если НЕ ЗначениеЗаполнено(СвОрг.ИННЮЛ) Тогда
			ЗарегистрироватьОшибкуПроверки(ПризнакОшибки, ОшибкиПроверки, "Не указан ИНН организации - представителя!");
		ИначеЕсли НЕ РегламентированнаяОтчетностьВызовСервера.ИННСоответствуетТребованиямНаСервере(СвОрг.ИННЮЛ, Ложь) Тогда
			ЗарегистрироватьОшибкуПроверки(ПризнакОшибки, ОшибкиПроверки, "Указан некорректный ИНН организации - представителя (" + СвОрг.ИННЮЛ + ")!");
		КонецЕсли;
		
		Если НЕ ЗначениеЗаполнено(СвОрг.КПП) Тогда
			ЗарегистрироватьОшибкуПроверки(ПризнакОшибки, ОшибкиПроверки, "Не указан КПП организации - представителя!");
		ИначеЕсли СтрДлина(СвОрг.КПП) <> 9 Тогда
			ЗарегистрироватьОшибкуПроверки(ПризнакОшибки, ОшибкиПроверки, "Указан некорректный КПП организации - представителя (" + СвОрг.КПП + ")!");
		КонецЕсли;
		
		Если ЗначениеЗаполнено(СвОрг.ОГРН) И СтрДлина(СвОрг.ОГРН) <> 13 Тогда
			ЗарегистрироватьОшибкуПроверки(ПризнакОшибки, ОшибкиПроверки, "Указан некорректный ОГРН организации - представителя (" + СвОрг.ОГРН + ")!");
		КонецЕсли;
		
	КонецЕсли;
	
	
	// сведения о представителе-ФЛ
	СведПредстФЛ = СвПред.СведФизЛ;
	
	// общие сведения
	Если ЗначениеЗаполнено(СведПредстФЛ.ИННФЛ) И НЕ РегламентированнаяОтчетностьВызовСервера.ИННСоответствуетТребованиямНаСервере(СведПредстФЛ.ИННФЛ, Истина) Тогда
		ЗарегистрироватьОшибкуПроверки(ПризнакОшибки, ОшибкиПроверки, "Указан некорректный ИНН физического лица - представителя (" + СведПредстФЛ.ИННФЛ + ")!");
	КонецЕсли;
	
	Если ЗначениеЗаполнено(СведПредстФЛ.ОГРНИП) И СтрДлина(СведПредстФЛ.ОГРНИП) <> 15 Тогда
		ЗарегистрироватьОшибкуПроверки(ПризнакОшибки, ОшибкиПроверки, "Указан некорректный ОГРНИП физического лица - представителя (" + СведПредстФЛ.ОГРНИП + ")!");
	КонецЕсли;
	
	Если ЗначениеЗаполнено(СведПредстФЛ.Гражданство) И СтрДлина(СведПредстФЛ.Гражданство) <> 3 Тогда
		ЗарегистрироватьОшибкуПроверки(ПризнакОшибки, ОшибкиПроверки, "Указан некорректный код страны гражданства физического лица - представителя (" + СведПредстФЛ.Гражданство + ")!");
	КонецЕсли;
	
	// ФИО
	ФИОПредстФЛ = СведПредстФЛ.ФИО;
	Если ЗначениеЗаполнено(ФИОПредстФЛ) Тогда
		Если НЕ ЗначениеЗаполнено(ФИОПредстФЛ.Фамилия) Тогда
			ЗарегистрироватьОшибкуПроверки(ПризнакОшибки, ОшибкиПроверки, "Не указана фамилия физического лица - представителя!");
		КонецЕсли;
		Если НЕ ЗначениеЗаполнено(ФИОПредстФЛ.Имя) Тогда
			ЗарегистрироватьОшибкуПроверки(ПризнакОшибки, ОшибкиПроверки, "Не указано имя физического лица - представителя!");
		КонецЕсли;
	Иначе
		ЗарегистрироватьОшибкуПроверки(ПризнакОшибки, ОшибкиПроверки, "Не указано ФИО физического лица - представителя!");
	КонецЕсли;
	
	// удостоверение личности
	УдЛичнПредстФЛ = СведПредстФЛ.УдЛичн;
	
	Если ЗначениеЗаполнено(УдЛичнПредстФЛ) Тогда
		
		Если НЕ ЗначениеЗаполнено(УдЛичнПредстФЛ.ВидДок) Тогда
			ЗарегистрироватьОшибкуПроверки(ПризнакОшибки, ОшибкиПроверки, "Не указан код вида документа, удостоверяющего личность физического лица - представителя!");
		ИначеЕсли СтрДлина(УдЛичнПредстФЛ.ВидДок) <> 2 Тогда
			ЗарегистрироватьОшибкуПроверки(ПризнакОшибки, ОшибкиПроверки, "Указан некорректный код вида документа, удостоверяющего личность физического лица - представителя!");
		КонецЕсли;
		
		Если НЕ ЗначениеЗаполнено(УдЛичнПредстФЛ.СерНомДок) Тогда
			ЗарегистрироватьОшибкуПроверки(ПризнакОшибки, ОшибкиПроверки, "Не указаны серия и номер документа, удостоверяющего личность физического лица - представителя!");
		КонецЕсли;
		
		Если НЕ ЗначениеЗаполнено(УдЛичнПредстФЛ.ДатаДок) Тогда
			ЗарегистрироватьОшибкуПроверки(ПризнакОшибки, ОшибкиПроверки, "Не указана дата документа, удостоверяющего личность физического лица - представителя!");
		КонецЕсли;
		
		Если НЕ ЗначениеЗаполнено(УдЛичнПредстФЛ.ВыдДок) Тогда
			ЗарегистрироватьОшибкуПроверки(ПризнакОшибки, ОшибкиПроверки, "Не указано наименование органа, выдавшего документ, удостоверяющий личность физического лица - представителя!");
		КонецЕсли;
		
		Если ЗначениеЗаполнено(УдЛичнПредстФЛ.КодВыдДок) И СтрДлина(УдЛичнПредстФЛ.КодВыдДок) <> 7 Тогда
			ЗарегистрироватьОшибкуПроверки(ПризнакОшибки, ОшибкиПроверки, "Указан некорректный код подразделения органа, выдавшего документ, удостоверяющий личность физического лица - представителя!");
		КонецЕсли;
		
	Иначе
		ЗарегистрироватьОшибкуПроверки(ПризнакОшибки, ОшибкиПроверки, "Не указаны сведения о документе, удостоверяющем личность физического лица - представителя!");
	КонецЕсли;
	
	// сведения о полномочиях представителя
	ПолнДов = СвУпПред.ПолнДов;
	Если НЕ ЗначениеЗаполнено(ПолнДов) Тогда
		ЗарегистрироватьОшибкуПроверки(ПризнакОшибки, ОшибкиПроверки, "Не определены полномочия представителя!");
	Иначе
	
		НомПП = 0;
		Для Каждого ЭлПолнДов Из ПолнДов Цикл
			
			НомПП = НомПП + 1;
			
			ОбластиПолномочий = ЭлПолнДов.ПрОблПолн;
			ОргФЛ = ЭлПолнДов.ОргФЛ;
			ОКАТО = ЭлПолнДов.ОКАТО;
			
			Если ОбластиПолномочий.Количество() = 0 Тогда
				ЗарегистрироватьОшибкуПроверки(ПризнакОшибки, ОшибкиПроверки, "Не определены полномочия в строке " + НомПП + " таблицы полномочий!");
			КонецЕсли;
			
			Если НЕ ОргФЛ.Свойство("ФЛ") Тогда
				Если ЗначениеЗаполнено(ОргФЛ.КПП) И СтрДлина(ОргФЛ.КПП) <> 9 Тогда
					ЗарегистрироватьОшибкуПроверки(ПризнакОшибки, ОшибкиПроверки, "В строке " + НомПП + " таблицы полномочий определен некорректный КПП!");
				КонецЕсли;
			КонецЕсли;
			
			Если ЗначениеЗаполнено(ОКАТО) И СтрДлина(ОКАТО) <> 11 Тогда
				ЗарегистрироватьОшибкуПроверки(ПризнакОшибки, ОшибкиПроверки, "В строке " + НомПП + " таблицы полномочий определен некорректный ОКАТО!");
			КонецЕсли;
			
		КонецЦикла;
		
	КонецЕсли;
	
	///////////////////////////////////////////////////////////////////////////////
	
	// сведения о нотариусе
	Если СведенияДоверенности.Свойство("Нотариус") Тогда
		
		СвНотариус = СведенияДоверенности.Нотариус;
		
		Если СвНотариус.Свойство("СвОрган") Тогда
			
			СвОрган = СвНотариус.СвОрган;
			
			// общие сведения
			Если НЕ ЗначениеЗаполнено(СвОрган.НаимОрг) Тогда
				ЗарегистрироватьОшибкуПроверки(ПризнакОшибки, ОшибкиПроверки, "Не указано наименование организации нотариуса!");
			КонецЕсли;
			
			Если НЕ ЗначениеЗаполнено(СвОрган.ИННЮЛ) Тогда
				ЗарегистрироватьОшибкуПроверки(ПризнакОшибки, ОшибкиПроверки, "Не указан ИНН организации нотариуса!");
			ИначеЕсли НЕ РегламентированнаяОтчетностьВызовСервера.ИННСоответствуетТребованиямНаСервере(СвОрган.ИННЮЛ, Ложь) Тогда
				ЗарегистрироватьОшибкуПроверки(ПризнакОшибки, ОшибкиПроверки, "Указан некорректный ИНН организации нотариуса (" + СвОрган.ИННЮЛ + ")!");
			КонецЕсли;
			
			Если НЕ ЗначениеЗаполнено(СвОрган.КПП) Тогда
				ЗарегистрироватьОшибкуПроверки(ПризнакОшибки, ОшибкиПроверки, "Не указан КПП организации нотариуса!");
			ИначеЕсли СтрДлина(СвОрган.КПП) <> 9 Тогда
				ЗарегистрироватьОшибкуПроверки(ПризнакОшибки, ОшибкиПроверки, "Указан некорректный КПП организации нотариуса (" + СвОрган.КПП + ")!");
			КонецЕсли;
			
			Если ЗначениеЗаполнено(СвОрган.ОГРН) И СтрДлина(СвОрган.ОГРН) <> 13 Тогда
				ЗарегистрироватьОшибкуПроверки(ПризнакОшибки, ОшибкиПроверки, "Указан некорректный ОГРН организации нотариуса (" + СвОрган.ОГРН + ")!");
			КонецЕсли;
			
		КонецЕсли;
		
		СвФизЛиц = СвНотариус.СвФизЛиц;
		Если ЗначениеЗаполнено(СвФизЛиц.ИННФЛ) И НЕ РегламентированнаяОтчетностьВызовСервера.ИННСоответствуетТребованиямНаСервере(СвФизЛиц.ИННФЛ, Истина) Тогда
			ЗарегистрироватьОшибкуПроверки(ПризнакОшибки, ОшибкиПроверки, "Указан некорректный ИНН нотариуса - физического лица (" + СвФизЛиц.ИННФЛ + ")!");
		КонецЕсли;
		
		ФИОНотариусаФЛ = СвФизЛиц.ФИО;
		Если ЗначениеЗаполнено(ФИОНотариусаФЛ) Тогда
			Если НЕ ЗначениеЗаполнено(ФИОНотариусаФЛ.Фамилия) Тогда
				ЗарегистрироватьОшибкуПроверки(ПризнакОшибки, ОшибкиПроверки, "Не указана фамилия нотариуса - физического лица!");
			КонецЕсли;
			Если НЕ ЗначениеЗаполнено(ФИОНотариусаФЛ.Имя) Тогда
				ЗарегистрироватьОшибкуПроверки(ПризнакОшибки, ОшибкиПроверки, "Не указано имя нотариуса - физического лица!");
			КонецЕсли;
		Иначе
			ЗарегистрироватьОшибкуПроверки(ПризнакОшибки, ОшибкиПроверки, "Не указано ФИО нотариуса - физического лица!");
		КонецЕсли;
		
	КонецЕсли;
	
	Если ОшибкиПроверки.Количество() > 0 Тогда
		ТекстСообщения = НСтр("ru = 'Не удалось выгрузить сведения о доверенности, выданной представителю:'");
		ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
		Для Каждого ТекстОшибки Из ОшибкиПроверки Цикл
			ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстОшибки);
		КонецЦикла;
	КонецЕсли;
	
	Возврат НЕ ПризнакОшибки;
	
КонецФункции

Функция ОпределитьИнспекцииПоИмениФайла(ИмяФайла) Экспорт
	
	Подстроки = РазложитьСтрокуВМассивПодстрок(ИмяФайла, "_");
	Если НЕ ЗначениеЗаполнено(Подстроки) ИЛИ Подстроки.Количество() < 4 Тогда
		Возврат Неопределено;
	КонецЕсли;
	
	Если нрег(РасширениеФайла(ИмяФайла)) = "txt" Тогда // это текстовый формат ФНС (2.00, 2.01, 3.00)
		
		Если СтрДлина(Подстроки[0]) = 13 Тогда // если отчет по физлицу, то коды ИФНС во второй компоненте имени
			СтрКодыИФНС = Подстроки[1];
		Иначе // иначе - в третьей
			СтрКодыИФНС = Подстроки[2];
		КонецЕсли;
		
		Если СтрДлина(СтрКодыИФНС) = 8 Тогда
			Возврат Новый Структура("КодИФНС1, КодИФНС2", Лев(СтрКодыИФНС, 4), Прав(СтрКодыИФНС, 4));
		КонецЕсли;
		
	Иначе
		
		ПервыйКодИФНС = СокрЛП(Подстроки[2]); // третья по порядку подстрока - первый код ИФНС
		ВторойКодИФНС = СокрЛП(Подстроки[3]); // четвертая по порядку подстрока - второй код ИФНС
		
		Если СтрДлина(ПервыйКодИФНС) <> 4 Тогда // если первый код не четырехзначный - значит есть проблемы
			Возврат Неопределено;
		КонецЕсли;
		
		Если СтрДлина(ВторойКодИФНС) <> 4 Тогда // если второй код отсутствует, то он равен первому (например, 2-НДФЛ)
			ВторойКодИФНС = ПервыйКодИФНС;
		КонецЕсли;
		
		Возврат Новый Структура("КодИФНС1, КодИФНС2", ПервыйКодИФНС, ВторойКодИФНС);
		
	КонецЕсли;
	
КонецФункции

Процедура ЗарегистрироватьОшибкуПроверки(ПризнакОшибки, ОшибкиПроверки, ТекстОшибки)
	
	ПризнакОшибки = Истина;
	ОшибкиПроверки.Добавить(ТекстОшибки);
	
КонецПроцедуры

Функция Отчет2НДФЛСданЧастично(Объект) Экспорт
	
	ТранспортноеСообщение = Объект.Ссылка;
	
	Если ЭтоПротокол2НДФЛ_КНД1110307(ТранспортноеСообщение) Тогда

		Сведения = СведенияИзПротокола2НДФЛ_1110307(ТранспортноеСообщение);
	    Возврат Сведения.Представленные <> Сведения.Принятые И Сведения.Принятые > 0;
		
	Иначе
	
		// ищем протокол 2-НДФЛ среди содержимого сообщения
		Протоколы2НДФЛ = ПолучитьВложенияТранспортногоСообщения(
			ТранспортноеСообщение, 
			Истина, 
			Перечисления.ТипыСодержимогоТранспортногоКонтейнера.Протокол2НДФЛ);
			
		КоличествоПротоколов = Протоколы2НДФЛ.Количество();
		Если КоличествоПротоколов = 0 Тогда
			Возврат Ложь;
		Иначе
			Протокол2НДФЛ = Протоколы2НДФЛ[0];
		КонецЕсли;
		
		// загружаем протокол из файла в строку
		ТекстОшибки = "";
		ТекстПротокола = ТекстИзФайлаВСтроку(Протокол2НДФЛ, ТекстОшибки);
		Если НЕ ЗначениеЗаполнено(ТекстПротокола) Тогда
			Возврат Ложь;
		КонецЕсли;
		
		ТекстПротокола = ВРег(ТекстПротокола);
		
		ВидСведений = "КОЛИЧЕСТВО ОБРАБОТАННЫХ СВЕДЕНИЙ:";
		КоличествоОбработанныхСведений = КоличествоСведенийВПротоколе2НДФЛ(ТекстПротокола, ВидСведений);
		
		ВидСведений = "КОЛИЧЕСТВО ПРИНЯТЫХ СВЕДЕНИЙ:";
		КоличествоПринятыхСведений1 = КоличествоСведенийВПротоколе2НДФЛ(ТекстПротокола, ВидСведений);
		
		ВидСведений = "КОЛИЧЕСТВО СВЕДЕНИЙ, ГОТОВЫХ К ПРИЕМУ:"; // с 2019 г.
		КоличествоПринятыхСведений2 = КоличествоСведенийВПротоколе2НДФЛ(ТекстПротокола, ВидСведений);
		
		КоличествоПринятыхСведений = Макс(КоличествоПринятыхСведений1, КоличествоПринятыхСведений2);
		
		ЕстьФайлНеПринят = СтрНайти(ТекстПротокола, "ФАЙЛ НЕ ПРИНЯТ") > 0;
		
		Возврат НЕ ЕстьФайлНеПринят
			И КоличествоОбработанныхСведений <> КоличествоПринятыхСведений;
			
	КонецЕсли;
	
КонецФункции
	
Функция КоличествоСведенийВПротоколе2НДФЛ(ТекстПротокола, КлючСтр)
	
	Для ИндСтр = 1 По СтрЧислоСтрок(ТекстПротокола) Цикл
		ТекСтр = СтрПолучитьСтроку(ТекстПротокола, ИндСтр);
		
		// ищем ключ в текущей строке
		ВхождениеКлюча = СтрНайти(ВРЕГ(ТекСтр), КлючСтр);
		Если ВхождениеКлюча <> 0 Тогда
			
			СтрокаБезПрефикса = СокрЛП(СтрЗаменить(ВРЕГ(ТекСтр), КлючСтр, ""));
			СтрокаБезПрефикса = СокрЛП(СтрЗаменить(ВРЕГ(СтрокаБезПрефикса), ":", ""));
			
			Если НЕ ЗначениеЗаполнено(СтрокаБезПрефикса) Тогда
				Возврат 0;
			КонецЕсли;
			
			Если СтроковыеФункцииКлиентСервер.ТолькоЦифрыВСтроке(СтрокаБезПрефикса) Тогда
				Количество = Число(СтрокаБезПрефикса);
				Если Количество > 0 Тогда
					Возврат Количество;
				КонецЕсли;
			КонецЕсли;
			
		КонецЕсли;
		
	КонецЦикла;
	
	Возврат 0;
	
КонецФункции

Процедура ЗаполнитьДанныеСпецоператораСвязи(СтруктураРеквизитов) Экспорт

	Макет = ПолучитьМакетОбработки("ПараметрыСпецоператоровСвязи");
	
	СтруктураРеквизитов.Вставить("СпецоператорыСвязи",					ПолучитьСпецоператоровСвязи());
	СтруктураРеквизитов.Вставить("ТекстМакетаСоглашение", 				ПросмотрМакета("ПечатьСоглашениеHTML"));
	СтруктураРеквизитов.Вставить("МакетПараметрыСпецоператоровСвязи", 	Макет);
	СтруктураРеквизитов.Вставить("Макет", 								Макет);
	СтруктураРеквизитов.Вставить("ЗначениеЗаполненияСпецоператораСвязи",Перечисления.СпецоператорыСвязи.КалугаАстрал);

КонецПроцедуры

// Определяет состояние сдачи отчета на основе приложения к протоколу ПФР.
Функция ОтчетПФРСданЧастично(
		Объект = Неопределено,
		ВложенияТранспортногоСообщения = Неопределено,
		ФорматДокументооборота = Неопределено) Экспорт
	
	Результат = Новый Структура;
	Результат.Вставить("ЕстьВложения", 		Ложь);
	Результат.Вставить("ЕстьПриложение", 	Ложь);
	Результат.Вставить("СданЧастично", 		Ложь);
	Результат.Вставить("НеСдан", 			Ложь);
	
	Если ФорматДокументооборота = Перечисления.ФорматыДокументооборотаСПФР.ЭДОК Тогда
		ТипыСодержимого = Перечисления.ТипыСодержимогоТранспортногоКонтейнера.ПротоколПФР;
		
	ИначеЕсли ФорматДокументооборота = Неопределено Тогда
		ТипыСодержимого = Новый Массив;
		ТипыСодержимого.Добавить(Перечисления.ТипыСодержимогоТранспортногоКонтейнера.ПротоколПФР);
		ТипыСодержимого.Добавить(Перечисления.ТипыСодержимогоТранспортногоКонтейнера.ПротоколПриложениеПФР);
		
	Иначе
		ТипыСодержимого = Перечисления.ТипыСодержимогоТранспортногоКонтейнера.ПротоколПриложениеПФР;
	КонецЕсли;
	
	// ищем приложения к протоколу / протоколы для ЭДОК среди содержимого сообщения
	Если ВложенияТранспортногоСообщения = Неопределено Тогда
		ВложенияТранспортногоСообщения = ПолучитьВложенияТранспортногоСообщения(Объект.Ссылка, Истина, ТипыСодержимого);
	КонецЕсли;
	
	Результат.ЕстьВложения = ВложенияТранспортногоСообщения.Количество() > 0;
	
	XMLПриложениеКПротоколуПФР = Неопределено;
	Для ИндексТипаСодержимого = 1 По 2 Цикл
		ТипСодержимого = ?(ИндексТипаСодержимого = 1, Перечисления.ТипыСодержимогоТранспортногоКонтейнера.ПротоколПФР,
			Перечисления.ТипыСодержимогоТранспортногоКонтейнера.ПротоколПриложениеПФР);
		АнализироватьТипСодержимого = ?(ТипЗнч(ТипыСодержимого) = Тип("Массив"),
			ТипыСодержимого.Найти(ТипСодержимого) <> Неопределено, ТипСодержимого = ТипыСодержимого);
		Если НЕ АнализироватьТипСодержимого Тогда
			Продолжить;
		КонецЕсли;
		
		Для каждого ВложениеТранспортногоСообщения Из ВложенияТранспортногоСообщения Цикл
			Если ВложениеТранспортногоСообщения.Тип = ТипСодержимого Тогда
				СвойстваФайла = ОбщегоНазначенияКлиентСервер.РазложитьПолноеИмяФайла(ВложениеТранспортногоСообщения.ИмяФайла);
				Если ВРЕГ(СвойстваФайла.Расширение) = ВРЕГ(".xml") Тогда
					XMLПриложениеКПротоколуПФР = ВложениеТранспортногоСообщения;
					
					ИмяВременногоФайла = ПолучитьИмяВременногоФайла();
					XMLПриложениеКПротоколуПФР.Данные.Получить().Записать(ИмяВременногоФайла);
					ДеревоXMLПриложенияКПротоколуПФР = ЗагрузитьXMLВДеревоЗначений(ИмяВременногоФайла);
					ОперацииСФайламиЭДКО.УдалитьВременныйФайл(ИмяВременногоФайла);
					
					Если ДеревоXMLПриложенияКПротоколуПФР <> Неопределено Тогда
						УзелУОРР = Неопределено;
						УзелУППО = НайтиСтрокиДереваЗначений(ДеревоXMLПриложенияКПротоколуПФР,,, "УПП", "Э");
						Если НЕ ЗначениеЗаполнено(УзелУППО) Тогда
							УзелУОРР = НайтиСтрокиДереваЗначений(ДеревоXMLПриложенияКПротоколуПФР,,, "УОРР", "Э");
						КонецЕсли;
						РезультатРассмотрения = Неопределено;
						Если ЗначениеЗаполнено(УзелУОРР) Тогда
							РезультатРассмотрения = НайтиСтрокиДереваЗначений(УзелУОРР,,, "РезультатРассмотрения", "Э");
							Если ЗначениеЗаполнено(РезультатРассмотрения) Тогда
								Результат.ЕстьПриложение = Истина;
								Если РезультатРассмотрения.Значение = "2" Тогда
									Результат.НеСдан = Истина;
								КонецЕсли;
							КонецЕсли;
						КонецЕсли;
						Если НЕ ЗначениеЗаполнено(РезультатРассмотрения) Тогда
							УзлыУППКодРезультата = Новый Массив;
							НайтиСтрокиДереваЗначений(ДеревоXMLПриложенияКПротоколуПФР,, УзлыУППКодРезультата, "КодРезультата", "Э");
							
							Для каждого УзелУППКодРезультата Из УзлыУППКодРезультата Цикл
								Результат.ЕстьПриложение = Истина;
								ЗначениеКодаРезультата = СокрЛП(УзелУППКодРезультата.Значение);
								Если ЗначениеКодаРезультата = "30" ИЛИ ЗначениеКодаРезультата = "40" Тогда
									Результат.СданЧастично = Истина;
								ИначеЕсли ЗначениеКодаРезультата = "50" Тогда
									Результат.СданЧастично = Ложь;
									Результат.НеСдан = Истина;
									Прервать;
								КонецЕсли;
							КонецЦикла;
						КонецЕсли;
					КонецЕсли;
					
					Если Результат.ЕстьПриложение Тогда
						Прервать;
					КонецЕсли;
				КонецЕсли;
			КонецЕсли;
		КонецЦикла;
	КонецЦикла;
	
	Возврат Результат;
	
КонецФункции

Функция НайтиСтрокиДереваЗначений(
		СтрокаДереваЗначений,
		ОкончаниеИмени = Неопределено,
		МассивНайденныхСтрок = Неопределено,
		ПолноеИмя = Неопределено,
		ТипЭлемента = Неопределено,
		ВключаяПодчиненные = Истина) Экспорт
	
	ЗначениеОкончанияИмени = ОкончаниеИмени;
	Если НЕ ЗначениеЗаполнено(ЗначениеОкончанияИмени) И ЗначениеЗаполнено(ПолноеИмя) Тогда
		ЗначениеОкончанияИмени = ":" + СокрЛП(ПолноеИмя);
	КонецЕсли;
	
	Если МассивНайденныхСтрок = Неопределено Тогда
		МассивНайденныхСтрок = Новый Массив;
	КонецЕсли;
	
	МассивСтрокПолногоИмени = Неопределено;
	Если ПолноеИмя <> Неопределено Тогда
		ПараметрыОтбора = Новый Структура;
		ПараметрыОтбора.Вставить("Имя", ПолноеИмя);
		Если ТипЭлемента <> Неопределено Тогда
			ПараметрыОтбора.Вставить("Тип", ТипЭлемента);
		КонецЕсли;
		МассивСтрокПолногоИмени = СтрокаДереваЗначений.Строки.НайтиСтроки(ПараметрыОтбора, ВключаяПодчиненные);
		ОбщегоНазначенияКлиентСервер.ДополнитьМассив(МассивНайденныхСтрок, МассивСтрокПолногоИмени);
	КонецЕсли;
	
	Если ЗначениеОкончанияИмени <> Неопределено И (МассивСтрокПолногоИмени = Неопределено
		ИЛИ МассивСтрокПолногоИмени.Количество() = 0) Тогда
		
		ДлинаОкончанияИмени = СтрДлина(ЗначениеОкончанияИмени);
		Для каждого СтрокаДерева Из СтрокаДереваЗначений.Строки Цикл
			Если Прав(СтрокаДерева.Имя, ДлинаОкончанияИмени) = ЗначениеОкончанияИмени
				И (ТипЭлемента = Неопределено ИЛИ СтрокаДерева.Тип = ТипЭлемента) Тогда
				
				МассивНайденныхСтрок.Добавить(СтрокаДерева);
			КонецЕсли;
			
			Если ВключаяПодчиненные Тогда
				НайтиСтрокиДереваЗначений(
					СтрокаДерева,
					ЗначениеОкончанияИмени,
					МассивНайденныхСтрок,,
					ТипЭлемента,
					ВключаяПодчиненные);
			КонецЕсли;
		КонецЦикла;
	КонецЕсли;
	
	Возврат ?(МассивНайденныхСтрок.Количество() > 0, МассивНайденныхСтрок[0], Неопределено);
	
КонецФункции

Функция ПолучитьПоследнююОтправкуОтчета(КонтролирующийОрган, ОтчетСсылка) Экспорт
	
	Если КонтролирующийОрган = "ФСС" ИЛИ КонтролирующийОрган = Перечисления.ТипыКонтролирующихОрганов.ФСС Тогда
		Возврат ПолучитьПоследнююОтправкуОтчетаВФСС(ОтчетСсылка);
		
	ИначеЕсли КонтролирующийОрган = "ФСРАР" ИЛИ КонтролирующийОрган = Перечисления.ТипыКонтролирующихОрганов.ФСРАР Тогда
		Возврат ПолучитьПоследнююОтправкуОтчетаВФСРАР(ОтчетСсылка);
		
	ИначеЕсли КонтролирующийОрган = "РПН" ИЛИ КонтролирующийОрган = Перечисления.ТипыКонтролирующихОрганов.РПН Тогда
		Возврат ПолучитьПоследнююОтправкуОтчетаВРПН(ОтчетСсылка);
		
	ИначеЕсли КонтролирующийОрган = "ФТС" ИЛИ КонтролирующийОрган = Перечисления.ТипыКонтролирующихОрганов.ФТС Тогда
		Возврат ПолучитьПоследнююОтправкуОтчетаВФТС(ОтчетСсылка);
		
	ИначеЕсли КонтролирующийОрган = "БанкРоссии" ИЛИ КонтролирующийОрган = Перечисления.ТипыКонтролирующихОрганов.БанкРоссии Тогда
		Возврат ПолучитьПоследнююОтправкуОтчетаВБанкРоссии(ОтчетСсылка);
		
	ИначеЕсли КонтролирующийОрган = "Минобороны" ИЛИ КонтролирующийОрган = Перечисления.ТипыКонтролирующихОрганов.Минобороны Тогда
		Возврат ПолучитьПоследнююОтправкуОтчетаВМинобороны(ОтчетСсылка);
		
	Иначе
		Возврат ДокументооборотСКОВызовСервера.ПолучитьПоследнийЦиклОбмена(ОтчетСсылка);
	КонецЕсли;
	
КонецФункции

Функция ОрганизацияИспользуетОбменСБанкомРоссии(ОрганизацияСсылка) Экспорт
	
	Если ОбщегоНазначения.ПодсистемаСуществует("РегламентированнаяОтчетность.ЭлектронныйДокументооборотСКонтролирующимиОрганами.СдачаОтчетностиВБанкРоссии") Тогда
				
		МодульДокументооборотСБанкомРоссии = ОбщегоНазначения.ОбщийМодуль("ДокументооборотСБанкомРоссии");
		НастройкиОбменаСБанком = МодульДокументооборотСБанкомРоссии.ПолучитьНастройки(ОрганизацияСсылка);
		
		Возврат НастройкиОбменаСБанком.ИспользоватьОбмен;
		
	Иначе
		
		Возврат Ложь;
		
	КонецЕсли;
	
КонецФункции

Функция ОрганизацияИспользуетОбменСМинобороны(ОрганизацияСсылка) Экспорт
	
	Если ОбщегоНазначения.ПодсистемаСуществует("РегламентированнаяОтчетность.ЭлектронныйДокументооборотСКонтролирующимиОрганами.СдачаОтчетностиВМинобороны") Тогда
				
		МодульДокументооборотСМинобороны = ОбщегоНазначения.ОбщийМодуль("ДокументооборотСМинобороны");
		НастройкиОбменаСМинобороны = МодульДокументооборотСМинобороны.ПолучитьНастройки(ОрганизацияСсылка);
		
		Возврат НастройкиОбменаСМинобороны.ИспользоватьОбмен;
		
	Иначе
		
		Возврат Ложь;
		
	КонецЕсли;
	
КонецФункции

Функция ПользователюРазрешеноВыполнятьОбменПоУчетнойЗаписи(УчетнаяЗапись) Экспорт
	
	Если Не ЗначениеЗаполнено(УчетнаяЗапись) Тогда
		Возврат Ложь;
	КонецЕсли;
	
	УстановитьПривилегированныйРежим(Истина);
	ЭтоЭлектроннаяПодписьВМоделиСервиса = УчетнаяЗапись.ЭлектроннаяПодписьВМоделиСервиса;
	УстановитьПривилегированныйРежим(Ложь);
	
	Если ЭтоЭлектроннаяПодписьВМоделиСервиса Тогда
		
		УстановитьПривилегированныйРежим(Истина);
		
		ТекущийПользователь = Пользователи.ТекущийПользователь();

		Запрос = Новый Запрос;
		Запрос.Текст =
		"ВЫБРАТЬ РАЗРЕШЕННЫЕ
		|	ПользователиУчетныхЗаписейДокументооборота.УчетнаяЗапись
		|ИЗ
		|	РегистрСведений.ПользователиУчетныхЗаписейДокументооборота КАК ПользователиУчетныхЗаписейДокументооборота
		|ГДЕ
		|	ПользователиУчетныхЗаписейДокументооборота.УчетнаяЗапись = &УчетнаяЗапись
		|	И ПользователиУчетныхЗаписейДокументооборота.Пользователь = &Пользователь
		|	И ПользователиУчетныхЗаписейДокументооборота.УчетнаяЗапись.ПометкаУдаления = ЛОЖЬ";
		
		Запрос.УстановитьПараметр("УчетнаяЗапись", УчетнаяЗапись);
		Запрос.УстановитьПараметр("Пользователь", ТекущийПользователь);
		
		ОбменРазрешен = Не Запрос.Выполнить().Пустой();
		Возврат ОбменРазрешен;
		
	Иначе
		
		// Если у пользователя нет полных прав/его нет в списке пользователей - 
		// значит он не видит учетную запись в списке.
		// Проверям, видна ли учетная запись пользователю в списке учетных записей.
		// Это возможно в следующих случаях:
		// 1. У пользователя полные права.
		// 2. Нет полных прав, но пользователь указан в списке пользователей учетной записи.
		// 3. В базе нет пользователя, но это значит, что есть полные права.
		
		НуженОтборТекущихУчетныхЗаписей = НЕ ОбщегоНазначения.РазделениеВключено()
			И НЕ Пользователи.ЭтоПолноправныйПользователь();
		
		Запрос = Новый Запрос("ВЫБРАТЬ РАЗРЕШЕННЫЕ
		                      |	УчетныеЗаписиДокументооборота.Наименование КАК Наименование,
		                      |	УчетныеЗаписиДокументооборота.ОбменНапрямую КАК ОбменНапрямую,
		                      |	УчетныеЗаписиДокументооборота.ОбменНапрямуюСФСГС КАК ОбменНапрямуюСФСГС,
		                      |	УчетныеЗаписиДокументооборота.ИмяПользователяPOP3 КАК ИмяПользователяPOP3,
		                      |	УчетныеЗаписиДокументооборота.ПарольPOP3 КАК ПарольPOP3,
		                      |	УчетныеЗаписиДокументооборота.СерверPOP3 КАК СерверPOP3,
		                      |	УчетныеЗаписиДокументооборота.ИмяПользователяSMTP КАК ИмяПользователяSMTP,
		                      |	УчетныеЗаписиДокументооборота.ПарольSMTP КАК ПарольSMTP,
		                      |	УчетныеЗаписиДокументооборота.СерверSMTP КАК СерверSMTP,
		                      |	УчетныеЗаписиДокументооборота.ТребуетсяSMTPАутентификация КАК ТребуетсяSMTPАутентификация,
		                      |	УчетныеЗаписиДокументооборота.АдресЭлектроннойПочты КАК АдресЭлектроннойПочты,
		                      |	УчетныеЗаписиДокументооборота.НалоговыйОрган КАК НалоговыйОрган,
		                      |	УчетныеЗаписиДокументооборота.СерверДокументооборота КАК СерверДокументооборота,
		                      |	УчетныеЗаписиДокументооборота.СертификатРуководителя КАК СертификатРуководителя,
		                      |	УчетныеЗаписиДокументооборота.СертификатГлавногоБухгалтера КАК СертификатГлавногоБухгалтера,
		                      |	УчетныеЗаписиДокументооборота.СертификатДляШифрования КАК СертификатДляШифрования,
		                      |	УчетныеЗаписиДокументооборота.ПредназначенаДляДокументооборотаСФНС КАК ПредназначенаДляДокументооборотаСФНС,
		                      |	УчетныеЗаписиДокументооборота.ПредназначенаДляДокументооборотаСПФР КАК ПредназначенаДляДокументооборотаСПФР,
		                      |	УчетныеЗаписиДокументооборота.ПредназначенаДляДокументооборотаСФСГС КАК ПредназначенаДляДокументооборотаСФСГС,
		                      |	УчетныеЗаписиДокументооборота.ИдентификаторАбонента КАК ИдентификаторАбонента,
		                      |	УчетныеЗаписиДокументооборота.ИдентификаторСпецоператора КАК ИдентификаторСпецоператора,
		                      |	УчетныеЗаписиДокументооборота.НалоговыйОрган.Представление КАК НалоговыйОрганПредставление,
		                      |	УчетныеЗаписиДокументооборота.НалоговыйОрган.АдресЭлектроннойПочтыДляЦелейДокументооборотаСНалогоплательщиками КАК НалоговыйОрганАдресЭлектроннойПочтыДляЦелейДокументооборотаСНалогоплательщиками,
		                      |	УчетныеЗаписиДокументооборота.НалоговыйОрган.Код КАК НалоговыйОрганКод,
		                      |	УчетныеЗаписиДокументооборота.НалоговыйОрган.Наименование КАК НалоговыйОрганНаименование
		                      |ИЗ
		                      |	Справочник.УчетныеЗаписиДокументооборота КАК УчетныеЗаписиДокументооборота
		                      |ГДЕ
		                      |	УчетныеЗаписиДокументооборота.Ссылка = &Ссылка" + ?(НуженОтборТекущихУчетныхЗаписей, "
		                      |	И УчетныеЗаписиДокументооборота.Ссылка В(&ТекущиеУчетныеЗаписи)", ""));
		
		Если НуженОтборТекущихУчетныхЗаписей Тогда
			Если ПараметрыСеанса.ТекущиеУчетныеЗаписиНалогоплательщика = Неопределено Тогда
				ТекущиеУчетныеЗаписи = Новый Массив;
			Иначе
				ТекущиеУчетныеЗаписи = ПараметрыСеанса.ТекущиеУчетныеЗаписиНалогоплательщика;
			КонецЕсли;
			
			Запрос.УстановитьПараметр("ТекущиеУчетныеЗаписи", ТекущиеУчетныеЗаписи);
		КонецЕсли;
		Запрос.УстановитьПараметр("Ссылка", УчетнаяЗапись);
		
		ОбменРазрешен = Не Запрос.Выполнить().Пустой();
		
		Возврат ОбменРазрешен;
		
	КонецЕсли;
		
КонецФункции

Функция НезавершенныеПоследниеОтправкиФССПоОрганизации(Организация) Экспорт
	
	НастройкиОбмена   = ДокументооборотСКОВызовСервера.ПолучитьНастройкиФСС(Организация);
	ИспользоватьОбмен = НастройкиОбмена.ИспользоватьОбмен;
	
	Если ИспользоватьОбмен Тогда
		Запрос = Новый Запрос;
		Запрос.МенеджерВременныхТаблиц = Новый МенеджерВременныхТаблиц;
		Запрос.Текст = "ВЫБРАТЬ РАЗРЕШЕННЫЕ РАЗЛИЧНЫЕ
		               |	ОтправкиФСС.ОтчетСсылка,
		               |	МАКСИМУМ(ОтправкиФСС.ДатаОтправки) КАК ДатаОтправки
		               |ПОМЕСТИТЬ МаксимальныеДатыНезавершенныхОтправок
		               |ИЗ
		               |	Справочник.ОтправкиФСС КАК ОтправкиФСС
		               |ГДЕ
		               |	ОтправкиФСС.ПометкаУдаления = ЛОЖЬ
		               |	И ОтправкиФСС.Организация = &Организация
		               |	И ОтправкиФСС.СтатусОтправки = &СтатусОтправки
		               |
		               |СГРУППИРОВАТЬ ПО
		               |	ОтправкиФСС.ОтчетСсылка
		               |;
		               |
		               |////////////////////////////////////////////////////////////////////////////////
		               |ВЫБРАТЬ РАЗРЕШЕННЫЕ РАЗЛИЧНЫЕ
		               |	ОтправкиФСС.Ссылка
		               |ИЗ
		               |	МаксимальныеДатыНезавершенныхОтправок КАК МаксимальныеДатыНезавершенныхОтправок
		               |		ЛЕВОЕ СОЕДИНЕНИЕ Справочник.ОтправкиФСС КАК ОтправкиФСС
		               |		ПО МаксимальныеДатыНезавершенныхОтправок.ОтчетСсылка = ОтправкиФСС.ОтчетСсылка
		               |			И МаксимальныеДатыНезавершенныхОтправок.ДатаОтправки = ОтправкиФСС.ДатаОтправки
		               |ГДЕ
		               |	ОтправкиФСС.ПометкаУдаления = ЛОЖЬ
					   |	И ОтправкиФСС.ОтчетСсылка.ПометкаУдаления = ЛОЖЬ
		               |	И ОтправкиФСС.Организация = &Организация
		               |	И ОтправкиФСС.СтатусОтправки = &СтатусОтправки";
					   
		Запрос.Параметры.Вставить("Организация", 	Организация);
		Запрос.Параметры.Вставить("СтатусОтправки", Перечисления.СтатусыОтправки.Отправлен);
		
		ТаблицаОтправок = Запрос.Выполнить().Выгрузить();
		Отправки = ТаблицаОтправок.ВыгрузитьКолонку("Ссылка");
	Иначе
		Отправки = Новый Массив;
	КонецЕсли;
	
	Возврат Отправки;

КонецФункции

Функция НезавершенныеПоследниеОтправкиФСРАРПоОрганизации(Организация) Экспорт
	
	НастройкиОбмена = ДокументооборотСКОВызовСервера.ПолучитьНастройкиФСРАР(Организация);
	ИспользоватьОбмен = НастройкиОбмена.ИспользоватьОбмен;

	Если ИспользоватьОбмен Тогда
		Запрос = Новый Запрос;
		Запрос.МенеджерВременныхТаблиц = Новый МенеджерВременныхТаблиц;
		Запрос.Текст = "ВЫБРАТЬ РАЗРЕШЕННЫЕ РАЗЛИЧНЫЕ
		               |	ОтправкиФСРАР.ОтчетСсылка,
		               |	МАКСИМУМ(ОтправкиФСРАР.ДатаОтправки) КАК ДатаОтправки
		               |ПОМЕСТИТЬ МаксимальныеДатыНезавершенныхОтправок
		               |ИЗ
		               |	Справочник.ОтправкиФСРАР КАК ОтправкиФСРАР
		               |ГДЕ
		               |	ОтправкиФСРАР.ПометкаУдаления = ЛОЖЬ
		               |	И ОтправкиФСРАР.Организация = &Организация
		               |	И ОтправкиФСРАР.СхемаСдачиОтчетности В(&СхемыСдачиОтчетности)
		               |	И ОтправкиФСРАР.СтатусОтправки = &СтатусОтправки
		               |
		               |СГРУППИРОВАТЬ ПО
		               |	ОтправкиФСРАР.ОтчетСсылка
		               |;
		               |
		               |////////////////////////////////////////////////////////////////////////////////
		               |ВЫБРАТЬ РАЗРЕШЕННЫЕ РАЗЛИЧНЫЕ
		               |	ОтправкиФСРАР.Ссылка
		               |ИЗ
		               |	МаксимальныеДатыНезавершенныхОтправок КАК МаксимальныеДатыНезавершенныхОтправок
		               |		ЛЕВОЕ СОЕДИНЕНИЕ Справочник.ОтправкиФСРАР КАК ОтправкиФСРАР
		               |		ПО МаксимальныеДатыНезавершенныхОтправок.ОтчетСсылка = ОтправкиФСРАР.ОтчетСсылка
		               |			И МаксимальныеДатыНезавершенныхОтправок.ДатаОтправки = ОтправкиФСРАР.ДатаОтправки
		               |ГДЕ
		               |	ОтправкиФСРАР.ПометкаУдаления = ЛОЖЬ
		               |	И ОтправкиФСРАР.Организация = &Организация
					   |	И ОтправкиФСРАР.ОтчетСсылка.ПометкаУдаления = ЛОЖЬ
		               |	И ОтправкиФСРАР.СхемаСдачиОтчетности В(&СхемыСдачиОтчетности)
		               |	И ОтправкиФСРАР.СтатусОтправки = &СтатусОтправки";
					   
		Запрос.Параметры.Вставить("Организация", 	Организация);
		Запрос.Параметры.Вставить("СтатусОтправки", Перечисления.СтатусыОтправки.Отправлен);
		
		СхемыСдачиОтчетности = Новый Массив;
		СхемыСдачиОтчетности.Добавить(Перечисления.СхемыСдачиОтчетностиФСРАР.ОбщийРегиональныйИФедеральныйСервер);
		СхемыСдачиОтчетности.Добавить(Перечисления.СхемыСдачиОтчетностиФСРАР.ФедеральныйСервер);
		Запрос.Параметры.Вставить("СхемыСдачиОтчетности", СхемыСдачиОтчетности);
		
		ТаблицаОтправок = Запрос.Выполнить().Выгрузить();
		Отправки = ТаблицаОтправок.ВыгрузитьКолонку("Ссылка");
	Иначе
		Отправки = Новый Массив;
	КонецЕсли;
	
	Возврат Отправки;

КонецФункции

Функция НезавершенныеПоследниеОтправкиРПНПоОрганизации(Организация) Экспорт
	
	НастройкиОбмена = ДокументооборотСКОВызовСервера.ПолучитьНастройкиРПН(Организация);
	ИспользоватьОбмен = НастройкиОбмена.ИспользоватьОбмен;

	Если ИспользоватьОбмен Тогда
		Запрос = Новый Запрос;
		Запрос.МенеджерВременныхТаблиц = Новый МенеджерВременныхТаблиц;
		Запрос.Текст = "ВЫБРАТЬ РАЗРЕШЕННЫЕ РАЗЛИЧНЫЕ
		               |	ОтправкиРПН.ОтчетСсылка,
		               |	МАКСИМУМ(ОтправкиРПН.ДатаОтправки) КАК ДатаОтправки
		               |ПОМЕСТИТЬ МаксимальныеДатыНезавершенныхОтправок
		               |ИЗ
		               |	Справочник.ОтправкиРПН КАК ОтправкиРПН
		               |ГДЕ
		               |	ОтправкиРПН.ПометкаУдаления = ЛОЖЬ
		               |	И ОтправкиРПН.Организация = &Организация
		               |	И ОтправкиРПН.СтатусОтправки = &СтатусОтправки
		               |
		               |СГРУППИРОВАТЬ ПО
		               |	ОтправкиРПН.ОтчетСсылка
		               |;
		               |
		               |////////////////////////////////////////////////////////////////////////////////
		               |ВЫБРАТЬ РАЗРЕШЕННЫЕ РАЗЛИЧНЫЕ
		               |	ОтправкиРПН.Ссылка
		               |ИЗ
		               |	МаксимальныеДатыНезавершенныхОтправок КАК МаксимальныеДатыНезавершенныхОтправок
		               |		ЛЕВОЕ СОЕДИНЕНИЕ Справочник.ОтправкиРПН КАК ОтправкиРПН
		               |		ПО МаксимальныеДатыНезавершенныхОтправок.ОтчетСсылка = ОтправкиРПН.ОтчетСсылка
		               |			И МаксимальныеДатыНезавершенныхОтправок.ДатаОтправки = ОтправкиРПН.ДатаОтправки
		               |ГДЕ
		               |	ОтправкиРПН.ПометкаУдаления = ЛОЖЬ
					   |	И ОтправкиРПН.ОтчетСсылка.ПометкаУдаления = ЛОЖЬ
		               |	И ОтправкиРПН.Организация = &Организация
		               |	И ОтправкиРПН.СтатусОтправки = &СтатусОтправки";
		
		Запрос.Параметры.Вставить("Организация", 	Организация);
		Запрос.Параметры.Вставить("СтатусОтправки", Перечисления.СтатусыОтправки.Отправлен);
		
		ТаблицаОтправок = Запрос.Выполнить().Выгрузить();
		Отправки = ТаблицаОтправок.ВыгрузитьКолонку("Ссылка");
	Иначе
		Отправки = Новый Массив;
	КонецЕсли;
	
	Возврат Отправки;

КонецФункции

Функция ПредставлениеОтчета(СсылкаНаОтчет) Экспорт
	
	СведенияОбОтчете = СведенияПоОтправляемымОбъектам(СсылкаНаОтчет);
	
	Результат = Строка(СведенияОбОтчете.Наименование);
	
	Если ЗначениеЗаполнено(СведенияОбОтчете.ПредставлениеПериода) Тогда
		ПредставлениеПериодаОтчета = Строка(СведенияОбОтчете.ПредставлениеПериода);
		
		Результат = СтрШаблон(
			НСтр("ru = '%1 за %2'"),
			Результат,
			ПредставлениеПериодаОтчета);
	КонецЕсли;
	
	Если ЗначениеЗаполнено(СведенияОбОтчете.ВариантОтчета) Тогда
		Если СведенияОбОтчете.ВариантОтчета = "П" Тогда
			Результат = СтрШаблон(
				НСтр("ru = '%1 (первичный)'"),
				Результат);
			
		ИначеЕсли Лев(СведенияОбОтчете.ВариантОтчета, 2) = "К/" Тогда
			НомерКорректировки = Сред(СведенияОбОтчете.ВариантОтчета, 3);
			
			Результат = СтрШаблон(
				НСтр("ru = '%1 (корректирующий / %2)'"),
				Результат,
				НомерКорректировки);
		КонецЕсли;
	КонецЕсли;
	
	Результат = ?(НЕ ЗначениеЗаполнено(Результат), Строка(СсылкаНаОтчет), Результат);
	
	Возврат Результат;
	
КонецФункции

Функция НаименованиеЕГРЮЛ_ЕГРИП(Объект) Экспорт
	
	Ссылка = Объект.Ссылка;
	
	// Наименование вида "Выписка по ИНН 980000000000, Иванов Иван"
	Наименование =НСтр("ru = 'Выписка по %1 %2%3%4'");
	Если Ссылка.ВидПараметраЗапроса = Перечисления.ВидыПараметровЗапросаНаВыпискуИзЕГРЮЛ_ЕГРИП.ИНН Тогда
		Наименование = СтрЗаменить(Наименование, "%1", "ИНН");
	ИначеЕсли Ссылка.ВидПараметраЗапроса = Перечисления.ВидыПараметровЗапросаНаВыпискуИзЕГРЮЛ_ЕГРИП.ОГРН Тогда
		Наименование = СтрЗаменить(Наименование, "%1", "ОГРН");
	Иначе
		Наименование = НСтр("ru = 'Выписка из ЕГРЮЛ/ЕГРИП'");
	КонецЕсли;
	Наименование = СтрЗаменить(Наименование, "%2", Строка(Ссылка.ПараметрЗапроса));
	
	Если ЗначениеЗаполнено(Ссылка.Контрагент) Тогда
		Наименование = СтрЗаменить(Наименование, "%3", ", ");
		Наименование = СтрЗаменить(Наименование, "%4", Ссылка.Контрагент.Наименование);
	Иначе
		
		ИННиНаименование = ИННиНаименованиеОрганизации(Ссылка.Организация);
		
		Если ИННиНаименование.ИНН = Строка(Ссылка.ПараметрЗапроса) Тогда
			Наименование = СтрЗаменить(Наименование, "%3", ", ");
			Наименование = СтрЗаменить(Наименование, "%4", ИННиНаименование.Наименование);
		Иначе
			Наименование = СтрЗаменить(Наименование, "%3", "");
			Наименование = СтрЗаменить(Наименование, "%4", "");
		КонецЕсли;
		
	КонецЕсли;
	
	Возврат Наименование;
	
КонецФункции

Функция НезавершенныеПоследниеОтправкиФТСПоОрганизации(Организация, НастройкиОбмена = Неопределено) Экспорт
	
	НастройкиОбмена = ДокументооборотСКОВызовСервера.ПолучитьНастройкиФТС(Организация);
	ИспользоватьОбмен = НастройкиОбмена.ИспользоватьОбмен;
	
	Если ИспользоватьОбмен Тогда
		// После сдачи статформы в ФТС возможна постановка ее на контроль, ожидаем этого к течение года
		МинимальнаяДатаОтправкиСданныхТребующихОбновления = ДобавитьМесяц(ТекущаяДатаСеанса(), -6);
		
		Запрос = Новый Запрос;
		Запрос.МенеджерВременныхТаблиц = Новый МенеджерВременныхТаблиц;
		Запрос.Текст = "ВЫБРАТЬ РАЗРЕШЕННЫЕ РАЗЛИЧНЫЕ
		               |	ОтправкиФТС.ОтчетСсылка,
		               |	МАКСИМУМ(ОтправкиФТС.ДатаОтправки) КАК ДатаОтправки
		               |ПОМЕСТИТЬ МаксимальныеДатыНезавершенныхОтправок
		               |ИЗ
		               |	Справочник.ОтправкиФТС КАК ОтправкиФТС
		               |ГДЕ
		               |	ОтправкиФТС.ПометкаУдаления = ЛОЖЬ
		               |	И ОтправкиФТС.Организация = &Организация
		               |	И (ОтправкиФТС.СтатусОтправки = ЗНАЧЕНИЕ(Перечисление.СтатусыОтправки.Отправлен)
		               |	ИЛИ ((ОтправкиФТС.СтатусОтправки = ЗНАЧЕНИЕ(Перечисление.СтатусыОтправки.Сдан)
		               |		ИЛИ ОтправкиФТС.НаКонтроле)
		               |		И ОтправкиФТС.ДатаОтправки >= &МинимальнаяДатаОтправкиСданныхТребующихОбновления))
		               |
		               |СГРУППИРОВАТЬ ПО
		               |	ОтправкиФТС.ОтчетСсылка
		               |;
		               |
		               |////////////////////////////////////////////////////////////////////////////////
		               |ВЫБРАТЬ РАЗРЕШЕННЫЕ РАЗЛИЧНЫЕ
		               |	ОтправкиФТС.Ссылка
		               |ИЗ
		               |	МаксимальныеДатыНезавершенныхОтправок КАК МаксимальныеДатыНезавершенныхОтправок
		               |		ЛЕВОЕ СОЕДИНЕНИЕ Справочник.ОтправкиФТС КАК ОтправкиФТС
		               |		ПО МаксимальныеДатыНезавершенныхОтправок.ОтчетСсылка = ОтправкиФТС.ОтчетСсылка
		               |			И МаксимальныеДатыНезавершенныхОтправок.ДатаОтправки = ОтправкиФТС.ДатаОтправки
		               |ГДЕ
		               |	ОтправкиФТС.ПометкаУдаления = ЛОЖЬ
		               |	И ОтправкиФТС.Организация = &Организация
					   |	И ОтправкиФТС.ОтчетСсылка.ПометкаУдаления = ЛОЖЬ
		               |	И (ОтправкиФТС.СтатусОтправки = ЗНАЧЕНИЕ(Перечисление.СтатусыОтправки.Отправлен)
		               |	ИЛИ ((ОтправкиФТС.СтатусОтправки = ЗНАЧЕНИЕ(Перечисление.СтатусыОтправки.Сдан)
		               |		ИЛИ ОтправкиФТС.НаКонтроле)
		               |		И ОтправкиФТС.ДатаОтправки >= &МинимальнаяДатаОтправкиСданныхТребующихОбновления))";
		
		Запрос.Параметры.Вставить("Организация", 										Организация);
		Запрос.Параметры.Вставить("МинимальнаяДатаОтправкиСданныхТребующихОбновления", 	МинимальнаяДатаОтправкиСданныхТребующихОбновления);
		
		ТаблицаОтправок = Запрос.Выполнить().Выгрузить();
		Отправки = ТаблицаОтправок.ВыгрузитьКолонку("Ссылка");
	Иначе
		Отправки = Новый Массив;
	КонецЕсли;
	
	Возврат Отправки;
	
КонецФункции

Функция ИННиНаименованиеОрганизации(Организация)
	
	ДополнительныеПараметры = Новый Структура();
	ДополнительныеПараметры.Вставить("ИНН", 		 "");
	ДополнительныеПараметры.Вставить("Наименование", "");

	Если ЗначениеЗаполнено(Организация) Тогда
		ЭтоИП = НЕ РегламентированнаяОтчетностьВызовСервера.ЭтоЮридическоеЛицо(Организация);
		Если ЭтоИП Тогда
			
			Сведения = РегламентированнаяОтчетностьВызовСервера.ПолучитьСведенияОбОрганизации(Организация, , "НаимЮЛСокр, ИННФЛ");
			
			ДополнительныеПараметры.Вставить("ИНН", 		 СокрЛП(Сведения.ИННФЛ));
			ДополнительныеПараметры.Вставить("Наименование", Сведения.НаимЮЛСокр);
			
		Иначе
			
			Сведения = РегламентированнаяОтчетностьВызовСервера.ПолучитьСведенияОбОрганизации(Организация, , "НаимЮЛСокр, ИННЮЛ");
			
			ДополнительныеПараметры.Вставить("ИНН", 		 СокрЛП(Сведения.ИННЮЛ));
			ДополнительныеПараметры.Вставить("Наименование", Сведения.НаимЮЛСокр);
			
		КонецЕсли;
	КонецЕсли;
	
	Возврат ДополнительныеПараметры;

КонецФункции

#Область ПечатьСертификата

Процедура ПечатьСертификатаНаСервере(ТабличныйДокумент, Сертификат) Экспорт
	
	Макет = ПолучитьМакетОбработки("ПечатьСертификат");
	
	СвойстваСертификата = ПолучитьСвойстваСертификатаДляПечати(Сертификат);
	
	Область = Макет.ПолучитьОбласть("Шапка");
	Область.Параметры.Заполнить(СвойстваСертификата);
	
	ТабличныйДокумент.Вывести(Область);
	
	Если СвойстваСертификата.ТипВладельца = "ЮридическоеЛицо" Тогда
		Область = Макет.ПолучитьОбласть("СведенияОВладельцеЮР");
	Иначе
		Область = Макет.ПолучитьОбласть("СведенияОВладельцеФЛ");
	КонецЕсли;
	Область.Параметры.Заполнить(СвойстваСертификата);
	
	ТабличныйДокумент.Вывести(Область);
	
	Область = Макет.ПолучитьОбласть("Подвал");
	Область.Параметры.Заполнить(СвойстваСертификата);
	
	ТабличныйДокумент.Вывести(Область);
	
	РазместитьНаОдномЛисте(ТабличныйДокумент);
	
КонецПроцедуры

Процедура РазместитьНаОдномЛисте(ТабличныйДокумент) Экспорт
	
	Попытка
		
		Дельта = 0;
		ТабличныйДокумент.МасштабПечати = 100;
		Пока ТабличныйДокумент.КоличествоСтраниц() > 1 И Дельта < 20 Цикл
			Дельта = Дельта + 1;
			ТабличныйДокумент.АвтоМасштаб = Ложь;
			ТабличныйДокумент.МасштабПечати = 100 - Дельта;
		КонецЦикла;
		
	Исключение
		
		ТабличныйДокумент.МасштабПечати = 100;
		ТабличныйДокумент.АвтоМасштаб = Истина;
		
		КодОсновногоЯзыка = ОбщегоНазначения.КодОсновногоЯзыка();
		ИнформацияОбОшибке = ПодробноеПредставлениеОшибки(ИнформацияОбОшибке());
		ЗаписьЖурналаРегистрации(НСтр("ru = 'Электронный документооборот с контролирующими органами.Печать на одну страницу'", КодОсновногоЯзыка),УровеньЖурналаРегистрации.Ошибка,,,ИнформацияОбОшибке);
		
	КонецПопытки;
	
КонецПроцедуры

Функция ПолучитьСвойстваСертификатаДляПечати(Сертификат) Экспорт
	
	РасширенныеСвойства = ПолучитьРасширенныеСвойстваСертификата(Сертификат);
	
	СертификатКриптографии = Новый СертификатКриптографии(Сертификат);
	
	СвойстваСертификата = РасширенныеСвойства;
	СвойстваСертификата.Вставить("СерийныйНомер", СертификатКриптографии.СерийныйНомер);
	ПериодДействия = СтрШаблон(
		НСтр("ru = '%1 по %2'"), 
		Формат(СертификатКриптографии.ДатаНачала, "ДФ='dd MMMM yyyy ''г.'' H:mm:ss'"),
		Формат(СертификатКриптографии.ДатаОкончания, "ДФ='dd MMMM yyyy ''г.'' H:mm:ss'"));
	СвойстваСертификата.Вставить("ПериодДействия", ПериодДействия);
	
	Если СертификатКриптографии.Субъект.Свойство("OID1_2_643_100_3") Тогда 
		СвойстваСертификата.Вставить("СНИЛС", СертификатКриптографии.Субъект.OID1_2_643_100_3);
	КонецЕсли;
	
	Если Лев(СертификатКриптографии.Субъект.OID1_2_643_3_131_1_1, 2) = "00" Тогда
		
		СвойстваСертификата.Вставить("ТипВладельца", "ЮридическоеЛицо");
		СвойстваСертификата.Вставить("Наименование", СертификатКриптографии.Субъект.CN);

		Если СертификатКриптографии.Субъект.Свойство("OID1_2_643_100_1") Тогда
			СвойстваСертификата.Вставить("ОГРН", СертификатКриптографии.Субъект.OID1_2_643_100_1);
		КонецЕсли;
		
		СвойстваСертификата.Вставить("ИНН", СертификатКриптографии.Субъект.OID1_2_643_3_131_1_1);
		СвойстваСертификата.Вставить("Адрес",   АдресИзСертификатаКриптографии(СертификатКриптографии.Субъект));
		СвойстваСертификата.Вставить("Фамилия", СертификатКриптографии.Субъект.OID2_5_4_4);
		СвойстваСертификата.Вставить("ИмяОтчество", СертификатКриптографии.Субъект.OID2_5_4_42);
		СвойстваСертификата.Вставить("Должность", СертификатКриптографии.Субъект.OID2_5_4_12);
		
		Владелец = СертификатКриптографии.Субъект.OID2_5_4_4 + " " + СертификатКриптографии.Субъект.OID2_5_4_42;
		
	Иначе
		СвойстваСертификата.Вставить("ТипВладельца", "ФизическоеЛицо");
		СвойстваСертификата.Вставить("ФИО", СертификатКриптографии.Субъект.CN);

		Владелец = СертификатКриптографии.Субъект.CN;
	КонецЕсли;
	
	СвойстваСертификата.Вставить("ОткрытыйКлюч", СертификатКриптографии.ОткрытыйКлюч);
	СвойстваСертификата.Вставить("НаименованиеУЦ", СертификатКриптографии.Издатель.CN);
	СвойстваСертификата.Вставить("АдресУЦ", АдресИзСертификатаКриптографии(СертификатКриптографии.Издатель));
	СвойстваСертификата.Вставить("Владелец", Владелец);
	
	Возврат СвойстваСертификата;
	
КонецФункции

Функция АдресИзСертификатаКриптографии(Источник)
	
	Издатель = Новый Структура("C, ST, L, Street"); 
	ЗаполнитьЗначенияСвойств(Издатель, Источник);
	
	Адрес = Новый Массив;
	
	Если ЗначениеЗаполнено(Издатель.C) Тогда
		Адрес.Добавить(Издатель.C);
	КонецЕсли;
	
	Если ЗначениеЗаполнено(Издатель.ST) Тогда
		Адрес.Добавить(Издатель.ST);
	КонецЕсли;
	
	Если ЗначениеЗаполнено(Издатель.L) Тогда
		Адрес.Добавить(Издатель.L);
	КонецЕсли;
	
	Если ЗначениеЗаполнено(Издатель.Street) Тогда
		Адрес.Добавить(Издатель.Street);
	КонецЕсли;
	
	Адрес = СтрСоединить(Адрес, ", ");
	
	Возврат Адрес;
			
КонецФункции

Функция НайтиСигнатуру(Байты, Сигнатура, Позиция)
	
	НачалаСигнатуры = -1;
	СигнатураНайдена = Ложь;
	Для Индекс = Позиция + 1 По Байты.ВГраница() - Сигнатура.ВГраница() Цикл
		Байт = Байты[Индекс];
		Если Байт = Сигнатура[0] Тогда
			СигнатураНайдена = Истина;
			Для Индекс2 = 1 По Сигнатура.Количество() - 1 Цикл
				Если Байты[Индекс2 + Индекс] <> Сигнатура[Индекс2] Тогда
					СигнатураНайдена = Ложь;
					Прервать;
				КонецЕсли;
			КонецЦикла;
			Если СигнатураНайдена Тогда
				НачалаСигнатуры = Индекс;
				Прервать;
			КонецЕсли;
		КонецЕсли;
	КонецЦикла;
	
	Возврат НачалаСигнатуры;
	
КонецФункции

Функция ПолучитьРасширенныеСвойстваСертификата(Сертификат)
	
	РасширенныеСвойства = Новый Структура;
	
	ИмяФайла = ПолучитьИмяВременногоФайла();
	Сертификат.Записать(ИмяФайла);
	
	Байты = ПрочитатьФайлВМассив(ИмяФайла);
	УдалитьФайлы(ИмяФайла);
	
	Сигнатура = Новый Массив; // Ищем OID 1.2.643.100.111
	Сигнатура.Добавить(6);
	Сигнатура.Добавить(5);
	Сигнатура.Добавить(42);
	Сигнатура.Добавить(133);
	Сигнатура.Добавить(3);
	Сигнатура.Добавить(100);
	Сигнатура.Добавить(111);
	
	Позиция = 0;
	НачалоСигнатуры = НайтиСигнатуру(Байты, Сигнатура, Позиция);
	
	Если НачалоСигнатуры <> -1 Тогда
		Позиция = НачалоСигнатуры + Сигнатура.Количество();
		Размер = ПрочитатьРазмер(Байты, Позиция); // OCTET STRING	
		Позиция = Позиция + 1; // сдвиг сразу к UTF8String
		Размер = ПрочитатьРазмер(Байты, Позиция); // OCTET STRING
		Строка = ПрочитатьСтроку(Байты, Позиция, Размер);
		
		РасширенныеСвойства.Вставить("СКЗИ", Строка);
	КонецЕсли;
	
	Сигнатура = Новый Массив; // Ищем OID 1.2.643.100.112
	Сигнатура.Добавить(6);
	Сигнатура.Добавить(5);
	Сигнатура.Добавить(42);
	Сигнатура.Добавить(133);
	Сигнатура.Добавить(3);
	Сигнатура.Добавить(100);
	Сигнатура.Добавить(112);
	
	НачалоСигнатуры = НайтиСигнатуру(Байты, Сигнатура, Позиция);	
		
	Если НачалоСигнатуры <> -1 Тогда
		Позиция = НачалоСигнатуры + Сигнатура.Количество();
		//Позиция = Позиция + 1;
		Размер = ПрочитатьРазмер(Байты, Позиция); // OCTET STRING	
		Позиция = Позиция + 1; // сдвиг сразу к UTF8String
		Размер = ПрочитатьРазмер(Байты, Позиция); // OCTET STRING
		Позиция = Позиция + 1;
		Размер = ПрочитатьРазмер(Байты, Позиция); // OCTET STRING
		Строка = ПрочитатьСтроку(Байты, Позиция, Размер);
		РасширенныеСвойства.Вставить("СКЗИ", Строка);
		
		Позиция = Позиция + 1;
		Размер = ПрочитатьРазмер(Байты, Позиция); // OCTET STRING
		Строка = ПрочитатьСтроку(Байты, Позиция, Размер);
		РасширенныеСвойства.Вставить("СКЗИ_УЦ", Строка);

		
		Позиция = Позиция + 1;
		Размер = ПрочитатьРазмер(Байты, Позиция); // OCTET STRING
		Строка = ПрочитатьСтроку(Байты, Позиция, Размер);
		РасширенныеСвойства.Вставить("Заключение", Строка);
		
		Позиция = Позиция + 1;
		Размер = ПрочитатьРазмер(Байты, Позиция); // OCTET STRING
		Строка = ПрочитатьСтроку(Байты, Позиция, Размер);
		РасширенныеСвойства.Вставить("ЗаключениеУЦ", Строка);
	КонецЕсли;
	
	РасширенныеСвойства.Вставить("КлассУЦ", "КС2");

	
	Сигнатура = Новый Массив; // Ищем 1.2.643.2.2.3
	Сигнатура.Добавить(6);
	Сигнатура.Добавить(6);
	Сигнатура.Добавить(42);
	Сигнатура.Добавить(133);
	Сигнатура.Добавить(3);
	Сигнатура.Добавить(2);
	Сигнатура.Добавить(2);
	Сигнатура.Добавить(3);
	
	НачалоСигнатуры = НайтиСигнатуру(Байты, Сигнатура, Позиция);
	
	Если НачалоСигнатуры = -1 Тогда
		Сигнатура = Новый Массив; // Ищем 1.2.643.7.1.1.3.2 (ГОСТ Р 34.10-2012 с ключом 256 бит)
		Сигнатура.Добавить(6);
		Сигнатура.Добавить(8);
		Сигнатура.Добавить(42);
		Сигнатура.Добавить(133);
		Сигнатура.Добавить(3);
		Сигнатура.Добавить(7);
		Сигнатура.Добавить(1);
		Сигнатура.Добавить(1);
		Сигнатура.Добавить(3);
		Сигнатура.Добавить(2);
		
		НачалоСигнатуры = НайтиСигнатуру(Байты, Сигнатура, Позиция);
		
		Если НачалоСигнатуры = -1 Тогда
			Сигнатура = Новый Массив; // Ищем 1.2.643.7.1.1.3.3 (ГОСТ Р 34.10-2012 с ключом 512 бит)
			Сигнатура.Добавить(6);
			Сигнатура.Добавить(8);
			Сигнатура.Добавить(42);
			Сигнатура.Добавить(133);
			Сигнатура.Добавить(3);
			Сигнатура.Добавить(7);
			Сигнатура.Добавить(1);
			Сигнатура.Добавить(1);
			Сигнатура.Добавить(3);
			Сигнатура.Добавить(3);
			
			НачалоСигнатуры = НайтиСигнатуру(Байты, Сигнатура, Позиция);
		КонецЕсли;
	КонецЕсли;
	
	Если НачалоСигнатуры <> -1 Тогда
		Позиция = НачалоСигнатуры + Сигнатура.Количество();
		Пока Байты[Позиция] <> 3 Цикл
			Позиция = Позиция + 1;
		КонецЦикла;
		Размер = ПрочитатьРазмер(Байты, Позиция); // OCTET STRING	
		Позиция = Позиция + 1; // сдвиг сразу к BIT STRING
		
		Подпись = Новый Массив;
		Для Индекс = 1 По Размер - 1 Цикл
			Позиция = Позиция + 1;
			Байт = Байты[Позиция];
			Подпись.Вставить(0, Байт);
		КонецЦикла;
		
		ИмяФайла = ПолучитьИмяВременногоФайла();
		ЗаписатьФайлИзМассива(ИмяФайла, Подпись);
		
		Подпись = Новый ДвоичныеДанные(ИмяФайла);
		УдалитьФайлы(ИмяФайла);
	КонецЕсли;
	
	Алгоритм = КриптографияЭДКО.АлгоритмКриптосообщенияИлиСертификата(
		Сертификат,
		Истина,
		Ложь);
	ИмяАлгоритма = КриптографияЭДКОКлиентСервер.СвойстваАлгоритма(Алгоритм).Имя;
	
	РасширенныеСвойства.Вставить("Алгоритм", ИмяАлгоритма);
	РасширенныеСвойства.Вставить("Класс", "Класс средства ЭП КС1;Класс средства ЭП КС2;");
	РасширенныеСвойства.Вставить("ОбластьИспользования", "Цифровая подпись, Неотрекаемость, Шифрование ключей, Шифрование данных");
	РасширенныеСвойства.Вставить("Подпись", Подпись);
	
	Возврат РасширенныеСвойства;
	
КонецФункции

Функция ПрочитатьРазмер(Байты, Позиция)
	
	Позиция = Позиция + 1;
	Байт = Байты[Позиция];
	БитоваяСтрока = ЧислоВСтрокуБитов(Байт);
	Если Сред(БитоваяСтрока, 1, 1) = "1" Тогда
		КоличествоБайт = СтрокаБитовВЧисло(Прав(БитоваяСтрока, 7));
		Размер = ПрочитатьЦелоеЧисло(Байты, Позиция, КоличествоБайт);
	Иначе
		Размер = Байт;		
	КонецЕсли;
	
	Возврат Размер;
	
КонецФункции

Функция ПрочитатьЦелоеЧисло(Байты, Позиция, КоличествоБайт)
	
	БитоваяСтрокаДлиныБлока = "";
	Для Индекс = 1 По КоличествоБайт Цикл
		Позиция = Позиция + 1;
		Байт = Байты[Позиция];
		БитоваяСтрокаДлиныБлока = БитоваяСтрокаДлиныБлока + ЧислоВСтрокуБитов(Байт);
	КонецЦикла;
	
	Возврат СтрокаБитовВЧисло(БитоваяСтрокаДлиныБлока);
	
КонецФункции

Функция ПрочитатьСтроку(Байты, Позиция, КоличествоБайт)
	
	БайтыДляЗаписи = Новый Массив;
	Для Индекс = 1 По КоличествоБайт Цикл
		Позиция = Позиция + 1;
		Байт = Байты[Позиция];
		БайтыДляЗаписи.Добавить(Байт);
	КонецЦикла;
	
	ИмяФайла = ПолучитьИмяВременногоФайла("txt");
	ЗаписатьФайлИзМассива(ИмяФайла, БайтыДляЗаписи);
	
	ЧтениеТекста = Новый ЧтениеТекста(ИмяФайла, "utf-8");
	Строка = ЧтениеТекста.Прочитать();
	ЧтениеТекста.Закрыть();
	УдалитьФайлы(ИмяФайла);
	
	Возврат Строка;
	
КонецФункции

// Переводит заданное десятичное число в соответствующую строку битов.
//
// Параметры:
// ИсходноеЧисло - Число - 10-тичное.
//
// Возвращаемое значение:
// Строка - последовательность битов, соответствующая заданному числу.
//
Функция ЧислоВСтрокуБитов(Знач ИсходноеЧисло) Экспорт

	ИсходноеЧисло = Цел(?(ИсходноеЧисло < 0, - ИсходноеЧисло, ИсходноеЧисло));
	Если ИсходноеЧисло = 0 Тогда
		Возврат "00000000";
	КонецЕсли;
	
	СтрокаБитов = "";
	
	Пока ИсходноеЧисло > 0 Цикл
		ЗначениеМладшегоБита = ИсходноеЧисло % 2;
		СтрокаБитов = Строка(ЗначениеМладшегоБита) + СтрокаБитов;
		ИсходноеЧисло = (ИсходноеЧисло - ЗначениеМладшегоБита) / 2;
	КонецЦикла;
	
	Префикс = "";
	Для Индекс = 1 По 8 - СтрДлина(СтрокаБитов) Цикл
		Префикс = Префикс + "0";
	КонецЦикла;
	
    Возврат Префикс + СтрокаБитов;
	
КонецФункции

// Переводит строку битов в соответствующее десятичное число.
//
// Параметры:
// СтрокаБитов - Строка.
//
// Возвращаемое значение:
// Число.
//
Функция СтрокаБитовВЧисло(Знач СтрокаБитов) Экспорт

	СтрокаБитов = СокрЛП(СтрокаБитов);
	ДлинаСтроки = СтрДлина(СтрокаБитов);
	Если ДлинаСтроки = 0 Тогда
		Возврат 0;
	КонецЕсли;
	
	Если Не СтрокаСодержитТолькоДопустимыеСимволы(СтрокаБитов, "01") Тогда
		ВызватьИсключение(СтроковыеФункцииКлиентСервер.ПодставитьПараметрыВСтроку(НСтр("ru = '""%1"" не является последовательностью битов'"), СтрокаБитов));
	КонецЕсли;	
	
	Число = 0;
	
	Для Индекс = 1 По ДлинаСтроки Цикл
		СимволЦифры = Сред(СтрокаБитов, Индекс, 1);
		Число = Число * 2 + Число(СимволЦифры)
	КонецЦикла;
	
    Возврат Число;
	
КонецФункции

Функция СтрокаСодержитТолькоДопустимыеСимволы(Знач ИсходнаяСтрока, Знач ДопустимыеСимволы)
	
	ДлинаСтроки = СтрДлина(ИсходнаяСтрока);
	Если ДлинаСтроки = 0 Тогда
		Возврат Ложь;
	КонецЕсли;	
	
   	Для Индекс = 1 По ДлинаСтроки Цикл 
    	ТекущийСимвол = Сред(ИсходнаяСтрока, Индекс, 1); 
	   	Если Найти(ДопустимыеСимволы, ТекущийСимвол) = 0 Тогда
		    Возврат Ложь;
	    КонецЕсли;
	КонецЦикла; 
	
   	Возврат Истина;
	
КонецФункции

Функция ПолучитьОтпечаток(УчетнаяЗапись, ТипСертификата, Объект) Экспорт
	
	Отпечаток = "";
	Если ТипСертификата = "Руководитель" Тогда
		Отпечаток = СокрЛП(УчетнаяЗапись.СертификатРуководителя);
	ИначеЕсли ТипСертификата = "Главный бухгалтер" Тогда
		Отпечаток = СокрЛП(УчетнаяЗапись.СертификатГлавногоБухгалтера);
	ИначеЕсли ТипСертификата = "Шифрование" Тогда
		Отпечаток = СокрЛП(УчетнаяЗапись.СертификатДляШифрования);
	ИначеЕсли ТипСертификата = "Сервер документооборота" Тогда
		Отпечаток = СокрЛП(Объект.Сертификат);
	ИначеЕсли ТипСертификата = "Орган ПФР" ИЛИ ТипСертификата = "Налоговый орган" ИЛИ ТипСертификата = "Орган ФСГС" Тогда
		Отпечаток = Объект.Сертификаты.ВыгрузитьКолонку("Сертификат");
	КонецЕсли;
	Возврат Отпечаток;
	
КонецФункции

Функция ПолучитьСертификатСубъекта(Объект, ТипСертификата = Неопределено) Экспорт
	
	ТипОбъекта = ТипЗнч(Объект);
	
	Отпечаток = "";
	Если ТипСертификата = "Руководитель" Тогда
		Отпечаток = СокрЛП(Объект.СертификатРуководителя);
	ИначеЕсли ТипСертификата = "Главный бухгалтер" Тогда
		Отпечаток = СокрЛП(Объект.СертификатГлавногоБухгалтера);
	ИначеЕсли ТипСертификата = "Шифрование" Тогда
		Отпечаток = СокрЛП(Объект.СертификатДляШифрования);
	Иначе
		ТипОбъекта = ТипЗнч(Объект);
		Если ТипОбъекта = Тип("СправочникСсылка.СерверыДокументооборота") Тогда
			Отпечаток = СокрЛП(Объект.Сертификат);
		ИначеЕсли ТипОбъекта = Тип("СправочникСсылка.НалоговыеОрганы") Тогда
			Отпечаток = Объект.Сертификаты.ВыгрузитьКолонку("Сертификат");
		ИначеЕсли ТипОбъекта = Тип("СправочникСсылка.ОрганыПФР") Тогда
			Если ТипСертификата = Перечисления.ФорматыДокументооборотаСПФР.ЭДОК Тогда
				Отпечаток = Объект.СертификатыЭДОК.ВыгрузитьКолонку("Сертификат");
			Иначе
				Отпечаток = Объект.Сертификаты.ВыгрузитьКолонку("Сертификат");
			КонецЕсли;
		ИначеЕсли ТипОбъекта = Тип("СправочникСсылка.ОрганыФСГС") Тогда
			Отпечаток = Объект.Сертификаты.ВыгрузитьКолонку("Сертификат");
		КонецЕсли;
	КонецЕсли;
	
	Возврат Отпечаток;
	
КонецФункции

Функция ПроверитьСертификатПоПериоду(ОбъектСертификат, ПогрешностьДат = 86400) Экспорт
	
	Если НЕ ЗначениеЗаполнено(ОбъектСертификат) Тогда
		Возврат Истина;
	КонецЕсли;
	
	ТекДата = ТекущаяДатаСеанса();
	Если Дата(ОбъектСертификат.ДействителенС - ПогрешностьДат) > ТекДата Тогда
		
		ТекстСообщения = СтроковыеФункцииКлиентСервер.ПодставитьПараметрыВСтроку(
			НСтр("ru = 'Период действия сертификата %1 еще не наступил!'"), Символ(34) + ПредставлениеСертификатаПоСвойствам(ОбъектСертификат) + Символ(34));
		ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
		
		Возврат Ложь;
	ИначеЕсли Дата(ОбъектСертификат.ДействителенПо + ПогрешностьДат) < ТекДата Тогда
		ТекстСообщения = СтроковыеФункцииКлиентСервер.ПодставитьПараметрыВСтроку(
			НСтр("ru = 'Период действия сертификата %1 истек!'"), Символ(34) + ПредставлениеСертификатаПоСвойствам(ОбъектСертификат) + Символ(34));
		ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
		
		Возврат Ложь;
	КонецЕсли;
	
	Возврат Истина;
	
КонецФункции

Функция ПредставлениеСертификатаПоСвойствам(СвойстваСертификата) Экспорт
	
	Если НЕ ЗначениеЗаполнено(СвойстваСертификата) Тогда
		Возврат "<отсутствует>";
	КонецЕсли;
	
	Если ТипЗнч(СвойстваСертификата) = Тип("ДанныеФормыКоллекция") ИЛИ ТипЗнч(СвойстваСертификата) = Тип("Массив") Тогда
		Возврат Формат(СвойстваСертификата.Количество(), "ЧН=") + " сертификатов";
	КонецЕсли;
	
	Наименование = "";
	Владелец = "";
	ДействителенС = "";
	ДействителенПо = "";
	Отпечаток = "";
	
	СвойстваСертификата.Свойство("Наименование", Наименование);
	СвойстваСертификата.Свойство("ИмяВладельца", Владелец);
	СвойстваСертификата.Свойство("ДействителенС", ДействителенС);
	СвойстваСертификата.Свойство("ДействителенПо", ДействителенПо);
	СвойстваСертификата.Свойство("Отпечаток", Отпечаток);
	
	ПредставлениеСертификата = "";
	Если ЗначениеЗаполнено(Наименование) Тогда
		ПредставлениеСертификата = СокрЛП(Наименование);
	ИначеЕсли ЗначениеЗаполнено(Владелец) Тогда
		ПредставлениеСертификата = СокрЛП(Владелец);
	Иначе
		ПредставлениеСертификата = СокрЛП(Отпечаток);
	КонецЕсли;
	
	Если ЗначениеЗаполнено(ДействителенС) И ЗначениеЗаполнено(ДействителенПо) Тогда
		Если НЕ ЭлектронныйДокументооборотСКонтролирующимиОрганамиКлиентСервер.ЭтоФайрФоксХромИлиСафари() Тогда
		ПредставлениеСертификата = ПредставлениеСертификата + " (" + Формат(ДействителенС, "ДФ='dd.MM.yyyy HH:mm'") + " - " + Формат(ДействителенПо, "ДФ='dd.MM.yyyy HH:mm'") + ")";
		Иначе
			ПредставлениеСертификата = ПредставлениеСертификата + " (" + Строка(ДействителенС) + " - " + Строка(ДействителенПо) + ")";
		КонецЕсли;
	КонецЕсли;
	
	Возврат ПредставлениеСертификата;
	
КонецФункции

Функция ПолучитьОтпечатокСертификатаПолучателя(ТранспортноеСообщениеСсылка) Экспорт

	СертификатПолучателя = Новый Структура;
	СертификатПолучателя.Вставить("Отпечаток", ТранспортноеСообщениеСсылка.УчетнаяЗапись.СертификатДляШифрования);
	СертификатПолучателя.Вставить("ЭтоЭлектроннаяПодписьВМоделиСервиса", ТранспортноеСообщениеСсылка.УчетнаяЗапись.ЭлектроннаяПодписьВМоделиСервиса);
	СертификатПолучателя.Вставить("КлючевойКонтейнер", ТранспортноеСообщениеСсылка.УчетнаяЗапись.ИдентификаторДокументооборота);
	
	Возврат СертификатПолучателя;

КонецФункции

Функция ПолучитьСвойстваСертификатовПоОтпечаткамНаСервере(Отпечатки, ЛогическоеХранилище = Неопределено, ВыполнятьПроверку = Ложь)
	
	Сертификаты = Новый Массив;
	Для Каждого Отпечаток Из Отпечатки Цикл 
		Сертификаты.Добавить(КриптографияЭДКО.НайтиСертификат(Новый Структура("Отпечаток", Отпечаток), ВыполнятьПроверку));
	КонецЦикла;
	
	Возврат Сертификаты;
	
КонецФункции

Функция ПолучитьСвойстваСертификатаПоОтпечаткуНаСервере(Отпечаток, ЛогическоеХранилище = Неопределено, ВыполнятьПроверку = Ложь) Экспорт
	
	Сертификат  = Новый Структура("Отпечаток", Отпечаток);
	Возврат КриптографияЭДКО.НайтиСертификат(Сертификат, ВыполнятьПроверку);
	
КонецФункции

Функция ПолучитьСвойстваСертификатаСубъектаНаСервере(Отпечаток, Хранилище = Неопределено) Экспорт
	
	Если ТипЗнч(Отпечаток) = Тип("Массив") Тогда 		
		Возврат ПолучитьСвойстваСертификатовПоОтпечаткамНаСервере(Отпечаток, Хранилище);
	Иначе
		Возврат ПолучитьСвойстваСертификатаПоОтпечаткуНаСервере(Отпечаток, Хранилище);
	КонецЕсли;
	
КонецФункции

#КонецОбласти

Функция ИмяФайлаЭПФайлаОписиИсходящихДокументов(Знач ИмяФайла) Экспорт
	
	ИмяФайлаБезРасширения = Сред(ИмяФайла, 1, СтрДлина(ИмяФайла) - 4);
	
	// ПРИКАЗ от 28 ноября 2016 г. N ММВ-7-6/643@
	//Имя файла ЭП имеет следующий вид:
	//ИмяФайл_XXX_N.sgn, где:
	//ИмяФайл - имя файла, представляемого в налоговый орган документа в соответствии с утвержденным форматом для данного документа, без расширения и точки;
	//XXX - постфикс файла ЭП, принимающий значение "SGN";
	//N - идентификационный номер файла (GUID);

	GUID 		= ВРЕГ(Строка(Новый УникальныйИдентификатор));
	Результат 	= ИмяФайлаБезРасширения + "_SGN_" + GUID + ".sgn";
	
	Возврат Результат;
	
КонецФункции

Процедура ПолучитьИдентификаторАдресаФИАС(ПараметрыЗапуска) Экспорт
	
	Попытка
	
		Параметры = Новый Массив;
		Параметры.Добавить(ПараметрыЗапуска);
		
		ФоновыеЗадания.Выполнить(
			"ЭлектронныйДокументооборотСКонтролирующимиОрганами.ПолучитьИдентификаторАдресаФИАСФоновоеЗадание", 
			Параметры, 
			, 
			НСтр("ru = 'Получение идентификаторов адресов ФИАС'"));
		
	Исключение
		
		ИмяСобытия = НСтр("ru = 'Электронный документооборот с контролирующими органами. Запуск получения идентификаторов адресов мастера по 1С-Отчетности'", 
			ОбщегоНазначения.КодОсновногоЯзыка());
			
		ИнформацияОбОшибке = ИнформацияОбОшибке();
			
		ЗаписьЖурналаРегистрации(
			ИмяСобытия,
			УровеньЖурналаРегистрации.Ошибка,,,
			ПодробноеПредставлениеОшибки(ИнформацияОбОшибке));
	
	КонецПопытки;
	
КонецПроцедуры

Процедура ПолучитьИдентификаторАдресаФИАСФоновоеЗадание(Параметры) Экспорт
	
	АдресЮридическийЗначение = Параметры.АдресЮридическийЗначение;
	АдресФактическийЗначение = Параметры.АдресФактическийЗначение;
	АдресХранилища			 = Параметры.АдресХранилища;
	
	Попытка
				
		ЭтоАдресПоФИАСу = УправлениеКонтактнойИнформациейКлиентСервер.ЭтоКонтактнаяИнформацияВXML(АдресЮридическийЗначение);
		Если ЭтоАдресПоФИАСу Тогда
			Идентификатор = АдресныйКлассификатор.КодыАдреса(АдресЮридическийЗначение).Идентификатор; 
		Иначе
			Идентификатор = "";
		КонецЕсли;
		Параметры.Вставить("АдресЮридическийИдентификаторПоФИАС", Идентификатор);
		
		Если АдресЮридическийЗначение <> АдресФактическийЗначение Тогда
			ЭтоАдресПоФИАСу = УправлениеКонтактнойИнформациейКлиентСервер.ЭтоКонтактнаяИнформацияВXML(АдресФактическийЗначение);
			Если ЭтоАдресПоФИАСу Тогда
				Идентификатор = АдресныйКлассификатор.КодыАдреса(АдресФактическийЗначение).Идентификатор;
			Иначе
				Идентификатор = "";
			КонецЕсли;
		КонецЕсли;
		Параметры.Вставить("АдресФактическийИдентификаторПоФИАС", Идентификатор);
		
		ПоместитьВоВременноеХранилище(Параметры, АдресХранилища);
		
	Исключение
		
		ИмяСобытия = НСтр("ru = 'Электронный документооборот с контролирующими органами. Получение идентификатор адресов мастера по 1С-Отчетности'", 
			ОбщегоНазначения.КодОсновногоЯзыка());
			
		ИнформацияОбОшибке = ИнформацияОбОшибке();
		
		ЗаписьЖурналаРегистрации(
			ИмяСобытия,
			УровеньЖурналаРегистрации.Ошибка,,,
			ПодробноеПредставлениеОшибки(ИнформацияОбОшибке));
			
		ПоместитьВоВременноеХранилище(Неопределено, АдресХранилища);
	
	КонецПопытки;
	
КонецПроцедуры

Процедура ПолучитьДанныеССервераКА(ПараметрыЗапуска) Экспорт
	
	Попытка
	
		Параметры = Новый Массив;
		Параметры.Добавить(ПараметрыЗапуска);
		
		ФоновыеЗадания.Выполнить(
			"ЭлектронныйДокументооборотСКонтролирующимиОрганами.ПолучитьДанныеССервераКАФоновоеЗадание", 
			Параметры, 
			, 
			НСтр("ru = 'Получение данных УЦ'"));
		
	Исключение
		
		ИмяСобытия = НСтр("ru = 'Электронный документооборот с контролирующими органами. Получение данных УЦ для заявления'", 
			ОбщегоНазначения.КодОсновногоЯзыка());
			
		ИнформацияОбОшибке = ИнформацияОбОшибке();
			
		ЗаписьЖурналаРегистрации(
			ИмяСобытия,
			УровеньЖурналаРегистрации.Ошибка,,,
			ПодробноеПредставлениеОшибки(ИнформацияОбОшибке));
	
	КонецПопытки;
	
КонецПроцедуры

Процедура ПолучитьДанныеССервераКАФоновоеЗадание(Параметры) Экспорт
	
	АдресХранилища = Параметры.АдресХранилища;
	
	Попытка
		
		ДанныеУЦ = ДанныеУЦИзВебСервиса(Параметры.Спецоператор);
		Параметры.Вставить("ДанныеУЦ", ДанныеУЦ);
		
		ДоверенныеСторонниеУЦ = ДоверенныеСторонниеУЦ();
		Параметры.Вставить("ДоверенныеСторонниеУЦ", ДоверенныеСторонниеУЦ);
		
		ПоместитьВоВременноеХранилище(Параметры, АдресХранилища);
		
	Исключение
		
		ИмяСобытия = НСтр("ru = 'Электронный документооборот с контролирующими органами. Получение данных УЦ и СУЦ для заявления'", 
			ОбщегоНазначения.КодОсновногоЯзыка());
			
		ИнформацияОбОшибке = ИнформацияОбОшибке();
		
		ЗаписьЖурналаРегистрации(
			ИмяСобытия,
			УровеньЖурналаРегистрации.Ошибка,,,
			ПодробноеПредставлениеОшибки(ИнформацияОбОшибке));
			
		ПоместитьВоВременноеХранилище(Неопределено, АдресХранилища);
	
	КонецПопытки;
	
КонецПроцедуры

Функция ДанныеУЦИзВебСервиса(Спецоператор)
	
	ПараметрыОператораСвязи = ПолучитьПараметрыОператораСвязи(Спецоператор);
	
	ДополнительныеПараметры = Новый Структура();
	ДополнительныеПараметры.Вставить("Определение", 				ПараметрыОператораСвязи.ВебСервисОпределение);
	ДополнительныеПараметры.Вставить("URIПространстваИменСервиса", 	ПараметрыОператораСвязи.ВебСервисURIПространстваИменСервиса);
	ДополнительныеПараметры.Вставить("ИмяСервиса", 					ПараметрыОператораСвязи.ВебСервисИмяСервиса);
	ДополнительныеПараметры.Вставить("ИмяТочкиПодключения", 		ПараметрыОператораСвязи.ВебСервисИмяТочкиПодключения);

	Сервис = ЭлектронныйДокументооборотСКонтролирующимиОрганами.ОбменССерверомСоздатьСервис(,,,ДополнительныеПараметры,Ложь);
	
	Если Сервис = Неопределено Тогда
		Возврат Неопределено;
	КонецЕсли;
	
	Попытка
		XDTOРезультат = Сервис.GetCaInfo1C(РегламентированнаяОтчетность.НазваниеИВерсияПрограммы());
	Исключение
		
		ТекстОшибки = СтроковыеФункцииКлиентСервер.ПодставитьПараметрыВСтроку(
					НСтр("ru = 'Не удалось получить наименование УЦ:
					|%1'"), ИнформацияОбОшибке().Описание);
					
		ЭлектронныйДокументооборотСКонтролирующимиОрганамиКлиентСервер.СообщитьИлиПередатьВМастерОшибку(
			ТекстОшибки, 
			, 
			, 
			Ложь);
			
		Возврат Неопределено;
	КонецПопытки;
	
	ЧтениеXML = Новый ЧтениеXML;
	ЧтениеXML.УстановитьСтроку(XDTOРезультат);
	
	ПостроительДОМ 	= Новый ПостроительDOM();
	ДОМ 			= ПостроительДОМ.Прочитать(ЧтениеXML);
	УзелDOM 		= ДОМ.ПолучитьЭлементыПоИмени("code");
	кодРезультата 	= УзелDOM[0].ТекстовоеСодержимое;
	
	
	Если кодРезультата = "0" Тогда
		
		УзелDOM 		= ДОМ.ПолучитьЭлементыПоИмени("ca");
		НаименованиеУЦ 	= УзелDOM[0].ТекстовоеСодержимое;
		
		УзелDOM 		= ДОМ.ПолучитьЭлементыПоИмени("caReglament");
		АдресРегламента = УзелDOM[0].ТекстовоеСодержимое;
		
		УзелDOM 		= ДОМ.ПолучитьЭлементыПоИмени("toWhom");
		Получатель		= УзелDOM[0].ТекстовоеСодержимое;		
		
		ДополнительныеПараметры = Новый Структура();
		ДополнительныеПараметры.Вставить("НаименованиеУЦ",  НаименованиеУЦ);
		ДополнительныеПараметры.Вставить("РегламентУЦ", 	АдресРегламента);
		ДополнительныеПараметры.Вставить("ПолучательЗаявленияВУЦ", Получатель);
		
		Возврат ДополнительныеПараметры;
		
	Иначе
		
		Возврат Неопределено;
		
	КонецЕсли;
	
	ЧтениеXML.Закрыть();
	
КонецФункции

Функция ПолучитьПараметрыОператораСвязи(Знач Спецоператор) Экспорт
	
	Макет = ПолучитьМакет("ПараметрыСпецоператоровСвязи");
	
	// Инициализируем ключи, чтобы не было падения при попытке получить значение из Неопределено
	Результат = Новый Структура();
	Для НомерСтолбца = 1 По Макет.ШиринаТаблицы Цикл
		Ключ = Макет.Область(0, НомерСтолбца, 0, НомерСтолбца).Имя;
		Результат.Вставить(Ключ, "");
	КонецЦикла;

	Если ЗначениеЗаполнено(Спецоператор) Тогда
		
		Если ТипЗнч(Спецоператор) = Тип("ПеречислениеСсылка.СпецоператорыСвязи") Тогда
			ИмяСпецоператора = ЭлектронныйДокументооборотСКонтролирующимиОрганамиВызовСервера.ПолучитьИмяЗначенияПеречисленияСпецоператорыСвязиПоСинониму(Спецоператор);
		Иначе
			ИмяСпецоператора = Спецоператор.Имя;
		КонецЕсли;
		
		Для НомерСтроки = 2 По Макет.ВысотаТаблицы Цикл
			
			ИмяТекущейСтроки = Макет.Область(НомерСтроки, 1).Текст;
			
			Если НРег(ИмяТекущейСтроки) = НРег(ИмяСпецоператора) Тогда
				
				Для НомерСтолбца = 1 По Макет.ШиринаТаблицы Цикл
					
					Ключ = Макет.Область(0, НомерСтолбца, 0, НомерСтолбца).Имя;
					Значение = Макет.Область(НомерСтроки, НомерСтолбца, НомерСтроки, НомерСтолбца).Текст;
					
					Результат[Ключ] = Значение;
					
				КонецЦикла;
			КонецЕсли;
			
		КонецЦикла;
		
	КонецЕсли;
	
	Возврат Результат;
	
КонецФункции

#Область ДиагностикаОтчетности_ОбщегоНазначения

Функция ВЧисло(Знач Значение) Экспорт
	
	Если ТипЗнч(Значение) = Тип("Число") Тогда 
		Возврат Значение;
	КонецЕсли;
	Если ТипЗнч(Значение) = Тип("Неопределено") Тогда 
		Возврат 0;
	КонецЕсли;
	Значение = СтрЗаменить(Значение, " ", "");
	Значение = СтрЗаменить(Значение, " ", "");
	Значение = СтрЗаменить(Значение, ", ", ".");
	Попытка
		Значение = Число(Значение);
	Исключение
		Значение = 0;
	КонецПопытки;
	Возврат Значение;
	
КонецФункции

Функция ПротоколПоПорту(Знач Порт) Экспорт
	
	Если Порт = Неопределено Тогда 
		Порт = 80;
	КонецЕсли;
	
	Порт = ВЧисло(Порт);
	Если Порт = 80 Тогда 
		Возврат "http://";
	ИначеЕсли Порт = 8090 Тогда 
		Возврат "http://";
	ИначеЕсли Порт = 443 Тогда 
		Возврат "https://";
	ИначеЕсли Порт = 110 Тогда 
		Возврат "pop3:";
	ИначеЕсли Порт = 995 Тогда 
		Возврат "pop3s:";
	ИначеЕсли Порт = 25 Тогда 
		Возврат "smtp:";
	ИначеЕсли Порт = 465 Тогда 
		Возврат "smtps:";
	КонецЕсли;
	
	Возврат "";
	
КонецФункции

Функция ПолучитьURL(ПП) Экспорт
	
	URL = "";
	
	Если ПП.Свойство("Адрес") Тогда 
		URL = URL + ПП.Адрес;
		Если ПП.Порт <> Неопределено И ПП.Порт <> 80 И ПП.Порт <> 443 Тогда 
			URL = URL + ":" + ПП.Порт;
		КонецЕсли;
		Если ПП.Урл = Неопределено или ПП.Урл = "" Тогда 
			URL = URL + "/";
		Иначе
			URL = URL + ПП.Урл;
		КонецЕсли;			
		URL = ПротоколПоПорту(ПП.Порт) + URL;
	Иначе
		
		Если ПП.АдресPOP3="" Тогда 
			URL = URL + ПП.АдресSMTP;
			Если ПП.ПортSMTP <> Неопределено Тогда 
				URL = URL + ":" + ПП.ПортSMTP;
			КонецЕсли;
		ИначеЕсли ПП.АдресSMTP="" Тогда 
			URL = URL + ПП.АдресPOP3;
			Если ПП.ПортPOP3 <> Неопределено Тогда 
				URL = URL + ":" + ПП.ПортPOP3;
			КонецЕсли;
		КонецЕсли;
		
	КонецЕсли;
	
	Возврат URL;
	
КонецФункции

Функция ПростоеСклонение(Знач Числ, Форма1, Форма2, Форма3) Экспорт
	
	Числ = ?(Числ < 0, -Числ, Числ) % 100;
	Числ1 = Числ % 10;
	Если (Числ > 10) И (Числ < 20) Тогда 
		Возврат Форма3;
	КонецЕсли;
	Если (Числ1 > 1) И (Числ1 < 5) Тогда 
		Возврат Форма2;
	КонецЕсли;
	Если (Числ1 = 1) Тогда 
		Возврат Форма1;
	КонецЕсли;
	
	Возврат Форма3;
	
КонецФункции

Функция ИнтервалСтрокой(Знач Дата1, Знач Дата2) Экспорт
	
	Дата1 = НачалоДня(Дата1);
	Дата2 = НачалоДня(Дата2);
	Разница = (Дата2 - Дата1) / 86400;
	Если Разница = 0 Тогда 
		Возврат "сегодня";
	ИначеЕсли Разница = 1 Тогда 
		Возврат "завтра";
	ИначеЕсли Разница = -1 Тогда 
		Возврат "вчера";
	ИначеЕсли Разница = -2 Тогда 
		Возврат "позавчера";
	ИначеЕсли Разница = 2 Тогда 
		Возврат "послезавтра";
	ИначеЕсли Разница = 7 Тогда 
		Возврат "через неделю";
	ИначеЕсли Разница = -7 Тогда 
		Возврат "неделю назад";
	ИначеЕсли Разница > 0 Тогда 
		
		Строкой = "через %1 "+ПростоеСклонение(Разница, "день", "дня", "дней");
		Строкой = СтроковыеФункцииКлиентСервер.ПодставитьПараметрыВСтроку(Строкой, Формат(Разница, "ЧДЦ=; ЧН=; ЧГ="));
		
		Возврат Строкой;
	ИначеЕсли Разница < 0 Тогда 
		
		Строкой = "%1 "+ПростоеСклонение(Разница, "день", "дня", "дней")+" назад";
		Строкой = СтроковыеФункцииКлиентСервер.ПодставитьПараметрыВСтроку(Строкой, Формат(-Разница, "ЧДЦ=; ЧН=; ЧГ="));
		
		Возврат Строкой;
	КонецЕсли;
	
КонецФункции

Функция ОберткаСклонения(Что, Склонение) Экспорт
	
	МодульСклонения = ОбщегоНазначения.ОбщийМодуль("СклонениеПредставленийОбъектов");
	
	Если МодульСклонения = Неопределено Тогда
		Возврат Что;
	КонецЕсли;
		
	Попытка		
		Ответ = МодульСклонения.ПросклонятьФИО(Что, Склонение);		
	Исключение
		Ответ = Что;
	КонецПопытки;
	
	Возврат Ответ;
	
КонецФункции
	
Процедура СклонятьВРп(Знач Вх0, Исх0, 
			Знач Вх1 = Неопределено, 
			Исх1 = Неопределено, 
			Знач Вх2 = Неопределено, 
			Исх2 = Неопределено, 
			Знач Вх3 = Неопределено, 
			Исх3 = Неопределено, Знач 
			Вх4 = Неопределено, 
			Исх4 = Неопределено, 
			Знач Вх5 = Неопределено, 
			Исх5 = Неопределено) Экспорт
	
	Исх0 = ОберткаСклонения(Вх0, 2);
	Если Вх1 <> Неопределено Тогда 
		Исх1 = ОберткаСклонения(Вх1, 2);
	КонецЕсли;
	Если Вх2 <> Неопределено Тогда 
		Исх2 = ОберткаСклонения(Вх2, 2);
	КонецЕсли;
	Если Вх3 <> Неопределено Тогда 
		Исх3 = ОберткаСклонения(Вх3, 2);
	КонецЕсли;
	Если Вх4 <> Неопределено Тогда 
		Исх4 = ОберткаСклонения(Вх4, 2);
	КонецЕсли;
	Если Вх5 <> Неопределено Тогда 
		Исх5 = ОберткаСклонения(Вх5, 2);
	КонецЕсли;
	
КонецПроцедуры

Процедура СклонятьВВп(Знач Вх0, Исх0, 
			Знач Вх1 = Неопределено, 
			Исх1 = Неопределено, 
			Знач Вх2 = Неопределено, 
			Исх2 = Неопределено, 
			Знач Вх3 = Неопределено, 
			Исх3 = Неопределено, 
			Знач Вх4 = Неопределено, 
			Исх4 = Неопределено, 
			Знач Вх5 = Неопределено, 
			Исх5 = Неопределено) Экспорт
	
	Исх0 = ОберткаСклонения(Вх0, 4);
	Если Вх1 <> Неопределено Тогда 
		Исх1 = ОберткаСклонения(Вх1, 4);
	КонецЕсли;
	Если Вх2 <> Неопределено Тогда 
		Исх2 = ОберткаСклонения(Вх2, 4);
	КонецЕсли;
	Если Вх3 <> Неопределено Тогда 
		Исх3 = ОберткаСклонения(Вх3, 4);
	КонецЕсли;
	Если Вх4 <> Неопределено Тогда 
		Исх4 = ОберткаСклонения(Вх4, 4);
	КонецЕсли;
	Если Вх5 <> Неопределено Тогда 
		Исх5 = ОберткаСклонения(Вх5, 4);
	КонецЕсли;
	
КонецПроцедуры

Процедура СклонятьВДп(Знач Вх0, Исх0, 
			Знач Вх1 = Неопределено, 
			Исх1 = Неопределено, 
			Знач Вх2 = Неопределено, 
			Исх2 = Неопределено, 
			Знач Вх3 = Неопределено, 
			Исх3 = Неопределено) Экспорт
	
	Исх0 = ОберткаСклонения(Вх0, 3);
	Если Вх1 <> Неопределено Тогда 
		Исх1 = ОберткаСклонения(Вх1, 3);
	КонецЕсли;
	Если Вх2 <> Неопределено Тогда 
		Исх2 = ОберткаСклонения(Вх2, 3);
	КонецЕсли;
	Если Вх3 <> Неопределено Тогда 
		Исх3 = ОберткаСклонения(Вх3, 3);
	КонецЕсли;
	
КонецПроцедуры

Функция НормализованныеФИО(Знач Фамилия, Знач Имя = "", Знач Отчество = "") Экспорт
	
	Если ТипЗнч(Фамилия) = Тип("Структура") Тогда 
		Имя = Фамилия.Имя;
		Отчество = Фамилия.Отчество;	
		Фамилия = Фамилия.Фамилия;
	КонецЕсли;
	
	Фамилия = СтрЗаменить(Фамилия, "  ", " ");
	Имя = СтрЗаменить(Имя, "  ", " ");
	Отчество = СтрЗаменить(Отчество, "  ", " ");
	
	Возврат ТРег(СокрЛП(Фамилия) + ?(ЗначениеЗаполнено(Имя), " ", "") + СокрЛП(Имя) + ?(ЗначениеЗаполнено(Отчество), " ", "") + СокрЛП(Отчество));
	
КонецФункции

Функция ТекущаяДатаСеансаКлиент(ЧасовойПояс = Неопределено) Экспорт
	
	ЧасовойПояс = ЧасовойПоясСеанса();
	Возврат ТекущаяДатаСеанса();
	
КонецФункции

#КонецОбласти

#Область АрхивацияДО_ОбщегоНазначения

Функция ОбъектВАрхиве(Знач ОбъектОтправки, Знач ИмяФайла = "") Экспорт
	
	ВидВладельцаМета2 = Неопределено;
	
	Если ТипЗнч(ОбъектОтправки) = Тип("СправочникСсылка.ОтправкиФТС")
		Или ТипЗнч(ОбъектОтправки) = Тип("СправочникОбъект.ОтправкиФТС") Тогда		
		ВидВладельцаМета = Перечисления.ВидыАрхивируемыхМетаданныхДО.ОтправкиФТС;
		
	ИначеЕсли ТипЗнч(ОбъектОтправки) = Тип("СправочникСсылка.ОтправкиРПН")
		Или ТипЗнч(ОбъектОтправки) = Тип("СправочникОбъект.ОтправкиРПН") Тогда		
		ВидВладельцаМета = Перечисления.ВидыАрхивируемыхМетаданныхДО.ОтправкиРПН;
		
	ИначеЕсли ТипЗнч(ОбъектОтправки) = Тип("СправочникСсылка.ОтправкиФСС")
		Или ТипЗнч(ОбъектОтправки) = Тип("СправочникОбъект.ОтправкиФСС") Тогда		
		ВидВладельцаМета = Перечисления.ВидыАрхивируемыхМетаданныхДО.ОтправкиФСС;
		
	ИначеЕсли ТипЗнч(ОбъектОтправки) = Тип("СправочникСсылка.ОтправкиФСРАР")
		Или ТипЗнч(ОбъектОтправки) = Тип("СправочникОбъект.ОтправкиФСРАР") Тогда		
		ВидВладельцаМета = Перечисления.ВидыАрхивируемыхМетаданныхДО.ОтправкиФСРАР;
		
	ИначеЕсли ТипЗнч(ОбъектОтправки) = Тип("СправочникСсылка.СканированныеДокументыДляПередачиВЭлектронномВиде")
		Или ТипЗнч(ОбъектОтправки) = Тип("СправочникОбъект.СканированныеДокументыДляПередачиВЭлектронномВиде") Тогда		
		ВидВладельцаМета = Перечисления.ВидыАрхивируемыхМетаданныхДО.ФайлыДокументовРеализацииПолномочийНалоговыхОрганов;
		
	ИначеЕсли ТипЗнч(ОбъектОтправки) = Тип("СправочникСсылка.ДокументыРеализацииПолномочийНалоговыхОрганов")
		Или ТипЗнч(ОбъектОтправки) = Тип("СправочникОбъект.ДокументыРеализацииПолномочийНалоговыхОрганов") Тогда		
		ВидВладельцаМета = Перечисления.ВидыАрхивируемыхМетаданныхДО.ФайлыДокументовРеализацииПолномочийНалоговыхОрганов;
		
	ИначеЕсли ТипЗнч(ОбъектОтправки) = Тип("СправочникСсылка.ОписиВходящихДокументовИзНалоговыхОрганов")
		Или ТипЗнч(ОбъектОтправки) = Тип("СправочникОбъект.ОписиВходящихДокументовИзНалоговыхОрганов") Тогда		
		ВидВладельцаМета = Перечисления.ВидыАрхивируемыхМетаданныхДО.ФайлыДокументовРеализацииПолномочийНалоговыхОрганов;
		
	ИначеЕсли ТипЗнч(ОбъектОтправки) = Тип("СправочникСсылка.ОписиИсходящихДокументовВНалоговыеОрганы")
		Или ТипЗнч(ОбъектОтправки) = Тип("СправочникОбъект.ОписиИсходящихДокументовВНалоговыеОрганы") Тогда		
		ВидВладельцаМета = Перечисления.ВидыАрхивируемыхМетаданныхДО.ФайлыДокументовРеализацииПолномочийНалоговыхОрганов;
		
	ИначеЕсли ТипЗнч(ОбъектОтправки) = Тип("СправочникСсылка.ЭлектронныеПредставленияРегламентированныхОтчетов")
		Или ТипЗнч(ОбъектОтправки) = Тип("СправочникОбъект.ЭлектронныеПредставленияРегламентированныхОтчетов") Тогда		
		ВидВладельцаМета = Перечисления.ВидыАрхивируемыхМетаданныхДО.ХранилищеЭлектронныхПредставленийРегламентированныхОтчетов;
		
	ИначеЕсли ТипЗнч(ОбъектОтправки) = Тип("СправочникСсылка.ПерепискаСКонтролирующимиОрганами")
		Или ТипЗнч(ОбъектОтправки) = Тип("СправочникОбъект.ПерепискаСКонтролирующимиОрганами") Тогда		
		ВидВладельцаМета = Перечисления.ВидыАрхивируемыхМетаданныхДО.ВложенияНеформализованныхДокументов;
		
	ИначеЕсли ТипЗнч(ОбъектОтправки) = Тип("ДокументСсылка.ЗапросНаИнформационноеОбслуживаниеСтрахователя")
		Или ТипЗнч(ОбъектОтправки) = Тип("ДокументОбъект.ЗапросНаИнформационноеОбслуживаниеСтрахователя") Тогда		
		ВидВладельцаМета = Перечисления.ВидыАрхивируемыхМетаданныхДО.ОтветыНаЗапросыИОС;
		
	ИначеЕсли ТипЗнч(ОбъектОтправки) = Тип("ДокументСсылка.ЗапросНаИнформационноеОбслуживаниеНалогоплательщика")
		Или ТипЗнч(ОбъектОтправки) = Тип("ДокументОбъект.ЗапросНаИнформационноеОбслуживаниеНалогоплательщика") Тогда		
		ВидВладельцаМета = Перечисления.ВидыАрхивируемыхМетаданныхДО.ОтветыНаЗапросыИОН;
		
	ИначеЕсли ТипЗнч(ОбъектОтправки) = Тип("ДокументСсылка.ЗапросНаВыпискуИзЕГРЮЛ_ЕГРИП")
		Или ТипЗнч(ОбъектОтправки) = Тип("ДокументОбъект.ЗапросНаВыпискуИзЕГРЮЛ_ЕГРИП") Тогда		
		ВидВладельцаМета = Перечисления.ВидыАрхивируемыхМетаданныхДО.ОтветыНаЗапросыВыпискиИзЕГРЮЛ_ЕГРИП;
		
	ИначеЕсли ТипЗнч(ОбъектОтправки) = Тип("ДокументСсылка.ТранспортноеСообщение")
		Или ТипЗнч(ОбъектОтправки) = Тип("ДокументОбъект.ТранспортноеСообщение") Тогда		
		ВидВладельцаМета = Перечисления.ВидыАрхивируемыхМетаданныхДО.ТранспортныеКонтейнеры;
		ВидВладельцаМета2 = Перечисления.ВидыАрхивируемыхМетаданныхДО.СодержимоеТранспортныхКонтейнеров;
		
	Иначе
		
		Возврат Ложь;
		
	КонецЕсли;
	
	Запрос = Новый Запрос;
	Запрос.Текст = "ВЫБРАТЬ
	                      |	ПризнакиАрхивирования.Объект КАК Объект,
	                      |	ПризнакиАрхивирования.ИмяФайла КАК ИмяФайла,
	                      |	ПризнакиАрхивирования.Владелец КАК Владелец,
	                      |	ПризнакиАрхивирования.Архивный КАК Архивный
	                      |ИЗ
	                      |	РегистрСведений.ПризнакиАрхивированияФайловДОСКонтролирующимиОрганами КАК ПризнакиАрхивирования
	                      |ГДЕ
	                      |	ПризнакиАрхивирования.Объект = &Объект";
	
	Запрос.Параметры.Вставить("Объект", ОбъектОтправки.Ссылка);
	
	Если ИмяФайла <> Неопределено Тогда
		Запрос.Текст = Запрос.Текст + "
		|	И ПризнакиАрхивирования.ИмяФайла = &ИмяФайла";
		Запрос.Параметры.Вставить("ИмяФайла", ИмяФайла);
	КонецЕсли;
	
	Если ВидВладельцаМета2 = Неопределено Тогда 		
		Запрос.Текст = Запрос.Текст + "
		|	И ПризнакиАрхивирования.Владелец = &Владелец";
		Запрос.Параметры.Вставить("Владелец", ВидВладельцаМета);
	Иначе
		ВидыВладельцев = Новый Массив;
		ВидыВладельцев.Добавить(ВидВладельцаМета);
		ВидыВладельцев.Добавить(ВидВладельцаМета2);	
		
		Запрос.Текст = Запрос.Текст + "
		|	И ПризнакиАрхивирования.Владелец в (&Владелец)";
		Запрос.Параметры.Вставить("Владелец", ВидыВладельцев);
	КонецЕсли;
	
	Результат = Запрос.Выполнить();
	Если Результат.Пустой() Тогда 
		Возврат Ложь;
	КонецЕсли;
	
	ТабРезультат = Результат.Выгрузить();
	Для Каждого Запись Из ТабРезультат Цикл
		Если Запись.Архивный Тогда
			Возврат Истина;
		КонецЕсли;
	КонецЦикла;
			
	Возврат Ложь;
	
КонецФункции

#КонецОбласти

#Область ЗаявлениеНаПереход

Процедура ПередВыгрузкойДанных(Контейнер) Экспорт
	
	ЗапомнитьМодельРаботыСКлючами(Истина);
	
КонецПроцедуры

Процедура ПослеВыгрузкиДанных(Контейнер) Экспорт
	
	УстановитьПривилегированныйРежим(Истина);
	МодельРаботыСКлючамиПередВыгрузкой = ХранилищеОбщихНастроек.Загрузить("ДокументооборотСКонтролирующимиОрганами_МодельДанныхПередВыгрузкой");
	Константы.МодельРаботыСКлючамиПередВыгрузкой.Установить(МодельРаботыСКлючамиПередВыгрузкой);
	ХранилищеОбщихНастроек.Удалить("ДокументооборотСКонтролирующимиОрганами_МодельДанныхПередВыгрузкой", , Неопределено);
	
КонецПроцедуры

Процедура ПослеЗагрузкиДанных(Контейнер) Экспорт
	
	ЗапомнитьМодельРаботыСКлючами(Ложь);
	
КонецПроцедуры

Процедура ЗапомнитьМодельРаботыСКлючами(ЭтоВыгрузка)
	
	УстановитьПривилегированныйРежим(Истина);
	
	ЕстьАктивныеУчетныеЗаписи = ЭлектронныйДокументооборотИспользуется();
	
	Если ЕстьАктивныеУчетныеЗаписи Тогда
		
		ТекущаяМодель = ТекущаяМодельРаботыСКлючами();
		
		Если ЭтоВыгрузка Тогда
			
			// Запоминаем предыдущую модель, чтобы потом восстановить исходное значение после выгрузки, 
			// чтобы выгрузка не повлияла на состояние выгружаемой базы.
			ХранилищеОбщихНастроек.Сохранить("ДокументооборотСКонтролирующимиОрганами_МодельДанныхПередВыгрузкой", , Константы.МодельРаботыСКлючамиПередВыгрузкой.Получить());
			Константы.МодельРаботыСКлючамиПередВыгрузкой.Установить(ТекущаяМодель);
			
		Иначе
			
			// Очищаем все "больше не показывать" и откладывания напоминаний по всем пользователям.
			// Делаем это при загрузке, потому что выгрузка может быть много раз и в этой базе признаки надо сохранить.
			ХранилищеОбщихНастроек.Удалить("ДокументооборотСКонтролирующимиОрганами_ДатыНапоминанийОПереходах", , Неопределено);
			
			Константы.ДатаЗагрузкиБазы.Установить(ТекущаяДатаСеанса());
			
			ОтметитьПослеЗагрузкиДанныхУчетныеЗаписиТребующиеПредупрежденияОбОшибках();
			
		КонецЕсли;
		
	КонецЕсли;
	
КонецПроцедуры

Функция ТекущаяМодельРаботыСКлючами() Экспорт
	
	ДоступнаЭлектроннаяПодписьВМоделиСервиса = ЭлектроннаяПодписьВМоделиСервиса.ИспользованиеВозможно();
	Если ДоступнаЭлектроннаяПодписьВМоделиСервиса Тогда
		ТекущаяМодель = Перечисления.МодельРаботыСКлючами.ВМоделиСервиса;
	Иначе
		ТекущаяМодель = Перечисления.МодельРаботыСКлючами.ТолькоЛокальныеКлючи;
	КонецЕсли;
	
	Возврат ТекущаяМодель;
	
КонецФункции

Процедура ОтметитьПослеЗагрузкиДанныхУчетныеЗаписиТребующиеПредупрежденияОбОшибках()
	
	БылВыполненПереход 	= БылВыполненПререходВДругуюМодельХраненияКлючей();
	
	Если НЕ БылВыполненПереход Тогда
		Возврат;
	КонецЕсли;
	
	Запрос = Новый Запрос("ВЫБРАТЬ РАЗРЕШЕННЫЕ РАЗЛИЧНЫЕ
	                      |	Организации.УчетнаяЗаписьОбмена КАК УчетнаяЗапись,
	                      |	ВЫБОР
	                      |		КОГДА &ЭтоПереходВКоробку
	                      |					И Организации.УчетнаяЗаписьОбмена.ЭлектроннаяПодписьВМоделиСервиса
	                      |				ИЛИ &ЭтоПереходВОблако
	                      |					И НЕ Организации.УчетнаяЗаписьОбмена.ЭлектроннаяПодписьВМоделиСервиса
	                      |			ТОГДА ИСТИНА
	                      |		ИНАЧЕ ЛОЖЬ
	                      |	КОНЕЦ КАК ЕстьОшибкиСменыМоделиХраненияКлючей
	                      |ИЗ
	                      |	Справочник.Организации КАК Организации
	                      |ГДЕ
	                      |	НЕ Организации.УчетнаяЗаписьОбмена.ПометкаУдаления
	                      |	И Организации.ВидОбменаСКонтролирующимиОрганами = ЗНАЧЕНИЕ(Перечисление.ВидыОбменаСКонтролирующимиОрганами.ОбменВУниверсальномФормате)
	                      |	И Организации.ПометкаУдаления = ЛОЖЬ");
	
	ТекущаяМодель 		= ТекущаяМодельРаботыСКлючами();
	ЭтоПереходВКоробку  = ТекущаяМодель = Перечисления.МодельРаботыСКлючами.ТолькоЛокальныеКлючи;
	
	Запрос.УстановитьПараметр("ЭтоПереходВКоробку", ЭтоПереходВКоробку);
	Запрос.УстановитьПараметр("ЭтоПереходВОблако",  НЕ ЭтоПереходВКоробку);
	
	Выборка = Запрос.Выполнить().Выбрать();
	
	Пока Выборка.Следующий() Цикл
		
		УчетнаяЗапись = Выборка.УчетнаяЗапись;
		
		УчетнаяЗаписьОбъект = УчетнаяЗапись.ПолучитьОбъект();
		УчетнаяЗаписьОбъект.ЕстьОшибкиСменыМоделиХраненияКлючей = Выборка.ЕстьОшибкиСменыМоделиХраненияКлючей;
		УчетнаяЗаписьОбъект.ОбменДанными.Загрузка = Истина;
		УчетнаяЗаписьОбъект.Записать();
	
	КонецЦикла; 

КонецПроцедуры

Функция ТаблицаОрганизацийТребующихНапоминанияОПереходе() Экспорт
	
	Запрос = Новый Запрос("ВЫБРАТЬ РАЗРЕШЕННЫЕ
	                      |	ЗаявлениеАбонентаСпецоператораСвязиИзменившиесяРеквизитыВторичногоЗаявления.Ссылка.УчетнаяЗапись КАК УчетнаяЗапись
	                      |ПОМЕСТИТЬ УчетныеЗаписиЗаявлений
	                      |ИЗ
	                      |	Документ.ЗаявлениеАбонентаСпецоператораСвязи.ИзменившиесяРеквизитыВторичногоЗаявления КАК ЗаявлениеАбонентаСпецоператораСвязиИзменившиесяРеквизитыВторичногоЗаявления
	                      |ГДЕ
	                      |	ЗаявлениеАбонентаСпецоператораСвязиИзменившиесяРеквизитыВторичногоЗаявления.Ссылка.ДатаОтправкиЗаявления > &ДатаЗагрузкиБазы
	                      |	И ЗаявлениеАбонентаСпецоператораСвязиИзменившиесяРеквизитыВторичногоЗаявления.Ссылка.ПометкаУдаления = ЛОЖЬ
	                      |	И ЗаявлениеАбонентаСпецоператораСвязиИзменившиесяРеквизитыВторичногоЗаявления.ИзмененныйРеквизит В (ЗНАЧЕНИЕ(Перечисление.ПараметрыПодключенияК1СОтчетности.ПереходВКоробку), ЗНАЧЕНИЕ(Перечисление.ПараметрыПодключенияК1СОтчетности.ПереходВОблако), ЗНАЧЕНИЕ(Перечисление.ПараметрыПодключенияК1СОтчетности.ИзменениеКриптопровайдера), ЗНАЧЕНИЕ(Перечисление.ПараметрыПодключенияК1СОтчетности.ПереизданиеСертификата))
	                      |	И (ЗаявлениеАбонентаСпецоператораСвязиИзменившиесяРеквизитыВторичногоЗаявления.Ссылка.ДатаОтправкиЗаявления > &ДвеНеделиНазад
	                      |				И ЗаявлениеАбонентаСпецоператораСвязиИзменившиесяРеквизитыВторичногоЗаявления.Ссылка.Статус = &Отправлено
	                      |			ИЛИ ЗаявлениеАбонентаСпецоператораСвязиИзменившиесяРеквизитыВторичногоЗаявления.Ссылка.Статус = &Одобрено)
	                      |	И ЗаявлениеАбонентаСпецоператораСвязиИзменившиесяРеквизитыВторичногоЗаявления.Ссылка.ТипЗаявления = ЗНАЧЕНИЕ(Перечисление.ТипыЗаявленияАбонентаСпецоператораСвязи.Изменение)
	                      |	И ЗаявлениеАбонентаСпецоператораСвязиИзменившиесяРеквизитыВторичногоЗаявления.Ссылка.УчетнаяЗапись.ЕстьОшибкиСменыМоделиХраненияКлючей = ИСТИНА
	                      |;
	                      |
	                      |////////////////////////////////////////////////////////////////////////////////
	                      |ВЫБРАТЬ РАЗРЕШЕННЫЕ РАЗЛИЧНЫЕ
	                      |	Организации.Ссылка КАК Организация,
	                      |	Организации.УчетнаяЗаписьОбмена КАК УчетнаяЗапись,
	                      |	ИСТИНА КАК ТребуетсяНапоминание
	                      |ИЗ
	                      |	Справочник.Организации КАК Организации
	                      |		ВНУТРЕННЕЕ СОЕДИНЕНИЕ РегистрСведений.ПользователиУчетныхЗаписейДокументооборота КАК ПользователиУчетныхЗаписейДокументооборота
	                      |		ПО Организации.УчетнаяЗаписьОбмена.Ссылка = ПользователиУчетныхЗаписейДокументооборота.УчетнаяЗапись
	                      |ГДЕ
	                      |	(&НуженОтборПоПользователям
	                      |				И ПользователиУчетныхЗаписейДокументооборота.Пользователь = &Пользователь
	                      |			ИЛИ НЕ &НуженОтборПоПользователям)
	                      |	И НЕ Организации.УчетнаяЗаписьОбмена.ПометкаУдаления
	                      |	И Организации.ВидОбменаСКонтролирующимиОрганами = ЗНАЧЕНИЕ(Перечисление.ВидыОбменаСКонтролирующимиОрганами.ОбменВУниверсальномФормате)
	                      |	И Организации.ПометкаУдаления = ЛОЖЬ
	                      |	И Организации.УчетнаяЗаписьОбмена.ДатаПодключения < &ДатаЗагрузкиБазы
	                      |	И (&ЭтоПереходВКоробку
	                      |				И Организации.УчетнаяЗаписьОбмена.ЭлектроннаяПодписьВМоделиСервиса
	                      |			ИЛИ &ЭтоПереходВОблако
	                      |				И НЕ Организации.УчетнаяЗаписьОбмена.ЭлектроннаяПодписьВМоделиСервиса)
	                      |	И НЕ Организации.УчетнаяЗаписьОбмена В
	                      |				(ВЫБРАТЬ
	                      |					УчетныеЗаписиЗаявлений.УчетнаяЗапись
	                      |				ИЗ
	                      |					УчетныеЗаписиЗаявлений КАК УчетныеЗаписиЗаявлений)
	                      |	И Организации.УчетнаяЗаписьОбмена.ЕстьОшибкиСменыМоделиХраненияКлючей = ИСТИНА");
	
	НуженОтборПоПользователям = ОбщегоНазначения.РазделениеВключено() 
		ИЛИ НЕ Пользователи.ЭтоПолноправныйПользователь();
		
	ТекущаяМодель 		= ТекущаяМодельРаботыСКлючами();
	ЭтоПереходВКоробку  = ТекущаяМодель = Перечисления.МодельРаботыСКлючами.ТолькоЛокальныеКлючи;
		
	ДатаЗагрузкиБазы = Константы.ДатаЗагрузкиБазы.Получить();
	ДвеНедели =  2 * 7 * 24 * 60 * 60;
		
	Запрос.УстановитьПараметр("Пользователь", 				Пользователи.ТекущийПользователь());
	Запрос.УстановитьПараметр("НуженОтборПоПользователям",  НуженОтборПоПользователям);
	Запрос.УстановитьПараметр("ДатаЗагрузкиБазы",  			ДатаЗагрузкиБазы);
	Запрос.УстановитьПараметр("ЭтоПереходВКоробку",  		ЭтоПереходВКоробку);
	Запрос.УстановитьПараметр("ЭтоПереходВОблако",  		НЕ ЭтоПереходВКоробку);
	Запрос.УстановитьПараметр("ДвеНеделиНазад",  			ТекущаяДатаСеанса() - ДвеНедели);
	Запрос.УстановитьПараметр("Одобрено",  					Перечисления.СтатусыЗаявленияАбонентаСпецоператораСвязи.Одобрено);
	Запрос.УстановитьПараметр("Отправлено",  				Перечисления.СтатусыЗаявленияАбонентаСпецоператораСвязи.Отправлено);
	
	ТаблицаУчетныхЗаписей = Запрос.Выполнить().Выгрузить();
	
	ОтметитьОрганизацииКоторымНужноСообщитьОПереходе(ТаблицаУчетныхЗаписей);

	ДополнительныеПараметры = Новый Структура();
	ДополнительныеПараметры.Вставить("ТребуетсяНапоминание", Истина);
	
	ТаблицаУчетныхЗаписей = ТаблицаУчетныхЗаписей.Скопировать(ДополнительныеПараметры);
	
	Возврат ТаблицаУчетныхЗаписей;
	
КонецФункции

Функция БылВыполненПререходВДругуюМодельХраненияКлючей() Экспорт

	ПредыдущаяМодель = Константы.МодельРаботыСКлючамиПередВыгрузкой.Получить();
	ТекущаяМодель    = ТекущаяМодельРаботыСКлючами();
	ПустаяМодель     = Перечисления.МодельРаботыСКлючами.ПустаяСсылка();
	
	БылВыполненПереход = ПредыдущаяМодель <> ПустаяМодель И ТекущаяМодель <> ПустаяМодель И ПредыдущаяМодель <> ТекущаяМодель;

	Возврат БылВыполненПереход;

КонецФункции

Функция УчетныеЗаписиТребующиеНапоминанияОПереходе() Экспорт
	
	ТаблицаОрганизаций = ТаблицаОрганизацийТребующихНапоминанияОПереходе();
	УчетныеЗаписи = ТаблицаОрганизаций.ВыгрузитьКолонку("УчетнаяЗапись");
	
	Возврат УчетныеЗаписи;
	
КонецФункции

Процедура ОтметитьОрганизацииКоторымНужноСообщитьОПереходе(ТаблицаУчетныхЗаписей)

	ДатыНапоминаний = ХранилищеОбщихНастроек.Загрузить("ДокументооборотСКонтролирующимиОрганами_ДатыНапоминанийОПереходах");
	
	ТекДата = ТекущаяДатаСеанса();
	
	Для Каждого СтрокаТаблицыУчетныхЗаписей Из ТаблицаУчетныхЗаписей Цикл
		
		ПоддерживаетсяВторичноеЗаявление = ПоддерживаетсяВторичноеЗаявление(СтрокаТаблицыУчетныхЗаписей.Организация);
		Если ПоддерживаетсяВторичноеЗаявление Тогда
		
			Если ДатыНапоминаний <> Неопределено Тогда 
				
				// Проверяем, не отложил ли пользователь показ предупреждения.
				СохраненнаяДатаНапоминания = ДатыНапоминаний.Получить(СтрокаТаблицыУчетныхЗаписей.УчетнаяЗапись);
				
				Если СохраненнаяДатаНапоминания = Дата(1,1,1) Тогда 
					// Больше не показывать
					СтрокаТаблицыУчетныхЗаписей.ТребуетсяНапоминание = Ложь;
				ИначеЕсли СохраненнаяДатаНапоминания <> Неопределено Тогда
					Если ТекДата < СохраненнаяДатаНапоминания Тогда
						// Еще не подошло время
						СтрокаТаблицыУчетныхЗаписей.ТребуетсяНапоминание = Ложь;
					КонецЕсли;
				КонецЕсли;
				
			КонецЕсли;
			
		Иначе
			СтрокаТаблицыУчетныхЗаписей.ТребуетсяНапоминание = Ложь;
		КонецЕсли;
	КонецЦикла;
	
КонецПроцедуры

Функция ЭтоЗаявлениеТолькоНаПереход(ДокументЗаявление) Экспорт
	
	Если ДокументЗаявление.Ссылка.ТипЗаявления <> Перечисления.ТипыЗаявленияАбонентаСпецоператораСвязи.Изменение Тогда
		Возврат Ложь;
	КонецЕсли;
	
	ИзмененныеРеквизиты = ДокументЗаявление.Ссылка.ИзменившиесяРеквизитыВторичногоЗаявления.Выгрузить().ВыгрузитьКолонку("ИзмененныйРеквизит");
	
	РеквизитыПерехода = Новый Массив;
	РеквизитыПерехода.Добавить(Перечисления.ПараметрыПодключенияК1СОтчетности.ПереходВКоробку);
	РеквизитыПерехода.Добавить(Перечисления.ПараметрыПодключенияК1СОтчетности.ПереходВОблако);
	РеквизитыПерехода.Добавить(Перечисления.ПараметрыПодключенияК1СОтчетности.ПереизданиеСертификата);
	РеквизитыПерехода.Добавить(Перечисления.ПараметрыПодключенияК1СОтчетности.ИзменениеКриптопровайдера);
	
	РеквизитыНеОтносящиесяКПереходу = ОбщегоНазначенияКлиентСервер.РазностьМассивов(ИзмененныеРеквизиты, РеквизитыПерехода);
	
	ЗаявлениеСодержитРеквизитыПерехода = 
		ИзмененныеРеквизиты.Найти(Перечисления.ПараметрыПодключенияК1СОтчетности.ПереходВОблако) <> Неопределено
		ИЛИ ИзмененныеРеквизиты.Найти(Перечисления.ПараметрыПодключенияК1СОтчетности.ПереходВКоробку) <> Неопределено
		ИЛИ ИзмененныеРеквизиты.Найти(Перечисления.ПараметрыПодключенияК1СОтчетности.ИзменениеКриптопровайдера) <> Неопределено;
	
	Возврат РеквизитыНеОтносящиесяКПереходу.Количество() = 0 И ЗаявлениеСодержитРеквизитыПерехода;
	
КонецФункции

Функция ДополнительныеРеквизитыУчетнойЗаписи(УчетнаяЗапись) Экспорт
	
	Запрос = Новый Запрос;
	Запрос.Текст = 
		"ВЫБРАТЬ РАЗРЕШЕННЫЕ
		|	*
		|ИЗ
		|	РегистрСведений.ДополнительныеРеквизитыУчетнойЗаписи КАК ДополнительныеРеквизитыУчетнойЗаписи
		|ГДЕ
		|	ДополнительныеРеквизитыУчетнойЗаписи.УчетнаяЗапись = &УчетнаяЗапись";

	Запрос.УстановитьПараметр("УчетнаяЗапись", УчетнаяЗапись);
	Результат = Запрос.Выполнить().Выгрузить();
	
	Если Результат.Количество() > 0 Тогда
		Возврат ОбщегоНазначения.СтрокаТаблицыЗначенийВСтруктуру(Результат[0]);
	Иначе
		Возврат Новый Структура();
	КонецЕсли;

КонецФункции

#КонецОбласти

Функция ПолучитьСписокСпецоператоровСвязи() Экспорт
	
	СпецоператорыСвязи = ПолучитьСпецоператоровСвязи();
	
	Массив = Новый Массив();
	
	Если СпецоператорыСвязи <> Неопределено Тогда
		
		Для Каждого СпецоператорСвязи Из СпецоператорыСвязи Цикл 
			
			СтруктураПараметра = Новый Структура("ВебСервисПризнак");
			
			ЕстьВебСервис = ЭлектронныйДокументооборотСКонтролирующимиОрганамиВызовСервера.ПолучитьПараметрСпецоператора(
				СпецоператорСвязи.Ссылка, 
				"ВебСервисПризнак");
			
			Если ЕстьВебСервис <> Неопределено И ЕстьВебСервис = "Истина" Тогда
				Массив.Добавить(Спецоператорсвязи.Ссылка);
			КонецЕсли;
			
		КонецЦикла;
		
	КонецЕсли;
	
	Возврат Массив
	
КонецФункции

Функция ПолучитьПараметрыСпецоператора(Знач Спецоператор, ПараметрыСтруктура, Макет = Неопределено) Экспорт
	
	ПараметрыСпецоператора = ПолучитьПараметрыОператораСвязи(Спецоператор);
	ЗаполнитьЗначенияСвойств(ПараметрыСтруктура, ПараметрыСпецоператора);
	Возврат ПараметрыСтруктура;
	
КонецФункции

Функция ЭПНотариусаДокСкана(ПрисоединенныйФайл) Экспорт
	
	СвойстваЭП = Новый Структура();
	СвойстваЭП.Вставить("ИмяФайла", 	"");
	СвойстваЭП.Вставить("АдресДанных", 	"");
	
	Если ЭлектроннаяПодпись.УстановленныеПодписи(ПрисоединенныйФайл).Количество() = 0 Тогда
		Возврат СвойстваЭП;
	КонецЕсли;
	
	СвойстваПодписиПрисоединенногоФайла =  ЭлектроннаяПодпись.УстановленныеПодписи(ПрисоединенныйФайл)[0];
		
	ИмяФайла	= СвойстваПодписиПрисоединенногоФайла.ИмяФайлаПодписи;
	АдресДанных = ПоместитьВоВременноеХранилище(СвойстваПодписиПрисоединенногоФайла.Подпись, Новый УникальныйИдентификатор);
	
	СвойстваЭП.ИмяФайла 	= ИмяФайла;
	СвойстваЭП.АдресДанных 	= АдресДанных;
	
	Возврат СвойстваЭП;
	
КонецФункции

#Область СкрытаяОтправкаЗаявленияНаИзменение

Функция СоздатьЗаявлениеНаИзменениеВСкрытомРежиме(Организация) Экспорт
	
	УчетнаяЗапись = УчетнаяЗаписьОрганизации(Организация);
	Если НЕ ЗначениеЗаполнено(УчетнаяЗапись) Тогда
		Возврат Неопределено;
	КонецЕсли;
	
	Если НЕ СоздатьЗаявлениеНаИзменениеВСкрытомРежиме_СпецоператорПоддерживаетВторичныеЗаявления(УчетнаяЗапись) Тогда
		Возврат Неопределено;
	КонецЕсли;
	
	ДополнительныеРеквизитыУчетнойЗаписи = СоздатьЗаявлениеНаИзменениеВСкрытомРежиме_ДополнительныеРеквизитыУчетнойЗаписи(УчетнаяЗапись);
	
	Если ДополнительныеРеквизитыУчетнойЗаписи.Количество() = 0 Тогда
		Возврат Неопределено;
	КонецЕсли;
	
	// Получаем реквизиты организации
	ДанныеЗаполнения 	= СоздатьЗаявлениеНаИзменениеВСкрытомРежиме_ДанныеЗаполнения(Организация);
	ДанныеОрганизации 	= ДанныеЗаполнения.СтруктураДанныхОрганизации;
	
	// Создаем заявление на изменение настроек подключения
	НовыйДокументЗаявление = Документы.ЗаявлениеАбонентаСпецоператораСвязи.СоздатьДокумент();
	
	СоздатьЗаявлениеНаИзменениеВСкрытомРежиме_ОпределитьСведенияОрганизации(
		НовыйДокументЗаявление,
		ДополнительныеРеквизитыУчетнойЗаписи,
		ДанныеЗаполнения,
		ДанныеОрганизации,
		Организация,
		УчетнаяЗапись);
	
	СоздатьЗаявлениеНаИзменениеВСкрытомРежиме_ОпределитьСлужебныеСведения(
		НовыйДокументЗаявление);
	
	СоздатьЗаявлениеНаИзменениеВСкрытомРежиме_ОпределитьТелефоны(
		НовыйДокументЗаявление,
		ДополнительныеРеквизитыУчетнойЗаписи);
		
	СоздатьЗаявлениеНаИзменениеВСкрытомРежиме_ОпределитьСведенияВладельцаЭП(
		НовыйДокументЗаявление,
		ДанныеОрганизации,
		ДополнительныеРеквизитыУчетнойЗаписи);
	
	СоздатьЗаявлениеНаИзменениеВСкрытомРежиме_ОпределитьНаправленияСдачиОтчетности(
		НовыйДокументЗаявление,
		ДополнительныеРеквизитыУчетнойЗаписи);
	
	Возврат НовыйДокументЗаявление;
	
КонецФункции

Функция СоздатьЗаявлениеНаИзменениеВСкрытомРежиме_ДанныеЗаполнения(Организация) Экспорт
	
	СтруктураРеквизитов = Новый Структура("Организация, ПриОткрытии", Организация, Истина);
	СтруктураРеквизитов.Вставить("АдресЮридический",);
	СтруктураРеквизитов.Вставить("АдресФактический",);
	ЗаполнитьДанныеОрганизации(СтруктураРеквизитов);
	ДанныеЗаполнения = ДополнитьДанныеОрганизацииДаннымиПоОтветственнымЛицам(СтруктураРеквизитов);
	
	Возврат ДанныеЗаполнения;
	
КонецФункции

Функция СоздатьЗаявлениеНаИзменениеВСкрытомРежиме_ДополнительныеРеквизитыУчетнойЗаписи(УчетнаяЗапись)
	
	Запрос = Новый Запрос;
	Запрос.Текст =
		"ВЫБРАТЬ РАЗРЕШЕННЫЕ
		|	ДополнительныеРеквизитыУчетнойЗаписи.НомерОсновнойПоставки1с,
		|	ДополнительныеРеквизитыУчетнойЗаписи.КраткоеНаименование,
		|	ДополнительныеРеквизитыУчетнойЗаписи.ПолноеНаименование,
		|	ДополнительныеРеквизитыУчетнойЗаписи.ИНН,
		|	ДополнительныеРеквизитыУчетнойЗаписи.КПП,
		|	ДополнительныеРеквизитыУчетнойЗаписи.ОГРН,
		|	ДополнительныеРеквизитыУчетнойЗаписи.РегНомерПФР,
		|	ДополнительныеРеквизитыУчетнойЗаписи.ЭлектроннаяПочта,
		|	ДополнительныеРеквизитыУчетнойЗаписи.ВладелецЭЦПФамилия,
		|	ДополнительныеРеквизитыУчетнойЗаписи.ВладелецЭЦПИмя,
		|	ДополнительныеРеквизитыУчетнойЗаписи.ВладелецЭЦПОтчество,
		|	ДополнительныеРеквизитыУчетнойЗаписи.ВладелецЭЦППодразделение,
		|	ДополнительныеРеквизитыУчетнойЗаписи.ВладелецЭЦПСНИЛС,
		|	ДополнительныеРеквизитыУчетнойЗаписи.ВладелецЭЦПДолжность,
		|	ДополнительныеРеквизитыУчетнойЗаписи.ТелефонМобильный,
		|	ДополнительныеРеквизитыУчетнойЗаписи.ТелефонОсновной,
		|	ДополнительныеРеквизитыУчетнойЗаписи.ТелефонДополнительный,
		|	ДополнительныеРеквизитыУчетнойЗаписи.КодРегионаФСРАР
		|ИЗ
		|	РегистрСведений.ДополнительныеРеквизитыУчетнойЗаписи КАК ДополнительныеРеквизитыУчетнойЗаписи
		|ГДЕ
		|	ДополнительныеРеквизитыУчетнойЗаписи.УчетнаяЗапись = &УчетнаяЗапись";
	
	Запрос.УстановитьПараметр("УчетнаяЗапись", УчетнаяЗапись);
	
	Возврат Запрос.Выполнить().Выгрузить();
	
КонецФункции

Функция СоздатьЗаявлениеНаИзменениеВСкрытомРежиме_СпецоператорПоддерживаетВторичныеЗаявления(УчетнаяЗапись)
	
	Спецоператор = УчетнаяЗапись.СпецоператорСвязи;
	
	ПараметрСпецоператора = ЭлектронныйДокументооборотСКонтролирующимиОрганамиВызовСервера.ПолучитьПараметрСпецоператора(
		Спецоператор, 
		"ПоддерживаетсяОтправкаВторичногоЗаявления");
	
	Возврат ПараметрСпецоператора = "Истина";
	
КонецФункции

Процедура СоздатьЗаявлениеНаИзменениеВСкрытомРежиме_ОпределитьСведенияОрганизации(
		НовыйДокументЗаявление,
		ДополнительныеРеквизитыУчетнойЗаписи,
		ДанныеЗаполнения,
		ДанныеОрганизации,
		Организация,
		УчетнаяЗапись)
		
	НомерОсновнойПоставки1с 	= ДополнительныеРеквизитыУчетнойЗаписи[0].НомерОсновнойПоставки1с;
	КраткоеНаименование 		= ДополнительныеРеквизитыУчетнойЗаписи[0].КраткоеНаименование;
	ПолноеНаименование 			= ДополнительныеРеквизитыУчетнойЗаписи[0].ПолноеНаименование;
	ИНН 						= ДополнительныеРеквизитыУчетнойЗаписи[0].ИНН;
	КПП 						= ДополнительныеРеквизитыУчетнойЗаписи[0].КПП;
	ОГРН 						= ДополнительныеРеквизитыУчетнойЗаписи[0].ОГРН;
	РегНомерПФР 				= ДополнительныеРеквизитыУчетнойЗаписи[0].РегНомерПФР;
	ЭлектроннаяПочта 			= ДополнительныеРеквизитыУчетнойЗаписи[0].ЭлектроннаяПочта;
	
	ЭтоЮридическоеЛицо  = ДанныеОрганизации.ТипОрганизации;
	Если ЭтоЮридическоеЛицо Тогда
		ПризнакОбособленногоПодразделения = ДанныеОрганизации.ПризнакОбособленногоПодразделения;
	КонецЕсли;
	АдрЮР = ДанныеЗаполнения.АдресЮридический;
	
	НовыйДокументЗаявление.Организация							= Организация;
	НовыйДокументЗаявление.УчетнаяЗапись 						= УчетнаяЗапись;
	НовыйДокументЗаявление.ТипОрганизации						= ЭтоЮридическоеЛицо;
	НовыйДокументЗаявление.ИНН									= ИНН;
	НовыйДокументЗаявление.КПП									= КПП;
	НовыйДокументЗаявление.ПолноеНаименование					= ПолноеНаименование;
	НовыйДокументЗаявление.КраткоеНаименование					= КраткоеНаименование;
	НовыйДокументЗаявление.ПризнакОбособленногоПодразделения	= ПризнакОбособленногоПодразделения;
	НовыйДокументЗаявление.НомерОсновнойПоставки1с				= НомерОсновнойПоставки1с;
	НовыйДокументЗаявление.ОГРН									= ОГРН;
	НовыйДокументЗаявление.АдресЮридический 					= РегламентированнаяОтчетностьВызовСервера.ЗаменитьТекстРегионаНаКодРегиона(АдрЮР);
	НовыйДокументЗаявление.РегНомерПФР							= РегНомерПФР;
	НовыйДокументЗаявление.ЭлектроннаяПочта						= ЭлектроннаяПочта;
	НовыйДокументЗаявление.ЭлектроннаяПодписьВМоделиСервиса		= ЭлектроннаяПодписьВМоделиСервисаБРОВызовСервера.ЭтоЭлектроннаяПодписьВМоделиСервиса(УчетнаяЗапись);
	НовыйДокументЗаявление.СпецоператорСвязи 					= УчетнаяЗапись.СпецоператорСвязи;
		
КонецПроцедуры

Процедура СоздатьЗаявлениеНаИзменениеВСкрытомРежиме_ОпределитьСлужебныеСведения(
		НовыйДокументЗаявление)
		
	НовыйДокументЗаявление.УстановитьНовыйНомер();
	
	НовыйДокументЗаявление.Дата								= ТекущаяДатаСеанса();
	НовыйДокументЗаявление.Ответственный					= Пользователи.ТекущийПользователь();
	НовыйДокументЗаявление.ДатаСозданияУчетнойЗаписи		= '00010101';
	НовыйДокументЗаявление.ТипКриптопровайдера				= Перечисления.ТипыКриптоПровайдеров.ПустаяСсылка();
	НовыйДокументЗаявление.ТипЗаявления						= Перечисления.ТипыЗаявленияАбонентаСпецоператораСвязи.Изменение;
	НовыйДокументЗаявление.ИдентификаторДокументооборота	= нрег(СтрЗаменить(Строка(Новый("УникальныйИдентификатор")), "-", ""));
	НовыйДокументЗаявление.Статус							= Перечисления.СтатусыЗаявленияАбонентаСпецоператораСвязи.Подготовлено;

КонецПроцедуры
	
Процедура СоздатьЗаявлениеНаИзменениеВСкрытомРежиме_ОпределитьТелефоны(
		НовыйДокументЗаявление,
		ДополнительныеРеквизитыУчетнойЗаписи)
		
	ТелефонМобильный		= ДополнительныеРеквизитыУчетнойЗаписи[0].ТелефонМобильный;
	ТелефонОсновной 		= ДополнительныеРеквизитыУчетнойЗаписи[0].ТелефонОсновной;
	ТелефонДополнительный 	= ДополнительныеРеквизитыУчетнойЗаписи[0].ТелефонДополнительный;
	
	НовыйДокументЗаявление.ТелефонОсновной			= ТелефонОсновной;
	НовыйДокументЗаявление.ТелефонДополнительный	= ТелефонДополнительный;
	
	Если ЭлектронныйДокументооборотСКонтролирующимиОрганамиКлиентСервер.ТелефонМобильныйБезРазделителей(ТелефонМобильный) = "" Тогда
		НовыйДокументЗаявление.ТелефонМобильный = "";
	Иначе
		// Приведение номера телефона 89851234567 к маске +7 (999) 999-99-99
		КодВСкобках =  Сред(ТелефонМобильный, 2, 3);
		ПервыйБлокЧисел = Сред(ТелефонМобильный, 5, 3);
		ВторойБлокЧисел = Сред(ТелефонМобильный, 8, 2);
		ТретийБлокЧисел = Сред(ТелефонМобильный, 10, 2);
		НовыйДокументЗаявление.ТелефонМобильный = СтроковыеФункцииКлиентСервер.ПодставитьПараметрыВСтроку(
			"+7 (%1) %2-%3-%4", КодВСкобках, ПервыйБлокЧисел, ВторойБлокЧисел, ТретийБлокЧисел);
	КонецЕсли;
		
КонецПроцедуры
		
Процедура СоздатьЗаявлениеНаИзменениеВСкрытомРежиме_ОпределитьНаправленияСдачиОтчетности(
		НовыйДокументЗаявление,
		ДополнительныеРеквизитыУчетнойЗаписи)
		
	УчетнаяЗапись 		= НовыйДокументЗаявление.УчетнаяЗапись;
	КодРегионаФСРАР 	= ДополнительныеРеквизитыУчетнойЗаписи[0].КодРегионаФСРАР;
	
	Запрос = Новый Запрос;
	Запрос.Текст =
		"ВЫБРАТЬ РАЗРЕШЕННЫЕ
		|	ДополнительныеРеквизитыУчетнойЗаписиПолучатели.ТипПолучателя,
		|	ДополнительныеРеквизитыУчетнойЗаписиПолучатели.КодПолучателя,
		|	ДополнительныеРеквизитыУчетнойЗаписиПолучатели.КПП
		|ИЗ
		|	РегистрСведений.ДополнительныеРеквизитыУчетнойЗаписиПолучатели КАК ДополнительныеРеквизитыУчетнойЗаписиПолучатели
		|ГДЕ
		|	ДополнительныеРеквизитыУчетнойЗаписиПолучатели.УчетнаяЗапись = &УчетнаяЗапись";
	
	Запрос.УстановитьПараметр("УчетнаяЗапись", УчетнаяЗапись);
	Получатели = Запрос.Выполнить().Выгрузить();
	
	// Копируем структуру таблицы
	ТаблицаКодовФНС = Получатели.Скопировать();
	ТаблицаКодовФНС.Очистить();
	
	СдаватьВФНС 	= Ложь;
	СдаватьВПФР 	= Ложь;
	КодПФР 			= "";
	СдаватьВРосстат = Ложь;
	КодРосстата 	= "";
	СдаватьВФСС 	= Ложь;
	СдаватьВФСРАР 	= Ложь;
	СдаватьВРПН 	= Ложь;
	СдаватьВФТС 	= Ложь;
	
	Для каждого СтрокаНаправлений Из Получатели Цикл
		Если СтрокаНаправлений.ТипПолучателя = Перечисления.ТипыКонтролирующихОрганов.ПФР Тогда
			СдаватьВПФР = Истина;
			КодПФР 		= ЭлектронныйДокументооборотСКонтролирующимиОрганамиКлиентСервер.СтрокаВФорматеДляСравнения(СтрокаНаправлений.КодПолучателя);
		ИначеЕсли СтрокаНаправлений.ТипПолучателя = Перечисления.ТипыКонтролирующихОрганов.ФСС Тогда
			СдаватьВФСС = Истина;
		ИначеЕсли СтрокаНаправлений.ТипПолучателя = Перечисления.ТипыКонтролирующихОрганов.ФНС Тогда
			СдаватьВФНС = Истина;
			НоваяСтрока = ТаблицаКодовФНС.Добавить();
			ЗаполнитьЗначенияСвойств(НоваяСтрока, СтрокаНаправлений);
		ИначеЕсли СтрокаНаправлений.ТипПолучателя = Перечисления.ТипыКонтролирующихОрганов.ФСГС Тогда
			СдаватьВРосстат = Истина;
			КодРосстата = ЭлектронныйДокументооборотСКонтролирующимиОрганамиКлиентСервер.СтрокаВФорматеДляСравнения(СтрокаНаправлений.КодПолучателя);
		ИначеЕсли СтрокаНаправлений.ТипПолучателя = Перечисления.ТипыКонтролирующихОрганов.ФСРАР Тогда
			СдаватьВФСРАР = Истина;
		ИначеЕсли СтрокаНаправлений.ТипПолучателя = Перечисления.ТипыКонтролирующихОрганов.РПН Тогда
			СдаватьВРПН = Истина;
		ИначеЕсли СтрокаНаправлений.ТипПолучателя = Перечисления.ТипыКонтролирующихОрганов.ФТС Тогда
			СдаватьВФТС = Истина;
		КонецЕсли;
	КонецЦикла;
	
	Получатели = ТаблицаКодовФНС;
		
	НовыйДокументЗаявление.Получатели.Очистить();
	
	// ФНС
	Если СдаватьВФНС Тогда
		Для Каждого СтрокаНаправлений Из Получатели Цикл
			НоваяСтрокаНаправления = НовыйДокументЗаявление.Получатели.Добавить();
			НоваяСтрокаНаправления.ТипПолучателя = СтрокаНаправлений.ТипПолучателя;
			НоваяСтрокаНаправления.КодПолучателя = СтрокаНаправлений.КодПолучателя;
			НоваяСтрокаНаправления.КПП = СтрокаНаправлений.КПП;
		КонецЦикла;
	КонецЕсли;
	
	// ПФР
	Если СдаватьВПФР Тогда
		НоваяСтрокаНаправления = НовыйДокументЗаявление.Получатели.Добавить();
		НоваяСтрокаНаправления.ТипПолучателя = Перечисления.ТипыКонтролирующихОрганов.ПФР;
		НоваяСтрокаНаправления.КодПолучателя = КодПФР;
		НоваяСтрокаНаправления.КПП = "";
	КонецЕсли;
	
	// ФСС
	Если СдаватьВФСС Тогда
		НоваяСтрокаНаправления = НовыйДокументЗаявление.Получатели.Добавить();
		НоваяСтрокаНаправления.ТипПолучателя = Перечисления.ТипыКонтролирующихОрганов.ФСС;
		НоваяСтрокаНаправления.КПП = "";
	КонецЕсли;
	
	// Росстат
	Если СдаватьВРосстат Тогда
		НоваяСтрокаНаправления = НовыйДокументЗаявление.Получатели.Добавить();
		НоваяСтрокаНаправления.ТипПолучателя = Перечисления.ТипыКонтролирующихОрганов.ФСГС;
		НоваяСтрокаНаправления.КодПолучателя = КодРосстата;
		НоваяСтрокаНаправления.КПП = "";
	КонецЕсли;
	
	// ФСРАР
	НовыйДокументЗаявление.ПодатьЗаявкуНаСертификатДляФСРАР 	= СдаватьВФСРАР;
	НовыйДокументЗаявление.КодРегионаФСРАР 						= КодРегионаФСРАР;
	
	// РПН
	НовыйДокументЗаявление.ПодатьЗаявкуНаПодключениеРПН = СдаватьВРПН;
	
	// ФТС
	НовыйДокументЗаявление.ПодатьЗаявкуНаПодключениеФТС = СдаватьВФТС;
	
КонецПроцедуры
		
Процедура СоздатьЗаявлениеНаИзменениеВСкрытомРежиме_ОпределитьСведенияВладельцаЭП(
		НовыйДокументЗаявление, 
		ДанныеОрганизации,
		ДополнительныеРеквизитыУчетнойЗаписи)
	
	Организация = НовыйДокументЗаявление.Организация;
	
	ВладелецЭЦПФамилия 			= ДополнительныеРеквизитыУчетнойЗаписи[0].ВладелецЭЦПФамилия;
	ВладелецЭЦПИмя 				= ДополнительныеРеквизитыУчетнойЗаписи[0].ВладелецЭЦПИмя;
	ВладелецЭЦПОтчество			= ДополнительныеРеквизитыУчетнойЗаписи[0].ВладелецЭЦПОтчество;
	ВладелецЭЦППодразделение 	= ДополнительныеРеквизитыУчетнойЗаписи[0].ВладелецЭЦППодразделение;
	ВладелецЭЦПСНИЛС 			= ДополнительныеРеквизитыУчетнойЗаписи[0].ВладелецЭЦПСНИЛС;
	ВладелецЭЦПДолжность 		= ДополнительныеРеквизитыУчетнойЗаписи[0].ВладелецЭЦПДолжность;
	
	
	Руководитель = ЭлектронныйДокументооборотСКонтролирующимиОрганамиВызовСервераПереопределяемый.Руководитель(Организация);
	ГлБухгалтер  = ЭлектронныйДокументооборотСКонтролирующимиОрганамиВызовСервераПереопределяемый.ГлБухгалтер(Организация);
	
	СтруктураФИОРуководителя = ЭлектронныйДокументооборотСКонтролирующимиОрганамиВызовСервера.ФИОФизЛица(Руководитель);
	СтруктураФИОГлБухгалтер = ЭлектронныйДокументооборотСКонтролирующимиОрганамиВызовСервера.ФИОФизЛица(ГлБухгалтер);
	
	// Пытаемся определить тип владельца ЭП исходя из ФИО
	Если СтруктураФИОРуководителя.Имя = ВладелецЭЦПИмя 
		И СтруктураФИОРуководителя.Отчество = ВладелецЭЦПОтчество
		И СтруктураФИОРуководителя.Фамилия = ВладелецЭЦПФамилия Тогда
		
		ВидВладельцаЭЦП = Перечисления.ТипыВладельцевЭЦП.Руководитель;
		ВладелецЭЦП     = Руководитель;
		
	ИначеЕсли СтруктураФИОГлБухгалтер.Имя = ВладелецЭЦПИмя 
		И СтруктураФИОГлБухгалтер.Отчество = ВладелецЭЦПОтчество
		И СтруктураФИОГлБухгалтер.Фамилия = ВладелецЭЦПФамилия Тогда
		
		ВидВладельцаЭЦП = Перечисления.ТипыВладельцевЭЦП.ГлавныйБухгалтер;
		ВладелецЭЦП     = ГлБухгалтер;
		
	Иначе
		
		ВидВладельцаЭЦП = Перечисления.ТипыВладельцевЭЦП.ДругойСотрудник;
		ВладелецЭЦП = ЭлектронныйДокументооборотСКонтролирующимиОрганамиВызовСервераПереопределяемый.ФизЛицоПоФИО(
			ВладелецЭЦПФамилия, 
			ВладелецЭЦПИмя, 
			ВладелецЭЦПОтчество, 
			ВладелецЭЦПСНИЛС, 
			Организация);
			
	КонецЕсли;
	
	ДанныеОрганизации.Вставить("Организация", Организация);
	
	ВладелецЭЦПВидДокумента        	= Справочники.ВидыДокументовФизическихЛиц.ПустаяСсылка();
	ВладелецЭЦПСерияДокумента      	= "";
	ВладелецЭЦПНомерДокумента      	= "";
	ВладелецЭЦПДатаВыдачиДокумента 	= "";
	ВладелецЭЦПКемВыданДокумент    	= "";
	ВладелецЭЦПДатаРождения			= Неопределено;
	ВладелецЭЦПМестоРождения		= Неопределено;
	ВладелецЭЦППол					= Неопределено;
	ВладелецЭЦПГражданство			= Неопределено;
	ВладелецЭЦПКодПодразделения		= Неопределено;
	
	Если ЗначениеЗаполнено(ВладелецЭЦП) Тогда

		ДанныеСотрудника = ЭлектронныйДокументооборотСКонтролирующимиОрганамиВызовСервера.ПолучитьДанныеСотрудника(
			ВидВладельцаЭЦП, 
			ДанныеОрганизации, 
			ВладелецЭЦП);
		
		ВладелецЭЦПВидДокумента			= ДанныеСотрудника.ВидДокумента;
		ВладелецЭЦПСерияДокумента		= ДанныеСотрудника.Серия;
		ВладелецЭЦПНомерДокумента		= ДанныеСотрудника.Номер;
		ВладелецЭЦПДатаВыдачиДокумента	= ДанныеСотрудника.ДатаВыдачи;
		ВладелецЭЦПКемВыданДокумент		= ДанныеСотрудника.КемВыдан;
		ВладелецЭЦПДатаРождения			= ДанныеСотрудника.ДатаРождения;
		ВладелецЭЦПМестоРождения		= ДанныеСотрудника.МестоРождения;
		ВладелецЭЦППол					= ДанныеСотрудника.Пол;
		ВладелецЭЦПГражданство			= ДанныеСотрудника.Гражданство;
		ВладелецЭЦПКодПодразделения		= ДанныеСотрудника.КодПодразделения;
		
	КонецЕсли;
	
	НовыйДокументЗаявление.ВладелецЭЦПТип					= ВидВладельцаЭЦП;
	НовыйДокументЗаявление.ВладелецЭЦП						= ВладелецЭЦП;
	
	НовыйДокументЗаявление.ВладелецЭЦПФамилия				= СокрЛП(ВладелецЭЦПФамилия);
	НовыйДокументЗаявление.ВладелецЭЦПИмя					= СокрЛП(ВладелецЭЦПИмя);
	НовыйДокументЗаявление.ВладелецЭЦПОтчество				= СокрЛП(ВладелецЭЦПОтчество);
	НовыйДокументЗаявление.ВладелецЭЦПДолжность				= ВладелецЭЦПДолжность;
	НовыйДокументЗаявление.ВладелецЭЦППодразделение			= ВладелецЭЦППодразделение;
	
	НовыйДокументЗаявление.ВладелецЭЦПВидДокумента			= ВладелецЭЦПВидДокумента;
	НовыйДокументЗаявление.ВладелецЭЦПНомерДокумента		= ВладелецЭЦПНомерДокумента;
	НовыйДокументЗаявление.ВладелецЭЦПСерияДокумента		= ВладелецЭЦПСерияДокумента;
	НовыйДокументЗаявление.ВладелецЭЦПДатаВыдачиДокумента	= ВладелецЭЦПДатаВыдачиДокумента;
	НовыйДокументЗаявление.ВладелецЭЦПКемВыданДокумент		= ВладелецЭЦПКемВыданДокумент;
	НовыйДокументЗаявление.ВладелецЭЦПДатаРождения        	= ВладелецЭЦПДатаРождения;
	НовыйДокументЗаявление.ВладелецЭЦПМестоРождения       	= ВладелецЭЦПМестоРождения;
	НовыйДокументЗаявление.ВладелецЭЦППол                 	= ВладелецЭЦППол;
	НовыйДокументЗаявление.ВладелецЭЦПГражданство         	= ВладелецЭЦПГражданство;
	НовыйДокументЗаявление.ВладелецЭЦПКодПодразделения    	= ВладелецЭЦПКодПодразделения;
	
КонецПроцедуры

Функция СообщениеОНеподключенномНаправлении_ЗаголовокФормы(СсылкаНаОбъект, Знач ОрганСтрокой) Экспорт
	
	Представление = ДлительнаяОтправкаКлиентСервер.НазваниеОбъектаВИменительномПадеже(СсылкаНаОбъект, Истина);
	ВидОбъекта    = ДлительнаяОтправкаКлиентСервер.ВидОбъекта(СсылкаНаОбъект);
	
	Если ВидОбъекта.ЭтоПисьмо 
		ИЛИ ВидОбъекта.ЭтоЗаявлениеОПенсии
		ИЛИ ВидОбъекта.ЭтоЗаявлениеВПФР Тогда
		Действие = НСтр("ru = 'отправлено'");
	Иначе
		Действие = НСтр("ru = 'отправлен'");
	КонецЕсли;
	
	Если ВидОбъекта.ЭтоЕГРЮЛ Тогда
		ОрганСтрокой = НСтр("ru = ' оператору связи'");
	ИначеЕсли ОрганСтрокой = НСтр("ru = 'ФНС'") Тогда
		ОрганСтрокой = НСтр("ru = ' в выбранную инспекцию ФНС'");
	Иначе
		ОрганСтрокой = НСтр("ru = ' в '") + ОрганСтрокой;
	КонецЕсли;
	
	Заголовок = Представление + НСтр("ru = ' не может быть '") + Действие + ОрганСтрокой + НСтр("ru = ' сейчас'");
	
	Возврат Заголовок;
	
КонецФункции

Функция СообщениеОНеподключенномНаправлении_ЧтоДелать(СсылкаНаОбъект, Знач ТекстНадписи, ОрганСтрокой) Экспорт
	
	НазваниеВРодительномПадеже = ДлительнаяОтправкаКлиентСервер.НазваниеОбъектаВРодительномПадеже(СсылкаНаОбъект);
	
	ТекстНадписи = СтрЗаменить(ТекстНадписи, "<орган>",  ОрганСтрокой);
	ТекстНадписи = СтрЗаменить(ТекстНадписи, "<отчета>", НазваниеВРодительномПадеже);
	
	Возврат ТекстНадписи;
	
КонецФункции

Процедура СообщениеОНеподключенномНаправлении_ОпределитьСведенияПоОбъекту(Форма) Экспорт
	
	Форма.СсылкаНаОбъект = Форма.Параметры.СсылкаНаОбъект;
	
	СведенияПоОбъекту 	 = СведенияПоОтправляемымОбъектам(Форма.СсылкаНаОбъект);
	
	Форма.Организация 			 	= СведенияПоОбъекту.Организация;
	Форма.ВидКонтролирующегоОргана 	= СведенияПоОбъекту.ВидКонтролирующегоОргана;
	Форма.КодКонтролирующегоОргана 	= СведенияПоОбъекту.КодКонтролирующегоОргана;
	Форма.ОрганСтрокой 			 	= Строка(Форма.ВидКонтролирующегоОргана);
	
КонецПроцедуры

Процедура СообщениеОНеподключенномНаправлении_УправлениеФормой(Форма) Экспорт
	
	Элементы 		= Форма.Элементы;
	СсылкаНаОбъект 	= Форма.СсылкаНаОбъект;
	ОрганСтрокой 	= Форма.ОрганСтрокой;
	
	Форма.Заголовок = СообщениеОНеподключенномНаправлении_ЗаголовокФормы(
		СсылкаНаОбъект, 
		ОрганСтрокой);
	
	НазваниеВРодительномПадеже = ДлительнаяОтправкаКлиентСервер.НазваниеОбъектаВРодительномПадеже(СсылкаНаОбъект);
	
	Элементы.ЧтоДелать.Заголовок = СообщениеОНеподключенномНаправлении_ЧтоДелать(
		СсылкаНаОбъект, 
		Элементы.ЧтоДелать.Заголовок, 
		ОрганСтрокой);
	
КонецПроцедуры
	
Процедура СообщениеОНеподключенномНаправлении_ПриСозданииНаСервере(Форма) Экспорт
	
	СообщениеОНеподключенномНаправлении_ОпределитьСведенияПоОбъекту(Форма);
	СообщениеОНеподключенномНаправлении_УправлениеФормой(Форма);
	
КонецПроцедуры

Функция ПредставлениеОрганизацииБезПрефиксов(Знач ПредставлениеОрганизации)

	ВидыОрганизации = Новый Массив;
	ВидыОрганизации.Добавить(НСтр("ru = 'ИП'"));
	ВидыОрганизации.Добавить(НСтр("ru = 'ООО'"));
	ВидыОрганизации.Добавить(НСтр("ru = 'АО'"));
	ВидыОрганизации.Добавить(НСтр("ru = 'ПАО'"));
	ВидыОрганизации.Добавить(НСтр("ru = 'ЗАО'"));
	
	Для каждого ВидОрганизации Из ВидыОрганизации Цикл
		
		Длина = СтрДлина(ВидОрганизации) + 1; // с пробелом
		
		// Удаляем справа
		Если Прав(ПредставлениеОрганизации, Длина) = НСтр("ru = ' '") + ВидОрганизации Тогда
			ПредставлениеОрганизации = Лев(ПредставлениеОрганизации, СтрДлина(ПредставлениеОрганизации) - Длина);
		КонецЕсли;
		
		// Удаляем слева
		Если Лев(ПредставлениеОрганизации, Длина) = ВидОрганизации + НСтр("ru = ' '") Тогда
			ПредставлениеОрганизации = Прав(ПредставлениеОрганизации, СтрДлина(ПредставлениеОрганизации) - Длина);
		КонецЕсли;
	
	КонецЦикла;
	
	Возврат ПредставлениеОрганизации;
	
КонецФункции

Процедура УстановитьПредставлениеЗаявленияВВидеФорматированнойСтроки(ЗаявлениеАбонента, Элемент, ВыводитьОрганизацию = Ложь) Экспорт
	
	СсылкаНаЗаявление = ПолучитьНавигационнуюСсылку(ЗаявлениеАбонента);
	СсылкаНаЗаявление = Новый ФорматированнаяСтрока(НСтр("ru = 'заявление'"),,,,СсылкаНаЗаявление);
	
	ДатаОтправкиЗаявления = НСтр("ru = ' от '") + Формат(ЗаявлениеАбонента.ДатаОтправкиЗаявления, "ДЛФ=D");
		
	ТипЗаявления = ЗаявлениеАбонента.ТипЗаявления;
	Если ТипЗаявления = Перечисления.ТипыЗаявленияАбонентаСпецоператораСвязи.Изменение Тогда
		ТипЗаявленияСтрокой = НСтр("ru = ' на изменение настроек 1С-Отчетности'");
	Иначе
		ТипЗаявленияСтрокой = НСтр("ru = ' на подключение к 1С-Отчетности'");
	КонецЕсли;
	
	Если ВыводитьОрганизацию Тогда
		
		ПредставлениеОрганизации = ПредставлениеОрганизацииБезПрефиксов(Строка(ЗаявлениеАбонента.Организация));
		ТипЗаявленияСтрокой      = ТипЗаявленияСтрокой + " по организации " + ПредставлениеОрганизации;
		
	КонецЕсли;
	
	ПредставлениеЗаявления = Новый ФорматированнаяСтрока(
		СсылкаНаЗаявление,
		ТипЗаявленияСтрокой,
		ДатаОтправкиЗаявления);
		
	Если ЗаявлениеАбонента.Статус = Перечисления.СтатусыЗаявленияАбонентаСпецоператораСвязи.Отправлено Тогда
		
		Элемент.Заголовок = Новый ФорматированнаяСтрока(
			НСтр("ru = 'Ваше '"),
			ПредставлениеЗаявления,
			НСтр("ru = ' еще не обработано.'"));
		
	ИначеЕсли ЗаявлениеАбонента.Статус = Перечисления.СтатусыЗаявленияАбонентаСпецоператораСвязи.Одобрено Тогда
		
		РезультатОбработкиЗаявления = Новый ФорматированнаяСтрока(НСтр("ru = ' одобрено'"),, Новый Цвет(1,128,0));
	
		Элемент.Заголовок = Новый ФорматированнаяСтрока(
			НСтр("ru = 'Ваше '"),
			ПредставлениеЗаявления,
			РезультатОбработкиЗаявления);
			
	ИначеЕсли ЗаявлениеАбонента.Статус = Перечисления.СтатусыЗаявленияАбонентаСпецоператораСвязи.Отклонено Тогда
		
		РезультатОбработкиЗаявления = Новый ФорматированнаяСтрока(НСтр("ru = ' отклонено'"),, Новый Цвет(226, 40, 40));
		
		Элемент.Заголовок = Новый ФорматированнаяСтрока(
			НСтр("ru = 'Ваше '"),
			ПредставлениеЗаявления,
			РезультатОбработкиЗаявления,
			НСтр("ru = ' по причине:'"));
			
	КонецЕсли;
	
КонецПроцедуры

#КонецОбласти

#Область РаспознаваниеРассылокПФРВЦиклахОбмена

Процедура РаспознатьРассылкиПФРВЦиклахОбмена() Экспорт
		
	Запрос = Новый Запрос;
	Запрос.Текст = "ВЫБРАТЬ
	               |	ТранспортноеСообщение.Ссылка КАК Ссылка
	               |ИЗ
	               |	Документ.ТранспортноеСообщение КАК ТранспортноеСообщение
	               |		ЛЕВОЕ СОЕДИНЕНИЕ Справочник.ЦиклыОбмена КАК ЦиклыОбмена
	               |		ПО ТранспортноеСообщение.ЦиклОбмена = ЦиклыОбмена.Ссылка
	               |ГДЕ
	               |	ТранспортноеСообщение.Тип = ЗНАЧЕНИЕ(Перечисление.ТипыТранспортныхСообщений.ПисьмоВходящееПФР)
	               |	И ТИПЗНАЧЕНИЯ(ТранспортноеСообщение.Отправитель) = ТИП(Справочник.ОрганыПФР)
	               |	И НЕ ЦиклыОбмена.Ссылка ЕСТЬ NULL
	               |	И ЦиклыОбмена.Тип <> ЗНАЧЕНИЕ(Перечисление.ТипыЦикловОбмена.РассылкаПФР)";
	
	Результат = Запрос.Выполнить();
	Если Результат.Пустой() Тогда 
		Возврат;
	КонецЕсли;
	
	ВходящиеПисьмаПФР = Результат.Выгрузить();
	Для Каждого ПисьмоПФР Из ВходящиеПисьмаПФР Цикл 
		
		Объект = ПисьмоПФР.Ссылка;
		
		Вложения = ПолучитьВложенияТранспортногоСообщения(Объект, Истина);
		
		ВложенияПисьма = Вложения.НайтиСтроки(Новый Структура("Тип", Перечисления.ТипыСодержимогоТранспортногоКонтейнера.Письмо));
		ВложенияПриложенияКПисьму = Вложения.НайтиСтроки(Новый Структура("Тип", Перечисления.ТипыСодержимогоТранспортногоКонтейнера.ПисьмоПриложение));
		ВложенияОписанияКПисьму = Вложения.НайтиСтроки(Новый Структура("Тип", Перечисления.ТипыСодержимогоТранспортногоКонтейнера.ОписаниеПисьма));
		
		Если ВложенияПисьма.Количество() = 0 Тогда
			Продолжить;
		КонецЕсли;
		
		Если ВложенияОписанияКПисьму.Количество() = 0 Тогда
			Продолжить;
		КонецЕсли;
		
		ВложениеПисьмо = ВложенияПисьма[0];
		ВложенияОписаниеКПисьму = ВложенияОписанияКПисьму[0];
		
		// загружаем описание к письму
		ВремФайлОписаниеКПисьму = ПолучитьИмяВременногоФайла();
		ВложенияОписаниеКПисьму.Данные.Получить().Записать(ВремФайлОписаниеКПисьму);
		ДеревоОписанияКПисьму = ЗагрузитьXMLВДеревоЗначений(ВремФайлОписаниеКПисьму);
		ОперацииСФайламиЭДКО.УдалитьВременныйФайл(ВремФайлОписаниеКПисьму);
		Если ДеревоОписанияКПисьму = Неопределено Тогда
			Продолжить;
		КонецЕсли;
		
		// извлекаем тему, идентификатор и дату-время отправки из описания письма
		УзелОписаниеПисьма = ДеревоОписанияКПисьму.Строки.Найти("описаниеПисьма", "Имя");
		Если УзелОписаниеПисьма = Неопределено Тогда
			Продолжить;
		КонецЕсли;
		
		УзелТема = УзелОписаниеПисьма.Строки.Найти("тема", "Имя");
		УзелДатаВремяОтправки = УзелОписаниеПисьма.Строки.Найти("датаВремяОтправки", "Имя");
		УзелИдентификаторПисьма = УзелОписаниеПисьма.Строки.Найти("идентификаторПисьма", "Имя");
		Если УзелТема = Неопределено ИЛИ УзелДатаВремяОтправки = Неопределено ИЛИ УзелИдентификаторПисьма = Неопределено Тогда
			Продолжить;
		КонецЕсли;
		Тема = XMLЗначениеВнутр(Тип("Строка"), УзелТема.Значение);
		ДатаВремяОтправки = XMLЗначениеВнутр(Тип("Дата"), УзелДатаВремяОтправки.Значение);
		ИдентификаторПисьма = XMLЗначениеВнутр(Тип("Строка"), УзелИдентификаторПисьма.Значение);
		
		УзелОтветНаПисьмо = УзелОписаниеПисьма.Строки.Найти("ответНаПисьмо", "Имя");
		Если УзелОтветНаПисьмо <> Неопределено Тогда
			ИдентификаторОснования = XMLЗначениеВнутр(Тип("Строка"), УзелОтветНаПисьмо.Значение);
		Иначе
			ИдентификаторОснования = "";
		КонецЕсли;
		
		УзелКатегорияПисьма = УзелОписаниеПисьма.Строки.Найти("категорияПисьма", "Имя");
		Если ЗначениеЗаполнено(УзелКатегорияПисьма) Тогда
			СтрКатегорияПисьма = XMLЗначениеВнутр(Тип("Строка"), УзелКатегорияПисьма.Значение);
			ПризнакРассылки = (нрег(СтрКатегорияПисьма) = "рассылка");
		Иначе
			ПризнакРассылки = Ложь;
		КонецЕсли;
		
		Если (ПризнакРассылки) И ЗначениеЗаполнено(Объект.ЦиклОбмена) Тогда
			ЦиклыОбмена = Объект.ЦиклОбмена.ПолучитьОбъект();			
			
			НачатьТранзакцию();			
			
			ЦиклыОбмена.Тип = Перечисления.ТипыЦикловОбмена.РассылкаПФР;
									
			Попытка
				ЦиклыОбмена.Записать();
			Исключение
				ТекстСообщения = СтроковыеФункцииКлиентСервер.ПодставитьПараметрыВСтроку(
					НСтр("ru = 'Ошибка записи ЦО, связанного с входящим письмом от ПФР:
					|%1'"), ИнформацияОбОшибке().Описание);					
				ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
				ОтменитьТранзакцию();
				Продолжить;
			КонецПопытки;		
			
			ЗафиксироватьТранзакцию();
			
		КонецЕсли;
		
	КонецЦикла;
	
	
КонецПроцедуры

#КонецОбласти

#Область ПереносТранспортныхСообщенийВПрисоединенныеФайлы

Функция ПеренестиТранспортныеСообщенияВПрисоединенныеФайлы(МаксимальноеКоличество = 10) Экспорт
	
	УстановитьПривилегированныйРежим(Истина);
	
	Обработано = 0;
	
	// перенос содержимого транспортных контейнеров
	
	ОсталосьСтрокой = Формат(МаксимальноеКоличество + 1, "ЧН=0; ЧГ=");
	
	Запрос = Новый Запрос;
	Запрос.Текст =
		"ВЫБРАТЬ РАЗРЕШЕННЫЕ ПЕРВЫЕ " + ОсталосьСтрокой + "
		|	СодержимоеТранспортныхКонтейнеров.ТранспортноеСообщение КАК ТранспортноеСообщение,
		|	СодержимоеТранспортныхКонтейнеров.ИмяФайла КАК ИмяФайла
		|ИЗ
		|	РегистрСведений.СодержимоеТранспортныхКонтейнеров КАК СодержимоеТранспортныхКонтейнеров
		|		ЛЕВОЕ СОЕДИНЕНИЕ Справочник.ТранспортноеСообщениеПрисоединенныеФайлы КАК ТранспортноеСообщениеПрисоединенныеФайлы
		|			ПО ТранспортноеСообщениеПрисоединенныеФайлы.ВладелецФайла = СодержимоеТранспортныхКонтейнеров.ТранспортноеСообщение
		|				И ТранспортноеСообщениеПрисоединенныеФайлы.ИсходноеИмяФайла = СодержимоеТранспортныхКонтейнеров.ИмяФайла
		|ГДЕ
		|	ТранспортноеСообщениеПрисоединенныеФайлы.ВладелецФайла ЕСТЬ NULL";
	
	СодержимоеТранспортныхКонтейнеров = Запрос.Выполнить().Выгрузить();
	
	Для каждого СодержимоеТранспортногоКонтейнера Из СодержимоеТранспортныхКонтейнеров Цикл
		
		Если Обработано >= МаксимальноеКоличество Тогда
			Возврат Ложь;
		КонецЕсли;
		
		НачатьТранзакцию();
		
		МенеджерЗаписи = РегистрыСведений.СодержимоеТранспортныхКонтейнеров.СоздатьМенеджерЗаписи();
		МенеджерЗаписи.ТранспортноеСообщение 	= СодержимоеТранспортногоКонтейнера.ТранспортноеСообщение;
		МенеджерЗаписи.ИмяФайла 				= СодержимоеТранспортногоКонтейнера.ИмяФайла;
		МенеджерЗаписи.Прочитать();
		
		ДвоичныеДанныеФайла 	= МенеджерЗаписи.Данные.Получить();
		Тип 					= МенеджерЗаписи.Тип;
		Идентификатор 			= МенеджерЗаписи.Идентификатор;
		ЭЦПСертификат 			= МенеджерЗаписи.ЭЦПСертификат;
		ЭЦПСтатусПроверки 		= МенеджерЗаписи.ЭЦПСтатусПроверки;
		ЭЦПИмяПодписанногоФайла = МенеджерЗаписи.ЭЦПИмяПодписанногоФайла;
		ЭЦПЭтоПодписьАбонента 	= МенеджерЗаписи.ЭЦПЭтоПодписьАбонента;
		ТипФайлаОтчетностиПФР 	= МенеджерЗаписи.ТипФайлаОтчетностиПФР;
		ТипСодержимогоФайла 	= МенеджерЗаписи.ТипСодержимогоФайла;
		
		Если ДвоичныеДанныеФайла = Неопределено Тогда
			ДвоичныеДанныеФайла = Base64Значение("");
		КонецЕсли;
		
		МенеджерЗаписи.Данные = Новый ХранилищеЗначения(Неопределено);
		
		АдресФайла = ПоместитьВоВременноеХранилище(ДвоичныеДанныеФайла, Новый УникальныйИдентификатор);
		
		КомпонентыИмениФайла = ОбщегоНазначенияКлиентСервер.РазложитьПолноеИмяФайла(СодержимоеТранспортногоКонтейнера.ИмяФайла);
		УникальноеИмяФайлаБезРасширения = ОбщегоНазначенияЭДКОКлиентСервер.УникальнаяСтрока(КомпонентыИмениФайла.ИмяБезРасширения, 150);
		РасширениеФайлаБезТочки = Сред(КомпонентыИмениФайла.Расширение, 2);
		
		ПараметрыФайла = Новый Структура;
		ПараметрыФайла.Вставить("Автор", 						Неопределено);
		ПараметрыФайла.Вставить("ВладелецФайлов", 				СодержимоеТранспортногоКонтейнера.ТранспортноеСообщение);
		ПараметрыФайла.Вставить("ИмяБезРасширения", 			УникальноеИмяФайлаБезРасширения);
		ПараметрыФайла.Вставить("РасширениеБезТочки", 			РасширениеФайлаБезТочки);
		ПараметрыФайла.Вставить("ВремяИзмененияУниверсальное", 	Неопределено);
		
		ИнформацияОбОбновляемомФайле = Новый Структура;
		ИнформацияОбОбновляемомФайле.Вставить("АдресФайлаВоВременномХранилище", АдресФайла);
		ИнформацияОбОбновляемомФайле.Вставить("АдресВременногоХранилищаТекста", Неопределено);
		ИнформацияОбОбновляемомФайле.Вставить("ИмяБезРасширения", 				УникальноеИмяФайлаБезРасширения);
		ИнформацияОбОбновляемомФайле.Вставить("Расширение", 					РасширениеФайлаБезТочки);
		
		Попытка
			МенеджерЗаписи.Записать(Истина);
			
			МассивФайлов = ОбщегоНазначенияЭДКО.ПрикрепленныеФайлыКОбъектуИзСправочника(
				СодержимоеТранспортногоКонтейнера.ТранспортноеСообщение,
				"ТранспортноеСообщениеПрисоединенныеФайлы",
				СодержимоеТранспортногоКонтейнера.ИмяФайла);
			
			Если МассивФайлов.Количество() > 0 Тогда
				ПрисоединенныйФайл = МассивФайлов[0];
				РаботаСФайлами.ОбновитьФайл(ПрисоединенныйФайл, ИнформацияОбОбновляемомФайле);
				
			Иначе
				НоваяСсылкаНаФайл = РаботаСФайлами.НоваяСсылкаНаФайл(
					СодержимоеТранспортногоКонтейнера.ТранспортноеСообщение,
					"ТранспортноеСообщениеПрисоединенныеФайлы");
				ПрисоединенныйФайл = РаботаСФайлами.ДобавитьФайл(ПараметрыФайла, АдресФайла,,, НоваяСсылкаНаФайл);
			КонецЕсли;
			
			ПрисоединенныйФайлОбъект = ПрисоединенныйФайл.ПолучитьОбъект();
			
			ПрисоединенныйФайлОбъект.Тип = Тип;
			ПрисоединенныйФайлОбъект.Идентификатор 				= Идентификатор;
			ПрисоединенныйФайлОбъект.ЭЦПСертификат 				= ЭЦПСертификат;
			ПрисоединенныйФайлОбъект.ЭЦПСтатусПроверки 			= ЭЦПСтатусПроверки;
			ПрисоединенныйФайлОбъект.ЭЦПИмяПодписанногоФайла 	= ЭЦПИмяПодписанногоФайла;
			ПрисоединенныйФайлОбъект.ЭЦПЭтоПодписьАбонента 		= ЭЦПЭтоПодписьАбонента;
			ПрисоединенныйФайлОбъект.ТипФайлаОтчетностиПФР 		= ТипФайлаОтчетностиПФР;
			ПрисоединенныйФайлОбъект.ТипСодержимогоФайла 		= ТипСодержимогоФайла;
			ПрисоединенныйФайлОбъект.ИсходноеИмяФайла 			= СодержимоеТранспортногоКонтейнера.ИмяФайла;
			
			ПрисоединенныйФайлОбъект.Записать();
		Исключение
			ОтменитьТранзакцию();
			УдалитьИзВременногоХранилища(АдресФайла);
			ТекстОшибки = ИнформацияОбОшибке().Описание;
			ТекстСообщения = СтрШаблон(
				НСтр("ru = 'Не удалось сохранить в информационной базе содержимое транспортного контейнера сообщения ""%1"".
						   |%2'"),
				Строка(СодержимоеТранспортногоКонтейнера.ТранспортноеСообщение),
				ТекстОшибки);
			ЗаписьЖурналаРегистрации(
				НСтр("ru = 'Перенос транспортных сообщений в механизм присоединенных файлов'"),
				УровеньЖурналаРегистрации.Ошибка,,,
				ТекстСообщения);
			Возврат Ложь;
		КонецПопытки;
		
		ЗафиксироватьТранзакцию();
		УдалитьИзВременногоХранилища(АдресФайла);
		
		Обработано = Обработано + 1;
		
	КонецЦикла;
	
	// перенос транспортных контейнеров
	
	Осталось = МаксимальноеКоличество - Обработано + 1;
	Осталось = ?(Осталось > 0, Осталось, 1);
	ОсталосьСтрокой = Формат(Осталось, "ЧН=0; ЧГ=");
	
	Запрос = Новый Запрос;
	Запрос.Текст =
		"ВЫБРАТЬ РАЗРЕШЕННЫЕ ПЕРВЫЕ " + ОсталосьСтрокой + "
		|	ТранспортныеКонтейнеры.ТранспортноеСообщение КАК ТранспортноеСообщение,
		|	ТранспортныеКонтейнеры.ИмяФайла КАК ИмяФайла
		|ИЗ
		|	РегистрСведений.ТранспортныеКонтейнеры КАК ТранспортныеКонтейнеры
		|		ЛЕВОЕ СОЕДИНЕНИЕ Справочник.ТранспортноеСообщениеКонтейнерыПрисоединенныеФайлы КАК ТранспортноеСообщениеКонтейнерыПрисоединенныеФайлы
		|			ПО ТранспортноеСообщениеКонтейнерыПрисоединенныеФайлы.ВладелецФайла = ТранспортныеКонтейнеры.ТранспортноеСообщение
		|				И ТранспортноеСообщениеКонтейнерыПрисоединенныеФайлы.ИсходноеИмяФайла = ТранспортныеКонтейнеры.ИмяФайла
		|ГДЕ
		|	ТранспортноеСообщениеКонтейнерыПрисоединенныеФайлы.ВладелецФайла ЕСТЬ NULL";
	
	ТранспортныеКонтейнеры = Запрос.Выполнить().Выгрузить();
	
	Для каждого ТранспортныйКонтейнер Из ТранспортныеКонтейнеры Цикл
		
		Если Обработано >= МаксимальноеКоличество Тогда
			Возврат Ложь;
		КонецЕсли;
		
		НачатьТранзакцию();
		
		МенеджерЗаписи = РегистрыСведений.ТранспортныеКонтейнеры.СоздатьМенеджерЗаписи();
		МенеджерЗаписи.ТранспортноеСообщение 	= ТранспортныйКонтейнер.ТранспортноеСообщение;
		МенеджерЗаписи.ИмяФайла 				= ТранспортныйКонтейнер.ИмяФайла;
		МенеджерЗаписи.Прочитать();
		
		ДвоичныеДанныеФайла 	= МенеджерЗаписи.Данные.Получить();
		
		Если ДвоичныеДанныеФайла = Неопределено Тогда
			ДвоичныеДанныеФайла = Base64Значение("");
		КонецЕсли;
		
		МенеджерЗаписи.Данные = Новый ХранилищеЗначения(Неопределено);
		
		АдресФайла = ПоместитьВоВременноеХранилище(ДвоичныеДанныеФайла, Новый УникальныйИдентификатор);
		
		КомпонентыИмениФайла = ОбщегоНазначенияКлиентСервер.РазложитьПолноеИмяФайла(ТранспортныйКонтейнер.ИмяФайла);
		УникальноеИмяФайлаБезРасширения = ОбщегоНазначенияЭДКОКлиентСервер.УникальнаяСтрока(КомпонентыИмениФайла.ИмяБезРасширения, 150);
		РасширениеФайлаБезТочки = Сред(КомпонентыИмениФайла.Расширение, 2);
		
		ПараметрыФайла = Новый Структура;
		ПараметрыФайла.Вставить("Автор", 						Неопределено);
		ПараметрыФайла.Вставить("ВладелецФайлов", 				ТранспортныйКонтейнер.ТранспортноеСообщение);
		ПараметрыФайла.Вставить("ИмяБезРасширения", 			УникальноеИмяФайлаБезРасширения);
		ПараметрыФайла.Вставить("РасширениеБезТочки", 			РасширениеФайлаБезТочки);
		ПараметрыФайла.Вставить("ВремяИзмененияУниверсальное", 	Неопределено);
		
		ИнформацияОбОбновляемомФайле = Новый Структура;
		ИнформацияОбОбновляемомФайле.Вставить("АдресФайлаВоВременномХранилище", АдресФайла);
		ИнформацияОбОбновляемомФайле.Вставить("АдресВременногоХранилищаТекста", Неопределено);
		ИнформацияОбОбновляемомФайле.Вставить("ИмяБезРасширения", 				УникальноеИмяФайлаБезРасширения);
		ИнформацияОбОбновляемомФайле.Вставить("Расширение", 					РасширениеФайлаБезТочки);
		
		Попытка
			МенеджерЗаписи.Записать(Истина);
			
			МассивФайлов = ОбщегоНазначенияЭДКО.ПрикрепленныеФайлыКОбъектуИзСправочника(
				ТранспортныйКонтейнер.ТранспортноеСообщение,
				"ТранспортноеСообщениеКонтейнерыПрисоединенныеФайлы",
				ТранспортныйКонтейнер.ИмяФайла);
			
			Если МассивФайлов.Количество() > 0 Тогда
				ПрисоединенныйФайл = МассивФайлов[0];
				РаботаСФайлами.ОбновитьФайл(ПрисоединенныйФайл, ИнформацияОбОбновляемомФайле);
				
			Иначе
				НоваяСсылкаНаФайл = РаботаСФайлами.НоваяСсылкаНаФайл(
					ТранспортныйКонтейнер.ТранспортноеСообщение,
					"ТранспортноеСообщениеКонтейнерыПрисоединенныеФайлы");
				ПрисоединенныйФайл = РаботаСФайлами.ДобавитьФайл(ПараметрыФайла, АдресФайла,,, НоваяСсылкаНаФайл);
			КонецЕсли;
			
			ПрисоединенныйФайлОбъект = ПрисоединенныйФайл.ПолучитьОбъект();
			
			ПрисоединенныйФайлОбъект.ИсходноеИмяФайла = ТранспортныйКонтейнер.ИмяФайла;
			
			ПрисоединенныйФайлОбъект.Записать();
		Исключение
			ОтменитьТранзакцию();
			УдалитьИзВременногоХранилища(АдресФайла);
			ТекстОшибки = ИнформацияОбОшибке().Описание;
			ТекстСообщения = СтрШаблон(
				НСтр("ru = 'Не удалось сохранить в информационной базе транспортный контейнер сообщения ""%1"".
						   |%2'"),
				Строка(ТранспортныйКонтейнер.ТранспортноеСообщение),
				ТекстОшибки);
			ЗаписьЖурналаРегистрации(
				НСтр("ru = 'Перенос транспортных сообщений в механизм присоединенных файлов'"),
				УровеньЖурналаРегистрации.Ошибка,,,
				ТекстСообщения);
			Возврат Ложь;
		КонецПопытки;
		
		ЗафиксироватьТранзакцию();
		УдалитьИзВременногоХранилища(АдресФайла);
		
		Обработано = Обработано + 1;
		
	КонецЦикла;
	
	// перенос вложений писем
	
	Осталось = МаксимальноеКоличество - Обработано + 1;
	Осталось = ?(Осталось > 0, Осталось, 1);
	ОсталосьСтрокой = Формат(Осталось, "ЧН=0; ЧГ=");
	
	Запрос = Новый Запрос;
	Запрос.Текст =
		"ВЫБРАТЬ РАЗРЕШЕННЫЕ ПЕРВЫЕ " + ОсталосьСтрокой + "
		|	ВложенияНеформализованныхДокументов.НеформализованныйДокумент КАК НеформализованныйДокумент,
		|	ВложенияНеформализованныхДокументов.ИмяФайла КАК ИмяФайла
		|ИЗ
		|	РегистрСведений.ВложенияНеформализованныхДокументов КАК ВложенияНеформализованныхДокументов
		|		ЛЕВОЕ СОЕДИНЕНИЕ Справочник.ПерепискаСКонтролирующимиОрганамиПрисоединенныеФайлы КАК ПерепискаСКонтролирующимиОрганамиПрисоединенныеФайлы
		|			ПО ПерепискаСКонтролирующимиОрганамиПрисоединенныеФайлы.ВладелецФайла = ВложенияНеформализованныхДокументов.НеформализованныйДокумент
		|				И ПерепискаСКонтролирующимиОрганамиПрисоединенныеФайлы.ИсходноеИмяФайла = ВложенияНеформализованныхДокументов.ИмяФайла
		|ГДЕ
		|	ПерепискаСКонтролирующимиОрганамиПрисоединенныеФайлы.ВладелецФайла ЕСТЬ NULL";
	
	ВложенияНеформализованныхДокументов = Запрос.Выполнить().Выгрузить();
	
	Для каждого ВложениеНеформализованногоДокумента Из ВложенияНеформализованныхДокументов Цикл
		
		Если Обработано >= МаксимальноеКоличество Тогда
			Возврат Ложь;
		КонецЕсли;
		
		НачатьТранзакцию();
		
		МенеджерЗаписи = РегистрыСведений.ВложенияНеформализованныхДокументов.СоздатьМенеджерЗаписи();
		МенеджерЗаписи.НеформализованныйДокумент 	= ВложениеНеформализованногоДокумента.НеформализованныйДокумент;
		МенеджерЗаписи.ИмяФайла 					= ВложениеНеформализованногоДокумента.ИмяФайла;
		МенеджерЗаписи.Прочитать();
		
		ДвоичныеДанныеФайла = МенеджерЗаписи.Данные.Получить();
		Тип 				= МенеджерЗаписи.Тип;
		
		Если ДвоичныеДанныеФайла = Неопределено Тогда
			ДвоичныеДанныеФайла = Base64Значение("");
		КонецЕсли;
		
		МенеджерЗаписи.Данные = Новый ХранилищеЗначения(Неопределено);
		
		АдресФайла = ПоместитьВоВременноеХранилище(ДвоичныеДанныеФайла, Новый УникальныйИдентификатор);
		
		КомпонентыИмениФайла = ОбщегоНазначенияКлиентСервер.РазложитьПолноеИмяФайла(ВложениеНеформализованногоДокумента.ИмяФайла);
		УникальноеИмяФайлаБезРасширения = ОбщегоНазначенияЭДКОКлиентСервер.УникальнаяСтрока(КомпонентыИмениФайла.ИмяБезРасширения, 150);
		РасширениеФайлаБезТочки = Сред(КомпонентыИмениФайла.Расширение, 2);
		
		ПараметрыФайла = Новый Структура;
		ПараметрыФайла.Вставить("Автор", 						Неопределено);
		ПараметрыФайла.Вставить("ВладелецФайлов", 				ВложениеНеформализованногоДокумента.НеформализованныйДокумент);
		ПараметрыФайла.Вставить("ИмяБезРасширения", 			УникальноеИмяФайлаБезРасширения);
		ПараметрыФайла.Вставить("РасширениеБезТочки", 			РасширениеФайлаБезТочки);
		ПараметрыФайла.Вставить("ВремяИзмененияУниверсальное", 	Неопределено);
		
		ИнформацияОбОбновляемомФайле = Новый Структура;
		ИнформацияОбОбновляемомФайле.Вставить("АдресФайлаВоВременномХранилище", АдресФайла);
		ИнформацияОбОбновляемомФайле.Вставить("АдресВременногоХранилищаТекста", Неопределено);
		ИнформацияОбОбновляемомФайле.Вставить("ИмяБезРасширения", 				УникальноеИмяФайлаБезРасширения);
		ИнформацияОбОбновляемомФайле.Вставить("Расширение", 					РасширениеФайлаБезТочки);
		
		Попытка
			МенеджерЗаписи.Записать(Истина);
			
			МассивФайлов = ОбщегоНазначенияЭДКО.ПрикрепленныеФайлыКОбъектуИзСправочника(
				ВложениеНеформализованногоДокумента.НеформализованныйДокумент,
				"ПерепискаСКонтролирующимиОрганамиПрисоединенныеФайлы",
				ВложениеНеформализованногоДокумента.ИмяФайла);
			
			Если МассивФайлов.Количество() > 0 Тогда
				ПрисоединенныйФайл = МассивФайлов[0];
				РаботаСФайлами.ОбновитьФайл(ПрисоединенныйФайл, ИнформацияОбОбновляемомФайле);
				
			Иначе
				НоваяСсылкаНаФайл = РаботаСФайлами.НоваяСсылкаНаФайл(
					ВложениеНеформализованногоДокумента.НеформализованныйДокумент,
					"ПерепискаСКонтролирующимиОрганамиПрисоединенныеФайлы");
				ПрисоединенныйФайл = РаботаСФайлами.ДобавитьФайл(ПараметрыФайла, АдресФайла,,, НоваяСсылкаНаФайл);
			КонецЕсли;
			
			ПрисоединенныйФайлОбъект = ПрисоединенныйФайл.ПолучитьОбъект();
			
			ПрисоединенныйФайлОбъект.Тип 				= Тип;
			ПрисоединенныйФайлОбъект.ИсходноеИмяФайла 	= ВложениеНеформализованногоДокумента.ИмяФайла;
			
			ПрисоединенныйФайлОбъект.Записать();
		Исключение
			ОтменитьТранзакцию();
			УдалитьИзВременногоХранилища(АдресФайла);
			ТекстОшибки = ИнформацияОбОшибке().Описание;
			ТекстСообщения = СтрШаблон(
				НСтр("ru = 'Не удалось сохранить в информационной базе вложение письма ""%1"".
						   |%2'"),
				Строка(ВложениеНеформализованногоДокумента.НеформализованныйДокумент),
				ТекстОшибки);
			ЗаписьЖурналаРегистрации(
				НСтр("ru = 'Перенос транспортных сообщений в механизм присоединенных файлов'"),
				УровеньЖурналаРегистрации.Ошибка,,,
				ТекстСообщения);
			Возврат Ложь;
		КонецПопытки;
		
		ЗафиксироватьТранзакцию();
		УдалитьИзВременногоХранилища(АдресФайла);
		
		Обработано = Обработано + 1;
		
	КонецЦикла;
	
	Возврат Истина;
	
КонецФункции

#КонецОбласти

#Область УпрощенноеЗаявлениеНаПодключение

Функция СкорректироватьВидУдостоверения(ВладелецЭЦПВидДокумента) Экспорт
	
	ЭтоПаспортРФ 			= ЭтоПаспортРФ(ВладелецЭЦПВидДокумента);
	ЭтоИностранныйПаспорт 	= ЭтоИностранныйПаспорт(ВладелецЭЦПВидДокумента);
	
	Если ЭтоПаспортРФ Тогда
		Результат = Перечисления.ВидыУдостоверенийВладельцаЭП.ПаспортРФ;
	ИначеЕсли ЭтоИностранныйПаспорт Тогда
		Результат = Перечисления.ВидыУдостоверенийВладельцаЭП.ИностранныйПаспорт;
	Иначе
		Результат = Перечисления.ВидыУдостоверенийВладельцаЭП.ПустаяСсылка();
	КонецЕсли;
	
	Возврат Результат;
	
КонецФункции

Функция ЭтоПаспортРФ(Документ) Экспорт
	
	Если ТипЗнч(Документ) = Тип("СправочникСсылка.ВидыДокументовФизическихЛиц") Тогда
		
		КодВидаДокумента  = ПолучитьКодВидаДокументаФизическогоЛица(Документ); 
		Результат 		  = СокрЛП(Строка(КодВидаДокумента)) = "21"
			ИЛИ Строка(Документ) = НСтр("ru = 'Паспорт гражданина РФ'");
		
	ИначеЕсли ТипЗнч(Документ) = Тип("ПеречислениеСсылка.ВидыУдостоверенийВладельцаЭП") Тогда
		
		Результат = Документ = Перечисления.ВидыУдостоверенийВладельцаЭП.ПаспортРФ;
		
	Иначе
		
		Результат = Ложь;
		
	КонецЕсли;
	
	Возврат Результат;
	
КонецФункции

Функция ЭтоИностранныйПаспорт(Документ) Экспорт
	
	Результат = Ложь;
	
	Если ТипЗнч(Документ) = Тип("СправочникСсылка.ВидыДокументовФизическихЛиц") Тогда
		
		КонтекстЭДОСервер = ДокументооборотСКО.ПолучитьОбработкуЭДО();
		КодВидаДокумента  = КонтекстЭДОСервер.ПолучитьКодВидаДокументаФизическогоЛица(Документ); 
		Результат 		  = СокрЛП(Строка(КодВидаДокумента)) = "10";
		
	ИначеЕсли ТипЗнч(Документ) = Тип("ПеречислениеСсылка.ВидыУдостоверенийВладельцаЭП") Тогда
		
		Результат = Документ = Перечисления.ВидыУдостоверенийВладельцаЭП.ИностранныйПаспорт;
		
	Иначе
		
		Результат = Ложь;

	КонецЕсли;
		
	Возврат Результат;
	
КонецФункции

Процедура ОтправитьОбратнуюСвязьСервер(СпецоператорСвязи, Пакет) Экспорт
	
	РезультатВыполнения = Новый Структура("Выполнено", Истина);
		
	Результат = СоздатьКлиентСервисаПоСпецоператоруСвязи(СпецоператорСвязи);
	Если Результат.Выполнено Тогда
		
		Попытка
			Ответ = Результат.КлиентСервиса.SendPacket(Пакет);
			ОтветXDTO = Дессериализовать(Ответ);
			Если ОтветXDTO.code <> "0" И ОтветXDTO.code <> "60" Тогда
				РезультатВыполнения.Вставить("Выполнено", Ложь);
				Если ОтветXDTO.Свойства().Получить("errorMessage") <> Неопределено Тогда
					РезультатВыполнения.Вставить("ОписаниеОшибки", ОтветXDTO.errorMessage);
				Иначе
					РезультатВыполнения.Вставить("ОписаниеОшибки", "Ошибка при отправке расписки в получении сертификата");
				КонецЕсли;
			КонецЕсли;
			
		Исключение
			
			ТекстОшибки 		= КраткоеПредставлениеОшибки(ОбщегоНазначенияЭДКОКлиентСервер.ПолучитьИнформациюОбОшибке(ИнформацияОбОшибке()));
			КодОсновногоЯзыка	= ОбщегоНазначения.КодОсновногоЯзыка();
			
			ЗаписьЖурналаРегистрации(
				НСтр("ru = 'Электронный документооборот с контролирующими органами. Отправка обратной связи'", КодОсновногоЯзыка),
				УровеньЖурналаРегистрации.Ошибка,,,
				ТекстОшибки);
			
		КонецПопытки;
	
	Иначе
		РезультатВыполнения = Результат;
	КонецЕсли;
	
КонецПроцедуры

Процедура ПолучитьКонтактыПФР(АдресХранилища, КодРегиона, КодПФР) Экспорт
	
	// Листинг всех органов региона 77:
	// http://91.239.5.7:8080/export/controlling-units-v1?request_type=listing&cont_unit_type=1&region_code=77&api_key=7cc38186f9b657c2301de7ffacd4dca3
	// Информация по одному органу:
	// http://91.239.5.7:8080/export/controlling-units-v1?request_type=single_cu_data&cont_unit_type=1&region_code=77&code=087-804&api_key=7cc38186f9b657c2301de7ffacd4dca3
	// Просмотрщик ответов: http://jsonviewer.stack.hu/
	
	УстановитьПривилегированныйРежим(Истина);
	
	Попытка
		
		URL = "http://91.239.5.7:8080/export/controlling-units-v1";
		
		Соединение = ДокументооборотСКО.УстановитьСоединениеССерверомИнтернета(URL);
		
		Путь = "/export/controlling-units-v1?request_type=single_cu_data&cont_unit_type=1&region_code=%1&code=%2&api_key=7cc38186f9b657c2301de7ffacd4dca3";
		Путь = СтрШаблон(Путь, КодРегиона, КодПФР);
		
		Запрос = Новый HTTPЗапрос(Путь);
		
		Ответ = Соединение.ВызватьHTTPМетод("GET", Запрос);
		
		Результат = Новый Структура;
		Результат.Вставить("Выполнено", 	Ложь);
		Результат.Вставить("Ошибка",    	"");
		Результат.Вставить("КодСостояния", 	Ответ.КодСостояния);
		Результат.Вставить("КодРегиона", 	КодРегиона);
		Результат.Вставить("КодПФР", 		КодПФР);
		Результат.Вставить("ПолныйОтвет", 	Ответ.ПолучитьТелоКакСтроку());
		Результат.Вставить("Путь", 			"http://91.239.5.7:8080" + Путь);
		
		//400	Некорректный запрос (запрос без обязательных параметров)
		//204	Запрос успешный, но в БД нет информации, соответствующей вашему запросу.
		//200	Запрос успешен, найдены данные. Данные находятся в теле ответа.
		//500	Ошибка на стороне сервиса. Мы очень надеемся, что вам никогда не придет такой ответ.
		//503	Сервис временно недоступен. Этот код будет возвращаться тогда, когда сервис находится на обслуживании.
		//405	Недопустимый метод запроса. Допустимые методы перечислены в заголовке ответа.
		
		Если Ответ.КодСостояния = 200 Тогда
			
			ПараметрыОтвета = JsonВСтруктуру(Ответ.ПолучитьТелоКакСтроку());
			
			Телефоны = СтрСоединить(ПараметрыОтвета.telephones, ", ");
			
			location = ПараметрыОтвета.location;
			
			ЧастиАдреса = Новый Структура();
			ЧастиАдреса.Вставить("Страна", НСтр("ru = 'Россия'"));
			ЧастиАдреса.Вставить("Индекс", СокрЛП(Строка(location.postcode)));
			ЧастиАдреса.Вставить("Регион", СокрЛП(Строка(location.region_title)));
			
			Если location.region_title <> location.settlement_title Тогда
				ЧастиАдреса.Вставить("Город",  СокрЛП(Строка(location.settlement_title)));
			КонецЕсли;
			
			ЧастиАдреса.Вставить("ГородСокращение", СокрЛП(Строка(location.settlement_type_short_title)));
			ЧастиАдреса.Вставить("Улица",  СокрЛП(Строка(location.address)));
			ЧастиАдреса.Вставить("УлицаСокращение",  СокрЛП(Строка(location.streets_type_short_title)));
			
			АдреcXML = УправлениеКонтактнойИнформацией.КонтактнаяИнформацияВXML(ЧастиАдреса, , Перечисления.ТипыКонтактнойИнформации.Адрес);
			Адрес = УправлениеКонтактнойИнформацией.ПредставлениеКонтактнойИнформации(АдреcXML,  Перечисления.ТипыКонтактнойИнформации.Адрес);
			
			ЧастиАдреса = Новый Массив();
			ЧастиАдреса.Добавить(СокрЛП(Адрес));
			ЧастиАдреса.Добавить(СокрЛП(Строка(location.house)));
			ЧастиАдреса.Добавить(НРег(СокрЛП(Строка(location.construction_type_short_title))));
			ЧастиАдреса.Добавить(СокрЛП(Строка(location.building_number)));
			ЧастиАдреса.Добавить(НРег(СокрЛП(Строка(location.construction_type_short_title_additional))));
			ЧастиАдреса.Добавить(СокрЛП(Строка(location.building_number_additional)));
			
			Адрес = СокрЛП(СтрСоединить(ЧастиАдреса, " "));
		
			Результат.Вставить("Телефон", 		Телефоны);
			Результат.Вставить("Наименование", 	ПараметрыОтвета.title);
			Результат.Вставить("Адрес", 		Адрес);
			Результат.Вставить("Выполнено", 	Истина);

		КонецЕсли;
		
		ПоместитьВоВременноеХранилище(Результат, АдресХранилища);
		
	Исключение
		
		ИмяСобытия = НСтр("ru = 'Электронный документооборот с контролирующими органами.Получение контактов ПФР http://91.239.5.7:8080/export/controlling-units'", ОбщегоНазначения.КодОсновногоЯзыка());
		
		Комментарий = ПодробноеПредставлениеОшибки(ИнформацияОбОшибке());
		
		ЗаписьЖурналаРегистрации(
			ИмяСобытия ,
			УровеньЖурналаРегистрации.Ошибка,,,
			Комментарий);
					
	КонецПопытки;
	
КонецПроцедуры

Функция ПаспортныеДанныеВладельцаЭП(УчетнаяЗапись) Экспорт
	
	ДополнительныеПараметры = Новый Структура();
	ДополнительныеПараметры.Вставить("КодВидаДокумента",               Неопределено);
	ДополнительныеПараметры.Вставить("ВладелецЭЦПДатаВыдачиДокумента", Неопределено);
	ДополнительныеПараметры.Вставить("ВладелецЭЦПКемВыданДокумент",    Неопределено);
	ДополнительныеПараметры.Вставить("ВладелецЭЦПКодПодразделения",    Неопределено);
	ДополнительныеПараметры.Вставить("ВладелецЭЦПСерияДокумента",      Неопределено);
	ДополнительныеПараметры.Вставить("ВладелецЭЦПНомерДокумента",      Неопределено);
	
	Запрос = Новый Запрос("ВЫБРАТЬ РАЗРЕШЕННЫЕ ПЕРВЫЕ 1
	                      |	ФайлыСведенийСпецоператоры.Данные КАК Данные,
	                      |	ФайлыСведенийСпецоператоры.ДатаФайла КАК ДатаФайла
	                      |ИЗ
	                      |	РегистрСведений.ФайлыСведенийСпецоператоры КАК ФайлыСведенийСпецоператоры
	                      |ГДЕ
	                      |	ФайлыСведенийСпецоператоры.УчетнаяЗапись = &УчетнаяЗапись
	                      |
	                      |УПОРЯДОЧИТЬ ПО
	                      |	ФайлыСведенийСпецоператоры.ДатаФайла УБЫВ");
	
	Запрос.УстановитьПараметр("УчетнаяЗапись", УчетнаяЗапись);
	
	Выборка = Запрос.Выполнить().Выбрать();
	Если Выборка.Следующий() Тогда
		Данные = Выборка.Данные;
		Если ЗначениеЗаполнено(Данные) Тогда
			Попытка
			
				ВременныйФайл = ПолучитьИмяВременногоФайла();
				Данные.Получить().Записать(ВременныйФайл);
				
				ДокументDOM = ЭлектронныйДокументооборотСКонтролирующимиОрганами.ЗагрузитьФайлXML(ВременныйФайл);
				
				Удостоверения = ДокументDOM.ПолучитьЭлементыПоИмени("УдЛичн");
				Если Удостоверения.Количество() = 0 Тогда
					Возврат ДополнительныеПараметры;
				КонецЕсли;
				
				Удостоверение = Удостоверения[0].Атрибуты;
				
				КодВидДокВФайле     = Удостоверение.ПолучитьИменованныйЭлемент("КодВидДок").ТекстовоеСодержимое;
				СерНомДокВФайле     = Удостоверение.ПолучитьИменованныйЭлемент("СерНомДок").ТекстовоеСодержимое;
				ДатаДокВФайле       = Удостоверение.ПолучитьИменованныйЭлемент("ДатаДок").ТекстовоеСодержимое;
				ВыдДокВФайле        = Удостоверение.ПолучитьИменованныйЭлемент("ВыдДок").ТекстовоеСодержимое;
				КодВыдДокВФайле     = Удостоверение.ПолучитьИменованныйЭлемент("КодВыдДок").ТекстовоеСодержимое;
				
				СерНомДокВФайле = СтрСоединить(СерНомДокВФайле,",");
				Если СерНомДокВФайле.Количество() > 0 Тогда
					ВладелецЭЦПСерияДокумента = СерНомДокВФайле[0];
					ВладелецЭЦПНомерДокумента = СерНомДокВФайле[1]; 
				КонецЕсли;
				
				
				ДополнительныеПараметры.Вставить("КодВидаДокумента",               КодВидДокВФайле);
				ДополнительныеПараметры.Вставить("ВладелецЭЦПДатаВыдачиДокумента", ДатаДокВФайле);
				ДополнительныеПараметры.Вставить("ВладелецЭЦПКемВыданДокумент",    ВыдДокВФайле);
				ДополнительныеПараметры.Вставить("ВладелецЭЦПКодПодразделения",    КодВыдДокВФайле);
				ДополнительныеПараметры.Вставить("ВладелецЭЦПСерияДокумента",      ВладелецЭЦПСерияДокумента);
				ДополнительныеПараметры.Вставить("ВладелецЭЦПНомерДокумента",      ВладелецЭЦПНомерДокумента);
				
			Исключение
				Возврат ДополнительныеПараметры;
			КонецПопытки;
		Иначе
			Возврат ДополнительныеПараметры;
		КонецЕсли;
	КонецЕсли;
	
	Возврат ДополнительныеПараметры;
	
КонецФункции

Функция ПолучитьOIDЛицензииКриптоПро() Экспорт
	
	УстановитьПривилегированныйРежим(Истина);
	OID = "";
	
	Попытка
	
		URL = "http://regservice.keydisk.ru";
	
		Соединение = ДокументооборотСКО.УстановитьСоединениеССерверомИнтернета(URL);
		
		Путь = "/regservice.asmx/GetCryptoProTemplateOid?";
		
		Запрос = Новый HTTPЗапрос(Путь);
		
		Ответ = Соединение.ВызватьHTTPМетод("GET", Запрос);
		
		ОписаниеОшибки = "";
		
		ТелоКакСтрока = Ответ.ПолучитьТелоКакСтроку();
		ТелоКакСтрока = СтрЗаменить(ТелоКакСтрока, "&lt;", "<");
		ТелоКакСтрока = СтрЗаменить(ТелоКакСтрока, "&gt;", ">");
		
		Результат = ЗагрузитьXMLВДеревоЗначений( , ТелоКакСтрока, ОписаниеОшибки);
		
		Если НЕ ЗначениеЗаполнено(ОписаниеОшибки) Тогда
			СтрокаOID = Результат.Строки.Найти("oid", "Имя", Истина);
			Если СтрокаOID <> Неопределено Тогда 
				OID = СтрокаOID.Значение;
			КонецЕсли;
		КонецЕсли;
		
	Исключение
		
		ИмяСобытия = НСтр("ru = 'Электронный документооборот с контролирующими органами.Получение OID лицензии КриптоПро'", ОбщегоНазначения.КодОсновногоЯзыка());
		
		Комментарий = ПодробноеПредставлениеОшибки(ИнформацияОбОшибке());
		
		ЗаписьЖурналаРегистрации(
			ИмяСобытия ,
			УровеньЖурналаРегистрации.Ошибка,,,
			Комментарий);
		
	КонецПопытки;
		
	Возврат OID;
	
КонецФункции

Функция JsonВСтруктуру(СтрокаJSON, ПараметрыПреобразования = Неопределено)
	
	ЧтениеJSON = Новый ЧтениеJSON;
	ЧтениеJSON.УстановитьСтроку(СтрокаJSON);
	
	Если ТипЗнч(ПараметрыПреобразования) = Тип("Структура")
		И ПараметрыПреобразования.Свойство("ИменаСвойствДляВосстановления")
		И ЗначениеЗаполнено(ПараметрыПреобразования.ИменаСвойствДляВосстановления) Тогда
		Объект = ПрочитатьJSON(
			ЧтениеJSON,,,, 
			"ПреобразоватьBase64ВДвоичныеДанные", 
			СервисКриптографииСлужебный, 
			ПараметрыПреобразования,
			ПараметрыПреобразования.ИменаСвойствДляВосстановления);
	Иначе
		Объект = ПрочитатьJSON(ЧтениеJSON);
	КонецЕсли;
	
	ЧтениеJSON.Закрыть();
	
	Возврат Объект;
	
КонецФункции

Процедура НапоминитьПозжеПроЗаявление(ЗаявлениеАбонента, БольшеНеНапоминать = Ложь) Экспорт
	
	Если БольшеНеНапоминать
		И ЗаявлениеАбонента.Статус = Перечисления.СтатусыЗаявленияАбонентаСпецоператораСвязи.Отклонено Тогда
		
		// Для отклоненных заявлений не запоминаем признак "Больше не показывать".
		// Проставляем флаг, что заявление обработано и больше о нем напоминать не нужно.
		ОбработкаЗаявленийАбонентаВызовСервера.ОбновитьРеквизитыЗаявления(
			ЗаявлениеАбонента.Ссылка, 
			Новый Структура("НастройкаЗавершена", Истина));
			
	Иначе
		
		Заявления = ХранилищеОбщихНастроек.Загрузить(ОбработкаЗаявленийАбонента.КлючЗаявленийТребующихНапоминанияПозже());
		
		Если Заявления = Неопределено Тогда
			Заявления = ТаблицаЗаявленийТребующихНапоминанияПозже();
		КонецЕсли;
		
		ЗаявлениеВТаблице = Заявления.Найти(ЗаявлениеАбонента, "Заявление");
		Если ЗаявлениеВТаблице = Неопределено Тогда
			ЗаявлениеВТаблице = Заявления.Добавить();
			ЗаявлениеВТаблице.Заявление = ЗаявлениеАбонента;
		Конецесли;
		
		Если БольшеНеНапоминать Тогда
			// Откладываем на "конец времен".
			ЗаявлениеВТаблице.Дата = Дата(3999, 12, 31, 23, 59, 59);
		Иначе
			// Откладываем показ на 4 часа.
			ЗаявлениеВТаблице.Дата = ТекущаяДатаСеанса() + 4 * 60 * 60;
		КонецЕсли;

		ХранилищеОбщихНастроек.Сохранить(ОбработкаЗаявленийАбонента.КлючЗаявленийТребующихНапоминанияПозже(), , Заявления);
		
	КонецЕсли;

КонецПроцедуры

Функция ТаблицаЗаявленийТребующихНапоминанияПозже()

	Таблица = Новый ТаблицаЗначений;
	Таблица.Колонки.Добавить("Заявление", 	Новый ОписаниеТипов("ДокументСсылка.ЗаявлениеАбонентаСпецоператораСвязи"));
	Таблица.Колонки.Добавить("Дата", 		Новый ОписаниеТипов("Дата")); // Когда следует начинать напоминать.
	
	Возврат Таблица;

КонецФункции

Функция ПроверитьСериюДокумента(ВидДокумента,СерияДокумента, ТихийРежим = Ложь) Экспорт
	
	ДанныеЗаполненыВерно = Истина;
	
	Если ЗначениеЗаполнено(ВидДокумента) Тогда
		ШаблонПроверки = ПолучитьШаблонПроверки(ВидДокумента);
	Иначе
		Если НЕ ТихийРежим Тогда
			ОбщегоНазначения.СообщитьПользователю(НСтр("ru = 'Укажите вид документа, удостоверяющего личность сотрудника.'"));
		КонецЕсли;
			
		Возврат Ложь;
	КонецЕсли;
	
	Если ШаблонПроверки <> "SSSSSSSSSSSSSSSSSSSSSSSSS" Тогда
		Если ПустаяСтрока(СерияДокумента) Тогда
			Возврат Ложь;
		КонецЕсли;
	КонецЕсли;
	
	Возврат ДанныеЗаполненыВерно;
	
КонецФункции

Функция ПолучитьШаблонПроверки(ДокументФизическогоЛица)
	
	КодДокумента = ПолучитьКодВидаДокументаФизическогоЛица(ДокументФизическогоЛица);
	
	Если ПустаяСтрока(КодДокумента) Тогда
		ЗначениеПоиска = СокрЛП(ДокументФизическогоЛица);
		ПоискПоКоду = Ложь;
	Иначе
		ЗначениеПоиска = КодДокумента;
		ПоискПоКоду = Истина;
	КонецЕсли;
	
	Макет = ПолучитьМакетОбработки("ВидыИКодыДокументовФизическихЛиц");
	
	Для Инд = 1 По Макет.ВысотаТаблицы Цикл
		
		Если ПоискПоКоду Тогда
		ЗначениеМакет = СокрЛП(Макет.Область(Инд, 1, Инд, 1).Текст);
		Иначе
		ЗначениеМакет = СокрЛП(Макет.Область(Инд, 2, Инд, 2).Текст);
		КонецЕсли;
		
		Если НРег(ЗначениеМакет) = НРег(ЗначениеПоиска) Тогда
			Возврат СокрЛП(Макет.Область(Инд, 3, Инд, 3).Текст);
		КонецЕсли;
		
	КонецЦикла;
	
	Возврат "SSSSSSSSSSSSSSSSSSSSSSSSS";
	
КонецФункции

Процедура НачатьОпределениеТекстаЗаявления(АдресХранилища, ДокументЗаявление, АлгоритмКонтейнераКлючей) Экспорт
	
	ДополнительныеПараметры = Новый Структура();
	ДополнительныеПараметры.Вставить("Выполнено", 		Ложь);
	ДополнительныеПараметры.Вставить("Результат", 		"");
	ДополнительныеПараметры.Вставить("ОписаниеОшибки", 	"");
	ДополнительныеПараметры.Вставить("ТекстОтправляемогоЗаявления", Неопределено);
	
	Попытка
	
		РезультатВыгрузки = ОбработкаЗаявленийАбонента.ВыгрузитьЗаявлениеАбонентаВМоделиСервиса(ДокументЗаявление, АлгоритмКонтейнераКлючей);
		
		ДополнительныеПараметры.Выполнено = РезультатВыгрузки.Выполнено;
		
		Если ДополнительныеПараметры.Выполнено Тогда
			ДополнительныеПараметры.ТекстОтправляемогоЗаявления = РезультатВыгрузки.Заявление;
		Иначе
			ДополнительныеПараметры.ОписаниеОшибки = РезультатВыгрузки.ОписаниеОшибки;
		КонецЕсли;
		
		ПоместитьВоВременноеХранилище(ДополнительныеПараметры, АдресХранилища);
		
	Исключение
		
		ИмяСобытия = НСтр("ru = 'Электронный документооборот с контролирующими органами. Получение текста заявления для ЭП в облаке'", 
			ОбщегоНазначения.КодОсновногоЯзыка());
			
		Комментарий = ПодробноеПредставлениеОшибки(ИнформацияОбОшибке());
		
		ЗаписьЖурналаРегистрации(
			ИмяСобытия ,
			УровеньЖурналаРегистрации.Ошибка,,,
			Комментарий);
			
		ДополнительныеПараметры.ОписаниеОшибки = НСтр("ru = 'Сервис временно недоступен. Обратитесь в службу поддержки, повторите попытку позже или отправьте заявление без электронного подписания.'");
			
		ПоместитьВоВременноеХранилище(ДополнительныеПараметры, АдресХранилища);
		
	КонецПопытки;
	
КонецПроцедуры

Процедура НачатьОтправкуОблачногоЗаявленияВБазуЗаявок(АдресХранилища, ДокументЗаявление, АлгоритмКонтейнераКлючей) Экспорт
	
	ДополнительныеПараметры = Новый Структура();
	ДополнительныеПараметры.Вставить("Выполнено", 		Ложь);
	ДополнительныеПараметры.Вставить("ДанныеЗаявления", Неопределено);
	ДополнительныеПараметры.Вставить("ОписаниеОшибки", 	"");
	
	Попытка
		
		Если ДокументЗаявление.ПодписатьЭП Тогда
			
			СвойстваЗаявления = ОбработкаЗаявленийАбонента.ПодготовитьЗаявление(ДокументЗаявление, АлгоритмКонтейнераКлючей);
			РезультатОтправки  = МенеджерСервисаКриптографии.ОтправитьПодписанноеЗаявление(СвойстваЗаявления);
			
			ДанныеЗаявления = Новый Структура;
			ДанныеЗаявления.Вставить("ДатаОтправкиЗаявления", ТекущаяДатаСеанса());
			
			Если РезультатОтправки.Выполнено Тогда
				
				Статус = ПредопределенноеЗначение("Перечисление.СтатусыЗаявленияАбонентаСпецоператораСвязи.Отправлено");
				
				ДанныеЗаявления.Вставить("ПутьКонтейнерЗакрытогоКлюча", Неопределено);
				ДанныеЗаявления.Вставить("СтатусКомментарий", 			"");
				
			Иначе
				
				Статус = ПредопределенноеЗначение("Перечисление.СтатусыЗаявленияАбонентаСпецоператораСвязи.Подготовлено");
				
				ДополнительныеПараметры.Вставить("ОписаниеОшибки", 	РезультатОтправки.ОписаниеОшибки);
				
			КонецЕсли;
			
			ДанныеЗаявления.Вставить("Статус", Статус);
			
			ДополнительныеПараметры.Вставить("ДанныеЗаявления", ДанныеЗаявления);
			ДополнительныеПараметры.Вставить("Выполнено", 		РезультатОтправки.Выполнено);
			
		Иначе
			
			ДанныеЗаявления = ОбработкаЗаявленийАбонентаВызовСервера.СформироватьИОтправитьЗаявлениеВМоделиСервиса(
				ДокументЗаявление.Ссылка, АлгоритмКонтейнераКлючей);
			ДополнительныеПараметры.Вставить("ДанныеЗаявления", ДанныеЗаявления);
			ДополнительныеПараметры.Вставить("Выполнено", 		ДанныеЗаявления.Выполнено);
			
			Если ДанныеЗаявления.Свойство("ОписаниеОшибки") Тогда
				ДополнительныеПараметры.Вставить("ОписаниеОшибки", 	ДанныеЗаявления.ОписаниеОшибки);
			КонецЕсли;
			
		КонецЕсли;
		
		ПоместитьВоВременноеХранилище(ДополнительныеПараметры, АдресХранилища);
		
	Исключение
		
		ИмяСобытия = НСтр("ru = 'Электронный документооборот с контролирующими органами. Отправка подписанного заявления'",
			ОбщегоНазначения.КодОсновногоЯзыка());
			
		Комментарий = ПодробноеПредставлениеОшибки(ИнформацияОбОшибке());
		
		ЗаписьЖурналаРегистрации(
			ИмяСобытия ,
			УровеньЖурналаРегистрации.Ошибка,,,
			Комментарий);
			
		ДополнительныеПараметры.ОписаниеОшибки = НСтр("ru = 'Сервис временно недоступен. Обратитесь в службу поддержки или повторите попытку позже.'");
			
		ПоместитьВоВременноеХранилище(ДополнительныеПараметры, АдресХранилища);
		
	КонецПопытки;
	
КонецПроцедуры

Функция ТОГСПоРегиону(КодРегиона, Спецоператор) Экспорт
	
	ТаблицаТОГС 	= ЭлектронныйДокументооборотСКонтролирующимиОрганамиВызовСервера.ТаблицаТОГС();

	Отбор = Новый Структура();
	Отбор.Вставить("КодРегиона", КодРегиона);
	
	Если ЗначениеЗаполнено(Спецоператор) Тогда
		ИмяКолонкиСпецоператора = ЭлектронныйДокументооборотСКонтролирующимиОрганамиВызовСервера.ПолучитьПараметрСпецоператора(
			Спецоператор, 
			"СпецоператорСвязи");
		Отбор.Вставить(ИмяКолонкиСпецоператора, "");
	КонецЕсли;
	
	СписокТОГСПоРегиону = ТаблицаТОГС.НайтиСтроки(Отбор);
	
	Если СписокТОГСПоРегиону.Количество() = 1 Тогда
		Возврат СписокТОГСПоРегиону[0].Код;
	Иначе
		Возврат "";
	КонецЕсли;
	
КонецФункции

Функция НаименованиеТОГСаПоКоду(КодТОГСа, Спецоператор) Экспорт
	
	ТаблицаТОГС 	= ЭлектронныйДокументооборотСКонтролирующимиОрганамиВызовСервера.ТаблицаТОГС();
	
	Отбор = Новый Структура();
	Отбор.Вставить("Код", КодТОГСа);
	
	Если ЗначениеЗаполнено(Спецоператор) Тогда
		ИмяКолонкиСпецоператора = ЭлектронныйДокументооборотСКонтролирующимиОрганамиВызовСервера.ПолучитьПараметрСпецоператора(
			Спецоператор, 
			"СпецоператорСвязи");
		Отбор.Вставить(ИмяКолонкиСпецоператора, "");
	КонецЕсли;
	
	СписокТОГСПоКоду = ТаблицаТОГС.НайтиСтроки(Отбор);
	
	Если СписокТОГСПоКоду.Количество() = 1 Тогда
		Возврат СписокТОГСПоКоду[0].Наименование;
	Иначе
		Возврат "";
	КонецЕсли;
	
КонецФункции

Процедура НовоеНаправление(Получатели, ТипПолучателя, КодПолучателя, КПП = "", Наименование = "") Экспорт
	
	Отбор = Новый Структура("ТипПолучателя, КодПолучателя, КПП",ТипПолучателя,КодПолучателя,КПП);
	
	Если Получатели.НайтиСтроки(Отбор).Количество() <> 0 Тогда 
		Возврат;
	КонецЕсли;
	
	НоваяСтрокаНаправления = Получатели.Добавить();
	НоваяСтрокаНаправления.ТипПолучателя = ТипПолучателя;
	НоваяСтрокаНаправления.КодПолучателя = КодПолучателя;
	
	Если ЗначениеЗаполнено(КПП) Тогда
		НоваяСтрокаНаправления.КПП = КПП;
	КонецЕсли;
	
	Если ЗначениеЗаполнено(Наименование) Тогда
		НоваяСтрокаНаправления.Наименование = Наименование;
	КонецЕсли;
	
КонецПроцедуры

Функция ПараметрыФормыНаправлений(Форма) Экспорт
	
	ПараметрыФормы = 
		// ФНС
		"СдаватьВФНС,
		// Росстат
		|СдаватьВРосстат,
		// ПФР
		|СдаватьВПФР,
		|РегНомерПФР,
		|КодПФР,
		// ФСС
		|СдаватьВФСС,
		// ФСРАР
		|КодРегионаФСРАР,
		|ПодатьЗаявкуНаСертификатДляФСРАР,
		// РПН
		|ПодатьЗаявкуНаПодключениеРПН,
		// ФТС
		|ПодатьЗаявкуНаПодключениеФТС,
		// Поддержка органов
		|ПодатьЗаявкуНаПодключениеФТС,
		|ПризнакПоддержкиПФР,
		|ПризнакПоддержкиРосстат,
		|ПризнакПоддержкиРПН,
		|ПризнакПоддержкиФНС,
		|ПризнакПоддержкиФСРАР,
		|ПризнакПоддержкиФСС,
		|ПризнакПоддержкиФТС,
		// Прочее
		|Организация,
		|Спецоператор,
		|ДанныеОрганизации,
		|КодРегиона,
		|ЭтоЮридическоеЛицо,
		|АдресЮридическийЗначение,
		|ВладелецЭЦПТип,
		|ИПИспользуетТрудНаемныхРаботников,
		|ПризнакОбособленногоПодразделения,
		|ЗапретитьИзменение";
		
	ДополнительныеПараметры = Новый Структура(ПараметрыФормы);
	ЗаполнитьЗначенияСвойств(ДополнительныеПараметры, Форма, ПараметрыФормы);
		
	// ФНС
	ТаблицаПолучателейФНС = Форма.РеквизитФормыВЗначение("ПолучателиФНС");
	АдресПолучателейФНС   = ПоместитьВоВременноеХранилище(ТаблицаПолучателейФНС, Новый УникальныйИдентификатор);
	ДополнительныеПараметры.Вставить("АдресПолучателейФНС", АдресПолучателейФНС);
	
	// ФСГС
	ТаблицаПолучателейФСГС = Форма.РеквизитФормыВЗначение("ПолучателиФСГС");
	АдресПолучателейФСГС   = ПоместитьВоВременноеХранилище(ТаблицаПолучателейФСГС, Новый УникальныйИдентификатор);
	ДополнительныеПараметры.Вставить("АдресПолучателейФСГС", АдресПолучателейФСГС);
	
	Возврат ДополнительныеПараметры;
	
КонецФункции

Функция РасширенныеНастройкиУказаныКорректно(
			Форма, 
			МастерДалее = Истина, 
			ВыводитьСообщения = Истина, 
			ЭтоПроверкаИзРасширенныхНастроек = Истина) Экспорт
	
	РезультатПроверкиРеквизитов = Новый Массив;
	ТекстОшибки = "";
	
	// телефон организации
	Если ЗначениеЗаполнено(СокрЛП(Форма.ТелефонОсновной)) Тогда
		
		Если ДокументооборотСКОКлиентСервер.НайденыЗапрещенныеСимволы(
				Форма.ТелефонОсновной, 
				НСтр("ru = 'Телефон организации'"), 
				"ТелефонОсновной",
				Истина,
				ТекстОшибки) Тогда
		
		РезультатПроверки = ДокументооборотСКОКлиентСервер.РезультатПроверкиРеквизитов();
		РезультатПроверки.ТекстОшибки = ТекстОшибки;
		РезультатПроверки.Реквизит    = "ПроверкаТелефонОсновной";
		РезультатПроверки.Поле        = "ТелефонОсновной";
		
		РезультатПроверкиРеквизитов.Добавить(РезультатПроверки);
			
		КонецЕсли;
		
	КонецЕсли;
	
	// Если 1С:Контрагент недоступен и это юр. лицо, тогда заполняем адрес из 1С:Контрагент'а
	// и не отображаем его в основной форме. В противном случае - отображаем в основной форме.
	Если ЭтоПроверкаИзРасширенныхНастроек И Форма.ЮрАдресВРасширенныхНастройках Тогда
		
		РезультатПроверки = ПроверитьАдресОрганизацииЗаявления(
			Форма.АдресЮридическийЗначение, 
			"АдресЮридическийЗначение", 
			НСтр("ru = 'Юридический адрес'"), 
			НСтр("ru = 'юридического адреса'"), 
			МастерДалее,
			Истина);
		РезультатПроверки.Реквизит = "ПроверкаАдресЮридическийПредставление";
		РезультатПроверкиРеквизитов.Добавить(РезультатПроверки);
			
	КонецЕсли;
		
	РезультатПроверки = ПроверитьАдресОрганизацииЗаявления(
		Форма.АдресФактическийЗначение, 
		"АдресФактическийЗначение", 
		НСтр("ru = 'Фактический адрес'"), 
		НСтр("ru = 'фактического адреса'"), 
		МастерДалее,
		Истина,
		Истина);
	РезультатПроверки.Реквизит = "ПроверкаАдресФактическийПредставление";
	РезультатПроверкиРеквизитов.Добавить(РезультатПроверки);
		
	// Указание мобильного в расширенных настройках возможно только для ЭП в облаке
	Если Форма.ПолучатьСМС И Форма.РежимРаботыСКлючами = 1 Тогда
			
		ТелефонМобильныйБезРазделителей = ЭлектронныйДокументооборотСКонтролирующимиОрганамиКлиентСервер.ТелефонМобильныйБезРазделителей(
			Форма.ТелефонМобильный);
			
		Если ПустаяСтрока(ТелефонМобильныйБезРазделителей) Тогда
			
			РезультатПроверки = ДокументооборотСКОКлиентСервер.РезультатПроверкиРеквизитов();
			РезультатПроверки.ТекстОшибки = НСтр("ru = 'Заполните мобильный телефон'");
			РезультатПроверки.Пустой      = Истина;
			РезультатПроверки.Реквизит    = "ПроверкаТелефонМобильный";
			РезультатПроверки.Поле        = "ТелефонМобильный";
			
			РезультатПроверкиРеквизитов.Добавить(РезультатПроверки);
			
		ИначеЕсли НЕ ДокументооборотСКОКлиентСервер.ПроверитьЦифровойКодЗаданнойДлины(ТелефонМобильныйБезРазделителей, 11, Истина) Тогда 
			
			РезультатПроверки = ДокументооборотСКОКлиентСервер.РезультатПроверкиРеквизитов();
			РезультатПроверки.ТекстОшибки = НСтр("ru = 'Мобильный телефон должен иметь формат 
					|+7 XXX XXX-XX-XX'");
			РезультатПроверки.Реквизит    = "ПроверкаТелефонМобильный";
			РезультатПроверки.Поле        = "ТелефонМобильный";
			
			РезультатПроверкиРеквизитов.Добавить(РезультатПроверки);
			
		ИначеЕсли ДокументооборотСКОКлиентСервер.НайденыЗапрещенныеСимволы(
				Форма.ТелефонМобильный, 
				НСтр("ru = 'Телефон мобильный'"), 
				"ТелефонМобильный",
				Истина,
				ТекстОшибки) Тогда
		
			РезультатПроверки = ДокументооборотСКОКлиентСервер.РезультатПроверкиРеквизитов();
			РезультатПроверки.ТекстОшибки = ТекстОшибки;
			РезультатПроверки.Реквизит    = "ПроверкаТелефонМобильный";
			РезультатПроверки.Поле        = "ТелефонМобильный";
			
			РезультатПроверкиРеквизитов.Добавить(РезультатПроверки);
			
		КонецЕсли;
		
	КонецЕсли;
	
	// Электронная почта
	// В расширенных настройках отображается только если эп локальная и включен сертификат.
	// Для облачного ключа здесь почту не показываем, т.к. ее нельзя менять, поскольку она уже задействована в амторизации
	Если Форма.РежимРаботыСКлючами <> 1 И ОбработкаЗаявленийАбонентаКлиентСервер.ИспользоватьСуществующий(Форма) Тогда
	
		Форма.ЭлектроннаяПочтаДляПаролей = СокрЛП(Форма.ЭлектроннаяПочтаДляПаролей);
		
		Если ПустаяСтрока(Форма.ЭлектроннаяПочтаДляПаролей) Тогда
			
			РезультатПроверки = ДокументооборотСКОКлиентСервер.РезультатПроверкиРеквизитов();
			РезультатПроверки.ТекстОшибки = НСтр("ru='Заполните адрес электронной почты'");
			РезультатПроверки.Реквизит    = "ПроверкаЭлектроннойПочты";
			РезультатПроверки.Поле        = "ЭлектроннаяПочтаДляПаролей";
			РезультатПроверки.Пустой	  = Истина;
			
			РезультатПроверкиРеквизитов.Добавить(РезультатПроверки);
			
		ИначеЕсли ДокументооборотСКОКлиентСервер.НайденыЗапрещенныеСимволы(
				Форма.ЭлектроннаяПочтаДляПаролей, 
				НСтр("ru = 'Электронная почта '"),
				"ЭлектроннаяПочтаДляПаролей",
				Истина,
				ТекстОшибки) Тогда
				
			РезультатПроверки = ДокументооборотСКОКлиентСервер.РезультатПроверкиРеквизитов();
			РезультатПроверки.ТекстОшибки = ТекстОшибки;
			РезультатПроверки.Реквизит    = "ПроверкаЭлектроннойПочты";
			РезультатПроверки.Поле        = "ЭлектроннаяПочтаДляПаролей";
			
			РезультатПроверкиРеквизитов.Добавить(РезультатПроверки);
			
		ИначеЕсли НЕ ОбщегоНазначенияКлиентСервер.АдресЭлектроннойПочтыСоответствуетТребованиям(Форма.ЭлектроннаяПочтаДляПаролей) Тогда
			
			РезультатПроверки = ДокументооборотСКОКлиентСервер.РезультатПроверкиРеквизитов();
			Если НЕ СтрНайти(Форма.ЭлектроннаяПочтаДляПаролей, "@") Тогда
				РезультатПроверки.ТекстОшибки = НСтр("ru = 'Некорректно указана электронная почта. Отсутствует символ @'");
			Иначе 
				РезультатПроверки.ТекстОшибки = НСтр("ru = 'Электронная почта содержит некорректные сочетания символов'");
			КонецЕсли;
			
			РезультатПроверки.Реквизит    = "ПроверкаЭлектроннойПочты";
			РезультатПроверки.Поле        = "ЭлектроннаяПочтаДляПаролей";
			
			РезультатПроверкиРеквизитов.Добавить(РезультатПроверки);
				
		КонецЕсли;
		
	КонецЕсли;	

	// Должность
	Если Форма.ЭтоЮридическоеЛицо И Форма.Доступен1СКонтрагент 
		И Форма.ВладелецЭЦПТип = ПредопределенноеЗначение("Перечисление.ТипыВладельцевЭЦП.Руководитель") Тогда
		
		// Если 1С:Контрагент не доступен или выбран не руководитель, то Должность заполняется на основной форме
		
		РезультатПроверки = ПроверитьДолжностьВУпрощенномЗаявлении(Форма, МастерДалее, ВыводитьСообщения);
		РезультатПроверки.Реквизит = "ПроверкаВладелецЭЦПДолжность";
		РезультатПроверки.Поле     = "ВладелецЭЦПДолжность";
		
		РезультатПроверкиРеквизитов.Добавить(РезультатПроверки);
			
	КонецЕсли;
	
	// Подразделение
	Если ДокументооборотСКОКлиентСервер.НайденыЗапрещенныеСимволы(
			Форма.ВладелецЭЦППодразделение, 
			НСтр("ru = 'Подразделение'"), 
			"ВладелецЭЦППодразделение",
			Истина,
			ТекстОшибки) Тогда
			
		РезультатПроверки = ДокументооборотСКОКлиентСервер.РезультатПроверкиРеквизитов();
		РезультатПроверки.ТекстОшибки = ТекстОшибки;
		РезультатПроверки.Реквизит    = "ПроверкаВладелецЭЦППодразделение";
		РезультатПроверки.Поле        = "ВладелецЭЦППодразделение";
		
		РезультатПроверкиРеквизитов.Добавить(РезультатПроверки);
		
	КонецЕсли;
	
	// Рег. номер
	Если Форма.РегНомерВРасширенныхНастройках Тогда
		Если ПустаяСтрока(Форма.НомерОсновнойПоставки1с) Тогда
			
			РезультатПроверки.ТекстОшибки = НСтр("ru = 'Заполните регистрационный номер программы'");
			РезультатПроверки.Пустой	  = Истина;
			РезультатПроверки.Реквизит    = "ПроверкаНомерОсновнойПоставки1с";
			РезультатПроверки.Поле        = "НомерОсновнойПоставки1с";
				
		ИначеЕсли ДокументооборотСКОКлиентСервер.НайденыЗапрещенныеСимволы(
				Форма.НомерОсновнойПоставки1с, 
				НСтр("ru = 'Регистрационный номер программы'"), 
				"НомерОсновнойПоставки1с", 
				Истина, 
				ТекстОшибки) Тогда
				
			РезультатПроверки = ДокументооборотСКОКлиентСервер.РезультатПроверкиРеквизитов();
			РезультатПроверки.ТекстОшибки = ТекстОшибки;
			РезультатПроверки.Реквизит    = "ПроверкаНомерОсновнойПоставки1с";
			РезультатПроверки.Поле        = "НомерОсновнойПоставки1с";
			
			РезультатПроверкиРеквизитов.Добавить(РезультатПроверки);
			
		КонецЕсли;
	КонецЕсли;

	Для каждого РезультатПроверки Из РезультатПроверкиРеквизитов Цикл
		ДокументооборотСКОКлиентСервер.ВывестиОшибкуПроверкиРеквизита(МастерДалее, РезультатПроверки, ВыводитьСообщения);
	КонецЦикла;
	
	Возврат РезультатПроверкиРеквизитов;
	
КонецФункции

Функция ПаспортныеДанныеУказаныКорректно(Форма, МастерДалее = Истина, ВыводитьСообщения = Истина) Экспорт
	
	ЭтоПаспортРФ = ЭтоПаспортРФ(Форма.ВладелецЭЦПВидДокумента);
	
	РезультатПроверкиРеквизитов = Новый Массив;
	ТекстОшибки = "";
	
	Если ЭтоПаспортРФ Тогда
		Реквизит = "ПроверкаВладелецЭЦПВидДокумента";
	Иначе
		Реквизит = "ПроверкаВладелецЭЦПВидДокумента1";
	Конецесли;
		
	Поле = "ВладелецЭЦПВидДокумента";
	
	// документ, удостоверяющий личность
	Если ПустаяСтрока(Форма.ВладелецЭЦПВидДокумента) Тогда
		
		РезультатПроверки = ДокументооборотСКОКлиентСервер.РезультатПроверкиРеквизитов();
		РезультатПроверки.ТекстОшибки = НСтр("ru = 'Заполните поле ""Вид документа""'");
		РезультатПроверки.Пустой      = Истина;
		РезультатПроверки.Реквизит    = Реквизит;
		РезультатПроверки.Поле        = Поле;
		
		РезультатПроверкиРеквизитов.Добавить(РезультатПроверки);
		
	ИначеЕсли ДокументооборотСКОКлиентСервер.НайденыЗапрещенныеСимволы(
			Форма.ВладелецЭЦПВидДокумента,
			НСтр("ru = 'Вид документа'"),
			Поле,
			Истина,
			ТекстОшибки) Тогда
			
		РезультатПроверки = ДокументооборотСКОКлиентСервер.РезультатПроверкиРеквизитов();
		РезультатПроверки.ТекстОшибки = ТекстОшибки;
		РезультатПроверки.Реквизит    = Реквизит;
		РезультатПроверки.Поле        = Поле;
		
		РезультатПроверкиРеквизитов.Добавить(РезультатПроверки);
		
	ИначеЕсли НЕ ЗначениеЗаполнено(ПолучитьКодВидаДокументаФизическогоЛица(Форма.ВладелецЭЦПВидДокумента)) Тогда
		
		ТекстОшибки = НСтр("ru = 'Заполните код ИФНС в виде документа ""%1""'");
		ТекстОшибки = СтрШаблон(ТекстОшибки, Строка(Форма.ВладелецЭЦПВидДокумента));
		
		РезультатПроверки = ДокументооборотСКОКлиентСервер.РезультатПроверкиРеквизитов();
		РезультатПроверки.ТекстОшибки = ТекстОшибки;
		РезультатПроверки.Реквизит    = Реквизит;
		РезультатПроверки.Поле        = Поле;
		
		РезультатПроверкиРеквизитов.Добавить(РезультатПроверки);
		
	КонецЕсли;
	
	// серия
	
	Если ЭтоПаспортРФ Тогда
		Реквизит = "ПроверкаВладелецЭЦПСерияДокумента";
	Иначе
		Реквизит = "ПроверкаВладелецЭЦПСерияДокумента1";
	КонецЕсли;
	
	Поле = "ВладелецЭЦПСерияДокумента";
	ВладелецЭЦПСерияДокументаБезРезделителя = ДокументооборотСКОКлиентСервер.УдалитьРазделители(Форма.ВладелецЭЦПСерияДокумента);
	Если ПустаяСтрока(Форма.ВладелецЭЦПСерияДокумента) И ЭтоПаспортРФ Тогда
		
		РезультатПроверки = ДокументооборотСКОКлиентСервер.РезультатПроверкиРеквизитов();
		РезультатПроверки.ТекстОшибки = НСтр("ru = 'Заполните поле ""Серия""'");
		РезультатПроверки.Пустой      = Истина;
		РезультатПроверки.Реквизит    = Реквизит;
		РезультатПроверки.Поле        = Поле;
		
		РезультатПроверкиРеквизитов.Добавить(РезультатПроверки);
		
	ИначеЕсли ДокументооборотСКОКлиентСервер.НайденыЗапрещенныеСимволы(
			Форма.ВладелецЭЦПСерияДокумента, 
			НСтр("ru = 'Серия'"), 
			Поле,
			Истина,
			ТекстОшибки) Тогда
			
		РезультатПроверки = ДокументооборотСКОКлиентСервер.РезультатПроверкиРеквизитов();
		РезультатПроверки.ТекстОшибки = ТекстОшибки;
		РезультатПроверки.Реквизит    = Реквизит;
		РезультатПроверки.Поле        = Поле;
		
		РезультатПроверкиРеквизитов.Добавить(РезультатПроверки);
		
	ИначеЕсли ЭтоПаспортРФ И НЕ ДокументооборотСКОКлиентСервер.ПроверитьЦифровойКодЗаданнойДлины(ВладелецЭЦПСерияДокументаБезРезделителя, 4, Истина) Тогда
		
		РезультатПроверки = ДокументооборотСКОКлиентСервер.РезультатПроверкиРеквизитов();
		РезультатПроверки.ТекстОшибки = НСтр("ru = 'Серия паспорта должна состоять из 4 цифр'");
		РезультатПроверки.Реквизит    = Реквизит;
		РезультатПроверки.Поле        = Поле;
		
		РезультатПроверкиРеквизитов.Добавить(РезультатПроверки);
		
	КонецЕсли;
	
	// номер
	
	Если ЭтоПаспортРФ Тогда
		Реквизит = "ПроверкаВладелецЭЦПНомерДокумента";
	Иначе
		Реквизит = "ПроверкаВладелецЭЦПНомерДокумента1";
	КонецЕсли;
	
	Поле = "ВладелецЭЦПНомерДокумента";
	
	Если ПустаяСтрока(Форма.ВладелецЭЦПНомерДокумента) И ЭтоПаспортРФ Тогда
		
		РезультатПроверки = ДокументооборотСКОКлиентСервер.РезультатПроверкиРеквизитов();
		РезультатПроверки.ТекстОшибки = НСтр("ru = 'Заполните поле ""Номер""'");
		РезультатПроверки.Пустой      = Истина;
		РезультатПроверки.Реквизит    = Реквизит;
		РезультатПроверки.Поле        = Поле;
		
		РезультатПроверкиРеквизитов.Добавить(РезультатПроверки);
		
	ИначеЕсли ДокументооборотСКОКлиентСервер.НайденыЗапрещенныеСимволы(
			Форма.ВладелецЭЦПНомерДокумента, 
			НСтр("ru = 'Номер'"), 
			Поле,
			Истина,
			ТекстОшибки) Тогда
			
		РезультатПроверки = ДокументооборотСКОКлиентСервер.РезультатПроверкиРеквизитов();
		РезультатПроверки.ТекстОшибки = ТекстОшибки;
		РезультатПроверки.Реквизит    = Реквизит;
		РезультатПроверки.Поле        = Поле;
		
		РезультатПроверкиРеквизитов.Добавить(РезультатПроверки);
		
	ИначеЕсли ЭтоПаспортРФ И НЕ ДокументооборотСКОКлиентСервер.ПроверитьЦифровойКодЗаданнойДлины(Форма.ВладелецЭЦПНомерДокумента, 6, Истина) Тогда
		
		РезультатПроверки = ДокументооборотСКОКлиентСервер.РезультатПроверкиРеквизитов();
		РезультатПроверки.ТекстОшибки = НСтр("ru = 'Номер паспорта должен состоять из 6 цифр'");
		РезультатПроверки.Реквизит    = Реквизит;
		РезультатПроверки.Поле        = Поле;
		
		РезультатПроверкиРеквизитов.Добавить(РезультатПроверки);
	
	КонецЕсли;
	
	Поле = "ВладелецЭЦПСерияДокумента";
	Если НЕ ЭтоПаспортРФ И ПустаяСтрока(Форма.ВладелецЭЦПСерияДокумента) И ПустаяСтрока(Форма.ВладелецЭЦПНомерДокумента) Тогда
		
		РезультатПроверки = ДокументооборотСКОКлиентСервер.РезультатПроверкиРеквизитов();
		РезультатПроверки.ТекстОшибки = НСтр("ru = 'Заполните хотя бы одно из полей ""Серия"" или ""Номер""'");
		РезультатПроверки.Пустой      = Истина;
		РезультатПроверки.Реквизит    = Реквизит;
		РезультатПроверки.Поле        = Поле;
		
		РезультатПроверкиРеквизитов.Добавить(РезультатПроверки);	
		
	КонецЕсли;
	
	// кем выдан
	
	Если ЭтоПаспортРФ Тогда
		Реквизит = "ПроверкаВладелецЭЦПКемВыданДокумент";
	Иначе
		Реквизит = "ПроверкаВладелецЭЦПКемВыданДокумент1";
	КонецЕсли;
	
	Поле = "ВладелецЭЦПКемВыданДокумент";
	
	Если ПустаяСтрока(Форма.ВладелецЭЦПКемВыданДокумент) Тогда
		
		РезультатПроверки = ДокументооборотСКОКлиентСервер.РезультатПроверкиРеквизитов();
		РезультатПроверки.ТекстОшибки = НСтр("ru = 'Заполните поле ""Документ выдан""'");
		РезультатПроверки.Пустой      = Истина;
		РезультатПроверки.Реквизит    = Реквизит;
		РезультатПроверки.Поле        = Поле;
		
		РезультатПроверкиРеквизитов.Добавить(РезультатПроверки);
		
	ИначеЕсли ДокументооборотСКОКлиентСервер.НайденыЗапрещенныеСимволы(
			Форма.ВладелецЭЦПКемВыданДокумент, 
			НСтр("ru = 'Документ выдан'"), 
			Поле,
			Истина,
			ТекстОшибки) Тогда
			
		РезультатПроверки = ДокументооборотСКОКлиентСервер.РезультатПроверкиРеквизитов();
		РезультатПроверки.ТекстОшибки = ТекстОшибки;
		РезультатПроверки.Реквизит    = Реквизит;
		РезультатПроверки.Поле        = Поле;
		
		РезультатПроверкиРеквизитов.Добавить(РезультатПроверки);
		
	КонецЕсли;
	
	// дата выдачи
	
	Если ЭтоПаспортРФ Тогда
		Реквизит = "ПроверкаВладелецЭЦПДатаВыдачиДокумента";
	Иначе
		Реквизит = "ПроверкаВладелецЭЦПДатаВыдачиДокумента1";
	КонецЕсли;
	
	Поле = "ВладелецЭЦПДатаВыдачиДокумента";
	
	Если НЕ ЗначениеЗаполнено(Форма.ВладелецЭЦПДатаВыдачиДокумента) Тогда
		
		РезультатПроверки = ДокументооборотСКОКлиентСервер.РезультатПроверкиРеквизитов();
		РезультатПроверки.ТекстОшибки = НСтр("ru = 'Заполните поле ""Дата выдачи""'");
		РезультатПроверки.Пустой      = Истина;
		РезультатПроверки.Реквизит    = Реквизит;
		РезультатПроверки.Поле        = Поле;
		
		РезультатПроверкиРеквизитов.Добавить(РезультатПроверки);
		
	ИначеЕсли Форма.ВладелецЭЦПДатаВыдачиДокумента > ТекущаяДатаСеанса() ИЛИ Год(Форма.ВладелецЭЦПДатаВыдачиДокумента) < 1900 Тогда
		
		РезультатПроверки = ДокументооборотСКОКлиентСервер.РезультатПроверкиРеквизитов();
		РезультатПроверки.ТекстОшибки = НСтр("ru = 'Некорректно указана дата выдачи'");
		РезультатПроверки.Реквизит    = Реквизит;
		РезультатПроверки.Поле        = Поле;
		
		РезультатПроверкиРеквизитов.Добавить(РезультатПроверки);
		
	КонецЕсли;
	
	// Код подразделения. Только для паспорта РФ.
	
	Если ЭтоПаспортРФ Тогда
		
		Реквизит = "ПроверкаВладелецЭЦПКодПодразделения";
		Поле     = "ВладелецЭЦПКодПодразделения";
		
		КодПодразделенияБезТире = СтрЗаменить(Форма.ВладелецЭЦПКодПодразделения, "-","");
		Если ПустаяСтрока(КодПодразделенияБезТире) Тогда
			
			РезультатПроверки = ДокументооборотСКОКлиентСервер.РезультатПроверкиРеквизитов();
			РезультатПроверки.ТекстОшибки = НСтр("ru = 'Заполните поле ""Код подразделения""'");
			РезультатПроверки.Пустой      = Истина;
			РезультатПроверки.Реквизит    = Реквизит;
			РезультатПроверки.Поле        = Поле;
			
			РезультатПроверкиРеквизитов.Добавить(РезультатПроверки);
			
		ИначеЕсли СтрДлина(Форма.ВладелецЭЦПКодПодразделения) = 6
			И ДокументооборотСКОКлиентСервер.ПроверитьЦифровойКодЗаданнойДлины(КодПодразделенияБезТире, 6, Истина) Тогда
			
			// Не ошибка. Написан без тире. Исправляем сами.
			Форма.ВладелецЭЦПКодПодразделения = 
				Лев(Форма.ВладелецЭЦПКодПодразделения, 3)
				+ "-"
				+ Прав(Форма.ВладелецЭЦПКодПодразделения, 3);
			
		ИначеЕсли СтрДлина(Форма.ВладелецЭЦПКодПодразделения) <> 7
			ИЛИ НЕ ДокументооборотСКОКлиентСервер.ПроверитьЦифровойКодЗаданнойДлины(КодПодразделенияБезТире, 6, Истина) Тогда
			
			РезультатПроверки = ДокументооборотСКОКлиентСервер.РезультатПроверкиРеквизитов();
			РезультатПроверки.ТекстОшибки = НСтр("ru = 'Код подразделения не соответствует маске ХХХ-ХХХ, где X – любая цифра'");
			РезультатПроверки.Реквизит    = Реквизит;
			РезультатПроверки.Поле        = Поле;
			
			РезультатПроверкиРеквизитов.Добавить(РезультатПроверки);
			
		КонецЕсли;
	КонецЕсли;

	// Пол
	Если ЭтоПаспортРФ Тогда
		Реквизит = "ПроверкаВладелецЭЦППол";
	Иначе
		Реквизит = "ПроверкаВладелецЭЦППол1";
	КонецЕсли;
	
	Поле = "ВладелецЭЦППол";
	
	Если НЕ ЗначениеЗаполнено(Форма.ВладелецЭЦППол) Тогда
		
		РезультатПроверки = ДокументооборотСКОКлиентСервер.РезультатПроверкиРеквизитов();
		РезультатПроверки.ТекстОшибки = НСтр("ru = 'Заполните поле ""Пол""'");
		РезультатПроверки.Пустой      = Истина;
		РезультатПроверки.Реквизит    = Реквизит;
		РезультатПроверки.Поле        = Поле;
		
		РезультатПроверкиРеквизитов.Добавить(РезультатПроверки);
		
	КонецЕсли;

	// Дата рождения
	Если ЭтоПаспортРФ Тогда
		Реквизит = "ПроверкаВладелецЭЦПДатаРождения";
	Иначе
		Реквизит = "ПроверкаВладелецЭЦПДатаРождения1";
	КонецЕсли;
	Поле     = "ВладелецЭЦПДатаРождения";
	
	Если Не ЗначениеЗаполнено(Форма.ВладелецЭЦПДатаРождения) Тогда
		
		РезультатПроверки = ДокументооборотСКОКлиентСервер.РезультатПроверкиРеквизитов();
		РезультатПроверки.ТекстОшибки = НСтр("ru = 'Заполните поле ""Дата рождения""'");
		РезультатПроверки.Пустой      = Истина;
		РезультатПроверки.Реквизит    = Реквизит;
		РезультатПроверки.Поле        = Поле;
		
		РезультатПроверкиРеквизитов.Добавить(РезультатПроверки);
		
	ИначеЕсли Год(Форма.ВладелецЭЦПДатаРождения) = Год(ТекущаяДатаСеанса())
		ИЛИ Форма.ВладелецЭЦПДатаРождения > ТекущаяДатаСеанса()
		ИЛИ Год(Форма.ВладелецЭЦПДатаРождения) < 1900 Тогда
		
		РезультатПроверки = ДокументооборотСКОКлиентСервер.РезультатПроверкиРеквизитов();
		РезультатПроверки.ТекстОшибки = НСтр("ru = 'Указано некорректное значение в поле ""Дата рождения""'");
		РезультатПроверки.Реквизит    = Реквизит;
		РезультатПроверки.Поле        = Поле;
		
		РезультатПроверкиРеквизитов.Добавить(РезультатПроверки);

	КонецЕсли;
	
	// Место рождения
	Если ЭтоПаспортРФ Тогда
		Реквизит = "ПроверкаВладелецЭЦПМестоРождения";
	Иначе
		Реквизит = "ПроверкаВладелецЭЦПМестоРождения1";
	КонецЕсли;
	
	Поле = "ВладелецЭЦПМестоРождения";
	
	Если ПустаяСтрока(Форма.ВладелецЭЦПМестоРождения) Тогда
		
		РезультатПроверки = ДокументооборотСКОКлиентСервер.РезультатПроверкиРеквизитов();
		РезультатПроверки.ТекстОшибки = НСтр("ru = 'Заполните поле ""Место рождения""'");
		РезультатПроверки.Пустой      = Истина;
		РезультатПроверки.Реквизит    = Реквизит;
		РезультатПроверки.Поле        = Поле;
		
		РезультатПроверкиРеквизитов.Добавить(РезультатПроверки);
		
	ИначеЕсли ДокументооборотСКОКлиентСервер.НайденыЗапрещенныеСимволы(
			Форма.ВладелецЭЦПКемВыданДокумент, 
			НСтр("ru = 'Документ выдан'"), 
			Поле,
			Истина,
			ТекстОшибки) Тогда
			
		РезультатПроверки = ДокументооборотСКОКлиентСервер.РезультатПроверкиРеквизитов();
		РезультатПроверки.ТекстОшибки = ТекстОшибки;
		РезультатПроверки.Реквизит    = Реквизит;
		РезультатПроверки.Поле        = Поле;
		
		РезультатПроверкиРеквизитов.Добавить(РезультатПроверки);

	КонецЕсли;
	
	// Гражданство
	
	Если ЭтоПаспортРФ Тогда
		Реквизит = "ПроверкаВладелецЭЦПГражданство";
	Иначе
		Реквизит = "ПроверкаВладелецЭЦПГражданство1";
	Конецесли;
	Поле = "ВладелецЭЦПГражданство";
	
	Если НЕ ЗначениеЗаполнено(Форма.ВладелецЭЦПГражданство) Тогда
		
		РезультатПроверки = ДокументооборотСКОКлиентСервер.РезультатПроверкиРеквизитов();
		РезультатПроверки.ТекстОшибки = НСтр("ru = 'Заполните поле ""Гражданство""'");
		РезультатПроверки.Пустой      = Истина;
		РезультатПроверки.Реквизит    = Реквизит;
		РезультатПроверки.Поле        = Поле;
		
		РезультатПроверкиРеквизитов.Добавить(РезультатПроверки);
		
	ИначеЕсли НЕ ЭлектронныйДокументооборотСКонтролирующимиОрганамиВызовСервера.КодАльфа2Заполнен(Форма.ВладелецЭЦПГражданство)
		И Форма.ВладелецЭЦПГражданство <> ОбщегоНазначения.ПредопределенныйЭлемент("Справочник.СтраныМира.Россия") Тогда
		
		РезультатПроверки = ДокументооборотСКОКлиентСервер.РезультатПроверкиРеквизитов();
		РезультатПроверки.ТекстОшибки = НСтр("ru = 'Заполните код альфа-2 у страны, указанной в качестве гражданства сотрудника-владельца ЭП'");
		РезультатПроверки.Реквизит    = Реквизит;
		РезультатПроверки.Поле        = Поле;
		
		РезультатПроверкиРеквизитов.Добавить(РезультатПроверки);
		
	КонецЕсли;

	Для каждого РезультатПроверки Из РезультатПроверкиРеквизитов Цикл
		ДокументооборотСКОКлиентСервер.ВывестиОшибкуПроверкиРеквизита(МастерДалее, РезультатПроверки, ВыводитьСообщения);
	КонецЦикла;
	
	Возврат РезультатПроверкиРеквизитов;
	
КонецФункции

Функция ПроверитьДолжностьВУпрощенномЗаявлении(Форма, МастерДалее = Истина, ВыводитьСообщения = Истина, ТекстОшибки = "") Экспорт
	
	РезультатПроверки = ДокументооборотСКОКлиентСервер.РезультатПроверкиРеквизитов();
	РезультатПроверки.Поле = "ВладелецЭЦПДолжность";
	
	Если Форма.ЭтоЮридическоеЛицо Тогда
		
		Если ПустаяСтрока(Форма.ВладелецЭЦПДолжность) Тогда
			
			РезультатПроверки.ТекстОшибки = НСтр("ru = 'Заполните поле ""Должность""'");
			РезультатПроверки.Пустой	  = Истина;
			
		ИначеЕсли ДокументооборотСКОКлиентСервер.НайденыЗапрещенныеСимволы(
				Форма.ВладелецЭЦПДолжность, 
				НСтр("ru = 'Должность'"), 
				"ВладелецЭЦПДолжность",
				Истина,
				ТекстОшибки) Тогда
			
			РезультатПроверки.ТекстОшибки = ТекстОшибки;
			
		КонецЕсли;
		
	КонецЕсли;
	
	ДокументооборотСКОКлиентСервер.ВывестиОшибкуПроверкиРеквизита(МастерДалее, РезультатПроверки, ВыводитьСообщения);
	Возврат РезультатПроверки;
		
КонецФункции

Функция НаправленияУказаныКорректно(Форма, МастерДалее = Истина, ВыводитьСообщения = Истина) Экспорт
	
	РезультатПроверкиРеквизитов = Новый Массив;
	ТекстОшибки = "";
	
	// ФНС ////////////////////////////////////////////////////////////////////////
	
	Реквизит = "ПроверкаСдаватьВФНС";
	Поле     = "ПолучателиФНС";
	
	Если Форма.ПризнакПоддержкиФНС И Форма.СдаватьВФНС Тогда
		
		Если Форма.ПолучателиФНС.Количество()=0 Тогда
			
			РезультатПроверки = ДокументооборотСКОКлиентСервер.РезультатПроверкиРеквизитов();
			РезультатПроверки.ТекстОшибки = НСтр("ru = 'Заполните коды ФНС'");
			РезультатПроверки.Реквизит    = Реквизит;
			РезультатПроверки.Поле        = Поле;
			
			РезультатПроверкиРеквизитов.Добавить(РезультатПроверки);
			
		Иначе
				
			Для каждого Получатель Из Форма.ПолучателиФНС Цикл
				
				Если НЕ ЗначениеЗаполнено(Получатель.КодПолучателя) Тогда
					
					РезультатПроверки = ДокументооборотСКОКлиентСервер.РезультатПроверкиРеквизитов();
					РезультатПроверки.ТекстОшибки = НСтр("ru = 'Заполните код ИФНС'");
					РезультатПроверки.Реквизит    = Реквизит;
					РезультатПроверки.Поле        = Поле;
					
					РезультатПроверкиРеквизитов.Добавить(РезультатПроверки);
					
				КонецЕсли;
				
				Если Форма.ЭтоЮридическоеЛицо И НЕ ЗначениеЗаполнено(Получатель.КПП) Тогда
					
					РезультатПроверки = ДокументооборотСКОКлиентСервер.РезультатПроверкиРеквизитов();
					РезультатПроверки.ТекстОшибки = НСтр("ru = 'Заполните КПП'");
					РезультатПроверки.Реквизит    = Реквизит;
					РезультатПроверки.Поле        = Поле;
					
					РезультатПроверкиРеквизитов.Добавить(РезультатПроверки);
					
				КонецЕсли;
				
				Если ДокументооборотСКОКлиентСервер.НайденыЗапрещенныеСимволы(
						Получатель.КПП, 
						НСтр("ru = 'КПП'"), 
						"ПолучателиФНС", 
						Истина,
						ТекстОшибки) Тогда
						
					РезультатПроверки = ДокументооборотСКОКлиентСервер.РезультатПроверкиРеквизитов();
					РезультатПроверки.ТекстОшибки = ТекстОшибки;
					РезультатПроверки.Реквизит    = Реквизит;
					РезультатПроверки.Поле        = Поле;
					
					РезультатПроверкиРеквизитов.Добавить(РезультатПроверки);
					
				КонецЕсли;
				
				Если ДокументооборотСКОКлиентСервер.НайденыЗапрещенныеСимволы(
						Получатель.КодПолучателя, 
						НСтр("ru = 'Код ФНС'"), 
						"ПолучателиФНС", 
						Истина,
						ТекстОшибки) Тогда
					
					РезультатПроверки = ДокументооборотСКОКлиентСервер.РезультатПроверкиРеквизитов();
					РезультатПроверки.ТекстОшибки = ТекстОшибки;
					РезультатПроверки.Реквизит    = Реквизит;
					РезультатПроверки.Поле        = Поле;
					
					РезультатПроверкиРеквизитов.Добавить(РезультатПроверки);
					
				КонецЕсли;
				
				Если Форма.ЭтоЮридическоеЛицо И ЗначениеЗаполнено(Получатель.КПП) 
					И НЕ ДокументооборотСКОКлиентСервер.ПроверитьКПП(Получатель.КПП) Тогда
					
					РезультатПроверки = ДокументооборотСКОКлиентСервер.РезультатПроверкиРеквизитов();
					РезультатПроверки.ТекстОшибки = НСтр("ru = 'Некорректно заполнен КПП.'");
					РезультатПроверки.Реквизит    = Реквизит;
					РезультатПроверки.Поле        = Поле;
					
					РезультатПроверкиРеквизитов.Добавить(РезультатПроверки);
					
				КонецЕсли;
				
			КонецЦикла; 
		КонецЕсли;
		
	КонецЕсли;
	
	// Росстат ////////////////////////////////////////////////////////////////////////
	
	Реквизит = "ПроверкаСдаватьВРосстат";
	Поле     = "ПолучателиФСГС";
	
	Если Форма.ПризнакПоддержкиРосстат И Форма.СдаватьВРосстат Тогда
		
		Если Форма.ПолучателиФСГС.Количество() = 0 Тогда
			
			РезультатПроверки = ДокументооборотСКОКлиентСервер.РезультатПроверкиРеквизитов();
			РезультатПроверки.ТекстОшибки = НСтр("ru = 'Заполните коды органа Росстата'");
			РезультатПроверки.Реквизит    = Реквизит;
			РезультатПроверки.Поле        = Поле;
			
			РезультатПроверкиРеквизитов.Добавить(РезультатПроверки);

		ИначеЕсли Форма.ПолучателиФСГС.Количество() > 0 Тогда
			
			Для каждого Получатель Из Форма.ПолучателиФСГС Цикл
				
				Если ПустаяСтрока(СтрЗаменить(Получатель.КодПолучателя,"-","")) Тогда
					
					РезультатПроверки = ДокументооборотСКОКлиентСервер.РезультатПроверкиРеквизитов();
					РезультатПроверки.ТекстОшибки = НСтр("ru = 'Заполните поле ""Код органа Росстата""'");
					РезультатПроверки.Реквизит    = Реквизит;
					РезультатПроверки.Поле        = Поле;
					
					РезультатПроверкиРеквизитов.Добавить(РезультатПроверки);
					
				ИначеЕсли СтрДлина(СокрЛП(Получатель.КодПолучателя)) < 4 Тогда
					
					РезультатПроверки = ДокументооборотСКОКлиентСервер.РезультатПроверкиРеквизитов();
					РезультатПроверки.ТекстОшибки = НСтр("ru = 'Код органа Росстата должен состоять как минимум из 4 цифр'");
					РезультатПроверки.Реквизит    = Реквизит;
					РезультатПроверки.Поле        = Поле;
					
					РезультатПроверкиРеквизитов.Добавить(РезультатПроверки);

				ИначеЕсли ДокументооборотСКОКлиентСервер.НайденыЗапрещенныеСимволы(
						Получатель.КодПолучателя, 
						НСтр("ru = 'Код органа Росстата'"), 
						"ПолучателиФСГС", 
						Истина,
						ТекстОшибки) Тогда
					
					РезультатПроверки = ДокументооборотСКОКлиентСервер.РезультатПроверкиРеквизитов();
					РезультатПроверки.ТекстОшибки = ТекстОшибки;
					РезультатПроверки.Реквизит    = Реквизит;
					РезультатПроверки.Поле        = Поле;
					
					РезультатПроверкиРеквизитов.Добавить(РезультатПроверки);
					
				КонецЕсли;
			
			КонецЦикла;
			
		КонецЕсли;
	
	КонецЕсли;
	
	// ПФР ////////////////////////////////////////////////////////////////////////
	
	Форма.РегНомерПФР = СокрЛП(Форма.РегНомерПФР);
	Форма.КодПФР = СокрЛП(Форма.КодПФР);
		
	Если Форма.ПризнакПоддержкиПФР И Форма.СдаватьВПФР Тогда
		
		// ПФР
		Форма.РегНомерПФР = СокрЛП(Форма.РегНомерПФР);
		Форма.КодПФР = СокрЛП(Форма.КодПФР);
			
		Если Форма.ПризнакПоддержкиПФР И Форма.СдаватьВПФР Тогда
			
			// Рег. номер ПФР
			РегНомерПФРБезТире = ДокументооборотСКОКлиентСервер.УдалитьРазделители(Форма.РегНомерПФР);
			РегНомерПФРПустой  = ПустаяСтрока(СтрЗаменить(РегНомерПФРБезТире,"-",""));
			
			Если РегНомерПФРПустой Тогда 
				
				РезультатПроверки = ДокументооборотСКОКлиентСервер.РезультатПроверкиРеквизитов();
				РезультатПроверки.ТекстОшибки = НСтр("ru = 'Заполните регистрационный номер в ПФР'");
				РезультатПроверки.Пустой      = Истина;
				РезультатПроверки.Реквизит    = "ПроверкаРегНомерПФР";
				РезультатПроверки.Поле        = "РегНомерПФР";
				
				РезультатПроверкиРеквизитов.Добавить(РезультатПроверки);
			
			ИначеЕсли СтрДлина(РегНомерПФРБезТире) >= 12 
				И ДокументооборотСКОКлиентСервер.ПроверитьЦифровойКодЗаданнойДлины(РегНомерПФРБезТире, СтрДлина(РегНомерПФРБезТире), Истина) Тогда
				
				// Не ошибка. Написан без тире. Исправляем сами.
				Форма.РегНомерПФР = 
					Лев(РегНомерПФРБезТире, 3)
					+ "-"
					+ Сред(РегНомерПФРБезТире, 4, 3)
					+ "-"
					+ Сред(РегНомерПФРБезТире, 7, 6);

			ИначеЕсли НЕ ДокументооборотСКОКлиентСервер.ПроверитьРегистрационныйНомерПФР(Форма.РегНомерПФР, Истина) Тогда
				
				РезультатПроверки = ДокументооборотСКОКлиентСервер.РезультатПроверкиРеквизитов();
				РезультатПроверки.ТекстОшибки = НСтр("ru = 'Регистрационный номер в ПФР должен состоять из 12 цифр (ХХХ-ХХХ-ХХХХХХ)'");
				РезультатПроверки.Реквизит    = "ПроверкаРегНомерПФР";
				РезультатПроверки.Поле        = "РегНомерПФР";
				
				РезультатПроверкиРеквизитов.Добавить(РезультатПроверки);
				
			КонецЕсли;
				
			// Код отделения ПФР
			КодПФРБезТире = ДокументооборотСКОКлиентСервер.УдалитьРазделители(Форма.КодПФР);
			Если ПустаяСтрока(СтрЗаменить(Форма.КодПФР,"-","")) Тогда
				
				РезультатПроверки = ДокументооборотСКОКлиентСервер.РезультатПроверкиРеквизитов();
				РезультатПроверки.ТекстОшибки = НСтр("ru = 'Заполните поле ""Код отделения ПФР""'");
				РезультатПроверки.Пустой      = Истина;
				РезультатПроверки.Реквизит    = "ПроверкаКодПФР";
				РезультатПроверки.Поле        = "КодПФР";
				
				РезультатПроверкиРеквизитов.Добавить(РезультатПроверки);

			ИначеЕсли ДокументооборотСКОКлиентСервер.НайденыЗапрещенныеСимволы(
					Форма.КодПФР, 
					НСтр("ru = 'Код отделения ПФР'"), 
					"КодПФР", 
					Истина,
					ТекстОшибки) Тогда
				
				РезультатПроверки = ДокументооборотСКОКлиентСервер.РезультатПроверкиРеквизитов();
				РезультатПроверки.ТекстОшибки = ТекстОшибки;
				РезультатПроверки.Реквизит    = "ПроверкаКодПФР";
				РезультатПроверки.Поле        = "КодПФР";
				
				РезультатПроверкиРеквизитов.Добавить(РезультатПроверки);
				
			ИначеЕсли СтрДлина(КодПФРБезТире) >= 6
				И ДокументооборотСКОКлиентСервер.ПроверитьЦифровойКодЗаданнойДлины(КодПФРБезТире, СтрДлина(КодПФРБезТире), Истина) Тогда
				
				// Не ошибка. Написан без тире. Исправляем сами.
				Форма.КодПФР = 
					Лев(КодПФРБезТире, 3)
					+ "-"
					+ Сред(КодПФРБезТире, 4, 3);
					
			ИначеЕсли СтрДлина(СокрЛП(Форма.КодПФР))<> 7 ИЛИ СтрДлина(КодПФРБезТире) <> 6 Тогда
				
				РезультатПроверки = ДокументооборотСКОКлиентСервер.РезультатПроверкиРеквизитов();
				РезультатПроверки.ТекстОшибки = НСтр("ru = 'Код отделения ПФР должен состоять из 6 цифр'");
				РезультатПроверки.Реквизит    = "ПроверкаКодПФР";
				РезультатПроверки.Поле        = "КодПФР";
				
				РезультатПроверкиРеквизитов.Добавить(РезультатПроверки);
					
			КонецЕсли;
			
		КонецЕсли;
	
	КонецЕсли;
	
	// ФСРАР ////////////////////////////////////////////////////////////////////////
	Если Форма.ПризнакПоддержкиФСРАР И Форма.ПодатьЗаявкуНаСертификатДляФСРАР Тогда
		
		Если НЕ ЗначениеЗаполнено(Форма.КодРегионаФСРАР) Тогда
			
			РезультатПроверки = ДокументооборотСКОКлиентСервер.РезультатПроверкиРеквизитов();
			РезультатПроверки.ТекстОшибки = НСтр("ru = 'Заполните регион для Росалкогольрегулирования'");
			РезультатПроверки.Пустой      = Истина;
			РезультатПроверки.Реквизит    = "ПроверкаКодРегионаФСРАР";
			РезультатПроверки.Поле        = "КодРегионаФСРАР";
			
			РезультатПроверкиРеквизитов.Добавить(РезультатПроверки);

		Иначе
				
			СхемаСдачиОтчетности = СхемаСдачиОтчетностиФСРАРпоКодуРегиона(Форма.КодРегионаФСРАР);
				
			Если СхемаСдачиОтчетности = ПредопределенноеЗначение("Перечисление.СхемыСдачиОтчетностиФСРАР.СдачаВручную") Тогда
				
				РезультатПроверки = ДокументооборотСКОКлиентСервер.РезультатПроверкиРеквизитов();
				РезультатПроверки.ТекстОшибки = НСтр("ru = 'Заполните регион для Росалкогольрегулирования'");
				РезультатПроверки.Пустой      = Истина;
				РезультатПроверки.Реквизит    = "ПроверкаКодРегионаФСРАР";
				РезультатПроверки.Поле        = "КодРегионаФСРАР";
				
				РезультатПроверкиРеквизитов.Добавить(РезультатПроверки);
					
			КонецЕсли;
				
		КонецЕсли;

	КонецЕсли;
	
	// Сочетаемость выбранных контролирующих органов ////////////////////////////////////////////////////////////////////////
	Если НЕ (Форма.СдаватьВФНС 
		ИЛИ Форма.СдаватьВПФР 
		ИЛИ Форма.СдаватьВФСС 
		ИЛИ Форма.СдаватьВРосстат 
		ИЛИ Форма.ПодатьЗаявкуНаСертификатДляФСРАР 
		ИЛИ Форма.ПодатьЗаявкуНаПодключениеРПН 
		ИЛИ Форма.ПодатьЗаявкуНаПодключениеФТС) Тогда
		
		РезультатПроверки = ДокументооборотСКОКлиентСервер.РезультатПроверкиРеквизитов();
		РезультатПроверки.ТекстОшибки = НСтр("ru = 'Укажите хотя бы один контролирующий орган, в который будет сдаваться отчетность'");
		РезультатПроверки.Реквизит    = "ПроверкаСдаватьВФНС";
		РезультатПроверки.Поле        = "ПолучателиФНС";
		
		РезультатПроверкиРеквизитов.Добавить(РезультатПроверки);
		
	ИначеЕсли НЕ Форма.СдаватьВФНС 
		И НЕ Форма.СдаватьВПФР Тогда
		
		РезультатПроверки = ДокументооборотСКОКлиентСервер.РезультатПроверкиРеквизитов();
		РезультатПроверки.ТекстОшибки = НСтр("ru = 'Среди контролирующих органов, в которые будет сдаваться отчетность, должены быть ФНС или ПФР'");
		РезультатПроверки.Реквизит    = "ПроверкаСдаватьВФНС";
		РезультатПроверки.Поле        = "ПолучателиФНС";
		
		РезультатПроверкиРеквизитов.Добавить(РезультатПроверки);
		
	КонецЕсли;
	
	Для каждого РезультатПроверки Из РезультатПроверкиРеквизитов Цикл
		ДокументооборотСКОКлиентСервер.ВывестиОшибкуПроверкиРеквизита(МастерДалее, РезультатПроверки, ВыводитьСообщения);
	КонецЦикла;
	
	Возврат РезультатПроверкиРеквизитов;
	
КонецФункции

Процедура ВывестиОшибку(
		Знач ТекстСообщенияПользователю,
		Знач Поле = "",
		Знач УникальныйИдентификатор,
		ПроверкаПройдена = Истина) Экспорт
	
	Сообщение = Новый СообщениеПользователю;
	Сообщение.Текст = ТекстСообщенияПользователю;
	Сообщение.Поле = Поле;
	Сообщение.ИдентификаторНазначения = УникальныйИдентификатор;
	Сообщение.Сообщить();
	
	ПроверкаПройдена = Ложь;
	
КонецПроцедуры

Функция СхемаСдачиОтчетностиФСРАР(ФедеральныйПортал, ТипПортала, КодРегиона) Экспорт
	
	Если ФедеральныйПортал Тогда
		Результат = Перечисления.СхемыСдачиОтчетностиФСРАР.ФедеральныйСервер;
		
	ИначеЕсли ТипПортала = "Общий" Тогда
		Результат = Перечисления.СхемыСдачиОтчетностиФСРАР.ОбщийРегиональныйИФедеральныйСервер;
		
	ИначеЕсли ТипПортала = "Типовой" Тогда
		Результат = Перечисления.СхемыСдачиОтчетностиФСРАР.ТиповойРегиональныйИФедеральныйСерверы;
		
	ИначеЕсли ТипПортала = "СвояРеализация" И КодРегиона = "77" Тогда
		Результат = Перечисления.СхемыСдачиОтчетностиФСРАР.МосковскийРегиональныйИФедеральныйСерверы;
		
	Иначе
		Результат = Перечисления.СхемыСдачиОтчетностиФСРАР.СдачаВручную;
	КонецЕсли;
	
	Возврат Результат;
	
КонецФункции

Функция СхемаСдачиОтчетностиФСРАРпоКодуРегиона(КодРегионаФСРАР) Экспорт
	
	ИнформацияОРегионе = ИнформацияОРегионеФСРАР(КодРегионаФСРАР);
		
	Если ИнформацияОРегионе = Неопределено Тогда
		
		СхемаСдачиОтчетности = ПредопределенноеЗначение("Перечисление.СхемыСдачиОтчетностиФСРАР.СдачаВручную");
		
	Иначе
		
		СхемаСдачиОтчетности = СхемаСдачиОтчетностиФСРАР(
			Ложь, 
			ИнформацияОРегионе.ТипПортала, 
			КодРегионаФСРАР);
			
	КонецЕсли;
		
	Возврат СхемаСдачиОтчетности;
	
КонецФункции

Функция ПроверитьПараметры1СЭДО(Форма, ТихийРежим = Ложь) Экспорт
	
	МастерДалее = Истина;
	
	Если Форма.ПодключитьЭДО Тогда
	
		// оператор ЭДО
		Если НЕ ЗначениеЗаполнено(Форма.ОператорЭДО) Тогда
			
			Если ТихийРежим Тогда
				МастерДалее = Ложь;
			Иначе
				ВывестиОшибку(
					НСтр("ru = 'Заполните поле ""Оператор ЭДО""'"), 
					"ОператорЭДО",
					Форма.УникальныйИдентификатор,
					МастерДалее);
			КонецЕсли;
			
		КонецЕсли;
		
		// код ФНС
		Если ПустаяСтрока(Форма.КодНалоговогоОрганаЭДО) Тогда
			
			Если ТихийРежим Тогда
				МастерДалее = Ложь;
			Иначе
				ВывестиОшибку(
					НСтр("ru = 'Заполните поле ""Код ФНС""'"), 
					"КодНалоговогоОрганаЭДО",
					Форма.УникальныйИдентификатор,
					МастерДалее);
			КонецЕсли;
			
		ИначеЕсли СтрДлина(СокрЛП(Форма.КодНалоговогоОрганаЭДО))<> 4 Тогда
			
			Если ТихийРежим Тогда
				МастерДалее = Ложь;
			Иначе
				ВывестиОшибку(
					НСтр("ru = 'Код ФНС должен состоять из 4 цифр'"), 
					"КодНалоговогоОрганаЭДО",
					Форма.УникальныйИдентификатор,
					МастерДалее);
			КонецЕсли;
			
		КонецЕсли;
		
	КонецЕсли;
	
	Возврат МастерДалее;
	
КонецФункции

Функция ПользователиУказаныКорректно(Форма, ТихийРежим = Ложь) Экспорт
	
	МастерДалее = Истина;
	
	ВыбранХотяБыОдинПользователь = Ложь;
	Для Каждого ЭлементСписка Из Форма.СписокПользователей Цикл
		
		Если ЭлементСписка.Пометка Тогда 
			ВыбранХотяБыОдинПользователь = Истина;
			Прервать;
		КонецЕсли;
		
	КонецЦикла;
	
	Если Форма.СписокПользователей.Количество() > 0 И НЕ ВыбранХотяБыОдинПользователь Тогда
		
		Если ТихийРежим Тогда
			МастерДалее = Ложь;
		Иначе
			ВывестиОшибку(
				НСтр("ru = 'Укажите хотя бы одного пользователя'"), 
				"СписокПользователей",
				Форма.УникальныйИдентификатор,
				МастерДалее);
		КонецЕсли;
		
	КонецЕсли;
		
	Возврат МастерДалее;
	
КонецФункции

Процедура ОтобразитьРезультатПроверкиРеквизитов(Форма, РезультатПроверки) Экспорт
	
	Элементы = Форма.Элементы;
	
	Для Каждого Элемент Из Элементы Цикл
		Если СтрНайти(Элемент.Имя, "Проверка") 
			И НЕ СтрНайти(Элемент.Имя, "РасширеннаяПодсказка")
			И ТипЗнч(Элемент) = Тип("ДекорацияФормы") Тогда
			
			Элемент.Картинка    = БиблиотекаКартинок.Пустышка16;
			Элемент.Гиперссылка = Ложь;
			Элемент.Подсказка   = "";
			
		КонецЕсли;
	КонецЦикла;
	
	Для каждого РезультатПроверкиРеквизита Из РезультатПроверки Цикл
		
		РеквизитПроверки = Элементы[РезультатПроверкиРеквизита.Реквизит];
		
		Если НЕ РезультатПроверкиРеквизита.Пустой И РезультатПроверкиРеквизита.ТекстОшибки <> "" Тогда
			
			РеквизитПроверки.Картинка    = БиблиотекаКартинок.ПризнакНаличияКритическойОшибки;
			РеквизитПроверки.Подсказка   = РезультатПроверкиРеквизита.ТекстОшибки;
			РеквизитПроверки.Гиперссылка = Истина;
			
		КонецЕсли;
	КонецЦикла;
	
КонецПроцедуры

#КонецОбласти

#Область ОтправкаРасписки

Функция ПрисоединитьФайлЗаявления(Заявление, АдресФайлаВоВременномХранилище, ИмяДокумента, РасширениеБезТочки = "xml") Экспорт
	
	СтрокаТаблицыЗаявление = Неопределено;
	Для Каждого СтрокаТаблицы Из Заявление.ЭлектронныеДокументы Цикл
		Если СтрокаТаблицы.Документ = ИмяДокумента Тогда
			СтрокаТаблицыЗаявление = СтрокаТаблицы;
			Прервать;
		КонецЕсли;
	КонецЦикла;
	
	Если СтрокаТаблицыЗаявление <> Неопределено Тогда
		ИнформацияОФайле = Новый Структура;
		ИнформацияОФайле.Вставить("АдресФайлаВоВременномХранилище", АдресФайлаВоВременномХранилище);
		ИнформацияОФайле.Вставить("АдресВременногоХранилищаТекста", "");
		
		РаботаСФайлами.ОбновитьФайл(СтрокаТаблицыЗаявление.Файл, ИнформацияОФайле);
		
		СтрокаТаблицыЗаявление.Размер = ОбщегоНазначения.ЗначениеРеквизитаОбъекта(СтрокаТаблицыЗаявление.Файл, "Размер");
		
		Возврат СтрокаТаблицыЗаявление.Файл;
		
	Иначе
		ПараметрыФайла = Новый Структура;
		ПараметрыФайла.Вставить("Автор", 				Пользователи.ТекущийПользователь());
		ПараметрыФайла.Вставить("ВладелецФайлов", 		Заявление.Ссылка);
		ПараметрыФайла.Вставить("ИмяБезРасширения", 	ИмяДокумента);
		ПараметрыФайла.Вставить("РасширениеБезТочки", 	РасширениеБезТочки);
		ПараметрыФайла.Вставить("ВремяИзмененияУниверсальное");
		
		ПрисоединенныйФайл = РаботаСФайлами.ДобавитьФайл(ПараметрыФайла, АдресФайлаВоВременномХранилище);
		
		НоваяСтрока = Заявление.ЭлектронныеДокументы.Добавить();
		НоваяСтрока.Документ = ИмяДокумента;
		НоваяСтрока.Файл     = ПрисоединенныйФайл;
		НоваяСтрока.Размер   = ОбщегоНазначения.ЗначениеРеквизитаОбъекта(ПрисоединенныйФайл, "Размер");
		
		Возврат ПрисоединенныйФайл;
		
	КонецЕсли;

КонецФункции

Функция ТипСКЗИ(Контекст)

	// Определяем СКЗИ из сертификата руководителя
	Если Контекст.Свойство("ДвДанныеСертификатаРуководителя") Тогда
		Сертификат = Новый Структура("Сертификат", Контекст.ДвДанныеСертификатаРуководителя);
		МассивИзОдного = ОбщегоНазначенияКлиентСервер.ЗначениеВМассиве(Сертификат);
		ИнформацияОСертификате = КриптографияЭДКОСлужебныйВызовСервера.ИзвлечьИнформациюОКриптопровайдереПоСертификату(МассивИзОдного);
		Если ИнформацияОСертификате.Количество() > 0 И ИнформацияОСертификате[0].Криптопровайдер <> Неопределено Тогда
			СвойстваКриптопровайдера = ИнформацияОСертификате[0].Криптопровайдер;
			ТипСКЗИ = ОпределитьТипСКЗИ(СвойстваКриптопровайдера);
		Иначе
			ТипСКЗИ = "Крипто-Про";
		КонецЕсли;
	Иначе
		ТипСКЗИ = "Крипто-Про";
	КонецЕсли;
	
	Возврат ТипСКЗИ;

КонецФункции 

Функция ПолучитьФайлАвтонастройки(СпецоператорСвязи, ИдентификаторАбонента, ДатаПоследнегоОбновления = Неопределено) Экспорт
	
	РезультатВыполнения = Новый Структура("Выполнено", Ложь);
	
	Результат = СоздатьКлиентСервисаПоСпецоператоруСвязи(СпецоператорСвязи);
	Если Не Результат.Выполнено Тогда
		Если Результат.Свойство("Результат") Тогда
 			РезультатВыполнения.Вставить("ОписаниеОшибки", Результат.ОписаниеОшибки);
		КонецЕсли;
		Возврат РезультатВыполнения;
	КонецЕсли;
	
	Попытка
		ДатаПоследнегоОбновления = ?(ЗначениеЗаполнено(ДатаПоследнегоОбновления), ДатаПоследнегоОбновления, Дата(1, 1, 1));
		Ответ = Результат.КлиентСервиса.ReceiveUpdatedPacket(ИдентификаторАбонента, ДатаПоследнегоОбновления);
		ОтветXDTO = Дессериализовать(Ответ);
		
		Результат = ОбработатьОтвет(ОтветXDTO);
		Если Результат.Выполнено Тогда
			Если Результат.ОбновлениеНеТребуется Тогда
				// получить файл из регистра
			Иначе
				Файлы = Разархивировать(ПоместитьВоВременноеХранилище(Результат.Пакет));
				Если Файлы.Количество() = 1 Тогда
					
					НастройкаXDTO = Дессериализовать(Файлы[0].Адрес);
					
					КонфЧасть = НастройкаXDTO.КонфиденциальнаяИнформация;
					
					РезультатВыполнения.Вставить("Выполнено", Истина);
					РезультатВыполнения.Вставить("КонфиденциальнаяИнформация", ПоместитьВоВременноеХранилище(Base64Значение(НастройкаXDTO.КонфиденциальнаяИнформация), Новый УникальныйИдентификатор));
					РезультатВыполнения.Вставить("Настройки", Файлы[0].Адрес);
					
					Возврат РезультатВыполнения;
					
				КонецЕсли;
			КонецЕсли;
		Иначе
			Если Результат.Свойство("Результат") Тогда
 				РезультатВыполнения.Вставить("ОписаниеОшибки", Результат.ОписаниеОшибки);
			ИначеЕсли ОтветXDTO.Свойства().Получить("errorMessage") <> Неопределено Тогда
				РезультатВыполнения.Вставить("ОписаниеОшибки", ОтветXDTO.errorMessage);
			КонецЕсли;
		КонецЕсли;
	Исключение
		ИнформацияОбОшибке = ИнформацияОбОшибке();
		ЗаписьЖурналаРегистрации(
			"Электронный документооборот с контролирующими органами.Создание клиента сервиса",
		    УровеньЖурналаРегистрации.Ошибка,,,
			ПодробноеПредставлениеОшибки(ИнформацияОбОшибке));
		РезультатВыполнения.Выполнено = Ложь;
 		РезультатВыполнения.Вставить("ОписаниеОшибки", КраткоеПредставлениеОшибки(ИнформацияОбОшибке));
	КонецПопытки;
	
	Возврат РезультатВыполнения;
	
КонецФункции

Функция Разархивировать(ИмяИлиАдресФайлаАрхива) Экспорт

	Если ЭтоАдресВременногоХранилища(ИмяИлиАдресФайлаАрхива) Тогда
		ИмяФайлаАрхива = ПолучитьИмяВременногоФайла("zip");
		ПолучитьИзВременногоХранилища(ИмяИлиАдресФайлаАрхива).Записать(ИмяФайлаАрхива);
		ВернутьКакАдрес = Истина;	
	Иначе
		ИмяФайлаАрхива = ИмяИлиАдресФайлаАрхива;
		ВернутьКакАдрес = Ложь;
	КонецЕсли;
	
	КаталогДляИзвлечения = ОперацииСФайламиЭДКО.СоздатьВременныйКаталог();
	
	ЧтениеZipФайла = Новый ЧтениеZipФайла(ИмяФайлаАрхива);
	ЧтениеZipФайла.ИзвлечьВсе(КаталогДляИзвлечения);
	
	
	Файлы = НайтиФайлы(КаталогДляИзвлечения, "*"); 
	
	ОписанияФайлов = Новый Массив;
	Для Каждого Файл Из Файлы Цикл
		ОписаниеФайла = Новый Структура;
		Если ВернутьКакАдрес Тогда
			ОписаниеФайла.Вставить("Адрес", ПоместитьВоВременноеХранилище(Новый ДвоичныеДанные(Файл.ПолноеИмя), Новый УникальныйИдентификатор));			
		Иначе
			ОписаниеФайла.Вставить("Путь", Файл.Путь);
		КонецЕсли;
		ОписаниеФайла.Вставить("Имя", Файл.Имя);
		ОписанияФайлов.Добавить(ОписаниеФайла);
	КонецЦикла;
	
	Если ВернутьКакАдрес Тогда
		ОперацииСФайламиЭДКО.УдалитьВременныйФайл(КаталогДляИзвлечения);
	КонецЕсли;
	
	Возврат ОписанияФайлов;	
	
КонецФункции

Функция ОбработатьОтвет(ОтветXDTO)
	
	Результат = Новый Структура("Выполнено,ОбновлениеНеТребуется", Ложь, Ложь);
	Если ОтветXDTO.code = "0" ИЛИ ОтветXDTO.code = "11" Тогда
		
		Если ОтветXDTO.Свойства().Получить("packet") <> Неопределено Тогда
			Результат.Выполнено = Истина;
			Результат.Вставить("Пакет", Base64Значение(ОтветXDTO.packet));
		ИначеЕсли ОтветXDTO.Свойства().Количество() = 1 Тогда  // в ответе только code 0

		Иначе
			ВызватьИсключение(НСтр("ru = 'Неизвестная структура ответа'"));			
		КонецЕсли;
		
		// message: Ошибка запроса. Не найдено заявление (обновление) для документооборота (абонента): {0}
		// description: Ответ (обновление) еще не сформирован(о).
	ИначеЕсли ОтветXDTO.code = "1" Тогда // Заявление еще обрабатывается
		
		// message: Ошибка сервера. Сервер обновления временно не доступен, повторите операцию позже.
		// description: Ошибка при получении ответа или обновления от сервера.
		
		//ИначеЕсли ОтветXDTO.Code = "17" Тогда // ошибка??
	ИначеЕсли ОтветXDTO.code = "11" Тогда
		//версия файла на сервер и в  кэше клиент совпала, скачивать не требуется
		Результат.Вставить("Выполнено", Истина);
		Результат.Вставить("ОбновлениеНеТребуется", Истина);
	ИначеЕсли ОтветXDTO.code = "94" Тогда // временная проблема с сервером КА
	Иначе // Ошибка обработки заявление - автоматически отказ
		//Результат.Отклонено	= Истина;
		//Результат.Комментарий = ПакетXDTO.errorMessage;
		Результат.Вставить("Выполнено", Ложь);
		Результат.Вставить("ОписаниеОшибки", ОтветXDTO.errorMessage);
	КонецЕсли;
	
	Возврат Результат;
	
КонецФункции

Функция ОтправитьРаспискуНаСервере(ДокументЗаявление, ИдентификаторАбонента) Экспорт
	
	ФайлыРасписки = ПодготовитьРаспискуКОтправке(ДокументЗаявление, ИдентификаторАбонента);
	
	РезультатВыполнения = Новый Структура("Выполнено", Истина);
	Файлы = Новый Массив;
	Файлы.Добавить(ФайлыРасписки.Расписка);
	Если ФайлыРасписки.Свойство("Подпись") Тогда
		Файлы.Добавить(ФайлыРасписки.Подпись);
	КонецЕсли;
	Файлы.Добавить(ФайлыРасписки.ОписаниеПакета);
	АдресПакета = ОперацииСФайламиЭДКОВызовСервера.УпаковатьФайлы(Файлы);
	
	ПрисоединитьФайлЗаявления(ДокументЗаявление, АдресПакета, "Пакет_с_распиской", "zip");
	
	Результат = СоздатьКлиентСервисаПоСпецоператоруСвязи(ДокументЗаявление.СпецоператорСвязи);
	Если Результат.Выполнено Тогда
		Ответ = Результат.КлиентСервиса.SendPacket(Base64Строка(ПолучитьИзВременногоХранилища(АдресПакета)));
		
		//code="222" message="Заявление по документообороту {0} было обработано вручную. Расписка не требуется.
		//code="170" message="Ошибка операции. Документооборот с идентификатором {0} завершен.
		//code="213" message="Ошибка авторизации. Подпись {0} для файла {1} не прошла проверку
		//code="131" message="Ошибка авторизации. Сертификат {0} не прошел проверку
		//code="141" message="Ошибка авторизации. Сертификат {0} издан на корневом сертификате {1}, 
		//  который не входит в список аккредитованных удостоверяющих центров УФО.
		
		ОтветXDTO = Дессериализовать(Ответ);		
		Если ОтветXDTO.code <> "0" 
			И ОтветXDTO.code <> "60" 
			И ОтветXDTO.code <> "222"
			И ОтветXDTO.code <> "170" Тогда
			
			РезультатВыполнения.Вставить("Выполнено", Ложь);
			Если ОтветXDTO.Свойства().Получить("errorMessage") <> Неопределено Тогда
				РезультатВыполнения.Вставить("ОписаниеОшибки", ОтветXDTO.errorMessage);
			Иначе
				РезультатВыполнения.Вставить("ОписаниеОшибки", "Ошибка при отправке расписки в получении сертификата");
			КонецЕсли;
			
		КонецЕсли;
		
		РезультатВыполнения.Вставить("Код", ОтветXDTO.code);
		
	Иначе
		РезультатВыполнения = Результат;
	КонецЕсли;
	
	Возврат РезультатВыполнения;
	
КонецФункции

Функция СоздатьКлиентСервисаПоСпецоператоруСвязи(СпецоператорСвязи) Экспорт
	
	Результат = Новый Структура("Выполнено", Истина);
	
	ПараметрыСпецоператораСвязи = ПолучитьПараметрыОператораСвязи(СпецоператорСвязи);
	
	ПараметрыПодключения = ОбщегоНазначения.ПараметрыПодключенияWSПрокси();
	ПараметрыПодключения.АдресWSDL 				= ПараметрыСпецоператораСвязи.ВебСервисОпределение;
	ПараметрыПодключения.URIПространстваИмен 	= ПараметрыСпецоператораСвязи.ВебСервисURIПространстваИменСервиса;
	ПараметрыПодключения.ИмяСервиса 			= ПараметрыСпецоператораСвязи.ВебСервисИмяСервиса;
	ПараметрыПодключения.ИмяТочкиПодключения 	= ПараметрыСпецоператораСвязи.ВебСервисИмяТочкиПодключения;
	ПараметрыПодключения.ИмяПользователя 		= "";
	ПараметрыПодключения.Пароль 				= "";
	ПараметрыПодключения.Таймаут 				= 120;
	
	Попытка
		КлиентаСервиса = ОбщегоНазначения.СоздатьWSПрокси(ПараметрыПодключения);
		Результат.Вставить("КлиентСервиса", КлиентаСервиса);
	Исключение
		ИнформацияОбОшибке = ИнформацияОбОшибке();
		ЗаписьЖурналаРегистрации(
			"Электронный документооборот с контролирующими органами.Создание клиента сервиса",
		    УровеньЖурналаРегистрации.Ошибка,,,
			ПодробноеПредставлениеОшибки(ИнформацияОбОшибке));
		Результат.Выполнено = Ложь;
 		Результат.Вставить("ОписаниеОшибки", КраткоеПредставлениеОшибки(ИнформацияОбОшибке));
	КонецПопытки;
	
	Возврат Результат;
		
КонецФункции

Функция ПодготовитьРаспискуКОтправке(ДокументЗаявление, ИдентификаторАбонента)
	
	ФайлыРасписки = ПолучитьРасписку(ДокументЗаявление);
	
	ИмяФайла = ПолучитьИмяВременногоФайла("xml");
	ЗаписьXML = Новый ЗаписьXML;
	ЗаписьXML.ОткрытьФайл(ИмяФайла, "windows-1251");
	ЗаписьXML.ЗаписатьОбъявлениеXML();
	ЗаписьXML.ЗаписатьНачалоЭлемента("пакет");
	ЗаписьXML.ЗаписатьАтрибут("версияФормата", "1С:1.1");
	ЗаписьXML.ЗаписатьАтрибут("версПрог", РегламентированнаяОтчетность.ВерсияПрограммы());
	ЗаписьXML.ЗаписатьАтрибут("типДокументооборота", "РегистрацияАбонента");
	ЗаписьXML.ЗаписатьАтрибут("типТранзакции", "Расписка");
	ЗаписьXML.ЗаписатьАтрибут("идентификаторДокументооборота", ДокументЗаявление.ИдентификаторДокументооборота);
	
	ЗаписьXML.ЗаписатьНачалоЭлемента("отправитель");	
	ЗаписьXML.ЗаписатьАтрибут("типСубъекта", "абонент");
	ЗаписьXML.ЗаписатьАтрибут("идентификаторСубъекта", ИдентификаторАбонента);
	ЗаписьXML.ЗаписатьКонецЭлемента();
	
	ЗаписьXML.ЗаписатьНачалоЭлемента("получатель");	
	ЗаписьXML.ЗаписатьАтрибут("типСубъекта", "спецоператор");
	ЗаписьXML.ЗаписатьАтрибут("идентификаторСубъекта", "КалугаАстрал");
	ЗаписьXML.ЗаписатьКонецЭлемента();
	
	ЗаписьXML.ЗаписатьНачалоЭлемента("документ");	
	ЗаписьXML.ЗаписатьАтрибут("идентификаторДокумента", ОбщегоНазначенияЭДКОКлиентСервер.НовыйИдентификатор());
	Если ФайлыРасписки.Свойство("Подпись") Тогда 
		ЗаписьXML.ЗаписатьАтрибут("типДокумента", "РаспискаЭП");
		ЗаписьXML.ЗаписатьАтрибут("типСодержимого", "cer");
	Иначе
		ЗаписьXML.ЗаписатьАтрибут("типДокумента", "РаспискаСкан");
		ЗаписьXML.ЗаписатьАтрибут("типСодержимого", ФайлыРасписки.Расписка.Расширение);
	КонецЕсли;
	ЗаписьXML.ЗаписатьАтрибут("сжат", "true");
	ЗаписьXML.ЗаписатьАтрибут("зашифрован", "false");
	
	ЗаписьXML.ЗаписатьНачалоЭлемента("содержимое");	
	ЗаписьXML.ЗаписатьАтрибут("имяФайла", ФайлыРасписки.Расписка.Имя);
	ЗаписьXML.ЗаписатьКонецЭлемента();
	
	Если ФайлыРасписки.Свойство("Подпись") Тогда
		ЗаписьXML.ЗаписатьНачалоЭлемента("подпись");	
		ЗаписьXML.ЗаписатьАтрибут("имяФайла", ФайлыРасписки.Подпись.Имя);
		ЗаписьXML.ЗаписатьКонецЭлемента();
	КонецЕсли;
		
	ЗаписьXML.ЗаписатьКонецЭлемента();

	ЗаписьXML.ЗаписатьКонецЭлемента();
	
	ЗаписьXML.Закрыть();

	ИмяФайлаРасписки = ФайлыРасписки.Расписка.Имя;
	ФайлыРасписки.Расписка.Имя = "file";
	ФайлыРасписки.Расписка.Адрес = ОперацииСФайламиЭДКОВызовСервера.УпаковатьФайлы(
		ОбщегоНазначенияКлиентСервер.ЗначениеВМассиве(ФайлыРасписки.Расписка));
	ФайлыРасписки.Расписка.Имя = ИмяФайлаРасписки;
	
	ОписаниеПакета = Новый Структура;
	ОписаниеПакета.Вставить("Имя", "packageDescription.xml");
	ОписаниеПакета.Вставить("Адрес", ПоместитьВоВременноеХранилище(Новый ДвоичныеДанные(ИмяФайла), Новый УникальныйИдентификатор));
	ФайлыРасписки.Вставить("ОписаниеПакета", ОписаниеПакета);
	
	ОперацииСФайламиЭДКО.УдалитьВременныйФайл(ИмяФайла);
	
	Возврат ФайлыРасписки;
	
КонецФункции

Функция ПолучитьРасписку(ДокументЗаявление)
	
	ФайлыРасписки = Новый Структура;
	
	ПрисоединенныйФайл = Неопределено;
	Для Каждого ЭлектронныйДокумент Из ДокументЗаявление.ЭлектронныеДокументы Цикл
		Если ЭлектронныйДокумент.Документ = "Расписка" Тогда
			ПрисоединенныйФайл = ЭлектронныйДокумент.Файл;
			Прервать;
		КонецЕсли;
	КонецЦикла;
	
	ДополнительныеПараметры = РаботаСФайламиКлиентСервер.ПараметрыДанныхФайла();
	ДополнительныеПараметры.ИдентификаторФормы 				= Новый УникальныйИдентификатор;
	ДополнительныеПараметры.ПолучатьСсылкуНаДвоичныеДанные 	= Истина;
	ДанныеФайла = РаботаСФайлами.ДанныеФайла(ПрисоединенныйФайл, ДополнительныеПараметры);
	СвойстваФайла = Новый Структура;
	СвойстваФайла.Вставить("Имя", ОбщегоНазначенияЭДКОКлиентСервер.НовыйИдентификатор() + ".bin");
	СвойстваФайла.Вставить("Адрес", ДанныеФайла.СсылкаНаДвоичныеДанныеФайла);
	СвойстваФайла.Вставить("Расширение", ДанныеФайла.Расширение);
	
	ФайлыРасписки.Вставить("Расписка", СвойстваФайла);
	
	Если ДанныеФайла.ПодписанЭП Тогда
		ДвоичныеДанныеПодписи = ЭлектроннаяПодпись.УстановленныеПодписи(ПрисоединенныйФайл)[0].Подпись;
		Если ТипЗнч(ДвоичныеДанныеПодписи) = Тип("ХранилищеЗначения") Тогда
			ДвоичныеДанныеПодписи = ДвоичныеДанныеПодписи.Получить();
		КонецЕсли;
		Адрес = ПоместитьВоВременноеХранилище(ДвоичныеДанныеПодписи, Новый УникальныйИдентификатор);
		
		СвойстваФайла = Новый Структура;
		СвойстваФайла.Вставить("Имя", ОбщегоНазначенияЭДКОКлиентСервер.НовыйИдентификатор() + ".bin");
		СвойстваФайла.Вставить("Адрес", Адрес);
		СвойстваФайла.Вставить("Расширение", "bin");
		
		ФайлыРасписки.Вставить("Подпись", СвойстваФайла);
	КонецЕсли;
	
	Возврат ФайлыРасписки;
		
КонецФункции

Функция Дессериализовать(ТекстXML, Фабрика = Неопределено) Экспорт
	
	УдалитьФайл = Ложь;
	
	ЧтениеXML = Новый ЧтениеXML;
	Если Найти(ТекстXML, "<") > 0 Тогда
		ЧтениеXML.УстановитьСтроку(ТекстXML);
	Иначе
		ИмяФайла = ТекстXML;
		Если ЭтоАдресВременногоХранилища(ТекстXML) Тогда
			УдалитьФайл = Истина;
			ИмяФайла = ПолучитьИмяВременногоФайла("xml");
			ПолучитьИзВременногоХранилища(ТекстXML).Записать(ИмяФайла);
		КонецЕсли;
		ЧтениеXML.ОткрытьФайл(ИмяФайла);
	КонецЕсли;
	ЧтениеXML.ПерейтиКСодержимому();
	
	Если Фабрика = Неопределено Тогда
		Фабрика = ФабрикаXDTO;
	КонецЕсли;
	ОбъектXDTO = Фабрика.ПрочитатьXML(ЧтениеXML);
	ЧтениеXML.Закрыть();
	
	Если УдалитьФайл Тогда
		ОперацииСФайламиЭДКО.УдалитьВременныйФайл(ИмяФайла);
	КонецЕсли;
	
	Возврат ОбъектXDTO;
	
КонецФункции

Функция ПолучитьСертификатыИзФайлаНастройки(АдресФайла) Экспорт
	
	ИмяВременногоФайла = ПолучитьИмяВременногоФайла("xml");
	ПолучитьИзВременногоХранилища(АдресФайла).Записать(ИмяВременногоФайла);
	
	НастройкиXDTO = Дессериализовать(ИмяВременногоФайла);
	ОперацииСФайламиЭДКО.УдалитьВременныйФайл(ИмяВременногоФайла);
	
	Сертификаты = Новый Массив;
	Для Каждого СертификатXDTO Из НастройкиXDTO.Сертификаты.Сертификат Цикл
		Сертификат = Новый Структура;
		Сертификат.Вставить("Сертификат", Base64Значение(СертификатXDTO.Последовательность().ПолучитьТекст(0)));
		Сертификат.Вставить("Отпечаток", СертификатXDTO.Отпечаток);
		Сертификат.Вставить("Хранилище", СертификатXDTO.Хранилище);
		
		Сертификаты.Добавить(Сертификат);
	КонецЦикла;
	
	Возврат Сертификаты;
	
КонецФункции

#КонецОбласти

#Область НастройкиОбмена

Функция НастройкиОбменаОрганизаций(Ссылки = Неопределено, Настройки = Неопределено) Экспорт
	
	МассивРезультатов = Новый Массив;
	
	НастройкиВызова = Новый Структура;
	НастройкиВызова.Вставить("НайтиСертификатыПрочихКонтролирующихОрганов", 		Ложь);
	НастройкиВызова.Вставить("ПолучитьРеквизитыКонтролирующихОргановОрганизации", 	Ложь);
	НастройкиВызова.Вставить("ПолучатьДляУдаленныхОрганизаций", 					Истина);
	НастройкиВызова.Вставить("ПолучатьПоддерживаемыеНаправленияДляОператоров", 		Истина);
	Если Настройки <> Неопределено Тогда
		ЗаполнитьЗначенияСвойств(НастройкиВызова, Настройки);
	КонецЕсли;
	
	ТекущаяДатаНаСервере = ТекущаяДатаСеанса();
	ПравоЧтенияУчетныхЗаписейОбмена = ПравоДоступа("Чтение", Метаданные.Справочники.УчетныеЗаписиДокументооборота);
	
	ПоддерживаемыеНаправленияДляОператоров = ?(НастройкиВызова.ПолучатьПоддерживаемыеНаправленияДляОператоров,
		ПоддерживаемыеНаправленияОператоров(), Неопределено);
	ПоддерживаетсяСдачаОтчетностиВБанкРоссии = ОбщегоНазначения.ПодсистемаСуществует(
		"РегламентированнаяОтчетность.ЭлектронныйДокументооборотСКонтролирующимиОрганами.СдачаОтчетностиВБанкРоссии");
	ПоддерживаетсяСдачаОтчетностиВМинобороныРоссии = ОбщегоНазначения.ПодсистемаСуществует(
		"РегламентированнаяОтчетность.ЭлектронныйДокументооборотСКонтролирующимиОрганами.СдачаОтчетностиВМинобороны");
	ЕстьРеквизитОрганизацииНаименованиеСокращенное =
		(Метаданные.Справочники.Организации.Реквизиты.Найти("НаименованиеСокращенное") <> Неопределено);
	УчетныеЗаписиОрганизаций = Новый Массив;
	
	Если Ссылки = Неопределено ИЛИ ТипЗнч(Ссылки) = Тип("Массив") ИЛИ ТипЗнч(Ссылки) = Тип("ФиксированныйМассив") Тогда
		Организации = Ссылки;
		
	Иначе
		Ссылка = Ссылки;
		Если ТипЗнч(Ссылка) = Тип("СправочникСсылка.УчетныеЗаписиДокументооборота") Тогда
			Организации = ОрганизацииУчетнойЗаписи(Ссылка);
			
		Иначе
			Если ТипЗнч(Ссылка) = Тип("СправочникСсылка.Организации") Тогда
				Организация = Ссылка;
				
			Иначе
				Заявление = Ссылка;
				Организация = ЗначенияРеквизитовОбъекта(Заявление, "Организация", Истина).Организация;
			КонецЕсли;
			
			Организации = ?(ЗначениеЗаполнено(Организация), ОбщегоНазначенияКлиентСервер.ЗначениеВМассиве(Организация), Новый Массив);
		КонецЕсли;
	КонецЕсли;
	
	Если ПоддерживаетсяСдачаОтчетностиВБанкРоссии Тогда
		МодульДокументооборотСБанкомРоссии = ОбщегоНазначения.ОбщийМодуль("ДокументооборотСБанкомРоссии");
		БанкРоссииТекстыДляЗапроса = МодульДокументооборотСБанкомРоссии.ПолучитьТекстыДляЗапросаСпискаНастроекОбмена();
	КонецЕсли;
	
	Если ПоддерживаетсяСдачаОтчетностиВМинобороныРоссии Тогда
		МодульДокументооборотСМинобророныРоссии = ОбщегоНазначения.ОбщийМодуль("ДокументооборотСМинобороны");
		МинобороныРоссииТекстыДляЗапроса = МодульДокументооборотСМинобророныРоссии.ПолучитьТекстыДляЗапросаСпискаНастроекОбмена();
	КонецЕсли;	
	
	// настройки обмена организаций
	
	Запрос = Новый Запрос;
	Запрос.Текст =
		"ВЫБРАТЬ РАЗРЕШЕННЫЕ
		|	ЗаявлениеАбонентаСпецоператораСвязи.Организация КАК Организация,
		|	ЗаявлениеАбонентаСпецоператораСвязи.Ссылка КАК Ссылка,
		|	ЗаявлениеАбонентаСпецоператораСвязи.ТипЗаявления КАК ТипЗаявления,
		|	ЗаявлениеАбонентаСпецоператораСвязи.Статус КАК Статус,
		|	ЗаявлениеАбонентаСпецоператораСвязи.СтатусКомментарий КАК СтатусКомментарий,
		|	ЗаявлениеАбонентаСпецоператораСвязи.НастройкаЗавершена КАК НастройкаЗавершена,
		|	ЗаявлениеАбонентаСпецоператораСвязи.Дата КАК Дата,
		|	ЗаявлениеАбонентаСпецоператораСвязи.НастройкаЭДОЗавершена КАК НастройкаЭДОЗавершена,
		|	ЗаявлениеАбонентаСпецоператораСвязи.НастройкиЭДО КАК НастройкиЭДО
		|ПОМЕСТИТЬ
		|	ПоследнееЗаявлениеАбонентаСпецоператораСвязи
		|ИЗ
		|	Документ.ЗаявлениеАбонентаСпецоператораСвязи КАК ЗаявлениеАбонентаСпецоператораСвязи
		|		ВНУТРЕННЕЕ СОЕДИНЕНИЕ (
		|			ВЫБРАТЬ
		|				ЗаявлениеАбонентаСпецоператораСвязи.Организация КАК Организация,
		|				ЗаявлениеАбонентаСпецоператораСвязи.Дата КАК Дата,
		|				МАКСИМУМ(ЗаявлениеАбонентаСпецоператораСвязи.Ссылка) КАК Ссылка
		|			ИЗ
		|				Документ.ЗаявлениеАбонентаСпецоператораСвязи КАК ЗаявлениеАбонентаСпецоператораСвязи
		|					ВНУТРЕННЕЕ СОЕДИНЕНИЕ (
		|						ВЫБРАТЬ
		|							ЗаявлениеАбонентаСпецоператораСвязи.Организация КАК Организация,
		|							МАКСИМУМ(ЗаявлениеАбонентаСпецоператораСвязи.Дата) КАК Дата
		|						ИЗ
		|							Документ.ЗаявлениеАбонентаСпецоператораСвязи КАК ЗаявлениеАбонентаСпецоператораСвязи
		|						ГДЕ
		|							НЕ ЗаявлениеАбонентаСпецоператораСвязи.ПометкаУдаления
		|						СГРУППИРОВАТЬ ПО
		|							ЗаявлениеАбонентаСпецоператораСвязи.Организация) КАК ПоследнееЗаявлениеАбонентаСпецоператораСвязи
		|					ПО ПоследнееЗаявлениеАбонентаСпецоператораСвязи.Организация = ЗаявлениеАбонентаСпецоператораСвязи.Организация
		|						И ПоследнееЗаявлениеАбонентаСпецоператораСвязи.Дата = ЗаявлениеАбонентаСпецоператораСвязи.Дата
		|			ГДЕ
		|				НЕ ЗаявлениеАбонентаСпецоператораСвязи.ПометкаУдаления
		|			СГРУППИРОВАТЬ ПО
		|				ЗаявлениеАбонентаСпецоператораСвязи.Организация,
		|				ЗаявлениеАбонентаСпецоператораСвязи.Дата) КАК ПоследнееЗаявлениеАбонентаСпецоператораСвязи
		|		ПО ПоследнееЗаявлениеАбонентаСпецоператораСвязи.Организация = ЗаявлениеАбонентаСпецоператораСвязи.Организация
		|			И ПоследнееЗаявлениеАбонентаСпецоператораСвязи.Дата = ЗаявлениеАбонентаСпецоператораСвязи.Дата
		|			И ПоследнееЗаявлениеАбонентаСпецоператораСвязи.Ссылка = ЗаявлениеАбонентаСпецоператораСвязи.Ссылка
		|ГДЕ
		|	НЕ ЗаявлениеАбонентаСпецоператораСвязи.ПометкаУдаления
		|ИНДЕКСИРОВАТЬ ПО
		|	ЗаявлениеАбонентаСпецоператораСвязи.Организация
		|;
		|
		|ВЫБРАТЬ РАЗРЕШЕННЫЕ
		|	МИНИМУМ(ЗаявлениеАбонентаСпецоператораСвязи.ДатаПолученияОтвета) КАК ДатаПолученияОтвета,
		|	ЗаявлениеАбонентаСпецоператораСвязи.Организация КАК Организация
		|ПОМЕСТИТЬ
		|	ПервоеОдобленноеЗаявлениеАбонентаСпецоператораСвязи
		|ИЗ
		|	Документ.ЗаявлениеАбонентаСпецоператораСвязи КАК ЗаявлениеАбонентаСпецоператораСвязи
		|ГДЕ
		|	ЗаявлениеАбонентаСпецоператораСвязи.Статус = ЗНАЧЕНИЕ(Перечисление.СтатусыЗаявленияАбонентаСпецоператораСвязи.Одобрено)
		|	И ЗаявлениеАбонентаСпецоператораСвязи.НастройкаЗавершена
		|	И НЕ ЗаявлениеАбонентаСпецоператораСвязи.ПометкаУдаления
		|	И ЗаявлениеАбонентаСпецоператораСвязи.ДатаПолученияОтвета ЕСТЬ НЕ NULL
		|	И ЗаявлениеАбонентаСпецоператораСвязи.ДатаПолученияОтвета <> ДАТАВРЕМЯ(0001,1,1,0,0,0)
		|СГРУППИРОВАТЬ ПО
		|	ЗаявлениеАбонентаСпецоператораСвязи.Организация
		|ИНДЕКСИРОВАТЬ ПО
		|	ЗаявлениеАбонентаСпецоператораСвязи.Организация
		|;
		|
		|ВЫБРАТЬ РАЗРЕШЕННЫЕ
		|	Организации.Ссылка КАК Организация,"
		+ ?(ЕстьРеквизитОрганизацииНаименованиеСокращенное, "
		|	Организации.НаименованиеСокращенное КАК НаименованиеСокращенное,", "
		|	Организации.Наименование КАК НаименованиеСокращенное,") + "
		|	Организации.Наименование КАК НаименованиеОрганизации,
		|	Организации.ПометкаУдаления КАК ПометкаУдаления,
		|	Организации.ВидОбменаСКонтролирующимиОрганами КАК ВидОбменаСКонтролирующимиОрганами,
		|	Организации.УчетнаяЗаписьОбмена КАК УчетнаяЗаписьОбмена,
		|	ЕСТЬNULL(УчетныеЗаписиДокументооборота.АдресЭлектроннойПочты, """") КАК АдресЭлектроннойПочты,
		|	ЕСТЬNULL(УчетныеЗаписиДокументооборота.ИдентификаторАбонента, """") КАК ИдентификаторАбонента,
		|	ЕСТЬNULL(УчетныеЗаписиДокументооборота.СпецоператорСвязи,
		|		ЗНАЧЕНИЕ(Перечисление.СпецоператорыСвязи.ПустаяСсылка)) КАК СпецоператорСвязи,
		|	ЕСТЬNULL(УчетныеЗаписиДокументооборота.РежимАвтонастройки,
		|		ЗНАЧЕНИЕ(Перечисление.РежимыАвтонастройкиУчетнойЗаписиНалогоплательщика.ПустаяСсылка)) КАК РежимАвтонастройки,
		|	ЕСТЬNULL(УчетныеЗаписиДокументооборота.ПредназначенаДляДокументооборотаСФНС, ЛОЖЬ) КАК ПредназначенаДляДокументооборотаСФНС,
		|	ЕСТЬNULL(УчетныеЗаписиДокументооборота.ПредназначенаДляДокументооборотаСПФР, ЛОЖЬ) КАК ПредназначенаДляДокументооборотаСПФР,
		|	ЕСТЬNULL(УчетныеЗаписиДокументооборота.ПредназначенаДляДокументооборотаСФСГС, ЛОЖЬ) КАК ПредназначенаДляДокументооборотаСФСГС,
		|	ЕСТЬNULL(УчетныеЗаписиДокументооборота.СертификатРуководителя, """") КАК СертификатРуководителя,
		|	ЕСТЬNULL(УчетныеЗаписиДокументооборота.ЛицензияНаименование, """") КАК ЛицензияНаименование,
		|	УчетныеЗаписиДокументооборота.ЛицензияДатаОкончания КАК ЛицензияДатаОкончания,
		|	ЕСТЬNULL(УчетныеЗаписиДокументооборота.ЛицензияТребуетсяНапоминаниеОбОкончанииСрокаДействия,
		|		ЛОЖЬ) КАК ЛицензияТребуетсяНапоминаниеОбОкончанииСрокаДействия,
		|	ЕСТЬNULL(УчетныеЗаписиДокументооборота.ЭлектроннаяПодписьВМоделиСервиса, ЛОЖЬ) КАК ЭлектроннаяПодписьВМоделиСервиса,
		|	УчетныеЗаписиДокументооборота.ДатаПодключения КАК ДатаПодключения,
		|	ЕСТЬNULL(ПоследнееЗаявлениеАбонентаСпецоператораСвязи.Ссылка,
		|		ЗНАЧЕНИЕ(Документ.ЗаявлениеАбонентаСпецоператораСвязи.ПустаяСсылка)) КАК ЗаявлениеАбонентаСпецоператораСвязи_Ссылка,
		|	ЕСТЬNULL(ПоследнееЗаявлениеАбонентаСпецоператораСвязи.ТипЗаявления,
		|		ЗНАЧЕНИЕ(Перечисление.ТипыЗаявленияАбонентаСпецоператораСвязи.ПустаяСсылка)) КАК ЗаявлениеАбонентаСпецоператораСвязи_ТипЗаявления,
		|	ЕСТЬNULL(ПоследнееЗаявлениеАбонентаСпецоператораСвязи.Статус,
		|		ЗНАЧЕНИЕ(Перечисление.СтатусыЗаявленияАбонентаСпецоператораСвязи.ПустаяСсылка)) КАК ЗаявлениеАбонентаСпецоператораСвязи_Статус,
		|	ЕСТЬNULL(ПоследнееЗаявлениеАбонентаСпецоператораСвязи.СтатусКомментарий,
		|		"""") КАК ЗаявлениеАбонентаСпецоператораСвязи_СтатусКомментарий,
		|	ЕСТЬNULL(ПоследнееЗаявлениеАбонентаСпецоператораСвязи.НастройкаЗавершена,
		|		ЛОЖЬ) КАК ЗаявлениеАбонентаСпецоператораСвязи_НастройкаЗавершена,
		|	ЕСТЬNULL(ПоследнееЗаявлениеАбонентаСпецоператораСвязи.НастройкаЭДОЗавершена,
		|		ЛОЖЬ) КАК ЗаявлениеАбонентаСпецоператораСвязи_НастройкаЭДОЗавершена,
		|	ЕСТЬNULL(ПоследнееЗаявлениеАбонентаСпецоператораСвязи.НастройкиЭДО,
		|		"""") КАК ЗаявлениеАбонентаСпецоператораСвязи_НастройкиЭДО,
		|	ПоследнееЗаявлениеАбонентаСпецоператораСвязи.Дата КАК ЗаявлениеАбонентаСпецоператораСвязи_Дата,
		|	ПервоеОдобленноеЗаявлениеАбонентаСпецоператораСвязи.ДатаПолученияОтвета КАК ЗаявлениеАбонентаСпецоператораСвязи_ДатаПолученияОтвета,
		|	ЕСТЬNULL(НастройкиОбменаФСС.ИспользоватьОбмен, ЛОЖЬ) КАК НастройкиОбменаФСС_ИспользоватьОбмен,
		|	ЕСТЬNULL(НастройкиОбменаФСС.СертификатСтрахователяОтпечаток, """") КАК НастройкиОбменаФСС_СертификатСтрахователяОтпечаток,
		|	ЕСТЬNULL(НастройкиОбменаФСС.ИспользоватьАвтонастройку, ЛОЖЬ) КАК НастройкиОбменаФСС_ИспользоватьАвтонастройку,
		|	ЕСТЬNULL(НастройкиОбменаФСРАР.ИспользоватьОбмен, ЛОЖЬ) КАК НастройкиОбменаФСРАР_ИспользоватьОбмен,
		|	ЕСТЬNULL(НастройкиОбменаФСРАР.СертификатАбонентаОтпечаток, """") КАК НастройкиОбменаФСРАР_СертификатАбонентаОтпечаток,
		|	ЕСТЬNULL(НастройкиОбменаФСРАР.ИспользоватьАвтонастройку, ЛОЖЬ) КАК НастройкиОбменаФСРАР_ИспользоватьАвтонастройку,
		|	ЕСТЬNULL(НастройкиОбменаРПН.ИспользоватьОбмен, ЛОЖЬ) КАК НастройкиОбменаРПН_ИспользоватьОбмен,
		|	ЕСТЬNULL(НастройкиОбменаРПН.СертификатАбонентаОтпечаток, """") КАК НастройкиОбменаРПН_СертификатАбонентаОтпечаток,
		|	ЕСТЬNULL(НастройкиОбменаРПН.ИспользоватьАвтонастройку, ЛОЖЬ) КАК НастройкиОбменаРПН_ИспользоватьАвтонастройку,
		|	ЕСТЬNULL(НастройкиОбменаФТС.ИспользоватьОбмен, ЛОЖЬ) КАК НастройкиОбменаФТС_ИспользоватьОбмен,
		|	ЕСТЬNULL(НастройкиОбменаФТС.СертификатАбонентаОтпечаток, """") КАК НастройкиОбменаФТС_СертификатАбонентаОтпечаток,
		|	ЕСТЬNULL(НастройкиОбменаФТС.ИспользоватьАвтонастройку, ЛОЖЬ) КАК НастройкиОбменаФТС_ИспользоватьАвтонастройку"
		+ ?(ПоддерживаетсяСдачаОтчетностиВБанкРоссии, ",
		|	" + БанкРоссииТекстыДляЗапроса.ПеречислениеКолонок, ",
		|	ЛОЖЬ КАК НастройкиОбменаБанкРоссии_ИспользоватьОбмен,
		|	"""" КАК НастройкиОбменаБанкРоссии_СертификатАбонентаОтпечаток")
		+ ?(ПоддерживаетсяСдачаОтчетностиВМинобороныРоссии, ",
		|	" + МинобороныРоссииТекстыДляЗапроса.ПеречислениеКолонок, ",
		|	ЛОЖЬ КАК НастройкиОбменаМинобороны_ИспользоватьОбмен,
		|	"""" КАК НастройкиОбменаМинобороны_СертификатАбонентаОтпечаток") + "
		|ИЗ
		|	Справочник.Организации КАК Организации
		|		ЛЕВОЕ СОЕДИНЕНИЕ Справочник.УчетныеЗаписиДокументооборота КАК УчетныеЗаписиДокументооборота
		|		ПО УчетныеЗаписиДокументооборота.Ссылка = Организации.УчетнаяЗаписьОбмена
		|			И Организации.ВидОбменаСКонтролирующимиОрганами =
		|			ЗНАЧЕНИЕ(Перечисление.ВидыОбменаСКонтролирующимиОрганами.ОбменВУниверсальномФормате)
		|		ЛЕВОЕ СОЕДИНЕНИЕ ПоследнееЗаявлениеАбонентаСпецоператораСвязи КАК ПоследнееЗаявлениеАбонентаСпецоператораСвязи
		|		ПО ПоследнееЗаявлениеАбонентаСпецоператораСвязи.Организация = Организации.Ссылка
		|		ЛЕВОЕ СОЕДИНЕНИЕ ПервоеОдобленноеЗаявлениеАбонентаСпецоператораСвязи КАК ПервоеОдобленноеЗаявлениеАбонентаСпецоператораСвязи
		|		ПО ПервоеОдобленноеЗаявлениеАбонентаСпецоператораСвязи.Организация = Организации.Ссылка
		|		ЛЕВОЕ СОЕДИНЕНИЕ РегистрСведений.НастройкиОбменаФСС КАК НастройкиОбменаФСС
		|		ПО НастройкиОбменаФСС.Организация = Организации.Ссылка
		|			И (НастройкиОбменаФСС.Пользователь ЕСТЬ NULL
		|			ИЛИ НастройкиОбменаФСС.Пользователь = ЗНАЧЕНИЕ(Справочник.Пользователи.ПустаяСсылка))
		|		ЛЕВОЕ СОЕДИНЕНИЕ РегистрСведений.НастройкиОбменаФСРАР КАК НастройкиОбменаФСРАР
		|		ПО НастройкиОбменаФСРАР.Организация = Организации.Ссылка
		|		ЛЕВОЕ СОЕДИНЕНИЕ РегистрСведений.НастройкиОбменаРПН КАК НастройкиОбменаРПН
		|		ПО НастройкиОбменаРПН.Организация = Организации.Ссылка
		|		ЛЕВОЕ СОЕДИНЕНИЕ РегистрСведений.НастройкиОбменаФТС КАК НастройкиОбменаФТС
		|		ПО НастройкиОбменаФТС.Организация = Организации.Ссылка"
		+ ?(ПоддерживаетсяСдачаОтчетностиВБанкРоссии, "
		|		" + БанкРоссииТекстыДляЗапроса.СоединениеСОрганизацией, "")
		+ ?(ПоддерживаетсяСдачаОтчетностиВМинобороныРоссии, "
		|		" + МинобороныРоссииТекстыДляЗапроса.СоединениеСОрганизацией, "")
		+ ?(Организации <> Неопределено, "
		|ГДЕ
		|	Организации.Ссылка В (&Организации)"
		+ ?(НастройкиВызова.ПолучатьДляУдаленныхОрганизаций, "", "
		|	И НЕ Организации.ПометкаУдаления"),
		?(НастройкиВызова.ПолучатьДляУдаленныхОрганизаций, "", "
		|ГДЕ
		|	НЕ Организации.ПометкаУдаления") + "
		|УПОРЯДОЧИТЬ ПО
		|	Организации.Наименование");
	
	Запрос.УстановитьПараметр("Организации", Организации);
	ТаблицаОрганизаций = Запрос.Выполнить().Выгрузить();
	ТаблицаОрганизаций.Колонки.Добавить("ИндексОрганизации");
	
	// пользователи базы
	
	СписокПользователейБазы = Новый СписокЗначений;
	
	Запрос = Новый Запрос;
	Запрос.Текст =
		"ВЫБРАТЬ РАЗРЕШЕННЫЕ
		|	Пользователи.Ссылка КАК Пользователь,
		|	Пользователи.Наименование КАК НаименованиеПользователя
		|ИЗ
		|	Справочник.Пользователи КАК Пользователи
		|ГДЕ
		|	НЕ Пользователи.ПометкаУдаления
		|	И НЕ Пользователи.Недействителен
		|	И НЕ Пользователи.Служебный
		|	И Пользователи.ИдентификаторПользователяИБ <> &ПустойИдентификаторПользователяИБ
		|УПОРЯДОЧИТЬ ПО
		|	Пользователи.Наименование";
	
	Запрос.УстановитьПараметр("ПустойИдентификаторПользователяИБ", Новый УникальныйИдентификатор("00000000-0000-0000-0000-000000000000"));
	Выборка = Запрос.Выполнить().Выбрать();
	
	ТекущийПользователь = Пользователи.ТекущийПользователь();
	Пока Выборка.Следующий() Цикл
		Если Выборка.Пользователь = ТекущийПользователь Тогда
			Картинка = БиблиотекаКартинок.Пользователь;
		Иначе
			Картинка = Неопределено;
		КонецЕсли;
		СписокПользователейБазы.Добавить(Выборка.Пользователь, Выборка.НаименованиеПользователя,, Картинка);
	КонецЦикла;
	
	Для ИндексОрганизации = 0 По ТаблицаОрганизаций.Количество() - 1 Цикл
		СвойстваОрганизации = ТаблицаОрганизаций[ИндексОрганизации];
		СвойстваОрганизации.ИндексОрганизации = ИндексОрганизации;
		
		Результат = Новый Структура;
		Для ИндексКолонки = 0 По ТаблицаОрганизаций.Колонки.Количество() - 1 Цикл
			Результат.Вставить(ТаблицаОрганизаций.Колонки[ИндексКолонки].Имя, СвойстваОрганизации[ИндексКолонки]);
		КонецЦикла;
		
		Результат.Вставить("НаименованиеОператора", "");
		Если ЗначениеЗаполнено(СвойстваОрганизации.СпецоператорСвязи) Тогда
			ИндексОператора = Перечисления.СпецоператорыСвязи.Индекс(СвойстваОрганизации.СпецоператорСвязи);
			Результат.НаименованиеОператора = Метаданные.Перечисления.СпецоператорыСвязи.ЗначенияПеречисления[ИндексОператора].Синоним;
		КонецЕсли;
		
		Организация = СвойстваОрганизации.Организация;
		
		// информация об организации
		
		Если НастройкиВызова.ПолучитьРеквизитыКонтролирующихОргановОрганизации Тогда
			ЭтоЮрЛицо = ЭлектронныйДокументооборотСКонтролирующимиОрганамиВызовСервера.ЭтоЮрЛицо(Организация);
			Результат.Вставить("ТипОрганизации", ЭтоЮрЛицо);
			ИПИспользуетТрудНаемныхРаботников = НЕ ЭтоЮрЛицо И РегламентированнаяОтчетность.ИПИспользуетТрудНаемныхРаботников(Организация);
			
			ЗапрашиваемыеРеквизиты = "КодНО, КодОрганаФСГС" + ?(ЭтоЮрЛицо, ", КППЮЛ", "")
				+ ?(ЭтоЮрЛицо ИЛИ ИПИспользуетТрудНаемныхРаботников,
				", РегНомПФР, РегистрационныйНомерФСС", ", РегНомПФРЗаСебя, РегистрационныйНомерФССДобрСтрах");
			
			СведенияОбОрганизации = РегламентированнаяОтчетностьВызовСервера.ПолучитьСведенияОбОрганизации(Организация,, ЗапрашиваемыеРеквизиты);
			Если НЕ ЭтоЮрЛицо Тогда
				Результат.Вставить("КППЮЛ", "");
			КонецЕсли;
			Если НЕ ЭтоЮрЛицо И НЕ ИПИспользуетТрудНаемныхРаботников Тогда
				СведенияОбОрганизации.Вставить("РегНомПФР", СведенияОбОрганизации.РегНомПФРЗаСебя);
				СведенияОбОрганизации.Удалить("РегНомПФРЗаСебя");
				СведенияОбОрганизации.Вставить("РегистрационныйНомерФСС", СведенияОбОрганизации.РегистрационныйНомерФССДобрСтрах);
				СведенияОбОрганизации.Удалить("РегистрационныйНомерФССДобрСтрах");
			КонецЕсли;
			ОбщегоНазначенияКлиентСервер.ДополнитьСтруктуру(Результат, СведенияОбОрганизации, Истина);
		КонецЕсли;
		
		// информация об учетной записи документооборота
		
		Результат.Вставить("ПравоЧтенияУчетнойЗаписиОбмена", ПравоЧтенияУчетныхЗаписейОбмена);
		
		Для каждого ТипПолучателя Из Метаданные.Перечисления.ТипыКонтролирующихОрганов.ЗначенияПеречисления Цикл
			Результат.Вставить("СдаватьВ" + ТипПолучателя.Имя, Ложь);
		КонецЦикла;
		Результат.Вставить("КодыФНС", 	Новый Массив);
		Результат.Вставить("КодПФР", 	"");
		Результат.Вставить("КодыФСГС", 	Новый Массив);
		Результат.Вставить("КодФСС", 	"");
		
		СписокПользователей = СписокПользователейБазы.Скопировать();
		Результат.Вставить("СписокПользователей", СписокПользователей);
		
		Если СвойстваОрганизации.ВидОбменаСКонтролирующимиОрганами = Перечисления.ВидыОбменаСКонтролирующимиОрганами.ОбменВУниверсальномФормате
			И ЗначениеЗаполнено(СвойстваОрганизации.УчетнаяЗаписьОбмена)
			И ПравоЧтенияУчетныхЗаписейОбмена Тогда
			
			УчетныеЗаписиОрганизаций.Добавить(СвойстваОрганизации.УчетнаяЗаписьОбмена);
			
			Результат.ПравоЧтенияУчетнойЗаписиОбмена = ЗначениеЗаполнено(СвойстваОрганизации.АдресЭлектроннойПочты) ИЛИ
				ЗначениеЗаполнено(СвойстваОрганизации.ИдентификаторАбонента) ИЛИ
				ЗначениеЗаполнено(СвойстваОрганизации.СпецоператорСвязи);
		КонецЕсли;
		
		Если НастройкиВызова.НайтиСертификатыПрочихКонтролирующихОрганов
			И СвойстваОрганизации.ВидОбменаСКонтролирующимиОрганами = Перечисления.ВидыОбменаСКонтролирующимиОрганами.ОбменВУниверсальномФормате
			И ЗначениеЗаполнено(СвойстваОрганизации.УчетнаяЗаписьОбмена)
			И НЕ Результат.ПравоЧтенияУчетнойЗаписиОбмена
			И (ЗначениеЗаполнено(Результат.НастройкиОбменаФСС_СертификатСтрахователяОтпечаток)
			ИЛИ ЗначениеЗаполнено(Результат.НастройкиОбменаФСРАР_СертификатАбонентаОтпечаток)
			ИЛИ ЗначениеЗаполнено(Результат.НастройкиОбменаРПН_СертификатАбонентаОтпечаток)
			ИЛИ ЗначениеЗаполнено(Результат.НастройкиОбменаФТС_СертификатАбонентаОтпечаток)
			ИЛИ ЗначениеЗаполнено(Результат.НастройкиОбменаБанкРоссии_СертификатАбонентаОтпечаток)
			ИЛИ ЗначениеЗаполнено(Результат.НастройкиОбменаМинобороны_СертификатАбонентаОтпечаток)) Тогда
			Результат.ЭлектроннаяПодписьВМоделиСервиса = ЭлектроннаяПодписьВМоделиСервисаБРОВызовСервера.РеквизитыУчетнойЗаписи(
				СвойстваОрганизации.УчетнаяЗаписьОбмена).ЭлектроннаяПодписьВМоделиСервиса;
		КонецЕсли;
		
		Результат.Вставить("СпособПодтвержденияКриптоопераций", Перечисления.СпособыПодтвержденияКриптоопераций.СессионныйТокен);
		Если Результат.ЭлектроннаяПодписьВМоделиСервиса = Истина
			И ЗначениеЗаполнено(Результат.СертификатРуководителя) Тогда
			ОтпечатокСертификата 	= Новый Структура("Отпечаток", Результат.СертификатРуководителя);
			СвойствоСертификата 	= ЭлектроннаяПодписьВМоделиСервиса.СвойстваРасшифрованияПодписанияСертификата(ОтпечатокСертификата);
			Результат.СпособПодтвержденияКриптоопераций = СвойствоСертификата.СпособПодтвержденияКриптоопераций;
		КонецЕсли;
		
		// учет информации из первого одобренного заявления
		
		Если НЕ ЗначениеЗаполнено(Результат.ДатаПодключения)
			ИЛИ (ЗначениеЗаполнено(Результат.ЗаявлениеАбонентаСпецоператораСвязи_ДатаПолученияОтвета)
			И Результат.ЗаявлениеАбонентаСпецоператораСвязи_ДатаПолученияОтвета < Результат.ДатаПодключения) Тогда
			
			Результат.ДатаПодключения = Результат.ЗаявлениеАбонентаСпецоператораСвязи_ДатаПолученияОтвета;
		КонецЕсли;
		
		ПоддерживаемыеНаправленияДляОператора = ?(ЗначениеЗаполнено(СвойстваОрганизации.СпецоператорСвязи)
			И ПоддерживаемыеНаправленияДляОператоров <> Неопределено,
			ПоддерживаемыеНаправленияДляОператоров[СвойстваОрганизации.СпецоператорСвязи], Неопределено);
		ПоддерживаемыеНаправления = ПоддерживаемыеНаправленияОрганизации(Организация, ПоддерживаемыеНаправленияДляОператора);
		ОбщегоНазначенияКлиентСервер.ДополнитьСтруктуру(Результат, ПоддерживаемыеНаправления, Истина);
		
		МассивРезультатов.Добавить(Результат);
	КонецЦикла;
	
	// получатели учетных записей
	
	Если УчетныеЗаписиОрганизаций.Количество() > 0 Тогда
		Запрос = Новый Запрос;
		Запрос.Текст =
			"ВЫБРАТЬ РАЗРЕШЕННЫЕ
			|	ДополнительныеРеквизитыУчетнойЗаписиПолучатели.УчетнаяЗапись,
			|	ДополнительныеРеквизитыУчетнойЗаписиПолучатели.ТипПолучателя,
			|	ДополнительныеРеквизитыУчетнойЗаписиПолучатели.КодПолучателя,
			|	ДополнительныеРеквизитыУчетнойЗаписиПолучатели.КПП
			|ИЗ
			|	РегистрСведений.ДополнительныеРеквизитыУчетнойЗаписиПолучатели КАК ДополнительныеРеквизитыУчетнойЗаписиПолучатели
			|ГДЕ
			|	ДополнительныеРеквизитыУчетнойЗаписиПолучатели.УчетнаяЗапись В (&УчетныеЗаписи)";
		
		Запрос.УстановитьПараметр("УчетныеЗаписи", УчетныеЗаписиОрганизаций);
		Выборка = Запрос.Выполнить().Выбрать();
		
		Пока Выборка.Следующий() Цикл
			Если ЗначениеЗаполнено(Выборка.ТипПолучателя) Тогда
				МетаданныеТипаПолучателя = Выборка.ТипПолучателя.Метаданные();
				ИндексТипаПолучателя = Перечисления[МетаданныеТипаПолучателя.Имя].Индекс(Выборка.ТипПолучателя);
				ИмяТипаПолучателя = МетаданныеТипаПолучателя.ЗначенияПеречисления[ИндексТипаПолучателя].Имя;
				
				ПараметрыОтбора = Новый Структура("УчетнаяЗаписьОбмена", Выборка.УчетнаяЗапись);
				МассивСвойствОрганизаций = ТаблицаОрганизаций.НайтиСтроки(ПараметрыОтбора);
				Для каждого СвойстваОрганизации Из МассивСвойствОрганизаций Цикл
					Если СвойстваОрганизации.ВидОбменаСКонтролирующимиОрганами =
						Перечисления.ВидыОбменаСКонтролирующимиОрганами.ОбменВУниверсальномФормате Тогда
						
						Результат = МассивРезультатов[СвойстваОрганизации.ИндексОрганизации];
						
						Результат.Вставить("СдаватьВ" + ИмяТипаПолучателя, Истина);
						
						Если Выборка.ТипПолучателя = Перечисления.ТипыКонтролирующихОрганов.ФНС Тогда
							КодФНС = Новый Структура("Код, КПП", Выборка.КодПолучателя, Выборка.КПП);
							Результат.КодыФНС.Добавить(КодФНС);
						ИначеЕсли Выборка.ТипПолучателя = Перечисления.ТипыКонтролирующихОрганов.ПФР Тогда
							Результат.КодПФР = Выборка.КодПолучателя;
						ИначеЕсли Выборка.ТипПолучателя = Перечисления.ТипыКонтролирующихОрганов.ФСГС Тогда
							Результат.КодыФСГС.Добавить(Выборка.КодПолучателя);
						КонецЕсли;
						
						МассивРезультатов[СвойстваОрганизации.ИндексОрганизации] = Результат;
					КонецЕсли;
				КонецЦикла;
			КонецЕсли;
		КонецЦикла;
		
		// пользователи учетных записей
		
		Запрос = Новый Запрос;
		Запрос.Текст =
			"ВЫБРАТЬ РАЗРЕШЕННЫЕ
			|	ПользователиУчетныхЗаписейДокументооборота.УчетнаяЗапись КАК УчетнаяЗапись,
			|	ПользователиУчетныхЗаписейДокументооборота.Пользователь КАК Пользователь
			|ИЗ
			|	РегистрСведений.ПользователиУчетныхЗаписейДокументооборота КАК ПользователиУчетныхЗаписейДокументооборота
			|ГДЕ
			|	ПользователиУчетныхЗаписейДокументооборота.УчетнаяЗапись В (&УчетныеЗаписи)";
		
		Запрос.УстановитьПараметр("УчетныеЗаписи", УчетныеЗаписиОрганизаций);
		Выборка = Запрос.Выполнить().Выбрать();
		
		Пока Выборка.Следующий() Цикл
			ПараметрыОтбора = Новый Структура("УчетнаяЗаписьОбмена", Выборка.УчетнаяЗапись);
			МассивСвойствОрганизаций = ТаблицаОрганизаций.НайтиСтроки(ПараметрыОтбора);
			Для каждого СвойстваОрганизации Из МассивСвойствОрганизаций Цикл
				Если СвойстваОрганизации.ВидОбменаСКонтролирующимиОрганами =
					Перечисления.ВидыОбменаСКонтролирующимиОрганами.ОбменВУниверсальномФормате Тогда
					
					Результат = МассивРезультатов[СвойстваОрганизации.ИндексОрганизации];
					
					ЭлементСпискаПользователей = Результат.СписокПользователей.НайтиПоЗначению(Выборка.Пользователь);
					Если ЭлементСпискаПользователей <> Неопределено Тогда
						ЭлементСпискаПользователей.Пометка = Истина;
					КонецЕсли;
					
					МассивРезультатов[СвойстваОрганизации.ИндексОрганизации] = Результат;
				КонецЕсли;
			КонецЦикла;
		КонецЦикла;
	КонецЕсли;
	
	Для каждого ЭлементРезультатов Из МассивРезультатов Цикл
		Состояние = СостояниеЭДОсПФР(ЭлементРезультатов.Организация);
		ЭлементРезультатов.Вставить("СостояниеЭДОсПФР", Состояние);
	КонецЦикла; 
	
	Результат = Новый Структура;
	Результат.Вставить("НастройкиОбмена", 							МассивРезультатов);
	Результат.Вставить("ТекущаяДатаНаСервере", 						ТекущаяДатаНаСервере);
	Результат.Вставить("ПоддерживаемыеНаправленияДляОператоров", 	ПоддерживаемыеНаправленияДляОператоров);
	Результат.Вставить("ПоддерживаетсяСдачаОтчетностиВБанкРоссии", 	ПоддерживаетсяСдачаОтчетностиВБанкРоссии);
	Результат.Вставить("ПоддерживаетсяСдачаОтчетностиВМинобороныРоссии", 	ПоддерживаетсяСдачаОтчетностиВМинобороныРоссии);
	Результат.Вставить("СписокПользователейБазы", 					СписокПользователейБазы);
	Возврат Результат;
	
КонецФункции

Функция ОрганизацииУчетнойЗаписи(УчетнаяЗапись)
	
	Запрос = Новый Запрос;
	Запрос.Текст =
		"ВЫБРАТЬ РАЗРЕШЕННЫЕ
		|	Организации.Ссылка КАК Ссылка
		|ИЗ
		|	Справочник.Организации КАК Организации
		|ГДЕ
		|	Организации.ВидОбменаСКонтролирующимиОрганами = ЗНАЧЕНИЕ(Перечисление.ВидыОбменаСКонтролирующимиОрганами.ОбменВУниверсальномФормате)
		|	И Организации.УчетнаяЗаписьОбмена = &УчетнаяЗаписьОбмена";
	
	Запрос.УстановитьПараметр("УчетнаяЗаписьОбмена", УчетнаяЗапись);
	ТаблицаЗапроса = Запрос.Выполнить().Выгрузить();
	
	Возврат ТаблицаЗапроса.ВыгрузитьКолонку("Ссылка");
	
КонецФункции

Процедура ЗаписатьПользователейУчетнойЗаписиДокументооборота(СписокПользователей, УчетнаяЗапись) Экспорт
	
	НаборЗаписей = РегистрыСведений.ПользователиУчетныхЗаписейДокументооборота.СоздатьНаборЗаписей();
	НаборЗаписей.Отбор.УчетнаяЗапись.Установить(УчетнаяЗапись);
	
	Для каждого СтрокаСписка Из СписокПользователей Цикл
		Если СтрокаСписка.Пометка Тогда
			НоваяСтрока = НаборЗаписей.Добавить();
			НоваяСтрока.УчетнаяЗапись = УчетнаяЗапись;
			НоваяСтрока.Пользователь = СтрокаСписка.Значение;
		КонецЕсли;
	КонецЦикла;
	
	Попытка
		НаборЗаписей.Записать();
	Исключение
		ТекстИсключения = ИнформацияОбОшибке().Описание;
		ТекстОшибки = СтрШаблон(
			НСтр("ru = 'Не удалось обновить список пользователей по учетной записи налогоплательщика ""%1"".'"),
			СокрЛП(УчетнаяЗапись));
		
		РегламентированнаяОтчетностьКлиентСервер.СообщитьОбОшибке(ТекстИсключения, Ложь, ТекстОшибки);
	КонецПопытки;
	
	ДокументооборотСКОВызовСервера.УстановитьПараметрСеансаТекущиеУчетныеЗаписиНалогоплательщика();
	
КонецПроцедуры

Функция ПоддерживаемыеНаправленияОператоров() Экспорт
	
	Результат = Новый Соответствие;
	
	Операторы = ПолучитьСписокСпецоператоровСвязи();
	
	СвойстваОператора = Новый Структура();
	СвойстваОператора.Вставить("ФСРАРПризнак");
	СвойстваОператора.Вставить("РПНПризнак");
	СвойстваОператора.Вставить("ФТСПризнак");
	
	Для каждого ОператорЭДО Из Операторы Цикл
		ПараметрыОператора = ПолучитьПараметрыСпецоператора(ОператорЭДО, СвойстваОператора);
		
		ПоддерживаемыеНаправления = Новый Структура;
		ПоддерживаемыеНаправления.Вставить("ПризнакПоддержкиФСРАР",
			ПараметрыОператора.ФСРАРПризнак = Истина ИЛИ ПараметрыОператора.ФСРАРПризнак = "Истина");
		ПоддерживаемыеНаправления.Вставить("ПризнакПоддержкиРПН",
			ПараметрыОператора.РПНПризнак = Истина ИЛИ ПараметрыОператора.РПНПризнак = "Истина");
		ПоддерживаемыеНаправления.Вставить("ПризнакПоддержкиФТС",
			ПараметрыОператора.ФТСПризнак = Истина ИЛИ ПараметрыОператора.ФТСПризнак = "Истина");
		
		Результат[ОператорЭДО] = ПоддерживаемыеНаправления;
	КонецЦикла;
	
	Возврат Результат;
	
КонецФункции

Функция ПоддерживаемыеНаправленияОрганизации(Организация, ПоддерживаемыеНаправленияДляОператора = Неопределено) Экспорт
	
	ПоддерживаемыеНаправленияВызова = Новый Структура;
	ПоддерживаемыеНаправленияВызова.Вставить("ПризнакПоддержкиФСРАР", 	Истина);
	ПоддерживаемыеНаправленияВызова.Вставить("ПризнакПоддержкиРПН", 	Истина);
	ПоддерживаемыеНаправленияВызова.Вставить("ПризнакПоддержкиФТС", 	Истина);
	Если ПоддерживаемыеНаправленияДляОператора <> Неопределено Тогда
		ЗаполнитьЗначенияСвойств(ПоддерживаемыеНаправленияВызова, ПоддерживаемыеНаправленияДляОператора);
	КонецЕсли;
	
	Направления = Новый Массив;
	
	ПараметрыНаправления = Новый Структура;
	ПараметрыНаправления.Вставить("Орган", 		Перечисления.ТипыКонтролирующихОрганов.ФНС);
	ПараметрыНаправления.Вставить("Доступен", 	Истина);
	ПараметрыНаправления.Вставить("Подключить", Истина);
	Направления.Добавить(ПараметрыНаправления);
	
	ПараметрыНаправления = Новый Структура;
	ПараметрыНаправления.Вставить("Орган", 		Перечисления.ТипыКонтролирующихОрганов.ПФР);
	ПараметрыНаправления.Вставить("Доступен", 	Истина);
	ПараметрыНаправления.Вставить("Подключить", Истина);
	Направления.Добавить(ПараметрыНаправления);
	
	ПараметрыНаправления = Новый Структура;
	ПараметрыНаправления.Вставить("Орган", 		Перечисления.ТипыКонтролирующихОрганов.ФСГС);
	ПараметрыНаправления.Вставить("Доступен", 	Истина);
	ПараметрыНаправления.Вставить("Подключить", Истина);
	Направления.Добавить(ПараметрыНаправления);
	
	ПараметрыНаправления = Новый Структура;
	ПараметрыНаправления.Вставить("Орган", 		Перечисления.ТипыКонтролирующихОрганов.ФСС);
	ПараметрыНаправления.Вставить("Доступен", 	Истина);
	ПараметрыНаправления.Вставить("Подключить", Истина);
	Направления.Добавить(ПараметрыНаправления);
	
	Если ПоддерживаемыеНаправленияВызова.ПризнакПоддержкиФСРАР Тогда
		ПараметрыНаправления = Новый Структура;
		ПараметрыНаправления.Вставить("Орган", 		Перечисления.ТипыКонтролирующихОрганов.ФСРАР);
		ПараметрыНаправления.Вставить("Доступен", 	Истина);
		ПараметрыНаправления.Вставить("Подключить", Истина);
		Направления.Добавить(ПараметрыНаправления);
	КонецЕсли;
	
	Если ПоддерживаемыеНаправленияВызова.ПризнакПоддержкиРПН Тогда
		ПараметрыНаправления = Новый Структура;
		ПараметрыНаправления.Вставить("Орган", 		Перечисления.ТипыКонтролирующихОрганов.РПН);
		ПараметрыНаправления.Вставить("Доступен", 	Истина);
		ПараметрыНаправления.Вставить("Подключить", Истина);
		Направления.Добавить(ПараметрыНаправления);
	КонецЕсли;
	
	Если ПоддерживаемыеНаправленияВызова.ПризнакПоддержкиФТС Тогда
		ПараметрыНаправления = Новый Структура;
		ПараметрыНаправления.Вставить("Орган", 		Перечисления.ТипыКонтролирующихОрганов.ФТС);
		ПараметрыНаправления.Вставить("Доступен", 	Истина);
		ПараметрыНаправления.Вставить("Подключить", Истина);
		Направления.Добавить(ПараметрыНаправления);
	КонецЕсли;
	
	ЭлектронныйДокументооборотСКонтролирующимиОрганамиПереопределяемый.ОпределитьПодключаемыеНаправленияСдачиОтчетности(Организация, Направления);
	
	Результат = Новый Структура;
	Результат.Вставить("ПризнакПоддержкиФНС", 	Ложь);
	Результат.Вставить("ПризнакПоддержкиПФР", 	Ложь);
	Результат.Вставить("ПризнакПоддержкиФСГС", 	Ложь);
	Результат.Вставить("ПризнакПоддержкиФСС", 	Ложь);
	Результат.Вставить("ПризнакПоддержкиФСРАР", Ложь);
	Результат.Вставить("ПризнакПоддержкиРПН", 	Ложь);
	Результат.Вставить("ПризнакПоддержкиФТС", 	Ложь);
	
	Для каждого НаправлениеЭДО Из Направления Цикл
		Если НаправлениеЭДО.Орган = Перечисления.ТипыКонтролирующихОрганов.ФНС Тогда
			Результат.ПризнакПоддержкиФНС = НаправлениеЭДО.Доступен;
			
		ИначеЕсли НаправлениеЭДО.Орган = Перечисления.ТипыКонтролирующихОрганов.ПФР Тогда
			Результат.ПризнакПоддержкиПФР = НаправлениеЭДО.Доступен;
			
		ИначеЕсли НаправлениеЭДО.Орган = Перечисления.ТипыКонтролирующихОрганов.ФСГС Тогда
			Результат.ПризнакПоддержкиФСГС = НаправлениеЭДО.Доступен;
			
		ИначеЕсли НаправлениеЭДО.Орган = Перечисления.ТипыКонтролирующихОрганов.ФСС Тогда
			Результат.ПризнакПоддержкиФСС = НаправлениеЭДО.Доступен;
			
		ИначеЕсли НаправлениеЭДО.Орган = Перечисления.ТипыКонтролирующихОрганов.ФСРАР
			И ПоддерживаемыеНаправленияВызова.ПризнакПоддержкиФСРАР Тогда
			
			Результат.ПризнакПоддержкиФСРАР = НаправлениеЭДО.Доступен;
			
		ИначеЕсли НаправлениеЭДО.Орган = Перечисления.ТипыКонтролирующихОрганов.РПН
			И ПоддерживаемыеНаправленияВызова.ПризнакПоддержкиРПН Тогда
			
			Результат.ПризнакПоддержкиРПН = НаправлениеЭДО.Доступен;
			
		ИначеЕсли НаправлениеЭДО.Орган = Перечисления.ТипыКонтролирующихОрганов.ФТС
			И ПоддерживаемыеНаправленияВызова.ПризнакПоддержкиФТС Тогда
			
			Результат.ПризнакПоддержкиФТС = НаправлениеЭДО.Доступен;
		КонецЕсли;
	КонецЦикла;
	
	Возврат Результат;
	
КонецФункции

Процедура СкорректироватьНаправленияСдачиОтчетностиСУчетомПредыдущихЗаявлений(
		НовыйДокументЗаявление,
		ТипКонтролирующегоОргана,
		КоличествоДней = 31) Экспорт
	
	Организация = НовыйДокументЗаявление.Организация;
	
	ДатаАвтоматическогоОтказа = ТекущаяДатаСеанса() - КоличествоДней * 24 * 60 * 60;
	ДатаАвтоматическогоОтказа = НачалоДня(ДатаАвтоматическогоОтказа);
	
	Запрос = Новый Запрос(
		"ВЫБРАТЬ РАЗРЕШЕННЫЕ
		|	ЗаявлениеАбонентаСпецоператораСвязи.Ссылка КАК Ссылка,
		|	ЗаявлениеАбонентаСпецоператораСвязи.Статус КАК Статус,
		|	ЗаявлениеАбонентаСпецоператораСвязи.НастройкаЗавершена КАК НастройкаЗавершена,
		|	ЗаявлениеАбонентаСпецоператораСвязи.ТипЗаявления КАК ТипЗаявления,
		|	ЗаявлениеАбонентаСпецоператораСвязи.РегНомерПФР КАК РегНомерПФР,
		|	ЗаявлениеАбонентаСпецоператораСвязи.ПодатьЗаявкуНаСертификатДляФСРАР КАК ПодатьЗаявкуНаСертификатДляФСРАР,
		|	ЗаявлениеАбонентаСпецоператораСвязи.КодРегионаФСРАР КАК КодРегионаФСРАР,
		|	ЗаявлениеАбонентаСпецоператораСвязи.ПодатьЗаявкуНаПодключениеРПН КАК ПодатьЗаявкуНаПодключениеРПН,
		|	ЗаявлениеАбонентаСпецоператораСвязи.ПодатьЗаявкуНаПодключениеФТС КАК ПодатьЗаявкуНаПодключениеФТС,
		|	ЗаявлениеАбонентаСпецоператораСвязи.ИзменившиесяРеквизитыВторичногоЗаявления КАК ИзменившиесяРеквизитыВторичногоЗаявления,
		|	ЗаявлениеАбонентаСпецоператораСвязи.Получатели КАК Получатели
		|ИЗ
		|	Документ.ЗаявлениеАбонентаСпецоператораСвязи КАК ЗаявлениеАбонентаСпецоператораСвязи
		|ГДЕ
		|	ЗаявлениеАбонентаСпецоператораСвязи.Организация = &Организация
		|	И НЕ ЗаявлениеАбонентаСпецоператораСвязи.ПометкаУдаления
		|	И (ЗаявлениеАбонентаСпецоператораСвязи.Статус = ЗНАЧЕНИЕ(Перечисление.СтатусыЗаявленияАбонентаСпецоператораСвязи.Отправлено)
		|		ИЛИ ЗаявлениеАбонентаСпецоператораСвязи.Статус = ЗНАЧЕНИЕ(Перечисление.СтатусыЗаявленияАбонентаСпецоператораСвязи.Одобрено))
		|	И ЗаявлениеАбонентаСпецоператораСвязи.ДатаОтправкиЗаявления >= &ДатаАвтоматическогоОтказа
		|	И (ЗаявлениеАбонентаСпецоператораСвязи.ТипЗаявления = ЗНАЧЕНИЕ(Перечисление.ТипыЗаявленияАбонентаСпецоператораСвязи.Первичное)
		|		ИЛИ ЗаявлениеАбонентаСпецоператораСвязи.ТипЗаявления = ЗНАЧЕНИЕ(Перечисление.ТипыЗаявленияАбонентаСпецоператораСвязи.Изменение)
		|		И ЗаявлениеАбонентаСпецоператораСвязи.ИзменившиесяРеквизитыВторичногоЗаявления.ИзмененныйРеквизит В (ЗНАЧЕНИЕ(Перечисление.ПараметрыПодключенияК1СОтчетности.СдаватьВФНС),
		|			ЗНАЧЕНИЕ(Перечисление.ПараметрыПодключенияК1СОтчетности.СдаватьВПФР),
		|			ЗНАЧЕНИЕ(Перечисление.ПараметрыПодключенияК1СОтчетности.СдаватьВРосстат),
		|			ЗНАЧЕНИЕ(Перечисление.ПараметрыПодключенияК1СОтчетности.СдаватьВФСС),
		|			ЗНАЧЕНИЕ(Перечисление.ПараметрыПодключенияК1СОтчетности.СдаватьВФСРАР),
		|			ЗНАЧЕНИЕ(Перечисление.ПараметрыПодключенияК1СОтчетности.СдаватьВРПН),
		|			ЗНАЧЕНИЕ(Перечисление.ПараметрыПодключенияК1СОтчетности.СдаватьВФТС)))
		|УПОРЯДОЧИТЬ ПО
		|	ЗаявлениеАбонентаСпецоператораСвязи.ДатаОтправкиЗаявления УБЫВ");
	Запрос.УстановитьПараметр("Организация", 				Организация);
	Запрос.УстановитьПараметр("ДатаАвтоматическогоОтказа", 	ДатаАвтоматическогоОтказа);
	
	Выборка = Запрос.Выполнить().Выбрать();
	Если Выборка.Следующий() И (Выборка.Статус <> Перечисления.СтатусыЗаявленияАбонентаСпецоператораСвязи.Одобрено
		ИЛИ НЕ Выборка.НастройкаЗавершена) Тогда
		
		// чтение направлений последнего заявления, если оно не было одобрено с завершением настройки
		
		СдачаВФНС 					= Ложь;
		КодыФНС 					= Новый Массив;
		СдачаВПФР 					= Ложь;
		ЕстьПолучательПФР 			= Ложь;
		КодПФР 						= "";
		ЗаданРегНомерПФР 			= Ложь;
		СдачаВФСГС 					= Ложь;
		КодыФСГС 					= Новый Массив;
		СдачаВФСС 					= Ложь;
		ЕстьПолучательФСС 			= Ложь;
		СдачаВФСРАР 				= Ложь;
		СдачаВРПН 					= Ложь;
		СдачаВФТС 					= Ложь;
		
		Если Выборка.ТипЗаявления = Перечисления.ТипыЗаявленияАбонентаСпецоператораСвязи.Первичное Тогда
			СдачаВФНС 				= Истина;
			СдачаВПФР 				= Истина;
			СдачаВФСГС 				= Истина;
			СдачаВФСС 				= Истина;
			СдачаВФСРАР 			= Истина;
			СдачаВРПН 				= Истина;
			СдачаВФТС 				= Истина;
			
		Иначе
			ТаблицаИзменившихсяРеквизитов = Выборка.ИзменившиесяРеквизитыВторичногоЗаявления.Выгрузить();
			Для каждого СтрокаТаблицы Из ТаблицаИзменившихсяРеквизитов Цикл
				Если СтрокаТаблицы.ИзмененныйРеквизит = Перечисления.ПараметрыПодключенияК1СОтчетности.СдаватьВФНС Тогда
					СдачаВФНС = Истина;
				ИначеЕсли СтрокаТаблицы.ИзмененныйРеквизит = Перечисления.ПараметрыПодключенияК1СОтчетности.СдаватьВПФР Тогда
					СдачаВПФР = Истина;
				ИначеЕсли СтрокаТаблицы.ИзмененныйРеквизит = Перечисления.ПараметрыПодключенияК1СОтчетности.РегНомерПФР Тогда
					ЗаданРегНомерПФР = Истина;
				ИначеЕсли СтрокаТаблицы.ИзмененныйРеквизит = Перечисления.ПараметрыПодключенияК1СОтчетности.СдаватьВРосстат Тогда
					СдачаВФСГС = Истина;
				ИначеЕсли СтрокаТаблицы.ИзмененныйРеквизит = Перечисления.ПараметрыПодключенияК1СОтчетности.СдаватьВФСС Тогда
					СдачаВФСС = Истина;
				ИначеЕсли СтрокаТаблицы.ИзмененныйРеквизит = Перечисления.ПараметрыПодключенияК1СОтчетности.СдаватьВФСРАР Тогда
					СдачаВФСРАР = Истина;
				ИначеЕсли СтрокаТаблицы.ИзмененныйРеквизит = Перечисления.ПараметрыПодключенияК1СОтчетности.СдаватьВРПН Тогда
					СдачаВРПН = Истина;
				ИначеЕсли СтрокаТаблицы.ИзмененныйРеквизит = Перечисления.ПараметрыПодключенияК1СОтчетности.СдаватьВФТС Тогда
					СдачаВФТС = Истина;
				КонецЕсли;
			КонецЦикла;
		КонецЕсли;
		
		Если СдачаВФНС ИЛИ СдачаВПФР ИЛИ СдачаВФСГС ИЛИ СдачаВФСС Тогда
			ТаблицаПолучателей = Выборка.Получатели.Выгрузить();
			
			Для каждого СтрокаТаблицы Из ТаблицаПолучателей Цикл
				Если СтрокаТаблицы.ТипПолучателя = Перечисления.ТипыКонтролирующихОрганов.ФНС И СдачаВФНС Тогда
					КодФНС = Новый Структура("КодПолучателя, КПП", СтрокаТаблицы.КодПолучателя, СтрокаТаблицы.КПП);
					КодыФНС.Добавить(КодФНС);
					
				ИначеЕсли СтрокаТаблицы.ТипПолучателя = Перечисления.ТипыКонтролирующихОрганов.ПФР И СдачаВПФР Тогда
					ЕстьПолучательПФР = Истина;
					КодПФР = СтрокаТаблицы.КодПолучателя;
					Если НЕ ЗаданРегНомерПФР Тогда
						ЗаданРегНомерПФР = ЗначениеЗаполнено(Выборка.РегНомерПФР);
					КонецЕсли;
					
				ИначеЕсли СтрокаТаблицы.ТипПолучателя = Перечисления.ТипыКонтролирующихОрганов.ФСГС И СдачаВФСГС Тогда
					КодыФСГС.Добавить(СтрокаТаблицы.КодПолучателя);
					
				ИначеЕсли СтрокаТаблицы.ТипПолучателя = Перечисления.ТипыКонтролирующихОрганов.ФСС И СдачаВФСС Тогда
					ЕстьПолучательФСС = Истина;
				КонецЕсли;
			КонецЦикла;
		КонецЕсли;
		
		// задание направлений в новом заявлении
		
		Если СдачаВФНС И ТипКонтролирующегоОргана <> Перечисления.ТипыКонтролирующихОрганов.ФНС Тогда
			НоваяСтрока = НовыйДокументЗаявление.ИзменившиесяРеквизитыВторичногоЗаявления.Добавить();
			НоваяСтрока.ИзмененныйРеквизит = Перечисления.ПараметрыПодключенияК1СОтчетности.КодыФНС;
			
			НоваяСтрока = НовыйДокументЗаявление.ИзменившиесяРеквизитыВторичногоЗаявления.Добавить();
			НоваяСтрока.ИзмененныйРеквизит = Перечисления.ПараметрыПодключенияК1СОтчетности.СдаватьВФНС;
			
			УдалитьПолучателейКонтролирующегоОргана(НовыйДокументЗаявление, Перечисления.ТипыКонтролирующихОрганов.ФНС);
			
			Для каждого КодФНС Из КодыФНС Цикл
				НоваяСтрокаНаправления = НовыйДокументЗаявление.Получатели.Добавить();
				НоваяСтрокаНаправления.ТипПолучателя 	= Перечисления.ТипыКонтролирующихОрганов.ФНС;
				НоваяСтрокаНаправления.КодПолучателя 	= КодФНС.КодПолучателя;
				НоваяСтрокаНаправления.КПП 				= КодФНС.КПП;
			КонецЦикла;
		КонецЕсли;
		
		Если СдачаВПФР И ТипКонтролирующегоОргана <> Перечисления.ТипыКонтролирующихОрганов.ПФР Тогда
			Если ЗаданРегНомерПФР Тогда
				НовыйДокументЗаявление.РегНомерПФР = Выборка.РегНомерПФР;
			КонецЕсли;
			
			НоваяСтрока = НовыйДокументЗаявление.ИзменившиесяРеквизитыВторичногоЗаявления.Добавить();
			НоваяСтрока.ИзмененныйРеквизит = Перечисления.ПараметрыПодключенияК1СОтчетности.СдаватьВПФР;
			
			УдалитьПолучателейКонтролирующегоОргана(НовыйДокументЗаявление, Перечисления.ТипыКонтролирующихОрганов.ПФР);
			
			Если ЕстьПолучательПФР Тогда
				НоваяСтрокаНаправления = НовыйДокументЗаявление.Получатели.Добавить();
				НоваяСтрокаНаправления.ТипПолучателя 	= Перечисления.ТипыКонтролирующихОрганов.ПФР;
				НоваяСтрокаНаправления.КодПолучателя 	= КодПФР;
				НоваяСтрокаНаправления.КПП 			 	= "";
			КонецЕсли;
		КонецЕсли;
		
		Если СдачаВФСГС И ТипКонтролирующегоОргана <> Перечисления.ТипыКонтролирующихОрганов.ФСГС Тогда
			НоваяСтрока = НовыйДокументЗаявление.ИзменившиесяРеквизитыВторичногоЗаявления.Добавить();
			НоваяСтрока.ИзмененныйРеквизит = Перечисления.ПараметрыПодключенияК1СОтчетности.КодРосстата;
			
			НоваяСтрока = НовыйДокументЗаявление.ИзменившиесяРеквизитыВторичногоЗаявления.Добавить();
			НоваяСтрока.ИзмененныйРеквизит = Перечисления.ПараметрыПодключенияК1СОтчетности.СдаватьВРосстат;
			
			УдалитьПолучателейКонтролирующегоОргана(НовыйДокументЗаявление, Перечисления.ТипыКонтролирующихОрганов.ФСГС);
			
			Для каждого КодФСГС Из КодыФСГС Цикл
				НоваяСтрокаНаправления = НовыйДокументЗаявление.Получатели.Добавить();
				НоваяСтрокаНаправления.ТипПолучателя 	= Перечисления.ТипыКонтролирующихОрганов.ФСГС;
				НоваяСтрокаНаправления.КодПолучателя 	= КодФСГС;
				НоваяСтрокаНаправления.КПП 			 	= "";
			КонецЦикла;
		КонецЕсли;
		
		Если СдачаВФСС И ТипКонтролирующегоОргана <> Перечисления.ТипыКонтролирующихОрганов.ФСС Тогда
			
			НоваяСтрока = НовыйДокументЗаявление.ИзменившиесяРеквизитыВторичногоЗаявления.Добавить();
			НоваяСтрока.ИзмененныйРеквизит = Перечисления.ПараметрыПодключенияК1СОтчетности.СдаватьВФСС;
			
			УдалитьПолучателейКонтролирующегоОргана(НовыйДокументЗаявление, Перечисления.ТипыКонтролирующихОрганов.ФСС);
			
			Если ЕстьПолучательФСС Тогда
				НоваяСтрокаНаправления = НовыйДокументЗаявление.Получатели.Добавить();
				НоваяСтрокаНаправления.ТипПолучателя 	= Перечисления.ТипыКонтролирующихОрганов.ФСС;
				НоваяСтрокаНаправления.КодПолучателя 	= "";
				НоваяСтрокаНаправления.КПП 			 	= "";
			КонецЕсли;
		КонецЕсли;
		
		Если СдачаВФСРАР И ТипКонтролирующегоОргана <> Перечисления.ТипыКонтролирующихОрганов.ФСРАР Тогда
			НовыйДокументЗаявление.ПодатьЗаявкуНаСертификатДляФСРАР = Выборка.ПодатьЗаявкуНаСертификатДляФСРАР;
			
			НовыйДокументЗаявление.КодРегионаФСРАР = Выборка.КодРегионаФСРАР;
			
			НоваяСтрока = НовыйДокументЗаявление.ИзменившиесяРеквизитыВторичногоЗаявления.Добавить();
			НоваяСтрока.ИзмененныйРеквизит = Перечисления.ПараметрыПодключенияК1СОтчетности.СдаватьВФСРАР;
		КонецЕсли;
		
		Если СдачаВРПН И ТипКонтролирующегоОргана <> Перечисления.ТипыКонтролирующихОрганов.РПН Тогда
			НовыйДокументЗаявление.ПодатьЗаявкуНаПодключениеРПН = Выборка.ПодатьЗаявкуНаПодключениеРПН;
			
			НоваяСтрока = НовыйДокументЗаявление.ИзменившиесяРеквизитыВторичногоЗаявления.Добавить();
			НоваяСтрока.ИзмененныйРеквизит = Перечисления.ПараметрыПодключенияК1СОтчетности.СдаватьВРПН;
		КонецЕсли;
		
		Если СдачаВФТС И ТипКонтролирующегоОргана <> Перечисления.ТипыКонтролирующихОрганов.ФТС Тогда
			НовыйДокументЗаявление.ПодатьЗаявкуНаПодключениеФТС = Выборка.ПодатьЗаявкуНаПодключениеФТС;
			
			НоваяСтрока = НовыйДокументЗаявление.ИзменившиесяРеквизитыВторичногоЗаявления.Добавить();
			НоваяСтрока.ИзмененныйРеквизит = Перечисления.ПараметрыПодключенияК1СОтчетности.СдаватьВФТС;
		КонецЕсли;
	КонецЕсли;
	
КонецПроцедуры

Процедура УдалитьПолучателейКонтролирующегоОргана(НовыйДокументЗаявление, ТипКонтролирующегоОргана) Экспорт
	
	ИндексПолучателя = НовыйДокументЗаявление.Получатели.Количество() - 1;
	Пока ИндексПолучателя >= 0 Цикл
		СтрокаНаправления = НовыйДокументЗаявление.Получатели[ИндексПолучателя];
		Если СтрокаНаправления.ТипПолучателя = ТипКонтролирующегоОргана Тогда
			НовыйДокументЗаявление.Получатели.Удалить(ИндексПолучателя);
		КонецЕсли;
		ИндексПолучателя = ИндексПолучателя - 1;
	КонецЦикла;
	
КонецПроцедуры

Функция ЗаявлениеСодержитМинимальноНеобходимыхПолучателей(НовыйДокументЗаявление) Экспорт
	
	Для каждого ПолучательЗаявления Из НовыйДокументЗаявление.Получатели Цикл
		Если ПолучательЗаявления.ТипПолучателя = Перечисления.ТипыКонтролирующихОрганов.ФНС Тогда
			Возврат Истина;
		ИначеЕсли ПолучательЗаявления.ТипПолучателя = Перечисления.ТипыКонтролирующихОрганов.ПФР Тогда
			Возврат Истина;
		КонецЕсли;
	КонецЦикла;
	
	Возврат Ложь;
	
КонецФункции

#КонецОбласти

#Область ПроверкаПервичногоЗаявления

Процедура ПроверитьСтатусПодключенияПередОтправкой(СсылкаНаОбъект, СообщениеОбОшибке)

	// Внимание!
	// ---------
	// В Контейнере есть одноименная процедура, которая используется при отправке отчетов
	// в интерактивном режиме пользователем.
	// При изменениях необходимо синхронно менять обе процедуры.
	
	СведенияОбОбъекте 	= СведенияПоОтправляемымОбъектам(СсылкаНаОбъект);
	Организация 		= СведенияОбОбъекте.Организация;
	
	ЗаявленияАбонента = ОбработкаЗаявленийАбонентаВызовСервера.ОтправленныеЗаявленияАбонентов(
		,
		Организация,
		Перечисления.ТипыЗаявленияАбонентаСпецоператораСвязи.Первичное);
		
	Если ЗаявленияАбонента.Количество() = 0 Тогда
		
		СообщениеОбОшибке = "ПоказатьФормуПредложениеОформитьЗаявлениеНаПодключение";
		
	Иначе
		
		ПоследнееЗаявлениеАбонента = ЗаявленияАбонента[0];
		
		Ответ = ОбработкаЗаявленийАбонентаВызовСервера.ПолучитьОтветСервераНаЗаявлениеАбонента(ПоследнееЗаявлениеАбонента);
		
		РезультатОтветаСервера 	= Ответ.РезультатОтветаСервера;
		РеквизитыДокумента 		= Ответ.РеквизитыДокумента;
		
		ЗаявлениеОтправлено = РеквизитыДокумента.Статус = Перечисления.СтатусыЗаявленияАбонентаСпецоператораСвязи.Отправлено;
		ЗаявлениеОтклонено  = РеквизитыДокумента.Статус = Перечисления.СтатусыЗаявленияАбонентаСпецоператораСвязи.Отклонено;
		ЗаявлениеОдобрено   = РеквизитыДокумента.Статус = Перечисления.СтатусыЗаявленияАбонентаСпецоператораСвязи.Одобрено;

		ОтсутствуетИнтернет = РезультатОтветаСервера.Свойство("СообщитьОбОтсутствииИнтернета");
		
		Если ОтсутствуетИнтернет Тогда
			
			СообщениеОбОшибке = "ПоказатьФормуПредложениеОформитьЗаявлениеНаПодключение";
			
		ИначеЕсли ЗаявлениеОтправлено Тогда
			
			СообщениеОбОшибке = "СообщениеОНеобработанномЗаявлении";
				
		ИначеЕсли ЗаявлениеОдобрено И НЕ РеквизитыДокумента.НастройкаЗавершена Тогда
			
			СообщениеОбОшибке = "СообщениеОбОдобренномЗаявлении";
			
		ИначеЕсли ЗаявлениеОтклонено И НЕ РеквизитыДокумента.НастройкаЗавершена Тогда
			
			СообщениеОбОшибке = "СообщениеОбОтклоненномЗаявлении";
			
		Иначе
			
			СообщениеОбОшибке = "ПоказатьФормуПредложениеОформитьЗаявлениеНаПодключение";
			
		КонецЕсли;
		 
	КонецЕсли;
	
КонецПроцедуры

#КонецОбласти

#Область НеподключенноеНаправление

Процедура СообщитьОНеподключенномНаправлении(СсылкаНаОбъект, СообщениеОбОшибке)
	
	// Внимание!
	// ---------
	// В Контейнере есть одноименная процедура, которая используется при отправке отчетов
	// в интерактивном режиме пользователем.
	// При изменениях необходимо синхронно менять обе процедуры.

	СведенияОбОбъекте 			= СведенияПоОтправляемымОбъектам(СсылкаНаОбъект);
	ВидКонтролирующегоОргана 	= СведенияОбОбъекте.ВидКонтролирующегоОргана;
	Организация					= СведенияОбОбъекте.Организация;
	
	// Если не подключено направление, то проверяем наличие отправленного заявления на изменение.
	// Если такого нет, то отправка невозможна и показывам предложение отправить заявление.
	// Если есть, то пытаемся завершить настройку ненастроенного заявления.
	
	ВходящийКонтекст = Новый Структура;
	ВходящийКонтекст.Вставить("СсылкаНаОбъект",           СсылкаНаОбъект);
	ВходящийКонтекст.Вставить("Организация", 			  Организация);
	ВходящийКонтекст.Вставить("ВидКонтролирующегоОргана", СведенияОбОбъекте.ВидКонтролирующегоОргана);
	ВходящийКонтекст.Вставить("КодКонтролирующегоОргана", СведенияОбОбъекте.КодКонтролирующегоОргана);
	
	ЗаявленияАбонента = ОтправленныеЗаявленияАбонентов(
		Организация,
		Перечисления.ТипыЗаявленияАбонентаСпецоператораСвязи.Изменение,
		ВидКонтролирующегоОргана);
		
	Если ЗаявленияАбонента.Количество() = 0 Тогда
		
		ПоказатьОшибкуНеподключенногоНаправления(ВходящийКонтекст, СообщениеОбОшибке);
		
	Иначе
		
		ПоследнееЗаявлениеАбонента = ЗаявленияАбонента[0];
		
		Ответ = ОбработкаЗаявленийАбонентаВызовСервера.ПолучитьОтветСервераНаЗаявлениеАбонента(ПоследнееЗаявлениеАбонента);
		
		РезультатОтветаСервера 	= Ответ.РезультатОтветаСервера;
		РеквизитыДокумента 		= Ответ.РеквизитыДокумента;
		
		ЗаявлениеОтправлено = РеквизитыДокумента.Статус = Перечисления.СтатусыЗаявленияАбонентаСпецоператораСвязи.Отправлено;
		ЗаявлениеОтклонено  = РеквизитыДокумента.Статус = Перечисления.СтатусыЗаявленияАбонентаСпецоператораСвязи.Отклонено;
		ЗаявлениеОдобрено   = РеквизитыДокумента.Статус = Перечисления.СтатусыЗаявленияАбонентаСпецоператораСвязи.Одобрено;
		
		ВходящийКонтекст.Вставить("РеквизитыДокумента", 	РеквизитыДокумента);
		ВходящийКонтекст.Вставить("РезультатОтветаСервера", РезультатОтветаСервера);
		ВходящийКонтекст.Вставить("Заявление", 				ПоследнееЗаявлениеАбонента);
		
		Если ЗаявлениеОтправлено Тогда
			
			// Аналог сообщения в форме СообщениеОНеобработанномЗаявлении.
			СообщениеОбОшибке = "СообщениеОНеобработанномЗаявлении";
				
		ИначеЕсли ЗаявлениеОдобрено И НЕ РеквизитыДокумента.НастройкаЗавершена Тогда
			
			// Аналог сообщения в форме СообщениеОбОдобренномЗаявлении.
			СообщениеОбОшибке = "СообщениеОбОдобренномЗаявлении";
			
		ИначеЕсли ЗаявлениеОтклонено И НЕ РеквизитыДокумента.НастройкаЗавершена Тогда
			
			// Аналог сообщения в форме СообщениеОбОтклоненномЗаявлении.
			СообщениеОбОшибке = "СообщениеОбОтклоненномЗаявлении";
			
		Иначе
			
			ПоказатьОшибкуНеподключенногоНаправления(ВходящийКонтекст, СообщениеОбОшибке);
			
		КонецЕсли;
		 
	КонецЕсли;	
	 
КонецПроцедуры

Процедура ПоказатьОшибкуНеподключенногоНаправления(ВходящийКонтекст, СообщениеОбОшибке)

	// Внимание!
	// ---------
	// В Контейнере есть одноименная процедура, которая используется при отправке отчетов
	// в интерактивном режиме пользователем.
	// При изменениях необходимо синхронно менять обе процедуры.
	
	Организация 					= ВходящийКонтекст.Организация;
	УчетнаяЗапись 					= УчетнаяЗаписьОрганизации(Организация);
	ЭтоУчетнаяЗаписьВМоделиСервиса 	= ЭлектроннаяПодписьВМоделиСервисаБРОВызовСервера.ЭтоЭлектроннаяПодписьВМоделиСервиса(УчетнаяЗапись);
	
	Если ЭтоУчетнаяЗаписьВМоделиСервиса Тогда

		СообщениеОбОшибке = "СообщениеОНеподключенномНаправлении";
		
	Иначе
		// Для отправки требуется клиентский сертификат, который доступен только из клиентского контекста,
		// отправка на сервере без обращения к клиентскому контексту невозможна.
		СообщениеОбОшибке = "ЭтоНеЭлектроннаяПодписьВМоделиСервиса";
	КонецЕсли;
	
КонецПроцедуры

Функция ЭтоЗаявлениеНаПодключениеНаправления(ДокументЗаявление, ВидКонтролирующегоОргана) Экспорт

	ПроверяемыеРеквизиты = ИзмененныеРеквизитыПоВидуКонтролирующегоОргана(ВидКонтролирующегоОргана);
	
	// Если количество реквизитов после объединения с удалением неуникальных меньше общего количества
	// до объединения, то значит в заявлении есть реквизиты из проверяемого списка.
	
	ИзменившиесяРеквизитыЗаявления = ДокументЗаявление.ИзменившиесяРеквизитыВторичногоЗаявления.Выгрузить().ВыгрузитьКолонку("ИзмененныйРеквизит");
	
	КоличествоРеквизитовЗаявления 	= ИзменившиесяРеквизитыЗаявления.Количество();
	КоличествоПроверяемыеРеквизиты 	= ПроверяемыеРеквизиты.Количество();
	ОбщееКоличествоРеквизитов 		= КоличествоРеквизитовЗаявления + КоличествоПроверяемыеРеквизиты;
	
	ОбщегоНазначенияКлиентСервер.ДополнитьМассив(ИзменившиесяРеквизитыЗаявления, ПроверяемыеРеквизиты, Истина);
	КоличествоРеквизитовПослеОбъединения = ИзменившиесяРеквизитыЗаявления.Количество();
	
	ЭтоПодключениеНаправления = КоличествоРеквизитовПослеОбъединения < ОбщееКоличествоРеквизитов;
	
	Возврат ЭтоПодключениеНаправления;
	
КонецФункции

Функция ОтправленныеЗаявленияАбонентов(Организация, ТипЗаявления, ВидКонтролирующегоОргана) Экспорт

	Если ТипЗаявления = Перечисления.ТипыЗаявленияАбонентаСпецоператораСвязи.Изменение Тогда
		ИзмененныеРеквизиты = ИзмененныеРеквизитыПоВидуКонтролирующегоОргана(ВидКонтролирующегоОргана);
	Иначе
		ИзмененныеРеквизиты = Неопределено;
	КонецЕсли;
	
	ЗаявленияАбонента = ОбработкаЗаявленийАбонентаВызовСервера.ОтправленныеЗаявленияАбонентов(
		,
		Организация,
		ТипЗаявления,
		ИзмененныеРеквизиты);

	Возврат ЗаявленияАбонента;

КонецФункции

Функция ИзмененныеРеквизитыПоВидуКонтролирующегоОргана(ВидКонтролирующегоОргана)
	
	ИзмененныеРеквизиты = Новый Массив;
	
	Если ВидКонтролирующегоОргана = Перечисления.ТипыКонтролирующихОрганов.ФНС Тогда
		
		ИзмененныеРеквизиты.Добавить(Перечисления.ПараметрыПодключенияК1СОтчетности.СдаватьВФНС);
		ИзмененныеРеквизиты.Добавить(Перечисления.ПараметрыПодключенияК1СОтчетности.КодыФНС);
		
	ИначеЕсли ВидКонтролирующегоОргана = Перечисления.ТипыКонтролирующихОрганов.ПФР Тогда
			
		ИзмененныеРеквизиты.Добавить(Перечисления.ПараметрыПодключенияК1СОтчетности.СдаватьВПФР);
		ИзмененныеРеквизиты.Добавить(Перечисления.ПараметрыПодключенияК1СОтчетности.КодПФР);
		
	ИначеЕсли ВидКонтролирующегоОргана = Перечисления.ТипыКонтролирующихОрганов.ФСС Тогда
			
		ИзмененныеРеквизиты.Добавить(Перечисления.ПараметрыПодключенияК1СОтчетности.СдаватьВФСС);
		
	ИначеЕсли ВидКонтролирующегоОргана = Перечисления.ТипыКонтролирующихОрганов.ФСГС Тогда
			
		ИзмененныеРеквизиты.Добавить(Перечисления.ПараметрыПодключенияК1СОтчетности.СдаватьВРосстат);
		ИзмененныеРеквизиты.Добавить(Перечисления.ПараметрыПодключенияК1СОтчетности.КодРосстата);
		
	ИначеЕсли ВидКонтролирующегоОргана = Перечисления.ТипыКонтролирующихОрганов.ФСРАР Тогда
			
		ИзмененныеРеквизиты.Добавить(Перечисления.ПараметрыПодключенияК1СОтчетности.СдаватьВФСРАР);
		
	ИначеЕсли ВидКонтролирующегоОргана = Перечисления.ТипыКонтролирующихОрганов.РПН Тогда
			
		ИзмененныеРеквизиты.Добавить(Перечисления.ПараметрыПодключенияК1СОтчетности.СдаватьВРПН);
		
	ИначеЕсли ВидКонтролирующегоОргана = Перечисления.ТипыКонтролирующихОрганов.ФТС Тогда
			
		ИзмененныеРеквизиты.Добавить(Перечисления.ПараметрыПодключенияК1СОтчетности.СдаватьВФТС);
		
	КонецЕсли;
	
	Возврат ИзмененныеРеквизиты;
	
КонецФункции

#КонецОбласти	

#Область АвтонастройкаУчетнойЗаписи

Процедура АвтоматическаяНастройкаУчетнойЗаписи(Знач УчетнаяЗапись, СообщениеОбОшибке) Экспорт

	// Внимание!
	// ---------
	// В Контейнере есть одноименная процедура, которая используется при отправке отчетов
	// в интерактивном режиме пользователем.
	// При изменениях необходимо синхронно менять обе процедуры.


	// Время предыдущего обращения к автонастройке при отправке на сервере не запоминается,
	// поэтому обновление автонастройки по времени на стороне сервера не проверяется.
	
	НастройкиУчетнойЗаписи = ПолучитьНастройкиУчетнойЗаписиИзИБ(УчетнаяЗапись);
	
	// Если настройки недоступны или учетка для обмена напрямую, то прервемся.
	Если НастройкиУчетнойЗаписи = Неопределено ИЛИ НастройкиУчетнойЗаписи.ОбменНапрямую Тогда
		Возврат;
	КонецЕсли;
	
	ОбновленияПризнак = ЭлектронныйДокументооборотСКонтролирующимиОрганамиВызовСервера.ПолучитьПараметрСпецоператора(НастройкиУчетнойЗаписи.СпецОператорСвязи, "ОбновленияПризнак");
	
	Если НастройкиУчетнойЗаписи.РежимАвтонастройки = Перечисления.РежимыАвтонастройкиУчетнойЗаписиНалогоплательщика.Отключена
		ИЛИ НастройкиУчетнойЗаписи.РежимАвтонастройки = Перечисления.РежимыАвтонастройкиУчетнойЗаписиНалогоплательщика.ПустаяСсылка() Тогда
			
		Возврат;
			
	КонецЕсли;
	
	// В настоящее время автонастройка учетной записи при отправке на стороне сервера
	// доступна только для спецоператора "Калуга Астрал".
	
	Если ОбновленияПризнак <> Неопределено И ОбновленияПризнак Тогда
		
		ИдентификаторАбонента = НастройкиУчетнойЗаписи.ИдентификаторАбонента;
		ИдентификаторСпецоператора = НастройкиУчетнойЗаписи.ИдентификаторСпецоператора;
		ИдентификаторАбонента = Нрег(Сред(ИдентификаторАбонента, СтрДлина(ИдентификаторСпецоператора)+1));
		
		ПараметрыОбработатьОбновление = ПараметрыОбработатьОбновление();
		ПараметрыОбработатьОбновление.ЭтоЭлектроннаяПодписьВМоделиСервиса 			= НастройкиУчетнойЗаписи.ЭлектроннаяПодписьВМоделиСервиса;
		ПараметрыОбработатьОбновление.ИдентификаторДокументооборота 				= НастройкиУчетнойЗаписи.ИдентификаторДокументооборота;
		ПараметрыОбработатьОбновление.ТелефонМобильныйДляАвторизации 				= НастройкиУчетнойЗаписи.ТелефонМобильныйДляАвторизации;
		ПараметрыОбработатьОбновление.ИдентификаторАбонента                         = ИдентификаторАбонента;
		ПараметрыОбработатьОбновление.СпецоператорСвязи                             = НастройкиУчетнойЗаписи.СпецОператорСвязи;
		ПараметрыОбработатьОбновление.УчетнаяЗапись                                 = УчетнаяЗапись;
		ПараметрыОбработатьОбновление.ТихийРежим                                    = Истина;
		ПараметрыОбработатьОбновление.РежимАвтонастройки                            = Истина;
		ПараметрыОбработатьОбновление.ВызовИзМастераПодключенияК1СОтчетности 		= Ложь;
		ПараметрыОбработатьОбновление.ТекстОшибокДляМастераПодключенияК1СОтчетности = "";
		ПараметрыОбработатьОбновление.ОбращениеКСерверуОтключено 					= Ложь;		
		ПараметрыОбработатьОбновление.СохранениеВИБОтключено 						= Ложь;		
		ПараметрыОбработатьОбновление.СтруктураДляИнформацииОСервереОнлайнПроверки  = Неопределено;		
		ПараметрыОбработатьОбновление.ВыполнятьРасшифровкуЗашифрованнойЧастиФайлаАвтонастроки = Истина;
		ПараметрыОбработатьОбновление.ИгнорироватьРежимАвтонастройки				= Ложь;		
		
		ОбработатьОбновление(ПараметрыОбработатьОбновление, СообщениеОбОшибке);
		
	КонецЕсли;
	
КонецПроцедуры

Процедура ОбработатьОбновление(ПараметрыФункции, СообщениеОбОшибке)

	// Внимание!
	// ---------
	// В Контейнере есть одноименная процедура, которая используется при отправке отчетов
	// в интерактивном режиме пользователем.
	// При изменениях необходимо синхронно менять обе процедуры.

	ВыводитьСообщения = Истина; // Сообщения выводим для перехвата и отправки вызывающему сервису.

	ПараметрыРегистрацияПолучитьДанныеССервера = ПараметрыРегистрацияПолучитьДанныеССервера();
	ЗаполнитьЗначенияСвойств(ПараметрыРегистрацияПолучитьДанныеССервера, ПараметрыФункции);
	ПараметрыРегистрацияПолучитьДанныеССервера.ФайлИзКэша = Ложь;
	ПараметрыРегистрацияПолучитьДанныеССервера.ВыводитьСообщения = ВыводитьСообщения;
	ПараметрыРегистрацияПолучитьДанныеССервера.ВременныйФайлОбновленийАдрес = Новый УникальныйИдентификатор();
	ПараметрыРегистрацияПолучитьДанныеССервера.ДанныеУзлаКонфиденциальнаяИнформацияАдрес = Новый УникальныйИдентификатор();
	
	Сертификаты = Неопределено; // Будет инициализирована после загрузки сертификатов.
	
	Результат = РегистрацияПолучитьДанныеССервера(ПараметрыРегистрацияПолучитьДанныеССервера, Ложь, Сертификаты);
	Если НЕ Результат Тогда
		// Обновление настроек учетной записи не является обязательным для отправки отчета.
		// Если оно по каким-то причинам не выполнено, то попробуем отправить отчет с существующими настройками.
		// Если в ходе отправки возникут ошибки (например, истек срок действия сертификата), то об этом будет выдано отдельное сообщение.
		// Текущую проблему регистрируем в журнале, но не прерываем дальнейшую отправку.
		
		ЗаписатьСобытиеВЖурнал(УровеньЖурналаРегистрации.Предупреждение, НСтр("ru = 'Не удалось обратиться к серверу обновлений'"));
		Возврат;
	КонецЕсли;
		
	ПараметрыАвторизации = Новый Структура;
	Если ЗначениеЗаполнено(ПараметрыФункции.УчетнаяЗапись) Тогда
		ПараметрыАвторизации.Вставить("УчетнаяЗапись", ПараметрыФункции.УчетнаяЗапись);
	Иначе
		ПараметрыАвторизации.Вставить("ИдентификаторДокументооборота", ПараметрыФункции.ИдентификаторДокументооборота);
		ПараметрыАвторизации.Вставить("ТелефонМобильныйДляАвторизации", ПараметрыФункции.ТелефонМобильныйДляАвторизации);
	КонецЕсли;

	ЗаполнитьЗначенияСвойств(ПараметрыФункции, ПараметрыРегистрацияПолучитьДанныеССервера);
	ФайлИзКэша = ПараметрыРегистрацияПолучитьДанныеССервера.ФайлИзКэша;
	ДанныеУзлаКонфиденциальнаяИнформацияАдрес = ПараметрыРегистрацияПолучитьДанныеССервера.ДанныеУзлаКонфиденциальнаяИнформацияАдрес;
	
	СвойствоКриптосообщения = ЭтоСообщениеЗашифрованоНаОблачныйСертификат(ДанныеУзлаКонфиденциальнаяИнформацияАдрес);
	Если СвойствоКриптосообщения <> Неопределено Тогда 
		ПараметрыФункции.ЭтоЭлектроннаяПодписьВМоделиСервиса = СвойствоКриптосообщения;
	КонецЕсли;
	
	Если Не ПараметрыФункции.ЭтоЭлектроннаяПодписьВМоделиСервиса Тогда
		// Если не используется электронная подпись в модели сервиса, то автоматическая отправка на стороне сервера не поддерживается.
		СообщениеОбОшибке = "ЭтоНеЭлектроннаяПодписьВМоделиСервиса";
		Возврат;
	КонецЕсли;

	// Установка сертификатов
	// Среди пришедших в рег файле отпечатков ищем тот, который пришел в ответе.
	// Если отпечатки совпадают, то устанавливаем сертификат, иначе пропускаем установку сертифката
	УстанавливатьСертификат = Истина;
	ЭтоВторичноеЗаявление = ПараметрыФункции.ЭтоВторичноеЗаявление;
	ОтпечатокСертификатаИзОтвета = ПараметрыФункции.ОтпечатокСертификатаИзОтвета;
	Если ЭтоВторичноеЗаявление Тогда
		УстанавливатьСертификат = УстанавливатьСертификат(ОтпечатокСертификатаИзОтвета, Сертификаты);
	Иначе
		УстанавливатьСертификат = Истина;
	КонецЕсли;
	
	// Установка сертификатов
	Если УстанавливатьСертификат Тогда
		ПараметрыРегистрацияУстановитьСертификаты = ПараметрыРегистрацияУстановитьСертификаты();
		ЗаполнитьЗначенияСвойств(ПараметрыРегистрацияУстановитьСертификаты, ПараметрыФункции);
	
		ОтпечатокСертификата = Неопределено; // Будет загружен в процессе установки сертификатов.
		РезультатУстановкиСертификатов = РегистрацияУстановитьСертификаты(ОтпечатокСертификата, Сертификаты);		
	Иначе
		ОтпечатокСертификата = ОтпечатокСертификатаИзОтвета;
		РезультатУстановкиСертификатов = Ложь;
	КонецЕсли;

	// Расшифровка данных, хранящихся в зашифрованной области рег. файла
	РезультатРасшифровки 		= Истина;
	АдресВоВременномХранилище 	= ДанныеУзлаКонфиденциальнаяИнформацияАдрес; // Расшифрованные данные помещаются по тому же адресу, где были до расшифровки.
	
	Если РезультатУстановкиСертификатов И Не ФайлИзКэша Тогда

		ВыполнятьРасшифровку = ПараметрыФункции.ВыполнятьРасшифровкуЗашифрованнойЧастиФайлаАвтонастроки;
		
		// Расшифровка
		Если ВыполнятьРасшифровку Тогда
			// Расшифровка на стороне сервера возможна только в случае электронной подписи в модели сервиса.
			// При этом сертификат используется из хранилища сертификатов.
			СообщениеОбОшибкеРасшифровки = "";
			РасшифроватьPKCS7(ДанныеУзлаКонфиденциальнаяИнформацияАдрес, СообщениеОбОшибкеРасшифровки);
			Если ЗначениеЗаполнено(СообщениеОбОшибкеРасшифровки) Тогда
				// Если в случае ошибки расшифровка не выполнена, то зарегистрируем ее, но не прерываемся, а передаем результат расшифровки в последующие процедуры.
				СообщениеОбОшибкеРасшифровки = СтрШаблон("Ошибка при расшифровке рег.данных в процессе автонастройки учетной записи: %1", СообщениеОбОшибкеРасшифровки);
				ЗаписатьСобытиеВЖурнал(УровеньЖурналаРегистрации.Предупреждение, СообщениеОбОшибкеРасшифровки);
				РезультатРасшифровки = Ложь;
			КонецЕсли;
		КонецЕсли;
		
	КонецЕсли;

	Если УстанавливатьСертификат Тогда
		РезультатРегистрацияУстановитьСертификаты = Новый Структура("РезультатВыполнения, ПараметрыФункции", РезультатУстановкиСертификатов, ПараметрыРегистрацияУстановитьСертификаты);
	Иначе
		РезультатРегистрацияУстановитьСертификаты = Новый Структура("РезультатВыполнения, ПараметрыФункции", Истина, Неопределено);
	КонецЕсли;
		
	ОбработатьОбновлениеПослеКриптографииЗавершение(
		АдресВоВременномХранилище, 
		Неопределено, 
		ВыводитьСообщения, 
		ПараметрыРегистрацияПолучитьДанныеССервера, 
		ПараметрыФункции, 
		Результат, 
		РезультатРасшифровки, 
		РезультатУстановкиСертификатов, 
		УстанавливатьСертификат, 
		ФайлИзКэша,
		РезультатРегистрацияУстановитьСертификаты,
		Новый Структура);
	
КонецПроцедуры

Процедура ОбработатьОбновлениеПослеКриптографииЗавершение(АдресВоВременномХранилище, ВременныйКаталог, ВыводитьСообщения, 
	ПараметрыРегистрацияПолучитьДанныеССервера, ПараметрыФункции, Результат, РезультатРасшифровки, РезультатУстановкиСертификатов, УстанавливатьСертификат, ФайлИзКэша,
	ТрансляцияОшибокУстановкиСертификатов = Неопределено,
	ТрансляцияОшибокРасшифровки = Неопределено)

	// Внимание!
	// ---------
	// В Контейнере есть одноименная процедура, которая используется при отправке отчетов
	// в интерактивном режиме пользователем.
	// При изменениях необходимо синхронно менять обе процедуры.

	// Обновление информации в справочниках
	Если РезультатУстановкиСертификатов И РезультатРасшифровки Тогда
		
		ПараметрыРегистрацияОбновитьСправочники = ПараметрыРегистрацияОбновитьСправочники();
		ЗаполнитьЗначенияСвойств(ПараметрыРегистрацияОбновитьСправочники, ПараметрыФункции);
		
		ПараметрыРегистрацияОбновитьСправочники.ВременныйФайлОбновленийАдрес 	= ПараметрыРегистрацияПолучитьДанныеССервера.ВременныйФайлОбновленийАдрес;
		ПараметрыРегистрацияОбновитьСправочники.АдресВоВременномХранилище 		= АдресВоВременномХранилище;
		ПараметрыРегистрацияОбновитьСправочники.ФайлИзКэша 						= ФайлИзКэша;
		
		РезультатОбновленияСправочников = РегистрацияОбновитьСправочники(ПараметрыРегистрацияОбновитьСправочники);
		ЗаполнитьЗначенияСвойств(ПараметрыФункции, ПараметрыРегистрацияОбновитьСправочники);
		
		ЭтоНоваяУчетнаяЗапись = ПараметрыРегистрацияОбновитьСправочники.ЭтоНоваяУчетнаяЗапись;
		СтруктураАвтонастройкиФСС = ПараметрыРегистрацияОбновитьСправочники.СтруктураАвтонастройкиФСС;
		СтруктураАвтонастройкиРПН = ПараметрыРегистрацияОбновитьСправочники.СтруктураАвтонастройкиРПН;
		СтруктураАвтонастройкиФТС = ПараметрыРегистрацияОбновитьСправочники.СтруктураАвтонастройкиФТС;
		
	КонецЕсли;
	
	// Сохранение дополнительных сведений по учетной записи в регистр
	ПараметрыСертификата = Неопределено;
	Если (РезультатУстановкиСертификатов ИЛИ НЕ УстанавливатьСертификат) Тогда
		ПараметрыУчетнойЗаписи = ПараметрыФункции.УчетнаяЗапись;
		ДополнительныеПараметры = Новый Структура;
		ДополнительныеПараметры.Вставить("ВременныйКаталог", ВременныйКаталог);
		ДополнительныеПараметры.Вставить("ВыводитьСообщения", ВыводитьСообщения);
		ДополнительныеПараметры.Вставить("ПараметрыРегистрацияПолучитьДанныеССервера", ПараметрыРегистрацияПолучитьДанныеССервера);
		ДополнительныеПараметры.Вставить("ПараметрыФункции", ПараметрыФункции);
		ДополнительныеПараметры.Вставить("Результат", Результат);
		ДополнительныеПараметры.Вставить("РезультатРасшифровки", РезультатРасшифровки);
		ДополнительныеПараметры.Вставить("РезультатУстановкиСертификатов", РезультатУстановкиСертификатов);
		ДополнительныеПараметры.Вставить("УстанавливатьСертификат", УстанавливатьСертификат);
		ДополнительныеПараметры.Вставить("ПараметрыУчетнойЗаписи", ПараметрыУчетнойЗаписи);
		ДополнительныеПараметры.Вставить("РезультатОбновленияСправочников", РезультатОбновленияСправочников);
		ДополнительныеПараметры.Вставить("СтруктураАвтонастройкиФСС", СтруктураАвтонастройкиФСС);
		ДополнительныеПараметры.Вставить("СтруктураАвтонастройкиРПН", СтруктураАвтонастройкиРПН);
		ДополнительныеПараметры.Вставить("СтруктураАвтонастройкиФТС", СтруктураАвтонастройкиФТС);
		ДополнительныеПараметры.Вставить("ЭтоНоваяУчетнаяЗапись", ЭтоНоваяУчетнаяЗапись);
		
		Попытка
			ОтпечатокСертификатаУчетнойЗаписи = ПолучитьОтпечаток(ПараметрыУчетнойЗаписи, "Руководитель", Неопределено);
			Если ЗначениеЗаполнено(ОтпечатокСертификатаУчетнойЗаписи) Тогда
				// Автонастройка сертификатов на стороне сервера возможна только в случае электронной подписи в модели сервиса.
				ПараметрыСертификата = ПолучитьСвойстваСертификатаСубъектаНаСервере(ОтпечатокСертификатаУчетнойЗаписи);
			КонецЕсли;
		Исключение
			ПараметрыСертификата = Неопределено;
		КонецПопытки;
		РезультатВыполнения = Новый Структура("Выполнено, МенеджерКриптографии, СертификатНайден, СвойстваСертификата", Истина, Неопределено, ПараметрыСертификата <> Неопределено, ПараметрыСертификата);
		ОбработатьОбновлениеПослеПоискаСертификатаЗавершение(РезультатВыполнения, ДополнительныеПараметры);
		
	КонецЕсли;
	
	// В настоящее время автонастройка учетной записи при вызове из серверного контекста поддерживается только для направления ФНС, ПФР и ФСС.
	// Для них дополнительных действий больше не требуется, этап автонастройки завершен.
	// Возникающие в процессе автонастройки сообщения о проблемах не являются блокирующими для последующей отправки.
	// В интерактивном режиме работы они выводятся пользователю.
	// В случае вызова автонастройки из серверного контекста они просто игнорируются.
	Если СтруктураАвтонастройкиРПН <> Неопределено
		ИЛИ СтруктураАвтонастройкиФТС <> Неопределено Тогда
		ВызватьИсключение НСтр("ru = 'ОбработатьОбновлениеПослеКриптографииЗавершение(): автонастройка учетной записи поддерживается только для ФНС, ПФР и ФСС'");
	КонецЕсли;
	
КонецПроцедуры

Процедура ОбработатьОбновлениеПослеПоискаСертификатаЗавершение(РезультатВыполнения, ДополнительныеПараметры)

	// Внимание!
	// ---------
	// В Контейнере есть одноименная процедура, которая используется при отправке отчетов
	// в интерактивном режиме пользователем.
	// При изменениях необходимо синхронно менять обе процедуры.
	
	ПараметрыРегистрацияПолучитьДанныеССервера = ДополнительныеПараметры.ПараметрыРегистрацияПолучитьДанныеССервера;
	ПараметрыФункции = ДополнительныеПараметры.ПараметрыФункции;
	РезультатУстановкиСертификатов = ДополнительныеПараметры.РезультатУстановкиСертификатов;
	УстанавливатьСертификат = ДополнительныеПараметры.УстанавливатьСертификат;
	ПараметрыУчетнойЗаписи = ДополнительныеПараметры.ПараметрыУчетнойЗаписи;
	СтруктураАвтонастройкиФСС = ДополнительныеПараметры.СтруктураАвтонастройкиФСС;
	РезультатОбновленияСправочников = ДополнительныеПараметры.РезультатОбновленияСправочников;
	РезультатРасшифровки = ДополнительныеПараметры.РезультатРасшифровки;
	
	ПараметрыСертификата = ?(РезультатВыполнения.Выполнено И РезультатВыполнения.СертификатНайден, РезультатВыполнения.СвойстваСертификата, Неопределено);
	
	Если (РезультатУстановкиСертификатов ИЛИ НЕ УстанавливатьСертификат) Тогда
		ПараметрыСохранитьДополнительныеРеквизитыУчетнойЗаписи 													= ПараметрыСохранитьДополнительныеРеквизитыУчетнойЗаписи();
		ПараметрыСохранитьДополнительныеРеквизитыУчетнойЗаписи.УчетнаяЗаписьОрганизации 						= ПараметрыУчетнойЗаписи;
		ПараметрыСохранитьДополнительныеРеквизитыУчетнойЗаписи.ПараметрыСертификата 							= ПараметрыСертификата;
		ПараметрыСохранитьДополнительныеРеквизитыУчетнойЗаписи.ИдентификаторАбонента 							= ПараметрыФункции.ИдентификаторАбонента;
		ПараметрыСохранитьДополнительныеРеквизитыУчетнойЗаписи.ВременныйФайлОбновленийАдрес 					= ПараметрыРегистрацияПолучитьДанныеССервера.ВременныйФайлОбновленийАдрес;
		ПараметрыСохранитьДополнительныеРеквизитыУчетнойЗаписи.ВызовИзМастераПодключенияК1СОтчетности 			= ПараметрыФункции.ВызовИзМастераПодключенияК1СОтчетности;
		ПараметрыСохранитьДополнительныеРеквизитыУчетнойЗаписи.ТекстОшибокДляМастераПодключенияК1СОтчетности 	= ПараметрыФункции.ТекстОшибокДляМастераПодключенияК1СОтчетности;
		
		СохранитьДополнительныеРеквизитыУчетнойЗаписи(ПараметрыСохранитьДополнительныеРеквизитыУчетнойЗаписи);
		ПараметрыФункции.ТекстОшибокДляМастераПодключенияК1СОтчетности = ПараметрыСохранитьДополнительныеРеквизитыУчетнойЗаписи.ТекстОшибокДляМастераПодключенияК1СОтчетности;
	КонецЕсли;

	Если (РезультатУстановкиСертификатов ИЛИ НЕ УстанавливатьСертификат) И РезультатРасшифровки И РезультатОбновленияСправочников Тогда
		// производим настройку параметров ФСС
		Если СтруктураАвтонастройкиФСС <> Неопределено И ПараметрыФункции.Организация <> Неопределено Тогда
			ФлагАвтозаполнения = СтруктураАвтонастройкиФСС.ФлагАвтозаполнения;
			Если ФлагАвтозаполнения Тогда
				ОбновитьРегистрыСведенийНастройкиОбменаФСС(
						ПараметрыФункции.Организация,
						СтруктураАвтонастройкиФСС.ОтпечатокСертификата,
						СтруктураАвтонастройкиФСС.СертификатФССОтпечаток,
						СтруктураАвтонастройкиФСС.СертификатФССЭЛНОтпечаток,
						Ложь);
			КонецЕсли;
		КонецЕсли;
	КонецЕсли;
	
КонецПроцедуры

Функция УстанавливатьСертификат(ОтпечатокСертификатаИзОтвета, Сертификаты)

	// Внимание!
	// ---------
	// В Контейнере есть одноименная процедура, которая используется при отправке отчетов
	// в интерактивном режиме пользователем.
	// При изменениях необходимо синхронно менять обе процедуры.

	УстанавливатьСертификат = Истина;
	
	Отбор = Новый Структура;
	Отбор.Вставить("Отпечаток", НРег(ОтпечатокСертификатаИзОтвета));
	Отбор.Вставить("Хранилище", "MY");
	
	НайденныеСертификаты = Сертификаты.НайтиСтроки(Отбор);
	
	Если НайденныеСертификаты.Количество() = 0 Тогда
		УстанавливатьСертификат = Ложь;
	КонецЕсли;
	
	Возврат УстанавливатьСертификат;
КонецФункции

Функция ПараметрыОбработатьОбновление()

	// Внимание!
	// ---------
	// В Контейнере есть одноименная процедура, которая используется при отправке отчетов
	// в интерактивном режиме пользователем.
	// При изменениях необходимо синхронно менять обе процедуры.
	
	ПараметрыФункции = Новый Структура;
	ПараметрыФункции.Вставить("ЭтоЭлектроннаяПодписьВМоделиСервиса");
	ПараметрыФункции.Вставить("ИдентификаторДокументооборота", "");
	ПараметрыФункции.Вставить("ТелефонМобильныйДляАвторизации", "");
	ПараметрыФункции.Вставить("ИдентификаторАбонента");
	ПараметрыФункции.Вставить("СпецОператорСвязи");
	ПараметрыФункции.Вставить("ПутьКонтейнерЗакрытогоКлюча");
	ПараметрыФункции.Вставить("Организация");
	ПараметрыФункции.Вставить("УчетнаяЗапись", Неопределено);
	ПараметрыФункции.Вставить("ТихийРежим", Ложь);
	ПараметрыФункции.Вставить("РежимАвтонастройки", Ложь);
	ПараметрыФункции.Вставить("ВызовИзМастераАвтонастройки", Ложь);
	ПараметрыФункции.Вставить("ВызовИзМастераПодключенияК1СОтчетности", Ложь);
	ПараметрыФункции.Вставить("ТекстОшибокДляМастераПодключенияК1СОтчетности", "");
	ПараметрыФункции.Вставить("ОтпечатокСертификатаИзОтвета", Неопределено);
	ПараметрыФункции.Вставить("ЭтоВторичноеЗаявление", Ложь);
	ПараметрыФункции.Вставить("ВыполнятьРасшифровкуЗашифрованнойЧастиФайлаАвтонастроки", Истина);
	ПараметрыФункции.Вставить("ОбращениеКСерверуОтключено", Ложь);
	ПараметрыФункции.Вставить("СохранениеВИБОтключено", Ложь);
	ПараметрыФункции.Вставить("СтруктураДляИнформацииОСервереОнлайнПроверки", Неопределено);
	ПараметрыФункции.Вставить("ИгнорироватьРежимАвтонастройки", Ложь);
	
	Возврат ПараметрыФункции;
	
КонецФункции

Функция ПараметрыРегистрацияПолучитьДанныеССервера() Экспорт

	ПараметрыФункции = Новый Структура;
	ПараметрыФункции.Вставить("ИдентификаторАбонента");
	ПараметрыФункции.Вставить("СпецоператорСвязи");
	ПараметрыФункции.Вставить("ВременныйФайлОбновленийАдрес");
	ПараметрыФункции.Вставить("УчетнаяЗапись");
	ПараметрыФункции.Вставить("ФайлИзКэша");
	ПараметрыФункции.Вставить("ДанныеУзлаКонфиденциальнаяИнформацияАдрес");
	ПараметрыФункции.Вставить("ВыводитьСообщения", Истина);
	ПараметрыФункции.Вставить("ВызовИзМастераПодключенияК1СОтчетности", Ложь);
	ПараметрыФункции.Вставить("ТекстОшибокДляМастераПодключенияК1СОтчетности", "");
	
	ПараметрыФункции.Вставить("ОбращениеКСерверуОтключено", Ложь);
	
	Возврат ПараметрыФункции;
	
КонецФункции

Функция РегистрацияПолучитьДанныеССервера(
		ПараметрыФункции,
		ПовторятьСоединение = Ложь,
		Сертификаты = Неопределено,
		ОбновленыПодписиСертификатовДокументооборота = Ложь) Экспорт
	
	ВременныйКаталогНаСервере = ОперацииСФайламиЭДКО.СоздатьВременныйКаталог();
	ИмяВременногоФайлаАрхива 		= ВременныйКаталогНаСервере + ПараметрыФункции.ИдентификаторАбонента + ".zip"; 
	ИмяВременногоФайлаОбновлений 	= ВременныйКаталогНаСервере + ПараметрыФункции.ИдентификаторАбонента + ".xml";
	
	Результат = Ложь;
	
	ДатаОбновленияФайлаНаСервере = Дата('00010101');
	
	Если ПараметрыФункции.УчетнаяЗапись <> Неопределено Тогда
		ПараметрыФункции.ФайлИзКэша = ПолучитьЗакэшированныйФайлНастроекСпецоператора(
			ИмяВременногоФайлаОбновлений, ПараметрыФункции.УчетнаяЗапись, 
			ДатаОбновленияФайлаНаСервере, ПараметрыФункции.ВыводитьСообщения,
			ПараметрыФункции.ВызовИзМастераПодключенияК1СОтчетности, 
			ПараметрыФункции.ТекстОшибокДляМастераПодключенияК1СОтчетности);
	КонецЕсли;
	
	ОбновленоТестовымСервером = Ложь;
	
	ОбновлениеСкачано = Ложь;
	Если НЕ ПараметрыФункции.ОбращениеКСерверуОтключено Тогда
		ОбновлениеСкачано = ОбменССерверомСкачатьОбновление(
			ПараметрыФункции.СпецоператорСвязи, ПараметрыФункции.ИдентификаторАбонента, 
			ИмяВременногоФайлаАрхива, ДатаОбновленияФайлаНаСервере, 
			ПараметрыФункции.ВыводитьСообщения, 
			ПараметрыФункции.ВызовИзМастераПодключенияК1СОтчетности, 
			ПараметрыФункции.ТекстОшибокДляМастераПодключенияК1СОтчетности, ПовторятьСоединение);
			Если НЕ ОбновлениеСкачано И ПовторятьСоединение Тогда
				ОбновленоТестовымСервером = ЗаменитьФайлАвтонастроекТестовыми(ИмяВременногоФайлаОбновлений);
				Если ОбновленоТестовымСервером <> Истина Тогда
					ОперацииСФайламиЭДКО.УдалитьВременныйФайл(ВременныйКаталогНаСервере);
					Возврат Ложь;
				КонецЕсли;
		КонецЕсли;
	КонецЕсли;
	
	Если ОбновлениеСкачано И РазархивироватьФайл(ИмяВременногоФайлаАрхива, ИмяВременногоФайлаОбновлений) <> Неопределено Тогда
		ПараметрыФункции.ФайлИзКэша = Ложь;
	ИначеЕсли ПараметрыФункции.ФайлИзКэша Тогда
		
	Иначе
		ОперацииСФайламиЭДКО.УдалитьВременныйФайл(ВременныйКаталогНаСервере);
		Возврат Ложь;
	КонецЕсли;
	
	Если ОбновленоТестовымСервером <> Истина Тогда
		ЗаменитьФайлАвтонастроекТестовыми(ИмяВременногоФайлаОбновлений);
	КонецЕсли;
	
	ДокументDOM = ЭлектронныйДокументооборотСКонтролирующимиОрганами.ЗагрузитьФайлXML(ИмяВременногоФайлаОбновлений);
	
	ЛичныеСертификаты = Неопределено;
	РегистрацияСформироватьТаблицуСертификатов(ДокументDOM, Сертификаты, ЛичныеСертификаты);
	
	ПараметрыФункции.ДанныеУзлаКонфиденциальнаяИнформацияАдрес = ПоместитьВоВременноеХранилище(
		Base64Значение(ДокументDOM.ПолучитьЭлементыПоИмени("КонфиденциальнаяИнформация")[0].Текстовоесодержимое),
		ПараметрыФункции.ДанныеУзлаКонфиденциальнаяИнформацияАдрес);
	
	ПараметрыФункции.ВременныйФайлОбновленийАдрес = ПоместитьВоВременноеХранилище(
		Новый ДвоичныеДанные(ИмяВременногоФайлаОбновлений), ПараметрыФункции.ВременныйФайлОбновленийАдрес);
	ОперацииСФайламиЭДКО.УдалитьВременныйФайл(ВременныйКаталогНаСервере);
		
	ОбновитьСертификатыВУчетнойЗаписиДО(ПараметрыФункции, ЛичныеСертификаты, Сертификаты);
	
	УстановитьПривилегированныйРежим(Истина);
	Если ОбновитьПодписиСертификатовДокументооборота(ДокументDOM, ПредопределенноеЗначение("Перечисление.СпецоператорыСвязи.КалугаАстрал")) Тогда
		ОбновленыПодписиСертификатовДокументооборота = Истина;
	КонецЕсли;
	
	Возврат Истина;
	
КонецФункции

Функция ЗаменитьФайлАвтонастроекТестовыми(ИмяВременногоФайлаОбновлений)
	
	Попытка
		Если ДокументооборотСКОВызовСервера.ИспользуетсяРежимТестирования() Тогда
			URLСервера = "http://ego712.tc7app.com:80";
			СоединениеHTTP = ДокументооборотСКО.УстановитьСоединениеССерверомИнтернета(URLСервера);
			Если СоединениеHTTP <> Неопределено Тогда
				ЗапросHTTP = Новый HTTPЗапрос("/ego/hs/Agents/AutoSettings/Kaluga/Change");
				ИдентификаторИнформационнойБазы = СтандартныеПодсистемыСервер.ИдентификаторИнформационнойБазы();
				ЗапросHTTP.Заголовки.Вставить("BaseID", ИдентификаторИнформационнойБазы);
				ЗапросHTTP.УстановитьИмяФайлаТела(ИмяВременногоФайлаОбновлений);
				ИмяВременногоФайлаОбновлений_Новый = СтрЗаменить(ИмяВременногоФайлаОбновлений, ".xml", "_test.xml");
				ОтветHTTP = СоединениеHTTP.ОтправитьДляОбработки(ЗапросHTTP, ИмяВременногоФайлаОбновлений_Новый);
				Если ОтветHTTP.КодСостояния = 200 Тогда
					ИмяВременногоФайлаОбновлений = ИмяВременногоФайлаОбновлений_Новый;
					Возврат Истина;
				КонецЕсли;
			КонецЕсли;
		КонецЕсли;
	Исключение
		Возврат Ложь;
	КонецПопытки;
	
	Возврат Ложь;

КонецФункции

Функция ОбменССерверомСкачатьОбновление(СпецоператорСвязи, ИдентификаторАбонента, ИмяВременногоФайлаАрхива, ДатаОбновленияФайлаНаСервере, ВыводитьСообщения, ВызовИзМастераПодключенияК1СОтчетности = ложь, ТекстОшибокДляМастераПодключенияК1СОтчетности = "", ПовторятьСоединение = Ложь)
	
	Сервис = ЭлектронныйДокументооборотСКонтролирующимиОрганами.ОбменССерверомСоздатьСервис(
		СпецоператорСвязи, 
		ВызовИзМастераПодключенияК1СОтчетности, 
		ТекстОшибокДляМастераПодключенияК1СОтчетности, , 
		НЕ ПовторятьСоединение);
		
	Если Сервис = Неопределено Тогда
		Если НЕ ПовторятьСоединение Тогда
			ЭлектронныйДокументооборотСКонтролирующимиОрганамиКлиентСервер.СообщитьИлиПередатьВМастерОшибку(НСтр("ru = 'Не удалось соединиться с сервером обновлений.'"),
							ВызовИзМастераПодключенияК1СОтчетности,
							ТекстОшибокДляМастераПодключенияК1СОтчетности,
							ВыводитьСообщения);
		КонецЕсли;
		Возврат Ложь;
	КонецЕсли;
	
	сообщениеОшибки = "";
	
	Попытка
		XDTOРезультат = Сервис.ReceiveUpdatedPacket(Строка(ИдентификаторАбонента),ДатаОбновленияФайлаНаСервере);
	Исключение
		Если НЕ ПовторятьСоединение Тогда
			ЭлектронныйДокументооборотСКонтролирующимиОрганамиКлиентСервер.СообщитьИлиПередатьВМастерОшибку(НСтр("ru = 'Не удалось соединиться с сервером обновлений.'"),
							ВызовИзМастераПодключенияК1СОтчетности,
							ТекстОшибокДляМастераПодключенияК1СОтчетности,
							ВыводитьСообщения);
		КонецЕсли;
		Возврат Ложь;
	КонецПопытки;
	ПовторятьСоединение = Ложь;
	
	ЧтениеXML = Новый ЧтениеXML; 
	ЧтениеXML.УстановитьСтроку(XDTOРезультат);
	ПостроительДОМ = Новый ПостроительDOM();
	ДОМ = ПостроительДОМ.Прочитать(ЧтениеXML);
	УзелDOM = ДОМ.ПолучитьЭлементыПоИмени("code");
	кодРезультата = УзелDOM[0].ТекстовоеСодержимое;
	Результат = Ложь;
	
	Если КодРезультата = "0" Тогда
		
		УзелDOM = ДОМ.ПолучитьЭлементыПоИмени("packet");
		
		Если УзелDOM.Количество() > 0 Тогда
			ДвоичныеДанные = Base64Значение(УзелDOM[0].ТекстовоеСодержимое);
			ДвоичныеДанные.Записать(ИмяВременногоФайлаАрхива);
			ЧтениеXML.Закрыть();
			Результат = Истина;
		Иначе
			ЭлектронныйДокументооборотСКонтролирующимиОрганамиКлиентСервер.СообщитьИлиПередатьВМастерОшибку(НСтр("ru = 'При получении обновлений возникла ошибка, файл обновлений нечитаем.'"),
							ВызовИзМастераПодключенияК1СОтчетности,
							ТекстОшибокДляМастераПодключенияК1СОтчетности,
							ВыводитьСообщения);
		КонецЕсли;
		
	ИначеЕсли КодРезультата = "1" Тогда
		сообщениеОшибки = "На сервере " + """" + СпецоператорСвязи + """" + " не найдена информация о настройках учетной записи (отсутствует файл /" + идентификаторАбонента + ".zip) !";
		ЭлектронныйДокументооборотСКонтролирующимиОрганамиКлиентСервер.СообщитьИлиПередатьВМастерОшибку(СтроковыеФункцииКлиентСервер.ПодставитьПараметрыВСтроку(НСтр("ru = '%1'"),сообщениеОшибки),
						ВызовИзМастераПодключенияК1СОтчетности,
						ТекстОшибокДляМастераПодключенияК1СОтчетности,
						ВыводитьСообщения);		
	ИначеЕсли кодРезультата = "11" Тогда
		//версия файла на сервер и в  кэше клиент совпала, скачивать не требуется
	Иначе
		
		УзелDOM = ДОМ.ПолучитьЭлементыПоИмени("errorMessage");
		
		Если УзелDOM.Количество() > 0 Тогда
			сообщениеОшибки = УзелDOM[0].ТекстовоеСодержимое;
			сообщениеОшибки = "При получении обновлений возникла ошибка: " + сообщениеОшибки;
		Иначе
			сообщениеОшибки = "При получении обновлений возникла ошибка: " + кодРезультата;
		КонецЕсли;
		
		ЭлектронныйДокументооборотСКонтролирующимиОрганамиКлиентСервер.СообщитьИлиПередатьВМастерОшибку(СтроковыеФункцииКлиентСервер.ПодставитьПараметрыВСтроку(НСтр("ru = '%1'"),сообщениеОшибки),
				ВызовИзМастераПодключенияК1СОтчетности,
				ТекстОшибокДляМастераПодключенияК1СОтчетности,
				ВыводитьСообщения);	
	КонецЕсли;
	
	ЧтениеXML.Закрыть();
	Возврат Результат;
	
КонецФункции

Процедура РегистрацияСформироватьТаблицуСертификатов(ДокументDOM, Сертификаты, ЛичныеСертификаты = Неопределено)

	ТипСтрока = ОбщегоНазначения.ОписаниеТипаСтрока(0);	

	ЛичныеСертификаты = Новый ТаблицаЗначений;
	ЛичныеСертификаты.Колонки.Добавить("Отпечаток",    ТипСтрока);
	ЛичныеСертификаты.Колонки.Добавить("Данные");
	ЛичныеСертификаты.Колонки.Добавить("Текущий");
		
	Если Сертификаты = Неопределено Тогда
		Сертификаты = Новый ТаблицаЗначений;
		Сертификаты.Колонки.Добавить("Отпечаток",    ТипСтрока);
		Сертификаты.Колонки.Добавить("Хранилище",    ТипСтрока);
		Сертификаты.Колонки.Добавить("Тело",         ТипСтрока);
		Сертификаты.Колонки.Добавить("Комментарий",  ТипСтрока);
		Сертификаты.Колонки.Добавить("ТребуетсяУстановить", Новый ОписаниеТипов("Булево"));
		Сертификаты.Колонки.Добавить("ТипПолучателя", ТипСтрока);
	Иначе
		Сертификаты.Очистить();
	КонецЕсли;
	
	УзелDOM = ДокументDOM.ПолучитьЭлементыПоИмени("ПредыдущийСертификат");
	
	Для Счетчик = 0 по УзелDOM.Количество()-1 Цикл
		
		Узел = УзелDOM[Счетчик];
		
		Отпечаток = Нрег(Узел.Атрибуты.ПолучитьИменованныйЭлемент("Отпечаток").Текстовоесодержимое);
		Хранилище = ВРег(Узел.Атрибуты.ПолучитьИменованныйЭлемент("Хранилище").Текстовоесодержимое);
		Тело =	   Узел.ТекстовоеСодержимое;
		
		Если Хранилище = "MY" Тогда 
			ЛичныйСертификат =  ЛичныеСертификаты.Добавить();
			ЛичныйСертификат.Отпечаток = Отпечаток;
			ЛичныйСертификат.Данные = Base64Значение(Тело);
			ЛичныйСертификат.Текущий = Ложь;
		КонецЕсли;
		
	КонецЦикла;
	
	УзелDOM = ДокументDOM.ПолучитьЭлементыПоИмени("Сертификат");
	
	Для Счетчик = 0 по УзелDOM.Количество()-1 Цикл
		
		Узел = УзелDOM[Счетчик];
		
		Сертификат =  Сертификаты.Добавить();
		Сертификат.Отпечаток = Нрег(Узел.Атрибуты.ПолучитьИменованныйЭлемент("Отпечаток").Текстовоесодержимое);
		Сертификат.Хранилище = ВРег(Узел.Атрибуты.ПолучитьИменованныйЭлемент("Хранилище").Текстовоесодержимое);
		Сертификат.Тело =	   Узел.ТекстовоеСодержимое;
		Сертификат.ТребуетсяУстановить = Ложь;
		Если Узел.Атрибуты.ПолучитьИменованныйЭлемент("ТипПолучателя") <> Неопределено Тогда
			Сертификат.ТипПолучателя = Узел.Атрибуты.ПолучитьИменованныйЭлемент("ТипПолучателя").ТекстовоеСодержимое;
		Иначе
			Сертификат.ТипПолучателя = "";
		КонецЕсли;
		
		Комментарий = Узел.Атрибуты.ПолучитьИменованныйЭлемент("Комментарий");
		
		Если Комментарий = Неопределено Тогда
			Комментарий = "Отпечаток: " + Сертификат.Отпечаток + " для хранилища: " +  Сертификат.Хранилище;
		Иначе
			Комментарий = Комментарий.Текстовоесодержимое;
		КонецЕсли;
		
		Сертификат.Комментарий = Комментарий;
		
		Если Сертификат.Хранилище = "MY" Тогда 
			ЛичныйСертификат =  ЛичныеСертификаты.Добавить();
			ЛичныйСертификат.Отпечаток = Сертификат.Отпечаток;
			ЛичныйСертификат.Данные = Base64Значение(Сертификат.Тело);
			ЛичныйСертификат.Текущий = Истина;
		КонецЕсли;
	КонецЦикла;
	
КонецПроцедуры

Функция ПолучитьЗакэшированныйФайлНастроекСпецоператора(ИмяФайлаНастроек, УчетнаяЗапись, ДатаОбновленияФайлаНаСервере, ВыводитьСообщения,ВызовИзМастераПодключенияК1СОтчетности = ложь, ТекстОшибокДляМастераПодключенияК1СОтчетности = "")
	
	Запрос = Новый Запрос("ВЫБРАТЬ РАЗРЕШЕННЫЕ ПЕРВЫЕ 1
	|ФайлыСведенийСпецоператоры.Данные,
	|ФайлыСведенийСпецоператоры.ДатаФайла
	|ИЗ
	|РегистрСведений.ФайлыСведенийСпецоператоры КАК ФайлыСведенийСпецоператоры
	|ГДЕ
	|ФайлыСведенийСпецоператоры.УчетнаяЗапись = &УчетнаяЗапись");
	
	Запрос.Текст = Запрос.Текст + "
	|УПОРЯДОЧИТЬ ПО
	|ФайлыСведенийСпецоператоры.ДатаФайла УБЫВ";
	
	Запрос.УстановитьПараметр("УчетнаяЗапись", УчетнаяЗапись.Ссылка);
	
	Выборка = Запрос.Выполнить().Выбрать();
	Если Выборка.Следующий() Тогда
		Данные = Выборка.Данные;
		Если ЗначениеЗаполнено(Данные) Тогда
			Если ИмяФайлаНастроек <> Неопределено Тогда
				Попытка
					
					Данные.Получить().Записать(ИмяФайлаНастроек);
					ДатаОбновленияФайлаНаСервере = Выборка.ДатаФайла;
					Возврат Истина;
				Исключение
					ЭлектронныйДокументооборотСКонтролирующимиОрганамиКлиентСервер.СообщитьИлиПередатьВМастерОшибку(
						СтроковыеФункцииКлиентСервер.ПодставитьПараметрыВСтроку(НСтр("ru = 'Ошибка разбора файла с информацией о настройках учетной записи:"+Символы.ПС+" %1'"),ИнформацияОбОшибке().Описание),
						ВызовИзМастераПодключенияК1СОтчетности,
						ТекстОшибокДляМастераПодключенияК1СОтчетности,
						ВыводитьСообщения);
					Возврат Ложь;
				КонецПопытки;
			КонецЕсли;
		КонецЕсли;
	КонецЕсли;
	
	Возврат Ложь;
	
КонецФункции

Функция ЭтоСообщениеЗашифрованоНаОблачныйСертификат(Адрес) Экспорт
	
	Если ЭлектроннаяПодписьВМоделиСервиса.ИспользованиеВозможно() Тогда
		ДанныеКриптосообщения = ПолучитьИзВременногоХранилища(Адрес);
		Если ДанныеКриптосообщения = Base64Значение("") Тогда 
			Возврат Неопределено;
		КонецЕсли;		
		СвойстваСообщения = СервисКриптографии.ПолучитьСвойстваКриптосообщения(ДанныеКриптосообщения);
		Если СвойстваСообщения.Тип = "envelopedData" Тогда
			Для Каждого Получатель Из СвойстваСообщения.Получатели Цикл
				Если Получатель.Свойство("Идентификатор") И ЗначениеЗаполнено(Получатель.Идентификатор) Тогда
					Возврат Истина;
				КонецЕсли;
			КонецЦикла;
		КонецЕсли;
	КонецЕсли;
	
	Возврат Ложь;
	
КонецФункции

Функция ХранилищеПоПредставлению(Хранилище) Экспорт
	
	Хранилище = СокрЛП(ВРег(Хранилище));
	
	ЛогическиеХранилищаСертификатов = Новый Соответствие;
	ЛогическиеХранилищаСертификатов.Вставить("ROOT", Перечисления.ТипХранилищаСертификатов.КорневыеСертификаты);
	ЛогическиеХранилищаСертификатов.Вставить("MY", Перечисления.ТипХранилищаСертификатов.ПерсональныеСертификаты);
	ЛогическиеХранилищаСертификатов.Вставить("CA", Перечисления.ТипХранилищаСертификатов.СертификатыУдостоверяющихЦентров);
	ЛогическиеХранилищаСертификатов.Вставить("ADDRESSBOOK", Перечисления.ТипХранилищаСертификатов.СертификатыПолучателей);
	
	ТипХранилище = ЛогическиеХранилищаСертификатов.Получить(Хранилище);
	
	ЛогическиеХранилищаСертификатов.Очистить();
	
	Возврат ТипХранилище;
	
КонецФункции

Функция ПараметрыРегистрацияУстановитьСертификаты() Экспорт

	ПараметрыФункции = Новый Структура;
	ПараметрыФункции.Вставить("ЭтоЭлектроннаяПодписьВМоделиСервиса");
	ПараметрыФункции.Вставить("ОтпечатокСертификата");
	ПараметрыФункции.Вставить("ВременныйКаталог");
	ПараметрыФункции.Вставить("ПутьКонтейнерЗакрытогоКлюча", Неопределено);
	ПараметрыФункции.Вставить("ТихийРежим");
	ПараметрыФункции.Вставить("РежимАвтонастройки");
	ПараметрыФункции.Вставить("ВызовИзМастераПодключенияК1СОтчетности", Ложь);
	ПараметрыФункции.Вставить("ТекстОшибокДляМастераПодключенияК1СОтчетности", "");
	
	Возврат ПараметрыФункции;
	
КонецФункции

Функция ОбновитьСертификатыВУчетнойЗаписиДО(ПараметрыНастройки, ЛичныеСертификаты, ПрочиеСертификаты) Экспорт
	Перем УчетнаяЗапись;
	ПараметрыНастройки.Свойство("УчетнаяЗапись", УчетнаяЗапись);
	
	Если ТипЗнч(УчетнаяЗапись) = Тип("СправочникСсылка.УчетныеЗаписиДокументооборота") Тогда 
		УчетнаяЗаписьОбъект = УчетнаяЗапись.ПолучитьОбъект();
	ИначеЕсли ТипЗнч(УчетнаяЗапись) = Тип("СправочникОбъект.УчетныеЗаписиДокументооборота") Тогда 
		УчетнаяЗаписьОбъект = УчетнаяЗапись;
	Иначе
		Возврат Ложь;
	КонецЕсли;
	
	ПараметрыОтбора = Новый Структура;
	АктуальныйЛичный = Неопределено;
	ДобавленоСертификатов = 0;
	
	Для Каждого ЛичныйСертификат Из ЛичныеСертификаты Цикл 
		
		Если ЛичныйСертификат.Текущий Тогда 
			АктуальныйЛичный = ЛичныйСертификат;
		КонецЕсли;
				
		ПараметрыОтбора.Вставить("Отпечаток", ЛичныйСертификат.Отпечаток);
		
		НайденныеСтроки = УчетнаяЗаписьОбъект.СертификатыЛичные.НайтиСтроки(ПараметрыОтбора);
		Если НайденныеСтроки.Количество() = 0 Тогда 
			
			Попытка 
				СвойстваСертификата = Новый СертификатКриптографии(ЛичныйСертификат.Данные);			
			Исключение
				Ошибка = КраткоеПредставлениеОшибки(ИнформацияОбОшибке());
				ТекстСообщения = СтрШаблон("Не удалось создать СертификатКриптографии. %1", Ошибка);
				ЗаписьЖурналаРегистрации(
					НСтр("ru = 'Электронный документооборот с контролирующими органами.ОбновитьСертификатыВУчетнойЗаписиДО()'"), 
					УровеньЖурналаРегистрации.Предупреждение,, УчетнаяЗаписьОбъект.Ссылка, ТекстСообщения);
				Продолжить;
			КонецПопытки;
			
			СертификатАбонента = УчетнаяЗаписьОбъект.СертификатыЛичные.Добавить();
			СертификатАбонента.СерийныйНомер = НРег(ПолучитьHexСтрокуИзДвоичныхДанных(СвойстваСертификата.СерийныйНомер));
			СертификатАбонента.Отпечаток = ЛичныйСертификат.Отпечаток;
			СертификатАбонента.Содержимое = Новый ХранилищеЗначения(ЛичныйСертификат.Данные);			
			СертификатАбонента.ПоставщикCN = ИзвлечьCNИздателя(СвойстваСертификата.Издатель);
			
			ДобавленоСертификатов = ДобавленоСертификатов + 1;
		КонецЕсли;
			
	КонецЦикла;
	
	ПараметрыОтбора.Очистить();
	ПараметрыОтбора.Вставить("Текущий", Истина);
	НайденныеСтроки = УчетнаяЗаписьОбъект.СертификатыЛичные.НайтиСтроки(ПараметрыОтбора);
	
	ПараметрыОтбора.Очистить();
	ПараметрыОтбора.Вставить("Отпечаток", АктуальныйЛичный.Отпечаток);		
	НайденныеСтрокиАктуального = УчетнаяЗаписьОбъект.СертификатыЛичные.НайтиСтроки(ПараметрыОтбора);
	
	ПерезаполнитьТекущий = Ложь;
	Если НайденныеСтрокиАктуального.Количество() = НайденныеСтроки.Количество() И НайденныеСтроки.Количество() > 0 Тогда 
		Если НайденныеСтрокиАктуального[0].Отпечаток <> НайденныеСтроки[0].Отпечаток Тогда
			ПерезаполнитьТекущий = Истина;
		КонецЕсли;
	Иначе
		ПерезаполнитьТекущий = Истина;
	КонецЕсли;
	
	Если ПерезаполнитьТекущий Тогда
		ДобавленоСертификатов = ДобавленоСертификатов + 1;
		Для Каждого Стр Из НайденныеСтроки Цикл 
			Стр.Текущий = Ложь;
		КонецЦикла;
		Для Каждого Стр Из НайденныеСтрокиАктуального Цикл 
			Стр.Текущий = Истина;
			Прервать;
		КонецЦикла;
	КонецЕсли;
	
	Неизвестные = Новый Соответствие;
	ДляПоиска = Новый Массив;
	Для Каждого ЛичныйСертификат Из УчетнаяЗаписьОбъект.СертификатыЛичные Цикл
		Если ЗначениеЗаполнено(ЛичныйСертификат.Криптопровайдер) Тогда 
			Продолжить;
		КонецЕсли;
		ДляПоиска.Добавить(ЛичныйСертификат.Отпечаток);
		Неизвестные.Вставить(ЛичныйСертификат.Отпечаток, ЛичныйСертификат.Содержимое.Получить());
	КонецЦикла;
		
	СервисОпрошенУспешно = Ложь;
	РежимSaaS = КриптографияЭДКОСлужебныйВызовСервера.ИспользованиеКриптосервисаВозможно();
	Если Неизвестные.Количество() > 0 И РежимSaaS Тогда 		
		Результат = МенеджерСервисаКриптографии.ПоискСертификатаПоОтпечаткуИлиСерийномуНомеру(ДляПоиска,, Истина);
		Если Результат.Выполнено Тогда // Пометим найденную часть личных сертификатов как облачные
			ПараметрыОтбора.Очистить();
			СервисОпрошенУспешно = Истина;
			Для Каждого Найденный Из Результат.Сертификаты Цикл 				
				ПараметрыОтбора.Вставить("Отпечаток", Найденный.Thumbprint);
				НайденныеСтроки = УчетнаяЗаписьОбъект.СертификатыЛичные.НайтиСтроки(ПараметрыОтбора);
				Для Каждого Стр Из НайденныеСтроки Цикл 
					ДобавленоСертификатов = ДобавленоСертификатов + 1;
					Стр.Криптопровайдер = Перечисления.ТипыКриптоПровайдеров.CryptoProHSM;
				КонецЦикла;
				Неизвестные.Удалить(Найденный.Thumbprint);
			КонецЦикла;
		КонецЕсли;
	КонецЕсли;
	
	// проверим для какого криптопровайдера изданы оставшиеся нераспознанные сертификаты
	Если Неизвестные.Количество() > 0 И (Не РежимSaaS Или СервисОпрошенУспешно) Тогда
		НеизвестныеСписком = Новый Массив;
		Для Каждого Неизвестный Из Неизвестные Цикл 
			НеизвестныеСписком.Добавить(Новый Структура("Сертификат", Неизвестный.Значение));
		КонецЦикла;
		ПараметрыОтбора.Очистить();
		Известные = КриптографияЭДКОСлужебныйВызовСервера.ИзвлечьИнформациюОКриптопровайдереПоСертификату(НеизвестныеСписком);
		Для Каждого Неизвестный Из Неизвестные Цикл 
			Если Известные.Количество() = 0 ИЛИ Известные[0].Криптопровайдер = Неопределено Тогда
				Прервать;
			КонецЕсли;
			ПараметрыОтбора.Вставить("Отпечаток", Неизвестный.Ключ);
			НайденныеСтроки = УчетнаяЗаписьОбъект.СертификатыЛичные.НайтиСтроки(ПараметрыОтбора);
			Для Каждого Стр Из НайденныеСтроки Цикл 
				ДобавленоСертификатов = ДобавленоСертификатов + 1;
				Стр.Криптопровайдер = Известные[0].Криптопровайдер.ТипКриптопровайдера;
			КонецЦикла;
			Известные.Удалить(0);				
		КонецЦикла;		
	КонецЕсли;
	
	ПараметрыОтбора.Очистить();
	Для Каждого ПрочийСертификат Из ПрочиеСертификаты Цикл 
		Если ПрочийСертификат.Хранилище = "MY" Тогда 
			Продолжить;
		КонецЕсли;
		
		ПараметрыОтбора.Вставить("Отпечаток", ПрочийСертификат.Отпечаток);		
		НайденныеСтроки = УчетнаяЗаписьОбъект.СертификатыПрочие.НайтиСтроки(ПараметрыОтбора);
		Если НайденныеСтроки.Количество() = 0 Тогда 
			
			Попытка 
				ПрочийСертификат_Данные = Base64Значение(ПрочийСертификат.Тело);
				СвойстваСертификата = Новый СертификатКриптографии(ПрочийСертификат_Данные);
			Исключение
				Ошибка = КраткоеПредставлениеОшибки(ИнформацияОбОшибке());
				ТекстСообщения = СтрШаблон("Не удалось создать СертификатКриптографии. %1", Ошибка);
				ЗаписьЖурналаРегистрации(
					НСтр("ru = 'Электронный документооборот с контролирующими органами.ОбновитьСертификатыВУчетнойЗаписиДО()'"), 
					УровеньЖурналаРегистрации.Предупреждение,, УчетнаяЗаписьОбъект.Ссылка, ТекстСообщения);
				Продолжить;
			КонецПопытки;
			
			СертификатАбонента = УчетнаяЗаписьОбъект.СертификатыПрочие.Добавить();
			СертификатАбонента.СерийныйНомер = НРег(ПолучитьHexСтрокуИзДвоичныхДанных(СвойстваСертификата.СерийныйНомер));
			СертификатАбонента.Отпечаток = ПрочийСертификат.Отпечаток;
			СертификатАбонента.Содержимое = Новый ХранилищеЗначения(ПрочийСертификат_Данные);			
			СертификатАбонента.ПоставщикCN = ИзвлечьCNИздателя(СвойстваСертификата.Издатель);
			
			ДобавленоСертификатов = ДобавленоСертификатов + 1;
		КонецЕсли;
			
	КонецЦикла;
	
	Если ДобавленоСертификатов > 0 Тогда 
		Попытка
			УчетнаяЗаписьОбъект.Записать();
			ПараметрыНастройки.УчетнаяЗапись = УчетнаяЗаписьОбъект.Ссылка;
		Исключение
			Ошибка = КраткоеПредставлениеОшибки(ИнформацияОбОшибке());
			ТекстСообщения = СтрШаблон("Не удалось обновить сертификаты учетной записи документооборота. %1", Ошибка);
			ЗаписьЖурналаРегистрации(
				НСтр("ru = 'Электронный документооборот с контролирующими органами.ОбновитьСертификатыВУчетнойЗаписиДО()'"), 
				УровеньЖурналаРегистрации.Предупреждение,, УчетнаяЗаписьОбъект.Ссылка, ТекстСообщения);
			Возврат Ложь;
		КонецПопытки;
	КонецЕсли;	
	
	Возврат Истина;
	
КонецФункции
	
Функция РегистрацияУстановитьСертификаты(ОтпечатокСертификата, Сертификаты) Экспорт

	// Устанавливаем сертификаты и обновляем настройки в ИБ.
	Для Каждого СтрокаТаблицы Из Сертификаты Цикл

		ЛогическоеХранилище = ХранилищеПоПредставлению(СтрокаТаблицы.Хранилище);
		
		Сертификат  = Новый Структура("Отпечаток", СтрокаТаблицы.Отпечаток);
		СвойстваСертификата = КриптографияЭДКО.НайтиСертификат(Сертификат);
		
		// Если свойства не получены, значит надо установить сертификат.
		Если СвойстваСертификата = Неопределено Тогда
			СертификатДвоичныеДанные = Base64Значение(СтрокаТаблицы.Тело);
			ЛогическоеХранилище = ХранилищеПоПредставлению(СтрокаТаблицы.Хранилище);
			ХранилищеСертификатов.Добавить(СертификатДвоичныеДанные, ЛогическоеХранилище);
			СвойстваСертификата = СервисКриптографии.ПолучитьСвойстваСертификата(СертификатДвоичныеДанные);
		КонецЕсли;
		Если ЛогическоеХранилище = Перечисления.ТипХранилищаСертификатов.ПерсональныеСертификаты Тогда //.MY
			ОтпечатокСертификата = СвойстваСертификата.Отпечаток;
		КонецЕсли;
	КонецЦикла;
	
	Возврат Истина;
	
КонецФункции

#КонецОбласти

#Область СервисКриптографии

Процедура РасшифроватьPKCS7(ЗашифрованныйФайл, СообщениеОбОшибке) Экспорт

	// Внимание!
	// ---------
	// В общем модуле КриптографияЭДКОСлужебныйКлиент есть аналогичные процедуры
	// РасшифроватьPKCS7() и РасшифроватьPKCS7ПослеРасшифровкиСервисомКриптографии(),
	// которые используется при отправке отчетов в интерактивном режиме пользователем.
	// При изменениях необходимо синхронно менять обе процедуры.
	
	Если НЕ КриптографияЭДКОКлиентСервер.ЭтоАдресВоВременномХранилище(ЗашифрованныйФайл) Тогда
		
		СообщениеОбОшибке = НСтр("ru = 'РасшифроватьPKCS7(): Для электронной подписи в облаке данные должны быть переданы как адрес в хранилище'");

		Возврат;
	КонецЕсли;

	ПараметрыШифрования = Новый Структура();
	ПараметрыШифрования.Вставить("ИспользоватьДлительныйМаркерБезопасности", Истина); // Для расшифровки можно использовать длительный маркер безопасности.

	РасшифрованныеДанные = Неопределено;
	
	Попытка
		// Чтобы в конфигурациях со старыми версиями БТС не возникали ошибки компиляции,
		// обращаемся к общему модулю через функцию БСП.
		// Для возможности отправки отчетов на стороне сервера из таких конфигураций потребуется обновить БТС до 1.0.17.
		МодульСервисКриптографии = ОбщегоНазначения.ОбщийМодуль("СервисКриптографии");
		РасшифрованныеДанные = МодульСервисКриптографии.РасшифроватьПереборомСертификатов(ЗашифрованныйФайл, , ПараметрыШифрования);	
	Исключение
		СообщениеОбОшибке  = ПодробноеПредставлениеОшибки(ИнформацияОбОшибке());
		ЗаписатьСобытиеВЖурнал(УровеньЖурналаРегистрации.Ошибка, СообщениеОбОшибке);
	КонецПопытки;
	
	Если НЕ ПроверитьРезультатВызоваСервисаКриптографии(РасшифрованныеДанные, СообщениеОбОшибке) Тогда
		Возврат;
	КонецЕсли;

	ИсходныйФайл = ЗашифрованныйФайл;
	
	// Результат необходимо переложить по адресу исходного файла / пути нового файла.
	Если ЭтоАдресВременногоХранилища(РасшифрованныеДанные) Тогда 
		КриптографияЭДКОКлиентСервер.ПереместитьВоВременномХранилище(РасшифрованныеДанные, ИсходныйФайл);
	Иначе // Двоичные данные
		ПоместитьВоВременноеХранилище(РасшифрованныеДанные, ИсходныйФайл);
	КонецЕсли;
		
КонецПроцедуры

Функция ПодписатьИЗашифроватьPKCS7(СертификатПодписанта, ИмяФайлаДанных, СертификатыПолучателей, СообщениеОбОшибке)

	// Внимание!
	// ---------
	// В общем модуле КриптографияЭДКОСлужебныйКлиент есть одноименная процедура,
	// которая используется при отправке отчетов в интерактивном режиме пользователем.
	// При изменениях необходимо синхронно менять обе процедуры.

	Если НЕ КриптографияЭДКОКлиентСервер.ЭтоАдресВоВременномХранилище(ИмяФайлаДанных) Тогда
		
		СообщениеОбОшибке = НСтр("ru = 'ПодписатьИЗашифроватьPKCS7(): Для электронной подписи в облаке данные должны быть переданы как адрес в хранилище'");

		Возврат Неопределено;
	КонецЕсли;

	ДанныеФайла = ПолучитьИзВременногоХранилища(ИмяФайлаДанных);

	ПараметрыПодписания = Новый Структура("ОтсоединеннаяПодпись", Ложь);
	
	Попытка
	
		ПодписанныеДанные = СервисКриптографии.Подписать(
			ДанныеФайла, 
			СертификатПодписанта,
			"CMS", 
			ПараметрыПодписания); 
		
	Исключение
		СообщениеОбОшибке  = ПодробноеПредставлениеОшибки(ИнформацияОбОшибке());
		ЗаписатьСобытиеВЖурнал(УровеньЖурналаРегистрации.Ошибка, СообщениеОбОшибке);
	КонецПопытки;

	Если НЕ ПроверитьРезультатВызоваСервисаКриптографии(ПодписанныеДанные, СообщениеОбОшибке) Тогда
		Возврат Неопределено;
	КонецЕсли;

	// Зашифровываем подписанные данные.
	Попытка

		ЗашифрованныеДанные = СервисКриптографии.Зашифровать(
			ПодписанныеДанные, 
			СертификатыПолучателей, 
			"CMS", Неопределено);
			
	Исключение
		СообщениеОбОшибке  = ПодробноеПредставлениеОшибки(ИнформацияОбОшибке());
		ЗаписатьСобытиеВЖурнал(УровеньЖурналаРегистрации.Ошибка, СообщениеОбОшибке);
	КонецПопытки;

	Если НЕ ПроверитьРезультатВызоваСервисаКриптографии(ЗашифрованныеДанные, СообщениеОбОшибке) Тогда
		Возврат Неопределено;
	КонецЕсли;
	
	Возврат ПоместитьВоВременноеХранилище(ЗашифрованныеДанные, Новый УникальныйИдентификатор);

КонецФункции

Функция ПроверитьРезультатВызоваСервисаКриптографии(Результат, СообщениеОбОшибке)

	Если ТипЗнч(Результат) = Тип("Структура")
		И Результат.Свойство("КодВозврата") Тогда
		// Особая ошибка сервиса криптографии, прекращаем выполнение и передаем информацию о ней вызывающему методу.
		СообщениеОбОшибке = Результат.КодВозврата;
		Возврат Ложь;
	КонецЕсли;
	
	Возврат Истина;

КонецФункции

#КонецОбласти

#Область Подписание

Функция ПолучитьНаборДокументовНаПодписаниеФНС(Сообщение, ТипСообщения)
	
	ТипыДИВ = Неопределено;
	СообщениеДокументыКоторогоПодписываются = Неопределено;
	
	// определяем набор документов, которые должны быть подписаны
	Если ТипСообщения = Перечисления.ТипыТранспортныхСообщений.ДекларацияНП Тогда
		
		СообщениеДокументыКоторогоПодписываются = Сообщение;
		
		ТипыДИВ = Новый Массив;
		ТипыДИВ.Добавить(Перечисления.ТипыСодержимогоТранспортногоКонтейнера.ФайлОтчетности);
		ТипыДИВ.Добавить(Перечисления.ТипыСодержимогоТранспортногоКонтейнера.Приложение);
		ТипыДИВ.Добавить(Перечисления.ТипыСодержимогоТранспортногоКонтейнера.ДоверенностьПредставление);
		
	ИначеЕсли ТипСообщения = Перечисления.ТипыТранспортныхСообщений.ЗаявлениеНП Тогда
		
		СообщениеДокументыКоторогоПодписываются = Сообщение;
		
		ТипыДИВ = Новый Массив;
		ТипыДИВ.Добавить(Перечисления.ТипыСодержимогоТранспортногоКонтейнера.Заявление);
		ТипыДИВ.Добавить(Перечисления.ТипыСодержимогоТранспортногоКонтейнера.ДоверенностьПредставление);
		
	ИначеЕсли ТипСообщения = Перечисления.ТипыТранспортныхСообщений.ИзвещениеПодтверждениеНП Тогда
		
		СообщениеДокументыКоторогоПодписываются = Новый Массив;
		СообщениеДокументыКоторогоПодписываются.Добавить(Сообщение);
		
		ТипыДИВ = Новый Массив;
		ТипыДИВ.Добавить(Перечисления.ТипыСодержимогоТранспортногоКонтейнера.ИзвещениеОПолучении);
		
	ИначеЕсли ТипСообщения = Перечисления.ТипыТранспортныхСообщений.ИзвещениеРезультатПриемаНП Тогда
		
		СообщениеДокументыКоторогоПодписываются = Новый Массив;
		СообщениеДокументыКоторогоПодписываются.Добавить(Сообщение);
		
		ТипыДИВ = Новый Массив;
		ТипыДИВ.Добавить(Перечисления.ТипыСодержимогоТранспортногоКонтейнера.ИзвещениеОПолучении);
		
	ИначеЕсли ТипСообщения = Перечисления.ТипыТранспортныхСообщений.ИзвещениеРезультатОбработкиНП Тогда
		
		СообщениеДокументыКоторогоПодписываются = Новый Массив;
		СообщениеДокументыКоторогоПодписываются.Добавить(Сообщение);
		
		ТипыДИВ = Новый Массив;
		ТипыДИВ.Добавить(Перечисления.ТипыСодержимогоТранспортногоКонтейнера.ИзвещениеОПолучении);
		
	ИначеЕсли ТипСообщения = Перечисления.ТипыТранспортныхСообщений.ИзвещениеРезультатОбработкиРФНП Тогда
		
		СообщениеДокументыКоторогоПодписываются = Новый Массив;
		СообщениеДокументыКоторогоПодписываются.Добавить(Сообщение);
		
		ТипыДИВ = Новый Массив;
		ТипыДИВ.Добавить(Перечисления.ТипыСодержимогоТранспортногоКонтейнера.ИзвещениеОПолучении);
		
	ИначеЕсли ТипСообщения = Перечисления.ТипыТранспортныхСообщений.ИзвещениеОбОтзывеЗаявлениеРФНП Тогда
		
		СообщениеДокументыКоторогоПодписываются = Новый Массив;
		СообщениеДокументыКоторогоПодписываются.Добавить(Сообщение);
		
		ТипыДИВ = Новый Массив;
		ТипыДИВ.Добавить(Перечисления.ТипыСодержимогоТранспортногоКонтейнера.ИзвещениеОПолучении);
		
	ИначеЕсли ТипСообщения = Перечисления.ТипыТранспортныхСообщений.ИзвещениеРезультатОбработкиТСНП Тогда
		
		СообщениеДокументыКоторогоПодписываются = Новый Массив;
		СообщениеДокументыКоторогоПодписываются.Добавить(Сообщение);
		
		ТипыДИВ = Новый Массив;
		ТипыДИВ.Добавить(Перечисления.ТипыСодержимогоТранспортногоКонтейнера.ИзвещениеОПолучении);
		
	ИначеЕсли ТипСообщения = Перечисления.ТипыТранспортныхСообщений.ИзвещениеДокументНП Тогда
		
		СообщениеДокументыКоторогоПодписываются = Новый Массив;
		СообщениеДокументыКоторогоПодписываются.Добавить(Сообщение);
		
		ТипыДИВ = Новый Массив;
		ТипыДИВ.Добавить(Перечисления.ТипыСодержимогоТранспортногоКонтейнера.ИзвещениеОПолучении);
		
	ИначеЕсли ТипСообщения = Перечисления.ТипыТранспортныхСообщений.РезультатПриемаДокументНП Тогда
		
		СообщениеДокументыКоторогоПодписываются = Новый Массив;
		СообщениеДокументыКоторогоПодписываются.Добавить(Сообщение);
		
		ТипыДИВ = Новый Массив;
		ТипыДИВ.Добавить(Перечисления.ТипыСодержимогоТранспортногоКонтейнера.КвитанцияОПриемеДокумент);
		ТипыДИВ.Добавить(Перечисления.ТипыСодержимогоТранспортногоКонтейнера.УведомлениеОбОтказеДокумент);
		
	ИначеЕсли ТипСообщения = Перечисления.ТипыТранспортныхСообщений.Форма2НДФЛНП Тогда
		
		СообщениеДокументыКоторогоПодписываются = Новый Массив;
		СообщениеДокументыКоторогоПодписываются.Добавить(Сообщение);
		
		ТипыДИВ = Новый Массив;
		ТипыДИВ.Добавить(Перечисления.ТипыСодержимогоТранспортногоКонтейнера.Форма2НДФЛ);
		ТипыДИВ.Добавить(Перечисления.ТипыСодержимогоТранспортногоКонтейнера.ДоверенностьПредставление);
		
	ИначеЕсли ТипСообщения = Перечисления.ТипыТранспортныхСообщений.ОбращениеНП Тогда
		
		СообщениеДокументыКоторогоПодписываются = Новый Массив;
		СообщениеДокументыКоторогоПодписываются.Добавить(Сообщение);
		
		ТипыДИВ = Новый Массив;
		ТипыДИВ.Добавить(Перечисления.ТипыСодержимогоТранспортногоКонтейнера.Обращение);
		ТипыДИВ.Добавить(Перечисления.ТипыСодержимогоТранспортногоКонтейнера.Приложение);
		ТипыДИВ.Добавить(Перечисления.ТипыСодержимогоТранспортногоКонтейнера.ДоверенностьПредставление);
		
	ИначеЕсли ТипСообщения = Перечисления.ТипыТранспортныхСообщений.ПредставлениеНП Тогда
		
		СообщениеДокументыКоторогоПодписываются = Новый Массив;
		СообщениеДокументыКоторогоПодписываются.Добавить(Сообщение);
		
		ТипыДИВ = Новый Массив;
		ТипыДИВ.Добавить(Перечисления.ТипыСодержимогоТранспортногоКонтейнера.Представление);
		ТипыДИВ.Добавить(Перечисления.ТипыСодержимогоТранспортногоКонтейнера.Приложение);
		ТипыДИВ.Добавить(Перечисления.ТипыСодержимогоТранспортногоКонтейнера.ДоверенностьПредставление);
		
	ИначеЕсли ТипСообщения = Перечисления.ТипыТранспортныхСообщений.ЗапросНП Тогда
		
		СообщениеДокументыКоторогоПодписываются = Новый Массив;
		СообщениеДокументыКоторогоПодписываются.Добавить(Сообщение);
		
		ТипыДИВ = Новый Массив;
		ТипыДИВ.Добавить(Перечисления.ТипыСодержимогоТранспортногоКонтейнера.Запрос);
		ТипыДИВ.Добавить(Перечисления.ТипыСодержимогоТранспортногоКонтейнера.ДоверенностьПредставление);
		
	ИначеЕсли ТипСообщения = Перечисления.ТипыТранспортныхСообщений.ИзвещениеПисьмоНП Тогда
		
		СообщениеДокументыКоторогоПодписываются = Новый Массив;
		СообщениеДокументыКоторогоПодписываются.Добавить(Сообщение);
		
		ТипыДИВ = Новый Массив;
		ТипыДИВ.Добавить(Перечисления.ТипыСодержимогоТранспортногоКонтейнера.ИзвещениеОПолучении);
		
	ИначеЕсли ТипСообщения = Перечисления.ТипыТранспортныхСообщений.ИзвещениеРассылкаНП Тогда
		
		СообщениеДокументыКоторогоПодписываются = Новый Массив;
		СообщениеДокументыКоторогоПодписываются.Добавить(Сообщение);
		
		ТипыДИВ = Новый Массив;
		ТипыДИВ.Добавить(Перечисления.ТипыСодержимогоТранспортногоКонтейнера.ИзвещениеОПолучении);
	
	Иначе 
		
		СообщениеДокументыКоторогоПодписываются = Сообщение;
		
	КонецЕсли;
	
	Возврат Новый Структура("СообщениеДокументыКоторогоПодписываются, ТипыДИВ", СообщениеДокументыКоторогоПодписываются, ТипыДИВ);
	
КонецФункции

Функция ПолучитьНаборДокументовНаПодписаниеПФР(Сообщение, ТипСообщения, СообщениеОснование)
	
	ТипыДИВ = Неопределено;
	СообщениеДокументыКоторогоПодписываются = Неопределено;
	
	// определяем набор документов, которые должны быть подписаны
	Если ТипСообщения = Перечисления.ТипыТранспортныхСообщений.ПервичноеСообщениеСодержащееОтчетностьПФР Тогда
		
		СообщениеДокументыКоторогоПодписываются = Сообщение;
		
		ТипыДИВ = Новый Массив;
		ТипыДИВ.Добавить(Перечисления.ТипыСодержимогоТранспортногоКонтейнера.ФайлОтчетностиПФР);
		ТипыДИВ.Добавить(Перечисления.ТипыСодержимогоТранспортногоКонтейнера.СведенияПриложениеПФР);
		ТипыДИВ.Добавить(Перечисления.ТипыСодержимогоТранспортногоКонтейнера.ФайлОписанияСведенийПФР);
		
	ИначеЕсли ТипСообщения = Перечисления.ТипыТранспортныхСообщений.ПротоколКвитанцияПФР Тогда
		
		СообщениеДокументыКоторогоПодписываются = СообщениеОснование;
		
		ТипыДИВ = Новый Массив;
		ТипыДИВ.Добавить(Перечисления.ТипыСодержимогоТранспортногоКонтейнера.ПротоколПФР);
		ТипыДИВ.Добавить(Перечисления.ТипыСодержимогоТранспортногоКонтейнера.ПротоколПриложениеПФР);
		
	ИначеЕсли ТипСообщения = Перечисления.ТипыТранспортныхСообщений.ПисьмоИсходящееПФР Тогда
		
		СообщениеДокументыКоторогоПодписываются = Сообщение;
		
		ТипыДИВ = Новый Массив;
		ТипыДИВ.Добавить(Перечисления.ТипыСодержимогоТранспортногоКонтейнера.Письмо);
		ТипыДИВ.Добавить(Перечисления.ТипыСодержимогоТранспортногоКонтейнера.ПисьмоПриложение);
		ТипыДИВ.Добавить(Перечисления.ТипыСодержимогоТранспортногоКонтейнера.ОписаниеПисьма);
		ТипыДИВ.Добавить(Перечисления.ТипыСодержимогоТранспортногоКонтейнера.ТранспортнаяИнформация);
		
	ИначеЕсли ТипСообщения = Перечисления.ТипыТранспортныхСообщений.ПисьмоВходящееКвитанцияПФР Тогда
		
		СообщениеДокументыКоторогоПодписываются = Сообщение;
		
		ТипыДИВ = Новый Массив;
		ТипыДИВ.Добавить(Перечисления.ТипыСодержимогоТранспортногоКонтейнера.ПодтверждениеПолученияПисьма);
		
	ИначеЕсли ТипСообщения = Перечисления.ТипыТранспортныхСообщений.ПервичноеСообщениеСодержащееЗапросПФР Тогда
		
		СообщениеДокументыКоторогоПодписываются = Сообщение;
		
		ТипыДИВ = Новый Массив;
		ТипыДИВ.Добавить(Перечисления.ТипыСодержимогоТранспортногоКонтейнера.ЗапросИОС);
		ТипыДИВ.Добавить(Перечисления.ТипыСодержимогоТранспортногоКонтейнера.ФайлОписанияЗапросаПФР);
		
	ИначеЕсли ТипСообщения = Перечисления.ТипыТранспортныхСообщений.ОтветНаЗапросКвитанцияПФР Тогда
		
		СообщениеДокументыКоторогоПодписываются = Новый Массив;
		СообщениеДокументыКоторогоПодписываются.Добавить(Сообщение);
		СообщениеДокументыКоторогоПодписываются.Добавить(СообщениеОснование);
		
		ТипыДИВ = Новый Массив;
		ТипыДИВ.Добавить(Перечисления.ТипыСодержимогоТранспортногоКонтейнера.ОтветНаЗапросПФР);
		ТипыДИВ.Добавить(Перечисления.ТипыСодержимогоТранспортногоКонтейнера.ОтветПриложениеНаЗапросПФР);
		ТипыДИВ.Добавить(Перечисления.ТипыСодержимогоТранспортногоКонтейнера.ПодтверждениеПолученияЗапросПФР);
		
	Иначе 
		
		СообщениеДокументыКоторогоПодписываются = Сообщение;
		
	КонецЕсли;
	
	Возврат Новый Структура("СообщениеДокументыКоторогоПодписываются, ТипыДИВ", СообщениеДокументыКоторогоПодписываются, ТипыДИВ);
	
КонецФункции

Функция ПолучитьНаборДокументовНаПодписаниеФСГС(Сообщение, ТипСообщения, СообщениеОснование)
	
	ТипыДИВ = Неопределено;
	СообщениеДокументыКоторогоПодписываются = Неопределено;
		
	// определяем набор документов, которые должны быть подписаны
	Если ТипСообщения = Перечисления.ТипыТранспортныхСообщений.ПервичноеСообщениеСодержащееОтчетностьФСГС Тогда
		
		СообщениеДокументыКоторогоПодписываются = Сообщение;
		
		ТипыДИВ = Новый Массив;		
		ТипыДИВ.Добавить(Перечисления.ТипыСодержимогоТранспортногоКонтейнера.ФайлОтчетностиФСГС);
		
	ИначеЕсли ТипСообщения = Перечисления.ТипыТранспортныхСообщений.ИзвещениеПодтверждениеДатыОтправкиФСГС Тогда
		
		СообщениеДокументыКоторогоПодписываются = СообщениеОснование;
		
		ТипыДИВ = Новый Массив;
		ТипыДИВ.Добавить(Перечисления.ТипыСодержимогоТранспортногоКонтейнера.ПодтверждениеОператораФСГС);
		
	ИначеЕсли ТипСообщения = Перечисления.ТипыТранспортныхСообщений.ИзвещениеПротоколВходногоКонтроляОтчетностиФСГС Тогда
		
		СообщениеДокументыКоторогоПодписываются = Сообщение;
		
		ТипыДИВ = Новый Массив;
		ТипыДИВ.Добавить(Перечисления.ТипыСодержимогоТранспортногоКонтейнера.ИзвещениеОПолученииДокументаФСГС);
		
	ИначеЕсли ТипСообщения = Перечисления.ТипыТранспортныхСообщений.ПисьмоИсходящееФСГС Тогда
		
		СообщениеДокументыКоторогоПодписываются = Сообщение;
		
		ТипыДИВ = Новый Массив;
		ТипыДИВ.Добавить(Перечисления.ТипыСодержимогоТранспортногоКонтейнера.ПисьмоФСГС);
		ТипыДИВ.Добавить(Перечисления.ТипыСодержимогоТранспортногоКонтейнера.ПриложениеПисьмаФСГС);
		
	ИначеЕсли ТипСообщения = Перечисления.ТипыТранспортныхСообщений.ИзвещениеПисьмоВходящееФСГС Тогда
	
		СообщениеДокументыКоторогоПодписываются = Сообщение;
	
		ТипыДИВ = Новый Массив;
		ТипыДИВ.Добавить(Перечисления.ТипыСодержимогоТранспортногоКонтейнера.ИзвещениеОПолученииДокументаФСГС);
	Иначе 
	
		СообщениеДокументыКоторогоПодписываются = Сообщение;
	
	КонецЕсли;
	
	Возврат Новый Структура("СообщениеДокументыКоторогоПодписываются, ТипыДИВ", СообщениеДокументыКоторогоПодписываются, ТипыДИВ);
	
КонецФункции

Функция ПодготовитьПараметрыПодписатьСообщение(Знач КонтролирующийОрган, 
                                               ЭтоЭлектроннаяПодписьВМоделиСервиса,
                                               Знач Сообщение,
                                               Знач ТипСообщения,
                                               Знач СертификатПодписанта, 
                                               Знач УчетнаяЗапись,
                                               Знач СообщениеОснованиеИПараметры) Экспорт
	
	Если ТипЗнч(СообщениеОснованиеИПараметры) = Тип("Структура") Тогда
		СообщениеОснование 				= СообщениеОснованиеИПараметры.СообщениеОснование;
		ЦиклОбмена 						= СообщениеОснованиеИПараметры.ЦиклОбмена;
		ФорматДокументооборота 			= СообщениеОснованиеИПараметры.ФорматДокументооборота;
		ПолучитьФорматДокументооборота 	= СообщениеОснованиеИПараметры.ПолучитьФорматДокументооборота;
		
	Иначе
		СообщениеОснование 				= СообщениеОснованиеИПараметры;
		ЦиклОбмена 						= Неопределено;
		ФорматДокументооборота 			= Неопределено;
		ПолучитьФорматДокументооборота 	= Ложь;
	КонецЕсли;
	
	Если КонтролирующийОрган = "ФНС" Тогда
		Результат = ПолучитьНаборДокументовНаПодписаниеФНС(Сообщение, ТипСообщения);
	ИначеЕсли КонтролирующийОрган = "ПФР" Тогда
		Результат = ПолучитьНаборДокументовНаПодписаниеПФР(Сообщение, ТипСообщения, СообщениеОснование);
	ИначеЕсли КонтролирующийОрган = "ФСГС" Тогда
		Результат = ПолучитьНаборДокументовНаПодписаниеФСГС(Сообщение, ТипСообщения, СообщениеОснование);		
	КонецЕсли;

	СообщениеДокументыКоторогоПодписываются = Результат.СообщениеДокументыКоторогоПодписываются;
	ТипыДИВ = Результат.ТипыДИВ;
	
	Если ЭтоЭлектроннаяПодписьВМоделиСервиса = Неопределено Тогда		
		ЭтоЭлектроннаяПодписьВМоделиСервиса = ЭлектроннаяПодписьВМоделиСервисаБРОВызовСервера.РеквизитыУчетнойЗаписи(УчетнаяЗапись).ЭлектроннаяПодписьВМоделиСервиса;
	КонецЕсли;

	// формируем массив сертификатов
	Если СертификатПодписанта = Неопределено Тогда
		СертификатыПодписантов = Новый Массив;
		СертификатыПодписантов.Добавить("Руководитель");
	ИначеЕсли ТипЗнч(СертификатПодписанта) = Тип("Строка") ИЛИ ТипЗнч(СертификатПодписанта) = Тип("Структура") Тогда
		СертификатыПодписантов = Новый Массив;
		СертификатыПодписантов.Добавить(СертификатПодписанта);
	Иначе
		СертификатыПодписантов = СертификатПодписанта;
	КонецЕсли;
	
	// получаем объекты сертификатов
	Для Инд = 0 По СертификатыПодписантов.Количество() - 1 Цикл
		Серт = СертификатыПодписантов[Инд];
		Если ТипЗнч(Серт) = Тип("Строка") Тогда
			Отпечаток = ПолучитьОтпечаток(УчетнаяЗапись, Серт,);
			СертификатыПодписантов[Инд] = Новый Структура("ЭтоЭлектроннаяПодписьВМоделиСервиса, Отпечаток", ЭтоЭлектроннаяПодписьВМоделиСервиса, Отпечаток);
		КонецЕсли;
	КонецЦикла;
	
	Если ФорматДокументооборота = Неопределено И ПолучитьФорматДокументооборота Тогда
		Если ЦиклОбмена = Неопределено И ЗначениеЗаполнено(СообщениеДокументыКоторогоПодписываются) Тогда
			ЦиклОбмена = ОбщегоНазначения.ЗначениеРеквизитаОбъекта(СообщениеДокументыКоторогоПодписываются, "ЦиклОбмена");
		КонецЕсли;
		Если ЗначениеЗаполнено(ЦиклОбмена) Тогда
			ФорматДокументооборота = ОбщегоНазначения.ЗначениеРеквизитаОбъекта(ЦиклОбмена, "ФорматДокументооборота");
		КонецЕсли;
	КонецЕсли;
	
	ПараметрыФункции = Новый Структура;
	ПараметрыФункции.Вставить("СообщениеДокументыКоторогоПодписываются", СообщениеДокументыКоторогоПодписываются);
	ПараметрыФункции.Вставить("Сообщение", Сообщение);
	ПараметрыФункции.Вставить("СертификатыПодписантов", СертификатыПодписантов);
	ПараметрыФункции.Вставить("ТипыДИВ", ТипыДИВ);
	ПараметрыФункции.Вставить("УчетнаяЗапись", УчетнаяЗапись);
	ПараметрыФункции.Вставить("ИспользоватьДлительныйМаркерБезопасности", МожноИспользоватьДлительныйМаркерБезопасностиДляСообщения(ТипСообщения));
	ПараметрыФункции.Вставить("ФорматДокументооборота", ФорматДокументооборота);
	
	Возврат ПараметрыФункции;

КонецФункции

Функция МожноИспользоватьДлительныйМаркерБезопасностиДляСообщения(ТипСообщения) 

	// Считаем, что использовать на сервере длительный маркер безопасности можно для автоподписания тех служебных квитанций,
	// которые автоматически отправляются при интерактивном работе в клиентском сеансе пользователя.
	ТипыАвтоотправляемыхСообщений = ЭлектронныйДокументооборотСКонтролирующимиОрганами.ТипыАвтоотправляемыхСообщений();
	
	Если ТипыАвтоотправляемыхСообщений.Найти(ТипСообщения) = Неопределено Тогда
		Возврат Ложь;
	КонецЕсли;

	// ФНС
	Если ТипСообщения = Перечисления.ТипыТранспортныхСообщений.ИзвещениеПисьмоНП  // Извещение о получении письма налогового органа
	 ИЛИ ТипСообщения = Перечисления.ТипыТранспортныхСообщений.РезультатПриемаДокументНП Тогда // Результат приема документа налогоплательщиком
		
	 	// Квитанции на документы, связанные с письмам и требованиями ФНС, автоматически не отправляем.
		Возврат Ложь;
	 
	КонецЕсли;
	
	Возврат Истина;

КонецФункции

Процедура ПодписатьСообщение(КонтролирующийОрган, КонтекстОтправки, Сообщение, ТипСообщения, СертификатПодписанта = Неопределено,
								УчетнаяЗапись = Неопределено, СообщениеОснование = Неопределено, СообщениеОбОшибке = "")

	// Внимание!
	// ---------
	// В Контейнере есть одноименная процедура, которая используется при отправке отчетов
	// в интерактивном режиме пользователем.
	// При изменениях необходимо синхронно менять обе процедуры.

	ЭтоЭлектроннаяПодписьВМоделиСервиса = Неопределено;
	КонтекстОтправки.Свойство("ЭПВМоделиСервиса", ЭтоЭлектроннаяПодписьВМоделиСервиса);
	
	ЦиклОбмена = ?(КонтекстОтправки.Свойство("ЦиклОбмена"), КонтекстОтправки.ЦиклОбмена, Неопределено);
	ФорматДокументооборота = ?(КонтекстОтправки.Свойство("ФорматДокументооборота"),
		КонтекстОтправки.ФорматДокументооборота, Неопределено);
	СообщениеОснованиеИПараметры = Новый Структура;
	СообщениеОснованиеИПараметры.Вставить("СообщениеОснование", 			СообщениеОснование);
	СообщениеОснованиеИПараметры.Вставить("ЦиклОбмена", 					ЦиклОбмена);
	СообщениеОснованиеИПараметры.Вставить("ФорматДокументооборота", 		ФорматДокументооборота);
	СообщениеОснованиеИПараметры.Вставить("ПолучитьФорматДокументооборота", КонтролирующийОрган = "ПФР");
	
	ПараметрыФункции = ПодготовитьПараметрыПодписатьСообщение(
		КонтролирующийОрган,
		ЭтоЭлектроннаяПодписьВМоделиСервиса,
		Сообщение,
		ТипСообщения,
		СертификатПодписанта,
		УчетнаяЗапись,
		СообщениеОснованиеИПараметры);

	КонтекстОтправки.Вставить("ЭПВМоделиСервиса", ЭтоЭлектроннаяПодписьВМоделиСервиса);
	ПараметрыФункции.Вставить("КонтекстОтправки", КонтекстОтправки);

	ДокументыДляПодписания = ПодготовитьДокументыДляПодписания(
		ПараметрыФункции.СообщениеДокументыКоторогоПодписываются,
		ПараметрыФункции.Сообщение,
		ПараметрыФункции.СертификатыПодписантов,
		ПараметрыФункции.ТипыДИВ,
		"");
	
	ПодписатьСообщенияПоследовательно(ПараметрыФункции, ДокументыДляПодписания, СообщениеОбОшибке);
			
КонецПроцедуры

Процедура ПодписатьСообщенияПоследовательно(ПараметрыФункции, ДокументыДляПодписания, СообщениеОбОшибке)

	// Внимание!
	// ---------
	// В Контейнере есть одноименная процедура, которая используется при отправке отчетов
	// в интерактивном режиме пользователем.
	// При изменениях необходимо синхронно менять обе процедуры.
	
	// Подписание на стороне сервера возможно только в режиме "ЭлектроннаяПодписьВМоделиСервиса".
	ЭПВМоделиСервиса = ПараметрыФункции.КонтекстОтправки.ЭПВМоделиСервиса;
	Если НЕ ЭПВМоделиСервиса Тогда
		СообщениеОбОшибке = "ЭтоНеЭлектроннаяПодписьВМоделиСервиса";
		Возврат;
	КонецЕсли;

	// Для вызова методов криптосервиса вызывающий код должен заранее установить маркеры безопасности.
	ПараметрыПодписания = Новый Структура();
	ПараметрыПодписания.Вставить("ОтсоединеннаяПодпись", Истина);
	Если ПараметрыФункции.Свойство("ИспользоватьДлительныйМаркерБезопасности") Тогда
		ПараметрыПодписания.Вставить("ИспользоватьДлительныйМаркерБезопасности", ПараметрыФункции.ИспользоватьДлительныйМаркерБезопасности);
	КонецЕсли;
	
	Для ТекущийДокумент = 0 По ДокументыДляПодписания.ВГраница() Цикл
		
		ДокументДляПодписывания = ДокументыДляПодписания[ТекущийДокумент];
		Данные = ИзвлечьДвоичныеДанныеПриНеобходимости(ДокументДляПодписывания.Данные);

		// Предполагается, что текущие методы для отправки со стороны сервера 
		// вызываются внутри фонового задания, поэтому к сервису подписания обращаемся напрямую
		// (уже без дополнительного фонового задания).
		Попытка
			ДанныеПодписи = СервисКриптографии.Подписать(
				Данные,
				ДокументДляПодписывания.Сертификат,
				"CMS",
				ПараметрыПодписания);
		Исключение
			// Запись в журнал регистрации зарегистрирована исходным методом, повторно не регистрируем.
			СообщениеОбОшибке = ПодробноеПредставлениеОшибки(ИнформацияОбОшибке());
			Возврат;
		КонецПопытки;
			
		// Если возникла ошибка при подписании, то прерываемся.
		Если ТипЗнч(ДанныеПодписи) = Тип("Структура") Тогда
			СообщениеОбОшибке = ДанныеПодписи.КодВозврата;
			Возврат;
		КонецЕсли;

		ИмяФайлаПодписи = СгенерироватьИмяФайлаДокументаВСоставеПакета();
		ДокументДляПодписывания.Вставить("ИмяФайлаПодписи", ИмяФайлаПодписи);	
		СтрокаBase64 = Base64Строка(ДанныеПодписи);

		// Cохраняем в регистр.
		ДобавитьСодержимоеТранспортногоКонтейнера(ПараметрыФункции.Сообщение,
			Перечисления.ТипыСодержимогоТранспортногоКонтейнера.ЭлектронноЦифроваяПодпись,
			Новый Структура("Строка64", СтрокаBase64),
			ОбщегоНазначенияКлиентСервер.РазложитьПолноеИмяФайла(ДокументДляПодписывания.ИмяФайлаПодписи).Имя,
			,
			ДокументДляПодписывания.ИмяФайла,
			ДокументДляПодписывания.Сертификат.Отпечаток,
			Перечисления.СтатусыПроверкиЭЦП.Валидна,
			Истина);
		
	КонецЦикла;
	
КонецПроцедуры

Функция ПодготовитьДокументыДляПодписания(СообщениеДокументыКоторогоПодписываются, Сообщение, СертификатыПодписантов, ТипыДИВ, ВременныйКаталог = "") Экспорт
	
	// получаем список ДИВ для подписания и существующие подписи
	ВложенияИПодписи = ПолучитьФайлыСообщенияДляПодписи(СообщениеДокументыКоторогоПодписываются, Сообщение, СертификатыПодписантов, ТипыДИВ);
	ВложенияДИВ = ВложенияИПодписи.ВложенияДИВ;
	Подписи = ВложенияИПодписи.Подписи;
	
	
	ДокументыДляПодписания = Новый Массив;
	
	// перебираем сертификаты
	Для Каждого ОбъектСертификат Из СертификатыПодписантов Цикл
		
		// определяем какие сообщения без подписи текущего подписанта
		ТребующиеПодписания = Новый Массив;
		Для Каждого ВложениеДИВ Из ВложенияДИВ Цикл
			Если НЕ СуществуютПодписиСЗаданнымИменемФайлаИСертификатом(Подписи, ВложениеДИВ.ИмяФайла, ОбъектСертификат.Отпечаток) Тогда
				ТребующиеПодписания.Добавить(ВложениеДИВ);
			КонецЕсли;
		КонецЦикла;
		
		Если ТребующиеПодписания.Количество() = 0 Тогда
			Продолжить;
		КонецЕсли;
		
		// создаем массив с именами документов, требующих подписания
		Для Каждого СтрДокументНаПодпись Из ТребующиеПодписания Цикл
			ДокументДляПодписывания = Новый Структура;
			ДокументДляПодписывания.Вставить("Данные", СтрДокументНаПодпись.Данные);
			ДокументДляПодписывания.Вставить("Сертификат", ОбъектСертификат);
			ДокументДляПодписывания.Вставить("ПолноеИмяФайла", ВременныйКаталог + СтрДокументНаПодпись.ИмяФайла);
			ДокументДляПодписывания.Вставить("ИмяФайла", СтрДокументНаПодпись.ИмяФайла);
			
			ДокументыДляПодписания.Добавить(ДокументДляПодписывания);
		КонецЦикла;
		
	КонецЦикла;

	Возврат ДокументыДляПодписания;
	
КонецФункции

Функция ПолучитьФайлыСообщенияДляПодписи(СообщениеДокументыКоторогоПодписываются, Сообщение, СертификатыПодписантов, ТипыДИВ)
	
	// получаем ДИВ
	ВложенияДИВ = ПолучитьВложенияТранспортногоСообщения(СообщениеДокументыКоторогоПодписываются, Истина, ТипыДИВ);
	ВложенияДИВ.Колонки.Добавить("Данные64", Новый ОписаниеТипов("Строка"));
	Для Каждого Стр Из ВложенияДИВ Цикл
		Стр.Данные64 = ПоместитьВоВременноеХранилище(Стр.Данные.Получить(), Новый УникальныйИдентификатор);
	КонецЦикла;
	
	// удаляем колонку с двоичными данными
	ВложенияДИВ.Колонки.Удалить(ВложенияДИВ.Колонки.Данные);
	
	// переименовываем колонку со строкой Данные64 в Данные
	ВложенияДИВ.Колонки.Данные64.Имя = "Данные";
	
	СтруктураДИВ = ТрансформироватьТЗвМассивСтруктур(ВложенияДИВ);
	
	// получаем подписи
	Подписи = ПолучитьВложенияТранспортногоСообщения(Сообщение, , Перечисления.ТипыСодержимогоТранспортногоКонтейнера.ЭлектронноЦифроваяПодпись);
	СтруктураПодписи = ТрансформироватьТЗвМассивСтруктур(Подписи);

	Возврат Новый Структура("ВложенияДИВ, Подписи", СтруктураДИВ, СтруктураПодписи);
	
КонецФункции

Функция ТрансформироватьТЗвМассивСтруктур(ВложенияДИВ)
	
	МассивРезультат = Новый Массив;
	Для Каждого Стр Из ВложенияДИВ Цикл
		
		НовСтруктура = Новый Структура;
		Для Каждого КолонокаВлождениеДИВ Из ВложенияДИВ.Колонки Цикл
			НовСтруктура.Вставить(КолонокаВлождениеДИВ.Имя, Стр[КолонокаВлождениеДИВ.Имя]);
		КонецЦикла;
		
		МассивРезультат.Добавить(НовСтруктура);
		
	КонецЦикла;
	
	Возврат МассивРезультат;
	
КонецФункции

Функция СуществуютПодписиСЗаданнымИменемФайлаИСертификатом(Подписи, ИмяФайла, Отпечаток)
	
	Для Каждого Стр Из Подписи Цикл
		Если Стр.ЭЦПИмяПодписанногоФайла = ИмяФайла И Стр.ЭЦПСертификат = Отпечаток Тогда
			Возврат Истина;
		КонецЕсли;
	КонецЦикла;
	Возврат Ложь;
	
КонецФункции

Функция ИзвлечьДвоичныеДанныеПриНеобходимости(Параметр)
	
	ИзвлеченныеДанные = Новый Массив;
	Если ТипЗнч(Параметр) = Тип("Массив") ИЛИ ТипЗнч(Параметр) = Тип("ФиксированныйМассив") Тогда
		Для Индекс = 0 По Параметр.ВГраница() Цикл
			ИзвлеченныеДанные.Добавить(ИзвлечьДвоичныеДанныеИзВременногоХранилищаПриНеобходимости(Параметр[Индекс]));
		КонецЦикла;
	Иначе
		ИзвлеченныеДанные = ИзвлечьДвоичныеДанныеИзВременногоХранилищаПриНеобходимости(Параметр);
	КонецЕсли;
	
	Возврат ИзвлеченныеДанные;
	
КонецФункции

Функция ИзвлечьДвоичныеДанныеИзВременногоХранилищаПриНеобходимости(Параметр)
	
	Если ТипЗнч(Параметр) = Тип("Строка") И ЭтоАдресВременногоХранилища(Параметр) Тогда
		Возврат ПолучитьИзВременногоХранилища(Параметр);
	Иначе
		Возврат Параметр;
	КонецЕсли;
	
КонецФункции

#КонецОбласти

#Область ОтправкаОтчетовНаСервереБезКлиентскогоВзаимодействия

// Отправляет временный пароль в виде sms лицу, которое должно подтвердить отправку отчета.
//
// Параметры:
//	ДокументСсылка - ДокументСсылка.РегламентированныйОтчет - Заполненный отчет,
//                 для отправки которого необходимо получить временный пароль.
//	СпособДоставкиПаролей - Строка - Одно из значений "phone" или "email".
//	ПовторнаяОтправка - Булево - Истина, если требуется повторная отправка.
//	СообщениеОбОшибке - Строка - Текст сообщения об ошибке, либо пустая строка, если ошибок не возникло.
//
// Возвращаемое значение:
//	Структура - см. СервисКриптографииСлужебный.ПолучитьВременныйПароль()
//
Функция ПолучитьВременныйПарольДляОбъектаОтправки(ДокументСсылка, СпособДоставкиПаролей, ПовторнаяОтправка, СообщениеОбОшибке) Экспорт

	СообщениеОбОшибке = "";
	Результат = Неопределено;
	
	МассивОбъектовСертификатов = ПолучитьСертификатыДляОбъектаОтправки(ДокументСсылка, СообщениеОбОшибке, Истина);

	Если ЗначениеЗаполнено(СообщениеОбОшибке) Тогда
		Возврат Неопределено;
	КонецЕсли;
	
	Если МассивОбъектовСертификатов.Количество() = 0 Тогда
		СообщениеОбОшибке = НСтр("ru = 'Не найден сертификат лица, которое может подписать отчет'");
		Возврат Неопределено;
	КонецЕсли;
		 
	Попытка

		// Чтобы в конфигурациях со старыми версиями БТС не возникали ошибки компиляции,
		// обращаемся к общему модулю через функцию БСП.
		// Для возможности отправки отчетов на стороне сервера из таких конфигураций потребуется обновить БТС до 1.0.17.
		МодульСервисКриптографии = ОбщегоНазначения.ОбщийМодуль("СервисКриптографии");
	
		Результат = МодульСервисКриптографии.ПолучитьВременныйПароль(
			 МассивОбъектовСертификатов[0].Идентификатор,
			 ПовторнаяОтправка,
			 СпособДоставкиПаролей);
		
	Исключение
	
		СообщениеОбОшибке = ПодробноеПредставлениеОшибки(ИнформацияОбОшибке());
		ЗаписатьСобытиеВЖурнал(УровеньЖурналаРегистрации.Ошибка, СообщениеОбОшибке, ДокументСсылка);
	
	КонецПопытки;
	
	Возврат Результат;
	
КонецФункции

// Получает по переданному временному паролю сеансовый ключ для отправки декларации.
//
Процедура ПодтвердитьВременныйПарольДляОбъектаОтправки(ДокументСсылка, ВременныйПароль, СообщениеОбОшибке) Экспорт

	МассивОбъектовСертификатов = ПолучитьСертификатыДляОбъектаОтправки(ДокументСсылка, СообщениеОбОшибке, Истина);

	Если ЗначениеЗаполнено(СообщениеОбОшибке) Тогда
		Возврат;
	КонецЕсли;
	
	Если МассивОбъектовСертификатов.Количество() = 0 Тогда
		СообщениеОбОшибке = НСтр("ru = 'Не найден сертификат лица, которое может подписать отчет'");
		Возврат;
	КонецЕсли;
	
	Попытка

		// Чтобы в конфигурациях со старыми версиями БТС не возникали ошибки компиляции,
		// обращаемся к общему модулю через функцию БСП.
		// Для возможности отправки отчетов на стороне сервера из таких конфигураций потребуется обновить БТС до 1.0.17.
	
		МодульСервисКриптографии = ОбщегоНазначения.ОбщийМодуль("СервисКриптографии");
	
		МодульСервисКриптографии.ПодтвердитьВременныйПароль(МассивОбъектовСертификатов[0].Идентификатор, ВременныйПароль);
	
	Исключение

		СообщениеОбОшибке = ПодробноеПредставлениеОшибки(ИнформацияОбОшибке());
		
		ЗаписатьСобытиеВЖурнал(УровеньЖурналаРегистрации.Ошибка, СообщениеОбОшибке, ДокументСсылка);
	
	КонецПопытки;
	
КонецПроцедуры

// Возвращает структуру с дополнительными параметрами для ОтправитьРегламентированныйОтчет().
//
// Возвращаемое значение:
//	Структура - Содержит ключи:
//		* ТолькоПроверкаБезОтправки - Булево - Если Истина, то выполняются только проверки возможности отправки,
//          которые не требуют обращения к маркерам безопасности. Собственно отправка не выполняется.
//			По умолчанию Ложь.
//		* ТребуетсяАвтонастройкаУчетнойЗаписи - Булево - Если Истина, то будет произведена автонастройка учетной записи перед отправкой.
//			По умолчанию Истина.
//
Функция НовыеПараметрыОтправки() Экспорт

	Результат = Новый Структура();
	Результат.Вставить("ТолькоПроверкаБезОтправки",           Ложь);
	Результат.Вставить("ТребуетсяАвтонастройкаУчетнойЗаписи", Истина);
	
	Возврат Результат;

КонецФункции

// Выполняет отправку регламентированного отчета, поддерживающего отправку на стороне сервера без обращения к клиентскому контексту.
//
// Параметры:
//	СсылкаНаОтчет - ДокументСсылка.* - Документ, содержащий данные отчета для отправки.
//		Допустимы типы документов, которые содержатся в составе типов измерения Ссылка регистра сведений ЖурналОтчетовСтатусы.
//	СообщениеОбОшибке - Строка - Текст с сообщением о возникшей ошибке. Если пусто, то отправка прошла успешно.
//	ПараметрыОтправки - Структура, Неопределено - см. НовыеПараметрыОтправки().
//
Процедура ОтправитьРегламентированныйОтчет(СсылкаНаОтчет, СообщениеОбОшибке, ПараметрыОтправки = Неопределено) Экспорт

	// Внимание!
	// ---------
	// В модуле общей формы РегламентированнаяОтчетность есть аналогичные процедуры 
	//	- ОтправитьОтчетПодтверждениеОтправкиОтчетаЗавершение(),
	//	- ПроверкаВозможностиВыгрузкиРегламентированногоОтчетаПередОтправкой(),
	//	- ОтправитьОтчетЗавершение(),
	// а также в общих модулях:
	//  - ЭлектронныйДокументооборотСКонтролирующимиОрганамиКлиент.ПриНажатииНаКнопкуОтправкиВКонтролирующийОрган()
	//  - ДокументооборотСКОКлиент.ПриНажатииНаКнопкуОтправкиВКонтролирующийОрган(),
	//    ОтправкаВКонтролирующийОрганПослеПодтвержденияОтправки() 
	// и вызываемые из них, которые используется при отправке отчетов в интерактивном режиме пользователем.
	//
	// При изменениях необходимо синхронно менять такие процедуры.

	СообщениеОбОшибке = "";

	Если НЕ ЗначениеЗаполнено(СсылкаНаОтчет) Тогда
		СообщениеОбОшибке = НСтр("ru = 'Не указан отчет для отправки'");
		Возврат;
	КонецЕсли;
	
	ВидКонтролирующегоОргана = ТипКонтролирующегоОрганаДляОбъектаОтправки(СсылкаНаОтчет);

	// Без взаимодействия с клиентским контекстом в настоящее время доступна только отправка в ФНС, ПФР и ФСС.
	Если ВидКонтролирующегоОргана <> Перечисления.ТипыКонтролирующихОрганов.ФНС
		И ВидКонтролирующегоОргана <> Перечисления.ТипыКонтролирующихОрганов.ПФР
		И ВидКонтролирующегоОргана <> Перечисления.ТипыКонтролирующихОрганов.ФСС Тогда
		СообщениеОбОшибке = СтрШаблон("Отправка отчетности в ""%1"" возможна только в интерактивном режиме работы", ВидКонтролирующегоОргана);
		Возврат;
	КонецЕсли;

	ТолькоПроверкаБезОтправки = ОбщегоНазначенияКлиентСервер.СвойствоСтруктуры(
		ПараметрыОтправки, "ТолькоПроверкаБезОтправки", Ложь);

	ТребуетсяАвтонастройкаУчетнойЗаписи = ОбщегоНазначенияКлиентСервер.СвойствоСтруктуры(
		ПараметрыОтправки, "ТребуетсяАвтонастройкаУчетнойЗаписи", Истина);

	КонтролирующийОрган = ЭлектронныйДокументооборотСКонтролирующимиОрганамиВызовСервера.ИмяПеречисления(ВидКонтролирующегоОргана);

	// Определим организацию отправляемого отчета.
	Если НЕ ОбщегоНазначения.ЕстьРеквизитОбъекта("Организация", СсылкаНаОтчет.Метаданные()) Тогда
		СообщениеОбОшибке = СтрШаблон(НСтр("ru = 'Отсутствует реквизит ""Организация"" у объекта ""%1"", отправка невозможна'"), СсылкаНаОтчет);
		Возврат;
	КонецЕсли;
	ОрганизацияОтчета = ОбщегоНазначения.ЗначениеРеквизитаОбъекта(СсылкаНаОтчет, "Организация");

	// Создадим структуру с данными отчета, используемыми при отправке
	// см. ДокументооборотСКОКлиент.ПриНажатииНаКнопкуОтправкиВКонтролирующийОрган()
	
	ПараметрыОтчета = Новый Структура;

	ПараметрыОтчета.Вставить("СсылкаНаОтчет",					СсылкаНаОтчет);
	ПараметрыОтчета.Вставить("КонтролирующийОрган",				КонтролирующийОрган);
	ПараметрыОтчета.Вставить("ЭтоОтправкаИзФормыОтчетность",	Ложь);
	ПараметрыОтчета.Вставить("ОрганизацияОтчета",				ОрганизацияОтчета);
	// В настоящее время перечисленные ниже типы сообщений не поддерживаются при работе на сервере без клиентского взаимодействия.
	ПараметрыОтчета.Вставить("ЭтоУведомлениеФНС",				Ложь);
	ПараметрыОтчета.Вставить("ЭтоЖурналСчетовФактурФНС",		Ложь);
	ПараметрыОтчета.Вставить("ЭтоРеестрНДС",					Ложь);
	ПараметрыОтчета.Вставить("ЭтоРеестрАкцизы",					Ложь);
	ПараметрыОтчета.Вставить("ТолькоПроверкаБезОтправки",           ТолькоПроверкаБезОтправки);
	ПараметрыОтчета.Вставить("ТребуетсяАвтонастройкаУчетнойЗаписи", ТребуетсяАвтонастройкаУчетнойЗаписи);
	
	Если ТипЗнч(СсылкаНаОтчет) = Тип("ДокументСсылка.РегламентированныйОтчет") Тогда
		// Отдельные виды проверок доступны только для видов документов "РегламентированныйОтчет".
		ПредставлениеДокументаРеглОтч = Неопределено;

		ПараметрыРеглОтчета = РегламентированнаяОтчетностьВызовСервера.ПолучитьПараметрыФормыИзСохраненногоОтчета(
			СсылкаНаОтчет,
			ПредставлениеДокументаРеглОтч);

		// Допишем в общие параметры отчета.
		ОбщегоНазначенияКлиентСервер.ДополнитьСтруктуру(ПараметрыОтчета, ПараметрыРеглОтчета, Истина);

		ПередОтправкойРегламентированногоОтчета(ПараметрыОтчета, СообщениеОбОшибке);
		Если ЗначениеЗаполнено(СообщениеОбОшибке) Тогда
			Возврат;
		КонецЕсли;

		ПередВыгрузкойРегламентированногоОтчета(ПараметрыОтчета, СообщениеОбОшибке);
		Если ЗначениеЗаполнено(СообщениеОбОшибке) Тогда
			Возврат;
		КонецЕсли;
		
		ПроверкаФорматаПоПериодуПрименения(ПараметрыОтчета, СообщениеОбОшибке);
		Если ЗначениеЗаполнено(СообщениеОбОшибке) Тогда
			Возврат;
		КонецЕсли;

	КонецЕсли;

	// Проверку наличия необработанного заявления на изменения не выполняем,
	// как при интерактивной работе - см. КонтейнерКлиентскихМетодов.ЗавершитьНастройкуЗаявленияНаИзменение() - 
	// т.к. такое заявление может не влиять на текущую отправку и возвращать о нем ошибку в мобильное приложение не нужно.
	// Если же оно является критичным для текущей отправки (например, заявление на продление окончившегося сертификата),
	// то текущая отправка будет прервана по ошибке на более поздних шагах.

	Если КонтролирующийОрган = "ФСС" Тогда 
		ОтправкаРегламентированногоОтчетаВФСС(ПараметрыОтчета, СообщениеОбОшибке);	
	Иначе

		// Общие для всех остальных контролирующих органов предварительные процедуры перед отправкой.
		ПроверитьЧтоЭДОПодключен(ПараметрыОтчета, СообщениеОбОшибке);
		Если ЗначениеЗаполнено(СообщениеОбОшибке) Тогда
			Возврат;
		КонецЕсли;
		
		Если КонтролирующийОрган = "ФНС" Тогда
			ОтправкаРегламентированногоОтчетаВФНС(ПараметрыОтчета, СообщениеОбОшибке);
		ИначеЕсли КонтролирующийОрган = "ПФР" Тогда
			ОтправкаРегламентированногоОтчетаВПФР(ПараметрыОтчета, СообщениеОбОшибке);
		КонецЕсли;
		
	КонецЕсли;

КонецПроцедуры

Функция ПолучитьСертификатыДляОбъектаОтправки(ДокументСсылка, СообщениеОбОшибке, ТребуетсяАвтонастройкаУчетнойЗаписи)

	СообщениеОбОшибке = "";
	
	УчетнаяЗапись = УчетнаяЗаписьПоОбъектуОтправки(ДокументСсылка);
	Если НЕ ЗначениеЗаполнено(УчетнаяЗапись) Тогда
		СообщениеОбОшибке = "ПоказатьФормуПредложениеОформитьЗаявлениеНаПодключение";
		Возврат Неопределено;
	КонецЕсли;
	
	// Выполним автонастройку учетной записи, чтобы в дальнейшем работать с актуальными сертификатами.
	Если ТребуетсяАвтонастройкаУчетнойЗаписи Тогда
		АвтоматическаяНастройкаУчетнойЗаписи(УчетнаяЗапись, СообщениеОбОшибке);
		Если ЗначениеЗаполнено(СообщениеОбОшибке) Тогда
			Возврат Неопределено;
		КонецЕсли;
	КонецЕсли;
	
	// Получим сертификаты из учетной записи.
	НастройкиУчетнойЗаписи = ПолучитьНастройкиУчетнойЗаписиИзИБ(УчетнаяЗапись);
	
	Сертификаты = СформироватьПакетФНС_ПодготовитьСертификатыДляПоиска(
		УчетнаяЗапись,
		НастройкиУчетнойЗаписи.СертификатРуководителя,
		НастройкиУчетнойЗаписи.СертификатГлавногоБухгалтера,
		НастройкиУчетнойЗаписи.СертификатДляШифрования);

	Если Сертификаты = Неопределено Тогда
		СообщениеОбОшибке = ТекстСообщенийПользователю();
		Возврат Неопределено;
	КонецЕсли;
		
	// Проверку сертификатов не выполняем, т.к. для нее требуется обращение к сервису криптографии,
	// а мы сейчас только готовимся запросить у него временный пароль для дальнейшей работы.
	РезультатПоиска = НайтиСертификаты(Сертификаты, Ложь);
	
	ОбъектыСертификатов = СформироватьПакетФНС_ПроверкиСертификатовПослеПолученияСертификатовРазобратьНайденное(
		РезультатПоиска.Сертификаты,
		НастройкиУчетнойЗаписи.СертификатРуководителя,
		НастройкиУчетнойЗаписи.СертификатГлавногоБухгалтера,
		НастройкиУчетнойЗаписи.СертификатДляШифрования);

	МассивОбъектовСертификатов = СертификатыДляПодписанияПоТипуСообщения(
		 Перечисления.ТипыТранспортныхСообщений.ДекларацияНП,
		 ОбъектыСертификатов.ОбъектСертификатРуководителя,
		 ОбъектыСертификатов.ОбъектСертификатГлавногоБухгалтера,
		 ОбъектыСертификатов.ОбъектСертификатОтветственногоЗаШифрование);
		 
	Возврат МассивОбъектовСертификатов;

КонецФункции

Процедура ПередОтправкойРегламентированногоОтчета(ПараметрыОтчета, СообщениеОбОшибке)

	ПроверкаОнлайнБлокировки(ПараметрыОтчета, "О", СообщениеОбОшибке);

КонецПроцедуры

Процедура ПередВыгрузкойРегламентированногоОтчета(ПараметрыОтчета, СообщениеОбОшибке)
	
	ПроверкаОнлайнБлокировки(ПараметрыОтчета, "В", СообщениеОбОшибке);
	
КонецПроцедуры

Процедура ПроверкаФорматаПоПериодуПрименения(ПараметрыОтчета, СообщениеОбОшибке)
	
	// Если механизм выключен, то прервем проверку.
	Если НЕ ОнлайнСервисыРегламентированнойОтчетностиВызовСервера.МеханизмОнлайнСервисовВключен() Тогда
		Возврат;
	КонецЕсли;
	
	// Имя отчета
	ИДОтчета = ПараметрыОтчета.ИсточникОтчета;
	
	// Получаем период отчета.
	ДатаКонцаПериодаОтчета = ПараметрыОтчета.мДатаКонцаПериодаОтчета;
	
	// Получаем имя формы.
	ИмяФормы = ВРЕГ(ПараметрыОтчета.мВыбраннаяФорма);
	
	СведенияПоБлокировке = ОнлайнСервисыРегламентированнойОтчетностиВызовСервера.ПроверкаФорматаПоПериодуПримененияСервер(
		ПараметрыОтчета, ДатаКонцаПериодаОтчета, ИмяФормы, ИДОтчета);
		
	// Если проверка показала, что блокировать не нужно, то предупреждения не показываем.
	Если СведенияПоБлокировке.РезультатПроверки Тогда
		Возврат;
	КонецЕсли;
	
	Если СведенияПоБлокировке.Свойство("ТекстПодробнее") И СведенияПоБлокировке.Свойство("ТекстИспользуйте") Тогда
		ТекстКомментарий = СтроковыеФункцииКлиентСервер.ПодставитьПараметрыВСтроку(
			НСтр("ru = '%1. Необходимо использовать %2.'"), СведенияПоБлокировке.ТекстПодробнее, СведенияПоБлокировке.ТекстИспользуйте);
	Иначе
		ТекстКомментарий = НСтр("ru = 'Формат выгрузки более не применяется. Попробуйте позже.'");
	КонецЕсли;

	СообщениеОбОшибке = СтроковыеФункцииКлиентСервер.ПодставитьПараметрыВСтроку(
		НСтр("ru = 'В настоящее время отправить %1 нельзя по причине: %2'"),
		ПараметрыОтчета.мСохраненныйДок, ТекстКомментарий);
	
КонецПроцедуры

// Выполняет проверку того, что форма отчета не заблокирована для использования, отправки и т.п.
//
// Параметры:
//	ПараметрыОтчета - Структура - См. РегламентированнаяОтчетностьВызовСервера.ПолучитьПараметрыФормыИзСохраненногоОтчета().
//	БлокируемаяФункция - Строка - Допустимые значения: "И" - использование, "О" - отправка, "П" - печать, 
//		"ПВ" - внешняя печать, "В" - выгрузка, "ЗГ" - загрузка, "З" - заполнение.
//	СообщениеОбОшибке - Строка - Текст с сообщением о возникшей ошибке. Если пусто, то отправка прошла успешно.
//
Процедура ПроверкаОнлайнБлокировки(ПараметрыОтчета, БлокируемаяФункция = "И", СообщениеОбОшибке = "")

	Если НЕ ОнлайнСервисыРегламентированнойОтчетностиВызовСервера.МеханизмОнлайнСервисовВключен() Тогда
		Возврат;
	КонецЕсли;
	
	ПолноеИмяФормы = РегламентированнаяОтчетностьВызовСервера.ПолныйПутьКФорме(
		ПараметрыОтчета.ИсточникОтчета, ПараметрыОтчета.мВыбраннаяФорма);
	
	ОбъектОтчета = РегламентированнаяОтчетностьВызовСервера.ОбъектОтчета(ПолноеИмяФормы);
	
	ВерсияОтчета = РегламентированнаяОтчетностьВызовСервера.ПолучитьВерсиюРегламентированногоОтчета(ОбъектОтчета);
	ВерсияВыпуска = ВРЕГ(?(ЗначениеЗаполнено(ВерсияОтчета),
		ВерсияОтчета,
		ОнлайнСервисыРегламентированнойОтчетностиВызовСервера.ИнформацияОПрограмме().ВерсияКонфигурации));
	
	ИДОтчета = ПараметрыОтчета.ИсточникОтчета;
	ИДФормы  = ВРЕГ(ПараметрыОтчета.мВыбраннаяФорма);
	
	СведенияПоБлокировке = ОнлайнСервисыРегламентированнойОтчетностиВызовСервера.ПроверкаОнлайнБлокировкиСервер(
		ВерсияВыпуска, ИДОтчета, ИДФормы, БлокируемаяФункция);
	
	// Если проверка показала, что блокировать не нужно
	// либо блокируется отправка, но модуль уже обновлен на тот, который содержит исправление ошибки,
	// то предупреждения не показываем.
	Если СведенияПоБлокировке.РезультатПроверки 
		ИЛИ (БлокируемаяФункция = "О" И ОнлайнСервисыРегламентированнойОтчетностиВызовСервера.ВерсияМодуляВБазеСодержитИсправление(
				СведенияПоБлокировке.ВерсияМодуляДокументооборотаСИсправлениемОшибки) = Истина) Тогда
		Возврат;
	КонецЕсли;

	СообщениеОбОшибке = СтроковыеФункцииКлиентСервер.ПодставитьПараметрыВСтроку(
		НСтр("ru = 'В настоящее время отправить %1 нельзя по причине: %2. Попробуйте позже.'"),
		ПараметрыОтчета.мСохраненныйДок, СведенияПоБлокировке.Комментарий);

КонецПроцедуры

Процедура ПроверитьЧтоЭДОПодключен(ПараметрыОтчета, СообщениеОбОшибке)

	// Внимание!
	// ---------
	// В общем модуле ДокументооборотСКОКлиент есть аналогичная процедура ПроверитьЧтоЭДОПодключенПослеДобавленияКонтекстаВПараметры(),
	// которая используется при отправке отчетов в интерактивном режиме пользователем.
	// При изменениях необходимо синхронно менять обе процедуры.

	НастроенОбменВУниверсальномФормате = Ложь;
	УчетнаяЗаписьПредназначенаДляДокументооборотаСКО = Ложь;
	
	ДокументооборотСКОВызовСервера.ПриНажатииНаКнопкуОтправкиВКонтролирующийОрган(
		ПараметрыОтчета.ОрганизацияОтчета, 
		ПараметрыОтчета.КонтролирующийОрган, 
		НастроенОбменВУниверсальномФормате, 
		УчетнаяЗаписьПредназначенаДляДокументооборотаСКО);

	Если НЕ НастроенОбменВУниверсальномФормате Тогда
		
		ПроверитьСтатусПодключенияПередОтправкой(ПараметрыОтчета.СсылкаНаОтчет, СообщениеОбОшибке);

		Возврат;
		
	Иначе

		Если УчетнаяЗаписьПредназначенаДляДокументооборотаСКО = Ложь Тогда
			
			СообщитьОНеподключенномНаправлении(ПараметрыОтчета.СсылкаНаОтчет, СообщениеОбОшибке);
			Возврат;
			
		ИначеЕсли ДокументооборотСКОКлиентСервер.ЭтоФНС(ПараметрыОтчета.КонтролирующийОрган)
			И НЕ ДокументооборотСКОВызовСервера.ОрганизацияПодключенаКИнспекции(ПараметрыОтчета.СсылкаНаОтчет) Тогда
			
			СообщитьОНеподключенномНаправлении(ПараметрыОтчета.СсылкаНаОтчет, СообщениеОбОшибке);
			Возврат;
			
		КонецЕсли;

	КонецЕсли;

КонецПроцедуры

//Преобразует текст в ДвоичныеДанные, при необходимости предварительно кодирует в base64
//КодироватьВBase64 и ДекодироватьИзBase64 взаимоисключающие параметры
Процедура ОбработкаДанныхХранилищаНаСервере(Адрес, ПреобразоватьВДвоичныеДанные = Ложь, КодироватьВBase64 = Ложь, ДекодироватьИзBase64 = Ложь, ПолноеИмяФайлаНаСервере = Неопределено, ИгнорироватьКодировку = Ложь)
	
	Данные = ПолучитьИзВременногоХранилища(Адрес);
	
	РаботаемСДД = ТипЗнч(Данные) = Тип("ДвоичныеДанные");
	
	Если РаботаемСДД И ПреобразоватьВДвоичныеДанные И Не КодироватьВBase64 И Не ДекодироватьИзBase64 Тогда
		Возврат;
	КонецЕсли;
	
	Временный = ПолучитьИмяВременногоФайла("bin");
		
	Если ДекодироватьИзBase64 Тогда
						
		Если РаботаемСДД Тогда			
			Данные.Записать(Временный);
			
			ТД = Новый ТекстовыйДокумент();
			ТД.Прочитать(Временный);
			ДанныеBase64 = ТД.ПолучитьТекст();
			ДД = Base64Значение(ДанныеBase64);
		Иначе
			ДД = Base64Значение(Данные);
		КонецЕсли;
		
		Если Не ПреобразоватьВДвоичныеДанные Тогда
			ДД.Записать(Временный);
			
			ТД = Новый ТекстовыйДокумент();
			ТД.Прочитать(Временный);
			ДанныеBase64 = ТД.ПолучитьТекст();
			
			ДД = ДанныеBase64;
		КонецЕсли;
		
	ИначеЕсли КодироватьВBase64 Тогда
		
		Если РаботаемСДД Тогда
			Б64Данные = Base64Строка(Данные);
		Иначе			
			//<?xml version="1.0" encoding="utf-8"?>
			Кодировка = "windows-1251";
			Декларация = Лев(Данные, 50);
			Если (Найти(НРег(Декларация), "utf-8")>0) или (Найти(НРег(Декларация), "encoding=") = 0) Тогда //Нет указания кодировки или явно указано UTF-8
				Кодировка = КодировкаТекста.UTF8;
			КонецЕсли;
			ТД = Новый ТекстовыйДокумент();
			ТД.УстановитьТекст(Данные); 
			ТД.Записать(Временный, Кодировка, "");
	
			ДД = Новый ДвоичныеДанные(Временный);
			Б64Данные = Base64Строка(ДД);
		КонецЕсли;			

		ТД = Новый ТекстовыйДокумент();
		ТД.УстановитьТекст(Б64Данные); 
		ТД.Записать(Временный, "windows-1251", "");
		
		Если ПреобразоватьВДвоичныеДанные Тогда
			ДД = Новый ДвоичныеДанные(Временный);
		Иначе
			ДД = Б64Данные;
		КонецЕсли;
	ИначеЕсли ПреобразоватьВДвоичныеДанные И Не РаботаемСДД Тогда
		Кодировка = "windows-1251";
		
		Если НЕ ИгнорироватьКодировку Тогда
			Декларация = Лев(Данные, 50);
			Если (Найти(НРег(Декларация), "encoding=""utf-8")>0)  Тогда //Явно указано UTF-8
				Кодировка = КодировкаТекста.UTF8;
			КонецЕсли;
		КонецЕсли;
			
		ТД = Новый ТекстовыйДокумент();
		ТД.УстановитьТекст(Данные); 
		ТД.Записать(Временный, Кодировка, "");
	
		ДД = Новый ДвоичныеДанные(Временный);
	КонецЕсли;	
	
	ПоместитьВоВременноеХранилище(ДД, Адрес);
	
	Если ПолноеИмяФайлаНаСервере = Неопределено Тогда
		УдалитьФайлы(Временный);
	Иначе
		ПолноеИмяФайлаНаСервере = Временный;
	КонецЕсли;
	
КонецПроцедуры

Функция ЗагрузитьДанныеНаСервер(ТекстФайлаВыгрузки, УникальныйИдентификатор, ПолноеИмяФайлаНаСервере = Неопределено, ПреобразоватьВДвоичныеДанные = Ложь, КодироватьВBase64 = Ложь, ДекодироватьИзBase64 = Ложь, ИгнорироватьКодировку = Ложь) Экспорт
	
	Адрес = ПоместитьВоВременноеХранилище(ТекстФайлаВыгрузки, УникальныйИдентификатор);
	Если ПреобразоватьВДвоичныеДанные Или КодироватьВBase64 Или ДекодироватьИзBase64 Тогда
		ОбработкаДанныхХранилищаНаСервере(Адрес, ПреобразоватьВДвоичныеДанные, КодироватьВBase64, ДекодироватьИзBase64, ПолноеИмяФайлаНаСервере, ИгнорироватьКодировку);	
	КонецЕсли;
	Возврат Адрес;
	
КонецФункции

Функция КодПодчиненности(ХранилищеДанныхОтчета) Экспорт
	
	Код = "";
	ДанныеОтчета = ХранилищеДанныхОтчета.Получить();
	Если ДанныеОтчета.ПоказателиОтчета.Свойство("ПолеТабличногоДокументаТитульный") Тогда
		ТитульныйЛист = ДанныеОтчета.ПоказателиОтчета.ПолеТабличногоДокументаТитульный;
	Иначе
		ТитульныйЛист = ДанныеОтчета.ПоказателиОтчета.ПолеТабличногоДокументаПриложение1;
	КонецЕсли;
	Если ТитульныйЛист.Свойство("КодПодчиненности") Тогда
		Код = ТитульныйЛист.КодПодчиненности;
	ИначеЕсли ТитульныйЛист.Свойство("КодПодчиненности_1") Тогда
		Код = ТитульныйЛист.КодПодчиненности_1
			+ ТитульныйЛист.КодПодчиненности_2
			+ ТитульныйЛист.КодПодчиненности_3
			+ ТитульныйЛист.КодПодчиненности_4
			+ ТитульныйЛист.КодПодчиненности_5;
	КонецЕсли;
	
	Возврат Код;
	
КонецФункции

Функция ВидОтправляемогоДокументаПоСсылке(ОбъектСсылка) Экспорт
	
	ТипОбъекта = Тип(ОбъектСсылка);
	ТаблицаОписания = РегламентированнаяОтчетность.ТаблицаОписанияОбъектовРегламентированнойОтчетности();
	СтрокаТаблицыОписания = ТаблицаОписания.Найти(ТипОбъекта, "ТипОбъекта");
	
	Если СтрокаТаблицыОписания = Неопределено Тогда
		Возврат Неопределено;
	КонецЕсли;
	
	Возврат СтрокаТаблицыОписания.ВидЭлектронногоПредставления;
	
КонецФункции

Функция ОтправитьТранспортноеСообщениеИЗаписатьСтатусОтправки(Контекст) Экспорт
	
	// отправляем
	Если НЕ ОтправитьТранспортноеСообщение(Контекст.ТранспортноеСообщение) Тогда
		ТекстСообщения = НСтр("ru = 'Не удалось отправить транспортное сообщение!'");
		ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
		Возврат Ложь;
	КонецЕсли;
	
	// пишем статус отправки
	ЗаписатьСтатусОтправкиОбъекта(Контекст.Документ, Перечисления.СтатусыОтправки.Отправлен);
	
	Возврат Истина;
	
КонецФункции

// Возвращает текст файла выгрузки регламентированного отчета.
//
// Параметры:
// 	РезультатВыгрузки - Структура - результат выгрузки, см. РегламентированнаяОтчетность.НовыйРезультатВыгрузкиРеглОтчета().
//
// Возвращаемое значение:
//	Строка - Текст файла выгрузки или пустая строка, если выгрузка не выполнена.
//
Функция ТекстФайлаВыгрузки(РезультатВыгрузки)

	Если НЕ РезультатВыгрузки.РеглОтчетВыгружен Тогда
		Возврат "";
	КонецЕсли;

	ФайлВыгрузки = ПолучитьИзВременногоХранилища(РезультатВыгрузки.АдресФайлаВыгрузки);
	
	Если ФайлВыгрузки = Неопределено Тогда
		Возврат "";
	КонецЕсли;
	
	ИмяВременногоФайла = ПолучитьИмяВременногоФайла("xml");
	ФайлВыгрузки.Записать(ИмяВременногоФайла);

	ЧтениеТекста = Новый ЧтениеТекста(ИмяВременногоФайла, РезультатВыгрузки.КодировкаФайлаВыгрузки);

	ТекстВыгрузки = ЧтениеТекста.Прочитать();

	ЧтениеТекста.Закрыть();
	ЧтениеТекста = Неопределено;

	УдалитьФайлы(ИмяВременногоФайла);

	Возврат ТекстВыгрузки;

КонецФункции

// Возвращает тексты с описание ошибок выгрузки.
//
// Параметры:
// 	РезультатВыгрузки - Структура - результат выгрузки, см. РегламентированнаяОтчетность.НовыйРезультатВыгрузкиРеглОтчета().
//	СохраненныйОтчет - ДокументСсылка.РегламентированныйОтчет - Выгружаемый регламентированный отчет.
//
// Возвращаемое значение:
//	Строка - Текст ошибок или пустая строка, если ошибок не было.
//
Функция ОписаниеОшибокВыгрузки(РезультатВыгрузки, СохраненныйОтчет)

	Если РезультатВыгрузки.РеглОтчетВыгружен Тогда
		Возврат "";
	КонецЕсли;

	Если РезультатВыгрузки.Ошибки.Количество() > 0 Тогда
		СообщениеОбОшибке = СтрСоединить(РезультатВыгрузки.Ошибки);
	Иначе    
		СообщениеОбОшибке = СтроковыеФункцииКлиентСервер.ПодставитьПараметрыВСтроку(
			НСтр("ru = 'Невозможно выгрузить отчет ""%1""'"), СохраненныйОтчет);
	КонецЕсли;
	
	Возврат СообщениеОбОшибке;

КонецФункции

#Область ОтправкаРегламентированногоОтчетаВФНС

Процедура ОтправкаРегламентированногоОтчетаВФНС(ПараметрыОтчета, СообщениеОбОшибке)

	// Внимание!
	// ---------
	// В Контейнере есть аналогичная процедура ОтправкаРегламентированногоОтчетаВФНСПослеПодтвержденияПовторнойОтправки,
	// которая используется при отправке отчетов в интерактивном режиме пользователем.
	// При изменениях необходимо синхронно менять обе процедуры.

	Док = ПараметрыОтчета.мСохраненныйДок;
	
	// инициализируем контекст отправки
	КонтекстОтправки = Новый Структура;
	
	Если НЕ ОтправкаРегламентированногоОтчетаВФНС_ПередВыгрузкойФайла(КонтекстОтправки, Док, Новый УникальныйИдентификатор, СообщениеОбОшибке) Тогда
		
		Если КонтекстОтправки.Свойство("ОтсутствуетУчетнаяЗапись") Тогда
			
			ПроверитьСтатусПодключенияПередОтправкой(Док, СообщениеОбОшибке);
			
		ИначеЕсли КонтекстОтправки.Свойство("НаправлениеНеПодключено") Тогда
			СообщитьОНеподключенномНаправлении(Док, СообщениеОбОшибке);
		Иначе
			
			Если НЕ ЗначениеЗаполнено(СообщениеОбОшибке) Тогда
				СообщениеОбОшибке = "ОповеститьОНеудачнойОтправке";
			КонецЕсли;

		КонецЕсли;
		
		Возврат;
	КонецЕсли;
	
	// выгружаем отчет и доверенность, при необходимости
	ВыгрузитьРегламентированныйОтчетИДоверенностьДляОтправки(КонтекстОтправки, СообщениеОбОшибке);
	Если ЗначениеЗаполнено(СообщениеОбОшибке) Тогда
		Возврат;
	КонецЕсли;

	Если ПараметрыОтчета.ТолькоПроверкаБезОтправки Тогда
		// Следующие процедуры выполняют либо сохранение в базе данных об отправке либо могут вызывать сервис криптографии,
		// поэтому в режиме "Только проверка без отправки" их уже не выполняем.
		// Считаем, что текущий отчет может быть отправлен.
		Возврат;
	КонецЕсли;

	ВыгрузитьРегламентированныйОтчетИДоверенностьДляОтправкиПослеВыгрузкиЗавершение(КонтекстОтправки, СообщениеОбОшибке);
	Если ЗначениеЗаполнено(СообщениеОбОшибке) Тогда
		Возврат;
	КонецЕсли;

	Если ПараметрыОтчета.ТребуетсяАвтонастройкаУчетнойЗаписи Тогда
		ВыполнитьАвтонастройкуУчетнойЗаписиПередОтправкойОбъекта(Док, СообщениеОбОшибке);
		Если ЗначениеЗаполнено(СообщениеОбОшибке) Тогда
			Возврат;
		КонецЕсли;
	КонецЕсли;

	ОтправкаРегламентированногоОтчетаВФНСПослеАвтонастройкиУчетнойЗаписи(КонтекстОтправки, СообщениеОбОшибке);
	Если ЗначениеЗаполнено(СообщениеОбОшибке) Тогда
		Возврат;
	КонецЕсли;

	ОтправкаРегламентированногоОтчетаВФНСПослеПроверкиСертификатов(КонтекстОтправки, СообщениеОбОшибке);
	Если ЗначениеЗаполнено(СообщениеОбОшибке) Тогда
		Возврат;
	КонецЕсли;
	
	ОтправкаРегламентированногоОтчетаВФНСПослеФормированияПакета(КонтекстОтправки, СообщениеОбОшибке);
	Если ЗначениеЗаполнено(СообщениеОбОшибке) Тогда
		Возврат;
	КонецЕсли;

КонецПроцедуры

Процедура ВыгрузитьРегламентированныйОтчетИДоверенностьДляОтправки(КонтекстОтправки, СообщениеОбОшибке)

	// Внимание!
	// ---------
	// В Контейнере есть одноименная процедура, которая используется при отправке отчетов
	// в интерактивном режиме пользователем.
	// При изменениях необходимо синхронно менять обе процедуры.

	
	// Выгружаем отчет.
	ТипОтправляемогоДокумента = КонтекстОтправки.ТипОтправляемогоДокумента;
	РезультатВыгрузки = РегламентированнаяОтчетность.НовыйРезультатВыгрузкиРеглОтчета();

	Если ТипОтправляемогоДокумента = "РегламентированныйОтчет" Тогда
		
		РезультатВыгрузки = ИнтерфейсыВзаимодействияБРО.ВыгрузитьРегламентированныйОтчет(КонтекстОтправки.Документ);
		
	Иначе
		
		// Другие типы отправляемых документов пока не поддерживаются.
		СообщениеОбОшибке = СтроковыеФункцииКлиентСервер.ПодставитьПараметрыВСтроку(
			НСтр("ru = 'Отправка документов типа ""%1"" возможна только в интерактивном режиме'"), ТипОтправляемогоДокумента);
		Возврат;
		
	КонецЕсли;
	
	Если НЕ РезультатВыгрузки.РеглОтчетВыгружен Тогда
		СообщениеОбОшибке = ОписаниеОшибокВыгрузки(РезультатВыгрузки, КонтекстОтправки.Документ);
		Возврат;
	КонецЕсли;
	
	ДвоичныеДанныеФайлаВыгрузки = ПолучитьИзВременногоХранилища(РезультатВыгрузки.АдресФайлаВыгрузки);
	ТекстВыгрузки = ПолучитьСтрокуИзДвоичныхДанных(ДвоичныеДанныеФайлаВыгрузки, РезультатВыгрузки.КодировкаФайлаВыгрузки);
	
	КонтекстОтправки.Вставить("ТекстВыгрузки",    ТекстВыгрузки);
	КонтекстОтправки.Вставить("ИмяФайлаВыгрузки", РезультатВыгрузки.ИмяФайлаВыгрузки);

КонецПроцедуры

Процедура ВыгрузитьРегламентированныйОтчетИДоверенностьДляОтправкиПослеВыгрузкиЗавершение(КонтекстОтправки, СообщениеОбОшибке) Экспорт

	// Внимание!
	// ---------
	// В Контейнере есть одноименная процедура, которая используется при отправке отчетов
	// в интерактивном режиме пользователем.
	// При изменениях необходимо синхронно менять обе процедуры.

	
	// выгружаем доверенность
	Если НЕ ВыгрузитьДоверенностьИНараститьСчетчикФайловПриНеобходимости(КонтекстОтправки) Тогда
		СообщениеОбОшибке = ТекстСообщенийПользователю();
		Если НЕ ЗначениеЗаполнено(СообщениеОбОшибке) Тогда
			СообщениеОбОшибке = СтроковыеФункцииКлиентСервер.ПодставитьПараметрыВСтроку(
				НСтр("ru = 'При выгрузке ""%1"" возникла неизвестная ошибка'"), КонтекстОтправки.Документ);
		КонецЕсли;
		Возврат;
	КонецЕсли;
	
КонецПроцедуры

Процедура ВыполнитьАвтонастройкуУчетнойЗаписиПередОтправкойОбъекта(ОбъектСсылка, СообщениеОбОшибке)

	// Внимание!
	// ---------
	// В Контейнере есть одноименная процедура, которая используется при отправке отчетов
	// в интерактивном режиме пользователем.
	// При изменениях необходимо синхронно менять обе процедуры.


	// Автонастройка не является обязательной для продолжения отправки.
	
	УчетнаяЗапись = УчетнаяЗаписьПоОбъектуОтправки(ОбъектСсылка);
	Если ЗначениеЗаполнено(УчетнаяЗапись) Тогда
		АвтоматическаяНастройкаУчетнойЗаписи(УчетнаяЗапись, СообщениеОбОшибке);
	КонецЕсли;
	
КонецПроцедуры

Процедура ОтправкаРегламентированногоОтчетаВФНСПослеАвтонастройкиУчетнойЗаписи(КонтекстОтправки, СообщениеОбОшибке) Экспорт

	// Внимание!
	// ---------
	// В Контейнере есть одноименная процедура, которая используется при отправке отчетов
	// в интерактивном режиме пользователем.
	// При изменениях необходимо синхронно менять обе процедуры.
	
	СформироватьПакетФНС_ПроверкиСертификатов(КонтекстОтправки, СообщениеОбОшибке);
	Если ЗначениеЗаполнено(СообщениеОбОшибке) Тогда
		Возврат;
	КонецЕсли;
	
КонецПроцедуры

Процедура ОтправкаРегламентированногоОтчетаВФНСПослеПроверкиСертификатов(КонтекстОтправки, СообщениеОбОшибке)

	// Внимание!
	// ---------
	// В Контейнере есть одноименная процедура, которая используется при отправке отчетов
	// в интерактивном режиме пользователем.
	// При изменениях необходимо синхронно менять обе процедуры.
	
	// отражаем изменения в базе
	Если НЕ ОтправкаРегламентированногоОтчетаВФНС_ОпределитьЦиклОбменаИСообщениеВИБ(КонтекстОтправки) Тогда // сервер
		
		СообщениеОбОшибке = ТекстСообщенийПользователю();
		
		Возврат;
		
	КонецЕсли;
	
	// формируем пакет
	СформироватьПакетФНС(КонтекстОтправки.ТранспортноеСообщение, КонтекстОтправки.УникальныйИдентификатор, СообщениеОбОшибке);
	Если ЗначениеЗаполнено(СообщениеОбОшибке) Тогда
		Возврат;
	КонецЕсли;
	
КонецПроцедуры

Процедура ОтправкаРегламентированногоОтчетаВФНСПослеФормированияПакета(КонтекстОтправки, СообщениеОбОшибке)

	// Внимание!
	// ---------
	// В Контейнере есть одноименная процедура, которая используется при отправке отчетов
	// в интерактивном режиме пользователем.
	// При изменениях необходимо синхронно менять обе процедуры.

	// формируем пакет
	Если НЕ ОтправитьТранспортноеСообщениеИЗаписатьСтатусОтправки(КонтекстОтправки) Тогда // сервер
		СообщениеОбОшибке = ТекстСообщенийПользователю();	
		Возврат;
	КонецЕсли;

КонецПроцедуры

#КонецОбласти

#Область ОтправкаРегламентированногоОтчетаВПФР

Процедура ОтправкаРегламентированногоОтчетаВПФР(ПараметрыОтчета, СообщениеОбОшибке)

	// Внимание!
	// ---------
	// В Контейнере есть аналогичная процедура ОтправкаРегламентированногоОтчетаВПФРПослеПодтвержденияПовторнойОтправки,
	// которая используется при отправке отчетов в интерактивном режиме пользователем.
	// При изменениях необходимо синхронно менять обе процедуры.

	Док = ПараметрыОтчета.СсылкаНаОтчет;
	
	// инициализируем контекст отправки
	КонтекстОтправки = Новый Структура;
	
	Если НЕ ОтправкаРегламентированногоОтчетаВПФР_ПередВыгрузкойФайла(КонтекстОтправки, Док, Новый УникальныйИдентификатор, СообщениеОбОшибке) Тогда

		Если КонтекстОтправки.Свойство("НаправлениеНеПодключено") Тогда
			СообщитьОНеподключенномНаправлении(Док, СообщениеОбОшибке);
		Иначе
			
			Если НЕ ЗначениеЗаполнено(СообщениеОбОшибке) Тогда
				СообщениеОбОшибке = "ОповеститьОНеудачнойОтправке";
			КонецЕсли;

		КонецЕсли;
		
		Возврат;
	КонецЕсли;

	// выгружаем отчет и доверенность, при необходимости
	ВыгрузитьРегламентированныйОтчетПФР(КонтекстОтправки, СообщениеОбОшибке);
	Если ЗначениеЗаполнено(СообщениеОбОшибке) Тогда
		Возврат;
	КонецЕсли;

	Если ПараметрыОтчета.ТолькоПроверкаБезОтправки Тогда
		// Следующие процедуры выполняют либо сохранение в базе данных об отправке либо могут вызывать сервис криптографии,
		// поэтому в режиме "Только проверка без отправки" их уже не выполняем.
		// Считаем, что текущий отчет может быть отправлен.
		Возврат;
	КонецЕсли;

	Если ПараметрыОтчета.ТребуетсяАвтонастройкаУчетнойЗаписи Тогда
		ВыполнитьАвтонастройкуУчетнойЗаписиПередОтправкойОбъекта(Док, СообщениеОбОшибке);
		Если ЗначениеЗаполнено(СообщениеОбОшибке) Тогда
			Возврат;
		КонецЕсли;
	КонецЕсли;

	ОтправкаРегламентированногоОтчетаВПФРПослеАвтонастройкиУчетнойЗаписи(КонтекстОтправки, СообщениеОбОшибке);
	Если ЗначениеЗаполнено(СообщениеОбОшибке) Тогда
		Возврат;
	КонецЕсли;

	ОтправкаРегламентированногоОтчетаВПФРПослеФормированияПакета(КонтекстОтправки, СообщениеОбОшибке);
	Если ЗначениеЗаполнено(СообщениеОбОшибке) Тогда
		Возврат;
	КонецЕсли;

КонецПроцедуры

Процедура ВыгрузитьРегламентированныйОтчетПФР(Контекст, СообщениеОбОшибке)

	// Внимание!
	// ---------
	// В Контейнере есть аналогичная процедура,
	// которая используется при отправке отчетов в интерактивном режиме пользователем.
	// При изменениях необходимо синхронно менять обе процедуры.
	
	Отчет = Контекст.Документ;
	ИмяМетаданныхДокумента = Контекст.ТипОтправляемогоДокумента;
	Если ИмяМетаданныхДокумента = "РегламентированныйОтчет" Тогда
		
		// Отправка в ПФР отчетов вида "РегламентированныйОтчет" без взаимодействия с клиентским контекстом в настоящее время не поддерживается.
		СообщениеОбОшибке = "Отправка в ПФР объектов вида РегламентированныйОтчет доступна только в интерактивном режиме работы.";
		Возврат;
	
	ИначеЕсли ИмяМетаданныхДокумента = "ЭлектронныеПредставленияРегламентированныхОтчетов" Тогда
		
		Результат = ВыгрузитьРегламентированныйОтчетПФРИзВнешнихЭлектронныхПредставлений(Контекст);
		ВсегоВАрхиве = 0;
		Для Каждого Запись Из Результат Цикл
			Если Запись.ВАрхиве Тогда
				ВсегоВАрхиве = ВсегоВАрхиве + 1;
			КонецЕсли;
		КонецЦикла;
		
		Если ВсегоВАрхиве > 0 Тогда //Данные одного или более файлов отсутствуют
			
			СообщениеОбОшибке = "Файл отчета был ранее перемещен в архив. Выгрузка содержимого невозможна.";
			Возврат;
			
		КонецЕсли;
	
		Контекст.Вставить("РезультатВыгрузки", Результат);
		
	Иначе
		
		Результат = Новый Массив;
		
		СвойстваФайлаВыгрузки = ЭлектронныйДокументооборотСКонтролирующимиОрганамиВызовСервераПереопределяемый.ВыгрузитьДокумент(Отчет, Контекст.УникальныйИдентификатор);
		Если НЕ ЗначениеЗаполнено(СвойстваФайлаВыгрузки) Тогда
			СообщениеОбОшибке = "ОповеститьОНеудачнойОтправке";
			Возврат;
			
		КонецЕсли;
		
		// заполняем результат
		Если ТипЗнч(СвойстваФайлаВыгрузки) = Тип("Структура") Тогда
			ТекстВыгрузки = СвойстваФайлаВыгрузки.АдресФайлаВыгрузки;
			ИмяФайлаВыгрузки = СвойстваФайлаВыгрузки.ИмяФайлаВыгрузки;
			ТипФайлаОтчетности = СвойстваФайлаВыгрузки.ТипФайлаВыгрузки;
			ОписательОтчета = Новый Структура("Документ, Тип, Имя, Адрес", Отчет, ТипПоИдентификаторуТипаФайлаВыгрузки(ТипФайлаОтчетности), СвойстваФайлаВыгрузки.ИмяФайлаВыгрузки, СвойстваФайлаВыгрузки.АдресФайлаВыгрузки);
			Результат.Добавить(ОписательОтчета);
		Иначе
			Для Инд = 0 По СвойстваФайлаВыгрузки.Количество() - 1 Цикл
				Результат.Добавить(Новый Структура("Документ, Тип, Имя, Адрес", СвойстваФайлаВыгрузки[Инд].Ссылка,
																					ТипПоИдентификаторуТипаФайлаВыгрузки(СвойстваФайлаВыгрузки[Инд].ТипФайлаВыгрузки),
																					СвойстваФайлаВыгрузки[Инд].ИмяФайлаВыгрузки,
																					СвойстваФайлаВыгрузки[Инд].АдресФайлаВыгрузки));
			КонецЦикла;
		КонецЕсли;
		
		Контекст.Вставить("РезультатВыгрузки", Результат);

	КонецЕсли;
	
КонецПроцедуры

Процедура ОтправкаРегламентированногоОтчетаВПФРПослеАвтонастройкиУчетнойЗаписи(КонтекстОтправки, СообщениеОбОшибке)

	// Внимание!
	// ---------
	// В Контейнере есть одноименная процедура, которая используется при отправке отчетов
	// в интерактивном режиме пользователем.
	// При изменениях необходимо синхронно менять обе процедуры.
	
	// отражаем изменения в базе
	Если НЕ ОтправкаРегламентированногоОтчетаВПФР_ОпределитьЦиклОбменаИСообщениеВИБ(КонтекстОтправки) Тогда // сервер
		
		СообщениеОбОшибке = ТекстСообщенийПользователю();

		Возврат;
		
	КонецЕсли;
	
	СформироватьПакетПФР(КонтекстОтправки.ТранспортноеСообщение, КонтекстОтправки.УникальныйИдентификатор, СообщениеОбОшибке);
	
КонецПроцедуры

Процедура ОтправкаРегламентированногоОтчетаВПФРПослеФормированияПакета(КонтекстОтправки, СообщениеОбОшибке)

	// Внимание!
	// ---------
	// В Контейнере есть одноименная процедура, которая используется при отправке отчетов
	// в интерактивном режиме пользователем.
	// При изменениях необходимо синхронно менять обе процедуры.

	// формируем пакет
	Если НЕ ОтправитьТранспортноеСообщениеИЗаписатьСтатусОтправки(КонтекстОтправки) Тогда // сервер
		СообщениеОбОшибке = ТекстСообщенийПользователю();	
		Возврат;
	КонецЕсли;
		
КонецПроцедуры

#КонецОбласти

#Область ОтправкаРегламентированногоОтчетаВФСС

Процедура ОтправкаРегламентированногоОтчетаВФСС(ПараметрыОтчета, СообщениеОбОшибке)

	// Внимание!
	// ---------
	// В Контейнере есть аналогичная процедура ПриНажатииНаКнопкуОтправкиВКонтролирующийОрганДляФСС(),
	// которая используется при отправке отчетов в интерактивном режиме пользователем.
	// При изменениях необходимо синхронно менять обе процедуры.

	ПараметрыОтчета.Вставить("ОтчетСсылка",             ПараметрыОтчета.СсылкаНаОтчет); // В процедурах отправки в ФСС имя ключа со ссылкой на отчет отличается.
	ПараметрыОтчета.Вставить("УникальныйИдентификатор", Новый УникальныйИдентификатор); // Используется вместо уникального идентификатора формы при отправке отчетов на сервере.

	ОтправкаРегламентированногоОтчетаВФСС_ПолучениеСвойствРегОтчета(ПараметрыОтчета, СообщениеОбОшибке);
	Если ЗначениеЗаполнено(СообщениеОбОшибке) Тогда
		Возврат;
	КонецЕсли;
	
	ОтправкаРегламентированногоОтчетаВФСС_ПроверкиПодключения(ПараметрыОтчета, СообщениеОбОшибке);
	Если ЗначениеЗаполнено(СообщениеОбОшибке) Тогда
		Возврат;
	КонецЕсли;

	ОтправкаРегламентированногоОтчетаВФСС_ВыгрузитьОтчет(ПараметрыОтчета, СообщениеОбОшибке);
	Если ЗначениеЗаполнено(СообщениеОбОшибке) Тогда
		Возврат;
	КонецЕсли;

	Если ПараметрыОтчета.ТолькоПроверкаБезОтправки Тогда
		// Следующие процедуры выполняют либо сохранение в базе данных об отправке либо могут вызывать сервис криптографии,
		// поэтому в режиме "Только проверка без отправки" их уже не выполняем.
		// Считаем, что текущий отчет может быть отправлен.
		Возврат;
	КонецЕсли;

	ОтправкаРегламентированногоОтчетаВФСС_ПослеИнициализации(ПараметрыОтчета, СообщениеОбОшибке);
	Если ЗначениеЗаполнено(СообщениеОбОшибке) Тогда
		Возврат;
	КонецЕсли;
	
	ОтправкаРегламентированногоОтчетаВФСС_ПослеВыгрузкиПакетаЗавершение(ПараметрыОтчета, СообщениеОбОшибке);
	Если ЗначениеЗаполнено(СообщениеОбОшибке) Тогда
		Возврат;
	КонецЕсли;

КонецПроцедуры

Процедура ОтправкаРегламентированногоОтчетаВФСС_ПолучениеСвойствРегОтчета(ПараметрыОтчета, СообщениеОбОшибке)

	// Внимание!
	// ---------
	// В Контейнере есть аналогичная процедура ПриНажатииНаКнопкуОтправкиВКонтролирующийОрганДляФСС_ПолучениеСвойствРегОтчета(),
	// которая используется при отправке отчетов в интерактивном режиме пользователем.
	// При изменениях необходимо синхронно менять обе процедуры.
	
	ОтчетСсылка = ПараметрыОтчета.ОтчетСсылка;

	ЗначенияРеквизитов = ЗначенияРеквизитовОбъекта(ОтчетСсылка, "ДатаНачала, ДатаОкончания, Вид, ДанныеОтчета");
	
	КодПодчиненности = КодПодчиненности(ЗначенияРеквизитов.ДанныеОтчета);
	
	ПоляДляЗаписиОтправки = Новый Структура("КодОрганаФСС, ДатаНачалаПериода, ДатаОкончанияПериода, Версия",
		Лев(КодПодчиненности, 4),
		НачалоДня(ЗначенияРеквизитов.ДатаНачала),
		КонецДня(ЗначенияРеквизитов.ДатаОкончания),
		ЗначенияРеквизитов.Вид);
	
	ПараметрыОтчета.Вставить("ПоляДляЗаписиОтправки", ПоляДляЗаписиОтправки);
	
КонецПроцедуры

Процедура ОтправкаРегламентированногоОтчетаВФСС_ПроверкиПодключения(ПараметрыОтчета, СообщениеОбОшибке)

	// Внимание!
	// ---------
	// В Контейнере есть аналогичная процедура ПриНажатииНаКнопкуОтправкиВКонтролирующийОрганДляФСС_ПроверкиПодключения(),
	// которая используется при отправке отчетов в интерактивном режиме пользователем.
	// При изменениях необходимо синхронно менять обе процедуры.

	ОтчетСсылка       = ПараметрыОтчета.ОтчетСсылка;
	ОрганизацияОтчета = ПараметрыОтчета.ОрганизацияОтчета;
	
	ОрганизацияИспользуетОбменСФСС                    = Ложь;
	ОрганизацияИспользуетАвтонастройкуПоУчетнойЗаписи = Ложь;
	НастроенОбменВУниверсальномФормате                = Ложь;	
	
	ПолучитьСвойстваОрганизацииДляФСС(
		ОрганизацияОтчета,
		НастроенОбменВУниверсальномФормате,
		ОрганизацияИспользуетОбменСФСС,
		ОрганизацияИспользуетАвтонастройкуПоУчетнойЗаписи);
	
	Если НЕ ОрганизацияИспользуетОбменСФСС Тогда
		Если НЕ НастроенОбменВУниверсальномФормате Тогда
			
			ПроверитьСтатусПодключенияПередОтправкой(ОтчетСсылка, СообщениеОбОшибке);
			
		Иначе
			СообщитьОНеподключенномНаправлении(ОтчетСсылка, СообщениеОбОшибке);
		КонецЕсли;
		
		Если ЗначениеЗаполнено(СообщениеОбОшибке) Тогда
			Возврат;
		КонецЕсли;
	КонецЕсли;
	
	Если НастроенОбменВУниверсальномФормате И ОрганизацияИспользуетАвтонастройкуПоУчетнойЗаписи Тогда

		Если ПараметрыОтчета.ТребуетсяАвтонастройкаУчетнойЗаписи Тогда
			// Выполняем автонастройку учетной записи, если это первый вызов отправки за сеанс.
			ВыполнитьАвтонастройкуУчетнойЗаписиПередОтправкойОбъекта(ОтчетСсылка, СообщениеОбОшибке);
		КонецЕсли;
		
	КонецЕсли;

КонецПроцедуры

Процедура ОтправкаРегламентированногоОтчетаВФСС_ВыгрузитьОтчет(ПараметрыОтчета, СообщениеОбОшибке)

	// Внимание!
	// ---------
	// В Контейнере есть аналогичная процедура ПриНажатииНаКнопкуОтправкиВКонтролирующийОрганДляФССПослеПодтвержденияЗавершение(),
	// которая используется при отправке отчетов в интерактивном режиме пользователем.
	// При изменениях необходимо синхронно менять обе процедуры.

	ОтчетСсылка = ПараметрыОтчета.ОтчетСсылка;
	
	РезультатВыгрузки = ВыгрузитьОтчетДляОтправкиВФСС(ПараметрыОтчета, СообщениеОбОшибке);
	
	Если РезультатВыгрузки = Неопределено Тогда
		
		Если НЕ ЗначениеЗаполнено(СообщениеОбОшибке) Тогда
			СообщениеОбОшибке = ТекстСообщенийПользователю();
		КонецЕсли;
		Возврат;
		
	КонецЕсли;

	ПараметрыОтчета.Вставить("ТекстФайлаВыгрузки", 	РезультатВыгрузки.ТекстФайлаВыгрузки);
	ПараметрыОтчета.Вставить("РасширениеСТочкой", 	РезультатВыгрузки.РасширениеСТочкой);
	ПараметрыОтчета.Вставить("ИмяФайлаВыгрузки", 	РезультатВыгрузки.ИмяФайлаВыгрузки);
	ПараметрыОтчета.Вставить("ТипЗнчСсылкаНаОтчет", ТипЗнч(ОтчетСсылка));
	
КонецПроцедуры

Функция ВыгрузитьОтчетДляОтправкиВФСС(ПараметрыОтчета, СообщениеОбОшибке)

	// Внимание!
	// ---------
	// В Контейнере есть одноименная процедура,
	// которая используется при отправке отчетов в интерактивном режиме пользователем.
	// При изменениях необходимо синхронно менять обе процедуры.
	
	ОтчетСсылка = ПараметрыОтчета.ОтчетСсылка;
	
	ИнформацияОбОтправляемомДокументе = ПолучитьТипВидаОтправляемогоДокументаПоСсылкеНаОбъектОтправкиДляФСС(
		ОтчетСсылка,
		Истина);
	ТипОтправляемогоДокумента = ИнформацияОбОтправляемомДокументе.ТипОтправляемогоДокумента;
	ВидОтправляемогоДокумента = ИнформацияОбОтправляемомДокументе.ВидОтправляемогоДокумента;
	
	ЭтоДанныеЭЛН = (ТипОтправляемогоДокумента = Перечисления.ТипыОтправляемыхДокументов.ДанныеЭЛНФСС);
	ЭтоПодтверждениеВидаДеятельности = ТипОтправляемогоДокумента =
		Перечисления.ТипыОтправляемыхДокументов.ПрочаяОтчетностьФСС
		И ВидОтправляемогоДокумента =
		Справочники.ВидыОтправляемыхДокументов.ПодтверждениеВидаДеятельности;
	ЭтоРеестрСтимулирующихВыплатМедицинскимИСоциальнымРаботникам = ТипОтправляемогоДокумента =
		Перечисления.ТипыОтправляемыхДокументов.ПрочаяОтчетностьФСС
		И ВидОтправляемогоДокумента =
		Справочники.ВидыОтправляемыхДокументов.РеестрСтимулирующихВыплатМедицинскимИСоциальнымРаботникам;
	
	Если ЭтоДанныеЭЛН Тогда
		СообщениеОбОшибке = НСтр("ru = 'Отправка реестра ЭЛН не поддерживается в мобильном клиенте.'");
		Возврат Неопределено;
		
	ИначеЕсли ЭтоПодтверждениеВидаДеятельности Тогда
		СообщениеОбОшибке = НСтр("ru = 'Отправка подтверждения вида деятельности не поддерживается в мобильном клиенте.'");
		Возврат Неопределено;
		
	ИначеЕсли ЭтоРеестрСтимулирующихВыплатМедицинскимИСоциальнымРаботникам Тогда
		СообщениеОбОшибке =
			НСтр("ru = 'Отправка реестра стимулирующих выплат медицинским и социальным работникам не поддерживается в мобильном клиенте.'");
		Возврат Неопределено;
	КонецЕсли;
	
	ТекстФайлаВыгрузки	= "";
	ИмяФайлаВыгрузки	= "";
	
	ТипЗнчСсылкаНаОтчет = ТипЗнч(ОтчетСсылка);
	
	ЭтоРегламентированныйОтчет = (ТипЗнчСсылкаНаОтчет = Тип("ДокументСсылка.РегламентированныйОтчет") ИЛИ ТипЗнчСсылкаНаОтчет = Тип("Неопределено"));
	ЭтоРеестрСведений = ДокументооборотСФССКлиентСервер.ЭтоРеестрСведенийНаВыплатуПособийФСС(ОтчетСсылка);
	ЭтоЭлектронноеПредставление = (ТипЗнчСсылкаНаОтчет = Тип("СправочникСсылка.ЭлектронныеПредставленияРегламентированныхОтчетов"));
	
	Если ЭтоРегламентированныйОтчет  Тогда

		// Выгрузка на сервере в настоящее время поддерживается не для всех регламентированных отчетов.
		// Если отчет ее не поддерживает, то будет возвращено сообщение об ошибке.
		РезультатВыгрузки  = ИнтерфейсыВзаимодействияБРО.ВыгрузитьРегламентированныйОтчет(ОтчетСсылка);
		Если РезультатВыгрузки.РеглОтчетВыгружен Тогда
			ИмяФайлаВыгрузки   = РезультатВыгрузки.ИмяФайлаВыгрузки;
			ТекстФайлаВыгрузки = ТекстФайлаВыгрузки(РезультатВыгрузки);
		Иначе
			СообщениеОбОшибке = ОписаниеОшибокВыгрузки(РезультатВыгрузки, ОтчетСсылка);
			Возврат Неопределено;
		КонецЕсли;
		
	ИначеЕсли ЭтоЭлектронноеПредставление Тогда
		
		ИзвлечьЭлектронноеПредставлениеИзХранилищаИПерекодировать(ОтчетСсылка, ТекстФайлаВыгрузки, ИмяФайлаВыгрузки);
		
	ИначеЕсли ЭтоРеестрСведений Тогда
		
		ПолучитьСвойстваФайлаВыгрузки(ОтчетСсылка, ТекстФайлаВыгрузки, ИмяФайлаВыгрузки);
		
	Иначе
		ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(
			НСтр("ru = 'Не удалось определить тип документа.'"));
		Возврат Неопределено;
		
	КонецЕсли;
	
	РасширениеФайла = ПолучитьРасширениеФайлаФССПоТипуОтправляемогоДокумента(ТипОтправляемогоДокумента); 
	
	РасширениеСТочкой = "";
	Если РасширениеФайла = Неопределено Тогда
		ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(
			НСтр("ru = 'Не удалось определить тип документа.'"));
		Возврат Неопределено;
	Иначе
		РасширениеСТочкой = "." + РасширениеФайла;
	КонецЕсли;
	
	Если НЕ ЗначениеЗаполнено(ТекстФайлаВыгрузки) Тогда
		ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(
			НСтр("ru = 'Не удалось сформировать файл выгрузки.'"));
		Возврат Неопределено;
	КонецЕсли;
	
	Если НЕ ЗначениеЗаполнено(ИмяФайлаВыгрузки) Тогда
		ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(
			НСтр("ru = 'Не определено имя файла выгрузки.'"));
		Возврат Неопределено;
	КонецЕсли;
	
	ДополнительныеПараметры = Новый Структура();
	ДополнительныеПараметры.Вставить("ТекстФайлаВыгрузки", 	ТекстФайлаВыгрузки);
	ДополнительныеПараметры.Вставить("ИмяФайлаВыгрузки", 	ИмяФайлаВыгрузки);
	ДополнительныеПараметры.Вставить("РасширениеСТочкой", 	РасширениеСТочкой);
	
	Возврат ДополнительныеПараметры;
	
КонецФункции

Процедура ОтправкаРегламентированногоОтчетаВФСС_ПослеИнициализации(ПараметрыОтчета, СообщениеОбОшибке)
	
	// Внимание!
	// ---------
	// В Контейнере есть аналогичная процедура ПриНажатииНаКнопкуОтправкиВКонтролирующийОрганДляФССПослеИнициализации(),
	// которая используется при отправке отчетов в интерактивном режиме пользователем.
	// При изменениях необходимо синхронно менять обе процедуры.

	ОрганизацияСсылка = ПараметрыОтчета.ОрганизацияОтчета;
	
	РеквизитыУчетнойЗаписи = ЭлектроннаяПодписьВМоделиСервисаБРОВызовСервера.РеквизитыУчетнойЗаписи(ОрганизацияСсылка);
	
	ЭтоЭлектроннаяПодписьВМоделиСервиса = РеквизитыУчетнойЗаписи.ЭлектроннаяПодписьВМоделиСервиса;
	УчетнаяЗапись 						= РеквизитыУчетнойЗаписи.Ссылка;
	Идентификатор 						= РеквизитыУчетнойЗаписи.ИдентификаторДокументооборота;
	
	Если НЕ ЭтоЭлектроннаяПодписьВМоделиСервиса Тогда
		// Если не используется электронная подпись в модели сервиса, то автоматическая отправка на стороне сервера не поддерживается.
		СообщениеОбОшибке = "ЭтоНеЭлектроннаяПодписьВМоделиСервиса";
		Возврат;
	КонецЕсли;

	// запрашиваем параметры формирования пакета
	ПараметрыОтчета.Вставить("Идентификатор", Идентификатор);
	
	ПолучитьСертификатыИзНастроекОрганизацииДляФСС(ПараметрыОтчета, СообщениеОбОшибке);
	Если ЗначениеЗаполнено(СообщениеОбОшибке) Тогда
		Возврат;
	КонецЕсли;
	
	ВыгрузитьПакетДляОтправкиВФССПослеПолученияСертификатовЗавершение(ПараметрыОтчета, СообщениеОбОшибке);
	Если ЗначениеЗаполнено(СообщениеОбОшибке) Тогда
		Возврат;
	КонецЕсли;
	
	ВыгрузитьПакетДляОтправкиВФССПослеЗаписиФайлаЗавершение(ПараметрыОтчета, СообщениеОбОшибке);
	
КонецПроцедуры

Процедура ПолучитьСертификатыИзНастроекОрганизацииДляФСС(ПараметрыОтчета, СообщениеОбОшибке)

	// Внимание!
	// ---------
	// В Контейнере есть одноименная процедура,
	// которая используется при отправке отчетов в интерактивном режиме пользователем.
	// При изменениях необходимо синхронно менять обе процедуры.

	ОтпечаткиСертификатов = ПолучитьОтпечаткиСертификатовИзНастроекОрганизацииДляФСС(ПараметрыОтчета.ОрганизацияОтчета);
	
	Если ОтпечаткиСертификатов = Неопределено Тогда
		СообщениеОбОшибке = "ОповеститьОНеудачнойОтправке";
		Возврат;
	КонецЕсли;
	
	СертификатСтрахователя = ПолучитьСвойстваСертификатаПоОтпечаткуНаСервере(ОтпечаткиСертификатов.СертификатСтрахователяОтпечаток);
	СертификатФСС = ПолучитьСвойстваСертификатаПоОтпечаткуНаСервере(ОтпечаткиСертификатов.СертификатФССОтпечаток);
	
	ПараметрыОтчета.Вставить("СертификатСтрахователя", СертификатСтрахователя);
	ПараметрыОтчета.Вставить("СертификатФСС",          СертификатФСС);
	
КонецПроцедуры

Функция СертификатыГодятсяДляПодписанияОтчетностиФСС(Сертификаты) Экспорт
	
	ТекДата = ТекущаяДатаСеанса();
	ПризнакОшибки = Ложь;
	
	Для Каждого Сертификат Из Сертификаты Цикл
		
		Серт 	= Сертификат.Значение;
		Предст 	= Сертификат.Представление;
		Если Серт = Неопределено Тогда
			ПризнакОшибки = Истина;
			ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(СтроковыеФункцииКлиентСервер.ПодставитьПараметрыВСтроку(НСтр("ru = '%1: сертификат не выбран в настройках сдачи отчетности в ФСС!'"), Предст));
			Продолжить;
		КонецЕсли;
		
		Если ТекДата < Серт.ДействителенС Тогда
			ПризнакОшибки = Истина;
			ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(СтроковыеФункцииКлиентСервер.ПодставитьПараметрыВСтроку(НСтр("ru = '%1: период действия сертификата еще не наступил (%2)!'"), Предст, Строка(Серт.ДействителенС)));
		КонецЕсли;
		
		Если ТекДата > Серт.ДействителенПо Тогда
			ПризнакОшибки = Истина;
			ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(СтроковыеФункцииКлиентСервер.ПодставитьПараметрыВСтроку(НСтр("ru = '%1: период действия сертификата истек (%2)!'"), Предст, Строка(Серт.ДействителенПо)));
		КонецЕсли;
		
	КонецЦикла;
	
	Возврат (НЕ ПризнакОшибки);
	
КонецФункции

Процедура ВыгрузитьПакетДляОтправкиВФССПослеПолученияСертификатовЗавершение(ПараметрыОтчета, СообщениеОбОшибке)

	// Внимание!
	// ---------
	// В Контейнере есть одноименная процедура,
	// которая используется при отправке отчетов в интерактивном режиме пользователем.
	// При изменениях необходимо синхронно менять обе процедуры.
	
	ЭтоЭлектроннаяПодписьВМоделиСервиса = Истина;
	ТекстФайлаВыгрузки					= ПараметрыОтчета.ТекстФайлаВыгрузки;
	ИмяФайлаВыгрузки					= ПараметрыОтчета.ИмяФайлаВыгрузки;
	РасширениеСТочкой					= ПараметрыОтчета.РасширениеСТочкой;
	
	СертификатСтрахователя = ПараметрыОтчета.СертификатСтрахователя;
	СертификатФСС = ПараметрыОтчета.СертификатФСС;
	
	//проверяем сертификаты
	СертификатыОбмена = Новый СписокЗначений();
	СертификатыОбмена.Добавить(СертификатСтрахователя, "Сертификат страхователя");
	СертификатыОбмена.Добавить(СертификатФСС, "Сертификат ФСС");
	
	Если НЕ СертификатыГодятсяДляПодписанияОтчетностиФСС(СертификатыОбмена) Тогда
		СообщениеОбОшибке = НСтр("ru = 'Сертификаты не подходят для подписания.'");
		Возврат;
	КонецЕсли;
	
	Если СертификатСтрахователя = Неопределено Тогда
		СообщениеОбОшибке = НСтр("ru = 'Не удалось определить сертификат.'");
		Возврат;
	КонецЕсли;
	
	СертификатСтрахователя.Вставить("ЭтоЭлектроннаяПодписьВМоделиСервиса", ЭтоЭлектроннаяПодписьВМоделиСервиса);
	
	Адрес = ЗагрузитьДанныеНаСервер(ТекстФайлаВыгрузки, ПараметрыОтчета.УникальныйИдентификатор,, Истина);
	
	ПараметрыОтчета.Вставить("ВременныйКаталог", "");
	ПараметрыОтчета.Вставить("ПолноеИмяФайлаВыгрузки", ИмяФайлаВыгрузки);
	ПараметрыОтчета.Вставить("АдресДанныхВыгрузки",    Адрес);
	    	    
КонецПроцедуры

Процедура ВыгрузитьПакетДляОтправкиВФССПослеЗаписиФайлаЗавершение(ПараметрыОтчета, СообщениеОбОшибке)

	// Внимание!
	// ---------
	// В Контейнере есть одноименная процедура,
	// которая используется при отправке отчетов в интерактивном режиме пользователем.
	// При изменениях необходимо синхронно менять обе процедуры.
	
	ВременныйКаталог = ПараметрыОтчета.ВременныйКаталог;
	ПолноеИмяФайлаВыгрузки = ПараметрыОтчета.ПолноеИмяФайлаВыгрузки;
	РасширениеСТочкой = ПараметрыОтчета.РасширениеСТочкой;
	СертификатСтрахователя = ПараметрыОтчета.СертификатСтрахователя;
	СертификатФСС = ПараметрыОтчета.СертификатФСС;
	АдресДанныхВыгрузки = ПараметрыОтчета.АдресДанныхВыгрузки;
	
	// подписываем и шифруем
	КороткоеИмяЗашифрованногоПакета = ОбщегоНазначенияКлиентСервер.РазложитьПолноеИмяФайла(ПолноеИмяФайлаВыгрузки).ИмяБезРасширения + РасширениеСТочкой;
	ИмяЗашифрованногоПакета = ВременныйКаталог + КороткоеИмяЗашифрованногоПакета;
	
	СертификатыЗашифрования = Новый Массив;
	СертификатыЗашифрования.Добавить(СертификатФСС);
	
	ЗашифрованныеДанные = ПодписатьИЗашифроватьPKCS7(СертификатСтрахователя, АдресДанныхВыгрузки, СертификатыЗашифрования, СообщениеОбОшибке);

	ЗашифрованныйПакет = Новый Структура;
	ЗашифрованныйПакет.Вставить("ИмяФайлаЗашифрованногоПакета", ЗашифрованныеДанные);
	ЗашифрованныйПакет.Вставить("ИмяФайлаВыгрузки", ИмяЗашифрованногоПакета);
							
	ПараметрыОтчета.Вставить("ЗашифрованныйПакет", ЗашифрованныйПакет);
	
	// Для совместимости с клиентским кодом сохраняем зашифрованные данные в нескольких ключах.
	ПараметрыОтчета.Вставить("АдресПодписанныхИЗашифрованныхДанных", ЗашифрованныеДанные);
	ПараметрыОтчета.Вставить("ИмяЗашифрованногоФайла",               ЗашифрованныеДанные);		
	
КонецПроцедуры

Процедура ОтправкаРегламентированногоОтчетаВФСС_ПослеВыгрузкиПакетаЗавершение(ПараметрыОтчета, СообщениеОбОшибке)

	// Внимание!
	// ---------
	// В Контейнере есть аналогичная процедура ПриНажатииНаКнопкуОтправкиВКонтролирующийОрганДляФССПослеВыгрузкиПакетаЗавершение(),
	// которая используется при отправке отчетов в интерактивном режиме пользователем.
	// При изменениях необходимо синхронно менять обе процедуры.

	ЭтоЭлектроннаяПодписьВМоделиСервиса = Истина;
	ОтчетСсылка                         = ПараметрыОтчета.ОтчетСсылка;
	ПоляДляЗаписиОтправки               = ПараметрыОтчета.ПоляДляЗаписиОтправки;
	ЗашифрованныйПакет                  = ПараметрыОтчета.ЗашифрованныйПакет;
	
	СтрокаBase64ФайлаПакета = ЗашифрованныйПакет.ИмяФайлаЗашифрованногоПакета;
	КороткоеИмяФайлаПакета = ОбщегоНазначенияКлиентСервер.РазложитьПолноеИмяФайла(ЗашифрованныйПакет.ИмяФайлаВыгрузки).Имя;
	
	// Далее аналогично методу ПриНажатииНаКнопкуОтправкиВКонтролирующийОрганДляФССПослеЧтенияФайлаВыгрузкиПакетаЗавершение() из Контейнера.
	
	ИнформацияОбОтправляемомДокументе = ПолучитьТипВидаОтправляемогоДокументаПоСсылкеНаОбъектОтправкиДляФСС(
		ОтчетСсылка,
		Истина);
	ТипОтправляемогоДокумента = ИнформацияОбОтправляемомДокументе.ТипОтправляемогоДокумента;
	ВидОтправляемогоДокумента = ИнформацияОбОтправляемомДокументе.ВидОтправляемогоДокумента;
	
	ИнформацияДляОтправкиВФСС = СформироватьФайлОтправкиВФССССервера(
		КороткоеИмяФайлаПакета,
		СтрокаBase64ФайлаПакета,
		ЭтоЭлектроннаяПодписьВМоделиСервиса,
		ПараметрыОтчета.УникальныйИдентификатор,
		ТипОтправляемогоДокумента);
	СтрокаBase64ФайлаПакета 	= ИнформацияДляОтправкиВФСС.СтрокаBase64ФайлаПакета;
	СтрокаBase64ФайлаОтправки 	= ИнформацияДляОтправкиВФСС.СтрокаBase64ФайлаОтправки;
	
	Если СтрокаBase64ФайлаОтправки = Неопределено Тогда
		
		СообщениеОбОшибке = НСтр("ru = 'Не удалось сформировать файл отправки.'");
		Возврат;
		
	КонецЕсли;
	
	// определим адрес сервера ФСС
	
	АдресСервера = СерверПриемаОтчетностиФСС(ТипОтправляемогоДокумента);
	
	Если АдресСервера = Неопределено Тогда
		
		СообщениеОбОшибке =	НСтр("ru = 'Не удалось определить тип документа.'");
		Возврат;
		
	КонецЕсли;
	
	// отправляем 

	// Далее аналогично ПриНажатииНаКнопкуОтправкиВКонтролирующийОрганДляФССОтправкаВЦикле() из Контейнера.
	ПовторятьСоединение = Ложь; // На стороне сервера выполняем только одну попытку отправку.
	ПараметрыОтправкиНаСервере = Новый Структура;
	ПараметрыОтправкиНаСервере.Вставить("АдресСервера", АдресСервера);
	ПараметрыОтправкиНаСервере.Вставить("РазделениеВключено", Неопределено);
	ПараметрыОтправкиНаСервере.Вставить("ПоляДляЗаписиОтправки", ПоляДляЗаписиОтправки);
	ПараметрыОтправкиНаСервере.Вставить("ТипОтправляемогоДокумента", ТипОтправляемогоДокумента);
	ПараметрыОтправкиНаСервере.Вставить("ВидОтправляемогоДокумента", ВидОтправляемогоДокумента);
	НоваяОтправкаСсылка = ОтправитьВФССССервера(
		ЭтоЭлектроннаяПодписьВМоделиСервиса,
		КороткоеИмяФайлаПакета,
		СтрокаBase64ФайлаОтправки,
		СтрокаBase64ФайлаПакета,
		ОтчетСсылка,
		ПовторятьСоединение,
		ПараметрыОтправкиНаСервере);
	
	Если НЕ ЗначениеЗаполнено(НоваяОтправкаСсылка) Тогда
		
		СообщениеОбОшибке = "ОповеститьОНеудачнойОтправке";
		
	КонецЕсли;
	
КонецПроцедуры

#КонецОбласти

#КонецОбласти

#Область ПакетыТранспортныхСообщений

#Область ОбщиеПроцедурыИФункцииПакетовСообщений

Процедура СформироватьПакет(Сообщение, СообщениеОбОшибке)

	// Внимание!
	// ---------
	// В Контейнере есть одноименная процедура, которая используется при отправке отчетов
	// в интерактивном режиме пользователем.
	// При изменениях необходимо синхронно менять обе процедуры.

	РеквизитыСообщения = Новый Структура();
	РеквизитыСообщения.Вставить("ВидЦиклаОбмена", "ЦиклОбмена.Вид");

	РеквизитыСообщения = ОбщегоНазначения.ЗначенияРеквизитовОбъекта(Сообщение, РеквизитыСообщения, Истина);
	ВидЦиклаОбмена = РеквизитыСообщения.ВидЦиклаОбмена;
	
	// В настоящий момент отправка рег.отчетов с сервера без взаимодействия с клиентским контекстом
	// поддерживается только для ФНС и ПФР.

	Если ВидЦиклаОбмена = Перечисления.ВидыЦикловОбмена.ЦиклОбменаСПФР Тогда
		СформироватьПакетПФР(Сообщение, Новый УникальныйИдентификатор, СообщениеОбОшибке);
	ИначеЕсли ВидЦиклаОбмена = Перечисления.ВидыЦикловОбмена.ЦиклОбменаСФНС Тогда
		СформироватьПакетФНС(Сообщение, Новый УникальныйИдентификатор, СообщениеОбОшибке);
	Иначе
		СообщениеОбОшибке = СтрШаблон(НСтр("ru = 'СформироватьПакет(): Неподдерживаемый без взаимодействия с клиентским контекстом вид цикла обмена %1'"), ВидЦиклаОбмена);
		ВызватьИсключение СообщениеОбОшибке;
	КонецЕсли;
	
КонецПроцедуры

Функция НайтиСертификаты(Сертификаты, ВыполнятьПроверку)

	// Внимание!
	// ---------
	// В общем модуле КриптографияЭДКОСлужебныйКлиент есть одноименная процедура,
	// которая используется при отправке отчетов в интерактивном режиме пользователем.
	// При изменениях необходимо синхронно менять обе процедуры.
	
	СертификатыВЗащищенномХранилищеНаСервере = Новый Массив;
	
	Для Каждого Сертификат Из Сертификаты Цикл
		Если КриптографияЭДКОКлиентСервер.СертификатВЗащищенномХранилищеНаСервере(Сертификат) Тогда
			СертификатыВЗащищенномХранилищеНаСервере.Добавить(Сертификат);
		КонецЕсли;
	КонецЦикла;
	
	РезультатПоиска = КриптографияЭДКОСлужебныйВызовСервера.НайтиСертификатыВЗащищенномХранилищеНаСервере(
		СертификатыВЗащищенномХранилищеНаСервере, ВыполнятьПроверку);

	Результат = КриптографияЭДКОКлиентСервер.ПодготовитьРезультат(Истина, "Сертификаты", РезультатПоиска.Сертификаты);
	Результат.Вставить("НенайденныеСертификаты", РезультатПоиска.НенайденныеСертификаты);
	
	Возврат Результат;
	
КонецФункции

Процедура СформироватьПакет_ОтработатьОтложенныеКоманды(КонтекстОтправки, СообщениеОбОшибке)
	
	// Внимание!
	// ---------
	// В Контейнере есть аналогичная процедура СформироватьПакет_ОтработатьОтложенныеКомандыНаКлиенте(),
	// которая используется при отправке отчетов в интерактивном режиме пользователем.
	// При изменениях необходимо синхронно менять обе процедуры.

	Перем ЭтоЭлектроннаяПодписьВМоделиСервиса;
	
	КонтекстОтправки.Свойство("ЭПВМоделиСервиса", ЭтоЭлектроннаяПодписьВМоделиСервиса);
	
	Если ЭтоЭлектроннаяПодписьВМоделиСервиса = Неопределено Тогда
	РеквизитыУчетнойЗаписи = ЭлектроннаяПодписьВМоделиСервисаБРОВызовСервера.РеквизитыУчетнойЗаписи(КонтекстОтправки.УчетнаяЗапись);
	ЭтоЭлектроннаяПодписьВМоделиСервиса = РеквизитыУчетнойЗаписи.ЭлектроннаяПодписьВМоделиСервиса;
		КонтекстОтправки.Вставить("ЭПВМоделиСервиса", ЭтоЭлектроннаяПодписьВМоделиСервиса);
	КонецЕсли;
	
	КонтекстОтправки.Вставить("ЭтоЭлектроннаяПодписьВМоделиСервиса", ЭтоЭлектроннаяПодписьВМоделиСервиса);
	
	СформироватьПакет_ОтработатьОтложенныеКомандыПоследовательно(КонтекстОтправки, СообщениеОбОшибке);
	
КонецПроцедуры

Процедура СформироватьПакет_ОтработатьОтложенныеКомандыПоследовательно(КонтекстОтправки, СообщениеОбОшибке)

	// Внимание!
	// ---------
	// В Контейнере есть аналогичная процедура СформироватьПакет_ОтработатьОтложенныеКомандыНаКлиентеПоследовательно(),
	// которая используется при отправке отчетов в интерактивном режиме пользователем.
	// При изменениях необходимо синхронно менять обе процедуры.
	
	ОтложенныеКоманды = КонтекстОтправки.ОтложенныеКоманды;

	Для ТекущаяКоманда = 0 По ОтложенныеКоманды.Количество() - 1 Цикл

		Данные = ИзвлечьДвоичныеДанныеПриНеобходимости(ОтложенныеКоманды[ТекущаяКоманда].Значение);
		
		Если ОтложенныеКоманды[ТекущаяКоманда].Представление = "Зашифровать" Тогда
			
			Если КонтекстОтправки.Свойство("ОбъектСертификатСервераДокументооборота")
				И ЗначениеЗаполнено(КонтекстОтправки.ОбъектСертификатСервераДокументооборота)
				И КонтекстОтправки.Свойство("СвойстваОтложенныхКоманд") Тогда
				
				ИдентификаторОтложеннойКоманды = ОтложенныеКоманды[ТекущаяКоманда].ПолучитьИдентификатор();
				СвойстваОтложеннойКоманды = КонтекстОтправки.СвойстваОтложенныхКоманд[ИдентификаторОтложеннойКоманды];
				Если СвойстваОтложеннойКоманды <> Неопределено И СвойстваОтложеннойКоманды.ДляСервераДокументооборота Тогда
					Попытка
						ЗашифрованныеДанные = СервисКриптографии.Зашифровать(
							Данные,
							КонтекстОтправки.ОбъектСертификатПолучателя);
						
						ЗашифрованныеДобавляемыеДанные = СервисКриптографии.Зашифровать(
							Данные,
							КонтекстОтправки.ОбъектСертификатСервераДокументооборота);
						
						МассивИменФайлов = Новый Массив;
						
						ФайлДанных = ПолучитьИмяВременногоФайла();
						ЗашифрованныеДанные.Записать(ФайлДанных);
						МассивИменФайлов.Добавить(ФайлДанных);
						
						ФайлРазделителя = ПолучитьИмяВременногоФайла();
						ДанныеРазделителя = Base64Значение("FD2WshXWT3yZW18vlRZXnA==");
						ДанныеРазделителя.Записать(ФайлРазделителя);
						МассивИменФайлов.Добавить(ФайлРазделителя);
						
						ФайлДобавляемыхДанных = ПолучитьИмяВременногоФайла();
						ЗашифрованныеДобавляемыеДанные.Записать(ФайлДобавляемыхДанных);
						МассивИменФайлов.Добавить(ФайлДобавляемыхДанных);
						
						ФайлРезультата = ПолучитьИмяВременногоФайла();
						ОбъединитьФайлы(МассивИменФайлов, ФайлРезультата);
						
						ОперацииСФайламиЭДКО.УдалитьВременныйФайл(ФайлДанных);
						ОперацииСФайламиЭДКО.УдалитьВременныйФайл(ФайлРазделителя);
						ОперацииСФайламиЭДКО.УдалитьВременныйФайл(ФайлДобавляемыхДанных);
						
						ДанныеРезультата = Новый ДвоичныеДанные(ФайлРезультата);
						ПоместитьВоВременноеХранилище(ДанныеРезультата, ОтложенныеКоманды[ТекущаяКоманда].Значение);
						
						ОперацииСФайламиЭДКО.УдалитьВременныйФайл(ФайлРезультата);
					Исключение
						// Запись в журнал регистрации зарегистрирована исходным методом, повторно не регистрируем.
						СообщениеОбОшибке = ПодробноеПредставлениеОшибки(ИнформацияОбОшибке());
						Возврат;
					КонецПопытки;
					
					Возврат;
				КонецЕсли;
			КонецЕсли;
			
			Попытка
				ЗашифрованныеДанные = СервисКриптографии.Зашифровать(
					Данные,
					КонтекстОтправки.ОбъектСертификатПолучателя);
				
				// Заменяем сами данные на их зашифрованное значение.
				ПоместитьВоВременноеХранилище(ЗашифрованныеДанные, ОтложенныеКоманды[ТекущаяКоманда].Значение);
			Исключение
				// Запись в журнал регистрации зарегистрирована исходным методом, повторно не регистрируем.
				СообщениеОбОшибке = ПодробноеПредставлениеОшибки(ИнформацияОбОшибке());
				Возврат;
			КонецПопытки;

		ИначеЕсли ОтложенныеКоманды[ТекущаяКоманда].Представление = "ПодписатьФайлОписанияПакета" Тогда
			
			// Команда "ПодписатьФайлОписанияПакета" используется при подготовке отчетности в ПФР.
			Попытка

				ПараметрыПодписания = Новый Структура;
				ПараметрыПодписания.Вставить("ОтсоединеннаяПодпись", Истина);
				ПараметрыПодписания.Вставить("ИспользоватьДлительныйМаркерБезопасности", 
					МожноИспользоватьДлительныйМаркерБезопасностиДляСообщения(КонтекстОтправки.ТипСообщения));

				Подпись = СервисКриптографии.Подписать(
					Данные,
					КонтекстОтправки.ОбъектСертификатРуководителя,
					"CMS",
					ПараметрыПодписания);

				Если ТипЗнч(Подпись) = Тип("Структура") Тогда
					// Это структурированная ошибка при работе сервиса криптографии, передаем ее вызывающей процедуре.
					СообщениеОбОшибке = Подпись.КодВозврата;
					Возврат;
				КонецЕсли;

				// Запоминаем результат подписания.
				КонтекстОтправки.Вставить("ФайлПодписиПодФайломОписанияПакета", ПоместитьВоВременноеХранилище(Подпись, Новый УникальныйИдентификатор()));
			Исключение
				// Запись в журнал регистрации зарегистрирована исходным методом, повторно не регистрируем.
				СообщениеОбОшибке = ПодробноеПредставлениеОшибки(ИнформацияОбОшибке());
				Возврат;
			КонецПопытки;
					
		КонецЕсли;
	КонецЦикла;
	
КонецПроцедуры

Функция КодПоТипуДокументаПакета(ТипДокумента, КодТипаДокументоооборота) Экспорт
	
	// отчетность ФНС
	Если ТипДокумента = Перечисления.ТипыСодержимогоТранспортногоКонтейнера.ФайлОтчетности Тогда
		Возврат "01";
	ИначеЕсли ТипДокумента = Перечисления.ТипыСодержимогоТранспортногоКонтейнера.Документ Тогда
		Возврат "01";
	ИначеЕсли ТипДокумента = Перечисления.ТипыСодержимогоТранспортногоКонтейнера.ОписаниеСведений Тогда
		Возврат "02";
	ИначеЕсли ТипДокумента = Перечисления.ТипыСодержимогоТранспортногоКонтейнера.ОписаниеДокумент Тогда
		Возврат "02";
	ИначеЕсли ТипДокумента = Перечисления.ТипыСодержимогоТранспортногоКонтейнера.Доверенность Тогда
		Если КодТипаДокументоооборота = "02" Тогда
			Возврат "04";
		Иначе
			Возврат "03";
		КонецЕсли;
	ИначеЕсли ТипДокумента = Перечисления.ТипыСодержимогоТранспортногоКонтейнера.ПодтверждениеДатыОтправки Тогда
		Если КодТипаДокументоооборота = "02" Тогда
			Возврат "05";
		Иначе
			Возврат "04";
		КонецЕсли;
	ИначеЕсли ТипДокумента = Перечисления.ТипыСодержимогоТранспортногоКонтейнера.УведомлениеОбОтказе Тогда
		Если КодТипаДокументоооборота = "02" Тогда
			Возврат "07";
		ИначеЕсли КодТипаДокументоооборота = "06" Тогда // запрос
			Возврат "06";
		Иначе
			Возврат "05";
		КонецЕсли;
	ИначеЕсли ТипДокумента = Перечисления.ТипыСодержимогоТранспортногоКонтейнера.КвитанцияОПриеме Тогда
		Если КодТипаДокументоооборота = "06" Тогда // запрос
			Возврат "05";
		Иначе
			Возврат "06";
		КонецЕсли;
	ИначеЕсли ТипДокумента = Перечисления.ТипыСодержимогоТранспортногоКонтейнера.КвитанцияОПриемеДокумент Тогда
		Возврат "06";
    ИначеЕсли ТипДокумента = Перечисления.ТипыСодержимогоТранспортногоКонтейнера.УведомлениеОбОтказеДокумент Тогда
		Возврат "05";
	ИначеЕсли ТипДокумента = Перечисления.ТипыСодержимогоТранспортногоКонтейнера.УведомлениеОбУточнении Тогда
		Возврат "07";
	ИначеЕсли ТипДокумента = Перечисления.ТипыСодержимогоТранспортногоКонтейнера.ИзвещениеОВводе Тогда
		Возврат "08";
	ИначеЕсли ТипДокумента = Перечисления.ТипыСодержимогоТранспортногоКонтейнера.ИзвещениеОПолучении Тогда
		Если КодТипаДокументоооборота = "08" Тогда // документооборот по 2-НДФЛ
			Возврат "07";
		ИначеЕсли КодТипаДокументоооборота = "02" Тогда
			Возврат "06";
		ИначеЕсли КодТипаДокументоооборота = "03" Тогда // письмоНО
			Возврат "05";
		ИначеЕсли КодТипаДокументоооборота = "05" Тогда // рассылка групповая
			Возврат "05";
		ИначеЕсли КодТипаДокументоооборота = "10" Тогда // документ
			Возврат "07";
		ИначеЕсли КодТипаДокументоооборота = "06" Тогда // запрос
			Возврат "08";
		ИначеЕсли КодТипаДокументоооборота = "09" Тогда // заявление
			Возврат "07";
		ИначеЕсли КодТипаДокументоооборота = "12" Тогда // представление
			Возврат "08";
		Иначе
			Возврат "09";
		КонецЕсли;
		
	//заявление
	ИначеЕсли ТипДокумента = Перечисления.ТипыСодержимогоТранспортногоКонтейнера.Заявление Тогда
		Возврат "01";
	ИначеЕсли ТипДокумента = Перечисления.ТипыСодержимогоТранспортногоКонтейнера.ОписаниеСведенийЗаявление Тогда
		Возврат "02";
	ИначеЕсли ТипДокумента = Перечисления.ТипыСодержимогоТранспортногоКонтейнера.КвитанцияОПриемеЗаявления Тогда
		Возврат "06";
	ИначеЕсли ТипДокумента = Перечисления.ТипыСодержимогоТранспортногоКонтейнера.СообщениеОПроверке Тогда
		Возврат "07";
	ИначеЕсли ТипДокумента = Перечисления.ТипыСодержимогоТранспортногоКонтейнера.СообщениеОбОтзывеЗаявления Тогда
		Возврат "09";
	ИначеЕсли ТипДокумента = Перечисления.ТипыСодержимогоТранспортногоКонтейнера.СообщениеОНесоответствиях Тогда
		Возврат "10";
		
	ИначеЕсли ТипДокумента = Перечисления.ТипыСодержимогоТранспортногоКонтейнера.СообщениеОПростОтметки Тогда
		Возврат "08";
	ИначеЕсли ТипДокумента = Перечисления.ТипыСодержимогоТранспортногоКонтейнера.УведомлениеОбОтказеОтметки Тогда
		Возврат "09";	
	
	// 2-НДФЛ
	ИначеЕсли ТипДокумента = Перечисления.ТипыСодержимогоТранспортногоКонтейнера.Форма2НДФЛ Тогда
		Возврат "01";
	ИначеЕсли ТипДокумента = Перечисления.ТипыСодержимогоТранспортногоКонтейнера.ОписаниеСведений2НДФЛ Тогда
		Возврат "02";
	ИначеЕсли ТипДокумента = Перечисления.ТипыСодержимогоТранспортногоКонтейнера.Протокол2НДФЛ Тогда
		Возврат "05";
	ИначеЕсли ТипДокумента = Перечисления.ТипыСодержимогоТранспортногоКонтейнера.Реестр2НДФЛ Тогда
		Возврат "06";
	
	// обращение
	ИначеЕсли ТипДокумента = Перечисления.ТипыСодержимогоТранспортногоКонтейнера.Обращение Тогда
		Возврат "01";
	ИначеЕсли ТипДокумента = Перечисления.ТипыСодержимогоТранспортногоКонтейнера.ОписаниеСведенийОбращениеПисьмоРассылка Тогда
		Возврат "02";
	ИначеЕсли ТипДокумента = Перечисления.ТипыСодержимогоТранспортногоКонтейнера.Приложение Тогда
		Если КодТипаДокументоооборота = "01" Тогда // ФНС
			Возврат "10";
		Иначе
			Возврат "03";
		КонецЕсли;
	ИначеЕсли ТипДокумента = Перечисления.ТипыСодержимогоТранспортногоКонтейнера.ПодтверждениеДатыПолучения Тогда
		Если КодТипаДокументоооборота = "06" Тогда // запрос
			Возврат "09";
		Иначе
			Возврат "08";
		КонецЕсли;
		
	// представление
	ИначеЕсли ТипДокумента = Перечисления.ТипыСодержимогоТранспортногоКонтейнера.Представление Тогда
		Возврат "01";
	ИначеЕсли ТипДокумента = Перечисления.ТипыСодержимогоТранспортногоКонтейнера.ДоверенностьПредставление Тогда
		Если КодТипаДокументоооборота = "01" ИЛИ КодТипаДокументоооборота = "06" ИЛИ КодТипаДокументоооборота = "08" ИЛИ КодТипаДокументоооборота = "09" Тогда
			Возврат "03";
		Иначе
			Возврат "04";
		КонецЕсли;
	ИначеЕсли ТипДокумента = Перечисления.ТипыСодержимогоТранспортногоКонтейнера.ОписаниеПредставление Тогда
		Возврат "02";
	ИначеЕсли ТипДокумента = Перечисления.ТипыСодержимогоТранспортногоКонтейнера.ПодтверждениеДатыОтправкиПредставление Тогда
		Если КодТипаДокументоооборота = "01" ИЛИ КодТипаДокументоооборота = "03" ИЛИ КодТипаДокументоооборота = "04" ИЛИ КодТипаДокументоооборота = "05"
			ИЛИ КодТипаДокументоооборота = "06" ИЛИ КодТипаДокументоооборота = "08" ИЛИ КодТипаДокументоооборота = "09" Тогда
			Возврат "04";
		Иначе
			Возврат "05";
		КонецЕсли;
		
	// запрос
	ИначеЕсли ТипДокумента = Перечисления.ТипыСодержимогоТранспортногоКонтейнера.Запрос Тогда
		Возврат "01";
	ИначеЕсли ТипДокумента = Перечисления.ТипыСодержимогоТранспортногоКонтейнера.Ответ Тогда
		Возврат "07";
	ИначеЕсли ТипДокумента = Перечисления.ТипыСодержимогоТранспортногоКонтейнера.ОписаниеСведенийЗапрос Тогда
		Возврат "02";
		
	// запрос на выписку ЕГРЮЛ/ЕГРИП
	ИначеЕсли ТипДокумента = Перечисления.ТипыСодержимогоТранспортногоКонтейнера.ЗапросНаВыпискуИзЕГРЮЛ_ЕГРИП Тогда
		Возврат "01";
	КонецЕсли;
	
КонецФункции

Функция ИдентификаторПоТипуДокументаПакета(ТипДокумента) Экспорт
	
	Если ТипЗнч(ТипДокумента) = Тип("Строка") Тогда
		Возврат СокрЛП(ТипДокумента);
		
	ИначеЕсли ТипДокумента = Перечисления.ТипыФайловОтчетностиПФР.ПачкаАДВ Тогда
		Возврат "пачкаАДВ";
	ИначеЕсли ТипДокумента = Перечисления.ТипыФайловОтчетностиПФР.ПачкаИС Тогда
		Возврат "пачкаИС";
	ИначеЕсли ТипДокумента = Перечисления.ТипыФайловОтчетностиПФР.ПачкаДСВ Тогда
		Возврат "пачкаДСВ";
	ИначеЕсли ТипДокумента = Перечисления.ТипыФайловОтчетностиПФР.РеестрДСВ Тогда
		Возврат "реестрДСВ";
	ИначеЕсли ТипДокумента = Перечисления.ТипыФайловОтчетностиПФР.ПачкаРСВ Тогда
		Возврат "пачкаРСВ";
	ИначеЕсли ТипДокумента = Перечисления.ТипыФайловОтчетностиПФР.ПачкаСВВ Тогда
		Возврат "пачкаСВВ";
	ИначеЕсли ТипДокумента = Перечисления.ТипыСодержимогоТранспортногоКонтейнера.СведенияПриложениеПФР Тогда
		Возврат "сведенияПриложение";
	ИначеЕсли ТипДокумента = Перечисления.ТипыСодержимогоТранспортногоКонтейнера.ФайлОписанияСведенийПФР Тогда
		Возврат "описаниеСведений";
	ИначеЕсли ТипДокумента = Перечисления.ТипыСодержимогоТранспортногоКонтейнера.ПротоколПФР Тогда
		Возврат "протокол";
	ИначеЕсли ТипДокумента = Перечисления.ТипыСодержимогоТранспортногоКонтейнера.ПротоколПриложениеПФР Тогда
		Возврат "протоколПриложение";
	ИначеЕсли ТипДокумента = Перечисления.ТипыСодержимогоТранспортногоКонтейнера.Письмо Тогда
		Возврат "письмо";
	ИначеЕсли ТипДокумента = Перечисления.ТипыСодержимогоТранспортногоКонтейнера.ПисьмоПриложение Тогда
		Возврат "письмоПриложение";
	ИначеЕсли ТипДокумента = Перечисления.ТипыСодержимогоТранспортногоКонтейнера.ОписаниеПисьма Тогда
		Возврат "описаниеПисьма";
	ИначеЕсли ТипДокумента = Перечисления.ТипыСодержимогоТранспортногоКонтейнера.ТранспортнаяИнформация Тогда
		Возврат "транспортнаяИнформация";
	ИначеЕсли ТипДокумента = Перечисления.ТипыСодержимогоТранспортногоКонтейнера.ПодтверждениеПолученияПисьма Тогда
		Возврат "подтверждениеПолучения";
	ИначеЕсли ТипДокумента = Перечисления.ТипыСодержимогоТранспортногоКонтейнера.Сертификат Тогда
		Возврат "сертификат";
		
	ИначеЕсли ТипДокумента = Перечисления.ТипыСодержимогоТранспортногоКонтейнера.ФайлОтчетности Тогда
		Возврат "декларация";
	ИначеЕсли ТипДокумента = Перечисления.ТипыСодержимогоТранспортногоКонтейнера.ОписаниеСведений Тогда
		Возврат "описание";
	ИначеЕсли ТипДокумента = Перечисления.ТипыСодержимогоТранспортногоКонтейнера.Доверенность Тогда
		Возврат "доверенность";
	ИначеЕсли ТипДокумента = Перечисления.ТипыСодержимогоТранспортногоКонтейнера.ПодтверждениеДатыОтправки Тогда
		Возврат "подтверждениеДатыОтправки";
	ИначеЕсли ТипДокумента = Перечисления.ТипыСодержимогоТранспортногоКонтейнера.УведомлениеОбОтказе Тогда
		Возврат "уведомлениеОбОтказе";
	ИначеЕсли ТипДокумента = Перечисления.ТипыСодержимогоТранспортногоКонтейнера.КвитанцияОПриеме Тогда
		Возврат "квитанцияОПриеме";
	ИначеЕсли ТипДокумента = Перечисления.ТипыСодержимогоТранспортногоКонтейнера.УведомлениеОбУточнении Тогда
		Возврат "уведомлениеОбУточнении";
	ИначеЕсли ТипДокумента = Перечисления.ТипыСодержимогоТранспортногоКонтейнера.ИзвещениеОВводе Тогда
		Возврат "извещениеОВводе";
	ИначеЕсли ТипДокумента = Перечисления.ТипыСодержимогоТранспортногоКонтейнера.ИзвещениеОПолучении Тогда
		Возврат "извещениеОПолучении";
		
	//документ	
	ИначеЕсли ТипДокумента = Перечисления.ТипыСодержимогоТранспортногоКонтейнера.Документ Тогда
		Возврат "документ";
	ИначеЕсли ТипДокумента = Перечисления.ТипыСодержимогоТранспортногоКонтейнера.ОписаниеДокумент Тогда
		Возврат "описание";
	ИначеЕсли ТипДокумента = Перечисления.ТипыСодержимогоТранспортногоКонтейнера.УведомлениеОбОтказеДокумент Тогда
		Возврат "уведомлениеОбОтказе";
	ИначеЕсли ТипДокумента = Перечисления.ТипыСодержимогоТранспортногоКонтейнера.КвитанцияОПриемеДокумент Тогда
		Возврат "квитанцияОПриеме";
		
	//заявление	
	ИначеЕсли ТипДокумента = Перечисления.ТипыСодержимогоТранспортногоКонтейнера.Заявление Тогда
		Возврат "заявление";
	ИначеЕсли ТипДокумента = Перечисления.ТипыСодержимогоТранспортногоКонтейнера.ОписаниеСведенийЗаявление Тогда
		Возврат "описание";
	ИначеЕсли ТипДокумента = Перечисления.ТипыСодержимогоТранспортногоКонтейнера.КвитанцияОПриемеЗаявления Тогда
		Возврат "квитанцияОПриеме";
	ИначеЕсли ТипДокумента = Перечисления.ТипыСодержимогоТранспортногоКонтейнера.СообщениеОПроверке Тогда
		Возврат "сообщениеОПроверке";	
	ИначеЕсли ТипДокумента = Перечисления.ТипыСодержимогоТранспортногоКонтейнера.СообщениеОбОтзывеЗаявления Тогда
		Возврат "сообщениеОбОтзывеЗаявления";
	ИначеЕсли ТипДокумента = Перечисления.ТипыСодержимогоТранспортногоКонтейнера.СообщениеОНесоответствиях Тогда
		Возврат "сообщениеОНесоответствиях";
		
	ИначеЕсли ТипДокумента = Перечисления.ТипыСодержимогоТранспортногоКонтейнера.СообщениеОПростОтметки Тогда
		Возврат "сообщениеОПростОтметки";
	ИначеЕсли ТипДокумента = Перечисления.ТипыСодержимогоТранспортногоКонтейнера.УведомлениеОбОтказеОтметки Тогда
		Возврат "уведомлениеОбОтказеОтметки";
		
	//2НДФЛ		
	ИначеЕсли ТипДокумента = Перечисления.ТипыСодержимогоТранспортногоКонтейнера.ОписаниеСведений2НДФЛ Тогда
		Возврат "описание";
	ИначеЕсли ТипДокумента = Перечисления.ТипыСодержимогоТранспортногоКонтейнера.Форма2НДФЛ Тогда
		Возврат "форма2НДФЛ";
	ИначеЕсли ТипДокумента = Перечисления.ТипыСодержимогоТранспортногоКонтейнера.Протокол2НДФЛ Тогда
		Возврат "протоколПриема2НДФЛ";
	ИначеЕсли ТипДокумента = Перечисления.ТипыСодержимогоТранспортногоКонтейнера.Реестр2НДФЛ Тогда
		Возврат "реестрПринятыхДокументов";
		
	// обращение
	ИначеЕсли ТипДокумента = Перечисления.ТипыСодержимогоТранспортногоКонтейнера.Обращение Тогда
		Возврат "обращение";
	ИначеЕсли ТипДокумента = Перечисления.ТипыСодержимогоТранспортногоКонтейнера.ОписаниеСведенийОбращениеПисьмоРассылка Тогда
		Возврат "описание";
	ИначеЕсли ТипДокумента = Перечисления.ТипыСодержимогоТранспортногоКонтейнера.Приложение Тогда
		Возврат "приложение";
	ИначеЕсли ТипДокумента = Перечисления.ТипыСодержимогоТранспортногоКонтейнера.ПодтверждениеДатыПолучения Тогда
		Возврат "подтверждениеДатыПолучения";
		
	// представление
	ИначеЕсли ТипДокумента = Перечисления.ТипыСодержимогоТранспортногоКонтейнера.Представление Тогда
		Возврат "представление";
	ИначеЕсли ТипДокумента = Перечисления.ТипыСодержимогоТранспортногоКонтейнера.ДоверенностьПредставление Тогда
		Возврат "доверенность";
	ИначеЕсли ТипДокумента = Перечисления.ТипыСодержимогоТранспортногоКонтейнера.ОписаниеПредставление Тогда
		Возврат "описание";
	ИначеЕсли ТипДокумента = Перечисления.ТипыСодержимогоТранспортногоКонтейнера.ПодтверждениеДатыОтправкиПредставление Тогда
		Возврат "подтверждениеДатыОтправки";
		
	// запрос
	ИначеЕсли ТипДокумента = Перечисления.ТипыСодержимогоТранспортногоКонтейнера.Запрос Тогда
		Возврат "запрос";
	ИначеЕсли ТипДокумента = Перечисления.ТипыСодержимогоТранспортногоКонтейнера.Ответ Тогда
		Возврат "ответ";
	ИначеЕсли ТипДокумента = Перечисления.ТипыСодержимогоТранспортногоКонтейнера.ОписаниеСведенийЗапрос Тогда
		Возврат "описание";
		
	// запрос на выписку ЕГРЮЛ/ЕГРИП
	ИначеЕсли ТипДокумента = Перечисления.ТипыСодержимогоТранспортногоКонтейнера.ЗапросНаВыпискуИзЕГРЮЛ_ЕГРИП Тогда
		Возврат "запрос";
		
	// Росстат
	ИначеЕсли ТипДокумента = Перечисления.ТипыСодержимогоТранспортногоКонтейнера.ФайлОтчетностиФСГС Тогда
		Возврат "отчет";
	ИначеЕсли ТипДокумента = Перечисления.ТипыСодержимогоТранспортногоКонтейнера.ОписаниеФайлаОтчетностиФСГС Тогда
		Возврат "описаниеОтчета";
	ИначеЕсли ТипДокумента = Перечисления.ТипыСодержимогоТранспортногоКонтейнера.ИзвещениеОПолученииДокументаФСГС Тогда
		Возврат "извещениеОПолучении";
	ИначеЕсли ТипДокумента = Перечисления.ТипыСодержимогоТранспортногоКонтейнера.ПисьмоФСГС Тогда
		Возврат "письмо";
	ИначеЕсли ТипДокумента = Перечисления.ТипыСодержимогоТранспортногоКонтейнера.ОписаниеПисьмаФСГС Тогда
		Возврат "описаниеПисьма";
	ИначеЕсли ТипДокумента = Перечисления.ТипыСодержимогоТранспортногоКонтейнера.ПриложениеПисьмаФСГС Тогда
		Возврат "приложениеПисьма";
	ИначеЕсли ТипДокумента = Перечисления.ТипыСодержимогоТранспортногоКонтейнера.УведомлениеОПриемеВОбработкуОтчетаФСГС Тогда
		Возврат "уведомлениеОПриемеВОбработку";
	ИначеЕсли ТипДокумента = Перечисления.ТипыСодержимогоТранспортногоКонтейнера.УведомлениеОНесоответствииФорматуОтчетаФСГС Тогда
		Возврат "уведомлениеОНесоответствииФормату";
	ИначеЕсли ТипДокумента = Перечисления.ТипыСодержимогоТранспортногоКонтейнера.УведомлениеОбУточненииОтчетаФСГС Тогда
		Возврат "уведомлениеОбУточнении";
	ИначеЕсли ТипДокумента = Перечисления.ТипыСодержимогоТранспортногоКонтейнера.УведомлениеОбОтказеОтчетаФСГС Тогда
		Возврат "уведомлениеОбОтказе";
	ИначеЕсли ТипДокумента = Перечисления.ТипыСодержимогоТранспортногоКонтейнера.УведомлениеОбОтклоненииОтчетаФСГС Тогда
		Возврат "уведомлениеОбОтклоненииОтчета";
	ИначеЕсли ТипДокумента = Перечисления.ТипыСодержимогоТранспортногоКонтейнера.РассылкаФСГС Тогда
		Возврат "рассылка";
	ИначеЕсли ТипДокумента = Перечисления.ТипыСодержимогоТранспортногоКонтейнера.ШаблонФСГС Тогда
		Возврат "шаблон";
	ИначеЕсли ТипДокумента = Перечисления.ТипыСодержимогоТранспортногоКонтейнера.ОписаниеРассылкиШаблоновФСГС Тогда
		Возврат "описаниеРассылкиШаблонов";
	ИначеЕсли ТипДокумента = Перечисления.ТипыСодержимогоТранспортногоКонтейнера.ПодтверждениеОператораФСГС Тогда
		Возврат "подтверждениеОператора";
		
		
	// запросы ПФР
	ИначеЕсли ТипДокумента = Перечисления.ТипыСодержимогоТранспортногоКонтейнера.ЗапросИОС Тогда
		Возврат "запрос";
	ИначеЕсли ТипДокумента = Перечисления.ТипыСодержимогоТранспортногоКонтейнера.ФайлОписанияЗапросаПФР Тогда
		Возврат "описаниеЗапроса";
	ИначеЕсли ТипДокумента = Перечисления.ТипыСодержимогоТранспортногоКонтейнера.ПодтверждениеПолученияЗапросПФР Тогда
		Возврат "подтверждениеПолучения";
	ИначеЕсли ТипДокумента = Перечисления.ТипыСодержимогоТранспортногоКонтейнера.ОтветНаЗапросПФР Тогда
		Возврат "ответ";
	ИначеЕсли ТипДокумента = Перечисления.ТипыСодержимогоТранспортногоКонтейнера.ОтветПриложениеНаЗапросПФР Тогда
		Возврат "ответПриложение";
		
	Иначе
		Возврат "";
	КонецЕсли;
	
КонецФункции

Функция ВыгрузитьФайлПакетаИзВременногоХранилищаВКаталог(КаталогФайловОтчетности, СтрДокумент) Экспорт
	
	ИмяДобавляемогоФайла = ?(ЗначениеЗаполнено(СтрДокумент.ИмяФайлаАрхива), СтрДокумент.ИмяФайлаАрхива, СтрДокумент.ИмяФайлаДокумента);
	ПолноеИмяДобавляемогоФайла = КаталогФайловОтчетности + ИмяДобавляемогоФайла;
	ПолучитьИзВременногоХранилища(СтрДокумент.Адрес).Записать(ПолноеИмяДобавляемогоФайла);
	
	Возврат ПолноеИмяДобавляемогоФайла;
	
КонецФункции

Функция СоздатьДеревоОписанияДокументовПакета() Экспорт
	
	ДеревоОписанияДокументовПакета = Новый ДеревоЗначений;
	ДеревоОписанияДокументовПакета.Колонки.Добавить("ТипДокумента");
	ДеревоОписанияДокументовПакета.Колонки.Добавить("ТипСодержимого");
	ДеревоОписанияДокументовПакета.Колонки.Добавить("ИдентификаторДокумента");
	ДеревоОписанияДокументовПакета.Колонки.Добавить("Сжат");
	ДеревоОписанияДокументовПакета.Колонки.Добавить("Зашифрован");
	ДеревоОписанияДокументовПакета.Колонки.Добавить("ИмяФайлаДокумента");
	ДеревоОписанияДокументовПакета.Колонки.Добавить("ИмяФайлаАрхива");
	ДеревоОписанияДокументовПакета.Колонки.Добавить("Адрес");
	Возврат ДеревоОписанияДокументовПакета;
	
КонецФункции

Функция ЗаархивироватьФайлПакета(
		СтрВложение,
		ИмяФайлаАрхива = Неопределено,
		ИдентификаторДляВременногоХранилища = Неопределено,
		ТипАрхива = Неопределено) Экспорт
	
	// создаем временный каталог, в который помещаем пакуемый файл
	ВременныйКаталог = ОперацииСФайламиЭДКО.СоздатьВременныйКаталог();
	
	// получаем двоичные данные файла из временного хранилища
	ДвоичныеДанныеФайла = ПолучитьИзВременногоХранилища(СтрВложение.Адрес);
	
	// если имя файла архива не задано, то генерируем новое
	Если ИмяФайлаАрхива = Неопределено Тогда
		ИмяФайлаАрхива = СгенерироватьИмяФайлаДокументаВСоставеПакета();
	КонецЕсли;
	
	// формируем имя временного файла
	ПолноеИмяАрхивируемогоФайла = ВременныйКаталог + ИмяФайлаАрхива;
	
	// выгружаем двоичные данные в файл
	ДвоичныеДанныеФайла.Записать(ПолноеИмяАрхивируемогоФайла);
	
	// пишем архив
	КороткоеИмяФайлаАрхива = СгенерироватьИмяФайлаДокументаВСоставеПакета();
	ПолноеИмяФайлаАрхива = ВременныйКаталог + КороткоеИмяФайлаАрхива;
	Если ТипАрхива = "gzip" Тогда
		КороткоеИмяФайлаАрхиваGzip = ИмяФайлаАрхива + ".gz.enc";
		ПолноеИмяФайлаАрхиваGzip = ВременныйКаталог + КороткоеИмяФайлаАрхиваGzip;
	КонецЕсли;
	Попытка
		ОбъектЗаписьЗИП = Новый ЗаписьZipФайла(ПолноеИмяФайлаАрхива,,,,
			?(ТипАрхива = "gzip", УровеньСжатияZIP.Оптимальный, УровеньСжатияZIP.Максимальный));
		ОбъектЗаписьЗИП.Добавить(ПолноеИмяАрхивируемогоФайла);
		ОбъектЗаписьЗИП.Записать();
		Если ТипАрхива = "gzip" Тогда
			МассивОписанийИДанных = ОбщегоНазначенияЭДКО.МассивОписанийИДанныхZipФайла(ПолноеИмяФайлаАрхива, Истина);
			Если МассивОписанийИДанных = Неопределено Тогда
				ОшибкаАрхивирования = Истина;
			Иначе
				МассивОписанийИДанных = ОбщегоНазначенияКлиентСервер.ЗначениеВМассиве(МассивОписанийИДанных[0]);
				ОбщегоНазначенияЭДКО.СформироватьGzip(МассивОписанийИДанных, ПолноеИмяФайлаАрхиваGzip);
				ОшибкаАрхивирования = Ложь;
			КонецЕсли;
		Иначе
			ОшибкаАрхивирования = Ложь;
		КонецЕсли;
	Исключение
		ОшибкаАрхивирования = Истина;
	КонецПопытки;
	Если ТипАрхива = "gzip" Тогда
		КороткоеИмяФайлаАрхива = КороткоеИмяФайлаАрхиваGzip;
		ПолноеИмяФайлаАрхива = ПолноеИмяФайлаАрхиваGzip;
	КонецЕсли;
	
	// уведомляем об ошибке, если она была
	Если ОшибкаАрхивирования Тогда
		
		ТекстСообщения = СтроковыеФункцииКлиентСервер.ПодставитьПараметрыВСтроку(НСтр("ru = 'Ошибка создания архива: %1'"), ИнформацияОбОшибке().Описание);
		ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
		
		// удаляем временный каталог
		ОперацииСФайламиЭДКО.УдалитьВременныйФайл(ВременныйКаталог);

		Возврат Неопределено;
	КонецЕсли;
	
	// помещаем архив обратно во временное хранилище
	СтрВложение.Адрес = ПоместитьВоВременноеХранилище(Новый ДвоичныеДанные(ПолноеИмяФайлаАрхива), ИдентификаторДляВременногоХранилища);
	
	// удаляем временный каталог
	ОперацииСФайламиЭДКО.УдалитьВременныйФайл(ВременныйКаталог);
	
	Возврат КороткоеИмяФайлаАрхива;
	
КонецФункции

Функция ОпределитьДокументВДеревеОписания(ДеревоОписанияДокументовПакета, ТипДокумента = Неопределено, ТипСодержимого = Неопределено, ИдентификаторДокумента = Неопределено, Сжат = Неопределено, Зашифрован = Неопределено, ИмяФайлаДокумента = Неопределено, ИмяФайлаАрхива = Неопределено, Адрес = Неопределено) Экспорт
	
	НовСтр = ДеревоОписанияДокументовПакета.Строки.Добавить();
	НовСтр.ТипДокумента = ТипДокумента;
	НовСтр.ТипСодержимого = ТипСодержимого;
	НовСтр.ИдентификаторДокумента = ИдентификаторДокумента;
	НовСтр.Сжат = Сжат;
	НовСтр.Зашифрован = Зашифрован;
	НовСтр.ИмяФайлаДокумента = ИмяФайлаДокумента;
	НовСтр.ИмяФайлаАрхива = ИмяФайлаАрхива;
	НовСтр.Адрес = Адрес;
	
	Возврат НовСтр;
	
КонецФункции

Функция ТипСодержимогоПоОписаниюДИВ(ВложениеДИВ, ДляПФР = Ложь) Экспорт
	
	Если ЗначениеЗаполнено(ВложениеДИВ.ТипСодержимогоФайла) Тогда
		Возврат ПредставлениеПоТипуСодержимогоФайла(ВложениеДИВ.ТипСодержимогоФайла, ДляПФР);
	Иначе
		Возврат "xml";
	КонецЕсли;
	
КонецФункции

Функция ПредставлениеПоТипуСодержимогоФайла(ТипСодержимогоФайла, ДляПФР)
	
	Если ТипСодержимогоФайла = Перечисления.ТипыСодержимогоФайлов.Текст1251 Тогда
		Возврат "plain1251";
	ИначеЕсли ТипСодержимогоФайла = Перечисления.ТипыСодержимогоФайлов.Текст866 Тогда
		Возврат "plain866";
	ИначеЕсли ТипСодержимогоФайла = Перечисления.ТипыСодержимогоФайлов.xml Тогда
		Возврат "xml";
	ИначеЕсли ТипСодержимогоФайла = Перечисления.ТипыСодержимогоФайлов.html Тогда
		Возврат "html";
	ИначеЕсли НЕ ДляПФР Тогда
		Если ТипСодержимогоФайла = Перечисления.ТипыСодержимогоФайлов.pdf Тогда
			Возврат "pdf";
		ИначеЕсли ТипСодержимогоФайла = Перечисления.ТипыСодержимогоФайлов.rtf Тогда
			Возврат "rtf";
		ИначеЕсли ТипСодержимогоФайла = Перечисления.ТипыСодержимогоФайлов.tiff Тогда
			Возврат "tiff";
		ИначеЕсли ТипСодержимогоФайла = Перечисления.ТипыСодержимогоФайлов.jpeg Тогда
			Возврат "jpeg";
		ИначеЕсли ТипСодержимогоФайла = Перечисления.ТипыСодержимогоФайлов.png Тогда
			Возврат "png";
		ИначеЕсли ТипСодержимогоФайла = Перечисления.ТипыСодержимогоФайлов.ms_word Тогда
			Возврат "ms-word";
		ИначеЕсли ТипСодержимогоФайла = Перечисления.ТипыСодержимогоФайлов.ms_excel Тогда
			Возврат "ms-excel";
		ИначеЕсли ТипСодержимогоФайла = Перечисления.ТипыСодержимогоФайлов.odf_text Тогда
			Возврат "odf-text";
		ИначеЕсли ТипСодержимогоФайла = Перечисления.ТипыСодержимогоФайлов.odf_spreadsheet Тогда
			Возврат "odf-spreadsheet";
		ИначеЕсли ТипСодержимогоФайла = Перечисления.ТипыСодержимогоФайлов.oxml_word Тогда
			Возврат "oxml-word";
		ИначеЕсли ТипСодержимогоФайла = Перечисления.ТипыСодержимогоФайлов.oxml_spreadsheet Тогда
			Возврат "oxml-spreadsheet";
		ИначеЕсли ТипСодержимогоФайла = Перечисления.ТипыСодержимогоФайлов.sgn Тогда
			Возврат "sgn";
		Иначе
			Возврат "unknown";
		КонецЕсли;
	Иначе
		Возврат "unknown";
	КонецЕсли;
	
КонецФункции

#КонецОбласти

#Область СформироватьПакетФНС

Функция СформироватьПакетФНС_ПодготовитьСертификатыДляПоиска(УчетнаяЗапись, СертификатРуководителя, СертификатГлавногоБухгалтера, СертификатОтветственногоЗаШифрование) Экспорт

	ЭтоЭлектроннаяПодписьВМоделиСервиса = ЭлектроннаяПодписьВМоделиСервисаБРОВызовСервера.РеквизитыУчетнойЗаписи(УчетнаяЗапись).ЭлектроннаяПодписьВМоделиСервиса;
	
	СертификатыДляПоиска = Новый Массив;
	
	ОтпечатокСертификатРуководителя = СокрЛП(СертификатРуководителя);
	ОтпечатокСертификатГлавногоБухгалтера = СокрЛП(СертификатГлавногоБухгалтера);
	ОтпечатокСертификатОтветственногоЗаШифрование = СокрЛП(СертификатОтветственногоЗаШифрование);
	
	Если Не ЗначениеЗаполнено(ОтпечатокСертификатРуководителя) Тогда
		ТекстСообщения = СтроковыеФункцииКлиентСервер.ПодставитьПараметрыВСтроку(
			НСтр("ru = 'В настройках учетной записи %1 не задан сертификат руководителя.'"), УчетнаяЗапись);
		ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
		Возврат Неопределено;
	Иначе
		СертификатыДляПоиска.Добавить(
			Новый Структура("Отпечаток,ЭтоЭлектроннаяПодписьВМоделиСервиса", 
				ОтпечатокСертификатРуководителя, ЭтоЭлектроннаяПодписьВМоделиСервиса));
	КонецЕсли;
	
	Если ЗначениеЗаполнено(ОтпечатокСертификатГлавногоБухгалтера) Тогда
		СертификатыДляПоиска.Добавить(
			Новый Структура("Отпечаток,ЭтоЭлектроннаяПодписьВМоделиСервиса", 
				ОтпечатокСертификатГлавногоБухгалтера, ЭтоЭлектроннаяПодписьВМоделиСервиса));
	КонецЕсли;
	
	Если Не ЗначениеЗаполнено(ОтпечатокСертификатОтветственногоЗаШифрование) Тогда
		ТекстСообщения = СтроковыеФункцииКлиентСервер.ПодставитьПараметрыВСтроку(
			НСтр("ru = 'В настройках учетной записи %1 не задан сертификат ответственного за шифрование.'"), УчетнаяЗапись);
		ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
		Возврат Неопределено;
	Иначе
		СертификатыДляПоиска.Добавить(
			Новый Структура("Отпечаток,ЭтоЭлектроннаяПодписьВМоделиСервиса", 
				ОтпечатокСертификатОтветственногоЗаШифрование, ЭтоЭлектроннаяПодписьВМоделиСервиса));
	КонецЕсли;
	
	Возврат СертификатыДляПоиска;

КонецФункции

Процедура СформироватьПакетФНС_ПроверкиСертификатов(КонтекстОтправки, СообщениеОбОшибке)

	// Внимание!
	// ---------
	// В Контейнере есть одноименная процедура, которая используется при отправке отчетов
	// в интерактивном режиме пользователем.
	// При изменениях необходимо синхронно менять обе процедуры.
	
	Если КонтекстОтправки.Свойство("ОбъектСертификатОтветственногоЗаШифрование") Тогда // Возможна ситуация, когда сертификаты уже получены и проверены.
		Возврат;
	КонецЕсли;
	
	СертификатыДляПоиска = СформироватьПакетФНС_ПодготовитьСертификатыДляПоиска(
		КонтекстОтправки.УчетнаяЗапись,
		КонтекстОтправки.СертификатРуководителя,
		КонтекстОтправки.СертификатГлавногоБухгалтера,
		КонтекстОтправки.СертификатОтветственногоЗаШифрование);
	
	Если СертификатыДляПоиска = Неопределено Тогда
		СообщениеОбОшибке = ТекстСообщенийПользователю();
		Возврат;
	КонецЕсли;
	
	РезультатПоиска = НайтиСертификаты(СертификатыДляПоиска, Истина);
	
	СформироватьПакетФНС_ПроверкиСертификатовПослеПолученияСертификатов(КонтекстОтправки, СообщениеОбОшибке, РезультатПоиска);

КонецПроцедуры

Процедура СформироватьПакетФНС_ПроверкиСертификатовПослеПолученияСертификатов(КонтекстОтправки, СообщениеОбОшибке, РезультатПоиска)

	// Внимание!
	// ---------
	// В Контейнере есть одноименная процедура, которая используется при отправке отчетов
	// в интерактивном режиме пользователем.
	// При изменениях необходимо синхронно менять обе процедуры.
	
	ОбъектыСертификатов = СформироватьПакетФНС_ПроверкиСертификатовПослеПолученияСертификатовРазобратьНайденное(
		РезультатПоиска.Сертификаты,
		КонтекстОтправки.СертификатРуководителя,
		КонтекстОтправки.СертификатГлавногоБухгалтера,
		КонтекстОтправки.СертификатОтветственногоЗаШифрование);
	
	Если ОбъектыСертификатов = Неопределено Тогда
		СообщениеОбОшибке = ТекстСообщенийПользователю();
		Если НЕ ЗначениеЗаполнено(СообщениеОбОшибке) Тогда
			СообщениеОбОшибке = НСтр("ru = 'Не удалось найти сертификаты ответственных лиц в системном хранилище сертификатов.'");
		КонецЕсли;
		Возврат;
	КонецЕсли;
		
	///////////////////////////////////////////////////////////////////////////////
	Для каждого ОбъектСертификат Из ОбъектыСертификатов Цикл
		КонтекстОтправки.Вставить(ОбъектСертификат.Ключ, ОбъектСертификат.Значение);
	КонецЦикла;
	
КонецПроцедуры

Функция СформироватьПакетФНС_ПроверкиСертификатовПослеПолученияСертификатовРазобратьНайденное(Сертификаты, СертификатРуководителя, СертификатГлавногоБухгалтера, СертификатОтветственногоЗаШифрование) Экспорт
	
	ОтпечатокСертификатРуководителя = СокрЛП(СертификатРуководителя);
	ОтпечатокСертификатГлавногоБухгалтера = СокрЛП(СертификатГлавногоБухгалтера);
	ОтпечатокСертификатОтветственногоЗаШифрование = СокрЛП(СертификатОтветственногоЗаШифрование);
	
	ОбъектСертификатРуководителя = Неопределено;
	ОбъектСертификатГлавногоБухгалтера = Неопределено;
	ОбъектСертификатОтветственногоЗаШифрование = Неопределено;
	
	Для Каждого НайденныйСертификат Из Сертификаты Цикл
		Если ОтпечатокСертификатРуководителя = НайденныйСертификат.Отпечаток Тогда
			ОбъектСертификатРуководителя = НайденныйСертификат;
		КонецЕсли;
		Если ОтпечатокСертификатГлавногоБухгалтера = НайденныйСертификат.Отпечаток Тогда
			ОбъектСертификатГлавногоБухгалтера = НайденныйСертификат;
		КонецЕсли;
		Если ОтпечатокСертификатОтветственногоЗаШифрование = НайденныйСертификат.Отпечаток Тогда
			ОбъектСертификатОтветственногоЗаШифрование = НайденныйСертификат;
		КонецЕсли;
	КонецЦикла;
	
	Если ОбъектСертификатРуководителя = Неопределено Тогда
		ТекстСообщения = НСтр("ru = 'Не удалось найти сертификат руководителя в системном хранилище сертификатов.'");
		ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
		Возврат Неопределено;
	КонецЕсли;

	// Сертификат мог быть получен без проверки на валидность, тогда в его свойствах не будет такого поля.
	Если ОбъектСертификатРуководителя.Свойство("Валиден") И Не ОбъектСертификатРуководителя.Валиден Тогда
		ТекстСообщения = НСтр("ru = 'Сертификат руководителя не валиден.'");
		ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
		Возврат Неопределено;
	КонецЕсли;
	
	Если ЗначениеЗаполнено(ОтпечатокСертификатГлавногоБухгалтера) И ОбъектСертификатГлавногоБухгалтера = Неопределено Тогда
		ТекстСообщения = НСтр("ru = 'Не удалось найти сертификат главного бухгалтера в системном хранилище сертификатов.'");
		ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
		Возврат Неопределено;
	КонецЕсли;
	
	Если ЗначениеЗаполнено(ОбъектСертификатГлавногоБухгалтера) 
		И ОбъектСертификатГлавногоБухгалтера.Свойство("Валиден") И Не ОбъектСертификатГлавногоБухгалтера.Валиден Тогда
		ТекстСообщения = НСтр("ru = 'Сертификат главного бухгалтера не валиден.'");
		ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
		Возврат Неопределено;
	КонецЕсли;
	
	Если ОтпечатокСертификатОтветственногоЗаШифрование = Неопределено Тогда
		ТекстСообщения = НСтр("ru = 'Не удалось найти сертификат ответственного за шифрование в системном хранилище сертификатов.'");
		ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
		Возврат Неопределено;
	КонецЕсли;
	
	Если ОбъектСертификатОтветственногоЗаШифрование.Свойство("Валиден") И Не ОбъектСертификатОтветственногоЗаШифрование.Валиден Тогда
		ТекстСообщения = НСтр("ru = 'Сертификат ответственного за шифрование не валиден.'");
		ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
		Возврат Неопределено;
	КонецЕсли;
		
	Результат = Новый Структура();
	Результат.Вставить("ОбъектСертификатРуководителя",               ОбъектСертификатРуководителя);
	Результат.Вставить("ОбъектСертификатГлавногоБухгалтера",         ОбъектСертификатГлавногоБухгалтера);
	Результат.Вставить("ОбъектСертификатОтветственногоЗаШифрование", ОбъектСертификатОтветственногоЗаШифрование);
	
	Возврат Результат;
	
КонецФункции

Процедура СформироватьПакетФНС(ТранспортноеСообщение, УникальныйИдентификатор, СообщениеОбОшибке)

	// Внимание!
	// ---------
	// В Контейнере есть одноименная процедура, которая используется при отправке отчетов
	// в интерактивном режиме пользователем.
	// При изменениях необходимо синхронно менять обе процедуры.
	
	КонтекстОтправки = Новый Структура;
	КонтекстОтправки.Вставить("ТранспортноеСообщение", ТранспортноеСообщение);
	КонтекстОтправки.Вставить("УникальныйИдентификатор", УникальныйИдентификатор);
	КонтекстОтправки.Вставить("Оповещать", Ложь);
	
	// общие проверки
	Если НЕ СформироватьПакетФНС_ОбщиеПроверкиПередФормированием(КонтекстОтправки) Тогда
		СообщениеОбОшибке = ТекстСообщенийПользователю();
		Возврат;
	КонецЕсли;
		
	// получение и проверки сертификатов
	СформироватьПакетФНС_ПолучитьИПроверитьСертификатыПолучателя(КонтекстОтправки, СообщениеОбОшибке);
	Если ЗначениеЗаполнено(СообщениеОбОшибке) Тогда
		Возврат;
	КонецЕсли;
	
	СформироватьПакетФНСПослеПроверкиИПолученияСертификатовПолучателя(КонтекстОтправки, СообщениеОбОшибке);
	Если ЗначениеЗаполнено(СообщениеОбОшибке) Тогда
		Возврат;
	КонецЕсли;

	СформироватьПакетФНСПослеПроверкиСертификатов(КонтекстОтправки, СообщениеОбОшибке);
	Если ЗначениеЗаполнено(СообщениеОбОшибке) Тогда
		Возврат;
	КонецЕсли;
	
	СформироватьПакетФНС_Подписание(КонтекстОтправки, СообщениеОбОшибке);
	Если ЗначениеЗаполнено(СообщениеОбОшибке) Тогда
		Возврат;
	КонецЕсли;
	
	СформироватьПакетФНСПослеПодписания(КонтекстОтправки, СообщениеОбОшибке);
	
КонецПроцедуры

Функция СформироватьПакетФНС_ОбщиеПроверкиПередФормированием(Контекст) Экспорт
	
	Сообщение = Контекст.ТранспортноеСообщение;
	СообщениеТема = Сообщение.Тема;
	
	// проверка на то, что сообщение исходящее
	СтатусТранспортногоСообщения = Сообщение.Статус;
	Если СтатусТранспортногоСообщения <> Перечисления.СтатусыПисем.Сохраненное
	И СтатусТранспортногоСообщения <> Перечисления.СтатусыПисем.Исходящее
	И СтатусТранспортногоСообщения <> Перечисления.СтатусыПисем.Отправленное Тогда
		ТекстСообщения = НСтр("ru = 'Невозможно формирование пакета по входящему сообщению!'");
		ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
		Возврат Ложь;
	КонецЕсли;
	
	ПризнакОшибки = Ложь;
	
	ТипСообщения = Сообщение.Тип;
	
	СсылкаОтправитель = Сообщение.Отправитель;
	СсылкаПолучатель = ОпределитьПолучателяТранспортногоКонтейнера(Сообщение);
	УчетнаяЗапись = Сообщение.УчетнаяЗапись;
	ЦиклОбмена = Сообщение.ЦиклОбмена;
	СообщениеОснование = Сообщение.Основание;
	
	Если НЕ ЗначениеЗаполнено(ТипСообщения) Тогда
		ТекстСообщения = НСтр("ru = 'Ошибка шифрования транспортного сообщения: тип транспортного сообщения не определен!'");
		ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
		ПризнакОшибки = Истина;
	КонецЕсли;
	
	Если НЕ ЗначениеЗаполнено(СсылкаОтправитель) Тогда
		ТекстСообщения = НСтр("ru = 'Ошибка шифрования транспортного сообщения: не задан отправитель!'");
		ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
		ПризнакОшибки = Истина;
	КонецЕсли;
	
	Если НЕ ЗначениеЗаполнено(СсылкаПолучатель) Тогда
		ТекстСообщения = НСтр("ru = 'Ошибка шифрования транспортного сообщения: не задан получатель!'");
		ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
		ПризнакОшибки = Истина;
	КонецЕсли;
	
	Если НЕ ЗначениеЗаполнено(УчетнаяЗапись) Тогда
		ТекстСообщения = НСтр("ru = 'Ошибка шифрования транспортного сообщения: не задана учетная запись!'");
		ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
		ПризнакОшибки = Истина;
	ИначеЕсли НЕ УчетнаяЗапись.ПредназначенаДляДокументооборотаСФНС Тогда
		ТекстСообщения = НСтр("ru = 'Ошибка шифрования транспортного сообщения: учетная запись не предназначена для электронного документооборота с ФНС!'");
		ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
		ПризнакОшибки = Истина;
	КонецЕсли;
	
	Если НЕ ЗначениеЗаполнено(ЦиклОбмена) Тогда
		ТекстСообщения = НСтр("ru = 'Ошибка шифрования транспортного сообщения: цикл обмена не задан!'");
		ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
		ПризнакОшибки = Истина;
	КонецЕсли;
	
	// определяем сертификат получателя
	СертификатПолучателя = ПолучитьСертификатСубъекта(СсылкаПолучатель);
	
	// определяем сертификат оператора
	СертификатСервераДокументооборота = ?(ЦиклОбмена.ВидОтчета = Справочники.ВидыОтправляемыхДокументов.НДС
		И НЕ УчетнаяЗапись.ОбменНапрямую И (УчетнаяЗапись.СпецоператорСвязи = Перечисления.СпецоператорыСвязи.КалугаАстрал
		ИЛИ УчетнаяЗапись.СпецоператорСвязи = Перечисления.СпецоператорыСвязи.Форус),
		ПолучитьСертификатСубъекта(УчетнаяЗапись.СерверДокументооборота), Неопределено);
	
	// определяем сертификаты отправителя
	СертификатРуководителя = СокрЛП(УчетнаяЗапись.СертификатРуководителя);
	СертификатГлавногоБухгалтера = СокрЛП(УчетнаяЗапись.СертификатГлавногоБухгалтера);
	СертификатОтветственногоЗаШифрование = СокрЛП(УчетнаяЗапись.СертификатДляШифрования);
	
	// определяем прочие свойства формирования пакета
	Если ТипСообщения = Перечисления.ТипыТранспортныхСообщений.ДекларацияНП Тогда
		
		КодТипаДокументооборота = "01";
		НаименованиеТипаДокументооборота = "Декларация";
		КодТипаТранзакции = "01";
		КодТипаОсновногоДокумента = "01";
		НаименованиеТипаТранзакции = "ДекларацияНП";
		
	ИначеЕсли ТипСообщения = Перечисления.ТипыТранспортныхСообщений.Форма2НДФЛНП Тогда
		
		КодТипаДокументооборота = "08";
		НаименованиеТипаДокументооборота = "Сведения2НДФЛ";
		КодТипаТранзакции = "01";
		КодТипаОсновногоДокумента = "01";
		НаименованиеТипаТранзакции = "Форма2НДФЛНП";
		
	ИначеЕсли ТипСообщения = Перечисления.ТипыТранспортныхСообщений.ИзвещениеПодтверждениеНП Тогда
		
		Если ЦиклОбмена.Тип = Перечисления.ТипыЦикловОбмена.Форма2НДФЛ Тогда
			КодТипаДокументооборота = "08";
			НаименованиеТипаДокументооборота = "Сведения2НДФЛ";
			КодТипаТранзакции = "11";
			КодТипаОсновногоДокумента = "07";
		ИначеЕсли ЦиклОбмена.Тип = Перечисления.ТипыЦикловОбмена.ОбращениеНП Тогда
			КодТипаДокументооборота = "02";
			НаименованиеТипаДокументооборота = "ОбращениеНП";
			КодТипаТранзакции = "11";
			КодТипаОсновногоДокумента = "06";
		ИначеЕсли ЦиклОбмена.Тип = Перечисления.ТипыЦикловОбмена.Представление Тогда
			КодТипаДокументооборота = "12";
			НаименованиеТипаДокументооборота = "Представление";
			КодТипаОсновногоДокумента = "08";
			КодТипаТранзакции = "08";	
		ИначеЕсли ЦиклОбмена.Тип = Перечисления.ТипыЦикловОбмена.ИОН Тогда
			КодТипаДокументооборота = "06";
			НаименованиеТипаДокументооборота = "ИОН";
			КодТипаТранзакции = "11";
			КодТипаОсновногоДокумента = "08";
		ИначеЕсли ЦиклОбмена.Тип = Перечисления.ТипыЦикловОбмена.Заявление Тогда
			КодТипаДокументооборота = "09";
			НаименованиеТипаДокументооборота = "Заявление";
			КодТипаТранзакции = "17";
			КодТипаОсновногоДокумента = "07";
		Иначе // прочая отчетность
			КодТипаДокументооборота = "01";
			НаименованиеТипаДокументооборота = "Декларация";
			КодТипаТранзакции = "11";
			КодТипаОсновногоДокумента = "09";
		КонецЕсли;
		НаименованиеТипаТранзакции = "ИзвещениеПодтверждениеНП";
		
	ИначеЕсли ТипСообщения = Перечисления.ТипыТранспортныхСообщений.ИзвещениеРезультатПриемаНП Тогда
		
		Если ЦиклОбмена.Тип = Перечисления.ТипыЦикловОбмена.Форма2НДФЛ Тогда
			КодТипаДокументооборота = "08";
			НаименованиеТипаДокументооборота = "Сведения2НДФЛ";
			КодТипаТранзакции = "06";
			КодТипаОсновногоДокумента = "07";
		ИначеЕсли ЦиклОбмена.Тип = Перечисления.ТипыЦикловОбмена.ОбращениеНП Тогда
			КодТипаДокументооборота = "02";
			НаименованиеТипаДокументооборота = "ОбращениеНП";
			КодТипаТранзакции = "06";
			КодТипаОсновногоДокумента = "06";
		ИначеЕсли ЦиклОбмена.Тип = Перечисления.ТипыЦикловОбмена.Представление Тогда
			КодТипаДокументооборота = "12";
			НаименованиеТипаДокументооборота = "Представление";
			КодТипаОсновногоДокумента = "08";
			КодТипаТранзакции = "06";
		ИначеЕсли ЦиклОбмена.Тип = Перечисления.ТипыЦикловОбмена.ИОН Тогда
			КодТипаДокументооборота = "06";
			НаименованиеТипаДокументооборота = "ИОН";
			КодТипаТранзакции = "06";
			КодТипаОсновногоДокумента = "08";
		ИначеЕсли ЦиклОбмена.Тип = Перечисления.ТипыЦикловОбмена.Заявление Тогда
			КодТипаДокументооборота = "09";
			НаименованиеТипаДокументооборота = "Заявление";
			КодТипаТранзакции = "06";
			КодТипаОсновногоДокумента = "07";
		Иначе // прочая отчетность
			КодТипаДокументооборота = "01";
			НаименованиеТипаДокументооборота = "Декларация";
			КодТипаТранзакции = "06";
			КодТипаОсновногоДокумента = "09";
		КонецЕсли;
		НаименованиеТипаТранзакции = "ИзвещениеРезультатПриемаНП";
		
	ИначеЕсли ТипСообщения = Перечисления.ТипыТранспортныхСообщений.ИзвещениеРезультатОбработкиНП Тогда
		
		Если ЦиклОбмена.Тип = Перечисления.ТипыЦикловОбмена.ИОН Тогда
			КодТипаДокументооборота = "06";
			НаименованиеТипаДокументооборота = "ИОН";
			КодТипаОсновногоДокумента = "08";
			КодТипаТранзакции = "09";
			НаименованиеТипаТранзакции = "ИзвещениеРезультатОбработкиНП";
		ИначеЕсли ЦиклОбмена.Тип = Перечисления.ТипыЦикловОбмена.Заявление Тогда
			КодТипаДокументооборота = "09";
			НаименованиеТипаДокументооборота = "Заявление";
			КодТипаОсновногоДокумента = "07";
			КодТипаТранзакции = "09";
			НаименованиеТипаТранзакции = "ИзвещениеРезультатОбработкиНП";
		Иначе
			КодТипаДокументооборота = "01";
			НаименованиеТипаДокументооборота = "Декларация";
			КодТипаТранзакции = "09";
			КодТипаОсновногоДокумента = "09";
		КонецЕсли;
		НаименованиеТипаТранзакции = "ИзвещениеРезультатОбработкиНП";
		
	ИначеЕсли ТипСообщения = Перечисления.ТипыТранспортныхСообщений.ОбращениеНП Тогда
		
		КодТипаДокументооборота = "02";
		НаименованиеТипаДокументооборота = "ОбращениеНП";
		КодТипаТранзакции = "01";
		КодТипаОсновногоДокумента = "01";
		НаименованиеТипаТранзакции = "ОбращениеНП";
		
	ИначеЕсли ТипСообщения = Перечисления.ТипыТранспортныхСообщений.ПредставлениеНП Тогда
		
		КодТипаДокументооборота = "12";
		НаименованиеТипаДокументооборота = "Представление";
		КодТипаТранзакции = "01";
		КодТипаОсновногоДокумента = "01";
		НаименованиеТипаТранзакции = "ПредставлениеНП";

	ИначеЕсли ТипСообщения = Перечисления.ТипыТранспортныхСообщений.ЗапросНП Тогда
		
		КодТипаДокументооборота = "06";
		НаименованиеТипаДокументооборота = "ИОН";
		КодТипаТранзакции = "01";
		КодТипаОсновногоДокумента = "01";
		НаименованиеТипаТранзакции = "ЗапросНП";
		
	ИначеЕсли ТипСообщения = Перечисления.ТипыТранспортныхСообщений.ИзвещениеПисьмоНП Тогда
		
		КодТипаДокументооборота = "03";
		НаименованиеТипаДокументооборота = "ПисьмоНО";
		КодТипаОсновногоДокумента = "05";
		КодТипаТранзакции = "03";
		НаименованиеТипаТранзакции = "ИзвещениеПисьмоНП";
		
	ИначеЕсли ТипСообщения = Перечисления.ТипыТранспортныхСообщений.ИзвещениеРассылкаНП Тогда
		
		КодТипаДокументооборота = "05";
		НаименованиеТипаДокументооборота = "РассылкаГрупповая";
		КодТипаОсновногоДокумента = "05";
		КодТипаТранзакции = "03";
		НаименованиеТипаТранзакции = "ИзвещениеРассылкаНП";
		
	ИначеЕсли ТипСообщения = Перечисления.ТипыТранспортныхСообщений.ЗаявлениеНП Тогда
		
		КодТипаДокументооборота = "09";
		НаименованиеТипаДокументооборота = "Заявление";
		КодТипаТранзакции = "01";
		КодТипаОсновногоДокумента = "01";
		НаименованиеТипаТранзакции = "ЗаявлениеНП";
		
	ИначеЕсли ТипСообщения = Перечисления.ТипыТранспортныхСообщений.ИзвещениеРезультатОбработкиРФНП Тогда
		
		КодТипаДокументооборота = "09";
		НаименованиеТипаДокументооборота = "Заявление";
		КодТипаТранзакции = "09";
		КодТипаОсновногоДокумента = "08";
		НаименованиеТипаТранзакции = "ИзвещениеРезультатОбработкиРФНП";
		
	ИначеЕсли ТипСообщения = Перечисления.ТипыТранспортныхСообщений.ИзвещениеОбОтзывеЗаявлениеРФНП Тогда
		
		КодТипаДокументооборота = "09";
		НаименованиеТипаДокументооборота = "Заявление";
		КодТипаТранзакции = "12";
		КодТипаОсновногоДокумента = "08";
		НаименованиеТипаТранзакции = "ИзвещениеОбОтзывеЗаявлениеРФНП";
		
	ИначеЕсли ТипСообщения = Перечисления.ТипыТранспортныхСообщений.ИзвещениеРезультатОбработкиТСНП Тогда
		
		КодТипаДокументооборота = "09";
		НаименованиеТипаДокументооборота = "Заявление";
		КодТипаТранзакции = "15";
		КодТипаОсновногоДокумента = "08";
		НаименованиеТипаТранзакции = "ИзвещениеРезультатОбработкиТСНП";
		
	ИначеЕсли ТипСообщения = Перечисления.ТипыТранспортныхСообщений.ИзвещениеДокументНП Тогда
		
		КодТипаДокументооборота = "10";
		НаименованиеТипаДокументооборота = "Документ";
		КодТипаОсновногоДокумента = "07";
		КодТипаТранзакции = "04";
		НаименованиеТипаТранзакции = "ИзвещениеДокументНП";
		
	ИначеЕсли ТипСообщения = Перечисления.ТипыТранспортныхСообщений.РезультатПриемаДокументНП Тогда
		
		КодТипаДокументооборота = "10";
		НаименованиеТипаДокументооборота = "Документ";
		
		Если СообщениеТема = "ФНС:1.0-квитанцияОПриеме-РезультатПриемаДокументНП" Тогда
			//квитанцияОПриеме
			КодТипаОсновногоДокумента = "06";
		Иначе
			//уведомлениеОбОтказе
			КодТипаОсновногоДокумента = "05";
		КонецЕсли;
		
		КодТипаТранзакции = "05";
		НаименованиеТипаТранзакции = "РезультатПриемаДокументНП";
		
	ИначеЕсли ТипСообщения = Перечисления.ТипыТранспортныхСообщений.ЗапросНаВыпискуЕРГЮЛ_ЕГРИП Тогда
		
		КодТипаДокументооборота = "06";
		НаименованиеТипаДокументооборота = "ИОН";
		КодТипаОсновногоДокумента = "01";
		КодТипаТранзакции = "01";
		НаименованиеТипаТранзакции = "ЗапросНП";

	КонецЕсли;
	
	// запоминаем в контексте
	Контекст.Вставить("ТипСообщения", ТипСообщения);
	Контекст.Вставить("СсылкаОтправитель", СсылкаОтправитель);
	Контекст.Вставить("СсылкаПолучатель", СсылкаПолучатель);
	Контекст.Вставить("СсылкаСерверДокументооборота", УчетнаяЗапись.СерверДокументооборота);
	Контекст.Вставить("УчетнаяЗапись", УчетнаяЗапись);
	Контекст.Вставить("ЦиклОбмена", ЦиклОбмена);
	Контекст.Вставить("СертификатРуководителя", СертификатРуководителя);
	Контекст.Вставить("СертификатГлавногоБухгалтера", СертификатГлавногоБухгалтера);
	Контекст.Вставить("СертификатОтветственногоЗаШифрование", СертификатОтветственногоЗаШифрование);
	Контекст.Вставить("СертификатПолучателя", СертификатПолучателя);
	Контекст.Вставить("СертификатСервераДокументооборота", СертификатСервераДокументооборота);
	
	Контекст.Вставить("КодТипаДокументооборота", КодТипаДокументооборота);
	Контекст.Вставить("НаименованиеТипаДокументооборота", НаименованиеТипаДокументооборота);
	Контекст.Вставить("КодТипаОсновногоДокумента", КодТипаОсновногоДокумента);
	Контекст.Вставить("КодТипаТранзакции", КодТипаТранзакции);
	Контекст.Вставить("НаименованиеТипаТранзакции", НаименованиеТипаТранзакции);
	
	Контекст.Вставить("ТипТранспортногоСообщения", ТипСообщения);
	Контекст.Вставить("СообщениеОснование", СообщениеОснование);
	
	Возврат НЕ ПризнакОшибки;
	
КонецФункции

Процедура СформироватьПакетФНС_ПолучитьИПроверитьСертификатыПолучателя(КонтекстОтправки, СообщениеОбОшибке)

	// Внимание!
	// ---------
	// В Контейнере есть одноименная процедура, которая используется при отправке отчетов
	// в интерактивном режиме пользователем.
	// При изменениях необходимо синхронно менять обе процедуры.
	
	ЭтоЭлектроннаяПодписьВМоделиСервиса = ЭлектроннаяПодписьВМоделиСервисаБРОВызовСервера.РеквизитыУчетнойЗаписи(КонтекстОтправки.УчетнаяЗапись).ЭлектроннаяПодписьВМоделиСервиса;
	
	Если ТипЗнч(КонтекстОтправки.СертификатПолучателя) <> Тип("Массив") Тогда
		Отпечатки = Новый Массив;
		Отпечатки.Добавить(КонтекстОтправки.СертификатПолучателя);
	Иначе
		Отпечатки = КонтекстОтправки.СертификатПолучателя;
	КонецЕсли;
	
	Если ЗначениеЗаполнено(КонтекстОтправки.СертификатСервераДокументооборота)
		И Отпечатки.Найти(КонтекстОтправки.СертификатСервераДокументооборота) = Неопределено Тогда
		
		Отпечатки = ОбщегоНазначения.СкопироватьРекурсивно(Отпечатки);
		Отпечатки.Добавить(КонтекстОтправки.СертификатСервераДокументооборота);
	КонецЕсли;
	
	СертификатыДляПоиска = Новый Массив;
	Для Каждого Отпечаток Из Отпечатки Цикл
		СертификатыДляПоиска.Добавить(Новый Структура("ЭтоЭлектроннаяПодписьВМоделиСервиса, Отпечаток", ЭтоЭлектроннаяПодписьВМоделиСервиса, Отпечаток));
	КонецЦикла;
	
	РезультатПоиска = НайтиСертификаты(СертификатыДляПоиска, Истина);
	
	СформироватьПакетФНС_ПолучитьИПроверитьСертификатыПолучателяПослеПолученияСертификатов(КонтекстОтправки, СообщениеОбОшибке, РезультатПоиска)
	
КонецПроцедуры

Процедура СформироватьПакетФНС_ПолучитьИПроверитьСертификатыПолучателяПослеПолученияСертификатов(КонтекстОтправки, СообщениеОбОшибке, РезультатПоиска)

	// Внимание!
	// ---------
	// В Контейнере есть одноименная процедура, которая используется при отправке отчетов
	// в интерактивном режиме пользователем.
	// При изменениях необходимо синхронно менять обе процедуры.

	НайденныеСертификаты 				= ?(РезультатПоиска.Свойство("Сертификаты"), РезультатПоиска.Сертификаты, Неопределено);
	НенайденныеСертификаты 				= ?(РезультатПоиска.Свойство("НенайденныеСертификаты"), РезультатПоиска.НенайденныеСертификаты,
											Неопределено);
	СсылкаПолучатель 					= КонтекстОтправки.СсылкаПолучатель;
	СертификатПолучателя 				= КонтекстОтправки.СертификатПолучателя;
	СертификатСервераДокументооборота 	= КонтекстОтправки.СертификатСервераДокументооборота;
	СсылкаСерверДокументооборота 		= КонтекстОтправки.СсылкаСерверДокументооборота;
	
	ИнформацияОПолучателяхИСертификатах = Новый Структура;
	ИнформацияОПолучателяхИСертификатах.Вставить("СертификатСервераДокументооборота", 	СертификатСервераДокументооборота);
	ИнформацияОПолучателяхИСертификатах.Вставить("СсылкаСерверДокументооборота", 		СсылкаСерверДокументооборота);
	ИнформацияОПолучателяхИСертификатах.Вставить("СертификатПолучателя", 				СертификатПолучателя);
	РезультатПроверки = СформироватьПакетФНС_ПолучитьИПроверитьСертификатыПолучателяПослеПолученияСертификатовРазобратьНайденное(
		РезультатПоиска.Выполнено,
		НайденныеСертификаты,
		НенайденныеСертификаты,
		СсылкаПолучатель,
		ИнформацияОПолучателяхИСертификатах);
	Если РезультатПроверки = Неопределено Тогда
		СообщениеОбОшибке = ТекстСообщенийПользователю();
		Возврат;
	КонецЕсли;
	
	КонтекстОтправки.Вставить("ОбъектСертификатПолучателя", РезультатПроверки.НайденныеСертификатыПолучателя);
	КонтекстОтправки.Вставить("ОбъектСертификатСервераДокументооборота",
		РезультатПроверки.НайденныйСертификатСервераДокументооборота);
	
КонецПроцедуры

Функция СформироватьПакетФНС_ПолучитьИПроверитьСертификатыПолучателяПослеПолученияСертификатовРазобратьНайденное(
		Выполнено,
		НайденныеСертификаты,
		НенайденныеСертификаты,
		СсылкаПолучатель,
		ИнформацияОПолучателяхИСертификатах = Неопределено) Экспорт
	
	Если ИнформацияОПолучателяхИСертификатах = Неопределено Тогда
		СертификатСервераДокументооборота 	= Неопределено;
		СсылкаСерверДокументооборота 		= Неопределено;
		СертификатПолучателя 				= Неопределено;
	Иначе
		СертификатСервераДокументооборота 	= СокрЛП(ИнформацияОПолучателяхИСертификатах.СертификатСервераДокументооборота);
		СсылкаСерверДокументооборота 		= ИнформацияОПолучателяхИСертификатах.СсылкаСерверДокументооборота;
		СертификатПолучателя 				= ИнформацияОПолучателяхИСертификатах.СертификатПолучателя;
	КонецЕсли;
	
	Если Не Выполнено Тогда
		ТекстСообщения = СтрШаблон(
			НСтр("ru = 'Не удалось определить цифровой сертификат получателя
                  |%1
                  |для шифрования сообщения!'"), СсылкаПолучатель);
		ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
		Возврат Неопределено;
	КонецЕсли;
	
	Если ЗначениеЗаполнено(СертификатСервераДокументооборота) Тогда
		Для каждого НенайденныйСертификат Из НенайденныеСертификаты Цикл
			Если СокрЛП(НенайденныйСертификат.Отпечаток) = СертификатСервераДокументооборота Тогда
				ТекстСообщения = СтрШаблон(
					НСтр("ru = 'Не удалось определить цифровой сертификат сервера документооборота %1.'"),
					СсылкаСерверДокументооборота);
				ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
				Возврат Неопределено;
			КонецЕсли;
		КонецЦикла;
	КонецЕсли;
	
	Если НенайденныеСертификаты.Количество() > 0 Тогда
		ТекстСообщения = СтрШаблон(
			НСтр("ru = 'Не удалось определить цифровой сертификат получателя
                  |%1
                  |для шифрования сообщения!'"), СсылкаПолучатель);
		ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
		Возврат Неопределено;
	КонецЕсли;
	
	ИндексСертификатаСервераДокументооборота = -1;
	Если ТипЗнч(СсылкаПолучатель) = Тип("СправочникСсылка.НалоговыеОрганы") Тогда
		
		Для ИндексНайденногоСертификата = 0 По НайденныеСертификаты.Количество() - 1 Цикл
			ОбъектНайденныйСертификат = НайденныеСертификаты[ИндексНайденногоСертификата];
			
			Если ЗначениеЗаполнено(СертификатСервераДокументооборота)
				И СокрЛП(ОбъектНайденныйСертификат.Отпечаток) = СертификатСервераДокументооборота Тогда
				
				Если НЕ ОбъектНайденныйСертификат.Валиден Тогда
					ТекстСообщения = СтрШаблон(
						НСтр("ru = 'Ошибка проверки сертификата сервера документооборота %1.'"),
						СсылкаСерверДокументооборота);
					ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
					Возврат Неопределено;
				КонецЕсли;
				ИндексСертификатаСервераДокументооборота = ИндексНайденногоСертификата;
				
			Иначе
				РезультатПроверкиСертификатаНалоговогоОргана = ПроверитьСертификатПоПериоду(ОбъектНайденныйСертификат);
				Если РезультатПроверкиСертификатаНалоговогоОргана <> Истина Тогда
					ТекстСообщения = СтрШаблон(
						НСтр("ru = 'Ошибка проверки сертификата налогового органа %1 с отпечатком %2.'"), 
						СсылкаПолучатель,
						ОбъектНайденныйСертификат.Отпечаток);
					ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
					Возврат Неопределено;
				КонецЕсли;
			КонецЕсли;
		КонецЦикла;
		
		Если ЗначениеЗаполнено(СертификатСервераДокументооборота) И ИндексСертификатаСервераДокументооборота = -1 Тогда
			ТекстСообщения = СтрШаблон(
				НСтр("ru = 'В свойствах сервера документооборота %1 не задан сертификат.'"),
				СсылкаСерверДокументооборота);
			ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
			Возврат Неопределено;
		КонецЕсли;
		
		// Проверяем на наличие хотя бы одного сертификата.
		Если НайденныеСертификаты.Количество() <= ?(ЗначениеЗаполнено(СертификатСервераДокументооборота)
			И СертификатПолучателя <> СертификатСервераДокументооборота И (ТипЗнч(СертификатПолучателя) <> Тип("Массив")
			ИЛИ СертификатПолучателя.Найти(СертификатСервераДокументооборота) = Неопределено), 1, 0) Тогда
			
			ТекстСообщения = СтрШаблон(
				НСтр("ru = 'В свойствах налогового органа %1 не задан ни один сертификат.'"), СсылкаПолучатель);
			ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
			Возврат Неопределено;
		КонецЕсли;
		
	ИначеЕсли ТипЗнч(СсылкаПолучатель) = Тип("СправочникСсылка.СерверыДокументооборота") Тогда
		
		// Проверяем сертификат.
		Если Не НайденныеСертификаты[0].Валиден Тогда
			ТекстСообщения = СтрШаблон(
				НСтр("ru = 'Ошибка проверки сертификата сервера документооборота %1.'"), СсылкаПолучатель);
			ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
			Возврат Неопределено;
		КонецЕсли;		
	КонецЕсли;
	
	Результат = Новый Структура;
	Результат.Вставить("НайденныеСертификатыПолучателя", НайденныеСертификаты);
	Результат.Вставить("НайденныйСертификатСервераДокументооборота", Неопределено);
	
	Если ИндексСертификатаСервераДокументооборота <> -1 Тогда
		Если СертификатПолучателя <> СертификатСервераДокументооборота И (ТипЗнч(СертификатПолучателя) <> Тип("Массив")
			ИЛИ СертификатПолучателя.Найти(СертификатСервераДокументооборота) = Неопределено) Тогда
			
			Результат.НайденныеСертификатыПолучателя = ОбщегоНазначения.СкопироватьРекурсивно(НайденныеСертификаты);
			Результат.НайденныеСертификатыПолучателя.Удалить(ИндексСертификатаСервераДокументооборота);
		КонецЕсли;
		
		Результат.НайденныйСертификатСервераДокументооборота = НайденныеСертификаты[ИндексСертификатаСервераДокументооборота];
	КонецЕсли;
	
	Возврат Результат;
	
КонецФункции

Процедура СформироватьПакетФНСПослеПроверкиИПолученияСертификатовПолучателя(КонтекстОтправки, СообщениеОбОшибке)

	// Внимание!
	// ---------
	// В Контейнере есть одноименная процедура, которая используется при отправке отчетов
	// в интерактивном режиме пользователем.
	// При изменениях необходимо синхронно менять обе процедуры.
	
	Если НЕ СформироватьПакетФНС_ПроверкиНаСервере(КонтекстОтправки) Тогда // универсальная
		СообщениеОбОшибке = ТекстСообщенийПользователю();
		Возврат;
	КонецЕсли;
	
	СформироватьПакетФНС_ПроверкиСертификатов(КонтекстОтправки, СообщениеОбОшибке);
	
КонецПроцедуры

Функция СформироватьПакетФНС_ПроверкиНаСервере(Контекст) Экспорт
	
	УчетнаяЗапись = Контекст.УчетнаяЗапись;
	СсылкаПолучатель = Контекст.СсылкаПолучатель;
	ЦиклОбмена = Контекст.ЦиклОбмена;
	
	// получаем и проверяем код отправителя
	КодОтправителя = СокрЛП(УчетнаяЗапись.ИдентификаторАбонента);
	Если ПустаяСтрока(КодОтправителя) Тогда
		ТекстСообщения = НСтр("ru = 'Не указан идентификатор абонента в свойствах учетной записи.'");
		ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
		Возврат Ложь;
	КонецЕсли;
	
	// получаем код оператора электронного документооборота
	Если УчетнаяЗапись.ОбменНапрямую Тогда
		КодСпецоператора = Неопределено;
	Иначе
		КодСпецоператора = СокрЛП(УчетнаяЗапись.ИдентификаторСпецоператора);
		Если ПустаяСтрока(КодСпецоператора) Тогда
			ТекстСообщения = НСтр("ru = 'Не указан идентификатор оператор электронного документооборота в свойствах учетной записи.'");
			ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
			Возврат Ложь;
		КонецЕсли;
	КонецЕсли;
	
	// получаем и проверяем код получателя
	Если Контекст.ТипСообщения = Перечисления.ТипыТранспортныхСообщений.ИзвещениеПодтверждениеНП
		ИЛИ Контекст.ТипСообщения = Перечисления.ТипыТранспортныхСообщений.ЗапросНаВыпискуЕРГЮЛ_ЕГРИП Тогда
		Если УчетнаяЗапись.ОбменНапрямую Тогда
			КодПолучателя = ПолучитьИдентификаторОтправителяПодтвержденияДатыОтправки(Контекст.ТранспортноеСообщение.Основание);
			Если ПустаяСтрока(КодПолучателя) Тогда
				ТекстСообщения = НСтр("ru = 'При создании извещения на подтверждение даты отправки не удалось определить отправителя исходного подтверждения.'");
				ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
				Возврат Ложь;
			КонецЕсли;
			ТипПолучателя = "налоговыйОрган";
		Иначе
			КодПолучателя = СокрЛП(УчетнаяЗапись.ИдентификаторСпецоператора);
			Если ПустаяСтрока(КодПолучателя) Тогда
				ТекстСообщения = НСтр("ru = 'Не указан идентификатор оператора электронного документооборота в свойствах учетной записи.'");
				ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
				Возврат Ложь;
			КонецЕсли;
			ТипПолучателя = "спецоператор";
		КонецЕсли;
	Иначе
		КодПолучателя = СокрЛП(СсылкаПолучатель.Код);
		Если ПустаяСтрока(КодПолучателя) Тогда
			ТекстСообщения = НСтр("ru = 'Не задан код налогового органа.'");
			ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
			Возврат Ложь;
		КонецЕсли;
		ТипПолучателя = "налоговыйОрган";
	КонецЕсли;
	
	//
	UUIDПакета = СокрЛП(ЦиклОбмена.Идентификатор);
	
	Контекст.Вставить("КодОтправителя", КодОтправителя);
	Контекст.Вставить("КодСпецоператора", КодСпецоператора);
	Контекст.Вставить("КодПолучателя", КодПолучателя);
	Контекст.Вставить("ТипПолучателя", ТипПолучателя);
	Контекст.Вставить("UUIDПакета", UUIDПакета);
	
	Возврат Истина;
	
КонецФункции

Процедура СформироватьПакетФНСПослеПроверкиСертификатов(КонтекстОтправки, СообщениеОбОшибке)

	// Внимание!
	// ---------
	// В Контейнере есть одноименная процедура, которая используется при отправке отчетов
	// в интерактивном режиме пользователем.
	// При изменениях необходимо синхронно менять обе процедуры.
	
	Если КонтекстОтправки.ТипСообщения = Перечисления.ТипыТранспортныхСообщений.ДекларацияНП
		ИЛИ КонтекстОтправки.ТипСообщения = Перечисления.ТипыТранспортныхСообщений.ЗаявлениеНП
		ИЛИ КонтекстОтправки.ТипСообщения = Перечисления.ТипыТранспортныхСообщений.Форма2НДФЛНП
		ИЛИ КонтекстОтправки.ТипСообщения = Перечисления.ТипыТранспортныхСообщений.ОбращениеНП
		ИЛИ КонтекстОтправки.ТипСообщения = Перечисления.ТипыТранспортныхСообщений.ПредставлениеНП
		ИЛИ КонтекстОтправки.ТипСообщения = Перечисления.ТипыТранспортныхСообщений.ЗапросНП Тогда
		РезультатФормирования = СформироватьОписаниеСведенийДляСообщенияФНС(КонтекстОтправки); // сервер
	Иначе	
		Если НЕ КонтекстОтправки.ТипСообщения = Перечисления.ТипыТранспортныхСообщений.РезультатПриемаДокументНП Тогда
			РезультатФормирования = СформироватьИзвещениеОПолученииДляСообщенияФНС(КонтекстОтправки); // сервер
			
			Если Не РезультатФормирования Тогда
				СообщениеОбОшибке = ТекстСообщенийПользователю();
				Возврат;
			КонецЕсли;

		КонецЕсли;
	КонецЕсли;
	
КонецПроцедуры

Функция СертификатыДляПодписанияПоТипуСообщения(ТипСообщения, ОбъектСертификатРуководителя, ОбъектСертификатГлавногоБухгалтера, ОбъектСертификатОтветственногоЗаШифрование)

	// формируем массив объектов-сертификатов, которыми будем подписывать
	МассивОбъектовСертификатов = Новый Массив;
	Если ТипСообщения = Перечисления.ТипыТранспортныхСообщений.ДекларацияНП
		ИЛИ ТипСообщения = Перечисления.ТипыТранспортныхСообщений.ЗаявлениеНП
		ИЛИ ТипСообщения = Перечисления.ТипыТранспортныхСообщений.Форма2НДФЛНП
		ИЛИ ТипСообщения = Перечисления.ТипыТранспортныхСообщений.ОбращениеНП
		ИЛИ ТипСообщения = Перечисления.ТипыТранспортныхСообщений.ПредставлениеНП
		ИЛИ ТипСообщения = Перечисления.ТипыТранспортныхСообщений.ЗапросНП
		ИЛИ ТипСообщения = Перечисления.ТипыТранспортныхСообщений.ЗапросНаВыпискуЕРГЮЛ_ЕГРИП Тогда
		МассивОбъектовСертификатов.Добавить(ОбъектСертификатРуководителя);
		Если ЗначениеЗаполнено(ОбъектСертификатГлавногоБухгалтера)
			И ОбъектСертификатГлавногоБухгалтера.Отпечаток <> ОбъектСертификатРуководителя.Отпечаток Тогда
			МассивОбъектовСертификатов.Добавить(ОбъектСертификатГлавногоБухгалтера);
		КонецЕсли;
	Иначе
		МассивОбъектовСертификатов.Добавить(ОбъектСертификатОтветственногоЗаШифрование);
	КонецЕсли;

	Возврат МассивОбъектовСертификатов;

КонецФункции

Процедура СформироватьПакетФНС_Подписание(КонтекстОтправки, СообщениеОбОшибке)

	// Внимание!
	// ---------
	// В Контейнере есть одноименная процедура, которая используется при отправке отчетов
	// в интерактивном режиме пользователем.
	// При изменениях необходимо синхронно менять обе процедуры.
	
	// формируем массив объектов-сертификатов, которыми будем подписывать
	МассивОбъектовСертификатов = СертификатыДляПодписанияПоТипуСообщения(
		 КонтекстОтправки.ТипСообщения,
		 КонтекстОтправки.ОбъектСертификатРуководителя,
		 КонтекстОтправки.ОбъектСертификатГлавногоБухгалтера,
		 КонтекстОтправки.ОбъектСертификатОтветственногоЗаШифрование);
		 
	// генерируем подписи руководителя
	ПодписатьСообщение("ФНС",
		КонтекстОтправки,
		КонтекстОтправки.ТранспортноеСообщение,
		КонтекстОтправки.ТипТранспортногоСообщения, 
		МассивОбъектовСертификатов,
		КонтекстОтправки.УчетнаяЗапись,
		КонтекстОтправки.СообщениеОснование,
		СообщениеОбОшибке);
	
КонецПроцедуры

Процедура СформироватьПакетФНСПослеПодписания(КонтекстОтправки, СообщениеОбОшибке)

	// Внимание!
	// ---------
	// В Контейнере есть одноименная процедура, которая используется при отправке отчетов
	// в интерактивном режиме пользователем.
	// При изменениях необходимо синхронно менять обе процедуры.
	
	// Формируем дерево описания документов пакета.
	Если НЕ СформироватьПакетФНС_СформироватьДеревоОписанияДокументовПакета(КонтекстОтправки) Тогда // универсальная
		СообщениеОбОшибке = ТекстСообщенийПользователю();
		Возврат;
	КонецЕсли;
	
	// Отрабатываем отложенные команды (шифрование).
	СформироватьПакет_ОтработатьОтложенныеКоманды(КонтекстОтправки, СообщениеОбОшибке);
	Если ЗначениеЗаполнено(СообщениеОбОшибке) Тогда
		Возврат;
	КонецЕсли;
	
	СформироватьПакетФНСПослеВыполненияОтложенныхКоманд(КонтекстОтправки, СообщениеОбОшибке);
	
КонецПроцедуры

Функция СформироватьПакетФНС_СформироватьДеревоОписанияДокументовПакета(Контекст) Экспорт
	
	// определяем структуру с отложенными командами
	ОтложенныеКоманды = Новый СписокЗначений;
	СвойстваОтложенныхКоманд = Новый Соответствие;
	
	// создаем вспомогательное дерево описания документов пакета
	ДеревоДокументов = СоздатьДеревоОписанияДокументовПакета();
	
	// выгружаем все файлы сообщения во временный каталог
	КаталогФайловОтчетности = ОперацииСФайламиЭДКО.СоздатьВременныйКаталог();
	
	// выгружаем все содержимое во временное хранилище
	ТипыСодержимого = Новый Массив;
	ТипыШифруемогоСодержимого = Новый Массив;
	
	Если Контекст.ТипСообщения = Перечисления.ТипыТранспортныхСообщений.ДекларацияНП Тогда
		
		ТипыСодержимого.Добавить(Перечисления.ТипыСодержимогоТранспортногоКонтейнера.ФайлОтчетности);
		ТипыСодержимого.Добавить(Перечисления.ТипыСодержимогоТранспортногоКонтейнера.Форма2НДФЛ);
		ТипыСодержимого.Добавить(Перечисления.ТипыСодержимогоТранспортногоКонтейнера.Приложение);
		ТипыСодержимого.Добавить(Перечисления.ТипыСодержимогоТранспортногоКонтейнера.ОписаниеСведений);
		ТипыСодержимого.Добавить(Перечисления.ТипыСодержимогоТранспортногоКонтейнера.ОписаниеСведений2НДФЛ);
		ТипыСодержимого.Добавить(Перечисления.ТипыСодержимогоТранспортногоКонтейнера.ДоверенностьПредставление);
		
		ТипыШифруемогоСодержимого.Добавить(Перечисления.ТипыСодержимогоТранспортногоКонтейнера.ФайлОтчетности);
		ТипыШифруемогоСодержимого.Добавить(Перечисления.ТипыСодержимогоТранспортногоКонтейнера.Форма2НДФЛ);
		ТипыШифруемогоСодержимого.Добавить(Перечисления.ТипыСодержимогоТранспортногоКонтейнера.Приложение);
		ТипыШифруемогоСодержимого.Добавить(Перечисления.ТипыСодержимогоТранспортногоКонтейнера.ДоверенностьПредставление);
		
	ИначеЕсли Контекст.ТипСообщения = Перечисления.ТипыТранспортныхСообщений.Форма2НДФЛНП Тогда
		
		ТипыСодержимого.Добавить(Перечисления.ТипыСодержимогоТранспортногоКонтейнера.Форма2НДФЛ);
		ТипыСодержимого.Добавить(Перечисления.ТипыСодержимогоТранспортногоКонтейнера.ОписаниеСведений2НДФЛ);
		ТипыСодержимого.Добавить(Перечисления.ТипыСодержимогоТранспортногоКонтейнера.ДоверенностьПредставление);
		
		ТипыШифруемогоСодержимого.Добавить(Перечисления.ТипыСодержимогоТранспортногоКонтейнера.Форма2НДФЛ);
		ТипыШифруемогоСодержимого.Добавить(Перечисления.ТипыСодержимогоТранспортногоКонтейнера.ДоверенностьПредставление);
		
	ИначеЕсли Контекст.ТипСообщения = Перечисления.ТипыТранспортныхСообщений.ИзвещениеПодтверждениеНП Тогда
		
		ТипыСодержимого.Добавить(Перечисления.ТипыСодержимогоТранспортногоКонтейнера.ИзвещениеОПолучении);
		
	ИначеЕсли Контекст.ТипСообщения = Перечисления.ТипыТранспортныхСообщений.ИзвещениеРезультатПриемаНП Тогда
		
		ТипыСодержимого.Добавить(Перечисления.ТипыСодержимогоТранспортногоКонтейнера.ИзвещениеОПолучении);
		
	ИначеЕсли Контекст.ТипСообщения = Перечисления.ТипыТранспортныхСообщений.ИзвещениеРезультатОбработкиНП Тогда
		
		ТипыСодержимого.Добавить(Перечисления.ТипыСодержимогоТранспортногоКонтейнера.ИзвещениеОПолучении);
		
	ИначеЕсли Контекст.ТипСообщения = Перечисления.ТипыТранспортныхСообщений.ОбращениеНП Тогда
		
		ТипыСодержимого.Добавить(Перечисления.ТипыСодержимогоТранспортногоКонтейнера.Обращение);
		ТипыСодержимого.Добавить(Перечисления.ТипыСодержимогоТранспортногоКонтейнера.Приложение);
		ТипыСодержимого.Добавить(Перечисления.ТипыСодержимогоТранспортногоКонтейнера.ОписаниеСведенийОбращениеПисьмоРассылка);
		ТипыСодержимого.Добавить(Перечисления.ТипыСодержимогоТранспортногоКонтейнера.ДоверенностьПредставление);
		
		ТипыШифруемогоСодержимого.Добавить(Перечисления.ТипыСодержимогоТранспортногоКонтейнера.Обращение);
		ТипыШифруемогоСодержимого.Добавить(Перечисления.ТипыСодержимогоТранспортногоКонтейнера.Приложение);
		ТипыШифруемогоСодержимого.Добавить(Перечисления.ТипыСодержимогоТранспортногоКонтейнера.ДоверенностьПредставление);
		
	ИначеЕсли Контекст.ТипСообщения = Перечисления.ТипыТранспортныхСообщений.ПредставлениеНП Тогда
		
		ТипыСодержимого.Добавить(Перечисления.ТипыСодержимогоТранспортногоКонтейнера.Представление);
		ТипыСодержимого.Добавить(Перечисления.ТипыСодержимогоТранспортногоКонтейнера.Приложение);
		ТипыСодержимого.Добавить(Перечисления.ТипыСодержимогоТранспортногоКонтейнера.ОписаниеПредставление);
		ТипыСодержимого.Добавить(Перечисления.ТипыСодержимогоТранспортногоКонтейнера.ДоверенностьПредставление);
		
		ТипыШифруемогоСодержимого.Добавить(Перечисления.ТипыСодержимогоТранспортногоКонтейнера.Представление);
		ТипыШифруемогоСодержимого.Добавить(Перечисления.ТипыСодержимогоТранспортногоКонтейнера.Приложение);
		ТипыШифруемогоСодержимого.Добавить(Перечисления.ТипыСодержимогоТранспортногоКонтейнера.ДоверенностьПредставление);
		
	ИначеЕсли Контекст.ТипСообщения = Перечисления.ТипыТранспортныхСообщений.ЗапросНП Тогда
		
		ТипыСодержимого.Добавить(Перечисления.ТипыСодержимогоТранспортногоКонтейнера.Запрос);
		ТипыСодержимого.Добавить(Перечисления.ТипыСодержимогоТранспортногоКонтейнера.ОписаниеСведенийЗапрос);
		ТипыСодержимого.Добавить(Перечисления.ТипыСодержимогоТранспортногоКонтейнера.ДоверенностьПредставление);
		
		ТипыШифруемогоСодержимого.Добавить(Перечисления.ТипыСодержимогоТранспортногоКонтейнера.Запрос);
		ТипыШифруемогоСодержимого.Добавить(Перечисления.ТипыСодержимогоТранспортногоКонтейнера.ДоверенностьПредставление);
		
	ИначеЕсли Контекст.ТипСообщения = Перечисления.ТипыТранспортныхСообщений.ИзвещениеПисьмоНП Тогда
		
		ТипыСодержимого.Добавить(Перечисления.ТипыСодержимогоТранспортногоКонтейнера.ИзвещениеОПолучении);
		
	ИначеЕсли Контекст.ТипСообщения = Перечисления.ТипыТранспортныхСообщений.ИзвещениеРассылкаНП Тогда
		
		ТипыСодержимого.Добавить(Перечисления.ТипыСодержимогоТранспортногоКонтейнера.ИзвещениеОПолучении);
		
	ИначеЕсли Контекст.ТипСообщения = Перечисления.ТипыТранспортныхСообщений.ЗаявлениеНП Тогда
		
		ТипыСодержимого.Добавить(Перечисления.ТипыСодержимогоТранспортногоКонтейнера.Заявление);
		ТипыСодержимого.Добавить(Перечисления.ТипыСодержимогоТранспортногоКонтейнера.ОписаниеСведенийЗаявление);
		ТипыСодержимого.Добавить(Перечисления.ТипыСодержимогоТранспортногоКонтейнера.ДоверенностьПредставление);
		
		ТипыШифруемогоСодержимого.Добавить(Перечисления.ТипыСодержимогоТранспортногоКонтейнера.Заявление);
		ТипыШифруемогоСодержимого.Добавить(Перечисления.ТипыСодержимогоТранспортногоКонтейнера.ДоверенностьПредставление);
		
	ИначеЕсли Контекст.ТипСообщения = Перечисления.ТипыТранспортныхСообщений.ИзвещениеРезультатОбработкиРФНП Тогда
		
		ТипыСодержимого.Добавить(Перечисления.ТипыСодержимогоТранспортногоКонтейнера.ИзвещениеОПолучении);
		
	ИначеЕсли Контекст.ТипСообщения = Перечисления.ТипыТранспортныхСообщений.ИзвещениеОбОтзывеЗаявлениеРФНП Тогда
		
		ТипыСодержимого.Добавить(Перечисления.ТипыСодержимогоТранспортногоКонтейнера.ИзвещениеОПолучении);
		
	ИначеЕсли Контекст.ТипСообщения = Перечисления.ТипыТранспортныхСообщений.ИзвещениеРезультатОбработкиТСНП Тогда
		
		ТипыСодержимого.Добавить(Перечисления.ТипыСодержимогоТранспортногоКонтейнера.ИзвещениеОПолучении);
		
	ИначеЕсли Контекст.ТипСообщения = Перечисления.ТипыТранспортныхСообщений.ИзвещениеДокументНП Тогда
		
		ТипыСодержимого.Добавить(Перечисления.ТипыСодержимогоТранспортногоКонтейнера.ИзвещениеОПолучении);
		
	ИначеЕсли Контекст.ТипСообщения = Перечисления.ТипыТранспортныхСообщений.РезультатПриемаДокументНП Тогда
		
		ТипыСодержимого.Добавить(Перечисления.ТипыСодержимогоТранспортногоКонтейнера.КвитанцияОПриемеДокумент);
		ТипыСодержимого.Добавить(Перечисления.ТипыСодержимогоТранспортногоКонтейнера.УведомлениеОбОтказеДокумент);
		
		ТипыШифруемогоСодержимого.Добавить(Перечисления.ТипыСодержимогоТранспортногоКонтейнера.КвитанцияОПриемеДокумент);
		ТипыШифруемогоСодержимого.Добавить(Перечисления.ТипыСодержимогоТранспортногоКонтейнера.УведомлениеОбОтказеДокумент);
	ИначеЕсли Контекст.ТипСообщения = Перечисления.ТипыТранспортныхСообщений.ЗапросНаВыпискуЕРГЮЛ_ЕГРИП Тогда
		ТипыСодержимого.Добавить(Перечисления.ТипыСодержимогоТранспортногоКонтейнера.ЗапросНаВыпискуИзЕГРЮЛ_ЕГРИП);
	КонецЕсли;
	
	Если НЕ Контекст.ТипСообщения = Перечисления.ТипыТранспортныхСообщений.ЗапросНаВыпискуЕРГЮЛ_ЕГРИП Тогда
		ТипыСодержимого.Добавить(Перечисления.ТипыСодержимогоТранспортногоКонтейнера.ЭлектронноЦифроваяПодпись);
	КонецЕсли;
	
	ВложенияДИВ = ПолучитьВложенияТранспортногоСообщения(Контекст.ТранспортноеСообщение, Истина, ТипыСодержимого);
	ВложенияДИВ.Колонки.Добавить("Адрес");
	Для Каждого ВложениеДИВ Из ВложенияДИВ Цикл
		Попытка
			ВложениеДИВ.Адрес = ПоместитьВоВременноеХранилище(ВложениеДИВ.Данные.Получить(), Контекст.УникальныйИдентификатор);
		Исключение
			ОперацииСФайламиЭДКО.УдалитьВременныйФайл(КаталогФайловОтчетности);
			ТекстСообщения = НСтр("ru = 'Ошибка формирования пакета: не удалось сохранить документ во временный каталог для подписания.'");
			ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
			Возврат Ложь;
		КонецПопытки;
	КонецЦикла;
	
	// заполняем дерево описания сначала ДИВ
	Для Каждого ВложениеДИВ Из ВложенияДИВ Цикл
		
		Если ВложениеДИВ.Тип = Перечисления.ТипыСодержимогоТранспортногоКонтейнера.ЭлектронноЦифроваяПодпись Тогда
			Продолжить;
		КонецЕсли;
		
		// пакуем файл
		КороткоеИмяФайлаАрхива = ЗаархивироватьФайлПакета(ВложениеДИВ, "file", Контекст.УникальныйИдентификатор);
		Если КороткоеИмяФайлаАрхива = Неопределено Тогда
			ОперацииСФайламиЭДКО.УдалитьВременныйФайл(КаталогФайловОтчетности);
			Возврат Ложь;
		КонецЕсли;
		
		Если ТипыШифруемогоСодержимого.Найти(ВложениеДИВ.Тип) <> Неопределено Тогда
			ОтложеннаяКоманда = ОтложенныеКоманды.Добавить(ВложениеДИВ.Адрес, "Зашифровать");
			СтрДокумент = ОпределитьДокументВДеревеОписания(ДеревоДокументов, ВложениеДИВ.Тип, ТипСодержимогоПоОписаниюДИВ(ВложениеДИВ), ВложениеДИВ.Идентификатор, Истина, Истина, ВложениеДИВ.ИмяФайла, КороткоеИмяФайлаАрхива, ВложениеДИВ.Адрес);
			
			ПрефиксИмениФайла = СокрЛП(ВложениеДИВ.ИмяФайла);
			ПрефиксИмениФайла = Лев(ПрефиксИмениФайла, 7);
			Если ВРег(ПрефиксИмениФайла) = "NO_NDS_" Тогда
				ИдентификаторОтложеннойКоманды = ОтложеннаяКоманда.ПолучитьИдентификатор();
				СвойстваОтложенныхКоманд[ИдентификаторОтложеннойКоманды] = Новый Структура("ДляСервераДокументооборота", Истина);
			КонецЕсли;
			
		Иначе
			СтрДокумент = ОпределитьДокументВДеревеОписания(ДеревоДокументов, ВложениеДИВ.Тип, "xml", ВложениеДИВ.Идентификатор, Истина, Ложь, ВложениеДИВ.ИмяФайла, КороткоеИмяФайлаАрхива, ВложениеДИВ.Адрес);
		КонецЕсли;
		
	КонецЦикла;
	
	// дополняем дерево описания информацией об ЭЦП
	Для Каждого ВложениеДИВ Из ВложенияДИВ Цикл
		Если ВложениеДИВ.Тип = Перечисления.ТипыСодержимогоТранспортногоКонтейнера.ЭлектронноЦифроваяПодпись Тогда
			ПодписанныйДокумент = ДеревоДокументов.Строки.Найти(ВложениеДИВ.ЭЦПИмяПодписанногоФайла, "ИмяФайлаДокумента");
			Если ПодписанныйДокумент <> Неопределено Тогда
				ОпределитьДокументВДеревеОписания(ПодписанныйДокумент, , , , , , ВложениеДИВ.ИмяФайла, , ВложениеДИВ.Адрес);
			КонецЕсли;
		КонецЕсли;
	КонецЦикла;
	
	// 
	Контекст.Вставить("ОтложенныеКоманды", ОтложенныеКоманды);
	Контекст.Вставить("СвойстваОтложенныхКоманд", СвойстваОтложенныхКоманд);
	Контекст.Вставить("ДеревоДокументов", ПоместитьВоВременноеХранилище(ДеревоДокументов, Контекст.УникальныйИдентификатор));
	
	Возврат Истина;
	
КонецФункции

Процедура СформироватьПакетФНСПослеВыполненияОтложенныхКоманд(КонтекстОтправки, СообщениеОбОшибке)

	// Внимание!
	// ---------
	// В Контейнере есть аналогичная процедура СформироватьПакетФНСПослеВыполненияОтложенныхКомандНаКлиенте(),
	// которая используется при отправке отчетов в интерактивном режиме пользователем.
	// При изменениях необходимо синхронно менять обе процедуры.
	
	// Формируем файл описания пакета, создаем архив и помещаем в ИБ.
	Если НЕ СформироватьПакетФНС_ЗаключительныеДействияНаСервере(КонтекстОтправки) Тогда // универсальная
		СообщениеОбОшибке = ТекстСообщенийПользователю();
		Возврат;
	КонецЕсли;
	
КонецПроцедуры

Функция СформироватьПакетФНС_ЗаключительныеДействияНаСервере(Контекст) Экспорт
	
	ДеревоДокументов = ПолучитьИзВременногоХранилища(Контекст.ДеревоДокументов);
	
	//////////////////////////////////////////////////////////////////////////////
	// ФОРМИРОВАНИЕ ФАЙЛА ОПИСАНИЯ ПАКЕТА
	
	// формируем
	ФайлОписаниеПакета = СформироватьОписаниеПакетаФНС(ДеревоДокументов,
														Контекст.UUIDПакета,
														Контекст.КодТипаДокументооборота,
														Контекст.НаименованиеТипаДокументооборота,
														Контекст.КодТипаТранзакции,
														Контекст.НаименованиеТипаТранзакции,
														Контекст.КодОтправителя,
														Контекст.КодСпецоператора,
														Контекст.КодПолучателя,
														Контекст.ТипПолучателя,
														Контекст.УникальныйИдентификатор);
	Если ФайлОписаниеПакета = Неопределено Тогда
		Возврат Ложь;
	КонецЕсли;
	
	//////////////////////////////////////////////////////////////////////////////
	// СОЗДАНИЕ АРХИВА ПАКЕТА
	
	КаталогФайловОтчетности = ОперацииСФайламиЭДКО.СоздатьВременныйКаталог();
	
	КороткоеИмяФайлаАрхива = "FNS_" +
							Контекст.КодОтправителя + "_" +
							Контекст.КодПолучателя + "_" +
							СгенерироватьUUID() + "_" + 
							Контекст.КодТипаДокументооборота + "_" +
							Контекст.КодТипаТранзакции + "_" +
							Контекст.КодТипаОсновногоДокумента +
							".zip";
	ПолноеИмяФайлаАрхива = ПолучитьКаталогВременныхФайлов() + КороткоеИмяФайлаАрхива;
	
	ЗаписьZIP = Новый ЗаписьZipФайла(ПолноеИмяФайлаАрхива, , , МетодСжатияZIP.Копирование); // архив пакета создается без сжатия
	Для Каждого СтрДокумент Из ДеревоДокументов.Строки Цикл
		
		ПолноеИмяФайлаПакета = ВыгрузитьФайлПакетаИзВременногоХранилищаВКаталог(КаталогФайловОтчетности, СтрДокумент);
		ЗаписьZIP.Добавить(ПолноеИмяФайлаПакета);
		
		Для Каждого СтрДокумент2 Из СтрДокумент.Строки Цикл
			ПолноеИмяФайлаПакета = ВыгрузитьФайлПакетаИзВременногоХранилищаВКаталог(КаталогФайловОтчетности, СтрДокумент2);
			ЗаписьZIP.Добавить(ПолноеИмяФайлаПакета);
		КонецЦикла;
		
	КонецЦикла;
	
	// добавляем описание пакета
	ПолноеИмяФайлаОписанияПакета = КаталогФайловОтчетности + "packageDescription.xml";
	ПолучитьИзВременногоХранилища(ФайлОписаниеПакета).Записать(ПолноеИмяФайлаОписанияПакета);
	ЗаписьZIP.Добавить(ПолноеИмяФайлаОписанияПакета);
	
	ЗаписьZIP.Записать();
	
	//////////////////////////////////////////////////////////////////////////////
	// ДОБАВЛЕНИЕ ПАКЕТА В РЕГИСТР КОНТЕЙНЕРОВ
	
	ДобавитьТранспортныйКонтейнер(Контекст.ТранспортноеСообщение, ПолноеИмяФайлаАрхива, КороткоеИмяФайлаАрхива);
	
	ОперацииСФайламиЭДКО.УдалитьВременныйФайл(КаталогФайловОтчетности);
	ОперацииСФайламиЭДКО.УдалитьВременныйФайл(ПолноеИмяФайлаАрхива);
	
	Для каждого Строка Из ДеревоДокументов.Строки Цикл
		УдалитьИзВременногоХранилища(Строка.Адрес);
	КонецЦикла; 
	
	Возврат Истина;
	
КонецФункции

Функция СформироватьОписаниеПакетаФНС(ОписаниеДокументов, UUIDПакета = Неопределено, КодТипаДокументоооборота = "01", ТипДокументооборота = "Декларация", КодТипаТранзакции = "01", ТипТранзакции = "ДекларацияНП", КодОтправителя = "", КодСОС = Неопределено, КодПолучателя = "", ТипПолучателя = "налоговыйОрган", ИдентификаторДляВременногоХранилища = Неопределено) Экспорт
	
	Если UUIDПакета = Неопределено Тогда
		UUIDПакета = СгенерироватьUUID();
	КонецЕсли;
	
	// создаем дерево описания XML
	Дерево = СоздатьДеревоXML();
	
	// добавляем узел "ТрансИнф"
	УзелТрансИнф = ДобавитьУзелXML(Дерево, "ТрансИнф");
	ДобавитьУзелXML(УзелТрансИнф, "версияФормата",							Истина, "ФНС:1.0");
	ДобавитьУзелXML(УзелТрансИнф, "кодТипаДокументооборота",				Истина, КодТипаДокументоооборота);
	ДобавитьУзелXML(УзелТрансИнф, "типДокументооборота",					Истина, ТипДокументооборота);
	ДобавитьУзелXML(УзелТрансИнф, "кодТипаТранзакции",						Истина, КодТипаТранзакции);
	ДобавитьУзелXML(УзелТрансИнф, "типТранзакции",							Истина, ТипТранзакции);
	ДобавитьУзелXML(УзелТрансИнф, "идентификаторДокументооборота", 			Истина, UUIDПакета);
	ДобавитьУзелXML(УзелТрансИнф, "ВерсПрог", 								Истина, СокрЛП(Лев(РегламентированнаяОтчетность.НазваниеИВерсияПрограммы(), 40)));
	
	// добавляем узел "отправитель"
	УзелОтправитель = ДобавитьУзелXML(УзелТрансИнф, "отправитель");
	ДобавитьУзелXML(УзелОтправитель, "идентификаторСубъекта",				Истина, КодОтправителя);
	ДобавитьУзелXML(УзелОтправитель, "типСубъекта",							Истина, "абонент");
	
	// добавляем узел "спецоператор"
	Если КодСОС <> Неопределено Тогда
		УзелСпецоператор = ДобавитьУзелXML(УзелТрансИнф, "спецоператор");
		ДобавитьУзелXML(УзелСпецоператор, "идентификаторСубъекта",			Истина, КодСОС);
		ДобавитьУзелXML(УзелСпецоператор, "типСубъекта",					Истина, "спецоператор");
	КонецЕсли;
	
	// добавляем узел "получатель"
	УзелПолучатель = ДобавитьУзелXML(УзелТрансИнф, "получатель");
	ДобавитьУзелXML(УзелПолучатель, "идентификаторСубъекта",				Истина, КодПолучателя);
	ДобавитьУзелXML(УзелПолучатель, "типСубъекта",							Истина, ТипПолучателя);
	
	// добавляем узлы "Документ"
	Для Каждого СтрДокумент Из ОписаниеДокументов.Строки Цикл
		
		// добавляем очередной узел "Документ"
		УзелДокумент = ДобавитьУзелXML(УзелТрансИнф, "документ");
		ДобавитьУзелXML(УзелДокумент, "кодТипаДокумента",				Истина, КодПоТипуДокументаПакета(СтрДокумент.ТипДокумента, КодТипаДокументоооборота));
		ДобавитьУзелXML(УзелДокумент, "типДокумента",					Истина, ИдентификаторПоТипуДокументаПакета(СтрДокумент.ТипДокумента));
		ДобавитьУзелXML(УзелДокумент, "типСодержимого",					Истина, СтрДокумент.ТипСодержимого);
		ДобавитьУзелXML(УзелДокумент, "сжат",							Истина, XMLСтрока(СтрДокумент.Сжат = Истина));
		ДобавитьУзелXML(УзелДокумент, "зашифрован",						Истина, XMLСтрока(СтрДокумент.Зашифрован = Истина));
		ДобавитьУзелXML(УзелДокумент, "идентификаторДокумента",			Истина, СтрДокумент.ИдентификаторДокумента);
		ДобавитьУзелXML(УзелДокумент, "исходноеИмяФайла",				Истина, СтрДокумент.ИмяФайлаДокумента);
		
		// добавляем узел "Содержимое"
		Если ЗначениеЗаполнено(СтрДокумент.ИмяФайлаАрхива) Тогда
			УзелСодержимое = ДобавитьУзелXML(УзелДокумент, "содержимое");
			ДобавитьУзелXML(УзелСодержимое, "имяФайла",					Истина, СтрДокумент.ИмяФайлаАрхива);
		КонецЕсли;
		
		// добавляем подписи под документом
		Для Каждого СтрПодпись Из СтрДокумент.Строки Цикл
			
			// добавляем очередной узел "Подпись"
			УзелПодпись = ДобавитьУзелXML(УзелДокумент, "подпись");
			ДобавитьУзелXML(УзелПодпись, "имяФайла",					Истина, СтрПодпись.ИмяФайлаДокумента);
			ДобавитьУзелXML(УзелПодпись, "роль",						Истина, "абонент");
			
		КонецЦикла;
		
	КонецЦикла;
	
	// формируем строку XML на основе дерева
	СтрокаXML = ВыгрузитьДеревоВXML_ПФР(Дерево);
	
	// записываем дерево в файл
	ПолноеИмяФайла = ПолучитьИмяВременногоФайла();
	РезультатЗаписи = ЗаписатьXMLВФайл(СтрокаXML, ПолноеИмяФайла);
	Если РезультатЗаписи = Ложь Тогда
		Возврат Неопределено;
	КонецЕсли;
	
	// записываем двоичные данные файла во временное хранилище
	АдресВХранилище = ПоместитьВоВременноеХранилище(Новый ДвоичныеДанные(ПолноеИмяФайла), ИдентификаторДляВременногоХранилища);
	
	Возврат АдресВХранилище;
	
КонецФункции

#КонецОбласти 

#Область СформироватьПакетПФР

Процедура СформироватьПакетПФР(ТранспортноеСообщение, УникальныйИдентификатор, СообщениеОбОшибке) Экспорт

	// Внимание!
	// ---------
	// В Контейнере есть одноименная процедура, которая используется при отправке отчетов
	// в интерактивном режиме пользователем.
	// При изменениях необходимо синхронно менять обе процедуры.
	 
	КонтекстОтправки = Новый Структура;
	КонтекстОтправки.Вставить("ТранспортноеСообщение", ТранспортноеСообщение);
	КонтекстОтправки.Вставить("УникальныйИдентификатор", УникальныйИдентификатор);
	КонтекстОтправки.Вставить("Оповещать", Ложь);
	
	// общие проверки
	Если НЕ СформироватьПакетПФР_ОбщиеПроверкиПередФормированием(КонтекстОтправки) Тогда
		СообщениеОбОшибке = ТекстСообщенийПользователю();
		Возврат;
	КонецЕсли;
		
	// получение и проверки сертификатов
	СформироватьПакетПФР_ПолучитьИПроверитьСертификатыПолучателя(КонтекстОтправки, СообщениеОбОшибке);
	Если ЗначениеЗаполнено(СообщениеОбОшибке) Тогда
		Возврат;
	КонецЕсли;
	
	СформироватьПакетПФРПослеПроверкиИПолученияСертификатовПолучателя(КонтекстОтправки, СообщениеОбОшибке);
	Если ЗначениеЗаполнено(СообщениеОбОшибке) Тогда
		Возврат;
	КонецЕсли;
	
	СформироватьПакетПФРПослеПроверкиСертификатов(КонтекстОтправки, СообщениеОбОшибке);
	Если ЗначениеЗаполнено(СообщениеОбОшибке) Тогда
		Возврат;
	КонецЕсли;
	
	СформироватьПакетПФР_Подписание(КонтекстОтправки, СообщениеОбОшибке);
	Если ЗначениеЗаполнено(СообщениеОбОшибке) Тогда
		Возврат;
	КонецЕсли;
	
	СформироватьПакетПФРПослеПодписания(КонтекстОтправки, СообщениеОбОшибке);
	
КонецПроцедуры

Функция СформироватьПакетПФР_ОбщиеПроверкиПередФормированием(Контекст) Экспорт
	
	Сообщение = Контекст.ТранспортноеСообщение;
	
	// проверка на то, что сообщение исходящее
	СтатусТранспортногоСообщения = Сообщение.Статус;
	Если СтатусТранспортногоСообщения <> Перечисления.СтатусыПисем.Сохраненное
	И СтатусТранспортногоСообщения <> Перечисления.СтатусыПисем.Исходящее
	И СтатусТранспортногоСообщения <> Перечисления.СтатусыПисем.Отправленное Тогда
		ТекстСообщения = НСтр("ru = 'Невозможно формирование пакета по входящему сообщению!'");
		ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
		Возврат Ложь;
	КонецЕсли;
	
	ПризнакОшибки = Ложь;
	
	ТипСообщения = Сообщение.Тип;
	
	СсылкаОтправитель = Сообщение.Отправитель;
	СсылкаПолучатель = ОпределитьПолучателяТранспортногоКонтейнера(Сообщение);
	УчетнаяЗапись = Сообщение.УчетнаяЗапись;
	ЦиклОбмена = Сообщение.ЦиклОбмена;
	СообщениеОснование = Сообщение.Основание;
	
	Если НЕ ЗначениеЗаполнено(ТипСообщения) Тогда
		ТекстСообщения = НСтр("ru = 'Ошибка шифрования транспортного сообщения: тип транспортного сообщения не определен!'");
		ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
		ПризнакОшибки = Истина;
	КонецЕсли;
	
	Если НЕ ЗначениеЗаполнено(СсылкаОтправитель) Тогда
		ТекстСообщения = НСтр("ru = 'Ошибка шифрования транспортного сообщения: не задан отправитель!'");
		ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
		ПризнакОшибки = Истина;
	КонецЕсли;
	
	Если НЕ ЗначениеЗаполнено(СсылкаПолучатель) Тогда
		ТекстСообщения = НСтр("ru = 'Ошибка шифрования транспортного сообщения: не задан получатель!'");
		ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
		ПризнакОшибки = Истина;
	КонецЕсли;
	
	Предмет = Сообщение.ЦиклОбмена.Предмет;
	ПропуститьПроверку = 
		ТипЗнч(Предмет) = Тип("ДокументСсылка.ЗаявленияПоЭлДокументооборотуСПФР") 
		И Предмет.Вид = Перечисления.ВидыЗаявленийНаЭДОВПФР.НаОтключение;
	
	Если НЕ ЗначениеЗаполнено(УчетнаяЗапись) Тогда
		ТекстСообщения = НСтр("ru = 'Ошибка шифрования транспортного сообщения: не задана учетная запись!'");
		ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
		ПризнакОшибки = Истина;
	ИначеЕсли НЕ УчетнаяЗапись.ПредназначенаДляДокументооборотаСПФР И НЕ ПропуститьПроверку Тогда
		ТекстСообщения = НСтр("ru = 'Ошибка шифрования транспортного сообщения: учетная запись не предназначена для электронного документооборота с ПФР!'");
		ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
		ПризнакОшибки = Истина;
	КонецЕсли;
	
	Если НЕ ЗначениеЗаполнено(ЦиклОбмена) Тогда
		ТекстСообщения = НСтр("ru = 'Ошибка шифрования транспортного сообщения: цикл обмена не задан!'");
		ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
		ПризнакОшибки = Истина;
	КонецЕсли;
	
	ФорматДокументооборота = ЦиклОбмена.ФорматДокументооборота;
	
	// определяем сертификат получателя
	СертификатПолучателя = ПолучитьСертификатСубъекта(СсылкаПолучатель, ФорматДокументооборота);
	Если СертификатПолучателя.Количество() = 0 Тогда
		ТекстСообщения = СтроковыеФункцииКлиентСервер.ПодставитьПараметрыВСтроку(
			НСтр("ru = 'В свойствах органа ПФР %1 не задан ни один сертификат.'"), СсылкаПолучатель);
		ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
		Возврат Ложь;
	КонецЕсли;
	
	// определяем сертификаты отправителя
	СертификатРуководителя = СокрЛП(УчетнаяЗапись.СертификатРуководителя);
	СертификатОтветственногоЗаШифрование = СокрЛП(УчетнаяЗапись.СертификатДляШифрования);
	
	// получаем и проверяем код отправителя
	ПараметрыОтправкиВПФР = СформироватьПараметрыОтправкиВПФР(Сообщение.Отправитель);
	КодОтправителя = СокрЛП(ПараметрыОтправкиВПФР.РегНомПФР);
	Если ПустаяСтрока(КодОтправителя) Тогда
		ТекстСообщения = НСтр("ru = 'Не задан регистрационный номер ПФР организации-отправителя.'");
		ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
		Возврат Ложь;
	ИначеЕсли НЕ РегистрационныйНомерПФРСоответствуетТребованиям(КодОтправителя) Тогда
		ТекстСообщения = НСтр("ru = 'Некорректно задан регистрационный номер ПФР организации-отправителя.'");
		ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
		Возврат Ложь;
	КонецЕсли;
	
	// получаем и проверяем код получателя
	КодПолучателя = СокрЛП(СсылкаПолучатель.Код);
	Если ПустаяСтрока(КодПолучателя) Тогда
		ТекстСообщения = НСтр("ru = 'Не задан код органа ПФР.'");
		ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
		Возврат Ложь;
	ИначеЕсли НЕ КодОрганаПФРСоответствуетТребованиям(КодПолучателя) Тогда
		ТекстСообщения = НСтр("ru = 'Некорректно задан код органа ПФР.'");
		ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
		Возврат Ложь;
	КонецЕсли;
	
	Если ТипСообщения = Перечисления.ТипыТранспортныхСообщений.ПервичноеСообщениеСодержащееОтчетностьПФР Тогда
		ТипДокументооборота = "СведенияПФР";
		ТипТранзакции = "сведения";
	ИначеЕсли ТипСообщения = Перечисления.ТипыТранспортныхСообщений.ПротоколКвитанцияПФР Тогда
		ТипДокументооборота = "СведенияПФР";
		ТипТранзакции = "протоколКвитанция";
	ИначеЕсли ТипСообщения = Перечисления.ТипыТранспортныхСообщений.ПисьмоИсходящееПФР Тогда
		ТипДокументооборота = "Письмо";
		ТипТранзакции = "письмо";
	ИначеЕсли ТипСообщения = Перечисления.ТипыТранспортныхСообщений.ПисьмоВходящееКвитанцияПФР Тогда
		ТипДокументооборота = "Письмо";
		ТипТранзакции = "письмоКвитанция";
	ИначеЕсли ТипСообщения = Перечисления.ТипыТранспортныхСообщений.ПервичноеСообщениеСодержащееЗапросПФР Тогда
		ТипДокументооборота = "УточнениеПлатежей";
		ТипТранзакции = "запрос";
	ИначеЕсли ТипСообщения = Перечисления.ТипыТранспортныхСообщений.ОтветНаЗапросКвитанцияПФР Тогда
		ТипДокументооборота = "УточнениеПлатежей";
		ТипТранзакции = "ответКвитанция";
	КонецЕсли;
	
	КодСистемыОтправителя = УчетнаяЗапись.ИдентификаторСистемыОтправителяПФР;
	
	UUIDПакета = СокрЛП(ЦиклОбмена.Идентификатор);
	ВидОтправляемогоОтчета = ЦиклОбмена.ВидОтчета;
	
	// запоминаем в контексте
	Контекст.Вставить("ТипСообщения", ТипСообщения);
	Контекст.Вставить("СсылкаОтправитель", СсылкаОтправитель);
	Контекст.Вставить("СсылкаПолучатель", СсылкаПолучатель);
	Контекст.Вставить("УчетнаяЗапись", УчетнаяЗапись);
	Контекст.Вставить("ЦиклОбмена", ЦиклОбмена);
	Контекст.Вставить("СертификатРуководителя", СертификатРуководителя);
	Контекст.Вставить("СертификатОтветственногоЗаШифрование", СертификатОтветственногоЗаШифрование);
	Контекст.Вставить("СертификатПолучателя", СертификатПолучателя);
	
	Контекст.Вставить("ТипДокументооборота", ТипДокументооборота);
	Контекст.Вставить("ТипТранзакции", ТипТранзакции);
	Контекст.Вставить("КодОтправителя", КодОтправителя);
	Контекст.Вставить("КодПолучателя", КодПолучателя);
	Контекст.Вставить("КодСистемыОтправителя", КодСистемыОтправителя);
	Контекст.Вставить("UUIDПакета", UUIDПакета);
	Контекст.Вставить("ВидОтправляемогоОтчета", ВидОтправляемогоОтчета);
	Контекст.Вставить("ФорматДокументооборота", ФорматДокументооборота);
	
	Контекст.Вставить("СообщениеОснование", СообщениеОснование);
	
	Возврат НЕ ПризнакОшибки;
	
КонецФункции

Функция СформироватьПараметрыОтправкиВПФР(Организация) Экспорт
	
	СведенияОбОрганизации = РегламентированнаяОтчетностьВызовСервера.ПолучитьСведенияОбОрганизации(Организация, , "РегНомПФР, КодОрганаПФР, РегНомПФРЗаСебя");
	ЭтоИП = НЕ РегламентированнаяОтчетностьВызовСервера.ЭтоЮридическоеЛицо(Организация);
	Если ЭтоИП Тогда
		
		ИПИспользуетТрудНаемныхРаботников = РегламентированнаяОтчетность.ИПИспользуетТрудНаемныхРаботников(Организация);
		Если НЕ ИПИспользуетТрудНаемныхРаботников Тогда
			СведенияОбОрганизации.Вставить("РегНомПФР", СведенияОбОрганизации.РегНомПФРЗаСебя); 
		КонецЕсли;
		
	КонецЕсли;
	
	СведенияОбОрганизации.Удалить("РегНомПФРЗаСебя");
	
	Возврат СведенияОбОрганизации;
	
КонецФункции

Функция РегистрационныйНомерПФРСоответствуетТребованиям(РегистрационныйНомерПФР)
	
	Возврат СтрокаСоответствуетШаблону(СокрЛП(РегистрационныйНомерПФР), "№№№-№№№-№№№№№№");
	
КонецФункции

Функция КодОрганаПФРСоответствуетТребованиям(КодОрганаПФР)
	
	Возврат СтрокаСоответствуетШаблону(СокрЛП(КодОрганаПФР), "№№№-№№№");
	
КонецФункции

Процедура СформироватьПакетПФР_ПолучитьИПроверитьСертификатыПолучателя(КонтекстОтправки, СообщениеОбОшибке)

	// Внимание!
	// ---------
	// В Контейнере есть одноименная процедура, которая используется при отправке отчетов
	// в интерактивном режиме пользователем.
	// При изменениях необходимо синхронно менять обе процедуры.
		
	ЭтоЭлектроннаяПодписьВМоделиСервиса = ЭлектроннаяПодписьВМоделиСервисаБРОВызовСервера.РеквизитыУчетнойЗаписи(КонтекстОтправки.УчетнаяЗапись).ЭлектроннаяПодписьВМоделиСервиса;
	
	Если ТипЗнч(КонтекстОтправки.СертификатПолучателя) <> Тип("Массив") Тогда
		Отпечатки = Новый Массив;
		Отпечатки.Добавить(КонтекстОтправки.СертификатПолучателя);
	Иначе
		Отпечатки = КонтекстОтправки.СертификатПолучателя;
	КонецЕсли;
	
 	СертификатыДляПоиска = Новый Массив;
	Для Каждого Отпечаток Из Отпечатки Цикл
		СертификатыДляПоиска.Добавить(Новый Структура("ЭтоЭлектроннаяПодписьВМоделиСервиса, Отпечаток", ЭтоЭлектроннаяПодписьВМоделиСервиса, Отпечаток));	
	КонецЦикла;
	
	РезультатПоиска = НайтиСертификаты(СертификатыДляПоиска, Истина);
	
	СформироватьПакетПФР_ПолучитьИПроверитьСертификатыПолучателяПослеПолученияСертификатов(КонтекстОтправки, СообщениеОбОшибке, РезультатПоиска)
	
КонецПроцедуры

Процедура СформироватьПакетПФР_ПолучитьИПроверитьСертификатыПолучателяПослеПолученияСертификатов(КонтекстОтправки, СообщениеОбОшибке, РезультатПоиска)

	// Внимание!
	// ---------
	// В Контейнере есть одноименная процедура, которая используется при отправке отчетов
	// в интерактивном режиме пользователем.
	// При изменениях необходимо синхронно менять обе процедуры.
	
	НайденныеСертификаты   = ?(РезультатПоиска.Свойство("Сертификаты"), РезультатПоиска.Сертификаты, Неопределено);
	НенайденныеСертификаты = ?(РезультатПоиска.Свойство("НенайденныеСертификаты"), РезультатПоиска.НенайденныеСертификаты, Неопределено);
	СсылкаПолучатель       = КонтекстОтправки.СсылкаПолучатель;
	
	Если НЕ СформироватьПакетПФР_ПолучитьИПроверитьСертификатыПолучателяПослеПолученияСертификатовРазобратьНайденное(РезультатПоиска.Выполнено, НайденныеСертификаты, НенайденныеСертификаты, СсылкаПолучатель) Тогда
		СообщениеОбОшибке = ТекстСообщенийПользователю();
		Возврат;
	КонецЕсли;
	
	КонтекстОтправки.Вставить("ОбъектСертификатПолучателя", НайденныеСертификаты);
	
КонецПроцедуры

Функция СформироватьПакетПФР_ПолучитьИПроверитьСертификатыПолучателяПослеПолученияСертификатовРазобратьНайденное(Выполнено, НайденныеСертификаты, НенайденныеСертификаты, СсылкаПолучатель) Экспорт

	Если Не Выполнено Тогда
		ТекстСообщения = СтроковыеФункцииКлиентСервер.ПодставитьПараметрыВСтроку(
			НСтр("ru = 'Не удалось определить цифровой сертификат получателя
                  |%1
                  |для шифрования сообщения!'"), СсылкаПолучатель);
		ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
		Возврат Ложь;
	КонецЕсли;
	
	Если НенайденныеСертификаты.Количество() > 0 Тогда
		ТекстСообщения = СтроковыеФункцииКлиентСервер.ПодставитьПараметрыВСтроку(
			НСтр("ru = 'Не удалось определить цифровой сертификат получателя
                  |%1
                  |для шифрования сообщения!'"), СсылкаПолучатель);
		ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
		Возврат Ложь;
	КонецЕсли;
	
	Если ТипЗнч(СсылкаПолучатель) = Тип("СправочникСсылка.ОрганыПФР") Тогда
	
		Для Каждого ОбъектСертификатОрганаПФР Из НайденныеСертификаты Цикл				
			РезультатПроверкиСертификатаОрганаПФР = ПроверитьСертификатПоПериоду(ОбъектСертификатОрганаПФР);
			Если РезультатПроверкиСертификатаОрганаПФР <> Истина Тогда
				ТекстСообщения = СтроковыеФункцииКлиентСервер.ПодставитьПараметрыВСтроку(
					НСтр("ru = 'Ошибка проверки сертификата органа ПФР %1 с отпечатком %2.'"),
					СсылкаПолучатель, 
					ОбъектСертификатОрганаПФР.Отпечаток);
				ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
				Возврат Ложь;
			КонецЕсли;			
		КонецЦикла;
			
			// проверяем на наличие хотя бы одного сертификата
		Если НайденныеСертификаты.Количество() = 0 Тогда 
			ТекстСообщения = СтроковыеФункцииКлиентСервер.ПодставитьПараметрыВСтроку(
				НСтр("ru = 'В свойствах органа ПФР %1 не задан ни один сертификат.'"), СсылкаПолучатель);
			ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
			Возврат Ложь;
		КонецЕсли;		
	КонецЕсли;
	
	Возврат Истина;
	
КонецФункции

Процедура СформироватьПакетПФРПослеПроверкиИПолученияСертификатовПолучателя(КонтекстОтправки, СообщениеОбОшибке)

	// Внимание!
	// ---------
	// В Контейнере есть одноименная процедура, которая используется при отправке отчетов
	// в интерактивном режиме пользователем.
	// При изменениях необходимо синхронно менять обе процедуры.

	СформироватьПакетПФР_ПроверкиСертификатов(КонтекстОтправки, СообщениеОбОшибке);
	
КонецПроцедуры

Функция СформироватьПакетПФР_ПодготовитьСертификатыДляПоиска(УчетнаяЗапись, СертификатРуководителя, СертификатОтветственногоЗаШифрование) Экспорт

	ЭтоЭлектроннаяПодписьВМоделиСервиса = ЭлектроннаяПодписьВМоделиСервисаБРОВызовСервера.РеквизитыУчетнойЗаписи(УчетнаяЗапись).ЭлектроннаяПодписьВМоделиСервиса;
	
	СертификатыДляПоиска = Новый Массив;
	
	ОтпечатокСертификатРуководителя = СокрЛП(СертификатРуководителя);
	ОтпечатокСертификатОтветственногоЗаШифрование = СокрЛП(СертификатОтветственногоЗаШифрование);
	
	Если Не ЗначениеЗаполнено(ОтпечатокСертификатРуководителя) Тогда
		ТекстСообщения = СтроковыеФункцииКлиентСервер.ПодставитьПараметрыВСтроку(
			НСтр("ru = 'В настройках учетной записи %1 не задан сертификат руководителя.'"), УчетнаяЗапись);
		ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
		Возврат Неопределено;
	Иначе
		СертификатыДляПоиска.Добавить(
			Новый Структура("Отпечаток,ЭтоЭлектроннаяПодписьВМоделиСервиса", 
				ОтпечатокСертификатРуководителя, ЭтоЭлектроннаяПодписьВМоделиСервиса));
	КонецЕсли;
	
	Если Не ЗначениеЗаполнено(ОтпечатокСертификатОтветственногоЗаШифрование) Тогда
		ТекстСообщения = СтроковыеФункцииКлиентСервер.ПодставитьПараметрыВСтроку(
			НСтр("ru = 'В настройках учетной записи %1 не задан сертификат ответственного за шифрование.'"), УчетнаяЗапись);
		ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
		Возврат Неопределено;
	ИначеЕсли ОтпечатокСертификатОтветственногоЗаШифрование <> ОтпечатокСертификатРуководителя Тогда
		СертификатыДляПоиска.Добавить(
			Новый Структура("Отпечаток,ЭтоЭлектроннаяПодписьВМоделиСервиса", 
				ОтпечатокСертификатОтветственногоЗаШифрование, ЭтоЭлектроннаяПодписьВМоделиСервиса));
	КонецЕсли;
	
	Возврат СертификатыДляПоиска;
	
КонецФункции

Процедура СформироватьПакетПФР_ПроверкиСертификатов(КонтекстОтправки, СообщениеОбОшибке)

	// Внимание!
	// ---------
	// В Контейнере есть одноименная процедура, которая используется при отправке отчетов
	// в интерактивном режиме пользователем.
	// При изменениях необходимо синхронно менять обе процедуры.
	
	Если КонтекстОтправки.Свойство("ОбъектСертификатОтветственногоЗаШифрование") Тогда // Возможна ситуация, когда сертификаты уже получены и проверены.
		Возврат;
	КонецЕсли;
	
	СертификатыДляПоиска = СформироватьПакетПФР_ПодготовитьСертификатыДляПоиска(
		КонтекстОтправки.УчетнаяЗапись,
		КонтекстОтправки.СертификатРуководителя,
		КонтекстОтправки.СертификатОтветственногоЗаШифрование);
	
	Если СертификатыДляПоиска = Неопределено Тогда
		СообщениеОбОшибке = ТекстСообщенийПользователю();
		Возврат;
	КонецЕсли;
	
	РезультатПоиска = НайтиСертификаты(СертификатыДляПоиска, Истина);
	
	СформироватьПакетПФР_ПроверкиСертификатовПослеПолученияСертификатов(КонтекстОтправки, СообщениеОбОшибке, РезультатПоиска);
	
КонецПроцедуры

Процедура СформироватьПакетПФР_ПроверкиСертификатовПослеПолученияСертификатов(КонтекстОтправки, СообщениеОбОшибке, РезультатПоиска)

	// Внимание!
	// ---------
	// В Контейнере есть одноименная процедура, которая используется при отправке отчетов
	// в интерактивном режиме пользователем.
	// При изменениях необходимо синхронно менять обе процедуры.
	
	ОбъектыСертификатов = СформироватьПакетПФР_ПроверкиСертификатовПослеПолученияСертификатовРазобратьНайденное(
		РезультатПоиска.Сертификаты,
		КонтекстОтправки.СертификатРуководителя,
		КонтекстОтправки.СертификатОтветственногоЗаШифрование);
	
	Если ОбъектыСертификатов = Неопределено Тогда
		СообщениеОбОшибке = ТекстСообщенийПользователю();
		Если НЕ ЗначениеЗаполнено(СообщениеОбОшибке) Тогда
			СообщениеОбОшибке = НСтр("ru = 'Не удалось найти сертификаты ответственных лиц в системном хранилище сертификатов.'");
		КонецЕсли;
		Возврат;
	КонецЕсли;
	
	///////////////////////////////////////////////////////////////////////////////
	Для каждого ОбъектСертификат Из ОбъектыСертификатов Цикл
		КонтекстОтправки.Вставить(ОбъектСертификат.Ключ, ОбъектСертификат.Значение);
	КонецЦикла;
	
КонецПроцедуры

Функция СформироватьПакетПФР_ПроверкиСертификатовПослеПолученияСертификатовРазобратьНайденное(Сертификаты, СертификатРуководителя, СертификатОтветственногоЗаШифрование) Экспорт

	ОтпечатокСертификатРуководителя = СокрЛП(СертификатРуководителя);
	ОтпечатокСертификатОтветственногоЗаШифрование = СокрЛП(СертификатОтветственногоЗаШифрование);
	
	ОбъектСертификатРуководителя = Неопределено;
	ОбъектСертификатОтветственногоЗаШифрование = Неопределено;
	
	Для Каждого НайденныйСертификат Из Сертификаты Цикл
		Если ОтпечатокСертификатРуководителя = НайденныйСертификат.Отпечаток Тогда
			ОбъектСертификатРуководителя = НайденныйСертификат;
		КонецЕсли;
		Если ОтпечатокСертификатОтветственногоЗаШифрование = НайденныйСертификат.Отпечаток Тогда
			ОбъектСертификатОтветственногоЗаШифрование = НайденныйСертификат;
		КонецЕсли;
	КонецЦикла;
	
	Если ОбъектСертификатРуководителя = Неопределено Тогда
		ТекстСообщения = НСтр("ru = 'Не удалось найти сертификат руководителя в системном хранилище сертификатов.'");
		ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
		Возврат Неопределено;
	КонецЕсли;
	
	Если ОбъектСертификатРуководителя.Свойство("Валиден") И Не ОбъектСертификатРуководителя.Валиден Тогда
		Возврат Неопределено;
	КонецЕсли;
	
	Если ОтпечатокСертификатОтветственногоЗаШифрование = Неопределено Тогда
		ТекстСообщения = НСтр("ru = 'Не удалось найти сертификат ответственного за шифрование в системном хранилище сертификатов.'");
		ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
		Возврат Неопределено;
	КонецЕсли;
	
	Если ОбъектСертификатОтветственногоЗаШифрование.Свойство("Валиден") И Не ОбъектСертификатОтветственногоЗаШифрование.Валиден Тогда
		Возврат Неопределено;
	КонецЕсли;
		
	///////////////////////////////////////////////////////////////////////////////
	Результат = Новый Структура();
	Результат.Вставить("ОбъектСертификатРуководителя", ОбъектСертификатРуководителя);
	Результат.Вставить("ОбъектСертификатОтветственногоЗаШифрование", ОбъектСертификатОтветственногоЗаШифрование);
	
	Возврат Результат;
	
КонецФункции

Процедура СформироватьПакетПФРПослеПроверкиСертификатов(КонтекстОтправки, СообщениеОбОшибке)

	// Внимание!
	// ---------
	// В Контейнере есть одноименная процедура, которая используется при отправке отчетов
	// в интерактивном режиме пользователем.
	// При изменениях необходимо синхронно менять обе процедуры.
			
	// формируем файл описания сведений, если это первичное сообщение с отчетностью
	Если КонтекстОтправки.ТипСообщения = Перечисления.ТипыТранспортныхСообщений.ПервичноеСообщениеСодержащееОтчетностьПФР
		ИЛИ КонтекстОтправки.ТипСообщения = Перечисления.ТипыТранспортныхСообщений.ПервичноеСообщениеСодержащееЗапросПФР Тогда
		
		Если НЕ СформироватьОписаниеСведенийДляСообщения(КонтекстОтправки) Тогда
			СообщениеОбОшибке = НСтр("ru = 'Формирование пакета прервано.'");
			ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(СообщениеОбОшибке);
			Возврат;
		КонецЕсли;
	КонецЕсли;
	
	ЭтоМакетПД = ЭтоМакетПД(КонтекстОтправки.ЦиклОбмена);
	ЭтоЗаявление = ЭтоЗаявлениеОНазначенииПенсии(КонтекстОтправки.ЦиклОбмена);
	Если ЭтоМакетПД ИЛИ ЭтоЗаявление Тогда
		
		Настройки = ПолучитьПараметрыОтправкиМакетовПенсионныхДелИЗаявления(КонтекстОтправки.ЦиклОбмена);
			
		Если Настройки = Неопределено Тогда
			СообщениеОбОшибке = ТекстСообщенийПользователю();
			Возврат;
		КонецЕсли;
		
		КонтекстОтправки.Вставить("Настройки", Настройки);
	КонецЕсли;
	
	КонтекстОтправки.Вставить("КонтекстОтправки", КонтекстОтправки);
	КонтекстОтправки.Вставить("ПервыйВызов", Истина);
	КонтекстОтправки.Вставить("СформироватьПакетМакетаПДИЗаявление", ЭтоМакетПД ИЛИ ЭтоЗаявление);
	
КонецПроцедуры

Функция СформироватьОписаниеСведенийДляСообщения(Контекст) Экспорт

	Сообщение = Контекст.ТранспортноеСообщение;
	
	Если Сообщение.Тип = Перечисления.ТипыТранспортныхСообщений.ПервичноеСообщениеСодержащееЗапросПФР Тогда
		
		ТипыВложений = Новый Массив;
		ТипыВложений.Добавить(Перечисления.ТипыСодержимогоТранспортногоКонтейнера.ЗапросИОС);
		ТипыВложений.Добавить(Перечисления.ТипыСодержимогоТранспортногоКонтейнера.ФайлОписанияЗапросаПФР);
		
		Вложения = ПолучитьВложенияТранспортногоСообщения(Сообщение, , ТипыВложений);
		
		Если Вложения.Найти(Перечисления.ТипыСодержимогоТранспортногоКонтейнера.ФайлОписанияЗапросаПФР, "Тип") <> Неопределено Тогда
			Возврат Истина;
		КонецЕсли;
		
		// генерируем файл
		КороткоеИмяФайла = Неопределено;
		ФайлОписаниеСведений = СформироватьОписаниеЗапроса(Сообщение, Вложения, КороткоеИмяФайла);
		Если ФайлОписаниеСведений = Неопределено Тогда
			Возврат Ложь;
		КонецЕсли;
		
		// загружаем файл в содержимое
		ДобавитьСодержимоеТранспортногоКонтейнера(Сообщение, Перечисления.ТипыСодержимогоТранспортногоКонтейнера.ФайлОписанияЗапросаПФР, ФайлОписаниеСведений, КороткоеИмяФайла, , , , , , , СгенерироватьUUID(), Перечисления.ТипыСодержимогоФайлов.xml);
		
	Иначе
		
		ТипыВложений = Новый Массив;
		ТипыВложений.Добавить(Перечисления.ТипыСодержимогоТранспортногоКонтейнера.ФайлОтчетностиПФР);
		ТипыВложений.Добавить(Перечисления.ТипыСодержимогоТранспортногоКонтейнера.СведенияПриложениеПФР);
		ТипыВложений.Добавить(Перечисления.ТипыСодержимогоТранспортногоКонтейнера.ФайлОписанияСведенийПФР);
		Вложения = ПолучитьВложенияТранспортногоСообщения(Сообщение, , ТипыВложений);
		
		Если Вложения.Найти(Перечисления.ТипыСодержимогоТранспортногоКонтейнера.ФайлОписанияСведенийПФР, "Тип") <> Неопределено Тогда
			Возврат Истина;
		КонецЕсли;
		
		Если НЕ Контекст.Свойство("ФорматДокументооборота")
			ИЛИ Контекст.ФорматДокументооборота <> Перечисления.ФорматыДокументооборотаСПФР.ЭДОК Тогда
			
			// генерируем файл
			КороткоеИмяФайла = Неопределено;
			ФайлОписаниеСведений = СформироватьОписаниеСведений(Сообщение, Вложения, КороткоеИмяФайла);
			Если ФайлОписаниеСведений = Неопределено Тогда
				Возврат Ложь;
			КонецЕсли;
			
			// загружаем файл в содержимое
			ДобавитьСодержимоеТранспортногоКонтейнера(
				Сообщение,
				Перечисления.ТипыСодержимогоТранспортногоКонтейнера.ФайлОписанияСведенийПФР,
				ФайлОписаниеСведений,
				КороткоеИмяФайла,,,,,,,
				СгенерироватьUUID(),
				Перечисления.ТипыСодержимогоФайлов.xml);
		КонецЕсли;



		
	КонецЕсли;
	
	Возврат Истина;
	
КонецФункции

Функция СформироватьОписаниеПисьмаДляСообщения(Контекст) Экспорт
	
	Сообщение = Контекст.ТранспортноеСообщение;
	
	// сначала получаем строки вложений без данных
	ТипыВложений = Новый Массив;
	ТипыВложений.Добавить(Перечисления.ТипыСодержимогоТранспортногоКонтейнера.Письмо);
	ТипыВложений.Добавить(Перечисления.ТипыСодержимогоТранспортногоКонтейнера.ПисьмоПриложение);
	ТипыВложений.Добавить(Перечисления.ТипыСодержимогоТранспортногоКонтейнера.ОписаниеПисьма);
	Вложения = ПолучитьВложенияТранспортногоСообщения(Сообщение, , ТипыВложений);
	
	// проверяем, не сформировано ли ранее описание письма
	Если Вложения.Найти(Перечисления.ТипыСодержимогоТранспортногоКонтейнера.ОписаниеПисьма, "Тип") <> Неопределено Тогда
		Возврат Истина;
	КонецЕсли;
	
	// получаем строки вложений ЭЦП с данными
	ТипыВложений.Добавить(Перечисления.ТипыСодержимогоТранспортногоКонтейнера.ЭлектронноЦифроваяПодпись);
	ВложенияЭЦП = ПолучитьВложенияТранспортногоСообщения(Сообщение, Истина, Перечисления.ТипыСодержимогоТранспортногоКонтейнера.ЭлектронноЦифроваяПодпись);
	
	// добавляем строки описания ЭЦП к ранее полученным строкам описаний
	Вложения.Колонки.Добавить("Данные");
	Для Каждого СтрВложениеЭЦП Из ВложенияЭЦП Цикл
		НовСтр = Вложения.Добавить();
		ЗаполнитьЗначенияСвойств(НовСтр, СтрВложениеЭЦП);
	КонецЦикла;
	
	// генерируем файл
	КороткоеИмяФайла = Неопределено;
	ФайлОписаниеПисьма = СформироватьОписаниеПисьма(Сообщение, Вложения, КороткоеИмяФайла, Контекст);
	Если ФайлОписаниеПисьма = Неопределено Тогда
		Возврат Ложь;
	КонецЕсли;
	
	// загружаем файл в содержимое
	ДобавитьСодержимоеТранспортногоКонтейнера(Сообщение, Перечисления.ТипыСодержимогоТранспортногоКонтейнера.ОписаниеПисьма, ФайлОписаниеПисьма, КороткоеИмяФайла, , , , , , , СгенерироватьUUID(), Перечисления.ТипыСодержимогоФайлов.xml);
	
	Возврат Истина;
	
КонецФункции

Функция СформироватьОписаниеПисьма(Сообщение, Вложения, КороткоеИмяФайла = Неопределено, КонтекстОтправки = Неопределено)
	
	// создаем дерево описания XML
	Дерево = СоздатьДеревоXML();
	
	// добавляем узел "тема"
	Если (ТипЗнч(Сообщение.ЦиклОбмена.Предмет) = Тип("СправочникСсылка.МакетыПенсионныхДел")
		ИЛИ ТипЗнч(Сообщение.ЦиклОбмена.Предмет) = Тип("СправочникСсылка.ЗаявлениеОНазначенииПенсии"))
		И ЗначениеЗаполнено(КонтекстОтправки) И ЗначениеЗаполнено(КонтекстОтправки.Настройки.Тема.Маска) Тогда
		Тема = КонтекстОтправки.Настройки.Тема.Маска;
		Для Каждого КлючЗначение Из КонтекстОтправки.Настройки.ПараметрыЗаполнения Цикл
			Тема = СтрЗаменить(Тема, СтрШаблон("{%1}", КлючЗначение.Ключ), КлючЗначение.Значение);
		КонецЦикла;
		УзелТема = ДобавитьУзелXML(Дерево, "тема", , XMLСтрока(СокрЛП(Тема)));
	Иначе
		УзелТема = ДобавитьУзелXML(Дерево, "тема", , XMLСтрока(СокрЛП(Сообщение.ЦиклОбмена.Предмет.Наименование)));
	КонецЕсли;
	
	// добавляем узел "датаВремяОтправки"
	УзелДатаВремяОтправки = ДобавитьУзелXML(Дерево, "датаВремяОтправки", , XMLСтрока(ТекущаяДатаСеанса()));
	
	// добавляем узел "идентификаторПисьма"
	УзелИдентификаторПисьма = ДобавитьУзелXML(Дерево, "идентификаторПисьма", , XMLСтрока(Сообщение.ЦиклОбмена.Предмет.Идентификатор));
	
	// добавляем узел "ответНаПисьмо"
	ИдентификаторОснования = Сообщение.ЦиклОбмена.Предмет.ИдентификаторОснования;
	Если ЗначениеЗаполнено(ИдентификаторОснования) Тогда
		УзелОтветНаПисьмо = ДобавитьУзелXML(Дерево, "ответНаПисьмо", , XMLСтрока(ИдентификаторОснования));
	КонецЕсли;
	
	// добавляем узел "категорияПисьма"
	Если ТипЗнч(Сообщение.ЦиклОбмена.Предмет) = Тип("СправочникСсылка.ПерепискаСКонтролирующимиОрганами") Тогда
		Если Сообщение.ЦиклОбмена.Предмет.Ретроконверсия Тогда
			УзелКатегорияПисьма = ДобавитьУзелXML(Дерево, "категорияПисьма", , XMLСтрока("Ретроконверсия"));
		Иначе
			СтрПисьмоПриложение = Вложения.Найти(Перечисления.ТипыСодержимогоТранспортногоКонтейнера.ПисьмоПриложение, "Тип");
			Если СтрПисьмоПриложение <> Неопределено
				И СтрПисьмоПриложение.ТипСодержимогоФайла = Перечисления.ТипыСодержимогоФайлов.Xml Тогда
				
				ЭлементыИмениФайла = СтрРазделить(СтрПисьмоПриложение.ИмяФайла, "_");
				Если ЭлементыИмениФайла.Количество() >= 3 И ЭлементыИмениФайла[0] = "ПФР" И ЭлементыИмениФайла[2] = "СИоЗП" Тогда
					УзелКатегорияПисьма = ДобавитьУзелXML(Дерево, "категорияПисьма",, "СИоЗП");
				КонецЕсли;
			КонецЕсли;
		КонецЕсли;
	КонецЕсли;
	Если ТипЗнч(Сообщение.ЦиклОбмена.Предмет) = Тип("СправочникСсылка.МакетыПенсионныхДел") Тогда
		УзелКатегорияПисьма = ДобавитьУзелXML(Дерево, "категорияПисьма", , "МакетЭВД");
	КонецЕсли;
	Если ТипЗнч(Сообщение.ЦиклОбмена.Предмет) = Тип("СправочникСсылка.ЗаявлениеОНазначенииПенсии") Тогда
		УзелКатегорияПисьма = ДобавитьУзелXML(Дерево, "категорияПисьма", , "Заявление");
	КонецЕсли;
	
	// добавляем узел "телоПисьма"
	УзелТелоПисьма = ДобавитьУзелXML(Дерево, "телоПисьма");
	
	// добавляем подузел "ИдентификаторДокумента"
	СтрПисьмо = Вложения.Найти(Перечисления.ТипыСодержимогоТранспортногоКонтейнера.Письмо, "Тип");
	Если СтрПисьмо = Неопределено Тогда
		ТекстСообщения = НСтр("ru = 'Ошибка формирования описания письма: не удалось обнаружить документ с телом письма.'");
		ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
		Возврат Неопределено;
	КонецЕсли;
	УзелИдентификаторДокумента = ДобавитьУзелXML(УзелТелоПисьма, "идентификаторДокумента", , СтрПисьмо.Идентификатор);
	
	// ищем подпись под телом письма
	СвойстваПодписиПодТеломПисьма = ОпределитьСвойстваПодписиПодТеломПисьма(Вложения);
	Если СвойстваПодписиПодТеломПисьма = Неопределено Тогда
		ТекстСообщения = НСтр("ru = 'Ошибка определения свойств подписи под телом письма при формировании описания письма из состава пакета.'");
		ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
		Возврат Неопределено;
	КонецЕсли;
	
	// добавляем подузел "подписи"
	УзелПодписи = ДобавитьУзелXML(УзелТелоПисьма, "подписи");
	
	// добавляем узел "подпись"
	ДанныеПодписиПодТеломПисьма = ПолучитьДанныеСтрокой64(СвойстваПодписиПодТеломПисьма.Данные.Получить());
	УзелПодпись = ДобавитьУзелXML(УзелПодписи, "подпись", , ДанныеПодписиПодТеломПисьма);
	
	// добавляем атрибут "роль"
	УзелАтрибутРоль = ДобавитьУзелXML(УзелПодпись, "роль", Истина, "руководитель");
	
	// добавляем узел "списокПриложений"
	УзелСписокПриложений = ДобавитьУзелXML(Дерево, "списокПриложений");
	
	// выгружаем описание приложений
	ПриложенияКПисьму = Вложения.НайтиСтроки(Новый Структура("Тип", Перечисления.ТипыСодержимогоТранспортногоКонтейнера.ПисьмоПриложение));
	Для Каждого ПриложениеКПисьму Из ПриложенияКПисьму Цикл
		
		// добавляем узел "приложение"
		УзелПриложение = ДобавитьУзелXML(УзелСписокПриложений, "приложение");
		
		// добавляем узел "идентификаторДокумента"
		УзелИдентификаторДокумента = ДобавитьУзелXML(УзелПриложение, "идентификаторДокумента", , ПриложениеКПисьму.Идентификатор);
		
		// добавляем узел "имяФайла"
		УзелИмяФайла = ДобавитьУзелXML(УзелПриложение, "имяФайла", , ПриложениеКПисьму.ИмяФайла);
		
		// добавляем узел "подписи"
		УзелПодписи = ДобавитьУзелXML(УзелПриложение, "подписи");
		
		// добавляем узлы "подпись"
		ПодписиПодДокументом = Вложения.НайтиСтроки(Новый Структура("ЭЦПИмяПодписанногоФайла", ПриложениеКПисьму.ИмяФайла));
		Для Каждого ПодписьПодДокументом Из ПодписиПодДокументом Цикл
			
			// добавляем узел "подпись"
			УзелПодпись = ДобавитьУзелXML(УзелПодписи, "подпись", , ПолучитьДанныеСтрокой64(ПодписьПодДокументом.Данные));
			
			// добавляем узел "роль"
			ДобавитьУзелXML(УзелПодпись, "роль", Истина, "руководитель");
			
		КонецЦикла;
		
	КонецЦикла;
	
	// формируем строку XML на основе дерева
	СтрокаXML = ВыгрузитьДеревоВXML_ПФР(Дерево, "описаниеПисьма");
	
	// записываем строку в файл
	КороткоеИмяФайла = СгенерироватьИмяФайлаДокументаВСоставеПакета();
	ПолноеИмяФайлаДокументаВСоставеПакета = ПолучитьКаталогВременныхФайлов() + КороткоеИмяФайла;
	РезультатЗаписи = ЗаписатьXMLВФайл(СтрокаXML, ПолноеИмяФайлаДокументаВСоставеПакета);
	Если РезультатЗаписи = Ложь Тогда
		Возврат Неопределено;
	КонецЕсли;
	
	Возврат ПолноеИмяФайлаДокументаВСоставеПакета;
	
КонецФункции

Функция СформироватьОписаниеЗапроса(Сообщение, Вложения, КороткоеИмяФайла = Неопределено)
	
	// создаем дерево описания XML
	Дерево = СоздатьДеревоXML(); // Поддержать ЭДОК
	
	// добавляем узел "датаВремяОтправки"
	УзелДатаВремяОтправки = ДобавитьУзелXML(Дерево, "датаВремяОтправки", , XMLСтрока(ТекущаяДатаСеанса()));
	
	// добавляем узел "регистрационныйНомерОрганизации"
	ПараметрыОтправкиВПФР = СформироватьПараметрыОтправкиВПФР(Сообщение.Отправитель);
	КодОтправителя = СокрЛП(ПараметрыОтправкиВПФР.РегНомПФР);
	УзелРегистрационныйНомерОрганизации = ДобавитьУзелXML(Дерево, "регистрационныйНомерОрганизации", , КодОтправителя);
	
	// добавляем узел "имяФайла"
	Для Каждого Стр Из Вложения Цикл
		ДобавитьУзелXML(Дерево, "имяФайла", , СокрЛП(Стр.ИмяФайла));
	КонецЦикла;
	
	// формируем строку XML на основе дерева
	СтрокаXML = ВыгрузитьДеревоВXML_ПФР(Дерево, "описаниеЗапроса");
	
	// записываем строку в файл
	КороткоеИмяФайла = СгенерироватьИмяФайлаДокументаВСоставеПакета();
	ПолноеИмяФайлаДокументаВСоставеПакета = ПолучитьКаталогВременныхФайлов() + КороткоеИмяФайла;
	РезультатЗаписи = ЗаписатьXMLВФайл(СтрокаXML, ПолноеИмяФайлаДокументаВСоставеПакета);
	Если РезультатЗаписи = Ложь Тогда
		Возврат Неопределено;
	КонецЕсли;
	
	Возврат ПолноеИмяФайлаДокументаВСоставеПакета;
	
КонецФункции

Функция СформироватьОписаниеСведений(Сообщение, Вложения, КороткоеИмяФайла = Неопределено)
	
	// создаем дерево описания XML
	Дерево = СоздатьДеревоXML();
	
	// добавляем узел "датаВремяОтправки"
	УзелДатаВремяОтправки = ДобавитьУзелXML(Дерево, "датаВремяОтправки", , XMLСтрока(ТекущаяДатаСеанса()));
	
	// добавляем узел "регистрационныйНомерОрганизации"
	ПараметрыОтправкиВПФР = СформироватьПараметрыОтправкиВПФР(Сообщение.Отправитель);
	КодОтправителя = СокрЛП(ПараметрыОтправкиВПФР.РегНомПФР);
	УзелРегистрационныйНомерОрганизации = ДобавитьУзелXML(Дерево, "регистрационныйНомерОрганизации", , КодОтправителя);
	
	// добавляем узел "описаниеПачек"
	УзелОписаниеПачек = ДобавитьУзелXML(Дерево, "описаниеПачек");
	
	// добавляем информацию о документах пакета
	Для Каждого ДокументПакета Из Вложения Цикл
	
		// добавляем узел "пачка"
		УзелПачка = ДобавитьУзелXML(УзелОписаниеПачек, "пачка");
		
		// добавляем узел "идентификаторДокумента"
		УзелИдентификаторДокумента = ДобавитьУзелXML(УзелПачка, "идентификаторДокумента", , ДокументПакета.Идентификатор);
		
		// добавляем узел "имяФайла"
		УзелИмяФайла = ДобавитьУзелXML(УзелПачка, "имяФайла", , ДокументПакета.ИмяФайла);
		
	КонецЦикла;
	
	// формируем строку XML на основе дерева
	СтрокаXML = ВыгрузитьДеревоВXML_ПФР(Дерево, "описаниеСведений");
	
	// записываем строку в файл
	КороткоеИмяФайла = СгенерироватьИмяФайлаДокументаВСоставеПакета();
	ПолноеИмяФайлаДокументаВСоставеПакета = ПолучитьКаталогВременныхФайлов() + КороткоеИмяФайла;
	РезультатЗаписи = ЗаписатьXMLВФайл(СтрокаXML, ПолноеИмяФайлаДокументаВСоставеПакета);
	Если РезультатЗаписи = Ложь Тогда
		Возврат Неопределено;
	КонецЕсли;
	
	Возврат ПолноеИмяФайлаДокументаВСоставеПакета;
	
КонецФункции

Процедура СформироватьПакетПФР_Подписание(КонтекстОтправки, СообщениеОбОшибке)
	
	// Внимание!
	// ---------
	// В Контейнере есть одноименная процедура, которая используется при отправке отчетов
	// в интерактивном режиме пользователем.
	// При изменениях необходимо синхронно менять обе процедуры.
		
	// генерируем подписи
	ПодписатьСообщение(
		"ПФР", 
		КонтекстОтправки,
		КонтекстОтправки.ТранспортноеСообщение, 
		КонтекстОтправки.ТипСообщения,
		,
		КонтекстОтправки.УчетнаяЗапись, 
		КонтекстОтправки.СообщениеОснование,
		СообщениеОбОшибке);
	
КонецПроцедуры

Процедура СформироватьПакетПФРПослеПодписания(КонтекстОтправки, СообщениеОбОшибке)

	// Внимание!
	// ---------
	// В Контейнере есть одноименная процедура, которая используется при отправке отчетов
	// в интерактивном режиме пользователем.
	// При изменениях необходимо синхронно менять обе процедуры.
		
	Если КонтекстОтправки.ТипСообщения = Перечисления.ТипыТранспортныхСообщений.ПисьмоИсходящееПФР
		И КонтекстОтправки.ПервыйВызов Тогда
		
		Если КонтекстОтправки.СформироватьПакетМакетаПДИЗаявление Тогда
			
			Если КонтекстОтправки.Настройки.Вложение.ТребуетсяАрхивирование 
				И НЕ СформироватьПакетМакетаПенсионногоДелаИЗаявления(КонтекстОтправки) Тогда
				СообщениеОбОшибке = ТекстСообщенийПользователю();
				Возврат;
			КонецЕсли;
			
			СформироватьПакетПФР_Подписание(КонтекстОтправки, СообщениеОбОшибке);
			Если ЗначениеЗаполнено(СообщениеОбОшибке) Тогда
				Возврат;
			КонецЕсли;

			КонтекстОтправки.Вставить("СформироватьПакетМакетаПДИЗаявление", Ложь);
			
			СформироватьПакетПФРПослеПодписания(КонтекстОтправки, СообщениеОбОшибке);
			Возврат;
		КонецЕсли;
		
		// формируем файл описания письма, если его нет
		Если НЕ СформироватьОписаниеПисьмаДляСообщения(КонтекстОтправки) Тогда
			СообщениеОбОшибке = ТекстСообщенийПользователю();
			Возврат;
		КонецЕсли;
		
		КонтекстОтправки.Вставить("ПервыйВызов", Ложь);
		
		// формируем файл с транспортной информацией, если его нет
		СформироватьТранспортнуюИнформациюДляСообщения(КонтекстОтправки, СообщениеОбОшибке);
		Если ЗначениеЗаполнено(СообщениеОбОшибке) Тогда
			Возврат;
		КонецЕсли;

		СформироватьТранспортнуюИнформациюДляСообщенияПослеВыполнения(КонтекстОтправки, СообщениеОбОшибке);
		
		Возврат;
	КонецЕсли;
	
	Если КонтекстОтправки.ТипСообщения = Перечисления.ТипыТранспортныхСообщений.ПисьмоВходящееКвитанцияПФР
		И КонтекстОтправки.ПервыйВызов Тогда
		// формируем подтверждение получения
		Если НЕ СформироватьПодтверждениеПолученияДляСообщения(КонтекстОтправки) Тогда
			СообщениеОбОшибке = ТекстСообщенийПользователю();
			Возврат;
		КонецЕсли;
		
		СформироватьПакетПФР_Подписание(КонтекстОтправки, СообщениеОбОшибке);
		Если ЗначениеЗаполнено(СообщениеОбОшибке) Тогда
			Возврат;
		КонецЕсли;
		
		КонтекстОтправки.Вставить("ПервыйВызов", Ложь);
		СформироватьПакетПФРПослеПодписания(КонтекстОтправки, СообщениеОбОшибке);
		Возврат;
	КонецЕсли;
	
	Если КонтекстОтправки.ТипСообщения = Перечисления.ТипыТранспортныхСообщений.ОтветНаЗапросКвитанцияПФР
		И КонтекстОтправки.ПервыйВызов Тогда
		// формируем подтверждение получения
		Если НЕ СформироватьПодтверждениеПолученияЗапросаПФРДляСообщения(КонтекстОтправки.ТранспортноеСообщение) Тогда
			СообщениеОбОшибке = ТекстСообщенийПользователю();
			Возврат;
		КонецЕсли;
		
		СформироватьПакетПФР_Подписание(КонтекстОтправки, СообщениеОбОшибке);
		Если ЗначениеЗаполнено(СообщениеОбОшибке) Тогда
			Возврат;
		КонецЕсли;
		
		КонтекстОтправки.Вставить("ПервыйВызов", Ложь);
		СформироватьПакетПФРПослеПодписания(КонтекстОтправки, СообщениеОбОшибке);
		Возврат;
	КонецЕсли;

	Результат = КриптографияЭДКОКлиентСервер.ПодготовитьРезультат(
		Истина, "СтрокаBase64", 
		КриптографияЭДКОСлужебныйВызовСервера.ЭкспортироватьСертификатВBase64(КонтекстОтправки.ОбъектСертификатРуководителя));

	Если Результат.Выполнено Тогда
		ДвДанныеСертификата = Base64Значение(Результат.СтрокаBase64);
		КонтекстОтправки.Вставить("ДвДанныеСертификатаРуководителя", ДвДанныеСертификата);
	КонецЕсли;
	
	// Формируем дерево описания документов пакета (сервер).
	Если НЕ СформироватьПакетПФР_СформироватьОписаниеДокументовПакета(КонтекстОтправки) Тогда
		СообщениеОбОшибке = ТекстСообщенийПользователю();
		Возврат;
	КонецЕсли;
	
	// Отрабатываем отложенные команды (шифрование).
	СформироватьПакет_ОтработатьОтложенныеКоманды(КонтекстОтправки, СообщениеОбОшибке);
	Если ЗначениеЗаполнено(СообщениеОбОшибке) Тогда
		Возврат;
	КонецЕсли;
		
	СформироватьПакетПФРПослеВыполненияОтложенныхКоманд(КонтекстОтправки, СообщениеОбОшибке);
	
КонецПроцедуры

Процедура СформироватьТранспортнуюИнформациюДляСообщения(КонтекстОтправки, СообщениеОбОшибке)

	// Внимание!
	// ---------
	// В Контейнере есть одноименная процедура, которая используется при отправке отчетов
	// в интерактивном режиме пользователем.
	// При изменениях необходимо синхронно менять обе процедуры.
	
	Если ТипЗнч(КонтекстОтправки.ОбъектСертификатПолучателя) = Тип("Массив") Тогда
		СвойстваСертификатаПолучателя = КонтекстОтправки.ОбъектСертификатПолучателя[0];
	Иначе
		СвойстваСертификатаПолучателя = КонтекстОтправки.ОбъектСертификатПолучателя;
	КонецЕсли;
	Если ТипЗнч(СвойстваСертификатаПолучателя) = Тип("ФиксированнаяСтруктура") Тогда
		СвойстваСертификатаПолучателя = Новый Структура(СвойстваСертификатаПолучателя);
	КонецЕсли;
		
	ЭтоЭлектроннаяПодписьВМоделиСервиса = ЭлектроннаяПодписьВМоделиСервисаБРОВызовСервера.РеквизитыУчетнойЗаписи(КонтекстОтправки.УчетнаяЗапись).ЭлектроннаяПодписьВМоделиСервиса;
	
	СвойстваСертификатаПолучателя.Вставить("ЭтоЭлектроннаяПодписьВМоделиСервиса", ЭтоЭлектроннаяПодписьВМоделиСервиса);
	
	// Отправка на стороне сервера без взаимодействия с клиентским контекстом поддерживается
	// только для электронной подписи в моделе сервиса.
	Если НЕ ЭтоЭлектроннаяПодписьВМоделиСервиса Тогда
		СообщениеОбОшибке = НСтр("ru = 'СформироватьТранспортнуюИнформациюДляСообщения(): на стороне сервера поддерживается работа только с электронной подписью в моделе сервиса'");
		Возврат;
	КонецЕсли;
	
	СтрокаBase64 = КриптографияЭДКОСлужебныйВызовСервера.ЭкспортироватьСертификатВBase64(СвойстваСертификатаПолучателя);
	
	СформироватьТранспортнуюИнформациюИСохранитьВИБ(КонтекстОтправки.ТранспортноеСообщение, СтрокаBase64);
		
КонецПроцедуры

Функция СформироватьТранспортнуюИнформациюИСохранитьВИБ(Сообщение, СертификатBase64) Экспорт
	
	// генерируем файл
	КороткоеИмяФайла = Неопределено;
	ФайлТранспортнаяИнформация = СформироватьТранспортнуюИнформацию(Сообщение, СертификатBase64, КороткоеИмяФайла);
	Если ФайлТранспортнаяИнформация = Неопределено Тогда
		Возврат Ложь;
	КонецЕсли;
	
	// загружаем файл в содержимое
	ДобавитьСодержимоеТранспортногоКонтейнера(Сообщение,
												Перечисления.ТипыСодержимогоТранспортногоКонтейнера.ТранспортнаяИнформация,
												ФайлТранспортнаяИнформация,
												КороткоеИмяФайла,
												,
												,
												,
												,
												,
												,
												СгенерироватьUUID(),
												Перечисления.ТипыСодержимогоФайлов.xml);
	
	Возврат Истина;
	
КонецФункции

Функция СформироватьТранспортнуюИнформацию(Сообщение, СертификатBase64, КороткоеИмяФайла = Неопределено)
	
	// создаем дерево описания XML
	Дерево = СоздатьДеревоXML();
	
	// добавляем узел "датаВремяОтправки"
	УзелДатаВремяОтправки = ДобавитьУзелXML(Дерево, "датаВремяОтправки", , XMLСтрока(ТекущаяДатаСеанса()));
	
	// добавляем узел "сертификатПолучателя"
	УзелСертификатПолучателя = ДобавитьУзелXML(Дерево, "сертификатПолучателя", , XMLСтрока(СертификатBase64));
	
	// формируем строку XML на основе дерева
	СтрокаXML = ВыгрузитьДеревоВXML_ПФР(Дерево, "транспортнаяИнформация");
	
	// записываем строку в файл
	КороткоеИмяФайла = СгенерироватьИмяФайлаДокументаВСоставеПакета();
	ПолноеИмяФайлаДокументаВСоставеПакета = ПолучитьКаталогВременныхФайлов() + КороткоеИмяФайла;
	РезультатЗаписи = ЗаписатьXMLВФайл(СтрокаXML, ПолноеИмяФайлаДокументаВСоставеПакета);
	Если РезультатЗаписи = Ложь Тогда
		Возврат Неопределено;
	КонецЕсли;
	
	Возврат ПолноеИмяФайлаДокументаВСоставеПакета;
	
КонецФункции

Процедура СформироватьТранспортнуюИнформациюДляСообщенияПослеВыполнения(КонтекстОтправки, СообщениеОбОшибке)

	// Внимание!
	// ---------
	// В Контейнере есть одноименная процедура, которая используется при отправке отчетов
	// в интерактивном режиме пользователем.
	// При изменениях необходимо синхронно менять обе процедуры.
	
	СформироватьПакетПФР_Подписание(КонтекстОтправки, СообщениеОбОшибке);
	Если ЗначениеЗаполнено(СообщениеОбОшибке) Тогда
		Возврат;
	КонецЕсли;
	
	СформироватьПакетПФРПослеПодписания(КонтекстОтправки, СообщениеОбОшибке);
	
КонецПроцедуры

Функция СформироватьПодтверждениеПолученияДляСообщения(Контекст) Экспорт
	
	ТранспортноеСообщение = Контекст.ТранспортноеСообщение;

	ТипыВложений = Новый Массив;
	ТипыВложений.Добавить(Перечисления.ТипыСодержимогоТранспортногоКонтейнера.ПодтверждениеПолученияПисьма);
	ТипыВложений.Добавить(Перечисления.ТипыСодержимогоТранспортногоКонтейнера.ЭлектронноЦифроваяПодпись);
	Вложения = ПолучитьВложенияТранспортногоСообщения(ТранспортноеСообщение, Истина, ТипыВложений);
	
	// проверяем, не сформировано ли ранее подтверждение получения письма
	Если Вложения.Найти(Перечисления.ТипыСодержимогоТранспортногоКонтейнера.ПодтверждениеПолученияПисьма, "Тип") <> Неопределено Тогда
		Возврат Истина;
	КонецЕсли;
	
	// генерируем файл
	КороткоеИмяФайла = Неопределено;
	ФайлОписаниеПисьма = СформироватьПодтверждениеПолучения(ТранспортноеСообщение, Вложения, КороткоеИмяФайла);
	Если ФайлОписаниеПисьма = Неопределено Тогда
		Возврат Ложь;
	КонецЕсли;
	
	// загружаем файл в содержимое
	ДобавитьСодержимоеТранспортногоКонтейнера(ТранспортноеСообщение, Перечисления.ТипыСодержимогоТранспортногоКонтейнера.ПодтверждениеПолученияПисьма, ФайлОписаниеПисьма, КороткоеИмяФайла, , , , , , , СгенерироватьUUID(), Перечисления.ТипыСодержимогоФайлов.xml);
	
	Возврат Истина;
	
КонецФункции

Функция СформироватьПодтверждениеПолучения(Сообщение, Вложения, КороткоеИмяФайла = Неопределено)
	
	///////////////////////////////////////////////////////////////////////////////
	// ЗАГРУЗКА ОПИСАНИЯ ПИСЬМА ИЗ ВХОДЯЩЕГО СООБЩЕНИЯ
	
	// получаем описание письма из входящего сообщения
	ОписанияПисьма = ПолучитьВложенияТранспортногоСообщения(Сообщение.Основание, Истина, Перечисления.ТипыСодержимогоТранспортногоКонтейнера.ОписаниеПисьма);
	Если ОписанияПисьма.Количество() = 0 Тогда
		ТекстСообщения = НСтр("ru = 'Не удалось сформировать подтверждение получения письма: среди содержимого исходного письма не обнаружено обязательное описание.'");
		ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
		Возврат Неопределено;
	КонецЕсли;
	ОписаниеПисьма = ОписанияПисьма[0];
	
	// выгружаем данные во временный файл
	ВремФайлОписаниеПисьма = ПолучитьИмяВременногоФайла();
	ОписаниеПисьма.Данные.Получить().Записать(ВремФайлОписаниеПисьма);
	
	// загружаем в дерево
	ДеревоОписаниеПисьма = ЗагрузитьXMLВДеревоЗначений(ВремФайлОписаниеПисьма);
	ОперацииСФайламиЭДКО.УдалитьВременныйФайл(ВремФайлОписаниеПисьма);
	
	///////////////////////////////////////////////////////////////////////////////
	// АНАЛИЗ ОПИСАНИЯ ПИСЬМА ИЗ ВХОДЯЩЕГО СООБЩЕНИЯ
	
	// находим узел "описаниеПисьма"
	УзелОписаниеПисьма = ДеревоОписаниеПисьма.Строки.Найти("описаниеПисьма", "Имя");
	Если УзелОписаниеПисьма = Неопределено Тогда
		ТекстСообщения = НСтр("ru = 'Не удалось сформировать подтверждение получения письма: некорректное описание письма в исходном сообщении.'");
		ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
		Возврат Неопределено;
	КонецЕсли;
	
	// находим узел "идентификаторПисьма"
	УзелИдентификаторПисьма = УзелОписаниеПисьма.Строки.Найти("идентификаторПисьма", "Имя");
	Если УзелИдентификаторПисьма = Неопределено Тогда
		ТекстСообщения = НСтр("ru = 'Не удалось сформировать подтверждение получения письма: некорректное описание письма в исходном сообщении.'");
		ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
		Возврат Неопределено;
	КонецЕсли;
	ИдентификаторПисьма = УзелИдентификаторПисьма.Значение;
	
	///////////////////////////////////////////////////////////////////////////////
	// СОЗДАНИЕ И ЗАПОЛНЕНИЕ ДЕРЕВА
	
	// создаем дерево описания XML
	Дерево = СоздатьДеревоXML();
	
	// добавляем узел "датаВремяПолучения"
	ДобавитьУзелXML(Дерево, "датаВремяПолучения", , XMLСтрока(ТекущаяДатаСеанса()));
	
	// добавляем узел "идентификаторПисьма"
	ДобавитьУзелXML(Дерево, "идентификаторПисьма", , XMLСтрока(ИдентификаторПисьма));
	
	// добавляем узел "телоПисьма"
	УзелТелоПисьма = ДобавитьУзелXML(Дерево, "телоПисьма");
	
	ИсходныйУзелТелоПисьма = УзелОписаниеПисьма.Строки.Найти("телоПисьма", "Имя");
	Если ИсходныйУзелТелоПисьма = Неопределено Тогда
		ТекстСообщения = НСтр("ru = 'Не удалось сформировать подтверждение получения письма: некорректное описание письма в исходном сообщении.'");

		ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
		Возврат Неопределено;
	КонецЕсли;
		
	ИсходныйУзелИдентификаторДокумента = ИсходныйУзелТелоПисьма.Строки.Найти("идентификаторДокумента", "Имя");
	Если ИсходныйУзелИдентификаторДокумента <> Неопределено Тогда
		ДобавитьУзелXML(УзелТелоПисьма, "идентификаторДокумента",, ИсходныйУзелИдентификаторДокумента.Значение);
	КонецЕсли;
	ИсходныйУзелПодписи = ИсходныйУзелТелоПисьма.Строки.Найти("подписи", "Имя");
	Если ИсходныйУзелПодписи <> Неопределено Тогда
		УзелТелоПисьмаПодписи = ДобавитьУзелXML(УзелТелоПисьма, "подписи");
		Для Каждого ИсходныйУзелПодпись Из ИсходныйУзелПодписи.Строки Цикл
			УзелТелоПисьмаПодпись = ДобавитьУзелXML(УзелТелоПисьмаПодписи, "подпись",, ИсходныйУзелПодпись.Значение);
			Для Каждого ИсходныйАтрибут Из ИсходныйУзелПодпись.Строки Цикл
				ДобавитьУзелXML(УзелТелоПисьмаПодпись, "роль", Истина, ИсходныйАтрибут.Значение);
			КонецЦикла;
		КонецЦикла;
	КонецЕсли;
	
	// списокПриложений
	ИсходныйУзелСписокПриложений = УзелОписаниеПисьма.Строки.Найти("списокПриложений", "Имя");
	Если ИсходныйУзелСписокПриложений <> Неопределено Тогда
		УзелСписокПриложений = ДобавитьУзелXML(Дерево, "списокПриложений");
		Для Каждого ИсходныйУзелПриложение Из ИсходныйУзелСписокПриложений.Строки Цикл
			УзелПриложение = ДобавитьУзелXML(УзелСписокПриложений, "приложение");
			ИсходныйУзелИдентификаторДокумента = ИсходныйУзелПриложение.Строки.Найти("идентификаторДокумента", "Имя");
			Если ИсходныйУзелИдентификаторДокумента <> Неопределено Тогда
				ДобавитьУзелXML(УзелПриложение, "идентификаторДокумента",, ИсходныйУзелИдентификаторДокумента.Значение);
			КонецЕсли;
			ИсходныйУзелИмяФайла = ИсходныйУзелПриложение.Строки.Найти("имяФайла", "Имя");
			Если ИсходныйУзелИмяФайла <> Неопределено Тогда
				ДобавитьУзелXML(УзелПриложение, "имяФайла",, ИсходныйУзелИмяФайла.Значение);
			КонецЕсли;

			ИсходныйУзелПодписи = ИсходныйУзелПриложение.Строки.Найти("подписи", "Имя");
			Если ИсходныйУзелПодписи <> Неопределено Тогда
				УзелПриложениеПодписи = ДобавитьУзелXML(УзелПриложение, "подписи");
				Для Каждого ИсходныйУзелПодпись Из ИсходныйУзелПодписи.Строки Цикл
					УзелПриложениеПодпись = ДобавитьУзелXML(УзелПриложениеПодписи, "подпись",, ИсходныйУзелПодпись.Значение);
					Для Каждого ИсходныйАтрибут Из ИсходныйУзелПодпись.Строки Цикл
						ДобавитьУзелXML(УзелПриложениеПодпись, "роль", Истина, ИсходныйАтрибут.Значение);
					КонецЦикла;
				КонецЦикла;
			КонецЕсли;
		КонецЦикла;
	КонецЕсли;
	
	// формируем строку XML на основе дерева
	СтрокаXML = ВыгрузитьДеревоВXML_ПФР(Дерево, "подтверждениеПолучения");
	
	// записываем строку в файл
	КороткоеИмяФайла = СгенерироватьИмяФайлаДокументаВСоставеПакета();
	ПолноеИмяФайлаДокументаВСоставеПакета = ПолучитьКаталогВременныхФайлов() + КороткоеИмяФайла;
	РезультатЗаписи = ЗаписатьXMLВФайл(СтрокаXML, ПолноеИмяФайлаДокументаВСоставеПакета);
	Если РезультатЗаписи = Ложь Тогда
		Возврат Неопределено;
	КонецЕсли;
	
	Возврат ПолноеИмяФайлаДокументаВСоставеПакета;
	
КонецФункции

Функция ОпределитьТипСодержимогоПриложенияКПисьму(СтрВложение) Экспорт
	
	Если ЗначениеЗаполнено(СтрВложение.ТипСодержимогоФайла) Тогда
		Результат = СтрВложение.ТипСодержимогоФайла;
	Иначе
		ТипСодержимого = ТипСодержимогоПоИмениФайла(СтрВложение.ИмяФайла, Истина);
		Если НЕ ЗначениеЗаполнено(ТипСодержимого) Тогда
			Результат = Перечисления.ТипыСодержимогоФайлов.Неизвестный;
		Иначе
			Результат = ТипСодержимого;
		КонецЕсли;
	КонецЕсли;
	
	Если Результат = Перечисления.ТипыСодержимогоФайлов.Неизвестный ИЛИ НЕ ЗначениеЗаполнено(Результат) Тогда
		Возврат "unknown";
	Иначе
		Возврат Результат;
	КонецЕсли;
	
КонецФункции

Функция ПредставлениеТипаСодержимогоФайла(ТипСодержимого)
	
	Если ТипСодержимого = Перечисления.ТипыСодержимогоФайлов.Текст866 Тогда
		Возврат "plain866";
	ИначеЕсли ТипСодержимого = Перечисления.ТипыСодержимогоФайлов.Текст1251 Тогда
		Возврат "plain1251";
	ИначеЕсли ТипСодержимого = Перечисления.ТипыСодержимогоФайлов.xml Тогда
		Возврат "xml";
	ИначеЕсли ТипСодержимого = Перечисления.ТипыСодержимогоФайлов.html Тогда
		Возврат "html";
	Иначе
		Возврат "unknown";
	КонецЕсли;
	
КонецФункции

Функция ОпределитьСвойстваПодписиПодТеломПисьма(Вложения)
	
	// находим тело письма
	СтрПисьмо = Вложения.Найти(Перечисления.ТипыСодержимогоТранспортногоКонтейнера.Письмо, "Тип");
	Если СтрПисьмо = Неопределено Тогда
		Возврат Неопределено;
	КонецЕсли;
	
	// находим подпись под телом письма
	СтрПодписьПодПисьмом = Вложения.Найти(СтрПисьмо.ИмяФайла, "ЭЦПИмяПодписанногоФайла");
	Если СтрПодписьПодПисьмом = Неопределено Тогда
		Возврат Неопределено;
	КонецЕсли;
	
	// возвращаем идентификатор документа подписи под телом письма
	Возврат СтрПодписьПодПисьмом;
	
КонецФункции

Функция СформироватьПодтверждениеПолученияЗапросаПФРДляСообщения(Сообщение) Экспорт
	
	// проверяем, не сформировано ли ранее подтверждение
	Вложения = ПолучитьВложенияТранспортногоСообщения(Сообщение.Основание, , Перечисления.ТипыСодержимогоТранспортногоКонтейнера.ПодтверждениеПолученияЗапросПФР);
	Если Вложения.Количество() > 0 Тогда
		Возврат Истина;
	КонецЕсли;
	
	// формируем комбинированную таблицу с вложениями
	Вложения = ПолучитьВложенияТранспортногоСообщения(Сообщение.Основание, , Перечисления.ТипыСодержимогоТранспортногоКонтейнера.ОтветНаЗапросПФР);
	Если Вложения.Количество() = 0 Тогда
		ТекстСообщения = НСтр("ru = 'Не обнаружен документ с ответом на запрос от ПФР в исходном сообщении.'");
		ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
		Возврат Ложь;
	КонецЕсли;
	ВложениеОтветНаЗапросПФР = Вложения[0];
	
	// генерируем файл
	КороткоеИмяФайла = Неопределено;
	ФайлПодтвержденияПолучения = СформироватьПодтверждениеПолученияЗапросаПФР(Сообщение, ВложениеОтветНаЗапросПФР.Идентификатор, КороткоеИмяФайла);
	Если ФайлПодтвержденияПолучения = Неопределено Тогда
		Возврат Ложь;
	КонецЕсли;
	
	// загружаем файл в содержимое
	ДобавитьСодержимоеТранспортногоКонтейнера(Сообщение, Перечисления.ТипыСодержимогоТранспортногоКонтейнера.ПодтверждениеПолученияЗапросПФР, ФайлПодтвержденияПолучения, КороткоеИмяФайла, , , , , , , СгенерироватьUUID(), Перечисления.ТипыСодержимогоФайлов.xml);
	
	Возврат Истина;
	
КонецФункции

Функция СформироватьПодтверждениеПолученияЗапросаПФР(Сообщение, ИдентификаторФайлаОтветаНаЗапрос, КороткоеИмяФайла = Неопределено)
	
	///////////////////////////////////////////////////////////////////////////////
	// СОЗДАНИЕ И ЗАПОЛНЕНИЕ ДЕРЕВА
	
	// создаем дерево описания XML
	Дерево = СоздатьДеревоXML();
	
	// добавляем узел "датаВремяПолучения"
	ДобавитьУзелXML(Дерево, "датаВремяПолучения", , XMLСтрока(ТекущаяДатаСеанса()));
	
	// добавляем узел "идентификаторПисьма"
	ДобавитьУзелXML(Дерево, "идентификаторДокумента", , XMLСтрока(ИдентификаторФайлаОтветаНаЗапрос));
	
	// формируем строку XML на основе дерева
	СтрокаXML = ВыгрузитьДеревоВXML_ПФР(Дерево, "подтверждениеПолучения");
	
	// записываем строку в файл
	КороткоеИмяФайла = СгенерироватьИмяФайлаДокументаВСоставеПакета();
	ПолноеИмяФайлаДокументаВСоставеПакета = ПолучитьКаталогВременныхФайлов() + КороткоеИмяФайла;
	РезультатЗаписи = ЗаписатьXMLВФайл(СтрокаXML, ПолноеИмяФайлаДокументаВСоставеПакета);
	Если РезультатЗаписи = Ложь Тогда
		Возврат Неопределено;
	КонецЕсли;
	
	Возврат ПолноеИмяФайлаДокументаВСоставеПакета;
	
КонецФункции

Функция СформироватьПакетПФР_СформироватьОписаниеДокументовПакета(Контекст) Экспорт
	
	//
	Сообщение = Контекст.ТранспортноеСообщение;
	
	// определяем структуру с отложенными командами
	ОтложенныеКоманды = Новый СписокЗначений;
	
	// создаем вспомогательное дерево описания документов пакета
	ДеревоДокументов = СоздатьДеревоОписанияДокументовПакета();
	
	ФорматДокументооборота = ?(Контекст.Свойство("ФорматДокументооборота"),
		Контекст.ФорматДокументооборота, Неопределено);
	Если ФорматДокументооборота = Перечисления.ФорматыДокументооборотаСПФР.ЭДОК Тогда
		Для каждого ДвДанныеСертификатаПолучателя Из Контекст.ДвДанныеСертификатаПолучателя Цикл
			Если ДвДанныеСертификатаПолучателя = Неопределено Тогда
				ТекстСообщения = НСтр("ru = 'Ошибка формирования пакета: не удалось получить сертификат шифрования ПФР.'");
				ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
				Возврат Ложь;
			КонецЕсли;
		КонецЦикла;
		
		ИндексСертификатПолучателя = 0;
		Для каждого ОбъектСертификатПолучателя Из Контекст.ОбъектСертификатПолучателя Цикл
			ДвДанныеСертификатаПолучателя = Контекст.ДвДанныеСертификатаПолучателя[ИндексСертификатПолучателя];
			
			ДобавитьСодержимоеТранспортногоКонтейнера(
				Сообщение,
				Перечисления.ТипыСодержимогоТранспортногоКонтейнера.Сертификат,
				ДвДанныеСертификатаПолучателя,
				ОбъектСертификатПолучателя.Отпечаток + ".der",,,,,,,
				СгенерироватьUUID(),
				Перечисления.ТипыСодержимогоФайлов.Неизвестный);
			
			ИндексСертификатПолучателя = ИндексСертификатПолучателя + 1;
		КонецЦикла;
	КонецЕсли;
	
	// создаем временный каталог для выгрузки файлов
	КаталогФайловОтчетности = ОперацииСФайламиЭДКО.СоздатьВременныйКаталог();
	
	//
	Если Контекст.ТипСообщения = Перечисления.ТипыТранспортныхСообщений.ПервичноеСообщениеСодержащееОтчетностьПФР Тогда
		
		ТипыСодержимого = Новый Массив;
		ТипыСодержимого.Добавить(Перечисления.ТипыСодержимогоТранспортногоКонтейнера.ФайлОтчетностиПФР);
		ТипыСодержимого.Добавить(Перечисления.ТипыСодержимогоТранспортногоКонтейнера.СведенияПриложениеПФР);
		ТипыСодержимого.Добавить(Перечисления.ТипыСодержимогоТранспортногоКонтейнера.ФайлОписанияСведенийПФР);
		ТипыСодержимого.Добавить(Перечисления.ТипыСодержимогоТранспортногоКонтейнера.ЭлектронноЦифроваяПодпись);
		Если ФорматДокументооборота = Перечисления.ФорматыДокументооборотаСПФР.ЭДОК Тогда
			ТипыСодержимого.Добавить(Перечисления.ТипыСодержимогоТранспортногоКонтейнера.Сертификат);
		КонецЕсли;
		ВложенияДИВ = ПолучитьВложенияТранспортногоСообщения(Сообщение, Истина, ТипыСодержимого);
		ВложенияДИВ.Колонки.Добавить("Адрес");
		Для Каждого ВложениеДИВ Из ВложенияДИВ Цикл
			Попытка
				ВложениеДИВ.Адрес = ПоместитьВоВременноеХранилище(ВложениеДИВ.Данные.Получить(), Контекст.УникальныйИдентификатор);
			Исключение
				ОперацииСФайламиЭДКО.УдалитьВременныйФайл(КаталогФайловОтчетности);
				ТекстСообщения = НСтр("ru = 'Ошибка формирования пакета: не удалось сохранить документ во временный каталог для подписания.'");
				ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
				Возврат Ложь;
			КонецПопытки;
		КонецЦикла;
		
		// заполняем дерево описания сначала отчетами, приложениями и описанием сведений
		Для Каждого ВложениеДИВ Из ВложенияДИВ Цикл
			
			Если ВложениеДИВ.Тип <> Перечисления.ТипыСодержимогоТранспортногоКонтейнера.ФайлОтчетностиПФР
			И ВложениеДИВ.Тип <> Перечисления.ТипыСодержимогоТранспортногоКонтейнера.ФайлОписанияСведенийПФР
			И ВложениеДИВ.Тип <> Перечисления.ТипыСодержимогоТранспортногоКонтейнера.СведенияПриложениеПФР
			И ВложениеДИВ.Тип <> Перечисления.ТипыСодержимогоТранспортногоКонтейнера.Сертификат Тогда
				Продолжить;
			КонецЕсли;
			
			Если ВложениеДИВ.Тип = Перечисления.ТипыСодержимогоТранспортногоКонтейнера.Сертификат Тогда
				КороткоеИмяФайлаИлиАрхива = ВложениеДИВ.ИмяФайла;
				
			Иначе
				// пакуем файл
				ТипАрхива = Неопределено; // zip, по умолчанию
				ИмяФайлаАрхива = "file";
				Если ВложениеДИВ.Тип <> Перечисления.ТипыСодержимогоТранспортногоКонтейнера.ФайлОписанияСведенийПФР
					И ФорматДокументооборота = Перечисления.ФорматыДокументооборотаСПФР.ЭДОК Тогда
					
					ТипАрхива = "gzip";
					ИмяФайлаАрхива = ВложениеДИВ.ИмяФайла;
				КонецЕсли;
				
				КороткоеИмяФайлаИлиАрхива = ЗаархивироватьФайлПакета(
					ВложениеДИВ,
					ИмяФайлаАрхива,
					Контекст.УникальныйИдентификатор,
					ТипАрхива);
				Если КороткоеИмяФайлаИлиАрхива = Неопределено Тогда
					ОперацииСФайламиЭДКО.УдалитьВременныйФайл(КаталогФайловОтчетности);
					Возврат Ложь;
				КонецЕсли;
				
				// шифруем файл
				ОтложенныеКоманды.Добавить(ВложениеДИВ.Адрес, "Зашифровать");
			КонецЕсли;
			



			// определяем документ в дереве описания
			ТипДокумента = ?(ВложениеДИВ.Тип = Перечисления.ТипыСодержимогоТранспортногоКонтейнера.ФайлОтчетностиПФР, ВложениеДИВ.ТипФайлаОтчетностиПФР, ВложениеДИВ.Тип);
			СтрДокумент = ОпределитьДокументВДеревеОписания(
				ДеревоДокументов,
				ТипДокумента,
				ТипСодержимогоПоОписаниюДИВ(ВложениеДИВ, Истина),
				ВложениеДИВ.Идентификатор,
				ВложениеДИВ.Тип <> Перечисления.ТипыСодержимогоТранспортногоКонтейнера.Сертификат,
				ВложениеДИВ.Тип <> Перечисления.ТипыСодержимогоТранспортногоКонтейнера.Сертификат,
				ВложениеДИВ.ИмяФайла,
				КороткоеИмяФайлаИлиАрхива,
				ВложениеДИВ.Адрес);
			
		КонецЦикла;
		
	ИначеЕсли Сообщение.Тип = Перечисления.ТипыТранспортныхСообщений.ПротоколКвитанцияПФР Тогда
		
		// выгружаем в него все файлы сообщения
		ТипыСодержимого = Новый Массив;
		ТипыСодержимого.Добавить(Перечисления.ТипыСодержимогоТранспортногоКонтейнера.ЭлектронноЦифроваяПодпись);
		ВложенияДИВ = ПолучитьВложенияТранспортногоСообщения(Сообщение, Истина, ТипыСодержимого);
		ВложенияДИВ.Колонки.Добавить("Адрес");
		Для Каждого ВложениеДИВ Из ВложенияДИВ Цикл
			Попытка
				ВложениеДИВ.Адрес = ПоместитьВоВременноеХранилище(ВложениеДИВ.Данные.Получить(), Контекст.УникальныйИдентификатор);
			Исключение
				ОперацииСФайламиЭДКО.УдалитьВременныйФайл(КаталогФайловОтчетности);
				ТекстСообщения = НСтр("ru = 'Ошибка формирования пакета: не удалось сохранить файлы пакета во временный каталог.'");
				ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
				Возврат Ложь;
			КонецПопытки;
		КонецЦикла;
		
		// извлекаем из сообщения-основания информацию о вложениях и заполняем дерево описания сначала ДИВами
		ДИВыОснования = ПолучитьВложенияТранспортногоСообщения(Сообщение.Основание, , , ВложенияДИВ.ВыгрузитьКолонку("ЭЦПИмяПодписанногоФайла"));
		Для Каждого СтрДИВОснование Из ДИВыОснования Цикл
			ОпределитьДокументВДеревеОписания(ДеревоДокументов, СтрДИВОснование.Тип, ТипСодержимогоПоОписаниюДИВ(СтрДИВОснование, Истина), СтрДИВОснование.Идентификатор, Истина, Истина, СтрДИВОснование.ИмяФайла, , ВложениеДИВ.Адрес);
		КонецЦикла;
		
	ИначеЕсли Контекст.ТипСообщения = Перечисления.ТипыТранспортныхСообщений.ПисьмоИсходящееПФР Тогда
		
		ТипыСодержимого = Новый Массив;
		ТипыСодержимого.Добавить(Перечисления.ТипыСодержимогоТранспортногоКонтейнера.Письмо);
		ТипыСодержимого.Добавить(Перечисления.ТипыСодержимогоТранспортногоКонтейнера.ПисьмоПриложение);
		ТипыСодержимого.Добавить(Перечисления.ТипыСодержимогоТранспортногоКонтейнера.ОписаниеПисьма);
		ТипыСодержимого.Добавить(Перечисления.ТипыСодержимогоТранспортногоКонтейнера.ТранспортнаяИнформация);
		ТипыСодержимого.Добавить(Перечисления.ТипыСодержимогоТранспортногоКонтейнера.ЭлектронноЦифроваяПодпись);
		ВложенияДИВ = ПолучитьВложенияТранспортногоСообщения(Сообщение, Истина, ТипыСодержимого);
		ВложенияДИВ.Колонки.Добавить("Адрес");
		Для Каждого ВложениеДИВ Из ВложенияДИВ Цикл
			Попытка
				ВложениеДИВ.Адрес = ПоместитьВоВременноеХранилище(ВложениеДИВ.Данные.Получить(), Контекст.УникальныйИдентификатор);
			Исключение
				ОперацииСФайламиЭДКО.УдалитьВременныйФайл(КаталогФайловОтчетности);
				ТекстСообщения = НСтр("ru = 'Ошибка формирования пакета: не удалось сохранить документ во временный каталог для подписания.'");
				ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
				Возврат Ложь;
			КонецПопытки;
		КонецЦикла;
		
		// заполняем дерево описания ДИВ'ами
		Для Каждого ВложениеДИВ Из ВложенияДИВ Цикл
			
			Если ВложениеДИВ.Тип = Перечисления.ТипыСодержимогоТранспортногоКонтейнера.ЭлектронноЦифроваяПодпись Тогда
				Продолжить;
			КонецЕсли;
			
			// пакуем файл
			КороткоеИмяФайлаАрхива = ЗаархивироватьФайлПакета(ВложениеДИВ, "file", Контекст.УникальныйИдентификатор);
			Если КороткоеИмяФайлаАрхива = Неопределено Тогда
				ОперацииСФайламиЭДКО.УдалитьВременныйФайл(КаталогФайловОтчетности);
				Возврат Ложь;
			КонецЕсли;
			
			Если ВложениеДИВ.Тип <> Перечисления.ТипыСодержимогоТранспортногоКонтейнера.ОписаниеПисьма
			И ВложениеДИВ.Тип <> Перечисления.ТипыСодержимогоТранспортногоКонтейнера.ТранспортнаяИнформация Тогда
				
				// шифруем файл
				ОтложенныеКоманды.Добавить(ВложениеДИВ.Адрес, "Зашифровать");
				
				// добавляем в дерево описания
				Если ВложениеДИВ.Тип = Перечисления.ТипыСодержимогоТранспортногоКонтейнера.Письмо Тогда
					ТипСодержимого = "plain1251";
				ИначеЕсли ВложениеДИВ.Тип = Перечисления.ТипыСодержимогоТранспортногоКонтейнера.ПисьмоПриложение Тогда
					ТипСодержимого = ОпределитьТипСодержимогоПриложенияКПисьму(ВложениеДИВ);
				КонецЕсли;
				СтрДокумент = ОпределитьДокументВДеревеОписания(ДеревоДокументов, ВложениеДИВ.Тип, ТипСодержимого, ВложениеДИВ.Идентификатор, Истина, Истина, ВложениеДИВ.ИмяФайла, КороткоеИмяФайлаАрхива, ВложениеДИВ.Адрес);
				
			Иначе
				// определяем документ в дереве описания
				СтрДокумент = ОпределитьДокументВДеревеОписания(ДеревоДокументов, ВложениеДИВ.Тип, "xml", ВложениеДИВ.Идентификатор, Истина, Ложь, ВложениеДИВ.ИмяФайла, КороткоеИмяФайлаАрхива, ВложениеДИВ.Адрес);
			КонецЕсли;
			
		КонецЦикла;
		
	ИначеЕсли Контекст.ТипСообщения = Перечисления.ТипыТранспортныхСообщений.ПисьмоВходящееКвитанцияПФР Тогда
		
		// выгружаем в него все файлы сообщения
		ТипыСодержимого = Новый Массив;
		ТипыСодержимого.Добавить(Перечисления.ТипыСодержимогоТранспортногоКонтейнера.ПодтверждениеПолученияПисьма);
		ТипыСодержимого.Добавить(Перечисления.ТипыСодержимогоТранспортногоКонтейнера.ЭлектронноЦифроваяПодпись);
		ВложенияДИВ = ПолучитьВложенияТранспортногоСообщения(Сообщение, Истина, ТипыСодержимого);
		ВложенияДИВ.Колонки.Добавить("Адрес");
		Для Каждого ВложениеДИВ Из ВложенияДИВ Цикл
			Попытка
				ВложениеДИВ.Адрес = ПоместитьВоВременноеХранилище(ВложениеДИВ.Данные.Получить(), Контекст.УникальныйИдентификатор);
			Исключение
				ОперацииСФайламиЭДКО.УдалитьВременныйФайл(КаталогФайловОтчетности);
				ТекстСообщения = НСтр("ru = 'Ошибка формирования пакета: не удалось сохранить файлы пакета во временный каталог.'");
				ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
				Возврат Ложь;
			КонецПопытки;
		КонецЦикла;
		
		// дополняем дерево информацией об подтверждении получения
		ВложенияПодтвержденияПолучения = ВложенияДИВ.НайтиСтроки(Новый Структура("Тип", Перечисления.ТипыСодержимогоТранспортногоКонтейнера.ПодтверждениеПолученияПисьма));
		Для Каждого СтрВложение Из ВложенияПодтвержденияПолучения Цикл
			
			// пакуем файл
			КороткоеИмяФайлаАрхива = ЗаархивироватьФайлПакета(СтрВложение, "file", Контекст.УникальныйИдентификатор);
			Если КороткоеИмяФайлаАрхива = Неопределено Тогда
				ОперацииСФайламиЭДКО.УдалитьВременныйФайл(КаталогФайловОтчетности);
				Возврат Ложь;
			КонецЕсли;
			
			// шифруем файл
			ОтложенныеКоманды.Добавить(СтрВложение.Адрес, "Зашифровать");
			
			// дополняем дерево описания
			ОпределитьДокументВДеревеОписания(ДеревоДокументов, СтрВложение.Тип, "xml", СтрВложение.Идентификатор, Истина, Истина, СтрВложение.ИмяФайла, КороткоеИмяФайлаАрхива, СтрВложение.Адрес);
			
		КонецЦикла;
		
	//
	ИначеЕсли Контекст.ТипСообщения = Перечисления.ТипыТранспортныхСообщений.ПервичноеСообщениеСодержащееЗапросПФР Тогда
		
		ТипыСодержимого = Новый Массив;
		ТипыСодержимого.Добавить(Перечисления.ТипыСодержимогоТранспортногоКонтейнера.ЗапросИОС);
		ТипыСодержимого.Добавить(Перечисления.ТипыСодержимогоТранспортногоКонтейнера.ФайлОписанияЗапросаПФР);
		ТипыСодержимого.Добавить(Перечисления.ТипыСодержимогоТранспортногоКонтейнера.ЭлектронноЦифроваяПодпись);
		ВложенияДИВ = ПолучитьВложенияТранспортногоСообщения(Сообщение, Истина, ТипыСодержимого);
		ВложенияДИВ.Колонки.Добавить("Адрес");
		Для Каждого ВложениеДИВ Из ВложенияДИВ Цикл
			Попытка
				ВложениеДИВ.Адрес = ПоместитьВоВременноеХранилище(ВложениеДИВ.Данные.Получить(), Контекст.УникальныйИдентификатор);
			Исключение
				ОперацииСФайламиЭДКО.УдалитьВременныйФайл(КаталогФайловОтчетности);
				ТекстСообщения = НСтр("ru = 'Ошибка формирования пакета: не удалось сохранить документ во временный каталог для подписания.'");
				ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
				Возврат Ложь;
			КонецПопытки;
		КонецЦикла;
		
		// заполняем дерево описания сначала отчетами и описанием сведений
		Для Каждого ВложениеДИВ Из ВложенияДИВ Цикл
			
			Если ВложениеДИВ.Тип <> Перечисления.ТипыСодержимогоТранспортногоКонтейнера.ЗапросИОС
			И ВложениеДИВ.Тип <> Перечисления.ТипыСодержимогоТранспортногоКонтейнера.ФайлОписанияЗапросаПФР Тогда
				Продолжить;
			КонецЕсли;
			
			// пакуем файл
			КороткоеИмяФайлаАрхива = ЗаархивироватьФайлПакета(ВложениеДИВ, "file", Контекст.УникальныйИдентификатор);
			Если КороткоеИмяФайлаАрхива = Неопределено Тогда
				ОперацииСФайламиЭДКО.УдалитьВременныйФайл(КаталогФайловОтчетности);
				Возврат Ложь;
			КонецЕсли;
			
			// шифруем файл
			ОтложенныеКоманды.Добавить(ВложениеДИВ.Адрес, "Зашифровать");
			
			// определяем документ в дереве описания
			СтрДокумент = ОпределитьДокументВДеревеОписания(ДеревоДокументов, ВложениеДИВ.Тип, "xml", ВложениеДИВ.Идентификатор, Истина, Истина, ВложениеДИВ.ИмяФайла, КороткоеИмяФайлаАрхива, ВложениеДИВ.Адрес);
			
		КонецЦикла;
		
	ИначеЕсли Сообщение.Тип = Перечисления.ТипыТранспортныхСообщений.ОтветНаЗапросКвитанцияПФР Тогда
		
		// выгружаем в него все файлы сообщения
		ТипыСодержимого = Новый Массив;
		ТипыСодержимого.Добавить(Перечисления.ТипыСодержимогоТранспортногоКонтейнера.ПодтверждениеПолученияЗапросПФР);
		ТипыСодержимого.Добавить(Перечисления.ТипыСодержимогоТранспортногоКонтейнера.ЭлектронноЦифроваяПодпись);
		ВложенияДИВ = ПолучитьВложенияТранспортногоСообщения(Сообщение, Истина, ТипыСодержимого);
		ВложенияДИВ.Колонки.Добавить("Адрес");
		Для Каждого ВложениеДИВ Из ВложенияДИВ Цикл
			Попытка
				ВложениеДИВ.Адрес = ПоместитьВоВременноеХранилище(ВложениеДИВ.Данные.Получить(), Контекст.УникальныйИдентификатор);
			Исключение
				ОперацииСФайламиЭДКО.УдалитьВременныйФайл(КаталогФайловОтчетности);
				ТекстСообщения = НСтр("ru = 'Ошибка формирования пакета: не удалось сохранить файлы пакета во временный каталог.'");
				ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
				Возврат Ложь;
			КонецПопытки;
		КонецЦикла;
		
		// извлекаем из сообщения-основания информацию о вложениях
		ДИВыОснования = ПолучитьВложенияТранспортногоСообщения(Сообщение.Основание, , , ВложенияДИВ.ВыгрузитьКолонку("ЭЦПИмяПодписанногоФайла"));
		
		// заполняем дерево описания сначала ДИВами
		Для Каждого СтрДИВОснование Из ДИВыОснования Цикл
			ОпределитьДокументВДеревеОписания(ДеревоДокументов, СтрДИВОснование.Тип, ПредставлениеТипаСодержимогоФайла(СтрДИВОснование.ТипСодержимогоФайла), СтрДИВОснование.Идентификатор, Истина, Истина, СтрДИВОснование.ИмяФайла, , ВложениеДИВ.Адрес);
		КонецЦикла;
		
		// дополняем дерево информацией об подтверждении получения
		ВложенияПодтвержденияПолучения = ВложенияДИВ.НайтиСтроки(Новый Структура("Тип", Перечисления.ТипыСодержимогоТранспортногоКонтейнера.ПодтверждениеПолученияЗапросПФР));
		Для Каждого СтрВложение Из ВложенияПодтвержденияПолучения Цикл
			
			// пакуем файл
			КороткоеИмяФайлаАрхива = ЗаархивироватьФайлПакета(ВложениеДИВ, "file", Контекст.УникальныйИдентификатор);
			Если КороткоеИмяФайлаАрхива = Неопределено Тогда
				ОперацииСФайламиЭДКО.УдалитьВременныйФайл(КаталогФайловОтчетности);
				Возврат Ложь;
			КонецЕсли;
			
			// шифруем файл
			ОтложенныеКоманды.Добавить(ВложениеДИВ.Адрес, "Зашифровать");
			
			// дополняем дерево описания
			ОпределитьДокументВДеревеОписания(ДеревоДокументов, СтрВложение.Тип, "xml", СтрВложение.Идентификатор, Истина, Истина, СтрВложение.ИмяФайла, КороткоеИмяФайлаАрхива, ВложениеДИВ.Адрес);
			
		КонецЦикла;
		
	КонецЕсли;
	
	// дополняем дерево описания информацией об ЭЦП
	Для Каждого ВложениеДИВ Из ВложенияДИВ Цикл
		Если ВложениеДИВ.Тип = Перечисления.ТипыСодержимогоТранспортногоКонтейнера.ЭлектронноЦифроваяПодпись Тогда
			ПодписанныйДокумент = ДеревоДокументов.Строки.Найти(ВложениеДИВ.ЭЦПИмяПодписанногоФайла, "ИмяФайлаДокумента");
			Если ПодписанныйДокумент <> Неопределено Тогда
				ОпределитьДокументВДеревеОписания(ПодписанныйДокумент, , , , , , ВложениеДИВ.ИмяФайла, , ВложениеДИВ.Адрес);
			КонецЕсли;
		КонецЕсли;
	КонецЦикла;
	
	// формируем файл описания пакета
	ДатаПакета = ТекущаяДатаСеанса();
	
	РеквизитыОператора = ЗначенияРеквизитовОбъекта(Контекст.УчетнаяЗапись,
		"ОператорНаименованиеКраткое, ОператорРегНомерПФР");
	
	ИнформацияОПакете = Новый Структура;
	ИнформацияОПакете.Вставить("UUIDПакета", 					Контекст.UUIDПакета);
	ИнформацияОПакете.Вставить("ТипДокументооборота", 			Контекст.ТипДокументооборота);
	ИнформацияОПакете.Вставить("ТипТранзакции", 				Контекст.ТипТранзакции);
	ИнформацияОПакете.Вставить("КодОтправителя", 				Контекст.КодОтправителя);
	ИнформацияОПакете.Вставить("КодПолучателя", 				Контекст.КодПолучателя);
	ИнформацияОПакете.Вставить("КодСистемыОтправителя", 		Контекст.КодСистемыОтправителя);
	ИнформацияОПакете.Вставить("ФорматДокументооборота", 		ФорматДокументооборота);
	ИнформацияОПакете.Вставить("СсылкаОтправитель", 			Контекст.СсылкаОтправитель);
	ИнформацияОПакете.Вставить("ДатаПакета", 					ДатаПакета);
	ИнформацияОПакете.Вставить("ОператорНаименованиеКраткое", 	РеквизитыОператора.ОператорНаименованиеКраткое);
	ИнформацияОПакете.Вставить("ОператорРегНомерПФР", 			РеквизитыОператора.ОператорРегНомерПФР);
	ТипСКЗИПакета = ТипСКЗИ(Контекст);
	ФайлОписаниеПакета = СформироватьОписаниеПакетаПФР(
		ДеревоДокументов,
		ИнформацияОПакете,
		ТипСКЗИПакета,
		Контекст.УникальныйИдентификатор);
	Если ФайлОписаниеПакета = Неопределено Тогда
		Возврат Ложь;
	КонецЕсли;
	
	// формируем команду подписания
	ОтложенныеКоманды.Добавить(ФайлОписаниеПакета, "ПодписатьФайлОписанияПакета");
	
	// 
	Контекст.Вставить("ОтложенныеКоманды", ОтложенныеКоманды);
	Контекст.Вставить("ДеревоДокументов", ПоместитьВоВременноеХранилище(ДеревоДокументов, Контекст.УникальныйИдентификатор));
	Контекст.Вставить("ФайлОписаниеПакета", ФайлОписаниеПакета);
	Контекст.Вставить("ДатаПакета", ДатаПакета);
	
	Возврат Истина;
	
КонецФункции

Процедура СформироватьПакетПФРПослеВыполненияОтложенныхКоманд(КонтекстОтправки, СообщениеОбОшибке)

	// Внимание!
	// ---------
	// В Контейнере есть аналогичная процедура СформироватьПакетПФРПослеВыполненияОтложенныхКомандНаКлиенте(),
	// которая используется при отправке отчетов в интерактивном режиме пользователем.
	// При изменениях необходимо синхронно менять обе процедуры.
	
	Если НЕ СформироватьПакетПФР_ЗаключительныеДействияНаСервере(КонтекстОтправки) Тогда // универсальная
		СообщениеОбОшибке = ТекстСообщенийПользователю();
		Возврат;
	КонецЕсли;
	
КонецПроцедуры

Функция СформироватьОписаниеПакетаПФР(
		ОписаниеДокументов,
		ИнформацияОПакете = Неопределено,
		типСКЗИ = Неопределено,
		ИдентификаторДляВременногоХранилища = Неопределено) Экспорт
	
	ИнформацияОПакетеВызова = Новый Структура;
	ИнформацияОПакетеВызова.Вставить("UUIDПакета", 					Неопределено);
	ИнформацияОПакетеВызова.Вставить("ТипДокументооборота", 		"СведенияПФР");
	ИнформацияОПакетеВызова.Вставить("ТипТранзакции", 				"сведения");
	ИнформацияОПакетеВызова.Вставить("КодОтправителя", 				"");
	ИнформацияОПакетеВызова.Вставить("КодПолучателя", 				"");
	ИнформацияОПакетеВызова.Вставить("КодСистемыОтправителя", 		Неопределено);
	ИнформацияОПакетеВызова.Вставить("ФорматДокументооборота", 		Неопределено);
	ИнформацияОПакетеВызова.Вставить("СсылкаОтправитель", 			Неопределено);
	ИнформацияОПакетеВызова.Вставить("ДатаПакета", 					Неопределено);
	ИнформацияОПакетеВызова.Вставить("ОператорНаименованиеКраткое", "");
	ИнформацияОПакетеВызова.Вставить("ОператорРегНомерПФР", 		"");
	Если ИнформацияОПакете <> Неопределено Тогда
		ЗаполнитьЗначенияСвойств(ИнформацияОПакетеВызова, ИнформацияОПакете);
	КонецЕсли;
	UUIDПакета 					= ИнформацияОПакетеВызова.UUIDПакета;
	ТипДокументооборота 		= ИнформацияОПакетеВызова.ТипДокументооборота;
	ТипТранзакции 				= ИнформацияОПакетеВызова.ТипТранзакции;
	КодОтправителя 				= СокрЛП(ИнформацияОПакетеВызова.КодОтправителя);
	КодПолучателя 				= ИнформацияОПакетеВызова.КодПолучателя;
	КодСистемыОтправителя 		= ИнформацияОПакетеВызова.КодСистемыОтправителя;
	ФорматДокументооборота 		= ИнформацияОПакетеВызова.ФорматДокументооборота;
	СсылкаОтправитель 			= ИнформацияОПакетеВызова.СсылкаОтправитель;
	ДатаПакета 					= ИнформацияОПакетеВызова.ДатаПакета;
	ОператорНаименованиеКраткое = ИнформацияОПакетеВызова.ОператорНаименованиеКраткое;
	ОператорРегНомерПФР 		= ИнформацияОПакетеВызова.ОператорРегНомерПФР;
	
	Если UUIDПакета = Неопределено Тогда
		UUIDПакета = СгенерироватьUUID(ФорматДокументооборота);
	КонецЕсли;
	
	Если НЕ ЗначениеЗаполнено(КодСистемыОтправителя) Тогда
		КодСистемыОтправителя = "1С";
	КонецЕсли;
	
	// создаем дерево описания XML
	Дерево = СоздатьДеревоXML();
	
	Если ФорматДокументооборота = Перечисления.ФорматыДокументооборотаСПФР.ЭДОК Тогда
		Если НЕ ЗначениеЗаполнено(ОператорРегНомерПФР) Тогда
			ТекстСообщения = НСтр("ru = 'Не заполнен регистрационный номер ПФР для оператора в расширенных настройках учетной записи документооборота'");
			ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
			Возврат Неопределено;
		КонецЕсли;
		
		Если НЕ ЗначениеЗаполнено(ОператорНаименованиеКраткое) Тогда
			ТекстСообщения = НСтр("ru = 'Не заполнено краткое наименование оператора в расширенных настройках учетной записи документооборота'");
			ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
			Возврат Неопределено;
		КонецЕсли;
		
		СведенияОбОрганизации = ОсновныеСведенияОбОрганизации(СсылкаОтправитель);
		
		Если СведенияОбОрганизации.ЭтоЮридическоеЛицо Тогда
			Если НЕ ЗначениеЗаполнено(СведенияОбОрганизации.НаименованиеСокращенное) Тогда
				ТекстСообщения = НСтр("ru = 'Не заполнено краткое наименование в свойствах организации'");
				ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
				Возврат Неопределено;
			КонецЕсли;
			
			Если НЕ ЗначениеЗаполнено(СведенияОбОрганизации.ИНН) Тогда
				ТекстСообщения = НСтр("ru = 'Не заполнен ИНН в свойствах организации'");
				ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
				Возврат Неопределено;
			КонецЕсли;
			
			Если НЕ ЗначениеЗаполнено(СведенияОбОрганизации.КПП) Тогда
				ТекстСообщения = НСтр("ru = 'Не заполнен КПП в свойствах организации'");
				ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
				Возврат Неопределено;
			КонецЕсли;
			
		Иначе
			Если НЕ ЗначениеЗаполнено(СведенияОбОрганизации.Фамилия) И НЕ ЗначениеЗаполнено(СведенияОбОрганизации.Имя)
				И НЕ ЗначениеЗаполнено(СведенияОбОрганизации.Отчество) Тогда
				ТекстСообщения = НСтр("ru = 'Не заполнены фамилия, имя, отчество физического лица'");
				ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
				Возврат Неопределено;
			КонецЕсли;
			
			Если НЕ ЗначениеЗаполнено(СведенияОбОрганизации.ИНН) Тогда
				ФИОИП = СведенияОбОрганизации.Фамилия
					+ ?(ЗначениеЗаполнено(СведенияОбОрганизации.Фамилия) И ЗначениеЗаполнено(СведенияОбОрганизации.Имя), " ", "")
					+ СведенияОбОрганизации.Имя
					+ ?((ЗначениеЗаполнено(СведенияОбОрганизации.Фамилия) ИЛИ ЗначениеЗаполнено(СведенияОбОрганизации.Имя)
					И ЗначениеЗаполнено(СведенияОбОрганизации.Отчество)), " ", "") + СведенияОбОрганизации.Отчество;
				
				ТекстСообщения = СтрШаблон(
					НСтр("ru = 'Не заполнен ИНН в свойствах физического лица ""%1""'"),
					ФИОИП);
				ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
				Возврат Неопределено;
			КонецЕсли;
		КонецЕсли;
		
		// добавляем узел "ЭДПФР"
		УзелЭДПФР = ДобавитьУзелXML(Дерево, "ЭДПФР");
		ДобавитьУзелXML(УзелЭДПФР, "xmlns", 								Истина, "http://пф.рф/ОСП/2019-11-14");
		ДобавитьУзелXML(УзелЭДПФР, "xmlns:УТ2", 							Истина, "http://пф.рф/УТ/2017-08-21");
		ДобавитьУзелXML(УзелЭДПФР, "xmlns:АФ4", 							Истина, "http://пф.рф/АФ/2017-08-21");
		
		// добавляем узел ОСП
		УзелОСП = ДобавитьУзелXML(УзелЭДПФР, "ОСП");
		
		// добавляем узел Оператор
		УзелОператор = ДобавитьУзелXML(УзелОСП, "Оператор");
		ДобавитьУзелXML(УзелОператор, "УТ2:РегНомер",, 						ОператорРегНомерПФР);
		ДобавитьУзелXML(УзелОператор, "УТ2:НаименованиеКраткое",, 			ОператорНаименованиеКраткое);
		
		// добавляем узел Страхователь
		УзелОператор = ДобавитьУзелXML(УзелОСП, "Страхователь");
		ДобавитьУзелXML(УзелОператор, "УТ2:РегНомер",, 						КодОтправителя);
		Если СведенияОбОрганизации.ЭтоЮридическоеЛицо Тогда
			ДобавитьУзелXML(УзелОператор, "УТ2:НаименованиеКраткое",, 		СведенияОбОрганизации.НаименованиеСокращенное);
			
		Иначе
			УзелФИО = ДобавитьУзелXML(УзелОператор, "УТ2:ФИО");
			Если ЗначениеЗаполнено(СведенияОбОрганизации.Фамилия) Тогда
				ДобавитьУзелXML(УзелФИО, "УТ2:Фамилия",, 					СведенияОбОрганизации.Фамилия);
			КонецЕсли;
			Если ЗначениеЗаполнено(СведенияОбОрганизации.Имя) Тогда
				ДобавитьУзелXML(УзелФИО, "УТ2:Имя",, 						СведенияОбОрганизации.Имя);
			КонецЕсли;
			Если ЗначениеЗаполнено(СведенияОбОрганизации.Отчество) Тогда
				ДобавитьУзелXML(УзелФИО, "УТ2:Отчество",, 					СведенияОбОрганизации.Отчество);
			КонецЕсли;
		КонецЕсли;
		ДобавитьУзелXML(УзелОператор, "УТ2:ИНН",, 							СведенияОбОрганизации.ИНН);
		Если СведенияОбОрганизации.ЭтоЮридическоеЛицо Тогда
			ДобавитьУзелXML(УзелОператор, "УТ2:КПП",, 						СведенияОбОрганизации.КПП);
		КонецЕсли;
		
		// добавляем узлы "Документ"
		ДокументыЗашифрованы = Ложь;
		ИндексОсновногоДокумента = -1;
		ИмяФайлаСертификата = "";
		Для НомерУзловДокументов = 1 По 2 Цикл
			ЭтоДобавлениеДополнительныхДокументов = (НомерУзловДокументов = 2);
			
			ИндексДокумента = 0;
			Для каждого СтрДокумент Из ОписаниеДокументов.Строки Цикл
				РасширениеДокументаНРег = ОбщегоНазначенияКлиентСервер.РазложитьПолноеИмяФайла(
					СтрДокумент.ИмяФайлаДокумента).Расширение;
				РасширениеДокументаНРег = нрег(РасширениеДокументаНРег);
				Если СтрДокумент.ТипДокумента = Перечисления.ТипыСодержимогоТранспортногоКонтейнера.Сертификат Тогда
					Если НЕ ЗначениеЗаполнено(ИмяФайлаСертификата) И НЕ ЭтоДобавлениеДополнительныхДокументов Тогда
						ИмяФайлаСертификата = СтрДокумент.ИмяФайлаДокумента;
					КонецЕсли;
					
				ИначеЕсли РасширениеДокументаНРег = ".xml" И ИндексОсновногоДокумента = -1
					ИЛИ ЭтоДобавлениеДополнительныхДокументов И (РасширениеДокументаНРег <> ".xml"
					ИЛИ ИндексДокумента > ИндексОсновногоДокумента) Тогда
					
					// добавляем очередной узел "Документ"
					Если РасширениеДокументаНРег = ".der" Тогда
						КодДокумента = "СертификатЭП";
						ТипФайла = "application/x-x509-ca-cert";
					Иначе
						ЭлементыИмениФайла = СтрРазделить(СтрДокумент.ИмяФайлаДокумента, "_");
						КодДокументаВРег = ВРег(ЭлементыИмениФайла[3]);
						КодДокумента = ЭлементыИмениФайла[?(КодДокументаВРег = "ЗПЭД" ИЛИ КодДокументаВРег = "ЗОЭД"
							ИЛИ КодДокументаВРег = "УПУП" ИЛИ КодДокументаВРег = "УПРУП", 3, 2)];
						ТипФайла = MIMEТипПоСтроке(СтрДокумент.ТипСодержимого);
					КонецЕсли;
					СтрПодпись = ?(СтрДокумент.Строки.Количество() > 0, СтрДокумент.Строки[0], Неопределено);
					
					Если ИндексОсновногоДокумента = -1 Тогда
						УзелДокумент = ДобавитьУзелXML(УзелОСП, "Документ");
					Иначе
						УзелДополнительныеДокументы = ДобавитьУзелXML(УзелОСП, "ДополнительныеДокументы");
						УзелДокумент = ДобавитьУзелXML(УзелДополнительныеДокументы, "Документ");
					КонецЕсли;
					ДобавитьУзелXML(УзелДокумент, "Код",, 						КодДокумента);
					ДобавитьУзелXML(УзелДокумент, "Файл",, 						СтрДокумент.ИмяФайлаАрхива);
					ДобавитьУзелXML(УзелДокумент, "ТипФайла",, 					ТипФайла);
					ДобавитьУзелXML(УзелДокумент, "Сжатие",, 					?(СтрДокумент.Сжат = Истина, "1", "0"));
					Если СтрДокумент.Зашифрован = Истина Тогда
						ДокументыЗашифрованы = Истина;
					КонецЕсли;
					Если СтрПодпись <> Неопределено Тогда
						ДобавитьУзелXML(УзелДокумент, "ФайлЭП",, 				СтрПодпись.ИмяФайлаДокумента);
					КонецЕсли;
					
					Если ИндексОсновногоДокумента = -1 Тогда
						ИндексОсновногоДокумента = ИндексДокумента;
					КонецЕсли;
				КонецЕсли;
				ИндексДокумента = ИндексДокумента + 1;
			КонецЦикла;
		КонецЦикла;
		
		// добавляем узел Зашифровано
		ДобавитьУзелXML(УзелОСП, "Зашифровано",, 								?(ДокументыЗашифрованы = Истина, "1", "0"));
		
		// добавляем узел ФайлЭП
		ДобавитьУзелXML(УзелОСП, "ФайлСерт",, 									ИмяФайлаСертификата);
		
		// добавляем узел СлужебнаяИнформация
		СтрокаGUIDПакета = UUIDПакета;
		СтрокаДатыВремениПакета = XMLСтрока(ДатаПакета);
		
		УзелСлужебнаяИнформация = ДобавитьУзелXML(УзелЭДПФР, "СлужебнаяИнформация");
		ДобавитьУзелXML(УзелСлужебнаяИнформация, "АФ4:GUID",, 					СтрокаGUIDПакета);
		ДобавитьУзелXML(УзелСлужебнаяИнформация, "АФ4:ДатаВремя",, 				СтрокаДатыВремениПакета);
		
	Иначе
		// добавляем узел "пакет"
		УзелПакет = ДобавитьУзелXML(Дерево, "пакет");
		ДобавитьУзелXML(УзелПакет, "версияФормата",							Истина, "1.2");
		ДобавитьУзелXML(УзелПакет, "типДокументооборота",					Истина, ТипДокументооборота);
		ДобавитьУзелXML(УзелПакет, "типТранзакции",							Истина, ТипТранзакции);
		ДобавитьУзелXML(УзелПакет, "идентификаторДокументооборота", 		Истина, UUIDПакета);
		
		// добавляем узел СКЗИ
		УзелСКЗИ = ДобавитьУзелXML(УзелПакет, "СКЗИ");
		ДобавитьУзелXML(УзелСКЗИ, "типСКЗИ",								Истина, ТипСКЗИ);
		
		// добавляем узел "отправитель"
		УзелОтправитель = ДобавитьУзелXML(УзелПакет, "отправитель");
		ДобавитьУзелXML(УзелОтправитель, "идентификаторСубъекта",			Истина, КодОтправителя);
		ДобавитьУзелXML(УзелОтправитель, "типСубъекта",						Истина, "АбонентСЭД");
		
		// добавляем узел "системаОтправителя"
		УзелСистемаОтправителя = ДобавитьУзелXML(УзелПакет, "системаОтправителя");
		ДобавитьУзелXML(УзелСистемаОтправителя, "идентификаторСубъекта",	Истина, СокрЛП(КодСистемыОтправителя));
		ДобавитьУзелXML(УзелСистемаОтправителя, "типСубъекта",				Истина, "Провайдер");
		
		// добавляем узел "Получатель"
		УзелПолучатель = ДобавитьУзелXML(УзелПакет, "получатель");
		ДобавитьУзелXML(УзелПолучатель, "идентификаторСубъекта",			Истина, СокрЛП(КодПолучателя));
		ДобавитьУзелXML(УзелПолучатель, "типСубъекта",						Истина, "ОрганПФР");
		
		// Создадим контекст ЭДО один раз до начала цикла обхода, чтобы не тратить время на каждом шаге.
		КонтекстЭДОСервер = ДокументооборотСКО.ПолучитьОбработкуЭДО();
		
		// добавляем узлы "Документ"
		Для Каждого СтрДокумент Из ОписаниеДокументов.Строки Цикл
			
			// добавляем очередной узел "Документ"
			УзелДокумент = ДобавитьУзелXML(УзелПакет, "документ");
			ДобавитьУзелXML(УзелДокумент, "типДокумента",					Истина, КонтекстЭДОСервер.ИдентификаторПоТипуДокументаПакета(СтрДокумент.ТипДокумента));
			ДобавитьУзелXML(УзелДокумент, "типСодержимого",					Истина, СтрДокумент.ТипСодержимого);
			ДобавитьУзелXML(УзелДокумент, "идентификаторДокумента",			Истина, СтрДокумент.ИдентификаторДокумента);
			ДобавитьУзелXML(УзелДокумент, "сжат",							Истина, XMLСтрока(СтрДокумент.Сжат = Истина));
			ДобавитьУзелXML(УзелДокумент, "зашифрован",						Истина, XMLСтрока(СтрДокумент.Зашифрован = Истина));
			
			// добавляем узел "Содержимое"
			Если ЗначениеЗаполнено(СтрДокумент.ИмяФайлаАрхива) Тогда
				УзелСодержимое = ДобавитьУзелXML(УзелДокумент, "содержимое");
				ДобавитьУзелXML(УзелСодержимое, "имяФайла",					Истина, СтрДокумент.ИмяФайлаАрхива);
			КонецЕсли;
			
			// добавляем подписи под документом
			Для Каждого СтрПодпись Из СтрДокумент.Строки Цикл
				
				// добавляем очередной узел "Подпись"
				УзелПодпись = ДобавитьУзелXML(УзелДокумент, "подпись");
				ДобавитьУзелXML(УзелПодпись, "имяФайла",					Истина, СтрПодпись.ИмяФайлаДокумента);
				ДобавитьУзелXML(УзелПодпись, "роль",						Истина, "руководитель");
				
			КонецЦикла;
			
		КонецЦикла;
	КонецЕсли;
	
	// формируем строку XML на основе дерева
	СтрокаXML = ВыгрузитьДеревоВXML_ПФР(Дерево,, ФорматДокументооборота);
	
	// записываем дерево в файл
	ПолноеИмяФайла = ПолучитьИмяВременногоФайла();
	РезультатЗаписи = ЗаписатьXMLВФайл(СтрокаXML, ПолноеИмяФайла,
		?(ФорматДокументооборота = Перечисления.ФорматыДокументооборотаСПФР.ЭДОК, "utf-8", "windows-1251"), Истина);
	Если РезультатЗаписи = Ложь Тогда
		Возврат Неопределено;
	КонецЕсли;
	
	// записываем двоичные данные файла во временное хранилище
	АдресВХранилище = ПоместитьВоВременноеХранилище(Новый ДвоичныеДанные(ПолноеИмяФайла), ИдентификаторДляВременногоХранилища);
	
	Возврат АдресВХранилище;
	
КонецФункции

Функция СформироватьПакетПФР_ЗаключительныеДействияНаСервере(Контекст) Экспорт
	
	ФорматДокументооборота = ?(Контекст.Свойство("ФорматДокументооборота"),
		Контекст.ФорматДокументооборота, Неопределено);
	
	// получаем дерево документов
	ДеревоДокументов = ПолучитьИзВременногоХранилища(Контекст.ДеревоДокументов);
	
	// создаем каталог, в который будем помещать пакуемые файлы
	КаталогФайловОтчетности = ОперацииСФайламиЭДКО.СоздатьВременныйКаталог();
	
	// создаем файл пакета
	КороткоеИмяФайлаПакета = Контекст.КодОтправителя + "_" + Контекст.КодПолучателя + "_" + СгенерироватьUUID() + ".zip";
	ПолноеИмяФайлаПакета = ПолучитьКаталогВременныхФайлов() + КороткоеИмяФайлаПакета;
	
	ПерекодироватьВручную = Истина; // обход ошибки платформы отсутствии поддержки русского для программ без Unicode
	
	// создаем архив, для ПФР без ЭДОК сжатие включено, для ФНС - нет
	ЗаписьZIP = Новый ЗаписьZipФайла(ПолноеИмяФайлаПакета,,,
		?(ФорматДокументооборота = Перечисления.ФорматыДокументооборотаСПФР.ЭДОК,
		МетодСжатияZIP.Копирование, МетодСжатияZIP.Сжатие),,,
		?(ФорматДокументооборота = Перечисления.ФорматыДокументооборотаСПФР.ЭДОК И НЕ ПерекодироватьВручную,
		КодировкаИменФайловВZipФайле.КодировкаОСДополнительноUTF8, КодировкаИменФайловВZipФайле.Авто));
	
	// Создадим контекст ЭДО один раз до начала цикла обхода, чтобы не тратить время на каждом шаге.
	КонтекстЭДОСервер = ДокументооборотСКО.ПолучитьОбработкуЭДО();
	
	// добавляем файлы из дерева описания пакета
	Для Каждого СтрДокумент Из ДеревоДокументов.Строки Цикл
		ЗаписьZIP.Добавить(КонтекстЭДОСервер.ВыгрузитьФайлПакетаИзВременногоХранилищаВКаталог(
			КаталогФайловОтчетности, СтрДокумент));
		Для каждого СтрДокумент2 Из СтрДокумент.Строки Цикл
			ЗаписьZIP.Добавить(КонтекстЭДОСервер.ВыгрузитьФайлПакетаИзВременногоХранилищаВКаталог(
				КаталогФайловОтчетности, СтрДокумент2));
		КонецЦикла;
	КонецЦикла;
	
	// добавляем само описание пакета
	Если ФорматДокументооборота <> Перечисления.ФорматыДокументооборотаСПФР.ЭДОК Тогда
		ПолноеИмяФайлаОписанияПакета = КаталогФайловОтчетности + "packageDescription.xml";
		ПолучитьИзВременногоХранилища(Контекст.ФайлОписаниеПакета).Записать(ПолноеИмяФайлаОписанияПакета);
		ЗаписьZIP.Добавить(ПолноеИмяФайлаОписанияПакета);
	КонецЕсли;
	
	// добавляем подпись под описанием пакета
	Если ФорматДокументооборота = Перечисления.ФорматыДокументооборотаСПФР.ЭДОК Тогда
		ПолноеИмяФайлаПодписиПодФайломОписанияПакета = КаталогФайловОтчетности + СтрШаблон(
			"ПФР_%1_%2_ОСП_%3_%4.xml",
			Контекст.КодОтправителя,
			СтрЗаменить(Контекст.КодПолучателя, "-", ""),
			Формат(Контекст.ДатаПакета, "ДФ=ггггММдд"),
			Контекст.UUIDПакета);
		
	Иначе
		ПолноеИмяФайлаПодписиПодФайломОписанияПакета = КаталогФайловОтчетности + "packageDescription.sign";
	КонецЕсли;
	ПолучитьИзВременногоХранилища(Контекст.ФайлПодписиПодФайломОписанияПакета).Записать(ПолноеИмяФайлаПодписиПодФайломОписанияПакета);
	ЗаписьZIP.Добавить(ПолноеИмяФайлаПодписиПодФайломОписанияПакета);
	
	// записываем архив
	ЗаписьZIP.Записать();
	
	Если ФорматДокументооборота = Перечисления.ФорматыДокументооборотаСПФР.ЭДОК И ПерекодироватьВручную Тогда
		МассивОписанийИДанных = ОбщегоНазначенияЭДКО.МассивОписанийИДанныхZipФайла(ПолноеИмяФайлаПакета);
		ОперацииСФайламиЭДКО.УдалитьВременныйФайл(ПолноеИмяФайлаПакета);
		КороткоеИмяФайлаПакета = Контекст.КодОтправителя + "_" + Контекст.КодПолучателя + "_" + СгенерироватьUUID() + ".zip";
		ПолноеИмяФайлаПакета = ПолучитьКаталогВременныхФайлов() + КороткоеИмяФайлаПакета;
		ОбщегоНазначенияЭДКО.СформироватьZip(МассивОписанийИДанных, ПолноеИмяФайлаПакета, "cp866");
	КонецЕсли;
	
	// добавляем архив в регистр
	ДобавитьТранспортныйКонтейнер(Контекст.ТранспортноеСообщение, ПолноеИмяФайлаПакета, КороткоеИмяФайлаПакета);
	
	// удаляем временный каталог
	ОперацииСФайламиЭДКО.УдалитьВременныйФайл(КаталогФайловОтчетности);
	ОперацииСФайламиЭДКО.УдалитьВременныйФайл(ПолноеИмяФайлаПакета);
	
	Возврат Истина;
	
КонецФункции

Функция ОсновныеСведенияОбОрганизации(Организация)
	
	Результат = Новый Структура;
	Результат.Вставить("ЭтоЮридическоеЛицо", 		Неопределено);
	Результат.Вставить("НаименованиеСокращенное", 	"");
	Результат.Вставить("Фамилия", 					"");
	Результат.Вставить("Имя", 						"");
	Результат.Вставить("Отчество", 					"");
	Результат.Вставить("ИНН", 						"");
	Результат.Вставить("КПП", 						"");
	
	Результат.ЭтоЮридическоеЛицо = РегламентированнаяОтчетностьВызовСервера.ЭтоЮридическоеЛицо(Организация);
	Если Результат.ЭтоЮридическоеЛицо Тогда
		СведенияОбОрганизации = РегламентированнаяОтчетностьВызовСервера.ПолучитьСведенияОбОрганизации(Организация,,
			"НаимЮЛСокр, ИННЮЛ, КППЮЛ");
		Результат.НаименованиеСокращенное 	= СокрЛП(СведенияОбОрганизации.НаимЮЛСокр);
		Результат.ИНН 						= СокрЛП(СведенияОбОрганизации.ИННЮЛ);
		Результат.КПП 						= СокрЛП(СведенияОбОрганизации.КППЮЛ);
		
		Если НЕ ЗначениеЗаполнено(Результат.НаименованиеСокращенное) Тогда
			Если Метаданные.Справочники.Организации.Реквизиты.Найти("НаименованиеСокращенное") = Неопределено Тогда
				Результат.НаименованиеСокращенное = ОбщегоНазначения.ЗначениеРеквизитаОбъекта(Организация,
					"Наименование");
			Иначе
				Результат.НаименованиеСокращенное = ОбщегоНазначения.ЗначениеРеквизитаОбъекта(Организация,
					"НаименованиеСокращенное");
			КонецЕсли;
			Результат.НаименованиеСокращенное = СокрЛП(Результат.НаименованиеСокращенное);
		КонецЕсли;
		
	Иначе
		СведенияОбОрганизации = РегламентированнаяОтчетностьВызовСервера.ПолучитьСведенияОбОрганизации(Организация,,
			"ФамилияИП, ИмяИП, ОтчествоИП, ФИО, ИННФЛ");
		Результат.Фамилия 					= СокрЛП(СведенияОбОрганизации.ФамилияИП);
		Результат.Имя 						= СокрЛП(СведенияОбОрганизации.ИмяИП);
		Результат.Отчество 					= СокрЛП(СведенияОбОрганизации.ОтчествоИП);
		Результат.ИНН 						= СокрЛП(СведенияОбОрганизации.ИННФЛ);
		
		Если НЕ ЗначениеЗаполнено(Результат.Фамилия) ИЛИ НЕ ЗначениеЗаполнено(Результат.Имя)
			ИЛИ Результат.Имя = СведенияОбОрганизации.ФИО Тогда
			
			СтруктураФИО = РегламентированнаяОтчетность.РазложитьФИО(СведенияОбОрганизации.ФИО);
			Результат.Фамилия 	= СокрЛП(СтруктураФИО.Фамилия);
			Результат.Имя 		= СокрЛП(СтруктураФИО.Имя);
			Результат.Отчество 	= СокрЛП(СтруктураФИО.Отчество);
		КонецЕсли;
	КонецЕсли;
	
	Возврат Результат;
	
КонецФункции

Функция MIMEТипПоСтроке(СтрТипСодержимогоИлиРасширение)
	
	СтрокаТипаВНижнемРегистре = нрег(СтрТипСодержимогоИлиРасширение);
	Если СтрокаТипаВНижнемРегистре = "txt" ИЛИ СтрокаТипаВНижнемРегистре = "plain866"
		ИЛИ СтрокаТипаВНижнемРегистре = "текст1251" ИЛИ СтрокаТипаВНижнемРегистре = "plain1251" Тогда
		Возврат "text/plain";
	ИначеЕсли СтрокаТипаВНижнемРегистре = "xml" Тогда
		Возврат "application/xml";
	ИначеЕсли СтрокаТипаВНижнемРегистре = "htm" ИЛИ СтрокаТипаВНижнемРегистре = "html" Тогда
		Возврат "text/html";
	ИначеЕсли СтрокаТипаВНижнемРегистре = "pdf" Тогда
		Возврат "application/pdf";
	ИначеЕсли СтрокаТипаВНижнемРегистре = "rtf" Тогда
		Возврат "application/rtf";
	ИначеЕсли СтрокаТипаВНижнемРегистре = "tif" ИЛИ СтрокаТипаВНижнемРегистре = "tiff" Тогда
		Возврат "image/tiff";
	ИначеЕсли СтрокаТипаВНижнемРегистре = "jpg" ИЛИ СтрокаТипаВНижнемРегистре = "jpeg" Тогда
		Возврат "image/jpeg";
	ИначеЕсли СтрокаТипаВНижнемРегистре = "png" Тогда
		Возврат "image/png";
	ИначеЕсли СтрокаТипаВНижнемРегистре = "gif" Тогда
		Возврат "image/gif";
	ИначеЕсли СтрокаТипаВНижнемРегистре = "bmp" Тогда
		Возврат "image/bmp";
	ИначеЕсли СтрокаТипаВНижнемРегистре = "doc" Тогда
		Возврат "application/msword";
	ИначеЕсли СтрокаТипаВНижнемРегистре = "xls" Тогда
		Возврат "application/vnd.ms-excel";
	ИначеЕсли СтрокаТипаВНижнемРегистре = "odt" Тогда
		Возврат "application/vnd.oasis.opendocument.text";
	ИначеЕсли СтрокаТипаВНижнемРегистре = "ods" Тогда
		Возврат "application/vnd.oasis.opendocument.spreadsheet";
	ИначеЕсли СтрокаТипаВНижнемРегистре = "docx" Тогда
		Возврат "application/vnd.openxmlformats-officedocument.wordprocessingml.document";
	ИначеЕсли СтрокаТипаВНижнемРегистре = "xlsx" Тогда
		Возврат "application/vnd.openxmlformats-officedocument.spreadsheetml.sheet";
	ИначеЕсли СтрокаТипаВНижнемРегистре = "sgn" Тогда
		Возврат "application/x-pkcs7-signature";
	Иначе // "unknown"
		Возврат "application/octet-stream";
	КонецЕсли;
	
КонецФункции

Функция ТипСодержимогоИРасширениеПоMIMEТипу(MIMEТип) Экспорт
	
	MIMEТипВНижнемРегистре = нрег(MIMEТип);
	Если MIMEТипВНижнемРегистре = "text/plain" Тогда
		// необходимо анализировать наполнение файла с целью автоопределения кодировки (Текст866 или Текст1251)
		Возврат Новый Структура("ТипСодержимого, Расширение", Перечисления.ТипыСодержимогоФайлов.Текст1251, "txt");
	ИначеЕсли MIMEТипВНижнемРегистре = "application/xml" ИЛИ MIMEТипВНижнемРегистре = "text/xml" Тогда
		Возврат Новый Структура("ТипСодержимого, Расширение", Перечисления.ТипыСодержимогоФайлов.xml, "xml");
	ИначеЕсли MIMEТипВНижнемРегистре = "text/html" Тогда
		Возврат Новый Структура("ТипСодержимого, Расширение", Перечисления.ТипыСодержимогоФайлов.html, "html");
	ИначеЕсли MIMEТипВНижнемРегистре = "application/pdf" ИЛИ MIMEТипВНижнемРегистре = "application/x-pdf"
		ИЛИ MIMEТипВНижнемРегистре = "application/acrobat" ИЛИ MIMEТипВНижнемРегистре = "applications/vnd.pdf"
		ИЛИ MIMEТипВНижнемРегистре = "text/pdf" ИЛИ MIMEТипВНижнемРегистре = "text/x-pdf" Тогда
		Возврат Новый Структура("ТипСодержимого, Расширение", Перечисления.ТипыСодержимогоФайлов.pdf, "pdf");
	ИначеЕсли MIMEТипВНижнемРегистре = "application/rtf" ИЛИ MIMEТипВНижнемРегистре = "application/x-rtf"
		ИЛИ MIMEТипВНижнемРегистре = "text/richtext" ИЛИ MIMEТипВНижнемРегистре = "text/rtf" Тогда
		Возврат Новый Структура("ТипСодержимого, Расширение", Перечисления.ТипыСодержимогоФайлов.rtf, "rtf");
	ИначеЕсли MIMEТипВНижнемРегистре = "image/tiff" ИЛИ MIMEТипВНижнемРегистре = "image/tif"
		ИЛИ MIMEТипВНижнемРегистре = "image/x-tiff" ИЛИ MIMEТипВНижнемРегистре = "image/x-tif"
		ИЛИ MIMEТипВНижнемРегистре = "application/tiff" ИЛИ MIMEТипВНижнемРегистре = "application/tif"
		ИЛИ MIMEТипВНижнемРегистре = "application/x-tiff" ИЛИ MIMEТипВНижнемРегистре = "application/x-tif" Тогда
		Возврат Новый Структура("ТипСодержимого, Расширение", Перечисления.ТипыСодержимогоФайлов.tiff, "tiff");
	ИначеЕсли MIMEТипВНижнемРегистре = "image/jpeg" ИЛИ MIMEТипВНижнемРегистре = "image/jpg"
		ИЛИ MIMEТипВНижнемРегистре = "application/jpg" ИЛИ MIMEТипВНижнемРегистре = "application/x-jpg"
		ИЛИ MIMEТипВНижнемРегистре = "image/pjpeg" Тогда
		Возврат Новый Структура("ТипСодержимого, Расширение", Перечисления.ТипыСодержимогоФайлов.jpeg, "jpeg");
	ИначеЕсли MIMEТипВНижнемРегистре = "image/png" ИЛИ MIMEТипВНижнемРегистре = "image/x-png"
		ИЛИ MIMEТипВНижнемРегистре = "application/png" ИЛИ MIMEТипВНижнемРегистре = "application/x-png"
		ИЛИ MIMEТипВНижнемРегистре = "image/gif" ИЛИ MIMEТипВНижнемРегистре = "image/vnd.wap.wbmp" Тогда
		Возврат Новый Структура("ТипСодержимого, Расширение", Перечисления.ТипыСодержимогоФайлов.png, "png");
	ИначеЕсли MIMEТипВНижнемРегистре = "image/gif" Тогда
		Возврат Новый Структура("ТипСодержимого, Расширение", Перечисления.ТипыСодержимогоФайлов.png, "gif");
	ИначеЕсли MIMEТипВНижнемРегистре = "image/bmp" ИЛИ MIMEТипВНижнемРегистре = "image/x-bmp"
		ИЛИ MIMEТипВНижнемРегистре = "image/x-bitmap" ИЛИ MIMEТипВНижнемРегистре = "image/x-xbitmap"
		ИЛИ MIMEТипВНижнемРегистре = "image/x-win-bitmap" ИЛИ MIMEТипВНижнемРегистре = "image/x-windows-bmp"
		ИЛИ MIMEТипВНижнемРегистре = "image/ms-bmp" ИЛИ MIMEТипВНижнемРегистре = "image/x-ms-bmp"
		ИЛИ MIMEТипВНижнемРегистре = "application/bmp" ИЛИ MIMEТипВНижнемРегистре = "application/x-bmp"
		ИЛИ MIMEТипВНижнемРегистре = "application/x-win-bitmap" ИЛИ MIMEТипВНижнемРегистре = "application/preview"
		ИЛИ MIMEТипВНижнемРегистре = "image/vnd.wap.wbmp" Тогда
		Возврат Новый Структура("ТипСодержимого, Расширение", Перечисления.ТипыСодержимогоФайлов.png, "bmp");
	ИначеЕсли MIMEТипВНижнемРегистре = "application/msword" ИЛИ MIMEТипВНижнемРегистре = "application/doc"
		ИЛИ MIMEТипВНижнемРегистре = "appl/text" ИЛИ MIMEТипВНижнемРегистре = "application/vnd.msword"
		ИЛИ MIMEТипВНижнемРегистре = "application/vnd.ms-word" ИЛИ MIMEТипВНижнемРегистре = "application/winword"
		ИЛИ MIMEТипВНижнемРегистре = "application/word" ИЛИ MIMEТипВНижнемРегистре = "application/x-msw6"
		ИЛИ MIMEТипВНижнемРегистре = "application/x-msword" Тогда
		Возврат Новый Структура("ТипСодержимого, Расширение", Перечисления.ТипыСодержимогоФайлов.ms_word, "doc");
	ИначеЕсли MIMEТипВНижнемРегистре = "application/vnd.ms-excel" ИЛИ MIMEТипВНижнемРегистре = "application/excel"
		ИЛИ MIMEТипВНижнемРегистре = "application/msexcell" ИЛИ MIMEТипВНижнемРегистре = "application/x-msexcel"
		ИЛИ MIMEТипВНижнемРегистре = "application/x-excel" ИЛИ MIMEТипВНижнемРегистре = "application/x-msexcel"
		ИЛИ MIMEТипВНижнемРегистре = "application/x-dos_ms_excel" ИЛИ MIMEТипВНижнемРегистре = "application/xls" Тогда
		Возврат Новый Структура("ТипСодержимого, Расширение", Перечисления.ТипыСодержимогоФайлов.ms_excel, "xls");
	ИначеЕсли MIMEТипВНижнемРегистре = "application/vnd.oasis.opendocument.text"
		ИЛИ MIMEТипВНижнемРегистре = "application/x-vnd.oasis.opendocument.text" Тогда
		Возврат Новый Структура("ТипСодержимого, Расширение", Перечисления.ТипыСодержимогоФайлов.odf_text, "odt");
	ИначеЕсли MIMEТипВНижнемРегистре = "application/vnd.oasis.opendocument.spreadsheet" Тогда
		Возврат Новый Структура("ТипСодержимого, Расширение", Перечисления.ТипыСодержимогоФайлов.odf_spreadsheet, "ods");
	ИначеЕсли MIMEТипВНижнемРегистре = "application/vnd.openxmlformats-officedocument.wordprocessingml.document" Тогда
		Возврат Новый Структура("ТипСодержимого, Расширение", Перечисления.ТипыСодержимогоФайлов.oxml_word, "docx");
	ИначеЕсли MIMEТипВНижнемРегистре = "application/vnd.openxmlformats-officedocument.spreadsheetml.sheet" Тогда
		Возврат Новый Структура("ТипСодержимого, Расширение", Перечисления.ТипыСодержимогоФайлов.oxml_spreadsheet, "xlsx");
	ИначеЕсли MIMEТипВНижнемРегистре = "application/x-pkcs7-signature" Тогда
		Возврат Новый Структура("ТипСодержимого, Расширение", Перечисления.ТипыСодержимогоФайлов.sgn, "sgn");
	ИначеЕсли MIMEТипВНижнемРегистре = "application/zip" Тогда
		Возврат Новый Структура("ТипСодержимого, Расширение", Перечисления.ТипыСодержимогоФайлов.Неизвестный, "zip");
	ИначеЕсли MIMEТипВНижнемРегистре = "application/x-rar-compressed" Тогда
		Возврат Новый Структура("ТипСодержимого, Расширение", Перечисления.ТипыСодержимогоФайлов.Неизвестный, "rar");
	ИначеЕсли MIMEТипВНижнемРегистре = "application/gzip" Тогда
		Возврат Новый Структура("ТипСодержимого, Расширение", Перечисления.ТипыСодержимогоФайлов.Неизвестный, "gz");
	ИначеЕсли MIMEТипВНижнемРегистре = "application/x-tar" Тогда
		Возврат Новый Структура("ТипСодержимого, Расширение", Перечисления.ТипыСодержимогоФайлов.Неизвестный, "tar");
	ИначеЕсли MIMEТипВНижнемРегистре = "application/octet-stream"
		ИЛИ MIMEТипВНижнемРегистре = "application/x-www-form-urlencoded" Тогда // "unknown"
		Возврат Новый Структура("ТипСодержимого, Расширение", Перечисления.ТипыСодержимогоФайлов.Неизвестный, "bin");
	Иначе
		Возврат Новый Структура("ТипСодержимого, Расширение", Перечисления.ТипыСодержимогоФайлов.ПустаяСсылка(), "");
	КонецЕсли;
	
КонецФункции

#КонецОбласти

#Область ОтправкаМакетовПенсионныхДел

Функция СформироватьПакетМакетаПенсионногоДелаИЗаявления(КонтекстОтправки) Экспорт
	
	Если Не КонтекстОтправки.Настройки.Вложение.ТребуетсяАрхивирование Тогда
		Возврат Истина;
	КонецЕсли;
	
	Запрос = Новый Запрос;
	Запрос.Текст =
	"ВЫБРАТЬ
	|	СодержимоеТранспортныхКонтейнеров.ТранспортноеСообщение,
	|	СодержимоеТранспортныхКонтейнеров.ИмяФайла,
	|	СодержимоеТранспортныхКонтейнеров.Данные,
	|	СодержимоеТранспортныхКонтейнеров.ЭЦПИмяПодписанногоФайла КАК ИмяПодписанногоФайла,
	|	СодержимоеТранспортныхКонтейнеров.Тип
	|	, ВЫБОР
	|		КОГДА СостояниеОбъектов.Архивный ЕСТЬ NULL ТОГДА Ложь
	|		КОГДА СостояниеОбъектов.Архивный = Ложь ТОГДА Ложь
	|	ИНАЧЕ
	|		Истина
	|	КОНЕЦ ВАрхиве
	|ПОМЕСТИТЬ Приложения
	|ИЗ	
	|	РегистрСведений.СодержимоеТранспортныхКонтейнеров КАК СодержимоеТранспортныхКонтейнеров
	|
	|		ЛЕВОЕ СОЕДИНЕНИЕ РегистрСведений.ПризнакиАрхивированияФайловДОСКонтролирующимиОрганами КАК СостояниеОбъектов
	|			ПО 		(СостояниеОбъектов.Объект = СодержимоеТранспортныхКонтейнеров.ТранспортноеСообщение)
	|				И	(СостояниеОбъектов.ИмяФайла = СодержимоеТранспортныхКонтейнеров.ИмяФайла)
	|				И	(СостояниеОбъектов.Владелец = Значение(Перечисление.ВидыАрхивируемыхМетаданныхДО.СодержимоеТранспортныхКонтейнеров))
	|
	|ГДЕ
	|	СодержимоеТранспортныхКонтейнеров.ТранспортноеСообщение = &ТранспортноеСообщение
	|	И СодержимоеТранспортныхКонтейнеров.Тип В (ЗНАЧЕНИЕ(Перечисление.ТипыСодержимогоТранспортногоКонтейнера.ПисьмоПриложение))
	|;
	|
	|////////////////////////////////////////////////////////////////////////////////
	|ВЫБРАТЬ
	|	СодержимоеТранспортныхКонтейнеров.ТранспортноеСообщение,
	|	СодержимоеТранспортныхКонтейнеров.ИмяФайла,
	|	СодержимоеТранспортныхКонтейнеров.Данные,
	|	СодержимоеТранспортныхКонтейнеров.ЭЦПИмяПодписанногоФайла КАК ИмяПодписанногоФайла,
	|	СодержимоеТранспортныхКонтейнеров.Тип
	|	, ВЫБОР
	|		КОГДА СостояниеОбъектов.Архивный ЕСТЬ NULL ТОГДА Ложь
	|		КОГДА СостояниеОбъектов.Архивный = Ложь ТОГДА Ложь
	|	ИНАЧЕ
	|		Истина
	|	КОНЕЦ ВАрхиве
	|ПОМЕСТИТЬ Подписи
	|ИЗ
	|	РегистрСведений.СодержимоеТранспортныхКонтейнеров КАК СодержимоеТранспортныхКонтейнеров
	|
	|		ЛЕВОЕ СОЕДИНЕНИЕ РегистрСведений.ПризнакиАрхивированияФайловДОСКонтролирующимиОрганами КАК СостояниеОбъектов
	|			ПО 		(СостояниеОбъектов.Объект = СодержимоеТранспортныхКонтейнеров.ТранспортноеСообщение)
	|				И	(СостояниеОбъектов.ИмяФайла = СодержимоеТранспортныхКонтейнеров.ИмяФайла)
	|				И	(СостояниеОбъектов.Владелец = Значение(Перечисление.ВидыАрхивируемыхМетаданныхДО.СодержимоеТранспортныхКонтейнеров))
	|
	|ГДЕ
	|	СодержимоеТранспортныхКонтейнеров.ТранспортноеСообщение = &ТранспортноеСообщение
	|	И СодержимоеТранспортныхКонтейнеров.Тип В (ЗНАЧЕНИЕ(Перечисление.ТипыСодержимогоТранспортногоКонтейнера.ЭлектронноЦифроваяПодпись))
	|;
	|
	|////////////////////////////////////////////////////////////////////////////////
	|ВЫБРАТЬ
	|	Подписи.ТранспортноеСообщение,
	|	Подписи.ИмяФайла КАК ИмяФайлаПодписи,
	|	Подписи.Данные КАК Подпись,
	|	Подписи.ИмяПодписанногоФайла КАК ИмяПодписанногоФайла,
	|	Приложения.ИмяФайла КАК ИмяФайлаПриложения,
	|	Приложения.Данные КАК Приложение
	|	, Приложения.ВАрхиве КАК ПриложениеВАрхиве
	|	, Подписи.ВАрхиве КАК ПодписьВАрхиве
	|ИЗ
	|	Приложения КАК Приложения
	|		ВНУТРЕННЕЕ СОЕДИНЕНИЕ Подписи КАК Подписи
	|		ПО Приложения.ТранспортноеСообщение = Подписи.ТранспортноеСообщение
	|			И Приложения.ИмяФайла = Подписи.ИмяПодписанногоФайла";
	Запрос.УстановитьПараметр("ТранспортноеСообщение", КонтекстОтправки.ТранспортноеСообщение);
	
	Выборка = Запрос.Выполнить().Выбрать();
	ФайлыДляУдаления = Новый Массив;
	Файлы = Новый Массив;
	
	ВсегоВАрхиве = 0;
	Пока Выборка.Следующий() Цикл
		Если Выборка.ПриложениеВАрхиве Или Выборка.ПодписьВАрхиве Тогда
			ВсегоВАрхиве = ВсегоВАрхиве + 1;
			Продолжить;
		КонецЕсли;
			
		ФайлыДляУдаления.Добавить(Выборка.ИмяФайлаПодписи);
		ФайлыДляУдаления.Добавить(Выборка.ИмяПодписанногоФайла);
				
		Файл = Новый Структура;
		Файл.Вставить("Имя", Выборка.ИмяПодписанногоФайла + ".sig");
		Файл.Вставить("ИмяФайла", Выборка.ИмяФайлаПодписи);
		Файл.Вставить("Данные", Выборка.Подпись.Получить());
		Файлы.Добавить(Файл);
		
		Файл = Новый Структура;
		Файл.Вставить("Имя", Выборка.ИмяПодписанногоФайла);
		Файл.Вставить("ИмяФайла", Выборка.ИмяФайлаПриложения);
		Файл.Вставить("Данные", Выборка.Приложение.Получить());
		Файлы.Добавить(Файл);
	КонецЦикла;
	
	Если ВсегоВАрхиве > 0 Тогда
		Возврат Ложь;
	КонецЕсли;
	
	ПрочитатьПрисоединенныеФайлы = Ложь;
	Для каждого Файл Из Файлы Цикл
		Если Файл.Данные = Неопределено Тогда
			ПрочитатьПрисоединенныеФайлы = Истина;
			Прервать;
		КонецЕсли;
	КонецЦикла;
	
	Если ПрочитатьПрисоединенныеФайлы Тогда
		ТаблицаФайлов = ОбщегоНазначенияЭДКО.ПрикрепленныеФайлыКОбъектуИзСправочника(
			КонтекстОтправки.ТранспортноеСообщение,
			"ТранспортноеСообщениеПрисоединенныеФайлы",
			,
			"ИсходноеИмяФайла");
		
		Для каждого СтрокаТаблицы Из ТаблицаФайлов Цикл
			СтрокаТаблицы.ИсходноеИмяФайла = ВРЕГ(СтрокаТаблицы.ИсходноеИмяФайла);
		КонецЦикла;
		
		Для каждого Файл Из Файлы Цикл
			Если Файл.Данные = Неопределено Тогда
				ИсходноеИмяФайла = ВРЕГ(Файл.ИмяФайла);
				
				СтрокаТаблицы = ТаблицаФайлов.Найти(ИсходноеИмяФайла, "ИсходноеИмяФайла");
				Если СтрокаТаблицы <> Неопределено Тогда
					Файл.Данные = РаботаСФайлами.ДвоичныеДанныеФайла(СтрокаТаблицы.Ссылка);
				КонецЕсли;
			КонецЕсли;
		КонецЦикла;
	КонецЕсли;
	
	НачатьТранзакцию();
	
	НаборЗаписей = РегистрыСведений.СодержимоеТранспортныхКонтейнеров.СоздатьНаборЗаписей();
	НаборЗаписей.Отбор.ТранспортноеСообщение.Установить(КонтекстОтправки.ТранспортноеСообщение);
	НаборЗаписей.Прочитать();
	
	ЗаписиДляУдаления = Новый Массив;
	ИменаПрисоединенныхФайловДляУдаления = Новый Массив;
	Для Каждого Запись Из НаборЗаписей Цикл
		Если ФайлыДляУдаления.Найти(Запись.ИмяФайла) <> Неопределено Тогда
			ЗаписиДляУдаления.Добавить(Запись);
			ИменаПрисоединенныхФайловДляУдаления.Добавить(Запись.ИмяФайла);
		КонецЕсли;
	КонецЦикла;
	Для Каждого ЗаписьДляУдаления Из ЗаписиДляУдаления Цикл
		НаборЗаписей.Удалить(ЗаписьДляУдаления);
	КонецЦикла;
	Если ПрочитатьПрисоединенныеФайлы Тогда
		Для каждого ИмяПрисоединенногоФайлаДляУдаления Из ИменаПрисоединенныхФайловДляУдаления Цикл
			ИсходноеИмяФайла = ВРЕГ(ИмяПрисоединенногоФайлаДляУдаления);
			
			СтрокаТаблицы = ТаблицаФайлов.Найти(ИсходноеИмяФайла, "ИсходноеИмяФайла");
			Если СтрокаТаблицы <> Неопределено Тогда
				ПрисоединенныйФайл = СтрокаТаблицы.Ссылка;
				ПрисоединенныйФайлОбъект = ПрисоединенныйФайл.ПолучитьОбъект();
				ПрисоединенныйФайлОбъект.УстановитьПометкуУдаления(Истина);
			КонецЕсли;
		КонецЦикла;
	КонецЕсли;
	АдресПакета = ОперацииСФайламиЭДКОВызовСервера.УпаковатьФайлы(Файлы);
	
	ИмяПакета = КонтекстОтправки.Настройки.Вложение.Маска;
	Для Каждого КлючЗначение Из КонтекстОтправки.Настройки.ПараметрыЗаполнения Цикл
		ИмяПакета = СтрЗаменить(ИмяПакета, СтрШаблон("{%1}", КлючЗначение.Ключ), КлючЗначение.Значение);
	КонецЦикла;
	
	НоваяЗапись = НаборЗаписей.Добавить();
	НоваяЗапись.ТранспортноеСообщение = КонтекстОтправки.ТранспортноеСообщение;
	НоваяЗапись.ИмяФайла = ИмяПакета;
	
	Пакет = ПолучитьИзВременногоХранилища(АдресПакета);
	НоваяЗапись.Тип = Перечисления.ТипыСодержимогоТранспортногоКонтейнера.ПисьмоПриложение;
	НоваяЗапись.Размер = Пакет.Размер();
	Идентификатор = НРег(СтрЗаменить(Новый УникальныйИдентификатор, "-", ""));
	НоваяЗапись.Идентификатор = Идентификатор;
	НоваяЗапись.ТипСодержимогоФайла = Перечисления.ТипыСодержимогоФайлов.Неизвестный;
	
	НаборЗаписей.Записать();
	
	КомпонентыИмениФайла = ОбщегоНазначенияКлиентСервер.РазложитьПолноеИмяФайла(ИмяПакета);
	УникальноеИмяФайлаБезРасширения = ОбщегоНазначенияЭДКОКлиентСервер.УникальнаяСтрока(КомпонентыИмениФайла.ИмяБезРасширения, 150);
	РасширениеФайлаБезТочки = Сред(КомпонентыИмениФайла.Расширение, 2);
	
	АдресФайла = ПоместитьВоВременноеХранилище(Пакет, Новый УникальныйИдентификатор);
	
	ИсходноеИмяФайла = ВРЕГ(ИмяПакета);
	
	СтрокаТаблицы = ТаблицаФайлов.Найти(ИсходноеИмяФайла, "ИсходноеИмяФайла");
	Если СтрокаТаблицы <> Неопределено Тогда
		ПрисоединенныйФайл = СтрокаТаблицы.Ссылка;
		ИнформацияОбОбновляемомФайле = Новый Структура;
		ИнформацияОбОбновляемомФайле.Вставить("АдресФайлаВоВременномХранилище", АдресФайла);
		ИнформацияОбОбновляемомФайле.Вставить("АдресВременногоХранилищаТекста", Неопределено);
		ИнформацияОбОбновляемомФайле.Вставить("ИмяБезРасширения", 				УникальноеИмяФайлаБезРасширения);
		ИнформацияОбОбновляемомФайле.Вставить("Расширение", 					РасширениеФайлаБезТочки);
		
		РаботаСФайлами.ОбновитьФайл(ПрисоединенныйФайл, ИнформацияОбОбновляемомФайле);
		
	Иначе
		ПараметрыФайла = Новый Структура;
		ПараметрыФайла.Вставить("Автор", 						Неопределено);
		ПараметрыФайла.Вставить("ВладелецФайлов", 				КонтекстОтправки.ТранспортноеСообщение);
		ПараметрыФайла.Вставить("ИмяБезРасширения", 			УникальноеИмяФайлаБезРасширения);
		ПараметрыФайла.Вставить("РасширениеБезТочки", 			РасширениеФайлаБезТочки);
		ПараметрыФайла.Вставить("ВремяИзмененияУниверсальное", 	Неопределено);
		
		НоваяСсылкаНаФайл = РаботаСФайлами.НоваяСсылкаНаФайл(КонтекстОтправки.ТранспортноеСообщение, "ТранспортноеСообщениеПрисоединенныеФайлы");
		ПрисоединенныйФайл = РаботаСФайлами.ДобавитьФайл(ПараметрыФайла, АдресФайла,,, НоваяСсылкаНаФайл);
	КонецЕсли;
	
	ПрисоединенныйФайлОбъект = ПрисоединенныйФайл.ПолучитьОбъект();
	
	ПрисоединенныйФайлОбъект.Тип = Перечисления.ТипыСодержимогоТранспортногоКонтейнера.ПисьмоПриложение;
	ПрисоединенныйФайлОбъект.Идентификатор = Идентификатор;
	ПрисоединенныйФайлОбъект.ТипСодержимогоФайла = Перечисления.ТипыСодержимогоФайлов.Неизвестный;
	
	ПрисоединенныйФайлОбъект.ИсходноеИмяФайла = ИмяПакета;
	
	ПрисоединенныйФайлОбъект.Записать();
	
	ЗафиксироватьТранзакцию();
	УдалитьИзВременногоХранилища(АдресФайла);
	
	Возврат Истина;
	
КонецФункции

Функция ПолучитьПараметрыОтправкиМакетовПенсионныхДелИЗаявления(ЦиклОбмена) Экспорт
	
	Документ = ЦиклОбмена.Предмет;
	
	ЭтоМакетПД   = ЭтоМакетПД(ЦиклОбмена);
	ЭтоЗаявление = ЭтоЗаявлениеОНазначенииПенсии(ЦиклОбмена);

	Если ЭтоМакетПД Тогда
		Настройки = РегистрыСведений.ПравилаОтправкиОтчетностиПоФизЛицам.ПолучитьНастройки(
			Документ.Отправитель,
			Документ.Получатель,
			Перечисления.ВидыОтчетностиПоФизЛицам.МакетПенсионныхДел);
	Иначе
		Настройки = РегистрыСведений.ПравилаОтправкиОтчетностиПоФизЛицам.ПолучитьНастройки(
			Документ.Отправитель, 
			Документ.Получатель,
			Перечисления.ВидыОтчетностиПоФизЛицам.ЗаявлениеОНазначенииИДоставкеПенсии);
	КонецЕсли;
	
	Если Не ЗначениеЗаполнено(Настройки) Тогда
		
		ТекстСообщения = НСтр("ru = 'Правила отправки документа для учетной записи ""%1"" и органа ПФР ""%2"" не заданы.
                               |Обратитесь в службу поддержки.'");
		ТекстСообщения = СтрШаблон(ТекстСообщения, Документ.Отправитель.УчетнаяЗаписьОбмена, Документ.Получатель);
		ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
		
		Возврат Неопределено;
		
	КонецЕсли;
	
	ПараметрыЗаполнения = Новый Структура;
	ПараметрыЗаполнения.Вставить("КодУПФР",                   СокрЛП(Документ.ОрганПФРПоМестуНазначенияПенсии));
	ПараметрыЗаполнения.Вставить("РегНомерПФР",               СокрЛП(Документ.Отправитель.РегистрационныйНомерПФР));
	
	Если ТипЗнч(Документ) = Тип("СправочникСсылка.МакетыПенсионныхДел") Тогда
		СНИЛС = Документ.СтраховойНомерПФР;
	Иначе
		СНИЛС = Документ.СНИЛС;
	КонецЕсли;
	
	ПараметрыЗаполнения.Вставить("СНИЛС",                     СокрЛП(СНИЛС));
	ПараметрыЗаполнения.Вставить("СНИЛСБезКонтрольногоЧисла", СокрЛП(Лев(СНИЛС, 11)));
	ПараметрыЗаполнения.Вставить("ДатаВыходаНаПенсию",        Формат(Документ.ДатаВыходаНаПенсию, "ДФ=yyyyMMdd"));
	ПараметрыЗаполнения.Вставить("Стаж",                      ?(Документ.Стаж = Перечисления.ВидыСтажаДляНазначенияПенсии.ЗаВыслугуЛет, "Л", "О"));
	ПараметрыЗаполнения.Вставить("КодРайона",                 Лев(Документ.ОрганПФРПоМестуНазначенияПенсии, 3));
	ПараметрыЗаполнения.Вставить("GUID",                      СтрЗаменить(Новый УникальныйИдентификатор, "-", ""));
	
	Настройки.Вставить("ПараметрыЗаполнения", ПараметрыЗаполнения);
	
	Возврат Настройки;
	
КонецФункции

Функция ЭтоМакетПД(ЦиклОбмена) Экспорт
	
	ЭтоМакет = Ложь;
	Если ЗначениеЗаполнено(ЦиклОбмена) И ТипЗнч(ЦиклОбмена.Предмет) = Тип("СправочникСсылка.МакетыПенсионныхДел") Тогда
		ЭтоМакет = Истина;
	КонецЕсли;
	
	Возврат ЭтоМакет;
		
КонецФункции

Функция ЭтоЗаявлениеОНазначенииПенсии(ЦиклОбмена) Экспорт
	
	ЭтоЗаявление = Ложь;
	Если ЗначениеЗаполнено(ЦиклОбмена) И ТипЗнч(ЦиклОбмена.Предмет) = Тип("СправочникСсылка.ЗаявлениеОНазначенииПенсии") Тогда
		ЭтоЗаявление = Истина;
	КонецЕсли;
	
	Возврат ЭтоЗаявление;
		
КонецФункции

#КонецОбласти

#Область ОтправкаИзвещений

Функция ПризнакАвтоматическогоФормированияПодтвержденийИТипСообщения(ОбъектСсылка, ПроанализироватьРассылкиПриАвтоподтверждении = Ложь) Экспорт
	
	ПоляОбъекта = Новый Структура();
	ПоляОбъекта.Вставить("УчетнаяЗапись", "УчетнаяЗапись");
	ПоляОбъекта.Вставить("Тип", "Тип");
	ПоляОбъекта.Вставить("НеФормироватьПодтвержденияАвтоматически", "УчетнаяЗапись.НеФормироватьПодтвержденияАвтоматически");
	ПоляОбъекта.Вставить("ЦиклОбмена", "ЦиклОбмена");
	
	РеквизитыОбъекта = ОбщегоНазначения.ЗначенияРеквизитовОбъекта(ОбъектСсылка, ПоляОбъекта, Истина);

	Результат = Новый Структура();
	Результат.Вставить("ОписаниеОшибки", Неопределено);
	
	Если ТипЗнч(РеквизитыОбъекта.НеФормироватьПодтвержденияАвтоматически) = Тип("Булево") Тогда
		Результат.Вставить("УчетнаяЗапись",          РеквизитыОбъекта.УчетнаяЗапись);
		Результат.Вставить("Автоподтверждение",      НЕ РеквизитыОбъекта.НеФормироватьПодтвержденияАвтоматически);
		Результат.Вставить("Тип",                    РеквизитыОбъекта.Тип);
	Иначе
		Результат.ОписаниеОшибки = НСтр("ru = 'Текущий пользователь не имеет права использования учетной записи документооборота'");
		Результат.Вставить("УчетнаяЗапись",          Неопределено);
		Результат.Вставить("Автоподтверждение",      Неопределено);
		Результат.Вставить("Тип",                    Неопределено);
	КонецЕсли;
	
	Результат.Вставить("ЭтоМассоваяРассылкаФНС", Ложь);
	Результат.Вставить("ЭтоРассылкаПФР",         Ложь);
	Результат.Вставить("ТребуетсяПодтверждение", Ложь);
	
	Если ЗначениеЗаполнено(Результат.ОписаниеОшибки) Тогда
		
		ЗаписьЖурналаРегистрации(
				НСтр("ru = 'Электронный документооборот с контролирующими органами.ПризнакАвтоматическогоФормированияПодтвержденийИТипСообщения()'"), 
				УровеньЖурналаРегистрации.Предупреждение,, ОбъектСсылка, Результат.ОписаниеОшибки);
				
		Возврат Результат;
	КонецЕсли;
	
	Если Результат.Автоподтверждение И ПроанализироватьРассылкиПриАвтоподтверждении Тогда
		Результат.ЭтоМассоваяРассылкаФНС = Результат.Тип = Перечисления.ТипыТранспортныхСообщений.РассылкаНО
			И ОбъектСсылка.ЦиклОбмена.Тип = Перечисления.ТипыЦикловОбмена.Рассылка;
		
		Если Результат.Тип = Перечисления.ТипыТранспортныхСообщений.ПисьмоВходящееПФР Тогда
			Результат.ЭтоРассылкаПФР = ЭтоРассылкаПФР(ОбъектСсылка);
		КонецЕсли;
	КонецЕсли;
	
	Результат.ТребуетсяПодтверждение = СообщениеТребуетПодтверждения(
		Результат.Тип, Результат.ЭтоМассоваяРассылкаФНС, Результат.ЭтоРассылкаПФР, РеквизитыОбъекта.ЦиклОбмена);
	
	Возврат Результат;
	
КонецФункции

Функция СообщениеТребуетПодтверждения(ТипСообщения, ЭтоМассоваяРассылкаФНС, ЭтоРассылкаПФР, ЦиклОбмена = Неопределено)

	Если ТипСообщения = Перечисления.ТипыТранспортныхСообщений.ПротоколПФР
		ИЛИ (ТипСообщения = Перечисления.ТипыТранспортныхСообщений.ПисьмоВходящееПФР И НЕ ЭтоРассылкаПФР)
		ИЛИ ТипСообщения = Перечисления.ТипыТранспортныхСообщений.ОтветНаЗапросПФР Тогда
			
			ФорматДокументооборота = ?(ЗначениеЗаполнено(ЦиклОбмена),
				ОбщегоНазначения.ЗначениеРеквизитаОбъекта(ЦиклОбмена, "ФорматДокументооборота", Истина), Неопределено);
			
			Возврат ФорматДокументооборота <> Перечисления.ФорматыДокументооборотаСПФР.ЭДОК;
			
	ИначеЕсли ТипСообщения = Перечисления.ТипыТранспортныхСообщений.ПодтверждениеДекларацияНО
	 ИЛИ ТипСообщения = Перечисления.ТипыТранспортныхСообщений.РезультатПриемаДекларацияНО
	 ИЛИ ТипСообщения = Перечисления.ТипыТранспортныхСообщений.РезультатОбработкиДекларацияНО
	 ИЛИ ТипСообщения = Перечисления.ТипыТранспортныхСообщений.ПодтверждениеФорма2НДФЛНО
	 ИЛИ ТипСообщения = Перечисления.ТипыТранспортныхСообщений.РезультатПриемаФорма2НДФЛНО
	 ИЛИ ТипСообщения = Перечисления.ТипыТранспортныхСообщений.ПодтверждениеОбращениеНО
	 ИЛИ ТипСообщения = Перечисления.ТипыТранспортныхСообщений.РезультатПриемаОбращениеНО
	 
	 ИЛИ ТипСообщения = Перечисления.ТипыТранспортныхСообщений.ПодтверждениеПредставлениеНО
	 ИЛИ ТипСообщения = Перечисления.ТипыТранспортныхСообщений.РезультатПриемаПредставлениеНО
	 
	 ИЛИ ТипСообщения = Перечисления.ТипыТранспортныхСообщений.ПисьмоНО
	 ИЛИ ТипСообщения = Перечисления.ТипыТранспортныхСообщений.РассылкаНО И НЕ ЭтоМассоваяРассылкаФНС
	 ИЛИ ТипСообщения = Перечисления.ТипыТранспортныхСообщений.ПодтверждениеЗапросНО
	 ИЛИ ТипСообщения = Перечисления.ТипыТранспортныхСообщений.РезультатПриемаЗапросНО
	 ИЛИ ТипСообщения = Перечисления.ТипыТранспортныхСообщений.РезультатОбработкиЗапросНО 
	 //Заявление
	 ИЛИ ТипСообщения = Перечисления.ТипыТранспортныхСообщений.ПодтверждениеЗаявлениеНО
	 ИЛИ ТипСообщения = Перечисления.ТипыТранспортныхСообщений.РезультатПриемаЗаявлениеНО
	 ИЛИ ТипСообщения = Перечисления.ТипыТранспортныхСообщений.РезультатОбработкиЗаявлениеРФНО
	 ИЛИ ТипСообщения = Перечисления.ТипыТранспортныхСообщений.СообщениеОбОтзывеЗаявлениеРФНО
	 ИЛИ ТипСообщения = Перечисления.ТипыТранспортныхСообщений.РезультатОбработкиЗаявлениеТСНО 
	 
	 ИЛИ ТипСообщения = Перечисления.ТипыТранспортныхСообщений.РезультатОбработкиЗаявлениеНО 
	 //Документ
	 ИЛИ ТипСообщения = Перечисления.ТипыТранспортныхСообщений.ДокументНО
	 //Росстат
	 ИЛИ ТипСообщения = Перечисления.ТипыТранспортныхСообщений.ПодтверждениеДатыОтправкиФСГС
	 ИЛИ ТипСообщения = Перечисления.ТипыТранспортныхСообщений.ПротоколВходногоКонтроляОтчетностиФСГС
	 ИЛИ ТипСообщения = Перечисления.ТипыТранспортныхСообщений.ПисьмоВходящееФСГС Тогда

		Возврат Истина;

	КонецЕсли;
	
	Возврат Ложь;

КонецФункции

Функция СоздатьПодтверждениеПриНеобходимости(СсылкаНаОбъект)

	// Внимание!
	// ---------
	// В Контейнере есть одноименная процедура, которая используется при отправке отчетов
	// в интерактивном режиме пользователем.
	// При изменениях необходимо синхронно менять обе процедуры.

	СообщениеОбОшибке = "";
	
	Результат = ПризнакАвтоматическогоФормированияПодтвержденийИТипСообщения(СсылкаНаОбъект, Истина);
	Если НЕ Результат.Автоподтверждение Тогда
		Возврат Истина;
	КонецЕсли;
	Если ЗначениеЗаполнено(Результат.ОписаниеОшибки) Тогда
		Возврат Истина;
	КонецЕсли;
	
	// формируем подтверждение на расшифрованное сообщение при необходимости
	Если Результат.ТребуетсяПодтверждение Тогда
	 
		ОтветноеСообщение = СоздатьПодтверждение(СсылкаНаОбъект);
		
		// Создадим пакет для отправки подтвержения.
		Если ОтветноеСообщение <> Неопределено Тогда
			СформироватьПакет(ОтветноеСообщение.Ссылка, СообщениеОбОшибке);
		Иначе
			СообщениеОбОшибке = ТекстСообщенийПользователю();
		КонецЕсли;

	КонецЕсли;
	
	Возврат НЕ ЗначениеЗаполнено(СообщениеОбОшибке);
	
КонецФункции

Функция СоздатьПодтверждение(Основание)

	// Внимание!
	// ---------
	// В Контейнере есть одноименная процедура, которая используется при отправке отчетов
	// в интерактивном режиме пользователем.
	// При изменениях необходимо синхронно менять обе процедуры.
	
	// создаем на сервере
	ОтветноеСообщение = СоздатьИЗаполнитьОтветноеСообщение(Основание);
	Если ОтветноеСообщение = Неопределено Тогда
		Возврат Неопределено;
	КонецЕсли;
	
	КонтекстОтправки = Новый Структура("Подтверждение", ОтветноеСообщение.Ссылка);
	
	// различные действия на сервере в зависимости от типа сообщения
	ТипОснования = ЭлектронныйДокументооборотСКонтролирующимиОрганамиВызовСервера.ЗначениеРеквизитаОбъекта(Основание, "Тип");
	Если ТипОснования = Перечисления.ТипыТранспортныхСообщений.ПротоколПФР Тогда
		
		ПодписатьСообщение("ПФР", КонтекстОтправки, ОтветноеСообщение.Ссылка, ОтветноеСообщение.Тип, "Руководитель", ОтветноеСообщение.УчетнаяЗапись, Основание);
		
	ИначеЕсли ТипОснования = Перечисления.ТипыТранспортныхСообщений.ПисьмоВходящееПФР Тогда
		
		ПодписатьСообщение("ПФР", КонтекстОтправки, ОтветноеСообщение.Ссылка, ОтветноеСообщение.Тип, "Руководитель", ОтветноеСообщение.УчетнаяЗапись, Основание);
		
	ИначеЕсли ТипОснования = Перечисления.ТипыТранспортныхСообщений.ПодтверждениеДатыОтправкиФСГС Тогда
		
		ПодписатьСообщение("ФСГС", КонтекстОтправки, ОтветноеСообщение.Ссылка, ОтветноеСообщение.Тип, "Руководитель", ОтветноеСообщение.УчетнаяЗапись, Основание);
	
	ИначеЕсли ТипОснования = Перечисления.ТипыТранспортныхСообщений.ПротоколВходногоКонтроляОтчетностиФСГС Тогда
		
		СформироватьИзвещениеОПолученииФСГС(ОтветноеСообщение.Ссылка);
		ПодписатьСообщение("ФСГС", КонтекстОтправки, ОтветноеСообщение.Ссылка, ОтветноеСообщение.Тип, "Руководитель", ОтветноеСообщение.УчетнаяЗапись, Основание);
		
	ИначеЕсли ТипОснования = Перечисления.ТипыТранспортныхСообщений.ПисьмоВходящееФСГС Тогда
		
		СформироватьИзвещениеОПолученииФСГС(ОтветноеСообщение.Ссылка);
		ПодписатьСообщение("ФСГС", КонтекстОтправки, ОтветноеСообщение.Ссылка, ОтветноеСообщение.Тип, "Руководитель", ОтветноеСообщение.УчетнаяЗапись, Основание);
		
	ИначеЕсли ТипОснования = Перечисления.ТипыТранспортныхСообщений.ПодтверждениеДекларацияНО Тогда
		
		Если НЕ СформироватьИзвещениеОПолучении(ОтветноеСообщение.Ссылка) Тогда
			Возврат Неопределено;
		КонецЕсли;
		
		ПодписатьСообщение("ФНС", КонтекстОтправки, ОтветноеСообщение.Ссылка, ОтветноеСообщение.Тип, "Шифрование", ОтветноеСообщение.УчетнаяЗапись, Основание);
		
	ИначеЕсли ТипОснования = Перечисления.ТипыТранспортныхСообщений.ПодтверждениеФорма2НДФЛНО Тогда
		
		Если НЕ СформироватьИзвещениеОПолучении(ОтветноеСообщение.Ссылка) Тогда
			Возврат Неопределено;
		КонецЕсли;
		
		ПодписатьСообщение("ФНС", КонтекстОтправки, ОтветноеСообщение.Ссылка, ОтветноеСообщение.Тип, "Шифрование", ОтветноеСообщение.УчетнаяЗапись, Основание);
		
	ИначеЕсли ТипОснования = Перечисления.ТипыТранспортныхСообщений.ПодтверждениеОбращениеНО Тогда
		
		Если НЕ СформироватьИзвещениеОПолучении(ОтветноеСообщение.Ссылка) Тогда
			Возврат Неопределено;
		КонецЕсли;
		
		ПодписатьСообщение("ФНС", КонтекстОтправки, ОтветноеСообщение.Ссылка, ОтветноеСообщение.Тип, "Шифрование", ОтветноеСообщение.УчетнаяЗапись, Основание);
		
	ИначеЕсли ТипОснования = Перечисления.ТипыТранспортныхСообщений.ПодтверждениеПредставлениеНО Тогда
		
		Если НЕ СформироватьИзвещениеОПолучении(ОтветноеСообщение.Ссылка) Тогда
			Возврат Неопределено;
		КонецЕсли;
		
		ПодписатьСообщение("ФНС", КонтекстОтправки, ОтветноеСообщение.Ссылка, ОтветноеСообщение.Тип, "Шифрование", ОтветноеСообщение.УчетнаяЗапись, Основание);
		
	ИначеЕсли ТипОснования = Перечисления.ТипыТранспортныхСообщений.ПодтверждениеЗапросНО Тогда
		
		Если НЕ СформироватьИзвещениеОПолучении(ОтветноеСообщение.Ссылка) Тогда
			Возврат Неопределено;
		КонецЕсли;
		
		ПодписатьСообщение("ФНС", КонтекстОтправки, ОтветноеСообщение.Ссылка, ОтветноеСообщение.Тип, "Шифрование", ОтветноеСообщение.УчетнаяЗапись, Основание);
		
	ИначеЕсли ТипОснования = Перечисления.ТипыТранспортныхСообщений.РезультатПриемаДекларацияНО Тогда
		
		Если НЕ СформироватьИзвещениеОПолучении(ОтветноеСообщение.Ссылка) Тогда
			Возврат Неопределено;
		КонецЕсли;
		
		ПодписатьСообщение("ФНС", КонтекстОтправки, ОтветноеСообщение.Ссылка, ОтветноеСообщение.Тип, "Шифрование", ОтветноеСообщение.УчетнаяЗапись, Основание);
		
	ИначеЕсли ТипОснования = Перечисления.ТипыТранспортныхСообщений.РезультатПриемаФорма2НДФЛНО Тогда
		
		Если НЕ СформироватьИзвещениеОПолучении(ОтветноеСообщение.Ссылка) Тогда
			Возврат Неопределено;
		КонецЕсли;
		
		ПодписатьСообщение("ФНС", КонтекстОтправки, ОтветноеСообщение.Ссылка, ОтветноеСообщение.Тип, "Шифрование", ОтветноеСообщение.УчетнаяЗапись, Основание);
		
	ИначеЕсли ТипОснования = Перечисления.ТипыТранспортныхСообщений.РезультатПриемаОбращениеНО Тогда
		
		Если НЕ СформироватьИзвещениеОПолучении(ОтветноеСообщение.Ссылка) Тогда
			Возврат Неопределено;
		КонецЕсли;
		
		ПодписатьСообщение("ФНС", КонтекстОтправки, ОтветноеСообщение.Ссылка, ОтветноеСообщение.Тип, "Шифрование", ОтветноеСообщение.УчетнаяЗапись, Основание);
		
	ИначеЕсли ТипОснования = Перечисления.ТипыТранспортныхСообщений.РезультатПриемаПредставлениеНО Тогда
		
		Если НЕ СформироватьИзвещениеОПолучении(ОтветноеСообщение.Ссылка) Тогда
			Возврат Неопределено;
		КонецЕсли;
		
		ПодписатьСообщение("ФНС", КонтекстОтправки, ОтветноеСообщение.Ссылка, ОтветноеСообщение.Тип, "Шифрование", ОтветноеСообщение.УчетнаяЗапись, Основание);
		
	ИначеЕсли ТипОснования = Перечисления.ТипыТранспортныхСообщений.РезультатПриемаЗапросНО Тогда
		
		Если НЕ СформироватьИзвещениеОПолучении(ОтветноеСообщение.Ссылка) Тогда
			Возврат Неопределено;
		КонецЕсли;
		
		ПодписатьСообщение("ФНС", КонтекстОтправки, ОтветноеСообщение.Ссылка, ОтветноеСообщение.Тип, "Шифрование", ОтветноеСообщение.УчетнаяЗапись, Основание);
		
	ИначеЕсли ТипОснования = Перечисления.ТипыТранспортныхСообщений.РезультатОбработкиДекларацияНО Тогда
		
		Если НЕ СформироватьИзвещениеОПолучении(ОтветноеСообщение.Ссылка) Тогда
			Возврат Неопределено;
		КонецЕсли;
		
		ПодписатьСообщение("ФНС", КонтекстОтправки, ОтветноеСообщение.Ссылка, ОтветноеСообщение.Тип, "Шифрование", ОтветноеСообщение.УчетнаяЗапись, Основание);
		
	ИначеЕсли ТипОснования = Перечисления.ТипыТранспортныхСообщений.РезультатОбработкиЗапросНО Тогда
		
		Если НЕ СформироватьИзвещениеОПолучении(ОтветноеСообщение.Ссылка) Тогда
			Возврат Неопределено;
		КонецЕсли;
		
		ПодписатьСообщение("ФНС", КонтекстОтправки, ОтветноеСообщение.Ссылка, ОтветноеСообщение.Тип, "Шифрование", ОтветноеСообщение.УчетнаяЗапись, Основание);
		
	ИначеЕсли ТипОснования = Перечисления.ТипыТранспортныхСообщений.ПисьмоНО Тогда
		
		Если НЕ СформироватьИзвещениеОПолучении(ОтветноеСообщение.Ссылка) Тогда
			Возврат Неопределено;
		КонецЕсли;
		
		ПодписатьСообщение("ФНС", КонтекстОтправки, ОтветноеСообщение.Ссылка, ОтветноеСообщение.Тип, "Шифрование", ОтветноеСообщение.УчетнаяЗапись, Основание);
		
	ИначеЕсли ТипОснования = Перечисления.ТипыТранспортныхСообщений.РассылкаНО Тогда
		
		Если НЕ СформироватьИзвещениеОПолучении(ОтветноеСообщение.Ссылка) Тогда
			Возврат Неопределено;
		КонецЕсли;
		
		ПодписатьСообщение("ФНС", КонтекстОтправки, ОтветноеСообщение.Ссылка, ОтветноеСообщение.Тип, "Шифрование", ОтветноеСообщение.УчетнаяЗапись, Основание);
		
	ИначеЕсли ТипОснования = Перечисления.ТипыТранспортныхСообщений.ПодтверждениеЗаявлениеНО
		ИЛИ ТипОснования = Перечисления.ТипыТранспортныхСообщений.РезультатПриемаЗаявлениеНО 
		
		ИЛИ ТипОснования = Перечисления.ТипыТранспортныхСообщений.РезультатОбработкиЗаявлениеНО
		
		ИЛИ ТипОснования = Перечисления.ТипыТранспортныхСообщений.РезультатОбработкиЗаявлениеРФНО
		ИЛИ ТипОснования = Перечисления.ТипыТранспортныхСообщений.СообщениеОбОтзывеЗаявлениеРФНО
		ИЛИ ТипОснования = Перечисления.ТипыТранспортныхСообщений.РезультатОбработкиЗаявлениеТСНО
		ИЛИ ТипОснования = Перечисления.ТипыТранспортныхСообщений.ДокументНО Тогда
		
		Если НЕ СформироватьИзвещениеОПолучении(ОтветноеСообщение.Ссылка) Тогда
			Возврат Неопределено;
		КонецЕсли;
		
		ПодписатьСообщение("ФНС", КонтекстОтправки, ОтветноеСообщение.Ссылка, ОтветноеСообщение.Тип, "Шифрование", ОтветноеСообщение.УчетнаяЗапись, Основание);
		
	КонецЕсли;
	
	Возврат ОтветноеСообщение;
	
КонецФункции

#КонецОбласти

#Область ОбработкаЗаявленийНаСервереБезКлиентскогоВзаимодействия

//Настройка программы по заявлению абонента

Процедура РегистрацияСобратьРасшифрованныйФайл(ДокументDOM, АдресВоВременномХранилище)
	
	ВременныйКаталогНаСервере = ОперацииСФайламиЭДКО.СоздатьВременныйКаталог();
	ИмяФайла = ВременныйКаталогНаСервере + "конфиденциальнаяинформация.txt";
	ДвФайл = ПолучитьИзВременногоХранилища(АдресВоВременномХранилище);
	ДвФайл.Записать(ИмяФайла);
	
	ОбъектЧтение = Новый ЧтениеТекста(ИмяФайла, "windows-1251");
	Текст = ОбъектЧтение.Прочитать();
	Текст= "<?xml version=""1.0"" encoding=""windows-1251""?>" + Символы.ПС +
	"<корневойЭлемент>" + Текст + "</корневойЭлемент>"; 
	ОбъектЧтение.Закрыть();
	
	ЧтениеXML = Новый ЧтениеXML;
	ЧтениеXML.УстановитьСтроку(Текст);
	ПостроительDOM = Новый ПостроительDOM;
	НовыйДокументDOM  = ПостроительDOM.Прочитать(ЧтениеXML);
	
	УзелDOM = ДокументDOM.ПолучитьЭлементыПоИмени("КонфиденциальнаяИнформация")[0];
	УзелDOM.Текстовоесодержимое="";
	
	Для каждого Узел из НовыйДокументDOM.ПолучитьЭлементыПоИмени("корневойЭлемент")[0].ДочерниеУзлы Цикл
		УзелDOM.ДобавитьДочерний(ДокументDOM.ИмпортироватьУзел(Узел, Истина));
	КонецЦикла;
	
	ОперацииСФайламиЭДКО.УдалитьВременныйФайл(ВременныйКаталогНаСервере);
	
КонецПроцедуры

Функция РегистрацияПолучитьУчетнуюЗапись(ИдентификаторАбонента)
	
	УчетныеЗаписиДокументооборота = Неопределено;
	
	Запрос = Новый Запрос;
	Запрос.Текст = 
	"ВЫБРАТЬ РАЗРЕШЕННЫЕ
	|	УчетныеЗаписиДокументооборота.Ссылка
	|ИЗ
	|	Справочник.УчетныеЗаписиДокументооборота КАК УчетныеЗаписиДокументооборота
	|ГДЕ
	|	УчетныеЗаписиДокументооборота.ИдентификаторАбонента = &ИдентификаторАбонента
	|	И НЕ УчетныеЗаписиДокументооборота.ПометкаУдаления";
	
	Запрос.УстановитьПараметр("ИдентификаторАбонента", ИдентификаторАбонента);
	
	Попытка
		Результат = Запрос.Выполнить();
		Выборка = Результат.Выбрать();
		
		Пока Выборка.Следующий() Цикл
			УчетныеЗаписиДокументооборота = Выборка.Ссылка;
		КонецЦикла;
	Исключение	КонецПопытки;
	
	Если УчетныеЗаписиДокументооборота = Неопределено Тогда
		УстановитьПривилегированныйРежим(Истина);	
		УчетныеЗаписиДокументооборота = Справочники.УчетныеЗаписиДокументооборота.СоздатьЭлемент();
		УчетныеЗаписиДокументооборота.ДатаПодключения = ТекущаяДатаСеанса();
		УстановитьПривилегированныйРежим(Ложь);
	КонецЕсли;
	
	Возврат УчетныеЗаписиДокументооборота;

КонецФункции

Функция РегистрацияПолучитьОрганизацию(УчетнаяЗапись)
	
	ОрганизацияСсылка = Неопределено;
	
	Запрос = Новый Запрос;
	Запрос.Текст = 
	"ВЫБРАТЬ РАЗРЕШЕННЫЕ
	|	Организации.Ссылка
	|ИЗ
	|	Справочник.Организации КАК Организации
	|ГДЕ
	|	Организации.УчетнаяЗаписьОбмена = &УчетнаяЗаписьОбмена";
	
	Запрос.УстановитьПараметр("УчетнаяЗаписьОбмена", УчетнаяЗапись);
	
	Попытка
		Результат = Запрос.Выполнить();
		Выборка = Результат.Выбрать();
		
		Пока Выборка.Следующий() Цикл
			Если Выборка.Ссылка.ВидОбменаСКонтролирующимиОрганами = Перечисления.ВидыОбменаСКонтролирующимиОрганами.ОбменВУниверсальномФормате Тогда
				ОрганизацияСсылка = Выборка.Ссылка; 
			КонецЕсли;
		КонецЦикла;
	Исключение	КонецПопытки;
	
	Возврат ОрганизацияСсылка;
	
КонецФункции

Функция МассивОтпечатковСертификатовАбонентовФСРАР(ДокументDOM) Экспорт
	
	Результат = Новый Массив;
	Сертификаты = ДокументDOM.ПолучитьЭлементыПоИмени("Сертификат");
	
	Для Каждого Сертификат из Сертификаты Цикл
		
		АтрибутТипПолучателя = Сертификат.Атрибуты.ПолучитьИменованныйЭлемент("ТипПолучателя");
		АтрибутОтпечаток = Сертификат.Атрибуты.ПолучитьИменованныйЭлемент("Отпечаток");
		Если АтрибутТипПолучателя <> Неопределено И АтрибутТипПолучателя.ТекстовоеСодержимое = "ФСРАР"
			И АтрибутОтпечаток <> Неопределено И НЕ ПустаяСтрока(АтрибутОтпечаток.ТекстовоеСодержимое) Тогда
			Результат.Добавить(НРег(АтрибутОтпечаток.ТекстовоеСодержимое));
		КонецЕсли;
		
	КонецЦикла;
	
	Возврат Результат;
	
КонецФункции

Функция СравнитьМассивыБезУчетаПорядкаЭлементов(Массив1, Массив2) Экспорт
	
	Если (НЕ ЗначениеЗаполнено(Массив1) И ЗначениеЗаполнено(Массив2))
	ИЛИ (НЕ ЗначениеЗаполнено(Массив2) И ЗначениеЗаполнено(Массив1)) Тогда
		Возврат Ложь;
	КонецЕсли;
	
	Если ЗначениеЗаполнено(Массив1) И ЗначениеЗаполнено(Массив2) Тогда
		// обходим элементы первого массива и ищем их во втором
		Для Каждого Эл Из Массив1 Цикл
			Если Массив2.Найти(Эл) = Неопределено Тогда
				Возврат Ложь;
			КонецЕсли;
		КонецЦикла;
		
		// обходим элементы второго массива и ищем их в первом
		Для Каждого Эл Из Массив2 Цикл
			Если Массив1.Найти(Эл) = Неопределено Тогда
				Возврат Ложь;
			КонецЕсли;
		КонецЦикла;
	КонецЕсли;
	
	Возврат Истина;
	
КонецФункции

Функция ПолучитьАтрибутУзлаXML(УзелDOM, НазваниеАтрибута, ЗначениеПоУмолчанию = "") Экспорт
	
	Если УзелDOM = Неопределено Тогда
		Возврат ЗначениеПоУмолчанию;
	КонецЕсли;
	
	Возврат УзелDOM.Атрибуты.ПолучитьИменованныйЭлемент(НазваниеАтрибута).ТекстовоеСодержимое;
	
КонецФункции

Функция ПолучитьЭлементУзлаXML(ДокументDOM, НазваниеУзла, ИндексЭлемента = 0, ЗначениеПоУмолчанию = Неопределено)
	
	Если ДокументDOM = Неопределено Тогда
		Возврат ЗначениеПоУмолчанию;
	КонецЕсли;
	
	УзелDOM = ДокументDOM.ПолучитьЭлементыПоИмени(НазваниеУзла);
	
	Если ИндексЭлемента < УзелDOM.Количество() Тогда
		Возврат УзелDOM[ИндексЭлемента];
	Иначе
		Возврат ЗначениеПоУмолчанию;
	КонецЕсли;
	
КонецФункции

Процедура РегистрацияРегистрФСРАР(
		ОтпечатокАбонента,
		ОтпечатокСубъектаРФ,
		ОтпечатокФСРАР,
		КодРегиона,
		Организация,
		ВыводитьСообщения,
		Настройки = Неопределено) Экспорт
	
	НастройкиВызова = Новый Структура;
	НастройкиВызова.Вставить("ПропускатьНезаданные", 		Ложь);
	НастройкиВызова.Вставить("ПрочитатьНастройкиОбмена", 	Истина);
	НастройкиВызова.Вставить("ЗаписатьНастройкиОбмена", 	Истина);
	Если Настройки <> Неопределено Тогда
		ЗаполнитьЗначенияСвойств(НастройкиВызова, Настройки);
	КонецЕсли;
	
	ФлагАвтозаполнения = Истина;
	ОсновнойОтпечатокСубъектаРФ = "";
	Если ОтпечатокСубъектаРФ <> Неопределено И ОтпечатокСубъектаРФ.Количество() > 0 Тогда
		ОсновнойОтпечатокСубъектаРФ = ОтпечатокСубъектаРФ[0];
	КонецЕсли;
	ОсновнойОтпечатокФСРАР = "";
	Если ОтпечатокФСРАР <> Неопределено И ОтпечатокФСРАР.Количество() > 0 Тогда
		ОсновнойОтпечатокФСРАР = ОтпечатокФСРАР[0];
	КонецЕсли;
	
	Если НастройкиВызова.ПрочитатьНастройкиОбмена Тогда
		Запрос = Новый Запрос;
		ТекстЗапроса = 
		"ВЫБРАТЬ РАЗРЕШЕННЫЕ
		|	НастройкиОбменаФСРАР.ИспользоватьАвтонастройку КАК ИспользоватьАвтонастройку,
		|	НастройкиОбменаФСРАР.ИспользоватьОбмен КАК ИспользоватьОбмен,
		|	НастройкиОбменаФСРАР.СертификатАбонентаОтпечаток КАК СертификатАбонентаОтпечаток,
		|	НастройкиОбменаФСРАР.СертификатСубъектаРФОтпечаток КАК СертификатСубъектаРФОтпечаток,
		|	НастройкиОбменаФСРАР.СертификатФСРАРОтпечаток КАК СертификатФСРАРОтпечаток,
		|	НастройкиОбменаФСРАР.КодРегиона КАК КодРегиона
		|ИЗ
		|	РегистрСведений.НастройкиОбменаФСРАР КАК НастройкиОбменаФСРАР
		|ГДЕ
		|	НастройкиОбменаФСРАР.Организация = &ОрганизацияСсылка";

		Запрос.Текст = ТекстЗапроса;
		Запрос.УстановитьПараметр("ОрганизацияСсылка", Организация);
		
		Попытка
			Выборка = Запрос.Выполнить().Выбрать();
		Исключение
			Возврат;
		КонецПопытки;
		
		ФлагСообщения 									= Ложь;
		ИспользоватьОбменВНастройках 					= Ложь;
		ИспользоватьАвтонастройкуВНастройках 			= Ложь;
		СертификатАбонентаОтпечатокВНастройках 			= "";
		СертификатФСРАРОтпечатокВНастройках 			= "";
		СертификатФСРАРСубъектаРФОтпечатокВНастройках 	= "";
		КодРегионаФСРАР 								= "";
		
		Если НЕ Выборка.Следующий() Тогда
			ФлагСообщения = Истина;
		Иначе
			Если НЕ Выборка.ИспользоватьАвтонастройку Тогда
				ФлагСообщения = Истина;
			КонецЕсли;
			Если Выборка.ИспользоватьОбмен И (Выборка.СертификатАбонентаОтпечаток = ОтпечатокАбонента
				И (Выборка.СертификатСубъектаРФОтпечаток = ОсновнойОтпечатокСубъектаРФ
				ИЛИ НастройкиВызова.ПропускатьНезаданные И НЕ ЗначениеЗаполнено(ОсновнойОтпечатокСубъектаРФ))
				И (Выборка.СертификатФСРАРОтпечаток = ОсновнойОтпечатокФСРАР
				ИЛИ НастройкиВызова.ПропускатьНезаданные И НЕ ЗначениеЗаполнено(ОсновнойОтпечатокФСРАР))
				И (Выборка.КодРегиона = КодРегиона ИЛИ НЕ ЗначениеЗаполнено(КодРегиона))
				ИЛИ НЕ Выборка.ИспользоватьАвтонастройку И НЕ ВыводитьСообщения)Тогда
				ФлагСообщения = Ложь;
				ФлагАвтозаполнения = Ложь;
			КонецЕсли;
			Если Выборка.ИспользоватьОбмен Тогда
				ИспользоватьОбменВНастройках 					= Истина;
				ИспользоватьАвтонастройкуВНастройках 			= Выборка.ИспользоватьАвтонастройку;
				СертификатАбонентаОтпечатокВНастройках 			= Выборка.СертификатАбонентаОтпечаток;
				СертификатФСРАРОтпечатокВНастройках 			= Выборка.СертификатФСРАРОтпечаток;
				СертификатФСРАРСубъектаРФОтпечатокВНастройках 	= Выборка.СертификатСубъектаРФОтпечаток;
				КодРегионаФСРАР 								= Выборка.КодРегиона;
			КонецЕсли;
		КонецЕсли;
		
		Настройки = Новый Структура;
		Настройки.Вставить("ОтпечатокСертификата", 								НРег(ОтпечатокАбонента));
		Настройки.Вставить("СертификатФСРАРОтпечаток", 							НРег(ОсновнойОтпечатокФСРАР));
		Настройки.Вставить("СертификатФСРАРСубъектаРФОтпечаток", 				НРег(ОсновнойОтпечатокСубъектаРФ));
		Настройки.Вставить("ФлагСообщения", 									ФлагСообщения);
		Настройки.Вставить("ФлагАвтозаполнения", 								ФлагАвтозаполнения);
		Настройки.Вставить("ИспользоватьОбменВНастройках", 						ИспользоватьОбменВНастройках);
		Настройки.Вставить("ИспользоватьАвтонастройкуВНастройках", 				ИспользоватьАвтонастройкуВНастройках);
		Настройки.Вставить("СертификатАбонентаОтпечатокВНастройках", 			СертификатАбонентаОтпечатокВНастройках);
		Настройки.Вставить("СертификатФСРАРОтпечатокВНастройках", 				СертификатФСРАРОтпечатокВНастройках);
		Настройки.Вставить("СертификатФСРАРСубъектаРФОтпечатокВНастройках", 	СертификатФСРАРСубъектаРФОтпечатокВНастройках);
		Настройки.Вставить("КодРегионаФСРАР", 									КодРегионаФСРАР);
	КонецЕсли;
	
	Если НастройкиВызова.ЗаписатьНастройкиОбмена И ФлагАвтозаполнения Тогда
		Попытка
			ОбменФСРАР = РегистрыСведений.НастройкиОбменаФСРАР.СоздатьМенеджерЗаписи();
			ОбменФСРАР.Организация = Организация.Ссылка;
			ОбменФСРАР.ИспользоватьОбмен = Истина;
			ОбменФСРАР.СертификатАбонентаОтпечаток = НРег(ОтпечатокАбонента);
			ОбменФСРАР.СертификатСубъектаРФОтпечаток = НРег(ОсновнойОтпечатокСубъектаРФ);
			ОбменФСРАР.СертификатФСРАРОтпечаток = НРег(ОсновнойОтпечатокФСРАР);
			ОбменФСРАР.КодРегиона = КодРегиона;
			ОбменФСРАР.ИспользоватьАвтонастройку = Истина;
			ОбменФСРАР.Записать();
			Если ВыводитьСообщения Тогда
				ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(
					НСтр("ru = 'Обновлены параметры отправки отчетности в Росалкогольрегулирование.'"));
			КонецЕсли;
		Исключение
			ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(СтрШаблон(
				НСтр("ru = 'Не удалось обновить параметры отправки отчетности в Росалкогольрегулирование, причина:
						   |""%1"".'"),
				ОписаниеОшибки()));
		КонецПопытки;
	КонецЕсли;
	
КонецПроцедуры

Процедура РегистрацияРегистрРПН(
		ОтпечатокАбонента,
		ОтпечатокРПН,
		Организация,
		ВыводитьСообщения,
		СтруктураАвтонастройкиРПН,
		ПропускатьНезаданные = Ложь) Экспорт
	
	Запрос = Новый Запрос;
	ТекстЗапроса = 
	"ВЫБРАТЬ РАЗРЕШЕННЫЕ
	|	НастройкиОбменаРПН.ИспользоватьАвтонастройку КАК ИспользоватьАвтонастройку,
	|	НастройкиОбменаРПН.ИспользоватьОбмен КАК ИспользоватьОбмен,
	|	НастройкиОбменаРПН.СертификатАбонентаОтпечаток КАК СертификатАбонентаОтпечаток,
	|	НастройкиОбменаРПН.СертификатРПНОтпечаток КАК СертификатРПНОтпечаток,
	|	НастройкиОбменаРПН.EMail КАК EMail,
	|	НастройкиОбменаРПН.Пароль КАК Пароль
	|ИЗ
	|	РегистрСведений.НастройкиОбменаРПН КАК НастройкиОбменаРПН
	|ГДЕ
	|	НастройкиОбменаРПН.Организация = &ОрганизацияСсылка";

	Запрос.Текст = ТекстЗапроса;
	Запрос.УстановитьПараметр("ОрганизацияСсылка", Организация);
	
	Попытка
		Выборка = Запрос.Выполнить().Выбрать();
	Исключение
		Возврат;
	КонецПопытки;
	
	ОсновнойОтпечатокРПН = "";
	Если ОтпечатокРПН <> Неопределено И ОтпечатокРПН.Количество() > 0 И ОтпечатокРПН[0] <> Неопределено Тогда
		Если ТипЗнч(ОтпечатокРПН[0]) = Тип("Строка") Тогда
			ОсновнойОтпечатокРПН = НРег(ОтпечатокРПН[0]);
		Иначе
			ОсновнойОтпечатокРПН = НРег(ОтпечатокРПН[0].ТекстовоеСодержимое);
		КонецЕсли;
	КонецЕсли;
	
	EMail 									= Неопределено;
	Пароль 									= Неопределено;
	ФлагСообщения 							= Ложь;
	ФлагАвтозаполнения 						= Истина;
	ИспользоватьОбменВНастройках 			= Ложь;
	ИспользоватьАвтонастройкуВНастройках 	= Ложь;
	СертификатАбонентаОтпечатокВНастройках 	= "";
	СертификатРПНОтпечатокВНастройках 		= "";
	
	Если НЕ Выборка.Следующий() Тогда
		ФлагСообщения = Истина;
	Иначе
		EMail = Выборка.EMail;
		Пароль = Выборка.Пароль;
		Если НЕ Выборка.ИспользоватьАвтонастройку Тогда
			ФлагСообщения = Истина;
		КонецЕсли;
		Если Выборка.ИспользоватьОбмен И (Выборка.СертификатАбонентаОтпечаток = ОтпечатокАбонента
			И (Выборка.СертификатРПНОтпечаток = ОсновнойОтпечатокРПН
			ИЛИ ПропускатьНезаданные И НЕ ЗначениеЗаполнено(ОсновнойОтпечатокРПН))
			ИЛИ НЕ Выборка.ИспользоватьАвтонастройку И НЕ ВыводитьСообщения)Тогда
			ФлагСообщения = Ложь;
			ФлагАвтозаполнения = Ложь;
		КонецЕсли;
		Если Выборка.ИспользоватьОбмен Тогда
			ИспользоватьОбменВНастройках 			= Истина;
			ИспользоватьАвтонастройкуВНастройках 	= Выборка.ИспользоватьАвтонастройку;
			СертификатАбонентаОтпечатокВНастройках 	= Выборка.СертификатАбонентаОтпечаток;
			СертификатРПНОтпечатокВНастройках 		= Выборка.СертификатРПНОтпечаток;
		КонецЕсли;
	КонецЕсли;
	
	СтруктураАвтонастройкиРПН = Новый Структура;
	СтруктураАвтонастройкиРПН.Вставить("ОтпечатокСертификата", 						НРег(ОтпечатокАбонента));
	СтруктураАвтонастройкиРПН.Вставить("СертификатРПНОтпечаток", 					НРег(ОсновнойОтпечатокРПН));
	СтруктураАвтонастройкиРПН.Вставить("ФлагСообщения", 							ФлагСообщения);
	СтруктураАвтонастройкиРПН.Вставить("ФлагАвтозаполнения", 						ФлагАвтозаполнения);
	СтруктураАвтонастройкиРПН.Вставить("EMail", 									EMail);
	СтруктураАвтонастройкиРПН.Вставить("Пароль", 									Пароль);
	СтруктураАвтонастройкиРПН.Вставить("ИспользоватьОбменВНастройках", 				ИспользоватьОбменВНастройках);
	СтруктураАвтонастройкиРПН.Вставить("ИспользоватьАвтонастройкуВНастройках", 		ИспользоватьАвтонастройкуВНастройках);
	СтруктураАвтонастройкиРПН.Вставить("СертификатАбонентаОтпечатокВНастройках", 	СертификатАбонентаОтпечатокВНастройках);
	СтруктураАвтонастройкиРПН.Вставить("СертификатРПНОтпечатокВНастройках", 		СертификатРПНОтпечатокВНастройках);
	
КонецПроцедуры

Процедура РегистрацияРегистрФТС(
		ОтпечатокАбонента,
		Организация,
		ВыводитьСообщения,
		СтруктураАвтонастройкиФТС,
		ПропускатьНезаданные = Ложь) Экспорт
	
	Запрос = Новый Запрос;
	ТекстЗапроса = 
	"ВЫБРАТЬ РАЗРЕШЕННЫЕ
	|	НастройкиОбменаФТС.ИспользоватьАвтонастройку КАК ИспользоватьАвтонастройку,
	|	НастройкиОбменаФТС.ИспользоватьОбмен КАК ИспользоватьОбмен,
	|	НастройкиОбменаФТС.СертификатАбонентаОтпечаток КАК СертификатАбонентаОтпечаток,
	|	НастройкиОбменаФТС.Логин КАК Логин,
	|	НастройкиОбменаФТС.Пароль КАК Пароль
	|ИЗ
	|	РегистрСведений.НастройкиОбменаФТС КАК НастройкиОбменаФТС
	|ГДЕ
	|	НастройкиОбменаФТС.Организация = &ОрганизацияСсылка";

	Запрос.Текст = ТекстЗапроса;
	Запрос.УстановитьПараметр("ОрганизацияСсылка", Организация);
	
	Попытка
		Выборка = Запрос.Выполнить().Выбрать();
	Исключение
		Возврат;
	КонецПопытки;
	
	Логин 									= Неопределено;
	Пароль 									= Неопределено;
	ФлагСообщения 							= Ложь;
	ФлагАвтозаполнения 						= Истина;
	ИспользоватьОбменВНастройках 			= Ложь;
	ИспользоватьАвтонастройкуВНастройках 	= Ложь;
	СертификатАбонентаОтпечатокВНастройках 	= "";
	
	Если НЕ Выборка.Следующий() Тогда
		ФлагСообщения = Истина;
	Иначе
		Логин = Выборка.Логин;
		Пароль = Выборка.Пароль;
		Если НЕ Выборка.ИспользоватьАвтонастройку Тогда
			ФлагСообщения = Истина;
		КонецЕсли;
		Если Выборка.ИспользоватьОбмен И (Выборка.СертификатАбонентаОтпечаток = ОтпечатокАбонента
			ИЛИ НЕ Выборка.ИспользоватьАвтонастройку И НЕ ВыводитьСообщения) Тогда
			ФлагСообщения = Ложь;
			ФлагАвтозаполнения = Ложь;
		КонецЕсли;
		Если Выборка.ИспользоватьОбмен Тогда
			ИспользоватьОбменВНастройках 			= Истина;
			ИспользоватьАвтонастройкуВНастройках 	= Выборка.ИспользоватьАвтонастройку;
			СертификатАбонентаОтпечатокВНастройках 	= Выборка.СертификатАбонентаОтпечаток;
		КонецЕсли;
	КонецЕсли;
	
	СтруктураАвтонастройкиФТС = Новый Структура;
	СтруктураАвтонастройкиФТС.Вставить("ОтпечатокСертификата", 						НРег(ОтпечатокАбонента));
	СтруктураАвтонастройкиФТС.Вставить("ФлагСообщения", 							ФлагСообщения);
	СтруктураАвтонастройкиФТС.Вставить("ФлагАвтозаполнения", 						ФлагАвтозаполнения);
	СтруктураАвтонастройкиФТС.Вставить("Логин", 									Логин);
	СтруктураАвтонастройкиФТС.Вставить("Пароль", 									Пароль);
	СтруктураАвтонастройкиФТС.Вставить("ИспользоватьОбменВНастройках", 				ИспользоватьОбменВНастройках);
	СтруктураАвтонастройкиФТС.Вставить("ИспользоватьАвтонастройкуВНастройках", 		ИспользоватьАвтонастройкуВНастройках);
	СтруктураАвтонастройкиФТС.Вставить("СертификатАбонентаОтпечатокВНастройках", 	СертификатАбонентаОтпечатокВНастройках);
	
КонецПроцедуры

Процедура РегистрацияОбновитьСправочникПолучателейПФРиФНСФСГС(
		Тип, 
		Код, 
		Наименование, 
		Сертификаты, 
		УчетнаяЗапись, 
		СхемаОтправкиМПД, 
		СхемаОтправкиЗНПЗДП, 
		ВыводитьСообщения)
	
	Если Тип = XMLСтрока(Перечисления.ТипыКонтролирующихОрганов.ФНС) Тогда
		Справочник = Справочники.НалоговыеОрганы;
	ИначеЕсли Тип = XMLСтрока(Перечисления.ТипыКонтролирующихОрганов.ПФР) ИЛИ Тип = "ЭДОК" ИЛИ Тип = "ЭТКПФР" Тогда
		Справочник = Справочники.ОрганыПФР;
	ИначеЕсли Тип = XMLСтрока(Перечисления.ТипыКонтролирующихОрганов.ФСГС) Тогда
		Справочник = Справочники.ОрганыФСГС;
	КонецЕсли;
	НазваниеСправочника = Метаданные.НайтиПоТипу(ТипЗнч(Справочник)).Синоним;
	
	НайденнаяСсылка = Справочник.НайтиПоКоду(Код);
	
	Если НайденнаяСсылка = Справочник.ПустаяСсылка() Тогда
		Получатель = Справочник.СоздатьЭлемент();
		Получатель.Код = Код;
		
		Если ВыводитьСообщения Тогда
			ТекстСообщения = СтроковыеФункцииКлиентСервер.ПодставитьПараметрыВСтроку(
				НСтр("ru = 'Создан новый элемент справочника ""%1"" с кодом ""%2"".'"), НазваниеСправочника, Код);
			ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
		КонецЕсли;
		
	Иначе 
		Получатель = НайденнаяСсылка.ПолучитьОбъект();
	КонецЕсли;
	
	Если Тип = XMLСтрока(Перечисления.ТипыКонтролирующихОрганов.ФНС) Тогда
		Если ПустаяСтрока(Получатель.Наименование) Тогда
			Получатель.Наименование = "ИФНС " + Строка(Код);
		КонецЕсли;
		Получатель.ПриемНалоговойОтчетности = Истина;
	ИначеЕсли Тип <> "ЭДОК" И Тип <> "ЭТКПФР" Тогда
		Получатель.Наименование = СокрЛП(Наименование);
	КонецЕсли;

	СертификатыОбновлены = Ложь;
	
	МассивСертификатов = Новый Массив;
	
	СертификатыПолучателя = ?(Тип = "ЭДОК" ИЛИ Тип = "ЭТКПФР", Получатель.СертификатыЭДОК, Получатель.Сертификаты);
	Для Каждого Сертификат из СертификатыПолучателя Цикл
		МассивСертификатов.Добавить(НРег(Сертификат.Сертификат));
	КонецЦикла;
	
	Если НЕ СравнитьМассивыБезУчетаПорядкаЭлементов(МассивСертификатов, Сертификаты) Тогда
		
		СертификатыПолучателя.Очистить();
		Для каждого Сертификат из Сертификаты Цикл
			НовыйСертификат = СертификатыПолучателя.Добавить();
			НовыйСертификат.Сертификат = НРег(Сертификат);
		КонецЦикла;
		
		СертификатыОбновлены = Истина;
		
	КонецЕсли;
	
	Если СертификатыОбновлены Тогда
		
		Модицифированность = Истина;
		
		Если НЕ Получатель.ЭтоНовый() И ВыводитьСообщения Тогда
			Если Тип = XMLСтрока(Перечисления.ТипыКонтролирующихОрганов.ФНС) Тогда
				ТекстСообщения = СтроковыеФункцииКлиентСервер.ПодставитьПараметрыВСтроку(
					НСтр("ru = 'Изменен сертификат налогового органа с кодом %1.'"), Код);				
			ИначеЕсли Тип = XMLСтрока(Перечисления.ТипыКонтролирующихОрганов.ПФР) Тогда
				ТекстСообщения = СтроковыеФункцииКлиентСервер.ПодставитьПараметрыВСтроку(
					НСтр("ru = 'Изменен сертификат органа ПФР с кодом %1.'"), Код);
			ИначеЕсли Тип = XMLСтрока(Перечисления.ТипыКонтролирующихОрганов.ФСГС) Тогда
				ТекстСообщения = СтроковыеФункцииКлиентСервер.ПодставитьПараметрыВСтроку(
					НСтр("ru = 'Изменен сертификат органа Росстат с кодом %1.'"), Код);
			КонецЕсли;
			ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
		КонецЕсли;
		
	КонецЕсли;
	
	ИзменилсяПризнакПолучения = Ложь;
	Если ТипЗнч(Получатель) = Тип("СправочникОбъект.ОрганыПФР") И Тип <> "ЭДОК" И Тип <> "ЭТКПФР" Тогда
		
		НастройкиМПД    = РазобратьНастройкиСхемыОтправкиОтчетностиПоФизЛицам(СхемаОтправкиМПД, Тип);
		НастройкиЗНПЗДП = РазобратьНастройкиСхемыОтправкиОтчетностиПоФизЛицам(СхемаОтправкиЗНПЗДП, Тип);

		ПринималМакетыПенсионныхДел = Получатель.ПринимаетМакетыПенсионныхДел;
		ПринималЗаявленияОНазначенииИДоставкеПенсии = Получатель.ПринимаетЗаявленияОНазначенииИДоставкеПенсии;
		
		Получатель.ПринимаетМакетыПенсионныхДел = ПолучательПринимаетОтчетностиПоФизЛицам(СхемаОтправкиМПД, Тип);
		Получатель.ПринимаетЗаявленияОНазначенииИДоставкеПенсии = ПолучательПринимаетОтчетностиПоФизЛицам(СхемаОтправкиЗНПЗДП, Тип);
		
		ИзменилсяПризнакПолучения = ПринималМакетыПенсионныхДел <> Получатель.ПринимаетМакетыПенсионныхДел
			ИЛИ ПринималЗаявленияОНазначенииИДоставкеПенсии <> Получатель.ПринимаетЗаявленияОНазначенииИДоставкеПенсии;
		
	КонецЕсли;
			
	Если СертификатыОбновлены ИЛИ Получатель.ЭтоНовый() ИЛИ ИзменилсяПризнакПолучения Тогда
		Получатель.Записать();
	КонецЕсли;
	
	Если ТипЗнч(Получатель) = Тип("СправочникОбъект.ОрганыПФР") И Тип <> "ЭДОК" И Тип <> "ЭТКПФР" Тогда
		
		ДобавитьЗаписьВРегистрПравилаОтправкиОтчетностиПоФизЛицам(
			НастройкиМПД,
			УчетнаяЗапись,
			Получатель,
			Перечисления.ВидыОтчетностиПоФизЛицам.МакетПенсионныхДел);
			
		ДобавитьЗаписьВРегистрПравилаОтправкиОтчетностиПоФизЛицам(
			НастройкиЗНПЗДП,
			УчетнаяЗапись,
			Получатель,
			Перечисления.ВидыОтчетностиПоФизЛицам.ЗаявлениеОНазначенииИДоставкеПенсии);
			
	КонецЕсли;
	
КонецПроцедуры

Функция РазобратьНастройкиСхемыОтправкиОтчетностиПоФизЛицам(СхемаОтправки, Тип)

	Настройки = Неопределено;
	
	Если Тип = XMLСтрока(Перечисления.ТипыКонтролирующихОрганов.ПФР)
		И СхемаОтправки.Количество() = 1 Тогда
		
		Настройки = Новый Структура;
		НастройкиВложение = Новый Структура;
		НастройкиТема = Новый Структура;
		
		// тема
		УзелТема = СхемаОтправки[0].ПолучитьЭлементыПоИмени("Тема")[0];
		НастройкиТема.Вставить("Маска", УзелТема.ПолучитьАтрибут("Маска"));
		
		// вложение
		УзелВложение = СхемаОтправки[0].ПолучитьЭлементыПоИмени("Вложение")[0];
		НастройкиВложение.Вставить("Маска", УзелВложение.ПолучитьАтрибут("Маска"));
		НастройкиВложение.Вставить("ДопустимыеТипы", УзелВложение.ПолучитьАтрибут("ДопустимыеТипы"));
		
		МаксимальныйРазмерФайла = УзелВложение.ПолучитьАтрибут("МаксимальныйРазмерФайла"); 
		НастройкиВложение.Вставить("МаксимальныйРазмерФайла", ?(ЗначениеЗаполнено(МаксимальныйРазмерФайла), Число(МаксимальныйРазмерФайла), 0));
		
		МаксимальныйРазмерВложения = УзелВложение.ПолучитьАтрибут("МаксимальныйРазмерВложения"); 
		НастройкиВложение.Вставить("МаксимальныйРазмерВложения", ?(ЗначениеЗаполнено(МаксимальныйРазмерВложения), Число(МаксимальныйРазмерВложения), 0));
		
		НастройкиВложение.Вставить("ТребуетсяАрхивирование", ?(УзелВложение.ПолучитьАтрибут("ТребуетсяАрхивирование")="true", Истина, Ложь));
		
		Настройки.Вставить("Тема", НастройкиТема);
		Настройки.Вставить("Вложение", НастройкиВложение);
		
	КонецЕсли;
	
	Возврат Настройки;
	
КонецФункции

Функция ПолучательПринимаетОтчетностиПоФизЛицам(СхемаОтправки, Тип)
	
	Возврат Тип = XMLСтрока(Перечисления.ТипыКонтролирующихОрганов.ПФР)
		И СхемаОтправки.Количество() = 1;
	
КонецФункции

Процедура ДобавитьЗаписьВРегистрПравилаОтправкиОтчетностиПоФизЛицам(Настройки, УчетнаяЗапись, Получатель, ВидОтчетности)
	
	Если ЗначениеЗаполнено(Настройки) Тогда
		МенеджерЗаписи = РегистрыСведений.ПравилаОтправкиОтчетностиПоФизЛицам.СоздатьМенеджерЗаписи();
		МенеджерЗаписи.УчетнаяЗапись = УчетнаяЗапись;
		МенеджерЗаписи.ОрганПФР = Получатель.Ссылка;
		МенеджерЗаписи.ВидОтчетности = ВидОтчетности;
		МенеджерЗаписи.Данные = Новый ХранилищеЗначения(Настройки);
		МенеджерЗаписи.Записать(Истина);			
	КонецЕсли;
	
КонецПроцедуры

Процедура РегистрацияОбновитьСправочникПолучателей(ДокументDOM, Организация, ВыводитьСообщения, СтруктураАвтонастройкиФСС, СтруктураАвтонастройкиРПН, СтруктураАвтонастройкиФТС, УчетнаяЗапись)
	
	ОтпечаткиСертификатовАбонентовФСРАР = МассивОтпечатковСертификатовАбонентовФСРАР(ДокументDOM);
	
	Получатели = ДокументDOM.ПолучитьЭлементыПоИмени("Получатель");
	ДополнительныеПолучатели = ДокументDOM.ПолучитьЭлементыПоИмени("ДополнительныйПолучатель");
	МассивСписковПолучателей = Новый Массив;
	МассивСписковПолучателей.Добавить(Получатели);
	Если ДополнительныеПолучатели <> Неопределено Тогда
		МассивСписковПолучателей.Добавить(ДополнительныеПолучатели);
	КонецЕсли;
	
	МассивСертификатовФСС = Неопределено;
	МассивСертификатовЭЛН = Неопределено;
	МассивСертификатовФСРАР = Неопределено;
	МассивСертификатовСубъектаРФ = Неопределено;
	КодРегиона = Неопределено;
	
	СертификатыПФР = Новый Соответствие;
	Для каждого ЭлементПолучатель Из Получатели Цикл
		Тип = ЭлектронныйДокументооборотСКонтролирующимиОрганами.ПолучитьЗначениеУзлаXML(ЭлементПолучатель, "ТипПолучателя");
		Код = ЭлектронныйДокументооборотСКонтролирующимиОрганами.ПолучитьЗначениеУзлаXML(ЭлементПолучатель, "КодПолучателя");
		Если Тип = XMLСтрока(Перечисления.ТипыКонтролирующихОрганов.ПФР) Тогда
			Если СертификатыПФР.Получить(Код) = Неопределено Тогда
				СертификатыПФР.Вставить(Код, Новый Массив);
			КонецЕсли;
			
			Сертификаты = ЭлементПолучатель.ПолучитьЭлементыПоИмени("Отпечаток");
			
			Для Каждого Сертификат из Сертификаты Цикл
				СертификатыПФР[Код].Добавить(НРег(Сертификат.текстовоесодержимое));
			КонецЦикла;
		КонецЕсли;
	КонецЦикла;
	
	ЕдиныеСертификатыПФРЭДОК = Новый Массив;
	СертификатыПФРЭДОК = Новый Соответствие;
	Если ДополнительныеПолучатели <> Неопределено Тогда
		Для каждого ЭлементПолучатель Из ДополнительныеПолучатели Цикл
			Тип = ЭлектронныйДокументооборотСКонтролирующимиОрганами.ПолучитьЗначениеУзлаXML(ЭлементПолучатель,
				"ТипПолучателя");
			Код = ЭлектронныйДокументооборотСКонтролирующимиОрганами.ПолучитьЗначениеУзлаXML(ЭлементПолучатель,
				"КодПолучателя");
			Если Тип = "ЭДОК" ИЛИ Тип = "ЭТКПФР" Тогда
				Код = СокрЛП(Код);
				ЭтоКодПФР = ЗначениеЗаполнено(Код) И СтроковыеФункцииКлиентСервер.ТолькоЦифрыВСтроке(Код);
				Если ЭтоКодПФР И СертификатыПФРЭДОК.Получить(Код) = Неопределено Тогда
					СертификатыПФРЭДОК.Вставить(Код, Новый Массив);
				КонецЕсли;
				
				Сертификаты = ЭлементПолучатель.ПолучитьЭлементыПоИмени("Отпечаток");
				
				Для Каждого Сертификат из Сертификаты Цикл
					Если ЭтоКодПФР Тогда
						СертификатыПФРЭДОК[Код].Добавить(НРег(Сертификат.текстовоесодержимое));
					Иначе
						ЕдиныеСертификатыПФРЭДОК.Добавить(НРег(Сертификат.текстовоесодержимое));
					КонецЕсли;
				КонецЦикла;
			КонецЕсли;
		КонецЦикла;
	КонецЕсли;
	
	Для Каждого Получатели Из МассивСписковПолучателей Цикл
		Для Каждого Получатель из Получатели Цикл
			
			Тип = ЭлектронныйДокументооборотСКонтролирующимиОрганами.ПолучитьЗначениеУзлаXML(Получатель,"ТипПолучателя");
			Код = ЭлектронныйДокументооборотСКонтролирующимиОрганами.ПолучитьЗначениеУзлаXML(Получатель,"КодПолучателя");
			Наименование = ЭлектронныйДокументооборотСКонтролирующимиОрганами.ПолучитьЗначениеУзлаXML(Получатель,"НаименованиеПолучателя");
			
			Если Тип <> XMLСтрока(Перечисления.ТипыКонтролирующихОрганов.ФТС) Тогда
				МассивСертификатов = Новый Массив;
				Сертификаты = Получатель.ПолучитьЭлементыПоИмени("Отпечаток");
				
				Для Каждого Сертификат из Сертификаты Цикл
					МассивСертификатов.Добавить(НРег(Сертификат.текстовоесодержимое));
				КонецЦикла;
			КонецЕсли;
			
			Если Тип = XMLСтрока(Перечисления.ТипыКонтролирующихОрганов.ФНС)
				ИЛИ Тип = XMLСтрока(Перечисления.ТипыКонтролирующихОрганов.ПФР) ИЛИ Тип = "ЭДОК" ИЛИ Тип = "ЭТКПФР"
				ИЛИ Тип = XMLСтрока(Перечисления.ТипыКонтролирующихОрганов.ФСГС) Тогда
				
				СхемаОтправкиМПД = Получатель.ПолучитьЭлементыПоИмени("СхемаОтправкиМПД");
				СхемаОтправкиЗНПЗДП = Получатель.ПолучитьЭлементыПоИмени("СхемаОтправкиЗНПЗДП");
				Если Тип = XMLСтрока(Перечисления.ТипыКонтролирующихОрганов.ПФР) Тогда
					МассивСертификатов = СертификатыПФР.Получить(Код);
					
				ИначеЕсли Тип = "ЭДОК" ИЛИ Тип = "ЭТКПФР" Тогда
					Код = СокрЛП(Код);
					ЭтоКодПФР = ЗначениеЗаполнено(Код) И СтроковыеФункцииКлиентСервер.ТолькоЦифрыВСтроке(Код);
					Если НЕ ЭтоКодПФР Тогда
						Для каждого СертификатПФР Из СертификатыПФР Цикл
							Код = СертификатПФР.Ключ;
							МассивСертификатов = СертификатыПФРЭДОК[Код];
							
							Если МассивСертификатов = Неопределено ИЛИ МассивСертификатов.Количество() = 0 Тогда
								РегистрацияОбновитьСправочникПолучателейПФРиФНСФСГС(
									Тип,
									Код,
									Наименование,
									ЕдиныеСертификатыПФРЭДОК,
									УчетнаяЗапись,
									СхемаОтправкиМПД,
									СхемаОтправкиЗНПЗДП,
									ВыводитьСообщения);
							КонецЕсли;
						КонецЦикла;
						
						Продолжить;
					КонецЕсли;
					
					МассивСертификатов = СертификатыПФРЭДОК.Получить(Код);
					Если МассивСертификатов = Неопределено ИЛИ МассивСертификатов.Количество() = 0 Тогда
						Продолжить;
					КонецЕсли;
				КонецЕсли;
				
				РегистрацияОбновитьСправочникПолучателейПФРиФНСФСГС(
					Тип,
					Код,
					Наименование,
					МассивСертификатов,
					УчетнаяЗапись,
					СхемаОтправкиМПД,
					СхемаОтправкиЗНПЗДП,
					ВыводитьСообщения);
					
			ИначеЕсли Тип = XMLСтрока(Перечисления.ТипыКонтролирующихОрганов.ФСС) И Организация <> Неопределено Тогда 
				МассивСертификатовФСС = МассивСертификатов;
			ИначеЕсли Тип = "ЭЛН" И Организация <> Неопределено Тогда
				МассивСертификатовЭЛН = МассивСертификатов;
			ИначеЕсли Тип = "ФСРАР" И Организация <> Неопределено Тогда
				МассивСертификатовФСРАР = МассивСертификатов;
			ИначеЕсли Тип = "ОИВ" И Организация <> Неопределено Тогда
				МассивСертификатовСубъектаРФ = МассивСертификатов;
				КодРегиона = ЭлектронныйДокументооборотСКонтролирующимиОрганами.ПолучитьЗначениеУзлаXML(Получатель, "КодПолучателя");
			ИначеЕсли Тип = XMLСтрока(Перечисления.ТипыКонтролирующихОрганов.РПН) И Организация <> Неопределено Тогда
				Узел = ДокументDOM.ПолучитьЭлементыПоИмени("ВладелецЭЦП")[0];
				ОтпечатокСертификата = ЭлектронныйДокументооборотСКонтролирующимиОрганами.ПолучитьЗначениеУзлаXML(Узел, "Отпечаток", , ОтпечаткиСертификатовАбонентовФСРАР, Истина);
				РегистрацияРегистрРПН(ОтпечатокСертификата, Сертификаты, Организация, ВыводитьСообщения, СтруктураАвтонастройкиРПН);
			ИначеЕсли Тип = XMLСтрока(Перечисления.ТипыКонтролирующихОрганов.ФТС) И Организация <> Неопределено Тогда
				Узел = ДокументDOM.ПолучитьЭлементыПоИмени("ВладелецЭЦП")[0];
				ОтпечатокСертификата = ЭлектронныйДокументооборотСКонтролирующимиОрганами.ПолучитьЗначениеУзлаXML(Узел, "Отпечаток", , ОтпечаткиСертификатовАбонентовФСРАР, Истина);
				РегистрацияРегистрФТС(ОтпечатокСертификата, Организация, ВыводитьСообщения, СтруктураАвтонастройкиФТС);
			КонецЕсли;
			
		КонецЦикла;
	КонецЦикла;
	
	Если Организация <> Неопределено Тогда
		
		Узел = ДокументDOM.ПолучитьЭлементыПоИмени("ВладелецЭЦП")[0];
		
		ОтпечатокСертификата = ЭлектронныйДокументооборотСКонтролирующимиОрганами.ПолучитьЗначениеУзлаXML(Узел, "Отпечаток", , ОтпечаткиСертификатовАбонентовФСРАР, Истина);
		
		Если МассивСертификатовФСС <> Неопределено ИЛИ МассивСертификатовЭЛН <> Неопределено Тогда
			СтруктураАвтонастройкиФСС = НастроитьОбменФСС(
				МассивСертификатовФСС,
				МассивСертификатовЭЛН,
				ОтпечатокСертификата,
				Организация,
				ВыводитьСообщения);
		КонецЕсли;
		
		ОтпечатокСертификата = ЭлектронныйДокументооборотСКонтролирующимиОрганами.ПолучитьЗначениеУзлаXML(Узел, "Отпечаток",, ОтпечаткиСертификатовАбонентовФСРАР, Ложь);
		
			Если (МассивСертификатовФСРАР <> Неопределено ИЛИ МассивСертификатовСубъектаРФ <> Неопределено) И НЕ(ЗначениеЗаполнено(ОтпечатокСертификата)) Тогда
				
				ОтпечатокСертификата = ЭлектронныйДокументооборотСКонтролирующимиОрганами.ПолучитьЗначениеУзлаXML(Узел, "Отпечаток",, ОтпечаткиСертификатовАбонентовФСРАР, Истина);
				
			КонецЕсли;
			
		Если ЗначениеЗаполнено(ОтпечатокСертификата) ИЛИ МассивСертификатовФСРАР <> Неопределено ИЛИ МассивСертификатовСубъектаРФ <> Неопределено Тогда
			РегистрацияРегистрФСРАР(ОтпечатокСертификата, МассивСертификатовСубъектаРФ, МассивСертификатовФСРАР, КодРегиона, Организация, ВыводитьСообщения);
		КонецЕсли;
		
	КонецЕсли;
	
КонецПроцедуры

Функция НастроитьОбменФСС(
		Сертификаты,
		СертификатыЭЛН,
		ОтпечатокСертификата,
		Организация,
		ВыводитьСообщения,
		ПропускатьНезаданные = Ложь) Экспорт
	
	СертификатФССОтпечаток = ?(Сертификаты <> Неопределено, нрег(Сертификаты[0]), "");
	СертификатФССЭЛНОтпечаток = ?(СертификатыЭЛН <> Неопределено, нрег(СертификатыЭЛН[0]), "");
	
	Запрос = Новый Запрос;
	ТекстЗапроса = 
	"ВЫБРАТЬ РАЗРЕШЕННЫЕ
	|	НастройкиОбменаФСС.ИспользоватьАвтонастройку КАК ИспользоватьАвтонастройку,
	|	НастройкиОбменаФСС.ИспользоватьОбмен КАК ИспользоватьОбмен,
	|	НастройкиОбменаФСС.СертификатСтрахователяОтпечаток КАК СертификатСтрахователяОтпечаток,
	|	НастройкиОбменаФСС.СертификатФССОтпечаток КАК СертификатФССОтпечаток,
	|	НастройкиОбменаФСС.СертификатФССЭЛНОтпечаток КАК СертификатФССЭЛНОтпечаток
	|ИЗ
	|	РегистрСведений.НастройкиОбменаФСС КАК НастройкиОбменаФСС
	|ГДЕ
	|	НастройкиОбменаФСС.Организация = &ОрганизацияСсылка
	|	И (НастройкиОбменаФСС.Пользователь ЕСТЬ NULL
	|	ИЛИ НастройкиОбменаФСС.Пользователь = ЗНАЧЕНИЕ(Справочник.Пользователи.ПустаяСсылка))";
	
	Запрос.Текст = ТекстЗапроса;
	Запрос.УстановитьПараметр("ОрганизацияСсылка", Организация);
	Выборка = Запрос.Выполнить().Выбрать();
	
	ФлагСообщения 								= Ложь;
	ФлагАвтозаполнения 							= Истина;
	ИспользоватьОбменВНастройках 				= Ложь;
	ИспользоватьАвтонастройкуВНастройках 		= Ложь;
	СертификатСтрахователяОтпечатокВНастройках 	= "";
	СертификатФССОтпечатокВНастройках 			= "";
	СертификатФССЭЛНОтпечатокВНастройках 		= "";
	
	Если НЕ Выборка.Следующий() Тогда
		ФлагСообщения = Истина;
	Иначе
		Если НЕ Выборка.ИспользоватьАвтонастройку Тогда
			ФлагСообщения = Истина;
		КонецЕсли;
		Если Выборка.ИспользоватьОбмен И (Выборка.СертификатСтрахователяОтпечаток = нрег(ОтпечатокСертификата)
			И (Выборка.СертификатФССОтпечаток = СертификатФССОтпечаток
			ИЛИ ПропускатьНезаданные И НЕ ЗначениеЗаполнено(СертификатФССОтпечаток))
			И (НЕ ЗначениеЗаполнено(СертификатФССЭЛНОтпечаток)
			ИЛИ Выборка.СертификатФССЭЛНОтпечаток = СертификатФССЭЛНОтпечаток)
			ИЛИ НЕ Выборка.ИспользоватьАвтонастройку И НЕ ВыводитьСообщения) Тогда
			ФлагСообщения = Ложь;
			ФлагАвтозаполнения = Ложь;
		КонецЕсли;
		Если Выборка.ИспользоватьОбмен Тогда
			ИспользоватьОбменВНастройках 				= Истина;
			ИспользоватьАвтонастройкуВНастройках 		= Выборка.ИспользоватьАвтонастройку;
			СертификатСтрахователяОтпечатокВНастройках 	= Выборка.СертификатСтрахователяОтпечаток;
			СертификатФССОтпечатокВНастройках 			= Выборка.СертификатФССОтпечаток;
			СертификатФССЭЛНОтпечатокВНастройках 		= Выборка.СертификатФССЭЛНОтпечаток;
		КонецЕсли;
	КонецЕсли;
	
	СтруктураАвтонастройкиФСС = Новый Структура;
	СтруктураАвтонастройкиФСС.Вставить("ОтпечатокСертификата", 							нрег(ОтпечатокСертификата));
	СтруктураАвтонастройкиФСС.Вставить("СертификатФССОтпечаток", 						СертификатФССОтпечаток);
	СтруктураАвтонастройкиФСС.Вставить("СертификатФССЭЛНОтпечаток", 					СертификатФССЭЛНОтпечаток);
	СтруктураАвтонастройкиФСС.Вставить("ФлагСообщения", 								ФлагСообщения);
	СтруктураАвтонастройкиФСС.Вставить("ФлагАвтозаполнения", 							ФлагАвтозаполнения);
	СтруктураАвтонастройкиФСС.Вставить("ИспользоватьОбменВНастройках", 					ИспользоватьОбменВНастройках);
	СтруктураАвтонастройкиФСС.Вставить("ИспользоватьАвтонастройкуВНастройках", 			ИспользоватьАвтонастройкуВНастройках);
	СтруктураАвтонастройкиФСС.Вставить("СертификатСтрахователяОтпечатокВНастройках", 	СертификатСтрахователяОтпечатокВНастройках);
	СтруктураАвтонастройкиФСС.Вставить("СертификатФССОтпечатокВНастройках", 			СертификатФССОтпечатокВНастройках);
	СтруктураАвтонастройкиФСС.Вставить("СертификатФССЭЛНОтпечатокВНастройках", 			СертификатФССЭЛНОтпечатокВНастройках);
	
	Возврат СтруктураАвтонастройкиФСС;
	
КонецФункции

Функция ПараметрыРегистрацияОбновитьСправочники() Экспорт
	
	ПараметрыФункции = Новый Структура;
	ПараметрыФункции.Вставить("ИдентификаторАбонента");
	ПараметрыФункции.Вставить("СпецОператорСвязи");
	ПараметрыФункции.Вставить("ВременныйФайлОбновленийАдрес");
	ПараметрыФункции.Вставить("АдресВоВременномХранилище");
	ПараметрыФункции.Вставить("Организация");
	ПараметрыФункции.Вставить("ФайлИзКэша");
	ПараметрыФункции.Вставить("ТихийРежим");
	ПараметрыФункции.Вставить("РежимАвтонастройки");
	ПараметрыФункции.Вставить("ВызовИзМастераАвтонастройки");
	ПараметрыФункции.Вставить("СтруктураАвтонастройкиФСС");
	ПараметрыФункции.Вставить("СтруктураАвтонастройкиРПН");
	ПараметрыФункции.Вставить("СтруктураАвтонастройкиФТС");
	ПараметрыФункции.Вставить("ЭтоНоваяУчетнаяЗапись");
	ПараметрыФункции.Вставить("ВызовИзМастераПодключенияК1СОтчетности", Ложь);
	ПараметрыФункции.Вставить("ТекстОшибокДляМастераПодключенияК1СОтчетности", "");
	
	ПараметрыФункции.Вставить("ЭтоЭлектроннаяПодписьВМоделиСервиса");
	ПараметрыФункции.Вставить("ТелефонМобильныйДляАвторизации");
	ПараметрыФункции.Вставить("ИдентификаторДокументооборота");
	
	ПараметрыФункции.Вставить("СохранениеВИБОтключено");
	ПараметрыФункции.Вставить("СтруктураДляИнформацииОСервереОнлайнПроверки");
	
	ПараметрыФункции.Вставить("НоваяУчетнаяЗапись");
	
	ПараметрыФункции.Вставить(
		"ОбязательныеПараметры",
		"ИдентификаторАбонента,СпецОператорСвязи,ВременныйФайлОбновленийАдрес,
		|АдресВоВременномХранилище,Организация,ФайлИзКэша,ТихийРежим,
		|РежимАвтонастройки,ВызовИзМастераАвтонастройки,СтруктураАвтонастройкиФСС,СтруктураАвтонастройкиРПН,СтруктураАвтонастройкиФТС,
		|ЭтоНоваяУчетнаяЗапись"); // обязательные параметры, которые нужно заполнять
	
	Возврат ПараметрыФункции;
	
КонецФункции

Функция РегистрацияОбновитьСправочники(ПараметрыФункции) Экспорт
	
	ВременныйКаталогНаСервере = ОперацииСФайламиЭДКО.СоздатьВременныйКаталог();
	ИмяВременногоФайлаОбновлений = ВременныйКаталогНаСервере + ПараметрыФункции.ИдентификаторАбонента + ".xml";
	ДвФайл = ПолучитьИзВременногоХранилища(ПараметрыФункции.ВременныйФайлОбновленийАдрес);
	ДвФайл.Записать(ИмяВременногоФайлаОбновлений);
	ДокументDOM = ЭлектронныйДокументооборотСКонтролирующимиОрганами.ЗагрузитьФайлXML(ИмяВременногоФайлаОбновлений);
	
	Если НЕ ПараметрыФункции.ФайлИзКэша Тогда
		РегистрацияСобратьРасшифрованныйФайл(ДокументDOM, ПараметрыФункции.АдресВоВременномХранилище);
	КонецЕсли;
	
	ИдентификаторАбонента = ЭлектронныйДокументооборотСКонтролирующимиОрганами.ПолучитьЗначениеУзлаXML(ДокументDOM,"ИдентификаторАбонента");
	ИдентификаторСпецоператора = ЭлектронныйДокументооборотСКонтролирующимиОрганами.ПолучитьЗначениеУзлаXML(ДокументDOM,"ИдентификаторСпецоператора");
	ПараметрыФункции.ИдентификаторАбонента = Строка(ИдентификаторСпецоператора) + Строка(ИдентификаторАбонента);
	
	УчетнаяЗапись = РегистрацияПолучитьУчетнуюЗапись(ПараметрыФункции.ИдентификаторАбонента);
	ПараметрыФункции.ЭтоНоваяУчетнаяЗапись = НЕ ЗначениеЗаполнено(УчетнаяЗапись.Ссылка);
	
	Если ПараметрыФункции.Организация = Неопределено Тогда
		ПараметрыФункции.Организация = РегистрацияПолучитьОрганизацию(УчетнаяЗапись);
	КонецЕсли;
	
	Если ПараметрыФункции.ЭтоНоваяУчетнаяЗапись Тогда
		УчетнаяЗаписьСсылка = Справочники.УчетныеЗаписиДокументооборота.ПолучитьСсылку();
		УчетнаяЗапись.УстановитьСсылкуНового(УчетнаяЗаписьСсылка);
	Иначе
		УчетнаяЗаписьСсылка = УчетнаяЗапись.Ссылка;
	КонецЕсли;
		
	Если НЕ ПараметрыФункции.СохранениеВИБОтключено Тогда
		ВыводитьСообщения = НЕ (ПараметрыФункции.ВызовИзМастераПодключенияК1СОтчетности или ПараметрыФункции.ТихийРежим ИЛИ ПараметрыФункции.РежимАвтонастройки);
		РегистрацияОбновитьСправочникПолучателей(ДокументDOM, ПараметрыФункции.Организация, ВыводитьСообщения, ПараметрыФункции.СтруктураАвтонастройкиФСС, ПараметрыФункции.СтруктураАвтонастройкиРПН, ПараметрыФункции.СтруктураАвтонастройкиФТС, УчетнаяЗаписьСсылка);
	КонецЕсли;
	
	Результат = РегистрацияОбновитьСправочникУчетныхЗаписей(
		УчетнаяЗапись, ДокументDOM, ПараметрыФункции.СпецОператорСвязи, 
		ПараметрыФункции.ЭтоНоваяУчетнаяЗапись, ПараметрыФункции.ТихийРежим, ПараметрыФункции.ВызовИзМастераАвтонастройки, 
		ПараметрыФункции.ВызовИзМастераПодключенияК1СОтчетности, 
		ПараметрыФункции.ТекстОшибокДляМастераПодключенияК1СОтчетности,
		ПараметрыФункции.ЭтоЭлектроннаяПодписьВМоделиСервиса,
		ПараметрыФункции.ТелефонМобильныйДляАвторизации,
		ПараметрыФункции.ИдентификаторДокументооборота,
		ПараметрыФункции.СохранениеВИБОтключено,
		ПараметрыФункции.СтруктураДляИнформацииОСервереОнлайнПроверки,
		ПараметрыФункции.Организация,
		ПараметрыФункции.НоваяУчетнаяЗапись);
		
	Если НЕ ПараметрыФункции.ФайлИзКэша Тогда
		ИмяФайла = ПолучитьИмяВременногоФайла();
		ЗаписьXML = Новый ЗаписьXML;
		ЗаписьXML.ОткрытьФайл(ИмяФайла);
		ЗаписьDOM = Новый ЗаписьDOM;
		ЗаписьDOM.Записать(ДокументDOM, ЗаписьXML);
		ЗаписьXML.Закрыть();
		СохранитьВКэшеФайлНастроекСпецоператора(ПараметрыФункции.НоваяУчетнаяЗапись, ИмяФайла, XMLЗначение(Тип("Дата"),ДокументDOM.ПолучитьЭлементыПоИмени("РегистрационныйФайл")[0].Атрибуты.ПолучитьИменованныйЭлемент("ДатаВремяФормирования").Текстовоесодержимое));
		
		ОперацииСФайламиЭДКО.УдалитьВременныйФайл(ИмяФайла);
	КонецЕсли;
	
	ОперацииСФайламиЭДКО.УдалитьВременныйФайл(ВременныйКаталогНаСервере);
	
	Возврат Результат;
КонецФункции

Процедура СохранитьВКэшеФайлНастроекСпецоператора(УчетнаяЗапись, ИмяФайла, ДатаФайла)
	
	УчетнаяЗаписьСсылка = УчетнаяЗапись.Ссылка;
	Если НЕ ЗначениеЗаполнено(УчетнаяЗаписьСсылка) Тогда
		Возврат;
	КонецЕсли;
	
	МенеджерЗаписи = РегистрыСведений.ФайлыСведенийСпецоператоры.СоздатьМенеджерЗаписи();
	МенеджерЗаписи.УчетнаяЗапись = УчетнаяЗаписьСсылка;
	МенеджерЗаписи.ДатаФайла = ДатаФайла;
	
	Попытка
		МенеджерЗаписи.Размер = ОпределитьРазмер(ИмяФайла);
		МенеджерЗаписи.Данные = Новый ХранилищеЗначения(Новый ДвоичныеДанные(ИмяФайла), Новый СжатиеДанных);
		МенеджерЗаписи.Записать(Истина);
	Исключение
	КонецПопытки;
	
КонецПроцедуры

Функция ПолучитьСерверДокументооборотаПоСертификату(ОтпечатокСертификата)
	
	Запрос = Новый Запрос;
	Запрос.Текст =
	"ВЫБРАТЬ
	|	СерверыДокументооборота.Ссылка
	|ИЗ
	|	Справочник.СерверыДокументооборота КАК СерверыДокументооборота
	|ГДЕ
	|	НЕ СерверыДокументооборота.ПометкаУдаления
	|	И СерверыДокументооборота.Сертификат = &Сертификат";
	Запрос.УстановитьПараметр("Сертификат", ОтпечатокСертификата);
	
	Результат = Запрос.Выполнить();
	Если Результат.Пустой() Тогда
		Возврат Неопределено;
	Иначе
		Выборка = Результат.Выбрать();
		Выборка.Следующий();
		Возврат Выборка.Ссылка;
	КонецЕсли;
	
КонецФункции

Функция РегистрацияСоздатьСерверДокументооборота(Узел)
	
	ОтпечатокСертификата = ЭлектронныйДокументооборотСКонтролирующимиОрганами.ПолучитьЗначениеУзлаXML(Узел, "Отпечаток");
	Если НЕ ЗначениеЗаполнено(ОтпечатокСертификата) Тогда
		Возврат Неопределено;
	КонецЕсли;
	
	СерверДокументооборотаСсылка = ПолучитьСерверДокументооборотаПоСертификату(ОтпечатокСертификата);
	Если ЗначениеЗаполнено(СерверДокументооборотаСсылка) Тогда
		НовыйОбъектСправочника = СерверДокументооборотаСсылка.ПолучитьОбъект();
	Иначе
		НовыйОбъектСправочника = Справочники.СерверыДокументооборота.СоздатьЭлемент();
		КонецЕсли;
	
	НовыйОбъектСправочника.Сертификат = ОтпечатокСертификата;
	НовыйОбъектСправочника.АдресЭлектроннойПочтыПФР  = ЭлектронныйДокументооборотСКонтролирующимиОрганами.ПолучитьЗначениеУзлаXML(Узел, "АдресЭлектроннойПочтыПФР");
	НовыйОбъектСправочника.АдресЭлектроннойПочтыФНС  = ЭлектронныйДокументооборотСКонтролирующимиОрганами.ПолучитьЗначениеУзлаXML(Узел, "АдресЭлектроннойПочтыФНС");
	НовыйОбъектСправочника.АдресЭлектроннойПочтыФСГС = ЭлектронныйДокументооборотСКонтролирующимиОрганами.ПолучитьЗначениеУзлаXML(Узел, "АдресЭлектроннойПочтыФСГС");
	НовыйОбъектСправочника.ДлительностьОжиданияСервера = 100;
	НовыйОбъектСправочника.Наименование = ЭлектронныйДокументооборотСКонтролирующимиОрганами.ПолучитьЗначениеУзлаXML(Узел, "Наименование");
	НовыйОбъектСправочника.Записать();
	
	Возврат НовыйОбъектСправочника.Ссылка;
	
КонецФункции

Функция РегистрацияПолучитьСерверОнлайнПроверки(Узел) Экспорт
	
	Если Узел = Неопределено Тогда
		Возврат Неопределено;
	КонецЕсли;
	
	ОтпечатокСертификата = ЭлектронныйДокументооборотСКонтролирующимиОрганами.ПолучитьЗначениеУзлаXML(Узел, "Отпечаток");
	Если НЕ ЗначениеЗаполнено(ОтпечатокСертификата) Тогда
		Возврат Неопределено;
	КонецЕсли;
	
	НаименованиеСервера = ЭлектронныйДокументооборотСКонтролирующимиОрганами.ПолучитьЗначениеУзлаXML(Узел, "Наименование");
	
	ТипРеализацииСервера = ЭлектронныйДокументооборотСКонтролирующимиОрганами.ПолучитьЗначениеУзлаXML(Узел, "ТипРеализации");
	
	УзелРесурса = ПолучитьЭлементУзлаXML(Узел, "Ресурс", 0);
	ПараметрыРесурса = Новый Структура("Определение, URIПространстваИменСервиса, ИмяСервиса, ИмяТочкиПодключения", "", "", "", "");
	ПараметрыРесурса.Определение = ПолучитьАтрибутУзлаXML(УзелРесурса, "Определение");
	ПараметрыРесурса.URIПространстваИменСервиса = ПолучитьАтрибутУзлаXML(УзелРесурса, "URIПространстваИменСервиса");
	ПараметрыРесурса.ИмяСервиса = ПолучитьАтрибутУзлаXML(УзелРесурса, "ИмяСервиса");
	ПараметрыРесурса.ИмяТочкиПодключения = ПолучитьАтрибутУзлаXML(УзелРесурса, "ИмяТочкиПодключения");
	
	Результат = Новый Структура("Наименование, ТипРеализации, Ресурс, Отпечаток", НаименованиеСервера, ТипРеализацииСервера, ПараметрыРесурса, ОтпечатокСертификата);
	
	Возврат Результат;
	
КонецФункции

Функция РегистрацияОбновитьСправочникУчетныхЗаписей(
		Запись, 
		ДокументDOM, 
		СпецОператорСвязи, 
		ЭтоНоваяУчетнаяЗапись, 
		ТихийРежим, 
		ВызовИзМастераАвтонастройки,
		ВызовИзМастераПодключенияК1СОтчетности = Ложь, 
		ТекстОшибокДляМастераПодключенияК1СОтчетности = "",
		ЭтоЭлектроннаяПодписьВМоделиСервиса = Ложь, 
		ТелефонМобильныйДляАвторизации = "", 
		ИдентификаторДокументооборота = "",
		СохранениеВИБОтключено = Ложь, 
		СтруктураДляИнформацииОСервереОнлайнПроверки = Неопределено, 
		Организация = Неопределено,
		НоваяУчетнаяЗапись = Неопределено) Экспорт
	
	Попытка
		
		Если НЕ ЭтоНоваяУчетнаяЗапись Тогда
			Запись = Запись.ПолучитьОбъект();
		КонецЕсли;
		
		Если Запись.ЭлектроннаяПодписьВМоделиСервиса <> ЭтоЭлектроннаяПодписьВМоделиСервиса Тогда
			Запись.ЭлектроннаяПодписьВМоделиСервиса = ЭтоЭлектроннаяПодписьВМоделиСервиса;
		КонецЕсли;
		Если СокрЛП(Запись.ТелефонМобильныйДляАвторизации) <> ТелефонМобильныйДляАвторизации Тогда
			Запись.ТелефонМобильныйДляАвторизации = ТелефонМобильныйДляАвторизации;
		КонецЕсли;
		Если СокрЛП(Запись.ИдентификаторДокументооборота) <> ИдентификаторДокументооборота Тогда
			Запись.ИдентификаторДокументооборота = ИдентификаторДокументооборота;
		КонецЕсли;
		
		Если Запись.СпецОператорСвязи <> СпецОператорСвязи Тогда
			Запись.СпецОператорСвязи = СпецОператорСвязи;
		КонецЕсли;
		
		ИдентификаторАбонента 		= ЭлектронныйДокументооборотСКонтролирующимиОрганами.ПолучитьЗначениеУзлаXML(ДокументDOM,"ИдентификаторАбонента");
		ИдентификаторСпецоператора  = ЭлектронныйДокументооборотСКонтролирующимиОрганами.ПолучитьЗначениеУзлаXML(ДокументDOM,"ИдентификаторСпецоператора");
		ИдентификаторАбонента 		= Строка(ИдентификаторСпецоператора) + Строка(ИдентификаторАбонента);
		
		Если СокрЛП(Запись.ИдентификаторАбонента) <> ИдентификаторАбонента Тогда
			Запись.ИдентификаторАбонента = ИдентификаторАбонента;
		КонецЕсли;
		
		Если СокрЛП(Запись.ИдентификаторСпецоператора) <> ИдентификаторСпецоператора Тогда
			Запись.ИдентификаторСпецоператора = ИдентификаторСпецоператора;
		КонецЕсли;
		
		Если СокрЛП(Запись.АдресЭлектроннойПочты) <> ЭлектронныйДокументооборотСКонтролирующимиОрганами.ПолучитьЗначениеУзлаXML(ДокументDOM,"АдресЭлектроннойПочты") Тогда
			Запись.АдресЭлектроннойПочты = ЭлектронныйДокументооборотСКонтролирующимиОрганами.ПолучитьЗначениеУзлаXML(ДокументDOM,"АдресЭлектроннойПочты");
		КонецЕсли;
		
		Значение = ЭлектронныйДокументооборотСКонтролирующимиОрганами.ПолучитьЗначениеУзлаXML(ДокументDOM, "ПризнакУполномоченногоПредставителя", Ложь, , , Тип("Булево"));
		
		Если Запись.ЯвляетсяУчетнойЗаписьюУполномоченногоПредставителя <> Значение Тогда
			Запись.ЯвляетсяУчетнойЗаписьюУполномоченногоПредставителя = Значение;
		КонецЕсли;
		
		Если Запись.ЯвляетсяУчетнойЗаписьюУполномоченногоПредставителя Тогда
			
			Если СокрЛП(Запись.ПолноеНаименованиеУполномоченногоПредставителя) <> ЭлектронныйДокументооборотСКонтролирующимиОрганами.ПолучитьЗначениеУзлаXML(ДокументDOM,"ПолноеНаименование") Тогда
				Запись.ПолноеНаименованиеУполномоченногоПредставителя = ЭлектронныйДокументооборотСКонтролирующимиОрганами.ПолучитьЗначениеУзлаXML(ДокументDOM,"ПолноеНаименование");
			КонецЕсли;
			
			Если СокрЛП(Запись.ИННУполномоченногоПредставителя) <> ЭлектронныйДокументооборотСКонтролирующимиОрганами.ПолучитьЗначениеУзлаXML(ДокументDOM,"ИНН") Тогда
				Запись.ИННУполномоченногоПредставителя  = ЭлектронныйДокументооборотСКонтролирующимиОрганами.ПолучитьЗначениеУзлаXML(ДокументDOM,"ИНН");
			КонецЕсли;
			
			Если СокрЛП(Запись.КППУполномоченногоПредставителя) <> ЭлектронныйДокументооборотСКонтролирующимиОрганами.ПолучитьЗначениеУзлаXML(ДокументDOM,"КПП") Тогда
				Запись.КППУполномоченногоПредставителя  = ЭлектронныйДокументооборотСКонтролирующимиОрганами.ПолучитьЗначениеУзлаXML(ДокументDOM,"КПП");
			КонецЕсли;
			
		КонецЕсли;
		
		ОтпечаткиСертификатовАбонентовФСРАР = МассивОтпечатковСертификатовАбонентовФСРАР(ДокументDOM);
		
		Узел = ДокументDOM.ПолучитьЭлементыПоИмени("ВладелецЭЦП")[0];
		ОтпечатокСертификата = ЭлектронныйДокументооборотСКонтролирующимиОрганами.ПолучитьЗначениеУзлаXML(Узел, "Отпечаток", , ОтпечаткиСертификатовАбонентовФСРАР, Истина);
		
		Если СокрЛП(Запись.СертификатРуководителя) <> ОтпечатокСертификата Тогда
			Запись.СертификатРуководителя = ОтпечатокСертификата;
		КонецЕсли;
		
		Если СокрЛП(Запись.СертификатГлавногоБухгалтера) <> ОтпечатокСертификата Тогда
			Запись.СертификатГлавногоБухгалтера = ОтпечатокСертификата;
		КонецЕсли;
		
		Если СокрЛП(Запись.СертификатДляШифрования) <> ОтпечатокСертификата Тогда
			Запись.СертификатДляШифрования = ОтпечатокСертификата;
		КонецЕсли;
		
		Узел = ПолучитьЭлементУзлаXML(ДокументDOM, "СерверPOP3", 0);
		
		Если СокрЛП(Запись.СерверPOP3) <> ЭлектронныйДокументооборотСКонтролирующимиОрганами.ПолучитьЗначениеУзлаXML(Узел,"Адрес") Тогда
			Запись.СерверPOP3 = ЭлектронныйДокументооборотСКонтролирующимиОрганами.ПолучитьЗначениеУзлаXML(Узел,"Адрес");
		КонецЕсли;
		
		ТекущийПортPOP3 = СокрЛП(Запись.ПортPOP3);
		
		Если ПустаяСтрока(ТекущийПортPOP3) ИЛИ ТекущийПортPOP3="0" Тогда
			
			Если ТекущийПортPOP3 <> ЭлектронныйДокументооборотСКонтролирующимиОрганами.ПолучитьЗначениеУзлаXML(Узел,"Порт",110) Тогда
				Запись.ПортPOP3 = ЭлектронныйДокументооборотСКонтролирующимиОрганами.ПолучитьЗначениеУзлаXML(Узел,"Порт",110);
			КонецЕсли;
			
		КонецЕсли;
		
		Узел = ПолучитьЭлементУзлаXML(ДокументDOM, "POP3", 0);
		
		Если СокрЛП(Запись.ИмяПользователяPOP3) <> ПолучитьАтрибутУзлаXML(Узел, "ИмяПользователя") Тогда
			Запись.ИмяПользователяPOP3 = ПолучитьАтрибутУзлаXML(Узел, "ИмяПользователя");
		КонецЕсли;
		
		ПарольPOP3 = СокрЛП(ПолучитьАтрибутУзлаXML(Узел, "Пароль"));
		Если Запись.ПарольPOP3 <> ПарольPOP3 Тогда
			Запись.ПарольPOP3 = ПарольPOP3;
		КонецЕсли;
		
		Узел = ПолучитьЭлементУзлаXML(ДокументDOM, "СерверSMTP", 0);
		
		Если СокрЛП(Запись.СерверSMTP) <> ЭлектронныйДокументооборотСКонтролирующимиОрганами.ПолучитьЗначениеУзлаXML(Узел,"Адрес") Тогда
			Запись.СерверSMTP = ЭлектронныйДокументооборотСКонтролирующимиОрганами.ПолучитьЗначениеУзлаXML(Узел,"Адрес");
		КонецЕсли;
		
		
		ТекущийПортSMTP = СокрЛП(Запись.ПортSMTP);
		
		Если ПустаяСтрока(ТекущийПортSMTP) ИЛИ ТекущийПортSMTP="0" Тогда
			
			Если ТекущийПортSMTP <> ЭлектронныйДокументооборотСКонтролирующимиОрганами.ПолучитьЗначениеУзлаXML(Узел,"Порт",25) Тогда
				Запись.ПортSMTP = ЭлектронныйДокументооборотСКонтролирующимиОрганами.ПолучитьЗначениеУзлаXML(Узел,"Порт",25);
			КонецЕсли;
			
		КонецЕсли;
		
		Значение = ЭлектронныйДокументооборотСКонтролирующимиОрганами.ПолучитьЗначениеУзлаXML(Узел, "ТребуетсяАвторизация", Ложь, , , Тип("Булево"));
		Если Запись.ТребуетсяSMTPАутентификация <> Значение Тогда
			Запись.ТребуетсяSMTPАутентификация = Значение;
		КонецЕсли;
		
		Если Запись.ТребуетсяSMTPАутентификация Тогда
			Узел = ПолучитьЭлементУзлаXML(ДокументDOM, "SMTP", 0);
			
			Если СокрЛП(Запись.ИмяПользователяSMTP) <> ПолучитьАтрибутУзлаXML(Узел, "ИмяПользователя") Тогда
				Запись.ИмяПользователяSMTP = ПолучитьАтрибутУзлаXML(Узел, "ИмяПользователя");
			КонецЕсли;
			
			Если СокрЛП(Запись.ПарольSMTP) <> ПолучитьАтрибутУзлаXML(Узел, "Пароль") Тогда
				Запись.ПарольSMTP = ПолучитьАтрибутУзлаXML(Узел, "Пароль");
			КонецЕсли;
			
		КонецЕсли;
		
		КодНалоговогоОрганаПолучателя = "";
		КодНалоговогоОргана = "";
		Если Организация <> Неопределено Тогда
			КодНалоговогоОрганаПолучателя = ?(Организация.КрупнейшийНалогоплательщик, Организация.КодНалоговогоОрганаПолучателя, "");
			КодНалоговогоОргана = РегламентированнаяОтчетностьВызовСервера.ПолучитьСведенияОбОрганизации(Организация, , "КодНО").КодНО;
		КонецЕсли;
		КодНалоговогоОрганаПолучателяНайденВОбновлении = Ложь;
		КодНалоговогоОрганаНайденВОбновлении = Ложь;
		ОсновнойКодНалоговогоОрганаВОбновлении = "";
		
		ФлагФНС  = Ложь;
		ФлагПФР  = Ложь;
		ФлагФСГС = Ложь;
		ОтпечатокФСС = "";
		
		Узлы = ДокументDOM.ПолучитьЭлементыПоИмени("Получатель");
		
		Для каждого Узел из Узлы Цикл
			Тип = ЭлектронныйДокументооборотСКонтролирующимиОрганами.ПолучитьЗначениеУзлаXML(Узел, "ТипПолучателя");
			Если Тип = XMLСтрока(Перечисления.ТипыКонтролирующихОрганов.ФНС) Тогда
				Код = ЭлектронныйДокументооборотСКонтролирующимиОрганами.ПолучитьЗначениеУзлаXML(Узел, "КодПолучателя");
				Если Не ФлагФНС Тогда
					ОсновнойКодНалоговогоОрганаВОбновлении = Код;
					
					ФлагФНС = Истина;
				КонецЕсли;
				Если НЕ КодНалоговогоОрганаПолучателяНайденВОбновлении И ЗначениеЗаполнено(КодНалоговогоОрганаПолучателя) И СокрЛП(Код) = СокрЛП(КодНалоговогоОрганаПолучателя) Тогда
					ОсновнойКодНалоговогоОрганаВОбновлении = Код;
					КодНалоговогоОрганаПолучателяНайденВОбновлении = Истина;
				ИначеЕсли НЕ КодНалоговогоОрганаПолучателяНайденВОбновлении И НЕ КодНалоговогоОрганаНайденВОбновлении И ЗначениеЗаполнено(КодНалоговогоОргана) И СокрЛП(Код) = СокрЛП(КодНалоговогоОргана) Тогда
					ОсновнойКодНалоговогоОрганаВОбновлении = Код;
					КодНалоговогоОрганаНайденВОбновлении = Истина;
				КонецЕсли;
			ИначеЕсли Тип = XMLСтрока(Перечисления.ТипыКонтролирующихОрганов.ПФР) Тогда
				ФлагПФР = Истина;
			ИначеЕсли Тип = XMLСтрока(Перечисления.ТипыКонтролирующихОрганов.ФСС) Тогда
				ОтпечатокФСС = ЭлектронныйДокументооборотСКонтролирующимиОрганами.ПолучитьЗначениеУзлаXML(Узел, "Отпечаток");
			ИначеЕсли Тип = XMLСтрока(Перечисления.ТипыКонтролирующихОрганов.ФСГС) Тогда
				ФлагФСГС = Истина;
			КонецЕсли;
		КонецЦикла;
		
		Если ЗначениеЗаполнено(ОсновнойКодНалоговогоОрганаВОбновлении) Тогда
			Значение = Справочники.НалоговыеОрганы.НайтиПоКоду(ОсновнойКодНалоговогоОрганаВОбновлении);
			
			Если Запись.НалоговыйОрган <> Значение Тогда
				Запись.НалоговыйОрган = Значение;
			КонецЕсли;
		КонецЕсли;
		
		ИдентификаторСистемыОтправителяПФР = ЭлектронныйДокументооборотСКонтролирующимиОрганами.ПолучитьЗначениеУзлаXML(ДокументDOM, "ИдентификаторСистемыОтправителяПФР");
		
		Если ФлагПФР И Не ПустаяСтрока(ИдентификаторСистемыОтправителяПФР) Тогда
			
			Если СокрЛП(Запись.ИдентификаторСистемыОтправителяПФР) <> ИдентификаторСистемыОтправителяПФР Тогда
				Запись.ИдентификаторСистемыОтправителяПФР = ИдентификаторСистемыОтправителяПФР;
			КонецЕсли;
			
		КонецЕсли;
		Если Запись.ПредназначенаДляДокументооборотаСФНС <> ФлагФНС Тогда
			Запись.ПредназначенаДляДокументооборотаСФНС = ФлагФНС;
		КонецЕсли;
		
		Если Запись.ПредназначенаДляДокументооборотаСПФР <> ФлагПФР Тогда
			Запись.ПредназначенаДляДокументооборотаСПФР = ФлагПФР;
		КонецЕсли;
		
		Если Запись.ПредназначенаДляДокументооборотаСФСГС <> ФлагФСГС Тогда
			Запись.ПредназначенаДляДокументооборотаСФСГС = ФлагФСГС;
		КонецЕсли;
		
		Значение = РегистрацияСоздатьСерверДокументооборота(ПолучитьЭлементУзлаXML(ДокументDOM, "СерверЭДО", 0));
		Если Запись.СерверДокументооборота <> Значение Тогда
			Запись.СерверДокументооборота = Значение;
		КонецЕсли;
		
		Если СтруктураДляИнформацииОСервереОнлайнПроверки <> Неопределено Тогда
			ПараметрыИнформацияОСервереОнлайнПроверки = РегистрацияПолучитьСерверОнлайнПроверки(ПолучитьЭлементУзлаXML(ДокументDOM, "СерверОнлайнПроверки", 0));
			Если ПараметрыИнформацияОСервереОнлайнПроверки <> Неопределено Тогда
				ЗаполнитьЗначенияСвойств(СтруктураДляИнформацииОСервереОнлайнПроверки, ПараметрыИнформацияОСервереОнлайнПроверки);
			КонецЕсли;
		КонецЕсли;
		
		//Выставляем только при первичном создании, если пользователь изменил значение то не меняем
		Если Запись.РежимАвтонастройки = Перечисления.РежимыАвтонастройкиУчетнойЗаписиНалогоплательщика.ПустаяСсылка() Тогда
			Запись.РежимАвтонастройки = Перечисления.РежимыАвтонастройкиУчетнойЗаписиНалогоплательщика.ВключенаВТихомРежиме;
		КонецЕсли;
		
		Если ЭтоНоваяУчетнаяЗапись Тогда
			ПоддержкаСервисаОнлайнПроверкиВУниверсальномФормате = ЭлектронныйДокументооборотСКонтролирующимиОрганамиВызовСервера.ПолучитьПараметрСпецоператора(Запись.СпецоператорСвязи, "ОнлайнПроверкаПризнак");
			Запись.ИспользоватьСервисОнлайнПроверкиОтчетов = (Запись.СпецоператорСвязи = Перечисления.СпецоператорыСвязи.Такском)
				ИЛИ (ПоддержкаСервисаОнлайнПроверкиВУниверсальномФормате <> Неопределено И Булево(ПоддержкаСервисаОнлайнПроверкиВУниверсальномФормате) = Истина);
		КонецЕсли;
		
		Если Запись.СтатусУчетнойЗаписи <> Перечисления.СтатусыУчетнойЗаписиДокументооборота.Активна Тогда
			Запись.СтатусУчетнойЗаписи = Перечисления.СтатусыУчетнойЗаписиДокументооборота.Активна;
		КонецЕсли;
		
		Узел = ПолучитьЭлементУзлаXML(ДокументDOM, "Оператор", 0);
		
		Если СокрЛП(Запись.ОператорРегНомерПФР) <>
			ЭлектронныйДокументооборотСКонтролирующимиОрганами.ПолучитьЗначениеУзлаXML(Узел, "РегНомерПФР") Тогда
			
			Запись.ОператорРегНомерПФР = ЭлектронныйДокументооборотСКонтролирующимиОрганами.ПолучитьЗначениеУзлаXML(
				Узел, "РегНомерПФР");
		КонецЕсли;
		
		Если СокрЛП(Запись.ОператорНаименованиеПолное) <>
			ЭлектронныйДокументооборотСКонтролирующимиОрганами.ПолучитьЗначениеУзлаXML(Узел, "ПолноеНаименование") Тогда
			
			Запись.ОператорНаименованиеПолное = ЭлектронныйДокументооборотСКонтролирующимиОрганами.ПолучитьЗначениеУзлаXML(
				Узел, "ПолноеНаименование");
		КонецЕсли;
		
		Если СокрЛП(Запись.ОператорНаименованиеКраткое) <>
			ЭлектронныйДокументооборотСКонтролирующимиОрганами.ПолучитьЗначениеУзлаXML(Узел, "КраткоеНаименование") Тогда
			
			Запись.ОператорНаименованиеКраткое = ЭлектронныйДокументооборотСКонтролирующимиОрганами.ПолучитьЗначениеУзлаXML(
				Узел, "КраткоеНаименование");
		КонецЕсли;
		
		Если СокрЛП(Запись.ОператорИНН) <>
			ЭлектронныйДокументооборотСКонтролирующимиОрганами.ПолучитьЗначениеУзлаXML(Узел, "ИНН") Тогда
			
			Запись.ОператорИНН = ЭлектронныйДокументооборотСКонтролирующимиОрганами.ПолучитьЗначениеУзлаXML(Узел, "ИНН");
		КонецЕсли;
		
		Если СокрЛП(Запись.ОператорКПП) <>
			ЭлектронныйДокументооборотСКонтролирующимиОрганами.ПолучитьЗначениеУзлаXML(Узел, "КПП") Тогда
			
			Запись.ОператорКПП = ЭлектронныйДокументооборотСКонтролирующимиОрганами.ПолучитьЗначениеУзлаXML(Узел, "КПП");
		КонецЕсли;
		
		Если НЕ СохранениеВИБОтключено Тогда
			Если Запись.Модифицированность() Тогда
				УстановитьПривилегированныйРежим(Истина);
				Запись.Записать();
				МенеджерЗаписи = РегистрыСведений.ПользователиУчетныхЗаписейДокументооборота.СоздатьМенеджерЗаписи();
				МенеджерЗаписи.УчетнаяЗапись = Запись.Ссылка;
				МенеджерЗаписи.Пользователь = Пользователи.ТекущийПользователь();
				МенеджерЗаписи.Записать();
				ДокументооборотСКОВызовСервера.УстановитьПараметрСеансаТекущиеУчетныеЗаписиНалогоплательщика();
				УстановитьПривилегированныйРежим(Ложь);
				НоваяУчетнаяЗапись = Запись.Ссылка;
				
				Если НЕ ТихийРежим Тогда
					Если ЭтоНоваяУчетнаяЗапись Тогда
							
						Если НЕ ВызовИзМастераПодключенияК1СОтчетности Тогда 
							Сообщение = СтроковыеФункцииКлиентСервер.ПодставитьПараметрыВСтроку(
								НСтр("ru = 'Создана новая учетная запись документооборота %1.
								|Ее можно посмотреть на вкладке ""Документооборот"" формы организации.'"),
								НоваяУчетнаяЗапись);
							ДлительнаяОтправкаКлиентСервер.ВывестиПредупреждение(Сообщение);
						КонецЕсли;
						
					Иначе
							
						Если НЕ ВызовИзМастераПодключенияК1СОтчетности Тогда 
							Сообщение = СтроковыеФункцииКлиентСервер.ПодставитьПараметрыВСтроку(НСтр("ru = 'Обновлена учетная запись документооборота %1.'"),НоваяУчетнаяЗапись);
							ДлительнаяОтправкаКлиентСервер.ВывестиПредупреждение(Сообщение);
						КонецЕсли;
						
					КонецЕсли;
				КонецЕсли;
			ИначеЕсли ВызовИзМастераАвтонастройки Тогда
					
				Если НЕ ВызовИзМастераПодключенияК1СОтчетности Тогда 
					Сообщение = СтроковыеФункцииКлиентСервер.ПодставитьПараметрыВСтроку(НСтр("ru = 'Найдена актуальная учетная запись документооборота %1.'"),НоваяУчетнаяЗапись);
					ДлительнаяОтправкаКлиентСервер.ВывестиПредупреждение(Сообщение);
				КонецЕсли;
				
			КонецЕсли;
		КонецЕсли;
		
		НоваяУчетнаяЗапись = Запись.Ссылка;
		
		Возврат Истина;
		
	Исключение
		Если Не ТихийРежим Тогда
			ЭлектронныйДокументооборотСКонтролирующимиОрганамиКлиентСервер.СообщитьИлиПередатьВМастерОшибку(
				СтроковыеФункцииКлиентСервер.ПодставитьПараметрыВСтроку(НСтр("ru = 'Ошибка разбора файла с информацией о настройках учетной записи: %1.'"),ОписаниеОшибки()),
				ВызовИзМастераПодключенияК1СОтчетности,
				ТекстОшибокДляМастераПодключенияК1СОтчетности, 
				не ВызовИзМастераПодключенияК1СОтчетности);
		КонецЕсли;
		Возврат Ложь;
	КонецПопытки;
	
КонецФункции

Функция ОбновитьПодписиСертификатовДокументооборота(ДокументDOM, ОператорСвязи)
	
	Результат = Ложь;
	
	УзелПодписи = ПолучитьЭлементУзлаXML(ДокументDOM, "Подписи", 0);
	Если УзелПодписи <> Неопределено Тогда
		УзлыПодпись = ДокументDOM.ПолучитьЭлементыПоИмени("Подпись");
		
		Для каждого УзелПодпись Из УзлыПодпись Цикл
			ПодписьСертификата64 = УзелПодпись.ТекстовоеСодержимое;
			
			АтрибутыУзлаПодпись = УзелПодпись.Атрибуты;
			АтрибутУзлаПодпись = АтрибутыУзлаПодпись.ПолучитьИменованныйЭлемент("Отпечаток");
			ОтпечатокСертификата = ?(АтрибутУзлаПодпись <> Неопределено, нрег(АтрибутУзлаПодпись.Значение), Неопределено);
			
			Если ЗначениеЗаполнено(ПодписьСертификата64) И ЗначениеЗаполнено(ОтпечатокСертификата) Тогда
				ДанныеПодписи = Base64Значение(ПодписьСертификата64);
				
				Попытка
					ПодписиСертификатовДокументооборота =
						РегистрыСведений.ПодписиСертификатовДокументооборота.СоздатьМенеджерЗаписи();
					ПодписиСертификатовДокументооборота.ОтпечатокСертификата = ОтпечатокСертификата;
					ПодписиСертификатовДокументооборота.ОператорСвязи = ОператорСвязи;
					ПодписиСертификатовДокументооборота.Прочитать();
					
					Если ПодписиСертификатовДокументооборота.Выбран() Тогда
						ДанныеПодписиВБазе = ПодписиСертификатовДокументооборота.Данные.Получить();
						Если Base64Строка(ДанныеПодписиВБазе) <> Base64Строка(ДанныеПодписи) Тогда
							ПодписиСертификатовДокументооборота.Данные = Новый ХранилищеЗначения(ДанныеПодписи);
							ПодписиСертификатовДокументооборота.ДатаПолучения = ТекущаяДатаСеанса();
							ПодписиСертификатовДокументооборота.Записать();
							Результат = Истина;
						КонецЕсли;
						
					Иначе
						ПодписиСертификатовДокументооборота.ОтпечатокСертификата = ОтпечатокСертификата;
						ПодписиСертификатовДокументооборота.ОператорСвязи = ОператорСвязи;
						ПодписиСертификатовДокументооборота.Данные = Новый ХранилищеЗначения(ДанныеПодписи);
						ПодписиСертификатовДокументооборота.ДатаПолучения = ТекущаяДатаСеанса();
						ПодписиСертификатовДокументооборота.Записать();
						Результат = Истина;
					КонецЕсли;
				Исключение
					ИнформацияОбОшибке = ИнформацияОбОшибке();
					ТекстСообщения = СтрШаблон(
						НСтр("ru = 'Не удалось обновить подписи сертификатов документооборота:
							 |%1'"),
						КраткоеПредставлениеОшибки(ИнформацияОбОшибке));
					ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
				КонецПопытки;
			КонецЕсли;
		КонецЦикла;
	КонецЕсли;
	
	Возврат Результат;
	
КонецФункции

Функция ПараметрыСохранитьДополнительныеРеквизитыУчетнойЗаписи() Экспорт
	
	ПараметрыФункции = Новый Структура;
	ПараметрыФункции.Вставить("УчетнаяЗаписьОрганизации");
	ПараметрыФункции.Вставить("ПараметрыСертификата", Неопределено);
	ПараметрыФункции.Вставить("ИдентификаторАбонента", "");
	ПараметрыФункции.Вставить("ВременныйФайлОбновленийАдрес", Неопределено);
	ПараметрыФункции.Вставить("ВызовИзМастераПодключенияК1СОтчетности", Ложь);
	ПараметрыФункции.Вставить("ТекстОшибокДляМастераПодключенияК1СОтчетности", "");
	
	Возврат ПараметрыФункции;
	
КонецФункции

Процедура СохранитьДополнительныеРеквизитыУчетнойЗаписи(ПараметрыФункции) Экспорт
	
	// ПараметрыФункции
	УчетнаяЗаписьОрганизации = ПараметрыФункции.УчетнаяЗаписьОрганизации;
	ИдентификаторАбонента = ПараметрыФункции.ИдентификаторАбонента;
	ВременныйФайлОбновленийАдрес = ПараметрыФункции.ВременныйФайлОбновленийАдрес;
	ВызовИзМастераПодключенияК1СОтчетности = ПараметрыФункции.ВызовИзМастераПодключенияК1СОтчетности;
		
	Если НЕ ЗначениеЗаполнено(УчетнаяЗаписьОрганизации) Тогда
		Возврат;
	КонецЕсли;
		
	ДатаОбновления = ТекущаяДатаСеанса();
		
	ВременныйКаталогНаСервере = ОперацииСФайламиЭДКО.СоздатьВременныйКаталог();
	ИмяВременногоФайлаОбновлений = ВременныйКаталогНаСервере + ИдентификаторАбонента + ".xml";
	ДвФайл = ПолучитьИзВременногоХранилища(ВременныйФайлОбновленийАдрес);
	ДвФайл.Записать(ИмяВременногоФайлаОбновлений);
	ФайлАвтонастрокиВФорматеDOM = ЭлектронныйДокументооборотСКонтролирующимиОрганами.ЗагрузитьФайлXML(ИмяВременногоФайлаОбновлений);
		
	Если ФайлАвтонастрокиВФорматеDOM = Неопределено Тогда
		Возврат;
	КонецЕсли;
	
	ОперацииСФайламиЭДКО.УдалитьВременныйФайл(ВременныйКаталогНаСервере);
	
	// Если файл автонастройки в формате меньше, чем 1.3, то запись в регистр не выполняем
	РегистрационныйФайл = ФайлАвтонастрокиВФорматеDOM.ПолучитьЭлементыПоИмени("РегистрационныйФайл");
	Если ЗначениеЗаполнено(РегистрационныйФайл) Тогда
		АтрибутыУзлаРегистрационныйФайл = РегистрационныйФайл[0].Атрибуты;
		
		АтрибутВерсФорм = АтрибутыУзлаРегистрационныйФайл.ПолучитьИменованныйЭлемент("ВерсФорм");
		Если АтрибутВерсФорм <> Неопределено Тогда
			ВерсФорм = XMLЗначение(Тип("Число"), АтрибутВерсФорм.Значение);
			Если ВерсФорм < 1.3 Тогда
				ЭлектронныйДокументооборотСКонтролирующимиОрганамиКлиентСервер.СообщитьИлиПередатьВМастерОшибку(НСтр("ru = 'Файл автонастройки учетной записи имеет формат меньше 1.3'"),
									ВызовИзМастераПодключенияК1СОтчетности,
									ПараметрыФункции.ТекстОшибокДляМастераПодключенияК1СОтчетности,
									Ложь);
				Возврат;
			КонецЕсли;
		КонецЕсли;
	КонецЕсли;
	
	// Прочитываем данные из регистра по учетной записи
	СсылкаНаУчетнуюЗапись =  УчетнаяЗаписьОрганизации.Ссылка;
	НаборЗаписейРегистра = РегистрыСведений.ДополнительныеРеквизитыУчетнойЗаписи.СоздатьНаборЗаписей();
	НаборЗаписейРегистра.Отбор.УчетнаяЗапись.Установить(СсылкаНаУчетнуюЗапись);
	НаборЗаписейРегистра.Прочитать();
	
	ОтпечатокСертификата = УчетнаяЗаписьОрганизации.СертификатРуководителя;
	ЗначенияСертификатаВзятыИзПрерыдущегоСостоянияРегистра = Ложь;
	
	НаборЗаписейРегистра.Очистить();
	
	НоваяЗаписьРегистра = НаборЗаписейРегистра.Добавить();
	НоваяЗаписьРегистра.УчетнаяЗапись = СсылкаНаУчетнуюЗапись;
	НоваяЗаписьРегистра.ОтпечатокСертификата = ОтпечатокСертификата;
	НоваяЗаписьРегистра.ДатаОбновления = ДатаОбновления;
	
	// ТипКриптопровайдера
	ИмяКриптопровайдера = ЭлектронныйДокументооборотСКонтролирующимиОрганами.ПолучитьЗначениеУзлаXML(ФайлАвтонастрокиВФорматеDOM, "ИмяКриптопровайдера");
	ТипКриптопровайдера = ЭлектронныйДокументооборотСКонтролирующимиОрганами.ПолучитьЗначениеУзлаXML(ФайлАвтонастрокиВФорматеDOM, "ТипКриптопровайдера");
	СвойстваКриптопровайдера = КриптографияЭДКОКлиентСервер.СвойстваКриптопровайдера(ИмяКриптопровайдера, ТипКриптопровайдера);
	Если СвойстваКриптопровайдера <> Неопределено Тогда
		НоваяЗаписьРегистра.ТипКриптопровайдера = СвойстваКриптопровайдера.ТипКриптопровайдера;
	КонецЕсли;
	
	// НомерОсновнойПоставки1с
	НоваяЗаписьРегистра.НомерОсновнойПоставки1с = ЭлектронныйДокументооборотСКонтролирующимиОрганами.ПолучитьЗначениеУзлаXML(ФайлАвтонастрокиВФорматеDOM,"НомерОсновнойПоставки1с");
	
	// КраткоеНаименование
	НоваяЗаписьРегистра.КраткоеНаименование = ЭлектронныйДокументооборотСКонтролирующимиОрганами.ПолучитьЗначениеУзлаXML(ФайлАвтонастрокиВФорматеDOM,"КраткоеНаименование");
	
	// ПолноеНаименование
	НоваяЗаписьРегистра.ПолноеНаименование = ЭлектронныйДокументооборотСКонтролирующимиОрганами.ПолучитьЗначениеУзлаXML(ФайлАвтонастрокиВФорматеDOM,"ПолноеНаименование");
	
	// ИНН
	НоваяЗаписьРегистра.ИНН = ЭлектронныйДокументооборотСКонтролирующимиОрганами.ПолучитьЗначениеУзлаXML(ФайлАвтонастрокиВФорматеDOM,"ИНН");
	
	// КПП
	МассивИсключенийИменРодительскихУзлов = ОбщегоНазначенияКлиентСервер.ЗначениеВМассиве("Оператор");
	НоваяЗаписьРегистра.КПП = ЭлектронныйДокументооборотСКонтролирующимиОрганами.ПолучитьЗначениеУзлаXML(
		ФайлАвтонастрокиВФорматеDOM,
		"КПП",,
		МассивИсключенийИменРодительскихУзлов,
		Истина,,
		Истина);
	
	// ОГРН
	НоваяЗаписьРегистра.ОГРН = ЭлектронныйДокументооборотСКонтролирующимиОрганами.ПолучитьЗначениеУзлаXML(ФайлАвтонастрокиВФорматеDOM,"ОГРН");
	
	// РегНомерПФР
	НоваяЗаписьРегистра.РегНомерПФР = ЭлектронныйДокументооборотСКонтролирующимиОрганами.ПолучитьЗначениеУзлаXML(ФайлАвтонастрокиВФорматеDOM,"РегНомерПФР");
	
	// Лицензия КриптоПро
	НоваяЗаписьРегистра.ЛицензияКриптоПроВключенаВСертификат = ЭлектронныйДокументооборотСКонтролирующимиОрганами.ПолучитьЗначениеУзлаXML(ФайлАвтонастрокиВФорматеDOM,"ЛицензияКриптоПроВключенаВСертификат");
	
	// ЭтоНотариусАдвокатИлиГКФХ
	НоваяЗаписьРегистра.ЭтоНотариусАдвокатИлиГКФХ = ЭлектронныйДокументооборотСКонтролирующимиОрганами.ПолучитьЗначениеУзлаXML(ФайлАвтонастрокиВФорматеDOM,"ПризнакФизическогоЛица");
	
	// ФИО владельца ЭЦП
	УзелФИО = ФайлАвтонастрокиВФорматеDOM.ПолучитьЭлементыПоИмени("ФИО");
	Если ЗначениеЗаполнено(УзелФИО) Тогда
		АтрибутыУзлаФИО = УзелФИО[0].Атрибуты;
		
		// ВладелецЭЦПФамилия
		ВладелецЭЦПФамилия = АтрибутыУзлаФИО.ПолучитьИменованныйЭлемент("Фамилия");
		Если ВладелецЭЦПФамилия <> Неопределено Тогда
			НоваяЗаписьРегистра.ВладелецЭЦПФамилия = ВладелецЭЦПФамилия.Значение;
		КонецЕсли;
		
		// ВладелецЭЦПИмя
		ВладелецЭЦПИмя = АтрибутыУзлаФИО.ПолучитьИменованныйЭлемент("Имя");
		Если ВладелецЭЦПИмя <> Неопределено Тогда
			НоваяЗаписьРегистра.ВладелецЭЦПИмя = ВладелецЭЦПИмя.Значение;
		КонецЕсли;
		
		// ВладелецЭЦПОтчество
		ВладелецЭЦПОтчество = АтрибутыУзлаФИО.ПолучитьИменованныйЭлемент("Отчество");
		Если ВладелецЭЦПОтчество <> Неопределено Тогда
			НоваяЗаписьРегистра.ВладелецЭЦПОтчество = ВладелецЭЦПОтчество.Значение;
		КонецЕсли;
			
	КонецЕсли;
	
	// ТелефонМобильный
	НоваяЗаписьРегистра.ТелефонМобильный = ЭлектронныйДокументооборотСКонтролирующимиОрганами.ПолучитьЗначениеУзлаXML(ФайлАвтонастрокиВФорматеDOM,"ТелефонМобильный");
	
	// ТелефонОсновной
	НоваяЗаписьРегистра.ТелефонОсновной = ЭлектронныйДокументооборотСКонтролирующимиОрганами.ПолучитьЗначениеУзлаXML(ФайлАвтонастрокиВФорматеDOM,"ТелефонОсновной");
	
	// ТелефонДополнительный
	НоваяЗаписьРегистра.ТелефонДополнительный = ЭлектронныйДокументооборотСКонтролирующимиОрганами.ПолучитьЗначениеУзлаXML(ФайлАвтонастрокиВФорматеDOM,"ТелефонДополнительный");
	
	// ЭлектроннаяПочта
	НоваяЗаписьРегистра.ЭлектроннаяПочта = ЭлектронныйДокументооборотСКонтролирующимиОрганами.ПолучитьЗначениеУзлаXML(ФайлАвтонастрокиВФорматеDOM, "ЭлектроннаяПочта");
	
	// ВладелецЭЦППодразделение
	НоваяЗаписьРегистра.ВладелецЭЦППодразделение = ЭлектронныйДокументооборотСКонтролирующимиОрганами.ПолучитьЗначениеУзлаXML(ФайлАвтонастрокиВФорматеDOM, "Подразделение");
		
	// ВладелецЭЦПСНИЛС
	НоваяЗаписьРегистра.ВладелецЭЦПСНИЛС = ЭлектронныйДокументооборотСКонтролирующимиОрганами.ПолучитьЗначениеУзлаXML(ФайлАвтонастрокиВФорматеDOM, "СНИЛС");
	
	// ВладелецЭЦПДолжность
	НоваяЗаписьРегистра.ВладелецЭЦПДолжность = ЭлектронныйДокументооборотСКонтролирующимиОрганами.ПолучитьЗначениеУзлаXML(ФайлАвтонастрокиВФорматеDOM, "Должность");
	
	// обновляем информацию в регистре ДополнительныеРеквизитыУчетнойЗаписиПолучатели
	//
	НаборЗаписейНаправленийСдачи = РегистрыСведений.ДополнительныеРеквизитыУчетнойЗаписиПолучатели.СоздатьНаборЗаписей();
	НаборЗаписейНаправленийСдачи.Отбор.УчетнаяЗапись.Установить(СсылкаНаУчетнуюЗапись);
	НаборЗаписейНаправленийСдачи.Прочитать();
	НаборЗаписейНаправленийСдачи.Очистить();
	
	// Получатели - файл автонастроки
	Получатели = ФайлАвтонастрокиВФорматеDOM.ПолучитьЭлементыПоИмени("Получатель");
	
	ХарактеристикиНаправленийСдачи = Новый ТаблицаЗначений;
	
	ХарактеристикиНаправленийСдачи.Колонки.Добавить("УчетнаяЗапись");
	ХарактеристикиНаправленийСдачи.Колонки.Добавить("ТипПолучателя");
	ХарактеристикиНаправленийСдачи.Колонки.Добавить("КодПолучателя");
	ХарактеристикиНаправленийСдачи.Колонки.Добавить("КПП");
	ХарактеристикиНаправленийСдачи.Колонки.Добавить("ДатаОбновления");
	
	Для Каждого Получатель из Получатели Цикл
		
		Тип = ЭлектронныйДокументооборотСКонтролирующимиОрганами.ПолучитьЗначениеУзлаXML(Получатель,"ТипПолучателя");
		Код = ЭлектронныйДокументооборотСКонтролирующимиОрганами.ПолучитьЗначениеУзлаXML(Получатель,"КодПолучателя");
		
		Если Тип = "ОИВ" Тогда
			НоваяЗаписьРегистра.КодРегионаФСРАР = Код;
		ИначеЕсли Тип = "ФНС" Тогда
			ПереченьКПП = Получатель.ПолучитьЭлементыПоИмени("КПП");
			
			Если ПереченьКПП.Количество() = 0 Тогда
				
				ТипПолучателя 	= Перечисления.ТипыКонтролирующихОрганов[Тип];
				КПППолучателя	= Неопределено;
				КодПолучателя 	= Код;
				
				СтруктураПоиска = Новый Структура;
				СтруктураПоиска.Вставить("ТипПолучателя", 	ТипПолучателя);
				СтруктураПоиска.Вставить("КПП", 			КПППолучателя);
				СтруктураПоиска.Вставить("КодПолучателя", 	КодПолучателя);
				
				Если ХарактеристикиНаправленийСдачи.НайтиСтроки(СтруктураПоиска).Количество() = 0 Тогда 
				
					НоваяСтрока = ХарактеристикиНаправленийСдачи.Добавить();
					
					НоваяСтрока.УчетнаяЗапись 	= СсылкаНаУчетнуюЗапись; 
					НоваяСтрока.ТипПолучателя 	= ТипПолучателя;
					НоваяСтрока.КПП 			= КПППолучателя;
					НоваяСтрока.КодПолучателя 	= Код;
					НоваяСтрока.ДатаОбновления 	= ДатаОбновления;
					
				КонецЕсли;
				
			Иначе
				
				Для каждого КПП Из ПереченьКПП Цикл
					
					ТипПолучателя 	= Перечисления.ТипыКонтролирующихОрганов[Тип];
					КПППолучателя	= XMLЗначение(Тип("Строка"), КПП.ТекстовоеСодержимое);
					КодПолучателя 	= Код;
					
					СтруктураПоиска = Новый Структура;
					СтруктураПоиска.Вставить("ТипПолучателя", 	ТипПолучателя);
					СтруктураПоиска.Вставить("КПП", 			КПППолучателя);
					СтруктураПоиска.Вставить("КодПолучателя", 	КодПолучателя);
					
					Если ХарактеристикиНаправленийСдачи.НайтиСтроки(СтруктураПоиска).Количество() = 0 Тогда 
					
						НоваяСтрока = ХарактеристикиНаправленийСдачи.Добавить();
						
						НоваяСтрока.УчетнаяЗапись 	= СсылкаНаУчетнуюЗапись; 
						НоваяСтрока.ТипПолучателя 	= ТипПолучателя;
						НоваяСтрока.КПП 			= КПППолучателя;
						НоваяСтрока.КодПолучателя 	= Код;
						НоваяСтрока.ДатаОбновления 	= ДатаОбновления;
						
					КонецЕсли;
					
				КонецЦикла;
				
			КонецЕсли;
			
		ИначеЕсли Тип <> "ЭЛН" Тогда
			
			Если Тип = "ПФР" И Получатель.ПолучитьЭлементыПоИмени("СхемаОтправкиМПД").Количество() > 0 Тогда
				Продолжить;
			КонецЕсли;
			
			Если Метаданные.Перечисления.ТипыКонтролирующихОрганов.ЗначенияПеречисления.Найти(Тип) = Неопределено Тогда
				Продолжить;
			КонецЕсли;
			
			ТипПолучателя 	= Перечисления.ТипыКонтролирующихОрганов[Тип];
			КПППолучателя	= Неопределено;
			КодПолучателя 	= Код;
			
			СтруктураПоиска = Новый Структура;
			СтруктураПоиска.Вставить("ТипПолучателя", 	ТипПолучателя);
			СтруктураПоиска.Вставить("КПП", 			КПППолучателя);
			СтруктураПоиска.Вставить("КодПолучателя", 	КодПолучателя);
			
			Если ХарактеристикиНаправленийСдачи.НайтиСтроки(СтруктураПоиска).Количество() = 0 Тогда 
			
				НоваяСтрока = ХарактеристикиНаправленийСдачи.Добавить();
				
				НоваяСтрока.УчетнаяЗапись 	= СсылкаНаУчетнуюЗапись; 
				НоваяСтрока.ТипПолучателя 	= ТипПолучателя;
				НоваяСтрока.КПП 			= КПППолучателя;
				НоваяСтрока.КодПолучателя 	= Код;
				НоваяСтрока.ДатаОбновления 	= ДатаОбновления;
				
			КонецЕсли;

		КонецЕсли;

	КонецЦикла;
	
	НаборЗаписейНаправленийСдачи.Загрузить(ХарактеристикиНаправленийСдачи);
	
	УчетнаяЗаписьОрганизацииОбъект = УчетнаяЗаписьОрганизации.ПолучитьОбъект();
	
	// Лицензия
	УзелЛицензия = ФайлАвтонастрокиВФорматеDOM.ПолучитьЭлементыПоИмени("Лицензия");
	Если ЗначениеЗаполнено(УзелЛицензия) Тогда
		АтрибутыУзлаЛицензия = УзелЛицензия[0].Атрибуты;
		
		// ЛицензияНаименование
		УчетнаяЗаписьОрганизацииОбъект.ЛицензияНаименование = АтрибутыУзлаЛицензия.ПолучитьИменованныйЭлемент("Наименование").Значение;
		
		// ЛицензияДатаНачала
		УчетнаяЗаписьОрганизацииОбъект.ЛицензияДатаНачала = XMLЗначение(Тип("Дата"), АтрибутыУзлаЛицензия.ПолучитьИменованныйЭлемент("ДатаНачала").Значение);
		
		// ЛицензияДатаОкончания
		УчетнаяЗаписьОрганизацииОбъект.ЛицензияДатаОкончания = XMLЗначение(Тип("Дата"), АтрибутыУзлаЛицензия.ПолучитьИменованныйЭлемент("ДатаОкончания").Значение);
		
		// ЛицензияДатаБлокировки
		ДатаБлокировки = АтрибутыУзлаЛицензия.ПолучитьИменованныйЭлемент("ДатаБлокировки");
		
		Если ДатаБлокировки = Неопределено Тогда
			УчетнаяЗаписьОрганизацииОбъект.ЛицензияДатаБлокировки = УчетнаяЗаписьОрганизацииОбъект.ЛицензияДатаОкончания;
		Иначе
			УчетнаяЗаписьОрганизацииОбъект.ЛицензияДатаБлокировки = XMLЗначение(Тип("Дата"), ДатаБлокировки.Значение);
		КонецЕсли;
		
		// ЛицензияТребуетсяНапоминаниеОбОкончанииСрокаДействия
		ИТС = АтрибутыУзлаЛицензия.ПолучитьИменованныйЭлемент("ИТС");
		Если ИТС <> Неопределено Тогда
			УчетнаяЗаписьОрганизацииОбъект.ЛицензияТребуетсяНапоминаниеОбОкончанииСрокаДействия = НЕ XMLЗначение(Тип("Булево"), ИТС.Значение);
		КонецЕсли;
	КонецЕсли;
	
	// Запись регистров и учетной записи
	НаборЗаписейРегистра.Записать();
	НаборЗаписейНаправленийСдачи.Записать();
	УчетнаяЗаписьОрганизацииОбъект.Записать();
			
КонецПроцедуры

//Методы веб сервиса для подачи заявления

Функция НачатьПроверкуТелефонаСМобильного(Ссылка, СообщениеОбОшибке) Экспорт
	
	ТелефонДляПаролей = ОбщегоНазначения.ЗначениеРеквизитаОбъекта(Ссылка, "ТелефонМобильныйДляАвторизации");
	Если НЕ ЗначениеЗаполнено(ТелефонДляПаролей) Тогда
		СообщениеОбОшибке = НСтр("ru = 'Не указан номер телефона для авторизации.'");
		Возврат "";
	КонецЕсли;
	
	//Подверждение номера телефона
	Результат = МенеджерСервисаКриптографии.ПолучитьКодПроверкиТелефона(ТелефонДляПаролей);
	Если Результат.Выполнено Тогда
		Возврат Результат.Идентификатор;
	Иначе
		СообщениеОбОшибке = Результат.ОписаниеОшибки;
		Возврат "";
	КонецЕсли;

КонецФункции

Функция ОтправитьЗаявлениеАбонентаСМобильного(Код, ИдентификаторПроверкиТелефона, Ссылка, СообщениеОбОшибке) Экспорт
	
	//Подверждение номера телефона
	Результат = МенеджерСервисаКриптографии.ПроверитьТелефонПоКоду(ИдентификаторПроверкиТелефона, Код);
	Если Результат.Выполнено Тогда
		
		//Получим регистрационный номер
		НомерОсновнойПоставки1с = "";
		АдресХранилища = ПоместитьВоВременноеХранилище(Неопределено, Новый УникальныйИдентификатор());
		ЭлектронныйДокументооборотСКонтролирующимиОрганами.КодРегистрацииПродуктаВМоделиСервиса(АдресХранилища);
		КодРегистрацииПродуктаСтруктура = ПолучитьИзВременногоХранилища(АдресХранилища);
		Если КодРегистрацииПродуктаСтруктура <> Неопределено И КодРегистрацииПродуктаСтруктура.Выполнено Тогда
			НомерОсновнойПоставки1с = КодРегистрацииПродуктаСтруктура.Результат;
		Иначе
			СообщениеОбОшибке = НСтр("ru = 'Не удалось получить регистрационный номер программы.'");
			Возврат Ложь;
		КонецЕсли;
		
		//Запрос на подтверждение номера телефона
		ЗаявлениеОбъект = Ссылка.ПолучитьОбъект();
		ЗаявлениеОбъект.ИдентификаторПроверкиТелефонаДляПаролей = ИдентификаторПроверкиТелефона;
		ЗаявлениеОбъект.НомерОсновнойПоставки1с = НомерОсновнойПоставки1с;
		ЗаявлениеОбъект.Записать();
		
		//Отправка заявления спецоператору
		АлгоритмКонтейнераКлючей = ЗаявлениеОпределитьАлгоритмДляСозданияКонтейнераКлючей(Ссылка);
		Если АлгоритмКонтейнераКлючей = Неопределено Тогда
			СообщениеОбОшибке = ?(ЗначениеЗаполнено(Результат.ОписаниеОшибки), 
				Результат.ОписаниеОшибки, 
				НСтр("ru = 'Не удалось получить алгоритм генерации ключа, попробуйте позже.'"));
			ЗаявлениеОбъект = Ссылка.ПолучитьОбъект();
			ЗаявлениеОбъект.ИдентификаторДокументооборота = СтрЗаменить(Новый УникальныйИдентификатор(), "-", "");
			ЗаявлениеОбъект.Записать();
			Возврат Ложь;
		КонецЕсли;
		Результат = ОбработкаЗаявленийАбонента.ВыгрузитьЗаявлениеАбонентаВМоделиСервиса(Ссылка, АлгоритмКонтейнераКлючей);
		Если Не Результат.Выполнено Тогда
			СообщениеОбОшибке = ?(ЗначениеЗаполнено(Результат.ОписаниеОшибки), 
				Результат.ОписаниеОшибки, 
				НСтр("ru = 'Не удалось передать заявление спецоператору, попробуйте позже.'"));
			ЗаявлениеОбъект = Ссылка.ПолучитьОбъект();
			ЗаявлениеОбъект.ИдентификаторДокументооборота = СтрЗаменить(Новый УникальныйИдентификатор(), "-", "");
			ЗаявлениеОбъект.Записать();
		Иначе
			ЗаявлениеОбъект = Ссылка.ПолучитьОбъект();
			ЗаявлениеОбъект.Статус = Перечисления.СтатусыЗаявленияАбонентаСпецоператораСвязи.Отправлено;
			ЗаявлениеОбъект.Записать();
		КонецЕсли;
		Если Результат.Выполнено Тогда
			ОбработкаЗаявленийАбонентаВызовСервера.ВключитьОтслеживаниеИзмененияСтатусаЗаявления(Ссылка);
		КонецЕсли;
		Возврат Результат.Выполнено;
	Иначе
		СообщениеОбОшибке = Результат.ОписаниеОшибки;
		Возврат Ложь;
	КонецЕсли;

КонецФункции

Функция ЗаявлениеОпределитьАлгоритмДляСозданияКонтейнераКлючей(ДанныеОрганизации) Экспорт
	
	// Внимание!
	// ---------
	// В Контейнере есть одноименная процедура, которая используется при отправке отчетов
	// в интерактивном режиме пользователем.
	// При изменениях необходимо синхронно менять обе процедуры.
	
	Возврат КриптографияЭДКОКлиентСервер.АлгоритмПоУмолчанию();
	
КонецФункции

Функция ПроверитьСтатусЗаявленияСМобильного(Статус, СтатусКомментарий) Экспорт
	
	ЕстьОтправленныеЗаявления = ОбработкаЗаявленийАбонентаВызовСервера.ОтправленныеЗаявленияАбонентов().Количество() > 0;
	
	Если ЕстьОтправленныеЗаявления Тогда
		
		Заявление = ОбработкаЗаявленийАбонентаВызовСервера.СледующееЗаявлениеТребующееРеакцииПользователя();
		
		Если ЗначениеЗаполнено(Заявление) И ЗаявлениеАбонентаВМобильном(Заявление) Тогда
			
			РеквизитыДокумента = ОбработкаЗаявленийАбонентаВызовСервера.ПолучитьСтруктуруРеквизитовЗаявления(Заявление);
			Статус             = ОбщегоНазначения.ИмяЗначенияПеречисления(РеквизитыДокумента.Статус);
			СтатусКомментарий  = РеквизитыДокумента.СтатусКомментарий;
			
			Возврат Истина;
			
		КонецЕсли
		
	КонецЕсли;
	
	Возврат Ложь;

КонецФункции

Функция НачатьНастройкуПодключенияСМобильного(ИдентификаторАбонента, ИдентификаторДокументооборота, ИдентификаторСертификата, СообщениеОбОшибке) Экспорт
	
	СообщениеОбОшибке = "";
	Заявление = ОбработкаЗаявленийАбонентаВызовСервера.СледующееЗаявлениеТребующееРеакцииПользователя();
	
	Если ЗначениеЗаполнено(Заявление) И ЗаявлениеАбонентаВМобильном(Заявление) Тогда
		
		РеквизитыДокумента     = ОбработкаЗаявленийАбонентаВызовСервера.ПолучитьСтруктуруРеквизитовЗаявления(Заявление);
		РезультатОтветаСервера = ОбработкаЗаявленийАбонентаВызовСервера.ПолучитьИРазобратьОтветНаЗаявление(Заявление, Истина);
		
		Если РеквизитыДокумента.Статус = Перечисления.СтатусыЗаявленияАбонентаСпецоператораСвязи.Одобрено
			И РезультатОтветаСервера.Выполнено И ЗначениеЗаполнено(РезультатОтветаСервера.ИдентификаторАбонента) Тогда
			
			ИдентификаторАбонента         = ВРег(РезультатОтветаСервера.ИдентификаторАбонента);
			ИдентификаторДокументооборота = РеквизитыДокумента.ИдентификаторДокументооборота;
			
			ПараметрыРегистрацияПолучитьДанныеССервера                                        = ПараметрыРегистрацияПолучитьДанныеССервера();
			ПараметрыРегистрацияПолучитьДанныеССервера.ИдентификаторАбонента                  = ИдентификаторАбонента;
			ПараметрыРегистрацияПолучитьДанныеССервера.СпецОператорСвязи                      = РеквизитыДокумента.СпецоператорСвязи;
			ПараметрыРегистрацияПолучитьДанныеССервера.ВызовИзМастераПодключенияК1СОтчетности = Истина;
			
			Сертификаты = Неопределено; // Будет инициализирована после загрузки сертификатов.			
			Результат = РегистрацияПолучитьДанныеССервера(ПараметрыРегистрацияПолучитьДанныеССервера, Ложь, Сертификаты);
			Если НЕ Результат Тогда
				СообщениеОбОшибке = НСтр("ru = 'Не удалось получить настройки с сервиса спецоператора.'");
				Возврат Ложь;
			КонецЕсли;
			
			// Установка сертификатов
			ОтпечатокСертификата = Неопределено; // Будет загружен в процессе установки сертификатов.
			РезультатУстановкиСертификатов = РегистрацияУстановитьСертификаты(ОтпечатокСертификата, Сертификаты);
			Если НЕ РезультатУстановкиСертификатов Тогда
				СообщениеОбОшибке = НСтр("ru = 'Не удалось установить сертификат.'");
				Возврат Ложь;
			КонецЕсли;
			
			Сертификат = Новый Структура("Отпечаток", ОтпечатокСертификата);
			СвойстваСертификата = КриптографияЭДКО.НайтиСертификат(Сертификат);
			ИдентификаторСертификата = СвойстваСертификата.Идентификатор;
			
			//Запросить пароль
			
			// Чтобы в конфигурациях со старыми версиями БТС не возникали ошибки компиляции,
			// обращаемся к общему модулю через функцию БСП.
			// Для возможности отправки отчетов на стороне сервера из таких конфигураций потребуется обновить БТС до 1.0.17.
			МодульСервисКриптографии = ОбщегоНазначения.ОбщийМодуль("СервисКриптографии");
			МодульСервисКриптографии.ПолучитьВременныйПароль(ИдентификаторСертификата, Ложь, "phone");
			
			Возврат Истина;
			
		КонецЕсли;
	Иначе
		СообщениеОбОшибке = НСтр("ru = 'Заявление на подключение отклонено!'");
	КонецЕсли;
	
	Возврат Ложь;
	
КонецФункции

Функция ЗаявлениеДляЗавершенияНастройкиСМобильного(ИдентификаторДокументооборота)
	
	Если НЕ ПравоДоступа("Чтение", Метаданные.Документы.ЗаявлениеАбонентаСпецоператораСвязи) Тогда
		Возврат Неопределено;
	КонецЕсли;
	
	Запрос = Новый Запрос;
	Запрос.Текст =
	"ВЫБРАТЬ РАЗРЕШЕННЫЕ ПЕРВЫЕ 1
	|	ЗаявлениеАбонентаСпецоператораСвязи.Ссылка КАК Ссылка
	|ИЗ
	|	Документ.ЗаявлениеАбонентаСпецоператораСвязи КАК ЗаявлениеАбонентаСпецоператораСвязи
	|ГДЕ
	|	ЗаявлениеАбонентаСпецоператораСвязи.Статус В (&Одобрено, &Отклонено)
	|	И ЗаявлениеАбонентаСпецоператораСвязи.ИдентификаторДокументооборота = &ИдентификаторДокументооборота
	|	И НЕ ЗаявлениеАбонентаСпецоператораСвязи.НастройкаЗавершена
	|
	|УПОРЯДОЧИТЬ ПО
	|	ЗаявлениеАбонентаСпецоператораСвязи.Дата УБЫВ";
	
	Запрос.УстановитьПараметр("Одобрено", 		Перечисления.СтатусыЗаявленияАбонентаСпецоператораСвязи.Одобрено);
	Запрос.УстановитьПараметр("Отклонено", 		Перечисления.СтатусыЗаявленияАбонентаСпецоператораСвязи.Отклонено);
	Запрос.УстановитьПараметр("ИдентификаторДокументооборота", ИдентификаторДокументооборота);
	
	РезультатЗапроса 	= Запрос.Выполнить();
	Выборка 			= РезультатЗапроса.Выбрать();
	
	Пока Выборка.Следующий() Цикл
		Возврат Выборка.Ссылка;
	КонецЦикла;

	Возврат Неопределено;
	
КонецФункции

Функция ЗавершитьНастройкуПодключенияСМобильного(ИдентификаторАбонента, ИдентификаторДокументооборота, ИдентификаторСертификата, ВременныйПароль, СообщениеОбОшибке) Экспорт
	
	СообщениеОбОшибке = "";

	// Чтобы в конфигурациях со старыми версиями БТС не возникали ошибки компиляции,
	// обращаемся к общему модулю через функцию БСП.
	// Для возможности отправки отчетов на стороне сервера из таких конфигураций потребуется обновить БТС до 1.0.17.
	МодульСервисКриптографии = ОбщегоНазначения.ОбщийМодуль("СервисКриптографии");
	МодульСервисКриптографии.ПодтвердитьВременныйПароль(ИдентификаторСертификата, ВременныйПароль);
	
	Заявление = ЗаявлениеДляЗавершенияНастройкиСМобильного(ИдентификаторДокументооборота);
	Если ЗначениеЗаполнено(Заявление) И ЗаявлениеАбонентаВМобильном(Заявление) Тогда
		РеквизитыЗаявления = ОбщегоНазначения.ЗначенияРеквизитовОбъекта(Заявление, "Организация, ТелефонМобильныйДляАвторизации");
	Иначе
		РеквизитыЗаявления = Новый Структура("Организация, ТелефонМобильныйДляАвторизации", Неопределено, "");
	КонецЕсли;
	
	ПараметрыРегистрацияПолучитьДанныеССервера                                           = ПараметрыРегистрацияПолучитьДанныеССервера();
	ПараметрыРегистрацияПолучитьДанныеССервера.ИдентификаторАбонента                     = ИдентификаторАбонента;
	ПараметрыРегистрацияПолучитьДанныеССервера.СпецОператорСвязи                         = Перечисления.СпецоператорыСвязи.КалугаАстрал;
	ПараметрыРегистрацияПолучитьДанныеССервера.ВременныйФайлОбновленийАдрес              = Новый УникальныйИдентификатор;
	ПараметрыРегистрацияПолучитьДанныеССервера.ДанныеУзлаКонфиденциальнаяИнформацияАдрес = Новый УникальныйИдентификатор;
	ПараметрыРегистрацияПолучитьДанныеССервера.ВызовИзМастераПодключенияК1СОтчетности    = Истина;
	
	Сертификаты = Неопределено; // Будет инициализирована после загрузки сертификатов.
	Результат = РегистрацияПолучитьДанныеССервера(ПараметрыРегистрацияПолучитьДанныеССервера, Ложь, Сертификаты);
	Если НЕ Результат Тогда
		СообщениеОбОшибке = НСтр("ru = 'Не удалось получить настройки с сервиса спецоператора.'");
		Возврат Ложь;
	КонецЕсли;
	
	ДанныеУзлаКонфиденциальнаяИнформацияАдрес = ПараметрыРегистрацияПолучитьДанныеССервера.ДанныеУзлаКонфиденциальнаяИнформацияАдрес;
	АдресВоВременномХранилище = ДанныеУзлаКонфиденциальнаяИнформацияАдрес;
	
	// Расшифровка
	РасшифроватьPKCS7(ДанныеУзлаКонфиденциальнаяИнформацияАдрес, СообщениеОбОшибке);
	
	//Обновление справочников
	ПараметрыРегистрацияОбновитьСправочники = ПараметрыРегистрацияОбновитьСправочники();
	ЗаполнитьЗначенияСвойств(ПараметрыРегистрацияОбновитьСправочники, ПараметрыРегистрацияПолучитьДанныеССервера);
	ЗаполнитьЗначенияСвойств(ПараметрыРегистрацияОбновитьСправочники, РеквизитыЗаявления);
	
	ПараметрыРегистрацияОбновитьСправочники.ИдентификаторДокументооборота       = ИдентификаторДокументооборота;
	ПараметрыРегистрацияОбновитьСправочники.ЭтоЭлектроннаяПодписьВМоделиСервиса = Истина;
	ПараметрыРегистрацияОбновитьСправочники.ТихийРежим                          = Истина;
	ПараметрыРегистрацияОбновитьСправочники.РежимАвтонастройки                  = Истина;
	ПараметрыРегистрацияОбновитьСправочники.СохранениеВИБОтключено              = Ложь;
	
	ПараметрыРегистрацияОбновитьСправочники.АдресВоВременномХранилище           = АдресВоВременномХранилище;
	
	РезультатОбновленияСправочников = РегистрацияОбновитьСправочники(ПараметрыРегистрацияОбновитьСправочники);
	Если НЕ РезультатОбновленияСправочников Тогда
		СообщениеОбОшибке = НСтр("ru = 'Не удалось применить настройки.'");
		Возврат Ложь;
	КонецЕсли;
	
	ПараметрыСохранитьДополнительныеРеквизитыУчетнойЗаписи = ПараметрыСохранитьДополнительныеРеквизитыУчетнойЗаписи();
	
	УчетнаяЗапись = РегистрацияПолучитьУчетнуюЗапись(ИдентификаторАбонента);
	ПараметрыУчетнойЗаписи = ?(НЕ ЗначениеЗаполнено(УчетнаяЗапись.Ссылка), ПараметрыРегистрацияОбновитьСправочники.НоваяУчетнаяЗапись, УчетнаяЗапись);
	//Получим свойства сертификата
	Запрос = Новый Запрос;
	Запрос.Текст =
	"ВЫБРАТЬ
	|	ХранилищеСертификатов.Сертификат КАК Сертификат
	|ИЗ
	|	РегистрСведений.ХранилищеСертификатов КАК ХранилищеСертификатов
	|ГДЕ
	|	ХранилищеСертификатов.Идентификатор = &Идентификатор";
	Запрос.УстановитьПараметр("Идентификатор", ИдентификаторСертификата);
	Выборка = Запрос.Выполнить().Выбрать();
	Если Выборка.Следующий() Тогда
		ПараметрыСертификата = Выборка.Сертификат.Получить();
	Иначе
		СообщениеОбОшибке = НСтр("ru = 'Не найден установленный сертификат.'");
		Возврат Ложь;
	КонецЕсли;
	
	ПараметрыСохранитьДополнительныеРеквизитыУчетнойЗаписи.УчетнаяЗаписьОрганизации 						= ПараметрыУчетнойЗаписи;
	ПараметрыСохранитьДополнительныеРеквизитыУчетнойЗаписи.ПараметрыСертификата 							= ПараметрыСертификата;
	ПараметрыСохранитьДополнительныеРеквизитыУчетнойЗаписи.ИдентификаторАбонента 							= ИдентификаторАбонента;
	ПараметрыСохранитьДополнительныеРеквизитыУчетнойЗаписи.ВременныйФайлОбновленийАдрес 					= ПараметрыРегистрацияОбновитьСправочники.ВременныйФайлОбновленийАдрес;
	ПараметрыСохранитьДополнительныеРеквизитыУчетнойЗаписи.ВызовИзМастераПодключенияК1СОтчетности 			= Истина;
	ПараметрыСохранитьДополнительныеРеквизитыУчетнойЗаписи.ТекстОшибокДляМастераПодключенияК1СОтчетности 	= "";
	
	СохранитьДополнительныеРеквизитыУчетнойЗаписи(ПараметрыСохранитьДополнительныеРеквизитыУчетнойЗаписи);
	
	СтруктураАвтонастройкиФСС = ПараметрыРегистрацияОбновитьСправочники.СтруктураАвтонастройкиФСС;
	Если СтруктураАвтонастройкиФСС <> Неопределено Тогда
		ОбновитьРегистрыСведенийНастройкиОбменаФСС(РеквизитыЗаявления.Организация,
						СтруктураАвтонастройкиФСС.ОтпечатокСертификата,
						СтруктураАвтонастройкиФСС.СертификатФССОтпечаток,
						СтруктураАвтонастройкиФСС.СертификатФССЭЛНОтпечаток,
						Ложь);
	КонецЕсли;
	
	Если ЗначениеЗаполнено(ПараметрыУчетнойЗаписи) Тогда
		
		//Установим реквизиты заявления на подключение
		Если ЗначениеЗаполнено(Заявление) И ЗаявлениеАбонентаВМобильном(Заявление) Тогда
			РеквизитыДокументаДляЗаписи = Новый Структура;
			РеквизитыДокументаДляЗаписи.Вставить("УчетнаяЗапись", 		ПараметрыУчетнойЗаписи);
			РеквизитыДокументаДляЗаписи.Вставить("Статус",				Перечисления.СтатусыЗаявленияАбонентаСпецоператораСвязи.Одобрено);
			РеквизитыДокументаДляЗаписи.Вставить("СтатусКомментарий",	"Создана учетная запись.");
			РеквизитыДокументаДляЗаписи.Вставить("НастройкаЗавершена", 	Истина);
			ОбработкаЗаявленийАбонентаВызовСервера.ОбновитьРеквизитыЗаявления(Заявление, РеквизитыДокументаДляЗаписи);
			
			//Запишем учетную запись в реквизиты организации
			Если ЗначениеЗаполнено(РеквизитыЗаявления.Организация) Тогда
				УстановитьПривилегированныйРежим(Истина);
				Объект = РеквизитыЗаявления.Организация.ПолучитьОбъект();
				Объект.УчетнаяЗаписьОбмена 				 = ПараметрыУчетнойЗаписи;
				Объект.ВидОбменаСКонтролирующимиОрганами = Перечисления.ВидыОбменаСКонтролирующимиОрганами.ОбменВУниверсальномФормате;
				Объект.Записать();
				УстановитьПривилегированныйРежим(Ложь);
			КонецЕсли;
		КонецЕсли;
		
	КонецЕсли;
	
	Возврат Истина;

КонецФункции

Функция ЗаявлениеАбонентаВМобильном(Ссылка, Организация = Неопределено) Экспорт
	
	МассивРеквизитов = Новый Массив;
	МассивРеквизитов.Добавить("Организация");
	МассивРеквизитов.Добавить("ПодписатьЭП");
	МассивРеквизитов.Добавить("ПризнакКрупнейшегоНалогоплательщика");
	МассивРеквизитов.Добавить("ПризнакОбособленногоПодразделения");
	МассивРеквизитов.Добавить("СпецоператорСвязи");
	МассивРеквизитов.Добавить("ТипЗаявления");
	МассивРеквизитов.Добавить("ЭлектроннаяПодписьВМоделиСервиса");
	
	РеквизитыЗаявления = ОбщегоНазначения.ЗначенияРеквизитовОбъекта(Ссылка, МассивРеквизитов);
	
	Если ЗначениеЗаполнено(Организация)
		И Организация <> РеквизитыЗаявления.Организация Тогда
		Возврат Ложь;
	ИначеЕсли РеквизитыЗаявления.ПодписатьЭП Тогда
		Возврат Ложь;
	ИначеЕсли РеквизитыЗаявления.ПризнакКрупнейшегоНалогоплательщика Тогда
		Возврат Ложь;
	ИначеЕсли РеквизитыЗаявления.ПризнакОбособленногоПодразделения Тогда
		Возврат Ложь;
	ИначеЕсли РеквизитыЗаявления.СпецоператорСвязи <> Перечисления.СпецоператорыСвязи.КалугаАстрал Тогда
		Возврат Ложь;
	ИначеЕсли РеквизитыЗаявления.ТипЗаявления <> Перечисления.ТипыЗаявленияАбонентаСпецоператораСвязи.Первичное Тогда
		Возврат Ложь;
	ИначеЕсли НЕ РеквизитыЗаявления.ЭлектроннаяПодписьВМоделиСервиса Тогда
		Возврат Ложь;
	КонецЕсли;
	
	Возврат Истина;
	
КонецФункции

Процедура ОбновитьРегистрыСведенийНастройкиОбменаФСС(
		Организация,
		ОтпечатокСертификата,
		СертификатФССОтпечаток,
		СертификатФССЭЛНОтпечаток,
		ВыводитьСообщения) Экспорт
	
	Попытка
		ОбменФСС = РегистрыСведений.НастройкиОбменаФСС.СоздатьМенеджерЗаписи();
		ОбменФСС.Организация = Организация;
		ОбменФСС.Пользователь = Справочники.Пользователи.ПустаяСсылка();
		ОбменФСС.Прочитать();
		ОбменФСС.Организация = Организация;
		ОбменФСС.Пользователь = Справочники.Пользователи.ПустаяСсылка();
		ОбменФСС.ИспользоватьОбмен = Истина;
		ОбменФСС.СертификатСтрахователяОтпечаток = ОтпечатокСертификата;
		ОбменФСС.СертификатФССОтпечаток = СертификатФССОтпечаток;
		Если (ЗначениеЗаполнено(СертификатФССЭЛНОтпечаток) И НЕ ОбменФСС.ТестовыйСерверФССЭЛН)
			ИЛИ НЕ ЗначениеЗаполнено(ОбменФСС.СертификатФССЭЛНОтпечаток) Тогда
			ОбменФСС.СертификатФССЭЛНОтпечаток = СертификатФССЭЛНОтпечаток;
			ОбменФСС.ТестовыйСерверФССЭЛН = Ложь;
		КонецЕсли;
		ОбменФСС.ИспользоватьАвтонастройку = Истина;
		ОбменФСС.Записать();
		Если ВыводитьСообщения Тогда
			ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(НСтр("ru = 'Обновлены параметры отправки отчетности в ФСС.'"));
		КонецЕсли;
		
	Исключение
		Если ВыводитьСообщения Тогда
			ТекстСообщения = СтроковыеФункцииКлиентСервер.ПодставитьПараметрыВСтроку(
				НСтр("ru = 'Не удалось обновить параметры отправки отчетности в ФСС по причине:
				|%1'"), КраткоеПредставлениеОшибки(ИнформацияОбОшибке()));
			ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
		КонецЕсли;
	КонецПопытки;
	
КонецПроцедуры

Процедура ЗаписатьСтатусОтправкиЕдиничногоОбъекта(ДокументСсылка, Статус)
	
	МенеджерЗаписи = РегистрыСведений.СтатусыОтправки.СоздатьМенеджерЗаписи();
	МенеджерЗаписи.Объект = ДокументСсылка;
	МенеджерЗаписи.Статус = Статус;
	МенеджерЗаписи.Записать(Истина);
		
КонецПроцедуры

#КонецОбласти

#КонецОбласти

Функция ПорядковыйНомерСообщенияВЦиклеОбмена(ЦиклОбмена, Сообщение) Экспорт
	
	Если Сообщение = Неопределено Тогда
		Возврат 0;
	Иначе
		СоответствиеТипаСообщенияПорядку = СоответствиеТипаСообщенияПорядку(ЦиклОбмена);
		Возврат СоответствиеТипаСообщенияПорядку.Получить(Сообщение.Тип);
	КонецЕсли;
	
КонецФункции

Функция СоответствиеТипаСообщенияПорядку(ЦиклОбмена) Экспорт
	
	СоответствиеТипаСообщенияПорядку = Новый Соответствие;
	
	Если ЦиклОбмена.Тип = Перечисления.ТипыЦикловОбмена.НалоговаяИлиБухгалтерскаяОтчетность Тогда
		
		Если ЦиклОбмена.ФорматДокументооборота = Перечисления.ФорматыДокументооборотаСФНС.Приказ534 Тогда
			
			СоответствиеТипаСообщенияПорядку.Вставить(Перечисления.ТипыТранспортныхСообщений.ДекларацияНП, 1);
			СоответствиеТипаСообщенияПорядку.Вставить(Перечисления.ТипыТранспортныхСообщений.ПодтверждениеДекларацияНО, 2);
			СоответствиеТипаСообщенияПорядку.Вставить(Перечисления.ТипыТранспортныхСообщений.ИзвещениеПодтверждениеНП, 3);
			СоответствиеТипаСообщенияПорядку.Вставить(Перечисления.ТипыТранспортныхСообщений.ИзвещениеДекларацияНО, 4);
			СоответствиеТипаСообщенияПорядку.Вставить(Перечисления.ТипыТранспортныхСообщений.РезультатПриемаДекларацияНО, 5);
			СоответствиеТипаСообщенияПорядку.Вставить(Перечисления.ТипыТранспортныхСообщений.ИзвещениеРезультатПриемаНП, 6);
			СоответствиеТипаСообщенияПорядку.Вставить(Перечисления.ТипыТранспортныхСообщений.РезультатОбработкиДекларацияНО, 7);
			СоответствиеТипаСообщенияПорядку.Вставить(Перечисления.ТипыТранспортныхСообщений.ИзвещениеРезультатОбработкиНП, 8);
			
		КонецЕсли;
		
	ИначеЕсли ЦиклОбмена.Тип = Перечисления.ТипыЦикловОбмена.Заявление Тогда
			
			СоответствиеТипаСообщенияПорядку.Вставить(Перечисления.ТипыТранспортныхСообщений.ЗаявлениеНП, 						1);
			СоответствиеТипаСообщенияПорядку.Вставить(Перечисления.ТипыТранспортныхСообщений.ПодтверждениеЗаявлениеНО, 			2);
			СоответствиеТипаСообщенияПорядку.Вставить(Перечисления.ТипыТранспортныхСообщений.ИзвещениеПодтверждениеНП, 			3);
			СоответствиеТипаСообщенияПорядку.Вставить(Перечисления.ТипыТранспортныхСообщений.ИзвещениеЗаявлениеНО, 				4);
			СоответствиеТипаСообщенияПорядку.Вставить(Перечисления.ТипыТранспортныхСообщений.РезультатПриемаЗаявлениеНО, 		5);
			СоответствиеТипаСообщенияПорядку.Вставить(Перечисления.ТипыТранспортныхСообщений.ИзвещениеРезультатПриемаНП, 		6);
			
			СоответствиеТипаСообщенияПорядку.Вставить(Перечисления.ТипыТранспортныхСообщений.РезультатОбработкиЗаявлениеНО, 	7);
			СоответствиеТипаСообщенияПорядку.Вставить(Перечисления.ТипыТранспортныхСообщений.ИзвещениеРезультатОбработкиНП, 	8);
			
			СоответствиеТипаСообщенияПорядку.Вставить(Перечисления.ТипыТранспортныхСообщений.РезультатОбработкиЗаявлениеРФНО, 	7);
			СоответствиеТипаСообщенияПорядку.Вставить(Перечисления.ТипыТранспортныхСообщений.ИзвещениеРезультатОбработкиРФНП, 	8);
			СоответствиеТипаСообщенияПорядку.Вставить(Перечисления.ТипыТранспортныхСообщений.СообщениеОбОтзывеЗаявлениеРФНО, 	9);
			СоответствиеТипаСообщенияПорядку.Вставить(Перечисления.ТипыТранспортныхСообщений.ИзвещениеОбОтзывеЗаявлениеРФНП, 	10);
			СоответствиеТипаСообщенияПорядку.Вставить(Перечисления.ТипыТранспортныхСообщений.РезультатОбработкиЗаявлениеТСНО, 	11);
			СоответствиеТипаСообщенияПорядку.Вставить(Перечисления.ТипыТранспортныхСообщений.ИзвещениеРезультатОбработкиТСНП, 	12);
			
	ИначеЕсли ЦиклОбмена.Тип = Перечисления.ТипыЦикловОбмена.Документ Тогда
		
		СоответствиеТипаСообщенияПорядку.Вставить(Перечисления.ТипыТранспортныхСообщений.ДокументНО, 					1);
		СоответствиеТипаСообщенияПорядку.Вставить(Перечисления.ТипыТранспортныхСообщений.ИзвещениеДокументНП, 			2);
		СоответствиеТипаСообщенияПорядку.Вставить(Перечисления.ТипыТранспортныхСообщений.РезультатПриемаДокументНП, 	3);
		СоответствиеТипаСообщенияПорядку.Вставить(Перечисления.ТипыТранспортныхСообщений.ИзвещениеРезультатПриемаНО, 	4);

	ИначеЕсли ЦиклОбмена.Тип = Перечисления.ТипыЦикловОбмена.Форма2НДФЛ Тогда
		
		СоответствиеТипаСообщенияПорядку.Вставить(Перечисления.ТипыТранспортныхСообщений.Форма2НДФЛНП, 1);
		СоответствиеТипаСообщенияПорядку.Вставить(Перечисления.ТипыТранспортныхСообщений.ПодтверждениеФорма2НДФЛНО, 2);
		СоответствиеТипаСообщенияПорядку.Вставить(Перечисления.ТипыТранспортныхСообщений.ИзвещениеПодтверждениеНП, 3);
		СоответствиеТипаСообщенияПорядку.Вставить(Перечисления.ТипыТранспортныхСообщений.ИзвещениеФорма2НДФЛНО, 4);
		СоответствиеТипаСообщенияПорядку.Вставить(Перечисления.ТипыТранспортныхСообщений.РезультатПриемаФорма2НДФЛНО, 5);
		СоответствиеТипаСообщенияПорядку.Вставить(Перечисления.ТипыТранспортныхСообщений.ИзвещениеРезультатПриемаНП, 6);
		
	ИначеЕсли ЦиклОбмена.Тип = Перечисления.ТипыЦикловОбмена.НеформализованныеДокументыНалоговогоОргана Тогда
		
		//
		
	ИначеЕсли ЦиклОбмена.Тип = Перечисления.ТипыЦикловОбмена.НеформализованныеДокументыНалогоплательщика Тогда
		
		//
		
	ИначеЕсли ЦиклОбмена.Тип = Перечисления.ТипыЦикловОбмена.ЗапросыНаИнформационноеОбслуживание Тогда
		
		//
		
	ИначеЕсли ЦиклОбмена.Тип = Перечисления.ТипыЦикловОбмена.ОтчетностьПФР Тогда
		
		СоответствиеТипаСообщенияПорядку.Вставить(Перечисления.ТипыТранспортныхСообщений.ПервичноеСообщениеСодержащееОтчетностьПФР, 1);
		СоответствиеТипаСообщенияПорядку.Вставить(Перечисления.ТипыТранспортныхСообщений.ПодтверждениеПолученияОтчетностиПФР, 2);
		СоответствиеТипаСообщенияПорядку.Вставить(Перечисления.ТипыТранспортныхСообщений.ПротоколПФР, 3);
		СоответствиеТипаСообщенияПорядку.Вставить(Перечисления.ТипыТранспортныхСообщений.ПротоколКвитанцияПФР, 4);
		
	ИначеЕсли ЦиклОбмена.Тип = Перечисления.ТипыЦикловОбмена.НеформализованнаяПерепискаПФРВходящие Тогда
		
		СоответствиеТипаСообщенияПорядку.Вставить(Перечисления.ТипыТранспортныхСообщений.ПисьмоВходящееПФР, 1);
		СоответствиеТипаСообщенияПорядку.Вставить(Перечисления.ТипыТранспортныхСообщений.ПисьмоВходящееКвитанцияПФР, 2);
		
	ИначеЕсли ЦиклОбмена.Тип = Перечисления.ТипыЦикловОбмена.РассылкаПФР Тогда		
		
		СоответствиеТипаСообщенияПорядку.Вставить(Перечисления.ТипыТранспортныхСообщений.ПисьмоВходящееПФР, 1);
		СоответствиеТипаСообщенияПорядку.Вставить(Перечисления.ТипыТранспортныхСообщений.ПисьмоВходящееКвитанцияПФР, 2);
		
	ИначеЕсли ЦиклОбмена.Тип = Перечисления.ТипыЦикловОбмена.НеформализованнаяПерепискаПФРИсходящие Тогда
		
		СоответствиеТипаСообщенияПорядку.Вставить(Перечисления.ТипыТранспортныхСообщений.ПисьмоИсходящееПФР, 1);
		СоответствиеТипаСообщенияПорядку.Вставить(Перечисления.ТипыТранспортныхСообщений.ПисьмоИсходящееКвитанцияПФР, 2);
		
	ИначеЕсли ЦиклОбмена.Тип = Перечисления.ТипыЦикловОбмена.ИОС Тогда
		
		СоответствиеТипаСообщенияПорядку.Вставить(Перечисления.ТипыТранспортныхСообщений.ПервичноеСообщениеСодержащееЗапросПФР, 1);
		СоответствиеТипаСообщенияПорядку.Вставить(Перечисления.ТипыТранспортныхСообщений.ПодтверждениеПолученияЗапросаПФР, 2);
		СоответствиеТипаСообщенияПорядку.Вставить(Перечисления.ТипыТранспортныхСообщений.ОтветНаЗапросПФР, 3);
		СоответствиеТипаСообщенияПорядку.Вставить(Перечисления.ТипыТранспортныхСообщений.ОтветНаЗапросКвитанцияПФР, 4);
		
	ИначеЕсли ЦиклОбмена.Тип = Перечисления.ТипыЦикловОбмена.ОбращениеНП Тогда
		
		СоответствиеТипаСообщенияПорядку.Вставить(Перечисления.ТипыТранспортныхСообщений.ОбращениеНП, 1);
		СоответствиеТипаСообщенияПорядку.Вставить(Перечисления.ТипыТранспортныхСообщений.ПодтверждениеОбращениеНО, 2);
		СоответствиеТипаСообщенияПорядку.Вставить(Перечисления.ТипыТранспортныхСообщений.ИзвещениеПодтверждениеНП, 3);
		СоответствиеТипаСообщенияПорядку.Вставить(Перечисления.ТипыТранспортныхСообщений.ИзвещениеОбращениеНО, 4);
		СоответствиеТипаСообщенияПорядку.Вставить(Перечисления.ТипыТранспортныхСообщений.РезультатПриемаОбращениеНО, 5);
		СоответствиеТипаСообщенияПорядку.Вставить(Перечисления.ТипыТранспортныхСообщений.ИзвещениеРезультатПриемаНП, 6);
		
	ИначеЕсли ЦиклОбмена.Тип = Перечисления.ТипыЦикловОбмена.Представление Тогда
		
		СоответствиеТипаСообщенияПорядку.Вставить(Перечисления.ТипыТранспортныхСообщений.ПредставлениеНП, 1);
		СоответствиеТипаСообщенияПорядку.Вставить(Перечисления.ТипыТранспортныхСообщений.ПодтверждениеПредставлениеНО, 2);
		СоответствиеТипаСообщенияПорядку.Вставить(Перечисления.ТипыТранспортныхСообщений.ИзвещениеПодтверждениеНП, 3);
		СоответствиеТипаСообщенияПорядку.Вставить(Перечисления.ТипыТранспортныхСообщений.ИзвещениеПредставлениеНО, 4);
		СоответствиеТипаСообщенияПорядку.Вставить(Перечисления.ТипыТранспортныхСообщений.РезультатПриемаПредставлениеНО, 5);
		СоответствиеТипаСообщенияПорядку.Вставить(Перечисления.ТипыТранспортныхСообщений.ИзвещениеРезультатПриемаНП, 6);
		
	ИначеЕсли ЦиклОбмена.Тип = Перечисления.ТипыЦикловОбмена.ПисьмоНО Тогда
		
		СоответствиеТипаСообщенияПорядку.Вставить(Перечисления.ТипыТранспортныхСообщений.ПисьмоНО, 1);
		СоответствиеТипаСообщенияПорядку.Вставить(Перечисления.ТипыТранспортныхСообщений.ИзвещениеПисьмоНП, 2);
		
	ИначеЕсли ЦиклОбмена.Тип = Перечисления.ТипыЦикловОбмена.Рассылка Тогда
		
		СоответствиеТипаСообщенияПорядку.Вставить(Перечисления.ТипыТранспортныхСообщений.РассылкаНО, 1);
		
	ИначеЕсли ЦиклОбмена.Тип = Перечисления.ТипыЦикловОбмена.РассылкаГрупповая Тогда
		
		СоответствиеТипаСообщенияПорядку.Вставить(Перечисления.ТипыТранспортныхСообщений.РассылкаНО, 1);
		СоответствиеТипаСообщенияПорядку.Вставить(Перечисления.ТипыТранспортныхСообщений.ИзвещениеРассылкаНП, 2);
		
	ИначеЕсли ЦиклОбмена.Тип = Перечисления.ТипыЦикловОбмена.ИОН Тогда
		
		СоответствиеТипаСообщенияПорядку.Вставить(Перечисления.ТипыТранспортныхСообщений.ЗапросНП, 1);
		СоответствиеТипаСообщенияПорядку.Вставить(Перечисления.ТипыТранспортныхСообщений.ПодтверждениеЗапросНО, 2);
		СоответствиеТипаСообщенияПорядку.Вставить(Перечисления.ТипыТранспортныхСообщений.ИзвещениеПодтверждениеНП, 3);
		СоответствиеТипаСообщенияПорядку.Вставить(Перечисления.ТипыТранспортныхСообщений.ИзвещениеЗапросНО, 4);
		СоответствиеТипаСообщенияПорядку.Вставить(Перечисления.ТипыТранспортныхСообщений.РезультатПриемаЗапросНО, 5);
		СоответствиеТипаСообщенияПорядку.Вставить(Перечисления.ТипыТранспортныхСообщений.ИзвещениеРезультатПриемаНП, 6);
		СоответствиеТипаСообщенияПорядку.Вставить(Перечисления.ТипыТранспортныхСообщений.РезультатОбработкиЗапросНО, 7);
		СоответствиеТипаСообщенияПорядку.Вставить(Перечисления.ТипыТранспортныхСообщений.ИзвещениеРезультатОбработкиНП, 8);
		
	ИначеЕсли ЦиклОбмена.Тип = Перечисления.ТипыЦикловОбмена.ОтчетностьФСГС Тогда
		
		СоответствиеТипаСообщенияПорядку.Вставить(Перечисления.ТипыТранспортныхСообщений.ПервичноеСообщениеСодержащееОтчетностьФСГС, 1);
		СоответствиеТипаСообщенияПорядку.Вставить(Перечисления.ТипыТранспортныхСообщений.ПодтверждениеДатыОтправкиФСГС, 2);
		СоответствиеТипаСообщенияПорядку.Вставить(Перечисления.ТипыТранспортныхСообщений.ИзвещениеПодтверждениеДатыОтправкиФСГС, 3);
		СоответствиеТипаСообщенияПорядку.Вставить(Перечисления.ТипыТранспортныхСообщений.ИзвещениеОПолученииОтчетностиФСГС, 4);
		СоответствиеТипаСообщенияПорядку.Вставить(Перечисления.ТипыТранспортныхСообщений.ПротоколВходногоКонтроляОтчетностиФСГС, 5);
		СоответствиеТипаСообщенияПорядку.Вставить(Перечисления.ТипыТранспортныхСообщений.ИзвещениеПротоколВходногоКонтроляОтчетностиФСГС, 6);
		
	ИначеЕсли ЦиклОбмена.Тип = Перечисления.ТипыЦикловОбмена.ПисьменноеОбращениеВФСГС Тогда
		
		СоответствиеТипаСообщенияПорядку.Вставить(Перечисления.ТипыТранспортныхСообщений.ПисьмоИсходящееФСГС, 1);
		СоответствиеТипаСообщенияПорядку.Вставить(Перечисления.ТипыТранспортныхСообщений.ПодтверждениеДатыОтправкиФСГС, 2);
		СоответствиеТипаСообщенияПорядку.Вставить(Перечисления.ТипыТранспортныхСообщений.ИзвещениеПодтверждениеДатыОтправкиФСГС, 3);
		СоответствиеТипаСообщенияПорядку.Вставить(Перечисления.ТипыТранспортныхСообщений.ИзвещениеПисьмоИсходящееФСГС, 4);
		
	ИначеЕсли ЦиклОбмена.Тип = Перечисления.ТипыЦикловОбмена.ИндивидуальноеИнформированиеФСГС Тогда
		
		СоответствиеТипаСообщенияПорядку.Вставить(Перечисления.ТипыТранспортныхСообщений.ПисьмоВходящееФСГС, 1);
		СоответствиеТипаСообщенияПорядку.Вставить(Перечисления.ТипыТранспортныхСообщений.ИзвещениеПисьмоВходящееФСГС, 2);
		
	ИначеЕсли ЦиклОбмена.Тип = Перечисления.ТипыЦикловОбмена.РассылкаФСГС Тогда
		
		СоответствиеТипаСообщенияПорядку.Вставить(Перечисления.ТипыТранспортныхСообщений.РассылкаФСГС, 1);

	ИначеЕсли ЦиклОбмена.Тип = Перечисления.ТипыЦикловОбмена.РассылкаШаблоновФСГС Тогда
		
		СоответствиеТипаСообщенияПорядку.Вставить(Перечисления.ТипыТранспортныхСообщений.РассылкаШаблоновФСГС, 1);
		
	ИначеЕсли ЦиклОбмена.Тип = Перечисления.ТипыЦикловОбмена.ЗапросНаВыпискуИзЕГРЮЛ_ЕГРИП Тогда 
				
	КонецЕсли;

	Возврат СоответствиеТипаСообщенияПорядку;
	
КонецФункции

Функция СоответствиеТипаСообщенияПредставлению() Экспорт
	
	СоответствиеТипаСообщенияПредставлению = Новый Соответствие;
	
	СоответствиеТипаСообщенияПредставлению.Вставить(Перечисления.ТипыТранспортныхСообщений.ДекларацияНП,																		"Первичное сообщение, содержащее отчетность");
	СоответствиеТипаСообщенияПредставлению.Вставить(Перечисления.ТипыТранспортныхСообщений.ИзвещениеДекларацияНО,																"Извещение о получении отчета");
	СоответствиеТипаСообщенияПредставлению.Вставить(Перечисления.ТипыТранспортныхСообщений.РезультатПриемаДекларацияНО,															"Результат приема декларации");
	СоответствиеТипаСообщенияПредставлению.Вставить(Перечисления.ТипыТранспортныхСообщений.ИзвещениеРезультатПриемаНП,															"Извещение о получении результата приема");
	СоответствиеТипаСообщенияПредставлению.Вставить(Перечисления.ТипыТранспортныхСообщений.РезультатОбработкиДекларацияНО,														"Результат обработки декларации");
	СоответствиеТипаСообщенияПредставлению.Вставить(Перечисления.ТипыТранспортныхСообщений.ИзвещениеРезультатОбработкиНП,														"Извещение о получении результата обработки");
	СоответствиеТипаСообщенияПредставлению.Вставить(Перечисления.ТипыТранспортныхСообщений.ПодтверждениеДекларацияНО,															"Подтверждение даты отправки");
	СоответствиеТипаСообщенияПредставлению.Вставить(Перечисления.ТипыТранспортныхСообщений.ИзвещениеПодтверждениеНП,															"Извещение о получении подтверждения даты отправки");
	
	СоответствиеТипаСообщенияПредставлению.Вставить(Перечисления.ТипыТранспортныхСообщений.Форма2НДФЛНП,																		"Первичное сообщение, содержащее сведения формы 2-НДФЛ");
	СоответствиеТипаСообщенияПредставлению.Вставить(Перечисления.ТипыТранспортныхСообщений.ПодтверждениеФорма2НДФЛНО,															"Подтверждение даты отправки");
	СоответствиеТипаСообщенияПредставлению.Вставить(Перечисления.ТипыТранспортныхСообщений.ИзвещениеФорма2НДФЛНО,																"Извещение о получении формы 2-НДФЛ");
	СоответствиеТипаСообщенияПредставлению.Вставить(Перечисления.ТипыТранспортныхСообщений.РезультатПриемаФорма2НДФЛНО,															"Результат приема формы 2-НДФЛ");
	
	СоответствиеТипаСообщенияПредставлению.Вставить(Перечисления.ТипыТранспортныхСообщений.ОбращениеНП,																			"Обращение налогоплательщика");
	СоответствиеТипаСообщенияПредставлению.Вставить(Перечисления.ТипыТранспортныхСообщений.ИзвещениеОбращениеНО,																"Извещение о получении обращения");
	СоответствиеТипаСообщенияПредставлению.Вставить(Перечисления.ТипыТранспортныхСообщений.ПодтверждениеОбращениеНО,															"Подтверждение даты отправки");
	СоответствиеТипаСообщенияПредставлению.Вставить(Перечисления.ТипыТранспортныхСообщений.РезультатПриемаОбращениеНО,															"Результат приема обращения");
	
	СоответствиеТипаСообщенияПредставлению.Вставить(Перечисления.ТипыТранспортныхСообщений.ПредставлениеНП,																		"Представление документов");
	СоответствиеТипаСообщенияПредставлению.Вставить(Перечисления.ТипыТранспортныхСообщений.ИзвещениеПредставлениеНО,															"Извещение о получении представленных документов");
	СоответствиеТипаСообщенияПредставлению.Вставить(Перечисления.ТипыТранспортныхСообщений.ПодтверждениеПредставлениеНО,														"Подтверждение даты отправки");
	СоответствиеТипаСообщенияПредставлению.Вставить(Перечисления.ТипыТранспортныхСообщений.РезультатПриемаПредставлениеНО,														"Результат приема представленных документов");
	
	СоответствиеТипаСообщенияПредставлению.Вставить(Перечисления.ТипыТранспортныхСообщений.ПисьмоНО,																			"Письмо налогового органа");
	СоответствиеТипаСообщенияПредставлению.Вставить(Перечисления.ТипыТранспортныхСообщений.ИзвещениеПисьмоНП,																	"Извещение о получении письма налогового органа");
	СоответствиеТипаСообщенияПредставлению.Вставить(Перечисления.ТипыТранспортныхСообщений.РассылкаНО,																			"Рассылка налогового органа");
	СоответствиеТипаСообщенияПредставлению.Вставить(Перечисления.ТипыТранспортныхСообщений.ИзвещениеРассылкаНП,																	"Извещение о получении рассылки налогового органа");
	
	СоответствиеТипаСообщенияПредставлению.Вставить(Перечисления.ТипыТранспортныхСообщений.ЗапросНП,																			"Первичное сообщение, содержащее запрос");
	СоответствиеТипаСообщенияПредставлению.Вставить(Перечисления.ТипыТранспортныхСообщений.ИзвещениеЗапросНО,																	"Извещение о получении запроса");
	СоответствиеТипаСообщенияПредставлению.Вставить(Перечисления.ТипыТранспортныхСообщений.РезультатПриемаЗапросНО,																"Результат приема запроса");
	СоответствиеТипаСообщенияПредставлению.Вставить(Перечисления.ТипыТранспортныхСообщений.ИзвещениеРезультатПриемаНП,															"Извещение о получении результата приема");
	СоответствиеТипаСообщенияПредставлению.Вставить(Перечисления.ТипыТранспортныхСообщений.РезультатОбработкиЗапросНО,															"Ответ на запрос");
	СоответствиеТипаСообщенияПредставлению.Вставить(Перечисления.ТипыТранспортныхСообщений.ПодтверждениеЗапросНО,																"Подтверждение даты отправки");
	СоответствиеТипаСообщенияПредставлению.Вставить(Перечисления.ТипыТранспортныхСообщений.ИзвещениеПодтверждениеНП,															"Извещение о получении подтверждения даты отправки");
	
	СоответствиеТипаСообщенияПредставлению.Вставить(Перечисления.ТипыТранспортныхСообщений.ПервичноеСообщениеСодержащееОтчетностьПФР,											"Первичное сообщение, содержащее отчетность");
	СоответствиеТипаСообщенияПредставлению.Вставить(Перечисления.ТипыТранспортныхСообщений.ПодтверждениеПолученияОтчетностиПФР,													"Квитанция о доставке отчетности");
	СоответствиеТипаСообщенияПредставлению.Вставить(Перечисления.ТипыТранспортныхСообщений.ПротоколПФР,																			"Протокол контроля отчетности");
	СоответствиеТипаСообщенияПредставлению.Вставить(Перечисления.ТипыТранспортныхСообщений.ПротоколКвитанцияПФР,																"Квитанция о доставке протокола контроля");
	
	СоответствиеТипаСообщенияПредставлению.Вставить(Перечисления.ТипыТранспортныхСообщений.ПервичноеСообщениеСодержащееЗапросПФР,												"Первичное сообщение, содержащее запрос");
	СоответствиеТипаСообщенияПредставлению.Вставить(Перечисления.ТипыТранспортныхСообщений.ПодтверждениеПолученияЗапросаПФР,													"Квитанция о доставке запроса");
	СоответствиеТипаСообщенияПредставлению.Вставить(Перечисления.ТипыТранспортныхСообщений.ОтветНаЗапросПФР,																	"Ответ на запрос");
	СоответствиеТипаСообщенияПредставлению.Вставить(Перечисления.ТипыТранспортныхСообщений.ОтветНаЗапросКвитанцияПФР,															"Квитанция о доставке ответа на запрос");
	
	СоответствиеТипаСообщенияПредставлению.Вставить(Перечисления.ТипыТранспортныхСообщений.ПисьмоИсходящееПФР,																	"Первичное сообщение, содержащее письмо в ПФР");
	СоответствиеТипаСообщенияПредставлению.Вставить(Перечисления.ТипыТранспортныхСообщений.ПисьмоИсходящееКвитанцияПФР,															"Квитанция о доставке письма");
	
	СоответствиеТипаСообщенияПредставлению.Вставить(Перечисления.ТипыТранспортныхСообщений.ПисьмоВходящееПФР,																	"Первичное сообщение, содержащее письмо от ПФР");
	СоответствиеТипаСообщенияПредставлению.Вставить(Перечисления.ТипыТранспортныхСообщений.ПисьмоВходящееКвитанцияПФР,															"Квитанция о доставке письма");
	
	СоответствиеТипаСообщенияПредставлению.Вставить(Перечисления.ТипыТранспортныхСообщений.КритическаяОшибка,																	"Сообщение о критической ошибке");
	СоответствиеТипаСообщенияПредставлению.Вставить(Перечисления.ТипыТранспортныхСообщений.УведомлениеОбОшибкеПФР,																"Уведомление об ошибке");
	СоответствиеТипаСообщенияПредставлению.Вставить(Перечисления.ТипыТранспортныхСообщений.УведомлениеОбОшибкеФНС,																"Уведомление об ошибке");
	
	СоответствиеТипаСообщенияПредставлению.Вставить(Перечисления.ТипыТранспортныхСообщений.ПервичноеСообщениеСодержащееОтчетностьФСГС,											"Первичное сообщение, содержащее отчетность");
	СоответствиеТипаСообщенияПредставлению.Вставить(Перечисления.ТипыТранспортныхСообщений.ПодтверждениеДатыОтправкиФСГС,														"Подтверждение даты отправки");
	СоответствиеТипаСообщенияПредставлению.Вставить(Перечисления.ТипыТранспортныхСообщений.ИзвещениеПодтверждениеДатыОтправкиФСГС,												"Извещение о получении подтверждения даты отправки");
	СоответствиеТипаСообщенияПредставлению.Вставить(Перечисления.ТипыТранспортныхСообщений.ИзвещениеОПолученииОтчетностиФСГС,													"Извещение о получении отчетности");
	СоответствиеТипаСообщенияПредставлению.Вставить(Перечисления.ТипыТранспортныхСообщений.ПротоколВходногоКонтроляОтчетностиФСГС,												"Протокол входного контроля отчетности");
	СоответствиеТипаСообщенияПредставлению.Вставить(Перечисления.ТипыТранспортныхСообщений.ИзвещениеПротоколВходногоКонтроляОтчетностиФСГС,										"Извещение о получении протокола входного контроля");
	СоответствиеТипаСообщенияПредставлению.Вставить(Перечисления.ТипыТранспортныхСообщений.ПисьмоИсходящееФСГС,																	"Первичное сообщение, содержащее письмо в Росстат");
	СоответствиеТипаСообщенияПредставлению.Вставить(Перечисления.ТипыТранспортныхСообщений.ИзвещениеПисьмоИсходящееФСГС,														"Извещение о получении письма его получателем");
	СоответствиеТипаСообщенияПредставлению.Вставить(Перечисления.ТипыТранспортныхСообщений.ПисьмоВходящееФСГС,																	"Первичное сообщение, содержащее письмо из Росстата");
	СоответствиеТипаСообщенияПредставлению.Вставить(Перечисления.ТипыТранспортныхСообщений.РассылкаФСГС,																		"Первичное сообщение, содержащее рассылку от Росстата");
	СоответствиеТипаСообщенияПредставлению.Вставить(Перечисления.ТипыТранспортныхСообщений.РассылкаШаблоновФСГС,																"Первичное сообщение, содержащее рассылку шаблона от Росстата");
	СоответствиеТипаСообщенияПредставлению.Вставить(Перечисления.ТипыТранспортныхСообщений.ИзвещениеПисьмоВходящееФСГС,															"Извещение о получении письма его получателем");
	СоответствиеТипаСообщенияПредставлению.Вставить(Перечисления.ТипыТранспортныхСообщений.УведомлениеОбОшибкеФСГС,																"Уведомление об ошибке");
	
	СоответствиеТипаСообщенияПредставлению.Вставить(Перечисления.ТипыТранспортныхСообщений.Прочее,																				"Прочее сообщение");
	СоответствиеТипаСообщенияПредставлению.Вставить(Перечисления.ТипыТранспортныхСообщений.ПустаяСсылка(),																		"Сообщение неизвестного типа");
	
	СоответствиеТипаСообщенияПредставлению.Вставить(Перечисления.ТипыТранспортныхСообщений.ЗаявлениеНП,																			"Первичное сообщение, содержащее отчетность");
	СоответствиеТипаСообщенияПредставлению.Вставить(Перечисления.ТипыТранспортныхСообщений.ИзвещениеЗаявлениеНО,																"Извещение о получении отчета");
	СоответствиеТипаСообщенияПредставлению.Вставить(Перечисления.ТипыТранспортныхСообщений.РезультатПриемаЗаявлениеНО,															"Результат приема заявления");
	СоответствиеТипаСообщенияПредставлению.Вставить(Перечисления.ТипыТранспортныхСообщений.ИзвещениеРезультатПриемаНП,															"Извещение о получении результата приема");
	СоответствиеТипаСообщенияПредставлению.Вставить(Перечисления.ТипыТранспортныхСообщений.РезультатОбработкиЗаявлениеРФНО,														"Результат обработки заявления в РФ");
	СоответствиеТипаСообщенияПредставлению.Вставить(Перечисления.ТипыТранспортныхСообщений.ИзвещениеРезультатОбработкиРФНП,														"Извещение о получении результата обработки заявления в РФ");
	СоответствиеТипаСообщенияПредставлению.Вставить(Перечисления.ТипыТранспортныхСообщений.ПодтверждениеЗаявлениеНО,															"Подтверждение даты отправки");
	СоответствиеТипаСообщенияПредставлению.Вставить(Перечисления.ТипыТранспортныхСообщений.ИзвещениеПодтверждениеНП,															"Извещение о получении подтверждения даты отправки");
	СоответствиеТипаСообщенияПредставлению.Вставить(Перечисления.ТипыТранспортныхСообщений.СообщениеОбОтзывеЗаявлениеРФНО,														"Сообщение об отзыве заявления в РФ");
	СоответствиеТипаСообщенияПредставлению.Вставить(Перечисления.ТипыТранспортныхСообщений.ИзвещениеОбОтзывеЗаявлениеРФНП,														"Извещение о получении сообщения об отзыве заявления в РФ");
	СоответствиеТипаСообщенияПредставлению.Вставить(Перечисления.ТипыТранспортныхСообщений.РезультатОбработкиЗаявлениеТСНО,														"Результат обработки заявления в ТС");
	СоответствиеТипаСообщенияПредставлению.Вставить(Перечисления.ТипыТранспортныхСообщений.ИзвещениеРезультатОбработкиТСНП,														"Извещение о получении результата обработки заявления в ТС");
	
	СоответствиеТипаСообщенияПредставлению.Вставить(Перечисления.ТипыТранспортныхСообщений.РезультатОбработкиЗаявлениеНО,														"Результат обработки заявления");
	
	СоответствиеТипаСообщенияПредставлению.Вставить(Перечисления.ТипыТранспортныхСообщений.ДокументНО,																			"Документ налогового органа");
	СоответствиеТипаСообщенияПредставлению.Вставить(Перечисления.ТипыТранспортныхСообщений.ИзвещениеДокументНП,																	"Извещение о получении документа налогового органа");
	СоответствиеТипаСообщенияПредставлению.Вставить(Перечисления.ТипыТранспортныхСообщений.РезультатПриемаДокументНП,															"Результат приема документа налогового органа");
	СоответствиеТипаСообщенияПредставлению.Вставить(Перечисления.ТипыТранспортныхСообщений.ИзвещениеРезультатПриемаНО,															"Извещение о получении результата приема документа налогового органа");
	
	СоответствиеТипаСообщенияПредставлению.Вставить(Перечисления.ТипыТранспортныхСообщений.ЗапросНаВыпискуЕРГЮЛ_ЕГРИП,															"Первичное сообщение, содержащее запрос");
	СоответствиеТипаСообщенияПредставлению.Вставить(Перечисления.ТипыТранспортныхСообщений.РезультатОбработкиЗапросаНаВыпискуЕРГЮЛ_ЕГРИП,										"Ответ на запрос");

	Возврат СоответствиеТипаСообщенияПредставлению;
	
КонецФункции

Функция ПредставлениеТранспортногоСообщнияПоТипу(ТранспортноеСообщение) Экспорт

	СоответствиеТипаСообщенияПредставлению = СоответствиеТипаСообщенияПредставлению();
	Представление = СоответствиеТипаСообщенияПредставлению.Получить(ТранспортноеСообщение.Тип);
	
	Возврат Представление;
	
КонецФункции

Функция ПечатьВозможнаДляЦиклаОбмена(Тип) Экспорт
	
	Возврат Тип = Перечисления.ТипыЦикловОбмена.НалоговаяИлиБухгалтерскаяОтчетность 
		ИЛИ Тип = Перечисления.ТипыЦикловОбмена.ОтчетностьПФР  
		ИЛИ Тип = Перечисления.ТипыЦикловОбмена.Форма2НДФЛ
		ИЛИ Тип = Перечисления.ТипыЦикловОбмена.ОтчетностьФСГС
		ИЛИ Тип = Перечисления.ТипыЦикловОбмена.Заявление
		ИЛИ Тип = Перечисления.ТипыЦикловОбмена.ИОН
		ИЛИ Тип = Перечисления.ТипыЦикловОбмена.Представление
		ИЛИ Тип = Перечисления.ТипыЦикловОбмена.Документ
		ИЛИ Тип = Перечисления.ТипыЦикловОбмена.ОбращениеНП
		ИЛИ Тип = Перечисления.ТипыЦикловОбмена.Рассылка
		ИЛИ Тип = Перечисления.ТипыЦикловОбмена.РассылкаГрупповая
		ИЛИ Тип = Перечисления.ТипыЦикловОбмена.ПисьмоНО
		ИЛИ Тип = Перечисления.ТипыЦикловОбмена.ИОС
		ИЛИ Тип = Перечисления.ТипыЦикловОбмена.НеформализованнаяПерепискаПФРИсходящие
		ИЛИ Тип = Перечисления.ТипыЦикловОбмена.ПисьменноеОбращениеВФСГС;
	
КонецФункции

Функция ПолучитьПараметрыДляЗапускаМетодаБЭД(ЗаявлениеСсылка) Экспорт
	
	Если ЗначениеЗаполнено(ЗаявлениеСсылка.НастройкиЭДО) Тогда
		Возврат ЗаявлениеСсылка.НастройкиЭДО;
	Иначе
		Результат = Неопределено;
		
		Запрос = Новый Запрос;
		Запрос.Текст = 
			"ВЫБРАТЬ РАЗРЕШЕННЫЕ ПЕРВЫЕ 1
			|	ЗаявлениеАбонентаСпецоператораСвязи.Организация,
			|	ЗаявлениеАбонентаСпецоператораСвязи.АдресЮридический,
			|	ЗаявлениеАбонентаСпецоператораСвязи.ПодключитьЭДО,
			|	ЗаявлениеАбонентаСпецоператораСвязи.ОператорЭДО,
			|	ЗаявлениеАбонентаСпецоператораСвязи.КодНалоговогоОрганаЭДО,
			|	ЗаявлениеАбонентаСпецоператораСвязи.ТипКриптопровайдера
			|ИЗ
			|	Документ.ЗаявлениеАбонентаСпецоператораСвязи КАК ЗаявлениеАбонентаСпецоператораСвязи
			|ГДЕ
			|	ЗаявлениеАбонентаСпецоператораСвязи.УчетнаяЗапись <> ЗНАЧЕНИЕ(Справочник.УчетныеЗаписиДокументооборота.ПустаяСсылка)
			|	И ЗаявлениеАбонентаСпецоператораСвязи.Ссылка = &ЗаявлениеСсылка";
		
		Запрос.УстановитьПараметр("ЗаявлениеСсылка", ЗаявлениеСсылка);
		
		РезультатЗапроса = Запрос.Выполнить();
		
		Выборка = РезультатЗапроса.Выбрать();
		
		Если Выборка.Следующий() Тогда
			
			Если Выборка.ПодключитьЭДО Тогда
				
				Результат = Новый Структура;
				
				Результат.Вставить("Организация", 				Выборка.Организация);
				Результат.Вставить("ОператорЭДО", 				Выборка.ОператорЭДО);
				Результат.Вставить("КодНалоговогоОргана", 		Выборка.КодНалоговогоОрганаЭДО);
				
				//ТипКриптопровайдера
				Если Выборка.ТипКриптопровайдера = Перечисления.ТипыКриптоПровайдеров.CryptoPro Тогда
					ТипКриптоПровайдера = "CryptoPro";
				ИначеЕсли Выборка.ТипКриптопровайдера = Перечисления.ТипыКриптоПровайдеров.VipNet Тогда 
					ТипКриптоПровайдера = "VipNet";
				Иначе
					Возврат Неопределено;
				КонецЕсли;
				Результат.Вставить("ТипКриптопровайдера", ТипКриптоПровайдера);
				
				АдресЮридический = Выборка.АдресЮридический;
				Если ЗначениеЗаполнено(АдресЮридический) Тогда
					
					ЭтоАдресПоФИАСу = УправлениеКонтактнойИнформациейКлиентСервер.ЭтоКонтактнаяИнформацияВXML(АдресЮридический);
					Если ЭтоАдресПоФИАСу Тогда
						
						СведенияОбАдресе = СведенияОбАдресе(АдресЮридический);

						Результат.Вставить("Индекс", 			СведенияОбАдресе.Индекс);
						Результат.Вставить("Район",  			СведенияОбАдресе.Район);
						Результат.Вставить("Город",  			СведенияОбАдресе.Город);
						Результат.Вставить("НаселенныйПункт",	СведенияОбАдресе.НаселенныйПункт);
						Результат.Вставить("Улица",				СведенияОбАдресе.Улица);
						Результат.Вставить("Дом", 				СведенияОбАдресе.Здание.Номер);
						
						// Корпус, квартира
						Результат.Вставить("Корпус", "");
						Результат.Вставить("Квартира", "");
						Корпуса = СведенияОбАдресе.Корпуса;
						Для каждого Корпус Из Корпуса Цикл
							
							Если ЗначениеЗаполнено(Корпус.Номер) И Корпус.ТипКорпуса = НСтр("ru = 'Корпус'") Тогда
								Результат.Вставить("Корпус", Корпус.Номер);
							КонецЕсли;
							
							Если ЗначениеЗаполнено(Корпус.Номер) И Корпус.ТипКорпуса = НСтр("ru = 'Квартира'") Тогда
								Результат.Вставить("Квартира", Корпус.Номер);
							КонецЕсли;
							
						КонецЦикла;
						
						Результат.Вставить("КодРегиона", 	СведенияОбАдресе.КодРегиона);
						Результат.Вставить("Регион", 		СведенияОбАдресе.Регион);
						
					Иначе
						
						СтруктураАдреса = РегламентированнаяОтчетностьКлиентСервер.РазложитьАдрес(АдресЮридический);
						
						Если ЗначениеЗаполнено(СтруктураАдреса) Тогда
							
							Результат.Вставить("Индекс", 			СтруктураАдреса.Индекс);
							Результат.Вставить("Район", 			СтруктураАдреса.Район);
							Результат.Вставить("Город", 			СтруктураАдреса.Город);
							Результат.Вставить("НаселенныйПункт", 	СтруктураАдреса.НаселенныйПункт);
							Результат.Вставить("Улица", 			СтруктураАдреса.Улица);
							Результат.Вставить("Дом", 				СтруктураАдреса.Дом);
							Результат.Вставить("Корпус", 			СтруктураАдреса.Корпус);
							Результат.Вставить("Квартира", 			СтруктураАдреса.Квартира);
							
							КодРегиона = СтруктураАдреса.Регион;
							Результат.Вставить("КодРегиона", КодРегиона);
							
							Регион = РегламентированнаяОтчетностьВызовСервера.ПолучитьНазваниеРегионаПоКоду(КодРегиона);
							Если ЗначениеЗаполнено(Регион) Тогда
								Результат.Вставить("Регион", Регион);
							КонецЕсли;
							
						Иначе
							
							Возврат Неопределено;
							
						КонецЕсли;
						
					КонецЕсли;
					
				Иначе
					
					Возврат Неопределено;
					
				КонецЕсли;

			КонецЕсли;
			
		КонецЕсли;
		
		Возврат Результат;
		
	КонецЕсли;
	
КонецФункции

Функция ИмяПрограммыБИП() Экспорт
	
	УстановитьПривилегированныйРежим(Истина);
	
	ВладелецТикета = ЭлектронныйДокументооборотСКонтролирующимиОрганамиКлиентСервер.ВладелецТикета();
	
	МодульИнтернетПоддержкаПользователей = ЭлектронныйДокументооборотСКонтролирующимиОрганами.МодульИнтернетПоддержкаПользователей();
	
	Если МодульИнтернетПоддержкаПользователей = Неопределено Тогда
		Возврат "";
	Иначе
		Возврат МодульИнтернетПоддержкаПользователей.ИмяПрограммы();
	КонецЕсли;
	
КонецФункции

#КонецОбласти

#КонецОбласти

#Область ЗаявленияНаЭДОВПФР

Процедура УстановитьВидимостьМенюПФР(Форма) Экспорт
	
	Отправляются = ДокументооборотСКОВызовСервера.ЗаявленияПоЭДООтправляютсяВПФР();
	Форма.Элементы.ЗаявленияВПФР.Видимость = Отправляются;
	
КонецПроцедуры

Процедура ОбновитьСостояниеЭДОсПФРПриЗаписиТранспортногоСообщения(Предмет,
		ОбновляемоеТранспортноеСообщение = Неопределено) Экспорт
	
	Если ЗначениеЗаполнено(ОбновляемоеТранспортноеСообщение) Тогда
		Отказ = Ложь;
		ПриЗаписиТранспортногоСообщения(ОбновляемоеТранспортноеСообщение, Отказ);
	КонецЕсли;
	Если ТипЗнч(Предмет) = Тип("ДокументСсылка.ЗаявленияПоЭлДокументооборотуСПФР") Тогда
		Отказ = Ложь;
		ПриЗаписиЗаявленияПоЭлДокументооборотуСПФР(Предмет, Отказ);
	КонецЕсли;
	
КонецПроцедуры

Функция ДоступнаОтправкаЗаявленияВПФР(Форма) Экспорт

	Заявление = Форма.ТекущееЗаявлениеПо1СОтчетности;
	Одобрено  = Заявление.Статус = Перечисления.СтатусыЗаявленияАбонентаСпецоператораСвязи.Одобрено;
	ЗаявленияОтправляются = ДокументооборотСКОВызовСервера.ЗаявленияПоЭДООтправляютсяВПФР();
	НужноеСостояние = 
		Заявление.НастройкаЗавершена И Форма.ЭтоПредупреждениеИзПанели 
		ИЛИ НЕ Форма.ЭтоПредупреждениеИзПанели;
	
	Возврат 
		Одобрено 
		И НужноеСостояние
		И Форма.СостояниеЭДОсПФР <> Неопределено
		И ЗаявленияОтправляются;

КонецФункции

Функция НужноОтправитьЗаявлениеВПФРИзМастера(Форма, Вид) Экспорт
	
	Заявление                = Форма.ТекущееЗаявлениеПо1СОтчетности;
	ЭтоПервичноеЗаявление    = Заявление.ТипЗаявления = Перечисления.ТипыЗаявленияАбонентаСпецоператораСвязи.Первичное;
	ЭтоВторичноеЗаявление    = НЕ ЭтоПервичноеЗаявление;
	ДоступнаОтправка         = ДоступнаОтправкаЗаявленияВПФР(Форма);
	НужноОтправить           = Ложь;
	
	// Если стоит ИспользоватьСуществующий, значит считаем, что все нужные заявления отправлены из другой базы.
	ИспользоватьСуществующий = ОбработкаЗаявленийАбонентаКлиентСервер.ИспользоватьСуществующий(Заявление);
	
	Если ДоступнаОтправка Тогда
		
		НаправлениеПодключено = Форма.СостояниеЭДОсПФР.НаправлениеПодключено;
		
		Если Вид = Форма.НаПодключение Тогда
			
			// Не рассматриваем случай, когда это заявление на переиздание сертификата и при этом ПФР было подключено ранее,
			// но заявление в отправлено ПФР не было.
			// Причина - потому что в тексте в панели указано, что в этом заявлении должно быть подключение ПФР
			СдаватьВПФР   = ЭтоПервичноеЗаявление И ВПервичномЗаявленииПодключилиПФР(Заявление);
			ПодключилиПФР = ЭтоВторичноеЗаявление И ЭтоИзменениеСдаватьВПФР(Заявление);

			НужноОтправить = 
				Форма.СостояниеЭДОсПФР.НаПодключение.НужноОтправитьЗаявление 
				И НаправлениеПодключено
				И (СдаватьВПФР ИЛИ ПодключилиПФР)
				И НЕ ИспользоватьСуществующий;
				
		ИначеЕсли Вид = Форма.НаОтключение Тогда
				
			НеПодключенВПФР = ЭтоПервичноеЗаявление И НЕ ВПервичномЗаявленииПодключилиПФР(Заявление);
			ОтключилиПФР    = ЭтоВторичноеЗаявление И ЭтоИзменениеСдаватьВПФР(Заявление);

			// Здесь не проверяем признак Состояние.НаправлениеПодключено 
			// потому что когда открывается форма настроки заявления этот признак все еще равен НаправлениеПодключено = Истина
			НужноОтправить = 
				Форма.СостояниеЭДОсПФР.НаОтключение.НужноОтправитьЗаявление 
				И Форма.СостояниеЭДОсПФР.НаПодключение.ОрганизацияПодключена
				И (НеПодключенВПФР ИЛИ ОтключилиПФР)
				И НЕ ИспользоватьСуществующий;
			
		ИначеЕсли Вид = Форма.НаСертификат Тогда

			ЭтоТекущийОтпечаток       = Заявление.ОтпечатокПолученногоСертификата = Форма.СостояниеЭДОсПФР.НаСертификат.Отпечаток;
			ЭтоПереизданиеСертификата = ЭтоВторичноеЗаявление И ЭтоПереизданиеСертификата(Заявление);
			// В одном заявлении могли отключить ПФР и переиздать серт.
			// В этом случае не нужно отправлять сертификат.
			ЭтоНеОтключение           = НЕ НужноОтправитьЗаявлениеВПФРИзМастера(Форма, Форма.НаОтключение);
			
			НужноОтправить = 
				НаправлениеПодключено
				И Форма.СостояниеЭДОсПФР.НаПодключение.ОрганизацияПодключена
				И (ЭтоПервичноеЗаявление ИЛИ ЭтоПереизданиеСертификата)
				И Форма.СостояниеЭДОсПФР.НаСертификат.НужноОтправитьЗаявление
				И ЭтоТекущийОтпечаток
				И ЭтоНеОтключение
				И НЕ ИспользоватьСуществующий
				И НЕ Форма.СостояниеЭДОсПФР.НаПодключение.ЗаявлениеОтправлено;
	
		КонецЕсли;
		
	КонецЕсли;
	
	Возврат НужноОтправить;

КонецФункции

Функция ВПервичномЗаявленииПодключилиПФР(Заявление) Экспорт
	
	СдаватьВПФР = Ложь;
	Для Каждого СтрокаНаправления из Заявление.Получатели цикл 
		Если СтрокаНаправления.ТипПолучателя = ПредопределенноеЗначение("Перечисление.ТипыКонтролирующихОрганов.ПФР") Тогда
			СдаватьВПФР = Истина;
		КонецЕсли;
	КонецЦикла;
	
	Возврат СдаватьВПФР;
	
КонецФункции

Функция ЭтоПереизданиеСертификата(Заявление)
	
	ИзменившиесяРеквизиты = Заявление.ИзменившиесяРеквизитыВторичногоЗаявления;
	
	ПереизданСертификат = ЭтотПараметрИзменился(
		ИзменившиесяРеквизиты, 
		Перечисления.ПараметрыПодключенияК1СОтчетности.ПереизданиеСертификата);
		
	Возврат ПереизданСертификат;

КонецФункции

Функция ЭтоИзменениеСдаватьВПФР(Заявление)
	
	ИзменившиесяРеквизиты = Заявление.ИзменившиесяРеквизитыВторичногоЗаявления;
	
	СдаватьВПФРИзменился = ЭтотПараметрИзменился(
		ИзменившиесяРеквизиты, 
		Перечисления.ПараметрыПодключенияК1СОтчетности.СдаватьВПФР);
		
	Возврат СдаватьВПФРИзменился;
			
КонецФункции

Процедура Мастер_ПриСозданииНаСервере(Форма, Заявление) Экспорт
	
	Форма.ТекущееЗаявлениеПо1СОтчетности = Заявление;
	Форма.ЭтоПредупреждениеИзМастера 	 = Истина;
	Форма.ЭтоПредупреждениеИзПанели 	 = Истина;
	
	ИнициализироватьВидыЗаявленийНаЭДОВПФР(Форма);
	
	Если Заявление.Статус = Перечисления.СтатусыЗаявленияАбонентаСпецоператораСвязи.Одобрено
		И Заявление.НастройкаЗавершена Тогда
		Форма.СостояниеЭДОсПФР = СостояниеЭДОсПФР(Заявление.Организация);
	КонецЕсли;
	
КонецПроцедуры

Процедура ИзменитьОформлениеПанелиПФРВМастере(Форма) Экспорт
	
	Заявление = Форма.ТекущееЗаявлениеПо1СОтчетности;
	Элементы  = Форма.Элементы;
	
	Элементы.ПодсказкаПФР.Видимость = Ложь;
	
	Если ЗначениеЗаполнено(Заявление) И ЗначениеЗаполнено(Заявление.Организация) Тогда
	
		Форма.СостояниеЭДОсПФР   = СостояниеЭДОсПФР(Заявление.Организация);
		Одобрено                 = Заявление.Статус = ПредопределенноеЗначение("Перечисление.СтатусыЗаявленияАбонентаСпецоператораСвязи.Одобрено");
		НастройкаЗавершена       = Заявление.НастройкаЗавершена;
		Отправляются             = ДокументооборотСКОВызовСервера.ЗаявленияПоЭДООтправляютсяВПФР();
		НужноОтправитьЗаявлениеНаПодключение = НужноОтправитьЗаявлениеВПФРИзМастера(Форма, Перечисления.ВидыЗаявленийНаЭДОВПФР.НаПодключение);
		НужноОтправитьЗаявлениеНаСертификат  = НужноОтправитьЗаявлениеВПФРИзМастера(Форма, Перечисления.ВидыЗаявленийНаЭДОВПФР.НаСертификат)
			И Форма.СостояниеЭДОсПФР.НаПодключение.ОрганизацияПодключена;
		
		Элементы.ПанельПФР.Видимость = 
			Отправляются И Одобрено И НастройкаЗавершена И
			(НужноОтправитьЗаявлениеНаПодключение ИЛИ НужноОтправитьЗаявлениеНаСертификат);
		
		Если НужноОтправитьЗаявлениеНаПодключение Тогда
			
			Текст1 = НСтр("ru = 'Вы отправили оператору заявление на подключение направления ПФР. Для начала использования электронных трудовых книжек вам также необходимо '");
			Текст2 = Новый ФорматированнаяСтрока(НСтр("ru = 'отправить'"),,,,"Отправьте");
			Текст3 = НСтр("ru = ' в ПФР специальное заявление.'");
			
		ИначеЕсли НужноОтправитьЗаявлениеНаСертификат Тогда
			
			Текст1 = НСтр("ru = 'Внимание! '");
			Текст2 = Новый ФорматированнаяСтрока(НСтр("ru = 'Отправьте'"),,,,"Отправьте");
			Текст3 = НСтр("ru = ' текущий сертификат в ПФР, чтобы иметь возможность обмениваться эл. документами с Фондом.'");
			
		КонецЕсли;
		
		Если НужноОтправитьЗаявлениеНаПодключение ИЛИ НужноОтправитьЗаявлениеНаСертификат Тогда
			Элементы.ПодсказкаПФР.Заголовок = Новый ФорматированнаяСтрока(Текст1, Текст2, Текст3);
			Элементы.ПодсказкаПФР.Видимость = Истина;
		КонецЕсли;
		
	Иначе
		Элементы.ПанельПФР.Видимость = Ложь;
	КонецЕсли;
	
КонецПроцедуры

Процедура ИзменитьОформлениеПредложенияОтправитьЗаявлениеВПФР(Форма, Элемент) Экспорт
	
	ЭтоНаПодключение = Форма.ИзначальныйВидЗаявленияПФР = Перечисления.ВидыЗаявленийНаЭДОВПФР.НаПодключение;
	ЭтоНаСертификат  = Форма.ИзначальныйВидЗаявленияПФР = Перечисления.ВидыЗаявленийНаЭДОВПФР.НаСертификат;
	
	Элемент.Видимость = ЭтоНаПодключение ИЛИ ЭтоНаСертификат;
	
	Если ЭтоНаПодключение Тогда
		Текст = НСтр("ru = 'Для того, чтобы вы могли начать использовать электронные трудовые книжки, после автоматической настройки будет предложено отправить в ПФР специальное заявление.'");
	ИначеЕсли ЭтоНаСертификат Тогда
		Текст = НСтр("ru = 'Для того, чтобы вы могли продолжить обмениваться документами с ПФР, после автоматической настройки будет предложено отправить в ПФР сертификат.'");
	КонецЕсли;
	Элемент.Заголовок = Текст;
	
КонецПроцедуры

Процедура ДействияПередОтправкойЗаявленияВПФР(Форма) Экспорт
	
	СохранитьНастройкиСостоянияЭДОСПФР(Форма);
	СоздатьЗаявленияВПФРЕслиНеСозданы(Форма);
	ОпределитьОтправляемоеЗаявлениеВПФР(Форма);

КонецПроцедуры

Функция ЗаявлениеПо1СОтчетностиДляЦелейПФР(Организация = Неопределено, Отпечаток = Неопределено) Экспорт
	
	// Получает список заявлений на подключение с направлением ПФР 
	// или на изменением с переизданием сертификата и направленинем ПФР или с подключением направления ПФР
	// Если передан отпечаток, то ищутся заявления по этому отпечатку, иначе - по организации.
	
	Запрос = Новый Запрос;
	Запрос.Текст = 
		"ВЫБРАТЬ РАЗРЕШЕННЫЕ
		|	ВторичныеЗаявления.Ссылка КАК Ссылка,
		|	ВторичныеЗаявления.Ссылка.Дата КАК ДатаСоздания
		|ИЗ
		|	Документ.ЗаявлениеАбонентаСпецоператораСвязи.ИзменившиесяРеквизитыВторичногоЗаявления КАК ВторичныеЗаявления
		|		ВНУТРЕННЕЕ СОЕДИНЕНИЕ Документ.ЗаявлениеАбонентаСпецоператораСвязи.Получатели КАК ВторичныеПолучатели
		|		ПО ВторичныеЗаявления.Ссылка = ВторичныеПолучатели.Ссылка
		|ГДЕ
		|	ВторичныеЗаявления.Ссылка.ПометкаУдаления = ЛОЖЬ
		|	И (ВторичныеЗаявления.ИзмененныйРеквизит = &СдаватьВПФР
		|				И &Отпечаток = НЕОПРЕДЕЛЕНО
		|			ИЛИ ВторичныеЗаявления.ИзмененныйРеквизит = &ПереизданиеСертификата
		|				И &Отпечаток <> НЕОПРЕДЕЛЕНО
		|				И ВторичныеЗаявления.Ссылка.СпособПолученияСертификата = &Новый)
		|	И ВторичныеПолучатели.ТипПолучателя = &ТипПолучателя
		|	И ВторичныеЗаявления.Ссылка.Статус = &Одобрено
		|	И ВторичныеЗаявления.Ссылка.НастройкаЗавершена = ИСТИНА
		|	И (ВторичныеЗаявления.Ссылка.Организация = &Организация
		|				И &Отпечаток = НЕОПРЕДЕЛЕНО
		|			ИЛИ ВторичныеЗаявления.Ссылка.ОтпечатокПолученногоСертификата = &Отпечаток
		|				И &Отпечаток <> НЕОПРЕДЕЛЕНО)
		|
		|ОБЪЕДИНИТЬ
		|
		|ВЫБРАТЬ
		|	ПервичныеЗаявления.Ссылка,
		|	ПервичныеЗаявления.Ссылка.Дата
		|ИЗ
		|	Документ.ЗаявлениеАбонентаСпецоператораСвязи КАК ПервичныеЗаявления
		|		ВНУТРЕННЕЕ СОЕДИНЕНИЕ Документ.ЗаявлениеАбонентаСпецоператораСвязи.Получатели КАК ПервичныеПолучатели
		|		ПО (ПервичныеПолучатели.Ссылка = ПервичныеПолучатели.Ссылка)
		|ГДЕ
		|	ПервичныеЗаявления.ПометкаУдаления = ЛОЖЬ
		|	И ПервичныеЗаявления.ТипЗаявления = &Первичное
		|	И ПервичныеПолучатели.ТипПолучателя = &ТипПолучателя
		|	И ПервичныеЗаявления.Статус = &Одобрено
		|	И ПервичныеЗаявления.НастройкаЗавершена = ИСТИНА
		|	И (ПервичныеЗаявления.Ссылка.Организация = &Организация
		|				И &Отпечаток = НЕОПРЕДЕЛЕНО
		|			ИЛИ ПервичныеЗаявления.Ссылка.ОтпечатокПолученногоСертификата = &Отпечаток
		|				И &Отпечаток <> НЕОПРЕДЕЛЕНО)
		|	И ПервичныеЗаявления.СпособПолученияСертификата = &Новый
		|
		|УПОРЯДОЧИТЬ ПО
		|	ДатаСоздания УБЫВ";
	
	Запрос.УстановитьПараметр("Первичное", Перечисления.ТипыЗаявленияАбонентаСпецоператораСвязи.Первичное);
	Запрос.УстановитьПараметр("СдаватьВПФР", Перечисления.ПараметрыПодключенияК1СОтчетности.СдаватьВПФР);
	Запрос.УстановитьПараметр("ПереизданиеСертификата", Перечисления.ПараметрыПодключенияК1СОтчетности.ПереизданиеСертификата);
	Запрос.УстановитьПараметр("ТипПолучателя", Перечисления.ТипыКонтролирующихОрганов.ПФР);
	Запрос.УстановитьПараметр("Одобрено", Перечисления.СтатусыЗаявленияАбонентаСпецоператораСвязи.Одобрено);
	Запрос.УстановитьПараметр("Организация", Организация);
	Запрос.УстановитьПараметр("Новый", Перечисления.СпособПолученияСертификата.ИздатьНовый);
	Запрос.УстановитьПараметр("Отпечаток", Отпечаток);
	
	РезультатЗапроса = Запрос.Выполнить();
	
	ВыборкаДетальныеЗаписи = РезультатЗапроса.Выбрать();
	
	Если ВыборкаДетальныеЗаписи.Следующий() Тогда
		Возврат ВыборкаДетальныеЗаписи.Ссылка;
	Иначе
		Возврат Неопределено;
	КонецЕсли;
	
КонецФункции

Функция ЗаявленияВПФРДанногоВида(Организация, Вид, СостояниеСдачиОтчетности, Отпечаток = "") Экспорт
	
	// Здесь получаем именно отправленные, но не завершенные
	Запрос = Новый Запрос;
	Запрос.Текст = "ВЫБРАТЬ РАЗРЕШЕННЫЕ
	               |	ЦиклОбмена.Ссылка КАК Ссылка,
	               |	ЦиклОбмена.Предмет КАК Предмет,
	               |	ЦиклОбмена.ДатаСоздания КАК ДатаСоздания
	               |ПОМЕСТИТЬ ЦиклыОбмена
	               |ИЗ
	               |	Справочник.ЦиклыОбмена КАК ЦиклОбмена
	               |ГДЕ
	               |	ЦиклОбмена.ПометкаУдаления = ЛОЖЬ
	               |	И ЦиклОбмена.ВидОтчета = ЗНАЧЕНИЕ(Справочник.ВидыОтправляемыхДокументов.ЗаявленияПоЭлДокументооборотуСПФР)
	               |	И ЦиклОбмена.Организация = &Организация
	               |
	               |ОБЪЕДИНИТЬ
	               |
	               |ВЫБРАТЬ
	               |	ЦиклОбмена.Ссылка,
	               |	ЦиклОбмена.Предмет,
	               |	ЦиклОбмена.ДатаСоздания
	               |ИЗ
	               |	Справочник.ЦиклыОбмена КАК ЦиклОбмена
	               |		ВНУТРЕННЕЕ СОЕДИНЕНИЕ Справочник.ЦиклыОбмена.ДополнительныеПредметы КАК ТЧЦиклОбмена
	               |		ПО (ТЧЦиклОбмена.Ссылка = ЦиклОбмена.Ссылка)
	               |ГДЕ
	               |	ЦиклОбмена.ПометкаУдаления = ЛОЖЬ
	               |	И ЦиклОбмена.ВидОтчета = ЗНАЧЕНИЕ(Справочник.ВидыОтправляемыхДокументов.ЗаявленияПоЭлДокументооборотуСПФР)
	               |	И ЦиклОбмена.Организация = &Организация
	               |;
	               |
	               |////////////////////////////////////////////////////////////////////////////////
	               |ВЫБРАТЬ РАЗРЕШЕННЫЕ ПЕРВЫЕ 1
	               |	ЦиклыОбмена.Ссылка КАК ЦиклОбмена,
	               |	ЗаявленияПоЭлДокументооборотуСПФР.Ссылка КАК Ссылка,
	               |	ТранспортноеСообщение.ДатаТранспорта КАК ДатаТранспорта,
	               |	ТранспортноеСообщение.Статус КАК Статус
	               |ИЗ
	               |	ЦиклыОбмена КАК ЦиклыОбмена
	               |		ВНУТРЕННЕЕ СОЕДИНЕНИЕ Документ.ТранспортноеСообщение КАК ТранспортноеСообщение
	               |		ПО (ТранспортноеСообщение.ЦиклОбмена = ЦиклыОбмена.Ссылка)
	               |		ВНУТРЕННЕЕ СОЕДИНЕНИЕ РегистрСведений.ЖурналОтправокВКонтролирующиеОрганы КАК ЖурналОтправокВКонтролирующиеОрганы
	               |		ПО (ЖурналОтправокВКонтролирующиеОрганы.Ссылка = ЦиклыОбмена.Предмет)
	               |		ВНУТРЕННЕЕ СОЕДИНЕНИЕ Документ.ЗаявленияПоЭлДокументооборотуСПФР КАК ЗаявленияПоЭлДокументооборотуСПФР
	               |		ПО ЦиклыОбмена.Предмет = ЗаявленияПоЭлДокументооборотуСПФР.Ссылка
	               |ГДЕ
	               |	ТранспортноеСообщение.Тип = ЗНАЧЕНИЕ(Перечисление.ТипыТранспортныхСообщений.ПервичноеСообщениеСодержащееОтчетностьПФР)
	               |	И ЗаявленияПоЭлДокументооборотуСПФР.Вид = &Вид
	               |	И (ЗаявленияПоЭлДокументооборотуСПФР.Отпечаток = &Отпечаток
	               |				И ЗаявленияПоЭлДокументооборотуСПФР.Вид = ЗНАЧЕНИЕ(Перечисление.ВидыЗаявленийНаЭДОВПФР.НаСертификат)
	               |			ИЛИ ЗаявленияПоЭлДокументооборотуСПФР.Вид <> ЗНАЧЕНИЕ(Перечисление.ВидыЗаявленийНаЭДОВПФР.НаСертификат))
	               |	И ЗаявленияПоЭлДокументооборотуСПФР.ПометкаУдаления = ЛОЖЬ
	               |	И ЖурналОтправокВКонтролирующиеОрганы.СостояниеСдачиОтчетности = &СостояниеСдачиОтчетности
	               |	И ЗаявленияПоЭлДокументооборотуСПФР.Организация = &Организация
	               |	И ТранспортноеСообщение.ДатаТранспорта <> ДАТАВРЕМЯ(1, 1, 1)
	               |
	               |УПОРЯДОЧИТЬ ПО
	               |	ТранспортноеСообщение.ДатаТранспорта УБЫВ";
	
	Запрос.УстановитьПараметр("Вид", Вид);
	Запрос.УстановитьПараметр("Отпечаток", Отпечаток);
	Запрос.УстановитьПараметр("Организация", Организация);
	Запрос.УстановитьПараметр("СостояниеСдачиОтчетности", СостояниеСдачиОтчетности);
	
	Возврат Запрос.Выполнить().Выгрузить();
	
КонецФункции

Функция ЕстьСотрудники(Организация)
	
	Если НЕ ЗначениеЗаполнено(Организация) Тогда
		Возврат Ложь;
	КонецЕсли;
		
	ЭтоЮрЛицо = ЭлектронныйДокументооборотСКонтролирующимиОрганамиВызовСервера.ЭтоЮрЛицо(Организация);
	
	Если ЭтоЮрЛицо Тогда 
		Возврат Истина;
	Иначе
		ИПИспользуетТрудНаемныхРаботников = РегламентированнаяОтчетность.ИПИспользуетТрудНаемныхРаботников(Организация);  
		Возврат ИПИспользуетТрудНаемныхРаботников;
	КонецЕсли;
	
КонецФункции

Функция ЗаявленияПоЭДОсПФРсОшибкой(Заявления)
	
	ЕстьОшибкиФормата = Ложь;
	Для каждого ЗаявлениеЭДОсПФР Из Заявления Цикл
		Если ЗначениеЗаполнено(ЗаявлениеЭДОсПФР.ЦиклОбмена) Тогда
			СообщенияЦиклаОбмена = ПолучитьСообщенияЦиклаОбмена(ЗаявлениеЭДОсПФР.ЦиклОбмена,
				Перечисления.ТипыТранспортныхСообщений.ПервичноеСообщениеСодержащееОтчетностьПФР);
			Если СообщенияЦиклаОбмена.Количество() > 0 Тогда
				Контейнеры = ПолучитьТранспортныйКонтейнер(СообщенияЦиклаОбмена[0].Ссылка, Истина);
				Если Контейнеры.Количество() > 0 И ЗначениеЗаполнено(Контейнеры[0].Данные)
					И ЗначениеЗаполнено(Контейнеры[0].Данные.Получить()) Тогда
					
					ИмяФайлаКонтейнера = ПолучитьИмяВременногоФайла("zip");
					Контейнеры[0].Данные.Получить().Записать(ИмяФайлаКонтейнера);
					
					// обход ошибки платформы для архива zip с кодировкой имен файлов cp866, сформированного в Linux
					ЧтениеЗИП = Неопределено;
					Попытка
						МассивОписанийИДанных = ОбщегоНазначенияЭДКО.МассивОписанийИДанныхZipФайла(ИмяФайлаКонтейнера);
						Если МассивОписанийИДанных <> Неопределено Тогда
							ОперацииСФайламиЭДКО.УдалитьВременныйФайл(ИмяФайлаКонтейнера);
							ИмяФайлаКонтейнера = ИмяФайлаКонтейнера + ".zip";
							ОбщегоНазначенияЭДКО.СформироватьZip(МассивОписанийИДанных, ИмяФайлаКонтейнера);
						КонецЕсли;
						ЧтениеЗИП = Новый ЧтениеZipФайла(ИмяФайлаКонтейнера);
					Исключение
						ОперацииСФайламиЭДКО.УдалитьВременныйФайл(ИмяФайлаКонтейнера);
					КонецПопытки;
					
					ИмяФайлаОписанияСведений = "";
					ЭлементОписаниеСведений = Неопределено;
					Если ЧтениеЗИП <> Неопределено Тогда
						Для каждого ЭлементКонтейнера Из ЧтениеЗИП.Элементы Цикл
							ИмяФайлаВРег = СокрЛП(ЭлементКонтейнера.ИмяБезРасширения);
							ИмяФайлаВРег = ВРег(ИмяФайлаВРег);
							РасширениеФайла = СокрЛП(ЭлементКонтейнера.Расширение);
							
							Если Лев(ИмяФайлаВРег, 4) = "ПФР_" И СтрНайти(ИмяФайлаВРег, "_ОСП_") > 0 И нрег(РасширениеФайла) = "xml" Тогда
								ИмяФайлаОписанияСведений = ЭлементКонтейнера.Имя;
								ЭлементОписаниеСведений = ЭлементКонтейнера;
								Прервать;
							КонецЕсли;
						КонецЦикла;
					КонецЕсли;
					
					Если ЭлементОписаниеСведений <> Неопределено Тогда
						КаталогРаспаковки = СоздатьВременныйКаталог();
						ЧтениеЗИП.Извлечь(ЭлементОписаниеСведений, КаталогРаспаковки, РежимВосстановленияПутейФайловZIP.НеВосстанавливать);
						
						ТекстXML = ПрочитатьТекстИзФайла(КаталогРаспаковки + ИмяФайлаОписанияСведений,, Истина);
						ЭтоОшибкаФормата = СтрНайти(ТекстXML, "<ФИО>") <> 0 И СтрНайти(ТекстXML, "<УТ2:ФИО>") = 0
							И СтрНайти(ТекстXML, "<УТ2:КПП>") = 0;
						
						ЧтениеЗИП.Закрыть();
						ОперацииСФайламиЭДКО.УдалитьВременныйФайл(КаталогРаспаковки);
						
						Если ЭтоОшибкаФормата Тогда
							ЕстьОшибкиФормата = Истина
						Иначе
							Возврат Ложь;
						КонецЕсли;
					КонецЕсли;
					ОперацииСФайламиЭДКО.УдалитьВременныйФайл(ИмяФайлаКонтейнера);
				КонецЕсли;
			КонецЕсли;
		КонецЕсли;
	КонецЦикла;
	
	Возврат ЕстьОшибкиФормата;
	
КонецФункции

Процедура ДополнитьСостояниеЭДОсПФРПодключением(Состояние, Организация, ЗаявленияОтправляются) Экспорт
	
	// На подключение
	НаПодключение = Новый Структура;
	
	НаПодключение.Вставить("ОрганизацияПодключена", ОрганизацияПодключенаКЭДОсПФР(Организация, Истина));
	
	Заявления = ЗаявленияВПФРДанногоВида(
		Организация, 
		Перечисления.ВидыЗаявленийНаЭДОВПФР.НаПодключение, 
		Перечисления.СостояниеСдачиОтчетности.ДокументооборотНачат);
		
	Если Заявления.Количество() > 0 Тогда
		НаПодключение.Вставить("ОтправленноеЗаявление", Заявления[0].Ссылка);
		НаПодключение.Вставить("ОтправленноеЗаявление_ДатаОтправки", Заявления[0].ДатаТранспорта);
		НаПодключение.Вставить("ЗаявлениеОтправлено", Истина);
	Иначе
		НаПодключение.Вставить("ОтправленноеЗаявление", Неопределено);
		НаПодключение.Вставить("ОтправленноеЗаявление_ДатаОтправки", Дата(1,1,1));
		НаПодключение.Вставить("ЗаявлениеОтправлено", Ложь);
	КонецЕсли;
	
	ПодключилиНоНеОтправилиЗаявление = 
		Состояние.НаправлениеПодключено 
		И НЕ НаПодключение.ОрганизацияПодключена
		И НЕ НаПодключение.ЗаявлениеОтправлено
		И ЗаявленияОтправляются
		И ЕстьСотрудники(Организация);
	
	ПодключилиИОтправилиЗаявлениеДляИПСОшибкой =
		ТекущаяДатаСеанса() < '20200201'
		И Состояние.НаправлениеПодключено
		И НЕ НаПодключение.ОрганизацияПодключена
		И НаПодключение.ЗаявлениеОтправлено
		И ЗаявленияОтправляются
		И НЕ РегламентированнаяОтчетностьВызовСервера.ЭтоЮридическоеЛицо(Организация)
		И ЕстьСотрудники(Организация)
		И ЗаявленияПоЭДОсПФРсОшибкой(Заявления);
	
	Одобренные = ЗаявленияВПФРДанногоВида(
		Организация, 
		Перечисления.ВидыЗаявленийНаЭДОВПФР.НаПодключение, 
		Перечисления.СостояниеСдачиОтчетности.ПоложительныйРезультатДокументооборота);
		
	НаПодключение.Вставить("ЕстьОдобренное", Одобренные.Количество() > 0);
	НаПодключение.Вставить("НужноОтправитьЗаявление", ПодключилиНоНеОтправилиЗаявление);
	НаПодключение.Вставить("НужноОтправитьПовторноеЗаявление", ПодключилиИОтправилиЗаявлениеДляИПСОшибкой);
	
	ЗаявлениеПо1СОтчетности = ЗаявлениеПо1СОтчетностиДляЦелейПФР(Организация);
	НаПодключение.Вставить("ЗаявлениеПо1СОтчетности", ЗаявлениеПо1СОтчетности);
	
	Состояние.Вставить("НаПодключение", НаПодключение);
	
КонецПроцедуры

Процедура ДополнитьСостояниеЭДОсПФРОтключением(Состояние, Организация, ЗаявленияОтправляются) Экспорт
	
	НаОтключение = Новый Структура;
	
	НаОтключение.Вставить("ОрганизацияОтключена", ОрганизацияОтключенаОтЭДКО(Организация));
	
	Заявления = ЗаявленияВПФРДанногоВида(
		Организация, 
		Перечисления.ВидыЗаявленийНаЭДОВПФР.НаОтключение,
		Перечисления.СостояниеСдачиОтчетности.ДокументооборотНачат);
	
	Если Заявления.Количество() > 0 Тогда
		НаОтключение.Вставить("ОтправленноеЗаявление", Заявления[0].Ссылка);
		НаОтключение.Вставить("ОтправленноеЗаявление_ДатаОтправки", Заявления[0].ДатаТранспорта);
		НаОтключение.Вставить("ЗаявлениеОтправлено", Истина);
	Иначе
		НаОтключение.Вставить("ОтправленноеЗаявление", Неопределено);
		НаОтключение.Вставить("ОтправленноеЗаявление_ДатаОтправки", Дата(1,1,1));
		НаОтключение.Вставить("ЗаявлениеОтправлено", Ложь);
	КонецЕсли;
	
	// Здесь не проверяем признак Состояние.НаправлениеПодключено 
	// потому что когда открывается форма настроки заявления этот признак все еще равен НаправлениеПодключено = Истина
	ОтключилиНоНеОтправилиЗаявление = 
		Состояние.НаПодключение.ОрганизацияПодключена
		И НЕ НаОтключение.ОрганизацияОтключена
		И НЕ НаОтключение.ЗаявлениеОтправлено
		И ЗаявленияОтправляются
		И ЕстьСотрудники(Организация);
		
	Одобренные = ЗаявленияВПФРДанногоВида(
		Организация, 
		Перечисления.ВидыЗаявленийНаЭДОВПФР.НаОтключение, 
		Перечисления.СостояниеСдачиОтчетности.ПоложительныйРезультатДокументооборота);
		
	НаОтключение.Вставить("ЕстьОдобренное", Одобренные.Количество() > 0);
		
	НаОтключение.Вставить("НужноОтправитьЗаявление", ОтключилиНоНеОтправилиЗаявление);
	
	Состояние.Вставить("НаОтключение", НаОтключение);
	
КонецПроцедуры

Процедура ДополнитьСостояниеЭДОсПФРСертификатом(Состояние, Организация, ЗаявленияОтправляются) Экспорт
	
	// На сертификат
	НаСертификат = Новый Структура;
	
	УчетнаяЗапись = УчетнаяЗаписьОрганизации(Организация);
	
	НаСертификат.Вставить("УчетнаяЗапись", УчетнаяЗапись);
	Если ЗначениеЗаполнено(УчетнаяЗапись) Тогда
		НаСертификат.Вставить("Отпечаток", УчетнаяЗапись.СертификатРуководителя);
	Иначе
		НаСертификат.Вставить("Отпечаток", "");
	КонецЕсли;
	
	// Заполняется позже на клиенте методом ДвДанныеСертификатаПоОрганизации
	НаСертификат.Вставить("ДвДанныеСертификата", Неопределено);
	
	НаСертификат.Вставить("ПФРУведомленОСмене", Ложь);
	НаСертификат.Вставить("ОтправленноеЗаявление", Неопределено);
	НаСертификат.Вставить("ОтправленноеЗаявление_ДатаОтправки", Дата(1,1,1));
	НаСертификат.Вставить("ЗаявлениеОтправлено", Ложь);
	
	Если ЗначениеЗаполнено(НаСертификат.Отпечаток) Тогда
		
		НаСертификат.Вставить("ПФРУведомленОСмене", СертификатПереданВПФР(НаСертификат.Отпечаток));
		
		Заявления = ЗаявленияВПФРДанногоВида(
			Организация, 
			Перечисления.ВидыЗаявленийНаЭДОВПФР.НаСертификат, 
			Перечисления.СостояниеСдачиОтчетности.ДокументооборотНачат, 
			НаСертификат.Отпечаток);
		
		Если Заявления.Количество() > 0 Тогда
			НаСертификат.Вставить("ОтправленноеЗаявление", Заявления[0].Ссылка);
			НаСертификат.Вставить("ОтправленноеЗаявление_ДатаОтправки", Заявления[0].ДатаТранспорта);
			НаСертификат.Вставить("ЗаявлениеОтправлено", Истина);
		КонецЕсли;
		
	КонецЕсли;
	
	СертификатыОтправляются = СертификатыОтправляютсяВПФР();
	ЗаявлениеПо1СОтчетности = ЗаявлениеПо1СОтчетностиДляЦелейПФР(, НаСертификат.Отпечаток);
	НаСертификат.Вставить("ЗаявлениеПо1СОтчетности", ЗаявлениеПо1СОтчетности);

	ПереиздалиНоНеОтправилиЗаявление = 
		Состояние.НаправлениеПодключено
		И НЕ Состояние.НаОтключение.ОрганизацияОтключена
		И СертификатыОтправляются
		И НЕ НаСертификат.ЗаявлениеОтправлено
		И НЕ НаСертификат.ПФРУведомленОСмене
		И ЗаявленияОтправляются
		И ЕстьСотрудники(Организация);
		
	НаСертификат.Вставить("НужноОтправитьЗаявление", ПереиздалиНоНеОтправилиЗаявление);
	
	Одобренные = ЗаявленияВПФРДанногоВида(
			Организация, 
			Перечисления.ВидыЗаявленийНаЭДОВПФР.НаСертификат, 
			Перечисления.СостояниеСдачиОтчетности.ПоложительныйРезультатДокументооборота, 
			НаСертификат.Отпечаток);
			
	НаСертификат.Вставить("СертификатыОтправляются", СертификатыОтправляются);
	НаСертификат.Вставить("ЕстьОдобренное", Одобренные.Количество() > 0);
	
	Состояние.Вставить("НаСертификат", НаСертификат);
	
КонецПроцедуры

Процедура ДополнитьСостояниеЭДОсПФРНеодобренным(Состояние, Организация) Экспорт
	
	Неодобренное = Новый Структура;
	
	Если ЗначениеЗаполнено(Состояние.НаПодключение.ОтправленноеЗаявление)
		И ЕщеНеОдобреноЗаявлениеНаПодключение(Состояние)
		И Состояние.НаПодключение.ОтправленноеЗаявление_ДатаОтправки > Состояние.НаСертификат.ОтправленноеЗаявление_ДатаОтправки  
		И Состояние.НаПодключение.ОтправленноеЗаявление_ДатаОтправки > Состояние.НаОтключение.ОтправленноеЗаявление_ДатаОтправки Тогда
		
	    Неодобренное.Вставить("Вид", Состояние.НаПодключение.ОтправленноеЗаявление.Вид);
		Неодобренное.Вставить("Заявление", Состояние.НаПодключение.ОтправленноеЗаявление);
		Неодобренное.Вставить("Предупредить", Истина);
		
	ИначеЕсли ЗначениеЗаполнено(Состояние.НаСертификат.ОтправленноеЗаявление)
		И ЕщеНеОдобреноЗаявлениеНаСертификат(Состояние)
		И Состояние.НаСертификат.ОтправленноеЗаявление_ДатаОтправки > Состояние.НаПодключение.ОтправленноеЗаявление_ДатаОтправки  
		И Состояние.НаСертификат.ОтправленноеЗаявление_ДатаОтправки > Состояние.НаОтключение.ОтправленноеЗаявление_ДатаОтправки Тогда
		
		Неодобренное.Вставить("Вид", Состояние.НаСертификат.ОтправленноеЗаявление.Вид);
		Неодобренное.Вставить("Заявление", Состояние.НаСертификат.ОтправленноеЗаявление);
		Неодобренное.Вставить("Предупредить", Истина);
		
	ИначеЕсли ЗначениеЗаполнено(Состояние.НаОтключение.ОтправленноеЗаявление)
		И ЕщеНеОдобреноЗаявлениеНаОтключение(Состояние)
		И Состояние.НаОтключение.ОтправленноеЗаявление_ДатаОтправки > Состояние.НаСертификат.ОтправленноеЗаявление_ДатаОтправки  
		И Состояние.НаОтключение.ОтправленноеЗаявление_ДатаОтправки > Состояние.НаПодключение.ОтправленноеЗаявление_ДатаОтправки Тогда
		
		Неодобренное.Вставить("Вид", Состояние.НаОтключение.ОтправленноеЗаявление.Вид);
		Неодобренное.Вставить("Заявление", Состояние.НаОтключение.ОтправленноеЗаявление);
		Неодобренное.Вставить("Предупредить", Истина);
		
	Иначе
		Неодобренное.Вставить("Вид", Неопределено);
		Неодобренное.Вставить("Заявление", Неопределено);
		Неодобренное.Вставить("Предупредить", Ложь);
	КонецЕсли;
	
	Состояние.Вставить("Неодобренное", Неодобренное);
	
КонецПроцедуры

// Функция - Состояние ЭДОс ПФР
//
// Параметры:
//  Организация	 -  - 
//  СДанными	 - Булево - Истина, если нужно вернуть ДвДанные сертификата (есть обращение к интернету для получения файла автонастройки).
// 
// Возвращаемое значение:
//	Состояние - Структура:
//
//	НаправлениеПодключено
//	ЗаявленияОтправляются
//
//	НаОтключение
//		ЕстьОдобренное
//		ЗаявлениеОтправлено
//		НужноОтправитьЗаявление
//		ОрганизацияОтключена
//		ОтправленноеЗаявление
//
//	НаПодключение
//		ЕстьОдобренное
//		ЗаявлениеОтправлено
//		НужноОтправитьЗаявление
//		ОрганизацияПодключена
//		ОтправленноеЗаявление
//
//	НаСертификат
//		ДвДанныеСертификата
//		ЕстьОдобренное
//		ЗаявлениеОтправлено
//		ЗаявлениеПо1СОтчетности
//		НужноОтправитьЗаявление
//		ОбновитьИзИнтернета
//		Отпечаток
//		ОтправленноеЗаявление
//		ПФРУведомленОСмене
//		УчетнаяЗапись
//
//	Неодобренное - для предупреждения о неодобренном (на подключение или сертификат)
//		Вид
//		Заявление
//		Предупредить
//
Функция СостояниеЭДОсПФР(Организация) Экспорт
	
	ЗаявленияОтправляются = ДокументооборотСКОВызовСервера.ЗаявленияПоЭДООтправляютсяВПФР();
	
	Состояние = Новый Структура();
	Состояние.Вставить("НаправлениеПодключено", СдаватьВПФР(Организация));
	Состояние.Вставить("ЗаявленияОтправляются", ЗаявленияОтправляются);
	
	ДополнитьСостояниеЭДОсПФРПодключением(Состояние, Организация, ЗаявленияОтправляются);
	ДополнитьСостояниеЭДОсПФРОтключением(Состояние, Организация, ЗаявленияОтправляются);
	ДополнитьСостояниеЭДОсПФРСертификатом(Состояние, Организация, ЗаявленияОтправляются);
	ДополнитьСостояниеЭДОсПФРНеодобренным(Состояние, Организация);
	
	Возврат Состояние;
	
КонецФункции

Функция ЗаявленияПоЭДООтправляютсяВПФР() Экспорт
	
	Возврат Истина;
	
КонецФункции

Функция СертификатыОтправляютсяВПФР() Экспорт
	
	Возврат СобытиеНаступилоПоЭДОПФР("Сертификаты отпр. в ПФР");
	
КонецФункции

Функция СобытиеНаступилоПоЭДОПФР(Имя, ЗначениеПоУмолчанию = Истина) Экспорт
	
	Актуализировать = Ложь;
	
	Результат = ОнлайнСервисыРегламентированнойОтчетностиВызовСервера.СобытиеНаступилоРасширенная(Имя, Актуализировать);
	Если Результат.СобытиеНеНайдено Тогда
		Возврат ЗначениеПоУмолчанию;
	Иначе
		Возврат Результат.СобытиеНаступило;
	КонецЕсли;
	
КонецФункции

Функция ВыгрузитьЗаявленияПоЭлДокументооборотуСПФР(Заявление) Экспорт
	
	Если Не ЗначениеЗаполнено(Заявление) Тогда
		Возврат Неопределено;
	КонецЕсли;
	
	НаПодключение = Перечисления.ВидыЗаявленийНаЭДОВПФР.НаПодключение;
	НаСертификат  = Перечисления.ВидыЗаявленийНаЭДОВПФР.НаСертификат;
	
	Если Заявление.Вид = НаСертификат Тогда
		
		АдресФайла = ПоместитьВоВременноеХранилище(Заявление.Сертификат.Получить(), Новый УникальныйИдентификатор);
		
		ДополнительныеПараметры = Новый Структура();
		ДополнительныеПараметры.Вставить("Документ", Заявление);
		ДополнительныеПараметры.Вставить("Тип",   Перечисления.ТипыФайловОтчетностиПФР.ПачкаИС); // см Распоряжение Правления ПФ РФ от 11.10.2007 N 190р (ред. от 19.03.2010)
		ДополнительныеПараметры.Вставить("Имя",   Заявление.Отпечаток + ".der");
		ДополнительныеПараметры.Вставить("Адрес", АдресФайла);
		
		Результат = ОбщегоНазначенияКлиентСервер.ЗначениеВМассиве(ДополнительныеПараметры);
		
	Иначе
	
		ЭтоЮридическоеЛицо = РегламентированнаяОтчетностьВызовСервера.ЭтоЮридическоеЛицо(Заявление.Организация);
		
		ИмяФайла = ПолучитьИмяВременногоФайла("xml");
		
		ЗаписьXML = Новый ЗаписьXML;
		ЗаписьXML.ОткрытьФайл(ИмяФайла, "UTF-8");
		ЗаписьXML.ЗаписатьОбъявлениеXML();
		
		ЗаписьXML.ЗаписатьНачалоЭлемента("ЭДПФР");
		
			Если ДействуетФорматАФ245д() Тогда
				ЗаявленияПоЭлДокументооборотуСПФР_ЗаписатьАтрибутыКорня_АФ245д(ЗаписьXML, Заявление);
			Иначе
				ЗаявленияПоЭлДокументооборотуСПФР_ЗаписатьАтрибутыКорня_АФ241д(ЗаписьXML, Заявление);
			КонецЕсли;
			
			Если Заявление.Вид = НаПодключение Тогда
				ЗаписьXML.ЗаписатьНачалоЭлемента("ЗПЭД"); // 3.1.3. Заявление на подключение к ЭДО ПФР (ЗПЭД)
			Иначе
				ЗаписьXML.ЗаписатьНачалоЭлемента("ЗОЭД"); // 3.1.4. Заявление на отключение от ЭДО ПФР (ЗОЭД)
			КонецЕсли;
			
				ЗаявленияПоЭлДокументооборотуСПФР_ЗаписатьУзелXML_ГрЮЛилиИП(ЗаписьXML, Заявление, ЭтоЮридическоеЛицо);
				
				Если Заявление.Вид = НаПодключение И НЕ ДействуетФорматАФ245д() ИЛИ ДействуетФорматАФ245д() Тогда
					ЗаявленияПоЭлДокументооборотуСПФР_ЗаписатьУзелXML_Оператор(ЗаписьXML, Заявление);
				КонецЕсли;
				ЗаявленияПоЭлДокументооборотуСПФР_ЗаписатьУзелXML_Руководитель(ЗаписьXML, Заявление);
				
			ЗаписьXML.ЗаписатьКонецЭлемента(); // ЗПЭД/ЗОЭД
			ЗаявленияПоЭлДокументооборотуСПФР_ЗаписатьУзелXML_СлужебнаяИнформация(ЗаписьXML, Заявление);
				
		ЗаписьXML.ЗаписатьКонецЭлемента(); // ЭДПФР
		
		ЗаписьXML.Закрыть();
		
		АдресФайла = ПоместитьВоВременноеХранилище(Новый ДвоичныеДанные(ИмяФайла), Новый УникальныйИдентификатор);
		
		ДополнительныеПараметры = Новый Структура();
		ДополнительныеПараметры.Вставить("Документ", Заявление);
		ДополнительныеПараметры.Вставить("Тип", Перечисления.ТипыФайловОтчетностиПФР.ПачкаИС); // см Распоряжение Правления ПФ РФ от 11.10.2007 N 190р (ред. от 19.03.2010)
		ДополнительныеПараметры.Вставить("Имя", ЗаявленияПоЭлДокументооборотуСПФР_ИмяФайла(Заявление));
		ДополнительныеПараметры.Вставить("Адрес", АдресФайла);
		
		Результат = ОбщегоНазначенияКлиентСервер.ЗначениеВМассиве(ДополнительныеПараметры);
		
	КонецЕсли;
	
	Возврат Результат;
	
КонецФункции

Процедура ИзменитьОформлениеГруппыСоглашениеСПФР(
		Организация,
		Элемент, 
		Знач СдаватьВПФР = Неопределено, 
		Знач ЭтоВторичноеЗаявление = Ложь, 
		Знач ПоддерживаетсяВторичноеЗаявление = Ложь) Экспорт
		
	Если СдаватьВПФР = Неопределено Тогда
		СдаватьВПФР = СдаватьВПФР(Организация)
	КонецЕсли;
	
	ЕстьСоглашениеСПФР = ОрганизацияПодключенаКЭДОсПФР(Организация);
	Элемент.Видимость = 
		НЕ ЕстьСоглашениеСПФР 
		И СдаватьВПФР 
		И (НЕ ЭтоВторичноеЗаявление ИЛИ ЭтоВторичноеЗаявление И ПоддерживаетсяВторичноеЗаявление);
	
КонецПроцедуры

Функция АдресДанныхФайлаАвтонастройки(УчетнаяЗапись) Экспорт
	
	Запрос = Новый Запрос;
	Запрос.Текст = 
		"ВЫБРАТЬ РАЗРЕШЕННЫЕ
		|	ФайлыСведенийСпецоператоры.Данные
		|ИЗ
		|	РегистрСведений.ФайлыСведенийСпецоператоры КАК ФайлыСведенийСпецоператоры
		|ГДЕ
		|	ФайлыСведенийСпецоператоры.УчетнаяЗапись = &УчетнаяЗапись";
	
	Запрос.УстановитьПараметр("УчетнаяЗапись", УчетнаяЗапись);
	
	РезультатЗапроса = Запрос.Выполнить();
	
	ВыборкаДетальныеЗаписи = РезультатЗапроса.Выбрать();
	
	Если ВыборкаДетальныеЗаписи.Следующий() Тогда
		Адрес = ПоместитьВоВременноеХранилище(ВыборкаДетальныеЗаписи.Данные.Получить(), Новый УникальныйИдентификатор);
	Иначе
		Адрес = Неопределено;
	КонецЕсли;
	
	Возврат Адрес;
	
КонецФункции

Функция ОтпечатокПоДвДаннымСертификата(ДвДанныеСертификата) Экспорт
	
	Если ТипЗнч(ДвДанныеСертификата) = Тип("ДвоичныеДанные") Тогда
		СертификатОбъект = Новый СертификатКриптографии(ДвДанныеСертификата);
		Отпечаток = Строка(СертификатОбъект.Отпечаток);
		Отпечаток = НРег(СтрЗаменить(Отпечаток, " ", ""));
	Иначе
		Отпечаток = "";
	КонецЕсли;
	
	Возврат Отпечаток;
	
КонецФункции

Функция ТекущийСертификатОрганизации(Организация, ОбновитьСертификатИзИнтернета) Экспорт
	
	Если ЗначениеЗаполнено(Организация)
		И ЗначениеЗаполнено(Организация.УчетнаяЗаписьОбмена) Тогда
		
		УчетнаяЗапись         = Организация.УчетнаяЗаписьОбмена;
		КонтекстЭДОСервер     = ДокументооборотСКО.ПолучитьОбработкуЭДО();
		
		Если ОбновитьСертификатИзИнтернета Тогда
			
			Оператор              = УчетнаяЗапись.СпецоператорСвязи;
			ИдентификаторАбонента = Сред(УчетнаяЗапись.ИдентификаторАбонента, 4);
			Результат             = КонтекстЭДОСервер.ПолучитьФайлАвтонастройки(Оператор, ИдентификаторАбонента);
			
			Если Результат.Выполнено Тогда
				АдресДанных = Результат.Настройки;
			Иначе
				// Если не получилось загрузить из сети, пытаемся получить из кэша
				АдресДанных = КонтекстЭДОСервер.АдресДанныхФайлаАвтонастройки(УчетнаяЗапись);
			КонецЕсли;
			
		Иначе
			
			// Для экономии скорости всегда получаем сертификат из регистра
			АдресДанных = КонтекстЭДОСервер.АдресДанныхФайлаАвтонастройки(УчетнаяЗапись);
			
		КонецЕсли;
		
		Если ЭтоАдресВременногоХранилища(АдресДанных) Тогда
			
			СертификатыИзФайлаНастройки = КонтекстЭДОСервер.ПолучитьСертификатыИзФайлаНастройки(АдресДанных);
			Для Каждого СертификатИзФайлаНастройки Из СертификатыИзФайлаНастройки Цикл
				Если Врег(СертификатИзФайлаНастройки.Хранилище) = "MY" Тогда
					ДвДанныеСертификата = СертификатИзФайлаНастройки.Сертификат;
					Возврат ДвДанныеСертификата;
				КонецЕсли;
			КонецЦикла;

		КонецЕсли;
		
	КонецЕсли;
	
	Возврат Неопределено;
	
КонецФункции

Процедура ПередЗаписьюЗаявлениеАбонентаСпецоператораСвязи(Объект, Отказ)

	Если Объект.Статус = Перечисления.СтатусыЗаявленияАбонентаСпецоператораСвязи.Одобрено
		И Объект.НастройкаЗавершена
		И (Объект.ТипЗаявления = Перечисления.ТипыЗаявленияАбонентаСпецоператораСвязи.Первичное  
		ИЛИ ЭтоЗаявлениеНаПереизданиеСертификата(Объект)) Тогда
		
		// Сохраняем сертификат для того, чтобы потом проверять, что он отправлен в ПФР
		// Не обновляем сертификат в методе ТекущийСертификатОрганизации, потому что он только что был обновлен при завершении настройки заявления.
		ДвДанныеСертификата = ТекущийСертификатОрганизации(Объект.Организация, Ложь);
		Если ДвДанныеСертификата <> Неопределено Тогда
			
			Отпечаток = ОтпечатокПоДвДаннымСертификата(ДвДанныеСертификата);
			Объект.ОтпечатокПолученногоСертификата = Отпечаток;
			УстановитьПризнакПередачиСертификатаВПФР(Отпечаток, Ложь);
			
		КонецЕсли;
		
	КонецЕсли;
	
КонецПроцедуры

Функция СертификатПереданВПФР(Отпечаток) Экспорт 
	
	УстановитьПривилегированныйРежим(Истина);
	
	Запрос = Новый Запрос;
	Запрос.Текст = 
		"ВЫБРАТЬ РАЗРЕШЕННЫЕ
		|	СертификатыПереданныеПФР.Передан КАК Передан
		|ИЗ
		|	РегистрСведений.СертификатыПереданныеПФР КАК СертификатыПереданныеПФР
		|ГДЕ
		|	СертификатыПереданныеПФР.Отпечаток = &Отпечаток";
	
	Запрос.УстановитьПараметр("Отпечаток", Отпечаток);
	
	РезультатЗапроса = Запрос.Выполнить();
	
	ВыборкаДетальныеЗаписи = РезультатЗапроса.Выбрать();
	
	Если ВыборкаДетальныеЗаписи.Следующий() Тогда
		Возврат ВыборкаДетальныеЗаписи.Передан;
	Иначе
		Возврат Ложь;
	КонецЕсли;
	
КонецФункции

Процедура УстановитьПризнакПередачиСертификатаВПФР(Отпечаток, Передан) Экспорт
	
	НачатьТранзакцию();
	
	Попытка
	
		НаборЗаписей = РегистрыСведений.СертификатыПереданныеПФР.СоздатьНаборЗаписей();
		НаборЗаписей.Отбор.Отпечаток.Установить(Отпечаток);
		НаборЗаписей.Очистить();
		
		Запись = НаборЗаписей.Добавить();
		Запись.Отпечаток = Отпечаток;
		Запись.Передан   = Передан;
			
		НаборЗаписей.Записать(Истина);
	
		ЗафиксироватьТранзакцию();
		
	Исключение
		
		ОтменитьТранзакцию();
		
		ИнформацияОбОшибке = ИнформацияОбОшибке();
		
		ЗаписьЖурналаРегистрации(
			НСтр("ru = 'Электронный документооборот с контролирующими органами. Установить признак передачи сертификата в ПФР'", ОбщегоНазначения.КодОсновногоЯзыка()), 
			УровеньЖурналаРегистрации.Ошибка,,,
			ПодробноеПредставлениеОшибки(ИнформацияОбОшибке));
			
	КонецПопытки;
	
КонецПроцедуры

Функция ОрганизацияОтключенаОтЭДКО(Организация) Экспорт 
	
	УстановитьПривилегированныйРежим(Истина);
	
	Запрос = Новый Запрос;
	Запрос.Текст = 
		"ВЫБРАТЬ РАЗРЕШЕННЫЕ
		|	СостояниеПодключенияКЭДОсПФР.ОрганизацияОтключенаОтЭДКО КАК ОрганизацияОтключена
		|ИЗ
		|	РегистрСведений.СостояниеПодключенияКЭДОсПФР КАК СостояниеПодключенияКЭДОсПФР
		|ГДЕ
		|	СостояниеПодключенияКЭДОсПФР.Организация = &Организация";
	
	Запрос.УстановитьПараметр("Организация", Организация);
	
	РезультатЗапроса = Запрос.Выполнить();
	
	ВыборкаДетальныеЗаписи = РезультатЗапроса.Выбрать();
	
	Если ВыборкаДетальныеЗаписи.Следующий() Тогда
		Возврат ВыборкаДетальныеЗаписи.ОрганизацияОтключена;
	Иначе
		Возврат Ложь;
	КонецЕсли;
	
КонецФункции

Функция ОрганизацияПодключенаКЭДОсПФР(Организация, ЭДКО = Ложь) Экспорт 
	
	УстановитьПривилегированныйРежим(Истина);
	
	Запрос = Новый Запрос;
	Запрос.Текст = 
		"ВЫБРАТЬ РАЗРЕШЕННЫЕ
		|	ВЫБОР
		|		КОГДА &ЭДКО
		|			ТОГДА СостояниеПодключенияКЭДОсПФР.ОрганизацияПодключенаКЭДКО
		|					И НЕ СостояниеПодключенияКЭДОсПФР.ОрганизацияОтключенаОтЭДКО
		|		ИНАЧЕ СостояниеПодключенияКЭДОсПФР.ОрганизацияПодключена
		|	КОНЕЦ КАК ОрганизацияПодключена
		|ИЗ
		|	РегистрСведений.СостояниеПодключенияКЭДОсПФР КАК СостояниеПодключенияКЭДОсПФР
		|ГДЕ
		|	СостояниеПодключенияКЭДОсПФР.Организация = &Организация";
	
	Запрос.УстановитьПараметр("Организация", Организация);
	Запрос.УстановитьПараметр("ЭДКО", ЭДКО);
	
	РезультатЗапроса = Запрос.Выполнить();
	
	ВыборкаДетальныеЗаписи = РезультатЗапроса.Выбрать();
	
	Если ВыборкаДетальныеЗаписи.Следующий() Тогда
		Возврат ВыборкаДетальныеЗаписи.ОрганизацияПодключена;
	Иначе
		Возврат Ложь;
	КонецЕсли;
	
КонецФункции

Процедура УстановитьСостояниеПодключенияКЭДОсПФР(
		Организация, 
		ОрганизацияПодключена = Неопределено, 
		ОрганизацияПодключенаКЭДКО = Неопределено,
		ОрганизацияОтключенаОтЭДКО = Неопределено) Экспорт
	
	НачатьТранзакцию();
	
	Попытка
	
		НаборЗаписей = РегистрыСведений.СостояниеПодключенияКЭДОсПФР.СоздатьНаборЗаписей();
		НаборЗаписей.Отбор.Организация.Установить(Организация);
		НаборЗаписей.Очистить();
		
		Запись = НаборЗаписей.Добавить();
		Запись.Организация = Организация;
		
		Если ОрганизацияПодключена = Неопределено Тогда
			Запись.ОрганизацияПодключена = ОрганизацияПодключенаКЭДОсПФР(Организация);
		Иначе
			Запись.ОрганизацияПодключена = ОрганизацияПодключена;
		КонецЕсли;
		
		Если ОрганизацияПодключенаКЭДКО = Неопределено Тогда
			Запись.ОрганизацияПодключенаКЭДКО = ОрганизацияПодключенаКЭДОсПФР(Организация, Истина);
		Иначе
			Запись.ОрганизацияПодключенаКЭДКО = ОрганизацияПодключенаКЭДКО;
		КонецЕсли;
		
		Если ОрганизацияОтключенаОтЭДКО = Неопределено Тогда
			Запись.ОрганизацияОтключенаОтЭДКО = ОрганизацияОтключенаОтЭДКО(Организация);
		Иначе
			Запись.ОрганизацияОтключенаОтЭДКО = ОрганизацияОтключенаОтЭДКО;
		КонецЕсли;
			
		НаборЗаписей.Записать(Истина);
	
		ЗафиксироватьТранзакцию();
		
	Исключение
		
		ОтменитьТранзакцию();
		
		ИнформацияОбОшибке = ИнформацияОбОшибке();
		
		ЗаписьЖурналаРегистрации(
			НСтр("ru = 'Электронный документооборот с контролирующими органами. Установить состояние подключения к ПФР'", ОбщегоНазначения.КодОсновногоЯзыка()), 
			УровеньЖурналаРегистрации.Ошибка,,,
			ПодробноеПредставлениеОшибки(ИнформацияОбОшибке));
			
	КонецПопытки;
	
КонецПроцедуры

// Обновление ИБ по всем организациям
Процедура ПроставитьПризнакЗаключенногоСоглашенияСПФР() Экспорт
	
	Запрос = Новый Запрос;
	Запрос.Текст = 
		"ВЫБРАТЬ РАЗЛИЧНЫЕ
		|	ЖурналОтчетовСтатусы.Организация КАК Организация
		|ИЗ
		|	РегистрСведений.ЖурналОтчетовСтатусы КАК ЖурналОтчетовСтатусы
		|		ЛЕВОЕ СОЕДИНЕНИЕ РегистрСведений.СостояниеПодключенияКЭДОсПФР КАК СостояниеПодключенияКЭДОсПФР
		|		ПО ЖурналОтчетовСтатусы.Организация = СостояниеПодключенияКЭДОсПФР.Организация
		|ГДЕ
		|	ЖурналОтчетовСтатусы.ВидКонтролирующегоОргана = ЗНАЧЕНИЕ(Перечисление.ТипыКонтролирующихОрганов.ПФР)
		|	И ЖурналОтчетовСтатусы.Статус = ""Сдано""
		|	И ЖурналОтчетовСтатусы.СостояниеСдачиОтчетности = ЗНАЧЕНИЕ(Перечисление.СостояниеСдачиОтчетности.ПоложительныйРезультатДокументооборота)
		|	И ЕСТЬNULL(СостояниеПодключенияКЭДОсПФР.ОрганизацияПодключена, ЛОЖЬ) = ЛОЖЬ";
	
	РезультатЗапроса = Запрос.Выполнить();
	
	ВыборкаДетальныеЗаписи = РезультатЗапроса.Выбрать();
	
	Пока ВыборкаДетальныеЗаписи.Следующий() Цикл
		УстановитьСостояниеПодключенияКЭДОсПФР(ВыборкаДетальныеЗаписи.Организация, Истина);
	КонецЦикла;
	
КонецПроцедуры

Функция СдаватьВПФР(Организация) Экспорт

	СдаватьВПФР = Ложь;
	
	Если ЗначениеЗаполнено(Организация) Тогда
		
		УчетнаяЗаписьОбмена = Организация.УчетнаяЗаписьОбмена;
		
		Если ЗначениеЗаполнено(УчетнаяЗаписьОбмена) Тогда
			
			Если УчетнаяЗаписьОбмена.СпецоператорСвязи = Перечисления.СпецоператорыСвязи.КалугаАстрал Тогда
				КонтекстЭДОСервер = ДокументооборотСКО.ПолучитьОбработкуЭДО();
														   
				Запрос = Новый Запрос;
				Запрос.Текст = 
					"ВЫБРАТЬ РАЗРЕШЕННЫЕ
					|	ДополнительныеРеквизитыУчетнойЗаписиПолучатели.ТипПолучателя,
					|	ДополнительныеРеквизитыУчетнойЗаписиПолучатели.КодПолучателя,
					|	ДополнительныеРеквизитыУчетнойЗаписиПолучатели.КПП
					|ИЗ
					|	РегистрСведений.ДополнительныеРеквизитыУчетнойЗаписиПолучатели КАК ДополнительныеРеквизитыУчетнойЗаписиПолучатели
					|ГДЕ
					|	ДополнительныеРеквизитыУчетнойЗаписиПолучатели.УчетнаяЗапись = &УчетнаяЗапись";

				Запрос.УстановитьПараметр("УчетнаяЗапись", КонтекстЭДОСервер.УчетнаяЗаписьОрганизации(Организация));
				Результат = Запрос.Выполнить().Выгрузить();
				
				Для Каждого СтрокаНаправлений Из Результат Цикл
					Если СтрокаНаправлений.ТипПолучателя = Перечисления.ТипыКонтролирующихОрганов.ПФР Тогда
						СдаватьВПФР = Истина;
					КонецЕсли;
				КонецЦикла;
			Иначе
				ПредназначенаДляДокументооборотаСПФР = ДокументооборотСКОВызовСервера.УчетнаяЗаписьПредназначенаДляДокументооборотаСПФР(УчетнаяЗаписьОбмена);
				СдаватьВПФР = ПредназначенаДляДокументооборотаСПФР = Истина;
			КонецЕсли;
			
		КонецЕсли;
		
	КонецЕсли;
	
	Возврат СдаватьВПФР;

КонецФункции

Процедура ПриЗаписиЗаявленияПоЭлДокументооборотуСПФР(Объект, Отказ) Экспорт
	
	ДополнительныеПараметры = Новый Структура();
	ДополнительныеПараметры.Вставить("ПолучатьДаты", 			Ложь);
	ДополнительныеПараметры.Вставить("ПолучатьОшибкиОтправки", 	Ложь);
	
	ТекущееСостояние = ТекущееСостояниеОтправки(
		Объект.Ссылка, 
		"ПФР", 
		ДополнительныеПараметры);
		
	Если ТекущееСостояние = Неопределено Тогда
		Возврат;
	КонецЕсли;
	
	ТекущийЭтапОтправки = ТекущееСостояние.ТекущийЭтапОтправки;
	
	Если ТекущийЭтапОтправки = Неопределено Тогда
		Возврат;
	КонецЕсли;
	
	СостояниеСдачиОтчетности = ТекущийЭтапОтправки.СостояниеСдачиОтчетности;
	Успешно = СостояниеСдачиОтчетности = Перечисления.СостояниеСдачиОтчетности.ПоложительныйРезультатДокументооборота;
	
	// Нельзя сбрасывать статус в Ложь, потому что он мог быть установлен вручную.
	// Поэтому делаем сохранение только про успешной отправке
	Если Успешно Тогда
	
		Если Объект.Вид = Перечисления.ВидыЗаявленийНаЭДОВПФР.НаПодключение Тогда
			УстановитьСостояниеПодключенияКЭДОсПФР(Объект.Организация,,Истина, Ложь);
			// 1. Если заявление в ПФР отправлено, это означает, что они уже получили этот сертификат, 
			// поэтому записываем его в регистр как отправленный.
			// 2. В заявлении отпечаток заполняется только для вида НаСертификат, поэтому получаем отпечаток из учетки.
			Отпечаток = Объект.Организация.УчетнаяЗаписьОбмена.СертификатРуководителя;
			УстановитьПризнакПередачиСертификатаВПФР(Отпечаток, Истина);
		ИначеЕсли Объект.Вид = Перечисления.ВидыЗаявленийНаЭДОВПФР.НаОтключение Тогда
			УстановитьСостояниеПодключенияКЭДОсПФР(Объект.Организация,,,Истина);
		ИначеЕсли Объект.Вид = Перечисления.ВидыЗаявленийНаЭДОВПФР.НаСертификат Тогда
			УстановитьПризнакПередачиСертификатаВПФР(Объект.Отпечаток, Истина);
		КонецЕсли;
		
	КонецЕсли;
	
КонецПроцедуры

Функция ЭтоЗаявлениеНаПереизданиеСертификата(ДокументЗаявление) Экспорт
	
	Если ДокументЗаявление.ТипЗаявления <> Перечисления.ТипыЗаявленияАбонентаСпецоператораСвязи.Изменение Тогда
		Возврат Ложь;
	КонецЕсли;
	
	Отбор = Новый Структура();
	Отбор.Вставить("ИзмененныйРеквизит", Перечисления.ПараметрыПодключенияК1СОтчетности.ПереизданиеСертификата);
	
	НайденныеСтроки = ДокументЗаявление.ИзменившиесяРеквизитыВторичногоЗаявления.Выгрузить().НайтиСтроки(Отбор);
		
	Возврат НайденныеСтроки.Количество() > 0;
	
КонецФункции

Функция ЕщеНеОдобреноЗаявлениеНаПодключение(Состояние) Экспорт
	
	Возврат НЕ Состояние.НаПодключение.ОрганизацияПодключена
		И Состояние.НаПодключение.ЗаявлениеОтправлено;
		
КонецФункции
	
Функция ЕщеНеОдобреноЗаявлениеНаОтключение(Состояние) Экспорт
	
	Возврат Состояние.НаОтключение.ЗаявлениеОтправлено;
		
КонецФункции

Функция ЕщеНеОдобреноЗаявлениеНаСертификат(Состояние) Экспорт
	
	Возврат Состояние.НаПодключение.ОрганизацияПодключена
		И Состояние.НаСертификат.ЗаявлениеОтправлено
		И НЕ Состояние.НаСертификат.ПФРУведомленОСмене;
		
КонецФункции

Процедура СоздатьЗаявленияВПФРЕслиНеСозданы(Форма) Экспорт
	
	Если Форма.ПредупредитьЧтоЕщеНеОдобрено Тогда
		
		Если Форма.СостояниеЭДОсПФР.Неодобренное.Вид = Форма.НаПодключение Тогда
			Форма.ЗаявлениеВПФР = Форма.СостояниеЭДОсПФР.Неодобренное.Заявление;
		ИначеЕсли Форма.СостояниеЭДОсПФР.Неодобренное.Вид = Форма.НаСертификат Тогда
			Форма.СертификатВПФР = Форма.СостояниеЭДОсПФР.Неодобренное.Заявление;
		КонецЕсли;
		
	Иначе
		
		Если Форма.ЭтоИзначальноНаПодключениеИлиОтключение И НЕ ЗначениеЗаполнено(Форма.ЗаявлениеВПФР) Тогда
			
			Форма.ЗаявлениеВПФР = СоздатьЗаявлениеВПФР(
				Форма.ИзначальныйВидЗаявленияПФР,
				Форма.Организация, 
				Форма.СостояниеЭДОсПФР, 
				Форма.ТекущееЗаявлениеПо1СОтчетности);
				
		КонецЕсли;
		
		ЗаявлениеНаПодключениеИлиОтключениеУжеЕсть = НЕ Форма.ЭтоИзначальноНаПодключениеИлиОтключение 
			ИЛИ Форма.ЭтоИзначальноНаПодключениеИлиОтключение И Форма.ФлагЗаявлениеУжеПредоставленоВПФР;
			
		Если НЕ ЗначениеЗаполнено(Форма.СертификатВПФР) И ЗаявлениеНаПодключениеИлиОтключениеУжеЕсть Тогда
			
			Форма.СертификатВПФР = СоздатьЗаявлениеВПФР(
				Форма.НаСертификат,
				Форма.Организация, 
				Форма.СостояниеЭДОсПФР, 
				Форма.ТекущееЗаявлениеПо1СОтчетности);
				
		КонецЕсли;
		
	КонецЕсли;
	
КонецПроцедуры

Функция СоздатьЗаявлениеВПФР( 
		Вид, 
		Организация, 
		СостояниеЭДОсПФР = Неопределено, 
		Основание = Неопределено) Экспорт
		
	НаПодключение = Перечисления.ВидыЗаявленийНаЭДОВПФР.НаПодключение;
	НаОтключение  = Перечисления.ВидыЗаявленийНаЭДОВПФР.НаОтключение;
	НаСертификат  = Перечисления.ВидыЗаявленийНаЭДОВПФР.НаСертификат;
		
	Если ТипЗнч(Вид) = Тип("Строка") Тогда
		
		Если Вид = Строка(НаПодключение) Тогда
			Вид = НаПодключение;
		ИначеЕсли Вид = Строка(НаОтключение) Тогда
			Вид = НаОтключение;
		ИначеЕсли Вид = Строка(НаСертификат) Тогда
			Вид = НаСертификат;
		КонецЕсли;

	КонецЕсли;
		
	ДополнительныеПараметры = Новый Структура();
	ДополнительныеПараметры.Вставить("Организация", Организация);
	ДополнительныеПараметры.Вставить("Вид", Вид);
	
	Если Вид = НаСертификат И СостояниеЭДОсПФР <> Неопределено Тогда
		ДополнительныеПараметры.Вставить("Отпечаток",           СостояниеЭДОсПФР.НаСертификат.Отпечаток);
		ДополнительныеПараметры.Вставить("ДвДанныеСертификата", СостояниеЭДОсПФР.НаСертификат.ДвДанныеСертификата);
	КонецЕсли;
	
	ДокументОбъект = Документы.ЗаявленияПоЭлДокументооборотуСПФР.СоздатьДокумент();
	ДокументОбъект.Заполнить(ДополнительныеПараметры);
	Если ЗначениеЗаполнено(Основание) Тогда
		ДокументОбъект.Основание = Основание;
	КонецЕсли;

	ДокументОбъект.Записать();
	
	Возврат ДокументОбъект.Ссылка;
	
КонецФункции

Процедура СохранитьНастройкиСостоянияЭДОСПФР(Форма) Экспорт

	// Сертификат
	Если Форма.ФлагСертификатУжеОтправленВПФР Тогда
		УстановитьПризнакПередачиСертификатаВПФР(Форма.СостояниеЭДОсПФР.НаСертификат.Отпечаток, Истина);
	КонецЕсли;
	
	// Заявление
	Если Форма.ФлагЗаявлениеУжеПредоставленоВПФР Тогда
		
		Если Форма.ИзначальныйВидЗаявленияПФР = Форма.НаПодключение Тогда
			
			УстановитьСостояниеПодключенияКЭДОсПФР(
				Форма.Организация, 
				,
				Истина);
				
		ИначеЕсли Форма.ИзначальныйВидЗаявленияПФР = Форма.НаОтключение Тогда
			
			УстановитьСостояниеПодключенияКЭДОсПФР(
				Форма.Организация, 
				,
				,
				Истина);
				
		КонецЕсли;
				
	КонецЕсли;

КонецПроцедуры

Процедура ИнициализироватьВидыЗаявленийНаЭДОВПФР(Форма) Экспорт
	
	Форма.НаПодключение = Перечисления.ВидыЗаявленийНаЭДОВПФР.НаПодключение;
	Форма.НаОтключение  = Перечисления.ВидыЗаявленийНаЭДОВПФР.НаОтключение;
	Форма.НаСертификат  = Перечисления.ВидыЗаявленийНаЭДОВПФР.НаСертификат;

КонецПроцедуры

Процедура ИнициализироватьПараметрыЭДОСПФР(Форма, ПолучатьСостояниеЭДОсПФР = Истина) Экспорт // Вызывается из всех кейсов ПФР
	
	Если ПолучатьСостояниеЭДОсПФР И Форма.СостояниеЭДОсПФР = Неопределено Тогда
		Форма.СостояниеЭДОсПФР = СостояниеЭДОсПФР(Форма.Организация);
	КонецЕсли;

	ИнициализироватьВидыЗаявленийНаЭДОВПФР(Форма);
	
	Форма.ЭтоИзначальноНаПодключениеИлиОтключение = 
		Форма.ИзначальныйВидЗаявленияПФР = Форма.НаПодключение
		ИЛИ Форма.ИзначальныйВидЗаявленияПФР = Форма.НаОтключение;
		
	ПредупредитьЧтоЕщеНеОдобреноИзНастроек = Форма.ЭтоПредупреждениеИзНастроек;
	ПредупредитьЧтоЕщеНеОдобреноИзОтправки = Форма.ЭтоПредупреждениеИзОтправки
		И Форма.СостояниеЭДОсПФР.Неодобренное.Предупредить
		И Форма.СостояниеЭДОсПФР.Неодобренное.Вид <> ПредопределенноеЗначение("Перечисление.ВидыЗаявленийНаЭДОВПФР.НаОтключение");
		
	Форма.ПредупредитьЧтоЕщеНеОдобрено = 
		Форма.СостояниеЭДОсПФР.Неодобренное.Предупредить
		И НЕ Форма.ЭтоПредупреждениеИзМастера
		И (ПредупредитьЧтоЕщеНеОдобреноИзНастроек ИЛИ ПредупредитьЧтоЕщеНеОдобреноИзОтправки);
		
	Если Форма.ЭтоПредупреждениеИзНастроек 
		ИЛИ Форма.ЭтоПредупреждениеИзМастера И Форма.ЭтоПредупреждениеИзПанели Тогда
		Форма.ЗаголовокКнопкиПоЗавершению = НСтр("ru = 'Сохранить и закрыть'");
	ИначеЕсли Форма.ЭтоПредупреждениеИзОтправки Тогда
		Название = ДлительнаяОтправкаКлиентСервер.НазваниеОбъектаВИменительномПадеже(Форма.Отчет);
		Форма.ЗаголовокКнопкиПоЗавершению = НСтр("ru = 'Продолжить отправку '") + Название;
	ИначеЕсли Форма.ЭтоПредупреждениеИзМастера Тогда
		Форма.ЗаголовокКнопкиПоЗавершению = НСтр("ru = 'Завершить настройку'");
	КонецЕсли;
	
КонецПроцедуры
	
Функция ЗаявлениеВПФРЗаполненоКорректно(ОтправляемоеЗаявление) Экспорт
	
	Корректно = ОтправляемоеЗаявление.ПолучитьОбъект().ПроверитьЗаполнение();
	Если НЕ Корректно Тогда
		ОбщегоНазначения.СообщитьПользователю(НСтр("ru = 'Зайдите в заявление по ссылке и исправьте найденные ошибки'"), ОтправляемоеЗаявление);
	КонецЕсли;
	
	Возврат Корректно;
	
КонецФункции

Процедура ОпределитьОтправляемоеЗаявлениеВПФР(Форма) Экспорт
	
	Если Форма.ПредупредитьЧтоЕщеНеОдобрено Тогда
		Форма.ОтправляемоеЗаявлениеПФР = Форма.СостояниеЭДОсПФР.Неодобренное.Заявление;
	ИначеЕсли Форма.ЭтоИзначальноНаПодключениеИлиОтключение И НЕ Форма.ФлагЗаявлениеУжеПредоставленоВПФР Тогда
		Форма.ОтправляемоеЗаявлениеПФР = Форма.ЗаявлениеВПФР;
	Иначе
		Форма.ОтправляемоеЗаявлениеПФР = Форма.СертификатВПФР;
	КонецЕсли;
	
	Форма.ВидОтправляемогоЗаявления = Форма.ОтправляемоеЗаявлениеПФР.Вид;
	
КонецПроцедуры

Процедура ЗаявленияПоЭлДокументооборотуСПФР_ЗаписатьУзелXML_ФИО(ЗаписьXML, Заявление) 
	
	ЗаписьXML.ЗаписатьНачалоЭлемента("УТ2:ФИО");
	
		ЗаписатьУзелXML(ЗаписьXML, "УТ2:Фамилия", СокрЛП(Заявление.Фамилия));
		ЗаписатьУзелXML(ЗаписьXML, "УТ2:Имя", СокрЛП(Заявление.Имя));
		ЗаписатьУзелXML(ЗаписьXML, "УТ2:Отчество", СокрЛП(Заявление.Отчество), Истина);
		
	ЗаписьXML.ЗаписатьКонецЭлемента();
	
КонецПроцедуры

Процедура ЗаявленияПоЭлДокументооборотуСПФР_ЗаписатьУзелXML_КонтактнаяИнформация(ЗаписьXML, Заявление) 
	
	Телефон = ЭлектронныйДокументооборотСКонтролирующимиОрганамиКлиентСервер.ТелефонМобильныйБезРазделителей(Заявление.Телефон);
	
	ЗаписатьУзелXML(ЗаписьXML, "УТ2:Телефон", СокрЛП(Телефон), Истина);
	ЗаписатьУзелXML(ЗаписьXML, "УТ2:АдресЭлПочты", СокрЛП(Заявление.ЭлектроннаяПочта), Истина);
	
КонецПроцедуры

Процедура ЗаявленияПоЭлДокументооборотуСПФР_ЗаписатьУзелXML_Адрес(ЗаписьXML, Заявление, ИмяУзла, ИмяРеквизита) 
	
	ЗаписьXML.ЗаписатьНачалоЭлемента(ИмяУзла);
	
		ЗначениеАдреса = Заявление[ИмяРеквизита];
		ПредставлениеАдреса = Документы.ЗаявленияПоЭлДокументооборотуСПФР.ПредставлениеАдреса(ЗначениеАдреса);
		ЗаписатьУзелXML(ЗаписьXML, "УТ2:Неструктурированный", СокрЛП(ПредставлениеАдреса), Истина);
		
	ЗаписьXML.ЗаписатьКонецЭлемента();

КонецПроцедуры

Процедура ЗаявленияПоЭлДокументооборотуСПФР_ЗаписатьУзелXML_Адреса(ЗаписьXML, Заявление, ЭтоЮридическоеЛицо) 
	
	ЗаявленияПоЭлДокументооборотуСПФР_ЗаписатьУзелXML_Адрес(ЗаписьXML, Заявление, "УТ2:АдресРегистрации", "АдресРегистрации");
	Если ЭтоЮридическоеЛицо Тогда
		ЗаявленияПоЭлДокументооборотуСПФР_ЗаписатьУзелXML_Адрес(ЗаписьXML, Заявление, "УТ2:АдресФактический", "АдресФактический");
	КонецЕсли;
	
КонецПроцедуры

Процедура ЗаявленияПоЭлДокументооборотуСПФР_ЗаписатьУзелXML_Оператор(ЗаписьXML, Заявление) 
	
	ЗаписьXML.ЗаписатьНачалоЭлемента("Оператор");
	
		ЗаписатьУзелXML(ЗаписьXML, "УТ2:РегНомер", Заявление.ОператорРегНомерПФР);  
		Если НЕ ДействуетФорматАФ245д() Тогда
			ЗаписатьУзелXML(ЗаписьXML, "УТ2:Наименование", Заявление.ОператорНаименованиеПолное);
		КонецЕсли;
		ЗаписатьУзелXML(ЗаписьXML, "УТ2:НаименованиеКраткое", Заявление.ОператорНаименованиеКраткое);
		ЗаписатьУзелXML(ЗаписьXML, "УТ2:ИНН", Заявление.ОператорИНН);
		ЗаписатьУзелXML(ЗаписьXML, "УТ2:КПП", Заявление.ОператорКПП);
	
	ЗаписьXML.ЗаписатьКонецЭлемента();
	
КонецПроцедуры

Процедура ЗаявленияПоЭлДокументооборотуСПФР_ЗаписатьУзелXML_СлужебнаяИнформация(ЗаписьXML, Заявление) 
	
	ЗаписьXML.ЗаписатьНачалоЭлемента("СлужебнаяИнформация");
	
		Если ДействуетФорматАФ245д() Тогда
			ЗаписатьУзелXML(ЗаписьXML, "АФ5:GUID", Заявление.УникальныйИдентификатор());
			ЗаписатьУзелXML(ЗаписьXML, "АФ5:ДатаВремя",  XMLСтрока(ТекущаяДатаСеанса()));
		Иначе
			ЗаписатьУзелXML(ЗаписьXML, "АФ4:GUID", Заявление.УникальныйИдентификатор());
			ЗаписатьУзелXML(ЗаписьXML, "АФ4:ДатаВремя",  XMLСтрока(ТекущаяДатаСеанса()));
		КонецЕсли;
		
	ЗаписьXML.ЗаписатьКонецЭлемента();
	
КонецПроцедуры

Функция ЗаявленияПоЭлДокументооборотуСПФР_ИмяФайла(Заявление)
	
	//Структура имени файла, должна быть следующей:
	//ПФР_[рег.номер]_[код ТО ПФР]_[код документа]_[дата формирования файла]_[GUID].xml
	//где:
	//•	[рег.номер] - номер под которым организация зарегистрирована как плательщик страховых взносов, с указанием кодов региона и района по классификации, принятой в ПФР;
	//•	[код ТО ПФР] - код территориального органа ПФР, принимающего/направляющего сведения, содержащиеся в файле (указывается согласно классификатору территориальных органов ПФР);
	//•	[код документа] – код формы документов, содержащихся в файле;
	//•	[дата формирования файла] – дата в формате ГГГГММДД;
	//•	[GUID] – глобальный уникальный идентификатор, обеспечивающий уникальность электронного документа. Значение GUID должно быть идентично значению GUID документа, содержащегося в файле. Реализация спецификации стандарта http://www.ietf.org/rfc/rfc4122.txt.
	
	РегНомер = СокрЛП(Заявление.РегНомерПФР);
	КодТОПФР = СтрЗаменить(СокрЛП(Заявление.Получатель.Код), "-", "");
	Если Заявление.Вид = Перечисления.ВидыЗаявленийНаЭДОВПФР.НаПодключение Тогда
		КодДокумента = "ЗПЭД"; // 3.1.3. Заявление на подключение к ЭДО ПФР (ЗПЭД)
	Иначе
		КодДокумента = "ЗОЭД"; // 3.1.4. Заявление на отключение от ЭДО ПФР (ЗОЭД)
	КонецЕсли;
	ДатаФормированияФайла = Формат(ТекущаяДатаСеанса(), "ДФ=ггггММдд");
	
	GUID = Заявление.УникальныйИдентификатор();
	
	ИмяФайла = "ПФР_%1_%2_%3_%4_%5.xml";
	ИмяФайла = СтрШаблон(ИмяФайла, РегНомер, КодТОПФР, КодДокумента, ДатаФормированияФайла, GUID);

	Возврат ИмяФайла;
	
КонецФункции

Процедура ЗаявленияПоЭлДокументооборотуСПФР_ЗаписатьУзелXML_Руководитель(ЗаписьXML, Заявление)
	
	Если ДействуетФорматАФ245д() Тогда
		ЗаписьXML.ЗаписатьНачалоЭлемента("ПредставительСотрудник");
	Иначе
		ЗаписьXML.ЗаписатьНачалоЭлемента("Руководитель");
	КонецЕсли;
	
		ЗаявленияПоЭлДокументооборотуСПФР_ЗаписатьУзелXML_ФИО(ЗаписьXML, Заявление);
		ЗаписатьУзелXML(ЗаписьXML, "УТ2:Должность", СокрЛП(Заявление.Должность));
	ЗаписьXML.ЗаписатьКонецЭлемента(); // Руководитель

КонецПроцедуры
	
Процедура ЗаявленияПоЭлДокументооборотуСПФР_ЗаписатьУзелXML_ГрЮЛилиИП(ЗаписьXML, Заявление, ЭтоЮридическоеЛицо)
	
	ЗаписьXML.ЗаписатьНачалоЭлемента("Страхователь");
	
	Если ЭтоЮридическоеЛицо Тогда
		
		Если ДействуетФорматАФ245д() Тогда
			ЗаписьXML.ЗаписатьНачалоЭлемента("ЮЛ");
		Иначе
			ЗаписьXML.ЗаписатьНачалоЭлемента("ЭДО:ЮЛ");
		КонецЕсли;
		
			ЗаписатьУзелXML(ЗаписьXML, "УТ2:РегНомер", СокрЛП(Заявление.РегНомерПФР));
			ЗаписатьУзелXML(ЗаписьXML, "УТ2:Наименование", СокрЛП(Заявление.НаименованиеПолное));
			ЗаписатьУзелXML(ЗаписьXML, "УТ2:НаименованиеКраткое", СокрЛП(Заявление.НаименованиеКраткое));
			ЗаписатьУзелXML(ЗаписьXML, "УТ2:ИНН", СокрЛП(Заявление.ИНН));
			ЗаписатьУзелXML(ЗаписьXML, "УТ2:КПП", СокрЛП(Заявление.КПП));
			
	Иначе
		
		Если ДействуетФорматАФ245д() Тогда
			ЗаписьXML.ЗаписатьНачалоЭлемента("ИП");
		Иначе
			ЗаписьXML.ЗаписатьНачалоЭлемента("ЭДО:ИП");
		КонецЕсли;
		
			ЗаписатьУзелXML(ЗаписьXML, "УТ2:РегНомер", СокрЛП(Заявление.РегНомерПФР));
			ЗаявленияПоЭлДокументооборотуСПФР_ЗаписатьУзелXML_ФИО(ЗаписьXML, Заявление);
			ЗаписатьУзелXML(ЗаписьXML, "УТ2:СНИЛС", СокрЛП(Заявление.СНИЛС));
			ЗаписатьУзелXML(ЗаписьXML, "УТ2:ИНН", СокрЛП(Заявление.ИНН));
			
	КонецЕсли;
		
			Если НЕ ДействуетФорматАФ245д() Тогда
				ЗаявленияПоЭлДокументооборотуСПФР_ЗаписатьУзелXML_Адреса(ЗаписьXML, Заявление, ЭтоЮридическоеЛицо);
			КонецЕсли;
			
			ЗаявленияПоЭлДокументооборотуСПФР_ЗаписатьУзелXML_КонтактнаяИнформация(ЗаписьXML, Заявление);

		ЗаписьXML.ЗаписатьКонецЭлемента(); // ЭДО:ЮЛ или ЭДО:ИП
		
	ЗаписьXML.ЗаписатьКонецЭлемента(); // Страхователь
	
КонецПроцедуры

Процедура ЗаявленияПоЭлДокументооборотуСПФР_ЗаписатьАтрибутыКорня_АФ241д(ЗаписьXML, Заявление)
	
	Если Заявление.Вид = Перечисления.ВидыЗаявленийНаЭДОВПФР.НаПодключение Тогда
		ЗаписьXML.ЗаписатьАтрибут("xmlns", "http://пф.рф/ЗПЭД/2018-11-26");
	Иначе
		ЗаписьXML.ЗаписатьАтрибут("xmlns", "http://пф.рф/ЗОЭД/2018-11-26");
	КонецЕсли;
	
	ЗаписьXML.ЗаписатьАтрибут("xmlns:УТ2", "http://пф.рф/УТ/2017-08-21");
	ЗаписьXML.ЗаписатьАтрибут("xmlns:ЭДО", "http://пф.рф/ВС/ЭДО/типы/2019-09-27");
	ЗаписьXML.ЗаписатьАтрибут("xmlns:АФ4", "http://пф.рф/АФ/2017-08-21");
		
КонецПроцедуры

Процедура ЗаявленияПоЭлДокументооборотуСПФР_ЗаписатьАтрибутыКорня_АФ245д(ЗаписьXML, Заявление)
	
	Если Заявление.Вид = Перечисления.ВидыЗаявленийНаЭДОВПФР.НаПодключение Тогда
		ЗаписьXML.ЗаписатьАтрибут("xmlns", "http://пф.рф/ЗПЭД/2020-02-11");
	Иначе
		ЗаписьXML.ЗаписатьАтрибут("xmlns", "http://пф.рф/ЗОЭД/2020-02-04");
	КонецЕсли;
	
	ЗаписьXML.ЗаписатьАтрибут("xmlns:УТ2", "http://пф.рф/УТ/2017-08-21");
	ЗаписьXML.ЗаписатьАтрибут("xmlns:ЭДО", "http://пф.рф/ВС/ЭДО/типы/2019-09-27");
	ЗаписьXML.ЗаписатьАтрибут("xmlns:АФ5", "http://пф.рф/АФ/2018-12-07");
		
КонецПроцедуры

Процедура ЗаписатьУзелXML(данныеXML, ИмяУзла, ЗначениеУзла, НеВыгружатьПустые = Ложь)
	
	Если НеВыгружатьПустые И ПустаяСтрока(ЗначениеУзла) Тогда 
		
	Иначе
		данныеXML.ЗаписатьНачалоЭлемента(ИмяУзла);
		данныеXML.ЗаписатьТекст(Строка(ЗначениеУзла));
		данныеXML.ЗаписатьКонецЭлемента();
	КонецЕсли;
	
КонецПроцедуры

Функция КомментарийКСостояниюНеОтправленоПФР(СсылкаНаОтчет) Экспорт 
	
	Если ТипЗнч(СсылкаНаОтчет) = Тип("ДокументСсылка.ЗаявленияПоЭлДокументооборотуСПФР") Тогда
		УчетнаяЗапись = УчетнаяЗаписьОрганизации(СсылкаНаОтчет.Организация);
		Если ЗначениеЗаполнено(УчетнаяЗапись) ИЛИ НЕ ЗначениеЗаполнено(СсылкаНаОтчет.Организация) Тогда
			Возврат "";
		Иначе
			Возврат ПредложениеПодключить1СОтчетностьИзПанели();
		КонецЕсли;
	Иначе
		Возврат КомментарийКСостояниюНеОтправлено();
	КонецЕсли;
	
КонецФункции

Функция ОтчетОтправляетсяПоЭДОК(Ссылка) Экспорт
	
	Если ТипЗнч(Ссылка) = Тип("СправочникСсылка.ЦиклыОбмена") Тогда
		Возврат Ссылка.ФорматДокументооборота = Перечисления.ФорматыДокументооборотаСПФР.ЭДОК;
	Иначе
		Сведения = СведенияПоОтправляемымОбъектам(Ссылка);
		Если Сведения.ВидКонтролирующегоОргана = Перечисления.ТипыКонтролирующихОрганов.ПФР Тогда
			Возврат ФорматДокументооборотаСПФР(Ссылка).ФорматДокументооборота = Перечисления.ФорматыДокументооборотаСПФР.ЭДОК;
		Иначе
			Возврат Ложь;
		КонецЕсли;
	КонецЕсли;
	
КонецФункции

Функция СвойстваПодписиСообщенияПоЭДОК(СтрокаСвойстваПодписи)
	
	// записываем вложение во временный файл
	ФайлСообщения = ПолучитьИмяВременногоФайла("xml");
	Попытка
		СтрокаСвойстваПодписи.ДвДанные.Записать(ФайлСообщения);
	Исключение
		Возврат Неопределено;
	КонецПопытки;
	
	// считываем уведомление из файла в дерево XML
	ОписаниеОшибки = "";
	КонтекстЭДО = ДокументооборотСКО.ПолучитьОбработкуЭДО();
	ДеревоXML = КонтекстЭДО.ЗагрузитьXMLВДеревоЗначений(ФайлСообщения, , ОписаниеОшибки);
	ОперацииСФайламиЭДКО.УдалитьВременныйФайл(ФайлСообщения);
	Если НЕ ЗначениеЗаполнено(ДеревоXML) Тогда
		Возврат Неопределено;
	КонецЕсли;
	
	// анализируем XML
	УзелX509Certificate = ДеревоXML.Строки.Найти("X509Certificate", "Имя", Истина);
	Если НЕ ЗначениеЗаполнено(УзелX509Certificate) Тогда
		Возврат Неопределено;
	КонецЕсли;

	ПодписьВBase64 = УзелX509Certificate.Значение;
	ДвДанныеСертификата = Base64Значение(ПодписьВBase64);
	
	Сертификат = Новый Структура;
	Сертификат.Вставить("Сертификат", ДвДанныеСертификата);
	
	Подписант = Новый Структура;
	Подписант.Вставить("Сертификат", Сертификат);
	
	Подписанты = Новый Массив;
	Подписанты.Добавить(Подписант);
	
	СвойстваПодписи = Новый Структура();
	СвойстваПодписи.Вставить("Подписанты", Подписанты);
	
	Возврат СвойстваПодписи;
	
КонецФункции

Функция ДействуетФорматАФ245д() Экспорт
	
	Возврат Истина;
	
КонецФункции

#КонецОбласти

Процедура ОпределениеИзмененныхПараметровПодключенияВФоне(ПолныеСвойстваВсехСертификатов, АдресРезультата) Экспорт
	
	Результат = ИзменившиесяПараметрыПодключения(ПолныеСвойстваВсехСертификатов);
	ПоместитьВоВременноеХранилище(Результат, АдресРезультата);
	
КонецПроцедуры
#Область СУЦ

Функция ЭлектроннаяПочтаВведенаКорректно(ЭлектроннаяПочта, ТипЗаявления, ВыводитьСообщения = Истина, Поле = "ЭлектроннаяПочта") Экспорт
	
	ВведенКорректно = Истина;
	
	// Для избежания ошибки "Ошибка формата. Электронный адрес содержит недопустимое значение"
	ЭлектроннаяПочта = СокрЛП(ЭлектроннаяПочта);
	ПочтовыйАдрес = СокрЛП(ЭлектроннаяПочта);
	Если СтрДлина(ПочтовыйАдрес) = 0 Тогда
		
		ВведенКорректно = Ложь;
		
		Если ВыводитьСообщения Тогда
			Если ТипЗаявления = ПредопределенноеЗначение("Перечисление.ТипыЗаявленияАбонентаСпецоператораСвязи.Первичное") Тогда
				ОбщегоНазначения.СообщитьПользователю(НСтр("ru = 'Заполните поле ""Электронная почта""'"), , Поле);
			Иначе
				ОбщегоНазначения.СообщитьПользователю(НСтр("ru = 'Заполните электронную почту'"), , Поле);
			КонецЕсли;
		КонецЕсли;
			
	ИначеЕсли СтрДлина(ПочтовыйАдрес) > 0 Тогда
		
		Если НЕ ОбщегоНазначенияКлиентСервер.АдресЭлектроннойПочтыСоответствуетТребованиям(ПочтовыйАдрес) Тогда
			
			ВведенКорректно = Ложь;
			
			Если ВыводитьСообщения Тогда
				Если НЕ СтрНайти(ПочтовыйАдрес, "@") Тогда
					ОбщегоНазначения.СообщитьПользователю(
						НСтр("ru = 'Некорректно указана электронная почта. Отсутствует символ @'"), ,Поле);
				Иначе 
					ОбщегоНазначения.СообщитьПользователю(
						НСтр("ru = 'Электронная почта содержит некорректные сочетания символов'"), ,Поле);
				КонецЕсли;
			КонецЕсли;
			
		КонецЕсли;
	КонецЕсли;
	
	Возврат ВведенКорректно; 
	
КонецФункции

Функция НаименованиеСубъектаРФПоКодуСубъекта(КодРегиона) Экспорт
	
	Результат = "";
	
	КонтекстЭДОСервер = ДокументооборотСКО.ПолучитьОбработкуЭДО();
	МакетКодыРегионов = КонтекстЭДОСервер.ПолучитьМакет("ФСРАРПорталыРегионов");
	
	НомерКолонкиКодРегиона = МакетКодыРегионов.Область("КодРегиона").Лево;
	НомерКолонкиНаименованиеРегиона = МакетКодыРегионов.Область("НаименованиеРегиона").Лево;
	ОбластьПоискаКодаРегиона = МакетКодыРегионов.Область(1, НомерКолонкиКодРегиона, МакетКодыРегионов.ВысотаТаблицы, НомерКолонкиКодРегиона);
	
	ОбластьСКодомРегиона = МакетКодыРегионов.НайтиТекст(Формат(Число(КодРегиона), "ЧН=0; ЧГ=; ЧЦ=2; ЧВН=;"), , ОбластьПоискаКодаРегиона, , Истина);
	
	Если ОбластьСКодомРегиона <> Неопределено Тогда
		Результат = МакетКодыРегионов.Область(ОбластьСКодомРегиона.Верх, НомерКолонкиНаименованиеРегиона, ОбластьСКодомРегиона.Верх, НомерКолонкиНаименованиеРегиона).Текст;
	КонецЕсли;
	
	Возврат Результат;
	
КонецФункции

Процедура ОчиститьВключаемыйСертификат(Форма) Экспорт
	
	Форма.ВключаемыйСертификат = Неопределено;
	Форма.ВключаемыйСертификатОблачный = Ложь;
	Форма.ИдентификаторПолученияСпискаОблачныхСертификатов = "";
	Форма.ОблачныеСертификатыКалуги.Очистить();
	
КонецПроцедуры

Процедура ЗаполнитьИспользованиеДолговременногоТокена(Форма) ЭКспорт
	
	НовоеЗначение = Форма.СпособПодтвержденияКриптоопераций;
	
	Если Форма.ЭтоУчетнаяЗаписьВМоделиСервиса И Форма.ПереиздатьСертификат <> 1 И Не Форма.ЭтоПереходВКоробку Тогда
		ТекущийСертификат 	= ОбщегоНазначения.ЗначениеРеквизитаОбъекта(Форма.УчетнаяЗапись, "СертификатРуководителя");
		СвойствоСертификата	= ЭлектроннаяПодписьВМоделиСервиса.СвойстваРасшифрованияПодписанияСертификата(ТекущийСертификат);
		НовоеЗначение 		= СвойствоСертификата.СпособПодтвержденияКриптоопераций;
	ИначеЕсли Форма.ЭтоПереходВОблако Тогда
		НовоеЗначение = Перечисления.СпособыПодтвержденияКриптоопераций.СессионныйТокен;
	КонецЕсли;
	
	Если Не ЗначениеЗаполнено(НовоеЗначение) Тогда
		НовоеЗначение = Перечисления.СпособыПодтвержденияКриптоопераций.СессионныйТокен;
	КонецЕсли;
	
	Форма.СпособПодтвержденияКриптоопераций 	= НовоеЗначение;
	Форма.ПроверенДоступДляТокена				= Ложь;
	
КонецПроцедуры

Функция ПроверитьНаправления(Форма, МастерДалее = Истина, ВыводитьСообщения = Истина) Экспорт
	
	УказаныКорректно = Истина;
	НаправленияУказаныКорректно(Форма, УказаныКорректно, Ложь);
	
	Если УказаныКорректно Тогда
		Возврат Истина;
	Иначе
		
		МастерДалее = Ложь;
		
		Если ВыводитьСообщения Тогда
			ОбщегоНазначения.СообщитьПользователю(НСтр("ru = 'Уточните направления сдачи отчетности'"), ,"Форма.УказательНаправлений");
		КонецЕсли;
		
		Возврат Ложь;
		
	КонецЕсли;

КонецФункции

Процедура ИзменитьОформлениеНаправлений(Форма) Экспорт
	
	Элементы    = Форма.Элементы;
	КрасныйЦвет = Новый Цвет(178,34, 34);
	
	Направления = ИнициализироватьТаблицуОрганов(Форма);
	
	Если НеВыбранНиОдинОрган(Направления) ИЛИ НЕ ПроверитьНаправления(Форма, , Ложь) Тогда
		
		// Ни один орган не выбран или не доступен
		Элементы.УказатьНаправления.Заголовок  = НСтр("ru = 'Уточнить'");
		Элементы.УказатьНаправления.ЦветТекста = КрасныйЦвет;
		
	ИначеЕсли ВыбраныВсеНаправления(Направления) И НЕ ВыбранТолькоОдинОрган(Направления) Тогда
		
		// Выбраны все органы
		Элементы.УказатьНаправления.Заголовок  = НСтр("ru = 'Все направления'");
		Элементы.УказатьНаправления.ЦветТекста = Новый Цвет();
		
	Иначе
		
		// Выбрана часть органов
		Элементы.УказатьНаправления.Заголовок  = ПредставлениеОрганов(Направления);
		Элементы.УказатьНаправления.ЦветТекста = Новый Цвет();
		
	КонецЕсли;
	
	Форма.ИзменитьОформлениеСоглашенияСПФР();
	
КонецПроцедуры

Функция КоличествоВыбраныхОрганов(Направления, Подключить = Неопределено, ПризнакПоддержки = Неопределено)
	
	Отбор = Новый Структура();
	
	Если Подключить <> Неопределено Тогда
		Отбор.Вставить("Подключить", Подключить);
	КонецЕсли;
	
	Если ПризнакПоддержки <> Неопределено Тогда
		Отбор.Вставить("ПризнакПоддержки", ПризнакПоддержки);
	КонецЕсли;
	
	НайденныеСтроки = Направления.НайтиСтроки(Отбор);
	
	Возврат НайденныеСтроки.Количество();
	
КонецФункции

Функция НеВыбранНиОдинОрган(Направления)
	
	Возврат КоличествоВыбраныхОрганов(Направления, Истина, Истина) = 0;
	
КонецФункции

Функция ВыбранТолькоОдинОрган(Направления)
	
	Возврат КоличествоВыбраныхОрганов(Направления, Истина, Истина) = 1;
	
КонецФункции

Функция ВыбраныВсеНаправления(Направления)
	
	Поддерживаемые = КоличествоВыбраныхОрганов(Направления, ,Истина);
	Подключенные   = КоличествоВыбраныхОрганов(Направления, Истина,);
	ПодключенныеИПоддерживаемые = КоличествоВыбраныхОрганов(Направления, Истина, Истина);
	
	Возврат Поддерживаемые = Подключенные И Подключенные = ПодключенныеИПоддерживаемые;
	
КонецФункции

Функция ПредставлениеОрганов(Направления)
	
	// Выбрана часть органов
	Органы = Новый Массив;
	Для каждого Направление Из Направления Цикл
		
		Если Направление.Подключить И Направление.ПризнакПоддержки Тогда
		
			ПредставлениеОргана = Направление.Орган;
			
			Если Направление.Детализация <> Неопределено И Направление.Детализация.Количество() > 1 Тогда
				ПредставлениеОргана = ПредставлениеОргана + "(" + Строка(Направление.Детализация.Количество()) + ")";
			КонецЕсли;
			
			Органы.Добавить(ПредставлениеОргана);
			
		КонецЕсли;
	
	КонецЦикла; 
	
	ПредставлениеОрганов 	= СтрСоединить(Органы, ", ");
	ПозицияПоследнейЗапятой = СтрНайти(ПредставлениеОрганов, ",", НаправлениеПоиска.СКонца);
	
	ЛеваяЧасть  = СокрЛП(Лев(ПредставлениеОрганов, ПозицияПоследнейЗапятой - 1));
	ПраваяЧасть = СокрЛП(Сред(ПредставлениеОрганов, ПозицияПоследнейЗапятой + 1));
	
	// Добавляем "и" перед последним словом
	Если ЛеваяЧасть = "" Тогда
		ПредставлениеОрганов = ПраваяЧасть;
	Иначе
		ПредставлениеОрганов = ЛеваяЧасть + " и " + ПраваяЧасть;
	КонецЕсли;

	Возврат ПредставлениеОрганов;
	
КонецФункции

Процедура ОбработатьВыбранныеНаправления(Форма, Результат) Экспорт
	
	// ФНС
	Форма.СдаватьВФНС 			= Результат.СдаватьВФНС;
	ТаблицаПолучателейФНС 	= ПолучитьИзВременногоХранилища(Результат.АдресПолучателейФНС);
	Форма.ЗначениеВРеквизитФормы(ТаблицаПолучателейФНС, "ПолучателиФНС");
		
	// ФСГС
	Форма.СдаватьВРосстат 		= Результат.СдаватьВРосстат;
	ТаблицаПолучателейФСГС 	= ПолучитьИзВременногоХранилища(Результат.АдресПолучателейФСГС);
	Форма.ЗначениеВРеквизитФормы(ТаблицаПолучателейФСГС, "ПолучателиФСГС");
	
	// ФПР
	Форма.СдаватьВПФР = Результат.СдаватьВПФР;
	Форма.РегНомерПФР = Результат.РегНомерПФР;
	Форма.КодПФР		= Результат.КодПФР;
	
	// ФСС
	Форма.СдаватьВФСС = Результат.СдаватьВФСС;
	
	// ФСРАР
	Форма.КодРегионаФСРАР 					= Результат.КодРегионаФСРАР;
	Форма.ПодатьЗаявкуНаСертификатДляФСРАР 	= Результат.ПодатьЗаявкуНаСертификатДляФСРАР;
	
	// Остальные органы
	Форма.ПодатьЗаявкуНаПодключениеРПН = Результат.ПодатьЗаявкуНаПодключениеРПН;
	Форма.ПодатьЗаявкуНаПодключениеФТС = Результат.ПодатьЗаявкуНаПодключениеФТС;
	
	ИзменитьОформлениеНаправлений(Форма);
	
КонецПроцедуры

Функция ИнициализироватьТаблицуОрганов(Форма) Экспорт
	
	Направления = Новый ТаблицаЗначений;
	Направления.Колонки.Добавить("Орган");
	Направления.Колонки.Добавить("Подключить");
	Направления.Колонки.Добавить("ПризнакПоддержки");
	Направления.Колонки.Добавить("Детализация");
	
	ДобавитьСтрокуНаправления(Направления, НСтр("ru = 'ФНС'"), Форма.СдаватьВФНС, Форма.ПризнакПоддержкиФНС, Форма.ПолучателиФНС);
	ДобавитьСтрокуНаправления(Направления, НСтр("ru = 'ПФР'"), Форма.СдаватьВПФР, Форма.ПризнакПоддержкиПФР);
	ДобавитьСтрокуНаправления(Направления, НСтр("ru = 'Росстат'"), Форма.СдаватьВРосстат, Форма.ПризнакПоддержкиРосстат, Форма.ПолучателиФСГС);
	ДобавитьСтрокуНаправления(Направления, НСтр("ru = 'ФСС'"), Форма.СдаватьВФСС, Форма.ПризнакПоддержкиФСС);
	ДобавитьСтрокуНаправления(Направления, НСтр("ru = 'ФСРАР'"), Форма.ПодатьЗаявкуНаСертификатДляФСРАР, Форма.ПризнакПоддержкиФСРАР);
	ДобавитьСтрокуНаправления(Направления, НСтр("ru = 'РПН'"), Форма.ПодатьЗаявкуНаПодключениеРПН, Форма.ПризнакПоддержкиРПН);
	ДобавитьСтрокуНаправления(Направления, НСтр("ru = 'ФТС'"), Форма.ПодатьЗаявкуНаПодключениеФТС, Форма.ПризнакПоддержкиФТС);
	
	Возврат Направления;
	
КонецФункции

Процедура ДобавитьСтрокуНаправления(Направления, Орган, Подключить, ПризнакПоддержки, Детализация = Неопределено)
	
	НоваяСтрока = Направления.Добавить();
	НоваяСтрока.Орган = Орган;
	НоваяСтрока.Подключить = Подключить;
	НоваяСтрока.ПризнакПоддержки = ПризнакПоддержки;
	НоваяСтрока.Детализация = Детализация;
	
КонецПроцедуры

Функция НоваяСтрокаТаблицыОрганов()
	
	ДополнительныеПараметры = Новый Структура();
	ДополнительныеПараметры.Вставить("Орган", 		Перечисления.ТипыКонтролирующихОрганов.ПустаяСсылка());
	ДополнительныеПараметры.Вставить("Доступен", 	Истина);
	ДополнительныеПараметры.Вставить("Подключить", 	Истина);
	
	Возврат ДополнительныеПараметры;
	
КонецФункции

Процедура ОпределитьПодключаемыеНаправленияСдачиОтчетности(Форма, ЭтоСкопированоеЗаявление = Ложь) Экспорт
	
	Направления = Новый Массив;
	
	// Признаки ПризнакПоддержкиФСРАР, ПризнакПоддержкиРПН, ПризнакПоддержкиФТС 
	// определяются настройками спецоператора в процедуре ИнициализироватьПараметрыУЦ
	
	// ФНС
	НоваяСтрока 			= НоваяСтрокаТаблицыОрганов();
	НоваяСтрока.Орган 		= Перечисления.ТипыКонтролирующихОрганов.ФНС;
	Направления.Добавить(НоваяСтрока);
	
	// ПФР
	НоваяСтрока 			= НоваяСтрокаТаблицыОрганов();
	НоваяСтрока.Орган 		= Перечисления.ТипыКонтролирующихОрганов.ПФР;
	Направления.Добавить(НоваяСтрока);
	
	// Росстат
	НоваяСтрока 			= НоваяСтрокаТаблицыОрганов();
	НоваяСтрока.Орган 		= Перечисления.ТипыКонтролирующихОрганов.ФСГС;
	Направления.Добавить(НоваяСтрока);
	
	// ФСС
	НоваяСтрока 			= НоваяСтрокаТаблицыОрганов();
	НоваяСтрока.Орган 		= Перечисления.ТипыКонтролирующихОрганов.ФСС;
	Направления.Добавить(НоваяСтрока);
	
	// ФСРАР
	Если Форма.ПризнакПоддержкиФСРАР Тогда
		// Если оператор не поддерживает орган, то не даем переопределять это признак
		НоваяСтрока 		= НоваяСтрокаТаблицыОрганов();
		НоваяСтрока.Орган 	= Перечисления.ТипыКонтролирующихОрганов.ФСРАР;
		Направления.Добавить(НоваяСтрока);
	КонецЕсли;
	
	// РПН
	Если Форма.ПризнакПоддержкиРПН Тогда
		// Если оператор не поддерживает орган, то не даем переопределять это признак
		НоваяСтрока 		= НоваяСтрокаТаблицыОрганов();
		НоваяСтрока.Орган 	= Перечисления.ТипыКонтролирующихОрганов.РПН;
		Направления.Добавить(НоваяСтрока);
	КонецЕсли;

	// ФТС
	Если Форма.ПризнакПоддержкиФТС Тогда
		// Если оператор не поддерживает орган, то не даем переопределять это признак
		НоваяСтрока 		= НоваяСтрокаТаблицыОрганов();
		НоваяСтрока.Орган 	= Перечисления.ТипыКонтролирующихОрганов.ФТС;
		Направления.Добавить(НоваяСтрока);
	КонецЕсли;
	
	ЭлектронныйДокументооборотСКонтролирующимиОрганамиПереопределяемый.ОпределитьПодключаемыеНаправленияСдачиОтчетности(Форма.Организация, Направления);
	
	// ФСРАР
	Если НЕ Форма.ПризнакПоддержкиФСРАР Тогда
		НоваяСтрока 			= НоваяСтрокаТаблицыОрганов();
		НоваяСтрока.Орган 		= Перечисления.ТипыКонтролирующихОрганов.ФСРАР;
		НоваяСтрока.Доступен 	= Ложь;
		НоваяСтрока.Подключить 	= Ложь;
		Направления.Добавить(НоваяСтрока);
	КонецЕсли;
	
	// РПН
	Если НЕ Форма.ПризнакПоддержкиРПН Тогда
		НоваяСтрока 			= НоваяСтрокаТаблицыОрганов();
		НоваяСтрока.Орган 		= Перечисления.ТипыКонтролирующихОрганов.РПН;
		НоваяСтрока.Доступен 	= Ложь;
		НоваяСтрока.Подключить 	= Ложь;
		Направления.Добавить(НоваяСтрока);
	КонецЕсли;

	// ФТС
	Если НЕ Форма.ПризнакПоддержкиФТС Тогда
		НоваяСтрока 			= НоваяСтрокаТаблицыОрганов();
		НоваяСтрока.Орган 		= Перечисления.ТипыКонтролирующихОрганов.ФТС;
		НоваяСтрока.Доступен 	= Ложь;
		НоваяСтрока.Подключить 	= Ложь;
		Направления.Добавить(НоваяСтрока);
	КонецЕсли;
	
	Для каждого Направление Из Направления Цикл
		
		Если Направление.Орган = Перечисления.ТипыКонтролирующихОрганов.ФНС Тогда
			
			Форма.ПризнакПоддержкиФНС = Направление.Доступен;
			
			Если ЭтоСкопированоеЗаявление Тогда
				Форма.СдаватьВФНС = Форма.СдаватьВФНС И Форма.ПризнакПоддержкиФНС;
			Иначе
				Форма.СдаватьВФНС = Направление.Подключить И Форма.ПризнакПоддержкиФНС;
			КонецЕсли;
			
		ИначеЕсли Направление.Орган = Перечисления.ТипыКонтролирующихОрганов.ПФР Тогда
			
			Форма.ПризнакПоддержкиПФР = Направление.Доступен;
			
			Если ЭтоСкопированоеЗаявление Тогда
				Форма.СдаватьВПФР = Форма.СдаватьВПФР И Форма.ПризнакПоддержкиПФР;
			Иначе
				Форма.СдаватьВПФР = Направление.Подключить И Форма.ПризнакПоддержкиПФР;
			КонецЕсли;
			
		ИначеЕсли Направление.Орган = Перечисления.ТипыКонтролирующихОрганов.ФСГС Тогда
			
			Форма.ПризнакПоддержкиРосстат = Направление.Доступен;
			
			Если ЭтоСкопированоеЗаявление Тогда
				Форма.СдаватьВРосстат = Форма.СдаватьВРосстат И Форма.ПризнакПоддержкиРосстат;
			Иначе
				Форма.СдаватьВРосстат = Направление.Подключить И Форма.ПризнакПоддержкиРосстат;
			КонецЕсли;
			
		ИначеЕсли Направление.Орган = Перечисления.ТипыКонтролирующихОрганов.ФСС Тогда
			
			Форма.ПризнакПоддержкиФСС = Направление.Доступен;
			
			Если ЭтоСкопированоеЗаявление Тогда
				Форма.СдаватьВФСС = Форма.СдаватьВФСС И Форма.ПризнакПоддержкиФСС;
			Иначе
				Форма.СдаватьВФСС = Направление.Подключить И Форма.ПризнакПоддержкиФСС;
			КонецЕсли;
			
		ИначеЕсли Направление.Орган = Перечисления.ТипыКонтролирующихОрганов.ФСРАР Тогда
			
			Форма.ПризнакПоддержкиФСРАР = Направление.Доступен;
			
			Если ЭтоСкопированоеЗаявление Тогда
				Форма.ПодатьЗаявкуНаСертификатДляФСРАР = Форма.ПодатьЗаявкуНаСертификатДляФСРАР И Форма.ПризнакПоддержкиФСРАР;
			Иначе
				Форма.ПодатьЗаявкуНаСертификатДляФСРАР = Направление.Подключить И Форма.ПризнакПоддержкиФСРАР;
			КонецЕсли;
			
		ИначеЕсли Направление.Орган = Перечисления.ТипыКонтролирующихОрганов.РПН Тогда
			
			Форма.ПризнакПоддержкиРПН = Направление.Доступен;
			
			Если ЭтоСкопированоеЗаявление Тогда
				Форма.ПодатьЗаявкуНаПодключениеРПН = Форма.ПодатьЗаявкуНаПодключениеРПН И Форма.ПризнакПоддержкиРПН;
			Иначе
				Форма.ПодатьЗаявкуНаПодключениеРПН = Направление.Подключить И Форма.ПризнакПоддержкиРПН;
			КонецЕсли;
			
		ИначеЕсли Направление.Орган = Перечисления.ТипыКонтролирующихОрганов.ФТС Тогда
			
			Форма.ПризнакПоддержкиФТС = Направление.Доступен;
			
			Если ЭтоСкопированоеЗаявление Тогда
				Форма.ПодатьЗаявкуНаПодключениеФТС = Форма.ПодатьЗаявкуНаПодключениеФТС И Форма.ПризнакПоддержкиФТС;
			Иначе
				Форма.ПодатьЗаявкуНаПодключениеФТС = Направление.Подключить И Форма.ПризнакПоддержкиФТС;
			КонецЕсли;
			
		КонецЕсли;
		
	КонецЦикла;
	
КонецПроцедуры

Процедура ИнициализироватьПараметрыУЦ(Форма) Экспорт
	
	Если НЕ ЗначениеЗаполнено(Форма.Спецоператор) Тогда
		Спецоператоры = ПолучитьСписокСпецоператоровСвязи();
		Форма.Спецоператор  = Спецоператоры[0];
	КонецЕсли;
	
	НачатьОпределениеДанныхУЦ(Форма);
	ПараметрыСпецоператора = ПолучитьПараметрыОператораСвязи(Форма.Спецоператор);
	
	Форма.ПризнакПоддержкиФСРАР = ПараметрыСпецоператора.ФСРАРПризнак = Истина 
		ИЛИ ПараметрыСпецоператора.ФСРАРПризнак = "Истина";
	
	Форма.ПризнакПоддержкиРПН = ПараметрыСпецоператора.РПНПризнак = Истина 
		ИЛИ ПараметрыСпецоператора.РПНПризнак = "Истина";
	
	Форма.ПризнакПоддержкиФТС = ПараметрыСпецоператора.ФТСПризнак = Истина 
		ИЛИ ПараметрыСпецоператора.ФТСПризнак = "Истина";
	
	Форма.ОператорПоддерживаетСМСУведомление = ПараметрыСпецоператора.ПризнакПоддержкиСМС = Истина
		ИЛИ ПараметрыСпецоператора.ПризнакПоддержкиСМС = "Истина";
	
КонецПроцедуры

Процедура НачатьОпределениеДанныхУЦ(Форма)

	Если Форма.ЗапретитьИзменение Тогда
		Возврат;
	КонецЕсли;
	
	Форма.АдресЗаданияПоПолучениюДанныхССервераКА = ПоместитьВоВременноеХранилище(Неопределено, Форма.УникальныйИдентификатор);
	
	ДополнительныеПараметры = Новый Структура();
	ДополнительныеПараметры.Вставить("Спецоператор", 	Форма.Спецоператор);
	ДополнительныеПараметры.Вставить("АдресХранилища",  Форма.АдресЗаданияПоПолучениюДанныхССервераКА);
	
	ПолучитьДанныеССервераКАФоновоеЗадание(ДополнительныеПараметры); 

КонецПроцедуры

Процедура ПроверитьВключаемыйСертификат(Форма, МастерДалее) Экспорт
	
	Если ОбработкаЗаявленийАбонентаКлиентСервер.ИспользоватьСуществующий(Форма) И Форма.ПереиздатьСертификат Тогда
		
		Если Форма.ВключаемыйСертификат = Неопределено Тогда
		
			МастерДалее = Ложь;
			ТекстОшибки = НСтр("ru = 'Выберите сертификат, либо переключитесь в режим получения нового сертификата'");
			ОбщегоНазначения.СообщитьПользователю(ТекстОшибки,,"Форма.УказательВключаемыйСертификат");
			
		ИначеЕсли Форма.ВключаемыйСертификатОблачный И НЕ Форма.ДоступнаЭлектроннаяПодписьВМоделиСервиса Тогда
		
			МастерДалее = Ложь;
			ТекстОшибки = НСтр("ru = 'Выбран облачный сертификат, но электронная подпись в модели сервиса недоступна. Выберите локальный сертификат, либо переключитесь в режим получения нового сертификата'");
			ОбщегоНазначения.СообщитьПользователю(ТекстОшибки,,"Форма.УказательВключаемыйСертификат");
			
		КонецЕсли;
		
	КонецЕсли;
		
КонецПроцедуры

Процедура УдалитьПрисоединенныеФайлы(НовыйДокументЗаявление)
	
	МассивУдаляемыхФайлов = Новый Массив;
	РаботаСФайлами.ЗаполнитьПрисоединенныеФайлыКОбъекту(НовыйДокументЗаявление.Ссылка, МассивУдаляемыхФайлов);
	
	УстановитьПривилегированныйРежим(Истина);
	
	Для каждого ПрисоединенныйФайлСсылка Из МассивУдаляемыхФайлов Цикл
		ПрисоединенныйФайлОбъект = ПрисоединенныйФайлСсылка.ПолучитьОбъект();
		ПрисоединенныйФайлОбъект.Удалить()
	КонецЦикла;
	
КонецПроцедуры

Процедура СохранитьДокументыЗаявления(Форма, НовыйДокументЗаявление) Экспорт
	
	НовыйДокументЗаявление.Записать();
	НовыйДокументЗаявление.ЭлектронныеДокументы.Очистить();
	УдалитьПрисоединенныеФайлы(НовыйДокументЗаявление);
	
	НомераФайлов = Новый Соответствие;
	
	Для Каждого ЭлементСписка Из Форма.ФайлыДокументов Цикл
		
		Документ = ЭлементСписка.Значение.Документ;
		ВидыДокументов = ОбработкаЗаявленийАбонентаКлиентСервер.ВидыДокументовПоФорме(Форма);
		
		// Не сохраняем документы, которые не видны.
		// Такая ситуация может возникнуть при смене способа подписания или при неудачной отправке заявления
		Если НЕ ВидыДокументов.Свойство(Документ) ИЛИ НЕ ВидыДокументов[Документ].Видимость Тогда
			Продолжить;
		КонецЕсли;
		
		ПараметрыФайла = Новый Структура;
		ПараметрыФайла.Вставить("Автор", Пользователи.ТекущийПользователь());
		ПараметрыФайла.Вставить("ВладелецФайлов", НовыйДокументЗаявление.Ссылка);
		
		Файл = Новый Файл(ЭлементСписка.Значение.ИсходноеИмя);
		
		Представление 	 = ВидыДокументов[Документ].Представление;
		
		ИмяБезРасширения = ИмяПрисоединенногоФайла(Документ, Представление, НомераФайлов);
		
		ПараметрыФайла.Вставить("ИмяБезРасширения", 			ИмяБезРасширения);
		ПараметрыФайла.Вставить("РасширениеБезТочки", 			СтрЗаменить(Файл.Расширение, ".", ""));
		ПараметрыФайла.Вставить("ВремяИзмененияУниверсальное", 	ТекущаяУниверсальнаяДата());
		ПараметрыФайла.Вставить("Автор", 						Пользователи.ТекущийПользователь());
		
		ПрисоединенныйФайл = РаботаСФайлами.ДобавитьФайл(ПараметрыФайла, ЭлементСписка.Значение.Адрес);
		
		НоваяСтрока = НовыйДокументЗаявление.ЭлектронныеДокументы.Добавить();
		НоваяСтрока.Документ 	= ЭлементСписка.Значение.Документ;
		НоваяСтрока.Файл 		= ПрисоединенныйФайл;
		НоваяСтрока.Размер 		= ЭлементСписка.Значение.Размер;
		
	КонецЦикла;
	
	Если ЗначениеЗаполнено(НовыйДокументЗаявление.ЭлектронныеДокументы) Тогда
		НовыйДокументЗаявление.Записать();
	КонецЕсли;
		
КонецПроцедуры

Функция ИмяПрисоединенногоФайла(Документ, Представление, НомераФайлов)

	Если НомераФайлов.Получить(Документ) = Неопределено Тогда
		НомераФайлов.Вставить(Документ, 1);
	Иначе
		НомераФайлов[Документ] = НомераФайлов[Документ] + 1;
	КонецЕсли;
	
	Если НомераФайлов[Документ] = 1 Тогда
		ИмяФайла = Представление;
	Иначе
		ИмяФайла = СтрШаблон("%1 - %2", Представление, Формат(НомераФайлов[Документ], "ЧВН=; ЧГ="));
	КонецЕсли;
	
	Возврат ИмяФайла;

КонецФункции

Процедура ЗаполнитьДокументыИзСкопированногоЗаявления(Форма) Экспорт
	
	ВидыДокументов = ОбработкаЗаявленийАбонентаКлиентСервер.ВидыДокументовПоФорме(Форма);
	
	Для Каждого ЭлектронныйДокумент Из Форма.Реквизит.ЭлектронныеДокументы Цикл
		
		// Среди присоединенных документов могут еще быть файлы архивов, текст заявления, подписи
		ЭтоСкан = ВидыДокументов.Свойство(ЭлектронныйДокумент.Документ);
		Если НЕ ЭтоСкан Тогда
			Продолжить;
		КонецЕсли;
		
		ДополнительныеПараметры = РаботаСФайламиКлиентСервер.ПараметрыДанныхФайла();
		ДополнительныеПараметры.ИдентификаторФормы 				= Форма.УникальныйИдентификатор;
		ДополнительныеПараметры.ПолучатьСсылкуНаДвоичныеДанные 	= Истина;
		
		ДанныеФайла = РаботаСФайлами.ДанныеФайла(ЭлектронныйДокумент.Файл, ДополнительныеПараметры);
		
		ОписаниеФайла = Новый Структура;
		ОписаниеФайла.Вставить("Размер", 		ДанныеФайла.Размер);
		ОписаниеФайла.Вставить("Адрес", 		ДанныеФайла.СсылкаНаДвоичныеДанныеФайла);
		ОписаниеФайла.Вставить("ИсходноеИмя", 	ДанныеФайла.ИмяФайла);
		ОписаниеФайла.Вставить("Документ", 		ЭлектронныйДокумент.Документ);
		
		Форма.ФайлыДокументов.Добавить(ОписаниеФайла);
		
	КонецЦикла;
	
КонецПроцедуры

Процедура УстановитьНастройкиРежимаОграниченнойФункциональности(Форма) Экспорт

	Форма.ЭтоРежимОграниченнойФункциональности = ЭлектронныйДокументооборотСКонтролирующимиОрганами.ЭтоРежимОграниченнойФункциональности();
	Форма.ЭтоРежимБесплатнойНулевойОтчетности  = ЭлектронныйДокументооборотСКонтролирующимиОрганами.ЭтоРежимБесплатнойНулевойОтчетности();
	
	// Для тестирования нулевки:
	//Форма.ЭтоРежимОграниченнойФункциональности = Истина;
	//Форма.ЭтоРежимБесплатнойНулевойОтчетности  = Истина;
	
	Если Форма.ЭтоРежимБесплатнойНулевойОтчетности Тогда
		Форма.СсылкаПерейдитеНаПлатныйТариф = ЭлектронныйДокументооборотСКонтролирующимиОрганами.СсылкаПерейдитеНаПлатныйТариф();
		// Для тестирования нулевки:
		// СсылкаПерейдитеНаПлатныйТариф = "https://1cnul.ru/tarif/";
	Иначе
		Форма.СсылкаПерейдитеНаПлатныйТариф = "";
	КонецЕсли;
	
КонецПроцедуры

Функция ЭтоСертификатУЦКалугиПоИмениУЦ(ИмяУЦСертификта) Экспорт
	
	ИменаУЦКалуги = ЭлектронныйДокументооборотСКонтролирующимиОрганамиВызовСервера.ИменаУЦКалуги();
	ИменаУЦ       = СтроковыеФункцииКлиентСервер.РазложитьСтрокуВМассивПодстрок(ИменаУЦКалуги, Символы.ПС);
	
	ЭтоСертификатКалуги = Ложь;
	Для каждого ИмяУЦ Из ИменаУЦ Цикл
		Если ИмяУЦ = ИмяУЦСертификта Тогда
			ЭтоСертификатКалуги = Истина;
			Прервать;
		КонецЕсли;
	КонецЦикла;  

	Возврат ЭтоСертификатКалуги;
	
КонецФункции

Функция НастройкиУчетнойЗаписиОрганизации(Организация) Экспорт
	
	УчетнаяЗапись = УчетнаяЗаписьОрганизации(Организация);
	Если УчетнаяЗапись = Неопределено ИЛИ УчетнаяЗапись = Справочники.УчетныеЗаписиДокументооборота.ПустаяСсылка() Тогда
		Возврат Неопределено;
	КонецЕсли;
	
	Возврат ПолучитьНастройкиУчетнойЗаписиИзИБ(УчетнаяЗапись);
	
КонецФункции

#КонецОбласти

#Область ЭДО

Функция ДокументЭДОпоНДС() Экспорт
	
	Отбор = Новый Структура();
	Отбор.Вставить("ЭтоНДС", Истина);
	
	Виды = ВидовПредставляемыхДокументовПоОтбору(Отбор);
	
	Возврат Виды;
	
КонецФункции

Функция ДокументЭДОСЭД() Экспорт
	
	Отбор = Новый Структура();
	Отбор.Вставить("ЕстьЭД", Истина);
	
	Виды = ВидовПредставляемыхДокументовПоОтбору(Отбор);
	
	Возврат Виды;
	
КонецФункции

Функция ВидыДокументовЭДОСПодтверждением() Экспорт

	Отбор = Новый Структура();
	Отбор.Вставить("ОбязательноПодтверждение", Истина);
	
	Виды = ВидовПредставляемыхДокументовПоОтбору(Отбор);
	
	Возврат Виды;
		
КонецФункции

Функция ВидовПредставляемыхДокументовПоОтбору(Отбор) Экспорт

	Характеристики = ВидыПредставляемыхДокументов();
	
	НайденныеСтроки = Характеристики.НайтиСтроки(Отбор);
	Виды = Характеристики.Скопировать(НайденныеСтроки);
	Виды = Виды.ВыгрузитьКолонку("Значение");
	
	Возврат Виды;
		
КонецФункции

#Область ФормированиеТаблицыВидов

Функция ВидыПредставляемыхДокументов() Экспорт

	Макет = ПолучитьМакет("ВидыПредставляемыхДокументов");
	КолонкиБулево = КолонкиДляПреобразованияВБулево();
	
	Виды = ДанныеМакетаЧерезПостроительЗапроса(
		Макет, 
		КолонкиБулево);
		
	Виды.Колонки.Добавить("Значение");
	
	Для каждого Вид Из Виды Цикл
		Вид.Значение = Перечисления.ВидыПредставляемыхДокументов[Вид.Имя];
	КонецЦикла; 
	
	Возврат Виды;
		
КонецФункции

Функция ДанныеМакетаЧерезПостроительЗапроса(
		Макет, 
		КолонкиБулево) Экспорт

	ОбластьЯчеек = Макет.Область(1, 1, Макет.ВысотаТаблицы, Макет.ШиринаТаблицы);
	
    ПостроительЗапроса = Новый ПостроительЗапроса;
    ПостроительЗапроса.ИсточникДанных = Новый ОписаниеИсточникаДанных(ОбластьЯчеек);
	ПостроительЗапроса.Выполнить();
	
	Таблица = ПостроительЗапроса.Результат.Выгрузить();
	ПреобразоватьКолонкиСтрокиВБулево(Таблица, КолонкиБулево);
	
	Возврат Таблица;
		
КонецФункции

Процедура ПреобразоватьКолонкиСтрокиВБулево(Таблица, КолонкиБулево) Экспорт

	ПредобработкаВидовПредставляемыхДокументов(Таблица, КолонкиБулево); 
	
	Для каждого Строка Из Таблица Цикл
		ОбработкаВидовПредставляемыхДокументов(Строка, КолонкиБулево); 

	КонецЦикла; 
	
	ПостобработкаВидовПредставляемыхДокументов(Таблица, КолонкиБулево);
	
КонецПроцедуры

Функция КолонкиДляПреобразованияВБулево() Экспорт

	КолонкиБулево = Новый Массив;
	КолонкиБулево.Добавить("ЭтоНДС");
	КолонкиБулево.Добавить("ЕстьЭД");
	КолонкиБулево.Добавить("ОбязательноПодтверждение");
	
	Возврат КолонкиБулево;
		
КонецФункции

Процедура ПредобработкаВидовПредставляемыхДокументов(Таблица, КолонкиБулево)

	Для каждого Имя Из КолонкиБулево Цикл
		Таблица.Колонки.Добавить(Имя + "_Булево");
	КонецЦикла; 
	
КонецПроцедуры

Процедура ОбработкаВидовПредставляемыхДокументов(Строка, КолонкиБулево)
	
	Для каждого Имя Из КолонкиБулево Цикл
		Строка[Имя + "_Булево"] = Булево(Строка[Имя]);
	КонецЦикла; 
	
КонецПроцедуры

Процедура ПостобработкаВидовПредставляемыхДокументов(Виды, КолонкиБулево)
	
	Для каждого Имя Из КолонкиБулево Цикл
		Виды.Колонки.Удалить(Имя);
		Виды.Колонки[Имя + "_Булево"].Имя = Имя;
	КонецЦикла; 
	
КонецПроцедуры

#КонецОбласти

#Область ЗаполнениеСписковВыбора

Процедура ЗаполнитьСписокВыбораВидДокументаФНС(Элемент) Экспорт
	
	СписокВыбораВидДокументаФНС = Элемент.СписокВыбора;
	Таблица = ВидыПредставляемыхДокументов();
	Виды = Таблица.ВыгрузитьКолонку("Значение");
	Элемент.СписокВыбора.ЗагрузитьЗначения(Виды);
	
КонецПроцедуры

Процедура ЗаполнитьСписокВыбораНаправление(Элемент) Экспорт
	
	СписокВыбораНаправление = Элемент.СписокВыбора;
	СписокВыбораНаправление.Добавить(Перечисления.НаправленияДокументаПоТребованиюФНС.Выдан);
	СписокВыбораНаправление.Добавить(Перечисления.НаправленияДокументаПоТребованиюФНС.Получен);
	
КонецПроцедуры

Процедура ЗаполнитьСписокВыбораГотовность(Элемент) Экспорт
	
	СписокВыбораГотовность = Элемент.СписокВыбора;
	СписокВыбораГотовность.Добавить("Готов");
	СписокВыбораГотовность.Добавить("Не готов");
	
КонецПроцедуры

#КонецОбласти

#Область ОбщиеМетоды

Процедура УстановитьОтборыДляРежимаВыбора(Форма) Экспорт
	
	Параметры = Форма.Параметры;
	Список = Форма.Список;
	
	Если ЗначениеЗаполнено(Параметры.ОтборОрганизация) Тогда
		ОтборОрганизация 		= Параметры.ОтборОрганизация;
		ЭлектронныйДокументооборотСКонтролирующимиОрганамиКлиентСервер.УстановитьОтборСписка(Список.Отбор, "Организация", ОтборОрганизация); 
	КонецЕсли;
	
	Если ЗначениеЗаполнено(Параметры.ОтборНаправление) Тогда
		ОтборНаправление = Параметры.ОтборНаправление;
		
		Если ОтборНаправление = ПредопределенноеЗначение("Перечисление.НаправленияДокументаПоТребованиюФНС.Получен") Тогда
			ОтборНаправлениеЧисло = 1;
		ИначеЕсли ОтборНаправление = ПредопределенноеЗначение("Перечисление.НаправленияДокументаПоТребованиюФНС.Выдан") Тогда
			ОтборНаправлениеЧисло = 2;
		КонецЕсли;
		
		ЭлектронныйДокументооборотСКонтролирующимиОрганамиКлиентСервер.УстановитьОтборСписка(Список.Отбор, "Направление", ОтборНаправлениеЧисло); 
		
	КонецЕсли;
	
	Если ЗначениеЗаполнено(Параметры.ОтборВидДокументаФНС) Тогда
		
		Если Параметры.ОтборВидДокументаФНС = Перечисления.ВидыПредставляемыхДокументов.КорректировочныйСчетФактура Тогда
			ОтборВидДокументаФНС 	= Перечисления.ВидыПредставляемыхДокументов.СчетФактура;
		ИначеЕсли Параметры.ОтборВидДокументаФНС = Перечисления.ВидыПредставляемыхДокументов.ДобавочныйЛистГрузовойТаможеннойДекларации Тогда
			ОтборВидДокументаФНС 	= Перечисления.ВидыПредставляемыхДокументов.ГрузоваяТаможеннаяДекларация;
		ИначеЕсли Параметры.ОтборВидДокументаФНС = Перечисления.ВидыПредставляемыхДокументов.ДополнениеКДоговору
			ИЛИ Параметры.ОтборВидДокументаФНС = Перечисления.ВидыПредставляемыхДокументов.СпецификацияЦены Тогда
			ОтборВидДокументаФНС 	= Перечисления.ВидыПредставляемыхДокументов.Договор;
		Иначе
			ОтборВидДокументаФНС 	= Параметры.ОтборВидДокументаФНС;
		КонецЕсли;
		
		ЭлектронныйДокументооборотСКонтролирующимиОрганамиКлиентСервер.УстановитьОтборСписка(Список.Отбор, "ВидДокументаФНС", ОтборВидДокументаФНС); 
		
	КонецЕсли;

КонецПроцедуры
	
#КонецОбласти 

#КонецОбласти

#Область СПАРК

Функция ЭтоНужныйВидОтчетаСПАРК(СсылкаНаОтчет) Экспорт
	
	ОтправкаОтчетностиВИнтерфакс = "РегламентированнаяОтчетность.ЭлектронныйДокументооборотСКонтролирующимиОрганами.ОтправкаОтчетностиВИнтерфакс";
	Если ОбщегоНазначения.ПодсистемаСуществует(ОтправкаОтчетностиВИнтерфакс) Тогда
		МодульОтправкаРегОтчетовВСПАРК = ОбщегоНазначения.ОбщийМодуль("ОтправкаРегОтчетовВСПАРК");
		Возврат МодульОтправкаРегОтчетовВСПАРК.ЭтоНужныйВидОтчетаСПАРК(СсылкаНаОтчет);
	Иначе
		Возврат Ложь;
	КонецЕсли;
	
КонецФункции

Функция СвойстваОтчетаСПАРК(СсылкаНаОтчет) Экспорт
	
	ОтправкаОтчетностиВИнтерфакс = "РегламентированнаяОтчетность.ЭлектронныйДокументооборотСКонтролирующимиОрганами.ОтправкаОтчетностиВИнтерфакс";
	Если ОбщегоНазначения.ПодсистемаСуществует(ОтправкаОтчетностиВИнтерфакс) Тогда
		МодульОтправкаРегОтчетовВСПАРК = ОбщегоНазначения.ОбщийМодуль("ОтправкаРегОтчетовВСПАРК");
		Возврат МодульОтправкаРегОтчетовВСПАРК.СвойстваОтчетаСПАРК(СсылкаНаОтчет);
	КонецЕсли;
	
КонецФункции

#КонецОбласти

#Область ПроверкаТекстовогоСодержимогоНаСимволыXML

Функция ПроверитьЗаменитьНедопустимыеСимволы(ПроверяемаяСтрока)
	
	НайденныеСимволы = ПроверитьНеДопустимыеСимволы(ПроверяемаяСтрока);
	Результат = ЗаменитьНедопустимыеСимволы(ПроверяемаяСтрока, НайденныеСимволы);
	Возврат Результат;
	
КонецФункции	

Функция ПроверитьНедопустимыеСимволы(ПроверяемаяСтрока) Экспорт
	
	Результат = Новый Соответствие;
	
	СтрокаАнализа = ПроверяемаяСтрока;
	
	НовоеВхождение = Неопределено;
	Позиция = НайтиНедопустимыеСимволыXML(СтрокаАнализа);
	Пока Позиция <> 0 Цикл
		Если НовоеВхождение <> Неопределено И НовоеВхождение + Результат[НовоеВхождение] = Позиция Тогда
			Результат[НовоеВхождение] = Результат[НовоеВхождение] + 1;
		Иначе	
			НовоеВхождение = Позиция;
			Результат.Вставить(Позиция, 1);
		КонецЕсли;
		Позиция = НайтиНедопустимыеСимволыXML(СтрокаАнализа, Позиция + 1);
	КонецЦикла;
	
	Возврат Результат;
	
КонецФункции

Функция ЗаменитьНедопустимыеСимволы(ПроверяемаяСтрока, КлючиНайденныхСимволов, СимволЗамены = "?") Экспорт
	
	Результат = ПроверяемаяСтрока;
	
	Для Каждого СтрокаКлюча Из КлючиНайденныхСимволов Цикл
		Результат = Сред(Результат, 1, СтрокаКлюча.Ключ - 1)
					+ СтроковыеФункцииКлиентСервер.СформироватьСтрокуСимволов(СимволЗамены, СтрокаКлюча.Значение)
					+ Сред(Результат, СтрокаКлюча.Ключ + СтрокаКлюча.Значение);
	КонецЦикла;
	
	Возврат Результат;
	
КонецФункции	

#КонецОбласти

#Область ТранспортныеСообщенияКРасшифровке

Функция ПолучитьСообщенияДляРасшифровки(МассивСообщений) Экспорт
	
	Результат = Новый Структура();
	Результат.Вставить("Отказ", Ложь);
	Результат.Вставить("ОтключеноПодтверждение", Ложь);
	Результат.Вставить("ИтогиПоСообщениям", Новый Массив);
	
	Запрос = Новый Запрос;
	Запрос.Текст = 
	"ВЫБРАТЬ РАЗРЕШЕННЫЕ
	|	ТранспортноеСообщение.ЦиклОбмена.Организация КАК Организация,
	|	КОЛИЧЕСТВО(РАЗЛИЧНЫЕ ТранспортноеСообщение.Ссылка) КАК КоличествоСообщений
	|ПОМЕСТИТЬ ВсеОрганизации
	|ИЗ
	|	Документ.ТранспортноеСообщение КАК ТранспортноеСообщение
	|ГДЕ
	|	ТранспортноеСообщение.Ссылка В(&Ссылка)
	|	И НЕ ТранспортноеСообщение.УчетнаяЗапись.ОтключитьАвтообмен
	|	И НЕ ТранспортноеСообщение.ЦиклОбмена.Организация.УчетнаяЗаписьОбмена = &ПустаяУчетнаяЗапись
	|
	|СГРУППИРОВАТЬ ПО
	|	ТранспортноеСообщение.ЦиклОбмена.Организация
	|;
	|
	|////////////////////////////////////////////////////////////////////////////////
	|ВЫБРАТЬ РАЗРЕШЕННЫЕ
	|	ВсеОрганизации.Организация КАК Организация,
	|	ВсеОрганизации.КоличествоСообщений КАК КоличествоСообщений,
	|	УчетныеЗаписиДокументооборота.Ссылка КАК УчетнаяЗапись,
	|	ЕСТЬNULL(УчетныеЗаписиДокументооборота.ЭлектроннаяПодписьВМоделиСервиса, ЛОЖЬ) КАК ЭлектроннаяПодписьВМоделиСервиса
	|ИЗ
	|	ВсеОрганизации КАК ВсеОрганизации
	|		ЛЕВОЕ СОЕДИНЕНИЕ Справочник.УчетныеЗаписиДокументооборота КАК УчетныеЗаписиДокументооборота
	|		ПО ВсеОрганизации.Организация.УчетнаяЗаписьОбмена = УчетныеЗаписиДокументооборота.Ссылка";
	Запрос.УстановитьПараметр("Ссылка", МассивСообщений);
	Запрос.УстановитьПараметр("ПустаяУчетнаяЗапись", Справочники.УчетныеЗаписиДокументооборота.ПустаяСсылка());
	
	РезультатЗапроса = Запрос.Выполнить().Выгрузить();
	
	Если РезультатЗапроса.Количество() = 0 Тогда 
		Результат.Отказ = Истина;
		Возврат Результат;
	КонецЕсли;
	
	ОтключеноПодтверждение = Истина;
	
	Для Каждого СтрокаРезультата Из РезультатЗапроса Цикл
		
		НоваяСтрока = Новый Структура("Организация, КоличествоСообщений");
		ЗаполнитьЗначенияСвойств(НоваяСтрока, СтрокаРезультата);
		Если СтрокаРезультата.ЭлектроннаяПодписьВМоделиСервиса Тогда
			ОтключеноПодтверждение = ОтключеноПодтверждение И ОтключеноПодтверждение(СтрокаРезультата.УчетнаяЗапись);
		Иначе
			ОтключеноПодтверждение = Ложь;
		КонецЕсли;
		
		Результат.ИтогиПоСообщениям.Добавить(НоваяСтрока);
		
	КонецЦикла;
	
	Результат.ОтключеноПодтверждение = ОтключеноПодтверждение;
	
	Возврат Результат;
	
КонецФункции

Функция СобратьСообщенияДляРасшифровки(ИтогиПоСообщениям) Экспорт
	
	ТипЧисло = ОбщегоНазначения.ОписаниеТипаЧисло(10);
	
	МассивОрганизаций = Новый Массив;
	ТаблицаРасшифровкиПоОрганизациям = Новый ТаблицаЗначений;
	ТаблицаРасшифровкиПоОрганизациям.Колонки.Добавить("Отметка", Новый ОписаниеТипов("Булево"));
	ТаблицаРасшифровкиПоОрганизациям.Колонки.Добавить("Организация", Новый ОписаниеТипов("СправочникСсылка.Организации"));
	ТаблицаРасшифровкиПоОрганизациям.Колонки.Добавить("КоличествоСообщений", ТипЧисло);
	ТаблицаРасшифровкиПоОрганизациям.Колонки.Добавить("ТекстСообщения", Новый ОписаниеТипов("Булево"));
	ТаблицаРасшифровкиПоОрганизациям.Колонки.Добавить("КоличествоРасшифрованных", ТипЧисло);
	ТаблицаРасшифровкиПоОрганизациям.Колонки.Добавить("КоличествоНеРасшифрованных", ТипЧисло);
	
	ТребующиеРасшифровки = Новый ТаблицаЗначений;
	ТребующиеРасшифровки.Колонки.Добавить("ТранспортноеСообщение", Новый ОписаниеТипов("ДокументСсылка.ТранспортноеСообщение"));
	ТребующиеРасшифровки.Колонки.Добавить("Организация", Новый ОписаниеТипов("СправочникСсылка.Организации"));
	ТребующиеРасшифровки.Колонки.Добавить("ТипТранспортногоСообщения", Новый ОписаниеТипов("ПеречислениеСсылка.ТипыТранспортныхСообщений"));
	
	Для Каждого СтрокаМассива Из ИтогиПоСообщениям Цикл
		Если МассивОрганизаций.Найти(СтрокаМассива.Организация) = Неопределено Тогда
			МассивОрганизаций.Добавить(СтрокаМассива.Организация);
		КонецЕсли;
		НоваяСтрока = ТаблицаРасшифровкиПоОрганизациям.Добавить();
		НоваяСтрока.Отметка = Истина;
		ЗаполнитьЗначенияСвойств(НоваяСтрока, СтрокаМассива);
	КонецЦикла;
	
	Запрос = Новый Запрос;
	Запрос.Текст = 
	"ВЫБРАТЬ РАЗРЕШЕННЫЕ РАЗЛИЧНЫЕ
	|	СвойстваТранспортныхСообщений.ТранспортноеСообщение.ЦиклОбмена.Организация КАК Организация,
	|	СвойстваТранспортныхСообщений.ТранспортноеСообщение КАК Ссылка
	|ИЗ
	|	РегистрСведений.СвойстваТранспортныхСообщений КАК СвойстваТранспортныхСообщений
	|ГДЕ
	|	СвойстваТранспортныхСообщений.ТранспортноеСообщение.ЦиклОбмена.Организация В(&Организации)
	|	И НЕ СвойстваТранспортныхСообщений.ТранспортноеСообщение.УчетнаяЗапись.ОтключитьАвтообмен
	|	И НЕ СвойстваТранспортныхСообщений.ТранспортноеСообщение.ЦиклОбмена.Организация.УчетнаяЗаписьОбмена = &ПустаяУчетнаяЗапись
	|	И СвойстваТранспортныхСообщений.ТребуетРасшифровки";
	Запрос.УстановитьПараметр("ПустаяУчетнаяЗапись", 	Справочники.УчетныеЗаписиДокументооборота.ПустаяСсылка());
	Запрос.УстановитьПараметр("Организации", 			МассивОрганизаций);
	
	Выборка = Запрос.Выполнить().Выбрать();
	Пока Выборка.Следующий() Цикл 
		НоваяСтрока = ТребующиеРасшифровки.Добавить();
		НоваяСтрока.Организация = Выборка.Организация;
		НоваяСтрока.ТранспортноеСообщение = Выборка.Ссылка;
		НоваяСтрока.ТипТранспортногоСообщения = Выборка.Ссылка.Тип;
	КонецЦикла;
	
	// уточнение количества собранных вх.сообщений
	ПромежуточнаяТаблица = ТребующиеРасшифровки.Скопировать();
	ПромежуточнаяТаблица.Свернуть("Организация");
	
	Для Каждого СтрокаСвертки ИЗ ПромежуточнаяТаблица Цикл 
		
		Отбор = Новый Структура("Организация", СтрокаСвертки.Организация);
		
		РезультатОтбора = ТаблицаРасшифровкиПоОрганизациям.НайтиСтроки(Отбор);
		Если РезультатОтбора.Количество() > 0 Тогда 
			ОтборТребующихРасшифровки = ТребующиеРасшифровки.НайтиСтроки(Отбор);
			РезультатОтбора[0].КоличествоСообщений = ОтборТребующихРасшифровки.Количество();
		КонецЕсли;
		
	КонецЦикла;
	
	Если ТребующиеРасшифровки.Количество() = 0 Тогда
		Результат = Неопределено;
		
	Иначе	
		АдресТребующиеРасшифровки = ПоместитьВоВременноеХранилище(ТребующиеРасшифровки, Новый УникальныйИдентификатор);
			
		АдресТаблицаРасшифровкиПоОрганизациям = ПоместитьВоВременноеХранилище(ТаблицаРасшифровкиПоОрганизациям, Новый УникальныйИдентификатор);
		
		Результат = Новый Структура();
		Результат.Вставить("АдресТребующиеРасшифровки", АдресТребующиеРасшифровки);
		Результат.Вставить("АдресТаблицаРасшифровкиПоОрганизациям", АдресТаблицаРасшифровкиПоОрганизациям);
		
	КонецЕсли;
	
	Возврат Результат;

КонецФункции

Функция ОтключеноПодтверждение(УчетнаяЗапись)
	
	Результат = Ложь;
	
	ДанныеУчетнойЗаписи = Новый Структура("ЭлектроннаяПодписьВМоделиСервиса, СертификатРуководителя", Ложь, "");
	
	Если ЭлектроннаяПодписьВМоделиСервиса.ИспользованиеВозможно() Тогда
		ДанныеУчетнойЗаписи = ОбщегоНазначения.ЗначенияРеквизитовОбъекта(УчетнаяЗапись, "ЭлектроннаяПодписьВМоделиСервиса, СертификатРуководителя");
	КонецЕсли;
	
	Если ДанныеУчетнойЗаписи.ЭлектроннаяПодписьВМоделиСервиса Тогда
		СвойствоСертификата = Новый Структура("Отпечаток", ДанныеУчетнойЗаписи.СертификатРуководителя);
		НастройкиПодтверждения = ЭлектроннаяПодписьВМоделиСервисаБРОВызовСервера.СвойстваРасшифрованияПодписанияСертификата(СвойствоСертификата);
		Результат = НастройкиПодтверждения.СпособПодтвержденияКриптоопераций = ПредопределенноеЗначение("Перечисление.СпособыПодтвержденияКриптоопераций.ДолговременныйТокен");
	КонецЕсли;
	
	Возврат Результат;
	
КонецФункции	

Функция РасшифроватьТранспортныеСообщенияВФоне(ТребующиеРасшифровки, АдресРезультата) Экспорт
	
	Результат = Новый Структура;
	Результат.Вставить("Выполнено", Истина);
	Результат.Вставить("ВсегоРасшифровано", 0);
	
	Для Каждого СтрокаМассива Из ТребующиеРасшифровки Цикл
		Выполнено = РасшифроватьТранспортноеСообщение(СтрокаМассива);
		Если Выполнено Тогда
			Результат.ВсегоРасшифровано = Результат.ВсегоРасшифровано + 1;
		КонецЕсли;
		Результат.Выполнено = Результат.Выполнено И Выполнено;
	КонецЦикла;
	
	Если АдресРезультата <> Неопределено Тогда
		ПоместитьВоВременноеХранилище(Результат, АдресРезультата);
	КонецЕсли;	
	
	Возврат Результат;
	
КонецФункции

#КонецОбласти

Функция ВидимостьКнопкиВыгрузкиОтветаИОНИПодписи(Запрос) Экспорт

	Если ТипЗнч(Запрос) = Тип("ДокументСсылка.ЗапросНаИнформационноеОбслуживаниеНалогоплательщика") Тогда
		Ответы            = ПолучитьОтветыНаЗапросИОН(Запрос); 
		ПоказатьКнопку    = Ответы.Количество() > 0;
	Иначе
		ПоказатьКнопку    = Ложь;
	КонецЕсли;
	
	Возврат ПоказатьКнопку;

КонецФункции

// Формирует список организаций доступных пользоветелю для обмена
//
// Возвращаемое значение:
//	ТаблицаЗначений с колонками
//		* Организация	- СправочникСсылка.Организация
//		* Ограничение 	- Булево
//
Функция ОрганизацииСОграничениемПрав() Экспорт
	
	Результат = Новый ТаблицаЗначений;
	
	ТекстЗапроса = 
	"ВЫБРАТЬ РАЗРЕШЕННЫЕ
	|	Организации.УчетнаяЗаписьОбмена КАК УчетнаяЗаписьОбмена,
	|	Организации.Ссылка КАК Ссылка,
	|	Организации.Наименование КАК Наименование
	|ПОМЕСТИТЬ ВсеОрганизации
	|ИЗ
	|	Справочник.Организации КАК Организации
	|ГДЕ
	|	Организации.ПометкаУдаления = ЛОЖЬ
	|	И Организации.ВидОбменаСКонтролирующимиОрганами = ЗНАЧЕНИЕ(Перечисление.ВидыОбменаСКонтролирующимиОрганами.ОбменВУниверсальномФормате)
	|	И Организации.УчетнаяЗаписьОбмена <> ЗНАЧЕНИЕ(Справочник.УчетныеЗаписиДокументооборота.ПустаяСсылка)
	|;
	|
	|////////////////////////////////////////////////////////////////////////////////
	|ВЫБРАТЬ
	|	ВсеОрганизации.Ссылка КАК Ссылка,
	|	НЕ РазрешенныеОрганизации.Ссылка ЕСТЬ NULL КАК НетОграничения
	|ИЗ
	|	ВсеОрганизации КАК ВсеОрганизации
	|		ЛЕВОЕ СОЕДИНЕНИЕ ВсеОрганизации КАК РазрешенныеОрганизации
	|		ПО ВсеОрганизации.Ссылка = РазрешенныеОрганизации.Ссылка
	|			И (РазрешенныеОрганизации.УчетнаяЗаписьОбмена В (&ТекущиеУчетныеЗаписиНалогоплательщика))
	|
	|УПОРЯДОЧИТЬ ПО
	|	ВсеОрганизации.Наименование";
	
	Запрос = Новый Запрос(ТекстЗапроса);
	Запрос.УстановитьПараметр("ТекущиеУчетныеЗаписиНалогоплательщика", ПараметрыСеанса.ТекущиеУчетныеЗаписиНалогоплательщика);
	Результат = Запрос.Выполнить().Выгрузить();
	
	Возврат Результат;
	
КонецФункции

#КонецОбласти

#Область ОсновнойРаздел
СоответствиеТипаСообщенияКраткомуПредставлению = Новый Соответствие;
// ФНС 534
СоответствиеТипаСообщенияКраткомуПредставлению.Вставить(Перечисления.ТипыТранспортныхСообщений.ДекларацияНП, "Отчет");
СоответствиеТипаСообщенияКраткомуПредставлению.Вставить(Перечисления.ТипыТранспортныхСообщений.ПодтверждениеДекларацияНО, "Подтверждение даты отправки");
СоответствиеТипаСообщенияКраткомуПредставлению.Вставить(Перечисления.ТипыТранспортныхСообщений.ИзвещениеПодтверждениеНП, "Извещение на подтверждение даты отправки");
СоответствиеТипаСообщенияКраткомуПредставлению.Вставить(Перечисления.ТипыТранспортныхСообщений.ИзвещениеДекларацияНО, "Извещение о получении отчета");
СоответствиеТипаСообщенияКраткомуПредставлению.Вставить(Перечисления.ТипыТранспортныхСообщений.РезультатПриемаДекларацияНО, "Результат приема");
СоответствиеТипаСообщенияКраткомуПредставлению.Вставить(Перечисления.ТипыТранспортныхСообщений.ИзвещениеРезультатПриемаНП, "Извещение о получении результата приема");
СоответствиеТипаСообщенияКраткомуПредставлению.Вставить(Перечисления.ТипыТранспортныхСообщений.РезультатОбработкиДекларацияНО, "Результат обработки");
СоответствиеТипаСообщенияКраткомуПредставлению.Вставить(Перечисления.ТипыТранспортныхСообщений.ИзвещениеРезультатОбработкиНП, "Извещение о получении результата обработки");

//ФНС Документ
СоответствиеТипаСообщенияКраткомуПредставлению.Вставить(Перечисления.ТипыТранспортныхСообщений.ДокументНО, "Документ");
СоответствиеТипаСообщенияКраткомуПредставлению.Вставить(Перечисления.ТипыТранспортныхСообщений.ИзвещениеДокументНП, "Извещение о получении документа");
СоответствиеТипаСообщенияКраткомуПредставлению.Вставить(Перечисления.ТипыТранспортныхСообщений.РезультатПриемаДокументНП,"Результат приема документа");
СоответствиеТипаСообщенияКраткомуПредставлению.Вставить(Перечисления.ТипыТранспортныхСообщений.ИзвещениеРезультатПриемаНО, "Извещение о получении результата приема документа");

//ФНС Заявление
СоответствиеТипаСообщенияКраткомуПредставлению.Вставить(Перечисления.ТипыТранспортныхСообщений.ЗаявлениеНП,						"Отчет");
СоответствиеТипаСообщенияКраткомуПредставлению.Вставить(Перечисления.ТипыТранспортныхСообщений.ПодтверждениеЗаявлениеНО,		"Подтверждение даты отправки");
СоответствиеТипаСообщенияКраткомуПредставлению.Вставить(Перечисления.ТипыТранспортныхСообщений.ИзвещениеЗаявлениеНО,			"Извещение о получении отчета");
СоответствиеТипаСообщенияКраткомуПредставлению.Вставить(Перечисления.ТипыТранспортныхСообщений.РезультатПриемаЗаявлениеНО,		"Результат приема");

СоответствиеТипаСообщенияКраткомуПредставлению.Вставить(Перечисления.ТипыТранспортныхСообщений.РезультатОбработкиЗаявлениеНО, 	"Результат обработки заявления");

СоответствиеТипаСообщенияКраткомуПредставлению.Вставить(Перечисления.ТипыТранспортныхСообщений.РезультатОбработкиЗаявлениеРФНО, "Результат обработки в РФ");
СоответствиеТипаСообщенияКраткомуПредставлению.Вставить(Перечисления.ТипыТранспортныхСообщений.ИзвещениеРезультатОбработкиРФНП,	"Извещение о получении результата обработки в РФ");
СоответствиеТипаСообщенияКраткомуПредставлению.Вставить(Перечисления.ТипыТранспортныхСообщений.СообщениеОбОтзывеЗаявлениеРФНО,	"Сообщение об отзыве заявления в РФ");
СоответствиеТипаСообщенияКраткомуПредставлению.Вставить(Перечисления.ТипыТранспортныхСообщений.ИзвещениеОбОтзывеЗаявлениеРФНП,	"Извещение о получении сообщения об отзыве заявления в РФ");
СоответствиеТипаСообщенияКраткомуПредставлению.Вставить(Перечисления.ТипыТранспортныхСообщений.РезультатОбработкиЗаявлениеТСНО,	"Результат обработки заявления в ТС");
СоответствиеТипаСообщенияКраткомуПредставлению.Вставить(Перечисления.ТипыТранспортныхСообщений.ИзвещениеРезультатОбработкиТСНП,	"Извещение о получении результата обработки заявления в ТС");

// ФНС 534 2-НДФЛ
СоответствиеТипаСообщенияКраткомуПредставлению.Вставить(Перечисления.ТипыТранспортныхСообщений.Форма2НДФЛНП, "Форма 2-НДФЛ");
СоответствиеТипаСообщенияКраткомуПредставлению.Вставить(Перечисления.ТипыТранспортныхСообщений.ПодтверждениеФорма2НДФЛНО, "Подтверждение даты отправки");
СоответствиеТипаСообщенияКраткомуПредставлению.Вставить(Перечисления.ТипыТранспортныхСообщений.ИзвещениеФорма2НДФЛНО, "Извещение о получении формы 2-НДФЛ");
СоответствиеТипаСообщенияКраткомуПредставлению.Вставить(Перечисления.ТипыТранспортныхСообщений.РезультатПриемаФорма2НДФЛНО, "Результат приема");

СоответствиеТипаСообщенияКраткомуПредставлению.Вставить(Перечисления.ТипыТранспортныхСообщений.УведомлениеОбОшибкеФНС, "Уведомление об ошибке");

// ФНС 534 НФД
СоответствиеТипаСообщенияКраткомуПредставлению.Вставить(Перечисления.ТипыТранспортныхСообщений.ОбращениеНП, "Письмо");
СоответствиеТипаСообщенияКраткомуПредставлению.Вставить(Перечисления.ТипыТранспортныхСообщений.ПодтверждениеОбращениеНО, "Подтверждение даты отправки");
СоответствиеТипаСообщенияКраткомуПредставлению.Вставить(Перечисления.ТипыТранспортныхСообщений.ИзвещениеОбращениеНО, "Извещение о получении письма");
СоответствиеТипаСообщенияКраткомуПредставлению.Вставить(Перечисления.ТипыТранспортныхСообщений.РезультатПриемаОбращениеНО, "Результат приема");

// ФНС Представление
СоответствиеТипаСообщенияКраткомуПредставлению.Вставить(Перечисления.ТипыТранспортныхСообщений.ПредставлениеНП, "Представление");
СоответствиеТипаСообщенияКраткомуПредставлению.Вставить(Перечисления.ТипыТранспортныхСообщений.ПодтверждениеПредставлениеНО, "Подтверждение даты отправки");
СоответствиеТипаСообщенияКраткомуПредставлению.Вставить(Перечисления.ТипыТранспортныхСообщений.ИзвещениеПредставлениеНО, "Извещение о получении представления");
СоответствиеТипаСообщенияКраткомуПредставлению.Вставить(Перечисления.ТипыТранспортныхСообщений.РезультатПриемаПредставлениеНО, "Результат приема");

СоответствиеТипаСообщенияКраткомуПредставлению.Вставить(Перечисления.ТипыТранспортныхСообщений.ПисьмоНО, "Письмо");
СоответствиеТипаСообщенияКраткомуПредставлению.Вставить(Перечисления.ТипыТранспортныхСообщений.ИзвещениеПисьмоНП, "Извещение о получении письма");

СоответствиеТипаСообщенияКраткомуПредставлению.Вставить(Перечисления.ТипыТранспортныхСообщений.РассылкаНО, "Рассылка");
СоответствиеТипаСообщенияКраткомуПредставлению.Вставить(Перечисления.ТипыТранспортныхСообщений.ИзвещениеРассылкаНП, "Извещение о получении рассылки");

// ФНС 534 ИОН
СоответствиеТипаСообщенияКраткомуПредставлению.Вставить(Перечисления.ТипыТранспортныхСообщений.ЗапросНП, "Запрос");
СоответствиеТипаСообщенияКраткомуПредставлению.Вставить(Перечисления.ТипыТранспортныхСообщений.ПодтверждениеЗапросНО, "Подтверждение даты отправки");
СоответствиеТипаСообщенияКраткомуПредставлению.Вставить(Перечисления.ТипыТранспортныхСообщений.ИзвещениеЗапросНО, "Извещение о получении запроса");
СоответствиеТипаСообщенияКраткомуПредставлению.Вставить(Перечисления.ТипыТранспортныхСообщений.РезультатПриемаЗапросНО, "Результат приема");
СоответствиеТипаСообщенияКраткомуПредставлению.Вставить(Перечисления.ТипыТранспортныхСообщений.РезультатОбработкиЗапросНО, "Результат обработки");

// ПФР
СоответствиеТипаСообщенияКраткомуПредставлению.Вставить(Перечисления.ТипыТранспортныхСообщений.ПервичноеСообщениеСодержащееОтчетностьПФР, "Отчет");
СоответствиеТипаСообщенияКраткомуПредставлению.Вставить(Перечисления.ТипыТранспортныхСообщений.ПодтверждениеПолученияОтчетностиПФР, "Подтверждение получения");
СоответствиеТипаСообщенияКраткомуПредставлению.Вставить(Перечисления.ТипыТранспортныхСообщений.ПротоколПФР, "Протокол");
СоответствиеТипаСообщенияКраткомуПредставлению.Вставить(Перечисления.ТипыТранспортныхСообщений.ПротоколКвитанцияПФР, "Квитанция на протокол");

СоответствиеТипаСообщенияКраткомуПредставлению.Вставить(Перечисления.ТипыТранспортныхСообщений.ПисьмоИсходящееПФР, "Исходящее письмо");
СоответствиеТипаСообщенияКраткомуПредставлению.Вставить(Перечисления.ТипыТранспортныхСообщений.ПисьмоИсходящееКвитанцияПФР, "Квитанция на исходящее письмо");

СоответствиеТипаСообщенияКраткомуПредставлению.Вставить(Перечисления.ТипыТранспортныхСообщений.ПисьмоВходящееПФР, "Входящее письмо");
СоответствиеТипаСообщенияКраткомуПредставлению.Вставить(Перечисления.ТипыТранспортныхСообщений.ПисьмоВходящееКвитанцияПФР, "Квитанция на входящее письмо");

СоответствиеТипаСообщенияКраткомуПредставлению.Вставить(Перечисления.ТипыТранспортныхСообщений.ПервичноеСообщениеСодержащееЗапросПФР, "Запрос");
СоответствиеТипаСообщенияКраткомуПредставлению.Вставить(Перечисления.ТипыТранспортныхСообщений.ПодтверждениеПолученияЗапросаПФР, "Подтверждение получения");
СоответствиеТипаСообщенияКраткомуПредставлению.Вставить(Перечисления.ТипыТранспортныхСообщений.ОтветНаЗапросПФР, "Ответ на запрос");
СоответствиеТипаСообщенияКраткомуПредставлению.Вставить(Перечисления.ТипыТранспортныхСообщений.ОтветНаЗапросКвитанцияПФР, "Квитанция на ответ на запрос");

СоответствиеТипаСообщенияКраткомуПредставлению.Вставить(Перечисления.ТипыТранспортныхСообщений.УведомлениеОбОшибкеПФР, "Уведомление об ошибке");

// Росстат
СоответствиеТипаСообщенияКраткомуПредставлению.Вставить(Перечисления.ТипыТранспортныхСообщений.ПервичноеСообщениеСодержащееОтчетностьФСГС, "Отчет");
СоответствиеТипаСообщенияКраткомуПредставлению.Вставить(Перечисления.ТипыТранспортныхСообщений.ПодтверждениеДатыОтправкиФСГС, "Подтверждение даты отправки");
СоответствиеТипаСообщенияКраткомуПредставлению.Вставить(Перечисления.ТипыТранспортныхСообщений.ИзвещениеПодтверждениеДатыОтправкиФСГС, "Извещение о получении подтверждения даты отправки");
СоответствиеТипаСообщенияКраткомуПредставлению.Вставить(Перечисления.ТипыТранспортныхСообщений.ИзвещениеОПолученииОтчетностиФСГС, "Извещение о получении отчетности");
СоответствиеТипаСообщенияКраткомуПредставлению.Вставить(Перечисления.ТипыТранспортныхСообщений.ПротоколВходногоКонтроляОтчетностиФСГС, "Протокол");
СоответствиеТипаСообщенияКраткомуПредставлению.Вставить(Перечисления.ТипыТранспортныхСообщений.ИзвещениеПротоколВходногоКонтроляОтчетностиФСГС, "Извещение о получении протокола");

СоответствиеТипаСообщенияКраткомуПредставлению.Вставить(Перечисления.ТипыТранспортныхСообщений.ПисьмоИсходящееФСГС, "Исходящее письмо");
СоответствиеТипаСообщенияКраткомуПредставлению.Вставить(Перечисления.ТипыТранспортныхСообщений.ИзвещениеПисьмоИсходящееФСГС, "Извещение о получении письма его получателем");
СоответствиеТипаСообщенияКраткомуПредставлению.Вставить(Перечисления.ТипыТранспортныхСообщений.ПисьмоВходящееФСГС, "Входящее письмо");
СоответствиеТипаСообщенияКраткомуПредставлению.Вставить(Перечисления.ТипыТранспортныхСообщений.РассылкаФСГС, "Рассылка");
СоответствиеТипаСообщенияКраткомуПредставлению.Вставить(Перечисления.ТипыТранспортныхСообщений.РассылкаШаблоновФСГС, "Рассылка шаблонов");
СоответствиеТипаСообщенияКраткомуПредставлению.Вставить(Перечисления.ТипыТранспортныхСообщений.ИзвещениеПисьмоВходящееФСГС, "Извещение о получении письма его получателем");
СоответствиеТипаСообщенияКраткомуПредставлению.Вставить(Перечисления.ТипыТранспортныхСообщений.УведомлениеОбОшибкеФСГС, "Уведомление об ошибке");
//
СоответствиеТипаСообщенияКраткомуПредставлению.Вставить(Перечисления.ТипыТранспортныхСообщений.КритическаяОшибка, "Критическая ошибка");
СоответствиеТипаСообщенияКраткомуПредставлению.Вставить(Перечисления.ТипыТранспортныхСообщений.ПустаяСсылка(), "Неизвестный тип");

#КонецОбласти

#КонецЕсли